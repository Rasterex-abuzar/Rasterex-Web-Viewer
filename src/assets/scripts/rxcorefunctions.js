var RxCoreMulti = (function() {
    RxCore = function () {
    'use strict';
    var globalscope = this;
    /* Andriy: aliases from old names */
    /* I simply do not want to change this in the code, just make aliases */

    var enableCycleSelect = false;

    var xmlurl = RxConfig.xmlurl;
    var xmlurldirect = RxConfig.xmlurldirect;
    var xmlurlmarkup = RxConfig.xmlurlmarkup;
    var opensessionurl = RxConfig.opensessionurl;
    var openMsessionurl = RxConfig.openMsessionurl;
    var openUsessionurlEx = RxConfig.openUsessionurlEx;
    var openUsessionurl = RxConfig.openUsessionurl;
    var closesessionurl = RxConfig.closesessionurl;
    var xmlurlmarkupsave = RxConfig.xmlurlmarkupsave;
    //set to false when done debugging.
    var debugmarkuprect = false;

    var CompareFilesServer = RxConfig.serverComparefiles;

    var markupsaveEx = RxConfig.markupsave;
    var savefileEx = RxConfig.savefile;

    var FileuploadURL = RxConfig.FileuploadURL;

    var WebClientPDFUploadURL = RxConfig.WebClientPDFUpload;

    var PenTableUpload = RxConfig.PenTableUpload;
    var signaturePut = RxConfig.putSignature;
    var signatureGet = RxConfig.getSignature;

    var initialPut = RxConfig.putInitial;
    var initialGet = RxConfig.getInitial;



    var copieddocumentIndex = -1;
    var copiedPageIndex = -1;
    var copiedBlob = null;

    var markupdisplayonload = false;

    var PDFExportURL = RxConfig.PDFExportURL;

    var PDFExportPageURL = RxConfig.PDFExportPageURL;


    var serverJSONcommand = RxConfig.serverJSONcommand;

    var PDFPrintPrepare = RxConfig.PDFPrintPrepare;
    var PDFPrintCreate = RxConfig.PDFPrintCreate;
    var CanvasSaveUrl = RxConfig.CanvasSaveUrl;
    var UploadServerfolder = RxConfig.UploadServerfolder;
    var UploadServerfolderd = RxConfig.UploadServerfolderd;
    var xmlurlrel = RxConfig.xmlurlrel;
    var xmlurlrelmarkup = RxConfig.xmlurlrelmarkup;
    var uploadfolderURL = RxConfig.uploadfolderURL;
    var htmlviewerurl = RxConfig.htmlviewerurl;
    var splashscreen = RxConfig.splashscreen;
    var noteImgSrc = RxConfig.noteImgSrc;
    var bGetconfig = RxConfig.bGetconfig;
    var bUseID = RxConfig.bUseID;
    var configurationLocation = RxConfig.configurationLocation;
    
    
    var bUseJSONConfig = false;
    var ConfigurationJSON = null;

    var bwelcomedisabled = false;

    /* Andriy: aliases fromc old names: end */

    var userInfoURL = '';
    var bUseCustomrelpath = false;
    var bUsequickPageload = false;
    var bUsecustomConfig = false;

    //new gltf 3d adaption
    var bUseFloorplanLoad = false;
    var nFloorplanIndex = null;
    //new gltf 3d adaption


    var xmlurlrelcustom = "";

    /* Andriy: added init state */

    var initialized = false;
    var configset = false;
    var readycalled = false;
    var foxitreadycalled = false;


    var ndimprecision = 3;
    var nstartuppixelratio = window.devicePixelRatio;
    var brestartmessageshown = false;

    /* Andriy: end */

    var rxcoreversion = 35.927;
    var rxcontainer, canvas, context, canvaso, contexto, canvimg, cntximg, canvas3D, magcanvas, magctx, clipboardcnv, clipboardctx;
    var scene, camera, renderer;

    var rxprintcontainer = undefined;

    var uploadTempFile = undefined;

    /*var globalPlaneX = new THREE.Plane( new THREE.Vector3( 1, 0, 0 ), 0 );
    var globalPlaneY = new THREE.Plane( new THREE.Vector3( 0, 1, 0 ), 0 );
    var globalPlaneZ = new THREE.Plane( new THREE.Vector3( 0, 0, -1 ), 0 );
    var globalPlanes = [ globalPlaneX,globalPlaneY,globalPlaneZ ],*/
    var Empty = Object.freeze( [] );

    //var geometry, material, mesh;

    /* default offset for canvas */

    var defaultLayout = {
        offsetWidth:0,
        offsetHeight:0,
        absolute:false
    };

    var navthreesize = {
        width : 200,
        height : 200
    };


    var magnifysize = {
        width : 225,
        height : 150
    };

    var fixedScaleSize = {
        width : 1920,
        height : 1080
    };

    var msearchpadding = {
        x: 50,
        y: 50,
        w: 50,
        h: 100
    };


    /* export parameters for upload file */

    var defaultexportparams = {
            consolidateonly : false,
            format : "PDF",
            UPI : "0",
            paperSize : "A3",
            markupFlag : "1",
            overridefilename : "not used"
    };

    /* export parameters for upload file */


    /* GUI Connector object variables*/
    var RxCore_GUI_ActivateFile = undefined;
    var RxCore_GUI_Users = undefined;

    var RxCore_GUI_penTable = undefined;

    var RxCore_GUI_penTableJSON = undefined;

    var RxCore_GUI_getsignatureComplete = undefined;
    var RxCore_GUI_putsignatureComplete = undefined;

    var Rxcore_GUI_SymbolAddFailed = undefined;

    var RxCore_GUI_MarkupLayers = undefined;
    var RxCore_GUI_MarkupHover = undefined;
    var Rxcore_GUI_exportComplete = undefined;

    var RxCore_GUI_Servercompare = undefined;


    var RxCore_GUI_FileInfo = undefined;
    var Rxcore_GUI_fileLoadComplete = undefined;

    var Rxcore_GUI_search3Denabled = undefined;

    var Rxcore_GUI_fileLoadError = undefined;

    var RxCore_GUI_compareLoadComplete = undefined;

    var Rxcore_GUI_CanSaveTop3DView = undefined;

    var RxCore_GUI_CloseDocInternal = undefined;


    var RxCore_GUI_GuidRecieved = undefined;


    
    var RxCore_GUI_UndoRedoEmpty = undefined;
    
    var RxCore_GUI_RenderComplete = undefined;

    var RxCore_GUI_CreationComplete = undefined;

    
    //new gltf 3d adaption
    var RxCore_GUI_FileActivated = undefined;
    var Rxcore_GUI_floorLoadComplete = undefined;
    var RxCore_GUI_floorlevel = undefined;
    var Rxcore_GUI_markupLoadComplete = undefined;

    var RxCore_GUI_SVGLoad = undefined;
    var Rxcore_GUI_foxitmarkupLoadComplete = undefined;
    
    var RxCore_GUI_3DNavPosition = undefined;
    
    var RxCore_GUI_3DView = undefined;

    var Rxcore_GUI_pageLoadComplete = undefined;

    var Rxcore_GUI_scaleListLoadComplete = undefined;

    var Rxcore_GUI_PDFRenderComplete = undefined;
    var Rxcore_GUI_PDFPageRenderComplete = undefined;

    var Rxcore_GUI_PDFPassWord = undefined;

    var RxCore_GUI_pagethumbs = undefined;
    var RxCore_GUI_pagethumb = undefined;

    var RxCore_GUI_floorthumb = undefined;
    var RxCore_GUI_FloorplanCanvas = undefined;
    
    //new gltf 3d adaption

    
    var RxCore_GUI_birdseye = undefined;
    var Rxcore_GUI_pagedimLoadComplete = undefined;
    var RxCore_GUI_VectorLayers = undefined;
    var RxCore_GUI_VectorBlocks = undefined;
    var RxCore_GUI_VectorBlocksLoaded = undefined;
    var RxCore_GUI_3DParts = undefined;
    var RxCore_GUI_Read3DComplete = undefined;

    var Rxcore_GUI_3DCameraSave = undefined;

    var Rxcore_GUI_3DPartInfo = undefined;

    var Rxcore_GUI_3DPartState = undefined;

    var Rxcore_GUI_3DMarkupInfo = undefined;
    
    var RxCore_GUI_3DMarkupList = undefined;
    var RxCore_GUI_3DMarkup = undefined;

    var RxCore_GUI_3DCameraSaves = undefined;

    var RxCore_GUI_2DBlockID = undefined;
    var RxCore_GUI_2DBlockInfo = undefined;
    var RxCore_GUI_2DBlockInfoScreen = undefined;
    var RxCore_GUI_2DEntityInfoScreen = undefined;
    var RxCore_GUI_2DEntityInfo  = undefined;

    var RxCore_GUI_2DBlock = undefined;
    var RxCore_GUI_2DBlockHover = undefined;

    var RxCore_GUI_Markuplist = undefined;
    var RxCore_GUI_MarkuplistJSON = undefined;
    var RxCore_GUI_FoxitAnnotlist = undefined;
    var RxCore_GUI_Stamps = undefined;
    var RxCore_GUI_CustomStamps = undefined;
    var RxCore_GUI_Symbols = undefined;
    var RxCore_GUI_Measurediag = undefined;
    var RxCore_GUI_Calibratediag = undefined;
    var RxCore_GUI_CalibrateComplete = undefined;
    var RxCore_GUI_Textdiag = undefined;
    var RxCore_GUI_TextInput = undefined;
    var RxCore_GUI_Notediag = undefined;
    var RxCore_GUI_State = undefined;
    var RxCore_GUI_Page = undefined;

    var RxCore_GUI_3DView = undefined;

    var RxCore_GUI_Markup = undefined;

    var RxCore_GUI_MarkupChanged = undefined;

    var RxCore_GUI_MarkupIndex = undefined;
    var RxCore_GUI_MarkupLink = undefined;
    var RxCore_GUI_MarkupUnselect = undefined;
    
    var RxCore_GUI_MarkupDrawValue = undefined;

    var RxCore_GUI_Upload = undefined;
    var RxCore_GUI_CameraPos = undefined;
    var RxCore_GUI_UpdatebirdsEye = undefined;
    var RxCore_GUI_Download = undefined;
    var RxCore_GUI_Birdseye = undefined;
    var RxCore_GUI_Magnify = undefined;
    var RxCore_GUI_Resize = undefined;
    var RxCore_GUI_Resize_ext = undefined;
    var RxCore_GUI_3DWalkthrough = undefined;
    var RxCore_GUI_CompareDiag = undefined;
    var RxCore_GUI_ContextMenuEvt = undefined;
    var RxCore_GUI_Nav3DreplaceDiag = undefined;
    var RxCore_GUI_CompareAlign = undefined;
    var RxCore_GUI_Consolidate = undefined;
    var RxCore_GUI_ConsolidateMarkup = undefined;
    var RxCore_GUI_Permissions = undefined;
    var RxCore_GUI_PDFBookmarks = undefined;
    var RxCore_GUI_PagePosition = undefined;
    var RxCore_GUI_PanUpdate = undefined;
    var RxCore_GUI_PanUpdated = undefined;

    var RxCore_GUI_TextCopied = undefined;

    var RxCore_GUI_ZoomUpdate = undefined;
    var RxCore_GUI_ZoomUpdated = undefined;
    var RxCore_GUI_MarkupSave = undefined;

    var RxCore_GUI_MarkupPaths = undefined;

    var RxCore_GUI_Ready = undefined;
    var RxCore_GUI_FoxitReady = undefined;
    var RxCore_GUI_RotatePage = undefined;
    var RxCore_GUI_RotateDocument = undefined;
    var RxCore_GUI_PrintRect = undefined;
    var RxCore_GUI_ClipRect = undefined;

    var RxCore_GUI_HasText = undefined;
    var RxCore_GUI_HatchChange = undefined;
    var RxCore_GUI_NumMathces = undefined;

    var RxCore_GUI_DocumentSearch = undefined;
    var RxCore_GUI_RemovePage = undefined;
    var RxCore_GUI_PageCountChanged = undefined;

    var RxCore_GUI_NumMathcesMarkup = undefined;
    var RxCore_GUI_NumMathcesRect = undefined;
    
    var RxCore_GUI_inputEvent = undefined;

    var RxCore_GUI_MathcesRectsPage = undefined;

    var RxCore_GUI_printpage = undefined;
    var RxCore_GUI_printCompare = undefined;
    var RxCore_GUI_CompareMeasure = undefined;
    var RxCore_GUI_markupdrawParams = undefined;
    var RxCore_GUI_MarkupAreaEdit = undefined;
    var RxCore_GUI_markupParamsError = undefined;
    var RxCore_GUI_getResponsHeaders = undefined;

    var RxCore_GUI_MarkupMeasureRealTimeData = undefined;

    var bOverridelineWidth = true;
    var dOveridelinewidth = 0.5;
    var nSnapSize = 5;

    var bAlwaysassignUniqueID = false;

    var canvasowidth = 0;
    var canvasoheight = 0;

    var canvasowidthsize = 0;
    var canvasoheightsize = 0;

    var dragdropfilename = "";

    var bIsMSIE = false;

    var bloadpentable = false;

    var buploadconvert = false;

    var szdispvalue = "initial";
    var szLicenCompanyFeature = "";

    var mbirdseyeoffsetx = 0;
    var mbirdseyeoffsety = 0;

    var imagewidth = 0,
        imageheight = 0;
    var drotation = 0;
    var imageswitchfactor = 1.5;

    var glpixelRatio = 1;
    var glpixellinewidthratio = 1;

    var imageloaded = false;
    var documentopen = false;
    var suspendrender = false;
    var documentcompare = false;
    var doCompare = false;

    var bCameramove = false;
    var nMarkupUndoIdstart = 0;

    var compareMode = 0;
    //var markuploaded = false;
    //var bMarkupchanged = false;
    var bLicenseAquired = false;
    var bsetcallbackfilepages = false;

    var bUseMarkupScaling = false;

    var bUseCompanyFeature = false;
    var bPDFExportfullPath = false;
    
    var bexportmarkupincludeAll = true;

    var bMarkupSavecheck = true;
    var bmarkupsaveconfirm = false;
    var bUseFixedScale = true;
    var bUseNoScale = false;
    var bNoMarkuprotate = false;


    var bdotextscale = true;

    var bUsePostprocess = false;

    var buseFoxitSnap = true;

    var buseFoxitrotateTo = true;

    //var bUsemouseinput = true;
    var bPrintpageloaded = false;
    var bAbortPageload = false;
    var bUseCredentials = false;
    var bLimMarkupExtent = true;
    var bKeepVectorColor = false;
    var bMarkupPostOnly = false;
    var bSingleDocmode = false;
    var bDoReScaleOnSize = true;
    var bPDFZoomAllinit = false;
    var bSuspendDraw = false;
    var bRestrictPan = true;
    var bLimitZoomOut = true;
    var bScrollZoomOverlay = true;
    var bAnimatePDFrender = false;
    var bDisableMSIE11Eventcheck = false;
    var bAutoloadThumbnails = true;
    var bLabelsOff = false;
    var enablemarkupLabels = false;
    var bmarkupLabels = false;
    var benablemarkuhover = true;
    var benableblockhover = false;
    var breverse3dXrotate = false;
    var bmovepolyallowed = false;
    
    var bDrawToFoxitonly = false;

    var printWin = 0;
    var PDFExpWin = 0;
    var DocXMLPrint;

    var pageCanvases = [];
    var aDrawpages = [];
    var aDrawmarkup = [];
    var canvimages = [];

    var canvaspages = [];
 
    var currentParentMarkupNumber = 0;
 

    var hasnotprinted = true;

    var ThumbnailWidth = 180;
    var ThumbnailHeight = 250;


    var PaperWidth = 100;
    var PaperHeight = 100;
    var PrintRect = undefined;

    const paperwidthA4px = 2460;
    const paperheightA4px = 3480;

    var Paper;
    var PDFExportFileURL = "";
    var PrintPageURL = "printcanvas.htm";
    var LicenseID = "";
    var OEMFlag = '';

    var backgroundColor = "#FFFFFF";

    var szmarkColor = "rgba(236, 236, 236, 0.5)";
    var szmarkVectorColor = "rgba(0, 0, 0, 1)";
    
    const nVectorhighlightwidthconstant = 2;
    var nVectorhighlightwidth = 2;
    


    var backgroundCustomColor = "#FF0000";
    var bCustomColor = false;

    var bMultiselect = false;
    var busepantomarkup = false;

    var bmulti3dSelect = false;

    var bDrawNavCam = true;

    var displayBGColor = "rgb(62,62,62)";
    var nScrollKeyNum = 17;
    var nMaximageArea = 64000000;
    var nMaximageAreaIPad = 16777216;
    var nLargePDF = 30;
    var nMaxpages = 30;
    var bIsmobile = false;

    var overlayBGColor = '#0000ff';
    var overlayFGColor = '#ff8000';

    var bOpeninitialdoc = false;
    var OpeninitialdocURL = "";

    var initialDoc = {
        open : bOpeninitialdoc,
        url : OpeninitialdocURL,
        custom : false,
        filepath: OpeninitialdocURL,
        mime: null,
        cacheid: null,
        displayname: null,
        openfileobj: null
    };

    var bMarkupcrated = false;

    var bDefaultpentable = false;
    var DefaultpentableURL = "";
    var DefaultpentableJSON = [];


    var convertPDFAnnots = false;
    var proxyPDFAnnots = false;

    var bNoteFocus = false;
    var bUseScrollKey = false;
    
    var bScrollSingleDisable = false;

    var enableZoomOnScrollMultipage = false;


    var bOrthoOn = false;
    var bOrthoOnEdit = false;
    var nOrthoDegree = 90;
    var nMarkupcreated = -1;
    var bMultimarkupadd = false;
    var bAlwaysMarkupCallback = false;
    var bAnimateready = false;
    var bPDFtemprender = false;
    var bPDFtemprendermag = false;
    var bPDFtemprenderbirdseye = false;

    var lastTouchdata = -1;
    var Canvasheight = 0;
    var Canvaswidth = 0;

    var nlinestyleinterval = 0;
    var bAnimateLineStyle = false;
    //1 = metric, 2 = Imperial, 3 = System, 4=Custom
    var Unitofmeasure = 1;
    var Unitlabel = "mm";
    var AreaUnitlabel = "mm\u00B2";
    var DegreeLabel = "\u00B0";
    var MeasureScale = 1.0;
    var nCalibrateScale = 1.0;
    var nCalibrateMeasured = 0.0;
    var nCalibrateSet = 0.0;
    var nArrowSize = 20;
    var nanglearcrad = 50;

    var nPanlimit = 15;
    var nLabelTextsize = 12;

    var nDefaultFontSize = 18;

    var nCounterSize = 12;
    var nCalloutWidth = 100;

    var nCamerathumbwidth = 210;

    var bUsemarkupbyref = false;
    var bUsemarkupbyrefEx = false;

    var busesharepointmarkup = false;

    var busedbmarkup = false;

    //mm = 1 meter = 1000 for metric units.
    var unitscale = 1;
    var unitscaleValue = 1;
    var fTextScale = 0.95;

    var lineheightfactor = 0.75;
    //1 = Millimeter, 2 = Centimeter, 3 = Decimeter, 4=Meter,5=Kilometer,6=Nautical Miles
    //1 = Inch,2=Feet,3=Yard,4=Mile,5=Nautical Miles
    var SubmeasureUnit = 1;
    var nFractionRes = 32; // 1/32
    var bUseFraction = true;

    // 0 = large 1=small
    var imagesize = 0;

    var markupForScale;

    //array to hold markup objects
    var byrefmarkupfiles = [];
    var localfilesPending = [];

    var toolcursorArr = [
        {name : 'hide3d', cursor : 'crosshair'},
        {name : 'measure3D',cursor : 'crosshair'},
        {name : 'measurearc',cursor : 'crosshair'},
        {name : 'select3d',cursor : 'crosshair'},
        {name : 'Walkthrough3D',cursor : 'default'},
        {name : 'orbitControl',cursor : 'default'},
        {name : 'magnify',cursor : 'default'},
        {name : 'note',cursor : 'default'},
        {name : 'pencil',cursor : 'crosshair'},
        {name : 'rect',cursor : 'crosshair'},
        {name : 'count',cursor : 'crosshair'},
        {name : 'symbol',cursor : 'default'},
        {name : 'zoomwindow',cursor : 'crosshair'},
        {name : 'rectText',cursor : 'default'},
        {name : 'rectStamp',cursor : 'default'},
        {name : 'PickPolygon',cursor : 'crosshair'},
        {name : 'pan',cursor : 'move'},
        {name : 'selectText',cursor : 'text'},
        {name : 'markupedit',cursor : 'default'},
        {name : 'arrow',cursor : 'crosshair'},
        {name : 'callout',cursor : 'default'}, //arrow type 6
        {name : 'area',cursor : 'crosshair'},
        {name : 'measuretool',cursor : 'crosshair'},
        {name : 'measure',cursor : 'crosshair'},
        {name : 'calibrate',cursor : 'crosshair'},
        {name: 'link', cursor: 'default'},
        {name: 'interactiveStamp', cursor: 'default'}
    ];


    //var markuplist = [];
    //var markupundolist = [];
    var markupprintlist = [];
    //var markupfiles = [];
    var Userlist = [];
    var Stamplist = [];
    var rotatepagearrs = [];
    var numUsers = 0;
    var Layerlist = [];
    var HatchPtrns = [];

    var numLayers = 0;
    var configJSON = {};
    //var nummarkups = 0;
    //var curcontrol3D = 'orbitControl';

    //var SzNoteText = "";

    /* Andriy: start, reason: unused variables */
    /*
     //tool state flags
     var bZoomWindow = false;
     var bPan = false;
     var bMarkupEdit = false;
     var bCalibrate = false;
     var bMarkupHide = false;
     var bMarkuperase = false;
     var bMarkupText = false;
     var bMarkupNote = false;
     var bMarkupStamp = false;
     var bMarkupLine = false;
     var bMarkupArrow = false;
     var bMarkupShape = false;
     var bMarkupDimension = false;
     var bMarkupArea = false;
     var bMarkupMarker = false;
     var bMarkupOutline = true;
     var bMarkupEdged = false;
     var bMarkupFilled = false;
     var bMarkupHatched = false;
     var bMeasure = false;
     */
    /* Andriy: end, reason: unused variables */


    //global configuration variables
    var markuplayer = 1;
    var markupcolor = "#FF0000";
    var markupfillcolor = "#FF0000";
    var markuplinecolor = "#FF0000";
    var markuplinecolorSegment = "#FFFF00";
    var markuptextcolor = "#FF0000";

    var globaltransparency = 100;
    var signature = "Default";
    var DisplayName = "Default";
    var fontstylevalue = "Arial";

    var linewidthvalue = 4;
    var linewidthvalueSegment = 4;
    //this.linewidthSegment = linewidthvalueSegment;
    //var Linestylevalue = "_______";
    var HatchStyle = 0;
    var fillstyle = 0;
    var nlinestyle = 0;

    var bCanChangeLayer = true;
    var bCanChangeSignature = false;
    var bCanConsolidate = false;
    var bSetGlobal = false;
    var readonlymode = false;
    var disablemarkupsavecheck = false;


    var buseAuth = false;
    var buseEncToken = false;
    var AuthToken = "";

    var AuthType = "";
    var AuthName = "";
    var AuthPass = "";

    //var bGetconfig = true;
    var bRefreshmarkup = false;
    var bReverseScale = false;
    //var bMarkupLocked = false;
    var bMarkupNoLabel = false;
    var bConsolidate = false;
    var bShowDiffonly = false;

    var locationSearchSet = false;
    var szLocationSearch = "";
    var szLatestresponseHeaders = "";
    var szdrawmode = 'xor';


    //new gltf 3d adaption
    var textureobjects = [];

    const Drawthumbnail = 1;
    const Drawbirdseye = 2;
    const Drawfloorplan = 3;
    const Floorplanreplace = 4;
    //new gltf 3d adaption

    const snapEndpoint = 1;
    const snapIntersection = 2;
    const snapMidpoint = 3;

    const TextArrowPosLeft = 1;
    const TextArrowPosRight = 2;
    const TextArrowPosTop = 3;
    const TextArrowPosBottom = 4;

    const undoModify = 1;
    const undoCreate = 2;
    const undoDelete = 3;

    const PixelToPoint = 72 / 96; //0.75
    const PointToPixel = 96 / 72; //1.3333

    //this.szBlockLoadMask = "*";

    //global configuration variables

//global configuration variables

    var consolidateObj = {
        isactive : false,
        consolidatelist : [],
        add : function(markup,settings,last){
            var listobj = {markup : markup, settings : settings};
            this.consolidatelist.push(listobj);
            if(last){
                while(this.consolidatelist.length > 0){
                    var consitem = this.consolidatelist.pop();
                    consitem.markup.Consolidate(consitem.settings);

                }
            }

        }
    };


    function noteimageload (imageURL, image){
                    
        const loadpromise = new Promise(function(resolve,reject){


            image.onload = function (ev) {
                
                resolve();                
                
            };

            try{
                image.src = imageURL;

            } catch{
                if(error.status){
                    console.log("markupimageload", error.status);
                }else{
                    console.log("markupimageload", error);
                }
                reject();

            }


        });

        return loadpromise;


    };            

    //note image
    var bnoteimageloaded = false;
    var noteimage = new Image();

    noteimage.onload = function () {

        bnoteimageloaded = true;
       //alert("image is loaded");
    }
    
    if (noteImgSrc){
        noteimage.src = noteImgSrc;
    }

    /* ANDRIY: start */
    //noteimage.src = 'images/note.png';
    

    // noteimage.src = '';
    /* ANDRIY: end */
    var TempID; //placeholder for notemarkup so that we can assign text from note dialog.

    var markupimage = new Image();
    markupimage.src = '';

    //hatch patterns
    //var hatchdiagforw = new Image();
    //hatchdiagforw.src = 'images/hatchdiagonalforward.png';

    function isImage(i) {
        return i instanceof HTMLImageElement;
    }

    function isCanvas(i) {
        return i instanceof HTMLCanvasElement;
    }


    var pattern = document.createElement('canvas');

    //hatch patterns

    //var display images;
    var splashimage = document.createElement('img'); //new Image();
    //var myimage = document.createElement('img');//new Image();
    //var myimagesmall = document.createElement('img');//new Image();
    //var myimagethumb = document.createElement('img');//new Image();
    //var Thumbnailpopup = "<p>No images loaded";
    //var Layerspopup = "<table><tr><td>Name</td><td>Color</td>State</td></tr></table>";

    //Document index.
    var nCurdocindex = 0;

    // The active tool instance.
    var tool;
    var tool_default = 'default';
    var szcurTool = 'default';
    // This object holds the implementation of each drawing tool.
    var tools = {};

    //var clock = new THREE.Clock();

    var DocObj;
    var CompareObj;
    //var SzMainImageSRC;
    //var SzSmallImageSRC;
    //var SzThumbImageSRC;

    // var DocumentObject = {}; //Document object
    // var PageObject = {}; //Document object

    var point = {}; //point object
    //var lineangle = {}; //line object
    /*Andriy: it's already declared below, var MarkupObject = {}; //General markup object
     var MarkupUndoObject = {}; //object for undo tracking */
    var OpenFiles = [];

    var BinPDFArrays = [];
    var PDFBinObj;


    var CompareFiles = [];
    var OpenfileNames = [];


    class markupsaveObjJSON {
        constructor(docobj) {
            var scope = this;
            this.jsonmarkup = null;
            this.DocRef = docobj;
            this.pages = [];
            this.pcounter = 0;
            this.bnotcomplete = true;

            if(docobj != null && docobj != undefined){
                docobj.markupsave = this;
            }

            //this.savePromise = undefined;

            this.callback = undefined;
        }
        
        setOncomplete(callback){

            //const xhrpromise = new Promise(function(resolve,reject){

            //scope.savePromise = new Promise();
            //scope.savePromise.resolve = scope.oncomplete(this.xmlmarkup);

            this.callback = callback;
        }
        setDocObj(DocRef) {

            if(DocRef != null && DocRef == undefined){
                this.DocRef = DocRef;
            }
            
        }
        oncomplete(){

            if (typeof this.callback === "function") {

                var returnstring = JSON.stringify(this.jsonmarkup);

                this.callback(returnstring);
            }


        }

        createjsonexport(signature, dispname, consolidateonly){

            //this.xmlmarkup = createxmlmarkup(true, false,false,signature, dispname, this);
            this.jsonmarkup = JSON.parse(createmarkupJSON(false, consolidateonly, bexportmarkupincludeAll, signature, dispname, this ));


            this.getnonloadedpages();

            //var markupxml = createxmlmarkup(false, consolidateonly, bexportmarkupincludeAll, signature, signature);

        }

        createjson(signature, dispname){
            
            this.jsonmarkup = JSON.parse(createmarkupJSON(true, false,true,signature, dispname, this));

            this.getnonloadedpages();

            /*if(this.pages.length == 0){
                this.oncomplete();
            }*/

            

        }
        //DocObj.markupsave.xmlmarkup = createxmlmarkup(true, false,false,signature, dispname, DocObj.markupsave);
        /*onerror(){

        }*/

        addpage(pagenumber){

            //var pagenum = pagenumber;
            var pagefound = false;

            for(var i=0; i < this.pages.length; i++){
                if(this.pages[i] == pagenumber){
                    pagefound = true;
                }

                //this.DocRef.getPageObjectLoad(this.pages[i]);
            }

            if(!pagefound){
                this.pages.push(pagenumber);
            }
            
        }
        getnonloadedpages(){

            if(this.pages.length > 0){
                if(this.DocRef != undefined){

                    var pagenum = this.pages.pop();
                    if (pagenum != undefined){
                        this.DocRef.getPageObjectLoad(pagenum);
                    }

                }
            }else{
                
                if(this.bnotcomplete){
                    this.oncomplete();
                    this.bnotcomplete = false;
                }


                //this.oncomplete();
            }

        }
        
        setpage(pageobject){

            var bpagehasmarkup = false;

            if(this.DocRef != undefined){
                if(this.jsonmarkup != null){



                    for(var mi=0; mi < this.DocRef.markuplist.length; mi++){
                        
                        if(this.DocRef.markuplist[mi].pagenumber ==  pageobject.pagenumber && !this.DocRef.markuplist[mi].pdfannotproxy){


                            
                            if (this.DocRef.markuplist[mi].bhasArrow && this.DocRef.markuplist[mi].markupArrowConnected != null){
                                
                            }else{

                                var entityNode = JSON.parse(createJSONmarkupentitynode(this.DocRef.markuplist[mi],this.DocRef,this.jsonmarkup, pageobject, false));
                            }

                            if(entityNode != undefined){
                                //this.xmlmarkup.getElementsByTagName("Entities")[0].appendChild(entityNode);
                                this.jsonmarkup.push(entityNode);
                            }

                        }


                    }

                }
            }

            if(this.pages.length > 0){
                this.getnonloadedpages();
            }else{
                if(this.bnotcomplete){
                    this.oncomplete();
                    this.bnotcomplete = false;
                }
                
            }

        }
    }

    class markupsaveObj {
        constructor(docobj) {
            var scope = this;
            this.xmlmarkup = null;
            this.DocRef = docobj;
            this.pages = [];
            this.pcounter = 0;
            this.bnotcomplete = true;

            if(docobj != null && docobj != undefined){
                docobj.markupsave = this;
            }

            //this.savePromise = undefined;

            this.callback = undefined;
        }
        
        setOncomplete(callback){

            //const xhrpromise = new Promise(function(resolve,reject){

            //scope.savePromise = new Promise();
            //scope.savePromise.resolve = scope.oncomplete(this.xmlmarkup);

            this.callback = callback;
        }
        setDocObj(DocRef) {

            if(DocRef != null && DocRef == undefined){
                this.DocRef = DocRef;
            }
            
        }
        oncomplete(){

            if (typeof this.callback === "function") {
                this.callback(this.xmlmarkup);
            }


        }

        createxmlexport(signature, dispname, consolidateonly){

            //this.xmlmarkup = createxmlmarkup(true, false,false,signature, dispname, this);
            this.xmlmarkup = createxmlmarkup(false, consolidateonly, bexportmarkupincludeAll, signature, dispname, this );


            this.getnonloadedpages();

            //var markupxml = createxmlmarkup(false, consolidateonly, bexportmarkupincludeAll, signature, signature);

        }

        createxml(signature, dispname){
            
            this.xmlmarkup = createxmlmarkup(true, false,false,signature, dispname, this);

            this.getnonloadedpages();

            /*if(this.pages.length == 0){
                this.oncomplete();
            }*/

            

        }
        //DocObj.markupsave.xmlmarkup = createxmlmarkup(true, false,false,signature, dispname, DocObj.markupsave);
        /*onerror(){

        }*/

        addpage(pagenumber){

            //var pagenum = pagenumber;
            var pagefound = false;

            for(var i=0; i < this.pages.length; i++){
                if(this.pages[i] == pagenumber){
                    pagefound = true;
                }

                //this.DocRef.getPageObjectLoad(this.pages[i]);
            }

            if(!pagefound){
                this.pages.push(pagenumber);
            }
            
        }
        getnonloadedpages(){

            if(this.pages.length > 0){
                if(this.DocRef != undefined){

                    var pagenum = this.pages.pop();
                    if (pagenum != undefined){
                        this.DocRef.getPageObjectLoad(pagenum);
                    }

                    /*for(var i=0; i < this.pages.length; i++){
                        
                    }*/
                }
            }else{
                
                if(this.bnotcomplete){
                    this.oncomplete();
                    this.bnotcomplete = false;
                }


                //this.oncomplete();
            }

        }
        
        setpage(pageobject){

            var bpagehasmarkup = false;

            if(this.DocRef != undefined){
                if(this.xmlmarkup != null){
                    for(var mi=0; mi < this.DocRef.markuplist.length; mi++){
                        
                        if(this.DocRef.markuplist[mi].pagenumber ==  pageobject.pagenumber){


                            //var entityNode = createxmlmarkupentitynode(this.DocRef.markuplist[mi],this.DocRef,this.xmlmarkup, pageobject, false);
                            if (this.DocRef.markuplist[mi].bhasArrow && this.DocRef.markuplist[mi].markupArrowConnected != null){
                                //var entityNode = createxmlmarkupentitynode(DocObj.markuplist[curmarkup].markupArrowConnected,DocObj,markupxml, false);    
                            }else{
                                //var entityNode = createxmlmarkupentitynode(DocObj.markuplist[curmarkup],DocObj,markupxml, pageobject, false);

                                /*if(!this.DocRef.markuplist[mi].bFirstdrawn){
                                    
                                }*/

                                //this.DocRef.markuplist[mi].offscreendrawdim(pageobject);

                                var entityNode = createxmlmarkupentitynode(this.DocRef.markuplist[mi],this.DocRef,this.xmlmarkup, pageobject, false);
                            }

                            if(entityNode != undefined){
                                this.xmlmarkup.getElementsByTagName("Entities")[0].appendChild(entityNode);
                            }

                        }

                        /*if(entityNode != undefined){
                            markupxml.getElementsByTagName("Entities")[0].appendChild(entityNode);    
                        }*/


                        
                        //DocRef.getPageObjectLoad(this.pages[i]);
                    }

                }
            }

            if(this.pages.length > 0){
                this.getnonloadedpages();
            }else{
                if(this.bnotcomplete){
                    this.oncomplete();
                    this.bnotcomplete = false;
                }
                
            }

            /*if(this.pcounter == this.pages.length){
                
            }*/
        }


    }    

    //new gltf 3d adaption
    class textureobject {
        constructor(image, w, h, color) {
            var texture = this;
            this.color = color;
            this.imagesrc = image;
            this.w = w;
            this.h = h;
            this.width = w;
            this.height = h;
            this.imageloaded = false;
            this.textureimage = new Image();
            this.threeTexture = null;
            this.threeTexture2 = null;
            this.canvas2 = null;
            this.context2 = null;
            this.markupmaterial = null;
            this.markupmaterialblank = null;
            this.imageload = function () {
                texture.imageloaded = true;
                //console.log('image loaded');
            };
            this.setimage = function () {
                //var canvas2 = document.createElement('canvas');
                //var context2 = canvas2.getContext('2d');
                // canvas contents will be used for a texture
                //var texture2 = new THREE.Texture(texture.canvas2);
                //texture.canvas2.width = 128;
                //texture.canvas2.height = 128;
                //texture.threeTexture2 = new THREE.Texture(texture.canvas2);
                // load an image
                //var imageObj = new Image();
                texture.textureimage.addEventListener('load', texture.imageload, false);
                texture.textureimage.src = texture.imagesrc;
                // after the image is loaded, this function executes
                /*imageObj.onload = function()
                {
                    context2.drawImage(imageObj, 0, 0);
                    if ( texture2 ) // checks if texture exists
                        texture2.needsUpdate = true;
                };*/
                //var imageObj = new Image();
                //imageObj.src = "images/Dice-Blue-1.png";
                //texture.textureimage.addEventListener('load', texture.imageload, false);
                //texture.textureimage.src = texture.imagesrc;
            };
            if(image){
                texture.setimage(image);
            }
            
        }
    }
        //new gltf 3d adaption
        class lineangle {
            constructor(startx, starty, endx, endy) {
                this.startx = startx;
                this.starty = starty;
                this.endx = endx;
                this.endy = endy;
                this.width = endx - startx;
                this.height = endy - starty;
                var rewidth = startx - endx;
                var reheight = starty - endy;
                this.angle = Math.atan2(this.height, this.width);
                this.reangle = Math.atan2(reheight, rewidth);
                this.rehangle = Math.atan2(this.height, rewidth);
                this.rewangle = Math.atan2(reheight, this.width);
            }
        }




        class TouchPoint {
            constructor(TouchID, x, y) {
                this.pointID = TouchID;
                this.x = x;
                this.y = y;
            }
        }

        class FontObject {
            constructor(FontName, Height, bold, italic) {
                var scope = this;
                this.fontName = FontName;
                this.height = Height;
                this.bold = bold;
                this.italic = italic;
                this.scale = 1;
                this.scalepx = 1;

                var scaledheigth = this.height * this.scale;
                if (bold) {
                    this.fontstring = "bold " + this.height + "pt " + this.fontName;
                    this.fontstringScaled = "bold " + scaledheigth + "pt " + this.fontName;
                    this.fontstringpx = "bold " + this.height + "px " + this.fontName;
                }
                else if (italic) {
                    this.fontstring = "italic " + this.height + "pt " + this.fontName;
                    this.fontstringScaled = "italic " + scaledheigth + "pt " + this.fontName;
                    this.fontstringpx = "italic " + this.height + "px " + this.fontName;
                }
                else {
                    this.fontstring = "normal " + this.height + "pt " + this.fontName;
                    this.fontstringScaled = "normal " + scaledheigth + "pt " + this.fontName;
                    this.fontstringpx = "normal " + this.height + "px " + this.fontName;
                }
                this.setFontname = function (FontName) {
                    this.fontName = FontName;
                    updateFont();
                };
                this.setHeight = function (Height) {
                    this.height = Height;
                    updateFont();
                };
                this.setBold = function (bOnOff) {
                    this.bold = bOnOff;
                    if (bOnOff) {
                        this.italic = false;
                    }
                    updateFont();
                };
                this.setItalic = function (bOnOff) {
                    this.italic = bOnOff;
                    if (bOnOff) {
                        this.bold = false;
                    }
                    updateFont();
                };
                this.setScale = function (scale) {
                    var pixelRatio = getPixelRatio();
                    this.scale = scale; 
                    this.scalepx = this.scale / pixelRatio;

                    updateFont();
                };
                this.getScaledHeight = function(){
                    return scaledheigth;
                };
                function updateFont() {
                    var scaledheigth = scope.height * scope.scale;
                    if (scope.bold) {
                        scope.fontstring = "bold " + scope.height + "pt " + scope.fontName;
                        scope.fontstringScaled = "bold " + scaledheigth + "pt " + scope.fontName;
                    }
                    else if (scope.italic) {
                        scope.fontstring = "italic " + scope.height + "pt " + scope.fontName;
                        scope.fontstringScaled = "italic " + scaledheigth + "pt " + scope.fontName;
                    }
                    else {
                        scope.fontstring = "normal " + scope.height + "pt " + scope.fontName;
                        scope.fontstringScaled = "normal " + scaledheigth + "pt " + scope.fontName;
                    }
                }
            }
        }
    var defaultFont = new FontObject(fontstylevalue, nDefaultFontSize, false, false);

    //var pressHoldEvent = new CustomEvent("pressHold");
    //var pressHoldDuration = 5;
    //var pressdownCounter = 0;
    //var timerID = null;


    /*function pressingDown(e, mousePos) {
        // Start the timer
        pressdownCounter = 0;
        requestAnimationFrame(timer);
        e.preventDefault();
    }*/

    /*function notPressingDown(e) {
        // Stop the timer
        cancelAnimationFrame(timerID);
        pressdownCounter = 0;
        timerID = null;
    }*/


    /*function timer() {
        //console.log("Timer tick!");
   
        
        if (pressdownCounter < pressHoldDuration) {
          pressdownCounter++;
        } else {
          pressdownCounter = 0;
          //console.log("Press threshold reached!");
          //canvas.dispatchEvent(pressHoldEvent);
          canvas.dispatchEvent(pressHoldEvent);
        }

        timerID = requestAnimationFrame(timer);

    }*/

    function createCORSRequest(method, url) {
        var xhr = new XMLHttpRequest();
        xhr.withCredentials = bUseCredentials;
        if ("withCredentials" in xhr) {
      
          // Check if the XMLHttpRequest object has a "withCredentials" property.
          // "withCredentials" only exists on XMLHTTPRequest2 objects.
          xhr.open(method, url, true);
      
        } else if (typeof XDomainRequest != "undefined") {
      
          // Otherwise, check if XDomainRequest.
          // XDomainRequest only exists in IE, and is IE's way of making CORS requests.
          xhr = new XDomainRequest();
          xhr.open(method, url);
      
        } else {
      
          // Otherwise, CORS is not supported by the browser.
          xhr = null;
      
        }
        return xhr;
    }


    
    function intersect(x1, y1, x2, y2, x3, y3, x4, y4) {

        // Check if none of the lines are of length 0
          if ((x1 === x2 && y1 === y2) || (x3 === x4 && y3 === y4)) {
              return false;
          }
      
         var denominator = ((y4 - y3) * (x2 - x1) - (x4 - x3) * (y2 - y1));
      
        // Lines are parallel
          if (denominator === 0) {
              return false;
          }
      
          var ua = ((x4 - x3) * (y1 - y3) - (y4 - y3) * (x1 - x3)) / denominator;
          var ub = ((x2 - x1) * (y1 - y3) - (y2 - y1) * (x1 - x3)) / denominator;
      
        // is the intersection along the segments
          if (ua < 0 || ua > 1 || ub < 0 || ub > 1) {
              return false;
          }
      
        // Return a object with the x and y coordinates of the intersection
          var x = x1 + ua * (x2 - x1);
          var y = y1 + ua * (y2 - y1);
      
          return {x: x, y: y};
      }


    function ortho_rotate(point){
        var centercanvX = (canvasowidth / 2);
        var centercanvY = (canvasoheight / 2);
        var CanvRotRad = 0;
        var transpoint = {x:point.x,y:point.y};

        switch(DocObj.getCurrentpage().getpageRotation()){
            case 0:
                CanvRotRad = DocObj.getCurrentpage().getpageRotation() * (Math.PI / 180);
                return transpoint;
                break;
            case 90:
                CanvRotRad = (DocObj.getCurrentpage().getpageRotation()) * (Math.PI / 180);
                break;
            case 270:
                CanvRotRad = (DocObj.getCurrentpage().getpageRotation()) * (Math.PI / 180);
                break;
            case 180:
                CanvRotRad = DocObj.getCurrentpage().getpageRotation() * (Math.PI / 180);
                break;
        }

        var cosangle = Math.cos(CanvRotRad);
        var sinangle = Math.sin(CanvRotRad);

        var hw = point.x - centercanvX;
        var hh = point.y - centercanvY;

        var newx = (hw * cosangle) - (hh * sinangle);
        var newy = (hw * sinangle) + (hh * cosangle);

        transpoint = {x:newx,y:newy};
        transpoint.x = centercanvX + transpoint.x;
        transpoint.y = centercanvY + transpoint.y;

        return transpoint;

    }

    function snap_rotated(point){
        var centercanvX = (canvasowidth / 2);
        var centercanvY = (canvasoheight / 2);
        var CanvRotRad = 0;
        var transpoint = {found:point.found,x:point.x,y:point.y};
        var bnochange = false;

        if (DocObj.foxitdoc != null) {

            bnochange = true;
           
        }


        switch(DocObj.getCurrentpage().getpageRotation()){
            case 0:
                CanvRotRad = DocObj.getCurrentpage().getpageRotation() * (Math.PI / 180);
                bnochange = true;
                //return transpoint;
                break;
            case 90:
                CanvRotRad = (DocObj.getCurrentpage().getpageRotation()) * (Math.PI / 180);
                break;
            case 270:
                CanvRotRad = (DocObj.getCurrentpage().getpageRotation()) * (Math.PI / 180);
                break;
            case 180:
                CanvRotRad = DocObj.getCurrentpage().getpageRotation() * (Math.PI / 180);
                break;
        }

        var cosangle = Math.cos(CanvRotRad);
        var sinangle = Math.sin(CanvRotRad);

        var hw = transpoint.x - centercanvX;
        var hh = transpoint.y - centercanvY;

        var newx = (hw * cosangle) - (hh * sinangle);
        var newy = (hw * sinangle) + (hh * cosangle);

        if(bnochange){
            transpoint = point;
        }else{
            transpoint.x = centercanvX + newx;
            transpoint.y = centercanvY + newy;
        }
        /*transpoint = {x:newx,y:newy};
        transpoint.x = centercanvX + transpoint.x;
        transpoint.y = centercanvY + transpoint.y;*/

        return transpoint;

    }

    function isObjectEqual(a, b) {
        // Create arrays of property names
        var aProps = Object.getOwnPropertyNames(a);
        var bProps = Object.getOwnPropertyNames(b);
    
        // If number of properties is different,
        // objects are not equivalent
        if (aProps.length != bProps.length) {
            return false;
        }
    
        for (var i = 0; i < aProps.length; i++) {
            var propName = aProps[i];
    
            // If values of same property are not equal,
            // objects are not equivalent
            if (a[propName] !== b[propName]) {
                return false;
            }
        }
    
        // If we made it this far, objects
        // are considered equivalent
        return true;
    }


    function rect_rotated(x1,y1,x2,y2){
        var centercanvX = (canvasowidth / 2);
        var centercanvY = (canvasoheight / 2);
        var CanvRotRad = 0;
        var transrect = {x1:x1,x2:x2,y1:y1,y2:y2};

        switch(DocObj.getCurrentpage().getpageRotation()){
            case 0:
                CanvRotRad = DocObj.getCurrentpage().getpageRotation() * (Math.PI / 180);
                return transrect;
                break;
            case 90:
                CanvRotRad = (DocObj.getCurrentpage().getpageRotation() + 180) * (Math.PI / 180);
                //CanvRotRad = (DocObj.getCurrentpage().getpageRotation()) * (Math.PI / 180);
                break;
            case 270:
                CanvRotRad = (DocObj.getCurrentpage().getpageRotation() - 180) * (Math.PI / 180);
                //CanvRotRad = (DocObj.getCurrentpage().getpageRotation()) * (Math.PI / 180);
                break;
            case 180:
                CanvRotRad = DocObj.getCurrentpage().getpageRotation() * (Math.PI / 180);
                break;
        }

        var cosangle = Math.cos(CanvRotRad);
        var sinangle = Math.sin(CanvRotRad);

        var hw = x1 - centercanvX;
        var hh = y1 - centercanvY;

        var newx = (hw * cosangle) - (hh * sinangle);
        var newy = (hw * sinangle) + (hh * cosangle);

        var hw2 = x2 - centercanvX;
        var hh2 = y2 - centercanvY;

        var newx2 = (hw2 * cosangle) - (hh2 * sinangle);
        var newy2 = (hw2 * sinangle) + (hh2 * cosangle);


        transrect = {x1:newx,y1:newy, x2:newx2, y2:newy2};
        transrect.x1 = centercanvX + transrect.x1;
        transrect.y1 = centercanvY + transrect.y1;
        transrect.x2 = centercanvX + transrect.x2;
        transrect.y2 = centercanvY + transrect.y2;

        switch(DocObj.getCurrentpage().getpageRotation()){
            case 90:
                var tempy = transrect.y2;
                transrect.y2 = transrect.y1;
                transrect.y1 = tempy; 
                break;
            case 270:
                var tempx = transrect.x2;
                transrect.x2 = transrect.x1;
                transrect.x1 = tempx;
                break;
            case 180:
                tempy = transrect.y2;
                transrect.y2 = transrect.y1;
                transrect.y1 = tempy; 
                tempx = transrect.x2;
                transrect.x2 = transrect.x1;
                transrect.x1 = tempx;
                break;
        }
        
        return transrect;

    }


    function getcentrepoint(){

        var centercanvX = (canvasowidth * 0.5);
        var centercanvY = (canvasoheight * 0.5);

        return {x : centercanvX, y : centercanvY};

    }

    function getrotrad(rotation){


        var CanvRotRad = rotation * (Math.PI / 180);

        switch (rotation) {
            case 0:
                CanvRotRad = rotation * (Math.PI / 180);
                break;
            case 90:
                CanvRotRad = (rotation + 180) * (Math.PI / 180);
                break;
            case 270:
                CanvRotRad = (rotation - 180) * (Math.PI / 180);
                break;
            case 180:
                CanvRotRad = rotation * (Math.PI / 180);
                break;
        }

        return CanvRotRad;

    }

    function mouse_rotatedPage(x,y, radrot, centre){

        var transpoint = {x:x,y:y};

        var cosangle = Math.cos(radrot);
        var sinangle = Math.sin(radrot);

        var hw = x - centre.x;
        var hh = y - centre.y;

        var newx = (hw * cosangle) - (hh * sinangle);
        var newy = (hw * sinangle) + (hh * cosangle);

        transpoint = {x:newx,y:newy};
        transpoint.x = centre.x + transpoint.x;
        transpoint.y = centre.y + transpoint.y;

        return transpoint;


    }



    function mouse_rotatedAngle(x,y, angle){

        var centercanvX = (canvasowidth / 2);
        var centercanvY = (canvasoheight / 2);
        var CanvRotRad = 0;
        var transpoint = {x:x,y:y};

        var pagerot = angle;

        switch(pagerot){
            case 0:
                CanvRotRad = pagerot * (Math.PI / 180);
                return transpoint;
                break;
            case 90:
                CanvRotRad = (pagerot + 180) * (Math.PI / 180);
                break;
            case 270:
                CanvRotRad = (pagerot - 180) * (Math.PI / 180);
                break;
            case 180:
                CanvRotRad = pagerot * (Math.PI / 180);
                break;
        }

        var cosangle = Math.cos(CanvRotRad);
        var sinangle = Math.sin(CanvRotRad);

        var hw = x - centercanvX;
        var hh = y - centercanvY;

        var newx = (hw * cosangle) - (hh * sinangle);
        var newy = (hw * sinangle) + (hh * cosangle);

        transpoint = {x:newx,y:newy};
        transpoint.x = centercanvX + transpoint.x;
        transpoint.y = centercanvY + transpoint.y;

        return transpoint;


    }

    function blockrectrotated(rect, angle){
        var centercanvX = (canvasowidth / 2);
        var centercanvY = (canvasoheight / 2);
        var width = centercanvX;
        var height = centercanvY;

        var CanvRotRad = 0;

        var transrect = {x : rect.x, y : rect.y, w: rect.w, h : rect.h};

        var CanvRotRad = angle * (Math.PI / 180);

            switch (angle) {
                case 0:
                    CanvRotRad = angle * (Math.PI / 180);
                    
                    break;
                case 90:
                    CanvRotRad = (angle) * (Math.PI / 180);
                    var switchwh = transrect.h;
                    transrect.h = transrect.w;
                    transrect.w = switchwh;
    
                    break;
                case 270:
                    CanvRotRad = (angle) * (Math.PI / 180);
                    switchwh = transrect.h;
                    transrect.h = transrect.w;
                    transrect.w = switchwh;
    
                    break;
                case 180:
                    CanvRotRad = angle * (Math.PI / 180);
                    break;
            }

        

        

        var cosangle = Math.cos(CanvRotRad);
        var sinangle = Math.sin(CanvRotRad);

        var hw = transrect.x - width;
        var hh = transrect.y - height;

        var newx = (hw * cosangle) - (hh * sinangle);
        var newy = (hw * sinangle) + (hh * cosangle);

        /*var transpoint = {x:newx, y:newy};
        transpoint.x = width + transpoint.x;
        transpoint.y = height + transpoint.y;*/

        transrect = {x:newx,y:newy, w : transrect.w, h : transrect.h};
        transrect.x = centercanvX + transrect.x;
        transrect.y = centercanvY + transrect.y;

        return transrect;


        //return transpoint;


    }

    function zoomrectrotated(rect, angle){
        var centercanvX = (canvasowidth / 2);
        var centercanvY = (canvasoheight / 2);
        var CanvRotRad = 0;
        //var transpoint = {x:rect.x, y: rect.y};
        var transrect = {x : rect.x, y : rect.y, w: rect.w, h : rect.h};


        switch(angle){
            case 0:
                CanvRotRad = angle * (Math.PI / 180);
                return transrect;
                break;
            case 90:
                CanvRotRad = (angle + 180) * (Math.PI / 180);
                var switchwh = transrect.h;
                transrect.h = transrect.w;
                transrect.w = switchwh;

                break;
            case 270:
                CanvRotRad = (angle - 180) * (Math.PI / 180);
                switchwh = transrect.h;
                transrect.h = transrect.w;
                transrect.w = switchwh;

                break;
            case 180:
                CanvRotRad = angle * (Math.PI / 180);
                //switchwh = transrect.h;
                //transrect.w = transrect.h;
                //transrect.h = switchwh;

                break;

        }

        var cosangle = Math.cos(CanvRotRad);
        var sinangle = Math.sin(CanvRotRad);

        var hw = transrect.x - centercanvX;
        var hh = transrect.y - centercanvY;

        var newx = (hw * cosangle) - (hh * sinangle);
        var newy = (hw * sinangle) + (hh * cosangle);

        transrect = {x:newx,y:newy, w : transrect.w, h : transrect.h};
        transrect.x = centercanvX + transrect.x;
        transrect.y = centercanvY + transrect.y;

        return transrect;


    }    


    function mouse_rotated(x,y){
        var centercanvX = (canvasowidth / 2);
        var centercanvY = (canvasoheight / 2);
        var CanvRotRad = 0;
        var transpoint = {x:x,y:y};

        var pagerot = DocObj.getCurrentpage().getpageRotation();

        switch(pagerot){
            case 0:
                CanvRotRad = pagerot * (Math.PI / 180);
                return transpoint;
                break;
            case 90:
                CanvRotRad = (pagerot + 180) * (Math.PI / 180);
                break;
            case 270:
                CanvRotRad = (pagerot - 180) * (Math.PI / 180);
                break;
            case 180:
                CanvRotRad = pagerot * (Math.PI / 180);
                break;
        }

        var cosangle = Math.cos(CanvRotRad);
        var sinangle = Math.sin(CanvRotRad);

        var hw = x - centercanvX;
        var hh = y - centercanvY;

        var newx = (hw * cosangle) - (hh * sinangle);
        var newy = (hw * sinangle) + (hh * cosangle);

        transpoint = {x:newx,y:newy};
        transpoint.x = centercanvX + transpoint.x;
        transpoint.y = centercanvY + transpoint.y;

        return transpoint;


    }

    function getrotpoint(width, height, x, y, anglerad){
        var cosangle = Math.cos(anglerad);
        var sinangle = Math.sin(anglerad);

        var hw = x - width;
        var hh = y - height;

        var newx = (hw * cosangle) - (hh * sinangle);
        var newy = (hw * sinangle) + (hh * cosangle);

        var transpoint = {x:newx, y:newy};
        transpoint.x = width + transpoint.x;
        transpoint.y = height + transpoint.y;
        return transpoint;

    }

    function rotate_pointrad(point, originX, originY, radians) {

        //var new_x_point = old_x_point * cos(Angle) - old_y_point * sin(Angle);
        //var new_y_point = old_y_point * cos(Angle) + old_x_point * sin(Angle);

        var transpx = point.x - originX;
        var transpy = point.y - originY;


        return {
            x: (Math.cos(radians) * (transpx) - Math.sin(radians) * (transpy)) + originX,
            y: (Math.sin(radians) * (transpx) + Math.cos(radians) * (transpy)) + originY
        };
    }

    function rotate_point(point, originX, originY, angle) {
        angle = angle * Math.PI / 180.0;
        return {
            x: Math.cos(angle) * (point.x-originX) - Math.sin(angle) * (point.y-originY) + originX,
            y: Math.sin(angle) * (point.x-originX) + Math.cos(angle) * (point.y-originY) + originY
        };
    }


    function colorisHex(color){

        isHex = false;
        
        var isHex = /^#[0-9A-F]{6}$/i.test(color);

        if(color.length == 9){
            isHex  = /^#[0-9A-F]{6}[0-9a-f]{0,2}$/i.test('#AABBCC80');
        }


        return isHex;
    }

    function getTransp(color){

        if (colorisHex(color)){
            color = convertHex(color, 100);
        }


        var colors = color.split(",");

        if(colors.length == 0){
            var alpha = 1;    
        }else{

            var r = colors[0].split("(");

            r = r[1].trim();
            var g = colors[1].trim();
            var b = colors[2].trim();
            alpha = colors[3].trim();
    
        }

        return alpha;

    }

    function setTransp(color, opacity) {
        var colors = color.split(",");
        var r = colors[0].split("(");

        if(r[1] == undefined){
            return;
        }

        r = r[1].trim();
        var g = colors[1].trim();
        var b = colors[2].trim();
        var alpha = colors[3].trim();

        return "rgba(" + r + "," + g + "," + b + "," + opacity / 100 + ")";


    }

    //Short code
    function matchRuleShort(str, rule) {
        
        var escape = rule.split("$").join("\\$");
        
        var lookfor = escape.split("*").join(".*");

        return new RegExp("^" + lookfor + "$", "i").test(str);
        
        //return new RegExp("^" + rule.split("*").join(".*") + "$").test(str);
    }

    function matchRuleLong(str, rule) {
        return new RegExp("^[a-zA-Z0-9!@#$&()\\-`.+,/\"]*$" + rule.split("*").join(".*") + "$").test(str);
    }

    function rgbToHex(color) {
        var bg = color.match(/^rgb\((\d+),\s*(\d+),\s*(\d+)\)$/);

        function hex(x) {
            return ("0" + parseInt(x).toString(16)).slice(-2);
        }

        return "#" + hex(bg[1]) + hex(bg[2]) + hex(bg[3]);
    }

    function convertHex(hex, opacity) {


        hex = hex.replace('#', '');
        var r = parseInt(hex.substring(0, 2), 16);
        var g = parseInt(hex.substring(2, 4), 16);
        var b = parseInt(hex.substring(4, 6), 16);

        if (hex.length == 8){

            r = parseInt(hex.substring(0, 2), 16);
            g = parseInt(hex.substring(2, 4), 16);
            b = parseInt(hex.substring(4, 6), 16);

            //opacity = parseInt(hex.substring(6, 8), 16);
            //opacity = Math.round((opacity / 255) * 100);

            
        }

        var result = 'rgba(' + r + ',' + g + ',' + b + ',' + opacity / 100 + ')';
        return result;
    }

    function convertHexrgb(hex) {
        hex = hex.replace('#', '');
        var r = parseInt(hex.substring(0, 2), 16);
        var g = parseInt(hex.substring(2, 4), 16);
        var b = parseInt(hex.substring(4, 6), 16);

        return {
            r : r,
            g : g,
            b : b
        };

    }


//sharpen canvas image 

function sharpen(ctx, w, h, mix) {

    var weights =  [0, -1, 0,  -1, 5, -1,  0, -1, 0],
        katet = Math.round(Math.sqrt(weights.length)),
        half = (katet * 0.5) |0,
        dstData = ctx.createImageData(w, h),
        dstBuff = dstData.data,
        srcBuff = ctx.getImageData(0, 0, w, h).data,
        y = h,
        x = w;

    while(y--) {

        x = w;

        while(x--) {

            var sy = y,
                sx = x,
                dstOff = (y * w + x) * 4,
                r = 0, g = 0, b = 0, a = 0;

            for (var cy = 0; cy < katet; cy++) {
                for (var cx = 0; cx < katet; cx++) {

                    var scy = sy + cy - half;
                    var scx = sx + cx - half;

                    if (scy >= 0 && scy < h && scx >= 0 && scx < w) {

                        var srcOff = (scy * w + scx) * 4;
                        var wt = weights[cy * katet + cx];

                        r += srcBuff[srcOff] * wt;
                        g += srcBuff[srcOff + 1] * wt;
                        b += srcBuff[srcOff + 2] * wt;
                        a += srcBuff[srcOff + 3] * wt;
                    }
                }
            }

            dstBuff[dstOff] = r * mix + srcBuff[dstOff] * (1 - mix);
            dstBuff[dstOff + 1] = g * mix + srcBuff[dstOff + 1] * (1 - mix);
            dstBuff[dstOff + 2] = b * mix + srcBuff[dstOff + 2] * (1 - mix)
            dstBuff[dstOff + 3] = srcBuff[dstOff + 3];
        }
    }

    ctx.putImageData(dstData, 0, 0);
}


// scales the canvas by (float) scale < 1
// returns a new canvas containing the scaled image.




function downScaleCanvas(cv, scale) {
    if (!(scale < 1) || !(scale > 0)) throw ('scale must be a positive number <1 ');
    var sqScale = scale * scale; // square scale = area of source pixel within target
    var sw = cv.width; // source image width
    var sh = cv.height; // source image height
    var tw = Math.ceil(sw * scale); // target image width
    var th = Math.ceil(sh * scale); // target image height
    var sx = 0, sy = 0, sIndex = 0; // source x,y, index within source array
    var tx = 0, ty = 0, yIndex = 0, tIndex = 0; // target x,y, x,y index within target array
    var tX = 0, tY = 0; // rounded tx, ty
    var w = 0, nw = 0, wx = 0, nwx = 0, wy = 0, nwy = 0; // weight / next weight x / y
    // weight is weight of current source point within target.
    // next weight is weight of current source point within next target's point.
    var crossX = false; // does scaled px cross its current px right border ?
    var crossY = false; // does scaled px cross its current px bottom border ?
    var sBuffer = cv.getContext('2d').getImageData(0, 0, sw, sh).data; // source buffer 8 bit rgba
    var tBuffer = new Float32Array(4 * sw * sh); // target buffer Float32 rgb
    var sR = 0, sG = 0,  sB = 0; // source's current point r,g,b
    // untested !
    var sA = 0;  //source alpha    

    for (sy = 0; sy < sh; sy++) {
        ty = sy * scale; // y src position within target
        tY = 0 | ty;     // rounded : target pixel's y
        yIndex = 4 * tY * tw;  // line index within target array
        crossY = (tY != (0 | ty + scale)); 
        if (crossY) { // if pixel is crossing botton target pixel
            wy = (tY + 1 - ty); // weight of point within target pixel
            nwy = (ty + scale - tY - 1); // ... within y+1 target pixel
        }
        for (sx = 0; sx < sw; sx++, sIndex += 4) {
            tx = sx * scale; // x src position within target
            tX = 0 |  tx;    // rounded : target pixel's x
            tIndex = yIndex + tX * 4; // target pixel index within target array
            crossX = (tX != (0 | tx + scale));
            if (crossX) { // if pixel is crossing target pixel's right
                wx = (tX + 1 - tx); // weight of point within target pixel
                nwx = (tx + scale - tX - 1); // ... within x+1 target pixel
            }
            sR = sBuffer[sIndex    ];   // retrieving r,g,b for curr src px.
            sG = sBuffer[sIndex + 1];
            sB = sBuffer[sIndex + 2];
            sA = sBuffer[sIndex + 3];
            
            if (!crossX && !crossY) { // pixel does not cross
                // just add components weighted by squared scale.
                tBuffer[tIndex    ] += sR * sqScale;
                tBuffer[tIndex + 1] += sG * sqScale;
                tBuffer[tIndex + 2] += sB * sqScale;
                tBuffer[tIndex + 3] += sA * sqScale;
            } else if (crossX && !crossY) { // cross on X only
                w = wx * scale;
                // add weighted component for current px
                tBuffer[tIndex    ] += sR * w;
                tBuffer[tIndex + 1] += sG * w;
                tBuffer[tIndex + 2] += sB * w;
                tBuffer[tIndex + 3] += sA * w;
                // add weighted component for next (tX+1) px                
                nw = nwx * scale
                tBuffer[tIndex + 4] += sR * nw; // not 3
                tBuffer[tIndex + 5] += sG * nw; // not 4
                tBuffer[tIndex + 6] += sB * nw; // not 5
                tBuffer[tIndex + 7] += sA * nw; // not 6
            } else if (crossY && !crossX) { // cross on Y only
                w = wy * scale;
                // add weighted component for current px
                tBuffer[tIndex    ] += sR * w;
                tBuffer[tIndex + 1] += sG * w;
                tBuffer[tIndex + 2] += sB * w;
                tBuffer[tIndex + 3] += sA * w;
                // add weighted component for next (tY+1) px                
                nw = nwy * scale
                tBuffer[tIndex + 4 * tw    ] += sR * nw; // *4, not 3
                tBuffer[tIndex + 4 * tw + 1] += sG * nw; // *4, not 3
                tBuffer[tIndex + 4 * tw + 2] += sB * nw; // *4, not 3
                tBuffer[tIndex + 4 * tw + 3] += sA * nw; // *4, not 3
            } else { // crosses both x and y : four target points involved
                // add weighted component for current px
                w = wx * wy;
                tBuffer[tIndex    ] += sR * w;
                tBuffer[tIndex + 1] += sG * w;
                tBuffer[tIndex + 2] += sB * w;
                tBuffer[tIndex + 3] += sA * w;
                // for tX + 1; tY px
                nw = nwx * wy;
                tBuffer[tIndex + 4] += sR * nw; // same for x
                tBuffer[tIndex + 5] += sG * nw;
                tBuffer[tIndex + 6] += sB * nw;
                tBuffer[tIndex + 7] += sA * nw;
                // for tX ; tY + 1 px
                nw = wx * nwy;
                tBuffer[tIndex + 4 * tw    ] += sR * nw; // same for mul
                tBuffer[tIndex + 4 * tw + 1] += sG * nw;
                tBuffer[tIndex + 4 * tw + 2] += sB * nw;
                tBuffer[tIndex + 4 * tw + 3] += sA * nw;
                // for tX + 1 ; tY +1 px
                nw = nwx * nwy;
                tBuffer[tIndex + 4 * tw + 4] += sR * nw; // same for both x and y
                tBuffer[tIndex + 4 * tw + 5] += sG * nw;
                tBuffer[tIndex + 4 * tw + 6] += sB * nw;
                tBuffer[tIndex + 4 * tw + 7] += sA * nw;
            }
        } // end for sx 
    } // end for sy

    // create result canvas
    var resCV = document.createElement('canvas');
    resCV.width = tw;
    resCV.height = th;
    var resCtx = resCV.getContext('2d');
    
    //setSmoothingEnabledEx(false, resCtx);

    var imgRes = resCtx.getImageData(0, 0, tw, th);
    var tByteBuffer = imgRes.data;
    // convert float32 array into a UInt8Clamped Array
    var pxIndex = 0; //  
    for (sIndex = 0, tIndex = 0; pxIndex < tw * th; sIndex += 4, tIndex += 4, pxIndex++) {
        tByteBuffer[tIndex] = Math.ceil(tBuffer[sIndex]);
        tByteBuffer[tIndex + 1] = Math.ceil(tBuffer[sIndex + 1]);
        tByteBuffer[tIndex + 2] = Math.ceil(tBuffer[sIndex + 2]);
        tByteBuffer[tIndex + 3] = Math.ceil(tBuffer[sIndex + 3]);
    }
    // writing result to canvas.
    resCtx.putImageData(imgRes, 0, 0);
    //sharpen(resCtx, tw, th, 0.8);
    sBuffer = null;
    tByteBuffer = null;
    tBuffer = null;
    return resCV;
}

    function getBase64ImageString(img){
        // Create an empty canvas element
        var canvas = document.createElement("canvas");
        canvas.width = img.width;
        canvas.height = img.height;

        // Copy the image contents to the canvas
        var ctx = canvas.getContext("2d");
        ctx.drawImage(img, 0, 0);

        // Get the data-URL formatted image
        // Firefox supports PNG and JPEG. You could check img.src to
        // guess the original format, but be aware the using "image/jpg"
        // will re-encode the image.
        var dataURL = canvas.toDataURL("image/png");

        //var pos = "data:image/png;base64,"
        //return dataURL.replace(/^data:image\/(png|jpg);base64,/, "");
        
        return dataURL;

    }

    function getBase64ImageRaw(img) {
        var canvas = document.createElement("canvas");
        canvas.width = img.width;
        canvas.height = img.height;

        // Copy the image contents to the canvas
        var ctx = canvas.getContext("2d");
        ctx.drawImage(img, 0, 0);

        // Get the data-URL formatted image
        // Firefox supports PNG and JPEG. You could check img.src to
        // guess the original format, but be aware the using "image/jpg"
        // will re-encode the image.
        var dataURL = canvas.toDataURL("image/png");
        //dataURL = "<![CDATA[" + dataURL + "]]>";

        //return dataURL.replace(/^data:image\/(png|jpg);base64,/, "");
        return dataURL;

    }


    function getBase64Image(img) {
        // Create an empty canvas element
        var canvas = document.createElement("canvas");
        canvas.width = img.width;
        canvas.height = img.height;

        // Copy the image contents to the canvas
        var ctx = canvas.getContext("2d");
        ctx.drawImage(img, 0, 0);

        // Get the data-URL formatted image
        // Firefox supports PNG and JPEG. You could check img.src to
        // guess the original format, but be aware the using "image/jpg"
        // will re-encode the image.
        var dataURL = canvas.toDataURL("image/png");
        dataURL = "<![CDATA[" + dataURL + "]]>";

        //return dataURL.replace(/^data:image\/(png|jpg);base64,/, "");
        return dataURL;

    }

    function convertImageToBase64(imgUrl) {
     

        const image64basepromise = new Promise(function(resolve,reject){

            const image = new Image();

            image.onload = (function(ev){

                const dataUrl = getBase64ImageRaw(ev.currentTarget);

                resolve(dataUrl);
    
              //callback && callback(dataUrl)
            });
            
            image.src = imgUrl;
    
        });
        
        return image64basepromise;
        //image.crossOrigin='anonymous';
        
    }


    function getChildNodes(node) {
        var children = new Array();
        for(var child in node.childNodes) {
            if(node.childNodes[child].nodeType == 1) {
                children.push(node.childNodes[child]);
            }
        }
        return children;
    }

    function splitstring(context, text, maxwidth, resultarray){
        var test = text;
        var bNeedsplit = false;

        var metrics = context.measureText(test);
        while (metrics.width > maxwidth && test.length > 1) {
            // Determine how much of the word will fit
            test = test.substring(0, test.length - 1);
            metrics = context.measureText(test);
        }

        if (text != test) {
            resultarray.push(test);
            var remainer = text.substr(test.length);
            metrics = context.measureText(remainer);
            if(metrics.width > maxwidth){
                bNeedsplit = true;
            }else{
                bNeedsplit = false;
                resultarray.push(remainer);
            }

        }else{
            resultarray.push(test);
            bNeedsplit = false;

        }

        return {
           bNeedSplit : bNeedsplit,
           remainer : remainer,
           resultarray : resultarray     
        };


    }

    function getTextDimensions_ex(context, text, x, y, maxWidth, lineHeight){
        var bNeedsplit = true;
        var line = '',
            lineCount = 0,
            i,
            test,
            metrics;

        var curwidth = 0;                

        var crlines = text.split('\n');
        var words = [];
        var splitwords = [];
        var mergewords = [];

        var charwidth = context.measureText(' ');

        maxWidth = maxWidth - charwidth.width;
        
        for (var crn = 0;crn < crlines.length;crn++){
            line = '';
            lineCount = 0;
            i = 0;
            var metrics = context.measureText(crlines[crn]);
            if (metrics.width > maxWidth ){
                words = words.concat(crlines[crn].split(' '));
                //test remove newline
                words.push('\n');
            }else{
                words.push(crlines[crn]);
                //test remove newline
                words.push('\n');

                if(metrics.width > curwidth){
                    curwidth = metrics.width;
                }


            }
            

        }
        
        for (i = 0; i < words.length; i++) {

            if(words[i] != '\n'){
                var splitresult = splitstring(context, words[i], maxWidth,splitwords);

                while(splitresult.bNeedSplit){
                    splitresult = splitstring(context, splitresult.remainer, maxWidth,splitwords);
                }
            }else{
                splitwords.push(words[i]);
            }


        }

        var bConcat = true;
        

        for (i = 0; i < splitwords.length; i++) {

            if(splitwords[i] != '\n'){

                test = line + splitwords[i] + ' ';
                metrics = context.measureText(test);

                if (metrics.width > maxWidth && i > 0) {
                    
                    if(line != ''){
                        mergewords.push(line);
                    }
                    
                    line = splitwords[i] + ' ';

                    
                    
                    if(metrics.width > curwidth){
                        curwidth = metrics.width;
                    }


                }else{
                    //below maxwidth keep adding concat
                    line = test;
                    

                }
                
                
            }else{
                if(line != ''){
                    mergewords.push(line);
                    
                }
                
                if(metrics.width > curwidth){
                    curwidth = metrics.width;
                }


                line = '';
                
            }

        }


        var crcount = 0;
        var lwrapcount = 0;

        for (i = 0; i < mergewords.length; i++) {

            if(mergewords[i] != '\n'){
                //context.fillText(mergewords[i], x, y);
                //line = '';
                y += lineHeight;
                lineCount++;
            }

        }

        var txtheight = (lineCount + 1) * lineHeight;

        return {width : curwidth, height : txtheight};



    }

    function getTextDimensions(context, text, x, y, maxWidth, lineHeight){
        var bNeedsplit = true;
        var line = '',
            lineCount = 0,
            i,
            test,
            metrics;

        var curwidth = 0;    

        var crlines = text.split('\n');
        var words = [];
        var splitwords = [];
        
        for (var crn = 0;crn < crlines.length;crn++){
            line = '';
            lineCount = 0;
            i = 0;
            var metrics = context.measureText(crlines[crn]);
            if (metrics.width > maxWidth ){
                words = words.concat(crlines[crn].split(' '));
                words.push('\n');
            }else{
                words.push(crlines[crn]);
                words.push('\n');
                if(metrics.width > curwidth){
                    curwidth = metrics.width;
                }
                

            }
            

        }
        
        for (i = 0; i < words.length; i++) {

            if(words[i] != '\n'){
                var splitresult = splitstring(context, words[i], maxWidth,splitwords);

                while(splitresult.bNeedSplit){
                    splitresult = splitstring(context, splitresult.remainer, maxWidth,splitwords);
                }
            }else{
                splitwords.push(words[i]);
            }


        }

        var bConcat = true;
        for (i = 0; i < splitwords.length; i++) {
            if(splitwords[i] != '\n'){
                
                test = line + splitwords[i] + ' ';
               
                metrics = context.measureText(test);
    
                if (metrics.width > maxWidth && i > 0) {
                    //context.fillText(line, x, y);
                    line = splitwords[i] + ' ';
                    y += lineHeight;
                    lineCount++;
                    
                    if(metrics.width > curwidth){
                        curwidth = metrics.width;
                    }
                }
                else {
                    line = test;
                }
    
            }else{
                bConcat = false;
                //context.fillText(line, x, y);
                line = '';
                y += lineHeight;

                if(metrics.width > curwidth){
                    curwidth = metrics.width;
                }
        
            }

        }

        var txtheight = (lineCount + 1) * lineHeight;

        return {width : curwidth, height : txtheight};

        /*context.fillText(line, x, y);
        y += lineHeight;*/

        //var words = text.split(' ');
    }

    function wrapText2(context, text, x, y, maxWidth, lineHeight) {

        var bNeedsplit = true;
        var line = '',
            lineCount = 0,
            i,
            test,
            metrics;

        var crlines = text.split('\n');
        var words = [];
        var splitwords = [];
        var mergewords = [];

        //context.save();
        //context.textAlign = "right";

        var charwidth = context.measureText(' ');

        maxWidth = maxWidth - charwidth.width;
        
        for (var crn = 0;crn < crlines.length;crn++){
            line = '';
            lineCount = 0;
            i = 0;
            var metrics = context.measureText(crlines[crn]);
            if (metrics.width > maxWidth ){
                words = words.concat(crlines[crn].split(' '));
                //test remove newline
                words.push('\n');
            }else{
                words.push(crlines[crn]);
                //test remove newline
                words.push('\n');

            }
            

        }
        
        for (i = 0; i < words.length; i++) {

            if(words[i] != '\n'){
                var splitresult = splitstring(context, words[i], maxWidth,splitwords);

                while(splitresult.bNeedSplit){
                    splitresult = splitstring(context, splitresult.remainer, maxWidth,splitwords);
                }
            }else{
                splitwords.push(words[i]);
            }


        }

        var bConcat = true;
        

        for (i = 0; i < splitwords.length; i++) {

            if(splitwords[i] != '\n'){

                test = line + splitwords[i] + ' ';
                metrics = context.measureText(test);

                if (metrics.width > maxWidth && i > 0) {
                    
                    if(line != ''){
                        mergewords.push(line);
                    }
                    
                    line = splitwords[i] + ' ';

                }else{
                    //below maxwidth keep adding concat
                    line = test;
                    

                }
                
                
            }else{
                if(line != ''){
                    mergewords.push(line);
                }
                
                line = '';
                
            }

        }


        var crcount = 0;
        var lwrapcount = 0;

        for (i = 0; i < mergewords.length; i++) {

            if(mergewords[i] != '\n'){
                context.fillText(mergewords[i], x, y);
                //line = '';
                y += lineHeight;
            }

        }

        //context.restore();

        /*for (i = 0; i < splitwords.length; i++) {
            if(splitwords[i] != '\n'){
                
                test = line + splitwords[i] + ' ';
               
                metrics = context.measureText(test);
    
                if (metrics.width > maxWidth && i > 0) {
                    context.fillText(line, x, y);

                    line = splitwords[i] + ' ';
                    //test remove lineheight add
                    y += lineHeight;
                    lwrapcount ++;

                    lineCount++;
                }
                else {
                    line = test;
                }
    
            }else{
                bConcat = false;
                context.fillText(line, x, y);
                line = '';
                y += lineHeight;
                crcount ++;
        
            }

        }*/

        //console.log("cr",crcount);
        //console.log("lwrap",lwrapcount);
        

        /*context.fillText(line, x, y);
        y += lineHeight;*/

        //var words = text.split(' ');

    }    

    function wrapText (context, text, x, y, maxWidth, lineHeight) {

        var bNeedsplit = true;
        var line = '',
            lineCount = 0,
            i,
            test,
            metrics;

        var crlines = text.split('\n');
        var words = [];
        var splitwords = [];
        
        for (var crn = 0;crn < crlines.length;crn++){
            line = '';
            lineCount = 0;
            i = 0;
            var metrics = context.measureText(crlines[crn]);
            if (metrics.width > maxWidth ){
                words = words.concat(crlines[crn].split(' '));
                words.push('\n');
            }else{
                words.push(crlines[crn]);
                words.push('\n');

            }
            

        }
        
        for (i = 0; i < words.length; i++) {

            if(words[i] != '\n'){
                var splitresult = splitstring(context, words[i], maxWidth,splitwords);

                while(splitresult.bNeedSplit){
                    splitresult = splitstring(context, splitresult.remainer, maxWidth,splitwords);
                }
            }else{
                splitwords.push(words[i]);
            }


        }

        var bConcat = true;
        for (i = 0; i < splitwords.length; i++) {
            if(splitwords[i] != '\n'){
                
                test = line + splitwords[i] + ' ';
               
                metrics = context.measureText(test);
    
                if (metrics.width > maxWidth && i > 0) {
                    context.fillText(line, x, y);
                    line = splitwords[i] + ' ';
                    y += lineHeight;
                    lineCount++;
                }
                else {
                    line = test;
                }
    
            }else{
                bConcat = false;
                context.fillText(line, x, y);
                line = '';
                y += lineHeight;
        
            }

        }

        /*context.fillText(line, x, y);
        y += lineHeight;*/

        //var words = text.split(' ');


    }



    function wordWrap(str, maxWidth) {
        var newLineStr = "\n";
        var done = false;
        var res = '';
        do {
            var found = false;
            // Inserts new line at first whitespace of the line
            for (var i = maxWidth - 1; i >= 0; i--) {
                if (testWhite(str.charAt(i))) {
                    res = res + [str.slice(0, i), newLineStr].join('');
                    str = str.slice(i + 1);
                    found = true;
                    break;
                }
            }
            // Inserts new line at maxWidth position, the word is too long to wrap
            if (!found) {
                res += [str.slice(0, maxWidth), newLineStr].join('');
                str = str.slice(maxWidth);
            }

            if (str.length < maxWidth)
                done = true;
        } while (!done);

        return res + str;
    }

    function testWhite(x) {
        var white = new RegExp(/^\s$/);
        return white.test(x.charAt(0));
    }

    point = function (x, y) {
        this.x = x;
        this.y = y;
    };

    class PaperSize {
        constructor(width, height) {
            this.width = width;
            this.height = height;
        }
    }

    class Rectangle {
        constructor(x, y, w, h) {
            this.x = x;
            this.y = y;
            this.w = w;
            this.h = h;
        }
    }

    class Users {
        constructor(Signature, DispName, Layer, Color, bdefault) {
            this.Signature = Signature;
            this.DisplayName = DispName;
            this.Layer = Layer;
            this.Color = Color;
            this.display = true;
            this.default = bdefault;
        }
    }

    class Layers {
        constructor(Number, Color, Name, Display) {
            this.Layer = Number;
            //this.Name = "Layer " + Number;
            this.Name = Name;
            this.Color = Color;
            this.display = Display;
        }
    }

    function GetUserexist(sign){
        var signfound = false;
    
        for (var i = 0; i < Userlist.length; i++) {
            if (Userlist[i].Signature == sign) {
            signfound = true;
            
            }
        }
        return signfound;
    
    
        }


    function GetDisplayName(sign) {
        var signfound = false;
        var displayname = 'default user';

        for (var i = 0; i < Userlist.length; i++) {
            if (Userlist[i].Signature == sign) {
                signfound = true;
                displayname = Userlist[i].DisplayName;

            }
        }
        return displayname;
    }

    function getSignState(sign){
        var signfound = false;
        var displaystate = true;

        for (var i = 0; i < Userlist.length; i++) {
            if (Userlist[i].Signature == sign) {
                signfound = true;
                displaystate = Userlist[i].display;

            }
        }
        return displaystate;

    }

    function getMarkupLayerColor(layer){
        var layerfound = false;
        var layerColor = '#FF0000';

        for (var i = 0; i < Layerlist.length; i++) {
            if (Layerlist[i].Layer == layer) {
                layerfound = true;
                layerColor = Layerlist[i].Color;

            }
        }
        
        return layerColor;

    }
    function getMarkupLayerState(layer){
        var layerfound = false;
        var displaystate = true;

        for (var i = 0; i < Layerlist.length; i++) {
            if (Layerlist[i].Layer == layer) {
                layerfound = true;
                displaystate = Layerlist[i].display;

            }
        }
        return displaystate;

    }


    function isEven(value) {
        return (value % 2 == 0);
        //    return true;
        //else
        //    return false;
    }

    function decimalToHex(d) {
        var hex = Number(d).toString(16);
        hex = "000000".substr(0, 6 - hex.length) + hex;
        hex = hex.toUpperCase();
        hex = "#" + hex;
        return hex;
    }

    function getDiagonal(width, height){
        var dimwsq = Math.pow(width, 2);
        var dimhsq = Math.pow(height, 2);
        var dimdiag = Math.sqrt((dimwsq + dimhsq));

        return dimdiag;

    }

    function getCalloutBox(x,y, scale, arrowpos){



        //if (markupobject.arrowTextPos == TextArrowPosLeft){
        
        if (scale){
            var textbh = ((defaultFont.height * scale) / 0.75);
            var nCalloutWidthScaled = nCalloutWidth * scale;

        }else{
            textbh = (defaultFont.height  / 0.75);
            nCalloutWidthScaled = nCalloutWidth;
        }

        if(arrowpos){
            if (arrowpos == TextArrowPosLeft){
                y = y - (textbh * 0.5);
                //x = x;
            }else if (arrowpos == TextArrowPosRight){
                x = x - nCalloutWidthScaled;
                y = y - (textbh * 0.5);

            }else if(arrowpos == TextArrowPosTop){
                
                x = x - (nCalloutWidthScaled * 0.5);
                y = y - textbh;

            }else if(arrowpos == TextArrowPosBottom){
                x = x - (nCalloutWidthScaled * 0.5);
                //box.y = height;

            }
        }
        

        //var textrect = {x : x, y :  y, w : nCalloutWidth, h : textbh};
        return {x : x, y :  y, w : nCalloutWidthScaled, h : textbh};



    }


    function getCurrentPageOffset(pagenum){

        var offset = {x : 0, y : 0};

        var pixelRatio = getPixelRatio();

        if(docpagNotReady()){
            return offset;
        }
        
        if(pagenum != undefined){
            var pageobj = DocObj.getpage(pagenum);
        }else {
            pageobj = DocObj.getCurrentpage();
        }

        if (pageobj.usevectorxml) {
            
            offset.x = pageobj.dxvector;
            offset.y = pageobj.dyvector;

        } else if (pageobj.usepdfjs) {
            
            offset.x = pageobj.dxpdf;
            offset.y = pageobj.dypdf;

            offset.x *= pixelRatio;
            offset.y *= pixelRatio;

        } else {
            
            offset.x = pageobj.dx;


            offset.y = pageobj.dy;
        }

        if(pageobj.usevector3Dxml){
            offset.x = canvasowidth * 0.5;
            offset.y = canvasoheight * 0.5;

        }


        return offset;

    }

    function getCurrentViewName(){
        var view3DName = null;

        if(docpagNotReady()){
            return view3DName;
        }

        
        if (DocObj.getCurrentpage().usevector3Dxml) {
            view3DName = DocObj.getCurrentpage().getcur3DView();
        }

        return view3DName;
    }

    function getCurrentPageFixedScale(pagenum){

        if(pagenum != undefined){
            var pageobj = DocObj.getpage(pagenum);
        }else {
            pageobj = DocObj.getCurrentpage();
        }


        return pageobj.fixedScale;
    }

    function getPageFixedScale(pageobject){
        return pageobject.fixedScale;
    }


    function getCurrentPageVectorloaded(){

        var vectorloaded = true;

        if (DocObj.getCurrentpage().usevectorxml) {
            vectorloaded = DocObj.getCurrentpage().vectorloaded;
        }

        return vectorloaded;
        
    }

    function getImageMainscale(pageobj){


        return pageobj.MainImageScaling;

        /// mainimagescale
    }

    function getPageScale(pageobj){

        if(pageobj == undefined){
            return 1;
        }

        var scalefactor = pageobj.dscale; // markupobject.scaling;
        if (pageobj.usevectorxml) {
            scalefactor = pageobj.dscalevector; // markupobject.scaling;
        }
        else if (pageobj.usepdfjs) {
            scalefactor = (pageobj.curpagescale * pageobj.dscalepdf); // markupobject.scaling;
        }
        else {
            scalefactor = pageobj.dscale; // markupobject.scaling;
            if (DocObj.Type == 0) {
                scalefactor = pageobj.dscale; // markupobject.scaling;
            }
        }

        //do new check here for 3D markup?
        if (pageobj.usevector3Dxml) {
            scalefactor = 1;
        }


        return scalefactor;
        

    }


    function isfoxitPDF(){

        var bisfoxit = false;

        if (DocObj.getCurrentpage().usepdfjs) {
            if (DocObj.getCurrentpage().DocRef.foxitdoc != null) {
                bisfoxit = true;
            }
        }

        return bisfoxit;
    }

    //var mainimagescale = getCurrentPageMainimageScale();
    function getCurrentPageMainimageScale(){

        if(docpagNotReady()){
            return 1;
        }

        var scalefactor = 1;

        if(getPagerasterScale(DocObj.getCurrentpage())){
            scalefactor = DocObj.getCurrentpage().MainImageScaling;
        }

         

        return scalefactor;
        
    }


    function getCurrentPageScale(pagenum){

        if(docpagNotReady()){
            return 1;
        }

        var pixelRatio = getPixelRatio();


        if(pagenum != undefined){
            var pageobj = DocObj.getpage(pagenum);
        }else {
            pageobj = DocObj.getCurrentpage();
        }



        var scalefactor = pageobj.dscale; // markupobject.scaling;
        if (pageobj.usevectorxml) {
            scalefactor = pageobj.dscalevector; // markupobject.scaling;
        }
        else if (pageobj.usepdfjs) {
            scalefactor = (pageobj.curpagescale * pageobj.dscalepdf); // markupobject.scaling;
            scalefactor *= pixelRatio;
        }
        else {
            scalefactor = pageobj.dscale; // markupobject.scaling;
            if (DocObj.Type == 0) {
                scalefactor = DocObj.pages[0].dscale; // markupobject.scaling;
            }
        }

        if (pageobj.usevector3Dxml) {
            scalefactor = 1;
        }


        return scalefactor;

    }

    function getPagerasterScale(pageobj){
     
        var brasterscale = false;

        if(pageobj == undefined){
            return brasterscale;
        }


        if (pageobj.usevectorxml) {
            brasterscale = false;
        }
        else if (pageobj.usepdfjs) {
            brasterscale = false;
        }
        else {
            brasterscale = true;
        }

        //do new check here for 3D markup?
        if (pageobj.usevector3Dxml) {
            brasterscale = true;
        }


        return brasterscale;

    }    

    function getCurrentPagerasterScale(){
        
        var brasterscale = false;

        if(docpagNotReady()){
            return brasterscale;
        }


        if (DocObj.getCurrentpage().usevectorxml) {
            brasterscale = false;
        }
        else if (DocObj.getCurrentpage().usepdfjs) {
            brasterscale = false;
        }
        else {
            brasterscale = true;
        }

        //do new check here for 3D markup?
        if (DocObj.getCurrentpage().usevector3Dxml) {
            brasterscale = true;
        }


        return brasterscale;
    }



    function MousePosradiusdrwext(centre, mousepos) {
        
        var bIswithin = false;

        if(docpagNotReady()){
            return bIswithin;
        }


        var xdiff = Math.max(centre.x, mousepos.x) - Math.min(centre.x, mousepos.x);
        var ydiff = Math.max(centre.y, mousepos.y) - Math.min(centre.y, mousepos.y);

        var radius = getDiagonal(xdiff, ydiff);

        var radiusnegx = centre.x - radius;
        var radiusposx = centre.x + radius;
        var radiusnegy = centre.y - radius; 
        var radiusposy = centre.y + radius;


        if(DocObj.getCurrentpage().getpageRotation() == 0){

            if (radiusnegx > DocObj.getCurrentpage().startx && radiusposx < DocObj.getCurrentpage().endx){
                if(radiusnegy > DocObj.getCurrentpage().starty && radiusposy < DocObj.getCurrentpage().endy){
                    bIswithin = true;
                }

            }
            if (bIswithin){
                if (mousepos.x > DocObj.getCurrentpage().startx && mousepos.x < DocObj.getCurrentpage().endx) {
                    if (mousepos.y > DocObj.getCurrentpage().starty && mousepos.y < DocObj.getCurrentpage().endy) {
                        bIswithin = true;
                    }
                }
            }

        }else{
            var rotrect = DocObj.getCurrentpage().rotatedRect();
            
            if (radiusnegx > rotrect.x && radiusposx < rotrect.w){
                if(radiusnegy > rotrect.y && radiusposy < rotrect.h){
                    bIswithin = true;
                }
            }
            if(bIswithin){
                if (mousepos.x > rotrect.x && mousepos.x < rotrect.w) {
                    if (mousepos.y > rotrect.y && mousepos.y < rotrect.h) {
                        bIswithin = true;
                    }
                }
            }
        }

        if (DocObj.getCurrentpage().usevector3Dxml) {

            bIswithin = true;
        }

        return bIswithin;
    }

    function MousePosdrwextRectPage(rect, pagerect){
        var bIswithin = false;

        if(docpagNotReady()){
            return bIswithin;
        }

        var xpos = rect.x;
        var ypos = rect.y;
        var xwidth = rect.x + rect.w;
        var yheight = rect.y + rect.h;
        var pagerot = DocObj.getCurrentpage().getpageRotation();
        pagerot = 0;
        
        if(pagerot == 0){

            if (xpos > pagerect.startx && xpos < pagerect.endx) {
                if (ypos > pagerect.starty && ypos < pagerect.endy) {

                    if (xwidth > pagerect.startx && xwidth < pagerect.endx) {
                        if (yheight > pagerect.starty && yheight < pagerect.endy) {
                            
                            bIswithin = true;
                            
                        }
        
                    }
        
                    
                }

            }



        }else{
            var rotrect = DocObj.getCurrentpage().rotatedRect();



            if (xpos > rotrect.x && xpos < rotrect.w) {
                if (ypos > rotrect.y && ypos < rotrect.h) {


                    if (xwidth > rotrect.x && xwidth < rotrect.w) {
                        if (yheight > rotrect.y && yheight < rotrect.h) {
                    
                            bIswithin = true;
                            
                            
                        }
        
                    }
        
                    
                }
            
            }


        }

        if (DocObj.getCurrentpage().usevector3Dxml) {
            bIswithin = true;
        }

        return bIswithin;

    }

    function MousePosdrwextRect(rect){

        var bIswithin = false;

        if(docpagNotReady()){
            return bIswithin;
        }

        var xpos = rect.x;
        var ypos = rect.y;
        var xwidth = rect.x + rect.w;
        var yheight = rect.y + rect.h;
        var pagerot = DocObj.getCurrentpage().getpageRotation();
        pagerot = 0;


        var pagerect = DocObj.getCurrentpage().getpagerectdirect();

        var startx = pagerect.x;
        var endx = pagerect.w;
        var starty = pagerect.y;
        var endy = pagerect.h;

        if(pagerect.usepixelscale){
            var pixelRatio = getPixelRatio();

            startx = startx * pixelRatio;
            endx = endx * pixelRatio;
            starty = starty * pixelRatio;
            endy = endy * pixelRatio;

        }



        if(pagerot == 0){

            if (xpos > startx && xpos < endx) {
                if (ypos > starty && ypos < endy) {

                    if (xwidth > startx && xwidth < endx) {
                        if (yheight > starty && yheight < endy) {
                            
                            bIswithin = true;
                            
                        }
        
                    }
        
                    
                }

            }



        }else{
            var rotrect = DocObj.getCurrentpage().rotatedRect();



            if (xpos > rotrect.x && xpos < rotrect.w) {
                if (ypos > rotrect.y && ypos < rotrect.h) {


                    if (xwidth > rotrect.x && xwidth < rotrect.w) {
                        if (yheight > rotrect.y && yheight < rotrect.h) {
                    
                            bIswithin = true;
                            
                            
                        }
        
                    }
        
                    
                }
            
            }


        }

        if (DocObj.getCurrentpage().usevector3Dxml) {
            bIswithin = true;
        }

        return bIswithin;

        
    }    

    function MousePosdrwext(mousepos) {

        var bIswithin = false;

        if(docpagNotReady()){
            return bIswithin;
        }

        var pagerect = DocObj.getCurrentpage().getpagerectdirect();

        var startx = pagerect.x;
        var endx = pagerect.w;
        var starty = pagerect.y;
        var endy = pagerect.h;

        var xpos = mousepos.x;
        var ypos = mousepos.y;

        /*if(DocObj.getCurrentpage().usepdfjs){

            var xpos = mousepos.xus;
            var ypos = mousepos.yus;

        }else{
    
        }*/

        if(pagerect.usepixelscale){
            var pixelRatio = getPixelRatio();

            startx = startx * pixelRatio;
            endx = endx * pixelRatio;
            starty = starty * pixelRatio;
            endy = endy * pixelRatio;

            //xpos = mousepos.xus;
            //ypos = mousepos.yus;
        }


        if(mousepos.busepadding){
            xpos = mousepos.xpadding;
            ypos = mousepos.ypadding;
        }

        
        if(DocObj.getCurrentpage().getpageRotation() == 0){

            //console.log(pagerect);

            if (xpos > startx && xpos < endx) {
                if (ypos > starty && ypos < endy) {
                    bIswithin = true;
                }

            }
        
            /*if (xpos > DocObj.getCurrentpage().startx && xpos < DocObj.getCurrentpage().endx) {
                if (ypos > DocObj.getCurrentpage().starty && ypos < DocObj.getCurrentpage().endy) {
                    bIswithin = true;
                }

            }*/

        }else{

            if(DocObj.getCurrentpage().usepdfjs){

                //get counter rotated mouse position.

                var rotatedmouse = mouse_rotated(xpos, ypos);

                var xposrot = rotatedmouse.x;
                var yposrot = rotatedmouse.y;

                if (xposrot > startx && xposrot < endx) {
                    if (yposrot > starty && yposrot < endy) {
                        bIswithin = true;
                    }
    
                }
    
            }else{
                var rotrect = DocObj.getCurrentpage().rotatedRect();

                if (xpos > rotrect.x && xpos < rotrect.w) {
                    if (ypos > rotrect.y && ypos < rotrect.h) {
                        bIswithin = true;
                    }
    
                }    
            }


        }

        if (DocObj.getCurrentpage().usevector3Dxml) {
            bIswithin = true;
        }

        

        return bIswithin;

        /*var bIswithin = false;

        if(DocObj.getCurrentpage().getpageRotation() == 0){
            if (mousepos.x > DocObj.getCurrentpage().startx && mousepos.x < DocObj.getCurrentpage().endx) {
                if (mousepos.y > DocObj.getCurrentpage().starty && mousepos.y < DocObj.getCurrentpage().endy) {
                    bIswithin = true;
                }

            }

        }else{
            var rotrect = DocObj.getCurrentpage().rotatedRect();

            if (mousepos.x > rotrect.x && mousepos.x < rotrect.w) {
                if (mousepos.y > rotrect.y && mousepos.y < rotrect.h) {
                    bIswithin = true;
                }

            }

        }
        return bIswithin;*/
    }

    function sqr(x) {
        return x * x;
    }

    function dist2(v, w) {
        return sqr(v.x - w.x) + sqr(v.y - w.y);
    }


    function compute2DPolygonCentroid(ptsorg){
        var pts = [];
        pts = ptsorg.slice();

        var centroid = {x:0, y:0};
        var signedArea = 0.0;
        var x0 = 0.0; // Current vertex X
        var y0 = 0.0; // Current vertex Y
        var x1 = 0.0; // Next vertex X
        var y1 = 0.0; // Next vertex Y
        var a = 0.0; // Partial signed area

        for (var i=0; i<pts.length-1; ++i){
            x0 = pts[i].x;
            y0 = pts[i].y;
            x1 = pts[i+1].x;
            y1 = pts[i+1].y;
            a = x0*y1 - x1*y0;
            signedArea += a;
            centroid.x += (x0 + x1)*a;
            centroid.y += (y0 + y1)*a;

        }
        // Do last vertex
        x0 = pts[i].x;
        y0 = pts[i].y;
        x1 = pts[0].x;
        y1 = pts[0].y;
        a = x0*y1 - x1*y0;
        signedArea += a;
        centroid.x += (x0 + x1)*a;
        centroid.y += (y0 + y1)*a;

        signedArea *= 0.5;
        centroid.x /= (6.0*signedArea);
        centroid.y /= (6.0*signedArea);
        return centroid;



    }
    function get_polygon_centroid(ptsorg) {
        
        var pts = [];
        pts = ptsorg.slice();

        var first = pts[0], last = pts[pts.length-1];
        if (first.x != last.x || first.y != last.y) pts.push(first);
        var twicearea=0,
            x=0, y=0,
            nPts = pts.length,
            p1, p2, f;
        for ( var i=0, j=nPts-1 ; i<nPts ; j=i++ ) {
            p1 = pts[i]; p2 = pts[j];
            f = p1.x*p2.y - p2.x*p1.y;
            twicearea += f;
            x += ( p1.x + p2.x ) * f;
            y += ( p1.y + p2.y ) * f;
        }
        f = twicearea * 3;
        return { x:x/f, y:y/f };
    }

/*
  Calculates a label position along the angle bisector.
 * @param {number} x1 - X of first point on first line
 * @param {number} y1 - Y of first point on first line
 * @param {number} x2 - X of second point on second line
 * @param {number} y2 - Y of second point on second line
 * @param {number} cx - X of common vertex
 * @param {number} cy - Y of common vertex
 * @param {number} r - Distance from vertex to label
 * @returns {{x: number, y: number}} - Coordinates for the label
 */
function calculateAngleLabelPosition(x1, y1, x2, y2, cx, cy, r) {
    // Vectors from vertex to each point
    let v1x = x1 - cx;
    let v1y = y1 - cy;
    let v2x = x2 - cx;
    let v2y = y2 - cy;

    // Normalize vectors
    let len1 = Math.hypot(v1x, v1y);
    let len2 = Math.hypot(v2x, v2y);
    if (len1 === 0 || len2 === 0) return { x: cx, y: cy }; // Avoid divide-by-zero

    v1x /= len1; v1y /= len1;
    v2x /= len2; v2y /= len2;

    // Compute the bisector vector
    let bx = v1x + v2x;
    let by = v1y + v2y;
    let blen = Math.hypot(bx, by);
    if (blen === 0) {
        // Lines are in opposite directions (180°), so label goes straight out from one side
        bx = -v1y; // perpendicular
        by = v1x;
        blen = Math.hypot(bx, by);
    }

    bx /= blen;
    by /= blen;

    // Return label position at distance r from vertex
    return {
        x: cx + bx * r,
        y: cy + by * r
    };
}


function RadiansTodegrees(radians){

    // Store the value of pi.
  var pi = Math.PI;
  // Multiply radians by 180 divided by pi to convert to degrees.
  return radians * (180/pi);


}

//******************************************************************************
//*** PointFromArcDist
//*** Return distance between circle and a points.
//*** Params:
//***            x0,y0          Point coordinates
//***            xc,yc          Arc center
//***            radius         Arc radius
//***            sang           Start angle
//***            sweep          Sweep angle
//*** Returns
//***            double         Distance
//******************************************************************************


function isAngleInArc(a, start, sweep) {
    a = (a + 2 * Math.PI) % (2 * Math.PI);
    start = (start + 2 * Math.PI) % (2 * Math.PI);
    let end = (start + sweep + 2 * Math.PI) % (2 * Math.PI);
    
    if (sweep >= 0) {
        return start < end ? a >= start && a <= end : a >= start || a <= end;
    } else {
        return start > end ? a <= start && a >= end : a <= start || a >= end;
    }
}



function PointFromArcDist(x, y, xc, yc, radius, sang, sweep){
    var xp;
    var yp;
    var da;
    var a;
    var eang;
    var t;
    var dt;

    a = Math.atan2( y-yc, x-xc );           //Find angle from center to point
    if (a < 0){
        a += 2 * Math.PI;
    }
    if (sang < 0){
        sang += 2 * Math.PI;
    } 
    eang = sang + sweep;

    if (eang < sang){
       t = sang;
       sang = eang;
       eang = t;
    }

    if (isAngleInArc(a, sang, sweep)) {
        xp = xc + radius * Math.cos(a);
        yp = yc + radius * Math.sin(a);
    } else {
        // Determine which endpoint is closer
        let distStart = Math.abs((a - sang + 2 * Math.PI) % (2 * Math.PI));
        let distEnd = Math.abs((a - (sang + sweep) + 2 * Math.PI) % (2 * Math.PI));
        let targetAngle = (distStart < distEnd) ? sang : (sang + sweep);
    
        xp = xc + radius * Math.cos(targetAngle);
        yp = yc + radius * Math.sin(targetAngle);
    }

    /*if (a >= sang && a <= eang){
       xp = xc + radius * Math.cos( a );       //Find point on circle at this angle
       yp = yc + radius * Math.sin( a );
    }else if (a < sang){
       xp = xc + radius * Math.cos( sang );       //Find point on circle at this angle
       yp = yc + radius * Math.sin( sang );
    }else{
       xp = xc + radius * Math.cos( eang );       //Find point on circle at this angle
       yp = yc + radius * Math.sin( eang );
    }*/
    
    
    return Math.sqrt(Math.pow(xp-x, 2)+Math.pow(yp-y, 2));   //calc distance from point 

    
}


    function distToSegmentSquared(p, v, w) {
        var l2 = dist2(v, w);
        if (l2 == 0) return dist2(p, v);
        var t = ((p.x - v.x) * (w.x - v.x) + (p.y - v.y) * (w.y - v.y)) / l2;
        if (t < 0) return dist2(p, v);
        if (t > 1) return dist2(p, w);
        return dist2(p, {
            x:v.x + t * (w.x - v.x),
            y:v.y + t * (w.y - v.y)
        });
    }

    function distToSegment(p, v, w) {
        return Math.sqrt(distToSegmentSquared(p, v, w));
    }

    function areIntersecting(v1x1,v1y1,v1x2,v1y2,v2x1,v2y1,v2x2,v2y2){
        var a1 = v1y2 - v1y1;
        var b1 = v1x1 - v1x2;
        var c1 = (v1x2 * v1y1) - (v1x1 * v1y2);

        var d1 = (a1 * v2x1) + (b1 * v2y1) + c1;
        var d2 = (a1 * v2x2) + (b1 * v2y2) + c1;

        if (d1 > 0 && d2 > 0){
            return 0;
        }
        if (d1 < 0 && d2 < 0){
            return 0;
        }
        var a2 = v2y2 - v2y1;
        var b2 = v2x1 - v2x2;
        var c2 = (v2x2 * v2y1) - (v2x1 * v2y2);

        d1 = (a2 * v1x1) + (b2 * v1y1) + c2;
        d2 = (a2 * v1x2) + (b2 * v1y2) + c2;

        if (d1 > 0 && d2 > 0){
            return 0;
        }
        if (d1 < 0 && d2 < 0){
            return 0;
        }

        if ((a1 * b2) - (a2 * b1) == 0.0){
            return 2;
        }

        return 1;

    }

    function insidepolygonpoints(scalefactor,offsetx,offsety,mediax, mediay, mediah,path,mouse,startpoint){
        var intersections = 0;
        var rayx1 = startpoint.x;
        var rayy1 = startpoint.y;
        var rayx2 = mouse.x;
        var rayy2 = mouse.y;

        var sidex1 = 0;
        var sidey1 = 0;
        var sidex2 = 0;
        var sidey2 = 0;
        //var sides = 0;


        for (var counter = 0; counter < path.points.length; counter += 2) {

            sidex1 = ((path.points[counter] - mediax) * scalefactor);
            sidey1 = ((mediah - path.points[counter + 1]) * scalefactor);
            //close polygon if not closed
            if(counter + 3 < path.points.length){
                sidex2 = ((path.points[counter + 2] - mediax) * scalefactor);
                sidey2 = ((mediah - path.points[counter + 3]) * scalefactor);
            }else{
                sidex2 = ((path.points[0] - mediax) * scalefactor);
                sidey2 = ((mediah - path.points[1]) * scalefactor);

            }

            sidex1 += offsetx;
            sidey1 += offsety;
            sidex2 += offsetx;
            sidey2 += offsety;

            intersections += areIntersecting(rayx1,rayy1,rayx2,rayy2,sidex1,sidey1,sidex2,sidey2);
            //sides ++;

        }
        /*console.log(intersections);
        console.log('sides:' + sides);
        console.log(path.points.length);
        cntximg.strokeStyle = "black";
        cntximg.lineWidth = 2;
        cntximg.beginPath();
        cntximg.moveTo(rayx1,rayy1);
        cntximg.lineTo(rayx2,rayy2);
        cntximg.stroke();*/

        if(path.gotsubpath){
            for (var scount = 0; scount < path.subpaths.length; scount++) {
                for ( counter = 0; counter < path.subpaths[scount].points.length; counter += 2) {

                    sidex1 = ((path.subpaths[scount].points[counter] - mediax) * scalefactor);
                    sidey1 = ((mediah - path.subpaths[scount].points[counter + 1]) * scalefactor);
                    //close polygon if not closed
                    if(counter + 3 < path.subpaths[scount].points.length){
                        sidex2 = ((path.subpaths[scount].points[counter + 2] - mediax) * scalefactor);
                        sidey2 = ((mediah - path.subpaths[scount].points[counter + 3]) * scalefactor);
                    }else{
                        sidex2 = ((path.subpaths[scount].points[0] - mediax) * scalefactor);
                        sidey2 = ((mediah - path.subpaths[scount].points[1]) * scalefactor);

                    }

                    sidex1 += offsetx;
                    sidey1 += offsety;
                    sidex2 += offsetx;
                    sidey2 += offsety;

                    intersections += areIntersecting(rayx1,rayy1,rayx2,rayy2,sidex1,sidey1,sidex2,sidey2);

                }

            }
        }
        //console.log(intersections);
        /*if (!isEven(intersections)){
            cntximg.strokeStyle = "black";
            cntximg.lineWidth = 2;
            cntximg.beginPath();
            cntximg.moveTo(rayx1,rayy1);
            cntximg.lineTo(rayx2,rayy2);
            cntximg.stroke();
            cntximg.strokeStyle = "yellow";
            cntximg.lineWidth = 4;


        }*/

        return !isEven(intersections);

    }


    function insidepolygon(scalefactor,dx,dy,xoffset,yoffset,polygon,mouse,startpoint){
        var intersections = 0;
        var rayx1 = startpoint.x;
        var rayy1 = startpoint.y;
        var rayx2 = mouse.x;
        var rayy2 = mouse.y;

        for (var counter = 0; counter < polygon.length; counter++) {
            var sidex1 = (polygon[counter].x1 - xoffset) * scalefactor;
            sidex1 += dx;
            var sidey1 = (polygon[counter].y1 - yoffset) * scalefactor;
            sidey1 += dy;
            var sidex2 = (polygon[counter].x2 - xoffset) * scalefactor;
            sidex2 += dx;
            var sidey2 = (polygon[counter].y2 - yoffset) * scalefactor;
            sidey2 += dy;
            intersections += areIntersecting(rayx1,rayy1,rayx2,rayy2,sidex1,sidey1,sidex2,sidey2);
        }
        //console.log(intersections);
        //var within = (intersections == 0);
        return !isEven(intersections);
        //return (intersections == 0);

    }

    function getpolygonarea(points){

        var i = 0;
        var area = 0.0;
        var n = points.length;


        for (i = 0; i < n - 1; i++) {
            area += (points[i].x * points[i + 1].y) - (points[i + 1].x * points[i].y);
        }
        area += (points[n - 1].x * points[0].y) - (points[0].x * points[n - 1].y);
        return Math.abs(area / 2.0);


    }

    function msieversion() {

        //var ua = window.navigator.userAgent;
        //var msie = ua.indexOf("MSIE ");

        if (navigator.userAgent.match(/Trident.*rv\:11\./))  // If Internet Explorer, return version number
        {
            bIsMSIE = true;
            szdispvalue = "";
            //alert(parseInt(ua.substring(msie + 5, ua.indexOf(".", msie))));
        }
        else  // If another browser, return 0
        {
            bIsMSIE = false;
            szdispvalue = "initial";
            //alert('otherbrowser');
        }

        //bIsMSIE = false;
        //szdispvalue = "initial";
        //return false;
    }

    function createLayers() {
        Layerlist[0] = new Layers(0, "#ffffff", "Layer 0", true);
        Layerlist[1] = new Layers(1, "#ff0000", "Layer 1", true);
        Layerlist[2] = new Layers(2, "#0000ff", "Layer 2", true);
        Layerlist[3] = new Layers(3, "#008000", "Layer 3", true);
        Layerlist[4] = new Layers(4, "#ffff00", "Layer 4", true);
        Layerlist[5] = new Layers(5, "#a52a2a", "Layer 5", true);
        Layerlist[6] = new Layers(6, "#ffd700", "Layer 6", true); //gold
        Layerlist[7] = new Layers(7, "#fff5ee", "Layer 7", true);
        Layerlist[8] = new Layers(8, "#fff8dc", "Layer 8", true);
        Layerlist[9] = new Layers(9, "#fffacd", "Layer 9", true);
        Layerlist[10] = new Layers(10, "#ffffe0", "Layer 10", true);
        Layerlist[11] = new Layers(11, "#98fb98", "Layer 11", true);
        Layerlist[12] = new Layers(12, "#afeeee", "Layer 12", true);
        Layerlist[13] = new Layers(13, "#e0ffff", "Layer 13", true);
        Layerlist[14] = new Layers(14, "#e6e6fa", "Layer 14", true);
        Layerlist[15] = new Layers(15, "#dda0dd", "Layer 15", true);
        Layerlist[16] = new Layers(16, "#d3d3d3", "Layer 16", true);
        Layerlist[17] = new Layers(17, "#ffc0cb", "Layer 17", true);
        Layerlist[18] = new Layers(18, "#ffe4c4", "Layer 18", true);
        Layerlist[19] = new Layers(19, "#ffe4b5", "Layer 19", true);
        Layerlist[20] = new Layers(20, "#f0e68c", "Layer 20", true);
        Layerlist[21] = new Layers(21, "#90ee90", "Layer 21", true);
        Layerlist[22] = new Layers(22, "#20b2aa", "Layer 22", true);
        Layerlist[23] = new Layers(23, "#87cefa", "Layer 23", true);
        Layerlist[24] = new Layers(24, "#6495ed", "Layer 24", true);
        Layerlist[25] = new Layers(25, "#ee82ee", "Layer 25", true);
        Layerlist[26] = new Layers(26, "#c0c0c0", "Layer 26", true);
        Layerlist[27] = new Layers(27, "#f08080", "Layer 27", true);
        Layerlist[28] = new Layers(28, "#f4a460", "Layer 28", true);
        /*


         24 background-color: #ffa500
         25 background-color: #eee8aa
         26 background-color: #7fff00
         27 background-color: #48d1cc
         28 background-color: #87ceeb
         29 background-color: #7b68ee
         30 background-color: #da70d6
         31 background-color: #808080

         33 background-color: #ff4500
         34 background-color: #ff8c00

         36 background-color: #32cd32
         37 background-color: #8fbc8f
         38 background-color: #4169e1
         39 background-color: #6a5acd
         40 background-color: #ba55d3
         41 background-color: #696969
         42 background-color: #dc143c
         43 background-color: #d2691e
         44 background-color: #ff7f50

         46 background-color: #228b22
         47 background-color: #2e8b57

         49 background-color: #8a2be2
         50 background-color: #9932cc
         51 background-color: #2f4f4f
         52 background-color: #b22222
         53 background-color: #8b4513
         54 background-color: #a0522d
         55 background-color: #808000

         57 background-color: #008b8b
         58 background-color: #0000cd
         59 background-color: #483d8b
         60 background-color: #8b008b

         62 background-color: #8b0000
         63 background-color: #800000

         65 background-color: #556b2f
         66 background-color: #006400
         67 background-color: #191970
         68 background-color: #000080
         69 background-color: #4b0082
         70 background-color: #800080

         */
        Layerlist[29] = new Layers(29, "#000000", "Layer 29", true); //black


    }

    function getMarkupControlProperties(type, subtype, alternative){

        /*var properties = {
            lineColor : false,
            fillColor : false,
            textColor : false,
            lineWidth : false,
            lineStyle : false,
            endpoint_open_single : false,
            endpoint_open_both : false,
            endpoint_filled_single : false,
            endpoint_filled_both : false,
            endpoints_bar : false,
            endpoints_arrow : false,
            lineSegmentWidth : false,
            transpacency : false,
            rotate : false,
            snap : false,
            fill : false,
            stroke : false,
            fontName : false,
            fontHeight : false,
            fontBold : false,
            fontItalic : false
        };*/

        var properties = {
            lineColor : false,
            fillColor : false,
            textColor : false,
            lineWidth : false,
            lineStyle : false,
            clockwise : false,
            customlabel : false,
            display : true,
            countsize : false,
            countshape : false,
            showperimeter : false,
            enablelink : false,
            endpoint_open_single : false,
            endpoint_open_both : false,
            endpoint_filled_single : false,
            endpoint_filled_both : false,
            endpoints_bar : false,
            endpoints_arrow : false,
            lineSegmentWidth : false,
            transpacency : false,
            rotate : false,
            snap : false,
            fill : false,
            fontName : false,
            fontHeight : false,
            labelsize : false, 
            fontBold : false,
            fontItalic : false
        };

        switch (type) {
            case 0:
                
                if (subtype == 0) {
                    //labelType = "Freehand pen";
                    properties.lineColor = true;
                    properties.lineWidth = true;

                }
                if (subtype == 1) {
                    //labelType = "Eraser";
                    properties.lineWidth = true;
                }
                break;
            case 1:
                if (subtype == 1) {
                    //labelType = "Polyline";
                    properties.lineColor = true;
                    properties.lineWidth = true;
                    properties.snap = true;
                }
                if (subtype == 2) {
                    //labelType = "Polygon";
                    properties.lineColor = true;
                    properties.fillColor = true;
                    properties.lineWidth = true;
                    properties.transpacency = true;
                    properties.snap = true;
                    properties.fill = true;
                }
                if (subtype == 3) {
                    //labelType = "Measure Path";
                    properties.lineColor = true;
                    properties.lineWidth = true;
                    properties.snap = true;
                }

                if (subtype == 4 || subtype == 5) {
                    //labelType = "angle measure";
                    properties.lineColor = true;
                    properties.lineWidth = true;
                    properties.snap = true;
                }

                if (subtype == 6) {
                    //labelType = "Measure Path";
                    properties.lineColor = true;
                    properties.lineWidth = true;
                    properties.lineSegmentWidth = true;
                    properties.snap = true;
                }

                break;
            case 2:
                //labelType = "Polycurve";
                properties.lineColor = true;
                properties.lineWidth = true;

                break;
            case 3:
                if (subtype == 0) {
                    //labelType = "Rectangle";

                }
                if (subtype == 1) {
                    //labelType = "Rounded Rectangle";
                }
                
                properties.lineColor = true;
                properties.fillColor = true;
                properties.lineWidth = true;
                properties.transpacency = true;
                properties.snap = true;
                properties.fill = true;
                properties.rotate = true;


                break;
            case 4:
                if (subtype == 0) {
                    //labelType = "Oval";
                    properties.rotate = true;

                }
                if (subtype == 1) {
                    //labelType = "Circle";
                }

                properties.lineColor = true;
                properties.fillColor = true;
                properties.lineWidth = true;
                properties.transpacency = true;
                properties.snap = true;
                properties.fill = true;


                break;
            case 5:
                //labelType = "Revision Cloud";

                properties.lineColor = true;
                properties.fillColor = true;
                properties.lineWidth = true;
                properties.transpacency = true;
                properties.snap = true;
                properties.fill = true;
                properties.rotate = true;


                break;
            case 6:
                if (subtype == 9) {
                    //labelType = "Line";
                    properties.lineColor = true;
                    properties.lineWidth = true;
                    properties.snap = true;
    
                }else if (subtype == 6){
                    
                    //callout

                } else {
                    //labelType = "Arrow";
                    properties.lineColor = true;
                    properties.lineWidth = true;
                    properties.snap = true;

                    properties.endpoint_open_single = true;
                    properties.endpoint_open_both = true;
                    properties.endpoint_filled_single = true;
                    properties.endpoint_filled_both = true;
                    //properties.endpoints_bar : true,
                    //properties.endpoints_arrow : true,

    
                }




                break;
            case 7:
                labelType = "Dimension Line";

                properties.lineColor = true;
                properties.lineWidth = true;
                properties.snap = true;

                properties.endpoints_bar = true;
                properties.endpoints_arrow = true;



                break;
            case 8:
                if (subtype == 1) {
                    //labelType = "Space";
                }
                else {
                    //labelType = "Area";
                    properties.lineColor = true;
                    properties.fillColor = true;
                    properties.lineWidth = true;
                    properties.transpacency = true;
                    properties.snap = true;
                    properties.fill = true;
    
                }
                break;
            case 9:
                //labelType = "Text";

                if (subtype == 1) {
                    properties.lineColor = true;
                    properties.fillColor = true;
                    properties.lineWidth = true;
                    properties.transpacency = true;
                    properties.fill = true;
                    properties.textColor = true;
                    properties.rotate = true;
                    
                    properties.fontName = true;
                    properties.fontHeight = true;
                    properties.fontBold = true;
                    properties.fontItalic = true;
                }



                break;
            case 11:
                //labelType = "Image";

                if (subtype == 0 || subtype == 1 || subtype == 12 || subtype == 20 || subtype == 21) {
                    properties.rotate = true;
                }

                

                break;
            case 10:
                labelType = "Note";
                
                break;
            case 12:
                //labelType = "Stamp";

                properties.lineColor = true;
                properties.fillColor = true;
                properties.lineWidth = true;
                properties.transpacency = true;
                properties.fill = true;
                properties.rotate = true;


                break;
            case 13:    
                //labelType = "Count";
                properties.fillColor = true;

                break;
            /*case 20:
                //labelType = "Image";
                properties.rotate = true;*/


        }
        
        return properties;

    }    


    function parseXML(text) {
        var doc = null;

        if(window.DOMParser){
            var parser = new DOMParser();
            doc = parser.parseFromString(text, "text/xml");
        }else if(window.ActiveXObject){
            doc = new ActiveXObject("Microsoft.XMLDOM");
            doc.async = "false";
            doc.loadXML(text);
        }else{
            throw new Error("Cannot parse XML");
        }
        return doc;
    }

    function addFill(type, name, HatchImage, size, color, szdescript){
        if(type == 'color'){
            setColorFill(name, color, szdescript);
        }else if (type == 'hatch'){
            setHatch(HatchImage, name, size, color, szdescript);
        }
    } 

    function setColorFill(name, color, szdescript){
        
        HatchPtrns.push({type : 'color', name : name, ptrn : null, tilesize : 0, color : color, description : szdescript, inuse : false});

    }

    function setHatch(HatchImage, name, size , color, szdescript){
        
        HatchPtrns.push({type : 'hatch', name : name, ptrn : HatchImage, tilesize : size, color : color, description : szdescript, inuse : false});
    }

    function getHatches(){
        
        return HatchPtrns;
    }

    /*function setHatch(HatchPtrn, name){
        HatchPtrns.push({name : name, ptrn : HatchPtrn });
    }*/

    function getHatchImagebyName(name){
        var HatchObj = null;
        for (var i = 0;i < HatchPtrns.length; i++){
            if(name == HatchPtrns[i].name){
                //HatchPtrn = HatchPtrns[i].ptrn;
                //HatchPtrn = ctx.createPattern(HatchPtrns[i].ptrn, "repeat");
                HatchObj = HatchPtrns[i];
                break;
            }
        }
        return HatchObj;

    }


    function setHatchDescription (name, szdescr){

        for (var i = 0;i < HatchPtrns.length; i++){
            if(name == HatchPtrns[i].name){
                //HatchPtrn = HatchPtrns[i].ptrn;
                HatchPtrns[i].description = szdescr;
                break;
            }
        }
       
    }

    function setHatchinUse(name, onOff){
        for (var i = 0;i < HatchPtrns.length; i++){
            if(name == HatchPtrns[i].name){
                //HatchPtrn = HatchPtrns[i].ptrn;
                HatchPtrns[i].inuse = onOff;

                if (RxCore_GUI_HatchChange != undefined){
                    RxCore_GUI_HatchChange.setHatchchange(HatchPtrns[i]);
                }

                break;
            }
        }

    }

    function getFillTypeByName(name){
        var type = null;

        for (var i = 0;i < HatchPtrns.length; i++){
            if(name == HatchPtrns[i].name){
                //HatchPtrn = HatchPtrns[i].ptrn;
                type = HatchPtrns[i].type;

                break;
            }
        }
        
        //return HatchPtrn;

        return type;

    }

    function getFillColorByName(name){
        var Color = null;

        for (var i = 0;i < HatchPtrns.length; i++){
            if(name == HatchPtrns[i].name){
                //HatchPtrn = HatchPtrns[i].ptrn;
                Color = HatchPtrns[i].color;

                break;
            }
        }
        
        //return HatchPtrn;

        return Color;

    }

    function getHatchbyName(ctx, name){
        var HatchPtrn = null;

        for (var i = 0;i < HatchPtrns.length; i++){
            if(name == HatchPtrns[i].name && HatchPtrns[i].type == 'hatch'){
                //HatchPtrn = HatchPtrns[i].ptrn;
                if(HatchPtrns[i].ptrn != null){
                    HatchPtrn = ctx.createPattern(HatchPtrns[i].ptrn, "repeat");
                }

                break;
            }
        }
        
        //return HatchPtrn;

        return HatchPtrn;
    }

    function getHatcTypeforName(Name){
        //diagonal-forward = 0
        //diagonal-back = 1
        //diagonal-cross = 2
        //hatch-horizontal = 3
        //hatch-vertical = 4
        //hatch-cross = 5

        var hatchtype = 0;

        if(Name == "diagonal-forward"){
            hatchtype = 3;
        }else if (Name == "diagonal-back"){
            hatchtype = 2;
        }else if (Name == "diagonal-cross"){
            hatchtype = 5;
        }else if (Name == "hatch-horizontal"){
            hatchtype = 0;
        }else if (Name == "hatch-vertical"){
            hatchtype = 1;
        }else if (Name == "hatch-cross"){
            hatchtype = 4;
        }else{
            hatchtype = -1;
        }

        return hatchtype;
    }

    function getHatchNameforType(type){
        //diagonal-forward = 0
        //diagonal-back = 1
        //diagonal-cross = 2
        //hatch-horizontal = 3
        //hatch-vertical = 4
        //hatch-cross = 5

        var hatchname = "diagonal-forward";

        switch (type) {
            case 0:
                hatchname = "hatch-horizontal";
                break;
            case 1:
                hatchname = "hatch-vertical";
                break;
            case 2:
                hatchname = "diagonal-back";
                break;
            case 3:
                hatchname = "diagonal-forward";
                break;
            case 4:
                hatchname = "hatch-cross";
                break;
            case 5:
                hatchname = "diagonal-cross";
                break;
        }

        return hatchname;
    }
    
    function updateHatchColor(type, color){

        
        var hatchobj = getHatchImagebyName(getHatchNameforType(type));

        if(hatchobj == null){
            return;
        }

        //HatchPtrns.push({name : name, ptrn : HatchImage, tilesize : size, color : color});

        if(isCanvas(hatchobj.ptrn)){

            if(hatchobj.color != color ){
                var pctx = hatchobj.ptrn.getContext('2d');
                drawhatchtype(pctx, type, color);
            }

        }else{
            createHatch(type, color);
        }

    }    

    function drawhatchtype(pctx, type, color){

        //diagonal-forward = 0
        //diagonal-back = 1
        //diagonal-cross = 2
        //hatch-horizontal = 3
        //hatch-vertical = 4
        //hatch-cross = 5


        pctx.strokeStyle = color;
        pctx.lineWidth = 1;

        switch (type) {
            case 3:
                pctx.beginPath();
                pctx.moveTo(0.0, 10.0);
                pctx.lineTo(10.0, 0.0);
                pctx.moveTo(10.0, 16.0);
                pctx.lineTo(16.0, 10.0);
                pctx.moveTo(2.0, 16.0);
                pctx.lineTo(16.0, 2.0);
                pctx.moveTo(0.0, 2.0);
                pctx.lineTo(2.0, 0.0);
                pctx.stroke();
                break;
            case 2:
                pctx.beginPath();
                pctx.moveTo(0.0, 14.0);
                pctx.lineTo(2.0, 16.0);
                pctx.moveTo(0.0, 6.0);
                pctx.lineTo(10.0, 16.0);
                pctx.moveTo(2.0, 0.0);
                pctx.lineTo(16.0, 14.0);
                pctx.moveTo(10.0, 0.0);
                pctx.lineTo(16.0, 6.0);
                pctx.stroke();
                break;
            case 5:
                pctx.beginPath();
                pctx.moveTo(0.0, 10.0);
                pctx.lineTo(10.0, 0.0);
                pctx.moveTo(10.0, 16.0);
                pctx.lineTo(16.0, 10.0);
                pctx.moveTo(2.0, 16.0);
                pctx.lineTo(16.0, 2.0);
                pctx.moveTo(0.0, 2.0);
                pctx.lineTo(2.0, 0.0);

                pctx.moveTo(0.0, 14.0);
                pctx.lineTo(2.0, 16.0);
                pctx.moveTo(0.0, 6.0);
                pctx.lineTo(10.0, 16.0);
                pctx.moveTo(2.0, 0.0);
                pctx.lineTo(16.0, 14.0);
                pctx.moveTo(10.0, 0.0);
                pctx.lineTo(16.0, 6.0);
                pctx.stroke();


                break;
            case 0:
                pctx.beginPath();
                pctx.moveTo(0.0, 4.0);
                pctx.lineTo(16.0, 4.0);
                pctx.moveTo(0.0, 12.0);
                pctx.lineTo(16.0, 12.0);

                pctx.stroke();

                break;
            case 1:
                pctx.beginPath();
                pctx.moveTo(4.0, 0.0);
                pctx.lineTo(4.0, 16.0);
                pctx.moveTo(12.0, 0.0);
                pctx.lineTo(12.0, 16.0);

                pctx.stroke();

                break;
            case 4:
                pctx.beginPath();
                pctx.moveTo(0.0, 4.0);
                pctx.lineTo(16.0, 4.0);
                pctx.moveTo(0.0, 12.0);
                pctx.lineTo(16.0, 12.0);

                pctx.moveTo(4.0, 0.0);
                pctx.lineTo(4.0, 16.0);
                pctx.moveTo(12.0, 0.0);
                pctx.lineTo(12.0, 16.0);
                pctx.stroke();
                break;
        }

    }    

    function createHatchtypeImage(type, color){
        var localpattern = document.createElement('canvas');

        localpattern.width = 16;
        localpattern.height = 16;

        var pctx = localpattern.getContext('2d');

        var hatchname = getHatchNameforType(type);

        drawhatchtype(pctx, type, color);

        var size = {w : 16, h : 16};
        
        return {image : localpattern, name : hatchname, size : size, color : color, description : "no description"};


    }

    function createHatch(type, color){
        //diagonal-forward = 0
        //diagonal-back = 1
        //diagonal-cross = 2
        //hatch-horizontal = 3
        //hatch-vertical = 4
        //hatch-cross = 5

        var localpattern = document.createElement('canvas');

        localpattern.width = 16;
        localpattern.height = 16;

        var pctx = localpattern.getContext('2d');

        var hatchname = getHatchNameforType(type);

        drawhatchtype(pctx, type, color);

        var size = {w : 16, h : 16};

        setHatch(localpattern, hatchname, size, color, "no description");
    }

    /*function getHatchbyName(name){
        var HatchPtrn = null;
        for (var i = 0;i < HatchPtrns.length; i++){
            if(name == HatchPtrns[i].name){
                HatchPtrn = HatchPtrns[i].ptrn;
                break;
            }
        }
        return HatchPtrn;
    }*/

    function getHatch(ctx, type, color) {


        pattern = document.createElement('canvas');

        pattern.width = 16;
        pattern.height = 16;

        var pctx = pattern.getContext('2d');

        drawhatchtype(pctx, type, color);


        var HatchPtrn = ctx.createPattern(pattern, "repeat");
        return HatchPtrn;

    }


    

    function getURLFile(s){
        var path = "";
        var sp = s.split('/');
        for (var i = 0; i < sp.length - 1; i++) {
            path += sp[i] + "/";
        }
        //var file = sp[sp.length-1];
        //alert(file);
        return sp[sp.length-1];

    }

    function getURLPath(s) {
        var path = "";
        
        if(s){
            var sp = s.split('/');
            for (var i = 0; i < sp.length - 1; i++) {
                path += sp[i] + "/";
            }
    
            if (bUseID){
                path = xmlurlrel;
            }
        }
        //var file = sp[sp.length-1];
        //alert(file);
        return path;
    }

    function getPath(s) {
        var path = "";
        var sp = s.split('\\');
        for (var i = 0; i < sp.length - 1; i++) {
            path += sp[i] + "\\";
        }
        //var file = sp[sp.length-1];
        //alert(file);
        if(!path) {
            path = s;
        }

        return path;

    }

    function getFileName(s) {
     var path = "";
     var file = "";
      if(s){
        if(s.indexOf('https://') > -1){
            file = getURLFile(s);
         }else if (s.indexOf('http://') > -1){
            file = getURLFile(s);
         }else{
            var sp = s.split('\\');
            for (var i = 0; i < sp.length - 1; i++) {
                    path += sp[i] + "\\";
                }
            file = sp[sp.length - 1];
            //alert(file);
         }
      }
     return file;

    }

    /*function getFileName(s) {
        var path = "";
        var file = "";
        var sp = s.split('\\');
        for (var i = 0; i < sp.length - 1; i++) {
            path += sp[i] + "\\";
        }
        file = sp[sp.length - 1];
        //alert(file);
        return file;

    }*/

    function addSlash(s) {
        var path = "";
        var sp = s.split('\\');
        for (var i = 0; i < sp.length; i++) {
            if (i < sp.length - 1) {
                path += sp[i] + "\\\\";
            } else {
                //path += sp[i];
            }

        }
        if(!path) {
            path = s;
        }

        return path;
    }


    function getAngleWithXAxis(r, x, y, onoff) {
        //ranges: -22.5 to 22.5, 22.5 to 67.5, 67.5 to 112.5, 112.5 to 157.5, 157.5 to 202.5, 202.5 to 247.5, 247.5 to 292.5,292.5 to 337.5
        
        //new gltf 3d adaption
        const moveForward = 1;
        const moveBackward = 2;
        const moveLeft = 3;
        const moveRight = 4;
        const moveUp = 5;
        const moveDown = 6;
        //new gltf 3d adaption

        var ranges = [
            {
                minX:r * Math.cos(22.5 * (Math.PI / 180)),
                maxX:r * Math.cos(0)
            },
            {
                minX:r * Math.cos(67.5 * (Math.PI / 180)),
                maxX:r * Math.cos(22.5 * (Math.PI / 180))
            },
            {
                minX:r * Math.cos(112.5 * (Math.PI / 180)),
                maxX:r * Math.cos(67.5 * (Math.PI / 180))
            },
            {
                minX:r * Math.cos(157.5 * (Math.PI / 180)),
                maxX:r * Math.cos(112.5 * (Math.PI / 180))
            },
            {
                minX:r * Math.cos(180 * (Math.PI / 180)),
                maxX:r * Math.cos(157.5 * (Math.PI / 180))
            },
            {
                minX:r * Math.cos(202.5 * (Math.PI / 180)),
                maxX:r * Math.cos(247.5 * (Math.PI / 180))
            },
            {
                minX:r * Math.cos(247.5 * (Math.PI / 180)),
                maxX:r * Math.cos(292.5 * (Math.PI / 180))
            },
            {
                minX:r * Math.cos(292.5 * (Math.PI / 180)),
                maxX:r * Math.cos(337.5 * (Math.PI / 180))
            }
        ];


        /*
         , Direction: {
         right: 'Right',
         topRight: 'TopRight',
         forward: 'Forward',
         topLeft: 'TopLeft',
         left: 'Left',
         bottomLeft: 'BottomLeft',
         backward: 'Backward',
         bottomRight: 'BottomRight'
         }

         */

        //this.moveState = { up: 0, down: 0, left: 0, right: 0, forward: 0, back: 0, pitchUp: 0, pitchDown: 0, yawLeft: 0, yawRight: 0, rollLeft: 0, rollRight: 0 };


        //var bonoff = (onoff == 1);

        var distance = Math.sqrt((Math.abs(x) * Math.abs(x)) + (Math.abs(y) * Math.abs(y)));


        var _endNormal = new THREE.Vector2(x, y).normalize();
        //we make all positions project on circle, as we multiply scale 'r'.
        var _end = _endNormal.clone().multiplyScalar(r);

        //console.log(_end.x);
        //console.log(_end.y);
        //so we only need to compare the 2 ranges in same quadrant
        if (_end.x >= 0 && _end.y >= 0) {
            //first quadrant
            if (distance > 50) {
                if (ranges[0].maxX >= _end.x && _end.x >= ranges[0].minX) {
                    //right

                    //new gltf 3d adaption
                    //DocObj.getCurrentpage().walkthroughcontrol.moveRight = (onoff == 1);
                    DocObj.getCurrentpage().walkthroughcontrol.movedirection(moveRight,(onoff == 1));

                    //return 'Right';//this.Direction.right;
                } else if (ranges[1].maxX >= _end.x && _end.x >= ranges[1].minX) {
                    //top right
                    //DocObj.getCurrentpage().walkthroughcontrol.moveState.rollRight = onoff;
                    //console.log('top right');
                    //return 'TopRight';//this.Direction.topRight;
                } else if (ranges[2].maxX >= _end.x && _end.x >= ranges[2].minX) {
                    //forward
                    //console.log('forward');
                    
                    //new gltf 3d adaption
                    //DocObj.getCurrentpage().walkthroughcontrol.moveForward = (onoff == 1);
                    DocObj.getCurrentpage().walkthroughcontrol.movedirection(moveForward,(onoff == 1));


                    //return 'Forward';// this.Direction.forward;
                }

            } else {

                //new gltf 3d adaption
                //DocObj.getCurrentpage().walkthroughcontrol.moveUp = (onoff == 1);
                DocObj.getCurrentpage().walkthroughcontrol.movedirection(moveUp,(onoff == 1));


                //return 'Up';
            }
        } else if (_end.x <= 0 && _end.y >= 0) {
            //second quadrant
            if (distance > 50) {
                if (ranges[2].maxX >= _end.x && _end.x >= ranges[2].minX) {
                    //forward
                    //console.log('forward');

                    //new gltf 3d adaption
                    //DocObj.getCurrentpage().walkthroughcontrol.moveForward = (onoff == 1);
                    DocObj.getCurrentpage().walkthroughcontrol.movedirection(moveForward,(onoff == 1));


                    //return 'Forward';//this.Direction.forward;
                } else if (ranges[3].maxX >= _end.x && _end.x >= ranges[3].minX) {
                    //top left
                    //DocObj.getCurrentpage().walkthroughcontrol.moveState.rollLeft = onoff;
                    //console.log('top left');
                    //return 'TopLeft';//this.Direction.topLeft;
                } else if (ranges[4].maxX >= _end.x && _end.x >= ranges[4].minX) {
                    //left
                    //console.log('left');

                    //new gltf 3d adaption
                    //DocObj.getCurrentpage().walkthroughcontrol.moveLeft = (onoff == 1);
                    DocObj.getCurrentpage().walkthroughcontrol.movedirection(moveLeft,(onoff == 1));

                    //return 'Left';//this.Direction.left;
                }

            } else {

                //new gltf 3d adaption
                //DocObj.getCurrentpage().walkthroughcontrol.moveUp = (onoff == 1);
                DocObj.getCurrentpage().walkthroughcontrol.movedirection(moveUp,(onoff == 1));


                //return 'Up';
            }
        } else if (_end.x <= 0 && _end.y <= 0) {
            //third quadrant
            if (distance > 50) {
                if (ranges[4].maxX >= _end.x && _end.x >= ranges[4].minX) {
                    //left
                    //console.log('left');
                    
                    //new gltf 3d adaption
                    //DocObj.getCurrentpage().walkthroughcontrol.moveLeft = (onoff == 1);
                    DocObj.getCurrentpage().walkthroughcontrol.movedirection(moveLeft,(onoff == 1));

                    //return 'Left';// this.Direction.left;
                } else if (ranges[5].maxX >= _end.x && _end.x >= ranges[5].minX) {
                    //bottom left
                    //console.log('bottom left');
                    //return 'BottomLeft';// this.Direction.bottomLeft;
                } else if (ranges[6].maxX >= _end.x && _end.x >= ranges[6].minX) {
                    //backward
                    //console.log('backward');

                    //new gltf 3d adaption
                    //DocObj.getCurrentpage().walkthroughcontrol.moveBackward = (onoff == 1);
                    DocObj.getCurrentpage().walkthroughcontrol.movedirection(moveBackward,(onoff == 1));
                    //return 'Backward';//this.Direction.backward;
                }
            } else {

                //new gltf 3d adaption
                //DocObj.getCurrentpage().walkthroughcontrol.moveDown = (onoff == 1);
                DocObj.getCurrentpage().walkthroughcontrol.movedirection(moveDown,(onoff == 1));

                //return 'Down';

            }
        } else if (_end.x >= 0 && _end.y <= 0) {
            //fourth quadrant
            if (distance > 50) {
                if (ranges[6].maxX >= _end.x && _end.x >= ranges[6].minX) {
                    //backward
                    //console.log('backward');

                    //new gltf 3d adaption
                    //DocObj.getCurrentpage().walkthroughcontrol.moveBackward = (onoff == 1);
                    DocObj.getCurrentpage().walkthroughcontrol.movedirection(moveBackward,(onoff == 1));

                    //return 'Backward';// this.Direction.backward;
                } else if (ranges[7].maxX >= _end.x && _end.x >= ranges[7].minX) {
                    //bottom right
                    //console.log('bottom right');
                    //return 'BottomRight';// this.Direction.bottomRight;
                } else if (ranges[0].maxX >= _end.x && _end.x >= ranges[0].minX) {
                    //right
                    //console.log('right');

                    //new gltf 3d adaption
                    //DocObj.getCurrentpage().walkthroughcontrol.moveRight = (onoff == 1);
                    DocObj.getCurrentpage().walkthroughcontrol.movedirection(moveRight,(onoff == 1));

                    //return 'Right';// this.Direction.right;
                }

            } else {

                //new gltf 3d adaption
                //DocObj.getCurrentpage().walkthroughcontrol.moveDown = (onoff == 1);
                DocObj.getCurrentpage().walkthroughcontrol.movedirection(moveDown,(onoff == 1));

                //return 'Down';

            }
        }

        //}


    }

    function SetCalibration(val) {
        if (val) {
            if (nCalibrateSet != 0) {
                nCalibrateScale = nCalibrateMeasured / nCalibrateSet;
                nCalibrateScale = 1 / nCalibrateScale;
                setCalibratebtn();
                //var scaledropdwn = document.getElementById('scaledropdown');
                //scaledropdwn.children[0].value = 'Calibration';
                MeasureScale = nCalibrateScale;
            }


        } else {
            nCalibrateScale = 1;
        }

    }

    function SetUserMarkupdisplay(numuser, state) {
        var curmarkup = 0;
        Userlist[numuser].display = state;

        for (curmarkup = 0; curmarkup < DocObj.markuplist.length; curmarkup++) {
            var layerdisplay = getMarkupLayerState(DocObj.markuplist[curmarkup].layer);

            if (DocObj.markuplist[curmarkup].signature == Userlist[numuser].Signature && layerdisplay) {

                DocObj.markuplist[curmarkup].display = Userlist[numuser].display;
            }

        }
        drawmarkupAll(cntximg);
        DrawMarkupSelected(context);

    }

    function SetLayerMarkupdisplay(numlayer, state) {
        var curmarkup = 0;
        Layerlist[numlayer].display = state;

        for (curmarkup = 0; curmarkup < DocObj.markuplist.length; curmarkup++) {

            var signdisplay = getSignState(DocObj.markuplist[curmarkup].signature);

            if (DocObj.markuplist[curmarkup].layer == Layerlist[numlayer].Layer && signdisplay) {

                DocObj.markuplist[curmarkup].display = Layerlist[numlayer].display;
            }

        }
        drawmarkupAll(cntximg);
        DrawMarkupSelected(context);

    }


    function setSignature(sign) {
        signature = sign;

    }

    function SetLayer(layer) {

        markuplayer = layer;
        markupcolor = Layerlist[layer].Color;
        markuplinecolor = Layerlist[layer].Color;
        markuptextcolor = Layerlist[layer].Color;
        ChangeMarkupSelected(0);
    }


    function SetTempCal(val) {

        nCalibrateSet = val;


    }

    function GetTempID() {

        return DocObj.selectedmarkup;
    }

    /*function GetNoteText(markupobj){
     var notetext = SzNoteText;
     showNDialog(false);
     notetext = SzNoteText;
     SzNoteText = "";
     return notetext;
     }*/

    function getpage(pagenum) {
        DocObj.GotoPage(pagenum);
    }

    function getDocObj() {
        return DocObj;
    }

    function getUnitArea(value) {


        //var Unitofmeasure = DocObj.getCurrentpage().Unitofmeasure;

        var Unitofmeasure = DocObj.getCurrentpage().scaleObject.Unitofmeasure;
        if(markupForScale) {
            Unitofmeasure = markupForScale.scaleObject.Unitofmeasure;
        }


        var dimValue = 0;
        var dpi = DocObj.getCurrentpage().DPI;
        var DPmm = dpi / 25.4;
        var DPInch = dpi;


        //var mscale = MeasureScale;
        var mscale = DocObj.getCurrentpage().scaleObject.getMeasureScale();

        if(markupForScale) {
            mscale = markupForScale.scaleObject.getMeasureScale();
        }



        if (mscale ==  undefined){
            mscale = MeasureScale;
        }

        //var unit = currentglobalunit;
        if (DocObj.getCurrentpage().usepdfjs) {
            dpi = 72;
            DPmm = dpi / 25.4;
            DPInch = dpi;
        }
        
        var Dpmmsquare = DPmm * DPmm;
        var DPInchSquare = DPInch * DPInch;
        var measureScaleSquared = mscale * mscale;
        if (DocObj.getCurrentpage().usevectorxml) {
            var mainimagesquare = DocObj.getCurrentpage().OriginalScale * DocObj.getCurrentpage().OriginalScale;
        } else if (DocObj.getCurrentpage().usepdfjs) {
            mainimagesquare = DocObj.getCurrentpage().OriginalScale * DocObj.getCurrentpage().OriginalScale;
        } else {
            mainimagesquare = DocObj.getCurrentpage().MainImageScaling * DocObj.getCurrentpage().MainImageScaling;
        }


        var dscalesquare = DocObj.getCurrentpage().dscale * DocObj.getCurrentpage().dscale;

        if (DocObj.getCurrentpage().usevectorxml) {
            dscalesquare = DocObj.getCurrentpage().dscalevector * DocObj.getCurrentpage().dscalevector;
        } else if (DocObj.getCurrentpage().usepdfjs) {
            dscalesquare = (DocObj.getCurrentpage().curpagescale * DocObj.getCurrentpage().dscalepdf) * (DocObj.getCurrentpage().curpagescale * DocObj.getCurrentpage().dscalepdf);
        }

        //var scalefactor = DocObj.getCurrentpage().dscale / this.scaling;
        //var scalefactorsquare = scalefactor * scalefactor;
        var orignalscalesquare = DocObj.getCurrentpage().OriginalScale * DocObj.getCurrentpage().OriginalScale;


        //dimValue = ((value  / mainimagesquare) / dscalesquare)*measureScaleSquared;//*scalefactorsquare;
        //change to make value independent of current zoom factor.
        dimValue = (value / mainimagesquare) * measureScaleSquared; //*scalefactorsquare;
        // dimValue = dimValue / (unitscale * unitscale);

        // dimValue = dimValue / (unitscale * unitscale);

        //var unitScaleValue = DocObj.getCurrentpage().getUnitScale();

        var unitScaleValue = DocObj.getCurrentpage().scaleObject.getUnitScale();

        if(markupForScale) {
            unitScaleValue = markupForScale.scaleObject.getUnitScale();
        }


        dimValue = dimValue / (unitScaleValue * unitScaleValue);





        if (dpi != 0) {
            switch (Unitofmeasure) {
                case 1:
                    dimValue = dimValue / Dpmmsquare;
                    break;
                case 2:
                    dimValue = dimValue / DPInchSquare;
                    break;
                case 3:
                    //dimValue = dimValue;
                    break;
            }


        } else {
            if (!DocObj.getCurrentpage().usevectorxml) {
                dimValue = dimValue / orignalscalesquare;
            }

        }
        return dimValue;
    }

    function getScreenDim(length, munit){

        var pixelwidth = 0;

        if(docpagNotReady()){
            return pixelwidth;
        }

        var dpi = DocObj.getCurrentpage().DPI;
        //var Unitofmeasure = DocObj.getCurrentpage().Unitofmeasure;
        var Unitofmeasure = DocObj.getCurrentpage().scaleObject.Unitofmeasure;
        var DPmm = dpi / 25.4;
        var DPInch = dpi;
        
        /*if(DocObj.getCurrentpage().usevectorxml){
            dpi = 0;
        }*/

        if (DocObj.getCurrentpage().usepdfjs) {
            dpi = 72;
            DPmm = dpi / 25.4;
            DPInch = dpi;
        }

        length = length * unitscale;
        //length *= DocObj.getCurrentpage().getdscale();

        //var mscale = MeasureScale;
        var mscale = DocObj.getCurrentpage().scaleObject.getMeasureScale();
        

        if(markupForScale) {
            mscale = markupForScale.scaleObject.getMeasureScale();
        }

        if (mscale == undefined){
            mscale = MeasureScale;
        }

        

        if (DocObj.getCurrentpage().usevectorxml || DocObj.getCurrentpage().usepdfjs || DocObj.getCurrentpage().usevector3Dxml) {
            //length = ((value / DocObj.getCurrentpage().OriginalScale)) * mscale;
            pixelwidth = (length / mscale) * DocObj.getCurrentpage().OriginalScale;   
        } else {
            //length = ((value / DocObj.getCurrentpage().MainImageScaling)) * mscale;
            pixelwidth = (length / mscale) * DocObj.getCurrentpage().MainImageScaling;
        }

        

        //pixelwidth = pixelwidth * DPmm;

        if (dpi != 0) {
            switch (Unitofmeasure) {
                case 1:
                    //dimValue = dimValue / DPmm;
                    pixelwidth = pixelwidth * DPmm;
                    break;
                case 2:
                    //dimValue = dimValue / DPInch;
                    pixelwidth = pixelwidth * DPInch;
                    break;
                case 3:
                    //dimvalue= dimvalue;
                    break;
            }


        } else {
            /*if (!DocObj.getCurrentpage().usevectorxml) {
                pixelwidth = pixelwidth * DocObj.getCurrentpage().getdscale();

            }*/


        }

        return pixelwidth;

    }

    var gcd = function(a, b) {
        if (b < 0.0000001) return a;                // Since there is a limited precision we need to limit the value.
      
        return gcd(b, Math.floor(a % b));           // Discard any fractions due to limitations in precision.
    };

    function getUnitlengthRemainder(value){
        var dimValue = 0;
        var remainder = 0;
        var remainddec = 0;


        if(docpagNotReady()){
            return;
        }

        var dpi = DocObj.getCurrentpage().DPI;
        var DPmm = dpi / 25.4;
        var DPInch = dpi;

        var mscale = DocObj.getCurrentpage().scaleObject.getMeasureScale();
        if(markupForScale) {
            mscale = markupForScale.scaleObject.getMeasureScale();
        }


        if (mscale ==  undefined){
            mscale = MeasureScale;
        }

        if (DocObj.getCurrentpage().usepdfjs) {
            dpi = 72;
            DPmm = dpi / 25.4;
            DPInch = dpi;
        }

        if (DocObj.getCurrentpage().usevectorxml || DocObj.getCurrentpage().usepdfjs || DocObj.getCurrentpage().usevector3Dxml) {
            dimValue = ((value / DocObj.getCurrentpage().OriginalScale)) * mscale; // * scalefactor;
        } else {
            dimValue = ((value / DocObj.getCurrentpage().MainImageScaling)) * mscale; // * scalefactor;
        }


        //dimValue = dimValue / unitscale;

        var unitScaleValue = DocObj.getCurrentpage().scaleObject.getUnitScale();
        if(markupForScale) {
            unitScaleValue = markupForScale.scaleObject.getUnitScale();
        }

        
        dimValue = dimValue / unitScaleValue;

        if (dpi != 0) {
            
            dimValue = dimValue / DPInch;


        } else {
            if (!DocObj.getCurrentpage().usevectorxml) {
                dimValue = dimValue / DocObj.getCurrentpage().OriginalScale;
            }


        }

        var rnddimValue = Math.floor(dimValue / unitscale);
        remainder = dimValue % unitscale;
        var rndrem = Math.floor(remainder);
        var remainddec = remainder - rndrem;

        remainddec = Math.round(remainddec * nFractionRes) / nFractionRes;

        var fractionlimit = 1 / nFractionRes;

        if (remainddec > fractionlimit){
            var len = remainddec.toString().length - 2;

            var nDenominator = Math.pow(10, len);
            var nNumerator = remainddec * nDenominator;
            
            var divisor = gcd(nNumerator, nDenominator);    // Should be 5
            
            nNumerator /= divisor;                         // Should be 687
            nDenominator /= divisor;                       // Should be 2000
            var nretnumerator = Math.floor(nNumerator);
            var nretdenominator = Math.floor(nDenominator);

        }else{
            
            nretnumerator = 0;
            nretdenominator = 0;
            
        }
        

        return {quot : rnddimValue, rem : rndrem, remdec : remainddec, numerator : nretnumerator, denominator : nretdenominator};
    }

    function getUnitlength(value) {
        var dimValue = 0;

        if(docpagNotReady()){
            return dimValue;
        }

        //var Unitofmeasure = DocObj.getCurrentpage().Unitofmeasure;

        var Unitofmeasure = DocObj.getCurrentpage().scaleObject.Unitofmeasure;
        if(markupForScale) {
            Unitofmeasure = markupForScale.scaleObject.Unitofmeasure;
        }


        var remainder = 0;
        var dpi = DocObj.getCurrentpage().DPI;
        var DPmm = dpi / 25.4;
        var DPInch = dpi;

        //var mscale = MeasureScale;

        var mscale = DocObj.getCurrentpage().scaleObject.getMeasureScale();
        if(markupForScale) {
            mscale = markupForScale.scaleObject.getMeasureScale();
        }


        if (mscale ==  undefined){
            mscale = MeasureScale;
        }

        //var scalefactor = DocObj.getCurrentpage().dscale / this.scaling;

        //dimValue = ((value  / DocObj.getCurrentpage().MainImageScaling) / DocObj.getCurrentpage().dscale)*MeasureScale;// * scalefactor;
        //value without the scale factor to estblish value independent from zoom factor.
        if (DocObj.getCurrentpage().usepdfjs) {
            dpi = 72;
            DPmm = dpi / 25.4;
            DPInch = dpi;
        }

        if (DocObj.getCurrentpage().usevectorxml || DocObj.getCurrentpage().usepdfjs || DocObj.getCurrentpage().usevector3Dxml) {
            dimValue = ((value / DocObj.getCurrentpage().OriginalScale)) * mscale; // * scalefactor;
        } else {
            dimValue = ((value / DocObj.getCurrentpage().MainImageScaling)) * mscale; // * scalefactor;
        }

        //dimValue = dimValue / unitscale;
        //remainder = dimValue % unitscale;

        //var unitScaleValue = DocObj.getCurrentpage().getUnitScale();

        var unitScaleValue = DocObj.getCurrentpage().scaleObject.getUnitScale();
        if(markupForScale) {
            unitScaleValue = markupForScale.scaleObject.getUnitScale();
        }


        dimValue = dimValue / unitScaleValue;
        remainder = dimValue % unitScaleValue;
        

        if (dpi != 0) {
            switch (Unitofmeasure) {
                case 1:
                    dimValue = dimValue / DPmm;
                    break;
                case 2:
                    dimValue = dimValue / DPInch;
                    break;
                case 3:
                    //dimvalue= dimvalue;
                    break;
            }


        } else {
            if (!DocObj.getCurrentpage().usevectorxml) {
                dimValue = dimValue / DocObj.getCurrentpage().OriginalScale;
            }


        }
        return dimValue;
    }

    function getFirstRedoOnly(array){
        var curredo = 0;
        var redobj;

        for(curredo = 0;curredo < array.length; curredo++){
            if(array[curredo].bredoonly){
                redobj = array[curredo];
                array.splice(curredo, 1);
                break;
                //redobj
            }
        }

        return redobj;

    }

    function MarkupRedoSingle(localmarkup){
        
        var found = false;

        if(localmarkup){
    
            if(!localmarkup.bredoonly){
                DocObj.markupundolist.push(localmarkup);
            }else{
                DocObj.markupredolist.push(localmarkup);
                
                localmarkup = getFirstRedoOnly(DocObj.markupredolist);
            }
    
        }
    
        if (localmarkup != undefined) {
            var id = localmarkup.markupnumber;
            var curmarkup = 0;
    
            for (curmarkup = 0; curmarkup < DocObj.markuplist.length; curmarkup++) {
                if (DocObj.markuplist[curmarkup] != null) {
                    if (DocObj.markuplist[curmarkup].markupnumber == localmarkup.markupnumber) {
    
                        localmarkup.PerformUndo(DocObj.markuplist[curmarkup], DocObj);
                        //.PerformUndo(localmarkup);
                        found = true;
                    }
                }
            }
            if (!found) {
     
                localmarkup.PerformUndo(undefined,DocObj);
    
     
            }            
    
        }


    }

    function MarkupRedo() {
        
        if(DocObj == null || DocObj == undefined){
            return;
        }
        
        
        var localmarkup = DocObj.markupredolist.pop();
    
        //console.log(DocObj.markupredolist.length);
    
        if(localmarkup){
            //localmarkup.bcurstate = true;
            while (localmarkup.bcurstate && DocObj.markupredolist.length > 0){
                DocObj.markupundolist.push(localmarkup);
                localmarkup = DocObj.markupredolist.pop();
            }
    
        }

        if(localmarkup){

            if(localmarkup.bMultiOp){
           

                MarkupRedoSingle(localmarkup);

                var tempredolist = [];
                var id = localmarkup.multiOpId;
                var ui = 0;
                
                for (ui=0; ui < DocObj.markupredolist.length; ui++){

                    if(DocObj.markupredolist[ui].bMultiOp && DocObj.markupredolist[ui].multiOpId == id){
                        MarkupRedoSingle(DocObj.markupredolist[ui]);
                    }else{
                        tempredolist.push(DocObj.markupredolist[ui]);
                    }
                }
                
                DocObj.markupredolist = tempredolist;
                

            }else{
                MarkupRedoSingle(localmarkup);
            }
            
        }

    
        if(DocObj.markupredolist.length == 0){
            if (RxCore_GUI_UndoRedoEmpty != undefined){
                
                RxCore_GUI_UndoRedoEmpty.listempty(false, true);
    
            }
        }
    
        
    
    }


    function MarkupUndoFromTemp(markup){

        var undoobj =  CheckcurStateExistTemp(markup.markupnumber, DocObj);
        if(undoobj != undefined){

            //check if first bmodify and mark as firstbmodify to skip in redo.
            if(isfirstundo(markup.markupnumber, DocObj.markupundolist)){
                
                undoobj.isfirstbmodify = true;
                //set flag on undoobj
            }

            DocObj.markupundolist.push(undoobj);
            RemovecurRedoState(markup.markupnumber, DocObj);
        }

    }


    function removecurstateflag(id, array){
        var cur = 0;

        for(cur = 0; cur < array.length; cur++){

            if (array[cur].markupnumber == id) {

                array[cur].bcurstate = false;   

            }
        }

    }

    function haslastredostate(markupnumber){

        var curmarkup = 0;
        var found = false;

        for (curmarkup = 0; curmarkup < DocObj.markupredolist.length; curmarkup++) {
            
                if (DocObj.markupredolist[curmarkup].markupnumber == markupnumber) {
                    
                    //DocObj.markuplist[curmarkup].PerformUndo(localmarkup);
                    found = true;
                }
            
        }
        return found;

    }


    function isfirstundo(id, array){
        var cur = 0;

        var numentries = 0;
        //var totalentries = 0;

        //var localmarkup = array.pop();
        //array.push(localmarkup);

        for(cur = 0; cur < array.length; cur++){

            if (array[cur].markupnumber == id) {

                    //totalentries++;

                if(array[cur].bmodify){
                    
                    numentries++;
                }
            }
        }
        
        return (numentries == 0);


    }

    function MarkupUndoSingle(localmarkup){

        var found = false;

        if(localmarkup){
    
            if(localmarkup.bcreate || localmarkup.bdelete){
                DocObj.markupredolist.push(localmarkup);
            }else{
                if(CheckcurstateExist(localmarkup.markupnumber, DocObj)){
                    MarkupSaveRedoState(localmarkup);
                }
    
                //
    
                removecurstateflag(localmarkup.markupnumber, DocObj.markupredolist);
                localmarkup.bcurstate = true;
    
                DocObj.markupredolist.push(localmarkup);
            }
            
        }
    
    
        var undodeletobj;
    
        if (localmarkup != undefined) {
            //var id = localmarkup.markupnumber;
    
            var curmarkup = 0;
            var curpoint = 0;
    
    
            for (curmarkup = 0; curmarkup < DocObj.markuplist.length; curmarkup++) {
                if (DocObj.markuplist[curmarkup] != null) {
                    if (DocObj.markuplist[curmarkup].markupnumber == localmarkup.markupnumber) {
    
                        localmarkup.PerformUndo(DocObj.markuplist[curmarkup], DocObj);
                        //DocObj.markuplist[curmarkup].PerformUndo(localmarkup);
                        found = true;
                    }
                }
            }
            if (!found) {
    
                localmarkup.PerformUndo(undefined, DocObj);
    
            }
        }

    }

    function MarkupUndo() {


        if(DocObj == null || DocObj == undefined){
            return;
        }
    
        
        
        //var lastundo = islastundo(DocObj.markupundolist);
        var localmarkup = DocObj.markupundolist.pop();

        if(localmarkup){
            if(localmarkup.bMultiOp){
           

                MarkupUndoSingle(localmarkup);

                var tempundolist = [];
                var id = localmarkup.multiOpId;
                var ui = 0;
                
                for (ui=0; ui < DocObj.markupundolist.length; ui++){

                    if(DocObj.markupundolist[ui].bMultiOp && DocObj.markupundolist[ui].multiOpId == id){
                        MarkupUndoSingle(DocObj.markupundolist[ui]);
                    }else{
                        tempundolist.push(DocObj.markupundolist[ui]);
                    }
                }
                
                DocObj.markupundolist = tempundolist;
                

            }else{
                MarkupUndoSingle(localmarkup);
            }
        }
    
        //console.log(DocObj.markupundolist.length);
    
    
        if(DocObj.markupundolist.length == 0){
            if (RxCore_GUI_UndoRedoEmpty != undefined){
                RxCore_GUI_UndoRedoEmpty.listempty(true, false);
            }
        }
    
        
        
    }    

    

    function displaydebuglabel(ctx, x, y, text) {
        var labelheight = 20;
        var labelwidth = 1;

        ctx.save();
        ctx.font = "10pt Arial";

        ctx.strokeStyle = "black";
        ctx.fillStyle = "rgba(113, 114, 118, 0.8)";
        ctx.lineWidth = 1;

        var labeltypewidth = ctx.measureText(text);
        var labetypesize = labeltypewidth.width;
        labelwidth = labetypesize;


        ctx.strokeRect(x, y + 30, labelwidth, labelheight);
        ctx.fillRect(x, y + 30, labelwidth, labelheight);

        ctx.fillStyle = "white";

        ctx.fillText(text, x + 3, y + 42);

        ctx.restore(); // restore context to what it was on entry

    }

    function MarkupPrintSave() {
        var curmarkup = 0;
        var undoobj;
        markupprintlist = [];
        //markupprintlist = DocObj.markuplist.slice(0);
        for (curmarkup = 0; curmarkup < DocObj.markuplist.length; curmarkup++) {
            if (DocObj.markuplist[curmarkup] != null) {
                DocObj.markuplist[curmarkup].drawn = false;
                undoobj = new MarkupUndoObject(DocObj.markuplist[curmarkup].markupnumber);
                undoobj.SetUndoValues(DocObj.markuplist[curmarkup]);
                markupprintlist.push(undoobj);

            }
        }

    }

    function RxCore_setmarkupRxGUID(){
        var curmarkup = 0;
        
        //markupprintlist = [];

        for (curmarkup = 0; curmarkup < DocObj.markuplist.length; curmarkup++) {
            if (DocObj.markuplist[curmarkup] != null) {

                if(!DocObj.markuplist[curmarkup].isempty){
                    if(DocObj.markuplist[curmarkup].getUniqueID() == "0"){
                        //console.log(DocObj.markuplist[curmarkup].type);
                        DocObj.markuplist[curmarkup].setRxUniqueID();
                    }else{
                        //DocObj.markuplist[curmarkup].getAttribute
                        var uniqueID = DocObj.markuplist[curmarkup].getUniqueID();
                        if (DocObj.markuplist[curmarkup].type == 8 && DocObj.markuplist[curmarkup].subtype == 1){
                            DocObj.markuplist[curmarkup].updateAttribute('SpaceID', uniqueID);
                        }
                    }
                }

            }
        }
        
    }


    function MarkupSaveStateGUID(GUID){
        for (var curdoc = 0;curdoc < OpenFiles.length;curdoc++){
            for (var curmarkup = 0; curmarkup < OpenFiles[curdoc].markuplist.length; curmarkup++) {
                if (OpenFiles[curdoc].markuplist[curmarkup] != null) {
                    if(OpenFiles[curdoc].markuplist[curmarkup].getUniqueID() == GUID){
                        MarkupSaveState(OpenFiles[curdoc].markuplist[curmarkup].markupnumber, undoModify);
                    }
                }
            }

        }

    }


    function CheckcurStateExistTemp(id, docRef){

        var curmarkup = 0;
        var undoobj = undefined;
        var undobjset = false;
        //var gottempstate = false;
        var tempundolistlocal = [];

        for (curmarkup = 0; curmarkup < docRef.tempundolist.length; curmarkup++) {
            if (docRef.tempundolist[curmarkup] != null) {


                if (docRef.tempundolist[curmarkup].markupnumber == id) {

                    undoobj = docRef.tempundolist[curmarkup];
                    /*if(!undobjset){
                        undobjset = true;
                    }*/

                }else{
                    tempundolistlocal.push(docRef.tempundolist[curmarkup]);
                }
            }
        }

        docRef.tempundolist = [];
        docRef.tempundolist = tempundolistlocal;

        return undoobj;

        //docRef.markupundolist.push(undoobj);
       
    }

    function ReplacecurRedostate(id,docRef, redoobj){
        var curmarkup = 0;
        var undoobj;
        var baddredocurstate = true;

        for (curmarkup = 0; curmarkup < docRef.markupredolist.length; curmarkup++) {
            if (docRef.markupredolist[curmarkup] != null) {
                if (docRef.markupredolist[curmarkup].markupnumber == id) {

                    if(docRef.markupredolist[curmarkup].bredoonly){
                        docRef.markupredolist[curmarkup] = redoobj;
                        //baddredocurstate = false;
                    }

                }
            }
        }
    }

    function CheckcurstateExist(id, docRef){
        var curmarkup = 0;
        var undoobj;
        var baddredocurstate = true;

        for (curmarkup = 0; curmarkup < docRef.markupredolist.length; curmarkup++) {
            if (docRef.markupredolist[curmarkup] != null) {
                if (docRef.markupredolist[curmarkup].markupnumber == id) {

                    if(docRef.markupredolist[curmarkup].bredoonly){
                        baddredocurstate = false;
                    }

                }
            }
        }

        return baddredocurstate;

    }

    function RemovecurRedoState(id, docRef){
        var curmarkup = 0;
        var localredolist = [];
        var markupcount = 0;
        
        for (curmarkup = 0; curmarkup < docRef.markupredolist.length; curmarkup++) {
            if (docRef.markupredolist[curmarkup] != null) {
                if (docRef.markupredolist[curmarkup].markupnumber == id) {

                    if(docRef.markupredolist[curmarkup].bredoonly){
                        //baddredocurstate = false;
                    }else{
                        localredolist[markupcount] = docRef.markupredolist[curmarkup];
                        markupcount ++;
                    }
                }
            }
        }        //DocObj.markupredolist.unshift(undoobj);

        docRef.markupredolist = [];
        docRef.markupredolist = localredolist;


    }

    function GetMarkupSaveRedoState(id){
        var curmarkup = 0;
        var undoobj;


        for (curmarkup = 0; curmarkup < DocObj.markuplist.length; curmarkup++) {
            if (DocObj.markuplist[curmarkup] != null) {
                if (DocObj.markuplist[curmarkup].markupnumber == id) {
                    undoobj = new MarkupUndoObject(id);

                    undoobj.bmodify = true;
                    undoobj.bredoonly = true;


                    undoobj.SetUndoValues(DocObj.markuplist[curmarkup]);
                    //DocObj.markupredolist.push(undoobj);
                    //DocObj.markupredolist.unshift(undoobj);
                    

                    /*if (RxCore_GUI_Markup != undefined) {
                        RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[curmarkup], {created : false, modified : true, deleted : false});
                    }*/

                }
            }
        }
        return undoobj;

    }

    function MarkupSaveRedoState(paramundoobj) {
        var curmarkup = 0;
        var undoobj;

        for (curmarkup = 0; curmarkup < DocObj.markuplist.length; curmarkup++) {
            if (DocObj.markuplist[curmarkup] != null) {
                if (DocObj.markuplist[curmarkup].markupnumber == paramundoobj.markupnumber) {

                    undoobj = new MarkupUndoObject(paramundoobj.markupnumber);

                    if(paramundoobj.bMultiOp){

                        undoobj.setmultiValues(paramundoobj.multiOpId);
        
                    }

                    undoobj.bmodify = true;
                    undoobj.bredoonly = true;


                    undoobj.SetUndoValues(DocObj.markuplist[curmarkup]);
                    //DocObj.markupredolist.push(undoobj);
                    DocObj.markupredolist.unshift(undoobj);
                    

                    /*if (RxCore_GUI_Markup != undefined) {
                        RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[curmarkup], {created : false, modified : true, deleted : false});
                    }*/

                }
            }
        }

    }

    function MarkupSaveStateChangedMulti(operation){
        var curmarkup = 0;

        /*if(DocObj.markupselectedChangeSaved){
            return;
        }*/

        nMarkupUndoIdstart += 1;
        var nmodifyid = nMarkupUndoIdstart;


        for (curmarkup = 0; curmarkup < DocObj.markuplist.length; curmarkup++) {
            if (DocObj.markuplist[curmarkup] != null) {

                var bseledit = (DocObj.markuplist[curmarkup].selectedit && DocObj.markuplist[curmarkup].type != 9);
                var bselected = (DocObj.markuplist[curmarkup].selected || bseledit);

                if (bselected) {
                    
                    if(DocObj.markuplist[curmarkup].bisTextArrow){
                        continue;
                    }

                    DocObj.markuplist[curmarkup].setmultiValues(nmodifyid);

                    DocObj.markuplist[curmarkup].savemarkupstateChanged(DocObj, operation);

                }
            }
        }

        //DocObj.markupselectedChangeSaved = true;

    }
    


    
    function MarkupSaveStateChanged(id, operation){
        var curmarkup = 0;
        
        for (curmarkup = 0; curmarkup < DocObj.markuplist.length; curmarkup++) {
            if (DocObj.markuplist[curmarkup] != null) {
                if (DocObj.markuplist[curmarkup].markupnumber == id) {
                    
                    if(DocObj.markuplist[curmarkup].bisTextArrow){
                        continue;
                    }

                    DocObj.markuplist[curmarkup].savemarkupstateChanged(DocObj, operation);

                    /*if(DocObj.markuplist[curmarkup].getmarkupmodified()){
                        
                        DocObj.markuplist[curmarkup].setmarkupmodified(false);
                    }*/

                }
            }
        }

    }
    
    function MarkupSaveState(id, operation) {
        var curmarkup = 0;
        var undoobj;

        for (curmarkup = 0; curmarkup < DocObj.markuplist.length; curmarkup++) {
            if (DocObj.markuplist[curmarkup] != null) {
                if (DocObj.markuplist[curmarkup].markupnumber == id) {
                    
                    if(DocObj.markuplist[curmarkup].bisTextArrow){
                        continue;
                    }

                    DocObj.markuplist[curmarkup].savemarkupstate(DocObj, operation);
                                        
                   /*undoobj = new MarkupUndoObject(id);
                    
                    if(operation == undoModify){
                        undoobj.bmodify = true;
                        undoobj.bdelete = false;
                        undoobj.bcreate = false;
                    }else if(operation == undoCreate){
                        undoobj.bmodify = false;
                        undoobj.bdelete = false;
                        undoobj.bcreate = true;
                    }else if(operation == undoDelete){
                        undoobj.bmodify = false;
                        undoobj.bdelete = true;
                        undoobj.bcreate = false;
                    }else{
                        undoobj.bmodify = true;
                        undoobj.bdelete = false;
                        undoobj.bcreate = false;
                    }

                    undoobj.SetUndoValues(DocObj.markuplist[curmarkup]);
                    DocObj.markupundolist.push(undoobj);*/

                    /*if (RxCore_GUI_Markup != undefined) {
                        RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[curmarkup], {created : false, modified : true, deleted : false});
                    }*/

                }
            }
        }

        //drawmarkupAll(cntximg);

    }

    function MarkupSaveStateByref(markupobj) {
        var undoobj = new MarkupUndoObject(markupobj.markupnumber);


        if(markupobj.bMultiOp){
            undoobj.setmultiValues(markupobj.multiOpId);
            markupobj.resetmultiValues();
        }


        undoobj.SetUndoValues(markupobj);
        DocObj.markupundolist.push(undoobj);

    }


    class RenderUtils {
        static renderPoints = (ctx, arc, color) => {
          if (arc.startX && arc.startY) {
            RenderUtils.renderPoint(ctx, { x: arc.startXscaled, y: arc.startYscaled }, color);
          }
          if (arc.secondX && arc.secondY) {
            RenderUtils.renderPoint(ctx, { x: arc.secondXscaled, y: arc.secondYscaled }, color);
          }
          if (arc.endX && arc.endY) {
            RenderUtils.renderPoint(ctx, { x: arc.endXscaled, y: arc.endYscaled }, color);
          }
        };
        static renderPoint = (ctx, point, color) => {
          ctx.save();
          ctx.fillStyle = color;
          ctx.beginPath();
          ctx.arc(point.x, point.y, 3, 0, Math.PI * 2);
          ctx.fill();
          ctx.restore();
        };


        static isclose = (p1, p2, p3, mouse, tolerance) => {
            const circle = this.calculateCircumCircle(p1, p2, p3);
            if (!circle) return false;
        
            const { center, radius } = circle;
        
            const startAngle = Math.atan2(p1.y - center.y, p1.x - center.x);
            const endAngle = Math.atan2(p2.y - center.y, p2.x - center.x);
            const midAngle  = Math.atan2(p3.y - center.y, p3.x - center.x);
        
            const normalize = (a) => (a + 2 * Math.PI) % (2 * Math.PI);
            const a1 = normalize(startAngle);
            const a2 = normalize(endAngle);
            const a3 = normalize(midAngle);
        
            const diff12 = (a2 - a1 + 2 * Math.PI) % (2 * Math.PI);
            const diff13 = (a3 - a1 + 2 * Math.PI) % (2 * Math.PI);
            const diff32 = (a2 - a3 + 2 * Math.PI) % (2 * Math.PI);
        
            let anticlockwise = false;
            if (diff12 < Math.PI) {
                if (diff13 > diff12 || diff32 > diff12) {
                    anticlockwise = true;
                }
            } else {
                if (diff13 < diff12 && diff32 < diff12) {
                    anticlockwise = false;
                } else {
                    anticlockwise = true;
                }
            }
        
            const sweep = anticlockwise
                ? (a1 - a2 + 2 * Math.PI) % (2 * Math.PI) * -1
                : (a2 - a1 + 2 * Math.PI) % (2 * Math.PI);
        
            const dist = PointFromArcDist(
                mouse.x,
                mouse.y,
                center.x,
                center.y,
                radius,
                a1,
                sweep
            );
        
            return dist <= tolerance;
        };

        static autoPlaceThirdPoint(p1, p2, curvatureFactor = 0.5) {
            const midX = (p1.x + p2.x) / 2;
            const midY = (p1.y + p2.y) / 2;
        
            const dx = p2.x - p1.x;
            const dy = p2.y - p1.y;
            const len = Math.hypot(dx, dy);
        
            if (len === 0) {
                return { x: midX, y: midY }; // fallback: no arc
            }
        
            const nx = -dy / len;
            const ny = dx / len;
        
            return {
                x: midX + nx * len * curvatureFactor,
                y: midY + ny * len * curvatureFactor
            };
        }

      
        static drawInitialArc = (ctx, p1, p2, radiusFactor = 1.2, color, linewidth) => {
          ctx.save();
          const distance = Math.hypot(p2.x - p1.x, p2.y - p1.y);
          if (distance === 0) {
            ctx.restore();
            return;
          }
          const radius = radiusFactor * distance;
          const midX = (p1.x + p2.x) / 2;
          const midY = (p1.y + p2.y) / 2;
      
          const dx = p2.x - p1.x;
          const dy = p2.y - p1.y;
          const len = Math.hypot(dx, dy);
          const nx = -dy / len;
          const ny = dx / len;
      
          const chordLength = distance;
          const h = Math.sqrt(
            radius * radius - (chordLength / 2) * (chordLength / 2)
          );
          const centerX = midX + nx * h;
          const centerY = midY + ny * h;
      
          const startAngle = Math.atan2(p1.y - centerY, p1.x - centerX);
          const endAngle = Math.atan2(p2.y - centerY, p2.x - centerX);
      
          const normalizeAngle = (angle) => (angle < 0 ? angle + 2 * Math.PI : angle);
          const a1 = normalizeAngle(startAngle);
          const a2 = normalizeAngle(endAngle);
          const diff = (a2 - a1 + 2 * Math.PI) % (2 * Math.PI);
          const anticlockwise = diff > Math.PI;
      
          ctx.beginPath();
          ctx.strokeStyle = color;
          ctx.lineWidth = linewidth;
          ctx.arc(centerX, centerY, radius, a1, a2, anticlockwise);
          ctx.stroke();
          ctx.beginPath();
          ctx.fillStyle = color;
          ctx.arc(centerX, centerY, 3, 0, 2 * Math.PI);
          ctx.fill();
          ctx.beginPath();
          ctx.arc(p1.x, p1.y, 3, 0, 2 * Math.PI);
          ctx.fill();
          ctx.beginPath();
          ctx.arc(p2.x, p2.y, 3, 0, 2 * Math.PI);
          ctx.fill();
          //this.drawLabelWithBox(ctx, `R${radius.toFixed(3)}`, midX, midY);
          ctx.restore();

          const point3 = {
            x: centerX + radius * Math.cos((a1 + a2) / 2),
            y: centerY + radius * Math.sin((a1 + a2) / 2),
            labelposx : midX, 
            labelposy: midY
          };
    
            return point3;
        };

        static calculateArcMeasurements(p1, p2, p3) {
            const circle = this.calculateCircumCircle(p1, p2, p3);
            if (!circle) return { radius: 0, arcLength: 0 };
          
            const { center, radius } = circle;
            const startAngle = Math.atan2(p1.y - center.y, p1.x - center.x);
            const endAngle = Math.atan2(p2.y - center.y, p2.x - center.x);
            const midAngle = Math.atan2(p3.y - center.y, p3.x - center.x);
          
            const normalize = (a) => (a + 2 * Math.PI) % (2 * Math.PI);
            const a1 = normalize(startAngle);
            const a2 = normalize(endAngle);
            const a3 = normalize(midAngle);
          
            const diff12 = (a2 - a1 + 2 * Math.PI) % (2 * Math.PI);
            const diff13 = (a3 - a1 + 2 * Math.PI) % (2 * Math.PI);
            const diff32 = (a2 - a3 + 2 * Math.PI) % (2 * Math.PI);
          
            let anticlockwise = false;
            if (diff12 < Math.PI) {
              if (diff13 > diff12 || diff32 > diff12) anticlockwise = true;
            } else {
              if (diff13 < diff12 && diff32 < diff12) anticlockwise = false;
              else anticlockwise = true;
            }
          
            const arcAngle = anticlockwise
              ? (a1 - a2 + 2 * Math.PI) % (2 * Math.PI)
              : (a2 - a1 + 2 * Math.PI) % (2 * Math.PI);
          
            const arcLength = radius * arcAngle;
          
            return { radius, arcLength };
        }

        static drawArcLabelFromText(ctx, x, y, labelText) {
            const lines = labelText.split("\n");
            const fontSize = 14;
            const lineHeight = fontSize + 2;
            const padding = 4;
        
            ctx.save();
            ctx.font = `${fontSize}px Arial`;
            ctx.textAlign = "center";
            ctx.textBaseline = "middle";
        
            const maxWidth = Math.max(...lines.map(line => ctx.measureText(line).width));
            const totalHeight = lines.length * lineHeight;
        
            const boxX = x - maxWidth / 2 - padding;
            const boxY = y - totalHeight / 2 - padding;
        
            ctx.fillStyle = "white";
            ctx.strokeStyle = "rgb(141, 147, 153)";
            ctx.lineWidth = 1;
            ctx.beginPath();
            ctx.rect(boxX, boxY, maxWidth + 2 * padding, totalHeight + 2 * padding);
            ctx.fill();
            ctx.stroke();
        
            ctx.fillStyle = "rgb(80, 80, 80)";
            lines.forEach((line, i) => {
                const lineY = boxY + padding + (i + 0.5) * lineHeight;
                ctx.fillText(line, x, lineY);
            });
        
            ctx.restore();
        }
        
        static drawArcLabel(ctx, x, y, radius, arcLength, unitLabel = "") {
            //const text = `R${radius.toFixed(3)}\nL${arcLength.toFixed(3)}${unitLabel}`;
            const text = `R${radius.toFixed(3)}${unitLabel}\nL${arcLength.toFixed(3)}${unitLabel}`;
            const lines = text.split("\n");
            const fontSize = 14;
            const lineHeight = fontSize + 2;
            const padding = 4;
        
            ctx.save();
            ctx.font = `${fontSize}px Arial`;
            ctx.textAlign = "center";
            ctx.textBaseline = "middle";
        
            const maxWidth = Math.max(...lines.map(line => ctx.measureText(line).width));
            const totalHeight = lines.length * lineHeight;
        
            const boxX = x - maxWidth / 2 - padding;
            const boxY = y - totalHeight / 2 - padding;
        
            ctx.fillStyle = "white";
            ctx.strokeStyle = "rgb(141, 147, 153)";
            ctx.lineWidth = 1;
            ctx.beginPath();
            ctx.rect(boxX, boxY, maxWidth + 2 * padding, totalHeight + 2 * padding);
            ctx.fill();
            ctx.stroke();
        
            ctx.fillStyle = "rgb(80, 80, 80)";
            lines.forEach((line, i) => {
                const lineY = boxY + padding + (i + 0.5) * lineHeight;
                ctx.fillText(line, x, lineY);
            });
        
            ctx.restore();
        }
      
        static calculateCircumCircle = (p1, p2, p3) => {
          const x1 = p1.x,
            y1 = p1.y;
          const x2 = p2.x,
            y2 = p2.y;
          const x3 = p3.x,
            y3 = p3.y;
      
          const A = x1 * (y2 - y3) - y1 * (x2 - x3) + x2 * y3 - x3 * y2;
          if (Math.abs(A) < 1e-10) {
            const distance = Math.hypot(p2.x - p1.x, p2.y - p1.y);
            const radius = 1.2 * distance;
      
            const dx = p2.x - p1.x;
            const dy = p2.y - p1.y;
            const len = Math.hypot(dx, dy);
            const nx = -dy / len;
            const ny = dx / len;
      
            // Midpoint of the segment
            const midX = (p1.x + p2.x) / 2;
            const midY = (p1.y + p2.y) / 2;
      
            const h = Math.sqrt(radius * radius - (distance / 2) * (distance / 2));
      
            const centerX = midX + nx * h;
            const centerY = midY + ny * h;
            return { center: { x: centerX, y: centerY }, radius };
          }
      
          const B =
            (x1 * x1 + y1 * y1) * (y3 - y2) +
            (x2 * x2 + y2 * y2) * (y1 - y3) +
            (x3 * x3 + y3 * y3) * (y2 - y1);
          const C =
            (x1 * x1 + y1 * y1) * (x2 - x3) +
            (x2 * x2 + y2 * y2) * (x3 - x1) +
            (x3 * x3 + y3 * y3) * (x1 - x2);
          const D =
            (x1 * x1 + y1 * y1) * (x3 * y2 - x2 * y3) +
            (x2 * x2 + y2 * y2) * (x1 * y3 - x3 * y1) +
            (x3 * x3 + y3 * y3) * (x2 * y1 - x1 * y2);
      
          const centerX = -B / (2 * A);
          const centerY = -C / (2 * A);
          const radius = Math.sqrt((B * B + C * C - 4 * A * D) / (4 * A * A));
      
          return { center: { x: centerX, y: centerY }, radius };
        };
      
        static drawArc = (ctx, p1, p2, p3, color, scale, linewidth) => {
          ctx.save();
          const circle = this.calculateCircumCircle(p1, p2, p3);
          if (!circle) {
            ctx.restore();
            return;
          }
          const { center, radius } = circle;
          const startAngle = Math.atan2(p1.y - center.y, p1.x - center.x);
          const endAngle = Math.atan2(p2.y - center.y, p2.x - center.x);
          const midAngle = Math.atan2(p3.y - center.y, p3.x - center.x);
      
          const normalizeAngle = (angle) => (angle < 0 ? angle + 2 * Math.PI : angle);
      
          const a1 = normalizeAngle(startAngle);
          const a2 = normalizeAngle(endAngle);
          const a3 = normalizeAngle(midAngle);
      
          const diff12 = (a2 - a1 + 2 * Math.PI) % (2 * Math.PI);
          const diff13 = (a3 - a1 + 2 * Math.PI) % (2 * Math.PI);
          const diff32 = (a2 - a3 + 2 * Math.PI) % (2 * Math.PI);
      
          let anticlockwise = false;
          if (diff12 < Math.PI) {
            if (diff13 > diff12 || diff32 > diff12) {
              anticlockwise = true;
            }
          } else {
            if (diff13 < diff12 && diff32 < diff12) {
              anticlockwise = false;
            } else {
              anticlockwise = true;
            }
          }

          //const arcAngle = anticlockwise ? (a1 - a2 + 2 * Math.PI) % (2 * Math.PI) : (a2 - a1 + 2 * Math.PI) % (2 * Math.PI);
          //const arcLength = radius * arcAngle;
      
      
          ctx.beginPath();
          ctx.strokeStyle = color;
          ctx.lineWidth = linewidth;
          ctx.arc(center.x, center.y, radius, a1, a2, anticlockwise);
          ctx.stroke();
          ctx.beginPath();
          ctx.fillStyle = color;
          ctx.arc(center.x, center.y, 3, 0, 2 * Math.PI);
          ctx.fill();
          ctx.beginPath();
          ctx.arc(p1.x, p1.y, 3, 0, 2 * Math.PI);
          ctx.fill();
          ctx.beginPath();
          ctx.arc(p2.x, p2.y, 3, 0, 2 * Math.PI);
          ctx.fill();
          const midX = (p1.x + p2.x) / 2;
          const midY = (p1.y + p2.y) / 2;

          //this.dimtext = getUnitlength(dimdiag / this.scaling);
          //const scaledradius = getUnitlength(radius);
          //const scaledlength = getUnitlength(arcLength);

          //const label = `R${scaledradius.toFixed(3)}\nL${scaledlength.toFixed(3)}`;
          //this.drawMultilineLabel(ctx, label, midX, midY);
      
          //this.drawLabelWithBox(ctx, `R${radius.toFixed(3)}`, midX, midY);
          ctx.restore();

          const labelpos = {
            labelposx : midX, 
            labelposy: midY
          };
    
         return labelpos;



        };

        static drawMultilineLabel = (ctx, text, x, y) => {
            const lines = text.split("\n");
            const fontSize = 14; // Slightly smaller
            const padding = 3;
            const lineHeight = fontSize + 2;
          
            ctx.save();
            ctx.font = `${fontSize}px Arial`;
            ctx.textAlign = "center";
            ctx.textBaseline = "alphabetic"; // More natural text placement
          
            const textWidths = lines.map(line => ctx.measureText(line).width);
            const maxWidth = Math.max(...textWidths);
            const totalHeight = lines.length * lineHeight;
          
            const boxX = x - maxWidth / 2 - padding;
            const boxY = y - totalHeight / 2 - padding;
          
            // Draw background box
            ctx.fillStyle = "white";
            ctx.strokeStyle = "rgb(141, 147, 153)";
            ctx.lineWidth = 1;
            ctx.beginPath();
            ctx.rect(boxX, boxY, maxWidth + 2 * padding, totalHeight + 2 * padding);
            ctx.fill();
            ctx.stroke();
          
            // Draw text
            ctx.fillStyle = "rgb(141, 147, 153)";
            lines.forEach((line, i) => {
              const lineY = boxY + padding + (i + 1) * lineHeight - 4; // Adjust vertical offset
              ctx.fillText(line, x, lineY);
            });
          
            ctx.restore();
          };
      
        static drawLabelWithBox = (ctx, text, x, y) => {
          ctx.font = `${16}px Arial`;
          ctx.textAlign = "center";
          ctx.textBaseline = "middle";
          const textMetrics = ctx.measureText(text);
          const textWidth = textMetrics.width;
          const textHeight = 16;
          const padding = 4;
          ctx.fillStyle = "white";
          ctx.strokeStyle = "rgb(141, 147, 153)";
          ctx.lineWidth = 1;
          ctx.beginPath();
          ctx.rect(
            x - textWidth / 2 - padding,
            y - textHeight / 2 - padding,
            textWidth + 2 * padding,
            textHeight + 2 * padding
          );
          ctx.fill();
          ctx.stroke();
          ctx.fillStyle = "rgb(141, 147, 153)";
          ctx.fillText(text, x, y);
        };
      
        static drawHollowCircle = (ctx, center, radius, color) => {
          ctx.save();
          ctx.beginPath();
          ctx.strokeStyle = color;
          ctx.lineWidth = 2;
          ctx.arc(center.x, center.y, radius, 0, 2 * Math.PI);
          ctx.stroke();
          ctx.beginPath();
          ctx.fillStyle = color;
          ctx.arc(center.x, center.y, 3, 0, 2 * Math.PI);
          ctx.fill();
          ctx.restore();
        };
      }


        //var MarkupDefaultFont = new FontObject("Helvetica",24,false,false);
        class MarkupUndoObject {
            constructor(id) {
                
                var undoscope = this;
                this.markupnumber = id;

                this.bMultiOp = false;
                this.multiOpId = null;

                this.type = 0;
                this.subtype = 0;
                this.alternative = 0;
                this.consolidated = false;
                this.bRedact = false;
                
                this.bdelete = false;
                this.bmodify = false;
                this.islastbmodify = false;

                this.isfirstbmodify = false;
                this.bcurstate = false;

                this.bcreate = false;
                this.bredoonly = false;

                this.bhasArrow = false;
                this.markupArrowConnected = null;

                this.bisTextArrow =  false;
                this.textBoxConnected = null;

                this.savetolist = false;
                
                this.x = 0;
                this.y = 0;
                this.w = 0;
                this.h = 0;
                this.xscaled = 0;
                this.yscaled = 0;
                this.wscaled = 0;
                this.hscaled = 0;
                this.rotatedrect = new Rectangle(0, 0, 0, 0);
                this.timestamp = 0;
                this.layer = 0;
                this.pagenumber = 0;
                this.layout = "Default";
                this.signature = "";
                this.color = "";
                this.transparency = 100;
                this.fillcolor = "rgba(255,0,0,1.0)";
                this.strokecolor = "rgba(255,0,0,1.0)";
                this.textcolor = "rgba(255,0,0,1.0)";
                this.markercolor = "rgba(255,0,0,1.0)";
                this.scaling = 0.0;
                this.xoffset = 0.0;
                this.yoffset = 0.0;
                this.leaderoffset = 0;
                this.scalingext = 0.0;
                this.xoffsetext = 0.0;
                this.yoffsetext = 0.0;
                this.rotation = 0.0;
                this.textrotate = 0;
                this.dimtext = "";

                this.dimtextx = "";
                this.dimtexty = "";

                this.linewidth = 0;
                this.textheight = 0;
                this.measuretextheight = 0;
                this.textwidth = 0;
                this.text = "";
                this.font = new FontObject("Helvetica", 24, false, false);
                //this.fontname = "";
                this.points = [];
                this.pointlist = [];

                this.countshapes = [];

                this.comments = [];
                this.customattributes = [];
                this.holes = [];
                this.holepointlist = [];


                this.innerRadius = 1;
                this.outerRadius = 1;
                this.startX = null;
                this.startY = null;
                this.secondX = null;
                this.secondY = null;
                this.endX = null;
                this.endY = null;

                this.image = undefined;

                //this.numpoints = -1;

                /*this.restoreDeleted = function(DocRef){

                    //var id = undoscope.markupnumber;

                    var undoMarkupdeletobj = new MarkupObject(undoscope.type, undoscope.subtype, undoscope.alternative);
                    
                    undoMarkupdeletobj.markupnumber = undoscope.markupnumber;
                    //ft 08.08.2018 changed from separate index to direct array length
                    
                    //DocObj.markuplist[DocObj.nummarkups - 1].markupnumber = id;
                    //DocObj.markuplist[DocObj.nummarkups - 1].PerformUndo(localmarkup);
    
                    //DocObj.markuplist[DocObj.markuplist.length - 1].markupnumber = undoscope.markupnumber;
                    undoscope.performUndo(undoMarkupdeletobj);

                    //DocObj.markuplist[DocObj.markuplist.length - 1].PerformUndo(localmarkup);

                    undoMarkupdeletobj.savemetolistLoad(DocRef);
    
                    if (RxCore_GUI_Markup != undefined) {
                        RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[DocObj.markuplist.length - 1], {created : true, modified : false, deleted : false});
                    }
    
                };*/

                this.restoreDeleted = function(){
                    var undoMarkupdeletobj = new MarkupObject(undoscope.type, undoscope.subtype, undoscope.alternative);
                    

                    return undoMarkupdeletobj;

                } 

                this.saverestoredtolist = function(undoMarkupdeletobj, DocRef){

                    if(undoMarkupdeletobj){
                        undoMarkupdeletobj.savemetolistLoad(DocRef);
                        undoMarkupdeletobj.display = true;
                        
                        undoMarkupdeletobj.markupnumber = undoscope.markupnumber;
                        
                        undoMarkupdeletobj.drawmescaled(cntximg);

                        //DocObj.markuplist[index]


                        if (RxCore_GUI_Markup != undefined) {
                            RxCore_GUI_Markup.setMarkupSelected(undoMarkupdeletobj, {created : true, modified : false, deleted : false});
                        }
    
                    }
    
                };

                this.PerformUndoCommon = function(MarkupObj, DocRef){
                    MarkupObj.type = undoscope.type;
                    MarkupObj.subtype = undoscope.subtype;
                    MarkupObj.alternative = undoscope.alternative;
                    MarkupObj.bRedact = undoscope.bRedact;
                    MarkupObj.consolidated = undoscope.consolidated;

                    MarkupObj.pagenumber = undoscope.pagenumber;
                    MarkupObj.display = true;
                    
                    MarkupObj.x = undoscope.x;
                    MarkupObj.y = undoscope.y;
                    MarkupObj.w = undoscope.w;
                    MarkupObj.h = undoscope.h;
                    MarkupObj.xscaled = undoscope.xscaled;
                    MarkupObj.yscaled = undoscope.yscaled;
                    MarkupObj.wscaled = undoscope.wscaled;
                    MarkupObj.hscaled = undoscope.hscaled;
                    MarkupObj.timestamp = undoscope.timestamp;
                    MarkupObj.layer = undoscope.layer;
                    MarkupObj.signature = undoscope.signature;
                    MarkupObj.color = undoscope.color;
                    MarkupObj.fillcolor = undoscope.fillcolor;
                    MarkupObj.strokecolor = undoscope.strokecolor;
                    MarkupObj.textcolor = undoscope.textcolor;
                    MarkupObj.scaling = undoscope.scaling;
                    MarkupObj.xoffset = undoscope.xoffset;
                    MarkupObj.yoffset = undoscope.yoffset;
                    MarkupObj.docdx = undoscope.docdx;
                    MarkupObj.docdy = undoscope.docdy;
                    MarkupObj.docdscale = undoscope.docdscale;
                    MarkupObj.scalingext = undoscope.scalingext;
                    MarkupObj.xoffsetext = undoscope.xoffsetext;
                    MarkupObj.yoffsetext = undoscope.yoffsetext;
                    MarkupObj.rotation = undoscope.rotation;
                    MarkupObj.textrotate = undoscope.textrotate;
                    MarkupObj.dimtext = undoscope.dimtext;
                    MarkupObj.leaderoffset = undoscope.leaderoffset;
                    

                    MarkupObj.linewidth = undoscope.linewidth;
                    MarkupObj.textheight = undoscope.textheight;
                    MarkupObj.measuretextheight = undoscope.measuretextheight;
                    MarkupObj.textwidth = undoscope.textwidth;
                    MarkupObj.text = undoscope.text;
                    MarkupObj.parent = undoscope.parent;
                    

                    //this.fontname = UndoObj.fontname;
                    MarkupObj.font = undoscope.font;
                    MarkupObj.editcurpoint = 0;
                    MarkupObj.points = undoscope.points.slice();

                    MarkupObj.countshapes = undoscope.countshapes.slice();
                    MarkupObj.innerRadius = undoscope.innerRadius;
                    MarkupObj.outerRadius = undoscope.outerRadius;

                    MarkupObj.startX = undoscope.startX;
                    MarkupObj.startY = undoscope.startY;
                    MarkupObj.secondX = undoscope.secondX;
                    MarkupObj.secondY = undoscope.secondY;
                    MarkupObj.endX = undoscope.endX;
                    MarkupObj.endY = undoscope.endY;
    

                    MarkupObj.numpoints = undoscope.points.length - 1;
                    for (var i = 0; i < undoscope.pointlist.length; i++) {
                        MarkupObj.pointlist[i] = undoscope.pointlist[i].slice();
                    }


                                        //var curcomms = 0;
                    //var curattr = 0;

                    /*this.comments = [];

                    for (curcomms = 0; curcomms < MarkupObj.curcomms.length; curcomms++) {

                        this.comments.push(MarkupObj.comments[curcomms]);
                    }*/

                    MarkupObj.comments = undoscope.comments.slice();

                    

                    /*this.customattributes = [];
                    
                    for (curattr = 0; curattr < MarkupObj.curattr.length; curattr++) {

                        this.customattributes.push(MarkupObj.customattributes[curcomms]);
                    }*/

                    MarkupObj.customattributes = undoscope.customattributes.slice();

                    MarkupObj.holes = undoscope.holes.slice();


                    //this.holes = [];

                    /*for (curholes = 0; curholes < MarkupObj.holes.length; curholes++) {

                        this.holes.push(MarkupObj.holes[curholes]);

                    }*/

                    /*for (var h = 0; h < undoscope.holepointlist.length; h++) {
                        //this.pointlist[i] = [];

                        //this.holepointlist[h] = [];

                        for (var p = 0; p < undoscope.holepointlist[h].length; p++) {

                            MarkupObj.holes[h].points[p].x = undoscope.holepointlist[h][p].x;
                            MarkupObj.holes[h].points[p].y = undoscope.holepointlist[h][p].y;


                            //this.holepointlist[h][p] = new point(MarkupObj.MarkupObj.holes[h].points[p].x, MarkupObj.MarkupObj.holes[h].points[p].y);
                            
                        }
                    }*/




                    if (MarkupObj.bhasArrow && MarkupObj.markupArrowConnected != null) {
                        MarkupObj.markupArrowConnected.setTextboxConnectionPoint(MarkupObj.x, MarkupObj.y, MarkupObj.w, MarkupObj.h);
                    }

                    /*for (curholes = 0; curholes < MarkupObj.holes.length; curholes++) {



                        this.holes.push(MarkupObj.holes[curholes]);


                        for (curhpoint = 0; curhpoint < MarkupObj.holes[curholes].points.length; curhpoint++) {
                            
                            this.holes[curholes].points[curhpoint] = new point(MarkupObj.holes[curholes].points[curhpoint].x, MarkupObj.holes[curholes].points[curhpoint].y);
                            //this.points[curpoint] = new point(MarkupObj.points[curpoint].x, MarkupObj.points[curpoint].y);
                        }
    

                    }*/

            
                    MarkupObj.findrectangle();

                    if(undoscope.savetolist){
                        undoscope.saverestoredtolist(MarkupObj,DocRef);
                        MarkupObj.addsides();
                        undoscope.savetolist = false;
                    }
                };

                this.PerformRedo = function(MarkupObj, DocRef){

                    if(undoscope.bcreate){
                        //undoscope.bdelete = true;
                        //undoscope.bcreate = false;
                    }

                    if(undoscope.bdelete){
                        undoscope.bdelete = false;
                        undoscope.bcreate = true;
                    }
                    undoscope.PerformUndo(MarkupObj, DocRef);

                };

                this.PerformUndo = function(MarkupObj, DocRef){

                    if(undoscope.type == 6 && undoscope.subtype == 6){
                        return;
                    }

                    if(undoscope.bcreate){
                        //delete created

                        if(undoscope.bhasArrow && undoscope.markupArrowConnected ){
                            undoremovemarkup(undoscope.markupArrowConnected.markupnumber);    
                        }

                        if(MarkupObj){

                            if(MarkupObj.type == 11 || MarkupObj.type == 20){
                                if(MarkupObj.image){
                                    undoscope.image = MarkupObj.image;
                                }

                            }
    
                        }



                        


                        undoremovemarkup(undoscope.markupnumber);
                        undoscope.bdelete = true;
                        undoscope.bcreate = false;



                        return;
                    }

                    if(undoscope.bdelete){
                        MarkupObj = undoscope.restoreDeleted();

                        if(undoscope.bhasArrow && undoscope.markupArrowConnected ){
                            MarkupObj.markupArrowConnected = undoscope.markupArrowConnected.restoreDeleted();
                            MarkupObj.bhasArrow = true;
                            MarkupObj.markupArrowConnected.textBoxConnected = MarkupObj;
                            MarkupObj.markupArrowConnected.bisTextArrow =  true;

                            undoscope.markupArrowConnected.savetolist = true;
                            //undoscope.markupArrowConnected.bdelete = false;
                            //undoscope.markupArrowConnected.bcreate = true;

                        }

                        if(MarkupObj.type == 8 && MarkupObj.subtype == 2){
                            undoscope.parent.holes.push(MarkupObj);
                        }
                        
                        if(MarkupObj.type == 11 || MarkupObj.type == 20){
                        
                            MarkupObj.image = undoscope.image;
                            MarkupObj.imageloaded = true;
                        }

                        undoscope.savetolist = true;
                        undoscope.bdelete = false;
                        undoscope.bcreate = true;

                        //MarkupObj.savemetolistLoad(DocObj);
                        MarkupObj.setfirstDraw();


                    }


                    if(!MarkupObj){
                        return;
                    }

                    var curpoint = 0;
                    var tempx = 0;
                    var tempy = 0;


                    undoscope.PerformUndoCommon(MarkupObj, DocRef);

                    if(undoscope.bhasArrow && undoscope.markupArrowConnected ){

                        if(MarkupObj.markupArrowConnected != null){
                            undoscope.markupArrowConnected.PerformUndoCommon(MarkupObj.markupArrowConnected, DocRef);
                        }
                        
                    }

                    /*if (undoscope.bdelete) {
                        //delete created
                        undoremovemarkup(undoscope.markupnumber);
                        //UndoObj
                        //remove from docobj markuplist
                    }*/

                };
                this.checkduplicate = function(Undoobj){
                    
                    var curpoint = 0;
                    var curcount = 0;

                    var duplicate = true;

                    duplicate = (this.type == Undoobj.type) && duplicate;

                    if(duplicate){
                        duplicate = (this.subtype == Undoobj.subtype) && duplicate;
                    }
                    if(duplicate){
                        duplicate = (this.alternative == Undoobj.alternative) && duplicate;
                    }
                    if(duplicate){
                        duplicate = (this.bRedact == Undoobj.bRedact) && duplicate;
                    }
                    if(duplicate){
                        duplicate = (this.consolidated == Undoobj.consolidated) && duplicate;
                    }
                    if(duplicate){
                        duplicate = (this.display == Undoobj.display) && duplicate;
                    }
                    if(duplicate){
                        duplicate = (this.x == Undoobj.x) && duplicate;
                    }
                    if(duplicate){
                        duplicate = (this.y == Undoobj.y) && duplicate;
                    }
                    if(duplicate){
                        duplicate = (this.w == Undoobj.w) && duplicate;
                    }
                    if(duplicate){
                        duplicate = (this.h == Undoobj.h) && duplicate;
                    }
                    if(duplicate){
                        duplicate = (this.xscaled == Undoobj.xscaled) && duplicate;    
                    }
                    if(duplicate){
                        duplicate = (this.yscaled == Undoobj.yscaled) && duplicate;    
                    }
                    if(duplicate){
                        duplicate = (this.wscaled == Undoobj.wscaled) && duplicate;    
                    }
                    if(duplicate){
                        duplicate = (this.hscaled == Undoobj.hscaled) && duplicate;    
                    }
                    if(duplicate){
                        duplicate = (this.rotatedrect == Undoobj.rotatedrect) && duplicate;    
                    }
                    if(duplicate){
                        duplicate = (this.timestamp == Undoobj.timestamp) && duplicate;    
                    }
                    if(duplicate){
                        duplicate = (this.layer == Undoobj.layer) && duplicate;    
                    }
                    if(duplicate){
                        duplicate = (this.pagenumber == Undoobj.pagenumber) && duplicate;
                    }
                    if(duplicate){
                        duplicate = (this.layout == Undoobj.layout) && duplicate;
                    }
                    if(duplicate){
                        duplicate = (this.signature == Undoobj.signature) && duplicate;
                    }
                    if(duplicate){
                        duplicate = (this.color == Undoobj.color) && duplicate;
                    }
                    if(duplicate){
                        duplicate = (this.transparency == Undoobj.transparency) && duplicate;
                    }
                    if(duplicate){
                        duplicate = (this.fillcolor == Undoobj.fillcolor) && duplicate;
                    }
                    if(duplicate){
                        duplicate = (this.strokecolor == Undoobj.strokecolor) && duplicate;
                    }
                    if(duplicate){
                        duplicate = (this.textcolor == Undoobj.textcolor) && duplicate;
                    }
                    if(duplicate){
                        duplicate = (this.markercolor == Undoobj.markercolor) && duplicate;   
                    }
                    if(duplicate){
                        duplicate = (this.scaling == Undoobj.scaling) && duplicate;
                    }
                    if(duplicate){
                        duplicate = (this.xoffset == Undoobj.xoffset) && duplicate;
                    }
                    if(duplicate){
                        duplicate = (this.yoffset == Undoobj.yoffset) && duplicate;
                    }
                    if(duplicate){
                        duplicate = (this.scalingext == Undoobj.scalingext) && duplicate;
                    }
                    if(duplicate){
                        duplicate = (this.xoffsetext == Undoobj.xoffsetext) && duplicate;
                    }
                    if(duplicate){
                        duplicate = (this.yoffsetext == Undoobj.yoffsetext) && duplicate;
                    }
                    if(duplicate){
                        duplicate = (this.rotation == Undoobj.rotation) && duplicate;
                    }
                    if(duplicate){
                        duplicate = (this.textrotate == Undoobj.textrotate) && duplicate;
                    }
                    if(duplicate){
                        duplicate = (this.dimtext == Undoobj.dimtext) && duplicate;
                    }
                    if(duplicate){
                        duplicate = (this.linewidth == Undoobj.linewidth) && duplicate;
                    }
                    if(duplicate){
                        duplicate = (this.textheight == Undoobj.textheight) && duplicate;
                    }
                    if(duplicate){
                        duplicate = (this.measuretextheight == Undoobj.measuretextheight) && duplicate;
                    }
                    if(duplicate){
                        duplicate = (this.textwidth == Undoobj.textwidth) && duplicate;
                    }
                    if(duplicate){
                        duplicate = (this.text == Undoobj.text) && duplicate;
                    }
                    if(duplicate){
                        duplicate = (this.font == Undoobj.font) && duplicate;
                    }
                    if(duplicate){
                        duplicate = (this.innerRadius == Undoobj.innerRadius) && duplicate;
                    }
                    if(duplicate){
                        duplicate = (this.outerRadius == Undoobj.outerRadius) && duplicate;
                    }

                    

                    if(duplicate){
                        for (curcount = 0; curcount < this.countshapes.length; curcount++) {
                        
                            duplicate = (Undoobj.countshapes[curcount].x == this.countshapes[curcount].x) && duplicate;
                            duplicate = (Undoobj.countshapes[curcount].y == this.countshapes[curcount].y) && duplicate;
    
                        }
                    }
                    if(duplicate){
                        for (curpoint = 0; curpoint < this.points.length; curpoint++) {

                            duplicate = (Undoobj.points[curpoint].x == this.points[curpoint].x) && duplicate;
                            duplicate = (Undoobj.points[curpoint].y == this.points[curpoint].y) && duplicate;
    
                        }
                    }


                    return duplicate;

                    

                };  

                this.setmultiValues = function(id){

                    this.bMultiOp = true;
                    this.multiOpId = id;
    
                };


                this.SetUndoValues = function (MarkupObj) {
                    var curpoint = 0;
                    var curhpoint = 0;
                    var curcount = 0;
                    var curcomms = 0;
                    var curattr = 0;
                    var curholes = 0;

                    
                    var cursides = 0;

                    var tempx = 0;
                    var tempy = 0;


                    this.image = MarkupObj.image;
                    this.parent = MarkupObj.parent;
                    this.type = MarkupObj.type;
                    this.subtype = MarkupObj.subtype;
                    this.alternative = MarkupObj.alternative;
                    this.bRedact = MarkupObj.bRedact;
                    this.consolidated = MarkupObj.consolidated;
                    this.display = MarkupObj.display;
                    this.x = MarkupObj.x;
                    this.y = MarkupObj.y;
                    this.w = MarkupObj.w;
                    this.h = MarkupObj.h;
                    this.xscaled = MarkupObj.xscaled;
                    this.yscaled = MarkupObj.yscaled;
                    this.wscaled = MarkupObj.wscaled;
                    this.hscaled = MarkupObj.hscaled;
                    this.rotatedrect = MarkupObj.rotatedrect;
                    this.timestamp = MarkupObj.timestamp;
                    this.layer = MarkupObj.layer;
                    this.pagenumber = MarkupObj.pagenumber;

                    this.viewname = MarkupObj.viewname;
                    this.bfor3D = MarkupObj.bfor3D;

                    this.layout = MarkupObj.layout;
                    this.signature = MarkupObj.signature;
                    this.color = MarkupObj.color;
                    this.transparency = MarkupObj.transparency;
                    this.fillcolor = MarkupObj.fillcolor;
                    this.strokecolor = MarkupObj.strokecolor;
                    this.textcolor = MarkupObj.textcolor;
                    this.markercolor = MarkupObj.markercolor;
                    this.scaling = MarkupObj.scaling;
                    this.xoffset = MarkupObj.xoffset;
                    this.yoffset = MarkupObj.yoffset;
                    this.leaderoffset = MarkupObj.leaderoffset;
                    this.scalingext = MarkupObj.scalingext;
                    this.xoffsetext = MarkupObj.xoffsetext;
                    this.yoffsetext = MarkupObj.yoffsetext;
                    this.rotation = MarkupObj.rotation;
                    this.textrotate = MarkupObj.textrotate;
                    this.dimtext = MarkupObj.dimtext;
                    this.linewidth = MarkupObj.linewidth;
                    this.textheight = MarkupObj.textheight;
                    this.measuretextheight = MarkupObj.measuretextheight;
                    this.textwidth = MarkupObj.textwidth;
                    this.text = MarkupObj.text;
                    this.font = MarkupObj.font;

                    this.innerRadius = MarkupObj.innerRadius;
                    this.outerRadius = MarkupObj.outerRadius;

                    this.startX = MarkupObj.startX;
                    this.startY = MarkupObj.startY;
                    this.secondX = MarkupObj.secondX;
                    this.secondY = MarkupObj.secondY;
                    this.endX = MarkupObj.endX;
                    this.endY = MarkupObj.endY;
    
                    


                    for (curcount = 0; curcount < MarkupObj.countshapes.length; curcount++) {
                        this.countshapes[curcount] = new point(MarkupObj.countshapes[curcount].x, MarkupObj.countshapes[curcount].y);
                    }

                    if (MarkupObj.bhasArrow && MarkupObj.markupArrowConnected != null) {

                        this.bhasArrow = true;
                        var undoobj = new MarkupUndoObject(MarkupObj.markupArrowConnected.markupnumber);
                        undoobj.SetUndoValues(MarkupObj.markupArrowConnected);
                        this.markupArrowConnected = undoobj;

                        //this.markupArrowConnected.setTextboxConnectionPoint(this.x, this.y, this.w, this.h);
                    }    

                    //this.fontname = MarkupObj.fontname;
                    for (curpoint = 0; curpoint < MarkupObj.points.length; curpoint++) {
                        this.points[curpoint] = new point(MarkupObj.points[curpoint].x, MarkupObj.points[curpoint].y);


                    }
                    //this.numpoints = this.points.length - 1;
                    for (var i = 0; i < MarkupObj.pointlist.length; i++) {
                        this.pointlist[i] = [];
                        for (var j = 0; j < MarkupObj.pointlist[i].length; j++) {
                            this.pointlist[i][j] = new point(MarkupObj.pointlist[i][j].x, MarkupObj.pointlist[i][j].y);
                            
                        }
                    }

                    //var curcomms = 0;
                    //var curattr = 0;

                    this.holes = [];

                    for (curholes = 0; curholes < MarkupObj.holes.length; curholes++) {

                        this.holes.push(MarkupObj.holes[curholes]);

                    }

                    /* for (var h = 0; h < MarkupObj.holes.length; h++) {
                        //this.pointlist[i] = [];

                        this.holepointlist[h] = [];

                        for (var p = 0; p < MarkupObj.holes[h].points.length; p++) {
                            this.holepointlist[h][p] = new point(MarkupObj.holes[h].points[p].x, MarkupObj.holes[h].points[p].y);
                            
                        }
                    } */


                    

                    this.comments = [];

                    for (curcomms = 0; curcomms < MarkupObj.comments.length; curcomms++) {

                        this.comments.push(MarkupObj.comments[curcomms]);
                    }

                    this.customattributes = [];

                    for (curattr = 0; curattr < MarkupObj.customattributes.length; curattr++) {

                        this.customattributes.push(MarkupObj.customattributes[curcomms]);
                    }

                };
            }
        }



        class MarkupObject {
            constructor(type, subtype, alternative) {
                var markupobject = this;
                this.markupnumber = 0;
                this.type = type;
                this.subtype = subtype;
                this.alternative = alternative;
                this.bRedact = false;
                this.aspectLock = true;
                if (alternative >= 3) {
                    this.hatchStyle = alternative - 3;
                }
                else {
                    this.hatchprt = 0;
                }
                this.saved = false;
                this.firstpointrotAdjusted = false;
                this.markupID = 0;
                this.uniqueID = 0;


                this.locked = false;
                this.pdfannot = false;
                this.pdfannotref = undefined;
                this.pdfannotproxy = false;
                this.pdfannotproxyType = "rectangle";

                this.timestamp = new Date().getTime();
                this.layer = markuplayer;
                this.signature = signature;
                this.color = markupcolor;
                this.transparency = globaltransparency;
                this.labeltransparency = 100;
                this.showlabel = true;
                this.consolidated = false;
                this.linkURL = "";
                this.actionName = "";
                this.imagehref = "";
                this.bhaveLink = false;
                this.noDraw = false;
                this.addedfoxit = false;
                this.bCanHaveLink = ((markupobject.type == 1 && markupobject.subtype == 2) || markupobject.type == 3 || markupobject.type == 4 || markupobject.type == 5 || markupobject.type == 9 || markupobject.type == 11 || markupobject.type == 20);
                this.anglelengthsupport = (markupobject.type == 6);
                this.widthHeightsupport = (markupobject.type == 3);
                this.setRadiusSupport = (markupobject.type == 4 && markupobject.subtype == 1);
                this.setPointSupport = (markupobject.type == 1 || markupobject.type == 8);

                var bmpath = (markupobject.type == 1 && markupobject.subtype == 3);
                var bmangle = (type == 1 && (subtype == 4 || subtype == 5));
                var brectarea = (type == 3 && subtype == 6);


                

                this.editcurpoint = 0;
                this.bpointEdit = false;
                this.fillcolor = convertHex(markupfillcolor, this.transparency);
                this.selectSpaceColor = "rgba(191,58,209,0.6)";
                //rgb(255, 255, 153) yellow.
                this.selectSpaceStopColor = "rgba(255,255,255,0.3)";
                this.strokecolor = convertHex(markuplinecolor, 100);
                this.strokecolorSegment = convertHex(markuplinecolorSegment, 30);
                this.textcolor = convertHex(markuptextcolor, 100);
                this.markercolor = convertHex(markupfillcolor, 30);
                this.labelfillcolor = "rgba(255,255,255,1)";
                this.orthoDrawAngle = 0;
                this.orthoDrawAngledeg = 0;
                this.pagenumber = 0;

                this.viewname = null;
                this.bfor3D = false;

                this.takeOffObject = undefined;

                this.bUseFixedScale = bUseFixedScale;
                

                this.pagerotation = 0;
                this.documentName = DocObj.OriginalURL;
                this.layout = "Default";
                //this.display = true;
                this.display = true;
                //markupdisplayonload
                this.relativeWH = false;
                this.drawn = false;
                this.imageloaded = false;
                this.arrowTextPos = 0;
                this.padding = undefined;
                this.labeltype = 1;

                this.bmodyfied = false;
                
                this.svgContent = undefined;

                //General placement and scaling properties
                this.x = 0;
                this.y = 0;
                this.w = 0;
                this.h = 0;
                this.origx = 0;
                this.origy = 0;
                this.origw = 0;
                this.origh = 0; 
                this.origset = false;


                this.radius = 0; //only used for circles
                this.xscaled = 0;
                this.yscaled = 0;
                this.wscaled = 0;
                this.hscaled = 0;

                //only used for arcs

                this.startX = null;
                this.startY = null;
                this.secondX = null;
                this.secondY = null;
                this.endX = null;
                this.endY = null;

                this.startXscaled = null;
                this.startYscaled = null;
                this.secondXscaled = null;
                this.secondYscaled = null;
                this.endXscaled = null;
                this.endYscaled = null;
                
                this.setsecond = false;
                this.setthird = false;


            
                //only used for arcs

                this.radiusScaled = 0; //only used for circles
                //for new dimension with leader offset.
                this.leaderoffset = 0;
                this.labelpos = { x: this.x, y: this.y, w: this.w, h: this.h };
                this.labeloffsetx = 0;
                this.labeloffsety = 0;
                this.labelRect = { x: 0, y: 0, w: 0, h: 0 };
                this.transformobj = {a : 1, b : 0, c : 0, d : 1, e : 0, f : 0};
                this.rotatedrect = new Rectangle(this.xscaled, this.yscaled, this.wscaled, this.hscaled);
                //Selection point areas for mouse manipulation
                this.LowerRightRect = new Rectangle(this.xscaled, this.yscaled, this.wscaled, this.hscaled);
                this.LowerLeftRect = new Rectangle(this.xscaled, this.yscaled, this.wscaled, this.hscaled);
                this.UpperLeftRect = new Rectangle(this.xscaled, this.yscaled, this.wscaled, this.hscaled);
                this.UpperRightRect = new Rectangle(this.xscaled, this.yscaled, this.wscaled, this.hscaled);
                this.RotmarkerRect = new Rectangle(this.xscaled, this.yscaled, this.wscaled, this.hscaled);
                this.LowerRightRectRot = new Rectangle(this.xscaled, this.yscaled, this.wscaled, this.hscaled);
                this.LowerLeftRectRot = new Rectangle(this.xscaled, this.yscaled, this.wscaled, this.hscaled);
                this.UpperLeftRectRot = new Rectangle(this.xscaled, this.yscaled, this.wscaled, this.hscaled);
                this.UpperRightRectRot = new Rectangle(this.xscaled, this.yscaled, this.wscaled, this.hscaled);
                this.RotmarkerRectRot = new Rectangle(this.xscaled, this.yscaled, this.wscaled, this.hscaled);
                //this.center = new point((this.w-this.x)/2,(this.h - this.y)/2);
                this.fixedscaleFactor = 1.0;
                this.printscale = 1.0;

                this.pageheight = 0.0;
                this.pagewidth = 0.0;

                this.scaling = 1.0;
                this.loadscaling = 1.0;
                this.xoffset = 0.0;
                this.yoffset = 0.0;
                this.scalingext = 0.0;
                this.xoffsetext = 0.0;
                this.yoffsetext = 0.0;
                this.rotation = 0;
                this.textrotate = 0;
                this.bUsemouseinput = true; //if angle and length set by numric input check this in adjustforroation
                this.docdx = DocObj.getCurrentpage().getdx();
                this.docdy = DocObj.getCurrentpage().getdy();
                this.docdscale = DocObj.getCurrentpage().getdscale();
                //measurement properties
                this.dimtext = "";
                this.dimtextx = "";
                this.dimtexty = "";

                this.dimradius = "";
                this.dimlength = "";


                this.dimarea = 0.0;
                this.dimlength = 0.0;

                this.hidevaluelabel = false;
                this.parentmarkupnumber = 0;
                this.parent = undefined;
                this.holes = [];

                this.dimtextangle = "";
                this.customlabeltext = "";
                this.linewidth = linewidthvalue;
                this.linewidthSegment = linewidthvalueSegment;
                //this.hatchStyle = HatchStyle;
                this.linestyle = nlinestyle;
                //this.linestyle = Linestylevalue.value;
                //General placement and scaling properties
                this.selected = false;

                this.marked = false;

                this.unselected = false;
                this.spaceselected = false;
                this.lastselected = false;
                this.selectedit = false;
                this.setmarkededit = false;
                this.firstpointset = false;
                this.lastpointset = false;

                this.hasScale = false;

                //0 = no action, 1 = move, 2 = scale, 3=rotate, 4=move point,5=edit text, 6=dimension label, 7=polyline segment select, 8=move label  
                this.editaction = 0;
                //0 = no corner, 1 = upper left, 2 = upper right, 3 = lower left, 4 = lover right,5=rotate marker
                this.scalecorner = 0;
                this.linesegment = { p1: -1, p2: -1 };
                this.multilinesegment = { p1: [], p2: [] };
                this.selectedpoint = -1;
                this.selectedmultipoint = [];

                this.bispartofGroup = false;
                this.groupRect = { x: 0, y: 0, w: 0, h: 0 };
                //text specific properties.
                this.textheight = 24;
                this.textwidth = 0;
                this.textdim = {width : 0, height : 0};
                this.stampsmalltwidth = 0;
                this.stampsmalltheight = 0;
                this.measuretextheight = nLabelTextsize;
                this.bhasArrow = false;
                this.bisTextArrow = false;
                this.markupArrowConnected = null;
                this.textBoxConnected = null;
                this.busecustomlabel = false;
                this.carpos = 0;
                this.text = "";
                this.intialtextset = false;
                this.smalltext = "";
                this.cursorblink = true;
                this.fontname = "Helvetica";
                
                var bmpath = (type == 1 && subtype == 3);
                var bmangle = (type == 1 && (subtype == 4 || subtype == 5));
                var brectarea = (type == 3 && subtype == 6);
                var barea = (type == 8);
                var bdimension = (type == 7);
                var bmsegments = (type == 1 && subtype == 6);
                var bmradius = (type == 4 && subtype == 1);
                var bmcount = (type == 13);
                var bmarc = (type == 14);

                this.ismeasure = (bmpath || bmangle || barea || brectarea || bdimension || bmsegments || bmradius || bmcount || bmarc);
                

                //this.font = new FontObject("Helvetica", 24, false, false);
                this.font = new FontObject("Helvetica", (type == 7 || type == 8 || bmpath || bmangle || brectarea) ? nLabelTextsize : 24, false, false);
                if(type == 14){
                    this.renderUtils = new RenderUtils();
                }else{
                    this.renderUtils = null;
                }
                

                this.arrowlength = nArrowSize + this.linewidth;
                //text specific properties.
                //properties for objects with multiple points, pencil polygon etc.
                this.pointlist = [];
                this.points = [];
                this.sides = [];

                //counter properties
                this.countshapes = [];
                this.innerRadius = 1;
                this.outerRadius = 1;
                //counter properties

                this.numpoints = -1;
                this.customattributes = [];
                this.comments = [];

                //for delayed loading when page object is not available
                this.delayedloadobj = {DocRef : null, xmlDoc : null, fileVersion: null, pageobject : null};
                this.status = "none";

                this.bMultiOp = false;
                this.multiOpId = null;

                var bfixedScale = false;
                var bNewFileformat = false;
                var bNewestFileformat = false;

                var bRasterScale = false;

                var bFirstdrawn = false;
                var bcnvRotated = false;

                var bSetodelete = false;

                var bFontset = false;
                var bpageloaded = false;


                //properties for objects with multiple points, pencil polygon etc.
                //General variables
                var markersize = 20;
                var labelsize = { w: 0, h: 0 };
                var markercolor = "rgba(255, 255, 0, 0.3)";
                var stampcolor = "rgba(255, 0, 0, 0.3)";
                var markerlinewidth = 15;
                var arrowangle = 22.5;
                var arrowlength = nArrowSize + this.linewidth;
                var arrowanglerad = arrowangle / (180 / Math.PI);
                //General variables
                //text specific variables
                var blinker;
                //text specific variables
                //variables for ovals and curves
                var kappa = .5522848;
                //variables for ovals and curves
                //square root scaling factors for area calculation
                //var dscalesqroot = Math.sqrt(DocObj.pages[this.pagenumber].dscale);
                //var mscaleroot = Math.sqrt(DocObj.pages[this.pagenumber].MainImageScaling);
                //square root scaling factors for area calculation

                


                this.scaleObject = new ScaleObject();

                // Add method to toggle aspect lock
                this.toggleAspectLock = function() {
                    this.aspectLock = !this.aspectLock;
                    return this.aspectLock;
                };


                this.PerformUndo = function (UndoObj) {
                    var curpoint = 0;
                    var tempx = 0;
                    var tempy = 0;

                    this.type = UndoObj.type;
                    this.subtype = UndoObj.subtype;
                    this.alternative = UndoObj.alternative;
                    this.bRedact = UndoObj.bRedact;
                    this.consolidated = UndoObj.consolidated;
                    this.x = UndoObj.x;
                    this.y = UndoObj.y;
                    this.w = UndoObj.w;
                    this.h = UndoObj.h;
                    this.xscaled = UndoObj.xscaled;
                    this.yscaled = UndoObj.yscaled;
                    this.wscaled = UndoObj.wscaled;
                    this.hscaled = UndoObj.hscaled;
                    this.timestamp = UndoObj.timestamp;
                    this.layer = UndoObj.layer;
                    this.signature = UndoObj.signature;
                    this.color = UndoObj.color;
                    this.fillcolor = UndoObj.fillcolor;
                    this.strokecolor = UndoObj.strokecolor;
                    this.textcolor = UndoObj.textcolor;
                    this.scaling = UndoObj.scaling;
                    this.xoffset = UndoObj.xoffset;
                    this.yoffset = UndoObj.yoffset;
                    this.docdx = UndoObj.docdx;
                    this.docdy = UndoObj.docdy;
                    this.docdscale = UndoObj.docdscale;
                    this.scalingext = UndoObj.scalingext;
                    this.xoffsetext = UndoObj.xoffsetext;
                    this.yoffsetext = UndoObj.yoffsetext;
                    this.rotation = UndoObj.rotation;
                    this.textrotate = UndoObj.textrotate;
                    this.dimtext = UndoObj.dimtext;
                    this.linewidth = UndoObj.linewidth;
                    this.textheight = UndoObj.textheight;
                    this.measuretextheight = UndoObj.measuretextheight;
                    this.textwidth = UndoObj.textwidth;
                    this.text = UndoObj.text;
                    //this.fontname = UndoObj.fontname;
                    this.font = UndoObj.font;
                    this.editcurpoint = 0;
                    this.points = UndoObj.points.slice();

                    this.countshapes = UndoObj.countshapes.slice();
                    this.innerRadius = UndoObj.innerRadius;
                    this.outerRadius = UndoObj.outerRadius;

                    this.numpoints = UndoObj.points.length - 1;
                    for (var i = 0; i < UndoObj.pointlist.length; i++) {
                        this.pointlist[i] = UndoObj.pointlist[i].slice();
                    }
                    if (UndoObj.bdelete) {
                        //this.visible = true;
                        
                        this.addsides();
                        undoremovemarkup(UndoObj.markupnumber);
                        //UndoObj
                        //remove from docobj markuplist
                    }
                };

                this.setmultiValues = function(id){

                    this.bMultiOp = true;
                    this.multiOpId = id;
    
                };

                this.resetmultiValues = function(){

                    this.bMultiOp = false;
                    this.multiOpId = null;
        
                };


                this.resetmarkupmodified = function(){
                    markupobject.bmodyfied = false;
                };

                this.getJSONUniqueID = function(operation){

                    if(markupobject.pdfannotproxy){
                        return;
                    }

                    const getguidpromise = new Promise(function(resolve, reject){

                        RxCore_GUI_GuidRecieved.connect(function(resp, operation, bok){

                            if(bok){

                                //markupobject.setUniqueID(resp);

                                //console.log(markupobject.uniqueID);

                                var jsonobj = createJSONmarkupentitynode(markupobject, DocObj, DocObj.getPageObject(markupobject.pagenumber), false, operation);
                                

                                resolve(jsonobj);

                            }else{
                                reject(resp);
                            }

                            

                        });

                        getGUIDEx(operation, markupobject);

                    });

                    return getguidpromise;

                    
                

                    /*if(operation.created || markupobject.uniqueID == 0){

                        getRxUniqueID().then( function(response){
                            var GUID = response;
                            markupobject.setUniqueID(GUID);

                            return createJSONmarkupentitynode(markupobject, DocObj, DocObj.getPageObject(markupobject.pagenumber), false, operation);

                        });

                        
                    }else{
                        return createJSONmarkupentitynode(markupobject, DocObj, DocObj.getPageObject(markupobject.pagenumber), false, operation);
                    }*/



                }
                this.getJSON = function(operation){

                    var retval = createJSONmarkupentitynode(markupobject, DocObj, DocObj.getPageObject(markupobject.pagenumber), false, operation);

                    if(markupobject.pdfannotproxy){

                        retval = undefined;
                    }

                    //{created : false, modified : false, deleted : false}

                    //this.getPageObject = function (pagenumber) {


                    return retval;

                }

                this.setmarkupmodified = function(bmodified){
                    
                    if(!markupobject.bmodyfied){
                        

                        MarkupUndoFromTemp(markupobject);

                        //movetemptoundo
                    }

                    /*if(RxCore_GUI_Markupmodify){
                        RxCore_GUI_Markupmodify()
                    }*/
                    
                    
                    markupobject.bmodyfied = bmodified;
                };
                
                this.getmarkupmodified = function(){
                    return markupobject.bmodyfied;
                };

                this.multipoint = function(){
                    
                    return (markupobject.type == 8 || markupobject.type == 1 || markupobject.type == 13);
                };
                this.isSelected = function(pagenum){


                    var bsel = false;

                    var bseledit = (markupobject.selectedit && markupobject.type != 9);
                    var bselected = (markupobject.selected || bseledit);


                    var btextArrow = (markupobject.type == 9 && markupobject.bhasArrow);
                    
                    if (!btextArrow && bselected && markupobject.pagenumber == pagenum) {
                        bsel = true;
                    }

                    return bsel;

                };


                this.getselected = function(){
                    
                    var selected = false

                    if(markupobject.multipoint()){
                        selected = markupobject.selectedit;
                    }else{
                        selected = markupobject.selected;
                    }
                    
                    return selected;
                    //var bpointtype = (DocObj.markuplist[nMarkupcreated].type == 1 || DocObj.markuplist[nMarkupcreated].type == 8);
                };
                this.getmarked = function(){

                    //var marked = false;
                    //marked = markupobject.marked;

                    return markupobject.marked;

                };

                this.setselectedit = function (select) {
                    //markupobject.selectedit = select;

                    if(markupobject.signature == signature){
                        markupobject.selectedit = select;


                        /*if(markupobject.holes.length > 0){
                            for(var hi = 0; hi < markupobject.holes.length; hi++){

                                if(!select){
                                    markupobject.holes[hi].selectedit = select;
                                }
                            }
                        }*/


                    }else{

                        markupobject.setmarkededit = select;
                        markupobject.setmarked(select);
                        //markupobject.editaction
                    }


                };
                this.setTextBoxArrowSelect = function (select) {
                    markupobject.selected = select;
                };

                this.setmarked = function(select){

                    markupobject.marked = select;

                };

                this.onselect = function(index, bCanSelectSpace) {

                    var noselect = true;


                    if(markupobject.bhaveLink && (DocObj.bMarkupLocked || markupobject.locked)){
                        //found = false;
                        //blink = true;
                        //var id = markupobject.markupnumber;
                        //index = curindex;
                        //locked = markupobject.locked;
                        
                        var result = {found : false, blink : true, id : markupobject.markupnumber, locked : markupobject.locked, index : index};
                        
                        return result;
                    }

                    if (markupobject.selectedit) {

                        markupobject.setselect(false);
                        markupobject.bUsemouseinput = true;

                        result = {found : true, blink : false, id : markupobject.markupnumber, locked : false, index : index};

                        if(markupobject.editaction == 4 || markupobject.editaction == 8){
                            //break;
                            return result;
                        }


                    }else{

                        if (markupobject.editaction == 2 || markupobject.editaction == 3){
                            
                            if(noselect){
                                markupobject.setselect(true);
                                
                                result = {found : true, blink : false, id : markupobject.markupnumber, locked : false, index : index};
                                
                                noselect = false;

                                markupobject.bUsemouseinput = true;
                                //found = true;
                                //id = markupobject.markupnumber;
                                //index = curindex;
                            }
                            
                            return result;
                        }

                        if(!bMultiselect && !markupobject.getselected()) {
                            unselelectallmarkup();
                            noselect = true;
                        }


                        switch (markupobject.type) {
                            case 1:
                                
                            
                            //editpolygon
                                if(noselect){

                                    markupobject.setselectedit(true);
                                    //DocObj.markuplist[curindex].setselectedit(true);
                                    markupobject.setselect(false);
                                    
                                    markupobject.bUsemouseinput = true;
                                    markupobject.lastselected = true;

                                    result = {found : true, blink : false, id : markupobject.markupnumber, locked : false, index : index};

                                    //found = true;
                                    //id = DocObj.markuplist[curindex].markupnumber;
                                    //index = curindex;

                                    markupobject.lengthangleCallbackSelect();
                                    noselect = false;
                                }
                                /*if(bMultiselect && DocObj.markuplist[curindex].selected) {
                                    
                                }*/

                                break;
                            case 8:
                                
                                if(bCanSelectSpace || markupobject.subtype != 1){
                                    
                                    if(noselect){
                                        //DocObj.markuplist[curindex].setselectedit(true);
                                        markupobject.setselectedit(true);
                                        markupobject.setselect(false);
                                        markupobject.bUsemouseinput = true;
                                        markupobject.lastselected = true;

                                        result = {found : true, blink : false, id : markupobject.markupnumber, locked : false, index : index};

                                        //found = true;
                                        //id = DocObj.markuplist[curindex].markupnumber;
                                        //index = curindex;
                                        
                                        markupobject.lengthangleCallbackSelect();
                                        noselect = false;
                                    }


                                } //edit area

                                break;

                            case 13:

                                if(noselect){
                                    //DocObj.markuplist[curindex].setselectedit(true);
                                    markupobject.setselectedit(true);
                                    markupobject.setselect(false);
                                    markupobject.bUsemouseinput = true;
                                    markupobject.lastselected = true;

                                    result = {found : true, blink : false, id : markupobject.markupnumber, locked : false, index : index};
                                    //found = true;
                                    //id = DocObj.markuplist[curindex].markupnumber;
                                    //index = curindex;
                                    noselect = false;
                                }
                                //DocObj.markuplist[curindex].lengthangleCallbackSelect();
                            
                                break;

                            case 14:
                                if(noselect){
                                    //DocObj.markuplist[curindex].setselectedit(true);
                                    markupobject.setselectedit(true);
                                    markupobject.setselect(false);
                                    markupobject.lastselected = true;

                                    result = {found : true, blink : false, id : markupobject.markupnumber, locked : false, index : index};
                                    //found = true;
                                    //id = DocObj.markuplist[curindex].markupnumber;
                                    //index = curindex;
                                    noselect = false;
                                }


                                break;    


                            default:
                                
                                if(noselect){
                                    markupobject.setselect(true);
                                    markupobject.lastselected = true;

                                    result = {found : true, blink : false, id : markupobject.markupnumber, locked : false, index : index};

                                    //found = true;
                                    //id = DocObj.markuplist[curindex].markupnumber;
                                    //index = curindex;
                                    //add markup angle lenght callback;
                                    
                                    markupobject.lengthangleCallbackSelect();
                                    noselect = false;

                                }

                                break;

                        } //end switch

                    }

                    return result;
                    
                    //markupobject.bUsemouseinput = true;

                };                 
                
                this.unselect = function(){

                    
                    if(markupobject.selected){
                        markupobject.setselect(false);
                        markupobject.bUsemouseinput = true;
                        
                        if(RxCore_GUI_MarkupUnselect != undefined){
                            RxCore_GUI_MarkupUnselect.setMarkupunSelected(markupobject);
                        }

                    }

                    if(markupobject.selectedit){
                        markupobject.selectedit = false;
                        markupobject.bpointEdit = false;
                        markupobject.bUsemouseinput = true;
            
                        if(RxCore_GUI_MarkupUnselect != undefined){
                            
                            RxCore_GUI_MarkupUnselect.setMarkupunSelected(markupobject);
                        }

                    }

                };


                this.selectSet = function(){


                    if(markupobject.multipoint()){
                        markupobject.setselectedit(true);
                    }else{

                        markupobject.setselect(true);
                        //markupobject.lengthangleCallbackSelect();
                    }

                    if (RxCore_GUI_Markup != undefined) {
                
                        var btextboxleader = (markupobject.type == 9 &&  markupobject.subtype == 2);
                        
                        if(btextboxleader){
                            //RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[index].markupArrowConnected, {created : false, modified : true, deleted : false});
                            RxCore_GUI_Markup.setMarkupSelected(markupobject, {created : false, modified : false, deleted : false});
                        }else{
                            RxCore_GUI_Markup.setMarkupSelected(markupobject, {created : false, modified : false, deleted : false});
                        }
        
                    }
        


                };


                this.setselect = function (select) {
                    /*var bmultipoint = (markupobject.type == 8 || markupobject.type == 1);
                    if(bmultipoint){
                        markupobject.selectedit = select;
                    }else{
                        markupobject.selected = select;
                    }*/

                    

                    if(markupobject.signature == signature){

                        markupobject.selected = select;
                        
                        if (markupobject.bhasArrow && markupobject.markupArrowConnected != null) {
                            //var arrowmobj = DocObj.getmarkupbynumber(markupobject.markupArrowConnected);
                            //DocObj.markuplist[arrowmobj].setselect(select);
                            markupobject.markupArrowConnected.setselect(select);
                            return;
                        }
                        if (markupobject.bisTextArrow && markupobject.textBoxConnected != null) {
                            markupobject.textBoxConnected.setTextBoxArrowSelect(select);
                            //markupobject.textBoxConnected.selected = select;
                        }


                    }else{

                        if(markupobject.setmarkededit && !select){
                            markupobject.setmarked(markupobject.setmarkededit);
                            markupobject.setmarkededit = false;

                        }else{

                            markupobject.setmarked(select);

                            if (markupobject.bhasArrow && markupobject.markupArrowConnected != null) {
                                markupobject.markupArrowConnected.setmarked(select);
                                return;
                            }
                            if (markupobject.bisTextArrow && markupobject.textBoxConnected != null) {
                                markupobject.textBoxConnected.setmarked(select);
                                //markupobject.textBoxConnected.selected = select;
                            }

                            
                        }

                    }
                    
                    
                    /*if(select){
                        if (RxCore_GUI_Markup != undefined) {
                            RxCore_GUI_Markup.setMarkupSelected(markupobject, {created : false, modified : true, deleted : false});
                        }
            
                    }*/
                };


                function numlines(text){


                    var textarray = text.split('\n');
                    return textarray.length;
                    
                    //labelnotewidth = 0;


                    /*for (var i = 0; i < textarray.length; i++) {
                        if (ctx.measureText(textarray[i]) > labelnotewidth) {
                            labelnotewidth = ctx.measureText(textarray[i]);
                        }
                    }*/

                    
                }

                function getTextrect(){

                    var rect = canvas.getBoundingClientRect();

                    var xrect  = markupobject.rotatedrect.x + rect.left;
                    var yrect = markupobject.rotatedrect.y + rect.top;

                    var wrect = markupobject.wscaled;
                    var hrect = markupobject.hscaled;


                    var centercanvX = (canvasowidth / 2);
                    var centercanvY = (canvasoheight / 2);

                    var centerx = xrect + (wrect*0.5);
                    var centery = yrect + (hrect*0.5);

                    var CanvRotRad = DocObj.getCurrentpage().getpageRotation() * (Math.PI / 180);

                    if(DocObj.getCurrentpage().getpageRotation() != 0){
                        var rectcenter = markupobject.getRotatedMarkup(centercanvX, centercanvY, centerx, centery, CanvRotRad);
                        centerx = rectcenter.x;
                        centery = rectcenter.y;

                    }

                    centerx += rect.left;
                    centery += rect.top;

                    var txtrect = new Rectangle(xrect,yrect,wrect,hrect);
                    txtrect.rotation = DocObj.getCurrentpage().getpageRotation();
                    txtrect.rotation = markupobject.pagerotation;

                    return txtrect;

                }

                this.getTextSize = function(){

                    var nettextSize = {numlines : 0, textheight : 0, rect : null};

                    if(markupobject.type == 9){

                        if (markupobject.bUseFixedScale) {
                            var temptxtscale = markupobject.fixedscaleFactor;
                            temptxtscale *= fTextScale;
                        }
                        else {
                            temptxtscale = scalefactor;
                        }
                        
                        var textscaled = this.font.height * temptxtscale;
                        var toffsety = ((this.font.height / 4) * 2.0) * temptxtscale;


                        nettextSize.textheight = textscaled + toffsety;
                        nettextSize.numlines = numlines(markupobject.text);
                        nettextSize.rect = getTextrect();
                    }

                    return nettextSize;
                };



                this.editTextmarkupRect = function(){

                    if (RxCore_GUI_TextInput != undefined) {

                        RxCore_GUI_TextInput.operation.start = false;
                        RxCore_GUI_TextInput.operation.create = false;
                        RxCore_GUI_TextInput.operation.edit = true;
                        RxCore_GUI_TextInput.operation.save = false;
                        RxCore_GUI_TextInput.operation.markup = markupobject;

                        var rect = canvas.getBoundingClientRect();

                        var xrect  = markupobject.rotatedrect.x + rect.left;
                        var yrect = markupobject.rotatedrect.y + rect.top;

                        var wrect = markupobject.wscaled;
                        var hrect = markupobject.hscaled;


                        var centercanvX = (canvasowidth / 2);
                        var centercanvY = (canvasoheight / 2);

                        var centerx = xrect + (wrect*0.5);
                        var centery = yrect + (hrect*0.5);

                        var CanvRotRad = DocObj.getCurrentpage().getpageRotation() * (Math.PI / 180);

                        if(DocObj.getCurrentpage().getpageRotation() != 0){
                            var rectcenter = markupobject.getRotatedMarkup(centercanvX, centercanvY, centerx, centery, CanvRotRad);
                            centerx = rectcenter.x;
                            centery = rectcenter.y;

                        }

                        centerx += rect.left;
                        centery += rect.top;

                        var txtrect = new Rectangle(xrect,yrect,wrect,hrect);
                        txtrect.rotation = DocObj.getCurrentpage().getpageRotation();
                        txtrect.rotation = markupobject.pagerotation;

                        var pixelRatio = getPixelRatio();
                        txtrect.x = ((txtrect.x - rect.left) / pixelRatio) + rect.left;
                        txtrect.y = ((txtrect.y - rect.top) / pixelRatio) + rect.top;
                        txtrect.w = txtrect.w / pixelRatio;
                        txtrect.h = txtrect.h / pixelRatio;


                        RxCore_GUI_TextInput.setTextInput(txtrect);
                        DocObj.selectedmarkup.edit = false;



                    }


                };                               

                this.ChangeMarkup = function(type){


                    //markupobject.savemarkupstate(DocObj,undoModify);
                    

                    //this.savemarkupstateChanged = function(DocRef,operation){


                    
                    switch (type) {
                        case 0:
                    
                            markupobject.savemarkupstate(DocObj,undoModify);
                            markupobject.layer = markuplayer;
                            

                            break;
                        case 1:
                            //markupobj.color = markupcolor;
                            markupobject.savemarkupstate(DocObj,undoModify);
                            markupobject.strokecolor = convertHex(markuplinecolor, 100);
                            
            
                            break;
                        case 2:
                            
                            markupobject.linewidth = linewidthvalue;
                            if (markupobject.type == 6 || markupobject.type == 7) {
                                var arrowlength = nArrowSize;
                                arrowlength += linewidthvalue;
                                markupobject.arrowlength = arrowlength;
                            }
                            markupobject.setmarkupmodified(true);
                            break;
                        case 3:
                            /* background pattern */
                            //markupobject.savemarkupstateChanged(DocObj,undoModify);
                            markupobject.savemarkupstate(DocObj,undoModify);
                            markupobject.alternative = fillstyle;
                            if (markupobject.alternative >= 3) {
                                markupobject.hatchStyle = markupobject.alternative - 3;
                            }
                            
                            //markupobject.setmarkupmodified(true);

                            break;
                        case 4:
                            markupobject.savemarkupstate(DocObj,undoModify);
                            //markupobject.savemarkupstateChanged(DocObj,undoModify);
                            //markupobj.fontname = fontstylevalue;
                            markupobject.font.setFontname(defaultFont.fontName);
                            markupobject.font.setHeight(defaultFont.height);
                            markupobject.font.setBold(defaultFont.bold);
                            markupobject.font.setItalic(defaultFont.italic);

                            //markupobject.setmarkupmodified(true);

            
                            break;
                        case 5:
                            //markupobj.rotation = markuprotation;
                            break;
                        case 6:
            
                            

                            if (colorisHex(markupobject.fillcolor)){
                                markupobject.fillcolor = convertHex(markupobject.fillcolor, 100);
                            }
                                
                            if (colorisHex(markupobject.strokecolor)){
                                markupobject.strokecolor = convertHex(markupobject.strokecolor, 100);
                            }
                            
                    

                            markupobject.fillcolor = setTransp(markupobject.fillcolor, globaltransparency);
                            markupobject.strokecolor = setTransp(markupobject.strokecolor, 100);
                            markupobject.transparency = globaltransparency;
                            markupobject.setmarkupmodified(true);

                            break;
                        case 7:
                            markupobject.savemarkupstate(DocObj,undoModify);
                            markupobject.fillcolor = convertHex(markupfillcolor, markupobject.transparency);
                            //markupobject.setmarkupmodified(true);
                            break;
                        case 8:
                            //markupobj.textcolor = convertHex(markuptextcolor, markupobj.transparency);
                            markupobject.savemarkupstate(DocObj,undoModify);
                            markupobject.textcolor = convertHex(markuptextcolor, 100);
                            //markupobject.setmarkupmodified(true);
                            break;
            
                        case 9:
                            markupobject.savemarkupstate(DocObj,undoModify);
                            markupobject.linestyle = nlinestyle;
                            //markupobject.setmarkupmodified(true);
                            break;
            
                        case 10:
                            markupobject.savemarkupstate(DocObj,undoModify);
                            if (markupobject.type == 6 || markupobject.type == 7) {
                                
                                var arrowlength = nArrowSize;
                                arrowlength += markupobject.linewidth;
                                markupobject.arrowlength = arrowlength;

                            }
                            //markupobject.setmarkupmodified(true);
                            break;
                        case 11:
                            markupobject.savemarkupstate(DocObj,undoModify);

                            var bmpath = (markupobject.type == 1 && markupobject.subtype == 3);

                            var bangle = (markupobject.type == 1 && (markupobject.subtype == 4 || markupobject.subtype == 5));
            
            
                            if (markupobject.type == 8 || markupobject.type == 7 || bmpath || bangle) {
                                
                                //var arrowlength = nArrowSize;
                                markupobject.font.setHeight(nLabelTextsize);
                                //arrowlength += markupobj.linewidth;
                                //markupobj.arrowlength = arrowlength;
                            }
                            //markupobject.setmarkupmodified(true);
                            break;
                        case 12:
            
                            //markupobj.fillcolor = setTransp(markupobj.fillcolor, globaltransparency);
                            //markupobj.strokecolor = setTransp(markupobj.strokecolor, 100);
                            //markupobject.savemarkupstateChanged(DocObj,undoModify);
                            markupobject.labelfillcolor = setTransp(markupobject.labelfillcolor, globaltransparency);
                            markupobject.labeltransparency = globaltransparency;
                            
                            markupobject.setmarkupmodified(true);
                            break;
            
                        case 13:
                            markupobject.savemarkupstate(DocObj,undoModify);

                            if (markupobject.type == 13) {
                                markupobject.setCountSize(nCounterSize);
                                //countmarkupobj.outerRadius = nCounterSize;
                                //countmarkupobj.innerRadius = nCounterSize * 0.4;
                            }
                            //markupobject.setmarkupmodified(true);
                            break;
                            
                        case 14 :
                            
                            markupobject.savemarkupstate(DocObj,undoModify);
                            markupobject.setdisplay(false);
                            
                            break;
            
                        case 15 :
                            markupobject.savemarkupstate(DocObj,undoModify);
                            markupobject.setdisplay(true);
                            
                            break;
            
                        case 16 : 
                            markupobject.savemarkupstate(DocObj,undoModify);
                            markupobject.showperimeter(true);
                            
                            break;
                            
                        case 17 :     
                            markupobject.savemarkupstate(DocObj,undoModify);
                            markupobject.showperimeter(false);
                            
                            break;

                        case 18 :
                            markupobject.savemarkupstate(DocObj,undoModify);

                            if (markupobject.type == 1 && markupobject.subtype == 4) {
                                markupobject.subtype = 5;
                            }else if (markupobject.type == 1 && markupobject.subtype == 5) {
                                markupobject.subtype = 4;
                            }


                            
                            break;
                        case 19 :
                            markupobject.linewidthSegment = linewidthvalueSegment;
                            markupobject.setmarkupmodified(true);
                            break;
    
            
                    }


                    if(RxCore_GUI_MarkupChanged != undefined){
                        var operation = markupobject.editaction;
                        RxCore_GUI_MarkupChanged.setMarkupSelected(markupobject, operation);
                    }

                    //drawmarkupAll(cntximg);
                    //DrawMarkupSelected(context);
                    //markupobject.setmarkupmodified(true);
                    
            

                };


                this.settxtboxArrow = function (arrowmarkup) {
                    if (markupobject.type != 9 && markupobject.subtype != 1) {
                        return;
                    }
                    markupobject.bhasArrow = true;
                    markupobject.markupArrowConnected = arrowmarkup;
                    arrowmarkup.bisTextArrow = true;

                    var scalefactor = getCurrentPageScale();
                    var fixedscale = getCurrentPageFixedScale();
                    markupobject.fixedscaleFactor = scalefactor / fixedscale;


                    if (markupobject.bUseFixedScale) {
                        var temptxtscale = markupobject.fixedscaleFactor;
                        temptxtscale *= fTextScale;
                    }
                    else {
                        temptxtscale = scalefactor;
                    }

                    markupobject.font.setScale(temptxtscale);



                    arrowmarkup.textBoxConnected = markupobject;
                };
                this.setLink = function (szURL, bhavelink) {
                    /*if (szURL != "") {
                    }*/

                    markupobject.linkURL = szURL;
                    markupobject.bhaveLink = bhavelink;

                };
                this.setAction = function(szActionName , bhavelink){
                    /*if (szURL != "") {
                    }*/

                    markupobject.actionName = szActionName;
                    markupobject.bhaveLink = bhavelink;


                };
                this.getLink = function () {
                    return {
                        bCanHaveLink: markupobject.bCanHaveLink,
                        link: markupobject.linkURL,
                        bhavelink: markupobject.bhaveLink
                    };
                };
                this.getUniqueID = function () {
                    return markupobject.uniqueID;
                };

                this.setdelete = function(){
                    bSetodelete = true;
                };

                this.getdelete = function(){
                    return bSetodelete;
                };


                this.setUniqueID = function (id) {
                    if (markupobject.uniqueID == 0) {
                        if (checkUniqueID(id)) {
                            markupobject.uniqueID = id;
                        }
                    }
                };
                this.setRxUniqueID = function () {
                    //call new server method to get GUID.
                    getGUID(markupobject);
                };
                this.onRotate = function () {
                };
                this.SetFromStorage = function () {
                };
                this.calculateDrawAngle = function (rect) {
                    var angleRadians = Math.atan2(rect.h - rect.y, rect.w - rect.x);
                    //normalize to remove negative values
                    if (angleRadians < 0)
                        angleRadians += 2 * Math.PI;
                    markupobject.orthoDrawAngle = angleRadians;
                    markupobject.orthoDrawAngledeg = markupobject.orthoDrawAngle * (180 / Math.PI);
                    //markupobject.orthoDrawAngle = angleRadians * (180 / Math.PI);
                    //calculateOrtho
                };
                this.calculateLength = function(points){

                    var xdiff = Math.max(points[0].x, points[1].x) - Math.min(points[0].x, points[1].x);
                    var ydiff = Math.max(points[0].y, points[1].y) - Math.min(points[0].y, points[1].y);


                    var radius = markupobject.getdiag(xdiff, ydiff);
                    return radius;
                    

                };
                this.calculateOrthoPoints = function(points){

                    //points is an array of two points point is an object with x and y value.
                    
                    var angleRadians = Math.atan2(-(points[1].y - points[0].y), points[1].x - points[0].x);
                    return angleRadians;


                };
                this.getClosestDegree = function(nOrthoDegree, degrees){

                    var resultangle = 0;

                    //var closeforward =  degrees + nOrthoDegree;
                    //var closebackward =  degrees - nOrthoDegree;

                    //var halfdeg = nOrthoDegree * 0.5;
                    //var sections = 360 / nOrthoDegree;

                    var bcontinue = true;
                    var startval = 0;
                    var endval = 360;
                    var curval = 0;
                    var diff = Math.abs(degrees - curval);
                    resultangle = curval;


                    while(bcontinue){
                        curval = curval + nOrthoDegree;
                        var curdiff = Math.abs(degrees - curval);
                        
                        if (curdiff < diff){
                            diff = curdiff;
                            resultangle = curval;
                            if (curval == endval){
                                resultangle = startval;
                            }
                        }
                        if (curval == endval){
                            bcontinue = false;
                        }

                    }

                    
                    
                    return resultangle;
                    

                };
                this.calculateOrthoDraw = function(startpoint, curpoint){
                    var pointLength = markupobject.calculateLength([{x:startpoint.x,y:startpoint.y},{x:curpoint.x,y:curpoint.y}]); //Math.atan2(this.h - this.y, this.w - this.x);
                    var angleRadians = markupobject.calculateOrthoPoints([{x:startpoint.x,y:startpoint.y},{x:curpoint.x,y:curpoint.y}]); //Math.atan2(this.h - this.y, this.w - this


                    if (angleRadians < 0)
                    angleRadians += 2 * Math.PI;
                    var degrees = angleRadians * (180 / Math.PI);

                    var anglesnap = markupobject.getClosestDegree(nOrthoDegree, degrees);

                    var point = markupobject.setAnglelengthUnscaled(startpoint, pointLength, anglesnap, undefined);

                    return point;

                };
                this.calculateOrtho = function (bscale, corner, point) {
                    //angle = atan2(vector2.y, vector2.x) - atan2(vector1.y, vector1.x);
                    //var angle = Math.atan2(this.h, this.w) - Math.atan2(this.y, this.x);
                    var pointLength = markupobject.calculateLength([{x:this.x,y:this.y},{x:point.x,y:point.y}]); //Math.atan2(this.h - this.y, this.w - this.x);
                    var angleRadians = markupobject.calculateOrthoPoints([{x:this.x,y:this.y},{x:this.w,y:this.h}]); //Math.atan2(this.h - this.y, this.w - this.x);
                    var pageangle = DocObj.getCurrentpage().getpageRotation() * (Math.PI / 180);
                    //angleRadians += pageangle;
                    /*if(bscale){
                        switch(corner){
                            case 1 :
                                angleRadians = Math.atan2(point.y - this.y, point.x- this.x);
                                break;
                            case 4 :
                                angleRadians = Math.atan2(this.h - point.y, this.w- point.x);
                                break;
                        }
                    }*/
                    if (angleRadians < 0)
                        angleRadians += 2 * Math.PI;
                    var degrees = angleRadians * (180 / Math.PI);

                    var anglesnap = markupobject.getClosestDegree(nOrthoDegree, degrees);

                    var point = markupobject.setAnglelengthUnscaled({x:this.x,y:this.y}, pointLength, anglesnap, undefined);


                    if (bscale && corner == 1) {
                        //this.y = this.h;
                        this.x = point.w;
                        this.y = point.h;
                    }
                    else {
                        //this.h = this.y;
                        this.w = point.w;
                        this.h = point.h;

                    }

                    //console.log(degrees);
                };
                this.ClearAttributes = function () {
                    this.customattributes = [];
                };
                this.ClearComments = function () {
                    this.comments = [];
                };

                this.ConsolidateList = function (settings, last) {
                    consolidateObj.add(markupobject, settings, last);
                };
                this.Consolidate = function (settings) {
                    //layer : markuplayer,
                    //fillcolor : markupfillcolor,
                    //linecolor : markuplinecolor,
                    //textcolor : markuptextcolor,
                    //changeStrokeColor : false,
                    //changeTexColort : false,
                    //changeLayer : true,
                    //bperformed : false,
                    //isactive : false,
                    //signature : signature,
                    //todo create undo save point.
                    //MarkupSaveState(markupobject.markupnumber);
                    MarkupSaveStateByref(markupobject);
                    DocObj.bMarkupchanged = true;

                    if (markupobject.bhasArrow && markupobject.markupArrowConnected != null){
                        markupobject.markupArrowConnected.Consolidate(settings);
                    }

                    markupobject.consolidated = true;

                    markupobject.setdisplay(getSignState(signature));

                    markupobject.signature = signature;
                    if (settings.changeStrokeColor) {
                        markupobject.strokecolor = settings.strokecolor;
                    }
                    if (settings.changeTexColort) {
                        markupobject.textcolor = settings.textcolor;
                    }
                    if (settings.changeLayer) {
                        markupobject.layer = settings.layer;
                        //todo change fill color if layer is changed.
                    }
                    drawmarkupAll(cntximg);
                    DrawMarkupSelected(context);

                    /*if (RxCore_GUI_MarkupConsolidated != undefined) {
                        RxCore_GUI_MarkupConsolidated.consolidated(markupobject, DocObj.bActive);
                    }*/

                    if(RxCore_GUI_ConsolidateMarkup != undefined){

                        RxCore_GUI_ConsolidateMarkup.Consolidatevt(markupobject);
                    }

                    if (RxCore_GUI_Markuplist != undefined) {
                        RxCore_GUI_Markuplist.setMarkupList(DocObj.getmarkuplist(), DocObj.bActive);
                    }
                };

                function autocomment(){
                    
                    var bautocomment = false;

                    var measuretype_1 = (markupobject.type == 7);
                    var measuretype_2 = (markupobject.type == 8);

                    var measurepath = (markupobject.type == 1 && markupobject.subtype == 3);
                    var measureobj_1 = (markupobject.type == 1 && markupobject.subtype == 4);
                    var measureobj_2 = (markupobject.type == 1 && markupobject.subtype == 5);
                    var measureobj_3 = (markupobject.type == 1 && markupobject.subtype == 6);

                    var measureobj_4 = (markupobject.type == 13);


                    //bautocomment = (measuretype_1 || measuretype_2 || measurepath || measureobj_1 || measureobj_2 || measureobj_3 || measureobj_4);
                    
                    if(markupobject.type == 9 && markupobject.text != ""){
                        bautocomment = true;
                    }

                    if(markupobject.type == 10 && markupobject.text != ""){
                        bautocomment = true;
                    }

                    return bautocomment;

                }

                this.AddAttribute = function (szName, szValue) {
                    var extendobj = {
                        name: szName,
                        value: szValue
                    };
                    markupobject.customattributes.push(extendobj);

                    //markupobject.savemarkupstate(DocObj,undoModify);

                };

                this.CreateComment = function (szValue) {

                    var btextcomment = (markupobject.type == 9 || markupobject.type == 10);

                    if (!btextcomment){
                        markupobject.text = szValue;    
                    }

                    /*var extendobj = {
                        id : markupobject.comments.length,
                        signature : getSignature(),
                        value: szValue
                    };
                    markupobject.comments.push(extendobj);*/
                };

                this.AddCommentInternal = function (id, signature, szValue, timestamp) {
                    var extendobj = {
                        id : id,
                        signature : signature,
                        value: szValue,
                        timestamp : "timestamp"
                    };
                    if(timestamp != undefined){
                        extendobj.timestamp = timestamp;
                    }else{
                        extendobj.timestamp = markupobject.timestamp;
                    }

                    markupobject.comments.push(extendobj);


                };


                this.AddComment = function (id, signature, szValue, timestamp) {
                    var extendobj = {
                        id : id,
                        signature : signature,
                        value: szValue,
                        timestamp : "timestamp"
                    };

                    if(timestamp != undefined){
                        extendobj.timestamp = timestamp;
                    }else{
                        extendobj.timestamp = markupobject.timestamp;
                    }

                    markupobject.comments.push(extendobj);

                    

                    if(markupobject.comments.length > 0){
                        markupobject.savemarkupstate(DocObj,undoModify);
                    }


                    if(RxCore_GUI_MarkupChanged != undefined){
                        var operation = markupobject.editaction;
                        RxCore_GUI_MarkupChanged.setMarkupSelected(markupobject, operation);
                    }


                };
                this.GetComments = function(){


                    /*if(autocomment()){
                        if(markupobject.comments.length == 0){
                            //var id = markupobject.comments.length;

                            markupobject.AddComment(0, signature, markupobject.text);
                        }else{

                            markupobject.comments[0].value = markupobject.text;
                        }
                        
                    }*/

                    /*if(markupobject.type == 9 && markupobject.text != ""){
                        if(markupobject.comments.length == 0){
                            //var id = markupobject.comments.length;

                            markupobject.AddComment(0, signature, markupobject.text);
                        }else{

                            markupobject.comments[0].value = markupobject.text;
                            //markupobject.editComment(0, markupobject.text);
                            //markupobject.deleteComment(0);
                            //markupobject.AddComment(0, signature, markupobject.text);
                        }
                        
                    }*/

                    /*if(markupobject.type == 10 && markupobject.text != ""){
                        if(markupobject.comments.length == 0){
                            //var id = markupobject.comments.length;

                            markupobject.AddComment(0, signature, markupobject.text);
                        }else{

                            markupobject.comments[0].value = markupobject.text;
                            //markupobject.editComment(0, markupobject.text);
                            //markupobject.deleteComment(0);
                            //markupobject.AddComment(0, signature, markupobject.text);
                        }
                        
                    }*/
                    //type == 1
                    //subtype == 3-6
                    

                    /*if(bmeasurearkup ){
                        if(markupobject.comments.length == 0){
                            //var id = markupobject.comments.length;

                            markupobject.AddComment(0, signature, markupobject.dimtext);
                        }else{

                            markupobject.comments[0].value = markupobject.dimtext;
                            //markupobject.editComment(0, markupobject.dimtext);
                            //markupobject.deleteComment(0);
                            //markupobject.AddComment(0, signature, markupobject.dimtext);
                        }
                        
                    }*/



                    return markupobject.comments;
                };

                this.GetAttribute = function(szName){

                    var attrib = undefined;

                    for (var i = 0; i < markupobject.customattributes.length; i++) {
                        if (markupobject.customattributes[i].name == szName) {
                            attrib = markupobject.customattributes[i];
                        }
                    }

                    return attrib;
                };
                this.GetAttributes = function () {
                    return markupobject.customattributes;
                };
                this.deleteAttribute = function (szName) {
                    var foundindex = -1;
                    for (var i = 0; i < markupobject.customattributes.length; i++) {
                        if (markupobject.customattributes[i].name == szName) {
                            foundindex = i;
                        }
                    }
                    if (foundindex != -1) {
                        //splice to remove entry
                        markupobject.customattributes.splice(foundindex, 1);
                    }

                    //markupobject.savemarkupstate(DocObj,undoModify);
                };
                this.deleteComment = function (id) {
                    var foundindex = -1;
                    for (var i = 0; i < markupobject.comments.length; i++) {
                        if (markupobject.comments[i].id == id) {
                            foundindex = i;
                        }
                    }
                    if (foundindex != -1) {
                        //splice to remove entry
                        markupobject.comments.splice(foundindex, 1);
                    }

                    if(RxCore_GUI_MarkupChanged != undefined){
                        var operation = markupobject.editaction;
                        RxCore_GUI_MarkupChanged.setMarkupSelected(markupobject, operation);
                    }

                    /*if(autocomment()){
                        if(markupobject.comments.length > 0){
                            markupobject.savemarkupstate(DocObj,undoModify);
                        }
    
                    }else{
                        markupobject.savemarkupstate(DocObj,undoModify);
                    }*/

                };

                this.findAttribute = function (szName, szValue) {
                    var found = false;
                    for (var i = 0; i < markupobject.customattributes.length; i++) {
                        if (markupobject.customattributes[i].name == szName && markupobject.customattributes[i].value == szValue) {
                            found = true;
                            //markupobject.customattributes[i].name = newszName;
                            //markupobject.customattributes[i].value = newszValue;
                        }
                    }
                    return found;
                };
                this.updateAttribute = function (newszName, newszValue) {
                    for (var i = 0; i < markupobject.customattributes.length; i++) {
                        if (markupobject.customattributes[i].name == newszName) {
                            markupobject.customattributes[i].name = newszName;
                            markupobject.customattributes[i].value = newszValue;
                        }
                    }


                    //markupobject.savemarkupstate(DocObj,undoModify);



                };

                this.editComment = function (id, newszValue,timestamp) {
                    
                    for (var i = 0; i < markupobject.comments.length; i++) {
                        if (markupobject.comments[i].id == id) {
                            markupobject.comments[i].value = newszValue;
                            
                            if(markupobject.comments[i].timestamp != undefined && timestamp != undefined){
                                markupobject.comments[i].timestamp = timestamp;
                            }
                            
                        }
                    }

                    //console.log(markupobject.comments.length);

                    /*if(markupobject.comments.length > 0){
                        markupobject.savemarkupstate(DocObj,undoModify);
                    }*/


/*                     if(autocomment()){
                        if(markupobject.comments.length > 0){
                            markupobject.savemarkupstate(DocObj,undoModify);
                        }
    
                    }else{
                        markupobject.savemarkupstate(DocObj,undoModify);
                    }

                    
 */

                    if(RxCore_GUI_MarkupChanged != undefined){
                        var operation = markupobject.editaction;
                        RxCore_GUI_MarkupChanged.setMarkupSelected(markupobject, operation);
                    }


                };

                this.restorestampfontSize = function () {
                    markupobject.font.setHeight(markupobject.w * 0.16);
                    if (markupobject.alternative == 0) {
                        markupobject.stampsmalltheight = (markupobject.w * 0.08) - 4;
                        if (markupobject.stampsmalltheight < 1) {
                            markupobject.stampsmalltheight = 1;
                        }
                        var combinedtextheight = markupobject.textheight + markupobject.stampsmalltheight + ((markupobject.h / 10) * 2);
                        if (combinedtextheight > markupobject.h) {
                            //this.textheight = (this.h / 2) - ((this.h / 10) * 2);
                            markupobject.font.setHeight((markupobject.h / 2) - ((markupobject.h / 10) * 2));
                            markupobject.stampsmalltheight = (markupobject.textheight / 2) - 4;
                            if (markupobject.stampsmalltheight < 1) {
                                markupobject.stampsmalltheight = 1;
                            }
                        }
                    }
                    else {
                        if ((markupobject.w * 0.6) >= markupobject.h) {
                            //this.textheight = (this.h / 4);
                            markupobject.font.setHeight(markupobject.h / 4);
                        }
                        else {
                            //this.textheight = this.w * 0.16;
                            markupobject.font.setHeight(markupobject.w * 0.16);
                        }
                    }
                };
                this.updatePageMarkup = function(pageobject){

                    if(bpageloaded){
                        return;
                    }

                    //this.delayedloadobj = {DocRef : null, xmlDoc : null, fileVersion: null, scaleobject : null};

                    if(markupobject.delayedloadobj.DocRef != null && markupobject.delayedloadobj.xmlDoc != null && markupobject.delayedloadobj.fileVersion != null){
                        
                        markupobject.delayedloadobj.pageobject = pageobject;
                        
                        
                        var bOK = markupobject.SetfromXML(markupobject.delayedloadobj.DocRef, markupobject.delayedloadobj.xmlDoc,markupobject.delayedloadobj.fileVersion);
                        
    
                    }
                    

                };
                
                this.setfromJSON = function(DocRef, markupJSON){
                    
                    bFirstdrawn = false;
                    
                    var internalscale = 1;
                    var setscaledvalues = true;
                    var pageloaded = false;

                    

                    var markup = JSON.parse(markupJSON);

                    if (markup.Entity != null && markup.Entity != undefined){

                        if (markup.Entity.View != null){
                            this.pagenumber = markup.Entity.View;
                        }else{
                            this.pagenumber = 0;    
                        }

                        if (markup.Entity.ViewName3D != null){
                            this.viewname = markup.Entity.ViewName3D;    


                            if (markup.Entity.For3DView != null){
                                this.bfor3D = (markup.Entity.For3DView == 1);

                            }
        
                            if (markup.Entity.PageHeight3D != null){
                                this.pageheight = PageHeight3D;

                            }

                            if (markup.Entity.PageWidth3D != null){
                                this.pagewidth = PageWidth3D;

                            }

                        }else{
                            this.viewname = null;
                            this.bfor3D = false;
    
                        }

                        bNewFileformat = true;
                        bNewestFileformat = true;
    

                        var pageobject = DocRef.getPageObject(this.pagenumber);

                        if (markup.Entity.Rect != null){
                            
                            this.x = markup.Entity.Rect.x;
                            this.y = markup.Entity.Rect.y;
                            this.w = markup.Entity.Rect.w;
                            this.h = markup.Entity.Rect.h;
    
                        }

                        if(isNaN(this.x)){
                            this.x = 10;
                        }
    
                        if(isNaN(this.y)){
                            this.y = 10;
                        }
    
                        if(isNaN(this.w)){
                            this.w = 100;
                        }
    
                        if(isNaN(this.h)){
                            this.h = 100;
                        }
    
                        if (markup.Entity.FixedScale != null){

                            this.bUseFixedScale = (markup.Entity.FixedScale == 1);
                            bfixedScale = (markup.Entity.FixedScale == 1);
                        }

                        if (markup.Entity.FixedScaleFactor != null){
                            this.fixedscaleFactor = markup.Entity.FixedScaleFactor;
                        }



                        if (markup.Entity.Redaction != null){
                            this.bRedact  = (markup.Entity.Redaction == 1);
                        }else{
                            this.bRedact = false;
                        }

                        if (this.h == 0 && this.w == 0) {
                            if (this.type == 8 || this.type == 1 || this.type == 2 || this.type == 0 || this.type == 14) {
                            }
                            else {
                                return false;
                            }
                        }
                        if (this.type == 8 || this.type == 1 || this.type == 2) {
                            //skip size check
                        }
                        else {
                            //empty else
                        }

                        this.bCanHaveLink = ((this.type == 1 && this.subtype == 2) || this.type == 3 || this.type == 4 || this.type == 5 || this.type == 9 || this.type == 11 || this.type == 20);


                        if (markup.Entity.HaveLink != null){
                            this.bhaveLink = (markup.Entity.HaveLink == 1);
                        }

                        if (markup.Entity.LinkURL != null){
                            this.linkURL = markup.Entity.LinkURL;
                        }

                        if (markup.Entity.Layer != null){
                            this.layer = markup.Entity.Layer;
                        }else{
                            this.layer = markuplayer;
                        }

                        if (markup.Entity.TimeStamp != null){
                            this.timestamp = markup.Entity.TimeStamp;
                        }

                        if (markup.Entity.Rotation != null){
                            this.rotation = markup.Entity.Rotation;
                        }

                        //Attributes
                        if (markup.Entity.Extended != null){

                            for (var nodec = 0; nodec < markup.Entity.Extended.length; nodec++) {
                                
                                //this.AddAttribute(markup.Entity.Extended[nodec].tagname, markup.Entity.Extended[nodec].tagvalue);

                            }

                            
                        }

                        //Comments

                        if (markup.Entity.Comments != null){
                            
                            
                            markupobject.comments = [];

                            for (var cnodec = 0; cnodec < markup.Entity.Comments.length; cnodec++) {

                                //var commentNode = {id : null, signature : null, value : null};

                                if(markup.Entity.Comments[cnodec].timestamp != undefined){
                                    markupobject.AddCommentInternal(markup.Entity.Comments[cnodec].id, markup.Entity.Comments[cnodec].signature, markup.Entity.Comments[cnodec].value, markup.Entity.Comments[cnodec].timestamp);    
                                }else{
                                    markupobject.AddCommentInternal(markup.Entity.Comments[cnodec].id, markup.Entity.Comments[cnodec].signature, markup.Entity.Comments[cnodec].value);
                                }
                                


                            }

                        }

                        if (markup.Entity.Consolidated != null){
                            this.consolidated = (markup.Entity.Consolidated == 1);
                        }

                        if (markup.Entity.TextRotation != null){
                            this.textrotate = markup.Entity.TextRotation;
                        }

                        if (markup.Entity.PageRotation != null){
                            this.pagerotation = markup.Entity.PageRotation;
                        }

                        if (markup.Entity.FileName != null){
                            this.documentName = markup.Entity.FileName;
                        }

                        if (markup.Entity.Layout != null){
                            this.layout = markup.Entity.Layout;
                        }


                        if (markup.Entity.Color != null){
                            this.color = markup.Entity.Color;
                        }

                        /*if (markup.Entity.Handle != null){
                            this.markupID = markup.Entity.Handle;
                        }*/

                        if (markup.Entity.UniqueID != null){
                            this.uniqueID = markup.Entity.UniqueID;
                        }

                        if (markup.Entity.DocOffsetX != null){
                            this.docdx = markup.Entity.DocOffsetX;

                        }else{
                            if(pageobject != undefined){
                                this.docdx = pageobject.getdx();
                            }else{
                                this.docdx = 0;
                            }

                        }

                        if (markup.Entity.DocOffsetY != null){
                            this.docdy = markup.Entity.DocOffsetY;

                        }else{
                            if(pageobject != undefined){
                                this.docdy = pageobject.getdy();
                            }else{
                                this.docdy = 0;
                            }

                        }

                        if (markup.Entity.DocScale != null){
                            this.docdscale = markup.Entity.DocScale;
                        }else{
                            if(pageobject != undefined){
                                this.docdscale = pageobject.getdscale();
                            }else{
                                this.docdscale = 1;
                            }
                            
                        }

                        if (markup.Entity.Transparency != null){
                            this.transparency = markup.Entity.Transparency;

    
                        }

                        if (this.type == 3 && this.subtype == 3) {
                            this.transparency = 30;
                            this.fillcolor = setTransp(this.fillcolor, this.transparency);
                        }


                        if (markup.Entity.LineColor != null){
                            this.strokecolor = markup.Entity.LineColor;

                            if (colorisHex(this.strokecolor)){
                                this.strokecolor = convertHex(this.strokecolor, 100);
                            }else{
                                this.strokecolor = setTransp(this.strokecolor, 100);
                            }
    
                        }


                        if (markup.Entity.TextColor != null){
                            this.textcolor = markup.Entity.TextColor;

                            if (colorisHex(this.textcolor)){
                                this.textcolor = convertHex(this.textcolor, 100);
                            }else{
                                this.textcolor = setTransp(this.textcolor, 100);
                            }

                        }

                        if (markup.Entity.Scaling != null){
                            this.scaling = markup.Entity.Scaling;

                            var bfinite = isFinite(this.scaling);
    
                            if (!bfinite) {
                                this.scaling = 1;
                            }
        
                            if(isNaN(this.scaling)){
                                this.scaling = 1;
                            }
    
    
                        }
                        if (markup.Entity.LoadScaling != null){
                            this.loadscaling = markup.Entity.LoadScaling;

                            var bfinite = isFinite(this.loadscaling);
    
                            if (!bfinite) {
                                this.loadscaling = 1;
                            }
        
                            if(isNaN(this.loadscaling)){
                                this.loadscaling = 1;
                            }
    
    
                        }else{
                            this.loadscaling = this.scaling;
                        }



                        if (markup.Entity.FillColor != null){

                            this.fillcolor = markup.Entity.FillColor;

                            if (colorisHex(this.fillcolor)){
                                this.fillcolor = convertHex(this.fillcolor, this.transparency);
                            }else {
                                this.fillcolor = setTransp(this.fillcolor, this.transparency);
                            }

                        }

                        if (markup.Entity.Font.Font.facename != null){
                            this.fontname = markup.Entity.Font.Font.facename;
                        }

                        if (markup.Entity.Font.height != null){
                            this.textheight = markup.Entity.Font.Font.height;
                        }

                        if (this.fontname != undefined || this.textheight != undefined) {
                            this.font = new FontObject(this.fontname, this.textheight, false, false);
                        }


                        if (markup.Entity.Font != null){


                            //this.fontname = markup.Entity.Font.facename;

                            var facename = markup.Entity.Font.Font.facename;
                            var height = markup.Entity.Font.Font.height;
                            var bold = (markup.Entity.Font.Font.bold == 1);
                            var italic = (markup.Entity.Font.Font.italic == 1);

                            this.font = new FontObject(facename, height, bold, italic);
                            this.textheight = height;

                            

                        }

                        if (markup.Entity.Signature != null){
                            this.signature = markup.Entity.Signature;
                        }

                        if (markup.Entity.Xoffset != null){
                            this.xoffset = markup.Entity.Xoffset;
                        }

                        if (markup.Entity.Yoffset != null){
                            this.yoffset = markup.Entity.Yoffset;
                        }
    
                        if(isNaN(this.xoffset)){
                            this.xoffset = 0;
                        }
    
                        if(isNaN(this.yoffset)){
                            this.yoffset = 0;
                        }
    

                        //markup.Entity.Font.facename
                        //entityJSONNode.Entity.Font = fontobj;
                        //var fontobj = {Font : {facename:null, height:null, bold:null, italic:null}};
                

                        if (this.type == 7) {


                            if (markup.Entity.LeaderLineOffset != null){
                                this.leaderoffset = markup.Entity.LeaderLineOffset;
                            }
                        }

                        if (this.type == 8) {

                            if (markup.Entity.LabelOffsetX != null){
                                this.labeloffsetx = markup.Entity.LabelOffsetX;
                            }

                            if (markup.Entity.LabelOffsetY != null){
                                this.labeloffsety = markup.Entity.LabelOffsetY;
                            }

                            if (markup.Entity.LabelColor != null){
                                this.labelfillcolor = markup.Entity.LabelColor;
                            }

                        }


                        if (markup.Entity.LineWidth != null){
                            this.linewidth = markup.Entity.LineWidth;
                        }
                        
                        if (markup.Entity.LineWidthSegment != null){
                            this.linewidthSegment = markup.Entity.LineWidthSegment;
                        }else{
                            this.linewidthSegment = linewidthvalueSegment;
                        }
    
                        if (markup.Entity.ArrowSize != null){
                            this.arrowlength = markup.Entity.ArrowSize;
                        }

                        if (markup.Entity.LineStyle != null){
                            this.linestyle = markup.Entity.LineStyle;
                        }

                        if (markup.Entity.Text != null){
                            this.text = markup.Entity.Text;

                            if (this.text == ".") {
                                this.text = "";
                            }

                        }else{
                            this.text = "";
                        }

                        if (markup.Entity.Status != null){
                            this.status = markup.Entity.Status;
                        }


                        if (markup.Entity.SmallText != null){
                            this.smalltext = markup.Entity.SmallText;

                            if (this.smalltext == ".") {
                                this.smalltext = "";
                            }

                        }

                        if (markup.Entity.DimText != null){
                            this.dimtext = markup.Entity.DimText;

                            if (this.dimtext == ".") {
                                this.dimtext = "";
                            }

                        }else{
                            this.dimtext = "";
                        }


                        if (markup.Entity.CustomLabelText != null){
                            this.customlabeltext = markup.Entity.CustomLabelText;

                            if (this.customlabeltext == ".") {
                                this.customlabeltext = "";
                            }

                        }else{
                            this.customlabeltext = "";
                        }

                        if (markup.Entity.CustomLabelOn != null){
                            this.busecustomlabel = (markup.Entity.CustomLabelOn == 1);
                        }

                        if (this.type == 13) {


                            if (markup.Entity.innerRadius != null){
                                this.innerRadius = markup.Entity.innerRadius;
                            }

                            if (markup.Entity.outerRadius != null){
                                this.outerRadius = markup.Entity.outerRadius;
                            }
    
    
                            this.removepoints();

                            if (markup.Entity.Points != null){

                                var PointList = markup.Entity.Points.Point;

                                if(PointList.length != 0){
                                    for (var i = 0; i < PointList.length; i++) {

                                        var x = PointList[i].x;
                                        var y = PointList[i].y;
    
                                        markupobject.addcountLoad({x : x, y : y});
    
                                    }
    
                                } else {
                                    return false;
                                }
                                    
                            }

                            //entityJSONNode.Entity.Points.Point.push({x: xpoint, y : ypoint});
    
                        }                        

                        if (this.type == 14) {

                            if(markup.Entity.startX != null){
                                markupobject.startX = markup.Entity.startX;
                            }

                            if(markup.Entity.startY != null){
                                markupobject.startY = markup.Entity.startY;
                            }

                            if(markup.Entity.secondX != null){
                                markupobject.secondX = markup.Entity.secondX;
                            }

                            if(markup.Entity.secondY != null){
                                markupobject.secondY = markup.Entity.secondY;
                            }

                            if(markup.Entity.endX != null){
                                markupobject.endX = markup.Entity.endX;
                            }

                            if(markup.Entity.endY != null){
                                markupobject.endY = markup.Entity.endY;
                            }


                        }

                        var bsegment = (this.type == 1 && this.subtype == 6);
                        var bpoly = (this.type == 1 || this.type == 2 || this.type == 8);


                        if (bpoly && !bsegment) {
                            this.removepoints();


                            if (markup.Entity.Points != null){

                                var PointList = markup.Entity.Points.Point;

                                if(PointList.length != 0){
                                    for (var i = 0; i < PointList.length; i++) {

                                        var x = PointList[i].x;
                                        var y = PointList[i].y;
    
                                        markupobject.addpoint(x, y);
    
                                    }

                                    markupobject.findrectangle();
    
                                } else {
                                    return false;
                                }
                                    
                            }
                            
                        }

                        if (this.type == 0 || bsegment) {
                            this.removepoints();

                            if (markup.Entity.Line != null && markup.Entity.Line.length != 0){

                                var pointLines = markup.Entity.Line;

                                for (i = 0; i < pointLines.length; i++) {
                                    var linePoints = pointLines[i].Point;


                                    for (var j = 0; j < linePoints.length; j++) {
                                        x = linePoints[j].x;
                                        y = linePoints[j].y;
                                        /*if (bNewFileformat && (pageobject.usepdfjs || pageobject.usevectorxml)) {
                                            x = (x + this.xoffset) * internalscale;
                                            y = (y + this.yoffset) * internalscale;
                                        }*/
                                        var tempPoint = new point(x, y);
                                        this.points.push(tempPoint);
                                        //this.pointlist[i][j].y = y;
                                    }
                                    this.pointlist.push(this.points);
                                    this.points = [];
                                }


                            }else {
                                return false;
                            }

                        }

                        if (this.type == 12) {
                            if (this.transparency == undefined) {
                                this.transparency = 30;
                                this.fillcolor = setTransp(this.fillcolor, this.transparency);
                            }
                            if (this.subtype == 100) {

                                this.text = markup.Entity.Text;
                            }

                            this.font.setFontname("Times New Roman");
                            this.font.setBold(true);
                        }

                        //create image either from separate image or embedded data.
                        //this.image = createimage;

                        if (this.type == 11 || this.type == 20) {
                            var createimage = new Image();

                            if(markup.Entity.SvgContent != null){
                                markupobject.svgContent = markup.Entity.SvgContent;
                                markupobject.markupsvgload(markup.Entity.SvgContent, createimage);
                            }else if (markup.Entity.Image != null){
                                var cdata = markup.Entity.Image.image;

                                cdata = cdata.slice(9, cdata.length - 3);

                                markupobject.markupcdataload(createimage, cdata);

                                //entityJSONNode.Entity.Image = {image : getBase64Image(markupobj.image), type : "png", encoding : "base64" };

                            }else if(markup.Entity.Imagehref != null){
                                var imageurl = markup.Entity.Imagehref;
                                markupobject.imagehref = imageurl;
                                markupobject.markupimageload(imageurl, createimage);
                            } else{
                                this.type = 3;
                                this.subtype = 0;
                                this.alternative = 0;

                            }
    


                            
                        }


                    }
                    
                    
                    return true;



                };
                
                this.SetfromXML = function (DocRef, xmlDoc, fileVersion) {
                    var internalscale = 1;
                    var setscaledvalues = true;
                    var pageloaded = false;
                    
                    //this.pagenumber = parseInt(xmlDoc.getElementsByTagName('View')[0].firstChild.nodeValue);

                    if(xmlDoc.getElementsByTagName('View')[0] != undefined){
                        this.pagenumber = parseInt(xmlDoc.getElementsByTagName('View')[0].firstChild.nodeValue);
                    }else{
                        this.pagenumber = 0;
                    }

                    if(xmlDoc.getElementsByTagName('ViewName3D')[0] != undefined){
                        this.viewname = xmlDoc.getElementsByTagName('ViewName3D')[0].firstChild.nodeValue;

                        if (xmlDoc.getElementsByTagName('For3DView')[0] != undefined) {
                            this.bfor3D = (parseInt(xmlDoc.getElementsByTagName('For3DView')[0].firstChild.nodeValue) == 1);
                        }
    

                        if(xmlDoc.getElementsByTagName('PageHeight3D')[0] != undefined){
                            this.pageheight = parseInt(xmlDoc.getElementsByTagName('PageHeight3D')[0].firstChild.nodeValue);
                        }

                        if(xmlDoc.getElementsByTagName('PageWidth3D')[0] != undefined){
                            this.pagewidth = parseInt(xmlDoc.getElementsByTagName('PageWidth3D')[0].firstChild.nodeValue);
                        }

                    }else{
                        this.viewname = null;
                        this.bfor3D = false;
                    }

                    bNewFileformat = (fileVersion == 'C360' || fileVersion == 'C365' || fileVersion == 'C366' || fileVersion == 'C370');
                    bNewestFileformat = (fileVersion == 'C370');

                    //bfor3D

                    var pageobject = DocRef.getPageObject(this.pagenumber);
                    


                    /*if (pageobject != undefined) {
                        var fixedscale = pageobject.fixedScale;

                        if(pageobject.usevectorxml && pageobject.vectorloaded){
                            pageloaded = true;
                        }

                        bpageloaded = true;

                        markupobject.delayedloadobj.DocRef = null;
                        markupobject.delayedloadobj.xmlDoc =  null;
                        markupobject.delayedloadobj.fileVersion = null;
                        markupobject.delayedloadobj.pageobject = null;
                        

                    }
                    else if(markupobject.delayedloadobj.pageobject != null) {
                        
                        pageobject = markupobject.delayedloadobj.pageobject;
                        fixedscale = pageobject.fixedScale;

                        bpageloaded = true;

                        markupobject.delayedloadobj.DocRef = null;
                        markupobject.delayedloadobj.xmlDoc =  null;
                        markupobject.delayedloadobj.fileVersion = null;
                        markupobject.delayedloadobj.pageobject = null;


                        //DocRef.getPageObjectLoad(this.pagenumber);

                        //pageobject = DocRef.getFirstLoadedPage();
                        //fixedscale = pageobject.fixedScale;

                    }else{
                        

                        markupobject.delayedloadobj.DocRef = DocRef;
                        markupobject.delayedloadobj.xmlDoc =  xmlDoc;
                        markupobject.delayedloadobj.fileVersion = fileVersion;
                        markupobject.delayedloadobj.pageobject = null;
                        
                        DocRef.getPageObjectLoad(this.pagenumber);

                        return true;


                    }*/
                    /*if(!bpageloaded){
                        return;
                    }*/
                    //<FileVersion>
                    /*if (xmlDoc.getElementsByTagName('HaveLeader')[0] != undefined) {
                        this.bhasArrow = (parseInt(xmlDoc.getElementsByTagName('HaveLeader')[0].firstChild.nodeValue) == 1);
                    }
                    if (this.bhasArrow){
                        if (xmlDoc.getElementsByTagName('Entity')[0] != undefined)
                        var arrow = xmlDoc.getElementsByTagName("Entity")[0];
                        xmlDoc = getChildNodes(xmlDoc);
        
                    }*/
                    this.x = parseFloat(xmlDoc.getElementsByTagName('Rect')[0].attributes.getNamedItem('x').value);
                    this.y = parseFloat(xmlDoc.getElementsByTagName('Rect')[0].attributes.getNamedItem('y').value);
                    this.w = parseFloat(xmlDoc.getElementsByTagName('Rect')[0].attributes.getNamedItem('w').value);
                    this.h = parseFloat(xmlDoc.getElementsByTagName('Rect')[0].attributes.getNamedItem('h').value);
                    
                    if(isNaN(this.x)){
                        this.x = 10;
                    }

                    if(isNaN(this.y)){
                        this.y = 10;
                    }

                    if(isNaN(this.w)){
                        this.w = 100;
                    }

                    if(isNaN(this.h)){
                        this.h = 100;
                    }

                    /*if (pageobject.usepdfjs && bNewFileformat) {
                        internalscale = pageobject.curpagescale * pageobject.dscalepdf;
                    }*/

                    
                    /*if (pageobject.usepdfjs && bNewFileformat) {
                        internalscale = pageobject.curpagescale * pageobject.dscalepdf;

                        

                    }
                    if (pageobject.usevectorxml && bNewFileformat) {
                        internalscale = pageobject.curpagescale * pageobject.dscalevector;
                        
                    }*/


                    if (xmlDoc.getElementsByTagName('FixedScale')[0] != undefined) {
                        bfixedScale = (parseFloat(xmlDoc.getElementsByTagName('FixedScale')[0].firstChild.nodeValue) == 1);
                        this.bUseFixedScale = (parseFloat(xmlDoc.getElementsByTagName('FixedScale')[0].firstChild.nodeValue) == 1);
                    }
                    if (xmlDoc.getElementsByTagName('FixedScaleFactor')[0] != undefined) {
                        //bfixedScale = (parseFloat(xmlDoc.getElementsByTagName('FixedScale')[0].firstChild.nodeValue) == 1);
                        this.fixedscaleFactor = parseFloat(xmlDoc.getElementsByTagName('FixedScaleFactor')[0].firstChild.nodeValue);
                    }


                    if (xmlDoc.getElementsByTagName('Redaction')[0] != undefined) {
                        this.bRedact = (parseInt(xmlDoc.getElementsByTagName('Redaction')[0].firstChild.nodeValue) == 1);
                    }
                    if (this.h == 0 && this.w == 0) {
                        if (this.type == 8 || this.type == 1 || this.type == 2 || this.type == 0) {
                        }
                        else {
                            return false;
                        }
                    }
                    if (this.type == 8 || this.type == 1 || this.type == 2) {
                        //skip size check
                    }
                    else {

                    }
                    this.bCanHaveLink = ((this.type == 1 && this.subtype == 2) || this.type == 3 || this.type == 4 || this.type == 5 || this.type == 9 || this.type == 11 || this.type == 20);
                    

                    if (xmlDoc.getElementsByTagName('HaveLink')[0] != undefined) {
                        this.bhaveLink = (parseInt(xmlDoc.getElementsByTagName('HaveLink')[0].firstChild.nodeValue) == 1);
                    }
                    if (xmlDoc.getElementsByTagName('LinkURL')[0] != undefined) {
                        if (xmlDoc.getElementsByTagName('LinkURL')[0].firstChild != null) {
                            this.linkURL = xmlDoc.getElementsByTagName('LinkURL')[0].firstChild.nodeValue;
                        }
                    }
                    /*if (xmlDoc.getElementsByTagName('HaveLeader')[0] != undefined) {
                        this.bhasArrow = (parseInt(xmlDoc.getElementsByTagName('HaveLeader')[0].firstChild.nodeValue) == 1);
                    }
        
                    if (this.bhasArrow){
                        if (xmlDoc.getElementsByTagName('LeaderReference')[0] != undefined) {
                            if(xmlDoc.getElementsByTagName('LeaderReference')[0].firstChild != null){
                                this.markupArrowConnected = parseInt(xmlDoc.getElementsByTagName('LeaderReference')[0].firstChild.nodeValue);
                            }
            
                        }
            
                    }*/
                    if (xmlDoc.getElementsByTagName('Layer')[0] != undefined) {
                        this.layer = xmlDoc.getElementsByTagName('Layer')[0].firstChild.nodeValue;
                    }
                    else {
                        this.layer = markuplayer;
                    }
                    /*if (this.layer == 0) {
                        this.layer = markuplayer;
                    }*/
                    this.timestamp = parseInt(xmlDoc.getElementsByTagName('TimeStamp')[0].firstChild.nodeValue);
                    
                    //PageRotation
                    this.rotation = parseFloat(xmlDoc.getElementsByTagName('Rotation')[0].firstChild.nodeValue);

                    //Attributes
                    if (xmlDoc.getElementsByTagName('Extended')[0] != undefined) {
                        var extendedobjects = xmlDoc.getElementsByTagName('Extended')[0].childNodes;
                        this.customattributes = [];
                        for (var nodec = 0; nodec < extendedobjects.length; nodec++) {
                            if (extendedobjects[nodec].firstChild != null) {
                                var attrname = extendedobjects[nodec].nodeName;
                                var attrvalue = extendedobjects[nodec].firstChild.nodeValue;
                                this.AddAttribute(attrname, attrvalue);
                            }
                        }
                    }

                    //Comments
                    if (xmlDoc.getElementsByTagName('Comments')[0] != undefined) {
                        var commentobjects = xmlDoc.getElementsByTagName('Comments')[0].childNodes;
                        this.comments = [];

                        //id : markupobject.comments.length,
                        //signature : getSignature(),
                        //value: szValue


                        for (var nodec = 0; nodec < commentobjects.length; nodec++) {
                            if (commentobjects[nodec].firstChild != null) {
                                
                                
                                /*-<Comments>

                                    <id_0 Signature="bob">asfasfas</id_0>
                                
                                </Comments>*/

                                var commentid = commentobjects[nodec].nodeName;
                                var attrvalue = commentobjects[nodec].firstChild.nodeValue;
                                var sign = commentobjects[nodec].attributes.getNamedItem('Signature').value;
                                var timestamp = commentobjects[nodec].attributes.getNamedItem('Time').value;
                                if(timestamp != undefined){
                                    this.AddCommentInternal(commentid, sign, attrvalue, timestamp);
                                }else{
                                    this.AddCommentInternal(commentid, sign, attrvalue);
                                }

                                //var time = 

                                //this.x = parseFloat(xmlDoc.getElementsByTagName('Rect')[0].attributes.getNamedItem('x').value);

                                
                            }
                        }
                    }

                    if (xmlDoc.getElementsByTagName('Consolidated')[0] != undefined) {
                        this.consolidated = (parseInt(xmlDoc.getElementsByTagName('Consolidated')[0].firstChild.nodeValue) == 1);
                    }
                    //
                    if (xmlDoc.getElementsByTagName('TextRotation')[0] != undefined) {
                        this.textrotate = parseFloat(xmlDoc.getElementsByTagName('TextRotation')[0].firstChild.nodeValue);
                    }
                    if (xmlDoc.getElementsByTagName('PageRotation')[0] != undefined) {
                        this.pagerotation = parseFloat(xmlDoc.getElementsByTagName('PageRotation')[0].firstChild.nodeValue);
                    }
                    this.documentName = xmlDoc.getElementsByTagName('FileName')[0].firstChild.nodeValue;
                    this.layout = xmlDoc.getElementsByTagName('Layout')[0].firstChild.nodeValue;
                    //this.pagenumber = parseInt(xmlDoc.getElementsByTagName('View')[0].firstChild.nodeValue);
                    if (xmlDoc.getElementsByTagName('Color')[0] != undefined) {
                        this.color = xmlDoc.getElementsByTagName('Color')[0].firstChild.nodeValue;
                    }
                    if (xmlDoc.getElementsByTagName('Handle')[0] != undefined) {
                        this.markupID = parseInt(xmlDoc.getElementsByTagName('Handle')[0].firstChild.nodeValue);
                    }
                    if (xmlDoc.getElementsByTagName('UniqueID')[0] != undefined) {
                        this.uniqueID = xmlDoc.getElementsByTagName('UniqueID')[0].firstChild.nodeValue;
                        if(this.uniqueID == "0"){
                            this.uniqueID = parseInt(this.uniqueID);
                        }
                    }
                    /*if(this.bhaveLink && this.uniqueID != "0"){
                        this.locked = true;
                    }*/

                    if(bAlwaysassignUniqueID){

                        if(markupobject.uniqueID == 0){
                            getGUID(markupobject);
                        }

                        
                    }


                    if (xmlDoc.getElementsByTagName('DocOffsetX')[0] != undefined) {
                        this.docdx = parseFloat(xmlDoc.getElementsByTagName('DocOffsetX')[0].firstChild.nodeValue);
                    }
                    else{
                        

                        if(pageobject != undefined){
                            this.docdx = pageobject.getdx();
                        }else{
                            this.docdx = 0;
                        }

                    }
                    if (xmlDoc.getElementsByTagName('DocOffsetY')[0] != undefined) {
                        this.docdy = parseFloat(xmlDoc.getElementsByTagName('DocOffsetY')[0].firstChild.nodeValue);
                    }
                    else{

                        if(pageobject != undefined){
                            this.docdy = pageobject.getdy();
                        }else{
                            this.docdy = 0;
                        }



                    }
                    if (xmlDoc.getElementsByTagName('DocScale')[0] != undefined) {
                        this.docdscale = parseFloat(xmlDoc.getElementsByTagName('DocScale')[0].firstChild.nodeValue);
                    }
                    else {

                        if(pageobject != undefined){
                            this.docdscale = pageobject.getdscale();
                        }else{
                            this.docdscale = 1;
                        }

                    }
                    if (xmlDoc.getElementsByTagName('Transparency')[0] != undefined) {
                        this.transparency = parseInt(xmlDoc.getElementsByTagName('Transparency')[0].firstChild.nodeValue);
                    }
                    if (this.type == 3 && this.subtype == 3) {
                        this.transparency = 30;
                        this.fillcolor = setTransp(this.fillcolor, this.transparency);
                    }
                    if (xmlDoc.getElementsByTagName('LineColor')[0] != undefined) {
                        this.strokecolor = xmlDoc.getElementsByTagName('LineColor')[0].firstChild.nodeValue;

                        if (colorisHex(this.strokecolor)){
                            this.strokecolor = convertHex(this.strokecolor, 100);
                        }else{
                            this.strokecolor = setTransp(this.strokecolor, 100);
                        }

                    }
                    if (xmlDoc.getElementsByTagName('TextColor')[0] != undefined) {

                        this.textcolor = xmlDoc.getElementsByTagName('TextColor')[0].firstChild.nodeValue;


                        if (colorisHex(this.textcolor)){
                            this.textcolor = convertHex(this.textcolor, 100);
                        }else{
                            this.textcolor = setTransp(this.textcolor, 100);
                        }

                        //setTransp(this.textcolor, 100);
                    }
                    
                    this.scaling = parseFloat(xmlDoc.getElementsByTagName('Scaling')[0].firstChild.nodeValue);

                    if(xmlDoc.getElementsByTagName('LoadScaling')[0] != undefined){
                        this.loadscaling = parseFloat(xmlDoc.getElementsByTagName('LoadScaling')[0].firstChild.nodeValue);
                    }else{
                        this.loadscaling = this.scaling;
                    }

                    //createAndAppendElement(entityNode, "LoadScaling", markupobj.scaling);

                    var bfinite = isFinite(this.scaling);

                    if (!bfinite) {
                        this.scaling = 1;
                    }

                    if(isNaN(this.scaling)){
                        this.scaling = 1;
                    }




                    if (xmlDoc.getElementsByTagName('FillColor')[0] != undefined) {

                        this.fillcolor = xmlDoc.getElementsByTagName('FillColor')[0].firstChild.nodeValue;

                        if (colorisHex(this.textcolor)){
                            this.fillcolor = convertHex(this.fillcolor, this.transparency);
                        }else {
                            this.fillcolor = setTransp(this.fillcolor, this.transparency);
                        }

                        
                        
                    }
                    if (xmlDoc.getElementsByTagName('FontName')[0] != undefined) {
                        this.fontname = xmlDoc.getElementsByTagName('FontName')[0].firstChild.nodeValue;
                    }
                    if (xmlDoc.getElementsByTagName('FontHeight')[0] != undefined) {
                        this.textheight = xmlDoc.getElementsByTagName('FontHeight')[0].firstChild.nodeValue;
                    }
                    if (this.fontname != undefined || this.textheight != undefined) {
                        this.font = new FontObject(this.fontname, this.textheight, false, false);
                    }
                    if (xmlDoc.getElementsByTagName('Font')[0] != undefined) {

                        //change to set when page is loaded not from page object.

                        var facename = xmlDoc.getElementsByTagName('Font')[0].attributes.getNamedItem('facename').value;
                        var height = parseFloat(xmlDoc.getElementsByTagName('Font')[0].attributes.getNamedItem('height').value);
                        var bold = (parseInt(xmlDoc.getElementsByTagName('Font')[0].attributes.getNamedItem('bold').value) == 1);
                        var italic = (parseInt(xmlDoc.getElementsByTagName('Font')[0].attributes.getNamedItem('italic').value) == 1);

                        this.font = new FontObject(facename, height, bold, italic);
                        this.textheight = height;


                        /* if (pageobject.usepdfjs || pageobject.usevectorxml) {
                            if (this.type != 12) {
                        
                                //change to set when page is loaded not from page object.

                                if (bfixedScale && bNewFileformat) {
                                    //height = Math.round(height * fixedscale);
                                    height = Math.round(height * 1);
                                }
                                else {
                                    //height = Math.round(height * internalscale);
                                    height = Math.round(height * 1);
                                }
                        
                            }
                        }
                        else {
                            if (this.type != 12) {

                                //change to set when page is loaded not from page object.

                                if (bfixedScale) {
                                    height = Math.round((height * fixedscale) / this.scaling);
                                    //fontNode.setAttribute("height", (DocObj.markuplist[curmarkup].font.height / fixedscale) * imagescale);
                                }
                                else {
                                    height = Math.round(height);
                                    //fontNode.setAttribute("height", DocObj.markuplist[curmarkup].font.height);    
                                }
                            }
                            else {
                                //fontNode.setAttribute("height", DocObj.markuplist[curmarkup].font.height);
                                height = Math.round(height);
                            }
                        } */
                        //this.font = new FontObject(facename, height, bold, italic);
                        //var bispath = (this.type == 1 && this.subtype == 3);
                        //this.font = new FontObject(facename, (this.type == 7 || this.type == 8 || bispath)? nLabelTextsize : height, bold, italic);
                        
                        //this.font = new FontObject(facename, height, bold, italic);
                        //this.textheight = parseFloat(height);
                    }
                    /*if (pageobject.usepdfjs || pageobject.usevectorxml) {
                        this.textheight *= 1; //changed to recreate correct textheight
                    }*/
                    this.signature = xmlDoc.getElementsByTagName('Signature')[0].firstChild.nodeValue;
                    if (bNewFileformat) {
                        /*if (pageobject.usepdfjs || pageobject.usevectorxml) {

                            if(bUseMarkupScaling){

                            }else{
                                this.scaling = 1;
                            }
                            
                        }*/
                    }
                    //this.scaling = DocObj.getCurrentpage().dscale;
                    this.xoffset = parseFloat(xmlDoc.getElementsByTagName('Xoffset')[0].firstChild.nodeValue);
                    this.yoffset = parseFloat(xmlDoc.getElementsByTagName('Yoffset')[0].firstChild.nodeValue);


                    if(isNaN(this.xoffset)){
                        this.xoffset = 0;
                    }

                    if(isNaN(this.yoffset)){
                        this.yoffset = 0;
                    }

                    /* if (bNewFileformat && (pageobject.usepdfjs || pageobject.usevectorxml)) {
                        
                        this.x = (this.x + this.xoffset) * internalscale;
                        this.y = (this.y + this.yoffset) * internalscale;
                        var bCircle = (this.type == 4 && this.subtype == 1 && bNewestFileformat);
                        if (this.type == 0 || this.type == 1 || this.type == 2 || this.type == 6 || this.type == 7 || this.type == 8 || bCircle) {
                            this.w = (this.w + this.xoffset) * internalscale;
                            this.h = (this.h + this.yoffset) * internalscale;
                        }
                        else {
                            if (this.type == 10 && bfixedScale) {
                                this.w *= fixedscale;
                                this.h *= fixedscale;
                            }
                            else {
                                this.w *= internalscale;
                                this.h *= internalscale;
                            }
                        }
                    } */
                    if (this.type == 7) {
                        if (xmlDoc.getElementsByTagName('LeaderLineOffset')[0] != undefined) {
                            this.leaderoffset = xmlDoc.getElementsByTagName('LeaderLineOffset')[0].firstChild.nodeValue;
                            /*if (bNewFileformat && (pageobject.usepdfjs || pageobject.usevectorxml)) {
                                this.leaderoffset *= 1;
                            }*/
                        }
                    }
                    if (this.type == 8) {
                        if (xmlDoc.getElementsByTagName('LabelOffsetX')[0] != undefined) {
                            this.labeloffsetx = xmlDoc.getElementsByTagName('LabelOffsetX')[0].firstChild.nodeValue;
                            /*if (bNewFileformat && (pageobject.usepdfjs || pageobject.usevectorxml)) {
                                this.labeloffsetx *= 1;
                            }*/
                        }
                        if (xmlDoc.getElementsByTagName('LabelOffsetY')[0] != undefined) {
                            this.labeloffsety = xmlDoc.getElementsByTagName('LabelOffsetY')[0].firstChild.nodeValue;
                            /*if (bNewFileformat && (pageobject.usepdfjs || pageobject.usevectorxml)) {
                                this.labeloffsety *= 1;
                            }*/
                        }
                        if (xmlDoc.getElementsByTagName('LabelColor')[0] != undefined) {
                            this.labelfillcolor = xmlDoc.getElementsByTagName('LabelColor')[0].firstChild.nodeValue;
                        }
                    }
                    this.linewidth = parseFloat(xmlDoc.getElementsByTagName('LineWidth')[0].firstChild.nodeValue);

                    if (xmlDoc.getElementsByTagName('LineWidthSegment')[0] != undefined) {

                        //LineWidthSegment
                        
                        this.linewidthSegment = parseFloat(xmlDoc.getElementsByTagName('LineWidthSegment')[0].firstChild.nodeValue);

                    }else{
                        this.linewidthSegment = linewidthvalueSegment;
                    }
                    
                    
                    /*if(this.type == 0 && this.subtype == 1){
                        this.linewidth /= 10;
                    }*/
                    /*if (bNewFileformat && (pageobject.usepdfjs || pageobject.usevectorxml)) {
                        //change to set when page is loaded not from page object.

                        if (bfixedScale) {
                            this.linewidth *= 1;
                        }
                        else {
                            this.linewidth *= 1;
                        }
                        this.arrowlength = nArrowSize + this.linewidth;
                    }*/
                    if (xmlDoc.getElementsByTagName('ArrowSize')[0] != undefined) {
                        this.arrowlength = parseInt(xmlDoc.getElementsByTagName('ArrowSize')[0].firstChild.nodeValue);
                        /*if (bNewFileformat && (pageobject.usepdfjs || pageobject.usevectorxml)) {
                            this.arrowlength = arrowsize + this.linewidth;
                        }*/

                        //this.arrowlength = arrowsize + this.linewidth;
                    }
                    if (xmlDoc.getElementsByTagName('LineStyle')[0] != undefined) {
                        this.linestyle = parseFloat(xmlDoc.getElementsByTagName('LineStyle')[0].firstChild.nodeValue);
                    }

                    var btext = (this.type == 9 || this.type == 10 || this.type == 12);

                    if(btext){

                        this.text = xmlDoc.getElementsByTagName('Text')[0].firstChild.nodeValue;
                        if (this.text == ".") {
                            this.text = "";
                        }
                    }


                    if (xmlDoc.getElementsByTagName('Status')[0] != undefined) {
                        this.status = xmlDoc.getElementsByTagName('Status')[0].firstChild.nodeValue;
                    }
                    


                    if (xmlDoc.getElementsByTagName('SmallText')[0] != undefined) {
                        this.smalltext = xmlDoc.getElementsByTagName('SmallText')[0].firstChild.nodeValue;
                        if (this.smalltext == ".") {
                            this.smalltext = "";
                        }
                    }
                    if (xmlDoc.getElementsByTagName('DimText')[0] != undefined && xmlDoc.getElementsByTagName('DimText')[0].firstChild != null) {
                        this.dimtext = xmlDoc.getElementsByTagName('DimText')[0].firstChild.nodeValue;
                        if (this.dimtext == ".") {
                            this.dimtext = "";
                        }
                    }
                    else {
                        this.dimtext = "";
                    }
                    if (xmlDoc.getElementsByTagName('CustomLabelText')[0] != undefined && xmlDoc.getElementsByTagName('CustomLabelText')[0].firstChild != null) {
                        this.customlabeltext = xmlDoc.getElementsByTagName('CustomLabelText')[0].firstChild.nodeValue;
                        if (this.customlabeltext == ".") {
                            this.customlabeltext = "";
                        }
                    }
                    else {
                        this.customlabeltext = "";
                    }
                    if (xmlDoc.getElementsByTagName('CustomLabelOn')[0] != undefined) {
                        this.busecustomlabel = (parseInt(xmlDoc.getElementsByTagName('CustomLabelOn')[0].firstChild.nodeValue) == 1);
                    }
                    if (this.type == 13) {

                        this.innerRadius = parseFloat(xmlDoc.getElementsByTagName('innerRadius')[0].firstChild.nodeValue);
                        this.outerRadius = parseFloat(xmlDoc.getElementsByTagName('outerRadius')[0].firstChild.nodeValue);

                        //if (bNewFileformat && (pageobject.usepdfjs || pageobject.usevectorxml)) {

                            //change to set when page is loaded not from page object.
                            //this.innerRadius *= internalscale;
                            //this.outerRadius *= internalscale;

                            /*if (bfixedScale) {
                                //this.linewidth *= fixedscale;
    
                                this.innerRadius *= fixedscale;
                                this.outerRadius *= fixedscale;
    
                            }
                            else {
                                
                                this.innerRadius *= internalscale;
                                this.outerRadius *= internalscale;
    
                                //this.linewidth *= internalscale;
                            }*/
    
                        //}



                        this.removepoints();
                        if (xmlDoc.getElementsByTagName('Point').length != 0) {
                            var PointList = xmlDoc.getElementsByTagName('Point');
                            for (var i = 0; i < PointList.length; i++) {
                                var x = parseFloat(PointList[i].attributes.getNamedItem('x').value);
                                var y = parseFloat(PointList[i].attributes.getNamedItem('y').value);
                                /*if (bNewFileformat && (pageobject.usepdfjs || pageobject.usevectorxml)) {
                                    x = (x + this.xoffset) * internalscale;
                                    y = (y + this.yoffset) * internalscale;
                                }*/
                                markupobject.addcountLoad({x : x, y : y});
                                
                            }
                            //this.numpoints = PointList.length;
                            //markupobject.findrectangle();
                            /*if (this.h == 0 && this.w == 0) {
        
                            }*/
                        }
                        else {
                            return false;
                        }

                    }

                    if (this.type == 14) {

                        this.startX = parseFloat(xmlDoc.getElementsByTagName('startX')[0].firstChild.nodeValue);
                        this.startY = parseFloat(xmlDoc.getElementsByTagName('startY')[0].firstChild.nodeValue);

                        this.secondX = parseFloat(xmlDoc.getElementsByTagName('secondX')[0].firstChild.nodeValue);
                        this.secondY = parseFloat(xmlDoc.getElementsByTagName('secondY')[0].firstChild.nodeValue);


                        this.endX = parseFloat(xmlDoc.getElementsByTagName('endX')[0].firstChild.nodeValue);
                        this.endY = parseFloat(xmlDoc.getElementsByTagName('endY')[0].firstChild.nodeValue);

                        //this.startX = null;
                        //this.startY = null;
                        //this.secondX = null;
                        //this.secondY = null;
                        //this.endX = null;
                        //this.endY = null;
        


                    }

                    var bsegment = (this.type == 1 && this.subtype == 6);
                    var bpoly = (this.type == 1 || this.type == 2 || this.type == 8);

                    if (bpoly && !bsegment) {
                        this.removepoints();
                        if (xmlDoc.getElementsByTagName('Point').length != 0) {
                            var PointList = xmlDoc.getElementsByTagName('Point');
                            for (i = 0; i < PointList.length; i++) {
                                x = parseFloat(PointList[i].attributes.getNamedItem('x').value);
                                y = parseFloat(PointList[i].attributes.getNamedItem('y').value);
                                /*if (bNewFileformat && (pageobject.usepdfjs || pageobject.usevectorxml)) {
                                    x = (x + this.xoffset) * internalscale;
                                    y = (y + this.yoffset) * internalscale;
                                }*/
                                markupobject.addpoint(x, y);
                            }
                            //this.numpoints = PointList.length;
                            markupobject.findrectangle();
                            /*if (this.h == 0 && this.w == 0) {
        
                            }*/
                        }
                        else {
                            return false;
                        }
                    }
                    if (this.type == 0 || bsegment) {
                        this.removepoints();
                        if (xmlDoc.getElementsByTagName('Line').length != 0) {
                            var pointLines = xmlDoc.getElementsByTagName('Line');
                            for (i = 0; i < pointLines.length; i++) {
                                var linePoints = pointLines[i].getElementsByTagName('Point');
                                for (var j = 0; j < linePoints.length; j++) {
                                    x = parseFloat(linePoints[j].attributes.getNamedItem('x').value);
                                    y = parseFloat(linePoints[j].attributes.getNamedItem('y').value);
                                    /*if (bNewFileformat && (pageobject.usepdfjs || pageobject.usevectorxml)) {
                                        x = (x + this.xoffset) * internalscale;
                                        y = (y + this.yoffset) * internalscale;
                                    }*/
                                    var tempPoint = new point(x, y);
                                    this.points.push(tempPoint);
                                    //this.pointlist[i][j].y = y;
                                }
                                this.pointlist.push(this.points);
                                this.points = [];
                            }
                            //markupobject.findrectangle();
                        }
                        else {
                            return false;
                        }
                    }
                    /*if (bNewFileformat && (pageobject.usepdfjs || pageobject.usevectorxml)) {
                        this.xoffset *= internalscale;
                        this.yoffset *= internalscale;
                    }*/
                    if (this.type == 12) {
                        if (this.transparency == undefined) {
                            this.transparency = 30;
                            this.fillcolor = setTransp(this.fillcolor, this.transparency);
                        }
                        if (this.subtype == 100) {
                            this.text = xmlDoc.getElementsByTagName('Text')[0].firstChild.nodeValue;
                        }
                        //this.textheight = this.w * 0.16;
                        //this.font.setHeight(this.textheight);
                        this.font.setFontname("Times New Roman");
                        this.font.setBold(true);
                        //ctx.font = "bold " + textstampscaled + "pt " + "Times New Roman";
                            /*this.stampsmalltheight = (this.w * 0.08) - 4;
                            var combinedtextheight = this.textheight + this.stampsmalltheight + ((this.h / 10) * 2);
                            if (combinedtextheight > this.h) {
                                this.textheight = (this.h / 2) - ((this.h / 10) * 2);
                                this.stampsmalltheight = (this.textheight / 2) - 4;
                            }*/
                     }
                    if (this.type == 11 || this.type == 20) {


                        var createimage = new Image();

                        if (xmlDoc.getElementsByTagName('SvgContent')[0] != undefined && xmlDoc.getElementsByTagName('SvgContent')[0].firstChild != null) {
                            var svgContent = xmlDoc.getElementsByTagName('SvgContent')[0].firstChild.nodeValue;
                            markupobject.svgContent = svgContent;
                            markupobject.markupsvgload(svgContent, createimage);
                        }else if (xmlDoc.getElementsByTagName('Image')[0] != undefined && xmlDoc.getElementsByTagName('Image')[0].firstChild != null) {
                            if (xmlDoc.getElementsByTagName('Image')[0].firstChild.nodeValue != "") {
                                if (xmlDoc.getElementsByTagName('Image')[0].firstChild.nodeValue == "\n") {
                                    var cdata = xmlDoc.getElementsByTagName('Image')[0].firstChild.nextSibling.nodeValue;
                                }
                                else {
                                    cdata = xmlDoc.getElementsByTagName('Image')[0].firstChild.nodeValue;
                                    cdata = cdata.slice(9, cdata.length - 3);
                                }
                                //cdata = cdata.slice(cdata.length - 3);
                                //"<![CDATA[" + dataURL + "]]>";
                                //createimage.setAttribute('src',cdata);
                                markupobject.markupcdataload(createimage, cdata);
                                //createimage.addEventListener('load', markupobject.markupcdataload, false);
                                //createimage.src = cdata;
                                //this.image = createimage;
                                //markupobject.imageloaded = true;
                            }
                        }
                        else if (xmlDoc.getElementsByTagName('Imagehref')[0] != undefined && xmlDoc.getElementsByTagName('Imagehref')[0].firstChild != null) {
                            var imageurl = xmlDoc.getElementsByTagName('Imagehref')[0].firstChild.nodeValue;
                            markupobject.imagehref = imageurl;
                            markupobject.markupimageload(imageurl, createimage);
                            //createimage.src = imageurl;
                            //this.image = createimage;
                            //this.dimtext = "";
                        }
                        else {
                            this.type = 3;
                            this.subtype = 0;
                            this.alternative = 0;
                        }
                        //create image either from separate image or embedded data.
                        //this.image = createimage;
                    }


                    //this.stampsmalltheight = this.textheight / 2;
                    //<Points>
                    /*if (xmlDoc.getElementsByTagName('DimText')[0].firstChild.length != 0){
                     this.text = xmlDoc.getElementsByTagName('DimText')[0].firstChild.nodeValue;
                     }*/
                    /*this.x = xmlDoc.getElementsByTagName('Rect')[0].attributes['x'].value;
                     this.y = xmlDoc.getElementsByTagName('Rect')[0].attributes['y'].value;
                     this.w = xmlDoc.getElementsByTagName('Rect')[0].attributes['w'].value;
                     this.h = xmlDoc.getElementsByTagName('Rect')[0].attributes['h'].value;*/
                    /*
                     Add function for getting points for polylines pencil etc.
                     */
                    return true;
                };
                
                this.markupcdataload = function (image, cdata) {
                    image.onload = function (ev) {
                        markupobject.image = image;
                        markupobject.imageloaded = true;
                        drawmarkupAll(cntximg);
                        DrawMarkupSelected(context);
                    };
                    image.src = cdata;
                };

                this.markupcdataset = function(image,cdata, resolve){

                    image.onload = function (ev) {
                        markupobject.image = image;
                        markupobject.imageloaded = true;
                        resolve(markupobject.image);

                    };
                    image.src = cdata;

                };
                this.markupimageblob = function(blob, image){
                    
                    const loadpromise = new Promise(function(resolve,reject){


                        var urlCreator = window.URL || window.webkitURL;
                    
                        var imageUrl = urlCreator.createObjectURL(blob);
    
                        //var url = URL.createObjectURL(blob);
    
                        if (imageUrl) {
                            markupobject.markupcdataset(image, imageUrl, resolve);
                        }
    
                        /*RxCore_GUI_CloseDocInternal.connect(function(index, bclosed){
                            
                            if(bclosed){
                                resolve(index);
                            }else{
                                //reject(index);
                            }
                            
            
                        });*/
            
                        //internalClosedoc(index);
            
            
                    });
            
                    return loadpromise;


                };                


                this.markupimageload = function (imageURL, image) {
                    
                    sendHttpRequest('GET', imageURL,'blob').then(function (response){
                    
                        var url = URL.createObjectURL(response);
                        if (url) {
                            markupobject.markupcdataload(image, url);
                        }
                    
                    }).catch(function(error){
           
                        if(error.status){
                            console.log("markupimageload", error.status);
                        }else{
                            console.log("markupimageload", error);
                        }
                        
                    });
                    
                    
                };
                this.markupsvgload = function (svgString, image) {
                    var base64Svg = btoa(svgString);
                    var svgImage = `data:image/svg+xml;base64,${base64Svg}`;

                    if(RxCore_GUI_SVGLoad != undefined){
                        RxCore_GUI_SVGLoad.loadComplete(svgString, markupobject);
                    }
                    

                    this.markupcdataload(image, svgImage);
                };
                this.GetLinestyle = function (linestyle, ctx, scale) {
                    switch (linestyle) {
                        case 0:
                            break;
                        case 1:
                            ctx.setLineDash([10 * scale]);
                            break;
                        case 2:
                            ctx.setLineDash([2 * scale, 5 * scale]);
                            break;
                        case 3:
                            ctx.setLineDash([10 * scale, 5 * scale, 2 * scale, 5 * scale, 2 * scale, 5 * scale]);
                            break;
                        case 4:
                            ctx.setLineDash([10 * scale, 5 * scale, 2 * scale, 5 * scale]);
                            break;
                        case 5:
                            ctx.setLineDash([0, 15 * scale, 15 * scale, 0]);
                            break;
                        case 6:
                            ctx.setLineDash([15 * scale, 15 * scale]);
                            break;
                    }
                };
                this.GetDateTime = function (time) {
                    var dDate = new Date(markupobject.timestamp);
                    var day = dDate.getDate();
                    var month = dDate.getMonth();
                    month = month + 1;
                    var year = dDate.getFullYear();
                    //var hours = dDate.getHours();
                    var hours = (dDate.getHours() < 10 ? '0' : '') + dDate.getHours();
                    //var minutes = dDate.getMinutes();
                    var minutes = (dDate.getMinutes() < 10 ? '0' : '') + dDate.getMinutes();
                    //var seconds = dDate.getSeconds();
                    var seconds = (dDate.getSeconds() < 10 ? '0' : '') + dDate.getSeconds();
                    if (time) {
                        return day + "." + month + "." + year + " " + hours + ":" + minutes + ":" + seconds;
                    }
                    else {
                        return day + "." + month + "." + year;
                    }
                };
                this.getdiag = function (width, height) {
                    var dimwsq = Math.pow(width, 2);
                    var dimhsq = Math.pow(height, 2);
                    var dimdiag = Math.sqrt((dimwsq + dimhsq));
                    return dimdiag;
                };
                this.onPanScale = function () {
                    //move operation to markup object.
                    /*var curscale = DocObj.getCurrentpage().getdscale();
                    var pagedx = DocObj.getCurrentpage().getdx();
                    var pagedy = DocObj.getCurrentpage().getdy();
        
                    var scalediff = (curscale / arrowmarkupobj.scaling);
        
                    var changex = ((arrowmarkupobj.w - pagedx) / scalediff) + arrowmarkupobj.xoffset;
                    var changey = ((arrowmarkupobj.h - pagedy) / scalediff) + arrowmarkupobj.yoffset;
        
                    arrowmarkupobj.w = changex;
                    arrowmarkupobj.h = changey;*/
                };
                this.setDimDrawme = function () {
                    if (this.type == 6 || this.type == 7 || this.type == 3) {
                        var coords = {
                            x: this.x,
                            y: this.y,
                            w: this.w,
                            h: this.h,
                        };
                    }
                    else if (this.type == 1 || this.type == 8) {
                        var point = this.getlastpoint();
                        if (!point) {
                            return { x: 0, y: 0, w: 0, h: 0 };
                        }
                        coords = {
                            x: point.x,
                            y: point.y,
                            w: point.x,
                            h: point.y,
                        };
                    }
                    else if(this.type == 13){

                        var point = this.getlastcount();
                        if (!point) {
                            return { x: 0, y: 0, w: 0, h: 0 };
                        }
                        
                        //console.log(point);
                        coords = {
                            x: point.x,
                            y: point.y,
                            w: point.x,
                            h: point.y,
                        };

                    }else if(this.type == 14){
                        var coords = {
                            x: this.startX,
                            y: this.startY,
                            x2: this.secondX,
                            y2: this.secondY,
                            w: this.endX,
                            h: this.endY,
                        };
   
                    }

                    var rotation = DocObj.getCurrentpage().getpageRotation();

                    var scalefactor = getCurrentPageScale();

                    //var fixedscale = DocObj.getCurrentpage().fixedScale;
                    //markupobject.fixedscaleFactor =  scalefactor / DocObj.getCurrentpage().fixedScale;
                    scalefactor /= markupobject.scaling;
                    var pagedx = 0;
                    var pagedy = 0;


                    var offset = getCurrentPageOffset(markupobject.pagenumber);
                    pagedx = offset.x;
                    pagedy = offset.y;

                    var pagecorner = getPageRotCorner({ x: pagedx, y: pagedy });
                    var rotoffset = getPageRotCorner({ x: this.xoffset, y: this.yoffset });

                    //rotation = 0;

                    switch (rotation) {
                        case 0:
                            if (this.type == 6 || this.type == 7) {

                                coords.x = ((this.x - pagedx) / scalefactor) + this.xoffset;
                                coords.y = ((this.y - pagedy) / scalefactor) + this.yoffset;
                                coords.w = ((this.w - pagedx) / scalefactor) + this.xoffset;
                                coords.h = ((this.h - pagedy) / scalefactor) + this.yoffset;

                            }
                            else if (this.type == 1 || this.type == 8 || this.type == 13) {
                                coords.w = ((point.x - pagedx) / scalefactor) + this.xoffset;
                                coords.h = ((point.y - pagedy) / scalefactor) + this.yoffset;
                            }else if(this.type == 3){

                                coords.x = ((this.x - pagedx) / scalefactor) + this.xoffset;
                                coords.y = ((this.y - pagedy) / scalefactor) + this.yoffset;
                                coords.w = (((this.x + this.w) - pagedx) / scalefactor) + this.xoffset;
                                coords.h = (((this.y + this.h) - pagedy) / scalefactor) + this.yoffset;

                                coords.w = (coords.w - this.x);
                                coords.h = (coords.h - this.y);


                                //dimensionmarkupobj.w = dimensionmarkupobj.w - dimensionmarkupobj.x;
                                //dimensionmarkupobj.h = dimensionmarkupobj.h - dimensionmarkupobj.y;



                                //coords.w = coords.x + coords.w;
                                //coords.h = coords.y + coords.h;
                            }else if (this.type == 14) {

                                if(this.startX != null && this.startY != null ){
                                    coords.x = ((this.startX - pagedx) / scalefactor) + this.xoffset;
                                    coords.y = ((this.startY - pagedy) / scalefactor) + this.yoffset;
                                }

                                if(this.secondX != null && this.secondY != null ){
                                    coords.x1 = ((this.secondX - pagedx) / scalefactor) + this.xoffset;
                                    coords.y2 = ((this.secondY - pagedy) / scalefactor) + this.yoffset;
                                }

                                if(this.endX != null && this.endY != null ){
                                    coords.w = ((this.endX - pagedx) / scalefactor) + this.xoffset;
                                    coords.h = ((this.endY - pagedy) / scalefactor) + this.yoffset;
    
                                }



                            }
                            break;
                        case 90:
                            if (this.type == 6 || this.type == 7) {
                                coords.x = ((this.x - pagecorner.x) / scalefactor) + rotoffset.x;
                                coords.y = ((this.y - pagecorner.y) / scalefactor) + rotoffset.y;
                                
                                coords.w = ((this.w - pagecorner.x) / scalefactor) + rotoffset.x;
                                coords.h = ((this.h - pagecorner.y) / scalefactor) + rotoffset.y;
                            }
                            else if (this.type == 1 || this.type == 8 || this.type == 13) {
                                coords.w = ((point.x - pagecorner.x) / scalefactor) + rotoffset.x;
                                coords.h = ((point.y - pagecorner.y) / scalefactor) + rotoffset.y;
                            }else if(this.type == 3){

                                coords.x = ((this.x - pagecorner.x) / scalefactor) + rotoffset.x;
                                coords.y = ((this.y - pagecorner.y) / scalefactor) + rotoffset.y;
                                coords.w = (((this.x + this.w) - pagecorner.x) / scalefactor) + rotoffset.x;
                                coords.h = (((this.y + this.h) - pagecorner.y) / scalefactor) + rotoffset.y;


                                coords.w = (coords.w - this.x);
                                coords.h = (coords.h - this.y);

                                //coords.w = this.w;
                                //coords.h = this.h;


                            }else if (this.type == 14) {



                                if(this.startX != null && this.startY != null ){
                                    coords.x = ((this.startX - pagecorner.x) / scalefactor) + rotoffset.x;
                                    coords.y = ((this.starty - pagecorner.y) / scalefactor) + rotoffset.y;
                                    }

                                if(this.secondX != null && this.secondY != null ){
                                    coords.x1 = ((this.secondX - pagecorner.x) / scalefactor) + rotoffset.x;
                                    coords.y2 = ((this.secondY - pagecorner.y) / scalefactor) + rotoffset.y;
                                    }

                                if(this.endX != null && this.endY != null ){
                                    coords.w = ((this.endX - pagecorner.x) / scalefactor) + rotoffset.x;
                                    coords.h = ((this.endY - pagecorner.y) / scalefactor) + rotoffset.y;
        
                                }

                                



                            }
                            break;
                        case 180:
                            if (this.type == 6 || this.type == 7) {
                                coords.x = ((this.x - pagecorner.x) / scalefactor) + rotoffset.x;
                                coords.y = ((this.y - pagecorner.y) / scalefactor) + rotoffset.y;
                                coords.w = ((this.w - pagecorner.x) / scalefactor) + rotoffset.x;
                                coords.h = ((this.h - pagecorner.y) / scalefactor) + rotoffset.y;
                            }
                            else if (this.type == 1 || this.type == 8 || this.type == 13) {
                                coords.w = ((point.x - pagecorner.x) / scalefactor) + rotoffset.x;
                                coords.h = ((point.y - pagecorner.y) / scalefactor) + rotoffset.y;
                            }else if(this.type == 3){
                                coords.x = ((this.x - pagecorner.x) / scalefactor) + rotoffset.x;
                                coords.y = ((this.y - pagecorner.y) / scalefactor) + rotoffset.y;

                                coords.w = (((this.x + this.w) - pagecorner.x) / scalefactor) + rotoffset.x;
                                coords.h = (((this.y + this.h) - pagecorner.y) / scalefactor) + rotoffset.y;


                                coords.w = (coords.w - this.x);
                                coords.h = (coords.h - this.y);


                            }else if (this.type == 14) {

                                
                                if(this.startX != null && this.startY != null ){
                                    coords.x = ((this.startX - pagecorner.x) / scalefactor) + rotoffset.x;
                                    coords.y = ((this.starty - pagecorner.y) / scalefactor) + rotoffset.y;
                                    }

                                if(this.secondX != null && this.secondY != null ){
                                    coords.x1 = ((this.secondX - pagecorner.x) / scalefactor) + rotoffset.x;
                                    coords.y2 = ((this.secondY - pagecorner.y) / scalefactor) + rotoffset.y;
                                    }

                                if(this.endX != null && this.endY != null ){
                                    coords.w = ((this.endX - pagecorner.x) / scalefactor) + rotoffset.x;
                                    coords.h = ((this.endY - pagecorner.y) / scalefactor) + rotoffset.y;
        
                                }



                            }
                            break;
                        case 270:
                            if (this.type == 6 || this.type == 7) {
                                coords.x = ((this.x - pagecorner.x) / scalefactor) + rotoffset.x;
                                coords.y = ((this.y - pagecorner.y) / scalefactor) + rotoffset.y;
                                coords.w = ((this.w - pagecorner.x) / scalefactor) + rotoffset.x;
                                coords.h = ((this.h - pagecorner.y) / scalefactor) + rotoffset.y;
                            }
                            else if (this.type == 1 || this.type == 8 || this.type == 13) {
                                coords.w = ((point.x - pagecorner.x) / scalefactor) + rotoffset.x;
                                coords.h = ((point.y - pagecorner.y) / scalefactor) + rotoffset.y;
                            }else if(this.type == 3){

                                coords.x = ((this.x - pagecorner.x) / scalefactor) + rotoffset.x;
                                coords.y = ((this.y - pagecorner.y) / scalefactor) + rotoffset.y;

                                coords.w = (((this.x + this.w) - pagecorner.x) / scalefactor) + rotoffset.x;
                                coords.h = (((this.y + this.h) - pagecorner.y) / scalefactor) + rotoffset.y;

                                coords.w = (coords.w - this.x);
                                coords.h = (coords.h - this.y);


                            }else if (this.type == 14) {

                                if(this.startX != null && this.startY != null ){
                                    coords.x = ((this.startX - pagecorner.x) / scalefactor) + rotoffset.x;
                                    coords.y = ((this.starty - pagecorner.y) / scalefactor) + rotoffset.y;
                                    }

                                if(this.secondX != null && this.secondY != null ){
                                    coords.x1 = ((this.secondX - pagecorner.x) / scalefactor) + rotoffset.x;
                                    coords.y2 = ((this.secondY - pagecorner.y) / scalefactor) + rotoffset.y;
                                    }

                                if(this.endX != null && this.endY != null ){
                                    coords.w = ((this.endX - pagecorner.x) / scalefactor) + rotoffset.x;
                                    coords.h = ((this.endY - pagecorner.y) / scalefactor) + rotoffset.y;
        
                                }



                            }
                            break;
                    }
                    var rotRad360 = 360 * (Math.PI / 180);
                    var abswidthandheight = true;
                    //coords.w = this.w * scalefactor;
                    //coords.h = this.h * scalefactor;
                    /*switch (this.type) {
                        case 6: //line arrow
        
                            coords.x += pagedx;
                            coords.y += pagedy;
        
                            coords.w += pagedx;
                            coords.h += pagedy;
        
                            //this.w = coords.w;
                            //this.h = coords.h;
        
                            //abswidthandheight = true;
        
        
                            break;
                        case 7: //dimension line
        
                            coords.x += pagedx;
                            coords.y += pagedy;
        
                            //coords.w = (coords.w - this.xoffset) * scalefactor;
                            //coords.h = (coords.h - this.yoffset) * scalefactor;
        
                            coords.w += pagedx;
                            coords.h += pagedy;
                            //this.w = coords.w;
                            //this.h = coords.h;
        
                            //abswidthandheight = true;
        
                            break;
                    }*/
                    /*var CanvRotRad = (360 - DocObj.getCurrentpage().getpageRotation()) * (Math.PI / 180);
                    var centercanvX = (canvasowidth / 2);
                    var centercanvY = (canvasoheight / 2);
        
                    if (DocObj.getCurrentpage().getpageRotation() != 0){
        
                        var transpoint = markupobject.setinitRotatedMarkupPoint(centercanvX, centercanvY, coords.x, coords.y, CanvRotRad);
                        coords.x = transpoint.x;
                        coords.y = transpoint.y;
            
                    }*/
                    return coords;
                };
                this.SetDim3D = function(scalefactor, rotation, pagedx, pagedy){

                    //pagedx = canvasowidth * 0.5;
                    //pagedy = canvasoheight * 0.5;

                    //scalefactor = canvasoheight / this.pageheight;

                    //dimensionmarkupobj.pageheight = canvasoheight;

                    var abswidthandheight = true;

                    this.xscaled = (this.x - this.xoffset) * scalefactor;
                    this.yscaled = (this.y - this.yoffset) * scalefactor;
                    this.wscaled = this.w * scalefactor;
                    this.hscaled = this.h * scalefactor;

                    //this.xscaled += pagedx;
                    //this.yscaled += pagedy;

                    switch (this.type) {
                        case 0: //pencil, marker, eraser

                            this.xscaled += pagedx;
                            this.yscaled += pagedy;
                            this.wscaled = (this.w - this.xoffset) * scalefactor;
                            this.hscaled = (this.h - this.yoffset) * scalefactor;
                           
                            this.wscaled += pagedx;
                            this.hscaled += pagedy;
                            abswidthandheight = true;
                            break;
                        case 1: //polygon
                        
                            this.xscaled += pagedx;
                            this.yscaled += pagedy;
                            this.wscaled = (this.w - this.xoffset) * scalefactor;
                            this.hscaled = (this.h - this.yoffset) * scalefactor;
                  
                            this.wscaled += pagedx;
                            this.hscaled += pagedy;
                            abswidthandheight = true;
                            break;
                        case 2:
                    
                            this.xscaled += pagedx;
                            this.yscaled += pagedy;
                            this.wscaled = (this.w - this.xoffset) * scalefactor;
                            this.hscaled = (this.h - this.yoffset) * scalefactor;
                        
                            this.wscaled += pagedx;
                            this.hscaled += pagedy;
                            abswidthandheight = true;
                            break;
                        case 3: //rectangle
                            this.xscaled += pagedx;
                            this.yscaled += pagedy;
                         
                            abswidthandheight = false;
                            break;
                        case 4: //oval
                            this.xscaled += pagedx;
                            this.yscaled += pagedy;
                            if (this.subtype == 1) {
                                this.wscaled = (this.w - this.xoffset) * scalefactor;
                                this.hscaled = (this.h - this.yoffset) * scalefactor;
                                this.wscaled += pagedx;
                                this.hscaled += pagedy;
                                abswidthandheight = true;
                            }
                            else {
                                abswidthandheight = false;
                            }
                          
                            break;
                        case 5:
                            this.xscaled += pagedx;
                            this.yscaled += pagedy;
                 
                            abswidthandheight = false;
                            break;
                        case 6: //line arrow
                            this.xscaled += pagedx;
                            this.yscaled += pagedy;
                            this.wscaled = (this.w - this.xoffset) * scalefactor;
                            this.hscaled = (this.h - this.yoffset) * scalefactor;
                            this.wscaled += pagedx;
                            this.hscaled += pagedy;
                     
                            abswidthandheight = true;
                            break;
                        case 7: //dimension line
                            this.xscaled += pagedx;
                            this.yscaled += pagedy;
                            this.wscaled = (this.w - this.xoffset) * scalefactor;
                            this.hscaled = (this.h - this.yoffset) * scalefactor;
                            this.wscaled += pagedx;
                            this.hscaled += pagedy;
                        
                            abswidthandheight = true;
                            break;
                        case 8:
                            this.xscaled += pagedx;
                            this.yscaled += pagedy;
                     
                            this.wscaled = (this.w - this.xoffset) * scalefactor;
                            this.hscaled = (this.h - this.yoffset) * scalefactor;
                            this.wscaled += pagedx;
                            this.hscaled += pagedy;
                    
                            abswidthandheight = true;
                            break;
                        case 9: //text
                            this.xscaled += pagedx;
                            this.yscaled += pagedy;
           
                            if (this.subtype == 1 || this.subtype == 2) {
                                abswidthandheight = false;
                            }
                            else {
                                this.wscaled = this.textwidth; // * scalefactor;
                                this.hscaled = this.textheight * scalefactor;
                            }
                            //width and height determined by text and font set in draw function
                            abswidthandheight = false;
                            break;
                        case 10:
                            this.xscaled += pagedx;
                            this.yscaled += pagedy;
                       
                            if (markupobject.bUseFixedScale) {
                                this.wscaled = this.w * markupobject.fixedscaleFactor;
                                this.hscaled = this.h * markupobject.fixedscaleFactor;
                            }
                            else {
                                this.wscaled = this.w * scalefactor;
                                this.hscaled = this.h * scalefactor;
                            }
                            abswidthandheight = false;
                            break;
                        //image
                        case 11:


                            if (this.subtype == 2){

                                this.wscaled = this.w;
                                this.hscaled = this.h;
            
                            }else{
                                this.wscaled = this.w * scalefactor;
                                this.hscaled = this.h * scalefactor;
                            }

                            this.xscaled += pagedx;
                            this.yscaled += pagedy;
                    

                            abswidthandheight = false;
                            break;
                        case 12:
                            this.xscaled += pagedx;
                            this.yscaled += pagedy;
               
                            abswidthandheight = false;
                            break;
                        
                        case 13:


                            this.xscaled += pagedx;
                            this.yscaled += pagedy;

                            if (markupobject.bUseFixedScale) {
                                this.wscaled = this.w * markupobject.fixedscaleFactor;
                                this.hscaled = this.h * markupobject.fixedscaleFactor;
                            }

                            break;


                        case 20:


                            if (this.subtype == 2){

                                this.wscaled = this.w;
                                this.hscaled = this.h;
            
                            }else{
                                this.wscaled = this.w * scalefactor;
                                this.hscaled = this.h * scalefactor;
                            }

                            this.xscaled += pagedx;
                            this.yscaled += pagedy;
                    

                            abswidthandheight = false;
                            break;
                            

                    }

                    this.rotatedrect.x = this.xscaled;
                    this.rotatedrect.y = this.yscaled;
                    this.rotatedrect.w = this.wscaled;
                    this.rotatedrect.h = this.hscaled;


                };
                this.setFirstdrawDim = function(scalefactor, fixedscale, bRasterScale){
                    

                    if(markupobject.pdfannot){
                        var pdfannot = DocObj.getFoxitAnnot(markupobject.pdfannotref);
                        
                        if(pdfannot != undefined){
                          
            
                          scalefactor = 1;
            
            
                          
                          var bpoly = this.type == 1 || this.type == 2 || this.type == 8;
            
            
                          if (bpoly) {
                            markupobject.findrectangle();
                          }
            
                        }
                    }

                    if(scalefactor == 0){
                        scalefactor = 1;
                    }

                    if(fixedscale == 0){
                        fixedscale = 1;
                    }


                    if(isNaN(scalefactor)){
                        scalefactor = 1;
                    }

                    if(isNaN(fixedscale)){
                        fixedscale = 1;
                    }


                    //var mainimagescale = getImageMainscale(pageobject);


                    var mainimagescale = getCurrentPageMainimageScale();

                    var internalscale = scalefactor;


                    if(bNewestFileformat){
                        internalscale = internalscale * mainimagescale;
                    }


                    


                    var fontheight = markupobject.font.height;

                    if(bUseMarkupScaling){
                        internalscale = markupobject.scaling;

                    }

                    
                    if (markupobject.type != 12) {

                        if(bRasterScale){

                            if (bfixedScale && !bUseMarkupScaling){
                                fontheight = Math.round((fontheight * fixedscale) / markupobject.scaling);
                            }else{
                                fontheight = markupobject.font.height;
                            }
    
                        }else{

                            if (bfixedScale && bNewFileformat && !bUseMarkupScaling){
                                fontheight = Math.round(fontheight * fixedscale);
                            }else{
                                fontheight = Math.round(fontheight * internalscale);
                            }
                            
                        }

                        if (isNaN(markupobject.font.height)) {
                            //markupobject.font.height = 11;
                            //markupobject.font.setHeight(11);
                            fontheight = 11;
                        }
    

                        markupobject.textheight = fontheight;
                        markupobject.font.setHeight(fontheight);
    
                    }


                    if (bNewFileformat){


                        if(!bRasterScale){
                            //markupobject.arrowlength = markupobject.arrowlength - markupobject.linewidth;

                            //this.arrowlength = parseInt(xmlDoc.getElementsByTagName('ArrowSize')[0].firstChild.nodeValue);
                            //this.linewidth = parseFloat(xmlDoc.getElementsByTagName('LineWidth')[0].firstChild.nodeValue);



                            if (bfixedScale && !bUseMarkupScaling) {
                                markupobject.linewidth *= fixedscale;
                                markupobject.linewidthSegment *= fixedscale;
                                


                            }
                            else {
                                markupobject.linewidth *= internalscale;
                                markupobject.linewidthSegment *= internalscale;
                            }
                            
                            markupobject.arrowlength = markupobject.arrowlength + markupobject.linewidth;
    
                        }else{
                            //markupobject.arrowlength = markupobject.arrowlength - markupobject.linewidth;

                            if (bfixedScale && !bUseMarkupScaling) {
                                markupobject.linewidth = (markupobject.linewidth * fixedscale) / internalscale;

                                markupobject.linewidthSegment = (markupobject.linewidthSegment * fixedscale) / internalscale;

                            }
                            
                            markupobject.arrowlength = markupobject.arrowlength + markupobject.linewidth;

                        }

                        if (isNaN(markupobject.linewidth)) {
                            markupobject.linewidth = 1;
                            markupobject.linewidthSegment = linewidthvalueSegment;
                            
                        }

                        if (isNaN(markupobject.arrowlength)) {
                            markupobject.arrowlength = nArrowSize + markupobject.linewidth;
                        }

                        //change to set when page is loaded not from page object.



                        
                    }

                    if (bNewFileformat && !bRasterScale){
                        this.x = (this.x + this.xoffset) * internalscale;
                        this.y = (this.y + this.yoffset) * internalscale;


                        if(markupobject.type == 14){


                           
                            //markupobject.points[pi].x = (markupobject.points[pi].x + this.xoffset) * internalscale;
                            //markupobject.points[pi].y = (markupobject.points[pi].y + this.yoffset) * internalscale;

                            markupobject.startX = (markupobject.startX + this.xoffset) * internalscale;
                            markupobject.startY = (markupobject.startY + this.yoffset) * internalscale;

                            markupobject.secondX = (markupobject.secondX + this.xoffset) * internalscale;
                            markupobject.secondY = (markupobject.secondY + this.yoffset) * internalscale;

                            markupobject.endX = (markupobject.endX + this.xoffset) * internalscale;
                            markupobject.endY = (markupobject.endY + this.yoffset) * internalscale;
    


                            markupobject.setthird = true;


                        }

                        
                        var bCircle = (this.type == 4 && this.subtype == 1 && bNewestFileformat);

                        if (this.type == 0 || this.type == 1 || this.type == 2 || this.type == 6 || this.type == 7 || this.type == 8 || bCircle) {
                            this.w = (this.w + this.xoffset) * internalscale;
                            this.h = (this.h + this.yoffset) * internalscale;
                        }
                        else {
                            if (this.type == 10 && bfixedScale) {
                                this.w *= fixedscale;
                                this.h *= fixedscale;
                            }
                            else {
                                this.w *= internalscale;
                                this.h *= internalscale;
                            }
                        }
                    }



                    if (this.type == 7) {

                        if (bNewFileformat && !bRasterScale){
                            markupobject.leaderoffset *= internalscale;
                        }

                    }
                    if (this.type == 8) {

                        if (bNewFileformat && !bRasterScale) {
                            markupobject.labeloffsetx *= internalscale;
                        }
                        if (bNewFileformat && !bRasterScale) {
                            markupobject.labeloffsety *= internalscale;
                        }
                    }

                    if (this.type == 13) {

                        if (bNewFileformat) {

                            if(!bRasterScale){
                                if (bfixedScale && !bUseMarkupScaling) {
                                    //this.linewidth *= fixedscale;
        
                                    this.innerRadius *= fixedscale;
                                    this.outerRadius *= fixedscale;
        
                                }
                                else {
                                    
                                    this.innerRadius *= internalscale;
                                    this.outerRadius *= internalscale;
        
                                    //this.linewidth *= internalscale;
                                }
    
                            }else{

                                //markupobject.linewidth = (markupobject.linewidth * fixedscale) / internalscale;

                                if (bfixedScale && !bUseMarkupScaling) {
                                    markupobject.innerRadius = (markupobject.innerRadius * fixedscale) / internalscale;
                                    markupobject.outerRadius = (markupobject.outerRadius * fixedscale) / internalscale;
                                }

                                //outerRadiusscale = ( localouterRadius / fixedscale) * imagescale;
                                //innerRadiusscale = ( localinnerRadius / fixedscale) * imagescale;
        
                            }

                        }



                        //markupobject.countshapes.push({x : mousepos.x, y : mousepos.y});

                        for (var ci = 0; ci < markupobject.countshapes.length; ci++) {

                            if (bNewFileformat && !bRasterScale) {

                                markupobject.countshapes[ci].x = (markupobject.countshapes[ci].x + this.xoffset) * internalscale;
                                markupobject.countshapes[ci].y = (markupobject.countshapes[ci].y + this.yoffset) * internalscale;

                            }

                            
                        }
                        markupobject.findrectangle();



                    }

                    var bsegment = (this.type == 1 && this.subtype == 6);
                    var bpoly = (this.type == 1 || this.type == 2 || this.type == 8);

                    if (bpoly && !bsegment) {

                        for (var pi = 0; pi < markupobject.points.length; pi++) {

                            if (bNewFileformat && !bRasterScale) {

                                markupobject.points[pi].x = (markupobject.points[pi].x + this.xoffset) * internalscale;
                                markupobject.points[pi].y = (markupobject.points[pi].y + this.yoffset) * internalscale;

                            }

                            
                        }
                        markupobject.findrectangle();

                        if(markupobject.holes.length > 0){

                            for(var h = 0; h < markupobject.holes.length; h++){

                                //markupobject.holes[h].parent = markupobject;

                                /*for (var pi = 0; pi < markupobject.holes[h].points.length; pi++) {

                                    if (bNewFileformat && !bRasterScale) {
        
                                        markupobject.holes[h].points[pi].x = (markupobject.holes[h].points[pi].x + markupobject.holes[h].xoffset) * internalscale;
                                        markupobject.holes[h].points[pi].y = (markupobject.holes[h].points[pi].y + markupobject.holes[h].yoffset) * internalscale;
        
                                    }
        
                                    
                                }*/


                            }
                            
                        }

                    }



                    if (this.type == 0 || bsegment) {


                        for (var lcounter = 0; lcounter < this.pointlist.length; lcounter++) {
                            for (var counter = 0; counter < this.pointlist[lcounter].length; counter++) {
                                

                                if (bNewFileformat && !bRasterScale) {

                                    this.pointlist[lcounter][counter].x = (this.pointlist[lcounter][counter].x + this.xoffset) * internalscale;
                                    this.pointlist[lcounter][counter].y = (this.pointlist[lcounter][counter].y + this.yoffset) * internalscale;
        
                                }
    
                                
                            }
                        }
                        markupobject.findrectangle();

                        
                    }


                    if (bNewFileformat && !bRasterScale) {
                        this.xoffset *= internalscale;
                        this.yoffset *= internalscale;

                        if (isNaN(this.docdx)) {
                            this.docdx = DocObj.getCurrentpage().getdx();
                        }

                        if (isNaN(this.docdy)) {
                            this.docdy = DocObj.getCurrentpage().getdy();
                        }

                        if (isNaN(this.docdscale)) {
                            this.docdscale = DocObj.getCurrentpage().getdscale();
                        }

                    }

                    if (this.type == 12) {
                     
                        //ctx.font = "bold " + textstampscaled + "pt " + "Times New Roman";
                        this.stampsmalltheight = (this.w * 0.08) - 4;
                        var combinedtextheight = this.textheight + this.stampsmalltheight + ((this.h / 10) * 2);
                     
                        if (combinedtextheight > this.h) {
                            this.textheight = (this.h / 2) - ((this.h / 10) * 2);
                            this.stampsmalltheight = (this.textheight / 2) - 4;
                        }
                    }

                    if (this.type == 12) {
                        markupobject.restorestampfontSize();
                    }

                    //markupobject.loadscaling = markupobject.scaling;
                    markupobject.scaling = scalefactor;

                };
                this.SetDimensions = function (scalefactor, rotation, pagedx, pagedy) {
                    //Calculate the scaled and rotated coordinates and store them.
                    //var scalefactor = DocObj.getCurrentpage().dscale / this.scaling;
                    /*if (!markupobject.saved){
                        rotation = 0;
                    }*/

                    if(this.bfor3D){
                        markupobject.SetDim3D(scalefactor, rotation, pagedx, pagedy);
                        return;
                    }

                    var rotRad360 = 360 * (Math.PI / 180);
                    var abswidthandheight = true;
                    this.xscaled = (this.x - this.xoffset) * scalefactor;
                    this.yscaled = (this.y - this.yoffset) * scalefactor;
                    this.wscaled = this.w * scalefactor;
                    this.hscaled = this.h * scalefactor;
                    if (DocObj.Type == 0) {
                        var docdx = DocObj.pages[0].dx;
                    }
                    else {
                        docdx = DocObj.getCurrentpage().dx;
                    }
                    switch (this.type) {
                        case 0: //pencil, marker, eraser
                            /*if (DocObj.getCurrentpage().usevectorxml){
                             this.xscaled += DocObj.getCurrentpage().dxvector;
                             this.yscaled += DocObj.getCurrentpage().dyvector;
        
                             }else if(DocObj.getCurrentpage().usepdfjs){
                             this.xscaled += DocObj.getCurrentpage().dxpdf;
                             this.yscaled += DocObj.getCurrentpage().dypdf;
        
                             }else{
                             this.xscaled += docdx;
                             //this.xscaled += DocObj.getCurrentpage().dx;
                             this.yscaled += DocObj.getCurrentpage().dy;
        
                             }*/
                            this.xscaled += pagedx;
                            this.yscaled += pagedy;
                            this.wscaled = (this.w - this.xoffset) * scalefactor;
                            this.hscaled = (this.h - this.yoffset) * scalefactor;

                            /*if (DocObj.getCurrentpage().usevectorxml){
                             this.wscaled += DocObj.getCurrentpage().dxvector;
                             this.hscaled += DocObj.getCurrentpage().dyvector;
        
                             }else if(DocObj.getCurrentpage().usepdfjs){
                             this.wscaled += DocObj.getCurrentpage().dxpdf;
                             this.hscaled += DocObj.getCurrentpage().dypdf;
        
                             }else{
                             this.wscaled += docdx;
                             //this.wscaled += DocObj.getCurrentpage().dx;
                             this.hscaled += DocObj.getCurrentpage().dy;
        
                             }*/
                            this.wscaled += pagedx;
                            this.hscaled += pagedy;
                            abswidthandheight = true;

                            break;
                        case 1: //polygon
                            /*if (DocObj.getCurrentpage().usevectorxml){
                             this.xscaled += DocObj.getCurrentpage().dxvector;
                             this.yscaled += DocObj.getCurrentpage().dyvector;
                             }else if(DocObj.getCurrentpage().usepdfjs){
                             this.xscaled += DocObj.getCurrentpage().dxpdf;
                             this.yscaled += DocObj.getCurrentpage().dypdf;
                             }else{
                             this.xscaled += docdx;
                             //this.xscaled += DocObj.getCurrentpage().dx;
                             this.yscaled += DocObj.getCurrentpage().dy;
        
                             }*/
                            this.xscaled += pagedx;
                            this.yscaled += pagedy;
                            this.wscaled = (this.w - this.xoffset) * scalefactor;
                            this.hscaled = (this.h - this.yoffset) * scalefactor;
                            /*if (DocObj.getCurrentpage().usevectorxml){
                             this.wscaled += DocObj.getCurrentpage().dxvector;
                             this.hscaled += DocObj.getCurrentpage().dyvector;
                             }else if(DocObj.getCurrentpage().usepdfjs){
                             this.wscaled += DocObj.getCurrentpage().dxpdf;
                             this.hscaled += DocObj.getCurrentpage().dypdf;
                             }else{
                             this.wscaled += docdx;
                             //this.wscaled += DocObj.getCurrentpage().dx;
                             this.hscaled += DocObj.getCurrentpage().dy;
        
                             }*/
                            this.wscaled += pagedx;
                            this.hscaled += pagedy;
                            abswidthandheight = true;
                            break;
                        case 2:
                            /*if (DocObj.getCurrentpage().usevectorxml){
                             this.xscaled += DocObj.getCurrentpage().dxvector;
                             this.yscaled += DocObj.getCurrentpage().dyvector;
                             }else if(DocObj.getCurrentpage().usepdfjs){
                             this.xscaled += DocObj.getCurrentpage().dxpdf;
                             this.yscaled += DocObj.getCurrentpage().dypdf;
        
                             }else{
                             this.xscaled += docdx;
                             //this.xscaled += DocObj.getCurrentpage().dx;
                             this.yscaled += DocObj.getCurrentpage().dy;
        
                             }*/
                            this.xscaled += pagedx;
                            this.yscaled += pagedy;
                            this.wscaled = (this.w - this.xoffset) * scalefactor;
                            this.hscaled = (this.h - this.yoffset) * scalefactor;
                            /*if (DocObj.getCurrentpage().usevectorxml){
                             this.wscaled += DocObj.getCurrentpage().dxvector;
                             this.hscaled += DocObj.getCurrentpage().dyvector;
                             }else if(DocObj.getCurrentpage().usepdfjs){
                             this.wscaled += DocObj.getCurrentpage().dxpdf;
                             this.hscaled += DocObj.getCurrentpage().dypdf;
        
                             }else{
                             this.wscaled += docdx;
                             //this.wscaled += DocObj.getCurrentpage().dx;
                             this.hscaled += DocObj.getCurrentpage().dy;
        
                             }*/
                            this.wscaled += pagedx;
                            this.hscaled += pagedy;
                            abswidthandheight = true;
                            break;
                        case 3: //rectangle
                            this.xscaled += pagedx;
                            this.yscaled += pagedy;
                            /*if (DocObj.getCurrentpage().usevectorxml){
                             this.xscaled += DocObj.getCurrentpage().dxvector;
                             this.yscaled += DocObj.getCurrentpage().dyvector;
                             }else if(DocObj.getCurrentpage().usepdfjs){
                             this.xscaled += DocObj.getCurrentpage().dxpdf;
                             this.yscaled += DocObj.getCurrentpage().dypdf;
        
                             }else{
                             this.xscaled += docdx;
                             //this.xscaled += DocObj.getCurrentpage().dx;
                             this.yscaled += DocObj.getCurrentpage().dy;
        
                             }*/
                            abswidthandheight = false;
                            break;
                        case 4: //oval
                            this.xscaled += pagedx;
                            this.yscaled += pagedy;
                            if (this.subtype == 1) {
                                this.wscaled = (this.w - this.xoffset) * scalefactor;
                                this.hscaled = (this.h - this.yoffset) * scalefactor;
                                this.wscaled += pagedx;
                                this.hscaled += pagedy;
                                abswidthandheight = true;
                            }
                            else {
                                abswidthandheight = false;
                            }
                            /*if (DocObj.getCurrentpage().usevectorxml){
                             this.xscaled += DocObj.getCurrentpage().dxvector;
                             this.yscaled += DocObj.getCurrentpage().dyvector;
                             }else if(DocObj.getCurrentpage().usepdfjs){
                             this.xscaled += DocObj.getCurrentpage().dxpdf;
                             this.yscaled += DocObj.getCurrentpage().dypdf;
        
                             }else{
                             this.xscaled += docdx;
                             //this.xscaled += DocObj.getCurrentpage().dx;
                             this.yscaled += DocObj.getCurrentpage().dy;
        
                             }*/
                            break;
                        case 5:
                            this.xscaled += pagedx;
                            this.yscaled += pagedy;
                            /*if (DocObj.getCurrentpage().usevectorxml){
                             this.xscaled += DocObj.getCurrentpage().dxvector;
                             this.yscaled += DocObj.getCurrentpage().dyvector;
                             }else if(DocObj.getCurrentpage().usepdfjs){
                             this.xscaled += DocObj.getCurrentpage().dxpdf;
                             this.yscaled += DocObj.getCurrentpage().dypdf;
        
                             }else{
                             this.xscaled += docdx;
                             //this.xscaled += DocObj.getCurrentpage().dx;
                             this.yscaled += DocObj.getCurrentpage().dy;
        
                             }*/
                            abswidthandheight = false;
                            break;
                        case 6: //line arrow
                            this.xscaled += pagedx;
                            this.yscaled += pagedy;
                            this.wscaled = (this.w - this.xoffset) * scalefactor;
                            this.hscaled = (this.h - this.yoffset) * scalefactor;
                            this.wscaled += pagedx;
                            this.hscaled += pagedy;
                            /*if (DocObj.getCurrentpage().usevectorxml){
                             this.xscaled += DocObj.getCurrentpage().dxvector;
                             this.yscaled += DocObj.getCurrentpage().dyvector;
                             this.wscaled += DocObj.pages[this.pagenumber].dxvector;
                             this.hscaled += DocObj.pages[this.pagenumber].dyvector;
                             }else if(DocObj.getCurrentpage().usepdfjs){
        
                             this.xscaled += DocObj.getCurrentpage().dxpdf;
                             this.yscaled += DocObj.getCurrentpage().dypdf;
                             this.wscaled += DocObj.pages[this.pagenumber].dxpdf;
                             this.hscaled += DocObj.pages[this.pagenumber].dypdf;
        
                             }else{
                             this.xscaled += docdx;
                             //this.xscaled += DocObj.getCurrentpage().dx;
                             this.yscaled += DocObj.getCurrentpage().dy;
                             this.wscaled += DocObj.pages[this.pagenumber].dx;
                             this.hscaled += DocObj.pages[this.pagenumber].dy;
        
                             }*/
                            abswidthandheight = true;
                            break;
                        case 7: //dimension line
                            this.xscaled += pagedx;
                            this.yscaled += pagedy;
                            this.wscaled = (this.w - this.xoffset) * scalefactor;
                            this.hscaled = (this.h - this.yoffset) * scalefactor;
                            this.wscaled += pagedx;
                            this.hscaled += pagedy;
                            /*if (DocObj.getCurrentpage().usevectorxml){
                             this.xscaled += DocObj.getCurrentpage().dxvector;
                             this.yscaled += DocObj.getCurrentpage().dyvector;
                             this.wscaled += DocObj.pages[this.pagenumber].dxvector;
                             this.hscaled += DocObj.pages[this.pagenumber].dyvector;
                             }else if(DocObj.getCurrentpage().usepdfjs){
        
                             this.xscaled += DocObj.getCurrentpage().dxpdf;
                             this.yscaled += DocObj.getCurrentpage().dypdf;
                             this.wscaled += DocObj.pages[this.pagenumber].dxpdf;
                             this.hscaled += DocObj.pages[this.pagenumber].dypdf;
        
                             }else{
                             this.xscaled += docdx;
                             //this.xscaled += DocObj.getCurrentpage().dx;
                             this.yscaled += DocObj.getCurrentpage().dy;
                             this.wscaled += DocObj.pages[this.pagenumber].dx;
                             this.hscaled += DocObj.pages[this.pagenumber].dy;
        
                             }*/
                            abswidthandheight = true;
                            break;
                        case 8:
                            this.xscaled += pagedx;
                            this.yscaled += pagedy;
                            /*if (DocObj.getCurrentpage().usevectorxml){
                             this.xscaled += DocObj.getCurrentpage().dxvector;
                             this.yscaled += DocObj.getCurrentpage().dyvector;
                             }else if(DocObj.getCurrentpage().usepdfjs){
        
                             this.xscaled += DocObj.getCurrentpage().dxpdf;
                             this.yscaled += DocObj.getCurrentpage().dypdf;
        
                             }else{
                             this.xscaled += docdx;
                             //this.xscaled += DocObj.getCurrentpage().dx;
                             this.yscaled += DocObj.getCurrentpage().dy;
        
                             }*/
                            this.wscaled = (this.w - this.xoffset) * scalefactor;
                            this.hscaled = (this.h - this.yoffset) * scalefactor;
                            this.wscaled += pagedx;
                            this.hscaled += pagedy;
                            /*if (DocObj.getCurrentpage().usevectorxml){
                             this.wscaled += DocObj.getCurrentpage().dxvector;
                             this.hscaled += DocObj.getCurrentpage().dyvector;
                             }else if(DocObj.getCurrentpage().usepdfjs){
        
                             this.wscaled += DocObj.getCurrentpage().dxpdf;
                             this.hscaled += DocObj.getCurrentpage().dypdf;
        
                             }else{
                             this.wscaled += docdx;
                             //this.wscaled += DocObj.getCurrentpage().dx;
                             this.hscaled += DocObj.getCurrentpage().dy;
        
                             }*/
                            abswidthandheight = true;
                            break;
                        case 9: //text
                            this.xscaled += pagedx;
                            this.yscaled += pagedy;
                            /*if (DocObj.getCurrentpage().usevectorxml){
                             this.xscaled += DocObj.getCurrentpage().dxvector;
                             this.yscaled += DocObj.getCurrentpage().dyvector;
                             }else if(DocObj.getCurrentpage().usepdfjs){
        
                             this.xscaled += DocObj.getCurrentpage().dxpdf;
                             this.yscaled += DocObj.getCurrentpage().dypdf;
        
                             }else{
                             this.xscaled += docdx;
                             //this.xscaled += DocObj.getCurrentpage().dx;
                             this.yscaled += DocObj.getCurrentpage().dy;
        
                             }*/
                            if (this.subtype == 1 || this.subtype == 2) {
                                abswidthandheight = false;
                            }
                            else {
                                this.wscaled = this.textwidth; // * scalefactor;
                                this.hscaled = this.textheight * scalefactor;
                            }
                            //width and height determined by text and font set in draw function
                            abswidthandheight = false;
                            break;
                        case 10:
                            this.xscaled += pagedx;
                            this.yscaled += pagedy;
                            /*if (DocObj.getCurrentpage().usevectorxml){
                             this.xscaled += DocObj.getCurrentpage().dxvector;
                             this.yscaled += DocObj.getCurrentpage().dyvector;
                             }else if(DocObj.getCurrentpage().usepdfjs){
        
                             this.xscaled += DocObj.getCurrentpage().dxpdf;
                             this.yscaled += DocObj.getCurrentpage().dypdf;
        
                             }else{
                             this.xscaled += docdx;
                             //this.xscaled += DocObj.getCurrentpage().dx;
                             this.yscaled += DocObj.getCurrentpage().dy;
        
                             }*/

                            //console.log(bUseFixedScale, markupobject.fixedscaleFactor, scalefactor);

                            if (markupobject.bUseFixedScale) {
                                this.wscaled = this.w * markupobject.fixedscaleFactor;
                                this.hscaled = this.h * markupobject.fixedscaleFactor;
                            }
                            else {
                                this.wscaled = this.w * scalefactor;
                                this.hscaled = this.h * scalefactor;
                            }
                            abswidthandheight = false;
                            break;
                        //image
                        case 11:
                            if (this.subtype == 2){
                                //this.xscaled = (this.x - this.xoffset);
                                //this.yscaled = (this.y - this.yoffset);
                                this.wscaled = this.w;
                                this.hscaled = this.h;
            
                            }else{
                                this.wscaled = this.w * scalefactor;
                                this.hscaled = this.h * scalefactor;
                            }

                            this.xscaled += pagedx;
                            this.yscaled += pagedy;

                            /*if (DocObj.getCurrentpage().usevectorxml){
                             this.xscaled += DocObj.getCurrentpage().dxvector;
                             this.yscaled += DocObj.getCurrentpage().dyvector;
                             }else if(DocObj.getCurrentpage().usepdfjs){
        
                             this.xscaled += DocObj.getCurrentpage().dxpdf;
                             this.yscaled += DocObj.getCurrentpage().dypdf;
        
                             }else{
                             this.xscaled += docdx;
                             //this.xscaled += DocObj.getCurrentpage().dx;
                             this.yscaled += DocObj.getCurrentpage().dy;
        
                             }*/
                            abswidthandheight = false;
                            break;
                        case 12:
                            this.xscaled += pagedx;
                            this.yscaled += pagedy;
                            /*if (DocObj.getCurrentpage().usevectorxml){
                             this.xscaled += DocObj.getCurrentpage().dxvector;
                             this.yscaled += DocObj.getCurrentpage().dyvector;
                             }else if(DocObj.getCurrentpage().usepdfjs){
        
                             this.xscaled += DocObj.getCurrentpage().dxpdf;
                             this.yscaled += DocObj.getCurrentpage().dypdf;
        
                             }else{
                             this.xscaled += docdx;
                             //this.xscaled += DocObj.getCurrentpage().dx;
                             this.yscaled += DocObj.getCurrentpage().dy;
        
                             }*/
                            abswidthandheight = false;
                            break;
                        case 13:


                            this.xscaled += pagedx;
                            this.yscaled += pagedy;

                            if (markupobject.bUseFixedScale) {
                                this.wscaled = this.w * markupobject.fixedscaleFactor;
                                this.hscaled = this.h * markupobject.fixedscaleFactor;
                            }

                            break;

                        case 14 : 

                        if(this.startX != null && this.startY != null ){
                            this.startXscaled = (this.startX - this.xoffset) * scalefactor;
                            this.startYscaled = (this.startY - this.yoffset) * scalefactor;
                            this.startXscaled += pagedx;
                            this.startYscaled += pagedy;

                        }


                        if(this.secondX != null && this.secondY != null ){
                            this.secondXscaled = (this.secondX - this.xoffset) * scalefactor;
                            this.secondYscaled = (this.secondY - this.yoffset) * scalefactor;
                            this.secondXscaled += pagedx;
                            this.secondYscaled += pagedy;

                        }

                        if(this.endX != null && this.endY != null ){
                            this.endXscaled = (this.endX - this.xoffset) * scalefactor;
                            this.endYscaled = (this.endY - this.yoffset) * scalefactor;

                            this.endXscaled += pagedx;
                            this.endYscaled += pagedy;


                        }


                            ///this.wscaled = this.w * scalefactor;
                            //this.hscaled = this.h * scalefactor;


                            //this.wscaled = (this.w - this.xoffset) * scalefactor;
                            //this.hscaled = (this.h - this.yoffset) * scalefactor;

                            abswidthandheight = true;

                        break;    

                        case 20:
                            if (this.subtype == 2){ 
                                this.wscaled = this.w;
                                this.hscaled = this.h;
            
                            }else{
                                this.wscaled = this.w * scalefactor;
                                this.hscaled = this.h * scalefactor;
                            }

                            this.xscaled += pagedx;
                            this.yscaled += pagedy;

                            abswidthandheight = false;
                            break;
                        


                    }
                    //var centerx = this.xscaled + (this.wscaled / 2);
                    //var centery = this.yscaled + (this.hscaled / 2);
                    var anglerad = rotation * (Math.PI / 180);
                    var centercanvX = (canvasowidth / 2);
                    var centercanvY = (canvasoheight / 2);
                    this.rotatedrect.x = this.xscaled;
                    this.rotatedrect.y = this.yscaled;
                    this.rotatedrect.w = this.wscaled;
                    this.rotatedrect.h = this.hscaled;
                    /*if (markupobject.textrotate !=0 && this.type == 9){
                     var textp = markupobject.getRotatedMarkup(this.xscaled,this.yscaled,this.rotatedrect.w,this.rotatedrect.h,markupobject.textrotate);
                     this.rotatedrect.w = textp.x;
                     this.rotatedrect.h = textp.y;
                     }*/
                    if (rotation != 0) {
                        markupobject.setRotatedMarkuprect(centercanvX, centercanvY, abswidthandheight, anglerad);
                    }
                    if (this.type == 9 && this.subtype == 0 && this.textrotate != 0) {
                        if (rotation == 0) {
                            //this.rotatedrect.x = this.xscaled;
                            //this.rotatedrect.y = this.yscaled;
                            //this.rotatedrect.w = this.xscaled + this.wscaled;
                            //this.rotatedrect.h = this.yscaled - this.hscaled;
                            //markupobject.setRotatedMarkuprect(centercanvX, centercanvY,abswidthandheight, anglerad);
                        }
                        var txtx = Math.min(this.rotatedrect.x, this.rotatedrect.w);
                        var txtw = Math.max(this.rotatedrect.x, this.rotatedrect.w);
                        var txty = Math.min(this.rotatedrect.y, this.rotatedrect.h);
                        var txth = Math.max(this.rotatedrect.y, this.rotatedrect.h);
                        var rotatefactor = markupobject.textrotate - rotRad360;
                        var textp = markupobject.getRotatedMarkup(this.rotatedrect.x, this.rotatedrect.y, this.rotatedrect.w, this.rotatedrect.h, rotatefactor);
                        this.rotatedrect.w = textp.x;
                        this.rotatedrect.h = textp.y;
                        if (this.textrotate / (Math.PI / 180) == 180 && rotation == 0) {
                            rotatefactor = markupobject.textrotate;
                            textp = markupobject.getRotatedMarkup(this.rotatedrect.x, this.rotatedrect.y, this.rotatedrect.w, this.rotatedrect.h, rotatefactor);
                            this.rotatedrect.w = textp.x;
                            this.rotatedrect.h = textp.y;
                        }
                        /*var txtx = Math.min(this.rotatedrect.x,this.rotatedrect.w);
                         var txtw = Math.max(this.rotatedrect.x,this.rotatedrect.w);
                         var txty = Math.min(this.rotatedrect.y,this.rotatedrect.h);
                         var txth = Math.max(this.rotatedrect.y,this.rotatedrect.h);
        
                         rotwidth = txtw - txtx;
                         rotheight = txth - txty;
        
                         var rotwidtheight = markupobject.getMarkupSelectPointRot(this.rotatedrect.w, this.rotatedrect.h,markupobject.textrotate);
                         this.rotatedrect.w = this.rotatedrect.x + rotwidtheight.x;
                         this.rotatedrect.h = this.rotatedrect.y + rotwidtheight.y;*/
                    }
                    if (this.type == 9 && (this.subtype == 1 || this.subtype == 2)) {
                        if (rotation != 0) {
                            //markupobject.setRotatedMarkuprect(centercanvX, centercanvY, abswidthandheight,anglerad - (90 * (Math.PI / 180)));
                            //var textrectx1 = Math.min(this.rotatedrect.x, this.rotatedrect.w);
                            //var textrectx2 = Math.max(this.rotatedrect.x, this.rotatedrect.w);
                            //var textrecty1 = Math.min(this.rotatedrect.y, this.rotatedrect.h);
                            //var textrecty2 = Math.max(this.rotatedrect.y, this.rotatedrect.h);
                            //this.xscaled = textrectx1;
                            //this.yscaled = textrecty2;
                        }
                    }
                    //this.getRotatedMarkuprect = function (centercanvX, centercanvY,abswidthandheight, anglerad){
                    /*this.rotatedrect.x = this.xscaled;
                     this.rotatedrect.y = this.yscaled;
                     this.rotatedrect.w = this.wscaled;
                     this.rotatedrect.h = this.hscaled;*/
                };
                function getscalepointX(point, basescale){

                    return (point.x - point.offsetx) * ( basescale / point.scale );
                } 

                function getscalepointY(point, basescale){

                    return (point.y - point.offsety)  * ( basescale / point.scale );

                } 
                function get_markup_norm_points(points){
                    var i = 0;
                    var pts = [];
                    //pts = points.slice();
                    
                    var basescale = points[0].scale;

                    var n = points.length;

                    for (i = 0; i < n; i++) {
                        
                        pts.push({ x: getscalepointX(points[i], basescale), y: getscalepointY(points[i], basescale), scale : points[i].scale, offsetx : points[i].offsetx, offsety : points[i].offsety});
                    }

                    return pts;
            
                }
                this.SquareArea = function(){
                    var area = 0.0;
                    area = Math.abs(this.w) * Math.abs(this.h);

                    return area;
                    
                };

                this.PolygonHoleArea = function(hole){

                    var i = 0;
                    
                    var area = 0.0;

                    var n = hole.points.length;

                    for (i = 0; i < n - 1; i++) {
                        area += (hole.points[i].x * hole.points[i + 1].y) - (hole.points[i + 1].x * hole.points[i].y);
                    }
                    area += (hole.points[n - 1].x * hole.points[0].y) - (hole.points[0].x * hole.points[n - 1].y);

                    return Math.abs(area / 2.0);

                }

                this.PolygonArea = function () {
                    

                    
                    var i = 0;
                    var h = 0;
                    
                    var area = 0.0;
                    var harea = 0.0;
                    

                    var n = this.points.length;

                    for (i = 0; i < n - 1; i++) {
                        area += (this.points[i].x * this.points[i + 1].y) - (this.points[i + 1].x * this.points[i].y);
                    }
                    area += (this.points[n - 1].x * this.points[0].y) - (this.points[0].x * this.points[n - 1].y);



                    return Math.abs(area / 2.0);



                };
                this.PolygonCentre = function (xy) {
                    var centre = 0;
                    var n = this.points.length;
                    var i;
                    if (xy == 'x') {
                        if (n > 0) {
                            for (i = 0; i < n; i++) {
                                centre += this.points[i].x;
                            }
                            centre = centre / n;
                        }
                    }
                    else {
                        if (n > 0) {
                            for (i = 0; i < n; i++) {
                                centre += this.points[i].y;
                            }
                            centre = centre / n;
                        }
                    }
                    return centre;
                };
                this.FindText = function (SearchExpr, casesens) {
                    var found = false;

                    if (!casesens){
                        var searchexpr = SearchExpr.toLowerCase();
                        var searchstr = this.text.toLowerCase();
                    }else{
                        searchexpr = SearchExpr;
                        searchstr = this.text;

                    }
                    //var searchexpr = SearchExpr.toLowerCase();
                    //var searchstr = this.text.toLowerCase();
                    var pos = searchstr.search(searchexpr);
                    //var pos = this.text.search(SearchExpr);

                    if (pos == -1) {
                        found = false;
                    }
                    else if (pos >= 0) {
                        found = true;
                    }
                    return found;
                };
                this.RotateText = function () {
                };

                this.AdjustPointForRotation = function(spoint){

                    var CanvRotRadPoints = DocObj.getCurrentpage().getpageRotation() * (Math.PI / 180);

                    switch (DocObj.getCurrentpage().getpageRotation()) {
                        case 0:
                            CanvRotRadPoints = DocObj.getCurrentpage().getpageRotation() * (Math.PI / 180);
                            break;
                        case 90:
                            CanvRotRadPoints = (DocObj.getCurrentpage().getpageRotation() + 180) * (Math.PI / 180);
                            break;
                        case 270:
                            CanvRotRadPoints = (DocObj.getCurrentpage().getpageRotation() - 180) * (Math.PI / 180);
                            break;
                        case 180:
                            CanvRotRadPoints = DocObj.getCurrentpage().getpageRotation() * (Math.PI / 180);
                            break;
                    }

                    var centercanvX = (canvasowidth / 2);
                    var centercanvY = (canvasoheight / 2);
                    var transpoint = new point();
                    
                    transpoint = markupobject.getRotatedMarkup(centercanvX, centercanvY, spoint.x, spoint.y, CanvRotRadPoints);

                    return transpoint;
                };                

                this.AdjustForRotation = function (save) {

                    var CanvRotRad = (360 - DocObj.getCurrentpage().getpageRotation()) * (Math.PI / 180);
                    var CanvRotRadOrig = DocObj.getCurrentpage().getpageRotation() * (Math.PI / 180);
                    var CanvRotRadPoints = DocObj.getCurrentpage().getpageRotation() * (Math.PI / 180);

                    switch (DocObj.getCurrentpage().getpageRotation()) {
                        case 0:
                            CanvRotRadPoints = DocObj.getCurrentpage().getpageRotation() * (Math.PI / 180);
                            break;
                        case 90:
                            CanvRotRadPoints = (DocObj.getCurrentpage().getpageRotation() + 180) * (Math.PI / 180);
                            break;
                        case 270:
                            CanvRotRadPoints = (DocObj.getCurrentpage().getpageRotation() - 180) * (Math.PI / 180);
                            break;
                        case 180:
                            CanvRotRadPoints = DocObj.getCurrentpage().getpageRotation() * (Math.PI / 180);
                            break;
                    }

                    var centercanvX = (canvasowidth / 2);
                    var centercanvY = (canvasoheight / 2);

                    var bisfoxitPDF = isfoxitPDF();

                    if(bisfoxitPDF){

                        var pixelRatio = getPixelRatio();

                        centercanvX *= pixelRatio;
                        centercanvY *= pixelRatio;
    
                    }



                    var counter = 0;
                    var lcounter = 0;
                    var transpoint = new point();
                    var absX = 0;
                    var absY = 0;
                    var ulx = 0;
                    var uly = 0;
                    if (DocObj.getCurrentpage().getpageRotation() == 0) {
                        return;
                    }
                    switch (DocObj.getCurrentpage().getpageRotation()) {
                        case 0:
                            return;
                            break;
                        case 90:
                            absX = this.x;
                            absY = this.y + this.h;
                            ulx = this.x + this.w;
                            uly = this.y;
                            break;
                        case 180:
                            absX = this.x;
                            absY = this.y;
                            ulx = this.x + this.w;
                            uly = this.y + this.h;
                            break;
                        case 270:
                            absX = this.x + this.w;
                            absY = this.y;
                            ulx = this.x;
                            uly = this.y + this.h;
                            break;
                    }
                    switch (this.type) {
                        case 0: //eraser, pencil, marker
                            //for each point
                            //markupobject.setinitRotatedMarkuppoint(centercanvX, centercanvY,abswidthandheight, CanvRotRad);
                            //set new this.x and new this.y
                            for (lcounter = 0; lcounter < this.pointlist.length; lcounter++) {
                                for (counter = 0; counter < this.pointlist[lcounter].length; counter++) {
                                    transpoint = markupobject.setinitRotatedMarkupPoint(centercanvX, centercanvY, this.pointlist[lcounter][counter].x, this.pointlist[lcounter][counter].y, CanvRotRad);
                                    this.pointlist[lcounter][counter].x = transpoint.x;
                                    this.pointlist[lcounter][counter].y = transpoint.y;
                                }
                            }
                            markupobject.findrectangle();
                            break;
                        case 1: //polygons
                            //for each point
                            /*for (counter = 0; counter < this.points.length; counter++) {
                                transpoint = markupobject.setinitRotatedMarkupPoint(centercanvX, centercanvY, this.points[counter].x, this.points[counter].y, CanvRotRad);
                                this.points[counter].x = transpoint.x;
                                this.points[counter].y = transpoint.y;
        
                            }*/
                            //always start with point 0
                            if (!markupobject.firstpointset) {
                                transpoint = markupobject.setinitRotatedMarkupPoint(centercanvX, centercanvY, this.points[0].x, this.points[0].y, CanvRotRad);
                                this.points[0].x = transpoint.x;
                                this.points[0].y = transpoint.y;
                                markupobject.firstpointset = true;
                            }
                            else {
                                //test always setting this.
                            }
                            var curpoint = this.points.length - 1;
                            transpoint = markupobject.setinitRotatedMarkupPoint(centercanvX, centercanvY, this.points[curpoint].x, this.points[curpoint].y, CanvRotRad);
                            this.points[curpoint].x = transpoint.x;
                            this.points[curpoint].y = transpoint.y;
                            markupobject.findrectangle();
                            break;
                        case 2: //Poly curves
                            //for each point
                            for (counter = 0; counter < this.points.length; counter++) {
                                transpoint = markupobject.setinitRotatedMarkupPoint(centercanvX, centercanvY, this.points[counter].x, this.points[counter].y, CanvRotRad);
                                this.points[counter].x = transpoint.x;
                                this.points[counter].y = transpoint.y;
                            }
                            markupobject.findrectangle();
                            break;
                        case 3: //Rectangle
                            transpoint = markupobject.setinitRotatedMarkupPoint(centercanvX, centercanvY, ulx, uly, CanvRotRad);
                            this.x = transpoint.x;
                            this.y = transpoint.y;
                            transpoint = markupobject.setinitRotatedMarkupPoint(centercanvX, centercanvY, absX, absY, CanvRotRad);
                            this.w = transpoint.x - this.x;
                            this.h = transpoint.y - this.y;
                            break;
                        case 4: //Oval
                            if (markupobject.subtype != 1) {
                                transpoint = markupobject.setinitRotatedMarkupPoint(centercanvX, centercanvY, ulx, uly, CanvRotRad);
                                this.x = transpoint.x;
                                this.y = transpoint.y;
                                transpoint = markupobject.setinitRotatedMarkupPoint(centercanvX, centercanvY, absX, absY, CanvRotRad);
                                this.w = transpoint.x - this.x;
                                this.h = transpoint.y - this.y;
                            }
                            else {
                                if (!save) {
                                    transpoint = markupobject.setinitRotatedMarkupPoint(centercanvX, centercanvY, this.x, this.y, CanvRotRad);
                                    this.x = transpoint.x;
                                    this.y = transpoint.y;
                                    transpoint = markupobject.setinitRotatedMarkupPoint(centercanvX, centercanvY, this.w, this.h, CanvRotRad);
                                    this.w = transpoint.x;
                                    this.h = transpoint.y;
                                }
                                else {
                                    transpoint = markupobject.setinitRotatedMarkupPoint(centercanvX, centercanvY, this.x, this.y, CanvRotRadOrig);
                                    this.x = transpoint.x;
                                    this.y = transpoint.y;
                                }
                            }
                            break;
                        case 5: //Cloud
                            transpoint = markupobject.setinitRotatedMarkupPoint(centercanvX, centercanvY, ulx, uly, CanvRotRad);
                            this.x = transpoint.x;
                            this.y = transpoint.y;
                            transpoint = markupobject.setinitRotatedMarkupPoint(centercanvX, centercanvY, absX, absY, CanvRotRad);
                            this.w = transpoint.x - this.x;
                            this.h = transpoint.y - this.y;
                            break;
                        case 6: //line arrow etc.
                            if (!save) {
                                transpoint = markupobject.setinitRotatedMarkupPoint(centercanvX, centercanvY, this.x, this.y, CanvRotRad);
                                this.x = transpoint.x;
                                this.y = transpoint.y;
                                transpoint = markupobject.setinitRotatedMarkupPoint(centercanvX, centercanvY, this.w, this.h, CanvRotRad);
                                this.w = transpoint.x;
                                this.h = transpoint.y;
                            }
                            else {
                                if (bOrthoOnEdit) {
                                    //console.log(this.w, this.h);
                                    transpoint = markupobject.setinitRotatedMarkupPoint(centercanvX, centercanvY, this.w, this.h, CanvRotRadOrig);
                                    this.w = transpoint.x;
                                    this.h = transpoint.y;
                                    //console.log(this.w, this.h);
                                }
                                transpoint = markupobject.setinitRotatedMarkupPoint(centercanvX, centercanvY, this.x, this.y, CanvRotRadOrig);
                                this.x = transpoint.x;
                                this.y = transpoint.y;
                            }
                            //markupobject.setinitRotatedMarkuppoint(centercanvX, centercanvY,abswidthandheight, CanvRotRad);
                            break;
                        case 7: //dimension line.
                            if (!save) {
                                transpoint = markupobject.setinitRotatedMarkupPoint(centercanvX, centercanvY, this.x, this.y, CanvRotRad);
                                this.x = transpoint.x;
                                this.y = transpoint.y;
                                transpoint = markupobject.setinitRotatedMarkupPoint(centercanvX, centercanvY, this.w, this.h, CanvRotRad);
                                this.w = transpoint.x;
                                this.h = transpoint.y;
                            }
                            else {

                                if (bOrthoOnEdit) {
                                    transpoint = markupobject.setinitRotatedMarkupPoint(centercanvX, centercanvY, this.w, this.h, CanvRotRadOrig);
                                    this.w = transpoint.x;
                                    this.h = transpoint.y;
                                }

                                transpoint = markupobject.setinitRotatedMarkupPoint(centercanvX, centercanvY, this.x, this.y, CanvRotRadOrig);
                                this.x = transpoint.x;
                                this.y = transpoint.y;
                            }
                            break;
                        case 8:
                            if (!this.firstpointrotAdjusted) {
                                transpoint = markupobject.setinitRotatedMarkupPoint(centercanvX, centercanvY, this.points[0].x, this.points[0].y, CanvRotRad);
                                this.points[0].x = transpoint.x;
                                this.points[0].y = transpoint.y;
                                this.firstpointrotAdjusted = true;
                            }
                            /*for (counter = 0; counter < this.points.length; counter++) {
                                transpoint = markupobject.setinitRotatedMarkupPoint(centercanvX, centercanvY, this.points[counter].x, this.points[counter].y, CanvRotRad);
                                this.points[counter].x = transpoint.x;
                                this.points[counter].y = transpoint.y;
                            }*/
                            if (this.points.length == 2) {
                                /*for (counter = 0; counter < this.points.length; counter++) {
                                    transpoint = markupobject.setinitRotatedMarkupPoint(centercanvX, centercanvY, this.points[counter].x, this.points[counter].y, CanvRotRad);
                                    this.points[counter].x = transpoint.x;
                                    this.points[counter].y = transpoint.y;
                                }*/
                                /*var curpoint = this.points.length - 1;
                                var prevpoint = this.points.length - 2;
                                transpoint = markupobject.setinitRotatedMarkupPoint(centercanvX, centercanvY, this.points[curpoint].x, this.points[curpoint].y, CanvRotRad);
                                this.points[curpoint].x = transpoint.x;
                                this.points[curpoint].y = transpoint.y;*/
                            }
                            var curpoint = markupobject.getlastpoint();

                            if(curpoint){

                                transpoint = markupobject.getRotatedMarkup(centercanvX, centercanvY, curpoint.x, curpoint.y, CanvRotRadPoints);
                                markupobject.setlastpoint(transpoint.x,transpoint.y);
                            
                            }
                            //var curpoint = this.points.length - 1;
                            //transpoint = markupobject.setinitRotatedMarkupPoint(centercanvX, centercanvY, this.points[curpoint].x, this.points[curpoint].y, CanvRotRad);
                            //this.points[curpoint].x = transpoint.x;
                            //this.points[curpoint].y = transpoint.y;
                            markupobject.findrectangle();
                            break;
                        case 9: //text markup
                            if (this.subtype == 1 || this.subtype == 2) {
                                transpoint = markupobject.setinitRotatedMarkupPoint(centercanvX, centercanvY, ulx, uly, CanvRotRad);
                                this.x = transpoint.x;
                                this.y = transpoint.y;
                                transpoint = markupobject.setinitRotatedMarkupPoint(centercanvX, centercanvY, absX, absY, CanvRotRad);
                                this.w = transpoint.x - this.x;
                                this.h = transpoint.y - this.y;
                            }
                            else {
                                transpoint = markupobject.setinitRotatedMarkupPoint(centercanvX, centercanvY, this.x, this.y, CanvRotRad);
                                this.x = transpoint.x;
                                this.y = transpoint.y;
                                this.textrotate = CanvRotRad;
                            }
                            /*if (markupobject.textrotate !=0){
                             var textp = markupobject.getRotatedMarkup(this.x,this.y,this.x+this.w,this.y-this.h,markupobject.textrotate);
                             this.rotatedrect.w = textp2.x;
                             this.rotatedrect.h = textp2.y;
                             }*/
                            break;
                        case 10: //note markup
                            transpoint = markupobject.setinitRotatedMarkupPoint(centercanvX, centercanvY, this.x, this.y, CanvRotRad);
                            this.x = transpoint.x;
                            this.y = transpoint.y;
                            break;
                        case 11:
                            transpoint = markupobject.setinitRotatedMarkupPoint(centercanvX, centercanvY, this.x, this.y, CanvRotRad);
                            this.x = transpoint.x;
                            this.y = transpoint.y;
                            /*transpoint = markupobject.setinitRotatedMarkupPoint(centercanvX, centercanvY,absX,absY, CanvRotRad);
                             this.w = transpoint.x - this.x;
                             this.h = transpoint.y - this.y;*/
                            break;
                        case 12: //stamp markup
                            transpoint = markupobject.setinitRotatedMarkupPoint(centercanvX, centercanvY, ulx, uly, CanvRotRad);
                            this.x = transpoint.x;
                            this.y = transpoint.y;
                            this.textrotate = CanvRotRad;
                            transpoint = markupobject.setinitRotatedMarkupPoint(centercanvX, centercanvY, absX, absY, CanvRotRad);
                            this.w = transpoint.x - this.x;
                            this.h = transpoint.y - this.y;
                            break;
                        case 13:

                            var bhavepoint = (this.countshapes.length > 0);

                            if (!this.firstpointrotAdjusted && bhavepoint) {
                                //transpoint = markupobject.setinitRotatedMarkupPoint(centercanvX, centercanvY, this.countshapes[0].x, this.countshapes[0].y, CanvRotRad);
                                //this.countshapes[0].x = transpoint.x;
                                //this.countshapes[0].y = transpoint.y;
                                this.firstpointrotAdjusted = true;
                            }

                            var curpoint = markupobject.getlastcount();

                            if(curpoint){

                                transpoint = markupobject.getRotatedMarkup(centercanvX, centercanvY, curpoint.x, curpoint.y, CanvRotRadPoints);
                                markupobject.setlastcount(transpoint.x,transpoint.y);
                            
                            }

                            break;
                        case 14:

                            if (!save) {

                                if(this.startX != null && this.startY != null ){
                                    transpoint = markupobject.setinitRotatedMarkupPoint(centercanvX, centercanvY, this.startX, this.startY, CanvRotRad);
                                    this.startX = transpoint.x;
                                    this.startY = transpoint.y;
    
                                }

                                if(this.secondX != null && this.secondY != null ){
                                    transpoint = markupobject.setinitRotatedMarkupPoint(centercanvX, centercanvY, this.secondX, this.secondY, CanvRotRad);
                                    this.secondX = transpoint.x;
                                    this.secondY = transpoint.y;
    
                                }

                                if(this.endX != null && this.endY != null ){
                                    transpoint = markupobject.setinitRotatedMarkupPoint(centercanvX, centercanvY, this.endX, this.endY, CanvRotRad);
                                    this.endX = transpoint.x;
                                    this.endY = transpoint.y;
        
                                }

                                



                            }
                            else {
                                /*if (bOrthoOnEdit) {
                                    //console.log(this.w, this.h);
                                    transpoint = markupobject.setinitRotatedMarkupPoint(centercanvX, centercanvY, this.w, this.h, CanvRotRadOrig);
                                    this.w = transpoint.x;
                                    this.h = transpoint.y;
                                    //console.log(this.w, this.h);
                                }*/
                                if(this.startX != null && this.startY != null ){
                                    transpoint = markupobject.setinitRotatedMarkupPoint(centercanvX, centercanvY, this.startX, this.startY, CanvRotRadOrig);
                                    this.startX = transpoint.x;
                                    this.startY = transpoint.y;
    
                                }
                            }

                            break;    

                        case 20:
                                transpoint = markupobject.setinitRotatedMarkupPoint(centercanvX, centercanvY, this.x, this.y, CanvRotRad);
                                this.x = transpoint.x;
                                this.y = transpoint.y;
                                break;


                        default:
                            break;
                    }
                };
                /* Andriy notify get markup listeners */
                /* Andriy nocallback */
                this.notify = function () {
                    RxCore_GUI_Markuplist.notify();
                };
                this.addsides = function () {

                    this.sides = [];

                    var scalefactor = getCurrentPageScale();
                    
                    if (this.points.length > 0) {
                        var side = {
                            x1: this.points[0].x,
                            y1: this.points[0].y,
                            x2: this.points[0].x,
                            y2: this.points[0].y
                        };
                        var counter = 0;
                        //
                        for (counter = 0; counter < this.points.length; counter++) {
                            if (counter == 0) {
                                side = {
                                    x1: this.points[counter].x,
                                    y1: this.points[counter].y,
                                    x2: this.points[counter].x,
                                    y2: this.points[counter].y
                                };
                            }
                            else if (counter > 0) {
                                side.x2 = this.points[counter].x;
                                side.y2 = this.points[counter].y;
                                this.sides.push({ x1: side.x1, y1: side.y1, x2: side.x2, y2: side.y2 });
                                side.x1 = this.points[counter].x;
                                side.y1 = this.points[counter].y;
                            }
                        }
                        if (this.sides[0] != undefined) {
                            side.x2 = this.sides[0].x1;
                            side.y2 = this.sides[0].y1;
                            this.sides.push({ x1: side.x1, y1: side.y1, x2: side.x2, y2: side.y2 });
                        }
                    }
                };
                this.savemarkupstateChanged = function(DocRef,operation){

                    var undoobj = new MarkupUndoObject(markupobject.markupnumber);
                    
                    if(markupobject.bMultiOp){
                        undoobj.setmultiValues(markupobject.multiOpId);
                        markupobject.resetmultiValues();
                    }

                    undoobj.SetUndoValues(markupobject);
                    
                    if(operation == undoModify){
                        undoobj.bmodify = true;
                        undoobj.bdelete = false;
                        undoobj.bcreate = false;
                    }else if(operation == undoCreate){
                        undoobj.bmodify = false;
                        undoobj.bdelete = false;
                        undoobj.bcreate = true;
                    }else if(operation == undoDelete){
                        undoobj.bmodify = false;
                        undoobj.bdelete = true;
                        undoobj.bcreate = false;
                    }

                    DocRef.tempundolist.push(undoobj);

                    markupobject.resetmarkupmodified();
                    

                };


                this.savemarkupstate = function(DocRef,operation){

                    var undoobj = new MarkupUndoObject(markupobject.markupnumber);

                    if(markupobject.bMultiOp){
                        undoobj.setmultiValues(markupobject.multiOpId);
                        markupobject.resetmultiValues();
                    }

                    undoobj.SetUndoValues(markupobject);
                    
                    if(operation == undoModify){
                        undoobj.bmodify = true;
                        undoobj.bdelete = false;
                        undoobj.bcreate = false;
                    }else if(operation == undoCreate){
                        undoobj.bmodify = false;
                        undoobj.bdelete = false;
                        undoobj.bcreate = true;
                    }else if(operation == undoDelete){
                        undoobj.bmodify = false;
                        undoobj.bdelete = true;
                        undoobj.bcreate = false;
                    }

                    
                    DocRef.markupundolist.push(undoobj);

                };

                this.savemetolistLoadMulti = function (DocRef, nOpID) {

                    //console.log(last);

                    //NewMarkupobj.savemetolistLoadMulti(DocObj, bmultiop);

                    markupobject.savemetolist(DocRef, true, false, false, nOpID);
                    //markupobject.markupnumber = markupobject.markupID;
                };

                this.setfirstDraw = function(){
                    bFirstdrawn = true;
                };
                this.savemetolistLoad = function (DocRef, last) {
                    markupobject.savemetolist(DocRef, !last, false, last);

                    markupobject.setdisplay(markupdisplayonload);
                    //if(markupdisplayonload


                    //markupobject.markupnumber = markupobject.markupID;
                };
                this.savemetolistDraw = function () {
                    
                    bFirstdrawn = true;

                    markupobject.noDraw = bDrawToFoxitonly;    
                    markupobject.savemetolist(DocObj, false, true);

                    if(bAlwaysassignUniqueID){

                        if(markupobject.uniqueID == 0){
                            getGUID(markupobject);
                        }

                        
                    }



                };
                this.savemetolist = function (DocRef, preventNotification, draw, last, nOpID) {
                //this.savemetolist = function (DocRef, preventNotification, draw, last) {


                    if ((markupobject.type == 1 && markupobject.subtype == 2) || markupobject.type == 8) {
                        //var numpoints = markupobject.points.length;
                        if(markupobject.points.length < 3){
                            return;
                        } 
                    }

                    markupobject.saved = true;
                    //
                    var bisCircle = (markupobject.type == 4 && markupobject.subtype == 1);
                    if (markupobject.type == 0 || markupobject.type == 6 || markupobject.type == 7 || markupobject.type == 9 || markupobject.type == 10 || markupobject.type == 11 || markupobject.type == 20 || bisCircle) {
                        if (draw && markupobject.bUsemouseinput) {
                            markupobject.AdjustForRotation(true);
                        }
                        markupobject.bUsemouseinput = true;
                    }
                    //var bhole = (markupobject.type == 8 && markupobject.subtype == 2);
                    
                    markupobject.markupnumber = getlastmarkupnumber();
                    DocRef.markupdraworder[DocRef.markupdraworder.length] = DocRef.markuplist.length;
                    DocRef.markuplist[DocRef.markuplist.length] = markupobject;

                        //create undoboject where delete flag is true
                    if (draw) {
                        
                        if(!markupobject.bisTextArrow){
                            markupobject.savemarkupstate(DocRef, undoCreate);
                        }

                        //var undoobj = new MarkupUndoObject(markupobject.markupnumber);
                        //undoobj.SetUndoValues(markupobject);
                        //undoobj.bdelete = true;
                        //DocRef.markupundolist.push(undoobj);

                        if (Rxcore_GUI_foxitmarkupLoadComplete != undefined && bDrawToFoxitonly) {
                            Rxcore_GUI_foxitmarkupLoadComplete.loadComplete(DocRef.markuplist, DocRef.fileindex, DocRef.bActive);
                        }


                    }
                    //DocObj.markuplist[markupobject.markupnumber].selected = true;
                    
                    /*if(!bhole){
                        
                    }*/

                    DocRef.nummarkups++;
                    


                    if (RxCore_GUI_Markuplist != undefined) {



                        RxCore_GUI_Markuplist.setMarkupList(DocRef.getmarkuplist(), DocRef.bActive, preventNotification);
                    }


                    if (last) {
                        if (Rxcore_GUI_markupLoadComplete != undefined) {
                            Rxcore_GUI_markupLoadComplete.loadComplete(DocRef.markuplist, DocRef.fileindex, DocRef.bActive);
                        }


                        //test if neccessary
                        /*if (RxCore_GUI_pagethumbs != undefined) {
                            RxCore_GUI_pagethumbs.setThumbnails(DocRef.thumbnails);
                        }*/
                    }
                };


                function getrrect(x, y){

                    var localx = x;
                    var localy = y;
                    var CanvRotRad = DocObj.getCurrentpage().getpageRotation() * (Math.PI / 180);
                    var centercanvX = (canvasowidth / 2);
                    var centercanvY = (canvasoheight / 2);


                    var bisfoxitPDF = isfoxitPDF();

                    if(bisfoxitPDF){

                        var pixelRatio = getPixelRatio();

                        centercanvX *= pixelRatio;
                        centercanvY *= pixelRatio;
    
                    }


                    var rotRad360 = 360 * (Math.PI / 180);
                    var negrotRad180 = -180 * (Math.PI / 180);
                    var rotRad180 = 180 * (Math.PI / 180);
                    var scalefactor = 1.0;

                    var scalefactor = getCurrentPageScale();
                    scalefactor = scalefactor / markupobject.scaling;

                    
                    /*var xscaled = this.x * scalefactor;
                     var yscaled = this.y * scalefactor;
                     var wscaled = this.w * scalefactor;
                     var hscaled = this.h * scalefactor;*/
                    var xscaled = (this.x - this.xoffset) * scalefactor;
                    var yscaled = (this.y - this.yoffset) * scalefactor;
                    var wscaled = this.w * scalefactor;
                    var hscaled = this.h * scalefactor;

                    var offset = getCurrentPageOffset(markupobject.pagenumber);
                    xscaled = xscaled + offset.x;
                    yscaled = yscaled + offset.y;


                    var curRot = markupobject.rotation;
                    var locRot = markupobject.rotation;
                    var brotate = true;

                    if(!markupobject.bfor3D){

                        var pagerect = DocObj.getCurrentpage().getpagerectdirect();

                        
                        var startx = pagerect.x;
                        var endx = pagerect.w;
                        var starty = pagerect.y;
                        var endy = pagerect.h;

                        if(pagerect.usepixelscale){
                            var pixelRatio = getPixelRatio();

                            startx = startx * pixelRatio;
                            endx = endx * pixelRatio;
                            starty = starty * pixelRatio;
                            endy = endy * pixelRatio;
    
                        }


                        /*var startx = DocObj.getCurrentpage().startx;
                        var endx = DocObj.getCurrentpage().endx;
                        var starty = DocObj.getCurrentpage().starty;
                        var endy = DocObj.getCurrentpage().endy;*/
                    }else{
                        startx = 0;
                        endx = canvasowidth;
                        starty = 0;
                        endy = canvasoheight;
                    }


                    /*if (DocObj.getCurrentpage().usevectorxml) {
                        xscaled = xscaled + DocObj.getCurrentpage().dxvector;
                        yscaled = yscaled + DocObj.getCurrentpage().dyvector;
                    }
                    else if (DocObj.getCurrentpage().usepdfjs) {
                        xscaled = xscaled + DocObj.getCurrentpage().dxpdf;
                        yscaled = yscaled + DocObj.getCurrentpage().dypdf;
                    }
                    else {
                        xscaled = xscaled + DocObj.getCurrentpage().dx;
                        yscaled = yscaled + DocObj.getCurrentpage().dy;
                    }*/

                    var centerx = xscaled + (wscaled / 2);
                    var centery = yscaled + (hscaled / 2);
                    if (this.type == 9 && this.subtype == 0) {
                        centerx = xscaled + (wscaled / 2);
                        centery = yscaled - (hscaled / 2);
                        var rotatefactor = 0;
                        if (this.textrotate != 0) {
                            rotatefactor = rotRad360 - this.textrotate;
                            var textscaled = this.textheight * scalefactor;
                            var textwscaled = this.textwidth * scalefactor;
                            var rmcenter = markupobject.getMarkupSelectPointRot(textwscaled / 2, -(textscaled / 2), markupobject.textrotate);
                            centerx = xscaled + rmcenter.x;
                            centery = yscaled + rmcenter.y;
                        }
                    }
                    var width = centerx - localx;
                    var height = centery - localy;
                    var temprot = Math.atan2(height, width);
                    if (temprot < -(Math.PI * 0.5)) {
                        temprot += Math.PI * 2;
                    }
                    var markupcenter = markupobject.getRotatedMarkup(centercanvX, centercanvY, centerx, centery, CanvRotRad);
                    var markupxy = markupobject.getRotatedMarkup(centercanvX, centercanvY, xscaled, yscaled, CanvRotRad);
                    switch (DocObj.getCurrentpage().getpageRotation()) {
                        case 0:
                            //localx = x;
                            //localy = y;
                            if (this.type == 9 && this.subtype == 0) {
                                
                                locRot = temprot - (Math.PI * 0.5) + rotatefactor;
                            }
                            else {
                                
                                locRot = temprot - (Math.PI * 0.5);
                            }
                            break;
                        case 90:
                            //localx = y;
                            //localy = -x;
                            width = localx - markupcenter.x;
                            height = localy - markupcenter.y;
                            temprot = Math.atan2(height, width);
                            if (this.type == 9 && this.subtype == 0) {
                                
                                locRot = temprot - (Math.PI * 0.5) + CanvRotRad + rotatefactor;
                            }
                            else {
                                
                                locRot = temprot - (Math.PI * 0.5) + CanvRotRad;
                            }
                            break;
                        case 180:
                            //localx = -x;
                            //localy = -y;
                            width = localx - markupcenter.x;
                            height = localy - markupcenter.y;
                            temprot = Math.atan2(height, width);
                            if (this.type == 9 && this.subtype == 0) {
                                
                                locRot = temprot - (Math.PI * 0.5) + rotatefactor;
                            }
                            else {
                                
                                locRot = temprot - (Math.PI * 0.5); // + CanvRotRad;
                            }
                            break;
                        case 270:
                            //localx = -y;
                            //localy = x;
                            width = localx - markupcenter.x;
                            height = localy - markupcenter.y;
                            temprot = Math.atan2(height, width);
                            if (this.type == 9 && this.subtype == 0) {
                                
                                locRot = temprot - (Math.PI * 0.5) + CanvRotRad + rotatefactor;
                            }
                            else {
                                locRot = temprot - (Math.PI * 0.5) + CanvRotRad;
                            }
                            break;
                    }
                    //rotation angle in radians
                    //temprot = Math.atan2(height,width);
                    //markupobject.rotation = temprot - (Math.PI*0.5) + CanvRotRad;
                    switch (this.type) {
                        case 0: //eraser, pencil, marker
                            //no rotation supported
                            markupobject.rotation = 0;
                            break;
                        case 1: //polygons
                            //no rotation supported
                            markupobject.rotation = 0;
                            break;
                        case 2: //Poly curves
                            //execute code block 2
                            markupobject.rotation = 0;
                            break;
                        case 3: //Rectangle
                            break;
                        case 4: //Oval
                            break;
                        case 5: //Cloud
                            break;
                        case 6: //line arrow etc.
                            //no rotation supported
                            markupobject.rotation = 0;
                            break;
                        case 7: //dimension line.
                            //no rotation supported
                            markupobject.rotation = 0;
                            break;
                        case 8:
                            //no rotation supported
                            markupobject.rotation = 0;
                            break;
                        case 9: //text markup
                            break;
                        case 12: //stamp markup
                            break;
                        default:
                            break;
                    }
                    //var negrotRad180 = -180 * (Math.PI / 180);
                    //var rotRad180 = 180 * (Math.PI / 180);

                    if (locRot < negrotRad180) {
                        locRot += rotRad360;
                    }
                    if (locRot > rotRad180) {
                        locRot -= rotRad360;
                    }
                    
                    var rrect = markupobject.rotrectrot(this.xscaled, this.yscaled, this.xscaled + this.wscaled, this.yscaled + this.hscaled, locRot);




                }

                this.zoomTo = function(padding){

                    if (DocObj.getcurPage() != markupobject.pagenumber) {
                
                        CheckPageloaded(markupobject.pagenumber).then(function(pagenum){
            
                            //DocObj.pages[markupobject.pagenumber].zoomToMarkup(markupobject, padding);
                            DocObj.pages[markupobject.pagenumber].MarkupZoombyMarkup(markupobject, padding);
                            
                                        //DocObj.panToMarkup(markupobject);
                        });
                    }else{
                        DocObj.pages[markupobject.pagenumber].MarkupZoombyMarkup(markupobject, padding);
                    }
                    
                    
                };

                this.rotate = function (x, y, corner) {

                    if(markupobject.pdfannotproxy){
                        return;
                    }

                    if(!markupobject.getselected()){
                        return;
                    }

                    if(bNoMarkuprotate){
                        return;
                    }




                    var localx = x;
                    var localy = y;
                    var CanvRotRad = DocObj.getCurrentpage().getpageRotation() * (Math.PI / 180);
                    var centercanvX = (canvasowidth / 2);
                    var centercanvY = (canvasoheight / 2);


                    var bisfoxitPDF = isfoxitPDF();

                    if(bisfoxitPDF){

                        var pixelRatio = getPixelRatio();

                        centercanvX *= pixelRatio;
                        centercanvY *= pixelRatio;
    
                    }


                    var rotRad360 = 360 * (Math.PI / 180);
                    var negrotRad180 = -180 * (Math.PI / 180);
                    var rotRad180 = 180 * (Math.PI / 180);
                    var scalefactor = 1.0;

                    var scalefactor = getCurrentPageScale();
                    scalefactor = scalefactor / markupobject.scaling;

                    
                    /*var xscaled = this.x * scalefactor;
                     var yscaled = this.y * scalefactor;
                     var wscaled = this.w * scalefactor;
                     var hscaled = this.h * scalefactor;*/
                    var xscaled = (this.x - this.xoffset) * scalefactor;
                    var yscaled = (this.y - this.yoffset) * scalefactor;
                    var wscaled = this.w * scalefactor;
                    var hscaled = this.h * scalefactor;

                    var offset = getCurrentPageOffset(markupobject.pagenumber);
                    xscaled = xscaled + offset.x;
                    yscaled = yscaled + offset.y;


                    var curRot = markupobject.rotation;
                    var locRot = markupobject.rotation;
                    var brotate = true;

                    if(!markupobject.bfor3D){

                        var pagerect = DocObj.getCurrentpage().getpagerectdirect();

                        
                        var startx = pagerect.x;
                        var endx = pagerect.w;
                        var starty = pagerect.y;
                        var endy = pagerect.h;

                        if(pagerect.usepixelscale){
                            var pixelRatio = getPixelRatio();

                            startx = startx * pixelRatio;
                            endx = endx * pixelRatio;
                            starty = starty * pixelRatio;
                            endy = endy * pixelRatio;
    
                        }


                        /*var startx = DocObj.getCurrentpage().startx;
                        var endx = DocObj.getCurrentpage().endx;
                        var starty = DocObj.getCurrentpage().starty;
                        var endy = DocObj.getCurrentpage().endy;*/
                    }else{
                        startx = 0;
                        endx = canvasowidth;
                        starty = 0;
                        endy = canvasoheight;
                    }


                    /*if (DocObj.getCurrentpage().usevectorxml) {
                        xscaled = xscaled + DocObj.getCurrentpage().dxvector;
                        yscaled = yscaled + DocObj.getCurrentpage().dyvector;
                    }
                    else if (DocObj.getCurrentpage().usepdfjs) {
                        xscaled = xscaled + DocObj.getCurrentpage().dxpdf;
                        yscaled = yscaled + DocObj.getCurrentpage().dypdf;
                    }
                    else {
                        xscaled = xscaled + DocObj.getCurrentpage().dx;
                        yscaled = yscaled + DocObj.getCurrentpage().dy;
                    }*/

                    var centerx = xscaled + (wscaled / 2);
                    var centery = yscaled + (hscaled / 2);
                    if (this.type == 9 && this.subtype == 0) {
                        centerx = xscaled + (wscaled / 2);
                        centery = yscaled - (hscaled / 2);
                        var rotatefactor = 0;
                        if (this.textrotate != 0) {
                            rotatefactor = rotRad360 - this.textrotate;
                            var textscaled = this.textheight * scalefactor;
                            var textwscaled = this.textwidth * scalefactor;
                            var rmcenter = markupobject.getMarkupSelectPointRot(textwscaled / 2, -(textscaled / 2), markupobject.textrotate);
                            centerx = xscaled + rmcenter.x;
                            centery = yscaled + rmcenter.y;
                        }
                    }
                    var width = centerx - localx;
                    var height = centery - localy;
                    var temprot = Math.atan2(height, width);
                    if (temprot < -(Math.PI * 0.5)) {
                        temprot += Math.PI * 2;
                    }
                    var markupcenter = markupobject.getRotatedMarkup(centercanvX, centercanvY, centerx, centery, CanvRotRad);
                    var markupxy = markupobject.getRotatedMarkup(centercanvX, centercanvY, xscaled, yscaled, CanvRotRad);
                    switch (DocObj.getCurrentpage().getpageRotation()) {
                        case 0:
                            //localx = x;
                            //localy = y;
                            if (this.type == 9 && this.subtype == 0) {
                                
                                locRot = temprot - (Math.PI * 0.5) + rotatefactor;
                            }
                            else {
                                
                                locRot = temprot - (Math.PI * 0.5);
                            }
                            break;
                        case 90:
                            //localx = y;
                            //localy = -x;
                            width = localx - markupcenter.x;
                            height = localy - markupcenter.y;
                            temprot = Math.atan2(height, width);
                            if (this.type == 9 && this.subtype == 0) {
                                
                                locRot = temprot - (Math.PI * 0.5) + CanvRotRad + rotatefactor;
                            }
                            else {
                                
                                locRot = temprot - (Math.PI * 0.5) + CanvRotRad;
                            }
                            break;
                        case 180:
                            //localx = -x;
                            //localy = -y;
                            width = localx - markupcenter.x;
                            height = localy - markupcenter.y;
                            temprot = Math.atan2(height, width);
                            if (this.type == 9 && this.subtype == 0) {
                                
                                locRot = temprot - (Math.PI * 0.5) + rotatefactor;
                            }
                            else {
                                
                                locRot = temprot - (Math.PI * 0.5); // + CanvRotRad;
                            }
                            break;
                        case 270:
                            //localx = -y;
                            //localy = x;
                            width = localx - markupcenter.x;
                            height = localy - markupcenter.y;
                            temprot = Math.atan2(height, width);
                            if (this.type == 9 && this.subtype == 0) {
                                
                                locRot = temprot - (Math.PI * 0.5) + CanvRotRad + rotatefactor;
                            }
                            else {
                                locRot = temprot - (Math.PI * 0.5) + CanvRotRad;
                            }
                            break;
                    }
                    //rotation angle in radians
                    //temprot = Math.atan2(height,width);
                    //markupobject.rotation = temprot - (Math.PI*0.5) + CanvRotRad;
                    switch (this.type) {
                        case 0: //eraser, pencil, marker
                            //no rotation supported
                            markupobject.rotation = 0;
                            break;
                        case 1: //polygons
                            //no rotation supported
                            markupobject.rotation = 0;
                            break;
                        case 2: //Poly curves
                            //execute code block 2
                            markupobject.rotation = 0;
                            break;
                        case 3: //Rectangle
                            if(markupobject.subtype == 6){
                                markupobject.rotation = 0;
                            }
                            break;
                        case 4: //Oval
                            break;
                        case 5: //Cloud
                            break;
                        case 6: //line arrow etc.
                            //no rotation supported
                            markupobject.rotation = 0;
                            break;
                        case 7: //dimension line.
                            //no rotation supported
                            markupobject.rotation = 0;
                            break;
                        case 8:
                            //no rotation supported
                            markupobject.rotation = 0;
                            break;
                        case 9: //text markup
                            break;
                        case 12: //stamp markup
                            break;
                        default:
                            break;
                    }
                    //var negrotRad180 = -180 * (Math.PI / 180);
                    //var rotRad180 = 180 * (Math.PI / 180);

                    if (locRot < negrotRad180) {
                        locRot += rotRad360;
                    }
                    if (locRot > rotRad180) {
                        locRot -= rotRad360;
                    }
                    
                    var rrect = markupobject.rotrectrot(this.xscaled, this.yscaled, this.xscaled + this.wscaled, this.yscaled + this.hscaled, locRot);

                                       

                    if ((rrect.x) < startx || (rrect.x) > endx) {
                        //xscaled = xscaled + localx;
                        //markupobject.rotation = curRot;
                        brotate = false;
                    }
                    if (rrect.y < starty || rrect.y > endy) {
                        //yscaled = yscaled + localy;
                        //markupobject.rotation = curRot;
                        brotate = false;
                    }

                    if ((rrect.x + rrect.w) < startx || (rrect.x + rrect.w) > endx) {
                        //xscaled = xscaled + localx;
                        brotate = false;
                    }
                    if ((rrect.y + rrect.h) < starty || (rrect.y + rrect.h) > endy) {
                        //yscaled = yscaled + localy;
                        brotate = false;
                    }


                    if(brotate){
                        markupobject.rotation = locRot;
                    }else{
                        markupobject.rotation = curRot;
                    }                    

                    markupobject.setmarkupmodified(true);

                    //rotate
                    if(RxCore_GUI_MarkupChanged != undefined){
                        var operation = markupobject.editaction;
                        RxCore_GUI_MarkupChanged.setMarkupSelected(markupobject, operation);
                    }


                };

                this.withinboundspoint = function(x,y){

                    var bwithin = false;
                    var boundstartx = DocObj.getCurrentpage().startx;
                    var boundstarty = DocObj.getCurrentpage().starty;
                    var boundendx = DocObj.getCurrentpage().endx;
                    var boundendy = DocObj.getCurrentpage().endy;

                    if (x > boundstartx) {
                        if (y > boundstarty) {
                            if (x < boundendx) {
                                if (y < boundendy) {
                                    bwithin = true;
                                }
                            }
                        }
                    }
                    return bwithin;

                };


                this.withinbounds = function (x, y) {
                    var bwithin = false;
                    var boundstartx = DocObj.getCurrentpage().startx;
                    var boundstarty = DocObj.getCurrentpage().starty;
                    var boundendx = DocObj.getCurrentpage().endx;
                    var boundendy = DocObj.getCurrentpage().endy;


                    var scalefactor = getCurrentPageScale();
                    scalefactor = scalefactor / markupobject.scaling;


                    //scalefactor = DocObj.getCurrentpage().dscale / this.scaling;
                    var thisx = this.x * scalefactor;
                    var thisy = this.y * scalefactor;
                    var thisw = this.w * scalefactor;
                    var thish = this.h * scalefactor;
                    switch (this.type) {
                        case 0: //eraser, pencil, marker
                            break;
                        case 1: //polygons
                            break;
                        case 2: //Poly curves
                            break;
                        case 3: //Rectangle
                            thisx = this.xscaled;
                            thisy = this.yscaled;
                            thisw = this.xscaled + this.wscaled;
                            thish = this.yscaled + this.hscaled;
                            break;
                        case 4: //Oval
                            thisx = this.xscaled;
                            thisy = this.yscaled;
                            thisw = this.xscaled + this.wscaled;
                            thish = this.yscaled + this.hscaled;
                            break;
                        case 5: //Cloud
                            thisx = this.xscaled;
                            thisy = this.yscaled;
                            thisw = this.xscaled + this.wscaled;
                            thish = this.yscaled + this.hscaled;
                            break;
                        case 6: //line arrow etc.
                            break;
                        case 7: //dimension line.
                            break;
                        case 8:
                            break;
                        case 9: //text markup
                            if (this.subtype == 0) {
                                thisx = this.xscaled;
                                thisy = this.yscaled;
                                thisw = this.xscaled + this.textwidth;
                                thish = this.yscaled - this.textheight;
                            }
                            else if (this.subtype == 1 || this.subtype == 2) {
                                thisx = this.xscaled;
                                thisy = this.yscaled;
                                thisw = this.xscaled + this.wscaled;
                                thish = this.yscaled + this.hscaled;
                            }
                            break;
                        case 10: //text markup
                            thisx = this.xscaled;
                            thisy = this.yscaled;
                            thisw = this.xscaled + this.wscaled;
                            thish = this.yscaled + this.hscaled;
                            break;
                        case 11: //image markup
                            thisx = this.xscaled;
                            thisy = this.yscaled;
                            thisw = this.xscaled + this.wscaled;
                            thish = this.yscaled + this.hscaled;
                            break;
                        case 12: //stamp markup
                            thisx = this.xscaled;
                            thisy = this.yscaled;
                            thisw = this.xscaled + this.wscaled;
                            thish = this.yscaled + this.hscaled;
                            break;
                        case 20: //link markup
                            thisx = this.xscaled;
                            thisy = this.yscaled;
                            thisw = this.xscaled + this.wscaled;
                            thish = this.yscaled + this.hscaled;
                            break;


                        default:
                            break;
                    }
                    if (thisx + x > boundstartx) {
                        if (thisy + y > boundstarty) {
                            if (thisw + x < boundendx) {
                                if (thish + y < boundendy) {
                                    bwithin = true;
                                }
                            }
                        }
                    }
                    return bwithin;
                };
                this.rotrect = function (x, y, width, height) {
                    var tx = x + ((width - x) / 2);
                    var ty = y + ((height - y) / 2);
                    var ulrotpoint = markupobject.getMarkupSelectPointRot(-((width - x) / 2), -((height - y) / 2), markupobject.rotation);
                    var lrrotpoint = markupobject.getMarkupSelectPointRot(((width - x) / 2), ((height - y) / 2), markupobject.rotation);
                    var llrotpoint = markupobject.getMarkupSelectPointRot(-((width - x) / 2), ((height - y) / 2), markupobject.rotation);
                    var urrotpoint = markupobject.getMarkupSelectPointRot(((width - x) / 2), -((height - y) / 2), markupobject.rotation);
                    var minX = Math.min(tx + ulrotpoint.x, tx + lrrotpoint.x, tx + llrotpoint.x, tx + urrotpoint.x);
                    var minY = Math.min(ty + ulrotpoint.y, ty + lrrotpoint.y, ty + llrotpoint.y, ty + urrotpoint.y);
                    var maxX = Math.max(tx + ulrotpoint.x, tx + lrrotpoint.x, tx + llrotpoint.x, tx + urrotpoint.x);
                    var maxY = Math.max(ty + ulrotpoint.y, ty + lrrotpoint.y, ty + llrotpoint.y, ty + urrotpoint.y);
                    return { x: minX, y: minY, w: maxX - minX, h: maxY - minY };
                };
                this.rotrectrot = function (x, y, width, height, rotation) {
                    var tx = x + ((width - x) / 2);
                    var ty = y + ((height - y) / 2);
                    var ulrotpoint = markupobject.getMarkupSelectPointRot(-((width - x) / 2), -((height - y) / 2), rotation);
                    var lrrotpoint = markupobject.getMarkupSelectPointRot(((width - x) / 2), ((height - y) / 2), rotation);
                    var llrotpoint = markupobject.getMarkupSelectPointRot(-((width - x) / 2), ((height - y) / 2), rotation);
                    var urrotpoint = markupobject.getMarkupSelectPointRot(((width - x) / 2), -((height - y) / 2), rotation);
                    var minX = Math.min(tx + ulrotpoint.x, tx + lrrotpoint.x, tx + llrotpoint.x, tx + urrotpoint.x);
                    var minY = Math.min(ty + ulrotpoint.y, ty + lrrotpoint.y, ty + llrotpoint.y, ty + urrotpoint.y);
                    var maxX = Math.max(tx + ulrotpoint.x, tx + lrrotpoint.x, tx + llrotpoint.x, tx + urrotpoint.x);
                    var maxY = Math.max(ty + ulrotpoint.y, ty + lrrotpoint.y, ty + llrotpoint.y, ty + urrotpoint.y);
                    return { x: minX, y: minY, w: maxX - minX, h: maxY - minY };
                };

                this.setCurPoint = function (point) {
                    var bpolyline = (markupobject.type == 1 && (markupobject.subtype == 1 || markupobject.subtype == 3));
                    if (bpolyline) {
                        if (markupobject.editcurpoint > 0) {
                            markupobject.editcurpoint = point;
                        }
                    }
                    else {
                        if (markupobject.editcurpoint >= 0) {
                            markupobject.editcurpoint = point;
                        }
                    }
                    return markupobject.editcurpoint;
                };
                this.resetcupoint = function () {
                    markupobject.editcurpoint = 0;
                };

                this.movemultiPoints = function(x,y){

                    if(markupobject.pdfannotproxy){
                        return;
                      }
            
                    var localx = x;
                    var localy = y;
                    var bMovepints = true;
                    var bMovepintsx = true;
                    var bMovepintsy = true;
                    switch (DocObj.getCurrentpage().getpageRotation()) {
                        case 0:
                            localx = x;
                            localy = y;
                            break;
                        case 90:
                            localx = y;
                            localy = -x;
                            break;
                        case 180:
                            localx = -x;
                            localy = -y;
                            break;
                        case 270:
                            localx = -y;
                            localy = x;
                            break;
                    }
                    var xscalepoint = 0;
                    var yscalepoint = 0;
                    var scalefactor = 0.0;


                    scalefactor = getCurrentPageScale();
                    scalefactor = scalefactor / markupobject.scaling;

                    if(markupobject.bfor3D){
                        scalefactor = canvasoheight / this.pageheight;
                    }


                    var xscaled = this.x * scalefactor;
                    var yscaled = this.y * scalefactor;
                    var wscaled = this.w * scalefactor;
                    var hscaled = this.h * scalefactor;

                    if(!markupobject.bfor3D){

                        var pagerect = DocObj.getCurrentpage().getpagerectdirect();

                        var startx = pagerect.x;
                        var endx = pagerect.w;
                        var starty = pagerect.y;
                        var endy = pagerect.h;

                        if(pagerect.usepixelscale){
                            var pixelRatio = getPixelRatio();

                            startx = startx * pixelRatio;
                            endx = endx * pixelRatio;
                            starty = starty * pixelRatio;
                            endy = endy * pixelRatio;
    
                        }


                    }else{
                        startx = 0;
                        endx = canvasowidth;
                        starty = 0;
                        endy = canvasoheight;
                    }


                    var pcounter = 0;
                    var lcounter = 0;
                    var counter = 0;

                    if (markupobject.rotation != 0) {
                        
                        var rrect = markupobject.rotrect(this.xscaled, this.yscaled, this.wscaled, this.hscaled);

                    }
                    else {
                        rrect = { x: this.xscaled, y: this.yscaled, w: this.wscaled - this.xscaled, h: this.hscaled - this.yscaled};

                    }


                    xscaled = xscaled - localx;
                    yscaled = yscaled - localy;
                    wscaled -= localx;
                    hscaled -= localy;
                    if (this.bispartofGroup) {
                        this.groupRect.x -= localx;
                        this.groupRect.y -= localy;
                        this.groupRect.w -= localx;
                        this.groupRect.h -= localy;
                    }
                    if (bLimMarkupExtent) {
                        if (this.bispartofGroup) {
                            if (this.groupRect.x - localx < startx || this.groupRect.x - localx > endx) {
                                xscaled = xscaled + localx;
                                this.groupRect.x += localx;
                                this.groupRect.w += localx;
                                wscaled += localx;
                                bMovepints = false;
                                bMovepintsx = false;
                            }
                            if (this.groupRect.y - localy < starty || this.groupRect.y - localy > endy) {
                                yscaled = yscaled + localy;
                                hscaled += localy;
                                this.groupRect.y += localy;
                                this.groupRect.h += localy;
                                bMovepints = false;
                                bMovepintsy = false;
                            }
                            if (this.groupRect.w - localx < startx || this.groupRect.w - localx > endx) {
                                wscaled += localx;
                                xscaled = xscaled + localx;
                                this.groupRect.x += localx;
                                this.groupRect.w += localx;
                                bMovepints = false;
                                bMovepintsx = false;
                            }
                            if (this.groupRect.h - localy < starty || this.groupRect.h - localy > endy) {
                                hscaled += localy;
                                yscaled = yscaled + localy;
                                this.groupRect.y += localy;
                                this.groupRect.h += localy;
                                bMovepints = false;
                                bMovepintsy = false;
                            }


                        }
                        else {
                            
                            xscalepoint = 0;
                            yscalepoint = 0;
        



                            
                            bMovepintsx = false;
                            bMovepintsy = false;
                            var pointsnew = [];
        

                            /*for (lcounter = 0; lcounter < this.pointlist.length; lcounter++) {
                                for (counter = 0; counter < this.pointlist[lcounter].length; counter++) {
                                    

                                    xscalepoint = (this.pointlist[lcounter][counter].x - this.xoffset) * scalefactor;
                                    xscalepoint += startx;
                                    xscalepoint -= localx;
    
                                    if (xscalepoint > startx && xscalepoint < endx) {
    
                                        bMovepintsx = true;
                                        //this.points[pcounter].x = xscalepoint / scalefactor;
                                    }else{
                                        bMovepintsx = false;
                                        break;
                                    }
    
                                    yscalepoint = (this.pointlist[lcounter][counter].y - this.yoffset) * scalefactor;
                                    yscalepoint += starty;
                                    yscalepoint -= localy;
    
                                    if (yscalepoint > starty && yscalepoint < endy) {

                                        bMovepintsy = true;
                                        
                                    }else{
                                        bMovepintsy = false;
                                        break;
                                    }

                                }
                            }*/

                            if (bLimMarkupExtent) {
                                if (this.bispartofGroup) {
                                    if ((this.groupRect.x - localx) < startx || (this.groupRect.x + localx) > endx) {
                                        xscaled = xscaled + localx;
                                        this.groupRect.x += localx;
                                        this.groupRect.w += localx;
                                    }
                                    if (this.groupRect.y - localy < starty || this.groupRect.y - localy > endy) {
                                        yscaled = yscaled + localy;
                                        this.groupRect.y += localy;
                                        this.groupRect.h += localy;
                                    }
                                }
                                else {
                                    if ((rrect.x - localx) < startx || (rrect.x + localx) > endx) {
                                        
                                        bMovepintsx = false;
                                    }else{

                                        if ((rrect.x + rrect.w) - localx < startx || (rrect.x + rrect.w) - localx > endx) {

                                            bMovepintsx = false;
                                        }else{
                                            bMovepintsx = true;
                                        }
    
                                        
                                    }
                                    if (rrect.y - localy < starty || rrect.y - localy > endy) {

                                        bMovepintsy = false;
                                    }else{

                                        if ((rrect.y + rrect.h) - localy < starty || (rrect.y + rrect.h) - localy > endy) {
                                        
                                            bMovepintsy = false;
                                        }else{
                                            bMovepintsy = true;
                                        }
    
                                    }

                                }
                            }                            

                            if(!bMovepintsy || !bMovepintsx){
                                return;
                            }

                            for (lcounter = 0; lcounter < this.pointlist.length; lcounter++) {
                                for (counter = 0; counter < this.pointlist[lcounter].length; counter++) {
                            
                                    xscalepoint = this.pointlist[lcounter][counter].x * scalefactor;

                                    xscalepoint -= localx;

    
                                    if (bMovepintsx) {
                                        this.pointlist[lcounter][counter].x = xscalepoint / scalefactor;
                                    }
    
    
                                    yscalepoint = this.pointlist[lcounter][counter].y * scalefactor;
                                    
                                    yscalepoint -= localy;
    
                                    if (bMovepintsy) {
                                        
                                        this.pointlist[lcounter][counter].y = yscalepoint / scalefactor;
                                    }
    
    


                                }
                            }
                            
                        }
                    }
                   
                    this.findrectangle();


                    markupobject.setmarkupmodified(true);

                    //move points
                    if(RxCore_GUI_MarkupChanged != undefined){
                        var operation = markupobject.editaction;
                        RxCore_GUI_MarkupChanged.setMarkupSelected(markupobject, operation);
                    }


                     


                };

                this.movePoints = function (x, y) {

                    if(markupobject.pdfannotproxy){
                        return;
                      }
            
                    var localx = x;
                    var localy = y;
                    var bMovepints = true;
                    var bMovepintsx = true;
                    var bMovepintsy = true;
                    switch (DocObj.getCurrentpage().getpageRotation()) {
                        case 0:
                            localx = x;
                            localy = y;
                            break;
                        case 90:
                            localx = y;
                            localy = -x;
                            break;
                        case 180:
                            localx = -x;
                            localy = -y;
                            break;
                        case 270:
                            localx = -y;
                            localy = x;
                            break;
                    }
                    var xscalepoint = 0;
                    var yscalepoint = 0;
                    var scalefactor = 0.0;


                    scalefactor = getCurrentPageScale();
                    scalefactor = scalefactor / markupobject.scaling;

                    if(markupobject.bfor3D){
                        scalefactor = canvasoheight / this.pageheight;
                    }


                    var xscaled = this.x * scalefactor;
                    var yscaled = this.y * scalefactor;
                    var wscaled = this.w * scalefactor;
                    var hscaled = this.h * scalefactor;
                    //var startx = DocObj.getCurrentpage().startx;
                    //var endx = DocObj.getCurrentpage().endx;
                    //var starty = DocObj.getCurrentpage().starty;
                    //var endy = DocObj.getCurrentpage().endy;

                    if(!markupobject.bfor3D){

                        var pagerect = DocObj.getCurrentpage().getpagerectdirect();

                        
                        var startx = pagerect.x;
                        var endx = pagerect.w;
                        var starty = pagerect.y;
                        var endy = pagerect.h;

                        if(pagerect.usepixelscale){
                            var pixelRatio = getPixelRatio();

                            startx = startx * pixelRatio;
                            endx = endx * pixelRatio;
                            starty = starty * pixelRatio;
                            endy = endy * pixelRatio;
    
                        }


                        //var startx = DocObj.getCurrentpage().startx;
                        //var endx = DocObj.getCurrentpage().endx;
                        //var starty = DocObj.getCurrentpage().starty;
                        //var endy = DocObj.getCurrentpage().endy;

                        //console.log(startx, endx, starty, endy);

                    }else{
                        startx = 0;
                        endx = canvasowidth;
                        starty = 0;
                        endy = canvasoheight;
                    }


                    var pcounter = 0;
                    //var within = false;
                    //wscaled = wscaled - localx;
                    //hscaled = hscaled - localy;
                    xscaled = xscaled - localx;
                    yscaled = yscaled - localy;
                    wscaled -= localx;
                    hscaled -= localy;
                    if (this.bispartofGroup) {
                        this.groupRect.x -= localx;
                        this.groupRect.y -= localy;
                        this.groupRect.w -= localx;
                        this.groupRect.h -= localy;
                    }
                    if (bLimMarkupExtent) {
                        if (this.bispartofGroup) {
                            if (this.groupRect.x - localx < startx || this.groupRect.x - localx > endx) {
                                xscaled = xscaled + localx;
                                this.groupRect.x += localx;
                                this.groupRect.w += localx;
                                wscaled += localx;
                                bMovepints = false;
                                bMovepintsx = false;
                            }
                            if (this.groupRect.y - localy < starty || this.groupRect.y - localy > endy) {
                                yscaled = yscaled + localy;
                                hscaled += localy;
                                this.groupRect.y += localy;
                                this.groupRect.h += localy;
                                bMovepints = false;
                                bMovepintsy = false;
                            }
                            if (this.groupRect.w - localx < startx || this.groupRect.w - localx > endx) {
                                wscaled += localx;
                                xscaled = xscaled + localx;
                                this.groupRect.x += localx;
                                this.groupRect.w += localx;
                                bMovepints = false;
                                bMovepintsx = false;
                            }
                            if (this.groupRect.h - localy < starty || this.groupRect.h - localy > endy) {
                                hscaled += localy;
                                yscaled = yscaled + localy;
                                this.groupRect.y += localy;
                                this.groupRect.h += localy;
                                bMovepints = false;
                                bMovepintsy = false;
                            }


                        }
                        else {
                            
                            xscalepoint = 0;
                            yscalepoint = 0;
        
                            
                            bMovepintsx = false;
                            bMovepintsy = false;
                            var pointsnew = [];
        
                            //set new this.x and new this.y

                            //var xscaled = (this.points[0].x - this.xoffset) * scalefactor;
                            //var yscaled = (this.points[0].y - this.yoffset) * scalefactor;
        

                            for (pcounter = 0; pcounter < this.points.length; pcounter++) {
                                /*if (bMovepintsx) {
                                    //this.points[pcounter].x = xscalepoint / scalefactor;
                                }*/

                                xscalepoint = (this.points[pcounter].x - this.xoffset) * scalefactor;
                                xscalepoint += startx;
                                xscalepoint -= localx;

                                if (xscalepoint > startx && xscalepoint < endx) {

                                    bMovepintsx = true;
                                    //this.points[pcounter].x = xscalepoint / scalefactor;
                                }else{
                                    bMovepintsx = false;
                                    break;
                                }



                                yscalepoint = (this.points[pcounter].y - this.yoffset) * scalefactor;
                                yscalepoint += starty;
                                yscalepoint -= localy;

                                if (yscalepoint > starty && yscalepoint < endy) {

                                    bMovepintsy = true;
                                    //this.points[pcounter].y = yscalepoint / scalefactor;
                                }else{
                                    bMovepintsy = false;
                                    break;
                                }


                                /*if (bMovepintsy) {
                                    //this.points[pcounter].y = yscalepoint / scalefactor;
                                }*/
                            }

                            if(!bMovepintsy || !bMovepintsx){
                                return;
                            }
        
                            for (pcounter = 0; pcounter < this.points.length; pcounter++) {

                                xscalepoint = this.points[pcounter].x * scalefactor;
                                //xscalepoint = (this.points[pcounter].x - this.xoffset) * scalefactor;

                                xscalepoint -= localx;

                                if (bMovepintsx) {
                                    this.points[pcounter].x = xscalepoint / scalefactor;
                                }


                                yscalepoint = this.points[pcounter].y * scalefactor;
                                //yscalepoint = (this.points[pcounter].y - this.yoffset) * scalefactor;
                                yscalepoint -= localy;

                                if (bMovepintsy) {
                                    //bMovepintsy = true;
                                    this.points[pcounter].y = yscalepoint / scalefactor;
                                }

                            }
                            
                            
                            /*if (this.xscaled - localx < startx || this.xscaled - localx > endx) {
                                xscaled = xscaled + localx;
                                wscaled += localx;
                                bMovepints = false;
                                bMovepintsx = false;
                            }
                            if (this.yscaled - localy < starty || this.yscaled - localy > endy) {
                                yscaled = yscaled + localy;
                                hscaled += localy;
                                bMovepints = false;
                                bMovepintsy = false;
                            }
                            if (this.wscaled - localx < startx || this.wscaled - localx > endx) {
                                wscaled += localx;
                                xscaled = xscaled + localx;
                                bMovepints = false;
                                bMovepintsx = false;
                            }
                            if (this.hscaled - localy < starty || this.hscaled - localy > endy) {
                                hscaled += localy;
                                yscaled = yscaled + localy;
                                bMovepints = false;
                                bMovepintsy = false;
                            }*/

                        }
                    }
                    //set new this.x and new this.y
                    /*if (bLimMarkupExtent) {
                        if (this.bispartofGroup) {
                        }
                        else {
                        }
                    }*/
                    /*this.x = xscaled / scalefactor;
                    this.y = yscaled / scalefactor;
                    this.w = wscaled / scalefactor;
                    this.h = hscaled / scalefactor;*/
                    
                    //xscalepoint = 0;
                    //yscalepoint = 0;


                    //set new this.x and new this.y
                    /*for (pcounter = 0; pcounter < this.points.length; pcounter++) {
                        if (bMovepintsx) {
                            xscalepoint = this.points[pcounter].x * scalefactor;
                            xscalepoint -= localx;
                            this.points[pcounter].x = xscalepoint / scalefactor;
                        }
                        if (bMovepintsy) {
                            yscalepoint = this.points[pcounter].y * scalefactor;
                            yscalepoint -= localy;
                            this.points[pcounter].y = yscalepoint / scalefactor;
                        }
                    }*/
                    this.findrectangle();


                    markupobject.setmarkupmodified(true);

                    //move points
                    if(RxCore_GUI_MarkupChanged != undefined){
                        var operation = markupobject.editaction;
                        RxCore_GUI_MarkupChanged.setMarkupSelected(markupobject, operation);
                    }


                    /*if (bMovepints) {
                        for (pcounter = 0; pcounter < this.points.length; pcounter++) {
                            xscalepoint = this.points[pcounter].x * scalefactor;
                            yscalepoint = this.points[pcounter].y * scalefactor;
                            xscalepoint -= localx;
                            yscalepoint -= localy;
        
                            this.points[pcounter].x = xscalepoint / scalefactor;
                            this.points[pcounter].y = yscalepoint / scalefactor;
                        }
                        this.findrectangle();
                    }else {
                        this.findrectangle();
                    }*/
                    //this.findrectangle();            
                };
                this.movedirect = function(x, y){

                    if(markupobject.pdfannotproxy){
                        return;
                      }
            

                    var scalefactor = 0.0;

                    scalefactor = getCurrentPageScale();
                    scalefactor = scalefactor / markupobject.scaling;
 
                    var xscaled = this.x * scalefactor;
                    var yscaled = this.y * scalefactor;
                    
                    /*var wscaled = this.w * scalefactor;
                    var hscaled = this.h * scalefactor;
                    var startx = DocObj.getCurrentpage().startx;
                    var endx = DocObj.getCurrentpage().endx;
                    var starty = DocObj.getCurrentpage().starty;
                    var endy = DocObj.getCurrentpage().endy;*/

                    xscaled = x;
                    yscaled = y;

                    this.x = xscaled / scalefactor;
                    this.y = yscaled / scalefactor;


                };
                this.moveShape = function (x, y) {
                    
                    if(markupobject.pdfannotproxy){
                        return;
                      }
            
                    var localx = x;
                    var localy = y;
                    //var bMovepints = true;
                    //var bMovepintsx = true;
                    //var bMovepintsy = true;
                    switch (DocObj.getCurrentpage().getpageRotation()) {
                        case 0:
                            localx = x;
                            localy = y;
                            break;
                        case 90:
                            localx = y;
                            localy = -x;
                            break;
                        case 180:
                            localx = -x;
                            localy = -y;
                            break;
                        case 270:
                            localx = -y;
                            localy = x;
                            break;
                    }
                    var xscalepoint = 0;
                    var yscalepoint = 0;
                    var scalefactor = 0.0;

                    scalefactor = getCurrentPageScale();
                    scalefactor = scalefactor / markupobject.scaling;

                    if(markupobject.bfor3D){
                        scalefactor = canvasoheight / this.pageheight;
                    }

                    //        scalefactor = DocObj.getCurrentpage().dscale / this.scaling;
                    var xscaled = this.x * scalefactor;
                    var yscaled = this.y * scalefactor;
                    var wscaled = this.w * scalefactor;
                    var hscaled = this.h * scalefactor;

                    if(!markupobject.bfor3D){

                        var pagerect = DocObj.getCurrentpage().getpagerectdirect();

                        var startx = pagerect.x;
                        var endx = pagerect.w;
                        var starty = pagerect.y;
                        var endy = pagerect.h;

                        if(pagerect.usepixelscale){
                            var pixelRatio = getPixelRatio();

                            startx = startx * pixelRatio;
                            endx = endx * pixelRatio;
                            starty = starty * pixelRatio;
                            endy = endy * pixelRatio;
    
                        }


                        //var startx = DocObj.getCurrentpage().startx;
                        //var endx = DocObj.getCurrentpage().endx;
                        //var starty = DocObj.getCurrentpage().starty;
                        //var endy = DocObj.getCurrentpage().endy;

                    }else{
                        startx = 0;
                        endx = canvasowidth;
                        starty = 0;
                        endy = canvasoheight;
                    }


                    xscaled = xscaled - localx;
                    yscaled = yscaled - localy;
                    if (this.bispartofGroup) {
                        this.groupRect.x -= localx;
                        this.groupRect.y -= localy;
                        this.groupRect.w -= localx;
                        this.groupRect.h -= localy;
                    }
                    if (markupobject.rotation != 0) {
                        var rrect = markupobject.rotrect(this.xscaled, this.yscaled, this.xscaled + this.wscaled, this.yscaled + this.hscaled);
                    }
                    else {

                        var xrect = Math.min(this.xscaled, this.xscaled + this.wscaled);
                        var yrect = Math.min(this.yscaled, this.yscaled + this.hscaled);
                        var wrect = Math.max(this.xscaled, this.xscaled + this.wscaled) - xrect;
                        var hrect = Math.max(this.yscaled, this.yscaled + this.hscaled) - yrect;


                        rrect = { x: xrect, y: yrect, w: wrect, h: hrect };
                    }

                    //var isimagetype2 = (this.type == 11 && this.subtype == 2);
                    var isimagetype2 = ((this.type == 11 || this.type == 20) && this.subtype == 2);

                    if (bLimMarkupExtent && !isimagetype2) {
                        /*if (markupobject.rotation != 0){
                            
        
                        }else{
                            if ((this.xscaled - localx) < startx || (this.xscaled + localx) > endx) {
                                xscaled = xscaled + localx;
        
                            }
                            if (this.yscaled - localy < starty || this.yscaled - localy > endy) {
                                yscaled = yscaled + localy;
                            }
        
                        }*/
                        if (this.bispartofGroup) {
                            if ((this.groupRect.x - localx) < startx || (this.groupRect.x + localx) > endx) {
                                xscaled = xscaled + localx;
                                this.groupRect.x += localx;
                                this.groupRect.w += localx;
                            }
                            if (this.groupRect.y - localy < starty || this.groupRect.y - localy > endy) {
                                yscaled = yscaled + localy;
                                this.groupRect.y += localy;
                                this.groupRect.h += localy;
                            }
                        }
                        else {
                            if ((rrect.x - localx) < startx || (rrect.x + localx) > endx) {
                                xscaled = xscaled + localx;
                            }
                            if (rrect.y - localy < starty || rrect.y - localy > endy) {
                                yscaled = yscaled + localy;
                            }
                        }
                    }
                    //set new this.x and new this.y
                    this.x = xscaled / scalefactor;
                    this.y = yscaled / scalefactor;
                    if (bLimMarkupExtent && !isimagetype2) {
                        /*if (markupobject.rotation != 0){
                            if ((this.rotatedrect.x + this.rotatedrect.w) - localx < startx || (this.rotatedrect.x + this.rotatedrect.w) - localx > endx) {
                                xscaled = xscaled + localx;
        
                            }
                            if ((this.rotatedrect.y + this.rotatedrect.h) - localy < starty || (this.rotatedrect.y + this.rotatedrect.h) - localy > endy) {
                                yscaled = yscaled + localy;
                            }
        
                        }else{
        
                        }*/
                        if (this.bispartofGroup) {
                            if (this.groupRect.w - localx < startx || this.groupRect.w - localx > endx) {
                                xscaled = xscaled + localx;
                                this.groupRect.x += localx;
                                this.groupRect.w += localx;
                            }
                            if (this.groupRect.h - localy < starty || this.groupRect.h - localy > endy) {
                                yscaled = yscaled + localy;
                                this.groupRect.y += localy;
                                this.groupRect.h += localy;
                            }
                        }
                        else {
                            if ((rrect.x + rrect.w) - localx < startx || (rrect.x + rrect.w) - localx > endx) {
                                xscaled = xscaled + localx;
                            }
                            if ((rrect.y + rrect.h) - localy < starty || (rrect.y + rrect.h) - localy > endy) {
                                yscaled = yscaled + localy;
                            }
                        }
                        this.x = xscaled / scalefactor;
                        this.y = yscaled / scalefactor;
                    }

                    markupobject.setmarkupmodified(true);

                    //move shape
                    if(RxCore_GUI_MarkupChanged != undefined){
                        var operation = markupobject.editaction;
                        RxCore_GUI_MarkupChanged.setMarkupSelected(markupobject, operation);
                    }


                };
                this.move = function (x, y, bmulti) {

                    if(markupobject.pdfannotproxy){
                        return;
                      }

                      if(!markupobject.getselected()){
                        return;
                      }
            

                    //if (this.type == 3 || this.type == 5 || (this.type == 4 && this.subtype == 0) || this.type == 10 || this.type == 11 || this.type == 12) {
                    if (this.type == 3 || this.type == 5 || (this.type == 4 && this.subtype == 0) || this.type == 10 || this.type == 11 || this.type == 12 || this.type == 20) {
                        markupobject.moveShape(x, y);
                        return;
                    }
                    var bspace = (this.type == 8 || this.subtype == 1);


                    if (this.type == 1 || this.type == 8) {
                        if (!bspace || bmovepolyallowed) {
                        //if (!bspace && bmovepolyallowed) {
                            markupobject.movePoints(x, y);
                        }
                        return;
                    }

                    if (this.type == 0) {
                        markupobject.movemultiPoints(x, y);
                        return;
                        
                    }

                    

                    var localx = x;
                    var localy = y;
                    var bMovepints = true;
                    var bMovepintsx = true;
                    var bMovepintsy = true;
                    switch (DocObj.getCurrentpage().getpageRotation()) {
                        case 0:
                            localx = x;
                            localy = y;
                            break;
                        case 90:
                            localx = y;
                            localy = -x;
                            break;
                        case 180:
                            localx = -x;
                            localy = -y;
                            break;
                        case 270:
                            localx = -y;
                            localy = x;
                            break;
                    }
                    var xscalepoint = 0;
                    var yscalepoint = 0;
                    var scalefactor = 0.0;

                    scalefactor = getCurrentPageScale();
                    scalefactor = scalefactor / markupobject.scaling;

                    if(markupobject.bfor3D){
                        scalefactor = canvasoheight / this.pageheight;
                    }

                    //        scalefactor = DocObj.getCurrentpage().dscale / this.scaling;
                    var xscaled = this.x * scalefactor;
                    var yscaled = this.y * scalefactor;
                    var wscaled = this.w * scalefactor;
                    var hscaled = this.h * scalefactor;
                    //this.xscaled = (this.x - this.xoffset) * scalefactor;
                    //this.yscaled = (this.y - this.yoffset) * scalefactor;
                    //var pageobjdim = DocObj.getCurrentpage().getpagerect();

                    if(!markupobject.bfor3D){

                        var pagerect = DocObj.getCurrentpage().getpagerectdirect();
                        var startx = pagerect.x;
                        var endx = pagerect.w;
                        var starty = pagerect.y;
                        var endy = pagerect.h;


                        if(pagerect.usepixelscale){
                            var pixelRatio = getPixelRatio();

                            //startx = pagerect.x * pixelRatio;
                            //endx = (pagerect.x + pagerect.w) * pixelRatio;
                            //starty = pagerect.y * pixelRatio;
                            //endy = (pagerect.y + pagerect.h) * pixelRatio;

                            startx = startx * pixelRatio;
                            endx = endx * pixelRatio;
                            starty = starty * pixelRatio;
                            endy = endy * pixelRatio;

    
                        }


                        //var startx = DocObj.getCurrentpage().startx;
                        //var endx = DocObj.getCurrentpage().endx;
                        //var starty = DocObj.getCurrentpage().starty;
                        //var endy = DocObj.getCurrentpage().endy;
                    }else{
                        startx = 0;
                        endx = canvasowidth;
                        starty = 0;
                        endy = canvasoheight;
                    }

                    /*if (this.bispartofGroup){
        
                    }else{
                        
                    }*/
                    switch (this.type) {
                        case 0: //eraser, pencil, marker
                            var counter = 0;
                            var lcounter = 0;
                            //var within = false;
                            xscaled = xscaled - localx;
                            yscaled = yscaled - localy;
                            wscaled -= localx;
                            hscaled -= localy;
                            /*if (bLimMarkupExtent){
                             if((this.xscaled - localx) < DocObj.getCurrentpage().startx || (this.xscaled - localx) > DocObj.getCurrentpage().endx){
                             xscaled = xscaled + localx;
                             wscaled += localx;
                             bMovepints = false;
        
                             }
                             if(this.yscaled - localy <DocObj.getCurrentpage().starty || this.yscaled - localy > DocObj.getCurrentpage().endy){
                             yscaled = yscaled + localy;
                             hscaled += localy;
                             bMovepints = false;
                             }
                             }*/
                            //set new this.x and new this.y
                            /*if (bLimMarkupExtent){
                             if(this.wscaled - localx < DocObj.getCurrentpage().startx || this.wscaled - localx > DocObj.getCurrentpage().endx){
                             xscaled += localx;
                             wscaled += localx;
                             bMovepints = false;
        
                             }
                             if(this.hscaled - localy <DocObj.getCurrentpage().starty || this.hscaled - localy > DocObj.getCurrentpage().endy){
                             yscaled += localy;
                             hscaled += localy;
                             bMovepints = false;
                             }
                             }*/

                             if(this.transformobj){
                                //this.transformobj = {a : scalew, b : 0, c : 0, d : scaleh, e : xmov, f : ymov};
                                var scalex = this.transformobj.a;
                                var scaley = this.transformobj.d;
                            }else{
                                scalex = 1;
                                scaley = 1;
                            }
        
                            this.x = xscaled / scalefactor;
                            this.y = yscaled / scalefactor;
                            if (this.subtype == 1 || this.subtype == 0) {
                                this.w = wscaled / scalefactor;
                                this.h = hscaled / scalefactor;
                                //set new this.x and new this.y
                                if (bMovepints) {
                                    for (lcounter = 0; lcounter < this.pointlist.length; lcounter++) {
                                        for (counter = 0; counter < this.pointlist[lcounter].length; counter++) {
                                            xscalepoint = this.pointlist[lcounter][counter].x * scalefactor;
                                            yscalepoint = this.pointlist[lcounter][counter].y * scalefactor;
                                            
                                            xscalepoint *= scalex;
                                            yscalepoint *= scaley;
                                            
                                            xscalepoint = xscalepoint - localx;
                                            yscalepoint = yscalepoint - localy;
                                            /*if (bLimMarkupExtent){
                                             if(xscalepoint < DocObj.getCurrentpage().startx || xscalepoint > DocObj.getCurrentpage().endx){
                                             xscalepoint += localx;
        
                                             }
                                             if(yscalepoint <DocObj.getCurrentpage().starty || yscalepoint > DocObj.getCurrentpage().endy){
                                             yscalepoint += localy;
                                             }
                                             }*/
                                            this.pointlist[lcounter][counter].x = (xscalepoint / scalefactor) / scalex;
                                            this.pointlist[lcounter][counter].y = (yscalepoint / scalefactor) / scaley;
                                        }
                                    }
                                }
                                else {
                                    this.findrectangle();
                                }
                                //this.findrectangle();
                            }
                            break;
                        case 2: //Poly curves
                            var curcounter = 0;
                            //var within = false;
                            xscaled = xscaled - localx;
                            yscaled = yscaled - localy;
                            wscaled -= localx;
                            hscaled -= localy;
                            if (bLimMarkupExtent) {
                                if (this.xscaled - localx < startx || this.xscaled - localx > endx) {
                                    xscaled = xscaled + localx;
                                    wscaled += localx;
                                    bMovepints = false;
                                }
                                if (this.yscaled - localy < starty || this.yscaled - localy > endy) {
                                    yscaled = yscaled + localy;
                                    hscaled += localy;
                                    bMovepints = false;
                                }
                            }
                            if (bLimMarkupExtent) {
                                if (wscaled < startx || wscaled > endx) {
                                    xscaled += localx;
                                    wscaled += localx;
                                    bMovepints = false;
                                }
                                if (this.hscaled - localy < starty || this.hscaled - localy > endy) {
                                    yscaled += localy;
                                    hscaled += localy;
                                    bMovepints = false;
                                }
                            }
                            //set new this.x and new this.y
                            this.x = xscaled / scalefactor;
                            this.y = yscaled / scalefactor;
                            this.w = wscaled / scalefactor;
                            this.h = hscaled / scalefactor;
                            //set new this.x and new this.y
                            if (bMovepints) {
                                for (curcounter = 0; curcounter < this.points.length; curcounter++) {
                                    xscalepoint = this.points[curcounter].x * scalefactor;
                                    yscalepoint = this.points[curcounter].y * scalefactor;
                                    xscalepoint -= localx;
                                    yscalepoint -= localy;
                                    /*if (bLimMarkupExtent){
                                     if(xscalepoint < DocObj.getCurrentpage().startx || xscalepoint > DocObj.getCurrentpage().endx){
                                     xscalepoint += localx;
        
        
                                     }
                                     if(yscalepoint <DocObj.getCurrentpage().starty || yscalepoint > DocObj.getCurrentpage().endy){
                                     yscalepoint += localy;
        
                                     }
                                     }*/
                                    this.points[curcounter].x = xscalepoint / scalefactor;
                                    this.points[curcounter].y = yscalepoint / scalefactor;
                                }
                            }
                            else {
                                this.findrectangle();
                            }
                            break;
                        case 4: //Oval
                            if (this.bispartofGroup) {
                                this.groupRect.x -= localx;
                                this.groupRect.y -= localy;
                                this.groupRect.w -= localx;
                                this.groupRect.h -= localy;
                            }
                            xscaled = xscaled - localx;
                            yscaled = yscaled - localy;
                            var xscaledactual = (this.x - this.xoffset) * scalefactor;
                            var yscaledactual = (this.y - this.yoffset) * scalefactor;
                            xscaledactual += startx;
                            yscaledactual += starty;
                            xscaledactual -= localx;
                            yscaledactual -= localy;
                            if (this.subtype == 1) {
                                wscaled -= localx;
                                hscaled -= localy;
                                var xdiff = Math.max(xscaled, wscaled) - Math.min(xscaled, wscaled);
                                var ydiff = Math.max(yscaled, hscaled) - Math.min(yscaled, hscaled);
                                var radius = markupobject.getdiag(xdiff, ydiff);
                                if (bLimMarkupExtent) {
                                    if (this.bispartofGroup) {
                                        if ((this.groupRect.x - localx) < startx || (this.groupRect.x - localx) > endx) {
                                            xscaled += localx;
                                            wscaled += localx;
                                            this.groupRect.x += localx;
                                            this.groupRect.w += localx;
                                        }
                                        if (this.groupRect.y - localy < starty || this.groupRect.y - localy > endy) {
                                            yscaled += localy;
                                            hscaled += localy;
                                            this.groupRect.y += localy;
                                            this.groupRect.h += localy;
                                        }
                                        if (this.groupRect.w - localx < startx || this.groupRect.w - localx > endx) {
                                            xscaled = xscaled + localx;
                                            wscaled += localx;
                                            this.groupRect.x += localx;
                                            this.groupRect.w += localx;
                                        }
                                        if (this.groupRect.h - localy < starty || this.groupRect.h - localy > endy) {
                                            yscaled = yscaled + localy;
                                            hscaled += localy;
                                            this.groupRect.y += localy;
                                            this.groupRect.h += localy;
                                        }
                                    }
                                    else {
                                        if ((xscaledactual - radius) < startx || (xscaledactual + radius) > endx) {
                                            xscaled = xscaled + localx;
                                            wscaled += localx;
                                            //yscaled = yscaled + localy;
                                        }
                                        if ((yscaledactual - radius) < starty || (yscaledactual + radius) > endy) {
                                            //xscaled = xscaled + localx;
                                            yscaled = yscaled + localy;
                                            hscaled += localy;
                                        }
                                    }
                                }
                                this.w = wscaled / scalefactor;
                                this.h = hscaled / scalefactor;
                                this.x = xscaled / scalefactor;
                                this.y = yscaled / scalefactor;
                            }
                            else {
                                if (markupobject.rotation != 0) {
                                    var rrect = markupobject.rotrect(this.xscaled, this.yscaled, this.xscaled + this.wscaled, this.yscaled + this.hscaled);
                                }
                                else {
                                    rrect = { x: this.xscaled, y: this.yscaled, w: this.wscaled, h: this.hscaled };
                                }
                                if (bLimMarkupExtent) {
                                    if (this.bispartofGroup) {
                                        if ((this.groupRect.x - localx) < startx || (this.groupRect.x + localx) > endx) {
                                            xscaled = xscaled + localx;
                                            this.groupRect.x += localx;
                                            this.groupRect.w += localx;
                                        }
                                        if (this.groupRect.y - localy < starty || this.groupRect.y - localy > endy) {
                                            yscaled = yscaled + localy;
                                            this.groupRect.y += localy;
                                            this.groupRect.h += localy;
                                        }
                                    }
                                    else {
                                        if (rrect.x - localx < startx || rrect.x - localx > endx) {
                                            xscaled = xscaled + localx;
                                        }
                                        if (rrect.y - localy < starty || rrect.y - localy > endy) {
                                            yscaled = yscaled + localy;
                                        }
                                    }
                                }
                                //set new this.x and new this.y
                                this.x = xscaled / scalefactor;
                                this.y = yscaled / scalefactor;
                                if (bLimMarkupExtent) {
                                    if (this.bispartofGroup) {
                                        if (this.groupRect.w - localx < startx || this.groupRect.w - localx > endx) {
                                            xscaled = xscaled + localx;
                                            this.groupRect.x += localx;
                                            this.groupRect.w += localx;
                                        }
                                        if (this.groupRect.h - localy < starty || this.groupRect.h - localy > endy) {
                                            yscaled = yscaled + localy;
                                            this.groupRect.y += localy;
                                            this.groupRect.h += localy;
                                        }
                                    }
                                    else {
                                        if ((rrect.x + rrect.w) - localx < startx || (rrect.x + rrect.w) - localx > endx) {
                                            xscaled = xscaled + localx;
                                        }
                                        if ((rrect.y + rrect.h) - localy < starty || (rrect.y + rrect.h) - localy > endy) {
                                            yscaled = yscaled + localy;
                                        }
                                    }
                                    this.x = xscaled / scalefactor;
                                    this.y = yscaled / scalefactor;
                                }
                            }
                            break;
                        case 6: //line arrow etc.
                            if (this.subtype == 6) {
                                break;
                            }
                            if (this.bispartofGroup) {
                                this.groupRect.x -= localx;
                                this.groupRect.y -= localy;
                                this.groupRect.w -= localx;
                                this.groupRect.h -= localy;
                            }
                            xscaled = xscaled - localx;
                            yscaled = yscaled - localy;
                            if (bLimMarkupExtent) {
                                if (this.bispartofGroup) {
                                    if (this.groupRect.x - localx < startx || this.groupRect.x - localx > endx) {
                                        wscaled += localx;
                                        xscaled += localx;
                                        this.groupRect.x += localx;
                                        this.groupRect.w += localx;
                                    }
                                    if (this.groupRect.y - localy < starty || this.groupRect.y - localy > endy) {
                                        hscaled += localy;
                                        yscaled += localy;
                                        this.groupRect.y += localy;
                                        this.groupRect.h += localy;
                                    }
                                }
                                else {
                                    if (this.xscaled - localx < startx || this.xscaled - localx > endx) {
                                        wscaled += localx;
                                        xscaled += localx;
                                    }
                                    if (this.yscaled - localy < starty || this.yscaled - localy > endy) {
                                        hscaled += localy;
                                        yscaled += localy;
                                    }
                                }
                            }
                            //set new this.x and new this.y
                            wscaled -= localx;
                            hscaled -= localy;
                            if (bLimMarkupExtent) {
                                if (this.bispartofGroup) {
                                    if (this.groupRect.w - localx < startx || this.groupRect.w - localx > endx) {
                                        wscaled += localx;
                                        xscaled += localx;
                                        this.groupRect.x += localx;
                                        this.groupRect.w += localx;
                                    }
                                    if (this.groupRect.h - localy < starty || this.groupRect.h - localy > endy) {
                                        hscaled += localy;
                                        yscaled += localy;
                                        this.groupRect.y += localy;
                                        this.groupRect.h += localy;
                                    }
                                }
                                else {
                                    if (this.wscaled - localx < startx || this.wscaled - localx > endx) {
                                        wscaled += localx;
                                        xscaled += localx;
                                    }
                                    if (this.hscaled - localy < starty || this.hscaled - localy > endy) {
                                        hscaled += localy;
                                        yscaled += localy;
                                    }
                                }
                            }
                            this.x = xscaled / scalefactor;
                            this.y = yscaled / scalefactor;
                            this.w = wscaled / scalefactor;
                            this.h = hscaled / scalefactor;
                            break;
                        case 7: //dimension line.
                            if (this.bispartofGroup) {
                                this.groupRect.x -= localx;
                                this.groupRect.y -= localy;
                                this.groupRect.w -= localx;
                                this.groupRect.h -= localy;
                            }
                            xscaled = xscaled - localx;
                            yscaled = yscaled - localy;
                            if (bLimMarkupExtent) {
                                if (this.bispartofGroup) {
                                    if (this.groupRect.x - localx < startx || this.groupRect.x - localx > endx) {
                                        wscaled += localx;
                                        xscaled += localx;
                                        this.groupRect.x += localx;
                                        this.groupRect.w += localx;
                                    }
                                    if (this.groupRect.y - localy < starty || this.groupRect.y - localy > endy) {
                                        hscaled += localy;
                                        yscaled += localy;
                                        this.groupRect.y += localy;
                                        this.groupRect.h += localy;
                                    }
                                }
                                else {
                                    if (this.xscaled - localx < startx || this.xscaled - localx > endx) {
                                        wscaled += localx;
                                        xscaled += localx;
                                    }
                                    if (this.yscaled - localy < starty || this.yscaled - localy > endy) {
                                        hscaled += localy;
                                        yscaled += localy;
                                    }
                                }
                            }
                            //set new this.x and new this.y
                            wscaled -= localx;
                            hscaled -= localy;
                            if (bLimMarkupExtent) {
                                if (this.bispartofGroup) {
                                    if (this.groupRect.w - localx < startx || this.groupRect.w - localx > endx) {
                                        wscaled += localx;
                                        xscaled += localx;
                                        this.groupRect.x += localx;
                                        this.groupRect.w += localx;
                                    }
                                    if (this.groupRect.h - localy < starty || this.groupRect.h - localy > endy) {
                                        hscaled += localy;
                                        yscaled += localy;
                                        this.groupRect.y += localy;
                                        this.groupRect.h += localy;
                                    }
                                }
                                else {
                                    if (this.wscaled - localx < startx || this.wscaled - localx > endx) {
                                        wscaled += localx;
                                        xscaled += localx;
                                    }
                                    if (this.hscaled - localy < starty || this.hscaled - localy > endy) {
                                        hscaled += localy;
                                        yscaled += localy;
                                    }
                                }
                            }
                            this.x = xscaled / scalefactor;
                            this.y = yscaled / scalefactor;
                            this.w = wscaled / scalefactor;
                            this.h = hscaled / scalefactor;
                            break;
                        case 9: //text markup
                            var textwscaled = this.textwidth * scalefactor;
                            if (this.bispartofGroup) {
                                this.groupRect.x -= localx;
                                this.groupRect.y -= localy;
                                this.groupRect.w -= localx;
                                this.groupRect.h -= localy;
                            }
                            xscaled = xscaled - localx;
                            yscaled = yscaled - localy;
                            var switchval = wscaled;
                            switch (this.textrotate / (Math.PI / 180)) {
                                case 0:
                                    break;
                                case 90:
                                    wscaled = hscaled;
                                    hscaled = switchval;
                                    break;
                                case 180:
                                    break;
                                case 270:
                                    wscaled = hscaled;
                                    hscaled = switchval;
                                    break;
                            }
                            if (markupobject.rotation != 0) {
                                rrect = markupobject.rotrect(this.xscaled, this.yscaled, this.xscaled + wscaled, this.yscaled + hscaled);
                            }
                            else {
                                rrect = { x: this.xscaled, y: this.yscaled, w: wscaled, h: hscaled };
                            }
                            if (bLimMarkupExtent) {
                                if (this.bispartofGroup) {
                                    if ((this.groupRect.x - localx) < startx || (this.groupRect.x + localx) > endx) {
                                        xscaled = xscaled + localx;
                                        this.groupRect.x += localx;
                                        this.groupRect.w += localx;
                                    }
                                    if ((this.groupRect.y - localy) < starty || (this.groupRect.y - localy) > endy) {
                                        yscaled = yscaled + localy;
                                        this.groupRect.y += localy;
                                        this.groupRect.h += localy;
                                    }
                                }
                                else {
                                    if (rrect.x - localx < startx || rrect.x - localx > endx) {
                                        xscaled += localx;
                                    }
                                    if (rrect.y - localy < starty || rrect.y - localy > endy) {
                                        yscaled += localy;
                                    }
                                }
                            }
                            if (bLimMarkupExtent) {
                                if (this.bispartofGroup) {
                                    if (this.groupRect.w - localx < startx || this.groupRect.w - localx > endx) {
                                        xscaled = xscaled + localx;
                                        this.groupRect.x += localx;
                                        this.groupRect.w += localx;
                                    }
                                }
                                else {
                                    if ((rrect.x + rrect.w) - localx < startx || (rrect.x + rrect.w) - localx > endx) {
                                        xscaled = xscaled + localx;
                                    }
                                }
                                if (this.subtype == 1 || this.subtype == 2) {
                                    if (this.bispartofGroup) {
                                        if (this.groupRect.h - localy < starty || this.groupRect.h - localy > endy) {
                                            yscaled = yscaled + localy;
                                            this.groupRect.y += localy;
                                            this.groupRect.h += localy;
                                        }
                                    }
                                    else {
                                        if ((rrect.y + rrect.h) - localy < starty || (rrect.y + rrect.h) - localy > endy) {
                                            yscaled = yscaled + localy;
                                        }
                                    }
                                }
                                else {
                                    if (this.bispartofGroup) {
                                        if (this.groupRect.h - localy < starty || this.groupRect.h - localy > endy) {
                                            yscaled = yscaled + localy;
                                            this.groupRect.y += localy;
                                            this.groupRect.h += localy;
                                        }
                                    }
                                    else {
                                        if ((rrect.y - rrect.h) - localy < starty || (rrect.y - rrect.h) - localy > endy) {
                                            yscaled = yscaled + localy;
                                        }
                                    }
                                }
                            }
                            this.x = xscaled / scalefactor;
                            this.y = yscaled / scalefactor;
                            if (this.bhasArrow && this.markupArrowConnected != null) {
                                this.markupArrowConnected.setTextboxConnectionPoint(this.x, this.y, this.w, this.h);
                            }
                            break;
                        case 10:
                            xscaled = xscaled - localx;
                            yscaled = yscaled - localy;
                            if (markupobject.rotation != 0) {
                                rrect = markupobject.rotrect(this.xscaled, this.yscaled, this.xscaled + this.wscaled, this.yscaled + this.hscaled);
                            }
                            else {
                                rrect = { x: this.xscaled, y: this.yscaled, w: this.wscaled, h: this.hscaled };
                            }
                            if (bLimMarkupExtent) {
                                if ((rrect.x - localx) < startx || (rrect.x + localx) > endx) {
                                    xscaled = xscaled + localx;
                                }
                                if (rrect.y - localy < starty || rrect.y - localy > endy) {
                                    yscaled = yscaled + localy;
                                }
                            }
                            //set new this.x and new this.y
                            if (bLimMarkupExtent) {
                                if ((rrect.x + rrect.w) - localx < startx || (rrect.x + rrect.w) - localx > endx) {
                                    xscaled = xscaled + localx;
                                }
                                if ((rrect.y + rrect.h) - localy < starty || (rrect.y + rrect.h) - localy > endy) {
                                    yscaled = yscaled + localy;
                                }
                            }
                            this.x = xscaled / scalefactor;
                            this.y = yscaled / scalefactor;
                            break;
                        case 11:
                            xscaled = xscaled - localx;
                            yscaled = yscaled - localy;
                            if (markupobject.rotation != 0) {
                                rrect = markupobject.rotrect(this.xscaled, this.yscaled, this.xscaled + this.wscaled, this.yscaled + this.hscaled);
                            }
                            else {
                                rrect = { x: this.xscaled, y: this.yscaled, w: this.wscaled, h: this.hscaled };
                            }
                            if (bLimMarkupExtent) {
                                if ((rrect.x - localx) < startx || (rrect.x + localx) > endx) {
                                    xscaled = xscaled + localx;
                                }
                                if (rrect.y - localy < starty || rrect.y - localy > endy) {
                                    yscaled = yscaled + localy;
                                }
                            }
                            //set new this.x and new this.y
                            if (bLimMarkupExtent) {
                                if ((rrect.x + rrect.w) - localx < startx || (rrect.x + rrect.w) - localx > endx) {
                                    xscaled = xscaled + localx;
                                }
                                if ((rrect.y + rrect.h) - localy < starty || (rrect.y + rrect.h) - localy > endy) {
                                    yscaled = yscaled + localy;
                                }
                            }
                            this.x = xscaled / scalefactor;
                            this.y = yscaled / scalefactor;
                            break;
                        case 12: //stamp markup
                            xscaled = xscaled - localx;
                            yscaled = yscaled - localy;
                            if (markupobject.rotation != 0) {
                                rrect = markupobject.rotrect(this.xscaled, this.yscaled, this.xscaled + this.wscaled, this.yscaled + this.hscaled);
                            }
                            else {
                                rrect = { x: this.xscaled, y: this.yscaled, w: this.wscaled, h: this.hscaled };
                            }
                            if (bLimMarkupExtent) {
                                if (rrect.x - localx < startx || rrect.x - localx > endx) {
                                    xscaled += localx;
                                }
                                if (rrect.y - localy < starty || rrect.y - localy > endy) {
                                    yscaled += localy;
                                }
                            }
                            if (bLimMarkupExtent) {
                                if ((rrect.x + rrect.w) - localx < startx || (rrect.x + rrect.w) - localx > endx) {
                                    xscaled = xscaled + localx;
                                }
                                if ((rrect.y + rrect.h) - localy < starty || (rrect.y + rrect.h) - localy > endy) {
                                    yscaled = yscaled + localy;
                                }
                            }
                            this.x = xscaled / scalefactor;
                            this.y = yscaled / scalefactor;
                            break;

                        case 20:
                            xscaled = xscaled - localx;
                            yscaled = yscaled - localy;
                            if (markupobject.rotation != 0) {
                                rrect = markupobject.rotrect(this.xscaled, this.yscaled, this.xscaled + this.wscaled, this.yscaled + this.hscaled);
                            }
                            else {
                                rrect = { x: this.xscaled, y: this.yscaled, w: this.wscaled, h: this.hscaled };
                            }
                            if (bLimMarkupExtent) {
                                if ((rrect.x - localx) < startx || (rrect.x + localx) > endx) {
                                    xscaled = xscaled + localx;
                                }
                                if (rrect.y - localy < starty || rrect.y - localy > endy) {
                                    yscaled = yscaled + localy;
                                }
                            }
                            //set new this.x and new this.y
                            if (bLimMarkupExtent) {
                                if ((rrect.x + rrect.w) - localx < startx || (rrect.x + rrect.w) - localx > endx) {
                                    xscaled = xscaled + localx;
                                }
                                if ((rrect.y + rrect.h) - localy < starty || (rrect.y + rrect.h) - localy > endy) {
                                    yscaled = yscaled + localy;
                                }
                            }
                            this.x = xscaled / scalefactor;
                            this.y = yscaled / scalefactor;
                            break;


                        default:
                            break;
                    }

                    markupobject.setmarkupmodified(true);

                    //move
                    if(RxCore_GUI_MarkupChanged != undefined){
                        var operation = markupobject.editaction;
                        RxCore_GUI_MarkupChanged.setMarkupSelected(markupobject, operation);
                    }


                    /*if (!this.withinbounds(localx,localy) && bLimMarkupExtent){
                     this.x = restorex;
                     this.y =  restorey;
                     this.w = restorew;
                     this.h = restoreh;
                     }*/
                };
                this.setArrowPoint = function(textbox){
                    markupobject.setTextboxConnectionPoint(textbox.x, textbox.y, textbox.w, textbox.h);

                };

                this.setTextboxConnectionPointDraw = function (){

                    var bisarrowtextbox = (markupobject.type == 6 && markupobject.subtype == 6);

                    if (!bisarrowtextbox) {
                        return;
                    }

                    var temptxtscale = markupobject.fixedscaleFactor;
                    temptxtscale *= fTextScale;
                    //textbh = (defaultFont.height / 0.75);
                    var fntheight = defaultFont.height * temptxtscale;

                    var textbox = getCalloutBox(markupobject.w,markupobject.h, temptxtscale);

                    var leftbox = markupobject.w - (textbox.w * 0.5);
                    var rightbox = markupobject.w + (textbox.w * 0.5);

                    var btextboxbetween = (markupobject.x > leftbox  && markupobject.x < rightbox);

                    if (btextboxbetween) {

                        if (textbox.y > markupobject.y) {
                            markupobject.arrowTextPos = TextArrowPosBottom;
                        }else if(textbox.y < markupobject.y){
                            markupobject.arrowTextPos = TextArrowPosTop;
                        }

                    }else if (markupobject.x < leftbox) {
                        markupobject.arrowTextPos = TextArrowPosLeft;
                    }else if (markupobject.x > rightbox){
                        markupobject.arrowTextPos = TextArrowPosRight;
                    }

                    switch (markupobject.pagerotation) {
                        case 90:
                            switch(markupobject.arrowTextPos){
                                case TextArrowPosRight :
                                    markupobject.arrowTextPos = TextArrowPosBottom;
                                    break;
                                case TextArrowPosLeft :
                                    markupobject.arrowTextPos = TextArrowPosTop;
                                    break;
                                case TextArrowPosBottom :                                    
                                    markupobject.arrowTextPos = TextArrowPosLeft;
                                    break;
                                case TextArrowPosTop :                                    
                                    markupobject.arrowTextPos = TextArrowPosRight;
                                    break;
                            }

                            break;
                        case 270:
                            switch(markupobject.arrowTextPos){
                                case TextArrowPosRight :
                                    markupobject.arrowTextPos = TextArrowPosTop;
                                    break;
                                case TextArrowPosLeft :
                                    markupobject.arrowTextPos = TextArrowPosBottom;
                                    break;
                                case TextArrowPosBottom :                                    
                                    markupobject.arrowTextPos = TextArrowPosRight;
                                    break;
                                case TextArrowPosTop :                                    
                                    markupobject.arrowTextPos = TextArrowPosLeft;
                                    break;
                            }
                            break;
                    }                    

                };

                this.setTextboxConnectionPoint = function (x, y, w, h) {
                    var bisarrowtextbox = (markupobject.type == 6 && markupobject.subtype == 6);
                    if (!bisarrowtextbox) {
                        return;
                    }
                    //left side center
                    if (this.x < x) {
                        markupobject.w = x;
                        markupobject.h = y + (h * 0.5);
                        markupobject.arrowTextPos = TextArrowPosLeft;
                    }
                    else if (x < this.x && this.x < (x + w)) {
                        if (y > this.y) {
                            //top side center
                            markupobject.w = x + (w * 0.5);
                            markupobject.h = y;
                            
                            markupobject.arrowTextPos = TextArrowPosBottom;
                        }
                        else if (y < this.y) {
                            //bottom side center
                            markupobject.w = x + (w * 0.5);
                            markupobject.h = y + h;
                            markupobject.arrowTextPos = TextArrowPosTop;
                        }
                    }
                    else if (this.x > x) {
                        //right side center
                        markupobject.w = x + w;
                        markupobject.h = y + (h * 0.5);
                        markupobject.arrowTextPos = TextArrowPosRight;
                    }
                };                
                this.movelabel = function (x, y) {
                    var localx = x;
                    var localy = y;
                    var tempx = 0;
                    var tempy = 0;
                    switch (DocObj.getCurrentpage().getpageRotation()) {
                        case 0:
                            localx = x;
                            localy = y;
                            break;
                        case 90:
                            localx = y;
                            localy = -x;
                            break;
                        case 180:
                            localx = -x;
                            localy = -y;
                            break;
                        case 270:
                            localx = -y;
                            localy = x;
                            break;
                    }
                    /*if (x > this.xscaled && x < this.wscaled) {
                        if (y > this.yscaled && y < this.hscaled) {
                            within = true;
                            markupobject.seteditaction(1);
                        }
                    }*/

                    var scalefactor = getCurrentPageScale();
                    scalefactor = scalefactor / markupobject.scaling;


                    var labeloffsetxscaled = markupobject.labeloffsetx * scalefactor;
                    var labeloffsetyscaled = markupobject.labeloffsety * scalefactor;
                    var bmoveright = (markupobject.labeloffsetx < (markupobject.labeloffsetx - localx));
                    var bmoveleft = (markupobject.labeloffsetx > (markupobject.labeloffsetx - localx));
                    var bmoveup = (markupobject.labeloffsety > (markupobject.labeloffsety - localy));
                    var bmovedown = (markupobject.labeloffsety < (markupobject.labeloffsety - localy));
                    var bleftext = (markupobject.labelRect.x > markupobject.xscaled);
                    var brightext = ((markupobject.labelRect.x + markupobject.labelRect.w) < markupobject.wscaled);
                    var btopext = (markupobject.labelRect.y > markupobject.yscaled);
                    var bbottomext = ((markupobject.labelRect.y + markupobject.labelRect.h) < markupobject.hscaled);
                    if ((bleftext || bmoveright) && (brightext || bmoveleft)) {
                        if ((btopext || bmovedown) && (bbottomext || bmoveup)) {
                            labeloffsetxscaled -= localx;
                            labeloffsetyscaled -= localy;
                            markupobject.labeloffsetx = labeloffsetxscaled / scalefactor;
                            markupobject.labeloffsety = labeloffsetyscaled / scalefactor;
                            //markupobject.labeloffsetx = (markupobject.labeloffsetx / scalefactor) - localx;
                            //markupobject.labeloffsety = (markupobject.labeloffsety / scalefactor) - localy;
                            //markupobject.labeloffsetx -= localx; 
                            //markupobject.labeloffsety -= localy;
                        }
                    }
                    /*if(markupobject.labelRect.x > markupobject.xscaled && (markupobject.labelRect.x + markupobject.labelRect.w) < markupobject.wscaled && (localx > 0)){
                        if(markupobject.labelRect.y > markupobject.yscaled && (markupobject.labelRect.y + markupobject.labelRect.h) < markupobject.hscaled){
                            markupobject.labeloffsetx -= localx;
                            markupobject.labeloffsety -= localy;
                        }
                    }*/

                    markupobject.setmarkupmodified(true);

                    //move label
                    if(RxCore_GUI_MarkupChanged != undefined){
                        var operation = markupobject.editaction;
                        RxCore_GUI_MarkupChanged.setMarkupSelected(markupobject, operation);
                    }


                };
                this.dimextend = function (x, y) {
                    //method only for dimensions.
                    var centercanvX = (canvasowidth / 2);
                    var centercanvY = (canvasoheight / 2);
                    var CanvRotRad = DocObj.getCurrentpage().getpageRotation() * (Math.PI / 180);
                    switch (DocObj.getCurrentpage().getpageRotation()) {
                        case 0:
                            CanvRotRad = DocObj.getCurrentpage().getpageRotation() * (Math.PI / 180);
                            break;
                        case 90:
                            CanvRotRad = (DocObj.getCurrentpage().getpageRotation() + 180) * (Math.PI / 180);
                            break;
                        case 270:
                            CanvRotRad = (DocObj.getCurrentpage().getpageRotation() - 180) * (Math.PI / 180);
                            break;
                        case 180:
                            CanvRotRad = DocObj.getCurrentpage().getpageRotation() * (Math.PI / 180);
                            break;
                    }
                    //var orgmousepoint = {x:x,y:y};
                    //var pagerotation = DocObj.getCurrentpage().getpageRotation();
                    if (DocObj.getCurrentpage().getpageRotation() != 0) {
                        var rotmousepoint = markupobject.getRotatedMarkup(centercanvX, centercanvY, x, y, CanvRotRad);
                        x = rotmousepoint.x;
                        y = rotmousepoint.y;
                    }

                    var scalefactor = getCurrentPageScale();
                    scalefactor = scalefactor / markupobject.scaling;

                    var mp = {
                        x: x,
                        y: y
                    };
                    var lp1 = {
                        x: this.xscaled,
                        y: this.yscaled
                    };
                    var lp2 = {
                        x: this.wscaled,
                        y: this.hscaled
                    };
                    var side = ((mp.x - lp1.x) * (lp2.y - lp1.y)) - ((mp.y - lp1.y) * (lp2.x - lp1.x));
                    //console.log(side);
                    var linedistance = distToSegment(mp, lp1, lp2);
                    if (side > 0) {
                        markupobject.leaderoffset = -(linedistance / scalefactor);
                    }
                    else {
                        markupobject.leaderoffset = (linedistance / scalefactor);
                    }
                    if (linedistance < 1) {
                        markupobject.leaderoffset = 0;
                        //console.log('can get to 0');
                    }
                    ;
                    //this.setDimOffset
                    /*if (distToSegment(mp, lp1, lp2) < 10) {
                        within = true;
                        markupobject.seteditaction(1);
                    }*/

                    markupobject.setmarkupmodified(true);

                    //dimtextend
                    if(RxCore_GUI_MarkupChanged != undefined){
                        var operation = markupobject.editaction;
                        RxCore_GUI_MarkupChanged.setMarkupSelected(markupobject, operation);
                    }


                };
                this.setSize = function (size) {
                    //markupobject.scale(size.width,size.height, 4, context);
                    //current size
                    //var wscaled = this.w * scalefactor;
                    //var hscaled = this.h * scalefactor;
                    wscaled = size.width;
                    hscaled = size.height;
                    if (bLimMarkupExtent) {
                        if ((this.xscaled + wscaled) < startx || (this.xscaled + wscaled) > endx) {
                            wscaled = this.w * scalefactor;
                        }
                        if ((this.yscaled + hscaled) < starty || (this.yscaled + hscaled) > endy) {
                            hscaled = this.h * scalefactor;
                        }
                    }
                    this.w = wscaled / scalefactor;
                    this.h = hscaled / scalefactor;
                };

                this.setinitialtext = function(bset){
                    markupobject.intialtextset = bset;
                };

                this.setText = function (text) {
                    
                    markupobject.text = text;

                    if(RxCore_GUI_MarkupChanged != undefined){
                        var operation = markupobject.editaction;
                        RxCore_GUI_MarkupChanged.setMarkupSelected(markupobject, operation);
                    }
                    

                };

                this.getTextDim = function(){

                    return markupobject.textdim;

                };

                this.setTextboxSize = function(w,h){
                    if(this.type != 9){
                        return;
                    }

                    var scalefactor = getCurrentPageScale();
                    scalefactor = scalefactor / markupobject.scaling;

                    var localw = w;
                    var localh = h;


                    /*switch (pagerotation) {

                        case 0:
                            break;
                        case 90:
                            localw = h;
                            localh = w;
        
                            break;
                        case 180:
                            break;
                        case 270:
                            localw = h;
                            localh = w;

                            break;

                    }*/



                    if(!markupobject.bfor3D){
                        var startx = DocObj.getCurrentpage().startx;
                        var endx = DocObj.getCurrentpage().endx;
                        var starty = DocObj.getCurrentpage().starty;
                        var endy = DocObj.getCurrentpage().endy;
                    }else{
                        startx = 0;
                        endx = canvasowidth;
                        starty = 0;
                        endy = canvasoheight;
                        scalefactor = canvasoheight / this.pageheight;
                    }

                    var xscaled = this.x * scalefactor;
                    var yscaled = this.y * scalefactor;
                    var wscaled = this.w * scalefactor;
                    var hscaled = this.h * scalefactor;

                    if (this.subtype == 1 || this.subtype == 2) {


                        if(localw != null){
                            wscaled = localw;
                        }

                        if(localh != null){
                            hscaled = localh;
                        }

                        if (bLimMarkupExtent) {
                            if ((this.xscaled + wscaled) < startx || (this.xscaled + wscaled) > endx) {
                                wscaled = this.wscaled;
                            }
                            if ((this.yscaled + hscaled) < starty || (this.yscaled + hscaled) > endy) {
                                hscaled = this.hscaled;
                            }
                        }
                        if (wscaled < 0) {
                            wscaled = 1;
                        }
                        if (hscaled < 0) {
                            hscaled = 1;
                        }
                        this.w = wscaled / scalefactor;
                        this.h = hscaled / scalefactor;

                        
                    }
                    else {
                        var textscaled = this.textheight * scalefactor;
                        var textwscaled = this.textwidth * scalefactor;


                        switch (this.textrotate / (Math.PI / 180)) {
                            case 0:
                                break;
                            case 90:
                                break;
                            case 180:
                                break;
                            case 270:
                                break;
                        }
                        //textscaled = textscaled - (localy * 0.25);
                        //textwscaled = textwscaled - (localx * 0.25);


                        //wscaled -= (localx * 0.25);
                        //hscaled -= (localy * 0.25);


                        this.font.setHeight(textscaled / scalefactor);
                        ctx.font = this.font.fontstring;
                        var dimt = ctx.measureText(this.text);
                        var dimtextwidth = dimt.width;


                        if (bLimMarkupExtent) {
                            if ((this.xscaled + wscaled) < startx || (this.xscaled + wscaled) > endx) {
                                //wscaled = wscaled + localx;
                                //textwscaled = textwscaled + localx;
                                //textscaled = textscaled + localy;

                                wscaled = this.wscaled;
                            }
                            if ((this.yscaled - hscaled) < starty || (this.yscaled - hscaled) > endy) {
                                //hscaled = hscaled + localy;
                                //textscaled = textscaled + localy;
                                
                                hscaled = this.hscaled;
                            }
                        }
                        this.w = wscaled / scalefactor;
                        this.h = hscaled / scalefactor;
                        this.textheight = textscaled / scalefactor;
                        this.textwidth = textwscaled / scalefactor;
                    }
                    if (this.bhasArrow && this.markupArrowConnected != null) {
                        //var arrowmobj = DocObj.getmarkupbynumber(this.markupArrowConnected);
                        //DocObj.markuplist[arrowmobj].setTextboxConnectionPoint(this.x, this.y, this.w, this.h);
                        this.markupArrowConnected.setTextboxConnectionPoint(this.x, this.y, this.w, this.h);
                    }                    
                    
                    //markupobject.

                    //markupobject.editTextmarkupRect();
                    //GUI_TextInput

                    return getTextrect();



                };                


                this.scale = function (x, y, corner, ctx) {
                    

                    // Store original dimensions
                    const originalWidth = this.w;
                    const originalHeight = this.h;
                    const aspectRatio = originalWidth / originalHeight;


                    if(markupobject.pdfannotproxy){
                        return;
                    }
           
                    if(!markupobject.getselected()){
                        return;
                    }

                    
                    var localx = x;
                    var localy = y;
                    var tempx = 0;
                    var tempy = 0;
                    switch (DocObj.getCurrentpage().getpageRotation()) {
                        case 0:
                            localx = x;
                            localy = y;
                            break;
                        case 90:
                            localx = y;
                            localy = -x;
                            break;
                        case 180:
                            localx = -x;
                            localy = -y;
                            break;
                        case 270:
                            localx = -y;
                            localy = x;
                            break;
                    }
                    if (markupobject.rotation != 0) {
                        tempx = localx;
                        tempy = localy;
                        var calcrot = -markupobject.rotation;
                        localx = (tempx * Math.cos(calcrot)) - (tempy * Math.sin(calcrot));
                        localy = (tempx * Math.sin(calcrot)) + (tempy * Math.cos(calcrot));
                    }
                    //var pageobjdim = DocObj.getCurrentpage().getpagerect();

                    var scalefactor = getCurrentPageScale();
                    scalefactor = scalefactor / markupobject.scaling;

                    if(!markupobject.bfor3D){

                        var pagerect = DocObj.getCurrentpage().getpagerectdirect();

                        var startx = pagerect.x;
                        var endx = pagerect.w;
                        var starty = pagerect.y;
                        var endy = pagerect.h;

                        if(pagerect.usepixelscale){
                            var pixelRatio = getPixelRatio();

                            startx = startx * pixelRatio;
                            endx = endx * pixelRatio;
                            starty = starty * pixelRatio;
                            endy = endy * pixelRatio;
    
                        }


                        //var startx = DocObj.getCurrentpage().startx;
                        //var endx = DocObj.getCurrentpage().endx;
                        //var starty = DocObj.getCurrentpage().starty;
                        //var endy = DocObj.getCurrentpage().endy;

                    }else{
                        startx = 0;
                        endx = canvasowidth;
                        starty = 0;
                        endy = canvasoheight;
                        scalefactor = canvasoheight / this.pageheight;
                    }

                    
                    var localpagerect = {startx : startx, endx : endx, starty : starty, endy : endy};

                    /*var startx = pageobjdim.x;
                    var endx = pageobjdim.x + pageobjdim.w;
                    var starty = pageobjdim.y;
                    var endy = pageobjdim.y + pageobjdim.h;*/
                    /*
                     Andriy: Unused variables
        
                     var xscalepoint = 0;
                     var yscalepoint = 0;*/

                     //var scalefactor = getCurrentPageScale();
                     //scalefactor = scalefactor / markupobject.scaling;

                    //var scalefactor = DocObj.getCurrentpage().dscale / this.scaling;
                    //var scalefactor = dscale / this.scaling;
                    var xscaled = this.x * scalefactor;
                    var yscaled = this.y * scalefactor;
                    var wscaled = this.w * scalefactor;
                    var hscaled = this.h * scalefactor;
                    /*
                     Unused variables
                     var restorex = this.x;
                     var restorey = this.y;
                     var restorew = this.w;
                     var restoreh = this.h;*/


                   if (markupobject.rotation != 0) {
                        var rrect = markupobject.rotrect(this.xscaled, this.yscaled, this.xscaled + this.wscaled, this.yscaled + this.hscaled);
                    }
                    else {
                        rrect = { x: this.xscaled, y: this.yscaled, w: this.wscaled, h: this.hscaled };
                    }

                    var type = this.type;
                    if(this.type == 20 || this.type == 21){
                        type = 11;
                    }

                    
                    switch (type) {
                        case 0: //pencil, marker, eraser, (currently not in use)

                        /*var origxscaled = this.origx * scalefactor;
                        var origyscaled = this.origy * scalefactor;
                        var origwscaled = this.origw * scalefactor;
                        var orighscaled = this.origh * scalefactor;
                        
                        var widthscaledorig = origwscaled - origxscaled;
                        var heightscaledorig = orighscaled - origyscaled;*/


                        /*switch (corner) {
                            case 4:
                                wscaled -= localx;
                                hscaled -= localy;


                                var widthscaled = wscaled - this.xscaled;
                                var heightscaled = hscaled - this.yscaled;

                                var scalew = (widthscaled / widthscaledorig);
                                var scaleh = (heightscaled / heightscaledorig);
    
                                var xmov = (1 - scalew) * this.xscaled; 
                                var ymov = (1 - scaleh) * this.yscaled;
                                //var xmov = 0; 
                                //var ymov = 0;
    
                                //console.log(xmov);
                                //console.log(ymov);
    
                                this.transformobj = {a : scalew, b : 0, c : 0, d : scaleh, e : xmov, f : ymov};
    
                                this.w = wscaled / scalefactor;
                                this.h = hscaled / scalefactor;


                                break;


                        }*/
                            //corner 4 
                            //wscaled -= localx;
                            //hscaled -= localy;

                            //wscaled *= this.transformobj.a;
                            //hscaled *= this.transformobj.d;

                            /*if (bLimMarkupExtent) {
                                if ((this.wscaled) - localx < startx || (this.wscaled) - localx > endx) {
                                    wscaled = wscaled + localx;
                                }
                                if ((this.hscaled) - localy < starty || (this.hscaled) - localy > endy) {
                                    hscaled = hscaled + localy;
                                }
                            }*/
                            



                            //var widthscaledchanged = (wscaled - this.xscaled) * this.transformobj.a;
                            //var heightscaledchanged = (hscaled - this.yscaled) * this.transformobj.d;

                            //var scalew = this.w;
                            //var scaleh = this.h;
                            
                            //this.w = wscaled / scalefactor;
                            //this.h = hscaled / scalefactor;

                            /*a	Horizontal scaling	
                            b	Horizontal skewing	
                            c	Vertical skewing	
                            d	Vertical scaling	
                            e	Horizontal moving	
                            f	Vertical moving*/
                            //ctx.transform(1, 0.5, -0.5, 1, 30, 10)

                            //var scalefactor = dscale / this.scaling;
                            //var counter = 0;
                            /*for (counter=0;counter<this.points.length;counter++){
                             xscalepoint = this.points[counter].x * scalefactor;
                             yscalepoint = this.points[counter].y * scalefactor;
                             this.points[counter].x = xscalepoint - localx;
                             this.points[counter].y = yscalepoint - localy;
                             }*/
                            //var xscaled = this.x * scalefactor;
                            //var yscaled = this.y * scalefactor;
                            //var wscaled = this.w * scalefactor;
                            //var hscaled = this.h * scalefactor;
                            //wscaled -= localx;
                            //hscaled -= localy;
                            //this.w = wscaled/scalefactor;
                            //this.h = hscaled/scalefactor;
                            break;
                        case 1: //polygon, (currently not in use)
                            break;
                        case 2: //polycurves, (currently not in use)
                            break;
                        case 3: //rectangle

                             //rrect = { x: this.xscaled, y: this.yscaled, w: this.wscaled, h: this.hscaled };
                            switch (corner) {
                                case 4:

                                    rrect.w -= localx;
                                    rrect.h -= localy;

                                    if (bLimMarkupExtent) {


                                        if(MousePosdrwextRectPage(rrect, localpagerect)){
                                            wscaled -= localx;
                                            hscaled -= localy;

                                        }
                                        /*if(MousePosdrwextRect(rrect)){
                                            wscaled -= localx;
                                            hscaled -= localy;

                                        }*/

                                        /*if ((rrect.x + rrect.w) - localx < startx || (rrect.x + rrect.w) - localx > endx) {
                                            wscaled = wscaled + localx;
                                        }
                                        if ((rrect.y + rrect.h) - localy < starty || (rrect.y + rrect.h) - localy > endy) {
                                            hscaled = hscaled + localy;
                                        }*/
                                    }

                                    /*if (bLimMarkupExtent) {
                                        if ((this.xscaled + this.wscaled) - localx < startx || (this.xscaled + this.wscaled) - localx > endx) {
                                            wscaled = wscaled + localx;
                                        }
                                        if ((this.yscaled + this.hscaled) - localy < starty || (this.yscaled + this.hscaled) - localy > endy) {
                                            hscaled = hscaled + localy;
                                        }
                                    }*/
                                    this.w = wscaled / scalefactor;
                                    this.h = hscaled / scalefactor;
                                    break;
                                case 1:


                                    rrect.x -= localx;
                                    rrect.y -= localy;
                                    rrect.w += localx;
                                    rrect.h += localy;





                                    if (bLimMarkupExtent) {

                                        
                                        if(MousePosdrwextRectPage(rrect, localpagerect)){
                                            xscaled -= localx;
                                            yscaled -= localy;
                                            wscaled += localx;
                                            hscaled += localy;


                                        }
                                        /*if(MousePosdrwextRect(rrect)){
                                            xscaled -= localx;
                                            yscaled -= localy;
                                            wscaled += localx;
                                            hscaled += localy;

                                        }*/


                                        /*if (this.xscaled - localx < startx || this.xscaled - localx > endx) {
                                            xscaled = xscaled + localx;
                                            wscaled = wscaled - localx;
                                        }
                                        if ((this.xscaled + this.wscaled) - localx < startx || (this.xscaled + this.wscaled) - localx > endx) {
                                            wscaled = wscaled - localx;
                                            xscaled = xscaled + localx;
                                        }
                                        if (this.yscaled - localy < starty || this.yscaled - localy > endy) {
                                            yscaled = yscaled + localy;
                                            hscaled = hscaled - localy;
                                        }
                                        if ((this.yscaled + this.hscaled) - localy < starty || (this.yscaled + this.hscaled) - localy > endy) {
                                            yscaled = yscaled + localy;
                                            hscaled = hscaled - localy;
                                        }*/
                                    }
                                    this.x = xscaled / scalefactor;
                                    this.y = yscaled / scalefactor;
                                    this.w = wscaled / scalefactor;
                                    this.h = hscaled / scalefactor;
                                    break;
                                case 3:


                                    rrect.x -= localx;
                                    //rrect.y -= localy;
                                    rrect.w += localx;
                                    rrect.h += localy;




                                    if (bLimMarkupExtent) {


                                        if(MousePosdrwextRectPage(rrect, localpagerect)){

                                            xscaled -= localx;
                                            //yscaled -= localy;
                                            wscaled += localx;
                                            hscaled -= localy;

                                        }
                                        /*if(MousePosdrwextRect(rrect)){
                                           xscaled -= localx;
                                            yscaled -= localy;
                                            wscaled += localx;
                                            hscaled += localy;

                                            xscaled -= localx;
                                            //yscaled -= localy;
                                            wscaled += localx;
                                            hscaled -= localy;
        

                                        }*/


                                        /*if (this.xscaled - localx < startx || this.xscaled - localx > endx) {
                                            xscaled = xscaled + localx;
                                            wscaled = wscaled - localx;
                                        }
                                        if ((this.xscaled + this.wscaled) - localx < startx || (this.xscaled + this.wscaled) - localx > endx) {
                                            xscaled = xscaled + localx;
                                            wscaled = wscaled - localx;
                                        }
                                        if(this.yscaled - localy <DocObj.getCurrentpage().starty || this.yscaled - localy > DocObj.getCurrentpage().endy){
                                         yscaled = yscaled + localy;
                                         //hscaled = hscaled - localy;
                                         }
                                        if ((this.yscaled + this.hscaled) - localy < starty || (this.yscaled + this.hscaled) - localy > endy) {
                                            hscaled = hscaled + localy;
                                            yscaled = yscaled - localy;
                                        }*/
                                    }
                                    this.x = xscaled / scalefactor;
                                    //this.y = yscaled/scalefactor;
                                    this.w = wscaled / scalefactor;
                                    this.h = hscaled / scalefactor;
                                    break;
                                case 2:

                                    //rrect.x -= localx;
                                    rrect.y -= localy;
                                    rrect.w += localx;
                                    rrect.h += localy;

                                    if (bLimMarkupExtent) {


                                        if(MousePosdrwextRectPage(rrect, localpagerect)){
                                            yscaled -= localy;
                                            wscaled -= localx;
                                            hscaled += localy;

                                        }


                                        /*if(MousePosdrwextRect(rrect)){


                                            //xscaled -= localx;
                                            yscaled -= localy;
                                            wscaled -= localx;
                                            hscaled += localy;
        

                                        }*/

                                        /*if(this.xscaled - localx < DocObj.getCurrentpage().startx || this.xscaled - localx > DocObj.getCurrentpage().endx){
                                         xscaled = xscaled + localx;
                                         wscaled = wscaled - localx;
        
                                         }
                                        if ((this.xscaled + this.wscaled) - localx < startx || (this.xscaled + this.wscaled) - localx > endx) {
                                            xscaled = xscaled - localx;
                                            wscaled = wscaled + localx;
                                        }
                                        if (this.yscaled - localy < starty || this.yscaled - localy > endy) {
                                            yscaled = yscaled + localy;
                                            hscaled = hscaled - localy;
                                        }
                                        if ((this.yscaled + this.hscaled) - localy < starty || (this.yscaled + this.hscaled) - localy > endy) {
                                            hscaled = hscaled - localy;
                                            yscaled = yscaled + localy;
                                        }*/
                                    }
                                    //this.x = xscaled/scalefactor;
                                    this.y = yscaled / scalefactor;
                                    this.w = wscaled / scalefactor;
                                    this.h = hscaled / scalefactor;
                                    break;
                            }
                            break;
                        case 4: //Oval
                            switch (corner) {
                                case 4:
                                    if (this.subtype == 1) {
                                        var wscaledactual = (this.w - this.xoffset) * scalefactor;
                                        var hscaledactual = (this.h - this.yoffset) * scalefactor;
                                        wscaledactual += startx;
                                        hscaledactual += starty;
                                        wscaledactual -= localx;
                                        hscaledactual -= localy;
                                        wscaled -= localx;
                                        hscaled -= localy;
                                        var xdiff = Math.max(this.xscaled, this.wscaled) - Math.min(this.xscaled, this.wscaled);
                                        var ydiff = Math.max(this.yscaled, this.hscaled) - Math.min(this.yscaled, this.hscaled);
                                        var newxdiff = Math.max(this.xscaled, wscaledactual) - Math.min(this.xscaled, wscaledactual);
                                        var newydiff = Math.max(this.yscaled, hscaledactual) - Math.min(this.yscaled, hscaledactual);
                                        var oldradius = markupobject.getdiag(xdiff, ydiff);
                                        var curradius = markupobject.getdiag(newxdiff, newydiff);
                                        //add 10 percent to prevent moving outside.
                                        //oldradius = oldradius + (oldradius * 0.1);
                                        if (bLimMarkupExtent) {
                                            if ((this.xscaled - curradius) < startx || (this.xscaled + curradius) > endx) {
                                                wscaled = wscaled + localx;
                                                hscaled = hscaled + localy;
                                                if (curradius < oldradius) {
                                                    //proceed
                                                }
                                                else {
                                                }
                                            }
                                            if ((this.yscaled - curradius) < starty || (this.yscaled + curradius) > endy) {
                                                wscaled = wscaled + localx;
                                                hscaled = hscaled + localy;
                                                if (curradius < oldradius) {
                                                    //proceed
                                                }
                                                else {
                                                }
                                            }
                                        }
                                        markupobject.radiusCallback(this.x, this.y, this.w, this.h, true);
                                        //this.radiusCallback = function(x,y, w, h, useabs){                            
                                    }
                                    else {


                                        rrect.w -= localx;
                                        rrect.h -= localy;
    

                                        if (bLimMarkupExtent) {
    

                                            if(MousePosdrwextRectPage(rrect, localpagerect)){
                                                wscaled -= localx;
                                                hscaled -= localy;

                                            }

                                            /*if(MousePosdrwextRect(rrect)){
                                                wscaled -= localx;
                                                hscaled -= localy;
                                            }*/

                                        }
    

                                        /*wscaled -= localx;
                                        hscaled -= localy;
                                        if (bLimMarkupExtent) {
                                            if ((this.xscaled + this.wscaled) - localx < startx || (this.xscaled + this.wscaled) - localx > endx) {
                                                wscaled = wscaled + localx;
                                            }
                                            if ((this.yscaled + this.hscaled) - localy < starty || (this.yscaled + this.hscaled) - localy > endy) {
                                                hscaled = hscaled + localy;
                                            }
                                        }*/
                                    }
                                    this.w = wscaled / scalefactor;
                                    this.h = hscaled / scalefactor;
                                    break;
                                case 1:


                                    rrect.x -= localx;
                                    rrect.y -= localy;
                                    rrect.w += localx;
                                    rrect.h += localy;


                                    if (bLimMarkupExtent) {


                                        if(MousePosdrwextRectPage(rrect, localpagerect)){
                                            xscaled -= localx;
                                            yscaled -= localy;
                                            wscaled += localx;
                                            hscaled += localy;

                                        }

                                        /*if(MousePosdrwextRect(rrect)){
                                            xscaled -= localx;
                                            yscaled -= localy;
                                            wscaled += localx;
                                            hscaled += localy;
                                        }*/

                                    }
                                    /*xscaled -= localx;
                                    yscaled -= localy;
                                    wscaled += localx;
                                    hscaled += localy;
                                    if (bLimMarkupExtent) {
                                        if (this.xscaled - localx < startx || this.xscaled - localx > endx) {
                                            xscaled = xscaled + localx;
                                            wscaled -= localx;
                                        }
                                        if (this.yscaled - localy < starty || this.yscaled - localy > endy) {
                                            yscaled = yscaled + localy;
                                            hscaled -= localy;
                                        }
                                    }*/
                                    this.x = xscaled / scalefactor;
                                    this.y = yscaled / scalefactor;
                                    this.w = wscaled / scalefactor;
                                    this.h = hscaled / scalefactor;
                                    break;
                                case 3:


                                    rrect.x -= localx;
                                    //rrect.y -= localy;
                                    rrect.w += localx;
                                    rrect.h -= localy;


                                    if (bLimMarkupExtent) {


                                        if(MousePosdrwextRectPage(rrect, localpagerect)){
                                            xscaled -= localx;
                                            //yscaled -= localy;
                                            wscaled += localx;
                                            hscaled -= localy;

                                        }

                                        /*if(MousePosdrwextRect(rrect)){
                                            xscaled -= localx;
                                            //yscaled -= localy;
                                            wscaled += localx;
                                            hscaled -= localy;
                                        }*/

                                    }


                                    /*if (bLimMarkupExtent) {
                                        if (this.xscaled - localx < startx || this.xscaled - localx > endx) {
                                            xscaled = xscaled + localx;
                                            wscaled = wscaled - localx;
                                        }
                                        if ((this.xscaled + this.wscaled) - localx < startx || (this.xscaled + this.wscaled) - localx > endx) {
                                            xscaled = xscaled + localx;
                                            wscaled = wscaled - localx;
                                        }
                                        if(this.yscaled - localy <DocObj.getCurrentpage().starty || this.yscaled - localy > DocObj.getCurrentpage().endy){
                                         yscaled = yscaled + localy;
                                         //hscaled = hscaled - localy;
                                         }
                                        if ((this.yscaled + this.hscaled) - localy < starty || (this.yscaled + this.hscaled) - localy > endy) {
                                            hscaled = hscaled + localy;
                                            yscaled = yscaled - localy;
                                        }
                                    }*/
                                    this.x = xscaled / scalefactor;
                                    //this.y = yscaled/scalefactor;
                                    this.w = wscaled / scalefactor;
                                    this.h = hscaled / scalefactor;
                                    break;
                                case 2:
                                    //rrect.x -= localx;
                                    rrect.y -= localy;
                                    rrect.w -= localx;
                                    rrect.h += localy;


                                    if (bLimMarkupExtent) {

                                        if(MousePosdrwextRectPage(rrect, localpagerect)){
                                            //xscaled -= localx;
                                            yscaled -= localy;
                                            wscaled -= localx;
                                            hscaled += localy;

                                        }

                                        /*if(MousePosdrwextRect(rrect)){
                                            //xscaled -= localx;
                                            yscaled -= localy;
                                            wscaled -= localx;
                                            hscaled += localy;
                                        }*/

                                    }

                                    /*if (bLimMarkupExtent) {
                                        if(this.xscaled - localx < DocObj.getCurrentpage().startx || this.xscaled - localx > DocObj.getCurrentpage().endx){
                                         xscaled = xscaled + localx;
                                         wscaled = wscaled - localx;
        
                                         }
                                        if ((this.xscaled + this.wscaled) - localx < startx || (this.xscaled + this.wscaled) - localx > endx) {
                                            xscaled = xscaled - localx;
                                            wscaled = wscaled + localx;
                                        }
                                        if (this.yscaled - localy < starty || this.yscaled - localy > endy) {
                                            yscaled = yscaled + localy;
                                            hscaled = hscaled - localy;
                                        }
                                        if ((this.yscaled + this.hscaled) - localy < starty || (this.yscaled + this.hscaled) - localy > endy) {
                                            hscaled = hscaled - localy;
                                            yscaled = yscaled + localy;
                                        }
                                    }*/
                                    //this.x = xscaled/scalefactor;
                                    this.y = yscaled / scalefactor;
                                    this.w = wscaled / scalefactor;
                                    this.h = hscaled / scalefactor;
                                    break;
                            }
                            /*if(corner == 4){
                             wscaled -= localx;
                             hscaled -= localy;
        
                             if (bLimMarkupExtent){
                             if((this.xscaled + this.wscaled) - localx < DocObj.getCurrentpage().startx || (this.xscaled + this.wscaled) - localx > DocObj.getCurrentpage().endx){
                             wscaled = wscaled + localx;
        
                             }
                             if((this.yscaled + this.hscaled)-localy <DocObj.getCurrentpage().starty || (this.yscaled + this.hscaled)-localy > DocObj.getCurrentpage().endy){
                             hscaled = hscaled + localy;
                             }
        
                             }
        
                             this.w = wscaled/scalefactor;
                             this.h = hscaled/scalefactor;
        
                             }
                             if (corner == 1){
                             xscaled -= localx;
                             yscaled -= localy;
                             wscaled += localx;
                             hscaled += localy;
        
                             if (bLimMarkupExtent){
                             if(this.xscaled - localx < DocObj.getCurrentpage().startx || this.xscaled - localx > DocObj.getCurrentpage().endx){
                             xscaled = xscaled + localx;
        
                             }
                             if(this.yscaled - localy <DocObj.getCurrentpage().starty || this.yscaled - localy > DocObj.getCurrentpage().endy){
                             yscaled = yscaled + localy;
                             }
        
                             }
        
                             this.x = xscaled/scalefactor;
                             this.y = yscaled/scalefactor;
                             this.w = wscaled/scalefactor;
                             this.h = hscaled/scalefactor;
        
                             }*/
                            break;
                        case 5: //Cloud
                            /*if(corner == 4){
                             wscaled -= localx;
                             hscaled -= localy;
                             if (bLimMarkupExtent){
                             if((this.xscaled + this.wscaled) - localx < DocObj.getCurrentpage().startx || (this.xscaled + this.wscaled) - localx > DocObj.getCurrentpage().endx){
                             wscaled = wscaled + localx;
        
                             }
                             if((this.yscaled + this.hscaled)-localy <DocObj.getCurrentpage().starty || (this.yscaled + this.hscaled)-localy > DocObj.getCurrentpage().endy){
                             hscaled = hscaled + localy;
                             }
        
                             }
        
                             this.w = wscaled/scalefactor;
                             this.h = hscaled/scalefactor;
        
                             }
                             if (corner == 1){
                             xscaled -= localx;
                             yscaled -= localy;
                             wscaled += localx;
                             hscaled += localy;
        
                             if (bLimMarkupExtent){
                             if(this.xscaled - localx < DocObj.getCurrentpage().startx || this.xscaled - localx > DocObj.getCurrentpage().endx){
                             xscaled = xscaled + localx;
        
                             }
                             if(this.yscaled - localy <DocObj.getCurrentpage().starty || this.yscaled - localy > DocObj.getCurrentpage().endy){
                             yscaled = yscaled + localy;
                             }
        
                             }
        
                             this.x = xscaled/scalefactor;
                             this.y = yscaled/scalefactor;
                             this.w = wscaled/scalefactor;
                             this.h = hscaled/scalefactor;
        
                             }*/
                            switch (corner) {
                                case 4:

                                    //rrect.x -= localx;
                                    //rrect.y -= localy;
                                    rrect.w -= localx;
                                    rrect.h -= localy;


                                    if (bLimMarkupExtent) {

                                        if(MousePosdrwextRectPage(rrect, localpagerect)){
                                            wscaled -= localx;
                                            hscaled -= localy;

                                        }
                                        
                                        /*if(MousePosdrwextRect(rrect)){
                                            wscaled -= localx;
                                            hscaled -= localy;
                                        }*/

                                    }


                                    /*if (bLimMarkupExtent) {
                                        if ((this.xscaled + this.wscaled) - localx < startx || (this.xscaled + this.wscaled) - localx > endx) {
                                            wscaled = wscaled + localx;
                                        }
                                        if ((this.yscaled + this.hscaled) - localy < starty || (this.yscaled + this.hscaled) - localy > endy) {
                                            hscaled = hscaled + localy;
                                        }
                                    }*/
                                    this.w = wscaled / scalefactor;
                                    this.h = hscaled / scalefactor;
                                    /*if(wscaled/scalefactor < 0){
                                     var x2 = this.x + wscaled/scalefactor;
                                     tempx = this.x;
                                     this.x += wscaled/scalefactor;
                                     x2 = tempx;
                                     this.w = x2 - tempx;
                                     }else{
                                     this.w = wscaled/scalefactor;
                                     }
                                     if(hscaled/scalefactor < 0){
                                     var y2 = this.y + hscaled/scalefactor;
                                     tempy = this.y;
                                     this.y += hscaled/scalefactor;
                                     this.h = y2 - tempy;
                                     }else{
                                     this.h = hscaled/scalefactor;
                                     }*/
                                    //console.log(this.w);
                                    //console.log(this.x);
                                    break;
                                case 1:

                                    rrect.x -= localx;
                                    rrect.y -= localy;
                                    rrect.w += localx;
                                    rrect.h += localy;


                                    if (bLimMarkupExtent) {


                                        if(MousePosdrwextRectPage(rrect, localpagerect)){
                                            xscaled -= localx;
                                            yscaled -= localy;
                                            wscaled += localx;
                                            hscaled += localy;

                                        }

                                        /*if(MousePosdrwextRect(rrect)){
                                            xscaled -= localx;
                                            yscaled -= localy;
                                            wscaled += localx;
                                            hscaled += localy;
                                        }*/

                                    }


                                    /*if (bLimMarkupExtent) {
                                        if (this.xscaled - localx < startx || this.xscaled - localx > endx) {
                                            xscaled = xscaled + localx;
                                            wscaled -= localx;
                                        }
                                        if (this.yscaled - localy < starty || this.yscaled - localy > endy) {
                                            yscaled = yscaled + localy;
                                            hscaled -= localy;
                                        }
                                    }*/
                                    this.x = xscaled / scalefactor;
                                    this.y = yscaled / scalefactor;
                                    this.w = wscaled / scalefactor;
                                    this.h = hscaled / scalefactor;
                                    /*if(this.w < 0){
                                     tempx = this.x;
                                     this.x += this.w;
                                     this.w = this.x - tempx;
                                     }
                                     if(this.h < 0){
                                     tempy = this.y;
                                     this.y += this.h;
                                     this.h = this.y - tempy;
                                     }*/
                                    break;
                                case 3:

                                    rrect.x -= localx;
                                    //rrect.y -= localy;
                                    rrect.w += localx;
                                    rrect.h -= localy;


                                    if (bLimMarkupExtent) {

                                        if(MousePosdrwextRectPage(rrect, localpagerect)){
                                            xscaled -= localx;
                                            //yscaled -= localy;
                                            wscaled += localx;
                                            hscaled -= localy;

                                        }

                                        /*if(MousePosdrwextRect(rrect)){
                                            xscaled -= localx;
                                            //yscaled -= localy;
                                            wscaled += localx;
                                            hscaled -= localy;
                                        }*/

                                    }

                                    /*if (bLimMarkupExtent) {
                                        if (this.xscaled - localx < startx || this.xscaled - localx > endx) {
                                            xscaled = xscaled + localx;
                                            wscaled = wscaled - localx;
                                        }
                                        if ((this.xscaled + this.wscaled) - localx < startx || (this.xscaled + this.wscaled) - localx > endx) {
                                            xscaled = xscaled + localx;
                                            wscaled = wscaled - localx;
                                        }
                                        if(this.yscaled - localy <DocObj.getCurrentpage().starty || this.yscaled - localy > DocObj.getCurrentpage().endy){
                                         yscaled = yscaled + localy;
                                         //hscaled = hscaled - localy;
                                         }
                                        if ((this.yscaled + this.hscaled) - localy < starty || (this.yscaled + this.hscaled) - localy > endy) {
                                            hscaled = hscaled + localy;
                                            yscaled = yscaled - localy;
                                        }
                                    }*/
                                    this.x = xscaled / scalefactor;
                                    //this.y = yscaled/scalefactor;
                                    this.w = wscaled / scalefactor;
                                    this.h = hscaled / scalefactor;
                                    /*if(this.w < 0){
                                     tempx = this.x;
                                     this.x += this.w;
                                     this.w = this.x - tempx;
                                     }
                                     if(this.h < 0){
                                     tempy = this.y;
                                     this.y += this.h;
                                     this.h = this.y - tempy;
                                     }*/
                                    break;
                                case 2:

                                    //rrect.x -= localx;
                                    rrect.y -= localy;
                                    rrect.w -= localx;
                                    rrect.h += localy;


                                    if (bLimMarkupExtent) {

                                        if(MousePosdrwextRectPage(rrect, localpagerect)){
                                            yscaled -= localy;
                                            wscaled -= localx;
                                            hscaled += localy;

                                        }

                                        /*if(MousePosdrwextRect(rrect)){
                                            yscaled -= localy;
                                            wscaled -= localx;
                                            hscaled += localy;
                                        }*/

                                    }


                                    //xscaled -= localx;
                                    /*if (bLimMarkupExtent) {
                                        if(this.xscaled - localx < DocObj.getCurrentpage().startx || this.xscaled - localx > DocObj.getCurrentpage().endx){
                                         xscaled = xscaled + localx;
                                         wscaled = wscaled - localx;
        
                                         }
                                        if ((this.xscaled + this.wscaled) - localx < startx || (this.xscaled + this.wscaled) - localx > endx) {
                                            xscaled = xscaled - localx;
                                            wscaled = wscaled + localx;
                                        }
                                        if (this.yscaled - localy < starty || this.yscaled - localy > endy) {
                                            yscaled = yscaled + localy;
                                            hscaled = hscaled - localy;
                                        }
                                        if ((this.yscaled + this.hscaled) - localy < starty || (this.yscaled + this.hscaled) - localy > endy) {
                                            hscaled = hscaled - localy;
                                            yscaled = yscaled + localy;
                                        }
                                    }*/
                                    //this.x = xscaled/scalefactor;
                                    this.y = yscaled / scalefactor;
                                    this.w = wscaled / scalefactor;
                                    this.h = hscaled / scalefactor;
                                    /*if(this.w < 0){
                                     tempx = this.x;
                                     this.x += this.w;
                                     this.w = this.x - tempx;
                                     }
                                     if(this.h < 0){
                                     tempy = this.y;
                                     this.y += this.h;
                                     this.h = this.y - tempy;
                                     }*/
                                    break;
                            }
                            break;
                        case 6: //line arrow etc.
                            if (corner == 4) {
                                if (this.subtype == 6) {
                                    return;
                                }
                                wscaled -= localx;
                                hscaled -= localy;
                                if (bLimMarkupExtent) {
                                    if (this.wscaled - localx < startx || this.wscaled - localx > endx) {
                                        wscaled = wscaled + localx;
                                    }
                                    if (this.hscaled - localy < starty || this.hscaled - localy > endy) {
                                        hscaled = hscaled + localy;
                                    }
                                }
                                this.w = wscaled / scalefactor;
                                this.h = hscaled / scalefactor;
                            }
                            if (corner == 1) {
                                xscaled -= localx;
                                yscaled -= localy;
                                if (bLimMarkupExtent) {
                                    if (this.xscaled - localx < startx || this.xscaled - localx > endx) {
                                        xscaled = xscaled + localx;
                                    }
                                    if (this.yscaled - localy < starty || this.yscaled - localy > endy) {
                                        yscaled = yscaled + localy;
                                    }
                                }
                                this.x = xscaled / scalefactor;
                                this.y = yscaled / scalefactor;
                            }
                            if (this.subtype == 6) {
                                //textBoxConnected
                                if (this.textBoxConnected != null) {
                                    this.setTextboxConnectionPoint(this.textBoxConnected.x, this.textBoxConnected.y, this.textBoxConnected.w, this.textBoxConnected.h);
                                }
                            }
                            markupobject.lengthangleCallback(this.x, this.y, this.w, this.h, DocObj.getCurrentpage().getpageRotation());
                            //this.lengthangleCallback = function(x, y, w, h, drotation)
                            //var point = {x : localx, y : localy};
                            if (bOrthoOnEdit) {
                                markupobject.calculateOrtho(true, corner, { x: localx, y: localy });
                            }
                            /*wscaled = wscaled - x;
                             hscaled = hscaled - y;
        
                             this.w = wscaled/scalefactor;
                             this.h = hscaled/scalefactor;*/
                            break;
                        case 7: //dimension line.
                            if (corner == 4) {
                                wscaled -= localx;
                                hscaled -= localy;
                                if (bLimMarkupExtent) {
                                    if (this.wscaled - localx < startx || this.wscaled - localx > endx) {
                                        wscaled = wscaled + localx;
                                    }
                                    if (this.hscaled - localy < starty || this.hscaled - localy > endy) {
                                        hscaled = hscaled + localy;
                                    }
                                }
                                this.w = wscaled / scalefactor;
                                this.h = hscaled / scalefactor;
                            }
                            if (corner == 1) {
                                xscaled -= localx;
                                yscaled -= localy;
                                if (bLimMarkupExtent) {
                                    if (this.xscaled - localx < startx || this.xscaled - localx > endx) {
                                        xscaled = xscaled + localx;
                                    }
                                    if (this.yscaled - localy < starty || this.yscaled - localy > endy) {
                                        yscaled = yscaled + localy;
                                    }
                                }
                                this.x = xscaled / scalefactor;
                                this.y = yscaled / scalefactor;
                            }
                            if (bOrthoOnEdit) {
                                markupobject.calculateOrtho(true, corner, { x: localx, y: localy });
                            }
                            /*wscaled = wscaled - x;
                             hscaled = hscaled - y;
        
        
                             this.w = wscaled/scalefactor;
                             this.h = hscaled/scalefactor;*/
                            //var dimwidth = this.w - this.x;
                            //var dimheight = this.h - this.y;
                            //var dimdiag = markupobject.getdiag(dimwidth, dimheight);
                            /*var dimwsq = Math.pow(dimwidth,2);
                             var dimhsq = Math.pow(dimheight,2);
                             var dimdiag = Math.sqrt((dimwsq+dimhsq));*/
                            markupobject.setdimvalue(this.x, this.y, this.w, this.h);
                            //this.dimtext = getUnitlength(dimdiag);
                            //this.dimtext = this.dimtext.toFixed(2);
                            //this.dimtext = this.dimtext + " " + Unitlabel;
                            break;
                        case 8:
                            //not implemented for area markup.
                            break;
                        case 9: //text markup
                            if (this.subtype == 1 || this.subtype == 2) {
                                switch (corner) {
                                    case 4:

                                        //rrect.x -= localx;
                                        //rrect.y -= localy;
                                        rrect.w -= localx;
                                        rrect.h -= localy;



                                        if (bLimMarkupExtent) {

                                            if(MousePosdrwextRectPage(rrect, localpagerect)){
                                                wscaled -= localx;
                                                hscaled -= localy;

                                            }

                                            /*if(MousePosdrwextRect(rrect)){
                                                wscaled -= localx;
                                                hscaled -= localy;
                                            }*/

                                        }


                                        /*if (bLimMarkupExtent) {
                                            if ((this.xscaled + this.wscaled) - localx < startx || (this.xscaled + this.wscaled) - localx > endx) {
                                                wscaled = wscaled + localx;
                                            }
                                            if ((this.yscaled + this.hscaled) - localy < starty || (this.yscaled + this.hscaled) - localy > endy) {
                                                hscaled = hscaled + localy;
                                            }
                                        }*/
                                        if (wscaled < 0) {
                                            wscaled = 1;
                                        }
                                        if (hscaled < 0) {
                                            hscaled = 1;
                                        }
                                        this.w = wscaled / scalefactor;
                                        this.h = hscaled / scalefactor;
                                        break;
                                    case 1:


                                        rrect.x -= localx;
                                        rrect.y -= localy;
                                        rrect.w += localx;
                                        rrect.h += localy;


                                        if (bLimMarkupExtent) {


                                            if(MousePosdrwextRectPage(rrect, localpagerect)){
                                                xscaled -= localx;
                                                yscaled -= localy;
                                                wscaled += localx;
                                                hscaled += localy;

                                            }
                                            /*if(MousePosdrwextRect(rrect)){
                                                xscaled -= localx;
                                                yscaled -= localy;
                                                wscaled += localx;
                                                hscaled += localy;
                                            }*/

                                        }


                                        /*if (bLimMarkupExtent) {
                                            if (this.xscaled - localx < startx || this.xscaled - localx > endx) {
                                                xscaled = xscaled + localx;
                                                wscaled = wscaled - localx;
                                            }
                                            if ((this.xscaled + this.wscaled) - localx < startx || (this.xscaled + this.wscaled) - localx > endx) {
                                                wscaled = wscaled - localx;
                                                xscaled = xscaled + localx;
                                            }
                                            if (this.yscaled - localy < starty || this.yscaled - localy > endy) {
                                                yscaled = yscaled + localy;
                                                hscaled = hscaled - localy;
                                            }
                                            if ((this.yscaled + this.hscaled) - localy < starty || (this.yscaled + this.hscaled) - localy > endy) {
                                                yscaled = yscaled + localy;
                                                hscaled = hscaled - localy;
                                            }
                                        }*/
                                        if (wscaled < 0) {
                                            wscaled = 1;
                                        }
                                        if (hscaled < 0) {
                                            hscaled = 1;
                                        }
                                        this.x = xscaled / scalefactor;
                                        this.y = yscaled / scalefactor;
                                        this.w = wscaled / scalefactor;
                                        this.h = hscaled / scalefactor;
                                        break;
                                    case 3:

                                            rrect.x -= localx;
                                            //rrect.y -= localy;
                                            rrect.w += localx;
                                            rrect.h -= localy;


                                            if (bLimMarkupExtent) {


                                                if(MousePosdrwextRectPage(rrect, localpagerect)){
                                                    xscaled -= localx;
                                                    //yscaled -= localy;
                                                    wscaled += localx;
                                                    hscaled -= localy;

                                                }

                                                /*if(MousePosdrwextRect(rrect)){
                                                    xscaled -= localx;
                                                    //yscaled -= localy;
                                                    wscaled += localx;
                                                    hscaled -= localy;
                                                }*/

                                            }


                                        /*if (bLimMarkupExtent) {
                                            if (this.xscaled - localx < startx || this.xscaled - localx > endx) {
                                                xscaled = xscaled + localx;
                                                wscaled = wscaled - localx;
                                            }
                                            if ((this.xscaled + this.wscaled) - localx < startx || (this.xscaled + this.wscaled) - localx > endx) {
                                                xscaled = xscaled + localx;
                                                wscaled = wscaled - localx;
                                            }
                                            if(this.yscaled - localy <DocObj.getCurrentpage().starty || this.yscaled - localy > DocObj.getCurrentpage().endy){
                                             yscaled = yscaled + localy;
                                             //hscaled = hscaled - localy;
                                             }
                                            if ((this.yscaled + this.hscaled) - localy < starty || (this.yscaled + this.hscaled) - localy > endy) {
                                                hscaled = hscaled + localy;
                                                yscaled = yscaled - localy;
                                            }
                                        }*/
                                        if (wscaled < 0) {
                                            wscaled = 1;
                                        }
                                        if (hscaled < 0) {
                                            hscaled = 1;
                                        }
                                        this.x = xscaled / scalefactor;
                                        //this.y = yscaled/scalefactor;
                                        this.w = wscaled / scalefactor;
                                        this.h = hscaled / scalefactor;
                                        break;
                                    case 2:

                                        //rrect.x -= localx;
                                        rrect.y -= localy;
                                        rrect.w -= localx;
                                        rrect.h += localy;


                                        if (bLimMarkupExtent) {

                                            if(MousePosdrwextRectPage(rrect, localpagerect)){
                                                //xscaled -= localx;
                                                yscaled -= localy;
                                                wscaled -= localx;
                                                hscaled += localy;

                                            }
                                            /*if(MousePosdrwextRect(rrect)){
                                                //xscaled -= localx;
                                                yscaled -= localy;
                                                wscaled -= localx;
                                                hscaled += localy;
                                            }*/

                                        }

                                        /*if (bLimMarkupExtent) {
                                            if(this.xscaled - localx < DocObj.getCurrentpage().startx || this.xscaled - localx > DocObj.getCurrentpage().endx){
                                             xscaled = xscaled + localx;
                                             wscaled = wscaled - localx;
        
                                             }
                                            if ((this.xscaled + this.wscaled) - localx < startx || (this.xscaled + this.wscaled) - localx > endx) {
                                                xscaled = xscaled - localx;
                                                wscaled = wscaled + localx;
                                            }
                                            if (this.yscaled - localy < starty || this.yscaled - localy > endy) {
                                                yscaled = yscaled + localy;
                                                hscaled = hscaled - localy;
                                            }
                                            if ((this.yscaled + this.hscaled) - localy < starty || (this.yscaled + this.hscaled) - localy > endy) {
                                                hscaled = hscaled - localy;
                                                yscaled = yscaled + localy;
                                            }
                                        }*/
                                        if (wscaled < 0) {
                                            wscaled = 1;
                                        }
                                        if (hscaled < 0) {
                                            hscaled = 1;
                                        }
                                        //this.x = xscaled/scalefactor;
                                        this.y = yscaled / scalefactor;
                                        this.w = wscaled / scalefactor;
                                        this.h = hscaled / scalefactor;
                                        break;
                                }
                            }
                            else {
                                var textscaled = this.textheight * scalefactor;
                                var textwscaled = this.textwidth * scalefactor;
                                switch (this.textrotate / (Math.PI / 180)) {
                                    case 0:
                                        break;
                                    case 90:
                                        break;
                                    case 180:
                                        break;
                                    case 270:
                                        localx = -y;
                                        localy = x;
                                        //localy = -localy;
                                        break;
                                }
                                textscaled = textscaled - (localy * 0.25);
                                textwscaled = textwscaled - (localx * 0.25);
                                wscaled -= (localx * 0.25);
                                hscaled -= (localy * 0.25);
                                this.font.setHeight(textscaled / scalefactor);
                                ctx.font = this.font.fontstring;
                                var dimt = ctx.measureText(this.text);
                                var dimtextwidth = dimt.width;
                                if (bLimMarkupExtent) {
                                    if ((this.xscaled + this.wscaled) - localx < startx || (this.xscaled + this.wscaled) - localx > endx) {
                                        wscaled = wscaled + localx;
                                        textwscaled = textwscaled + localx;
                                        textscaled = textscaled + localy;
                                    }
                                    if ((this.yscaled - this.hscaled) - localy < starty || (this.yscaled - this.hscaled) - localy > endy) {
                                        hscaled = hscaled + localy;
                                        textscaled = textscaled + localy;
                                    }
                                }
                                this.w = wscaled / scalefactor;
                                this.h = hscaled / scalefactor;
                                this.textheight = textscaled / scalefactor;
                                this.textwidth = textwscaled / scalefactor;
                            }
                            if (this.bhasArrow && this.markupArrowConnected != null) {
                                //var arrowmobj = DocObj.getmarkupbynumber(this.markupArrowConnected);
                                //DocObj.markuplist[arrowmobj].setTextboxConnectionPoint(this.x, this.y, this.w, this.h);
                                this.markupArrowConnected.setTextboxConnectionPoint(this.x, this.y, this.w, this.h);
                            }
                            break;
                        case 11:
                            switch (corner) {

                                
                                case 4:

                                    if (this.aspectLock) {

                                        rrect.w -= localx;
                                        rrect.h = rrect.w / aspectRatio;

                                        if (bLimMarkupExtent && MousePosdrwextRectPage(rrect, localpagerect)) {
                                            wscaled -= localx;
                                            hscaled = wscaled / aspectRatio;

                                        }

                                    
                                    }else{

                                        rrect.w -= localx;
                                        rrect.h -= localy;
    
                                        if (bLimMarkupExtent) {
    
                                            if(MousePosdrwextRectPage(rrect, localpagerect)){
                                                wscaled -= localx;
                                                hscaled -= localy;
    
                                            }
    
                                        }
    

                                    }
                                

                                    this.w = wscaled / scalefactor;
                                    this.h = hscaled / scalefactor;
                                    break;
                                case 1:


                                    if (this.aspectLock) {
                                        rrect.x -= localx;

                                        rrect.w += localx;
                                        rrect.h = rrect.w / aspectRatio;
                                        rrect.y = rrect.y + (rrect.h - (rrect.w / aspectRatio));


                                        
                                        if (bLimMarkupExtent && MousePosdrwextRectPage(rrect, localpagerect)) {
                                            xscaled -= localx;

                                            wscaled += localx;
                                            hscaled = wscaled / aspectRatio;
                                            yscaled = yscaled + (hscaled - (wscaled / aspectRatio));
                                        }

                                    }else{
                                        rrect.x -= localx;
                                        rrect.y -= localy;
                                        rrect.w += localx;
                                        rrect.h += localy;
    
    
                                        if (bLimMarkupExtent) {
    
                                            if(MousePosdrwextRectPage(rrect, localpagerect)){
                                                xscaled -= localx;
                                                yscaled -= localy;
                                                wscaled += localx;
                                                hscaled += localy;
    
                                            }
    
                                        }
    
                                    }



                                    
                                    this.x = xscaled / scalefactor;
                                    this.y = yscaled / scalefactor;
                                    this.w = wscaled / scalefactor;
                                    this.h = hscaled / scalefactor;
                                    break;
                                case 3:

                                    if (this.aspectLock) {

                                        rrect.w += localx;
                                        rrect.h = rrect.w / aspectRatio;

                                        
                                        if (bLimMarkupExtent && MousePosdrwextRectPage(rrect, localpagerect)) {

                                            wscaled += localx;
                                            hscaled = wscaled / aspectRatio;

                                        }


                                    }else{
                                        rrect.x -= localx;
                                        //rrect.y -= localy;
                                        rrect.w += localx;
                                        rrect.h -= localy;


                                        if (bLimMarkupExtent) {

                                            if(MousePosdrwextRectPage(rrect, localpagerect)){
                                                xscaled -= localx;
                                                //yscaled -= localy;
                                                wscaled += localx;
                                                hscaled -= localy;

                                            }

                                        }

                                    }

                                    

                                    this.x = xscaled / scalefactor;
                                    //this.y = yscaled/scalefactor;
                                    this.w = wscaled / scalefactor;
                                    this.h = hscaled / scalefactor;
                                    break;
                                case 2:

                                    if (this.aspectLock) {
                                        rrect.w -= localx;
                                        rrect.h = rrect.w / aspectRatio;
                                        
                                        if (bLimMarkupExtent && MousePosdrwextRectPage(rrect, localpagerect)) {

                                            wscaled -= localx;
                                            hscaled = wscaled / aspectRatio;

                                        }


                                    }else{
                                        //rrect.x -= localx;
                                        rrect.y -= localy;
                                        rrect.w -= localx;
                                        rrect.h += localy;


                                        if (bLimMarkupExtent) {

                                            if(MousePosdrwextRectPage(rrect, localpagerect)){
                                                //xscaled -= localx;
                                                yscaled -= localy;
                                                wscaled -= localx;
                                                hscaled += localy;

                                            }


                                        }

                                    }
                                    
                                    //this.x = xscaled/scalefactor;
                                    this.y = yscaled / scalefactor;
                                    this.w = wscaled / scalefactor;
                                    this.h = hscaled / scalefactor;
                                    break;
                            }
                            break;
                        case 12: //stamp markup
                            //var textstampscaled = this.textheight * scalefactor;
                            //var textwstampscaled = this.textwidth * scalefactor;
                            //var textsmallscaled = this.stampsmalltheight * scalefactor;
                            switch (corner) {
                                case 4:
                                    //rrect.x -= localx;
                                    //rrect.y -= localy;
                                    rrect.w -= localx;
                                    rrect.h -= localy;


                                    if (bLimMarkupExtent) {


                                        if(MousePosdrwextRectPage(rrect, localpagerect)){
                                            wscaled -= localx;
                                            hscaled -= localy;

                                        }

                                        /*if(MousePosdrwextRect(rrect)){
                                        

                                            wscaled -= localx;
                                            hscaled -= localy;
                        
                                                
                                        }*/

                                    }

                                    /*if (bLimMarkupExtent) {
                                        if ((this.xscaled + this.wscaled) - localx < startx || (this.xscaled + this.wscaled) - localx > endx) {
                                            wscaled = wscaled + localx;
                                        }
                                        if ((this.yscaled + this.hscaled) - localy < starty || (this.yscaled + this.hscaled) - localy > endy) {
                                            hscaled = hscaled + localy;
                                        }
                                    }*/
                                    if (wscaled < 0) {
                                        wscaled = 1;
                                    }
                                    if (hscaled < 0) {
                                        hscaled = 1;
                                    }
                                    this.w = wscaled / scalefactor;
                                    this.h = hscaled / scalefactor;
                                    break;
                                case 1:
                                    rrect.x -= localx;
                                    rrect.y -= localy;
                                    rrect.w += localx;
                                    rrect.h += localy;


                                    if (bLimMarkupExtent) {


                                        if(MousePosdrwextRectPage(rrect, localpagerect)){
                                            xscaled -= localx;
                                            yscaled -= localy;
                                            wscaled += localx;
                                            hscaled += localy;


                                        }
                                        /*if(MousePosdrwextRect(rrect)){
                                        

                                            xscaled -= localx;
                                            yscaled -= localy;
                                            wscaled += localx;
                                            hscaled += localy;
                                
                                                
                                        }*/

                                    }                                    
                                    /*if (bLimMarkupExtent) {
                                        if (this.xscaled - localx < startx || this.xscaled - localx > endx) {
                                            xscaled = xscaled + localx;
                                            wscaled = wscaled - localx;
                                        }
                                        if ((this.xscaled + this.wscaled) - localx < startx || (this.xscaled + this.wscaled) - localx > endx) {
                                            wscaled = wscaled - localx;
                                            xscaled = xscaled + localx;
                                        }
                                        if (this.yscaled - localy < starty || this.yscaled - localy > endy) {
                                            yscaled = yscaled + localy;
                                            hscaled = hscaled - localy;
                                        }
                                        if ((this.yscaled + this.hscaled) - localy < starty || (this.yscaled + this.hscaled) - localy > endy) {
                                            yscaled = yscaled + localy;
                                            hscaled = hscaled - localy;
                                        }
                                    }*/
                                    if (wscaled < 0) {
                                        wscaled = 1;
                                    }
                                    if (hscaled < 0) {
                                        hscaled = 1;
                                    }
                                    this.x = xscaled / scalefactor;
                                    this.y = yscaled / scalefactor;
                                    this.w = wscaled / scalefactor;
                                    this.h = hscaled / scalefactor;
                                    break;
                                case 3:
                                    rrect.x -= localx;
                                    //rrect.y -= localy;
                                    rrect.w += localx;
                                    rrect.h -= localy;


                                    if (bLimMarkupExtent) {

                                        if(MousePosdrwextRectPage(rrect, localpagerect)){
                                            xscaled -= localx;
                                            //yscaled -= localy;
                                            wscaled += localx;
                                            hscaled -= localy;


                                        }

                                        /*if(MousePosdrwextRect(rrect)){
                                        

                                            xscaled -= localx;
                                            //yscaled -= localy;
                                            wscaled += localx;
                                            hscaled -= localy;
                                        
                                                
                                        }*/

                                    }                                    

                                    /*if (bLimMarkupExtent) {
                                        if (this.xscaled - localx < startx || this.xscaled - localx > endx) {
                                            xscaled = xscaled + localx;
                                            wscaled = wscaled - localx;
                                        }
                                        if ((this.xscaled + this.wscaled) - localx < startx || (this.xscaled + this.wscaled) - localx > endx) {
                                            xscaled = xscaled + localx;
                                            wscaled = wscaled - localx;
                                        }
                                        if(this.yscaled - localy <DocObj.getCurrentpage().starty || this.yscaled - localy > DocObj.getCurrentpage().endy){
                                         yscaled = yscaled + localy;
                                         //hscaled = hscaled - localy;
                                         }
                                        if ((this.yscaled + this.hscaled) - localy < starty || (this.yscaled + this.hscaled) - localy > endy) {
                                            hscaled = hscaled + localy;
                                            yscaled = yscaled - localy;
                                        }
                                    }*/
                                    if (wscaled < 0) {
                                        wscaled = 1;
                                    }
                                    if (hscaled < 0) {
                                        hscaled = 1;
                                    }
                                    this.x = xscaled / scalefactor;
                                    //this.y = yscaled/scalefactor;
                                    this.w = wscaled / scalefactor;
                                    this.h = hscaled / scalefactor;
                                    break;
                                case 2:
                                    //rrect.x -= localx;
                                    rrect.y -= localy;
                                    rrect.w -= localx;
                                    rrect.h += localy;


                                    if (bLimMarkupExtent) {

                                        if(MousePosdrwextRectPage(rrect, localpagerect)){
                                                //xscaled -= localx;
                                                yscaled -= localy;
                                                wscaled -= localx;
                                                hscaled += localy;

                                        }

                                        /*if(MousePosdrwextRect(rrect)){
                                                //xscaled -= localx;
                                                yscaled -= localy;
                                                wscaled -= localx;
                                                hscaled += localy;
                                        
                                                
                                        }*/

                                    }                                    

                                    /*if (bLimMarkupExtent) {
                                        if(this.xscaled - localx < DocObj.getCurrentpage().startx || this.xscaled - localx > DocObj.getCurrentpage().endx){
                                         xscaled = xscaled + localx;
                                         wscaled = wscaled - localx;
        
                                         }
                                        if ((this.xscaled + this.wscaled) - localx < startx || (this.xscaled + this.wscaled) - localx > endx) {
                                            xscaled = xscaled - localx;
                                            wscaled = wscaled + localx;
                                        }
                                        if (this.yscaled - localy < starty || this.yscaled - localy > endy) {
                                            yscaled = yscaled + localy;
                                            hscaled = hscaled - localy;
                                        }
                                        if ((this.yscaled + this.hscaled) - localy < starty || (this.yscaled + this.hscaled) - localy > endy) {
                                            hscaled = hscaled - localy;
                                            yscaled = yscaled + localy;
                                        }
                                    }*/
                                    if (wscaled < 0) {
                                        wscaled = 1;
                                    }
                                    if (hscaled < 0) {
                                        hscaled = 1;
                                    }
                                    //this.x = xscaled/scalefactor;
                                    this.y = yscaled / scalefactor;
                                    this.w = wscaled / scalefactor;
                                    this.h = hscaled / scalefactor;
                                    break;
                            }
                            /*wscaled -= localx;
                             hscaled -= localy;
        
                             if (bLimMarkupExtent){
                             if((this.xscaled + this.wscaled) - localx < DocObj.getCurrentpage().startx || (this.xscaled + this.wscaled) - localx > DocObj.getCurrentpage().endx){
                             wscaled = wscaled + localx;
        
                             }
                             if((this.yscaled + this.hscaled)-localy <DocObj.getCurrentpage().starty || (this.yscaled + this.hscaled)-localy > DocObj.getCurrentpage().endy){
                             hscaled = hscaled + localy;
                             }
        
                             }
        
        
                             this.w = wscaled/scalefactor;
                             this.h = hscaled/scalefactor;*/
                            //this.textheight = this.w * 0.16;
                            this.font.setHeight(this.w * 0.16);
                            if (this.alternative == 0) {
                                this.stampsmalltheight = (this.w * 0.08) - 4;
                                if (this.stampsmalltheight < 1) {
                                    this.stampsmalltheight = 1;
                                }
                                var combinedtextheight = this.textheight + this.stampsmalltheight + ((this.h / 10) * 2);
                                if (combinedtextheight > this.h) {
                                    //this.textheight = (this.h / 2) - ((this.h / 10) * 2);
                                    this.font.setHeight((this.h / 2) - ((this.h / 10) * 2));
                                    this.stampsmalltheight = (this.textheight / 2) - 4;
                                    if (this.stampsmalltheight < 1) {
                                        this.stampsmalltheight = 1;
                                    }
                                }
                            }
                            else {
                                if ((this.w * 0.6) >= this.h) {
                                    //this.textheight = (this.h / 4);
                                    this.font.setHeight(this.h / 4);
                                }
                                else {
                                    //this.textheight = this.w * 0.16;
                                    this.font.setHeight(this.w * 0.16);
                                }
                            }
                            /*if (this.h < 40){
                             this.stampsmalltheight = 1;
                             this.textheight = 1;
        
                             }*/
                            //recalculate text size for stampe here.
                            break;
                        
    

                        default:
                        //code to be executed if n is different from case 1 and 2
                    }
                    /* if (!this.withinbounds(localx,localy) && bLimMarkupExtent){
                     this.x = restorex;
                     this.y = restorey;
                     this.w = restorew;
                     this.h = restoreh;
                     }*/

                     markupobject.setmarkupmodified(true);

                     //scale
                     if(RxCore_GUI_MarkupChanged != undefined){
                        var operation = markupobject.editaction;
                        RxCore_GUI_MarkupChanged.setMarkupSelected(markupobject, operation);
                    }


                };
                //rotated rectangle for shapes
                this.rotatedRectangle = function () {
                    var minx = this.rotatedrect.x;
                    var miny = this.rotatedrect.y;
                    var maxx = this.rotatedrect.w;
                    var maxy = this.rotatedrect.w;
                };
                //functions for polygons, pencil, polycurves

                this.findrotatedrectangle = function (){

                    var pointtype = (this.type == 0 || this.type == 1 || this.type == 13 || this.type == 8);

                    if(!pointtype){
                        return;
                    }

                    var counter = 0;
                    var lcounter = 0;

                    if(this.type == 13){

                        var rotpoint = markupobject.getrotatedPoint(markupobject.countshapes[0].x, markupobject.countshapes[0].y);                        

                        var minx = rotpoint.x;
                        var miny = rotpoint.y;
                        var maxx = rotpoint.x;
                        var maxy = rotpoint.y;



                        for (var scounter = 1; scounter < markupobject.countshapes.length; scounter++) {

                            rotpoint = markupobject.getrotatedPoint(markupobject.countshapes[scounter].x, markupobject.countshapes[scounter].y);

                            if (rotpoint.x < minx) {
                                minx = rotpoint.x;
                            }
                            if (rotpoint.y < miny) {
                                miny = rotpoint.y;
                            }
                            if (rotpoint.x > maxx) {
                                maxx = rotpoint.x;
                            }
                            if (rotpoint.y > maxy) {
                                maxy = rotpoint.y;
                            }

                        }
                    }else{
                        if (this.type == 0 || (this.type == 1 && this.subtype == 6)) {
                            
                            if(this.pointlist.length != 0){

                                rotpoint = markupobject.getrotatedPoint(this.pointlist[0][0].x, this.pointlist[0][0].y);                        

                                var minx = rotpoint.x;
                                var miny = rotpoint.y;
                                var maxx = rotpoint.x;
                                var maxy = rotpoint.y;


                                for (lcounter = 0; lcounter < this.pointlist.length; lcounter++) {
                                    for (counter = 0; counter < this.pointlist[lcounter].length; counter++) {

                                        rotpoint = markupobject.getrotatedPoint(this.pointlist[lcounter][counter].x, this.pointlist[lcounter][counter].y);                        

                                        if (rotpoint.x < minx) {
                                            minx = rotpoint.x;
                                        }
                                        if (rotpoint.y < miny) {
                                            miny = rotpoint.y;
                                        }
                                        if (rotpoint.x > maxx) {
                                            maxx = rotpoint.x;
                                        }
                                        if (rotpoint.y > maxy) {
                                            maxy = rotpoint.y;
                                        }
                                    }
                                }
                            }

                        }
                        else {

                            if (this.points.length > 0){
                                var scalefactor = getCurrentPageScale();

                                var drawscale = scalefactor / this.points[0].scale;
    
    
                                rotpoint = markupobject.getrotatedPoint(this.points[0].x, this.points[0].y);                        
                                var rotpointoffset = markupobject.getrotatedPoint(this.points[0].offsetx, this.points[0].offsety);                        

                                var xscaled = (rotpoint.x - rotpointoffset.x) * drawscale;
                                var yscaled = (rotpoint.y - rotpointoffset.y) * drawscale;


                                minx = rotpoint.x;
                                miny = rotpoint.y;
                                maxx = rotpoint.x;
                                maxy = rotpoint.y;
    
                                

                                for (counter = 0; counter < this.points.length; counter++) {
    
                                    drawscale = scalefactor / this.points[counter].scale;

                                    rotpoint = markupobject.getrotatedPoint(this.points[counter].x, this.points[counter].y);                        
                                    rotpointoffset = markupobject.getrotatedPoint(this.points[counter].offsetx, this.points[counter].offsety);                        
    
                                    xscaled = (rotpoint.x - rotpointoffset.x) * drawscale;
                                    yscaled = (rotpoint.y - rotpointoffset.y) * drawscale;
    
                                    if (rotpoint.x < minx) {
                                        minx = rotpoint.x;
                                    }
                                    if (rotpoint.y < miny) {
                                        miny = rotpoint.y;
                                    }
                                    if (rotpoint.x > maxx) {
                                        maxx = rotpoint.x;
                                    }
                                    if (rotpoint.y > maxy) {
                                        maxy = rotpoint.y;
                                    }
                                }
    
                            }
                        }

                    }
                    
                    //this.x = minx;
                    //this.y = miny;
                    //this.w = maxx;
                    //this.h = maxy;

                    //this.xscaled = this.x;
                    //this.yscaled = this.y;
                    //this.wscaled = this.w;
                    //this.hscaled = this.h;

                    /*if (this.type == 0 && !this.origset) {
                        
                        this.origx = minx;
                        this.origy = miny;
                        this.origw = maxx;
                        this.origh = maxy;

                        this.origset = true;
                        //store original rect here
                    }*/

                    return {x : minx, y : miny, w : maxx, h : maxy};

                }

                this.findrectangle = function () {
                    
                    var pointtype = (this.type == 0 || this.type == 1 || this.type == 13 || this.type == 8);

                    if(!pointtype){
                        return;
                    }

                    var counter = 0;
                    var lcounter = 0;

                    if(this.type == 13){
                        var minx = markupobject.countshapes[0].x;
                        var miny = markupobject.countshapes[0].y;
                        var maxx = markupobject.countshapes[0].x;
                        var maxy = markupobject.countshapes[0].y;

                        for (var scounter = 1; scounter < markupobject.countshapes.length; scounter++) {
                            if (markupobject.countshapes[scounter].x < minx) {
                                minx = markupobject.countshapes[scounter].x;
                            }
                            if (markupobject.countshapes[scounter].y < miny) {
                                miny = markupobject.countshapes[scounter].y;
                            }
                            if (markupobject.countshapes[scounter].x > maxx) {
                                maxx = markupobject.countshapes[counter].x;
                            }
                            if (markupobject.countshapes[scounter].y > maxy) {
                                maxy = markupobject.countshapes[scounter].y;
                            }

                        }
                    }else{
                        if (this.type == 0 || (this.type == 1 && this.subtype == 6)) {
                            
                            if(this.pointlist.length != 0){
                                var minx = this.pointlist[0][0].x;
                                var miny = this.pointlist[0][0].y;
                                var maxx = this.pointlist[0][0].x;
                                var maxy = this.pointlist[0][0].y;
                                for (lcounter = 0; lcounter < this.pointlist.length; lcounter++) {
                                    for (counter = 0; counter < this.pointlist[lcounter].length; counter++) {
                                        if (this.pointlist[lcounter][counter].x < minx) {
                                            minx = this.pointlist[lcounter][counter].x;
                                        }
                                        if (this.pointlist[lcounter][counter].y < miny) {
                                            miny = this.pointlist[lcounter][counter].y;
                                        }
                                        if (this.pointlist[lcounter][counter].x > maxx) {
                                            maxx = this.pointlist[lcounter][counter].x;
                                        }
                                        if (this.pointlist[lcounter][counter].y > maxy) {
                                            maxy = this.pointlist[lcounter][counter].y;
                                        }
                                    }
                                }
                            }

                        }
                        else {

                            if (this.points.length > 0){
                                var scalefactor = getCurrentPageScale();

                                var drawscale = scalefactor / this.points[0].scale;
    
                                var xscaled = (this.points[0].x - this.points[0].offsetx) * drawscale;
                                var yscaled = (this.points[0].y - this.points[0].offsety) * drawscale;
    
    
                                minx = this.points[0].x;
                                miny = this.points[0].y;
                                maxx = this.points[0].x;
                                maxy = this.points[0].y;
                                for (counter = 0; counter < this.points.length; counter++) {
    
                                    drawscale = scalefactor / this.points[counter].scale;
                                    xscaled = (this.points[counter].x - this.points[counter].offsetx) * drawscale;
                                    yscaled = (this.points[counter].y - this.points[counter].offsety) * drawscale;
    
                                    if (this.points[counter].x < minx) {
                                        minx = this.points[counter].x;
                                    }
                                    if (this.points[counter].y < miny) {
                                        miny = this.points[counter].y;
                                    }
                                    if (this.points[counter].x > maxx) {
                                        maxx = this.points[counter].x;
                                    }
                                    if (this.points[counter].y > maxy) {
                                        maxy = this.points[counter].y;
                                    }
                                }
    
                            }
                        }

                    }
                    
                    this.x = minx;
                    this.y = miny;
                    this.w = maxx;
                    this.h = maxy;

                    this.xscaled = this.x;
                    this.yscaled = this.y;
                    this.wscaled = this.w;
                    this.hscaled = this.h;

                    if (this.type == 0 && !this.origset) {
                        
                        this.origx = minx;
                        this.origy = miny;
                        this.origw = maxx;
                        this.origh = maxy;

                        this.origset = true;
                        //store original rect here
                    }


                    if ((this.type == 1 && this.subtype == 2) || this.type == 8) {
                        this.addsides();
                    }
                    //alert(x);
                };
                this.removepoints = function () {
                    this.pointlist = [];
                    this.points = [];
                    this.sides = [];
                    this.countshapes = [];
                };
                this.addpoint = function (x, y) {
                    //this.points.push(new point(x, y));

                    if(this.type == 1 && this.subtype == 6){
                        if(this.points.length == 0){
                            this.points.push({ x: x, y: y });        
                            var coords = markupobject.setDimDrawme();
                            this.setlastpoint(coords.w, coords.h);
                        }    
                    }

                    this.points.push({ x: x, y: y });
                };

                this.addrectpoint = function (x, y) {
                    this.points.push({ x: x, y: y });
                };

                this.hidelabelmarkupobj = function () {			
                    this.hidevaluelabel = true;                    
                };
         
                this.showlabelmarkupobj = function () {
                    this.hidevaluelabel = false;
                };

                this.addline = function () {
                    this.pointlist.push(markupobject.points);
                    this.points = [];
                    //this.points.push(new point(x, y));
                };
                this.snapToCorner = function (x, y, corner) {
                    var snappoint = markupobject.getsnappoint(x, y);
                    //use to edit end points of  of dimensions and arrows.
                    if (corner == 1) {
                        this.x = snappoint.x;
                        this.y = snappoint.y;
                    }
                    else if (corner == 4) {
                        this.w = snappoint.x;
                        this.h = snappoint.y;
                    }

                    markupobject.setmarkupmodified(true);

                };
                this.snapTo = function (x, y, point) {
                    var snappoint = markupobject.getsnappoint(x, y);
                    if (point >= 0 && point <= this.points.length - 1) {
                        this.points[point].x = snappoint.x; //((x - pagedx) / scalefactor) + this.xoffset;
                        this.points[point].y = snappoint.y; //((y - pagedy) / scalefactor) + this.yoffset;
                    }

                    markupobject.setmarkupmodified(true);

                };
                this.getsnappoint = function (x, y) {
                    var pagedx = 0;
                    var pagedy = 0;


                    var offset = getCurrentPageOffset(markupobject.pagenumber);

                    pagedx = offset.x;
                    pagedy = offset.y;

                    var scalefactor = getCurrentPageScale();

                    if(markupobject.bfor3D){
                        scalefactor = canvasoheight / this.pageheight;
                    }

                    //var scalefactor = getCurrentPageScale();
                    scalefactor = scalefactor / markupobject.scaling;

                    
                    return {
                        x: ((x - pagedx) / scalefactor) + this.xoffset,
                        y: ((y - pagedy) / scalefactor) + this.yoffset
                    };
                };

                this.getSelectedpoint = function(){

                    var point = undefined;

                    if(markupobject.selectedpoint != -1){
                        if(this.points[markupobject.selectedpoint]){

                            if (markupobject.selectedpoint >= 0 && markupobject.selectedpoint <= this.points.length - 1) {
                                point = this.points[markupobject.selectedpoint];
                            }
                            
                        }
    
                        if(this.countshapes[markupobject.selectedpoint]){
                            if (markupobject.selectedpoint >= 0 && markupobject.selectedpoint <= this.countshapes.length - 1) {
                                point = this.countshapes[markupobject.selectedpoint];
                            }
                        }
    
                    }
                    if(markupobject.selectedmultipoint.length != 0){
                        //this.pointlist[lcounter][counter]
                        if(this.pointlist[markupobject.selectedmultipoint[0]][markupobject.selectedmultipoint[1]]){

                            if (markupobject.selectedmultipoint[1] >= 0 && markupobject.selectedmultipoint[1] <= this.pointlist[markupobject.selectedmultipoint[0]].length - 1) {
                                point = this.pointlist[markupobject.selectedmultipoint[0]][markupobject.selectedmultipoint[1]];    
                            }
                            
                        }
    

                    }


                    return point;

                };

                this.editpoint = function (x, y, point) {
                    
                    if(markupobject.editaction != 4){
                        return;
                    }


                    var pnt = this.getSelectedpoint(point);
                    
                    var localx = x;
                    var localy = y;

                    var scalefactor = getCurrentPageScale();

                    if(markupobject.bfor3D){
                        scalefactor = canvasoheight / this.pageheight;
                    }

                    scalefactor = scalefactor / markupobject.scaling;

                    if(!markupobject.bfor3D){

                        var pagerect = DocObj.getCurrentpage().getpagerectdirect();

                        var startx = pagerect.x;
                        var endx = pagerect.w;
                        var starty = pagerect.y;
                        var endy = pagerect.h;


                        if(pagerect.usepixelscale){
                            var pixelRatio = getPixelRatio();

                            startx = startx * pixelRatio;
                            endx = endx * pixelRatio;
                            starty = starty * pixelRatio;
                            endy = endy * pixelRatio;
    
                        }


                        //var startx = DocObj.getCurrentpage().startx;
                        //var endx = DocObj.getCurrentpage().endx;
                        //var starty = DocObj.getCurrentpage().starty;
                        //var endy = DocObj.getCurrentpage().endy;

                        //console.log(startx, endx, starty, endy);

                    }else{
                        startx = 0;
                        endx = canvasowidth;
                        starty = 0;
                        endy = canvasoheight;
                    }

                    
                    switch (DocObj.getCurrentpage().getpageRotation()) {
                        case 0:
                            localx = x;
                            localy = y;
                            break;
                        case 90:
                            localx = y;
                            localy = -x;
                            break;
                        case 180:
                            localx = -x;
                            localy = -y;
                            break;
                        case 270:
                            localx = -y;
                            localy = x;
                            break;
                    }

                    /*for (pcounter = 0; pcounter < this.points.length; pcounter++) {

                        xscalepoint = (this.points[pcounter].x - this.xoffset) * scalefactor;
                        xscalepoint += startx;
                        xscalepoint -= localx;

                        if (xscalepoint > startx && xscalepoint < endx) {

                            bMovepintsx = true;
                            
                        }else{
                            bMovepintsx = false;
                            break;
                        }



                        yscalepoint = (this.points[pcounter].y - this.yoffset) * scalefactor;
                        yscalepoint += starty;
                        yscalepoint -= localy;

                        if (yscalepoint > starty && yscalepoint < endy) {

                            bMovepintsy = true;

                        }else{
                            bMovepintsy = false;
                            break;
                        }


                    }*/

                    
                    
                    
                    

                    /*if (point >= 0 && point <= this.points.length - 1) {

                    }*/
                    if (markupobject.type == 14){

                        /* if(markupobject.type == 14){
                             switch (markupobject.selectedpoint){
                                 case 1 : markupobject.startX
                             }
                         }*/
     
                         if(markupobject.selectedpoint == 1){
                             
                             markupobject.setsecond = true;
                             markupobject.setthird = false;
                             var xscalepoint = 0.0;
                             var yscalepoint = 0.0;

                             markupobject.endXscaled = null;
                             markupobject.endYscaled = null;

                             markupobject.endX = null;
                             markupobject.endY = null;

     
                             //scalefactor = scalefactor / this.points[point].scale;
                             
                             xscalepoint = markupobject.startX * scalefactor;
                             yscalepoint = markupobject.startY * scalefactor;
                             
     
                             xscalepoint = xscalepoint - localx;
                             yscalepoint = yscalepoint - localy;
     
                             markupobject.startX = xscalepoint / scalefactor;
                             markupobject.startY = yscalepoint / scalefactor;
 
                         }

                         if(markupobject.selectedpoint == 2){
                             
                             markupobject.setsecond = true;
                             markupobject.setthird = false;
                             var xscalepoint = 0.0;
                             var yscalepoint = 0.0;

                             markupobject.endXscaled = null;
                             markupobject.endYscaled = null;

                             markupobject.endX = null;
                             markupobject.endY = null;


     
                             //scalefactor = scalefactor / this.points[point].scale;
                             
                             xscalepoint = markupobject.secondX * scalefactor;
                             yscalepoint = markupobject.secondY * scalefactor;
                             
     
                             xscalepoint = xscalepoint - localx;
                             yscalepoint = yscalepoint - localy;
     
                             markupobject.secondX = xscalepoint / scalefactor;
                             markupobject.secondY = yscalepoint / scalefactor;
 
                         }
                         if(markupobject.selectedpoint == 3){
                             
                             markupobject.setsecond = false;
                             markupobject.setthird = true;
                             var xscalepoint = 0.0;
                             var yscalepoint = 0.0;
     
                             //scalefactor = scalefactor / this.points[point].scale;
                             
                             xscalepoint = markupobject.endX * scalefactor;
                             yscalepoint = markupobject.endY * scalefactor;
                             
     
                             xscalepoint = xscalepoint - localx;
                             yscalepoint = yscalepoint - localy;
     
                             markupobject.endX = xscalepoint / scalefactor;
                             markupobject.endY = yscalepoint / scalefactor;
 
                         }

                         
     
                         
                    }

                    if (pnt != undefined){
                        if(this.type == 13){
                            var xscalepoint = 0.0;
                            var yscalepoint = 0.0;
    
                            //scalefactor = scalefactor / this.points[point].scale;
                            
                            xscalepoint = pnt.x * scalefactor;
                            yscalepoint = pnt.y * scalefactor;
    
                            xscalepoint = xscalepoint - localx;
                            yscalepoint = yscalepoint - localy;
    
                            pnt.x = xscalepoint / scalefactor;
                            pnt.y = yscalepoint / scalefactor;
    
                            
    
                        }else{
                            var xscalepoint = 0.0;
                            var yscalepoint = 0.0;
        
                            var xscalepointcalc = 0.0;
                            var yscalepointcalc = 0.0;
        
                            xscalepointcalc = (pnt.x - this.xoffset) * scalefactor;
        
                            xscalepointcalc += startx;
                            xscalepointcalc -= localx;
        
                            //xscalepoint = xscalepoint - localx;
        
                            yscalepointcalc = (pnt.y - this.yoffset) * scalefactor;
        
                            yscalepointcalc += starty;
                            yscalepointcalc -= localy;
        
        
                            xscalepoint = pnt.x * scalefactor;
                            yscalepoint = pnt.y * scalefactor;
                            
                            xscalepoint = xscalepoint - localx;
                            yscalepoint = yscalepoint - localy;
        
        
                            if (xscalepointcalc > startx && xscalepointcalc < endx) {
                                pnt.x = xscalepoint / scalefactor;
                            }
        
                            if (yscalepointcalc > starty && yscalepointcalc < endy) {
                                pnt.y = yscalepoint / scalefactor;
                            }
                        }
    
                        this.findrectangle();
    
                    }

                    

                    /*if (point >= 0 && point <= this.countshapes.length - 1) {
                        
                    }*/

                    markupobject.setmarkupmodified(true);


                    //edit point
                    if(RxCore_GUI_MarkupChanged != undefined){
                        var operation = markupobject.editaction;
                        RxCore_GUI_MarkupChanged.setMarkupSelected(markupobject, operation);
                    }



                };
                this.deletepoint = function (point) {

                    if(this.type == 1){
                        if(this.subtype == 4 || this.subtype == 5){
                            return;
                        }

                    }

                    //var index = array.indexOf(5);
                    if (point >= 0 && point <= this.points.length - 1) {
                        this.points.splice(point, 1);
                    }

                    if (point >= 0 && point <= this.countshapes.length - 1) {
                        this.countshapes.splice(point, 1);
                    }

                    this.selectedpoint = -1;

                    markupobject.setmarkupmodified(true);
                };

                this.insertcount = function (mousepos){
                    markupobject.addcount(mousepos);
                    markupobject.findrectangle();

                    markupobject.setmarkupmodified(true);

                };
                this.insertpoint = function () {

                    if(this.type == 1){
                        if(this.subtype == 4 || this.subtype == 5){
                            return;
                        }

                    }
                    

                    var bfirstpoint = (markupobject.linesegment.p1 >= 0 && markupobject.linesegment.p1 <= this.points.length - 1);
                    var bsecondpoint = (markupobject.linesegment.p2 >= 0 && markupobject.linesegment.p2 <= this.points.length - 1);


                    if (bfirstpoint && bsecondpoint) {
                        if(markupobject.type == 1 && markupobject.subtype == 6){


                        }else{
                            var x0 = this.points[markupobject.linesegment.p1].x;
                            var y0 = this.points[markupobject.linesegment.p1].y;
                            var x1 = this.points[markupobject.linesegment.p2].x;
                            var y1 = this.points[markupobject.linesegment.p2].y;
                            var midX = x0 + (x1 - x0) * 0.50;
                            var midY = y0 + (y1 - y0) * 0.50;
                            if (markupobject.linesegment.p2 != 0) {
                                this.points.splice(markupobject.linesegment.p2, 0, { x: midX, y: midY });
                            }else if(this.points.length == 2){
                                this.points.splice(-1, 0, { x: midX, y: midY });
                            }
                            else {
                                this.points.push({ x: midX, y: midY });
                            }
    
                        }
                    
                    }

                    markupobject.setmarkupmodified(true);

                };
                this.movepoint = function (x, y, point) {
                    if (point >= 0 && point <= this.points.length - 1) {
                        this.points[point].x = x;
                        this.points[point].y = y;
                    }
                };
                this.setlastcount = function(x,y){
                    
                    //&& !this.lastpointset
                    if(this.countshapes[this.countshapes.length - 1] ){
                        this.countshapes[this.countshapes.length - 1].x = x;
                        this.countshapes[this.countshapes.length - 1].y = y;
                        //this.lastpointset = true;
                    }

                };

                this.getnumpoints = function(){
                    
                    var numpoints = this.points.length;

                    if(this.type == 1 && this.subtype == 6){
                        //return;
                        for (var lcounter = 0; lcounter < this.pointlist.length; lcounter++) {
                            numpoints += this.pointlist[lcounter].length;
                        }

                    }

                    return numpoints;
                    
                    
                }

                this.setlastpoint = function (x, y) {

                    if(this.points.length > 0){
                        this.points[this.points.length - 1].x = x;
                        this.points[this.points.length - 1].y = y;
                        this.findrectangle();
                    }

                };
                this.getprevpoint = function() {
                    if (this.points.length > 1) {
                        return {
                            x: this.points[this.points.length - 2].x,
                            y: this.points[this.points.length - 2].y
                        };
                    }
                    else {
                        return false;
                    }

                };
                this.getlastcount = function (){

                    if (this.countshapes.length > 0) {
                        return {
                            x: this.countshapes[this.countshapes.length - 1].x,
                            y: this.countshapes[this.countshapes.length - 1].y
                        };
                    }
                    else {
                        return false;
                    }

                };
                this.getlastpoint = function () {
                    if (this.points.length > 0) {
                        return {
                            x: this.points[this.points.length - 1].x,
                            y: this.points[this.points.length - 1].y
                        };
                    }
                    else {
                        return false;
                    }
                };
                this.startdraw = function (ctx) {

                    if(this.type == 1 && this.subtype == 6){
                        return;
                    }

                    ctx.beginPath();
                    ctx.moveTo(this.points[this.points.length - 1].x, this.points[this.points.length - 1].y);
                };
                // common drawing functions
                this.displayurl = function (ctx, x, y) {

                    if((markupobject.type == 11 || markupobject.type == 20) && markupobject.subtype == 3){
                        return;
                    }

                    var labelwidth = 1;
                    var labelheight = 20;
                    var labelType = markupobject.linkURL;
                    ctx.save();
                    ctx.font = "10pt Arial";
                    var labeltypewidth = ctx.measureText(labelType);
                    var labetypesize = labeltypewidth.width;
                    if (labetypesize > labelwidth) {
                        labelwidth = labetypesize;
                    }
                    labelwidth += 10;
                    ctx.strokeStyle = "black";
                    ctx.fillStyle = "rgba(113, 114, 118, 0.8)";
                    ctx.lineWidth = 1;
                    ctx.strokeRect(x, y + 30, labelwidth, labelheight);
                    ctx.fillRect(x, y + 30, labelwidth, labelheight);
                    ctx.fillStyle = "white";
                    ctx.fillText(labelType, x + 3, y + 42);
                    ctx.restore();
                };

                this.hide = function(){
                    markupobject.display = false;
                };

                this.show = function(){
                    markupobject.display = true;
                };
                this.showlabeltype = function(ntype){
                    if (this.type == 8){    
                        
                        this.labeltype = ntype;

                    }
                };

                
                this.getMarkupProperties = function(){
                   

                    //"type": "OPEN_SINGLE_END",
                    //"type": "FILLED_SINGLE_END",
                    //"type": "OPEN_BOTH_ENDS",
                    //"type": "FILLED_BOTH_ENDS",

                    //"type": "BAR",
                    //"type": "OPEN_ARROW",

                    var properties = {
                        lineColor : false,
                        fillColor : false,
                        textColor : false,
                        lineWidth : false,
                        lineStyle : false,
                        clockwise : false,
                        customlabel : false,
                        display : true,
                        countsize : false,
                        showperimeter : false,
                        endpoint_open_single : false,
                        endpoint_open_both : false,
                        endpoint_filled_single : false,
                        endpoint_filled_both : false,
                        endpoints_bar : false,
                        endpoints_arrow : false,
                        lineSegmentWidth : false,
                        transpacency : false,
                        rotate : false,
                        snap : false,
                        fill : false,
                        fontName : false,
                        fontHeight : false,
                        labelsize : false, 
                        fontBold : false,
                        fontItalic : false
                    };      

                    var labeltype = this.getMarkupType();

                    properties = RxCore_getMarkupProperties({label : labeltype.label, typename : labeltype.type, type : this.type, subtype : this.subtype});

                    //properties = RxCore_getMarkupProperties({label : "Count", typename : 'COUNT', type : 13, subtype : 0});
                    //labelType.label = "Count";
                    //labelType.type = 'COUNT';

            
                    
                    
                    return properties;
            
                };                      



                this.getMarkupType = function(){

                    var labelType = {label : "", type : "", groups :[]};

                    switch (this.type) {
                        case 0:
                            if (this.subtype == 0) {
                                labelType.label = "Freehand pen";
                                labelType.type = 'PEN';
                            }
                            if (this.subtype == 1) {
                                labelType.label = "Eraser";
                                labelType.type = 'ERASER';
                            }
                            break;
                        case 1:
                            if (this.subtype == 0) {
                                labelType.label = "Polygon";
                                labelType.type = 'POLYGON';
                            }
                            if (this.subtype == 1) {
                                labelType.label = "Polyline";
                                labelType.type = ['PEN','POLYLINE'];

                            }
                            if (this.subtype == 2) {
                                labelType.label = "Polygon";
                                labelType.type = 'POLYGON';
                            }
                            if (this.subtype == 3) {
                                labelType.label = "Measure Path";
                                labelType.type = 'PATH';
                            }

                            if (subtype == 4) {
                                labelType.label = "Measure angle";
                                labelType.type = 'ANGLE';
                            }
        
                            if (subtype == 5) {
                                labelType.label = "Measure angle";
                                labelType.type = 'ANGLEBCKWRD';
                            }
                            if (subtype == 6) {
                                labelType.label = "Measure segment";
                                labelType.type = 'SEGMENT';
                            }
        
                            if (subtype == 7) {
                                labelType.label = "Revision Cloud";
                                labelType.type = 'REVCLOUD';
                            }

                            break;
                        case 2:
                            labelType.label = "Polycurve";
                            labelType.type = ['SHAPE', 'POLYCURVE'];

                            break;
                        case 3:
                            if (this.subtype == 0) {
                                labelType.label = "Rectangle";
                                labelType.type = ['SHAPE', 'RECTANGLE'];
                            }
                            if (this.subtype == 1) {
                                labelType.label = "Rounded Rectangle";
                                labelType.type = ['SHAPE', 'ROUNDED_RECTANGLE'];
                            }
                            if (this.subtype == 3) {
                                labelType.label = "Marker";
                                labelType.type = 'MARKER';
                            }
                            if (this.subtype == 6) {
                                labelType.label = "Rectanglular Area";
                                labelType.type = 'AREARECT';
                            }

                            break;
                        case 4:
                            if (this.subtype == 0) {
                                labelType.label = "Oval";
                                labelType.type = ['SHAPE', 'ELLIPSE'];
                            }
                            if (this.subtype == 1) {
                                labelType.label = "Circle";
                                labelType.type = ['SHAPE', 'CIRCLE'];
                            }
                            break;
                        case 5:
                            labelType.label = "Revision Cloud";
                            labelType.type = ['SHAPE', 'CLOUD'];

                            break;
                        case 6:
                            if (this.subtype == 9) {
                                labelType.label = "Line";
                                labelType.type = ['ARROW', 'NO_ENDS'];
                            } else if(this.subtype == 6){

                                labelType.label = "Callout";
                                labelType.type = 'TEXTARROW';

                            }else if (this.subtype == 0){
                                labelType.label = "Arrow";
                                labelType.type = ['OPEN_SINGLE_END', 'ARROW'];
                            }else if (this.subtype == 1){
                                labelType.label = "Arrow";
                                labelType.type = ['FILLED_SINGLE_END', 'ARROW'];
                            }else if (this.subtype == 2){
                                labelType.label = "Arrow";    
                                labelType.type = ['OPEN_BOTH_ENDS', 'ARROW'];
                            }else if (this.subtype == 3){
                                labelType.label = "Arrow";
                                labelType.type = ['FILLED_BOTH_ENDS','ARROW'];
                            }
                            else {
                                labelType.label = "Arrow";
                            }

                            break;
                        case 7:
                            labelType.label = "Dimension Line";

                            if (this.subtype == 0){
                                labelType.type = ["BAR","DIMENSION"];
                            }else if (this.subtype == 1){
                                labelType.type = ["OPEN_ARROW", "DIMENSION"];
                            }
                            break;
                        case 8:
                            if (this.subtype == 1) {
                                labelType.label = "Space";
                                labelType.type = 'SPACE';
                            }
                            else {
                                labelType.label = "Area";
                                labelType.type = 'AREA';
                            }
                            break;
                        case 9:
                            
                            if (this.bhasArrow) {
                                labelType.label = "Callout";
                                labelType.type = 'TEXT';
                            }else{
                                labelType.label = "Text";
                                labelType.type = 'TEXT';
                            }
                            break;
                        case 11:
                            /*{label : "Image", typename : 'IMAGE', type : 11, subtype : 0},
                            {label : "Button", typename : 'BUTTON', type : 11, subtype : 3},
                            {label : "Signature", typename : 'SIGNATURE', type : 11, subtype : 4},
                            {label : "Stamp", typename : 'IMAGESTAMP', type : 11, subtype : 12},
                            {label : "Symbol", typename : 'SYMBOL', type : 11, subtype : 1},
                            */
            
                            switch(subtype){
                                case 0:
                                    labelType.label = "Image";
                                    labelType.type = 'IMAGE';
                                    break;
                                case 1:
                                    labelType.label = "Symbol";
                                    labelType.type = 'SYMBOL';
                                    break;
                                case 3:
                                    labelType.label = "Button";
                                    labelType.type = 'BUTTON';
                                    break;
                                case 4:
                                    labelType.label = "Signature";
                                    labelType.type = 'SIGNATURE';
                                    break;
                                case 12:
                                    labelType.label = "Stamp";
                                    labelType.type = 'IMAGESTAMP';
                                    break;

                                case 20:
                                    labelType.label = "Link";
                                    labelType.type = 'Link';
                                    
        
                                    break;
            
                                case 21:
                                    labelType.label = "InteractiveStamp";
                                    labelType.type = 'InteractiveStamp';
                                        
            
                                    break;
            
                                
                            }



                            

                            break;
                        case 10:
                            labelType.label = "Note";
                            labelType.type = 'NOTE';
                            
                            break;
                        case 12:
                            
                            labelType.label = "Stamp";
                            labelType.type = 'STAMP';

                            break;
                        case 13:    
                            labelType.label = "Count";
                            labelType.type = 'COUNT';
                            //labelType.groups = [{"items": [{"type": "FILL","title": "Count","tooltip": "Count options","options":  {"groups":  [ {"items":  [ {"type": "FILL_COLOR","title": "Color","tooltip": "Fill Color"}]}]}}]}];
                            break;

                        case 14:    
                            labelType.label = "MeasureArc";
                            labelType.type = 'MEASUREARC';
    
                            break;
                        

 
                    }
                    
                    return labelType;

                };

                this.getDisplayName = function(){
                    return GetDisplayName(markupobject.signature);
                };

                this.getLayerName = function(){
                    return Layerlist[markupobject.layer].Name;
                };                
                this.replaceImage = function(dropdata){

                    //if(markupobject.type == 11 && markupobject.subtype == 2){
                    
                    if((markupobject.type == 11 || markupobject.type == 20) && markupobject.subtype == 2){
                    

                        markupobject.w = dropdata.width;
                        markupobject.h = dropdata.height;
                
                        var createimage = new Image();
                        createimage.src = dropdata.src;
                        markupobject.image = createimage;
                        markupobject.imageloaded = true;

                    //}else if(markupobject.type == 11 && markupobject.subtype == 3) {
                   
                    }else if((markupobject.type == 11 || markupobject.type == 20) && markupobject.subtype == 3) {                        
    
                    

                        markupobject.w = dropdata.width;
                        markupobject.h = dropdata.height;
                
                        var createimage = new Image();
                        createimage.src = dropdata.src;
                        markupobject.image = createimage;
                        markupobject.imageloaded = true;
                        markupobject.subtype = 4;

                    }



                    //markupobject.src = dropdata.src;
                    //this.drawmeselected
                };
                this.displaylabel = function (ctx, x, y) {
                    
                    
                    if(RxCore_GUI_MarkupHover != undefined){
                        RxCore_GUI_MarkupHover.setMarkupHover(markupobject, x, y);
                    }


                    
                    if (!bmarkupLabels) {
                        return;
                    }
                    var labelType = "";
                    var labelType = markupobject.getMarkupType();
                    var labelSign = markupobject.getDisplayName();
                    //var labelSign = GetDisplayName(this.signature);
                    //var labelLayer = "Markup Layer " + this.layer;
                    //var labelLayer = Layerlist[this.layer].Name;
                    var labelLayer = markupobject.getLayerName();
                    var labeldate = markupobject.GetDateTime(true);
                    //var labeldate = this.GetDateTime(true);
                    var notetext = "";
                    var labelwidth = 1;
                    var labelheight = (10 * 4) + 10;
                    var noteBool = false;
                    ctx.save();
                    ctx.font = "10pt Arial";
                    switch (this.type) {

                        case 9:
                            //labelType = "Text";
                            notetext = this.text;
                            var labelnotewidth = ctx.measureText(notetext);
                            var labelnotesize = labelnotewidth.width;
                            /*if (labelnotesize > labelwidth) {
                                labelwidth = labelnotesize;
                            }
                            labelheight = (10 * 4) + 10;
                            noteBool = true;*/
                            break;
                        case 10:
                            //labelType = "Note";

                            if (this.text != undefined) {
                                notetext = this.text;
                            }
                            else {
                                notetext = "";
                            }
                            var disptext = wordWrap(notetext, 30);
                            var textarray = disptext.split('\n');
                            labelnotewidth = 0;
                            for (var i = 0; i < textarray.length; i++) {
                                if (ctx.measureText(textarray[i]) > labelnotewidth) {
                                    labelnotewidth = ctx.measureText(textarray[i]);
                                }
                            }
                            //labelnotewidth = ctx.measureText(notetext);
                            labelnotesize = labelnotewidth.width;
                            if (labelnotesize > labelwidth) {
                                labelwidth = labelnotesize;
                            }
                            labelheight = (10 * 5) + 10;
                            noteBool = true;
                            break;
                    }
                    var labeltypewidth = ctx.measureText(labelType);
                    var labetypesize = labeltypewidth.width;
                    if (labetypesize > labelwidth) {
                        labelwidth = labetypesize;
                    }
                    var labelSignwidth = ctx.measureText(labelSign);
                    var labelsignsize = labelSignwidth.width;
                    if (labelsignsize > labelwidth) {
                        labelwidth = labelsignsize;
                    }
                    var labelLayerwidth = ctx.measureText(labelLayer);
                    var labellayersize = labelLayerwidth.width;
                    if (labellayersize > labelwidth) {
                        labelwidth = labellayersize;
                    }
                    var labeldatewidth = ctx.measureText(labeldate);
                    var labeldatesize = labeldatewidth.width;
                    if (labeldatesize > labelwidth) {
                        labelwidth = labeldatesize;
                    }
                    if (this.type != 10) {
                        labelwidth += 10;
                        ctx.strokeStyle = "black";
                        ctx.fillStyle = "rgba(113, 114, 118, 0.8)";
                        ctx.lineWidth = 1;
                        ctx.strokeRect(x, y + 30, labelwidth, labelheight);
                        ctx.fillRect(x, y + 30, labelwidth, labelheight);
                        ctx.fillStyle = "white";
                        ctx.fillText(labelType, x + 3, y + 42);
                        ctx.fillText(labelSign, x + 3, y + 54);
                        ctx.fillText(labelLayer, x + 3, y + 66);
                        ctx.fillText(labeldate, x + 3, y + 78);
                        if (noteBool) {
                            ctx.fillText(notetext, x + 3, y + 90);
                        }
                    }
                    else {
                        if (labelwidth < 200) {
                            labelwidth = 200;
                        }
                        else {
                            labelwidth += 10;
                        }
                        labelheight = 150;
                        ctx.strokeStyle = "black";
                        ctx.fillStyle = "rgba(255, 255, 0, 0.8)";
                        ctx.lineWidth = 1;
                        ctx.strokeRect(x, y + 30, labelwidth, labelheight);
                        ctx.fillRect(x, y + 30, labelwidth, labelheight);
                        ctx.fillStyle = "black";
                        ctx.save();
                        ctx.beginPath();
                        ctx.rect(x, y + 30, labelwidth, labelheight);
                        ctx.clip();
                        ctx.fillText(labelSign, x + 5, y + 46);
                        ctx.fillText(labeldate, x + 5, y + 60);
                        ctx.fillText(labelLayer, x + 5, y + 74);
                        //ctx.fillText(labelLayer, x+3 , y+66);
                        if (noteBool) {
                            var ystart = y + 100;
                            for (i = 0; i < textarray.length; i++) {
                                ctx.fillText(textarray[i], x + 5, ystart);
                                ystart += 12;
                            }
                            //ctx.fillText(notetext, x+3 , y+100);
                        }
                        ctx.restore();
                    }
                    ctx.restore(); // restore context to what it was on entry
                };
                this.Rect = function (ctx, x, y, width, height, linewidth, fill, stroke, fillcolor, strokecolor) {
                    ctx.save(); // save the context so we don't mess up others
                    ctx.strokeStyle = strokecolor;
                    ctx.fillStyle = fillcolor;
                    ctx.lineWidth = linewidth;
                    if (markupobject.rotation != 0 && markupobject.type != 9) {
                        var tx = x + (width / 2);
                        var ty = y + (height / 2);
                        ctx.translate(tx, ty);
                        ctx.rotate(markupobject.rotation);
                        ctx.translate(-tx, -ty);
                    }
                    ctx.beginPath();
                    ctx.moveTo(x, y);
                    ctx.lineTo(x + width, y);
                    ctx.lineTo(x + width, y + height);
                    ctx.lineTo(x, y + height);
                    ctx.closePath();
                    if (fill) {
                        ctx.fill();
                    }
                    if (stroke) {
                        ctx.stroke();
                    }
                    ctx.restore(); // restore context to what it was on entry
                };
                this.drawradius = function (ctx, x, y, w, h, crossl, linewidth, strokecolor, bdiameter) {
                    ctx.save(); // save the context so we don't mess up others
                    ctx.strokeStyle = strokecolor;
                    ctx.lineWidth = 2;
                    var xdiff = Math.max(x, w) - Math.min(x, w);
                    var ydiff = Math.max(y, h) - Math.min(y, h);
                    var radius = markupobject.getdiag(xdiff, ydiff);
                    //draw center cross.
                    ctx.beginPath();
                    ctx.moveTo(x - crossl, y);
                    ctx.lineTo(x + crossl, y);
                    ctx.moveTo(x, y - crossl);
                    ctx.lineTo(x, y + crossl);
                    ctx.stroke();
                    //draw leader
                    ctx.restore(); // restore context to what it was on entry
                };
                this.radiusDimLeader = function (ctx, x, y, w, h, linewidth, strokecolor) {


                };
                this.Circle = function (ctx, x, y, w, h, linewidth, fill, stroke, fillcolor, strokecolor) {
                    ctx.save(); // save the context so we don't mess up others
                    ctx.strokeStyle = strokecolor;
                    ctx.fillStyle = fillcolor;
                    ctx.lineWidth = linewidth;
                    /*if (markupobject.rotation != 0) {
                        ctx.translate(x, y);
                        ctx.rotate(markupobject.rotation);
                        ctx.translate(-x, -y);
                    }*/
                    var xdiff = Math.max(x, w) - Math.min(x, w);
                    var ydiff = Math.max(y, h) - Math.min(y, h);
                    var radius = markupobject.getdiag(xdiff, ydiff);
                    ctx.beginPath();
                    ctx.arc(x, y, radius, 0, 2 * Math.PI);
                    if (fill) {
                        ctx.fill();
                    }
                    if (stroke) {
                        ctx.stroke();
                    }
                    ctx.restore(); // restore context to what it was on entry
                };
                this.Oval = function (ctx, x, y, width, height, linewidth, fill, stroke, fillcolor, strokecolor) {
                    var ox = (width / 2) * kappa;
                    var oy = (height / 2) * kappa;
                    var xe = x + width;
                    var ye = y + height;
                    var xm = x + width / 2;
                    var ym = y + height / 2;
                    ctx.save(); // save the context so we don't mess up others
                    ctx.strokeStyle = strokecolor;
                    ctx.fillStyle = fillcolor;
                    ctx.lineWidth = linewidth;
                    if (markupobject.rotation != 0) {
                        var tx = x + (width / 2);
                        var ty = y + (height / 2);
                        ctx.translate(tx, ty);
                        ctx.rotate(markupobject.rotation);
                        ctx.translate(-tx, -ty);
                    }
                    ctx.beginPath();
                    ctx.moveTo(x, ym);
                    ctx.bezierCurveTo(x, ym - oy, xm - ox, y, xm, y);
                    ctx.bezierCurveTo(xm + ox, y, xe, ym - oy, xe, ym);
                    ctx.bezierCurveTo(xe, ym + oy, xm + ox, ye, xm, ye);
                    ctx.bezierCurveTo(xm - ox, ye, x, ym + oy, x, ym);
                    ctx.closePath();
                    if (fill) {
                        ctx.fill();
                    }
                    if (stroke) {
                        ctx.stroke();
                    }
                    ctx.restore(); // restore context to what it was on entry
                };
                /*this.polygon=function(ctx,linewidth,fill,stroke,fillcolor,strokecolor){
                 var counter = 0;
        
                 ctx.save();
        
                 ctx.strokeStyle = strokecolor;
                 ctx.lineWidth = linewidth;
                 ctx.fillStyle = fillcolor;
                 ctx.beginPath();
                 ctx.moveTo(markupobject.points[0].x, markupobject.points[0].y);
        
                 for (counter=1;counter<markupobject.points.length;counter++){
                 ctx.lineTo(markupobject.points[counter].x, markupobject.points[counter].y);
                 }
                 ctx.closePath();
        
        
                 if(fill){
                 ctx.fill();
                 }
                 if(stroke){
                 ctx.stroke();
                 }
                 ctx.restore();
        
        
                 };*/

                 this.addcountLoad = function(mousepos){

                    markupobject.countshapes.push({x : mousepos.x, y : mousepos.y});

                    //markupobject.lastpointset = false;
                    //console.log(mousepos);

                };

                this.addcount = function(mousepos){
                    markupobject.countshapes.push({x : mousepos.x, y : mousepos.y});

                    var coords = markupobject.setDimDrawme();
                    this.setlastcount(coords.w, coords.h);

                };
                this.getcount = function(){
                    
                    markupobject.dimtext = markupobject.countshapes.length.toString();
                    return markupobject.countshapes.length;

                };
                this.setCountSize = function(nsize){

                    //this.innerRadius *= internalscale;
                    markupobject.outerRadius = nsize;
                    markupobject.innerRadius = nsize * 0.4;

                };
                this.shapes = function(ctx, subtype, outerRadius, innerRadius, linewidth, fillcolor, strokecolor, scalefactor, pagedx, pagedy){
                    
                    if(markupobject.countshapes.length == 0){
                        return;
                    }


                    var counter = 0;

                    var outerRadiusScaled = outerRadius * markupobject.fixedscaleFactor;
                    var innerRadiusScaled = innerRadius * markupobject.fixedscaleFactor;


                    if(bUseNoScale){
                        var outerRadiusScaled = outerRadius;
                        var innerRadiusScaled = innerRadius;
                    }
                    

                    var xscalepoint = 0;
                    var yscalepoint = 0;


                    for (counter = 0; counter < markupobject.countshapes.length; counter++) {

                        xscalepoint = (markupobject.countshapes[counter].x - this.xoffset) * scalefactor;
                        yscalepoint = (markupobject.countshapes[counter].y - this.yoffset) * scalefactor;


                        
                        xscalepoint += pagedx;
                        yscalepoint += pagedy;
                        
                        switch (subtype){
                            case 0: 
                                drawCircle(ctx, xscalepoint, yscalepoint, outerRadiusScaled, innerRadiusScaled, linewidth, fillcolor, fillcolor);
                                break;

                            case 1:

                                //square
                                
                                drawSquare(ctx, xscalepoint, yscalepoint, outerRadiusScaled, innerRadiusScaled, linewidth, fillcolor, fillcolor);
                                break;

                            case 2:

                                //triangle

                                drawTriangle(ctx, xscalepoint, yscalepoint, outerRadiusScaled, innerRadiusScaled, linewidth, fillcolor, fillcolor);

                                break;

                            case 3: 
                            
                                //diamond
                                drawDiamond(ctx, xscalepoint, yscalepoint, outerRadiusScaled, innerRadiusScaled, linewidth, fillcolor, fillcolor);
                                
                                break;

                            case 4: //star

                                innerRadius = nCounterSize * 0.4;
                                innerRadiusScaled = innerRadius * markupobject.fixedscaleFactor;

                                drawStar(ctx, xscalepoint, yscalepoint, outerRadiusScaled, innerRadiusScaled, linewidth, fillcolor, fillcolor);

                                break;

                            case 5:
                            
                                innerRadius = nCounterSize * 0.5;
                                innerRadiusScaled = innerRadius * markupobject.fixedscaleFactor;

                                //cross
                                linewidth = innerRadiusScaled * 0.8;
                                
                                drawCross(ctx, xscalepoint, yscalepoint, outerRadiusScaled, innerRadiusScaled, linewidth, fillcolor, fillcolor);

                                break;

                            case 6:

                                
                                innerRadius = nCounterSize * 0.5;
                                innerRadiusScaled = innerRadius * markupobject.fixedscaleFactor;
                                
                                linewidth = innerRadiusScaled * 0.8;
                                drawTick(ctx, xscalepoint, yscalepoint, outerRadiusScaled, innerRadiusScaled, linewidth, fillcolor, fillcolor);

                                break;

                            case 7:

                                innerRadius = nCounterSize * 0.5;
                                innerRadiusScaled = innerRadius * markupobject.fixedscaleFactor;

                                //dcross
                                linewidth = innerRadiusScaled * 0.8;
                                
                                drawDiagonalCross(ctx, xscalepoint, yscalepoint, outerRadiusScaled, innerRadiusScaled, linewidth, fillcolor, fillcolor);

                                

                                break;
                                

                        }
                    }

                };


                this.polygoncloud = function (ctx, radius, linewidth, fill, stroke, fillcolor, strokecolor, scalefactor, dx, dy, useoffset, closed ) {
                    if (this.points.length == 0) {
                      return;
                    }
          
                    var diameter = radius * 2;
          
                    var counter = 0;
                    var xscaled = (this.points[0].x - this.xoffset) * scalefactor;
                    var yscaled = (this.points[0].y - this.yoffset) * scalefactor;
          
                    if (useoffset) {
                      xscaled += dx;
                      yscaled += dy;
                    }
          
                    var startX = xscaled;
                    var startY = yscaled;
          
                    var endX = xscaled;
                    var endY = yscaled;
          
          
                    ctx.save();
          
                    if (markupobject.bUseFixedScale) {
                      ctx.lineWidth = linewidth * markupobject.fixedscaleFactor;
                    } else {
                      ctx.lineWidth = linewidth * scalefactor;
                    }
          
                    if (bUseNoScale) {
                      ctx.lineWidth = linewidth;
                    }
          
                    ctx.fillStyle = fillcolor;
                    ctx.strokeStyle = strokecolor;
          
          
                    ctx.beginPath();
          
                    
          
                    for (counter = 1; counter < markupobject.points.length; counter++) {
                     
          
                      startX = xscaled;
                      startY = yscaled;
          
                      xscaled = (this.points[counter].x - this.xoffset) * scalefactor;
                      yscaled = (this.points[counter].y - this.yoffset) * scalefactor;
          
                      if (useoffset) {
                        xscaled += dx;
                        yscaled += dy;
                      }
          
                      
          
                      endX = xscaled;
                      endY = yscaled;
          
                      const distance = Math.sqrt( (endX - startX) ** 2 + (endY - startY) ** 2 );
                      const angle = Math.atan2(endY - startY, endX - startX);
                      
          
                      var diameter = radius * 2;
          
          
                      for (let j = 0; j <= distance; j += diameter) {
                        const x = startX + j * Math.cos(angle);
                        const y = startY + j * Math.sin(angle);
                        
          
                        if(counter > 1 && j == 0){
                          ctx.arc(x, y, radius, (Math.PI * 0.5) + angle, 0 + angle); 
                        }else{
                          ctx.arc(x, y, radius, Math.PI + angle, 0 + angle);  
                        }
                        
                        
                      }
          
                      
                      if (stroke) {
                        ctx.stroke();
                        ctx.beginPath();
                      }
          
                      
                    }
          
                    if(closed && markupobject.points.length > 2){
          
                      startX = endX;
                      startY = endY;
          
                      xscaled = (this.points[0].x - this.xoffset) * scalefactor;
                      yscaled = (this.points[0].y - this.yoffset) * scalefactor;
            
                      if (useoffset) {
                        xscaled += dx;
                        yscaled += dy;
                      }
            
                      var endX = xscaled;
                      var endY = yscaled;
          
                      const distance = Math.sqrt( (endX - startX) ** 2 + (endY - startY) ** 2 );
                      const angle = Math.atan2(endY - startY, endX - startX);
                      
          
                      var diameter = radius * 2;
          
          
                      for (let j = 0; j <= distance; j += diameter) {
                        const x = startX + j * Math.cos(angle);
                        const y = startY + j * Math.sin(angle);
                        
          
                        if(counter > 1 && j == 0){
                          ctx.arc(x, y, radius, (Math.PI * 0.5) + angle, 0 + angle); 
                        }else{
                          ctx.arc(x, y, radius, Math.PI + angle, 0 + angle);  
                        }
                        
                        
                      }
          
                      
                      if (stroke) {
                        ctx.stroke();
                        //ctx.beginPath();
                      }
          
            
                    }
          
          
                    if (fill) {
                      ctx.fill();
                    }
                    ctx.restore();
                              
                };                
                

                
                this.polygonhole = function (ctx, linewidth, fill, stroke, fillcolor, strokecolor, scalefactor, dx, dy, useoffset, hole) {

                    if(hole.points.length == 0){
                        return;
                    }

                    var lastpoint = hole.points.length - 1;

                    var counter = lastpoint;
                    /*var xscaled = (hole.points[lastpoint].x - this.xoffset) * scalefactor;
                    var yscaled = (hole.points[lastpoint].y - this.yoffset) * scalefactor;

                    if (useoffset) {
                        xscaled += dx;
                        yscaled += dy;
                    }*/
                    
                    scalefactor = scalefactor / hole.scaling;
                    
                    //      ctx.moveTo(markupobject.points[0].x, markupobject.points[0].y);
                    
                    for (counter = lastpoint; counter >= 0; counter--) {

                        
                        //            ctx.lineTo(markupobject.points[counter].x, markupobject.points[counter].y);
                        var xscaled = (hole.points[counter].x - hole.xoffset) * scalefactor;
                        var yscaled = (hole.points[counter].y - hole.yoffset) * scalefactor;
                        if (useoffset) {
                            xscaled += dx;
                            yscaled += dy;
                        }


                        if(counter == lastpoint){
                            ctx.moveTo(xscaled, yscaled);
                        }


                        ctx.lineTo(xscaled, yscaled);
                    }

                    ctx.closePath();

                    


                };

                this.polygon = function (ctx, linewidth, fill, stroke, fillcolor, strokecolor, scalefactor, dx, dy, useoffset) {
                    
                    if(this.points.length == 0){
                        return;
                    }

                    var counter = 0;
                    var xscaled = (this.points[0].x - this.xoffset) * scalefactor;
                    var yscaled = (this.points[0].y - this.yoffset) * scalefactor;
                    if (useoffset) {
                        xscaled += dx;
                        yscaled += dy;
                    }
                    ctx.save();
                    ctx.strokeStyle = strokecolor;


                    if (markupobject.bUseFixedScale) {
                        ctx.lineWidth = linewidth * markupobject.fixedscaleFactor;
                    }else{
                        ctx.lineWidth = linewidth * scalefactor;
                    }

                    if(bUseNoScale) {
                        ctx.lineWidth = linewidth;
                    }


                    ctx.fillStyle = fillcolor;
                    ctx.beginPath();
                    //      ctx.moveTo(markupobject.points[0].x, markupobject.points[0].y);
                    ctx.moveTo(xscaled, yscaled);
                    for (counter = 1; counter < markupobject.points.length; counter++) {
                        //            ctx.lineTo(markupobject.points[counter].x, markupobject.points[counter].y);
                        xscaled = (this.points[counter].x - this.xoffset) * scalefactor;
                        yscaled = (this.points[counter].y - this.yoffset) * scalefactor;
                        if (useoffset) {
                            xscaled += dx;
                            yscaled += dy;
                        }
                        ctx.lineTo(xscaled, yscaled);
                    }


                    ctx.closePath();

                    var holescalefactor = scalefactor * markupobject.scaling;
                    //scalefactor = scalefactor / hole.scaling;
                    

                    if(this.holes.length != 0){
                        for(var hi = 0; hi < this.holes.length; hi++){

                            //this.holes[hi].SetDimensions(scalefactor, pagerotation, pagedx, pagedy);

                            this.polygonhole(ctx, linewidth, fill, stroke, fillcolor, strokecolor, holescalefactor, dx, dy, useoffset, this.holes[hi]);
                            
                        }
                    }


                    if (fill) {
                        ctx.fill();
                    }
                    if (stroke) {
                        ctx.stroke();
                    }
                    ctx.restore();
                };

                this.getangleofline = function (startx, starty, endx, endy, useabs) {

                    var width = 0;
                    var height = 0;
                    
                    if (useabs) {
                        width = Math.abs(endx - startx);
                        height = Math.abs(endy - starty);
                    } else {
                        width = endx - startx;
                        height = endy - starty;
                    }

                    //var angleRadians = Math.atan2(p2.y - p1.y, p2.x - p1.x);


                    //var angleobj = {angle : Math.atan2(height, width), pointx : endx, pointy : endy}

                    return {angle : Math.atan2(height, width), pointx : endx, pointy : endy}
                };

                /**
                 * Calculates the angle in radians between two lines meeting at a common vertex.
                 * @param {number} x1 - X of point on first line
                 * @param {number} y1 - Y of point on first line
                 * @param {number} cx - X of common vertex (angle point)
                 * @param {number} cy - Y of common vertex (angle point)
                 * @param {number} x2 - X of point on second line
                 * @param {number} y2 - Y of point on second line
                 * @returns {number} angle in radians (0 to π)
                 */
                this.calculateAngleBetweenLines = function(x1, y1, cx, cy, x2, y2){
                
                    let v1x = x1 - cx;
                    let v1y = y1 - cy;

                    let v2x = x2 - cx;
                    let v2y = y2 - cy;

                    let len1 = Math.hypot(v1x, v1y);
                    let len2 = Math.hypot(v2x, v2y);

                    if (len1 === 0 || len2 === 0) return 0; // Prevent divide-by-zero

                    // Normalize both vectors
                    v1x /= len1;
                    v1y /= len1;
                    v2x /= len2;
                    v2y /= len2;

                    // Dot product gives cos(θ)
                    let dot = v1x * v2x + v1y * v2y;
                    dot = Math.max(-1, Math.min(1, dot)); // Clamp to avoid NaN from rounding

                    // Return the angle in radians (always the smaller angle: 0 to π)
                    return Math.acos(dot);
                }

                


                this.polylineSegments = function (ctx, linewidth, strokecolor, scalefactor, dx, dy, useoffset){

                    var xscalepoint = 0;
                    var yscalepoint = 0;

                    var counter = 0;
                    var lcounter = 0;

                    ctx.save();
                    ctx.strokeStyle = strokecolor;

                    if (markupobject.bUseFixedScale) {
                        ctx.lineWidth = linewidth * markupobject.fixedscaleFactor;
                    }else{
                        ctx.lineWidth = linewidth * scalefactor;
                    }

                    if(bUseNoScale) {
                        ctx.lineWidth = linewidth;
                    }


                    ctx.beginPath();


                    for (counter = 0; counter < markupobject.points.length; counter++) {
                        //            ctx.lineTo(markupobject.points[counter].x, markupobject.points[counter].y);
                        xscalepoint = (this.points[counter].x - this.xoffset) * scalefactor;
                        yscalepoint = (this.points[counter].y - this.yoffset) * scalefactor;
                        if (useoffset) {
                            xscalepoint += dx;
                            yscalepoint += dy;
                        }

                        if (counter == 0) {
                            ctx.moveTo(xscalepoint, yscalepoint);
                        }
                        else {
                            ctx.lineTo(xscalepoint, yscalepoint);
                        }

                        
                    }
                    ctx.stroke();

                    ctx.beginPath();

                    for (lcounter = 0; lcounter < this.pointlist.length; lcounter++) {
                        for (counter = 0; counter < this.pointlist[lcounter].length; counter++) {

                            xscalepoint = (this.pointlist[lcounter][counter].x - this.xoffset) * scalefactor;
                            yscalepoint = (this.pointlist[lcounter][counter].y - this.yoffset) * scalefactor;
                            //xscalepoint = (this.points[counter].x - this.xoffset) * scalefactor;
                            //yscalepoint = (this.points[counter].y - this.yoffset) * scalefactor;
                            if(useoffset){
                                xscalepoint += dx;
                                yscalepoint += dy;
                            }
                            

                            if (counter == 0) {
                                ctx.moveTo(xscalepoint, yscalepoint);
                            }
                            else {
                                ctx.lineTo(xscalepoint, yscalepoint);
                            }
                        }
                    }

                    ctx.stroke();
                    ctx.restore();


                };
                this.polyline = function (ctx, linewidth, strokecolor, scalefactor, dx, dy, useoffset) {
                    
                    if(this.points.length == 0){
                        return;
                    }

                    
                    var counter = 0;
                    var xscaled = (this.points[0].x - this.xoffset) * scalefactor;
                    var yscaled = (this.points[0].y - this.yoffset) * scalefactor;
                    if (useoffset) {
                        xscaled += dx;
                        yscaled += dy;
                    }
                    ctx.save();
                    ctx.strokeStyle = strokecolor;

                    if (markupobject.bUseFixedScale) {
                        ctx.lineWidth = linewidth * markupobject.fixedscaleFactor;
                    }else{
                        ctx.lineWidth = linewidth * scalefactor;
                    }

                    if(bUseNoScale) {
                        ctx.lineWidth = linewidth;
                    }

                    
                    ctx.beginPath();
                    //      ctx.moveTo(markupobject.points[0].x, markupobject.points[0].y);
                    ctx.moveTo(xscaled, yscaled);
                    for (counter = 1; counter < markupobject.points.length; counter++) {
                        //            ctx.lineTo(markupobject.points[counter].x, markupobject.points[counter].y);
                        xscaled = (this.points[counter].x - this.xoffset) * scalefactor;
                        yscaled = (this.points[counter].y - this.yoffset) * scalefactor;
                        if (useoffset) {
                            xscaled += dx;
                            yscaled += dy;
                        }
                        ctx.lineTo(xscaled, yscaled);
                    }
                    //ctx.closePath();
                    ctx.stroke();
                    ctx.restore();
                };

                this.drawangle = function(ctx, linewidth, strokecolor, scalefactor, dx, dy, useoffset, bmeasureOuter){
                    if (this.points.length < 2) return; // At least one line segment is needed

                    const fullCircle = 2 * Math.PI;
                    const norm = a => (a + fullCircle) % fullCircle;

                    const toScaled = (pt) => ({
                        x: (pt.x - this.xoffset) * scalefactor + (useoffset ? dx : 0),
                        y: (pt.y - this.yoffset) * scalefactor + (useoffset ? dy : 0)
                    });

                    const p0 = toScaled(this.points[0]);
                    const p1 = toScaled(this.points[1]);
                    const hasSecondLine = this.points.length >= 3;
                    const p2 = hasSecondLine ? toScaled(this.points[2]) : null;

                    ctx.save();
                    ctx.strokeStyle = strokecolor;

                    const scaleFactor = markupobject.bUseFixedScale ? markupobject.fixedscaleFactor : scalefactor;
                    ctx.lineWidth = bUseNoScale ? linewidth : linewidth * scaleFactor;
                    const arcradius = nanglearcrad * (bUseNoScale ? 1 : scaleFactor);
                    const labelradius = (nanglearcrad + 35) * (bUseNoScale ? 1 : scaleFactor);

                    // Draw one or both lines
                    ctx.beginPath();
                    ctx.moveTo(p0.x, p0.y);
                    ctx.lineTo(p1.x, p1.y);
                    if (hasSecondLine) {
                        ctx.lineTo(p2.x, p2.y);
                    }
                    ctx.stroke();

                    // Only draw arc and label if both lines exist
                    if (hasSecondLine) {
                        const angle1 = Math.atan2(p0.y - p1.y, p0.x - p1.x);
                        const angle2 = Math.atan2(p2.y - p1.y, p2.x - p1.x);

                        let startAngle = norm(angle1);
                        let endAngle = norm(angle2);
                        let sweep = (endAngle - startAngle + fullCircle) % fullCircle;

                        let anticlockwise = false;
                        if ((sweep > Math.PI && !bmeasureOuter) || (sweep <= Math.PI && bmeasureOuter)) {
                            anticlockwise = true;
                        }

                        // Clamp arc radius to shortest line length
                        const line1Length = Math.hypot(p0.x - p1.x, p0.y - p1.y);
                        const line2Length = Math.hypot(p2.x - p1.x, p2.y - p1.y);
                        const minLineLength = Math.min(line1Length, line2Length);

                        const adjustedArcRadius  = Math.min(arcradius, minLineLength);

                        // Draw arc
                        ctx.beginPath();
                        ctx.arc(p1.x, p1.y, adjustedArcRadius, startAngle, endAngle, anticlockwise);
                        ctx.strokeStyle = "blue";
                        ctx.lineWidth = 4;
                        ctx.stroke();

                        // Compute label position
                        const dimpos = calculateAngleLabelPosition(p0.x, p0.y, p2.x, p2.y, p1.x, p1.y, labelradius);

                        // Compute the angle between lines
                        const angleRad = this.calculateAngleBetweenLines(p0.x, p0.y, p1.x, p1.y, p2.x, p2.y);
                        let angleDeg = angleRad * (180 / Math.PI);
                        if (bmeasureOuter) angleDeg = 360 - angleDeg;

                        const dimtext = angleDeg.toFixed(1) + DegreeLabel;

                        ctx.restore();

                        return {
                            text: dimtext,
                            pos: dimpos
                        };
                    }

                    ctx.restore();

                    return {
                        text: "",           // No label to show yet
                        pos: { x: p1.x, y: p1.y }  // Default to vertex for now
                    };
                    //return null; // Nothing to measure yet
                };

                this.drawangleold = function(ctx, linewidth, strokecolor, scalefactor, dx, dy, useoffset, bmeasureOuter){

                    if(this.points.length == 0){
                        return;
                    }

                    //use bcounterclock for inner and outer angle.
                    //bcounterclock = inner angle false outer.


                    let startAngle = 0.0;
                    let endAngle = 0.0;
                    let normStartAngle = 0.0; 
                    let normEndAngle = 0.0; 

                    var angles = [];
                    
                    var arcx = 0;
                    var arcy = 0;
                    var drawangle = false;
                    let sumAngle = 0.0;
                    var dimtext = "";

                    
                    var counter = 0;
                    var xscaled = (this.points[0].x - this.xoffset) * scalefactor;
                    var yscaled = (this.points[0].y - this.yoffset) * scalefactor;
                    
                    if (useoffset) {
                        xscaled += dx;
                        yscaled += dy;
                    }

                    var prevxscaled = xscaled;
                    var prevyscaled = yscaled;

                    ctx.save();
                    ctx.strokeStyle = strokecolor;

                    if (bUseFixedScale) {
                        ctx.lineWidth = linewidth * markupobject.fixedscaleFactor;
                        var arcradius = nanglearcrad * markupobject.fixedscaleFactor;
                        var labelradius = (nanglearcrad + 35) * markupobject.fixedscaleFactor;

                    }else{
                        ctx.lineWidth = linewidth * scalefactor;
                        arcradius = nanglearcrad * scalefactor;
                        labelradius = (nanglearcrad + 35) * scalefactor;
                    }

                    if(bUseNoScale) {
                        ctx.lineWidth = linewidth;
                        arcradius = nanglearcrad;
                        labelradius = (nanglearcrad + 35);

                    }
                    
                    ctx.beginPath();
                    
                    ctx.moveTo(xscaled, yscaled);

                    //function calculateAngleLabelPosition(x1, y1, x2, y2, cx, cy, r) {
                    
                    let dimlines = {x1 : xscaled, y1 : yscaled, x2 : xscaled, y2 : yscaled, cx : xscaled, cy : yscaled, r : labelradius};

                    for (counter = 1; counter < markupobject.points.length; counter++) {
                        //            ctx.lineTo(markupobject.points[counter].x, markupobject.points[counter].y);

                        xscaled = (this.points[counter].x - this.xoffset) * scalefactor;
                        yscaled = (this.points[counter].y - this.yoffset) * scalefactor;
                        if (useoffset) {
                            xscaled += dx;
                            yscaled += dy;
                        }

                        angles.push(markupobject.getangleofline(prevxscaled, prevyscaled, xscaled, yscaled, false));
                        
                        var line = {x1 : prevxscaled, y1 : prevyscaled, x2 : xscaled, y2 : yscaled};

                        ctx.lineTo(xscaled, yscaled);

                        if(counter == 1){
                            dimlines.cx = xscaled;
                            dimlines.cy = yscaled;
    
                        }else if (counter == 2){
                            dimlines.x2 = xscaled;
                            dimlines.y2 = yscaled;

                        }

                        
                        var linelength = this.getlinelength(line.x1, line.y1, line.x2, line.y2);
                        if(linelength < arcradius){
                            arcradius = linelength;
                        }


                        prevxscaled = xscaled;
                        prevyscaled = yscaled;


    
                    }

                    if(angles.length == 2){

                        startAngle = angles[0].angle;

                        startAngle = startAngle - (Math.PI);

                        endAngle = angles[1].angle;
                        //endAngle = endAngle - Math.PI;
                        
                        arcx = angles[0].pointx;
                        arcy = angles[0].pointy;

                        drawangle = true;

                    }

                    ctx.stroke();

                    let dimpos = calculateAngleLabelPosition(dimlines.x1, dimlines.y1, dimlines.x2, dimlines.y2, dimlines.cx, dimlines.cy, dimlines.r);


                    if (drawangle) {
                        ctx.beginPath();
                        
                        

                        let anticlockwise = false;
                        let angleDeg = (endAngle - startAngle) * (180 / Math.PI);
                        if (angleDeg < 0) angleDeg += 360;

                        // Decide direction based on inner/outer toggle
                        if ((angleDeg > 180 && !bmeasureOuter) || (angleDeg <= 180 && bmeasureOuter)) {
                            anticlockwise = true;
                        }

                        ctx.arc(arcx, arcy, arcradius, startAngle, endAngle, anticlockwise);
                        ctx.strokeStyle = "blue";
                        ctx.lineWidth = 4;
                        ctx.stroke();

                        // Normalize for angle calculation
                        normStartAngle = (startAngle < 0) ? startAngle + 2 * Math.PI : startAngle;
                        normEndAngle = (endAngle < 0) ? endAngle + 2 * Math.PI : endAngle;

                        let startAngledeg = normStartAngle * (180 / Math.PI);
                        let endAngledeg = normEndAngle * (180 / Math.PI);
                        sumAngle = endAngledeg - startAngledeg;

                        if (sumAngle < 0) sumAngle += 360;
                        if (!bmeasureOuter && sumAngle > 180) sumAngle = 360 - sumAngle;

                        dimtext = sumAngle.toFixed(1) + DegreeLabel;



                        
                        

                    }

                    ctx.restore();

                    let dimobject = {text : dimtext, pos : dimpos};

                    return dimobject;
                    

                };

                this.polycurves = function (ctx, linewidth, strokecolor, scalefactor) {
                    var counter = 0;
                    var control = new point();
                    var start = new point();
                    var xscaled = (this.points[0].x - this.xoffset) * scalefactor;
                    var yscaled = (this.points[0].y - this.yoffset) * scalefactor;
                    if (DocObj.getCurrentpage().usevectorxml) {
                        xscaled += DocObj.getCurrentpage().dxvector;
                        yscaled += DocObj.getCurrentpage().dyvector;
                    }
                    else if (DocObj.getCurrentpage().usepdfjs) {
                        xscaled += DocObj.getCurrentpage().dxpdf;
                        yscaled += DocObj.getCurrentpage().dypdf;
                    }
                    else {
                        xscaled = xscaled + DocObj.getCurrentpage().dx;
                        yscaled = yscaled + DocObj.getCurrentpage().dy;
                    }
                    ctx.save();
                    ctx.strokeStyle = strokecolor;
                    ctx.lineWidth = linewidth * scalefactor;
                    ctx.beginPath();
                    //      ctx.moveTo(markupobject.points[0].x, markupobject.points[0].y);
                    ctx.moveTo(xscaled, yscaled);
                    start.x = xscaled;
                    start.y = yscaled;
                    for (counter = 1; counter < markupobject.points.length; counter++) {
                        xscaled = (this.points[counter].x - this.xoffset) * scalefactor;
                        yscaled = (this.points[counter].y - this.yoffset) * scalefactor;
                        if (DocObj.getCurrentpage().usevectorxml) {
                            xscaled += DocObj.getCurrentpage().dxvector;
                            yscaled += DocObj.getCurrentpage().dyvector;
                        }
                        else if (DocObj.getCurrentpage().usepdfjs) {
                            xscaled += DocObj.getCurrentpage().dxpdf;
                            yscaled += DocObj.getCurrentpage().dypdf;
                        }
                        else {
                            xscaled = xscaled + DocObj.getCurrentpage().dx;
                            yscaled = yscaled + DocObj.getCurrentpage().dy;
                        }
                        if (isEven(counter)) {
                            ctx.moveTo(start.x, start.y);
                            ctx.quadraticCurveTo(control.x, control.y, xscaled, yscaled);
                            start.x = xscaled;
                            start.y = yscaled;
                        }
                        else {
                            //dont draw if converted to curve.
                            if (counter + 1 == markupobject.points.length) {
                                ctx.lineTo(xscaled, yscaled);
                            }
                            control.x = xscaled;
                            control.y = yscaled;
                        }
                        //ctx.lineTo(xscaled, yscaled);
                    }
                    //ctx.closePath();
                    ctx.stroke();
                    ctx.restore();
                };
                this.dimensionLeader = function (ctx, x, y, width, height, offset, arrowl, arrowa, linewidth, type, fillcolor, strokecolor) {
                    //bounding box
                    var arrwidth = width - x;
                    var arrheight = height - y;
                    var baranglerad = 90 / (180 / Math.PI);
                    var arrowanglerad = arrowa / (180 / Math.PI);
                    //angle of diagonal in radians
                    var arrdiagrad = Math.atan2(arrheight, arrwidth);
                    //arrowhead length
                    var arrowhead = Math.abs(arrowl / Math.cos(arrowanglerad));
                    //bar length
                    var barhead = arrowl / 2;
                    //negative angle of diagonal
                    var arrangleradneg = Math.PI + arrdiagrad;
                    //angle of end barb lines relative to line
                    var aarrowupperangle = arrangleradneg + arrowanglerad;
                    var aarrowlowerangle = arrangleradneg - arrowanglerad;
                    //angle of end bar lines relative to line
                    var abarupperangle = arrangleradneg + baranglerad;
                    var abarlowerangle = arrangleradneg - baranglerad;
                    //calculated points of the line end barbs
                    var arrtopx = width + Math.cos(aarrowupperangle) * arrowhead;
                    var arrtopy = height + Math.sin(aarrowupperangle) * arrowhead;
                    var arrbotx = width + Math.cos(aarrowlowerangle) * arrowhead;
                    var arrboty = height + Math.sin(aarrowlowerangle) * arrowhead;
                    //calculated points of the line end bar
                    var bartopx = width + Math.cos(abarupperangle) * barhead;
                    var bartopy = height + Math.sin(abarupperangle) * barhead;
                    var barbotx = width + Math.cos(abarlowerangle) * barhead;
                    var barboty = height + Math.sin(abarlowerangle) * barhead;
                    //new end points for main line with leader line offsets.
                    var leaderoffsetendx = width + Math.cos(abarlowerangle) * offset;
                    var leaderoffsetendy = height + Math.sin(abarlowerangle) * offset;
                    var startx = x + Math.cos(arrdiagrad) * barhead;
                    var starty = y + Math.sin(arrdiagrad) * barhead;
                    var endx = width - Math.cos(arrdiagrad) * barhead;
                    var endy = height - Math.sin(arrdiagrad) * barhead;
                    //angle of start barb lines relative to line
                    var asarrowupperangle = arrdiagrad + arrowanglerad;
                    var asarrowlowerangle = arrdiagrad - arrowanglerad;
                    //angle of start bar lines relative to line
                    var asbarupperangle = arrdiagrad + baranglerad;
                    var asbarlowerangle = arrdiagrad - baranglerad;
                    //calculated points of the line start barbs
                    var arrstopx = x + Math.cos(asarrowupperangle) * arrowhead;
                    var arrstopy = y + Math.sin(asarrowupperangle) * arrowhead;
                    var arrsbotx = x + Math.cos(asarrowlowerangle) * arrowhead;
                    var arrsboty = y + Math.sin(asarrowlowerangle) * arrowhead;
                    //calculated points of the line start bar
                    var barstopx = x + Math.cos(asbarupperangle) * barhead;
                    var barstopy = y + Math.sin(asbarupperangle) * barhead;
                    var barsbotx = x + Math.cos(asbarlowerangle) * barhead;
                    var barsboty = y + Math.sin(asbarlowerangle) * barhead;
                    //new position for main line start and stop when having leader line offsets.
                    var leaderoffsetx = x + Math.cos(asbarupperangle) * offset;
                    var leaderoffsety = y + Math.sin(asbarupperangle) * offset;
                    //calculated points of the line end barbs for offset
                    var arrtopxoffset = leaderoffsetendx + Math.cos(aarrowupperangle) * arrowhead;
                    var arrtopyoffset = leaderoffsetendy + Math.sin(aarrowupperangle) * arrowhead;
                    var arrbotxoffset = leaderoffsetendx + Math.cos(aarrowlowerangle) * arrowhead;
                    var arrbotyoffset = leaderoffsetendy + Math.sin(aarrowlowerangle) * arrowhead;
                    //calculated points of the line start barbs with offset
                    var arrstopxoffset = leaderoffsetx + Math.cos(asarrowupperangle) * arrowhead;
                    var arrstopyoffset = leaderoffsety + Math.sin(asarrowupperangle) * arrowhead;
                    var arrsbotxoffset = leaderoffsetx + Math.cos(asarrowlowerangle) * arrowhead;
                    var arrsbotyoffset = leaderoffsety + Math.sin(asarrowlowerangle) * arrowhead;
                    //var offsetdims = {x:leaderoffsetx, y: leaderoffsety, w : leaderoffsetendx, h: leaderoffsetendy};
                    //var leaderpoint = {x: }
                    ctx.save(); // save the context so we don't mess up others
                    ctx.lineWidth = linewidth;
                    ctx.strokeStyle = strokecolor;
                    ctx.fillStyle = fillcolor;
                    if (this.subtype == 9) {
                        ctx.lineCap = "butt";
                    }
                    else {
                        ctx.lineCap = 'round';
                    }
                    ctx.beginPath();
                    if (type == 1 || type == 3) {
                        if (type == 1) {
                            ctx.moveTo(x, y);
                        }
                        else if (type == 3) {
                            ctx.moveTo(startx, starty);
                        }
                        ctx.lineTo(endx, endy);
                    }
                    else {
                        if (offset == 0) {
                            ctx.moveTo(x, y);
                            ctx.lineTo(width, height);
                        }
                        else {
                            ctx.moveTo(leaderoffsetx, leaderoffsety);
                            ctx.lineTo(leaderoffsetendx, leaderoffsetendy);
                        }
                    }
                    //ctx.closePath();
                    ctx.stroke();
                    if (type == 0) {
                        ctx.beginPath();
                        ctx.moveTo(width, height);
                        ctx.lineTo(arrtopx, arrtopy);
                        //ctx.closePath();
                        ctx.stroke();
                        ctx.beginPath();
                        ctx.moveTo(width, height);
                        ctx.lineTo(arrbotx, arrboty);
                        //ctx.closePath();
                        ctx.stroke();
                    }
                    if (type == 1) {
                        //ctx.lineWidth = 1;
                        ctx.beginPath();
                        ctx.moveTo(width, height);
                        ctx.lineTo(arrtopx, arrtopy);
                        ctx.lineTo(arrbotx, arrboty);
                        ctx.closePath();
                        //ctx.stroke();
                        ctx.fill();
                    }
                    if (type == 2) {
                        ctx.beginPath();
                        ctx.moveTo(x, y);
                        ctx.lineTo(arrstopx, arrstopy);
                        //ctx.closePath();
                        ctx.stroke();
                        ctx.beginPath();
                        ctx.moveTo(x, y);
                        ctx.lineTo(arrsbotx, arrsboty);
                        //ctx.closePath();
                        ctx.stroke();
                        ctx.beginPath();
                        ctx.moveTo(width, height);
                        ctx.lineTo(arrtopx, arrtopy);
                        //ctx.closePath();
                        ctx.stroke();
                        ctx.beginPath();
                        ctx.moveTo(width, height);
                        ctx.lineTo(arrbotx, arrboty);
                        //ctx.closePath();
                        ctx.stroke();
                    }
                    if (type == 3) {
                        //ctx.lineWidth = 1;
                        ctx.beginPath();
                        ctx.moveTo(x, y);
                        ctx.lineTo(arrstopx, arrstopy);
                        ctx.lineTo(arrsbotx, arrsboty);
                        ctx.closePath();
                        ctx.fill();
                        ctx.lineCap = 'round';
                        //ctx.stroke();
                        ctx.beginPath();
                        ctx.moveTo(width, height);
                        ctx.lineTo(arrtopx, arrtopy);
                        ctx.lineTo(arrbotx, arrboty);
                        ctx.closePath();
                        ctx.fill();
                        ctx.lineCap = 'round';
                        //ctx.stroke();
                    }
                    //bars both ends
                    if (type == 4) {
                        ctx.beginPath();
                        ctx.moveTo(x, y);
                        if (offset == 0) {
                            ctx.lineTo(barstopx, barstopy);
                        }
                        else {
                            ctx.lineTo(leaderoffsetx, leaderoffsety);
                        }
                        //ctx.closePath();
                        ctx.stroke();
                        if (offset == 0) {
                            ctx.beginPath();
                            ctx.moveTo(x, y);
                            ctx.lineTo(barsbotx, barsboty);
                            //ctx.closePath();
                            ctx.stroke();
                            ctx.beginPath();
                            ctx.moveTo(width, height);
                            ctx.lineTo(bartopx, bartopy);
                            //ctx.closePath();
                            ctx.stroke();
                        }
                        ctx.beginPath();
                        ctx.moveTo(width, height);
                        if (offset == 0) {
                            ctx.lineTo(barbotx, barboty);
                        }
                        else {
                            ctx.lineTo(leaderoffsetendx, leaderoffsetendy);
                        }
                        //ctx.closePath();
                        ctx.stroke();
                    }
                    //bars and open arrows
                    if (type == 5) {
                        if (offset == 0) {
                            var awstrtx = x;
                            var awstrty = y;
                            var awendx = width;
                            var awendy = height;
                            var arwstrttopx = arrstopx;
                            var arwstrttopy = arrstopy;
                            var arwstrtbotx = arrsbotx;
                            var arwstrtboty = arrsboty;
                            var arwendtopx = arrtopx;
                            var arwendtopy = arrtopy;
                            var arwendbotx = arrbotx;
                            var arwendboty = arrboty;
                        }
                        else {
                            awstrtx = leaderoffsetx;
                            awstrty = leaderoffsety;
                            awendx = leaderoffsetendx;
                            awendy = leaderoffsetendy;
                            arwstrttopx = arrstopxoffset;
                            arwstrttopy = arrstopyoffset;
                            arwstrtbotx = arrsbotxoffset;
                            arwstrtboty = arrsbotyoffset;
                            arwendtopx = arrtopxoffset;
                            arwendtopy = arrtopyoffset;
                            arwendbotx = arrbotxoffset;
                            arwendboty = arrbotyoffset;
                        }
                        ctx.beginPath();
                        ctx.moveTo(awstrtx, awstrty);
                        ctx.lineTo(arwstrttopx, arwstrttopy);
                        //ctx.closePath();
                        ctx.stroke();
                        ctx.beginPath();
                        ctx.moveTo(awstrtx, awstrty);
                        ctx.lineTo(arwstrtbotx, arwstrtboty);
                        //ctx.closePath();
                        ctx.stroke();
                        ctx.beginPath();
                        ctx.moveTo(awendx, awendy);
                        ctx.lineTo(arwendtopx, arwendtopy);
                        //ctx.closePath();
                        ctx.stroke();
                        ctx.beginPath();
                        ctx.moveTo(awendx, awendy);
                        ctx.lineTo(arwendbotx, arwendboty);
                        //ctx.closePath();
                        ctx.stroke();
                        ctx.beginPath();
                        ctx.moveTo(x, y);
                        if (offset == 0) {
                            ctx.lineTo(barstopx, barstopy);
                        }
                        else {
                            ctx.lineTo(leaderoffsetx, leaderoffsety);
                        }
                        //ctx.closePath();
                        ctx.stroke();
                        if (offset == 0) {
                            ctx.beginPath();
                            ctx.moveTo(x, y);
                            ctx.lineTo(barsbotx, barsboty);
                            //ctx.closePath();
                            ctx.stroke();
                            ctx.beginPath();
                            ctx.moveTo(width, height);
                            ctx.lineTo(bartopx, bartopy);
                            //ctx.closePath();
                            ctx.stroke();
                        }
                        ctx.beginPath();
                        ctx.moveTo(width, height);
                        if (offset == 0) {
                            ctx.lineTo(barbotx, barboty);
                        }
                        else {
                            ctx.lineTo(leaderoffsetendx, leaderoffsetendy);
                        }
                        //ctx.closePath();
                        ctx.stroke();
                    }
                    /*if (type == 9) {
                        ctx.beginPath();
                        ctx.moveTo(x, y);
                        ctx.lineTo(width, height);
                        ctx.stroke();
                    }*/
                    ctx.restore(); // restore context to what it was on entry
                    var offsetdims = { x: leaderoffsetx, y: leaderoffsety, w: leaderoffsetendx, h: leaderoffsetendy };
                    return offsetdims;
                };
                this.barhead = function (ctx, x, y, width, height, lineEnds, arrowl, linewidth, strokecolor) {
                    var arrwidth = width - x;
                    var arrheight = height - y;
                    var baranglerad = 90 / (180 / Math.PI);
                    //angle of diagonal in radians
                    var arrdiagrad = Math.atan2(arrheight, arrwidth);
                    //bar length
                    var barhead = arrowl / 2;
                    //negative angle of diagonal
                    var arrangleradneg = Math.PI + arrdiagrad;
                    //angle of end bar lines relative to line
                    var abarupperangle = arrangleradneg + baranglerad;
                    var abarlowerangle = arrangleradneg - baranglerad;
                    //calculated points of the line end bar
                    var bartopx = width + Math.cos(abarupperangle) * barhead;
                    var bartopy = height + Math.sin(abarupperangle) * barhead;
                    var barbotx = width + Math.cos(abarlowerangle) * barhead;
                    var barboty = height + Math.sin(abarlowerangle) * barhead;
                    var startx = x + Math.cos(arrdiagrad) * barhead;
                    var starty = y + Math.sin(arrdiagrad) * barhead;
                    var endx = width - Math.cos(arrdiagrad) * barhead;
                    var endy = height - Math.sin(arrdiagrad) * barhead;
                    //angle of start bar lines relative to line
                    var asbarupperangle = arrdiagrad + baranglerad;
                    var asbarlowerangle = arrdiagrad - baranglerad;
                    //calculated points of the line start bar
                    var barstopx = x + Math.cos(asbarupperangle) * barhead;
                    var barstopy = y + Math.sin(asbarupperangle) * barhead;
                    var barsbotx = x + Math.cos(asbarlowerangle) * barhead;
                    var barsboty = y + Math.sin(asbarlowerangle) * barhead;
                    ctx.save(); // save the context so we don't mess up others
                    ctx.lineCap = 'round';
                    ctx.lineWidth = linewidth;
                    ctx.strokeStyle = strokecolor;
                    if (lineEnds.start) {
                        ctx.beginPath();
                        ctx.moveTo(x, y);
                        ctx.lineTo(barstopx, barstopy);
                        //ctx.closePath();
                        ctx.stroke();
                        ctx.beginPath();
                        ctx.moveTo(x, y);
                        ctx.lineTo(barsbotx, barsboty);
                        //ctx.closePath();
                        ctx.stroke();
                    }
                    if (lineEnds.end) {
                        ctx.beginPath();
                        ctx.moveTo(width, height);
                        ctx.lineTo(bartopx, bartopy);
                        //ctx.closePath();
                        ctx.stroke();
                        ctx.beginPath();
                        ctx.moveTo(width, height);
                        ctx.lineTo(barbotx, barboty);
                        //ctx.closePath();
                        ctx.stroke();
                    }
                    ctx.restore(); // restore context to what it was on entry
                };
                this.getPadding = function(mousePos){

                    //biscallout = (markupobject.type == 6 && markupobject.subtype == 6);


                    if(markupobject.padding){

                        if (mousePos.x > markupobject.x){
                            //add to mousepos.x
                            mousePos.xpadding = mousePos.x + markupobject.padding.w;
                        }else if (mousePos.x < markupobject.x){
                            mousePos.xpadding = mousePos.x - markupobject.padding.w;
                        }

                        if (mousePos.y > markupobject.y){
                            mousePos.ypadding = mousePos.y + markupobject.padding.h;
                        }else if(mousePos.y < markupobject.y){
                            mousePos.ypadding = mousePos.y - markupobject.padding.h;
                        }
                        
                        mousePos.busepadding = true;

                    }
                    /*markupobject.padding = {
                        maxx : box.x + box.w,
                        maxy : box.y + box.h,
                        minx : box.x,
                        miny : box.y,
                        w : box.w,
                        h : box.h
                    };*/

                    return mousePos;
                };

                this.calloutbox = function(ctx, box, w, h, linewidth, strokecolor){
                    
                    ctx.save(); // save the context so we don't mess up others
                    ctx.lineWidth = linewidth;
                    ctx.strokeStyle = strokecolor;


                    if (markupobject.pagerotation != 0 && markupobject.pagerotation != 180) {
                        var RotRad = markupobject.pagerotation * (Math.PI / 180);
                        //need to be in radians
                        //need to rotate around w and h of markupobject.
                        //var tx = box.x + (box.w / 2);
                        //var ty = box.y + (box.h / 2);

                        var tx = w;
                        var ty = h;

                        ctx.translate(tx, ty);
                        ctx.rotate(RotRad);
                        ctx.translate(-tx, -ty);
                    }

                    ctx.beginPath();
                    ctx.moveTo(box.x, box.y);
                    ctx.lineTo(box.x + box.w, box.y);
                    ctx.lineTo(box.x + box.w, box.y + box.h);
                    ctx.lineTo(box.x, box.y + box.h);
                    

                    ctx.closePath();
                    ctx.stroke();

                    ctx.restore(); // restore context to what it was on entry

                };
                this.arrowhead = function (ctx, x, y, width, height, lineEnds, arrowa, arrowl, linewidth, strokecolor, bopen) {
                    
                    var arrwidth = width - x;
                    var arrheight = height - y;
                    var arrowanglerad = arrowa / (180 / Math.PI);
                    //angle of diagonal in radians
                    var arrdiagrad = Math.atan2(arrheight, arrwidth);
                    //arrowhead length
                    var arrowhead = Math.abs(arrowl / Math.cos(arrowanglerad));
                    //negative angle of diagonal
                    var arrangleradneg = Math.PI + arrdiagrad;
                    //angle of end barb lines relative to line
                    var aarrowupperangle = arrangleradneg + arrowanglerad;
                    var aarrowlowerangle = arrangleradneg - arrowanglerad;
                    //calculated points of the line end barbs
                    var arrtopx = width + Math.cos(aarrowupperangle) * arrowhead;
                    var arrtopy = height + Math.sin(aarrowupperangle) * arrowhead;
                    var arrbotx = width + Math.cos(aarrowlowerangle) * arrowhead;
                    var arrboty = height + Math.sin(aarrowlowerangle) * arrowhead;
                    //angle of start barb lines relative to line
                    var asarrowupperangle = arrdiagrad + arrowanglerad;
                    var asarrowlowerangle = arrdiagrad - arrowanglerad;
                    //calculated points of the line start barbs
                    var arrstopx = x + Math.cos(asarrowupperangle) * arrowhead;
                    var arrstopy = y + Math.sin(asarrowupperangle) * arrowhead;
                    var arrsbotx = x + Math.cos(asarrowlowerangle) * arrowhead;
                    var arrsboty = y + Math.sin(asarrowlowerangle) * arrowhead;
                    //if blinestart arrow on start of line else on end of line.
                    ctx.save(); // save the context so we don't mess up others
                    ctx.lineCap = 'round';
                    ctx.lineWidth = linewidth;
                    ctx.strokeStyle = strokecolor;
                    if (lineEnds.start) {
                        if (bopen) {
                            ctx.beginPath();
                            ctx.moveTo(x, y);
                            ctx.lineTo(arrstopx, arrstopy);
                            ctx.closePath();
                            ctx.stroke();
                            ctx.beginPath();
                            ctx.moveTo(x, y);
                            ctx.lineTo(arrsbotx, arrsboty);
                            ctx.closePath();
                            ctx.stroke();
                        }
                        else {
                            ctx.beginPath();
                            ctx.moveTo(x, y);
                            ctx.lineTo(arrstopx, arrstopy);
                            ctx.lineTo(arrsbotx, arrsboty);
                            ctx.closePath();
                            ctx.fill();
                            ctx.lineCap = 'round';
                        }
                    }
                    if (lineEnds.end) {
                        if (bopen) {
                            ctx.beginPath();
                            ctx.moveTo(width, height);
                            ctx.lineTo(arrtopx, arrtopy);
                            ctx.closePath();
                            ctx.stroke();
                            ctx.beginPath();
                            ctx.moveTo(width, height);
                            ctx.lineTo(arrbotx, arrboty);
                            ctx.closePath();
                            ctx.stroke();
                        }
                        else {
                            ctx.beginPath();
                            ctx.moveTo(width, height);
                            ctx.lineTo(arrtopx, arrtopy);
                            ctx.lineTo(arrbotx, arrboty);
                            ctx.closePath();
                            //ctx.stroke();
                            ctx.fill();
                        }
                    }
                    ctx.restore(); // restore context to what it was on entry
                };
                this.getdrawBox = function(mousepos){

                    //var fntheight = markupobject.font.getScaledHeight();

                    var temptxtscale = markupobject.fixedscaleFactor;
                    //temptxtscale *= fTextScale;
                    //textbh = (defaultFont.height / 0.75);

                    var fntheight = defaultFont.height * temptxtscale;



                    if (!mousepos){
                        mousepos = {x : markupobject.w, y : markupobject.h};

                    }
                    
                    return getCalloutBox(mousepos.x,mousepos.y, temptxtscale, markupobject.arrowTextPos);

                };
                function getbendpoint(reppoint){


                    if(!markupobject.saved){
                        switch (markupobject.arrowTextPos){
                            case TextArrowPosLeft : 

                                switch (markupobject.pagerotation){
                                    case 90 : 
                                        reppoint.repwidth = reppoint.width;
                                        reppoint.repheight = reppoint.height - reppoint.arrowl;
                                        break;
                                    case 270 : 
                                        reppoint.repwidth = reppoint.width;
                                        reppoint.repheight = reppoint.height + reppoint.arrowl;
                                        break;
                                    default:
                                        reppoint.repwidth = reppoint.width - reppoint.arrowl;
                                        reppoint.repheight = reppoint.height;
                                        break;  

                                }

                                break;
                            case TextArrowPosRight : 

                                switch (markupobject.pagerotation){
                                    case 90 : 
                                        reppoint.repwidth = reppoint.width;
                                        reppoint.repheight = reppoint.height + reppoint.arrowl;

                                        break;
                                    case 270 : 
                                        reppoint.repwidth = reppoint.width;
                                        reppoint.repheight = reppoint.height - reppoint.arrowl;
                                        break;
                                    default:
                                        reppoint.repwidth = reppoint.width + reppoint.arrowl;
                                        reppoint.repheight = reppoint.height;
                                        break;  




                                }


                                break;
                            case TextArrowPosTop :
                                //repheight = height + arrowl;    

                                switch (markupobject.pagerotation){
                                    case 90 : 
                                        //repwidth = width - arrowl;
                                        //repheight = height;

                                        reppoint.repwidth = reppoint.width - reppoint.arrowl;
                                        reppoint.repheight = reppoint.height;

                                        break;
                                    case 270 : 
                                        //repwidth = width + arrowl;
                                        //repheight = height;

                                        reppoint.repwidth = reppoint.width + reppoint.arrowl;
                                        reppoint.repheight = reppoint.height;
                                        break;
                                    default:
                                        reppoint.repheight = reppoint.height + reppoint.arrowl;
                                        reppoint.repwidth = reppoint.width;
                                        break;  

                                }
    
                                break;
                            case TextArrowPosBottom :
                                //repheight = height - arrowl;
                                switch (markupobject.pagerotation){
                                    case 90 : 
                                        //repwidth = width + arrowl;
                                        //repheight = height;
                                        reppoint.repwidth = reppoint.width + reppoint.arrowl;
                                        reppoint.repheight = reppoint.height;
                                        break;
                                    case 270 : 
                                        //repwidth = width - arrowl;
                                        //repheight = height;
                                        reppoint.repwidth = reppoint.width - reppoint.arrowl;
                                        reppoint.repheight = reppoint.height;
                                        break;
                                    default:
                                        reppoint.repheight = reppoint.height - reppoint.arrowl;
                                        reppoint.repwidth = reppoint.width;
                                        break;  

                                }



                                break;    

    
                        }
                    }else{
                        switch (markupobject.arrowTextPos){
                            case TextArrowPosLeft : 
                                reppoint.repwidth = reppoint.width - reppoint.arrowl;
                                reppoint.repheight = reppoint.height;
                                break;
                            case TextArrowPosRight : 
                                reppoint.repwidth = reppoint.width + reppoint.arrowl;
                                reppoint.repheight = reppoint.height;
                                break;
                            case TextArrowPosTop :
                                //repheight = height + arrowl;    
                                reppoint.repheight = reppoint.height + reppoint.arrowl;
                                reppoint.repwidth = reppoint.width;
    
                                break;
                            case TextArrowPosBottom :
                                //repheight = height - arrowl;
                                reppoint.repheight = reppoint.height - reppoint.arrowl;
                                reppoint.repwidth = reppoint.width;

                                break;    
    
                        }    
                    }

                    return reppoint;


                }
                
                this.arrow = function (ctx, x, y, width, height, arrowl, arrowa, linewidth, type, fillcolor, strokecolor) {
                    //bounding box
                    
                    var repheight = height;
                    var repwidth = width;

                    var reppoint = {
                        repheight : height,
                        repwidth : width,
                        width : width,
                        height : height,
                        arrowl : arrowl
                    };


                    var arrwidth = width - x;
                    var arrheight = height - y;
                    var arrdiagrad = Math.atan2(arrheight, arrwidth);
                    var barhead = arrowl / 2;
                    var startx = x + Math.cos(arrdiagrad) * barhead;
                    var starty = y + Math.sin(arrdiagrad) * barhead;
                    var endx = width - Math.cos(arrdiagrad) * barhead;
                    var endy = height - Math.sin(arrdiagrad) * barhead;
                    var lineEnds = { start: false, end: false };
                    ctx.save(); // save the context so we don't mess up others
                    ctx.lineWidth = linewidth;
                    ctx.strokeStyle = strokecolor;
                    ctx.fillStyle = fillcolor;
                    if (this.subtype == 9) {
                        ctx.lineCap = "butt";
                    }
                    else {
                        ctx.lineCap = 'round';
                    }
                    ctx.beginPath();
                    if (type == 1 || type == 3) {
                        if (type == 1) {
                            ctx.moveTo(x, y);
                        }
                        else if (type == 3) {
                            ctx.moveTo(startx, starty);
                        }
                        ctx.lineTo(endx, endy);
                    }
                    else {

                        if(type == 6){

                            var temptxtscale = markupobject.fixedscaleFactor;
                            temptxtscale *= fTextScale;

                            var fntheight = defaultFont.height * temptxtscale;

                            var box = getCalloutBox(width,height, temptxtscale, markupobject.arrowTextPos);


                            ctx.moveTo(x, y);

                            reppoint = getbendpoint(reppoint);

                            ctx.lineTo(reppoint.repwidth, reppoint.repheight);
                            ctx.lineTo(width, height);

                       
                        }else{
                            ctx.moveTo(x, y);
                            ctx.lineTo(width, height);
                        }

                        //arrowl
                    }
                    //ctx.closePath();
                    ctx.stroke();
                    if (type == 0) {
                        lineEnds.end = true;
                        lineEnds.start = false;
                        markupobject.arrowhead(ctx, x, y, width, height, lineEnds, arrowa, arrowl, linewidth, strokecolor, true);
                    }
                    if (type == 1) {
                        lineEnds.end = true;
                        lineEnds.start = false;
                        markupobject.arrowhead(ctx, x, y, width, height, lineEnds, arrowa, arrowl, linewidth, strokecolor, false);
                    }
                    if (type == 2) {
                        lineEnds.end = true;
                        lineEnds.start = true;
                        markupobject.arrowhead(ctx, x, y, width, height, lineEnds, arrowa, arrowl, linewidth, strokecolor, true);
                    }
                    if (type == 3) {
                        lineEnds.end = true;
                        lineEnds.start = true;
                        markupobject.arrowhead(ctx, x, y, width, height, lineEnds, arrowa, arrowl, linewidth, strokecolor, false);
                    }
                    //bars both ends
                    if (type == 4) {
                        lineEnds.end = true;
                        lineEnds.start = true;
                        markupobject.barhead(ctx, x, y, width, height, lineEnds, arrowl, linewidth, strokecolor);
                    }
                    //bars and open arrows
                    if (type == 5) {
                        lineEnds.end = true;
                        lineEnds.start = true;
                        markupobject.arrowhead(ctx, x, y, width, height, lineEnds, arrowa, arrowl, linewidth, strokecolor, true);
                        lineEnds.end = true;
                        lineEnds.start = true;
                        markupobject.barhead(ctx, x, y, width, height, lineEnds, arrowl, linewidth, strokecolor);
                    }
                    //callout arrow
                    if (type == 6) {
                        //console.log("text rect arrow");
                        lineEnds.end = false;
                        lineEnds.start = true;
                        markupobject.arrowhead(ctx, x, y, reppoint.repwidth, reppoint.repheight, lineEnds, arrowa, arrowl, linewidth, strokecolor, true);
                        if(!markupobject.saved){
                            markupobject.padding = {
                                maxx : box.x + box.w,
                                maxy : box.y + box.h,
                                minx : box.x,
                                miny : box.y,
                                w : box.w,
                                h : box.h
                            };
                            markupobject.calloutbox(ctx, box, width, height, linewidth, strokecolor);

                        }else{
                            markupobject.padding = undefined;
                        }
                    }
                    /*if (type == 9) {
                        ctx.beginPath();
                        ctx.moveTo(x, y);
                        ctx.lineTo(width, height);
                        ctx.stroke();
                    }*/
                    ctx.restore(); // restore context to what it was on entry
                };
                this.setdimvaluearea = function(){
                    markupobject.dimarea = 0.0;
                    var holearea = 0;
                    var h = 0;

                    var dimtext = "";


                    //var AreaUnitlabel = DocObj.getCurrentpage().AreaUnitlabel;

                    var AreaUnitlabel = DocObj.getCurrentpage().scaleObject.AreaUnitlabel;
                    var dimPrecision = DocObj.getCurrentpage().scaleObject.ndimprecision;
                    if(markupobject.hasScale) {
                        AreaUnitlabel = markupobject.scaleObject.AreaUnitlabel;
                        dimPrecision = markupobject.scaleObject.ndimprecision;
                        markupForScale = markupobject; 
                    }


                    //var AreaUnitlabel = DocObj.getCurrentpage().scaleObject.AreaUnitlabel;
                    
                    /*if(markupobject.hasScale) {
                        AreaUnitlabel = markupobject.scaleObject.AreaUnitlabel;
                        markupForScale = markupobject; 
                    }*/

                    var markupscalesq = this.scaling * this.scaling;

                    if(this.type == 3){
                        //dimarea = markupobject.PolygonArea();
                        markupobject.dimarea = markupobject.SquareArea();
                        dimtext = getUnitArea(markupobject.dimarea / markupscalesq);
                        //dimtext = dimtext.toFixed(ndimprecision);
                        dimtext = dimtext.toFixed(dimPrecision);
                        dimtext = dimtext + " " + AreaUnitlabel;
                    }else{
                        if(this.points.length == 0){
                            dimtext = "";
                        }else{
                            markupobject.dimarea = markupobject.PolygonArea();

                            if(markupobject.holes.length > 0){

                                for(h = 0; h < markupobject.holes.length; h++){
        
                                    holearea = markupobject.PolygonHoleArea(markupobject.holes[h]);

                                    markupobject.holes[h].dimarea = holearea;

                                    markupobject.dimarea -= holearea;

                                    markupobject.holes[h].dimarea = getUnitArea(markupobject.holes[h].dimarea / markupscalesq);
        
                                }
        
                            
                            }
        

                            //dimtext = getUnitArea(markupobject.dimarea / markupscalesq);

                            markupobject.dimarea = getUnitArea(markupobject.dimarea / markupscalesq);

                            //dimtext = markupobject.dimarea.toFixed(ndimprecision);
                            dimtext = markupobject.dimarea.toFixed(dimPrecision);
                            dimtext = dimtext + " " + AreaUnitlabel;
                        }

                        
    
                    }

                    markupForScale = null;


                    return dimtext;

                };
                this.setdimvalueperimeter = function () {
                    var dimtext = "";
                    var blocalusefraction = false;

                    //var Unitofmeasure = DocObj.getCurrentpage().Unitofmeasure;
                    //var Unitlabel = DocObj.getCurrentpage().Unitlabel;


                    var Unitofmeasure = DocObj.getCurrentpage().scaleObject.Unitofmeasure;
                    var Unitlabel = DocObj.getCurrentpage().scaleObject.Unitlabel; 
                    var dimPrecision = DocObj.getCurrentpage().scaleObject.ndimprecision;

                    if(markupobject.hasScale) {
                        Unitofmeasure = markupobject.scaleObject.Unitofmeasure;
                        Unitlabel = markupobject.scaleObject.Unitlabel;
                        dimPrecision = markupobject.scaleObject.ndimprecision;
                        markupForScale = markupobject;
                    }


                    
                    var dimdiag = 0;
                    for (var i = 0; i < markupobject.points.length; i++) {
                        if (i > 0) {
                            var dimwidth = markupobject.points[i].x - markupobject.points[i - 1].x;
                            var dimheight = markupobject.points[i].y - markupobject.points[i - 1].y;
                            dimdiag += markupobject.getdiag(dimwidth, dimheight);
                            //markupobject.dimtext = getUnitlength(dimdiag / markupobject.scaling);
                            //markupobject.dimtext = markupobject.dimtext.toFixed(2);
                            //markupobject.dimtext = markupobject.dimtext + " " + Unitlabel;
                        }
                    }

                    dimwidth = markupobject.points[0].x - markupobject.points[markupobject.points.length - 1].x;
                    dimheight = markupobject.points[0].y - markupobject.points[markupobject.points.length - 1].y;
                    dimdiag += markupobject.getdiag(dimwidth, dimheight);

                    
                    

                    if (Unitofmeasure == 2) {
                        if (bUseFraction) {
                            blocalusefraction = true;
                        }
                        var szdimtext = markupobject.setfractionvalue(dimdiag);
                    }


                    dimtext = getUnitlength(dimdiag / markupobject.scaling);
                    //dimtext = dimtext.toFixed(ndimprecision);
                    dimtext = dimtext.toFixed(dimPrecision);
                    dimtext = dimtext + " " + Unitlabel;

                    markupForScale = null;

                    if (blocalusefraction) {
                        dimtext = szdimtext;
                    }

                    return dimtext;

                };                

                this.setdimvaluepolySegments = function (){
                    var dimdiag = 0;
                    var dimtext = "";
                    var blocalusefraction = false;

                    var dimdiags = [];
                    var dimtexts = [];

                    //var Unitofmeasure = DocObj.getCurrentpage().Unitofmeasure;
                    //var Unitlabel = DocObj.getCurrentpage().Unitlabel;

                    var Unitofmeasure = DocObj.getCurrentpage().scaleObject.Unitofmeasure;
                    var Unitlabel = DocObj.getCurrentpage().scaleObject.Unitlabel;


                    var counter = 0;
                    var lcounter = 0;
                    var dimi = 0;

                    for (var i = 0; i < markupobject.points.length; i++) {
                        if (i > 0) {
                            var dimwidth = markupobject.points[i].x - markupobject.points[i - 1].x;
                            var dimheight = markupobject.points[i].y - markupobject.points[i - 1].y;
                            dimdiag += markupobject.getdiag(dimwidth, dimheight);

                        }
                    }


                    for (lcounter = 0; lcounter < this.pointlist.length; lcounter++) {
                        for (counter = 0; counter < this.pointlist[lcounter].length; counter++) {

                            if(counter > 0){
                                dimwidth = markupobject.pointlist[lcounter][counter].x - markupobject.pointlist[lcounter][counter - 1].x;
                                dimheight = markupobject.pointlist[lcounter][counter].y - markupobject.pointlist[lcounter][counter - 1].y;
    

                                /*if(markupobject.getdiag(dimwidth, dimheight) == 0){

                                }*/

                                dimdiags.push(markupobject.getdiag(dimwidth, dimheight));
                                dimdiag += markupobject.getdiag(dimwidth, dimheight);
                            }


                            
                        }
                    }

                    /*for (var i = 0; i < markupobject.points.length; i++) {
                        if (i > 0) {
                            var dimwidth = markupobject.points[i].x - markupobject.points[i - 1].x;
                            var dimheight = markupobject.points[i].y - markupobject.points[i - 1].y;


                            dimdiag += markupobject.getdiag(dimwidth, dimheight);

                        }
                    }*/

                    for(dimi = 0; dimi < dimdiags.length; dimi ++ ){
                        if(dimdiags[dimi] == 0){
                            dimtexts.push("");
                        }else{
                            if (Unitofmeasure == 2) {
                                if (bUseFraction) {
                                    blocalusefraction = true;
                                }
                                var szdimtext = markupobject.setfractionvalue(dimdiags[dimi]);
                            }
        
                            dimtext = getUnitlength(dimdiags[dimi] / markupobject.scaling);
                            dimtext = dimtext.toFixed(ndimprecision);
                            dimtext = dimtext + " " + Unitlabel;
    
                            if (blocalusefraction) {
                                dimtext = szdimtext;
                            }
                            dimtexts.push(dimtext);
                        }
                    }

                    if(dimdiag == 0){

                        dimtext = "";

                    }else{
                        if (Unitofmeasure == 2) {
                            if (bUseFraction) {
                                blocalusefraction = true;
                            }
                            var szdimtext = markupobject.setfractionvalue(dimdiag);
                        }
    
                        dimtext = getUnitlength(dimdiag / markupobject.scaling);
                        dimtext = dimtext.toFixed(ndimprecision);
                        dimtext = dimtext + " " + Unitlabel;

                        if (blocalusefraction) {
                            dimtext = szdimtext;
                        }
    
                    }
                    dimtexts.push(dimtext);

                    //all segments + total at the end.

                    return dimtexts;

                };

                this.getlinelength = function(p1x, p1y, p2x, p2y){
                    var dimdiag = 0;

                    var dimwidth = p2x - p1x;
                    var dimheight = p2y - p1y;

                    dimdiag = markupobject.getdiag(dimwidth, dimheight);
                    
                    return dimdiag;


                };

                this.setdimvaluepoly = function () {
                    var dimdiag = 0;
                    var dimtext = "";
                    var blocalusefraction = false;

                    //var Unitofmeasure = DocObj.getCurrentpage().Unitofmeasure;
                    //var Unitlabel = DocObj.getCurrentpage().Unitlabel;

                    var Unitofmeasure = DocObj.getCurrentpage().scaleObject.Unitofmeasure;
                    var Unitlabel = DocObj.getCurrentpage().scaleObject.Unitlabel;
                    var dimPrecision = DocObj.getCurrentpage().scaleObject.ndimprecision;

                    if(markupobject.hasScale) {
                        Unitofmeasure = markupobject.scaleObject.Unitofmeasure;
                        Unitlabel = markupobject.scaleObject.Unitlabel;
                        dimPrecision = markupobject.scaleObject.ndimprecision;
                        markupForScale = markupobject;
                    }


                    for (var i = 0; i < markupobject.points.length; i++) {
                        if (i > 0) {
                            var dimwidth = markupobject.points[i].x - markupobject.points[i - 1].x;
                            var dimheight = markupobject.points[i].y - markupobject.points[i - 1].y;
                            dimdiag += markupobject.getdiag(dimwidth, dimheight);

                        }
                    }

                    if(dimdiag == 0){

                        dimtext = "";

                    }else{
                        if (Unitofmeasure == 2) {
                            if (bUseFraction) {
                                blocalusefraction = true;
                            }
                            var szdimtext = markupobject.setfractionvalue(dimdiag);
                        }
    
                        dimtext = getUnitlength(dimdiag / markupobject.scaling);
                        //dimtext = dimtext.toFixed(ndimprecision);
                        dimtext = dimtext.toFixed(dimPrecision);
                        dimtext = dimtext + " " + Unitlabel;

                        markupForScale = null;

                        if (blocalusefraction) {
                            dimtext = szdimtext;
                        }
    
                    }

                    return dimtext;

                };
                this.setfractionvalue = function(dimdiag){


                    //var SubmeasureUnit = DocObj.getCurrentpage().SubmeasureUnit;

                    var SubmeasureUnit = DocObj.getCurrentpage().scaleObject.SubmeasureUnit;
                    if(markupobject.hasScale) {
                        SubmeasureUnit = markupobject.scaleObject.SubmeasureUnit;
                    }

                    
                    var fractionval = getUnitlengthRemainder(dimdiag / this.scaling);
                    var dimtextfrac = fractionval.quot;
                    var dimtextrem = fractionval.rem;
                    if (SubmeasureUnit == 1) {
                        var szdimtext = dimtextfrac + "\"";
                        if (fractionval.numerator != 0 && fractionval.denominator != 0) {
                            if (fractionval.numerator == fractionval.denominator) {
                                dimtextfrac + 1;
                                var szdimtext = dimtextfrac + "\"";
                            }
                            else {
                                var szfraction = fractionval.numerator + "/" + fractionval.denominator;
                                var szdimtext = dimtextfrac + " " + szfraction + "\"";
                            }
                        }
                    }
                    if (SubmeasureUnit == 2) {
                        if (dimtextrem != 0) {
                            var szdimtext = dimtextfrac + "\' - " + dimtextrem + "\"";
                        }
                        else {
                            szdimtext = dimtextfrac + "\'";
                        }
                        if (fractionval.numerator != 0 && fractionval.denominator != 0) {
                            if (fractionval.numerator == fractionval.denominator) {
                                dimtextrem + 1;
                                var szdimtext = dimtextfrac + "\' - " + dimtextrem + "\"";
                            }
                            else {
                                if (dimtextrem != 0) {
                                    var szfraction = fractionval.numerator + "/" + fractionval.denominator;
                                    var szdimtext = dimtextfrac + "\' - " + dimtextrem + " " + szfraction + "\"";
                                }
                                else {
                                    var szfraction = fractionval.numerator + "/" + fractionval.denominator;
                                    var szdimtext = dimtextfrac + "\' - " + szfraction + "\"";
                                }
                            }
                        }
                    }
                    
                    return szdimtext;


                };

                this.getunitofmeasure = function(measureobj){

                    var Unitofmeasure = DocObj.getCurrentpage().scaleObject.Unitofmeasure;
                    var Unitlabel = DocObj.getCurrentpage().scaleObject.Unitlabel;

                };

                this.setarcvalue = function (radius, arclength) {
                    let Unitofmeasure = DocObj.getCurrentpage().scaleObject.Unitofmeasure;                    
                    let Unitlabel = DocObj.getCurrentpage().scaleObject.Unitlabel;
                    let dimPrecision = DocObj.getCurrentpage().scaleObject.ndimprecision;
                
                    if (markupobject.hasScale) {
                        Unitofmeasure = markupobject.scaleObject.Unitofmeasure;
                        Unitlabel = markupobject.scaleObject.Unitlabel;
                        dimPrecision = markupobject.scaleObject.ndimprecision;
                        markupForScale = markupobject;
                    }
                
                    let blocalusefraction = (Unitofmeasure === 2 && bUseFraction);
                
                    if (blocalusefraction) {
                        this.dimradius = markupobject.setfractionvalue(radius);
                        this.dimlength = markupobject.setfractionvalue(arclength);
                    } else {
                        let scaledradius = getUnitlength(radius / this.scaling);
                        let scaledlength = getUnitlength(arclength / this.scaling);
                
                        this.dimradius = scaledradius.toFixed(dimPrecision) + " " + Unitlabel;
                        this.dimlength = scaledlength.toFixed(dimPrecision) + " " + Unitlabel;
                    }
                
                    markupForScale = null;
                };                

                this.setdimvalue = function (x, y, width, height) {
                    var dimwidth = Math.abs(width - x);
                    var dimheight = Math.abs(height - y);
                    var dimdiag = markupobject.getdiag(dimwidth, dimheight);
                    var blocalusefraction = false;


                    var Unitofmeasure = DocObj.getCurrentpage().scaleObject.Unitofmeasure;                    
                    var Unitlabel = DocObj.getCurrentpage().scaleObject.Unitlabel;
                    var dimPrecision = DocObj.getCurrentpage().scaleObject.ndimprecision;

                    if(markupobject.hasScale) {
                        Unitofmeasure = markupobject.scaleObject.Unitofmeasure;
                        Unitlabel = markupobject.scaleObject.Unitlabel;
                        dimPrecision = markupobject.scaleObject.ndimprecision;
                        markupForScale = markupobject;
                    }


                    if (Unitofmeasure == 2) {
                        if (bUseFraction) {
                            blocalusefraction = true;
                        }
                        var szdimtext = markupobject.setfractionvalue(dimdiag);
                        var szdimtextx = markupobject.setfractionvalue(dimwidth);
                        var szdimtexty = markupobject.setfractionvalue(dimheight);
                    }

                    



                    this.dimtext = getUnitlength(dimdiag / this.scaling);


                    //this.dimtext = this.dimtext.toFixed(ndimprecision);
                    this.dimtext = this.dimtext.toFixed(dimPrecision);
                    this.dimtext = this.dimtext + " " + Unitlabel;

                    this.dimtextx = getUnitlength(dimwidth / this.scaling);
                    //this.dimtextx = this.dimtextx.toFixed(ndimprecision);
                    this.dimtextx = this.dimtextx.toFixed(dimPrecision);
                    this.dimtextx = this.dimtextx + " " + Unitlabel;

                    this.dimtexty = getUnitlength(dimheight / this.scaling);
                    //this.dimtexty = this.dimtexty.toFixed(ndimprecision);
                    this.dimtexty = this.dimtexty.toFixed(dimPrecision);
                    this.dimtexty = this.dimtexty + " " + Unitlabel;

                    //var szdimtextx = markupobject.setfractionvalue(dimwidth);
                    //var szdimtexty = markupobject.setfractionvalue(dimheight);

                    markupForScale = null;

                    if (blocalusefraction) {
                        this.dimtext = szdimtext;
                        this.dimtextx = szdimtextx;
                        this.dimtexty = szdimtexty;

                    }
                };

                this.getUnitLength = function (value){

                    var dimtext = "";

                    var blocalusefraction = false;

                    //var Unitofmeasure = DocObj.getCurrentpage().Unitofmeasure;
                    //var Unitlabel = DocObj.getCurrentpage().Unitlabel;

                    var Unitofmeasure = DocObj.getCurrentpage().scaleObject.Unitofmeasure;
                    var Unitlabel = DocObj.getCurrentpage().scaleObject.Unitlabel;
                    var dimPrecision = DocObj.getCurrentpage().scaleObject.ndimprecision;

                    if(markupobject.hasScale) {
                        Unitofmeasure = markupobject.scaleObject.Unitofmeasure;
                        Unitlabel = markupobject.scaleObject.Unitlabel;
                        dimPrecision = markupobject.scaleObject.ndimprecision;
                        markupForScale = markupobject;
                    }


                    if (Unitofmeasure == 2) {
                        if (bUseFraction) {
                            blocalusefraction = true;
                        }
                        var szdimtext = markupobject.setfractionvalue(value);
                    }

                    dimtext = getUnitlength(value / this.scaling);

                    //dimtext = dimtext.toFixed(ndimprecision);
                    dimtext = dimtext.toFixed(dimPrecision);
                    dimtext = dimtext + " " + Unitlabel;
                    if (blocalusefraction) {
                        dimtext = szdimtext;
                    }

                    markupForScale = null;

                    return dimtext;

                }

                this.customlabeldraw = function (ctx, textarray, areatextxscaled, areatextyscaled, strokecolor, areatextscaled, localscalefactor) {
                    ctx.textAlign = "start";
                    markupobject.font.setScale(localscalefactor);
                    ctx.font = markupobject.font.fontstringScaled;
                    //ctx.font = areatextscaled + "pt " + "Helvetica";
                    //this.textheight = this.measuretextheight;
                    var areat = ctx.measureText(dimtext);
                    var areatextwidth = areat.width;
                    var areatextheight = areatextscaled;
                    areatextxscaled += (markupobject.labeloffsetx * localscalefactor);
                    areatextyscaled += (markupobject.labeloffsety * localscalefactor);
                    areatextxscaled = areatextxscaled - (areatextwidth / 2);
                    areatextyscaled = areatextyscaled + (areatextheight / 2);
                    ctx.lineWidth = 1;
                    ctx.strokeStyle = strokecolor;
                    ctx.fillStyle = "white";
                    ctx.fillRect(areatextxscaled - (10 * localscalefactor), areatextyscaled - (20 * localscalefactor), areatextwidth + (20 * localscalefactor), areatextscaled + (15 * localscalefactor));
                    ctx.strokeRect(areatextxscaled - (10 * localscalefactor), areatextyscaled - (20 * localscalefactor), areatextwidth + (20 * localscalefactor), areatextscaled + (15 * localscalefactor));
                };
                this.insidelabel = function (x, y) {
                    var within = false;
                    //var showlabel = !markupobject.showlabel || bMarkupNoLabel;
                    
                    if ((!markupobject.busecustomlabel && markupobject.subtype == 1) || (bMarkupNoLabel || markupobject.hidevaluelabel)) {
                        return within;
                    }
                    else {
                        if (x > markupobject.labelRect.x && x < (markupobject.labelRect.x + markupobject.labelRect.w)) {
                            if (y > markupobject.labelRect.y && y < (markupobject.labelRect.y + markupobject.labelRect.h)) {
                                within = true;
                            }
                        }
                    }
                    return within;
                };
                this.areavaluedraw = function (ctx, dimtext, areatextxscaled, areatextyscaled, strokecolor, areatextscaled, localscalefactor, scalefactor) {
                    
                    ctx.textAlign = "start";

                    if (bUseNoScale){
                        localscalefactor = 1;
                    }

                    markupobject.font.setScale(localscalefactor);
                    ctx.font = markupobject.font.fontstringScaled;
                    //ctx.font = areatextscaled + "pt " + "Helvetica";
                    //this.textheight = this.measuretextheight;
                    var labelsizefactor = markupobject.font.height / 12;
                    var labelstartoffsetx = 10 * labelsizefactor;
                    var labelstartoffsety = 20 * labelsizefactor;
                    var labelwidthtoffset = 20 * labelsizefactor;
                    var labelheightoffset = 15 * labelsizefactor;
                    var areat = ctx.measureText(dimtext);
                    var areatextwidth = areat.width;
                    var areatextheight = areatextscaled;
                    var scaletextheight = markupobject.font.height * localscalefactor;

                    if (bUseNoScale){
                        areatextxscaled += (markupobject.labeloffsetx * 1);
                        areatextyscaled += (markupobject.labeloffsety * 1);
    
                    }else{
                        areatextxscaled += (markupobject.labeloffsetx * scalefactor);
                        areatextyscaled += (markupobject.labeloffsety * scalefactor);
    
                    }


                    //areatextxscaled += markupobject.labeloffsetx; 
                    //areatextyscaled += markupobject.labeloffsety; 
                    //areatextxscaled += (markupobject.labeloffsetx * scalefactor);
                    //areatextyscaled += (markupobject.labeloffsety * scalefactor);

                    areatextxscaled = areatextxscaled - (areatextwidth / 2);
                    areatextyscaled = areatextyscaled + (areatextheight / 2);
                    ctx.lineWidth = 1;
                    ctx.strokeStyle = strokecolor;
                    ctx.fillStyle = markupobject.labelfillcolor;
                    markupobject.labelRect.x = areatextxscaled - (labelstartoffsetx * localscalefactor);
                    markupobject.labelRect.y = areatextyscaled - (labelstartoffsety * localscalefactor);
                    markupobject.labelRect.w = areatextwidth + (labelwidthtoffset * localscalefactor);
                    markupobject.labelRect.h = scaletextheight + (labelheightoffset * localscalefactor);
                    //ctx.fillRect(areatextxscaled - (labelstartoffsetx * localscalefactor), areatextyscaled - (labelstartoffsety * localscalefactor), areatextwidth + (labelwidthtoffset * localscalefactor), areatextscaled + (labelheightoffset * localscalefactor));
                    ctx.fillRect(areatextxscaled - (labelstartoffsetx * localscalefactor), areatextyscaled - (labelstartoffsety * localscalefactor), areatextwidth + (labelwidthtoffset * localscalefactor), scaletextheight + (labelheightoffset * localscalefactor));
                    //ctx.strokeRect(areatextxscaled - (labelstartoffsetx * localscalefactor), areatextyscaled - (labelstartoffsety * localscalefactor), areatextwidth + (labelwidthtoffset * localscalefactor), areatextscaled + (labelheightoffset * localscalefactor));
                    ctx.strokeRect(areatextxscaled - (labelstartoffsetx * localscalefactor), areatextyscaled - (labelstartoffsety * localscalefactor), areatextwidth + (labelwidthtoffset * localscalefactor), scaletextheight + (labelheightoffset * localscalefactor));
                    // ctx.fillStyle = this.color;
                    ctx.fillStyle = "black";
                    ctx.fillText(dimtext, areatextxscaled, areatextyscaled);
                };

                this.anglevaluedraw = function(ctx, dimpos, width, height, color, scalefactor){
                    if(markupobject.hidevaluelabel) {
                        return;
                    }

                    ctx.save();

                    ctx.textAlign = "start";
                    markupobject.font.setScale(scalefactor);
                    var scaletextheight = markupobject.font.height * scalefactor;
                    this.textheight = markupobject.font.height;
                    var labelsizefactor = markupobject.font.height / 12;
                    var labelstartoffsetx = 10 * labelsizefactor;
                    var labelstartoffsety = 5 * labelsizefactor;
                    var labelwidthtoffset = 20 * labelsizefactor;
                    var labelheightoffset = 10 * labelsizefactor;
                    ctx.font = markupobject.font.fontstringScaled;
                    var dimt = ctx.measureText(this.dimtext);
                    var dimtextwidth = dimt.width;

                    if (markupobject.saved) {
                        var dimtextx = dimpos.x - (dimtextwidth / 2);
                        var dimtexty = dimpos.y + (scaletextheight / 2);

                    }
                    else {
                        //var dimtextx = x + (dimwidth / 2) - (dimtextwidth / 2);
                        dimtextx = width + (15 * scalefactor);
                        dimtexty = height;

                        //var dimtexty = y + (dimheight / 2) + (scaletextheight / 2);
                    }

                    ctx.lineWidth = 1;
                    var yrect = dimtexty - scaletextheight;
                    ctx.fillStyle = "white";
                    ctx.strokeStyle = this.strokecolor;

                    ctx.fillRect(dimtextx - (labelstartoffsetx * scalefactor), yrect - (labelstartoffsety * scalefactor), dimtextwidth + (labelwidthtoffset * scalefactor), scaletextheight + (labelheightoffset * scalefactor));
                    ctx.strokeRect(dimtextx - (labelstartoffsetx * scalefactor), yrect - (labelstartoffsety * scalefactor), dimtextwidth + (labelwidthtoffset * scalefactor), scaletextheight + (labelheightoffset * scalefactor));

                    labelsize.w = dimtextwidth + (labelwidthtoffset * scalefactor);
                    labelsize.h = scaletextheight + (labelheightoffset * scalefactor);
                    ctx.fillStyle = "black";
                    ctx.fillText(this.dimtext, dimtextx, dimtexty);

                    ctx.restore();

                }; 
                this.dimvaluedraw = function (ctx, x, y, width, height, color, scalefactor, brotate) {

                    if(markupobject.hidevaluelabel) {
                        return;
                    }

                    ctx.save();

                    if (bUseNoScale){
                        scalefactor = 1;
                    }

                    var dimwidth = width - x;
                    var dimheight = height - y;

                    if(markupobject.type == 3){

                        dimwidth = width;
                        dimheight = height;
                    }


                    var dimdiag = markupobject.getdiag(dimwidth, dimheight);
                    var dimanglerad = Math.atan2(dimheight, dimwidth);
                    ctx.textAlign = "start";
                    markupobject.font.setScale(scalefactor);
                    var scaletextheight = markupobject.font.height * scalefactor;
                    this.textheight = markupobject.font.height;
                    var labelsizefactor = markupobject.font.height / 12;
                    var labelstartoffsetx = 10 * labelsizefactor;
                    var labelstartoffsety = 5 * labelsizefactor;
                    var labelwidthtoffset = 20 * labelsizefactor;
                    var labelheightoffset = 10 * labelsizefactor;
                    ctx.font = markupobject.font.fontstringScaled;
                    var dimt = ctx.measureText(this.dimtext);
                    var dimtextwidth = dimt.width;


                    if (markupobject.saved) {
                        var dimtextx = x + (dimwidth / 2) - (dimtextwidth / 2);
                        var dimtexty = y + (dimheight / 2) + (scaletextheight / 2);

                    }
                    else {
                        //var dimtextx = x + (dimwidth / 2) - (dimtextwidth / 2);
                        dimtextx = width + (15 * scalefactor);
                        dimtexty = height;

                        if(markupobject.type == 3){
                            dimtextx = width + x + (15 * scalefactor);
                            dimtexty = height + y;
                                
                        }


                        //var dimtexty = y + (dimheight / 2) + (scaletextheight / 2);
                    }
                    if (dimanglerad > (Math.PI * 0.5) || dimanglerad < -(Math.PI * 0.5)) {
                        if (markupobject.pagerotation == 0) {
                            dimanglerad += Math.PI;
                        }
                    }
                    //console.log(dimanglerad);
                    if (brotate && markupobject.saved) {
                        var tx = x + (dimwidth / 2);
                        var ty = y + (dimheight / 2);
                        ctx.translate(tx, ty);
                        ctx.rotate(dimanglerad);
                        ctx.translate(-tx, -ty);
                    }
                    /*if(rotation !=0){
                     }*/
                    ctx.lineWidth = 1;
                    var yrect = dimtexty - scaletextheight;
                    ctx.fillStyle = "white";
                    ctx.strokeStyle = this.strokecolor;

                    ctx.fillRect(dimtextx - (labelstartoffsetx * scalefactor), yrect - (labelstartoffsety * scalefactor), dimtextwidth + (labelwidthtoffset * scalefactor), scaletextheight + (labelheightoffset * scalefactor));
                    ctx.strokeRect(dimtextx - (labelstartoffsetx * scalefactor), yrect - (labelstartoffsety * scalefactor), dimtextwidth + (labelwidthtoffset * scalefactor), scaletextheight + (labelheightoffset * scalefactor));

                    labelsize.w = dimtextwidth + (labelwidthtoffset * scalefactor);
                    labelsize.h = scaletextheight + (labelheightoffset * scalefactor);
                    ctx.fillStyle = "black";
                    ctx.fillText(this.dimtext, dimtextx, dimtexty);

                    ctx.restore();
                };
                this.roundedRect = function (ctx, x, y, width, height, rotation, radius, linewidth, fill, stroke, fillcolor, strokecolor) {
                    ctx.save(); // save the context so we don't mess up others
                    ctx.strokeStyle = strokecolor;
                    ctx.fillStyle = fillcolor;
                    ctx.lineWidth = linewidth;
                    if (rotation != 0) {
                        var tx = x + (width / 2);
                        var ty = y + (height / 2);
                        ctx.translate(tx, ty);
                        ctx.rotate(markupobject.rotation);
                        ctx.translate(-tx, -ty);
                    }
                    //alternative 2 start
                    ctx.beginPath();
                    ctx.moveTo(x + radius, y);
                    ctx.lineTo(x + width - radius, y);
                    ctx.arcTo(x + width, y, x + width, y + radius, radius);
                    ctx.lineTo(x + width, y + height - radius);
                    ctx.arcTo(x + width, y + height, x + width - radius, y + height, radius);
                    ctx.lineTo(x + radius, y + height);
                    ctx.arcTo(x, y + height, x, y + height - radius, radius);
                    ctx.lineTo(x, y + radius);
                    ctx.arcTo(x, y, x + radius, y, radius);
                    //alternative 2 end
                    /*ctx.beginPath();
        
                     // draw top and top right corner
                     ctx.moveTo(x+radius,y);
                     ctx.arcTo(x+width,y,x+width,y+radius,radius);
        
                     // draw right side and bottom right corner
                     ctx.arcTo(x+width,y+height,x+width-radius,y+height,radius);
        
                     // draw bottom and bottom left corner
                     ctx.arcTo(x,y+height,x,y+height-radius,radius);
        
                     // draw left and top left corner
                     ctx.arcTo(x,y,x+radius,y,radius);*/
                    if (fill) {
                        ctx.fill();
                    }
                    if (stroke) {
                        ctx.stroke();
                    }
                    ctx.restore(); // restore context to what it was on entry
                };
                this.newcloud = function (ctx, x, y, width, height, radius, linewidth, fill, stroke, fillcolor, strokecolor) {
                    if (width == 0 || height == 0) {
                        return;
                    }
                    if (width <= 0 || height <= 0) {
                        if (width < 0) {
                            x += width;
                            width = Math.abs(width);
                        }
                        if (height < 0) {
                            y += height;
                            height = Math.abs(height);
                        }
                        //return;
                    }
                    var diameter = radius * 2;
                    var numdiamh = Math.floor(width / diameter);
                    var numdiamv = Math.floor(height / diameter);
                    var diamwidth = diameter * numdiamh;
                    var diamheight = diameter * numdiamv;
                    var doscale = false;
                    var xscale = 1;
                    var yscale = 1;
                    var lNumX = 8;
                    var dRadX = width / lNumX;
                    var lNumY = height / dRadX;
                    if (lNumY < 6) {
                        dRadX = height / 6;
                        lNumY = 6;
                        lNumX = width / dRadX;
                    }
                    var dRadY = height / lNumY;
                    //var nArcsX = lNumX/2;
                    //var nArcsY = lNumY/2 - 1;
                    var localradiusx = dRadX;
                    var diameterx = localradiusx * 2;
                    var localradiusy = dRadY;
                    var diametery = localradiusy * 2;
                    numdiamh = Math.floor(width / diameterx);
                    var doublediamh = (width / diameterx);
                    numdiamv = Math.floor(height / diameterx);
                    var doublediamv = (height / diameterx);
                    /*if(width > 0){
                     }else{
                     numdiamh = 1;
                     doublediamh = 1;
                     }
                     if(height > 0){
        
                     }else{
                     numdiamv = 1;
                     doublediamv = 1;
                     }*/
                    var doublediamwidth = diameterx * doublediamh;
                    var doublediamheight = diameterx * doublediamv;
                    diamwidth = diameterx * numdiamh;
                    diamheight = diameterx * numdiamv;
                    ctx.save(); // save the context so we don't mess up others
                    ctx.strokeStyle = strokecolor;
                    ctx.fillStyle = fillcolor;
                    ctx.lineWidth = linewidth;
                    if (markupobject.rotation != 0) {
                        var tx = x + (width / 2);
                        var ty = y + (height / 2);
                        ctx.translate(tx, ty);
                        ctx.rotate(markupobject.rotation);
                        ctx.translate(-tx, -ty);
                    }
                    if (width > diamwidth) {
                        xscale = width / diamwidth;
                        doscale = true;
                    }
                    if (height > diamheight) {
                        yscale = height / diamheight;
                        doscale = true;
                    }
                    if (doscale) {
                        ctx.translate(x, y);
                        ctx.scale(xscale, yscale);
                        ctx.translate(-x, -y);
                    }
                    ctx.beginPath();
                    //ctx.moveTo(x+localradiusx,y+diametery);
                    //upper left bubble
                    ctx.arc(x + localradiusx, y + localradiusx, localradiusx, (0.5 * Math.PI), 0, false);
                    //if (numdiamh > 2){
                    for (var i = 2; i < numdiamh; i++) {
                        var vertoffsett = x + localradiusx + (diameterx * (i - 1));
                        ctx.arc(vertoffsett, y + localradiusx, localradiusx, Math.PI, 0, false);
                    }
                    //}
                    //upper right bubble
                    ctx.arc(x + diamwidth - localradiusx, y + localradiusx, localradiusx, Math.PI, (0.5 * Math.PI), false);
                    //if (numdiamv > 2){
                    for (i = 2; i < numdiamv; i++) {
                        var horoffsetr = y + localradiusx + (diameterx * (i - 1));
                        ctx.arc(x + diamwidth - localradiusx, horoffsetr, localradiusx, 1.5 * Math.PI, 0.5 * Math.PI, false);
                    }
                    //}
                    //lower right bubble
                    ctx.arc(x + diamwidth - localradiusx, y + diamheight - localradiusx, localradiusx, (1.5 * Math.PI), Math.PI, false);
                    //if (numdiamh > 2){
                    for (i = numdiamh - 1; i > 1; i--) {
                        var vertoffsetb = x + localradiusx + (diameterx * (i - 1));
                        ctx.arc(vertoffsetb, y + diamheight - localradiusx, localradiusx, 0, Math.PI, false);
                    }
                    //}
                    //lower left bubble
                    ctx.arc(x + localradiusx, y + diamheight - localradiusx, localradiusx, 0, (1.5 * Math.PI), false);
                    //if (numdiamv > 2){
                    for (i = numdiamv - 1; i > 1; i--) {
                        var horoffsetl = y + localradiusx + (diameterx * (i - 1));
                        ctx.arc(x + localradiusx, horoffsetl, localradiusx, 0.5 * Math.PI, 1.5 * Math.PI, false);
                    }
                    //}
                    /*if (width<diameter*2 && height<diameter*2){
                     //draw a circle until width > radius and height > radius
                     ctx.arc(x+radius, y+radius, width/2, 0 , 2 * Math.PI, false);
                     //ArcToPolyline( x1+nPosX*dRadX, y1 + dRadY, dRadX, dRadY, 90, 270, 0, pDestPnts, &lPoints );
                     }else{
        
                     }*/
                    //ctx.closePath();
                    if (fill) {
                        ctx.fill();
                    }
                    if (stroke) {
                        ctx.stroke();
                    }
                    ctx.restore(); // restore context to what it was on entry
                };
                this.cloud = function (ctx, x, y, width, height, radius, linewidth, fill, stroke, fillcolor, strokecolor) {
                    var diameter = radius * 2;


                    var numdiamh = Math.floor(width / diameter);
                    var numdiamv = Math.floor(height / diameter);
                    var diamwidth = diameter * numdiamh;
                    var diamheight = diameter * numdiamv;
                    
                    ctx.save(); // save the context so we don't mess up others
                    ctx.strokeStyle = strokecolor;
                    ctx.fillStyle = fillcolor;
                    ctx.lineWidth = linewidth;
                    
                    if (markupobject.rotation != 0) {
                        var tx = x + (width / 2);
                        var ty = y + (height / 2);
                        ctx.translate(tx, ty);
                        ctx.rotate(markupobject.rotation);
                        ctx.translate(-tx, -ty);
                    }
                    
                    
                    ctx.beginPath();
                    if (width < diameter * 2 && height < diameter * 2) {
                        //draw a circle until width > radius and height > radius
                        ctx.arc(x + radius, y + radius, width / 2, 0, 2 * Math.PI, false);
                    }
                    else {
                        ctx.moveTo(x + radius, y + diameter);
                        //upper left bubble
                        ctx.arc(x + radius, y + radius, radius, (0.5 * Math.PI), 0, false);
                        if (numdiamh > 2) {
                            for (var i = 2; i < numdiamh; i++) {
                                var vertoffsett = x + radius + diameter * (i - 1);
                                ctx.arc(vertoffsett, y + radius, radius, Math.PI, 0, false);
                            }
                        }
                        //upper right bubble
                        ctx.arc(x + diamwidth - radius, y + radius, radius, Math.PI, (0.5 * Math.PI), false);
                        if (numdiamv > 2) {
                            for (i = 2; i < numdiamv; i++) {
                                var horoffsetr = y + radius + diameter * (i - 1);
                                ctx.arc(x + diamwidth - radius, horoffsetr, radius, 1.5 * Math.PI, 0.5 * Math.PI, false);
                            }
                        }
                        //lower right bubble
                        ctx.arc(x + diamwidth - radius, y + diamheight - radius, radius, (1.5 * Math.PI), Math.PI, false);
                        if (numdiamh > 2) {
                            for (i = numdiamh - 1; i > 1; i--) {
                                var vertoffsetb = x + radius + (diameter * (i - 1));
                                ctx.arc(vertoffsetb, y + diamheight - radius, radius, 0, Math.PI, false);
                            }
                        }
                        //lower left bubble
                        ctx.arc(x + radius, y + diamheight - radius, radius, 0, (1.5 * Math.PI), false);
                        if (numdiamv > 2) {
                            for (i = numdiamv - 1; i > 1; i--) {
                                var horoffsetl = y + radius + (diameter * (i - 1));
                                ctx.arc(x + radius, horoffsetl, radius, 0.5 * Math.PI, 1.5 * Math.PI, false);
                            }
                        }
                    }
                    //ctx.closePath();
                    if (fill) {
                        ctx.fill();
                    }
                    if (stroke) {
                        ctx.stroke();
                    }
                    ctx.restore(); // restore context to what it was on entry
                };
                this.markpolygon = function(ctx, scalefactor){

                    var counter = 0;
                    ctx.save();
                    //ctx.strokeStyle = "red";
                    //ctx.fillStyle = "white";
                    ctx.fillStyle = "rgba(236, 236, 236, 0.5)";
                    //ctx.lineWidth = 1;
                    //ctx.beginPath();
                    //ctx.moveTo(this.points[0].x, this.points[0].y);
                    var xscaled = 0.0;
                    var yscaled = 0.0;

                    var offset = getCurrentPageOffset(markupobject.pagenumber);

                    if(markupobject.type == 13) {

                        counter = 0;
                        
                        for (counter = 0; counter < markupobject.countshapes.length; counter++) {
                            xscaled = (markupobject.countshapes[counter].x - this.xoffset) * scalefactor;
                            yscaled = (markupobject.countshapes[counter].y - this.yoffset) * scalefactor;

                            xscaled += offset.x;
                            yscaled += offset.y;

                            var outerRadiusScaled = markupobject.outerRadius * markupobject.fixedscaleFactor;

                            if(bUseNoScale){
                                outerRadiusScaled = markupobject.outerRadius;
                                //var innerRadiusScaled = innerRadius;
                            }

                            //ctx.strokeStyle = "blue";
                            ctx.beginPath();
                            ctx.arc(xscaled, yscaled, outerRadiusScaled + 3, 0, Math.PI * 2, false);
                            ctx.closePath();
                            ctx.fill();
                            
                        }
                    }

                    ctx.restore();
                };

                this.editpolygonhole = function(ctx, scalefactor, hole){

                    var counter = 0;
                    var lcounter = 0;
                    ctx.save();
                    ctx.strokeStyle = "red";
                    ctx.fillStyle = "white";
                    ctx.lineWidth = 1;
                    //ctx.beginPath();
                    //ctx.moveTo(this.points[0].x, this.points[0].y);
                    var xscaled = 0.0;
                    var yscaled = 0.0;

                    var offset = getCurrentPageOffset(markupobject.pagenumber);

                    for (counter = 0; counter < hole.points.length; counter++) {
                        xscaled = (hole.points[counter].x - hole.xoffset) * scalefactor;
                        yscaled = (hole.points[counter].y - hole.yoffset) * scalefactor;


                        xscaled += offset.x;
                        yscaled += offset.y;


                        //var bpolyline = (this.type == 1 && (this.subtype == 1 || this.subtype == 3));
                        //var blastpoint = (counter == markupobject.points.length - 1);
                        var bnonextpoint = (hole.editcurpoint + 1 > hole.points.length - 1);
                        var bmarkfirstpoint = (bnonextpoint && counter == 0);
                        var bpolylinelastpoint = false;


                        if (hole.bpointEdit && (hole.editcurpoint == counter || hole.editcurpoint + 1 == counter || bmarkfirstpoint) && !bpolylinelastpoint) {
                            ctx.save();
                            ctx.fillStyle = "blue";
                            ctx.lineWidth = 2;
                            ctx.fillRect(xscaled - 5, yscaled - 5, 10, 10);
                            ctx.strokeRect(xscaled - 5, yscaled - 5, 10, 10);
                            ctx.restore();
                        }
                        else {
                            ctx.fillRect(xscaled - 5, yscaled - 5, 10, 10);
                            ctx.strokeRect(xscaled - 5, yscaled - 5, 10, 10);
                        }
                    }

                    ctx.restore();

                }

                this.editpolygon = function (ctx, scalefactor) {
                    var counter = 0;
                    var lcounter = 0;
                    ctx.save();
                    ctx.strokeStyle = "red";
                    ctx.fillStyle = "white";
                    ctx.lineWidth = 1;
                    //ctx.beginPath();
                    //ctx.moveTo(this.points[0].x, this.points[0].y);
                    var xscaled = 0.0;
                    var yscaled = 0.0;

                    var arcpoint1x = 0.0;
                    var arcpoint1y = 0.0;
                    var arcpoint2x = 0.0;
                    var arcpoint2y = 0.0;
                    var arcpoint3x = 0.0;
                    var arcpoint3y = 0.0;

                    var offset = getCurrentPageOffset(markupobject.pagenumber);

                    if(markupobject.type == 14) {

                        arcpoint1x = (markupobject.startX - this.xoffset) * scalefactor;
                        arcpoint1y = (markupobject.startY - this.yoffset) * scalefactor;

                        arcpoint1x += offset.x;
                        arcpoint1y += offset.y;

                        arcpoint2x = (markupobject.secondX - this.xoffset) * scalefactor;
                        arcpoint2y = (markupobject.secondY - this.yoffset) * scalefactor;

                        arcpoint2x += offset.x;
                        arcpoint2y += offset.y;

                        var bnothirdpoint = (markupobject.endX == null && markupobject.endY == null);

                        
                        if(!bnothirdpoint){
                            arcpoint3x = (markupobject.endX - this.xoffset) * scalefactor;
                            arcpoint3y = (markupobject.endY - this.yoffset) * scalefactor;
    
                            arcpoint3x += offset.x;
                            arcpoint3y += offset.y;
    
                        }



                        //var p1 = {x : arcpoint1x, y:arcpoint1y};
                        //var p2 = {x : arcpoint2x, y:arcpoint2y};
                        //var p3 = {x : arcpoint3x, y:arcpoint3y};


                        ctx.fillRect(arcpoint1x - 5, arcpoint1y - 5, 10, 10);
                        ctx.strokeRect(arcpoint1x - 5, arcpoint1y - 5, 10, 10);

                        ctx.fillRect(arcpoint2x - 5, arcpoint2y - 5, 10, 10);
                        ctx.strokeRect(arcpoint2x - 5, arcpoint2y - 5, 10, 10);

                        if(!bnothirdpoint){
                            ctx.fillRect(arcpoint3x - 5, arcpoint3y - 5, 10, 10);
                            ctx.strokeRect(arcpoint3x - 5, arcpoint3y - 5, 10, 10);
                        }




                        //this.editArc(ctx, scalefactor);
                    }

                    for (counter = 0; counter < markupobject.points.length; counter++) {
                        xscaled = (this.points[counter].x - this.xoffset) * scalefactor;
                        yscaled = (this.points[counter].y - this.yoffset) * scalefactor;


                        xscaled += offset.x;
                        yscaled += offset.y;


                        var bpolyline = (this.type == 1 && (this.subtype == 1 || this.subtype == 3));
                        var blastpoint = (counter == markupobject.points.length - 1);
                        var bnonextpoint = (markupobject.editcurpoint + 1 > markupobject.points.length - 1);
                        var bmarkfirstpoint = (!bpolyline && bnonextpoint && counter == 0);
                        var bpolylinelastpoint = (bpolyline && bnonextpoint);
                        if (markupobject.bpointEdit && (markupobject.editcurpoint == counter || markupobject.editcurpoint + 1 == counter || bmarkfirstpoint) && !bpolylinelastpoint) {
                            ctx.save();
                            ctx.fillStyle = "blue";
                            ctx.lineWidth = 2;
                            ctx.fillRect(xscaled - 5, yscaled - 5, 10, 10);
                            ctx.strokeRect(xscaled - 5, yscaled - 5, 10, 10);
                            ctx.restore();
                        }
                        else {
                            ctx.fillRect(xscaled - 5, yscaled - 5, 10, 10);
                            ctx.strokeRect(xscaled - 5, yscaled - 5, 10, 10);
                        }
                    }

                    if(this.type == 1 && this.subtype == 6){

                        for (lcounter = 0; lcounter < this.pointlist.length; lcounter++) {
                            for (counter = 0; counter < this.pointlist[lcounter].length; counter++) {

                                xscaled = (this.pointlist[lcounter][counter].x - this.xoffset) * scalefactor;
                                yscaled = (this.pointlist[lcounter][counter].y - this.yoffset) * scalefactor;
        
                                xscaled += offset.x;
                                yscaled += offset.y;


                                var bnonextpoint = (markupobject.editcurpoint + 1 > this.pointlist[lcounter].length - 1);
                                var bmarkfirstpoint = (bnonextpoint && counter == 0);
                                var bpolylinelastpoint = (bnonextpoint);
        
                                if (markupobject.bpointEdit && (markupobject.editcurpoint == counter || markupobject.editcurpoint + 1 == counter || bmarkfirstpoint) && !bpolylinelastpoint) {
                                    ctx.save();
                                    ctx.fillStyle = "blue";
                                    ctx.lineWidth = 2;
                                    ctx.fillRect(xscaled - 5, yscaled - 5, 10, 10);
                                    ctx.strokeRect(xscaled - 5, yscaled - 5, 10, 10);
                                    ctx.restore();
                                }else{
                                    ctx.fillRect(xscaled - 5, yscaled - 5, 10, 10);
                                    ctx.strokeRect(xscaled - 5, yscaled - 5, 10, 10);
                                }
        
        
    
                                /*if(counter > 0){
                                    dimwidth = markupobject.pointlist[lcounter][counter].x - markupobject.pointlist[lcounter][counter - 1].x;
                                    dimheight = markupobject.pointlist[lcounter][counter].y - markupobject.pointlist[lcounter][counter - 1].y;
        
                                    dimdiag += markupobject.getdiag(dimwidth, dimheight);
                                }*/
    
                                
                            }
                        }
    

                        //for (lcounter = 0; lcounter <  )
                    }

                    if(markupobject.type == 13) {

                        counter = 0;

                        
                        for (counter = 0; counter < markupobject.countshapes.length; counter++) {
                            xscaled = (markupobject.countshapes[counter].x - this.xoffset) * scalefactor;
                            yscaled = (markupobject.countshapes[counter].y - this.yoffset) * scalefactor;
    
                            xscaled += offset.x;
                            yscaled += offset.y;


                            var outerRadiusScaled = markupobject.outerRadius * markupobject.fixedscaleFactor;

                            if(bUseNoScale){
                                outerRadiusScaled = markupobject.outerRadius;
                                //var innerRadiusScaled = innerRadius;
                            }
                            
                            ctx.strokeStyle = "blue";
                            ctx.beginPath();
                            ctx.arc(xscaled, yscaled, outerRadiusScaled + 3, 0, Math.PI * 2, false);
                            ctx.closePath();
                            ctx.stroke();
    
                            
                        }

                    }                    

                    //ctx.closePath();
                    //markupobject
                    ctx.restore();
                };
                this.selectLabel = function (ctx, x, y, width, height, full, rotate) {
                    var halfmarker = markersize / 2;
                    ctx.save(); // save the context so we don't mess up others
                    ctx.strokeStyle = "blue";
                    ctx.fillStyle = "white";
                    ctx.lineWidth = 1;
                    ctx.beginPath();
                    var tx = x + ((width - x) / 2);
                    var ty = y + ((height - y) / 2);
                    //var centerx = width - (x / 2);
                    //var centery = height - (y / 2);
                    ctx.arc(tx, ty, halfmarker, 0, Math.PI * 2, false);
                    ctx.closePath();
                    ctx.fill();
                    ctx.stroke();
                    ctx.restore(); // restore context to what it was on entry
                };
                this.mark = function(ctx, x, y, width, height, full, rotate){

                    var tx = x + ((width - x) / 2);
                    var ty = y + ((height - y) / 2);

                    ctx.save(); // save the context so we don't mess up others

                    ctx.fillStyle = szmarkColor;

                    if (full) {

                        if (markupobject.rotation != 0) {
                            //var tx = x+(width/2);
                            //var ty = y+(height/2);
                            ctx.translate(tx, ty);
                            ctx.rotate(markupobject.rotation);
                            ctx.translate(-tx, -ty);
                        }

                        ctx.beginPath();
                        ctx.moveTo(x,y);
                        ctx.lineTo(width,y);
                        ctx.lineTo(width,height);
                        ctx.lineTo(x,height);
                        ctx.closePath();
                        ctx.fill();


                    }else{


                        if (markupobject.rotation != 0) {
                            //var tx = x+(width/2);
                            //var ty = y+(height/2);
                            ctx.translate(tx, ty);
                            ctx.rotate(markupobject.rotation);
                            ctx.translate(-tx, -ty);
                        }

                        ctx.beginPath();
                        ctx.moveTo(x,y);
                        ctx.lineTo(width,y);
                        ctx.lineTo(width,height);
                        ctx.lineTo(x,height);
                        ctx.closePath();
                        ctx.fill();


                        //ctx.strokeStyle = "blue";
                        
                        
                        /*ctx.lineWidth = 1;
                        ctx.beginPath();
                        ctx.arc(x, y, halfmarker, 0, Math.PI * 2, false);
                        ctx.closePath();
                        ctx.fill();
                        ctx.stroke();
                        var btextboxleader = (this.type == 6 && this.subtype == 6);
                        if (!btextboxleader) {
                            ctx.beginPath();
                            ctx.arc(width, height, halfmarker, 0, Math.PI * 2, false);
                            ctx.closePath();
                            ctx.stroke();
                            ctx.fill();
                        }*/
                    }
                    ctx.restore(); // restore context to what it was on entry

                };                
                this.select = function (ctx, x, y, width, height, full, rotate) {


                    if(bNoMarkuprotate){
                        rotate = !bNoMarkuprotate;
                    }

                    var centercanvX = (canvasowidth / 2);
                    var centercanvY = (canvasoheight / 2);

                    var bisfoxitPDF = isfoxitPDF();

                    if(bisfoxitPDF){

                        var pixelRatio = getPixelRatio();

                        centercanvX *= pixelRatio;
                        centercanvY *= pixelRatio;
    
                    }



                    var CanvRotRad = DocObj.getCurrentpage().getpageRotation() * (Math.PI / 180);
                    var rotRad360 = 360 * (Math.PI / 180);
                    var halfmarker = markersize / 2;
                    var pointul = new point();
                    var pointlr = new point();
                    
                    pointul.x = x;
                    pointul.y = y;
                    pointlr.x = width;
                    pointlr.y = height;

                    var tx = x + ((width - x) / 2);
                    var ty = y + ((height - y) / 2);
                    if (DocObj.getCurrentpage().getpageRotation() != 0) {
                        pointul = markupobject.getRotatedMarkup(centercanvX, centercanvY, x, y, CanvRotRad);
                        pointlr = markupobject.getRotatedMarkup(centercanvX, centercanvY, width, height, CanvRotRad);
                    }
                    ctx.save(); // save the context so we don't mess up others
                    if (full) {
                        //if text created on rotated canvas
                        if (markupobject.textrotate != 0 && markupobject.type == 9 && markupobject.subtype == 0) {
                            ctx.translate(x, y);
                            ctx.rotate(markupobject.textrotate);
                            ctx.translate(-x, -y);
                        }
                        //find center
                        if (markupobject.rotation != 0) {
                            //var tx = x+(width/2);
                            //var ty = y+(height/2);
                            ctx.translate(tx, ty);
                            ctx.rotate(markupobject.rotation);
                            ctx.translate(-tx, -ty);
                            if (markupobject.type == 9 && markupobject.subtype == 0) {
                                ty = y - ((y - height) / 2);
                                var negheightcenter = (height - y) / 2;
                                if (markupobject.textrotate != 0) {
                                    var lrrotpoint = markupobject.getMarkupSelectPointRot(((width - x) / 2), (-(height - y) / 2), markupobject.rotation);
                                    var tulrotpoint = markupobject.getMarkupSelectPointRot(lrrotpoint.x, lrrotpoint.y, markupobject.textrotate);
                                    var rmrkcenter = markupobject.getMarkupSelectPointRot((width - x) / 2, negheightcenter, markupobject.textrotate);
                                    tx = x + rmrkcenter.x;
                                    ty = y + rmrkcenter.y;
                                    this.LowerRightRectRot.x = tx + tulrotpoint.x - halfmarker;
                                    this.LowerRightRectRot.y = ty + tulrotpoint.y - halfmarker;
                                    this.LowerRightRectRot.w = tx + tulrotpoint.x + halfmarker;
                                    this.LowerRightRectRot.h = ty + tulrotpoint.y + halfmarker;
                                }
                                else {
                                    var ulrotpoint = markupobject.getMarkupSelectPointRot(-((width - x) / 2), -(-((height - y) / 2)), markupobject.rotation);
                                    this.UpperLeftRectRot.x = tx + ulrotpoint.x - halfmarker;
                                    this.UpperLeftRectRot.y = ty + ulrotpoint.y - halfmarker;
                                    this.UpperLeftRectRot.w = tx + ulrotpoint.x + halfmarker;
                                    this.UpperLeftRectRot.h = ty + ulrotpoint.y + halfmarker;
                                    lrrotpoint = markupobject.getMarkupSelectPointRot(((width - x) / 2), (-(height - y) / 2), markupobject.rotation);
                                    this.LowerRightRectRot.x = tx + lrrotpoint.x - halfmarker;
                                    this.LowerRightRectRot.y = ty + lrrotpoint.y - halfmarker;
                                    this.LowerRightRectRot.w = tx + lrrotpoint.x + halfmarker;
                                    this.LowerRightRectRot.h = ty + lrrotpoint.y + halfmarker;
                                }
                            }
                            else {
                                ulrotpoint = markupobject.getMarkupSelectPointRot(-((width - x) / 2), -((height - y) / 2), markupobject.rotation);
                                this.UpperLeftRectRot.x = tx + ulrotpoint.x - halfmarker;
                                this.UpperLeftRectRot.y = ty + ulrotpoint.y - halfmarker;
                                this.UpperLeftRectRot.w = tx + ulrotpoint.x + halfmarker;
                                this.UpperLeftRectRot.h = ty + ulrotpoint.y + halfmarker;
                                lrrotpoint = markupobject.getMarkupSelectPointRot(((width - x) / 2), ((height - y) / 2), markupobject.rotation);
                                this.LowerRightRectRot.x = tx + lrrotpoint.x - halfmarker;
                                this.LowerRightRectRot.y = ty + lrrotpoint.y - halfmarker;
                                this.LowerRightRectRot.w = tx + lrrotpoint.x + halfmarker;
                                this.LowerRightRectRot.h = ty + lrrotpoint.y + halfmarker;
                                var llrotpoint = markupobject.getMarkupSelectPointRot(-((width - x) / 2), ((height - y) / 2), markupobject.rotation);
                                this.LowerLeftRectRot.x = tx + llrotpoint.x - halfmarker;
                                this.LowerLeftRectRot.y = ty + llrotpoint.y - halfmarker;
                                this.LowerLeftRectRot.w = tx + llrotpoint.x + halfmarker;
                                this.LowerLeftRectRot.h = ty + llrotpoint.y + halfmarker;
                                var urrotpoint = markupobject.getMarkupSelectPointRot(((width - x) / 2), -((height - y) / 2), markupobject.rotation);
                                this.UpperRightRectRot.x = tx + urrotpoint.x - halfmarker;
                                this.UpperRightRectRot.y = ty + urrotpoint.y - halfmarker;
                                this.UpperRightRectRot.w = tx + urrotpoint.x + halfmarker;
                                this.UpperRightRectRot.h = ty + urrotpoint.y + halfmarker;
                            }
                            if (DocObj.getCurrentpage().getpageRotation() != 0) {
                                var ULrotpointul = markupobject.getRotatedMarkup(centercanvX, centercanvY, this.UpperLeftRectRot.x, this.UpperLeftRectRot.y, CanvRotRad);
                                var ULrotpointlr = markupobject.getRotatedMarkup(centercanvX, centercanvY, this.UpperLeftRectRot.w, this.UpperLeftRectRot.h, CanvRotRad);
                                this.UpperLeftRectRot.x = Math.min(ULrotpointul.x, ULrotpointlr.x);
                                this.UpperLeftRectRot.y = Math.min(ULrotpointul.y, ULrotpointlr.y);
                                this.UpperLeftRectRot.w = Math.max(ULrotpointul.x, ULrotpointlr.x);
                                this.UpperLeftRectRot.h = Math.max(ULrotpointul.y, ULrotpointlr.y);
                                var URrotpointul = markupobject.getRotatedMarkup(centercanvX, centercanvY, this.UpperRightRectRot.x, this.UpperRightRectRot.y, CanvRotRad);
                                var URrotpointlr = markupobject.getRotatedMarkup(centercanvX, centercanvY, this.UpperRightRectRot.w, this.UpperRightRectRot.h, CanvRotRad);
                                this.UpperRightRectRot.x = Math.min(URrotpointul.x, URrotpointlr.x);
                                this.UpperRightRectRot.y = Math.min(URrotpointul.y, URrotpointlr.y);
                                this.UpperRightRectRot.w = Math.max(URrotpointul.x, URrotpointlr.x);
                                this.UpperRightRectRot.h = Math.max(URrotpointul.y, URrotpointlr.y);
                                var LLrotpointul = markupobject.getRotatedMarkup(centercanvX, centercanvY, this.LowerLeftRectRot.x, this.LowerLeftRectRot.y, CanvRotRad);
                                var LLrotpointlr = markupobject.getRotatedMarkup(centercanvX, centercanvY, this.LowerLeftRectRot.w, this.LowerLeftRectRot.h, CanvRotRad);
                                this.LowerLeftRectRot.x = Math.min(LLrotpointul.x, LLrotpointlr.x);
                                this.LowerLeftRectRot.y = Math.min(LLrotpointul.y, LLrotpointlr.y);
                                this.LowerLeftRectRot.w = Math.max(LLrotpointul.x, LLrotpointlr.x);
                                this.LowerLeftRectRot.h = Math.max(LLrotpointul.y, LLrotpointlr.y);
                                var LRrotpointul = markupobject.getRotatedMarkup(centercanvX, centercanvY, this.LowerRightRectRot.x, this.LowerRightRectRot.y, CanvRotRad);
                                var LRrotpointlr = markupobject.getRotatedMarkup(centercanvX, centercanvY, this.LowerRightRectRot.w, this.LowerRightRectRot.h, CanvRotRad);
                                this.LowerRightRectRot.x = Math.min(LRrotpointul.x, LRrotpointlr.x);
                                this.LowerRightRectRot.y = Math.min(LRrotpointul.y, LRrotpointlr.y);
                                this.LowerRightRectRot.w = Math.max(LRrotpointul.x, LRrotpointlr.x);
                                this.LowerRightRectRot.h = Math.max(LRrotpointul.y, LRrotpointlr.y);
                            }
                            if (rotate) {
                                if (markupobject.type == 9 && markupobject.subtype == 0) {
                                    if (markupobject.textrotate == 0) {
                                        var rmrotpoint = markupobject.getMarkupSelectPointRot(0, -(-(negheightcenter - 20)), markupobject.rotation);
                                    }
                                    else {
                                        rmrotpoint = markupobject.getMarkupSelectPointRot(0, -(-(negheightcenter - 20)), markupobject.rotation);
                                        rmrotpoint = markupobject.getMarkupSelectPointRot(rmrotpoint.x, rmrotpoint.y, markupobject.textrotate);
                                        var rmcenter = markupobject.getMarkupSelectPointRot((width - x) / 2, negheightcenter, markupobject.textrotate);
                                        var xyrot = markupobject.getMarkupSelectPointRot((width - x) / 2, negheightcenter, markupobject.rotation);
                                        tx = x + rmcenter.x;
                                        ty = y + rmcenter.y;
                                    }
                                }
                                else {
                                    rmrotpoint = markupobject.getMarkupSelectPointRot(0, -(((height - y) / 2) + 20), markupobject.rotation);
                                }
                                this.RotmarkerRectRot.x = tx + rmrotpoint.x - halfmarker;
                                this.RotmarkerRectRot.y = ty + rmrotpoint.y - halfmarker;
                                this.RotmarkerRectRot.w = tx + rmrotpoint.x + halfmarker;
                                this.RotmarkerRectRot.h = ty + rmrotpoint.y + halfmarker;
                                if (DocObj.getCurrentpage().getpageRotation() != 0) {
                                    var rotrotpointul = markupobject.getRotatedMarkup(centercanvX, centercanvY, this.RotmarkerRectRot.x, this.RotmarkerRectRot.y, CanvRotRad);
                                    var rotrotpointlr = markupobject.getRotatedMarkup(centercanvX, centercanvY, this.RotmarkerRectRot.w, this.RotmarkerRectRot.h, CanvRotRad);
                                    this.RotmarkerRectRot.x = Math.min(rotrotpointul.x, rotrotpointlr.x);
                                    this.RotmarkerRectRot.y = Math.min(rotrotpointul.y, rotrotpointlr.y);
                                    this.RotmarkerRectRot.w = Math.max(rotrotpointul.x, rotrotpointlr.x);
                                    this.RotmarkerRectRot.h = Math.max(rotrotpointul.y, rotrotpointlr.y);
                                }
                            }
                        }
                        if (markupobject.type == 9 && markupobject.subtype == 0) {
                            if (markupobject.textrotate == 0) {
                                //markupobject.textrotate
                                this.LowerLeftRect.x = x - halfmarker;
                                this.LowerLeftRect.y = y - halfmarker;
                                this.LowerLeftRect.w = x + halfmarker;
                                this.LowerLeftRect.h = y + halfmarker;
                                this.LowerRightRect.x = width - halfmarker;
                                this.LowerRightRect.y = y - halfmarker;
                                this.LowerRightRect.w = width + halfmarker;
                                this.LowerRightRect.h = y + halfmarker;
                                this.UpperRightRect.x = width - halfmarker;
                                this.UpperRightRect.y = height - halfmarker;
                                this.UpperRightRect.w = width + halfmarker;
                                this.UpperRightRect.h = height + halfmarker;
                                this.UpperLeftRect.x = x - halfmarker;
                                this.UpperLeftRect.y = height - halfmarker;
                                this.UpperLeftRect.w = x + halfmarker;
                                this.UpperLeftRect.h = height + halfmarker;
                            }
                            else {
                                lrrotpoint = markupobject.getMarkupSelectPointRot((width - x), 0, markupobject.textrotate);
                                this.LowerRightRect.x = x + lrrotpoint.x - halfmarker;
                                this.LowerRightRect.y = y + lrrotpoint.y - halfmarker;
                                this.LowerRightRect.w = x + lrrotpoint.x + halfmarker;
                                this.LowerRightRect.h = y + lrrotpoint.y + halfmarker;
                                if (DocObj.getCurrentpage().getpageRotation() != 0) {
                                    //lrrotpoint = markupobject.getMarkupSelectPointRot((this.rotatedrect.w-this.rotatedrect.x), 0,markupobject.textrotate);
                                    this.LowerRightRect.x = this.rotatedrect.w - halfmarker;
                                    this.LowerRightRect.y = this.rotatedrect.y - halfmarker;
                                    this.LowerRightRect.w = this.rotatedrect.w + halfmarker;
                                    this.LowerRightRect.h = this.rotatedrect.y + halfmarker;
                                }
                            }
                        }
                        else {
                            this.UpperLeftRect.x = pointul.x - halfmarker;
                            this.UpperLeftRect.y = pointul.y - halfmarker;
                            this.UpperLeftRect.w = pointul.x + halfmarker;
                            this.UpperLeftRect.h = pointul.y + halfmarker;
                            this.LowerLeftRect.x = pointul.x - halfmarker;
                            this.LowerLeftRect.y = pointlr.y - halfmarker;
                            this.LowerLeftRect.w = pointul.x + halfmarker;
                            this.LowerLeftRect.h = pointlr.y + halfmarker;
                            this.UpperRightRect.x = pointlr.x - halfmarker;
                            this.UpperRightRect.y = pointul.y - halfmarker;
                            this.UpperRightRect.w = pointlr.x + halfmarker;
                            this.UpperRightRect.h = pointul.y + halfmarker;
                            this.LowerRightRect.x = pointlr.x - halfmarker;
                            this.LowerRightRect.y = pointlr.y - halfmarker;
                            this.LowerRightRect.w = pointlr.x + halfmarker;
                            this.LowerRightRect.h = pointlr.y + halfmarker;
                        }
                        ctx.strokeStyle = "blue";
                        ctx.fillStyle = "white";
                        ctx.lineWidth = 1;
                        if (markupobject.type != 10) {


                            if((markupobject.type == 11 || markupobject.type == 20) && (markupobject.subtype == 3 || markupobject.subtype == 5)){
                                //draw rectangle around when selected
                                ctx.lineWidth = 3;
                                ctx.beginPath();
                                
                                ctx.moveTo(x - 3, y - 3);
                                ctx.lineTo(width + 3, y - 3);
                                ctx.lineTo(width + 3, height + 3);
                                ctx.lineTo(x - 3, height + 3);
                                //ctx.lineTo(x - 3, y - 3);
                                
                                ctx.closePath();
                                //ctx.fill();
                                ctx.stroke();
                            }else{
                                ctx.beginPath();
                                ctx.arc(x, y, halfmarker, 0, Math.PI * 2, false);
                                //ctx.closePath();
                                //ctx.fill();
                                //ctx.stroke();
                                //ctx.beginPath();
                                ctx.moveTo(x + halfmarker, height);
                                ctx.arc(x, height, halfmarker, 0, Math.PI * 2, false);
                                //ctx.closePath();
                                //ctx.fill();
                                //ctx.stroke();
                                //ctx.beginPath();
                                ctx.moveTo(width + halfmarker, y);
                                ctx.arc(width, y, halfmarker, 0, Math.PI * 2, false);
                                //ctx.closePath();
                                //ctx.fill();
                                //ctx.stroke();
                                ctx.moveTo(width + halfmarker, height);
                                //ctx.beginPath();
                                ctx.arc(width, height, halfmarker, 0, Math.PI * 2, false);
                                ctx.closePath();
                                ctx.fill();
                                ctx.stroke();
                            }


                        }
                        ctx.strokeStyle = "red";
                        ctx.strokeRect(((x + width) / 2) - 3, y - 3, 6, 6);
                        ctx.strokeRect(x - 3, ((height + y) / 2) - 3, 6, 6);
                        ctx.strokeRect(width - 3, ((height + y) / 2) - 3, 6, 6);
                        ctx.strokeRect(((width + x) / 2) - 3, height - 3, 6, 6);
                        if (rotate) {
                            if (markupobject.type == 9 && markupobject.subtype == 0) {
                                ctx.beginPath();
                                ctx.moveTo((x + width) / 2, height - 3);
                                ctx.lineTo((x + width) / 2, height - 15);
                                ctx.closePath();
                                ctx.stroke();
                                ctx.strokeStyle = "blue";
                                ctx.lineWidth = 1;
                                ctx.beginPath();
                                ctx.arc((x + width) / 2, height - 20, halfmarker, 0, Math.PI * 2, false);
                                ctx.stroke();
                                if (markupobject.textrotate == 0) {
                                    this.RotmarkerRect.x = ((x + width) / 2) - halfmarker;
                                    this.RotmarkerRect.y = (height - 20) - halfmarker;
                                    this.RotmarkerRect.w = ((x + width) / 2) + halfmarker;
                                    this.RotmarkerRect.h = (height - 20) + halfmarker;
                                }
                                else {
                                    rmrotpoint = markupobject.getMarkupSelectPointRot((width - x) / 2, -((y - (height)) + 20), markupobject.textrotate);
                                    this.RotmarkerRect.x = x + rmrotpoint.x - halfmarker;
                                    this.RotmarkerRect.y = y + rmrotpoint.y - halfmarker;
                                    this.RotmarkerRect.w = x + rmrotpoint.x + halfmarker;
                                    this.RotmarkerRect.h = y + rmrotpoint.y + halfmarker;
                                }
                            }
                            else {
                                ctx.beginPath();
                                ctx.moveTo((x + width) / 2, y - 3);
                                ctx.lineTo((x + width) / 2, y - 15);
                                ctx.closePath();
                                ctx.stroke();
                                ctx.strokeStyle = "blue";
                                ctx.lineWidth = 1;
                                ctx.beginPath();
                                ctx.arc((x + width) / 2, y - 20, halfmarker, 0, Math.PI * 2, false);
                                ctx.fill();
                                ctx.stroke();
                                this.RotmarkerRect.x = ((x + width) / 2) - halfmarker;
                                this.RotmarkerRect.y = (y - 20) - halfmarker;
                                this.RotmarkerRect.w = ((x + width) / 2) + halfmarker;
                                this.RotmarkerRect.h = (y - 20) + halfmarker;
                            }
                            if (DocObj.getCurrentpage().getpageRotation() != 0) {
                                var rotpointul = markupobject.getRotatedMarkup(centercanvX, centercanvY, this.RotmarkerRect.x, this.RotmarkerRect.y, CanvRotRad);
                                var rotpointlr = markupobject.getRotatedMarkup(centercanvX, centercanvY, this.RotmarkerRect.w, this.RotmarkerRect.h, CanvRotRad);
                                this.RotmarkerRect.x = Math.min(rotpointul.x, rotpointlr.x);
                                this.RotmarkerRect.y = Math.min(rotpointul.y, rotpointlr.y);
                                this.RotmarkerRect.w = Math.max(rotpointul.x, rotpointlr.x);
                                this.RotmarkerRect.h = Math.max(rotpointul.y, rotpointlr.y);
                            }
                        }
                        //find center
                        /*var centerx = (x+width)/2;
                         var centery = (y+height)/2;
                         //draw circle in center
                         ctx.strokeStyle = "blue";
                         ctx.lineWidth = 1;
                         ctx.beginPath();
                         ctx.arc(centerx, centery, 5, 0, Math.PI * 2,false);
                         ctx.closePath();
                         ctx.stroke();*/
                    }
                    else {
                        ctx.strokeStyle = "blue";
                        ctx.fillStyle = "white";
                        ctx.lineWidth = 1;
                        ctx.beginPath();
                        ctx.arc(x, y, halfmarker, 0, Math.PI * 2, false);
                        ctx.closePath();
                        ctx.fill();
                        ctx.stroke();
                        var btextboxleader = (this.type == 6 && this.subtype == 6);
                        if (!btextboxleader) {
                            ctx.beginPath();
                            ctx.arc(width, height, halfmarker, 0, Math.PI * 2, false);
                            ctx.closePath();
                            ctx.stroke();
                            ctx.fill();
                        }
                    }
                    ctx.restore(); // restore context to what it was on entry
                };
                this.setAnglelengthPoints = function (length, angle) {
                    var point = markupobject.points.length - 2;
                    return markupobject.setAngleLengthinternalPoints(length, angle, true, point);
                };
                this.setWidthHeight = function (w, h) {
                    var scalewidth = w * markupobject.scaling;
                    var scaleheight = h * markupobject.scaling;
                    var widthvalue = getScreenDim(scalewidth);
                    var heighthvalue = getScreenDim(scaleheight);
                    var x2 = widthvalue;
                    var y2 = heighthvalue;
                    return { w: x2, h: y2 };
                };
                this.setWidthHeightEdit = function (w, h) {
                    return markupobject.setWidthHeight(w, h);
                };
                this.setAnglelengthUnscaled = function(point, length, angle, type){
                    
                    return markupobject.setAngleLengthinternalUnscaled(point, length, angle, type, true);

                };
                this.setAnglelength = function (length, angle, type) {
                    //(x2, y2) = (x1 + l * cos(a),y1 + l * sin(a));
                    return markupobject.setAngleLengthinternal(length, angle, type, true);
                };
                this.setAngleLenghtEdit = function (length, angle, type) {
                    return markupobject.setAngleLengthinternal(length, angle, type, false);
                };
                this.setAngleLengthPointsEdit = function (length, angle) {
                    var point = markupobject.editcurpoint;
                    markupobject.bpointEdit = true;
                    return markupobject.setAngleLengthinternalPoints(length, angle, false, point);
                };
                this.setAngleLengthinternalPoints = function (length, angle, brotate, point) {
                    var bpointexist = (point >= 0 && point <= this.points.length - 1);
                    if (!bpointexist) {
                        return;
                    }
                    var bpolyline = (this.type == 1 && (this.subtype == 1 || this.subtype == 3));
                    var centercanvX = (canvasowidth / 2);
                    var centercanvY = (canvasoheight / 2);
                    var CanvRotRad = (360 - DocObj.getCurrentpage().getpageRotation()) * (Math.PI / 180);
                    var CanvRotRadOrig = DocObj.getCurrentpage().getpageRotation() * (Math.PI / 180);
                    if (angle > 0) {
                        angle *= -1;
                    }
                    else {
                        angle = 360 - angle;
                    }
                    if (!brotate) {
                        //add page rotation to draw angle.
                        angle += (360 - DocObj.getCurrentpage().getpageRotation());
                        if (angle > 360) {
                            angle -= 360;
                        }
                    }
                    var scalelength = length * markupobject.scaling;
                    var linewidthvalue = getScreenDim(scalelength);
                    var lineangle = angle * (Math.PI / 180);
                    var origx = markupobject.points[0].x;
                    var origy = markupobject.points[0].y;
                    //use points
                    var x2 = markupobject.points[point].x + (linewidthvalue * Math.cos(lineangle));
                    var y2 = markupobject.points[point].y + (linewidthvalue * Math.sin(lineangle));
                    markupobject.bUsemouseinput = false;
                    if (DocObj.getCurrentpage().getpageRotation() != 0 && brotate) {
                        //use points
                        var transpoint = markupobject.setinitRotatedMarkupPoint(centercanvX, centercanvY, markupobject.points[point].x, markupobject.points[point].y, CanvRotRad);
                        markupobject.points[point].x = transpoint.x;
                        markupobject.points[point].y = transpoint.y;
                        //use points
                        transpoint = markupobject.setinitRotatedMarkupPoint(centercanvX, centercanvY, x2, y2, CanvRotRad);
                        x2 = transpoint.x;
                        y2 = transpoint.y;
                        //add document page rotation to markup rotation here.
                    }
                    if (point == markupobject.points.length - 1) {
                        markupobject.editcurpoint = 0;
                        if (bpolyline) {
                            x2 = origx;
                            y2 = origy;
                        }
                    }
                    else {
                        markupobject.editcurpoint = point + 1;
                    }
                    return { w: x2, h: y2, p: markupobject.editcurpoint };
                };
                this.setAngleLengthinternalUnscaled = function(point, length, angle, type, brotate){
                    var centercanvX = (canvasowidth / 2);
                    var centercanvY = (canvasoheight / 2);
                    var CanvRotRad = (360 - DocObj.getCurrentpage().getpageRotation()) * (Math.PI / 180);
                    var CanvRotRadOrig = DocObj.getCurrentpage().getpageRotation() * (Math.PI / 180);
                    if (angle > 0) {
                        angle *= -1;
                    }
                    else {
                        angle = 360 - angle;
                    }
                    if (!brotate) {
                        //add page rotation to draw angle.
                        angle += (360 - DocObj.getCurrentpage().getpageRotation());
                        if (angle > 360) {
                            angle -= 360;
                        }
                        //console.log('brotate');
                    }

                    var lineangle = angle * (Math.PI / 180);
                    var x2 = point.x + (length * Math.cos(lineangle));
                    var y2 = point.y + (length * Math.sin(lineangle));

                    /*if (DocObj.getCurrentpage().getpageRotation() != 0 && brotate) {
                        var transpoint = markupobject.setinitRotatedMarkupPoint(centercanvX, centercanvY, point.x, point.y, CanvRotRad);
                        point.x = transpoint.x;
                        point.y = transpoint.y;
                        transpoint = markupobject.setinitRotatedMarkupPoint(centercanvX, centercanvY, x2, y2, CanvRotRad);
                        x2 = transpoint.x;
                        y2 = transpoint.y;
                        //add document page rotation to markup rotation here.
                    }*/

                    return { w: x2, h: y2 };
                };
                this.setAngleLengthinternal = function (length, angle, type, brotate) {
                    var centercanvX = (canvasowidth / 2);
                    var centercanvY = (canvasoheight / 2);
                    var CanvRotRad = (360 - DocObj.getCurrentpage().getpageRotation()) * (Math.PI / 180);
                    var CanvRotRadOrig = DocObj.getCurrentpage().getpageRotation() * (Math.PI / 180);
                    if (angle > 0) {
                        angle *= -1;
                    }
                    else {
                        angle = 360 - angle;
                    }
                    if (!brotate) {
                        //add page rotation to draw angle.
                        angle += (360 - DocObj.getCurrentpage().getpageRotation());
                        if (angle > 360) {
                            angle -= 360;
                        }
                    }
                    var scalelength = length * markupobject.scaling;
                    var linewidthvalue = getScreenDim(scalelength, type);
                    var lineangle = angle * (Math.PI / 180);
                    var x2 = this.x + (linewidthvalue * Math.cos(lineangle));
                    var y2 = this.y + (linewidthvalue * Math.sin(lineangle));
                    //this.AdjustForRotation(false);
                    markupobject.bUsemouseinput = false;
                    if (DocObj.getCurrentpage().getpageRotation() != 0 && brotate) {
                        var transpoint = markupobject.setinitRotatedMarkupPoint(centercanvX, centercanvY, this.x, this.y, CanvRotRad);
                        this.x = transpoint.x;
                        this.y = transpoint.y;
                        transpoint = markupobject.setinitRotatedMarkupPoint(centercanvX, centercanvY, x2, y2, CanvRotRad);
                        x2 = transpoint.x;
                        y2 = transpoint.y;
                        //add document page rotation to markup rotation here.
                    }
                    return { w: x2, h: y2 };
                };
                this.lengthangleCallbackSelect = function () {
                    if (markupobject.anglelengthsupport) {
                        markupobject.lengthangleCallback(this.x, this.y, this.w, this.h, DocObj.getCurrentpage().getpageRotation());
                    }
                    if (markupobject.setRadiusSupport) {
                        markupobject.radiusCallback(this.x, this.y, this.w, this.h, true);
                    }
                    if (markupobject.setPointSupport) {
                        markupobject.lengthangleCallbackPointSelect();
                    }
                    if (markupobject.widthHeightsupport) {
                        markupobject.widthHeightCallback(this.w, this.h);
                    }
                };
                this.lengthangleCallbackPointSelect = function () {
                    if (this.points.length < 2) {
                        return;
                    }
                    var drotation = DocObj.getCurrentpage().getpageRotation();
                    if (markupobject.editcurpoint == 0) {
                        //var curpoint = 0;
                        var prevpoint = markupobject.points.length - 1;
                        //markupobject.points.length - 1
                        //x2 = origx;
                        //y2 = origy;
                    }
                    else {
                        //markupobject.editcurpoint = point + 1;
                        prevpoint = markupobject.editcurpoint - 1;
                    }
                    if (markupobject.editcurpoint == markupobject.points.length - 1) {
                        var nextpoint = 0;
                    }
                    else {
                        nextpoint = markupobject.editcurpoint + 1;
                    }
                    var x = this.points[markupobject.editcurpoint].x;
                    var y = this.points[markupobject.editcurpoint].y;
                    var w = this.points[nextpoint].x;
                    var h = this.points[nextpoint].y;
                    //var x = this.points[prevpoint].x;
                    //var y = this.points[prevpoint].y;
                    //var w = this.points[markupobject.editcurpoint].x;
                    //var h = this.points[markupobject.editcurpoint].y;
                    //var w = this.points[prevpoint].x;
                    //var h = this.points[prevpoint].y;
                    var lengthangle = markupobject.anglelength(x, y, w, h, false);
                    lengthangle.length = getUnitlength(lengthangle.length / markupobject.scaling);
                    if (RxCore_GUI_markupdrawParams != undefined) {
                        if (!markupobject.bUsemouseinput) {
                            lengthangle.angle -= drotation;
                        }
                        RxCore_GUI_markupdrawParams.onDrawEvent(lengthangle);
                    }
                };
                this.lengthangleCallbackPoints = function (drotation) {
                    if (this.points.length < 2) {
                        return;
                    }
                    var x = this.points[this.points.length - 2].x;
                    var y = this.points[this.points.length - 2].y;
                    var w = this.points[this.points.length - 1].x;
                    var h = this.points[this.points.length - 1].y;
                    //var x = this.points[0].x;
                    //var y = this.points[0].y;
                    //var w = this.points[1].x;
                    //var h = this.points[1].y;
                    var lengthangle = markupobject.anglelength(x, y, w, h, false);
                    lengthangle.length = getUnitlength(lengthangle.length / markupobject.scaling);
                    if (RxCore_GUI_markupdrawParams != undefined) {
                        if (!markupobject.bUsemouseinput) {
                            lengthangle.angle -= drotation;
                        }
                        RxCore_GUI_markupdrawParams.onDrawEvent(lengthangle);
                    }
                };
                this.widthHeightCallback = function (w, h) {
                    var lengthangle = { width: w, height: h };
                    lengthangle.width = getUnitlength(w / markupobject.scaling);
                    lengthangle.height = getUnitlength(h / markupobject.scaling);
                    if (RxCore_GUI_markupdrawParams != undefined) {
                        RxCore_GUI_markupdrawParams.onDrawEvent(lengthangle);
                    }
                };
                this.lengthangleCallback = function (x, y, w, h, drotation) {
                    var lengthangle = markupobject.anglelength(x, y, w, h, false);
                    lengthangle.length = getUnitlength(lengthangle.length / markupobject.scaling);
                    if (RxCore_GUI_markupdrawParams != undefined) {
                        if (!markupobject.bUsemouseinput) {
                            lengthangle.angle -= drotation;
                        }
                        RxCore_GUI_markupdrawParams.onDrawEvent(lengthangle);
                    }
                };
                this.radiusCallback = function (x, y, w, h, useabs) {
                    var lengthangle = markupobject.anglelength(x, y, w, h, useabs);
                    lengthangle.length = getUnitlength(lengthangle.length / markupobject.scaling);
                    if (RxCore_GUI_markupdrawParams != undefined) {
                        RxCore_GUI_markupdrawParams.onDrawEvent(lengthangle);
                    }
                };
                this.anglelength = function (startx, starty, endx, endy, useabs) {
                    var width = 0;
                    var height = 0;
                    width = Math.abs(endx - startx);
                    height = Math.abs(endy - starty);
                    var dimwsq = Math.pow(width, 2);
                    var dimhsq = Math.pow(height, 2);
                    var dimdiag = Math.sqrt((dimwsq + dimhsq));
                    if (useabs) {
                        width = Math.abs(endx - startx);
                        height = Math.abs(endy - starty);
                    }
                    else {
                        width = endx - startx;
                        height = endy - starty;
                    }
                    var lineangle = Math.atan2(height, width) * (180 / Math.PI);
                    if (lineangle < 0) {
                        lineangle *= -1;
                    }
                    else {
                        lineangle = 360 - lineangle;
                    }
                    return {
                        length: dimdiag,
                        angle: lineangle
                    };
                };
                this.drawme = function (ctx) {

                    var scalefactor = getCurrentPageScale();
                    
                    bFirstdrawn = true;
                    
                    markupobject.fixedscaleFactor = scalefactor / DocObj.getCurrentpage().fixedScale;

                    if (markupobject.bUseFixedScale) {
                        var linewidthlocal = this.linewidth * markupobject.fixedscaleFactor;
                    }
                    else {
                        linewidthlocal = this.linewidth;
                    }
                    if (markupobject.bUseFixedScale) {
                        var arrowlengthlocal = this.arrowlength * markupobject.fixedscaleFactor;
                    }
                    else {
                        arrowlengthlocal = this.arrowlength;
                    }

                    if(bUseNoScale){
                        linewidthlocal = this.linewidth;
                        arrowlengthlocal = this.arrowlength;
                    }
                    /*if (DocObj.getCurrentpage().getpageRotation() != 0) {
                        var txcanv = (canvasowidth / 2);
                        var tycanv = (canvasoheight / 2);
                        ctx.save();
                        ctx.translate(txcanv, tycanv);
                        ctx.rotate((360 - DocObj.getCurrentpage().getpageRotation()) * (Math.PI / 180));
                        ctx.translate(-txcanv, -tycanv);
        
                    }*/
                    switch (this.type) {
                        case 0: //pencil
                            /*ctx.save();
                            if (this.subtype == 1) {
                                if (DocObj.backgroundColor != undefined) {
                                    ctx.strokeStyle = DocObj.backgroundColor;
                                }
                                else {
                                    ctx.strokeStyle = "white";
                                }
                                ctx.lineWidth = linewidthlocal * 10;
                            }
                            else {
                                ctx.lineWidth = linewidthlocal;
                                ctx.strokeStyle = this.strokecolor;
                                this.GetLinestyle(markupobject.linestyle, ctx, 1);
                            }
                            ctx.lineTo(this.points[this.points.length - 1].x, this.points[this.points.length - 1].y);
                            ctx.stroke();
                            ctx.restore();*/

                            this.drawmescaled(ctx);
                            break;
                        case 1: //polygon
                            /*switch (this.subtype){
                                case 3:
                                    //markupobject.findrectangle();
                                    break;
        
                            }*/
                            var coords = markupobject.setDimDrawme();
                            this.setlastpoint(coords.w, coords.h);
                            if (markupobject.bUsemouseinput) {
                                this.AdjustForRotation(false);
                            }

                            if (bOrthoOn) {

                                var curpoint = markupobject.getlastpoint();    
                                var prevpoint = markupobject.getprevpoint();

                                if (curpoint && prevpoint){
                                    var snappoint = markupobject.calculateOrthoDraw({x: prevpoint.x, y: prevpoint.y}, { x: curpoint.x, y: curpoint.y });
                                    markupobject.setlastpoint(snappoint.w, snappoint.h);

                                }    

                            }

                            markupobject.lengthangleCallbackPoints(DocObj.getCurrentpage().getpageRotation());
                            this.drawmescaled(ctx);
                            break;
                        case 2:
                            switch (this.subtype) {
                                case 1:
                                    markupobject.polycurves(ctx, markupobject.linewidth, markupobject.color, 1);
                                    break;
                            }
                            break;
                        case 3: //rectangle

                            var coords = markupobject.setDimDrawme();
                            
                            if(this.subtype == 6){

                                //this.x = coords.x;
                                //this.y = coords.y;
                                this.w = coords.w;
                                this.h = coords.h;

                            }

                            /*if (markupobject.bUsemouseinput) {
                                this.AdjustForRotation(false);
                            }*/

                            this.xscaled = this.x;
                            this.yscaled = this.y;
                            this.wscaled = this.w;
                            this.hscaled = this.h;
                            
                            this.AdjustForRotation(false);

                            markupobject.widthHeightCallback(this.w, this.h);
                            this.drawmescaled(ctx);
                            break;
                        case 4: //oval
                            this.xscaled = this.x;
                            this.yscaled = this.y;
                            this.wscaled = this.w;
                            this.hscaled = this.h;
                            this.AdjustForRotation(false);
                            if (this.subtype == 1) {
                                markupobject.radiusCallback(this.x, this.y, this.w, this.h, true);
                            }
                            this.drawmescaled(ctx);
                            break;
                        case 5: //cloud//(ctx,x,y,width,height,radius,linewidth,fill,stroke,fillcolor,strokecolor)
                            this.xscaled = this.x;
                            this.yscaled = this.y;
                            this.wscaled = this.w;
                            this.hscaled = this.h;
                            this.AdjustForRotation(false);
                            this.drawmescaled(ctx);
                            break;
                        case 6: //line, arrow
                            /*ctx.save();
                            markupobject.GetLinestyle(markupobject.linestyle,ctx,1);
                            var coords = markupobject.setDimDrawme();
                            markupobject.arrow(ctx, coords.x, coords.y, this.w, this.h, arrowlengthlocal, arrowangle, linewidthlocal, this.subtype, markupobject.strokecolor, markupobject.strokecolor);
                            ctx.restore();*/


                            /*if (bOrthoOn) {
                                markupobject.calculateOrtho(false, 0, { x: 0, y: 0 });
                            }*/


                            var coords = markupobject.setDimDrawme();
                            this.w = coords.w;
                            this.h = coords.h;

                            if (markupobject.bUsemouseinput) {
                                this.AdjustForRotation(false);
                            }

                            if (bOrthoOn) {
                                //markupobject.calculateOrtho(false, 0, { x: 0, y: 0 });

                                var curpoint = markupobject.calculateOrthoDraw({x: this.x, y: this.y}, { x: this.w, y: this.h });
                                this.w = curpoint.w;
                                this.h = curpoint.h;


                            }

                            this.xscaled = this.x;
                            this.yscaled = this.y;
                            this.wscaled = this.w;
                            this.hscaled = this.h;

                            markupobject.lengthangleCallback(this.x, this.y, this.w, this.h, DocObj.getCurrentpage().getpageRotation());
                            /*var lengthangle = markupobject.anglelength(this.xscaled, this.yscaled, this.wscaled, this.hscaled, false);
                            lengthangle.length = getUnitlength(lengthangle.length / markupobject.scaling);
        
                            if (RxCore_GUI_markupdrawParams != undefined){
                                if(!markupobject.bUsemouseinput){
                                    lengthangle.angle -= DocObj.getCurrentpage().getpageRotation();
                                }
                                RxCore_GUI_markupdrawParams.onDrawEvent(lengthangle);
                            }*/

                            this.setTextboxConnectionPointDraw();

                            this.drawmescaled(ctx);
                            break;
                        case 7: //dimension line
                            
                            var coords = markupobject.setDimDrawme();
                            this.w = coords.w;
                            this.h = coords.h;

                            if (markupobject.bUsemouseinput) {
                                this.AdjustForRotation(false);
                            }

                            if (bOrthoOn) {

                                var curpoint = markupobject.calculateOrthoDraw({x: this.x, y: this.y}, { x: this.w, y: this.h });
                                this.w = curpoint.w;
                                this.h = curpoint.h;
    

                            }

                        
                            /*if (bOrthoOn) {
                                markupobject.calculateOrtho(false, 0, { x: 0, y: 0 });
                            }*/
                            /*ctx.save();
                            markupobject.GetLinestyle(markupobject.linestyle,ctx,1);
                            var dimtype = this.subtype + 4;
                            var coords = markupobject.setDimDrawme();
                            markupobject.arrow(ctx, coords.x, coords.y, this.w, this.h, arrowlengthlocal, arrowangle, linewidthlocal, dimtype, markupobject.strokecolor, markupobject.strokecolor);
                            ctx.restore();
                            markupobject.setdimvalue(this.x, this.y, this.w, this.h);
                            if(bUseFixedScale){
                                var localscale = markupobject.fixedscaleFactor;
                            }else{
                                localscale = 1;
                            }
                            
                            markupobject.dimvaluedraw(ctx, coords.x, coords.y, coords.w, coords.h, this.strokecolor, localscale,true);*/
                            this.xscaled = this.x;
                            this.yscaled = this.y;
                            this.wscaled = this.w;
                            this.hscaled = this.h;

                            markupobject.lengthangleCallback(this.x, this.y, this.w, this.h, DocObj.getCurrentpage().getpageRotation());
                            this.drawmescaled(ctx);
                            break;
                        case 8:

                            var coords = markupobject.setDimDrawme();
                            this.setlastpoint(coords.w, coords.h);

                            if (markupobject.bUsemouseinput) {
                                this.AdjustForRotation(false);
                            }

                            if (bOrthoOn) {

                                var curpoint = markupobject.getlastpoint();    
                                var prevpoint = markupobject.getprevpoint();

                                if (curpoint && prevpoint){
                                    var snappoint = markupobject.calculateOrthoDraw({x: prevpoint.x, y: prevpoint.y}, { x: curpoint.x, y: curpoint.y });
                                    markupobject.setlastpoint(snappoint.w, snappoint.h);

                                }    


                            }

                            markupobject.lengthangleCallbackPoints(DocObj.getCurrentpage().getpageRotation());
                            this.drawmescaled(ctx);
                            break;
                        case 9: //text markup
                            ctx.font = this.font.fontstring;
                            var dim = ctx.measureText(this.text);
                            this.textwidth = dim.width;
                            this.w = this.textwidth;
                            this.h = this.textheight;
                            ctx.textAlign = "start";
                            //cursor
                            //drawing of text goes here.
                            ctx.fillStyle = markupobject.fillcolor;
                            //ctx.font = this.textheight + "pt Helvetica";
                            if (this.subtype == 1 || this.subtype == 2) {
                                markupobject.Rect(ctx, this.x, this.y, this.w, this.h, this.linewidth, true, true, markupobject.fillcolor, markupobject.strokecolor);
                                ctx.fillText(this.text, this.x + 10, this.y + this.h + 10);
                            }
                            else {
                                //fontstylevalue.value;
                                ctx.fillText(this.text, this.x, this.y);
                                this.xscaled = this.x;
                                this.yscaled = this.y;
                                this.wscaled = this.w;
                                this.hscaled = this.h;
                            }
                            break;
                        case 10:

                            if(bnoteimageloaded){

                                this.w = noteimage.width;
                                this.h = noteimage.height;

                                if(markupobject.bUseFixedScale){
                                    ctx.drawImage(noteimage, this.x, this.y, this.w * markupobject.fixedscaleFactor, this.h * markupobject.fixedscaleFactor);
                                }else{
                                    ctx.drawImage(noteimage, this.x, this.y, this.w, this.h);
                                }
    

                                this.xscaled = this.x;
                                this.yscaled = this.y;
                                this.wscaled = this.w;
                                this.hscaled = this.h;
                            }
                            break;
                        case 11:
                            this.w = this.image.width;
                            this.h = this.image.height;
                            if (markupobject.imageloaded) {
                                ctx.drawImage(this.image, this.x, this.y, this.w, this.h);
                            }
                            this.xscaled = this.x;
                            this.yscaled = this.y;
                            this.wscaled = this.w;
                            this.hscaled = this.h;
                            break;


                            /* case 20:this.w = this.image.width;  also link type 20
                            this.h = this.image.height;
                            if (markupobject.imageloaded) {
                                ctx.drawImage(this.image, this.x, this.y, this.w, this.h);
                            }
                            this.xscaled = this.x;
                            this.yscaled = this.y;
                            this.wscaled = this.w;
                            this.hscaled = this.h;
                            break;*/



                        case 12:
                            this.xscaled = this.x;
                            this.yscaled = this.y;
                            this.wscaled = this.w;
                            this.hscaled = this.h;
                            this.AdjustForRotation(false);
                            this.drawmescaled(ctx);
                            break;
                        case 13:

                            //this.findrectangle();

                            this.AdjustForRotation(false);

                            this.drawmescaled(ctx);

                            break;

                        case 14:

                            var coords = markupobject.setDimDrawme();
                            this.endX = coords.w;
                            this.endY = coords.h;


                            this.AdjustForRotation(false);

                            this.drawmescaled(ctx);

                            break;
                    


                    }
                    /*if (DocObj.getCurrentpage().getpageRotation() != 0) {
                        ctx.restore();
                    }*/
                };

                this.isdrawn = function(){
                    return bFirstdrawn;
                };

                this.offscreendrawdim = function(pageobject){

                    //bRasterScale = getCurrentPagerasterScale();


                    if(bFirstdrawn){
                        return;
                    }

                    bRasterScale = getPagerasterScale(pageobject);
                    bRasterScale = false;

                    var scalefactor = getPageScale(pageobject);

                    //var scalefactor = getCurrentPageScale();
                    //var fixedscale = getCurrentPageFixedScale();

                    var fixedscale = getPageFixedScale(pageobject);

                    //var pagescalefactor = scalefactor;
                    //var bvectorloaded = getCurrentPageVectorloaded();


                    

                    markupobject.setFirstdrawDim(scalefactor, fixedscale, bRasterScale);
                    bFirstdrawn = true;


                };

                this.getrect = function(){


                    //rect: {top: 2216.989990234375, right: 870.9220581054688, bottom: 2069.130126953125, left: 622.7640380859375}

                    //return markupobject.select(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, true, false);


                    var curpage = DocObj.getPageObject(this.pagenumber);

                    var pdfx = (this.x - curpage.dxpdf) / (curpage.curpagescale * curpage.dscalepdf); 
                    var pdfy = -(((this.y - curpage.dypdf) / (curpage.curpagescale * curpage.dscalepdf)) - curpage.pdfpageheight);


                    var pdfright = (this.w - curpage.dxpdf) / (curpage.curpagescale * curpage.dscalepdf);
                    var pdfbottom = (((curpage.pdfpageheight - this.h) * (curpage.curpagescale * curpage.dscalepdf)) + curpage.dypdf);

                    //var x = ((SearcMatches[sm][pm].match.rectArray[0].left ) + thispage.dxpdf);
                    //var y = ((SearcMatches[sm][pm].match.rectArray[0].top ) + thispage.dypdf);
                    //var right = ((SearcMatches[sm][pm].match.rectArray[0].right ) + thispage.dxpdf);
                    //var bottom = ((SearcMatches[sm][pm].match.rectArray[0].bottom ) + thispage.dypdf);
                    var width = pdfright - pdfx;
                    var height = pdfbottom - pdfy;

                    if(this.type == 0) {
                        var rect = {top: pdfy, right: width, bottom: height, left: pdfx};
                    }else if(this.type == 9){

                        var width = this.w / (curpage.curpagescale * curpage.dscalepdf);
                        var height = this.h / (curpage.curpagescale * curpage.dscalepdf);
    
                        pdfright = pdfx + width; 
                        pdfbottom = pdfy + height;
    
                        rect = {top: pdfy, right: pdfright, bottom: pdfbottom, left: pdfx};

                    }

                    return rect;

                };
                this.getinklist = function(){
                    
                    var counter = 0;
                    var lcounter = 0;

                    var xscalepoint = 0;
                    var yscalepoint = 0;

                    var inklist = [];
                    
                    if (this.type == 0){

                        for (lcounter = 0; lcounter < this.pointlist.length; lcounter++) {
                            for (counter = 0; counter < this.pointlist[lcounter].length; counter++) {

                                
                                
                                /*xscalepoint = (this.pointlist[lcounter][counter].x - this.xoffset) * scalefactor;
                                yscalepoint = (this.pointlist[lcounter][counter].y - this.yoffset) * scalefactor;
                                //xscalepoint = (this.points[counter].x - this.xoffset) * scalefactor;
                                //yscalepoint = (this.points[counter].y - this.yoffset) * scalefactor;
                                xscalepoint += pagedx;
                                yscalepoint += pagedy;*/

                                //var curpage = DocObj.getCurrentpage();
                                var curpage = DocObj.getPageObject(this.pagenumber);

                                if(curpage.getpageRotation() == 0){
                                    //SearcMatches[sm][pm].match.rectArray[0].left = (x - thispage.dxpdf) / (thispage.curpagescale * thispage.dscalepdf); 
                                    var pdfx = (this.pointlist[lcounter][counter].x - curpage.dxpdf) / (curpage.curpagescale * curpage.dscalepdf); 
                                    var pdfy = -(((this.pointlist[lcounter][counter].y - curpage.dypdf) / (curpage.curpagescale * curpage.dscalepdf)) - curpage.pdfpageheight);
                                    
                                }else{
                                    // calculate rotated point.
                                    var pdfx = (this.pointlist[lcounter][counter].x - curpage.dxpdf) / (curpage.curpagescale * curpage.dscalepdf); 
                                    var pdfy = -(((this.pointlist[lcounter][counter].y - curpage.dypdf) / (curpage.curpagescale * curpage.dscalepdf)) - curpage.pdfpageheight);


                                }

                                if (counter == 0) {
                                    //ctx.moveTo(xscalepoint, yscalepoint);
                                    //0: {x: 655.1089477539062, y: 2213.090087890625, type: 1}
                                    inklist.push({x: pdfx, y: pdfy, type: 1}); 
                                }
                                else {
                                    //ctx.lineTo(xscalepoint, yscalepoint);
                                    //0: {x: 655.1089477539062, y: 2213.090087890625, type: 2}
                                    inklist.push({x: pdfx, y: pdfy, type: 2}); 
                                }

                            }
                        }

                    }
                    
                    return inklist;

                };

                this.getpagexy = function(){
                    var pagedx = 0;
                    var pagedy = 0;
                    
                    if (DocObj.Type == 0) {
                        var docdx = DocObj.pages[0].dx;
                    }
                    else {
                        docdx = DocObj.getCurrentpage().dx;
                    }
                    if (DocObj.getCurrentpage().usevectorxml) {
                        pagedx = DocObj.getCurrentpage().dxvector;
                        pagedy = DocObj.getCurrentpage().dyvector;
                    }
                    else if (DocObj.getCurrentpage().usepdfjs) {
                        if (DocObj.foxitdoc != null) {
                            pagedx = DocObj.getCurrentpage().foxitdx;
                            pagedy = DocObj.getCurrentpage().foxitdy;
                            //get foxitpage position
                        }
                        else {
                            pagedx = DocObj.getCurrentpage().dxpdf;
                            pagedy = DocObj.getCurrentpage().dypdf;
                        }
                    }
                    else {
                        pagedx = docdx;
                        pagedy = DocObj.getCurrentpage().dy;
                    }

                    return {pagedx: pagedx, pagedy: pagedy};

                };

                this.setDrawdim = function(){

                    bRasterScale = getCurrentPagerasterScale();

                    bRasterScale = false;

                    var fixedscale = getCurrentPageFixedScale();
                    var scalefactor = getCurrentPageScale();

                    markupobject.setFirstdrawDim(scalefactor, fixedscale, bRasterScale);
                    bFirstdrawn = true;


                };

                

                this.drawmescaled = function (ctx) {

                    /*if(bcnvRotated){
                        return;
                    }*/
                    
                    bRasterScale = getCurrentPagerasterScale();

                    bRasterScale = false;

                    var fixedscale = getCurrentPageFixedScale(markupobject.pagenumber);
                    var scalefactor = getCurrentPageScale(markupobject.pagenumber);
                    var loadscalefactor = scalefactor;



                    var bvectorloaded = getCurrentPageVectorloaded();

                    var pagerotation = DocObj.getpage(markupobject.pagenumber).getpageRotation();

                    var ballowedrots = (pagerotation == 0 || pagerotation == 90 || pagerotation == 180 || pagerotation == 270);

                    if(!ballowedrots){
                        return;
                    }

                    //var pagerotation = DocObj.getCurrentpage().getpageRotation();

                    var bisfoxitPDF = isfoxitPDF();



                    if(markupobject.noDraw){
                        return;
                    }

                    if(!bFirstdrawn && bvectorloaded){
                        markupobject.setFirstdrawDim(scalefactor, fixedscale, bRasterScale);
                        bFirstdrawn = true;

                    }

                    //var fixedscale = DocObj.getCurrentpage().fixedScale;
                    markupobject.fixedscaleFactor = scalefactor / fixedscale;
                    scalefactor /= markupobject.scaling;
                    loadscalefactor /= markupobject.loadscaling;

                   var pagescalefactor = scalefactor;
                    
                    var pagedx = 0;
                    var pagedy = 0;

                    var pageoffset = getCurrentPageOffset(markupobject.pagenumber);

                    if(DocObj.currentpage != markupobject.pagenumber && pageoffset.y == 0){
                        return;
                    }

                    pagedx = pageoffset.x;
                    pagedy = pageoffset.y;


                    if(markupobject.bfor3D){
                        scalefactor = canvasoheight / this.pageheight;

                    }

                    //var pagedx = markupobject.getpagexy().pagedx;
                    //var pagedy = markupobject.getpagexy().pagedy;

                    //pagedx = markupobject.getpagexy().pagedx; 
                    //pagedy = markupobject.getpagexy().pagedy; 

                    /*if (DocObj.Type == 0) {
                        var docdx = DocObj.pages[0].dx;
                    }
                    else {
                        docdx = DocObj.getCurrentpage().dx;
                    }
                    if (DocObj.getCurrentpage().usevectorxml) {
                        pagedx = DocObj.getCurrentpage().dxvector;
                        pagedy = DocObj.getCurrentpage().dyvector;
                    }
                    else if (DocObj.getCurrentpage().usepdfjs) {
                        if (DocObj.foxitdoc != null) {
                            pagedx = DocObj.getCurrentpage().foxitdx;
                            pagedy = DocObj.getCurrentpage().foxitdy;
                            //get foxitpage position
                        }
                        else {
                            pagedx = DocObj.getCurrentpage().dxpdf;
                            pagedy = DocObj.getCurrentpage().dypdf;
                        }
                    }
                    else {
                        pagedx = docdx;
                        pagedy = DocObj.getCurrentpage().dy;
                    }*/
                    //console.log(pagedx);
                    //console.log(pagedy);
                    var xscalepoint = 0;
                    var yscalepoint = 0;
                    if (markupobject.bUseFixedScale) {
                        var arrowlengthscaled = this.arrowlength * markupobject.fixedscaleFactor;
                    }
                    else {
                        arrowlengthscaled = this.arrowlength * loadscalefactor;
                    }
                    if (markupobject.bUseFixedScale) {
                        var radiusScaled = 10 * markupobject.fixedscaleFactor;
                    }
                    else {
                        radiusScaled = 10 * scalefactor;
                    }
                    var textoffsetScaled = (this.h / 10) * scalefactor;
                    if (markupobject.bUseFixedScale) {
                        var linewidthScaled = this.linewidth * markupobject.fixedscaleFactor;
                        var crossl = 5 * markupobject.fixedscaleFactor;
                    }
                    else {
                        linewidthScaled = this.linewidth * loadscalefactor;
                        crossl = 5 * scalefactor;
                    }

                    var leaderOffsetScalded = this.leaderoffset * scalefactor;
                    var tx = 0;
                    var ty = 0;


                    if(bUseNoScale){
                        arrowlengthscaled = this.arrowlength;
                        linewidthScaled = this.linewidth;
                        radiusScaled = 10;
                        //leaderOffsetScalded = this.leaderoffset
                    }


                    var ptrn = null;

                    if(markupobject.hatchStyle != undefined){
                        updateHatchColor(markupobject.hatchStyle, this.color);
                        ptrn = getHatchbyName(ctx, getHatchNameforType(markupobject.hatchStyle));
                    }

                    //var ptrn = getHatch(ctx, markupobject.hatchStyle, this.color);
                    //console.log("pagerot",pagerotation);

                    

                    if (pagerotation != 0) {
                        var txcanv = (canvasowidth / 2);
                        var tycanv = (canvasoheight / 2);

                        //pixelscale

                        var pixelRatio = getPixelRatio();

                        if(bisfoxitPDF){

                            //w = canvasowidthsize;
                            //h = canvasoheightsize;
    
                            txcanv *= pixelRatio;  //(canvasowidthsize / 2);
                            tycanv *= pixelRatio;
    
                        }

                        ctx.save();
                        ctx.translate(txcanv, tycanv);
                        ctx.rotate(pagerotation * (Math.PI / 180));
                        ctx.translate(-txcanv, -tycanv);
                        bcnvRotated = true;

                    }
                    switch (this.type) {
                        case 0: //pencil, marker, eraser

                            if (markupobject.bUseFixedScale) {
                                linewidthScaled = this.linewidth * markupobject.fixedscaleFactor;
                            }
                            else {
                                linewidthScaled = this.linewidth * scalefactor;
                            }


                            var counter = 0;
                            var lcounter = 0;
                            markupobject.SetDimensions(scalefactor, pagerotation, pagedx, pagedy);
                            /*xscalepoint = this.pointlist[0][0].x - this.xoffset * scalefactor;
                            yscalepoint = this.pointlist[0][0].y - this.yoffset * scalefactor;
        
                            //xscalepoint = (this.points[0].x - this.xoffset) * scalefactor;
                            //yscalepoint = (this.points[0].y - this.yoffset) * scalefactor;
        
                            if (DocObj.getCurrentpage().usevectorxml) {
                                xscalepoint += DocObj.getCurrentpage().dxvector;
                                yscalepoint += DocObj.getCurrentpage().dyvector;
                            } else if (DocObj.getCurrentpage().usepdfjs) {
        
                                xscalepoint += DocObj.getCurrentpage().dxpdf;
                                yscalepoint += DocObj.getCurrentpage().dypdf;
        
                            } else {
                                xscalepoint += DocObj.getCurrentpage().dx;
                                yscalepoint += DocObj.getCurrentpage().dy;
        
                            }*/

                            if(!markupobject.pdfannotproxy){

                                ctx.save();
                                if (this.subtype == 1) {

        
                                    if (DocObj.backgroundColor != undefined) {
                                        ctx.strokeStyle = DocObj.backgroundColor;
                                    }
                                    else {
                                        ctx.strokeStyle = "white";
                                    }
                                    ctx.lineWidth = linewidthScaled * 10;
                                }
                                else {
                                    ctx.strokeStyle = this.strokecolor;
                                    ctx.lineWidth = linewidthScaled;
                                    markupobject.GetLinestyle(markupobject.linestyle, ctx, markupobject.fixedscaleFactor);
                                }
                                
                                //ctx.transform(markupobject.transformobj.a, markupobject.transformobj.b, markupobject.transformobj.c, markupobject.transformobj.d, markupobject.transformobj.e, markupobject.transformobj.f);

                                //{a : 1, b : 0, c : 0, d : 1, e : 0, f : 0}


                                ctx.beginPath();
                                if(this.points.length > 0){

                                    for (counter = 0; counter < this.points.length; counter++) {
                                        xscalepoint = (this.points[counter].x - this.xoffset) * scalefactor;
                                        yscalepoint = (this.points[counter].y - this.yoffset) * scalefactor;
                                        //xscalepoint = (this.points[counter].x - this.xoffset) * scalefactor;
                                        //yscalepoint = (this.points[counter].y - this.yoffset) * scalefactor;
                                        xscalepoint += pagedx;
                                        yscalepoint += pagedy;

                                        if (counter == 0) {
                                            ctx.moveTo(xscalepoint, yscalepoint);
                                        }
                                        else {
                                            ctx.lineTo(xscalepoint, yscalepoint);
                                        }
                                    }


                                    //ctx.lineTo(this.points[this.points.length - 1].x, this.points[this.points.length - 1].y);
                                }

                                for (lcounter = 0; lcounter < this.pointlist.length; lcounter++) {
                                    for (counter = 0; counter < this.pointlist[lcounter].length; counter++) {
                                        xscalepoint = (this.pointlist[lcounter][counter].x - this.xoffset) * scalefactor;
                                        yscalepoint = (this.pointlist[lcounter][counter].y - this.yoffset) * scalefactor;
                                        //xscalepoint = (this.points[counter].x - this.xoffset) * scalefactor;
                                        //yscalepoint = (this.points[counter].y - this.yoffset) * scalefactor;
                                        xscalepoint += pagedx;
                                        yscalepoint += pagedy;

                                        if (counter == 0) {
                                            ctx.moveTo(xscalepoint, yscalepoint);
                                        }
                                        else {
                                            ctx.lineTo(xscalepoint, yscalepoint);
                                        }
                                    }
                                }



                                ctx.stroke();
                                ctx.restore();
                            }/*dont draw proxy elements*/

                            

                            if (markupobject.selected) {
                                markupobject.select(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, true, false);  
                            }

                            if(markupobject.marked){
                                markupobject.mark(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, true, false);
                            }


                            break;
                        case 1: //polygon
                            var pcounter = 0;
                            markupobject.SetDimensions(scalefactor, pagerotation, pagedx, pagedy);

                            if(!markupobject.pdfannotproxy){

                            
                                switch (this.subtype) {
                                    case 1: //polyline
                                        ctx.save();
                                        markupobject.GetLinestyle(markupobject.linestyle, ctx, markupobject.fixedscaleFactor);
                                        markupobject.polyline(ctx, markupobject.linewidth, markupobject.strokecolor, scalefactor, pagedx, pagedy, true);
                                        ctx.restore();
                                        break;
                                    case 2: //polygon
                                        if (this.alternative == 0) {
                                            ctx.save();
                                            markupobject.GetLinestyle(markupobject.linestyle, ctx, markupobject.fixedscaleFactor);
                                            markupobject.polygon(ctx, markupobject.linewidth, false, true, markupobject.fillcolor, markupobject.strokecolor, scalefactor, pagedx, pagedy, true);
                                            ctx.restore();
                                        }
                                        if (this.alternative == 1) {
                                            ctx.save();
                                            markupobject.GetLinestyle(markupobject.linestyle, ctx, markupobject.fixedscaleFactor);
                                            markupobject.polygon(ctx, markupobject.linewidth, true, true, markupobject.fillcolor, markupobject.strokecolor, scalefactor, pagedx, pagedy, true);
                                            ctx.restore();
                                        }
                                        if (this.alternative == 2) {
                                            markupobject.polygon(ctx, markupobject.linewidth, true, true, "white", markupobject.color, scalefactor, pagedx, pagedy, true);
                                        }
                                        if (this.alternative >= 3) {

                                            if (ptrn != null){
                                                markupobject.polygon(ctx, markupobject.linewidth, true, true, ptrn, markupobject.color, scalefactor, pagedx, pagedy, true);
                                            }else{
                                                markupobject.polygon(ctx, markupobject.linewidth, true, true, "white", markupobject.color, scalefactor, pagedx, pagedy, true);
                                            }
                                            
                                        }
                                        break;
                                    case 3: //measure path
                                        ctx.save();
                                        markupobject.GetLinestyle(markupobject.linestyle, ctx, markupobject.fixedscaleFactor);
                                        markupobject.polyline(ctx, markupobject.linewidth, markupobject.strokecolor, scalefactor, pagedx, pagedy, true);
                                        ctx.restore();
                                        //markupobject.findrectangle();
                                        markupobject.dimtext = markupobject.setdimvaluepoly();
                                        //markupobject.setdimvaluepoly();

                                        if (!markupobject.saved) {
                                            //add callback for dimension text.

                                            if(RxCore_GUI_MarkupDrawValue != undefined){
                                                RxCore_GUI_MarkupDrawValue.setdimtext(markupobject.dimtext);
                                            }

                                            if(RxCore_GUI_MarkupMeasureRealTimeData != undefined){
                                                RxCore_GUI_MarkupMeasureRealTimeData.setMarkupMeasureRealTimeData(markupobject);
                                            }
                                            
                                

                                        }

                                        if(!bMarkupNoLabel && !markupobject.hidevaluelabel){
                                            if (markupobject.bUseFixedScale) {
                                                markupobject.dimvaluedraw(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, this.strokecolor, markupobject.fixedscaleFactor, false);
                                            }
                                            else {
                                                markupobject.dimvaluedraw(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, this.strokecolor, scalefactor, false);
                                            }
                                        }

                                        //markupobject.setdimvaluepoly();
                                        //markupobject.findrectangle();
                                        //markupobject.dimvaluedraw(ctx, this.x, this.y, this.w, this.h, this.color, 1);
                                        break;
                                    case 4:
                                        ctx.save();
                                        markupobject.GetLinestyle(markupobject.linestyle, ctx, markupobject.fixedscaleFactor);
                                        var dimobj = markupobject.drawangle(ctx, markupobject.linewidth, markupobject.strokecolor, scalefactor, pagedx, pagedy, true, false);
                                        markupobject.dimtext = dimobj.text;

                                        //let dimobject = {text : dimtext, pos : dimpos};

                                        ctx.restore();
                                        //markupobject.findrectangle();

                                        if (!markupobject.saved) {

                                            if(RxCore_GUI_MarkupDrawValue != undefined){
                                                RxCore_GUI_MarkupDrawValue.setdimtext(markupobject.dimtext);
                                            }

                                            if(RxCore_GUI_MarkupMeasureRealTimeData != undefined){
                                                RxCore_GUI_MarkupMeasureRealTimeData.setMarkupMeasureRealTimeData(markupobject);
                                            }


                                            //add callback for dimension text.
                                            
                                        }

                                        
                                        //replace with calculated angle
                                        //markupobject.dimtext = markupobject.setdimvaluepoly();
                                        
                                        //markupobject.setdimvaluepoly();

                                        if(!bMarkupNoLabel && !markupobject.hidevaluelabel){
                                            if (markupobject.bUseFixedScale) {

                                                if(markupobject.dimtext != ""){
                                                    //markupobject.dimvaluedraw(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, this.strokecolor, markupobject.fixedscaleFactor, false);

                                                    markupobject.anglevaluedraw(ctx, dimobj.pos, this.wscaled, this.hscaled, this.strokecolor, markupobject.fixedscaleFactor);
                                                }

                                                
                                            }
                                            else {
                                                if(markupobject.dimtext != ""){
                                                    //markupobject.dimvaluedraw(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, this.strokecolor, scalefactor, false);
                                                    markupobject.anglevaluedraw(ctx, dimobj.pos, this.wscaled, this.hscaled, this.strokecolor, scalefactor);

                                                    //var pos = calculateAngleLabelPosition(this.points, r);
                                                }
                                                
                                            }
                                        }

                                        break;
                                    case 5:
                                        ctx.save();
                                        markupobject.GetLinestyle(markupobject.linestyle, ctx, markupobject.fixedscaleFactor);
                                        var dimobj = markupobject.drawangle(ctx, markupobject.linewidth, markupobject.strokecolor, scalefactor, pagedx, pagedy, true, true);

                                        markupobject.dimtext = dimobj.text;
                                        ctx.restore();
                                        //markupobject.findrectangle();
                                        
                                        //replace with calculated angle
                                        //markupobject.dimtext = markupobject.setdimvaluepoly();
                                        
                                        //markupobject.setdimvaluepoly();

                                        if (!markupobject.saved) {
                                            //add callback for dimension text.
                                            if(RxCore_GUI_MarkupDrawValue != undefined){
                                                RxCore_GUI_MarkupDrawValue.setdimtext(markupobject.dimtext);
                                            }

                                            if(RxCore_GUI_MarkupMeasureRealTimeData != undefined){
                                                RxCore_GUI_MarkupMeasureRealTimeData.setMarkupMeasureRealTimeData(markupobject);
                                            }

                                            
                                        }


                                        if(!bMarkupNoLabel && !markupobject.hidevaluelabel){
                                            if (markupobject.bUseFixedScale) {
                                                if(markupobject.dimtext != ""){
                                                    //markupobject.dimvaluedraw(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, this.strokecolor, markupobject.fixedscaleFactor, false);
                                                    //markupobject.anglevaluedraw(ctx, dimobj.dimpos, this.wscaled, this.hscaled, this.strokecolor, markupobject.fixedscaleFactor);
                                                    markupobject.anglevaluedraw(ctx, dimobj.pos, this.wscaled, this.hscaled, this.strokecolor, markupobject.fixedscaleFactor);
                                                }
                                            }
                                            else {
                                                if(markupobject.dimtext != ""){
                                                    //markupobject.dimvaluedraw(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, this.strokecolor, scalefactor, false);
                                                    //markupobject.anglevaluedraw(ctx, dimobj.dimpos, this.wscaled, this.hscaled, this.strokecolor, markupobject.fixedscaleFactor);
                                                    markupobject.anglevaluedraw(ctx, dimobj.pos, this.wscaled, this.hscaled, this.strokecolor, scalefactor);
                                                }
                                                
                                            }
                                        }
                                        break;
                                    case 6:
                                        ctx.save();
                                        markupobject.GetLinestyle(markupobject.linestyle, ctx, markupobject.fixedscaleFactor);
                                        markupobject.polylineSegments(ctx, markupobject.linewidth, markupobject.strokecolor, scalefactor, pagedx, pagedy, true);
                                        markupobject.polylineSegments(ctx, markupobject.linewidthSegment, markupobject.strokecolorSegment, scalefactor, pagedx, pagedy, true);
                    
                                        //this.strokecolorSegment = convertHex(markuplinecolorSegment, 30);
                                        //this.linewidthSegment = linewidthvalueSegment;

                                        ctx.restore();

                                        markupobject.dimtext = markupobject.setdimvaluepolySegments();

                                        if (!markupobject.saved) {
                                            //add callback for dimension text.
                                            if(RxCore_GUI_MarkupDrawValue != undefined){
                                                RxCore_GUI_MarkupDrawValue.setdimtext(markupobject.dimtext);
                                            }

                                            if(RxCore_GUI_MarkupMeasureRealTimeData != undefined){
                                                RxCore_GUI_MarkupMeasureRealTimeData.setMarkupMeasureRealTimeData(markupobject);
                                            }

                                            
                                        }



                                        if(!bMarkupNoLabel && !markupobject.hidevaluelabel){
                                            if (markupobject.bUseFixedScale) {
                                                //markupobject.dimvaluedraw(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, this.strokecolor, markupobject.fixedscaleFactor, false);
                                            }
                                            else {
                                                //markupobject.dimvaluedraw(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, this.strokecolor, scalefactor, false);
                                            }
                                        }

                                        break;
                                    case 7: //rev cloud
                                        ctx.save();
                                        //markupobject.GetLinestyle(markupobject.linestyle, ctx, markupobject.fixedscaleFactor);
                                        markupobject.polygoncloud(ctx, radiusScaled, markupobject.linewidth, false, true, markupobject.fillcolor, markupobject.strokecolor, scalefactor, pagedx, pagedy, true, true);
                                        //this.polygoncloud = function(ctx, radius, linewidth, fill, stroke, fillcolor, strokecolor, scalefactor, dx, dy, useoffset){
                                        ctx.restore();
                                    
                                        break;

                                        
                                }

                            }/*dont draw proxy elements*/
                            


                            if (markupobject.selected) {
                                markupobject.select(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, true, false);
                            }
                            if (markupobject.selectedit) {
                                markupobject.editpolygon(ctx, scalefactor);
                            }
                            if(markupobject.marked){
                                markupobject.mark(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, true, false);
                            }

                            break;
                        case 2:
                            var curvcounter = 0;
                            markupobject.SetDimensions(scalefactor, pagerotation, pagedx, pagedy);


                            if(!markupobject.pdfannotproxy){
                                switch (this.subtype) {
                                    case 1:
                                        markupobject.polycurves(ctx, markupobject.linewidth, markupobject.color, scalefactor);
                                        break;
                                }
    
                            }/*dont draw proxy elements*/

                            if (markupobject.selected) {
                                markupobject.select(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, true, false);
                            }
                            if (markupobject.selectedit) {
                                markupobject.editpolygon(ctx, scalefactor);
                            }
                            break;
                        case 3: //rectangle
                            markupobject.SetDimensions(scalefactor, pagerotation, pagedx, pagedy);


                            if (this.subtype == 6) {
                                markupobject.dimtext = markupobject.setdimvaluearea();
                            }



                            if(!markupobject.pdfannotproxy){

                                if (this.subtype == 2) {
                                    if (DocObj.backgroundColor != undefined) {
                                        var erasecolor = DocObj.backgroundColor;
                                    }
                                    else {
                                        erasecolor = "white";
                                    }
                                    markupobject.Rect(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, linewidthScaled, true, false, erasecolor, erasecolor);
                                    //ctx.lineWidth = this.linewidth * 10 * scalefactor;
                                }
                                if (this.subtype == 4) {
                                    markupobject.Rect(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, 1, true, true, markupobject.fillcolor, markupobject.fillcolor);
                                }
                                if (this.subtype == 3) {
                                    markupobject.Rect(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, linewidthScaled, true, false, markupobject.fillcolor, markupobject.fillcolor);
                                }
                                if (this.subtype == 1) {
                                    //
                                    if (this.alternative == 0) {
                                        ctx.save();
                                        markupobject.GetLinestyle(markupobject.linestyle, ctx, markupobject.fixedscaleFactor);
                                        markupobject.roundedRect(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, markupobject.rotation, radiusScaled, linewidthScaled, false, true, markupobject.fillcolor, markupobject.strokecolor);
                                        ctx.restore();
                                    }
                                    if (this.alternative == 1) {
                                        ctx.save();
                                        markupobject.GetLinestyle(markupobject.linestyle, ctx, markupobject.fixedscaleFactor);
                                        markupobject.roundedRect(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, markupobject.rotation, radiusScaled, linewidthScaled, true, true, markupobject.fillcolor, markupobject.strokecolor);
                                        ctx.restore();
                                    }
                                    if (this.alternative == 2) {
                                        markupobject.roundedRect(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, markupobject.rotation, radiusScaled, linewidthScaled, true, true, "white", this.color);
                                    }
                                    if (this.alternative >= 3) {
    
                                        if (ptrn != null){
                                            markupobject.roundedRect(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, markupobject.rotation, radiusScaled, linewidthScaled, true, true, ptrn, this.color);
                                        }else{
                                            markupobject.roundedRect(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, markupobject.rotation, radiusScaled, linewidthScaled, true, true, "white", this.color);
                                        }
                                        
                                    }
                                }
                                if (this.subtype == 0) {
                                    if (this.alternative == 0) {
                                        ctx.save();
                                        markupobject.GetLinestyle(markupobject.linestyle, ctx, markupobject.fixedscaleFactor);
                                        markupobject.Rect(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, linewidthScaled, false, true, markupobject.fillcolor, markupobject.strokecolor);
                                        ctx.restore();
                                    }
                                    if (this.alternative == 1) {
                                        ctx.save();
                                        markupobject.GetLinestyle(markupobject.linestyle, ctx, markupobject.fixedscaleFactor);
                                        markupobject.Rect(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, linewidthScaled, true, true, markupobject.fillcolor, markupobject.strokecolor);
                                        ctx.restore();
                                    }
                                    if (this.alternative == 2) {
                                        markupobject.Rect(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, linewidthScaled, true, true, "white", this.color);
                                    }
                                    if (this.alternative >= 3) {
                                        //var ptrn = ctx.createPattern(hatchdiagforw,'repeat');
    
                                        if (ptrn != null){
                                            markupobject.Rect(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, linewidthScaled, true, true, ptrn, this.color);    
                                        }else{
                                            markupobject.Rect(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, linewidthScaled, true, true, "white", this.color);
                                        }
                                        
                                    }
                                }
                                //rect area tool
                                if (this.subtype == 6) {
    
                                    var bfill = (markupobject.alternative == 1);
    
                                    markupobject.Rect(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, linewidthScaled, bfill, true, markupobject.fillcolor, markupobject.strokecolor);
    
                                    //markupobject.dimtext = markupobject.setdimvaluearea();
    
                                    if (!markupobject.saved) {
                                        //add callback for dimension text.
                                        if(RxCore_GUI_MarkupDrawValue != undefined){
                                            RxCore_GUI_MarkupDrawValue.setdimtext(markupobject.dimtext);
                                        }
    
                                        if(RxCore_GUI_MarkupMeasureRealTimeData != undefined){
                                            RxCore_GUI_MarkupMeasureRealTimeData.setMarkupMeasureRealTimeData(markupobject);
                                        }
    
                                        
                                    }
    
    
                                    if(!bMarkupNoLabel && !markupobject.hidevaluelabel){
                                        if (markupobject.bUseFixedScale) {
                                            markupobject.dimvaluedraw(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, this.strokecolor, markupobject.fixedscaleFactor, false);
                                        }
                                        else {
                                            markupobject.dimvaluedraw(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, this.strokecolor, scalefactor, false);
                                        }
                                    }
    
    
                                }


                            }/*dont draw proxy elements*/

                            

                            if (markupobject.selected) {

                                if(markupobject.subtype != 5){

                                    var brotsel = !markupobject.pdfannotproxy;

                                    if(markupobject.subtype == 6){
                                        markupobject.select(ctx, this.xscaled, this.yscaled, this.xscaled + this.wscaled, this.yscaled + this.hscaled, true, false);
                                    }else{
                                        
                                        
                                        markupobject.select(ctx, this.xscaled, this.yscaled, this.xscaled + this.wscaled, this.yscaled + this.hscaled, true, brotsel);
                                    }

                                    
                                }

                            }
                            if(markupobject.marked){
                                markupobject.mark(ctx, this.xscaled, this.yscaled, this.xscaled + this.wscaled, this.yscaled + this.hscaled, true, true);
                            }


                            break;
                        case 4: //oval
                            markupobject.SetDimensions(scalefactor, pagerotation, pagedx, pagedy);
                            if(!markupobject.pdfannotproxy){


                                if (this.alternative == 0) {
                                    ctx.save();
                                    markupobject.GetLinestyle(markupobject.linestyle, ctx, markupobject.fixedscaleFactor);
                                    if (this.subtype == 1) {
                                        markupobject.Circle(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, linewidthScaled, false, true, markupobject.fillcolor, markupobject.strokecolor);
                                        markupobject.drawradius(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, crossl, linewidthScaled, markupobject.strokecolor, false);
                                    }
                                    else {
                                        markupobject.Oval(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, linewidthScaled, false, true, markupobject.fillcolor, markupobject.strokecolor);
                                    }
                                    ctx.restore();
                                }
                                if (this.alternative == 1) {
                                    ctx.save();
                                    markupobject.GetLinestyle(markupobject.linestyle, ctx, markupobject.fixedscaleFactor);
                                    if (this.subtype == 1) {
                                        markupobject.Circle(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, linewidthScaled, true, true, markupobject.fillcolor, markupobject.strokecolor);
                                        markupobject.drawradius(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, crossl, linewidthScaled, markupobject.strokecolor, false);
                                    }
                                    else {
                                        markupobject.Oval(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, linewidthScaled, true, true, markupobject.fillcolor, markupobject.strokecolor);
                                    }
                                    ctx.restore();
                                }
                                if (this.alternative == 2) {
                                    markupobject.Oval(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, linewidthScaled, true, true, "white", this.color);
                                }
                                if (this.alternative >= 3) {
    
                                    if (ptrn != null){
                                        markupobject.Oval(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, linewidthScaled, true, true, ptrn, this.color);
                                    }else{
                                        markupobject.Oval(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, linewidthScaled, true, true, "white", this.color);
                                    }
    
                                }

                            }/*dont draw proxy elements*/

                            
                            if (markupobject.selected) {
                                if (this.subtype == 1) {
                                    //markupobject.select(ctx, this.xscaled, this.yscaled, this.xscaled + this.wscaled, this.yscaled + this.hscaled, false, false);
                                    markupobject.select(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, false, false);
                                }
                                else {
                                    markupobject.select(ctx, this.xscaled, this.yscaled, this.xscaled + this.wscaled, this.yscaled + this.hscaled, true, true);
                                }
                            }
                            
                            if(markupobject.marked){
                                markupobject.mark(ctx, this.xscaled, this.yscaled, this.xscaled + this.wscaled, this.yscaled + this.hscaled, true, true);
                            }

                            break;
                        case 5: //cloud
                            markupobject.SetDimensions(scalefactor, pagerotation, pagedx, pagedy);

                            if(!markupobject.pdfannotproxy){

                                if (this.alternative == 0) {
                                    ctx.save();
                                    markupobject.GetLinestyle(markupobject.linestyle, ctx, markupobject.fixedscaleFactor);
                                    //markupobject.cloud(ctx,this.xscaled,this.yscaled,this.wscaled,this.hscaled,radiusScaled,linewidthScaled,false,true,this.color,this.color);
                                    markupobject.newcloud(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, radiusScaled, linewidthScaled, false, true, markupobject.fillcolor, markupobject.strokecolor);
                                    ctx.restore();
                                }
                                if (this.alternative == 1) {
                                    //markupobject.cloud(ctx,this.xscaled,this.yscaled,this.wscaled,this.hscaled,radiusScaled,linewidthScaled,true,true,this.color,this.color);
                                    ctx.save();
                                    markupobject.GetLinestyle(markupobject.linestyle, ctx, markupobject.fixedscaleFactor);
                                    markupobject.newcloud(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, radiusScaled, linewidthScaled, true, true, markupobject.fillcolor, markupobject.strokecolor);
                                    ctx.restore();
                                }
                                if (this.alternative == 2) {
                                    //markupobject.cloud(ctx,this.xscaled,this.yscaled,this.wscaled,this.hscaled,radiusScaled,linewidthScaled,true,true,"white",this.color);
                                    markupobject.newcloud(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, radiusScaled, linewidthScaled, true, true, "white", this.color);
                                }
                                if (this.alternative >= 3) {
                                    //markupobject.cloud(ctx,this.xscaled,this.yscaled,this.wscaled,this.hscaled,radiusScaled,linewidthScaled,true,true,ptrn,this.color);
    
                                    if (ptrn != null){
                                        markupobject.newcloud(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, radiusScaled, linewidthScaled, true, true, ptrn, this.color);
                                    }else{
                                        markupobject.newcloud(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, radiusScaled, linewidthScaled, true, true, "white", this.color);
                                    }
    
    
                                }

                            }/*dont draw proxy elements*/

                            
                            if (markupobject.selected) {
                                markupobject.select(ctx, this.xscaled, this.yscaled, this.xscaled + this.wscaled, this.yscaled + this.hscaled, true, true);
                            }
                            if(markupobject.marked){
                                markupobject.mark(ctx, this.xscaled, this.yscaled, this.xscaled + this.wscaled, this.yscaled + this.hscaled, true, true);
                            }

                            break;
                        case 6: //line arrow
                            markupobject.SetDimensions(scalefactor, pagerotation, pagedx, pagedy);


                            if(!markupobject.pdfannotproxy){
                                ctx.save();
                                markupobject.GetLinestyle(markupobject.linestyle, ctx, markupobject.fixedscaleFactor);
                                markupobject.arrow(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, arrowlengthscaled, arrowangle, linewidthScaled, this.subtype, markupobject.strokecolor, markupobject.strokecolor);
                                ctx.restore();
    

                            }/*dont draw proxy elements*/

                            if (markupobject.selected) {
                                markupobject.select(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, false, false);
                            }
                            if(markupobject.marked){
                                markupobject.mark(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, false, false);
                            }

                            break;
                        case 7: //dimension line
                            markupobject.SetDimensions(scalefactor, pagerotation, pagedx, pagedy);
                            var dimtype = this.subtype + 4;

                            markupobject.setdimvalue(this.x, this.y, this.w, this.h);

                            if(!markupobject.pdfannotproxy){

                                ctx.save();
                                markupobject.GetLinestyle(markupobject.linestyle, ctx, markupobject.fixedscaleFactor);
                                this.labelpos = markupobject.dimensionLeader(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, leaderOffsetScalded, arrowlengthscaled, arrowangle, linewidthScaled, dimtype, markupobject.strokecolor, markupobject.strokecolor);
                                ctx.restore();
    
        
    
                                if (!markupobject.saved) {
                                    //add callback for dimension text.
                                    if(RxCore_GUI_MarkupDrawValue != undefined){
                                        RxCore_GUI_MarkupDrawValue.setdimtext(markupobject.dimtext);
                                    }
    
                                    if(RxCore_GUI_MarkupMeasureRealTimeData != undefined){
                                        RxCore_GUI_MarkupMeasureRealTimeData.setMarkupMeasureRealTimeData(markupobject);
                                    }
    
                                    
                                }
        
                                if (markupobject.bUseFixedScale) {
                                    if (this.leaderoffset == 0) {
                                        markupobject.dimvaluedraw(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, this.strokecolor, markupobject.fixedscaleFactor, true);
                                    }
                                    else {
                                        markupobject.dimvaluedraw(ctx, this.labelpos.x, this.labelpos.y, this.labelpos.w, this.labelpos.h, this.strokecolor, markupobject.fixedscaleFactor, true);
                                    }
                                }
                                else {
                                    if (this.leaderoffset == 0) {
                                        markupobject.dimvaluedraw(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, this.strokecolor, scalefactor, true);
                                    }
                                    else {
                                        markupobject.dimvaluedraw(ctx, this.labelpos.x, this.labelpos.y, this.labelpos.w, this.labelpos.h, this.strokecolor, scalefactor, true);
                                    }
                                }
    

                            }/*dont draw proxy elements*/


                            if (markupobject.selected) {
                                markupobject.select(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, false, false);
                                if (this.leaderoffset == 0) {
                                    markupobject.selectLabel(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, false, false);
                                }
                                else {
                                    markupobject.selectLabel(ctx, this.labelpos.x, this.labelpos.y, this.labelpos.w, this.labelpos.h, false, false);
                                }
                            }
                            if(markupobject.marked){
                                markupobject.mark(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, false, false);
                            }


                            break;
                        case 8:

                            if(markupobject.points.length == 0){
                                break;
                            }

                            var acounter = 0;
                            var dimarea = 0;
                            markupobject.SetDimensions(scalefactor, pagerotation, pagedx, pagedy);

                            /*if(markupobject.holes.length != 0){
                                for(var hi = 0; hi < markupobject.holes.length; hi++){
        
                                    markupobject.holes[hi].SetDimensions(scalefactor, pagerotation, pagedx, pagedy);
                                            
                                }
                            }*/
        


                            if (markupobject.bUseFixedScale) {
                                var localscalefactor = markupobject.fixedscaleFactor;
                            }
                            else {
                                localscalefactor = scalefactor;
                            }
                            /*if(markupobject.subtype == 1){
        
                            }*/

                            if(markupobject.bshowperimeter){
                                markupobject.dimtext = markupobject.setdimvalueperimeter();
                            }else{
                                markupobject.dimtext = markupobject.setdimvaluearea();
                                if (markupobject.subtype == 1) {
                                    markupobject.updateAttribute('Rx_Area', this.dimtext);
                                }

                            }


                            if(!markupobject.pdfannotproxy){

                                if (!markupobject.spaceselected && markupobject.subtype == 1) {
                                    markupobject.linestyle = 0;
                                }
                                if (markupobject.spaceselected && markupobject.subtype == 1) {
                                    ctx.save();
                                    var grd = ctx.createLinearGradient(this.xscaled, this.yscaled, this.wscaled, this.hscaled);
                                    grd.addColorStop(0, markupobject.selectSpaceColor);
                                    grd.addColorStop(.5, markupobject.selectSpaceStopColor);
                                    grd.addColorStop(1, markupobject.selectSpaceColor);
                                    markupobject.GetLinestyle(markupobject.linestyle, ctx, markupobject.fixedscaleFactor);
                                    //markupobject.polygon(ctx, markupobject.linewidth, true, true, markupobject.selectSpaceColor, markupobject.strokecolor, scalefactor,pagedx, pagedy,true);
                                    markupobject.polygon(ctx, markupobject.linewidth, true, true, grd, markupobject.strokecolor, scalefactor, pagedx, pagedy, true);
                                    /*if(markupobject.linestyle == 6 && bAnimateLineStyle){
                                        markupobject.polygon(ctx, markupobject.linewidth, false, true, markupobject.selectSpaceColor, markupobject.strokecolor, scalefactor,pagedx, pagedy,true);
                                    }else{
                                        
                                    }*/
                                    ctx.restore();
                                }
                                else {
    
                                    
                                    var drawbhole = (markupobject.subtype == 2) && markupobject.saved;
    
                                    if(!drawbhole){
                                        if (this.alternative == 0) {
                                            ctx.save();
                                            markupobject.GetLinestyle(markupobject.linestyle, ctx, markupobject.fixedscaleFactor);
                                            markupobject.polygon(ctx, markupobject.linewidth, false, true, markupobject.fillcolor, markupobject.strokecolor, scalefactor, pagedx, pagedy, true);
                                            ctx.restore();
                                        }
                                        if (this.alternative == 1) {
                                            ctx.save();
                                            markupobject.GetLinestyle(markupobject.linestyle, ctx, markupobject.fixedscaleFactor);
                                            markupobject.polygon(ctx, markupobject.linewidth, true, true, markupobject.fillcolor, markupobject.strokecolor, scalefactor, pagedx, pagedy, true);
                                            ctx.restore();
                                        }
                                        if (this.alternative == 2) {
                                            markupobject.polygon(ctx, markupobject.linewidth, true, true, "white", markupobject.color, scalefactor, pagedx, pagedy, true);
                                        }
                                        if (this.alternative >= 3) {
                                            
                                            if (ptrn != null){
                                                markupobject.polygon(ctx, markupobject.linewidth, true, true, ptrn, markupobject.color, scalefactor, pagedx, pagedy, true);
                                            }else{
                                                markupobject.polygon(ctx, markupobject.linewidth, true, true, "white", markupobject.color, scalefactor, pagedx, pagedy, true);
                                            }
        
        
                                        }
    
                                    }
                                    
                                    
                                }
    
    
                                /*var markupscalesq = this.scaling * this.scaling;
                                dimarea = markupobject.PolygonArea();
                                this.dimtext = getUnitArea(dimarea / markupscalesq);
                                this.dimtext = this.dimtext.toFixed(2);
                                this.dimtext = this.dimtext + " " + AreaUnitlabel;*/
    
    
                                var centroidpt = get_polygon_centroid(this.points);
                                if (markupobject.subtype == 1) {
                                    markupobject.updateAttribute('Rx_Area', this.dimtext);
                                }
                                //var areatextx = markupobject.PolygonCentre('x');
                                //var areatexty = markupobject.PolygonCentre('y');
                                var areatextx = centroidpt.x;
                                var areatexty = centroidpt.y;
                                var areatextxscaled = (areatextx - this.xoffset) * scalefactor;
                                var areatextyscaled = (areatexty - this.yoffset) * scalefactor;
    
                                var offset = getCurrentPageOffset(markupobject.pagenumber);
                                
    
                                var areatextxscaled = areatextxscaled + offset.x;
                                var areatextyscaled = areatextyscaled + offset.y;
    
                                var areatextscaled = this.measuretextheight * scalefactor;
                                if (markupobject.bUseFixedScale) {
                                    areatextscaled = this.measuretextheight * markupobject.fixedscaleFactor;
                                }
    
                                if(bUseNoScale){
                                    areatextscaled = this.measuretextheight * 1;
                                }
    
                                if (!markupobject.saved) {
                                    //add callback for dimension text.
    
                                    if(RxCore_GUI_MarkupDrawValue != undefined){
                                        RxCore_GUI_MarkupDrawValue.setdimtext(markupobject.dimtext);
                                    }
    
                                    if(RxCore_GUI_MarkupMeasureRealTimeData != undefined){
                                        RxCore_GUI_MarkupMeasureRealTimeData.setMarkupMeasureRealTimeData(markupobject);
                                    }
    
    
                                    
                                }
    
    
                                if (!bMarkupNoLabel && markupobject.busecustomlabel && !markupobject.hidevaluelabel && markupobject.subtype != 2) {
                                    markupobject.areavaluedraw(ctx, markupobject.customlabeltext, areatextxscaled, areatextyscaled, markupobject.strokecolor, areatextscaled, localscalefactor, scalefactor);
                                }
                                if (!bMarkupNoLabel && !markupobject.busecustomlabel && markupobject.subtype != 1 && !markupobject.hidevaluelabel && markupobject.subtype != 2) {
                                    
                                    markupobject.areavaluedraw(ctx, markupobject.dimtext, areatextxscaled, areatextyscaled, markupobject.strokecolor, areatextscaled, localscalefactor, scalefactor);
                                    /*ctx.textAlign = "start";
                                    ctx.font = areatextscaled + "pt " + "Helvetica";
                                    this.textheight = this.measuretextheight;
            
                                    var areat = ctx.measureText(this.dimtext);
                                    var areatextwidth = areat.width;
                                    var areatextheight = areatextscaled;
                                    areatextxscaled = areatextxscaled - (areatextwidth / 2);
                                    areatextyscaled = areatextyscaled + (areatextheight / 2);
                                    ctx.lineWidth = 1;
                                    ctx.strokeStyle = this.strokecolor;
                                    ctx.fillStyle = "white";
                                    
                                    ctx.fillRect(areatextxscaled - (10 * localscalefactor), areatextyscaled - (20 * localscalefactor), areatextwidth + (20 * localscalefactor), areatextscaled + (15 * localscalefactor));
                                    ctx.strokeRect(areatextxscaled - (10 * localscalefactor), areatextyscaled - (20 * localscalefactor), areatextwidth + (20 * localscalefactor), areatextscaled + (15 * localscalefactor));*/
                                    // ctx.fillStyle = this.color;
                                    //ctx.fillStyle = "black";
                                    //ctx.fillText(this.dimtext, areatextxscaled, areatextyscaled);
                                }

                            }/*dont draw proxy elements*/

                            

                            if (markupobject.selected) {
                                markupobject.select(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, true, false);
                            }
                            if (markupobject.selectedit && markupobject.subtype != 2) {
                                markupobject.editpolygon(ctx, scalefactor);
                            }

                            if(markupobject.holes.length > 0){

                                for (var hi = 0; hi < markupobject.holes.length; hi++){
                                    
        
                                    if(markupobject.holes[hi].selectedit){

                                        var holescalefactor = scalefactor * markupobject.scaling;
                                        scalefactor = holescalefactor /markupobject.holes[hi].scaling;
                    

                                        markupobject.holes[hi].editpolygon(ctx,scalefactor);
                                    }
                                    
                                }
        
                                
                            }
        

                            if(markupobject.marked){
                                markupobject.mark(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, true, false);
                            }


                            break;
                        case 9: //text
                            markupobject.SetDimensions(scalefactor, pagerotation, pagedx, pagedy);
                            if (markupobject.selectedit) {
                                break;
                            }
                            if (markupobject.bUseFixedScale) {
                                var temptxtscale = markupobject.fixedscaleFactor;
                                temptxtscale *= fTextScale;
                            }
                            else {
                                temptxtscale = scalefactor;
                            }

                            if(!markupobject.pdfannotproxy){

                                this.font.setScale(temptxtscale);
                                var textscaled = this.font.height * temptxtscale;
                                ctx.font = this.font.fontstringScaled;
                                var dimsel = ctx.measureText(this.text);
                                this.textwidth = dimsel.width;
                                //draw text selected goes here.
                                ctx.save();
                                tx = this.xscaled;
                                ty = this.yscaled;
                                if (markupobject.textrotate != 0 && markupobject.subtype == 0) {
                                    ctx.translate(tx, ty);
                                    ctx.rotate(markupobject.textrotate);
                                    ctx.translate(-tx, -ty);
                                }
                                //console.log(markupobject.rotation * (180 / Math.PI));
                                if (markupobject.rotation != 0) {
                                    tx = this.xscaled + (this.wscaled / 2);
                                    if (markupobject.subtype == 0) {
                                        ty = this.yscaled - (this.hscaled / 2);
                                    }
                                    else {
                                        ty = this.yscaled + (this.hscaled / 2);
                                    }
                                    ctx.translate(tx, ty);
                                    ctx.rotate(markupobject.rotation);
                                    ctx.translate(-tx, -ty);
                                }
                                ctx.textAlign = "start";
                                ctx.fillStyle = markupobject.textcolor;
                                if (this.subtype == 1 || this.subtype == 2) {
                                    //markupobject.fillcolor = "rgba(255,255,255, 0.9)";
                                    //this.textcolor = "rgba(255,255,255, 0.9)";
                                    var toffsetx = 4 * temptxtscale;
                                    //var toffsety = 2*scalefactor;

                                    /*if (this.bhasArrow) {
                                        var toffsety = ((this.font.height / 8) * 2.0) * temptxtscale;
                                    }else{
                                        toffsety = ((this.font.height / 4) * 2.0) * temptxtscale;
                                    }*/


                                    var toffsety = (this.font.height * lineheightfactor) * temptxtscale;


                                    var toffsetystart = ((this.font.height / 10) * 2.0) * temptxtscale;



                                    //var toffsety = ((this.font.height / 3)) * temptxtscale;
                                    if (markupobject.linewidth == 0) {
                                        markupobject.Rect(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, linewidthScaled, true, false, markupobject.fillcolor, markupobject.strokecolor);
                                    }
                                    else {
                                        ctx.save();
                                        markupobject.GetLinestyle(markupobject.linestyle, ctx, markupobject.fixedscaleFactor);
                                        markupobject.Rect(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, linewidthScaled, true, true, markupobject.fillcolor, markupobject.strokecolor);
                                        ctx.restore();
                                    }
                                    ctx.save();
                                    ctx.beginPath();
                                    ctx.rect(this.xscaled, this.yscaled, this.wscaled, this.hscaled);
                                    ctx.clip();
                                    //ctx.font=font;
                                    //ctx.fillText(theText,3,50);
                                    //wraptext(ctx, this.text, x, y, maxWidth, lineHeight);
                                    var textarray = this.text.split('\n');
                                    //var ystart = this.yscaled + textscaled + toffsety;

                                    var ystart = this.yscaled + textscaled + toffsetystart;

                                    var ttx = this.xscaled; // + (this.wscaled);
                                    var tty = this.yscaled; //+ (this.hscaled/2);
                                    var txtrotate = markupobject.textrotate * (180 / Math.PI);
                                    switch (markupobject.pagerotation) {
                                        case 0:
                                            var startx = this.xscaled + toffsetx;
                                            var maxwidth = this.wscaled;
                                            break;
                                        case 90:
                                            startx = (this.xscaled + toffsetx) - this.hscaled;
                                            ctx.translate(ttx, tty);
                                            ctx.rotate(-(markupobject.pagerotation * (Math.PI / 180)));
                                            ctx.translate(-ttx, -tty);
                                            maxwidth = this.hscaled;
                                            break;
                                        case 270:
                                            //startx = this.xscaled + toffsetx;
                                            startx = (this.xscaled + toffsetx); // + this.hscaled;
                                            //ystart = (this.yscaled + toffsety) - this.wscaled;
                                            //ystart = (this.yscaled + textscaled + toffsety) - this.wscaled;

                                            ystart = (this.yscaled + textscaled + toffsetystart) - this.wscaled;
                                            ctx.translate(ttx, tty);
                                            ctx.rotate(-(markupobject.pagerotation * (Math.PI / 180)));
                                            ctx.translate(-ttx, -tty);
                                            maxwidth = this.hscaled;
                                            break;
                                        case 180:
                                            startx = this.xscaled + toffsetx;
                                            ttx = this.xscaled + (this.wscaled / 2);
                                            tty = this.yscaled + (this.hscaled / 2);
                                            ctx.translate(ttx, tty);
                                            ctx.rotate(-(markupobject.pagerotation * (Math.PI / 180)));
                                            ctx.translate(-ttx, -tty);
                                            //adjust maxwidth to match text area
                                            maxwidth = this.wscaled;
                                            break;
                                    }
                                    maxwidth -= 5;

                                    wrapText2(ctx, this.text, startx, ystart, maxwidth, textscaled + toffsety);

                                    markupobject.textdim = getTextDimensions_ex(ctx, this.text, startx, ystart, maxwidth, textscaled + toffsety);


                                    //wrapText(ctx, this.text, startx, ystart, maxwidth, textscaled + toffsety);
                                    /*for (var i = 0; i < textarray.length; i++) {
            
                                        ctx.fillText(textarray[i], startx , ystart);
                                        ystart += textscaled + toffsety;
                                        //ctx.fillText(textarray[i], x + 5, ystart);
            
                                    }*/
                                    ctx.restore();
                                }
                                else {
                                    ctx.fillText(this.text, this.xscaled, this.yscaled);
                                }
                                ctx.restore();

                            }/*dont draw proxy elements*/

                            



                            if (markupobject.selected) {
                                if (this.subtype == 1 || this.subtype == 2) {
                                    if (this.bhasArrow) {
                                        markupobject.select(ctx, this.xscaled, this.yscaled, this.xscaled + this.wscaled, this.yscaled + this.hscaled, true, false);
                                    }
                                    else {
                                        markupobject.select(ctx, this.xscaled, this.yscaled, this.xscaled + this.wscaled, this.yscaled + this.hscaled, true, true);
                                    }
                                }
                                else {
                                    markupobject.select(ctx, this.xscaled, this.yscaled, this.xscaled + this.wscaled, this.yscaled - this.hscaled, true, true);
                                }
                            }

                            if(markupobject.marked){
                                markupobject.mark(ctx, this.xscaled, this.yscaled, this.xscaled + this.wscaled, this.yscaled + this.hscaled, true, true);
                            }

                            break;
                        case 10:

                            if(!bnoteimageloaded){
                                break;
                            }

                            markupobject.SetDimensions(scalefactor, pagerotation, pagedx, pagedy);
                            /*if (bUseFixedScale){
                                
                            }else{
                                markupobject.SetDimensions(scalefactor, DocObj.getCurrentpage().getpageRotation(), pagedx, pagedy);
                            }*/

                            if(!markupobject.pdfannotproxy){

                                ctx.save();

                                markupobject.rotatecnvs(ctx, markupobject.xscaled, markupobject.yscaled, markupobject.wscaled, markupobject.hscaled, markupobject.rotation);
                                
                                /*tx = this.xscaled;
                                ty = this.yscaled;
                                if (markupobject.rotation != 0) {
                                    tx = this.xscaled + (this.wscaled / 2);
                                    ty = this.yscaled - (this.hscaled / 2);
                                    ctx.translate(tx, ty);
                                    ctx.rotate(markupobject.rotation);
                                    ctx.translate(-tx, -ty);
                                }*/
    
                                if(bUseNoScale){
                                    ctx.drawImage(noteimage, this.xscaled, this.yscaled, this.w, this.h);
                                }else{
                                    ctx.drawImage(noteimage, this.xscaled, this.yscaled, this.wscaled, this.hscaled);
                                }
    
                                ctx.restore();

                            }/*dont draw proxy elements*/

                            
                            if (markupobject.selected) {

                                if(bUseNoScale){
                                    markupobject.select(ctx, this.xscaled, this.yscaled, this.xscaled + this.w, this.yscaled + this.h, true, false);
                                }else{
                                    markupobject.select(ctx, this.xscaled, this.yscaled, this.xscaled + this.wscaled, this.yscaled + this.hscaled, true, false);
                                }
                            }

                            if(markupobject.marked){

                                if(bUseNoScale){
                                    markupobject.mark(ctx, this.xscaled, this.yscaled, this.xscaled + this.w, this.yscaled + this.h, true, false);
                                }else{
                                    
                                    markupobject.mark(ctx, this.xscaled, this.yscaled, this.xscaled + this.wscaled, this.yscaled + this.hscaled, true, false);
                                }

                            }

                            break;
                        case 11:
                            if (!markupobject.imageloaded) {
                                break;
                            }
                            markupobject.SetDimensions(scalefactor, pagerotation, pagedx, pagedy);

                            if(!markupobject.pdfannotproxy){

                                if(this.subtype == 1){
                                    ctx.save();
                                    markupobject.rotatecnvs(ctx, markupobject.xscaled, markupobject.yscaled, markupobject.wscaled, markupobject.hscaled, markupobject.rotation);    
    
                                    if (markupobject.imageloaded) {
                                        ctx.drawImage(this.image, this.xscaled, this.yscaled, this.wscaled, this.hscaled);
                                    }
                                    ctx.restore();
    
    
        
        
                                }else if (this.subtype == 2){

                                    //custom image for print or export to pdf.
    
                                }else if(this.subtype == 3 || this.subtype == 5){
                                    //action button
                                    ctx.save();
                                    
                                    markupobject.rotatecnvs(ctx, markupobject.xscaled, markupobject.yscaled, markupobject.wscaled, markupobject.hscaled, markupobject.rotation);    
    
                                    if (markupobject.imageloaded) {
                                        ctx.drawImage(this.image, this.xscaled, this.yscaled, this.wscaled, this.hscaled);
                                    }
                                    ctx.restore();
    
    
    
    
                                }else if(this.subtype == 4){
                                    //signature
                                    ctx.save();
                                    
                                    markupobject.rotatecnvs(ctx, markupobject.xscaled, markupobject.yscaled, markupobject.wscaled, markupobject.hscaled, markupobject.rotation);    
    
                                    if (markupobject.imageloaded) {
                                        ctx.drawImage(this.image, this.xscaled, this.yscaled, this.wscaled, this.hscaled);
                                    }
                                    ctx.restore();
    
                                    /*if (markupobject.selected) {
                                        markupobject.select(ctx, this.xscaled, this.yscaled, this.xscaled + this.wscaled, this.yscaled + this.hscaled, true, false);
                                    }*/
    
                                }else if(this.subtype == 12){
                                    ctx.save();
                                    markupobject.rotatecnvs(ctx, markupobject.xscaled, markupobject.yscaled, markupobject.wscaled, markupobject.hscaled, markupobject.rotation);    
    
                                    if (markupobject.imageloaded) {
                                        ctx.drawImage(this.image, this.xscaled, this.yscaled, this.wscaled, this.hscaled);
                                    }
                                    ctx.restore();

                                }else if(this.subtype == 20){
                                    ctx.save();
                                    markupobject.rotatecnvs(ctx, markupobject.xscaled, markupobject.yscaled, markupobject.wscaled, markupobject.hscaled, markupobject.rotation);    
    
                                    if (markupobject.imageloaded) {
                                        ctx.drawImage(this.image, this.xscaled, this.yscaled, this.wscaled, this.hscaled);
                                    }
                                    ctx.restore();

                                }

                            }/*dont draw proxy elements*/

                            if(this.subtype == 1){
                                if (markupobject.selected) {
                                    markupobject.select(ctx, this.xscaled, this.yscaled, this.xscaled + this.wscaled, this.yscaled + this.hscaled, true, true);
                                }

                                if(markupobject.marked){

                                    markupobject.mark(ctx, this.xscaled, this.yscaled, this.xscaled + this.wscaled, this.yscaled + this.hscaled, true, true);
                                }

                            }else if (this.subtype == 2){
    
                                //custom image for print or export to pdf.

                            }else if(this.subtype == 3 || this.subtype == 5){ //action button

                                if (markupobject.selected) {
                                    markupobject.select(ctx, this.xscaled, this.yscaled, this.xscaled + this.wscaled, this.yscaled + this.hscaled, true, false);
                                }

                                if(markupobject.marked){
                                    markupobject.mark(ctx, this.xscaled, this.yscaled, this.xscaled + this.wscaled, this.yscaled + this.hscaled, true, false);
                                }

                            }else if(this.subtype == 4){ //signature not selectable

                            }else if(this.subtype == 12){ //image stamp
                                if (markupobject.selected) {
                                    markupobject.select(ctx, this.xscaled, this.yscaled, this.xscaled + this.wscaled, this.yscaled + this.hscaled, true, true);
                                }

                                if(markupobject.marked){

                                    markupobject.mark(ctx, this.xscaled, this.yscaled, this.xscaled + this.wscaled, this.yscaled + this.hscaled, true, true);
                                }
                            }else if(this.subtype == 20){

                                if (markupobject.selected) {
                                    markupobject.select(ctx, this.xscaled, this.yscaled, this.xscaled + this.wscaled, this.yscaled + this.hscaled, true, true);
                                }

                                if(markupobject.marked){

                                    markupobject.mark(ctx, this.xscaled, this.yscaled, this.xscaled + this.wscaled, this.yscaled + this.hscaled, true, true);
                                }


                            }



                            
                            /*tx = this.xscaled;
                            ty = this.yscaled;
                            if (markupobject.rotation != 0) {
                                tx = this.xscaled + (this.wscaled / 2);
                                ty = this.yscaled + (this.hscaled / 2);
                                ctx.translate(tx, ty);
                                ctx.rotate(markupobject.rotation);
                                ctx.translate(-tx, -ty);
                            }*/

                            break;
                        case 12:
                            markupobject.SetDimensions(scalefactor, pagerotation, pagedx, pagedy);
                            var textstampscaled = this.font.height * scalefactor;
                            this.font.setScale(scalefactor);
                            var textsmallstampscaled = markupobject.stampsmalltheight * scalefactor;
                            if (markupobject.bUseFixedScale) {
                                markupobject.roundedRect(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, markupobject.rotation, radiusScaled, 3 * markupobject.fixedscaleFactor, true, true, markupobject.fillcolor, markupobject.strokecolor);
                            }
                            else {
                                markupobject.roundedRect(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, markupobject.rotation, radiusScaled, 3 * scalefactor, true, true, markupobject.fillcolor, markupobject.strokecolor);
                            }
                            if (markupobject.selected) {
                                markupobject.select(ctx, this.xscaled, this.yscaled, this.xscaled + this.wscaled, this.yscaled + this.hscaled, true, true);
                            }
                            
                            if(markupobject.marked){
                                markupobject.mark(ctx, this.xscaled, this.yscaled, this.xscaled + this.wscaled, this.yscaled + this.hscaled, true, true);
                            }

                            var Displayname = GetDisplayName(markupobject.signature);
                            var datetext = markupobject.GetDateTime(false);
                            //var smalltext = "By " + markupobject.signature + "," + " ";
                            this.smalltext = "By " + Displayname + "," + " ";
                            this.smalltext += datetext;
                            /*if (this.subtype != 100) {
                                markupobject.text = Stamplist[this.subtype];
                            }*/
                            /*if (this.subtype == 0){ markupobject.text = "Approved";}
                             if (this.subtype == 1){ markupobject.text = "Draft";}
                             if (this.subtype == 2){ markupobject.text = "Received";}
                             if (this.subtype == 3){ markupobject.text = "Rejected";}
                             if (this.subtype == 4){ markupobject.text = "Reviewed";}
                             if (this.subtype == 5){ markupobject.text = "Revised";}*/
                            ctx.save();
                            if (markupobject.rotation != 0) {
                                tx = this.xscaled + (this.wscaled / 2);
                                ty = this.yscaled + (this.hscaled / 2);
                                ctx.translate(tx, ty);
                                ctx.rotate(markupobject.rotation);
                                ctx.translate(-tx, -ty);
                            }
                            /*var heightorient = this.hscaled;
                             var widthorient = this.wscaled;
                             //main stamp text.
                             if(this.hscaled > this.wscaled){
                             heightorient = this.wscaled;
                             widthorient = this.hscaled;
                             }else
                             markupobject.textheight = (heightorient/4);*/
                            /*if(DocObj.getCurrentpage().getpageRotation() == 90 || DocObj.getCurrentpage().getpageRotation() == 270){
                             markupobject.textheight = (this.wscaled/4);
                             tx = this.xscaled+(this.hscaled/2);
                             ty = this.yscaled+(this.wscaled/2)-textoffsetScaled;
        
                             }else{
        
                             }*/
                            ctx.fillStyle = markupobject.strokecolor;
                            //ctx.font = "bold " + textstampscaled + "pt " + "Times New Roman";
                            ctx.font = this.font.fontstringScaled;
                            var stampdim = ctx.measureText(this.text);
                            this.textwidth = stampdim.width;
                            tx = this.xscaled + (this.wscaled / 2);
                            //ty = this.yscaled+(this.hscaled/2);
                            if (this.alternative == 0) {
                                ty = this.yscaled + ((this.hscaled / 4) + (textstampscaled / 2));
                            }
                            else {
                                ty = this.yscaled + (this.hscaled / 2);
                                ty += (textstampscaled / 4);
                            }
                            //tx = this.xscaled+(this.wscaled/2);
                            //ty = this.yscaled+(this.hscaled/2);
                            if (markupobject.textrotate != 0) {
                                ty = this.yscaled + (this.hscaled / 2);
                                ctx.translate(tx, ty);
                                ctx.rotate(markupobject.textrotate);
                                ctx.translate(-tx, -ty);
                                tx = this.xscaled + (this.wscaled / 2);
                                //ty = this.yscaled+(this.hscaled/2);
                                if (this.alternative == 0) {
                                    ty = this.yscaled + ((this.hscaled / 4) + (textstampscaled / 2));
                                }
                                else {
                                    ty = this.yscaled + (this.hscaled / 2);
                                    ty += (textstampscaled / 4);
                                }
                            }
                            ctx.textAlign = 'center';
                            if (pagerotation == 90 || pagerotation == 270) {
                                ctx.fillText(this.text, tx, ty);
                            }
                            else {
                                ctx.fillText(this.text, tx, ty);
                            }
                            if (this.alternative == 0) {
                                //stamp information text.
                                //var smalltextsize = markupobject.textheight / 2;
                                ctx.fillStyle = this.color;
                                ctx.font = "bold " + textsmallstampscaled + "pt " + "Times New Roman";
                                /*tx = this.xscaled+(this.wscaled/2);
                                 ty = this.yscaled+(this.hscaled/2)+textoffsetScaled;
        
                                 if (markupobject.textrotate !=0){
                                 ctx.translate(tx,ty);
                                 ctx.rotate(markupobject.textrotate);
                                 ctx.translate(-tx,-ty);
        
                                 }*/
                                ty = this.yscaled + ((this.hscaled / 4) * 3);
                                if (pagerotation == 90 || pagerotation == 270) {
                                    ctx.fillText(this.smalltext, tx, ty);
                                }
                                else {
                                    ctx.fillText(this.smalltext, tx, ty);
                                }
                            }
                            ctx.restore();
                            break;

                        case 13 :

                            markupobject.SetDimensions(scalefactor, pagerotation, pagedx, pagedy);

                            
                            if(!markupobject.pdfannotproxy){
                                markupobject.shapes(ctx, markupobject.subtype, markupobject.outerRadius, markupobject.innerRadius, markupobject.linewidth, markupobject.fillcolor, markupobject.strokecolor, scalefactor, pagedx, pagedy);

                            }/*dont draw proxy elements*/


                            if (!markupobject.saved) {
                                //add callback for dimension text.

                                if(RxCore_GUI_MarkupDrawValue != undefined){
                                    RxCore_GUI_MarkupDrawValue.setdimtext(markupobject.getcount());
                                }
                    
                                if(RxCore_GUI_MarkupMeasureRealTimeData != undefined){
                                    RxCore_GUI_MarkupMeasureRealTimeData.setMarkupMeasureRealTimeData(markupobject);
                                }


                            }

                            if (markupobject.selectedit) {
                                markupobject.editpolygon(ctx, scalefactor);
                            }

                            if(markupobject.marked){
                                markupobject.markpolygon(ctx, scalefactor);
                            }

                            //markupobject.newcloud(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, radiusScaled, linewidthScaled, false, true, markupobject.fillcolor, markupobject.strokecolor);
                            //this.shapes = function(ctx, subtype, outerRadius, innerRadius, linewidth, fillcolor, strokecolor){

                            break;         
                            
                        case 14 : //measure arc
                            markupobject.SetDimensions(scalefactor, pagerotation, pagedx, pagedy);

                            if(markupobject.startX != null && markupobject.startY != null){
                                if(markupobject.secondX != null && markupobject.secondY != null){
                                    markupobject.xscaled = Math.min(markupobject.startXscaled, markupobject.secondXscaled);
                                    markupobject.yscaled = Math.min(markupobject.startYscaled, markupobject.secondYscaled);
                                    markupobject.wscaled = Math.max(markupobject.startXscaled, markupobject.secondXscaled);
                                    markupobject.hscaled = Math.max(markupobject.startYscaled, markupobject.secondYscaled);
                                }
                            }

                            //var Unitofmeasure = DocObj.getCurrentpage().scaleObject.Unitofmeasure;                    
                            //var Unitlabel = DocObj.getCurrentpage().scaleObject.Unitlabel;


                            var p1 = {x : markupobject.startXscaled, y:markupobject.startYscaled};
                            var p2 = {x : markupobject.secondXscaled, y:markupobject.secondYscaled};
                            var p3 = {x : markupobject.endXscaled, y:markupobject.endYscaled};

                            var p1us = {x : markupobject.startX, y:markupobject.startY};
                            var p2us = {x : markupobject.secondX, y:markupobject.secondY};
                            var p3us = {x : markupobject.endX, y:markupobject.endY};


                            const pointColor = "rgb(109, 166, 209)";

                            RenderUtils.renderPoints(ctx, markupobject, pointColor);

                            

                            // Draw the appropriate preview based on how many points have been placed
                            if (markupobject.setsecond) {
                                // First point placed, drawing line/arc to mouse position
                                var retp3 = RenderUtils.drawInitialArc(ctx, p1, p2,1.2, markupobject.strokecolor, linewidthScaled);

                                if(markupobject.editaction == 4){


                                    markupobject.endX = ((retp3.x - pagedx)  / scalefactor) + markupobject.xoffset;
                                    markupobject.endY = ((retp3.y - pagedy)  / scalefactor) + markupobject.yoffset;

                                    p3us = {x : markupobject.endX, y:markupobject.endY};


                                    var mesureobj = RenderUtils.calculateArcMeasurements(p1us, p2us, p3us);

                                    //this.dimtext = getUnitlength(dimdiag / this.scaling);

                                    markupobject.setarcvalue(mesureobj.radius, mesureobj.arcLength);

                                    //this.dimradius = scaledradius.toFixed(dimPrecision) + " " + Unitlabel;
                                    //this.dimlength = scaledlength.toFixed(dimPrecision) + " " + Unitlabel;
            

                                    const labelText = `R${markupobject.dimradius}\nL${markupobject.dimlength}`;

                                    RenderUtils.drawArcLabelFromText(ctx, retp3.labelposx, retp3.labelposy, labelText);
                

                                    //mesureobj.radius = getUnitlength(mesureobj.radius / markupobject.scaling);
                                    //mesureobj.arcLength = getUnitlength(mesureobj.arcLength / markupobject.scaling);
                                    //getUnitlength(dimdiag / this.scaling)

                                    //RenderUtils.drawArcLabel(ctx, retp3.labelposx, retp3.labelposy, mesureobj.radius, mesureobj.arcLength, Unitlabel);

                                    /*if(this.endX != null && this.endY != null ){
                                        this.endXscaled = (this.endX - this.xoffset) * scalefactor;
                                        this.endYscaled = (this.endY - this.yoffset) * scalefactor;
            
                                        this.endXscaled += pagedx;
                                        this.endYscaled += pagedy;
            
            
                                    }*/
            

                                    //markupobject.endX = retp3.x;
                                    //markupobject.endY = retp3.y;
                                    
                                    //markupobject.SetDimensions(scalefactor, pagerotation, pagedx, pagedy);

                                    
                                }
            
                                //if ()
                            
                            } else if (markupobject.setthird) {
                                // Two points placed, drawing arc through all three points
                                let labelpos = RenderUtils.drawArc(ctx,p1,p2, p3,markupobject.strokecolor, markupobject.scaling, linewidthScaled);

                                mesureobj = RenderUtils.calculateArcMeasurements(p1us, p2us, p3us);

                                markupobject.setarcvalue(mesureobj.radius, mesureobj.arcLength);

                                const labelText = `R${markupobject.dimradius}\nL${markupobject.dimlength}`;

                                RenderUtils.drawArcLabelFromText(ctx, labelpos.labelposx, labelpos.labelposy, labelText);


                                //mesureobj.radius = getUnitlength(mesureobj.radius / markupobject.scaling);
                                //mesureobj.arcLength = getUnitlength(mesureobj.arcLength / markupobject.scaling);

                                //RenderUtils.drawArcLabel(ctx, labelpos.labelposx, labelpos.labelposy, mesureobj.radius, mesureobj.arcLength,Unitlabel);

                            }

                            if (markupobject.selectedit) {
                                markupobject.editpolygon(ctx, scalefactor);
                            }

                            //var p1 = {x : markupobject.startXscaled, y:markupobject.startYscaled};
                            //var p2 = {x : markupobject.secondXscaled, y:markupobject.secondYscaled};


                            
                            //markupobject.renderUtils.drawArc(ctx, p1, p2, p3);

                            //markupobject.drawArc(ctx, markupobject.subtype, markupobject.outerRadius, markupobject.innerRadius, markupobject.linewidth, markupobject.fillcolor, markupobject.strokecolor, scalefactor, pagedx, pagedy);
                                       
                            break;
                            
                    }

                    if (pagerotation != 0) {
                        ctx.restore();
                        bcnvRotated = false;
                    }
                };
                this.rotatecnvs = function(ctx, x, y, w, h, rotation){

                    if(rotation == 0){
                        return;
                    }else{
                        var tx = x;
                        var ty = y;
                        tx = x + (w / 2);
                        ty = y + (h / 2);
                        ctx.translate(tx, ty);
                        ctx.rotate(markupobject.rotation);
                        ctx.translate(-tx, -ty);
                    }
                };
                this.drawbirdseye = function (thumbnailobject) {
                    if (thumbnailobject.birdseyemarkupctx == null) {
                        return;
                    }
                    if (thumbnailobject.birdseyeobj.birdseye == null) {
                        return;
                    }
                    if(markupobject.bfor3D){
                        return;
                    }
                    //ctx.clearRect(0, 0, canvasowidth, canvasoheight);

                    var offsetx = 0;
                    var offsety = 0;

                    if (thumbnailobject.birdseyeobj.rotation != 0) {
                        var txcanv = (thumbnailobject.birdseyeobj.birdseye.width / 2);
                        var tycanv = (thumbnailobject.birdseyeobj.birdseye.height / 2);

                        if(thumbnailobject.birdseyeobj.rotation == 90){
                            offsetx = txcanv - tycanv;
                            offsety = txcanv - tycanv;
                        }else if (thumbnailobject.birdseyeobj.rotation == 270){
                            offsetx = tycanv - txcanv;
                            offsety = tycanv - txcanv;

                        }


                        if(thumbnailobject.birdseyeobj.ignorerotation){
                            offsetx = 0;
                            offsety = 0;

                            if(thumbnailobject.birdseyeobj.rotation == 90){
                                //offsetx = tycanv - txcanv;
                                offsetx = txcanv - tycanv;
                                offsety = tycanv - txcanv;
                            }else if (thumbnailobject.birdseyeobj.rotation == 270){
                                offsetx = txcanv - tycanv;
                                offsety = -(txcanv - tycanv);
    
                            }
    
                        } 


                        thumbnailobject.birdseyemarkupctx.save();
                        thumbnailobject.birdseyemarkupctx.translate(txcanv, tycanv);
                        //thumbnailobject.birdseyemarkupctx.rotate(thumbnailobject.birdseyeobj.birdseye.rotation * (Math.PI / 180));
                        thumbnailobject.birdseyemarkupctx.rotate(thumbnailobject.birdseyeobj.rotation * (Math.PI / 180));
                        thumbnailobject.birdseyemarkupctx.translate(-txcanv, -tycanv);
                    }
                    //this.drawprint(thumbnailobject.birdseyemarkupctx, 0, 0, thumbnailobject.birdseyeobj.dscalebirdseye, thumbnailobject.birdseyeobj.rotation, true);
                    this.drawprint(thumbnailobject.birdseyemarkupctx, offsetx, offsety, thumbnailobject.birdseyeobj.dscalebirdseye, thumbnailobject.birdseyeobj.rotation, true);

                    if (thumbnailobject.birdseyeobj.rotation != 0) {
                        thumbnailobject.birdseyemarkupctx.restore();
                    }

                    //thumbnailobject.birdseyemarkupctx.restore();
                };
                this.drawfloorplan = function(ctx, scale, dx, dy){
                    this.drawprint(ctx, dx, dy, scale, 0, false);
                };
                this.drawWidthHeight = function(ctx, width, height){

                    //var dscale = height / canvasoheight;

                    if(markupobject.bfor3D){
                        var dscale = height / markupobject.pageheight;
                        var cscale = width / markupobject.pagewidth;
                    }

                    var dyadd = 0;
                    var dxadd = 0;
                    var dxscaled = (canvasowidth * cscale);
                    //var dyscaled = (canvasoheight * dscale) * 0.5;
                    var dyscaled = (canvasoheight * dscale);

                    var dx = width * 0.5;
                    var dy = height * 0.5;

                    if(dyscaled > height){
                        dyadd = (dyscaled - height) * 0.5;
                    }else{
                        dyadd = (height - dyscaled) * 0.5;
                    }

                    if(dxscaled > width){
                        dxadd = (width - dxscaled) * 0.5;
                    }else{
                        dxadd = (dxscaled - width) * 0.5;
                        
                    }

                    //dx = dx + dxadd;
                    //dy = dy + dyadd;


                    if(this.type == 9 && this.selectedit){
                        return;
                    }else{
                        this.drawprint(ctx, dx, dy, dscale, 0, true);
                    }

                };

                this.drawthumb = function (ctx) {
                    //this.drawprint(ctx,DocObj.getCurrentpage().dxthumb,DocObj.getCurrentpage().dythumb,DocObj.getCurrentpage().dscalethumb);

                    
                    if(DocObj.getpageThumb(markupobject.pagenumber)){

                        var thumbnailobject = DocObj.getpageThumb(markupobject.pagenumber);
                        //var rotation = DocObj.getCurrentThumb().thumbnailobj.rotation;
                        var rotation = thumbnailobject.thumbnailobj.rotation;


                        var offsetx = thumbnailobject.dxthumb;
                        var offsety = thumbnailobject.dythumb;

                        var width = thumbnailobject.thumbcanvas.width;
                        var height = thumbnailobject.thumbcanvas.height;

    
                        if (rotation != 0) {

                            /*if(rotation == 90 || rotation == 270){
                                var tempwidth = width;
                                width = height;
                                height = tempwidth;
                            }*/
    
                            var txcanv = (width / 2);
                            var tycanv = (height / 2);
    
                            if(rotation == 90){
                                offsetx = txcanv - tycanv;
                                offsety = tycanv - txcanv;
                            }else if (rotation == 270){
                                offsetx = txcanv - tycanv;
                                offsety = tycanv - txcanv;
    
                            }
        
                            /*if(thumbnailobject.birdseyeobj.ignorerotation){
                                offsetx = 0;
                                offsety = 0;
    
                                if(thumbnailobject.birdseyeobj.rotation == 90){
                                    //offsetx = tycanv - txcanv;
                                    offsetx = txcanv - tycanv;
                                    offsety = tycanv - txcanv;
                                }else if (thumbnailobject.birdseyeobj.rotation == 270){
                                    offsetx = txcanv - tycanv;
                                    offsety = -(txcanv - tycanv);
        
                                }
        
                            }*/ 
    
    
                            ctx.save();
                            ctx.translate(txcanv, tycanv);
                            //thumbnailobject.birdseyemarkupctx.rotate(thumbnailobject.birdseyeobj.birdseye.rotation * (Math.PI / 180));
                            ctx.rotate(rotation * (Math.PI / 180));
                            ctx.translate(-txcanv, -tycanv);
                        }
    


                        this.drawprint(ctx, offsetx, offsety, thumbnailobject.dscalethumb, rotation, true);

                        if (rotation != 0) {
                            ctx.restore();
                        }
    
                    }


                };
                this.drawprint = function (ctx, dx, dy, dscale, drotation, bthumbnail) {

                    if(markupobject.noDraw){
                        return;
                    }

                    if(markupobject.pdfannotproxy){
                        return;
                    }


                    markupobject.fixedscaleFactor = dscale / DocObj.getCurrentpage().fixedScale;
                    var scalefactor = dscale / markupobject.scaling;
                    var pagedx = dx;
                    var pagedy = dy;
                    //var scalefactor = DocObj.getCurrentpage().dscalethumb / markupobject.scaling;
                    //var pagedx = DocObj.getCurrentpage().dxthumb;
                    //var pagedy = DocObj.getCurrentpage().dythumb;
                    var xscalepoint = 0;
                    var yscalepoint = 0;
                    if (markupobject.bUseFixedScale && !bthumbnail) {
                        var arrowlengthscaled = this.arrowlength * markupobject.fixedscaleFactor;
                    }
                    else {
                        arrowlengthscaled = this.arrowlength * scalefactor;
                    }
                    if (markupobject.bUseFixedScale && !bthumbnail) {
                        var radiusScaled = 10 * markupobject.fixedscaleFactor;
                    }
                    else {
                        radiusScaled = 10 * scalefactor;
                    }
                    var textoffsetScaled = (this.h / 10) * scalefactor;
                    if (markupobject.bUseFixedScale && !bthumbnail) {
                        var linewidthScaled = this.linewidth * markupobject.fixedscaleFactor;
                    }
                    else {
                        linewidthScaled = this.linewidth * scalefactor;
                    }
                    var leaderOffsetScalded = this.leaderoffset * scalefactor;
                    var tx = 0;
                    var ty = 0;

                    var ptrn = null;

                    if(markupobject.hatchStyle != undefined){
                        updateHatchColor(markupobject.hatchStyle, this.color);
                        ptrn = getHatchbyName(ctx, getHatchNameforType(markupobject.hatchStyle));
                    }

                    //var ptrn = getHatch(ctx, markupobject.hatchStyle, this.color);
                    if (markupobject.bUseFixedScale && !bthumbnail) {
                        var linewidthScaled = this.linewidth * markupobject.fixedscaleFactor;
                        var crossl = 5 * markupobject.fixedscaleFactor;
                    }
                    else {
                        linewidthScaled = this.linewidth * scalefactor;
                        crossl = 5 * scalefactor;
                    }
                    switch (this.type) {
                        case 0: //pencil, marker, eraser
                            var counter = 0;
                            var lcounter = 0;
                            //markupobject.SetDimensions(scalefactor,0,pagedx,pagedy);
                            markupobject.SetDimensions(scalefactor, drotation, pagedx, pagedy);
                            ctx.save();
                            if (this.subtype == 1) {
                                ctx.strokeStyle = "white";
                                /*if(backgroundColor != undefined){
                                    ctx.strokeStyle = backgroundColor;
                                }else{
                                    ctx.strokeStyle = "white";
                                }*/
                                ctx.lineWidth = this.linewidth * 10 * scalefactor;
                            }
                            else {
                                ctx.strokeStyle = this.strokecolor;
                                ctx.lineWidth = this.linewidth * scalefactor;
                                markupobject.GetLinestyle(markupobject.linestyle, ctx, markupobject.fixedscaleFactor);
                            }
                            ctx.beginPath();
                            for (lcounter = 0; lcounter < this.pointlist.length; lcounter++) {
                                for (counter = 0; counter < this.pointlist[lcounter].length; counter++) {
                                    xscalepoint = (this.pointlist[lcounter][counter].x - this.xoffset) * scalefactor;
                                    yscalepoint = (this.pointlist[lcounter][counter].y - this.yoffset) * scalefactor;
                                    xscalepoint += dx;
                                    yscalepoint += dy;
                                    if (counter == 0) {
                                        ctx.moveTo(xscalepoint, yscalepoint);
                                    }
                                    else {
                                        ctx.lineTo(xscalepoint, yscalepoint);
                                    }
                                }
                            }
                            ctx.stroke();
                            ctx.restore();
                            break;
                        case 1: //polygon
                            var pcounter = 0;
                            markupobject.SetDimensions(scalefactor, drotation, pagedx, pagedy);
                            switch (this.subtype) {
                                case 1:
                                    ctx.save();
                                    markupobject.GetLinestyle(markupobject.linestyle, ctx, markupobject.fixedscaleFactor);
                                    markupobject.polyline(ctx, markupobject.linewidth, markupobject.strokecolor, scalefactor, dx, dy, true);
                                    ctx.restore();
                                    break;
                                case 2:
                                    if (this.alternative == 0) {
                                        ctx.save();
                                        markupobject.GetLinestyle(markupobject.linestyle, ctx, markupobject.fixedscaleFactor);
                                        markupobject.polygon(ctx, markupobject.linewidth, false, true, markupobject.fillcolor, markupobject.strokecolor, scalefactor, dx, dy, true);
                                        ctx.restore();
                                    }
                                    if (this.alternative == 1) {
                                        markupobject.polygon(ctx, markupobject.linewidth, true, true, markupobject.fillcolor, markupobject.strokecolor, scalefactor, dx, dy, true);
                                    }
                                    if (this.alternative == 2) {
                                        markupobject.polygon(ctx, markupobject.linewidth, true, true, "white", markupobject.color, scalefactor, dx, dy, true);
                                    }
                                    if (this.alternative >= 3) {
                                        if (ptrn != null){
                                            markupobject.polygon(ctx, markupobject.linewidth, true, true, ptrn, markupobject.color, scalefactor, dx, dy, true);
                                        }else{
                                            markupobject.polygon(ctx, markupobject.linewidth, true, true, "white", markupobject.color, scalefactor, dx, dy, true);
                                        }
                                        //markupobject.polygon(ctx, markupobject.linewidth, true, true, ptrn, markupobject.color, scalefactor, dx, dy, true);
                                    }
                                    break;
                                case 3:
                                    ctx.save();
                                    markupobject.GetLinestyle(markupobject.linestyle, ctx, markupobject.fixedscaleFactor);
                                    markupobject.polyline(ctx, markupobject.linewidth, markupobject.strokecolor, scalefactor, dx, dy, true);
                                    ctx.restore();
                                    markupobject.dimtext = markupobject.setdimvaluepoly();
                                    if (markupobject.bUseFixedScale && !bthumbnail) {
                                        markupobject.dimvaluedraw(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, this.strokecolor, markupobject.fixedscaleFactor, false);
                                    }
                                    else {
                                        markupobject.dimvaluedraw(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, this.strokecolor, scalefactor, false);
                                    }
                                    break;

                                case 4:

                                    ctx.save();
                                    markupobject.GetLinestyle(markupobject.linestyle, ctx, markupobject.fixedscaleFactor);
                                    markupobject.dimtext = markupobject.drawangle(ctx, markupobject.linewidth, markupobject.strokecolor, scalefactor, pagedx, pagedy, true, false);
                                    ctx.restore();

                                    if(!bMarkupNoLabel && !markupobject.hidevaluelabel){
                                        if (markupobject.bUseFixedScale) {

                                            if(markupobject.dimtext != ""){
                                                markupobject.dimvaluedraw(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, this.strokecolor, markupobject.fixedscaleFactor, false);
                                            }

                                            
                                        }
                                        else {
                                            if(markupobject.dimtext != ""){
                                                markupobject.dimvaluedraw(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, this.strokecolor, scalefactor, false);
                                            }
                                            
                                        }
                                    }


                                    break;

                                case 5:                                    

                                    ctx.save();
                                    markupobject.GetLinestyle(markupobject.linestyle, ctx, markupobject.fixedscaleFactor);
                                    markupobject.dimtext = markupobject.drawangle(ctx, markupobject.linewidth, markupobject.strokecolor, scalefactor, pagedx, pagedy, true, true);
                                    ctx.restore();

                                    if(!bMarkupNoLabel && !markupobject.hidevaluelabel){
                                        if (markupobject.bUseFixedScale) {
                                            if(markupobject.dimtext != ""){
                                                markupobject.dimvaluedraw(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, this.strokecolor, markupobject.fixedscaleFactor, false);
                                            }
                                        }
                                        else {
                                            if(markupobject.dimtext != ""){
                                                markupobject.dimvaluedraw(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, this.strokecolor, scalefactor, false);
                                            }
                                            
                                        }
                                    }


                                    break;
                                case 6:    

                                    ctx.save();
                                    markupobject.GetLinestyle(markupobject.linestyle, ctx, markupobject.fixedscaleFactor);
                                    markupobject.polylineSegments(ctx, markupobject.linewidth, markupobject.strokecolor, scalefactor, pagedx, pagedy, true);
                                    markupobject.polylineSegments(ctx, markupobject.linewidthSegment, markupobject.strokecolorSegment, scalefactor, pagedx, pagedy, true);

                                    ctx.restore();

                                    markupobject.dimtext = markupobject.setdimvaluepolySegments();

                                    if(!bMarkupNoLabel && !markupobject.hidevaluelabel){
                                        if (markupobject.bUseFixedScale) {
                                            //markupobject.dimvaluedraw(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, this.strokecolor, markupobject.fixedscaleFactor, false);
                                        }
                                        else {
                                            //markupobject.dimvaluedraw(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, this.strokecolor, scalefactor, false);
                                        }
                                    }



                                    break;
                            }
                            break;
                        case 2:
                            var curvcounter = 0;
                            markupobject.SetDimensions(scalefactor, drotation, pagedx, pagedy);
                            switch (this.subtype) {
                                case 1:
                                    markupobject.polycurves(ctx, markupobject.linewidth, markupobject.color, scalefactor);
                                    break;
                            }
                            break;
                        case 3: //rectangle
                            markupobject.SetDimensions(scalefactor, drotation, pagedx, pagedy);
                            if (this.subtype == 2) {
                                if (DocObj.backgroundColor != undefined) {
                                    var erasecolor = DocObj.backgroundColor;
                                }
                                else {
                                    erasecolor = "white";
                                }
                                markupobject.Rect(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, linewidthScaled, true, false, erasecolor, erasecolor);
                                //ctx.lineWidth = this.linewidth * 10 * scalefactor;
                            }
                            if (this.subtype == 4) {
                                markupobject.Rect(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, linewidthScaled, true, true, markupobject.fillcolor, markupobject.fillcolor);
                            }
                            if (this.subtype == 3) {
                                markupobject.Rect(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, linewidthScaled, true, false, markupobject.fillcolor, markupobject.fillcolor);
                            }
                            if (this.subtype == 1) {
                                //
                                if (this.alternative == 0) {
                                    ctx.save();
                                    markupobject.GetLinestyle(markupobject.linestyle, ctx, markupobject.fixedscaleFactor);
                                    markupobject.roundedRect(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, markupobject.rotation, radiusScaled, linewidthScaled, false, true, markupobject.fillcolor, markupobject.strokecolor);
                                    ctx.restore();
                                }
                                if (this.alternative == 1) {
                                    ctx.save();
                                    markupobject.GetLinestyle(markupobject.linestyle, ctx, markupobject.fixedscaleFactor);
                                    markupobject.roundedRect(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, markupobject.rotation, radiusScaled, linewidthScaled, true, true, markupobject.fillcolor, markupobject.strokecolor);
                                    ctx.restore();
                                }
                                if (this.alternative == 2) {
                                    markupobject.roundedRect(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, markupobject.rotation, radiusScaled, linewidthScaled, true, true, "white", this.color);
                                }
                                if (this.alternative >= 3) {
                                    
                                    if (ptrn != null){
                                        markupobject.roundedRect(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, markupobject.rotation, radiusScaled, linewidthScaled, true, true, ptrn, this.color);
                                    }else{
                                        markupobject.roundedRect(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, markupobject.rotation, radiusScaled, linewidthScaled, true, true, "white", this.color);
                                    }
                                    //markupobject.roundedRect(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, markupobject.rotation, radiusScaled, linewidthScaled, true, true, ptrn, this.color);
                                }
                            }
                            if (this.subtype == 0) {
                                if (this.alternative == 0) {
                                    ctx.save();
                                    markupobject.GetLinestyle(markupobject.linestyle, ctx, markupobject.fixedscaleFactor);
                                    markupobject.Rect(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, linewidthScaled, false, true, markupobject.fillcolor, markupobject.strokecolor);
                                    ctx.restore();
                                }
                                if (this.alternative == 1) {
                                    ctx.save();
                                    markupobject.GetLinestyle(markupobject.linestyle, ctx, markupobject.fixedscaleFactor);
                                    markupobject.Rect(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, linewidthScaled, true, true, markupobject.fillcolor, markupobject.strokecolor);
                                    ctx.restore();
                                }
                                if (this.alternative == 2) {
                                    markupobject.Rect(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, linewidthScaled, true, true, "white", this.color);
                                }
                                if (this.alternative >= 3) {
                                    //var ptrn = ctx.createPattern(hatchdiagforw,'repeat');
                                    //markupobject.Rect(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, linewidthScaled, true, true, ptrn, this.color);
                                    if (ptrn != null){
                                        markupobject.Rect(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, linewidthScaled, true, true, ptrn, this.color);    
                                    }else{
                                        markupobject.Rect(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, linewidthScaled, true, true, "white", this.color);
                                    }
                                }
                            }
                            if (this.subtype == 6) {
                                markupobject.Rect(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, linewidthScaled, false, true, markupobject.fillcolor, markupobject.strokecolor);
                                markupobject.dimtext = markupobject.setdimvaluearea();

                                if(!bMarkupNoLabel && !markupobject.hidevaluelabel){
                                    if (markupobject.bUseFixedScale) {
                                        markupobject.dimvaluedraw(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, this.strokecolor, markupobject.fixedscaleFactor, false);
                                    }
                                    else {
                                        markupobject.dimvaluedraw(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, this.strokecolor, scalefactor, false);
                                    }
                                }

                            }


                            break;
                        case 4: //oval
                            markupobject.SetDimensions(scalefactor, drotation, pagedx, pagedy);
                            if (this.alternative == 0) {
                                ctx.save();
                                markupobject.GetLinestyle(markupobject.linestyle, ctx, markupobject.fixedscaleFactor);
                                if (this.subtype == 1) {
                                    markupobject.Circle(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, linewidthScaled, false, true, markupobject.fillcolor, markupobject.strokecolor);
                                    markupobject.drawradius(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, crossl, linewidthScaled, markupobject.strokecolor, false);
                                }
                                else {
                                    markupobject.Oval(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, linewidthScaled, false, true, markupobject.fillcolor, markupobject.strokecolor);
                                }
                                ctx.restore();
                            }
                            if (this.alternative == 1) {
                                ctx.save();
                                markupobject.GetLinestyle(markupobject.linestyle, ctx, markupobject.fixedscaleFactor);
                                if (this.subtype == 1) {
                                    markupobject.Circle(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, linewidthScaled, true, true, markupobject.fillcolor, markupobject.strokecolor);
                                    markupobject.drawradius(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, crossl, linewidthScaled, markupobject.strokecolor, false);
                                }
                                else {
                                    markupobject.Oval(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, linewidthScaled, true, true, markupobject.fillcolor, markupobject.strokecolor);
                                }
                                ctx.restore();
                            }
                            if (this.alternative == 2) {
                                markupobject.Oval(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, linewidthScaled, true, true, "white", this.color);
                            }
                            if (this.alternative >= 3) {
                                
                                if (ptrn != null){
                                    markupobject.Oval(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, linewidthScaled, true, true, ptrn, this.color);
                                }else{
                                    markupobject.Oval(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, linewidthScaled, true, true, "white", this.color);
                                }
                                //markupobject.Oval(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, linewidthScaled, true, true, ptrn, this.color);
                            }
                            break;
                        case 5:
                            markupobject.SetDimensions(scalefactor, drotation, pagedx, pagedy);
                            if (this.alternative == 0) {
                                ctx.save();
                                markupobject.GetLinestyle(markupobject.linestyle, ctx, markupobject.fixedscaleFactor);
                                markupobject.newcloud(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, radiusScaled, linewidthScaled, false, true, markupobject.fillcolor, markupobject.strokecolor);
                                ctx.restore();
                            }
                            if (this.alternative == 1) {
                                ctx.save();
                                markupobject.GetLinestyle(markupobject.linestyle, ctx, markupobject.fixedscaleFactor);
                                markupobject.newcloud(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, radiusScaled, linewidthScaled, true, true, markupobject.fillcolor, markupobject.strokecolor);
                                ctx.restore();
                            }
                            if (this.alternative == 2) {
                                markupobject.newcloud(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, radiusScaled, linewidthScaled, true, true, "white", this.color);
                            }
                            if (this.alternative >= 3) {
                                //markupobject.newcloud(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, radiusScaled, linewidthScaled, true, true, ptrn, this.color);
                                if (ptrn != null){
                                    markupobject.newcloud(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, radiusScaled, linewidthScaled, true, true, ptrn, this.color);
                                }else{
                                    markupobject.newcloud(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, radiusScaled, linewidthScaled, true, true, "white", this.color);
                                }

                            }
                            break;
                        case 6: //line arrow
                            markupobject.SetDimensions(scalefactor, drotation, pagedx, pagedy);
                            ctx.save();
                            markupobject.GetLinestyle(markupobject.linestyle, ctx, markupobject.fixedscaleFactor);
                            markupobject.arrow(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, arrowlengthscaled, arrowangle, linewidthScaled, this.subtype, markupobject.strokecolor, markupobject.strokecolor);
                            ctx.restore();
                            break;
                        case 7: //dimension line
                            markupobject.SetDimensions(scalefactor, drotation, pagedx, pagedy);
                            var dimtype = this.subtype + 4;
                            ctx.save();
                            markupobject.GetLinestyle(markupobject.linestyle, ctx, markupobject.fixedscaleFactor);
                            //markupobject.arrow(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, arrowlengthscaled, arrowangle, linewidthScaled, dimtype, markupobject.strokecolor, markupobject.strokecolor);
                            markupobject.labelpos = markupobject.dimensionLeader(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, leaderOffsetScalded, arrowlengthscaled, arrowangle, linewidthScaled, dimtype, markupobject.strokecolor, markupobject.strokecolor);
                            ctx.restore();
                            
                            markupobject.setdimvalue(this.x, this.y, this.w, this.h);


                            if (markupobject.bUseFixedScale && !bthumbnail) {
                                if (this.leaderoffset == 0) {
                                    markupobject.dimvaluedraw(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, this.strokecolor, markupobject.fixedscaleFactor, true);
                                }
                                else {
                                    markupobject.dimvaluedraw(ctx, this.labelpos.x, this.labelpos.y, this.labelpos.w, this.labelpos.h, this.strokecolor, markupobject.fixedscaleFactor, true);
                                }
                            }
                            else {
                                if (this.leaderoffset == 0) {
                                    markupobject.dimvaluedraw(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, this.strokecolor, scalefactor, true);
                                }
                                else {
                                    markupobject.dimvaluedraw(ctx, this.labelpos.x, this.labelpos.y, this.labelpos.w, this.labelpos.h, this.strokecolor, scalefactor, true);
                                }
                            }
                            break;
                        case 8:
                            var acounter = 0;
                            var dimarea = 0;
                            markupobject.SetDimensions(scalefactor, drotation, pagedx, pagedy);
                            if (markupobject.bUseFixedScale && !bthumbnail) {
                                var localscalefactor = markupobject.fixedscaleFactor;
                            }
                            else {
                                localscalefactor = scalefactor;
                            }
                            if (!markupobject.spaceselected && markupobject.subtype == 1) {
                                markupobject.linestyle = 0;
                            }
                            if (markupobject.spaceselected && markupobject.subtype == 1) {
                                ctx.save();
                                var grd = ctx.createLinearGradient(this.xscaled, this.yscaled, this.wscaled, this.hscaled);
                                grd.addColorStop(0, markupobject.selectSpaceColor);
                                grd.addColorStop(.5, markupobject.selectSpaceStopColor);
                                grd.addColorStop(1, markupobject.selectSpaceColor);
                                markupobject.GetLinestyle(markupobject.linestyle, ctx, markupobject.fixedscaleFactor);
                                markupobject.polygon(ctx, markupobject.linewidth, true, true, grd, markupobject.strokecolor, scalefactor, pagedx, pagedy, true);
                                /*if(markupobject.linestyle == 6 && bAnimateLineStyle){
                                    markupobject.polygon(ctx, markupobject.linewidth, false, true, markupobject.selectSpaceColor, markupobject.strokecolor, scalefactor,pagedx, pagedy,true);
                                }else{
                                    
                                }*/
                                ctx.restore();
                            }
                            /*if(bUseFixedScale && !bthumbnail){
                                markupobject.dimvaluedraw(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, this.strokecolor, markupobject.fixedscaleFactor,true);
                            }else{
                                markupobject.dimvaluedraw(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, this.strokecolor, scalefactor,true);
                            }*/
                            if (this.alternative == 0) {
                                ctx.save();
                                markupobject.GetLinestyle(markupobject.linestyle, ctx, markupobject.fixedscaleFactor);
                                markupobject.polygon(ctx, markupobject.linewidth, false, true, markupobject.fillcolor, markupobject.strokecolor, scalefactor, pagedx, pagedy, true);
                                ctx.restore();
                            }
                            if (this.alternative == 1) {
                                ctx.save();
                                markupobject.GetLinestyle(markupobject.linestyle, ctx, markupobject.fixedscaleFactor);
                                markupobject.polygon(ctx, markupobject.linewidth, true, true, markupobject.fillcolor, markupobject.strokecolor, scalefactor, pagedx, pagedy, true);
                                ctx.restore();
                            }
                            if (this.alternative == 2) {
                                markupobject.polygon(ctx, markupobject.linewidth, true, true, "white", markupobject.color, scalefactor, pagedx, pagedy, true);
                            }
                            if (this.alternative >= 3) {
                                //markupobject.polygon(ctx, markupobject.linewidth, true, true, ptrn, markupobject.color, scalefactor, pagedx, pagedy, true);

                                if (ptrn != null){
                                    markupobject.polygon(ctx, markupobject.linewidth, true, true, ptrn, markupobject.color, scalefactor, pagedx, pagedy, true);
                                }else{
                                    markupobject.polygon(ctx, markupobject.linewidth, true, true, "white", markupobject.color, scalefactor, pagedx, pagedy, true);
                                }


                            }
                            
                            if(markupobject.bshowperimeter){
                                markupobject.dimtext = markupobject.setdimvalueperimeter();
                            }else{
                                markupobject.dimtext = markupobject.setdimvaluearea();
                                if (markupobject.subtype == 1) {
                                    markupobject.updateAttribute('Rx_Area', this.dimtext);
                                }

                            }
                            
                            /*var markupscalesq = this.scaling * this.scaling;
                            dimarea = markupobject.PolygonArea();
                            this.dimtext = getUnitArea(dimarea / markupscalesq);
                            this.dimtext = this.dimtext.toFixed(2);
                            this.dimtext = this.dimtext + " " + AreaUnitlabel;*/


                            var centroidpt = get_polygon_centroid(this.points);

                            var areatextx = centroidpt.x;
                            var areatexty = centroidpt.y;

                            //var areatextx = markupobject.PolygonCentre('x');
                            //var areatexty = markupobject.PolygonCentre('y');


                            var areatextxscaled = (areatextx - this.xoffset) * scalefactor;
                            var areatextyscaled = (areatexty - this.yoffset) * scalefactor;
                            areatextxscaled += dx;
                            areatextyscaled += dy;
                            var areatextscaled = this.measuretextheight * scalefactor;
                            if (markupobject.bUseFixedScale && !bthumbnail) {
                                areatextscaled = this.measuretextheight * markupobject.fixedscaleFactor;
                            }
                            if (!bMarkupNoLabel && markupobject.busecustomlabel && !markupobject.hidevaluelabel) {
                                markupobject.areavaluedraw(ctx, markupobject.customlabeltext, areatextxscaled, areatextyscaled, markupobject.strokecolor, areatextscaled, localscalefactor, scalefactor);
                            }
                            if (!bMarkupNoLabel && markupobject.subtype != 1 && !markupobject.hidevaluelabel) {
                                //markupobject.areavaluedraw(ctx, this.dimtext, areatextxscaled, areatextyscaled, this.strokecolor, areatextscaled, localscalefactor);
                                markupobject.areavaluedraw(ctx, markupobject.dimtext, areatextxscaled, areatextyscaled, markupobject.strokecolor, areatextscaled, localscalefactor, scalefactor);
                                /*var areatextscaled = this.measuretextheight * scalefactor;
        
                                if (bUseFixedScale && !bthumbnail){
                                    areatextscaled = this.measuretextheight * markupobject.fixedscaleFactor;
                                }*/
                                /*ctx.textAlign  = "start";
                                ctx.font = areatextscaled + "pt " + "Helvetica";
                                this.textheight = this.measuretextheight;
        
                                var areat = ctx.measureText(this.dimtext);
                                var areatextwidth = areat.width;
                                var areatextheight = areatextscaled;
                                areatextxscaled = areatextxscaled - (areatextwidth/2);
                                areatextyscaled = areatextyscaled + (areatextheight/2);
                                ctx.lineWidth = 1;
                                ctx.strokeStyle=this.color;
                                ctx.fillStyle = "white";
                                ctx.fillRect(areatextxscaled-(10*localscalefactor), areatextyscaled-(20*localscalefactor),areatextwidth + (20*localscalefactor),areatextscaled + (15*localscalefactor));
                                ctx.strokeRect(areatextxscaled-(10*localscalefactor), areatextyscaled-(20*localscalefactor),areatextwidth + (20*localscalefactor),areatextscaled + (15*localscalefactor));*/
                                /*ctx.fillStyle = "black";
                                ctx.fillText(this.dimtext, areatextxscaled, areatextyscaled);*/
                            }
                            break;
                        case 9: //text
                            markupobject.SetDimensions(scalefactor, drotation, pagedx, pagedy);
                            if (markupobject.selectedit) {
                                break;
                            }
                            if (markupobject.bUseFixedScale && !bthumbnail) {
                                var temptxtscale = markupobject.fixedscaleFactor;
                            }
                            else {
                                temptxtscale = scalefactor;
                            }
                            this.font.setScale(temptxtscale);
                            var textscaled = this.font.height * temptxtscale;
                            ctx.font = this.font.fontstringScaled;
                            var dimsel = ctx.measureText(this.text);
                            this.textwidth = dimsel.width;
                            //draw text selected goes here.
                            ctx.save();
                            tx = this.xscaled;
                            ty = this.yscaled;
                            if (markupobject.textrotate != 0 && markupobject.subtype == 0) {
                                ctx.translate(tx, ty);
                                ctx.rotate(markupobject.textrotate);
                                ctx.translate(-tx, -ty);
                            }
                            if (markupobject.rotation != 0) {
                                tx = this.xscaled + (this.wscaled / 2);
                                if (markupobject.subtype == 0) {
                                    ty = this.yscaled - (this.hscaled / 2);
                                }
                                else {
                                    ty = this.yscaled + (this.hscaled / 2);
                                }
                                ctx.translate(tx, ty);
                                ctx.rotate(markupobject.rotation);
                                ctx.translate(-tx, -ty);
                            }


                            ctx.textAlign = "start";
                            ctx.fillStyle = markupobject.textcolor;
                            if (this.subtype == 1 || this.subtype == 2) {
                                var toffsetx = 4 * temptxtscale;
                                //var toffsety = 8*scalefactor;
                                //var toffsety = ((this.font.height / 4) * 1.3) * scalefactor;
                                
                                //var toffsety = ((this.font.height / 4) * 2.0) * temptxtscale;


                                //var toffsetx = 4 * temptxtscale;
                                //var toffsety = 2*scalefactor;

                                /*if (this.bhasArrow) {
                                    var toffsety = ((this.font.height / 8) * 2.0) * temptxtscale;
                                }else{
                                    toffsety = ((this.font.height / 4) * 2.0) * temptxtscale;
                                }*/


                                var toffsety = (this.font.height * lineheightfactor) * temptxtscale;


                                var toffsetystart = ((this.font.height / 10) * 2.0) * temptxtscale;




                                if (markupobject.linewidth == 0) {
                                    markupobject.Rect(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, linewidthScaled, true, false, markupobject.fillcolor, markupobject.strokecolor);
                                }
                                else {
                                    ctx.save();
                                    markupobject.GetLinestyle(markupobject.linestyle, ctx, markupobject.fixedscaleFactor);
                                    markupobject.Rect(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, linewidthScaled, true, true, markupobject.fillcolor, markupobject.strokecolor);
                                    ctx.restore();
                                }
                                ctx.save();
                                ctx.beginPath();
                                ctx.rect(this.xscaled, this.yscaled, this.wscaled, this.hscaled);
                                ctx.clip();
                                var textarray = this.text.split('\n');
                                //var ystart = this.yscaled + textscaled + toffsety;
                                var ystart = this.yscaled + textscaled + toffsetystart;
                                var ttx = this.xscaled; // + (this.wscaled);
                                var tty = this.yscaled; //+ (this.hscaled/2);
                                var txtrotate = markupobject.textrotate * (180 / Math.PI);
                                switch (markupobject.pagerotation) {
                                    case 0:
                                        var startx = this.xscaled + toffsetx;
                                        var maxwidth = this.wscaled;
                                        break;
                                    case 90:
                                        startx = (this.xscaled + toffsetx) - this.hscaled;
                                        ctx.translate(ttx, tty);
                                        ctx.rotate(-(markupobject.pagerotation * (Math.PI / 180)));
                                        ctx.translate(-ttx, -tty);
                                        maxwidth = this.hscaled;
                                        break;
                                    case 270:
                                        //startx = this.xscaled + toffsetx;
                                        startx = (this.xscaled + toffsetx); // + this.hscaled;
                                        //ystart = (this.yscaled + toffsety) - this.wscaled;
                                        //ystart = (this.yscaled + textscaled + toffsety) - this.wscaled;
                                        ystart = (this.yscaled + textscaled + toffsetystart) - this.wscaled;
                                        ctx.translate(ttx, tty);
                                        ctx.rotate(-(markupobject.pagerotation * (Math.PI / 180)));
                                        ctx.translate(-ttx, -tty);
                                        maxwidth = this.hscaled;
                                        break;
                                    case 180:
                                        startx = this.xscaled + toffsetx;
                                        ttx = this.xscaled + (this.wscaled / 2);
                                        tty = this.yscaled + (this.hscaled / 2);
                                        ctx.translate(ttx, tty);
                                        ctx.rotate(-(markupobject.pagerotation * (Math.PI / 180)));
                                        ctx.translate(-ttx, -tty);
                                        maxwidth = this.wscaled;
                                        break;
                                }
                                //wrapText(ctx, this.text, startx, ystart, maxwidth, textscaled + toffsety);
                                wrapText2(ctx, this.text, startx, ystart, maxwidth, textscaled + toffsety);
                                /*for (var i = 0; i < textarray.length; i++) {
        
                                    ctx.fillText(textarray[i], startx , ystart);
                                    ystart += textscaled + toffsety;
        
                                }*/
                                ctx.restore();
                            }
                            else {
                                ctx.fillText(this.text, this.xscaled, this.yscaled);
                            }
                            ctx.restore();
                            break;
                        case 10:

                            if(!bnoteimageloaded){
                                return;
                            }

                            markupobject.SetDimensions(scalefactor, drotation, pagedx, pagedy);
                            ctx.save();
                            markupobject.rotatecnvs(ctx, markupobject.xscaled, markupobject.yscaled, markupobject.wscaled, markupobject.hscaled, markupobject.rotation);

                            /*tx = this.xscaled;
                            ty = this.yscaled;
                            if (markupobject.rotation != 0) {
                                tx = this.xscaled + (this.wscaled / 2);
                                ty = this.yscaled - (this.hscaled / 2);
                                ctx.translate(tx, ty);
                                ctx.rotate(markupobject.rotation);
                                ctx.translate(-tx, -ty);
                            }*/
                            ctx.drawImage(noteimage, this.xscaled, this.yscaled, this.wscaled, this.hscaled);
                            ctx.restore();
                            break;
                        case 11:
                            if (!markupobject.imageloaded) {
                                break;
                            }
                            markupobject.SetDimensions(scalefactor, drotation, pagedx, pagedy);
                            ctx.save();

                            markupobject.rotatecnvs(ctx, markupobject.xscaled, markupobject.yscaled, markupobject.wscaled, markupobject.hscaled, markupobject.rotation);

                            /*tx = this.xscaled;
                            ty = this.yscaled;
                            if (markupobject.rotation != 0) {
                                tx = this.xscaled + (this.wscaled / 2);
                                ty = this.yscaled + (this.hscaled / 2);
                                ctx.translate(tx, ty);
                                ctx.rotate(markupobject.rotation);
                                ctx.translate(-tx, -ty);
                            }*/
                            ctx.drawImage(this.image, this.xscaled, this.yscaled, this.wscaled, this.hscaled);
                            ctx.restore();
                            break;
                        case 12:
                            markupobject.SetDimensions(scalefactor, drotation, pagedx, pagedy);
                            var textstampscaled = this.font.height * scalefactor;
                            this.font.setScale(scalefactor);
                            var textsmallstampscaled = markupobject.stampsmalltheight * scalefactor;
                            if (markupobject.bUseFixedScale && !bthumbnail) {
                                markupobject.roundedRect(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, markupobject.rotation, radiusScaled, 3 * markupobject.fixedscaleFactor, true, true, markupobject.fillcolor, markupobject.strokecolor);
                            }
                            else {
                                markupobject.roundedRect(ctx, this.xscaled, this.yscaled, this.wscaled, this.hscaled, markupobject.rotation, radiusScaled, 3 * scalefactor, true, true, markupobject.fillcolor, markupobject.strokecolor);
                            }
                            var Displayname = GetDisplayName(markupobject.signature);
                            var datetext = markupobject.GetDateTime(false);
                            //var smalltext = "By " + markupobject.signature + "," + " ";
                            this.smalltext = "By " + Displayname + "," + " ";
                            this.smalltext += datetext;
                            ctx.save();
                            if (markupobject.rotation != 0) {
                                tx = this.xscaled + (this.wscaled / 2);
                                ty = this.yscaled + (this.hscaled / 2);
                                ctx.translate(tx, ty);
                                ctx.rotate(markupobject.rotation);
                                ctx.translate(-tx, -ty);
                            }
                            ctx.fillStyle = markupobject.strokecolor;
                            ctx.font = markupobject.font.fontstringScaled;
                            var stampdim = ctx.measureText(this.text);
                            this.textwidth = stampdim.width;
                            tx = this.xscaled + (this.wscaled / 2);
                            //ty = this.yscaled+(this.hscaled/2);
                            if (this.alternative == 0) {
                                ty = this.yscaled + ((this.hscaled / 4) + (textstampscaled / 2));
                            }
                            else {
                                ty = this.yscaled + (this.hscaled / 2);
                                ty += (textstampscaled / 4);
                            }
                            //tx = this.xscaled+(this.wscaled/2);
                            //ty = this.yscaled+(this.hscaled/2);
                            if (markupobject.textrotate != 0) {
                                ctx.translate(tx, ty);
                                ctx.rotate(markupobject.textrotate);
                                ctx.translate(-tx, -ty);
                                tx = this.xscaled + (this.wscaled / 2);
                                //ty = this.yscaled+(this.hscaled/2);
                                if (this.alternative == 0) {
                                    ty = this.yscaled + ((this.hscaled / 4) + (textstampscaled / 2));
                                }
                                else {
                                    ty = this.yscaled + (this.hscaled / 2);
                                    ty += (textstampscaled / 4);
                                }
                            }
                            ctx.textAlign = 'center';
                            ctx.fillText(this.text, tx, ty);
                            if (this.alternative == 0) {
                                ctx.fillStyle = this.color;
                                ctx.font = "bold " + textsmallstampscaled + "pt " + "Times New Roman";
                                ty = this.yscaled + ((this.hscaled / 4) * 3);
                                ctx.fillText(this.smalltext, tx, ty);
                            }
                            ctx.restore();
                            break;
                        case 13:    

                            //markupobject.SetDimensions(scalefactor, DocObj.getCurrentpage().getpageRotation(), pagedx, pagedy);
                            markupobject.SetDimensions(scalefactor, drotation, pagedx, pagedy);
                            markupobject.shapes(ctx, markupobject.subtype, markupobject.outerRadius, markupobject.innerRadius, markupobject.linewidth, markupobject.fillcolor, markupobject.strokecolor, scalefactor, pagedx, pagedy);

                            /*if (markupobject.selectedit) {
                                markupobject.editpolygon(ctx, scalefactor);
                            }*/

                            break;

                        case 20:
                            if (!markupobject.imageloaded) {
                                break;
                            }
                            markupobject.SetDimensions(scalefactor, drotation, pagedx, pagedy);
                            ctx.save();

                            markupobject.rotatecnvs(ctx, markupobject.xscaled, markupobject.yscaled, markupobject.wscaled, markupobject.hscaled, markupobject.rotation);
                            ctx.drawImage(this.image, this.xscaled, this.yscaled, this.wscaled, this.hscaled);
                            ctx.restore();
                            break;



                    }
                };
                this.setinitRotatedMarkupPoint = function (width, height, x, y, anglerad) {
                    var cosangle = Math.cos(anglerad);
                    var sinangle = Math.sin(anglerad);
                    var hw = x - width;
                    var hh = y - height;
                    var newx = (hw * cosangle) - (hh * sinangle);
                    var newy = (hw * sinangle) + (hh * cosangle);
                    var transpoint = new point(newx, newy);
                    transpoint.x = width + transpoint.x;
                    transpoint.y = height + transpoint.y;
                    return transpoint;
                };
                /*this.setinitRotatedMarkuprect = function(width, height,absolute, anglerad){
                 var origx = this.x;
                 var origy = this.y;
                 var origw = this.w;
                 var origh = this.h;
        
                 var cosangle = Math.cos(anglerad);
                 var sinangle = Math.sin(anglerad);
        
        
                 var hw = origx - width;
                 var hh = origy - height;
        
                 var newx = (hw*cosangle) - (hh*sinangle);
                 var newy = (hw*sinangle) + (hh*cosangle);
        
                 //var transpoint = new point(newx,newy);
                 newx = width + newx;
                 newy = height + newy;
        
                 this.x = newx;
                 this.y = newy;
        
                 if (absolute){
                 hw = origw - width;
                 hh = origh - height;
                 }else{
                 hw = (origx + origw) - width;
                 hh = (origy + origh) - height;
                 }
        
                 var neww = (hw*cosangle) - (hh*sinangle);
                 var newh = (hw*sinangle) + (hh*cosangle);
        
                 neww = width + neww;
                 newh = height + newh;
        
        
                 this.w = neww;
                 this.h = newh;
        
                 };*/
                //transformed coordinates for rotated canvas.
                this.setRotatedMarkuprect = function (width, height, absolute, anglerad) {
                    var origx = this.rotatedrect.x;
                    var origy = this.rotatedrect.y;
                    var origw = this.rotatedrect.w;
                    var origh = this.rotatedrect.h;
                    var cosangle = Math.cos(anglerad);
                    var sinangle = Math.sin(anglerad);
                    var hw = origx - width;
                    var hh = origy - height;
                    var newx = (hw * cosangle) - (hh * sinangle);
                    var newy = (hw * sinangle) + (hh * cosangle);
                    //var transpoint = new point(newx,newy);
                    newx = width + newx;
                    newy = height + newy;
                    this.rotatedrect.x = newx;
                    this.rotatedrect.y = newy;
                    if (absolute) {
                        hw = origw - width;
                        hh = origh - height;
                    }
                    else {
                        if (this.type == 9 && this.subtype == 0) {
                            hw = (origx + origw) - width;
                            hh = (origy - origh) - height;
                        }
                        else {
                            hw = (origx + origw) - width;
                            hh = (origy + origh) - height;
                        }
                    }
                    var neww = (hw * cosangle) - (hh * sinangle);
                    var newh = (hw * sinangle) + (hh * cosangle);
                    neww = width + neww;
                    newh = height + newh;
                    if (markupobject.textrotate != 0) {
                        cosangle = Math.cos(markupobject.textrotate);
                        sinangle = Math.sin(markupobject.textrotate);
                    }
                    this.rotatedrect.w = neww;
                    this.rotatedrect.h = newh;
                };
                //transformed coordinates for rotated canvas.
                this.getRotatedMarkup = function (width, height, x, y, anglerad) {
                    var cosangle = Math.cos(anglerad);
                    var sinangle = Math.sin(anglerad);
                    var hw = x - width;
                    var hh = y - height;
                    var newx = (hw * cosangle) - (hh * sinangle);
                    var newy = (hw * sinangle) + (hh * cosangle);
                    var transpoint = new point(newx, newy);
                    transpoint.x = width + transpoint.x;
                    transpoint.y = height + transpoint.y;
                    return transpoint;
                };
                this.getMarkupSelectPointRot = function (width, height, anglerad) {
                    var hw = width;
                    var hh = height;
                    var cosangle = Math.cos(anglerad);
                    var sinangle = Math.sin(anglerad);
                    var newx = (hw * cosangle) - (hh * sinangle);
                    var newy = (hw * sinangle) + (hh * cosangle);
                    var transpoint = new point(newx, newy);
                    return transpoint;
                };
                this.getTopLeftRot = function (width, height, anglerad) {
                    var hw = -width / 2;
                    var hh = -height / 2;
                    var cosangle = Math.cos(anglerad);
                    var sinangle = Math.sin(anglerad);
                    var newx = (hw * cosangle) - (hh * sinangle);
                    var newy = (hw * sinangle) + (hh * cosangle);
                    var transpoint = new point(newx, newy);
                    return transpoint;
                };
                this.getBottomRightRot = function (width, height, anglerad) {
                    var hw = width / 2;
                    var hh = height / 2;
                    var cosangle = Math.cos(anglerad);
                    var sinangle = Math.sin(anglerad);
                    var newx = (hw * cosangle) - (hh * sinangle);
                    var newy = (hw * sinangle) + (hh * cosangle);
                    var transpoint = new point(newx, newy);
                    return transpoint;
                };
                this.getRotSelectRot = function (width, height, anglerad) {
                    var hw = 0;
                    var hh = (-height / 2) - 15;
                    var cosangle = Math.cos(anglerad);
                    var sinangle = Math.sin(anglerad);
                    var newx = (hw * cosangle) - (hh * sinangle);
                    var newy = (hw * sinangle) + (hh * cosangle);
                    var transpoint = new point(newx, newy);
                    return transpoint;
                };
                
                this.seteditaction = function(neditaction){
                    if(markupobject.signature == signature){
                        markupobject.editaction = neditaction;
                    }
                };

                this.GetwithinCorner = function (x, y) {
                    var within = false;
                    var scalecorner = 0;
                    var editaction = 0;
                    var pagerotation = DocObj.getCurrentpage().getpageRotation();


                    if (markupobject.rotation != 0) {
                        var x1 = this.LowerRightRectRot.x;
                        var x2 = this.LowerRightRectRot.w;
                        var y1 = this.LowerRightRectRot.y;
                        var y2 = this.LowerRightRectRot.h;
                        if (markupobject.Getwithin(x, y, x1, x2, y1, y2)) {
                            within = true;
                            scalecorner = 4;
                            editaction = 2;
                        }
                        x1 = this.UpperLeftRectRot.x;
                        x2 = this.UpperLeftRectRot.w;
                        y1 = this.UpperLeftRectRot.y;
                        y2 = this.UpperLeftRectRot.h;
                        if (markupobject.Getwithin(x, y, x1, x2, y1, y2)) {
                            within = true;
                            scalecorner = 1;
                            editaction = 2;
                        }
                        x1 = this.UpperRightRectRot.x;
                        x2 = this.UpperRightRectRot.w;
                        y1 = this.UpperRightRectRot.y;
                        y2 = this.UpperRightRectRot.h;
                        if (markupobject.Getwithin(x, y, x1, x2, y1, y2)) {
                            within = true;
                            scalecorner = 2;
                            editaction = 2;
                        }
                        x1 = this.LowerLeftRectRot.x;
                        x2 = this.LowerLeftRectRot.w;
                        y1 = this.LowerLeftRectRot.y;
                        y2 = this.LowerLeftRectRot.h;
                        if (markupobject.Getwithin(x, y, x1, x2, y1, y2)) {
                            within = true;
                            scalecorner = 3;
                            editaction = 2;
                        }
                        x1 = this.RotmarkerRectRot.x;
                        x2 = this.RotmarkerRectRot.w;
                        y1 = this.RotmarkerRectRot.y;
                        y2 = this.RotmarkerRectRot.h;
                        if (markupobject.Getwithin(x, y, x1, x2, y1, y2)) {
                            within = true;
                            scalecorner = 5;
                            editaction = 3;
                        }
                    }
                    else {
                        //this.LowerRightRect.x, this.LowerRightRect.w, this.LowerRightRect.y, this.LowerRightRect.h
                        x1 = this.LowerRightRect.x;
                        x2 = this.LowerRightRect.w;
                        y1 = this.LowerRightRect.y;
                        y2 = this.LowerRightRect.h;
                        if (markupobject.Getwithin(x, y, x1, x2, y1, y2)) {
                            within = true;
                            scalecorner = 4;
                            editaction = 2;
                        }
                        //this.UpperLeftRect.x, this.UpperLeftRect.w, this.UpperLeftRect.y, this.UpperLeftRect.h
                        x1 = this.UpperLeftRect.x;
                        x2 = this.UpperLeftRect.w;
                        y1 = this.UpperLeftRect.y;
                        y2 = this.UpperLeftRect.h;
                        if (markupobject.Getwithin(x, y, x1, x2, y1, y2)) {
                            within = true;
                            scalecorner = 1;
                            editaction = 2;
                        }
                        x1 = this.UpperRightRect.x;
                        x2 = this.UpperRightRect.w;
                        y1 = this.UpperRightRect.y;
                        y2 = this.UpperRightRect.h;
                        if (markupobject.Getwithin(x, y, x1, x2, y1, y2)) {
                            within = true;
                            if (pagerotation == 90 || pagerotation == 270) {
                                scalecorner = 3;
                            }
                            else {
                                scalecorner = 2;
                            }
                            editaction = 2;
                        }
                        x1 = this.LowerLeftRect.x;
                        x2 = this.LowerLeftRect.w;
                        y1 = this.LowerLeftRect.y;
                        y2 = this.LowerLeftRect.h;
                        if (markupobject.Getwithin(x, y, x1, x2, y1, y2)) {
                            within = true;
                            if (pagerotation == 90 || pagerotation == 270) {
                                scalecorner = 2;
                            }
                            else {
                                scalecorner = 3;
                            }
                            editaction = 2;
                        }
                        //this.RotmarkerRect.x, this.RotmarkerRect.w, this.RotmarkerRect.y, this.RotmarkerRect.h
                        x1 = this.RotmarkerRect.x;
                        x2 = this.RotmarkerRect.w;
                        y1 = this.RotmarkerRect.y;
                        y2 = this.RotmarkerRect.h;
                        if (markupobject.Getwithin(x, y, x1, x2, y1, y2)) {
                            within = true;
                            scalecorner = 5;
                            editaction = 3;
                        }
                    }
                    return {
                        within: within,
                        scalecorner: scalecorner,
                        editaction: editaction
                    };
                };
                this.Getwithin = function (x, y, x1, x2, y1, y2) {
                    var within = false;
                    if (x > Math.min(x1,x2) && x < Math.max(x1,x2)) {
                        if (y > Math.min(y1, y2) && y < Math.max(y1, y2)) {
                            within = true;
                        }
                    }
                    return within;
                };
                this.normalizeRect = function (rect) {
                    var normrect = {
                        x: rect.x,
                        y: rect.y,
                        w: rect.w,
                        h: rect.h
                    };
                    normrect.x = Math.min(rect.x, rect.w);
                    normrect.y = Math.min(rect.y, rect.h);
                    normrect.w = Math.max(rect.x, rect.w);
                    normrect.h = Math.max(rect.y, rect.h);
                    return normrect;
                };
                this.GetwithinRect = function (x, y, rect) {
                    var within = false;
                    if (x > rect.x && x < rect.w) {
                        if (y > rect.y && y < rect.h) {
                            within = true;
                        }
                    }
                    return within;
                };
                /*this.getBottomRightRot = function (width, height, anglerad,point){
        
                 var hw = width /2;
                 var hh = width /2
        
                 var cosangle = Math.cos(anglerad);
                 var sinangle = Math.sin(anglerad);
        
        
                 switch(point){
                 case 1://upper left
                 hw = -width /2;
                 hh = -height /2;
        
                 break;
                 case 2://lower right
                 break;
                 case 3: //upper right
                 hw = width /2;
                 hh = -height /2;
                 break;
                 case 4:
                 hw = -widht/2;
                 hh = height /2;
                 break;
                 case 5: //rotate point
                 hw = 0;
                 hh = -height/2-15;
                 break;
                 }
        
                 var newx = (hw*cosangle) - (hh*sinangle);
                 var newy = (hw*sinangle)+(hh*cosangle);
                 var transpoint = new point(newx,newy);
                 return transpoint;
                 };*/
                /*function getTopLeft( width:Number, height:Number, theta:Number ) : Point
                 {
                 //half width and height directed appropriately (for top left)
                 var hw:Number = -width / 2;
                 var hh:Number = -height / 2;
                 var cos:Number = Math.cos( theta );
                 var sin:Number = Math.sin( theta );
                 return new Point( hw * cos - hh * sin, hw * sin + hh * cos );
                 }*/

                this.getrotatedPoint = function (x,y) {
                    var centercanvX = (canvasowidth / 2);
                    var centercanvY = (canvasoheight / 2);

                    var bisfoxitPDF = isfoxitPDF();

                    if(bisfoxitPDF){

                        var pixelRatio = getPixelRatio();

                        centercanvX *= pixelRatio;
                        centercanvY *= pixelRatio;
    
                    }

                    var pagerot = DocObj.getCurrentpage().getpageRotation();

                    var CanvRotRad = pagerot * (Math.PI / 180);
                    switch (DocObj.getCurrentpage().getpageRotation()) {
                        case 0:
                            CanvRotRad = pagerot * (Math.PI / 180);
                            break;
                        case 90:
                            CanvRotRad = pagerot * (Math.PI / 180);
                            break;
                        case 270:
                            CanvRotRad = pagerot * (Math.PI / 180);
                            break;
                        case 180:
                            CanvRotRad = pagerot * (Math.PI / 180);
                            break;
                    }

                    
                    //var pagerotation = DocObj.getCurrentpage().getpageRotation();
                    if (DocObj.getCurrentpage().getpageRotation() != 0) {
                        
                        //var rotpoint = markupobject.getRotatedMarkup(centercanvX, centercanvY, x, y, CanvRotRad);

                        var rotpoint = getRotPoint(centercanvX, centercanvY, x, y, CanvRotRad);
                        x = rotpoint.x;
                        y = rotpoint.y;
                    }


                    return {x : x, y : y};

                };

                this.iswithin = function (x, y) {

                    if(markupobject.noDraw){

                        return false;
                    }

                    if (DocObj.getcurPage() != markupobject.pagenumber) {
                        return false;
                    }

                    


                    var centercanvX = (canvasowidth / 2);
                    var centercanvY = (canvasoheight / 2);

                    

                    var bisfoxitPDF = isfoxitPDF();

                    if(bisfoxitPDF){

                        var pixelRatio = getPixelRatio();

                        centercanvX *= pixelRatio;
                        centercanvY *= pixelRatio;
    
                    }

                    

                    var pagerot = DocObj.getCurrentpage().getpageRotation();

                    var CanvRotRad = pagerot * (Math.PI / 180);
                    switch (DocObj.getCurrentpage().getpageRotation()) {
                        case 0:
                            CanvRotRad = pagerot * (Math.PI / 180);
                            break;
                        case 90:
                            CanvRotRad = (pagerot + 180) * (Math.PI / 180);
                            break;
                        case 270:
                            CanvRotRad = (pagerot - 180) * (Math.PI / 180);
                            break;
                        case 180:
                            CanvRotRad = pagerot * (Math.PI / 180);
                            break;
                    }


                    var offset = getCurrentPageOffset(markupobject.pagenumber);

                    var pagedx = offset.x;
                    var pagedy = offset.y;


                    var scalefactor = getCurrentPageScale();
                    scalefactor = scalefactor / markupobject.scaling;


                    /*var pagedx = 0;
                    var pagedy = 0;
                    if (DocObj.Type == 0) {
                        var docdx = DocObj.pages[0].dx;
                    }
                    else {
                        docdx = DocObj.getCurrentpage().dx;
                    }
                    if (DocObj.getCurrentpage().usevectorxml) {
                        pagedx = DocObj.getCurrentpage().dxvector;
                        pagedy = DocObj.getCurrentpage().dyvector;
                    }
                    else if (DocObj.getCurrentpage().usepdfjs) {
                        pagedx = DocObj.getCurrentpage().dxpdf;
                        pagedy = DocObj.getCurrentpage().dypdf;
                    }
                    else {
                        pagedx = docdx;
                        pagedy = DocObj.getCurrentpage().dy;
                    }
                    var scalefactor = DocObj.getCurrentpage().dscale / this.scaling;
                    if (DocObj.getCurrentpage().usevectorxml) {
                        scalefactor = DocObj.getCurrentpage().dscalevector / markupobject.scaling;
                    }
                    else if (DocObj.getCurrentpage().usepdfjs) {
                        scalefactor = (DocObj.getCurrentpage().curpagescale * DocObj.getCurrentpage().dscalepdf) / markupobject.scaling;
                    }
                    else {
                        scalefactor = DocObj.getCurrentpage().dscale / markupobject.scaling;
                    }*/
                    //markupobject.SetDimensions(scalefactor,DocObj.getCurrentpage().getpageRotation());
                    var centerx = this.xscaled + (this.wscaled / 2);
                    var centery = this.yscaled + (this.hscaled / 2);
                    var rotX = 0.0;
                    var rotY = 0.0;
                    var xscaledpoint = 0.0;
                    var yscaledpoint = 0.0;

                    var nxtxscaledpoint = 0.0;
                    var nxtyscaledpoint = 0.0;


                    var nodeselectsize = 8;
                    var rotaneg = 0.0;
                    if (markupobject.rotation != 0) {
                        rotaneg = markupobject.rotation;
                        var rotpoint = markupobject.getBottomRightRot(this.wscaled, this.hscaled, rotaneg);
                        rotX = rotpoint.x;
                        rotY = rotpoint.y;
                        var rotXLower = centerx + rotX;
                        var rotYLower = centery + rotY;
                        rotpoint = markupobject.getRotSelectRot(this.wscaled, this.hscaled, rotaneg);
                        var rotXSelect = centerx + rotpoint.x;
                        var rotYSelect = centery + rotpoint.y;
                    }
                    var within = false;
                    var orgmousepoint = { x: x, y: y };
                    //var pagerotation = DocObj.getCurrentpage().getpageRotation();
                    if (DocObj.getCurrentpage().getpageRotation() != 0) {

                        var rotmousepoint = markupobject.getRotatedMarkup(centercanvX, centercanvY, x, y, CanvRotRad);
                        x = rotmousepoint.x;
                        y = rotmousepoint.y;
                    }
                    else {
                        //rotmousepoint.x = x;
                        //rotmousepoint.y = y;
                    }
                    var extentx = Math.min(this.xscaled, this.xscaled + this.wscaled);
                    var extentw = Math.max(this.xscaled, this.xscaled + this.wscaled);
                    var extenty = Math.min(this.yscaled, this.yscaled + this.hscaled);
                    var extenth = Math.max(this.yscaled, this.yscaled + this.hscaled);
                    var extentxabs = Math.min(this.xscaled, this.wscaled);
                    var extentwabs = Math.max(this.xscaled, this.wscaled);
                    var extentyabs = Math.min(this.yscaled, this.hscaled);
                    var extenthabs = Math.max(this.yscaled, this.hscaled);
                    var checktype = this.type;

                    markupobject.linesegment = { p1: -1, p2: -1 };
                    markupobject.multilinesegment = { p1: [], p2: [] };
                    markupobject.selectedpoint = -1;
                    markupobject.selectedmultipoint = [];
    
                    
                    //if(checktype == 1 && this.subtype == 2){checktype = 8};
                    switch (checktype) {
                        case 0:
                            /*this.wscaled = (this.w - this.xoffset) * scalefactor;
                             this.hscaled = (this.h - this.yoffset) * scalefactor;
        
                             this.wscaled = this.wscaled + DocObj.getCurrentpage().dx;
                             this.hscaled = this.hscaled + DocObj.getCurrentpage().dy;*/
                            //xscaled = xscaled + dx;
                            //yscaled = yscaled + dy;
                            if (x > this.xscaled && x < this.wscaled) {
                                if (y > this.yscaled && y < this.hscaled) {
                                    within = true;
                                    markupobject.seteditaction(1);
                                }
                            }
                            //within lower right scaling area (disabled for this type)
                            if (x > (this.wscaled - markersize) && x < (this.wscaled + markersize)) {
                                if (y > (this.hscaled - markersize) && y < (this.hscaled + markersize)) {
                                    within = true;
                                    //markupobject.seteditaction(2);
                                    //this.scalecorner = 4;
                                }
                            }

                            //var selectcorner = markupobject.GetwithinCorner(orgmousepoint.x, orgmousepoint.y);

                            /*if (selectcorner.within) {
                                within = true;
                                markupobject.seteditaction(selectcorner.editaction);
                                this.scalecorner = selectcorner.scalecorner;
                            }*/

                            break;
                        case 1:


                                /*
                                subtype 1 polyline close to line only.
                                subtype 3 measure pateh close to line only.
                                subtype 4-6 angles close to line only.
                                */ 
                

                            var bangle = (markupobject.subtype == 5 || markupobject.subtype == 4);

                            var bsegment = (markupobject.subtype == 6);

                            var bpolyline = (markupobject.subtype == 1 || markupobject.subtype == 3 || markupobject.subtype == 7);

                            


                            if(markupobject.bfor3D){
                                scalefactor = canvasoheight / markupobject.pageheight;
                            }

                            var mp = {
                                x: x,
                                y: y
                            };
                            var lp1 = {
                                x: this.xscaled,
                                y: this.yscaled
                            };
                            var lp2 = {
                                x: this.wscaled,
                                y: this.hscaled
                            };
                            var epsilon = {
                                x: lp1.x - ((lp2.x - lp1.x) / 10),
                                y: lp2.y - ((lp2.y - lp1.y) / 2)
                            };

                            if (markupobject.subtype == 2) {
                                within = insidepolygon(scalefactor, pagedx, pagedy, markupobject.xoffset, markupobject.yoffset, markupobject.sides, mp, epsilon);
                                markupobject.seteditaction(1);
                            }else if (bsegment) {

                                var lcounter = 0;
                                counter = 0;


                                for (lcounter = 0; lcounter < markupobject.pointlist.length; lcounter++) {
                                    for (counter = 0; counter < markupobject.pointlist[lcounter].length; counter++) {
            
                                        xscaledpoint = (markupobject.pointlist[lcounter][counter].x - markupobject.xoffset) * scalefactor;
                                        yscaledpoint = (markupobject.pointlist[lcounter][counter].y - markupobject.yoffset) * scalefactor;

                                        //var xscaledpoint = 0.0;
                                        //var yscaledpoint = 0.0;
                    
                                        xscaledpoint = xscaledpoint + pagedx;
                                        yscaledpoint = yscaledpoint + pagedy;

                                        if (counter == markupobject.pointlist[lcounter].length - 1) {
                                            nxtxscaledpoint = (markupobject.pointlist[lcounter][0].x - markupobject.xoffset) * scalefactor;
                                            nxtyscaledpoint = (markupobject.pointlist[lcounter][0].y - markupobject.yoffset) * scalefactor;

                                            //var point1no = counter;
                                            //var point2no = 0;

                                            var point1no = [lcounter, counter];
                                            var point2no = [lcounter, 0];


                                        }else{
                                            nxtxscaledpoint = (markupobject.pointlist[lcounter][counter + 1].x - markupobject.xoffset) * scalefactor;
                                            nxtyscaledpoint = (markupobject.pointlist[lcounter][counter + 1].y - markupobject.yoffset) * scalefactor;

                                            //point1no = counter;
                                            //point2no = counter + 1;

                                            point1no = [lcounter, counter];
                                            point2no = [lcounter, counter + 1];

    
                                        }

                                        nxtxscaledpoint += pagedx;
                                        nxtyscaledpoint += pagedy;

                                        mp = {
                                            x: x,
                                            y: y
                                        };
                                        lp1 = {
                                            x: xscaledpoint,
                                            y: yscaledpoint
                                        };
                                        lp2 = {
                                            x: nxtxscaledpoint,
                                            y: nxtyscaledpoint
                                        };

                                        if (markupobject.subtype == 7){
                                            var linedist = 15;
                                          }else{
                                            linedist = 5;
                                          }
                        

                                        if (distToSegment(mp, lp1, lp2) < linedist) {
                                            within = true;
                                            //new select line segment action
                                            
                                            markupobject.seteditaction(1);

                                            markupobject.multilinesegment = { p1: point1no, p2: point2no };
                                            
                                            //markupobject.seteditaction(7);;
                                            //markupobject.linesegment = { p1: point1no, p2: point2no };

                                            //return within;
                                        }
                                        /*if (x > xscaledpoint - nodeselectsize && x < xscaledpoint + nodeselectsize) {
                                            if (y > yscaledpoint - nodeselectsize && y < yscaledpoint + nodeselectsize) {
                                                within = true;
                                                markupobject.selectedpoint = counter;
                                                markupobject.seteditaction(4);
                                                break;
                                            }
                                        }*/

                                    }
                                }

                            }else if(bpolyline || bangle) {
                                /*if (x > this.xscaled && x < this.wscaled) {
                                    if (y > this.yscaled && y < this.hscaled) {
                                        within = true;
                                        markupobject.seteditaction(1);
                                    }
                                }*/
                                var counter = 0;
                                for (counter = 0; counter < markupobject.points.length; counter++) {
                                    xscaledpoint = (markupobject.points[counter].x - this.xoffset) * scalefactor;
                                    yscaledpoint = (markupobject.points[counter].y - this.yoffset) * scalefactor;
                                    xscaledpoint = xscaledpoint + pagedx;
                                    yscaledpoint = yscaledpoint + pagedy;
                                    if (counter == markupobject.points.length - 1) {
                                        nxtxscaledpoint = (markupobject.points[0].x - this.xoffset) * scalefactor;
                                        nxtyscaledpoint = (markupobject.points[0].y - this.yoffset) * scalefactor;
                                        nxtxscaledpoint += pagedx;
                                        nxtyscaledpoint += pagedy;

                                        var point1no = counter;
                                        var point2no = 0;
                                    }
                                    else {
                                        nxtxscaledpoint = (markupobject.points[counter + 1].x - this.xoffset) * scalefactor;
                                        nxtyscaledpoint = (markupobject.points[counter + 1].y - this.yoffset) * scalefactor;
                                        nxtxscaledpoint += pagedx;
                                        nxtyscaledpoint += pagedy;
                                        point1no = counter;
                                        point2no = counter + 1;
                                    }
                                    mp = {
                                        x: x,
                                        y: y
                                    };
                                    lp1 = {
                                        x: xscaledpoint,
                                        y: yscaledpoint
                                    };
                                    lp2 = {
                                        x: nxtxscaledpoint,
                                        y: nxtyscaledpoint
                                    };

                                    if (markupobject.subtype == 7){
                                        var linedist = 15;
                                      }else{
                                        linedist = 5;
                                      }
                    

                                    if (distToSegment(mp, lp1, lp2) < linedist) {
                                        within = true;
                                        //new select line segment action
                                        markupobject.seteditaction(1);
                                        //markupobject.linesegment = {p1 : point1no, p2 : point2no};
                                        //return within;
                                    }
                                } //end for
                                //within lower right scaling area (disabled for this type)
                                /*if (x > (this.wscaled - markersize) && x < (this.wscaled + markersize)) {
                                 if (y > (this.hscaled - markersize) && y < (this.hscaled + markersize)) {
                                 within = true;
                                 //markupobject.seteditaction(2);
                                 //this.scalecorner = 4;
                                 }
                                 }*/
                            }
                            if (markupobject.selectedit) {
                                

                                if (markupobject.subtype == 6) {


                                    var lcounter = 0;
                                    counter = 0;


                                    for (lcounter = 0; lcounter < markupobject.pointlist.length; lcounter++) {
                                        for (counter = 0; counter < markupobject.pointlist[lcounter].length; counter++) {
                
                                            xscaledpoint = (markupobject.pointlist[lcounter][counter].x - markupobject.xoffset) * scalefactor;
                                            yscaledpoint = (markupobject.pointlist[lcounter][counter].y - markupobject.yoffset) * scalefactor;

                                            //var xscaledpoint = 0.0;
                                            //var yscaledpoint = 0.0;
                        
                                            xscaledpoint = xscaledpoint + pagedx;
                                            yscaledpoint = yscaledpoint + pagedy;

                                            if (counter == markupobject.pointlist[lcounter].length - 1) {
                                                nxtxscaledpoint = (markupobject.pointlist[lcounter][0].x - markupobject.xoffset) * scalefactor;
                                                nxtyscaledpoint = (markupobject.pointlist[lcounter][0].y - markupobject.yoffset) * scalefactor;

                                                var point1no = [lcounter, counter];
                                                var point2no = [lcounter, 0];

                                            }else{
                                                nxtxscaledpoint = (markupobject.pointlist[lcounter][counter + 1].x - markupobject.xoffset) * scalefactor;
                                                nxtyscaledpoint = (markupobject.pointlist[lcounter][counter + 1].y - markupobject.yoffset) * scalefactor;

                                                point1no = [lcounter, counter];
                                                point2no = [lcounter, counter + 1];
        
                                            }

                                            nxtxscaledpoint += pagedx;
                                            nxtyscaledpoint += pagedy;

                                            mp = {
                                                x: x,
                                                y: y
                                            };
                                            lp1 = {
                                                x: xscaledpoint,
                                                y: yscaledpoint
                                            };
                                            lp2 = {
                                                x: nxtxscaledpoint,
                                                y: nxtyscaledpoint
                                            };
                                            if (distToSegment(mp, lp1, lp2) < 5) {
                                                within = true;
                                                //new select line segment action

                                                within = true;
                                                //new select line segment action
                                                
                                                markupobject.seteditaction(7);
                                                
                                                markupobject.multilinesegment = { p1: point1no, p2: point2no };

                                                //markupobject.seteditaction(1);
                                                //markupobject.seteditaction(7);;
                                                //markupobject.linesegment = { p1: point1no, p2: point2no };

                                                //return within;
                                            }
                                            if (x > xscaledpoint - nodeselectsize && x < xscaledpoint + nodeselectsize) {
                                                if (y > yscaledpoint - nodeselectsize && y < yscaledpoint + nodeselectsize) {
                                                    within = true;
                                                    markupobject.selectedmultipoint = [lcounter, counter];
                                                    markupobject.seteditaction(4);
                                                    break;
                                                }
                                            }

                                        }
                                    }
                                        //need to check selectedit usage                                    

                                }else{
                                    
                                    for (counter = 0; counter < markupobject.points.length; counter++) {
                                        xscaledpoint = (markupobject.points[counter].x - this.xoffset) * scalefactor;
                                        yscaledpoint = (markupobject.points[counter].y - this.yoffset) * scalefactor;
                                        xscaledpoint = xscaledpoint + pagedx;
                                        yscaledpoint = yscaledpoint + pagedy;
                                        if (counter == markupobject.points.length - 1) {
                                            var nxtxscaledpoint = (markupobject.points[0].x - this.xoffset) * scalefactor;
                                            var nxtyscaledpoint = (markupobject.points[0].y - this.yoffset) * scalefactor;
                                            nxtxscaledpoint += pagedx;
                                            nxtyscaledpoint += pagedy;
                                            var point1no = counter;
                                            var point2no = 0;
                                        }
                                        else {
                                            nxtxscaledpoint = (markupobject.points[counter + 1].x - this.xoffset) * scalefactor;
                                            nxtyscaledpoint = (markupobject.points[counter + 1].y - this.yoffset) * scalefactor;
                                            nxtxscaledpoint += pagedx;
                                            nxtyscaledpoint += pagedy;
                                            point1no = counter;
                                            point2no = counter + 1;
                                        }
                                        mp = {
                                            x: x,
                                            y: y
                                        };
                                        lp1 = {
                                            x: xscaledpoint,
                                            y: yscaledpoint
                                        };
                                        lp2 = {
                                            x: nxtxscaledpoint,
                                            y: nxtyscaledpoint
                                        };
                                        if (distToSegment(mp, lp1, lp2) < 5) {
                                            within = true;
                                            //new select line segment action
                                            markupobject.seteditaction(7);

                                            markupobject.linesegment = { p1: point1no, p2: point2no };
                                            //return within;
                                        }
                                        if (x > xscaledpoint - nodeselectsize && x < xscaledpoint + nodeselectsize) {
                                            if (y > yscaledpoint - nodeselectsize && y < yscaledpoint + nodeselectsize) {
                                                within = true;
                                                markupobject.selectedpoint = counter;

                                                markupobject.seteditaction(4);
                                                
                                                break;
                                            }
                                        }
                                    }
                                }
                                
                                
                                return within;
                            }
                            /*if (markupobject.selectedit) {
                                var counter = 0;
                                for (counter = 0; counter < markupobject.points.length; counter++) {
                                    xscaledpoint = (markupobject.points[counter].x - this.xoffset) * scalefactor;
                                    yscaledpoint = (markupobject.points[counter].y - this.yoffset) * scalefactor;
        
                                    xscaledpoint = xscaledpoint + pagedx;
                                    yscaledpoint = yscaledpoint + pagedy;
        
        
                                    if (x > xscaledpoint - nodeselectsize && x < xscaledpoint + nodeselectsize) {
                                        if (y > yscaledpoint - nodeselectsize && y < yscaledpoint + nodeselectsize) {
                                            within = true;
                                            markupobject.selectedpoint = counter;
                                            markupobject.seteditaction(4);
                                        }
                                    }
        
        
                                }
        
                            }*/
                            break;
                        case 2:
                            if (x > this.xscaled && x < this.wscaled) {
                                if (y > this.yscaled && y < this.hscaled) {
                                    within = true;
                                    markupobject.seteditaction(1);
                                }
                            }
                            //within lower right scaling area (disabled for this type)
                            if (x > (this.wscaled - markersize) && x < (this.wscaled + markersize)) {
                                if (y > (this.hscaled - markersize) && y < (this.hscaled + markersize)) {
                                    within = true;
                                    //markupobject.seteditaction(2);
                                    //this.scalecorner = 4;
                                }
                            }
                            if (markupobject.selectedit) {
                                counter = 0;
                                for (counter = 0; counter < markupobject.points.length; counter++) {
                                    xscaledpoint = (markupobject.points[counter].x - this.xoffset) * scalefactor;
                                    yscaledpoint = (markupobject.points[counter].y - this.yoffset) * scalefactor;
                                    xscaledpoint = xscaledpoint + pagedx;
                                    yscaledpoint = yscaledpoint + pagedy;
                                    if (x > xscaledpoint - nodeselectsize && x < xscaledpoint + nodeselectsize) {
                                        if (y > yscaledpoint - nodeselectsize && y < yscaledpoint + nodeselectsize) {
                                            within = true;
                                            markupobject.selectedpoint = counter;

                                            markupobject.seteditaction(4);

                                            
                                        }
                                    }
                                }
                            }
                            break;
                        case 3:
                            if (markupobject.Getwithin(x, y, extentx, extentw, extenty, extenth)) {
                                within = true;
                                markupobject.seteditaction(1);
                            }
                            var selectcorner = markupobject.GetwithinCorner(orgmousepoint.x, orgmousepoint.y);
                            if (selectcorner.within) {
                                within = true;
                                
                                markupobject.seteditaction(selectcorner.editaction);
                                
                                this.scalecorner = selectcorner.scalecorner;
                            }
                            break;
                        case 4:
                            if (this.subtype == 1) {
                                var xdiff = Math.max(this.xscaled, this.wscaled) - Math.min(this.xscaled, this.wscaled);
                                var ydiff = Math.max(this.yscaled, this.hscaled) - Math.min(this.yscaled, this.hscaled);
                                var radius = markupobject.getdiag(xdiff, ydiff);
                                if (Math.sqrt((x - this.xscaled) * (x - this.xscaled) + (y - this.yscaled) * (y - this.yscaled)) < radius) {
                                    within = true;
                                    markupobject.seteditaction(1);
                                }
                                //within lower right scaling area
                                var xlr = this.wscaled;
                                var ylr = this.hscaled;
                                var xlrl = xlr - markersize;
                                var xlrr = xlr + markersize;
                                var ylrl = ylr - markersize;
                                var ylrr = ylr + markersize;
                                if (x > (xlrl) && x < (xlrr)) {
                                    if (y > (ylrl) && y < (ylrr)) {
                                        within = true;

                                        markupobject.seteditaction(2);
                                        this.scalecorner = 4;
                                    }
                                }
                            }
                            else {
                                if (markupobject.Getwithin(x, y, extentx, extentw, extenty, extenth)) {
                                    within = true;
                                    markupobject.seteditaction(1);
                                    
                                }
                                selectcorner = markupobject.GetwithinCorner(orgmousepoint.x, orgmousepoint.y);
                                if (selectcorner.within) {
                                    within = true;
                                    
                                    markupobject.seteditaction(selectcorner.editaction);

                                    this.scalecorner = selectcorner.scalecorner;
                                }
                            }
                            break;
                        case 5:
                            if (markupobject.Getwithin(x, y, extentx, extentw, extenty, extenth)) {
                                within = true;
                                markupobject.seteditaction(1);
                                
                            }
                            selectcorner = markupobject.GetwithinCorner(orgmousepoint.x, orgmousepoint.y);
                            if (selectcorner.within) {
                                within = true;
                                
                                markupobject.seteditaction(selectcorner.editaction);

                                this.scalecorner = selectcorner.scalecorner;
                            }
                            break;
                        case 6: //line, arrow
                            /*this.wscaled = (this.w - this.xoffset) * scalefactor;
                             this.hscaled = (this.h - this.yoffset) * scalefactor;
        
        
                             this.wscaled = this.wscaled + DocObj.getCurrentpage().dx;
                             this.hscaled = this.hscaled + DocObj.getCurrentpage().dy;*/
                            //                  xscaled = xscaled + dx;
                            //                  yscaled = yscaled + dy;
                            /*if (x> extentxabs && x<extentwabs ){
                             if (y> extentyabs && y<extenthabs){
                             within = true;
                             markupobject.seteditaction(1);
                             }
                             }*/
                            mp = {
                                x: x,
                                y: y
                            };
                            lp1 = {
                                x: this.xscaled,
                                y: this.yscaled
                            };
                            lp2 = {
                                x: this.wscaled,
                                y: this.hscaled
                            };
                            if (distToSegment(mp, lp1, lp2) < 10) {
                                within = true;
                                markupobject.seteditaction(1);
                                
                            }
                            /*if (x> extentxabs && x<extentwabs ){
                             if (this.yscaled == this.hscaled || Math.abs(this.yscaled - this.hscaled) < markersize ){
                             if (y> this.yscaled - markersize && y<this.hscaled + markersize){
                             within = true;
                             markupobject.seteditaction(1);
                             }
        
                             }
                             if (this.yscaled < this.hscaled){
        
                             if (y> this.yscaled && y<this.hscaled){
                             within = true;
                             markupobject.seteditaction(1);
                             }
                             }else{
                             if (y> this.hscaled && y<this.yscaled){
                             within = true;
                             markupobject.seteditaction(1);
                             }
        
                             }
        
                             }*/
                            //within lower right scaling area
                            if (x > (this.wscaled - markersize) && x < (this.wscaled + markersize)) {
                                if (y > (this.hscaled - markersize) && y < (this.hscaled + markersize)) {
                                    if (this.subtype != 6) {
                                        within = true;

                                        markupobject.seteditaction(2);                                        
                                        
                                        this.scalecorner = 4;
                                    }
                                }
                            }
                            //within upper left scaling area
                            if (x > (this.xscaled - markersize) && x < (this.xscaled + markersize)) {
                                if (y > (this.yscaled - markersize) && y < (this.yscaled + markersize)) {
                                    if (this.subtype != 6) {
                                        within = true;
                                        markupobject.seteditaction(2);
                                        this.scalecorner = 4;
                                    }
                                }
                            }
                            //within upper left scaling area
                            if (x > (this.xscaled - markersize) && x < (this.xscaled + markersize)) {
                                if (y > (this.yscaled - markersize) && y < (this.yscaled + markersize)) {
                                    within = true;
                                    markupobject.seteditaction(2);
                                    this.scalecorner = 1;
                                }
                            }
                            break;
                        case 7: //dimention line
                            /*switch(DocObj.getCurrentpage().currentimage){
                             case 0:
                             xscaled = xscaled + DocObj.getCurrentpage().dx;
                             yscaled = yscaled + DocObj.getCurrentpage().dy;
                             break;
                             case 1:
                             xscaled = xscaled + DocObj.getCurrentpage().dxextent;
                             yscaled = yscaled + DocObj.getCurrentpage().dyextent;
        
                             break;
                             }
        
                             this.wscaled = (this.w - this.xoffset) * scalefactor;
                             this.hscaled = (this.h - this.yoffset) * scalefactor;
        
        
                             this.wscaled = this.wscaled + DocObj.getCurrentpage().dx;
                             this.hscaled = this.hscaled + DocObj.getCurrentpage().dy;*/
                            //                  xscaled = xscaled + dx;
                            //                  yscaled = yscaled + dy;
                            mp = {
                                x: x,
                                y: y
                            };
                            lp1 = {
                                x: this.xscaled,
                                y: this.yscaled
                            };
                            lp2 = {
                                x: this.wscaled,
                                y: this.hscaled
                            };
                            if (distToSegment(mp, lp1, lp2) < 10) {
                                within = true;
                                markupobject.seteditaction(1);
                                
                            }
                            /*if (x> extentxabs && x<extentwabs ){
                             if (this.yscaled == this.hscaled || Math.abs(this.yscaled - this.hscaled) < markersize ){
                             if (y> this.yscaled - markersize && y<this.hscaled + markersize){
                             within = true;
                             markupobject.seteditaction(1);
                             }
        
                             }
                             if (this.yscaled < this.hscaled){
        
                             if (y> this.yscaled && y<this.hscaled){
                             within = true;
                             markupobject.seteditaction(1);
                             }
                             }else{
                             if (y> this.hscaled && y<this.yscaled){
                             within = true;
                             markupobject.seteditaction(1);
                             }
        
                             }
        
                             }*/
                            //within lower right scaling area
                            if (x > (this.wscaled - markersize) && x < (this.wscaled + markersize)) {
                                if (y > (this.hscaled - markersize) && y < (this.hscaled + markersize)) {
                                    within = true;
                                    
                                    markupobject.seteditaction(2);
                                    this.scalecorner = 4;
                                }
                            }
                            //within upper left scaling area
                            if (x > (this.xscaled - markersize) && x < (this.xscaled + markersize)) {
                                if (y > (this.yscaled - markersize) && y < (this.yscaled + markersize)) {
                                    within = true;
                                    markupobject.seteditaction(2);
                                    this.scalecorner = 4;
                                }
                            }
                            //within upper left scaling area
                            if (x > (this.xscaled - markersize) && x < (this.xscaled + markersize)) {
                                if (y > (this.yscaled - markersize) && y < (this.yscaled + markersize)) {
                                    within = true;
                                    markupobject.seteditaction(2);
                                    this.scalecorner = 1;
                                }
                            }
                            //labelposition for changing leader line offset.
                            if (this.leaderoffset == 0) {
                                var tx = this.xscaled + ((this.wscaled - this.xscaled) / 2);
                                var ty = this.yscaled + ((this.hscaled - this.yscaled) / 2);
                            }
                            else {
                                tx = this.labelpos.x + ((this.labelpos.w - this.labelpos.x) / 2);
                                ty = this.labelpos.y + ((this.labelpos.h - this.labelpos.y) / 2);
                            }
                            //labelsize.w = dimtextwidth + (20 * scalefactor);
                            //labelsize.h = scaletextheight + (10 * scalefactor);
                            if (labelsize.w != 0 && labelsize.h != 0) {
                                var lwsize = labelsize.w * 0.5;
                                if (x > (tx - lwsize) && x < (tx + lwsize)) {
                                    if (y > (ty - lwsize) && y < (ty + lwsize)) {
                                        within = true;
                                        

                                        markupobject.seteditaction(6);

                                        //this.scalecorner = 3;
                                    }
                                }
                            }
                            else {
                                if (x > (tx - markersize) && x < (tx + markersize)) {
                                    if (y > (ty - markersize) && y < (ty + markersize)) {
                                        within = true;
                                        markupobject.seteditaction(6);
                                        //this.scalecorner = 3;
                                    }
                                }
                            }
                            /*if (x > (this.xscaled - markersize) && x < (this.xscaled + markersize)) {
                                if (y > (this.yscaled - markersize) && y < (this.yscaled + markersize)) {
                                    within = true;
                                    markupobject.seteditaction(2);
                                    this.scalecorner = 1;
                                }
        
                            }*/
                            break;
                        case 8:
                            /* this.wscaled = (this.w - this.xoffset) * scalefactor;
                             this.hscaled = (this.h - this.yoffset) * scalefactor;
        
                             this.wscaled = this.wscaled + DocObj.getCurrentpage().dx;
                             this.hscaled = this.hscaled + DocObj.getCurrentpage().dy;*/
                            mp = {
                                x: x,
                                y: y
                            };
                            lp1 = {
                                x: this.xscaled,
                                y: this.yscaled
                            };
                            lp2 = {
                                x: this.wscaled,
                                y: this.hscaled
                            };
                            epsilon = {
                                x: lp1.x - ((lp2.x - lp1.x) / 10),
                                y: lp2.y - ((lp2.y - lp1.y) / 2)
                            };



                            within = insidepolygon(scalefactor, pagedx, pagedy, markupobject.xoffset, markupobject.yoffset, markupobject.sides, mp, epsilon);

                            if(this.holes.length > 0){
                                for(var h=0; h < this.holes.length; h++){
                                    if(this.holes[h].selectedit){
                                        within = false;
                                    }
                                }
                                
                            }

                            if (within) {
                                markupobject.seteditaction(1);
                                
                            }
                            /*if (x > this.xscaled && x < this.wscaled) {
                             if (y > this.yscaled && y < this.hscaled) {
                             within = true;
                             markupobject.seteditaction(1);
                             }
                             }*/
                            //within lower right scaling area (disabled for this type)
                            /*if (x > (this.wscaled - markersize) && x < (this.wscaled + markersize)) {
                                if (y > (this.hscaled - markersize) && y < (this.hscaled + markersize)) {
                                    within = true;
                                    markupobject.seteditaction(2);
                                    //this.scalecorner = 4;
                                }
                            }*/
                            if (markupobject.selectedit) {
                                var acounter = 0;
                                for (acounter = 0; acounter < markupobject.points.length; acounter++) {
                                    xscaledpoint = (markupobject.points[acounter].x - this.xoffset) * scalefactor;
                                    yscaledpoint = (markupobject.points[acounter].y - this.yoffset) * scalefactor;
                                    xscaledpoint = xscaledpoint + pagedx;
                                    yscaledpoint = yscaledpoint + pagedy;
                                    if (acounter == markupobject.points.length - 1) {
                                        var nxtxscaledpoint = (markupobject.points[0].x - this.xoffset) * scalefactor;
                                        var nxtyscaledpoint = (markupobject.points[0].y - this.yoffset) * scalefactor;
                                        nxtxscaledpoint += pagedx;
                                        nxtyscaledpoint += pagedy;
                                        var point1no = acounter;
                                        var point2no = 0;
                                    }
                                    else {
                                        nxtxscaledpoint = (markupobject.points[acounter + 1].x - this.xoffset) * scalefactor;
                                        nxtyscaledpoint = (markupobject.points[acounter + 1].y - this.yoffset) * scalefactor;
                                        nxtxscaledpoint += pagedx;
                                        nxtyscaledpoint += pagedy;
                                        point1no = acounter;
                                        point2no = acounter + 1;
                                    }
                                    mp = {
                                        x: x,
                                        y: y
                                    };
                                    lp1 = {
                                        x: xscaledpoint,
                                        y: yscaledpoint
                                    };
                                    lp2 = {
                                        x: nxtxscaledpoint,
                                        y: nxtyscaledpoint
                                    };
                                    if (distToSegment(mp, lp1, lp2) < 5) {
                                        within = true;
                                        //new select line segment action
                                        
                                        markupobject.seteditaction(7);

                                                                                

                                        markupobject.linesegment = { p1: point1no, p2: point2no };
                                        //return within;
                                    }
                                    if (x > xscaledpoint - nodeselectsize && x < xscaledpoint + nodeselectsize) {
                                        if (y > yscaledpoint - nodeselectsize && y < yscaledpoint + nodeselectsize) {
                                            within = true;
                                            markupobject.selectedpoint = acounter;
                                            
                                            markupobject.seteditaction(4);

                                            break;
                                            //return within;
                                        }
                                    }
                                } //end for
                                if (markupobject.insidelabel(x, y)) {
                                    within = true;
                                    
                                    markupobject.seteditaction(8);

                                }
                                return within;
                            }
                            break;
                        case 9:
                            if (this.subtype == 1 || this.subtype == 2) {
                                if (markupobject.Getwithin(x, y, extentx, extentw, extenty, extenth)) {
                                    within = true;
                                    markupobject.seteditaction(1);
                                    
                                }
                                selectcorner = markupobject.GetwithinCorner(orgmousepoint.x, orgmousepoint.y);
                                if (selectcorner.within) {
                                    within = true;

                                    markupobject.seteditaction(selectcorner.editaction);
                                    this.scalecorner = selectcorner.scalecorner;
                                }
                            }
                            else if (this.subtype == 0) {
                                this.hscaled = this.textheight * scalefactor;
                                this.wscaled = this.textwidth;
                                var wscaled = this.wscaled;
                                var hscaled = this.hscaled;
                                var switchval = wscaled;
                                switch (this.textrotate / (Math.PI / 180)) {
                                    case 0:
                                        if (this.subtype == 0) {
                                            if (markupobject.Getwithin(x, y, this.xscaled, this.xscaled + wscaled, this.yscaled - hscaled, this.yscaled)) {
                                                within = true;
                                                markupobject.seteditaction(1);
                                            }
                                        }
                                        else if (this.subtype == 1 || this.subtype == 2) {
                                            if (markupobject.Getwithin(x, y, this.xscaled, this.xscaled + wscaled, this.yscaled + hscaled, this.yscaled)) {
                                                within = true;
                                                markupobject.seteditaction(1);
                                            }
                                        }
                                        break;
                                    case 90:
                                        wscaled = hscaled;
                                        hscaled = switchval;
                                        var mtextx1 = Math.min(this.xscaled, this.xscaled + wscaled);
                                        var mtextx2 = Math.max(this.xscaled, this.xscaled + wscaled);
                                        var mtexty1 = Math.min(this.yscaled, this.yscaled + hscaled);
                                        var mtexty2 = Math.max(this.yscaled, this.yscaled + hscaled);
                                        if (markupobject.Getwithin(x, y, mtextx1, mtextx2, mtexty1, mtexty2)) {
                                            within = true;
                                            markupobject.seteditaction(1);
                                        }
                                        break;
                                    case 180:
                                        mtextx1 = Math.min(this.xscaled, this.xscaled - wscaled);
                                        mtextx2 = Math.max(this.xscaled, this.xscaled - wscaled);
                                        mtexty1 = Math.min(this.yscaled, this.yscaled + hscaled);
                                        mtexty2 = Math.max(this.yscaled, this.yscaled + hscaled);
                                        if (markupobject.Getwithin(x, y, mtextx1, mtextx2, mtexty1, mtexty2)) {
                                            within = true;
                                            markupobject.seteditaction(1);
                                        }
                                        break;
                                    case 270:
                                        wscaled = hscaled;
                                        hscaled = switchval;
                                        mtextx1 = Math.min(this.xscaled, this.xscaled - wscaled);
                                        mtextx2 = Math.max(this.xscaled, this.xscaled - wscaled);
                                        mtexty1 = Math.min(this.yscaled, this.yscaled - hscaled);
                                        mtexty2 = Math.max(this.yscaled, this.yscaled - hscaled);
                                        if (markupobject.Getwithin(x, y, mtextx1, mtextx2, mtexty1, mtexty2)) {
                                            within = true;
                                            markupobject.seteditaction(1);
                                        }
                                        break;
                                }
                                selectcorner = markupobject.GetwithinCorner(orgmousepoint.x, orgmousepoint.y);
                                if (selectcorner.within) {
                                    within = true;


                                    markupobject.seteditaction(selectcorner.editaction);
                                    
                                    this.scalecorner = selectcorner.scalecorner;
                                }
                            }
                            break;
                        case 10:

                            if(bUseNoScale){
                                if (x > this.xscaled && x < (this.xscaled + this.w)) {
                                    if (y > this.yscaled && y < (this.yscaled + this.h)) {
                                        //also hover over
                                        within = true;
                                        markupobject.seteditaction(1);
                                    }
                                }

                            }else{
                                if (x > this.xscaled && x < (this.xscaled + this.wscaled)) {
                                    if (y > this.yscaled && y < (this.yscaled + this.hscaled)) {
                                        //also hover over
                                        within = true;
                                        markupobject.seteditaction(1);
                                    }
                                }

                            }

                            break;
                        case 11:
                        
                            var bselectimage = (markupobject.subtype == 1 || markupobject.subtype == 3 || markupobject.subtype == 12 || markupobject.subtype == 20 || markupobject.subtype == 21 );

                            if(bselectimage){
                                if (markupobject.Getwithin(x, y, this.xscaled, this.xscaled + this.wscaled, this.yscaled, this.yscaled + this.hscaled)) {
                                    within = true;
                                    markupobject.seteditaction(1);
                                }
                                selectcorner = markupobject.GetwithinCorner(orgmousepoint.x, orgmousepoint.y);
                                if (selectcorner.within) {
                                    within = true;
                                    markupobject.seteditaction(selectcorner.editaction);
                                    this.scalecorner = selectcorner.scalecorner;
                                }
                            }
                            break;
                        case 12:
                            if (x > this.xscaled && x < (this.xscaled + this.wscaled)) {
                                if (y > this.yscaled && y < (this.yscaled + this.hscaled)) {
                                    within = true;
                                    markupobject.seteditaction(1);
                                }
                            }
                            selectcorner = markupobject.GetwithinCorner(orgmousepoint.x, orgmousepoint.y);
                            if (selectcorner.within) {
                                within = true;
                                markupobject.seteditaction(selectcorner.editaction);
                                this.scalecorner = selectcorner.scalecorner;
                            }
                            break;
                        case 13:

                            //markupobject.countshapes[counter].x

                            if(markupobject.bfor3D){
                                scalefactor = canvasoheight / markupobject.pageheight;
                            }

                        
                            var ccounter = 0;
                            for (ccounter = 0; ccounter < markupobject.countshapes.length; ccounter++) {
                                
                                xscaledpoint = (markupobject.countshapes[ccounter].x - this.xoffset) * scalefactor;
                                yscaledpoint = (markupobject.countshapes[ccounter].y - this.yoffset) * scalefactor;

                                xscaledpoint = xscaledpoint + pagedx;
                                yscaledpoint = yscaledpoint + pagedy;

                                /*if (ccounter == markupobject.countshapes.length - 1) {
                                    var nxtxscaledpoint = (markupobject.countshapes[0].x - this.xoffset) * scalefactor;
                                    var nxtyscaledpoint = (markupobject.countshapes[0].y - this.yoffset) * scalefactor;
                                    nxtxscaledpoint += pagedx;
                                    nxtyscaledpoint += pagedy;
                                    var point1no = ccounter;
                                    var point2no = 0;
                                }
                                else {
                                    nxtxscaledpoint = (markupobject.countshapes[ccounter + 1].x - this.xoffset) * scalefactor;
                                    nxtyscaledpoint = (markupobject.countshapes[ccounter + 1].y - this.yoffset) * scalefactor;
                                    nxtxscaledpoint += pagedx;
                                    nxtyscaledpoint += pagedy;
                                    point1no = ccounter;
                                    point2no = ccounter + 1;
                                }*/
                                mp = {
                                    x: x,
                                    y: y
                                };
                                lp1 = {
                                    x: xscaledpoint,
                                    y: yscaledpoint
                                };
                                /*lp2 = {
                                    x: nxtxscaledpoint,
                                    y: nxtyscaledpoint
                                };*/
                                /*if (distToSegment(mp, lp1, lp2) < 5) {
                                    within = true;
                                    //new select line segment action
                                    markupobject.seteditaction(7);;
                                    markupobject.linesegment = { p1: point1no, p2: point2no };
                                    //return within;
                                }*/
                                if (x > xscaledpoint - nodeselectsize && x < xscaledpoint + nodeselectsize) {
                                    if (y > yscaledpoint - nodeselectsize && y < yscaledpoint + nodeselectsize) {
                                        within = true;
                                        markupobject.selectedpoint = ccounter;
                                        markupobject.seteditaction(4);
                                        break;
                                        //return within;
                                    }
                                }
                            } //end for
                            /*if (markupobject.insidelabel(x, y)) {
                                within = true;
                                markupobject.seteditaction(8);
                            }
                            return within;*/

                            break;    
                        case 14:

                            //isclose = (p1, p2, p3, mouse, tolerance = 5) => {

                            var p1 = {x : markupobject.startXscaled, y:markupobject.startYscaled};
                            var p2 = {x : markupobject.secondXscaled, y:markupobject.secondYscaled};
                            var p3 = {x : markupobject.endXscaled, y:markupobject.endYscaled};

                            var tolerance = 5;
                            within = RenderUtils.isclose(p1, p2, p3, { x: x, y: y }, tolerance);
                            if(within){
                                markupobject.seteditaction(1);
                            }

                            if (x > p1.x - nodeselectsize && x < p1.x + nodeselectsize) {
                                if (y > p1.y - nodeselectsize && y < p1.y + nodeselectsize) {
                                    within = true;
                                    markupobject.selectedpoint = 1;
                                    
                                    markupobject.seteditaction(4);

                                    break;
                                    //return within;
                                }
                            }

                            if (x > p2.x - nodeselectsize && x < p2.x + nodeselectsize) {
                                if (y > p2.y - nodeselectsize && y < p2.y + nodeselectsize) {
                                    within = true;
                                    markupobject.selectedpoint = 2;
                                    
                                    markupobject.seteditaction(4);

                                    break;
                                    //return within;
                                }
                            }

                            if (x > p3.x - nodeselectsize && x < p3.x + nodeselectsize) {
                                if (y > p3.y - nodeselectsize && y < p3.y + nodeselectsize) {
                                    within = true;
                                    markupobject.selectedpoint = 3;
                                    
                                    markupobject.seteditaction(4);

                                    break;
                                    //return within;
                                }
                            }


                            
                            break;    

                        case 20:
                                
                            if(markupobject.subtype == 1 || markupobject.subtype == 3){
                                if (markupobject.Getwithin(x, y, this.xscaled, this.xscaled + this.wscaled, this.yscaled, this.yscaled + this.hscaled)) {
                                    within = true;
                                    markupobject.seteditaction(1);
                                }
                                selectcorner = markupobject.GetwithinCorner(orgmousepoint.x, orgmousepoint.y);
                                if (selectcorner.within) {
                                    within = true;
                                    markupobject.seteditaction(selectcorner.editaction);
                                    this.scalecorner = selectcorner.scalecorner;
                                }
                            }
                            break;
                            

                    }
                    return within;
                };

                this.ispointwithinarea = function (x, y) {
 
                    if(markupobject.noDraw) {
                        return false;
                    }
 
                    var offset = getCurrentPageOffset(markupobject.pagenumber);
 
                    var pagedx = offset.x;
                    var pagedy = offset.y;
 
                    var scalefactor = getCurrentPageScale();
                    scalefactor = scalefactor / markupobject.scaling;
 
                    var within = false;
                    var orgmousepoint = { x: x, y: y };
                    var extentx = Math.min(this.xscaled, this.xscaled + this.wscaled);
                    var extentw = Math.max(this.xscaled, this.xscaled + this.wscaled);
                    var extenty = Math.min(this.yscaled, this.yscaled + this.hscaled);
                    var extenth = Math.max(this.yscaled, this.yscaled + this.hscaled);
                    var checktype = this.type;
                    switch (checktype) {
                        case 3:
                            if (markupobject.Getwithin(x, y, extentx, extentw, extenty, extenth)) {
                                within = true;                                
                            }
                            var selectcorner = markupobject.GetwithinCorner(orgmousepoint.x, orgmousepoint.y);
                            if (selectcorner.within) {
                                within = true;
                            }
                            break;
                        case 8:
                            var mp = {
                                x: x,
                                y: y
                            };
                            var lp1 = {
                                x: this.xscaled,
                                y: this.yscaled
                            };
                            var lp2 = {
                                x: this.wscaled,
                                y: this.hscaled
                            };
                            var epsilon = {
                                x: lp1.x - ((lp2.x - lp1.x) / 10),
                                y: lp2.y - ((lp2.y - lp1.y) / 2)
                            };
                            within = insidepolygon(scalefactor, pagedx, pagedy, markupobject.xoffset, markupobject.yoffset, markupobject.sides, mp, epsilon);
                    }
                    return within;
                };                

                this.setdisplay = function (onOff) {
                    if (markupobject.type == 0 && markupobject.subtype == 1) {
                        return;
                    }
                    markupobject.display = onOff;
                };

                this.showperimeter = function(onoff){
                    
                    markupobject.bshowperimeter = onoff;

                };



            }
        }

    function finddoublemarkup(x, y) {
        var curmarkup = 0;
        var found = false;
        var id = -1;

        //for (curmarkup = 0; curmarkup < DocObj.markuplist.length; curmarkup++) {
        for (curmarkup = DocObj.markupdraworder.length; curmarkup > 0; curmarkup--) {
            var curindex = DocObj.markupdraworder[curmarkup-1];

            if (DocObj.markuplist[curindex] != null) {

                var bcurrentView = markupcurrentview(DocObj.markuplist[curindex]);

                if (DocObj.markuplist[curindex].iswithin(x, y) && DocObj.markuplist[curindex].signature == signature && DocObj.markuplist[curindex].display && bcurrentView) {
                //if (DocObj.markuplist[curindex].iswithin(x, y) && DocObj.markuplist[curindex].signature == signature && DocObj.markuplist[curindex].display) {


                    switch (DocObj.markuplist[curindex].type) {
                        /*case 1:
                         //editpolygon
                         DocObj.markuplist[curmarkup - 1].selectedit = true;
                         DocObj.markuplist[curmarkup - 1].selected = false;
                         found = true;
                         id = DocObj.markuplist[curmarkup - 1].markupnumber;
                         break;
                         case 8:
                         //edit area
                         DocObj.markuplist[curmarkup - 1].selectedit = true;
                         DocObj.markuplist[curmarkup - 1].selected = false;
                         found = true;
                         id = DocObj.markuplist[curmarkup - 1].markupnumber;

                         break;*/
                        case 9:
                            unselelectallmarkup();
                            unselecteditmarkupall();
                            //edit text
                            DocObj.markuplist[curindex].setselectedit(true);
                            DocObj.markuplist[curindex].setselect(false);
                            
                            found = true;
                            id = DocObj.markuplist[curindex].markupnumber;

                            break;
                        case 10:
                            //unselelectallmarkup();
                            //unselecteditmarkupall();
                            //DocObj.markuplist[curmarkup - 1].selectedit = true;
                            //DocObj.markuplist[curmarkup - 1].selected = false;
                            //found = true;
                            //id = DocObj.markuplist[curmarkup - 1].markupnumber;

                            break;
                        default:
                            //DocObj.markuplist[curmarkup - 1].selected = true;
                            //found = true;
                            //id = DocObj.markuplist[curmarkup - 1].markupnumber;

                            break;

                    } //end switch

                } //end if
            } //end if
            if(found){
              break;
            }
        } //end for
        if (!found) {
            /*for (curmarkup = 0; curmarkup < DocObj.markuplist.length; curmarkup++) {
                if (DocObj.markuplist[curmarkup] != null) {
                    DocObj.markuplist[curmarkup].selected = false;
                    DocObj.markuplist[curmarkup].selectedit = false;
                }

            }*/

        }


        return id;

    }

    function delayedmarkup(pageobject){
        var curmarkup = 0;
    }

    function getxmlentitynode(pageobject){
        var curmarkup = 0;
        //var found = false;
        //var id = -1;

        var pgnumber = pageobject.pagenumber;

        for (curmarkup = 0; curmarkup < DocObj.markuplist.length; curmarkup++) {

            if (DocObj.markuplist[curmarkup] != null && DocObj.markuplist[curmarkup].pagenumber == pgnumber) {
                //var fixedscale = pageobject.fixedScale;
                //var internalscale = pageobject.curpagescale * pageobject.dscalepdf;

                //DocObj.markuplist[curmarkup].updatePageMarkup(pageobject);


                DocObj.markuplist[curmarkup].createXMLEntitynode(pageobject);


            }
        }


    }


    function setmarkupdimensions(pageobject){
        var curmarkup = 0;
        //var found = false;
        //var id = -1;

        if(DocObj == null || DocObj == undefined){
            return;
        }

        var pgnumber = pageobject.pagenumber;

        for (curmarkup = 0; curmarkup < DocObj.markuplist.length; curmarkup++) {

            if (DocObj.markuplist[curmarkup] != null && DocObj.markuplist[curmarkup].pagenumber == pgnumber) {
                //var fixedscale = pageobject.fixedScale;
                //var internalscale = pageobject.curpagescale * pageobject.dscalepdf;

                DocObj.markuplist[curmarkup].updatePageMarkup(pageobject);

            }
        }

    }


    function findmarkupHover(x, y) {
        var curmarkup = 0;
        var found = false;
        var id = -1;

        if(!benablemarkuhover){
            return id;
        }


        for (curmarkup = 0; curmarkup < DocObj.markuplist.length; curmarkup++) {
            var curindex = DocObj.markupdraworder[curmarkup];

            if (DocObj.markuplist[curmarkup] != null) {

                var bcurrentView = markupcurrentview(DocObj.markuplist[curmarkup]);

                if (DocObj.markuplist[curmarkup].iswithin(x, y) && DocObj.markuplist[curmarkup].display && bcurrentView) {
                //if (DocObj.markuplist[curmarkup].iswithin(x, y) && DocObj.markuplist[curmarkup].display && DocObj.markuplist[curmarkup].pagenumber == DocObj.getcurPage()) {
                    found = true;
                    id = DocObj.markuplist[curmarkup].markupnumber;
                }
            }
        }

        return id;

    }

    function CutmarkupSelected(){

        CopymarkupSelected();
        deletemarkup();
        
    }

    function CopymarkupSelected() {
        var curmarkup = 0;
        
        if (typeof(Storage) !== "undefined") {
            localStorage.clear();
            licTolocalStorage(LicenseID);
            
            userToLocalStorage("","",false);

        }
        

        for (curmarkup = 0; curmarkup < DocObj.markuplist.length; curmarkup++) {
            if (DocObj.markuplist[curmarkup].selected == true || DocObj.markuplist[curmarkup].selectedit ) {
                
                if (bMultiselect || DocObj.getMarkupMultiselected()){
                    multiMarkupToLocalStorage(DocObj.markuplist[curmarkup]);
                }else{
                    singleMarkupToLocalStorage(DocObj.markuplist[curmarkup]);
                }
                
                //MarkupToLocalStorage(DocObj.markuplist[curmarkup]);
            }
        }
    }

    
    function getSelectedmarkup() {
        var selectedmarkup = {isempty : true};

        for (var curmarkup = 0; curmarkup < DocObj.markuplist.length; curmarkup++) {
            if (DocObj.markuplist[curmarkup].selected || DocObj.markuplist[curmarkup].selectedit ) {
                if(DocObj.markuplist[curmarkup].bhasArrow && DocObj.markuplist[curmarkup].markupArrowConnected != null){
                    
                    selectedmarkup = DocObj.markuplist[curmarkup].markupArrowConnected;
                    
                }else{
                    selectedmarkup = DocObj.markuplist[curmarkup];
                    
                }
            }
        }
        return selectedmarkup;
    }


    function ChangeMspaceColorByindx(indx,type){

        DocObj.markuplist[curmarkup].fillcolor = convertHex(markupfillcolor, DocObj.markuplist[curmarkup].transparency);


    }
    function ChangeMarkupByindx(indx,type){
        var curmarkup = 0;

        for (curmarkup = 0; curmarkup < DocObj.markuplist.length; curmarkup++) {
            if (DocObj.markuplist[curmarkup].markupnumber == indx) {

                ChangeMarkup(DocObj.markuplist[curmarkup], type);

                //drawmarkupAll(cntximg);
                //DrawMarkupSelected(context);


            }
        }
    }

    function SpaceidAttribute(szname, szvalue){
        var curmarkup = 0;

        for (curmarkup = 0; curmarkup < DocObj.markuplist.length; curmarkup++) {
            if (DocObj.markuplist[curmarkup].selected || DocObj.markuplist[curmarkup].selectedit) {
                if (DocObj.markuplist[curmarkup].type == 8 && DocObj.markuplist[curmarkup].subtype == 1){
                    DocObj.markuplist[curmarkup].updateAttribute(szname, szvalue);
                }
            }
        }
    }

    function ChangeMarkupSelectedDisplay(onoff){
        var curmarkup = 0;

        for (curmarkup = 0; curmarkup < DocObj.markuplist.length; curmarkup++) {
            if (DocObj.markuplist[curmarkup].selected || DocObj.markuplist[curmarkup].selectedit) {

                if(onoff){
                    DocObj.markuplist[curmarkup].show();
                }else{
                    DocObj.markuplist[curmarkup].hide();
                }
            }
        }


        drawmarkupAll(cntximg);
        DrawMarkupSelected(context);

    }

    function ChangeMarkup(markupobj, type){

        if(markupobj){
            if(markupobj.ChangeMarkup != undefined){
                if (typeof markupobj.ChangeMarkup === "function") {

                    markupobj.ChangeMarkup(type);
        
                }
            }
        }


    }    

    function ChangeMarkupSelected(type, bAll) {
        var curmarkup = 0;

        if(bMultiselect){
            nMarkupUndoIdstart += 1;
            var nchangeid = nMarkupUndoIdstart;
        }


        for (curmarkup = 0; curmarkup < DocObj.markuplist.length; curmarkup++) {
            if (DocObj.markuplist[curmarkup].selected || DocObj.markuplist[curmarkup].selectedit || bAll) {

                if(bMultiselect){

                    DocObj.markuplist[curmarkup].setmultiValues(nchangeid);
                      
               }

                ChangeMarkup(DocObj.markuplist[curmarkup], type);

            }
        }

        if (type == 4 && DocObj.selectedmarkup.id != -1 && DocObj.selectedmarkup.edit && DocObj.selectedmarkup.selected){

            ChangeMarkup(DocObj.markuplist[DocObj.selectedmarkup.id], type);

            //DocObj.markuplist[DocObj.selectedmarkup.id].font.setFontname(defaultFont.fontName);
            //DocObj.markuplist[DocObj.selectedmarkup.id].font.setHeight(defaultFont.height);
            //DocObj.markuplist[DocObj.selectedmarkup.id].font.setBold(defaultFont.bold);
            //DocObj.markuplist[DocObj.selectedmarkup.id].font.setItalic(defaultFont.italic);

        }

        drawmarkupAll(cntximg);
        DrawMarkupSelected(context);


    }

    function unselectmarkupindex(indx){

        if(DocObj.markuplist[indx] == undefined){
            return;
        }

        if(DocObj.markuplist[indx].selected){
            DocObj.markuplist[indx].setselect(false);
            DocObj.markuplist[indx].bUsemouseinput = true;

            if(RxCore_GUI_MarkupUnselect != undefined){
                RxCore_GUI_MarkupUnselect.setMarkupunSelected(DocObj.markuplist[indx]);
            }
        }
        if(DocObj.markuplist[indx].selectedit){
            
            DocObj.markuplist[indx].setselectedit(false);
            DocObj.markuplist[indx].bpointEdit = false;
            DocObj.markuplist[indx].bUsemouseinput = true;

            if(RxCore_GUI_MarkupUnselect != undefined){
                RxCore_GUI_MarkupUnselect.setMarkupunSelected(DocObj.markuplist[indx]);
            }
        }


    }

    function unselectmarkup(id){
        for (var curmarkup = 0; curmarkup < DocObj.markuplist.length; curmarkup++) {
            if (DocObj.markuplist[curmarkup] != null && DocObj.markuplist[curmarkup].markupnumber == id) {
                //DocObj.markuplist[curmarkup].selected = false;
                if(DocObj.markuplist[curmarkup].selected){
                    DocObj.markuplist[curmarkup].setselect(false);
                    DocObj.markuplist[curmarkup].bUsemouseinput = true;
                    if(RxCore_GUI_MarkupUnselect != undefined){
                        RxCore_GUI_MarkupUnselect.setMarkupunSelected(DocObj.markuplist[curmarkup]);
                    }
                }
                if(DocObj.markuplist[curmarkup].selectedit){
                    
                    DocObj.markuplist[curmarkup].setselectedit(false);
                    DocObj.markuplist[curmarkup].bpointEdit = false;
                    DocObj.markuplist[curmarkup].bUsemouseinput = true;

                    if(RxCore_GUI_MarkupUnselect != undefined){
                        RxCore_GUI_MarkupUnselect.setMarkupunSelected(DocObj.markuplist[curmarkup]);
                    }
                }


            }

        }
        
        //DocObj.setMarkupMultiselected();
        //drawmarkupAll(cntximg);
        //DrawMarkupSelected(context);

    }

    function unselectallnotid(id){

        var compid = null;
        var bunselect = false;
        var bcompunselect = false;

        for (var curmarkup = 0; curmarkup < DocObj.markuplist.length; curmarkup++) {

            bunselect = (DocObj.markuplist[curmarkup].markupnumber != id);
            
            if (DocObj.markuplist[curmarkup].bhasArrow && DocObj.markuplist[curmarkup].markupArrowConnected != null) {
                
                compid = DocObj.markuplist[curmarkup].markupArrowConnected.markupnumber;
                bcompunselect = (id == compid);
            }
            if (DocObj.markuplist[curmarkup].bisTextArrow && DocObj.markuplist[curmarkup].textBoxConnected != null) {
                
                compid = DocObj.markuplist[curmarkup].textBoxConnected.markupnumber;
                bcompunselect = (id == compid);
            }

            bunselect = (!bcompunselect && DocObj.markuplist[curmarkup].markupnumber != id);
    
            /*if (compid != null){
                bunselect = bcompunselect && bunselect;
            }*/

            if (DocObj.markuplist[curmarkup] != null && bunselect) {
                //DocObj.markuplist[curmarkup].selected = false;
                if(DocObj.markuplist[curmarkup].selected){
                    DocObj.markuplist[curmarkup].setselect(false);
                    DocObj.markuplist[curmarkup].bUsemouseinput = true;
                    if(RxCore_GUI_MarkupUnselect != undefined){
                        RxCore_GUI_MarkupUnselect.setMarkupunSelected(DocObj.markuplist[curmarkup]);
                    }
                }
                if(DocObj.markuplist[curmarkup].selectedit){
                    
                    //DocObj.markuplist[curmarkup].selectedit = false;
                    DocObj.markuplist[curmarkup].setselectedit(false);
                    DocObj.markuplist[curmarkup].bpointEdit = false;
                    DocObj.markuplist[curmarkup].bUsemouseinput = true;

                    if(RxCore_GUI_MarkupUnselect != undefined){
                        RxCore_GUI_MarkupUnselect.setMarkupunSelected(DocObj.markuplist[curmarkup]);
                    }
                }


            }

        }

        
        //DocObj.setMarkupMultiselected();

        drawmarkupAll(cntximg);
        DrawMarkupSelected(context);


    }    

    function unselecteditmarkup(id) {

        for (var curmarkup = 0; curmarkup < DocObj.markuplist.length; curmarkup++) {
            if (DocObj.markuplist[curmarkup] != null && DocObj.markuplist[curmarkup].markupnumber != id) {
                //DocObj.markuplist[curmarkup].selected = false;
                if(DocObj.markuplist[curmarkup].selectedit){
                    DocObj.markuplist[curmarkup].setselectedit(false);
                    DocObj.markuplist[curmarkup].bpointEdit = false;
                    if(RxCore_GUI_MarkupUnselect != undefined){
                        RxCore_GUI_MarkupUnselect.setMarkupunSelected(DocObj.markuplist[curmarkup]);
                    }
                }


            }

        }
        
        //DocObj.setMarkupMultiselected();

        drawmarkupAll(cntximg);
        DrawMarkupSelected(context);

    }

    function deleteMarkup3DView(name){


        var curmarkup = 0;
        var deletedmarkup = 0;
        var markupcount = 0;
        var id = -1;
        //var tempmarkuplistsel = new Array();
        var tempmarkuplist = [];
        DocObj.markupdraworder = [];

        for (curmarkup = 0; curmarkup < DocObj.markuplist.length; curmarkup++) {
            //DocObj.markuplist[curmarkup].usser
            if (DocObj.markuplist[curmarkup] != null ) {
                var bOwned = (DocObj.markuplist[curmarkup].signature == signature);
                //console.log(DocObj.markuplist[curmarkup].selected, DocObj.markuplist[curmarkup].selectedit);

                if(bOwned && (DocObj.markuplist[curmarkup].bfor3D && DocObj.markuplist[curmarkup].viewname == name)){
                    //id = DocObj.markuplist[curmarkup].markupnumber;
                    //MarkupSaveState(id, undoDelete);

                    /*if (RxCore_GUI_Markup != undefined) {
                        RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[curmarkup], {created : false, modified : false, deleted : true});
                    }*/
                }else{
                    tempmarkuplist[markupcount] = DocObj.markuplist[curmarkup];
                    DocObj.markupdraworder[markupcount] = markupcount;
                    markupcount++;
                }

                /*if (!DocObj.markuplist[curmarkup].selected && !DocObj.markuplist[curmarkup].selectedit ) {
                    //id = DocObj.markuplist[curmarkup].markupnumber;
                    //MarkupSaveState(id);

                    tempmarkuplist[markupcount] = DocObj.markuplist[curmarkup];                                                                                                   
                    markupcount++;
                } else {
                    id = DocObj.markuplist[curmarkup].markupnumber;
                    MarkupSaveState(id);

                }*/

            }
        }
        //DocObj.markuplist = [];
        DocObj.markuplist = tempmarkuplist;
        DocObj.nummarkups = DocObj.markuplist.length;

        /*    for (deletedmarkup=0;deletedmarkup<tempmarkuplist.length;deletedmarkup++){
         //delete DocObj.markuplist[tempmarkuplist[deletedmarkup]];
         //DocObj.markuplist.splice(tempmarkuplist[deletedmarkup],1);
         }*/

        if (RxCore_GUI_Markuplist != undefined) {
            RxCore_GUI_Markuplist.setMarkupList(DocObj.getmarkuplist(), DocObj.bActive);
            /* Andriy */
            // RxCore_GUI_Markuplist.notify();

        }
        if (RxCore_GUI_pagethumbs != undefined) {
            RxCore_GUI_pagethumbs.setThumbnails(DocObj.thumbnails);
        }

        drawmarkupAll(cntximg);
        DrawMarkupSelected(context);

    }



    function deleteUniqueMarkup(GUID){
        for (var curdoc = 0;curdoc < OpenFiles.length;curdoc++){
            for (var curmarkup = 0; curmarkup < OpenFiles[curdoc].markuplist.length; curmarkup++) {
                if (OpenFiles[curdoc].markuplist[curmarkup] != null) {


                    if(OpenFiles[curdoc].markuplist[curmarkup].getUniqueID() == GUID){

                        if(OpenFiles[curdoc].markuplist[curmarkup].bhasArrow &&  OpenFiles[curdoc].markuplist[curmarkup].markupArrowConnected != null){
                            OpenFiles[curdoc].markuplist[curmarkup].markupArrowConnected.setdelete();
                        }

                        OpenFiles[curdoc].markuplist[curmarkup].setdelete();

                        //MarkupSaveState(OpenFiles[curdoc].markuplist[curmarkup].markupnumber, undoDelete);
                        //OpenFiles[curdoc].markuplist.splice(curmarkup,1);
                    }
                }
            }

        }

        deletemarkup();

    }

    function findUniqueMarkup(GUID){
        var uniqueMarkup = {isempty : true};
        for (var curdoc = 0;curdoc < OpenFiles.length;curdoc++){
            for (var curmarkup = 0; curmarkup < OpenFiles[curdoc].markuplist.length; curmarkup++) {
                if (OpenFiles[curdoc].markuplist[curmarkup] != null) {
                    if(OpenFiles[curdoc].markuplist[curmarkup].getUniqueID() == GUID){
                        uniqueMarkup = OpenFiles[curdoc].markuplist[curmarkup];
                    }
                }
            }

        }
        return uniqueMarkup;

    }
    function checkUniqueID(ID){
        var bidunique = true;
        for (var curdoc = 0;curdoc < OpenFiles.length;curdoc++){
            for (var curmarkup = 0; curmarkup < OpenFiles[curdoc].markuplist.length; curmarkup++) {
                if (OpenFiles[curdoc].markuplist[curmarkup] != null) {
                    if(OpenFiles[curdoc].markuplist[curmarkup].getUniqueID() == ID){
                        bidunique = false;
                    }
                }
            }

        }
        return bidunique;
    }

    function unselecteditmarkupall() {

        if(DocObj == undefined){
            return;
        }

        for (var curmarkup = 0; curmarkup < DocObj.markuplist.length; curmarkup++) {
            if (DocObj.markuplist[curmarkup] != null) {
                //DocObj.markuplist[curmarkup].selected = false;
                if(DocObj.markuplist[curmarkup].selectedit){
                    DocObj.markuplist[curmarkup].setselectedit(false);
                    DocObj.markuplist[curmarkup].bpointEdit = false;

                    if(RxCore_GUI_MarkupUnselect != undefined){
                        RxCore_GUI_MarkupUnselect.setMarkupunSelected(DocObj.markuplist[curmarkup]);
                    }
                }


            }

        }
        //DocObj.setMarkupMultiselected();

        drawmarkupAll(cntximg);
        DrawMarkupSelected(context);

    }

    function setpointbeforeunselect(mousepos){
        for (var curmarkup = 0; curmarkup < DocObj.markuplist.length; curmarkup++) {
            if (DocObj.markuplist[curmarkup] != null) {
                
            if(DocObj.markuplist[curmarkup].selectedit && DocObj.markuplist[curmarkup].type == 13){

                //this.findrectangle
                DocObj.markuplist[curmarkup].insertcount(mousepos);
                
                if(tool.name == 'markupedit'){
                    tool.disablePointInsert();
                }
        

            }

                /*if(DocObj.markuplist[curmarkup].selected){
                    DocObj.markuplist[curmarkup].setselect(false);
                    DocObj.markuplist[curmarkup].bUsemouseinput = true;
                    if(RxCore_GUI_MarkupUnselect != undefined){
                        RxCore_GUI_MarkupUnselect.setMarkupunSelected(DocObj.markuplist[curmarkup]);
                    }
                }*/

                //DocObj.markuplist[curmarkup].selectedit = false;
            }

        }
    }
    
    function checkmarkupselected(id, markup, found){

        if (!found) {
            
            if (RxCore_GUI_Markup != undefined) {
                if (id == -1){
                    RxCore_GUI_Markup.setMarkupSelected(id, {created : false, modified : false, deleted : false});
                }else{
                    var btextboxleader = (markup.type == 9 &&  markup.subtype == 2);
                    if(!btextboxleader){
                        RxCore_GUI_Markup.setMarkupSelected(markup, {created : false, modified : false, deleted : false});
                    }else{
                        //RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[index].markupArrowConnected, {created : false, modified : false, deleted : false});
                        RxCore_GUI_Markup.setMarkupSelected(markup, {created : false, modified : false, deleted : false});
                    }
                    
                }    
                
            }

        } else {
            if (RxCore_GUI_Markup != undefined && markup != undefined) {
                
                btextboxleader = (markup.type == 9 &&  markup.subtype == 2);
                
                if(btextboxleader){
                    //RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[index].markupArrowConnected, {created : false, modified : true, deleted : false});
                    RxCore_GUI_Markup.setMarkupSelected(markup, {created : false, modified : true, deleted : false});
                }else{
                    RxCore_GUI_Markup.setMarkupSelected(markup, {created : false, modified : true, deleted : false});
                }

            }

            

        }        

    }

    function checkmarkupunselected(){
        for (var curmarkup = 0; curmarkup < DocObj.markuplist.length; curmarkup++) {
            if (DocObj.markuplist[curmarkup] != null) {
                
                

                if(!DocObj.markuplist[curmarkup].selected && DocObj.markuplist[curmarkup].unselected){
                    //DocObj.markuplist[curmarkup].setselect(false);
                    //DocObj.markuplist[curmarkup].bUsemouseinput = true;
                    DocObj.markuplist[curmarkup].unselected = false;


                    if(RxCore_GUI_MarkupUnselect != undefined){
                        RxCore_GUI_MarkupUnselect.setMarkupunSelected(DocObj.markuplist[curmarkup]);
                    }
                }

                //DocObj.markuplist[curmarkup].selectedit = false;
            }

        }
    }

    function unselelecteditallmarkup() {
        for (var curmarkup = 0; curmarkup < DocObj.markuplist.length; curmarkup++) {
            if (DocObj.markuplist[curmarkup] != null) {
                
                

                /*if(DocObj.markuplist[curmarkup].selected){
                    DocObj.markuplist[curmarkup].setselect(false);
                    DocObj.markuplist[curmarkup].bUsemouseinput = true;
                    DocObj.markuplist[curmarkup].unselected = true;
                    if(RxCore_GUI_MarkupUnselect != undefined){
                        RxCore_GUI_MarkupUnselect.setMarkupunSelected(DocObj.markuplist[curmarkup]);
                    }
    
                }*/

                if(DocObj.markuplist[curmarkup].selectedit){
                    
                    DocObj.markuplist[curmarkup].setselectedit(false);
                    DocObj.markuplist[curmarkup].bpointEdit = false;
                    DocObj.markuplist[curmarkup].bUsemouseinput = true;
                    DocObj.markuplist[curmarkup].unselected = true;

                    /*if(RxCore_GUI_MarkupUnselect != undefined){
                        RxCore_GUI_MarkupUnselect.setMarkupunSelected(DocObj.markuplist[curmarkup]);
                    }*/
                }



                if(DocObj.markuplist[curmarkup].marked){
                    DocObj.markuplist[curmarkup].setmarked(false); 
                }

                //DocObj.markuplist[curmarkup].selectedit = false;
            }

        }
        
        //DocObj.setMarkupMultiselected();

        drawmarkupAll(cntximg);
        DrawMarkupSelected(context);
    }

    function unselectallBlocks(){
        DocObj.unselectallBlocks();
    }

    function unselelectallmarkup() {

        if(DocObj == undefined){
            return;
        }


        for (var curmarkup = 0; curmarkup < DocObj.markuplist.length; curmarkup++) {
            if (DocObj.markuplist[curmarkup] != null) {
                
                

                if(DocObj.markuplist[curmarkup].selected){
                    DocObj.markuplist[curmarkup].setselect(false);
                    DocObj.markuplist[curmarkup].bUsemouseinput = true;
                    DocObj.markuplist[curmarkup].unselected = true;
                    /*if(RxCore_GUI_MarkupUnselect != undefined){
                        RxCore_GUI_MarkupUnselect.setMarkupunSelected(DocObj.markuplist[curmarkup]);
                    }*/

    
                }

                /*if(DocObj.markuplist[curmarkup].selectedit){
                    
                    DocObj.markuplist[curmarkup].selectedit = false;
                    DocObj.markuplist[curmarkup].bpointEdit = false;
                    DocObj.markuplist[curmarkup].bUsemouseinput = true;
                    DocObj.markuplist[curmarkup].unselected = true;

                    if(RxCore_GUI_MarkupUnselect != undefined){
                        RxCore_GUI_MarkupUnselect.setMarkupunSelected(DocObj.markuplist[curmarkup]);
                    }
                }*/



                if(DocObj.markuplist[curmarkup].marked){
                    DocObj.markuplist[curmarkup].setmarked(false); 
                }

                //DocObj.markuplist[curmarkup].selectedit = false;
            }

        }
        
        //DocObj.setMarkupMultiselected();

        drawmarkupAll(cntximg);
        DrawMarkupSelected(context);

    }


    function selectSpaceMarkup(blockid, multi){
        
        var markupnull = {};
        var customattribs = [];
        var partlist = [];
        var selected = [];
        var spaceobj = {isempty : true};
        var curmarkup = 0;
        var id = -1;

        var spacemarkup = findSpaceMarkup(blockid);
        var bfound = (spacemarkup != undefined);

        for (curmarkup = DocObj.markuplist.length; curmarkup > 0; curmarkup--) {

            if(bfound){
                var bspaceid = (spacemarkup.markupnumber == DocObj.markuplist[curmarkup - 1].markupnumber);
            }else{
                bspaceid = false;
            }

            if (DocObj.markuplist[curmarkup - 1] != null && !bspaceid) {
                
                if(DocObj.markuplist[curmarkup - 1].type == 8 && DocObj.markuplist[curmarkup - 1].subtype == 1){

                    
                    if(multi && DocObj.markuplist[curmarkup - 1].spaceselected){

                        id = DocObj.markuplist[curmarkup - 1].markupnumber;
                            customattribs = DocObj.markuplist[curmarkup - 1].GetAttributes();
                            spaceobj = {
                                id : id,
                                attr : customattribs,
                                isempty : false 
                            };
                            selected.push(spaceobj);
                    }else{
                            DocObj.markuplist[curmarkup - 1].spaceselected = false;
                            DocObj.markuplist[curmarkup - 1].linewidth = 1;
                    }

                }
            }
        }



        if(bfound){
            id = spacemarkup.markupnumber;
            
            spacemarkup.spaceselected = !spacemarkup.spaceselected;
           
            customattribs = spacemarkup.GetAttributes();

            if (spacemarkup.spaceselected){

                spacemarkup.linewidth = 2;
                spaceobj = {
                    id : id,
                    attr : customattribs,
                    isempty : false 
                };                
                selected.push(spaceobj);

            }else{
                spacemarkup.linewidth = 1;
            }
            
        }
        

        if (selected.length == 0 && !spacemarkup.spaceselected){
            RxCore_animateLineStyle(false);
        }else{
            RxCore_animateLineStyle(true);
        }
        

        if(multi){
            if (RxCore_GUI_2DBlockID != undefined) {
                RxCore_GUI_2DBlockID.set2DBlockID(selected);
            }
        }else{
            if (RxCore_GUI_2DBlockID != undefined) {
                RxCore_GUI_2DBlockID.set2DBlockID(spaceobj);
            }
        }        



        drawmarkupAll(cntximg);
        DrawMarkupSelected(context);


    }

    function findSpaceMarkup(blockid){
        var curmarkup = 0;
        var markupobj = undefined;

        for (curmarkup = DocObj.markuplist.length; curmarkup > 0; curmarkup--) {
            if (DocObj.markuplist[curmarkup - 1] != null) {
                if (DocObj.markuplist[curmarkup - 1].display && DocObj.markuplist[curmarkup - 1].pagenumber == DocObj.getcurPage()) {

                    if(DocObj.markuplist[curmarkup - 1].type == 8 && DocObj.markuplist[curmarkup - 1].subtype == 1){
                        
                        
                        var szblockid = blockid.toString();
                        if (DocObj.markuplist[curmarkup - 1].findAttribute('SpaceID', szblockid)){
                            markupobj = DocObj.markuplist[curmarkup - 1];
                            
                        }
                
                    }
                    
                }

            }
        }

        return markupobj;        

    }


    function restoreSpacemarkupStates(){
        var curmarkup = 0;
        for (curmarkup = DocObj.markuplist.length; curmarkup > 0; curmarkup--) {
            if (DocObj.markuplist[curmarkup - 1] != null) {
                if (DocObj.markuplist[curmarkup - 1].display && DocObj.markuplist[curmarkup - 1].pagenumber == DocObj.getcurPage()) {

                    if(DocObj.markuplist[curmarkup - 1].type == 8 && DocObj.markuplist[curmarkup - 1].subtype == 1){
                        
                        //id = DocObj.markuplist[curmarkup - 1].markupnumber;
                        //DocObj.markuplist[curmarkup - 1].spaceselected = !DocObj.markuplist[curmarkup - 1].spaceselected;
                        
                        DocObj.markuplist[curmarkup - 1].alternative = 0;

                    }
                    
                }

            }
        }

        drawmarkupAll(cntximg);
        DrawMarkupSelected(context);
    }

    function setMarkupSpaceHatch(blockid, hatchname, override){
        var curmarkup = 0;
        var id = -1;
        var customattribs = [];
        var found = false;

        for (curmarkup = DocObj.markuplist.length; curmarkup > 0; curmarkup--) {
            if (DocObj.markuplist[curmarkup - 1] != null) {
                if (DocObj.markuplist[curmarkup - 1].display && DocObj.markuplist[curmarkup - 1].pagenumber == DocObj.getcurPage()) {

                    if(DocObj.markuplist[curmarkup - 1].type == 8 && DocObj.markuplist[curmarkup - 1].subtype == 1){
                        
                        //id = DocObj.markuplist[curmarkup - 1].markupnumber;
                        //DocObj.markuplist[curmarkup - 1].spaceselected = !DocObj.markuplist[curmarkup - 1].spaceselected;
                        
                        var szblockid = blockid.toString();
                        if (DocObj.markuplist[curmarkup - 1].findAttribute('SpaceID', szblockid)){
                            found = true;
                            if(override){

                                var type = getHatcTypeforName(hatchname);
                                if(type != -1){
                                    DocObj.markuplist[curmarkup - 1].alternative = type + 3;
                                    DocObj.markuplist[curmarkup - 1].hatchStyle = type;
                                }
                                
                                //DocObj.markuplist[curmarkup - 1].fillcolor = Color;

                            }else{
                                DocObj.markuplist[curmarkup - 1].alternative = 0;
                                //DocObj.markuplist[curmarkup - 1].fillcolor = Color;

                            }
                            //DocObj.markuplist[curmarkup - 1].setFillColor(Color, override);
                        }
                
                    }
                    
                }

            }
        }

        drawmarkupAll(cntximg);
        DrawMarkupSelected(context);
        return found;
    }

    function setMarkupSpaceColor(blockid, Color, override){
        var curmarkup = 0;
        var id = -1;
        var customattribs = [];
        var found = false;

        for (curmarkup = DocObj.markuplist.length; curmarkup > 0; curmarkup--) {
            if (DocObj.markuplist[curmarkup - 1] != null) {
                if (DocObj.markuplist[curmarkup - 1].display && DocObj.markuplist[curmarkup - 1].pagenumber == DocObj.getcurPage()) {

                    if(DocObj.markuplist[curmarkup - 1].type == 8 && DocObj.markuplist[curmarkup - 1].subtype == 1){
                        
                        //id = DocObj.markuplist[curmarkup - 1].markupnumber;
                        //DocObj.markuplist[curmarkup - 1].spaceselected = !DocObj.markuplist[curmarkup - 1].spaceselected;
                        
                        var szblockid = blockid.toString();
                        if (DocObj.markuplist[curmarkup - 1].findAttribute('SpaceID', szblockid)){
                            found = true;
                            if(override){
                                DocObj.markuplist[curmarkup - 1].alternative = 1;
                                DocObj.markuplist[curmarkup - 1].fillcolor = Color;

                            }else{
                                DocObj.markuplist[curmarkup - 1].alternative = 0;
                                //DocObj.markuplist[curmarkup - 1].fillcolor = Color;

                            }
                            //DocObj.markuplist[curmarkup - 1].setFillColor(Color, override);
                        }
                
                    }
                    
                }

            }
        }

        drawmarkupAll(cntximg);
        DrawMarkupSelected(context);
        return found;
    }

    function unselectAllSpaceMarkup(){
        for (var curmarkup = 0; curmarkup < DocObj.markuplist.length; curmarkup++) {
            if (DocObj.markuplist[curmarkup] != null) {
                
                if(DocObj.markuplist[curmarkup - 1].type == 8 && DocObj.markuplist[curmarkup - 1].subtype == 1){
                    DocObj.markuplist[curmarkup].spaceselected = false;
                }
                


                /*if(DocObj.markuplist[curmarkup].spaceselected){
                    DocObj.markuplist[curmarkup].setselect(false);
                    DocObj.markuplist[curmarkup].bUsemouseinput = true;
                    
                    
                    
                    if(RxCore_GUI_MarkupUnselect != undefined){
                        RxCore_GUI_MarkupUnselect.setMarkupunSelected(DocObj.markuplist[curmarkup]);
                    }
                }*/

                //DocObj.markuplist[curmarkup].selectedit = false;
            }

        }
        
        if (RxCore_GUI_2DBlockID != undefined) {
            RxCore_GUI_2DBlockID.set2DBlockID([]);
        }

        
        //DocObj.setMarkupMultiselected();

        drawmarkupAll(cntximg);
        DrawMarkupSelected(context);

    }
    

    function unselectAllSpaces(){
        for (var curmarkup = 0; curmarkup < DocObj.markuplist.length; curmarkup++) {
            if (DocObj.markuplist[curmarkup] != null) {
                
                
                if(DocObj.markuplist[curmarkup - 1].type == 8 && DocObj.markuplist[curmarkup - 1].subtype == 1){
                    if(DocObj.markuplist[curmarkup].spaceselected){

                        DocObj.markuplist[curmarkup].spaceselected = false;
                        
                    }

                }

                //DocObj.markuplist[curmarkup].selectedit = false;
            }

        }
        
        //DocObj.setMarkupMultiselected();

        drawmarkupAll(cntximg);
        DrawMarkupSelected(context);
    }

    function findspacemarkup(x,y, multi){
        var curmarkup = 0;
        var found = false;
        var foundwithin = false;
        var id = -1;
        var blink = false;
        var index = -1;
        var markupnull = {};
        var customattribs = [];
        var partlist = [];
        var selected = [];
        var spaceobj = {isempty : true};

  

        for (curmarkup = DocObj.markupdraworder.length; curmarkup > 0; curmarkup--) {

            //var curindex  = DocObj.markupdraworder[curmarkup - 1];

            if (DocObj.markuplist[curmarkup - 1] != null) {
                
                if(DocObj.markuplist[curmarkup - 1].type == 8 && DocObj.markuplist[curmarkup - 1].subtype == 1){
                    if (DocObj.markuplist[curmarkup - 1].iswithin(x, y) && DocObj.markuplist[curmarkup - 1].display && DocObj.markuplist[curmarkup - 1].pagenumber == DocObj.getcurPage()) {
    
                            var drawindex = curmarkup - 1;

                            foundwithin = true;
                            id = DocObj.markuplist[curmarkup - 1].markupnumber;
                            DocObj.markuplist[curmarkup - 1].spaceselected = !DocObj.markuplist[curmarkup - 1].spaceselected;
                            found = DocObj.markuplist[curmarkup - 1].spaceselected;

                            if (found){
                                DocObj.markuplist[curmarkup - 1].linewidth = 2;
                            }else{
                                DocObj.markuplist[curmarkup - 1].linewidth = 1;
                            }
                            
                            customattribs = DocObj.markuplist[curmarkup - 1].GetAttributes();
    
                            spaceobj = {
                                id : id,
                                attr : customattribs,
                                isempty : false 
                            };
                            
                            /*if(multi && DocObj.markuplist[curmarkup - 1].spaceselected){
                                selected.push(spaceobj);
                            }*/
                    
                        
                    }else{

                        if(!multi){
                            DocObj.markuplist[curmarkup - 1].spaceselected = false;
                            DocObj.markuplist[curmarkup - 1].linewidth = 1;
                         }

                        /*if(multi && DocObj.markuplist[curmarkup - 1].spaceselected){
                            
                            found = DocObj.markuplist[curmarkup - 1].spaceselected;
                            
                            id = DocObj.markuplist[curmarkup - 1].markupnumber;
                            customattribs = DocObj.markuplist[curmarkup - 1].GetAttributes();
                            spaceobj = {
                                id : id,
                                attr : customattribs,
                                isempty : false 
                            };
                             
                            selected.push(spaceobj);
                            
                        }*/
     
                    }                
                }
                /*if (DocObj.markuplist[curmarkup - 1].iswithin(x, y) && DocObj.markuplist[curmarkup - 1].display && DocObj.markuplist[curmarkup - 1].pagenumber == DocObj.getcurPage()) {


                    if(DocObj.markuplist[curmarkup - 1].type == 8 && DocObj.markuplist[curmarkup - 1].subtype == 1){
                        
                        id = DocObj.markuplist[curmarkup - 1].markupnumber;
                        DocObj.markuplist[curmarkup - 1].spaceselected = !DocObj.markuplist[curmarkup - 1].spaceselected;
                        
                        customattribs = DocObj.markuplist[curmarkup - 1].GetAttributes();

                        spaceobj = {
                            id : id,
                            attr : customattribs
                        };
                       
                        if(!multi){
                            if (RxCore_GUI_2DBlockInfo != undefined) {
                                RxCore_GUI_2DBlockInfo.set2DBlockInfo(spaceobj);
                            }
            
                        }
                
                    }
                    
                }else{
                    if(DocObj.markuplist[curmarkup - 1].type == 8 && DocObj.markuplist[curmarkup - 1].subtype == 1){
                        DocObj.markuplist[curmarkup - 1].spaceselected = false;
                    }
                }*/


            }// end if not null
        }//end for

        //reorder draworder array to find another markup on second select
        //var tempentry = DocObj.markupdraworder[drawindex];
        //var lastentry = DocObj.markupdraworder[DocObj.markupdraworder.length - 1];
        //DocObj.markupdraworder[DocObj.markupdraworder.length - 1] = tempentry;
        //DocObj.markupdraworder[drawindex] = lastentry;
        
        for (curmarkup = DocObj.markuplist.length; curmarkup > 0; curmarkup--) {
            
            
            if (DocObj.markuplist[curmarkup - 1] != null) {
                
                if(DocObj.markuplist[curmarkup - 1].type == 8 && DocObj.markuplist[curmarkup - 1].subtype == 1){
                    
                    if(!multi && spaceobj.id != DocObj.markuplist[curmarkup - 1].markupnumber){
                        DocObj.markuplist[curmarkup - 1].spaceselected = false;
                    }

                    if(multi && DocObj.markuplist[curmarkup - 1].spaceselected){
                        found = DocObj.markuplist[curmarkup - 1].spaceselected;
                        id = DocObj.markuplist[curmarkup - 1].markupnumber;
                            customattribs = DocObj.markuplist[curmarkup - 1].GetAttributes();
                            spaceobj = {
                                id : id,
                                attr : customattribs,
                                isempty : false 
                            };
                            selected.push(spaceobj);
                    }

                }
            }
        }

        if(multi){
            
            if (RxCore_GUI_2DBlockID != undefined) {
                RxCore_GUI_2DBlockID.set2DBlockID(selected);
            }
            
        }else{
            if (RxCore_GUI_2DBlockID != undefined) {
                RxCore_GUI_2DBlockID.set2DBlockID(spaceobj);
            }
            
        }

        if(!bAnimateLineStyle){
            drawmarkupAll(cntximg);
            DrawMarkupSelected(context);
        }
        
        return {found : found, within : foundwithin};

    }

    function toggleCycleSelect(){
        enableCycleSelect = !enableCycleSelect;

        //console.log('Cycle Select enabled:', enableCycleSelect);
    }


    function findmarkup(x, y, bCanSelectSpace) {
        var curmarkup = 0;
        var found = false;
        var id = -1;
        var blink = false;
        var index = -1;
        var locked = false;
        var markupnull = {};
        var foundmarkup = [];
        var noselect = true;

        var selectresult = {found : found, blink : blink, id : id, locked : locked, index : index};

        if(DocObj == null || DocObj == undefined){
            return;
        }
        
        DocObj.setMarkupMultiselected();

        if(!bMultiselect && !DocObj.getMarkupMultiselected()){
            unselelectallmarkup();
        }
        
        let indexList = [];


        for (curmarkup = DocObj.markupdraworder.length; curmarkup > 0; curmarkup--) {

            var curindex  = DocObj.markupdraworder[curmarkup - 1];

            if (DocObj.markuplist[curindex] != null) {

                var bcurrentView = markupcurrentview(DocObj.markuplist[curindex]);

                if (DocObj.markuplist[curindex].display && bcurrentView) {

                    if (DocObj.markuplist[curindex].iswithin(x, y)){

                        var binsidehole = false;
                        
                        var drawindex = curmarkup - 1;

                        foundmarkup.push(drawindex);


                        selectresult = DocObj.markuplist[curindex].onselect(curindex, bCanSelectSpace);
                        indexList.push(curindex);

                        if (!enableCycleSelect) {
                            selectresult = DocObj.markuplist[curindex].onselect(curindex, bCanSelectSpace);
                            DocObj.markupdraworderChange = false;


                            break;
                        } else {
                            selectresult = DocObj.markuplist[curindex].onselect(curindex, bCanSelectSpace);

                        }


                        
                    }


                }

            }
        }


        if(DocObj.markupdraworderChange){

            for (var foundc = 0;  foundc < foundmarkup.length; foundc++ ){
                var tempentry = DocObj.markupdraworder[foundmarkup[foundc]];    
                var lastentry = DocObj.markupdraworder[DocObj.markupdraworder.length - 1];
                DocObj.markupdraworder[DocObj.markupdraworder.length - 1] = tempentry;
                DocObj.markupdraworder[foundmarkup[foundc]] = lastentry;
            }
            
            if(foundmarkup.length > 0){
                DocObj.markupdraworderChange = false;
            }
            
    
        }else{

            if(foundmarkup.length > 0 && enableCycleSelect){
                DocObj.markupdraworderChange = true;
            }
            
        }
        
        

        if(!bMultiselect && !DocObj.getMarkupMultiselected()){
            unselectallnotid(selectresult.id);
        }

        if (!selectresult.found) {
            

            for (curmarkup = 0; curmarkup < DocObj.markuplist.length; curmarkup++) {
                if (DocObj.markuplist[curmarkup] != null) {
                    DocObj.markuplist[curmarkup].setselect(false);
                    DocObj.markuplist[curmarkup].bUsemouseinput = true;
                    DocObj.markuplist[curmarkup].setselectedit(false);
                    DocObj.markuplist[curmarkup].bpointEdit = false;
                    DocObj.markuplist[curmarkup].resetcupoint();


                }

            }

        }
        //add multiselect state here.
        DocObj.setMarkupMultiselected();

        checkmarkupunselected();
        checkmarkupselected(selectresult.id, DocObj.markuplist[selectresult.index], selectresult.found);


        if (RxCore_GUI_Markuplist != undefined) {
            RxCore_GUI_Markuplist.setMarkupList(DocObj.getmarkuplist(), DocObj.bActive);
            /* Andriy Notify */

        }
        


        return {
            id : selectresult.id,
            blink : selectresult.blink,
            index : selectresult.index,
            locked : selectresult.locked
        };

    }    



    function moveToFront() {
        /* Andriy: it can be declared in for loop, no need to set to 0 twice */
        // var curmarkup = 0;


        var mindex = null;

        for (var curmarkup = DocObj.markupdraworder.length; curmarkup > 0; curmarkup--) {
            var curindex = DocObj.markupdraworder[curmarkup-1];

            if (DocObj.markuplist[curindex].selected || DocObj.markuplist[curindex].selectedit) {

                var foundc = curindex;
                mindex = curmarkup-1;
                break;
            }
        }

        if(mindex != null){


            DocObj.markupdraworder.splice(mindex, 1);     // remove it
            DocObj.markupdraworder.push(foundc);      // add to end

    
        }




        var id = -1;

        /*for (var curmarkup = 0; curmarkup < DocObj.markuplist.length; curmarkup++) {
            if (DocObj.markuplist[curmarkup] != null) {
                if (!DocObj.markuplist[curmarkup].selected && !DocObj.markuplist[curmarkup].selectedit) {

                } else {
                    id = DocObj.markuplist[curmarkup].markupnumber;
                    
                    MarkupSaveState(id, undoModify);
                    
                    
                    var tempmarkup = DocObj.markuplist[curmarkup];
                    DocObj.markuplist[curmarkup] = DocObj.markuplist[DocObj.markuplist.length - 1];
                    DocObj.markuplist[DocObj.markuplist.length - 1] = tempmarkup;

                    break;
                }

            }
        }*/


        if (RxCore_GUI_Markuplist != undefined) {
            RxCore_GUI_Markuplist.setMarkupList(DocObj.getmarkuplist(), DocObj.bActive);
        }
        if (RxCore_GUI_pagethumbs != undefined) {
            RxCore_GUI_pagethumbs.setThumbnails(DocObj.thumbnails);
        }

        
        unselelectallmarkup();
        drawmarkupAll(cntximg);
        DrawMarkupSelected(context);
    }

    function moveToBack() {
        /* Andriy: it can be declared in for loop, no need to set to 0 twice */
        // var curmarkup = 0;


        /*function moveToBack(arr, index) {
            var i = arr.indexOf(index);
            if (i > -1) {
              arr.splice(i, 1);     // remove it
              arr.push(index);      // add to end
            }
            return arr;
          }*/


        var mindex = null;

        for (var curmarkup = DocObj.markupdraworder.length; curmarkup > 0; curmarkup--) {
            var curindex = DocObj.markupdraworder[curmarkup-1];

            if (DocObj.markuplist[curindex].selected || DocObj.markuplist[curindex].selectedit) {

                var foundc = curindex;
                mindex = curmarkup-1;
                break;

            }


        }

        if(mindex != null){

            DocObj.markupdraworder.splice(mindex, 1);     // remove it
            DocObj.markupdraworder.unshift(foundc);      // add to start

            //var tempentry = foundc;
            //var firstentry = DocObj.markupdraworder[0];
            
            //DocObj.markupdraworder[0] = tempentry;
            //DocObj.markupdraworder[mindex] = firstentry;
    
        }


        var id = -1;

        /*for (var curmarkup = 0; curmarkup < DocObj.markuplist.length; curmarkup++) {
            if (DocObj.markuplist[curmarkup] != null) {
                if (!DocObj.markuplist[curmarkup].selected && !DocObj.markuplist[curmarkup].selectedit) {

                } else {
                    id = DocObj.markuplist[curmarkup].markupnumber;
                    var tempmarkup = DocObj.markuplist[curmarkup];
                    DocObj.markuplist[curmarkup] = DocObj.markuplist[0];
                    DocObj.markuplist[0] = tempmarkup;
                    MarkupSaveState(id, undoModify);
                    break;
                }

            }
        }*/


        if (RxCore_GUI_Markuplist != undefined) {
            RxCore_GUI_Markuplist.setMarkupList(DocObj.getmarkuplist(), DocObj.bActive);
        }
        if (RxCore_GUI_pagethumbs != undefined) {
            RxCore_GUI_pagethumbs.setThumbnails(DocObj.thumbnails);
        }

        unselelectallmarkup();
        drawmarkupAll(cntximg);
        DrawMarkupSelected(context);
    }

    function undoremovemarkup(id){
        var curmarkup = 0;
        var deletedmarkup = 0;
        var markupcount = 0;

        var tempmarkuplist = [];

        for (curmarkup = 0; curmarkup < DocObj.markuplist.length; curmarkup++) {
            if (DocObj.markuplist[curmarkup] != null ) {
                
                var bOwned = (DocObj.markuplist[curmarkup].signature == signature);
                if(id == DocObj.markuplist[curmarkup].markupnumber){

                    var bhole = (DocObj.markuplist[curmarkup].type == 8 && DocObj.markuplist[curmarkup].subtype == 2 );

                    if(bhole){
                        
                        undoremoveholes(DocObj.markuplist[curmarkup].parent, id);
                        //DocObj.markuplist[curmarkup].parent
                    }

                    if (RxCore_GUI_Markup != undefined) {
                        RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[curmarkup], {created : false, modified : false, deleted : true});
                    }
    
                }else{
                    tempmarkuplist[markupcount] = DocObj.markuplist[curmarkup];
                    markupcount++;
                }

            }
        }

        DocObj.markuplist = tempmarkuplist;
        DocObj.nummarkups = DocObj.markuplist.length;

        if (RxCore_GUI_Markuplist != undefined) {
            RxCore_GUI_Markuplist.setMarkupList(DocObj.getmarkuplist(), DocObj.bActive);
        }

        if (RxCore_GUI_pagethumbs != undefined) {
            RxCore_GUI_pagethumbs.setThumbnails(DocObj.thumbnails);
        }

        drawmarkupAll(cntximg);
        DrawMarkupSelected(context);

    }

    function findImagemarkup(subtype){

        var curmarkup = 0;
        var markupcount = 0;
        var id = -1;
        var imagetype = 11 || 20;
        var markupobj = null;


        for (curmarkup = 0; curmarkup < DocObj.markuplist.length; curmarkup++) {
            if (DocObj.markuplist[curmarkup] != null ) {

                if(DocObj.markuplist[curmarkup].type == imagetype && DocObj.markuplist[curmarkup].subtype == subtype){
                    
                    markupobj = DocObj.markuplist[curmarkup];
                    /*id = DocObj.markuplist[curmarkup].markupnumber;


                    MarkupSaveState(id);
                    if (RxCore_GUI_Markup != undefined) {
                        RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[curmarkup], {created : false, modified : true, deleted : false});
                    }*/

                }
            }
        }

        return markupobj;

    }

    function deleteImagemarkups(subtype){

        var curmarkup = 0;
        var deletedmarkup = 0;
        var markupcount = 0;
        var id = -1;
        var imagetype = 11 || 20;

        //var tempmarkuplistsel = new Array();
        var tempmarkuplist = [];
        DocObj.markupdraworder = [];

        for (curmarkup = 0; curmarkup < DocObj.markuplist.length; curmarkup++) {
            //DocObj.markuplist[curmarkup].usser
            if (DocObj.markuplist[curmarkup] != null ) {
                //var bOwned = (DocObj.markuplist[curmarkup].signature == signature);
                //console.log(DocObj.markuplist[curmarkup].selected, DocObj.markuplist[curmarkup].selectedit);

                if(DocObj.markuplist[curmarkup].type == imagetype && DocObj.markuplist[curmarkup].subtype == subtype){
                    id = DocObj.markuplist[curmarkup].markupnumber;
                    
                    MarkupSaveState(id, undoDelete);

                    if (RxCore_GUI_Markup != undefined) {
                        RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[curmarkup], {created : false, modified : false, deleted : true});
                    }

                }else{
                    tempmarkuplist[markupcount] = DocObj.markuplist[curmarkup];
                    DocObj.markupdraworder[markupcount] = markupcount;
                    markupcount++;
                }
            }
        }
        //DocObj.markuplist = [];
        DocObj.markuplist = tempmarkuplist;
        DocObj.nummarkups = DocObj.markuplist.length;

        /*    for (deletedmarkup=0;deletedmarkup<tempmarkuplist.length;deletedmarkup++){
         //delete DocObj.markuplist[tempmarkuplist[deletedmarkup]];
         //DocObj.markuplist.splice(tempmarkuplist[deletedmarkup],1);
         }*/

        if (RxCore_GUI_Markuplist != undefined) {
            RxCore_GUI_Markuplist.setMarkupList(DocObj.getmarkuplist(), DocObj.bActive);
            /* Andriy */
            // RxCore_GUI_Markuplist.notify();

        }
        if (RxCore_GUI_pagethumbs != undefined) {
            RxCore_GUI_pagethumbs.setThumbnails(DocObj.thumbnails);
        }

        drawmarkupAll(cntximg);
        DrawMarkupSelected(context);

    }    
    

    function deletemarkupbyGUID(id){
        var curmarkup = 0;
        var deletedmarkup = 0;
        var markupcount = 0;
        //var id = -1;
        //var tempmarkuplistsel = new Array();
        var tempmarkuplist = [];
        DocObj.markupdraworder = [];

        if(bMultiselect){
            nMarkupUndoIdstart += 1;
            var ndeleteid = nMarkupUndoIdstart;
        }


        for (curmarkup = 0; curmarkup < DocObj.markuplist.length; curmarkup++) {
            //DocObj.markuplist[curmarkup].usser
            if (DocObj.markuplist[curmarkup] != null ) {
                //var bOwned = (DocObj.markuplist[curmarkup].signature == signature);
                //console.log(DocObj.markuplist[curmarkup].selected, DocObj.markuplist[curmarkup].selectedit);

                //id = DocObj.markuplist[curmarkup].markupnumber;
                
                if(DocObj.markuplist[curmarkup].uniqueID == id){
                    //nid = DocObj.markuplist[curmarkup].markupnumber;

                    /*if(bMultiselect){
                        DocObj.markuplist[curmarkup].setmultiValues(ndeleteid);
                    }*/
                    
                    //MarkupSaveState(nid, undoDelete);

                    /*if (RxCore_GUI_Markup != undefined) {
                        RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[curmarkup], {created : false, modified : false, deleted : true});
                    }*/
                }else{
                    tempmarkuplist[markupcount] = DocObj.markuplist[curmarkup];
                    DocObj.markupdraworder[markupcount] = markupcount;
                    markupcount++;
                }

                /*if (!DocObj.markuplist[curmarkup].selected && !DocObj.markuplist[curmarkup].selectedit ) {
                    //id = DocObj.markuplist[curmarkup].markupnumber;
                    //MarkupSaveState(id);

                    tempmarkuplist[markupcount] = DocObj.markuplist[curmarkup];                                                                                                   
                    markupcount++;
                } else {
                    id = DocObj.markuplist[curmarkup].markupnumber;
                    MarkupSaveState(id);

                }*/

            }
        }
        //DocObj.markuplist = [];
        DocObj.markuplist = tempmarkuplist;
        DocObj.nummarkups = DocObj.markuplist.length;

        /*    for (deletedmarkup=0;deletedmarkup<tempmarkuplist.length;deletedmarkup++){
         //delete DocObj.markuplist[tempmarkuplist[deletedmarkup]];
         //DocObj.markuplist.splice(tempmarkuplist[deletedmarkup],1);
         }*/

        if (RxCore_GUI_Markuplist != undefined) {
            RxCore_GUI_Markuplist.setMarkupList(DocObj.getmarkuplist(), DocObj.bActive);
            /* Andriy */
            // RxCore_GUI_Markuplist.notify();

        }
        if (RxCore_GUI_pagethumbs != undefined) {
            RxCore_GUI_pagethumbs.setThumbnails(DocObj.thumbnails);
        }
        
        
        drawmarkupAll(cntximg);
        DrawMarkupSelected(context);
    }

    function deletemarkupbyID(id){

        var curmarkup = 0;
        var deletedmarkup = 0;
        var markupcount = 0;
        //var id = -1;
        //var tempmarkuplistsel = new Array();
        var tempmarkuplist = [];
        DocObj.markupdraworder = [];

        if(bMultiselect){
            nMarkupUndoIdstart += 1;
            var ndeleteid = nMarkupUndoIdstart;
        }


        for (curmarkup = 0; curmarkup < DocObj.markuplist.length; curmarkup++) {
            //DocObj.markuplist[curmarkup].usser
            if (DocObj.markuplist[curmarkup] != null ) {
                //var bOwned = (DocObj.markuplist[curmarkup].signature == signature);
                //console.log(DocObj.markuplist[curmarkup].selected, DocObj.markuplist[curmarkup].selectedit);

                //id = DocObj.markuplist[curmarkup].markupnumber;
                
                if(DocObj.markuplist[curmarkup].markupID == id){
                    //nid = DocObj.markuplist[curmarkup].markupnumber;

                    /*if(bMultiselect){
                        DocObj.markuplist[curmarkup].setmultiValues(ndeleteid);
                    }*/
                    
                    //MarkupSaveState(nid, undoDelete);

                    /*if (RxCore_GUI_Markup != undefined) {
                        RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[curmarkup], {created : false, modified : false, deleted : true});
                    }*/
                }else{
                    tempmarkuplist[markupcount] = DocObj.markuplist[curmarkup];
                    DocObj.markupdraworder[markupcount] = markupcount;
                    markupcount++;
                }

                /*if (!DocObj.markuplist[curmarkup].selected && !DocObj.markuplist[curmarkup].selectedit ) {
                    //id = DocObj.markuplist[curmarkup].markupnumber;
                    //MarkupSaveState(id);

                    tempmarkuplist[markupcount] = DocObj.markuplist[curmarkup];                                                                                                   
                    markupcount++;
                } else {
                    id = DocObj.markuplist[curmarkup].markupnumber;
                    MarkupSaveState(id);

                }*/

            }
        }
        //DocObj.markuplist = [];
        DocObj.markuplist = tempmarkuplist;
        DocObj.nummarkups = DocObj.markuplist.length;

        /*    for (deletedmarkup=0;deletedmarkup<tempmarkuplist.length;deletedmarkup++){
         //delete DocObj.markuplist[tempmarkuplist[deletedmarkup]];
         //DocObj.markuplist.splice(tempmarkuplist[deletedmarkup],1);
         }*/

        if (RxCore_GUI_Markuplist != undefined) {
            RxCore_GUI_Markuplist.setMarkupList(DocObj.getmarkuplist(), DocObj.bActive);
            /* Andriy */
            // RxCore_GUI_Markuplist.notify();

        }
        if (RxCore_GUI_pagethumbs != undefined) {
            RxCore_GUI_pagethumbs.setThumbnails(DocObj.thumbnails);
        }
        
        
        drawmarkupAll(cntximg);
        DrawMarkupSelected(context);

    }

    function deleteFromParentHoles(markupIndex) {
        let parentMarkupNo = DocObj.markuplist[markupIndex].parentmarkupnumber;
 
        if(parentMarkupNo) {
            let parentMarkup = DocObj.markuplist.find(obj => obj.markupnumber === parentMarkupNo);
            if(parentMarkup && parentMarkup.holes && parentMarkup.holes.length) {
                parentMarkup.holes = parentMarkup.holes.filter(item => item !== DocObj.markuplist[markupIndex].markupnumber); 
            }
        }   
    }

    function deleteAllHoles(holes) {
        for(let idx = 0; idx < holes.length; idx++) {
            RxCore_SelectMarkupbyIndex(holes[idx]);
            deletemarkup(); 
        }           
    }

    function undoremoveholes(markup, id){

        var curhole = 0;
        var holecount = 0;
        var id = -1;
        var tempholelist = [];
        var bdeleted = true;


        for (curhole = 0; curhole < markup.holes.length; curhole++) {

            
            var bSekected = (markup.holes[curhole].id = id);

            if(bSekected){

                id = markup.markupnumber;
                //MarkupSaveState(id, undoModify);

                bdeleted = false;

                if (RxCore_GUI_Markup != undefined) {
                    RxCore_GUI_Markup.setMarkupSelected(markup.holes[curhole], {created : false, modified : false, deleted : true});
                }


            }else{
                tempholelist[holecount] = markup.holes[curhole];
                holecount++;

            }
        

        }

        markup.holes = tempholelist;

        return bdeleted;


    }
    

    function deletehole(markup){
        var curhole = 0;
        var holecount = 0;
        var id = -1;
        var tempholelist = [];
        var bdeleted = true;


        for (curhole = 0; curhole < markup.holes.length; curhole++) {
            var bSekected = (markup.holes[curhole].selectedit);

            if(bSekected){

                id = markup.markupnumber;
                //MarkupSaveState(id, undoModify);

                bdeleted = false;

                if (RxCore_GUI_Markup != undefined) {
                    RxCore_GUI_Markup.setMarkupSelected(markup.holes[curhole], {created : false, modified : false, deleted : true});
                }


            }else{
                tempholelist[holecount] = markup.holes[curhole];
                holecount++;

            }
        

        }

        markup.holes = tempholelist;

        return bdeleted;


    }

    function deletemarkup() {
        var curmarkup = 0;
        var deletedmarkup = 0;
        var markupcount = 0;
        var id = -1;
        //var tempmarkuplistsel = new Array();
        var tempmarkuplist = [];
        DocObj.markupdraworder = [];

        if(bMultiselect){
            nMarkupUndoIdstart += 1;
            var ndeleteid = nMarkupUndoIdstart;
        }

        var deleteMarkupIndexes = [];

        for (curmarkup = 0; curmarkup < DocObj.markuplist.length; curmarkup++) {
            //DocObj.markuplist[curmarkup].usser
            if (DocObj.markuplist[curmarkup] != null ) {
                var bOwned = (DocObj.markuplist[curmarkup].signature == signature);
                //console.log(DocObj.markuplist[curmarkup].selected, DocObj.markuplist[curmarkup].selectedit);

                var bhole = (DocObj.markuplist[curmarkup].type == 8 && DocObj.markuplist[curmarkup].subtype == 2);

                var bDelete = DocObj.markuplist[curmarkup].getdelete();

                var bSekected = (DocObj.markuplist[curmarkup].selected || DocObj.markuplist[curmarkup].selectedit);




                if((bOwned && bSekected) || bDelete){
                    id = DocObj.markuplist[curmarkup].markupnumber;

                    if(bMultiselect){
                        DocObj.markuplist[curmarkup].setmultiValues(ndeleteid);
                    }

                    if(bhole){
                        if(DocObj.markuplist[curmarkup].parent.holes.length > 0){
                            var bSekected = deletehole(DocObj.markuplist[curmarkup].parent);
        
                        }
        
                    }
    
                    
                    MarkupSaveState(id, undoDelete);


                    /*deleteFromParentHoles(curmarkup);
                    if(DocObj.markuplist[curmarkup].holes && DocObj.markuplist[curmarkup].holes.length) {
                        deleteMarkupIndexes.push(DocObj.markuplist[curmarkup].holes);
                    }*/

                    if (RxCore_GUI_Markup != undefined) {
                        RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[curmarkup], {created : false, modified : false, deleted : true});
                    }
                }else{
                    tempmarkuplist[markupcount] = DocObj.markuplist[curmarkup];
                    DocObj.markupdraworder[markupcount] = markupcount;
                    markupcount++;
                }

                /*if (!DocObj.markuplist[curmarkup].selected && !DocObj.markuplist[curmarkup].selectedit ) {
                    //id = DocObj.markuplist[curmarkup].markupnumber;
                    //MarkupSaveState(id);

                    tempmarkuplist[markupcount] = DocObj.markuplist[curmarkup];                                                                                                   
                    markupcount++;
                } else {
                    id = DocObj.markuplist[curmarkup].markupnumber;
                    MarkupSaveState(id);

                }*/

            }
        }
        //DocObj.markuplist = [];
        DocObj.markuplist = tempmarkuplist;
        DocObj.nummarkups = DocObj.markuplist.length;

        /*    for (deletedmarkup=0;deletedmarkup<tempmarkuplist.length;deletedmarkup++){
         //delete DocObj.markuplist[tempmarkuplist[deletedmarkup]];
         //DocObj.markuplist.splice(tempmarkuplist[deletedmarkup],1);
         }*/

         /*if(deleteMarkupIndexes.length) {
            for(let idx = 0; idx < deleteMarkupIndexes.length; idx++) {
                deleteAllHoles(deleteMarkupIndexes[idx]);
            }            
        }*/

        if (RxCore_GUI_Markuplist != undefined) {
            RxCore_GUI_Markuplist.setMarkupList(DocObj.getmarkuplist(), DocObj.bActive);
            /* Andriy */
            // RxCore_GUI_Markuplist.notify();

        }
        if (RxCore_GUI_pagethumbs != undefined) {
            RxCore_GUI_pagethumbs.setThumbnails(DocObj.thumbnails);
        }
        
        

        drawmarkupAll(cntximg);
        DrawMarkupSelected(context);
    }

    function drawmarkupMagnify(ctx,page,dx,dy,dscale,drotation){


        if(DocObj == null || DocObj == undefined){
            return;
        }

        var curmarkup = 0;
        if (DocObj.Drawmarkup) {
            for (curmarkup = 0; curmarkup < DocObj.markuplist.length; curmarkup++) {
                if (DocObj.markuplist[curmarkup] != null) {
                    if (DocObj.markuplist[curmarkup].pagenumber == page) {
                        if (DocObj.markuplist[curmarkup].display) {
                            DocObj.markuplist[curmarkup].drawprint(ctx,dx,dy,dscale,drotation,false);

                        }
                    }
                }
            }
        }
    }

    /*function drawmarkupPrint(ctx,page,dx,dy,dscale){
        var curmarkup = 0;

        for (curmarkup = 0; curmarkup < DocObj.markuplist.length; curmarkup++) {
            if (DocObj.markuplist[curmarkup] != null) {
                if (DocObj.markuplist[curmarkup].pagenumber == page) {
                    if (DocObj.markuplist[curmarkup].display) {
                        DocObj.markuplist[curmarkup].drawprint(ctx,dx,dy,dscale,0,false);

                    }
                }
            }
        }
        if(RxCore_GUI_printpage != undefined){
            RxCore_GUI_printpage.printpageComplete(DocObj.pages[page].printobj);
        }



    }*/
    function drawmarkupPrint(ctx,page,dx,dy,dscale){
        var curmarkup = 0;

        if (RxCore_printhelper().docObj == null){
            return
        }


        for (curmarkup = 0; curmarkup < RxCore_printhelper().docObj.markuplist.length; curmarkup++) {
            
            var bcurrentView = markupcurrentview(DocObj.markuplist[curmarkup]);

            if (RxCore_printhelper().docObj.markuplist[curmarkup] != null) {
                if (RxCore_printhelper().docObj.markuplist[curmarkup].pagenumber == page) {
                    if (RxCore_printhelper().docObj.markuplist[curmarkup].display) {

                        if(RxCore_printhelper().docObj.markuplist[curmarkup].bfor3D){
                            if (bcurrentView) {
                                RxCore_printhelper().docObj.markuplist[curmarkup].drawprint(ctx,dx,dy,dscale,0,false);
                            }
                        }else{
                            RxCore_printhelper().docObj.markuplist[curmarkup].drawprint(ctx,dx,dy,dscale,0,false);
                        }

                        

                    }
                }
            }
        }
        if(RxCore_GUI_printpage != undefined){
            

            if(RxCore_printhelper().docObj.pages[page] != undefined){
                RxCore_GUI_printpage.printpageComplete(RxCore_printhelper().docObj.pages[page].printobj);
            }else{
                aDrawpages[page] = true;
            }

            /*if (RxCore_printhelper().docObj.foxitdoc != null){
                RxCore_GUI_printpage.printpageComplete(canvaspages[page].printobj);
                //canvaspages[i].printobj

            }else{
                
            }*/
            
            

        }



    }    

    function getmaxmarkupredonumber(){
        var curredo = 0;
        var curmarkupnum = 0;
        var prevmarkupnum = 0;
        var maxnumredo = 0;

        for (curredo = 0; curredo < DocObj.markupredolist.length; curredo++) {
            if (DocObj.markupundolist[curredo] != null) {
                
                    prevmarkupnum = curmarkupnum;
                    curmarkupnum = DocObj.markupredolist[curredo].markupnumber;
                    
                    //nextnumber = curmarkupnum + 1;
                    if(curmarkupnum > prevmarkupnum){
                        maxnumredo = curmarkupnum;
                    }
                
            }
        }
    
        return maxnumredo;
    }

    function getmaxmarkupundonumber(){
        var curundo = 0;
        var curmarkupnum = 0;
        var prevmarkupnum = 0;
        var maxnumundo = 0;

        for (curundo = 0; curundo < DocObj.markupundolist.length; curundo++) {
            if (DocObj.markupundolist[curundo] != null) {
                
                    prevmarkupnum = curmarkupnum;
                    curmarkupnum = DocObj.markupundolist[curundo].markupnumber;
                    
                    //nextnumber = curmarkupnum + 1;
                    if(curmarkupnum > prevmarkupnum){
                        maxnumundo = curmarkupnum;
                    }
                
            }
        }

    
        return maxnumundo;
    }

    function getmaxmarkupnumber(){
        var curmarkup = 0;
        var curmarkupnum = 0;
        var prevmarkupnum = 0;
        var maxmarkupnum = 0;

        for (curmarkup = 0; curmarkup < DocObj.markuplist.length; curmarkup++) {
            if (DocObj.markuplist[curmarkup] != null) {

                prevmarkupnum = curmarkupnum;
                curmarkupnum = DocObj.markuplist[curmarkup].markupnumber;

                if(curmarkupnum > prevmarkupnum){
                    maxmarkupnum = curmarkupnum;
                }

            }
        }
        return maxmarkupnum;
    }


    function getlastmarkupnumber(){
        
        var nextnumber = 0;
        var maxnum = 0;
        
        var maxmarkupnum = getmaxmarkupnumber();

        if(maxmarkupnum > maxnum){
            maxnum = maxmarkupnum;
        }


        var maxnumundo = getmaxmarkupundonumber();
        if(maxnumundo > maxnum){
            maxnum = maxnumundo;
        }

        var maxnumredo = getmaxmarkupredonumber();
        if(maxnumredo > maxnum){
            maxnum = maxnumredo;
        }

        nextnumber = maxnum + 1;

        //var nextundonum = 0;


        return nextnumber;

    }

    function markupcurrentview(markup){

        var bcurrentView = false;

        if(markup.bfor3D){
            bcurrentView = (markup.viewname == DocObj.getcur3DView());
        }else{
            //bcurrentView = (markup.pagenumber == DocObj.getcurPage());

            bcurrentView = DocObj.visiblepage(markup.pagenumber);


        }

        return bcurrentView;


    }

    function getLastMarkup() {
        if (!DocObj) {
            return undefined;
        }
        if (DocObj.markuplist.length > 0) {
            return DocObj.markuplist[DocObj.markuplist.length - 1];
        }

        return undefined;
    }


    function hidemarkupAll(onOff){
        var curmarkup = 0;

        for (curmarkup = 0; curmarkup < DocObj.markuplist.length; curmarkup++) {
            if (DocObj.markuplist[curmarkup] != null) {

                DocObj.markuplist[curmarkup].setdisplay(onOff);

                /*if (DocObj.markuplist[curmarkup].type == 0 && DocObj.markuplist[curmarkup].subtype == 1) {
                }else{
                    DocObj.markuplist[curmarkup].display = onOff;
                }*/

            }

        }

    }
    function drawFloorplanMarkupAll(ctx, scale, dx, dy){
        var curmarkup = 0;

        for (curmarkup = 0; curmarkup < DocObj.markuplist.length; curmarkup++) {
            if (DocObj.markuplist[curmarkup] != null) {
                if (!DocObj.markuplist[curmarkup].selected && !DocObj.markuplist[curmarkup].selectedit) {
                    if (DocObj.markuplist[curmarkup].display) {
                        if(DocObj.pages[DocObj.markuplist[curmarkup].pagenumber] != undefined){
                            DocObj.markuplist[curmarkup].drawfloorplan(ctx, scale, dx, dy);
                        }
                    }
                }
            }
        }

    }

    function drawmarkupToCanvas(ctx,width, height, name){
        
        if (!documentopen) {
            return;
        }

        if(docpagNotReady()){
            return;
        }
        
        var curmarkup = 0;

        if(DocObj){
            if (DocObj.getCurrentpage().usevector3Dxml) {
                for (curmarkup = 0; curmarkup < DocObj.markuplist.length; curmarkup++) {
                    if (DocObj.markuplist[curmarkup] != null) {
    
                        if (DocObj.markuplist[curmarkup].display) {
    
                            if(DocObj.markuplist[curmarkup].bfor3D && DocObj.markuplist[curmarkup].viewname == name){
                                
                                DocObj.markuplist[curmarkup].drawWidthHeight(ctx, width, height);
                            }
                        }
    
                    }
                }
            }
        }

        drawmarkupAll(cntximg);
        DrawMarkupSelected(context);
    }

    function drawpagerect(ctx, pagerect){

        var stroke = true;

        console.log(pagerect);

        var pixelRatio = getPixelRatio();

        ctx.save(); // save the context so we don't mess up others
        ctx.strokeStyle = "black";
        //ctx.fillStyle = fillcolor;
        ctx.lineWidth = 4;
        /*if (markupobject.rotation != 0 && markupobject.type != 9) {
            var tx = x + (width / 2);
            var ty = y + (height / 2);
            ctx.translate(tx, ty);
            ctx.rotate(markupobject.rotation);
            ctx.translate(-tx, -ty);
        }*/
        ctx.beginPath();
        ctx.moveTo(pagerect.x * pixelRatio, pagerect.y * pixelRatio);
        ctx.lineTo(pagerect.w * pixelRatio, pagerect.y * pixelRatio);
        ctx.lineTo(pagerect.w * pixelRatio, pagerect.h * pixelRatio);
        ctx.lineTo(pagerect.x * pixelRatio, pagerect.h * pixelRatio);
        ctx.closePath();
        /*if (fill) {
            ctx.fill();
        }*/
        if (stroke) {
            ctx.stroke();
        }
        ctx.restore(); // restore context to what it was on entry


    }


    function drawmarkupAll(ctx) {

        if(DocObj == null || DocObj == undefined){
            return;
        }

        var bnot3dmarkup = get3DViewNotActive();

        if(bCameramove && bnot3dmarkup){
            ctx.clearRect(0, 0, canvasowidth, canvasoheight);
            return;
        }


        var curmarkup = 0;
        var drawthumb = true;
        ctx.clearRect(0, 0, canvasowidth, canvasoheight);

        //DocObj.getCurrentpage().draw_thumbnail();

        if(DocObj.getCurrentThumb() != undefined){
            DocObj.getCurrentThumb().draw_thumbnail();
            DocObj.getCurrentThumb().clearBirdseyeMarkupctx();
        }        

        DocObj.drawHoverSpace(ctx);

        if (bSuspendDraw){
            return;
        }   

        if(!DocObj.getCurrentpage()){
            return;
        }   

        if(DocObj.getCurrentpage().Vector3DPageObj){
            DocObj.getCurrentpage().Vector3DPageObj.renderMarkupPoints(cntximg);
            drawthumb = false;
        }


        if (DocObj.Drawmarkup) {

            for (curmarkup = 0; curmarkup < DocObj.markupdraworder.length; curmarkup++) {

                var curmarkupobj = DocObj.markuplist[DocObj.markupdraworder[curmarkup]];

                if (curmarkupobj != null) {
                    if (!curmarkupobj.selected && !curmarkupobj.selectedit) {
                        if (curmarkupobj.display) {
                            if(curmarkupobj.pagenumber != undefined){

                                if(drawthumb){

                                    var thumbnail = DocObj.getThumb(curmarkupobj.pagenumber);

                                    if(curmarkupobj.bfor3D){
                                        thumbnail = undefined;
                                    }

                                    if(thumbnail != undefined){
                                        //DocObj.markuplist[curmarkup].drawthumb(thumbnail.thumbctx);
                                        curmarkupobj.drawbirdseye(thumbnail);
                                            
                                    }
    

                                }


                            }
                            
                        }

                    }

                    var bcurrentView = markupcurrentview(curmarkupobj);

                    if (bcurrentView) {
                    //if (DocObj.markuplist[curmarkup].pagenumber == DocObj.getcurPage()) {
                        var bhasholeselected = false;
                        
                        if (curmarkupobj.holes.length > 0){
                            for(var h=0; h < curmarkupobj.holes.length; h++){
                                if (curmarkupobj.holes[h].selected || curmarkupobj.holes[h].selectedit){
                                    bhasholeselected = true;
                                }
                            }
    
                        }


                        if (!curmarkupobj.selected && !curmarkupobj.selectedit && !bhasholeselected) {
                            if (curmarkupobj.display) {

                                var thumbnail = DocObj.getThumb(curmarkupobj.pagenumber);
                                curmarkupobj.drawthumb(thumbnail.thumbctx);


                                
                                curmarkupobj.drawmescaled(ctx);

                                //DocObj.markuplist[curmarkup].drawthumb(DocObj.getCurrentpage().thumbctx);
                                //strictly for testing page rect stability remove after test
                                if(debugmarkuprect){
                                    var pagerect = DocObj.getpage(curmarkupobj.pagenumber).getpagerectdirect();
                                    drawpagerect(ctx, pagerect);
    
                                }





                            }

                        }

                    }

                }

            }

        } else {

            

            for (curmarkup = 0; curmarkup < DocObj.markupdraworder.length; curmarkup++) {

                curmarkupobj = DocObj.markuplist[DocObj.markupdraworder[curmarkup]];

                if (curmarkupobj != null) {

                    var bcurrentView = markupcurrentview(curmarkupobj);

                    if (bcurrentView) {
                    //if (DocObj.markuplist[curmarkup].pagenumber == DocObj.getcurPage()) {

                        if (curmarkupobj.type == 0 && curmarkupobj.subtype == 1) {
                            if (curmarkupobj.display) {
                                //DocObj.markuplist[curmarkup].drawthumb(DocObj.getCurrentpage().thumbctx);

                                if(drawthumb){
                                    if(DocObj.getCurrentThumb() != undefined){
                                        curmarkupobj.drawthumb(DocObj.getCurrentThumb().thumbctx);
                                        curmarkupobj.drawbirdseye(DocObj.getCurrentThumb());
                                    }
    
                                }


                                curmarkupobj.drawmescaled(ctx);
                            }

                        }
                    }
                }

            }

        }

        RxCore_forcetextsearchmarker();
        RxCore_drawselectedBlocks();

    }

    function DrawMarkupSelected(ctx) {
        

        if(DocObj == null || DocObj == undefined){
            return;
        }

        if(tool == null || tool == undefined){
            return;
        }

        var bnot3dmarkup = get3DViewNotActive();

        if(bCameramove && bnot3dmarkup){
            ctx.clearRect(0, 0, canvasowidth, canvasoheight);
            return;
        }

        var curmarkup = 0;

        ctx.clearRect(0, 0, canvasowidth, canvasoheight);

        

        if(tool.refreshSupport){
            tool.refreshDraw();
        }

        //console.log("context clear 2");


        if(DocObj){



            if (DocObj.Drawmarkup) {
                for (curmarkup = 0; curmarkup < DocObj.markuplist.length; curmarkup++) {
                    if (DocObj.markuplist[curmarkup] != null) {

                        var bcurrentView = markupcurrentview(DocObj.markuplist[curmarkup]);

                        if (bcurrentView) {
                        //if (DocObj.markuplist[curmarkup].pagenumber == DocObj.getcurPage()) {
                            if (DocObj.markuplist[curmarkup].selected || DocObj.markuplist[curmarkup].selectedit) {
                                if (DocObj.markuplist[curmarkup].display) {
                                    //DocObj.markuplist[curmarkup].drawthumb(DocObj.getCurrentpage().thumbctx);
    
                                    if(DocObj.getCurrentThumb() != undefined){
                                        DocObj.markuplist[curmarkup].drawbirdseye(DocObj.getCurrentThumb());
                                        DocObj.markuplist[curmarkup].drawthumb(DocObj.getCurrentThumb().thumbctx);
                                    }


                                    
                                    //DocObj.markuplist[curmarkup].drawmescaled(ctx);

                                    if(DocObj.markuplist[curmarkup].type == 8 && DocObj.markuplist[curmarkup].subtype == 2){
                                        if(DocObj.markuplist[curmarkup].parent){
                                            DocObj.markuplist[curmarkup].parent.drawmescaled(ctx);
                                        }
                                        
                                    }else{
                                        DocObj.markuplist[curmarkup].drawmescaled(ctx);
                                    }

                                }
    
                            }
                        }
                    }
    
                }
    
            } else {
                for (curmarkup = 0; curmarkup < DocObj.markuplist.length; curmarkup++) {
                    if (DocObj.markuplist[curmarkup] != null) {
                        
                        var bcurrentView = markupcurrentview(DocObj.markuplist[curmarkup]);

                        if (bcurrentView) {
                        //if (DocObj.markuplist[curmarkup].pagenumber == DocObj.getcurPage()) {
                            if (DocObj.markuplist[curmarkup].type == 0 && DocObj.markuplist[curmarkup].subtype == 1) {
                                if (DocObj.markuplist[curmarkup].display) {
                                    
                                    if(DocObj.getCurrentThumb() != undefined){
                                        DocObj.markuplist[curmarkup].drawbirdseye(DocObj.getCurrentThumb());
                                        DocObj.markuplist[curmarkup].drawthumb(DocObj.getCurrentThumb().thumbctx);
                                    }

                                    if(DocObj.markuplist[curmarkup].type == 8 && DocObj.markuplist[curmarkup].subtype == 2){

                                        if(DocObj.markuplist[curmarkup].parent){
                                            DocObj.markuplist[curmarkup].parent.drawmescaled(ctx);
                                        }
                                        
                                    }else{
                                        DocObj.markuplist[curmarkup].drawmescaled(ctx);
                                    }

                                    
                                }
    
                            }
                        }
                    }
    
                }
            }
        }
    }

    function get3DDoc(){
        var docis3d = false;

        if(docpagNotReady()){
            return docis3d;
        }


        if(DocObj.getCurrentpage() != undefined){
            docis3d = DocObj.getCurrentpage().usevector3Dxml;
        }

        
        return docis3d;

    }

    function get3DViewNotActive(){
        var bnot3dmarkup = true;

        if(DocObj == null || DocObj == undefined){
            bnot3dmarkup = true;
        }else{
            bnot3dmarkup = (DocObj.getcur3DView() == null);
        }
        
        return bnot3dmarkup;
    }


    function drawSpaceSelected(ctx){
        
        if(DocObj == null || DocObj == undefined){
            return;
        }

        var bnot3dmarkup = get3DViewNotActive();

        if(bCameramove && bnot3dmarkup){
            ctx.clearRect(0, 0, canvasowidth, canvasoheight);
            return;
        }

        
        var curmarkup = 0;
        var bisSpace = false;
        ctx.clearRect(0, 0, canvasowidth, canvasoheight);

        if (DocObj.Drawmarkup) {
            for (curmarkup = 0; curmarkup < DocObj.markuplist.length; curmarkup++) {
                if (DocObj.markuplist[curmarkup] != null) {

                    var bcurrentView = markupcurrentview(DocObj.markuplist[curmarkup]);
                    if (bcurrentView) {
                    //if (DocObj.markuplist[curmarkup].pagenumber == DocObj.getcurPage()) {
                        if (DocObj.markuplist[curmarkup].spaceselected) {
                            bisSpace = (DocObj.markuplist[curmarkup].type == 8 && DocObj.markuplist[curmarkup].subtype == 1)
                            if (DocObj.markuplist[curmarkup].display && bisSpace) {
                                DocObj.markuplist[curmarkup].drawmescaled(ctx);
                            }
                        }
                    }
                }

            }

        } else {
            for (curmarkup = 0; curmarkup < DocObj.markuplist.length; curmarkup++) {
                if (DocObj.markuplist[curmarkup] != null) {
                    var bcurrentView = markupcurrentview(DocObj.markuplist[curmarkup]);
                    if (bcurrentView) {
                    //if (DocObj.markuplist[curmarkup].pagenumber == DocObj.getcurPage()) {
                        if (DocObj.markuplist[curmarkup].type == 0 && DocObj.markuplist[curmarkup].subtype == 1) {
                            if (DocObj.markuplist[curmarkup].display) {
                                DocObj.markuplist[curmarkup].drawmescaled(ctx);
                            }

                        }
                    }
                }

            }
        }        
    }

        /*function getXML2dData(pageobject){
        
             var xhr = new XMLHttpRequest();
        
             try{
             xhr.open('GET', pageobject.Vector2DSRC, true);
             }
             catch(e){
             alert("Error 1 - " + e);
             }
             try{
             xhr.responseType = 'document';
             }
             catch(e){
             //alert("Error 2 - " + e);
             }
        
        
             xhr.onload = function(e) {
             if (this.status == 200) {
             var xmlDoc = this.responseXML;
             var pageobject.VectorPageObj = new VectorPageObject(xmlDoc);
             }
             };
        
             xhr.send();
        
             }*/

        function base64StringToByteArray(base64String) {
                        
            base64String = base64String.split('base64,')[1];

            var byteCharacters = atob(base64String);
            var byteNumbers = new Array(byteCharacters.length);

            for (var i = 0; i < byteCharacters.length; i++) {
                byteNumbers[i] = byteCharacters.charCodeAt(i);
            }

            return new Uint8Array(byteNumbers);
        }

        


        function bytes2Int(bytes){
            var ret = 0;

            for (var idx = 0; idx<bytes.length; idx++) {
                ret += bytes[idx];
                if (idx < bytes.length - 1) {
                    ret = ret << 8;
                }
            }

            return ret;
        }
        function getChunk(pngData, chunkName){
            for (var idx = 0; idx<pngData.chunks.length; idx++) {
                if (pngData.chunks[idx].name = chunkName) {
                    return pngData.chunks[idx];
                }
            }
            return null;
        }

        function enrichParsedData(pngData){
            var idhrChunk = getChunk(pngData, 'IDHR');
            pngData.width = bytes2Int(idhrChunk.data.slice(0, 4));
            pngData.height = bytes2Int(idhrChunk.data.slice(4, 8));
            pngData.bitDepth = bytes2Int(idhrChunk.data.slice(8, 9));
            pngData.colorType = bytes2Int(idhrChunk.data.slice(9, 10));
            pngData.compressionMethod = bytes2Int(idhrChunk.data.slice(10, 11));
            pngData.filterMethod = bytes2Int(idhrChunk.data.slice(11, 12));
            pngData.interlaceMethod =bytes2Int(idhrChunk.data.slice(12, 13));

            pngData.isGreyScale = pngData.colorType == 0 || pngData.colorType == 4;
            pngData.isRgb = pngData.colorType == 2 || pngData.colorType == 6;
            pngData.hasAlpha = pngData.colorType == 4 || pngData.colorType == 6;
            pngData.hasPaletteMode = pngData.colorType == 3 && getChunk(pngData, 'PLTE') != null;

            return pngData;
        }
        
        function parseChunk(bytes, start){
            var chunkLength = bytes2Int(bytes.slice(start, start + 4));

            var chunkName = '';
            chunkName += String.fromCharCode(bytes[start + 4]);
            chunkName += String.fromCharCode(bytes[start + 5]);
            chunkName += String.fromCharCode(bytes[start + 6]);
            chunkName += String.fromCharCode(bytes[start + 7]);

            var chunkData = [];
            for (var idx = start + 8; idx<chunkLength + start + 8; idx++) {
                chunkData.push(bytes[idx]);
            }

            //TODO validate crc as required!

            return {
                start: start,
                end: Number(start) + Number(chunkLength) + 12, //12 = 4 (length) + 4 (name) + 4 (crc)
                length: chunkLength,
                name: chunkName,
                data: chunkData,
                crc: [
                    bytes[chunkLength + start + 8],
                    bytes[chunkLength + start + 9],
                    bytes[chunkLength + start + 10],
                    bytes[chunkLength + start + 11]
                ],
                crcChecked: false
            };
        }

        function parseBytes(bytes){
            var pngData = {};
            
            //verify file header
            pngData['headerIsValid'] = bytes[0] == 0x89
                        && bytes[1] == 0x50
                        && bytes[2] == 0x4E
                        && bytes[3] == 0x47
                        && bytes[4] == 0x0D
                        && bytes[5] == 0x0A
                        && bytes[6] == 0x1A
                        && bytes[7] == 0x0A

            if (!pngData.headerIsValid) {
                console.warn('Provided data does not belong to a png');
                return pngData;
            }
            var chunks = [];

            var chunk = parseChunk(bytes, 8);
            chunks.push(chunk);

            while (chunk.name !== 'IEND') {
                chunk = parseChunk(bytes, chunk.end);
                chunks.push(chunk);
            }

            pngData['chunks'] = chunks;
            return pngData;

        }        

        function getPixelRatio() {
            var dpr = window.devicePixelRatio || 1;
            var bsr = 1;


            if(dpr != nstartuppixelratio){
                if(!brestartmessageshown){
                    brestartmessageshown = true;
                    alert("the browser window scaling has changed, restart the viewer");
                    
                }
                
            }

            return dpr / bsr;
        }

        function setCtxTranslatehpx(ctx){
            var pixelRatio = getPixelRatio();
            glpixelRatio = pixelRatio;
            glpixellinewidthratio = 1 * pixelRatio;
            var halfx = pixelRatio/2;
            ctx.transform(1, 0, 0, 1, 0.5, 0.5);
            //ctx.translate(halfx, halfx);
            
        }

        function pxround(num){
            
            var rnum = (num + (num>0?0.5:-0.5)) << 0;
            
            return rnum;
        }

        class textObject {
            constructor(texobject, parent, fontobject, blockname, pathindx) {
                var scope = this;
                this.type = "text";
                this.textwidthset = false;
                this.textwidth = fontobject.height;
                this.parent = parent;
                this.blockname = blockname;
                this.index = pathindx;
                this.blockstate = 1;
                this.layerstate = 1;
                this.x1 = texobject.x1;
                this.y1 = texobject.y1;
                this.height = fontobject.height;
                this.fontname = fontobject.name;
                this.weight = fontobject.weight;
                this.rotation = -(fontobject.rotation * (Math.PI / 180));
                this.layer = texobject.layer;
                this.layerstate = texobject.layerstate;
                this.drawmode = texobject.drawmode;
                this.pen = texobject.pen;
                this.penarrindex = parent.pentable.getPenArrindex(this.pen);
                this.text = texobject.text;
                this.strokecolor = texobject.strokecolor;
                this.fillcolor = texobject.fillcolor;
                this.stroketempcolor = texobject.strokecolor;
                this.filltempcolor = texobject.fillcolor;
                this.selected = false;
                this.hasArea = false;
                this.entityType = texobject.entityType;
                this.role = texobject.role;
                this.mouseon = false;
                this.mouseover = false;

                var highlightdraw = false;

                this.insidePolygon = function (x, y, scalefactor, offsetx, offsety, mediax, mediay, mediah) {
                    return false;
                };
                this.toggleselect = function () {
                    var selectstate = !scope.selected;
                    scope.selected = selectstate;
                };

                this.iswithin = function (x, y, scalefactor, offsetx, offsety, mediax, mediay, mediah, hover) {

                    var within = false;

                    var rect = scope.getRectangle(scalefactor, offsetx, offsety, mediax, mediay, mediah);

                    if(x > rect.x && x < (rect.x + rect.w)){
                        if(y > rect.y && y < (rect.y + rect.h)){
                            within = true;
                        }
                    }

                    

                    return within;


                };

                this.getArea = function(){


                    var minx = scope.x1;
                    var miny = scope.y1;

                    var maxy = miny + scope.height;
                    var maxx = minx + scope.textwidth;

                    //var maxx = scope.x2;
                    //var maxy = scope.y2;

                    if (minx > maxx) {
                        var tempx = minx;
                        minx = maxx;
                        maxx = tempx;
                    }
                    if (miny > maxy) {
                        var tempy = miny;
                        miny = maxy;
                        maxy = tempy;
                    }
                    /*return {
                        area: (maxx - minx) * (maxy - miny),
                        x: minx,
                        y: miny,
                        w: maxx - minx,
                        h: maxy - miny,
                        wp: (maxx - minx) / 10,
                        hp: (maxy - miny) / 10
                    };*/

                    return (maxx - minx) * (maxy - miny);
                    
                };

                function getrotrect(rect, angle, rotpoint){
                    var origx = rect.x;
                    var origy = rect.y;

                    var origw = rect.w;
                    var origh = rect.h;

                    var CanvRotRad = angle;// * (Math.PI / 180);
                    var cosangle = Math.cos(CanvRotRad);
                    var sinangle = Math.sin(CanvRotRad);


                    var centercanvX = rotpoint.x;
                    var centercanvY = rotpoint.y;
                    
                    var hw = origx - centercanvX;
                    var hh = origy - centercanvY;

                    var newx = (hw * cosangle) - (hh * sinangle);
                    var newy = (hw * sinangle) + (hh * cosangle);

                    newx = centercanvX + newx;
                    newy = centercanvY + newy;
                    hw = origw - centercanvX;
                    hh = origh - centercanvY;
                    var neww = (hw * cosangle) - (hh * sinangle);
                    var newh = (hw * sinangle) + (hh * cosangle);
                    neww = centercanvX + neww;
                    newh = centercanvY + newh;
                    
                    return {
                        x: Math.min(newx, neww),
                        y: Math.min(newy, newh),
                        w: Math.max(neww, newx),
                        h: Math.max(newh, newy)
                    };

                    
                }


                this.getRectangle = function (scalefactor, offsetx, offsety, mediax, mediay, mediah) {
                    var minx = (scope.x1 - mediax) * scalefactor;
                    var miny = (mediah - scope.y1) * scalefactor;
                    //var maxx = (scope.x2 - mediax) * scalefactor;
                    //var maxy = (mediah - scope.y2) * scalefactor;
                    var height = scope.height * scalefactor;
                    var width = scope.textwidth * scalefactor;

                    

                    var maxy = miny + height;
                    //var maxx = minx + height;
                    var maxx = minx + width;
                    minx += offsetx;
                    maxx += offsetx;
                    miny += offsety;
                    maxy += offsety;
                    if (minx > maxx) {
                        var tempx = minx;
                        minx = maxx;
                        maxx = tempx;
                    }
                    if (miny > maxy) {
                        var tempy = miny;
                        miny = maxy;
                        maxy = tempy;
                    }

                    //ctx.translate(x1scaled, y1scaled - height);

                    var rotpoint = {x : minx, y : miny};

                    if (scope.rotation != 0) {
                       var rotrect = getrotrect({x : minx, y : miny, w : maxx, h : maxy},scope.rotation, rotpoint);
                       minx = rotrect.x;
                       miny = rotrect.y;
                       maxx = rotrect.w;
                       maxy = rotrect.h;
                    }

                    return {
                        found: true,
                        x: minx,
                        y: miny,
                        w: maxx - minx,
                        h: maxy - miny,
                        wp: (maxx - minx) / 10,
                        hp: (maxy - miny) / 10
                    };
                };
                this.findsnapPoint = function (x, y, scalefactor, offsetx, offsety, mediax, mediay, mediah) {
                    return {
                        found: false,
                        x: 0,
                        y: 0,
                        type: snapEndpoint
                    };
                };

                this.getdrawColors = function(drawprint){

                    var linecolor = scope.stroketempcolor;
                    var fillcolor = scope.filltempcolor;
                    var drawcolors = {linecolor : linecolor, fillcolor : fillcolor };


                    if(scope.parent.applypentable && scope.parent.pentable.changed){
                        if(scope.parent.pentable.getPendirect(scope.penarrindex) != undefined){
                            scope.stroketempcolor = scope.parent.pentable.getPendirect(scope.penarrindex).getcolor();
                            linecolor = scope.stroketempcolor;
                            fillcolor = linecolor;
                            scope.stroketempcolor = fillcolor;

                            

                        }
                        
                    }else{

                        scope.stroketempcolor = scope.strokecolor;
                        linecolor = scope.stroketempcolor;

                        
                    }



                    if (!bKeepVectorColor) {
                        if (scope.stroketempcolor == DocObj.backgroundColor) {
                            if (DocObj.backgroundColor == "#FFFFFF") {
                                scope.stroketempcolor = "#000000";
                                linecolor = scope.stroketempcolor;
                            }
                            if (DocObj.backgroundColor == "#000000") {
                                scope.stroketempcolor = "#FFFFFF";
                                linecolor = scope.stroketempcolor;
                            }
                        }
                        if (this.filltempcolor == DocObj.backgroundColor) {
                            if (DocObj.backgroundColor == "#FFFFFF") {
                                scope.filltempcolor = "#000000";
                                fillcolor = scope.filltempcolor;
                            }
                            if (DocObj.backgroundColor == "#000000") {
                                scope.filltempcolor = "#FFFFFF";
                                fillcolor = scope.filltempcolor;
                            }
                        }
                    }
                    else {
                        scope.stroketempcolor = scope.strokecolor;
                        linecolor = scope.stroketempcolor;
                        scope.filltempcolor = scope.fillcolor;
                        fillcolor = scope.filltempcolor;
                    }
                    if (drawprint && scope.strokecolor == "#FFFFFF") {
                        scope.stroketempcolor = "#000000";
                        linecolor = scope.stroketempcolor;
                    }
                    if (drawprint && scope.fillcolor == "#FFFFFF") {
                        scope.filltempcolor = "#000000";
                        fillcolor = scope.filltempcolor;
                    }
                    
                    if (scope.drawcompare) {
                        //ctx.strokeStyle = scope.comparecolor;

                        linecolor = scope.comparecolor;

                    }
                    else if (scope.parent.viewmode == 1) {

                        if (DocObj.backgroundColor == "#FFFFFF") {
                            //pathHandle.filltempcolor = "#000000";

                            linecolor = "#000000";
                            fillcolor = "#000000";

                            //ctx.strokeStyle = "#000000";
                            //ctx.fillStyle = "#000000";
                        }
                        if (DocObj.backgroundColor == "#000000" || DocObj.backgroundColor == "#A0A0A0") {
                        //if (DocObj.backgroundColor == "#000000") {
                            //pathHandle.filltempcolor = "#FFFFFF";
                            //ctx.strokeStyle = "#FFFFFF";
                            //ctx.fillStyle = "#FFFFFF";

                            linecolor = "#FFFFFF";
                            fillcolor = "#FFFFFF";

                        }

                        //ctx.fillStyle = "black";
                    }
                    else {
                        //ctx.strokeStyle = scope.stroketempcolor;
                        linecolor = scope.stroketempcolor;
                    }

                    if( scope.parent.comparemode == 0 ){
                        if(scope.role == 1){

                            //ctx.fillStyle = scope.parent.backgroundColor;
                            //ctx.strokeStyle = scope.parent.backgroundColor;

                            linecolor = scope.parent.backgroundColor;
                            fillcolor = scope.parent.backgroundColor;

    
                        }else if (scope.role == 2){
                            //ctx.fillStyle = scope.parent.overlayColor;
                            //ctx.strokeStyle = scope.parent.overlayColor;

                            linecolor = scope.parent.overlayColor;
                            fillcolor = scope.parent.overlayColor;


                        }
    
                    }

                    if(highlightdraw){

                        fillcolor = szmarkVectorColor;
                        linecolor = szmarkVectorColor;
    
                    }

                    

                    return drawcolors = {linecolor : linecolor, fillcolor : fillcolor };
                };                     

                function getTextWidth(ctx, text, font) {
                    // re-use canvas object for better performance
                    //const canvas = getTextWidth.canvas || (getTextWidth.canvas = document.createElement("canvas"));
                    //const context = canvas.getContext("2d");
                    ctx.save();
                    ctx.font = font;
                    const metrics = ctx.measureText(text);
                    ctx.restore();


                    return metrics.width;
                }


                this.setTextWidth = function(ctx){
                    if(scope.textwidthset){
                        return;
                    }

                    var fontvalue = "normal " + scope.height + "px " + scope.fontname + ", Helvetica";

                    scope.textwidth = getTextWidth(ctx, scope.text, fontvalue);

                    scope.textwidthset = true;


                }

                this.highlight = function(ctx, scalefactor, offsetx, offsety, mediax, mediay, mediah){

                    highlightdraw = true;
                    scope.drawme(ctx, scalefactor, offsetx, offsety, mediax, mediay, mediah, 1, 1, false, false);
                    highlightdraw = false;

                };

                
                this.drawme = function (ctx, scalefactor, offsetx, offsety, mediax, mediay, mediah, ls, bs, drawprint, magnify) {
                    var x1scaled = (scope.x1 - mediax) * scalefactor;
                    var y1scaled = (mediah - scope.y1) * scalefactor;
                    var height = scope.height * scalefactor;
                    y1scaled += height;
                    //var x2scaled = (this.x2-mediax) * scalefactor;
                    //var y2scaled = (mediah - this.y2) * scalefactor;
                    if (ls == 0 || bs == 0) {
                        return;
                    }

                    if(!DocObj){
                        return;
                    }


                    //return drawcolors = {linecolor : linecolor, fillcolor : fillcolor };

                    var drawcolors = scope.getdrawColors(drawprint);

                    ctx.fillStyle = drawcolors.fillcolor;
                    ctx.strokeStyle = drawcolors.linecolor;

                    if (height < 5) {
                        //var fontvalue = "800 " + height + "px " + scope.fontname + ", Helvetica";
                        var fontvalue = "normal " + height + "px " + scope.fontname + ", Helvetica";
                        //console.log(height);
                    }
                    else {
                        fontvalue = "normal " + height + "px " + scope.fontname + ", Helvetica";
                    }

                    if (highlightdraw){
                        fontvalue = "normal " + height + 1 + "px " + scope.fontname + ", Helvetica";
                    }
                    //ctx.font = this.weight + " " + height + "pt " +  this.fontname;
                    ctx.font = fontvalue;


                    x1scaled += offsetx;
                    y1scaled += offsety;

                    x1scaled = pxround(x1scaled);
                    y1scaled = pxround(y1scaled);


                    ctx.save();
                    if (scope.rotation != 0) {
                        ctx.translate(x1scaled, y1scaled - height);
                        ctx.rotate(scope.rotation);
                        ctx.translate(-x1scaled, -(y1scaled - height));
                    }
                    ctx.fillText(scope.text, x1scaled, y1scaled);
                    ctx.restore();

                    scope.setTextWidth(ctx);
                    
                    scope.drawcompare = false;
                };
                this.Close = function () {
                };
            }
        }

        class pdfimageObject {
            constructor(xmldata, parent, blockname, binary, pathindx) {
                //<image x1='1908247.00' y1='1777044.00' x2='2851685.00' y2='2484622.00' type="png" encoding="base64">
                var scope = this;
                this.parent = parent;
                this.index = pathindx;
                this.type = "image";

                this.magnify = xmldata.magnify;
                
                this.pagenumber = 0;
                this.pdfisrendered = false;
                this.pdfpagewidth = 0;
                this.pdfpageheight = 0;
                this.cleanupAfterRender = true;
                this.pageRendering = false;
                this.pdfrenderopque = 0;
                this.dimset = false;
                this.bMaxzoom = false;
                this.nPDFMaxFactor = 4;
                this.viewport = null;
                //this.pdfscale = 1;

                this.dscalepdf = 1;
                this.pdfpagescale = 1;


                //this.pagedrawcnv = document.createElement('canvas');
                //this.pagedrawctx = this.pagedrawcnv.getContext('2d');

                //canvasContext: scope.pagedrawctx,

                //thispage.pagedrawCanvas = document.createElement('canvas');
                //thispage.pagedrawctx = thispage.pagedrawCanvas.getContext('2d');
                //thispage.pdfisrendered = false;


                //scope.pagedrawctx


                this.image = undefined;
                this.blockname = blockname;
                this.blockstate = 1;
                this.layerstate = 1;
                this.x1 = xmldata.x1;
                this.y1 = xmldata.y1;
                this.x2 = xmldata.x2;
                this.y2 = xmldata.y2;
                this.width = (this.x2 - parent.x)-(this.x1 - parent.x);
                this.height = (parent.h - this.y1) - (parent.h - this.y2);
                this.role = xmldata.role;
                this.isoverlay = false;
                this.isbackground = false;
                this.bdrawn = false;
                this.numcolors = 0;
                this.mouseon = false;
                this.mouseover = false;
                
                //scope.x, scope.y, scope.h
                //mediax, mediay, mediah

                this.layer = xmldata.layer;
                this.layerstate = xmldata.layerstate;
                this.drawmode = xmldata.drawmode;
                this.selected = false;
                this.hasArea = false;
                this.imageloaded = false;
                this.invertible = false;
                this.imagecnv = document.createElement('canvas');
                this.imagectx = this.imagecnv.getContext('2d', { willReadFrequently: true });

                //var createimage = new Image();

                var cachfolder;

                var highlightdraw = false;

                if (xmldata.role == 1 || xmldata.role == 2){
                    this.invertible = true;
                    
                }

                /*if(xmldata.useref){

                }else if (!xmldata.useref) {

                }*/

                
                
                /*imageobject = {
                 x1:lX1,
                 y1:lY1,
                 x2:lX2,
                 y2:lY2,
                 image : imagestr
                 };*/

                /*function createimagedata(image){
                    var byteData = base64StringToByteArray(image);
                    var parsedPngData = parseBytes(byteData);
                    var imagedata = enrichParsedData(parsedPngData);
                    
                    scope.imagecnv.width = imagedata.width;
                    scope.imagecnv.height = imagedata.height;


                    if (imagedata.bitDepth == 1){
                        this.invertible = true;
                    }

                    createimage.onload = scope.imageload;
                    createimage.src = image;


                }*/

                function transparent (data) {
                    for (var i = 0; i < data.length; i += 4) {
                        if (data[i] == 255 && data[i + 1] == 255 && data[i + 2] == 255) {
                            data[i + 3] = 100; // - data[i + 2]; // transparent
                            //do nothting with white
                        }
                    }
                };


                function setinvert(data) {

                    for (var i = 0; i < data.length; i += 4) {
                        data[i] = 255 - data[i]; // red
                        data[i + 1] = 255 - data[i + 1]; // green
                        data[i + 2] = 255 - data[i + 2]; // blue
                    }

                }

                function setcolor(data, color) {
                    //var c = color; // 16712640
                    /*var components = {
                        r: (color & 0xff0000) >> 16,
                        g: (color & 0x00ff00) >> 8,
                        b: (color & 0x0000ff)
                    };*/
                    var components = convertHexrgb(color);
                    for (var i = 0; i < data.length; i += 4) {
                        if (data[i] == 255 && data[i + 1] == 255 && data[i + 2] == 255) {
                            data[i + 3] = 100; // - data[i + 2]; // transparent
                            //do nothting with white
                        }else if(data[i] == 254 && data[i + 1] == 254 && data[i + 2] == 254){
                            data[i + 3] = 100; // - data[i + 2]; // transparent
                        }
                        else {
                            //console.log(data[i], data[i + 1], data[i + 2]);
                            data[i] = components.r; //255 - data[i];     // red
                            data[i + 1] = components.g; //255 - data[i + 1]; // green
                            data[i + 2] = components.b; // - data[i + 2]; // blue
                            //this.overlayColor = '#ff8000';
                        }
                    }
                }


                function countColors(data){
                    var count = 1;
                    var color = {r : data[0], g : data[1], b : data[2]};
                    var newcolor = {r : data[0], g : data[1], b : data[2]};

                    for (var i = 0; i < data.length; i += 4) {
                        /*data[i] = 255 - data[i]; // red
                        data[i + 1] = 255 - data[i + 1]; // green
                        data[i + 2] = 255 - data[i + 2]; // blue*/


                        newcolor = {r : data[i], g : data[i + 1], b : data[i + 2]};
                        if(newcolor.r == color.r && newcolor.g == color.g && newcolor.b == color.b){

                        }else{
                            count ++;
                            color.r = newcolor.r;
                            color.g = newcolor.g;
                            color.b = newcolor.b;

                        }

                    }

                    return count;
                }

                this.setpdfScale = function (scale, curscale) {
                    scope.dscalepdf = scale;
                    scope.pdfpagescale = curscale;

                };


                this.setPageBitmap = function (bitmap) {
                    
                    if(bitmap.error){

                        scope.pdfisrendered = false;
                        return;
                        //check for error

                    }

                    //bitmap.width, bitmap.height

                    if(bitmap.width == 0 || bitmap.height == 0){
                        return;
                    }
                    
                    /*-if (scope.role == 1){
                        scope.invert(parent.backgroundColor, bitmap);
                    }

                    if (scope.role == 2){
                        scope.invert(parent.overlayColor, bitmap);
                    }*/


                    if(parent){

                        if( parent.comparemode == 0 ){
                            if (scope.role == 1){
                                scope.invert(bitmap, parent.backgroundColor );
                            }
    
                            if (scope.role == 2){
                                scope.invert(bitmap, parent.overlayColor );
                            }
    
                        }else if( parent.comparemode == 1 ){

                            if (scope.role == 1){
                                scope.invert(bitmap);
                            }

                            if (scope.role == 2){
                                scope.invert(bitmap);
                            }

                        }


                        if (scope.role == 1 || scope.role == 2){

                            if(!scope.imageloaded){

                                scope.imageloaded = true;
                                parent.imageloadcomplete(scope.role);

                            }
                        }

                        scope.pdfrenderopque -= 1;
                        //console.log(thispage.pdfrenderopque);
                        if (scope.pdfrenderopque <= 0) {
                            scope.pdfrenderopque = 0;
                        }

                        scope.pdfisrendered = true;
                        scope.pageRendering = false;
                        
                        parent.refreshdraw();    
                    }

                    //this.role = 0;
                    // 0 - no role.
                    // 1 - backgroundpage.
                    // 2 - overlaypage.
                    
                    //thispage.dscalepdf = 1;
                    //thispage.pagecanvas.width = bitmap.width;
                    //thispage.pagecanvas.height = bitmap.height;
                    //thispage.offscreenwidth = bitmap.width;
                    //thispage.offscreenheight = bitmap.height;


                    //var cmprcolors = thispage.compositereference.getColors();

                    //test if setting color affects stripes
                    //thispage.canvtileArray[i][j].ctx.putImageData(imageData, 0, 0);
                    /*if(binvert){
                        thispage.setinvert(imageData.data);
                    }else{
                        thispage.transparent(imageData.data);
            
                    }*/
                };



                this.getfoxitbitmap = function (scale) {
                    //var pgscalefactor = (thispage.offscreenwidth / thispage.pdfpagewidth);

                    if (scope.pageRendering) {
                        //do not start to render twice.
                        return;
                    }

                    var rect = {x : scope.x1, y : (scope.parent.h - scope.y1) - scope.height, w : scope.width, h : scope.height};
                    //var rect = {x : scope.x1, y : (scope.height - scope.y1) - scope.height, w : scope.width, h : scope.height};
                    //console.log(rect);

                    if (scope.role == 1){
                        var docref = scope.parent.bgDocObjref;
                        var pageindex = scope.parent.bgpageindex;

                        scope.pdfpagewidth = docref.pages[pageindex].originalPDFwidth;
                        scope.pdfpageheight = docref.pages[pageindex].originalPDFheight;
        

                    }else if (scope.role == 2){
                        docref = scope.parent.fgDocObjref;
                        pageindex = scope.parent.fgpageindex;

                        scope.pdfpagewidth = docref.pages[pageindex].originalPDFwidth;
                        scope.pdfpageheight = docref.pages[pageindex].originalPDFheight;


                    }

                    rect = {x : scope.x1, y : (scope.pdfpageheight - scope.y1) - scope.height, w : scope.width, h : scope.height};

                    if(docref == undefined){
                        return;
                    }

                    if(pageindex == undefined){
                        return;
                    }

                    scope.pageRendering = true;
                    
                    if (scope.role == 1 || scope.role == 2) {


                        if(docref.foxitdoc != null){


                         scope.nPDFMaxFactor = Math.sqrt(nMaximageArea / (scope.pdfpagewidth * scope.pdfpageheight));
                         //console.log(scope.nPDFMaxFactor);

                        if (scale > scope.nPDFMaxFactor) {
                            var restscale = scale / scope.nPDFMaxFactor;
                            scale = scope.nPDFMaxFactor;
                            scope.bMaxzoom = true;
                        }
                        else {
                            restscale = 1;
                            scope.bMaxzoom = false;
                        }
                            //var bitmapscale = scale;

                            scope.setpdfScale( restscale, scale );
                            
                            rect.x = rect.x * scale;
                            rect.y = rect.y * scale;
                            rect.w = rect.w * scale;
                            rect.h = rect.h * scale;


                            docref.foxitdoc.getBitmapRect(pageindex, scale, rect, scope.setPageBitmap);
                            //docref.foxitdoc.getBitmap(pageindex, scale, scope.setPageBitmap);
                            

                        }

                    }
                    //var pgscalefactor = thispage.getPDFpageScale();
                    //var scalefactor = pgscalefactor * PixelToPoint;
                    //scalefactor *= scale;
                };

                this.imageload = function(ev) {
                    scope.image = ev.currentTarget;
                    
                    //this.width = (this.x2 - parent.x)-(this.x1 - parent.x);
                    //this.height = (parent.h - this.y1) - (parent.h - this.y2);
                    /*if (xmldata.useref) {

                        scope.imagecnv.width = scope.image.naturalWidth;
                        scope.imagecnv.height = scope.image.naturalHeight;
                            
                    }*/

                    //scope.imagecnv.width = document.createElement('canvas');
                    //scope.imagectx = this.imagecnv.getContext('2d');
    
                    scope.imagectx.drawImage(scope.image, 0, 0);
                    
                    scope.imageloaded = true;

                    if(parent){

                        if (scope.role == 1 || scope.role == 2){
                            parent.imageloadcomplete(scope.role);
                        }
                        
                        parent.refreshdraw();
                    }

                    //
                };




                this.getnumColors = function(){
                    var imageData = scope.imagectx.getImageData(0, 0, scope.imagecnv.width, scope.imagecnv.height);
                    
                    scope.numcolors = countColors(imageData);
                    //scope.invertible

                };

                this.invert = function (bitmap, bgcolor){

                    
                    if(scope.bdrawn && scope.invertible){

                        var tdv = new Uint8ClampedArray(bitmap.buffer);
                        var imageData = new ImageData(tdv, bitmap.width, bitmap.height);



                        //scope.pdfpagescale

                        //var imageData = scope.imagectx.getImageData(0, 0, scope.imagecnv.width, scope.imagecnv.height);

                        /*if (DocObj.backgroundColor == "#FFFFFF") {
                            scope.stroketempcolor = "#000000";
                        }
                        if (DocObj.backgroundColor == "#000000") {
                            scope.stroketempcolor = "#FFFFFF";
                        }
                        if(bgcolor)*/

                        if(bgcolor){
                            setcolor(imageData.data, bgcolor);
                        }else{

                            if(parent.comparemode == 1){

                                if (scope.role == 2){
                                    transparent(imageData.data);
                                }
                                
                            }

                            //setinvert(imageData.data);
                        }

                        scope.imagecnv.width = bitmap.width;
                        scope.imagecnv.height = bitmap.height;
    
                        //scope.setinvert(imageData.data);
    
                        scope.imagectx.putImageData(imageData, 0, 0);
    
                    }


                };

                this.countColors = function(data){
                    //not needed for now.
                    
                };

               


                this.insidePolygon = function (x, y, scalefactor, offsetx, offsety, mediax, mediay, mediah) {
                    return false;
                };
                this.toggleselect = function () {
                    var selectstate = !scope.selected;
                    scope.selected = selectstate;
                };

                this.iswithin = function (x, y, scalefactor, offsetx, offsety, mediax, mediay, mediah, hover) {

                    var within = false;

                    var rect = scope.getRectangle(scalefactor, offsetx, offsety, mediax, mediay, mediah);

                    if(x > rect.x && x < (rect.x + rect.w)){
                        if(y > rect.y && y < (rect.y + rect.h)){
                            within = true;
                        }
                    }

                    return within;


                };

                this.getArea = function(){


                    var minx = scope.x1;
                    var miny = scope.y1;
                    var maxx = scope.x2;
                    var maxy = scope.y2;

                    if (minx > maxx) {
                        var tempx = minx;
                        minx = maxx;
                        maxx = tempx;
                    }
                    if (miny > maxy) {
                        var tempy = miny;
                        miny = maxy;
                        maxy = tempy;
                    }
                    /*return {
                        area: (maxx - minx) * (maxy - miny),
                        x: minx,
                        y: miny,
                        w: maxx - minx,
                        h: maxy - miny,
                        wp: (maxx - minx) / 10,
                        hp: (maxy - miny) / 10
                    };*/

                    return (maxx - minx) * (maxy - miny);
                    
                };


                this.getRectangle = function (scalefactor, offsetx, offsety, mediax, mediay, mediah) {
                    var minx = (scope.x1 - mediax) * scalefactor;
                    var miny = (mediah - scope.y1) * scalefactor;
                    var maxx = (scope.x2 - mediax) * scalefactor;
                    var maxy = (mediah - scope.y2) * scalefactor;
                    minx += offsetx;
                    maxx += offsetx;
                    miny += offsety;
                    maxy += offsety;
                    if (minx > maxx) {
                        var tempx = minx;
                        minx = maxx;
                        maxx = tempx;
                    }
                    if (miny > maxy) {
                        var tempy = miny;
                        miny = maxy;
                        maxy = tempy;
                    }
                    return {
                        found: true,
                        x: minx,
                        y: miny,
                        w: maxx - minx,
                        h: maxy - miny,
                        wp: (maxx - minx) / 10,
                        hp: (maxy - miny) / 10
                    };
                };
                this.findsnapPoint = function (x, y, scalefactor, offsetx, offsety, mediax, mediay, mediah) {
                    return {
                        found: false,
                        x: 0,
                        y: 0,
                        type: snapEndpoint
                    };

                };

                this.highlight = function(ctx, scalefactor, offsetx, offsety, mediax, mediay, mediah){

                    highlightdraw = true;
                    scope.drawme(ctx, scalefactor, offsetx + 2, offsety + 2, mediax, mediay, mediah, 1, 1, false, false);
                    highlightdraw = false;

                };


                //this.drawme = function (ctx, scalefactor, offsetx, offsety, mediax, mediay, mediah,ls,bs,drawprint) {
                this.drawme = function (ctx, scalefactor, offsetx, offsety, mediax, mediay, mediah, ls, bs, drawprint, magnify) {
                    var x1scaled = (scope.x1 - mediax) * scalefactor;
                    var y1scaled = (mediah - scope.y1) * scalefactor;
                    var x2scaled = (scope.x2 - mediax) * scalefactor;
                    var y2scaled = (mediah - scope.y2) * scalefactor;



                    if (ls == 0 || bs == 0) {
                        return;
                    }

                    if(!DocObj){
                        return;
                    }

                    if(!scope.magnify && magnify){
                        return;
                    }

                    if(scope.magnify && !magnify){
                        return;
                    }


                    x1scaled += offsetx;
                    y1scaled += offsety;
                    x2scaled += offsetx;
                    y2scaled += offsety;
                    var height = y1scaled - y2scaled;
                    var width = x2scaled - x1scaled;

                    x1scaled = pxround(x1scaled);
                    y2scaled = pxround(y2scaled);


                    if((!scope.parent.backgroundrender || magnify) && !drawprint){

                        var cnvwidth = canvasowidth;
                        var cnvheight = canvasoheight;
        
                        if(magnify){
                            cnvwidth = magnifysize.width;
                            cnvheight = magnifysize.height;
        
                        }
        
                        //if(!lineobj.parent.backgroundrender){
                        if (x1scaled < 0 && x2scaled < 0) {
        
                            return;
                        }
                        if (y1scaled < 0 && y2scaled < 0) {
        
                            return;
                        }
                        if (x1scaled > cnvwidth && x2scaled > cnvwidth) {
                        //if (x1scaled > canvasowidth && x2scaled > canvasowidth) {
        
                            return;
                        }
                        if (y1scaled > cnvheight && y2scaled > cnvheight) {
                        //if (y1scaled > canvasoheight && y2scaled > canvasoheight) {
        
                            return;
                        }
        
                    }


                    if(scope.imageloaded){


                        //scope.pagedrawcnv.width = scope.viewport.width;
                        //scope.pagedrawcnv.height = scope.viewport.height;
                        //this.dscalepdf = 1;
                        //this.pdfpagescale = 1;
        
                        /*if(!scope.bMaxzoom){

                        }*/

                        if(scope.pdfpagescale != scalefactor){

                            scope.pdfrenderopque += 1;
                            //scope.renderPDFpagescale(scalefactor);
                            scope.getfoxitbitmap(scalefactor);
                         
                        //call scope.renderPDFpagescale with new calculated scale and add to scope.pdfrenderopque
                        }


                        /*if(width / scope.viewport.width != 1){
                            
                            var newscale = (width / scope.viewport.width);
                            console.log(newscale);

                        }*/

                        ctx.drawImage(scope.imagecnv, x1scaled, y2scaled, width, height);    
                        this.bdrawn = true;
                        //scope.imagecnv
                        //ctx.drawImage(this.image, x1scaled, y2scaled, width, height);    
                    }

                    
                    //ctx.save();
                };
                this.Close = function () {
                };


                scope.getfoxitbitmap(1);
                //scope.renderPDFpagescale(1);

                /*if (xmldata.useref) {

                    if(xmldata.addcache){
                        cachfolder = getURLPath(DocObj.FileNameSRC);
                        var imageurl = cachfolder + xmldata.image;
                        //createimage.src = 
                        
    
                    }else{
                        //createimage.src = xmldata.image;
                        imageurl = xmldata.image;
                    }

                    convertImageToBase64(imageurl).then(function(base64image){
                        
                        createimagedata(base64image);


                    });

                }else{
                    createimagedata(xmldata.image);
                }*/

                
                /*if (xmldata.useref) {

                    if(xmldata.addcache){
                        cachfolder = getURLPath(DocObj.FileNameSRC);
                        createimage.src = cachfolder + xmldata.image;
    
                    }else{
                        createimage.src = xmldata.image;
                    }
                }
                else {
                    

                }*/

            }
        }

        class imageObject {
            constructor(xmldata, parent, blockname, binary, pathindx) {
                //<image x1='1908247.00' y1='1777044.00' x2='2851685.00' y2='2484622.00' type="png" encoding="base64">
                var scope = this;
                this.parent = parent;
                this.index = pathindx;
                this.type = "image";
                this.image = undefined;
                this.blockname = blockname;
                this.blockstate = 1;
                this.layerstate = 1;
                this.x1 = xmldata.x1;
                this.y1 = xmldata.y1;
                this.x2 = xmldata.x2;
                this.y2 = xmldata.y2;
                this.width = (this.x2 - parent.x)-(this.x1 - parent.x);
                this.height = (parent.h - this.y1) - (parent.h - this.y2);
                this.entityType = xmldata.entityType;
                this.entityType.type = 18;
                this.role = xmldata.role;
                this.isoverlay = false;
                this.isbackground = false;
                this.bdrawn = false;
                this.numcolors = 0;
                this.mouseon = false;
                this.mouseover = false;


                //scope.x, scope.y, scope.h
                //mediax, mediay, mediah


                this.layer = xmldata.layer;
                this.layerstate = xmldata.layerstate;
                this.drawmode = xmldata.drawmode;
                this.selected = false;
                this.hasArea = false;
                this.imageloaded = false;
                this.invertible = false;
                this.imagecnv = document.createElement('canvas');
                this.imagectx = this.imagecnv.getContext('2d', { willReadFrequently: true });
                //this.imagectx = this.imagecnv.getContext('2d');

                var createimage = new Image();

                var cachfolder;

                var highlightdraw = false;

                if (xmldata.role == 1 || xmldata.role == 2){
                    scope.invertible = true;
                    
                }

                /*if(xmldata.useref){

                }else if (!xmldata.useref) {

                }*/

                
                
                /*imageobject = {
                 x1:lX1,
                 y1:lY1,
                 x2:lX2,
                 y2:lY2,
                 image : imagestr
                 };*/

                function createimagedata(image){
                    var byteData = base64StringToByteArray(image);
                    var parsedPngData = parseBytes(byteData);
                    var imagedata = enrichParsedData(parsedPngData);

                    
                    //var imageblobPNGData = new Blob(byteData, {type: "image/png"});
                    //var blob = 

                    
                    scope.imagecnv.width = imagedata.width;
                    scope.imagecnv.height = imagedata.height;


                    if (imagedata.bitDepth == 1){
                        scope.invertible = true;
                    }

                    

                    //var urlCreator = window.URL || window.webkitURL;
                    //var imageUrl = urlCreator.createObjectURL(imageblobPNGData);

                    createimage.onload = scope.imageload;
                    createimage.src = image;


                }

                function transparent (data) {
                    for (var i = 0; i < data.length; i += 4) {
                        if (data[i] == 255 && data[i + 1] == 255 && data[i + 2] == 255) {
                            data[i + 3] = 100; // - data[i + 2]; // transparent
                            //do nothting with white
                        }
                    }
                };


                function setinvert(data) {

                    for (var i = 0; i < data.length; i += 4) {
                        data[i] = 255 - data[i]; // red
                        data[i + 1] = 255 - data[i + 1]; // green
                        data[i + 2] = 255 - data[i + 2]; // blue
                    }

                }

                function setcolor(data, color) {
                    //var c = color; // 16712640
                    /*var components = {
                        r: (color & 0xff0000) >> 16,
                        g: (color & 0x00ff00) >> 8,
                        b: (color & 0x0000ff)
                    };*/
                    var components = convertHexrgb(color);
                    for (var i = 0; i < data.length; i += 4) {
                        if (data[i] == 255 && data[i + 1] == 255 && data[i + 2] == 255) {
                            data[i + 3] = 100; // - data[i + 2]; // transparent
                            //do nothting with white
                        }else if(data[i] == 254 && data[i + 1] == 254 && data[i + 2] == 254){
                            data[i + 3] = 100; // - data[i + 2]; // transparent
                        }
                        else {
                            //console.log(data[i], data[i + 1], data[i + 2]);
                            data[i] = components.r; //255 - data[i];     // red
                            data[i + 1] = components.g; //255 - data[i + 1]; // green
                            data[i + 2] = components.b; // - data[i + 2]; // blue
                            //this.overlayColor = '#ff8000';
                        }
                    }
                }


                function countColors(data){
                    var count = 1;
                    var color = {r : data[0], g : data[1], b : data[2]};
                    var newcolor = {r : data[0], g : data[1], b : data[2]};

                    for (var i = 0; i < data.length; i += 4) {
                        /*data[i] = 255 - data[i]; // red
                        data[i + 1] = 255 - data[i + 1]; // green
                        data[i + 2] = 255 - data[i + 2]; // blue*/


                        newcolor = {r : data[i], g : data[i + 1], b : data[i + 2]};
                        if(newcolor.r == color.r && newcolor.g == color.g && newcolor.b == color.b){

                        }else{
                            count ++;
                            color.r = newcolor.r;
                            color.g = newcolor.g;
                            color.b = newcolor.b;

                        }

                    }

                    return count;
                }

                            

                this.imageload = function(ev) {
                    scope.image = ev.currentTarget;
                    
                    //this.width = (this.x2 - parent.x)-(this.x1 - parent.x);
                    //this.height = (parent.h - this.y1) - (parent.h - this.y2);
                    /*if (xmldata.useref) {

                        scope.imagecnv.width = scope.image.naturalWidth;
                        scope.imagecnv.height = scope.image.naturalHeight;
                            
                    }*/

                    //scope.imagecnv.width = document.createElement('canvas');
                    //scope.imagectx = this.imagecnv.getContext('2d');
    
                    scope.imagectx.drawImage(scope.image, 0, 0);
                    
                    scope.imageloaded = true;

                    if(parent){

                        if (scope.role == 1 || scope.role == 2){
                            parent.imageloadcomplete(scope.role);
                        }
                        
                        parent.refreshdraw();
                    }

                    //
                };


                this.getnumColors = function(){
                    var imageData = scope.imagectx.getImageData(0, 0, scope.imagecnv.width, scope.imagecnv.height);
                    
                    scope.numcolors = countColors(imageData);
                    //scope.invertible

                };

                this.invert = function (bgcolor){

                    
                    if(scope.imageloaded && scope.invertible){
                        var imageData = scope.imagectx.getImageData(0, 0, scope.imagecnv.width, scope.imagecnv.height);

                        /*if (DocObj.backgroundColor == "#FFFFFF") {
                            scope.stroketempcolor = "#000000";
                        }
                        if (DocObj.backgroundColor == "#000000") {
                            scope.stroketempcolor = "#FFFFFF";
                        }
                        if(bgcolor)*/

                        setinvert(imageData.data);

                        /*if(bgcolor){
                            setcolor(imageData.data, bgcolor);
                        }else{
                            
                            if(parent.comparemode == 1){
                                transparent(imageData.data);
                            }

                            //setinvert(imageData.data);
                        }*/
    
                        //scope.setinvert(imageData.data);
    
                        scope.imagectx.putImageData(imageData, 0, 0);
    
                    }


                };

                this.countColors = function(data){
                    //not needed for now.
                    
                };

               


                this.insidePolygon = function (x, y, scalefactor, offsetx, offsety, mediax, mediay, mediah) {
                    return false;
                };
                this.toggleselect = function () {
                    var selectstate = !scope.selected;
                    scope.selected = selectstate;
                };

                this.iswithin = function (x, y, scalefactor, offsetx, offsety, mediax, mediay, mediah, hover) {

                    var within = false;

                    var rect = scope.getRectangle(scalefactor, offsetx, offsety, mediax, mediay, mediah);

                    if(x > rect.x && x < (rect.x + rect.w)){
                        if(y > rect.y && y < (rect.y + rect.h)){
                            within = true;
                        }
                    }


                    return within;


                };

                this.getArea = function(){


                    var minx = scope.x1;
                    var miny = scope.y1;
                    var maxx = scope.x2;
                    var maxy = scope.y2;

                    if (minx > maxx) {
                        var tempx = minx;
                        minx = maxx;
                        maxx = tempx;
                    }
                    if (miny > maxy) {
                        var tempy = miny;
                        miny = maxy;
                        maxy = tempy;
                    }
                    /*return {
                        area: (maxx - minx) * (maxy - miny),
                        x: minx,
                        y: miny,
                        w: maxx - minx,
                        h: maxy - miny,
                        wp: (maxx - minx) / 10,
                        hp: (maxy - miny) / 10
                    };*/

                    return (maxx - minx) * (maxy - miny);
                    
                };


                this.getRectangle = function (scalefactor, offsetx, offsety, mediax, mediay, mediah) {
                    var minx = (scope.x1 - mediax) * scalefactor;
                    var miny = (mediah - scope.y1) * scalefactor;
                    var maxx = (scope.x2 - mediax) * scalefactor;
                    var maxy = (mediah - scope.y2) * scalefactor;
                    minx += offsetx;
                    maxx += offsetx;
                    miny += offsety;
                    maxy += offsety;
                    if (minx > maxx) {
                        var tempx = minx;
                        minx = maxx;
                        maxx = tempx;
                    }
                    if (miny > maxy) {
                        var tempy = miny;
                        miny = maxy;
                        maxy = tempy;
                    }
                    return {
                        found: true,
                        x: minx,
                        y: miny,
                        w: maxx - minx,
                        h: maxy - miny,
                        wp: (maxx - minx) / 10,
                        hp: (maxy - miny) / 10
                    };
                };
                this.findsnapPoint = function (x, y, scalefactor, offsetx, offsety, mediax, mediay, mediah) {
                    return {
                        found: false,
                        x: 0,
                        y: 0,
                        type: snapEndpoint
                    };
                };

                this.highlight = function(ctx, scalefactor, offsetx, offsety, mediax, mediay, mediah){

                    var drawrect = scope.getRectangle(scalefactor, offsetx, offsety, mediax, mediay, mediah);

                    ctx.strokeRect(drawrect.x, drawrect.y, drawrect.w, drawrect.h);
                    highlightdraw = true;
                    //scope.drawme(ctx, scalefactor, offsetx + 2, offsety + 2, mediax, mediay, mediah, 1, 1, false, false);
                    highlightdraw = false;

                };

                
                //this.drawme = function (ctx, scalefactor, offsetx, offsety, mediax, mediay, mediah,ls,bs,drawprint) {
                this.drawme = function (ctx, scalefactor, offsetx, offsety, mediax, mediay, mediah, ls, bs, drawprint, magnify) {
                    var x1scaled = (scope.x1 - mediax) * scalefactor;
                    var y1scaled = (mediah - scope.y1) * scalefactor;
                    var x2scaled = (scope.x2 - mediax) * scalefactor;
                    var y2scaled = (mediah - scope.y2) * scalefactor;



                    if (ls == 0 || bs == 0) {
                        return;
                    }

                    if(!DocObj){
                        return;
                    }


                    x1scaled += offsetx;
                    y1scaled += offsety;
                    x2scaled += offsetx;
                    y2scaled += offsety;
                    var height = y1scaled - y2scaled;
                    var width = x2scaled - x1scaled;

                    x1scaled = pxround(x1scaled);
                    y2scaled = pxround(y2scaled);

                    if((!scope.parent.backgroundrender || magnify) && !drawprint){

                        var cnvwidth = canvasowidth;
                        var cnvheight = canvasoheight;
        
                        if(magnify){
                            cnvwidth = magnifysize.width;
                            cnvheight = magnifysize.height;
        
                        }
        
                        //if(!lineobj.parent.backgroundrender){
                        if (x1scaled < 0 && x2scaled < 0) {
        
                            return;
                        }
                        if (y1scaled < 0 && y2scaled < 0) {
        
                            return;
                        }
                        if (x1scaled > cnvwidth && x2scaled > cnvwidth) {
                        //if (x1scaled > canvasowidth && x2scaled > canvasowidth) {
        
                            return;
                        }
                        if (y1scaled > cnvheight && y2scaled > cnvheight) {
                        //if (y1scaled > canvasoheight && y2scaled > canvasoheight) {
        
                            return;
                        }
        
                    }


                    if(scope.imageloaded){
                        ctx.drawImage(scope.imagecnv, x1scaled, y2scaled, width, height);    
                        this.bdrawn = true;
                        //scope.imagecnv
                        //ctx.drawImage(this.image, x1scaled, y2scaled, width, height);    
                    }

                    
                    //ctx.save();
                };
                this.Close = function () {
                };

                if (xmldata.useref) {

                    if(xmldata.addcache){
                        cachfolder = getURLPath(DocObj.FileNameSRC);
                        var imageurl = cachfolder + xmldata.image;
                        //createimage.src = 
                        
    
                    }else{
                        //createimage.src = xmldata.image;
                        imageurl = xmldata.image;
                    }

                    convertImageToBase64(imageurl).then(function(base64image){
                        
                        createimagedata(base64image);


                    });

                }else{
                    createimagedata(xmldata.image);
                }

                
                /*if (xmldata.useref) {

                    if(xmldata.addcache){
                        cachfolder = getURLPath(DocObj.FileNameSRC);
                        createimage.src = cachfolder + xmldata.image;
    
                    }else{
                        createimage.src = xmldata.image;
                    }
                }
                else {
                    

                }*/

            }
        }        

        function drawtextmarker(marerobj,ctx,scalefactor,offsetx,offsety,mediax,mediay,mediah,ls,bs){
            var x1scaled = (marerobj.x1 - mediax) * scalefactor;
            var y1scaled = (mediah - marerobj.y1) * scalefactor;
            var x2scaled = (marerobj.x2 - mediax) * scalefactor;
            var y2scaled = (mediah - marerobj.y2) * scalefactor;

            x1scaled += offsetx;
            y1scaled += offsety;
            x2scaled += offsetx;
            y2scaled += offsety;

            var markercolor = "rgba(255, 255, 0, 0.3)";
            ctx.fillStyle = markercolor;
            ctx.strokeStyle = markercolor;
            ctx.fillRect(x1scaled, y1scaled, x2scaled - x1scaled, y2scaled - y1scaled);



        }

        function drawvectorline(lineobj,ctx, scalefactor, offsetx, offsety, mediax, mediay, mediah,ls,bs,drawprint, magnify){    
            
            //drawvectorline(scope.pathlist[pindx], context, scalefactor, offsetx, offsety, scope.x, scope.y, scope.h, layerstate, blockstate, drawprint, false);
            var x1scaled = (lineobj.x1 - mediax) * scalefactor;
            var y1scaled = (mediah - lineobj.y1) * scalefactor;
            var x2scaled = (lineobj.x2 - mediax) * scalefactor;
            var y2scaled = (mediah - lineobj.y2) * scalefactor;

            if (ls == 0 || bs == 0) {
                return;
            }

            if(!DocObj){
                return;
            }

            

            x1scaled += offsetx;
            y1scaled += offsety;
            x2scaled += offsetx;
            y2scaled += offsety;


            var xdiff = Math.max(x1scaled, x2scaled) - Math.min(x1scaled, x2scaled);
            var ydiff = Math.max(y1scaled, y2scaled) - Math.min(y1scaled, y2scaled);
            var radius = getDiagonal(xdiff, ydiff);

            if(radius < 0.5){
                return;
            }

            

            if((!lineobj.parent.backgroundrender || magnify) && !drawprint){

                var cnvwidth = canvasowidth;
                var cnvheight = canvasoheight;

                if(magnify){
                    cnvwidth = magnifysize.width;
                    cnvheight = magnifysize.height;

                }

                //if(!lineobj.parent.backgroundrender){
                if (x1scaled < 0 && x2scaled < 0) {

                    return;
                }
                if (y1scaled < 0 && y2scaled < 0) {

                    return;
                }
                if (x1scaled > cnvwidth && x2scaled > cnvwidth) {
                //if (x1scaled > canvasowidth && x2scaled > canvasowidth) {

                    return;
                }
                if (y1scaled > cnvheight && y2scaled > cnvheight) {
                //if (y1scaled > canvasoheight && y2scaled > canvasoheight) {

                    return;
                }

            }


            /*if (DocObj.getCurrentpage().VectorPageObj != undefined) {
                if (!DocObj.getCurrentpage().VectorPageObj.backgroundrender) {
                    if (x1scaled < 0 && x2scaled < 0) {
                        return;
                    }
                    if (y1scaled < 0 && y2scaled < 0) {
                        return;
                    }

                    if (x1scaled > canvasowidth && x2scaled > canvasowidth) {
                        return;
                    }
                    if (y1scaled > canvasoheight && y2scaled > canvasoheight) {
                        return;
                    }
                }

            } else {
                if (x1scaled < 0 && x2scaled < 0) {
                    return;
                }
                if (y1scaled < 0 && y2scaled < 0) {
                    return;
                }

                if (x1scaled > canvasowidth && x2scaled > canvasowidth) {
                    return;
                }
                if (y1scaled > canvasoheight && y2scaled > canvasoheight) {
                    return;
                }

            }*/

            //ctx.save();
            ctx.lineCap = 'round';

            
            //ctx.globalAlpha = 0.5;
            //ctx.imageSmoothingEnabled = false;
            /*if(!bKeepVectorColor){
                if (lineobj.stroketempcolor == DocObj.backgroundColor) {
                    if (DocObj.backgroundColor == "#FFFFFF") {
                        lineobj.stroketempcolor = "#000000";
                    }
                    if (DocObj.backgroundColor == "#000000") {
                        lineobj.stroketempcolor = "#FFFFFF";
                    }
                }
            }else{
                lineobj.stroketempcolor = lineobj.strokecolor;
            }*/


            /*if(drawprint && lineobj.stroketempcolor == "#FFFFFF" ){
                lineobj.stroketempcolor = "#000000";
            }*/

            /*if (lineobj.drawcompare) {
                ctx.strokeStyle = lineobj.comparecolor;
            } else if (lineobj.parent.viewmode == 1) {

                //ctx.strokeStyle = "black";

                if (DocObj.backgroundColor == "#FFFFFF") {
                    //pathHandle.filltempcolor = "#000000";
                    ctx.strokeStyle = "#000000";
                    //ctx.fillStyle = "#000000";
                }
                if (DocObj.backgroundColor == "#000000") {
                    //pathHandle.filltempcolor = "#FFFFFF";
                    ctx.strokeStyle = "#FFFFFF";
                    //ctx.fillStyle = "#FFFFFF";
                }


            }else{
                ctx.strokeStyle = lineobj.stroketempcolor;
            }*/

            /*if( lineobj.parent.comparemode == 0 ){
                if(lineobj.role == 1){

                    //ctx.fillStyle = lineobj.parent.backgroundColor;
                    ctx.strokeStyle = lineobj.parent.backgroundColor;
    
                }else if (lineobj.role == 2){
                    //ctx.fillStyle = scope.parent.overlayColor;
                    ctx.strokeStyle = lineobj.parent.overlayColor;
                }
    
            }*/


            /*if (lineobj.linewidth * scalefactor < 1) {
                
                ctx.lineWidth = 1;

                if(drawprint){
                    ctx.lineWidth = 4;    
                }

            } else {
                ctx.lineWidth = lineobj.linewidth * scalefactor;

                if(drawprint){
                    ctx.lineWidth = 4;    
                }

            }*/

            /*if(bOverridelineWidth){
                ctx.lineWidth = 1;
            }*/

            ctx.strokeStyle = lineobj.getlineColor(drawprint);

            ctx.lineWidth = lineobj.getlinewidth(scalefactor, drawprint);
            //ctx.lineWidth = this.linewidth * scalefactor;

            x1scaled = pxround(x1scaled);
            y1scaled = pxround(y1scaled);

            x2scaled = pxround(x2scaled);
            y2scaled = pxround(y2scaled);

            ctx.beginPath();
            ctx.moveTo(x1scaled, y1scaled);
            ctx.lineTo(x2scaled, y2scaled);
            ctx.stroke();

            lineobj.drawcompare = false;
        }

        class lineObject {
            constructor(xmldata, parent, blockname, binary, pathindx) {
                // <line x1="1108602768.00" y1="-433854298.00" x2="1108602767.00" y2="-433830557.00" fs='0' ls='0' lc="#FF69B4" lw='0.00' pen='2' la='41'/>
                var scope = this;
                this.parent = parent;
                this.index = pathindx;
                this.type = "line";
                this.blockname = blockname;
                this.blockstate = 1;
                this.layer = xmldata.layer;
                this.layerstate = 1;
                this.drawmode = xmldata.drawmode;
                this.pen = xmldata.pen;
                this.penarrindex = parent.pentable.getPenArrindex(this.pen);
                this.strokecolor = xmldata.strokecolor;
                this.fillcolor = xmldata.fillcolor;
                this.stroketempcolor = xmldata.strokecolor;
                this.filltempcolor = xmldata.fillcolor;
                this.drawcompare = false;
                this.comparecolor = this.fillcolor;
                this.entityType = xmldata.entityType;
                this.role = xmldata.role;
                
                this.hasArea = false;
                this.selected = false;
                this.linewidth = xmldata.linewidth;
                this.originallinewidth = xmldata.linewidth;
                this.mouseon = false;
                this.mouseover = false;


                var lw = xmldata.linewidth;
                if (lw == 0) {
                    this.linewidth = 1;
                }
                else {
                    this.linewidth = lw;
                }
                this.layerstate = xmldata.layerstate;
                /*var lineobject = {
                layer : drawlayer,
                pen : drawpen,
                strokecolor : hexdrawcolor,
                fillcolor : hexfillcolor,
                linewidth : linewidth,
                x1 : lX1,
                y1 : lY1,
                x2 : lX2,
                y2 : lY2
                };*/
                this.x1 = xmldata.x1;
                this.y1 = xmldata.y1;
                this.x2 = xmldata.x2;
                this.y2 = xmldata.y2;

                var highlightdraw = false;
                
                this.insidePolygon = function (x, y, scalefactor, offsetx, offsety, mediax, mediay, mediah) {
                    return false;
                };
                this.getRectangle = function (scalefactor, offsetx, offsety, mediax, mediay, mediah) {
                    var minx = (scope.x1 - mediax) * scalefactor;
                    var miny = (mediah - scope.y1) * scalefactor;
                    var maxx = (scope.x2 - mediax) * scalefactor;
                    var maxy = (mediah - scope.y2) * scalefactor;
                    minx += offsetx;
                    maxx += offsetx;
                    miny += offsety;
                    maxy += offsety;
                    if (minx > maxx) {
                        var tempx = minx;
                        minx = maxx;
                        maxx = tempx;
                    }
                    if (miny > maxy) {
                        var tempy = miny;
                        miny = maxy;
                        maxy = tempy;
                    }
                    return {
                        found: true,
                        x: minx,
                        y: miny,
                        w: maxx - minx,
                        h: maxy - miny,
                        wp: (maxx - minx) / 10,
                        hp: (maxy - miny) / 10
                    };
                };
                this.toggleselect = function () {
                    var selectstate = !scope.selected;
                    scope.selected = selectstate;
                };
                this.findInterSections = function (line) {
                    return intersect(this.x1, this.y1, this.x2, this.y2, line.x1, line.y1, line.x2, line.y2);
                };
                this.getInterSection = function (x, y, line, scalefactor, offsetx, offsety, mediax, mediay, mediah) {
                    var x1scaled = (scope.x1 - mediax) * scalefactor;
                    var y1scaled = (mediah - scope.y1) * scalefactor;
                    var x2scaled = (scope.x2 - mediax) * scalefactor;
                    var y2scaled = (mediah - scope.y2) * scalefactor;
                    x1scaled += offsetx;
                    y1scaled += offsety;
                    x2scaled += offsetx;
                    y2scaled += offsety;
                    var x3scaled = (line.x1 - mediax) * scalefactor;
                    var y3scaled = (mediah - line.y1) * scalefactor;
                    var x4scaled = (line.x2 - mediax) * scalefactor;
                    var y4scaled = (mediah - line.y2) * scalefactor;
                    x3scaled += offsetx;
                    y3scaled += offsety;
                    x4scaled += offsetx;
                    y4scaled += offsety;
                    //returns false if not found.
                    var intsect = intersect(x1scaled, y1scaled, x2scaled, y2scaled, x3scaled, y3scaled, x4scaled, y4scaled);
                    var snapradius = 10;
                    if (intsect) {
                        if (intsect.x > x - snapradius && intsect.x < x + snapradius) {
                            if (intsect.y > y - snapradius && intsect.y < y + snapradius) {
                                return intsect;
                            }
                        }
                    }
                    return false;
                    //line.x1,  line.y1, line.x2, line.y2
                };

                this.iswithin = function (x, y, scalefactor, offsetx, offsety, mediax, mediay, mediah, hover) {

                    var within = false;

                    within = scope.closeToMouse(x,y,scalefactor, offsetx, offsety, mediax, mediay, mediah);


                    return within;


                };


                this.closeToMouse = function (x, y, scalefactor, offsetx, offsety, mediax, mediay, mediah) {
                    var snapradius = 5;
                    var within = false;
                    var x1scaled = (scope.x1 - mediax) * scalefactor;
                    var y1scaled = (mediah - scope.y1) * scalefactor;
                    var x2scaled = (scope.x2 - mediax) * scalefactor;
                    var y2scaled = (mediah - scope.y2) * scalefactor;
                    x1scaled += offsetx;
                    y1scaled += offsety;
                    x2scaled += offsetx;
                    y2scaled += offsety;
                    var mp = {
                        x: x,
                        y: y
                    };
                    var lp1 = {
                        x: x1scaled,
                        y: y1scaled
                    };
                    var lp2 = {
                        x: x2scaled,
                        y: y2scaled
                    };
                    if (distToSegment(mp, lp1, lp2) < snapradius) {
                        within = true;
                    }
                    return within;
                };

                this.getArea = function(){


                    var minx = scope.x1;
                    var miny = scope.y1;
                    var maxx = scope.x2;
                    var maxy = scope.y2;

                    if (minx > maxx) {
                        var tempx = minx;
                        minx = maxx;
                        maxx = tempx;
                    }
                    if (miny > maxy) {
                        var tempy = miny;
                        miny = maxy;
                        maxy = tempy;
                    }
                    /*return {
                        area: 0,
                        x: minx,
                        y: miny,
                        w: maxx - minx,
                        h: maxy - miny,
                        wp: (maxx - minx) / 10,
                        hp: (maxy - miny) / 10
                    };*/
                    
                    return 0;
                };

                this.findsnapPoint = function (x, y, scalefactor, offsetx, offsety, mediax, mediay, mediah) {
                    var x1scaled = (scope.x1 - mediax) * scalefactor;
                    var y1scaled = (mediah - scope.y1) * scalefactor;
                    var x2scaled = (scope.x2 - mediax) * scalefactor;
                    var y2scaled = (mediah - scope.y2) * scalefactor;
                    x1scaled += offsetx;
                    y1scaled += offsety;
                    x2scaled += offsetx;
                    y2scaled += offsety;
                    var snapradius = 10;
                    var midpointx = (x1scaled + x2scaled) * 0.5;
                    var midpointy = (y1scaled + y2scaled) * 0.5;
                    if (x1scaled > x - snapradius && x1scaled < x + snapradius) {
                        if (y1scaled > y - snapradius && y1scaled < y + snapradius) {
                            return {
                                found: true,
                                x: x1scaled,
                                y: y1scaled,
                                type: snapEndpoint
                            };
                        }
                    }
                    if (x2scaled > x - snapradius && x2scaled < x + snapradius) {
                        if (y2scaled > y - snapradius && y2scaled < y + snapradius) {
                            return {
                                found: true,
                                x: x2scaled,
                                y: y2scaled,
                                type: snapEndpoint
                            };
                        }
                    }
                    if (midpointx > x - snapradius && midpointx < x + snapradius) {
                        if (midpointy > y - snapradius && midpointy < y + snapradius) {
                            return {
                                found: true,
                                x: midpointx,
                                y: midpointy,
                                type: snapMidpoint
                            };
                        }
                    }
                    return {
                        found: false,
                        x: 0,
                        y: 0,
                        type: snapEndpoint
                    };
                };
                this.getlinewidth = function(scalefactor, drawprint){
                    


                    /*if (lineobj.linewidth * scalefactor < 1) {
                
                        ctx.lineWidth = 1;
        
                        if(drawprint){
                            ctx.lineWidth = 4;    
                        }
        
                    } else {
                        ctx.lineWidth = lineobj.linewidth * scalefactor;
        
                        if(drawprint){
                            ctx.lineWidth = 4;    
                        }
        
                    }
        
                    if(bOverridelineWidth){
                        ctx.lineWidth = 1;
                    }*/

                    var linewidth = scope.linewidth;

                    if(scope.parent.applypentable && scope.parent.pentable.changed){

                        if(scope.parent.pentable.getPendirect(scope.penarrindex) != undefined){
                            scope.linewidth = scope.parent.pentable.getPendirect(scope.penarrindex).getwidth();
                            //.getPendirect(scope.penarrindex).getLineWidth();
                        }
                        linewidth = scope.linewidth;
                        
                    }else{
                        scope.linewidth = scope.originallinewidth;
                        linewidth = scope.linewidth;
                    }


                    if (scope.linewidth * scalefactor < 1) {
                
                        //ctx.lineWidth = 1;

                        if(scope.parent.pentable.getscaled()){
                            //linewidth = scope.linewidth * scalefactor;
                            linewidth = 1;
                        }else{
                            linewidth = scope.linewidth;
                        }

                        //linewidth = 1;
        
                        if(drawprint){
                            //ctx.lineWidth = 4;    
                            linewidth = 4;
                        }
        
                    } else {

                        if(scope.parent.applypentable && scope.parent.pentable.changed){
                            if(scope.parent.pentable.getscaled()){
                                linewidth = scope.linewidth * scalefactor;
                            }else{
                                linewidth = scope.linewidth;
                            }
    
                        }else{
                            linewidth = scope.linewidth * scalefactor;
                        }
                        //linewidth = scope.linewidth * scalefactor;
        
                        if(drawprint){
                            linewidth = 4;    
                        }
        
                    }


        
                    if(bOverridelineWidth && !scope.parent.applypentable){
                        //ctx.lineWidth = 1;
                        linewidth = dOveridelinewidth;
                    }

                    if(highlightdraw){

                        linewidth = nVectorhighlightwidth;
    
                    }


                    return linewidth;

                };
                this.getlineColor = function(drawprint){

                    var linecolor = scope.stroketempcolor;

                    if(scope.parent.applypentable && scope.parent.pentable.changed){
                        if(scope.parent.pentable.getPendirect(scope.penarrindex) != undefined){
                            scope.stroketempcolor = scope.parent.pentable.getPendirect(scope.penarrindex).getcolor();
                            linecolor = scope.stroketempcolor;
                        }
                        
                    }else{
                        scope.stroketempcolor = scope.strokecolor;
                        linecolor = scope.stroketempcolor;

                    }

                    

                    if (!bKeepVectorColor) {
                        if (scope.stroketempcolor == DocObj.backgroundColor) {
                            if (DocObj.backgroundColor == "#FFFFFF") {
                                scope.stroketempcolor = "#000000";
                                linecolor = scope.stroketempcolor;

                            }
                            if (DocObj.backgroundColor == "#000000") {
                                scope.stroketempcolor = "#FFFFFF";
                                linecolor = scope.stroketempcolor;
                            }
                        }
                    }
                    else {
                        scope.stroketempcolor = scope.strokecolor;
                        linecolor = scope.stroketempcolor;
                        //scope.filltempcolor = xmldata.fillcolor;
                    }
                    if (drawprint && scope.stroketempcolor == "#FFFFFF") {
                        scope.stroketempcolor = "#000000";
                        linecolor = scope.stroketempcolor;
                    }
                    if (scope.drawcompare) {
                        //ctx.strokeStyle = scope.comparecolor;
                        linecolor = scope.comparecolor;
                    }

                    
                    else if (scope.parent.viewmode == 1) {

                        if (DocObj.backgroundColor == "#FFFFFF") {
                            //pathHandle.filltempcolor = "#000000";
                            //ctx.strokeStyle = "#000000";
                            linecolor = "#000000";
                            //ctx.fillStyle = "#000000";
                        }
                        if (DocObj.backgroundColor == "#000000" || DocObj.backgroundColor == "#A0A0A0") {
                        //if (DocObj.backgroundColor == "#000000") {
                            //pathHandle.filltempcolor = "#FFFFFF";
                            //ctx.strokeStyle = "#FFFFFF";
                            linecolor = "#FFFFFF";
                            //ctx.fillStyle = "#FFFFFF";
                        }
                        //ctx.strokeStyle = "black";

                    }
                    else {
                        //ctx.strokeStyle = scope.stroketempcolor;
                        linecolor = scope.stroketempcolor;
                    }

                    if( scope.parent.comparemode == 0 ){
                        if(scope.role == 1){

                            //ctx.fillStyle = scope.parent.backgroundColor;
                            //ctx.strokeStyle = scope.parent.backgroundColor;
                            linecolor = scope.parent.backgroundColor;
    
                        }else if (scope.role == 2){
                            //ctx.fillStyle = scope.parent.overlayColor;
                            //ctx.strokeStyle = scope.parent.overlayColor;
                            linecolor = scope.parent.overlayColor;
                        }
    
                    }

                    if(highlightdraw){

                        //fillcolor = szmarkColor;
                        linecolor = szmarkVectorColor;
    
                    }

                    return linecolor;
                };                

                this.highlight = function(ctx, scalefactor, offsetx, offsety, mediax, mediay, mediah){

                    highlightdraw = true;
                    scope.drawme(ctx, scalefactor, offsetx, offsety, mediax, mediay, mediah, 1, 1, false, false);
                    highlightdraw = false;

                };


                //this.drawme = function (ctx, scalefactor, offsetx, offsety, mediax, mediay, mediah,ls,bs,drawprint) {
                this.drawme = function (ctx, scalefactor, offsetx, offsety, mediax, mediay, mediah, ls, bs, drawprint, magnify) {
                    var x1scaled = (scope.x1 - mediax) * scalefactor;
                    var y1scaled = (mediah - scope.y1) * scalefactor;
                    var x2scaled = (scope.x2 - mediax) * scalefactor;
                    var y2scaled = (mediah - scope.y2) * scalefactor;
                    if (ls == 0 || bs == 0) {
                        return;
                    }

                    if(!DocObj){
                        return;
                    }


                    x1scaled += offsetx;
                    y1scaled += offsety;
                    x2scaled += offsetx;
                    y2scaled += offsety;
                    //if (!scope.parent.backgroundrender){
                    if (!scope.parent.backgroundrender || magnify) {
                        var cnvwidth = canvasowidth;
                        var cnvheight = canvasoheight;
                        if (magnify) {
                            cnvwidth = magnifysize.width;
                            cnvheight = magnifysize.height;
                        }
                        if (x1scaled < 0 && x2scaled < 0) {
                            return;
                        }
                        if (y1scaled < 0 && y2scaled < 0) {
                            return;
                        }
                        if (x1scaled > cnvwidth && x2scaled > cnvwidth) {
                            //if (x1scaled > canvasowidth && x2scaled > canvasowidth) {
                            return;
                        }
                        if (y1scaled > cnvheight && y2scaled > cnvheight) {
                            //if (y1scaled > canvasoheight && y2scaled > canvasoheight) {
                            return;
                        }
                    }
                    /*if (DocObj.getCurrentpage().VectorPageObj != undefined) {
                        if (!DocObj.getCurrentpage().VectorPageObj.backgroundrender) {
                            if (x1scaled < 0 && x2scaled < 0) {
                                return;
                            }
                            if (y1scaled < 0 && y2scaled < 0) {
                                return;
                            }
        
                            if (x1scaled > canvasowidth && x2scaled > canvasowidth) {
                                return;
                            }
                            if (y1scaled > canvasoheight && y2scaled > canvasoheight) {
                                return;
                            }
                        }
        
                    } else {
                        if (x1scaled < 0 && x2scaled < 0) {
                            return;
                        }
                        if (y1scaled < 0 && y2scaled < 0) {
                            return;
                        }
        
                        if (x1scaled > canvasowidth && x2scaled > canvasowidth) {
                            return;
                        }
                        if (y1scaled > canvasoheight && y2scaled > canvasoheight) {
                            return;
                        }
        
                    }*/
                    //ctx.save();
                    ctx.lineCap = 'round';

                    ctx.strokeStyle = scope.getlineColor(drawprint);

                    //ctx.globalAlpha = 0.5;
                    //ctx.imageSmoothingEnabled = false;
                    /*if (!bKeepVectorColor) {
                        if (scope.stroketempcolor == DocObj.backgroundColor) {
                            if (DocObj.backgroundColor == "#FFFFFF") {
                                scope.stroketempcolor = "#000000";
                            }
                            if (DocObj.backgroundColor == "#000000") {
                                scope.stroketempcolor = "#FFFFFF";
                            }
                        }
                    }
                    else {
                        scope.stroketempcolor = scope.strokecolor;
                        //scope.filltempcolor = xmldata.fillcolor;
                    }*/
                    /*if (drawprint && scope.stroketempcolor == "#FFFFFF") {
                        scope.stroketempcolor = "#000000";
                    }*/
                    /*if (scope.drawcompare) {
                        ctx.strokeStyle = scope.comparecolor;
                    }
                    else if (scope.parent.viewmode == 1) {

                        if (DocObj.backgroundColor == "#FFFFFF") {
                            //pathHandle.filltempcolor = "#000000";
                            ctx.strokeStyle = "#000000";
                            //ctx.fillStyle = "#000000";
                        }
                        if (DocObj.backgroundColor == "#000000") {
                            //pathHandle.filltempcolor = "#FFFFFF";
                            ctx.strokeStyle = "#FFFFFF";
                            //ctx.fillStyle = "#FFFFFF";
                        }
                        //ctx.strokeStyle = "black";

                    }
                    else {
                        ctx.strokeStyle = scope.stroketempcolor;
                    }*/

                    /*if( scope.parent.comparemode == 0 ){
                        if(scope.role == 1){

                            //ctx.fillStyle = scope.parent.backgroundColor;
                            ctx.strokeStyle = scope.parent.backgroundColor;
    
                        }else if (scope.role == 2){
                            //ctx.fillStyle = scope.parent.overlayColor;
                            ctx.strokeStyle = scope.parent.overlayColor;
                        }
    
                    }*/


                    

                    /*if (scope.linewidth * scalefactor < 1) {
                        ctx.lineWidth = 1;
                    }
                    else {
                        ctx.lineWidth = scope.linewidth * scalefactor;
                    }

                    if(bOverridelineWidth){
                        ctx.lineWidth = 1;
                    }*/
                    ctx.lineWidth = scope.getlinewidth(scalefactor, drawprint);

                    //ctx.lineWidth = this.linewidth * scalefactor;
                    ctx.beginPath();
                    ctx.moveTo(x1scaled, y1scaled);
                    ctx.lineTo(x2scaled, y2scaled);
                    ctx.stroke();
                    scope.drawcompare = false;
                    //ctx.restore();
                };
                this.Close = function () {
                };
            }
        }

        class lineArrayObject{

            constructor(xmldata, parent, blockname, binary, pathindx) {
                
                var linesHandle = this;
                this.type = "linesArray";
                this.parent = parent;
                this.index = pathindx;
                
                this.blockname = blockname;
                this.blockstate = 1;

                this.entityType = xmldata.entityType;
                this.role = xmldata.role;
                
                this.pen = xmldata.pen;
                this.penarrindex = parent.pentable.getPenArrindex(this.pen);
                this.selected = false;
                this.hasArea = true;
                this.layer = xmldata.layer;
                this.layerstate = 1;
                this.drawmode = xmldata.drawmode;
                this.filled = xmldata.filled;
                this.strokecolor = xmldata.strokecolor;
                this.fillcolor = xmldata.fillcolor;
                this.stroketempcolor = xmldata.strokecolor;
                this.filltempcolor = xmldata.fillcolor;
                this.drawcompare = false;
                this.comparecolor = this.fillcolor;
                this.mouseon = false;
                this.mouseover = false;

                var lw = xmldata.linewidth;
                if (lw == 0) {
                    this.linewidth = 1;
                    this.originallinewidth = 1;
                }
                else {
                    this.linewidth = lw;
                    this.originallinewidth = lw;
                }
                this.linesarray = xmldata.lines;
                

                this.x = 0;
                this.y = 0;
                this.w = 0;
                this.h = 0;

                var highlightdraw = false;


                this.insidePolygon = function (x, y, scalefactor, offsetx, offsety, mediax, mediay, mediah) {
                    return false;
                };


                /*this.closeToMouse = function (x, y, scalefactor, offsetx, offsety, mediax, mediay, mediah) {
                    var snapradius = 5;
                    var within = false;
                    var x1scaled = (linesHandle.x1 - mediax) * scalefactor;
                    var y1scaled = (mediah - linesHandle.y1) * scalefactor;
                    var x2scaled = (linesHandle.x2 - mediax) * scalefactor;
                    var y2scaled = (mediah - linesHandle.y2) * scalefactor;
                    x1scaled += offsetx;
                    y1scaled += offsety;
                    x2scaled += offsetx;
                    y2scaled += offsety;
                    var mp = {
                        x: x,
                        y: y
                    };
                    var lp1 = {
                        x: x1scaled,
                        y: y1scaled
                    };
                    var lp2 = {
                        x: x2scaled,
                        y: y2scaled
                    };
                    if (distToSegment(mp, lp1, lp2) < snapradius) {
                        within = true;
                    }
                    return within;
                };*/

                this.closeToMouse = function (x, y, scalefactor, offsetx, offsety, mediax, mediay, mediah) {

                    var rect = linesHandle.getRectangle(scalefactor, offsetx, offsety, mediax, mediay, mediah);


                    

                    var within = false;
                    var snapradius = 5;

                    var mp = {
                        x: x,
                        y: y
                    };
                    var lp1 = {
                        x: rect.x,
                        y: rect.y
                    };
                    var lp2 = {
                        x: rect.x + rect.w,
                        y: rect.y + rect.h
                    };
                    if (distToSegment(mp, lp1, lp2) < snapradius) {
                        within = true;
                    }
                    return within;
                };


                this.findrectangle = function(){

                    var minx = Math.min(linesHandle.linesarray[0].x1,linesHandle.linesarray[0].x2);
                    var miny = Math.min(linesHandle.linesarray[0].y1,linesHandle.linesarray[0].y2);
                    var maxx = minx;
                    var maxy = miny;


                    for (var counter = 1; counter < linesHandle.linesarray.length; counter ++) {
                        
                        var relxscaled = Math.min(linesHandle.linesarray[counter].x1,linesHandle.linesarray[counter].x2);
                        var relyscaled = Math.min(linesHandle.linesarray[counter].y1,linesHandle.linesarray[counter].y2);

                        if (relxscaled < minx) {
                            minx = relxscaled;
                        }
                        if (relyscaled < miny) {
                            miny = relyscaled;
                        }
                        if (relxscaled > maxx) {
                            maxx = relxscaled;
                        }
                        if (relyscaled > maxy) {
                            maxy = relyscaled;
                        }
                    }

                    this.x = minx;
                    this.y = miny;
                    this.w = maxx;
                    this.h = maxy;
                };

                this.iswithin = function (x, y, scalefactor, offsetx, offsety, mediax, mediay, mediah, hover) {

                    var within = false;


                    if(linesHandle.entityType.type == 0){

                        within = linesHandle.closeToMouse(x,y,scalefactor, offsetx, offsety, mediax, mediay, mediah);

                    }else if (linesHandle.entityType.type == 17){
                        var rect = linesHandle.getRectangle(scalefactor, offsetx, offsety, mediax, mediay, mediah);

                        if(x > rect.x && x < (rect.x + rect.w)){
                            if(y > rect.y && y < (rect.y + rect.h)){
                                within = true;
                            }
                        }
    
                    }

                    /*var rect = linesHandle.getRectangle(scalefactor, offsetx, offsety, mediax, mediay, mediah);

                    if(x > rect.x && x < (rect.x + rect.w)){
                        if(y > rect.y && y < (rect.y + rect.h)){
                            within = true;
                        }
                    }*/


                    return within;


                };

                this.getArea = function(){


                    var minx = linesHandle.x;
                    var miny = linesHandle.y;
                    var maxx = linesHandle.w;
                    var maxy = linesHandle.h;


                    if (minx > maxx) {
                        var tempx = minx;
                        minx = maxx;
                        maxx = tempx;
                    }
                    if (miny > maxy) {
                        var tempy = miny;
                        miny = maxy;
                        maxy = tempy;
                    }
                    /*return {
                        area: (maxx - minx) * (maxy - miny),
                        x: minx,
                        y: miny,
                        w: maxx - minx,
                        h: maxy - miny,
                        wp: (maxx - minx) / 10,
                        hp: (maxy - miny) / 10
                    };*/

                    return (maxx - minx) * (maxy - miny);
                    
                };


                this.getRectangle = function (scalefactor, offsetx, offsety, mediax, mediay, mediah) {

                    var minx = (linesHandle.x - mediax) * scalefactor;
                    var miny = (mediah - linesHandle.y) * scalefactor;
                    var maxx = (linesHandle.w - mediax) * scalefactor;
                    var maxy = (mediah - linesHandle.h) * scalefactor;
                    minx += offsetx;
                    maxx += offsetx;
                    miny += offsety;
                    maxy += offsety;
                    if (minx > maxx) {
                        var tempx = minx;
                        minx = maxx;
                        maxx = tempx;
                    }
                    if (miny > maxy) {
                        var tempy = miny;
                        miny = maxy;
                        maxy = tempy;
                    }
                    return {
                        found: true,
                        x: minx,
                        y: miny,
                        w: maxx - minx,
                        h: maxy - miny,
                        wp: (maxx - minx) / 10,
                        hp: (maxy - miny) / 10
                    };
                };
                this.toggleselect = function () {
                    var selectstate = !linesHandle.selected;
                    linesHandle.selected = selectstate;
                };
                this.findsnapPoint = function (x, y, scalefactor, offsetx, offsety, mediax, mediay, mediah) {

                    return {
                        found: false,
                        x: 0,
                        y: 0,
                        type: snapEndpoint
                    };


                };
                
                this.getlinewidth = function(scalefactor, drawprint){
                    

                    var linewidth = linesHandle.linewidth;

                    if(linesHandle.parent.applypentable && linesHandle.parent.pentable.changed){

                        if(linesHandle.parent.pentable.getPendirect(linesHandle.penarrindex) != undefined){
                            linesHandle.linewidth = linesHandle.parent.pentable.getPendirect(linesHandle.penarrindex).getwidth();
                        }
                        linewidth = linesHandle.linewidth;
                        
                    }else{
                        linesHandle.linewidth = linesHandle.originallinewidth;
                        linewidth = linesHandle.linewidth;
                    }


                    if (linesHandle.linewidth * scalefactor < 1) {
                        
                        
                        if(linesHandle.parent.pentable.getscaled()){
                        
                            linewidth = 1;
                        }else{
                            linewidth = linesHandle.linewidth;
                        }

                        

                        if(drawprint){
                            
                            linewidth = 4; 
                        }
            
                    }
                    else {
                        

                        if(linesHandle.parent.applypentable && linesHandle.parent.pentable.changed){
                            if(linesHandle.parent.pentable.getscaled()){
                                linewidth = linesHandle.linewidth * scalefactor;
                            }else{
                                linewidth = linesHandle.linewidth;
                            }
    
                        }else{
                            linewidth = linesHandle.linewidth * scalefactor;
                        }


                        
                        
                        if(drawprint){
                            
                            linewidth = 4; 
                        }

                    }

                    if(bOverridelineWidth && !linesHandle.parent.applypentable){
                        
                        linewidth = dOveridelinewidth;
                    }

                    if(highlightdraw){

                        linewidth = nVectorhighlightwidth;
    
                    }
                   

                    return linewidth;

                };

                this.getdrawColors = function(drawprint){

                    var linecolor = linesHandle.stroketempcolor;
                    var fillcolor = linesHandle.filltempcolor;
                    var drawcolors = {linecolor : linecolor, fillcolor : fillcolor };


                    if(linesHandle.parent.applypentable && linesHandle.parent.pentable.changed){
                        if(linesHandle.parent.pentable.getPendirect(linesHandle.penarrindex) != undefined){
                            linesHandle.stroketempcolor = linesHandle.parent.pentable.getPendirect(linesHandle.penarrindex).getcolor();
                            linecolor = linesHandle.stroketempcolor;
                        }
                        
                    }else{

                        linesHandle.stroketempcolor = linesHandle.strokecolor;
                        linecolor = linesHandle.stroketempcolor;

                        
                    }



                    if (!bKeepVectorColor) {
                        if (linesHandle.stroketempcolor == DocObj.backgroundColor) {
                            if (DocObj.backgroundColor == "#FFFFFF") {
                                linesHandle.stroketempcolor = "#000000";
                                linecolor = linesHandle.stroketempcolor;
                            }
                            if (DocObj.backgroundColor == "#000000") {
                                linesHandle.stroketempcolor = "#FFFFFF";
                                linecolor = linesHandle.stroketempcolor;
                            }
                        }
                        if (this.filltempcolor == DocObj.backgroundColor) {
                            if (DocObj.backgroundColor == "#FFFFFF") {
                                linesHandle.filltempcolor = "#000000";
                                fillcolor = linesHandle.filltempcolor;
                            }
                            if (DocObj.backgroundColor == "#000000") {
                                linesHandle.filltempcolor = "#FFFFFF";
                                fillcolor = linesHandle.filltempcolor;
                            }
                        }
                    }
                    else {
                        linesHandle.stroketempcolor = linesHandle.strokecolor;
                        linecolor = linesHandle.stroketempcolor;
                        linesHandle.filltempcolor = linesHandle.fillcolor;
                        fillcolor = linesHandle.filltempcolor;
                    }
                    if (drawprint && linesHandle.strokecolor == "#FFFFFF") {
                        linesHandle.stroketempcolor = "#000000";
                        linecolor = linesHandle.stroketempcolor;
                    }
                    if (drawprint && linesHandle.fillcolor == "#FFFFFF") {
                        linesHandle.filltempcolor = "#000000";
                        fillcolor = linesHandle.filltempcolor;
                    }
                    
                    if (linesHandle.drawcompare) {
                        

                        linecolor = linesHandle.comparecolor;

                    }
                    else if (linesHandle.parent.viewmode == 1) {

                        if (DocObj.backgroundColor == "#FFFFFF") {
                            

                            linecolor = "#000000";
                            fillcolor = "#000000";

                        }
                        if (DocObj.backgroundColor == "#000000" || DocObj.backgroundColor == "#A0A0A0") {

                            linecolor = "#FFFFFF";
                            fillcolor = "#FFFFFF";

                        }

                        
                    }
                    else {
                        
                        linecolor = linesHandle.stroketempcolor;
                    }

                    if( linesHandle.parent.comparemode == 0 ){
                        if(linesHandle.role == 1){


                            linecolor = linesHandle.parent.backgroundColor;
                            fillcolor = linesHandle.parent.backgroundColor;

    
                        }else if (linesHandle.role == 2){

                            linecolor = linesHandle.parent.overlayColor;
                            fillcolor = linesHandle.parent.overlayColor;


                        }
    
                    }
                    
                    if(highlightdraw){

                        fillcolor = szmarkVectorColor;
                        linecolor = szmarkVectorColor;
    
                    }


                    return {linecolor : linecolor, fillcolor : fillcolor };
                };      

                this.highlight = function(ctx, scalefactor, offsetx, offsety, mediax, mediay, mediah){

                    highlightdraw = true;
                    linesHandle.drawme(ctx, scalefactor, offsetx, offsety, mediax, mediay, mediah, 1, 1, false, false);
                    highlightdraw = false;

                };


                this.drawme = function (ctx, scalefactor, offsetx, offsety, mediax, mediay, mediah, ls, bs, drawprint, magnify) {


                    var relstartxscaled = 0;
                    var relstartyscaled = 0;

                    var relendxscaled = 0;
                    var relendtyscaled = 0;

                    if (ls == 0 || bs == 0) {
                        return;
                    }
                    if(!DocObj){
                        return;
                    }

                    linesHandle.render = true;
                    var minx = (linesHandle.x - mediax) * scalefactor;
                    var miny = (mediah - linesHandle.y) * scalefactor;
                    var maxx = (linesHandle.w - mediax) * scalefactor;
                    var maxy = (mediah - linesHandle.h) * scalefactor;
                    minx += offsetx;
                    maxx += offsetx;
                    miny += offsety;
                    maxy += offsety;


                    var xdiff = Math.max(minx, maxx) - Math.min(minx, maxx);
                    var ydiff = Math.max(miny, maxy) - Math.min(miny, maxy);
                    var radius = getDiagonal(xdiff, ydiff);
        
                    if(radius < 20){
                        linesHandle.render = false;
                        return;
                    }



                    if ((!linesHandle.parent.backgroundrender || magnify) && !drawprint) {
                        var cnvwidth = canvasowidth;
                        var cnvheight = canvasoheight;
                        if (magnify) {
                            cnvwidth = magnifysize.width;
                            cnvheight = magnifysize.height;
                        }
                        if ((minx < 0 && maxx < 0) || (miny < 0 && maxy < 0)) {
                            linesHandle.render = false;
                            return;
                        }
                        
                        if ((minx > cnvwidth && maxx > cnvwidth) || (miny > cnvheight && maxy > cnvheight)) {
                            linesHandle.render = false;
                            return;
                        }
                    }

                    var drawcolors = linesHandle.getdrawColors(drawprint);

                    ctx.fillStyle = drawcolors.fillcolor;
                    ctx.strokeStyle = drawcolors.linecolor;
                    ctx.lineWidth = linesHandle.getlinewidth(scalefactor, drawprint);
                    
                    ctx.beginPath();


                    for (var counter = 0; counter < linesHandle.linesarray.length; counter ++) {
                        
                        relstartxscaled = ((linesHandle.linesarray[counter].x1 - mediax) * scalefactor);
                        relstartyscaled = ((mediah - linesHandle.linesarray[counter].y1) * scalefactor);

                        relendxscaled = ((linesHandle.linesarray[counter].x2 - mediax) * scalefactor);
                        relendtyscaled = ((mediah - linesHandle.linesarray[counter].y2) * scalefactor);


                        relstartxscaled += offsetx;
                        relstartyscaled += offsety;
                        
                        relendxscaled += offsetx;
                        relendtyscaled += offsety; 

                        

                        ctx.moveTo(relstartxscaled, relstartyscaled);
                        ctx.lineTo(relendxscaled, relendtyscaled);

                        

                    }
                    
                    ctx.stroke();

                    
                    
                    linesHandle.drawcompare = false;
                };
                this.Close = function () {
                };
                this.findrectangle();
            }

        }

        class dotArrayObject {

            constructor(xmldata, parent, blockname, binary, pathindx) {
                
                var dotsHandle = this;
                this.type = "dotArray";
                this.parent = parent;
                this.index = pathindx;
                this.blockname = blockname;
                this.blockstate = 1;
                this.entityType = xmldata.entityType;
                this.role = xmldata.role;
                this.pen = xmldata.pen;
                this.penarrindex = parent.pentable.getPenArrindex(this.pen);
                this.selected = false;
                this.hasArea = true;
                this.layer = xmldata.layer;
                this.layerstate = 1;
                this.drawmode = xmldata.drawmode;
                this.filled = xmldata.filled;
                this.strokecolor = xmldata.strokecolor;
                this.fillcolor = xmldata.fillcolor;
                this.stroketempcolor = xmldata.strokecolor;
                this.filltempcolor = xmldata.fillcolor;
                this.drawcompare = false;
                this.comparecolor = this.fillcolor;
                this.mouseon = false;
                this.mouseover = false;

                var lw = xmldata.linewidth;
                if (lw == 0) {
                    this.linewidth = 1;
                    this.originallinewidth = 1;
                }
                else {
                    this.linewidth = lw;
                    this.originallinewidth = lw;
                }
                this.dotarray = xmldata.dots;
                this.r = this.linewidth;

                this.x = 0;
                this.y = 0;
                this.w = 0;
                this.h = 0;

                var highlightdraw = false;

                this.insidePolygon = function (x, y, scalefactor, offsetx, offsety, mediax, mediay, mediah) {
                    return false;
                };

                this.closeToMouse = function (x, y, scalefactor, offsetx, offsety, mediax, mediay, mediah) {

                    var rect = dotsHandle.getRectangle(scalefactor, offsetx, offsety, mediax, mediay, mediah);


                    /*if(x > rect.x && x < (rect.x + rect.w)){
                        if(y > rect.y && y < (rect.y + rect.h)){
                            within = true;
                        }
                    }*/


                    /*
                    var within = false;
                    var x1scaled = (dotsHandle.x1 - mediax) * scalefactor;
                    var y1scaled = (mediah - dotsHandle.y1) * scalefactor;
                    var x2scaled = (dotsHandle.x2 - mediax) * scalefactor;
                    var y2scaled = (mediah - dotsHandle.y2) * scalefactor;
                    x1scaled += offsetx;
                    y1scaled += offsety;
                    x2scaled += offsetx;
                    y2scaled += offsety;*/

                    var within = false;
                    var snapradius = 5;

                    var mp = {
                        x: x,
                        y: y
                    };
                    var lp1 = {
                        x: rect.x,
                        y: rect.y
                    };
                    var lp2 = {
                        x: rect.x + rect.w,
                        y: rect.y + rect.h
                    };
                    if (distToSegment(mp, lp1, lp2) < snapradius) {
                        within = true;
                    }
                    return within;
                };

                this.findrectangle = function(){

                    var minx = dotsHandle.dotarray[0].x;
                    var miny = dotsHandle.dotarray[0].y;
                    var maxx = dotsHandle.dotarray[0].x;
                    var maxy = dotsHandle.dotarray[0].y;


                    for (var counter = 2; counter < dotsHandle.dotarray.length; counter ++) {
                        
                        var relxscaled = dotsHandle.dotarray[counter].x;
                        var relyscaled = dotsHandle.dotarray[counter].y;

                        if (relxscaled < minx) {
                            minx = relxscaled;
                        }
                        if (relyscaled < miny) {
                            miny = relyscaled;
                        }
                        if (relxscaled > maxx) {
                            maxx = relxscaled;
                        }
                        if (relyscaled > maxy) {
                            maxy = relyscaled;
                        }
                    }

                    this.x = minx;
                    this.y = miny;
                    this.w = maxx;
                    this.h = maxy;
                };

                this.iswithin = function (x, y, scalefactor, offsetx, offsety, mediax, mediay, mediah, hover) {

                    var within = false;

                    if(dotsHandle.entityType.type == 0){

                        within = dotsHandle.closeToMouse(x,y,scalefactor, offsetx, offsety, mediax, mediay, mediah);

                    }else if (dotsHandle.entityType.type == 17){
                        var rect = dotsHandle.getRectangle(scalefactor, offsetx, offsety, mediax, mediay, mediah);

                        if(x > rect.x && x < (rect.x + rect.w)){
                            if(y > rect.y && y < (rect.y + rect.h)){
                                within = true;
                            }
                        }
    
                    }


                    return within;


                };

                this.getArea = function(){


                    var minx = dotsHandle.x;
                    var miny = dotsHandle.y;
                    var maxx = dotsHandle.w;
                    var maxy = dotsHandle.h;


                    if (minx > maxx) {
                        var tempx = minx;
                        minx = maxx;
                        maxx = tempx;
                    }
                    if (miny > maxy) {
                        var tempy = miny;
                        miny = maxy;
                        maxy = tempy;
                    }
                    /*return {
                        area: (maxx - minx) * (maxy - miny),
                        x: minx,
                        y: miny,
                        w: maxx - minx,
                        h: maxy - miny,
                        wp: (maxx - minx) / 10,
                        hp: (maxy - miny) / 10
                    };*/

                    return (maxx - minx) * (maxy - miny);
                    
                };


                this.getRectangle = function (scalefactor, offsetx, offsety, mediax, mediay, mediah) {

                    var minx = (dotsHandle.x - mediax) * scalefactor;
                    var miny = (mediah - dotsHandle.y) * scalefactor;
                    var maxx = (dotsHandle.w - mediax) * scalefactor;
                    var maxy = (mediah - dotsHandle.h) * scalefactor;
                    minx += offsetx;
                    maxx += offsetx;
                    miny += offsety;
                    maxy += offsety;
                    if (minx > maxx) {
                        var tempx = minx;
                        minx = maxx;
                        maxx = tempx;
                    }
                    if (miny > maxy) {
                        var tempy = miny;
                        miny = maxy;
                        maxy = tempy;
                    }
                    return {
                        found: true,
                        x: minx,
                        y: miny,
                        w: maxx - minx,
                        h: maxy - miny,
                        wp: (maxx - minx) / 10,
                        hp: (maxy - miny) / 10
                    };
                };
                this.toggleselect = function () {
                    var selectstate = !dotsHandle.selected;
                    dotsHandle.selected = selectstate;
                };
                this.findsnapPoint = function (x, y, scalefactor, offsetx, offsety, mediax, mediay, mediah) {

                    return {
                        found: false,
                        x: 0,
                        y: 0,
                        type: snapEndpoint
                    };


                };

                this.getlinewidth = function(scalefactor, drawprint){
                    

                    var linewidth = dotsHandle.linewidth;

                    if(dotsHandle.parent.applypentable && dotsHandle.parent.pentable.changed){

                        if(dotsHandle.parent.pentable.getPendirect(dotsHandle.penarrindex) != undefined){
                            dotsHandle.linewidth = dotsHandle.parent.pentable.getPendirect(dotsHandle.penarrindex).getwidth();
                        }
                        linewidth = dotsHandle.linewidth;
                        
                    }else{
                        dotsHandle.linewidth = dotsHandle.originallinewidth;
                        linewidth = dotsHandle.linewidth;
                    }


                    if (dotsHandle.linewidth * scalefactor < 1) {
                        
                        
                        if(dotsHandle.parent.pentable.getscaled()){
                            
                            linewidth = 1;
                        }else{
                            linewidth = dotsHandle.linewidth;
                        }

                        

                        if(drawprint){
                            
                            linewidth = 4; 
                        }
            
                    }
                    else {
                        

                        if(dotsHandle.parent.applypentable && dotsHandle.parent.pentable.changed){
                            if(dotsHandle.parent.pentable.getscaled()){
                                linewidth = dotsHandle.linewidth * scalefactor;
                            }else{
                                linewidth = dotsHandle.linewidth;
                            }
    
                        }else{
                            linewidth = dotsHandle.linewidth * scalefactor;
                        }


                        
                        
                        if(drawprint){
                            
                            linewidth = 4; 
                        }

                    }

                    if(bOverridelineWidth && !dotsHandle.parent.applypentable){
                        
                        linewidth = dOveridelinewidth;
                    }
                   

                    return linewidth;

                };

                this.getdrawColors = function(drawprint){

                    var linecolor = dotsHandle.stroketempcolor;
                    var fillcolor = dotsHandle.filltempcolor;
                    


                    if(dotsHandle.parent.applypentable && dotsHandle.parent.pentable.changed){
                        if(dotsHandle.parent.pentable.getPendirect(dotsHandle.penarrindex) != undefined){
                            dotsHandle.stroketempcolor = dotsHandle.parent.pentable.getPendirect(dotsHandle.penarrindex).getcolor();
                            linecolor = dotsHandle.stroketempcolor;
                        }
                        
                    }else{

                        dotsHandle.stroketempcolor = dotsHandle.strokecolor;
                        linecolor = dotsHandle.stroketempcolor;

                        
                    }



                    if (!bKeepVectorColor) {
                        if (dotsHandle.stroketempcolor == DocObj.backgroundColor) {
                            if (DocObj.backgroundColor == "#FFFFFF") {
                                dotsHandle.stroketempcolor = "#000000";
                                linecolor = dotsHandle.stroketempcolor;
                            }
                            if (DocObj.backgroundColor == "#000000") {
                                dotsHandle.stroketempcolor = "#FFFFFF";
                                linecolor = dotsHandle.stroketempcolor;
                            }
                        }
                        if (this.filltempcolor == DocObj.backgroundColor) {
                            if (DocObj.backgroundColor == "#FFFFFF") {
                                dotsHandle.filltempcolor = "#000000";
                                fillcolor = dotsHandle.filltempcolor;
                            }
                            if (DocObj.backgroundColor == "#000000") {
                                dotsHandle.filltempcolor = "#FFFFFF";
                                fillcolor = dotsHandle.filltempcolor;
                            }
                        }
                    }
                    else {
                        dotsHandle.stroketempcolor = dotsHandle.strokecolor;
                        linecolor = dotsHandle.stroketempcolor;
                        dotsHandle.filltempcolor = dotsHandle.fillcolor;
                        fillcolor = dotsHandle.filltempcolor;
                    }
                    if (drawprint && dotsHandle.strokecolor == "#FFFFFF") {
                        dotsHandle.stroketempcolor = "#000000";
                        linecolor = dotsHandle.stroketempcolor;
                    }
                    if (drawprint && dotsHandle.fillcolor == "#FFFFFF") {
                        dotsHandle.filltempcolor = "#000000";
                        fillcolor = dotsHandle.filltempcolor;
                    }
                    
                    if (dotsHandle.drawcompare) {
                        

                        linecolor = dotsHandle.comparecolor;

                    }
                    else if (dotsHandle.parent.viewmode == 1) {

                        if (DocObj.backgroundColor == "#FFFFFF") {
                            

                            linecolor = "#000000";
                            fillcolor = "#000000";

                        }
                        if (DocObj.backgroundColor == "#000000" || DocObj.backgroundColor == "#A0A0A0") {

                            linecolor = "#FFFFFF";
                            fillcolor = "#FFFFFF";

                        }

                        
                    }
                    else {
                        
                        linecolor = dotsHandle.stroketempcolor;
                    }

                    if( dotsHandle.parent.comparemode == 0 ){
                        if(dotsHandle.role == 1){


                            linecolor = dotsHandle.parent.backgroundColor;
                            fillcolor = dotsHandle.parent.backgroundColor;

    
                        }else if (dotsHandle.role == 2){

                            linecolor = dotsHandle.parent.overlayColor;
                            fillcolor = dotsHandle.parent.overlayColor;


                        }
    
                    }
                    
                    if(highlightdraw){

                        fillcolor = szmarkVectorColor;
                        linecolor = szmarkVectorColor;
    
                    }

                    return {linecolor : linecolor, fillcolor : fillcolor };
                };      

                this.highlight = function(ctx, scalefactor, offsetx, offsety, mediax, mediay, mediah){

                    highlightdraw = true;
                    dotsHandle.drawme(ctx, scalefactor, offsetx, offsety, mediax, mediay, mediah, 1, 1, false, false);
                    highlightdraw = false;

                };


                //this.drawme = function (ctx, scalefactor, offsetx, offsety, mediax, mediay, mediah,ls,bs,drawprint) {
                this.drawme = function (ctx, scalefactor, offsetx, offsety, mediax, mediay, mediah, ls, bs, drawprint, magnify) {


                    var absxscaled = (dotsHandle.dotarray[0].x - mediax) * scalefactor;
                    var absyscaled = (mediah - dotsHandle.dotarray[0].y) * scalefactor;
                    var relxscaled = 0;
                    var relyscaled = 0;
                    if (ls == 0 || bs == 0) {
                        return;
                    }
                    if(!DocObj){
                        return;
                    }

                    absxscaled += offsetx;
                    absyscaled += offsety;

                    dotsHandle.render = true;
                    var minx = (dotsHandle.x - mediax) * scalefactor;
                    var miny = (mediah - dotsHandle.y) * scalefactor;
                    var maxx = (dotsHandle.w - mediax) * scalefactor;
                    var maxy = (mediah - dotsHandle.h) * scalefactor;
                    minx += offsetx;
                    maxx += offsetx;
                    miny += offsety;
                    maxy += offsety;


                    var xdiff = Math.max(minx, maxx) - Math.min(minx, maxx);
                    var ydiff = Math.max(miny, maxy) - Math.min(miny, maxy);
                    var radius = getDiagonal(xdiff, ydiff);
        
                    if(radius < 20){
                        
                        dotsHandle.render = false;
                        return;
                    }

                    if (ls == 0 || bs == 0) {
                        return;
                    }

                    if(!DocObj){
                        return;
                    }

                    if ((!dotsHandle.parent.backgroundrender || magnify) && !drawprint) {
                        var cnvwidth = canvasowidth;
                        var cnvheight = canvasoheight;
                        if (magnify) {
                            cnvwidth = magnifysize.width;
                            cnvheight = magnifysize.height;
                        }
                        if ((minx < 0 && maxx < 0) || (miny < 0 && maxy < 0)) {
                            dotsHandle.render = false;
                            return;
                        }
                        
                        if ((minx > cnvwidth && maxx > cnvwidth) || (miny > cnvheight && maxy > cnvheight)) {
                            dotsHandle.render = false;
                            return;
                        }
                    }

                    
                    
                    


                    var drawcolors = dotsHandle.getdrawColors(drawprint);
                    var absradius = dotsHandle.getlinewidth(scalefactor, drawprint);
                    ctx.fillStyle = drawcolors.fillcolor;
                    ctx.strokeStyle = drawcolors.linecolor;
                    ctx.lineWidth = absradius;
                    



                    //ctx.beginPath();

                    //absxscaled = pxround(absxscaled);
                    //absyscaled = pxround(absyscaled);

                    
                    //ctx.moveTo(absxscaled, absyscaled); //convert to integer pixels.

                    for (var counter = 0; counter < dotsHandle.dotarray.length; counter ++) {
                        
                        relxscaled = ((dotsHandle.dotarray[counter].x - mediax) * scalefactor);
                        relyscaled = ((mediah - dotsHandle.dotarray[counter].y) * scalefactor);

                        relxscaled += offsetx;
                        relyscaled += offsety;

                        //relxscaled = pxround(relxscaled);
                        //relyscaled = pxround(relyscaled);

                        ctx.beginPath();

                        ctx.arc(relxscaled, relyscaled, absradius, 0, 2 * Math.PI, false);

                        ctx.fill();
                        ctx.stroke();
    
                        //ctx.lineTo(relxscaled, relyscaled); //convert to integer pixels

                    }


                    dotsHandle.drawcompare = false;
                };
                this.Close = function () {
                };
                this.findrectangle();
            }
        }

        class circleObject {
            constructor(xmldata, parent, blockname, binary, pathindx) {
                var circleHandle = this;
                this.type = "circle";
                this.parent = parent;
                this.index = pathindx;
                //circle cx="388466.18" cy="1351977.86" r="10152.93"  fs='0' ls='0' lc="#808080" lw='0.00' wt='25' pen='8' la='3'/>
                this.blockname = blockname;
                this.blockstate = 1;
                this.entityType = xmldata.entityType;
                this.role = xmldata.role;
                this.pen = xmldata.pen;
                this.penarrindex = parent.pentable.getPenArrindex(this.pen);
                /*circleobject = {
                 layer : drawlayer,
                 pen : drawpen,
                 strokecolor : hexdrawcolor,
                 fillcolor : hexfillcolor,
                 linewidth : linewidth,
                 cx : cX,
                 cy : cY,
                 cr : cR,
                 filled : false,
                 };*/
                this.selected = false;
                this.hasArea = true;
                this.layer = xmldata.layer;
                this.layerstate = 1;
                this.drawmode = xmldata.drawmode;
                this.filled = xmldata.filled;
                this.strokecolor = xmldata.strokecolor;
                this.fillcolor = xmldata.fillcolor;
                this.stroketempcolor = xmldata.strokecolor;
                this.filltempcolor = xmldata.fillcolor;
                this.drawcompare = false;
                this.comparecolor = this.fillcolor;
                this.mouseon = false;
                this.mouseover = false;
                

                var lw = xmldata.linewidth;
                if (lw == 0) {
                    this.linewidth = 1;
                    this.originallinewidth = 1;
                }
                else {
                    this.linewidth = lw;
                    this.originallinewidth = lw;
                }
                this.cx = xmldata.cx;
                this.cy = xmldata.cy;
                this.r = xmldata.cr;
                this.fullcircle = Math.PI * 2;

                var highlightdraw = false;

                this.insidePolygon = function (x, y, scalefactor, offsetx, offsety, mediax, mediay, mediah) {
                    return false;
                };

                this.getdisttoArc = function(x, y, scalefactor, offsetx, offsety, mediax, mediay, mediah){

                    var absxscaled = (circleHandle.cx - mediax) * scalefactor;
                    var absyscaled = (mediah - circleHandle.cy) * scalefactor;
                    var absradius = circleHandle.r * scalefactor;

                    absxscaled += offsetx;
                    absyscaled += offsety;

                    //function PointFromArcDist(x, y, xc, yc, radius, sang, sweep){

                    let quartercircle = (Math.PI * 0.5);
                    let startAngle = 0;
                    let endAngle = circleHandle.fullcircle;
                    let sweep;
                    
                    /*if (!arcHandle.clockwise) {
                        // Counter-clockwise sweep
                        
                    } else {
                        // Clockwise sweep
                        sweep = -((startAngle - endAngle + circleHandle.fullcircle) % (circleHandle.fullcircle));
                    }*/

                    sweep = (endAngle - startAngle + circleHandle.fullcircle) % (circleHandle.fullcircle);

                    //var sweep = arcHandle.ea - arcHandle.sa;

                    var dist = PointFromArcDist(x, y, absxscaled, absyscaled, absradius, startAngle + quartercircle, sweep);


                    //ctx.arc(absxscaled, absyscaled, absradius, arcHandle.fullcircle - arcHandle.sa, arcHandle.fullcircle - arcHandle.ea, arcHandle.clockwise);
                    //var dist = PointFromArcDist(x, y, absxscaled, absyscaled, absradius, Math.abs(arcHandle.sa), Math.abs(arcHandle.ea));
                    //var dist = PointFromArcDist(x, y, absxscaled, absyscaled, absradius,arcHandle.fullcircle - arcHandle.sa, arcHandle.fullcircle - arcHandle.ea);
                    return dist;

                };

                this.getlength = function() {

                                        
                    
                    //return radians * (180/pi);


                    return {radius : circleHandle.r, length : Math.PI * 2  * circleHandle.r, Area : Math.PI * 2  * (circleHandle.r * circleHandle.r)};

                    //return sweep * arcHandle.r;

                };


                this.iswithin = function (x, y, scalefactor, offsetx, offsety, mediax, mediay, mediah, hover) {

                    var within = false;

                    var dist = circleHandle.getdisttoArc(x, y, scalefactor, offsetx, offsety, mediax, mediay, mediah);

                    if (dist < 10){
                        within = true;
                    }

                    //var rect = circleHandle.getRectangle(scalefactor, offsetx, offsety, mediax, mediay, mediah);

                    /*if(x > rect.x && x < (rect.x + rect.w)){
                        if(y > rect.y && y < (rect.y + rect.h)){
                            within = true;
                        }
                    }*/


                    return within;


                };


                this.getRectangle = function (scalefactor, offsetx, offsety, mediax, mediay, mediah) {
                    var absxscaled = (circleHandle.cx - mediax) * scalefactor;
                    var absyscaled = (mediah - circleHandle.cy) * scalefactor;
                    var absradius = circleHandle.r * scalefactor;
                    var minx = absxscaled - absradius;
                    var miny = absyscaled - absradius;
                    var maxx = absxscaled + absradius;
                    var maxy = absyscaled + absradius;
                    minx += offsetx;
                    maxx += offsetx;
                    miny += offsety;
                    maxy += offsety;
                    if (minx > maxx) {
                        var tempx = minx;
                        minx = maxx;
                        maxx = tempx;
                    }
                    if (miny > maxy) {
                        var tempy = miny;
                        miny = maxy;
                        maxy = tempy;
                    }
                    return {
                        found: true,
                        x: minx,
                        y: miny,
                        w: maxx - minx,
                        h: maxy - miny,
                        wp: (maxx - minx) / 10,
                        hp: (maxy - miny) / 10
                    };
                };

                /*this.getArea = function(){

                    var absxscaled = circleHandle.cx;
                    var absyscaled = circleHandle.cy;
                    var absradius = circleHandle.r;

                    var minx = absxscaled - absradius;
                    var miny = absyscaled - absradius;
                    var maxx = absxscaled + absradius;
                    var maxy = absyscaled + absradius;

                    if (minx > maxx) {
                        var tempx = minx;
                        minx = maxx;
                        maxx = tempx;
                    }
                    if (miny > maxy) {
                        var tempy = miny;
                        miny = maxy;
                        maxy = tempy;
                    }
                    return {
                        area: (maxx - minx) * (maxy - miny),
                        x: minx,
                        y: miny,
                        w: maxx - minx,
                        h: maxy - miny,
                        wp: (maxx - minx) / 10,
                        hp: (maxy - miny) / 10
                    };
                    
                };*/

                this.toggleselect = function () {
                    var selectstate = !circleHandle.selected;
                    circleHandle.selected = selectstate;
                };
                this.findsnapPoint = function (x, y, scalefactor, offsetx, offsety, mediax, mediay, mediah) {
                    var absxscaled = (this.cx - mediax) * scalefactor;
                    var absyscaled = (mediah - this.cy) * scalefactor;
                    absxscaled += offsetx;
                    absyscaled += offsety;
                    var snapradius = 10;
                    if (absxscaled > x - snapradius && absxscaled < x + snapradius) {
                        if (absyscaled > y - snapradius && absyscaled < y + snapradius) {
                            return {
                                found: true,
                                x: absxscaled,
                                y: absyscaled,
                                type: snapEndpoint
                            };
                        }
                    }
                    else {
                        return {
                            found: false,
                            x: 0,
                            y: 0,
                            type: snapEndpoint
                        };
                    }
                };
                this.getArea = function(){
                    
                    var rsquared = circleHandle.r * circleHandle.r;
                    return rsquared * Math.PI;


                };
                this.getlinewidth = function(scalefactor, drawprint){
                    

                    var linewidth = circleHandle.linewidth;

                    if(circleHandle.parent.applypentable && circleHandle.parent.pentable.changed){

                        if(circleHandle.parent.pentable.getPendirect(circleHandle.penarrindex) != undefined){
                            circleHandle.linewidth = circleHandle.parent.pentable.getPendirect(circleHandle.penarrindex).getwidth();
                        }
                        linewidth = circleHandle.linewidth;
                        
                    }else{
                        circleHandle.linewidth = circleHandle.originallinewidth;
                        linewidth = circleHandle.linewidth;
                    }


                    if (circleHandle.linewidth * scalefactor < 1) {
                        //ctx.lineWidth = 1;
                        
                        if(circleHandle.parent.pentable.getscaled()){
                            //linewidth = scope.linewidth * scalefactor;
                            linewidth = 1;
                        }else{
                            linewidth = circleHandle.linewidth;
                        }

                        //linewidth = 1; 

                        if(drawprint){
                            //ctx.lineWidth = 4;    
                            linewidth = 4; 
                        }
            
                    }
                    else {
                        //ctx.lineWidth = circleHandle.linewidth * scalefactor;

                        if(circleHandle.parent.applypentable && circleHandle.parent.pentable.changed){
                            if(circleHandle.parent.pentable.getscaled()){
                                linewidth = circleHandle.linewidth * scalefactor;
                            }else{
                                linewidth = circleHandle.linewidth;
                            }
    
                        }else{
                            linewidth = circleHandle.linewidth * scalefactor;
                        }


                        //linewidth = circleHandle.linewidth * scalefactor;
                        
                        if(drawprint){
                            //ctx.lineWidth = 4;    
                            linewidth = 4; 
                        }

                    }

                    if(bOverridelineWidth && !circleHandle.parent.applypentable){
                        //ctx.lineWidth = 1;
                        linewidth = dOveridelinewidth;
                    }
                   
                    if(highlightdraw){

                        linewidth = nVectorhighlightwidth;
    
                    }


                    return linewidth;

                };

                this.getdrawColors = function(drawprint){

                    var linecolor = circleHandle.stroketempcolor;
                    var fillcolor = circleHandle.filltempcolor;
                    var drawcolors = {linecolor : linecolor, fillcolor : fillcolor };


                    if(circleHandle.parent.applypentable && circleHandle.parent.pentable.changed){
                        if(circleHandle.parent.pentable.getPendirect(circleHandle.penarrindex) != undefined){
                            circleHandle.stroketempcolor = circleHandle.parent.pentable.getPendirect(circleHandle.penarrindex).getcolor();
                            linecolor = circleHandle.stroketempcolor;
                        }
                        
                    }else{

                        circleHandle.stroketempcolor = circleHandle.strokecolor;
                        linecolor = circleHandle.stroketempcolor;

                        
                    }



                    if (!bKeepVectorColor) {
                        if (circleHandle.stroketempcolor == DocObj.backgroundColor) {
                            if (DocObj.backgroundColor == "#FFFFFF") {
                                circleHandle.stroketempcolor = "#000000";
                                linecolor = circleHandle.stroketempcolor;
                            }
                            if (DocObj.backgroundColor == "#000000") {
                                circleHandle.stroketempcolor = "#FFFFFF";
                                linecolor = circleHandle.stroketempcolor;
                            }
                        }
                        if (this.filltempcolor == DocObj.backgroundColor) {
                            if (DocObj.backgroundColor == "#FFFFFF") {
                                circleHandle.filltempcolor = "#000000";
                                fillcolor = circleHandle.filltempcolor;
                            }
                            if (DocObj.backgroundColor == "#000000") {
                                circleHandle.filltempcolor = "#FFFFFF";
                                fillcolor = circleHandle.filltempcolor;
                            }
                        }
                    }
                    else {
                        circleHandle.stroketempcolor = circleHandle.strokecolor;
                        linecolor = circleHandle.stroketempcolor;
                        circleHandle.filltempcolor = circleHandle.fillcolor;
                        fillcolor = circleHandle.filltempcolor;
                    }
                    if (drawprint && circleHandle.strokecolor == "#FFFFFF") {
                        circleHandle.stroketempcolor = "#000000";
                        linecolor = circleHandle.stroketempcolor;
                    }
                    if (drawprint && circleHandle.fillcolor == "#FFFFFF") {
                        circleHandle.filltempcolor = "#000000";
                        fillcolor = circleHandle.filltempcolor;
                    }
                    
                    if (circleHandle.drawcompare) {
                        //ctx.strokeStyle = circleHandle.comparecolor;

                        linecolor = circleHandle.comparecolor;

                    }
                    else if (circleHandle.parent.viewmode == 1) {

                        if (DocObj.backgroundColor == "#FFFFFF") {
                            

                            linecolor = "#000000";
                            fillcolor = "#000000";

                            //ctx.strokeStyle = "#000000";
                            //ctx.fillStyle = "#000000";
                        }
                        if (DocObj.backgroundColor == "#000000" || DocObj.backgroundColor == "#A0A0A0") {
                        //if (DocObj.backgroundColor == "#000000") {
                            
                            //ctx.strokeStyle = "#FFFFFF";
                            //ctx.fillStyle = "#FFFFFF";

                            linecolor = "#FFFFFF";
                            fillcolor = "#FFFFFF";

                        }

                        //ctx.fillStyle = "black";
                    }
                    else {
                        //ctx.strokeStyle = circleHandle.stroketempcolor;
                        linecolor = circleHandle.stroketempcolor;
                    }

                    if( circleHandle.parent.comparemode == 0 ){
                        if(circleHandle.role == 1){

                            //ctx.fillStyle = circleHandle.parent.backgroundColor;
                            //ctx.strokeStyle = circleHandle.parent.backgroundColor;

                            linecolor = circleHandle.parent.backgroundColor;
                            fillcolor = circleHandle.parent.backgroundColor;

    
                        }else if (circleHandle.role == 2){
                            //ctx.fillStyle = circleHandle.parent.overlayColor;
                            //ctx.strokeStyle = circleHandle.parent.overlayColor;

                            linecolor = circleHandle.parent.overlayColor;
                            fillcolor = circleHandle.parent.overlayColor;


                        }
    
                    }
                    
                    if(highlightdraw){

                        fillcolor = szmarkVectorColor;
                        linecolor = szmarkVectorColor;
    
                    }

                    return drawcolors = {linecolor : linecolor, fillcolor : fillcolor };
                };      

                this.highlight = function(ctx, scalefactor, offsetx, offsety, mediax, mediay, mediah){

                    highlightdraw = true;
                    circleHandle.drawme(ctx, scalefactor, offsetx, offsety, mediax, mediay, mediah, 1, 1, false, false);
                    highlightdraw = false;

                };


                //this.drawme = function (ctx, scalefactor, offsetx, offsety, mediax, mediay, mediah,ls,bs,drawprint) {
                this.drawme = function (ctx, scalefactor, offsetx, offsety, mediax, mediay, mediah, ls, bs, drawprint, magnify) {
                    var absxscaled = (circleHandle.cx - mediax) * scalefactor;
                    var absyscaled = (mediah - circleHandle.cy) * scalefactor;
                    var absradius = circleHandle.r * scalefactor;
                    if (ls == 0 || bs == 0) {
                        return;
                    }

                    if(!DocObj){
                        return;
                    }



                    absxscaled += offsetx;
                    absyscaled += offsety;
                    //if (!circleHandle.parent.backgroundrender){
                    if ((!circleHandle.parent.backgroundrender || magnify) && !drawprint) {
                        var cnvwidth = canvasowidth;
                        var cnvheight = canvasoheight;
                        if (magnify) {
                            cnvwidth = magnifysize.width;
                            cnvheight = magnifysize.height;
                        }
                        if (absxscaled < 0 && absxscaled + absradius < 0) {
                            return;
                        }
                        if (absyscaled < 0 && absyscaled + absradius < 0) {
                            return;
                        }
                        //if (absxscaled > canvasowidth && absxscaled - absradius > canvasowidth) {
                        if (absxscaled > cnvwidth && absxscaled - absradius > cnvwidth) {
                            return;
                        }
                        if (absyscaled > cnvheight && absyscaled - absradius > cnvheight) {
                            //if (absyscaled > canvasoheight && absyscaled - absradius > canvasoheight) {
                            return;
                        }
                    }
                    /*if (DocObj.getCurrentpage().VectorPageObj != undefined) {
                        if (!DocObj.getCurrentpage().VectorPageObj.backgroundrender) {
                            if (absxscaled < 0 && absxscaled + absradius < 0) {
                                return;
                            }
                            if (absyscaled < 0 && absyscaled + absradius < 0) {
                                return;
                            }
        
                            if (absxscaled > canvasowidth && absxscaled - absradius > canvasowidth) {
                                return;
                            }
                            if (absyscaled > canvasoheight && absyscaled - absradius > canvasoheight) {
                                return;
                            }
                        }
                    } else {
                        if (absxscaled < 0 && absxscaled + absradius < 0) {
                            return;
                        }
                        if (absyscaled < 0 && absyscaled + absradius < 0) {
                            return;
                        }
        
                        if (absxscaled > canvasowidth && absxscaled - absradius > canvasowidth) {
                            return;
                        }
                        if (absyscaled > canvasoheight && absyscaled - absradius > canvasoheight) {
                            return;
                        }
        
                    }*/



                    /*if (!bKeepVectorColor) {
                        if (circleHandle.stroketempcolor == DocObj.backgroundColor) {
                            if (DocObj.backgroundColor == "#FFFFFF") {
                                circleHandle.stroketempcolor = "#000000";
                            }
                            if (DocObj.backgroundColor == "#000000") {
                                circleHandle.stroketempcolor = "#FFFFFF";
                            }
                        }
                        if (this.filltempcolor == DocObj.backgroundColor) {
                            if (DocObj.backgroundColor == "#FFFFFF") {
                                circleHandle.filltempcolor = "#000000";
                            }
                            if (DocObj.backgroundColor == "#000000") {
                                circleHandle.filltempcolor = "#FFFFFF";
                            }
                        }
                    }
                    else {
                        circleHandle.stroketempcolor = circleHandle.strokecolor;
                        circleHandle.filltempcolor = circleHandle.fillcolor;
                    }
                    if (drawprint && circleHandle.strokecolor == "#FFFFFF") {
                        circleHandle.stroketempcolor = "#000000";
                    }
                    if (drawprint && circleHandle.fillcolor == "#FFFFFF") {
                        circleHandle.filltempcolor = "#000000";
                    }*/


                    var drawcolors = circleHandle.getdrawColors(drawprint);
                    ctx.fillStyle = drawcolors.fillcolor;
                    ctx.strokeStyle = drawcolors.linecolor;


                    ctx.lineCap = 'round';


                    /*if (circleHandle.drawcompare) {
                        ctx.strokeStyle = circleHandle.comparecolor;
                    }
                    else if (circleHandle.parent.viewmode == 1) {

                        if (DocObj.backgroundColor == "#FFFFFF") {
                            
                            ctx.strokeStyle = "#000000";
                            ctx.fillStyle = "#000000";
                        }
                        if (DocObj.backgroundColor == "#000000") {
                            
                            ctx.strokeStyle = "#FFFFFF";
                            ctx.fillStyle = "#FFFFFF";
                        }

                        //ctx.fillStyle = "black";
                    }
                    else {
                        ctx.strokeStyle = circleHandle.stroketempcolor;
                    }*/

                    /*if( circleHandle.parent.comparemode == 0 ){
                        if(circleHandle.role == 1){

                            ctx.fillStyle = circleHandle.parent.backgroundColor;
                            ctx.strokeStyle = circleHandle.parent.backgroundColor;
    
                        }else if (circleHandle.role == 2){
                            ctx.fillStyle = circleHandle.parent.overlayColor;
                            ctx.strokeStyle = circleHandle.parent.overlayColor;
                        }
    
                    }*/



                    /*if (circleHandle.linewidth * scalefactor < 1) {
                        ctx.lineWidth = 1;

                        if(drawprint){
                            ctx.lineWidth = 4;    
                        }
            
                    }
                    else {
                        ctx.lineWidth = circleHandle.linewidth * scalefactor;
                        if(drawprint){
                            ctx.lineWidth = 4;    
                        }

                    }

                    if(bOverridelineWidth){
                        ctx.lineWidth = 1;
                    }*/

                    ctx.lineWidth = circleHandle.getlinewidth(scalefactor, drawprint);


                    ctx.beginPath();
                    ctx.arc(absxscaled, absyscaled, absradius, 0, 2 * Math.PI, false);
                    if (this.filled == 1) {
                        if (circleHandle.drawcompare) {
                            ctx.fillStyle = circleHandle.comparecolor;
                        }
                        else {
                            if (parent.blocklist[circleHandle.blockname].overridecolor) {
                                ctx.fillStyle = parent.blocklist[circleHandle.blockname].color;
                            }
                            else if (circleHandle.selected) {
                                //var grd = ctx.createLinearGradient(minx, miny, maxx, maxy);
                                var rect = this.getRectangle(scalefactor, offsetx, offsety, mediax, mediay, mediah);
                                /*found : true,
                                x : minx,
                                y : miny,
                                w : maxx - minx,
                                h : maxy - miny,
                                wp : (maxx - minx) / 10,
                                hp : (maxy - miny) / 10*/
                                var minx = rect.x;
                                var miny = rect.y;
                                var maxx = rext.w + rect.x;
                                var maxy = rext.h + rect.y;
                                var grd = ctx.createLinearGradient(maxx, miny, minx, maxy);
                                grd.addColorStop(0, parent.selectColor);
                                grd.addColorStop(.5, parent.gradientStop);
                                grd.addColorStop(1, parent.selectColor);
                                //grd.addColorStop(0, parent.selectColor);
                                //grd.addColorStop(1, parent.gradientStop);
                                //ctx.fillStyle = parent.selectColor;//"#bf3ad1";
                                ctx.fillStyle = grd;
                                //ctx.fillStyle = parent.selectColor;//"#bf3ad1";
                            }
                            else {
                                ctx.fillStyle = circleHandle.filltempcolor;
                            }
                        }
                        ctx.fill();
                        //ctx.lineWidth = 5;
                        //ctx.strokeStyle = '#003300';
                    }
                    ctx.stroke();
                    circleHandle.drawcompare = false;
                };
                this.Close = function () {
                };
            }
        }

        class arcObject {
            constructor(xmldata, parent, blockname, binary, pathindx) {
                var arcHandle = this;
                this.type = "arc";
                this.parent = parent;
                this.index = pathindx;
                //circle cx="388466.18" cy="1351977.86" r="10152.93"  fs='0' ls='0' lc="#808080" lw='0.00' wt='25' pen='8' la='3'/>
                this.blockname = blockname;
                this.blockstate = 1;
                this.entityType = xmldata.entityType;

                this.role = xmldata.role;
                this.pen = xmldata.pen;
                this.penarrindex = parent.pentable.getPenArrindex(this.pen);

                this.sweepAngle = undefined;
                this.sweepAngle = undefined;

                /*circleobject = {
                 layer : drawlayer,
                 pen : drawpen,
                 strokecolor : hexdrawcolor,
                 fillcolor : hexfillcolor,
                 linewidth : linewidth,
                 cx : cX,
                 cy : cY,
                 cr : cR,
                 filled : false,
                 };*/
                this.selected = false;
                this.mouseon = false;
                this.mouseover = false;
                this.hasArea = false;
                this.layer = xmldata.layer;
                this.layerstate = 1;
                this.drawmode = xmldata.drawmode;
                //this.filled = xmldata.filled;
                this.clockwise = xmldata.clockwise;
                this.strokecolor = xmldata.strokecolor;
                this.fillcolor = xmldata.fillcolor;
                this.stroketempcolor = xmldata.strokecolor;
                this.filltempcolor = xmldata.fillcolor;
                this.drawcompare = false;
                this.comparecolor = this.fillcolor;
                var lw = xmldata.linewidth;
                if (lw == 0) {
                    this.linewidth = 1;
                    this.originallinewidth = 1;

                }
                else {
                    this.linewidth = lw;
                    this.originallinewidth = lw;

                }
                this.cx = xmldata.ax;
                this.cy = xmldata.ay;
                this.r = xmldata.ar;
                this.sa = xmldata.asa;
                this.ea = xmldata.aea;
                this.fullcircle = Math.PI * 2;
                this.length = 0;

                var highlightdraw = false;

                this.insidePolygon = function (x, y, scalefactor, offsetx, offsety, mediax, mediay, mediah) {
                    return false;
                };

                this.iswithin = function (x, y, scalefactor, offsetx, offsety, mediax, mediay, mediah, hover) {

                    var within = false;

                    var dist = arcHandle.getdisttoArc(x, y, scalefactor, offsetx, offsety, mediax, mediay, mediah);

                    if (dist < 10){
                        within = true;
                    }

                    /*var arcpos = arcHandle.getcenterandradius(scalefactor, offsetx, offsety, mediax, mediay, mediah);



                    if(x > (arcpos.x - arcpos.r) && x < (arcpos.x + arcpos.r) ){
                        if(y > (arcpos.y - arcpos.r) && y < (arcpos.y + arcpos.r)){
                            within = true;
                        }

                    }*/

                    

                    //function PointFromArcDist(x, y, xc, yc, radius, sang, sweep){
                    /*var rect = arcHandle.getRectangle(scalefactor, offsetx, offsety, mediax, mediay, mediah);

                    if(x > rect.x && x < (rect.x + rect.w)){
                        if(y > rect.y && y < (rect.y + rect.h)){
                            within = true;
                        }
                    }*/

                    return within;



                };

                this.getArea = function(){

                    var absxscaled = arcHandle.cx;
                    var absyscaled = arcHandle.cy;
                    var absradius = arcHandle.r;

                    var minx = absxscaled - absradius;
                    var miny = absyscaled - absradius;
                    var maxx = absxscaled + absradius;
                    var maxy = absyscaled + absradius;

                    if (minx > maxx) {
                        var tempx = minx;
                        minx = maxx;
                        maxx = tempx;
                    }
                    if (miny > maxy) {
                        var tempy = miny;
                        miny = maxy;
                        maxy = tempy;
                    }
                    return {
                        area: (maxx - minx) * (maxy - miny),
                        x: minx,
                        y: miny,
                        w: maxx - minx,
                        h: maxy - miny,
                        wp: (maxx - minx) / 10,
                        hp: (maxy - miny) / 10
                    };
                    
                };

                this.getlength = function() {

                    let quartercircle = (Math.PI * 0.5);
                    let startAngle = arcHandle.fullcircle - arcHandle.sa;
                    let endAngle = arcHandle.fullcircle - arcHandle.ea;
                    let sweep;
                    
                    if (!arcHandle.clockwise) {
                        // Counter-clockwise sweep
                        sweep = (endAngle - startAngle + arcHandle.fullcircle) % (arcHandle.fullcircle);
                    } else {
                        // Clockwise sweep
                        sweep = -((startAngle - endAngle + arcHandle.fullcircle) % (arcHandle.fullcircle));
                    }

                    //return radians * (180/pi);


                    return {sweep: RadiansTodegrees(sweep), radius : arcHandle.r, length : sweep * arcHandle.r};

                    //return sweep * arcHandle.r;

                };

                this.getdisttoArc = function(x, y, scalefactor, offsetx, offsety, mediax, mediay, mediah) {
                    const absxscaled = (arcHandle.cx - mediax) * scalefactor + offsetx;
                    const absyscaled = (mediah - arcHandle.cy) * scalefactor + offsety;
                    const absradius = arcHandle.r * scalefactor;
                
                    const fullCircle = 2 * Math.PI;
                    const sa = arcHandle.sa;
                    const ea = arcHandle.ea;
                    const clockwise = arcHandle.clockwise;
                
                    // Canvas uses reversed Y axis, so angles must be flipped
                    let startAngle = fullCircle - sa;
                    let endAngle = fullCircle - ea;
                
                    // Normalize angles to [0, 2π]
                    startAngle = startAngle % fullCircle;
                    endAngle = endAngle % fullCircle;
                
                    // Calculate sweep
                    let sweep;
                    if (!clockwise) {
                        sweep = (endAngle - startAngle + fullCircle) % fullCircle;
                    } else {
                        sweep = -((startAngle - endAngle + fullCircle) % fullCircle);
                    }
                
                    // Fix for full circle cases (very small or near-2π sweeps)
                    if (Math.abs(Math.abs(sweep) - fullCircle) < 1e-4 || Math.abs(sweep) < 1e-4) {
                        sweep = clockwise ? -fullCircle : fullCircle;
                    }
                
                    // Adjust arc origin (canvas arc uses offset of 90 degrees by default)
                    const quarterCircle = 0.5 * Math.PI;
                    const dist = PointFromArcDist(
                        x,
                        y,
                        absxscaled,
                        absyscaled,
                        absradius,
                        startAngle + quarterCircle,
                        sweep
                    );
                
                    return dist;
                };

                this.getdisttoArcold = function(x, y, scalefactor, offsetx, offsety, mediax, mediay, mediah){

                    var absxscaled = (arcHandle.cx - mediax) * scalefactor;
                    var absyscaled = (mediah - arcHandle.cy) * scalefactor;
                    var absradius = arcHandle.r * scalefactor;

                    absxscaled += offsetx;
                    absyscaled += offsety;

                    //function PointFromArcDist(x, y, xc, yc, radius, sang, sweep){



                    let quartercircle = (Math.PI * 0.5);
                    let startAngle = arcHandle.fullcircle - arcHandle.sa;
                    let endAngle = arcHandle.fullcircle - arcHandle.ea;
                    
                    let sweep = arcHandle.ea - arcHandle.sa;
                    
                    if (!arcHandle.clockwise) {
                        // Counter-clockwise sweep
                        sweep = (endAngle - startAngle + arcHandle.fullcircle) % (arcHandle.fullcircle);
                    } else {
                        // Clockwise sweep
                        sweep = -((startAngle - endAngle + arcHandle.fullcircle) % (arcHandle.fullcircle));
                    }


                    

                    var dist = PointFromArcDist(x, y, absxscaled, absyscaled, absradius, startAngle + quartercircle, sweep);


                    return dist;

                };

                this.getcenterandradius = function(scalefactor, offsetx, offsety, mediax, mediay, mediah){

                    var absxscaled = (arcHandle.cx - mediax) * scalefactor;
                    var absyscaled = (mediah - arcHandle.cy) * scalefactor;
                    var absradius = arcHandle.r * scalefactor;

                    absxscaled += offsetx;
                    absyscaled += offsety;


                    return {x : absxscaled, y : absyscaled, r : absradius};
                };


                this.getRectangle = function (scalefactor, offsetx, offsety, mediax, mediay, mediah) {
                    var absxscaled = (arcHandle.cx - mediax) * scalefactor;
                    var absyscaled = (mediah - arcHandle.cy) * scalefactor;
                    var absradius = arcHandle.r * scalefactor;
                    var minx = absxscaled - absradius;
                    var miny = absyscaled - absradius;
                    var maxx = absxscaled + absradius;
                    var maxy = absyscaled + absradius;
                    minx += offsetx;
                    maxx += offsetx;
                    miny += offsety;
                    maxy += offsety;
                    if (minx > maxx) {
                        var tempx = minx;
                        minx = maxx;
                        maxx = tempx;
                    }
                    if (miny > maxy) {
                        var tempy = miny;
                        miny = maxy;
                        maxy = tempy;
                    }
                    return {
                        found: true,
                        x: minx,
                        y: miny,
                        w: maxx - minx,
                        h: maxy - miny,
                        wp: (maxx - minx) / 10,
                        hp: (maxy - miny) / 10
                    };
                };
                this.toggleselect = function () {
                    var selectstate = !arcHandle.selected;
                    arcHandle.selected = selectstate;
                };
                this.findsnapPoint = function (x, y, scalefactor, offsetx, offsety, mediax, mediay, mediah) {
                    var absxscaled = (this.cx - mediax) * scalefactor;
                    var absyscaled = (mediah - this.cy) * scalefactor;
                    absxscaled += offsetx;
                    absyscaled += offsety;
                    var snapradius = 10;
                    if (absxscaled > x - snapradius && absxscaled < x + snapradius) {
                        if (absyscaled > y - snapradius && absyscaled < y + snapradius) {
                            return {
                                found: true,
                                x: absxscaled,
                                y: absyscaled,
                                type: snapEndpoint
                            };
                        }
                    }
                    else {
                        return {
                            found: false,
                            x: 0,
                            y: 0,
                            type: snapEndpoint
                        };
                    }

                };

                this.getlinewidth = function(scalefactor, drawprint){
                    

                    var linewidth = arcHandle.linewidth;


                    if(arcHandle.parent.applypentable && arcHandle.parent.pentable.changed){

                        if(arcHandle.parent.pentable.getPendirect(arcHandle.penarrindex) != undefined){
                            arcHandle.linewidth = arcHandle.parent.pentable.getPendirect(arcHandle.penarrindex).getwidth();
                        }
                        linewidth = arcHandle.linewidth;
                        
                    }else{
                        arcHandle.linewidth = arcHandle.originallinewidth;
                        linewidth = arcHandle.linewidth;
                    }


                    if (arcHandle.linewidth * scalefactor < 1) {
                        //ctx.lineWidth = 1;
                        
                        if(arcHandle.parent.pentable.getscaled()){
                            //linewidth = scope.linewidth * scalefactor;
                            linewidth = 1;
                        }else{
                            linewidth = arcHandle.linewidth;
                        }

                        
                        //linewidth = 1;

                        if(drawprint){
                            //ctx.lineWidth = 4;    
                            linewidth = 4;
                        }
            
                    }
                    else {

                        if(arcHandle.parent.applypentable && arcHandle.parent.pentable.changed){
                            if(arcHandle.parent.pentable.getscaled()){
                                linewidth = arcHandle.linewidth * scalefactor;
                            }else{
                                linewidth = arcHandle.linewidth;
                            }
    
                        }else{
                            linewidth = arcHandle.linewidth * scalefactor;
                        }

                        //linewidth = arcHandle.linewidth * scalefactor;

                        
                        if(drawprint){
                            linewidth = 4;    
                        }

                    }

                    if(bOverridelineWidth && !arcHandle.parent.applypentable){
                        //ctx.lineWidth = 1;
                        linewidth = dOveridelinewidth;
                    }                                       

                    if(highlightdraw){

                        linewidth = nVectorhighlightwidth;
    
                    }

                    return linewidth;

                };

                this.getdrawColors = function(drawprint){

                    var linecolor = arcHandle.stroketempcolor;
                    var fillcolor = arcHandle.filltempcolor;

                    var drawcolors = {linecolor : linecolor, fillcolor : fillcolor };


                    if(arcHandle.parent.applypentable && arcHandle.parent.pentable.changed){
                        if(arcHandle.parent.pentable.getPendirect(arcHandle.penarrindex) != undefined){
                            arcHandle.stroketempcolor = arcHandle.parent.pentable.getPendirect(arcHandle.penarrindex).getcolor();
                            linecolor = arcHandle.stroketempcolor;
                        }
                        
                    }else{
                        arcHandle.stroketempcolor = arcHandle.strokecolor;
                        linecolor = arcHandle.stroketempcolor;

                    }


                    
                    if (!bKeepVectorColor) {
                        if (arcHandle.stroketempcolor == DocObj.backgroundColor) {
                            if (DocObj.backgroundColor == "#FFFFFF") {
                                arcHandle.stroketempcolor = "#000000";
                                linecolor = arcHandle.stroketempcolor;
                            }
                            if (DocObj.backgroundColor == "#000000") {
                                arcHandle.stroketempcolor = "#FFFFFF";
                                linecolor = arcHandle.stroketempcolor;
                            }
                        }
                        if (this.filltempcolor == DocObj.backgroundColor) {
                            if (DocObj.backgroundColor == "#FFFFFF") {
                                arcHandle.filltempcolor = "#000000";
                                fillcolor = arcHandle.filltempcolor;
                            }
                            if (DocObj.backgroundColor == "#000000") {
                                arcHandle.filltempcolor = "#FFFFFF";
                                fillcolor = arcHandle.filltempcolor;
                            }
                        }
                    }
                    else {
                        arcHandle.stroketempcolor = arcHandle.strokecolor;
                        arcHandle.filltempcolor = arcHandle.fillcolor;

                        linecolor = arcHandle.stroketempcolor;
                        fillcolor = arcHandle.filltempcolor;

                    }
                    if (drawprint && arcHandle.strokecolor == "#FFFFFF") {
                        arcHandle.stroketempcolor = "#000000";
                        linecolor = arcHandle.stroketempcolor;
                        fillcolor = arcHandle.filltempcolor;
                    }
                    if (drawprint && arcHandle.fillcolor == "#FFFFFF") {
                        arcHandle.filltempcolor = "#000000";
                        fillcolor = arcHandle.filltempcolor;
                    }
                    
                    if (arcHandle.drawcompare) {


                        //ctx.strokeStyle = arcHandle.comparecolor;
                        linecolor = arcHandle.comparecolor;
                    }
                    else if (arcHandle.parent.viewmode == 1) {
                        //ctx.fillStyle = "black";

                        if (DocObj.backgroundColor == "#FFFFFF") {
                            
                            //ctx.strokeStyle = "#000000";
                            linecolor = "#000000";
                        }
                        if (DocObj.backgroundColor == "#000000" || DocObj.backgroundColor == "#A0A0A0") {
                        //if (DocObj.backgroundColor == "#000000") {
                            
                            //ctx.strokeStyle = "#FFFFFF";
                            linecolor = "#FFFFFF";
                        }


                    }
                    else {
                        //ctx.strokeStyle = arcHandle.stroketempcolor;
                        linecolor = arcHandle.stroketempcolor;
                    }

                    if( arcHandle.parent.comparemode == 0 ){
                        if(arcHandle.role == 1){

                            //ctx.fillStyle = arcHandle.parent.backgroundColor;
                            fillcolor = arcHandle.parent.backgroundColor;
                            //ctx.strokeStyle = arcHandle.parent.backgroundColor;
                            linecolor = arcHandle.parent.backgroundColor;
    
                        }else if (arcHandle.role == 2){
                            //ctx.fillStyle = arcHandle.parent.overlayColor;
                            fillcolor = arcHandle.parent.overlayColor;
                            //ctx.strokeStyle = arcHandle.parent.overlayColor;
                            linecolor = arcHandle.parent.overlayColor;
                        }
    
                    }
                    
                    if(highlightdraw){

                        fillcolor = szmarkVectorColor;
                        linecolor = szmarkVectorColor;
    
                    }


                    return drawcolors = {linecolor : linecolor, fillcolor : fillcolor };
                };                     

                this.highlight = function(ctx, scalefactor, offsetx, offsety, mediax, mediay, mediah){

                    highlightdraw = true;
                    arcHandle.drawme(ctx, scalefactor, offsetx, offsety, mediax, mediay, mediah, 1, 1, false, false);
                    highlightdraw = false;

                };

               
                this.drawme = function (ctx, scalefactor, offsetx, offsety, mediax, mediay, mediah, ls, bs, drawprint, magnify) {
                    //this.drawme = function (ctx, scalefactor, offsetx, offsety, mediax, mediay, mediah,ls,bs,drawprint) {
                    var absxscaled = (arcHandle.cx - mediax) * scalefactor;
                    var absyscaled = (mediah - arcHandle.cy) * scalefactor;
                    var absradius = arcHandle.r * scalefactor;
                    if (ls == 0 || bs == 0) {
                        return;
                    }

                    if(!DocObj){
                        return;
                    }

                    


                    absxscaled += offsetx;
                    absyscaled += offsety;
                    //if (!arcHandle.parent.backgroundrender){
                    if ((!arcHandle.parent.backgroundrender || magnify) && !drawprint) {
                        var cnvwidth = canvasowidth;
                        var cnvheight = canvasoheight;
                        if (magnify) {
                            cnvwidth = magnifysize.width;
                            cnvheight = magnifysize.height;
                        }
                        if (absxscaled < 0 && absxscaled + absradius < 0) {
                            return;
                        }
                        if (absyscaled < 0 && absyscaled + absradius < 0) {
                            return;
                        }
                        if (absxscaled > cnvwidth && absxscaled - absradius > cnvwidth) {
                            //if (absxscaled > canvasowidth && absxscaled - absradius > canvasowidth) {
                            return;
                        }
                        if (absyscaled > cnvheight && absyscaled - absradius > cnvheight) {
                            //if (absyscaled > canvasoheight && absyscaled - absradius > canvasoheight) {
                            return;
                        }
                    }


                    /*if (!bKeepVectorColor) {
                        if (arcHandle.stroketempcolor == DocObj.backgroundColor) {
                            if (DocObj.backgroundColor == "#FFFFFF") {
                                arcHandle.stroketempcolor = "#000000";
                            }
                            if (DocObj.backgroundColor == "#000000") {
                                arcHandle.stroketempcolor = "#FFFFFF";
                            }
                        }
                        if (this.filltempcolor == DocObj.backgroundColor) {
                            if (DocObj.backgroundColor == "#FFFFFF") {
                                arcHandle.filltempcolor = "#000000";
                            }
                            if (DocObj.backgroundColor == "#000000") {
                                arcHandle.filltempcolor = "#FFFFFF";
                            }
                        }
                    }
                    else {
                        arcHandle.stroketempcolor = arcHandle.strokecolor;
                        arcHandle.filltempcolor = arcHandle.fillcolor;
                    }
                    if (drawprint && arcHandle.strokecolor == "#FFFFFF") {
                        arcHandle.stroketempcolor = "#000000";
                    }
                    if (drawprint && arcHandle.fillcolor == "#FFFFFF") {
                        arcHandle.filltempcolor = "#000000";
                    }*/

                    var drawcolors = arcHandle.getdrawColors(drawprint);
                    ctx.fillStyle = drawcolors.fillcolor;
                    ctx.strokeStyle = drawcolors.linecolor;


                    ctx.lineCap = 'round';
                    
                    /*if (arcHandle.drawcompare) {
                        ctx.strokeStyle = arcHandle.comparecolor;
                    }
                    else if (arcHandle.parent.viewmode == 1) {
                        //ctx.fillStyle = "black";

                        if (DocObj.backgroundColor == "#FFFFFF") {
                            
                            ctx.strokeStyle = "#000000";
                        }
                        if (DocObj.backgroundColor == "#000000") {
                            
                            ctx.strokeStyle = "#FFFFFF";
                        }


                    }
                    else {
                        ctx.strokeStyle = arcHandle.stroketempcolor;
                    }

                    if( arcHandle.parent.comparemode == 0 ){
                        if(arcHandle.role == 1){

                            ctx.fillStyle = arcHandle.parent.backgroundColor;
                            ctx.strokeStyle = arcHandle.parent.backgroundColor;
    
                        }else if (arcHandle.role == 2){
                            ctx.fillStyle = arcHandle.parent.overlayColor;
                            ctx.strokeStyle = arcHandle.parent.overlayColor;
                        }
    
                    }*/


                    /*if (arcHandle.linewidth * scalefactor < 1) {
                        ctx.lineWidth = 1;

                        if(drawprint){
                            ctx.lineWidth = 4;    
                        }
            
                    }
                    else {
                        ctx.lineWidth = arcHandle.linewidth * scalefactor;
                        if(drawprint){
                            ctx.lineWidth = 4;    
                        }

                    }

                    if(bOverridelineWidth){
                        ctx.lineWidth = 1;
                    }*/

                    ctx.lineWidth = arcHandle.getlinewidth(scalefactor, drawprint);

                    //ctx.save();
                    ctx.beginPath();
                    //ctx.translate(absxscaled, absyscaled);
                    //ctx.scale(1,-1);
                    ctx.arc(absxscaled, absyscaled, absradius, arcHandle.fullcircle - arcHandle.sa, arcHandle.fullcircle - arcHandle.ea, arcHandle.clockwise);
                    /*if (this.filled == 1 && arcHandle.parent.viewmode == 0) {
                        if (arcHandle.drawcompare) {
                            ctx.fillStyle = arcHandle.comparecolor;
        
                        }else{
                            if(parent.blocklist[arcHandle.blockname].overridecolor){
                                ctx.fillStyle = parent.blocklist[arcHandle.blockname].color;
                            }else if (arcHandle.selected) {
                                ctx.fillStyle = parent.selectColor;//"#bf3ad1";
                            }else{
                                ctx.fillStyle = arcHandle.filltempcolor;
                            }
        
                        }
        
        
                        ctx.fill();
                        //ctx.lineWidth = 5;
                        //ctx.strokeStyle = '#003300';
        
                    }*/
                    ctx.stroke();
                    //ctx.restore();
                    
                    arcHandle.drawcompare = false;
                };
                this.Close = function () {
                };
            }
        }

        class subPathObject {
            constructor(points, parentpath, last, precision, binary) {
                var pathHandle = this;
                this.type = "subpath";
                this.precision = precision;
                this.parentpath = parentpath;
                this.blockname = parentpath.blockname;
                this.blockstate = parentpath.blockstate;
                this.layer = parentpath.layer;
                this.layerstate = parentpath.layerstate;
                this.drawmode = parentpath.drawmode;
                this.strokecolor = parentpath.strokecolor;
                this.fillcolor = parentpath.fillcolor;
                this.stroketempcolor = parentpath.strokecolor;
                this.filltempcolor = parentpath.fillcolor;
                this.filled = parentpath.filled;
                this.selected = false;
                this.pen = parentpath.pen;
                this.penarrindex = parentpath.penarrindex;
                this.linewidth = parentpath.linewidth;
                this.originallinewidth = parentpath.originallinewidth;
                
                this.linestyle = parentpath.linestyle;

                this.role = parentpath.role;

                this.svgpathstr = '';
                if (pathHandle.precision == 0) {
                    this.points = new Float32Array(points.length);
                }
                else if (pathHandle.precision == 1) {
                    this.points = new Float64Array(points.length);
                }
                var numpoints = points.length;
                this.last = last;
                var pctr = 0;
                while (pctr < numpoints) {
                    this.points[pctr] = points[pctr];
                    pctr++;
                }
                this.findsnapPoint = function (x, y, scalefactor, offsetx, offsety, mediax, mediay, mediah) {
                    var absxscaled = (this.points[0] - mediax) * scalefactor;
                    var absyscaled = (mediah - this.points[1]) * scalefactor;
                    var relxscaled = 0;
                    var relyscaled = 0;
                    absxscaled += offsetx;
                    absyscaled += offsety;
                    var snapradius = 10;
                    if (absxscaled > x - snapradius && absxscaled < x + snapradius) {
                        if (absyscaled > y - snapradius && absyscaled < y + snapradius) {
                            return {
                                found: true,
                                x: absxscaled,
                                y: absyscaled,
                                type: snapEndpoint
                            };
                        }
                    }
                    for (var counter = 2; counter < this.points.length; counter += 2) {
                        relxscaled = ((this.points[counter] - mediax) * scalefactor);
                        relyscaled = ((mediah - this.points[counter + 1]) * scalefactor);
                        relxscaled += offsetx;
                        relyscaled += offsety;
                        if (relxscaled > x - snapradius && relxscaled < x + snapradius) {
                            if (relyscaled > y - snapradius && relyscaled < y + snapradius) {
                                return {
                                    found: true,
                                    x: relxscaled,
                                    y: relyscaled,
                                    type: snapEndpoint
                                };
                            }
                        }
                    }
                    return {
                        found: false,
                        x: 0,
                        y: 0,
                        type: snapEndpoint
                    };
                };
                this.getlinewidth = function(scalefactor, drawprint){
                    

                    var linewidth = pathHandle.linewidth;


                    if(pathHandle.parentpath.parent.applypentable && pathHandle.parent.pentable.changed){

                        if(pathHandle.parentpath.parent.pentable.getPendirect(pathHandle.penarrindex) != undefined){
                            pathHandle.linewidth = pathHandle.parentpath.parent.pentable.getPendirect(pathHandle.penarrindex).getwidth();
                        }
                        linewidth = pathHandle.linewidth;
                        
                    }else{
                        pathHandle.linewidth = pathHandle.originallinewidth;
                        linewidth = pathHandle.linewidth;
                    }

                    if(!pathHandle.linewidth){
                        //ctx.lineWidth = 1;
                        linewidth = 1;
                    }

                    if (pathHandle.linewidth * scalefactor < 1 && pathHandle.filled == 0) {
                        //ctx.lineWidth = 1;
                        
                        if(pathHandle.parent.pentable.getscaled()){
                            //linewidth = scope.linewidth * scalefactor;
                            linewidth = 1;
                        }else{
                            linewidth = pathHandle.linewidth;
                        }

                        
                        //linewidth = 1;

                        if(drawprint){
                            //ctx.lineWidth = 4;    
                            linewidth = 4;
                        }
            

                    }
                    else {
                        //ctx.lineWidth = pathHandle.linewidth * scalefactor;


                        if(pathHandle.parentpath.parent.applypentable && pathHandle.parentpath.parent.pentable.changed){
                            if(pathHandle.parentpath.parent.pentable.getscaled()){
                                linewidth = pathHandle.linewidth * scalefactor;
                            }else{
                                linewidth = pathHandle.linewidth;
                            }
    
                        }else{
                            linewidth = pathHandle.linewidth * scalefactor;
                        }

                        //linewidth = pathHandle.linewidth * scalefactor;

                        if(drawprint){
                            //ctx.lineWidth = 4;    
                            linewidth = 4;
                        }

                        //var linew = (0.5 + (this.linewidth * scalefactor)) | 0;
                        //ctx.lineWidth = linew;
                    }

                    if(bOverridelineWidth && !pathHandle.parentpath.parent.applypentable){
                        //ctx.lineWidth = 1;
                        linewidth = dOveridelinewidth;
                    }
                                  

                    return linewidth;

                };

                this.getdrawColors = function(drawprint){

                    var linecolor = pathHandle.stroketempcolor;
                    var fillcolor = pathHandle.filltempcolor;

                    var drawcolors = {linecolor : linecolor, fillcolor : fillcolor };

                    if (pathHandle.filled == 0){
                        if(pathHandle.parentpath.parent.applypentable && pathHandle.parent.pentable.changed){

                            if(pathHandle.parentpath.parent.pentable.getPendirect(pathHandle.penarrindex) != undefined){
                                pathHandle.stroketempcolor = pathHandle.parentpath.parent.pentable.getPendirect(pathHandle.penarrindex).getcolor();
                                linecolor = pathHandle.stroketempcolor;
                            }
                            
                        }else{
                            
                            pathHandle.stroketempcolor = pathHandle.strokecolor;
                            linecolor = pathHandle.stroketempcolor;
    
                        }
    
                    }

                    
                    if (!bKeepVectorColor) {
                        if (pathHandle.stroketempcolor == DocObj.backgroundColor) {
                            if (DocObj.backgroundColor == "#FFFFFF") {
                                pathHandle.stroketempcolor = "#000000";
                                pathHandle.filltempcolor = "#000000";
                                
                                linecolor = pathHandle.stroketempcolor;
                                fillcolor = pathHandle.filltempcolor;
            
                            }
                            if (DocObj.backgroundColor == "#000000") {
                                pathHandle.stroketempcolor = "#FFFFFF";
                                pathHandle.filltempcolor = "#FFFFFF";

                                linecolor = pathHandle.stroketempcolor;
                                fillcolor = pathHandle.filltempcolor;

                            }
                        }
                    }
                    else {
                        pathHandle.stroketempcolor = pathHandle.strokecolor;
                        pathHandle.filltempcolor = pathHandle.fillcolor;

                        linecolor = pathHandle.stroketempcolor;
                        fillcolor = pathHandle.filltempcolor;

                    }
                    if (drawprint && pathHandle.stroketempcolor == "#FFFFFF") {
                        pathHandle.stroketempcolor = "#000000";
                        pathHandle.filltempcolor = "#000000";

                        linecolor = pathHandle.stroketempcolor;
                        fillcolor = pathHandle.filltempcolor;

                    }
                    if (pathHandle.drawcompare) {
                        //ctx.strokeStyle = pathHandle.comparecolor;

                        linecolor = pathHandle.comparecolor;
                        //fillcolor = pathHandle.filltempcolor;



                    }
                    else if (pathHandle.parentpath.viewmode == 1) {
                        
                        if (DocObj.backgroundColor == "#FFFFFF") {
                            //pathHandle.filltempcolor = "#000000";
                            //ctx.strokeStyle = "#000000";
                            //ctx.fillStyle = "#000000";
                            pathHandle.filltempcolor = "#000000";

                            linecolor = "#000000";
                            fillcolor = "#000000";
    

                        }
                        if (DocObj.backgroundColor == "#000000") {
                            //pathHandle.filltempcolor = "#FFFFFF";
                            //ctx.strokeStyle = "#FFFFFF";
                            //ctx.fillStyle = "#FFFFFF";

                            pathHandle.filltempcolor = "#FFFFFF";

                            linecolor = "#FFFFFF";
                            fillcolor = "#FFFFFF";

                        }
                        //ctx.strokeStyle = "black";

                    }
                    else {
                        //ctx.strokeStyle = pathHandle.stroketempcolor;

                        linecolor = pathHandle.stroketempcolor;
                        //fillcolor = pathHandle.filltempcolor;

                    }

                    if( pathHandle.parentpath.parent.comparemode == 0 ){
                        if(pathHandle.role == 1){

                            //ctx.fillStyle = pathHandle.parentpath.parent.backgroundColor;
                            //ctx.strokeStyle = pathHandle.parentpath.parent.backgroundColor;

                            linecolor = pathHandle.parentpath.parent.backgroundColor;
                            fillcolor = pathHandle.parentpath.parent.backgroundColor;
    
    
                        }else if (pathHandle.role == 2){
                            //ctx.fillStyle = pathHandle.parentpath.parent.overlayColor;
                            //ctx.strokeStyle = pathHandle.parentpath.parent.overlayColor;

                            linecolor = pathHandle.parentpath.parent.overlayColor;
                            fillcolor = pathHandle.parentpath.parent.overlayColor;

                        }
    
                    }
                    

                    return drawcolors = {linecolor : linecolor, fillcolor : fillcolor };
                };                     



                this.drawme = function (ctx, scalefactor, offsetx, offsety, mediax, mediay, mediah, ls, bs, drawprint, magnify) {
                    //this.drawme = function (ctx, scalefactor, offsetx, offsety, mediax, mediay, mediah,ls,bs,drawprint) {
                    



                    
                    var absxscaled = (pathHandle.points[0] - mediax) * scalefactor;
                    var absyscaled = (mediah - pathHandle.points[1]) * scalefactor;
                    var relxscaled = 0;
                    var relyscaled = 0;
                    if (ls == 0 || bs == 0) {
                        return;
                    }
                    if(!DocObj){
                        return;
                    }

                    




                    absxscaled += offsetx;
                    absyscaled += offsety;
                    if (!pathHandle.parentpath.render) {
                        return;
                    }
                    //ctx.save();
                    //ctx.imageSmoothingEnabled = false;
                    //ctx.globalCompositeOperation = 'destination-out';



                    /*if (!bKeepVectorColor) {
                        if (pathHandle.stroketempcolor == DocObj.backgroundColor) {
                            if (DocObj.backgroundColor == "#FFFFFF") {
                                pathHandle.stroketempcolor = "#000000";
                                pathHandle.filltempcolor = "#000000";
                            }
                            if (DocObj.backgroundColor == "#000000") {
                                pathHandle.stroketempcolor = "#FFFFFF";
                                pathHandle.filltempcolor = "#FFFFFF";
                            }
                        }
                    }
                    else {
                        this.stroketempcolor = pathHandle.strokecolor;
                        this.filltempcolor = pathHandle.fillcolor;
                    }
                    if (drawprint && pathHandle.stroketempcolor == "#FFFFFF") {
                        pathHandle.stroketempcolor = "#000000";
                        pathHandle.filltempcolor = "#000000";
                    }*/
                    /*if (this.drawcompare) {
                        ctx.strokeStyle = pathHandle.comparecolor;
                    }
                    else if (pathHandle.parentpath.viewmode == 1) {
                        
                        if (DocObj.backgroundColor == "#FFFFFF") {
                            //pathHandle.filltempcolor = "#000000";
                            ctx.strokeStyle = "#000000";
                            ctx.fillStyle = "#000000";
                            pathHandle.filltempcolor = "#000000";

                        }
                        if (DocObj.backgroundColor == "#000000") {
                            //pathHandle.filltempcolor = "#FFFFFF";
                            ctx.strokeStyle = "#FFFFFF";
                            ctx.fillStyle = "#FFFFFF";
                            pathHandle.filltempcolor = "#FFFFFF";
                        }
                        //ctx.strokeStyle = "black";

                    }
                    else {
                        ctx.strokeStyle = pathHandle.stroketempcolor;
                    }*/

                    /*if( pathHandle.parentpath.parent.comparemode == 0 ){
                        if(pathHandle.role == 1){

                            ctx.fillStyle = pathHandle.parentpath.parent.backgroundColor;
                            ctx.strokeStyle = pathHandle.parentpath.parent.backgroundColor;
    
                        }else if (pathHandle.role == 2){
                            ctx.fillStyle = pathHandle.parentpath.parent.overlayColor;
                            ctx.strokeStyle = pathHandle.parentpath.parent.overlayColor;
                        }
    
                    }*/

                    //var drawcolors = pathHandle.parentpath.getdrawColors(drawprint);

                    //ctx.fillStyle = drawcolors.fillcolor;
                    //ctx.strokeStyle = drawcolors.linecolor;



                    //console.log('subpath');
                    //console.log(this.filled);
                    //ctx.lineWidth = this.linewidth * scalefactor;

                    /*if(!pathHandle.linewidth){
                        ctx.lineWidth = 1;
                    }

                    if (pathHandle.linewidth * scalefactor < 1 && pathHandle.filled == 0) {
                        ctx.lineWidth = 1;

                        if(drawprint){
                            ctx.lineWidth = 4;    
                        }
            

                    }
                    else {
                        ctx.lineWidth = pathHandle.linewidth * scalefactor;

                        if(drawprint){
                            ctx.lineWidth = 4;    
                        }

                        //var linew = (0.5 + (this.linewidth * scalefactor)) | 0;
                        //ctx.lineWidth = linew;
                    }

                    if(bOverridelineWidth){
                        ctx.lineWidth = 1;
                    }*/

                    ctx.lineWidth = pathHandle.getlinewidth(scalefactor,drawprint);

                    //ctx.lineWidth = this.linewidth * scalefactor;
                    //ctx.beginPath();
                    //      ctx.moveTo(markupobject.points[0].x, markupobject.points[0].y);

                    absxscaled = pxround(absxscaled);
                    absyscaled = pxround(absyscaled);


                    ctx.moveTo(absxscaled, absyscaled); //round off test for better performance
                    //ctx.moveTo(((0.5 + absxscaled) | 0), ((0.5 + absyscaled) | 0));
                    /* Andriy: unused variable var count = 0; */
                    for (var counter = 2; counter < this.points.length; counter += 2) {
                        //            ctx.lineTo(markupobject.points[counter].x, markupobject.points[counter].y);
                        relxscaled = ((pathHandle.points[counter] - mediax) * scalefactor);
                        relyscaled = ((mediah - pathHandle.points[counter + 1]) * scalefactor);


    

                        relxscaled += offsetx;
                        relyscaled += offsety;
                        //xscaled = xscaled + offsetx;
                        //yscaled = yscaled + offsety;

                        relxscaled = pxround(relxscaled);
                        relyscaled = pxround(relyscaled);

                        ctx.lineTo(relxscaled, relyscaled); //round off test for better performance
                        //ctx.lineTo(((0.5 + relxscaled) | 0), ((0.5 + relyscaled) | 0));
                    }
                    ctx.closePath();
                    //ctx.lineCap = 'round';
                    /*if (pathHandle.last) {

                        var blockoverridecolor = false;
                        var blockoverridefill = false;

                        if(parentpath.parent.blocklist[pathHandle.blockname] != undefined){
                            blockoverridecolor = parentpath.parent.blocklist[pathHandle.blockname].overridecolor;
                            blockoverridefill =parentpath.parent.blocklist[pathHandle.blockname].overridefill;
                        }
                        

                        if ((pathHandle.filled == 1) || pathHandle.selected || blockoverridecolor || blockoverridefill ) {


                            if (pathHandle.drawcompare) {
                                ctx.fillStyle = pathHandle.comparecolor;
                            }
                            else {

                                if (pathHandle.selected) {
                                    var minx = (parentpath.x - mediax) * scalefactor;
                                    var miny = (mediah - parentpath.y) * scalefactor;
                                    var maxx = (parentpath.w - mediax) * scalefactor;
                                    var maxy = (mediah - parentpath.h) * scalefactor;
                                    //var grd = ctx.createLinearGradient(minx, miny, maxx, maxy);
                                    var grd = ctx.createLinearGradient(maxx, miny, minx, maxy);
                                    grd.addColorStop(0, parentpath.parent.selectColor);
                                    grd.addColorStop(.5, parentpath.parent.gradientStop);
                                    grd.addColorStop(1, parentpath.parent.selectColor);
                                    //grd.addColorStop(0, parent.selectColor);
                                    //grd.addColorStop(1, parent.gradientStop);
                                    //ctx.fillStyle = parent.selectColor;//"#bf3ad1";
                                    ctx.fillStyle = grd;
                                    //ctx.fillStyle = parentpath.parent.selectColor;
                                }else{
                                    
                                    if(parentpath.parent.blocklist[pathHandle.blockname] != undefined){
                                        if (parentpath.parent.blocklist[pathHandle.blockname].overridecolor) {
                                            ctx.fillStyle = parentpath.parent.blocklist[pathHandle.blockname].color;
                                        }
                                        else if(parentpath.parent.blocklist[pathHandle.blockname].overridefill && parentpath.parent.blocklist[pathHandle.blockname].fillstyle != null){
        
                                            if(parentpath.parent.blocklist[pathHandle.blockname].fillstyle == 3 && parentpath.parent.blocklist[pathHandle.blockname].hatchstyle != null){
                                                
                                                var ptrn = getHatchbyName(ctx, parentpath.parent.blocklist[pathHandle.blockname].hatchstyle);
    
                                                if (ptrn != null){
                                                    ctx.fillStyle = ptrn;
                                                }
    
                                                //ctx.fillStyle = getHatchbyName(ctx, parentpath.parent.blocklist[pathHandle.blockname].hatchstyle);
                                                
                                            }
        
                                            //ctx.fillStyle = parentpath.parent.blocklist[pathHandle.blockname].fillstyle;
        
                                        }
                                        else {
                                            ctx.fillStyle = pathHandle.filltempcolor;
                                        }
                                    }

    
                                }

                            }

                            if( pathHandle.parentpath.parent.comparemode == 0 ){
                                if(pathHandle.role == 1){

                                    ctx.fillStyle = pathHandle.parentpath.parent.backgroundColor;
                                    //ctx.strokeStyle = pathHandle.parentpath.parent.backgroundColor;
            
                                }else if (pathHandle.role == 2){
                                    ctx.fillStyle = pathHandle.parentpath.parent.overlayColor;
                                    //ctx.strokeStyle = pathHandle.parentpath.parent.overlayColor;
                                }
    
                            }
        

                            ctx.fill();
                        }
                        else {
                            //ctx.closePath();
                            //ctx.stroke();
                            //ctx.fillStyle = this.fillcolor;
                            //ctx.fill();
                        }
                        //ctx.closePath();
                        ctx.stroke();
                    }*/
                    //ctx.clip();
                    //ctx.restore();
                    this.drawcompare = false;
                };
                this.Close = function () {
                    this.points = null;
                };
            }
        }

        class pathObject {
            constructor(xmldata, parent, blockname, binary, pathindx) {
                var pathHandle = this;
                this.type = "path";
                this.precision = xmldata.precision; //0 = float32, 1=float64
                this.parent = parent;
                this.index = pathindx;
                this.x = 0.0;
                this.y = 0.0;
                this.w = 0.0;
                this.h = 0.0;
                this.centroid = { x: 0, y: 0 };
                this.render = true;
                this.selected = false;
                this.hasArea = true;
                this.Area = undefined;
                this.Length = undefined;
                this.svgpathstr = '';
                this.drawcompare = false;
                this.comparecolor = this.fillcolor;
                this.shape = xmldata.shape;
                this.blockname = blockname;
                this.blockstate = 1;
                this.closed = false;
                this.layer = xmldata.layer;
                this.layerstate = 1;
                this.drawmode = xmldata.drawmode;

                this.entityType = xmldata.entityType;

                this.pen = xmldata.pen;
                this.penarrindex = parent.pentable.getPenArrindex(this.pen);
                this.role = xmldata.role;

                this.overridefill = false;
                this.drawoverride = false;


                this.strokecolor = xmldata.strokecolor;
                this.fillcolor = xmldata.fillcolor;
                this.stroketempcolor = xmldata.strokecolor;
                this.filltempcolor = xmldata.fillcolor;
                this.filled = xmldata.filled;
                this.gotsubpath = xmldata.gotsubpath;
                this.subpaths = [];
                this.linestyle = 0;
                this.mouseover = false;
                //this.mouseon = false;


                this.linewidth = xmldata.linewidth;
                this.originallinewidth = xmldata.linewidth;


                if (xmldata.linewidth == 0) {
                    this.linewidth = 1;
                    this.originallinewidth = 1;
                }
                
                if (xmldata.gotsubpath) {
                    this.gotsubpath = true;
                    if (this.precision == 0) {
                        this.points = new Float32Array(xmldata.numpoints);
                    }
                    else if (this.precision == 1) {
                        this.points = new Float64Array(xmldata.numpoints);
                    }
                    var cpnts = 0;
                    var numpoints = xmldata.numpoints;
                    var mainarray = xmldata.points[0];
                    while (cpnts < numpoints) {
                        this.points[cpnts] = mainarray[cpnts];
                        cpnts++;
                    }
                    /*pathobject = {
                     layer : drawlayer,
                     pen : drawpen,
                     strokecolor : hexdrawcolor,
                     fillcolor : hexfillcolor,
                     linewidth : linewidth,
                     numpoints : numpointarr[0],
                     points : subpatharrays,
                     filled : true,
                     gotsubpath : true
                     };*/
                    var numsubpaths = xmldata.points.length;
                    var spc = 1;
                    var islasthole = false;
                    while (spc < numsubpaths) {
                        mainarray = xmldata.points[spc];
                        if (spc == numsubpaths - 1) {
                            islasthole = true;
                        }
                        spc++;
                        this.subpaths.push(new subPathObject(mainarray, pathHandle, islasthole, pathHandle.precision, true));
                    }
                }
                else {
                    if (this.precision == 0) {
                        this.points = new Float32Array(xmldata.numpoints);
                    }
                    else if (this.precision == 1) {
                        this.points = new Float64Array(xmldata.numpoints);
                    }
                    //this.points = new Float32Array(xmldata.numpoints);
                    //this.points[0] = xmldata.points[0];
                    //this.points[1] = xmldata.points[1];
                    cpnts = 0;
                    numpoints = xmldata.numpoints;
                    while (cpnts < numpoints) {
                        this.points[cpnts] = xmldata.points[cpnts];
                        cpnts++;
                    }
                }
                if (this.shape == 2) {
                    if (this.points[0] == this.points[this.points.length - 2] && this.points[1] == this.points[this.points.length - 1]) {
                        this.closed = true;
                    }
                }
                /*                    var pathobject = {
                 layer : drawlayer,
                 pen : drawpen,
                 strokecolor : hexdrawcolor,
                 fillcolor : hexfillcolor,
                 linewidth : linewidth,
                 numpoints : numpoints,
                 points : lpoints,
                 filled : false,
                 gotsubpath : false
                 };
                 */
                //fs='0' ls='0' lc="#FFFFFF" lw='0.00' wt='25' pen='7' la='8'

                var highlightdraw = false;

                //scope.pathlist[i].mouseoff();
                //scope.pathlist[i].mouseon();

                this.mouseoff = function() {
                    pathHandle.mouseover = false;
                };

                this.mouseon = function() {
                    pathHandle.mouseover = true;
                };


                this.toggleselect = function () {
                    var selectstate = !pathHandle.selected;
                    pathHandle.selected = selectstate;
                    for (var scount = 0; scount < pathHandle.subpaths.length; scount++) {
                        pathHandle.subpaths[scount].selected = selectstate;
                    }
                };
                this.unselect = function () {
                    pathHandle.selected = false;
                    for (var scount = 0; scount < pathHandle.subpaths.length; scount++) {
                        pathHandle.subpaths[scount].selected = false;
                    }
                };
                this.select = function () {
                    pathHandle.selected = true;
                    for (var scount = 0; scount < pathHandle.subpaths.length; scount++) {
                        pathHandle.subpaths[scount].selected = true;
                    }
                };
                this.findcentroid = function () {
                    var pts = [];
                    var pointsctr = 0;
                    for (var counter = 0; counter < this.points.length; counter += 2) {
                        pts[pointsctr] = { x: this.points[counter], y: this.points[counter + 1] };
                        pointsctr++;
                    }
                    if (this.gotsubpath) {
                        for (var scount = 0; scount < pathHandle.subpaths.length; scount++) {
                            //pathHandle.subpaths[scount].selected = false;
                            for (counter = 0; counter < pathHandle.subpaths[scount].points.length; counter += 2) {
                                pts[pointsctr] = { x: pathHandle.subpaths[scount].points[counter], y: pathHandle.subpaths[scount].points[counter + 1] };
                                pointsctr++;
                            }
                        }
                    }
                    pathHandle.centroid = get_polygon_centroid(pts);
                };
                this.getcentroid = function (scalefactor, offsetx, offsety, mediax, mediay, mediah) {
                    if (pathHandle.centroid.x == 0 && pathHandle.centroid.y == 0) {
                        pathHandle.findcentroid();
                    }
                    var centrx = (pathHandle.centroid.x - mediax) * scalefactor;
                    var centry = (mediah - pathHandle.centroid.y) * scalefactor;
                    centrx += offsetx;
                    centry += offsety;
                    return { x: centrx, y: centry };
                };
                this.findrectangle = function () {
                    /* Andriy: variable initialization is reduntant as it's set to 2 in the first interation of the loop */
                    /*var counter = 0;*/
                    var minx = this.points[0];
                    var miny = this.points[1];
                    var maxx = this.points[0];
                    var maxy = this.points[1];
                    for (var counter = 2; counter < this.points.length; counter += 2) {
                        var relxscaled = this.points[counter];
                        var relyscaled = this.points[counter + 1];
                        if (relxscaled < minx) {
                            minx = relxscaled;
                        }
                        if (relyscaled < miny) {
                            miny = relyscaled;
                        }
                        if (relxscaled > maxx) {
                            maxx = relxscaled;
                        }
                        if (relyscaled > maxy) {
                            maxy = relyscaled;
                        }
                    }
                    if (this.gotsubpath) {
                        for (var scount = 0; scount < pathHandle.subpaths.length; scount++) {
                            //pathHandle.subpaths[scount].selected = false;
                            for (counter = 0; counter < pathHandle.subpaths[scount].points.length; counter += 2) {
                                relxscaled = pathHandle.subpaths[scount].points[counter];
                                relyscaled = pathHandle.subpaths[scount].points[counter + 1];
                                if (relxscaled < minx) {
                                    minx = relxscaled;
                                }
                                if (relyscaled < miny) {
                                    miny = relyscaled;
                                }
                                if (relxscaled > maxx) {
                                    maxx = relxscaled;
                                }
                                if (relyscaled > maxy) {
                                    maxy = relyscaled;
                                }
                            }
                        }
                    }
                    this.x = minx;
                    this.y = miny;
                    this.w = maxx;
                    this.h = maxy;
                };
                this.getblockPath = function () {
                    return pathHandle;
                };
                this.getblockPathPoints = function () {
                    return pathHandle.points;
                };
                this.getRectangle = function (scalefactor, offsetx, offsety, mediax, mediay, mediah) {
                    var minx = (pathHandle.x - mediax) * scalefactor;
                    var miny = (mediah - pathHandle.y) * scalefactor;
                    var maxx = (pathHandle.w - mediax) * scalefactor;
                    var maxy = (mediah - pathHandle.h) * scalefactor;
                    minx += offsetx;
                    maxx += offsetx;
                    miny += offsety;
                    maxy += offsety;
                    if (minx > maxx) {
                        var tempx = minx;
                        minx = maxx;
                        maxx = tempx;
                    }
                    if (miny > maxy) {
                        var tempy = miny;
                        miny = maxy;
                        maxy = tempy;
                    }
                    return {
                        found: true,
                        x: minx,
                        y: miny,
                        w: maxx - minx,
                        h: maxy - miny,
                        wp: (maxx - minx) / 10,
                        hp: (maxy - miny) / 10
                    };
                };
                this.getpath = function (x, y, scalefactor, offsetx, offsety, mediax, mediay, mediah) {
                    if (pathHandle.insidePolygon(x, y, scalefactor, offsetx, offsety, mediax, mediay, mediah)) {
                        return pathHandle;
                    }
                    else {
                        return null;
                    }
                };
                this.getpathPoints = function (x, y, scalefactor, offsetx, offsety, mediax, mediay, mediah) {
                    if (pathHandle.insidePolygon(x, y, scalefactor, offsetx, offsety, mediax, mediay, mediah)) {
                        return pathHandle.points;
                        /*if (!pathHandle.gotsubpath) {
                            
                        }else{
                            for (var scount = 0; scount < pathHandle.subpaths.length; scount++) {
                                if (pathHandle.drawcompare) {
                                    pathHandle.subpaths[scount].drawemecompare(ctx, scalefactor, offsetx, offsety, mediax, mediay, mediah, pathHandle.comparecolor,ls,bs);
                                    //this.drawemecompare = function(ctx,scalefactor,offsetx,offsety, mediax, mediay,mediah,color){
                                } else {
            
                                    pathHandle.subpaths[scount].drawme(ctx, scalefactor, offsetx, offsety, mediax, mediay, mediah,ls,bs,drawprint);
                                }
            
                            }
            
                        }*/
                    }
                    else {
                        return [];
                    }
                };

                this.iswithin = function (x, y, scalefactor, offsetx, offsety, mediax, mediay, mediah, hover) {

                    var within = false;

                    var mp = {
                        x: x,
                        y: y
                    };
                    var minx = (pathHandle.x - mediax) * scalefactor;
                    var miny = (mediah - pathHandle.y) * scalefactor;
                    var maxx = (pathHandle.w - mediax) * scalefactor;
                    var maxy = (mediah - pathHandle.h) * scalefactor;
                    minx += offsetx;
                    maxx += offsetx;
                    miny += offsety;
                    maxy += offsety;
                    if (minx > maxx) {
                        var tempx = minx;
                        minx = maxx;
                        maxx = tempx;
                    }
                    if (miny > maxy) {
                        var tempy = miny;
                        miny = maxy;
                        maxy = tempy;
                    }

                    var cnvwidth = canvasowidth;
                    var cnvheight = canvasoheight;

                    if ((minx < 0 && maxx < 0) || (miny < 0 && maxy < 0)) {
                        
                        return false;
                    }
                    //if ((minx > canvasowidth && maxx > canvasowidth) || (miny > canvasowidth && maxy > canvasoheight)) {
                    if ((minx > cnvwidth && maxx > cnvwidth) || (miny > cnvheight && maxy > cnvheight)) {
                        
                        return false;
                    }

                    if ((mp.x < minx || mp.x > maxx) || (mp.y < miny || mp.y > maxy)) {
                        
                        within = false;
                        

                    }else{

                        var epsilon = {
                            x: minx - ((maxx - minx) / 100),
                            y: maxy - ((maxy - miny) / 2)
                        };

                        within = insidepolygonpoints(scalefactor, offsetx, offsety, mediax, mediay, mediah, pathHandle, mp, epsilon);
    
                    }

                    return within;



                };

                this.insidePolygon = function (x, y, scalefactor, offsetx, offsety, mediax, mediay, mediah, hover) {
                    var polyshape = (pathHandle.shape == 3 || pathHandle.shape == 6 || pathHandle.shape == 23 || pathHandle.shape == 26);
                    var closedpoly = (pathHandle.shape == 2 && pathHandle.closed);
                    if (pathHandle.shape == 2) {
                        //console.log('polyline');
                    }
                    if (!polyshape && !closedpoly) {
                        pathHandle.unselect();
                        return false;
                    }
                    /*if(pathHandle.shape != 3 && pathHandle.shape != 6 && pathHandle.shape != 23 && pathHandle.shape != 26){
                        pathHandle.unselect();
                        return false;
                    }*/
                    var mp = {
                        x: x,
                        y: y
                    };
                    var minx = (pathHandle.x - mediax) * scalefactor;
                    var miny = (mediah - pathHandle.y) * scalefactor;
                    var maxx = (pathHandle.w - mediax) * scalefactor;
                    var maxy = (mediah - pathHandle.h) * scalefactor;
                    minx += offsetx;
                    maxx += offsetx;
                    miny += offsety;
                    maxy += offsety;
                    if (minx > maxx) {
                        var tempx = minx;
                        minx = maxx;
                        maxx = tempx;
                    }
                    if (miny > maxy) {
                        var tempy = miny;
                        miny = maxy;
                        maxy = tempy;
                    }
                    //cntximg.strokeStyle = "yellow";
                    //cntximg.lineWidth = 2;
                    //cntximg.strokeRect(minx, miny, maxx - minx, maxy - miny);
                    if ((mp.x < minx || mp.x > maxx) || (mp.y < miny || mp.y > maxy)) {
                        if (!pathHandle.parent.multiselect && !hover) {
                            pathHandle.unselect();
                        }
                        return false;
                    }
                    var epsilon = {
                        x: minx - ((maxx - minx) / 100),
                        y: maxy - ((maxy - miny) / 2)
                    };
                    var within = insidepolygonpoints(scalefactor, offsetx, offsety, mediax, mediay, mediah, pathHandle, mp, epsilon);
                    if (within) {
                        //pathHandle.toggleselect();
                        pathHandle.mouseon();
                        return true;

                        //console.log(pathHandle.blockname);
                    }
                    else {
                        if (!pathHandle.parent.multiselect && !hover) {
                            pathHandle.unselect();
                        }
                        pathHandle.mouseoff();
                        return false;
                    }
                    /*var within = insidepolygonpoints(scalefactor,offsetx,offsety,mediax, mediay, mediah,pathHandle,mp,epsilon);
                    if(within){
                        pathHandle.select();
                        return true;
                        //console.log(pathHandle.blockname);
                    }else{
                        pathHandle.unselect();
                        return false;
                    }*/
                    //(scalefactor,offsetx,offsety,mediax, mediay, mediah,pathHandle.points,mp,epsilon){
                };
                this.findsnapPoint = function (x, y, scalefactor, offsetx, offsety, mediax, mediay, mediah) {
                    var absxscaled = (this.points[0] - mediax) * scalefactor;
                    var absyscaled = (mediah - this.points[1]) * scalefactor;
                    var relxscaled = 0;
                    var relyscaled = 0;
                    absxscaled += offsetx;
                    absyscaled += offsety;
                    var snapradius = 10;
                    if (absxscaled > x - snapradius && absxscaled < x + snapradius) {
                        if (absyscaled > y - snapradius && absyscaled < y + snapradius) {
                            return {
                                found: true,
                                x: absxscaled,
                                y: absyscaled,
                                type: snapEndpoint
                            };
                        }
                    }
                    for (var counter = 2; counter < this.points.length; counter += 2) {
                        relxscaled = ((this.points[counter] - mediax) * scalefactor);
                        relyscaled = ((mediah - this.points[counter + 1]) * scalefactor);
                        relxscaled += offsetx;
                        relyscaled += offsety;
                        if (relxscaled > x - snapradius && relxscaled < x + snapradius) {
                            if (relyscaled > y - snapradius && relyscaled < y + snapradius) {
                                return {
                                    found: true,
                                    x: relxscaled,
                                    y: relyscaled,
                                    type: snapEndpoint
                                };
                            }
                        }
                    }
                    return {
                        found: false,
                        x: 0,
                        y: 0,
                        type: snapEndpoint
                    };
                };

                this.getLength = function(){

                    if(pathHandle.Length == undefined){

                        var lengtharray = [];

                        var absxscaled = (pathHandle.points[0] - pathHandle.parent.x);
                        var absyscaled = (pathHandle.parent.h - pathHandle.points[1]);
                        
    
                        lengtharray.push({x : absxscaled, y : absyscaled});
    
                        var relxscaled = 0;
                        var relyscaled = 0;
                       
                        for (var counter = 2; counter < pathHandle.points.length; counter += 2) {
    
                            relxscaled = ((pathHandle.points[counter] - pathHandle.parent.x));
                            relyscaled = ((pathHandle.parent.h - pathHandle.points[counter + 1]));
                            lengtharray.push({x : relxscaled, y : relyscaled});
                
                        }


                        var dimdiag = 0;


                        for (var i = 0; i < lengtharray.length; i++) {
                            if (i > 0) {
                                var dimwidth = lengtharray[i].x - lengtharray[i - 1].x;
                                var dimheight = lengtharray[i].y - lengtharray[i - 1].y;

                                dimdiag += getDiagonal(dimwidth, dimheight);
                                //markupobject.dimtext = getUnitlength(dimdiag / markupobject.scaling);
                                //markupobject.dimtext = markupobject.dimtext.toFixed(2);
                                //markupobject.dimtext = markupobject.dimtext + " " + Unitlabel;
                            }
                        }
    
                        dimwidth = lengtharray[0].x - lengtharray[lengtharray.length - 1].x;
                        dimheight = lengtharray[0].y - lengtharray[lengtharray.length - 1].y;
                        dimdiag += getDiagonal(dimwidth, dimheight);
    
                        pathHandle.Length = dimdiag;

                    }




                    return pathHandle.Length;

                };

                this.getArea = function(){

                    if(pathHandle.Area == undefined){
                        var areaarray = [];

                        var absxscaled = (pathHandle.points[0] - pathHandle.parent.x);
                        var absyscaled = (pathHandle.parent.h - pathHandle.points[1]);
                        
    
                        areaarray.push({x : absxscaled, y : absyscaled});
    
                        var relxscaled = 0;
                        var relyscaled = 0;
                       
                        for (var counter = 2; counter < pathHandle.points.length; counter += 2) {
    
                            relxscaled = ((pathHandle.points[counter] - pathHandle.parent.x));
                            relyscaled = ((pathHandle.parent.h - pathHandle.points[counter + 1]));
                            areaarray.push({x : relxscaled, y : relyscaled});
                
                        }
    
                        var areapoints = getpolygonarea(areaarray);
    
                        var temppointarray = [];
    
                        if(pathHandle.gotsubpath){
                            for (var scount = 0; scount < pathHandle.subpaths.length; scount++) {
                                for ( counter = 0; counter < pathHandle.subpaths[scount].points.length; counter += 2) {
                
                                    relxscaled = ((pathHandle.subpaths[scount].points[counter] - pathHandle.parent.x));
                                    relyscaled = ((pathHandle.parent.h - pathHandle.subpaths[scount].points[counter + 1]));
                
                                    temppointarray.push({x:relxscaled,y:relyscaled});
                                }
                
                                var parea = getpolygonarea(temppointarray);
                
                                if(parea > areapoints){
                                    areaarray = temppointarray.slice();
                                    temppointarray = [];
                                    areapoints = parea;
                                }
                
                            }
                        }
    
                        pathHandle.Area = areapoints;
                    }
                    

                    return pathHandle.Area;

                };

                this.setoverridepathlist = function(bchanged){
                    if(bchanged){
                        if(pathHandle.overridefill){
                            pathHandle.parent.adddrawfirstlist(pathHandle);
                        }else{
                            pathHandle.parent.removedrawfirstlist(pathHandle);
                        }
                    }

                };

                this.checkoverrideChange = function(block){

                    var bstatechanged = false;

                    if(block){
                        var blockovrstate = (block.overridefill || block.overridecolor);

                        //parent.blocklist[pathHandle.blockname].overridefill
                        //parent.blocklist[pathHandle.blockname].overridecolor
    
    
                        var bstatechanged = false;
    
                        if(blockovrstate){
    
                            if(pathHandle.overridefill){
                                
                                bstatechanged = false;
                            }else{
                                pathHandle.overridefill = true;
                                bstatechanged = true;
                            }
                            
    
                        }else{
                            if(pathHandle.overridefill){
                                pathHandle.overridefill = false;
                                bstatechanged = true;
                            }else{
                                bstatechanged = false;
                            }
    
    
                        }
                    }
                    
                    return bstatechanged;
                    
                };

                this.getlinewidth = function(scalefactor, drawprint){
                    

                    var linewidth = pathHandle.linewidth;

                    if(pathHandle.parent.applypentable && pathHandle.parent.pentable.changed){

                        if(pathHandle.parent.pentable.getPendirect(pathHandle.penarrindex) != undefined){
                            pathHandle.linewidth = pathHandle.parent.pentable.getPendirect(pathHandle.penarrindex).getwidth();
                        }
                        linewidth = pathHandle.linewidth;
                        
                    }else{
                        pathHandle.linewidth = pathHandle.originallinewidth;
                        linewidth = pathHandle.linewidth;
                    }

                    
                    if(!pathHandle.linewidth){
                        pathHandle.linewidth = 1;
                        linewidth = 1;
                    }

                    /*if(pathHandle.parent.applypentable){
                        pathHandle.linewidth = pathHandle.parent.pentable.getPen().getLineWidth();
                        linewidth = pathHandle.linewidth;
                    }else{
                        pathHandle.linewidth = pathHandle.originallinewidth;
                        linewidth = pathHandle.linewidth;
                    }*/

                    if (pathHandle.linewidth * scalefactor < 1 && pathHandle.filled == 0) {
                        //ctx.lineWidth = 1;


                        if(pathHandle.parent.pentable.getscaled()){
                            //linewidth = scope.linewidth * scalefactor;
                            linewidth = 1;
                        }else{
                            linewidth = pathHandle.linewidth;
                        }


                        //linewidth = 1;

                        if(drawprint){
                            //ctx.lineWidth = 4;    
                            linewidth = 4;
                        }
                
                    }
                    else {
                        //ctx.lineWidth = pathHandle.linewidth * scalefactor;

                        if(pathHandle.parent.applypentable && pathHandle.parent.pentable.changed){
                            if(pathHandle.parent.pentable.getscaled()){
                                linewidth = pathHandle.linewidth * scalefactor;
                            }else{
                                linewidth = pathHandle.linewidth;
                            }
    
                        }else{
                            linewidth = pathHandle.linewidth * scalefactor;
                        }


                        
                        //ctx.lineWidth = (0.5 + (this.linewidth * scalefactor)) | 0;

                        if(drawprint){
                            //ctx.lineWidth = 4;    
                            linewidth = 4;
                        }

                    }

                    if(bOverridelineWidth && !pathHandle.parent.applypentable){
                        //ctx.lineWidth = 1;
                        linewidth = dOveridelinewidth;
                    }
                                  
                    if(highlightdraw){

                        linewidth = nVectorhighlightwidth;
    
                    }



                    return linewidth;

                };

                this.getdrawColors = function(drawprint){

                    var linecolor = pathHandle.stroketempcolor;
                    var fillcolor = pathHandle.filltempcolor;

                    var drawcolors = {linecolor : linecolor, fillcolor : fillcolor };


                    if(pathHandle.parent.applypentable && pathHandle.parent.pentable.changed){

                        if(pathHandle.parent.pentable.getPendirect(pathHandle.penarrindex) != undefined){
                            pathHandle.stroketempcolor = pathHandle.parent.pentable.getPendirect(pathHandle.penarrindex).getcolor();
                            linecolor = pathHandle.stroketempcolor;
                        }
                        
                    }else{
                        pathHandle.stroketempcolor = pathHandle.strokecolor;
                        linecolor = pathHandle.stroketempcolor;
                    }

                    
                    if (!bKeepVectorColor) {
                        if (pathHandle.filltempcolor == DocObj.backgroundColor) {
                            if (DocObj.backgroundColor == "#FFFFFF") {
                                pathHandle.filltempcolor = "#000000";
                                fillcolor = pathHandle.filltempcolor;

                            }
                            if (DocObj.backgroundColor == "#000000") {
                                pathHandle.filltempcolor = "#FFFFFF";
                                fillcolor = pathHandle.filltempcolor;
                            }
                        }
                        if (pathHandle.stroketempcolor == DocObj.backgroundColor) {
                            if (DocObj.backgroundColor == "#FFFFFF") {
                                pathHandle.stroketempcolor = "#000000";
                                linecolor = pathHandle.stroketempcolor;
                            }
                            if (DocObj.backgroundColor == "#000000") {
                                pathHandle.stroketempcolor = "#FFFFFF";
                                linecolor = pathHandle.stroketempcolor;
                            }
                        }
                    }
                    else {
                        pathHandle.stroketempcolor = pathHandle.strokecolor;
                        pathHandle.filltempcolor = pathHandle.fillcolor;

                        linecolor = pathHandle.stroketempcolor;
                        fillcolor = pathHandle.filltempcolor;

                    }
                    if (drawprint && pathHandle.stroketempcolor == "#FFFFFF") {
                        pathHandle.stroketempcolor = "#000000";
                        linecolor = pathHandle.stroketempcolor;
                    }
                    if (drawprint && pathHandle.filltempcolor == "#FFFFFF") {
                        pathHandle.filltempcolor = "#000000";
                        fillcolor = pathHandle.filltempcolor;
                    }
                    if (pathHandle.drawcompare) {
                        //ctx.strokeStyle = pathHandle.comparecolor;
                        linecolor = pathHandle.comparecolor;
                    }
                    else if (pathHandle.parent.viewmode == 1) {

                        if (DocObj.backgroundColor == "#FFFFFF") {
                            pathHandle.filltempcolor = "#000000";
                            
                            //ctx.strokeStyle = "#000000";
                            //ctx.fillStyle = "#000000";

                            linecolor = "#000000";
                            fillcolor = "#000000";
    

                        }
                        if (DocObj.backgroundColor == "#000000" || DocObj.backgroundColor == "#A0A0A0") {
                            pathHandle.filltempcolor = "#FFFFFF";
                            
                            //ctx.strokeStyle = "#FFFFFF";
                            //ctx.fillStyle = "#FFFFFF";

                            linecolor = "#FFFFFF";
                            fillcolor = "#FFFFFF";

                        }

                        //ctx.strokeStyle = "black";
                    }
                    else {

                        if(pathHandle.parent.viewmode == 0){
                            pathHandle.filltempcolor = pathHandle.fillcolor;

                            fillcolor = pathHandle.filltempcolor;
                        }

                        //ctx.strokeStyle = pathHandle.stroketempcolor;
                        linecolor = pathHandle.stroketempcolor;
                        
                    }

                    if( pathHandle.parent.comparemode == 0 ){
                        if(pathHandle.role == 1){

                            //ctx.fillStyle = pathHandle.parent.backgroundColor;
                            //ctx.strokeStyle = pathHandle.parent.backgroundColor;

                            linecolor = pathHandle.parent.backgroundColor;
                            fillcolor = pathHandle.parent.backgroundColor;

    
                        }else if (pathHandle.role == 2){
                            //ctx.fillStyle = pathHandle.parent.overlayColor;
                            //ctx.strokeStyle = pathHandle.parent.overlayColor;
                            
                            linecolor = pathHandle.parent.overlayColor;
                            fillcolor = pathHandle.parent.overlayColor;

                        }
    
                    }

                    if(highlightdraw){

                        fillcolor = szmarkVectorColor;
                        linecolor = szmarkVectorColor;
    
                    }

                    

                    return drawcolors = {linecolor : linecolor, fillcolor : fillcolor };
                };     
                
                this.drawmefirst = function(ctx, scalefactor, offsetx, offsety, mediax, mediay, mediah, ls, bs, drawprint, magnify){
                    pathHandle.drawoverride = true;
                    pathHandle.drawme(ctx, scalefactor, offsetx, offsety, mediax, mediay, mediah, ls, bs, drawprint, magnify);
                    pathHandle.drawoverride = false;
                };

                this.highlight = function(ctx, scalefactor, offsetx, offsety, mediax, mediay, mediah){

                    highlightdraw = true;

                    pathHandle.drawme(ctx, scalefactor, offsetx, offsety, mediax, mediay, mediah, 1, 1, false, false);

                    highlightdraw = false;

                };

                //this.drawme = function (ctx, scalefactor, offsetx, offsety, mediax, mediay, mediah,ls,bs,drawprint) {
                this.drawme = function (ctx, scalefactor, offsetx, offsety, mediax, mediay, mediah, ls, bs, drawprint, magnify) {
                    
                    /*if(pathHandle.role == 2){
                        mediax = pathHandle.parent.overlayoffsetx;
                        mediah = pathHandle.parent.overlayoffsety;

                        scalefactor = scalefactor * pathHandle.parent.overlayscale;
                        
                    }*/
                    
                    var absxscaled = (pathHandle.points[0] - mediax) * scalefactor;
                    var absyscaled = (mediah - pathHandle.points[1]) * scalefactor;
                    var relxscaled = 0;
                    var relyscaled = 0;
                    if (ls == 0 || bs == 0) {
                        return;
                    }
                    if(!DocObj){
                        return;
                    }

                    var overridechanged = pathHandle.checkoverrideChange(parent.blocklist[pathHandle.blockname]);
                    pathHandle.setoverridepathlist(overridechanged);


                    if(pathHandle.overridefill && !pathHandle.drawoverride){
                        //console.log(pathHandle.index);
                        return;
                    }



                    /*if(parent.blocklist[pathHandle.blockname].overridefill && parent.blocklist[pathHandle.blockname].fillstyle != null){
                        

                    }*/




                    absxscaled += offsetx;
                    absyscaled += offsety;
                    pathHandle.render = true;
                    var minx = (pathHandle.x - mediax) * scalefactor;
                    var miny = (mediah - pathHandle.y) * scalefactor;
                    var maxx = (pathHandle.w - mediax) * scalefactor;
                    var maxy = (mediah - pathHandle.h) * scalefactor;
                    minx += offsetx;
                    maxx += offsetx;
                    miny += offsety;
                    maxy += offsety;


                    var xdiff = Math.max(minx, maxx) - Math.min(minx, maxx);
                    var ydiff = Math.max(miny, maxy) - Math.min(miny, maxy);
                    var radius = getDiagonal(xdiff, ydiff);
        
                    if(radius < 0.5){
                        //vobject.render = false;
                        pathHandle.render = false;
                        return;
                    }
        

                    //if(!pathHandle.parent.backgroundrender){


                    if ((!pathHandle.parent.backgroundrender || magnify) && !drawprint) {
                        var cnvwidth = canvasowidth;
                        var cnvheight = canvasoheight;
                        if (magnify) {
                            cnvwidth = magnifysize.width;
                            cnvheight = magnifysize.height;
                        }
                        if ((minx < 0 && maxx < 0) || (miny < 0 && maxy < 0)) {
                            pathHandle.render = false;
                            return;
                        }
                        //if ((minx > canvasowidth && maxx > canvasowidth) || (miny > canvasowidth && maxy > canvasoheight)) {
                        if ((minx > cnvwidth && maxx > cnvwidth) || (miny > cnvheight && maxy > cnvheight)) {
                            pathHandle.render = false;
                            return;
                        }
                    }
                    /*if (DocObj.getCurrentpage().VectorPageObj != undefined) {
                        if (!DocObj.getCurrentpage().VectorPageObj.backgroundrender) {
        
                            if ((minx < 0 && maxx < 0) || (miny < 0 && maxy < 0)) {
                                this.render = false;
                                return;
                            }
        
                            if ((minx > canvasowidth && maxx > canvasowidth) || (miny > canvasowidth && maxy > canvasoheight)) {
                                this.render = false;
                                return;
                            }
        
                        }
        
                    } else {
                        if ((minx < 0 && maxx < 0) || (miny < 0 && maxy < 0)) {
                            this.render = false;
                            return;
                        }
        
                        if ((minx > canvasowidth && maxx > canvasowidth) || (miny > canvasowidth && maxy > canvasoheight)) {
                            this.render = false;
                            return;
                        }
        
                    }*/
                    //ctx.save();
                    //ctx.imageSmoothingEnabled = false;
                    //ctx.restore();
                    //ctx.globalCompositeOperation = 'source-over';
                    /*if (!bKeepVectorColor) {
                        if (pathHandle.filltempcolor == DocObj.backgroundColor) {
                            if (DocObj.backgroundColor == "#FFFFFF") {
                                pathHandle.filltempcolor = "#000000";
                            }
                            if (DocObj.backgroundColor == "#000000") {
                                pathHandle.filltempcolor = "#FFFFFF";
                            }
                        }
                        if (pathHandle.stroketempcolor == DocObj.backgroundColor) {
                            if (DocObj.backgroundColor == "#FFFFFF") {
                                pathHandle.stroketempcolor = "#000000";
                            }
                            if (DocObj.backgroundColor == "#000000") {
                                pathHandle.stroketempcolor = "#FFFFFF";
                            }
                        }
                    }
                    else {
                        pathHandle.stroketempcolor = pathHandle.strokecolor;
                        pathHandle.filltempcolor = pathHandle.fillcolor;
                    }*/
                    /*if (drawprint && pathHandle.stroketempcolor == "#FFFFFF") {
                        pathHandle.stroketempcolor = "#000000";
                    }
                    if (drawprint && pathHandle.filltempcolor == "#FFFFFF") {
                        pathHandle.filltempcolor = "#000000";
                    }
                    if (pathHandle.drawcompare) {
                        ctx.strokeStyle = pathHandle.comparecolor;
                    }
                    else if (pathHandle.parent.viewmode == 1) {

                        if (DocObj.backgroundColor == "#FFFFFF") {
                            pathHandle.filltempcolor = "#000000";
                            ctx.strokeStyle = "#000000";
                            ctx.fillStyle = "#000000";
                        }
                        if (DocObj.backgroundColor == "#000000") {
                            pathHandle.filltempcolor = "#FFFFFF";
                            ctx.strokeStyle = "#FFFFFF";
                            ctx.fillStyle = "#FFFFFF";
                        }

                        //ctx.strokeStyle = "black";
                    }
                    else {

                        if(pathHandle.parent.viewmode == 0){
                            pathHandle.filltempcolor = pathHandle.fillcolor;
                        }

                        ctx.strokeStyle = pathHandle.stroketempcolor;
                    }

                    if( pathHandle.parent.comparemode == 0 ){
                        if(pathHandle.role == 1){

                            ctx.fillStyle = pathHandle.parent.backgroundColor;
                            ctx.strokeStyle = pathHandle.parent.backgroundColor;
    
                        }else if (pathHandle.role == 2){
                            ctx.fillStyle = pathHandle.parent.overlayColor;
                            ctx.strokeStyle = pathHandle.parent.overlayColor;
                        }
    
                    }*/

                    var drawcolors = pathHandle.getdrawColors(drawprint);


                    ctx.fillStyle = drawcolors.fillcolor;
                    ctx.strokeStyle = drawcolors.linecolor;



                    //console.log(this.linewidth * scalefactor);
                    //console.log('path');
                    //console.log(this.filled);
                    //ctx.lineWidth = this.linewidth * scalefactor;

                    /*if(!pathHandle.linewidth){
                        pathHandle.linewidth = 1;
                    }*/


                    /*if (pathHandle.linewidth * scalefactor < 1 && pathHandle.filled == 0) {
                        ctx.lineWidth = 1;

                        if(drawprint){
                            ctx.lineWidth = 4;    
                        }
                
                    }
                    else {
                        ctx.lineWidth = pathHandle.linewidth * scalefactor;
                        //ctx.lineWidth = (0.5 + (this.linewidth * scalefactor)) | 0;

                        if(drawprint){
                            ctx.lineWidth = 4;    
                        }

                    }

                    if(bOverridelineWidth){
                        ctx.lineWidth = 1;
                    }*/

                    ctx.lineWidth = pathHandle.getlinewidth(scalefactor,drawprint);

                    //ctx.lineWidth = this.linewidth * scalefactor;
                    ctx.beginPath();

                    absxscaled = pxround(absxscaled);
                    absyscaled = pxround(absyscaled);

                    //ctx.moveTo(markupobject.points[0].x, markupobject.points[0].y);
                    ctx.moveTo(absxscaled, absyscaled); //convert to integer pixels.
                    //ctx.moveTo(((0.5 + absxscaled) | 0), ((0.5 + absyscaled) | 0));
                    /*Andriy: Unused variable var count = 0;*/
                    for (var counter = 2; counter < this.points.length; counter += 2) {
                        //            ctx.lineTo(markupobject.points[counter].x, markupobject.points[counter].y);
                        relxscaled = ((pathHandle.points[counter] - mediax) * scalefactor);
                        relyscaled = ((mediah - pathHandle.points[counter + 1]) * scalefactor);

    

                        relxscaled += offsetx;
                        relyscaled += offsety;

                        relxscaled = pxround(relxscaled);
                        relyscaled = pxround(relyscaled);

                        //xscaled = xscaled + offsetx;
                        //yscaled = yscaled + offsety;
                        ctx.lineTo(relxscaled, relyscaled); //convert to integer pixels
                        //ctx.lineTo(((0.5 + relxscaled) | 0), ((0.5 + relyscaled) | 0));
                    }
                    var blockoverridecolor = false;
                    var blockoverridefill = false;

                    if(parent.blocklist[pathHandle.blockname] != undefined){
                        blockoverridecolor = parent.blocklist[pathHandle.blockname].overridecolor;
                        blockoverridefill = parent.blocklist[pathHandle.blockname].overridefill;
                    }
                    

                    //if ((pathHandle.filled == 1) || pathHandle.selected || parent.blocklist[pathHandle.blockname].overridecolor || parent.blocklist[pathHandle.blockname].overridefill) {
                    if ((pathHandle.filled == 1) || pathHandle.selected || blockoverridecolor || blockoverridefill ) {

                    
                        ctx.lineWidth = 1;
                        ctx.closePath();
                        
                        if (pathHandle.drawcompare) {
                            ctx.fillStyle = pathHandle.comparecolor;
                        }
                        else {

                            if (pathHandle.selected) {
                                //var grd = ctx.createLinearGradient(minx, miny, maxx, maxy);
                                var grd = ctx.createLinearGradient(maxx, miny, minx, maxy);
                                grd.addColorStop(0, parent.selectColor);
                                grd.addColorStop(.5, parent.gradientStop);
                                grd.addColorStop(1, parent.selectColor);
                                //grd.addColorStop(0, parent.selectColor);
                                //grd.addColorStop(1, parent.gradientStop);
                                //ctx.fillStyle = parent.selectColor;//"#bf3ad1";
                                ctx.fillStyle = grd;
                                //getlinestyle
                            }else{


                                if(parent.blocklist[pathHandle.blockname] != undefined){
                                    if (parent.blocklist[pathHandle.blockname].overridecolor) {
                                        ctx.fillStyle = parent.blocklist[pathHandle.blockname].color;
                                    }else if(parent.blocklist[pathHandle.blockname].overridefill && parent.blocklist[pathHandle.blockname].fillstyle != null){
                                        
                                        //ctx.fillStyle = parent.blocklist[pathHandle.blockname].fillstyle;
                                        if(parent.blocklist[pathHandle.blockname].fillstyle == 3 && parent.blocklist[pathHandle.blockname].hatchstyle != null){
    
                                            var ptrn = getHatchbyName(ctx, parent.blocklist[pathHandle.blockname].hatchstyle);

                                            if(ptrn != null){
                                                ctx.fillStyle = ptrn;
                                            }

                                            //ctx.fillStyle = getHatchbyName(ctx, parent.blocklist[pathHandle.blockname].hatchstyle);
    
                                        }
    
    
                                    }else{
                                        ctx.fillStyle = pathHandle.filltempcolor;
                                    }
                                }

                                
                            }
          
                        }

                        if( pathHandle.parent.comparemode == 0 ){
                            if(pathHandle.role == 1){

                                ctx.fillStyle = pathHandle.parent.backgroundColor;
                                //ctx.strokeStyle = pathHandle.parent.backgroundColor;
        
                            }else if (pathHandle.role == 2){
                                ctx.fillStyle = pathHandle.parent.overlayColor;
                                //ctx.strokeStyle = pathHandle.parent.overlayColor;
                            }

                        }
    

                        
                    }
                    else {
                        //ctx.lineCap = 'round';
                        //ctx.stroke();
                    }
                    ctx.lineCap = 'round';
                    ctx.lineJoin = "round";

                    if (!pathHandle.gotsubpath) {


                        
                        
                    }
                    else {
                        for (var scount = 0; scount < pathHandle.subpaths.length; scount++) {

                            pathHandle.subpaths[scount].drawme(ctx, scalefactor, offsetx, offsety, mediax, mediay, mediah, ls, bs, drawprint, false);

                        }
                    }

                    if (pathHandle.filled) {
                        ctx.fill();
                    }
                    
                    ctx.stroke();


                    //ctx.clip();
                    //ctx.restore();
                    pathHandle.drawcompare = false;
                    
                };
                this.Close = function () {
                    if (pathHandle.gotsubpath) {
                        while (pathHandle.subpaths.length != 0) {
                            pathHandle.subpaths.pop();
                        }
                    }
                    pathHandle.points = null;
                };
                this.findrectangle();
            }
        }

        class PenTable {

            pens = [];
            curindex = 0;

            constructor(){
                this.pens = [];
            }

            unit = 1; //metric mm
            scaled = true;

            changed = false;
            
            setunit(nunit){
                this.unit = nunit;
            }

            setscaled(bscaled){
                this.scaled = bscaled;
            }
            getscaled(){
                return this.scaled;
            }
            getunit(){
                return this.unit;
            }

            add(penobject){
                this.pens.push(penobject)
                this.curindex = this.pens.length - 1;
            }

            getcurIndex (){
                return this.pens.length;
            }
            setcurIndex(indx){

                for(var i = 0; i < this.pens.length; i++ ){
                    if(this.pens[i].penindex == indx){
                        //exists = true;
                        this.curindex = i;
                        break;
                    }
                }
                //this.curindex = indx;
            }

            exists(indx){
                var exists = false;

                for(var i = 0; i < this.pens.length; i++ ){
                    if(this.pens[i].penindex == indx){
                        exists = true;
                        break;
                    }
                }

                return exists;
            }

            existSetcurrent(indx){
                var exists = false;

                for(var i = 0; i < this.pens.length; i++ ){
                    if(this.pens[i].penindex == indx){
                        exists = true;
                        this.curindex = i;
                        break;
                    }
                }

                return exists;
            }


            getPen (indx){
                var pen =  undefined;
                //var pen = this.pens[arrindx];

                for(var i = 0; i < this.pens.length; i++ ){
                    if(this.pens[i].penindex == indx){
                        pen = this.pens[i];
                        break;
                    }
                }

                return pen;

            }

            getPenArrindex (indx){
                var pen =  undefined;
                var penindex = -1;
                //var pen = this.pens[arrindx];

                for(var i = 0; i < this.pens.length; i++ ){
                    if(this.pens[i].penindex == indx){
                        pen = this.pens[i];
                        penindex = pen.getarrindex();
                        break;
                    }
                }

                return penindex;

            }

            getPendirect (indx){
                var pen = this.pens[indx];
                return pen;
            }
            getCurrentPen (){
                return this.pens[this.curindex];
            }


        }
        class PenTableObject {

            penindex = 0;
            arrayindex = 0;

            originalcolor = "#FFFFFF";
            color = "#FFFFFF";
            originalstyle = 0;
            style = 0;

            penwidth = 0;
            originalpenwidth = 0;
            displaywidth = 0;



            unitwidth = 0;
            
            constructor(index){
                this.penindex = index;
                //this.arrayindex = count;
                //this.color = pendata.color;
                //this.style = pendata.style;
                //this.penwidth = pendata.width;
            }

            //vectorobj.pentable.getCurrentPen().setOriginalwidth(readstate.lwidth);
            //vectorobj.pentable.getCurrentPen().setOriginalcolor(readstate.scolor);

            setOriginalwidth(pwidth){

                this.penwidth = pwidth;
                this.originalpenwidth = pwidth;
            }

            setdisplaywidth(pwidth){
                this.displaywidth = pwidth;
            }

            setOriginalcolor(pcolor){

                this.color = pcolor;
                this.originalcolor = pcolor;
            }

            setOriginalStyle(pstyle){

                this.style = pstyle;
                this.originalstyle = pstyle;

            }

            resetwidth(){
                this.penwidth = this.originalpenwidth;
                this.displaywidth = this.penwidth;
            }

            resetcolor(){
                this.color = this.originalcolor;
            }
            resetstyle(){
                this.style = this.originalstyle;
            }

            setcolor(pcolor){
                this.color = pcolor;
            }
            getcolor(){
                return this.color;
            }

            setwidth(pwidth){
                this.penwidth = pwidth;
            }

            getwidth(){
                return this.penwidth;
            }

            getdisplaywidth(){
                return this.displaywidth;
            }

            setstyle(pstyle){
                this.style = pstyle;
            }

            setarrayindex(indx){
                this.arrayindex = indx;
            }

            setunitwidth(uwidth){
                this.unitwidth = uwidth;
            }

            getarrindex(){
                return this.arrayindex;
            }
             
        }        

        class HyperLinkObject {
            constructor(hyperlinkobj) {

                this.x1 = parseFloat(hyperlinkobj.x1);
                this.x2 = parseFloat(hyperlinkobj.x2); 
                this.y1 = parseFloat(hyperlinkobj.y1); 
                this.y2 = parseFloat(hyperlinkobj.y2); 
                this.page = hyperlinkobj.page; 
                this.dx1 = parseFloat(hyperlinkobj.dx1);
                this.dx2 = parseFloat(hyperlinkobj.dx2);
                this.dy1 = parseFloat(hyperlinkobj.dy1);
                this.dy2 = parseFloat(hyperlinkobj.dy2);
                this.mouseover = false;
                this.currentpage = 0;
                

            }

            onhyperlinkClick(x,y){
                
            }

            Getwithin (x, y, x1, x2, y1, y2) {
                var within = false;



                if (x > x1 && x < x2) {
                    if (y > y1 && y < y2) {
                        within = true;
                    }
                }
                return within;
            }

            drawhyperlink (ctx, scalefactor, offsetx, offsety, mediax, mediay, mediah){

                var minx = (this.x1 - mediax) * scalefactor;
                var miny = (mediah - this.y1) * scalefactor;
                var maxx = (this.x2 - mediax) * scalefactor;
                var maxy = (mediah - this.y2) * scalefactor;
                minx += offsetx;
                maxx += offsetx;
                miny += offsety;
                maxy += offsety;
                if (minx > maxx) {
                    var tempx = minx;
                    minx = maxx;
                    maxx = tempx;
                }
                if (miny > maxy) {
                    var tempy = miny;
                    miny = maxy;
                    maxy = tempy;
                }

                ctx.strokeStyle = "blue";
                ctx.lineWidth = 2;
                ctx.strokeRect(minx, miny, maxx - minx, maxy - miny);

            }

            insideHyperlink (x, y, scalefactor, offsetx, offsety, mediax, mediay, mediah, currentpage, hover) {
                
                var mp = {
                    x: x,
                    y: y
                };

                this.currentpage = currentpage;

                var minx = (this.x1 - mediax) * scalefactor;
                var miny = (mediah - this.y1) * scalefactor;
                var maxx = (this.x2 - mediax) * scalefactor;
                var maxy = (mediah - this.y2) * scalefactor;
                minx += offsetx;
                maxx += offsetx;
                miny += offsety;
                maxy += offsety;
                if (minx > maxx) {
                    var tempx = minx;
                    minx = maxx;
                    maxx = tempx;
                }
                if (miny > maxy) {
                    var tempy = miny;
                    miny = maxy;
                    maxy = tempy;
                }
                //cntximg.strokeStyle = "yellow";
                //cntximg.lineWidth = 2;
                //cntximg.strokeRect(minx, miny, maxx - minx, maxy - miny);
                if ((mp.x < minx || mp.x > maxx) || (mp.y < miny || mp.y > maxy)) {
                    return false;
                }


                var within = this.Getwithin(mp.x,mp.y, minx, maxx ,miny, maxy);


                if (within) {
                    //pathHandle.toggleselect();
                    this.mouseon();

                    if(hover){
                        if(RxCore_GUI_HyperlinkHover != undefined){
                            RxCore_GUI_HyperlinkHover.setMarkupHover(this, mp.x, mp.y);
                        }
                        
                
                    }else{

                        if(RxCore_GUI_Hyperlink != undefined){
                            RxCore_GUI_Hyperlink.markupLink(this);
                        }

                        if(bautohyperlink){
                            var pageindex = parseInt(this.page) - 1;

                            if(pageindex != this.currentpage){
                            
                                RxCore.gotoPage(pageindex);
            
                                //console.log(hyperlink.page);
                            }
    
                        }
                        
                    }

                    return true;

                    //console.log(pathHandle.blockname);
                }
                else {

                    this.mouseoff();
                    return false;
                }

            }

            mouseon (){
                this.mouseover = true;
            }

            mouseoff(){
                this.mouseover = false;
            }



        }        

        class LayerObject {
            constructor(layerxmldata, binary) {
                if(binary){
                    this.index = parseInt(layerxmldata.getInt32(0, true));
                    var ncolor = parseInt(layerxmldata.getInt32(4, true));
                    this.color = decimalToHex(ncolor);
                    var statevar = parseInt(layerxmldata.getInt32(8, true));
                    this.state = statevar;
                    this.isplottable = parseInt(layerxmldata.getInt32(12, true));
                    this.defaultstate = statevar;
                    var nname = layerxmldata.getUint16(16, true);
                    var lname = (nname == 0) ? " " : String.fromCharCode(nname);
                    var i = 18;
                    while (i <= 128) {
                        nname = layerxmldata.getUint16(i, true);
                        lname += (nname == 0) ? " " : String.fromCharCode(nname);
                        i += 2;
                    }
                    this.name = lname;
                }else{
                    this.index = layerxmldata.index;
                    this.color = layerxmldata.color;
                    this.state = layerxmldata.state;
                    this.isplottable = true;
                    this.defaultstate = layerxmldata.state;
                    this.name = layerxmldata.name;
                }

                this.turnLayerOnOff = function (index) {
                };
            }
        }

        class BlockInsertObject {

            constructor(insertblock) {
                this.blockhandleLow = insertblock.blockhandleLow;
                this.blockhandleHigh = insertblock.blockhandleHigh;
                this.insertX = insertblock.insertX;
                this.insertY = insertblock.insertY;
                this.insertZ = insertblock.insertZ;
                this.insertRot = insertblock.insertRot;
                this.insertscaleX = insertblock.insertscaleX;
                this.insertscaleY = insertblock.insertscaleY;
                this.insertscaleZ = insertblock.insertscaleZ;
                this.blockref = insertblock.blockref
            }
            /*var blockinsertobj = {
                blockhandleLow : blockhandleLow,
                blockhandleHigh : blockhandleHigh,
                insertX : insertX,
                insertY : insertY,
                insertZ : insertZ,
                insertRot : insertRot,
                insertscaleX : insertscaleX,
                insertscaleY : insertscaleY,
                insertscaleZ : insertscaleZ,
                blockref: readstate.blockpart
            }*/

        }

        class BlockObject {
            constructor(index, name, state, color, blist) {
                
                var blockhandle = this;
                this.index = index;
                this.name = name;
                this.state = state;
                this.defaultstate = state;
                this.defaultcolor = color;
                this.color = color;
                this.fillstyle = null;
                this.defaultfillstyle = null;
                this.overridecolor = false;
                this.overridefill = false;
                this.hatchstyle = null;
                this.selected = false;
                this.listed = blist;
                this.insert = undefined;
                this.drawn = false;
                this.mouseon = false;

                /* this.getRectangles = function(scalefactor, offsetx, offsety, mediax, mediay, mediah){

                    for (var ri = 0; ri < blockhandle.inserts.length; ri++ ){
                        var rect = blockhandle.getRectangle(blockhandle.inserts[ri], scalefactor, offsetx, offsety, mediax, mediay, mediah);        
                    }
                }; */

                

                this.iswithin = function (x, y, scalefactor, offsetx, offsety, mediax, mediay, mediah) {

                    var within = false;

                    if(blockhandle.insert != undefined){

                        
                        var rect = blockhandle.getRectangle(scalefactor, offsetx, offsety, mediax, mediay, mediah);

                        if(x > rect.x && x < (rect.x + rect.w)){
                            if(y > rect.y && y < (rect.y + rect.h)){
                                within = true;
                            }
                        }

                    }


                    return within;


                };

                this.getArea = function(){

                    if(blockhandle.insert != undefined){

                        var minx = blockhandle.insert.MinX;
                        var miny = blockhandle.insert.MinY;
                        var maxx = blockhandle.insert.MaxX;
                        var maxy = blockhandle.insert.MaxY;
    
                        if (minx > maxx) {
                            var tempx = minx;
                            minx = maxx;
                            maxx = tempx;
                        }
                        if (miny > maxy) {
                            var tempy = miny;
                            miny = maxy;
                            maxy = tempy;
                        }
                        /*return {
                            area: (maxx - minx) * (maxy - miny),
                            x: minx,
                            y: miny,
                            w: maxx - minx,
                            h: maxy - miny,
                            wp: (maxx - minx) / 10,
                            hp: (maxy - miny) / 10
                        };*/
    
                        return (maxx - minx) * (maxy - miny);
                        
    
                    }else{
                        return 0;
                    }

                };

                this.select = function(){

                    if (blockhandle.selected){
                        blockhandle.selected = false;
                    }else{
                        blockhandle.selected = true;
                    }

                };

                this.unselect = function(){

                    blockhandle.selected = false;

                };


                this.highlight = function(ctx, scalefactor, offsetx, offsety, mediax, mediay, mediah){

                    if(blockhandle.insert != undefined){

                        
                        var rect = blockhandle.getRectangle(scalefactor, offsetx, offsety, mediax, mediay, mediah);
                        ctx.save();
    
                        ctx.lineWidth = 2;
                        ctx.fillStyle = "rgba(255,0,0,0.5)";

                        ctx.fillRect(rect.x, rect.y, rect.w, rect.h);

                        ctx.restore();

                        blockhandle.drawn = true;
    
                    }


                };



                this.getRectangle = function (scalefactor, offsetx, offsety, mediax, mediay, mediah) {

                    var minx = 0;
                    var miny = 0;
                    var maxx = 0;
                    var maxy = 0;


                    if(blockhandle.insert != undefined){

                        minx = (blockhandle.insert.MinX - mediax) * scalefactor;
                        miny = (mediah - blockhandle.insert.MinY) * scalefactor;
                        maxx = (blockhandle.insert.MaxX - mediax) * scalefactor;
                        maxy = (mediah - blockhandle.insert.MaxY) * scalefactor;

                        minx += offsetx;
                        maxx += offsetx;
                        miny += offsety;
                        maxy += offsety;

                        if (minx > maxx) {
                            var tempx = minx;
                            minx = maxx;
                            maxx = tempx;
                        }
                        if (miny > maxy) {
                            var tempy = miny;
                            miny = maxy;
                            maxy = tempy;
                        }
                    }
                    return {
                        found: true,
                        x: minx,
                        y: miny,
                        w: maxx - minx,
                        h: maxy - miny,
                        //zoomrectx : minx * 
                        wp: (maxx - minx) / 10,
                        hp: (maxy - miny) / 10
                    };
                };

                //BloclistGUIContainer
                //BlocksContainer.Addblock(this.index,this.name,this.state);
                /*var nametd = "<TD>" + this.name + "</TD>";
        
                 if (this.state == 1){
                 var checkboxtd = "<TD>" + "<input type='checkbox' name='"+ this.name + "' value='On' checked onChange='VturnBlockOnOff(" + "\"" +  this.name + "\"" + ")'>"+"</TD>\n";
                 }else{
                 checkboxtd = "<TD>" + "<input type='checkbox' name='"+ this.name + "' value='Off' onChange='VturnBlockOnOff(" + "\"" + this.name + "\"" + ")'>"+"</TD>\n";
                 }
        
                 this.tableline = '<TR>' + nametd + checkboxtd + '</TR>';*/
            }
        }


        class VectorLoader {
            constructor (vectorobj, pagexmldata, compareobj){

                /*var lineweight = 1;
                var numpoints = 0;
                var vectordv = undefined;
                var bnamelength = undefined;
                var bnamecount = 0;
                var blockname = "";*/

                var vscope = this;
                var header = "";
                var dv = undefined;
                var layerSize = 0;
                var startpos = 0;
                var layertotsize = 0;
                var layerdv = undefined;
                var nlayer = 0;
                var blockdv = undefined;
                var blockindx = 0;
                var bobjid = 0;
                var blockid = 0;
                var blockflag = 0;
                var bnamelength = 0;
                var blocknamedv = undefined;
                var bnamecount = 0;
                var blockname = "";
                var vectordv = undefined;
                var dvpos = 0;
                var blist = true;

                var objid = undefined;
                var prevobjid = objid;
                var drawlayer = -1;
                var layerstate = 1;
                var drawpen = 0;
                var lineweight = 1;
                var linewidth = 0;
                var numpoints = 0;
                var fontobject = {
                    height: 10,
                    rotation: 0,
                    weight: 100,
                    italic: 0,
                    underline: 0,
                    name: "Arial"
                };

                
                this.role = 0;
                //1 background, 2 overlay, 0 none

                this.x = 0;
                this.y = 0;
                this.w = 0;
                this.h = 0;
                this.scale = 1;
                this.offsetx = 0;
                this.offsety = 0;
                this.numlayers = 0;
                this.flag = -1;
                this.fileversion = "";
                this.layerlist = [];
                this.penlist = [];
                this.bnameunique = true;
                this.pathlist = [];
                this.numpaths = 0;


                if(compareobj){
                    this.role = compareobj.role;
                }



                function init(vectorobj, pagexmldata, role){
                    dv = new DataView(pagexmldata);
                    header = String.fromCharCode(dv.getUint8(0));
                    var i = 1;
                    while (i <= 15) {
                        header += String.fromCharCode(dv.getUint8(i));
                        i++;
                    }
    
                    vscope.fileversion = parseInt(dv.getInt32(16, true));
                    vscope.flag = dv.getInt32(20, true);
                    //var version = parseInt(dv.getInt32(16, true));
                    //console.log('version:' + version);
                    var layerdvpos = 52;
                    if (vscope.flag == 0) {
                        vscope.x = dv.getFloat32(24, true);
                        vscope.y = dv.getFloat32(28, true);
                        vscope.w = dv.getFloat32(32, true);
                        vscope.h = dv.getFloat32(36, true);
                        vscope.scale = dv.getFloat32(40, true);
                        vscope.offsetx = dv.getFloat32(44, true);
                        vscope.offsety = dv.getFloat32(48, true);
                        layerdvpos = 52;
                    }
                    else if (vscope.flag == 1) {
                        vscope.x = dv.getFloat64(24, true);
                        vscope.y = dv.getFloat64(32, true);
                        vscope.w = dv.getFloat64(40, true);
                        vscope.h = dv.getFloat64(48, true);
                        vscope.scale = dv.getFloat64(56, true);
                        vscope.offsetx = dv.getFloat64(64, true);
                        vscope.offsety = dv.getFloat64(72, true);
                        layerdvpos = 80;
                    }else{
                        console.log('no flag in binary file');
                    }
    

                    vectorobj.setdim(vscope, role);
    
                    /*if(vscope.role == 0){
                        
                        vectorobj.x = vscope.x;
                        vectorobj.y = vscope.y;
                        vectorobj.w = vscope.w;
                        vectorobj.h = vscope.h;
                        vectorobj.scale = vscope.scale;
                        vectorobj.offsetx = vscope.offsetx;
                        vectorobj.offsety = vscope.offsety;
    
                    }*/
                    
    
                    /*---                           ---*/
                    vscope.numlayers = parseInt(dv.getInt32(layerdvpos, true));
    
    
                    layerSize = 144;
                    startpos = layerdvpos + 4;
                    layertotsize = vscope.numlayers * layerSize;

                    if (vscope.numlayers != 0) {
                        layerdv = new DataView(pagexmldata, startpos, layerSize);
                        //Layer number
                        nlayer = 0;
                        while (nlayer < vscope.numlayers) {
                            var NewLayer = new LayerObject(layerdv, true);
                            //vectorobj.layerlist[NewLayer.index] = NewLayer;

                            vscope.layerlist.push(NewLayer);

                            
                            //vectorobj.numlayers++;
                            nlayer++;
                            startpos += layerSize;
                            if (nlayer < vscope.numlayers) {
                                layerdv = new DataView(pagexmldata, startpos, layerSize);
                            }
                        }
    
                        /*if (RxCore_GUI_VectorLayers != undefined && !badd) {
                            RxCore_GUI_VectorLayers.setVectorLayers(vectorobj.layerlist);
                        }*/

                        vectorobj.setlayers(vscope, role);



                    }
    
                    //startpos += layerSize;
    
                    //this.role = compareobj.role;


                    vectorobj.initpath(role);



    
                    blockdv = new DataView(pagexmldata, startpos);
                    //vectorobj.numpaths = -1;
                    
    
    
                    blockindx = 0;
    
    
                    bobjid = parseInt(blockdv.getInt32(0, true));
                    if (bobjid != 32768) {
                        blockid = parseInt(blockdv.getInt32(4, true));
                        blockflag = parseInt(blockdv.getInt32(8, true));
                        blist = (blockflag != 128);
                        vscope.bnameunique = (blockflag == 1);
                        bnamelength = parseInt(blockdv.getInt32(12, true));
                        blocknamedv = new DataView(pagexmldata, startpos + 16, bnamelength * 2);
                        bnamecount = 0;
                        blockname = String.fromCharCode(blocknamedv.getUint16(bnamecount, true));
                        bnamecount += 2;
                        while (bnamecount < bnamelength * 2) {
                            blockname += String.fromCharCode(blocknamedv.getUint16(bnamecount, true));
                            bnamecount += 2;
                        }
                        //move to position after first block description
                        vectordv = new DataView(pagexmldata, startpos + 16 + bnamelength * 2);

                        //vectorobj.blocklist[blockindx] = new BlockObject(blockid, blockname, 1, backgroundColor);
                        //[blockindx] = new BlockObject(blockid, blockname, 1, backgroundColor);
                        
                        vectorobj.updateblocks(new BlockObject(blockid, blockname, 1, backgroundColor, blist),role);

                        dvpos = 0;
                        objid = parseInt(vectordv.getInt32(dvpos, true));
                        prevobjid = objid;
                        drawlayer = -1;
                        layerstate = 1;
                        drawpen = 0;
                        lineweight = 1;
                        linewidth = 0;
                        numpoints = 0;
                        fontobject = {
                            height: 10,
                            rotation: 0,
                            weight: 100,
                            italic: 0,
                            underline: 0,
                            name: "Arial"
                        };
                    }
    
                }

                function parse2Dloop(role) {
                    var readstate = {
                        objectid: objid,
                        prevobjectid: prevobjid,
                        blockpart: blockindx,
                        maindvpos: dvpos,
                        dlayer: drawlayer,
                        lstate: layerstate,
                        dpen: drawpen,
                        scolor: decimalToHex(0),
                        fcolor: decimalToHex(0),
                        lwidth: linewidth,
                        font: fontobject,
                        drawmode: 13,
                        transparency : 1,
                        role : role,
                        entityType : {type : 0, handleLow :0, handleHigh :0}
                    };


                    //blockindx = readstate.blockpart
                    yieldingLoop(readstate, 1000, function () {
                        readstate = parse2Dbinary(readstate);
                    }, function () {
                        readbinarycomplete(readstate.role);
                    });
                    (function timer() {
                        if (vectordv)
                            if (readstate.objectid != 32768) {
                                setTimeout(timer, 10);
                            }
                    })();
                }
                function parse2Dbinary(readstate) {
                    switch (readstate.objectid) {
                        case 1:
                            readstate.maindvpos += 4;
                            var lX1 = vectordv.getFloat32(readstate.maindvpos, true);
                            readstate.maindvpos += 4;
                            var lY1 = vectordv.getFloat32(readstate.maindvpos, true);
                            readstate.maindvpos += 4;
                            var lX2 = vectordv.getFloat32(readstate.maindvpos, true);
                            readstate.maindvpos += 4;
                            var lY2 = vectordv.getFloat32(readstate.maindvpos, true);
                            var lineobject = {
                                layer: readstate.dlayer,
                                drawmode: readstate.drawmode,
                                layerstate: readstate.lstate,
                                pen: readstate.dpen,
                                strokecolor: readstate.scolor,
                                fillcolor: readstate.fcolor,
                                linewidth: readstate.lwidth,
                                x1: lX1,
                                y1: lY1,
                                x2: lX2,
                                y2: lY2,
                                entityType : readstate.entityType,
                                role : readstate.role
                            };
                            var lineobj = new lineObject(lineobject, vectorobj, readstate.blockpart, true, vscope.numpaths);

                            //vscope.pathlist.push(lineobj);
                            vscope.numpaths++;

                            vectorobj.updatepathlist(lineobj, readstate.role);


                            readstate.maindvpos += 4;
                            readstate.prevobjectid = readstate.objectid;
                            readstate.objectid = vectordv.getInt32(readstate.maindvpos, true);
                            break;
                        case 2:
                            readstate.maindvpos += 4;
                            numpoints = vectordv.getInt32(readstate.maindvpos, true);
                            readstate.maindvpos += 4;
                            lX1 = vectordv.getFloat32(readstate.maindvpos, true);
                            readstate.maindvpos += 4;
                            lY1 = vectordv.getFloat32(readstate.maindvpos, true);
                            var lpoints = [];
                            lpoints.push(lX1);
                            lpoints.push(lY1);
                            var cpnts = 1;
                            while (cpnts < numpoints) {
                                readstate.maindvpos += 4;
                                lpoints.push(vectordv.getFloat32(readstate.maindvpos, true));
                                readstate.maindvpos += 4;
                                lpoints.push(vectordv.getFloat32(readstate.maindvpos, true));
                                cpnts++;
                            }
                            var pathobject = {
                                precision: 0,
                                layer: readstate.dlayer,
                                drawmode: readstate.drawmode,
                                layerstate: readstate.lstate,
                                pen: readstate.dpen,
                                strokecolor: readstate.scolor,
                                fillcolor: readstate.fcolor,
                                linewidth: readstate.lwidth,
                                numpoints: numpoints * 2,
                                points: lpoints,
                                filled: 0,
                                gotsubpath: false,
                                shape: readstate.objectid,
                                entityType : readstate.entityType,
                                role : readstate.role 
                            };
                            var pathobj = new pathObject(pathobject, vectorobj, readstate.blockpart, true, vscope.numpaths);

                            //vectorobj.pathlist.push(pathobj);
                            //vectorobj.numpaths++;

                            vscope.numpaths++;
                            vectorobj.updatepathlist(pathobj, readstate.role);


                            readstate.maindvpos += 4;
                            readstate.prevobjectid = readstate.objectid;
                            readstate.objectid = vectordv.getInt32(readstate.maindvpos, true);
                            break;
                        case 3:
                            readstate.maindvpos += 4;
                            numpoints = vectordv.getInt32(readstate.maindvpos, true);
                            readstate.maindvpos += 4;
                            lX1 = vectordv.getFloat32(readstate.maindvpos, true);
                            readstate.maindvpos += 4;
                            lY1 = vectordv.getFloat32(readstate.maindvpos, true);
                            lpoints = [];
                            lpoints.push(lX1);
                            lpoints.push(lY1);
                            cpnts = 1;
                            while (cpnts < numpoints) {
                                readstate.maindvpos += 4;
                                lpoints.push(vectordv.getFloat32(readstate.maindvpos, true));
                                readstate.maindvpos += 4;
                                lpoints.push(vectordv.getFloat32(readstate.maindvpos, true));
                                cpnts++;
                            }
                            pathobject = {
                                precision: 0,
                                layer: readstate.dlayer,
                                drawmode: readstate.drawmode,
                                layerstate: readstate.lstate,
                                pen: readstate.dpen,
                                strokecolor: readstate.scolor,
                                fillcolor: readstate.fcolor,
                                linewidth: readstate.lwidth,
                                numpoints: numpoints * 2,
                                points: lpoints,
                                filled: 1,
                                gotsubpath: false,
                                shape: readstate.objectid,
                                entityType : readstate.entityType,
                                role : readstate.role 
                            };
                            pathobj = new pathObject(pathobject, vectorobj, readstate.blockpart, true, vscope.numpaths);

                            vscope.numpaths++;
                            vectorobj.updatepathlist(pathobj, readstate.role);

                            //vectorobj.pathlist.push(pathobj);
                            //vectorobj.numpaths++;
                            readstate.maindvpos += 4;
                            readstate.prevobjectid = readstate.objectid;
                            readstate.objectid = vectordv.getInt32(readstate.maindvpos, true);
                            break;
                        case 13:
                            readstate.maindvpos += 4;
                            var aX = vectordv.getFloat32(readstate.maindvpos, true);
                            readstate.maindvpos += 4;
                            var aY = vectordv.getFloat32(readstate.maindvpos, true);
                            readstate.maindvpos += 4;
                            var aR = vectordv.getFloat32(readstate.maindvpos, true);
                            readstate.maindvpos += 4;
                            var aSa = vectordv.getFloat32(readstate.maindvpos, true);
                            readstate.maindvpos += 4;
                            var aEa = vectordv.getFloat32(readstate.maindvpos, true);
                            //var endangle = aSa + Math.PI;
                            /*if(aSa > 0){
                                endangle = aSa + Math.PI;
                            }else{
                                endangle = aSa - Math.PI;
                            }*/
                            /*var startangle = aSa;
                            if(startangle < 0){
                                startangle += Math.PI*2;
                            }*/
                            var arcobject = {
                                layer: readstate.dlayer,
                                drawmode: readstate.drawmode,
                                layerstate: readstate.lstate,
                                pen: readstate.dpen,
                                strokecolor: readstate.scolor,
                                fillcolor: readstate.fcolor,
                                linewidth: readstate.lwidth,
                                clockwise: (aEa > 0),
                                ax: aX,
                                ay: aY,
                                ar: aR,
                                asa: aSa,
                                aea: aSa + aEa,
                                entityType : readstate.entityType,
                                role : readstate.role
                            };
                            var arcobj = new arcObject(arcobject, vectorobj, readstate.blockpart, true, vscope.numpaths);
                            
                            vectorobj.updatepathlist(arcobj, readstate.role);
                            vscope.numpaths++;
                            //vectorobj.pathlist.push(arcobj);
                            //vectorobj.numpaths++;
                            readstate.maindvpos += 4;
                            readstate.prevobjectid = readstate.objectid;
                            readstate.objectid = vectordv.getInt32(readstate.maindvpos, true);
                            break;
                        case 4:
                            readstate.maindvpos += 4;
                            var cX = vectordv.getFloat32(readstate.maindvpos, true);
                            readstate.maindvpos += 4;
                            var cY = vectordv.getFloat32(readstate.maindvpos, true);
                            readstate.maindvpos += 4;
                            var cR = vectordv.getFloat32(readstate.maindvpos, true);
                            var circleobject = {
                                layer: readstate.dlayer,
                                drawmode: readstate.drawmode,
                                layerstate: readstate.lstate,
                                pen: readstate.dpen,
                                strokecolor: readstate.scolor,
                                fillcolor: readstate.fcolor,
                                linewidth: readstate.lwidth,
                                cx: cX,
                                cy: cY,
                                cr: cR,
                                filled: 0,
                                entityType : readstate.entityType,
                                role : readstate.role 
                            };
                            var circleobj = new circleObject(circleobject, vectorobj, readstate.blockpart, true, vscope.numpaths);

                            vectorobj.updatepathlist(circleobj, readstate.role);

                            vscope.numpaths++;

                            //vectorobj.pathlist.push(circleobj);
                            //vectorobj.numpaths++;
                            readstate.maindvpos += 4;
                            readstate.prevobjectid = readstate.objectid;
                            readstate.objectid = vectordv.getInt32(readstate.maindvpos, true);
                            break;
                        case 5:
                            readstate.maindvpos += 4;
                            cX = vectordv.getFloat32(readstate.maindvpos, true);
                            readstate.maindvpos += 4;
                            cY = vectordv.getFloat32(readstate.maindvpos, true);
                            readstate.maindvpos += 4;
                            cR = vectordv.getFloat32(readstate.maindvpos, true);
                            circleobject = {
                                layer: readstate.dlayer,
                                drawmode: readstate.drawmode,
                                layerstate: readstate.lstate,
                                pen: readstate.dpen,
                                strokecolor: readstate.scolor,
                                fillcolor: readstate.fcolor,
                                linewidth: readstate.lwidth,
                                cx: cX,
                                cy: cY,
                                cr: cR,
                                filled: 1,
                                entityType : readstate.entityType,
                                role : readstate.role 
                            };
                            circleobj = new circleObject(circleobject, vectorobj, readstate.blockpart, true, vscope.numpaths);
                            
                            vectorobj.updatepathlist(circleobj, readstate.role);

                            vscope.numpaths++;
                            //vectorobj.pathlist.push(circleobj);
                            //vectorobj.numpaths++;

                            readstate.maindvpos += 4;
                            readstate.prevobjectid = readstate.objectid;
                            readstate.objectid = vectordv.getInt32(readstate.maindvpos, true);
                            break;
                        case 6:
                            var numpointarr = [];
                            var subpatharr = [];
                            var subpatharrays = [];
                            //readstate.maindvpos += 4;
                            //var tnumpoints = vectordv.getInt32(readstate.maindvpos,true);
                            readstate.maindvpos += 4;
                            var subpolygons = vectordv.getInt32(readstate.maindvpos, true);
                            //readstate.maindvpos += 4;
                            //var numpoints_1 = vectordv.getInt32(readstate.maindvpos,true);
                            var pcountr = 0;
                            while (pcountr < subpolygons) {
                                readstate.maindvpos += 4;
                                numpointarr.push(vectordv.getInt32(readstate.maindvpos, true));
                                pcountr++;
                            }
                            pcountr = 0;
                            while (pcountr < subpolygons) {
                                for (var pc = 0; pc < numpointarr[pcountr]; pc++) {
                                    readstate.maindvpos += 4;
                                    subpatharr.push(vectordv.getFloat32(readstate.maindvpos, true));
                                    readstate.maindvpos += 4;
                                    subpatharr.push(vectordv.getFloat32(readstate.maindvpos, true));
                                }
                                subpatharrays.push(subpatharr);
                                subpatharr = [];
                                pcountr++;
                            }
                            pathobject = {
                                precision: 0,
                                layer: readstate.dlayer,
                                drawmode: readstate.drawmode,
                                layerstate: readstate.lstate,
                                pen: readstate.dpen,
                                strokecolor: readstate.scolor,
                                fillcolor: readstate.fcolor,
                                linewidth: readstate.lwidth,
                                numpoints: numpointarr[0] * 2,
                                points: subpatharrays,
                                filled: 1,
                                gotsubpath: true,
                                shape: readstate.objectid,
                                entityType : readstate.entityType,
                                role : readstate.role 
                            };
                            pathobj = new pathObject(pathobject, vectorobj, readstate.blockpart, true, vscope.numpaths);
                            //lpoints = [];
                            //lpoints.push(lX1);
                            //lpoints.push(lY1);
                            //cpnts = 1;
                            /*while(cpnts<numpoints){
                             readstate.maindvpos += 4;
                             lpoints.push(vectordv.getFloat32(readstate.maindvpos,true));
                             readstate.maindvpos += 4;
                             lpoints.push(vectordv.getFloat32(readstate.maindvpos,true));
                             cpnts++;
                             }*/
                            //pathobj = new pathObject(pathobject,blockindx,true);
                            vectorobj.updatepathlist(pathobj, readstate.role);
                            vscope.numpaths++;
                            //vectorobj.pathlist.push(pathobj);
                            //vectorobj.numpaths++;
                            readstate.maindvpos += 4;
                            readstate.prevobjectid = readstate.objectid;
                            readstate.objectid = vectordv.getInt32(readstate.maindvpos, true);
                            break;
                        case 7:
                            readstate.maindvpos += 4;
                            lX1 = vectordv.getFloat32(readstate.maindvpos, true);
                            readstate.maindvpos += 4;
                            lY1 = vectordv.getFloat32(readstate.maindvpos, true);
                            readstate.maindvpos += 4;
                            lX2 = vectordv.getFloat32(readstate.maindvpos, true);
                            readstate.maindvpos += 4;
                            lY2 = vectordv.getFloat32(readstate.maindvpos, true);
                            readstate.maindvpos += 4;
                            var imagelength = vectordv.getInt32(readstate.maindvpos, true);
                            readstate.maindvpos += 4;
                            var imagestr = "";
                            var curpos = readstate.maindvpos;
                            while (curpos < imagelength + readstate.maindvpos) {
                                imagestr += String.fromCharCode(vectordv.getUint8(curpos));
                                curpos++;
                            }
                            readstate.maindvpos = curpos;
                            var imageobject = {
                                layer: readstate.dlayer,
                                drawmode: readstate.drawmode,
                                layerstate: readstate.lstate,
                                x1: lX1,
                                y1: lY1,
                                x2: lX2,
                                y2: lY2,
                                image: imagestr,
                                useref: false,
                                entityType : readstate.entityType,
                                role : readstate.role 
                            };
                            var imgobj = new imageObject(imageobject, vectorobj, readstate.blockpart, true, vscope.numpaths);

                            vectorobj.updatepathlist(imgobj, readstate.role);
                            vscope.numpaths++;

                            //vectorobj.pathlist.push(imgobj);
                            //vectorobj.numpaths++;
                            //readstate.maindvpos += 4;
                            readstate.prevobjectid = readstate.objectid;
                            readstate.objectid = vectordv.getInt32(readstate.maindvpos, true);
                            break;
                        case 8:
                            readstate.maindvpos += 4;
                            lX1 = vectordv.getFloat32(readstate.maindvpos, true);
                            readstate.maindvpos += 4;
                            lY1 = vectordv.getFloat32(readstate.maindvpos, true);
                            readstate.maindvpos += 4;
                            lX2 = vectordv.getFloat32(readstate.maindvpos, true);
                            readstate.maindvpos += 4;
                            lY2 = vectordv.getFloat32(readstate.maindvpos, true);
                            readstate.maindvpos += 4;
                            var nameLength = vectordv.getInt32(readstate.maindvpos, true);
                            readstate.maindvpos += 4;
                            imagestr = "";
                            curpos = readstate.maindvpos;
                            while (curpos < nameLength + readstate.maindvpos) {
                                imagestr += String.fromCharCode(vectordv.getUint8(curpos));
                                curpos++;
                            }
                            readstate.maindvpos = curpos;
                            imageobject = {
                                layer: readstate.dlayer,
                                drawmode: readstate.drawmode,
                                layerstate: readstate.lstate,
                                x1: lX1,
                                y1: lY1,
                                x2: lX2,
                                y2: lY2,
                                image: imagestr,
                                useref: true,
                                entityType : readstate.entityType,
                                role : readstate.role 
                            };
                            imgobj = new imageObject(imageobject, vectorobj, readstate.blockpart, true, vscope.numpaths);


                            vectorobj.updatepathlist(imgobj, readstate.role);
                            vscope.numpaths++;


                            //vectorobj.pathlist.push(imgobj);
                            //vectorobj.numpaths++;
                            //readstate.maindvpos += 4;
                            readstate.prevobjectid = readstate.objectid;
                            readstate.objectid = vectordv.getInt32(readstate.maindvpos, true);
                            break;
                        case 10:
                            //Attribute
                            readstate.maindvpos += 4;
                            var nAttrbutenl = parseInt(vectordv.getInt32(readstate.maindvpos, true));
                            readstate.maindvpos += 4;
                            var battrncount = readstate.maindvpos;
                            var szatrrname = "";
                            while (battrncount < readstate.maindvpos + nAttrbutenl * 2) {
                                szatrrname += String.fromCharCode(vectordv.getUint16(battrncount, true));
                                battrncount += 2;
                            }
                            readstate.maindvpos = battrncount;
                            var nAttrbutevl = parseInt(vectordv.getInt32(readstate.maindvpos, true));
                            readstate.maindvpos += 4;
                            var battrvcount = readstate.maindvpos;
                            var szatrtvalue = "";
                            while (battrvcount < readstate.maindvpos + nAttrbutevl * 2) {
                                szatrtvalue += String.fromCharCode(vectordv.getUint16(battrvcount, true));
                                battrvcount += 2;
                            }
                            readstate.maindvpos = battrvcount;
                            var AttributeObject = {
                                name: szatrrname,
                                value: szatrtvalue,
                                blockref: readstate.blockpart
                            };

                            vectorobj.updateAttributes(AttributeObject, readstate.role);

                            //vectorobj.attributes.push(AttributeObject);
                            //vector3Dref.partlist[blockid].addAtrib(szatrrname,szatrtvalue);
                            /*
                             0	4	LONG	Object ID	10
                             4	4	LONG	NameLength		Number of characters in name
                             8	N	CHAR[N]	Name		Attribute Name
                             8+NameLength	4	LONG	ValueLength		Number of characters in value
                             8+NameLen+4	N	CHAR[N]	Value		Attribute Value
        
                             */
                            readstate.prevobjectid = readstate.objectid;
                            readstate.objectid = parseInt(vectordv.getInt32(readstate.maindvpos, true));
                            break;
                        case 11:
                            readstate.maindvpos += 4;
                            var ltX1 = vectordv.getFloat32(readstate.maindvpos, true);
                            readstate.maindvpos += 4;
                            var ltY1 = vectordv.getFloat32(readstate.maindvpos, true);
                            readstate.maindvpos += 4;
                            var ltX2 = vectordv.getFloat32(readstate.maindvpos, true);
                            readstate.maindvpos += 4;
                            var ltY2 = vectordv.getFloat32(readstate.maindvpos, true);
                            readstate.maindvpos += 4;
                            //vscope
                            if (vscope.fileversion >= 2) {
                                var ltrot = vectordv.getFloat32(readstate.maindvpos, true);
                                readstate.maindvpos += 4;
                            }
                            else {
                                ltrot = 0;
                            }
                            if (ltrot != 0) {
                                //console.log(ltrot);
                            }
                            var nTextLength = vectordv.getInt32(readstate.maindvpos, true);
                            nTextLength *= 2;
                            readstate.maindvpos += 4;
                            var textncount = readstate.maindvpos;
                            var sztextobj = "";
                            while (textncount < readstate.maindvpos + nTextLength) {
                                sztextobj += String.fromCharCode(vectordv.getUint16(textncount, true));
                                textncount += 2;
                            }
                            readstate.maindvpos = textncount;
                            var textobject = {
                                layer: readstate.dlayer,
                                drawmode: readstate.drawmode,
                                layerstate: readstate.lstate,
                                x1: ltX1,
                                y1: ltY1,
                                x2: ltX2,
                                y2: ltY2,
                                rot: ltrot,
                                text: sztextobj,
                                draw: false
                            };
                            
                            
                            //vectorobj.textlist.push(textobject);


                            vectorobj.updatetextlist(textobject, readstate.role);
                            


                            readstate.prevobjectid = readstate.objectid;
                            readstate.objectid = vectordv.getInt32(readstate.maindvpos, true);
                            break;
                        case 12:
                            readstate.maindvpos += 4;
                            var ltoX1 = vectordv.getFloat32(readstate.maindvpos, true);
                            readstate.maindvpos += 4;
                            var lt0Y1 = vectordv.getFloat32(readstate.maindvpos, true);
                            readstate.maindvpos += 4;
                            nTextLength = vectordv.getInt32(readstate.maindvpos, true);
                            nTextLength *= 2;
                            readstate.maindvpos += 4;
                            textncount = readstate.maindvpos;
                            sztextobj = "";
                            while (textncount < readstate.maindvpos + nTextLength) {
                                sztextobj += String.fromCharCode(vectordv.getUint16(textncount, true));
                                textncount += 2;
                            }
                            readstate.maindvpos = textncount;
                            var dtextobject = {
                                strokecolor: readstate.scolor,
                                fillcolor: readstate.fcolor,
                                layer: readstate.dlayer,
                                pen: readstate.dpen,
                                drawmode: readstate.drawmode,
                                layerstate: readstate.lstate,
                                x1: ltoX1,
                                y1: lt0Y1,
                                text: sztextobj,
                                entityType : readstate.entityType,
                                role : readstate.role 
                            };
                            var textobj = new textObject(dtextobject, vectorobj, readstate.font, readstate.blockpart, vscope.numpaths);


                            vectorobj.updatepathlist(textobj, readstate.role);
                            vscope.numpaths++;


                            //vectorobj.pathlist.push(textobj);
                            //vectorobj.numpaths++;
                            readstate.prevobjectid = readstate.objectid;
                            readstate.objectid = vectordv.getInt32(readstate.maindvpos, true);
                            break;
                        case 14: //line array     
                            readstate.maindvpos += 4;
                            var numlines = vectordv.getInt32(readstate.maindvpos, true);
                            var linearray = [];
                            readstate.maindvpos += 4;

                            for (var ln = 0; ln < numlines; ln++){
                                var x1 = vectordv.getFloat32(readstate.maindvpos, true);
                                readstate.maindvpos += 4;
                                var y1 = vectordv.getFloat32(readstate.maindvpos, true);
                                readstate.maindvpos += 4;
                                var x2 = vectordv.getFloat32(readstate.maindvpos, true);
                                readstate.maindvpos += 4;
                                var y2 = vectordv.getFloat32(readstate.maindvpos, true);
                                readstate.maindvpos += 4;
                                
                                linearray.push({x1:x1,y1 :y1 ,x2: x2,y2: y2});

                            }
                            

                            var linearrayobject = {
                                layer: readstate.dlayer,
                                drawmode: readstate.drawmode,
                                layerstate: readstate.lstate,
                                pen: readstate.dpen,
                                strokecolor: readstate.scolor,
                                fillcolor: readstate.fcolor,
                                linewidth: readstate.lwidth,
                                lines : linearray,
                                entityType : readstate.entityType,
                                role : readstate.role 
                            };

                            var linearrobj = new lineArrayObject(linearrayobject, vectorobj, readstate.blockpart, true, vscope.numpaths);


                            vectorobj.updatepathlist(linearrobj, readstate.role);
                            vscope.numpaths++;


                            readstate.prevobjectid = readstate.objectid;
                            readstate.objectid = vectordv.getInt32(readstate.maindvpos, true);
                            break;

                        case 15: //dot array double
                            readstate.maindvpos += 4;
                            var numlines = vectordv.getInt32(readstate.maindvpos, true);
                            var dotarray = [];
                            readstate.maindvpos += 4;

                            for (var ln = 0; ln < numlines; ln++){
                                var x = vectordv.getFloat32(readstate.maindvpos, true);
                                readstate.maindvpos += 4;
                                var y = vectordv.getFloat32(readstate.maindvpos, true);
                                readstate.maindvpos += 4;

                                
                                dotarray.push({x : x,y : y});

                            }

                            var dotarrayobject = {
                                layer: readstate.dlayer,
                                drawmode: readstate.drawmode,
                                layerstate: readstate.lstate,
                                pen: readstate.dpen,
                                strokecolor: readstate.scolor,
                                fillcolor: readstate.fcolor,
                                linewidth: readstate.lwidth,
                                dots : dotarray,
                                entityType : readstate.entityType,
                                role : readstate.role
                            };

                            var dotarr = new dotArrayObject(dotarrayobject, vectorobj, readstate.blockpart, true, vscope.numpaths);


                            vectorobj.updatepathlist(dotarr, readstate.role);
                            vscope.numpaths++;


                            readstate.prevobjectid = readstate.objectid;
                            readstate.objectid = vectordv.getInt32(readstate.maindvpos, true);   
                            break;                                    

                        case 16: //block insert float
                            readstate.maindvpos += 4;
                            var blockhandleLow = vectordv.getInt32(readstate.maindvpos, true);
                            readstate.maindvpos += 4;
                            var blockhandleHigh = vectordv.getInt32(readstate.maindvpos, true);
                            readstate.maindvpos += 4;
                            var insertX = vectordv.getFloat32(readstate.maindvpos, true);
                            readstate.maindvpos += 4;
                            var insertY = vectordv.getFloat32(readstate.maindvpos, true);
                            readstate.maindvpos += 4;
                            var insertZ = vectordv.getFloat32(readstate.maindvpos, true);
                            readstate.maindvpos += 4;
                            var insertRot = vectordv.getFloat32(readstate.maindvpos, true);
                            readstate.maindvpos += 4;
                            var insertscaleX = vectordv.getFloat32(readstate.maindvpos, true);
                            readstate.maindvpos += 4;
                            var insertscaleY = vectordv.getFloat32(readstate.maindvpos, true);
                            readstate.maindvpos += 4;
                            var insertscaleZ = vectordv.getFloat32(readstate.maindvpos, true);
                            readstate.maindvpos += 4;

                            if (vscope.fileversion >= 5) {
                                var minx = vectordv.getFloat32(readstate.maindvpos, true);
                                readstate.maindvpos += 4;
                                var miny = vectordv.getFloat32(readstate.maindvpos, true);
                                readstate.maindvpos += 4;
                                var maxx = vectordv.getFloat32(readstate.maindvpos, true);
                                readstate.maindvpos += 4;
                                var maxy = vectordv.getFloat32(readstate.maindvpos, true);
                                readstate.maindvpos += 4;

                            }else{
                                minx = 0;
                                miny = 0;
                                maxx = 0;
                                maxy = 0
                            }

                            
                            var blockinsertobj = {

                                blockhandleLow : blockhandleLow,
                                blockhandleHigh : blockhandleHigh,
                                insertX : insertX,
                                insertY : insertY,
                                insertZ : insertZ,
                                insertRot : insertRot,
                                insertscaleX : insertscaleX,
                                insertscaleY : insertscaleY,
                                insertscaleZ : insertscaleZ,
                                blockref: readstate.blockpart,
                                MinX : minx,
                                MinY : miny,
                                MaxX : maxx,
                                MaxY : maxy,
                                type : 'float'
                            }
                            
                            vectorobj.updateBlockInsert(readstate.blockid, blockinsertobj);

                            readstate.prevobjectid = readstate.objectid;
                            readstate.objectid = vectordv.getInt32(readstate.maindvpos, true);

                            break;
                        case 21: //double line.
                            readstate.maindvpos += 4;
                            lX1 = vectordv.getFloat64(readstate.maindvpos, true);
                            readstate.maindvpos += 8;
                            lY1 = vectordv.getFloat64(readstate.maindvpos, true);
                            readstate.maindvpos += 8;
                            lX2 = vectordv.getFloat64(readstate.maindvpos, true);
                            readstate.maindvpos += 8;
                            lY2 = vectordv.getFloat64(readstate.maindvpos, true);
                            lineobject = {
                                layer: readstate.dlayer,
                                drawmode: readstate.drawmode,
                                layerstate: readstate.lstate,
                                pen: readstate.dpen,
                                strokecolor: readstate.scolor,
                                fillcolor: readstate.fcolor,
                                linewidth: readstate.lwidth,
                                x1: lX1,
                                y1: lY1,
                                x2: lX2,
                                y2: lY2,
                                entityType : readstate.entityType,
                                role : readstate.role 
                            };
                            lineobj = new lineObject(lineobject, vectorobj, readstate.blockpart, true, vscope.numpaths);

                            vectorobj.updatepathlist(lineobj, readstate.role);
                            vscope.numpaths++;

                            //vectorobj.pathlist.push(lineobj);
                            //vectorobj.numpaths++;
                            readstate.maindvpos += 8;
                            readstate.prevobjectid = readstate.objectid;
                            readstate.objectid = vectordv.getInt32(readstate.maindvpos, true);
                            break;
                        case 22: //double polyline.
                            readstate.maindvpos += 4;
                            numpoints = vectordv.getInt32(readstate.maindvpos, true);
                            readstate.maindvpos += 4;
                            lX1 = vectordv.getFloat64(readstate.maindvpos, true);
                            readstate.maindvpos += 8;
                            lY1 = vectordv.getFloat64(readstate.maindvpos, true);
                            lpoints = [];
                            lpoints.push(lX1);
                            lpoints.push(lY1);
                            cpnts = 1;
                            while (cpnts < numpoints) {
                                readstate.maindvpos += 8;
                                lpoints.push(vectordv.getFloat64(readstate.maindvpos, true));
                                readstate.maindvpos += 8;
                                lpoints.push(vectordv.getFloat64(readstate.maindvpos, true));
                                cpnts++;
                            }
                            pathobject = {
                                precision: 1,
                                layer: readstate.dlayer,
                                drawmode: readstate.drawmode,
                                layerstate: readstate.lstate,
                                pen: readstate.dpen,
                                strokecolor: readstate.scolor,
                                fillcolor: readstate.fcolor,
                                linewidth: readstate.lwidth,
                                numpoints: numpoints * 2,
                                points: lpoints,
                                filled: 0,
                                gotsubpath: false,
                                shape: readstate.objectid,
                                entityType : readstate.entityType,
                                role : readstate.role 
                            };
                            pathobj = new pathObject(pathobject, vectorobj, readstate.blockpart, true, vscope.numpaths);

                            vectorobj.updatepathlist(pathobj, readstate.role);
                            vscope.numpaths++;

                            //vectorobj.pathlist.push(pathobj);
                            //vectorobj.numpaths++;
                            readstate.maindvpos += 8;
                            readstate.prevobjectid = readstate.objectid;
                            readstate.objectid = vectordv.getInt32(readstate.maindvpos, true);
                            break;
                        case 23: //double polygon.
                            readstate.maindvpos += 4;
                            numpoints = vectordv.getInt32(readstate.maindvpos, true);
                            readstate.maindvpos += 4;
                            lX1 = vectordv.getFloat64(readstate.maindvpos, true);
                            readstate.maindvpos += 8;
                            lY1 = vectordv.getFloat64(readstate.maindvpos, true);
                            lpoints = [];
                            lpoints.push(lX1);
                            lpoints.push(lY1);
                            cpnts = 1;
                            while (cpnts < numpoints) {
                                readstate.maindvpos += 8;
                                lpoints.push(vectordv.getFloat64(readstate.maindvpos, true));
                                readstate.maindvpos += 8;
                                lpoints.push(vectordv.getFloat64(readstate.maindvpos, true));
                                cpnts++;
                            }
                            pathobject = {
                                precision: 1,
                                layer: readstate.dlayer,
                                drawmode: readstate.drawmode,
                                layerstate: readstate.lstate,
                                pen: readstate.dpen,
                                strokecolor: readstate.scolor,
                                fillcolor: readstate.fcolor,
                                linewidth: readstate.lwidth,
                                numpoints: numpoints * 2,
                                points: lpoints,
                                filled: 1,
                                gotsubpath: false,
                                shape: readstate.objectid,
                                entityType : readstate.entityType,
                                role : readstate.role 
                            };
                            pathobj = new pathObject(pathobject, vectorobj, readstate.blockpart, true, vscope.numpaths);

                            vectorobj.updatepathlist(pathobj, readstate.role);
                            vscope.numpaths++;

                            //vectorobj.pathlist.push(pathobj);
                            //vectorobj.numpaths++;
                            readstate.maindvpos += 8;
                            readstate.prevobjectid = readstate.objectid;
                            readstate.objectid = vectordv.getInt32(readstate.maindvpos, true);
                            break;
                        case 24: //double outline circle.
                            readstate.maindvpos += 4;
                            cX = vectordv.getFloat64(readstate.maindvpos, true);
                            readstate.maindvpos += 8;
                            cY = vectordv.getFloat64(readstate.maindvpos, true);
                            readstate.maindvpos += 8;
                            cR = vectordv.getFloat64(readstate.maindvpos, true);
                            circleobject = {
                                layer: readstate.dlayer,
                                drawmode: readstate.drawmode,
                                layerstate: readstate.lstate,
                                pen: readstate.dpen,
                                strokecolor: readstate.scolor,
                                fillcolor: readstate.fcolor,
                                linewidth: readstate.lwidth,
                                cx: cX,
                                cy: cY,
                                cr: cR,
                                filled: 0,
                                entityType : readstate.entityType,
                                role : readstate.role 
                            };
                            circleobj = new circleObject(circleobject, vectorobj, readstate.blockpart, true, vscope.numpaths);

                            vectorobj.updatepathlist(circleobj, readstate.role);
                            vscope.numpaths++;

                            //vectorobj.pathlist.push(circleobj);
                            //vectorobj.numpaths++;
                            readstate.maindvpos += 8;
                            readstate.prevobjectid = readstate.objectid;
                            readstate.objectid = vectordv.getInt32(readstate.maindvpos, true);
                            break;
                        case 34:
                            readstate.maindvpos += 4;
                            aX = vectordv.getFloat64(readstate.maindvpos, true);
                            readstate.maindvpos += 8;
                            aY = vectordv.getFloat64(readstate.maindvpos, true);
                            readstate.maindvpos += 8;
                            aR = vectordv.getFloat64(readstate.maindvpos, true);
                            readstate.maindvpos += 8;
                            aSa = vectordv.getFloat64(readstate.maindvpos, true);
                            readstate.maindvpos += 8;
                            aEa = vectordv.getFloat64(readstate.maindvpos, true);
                            arcobject = {
                                layer: readstate.dlayer,
                                drawmode: readstate.drawmode,
                                layerstate: readstate.lstate,
                                pen: readstate.dpen,
                                strokecolor: readstate.scolor,
                                fillcolor: readstate.fcolor,
                                linewidth: readstate.lwidth,
                                clockwise: (aEa > 0),
                                ax: aX,
                                ay: aY,
                                ar: aR,
                                asa: aSa,
                                aea: aSa + aEa,
                                entityType : readstate.entityType,
                                role : readstate.role 
                            };
                            arcobj = new arcObject(arcobject, vectorobj, readstate.blockpart, true, vscope.numpaths);

                            vectorobj.updatepathlist(arcobj, readstate.role);
                            vscope.numpaths++;

                            //vectorobj.pathlist.push(arcobj);
                            //vectorobj.numpaths++;
                            readstate.maindvpos += 8;
                            readstate.prevobjectid = readstate.objectid;
                            readstate.objectid = vectordv.getInt32(readstate.maindvpos, true);
                            break;
                        case 25: //double filled circle.
                            readstate.maindvpos += 4;
                            cX = vectordv.getFloat64(readstate.maindvpos, true);
                            readstate.maindvpos += 8;
                            cY = vectordv.getFloat64(readstate.maindvpos, true);
                            readstate.maindvpos += 8;
                            cR = vectordv.getFloat64(readstate.maindvpos, true);
                            circleobject = {
                                layer: readstate.dlayer,
                                drawmode: readstate.drawmode,
                                layerstate: readstate.lstate,
                                pen: readstate.dpen,
                                strokecolor: readstate.scolor,
                                fillcolor: readstate.fcolor,
                                linewidth: readstate.lwidth,
                                cx: cX,
                                cy: cY,
                                cr: cR,
                                filled: 1,
                                entityType : readstate.entityType,
                                role : readstate.role 
                            };
                            circleobj = new circleObject(circleobject, vectorobj, readstate.blockpart, true, vscope.numpaths);
                            vectorobj.updatepathlist(circleobj, readstate.role);
                            vscope.numpaths++;

                            //vectorobj.pathlist.push(circleobj);
                            //vectorobj.numpaths++;
                            readstate.maindvpos += 8;
                            readstate.prevobjectid = readstate.objectid;
                            readstate.objectid = vectordv.getInt32(readstate.maindvpos, true);
                            break;
                        case 26: //double polypolygon.
                            numpointarr = [];
                            subpatharr = [];
                            subpatharrays = [];
                            readstate.maindvpos += 4;
                            subpolygons = vectordv.getInt32(readstate.maindvpos, true);
                            pcountr = 0;
                            while (pcountr < subpolygons) {
                                readstate.maindvpos += 4;
                                numpointarr.push(vectordv.getInt32(readstate.maindvpos, true));
                                pcountr++;
                            }
                            readstate.maindvpos += 4;
                            pcountr = 0;
                            while (pcountr < subpolygons) {
                                for (pc = 0; pc < numpointarr[pcountr]; pc++) {
                                    /*if(pc == 0){
                                        readstate.maindvpos += 4;
                                    }else{
                                        readstate.maindvpos += 8;
                                    }*/
                                    subpatharr.push(vectordv.getFloat64(readstate.maindvpos, true));
                                    readstate.maindvpos += 8;
                                    subpatharr.push(vectordv.getFloat64(readstate.maindvpos, true));
                                    readstate.maindvpos += 8;
                                }
                                subpatharrays.push(subpatharr);
                                subpatharr = [];
                                pcountr++;
                            }
                            pathobject = {
                                precision: 1,
                                layer: readstate.dlayer,
                                drawmode: readstate.drawmode,
                                layerstate: readstate.lstate,
                                pen: readstate.dpen,
                                strokecolor: readstate.scolor,
                                fillcolor: readstate.fcolor,
                                linewidth: readstate.lwidth,
                                numpoints: numpointarr[0] * 2,
                                points: subpatharrays,
                                filled: 1,
                                gotsubpath: true,
                                shape: readstate.objectid,
                                entityType : readstate.entityType,
                                role : readstate.role 
                            };
                            pathobj = new pathObject(pathobject, vectorobj, readstate.blockpart, true, vscope.numpaths);

                            vectorobj.updatepathlist(pathobj, readstate.role);
                            vscope.numpaths++;

                            //vectorobj.pathlist.push(pathobj);
                            //vectorobj.numpaths++;
                            //readstate.maindvpos += 8;
                            readstate.prevobjectid = readstate.objectid;
                            readstate.objectid = vectordv.getInt32(readstate.maindvpos, true);
                            break;
                        case 27: //double image embedded.
                            readstate.maindvpos += 4;
                            lX1 = vectordv.getFloat64(readstate.maindvpos, true);
                            readstate.maindvpos += 8;
                            lY1 = vectordv.getFloat64(readstate.maindvpos, true);
                            readstate.maindvpos += 8;
                            lX2 = vectordv.getFloat64(readstate.maindvpos, true);
                            readstate.maindvpos += 8;
                            lY2 = vectordv.getFloat64(readstate.maindvpos, true);
                            readstate.maindvpos += 8;
                            imagelength = vectordv.getInt32(readstate.maindvpos, true);
                            readstate.maindvpos += 4;
                            imagestr = "";
                            curpos = readstate.maindvpos;
                            while (curpos < imagelength + readstate.maindvpos) {
                                imagestr += String.fromCharCode(vectordv.getUint8(curpos));
                                curpos++;
                            }
                            readstate.maindvpos = curpos;
                            imageobject = {
                                layer: readstate.dlayer,
                                drawmode: readstate.drawmode,
                                layerstate: readstate.lstate,
                                x1: lX1,
                                y1: lY1,
                                x2: lX2,
                                y2: lY2,
                                image: imagestr,
                                useref: false,
                                entityType : readstate.entityType,
                                role : readstate.role 
                            };
                            imgobj = new imageObject(imageobject, vectorobj, readstate.blockpart, true, vscope.numpaths);

                            vectorobj.updatepathlist(imgobj, readstate.role);
                            vscope.numpaths++;



                            //vectorobj.pathlist.push(imgobj);
                            //vectorobj.numpaths++;
                            //readstate.maindvpos += 4;
                            readstate.prevobjectid = readstate.objectid;
                            readstate.objectid = vectordv.getInt32(readstate.maindvpos, true);
                            break;
                        case 28: //double image referenced.
                            readstate.maindvpos += 4;
                            lX1 = vectordv.getFloat64(readstate.maindvpos, true);
                            readstate.maindvpos += 8;
                            lY1 = vectordv.getFloat64(readstate.maindvpos, true);
                            readstate.maindvpos += 8;
                            lX2 = vectordv.getFloat64(readstate.maindvpos, true);
                            readstate.maindvpos += 8;
                            lY2 = vectordv.getFloat64(readstate.maindvpos, true);
                            readstate.maindvpos += 8;
                            nameLength = vectordv.getInt32(readstate.maindvpos, true);
                            readstate.maindvpos += 4;
                            imagestr = "";
                            curpos = readstate.maindvpos;
                            while (curpos < nameLength + readstate.maindvpos) {
                                imagestr += String.fromCharCode(vectordv.getUint8(curpos));
                                curpos++;
                            }
                            readstate.maindvpos = curpos;
                            imageobject = {
                                layer: readstate.dlayer,
                                drawmode: readstate.drawmode,
                                layerstate: readstate.lstate,
                                x1: lX1,
                                y1: lY1,
                                x2: lX2,
                                y2: lY2,
                                image: imagestr,
                                useref: true,
                                entityType : readstate.entityType,
                                role : readstate.role 
                            };
                            imgobj = new imageObject(imageobject, vectorobj, readstate.blockpart, true, vscope.numpaths);

                            vectorobj.updatepathlist(imgobj, readstate.role);
                            vscope.numpaths++;


                            //vectorobj.pathlist.push(imgobj);
                            //vectorobj.numpaths++;
                            //readstate.maindvpos += 4;
                            readstate.prevobjectid = readstate.objectid;
                            readstate.objectid = vectordv.getInt32(readstate.maindvpos, true);
                            break;
                        case 31: //double text element.
                            readstate.maindvpos += 4;
                            ltX1 = vectordv.getFloat64(readstate.maindvpos, true);
                            readstate.maindvpos += 8;
                            ltY1 = vectordv.getFloat64(readstate.maindvpos, true);
                            readstate.maindvpos += 8;
                            ltX2 = vectordv.getFloat64(readstate.maindvpos, true);
                            readstate.maindvpos += 8;
                            ltY2 = vectordv.getFloat64(readstate.maindvpos, true);
                            readstate.maindvpos += 8;
                            if (vscope.fileversion >= 2) {
                                ltrot = vectordv.getFloat64(readstate.maindvpos, true);
                                readstate.maindvpos += 8;
                            }
                            else {
                                ltrot = 0;
                            }
                            if (ltrot != 0) {
                                //console.log(ltrot);
                            }
                            nTextLength = vectordv.getInt32(readstate.maindvpos, true);
                            nTextLength *= 2;
                            readstate.maindvpos += 4;
                            textncount = readstate.maindvpos;
                            sztextobj = "";
                            while (textncount < readstate.maindvpos + nTextLength) {
                                sztextobj += String.fromCharCode(vectordv.getUint16(textncount, true));
                                textncount += 2;
                            }
                            readstate.maindvpos = textncount;
                            textobject = {
                                layer: readstate.dlayer,
                                drawmode: readstate.drawmode,
                                layerstate: readstate.lstate,
                                x1: ltX1,
                                y1: ltY1,
                                x2: ltX2,
                                y2: ltY2,
                                rot: ltrot,
                                text: sztextobj,
                                draw: false
                            };
                            
                            //vectorobj.textlist.push(textobject);
                            
                            //vectorobj.updatepathlist(textobject, readstate.role);
                            vectorobj.updatetextlist(textobject, readstate.role);

                            
                            readstate.prevobjectid = readstate.objectid;
                            readstate.objectid = vectordv.getInt32(readstate.maindvpos, true);




                            break;
                        case 32: //double text output.
                            readstate.maindvpos += 4;
                            ltoX1 = vectordv.getFloat64(readstate.maindvpos, true);
                            readstate.maindvpos += 8;
                            lt0Y1 = vectordv.getFloat64(readstate.maindvpos, true);
                            readstate.maindvpos += 8;
                            nTextLength = vectordv.getInt32(readstate.maindvpos, true);
                            nTextLength *= 2;
                            readstate.maindvpos += 4;
                            textncount = readstate.maindvpos;
                            sztextobj = "";
                            while (textncount < readstate.maindvpos + nTextLength) {
                                sztextobj += String.fromCharCode(vectordv.getUint16(textncount, true));
                                textncount += 2;
                            }
                            readstate.maindvpos = textncount;
                            dtextobject = {
                                strokecolor: readstate.scolor,
                                fillcolor: readstate.fcolor,
                                layer: readstate.dlayer,
                                pen: readstate.dpen,
                                drawmode: readstate.drawmode,
                                layerstate: readstate.lstate,
                                x1: ltoX1,
                                y1: lt0Y1,
                                text: sztextobj,
                                entityType : readstate.entityType,
                                role : readstate.role 
                            };
                            textobj = new textObject(dtextobject, vectorobj, readstate.font, readstate.blockpart, vscope.numpaths);

                            vectorobj.updatepathlist(textobj, readstate.role);
                            vscope.numpaths++;

                            //vectorobj.pathlist.push(textobj);
                            //vectorobj.numpaths++;
                            readstate.prevobjectid = readstate.objectid;
                            readstate.objectid = vectordv.getInt32(readstate.maindvpos, true);
                            break;

                        case 35: //line array double
                            readstate.maindvpos += 4;
                            var numlines = vectordv.getInt32(readstate.maindvpos, true);
                            var linearray = [];
                            readstate.maindvpos += 4;

                            for (var ln = 0; ln < numlines; ln++){
                                var x1 = vectordv.getFloat64(readstate.maindvpos, true);
                                readstate.maindvpos += 8;
                                var y1 = vectordv.getFloat64(readstate.maindvpos, true);
                                readstate.maindvpos += 8;
                                var x2 = vectordv.getFloat64(readstate.maindvpos, true);
                                readstate.maindvpos += 8;
                                var y2 = vectordv.getFloat64(readstate.maindvpos, true);
                                readstate.maindvpos += 8;
                                
                                linearray.push({x1:x1,y1 :y1 ,x2: x2,y2: y2});

                            }

                            linearrayobject = {
                                layer: readstate.dlayer,
                                drawmode: readstate.drawmode,
                                layerstate: readstate.lstate,
                                pen: readstate.dpen,
                                strokecolor: readstate.scolor,
                                fillcolor: readstate.fcolor,
                                linewidth: readstate.lwidth,
                                lines : linearray,
                                entityType : readstate.entityType,
                                role : readstate.role 
                            };

                            

                            linearrobj = new lineArrayObject(linearrayobject, vectorobj, readstate.blockpart, true, vscope.numpaths);

                            vectorobj.updatepathlist(linearrobj, readstate.role);
                            vscope.numpaths++;


                            readstate.prevobjectid = readstate.objectid;
                            readstate.objectid = vectordv.getInt32(readstate.maindvpos, true);   
                            break;

                        case 36: //dot array double
                            readstate.maindvpos += 4;
                            var numlines = vectordv.getInt32(readstate.maindvpos, true);
                            var dotarray = [];
                            readstate.maindvpos += 4;

                            for (var ln = 0; ln < numlines; ln++){
                                var x = vectordv.getFloat64(readstate.maindvpos, true);
                                readstate.maindvpos += 8;
                                var y = vectordv.getFloat64(readstate.maindvpos, true);
                                readstate.maindvpos += 8;

                                
                                dotarray.push({x : x,y : y});

                            }

                            dotarrayobject = {
                                layer: readstate.dlayer,
                                drawmode: readstate.drawmode,
                                layerstate: readstate.lstate,
                                pen: readstate.dpen,
                                strokecolor: readstate.scolor,
                                fillcolor: readstate.fcolor,
                                linewidth: readstate.lwidth,
                                dots : dotarray,
                                entityType : readstate.entityType,
                                role : readstate.role 
                            };

                            dotarr = new dotArrayObject(dotarrayobject, vectorobj, readstate.blockpart, true, vscope.numpaths);


                            

                            vectorobj.updatepathlist(dotarr, readstate.role);
                            vscope.numpaths++;




                            readstate.prevobjectid = readstate.objectid;
                            readstate.objectid = vectordv.getInt32(readstate.maindvpos, true);   
                            break;                            
                        case 37: //block insert double     

                            readstate.maindvpos += 4;
                            var blockhandleLow = vectordv.getInt32(readstate.maindvpos, true);
                            readstate.maindvpos += 4;
                            var blockhandleHigh = vectordv.getInt32(readstate.maindvpos, true);
                            readstate.maindvpos += 4;
                            var insertX = vectordv.getFloat64(readstate.maindvpos, true);
                            readstate.maindvpos += 8;
                            var insertY = vectordv.getFloat64(readstate.maindvpos, true);
                            readstate.maindvpos += 8;
                            var insertZ = vectordv.getFloat64(readstate.maindvpos, true);
                            readstate.maindvpos += 8;
                            var insertRot = vectordv.getFloat64(readstate.maindvpos, true);
                            readstate.maindvpos += 8;
                            var insertscaleX = vectordv.getFloat64(readstate.maindvpos, true);
                            readstate.maindvpos += 8;
                            var insertscaleY = vectordv.getFloat64(readstate.maindvpos, true);
                            readstate.maindvpos += 8;
                            var insertscaleZ = vectordv.getFloat64(readstate.maindvpos, true);
                            readstate.maindvpos += 8;

                            if (vscope.fileversion >= 5) {
                                var minx = vectordv.getFloat64(readstate.maindvpos, true);
                                readstate.maindvpos += 8;
                                var miny = vectordv.getFloat64(readstate.maindvpos, true);
                                readstate.maindvpos += 8;
                                var maxx = vectordv.getFloat64(readstate.maindvpos, true);
                                readstate.maindvpos += 8;
                                var maxy = vectordv.getFloat64(readstate.maindvpos, true);
                                readstate.maindvpos += 8;

                            }else{
                                minx = 0;
                                miny = 0;
                                maxx = 0;
                                maxy = 0
                            }

                            
                            var blockinsertobj = {

                                blockhandleLow : blockhandleLow,
                                blockhandleHigh : blockhandleHigh,
                                insertX : insertX,
                                insertY : insertY,
                                insertZ : insertZ,
                                insertRot : insertRot,
                                insertscaleX : insertscaleX,
                                insertscaleY : insertscaleY,
                                insertscaleZ : insertscaleZ,
                                blockref: readstate.blockpart,
                                MinX : minx,
                                MinY : miny,
                                MaxX : maxx,
                                MaxY : maxy,
                                type : 'dobule'
                            }

                           
                            
                            vectorobj.updateBlockInsert(readstate.blockid, blockinsertobj);


                            readstate.prevobjectid = readstate.objectid;
                            readstate.objectid = vectordv.getInt32(readstate.maindvpos, true);

                            break;
                        case 64:
                            readstate.maindvpos += 4;
                            var ndrawcolor = vectordv.getInt32(readstate.maindvpos, true);
                            readstate.scolor = decimalToHex(ndrawcolor);
                            readstate.maindvpos += 4;
                            readstate.prevobjectid = readstate.objectid;
                            readstate.objectid = vectordv.getInt32(readstate.maindvpos, true);
                            break;
                        case 65:
                            readstate.maindvpos += 4;
                            var nfillcolor = vectordv.getInt32(readstate.maindvpos, true);
                            readstate.fcolor = decimalToHex(nfillcolor);
                            readstate.maindvpos += 4;
                            readstate.prevobjectid = readstate.objectid;
                            readstate.objectid = vectordv.getInt32(readstate.maindvpos, true);
                            break;
                        case 66:
                            readstate.maindvpos += 4;
                            readstate.lwidth = vectordv.getFloat32(readstate.maindvpos, true);
                            readstate.maindvpos += 4;
                            readstate.prevobjectid = readstate.objectid;
                            readstate.objectid = vectordv.getInt32(readstate.maindvpos, true);
                            break;
                        case 67:
                            readstate.maindvpos += 4;
                            var linestyle = vectordv.getInt32(readstate.maindvpos, true);
                            readstate.maindvpos += 4;
                            readstate.prevobjectid = readstate.objectid;
                            readstate.objectid = vectordv.getInt32(readstate.maindvpos, true);
                            break;
                        case 68:
                            readstate.maindvpos += 4;
                            readstate.dlayer = vectordv.getInt32(readstate.maindvpos, true);

                            /*if(!readstate.badd){
                                console.log(readstate.dlayer);
                            }*/
                            
                            readstate.maindvpos += 4;
                            readstate.prevobjectid = readstate.objectid;
                            readstate.objectid = vectordv.getInt32(readstate.maindvpos, true);
                            /*for(var lc = 0;lc<vectorobj.layerlist.length;lc++){
                             if (vectorobj.layerlist[lc].index == readstate.dlayer){
                             readstate.lstate = vectorobj.layerlist[lc].state;
                             }
                             }*/
                            break;
                        case 69:
                            readstate.maindvpos += 4;
                            readstate.dpen = vectordv.getInt32(readstate.maindvpos, true);

                            if(vectorobj.pentable.existSetcurrent(readstate.dpen)){

                                vectorobj.pentable.getCurrentPen().setOriginalwidth(readstate.lwidth);
                                vectorobj.pentable.getCurrentPen().setdisplaywidth(readstate.lwidth);
                                vectorobj.pentable.getCurrentPen().setOriginalcolor(readstate.scolor);

                                //vectorobj.pentable.getPen(readstate.dpen).setpenwidth

                            }else{
                               if(readstate.dpen >= 0){
                                    vectorobj.pentable.add(new PenTableObject(readstate.dpen, vectorobj.pentable.getcurIndex()));
                                    vectorobj.pentable.getCurrentPen().setOriginalwidth(readstate.lwidth);
                                    vectorobj.pentable.getCurrentPen().setdisplaywidth(readstate.lwidth);
                                    vectorobj.pentable.getCurrentPen().setOriginalcolor(readstate.scolor);

                               }  

                                //vectorobj.pentable.getCurrentPen.
                            }                            

                            readstate.maindvpos += 4;
                            readstate.prevobjectid = readstate.objectid;
                            readstate.objectid = vectordv.getInt32(readstate.maindvpos, true);
                            break;
                        case 70:
                            readstate.maindvpos += 4;
                            lineweight = vectordv.getInt32(readstate.maindvpos, true);
                            readstate.maindvpos += 4;
                            readstate.prevobjectid = readstate.objectid;
                            readstate.objectid = vectordv.getInt32(readstate.maindvpos, true);
                            break;
                        case 71:
                            var drawbackcolor = true;
                            readstate.maindvpos += 4;
                            readstate.prevobjectid = readstate.objectid;
                            readstate.objectid = vectordv.getInt32(readstate.maindvpos, true);
                            break;
                        case 72:
                            var fillbackcolor = true;
                            readstate.maindvpos += 4;
                            readstate.prevobjectid = readstate.objectid;
                            readstate.objectid = vectordv.getInt32(readstate.maindvpos, true);
                            break;
                        case 73:
                            readstate.maindvpos += 4;
                            var drawmode = vectordv.getInt32(readstate.maindvpos, true);
                            readstate.drawmode = drawmode;
                            readstate.maindvpos += 4;
                            readstate.prevobjectid = readstate.objectid;
                            readstate.objectid = vectordv.getInt32(readstate.maindvpos, true);
                            break;
                        case 76:
                            readstate.maindvpos += 4;
                            var fheight = vectordv.getFloat32(readstate.maindvpos, true);
                            readstate.maindvpos += 4;
                            var frotation = vectordv.getFloat32(readstate.maindvpos, true);
                            readstate.maindvpos += 4;
                            var fweight = vectordv.getInt32(readstate.maindvpos, true);
                            readstate.maindvpos += 4;
                            var fitalic = vectordv.getInt32(readstate.maindvpos, true);
                            readstate.maindvpos += 4;
                            var funderline = vectordv.getInt32(readstate.maindvpos, true);
                            readstate.maindvpos += 4;
                            var fontnamelength = vectordv.getInt32(readstate.maindvpos, true);
                            fontnamelength *= 2;
                            readstate.maindvpos += 4;
                            var fnamecount = readstate.maindvpos;
                            var szfontname = "";
                            while (fnamecount < readstate.maindvpos + fontnamelength) {
                                if (vectordv.getUint16(fnamecount, true) != 0) {
                                    szfontname += String.fromCharCode(vectordv.getUint16(fnamecount, true));
                                }
                                fnamecount += 2;
                            }
                            readstate.maindvpos = fnamecount;
                            readstate.font = {
                                height: fheight,
                                rotation: frotation,
                                weight: fweight,
                                italic: fitalic,
                                underline: funderline,
                                name: szfontname
                            };
                            readstate.prevobjectid = readstate.objectid;
                            readstate.objectid = vectordv.getInt32(readstate.maindvpos, true);
                            break;
                        case 77:
                            readstate.maindvpos += 4;
                            var transpvalue = vectordv.getFloat32(readstate.maindvpos, true);
                            readstate.transparency = transpvalue;
                            readstate.maindvpos += 4;
                            readstate.prevobjectid = readstate.objectid;
                            readstate.objectid = vectordv.getInt32(readstate.maindvpos, true);
                            //transparency
                            break;
                        case 78: //handle
                            readstate.maindvpos += 4;
                            //readstate.handles++

                            var handleLow = vectordv.getInt32(readstate.maindvpos, true);
                            readstate.maindvpos += 4;
                            var handleHigh = vectordv.getInt32(readstate.maindvpos, true);
                            readstate.maindvpos += 4;

                            //vectorobj.handlelist[readstate.handles] = new HandleObject(handleLow, handleHigh);

                            readstate.prevobjectid = readstate.objectid;
                            readstate.objectid = vectordv.getInt32(readstate.maindvpos, true);
                            break;
                        case 79: //Entity
                            readstate.maindvpos += 4;

                            /*if(scope.pathlist[i].entityType.type != 0){
                                console.log(scope.pathlist[i].entityType.type);
                            }*/

                            var type = vectordv.getInt32(readstate.maindvpos, true);
                            readstate.maindvpos += 4;
                            var handleLow = vectordv.getInt32(readstate.maindvpos, true);
                            readstate.maindvpos += 4;
                            var handleHigh = vectordv.getInt32(readstate.maindvpos, true);
                            readstate.maindvpos += 4;

                            readstate.entityType = {type : type, handleLow :handleLow, handleHigh :handleHigh};
                            

                            /*readstate.entityType.type = vectordv.getInt32(readstate.maindvpos, true);
                            readstate.maindvpos += 4;
                            readstate.entityType.handleLow = vectordv.getInt32(readstate.maindvpos, true);
                            readstate.maindvpos += 4;
                            readstate.entityType.handleHigh = vectordv.getInt32(readstate.maindvpos, true);*/
                            


                            readstate.prevobjectid = readstate.objectid;
                            readstate.objectid = vectordv.getInt32(readstate.maindvpos, true);

                            break;
                        case 12288:
                            readstate.objectid = 12288;
                            readstate.maindvpos += 4;
                            readstate.prevobjectid = readstate.objectid;
                            readstate.objectid = vectordv.getInt32(readstate.maindvpos, true);
                            //end of block;
                            break;
                        case 4096:
                            readstate.objectid = 4096;
                            readstate.blockpart++;
                            //readstate.maindvpos += 4;
                            //bobjid = parseInt(vectordv.getInt32(readstate.maindvpos,true));
                            readstate.maindvpos += 4;
                            readstate.blockid = parseInt(vectordv.getInt32(readstate.maindvpos, true));
                            readstate.maindvpos += 4;
                            blockflag = parseInt(vectordv.getInt32(readstate.maindvpos, true));
                            vscope.bnameunique = (blockflag == 1);
                            blist = (blockflag != 128);
                            readstate.maindvpos += 4;
                            bnamelength = parseInt(vectordv.getInt32(readstate.maindvpos, true));
                            readstate.maindvpos += 4;
                            bnamecount = readstate.maindvpos;
                            blockname = "";
                            //bnamecount++;
                            while (bnamecount < readstate.maindvpos + bnamelength * 2) {
                                blockname += String.fromCharCode(vectordv.getUint16(bnamecount, true));
                                bnamecount += 2;
                            }
                            readstate.maindvpos = bnamecount;
                            var bexist = false;


                            if (vscope.bnameunique) {

                                //return {bexist : bexist, count : blockpart};
                                var blockresponse = vectorobj.getblocklistlength(readstate.role, blockname, readstate.blockpart);
                                bexist = blockresponse.bexist;
                                readstate.blockpart = blockresponse.count

                                /*for (var bcount = 0; bcount < blen; bcount++) {
                                    if (blockname == vectorobj.blocklist[bcount].name) {
                                        readstate.blockpart = bcount;
                                        bexist = true;
                                        break;
                                    }
                                }*/
                            }

                            /*if(blockname == "CCBASE"){
                                console.log(blockname);
                            }*/
                            
                            if (!bexist) {
                                //vectorobj.blocklist[readstate.blockpart] = new BlockObject(readstate.blockid, blockname, 1, readstate.fcolor);
                                vectorobj.updateblocks(new BlockObject(readstate.blockid, blockname, 1, readstate.fcolor, blist),readstate.role);
                            }
                            
                            //readstate.maindvpos += 4;
                            readstate.prevobjectid = readstate.objectid;
                            readstate.objectid = vectordv.getInt32(readstate.maindvpos, true);
                            //start of new block
                            break;
                        case 32768:
                            //read complete
                            readstate.objectid = 32768;
                            break;
                        default:
                            console.log('default');
                            vectorobj.invalid = true;
                            console.log(readstate.objectid);
                            console.log(readstate.prevobjectid);
                            console.log(readstate.maindvpos);
                            readstate.objectid = 32768;
                    }
                    return readstate;
                }

                function readbinarycomplete(role) {

                    vectorobj.readbinarycomplete(role);
                    

                }

                /*this.addVectors = function (vectorobj, pagexmldata, compareobj){


                    if(compareobj){
                        vscope.role = compareobj.role;
                    }
    

                    //scope, pagexmldata
                    init(vectorobj, pagexmldata, true);
                    parse2Dloop(vscope.role);
                };*/

                init(vectorobj, pagexmldata, vscope.role);
                parse2Dloop(vscope.role);

                /*(if !badd){
                }*/
                
                
                function yieldingLoop(state, chunksize, callback, finished) {
                    (function chunk() {
                        var i = 0;
                        //var end = Math.min(i + chunksize, count);
                        var end = (i < chunksize);
                        for (; i < chunksize; ++i) {
                            callback.call(null, state);
                            if (state.objectid == 32768) {
                                break;
                            }
                        }
                        if (state.objectid != 32768) {
                            setTimeout(chunk, 0);
                        }
                        else {
                            //console.log(state.objectid);
                            finished.call(null);
                        }
                    })();
                }


            }//constructor
        }

        class VectorPageObject {
            constructor(page, pagexmldata, binary,compareobj) {
                var scope = this;
                this.parent = page;

                
                this.firstdraw = false;
                this.firstdrawcompare = false;

                this.offscreen = document.createElement('canvas');
                this.offscreenctx = this.offscreen.getContext('2d');

                this.offscreenprint = document.createElement('canvas');
                this.offscreenprintctx = this.offscreenprint.getContext('2d');

                this.thumboffscreen = document.createElement('canvas');
                this.thumboffscreenctx = this.offscreen.getContext('2d');

                this.renderinprogress = false;
                this.backgroundrender = true;
                
                this.docompare = false;
                this.dooverlay = false;


                this.bcompare = page.DocRef.bcompare;
                this.comparemode = page.OverlayComparemode;


                this.isbackground = false;
                this.comparecolor = 'red';
                this.novectors = false;
                this.bgimage = false;
                this.ovimage = false;

                this.layerlist = [];

                this.pentable = new PenTable();

                
                var Unitofmeasure = scope.parent.Unitofmeasure;
                this.pentable.setunit(Unitofmeasure);
                this.pentable.setscaled(true);
                
                this.layerlistbg = [];
                this.layerlistfg = [];


                this.vectorpagetime = 0;
                this.textlayerset = false;

                this.blocklist = [];
                this.filterblocklist = [];
                this.filterAttributelist = [];

                this.blocklistbg = [];
                this.filterblocklistbg = [];
                this.filterAttributelistbg = [];

                this.blocklistfg = [];
                this.filterblocklistfg = [];
                this.filterAttributelistfg = [];

                



                this.numblocks = -1;
                this.bnameunique = false;
                this.selectedobject = undefined;
                //this.selectColor = "#bf3ad1";
                this.selectColor = "rgba(191,58,209,0.6)";
                this.gradientStop = "rgba(255,255,255,0.3)";
                this.viewmode = 0;
                this.applypentable = false;
                this.multiselect = false;
                this.selectedblocks = [];
                this.fileversion = "";
                
                this.flag = -1;

                this.compare = false;


                this.x = 0;
                this.y = 0;
                this.w = 0;
                this.h = 0;
                this.width = 0;
                this.height = 0;
                this.scale = 1;
                this.offsetx = 0;
                this.offsety = 0;
                this.numlayers = 0;

                //overlay-compare
                this.overlayscale = 1;
                this.overlayoffsetx = 0;
                this.overlayoffsety = 0;
                this.nudgeScalefactor = 0;

                this.overlayrotation = 0;
                this.fgdrawangle = 0;
                this.bnudgeangleused = false;

                this.nOffsetX = 0;
                this.nOffsetY = 0;

                this.backupoverlayscale = 1;
                this.backupoverlayoffsetx = 0;
                this.backupoverlayoffsety = 0;

                //overlay-compare

                this.backupoverlayrotation = 0;



                this.blocklistovoffset = 0;
                this.layerlistovoffset = 0;


                this.pathlist = [];
                this.pathlistoverride = [];
                this.textlist = [];
                this.attributes = [];
                this.intersections = [];

                this.pathlistbg = [];
                this.pathlistoverridebg = [];
                this.textlistbg = [];
                this.attributesbg = [];
                this.intersectionsbg = [];

                this.pathlistfg = [];
                this.pathlistoverridefg = [];
                this.textlistfg = [];
                this.attributesfg = [];
                this.intersectionsfg = [];



                this.numpaths = 0;
                this.vectorloader = undefined;
                this.addvectorloader = undefined;

                this.bgDocObjref = undefined;
                this.fgDocObjref = undefined;

                this.bgpageindex = undefined;
                this.fgpageindex = undefined;

                this.cmprimagestoload = 0;
                this.cmprvctrsrctoload = 0;

                var partlist = [];


                if(compareobj){
                    this.backgroundColor = compareobj.bgColor;
                    this.overlayColor = compareobj.fgColor;

                    this.bgpageindex = scope.parent.comparebgpageindex;
                    this.fgpageindex = scope.parent.comparefgpageindex;

                    for (var i = 0; i < OpenFiles.length; i++) {
                        if(OpenFiles[i].fileindex == scope.parent.comparebgdocindex){
                            
                            this.bgDocObjref = OpenFiles[i];
                            this.bgDocObjref.setComparelink = new onCloseCallback(this.parent.DocRef.bgFileClosed);
                            

                            
                         }
                         if( OpenFiles[i].fileindex == scope.parent.comparefgdocindex ){
                            this.fgDocObjref = OpenFiles[i];
                            this.fgDocObjref.setComparelink = new onCloseCallback(this.parent.DocRef.fgFileClosed);
                         }

                    }

                    if(scope.parent.loadbackgroundimage){
                        this.cmprimagestoload += 1;
                    }

                    if(scope.parent.loadoverlayimage){
                        this.cmprimagestoload += 1;
                    }

                    if(scope.parent.loadbackgroundpdf){
                        this.cmprimagestoload += 1;
                    }

                    if(scope.parent.loadoverlaypdf){
                        this.cmprimagestoload += 1;
                    }

                    
                    //thispage.loadbackgroundvectors = true;
                    if(scope.parent.loadbackgroundvectors){
                        
                        this.cmprvctrsrctoload += 1;
                    }

                    if(scope.parent.loadoverlayvectors){
                        this.cmprvctrsrctoload += 1;
                    }


                    //thispage.usevectorbinarycompare = true;
                    //thispage.loadbackgroundvectors = false;
                    //thispage.loadbackgroundimage = true;
                    //thispage.usevectorbinarycompare = true;
                    //thispage.loadoverlayvectors = true;
                    //thispage.usevectorbinarycompare = true;
                    
                    //thispage.usevectorbinarycompare = true;
                    //thispage.loadoverlayvectors = false;
                    //thispage.loadoverlayimage = true;

                   

                }



                /*if(index == 0){
                    RxCore_setActiveFile(scope.parent.DocRef.comparebgdocindex);
                }
                if(index == 1){
                    RxCore_setActiveFile(scope.parent.DocRef.comparefgdocindex);

                }*/


                function loadvectors(){
                    if(compareobj){
                        scope.vectorloader = new VectorLoader(scope, pagexmldata, compareobj);
                    }else{
                        scope.vectorloader = new VectorLoader(scope, pagexmldata);
                    }
                    
                }

                function createempty(pageobject){
                    scope.flag = 1;

                    scope.novectors = true;

                    scope.x = pageobject.x;
                    scope.y = pageobject.y;
                    scope.w = pageobject.w;
                    scope.h = pageobject.h;

                    scope.scale = pageobject.scale;
                    scope.offsetx = pageobject.offsetx;
                    scope.offsety = pageobject.offsety;
    
                    scope.width = scope.w - scope.x;
                    scope.height = scope.h - scope.y;
                    
                    scope.fileversion = 1;
                    scope.flag = 1;

                    scope.bcompare = true;

                };
                
                function createlayer(pageobject, bbackground){

                    

                    var backroundlayer = {
                        index : 0,
                        color : pageobject.bgColor,
                        state : 1,
                        name : "backgroundlayer"
                    };

                    var overlaylayer = {
                        index : 1,
                        color : pageobject.fgColor,
                        state : 1,
                        name : "overlaylayer"
                    };

                    var layerindex = 0;
                    if(bbackground){
                        scope.layerlist.push(new LayerObject(backroundlayer, false));
                        layerindex = 0;
                    }else{
                        scope.layerlist.push(new LayerObject(overlaylayer, false));
                        layerindex = 1;
                    }

                    return layerindex;
                    
                    

                    /*this.index = layerxmldata.index;
                    this.color = layerxmldata.color;
                    this.state = layerxmldata.state;
                    this.isplottable = true;
                    this.defaultstate = layerxmldata.state;
                    this.name = layerxmldata.name;*/

                }

                function createblock(pageobject, bbackground){
                    var blockindex = 0;

                    //var compareobject = {role : 1, bgColor : overlayBGColor, fgColor : overlayFGColor};

                    if(bbackground){
                        scope.blocklist.push(new BlockObject(0,"backgroundblock",1,pageobject.bgColor, true));
                        blockindex = 0;
                    }else{
                        scope.blocklist.push(new BlockObject(1,"overlayblock",1,pageobject.fgColor, true));
                        blockindex = 1;
                    }

                    return blockindex;
                    
                    /*class BlockObject {
                        constructor(index, name, state, color) {*/
            
                }
                
                /*function readbinarycomplete() {
                    

                }*/

                /*function loadVectors(){
                    if(scope.vectorloader.load){
                        scope.vectorloader.load();
                    }
                }*/

                function blockon(count) {
                    var bblcckon = true;
                    if (scope.blocklist.length > 0) {

                        if(scope.blocklist[scope.pathlist[count].blockname] != undefined){

                            if(scope.pathlist[count].role == 2){

                                if(scope.blocklist[scope.pathlist[count].blockname + scope.blocklistovoffset] != undefined){
                                    bblcckon = (scope.blocklist[scope.pathlist[count].blockname + scope.blocklistovoffset].state == 1);
                                }

                                
                            }else{
                                bblcckon = (scope.blocklist[scope.pathlist[count].blockname].state == 1);
                            }
                            
                        }
                        
                    }
                    return bblcckon;
                }
                function layeron(count) {
                    var blayeron = true;


                    if (scope.layerlist.length > 0) {


                        if (scope.pathlist[count].layer != -1 && scope.layerlist[scope.pathlist[count].layer] != undefined) {
                            
                            /*if(scope.pathlist[count].role == 1){
                                //console.log(scope.layerlist[scope.pathlist[count].layer].state);
                                //console.log(scope.pathlist[count].layer);
                            }*/
                            
                            /*if(scope.pathlist[count].role == 1){
                                blayeron = true;    
                            }else{
                                
                            }*/

                            if(scope.pathlist[count].role == 2){

                                if(scope.layerlist[scope.pathlist[count].layer + scope.layerlistovoffset] != undefined){
                                    blayeron = (scope.layerlist[scope.pathlist[count].layer + scope.layerlistovoffset].state == 1);
                                }
                                //count = count + scope.layerlistovoffset;
                                
                            }else{
                                blayeron = (scope.layerlist[scope.pathlist[count].layer].state == 1);
                            }
        

                            
                            
                            

                        }

                    }
                    return blayeron;
                }
                function getBlockID(blockindex) {
                    //var blockindex = -1;
                    var blockID = scope.blocklist[blockindex].index;
                    return blockID;
                }
                function getBlockIndex(blockid) {
                    var blockindex = -1;
                    for (var bcount = 0; bcount < scope.blocklist.length; bcount++) {
                        if (scope.blocklist[bcount].index == blockid) {
                            blockindex = bcount;
                            break;
                        }
                    }
                    return blockindex;
                }

                function getBlockObject(blockid){
                    var blockobj = undefined;

                    for (var bcount = 0; bcount < scope.blocklist.length; bcount++) {
                        if (scope.blocklist[bcount].index == blockid) {
                            
                            blockobj = scope.blocklist[bcount];
                            break;
                        }
                    }
                    return blockobj;

                }

                function getBlockName(blockid) {
                    var blockname = undefined;

                    for (var bcount = 0; bcount < scope.blocklist.length; bcount++) {
                        if (scope.blocklist[bcount].index == blockid) {
                            
                            blockname = scope.blocklist[bcount].name;
                            break;
                        }
                    }
                    return blockname;
                }

                function getLayername(layerindex){
                    //this.name
                }

                function getAttributes(blockindex) {
                    var partlist = [];
                    //var blockindex = getBlockIndex(blockid);
                    for (var attrc = 0; attrc < scope.attributes.length; attrc++) {
                        if (scope.attributes[attrc].blockref == blockindex) {
                            partlist.push(scope.attributes[attrc]);
                        }
                    }
                    return partlist;
                }

                function getpdfimageobjectdim(imageobject, numx, numy, numtiles){

                    var x1 = imageobject.x1; 
                    var y1 = imageobject.y1;
                    var x2 = imageobject.x2;
                    var y2 = imageobject.y2;

                    var fractile = 1 / numtiles;
                    var tileheight = y2 * fractile;
                    var tilewidth = x2 * fractile;

                    //var revnumy = (numtiles) - numy;
                    

                    imageobject.x1 = tilewidth * numx; 
                    imageobject.y1 = tileheight * numy;
                    imageobject.x2 = tilewidth * (numx + 1); 
                    imageobject.y2 = tileheight * (numy + 1);



                    return imageobject;
                }
                function addpdfimagetiles(imageobject, scope, blockindex ,binary, pathindex){
                    

                    return new pdfimageObject(imageobject, scope, blockindex , binary, pathindex);

                }

                this.addpdfimage = function(imageobject, bbackground, compareobj){

                    var dlayer = createlayer(compareobj, bbackground);
                    var blockindex = createblock(compareobj, bbackground);

                    var numtiles = 4; // 4x4 = 16 tiles 

                    var role = 0;

                    var x1 = imageobject.x1; 
                    var y1 = imageobject.y1;
                    var x2 = imageobject.x2;
                    var y2 = imageobject.y2;


                    imageobject = {
                        layer: dlayer,
                        drawmode: imageobject.drawmode,
                        layerstate: imageobject.lstate,
                        x1: imageobject.x1,
                        y1: imageobject.y1,
                        x2: imageobject.x2,
                        y2: imageobject.y2,
                        magnify : false,
                        //image: imageobject.imagestr,
                        useref: true,
                        addcache : false,
                        role : role
                    };

                    if(bbackground){
                        
                        //add 4x4 

                        
                        role = 1;

                        imageobject.magnify = true;

                        for (var tix = 0;tix < numtiles;tix++){

                            for (var tiy = 0;tiy < numtiles;tiy++){
                                imageobject.role = 1;    

                                imageobject = getpdfimageobjectdim(imageobject, tix, tiy, numtiles);

                                var imgobj = addpdfimagetiles(imageobject, scope, blockindex , true, scope.pathlist.length);

                                imgobj.isbackground = true;
                                
    
                                scope.pathlistbg.unshift(imgobj);
                                
    
                                imageobject.x1 = x1; 
                                imageobject.y1 = y1;
                                imageobject.x2 = x2;
                                imageobject.y2 = y2;
    
                            }

                                    
                        } 

                        imageobject.magnify = false;

                        for (var tix = 0;tix < numtiles;tix++){

                            for (var tiy = 0;tiy < numtiles;tiy++){
                                imageobject.role = 1;    

                                imageobject = getpdfimageobjectdim(imageobject, tix, tiy, numtiles);

                                var imgobj = addpdfimagetiles(imageobject, scope, blockindex , true, scope.pathlist.length);

                                imgobj.isbackground = true;
                                
    
                                scope.pathlistbg.unshift(imgobj);
                                
    
                                imageobject.x1 = x1; 
                                imageobject.y1 = y1;
                                imageobject.x2 = x2;
                                imageobject.y2 = y2;
    
                            }

                                    
                        } 

                        scope.bgimage = true;

                        //imageobject.x2 = x2 * 0.5;
                        //imageobject.y2 = y2 * 0.5;
                        
                        

                        

                        


                        //imageobject.x1 = x2 * 0.5;
                        //imageobject.y1 = y2 * 0.5;
                        //imageobject.x2 = x2;
                        //imageobject.y2 = y2;

                        
                        //imgobj = new pdfimageObject(imageobject, scope, blockindex , true, scope.pathlist.length);
                        //imgobj.isbackground = true;

                        //scope.pathlistbg.unshift(imgobj);

                        

                        //this.isoverlay = false;
                        //this.isbackground = false;
        
                        
        
                        //scope.pathlist.unshift(imgobj);


                    }else{

                        
                        role = 2;

                        imageobject.magnify = true;

                        for (tix = 0;tix < numtiles;tix++){

                            for (tiy = 0;tiy < numtiles;tiy++){
                                //imageobject.role = 1;    
                                imageobject.role = 2;

                                imageobject = getpdfimageobjectdim(imageobject, tix, tiy, numtiles);
    
                                //scope.pathlistbg.unshift();
                                imgobj = addpdfimagetiles(imageobject, scope, blockindex , true, scope.pathlist.length);
                                imgobj.isoverlay = true;

                                scope.pathlistfg.push(imgobj);

                                

                                
    
                                imageobject.x1 = x1; 
                                imageobject.y1 = y1;
                                imageobject.x2 = x2;
                                imageobject.y2 = y2;
    
                            }

                                    
                        }
                        
                        imageobject.magnify = false;


                        for (tix = 0;tix < numtiles;tix++){

                            for (tiy = 0;tiy < numtiles;tiy++){
                                //imageobject.role = 1;    
                                imageobject.role = 2;

                                imageobject = getpdfimageobjectdim(imageobject, tix, tiy, numtiles);
    
                                //scope.pathlistbg.unshift();
                                imgobj = addpdfimagetiles(imageobject, scope, blockindex , true, scope.pathlist.length);
                                imgobj.isoverlay = true;

                                scope.pathlistfg.push(imgobj);

                                

                                
    
                                imageobject.x1 = x1; 
                                imageobject.y1 = y1;
                                imageobject.x2 = x2;
                                imageobject.y2 = y2;
    
                            }

                                    
                        }
                        //imageobject.x2 = x2 * 0.5;
                        //imageobject.y2 = y2 * 0.5;

                        //imgobj = new pdfimageObject(imageobject, scope, blockindex , true, scope.pathlist.length);
                        //scope.pathlist.push(imgobj);

                        //imgobj.isoverlay = true;

                        
                        scope.ovimage = true;

                        //imageobject.x1 = x2 * 0.5;
                        //imageobject.y1 = y2 * 0.5;
                        //imageobject.x2 = x2;
                        //imageobject.y2 = y2;

                        //imgobj = new pdfimageObject(imageobject, scope, blockindex , true, scope.pathlist.length);

                        

                        //scope.pathlistfg.push(imgobj);

                        
                    }

                    var loader = {
                        x : x1,
                        y : y1,
                        w : x2,
                        h : y2,
                        scale : scope.scale,
                        offsetx : scope.offsetx,
                        offsety : scope.offsety,
                        fileversion : scope.fileversion,
                        flag : scope.flag
                    }


                    if(scope.novectors){
                        scope.pathlist = scope.pathlistbg.concat(scope.pathlistfg);

                            //var width = loader.w - loader.x;
                            //var height = loader.h - loader.y;

                            //x1: imageobject.x1,
                            //y1: imageobject.y1,
                            //x2: imageobject.x2,
                            //y2: imageobject.y2,
    


                        scope.setdim(loader, role);

    
                        

                        //scope.overlayscale = Math.min((scope.width / width), (scope.height / height));
                        
                        //scope.overlayoffsetx = loader.x;
                        //scope.overlayoffsety = loader.h;

                        //scope.overlayrotation = 0;


                    }else{

                        scope.pathlist = scope.pathlistbg.concat(scope.pathlistfg);
                        scope.setdim(loader, role);
                    }

                    //


                    


                    /*class imageObject {
                        constructor(xmldata, parent, blockname, binary, pathindx) {
                            //<image x1='1908247.00' y1='1777044.00' x2='2851685.00' y2='2484622.00' type="png" encoding="base64">
                            var scope = this;
                            this.parent = parent;
                            this.index = pathindx;
                            this.type = "image";
                            this.blockname = blockname;
                            this.blockstate = 1;
                            this.layerstate = 1;
                            this.x1 = xmldata.x1;
                            this.y1 = xmldata.y1;
                            this.x2 = xmldata.x2;
                            this.y2 = xmldata.y2;
                            this.width = (this.x2 - parent.x)-(this.x1 - parent.x);
                            this.height = (parent.h - this.y1) - (parent.h - this.y2);*/


                };

                this.addimage = function(imageobject, bbackground, compareobj){

                    var dlayer = createlayer(compareobj, bbackground);
                    var blockindex = createblock(compareobj, bbackground);

                    var role = 0;

                    imageobject = {
                        layer: dlayer,
                        drawmode: imageobject.drawmode,
                        layerstate: imageobject.lstate,
                        x1: imageobject.x1,
                        y1: imageobject.y1,
                        x2: imageobject.x2,
                        y2: imageobject.y2,
                        image: imageobject.imagestr,
                        useref: true,
                        addcache : false,
                        role : role
                    };
                    
                    //scope.blocklist.push(new BlockObject(0,"backgroundblock",1,pageobject.bgcolor));
                    //scope.blocklist.push(new BlockObject(1,"overlayblock",1,pageobject.fgcolor));

                    /*if(scope.parent.loadbackgroundimage){
                        this.cmprimagestoload += 1;
                    }

                    if(scope.parent.loadoverlayimage){
                        this.cmprimagestoload += 1;
                    }
                    
                    //thispage.loadbackgroundvectors = true;
                    if(scope.parent.loadbackgroundvectors){
                        
                        this.cmprvctrsrctoload += 1;
                    }

                    if(scope.parent.loadoverlayvectors){
                        this.cmprvctrsrctoload += 1;
                    }*/

                    
                    if(bbackground){
                        
                        

                        imageobject.role = 1;
                        role = 1;

                        var imgobj = new imageObject(imageobject, scope, blockindex , true, scope.pathlist.length);

                        imgobj.isbackground = true;

                        scope.pathlistbg.unshift(imgobj);

                        scope.bgimage = true;

                        //this.isoverlay = false;
                        //this.isbackground = false;
        
                        
        
                        //scope.pathlist.unshift(imgobj);


                    }else{

                        imageobject.role = 2;
                        role = 2;

                        var imgobj = new imageObject(imageobject, scope, blockindex , true, scope.pathlist.length);
                        //scope.pathlist.push(imgobj);

                        imgobj.isoverlay = true;

                        scope.pathlistfg.push(imgobj);
                        scope.ovimage = true;


                        
                    }

                    var loader = {
                        x : imageobject.x1,
                        y : imageobject.y1,
                        w : imageobject.x2,
                        h : imageobject.y2,
                        scale : scope.scale,
                        offsetx : scope.offsetx,
                        offsety : scope.offsety,
                        fileversion : scope.fileversion,
                        flag : scope.flag
                    }


                    if(scope.novectors){
                        scope.pathlist = scope.pathlistbg.concat(scope.pathlistfg);

                            //var width = loader.w - loader.x;
                            //var height = loader.h - loader.y;

                            //x1: imageobject.x1,
                            //y1: imageobject.y1,
                            //x2: imageobject.x2,
                            //y2: imageobject.y2,
    


                        scope.setdim(loader, role);

    
                        

                        //scope.overlayscale = Math.min((scope.width / width), (scope.height / height));
                        
                        //scope.overlayoffsetx = loader.x;
                        //scope.overlayoffsety = loader.h;

                        //scope.overlayrotation = 0;


                    }else{

                        scope.pathlist = scope.pathlistbg.concat(scope.pathlistfg);
                        scope.setdim(loader, role);
                    }

                    //


                    


                    /*class imageObject {
                        constructor(xmldata, parent, blockname, binary, pathindx) {
                            //<image x1='1908247.00' y1='1777044.00' x2='2851685.00' y2='2484622.00' type="png" encoding="base64">
                            var scope = this;
                            this.parent = parent;
                            this.index = pathindx;
                            this.type = "image";
                            this.blockname = blockname;
                            this.blockstate = 1;
                            this.layerstate = 1;
                            this.x1 = xmldata.x1;
                            this.y1 = xmldata.y1;
                            this.x2 = xmldata.x2;
                            this.y2 = xmldata.y2;
                            this.width = (this.x2 - parent.x)-(this.x1 - parent.x);
                            this.height = (parent.h - this.y1) - (parent.h - this.y2);*/

                };

                
    
                /*if(vscope.role == 0){
                    

                }*/

                this.initpath = function(role){
                    

                    scope.pathlistoverride = [];
                    scope.intersections = [];

                    switch (role){
                        case 0 :
                            scope.pathlist = [];
                            scope.textlist = [];
                            scope.attributes = [];
                            scope.numpaths = 0;
    
                        break;
                        case 1 : 
                            scope.pathlistbg = [];
                            scope.textlistbg = [];
                            scope.attributesbg = [];
                            scope.numpathsbg = 0;
                        break;
                        case 2 : 
                            scope.pathlistfg = [];
                            scope.textlistfg = [];
                            scope.attributesfg = [];
                            scope.numpathsfg = 0;
                        break;


                    }
                    
                };

                this.getblocklistlength = function(role, blockname, blockpart){


                    var bexist = false;
                    var bcount = 0;
                    //var blockpart = 0;

                    //var length = 0;

                    if(role == 0){
                        
                        //length = scope.blocklist.length;

                        for (var bcount = 0; bcount < scope.blocklist.length; bcount++) {
                            if (blockname == scope.blocklist[bcount].name) {
                                blockpart = bcount;
                                bexist = true;
                                break;
                            }
                        }
                            


                    }else if(role == 1){
                        
                        //length = scope.blocklistbg.length;

                        for (bcount = 0; bcount < scope.blocklistbg.length; bcount++) {
                            if (blockname == scope.blocklistbg[bcount].name) {
                                blockpart = bcount;
                                bexist = true;
                                break;
                            }
                        }
                        
                        
            
                    }else if(role == 2){

                        
                        //length = scope.blocklistfg.length;
                        for (bcount = 0; bcount < scope.blocklistfg.length; bcount++) {
                            if (blockname == scope.blocklistfg[bcount].name) {
                                blockpart = bcount;
                                bexist = true;
                                break;
                            }
                        }


                    }
                    return {bexist : bexist, count : blockpart};

                }

                this.updateBlockInsert = function(blockid, blockinsert){
                    for(var bi = 0; bi < scope.blocklist.length ; bi++ ){
                        if(scope.blocklist[bi].index == blockid){
                            scope.blocklist[bi].insert = blockinsert;
                            //scope.blocklist[bi].inserts.push(blockinsert);
                        }
                    }

                }

                this.updateblocks = function(blockobj, role) {

                    if(role == 0){
                        
                        scope.blocklist.push(blockobj);


                    }else if(role == 1){
                        
                        scope.blocklistbg.push(blockobj);//
                        
            
                    }else if(role == 2){

                        scope.blocklistfg.push(blockobj);//

                    }
                };

                this.updatepentTable = function(penobject){
                    scope.pentable.add(penobject);
                };

                this.getpenTable = function(){

                    return scope.pentable;
                
                };

                //DocObj.getCurrentpage().(penobj);

                this.setpentableUnit = function(munit){

                    scope.pentable.setunit(munit);
                    
                }

                this.setpentableScaled = function(bscaled){
                    scope.pentable.setscaled(bscaled);
                }

                this.setPenwidth = function(penobj){

                    var pen = scope.pentable.getPen(penobj.penindex);
                    var bscaled = scope.pentable.getscaled();
                    var munit = scope.pentable.getunit();
                    var displaywidth = parseFloat(pen.getdisplaywidth());

                    if(bscaled){


                        if(displaywidth != NaN){
                            var fixedscale = scope.parent.fixedScale;
                            var pixelwidth = scope.getScreenDim(parseFloat(penobj.penwidth));
                            pixelwidth *= fixedscale;
                            pen.setwidth(pixelwidth);
                            //pen.setdisplaywidth(penobj.penwidth);
                        }
    


                    }else{
                        if(displaywidth != NaN){
                            if(munit == 1){ //metric mm
                                pen.setwidth((displaywidth /  25.4) * 96);
                            } 
                            if(munit == 2){ //metric mm
                                pen.setwidth(displaywidth * 96);
                            }
                        }

                    }

                };

                this.setPenTablepen = function(penobj){

                    /*color :  "#000000"
                    penindex :  "1"
                    penwidth :  "0.250000"
                    style :  "-1"*/

                    var pen = scope.pentable.getPen(penobj.penindex);

                    pen.setcolor(penobj.color);

                    if(parseFloat(penobj.penwidth) != NaN){

                        var fixedscale = scope.parent.fixedScale;
                        var pixelwidth = scope.getScreenDim(parseFloat(penobj.penwidth));
                        pixelwidth *= fixedscale;
                        pen.setwidth(pixelwidth);
                        pen.setdisplaywidth(penobj.penwidth);
                    }
                    
                    if(parseInt(penobj.style) != NaN && parseInt(penobj.style) != -1){
                        pen.setstyle(penobj.style);
                    }

                };

                this.resetPenTablepen = function(penobj){

                    var pen = scope.pentable.getPen(penobj.penindex);
                    pen.resetcolor();
                    pen.resetwidth();
                    pen.resetstyle();

                };

                this.setpencolor = function(penindex, color){
                    //this.getpenTable()
                    scope.pentable.getPen(penindex).setcolor(color);
                };

                this.resetpencolor = function(penindex){
                    scope.pentable.getPen(penindex).resetcolor();
                };

                /*setPenwidth
                this.setpenwidth = function(penindex, uwidth){
                    //this.getpenTable()

                    
                    
                };*/

                this.resetpenwidth = function(penindex){
                    scope.pentable.getPen(penindex).resetwidth();
                };                

                this.updatepathlist = function(pathobj, role){


                             //vscope.pathlist.push(lineobj);
                            //vscope.numpaths++;

                    switch (role){
                        case 0 :
                            scope.pathlist.push(pathobj);
                            scope.numpaths ++;
    
                        break;
                        case 1 : 

                            scope.pathlistbg.push(pathobj);
                            scope.numpathsbg ++;

                        break;
                        case 2 : 
                            scope.pathlistfg.push(pathobj);
                            scope.numpathsfg ++;
                        break;


                    }


                };

                this.setlayers = function(loader, role){

                    switch(role){
                        case 0 :
                            scope.layerlist = loader.layerlist.slice();

                            scope.numlayers = loader.numlayers;

                            if (RxCore_GUI_VectorLayers != undefined) {
                                RxCore_GUI_VectorLayers.setVectorLayers(scope.layerlist);
                            }
                            break;
                        case 1 :
                            scope.layerlistbg = loader.layerlist.slice();

                            /*if (RxCore_GUI_VectorLayers != undefined) {
                                RxCore_GUI_VectorLayers.setVectorLayers(scope.layerlist);
                            }*/
                            break;
                        case 2 :

                            scope.layerlistovoffset = scope.layerlistbg.length;
                            scope.layerlistfg = loader.layerlist.slice();

                            scope.layerlist = scope.layerlistbg.concat(scope.layerlistfg);

                            if (RxCore_GUI_VectorLayers != undefined) {
                                RxCore_GUI_VectorLayers.setVectorLayers(scope.layerlist);
                            }
                            break;

                    }
    
                };


                this.getScreenDim = function(length, munit){
                    var pixelwidth = 0;


                    var dpi = scope.parent.DPI;

                    var Unitofmeasure = scope.parent.Unitofmeasure;
                    var DPmm = dpi / 25.4;
                    var DPInch = dpi;

                    length = length * unitscale;

                    var mscale = scope.parent.getMeasureScale();
                    if (mscale == undefined){
                        mscale = MeasureScale;
                    }


                    pixelwidth = (length / mscale) * scope.parent.OriginalScale;


                    if (dpi != 0) {
                        switch (Unitofmeasure) {
                            case 1:
                                //dimValue = dimValue / DPmm;
                                pixelwidth = pixelwidth * DPmm;
                                break;
                            case 2:
                                //dimValue = dimValue / DPInch;
                                pixelwidth = pixelwidth * DPInch;
                                break;
                            case 3:
                                //dimvalue= dimvalue;
                                break;
                        }
            
            
                    } else {
                        /*if (!DocObj.getCurrentpage().usevectorxml) {
                            pixelwidth = pixelwidth * DocObj.getCurrentpage().getdscale();
            
                        }*/
            
            
                    }
            
                    return pixelwidth;

                };


                this.setdim = function(loader, role){
                    //keep simple for now future calculate total size

                    if(role == 0){

                        scope.x = loader.x;
                        scope.y = loader.y;
                        scope.w = loader.w;
                        scope.h = loader.h;
                        scope.scale = loader.scale;
                        scope.offsetx = loader.offsetx;
                        scope.offsety = loader.offsety;
    
                        scope.fileversion = loader.flag;
                        scope.flag = loader.flag;
    
                        scope.width = scope.w - scope.x;
                        scope.height = scope.h - scope.y;
    
                    }

                    if(role == 2){

                        if(scope.parent.loadbackgroundvectors){
                            var width = loader.w - loader.x;
                            var height = loader.h - loader.y;
    
    
                            scope.overlayscale = Math.min((scope.width / width), (scope.height / height));
                            scope.backupoverlayscale = scope.overlayscale;
                            
                            scope.overlayoffsetx = loader.x;
                            scope.overlayoffsety = loader.h;

                            scope.backupoverlayoffsetx = scope.overlayoffsetx;
                            scope.backupoverlayoffsety = scope.overlayoffsety;
            
    
                            scope.overlayrotation = 0;
                            scope.backupoverlayrotation = scope.overlayrotation;
    
                        }else{

                            if(scope.parent.loadoverlayvectors){
                                scope.x = loader.x;
                                scope.y = loader.y;
                                scope.w = loader.w;
                                scope.h = loader.h;
                                scope.scale = loader.scale;
                                scope.offsetx = loader.offsetx;
                                scope.offsety = loader.offsety;
        
                                scope.fileversion = loader.flag;
                                scope.flag = loader.flag;
        
                                scope.width = scope.w - scope.x;
                                scope.height = scope.h - scope.y;
        
    
                                width = loader.w - loader.x;
                                height = loader.h - loader.y;
    
                                scope.overlayscale = Math.min((width / scope.width), (height / scope.height));
                                scope.backupoverlayscale = scope.overlayscale;
                                //scope.overlayscale = Math.min((scope.width / width), (scope.height / height));
                            
                                scope.overlayrotation = 0;
                                scope.backupoverlayrotation = scope.overlayrotation;
                            }else{
                                width = loader.w - loader.x;
                                height = loader.h - loader.y;
        
                                scope.overlayscale = Math.min((scope.width / width), (scope.height / height));
                                scope.backupoverlayscale = scope.overlayscale;
                                
                                scope.overlayoffsetx = loader.x;
                                scope.overlayoffsety = loader.h;
                                scope.backupoverlayoffsetx = scope.overlayoffsetx;
                                scope.backupoverlayoffsety = scope.overlayoffsety;
    
        
                                scope.overlayrotation = 0;
                                scope.backupoverlayrotation = scope.overlayrotation;
    
                            }


                        }

                        

                    }
                    
                    if (role == 1){

                        if(scope.parent.loadoverlayvectors && !scope.parent.loadbackgroundvectors){
                            var width = loader.w - loader.x;
                            var height = loader.h - loader.y;
    
    
                            scope.overlayscale = Math.min((scope.width / width), (scope.height / height));
                            scope.backupoverlayscale = scope.overlayscale;
                            
                            scope.overlayoffsetx = loader.x;
                            scope.overlayoffsety = loader.h;

                            scope.backupoverlayoffsetx = scope.overlayoffsetx;
                            scope.backupoverlayoffsety = scope.overlayoffsety;

    
                            scope.overlayrotation = 0;
                            scope.backupoverlayrotation = scope.overlayrotation;

                        }else{

                            scope.x = loader.x;
                            scope.y = loader.y;
                            scope.w = loader.w;
                            scope.h = loader.h;
                            scope.scale = loader.scale;
                            scope.offsetx = loader.offsetx;
                            scope.offsety = loader.offsety;
    
                            scope.fileversion = loader.flag;
                            scope.flag = loader.flag;
    
                            scope.width = scope.w - scope.x;
                            scope.height = scope.h - scope.y;
    
                            width = loader.w - loader.x;
                            height = loader.h - loader.y;

                            scope.overlayscale = Math.min((width / scope.width), (height / scope.height));
                            scope.backupoverlayscale = scope.overlayscale;
                        
                            //scope.overlayoffsetx = loader.x - (scope.x / scope.overlayscale);
                            //scope.overlayoffsety = loader.h - (scope.h / scope.overlayscale);

                            //scope.overlayoffsetx = 0;
                            //scope.overlayoffsety = 0;
                            scope.overlayrotation = 0;
                            scope.backupoverlayrotation = scope.overlayrotation;
                        }

                        

                    }

                };

                this.updateAttributes = function(AttributeObject, role){

                    //vectorobj.updateAttributes(AttributeObject, readstate.role)

                    //vectorobj.attributes.push(AttributeObject);


                    switch (role){
                        case 0 :
                            scope.attributes.push(AttributeObject);

    
                        break;
                        case 1 : 

                            scope.attributesbg.push(AttributeObject);

                        break;
                        case 2 : 

                            scope.attributesfg.push(AttributeObject);

                        break;


                    }

                };

                this.updatetextlist = function(textobject, role){


                    switch (role){
                        case 0 :
                            scope.textlist.unshift(textobject);
                            
    
                        break;
                        case 1 : 
                            
                            scope.textlistbg.unshift(textobject);
                            
                            
                        break;
                        case 2 : 
                            
                            scope.textlistfg.unshift(textobject);
                            
                            
                        break;


                    }
                    
                };


                this.imageloadcomplete = function(role){

                    if(scope.parent.loadbackgroundimage){
                        if(role == 1){

                            scope.setcompareColors();
                            scope.parent.redraw();

                            if(scope.cmprimagestoload > 0){
                                scope.cmprimagestoload -= 1;
                            }
    

                        }
                    }
                    if(scope.parent.loadoverlayimage){
                        if(role == 2){
                            
                            scope.setcompareColors();
                            scope.parent.redraw();

                            if(scope.cmprimagestoload > 0){
                                scope.cmprimagestoload -= 1;
                            }

        
                        }

                    }

                    if(scope.parent.loadbackgroundpdf){
                        if(role == 1){

                            //scope.setcompareColors();
                            scope.parent.redraw();

                            if(scope.cmprimagestoload > 0){
                                scope.cmprimagestoload -= 1;
                            }
    

                        }

                    }

                    if(scope.parent.loadoverlaypdf){
                        if(role == 2){

                            //scope.setcompareColors();
                            scope.parent.redraw();

                            if(scope.cmprimagestoload > 0){
                                scope.cmprimagestoload -= 1;
                            }
    

                        }

                    }
                    


                    var imagesloaded = (scope.cmprimagestoload == 0);
                    var vectorsloaded = (scope.cmprvctrsrctoload == 0);

                    if(scope.bcompare && imagesloaded && vectorsloaded){

                        if(RxCore_GUI_compareLoadComplete != undefined){
                            RxCore_GUI_compareLoadComplete.compareComplete(scope.parent.DocRef.fileindex);
                        }

                        
                    }


                };
                

                this.readbinarycomplete = function(role){


                    scope.pathlistoverride = [];
                    scope.intersections = [];

                    var biscompare = false;

                    /*switch (role){
                        case 0 :
                            scope.pathlist = [];
                            scope.textlist = [];
                            scope.attributes = [];
                            scope.numpaths = 0;
    
                        break;
                        case 1 : 
                            scope.pathlistbg = [];
                            scope.textlistbg = [];
                            scope.attributesbg = [];
                            scope.numpathsbg = 0;
                        break;
                        case 2 : 
                            scope.pathlistfg = [];
                            scope.textlistfg = [];
                            scope.attributesfg = [];
                            scope.numpathsfg = 0;
                        break;


                    }*/

                    if(role == 0){
                        
                        //scope.blocklist.push(blockobj);


                    }else if(role == 1){


                        biscompare = true;

                        scope.blocklistovoffset = scope.blocklistbg.length;
                        
        

                        scope.blocklist = scope.blocklistbg.concat(scope.blocklistfg);

                        scope.pathlist = scope.pathlistbg.concat(scope.pathlistfg);


                        scope.textlist = scope.textlistbg.concat(scope.textlistfg);
                        scope.attributes = scope.attributesbg.concat(scope.attributesfg);

                        
                            //scope.blocklistbg.push(blockobj);//

                       if (scope.cmprvctrsrctoload > 0){
                            scope.cmprvctrsrctoload -= 1;
                        }
    
                        
            
                    }else if(role == 2){


                        biscompare = true;

                        scope.blocklistovoffset = scope.blocklistbg.length;
                        
        

                        scope.blocklist = scope.blocklistbg.concat(scope.blocklistfg);
                        
                        scope.pathlist = scope.pathlistbg.concat(scope.pathlistfg);
                        
                        scope.textlist = scope.textlistbg.concat(scope.textlistfg);
                        scope.attributes = scope.attributesbg.concat(scope.attributesfg);


                        if (scope.cmprvctrsrctoload > 0){
                            scope.cmprvctrsrctoload -= 1;
                        }

                        //scope.blocklistfg.push(blockobj);//

                    }




                    //scope.filterblocklist = scope.blocklist.slice();

                    scope.filterblocklist = scope.blocklist.filter(function (block) {
                        
                        return block.listed;

                    });


                    /*var res = undefined;

                    if(block.listed){
                        
                    }*/


                    scope.filterAttributelist = scope.attributes.slice();


                    if (RxCore_GUI_VectorBlocks != undefined) {
                        RxCore_GUI_VectorBlocks.setVectorBlocks(scope.filterblocklist);
                    }
                    if (RxCore_GUI_VectorBlocksLoaded != undefined) {
                        RxCore_GUI_VectorBlocksLoaded.setVectorBlocks(scope.filterblocklist);
                    }
                    if (scope.docompare) {
                        //comparedrawcheck();
                    }
                    else {
                        //DocObj.getCurrentpage().draw_vector(true);
                        //DocObj.getCurrentpage().redraw();

                        scope.parent.redraw();
                        scope.setcompareColors();

                        //scope.
                       // var scope = this;
                        //this.parent = page;
        

                    }
                    if (scope.textlist.length > 0) {
                        if (RxCore_GUI_HasText != undefined) {
                            RxCore_GUI_HasText.hastext = true;
                            scope.parent.DocRef.hastext = true;
                            RxCore_GUI_HasText.hasText(scope.parent.DocRef.hastext, scope.parent.DocRef.bActive);
                        }

                        if(scope.parent.DocRef.pages[scope.parent.DocRef.currentpage]){
                            if (scope.parent.DocRef.pages[scope.parent.DocRef.currentpage].VectorPageObj != undefined) {
                                scope.parent.DocRef.pages[scope.parent.DocRef.currentpage].settextdivs(true);
                            }
                        }

                    }
                    else {
                        if (RxCore_GUI_HasText != undefined) {
                            RxCore_GUI_HasText.hastext = false;
                            scope.parent.DocRef.hastext = false;
                            RxCore_GUI_HasText.hasText(scope.parent.DocRef.hastext, scope.parent.DocRef.bActive);
                        }
                    }


                    var imagesloaded = (scope.cmprimagestoload == 0);
                    var vectorsloaded = (scope.cmprvctrsrctoload == 0);

                    if(scope.bcompare && imagesloaded && vectorsloaded){

                        if(RxCore_GUI_compareLoadComplete != undefined){
                            RxCore_GUI_compareLoadComplete.compareComplete(scope.parent.DocRef.fileindex);
                        }

                        
                    }


                    if(bDefaultpentable && DefaultpentableJSON.length > 0){

                        if (RxCore_GUI_penTableJSON != undefined){
                            RxCore_GUI_penTableJSON.setPentablelist(DefaultpentableJSON, scope.parent.DocRef.bActive);
                        }

                        if (RxCore_GUI_autopenTable != undefined){
                            RxCore_GUI_autopenTable.setAutoPenLoad();
                        }
            

                        
                    }


                    if(bsetcallbackfilepages){
                        var stateobj = {
                            iscompare:documentcompare,
                            numOpenFiles:OpenFiles.length,
                            isPDF:false,
                            is3D:false,
                            is2D:true,
                            numpages:parent.DocRef.pages.length,
                            currentpage:parent.DocRef.getcurPage(),
                            activefile : parent.DocRef.bActive,
                            source : 'vectorbinaryloadcomplete' 
                        };
            
                        if (RxCore_GUI_State != undefined) {
                            RxCore_GUI_State.setGUIState(stateobj);
                        }
        
                        var pagingobject = {
                            numpages:parent.DocRef.pages.length,
                            currentpage:parent.DocRef.getcurPage()
                        };
            
                        if(RxCore_GUI_Page != undefined){
                            RxCore_GUI_Page.pageEvent(pagingobject);
                        }
                        bsetcallbackfilepages = false;
    
                    }
                };

                this.addvectors = function(pagexmldata, compareobj){


                    if(compareobj){
                        //scope.vectorloader = new VectorLoader(scope, pagexmldata, false, compareobj);
                        //scope.vectorloader.addVectors(scope, pagexmldata, compareobj);
                        scope.addvectorloader = new VectorLoader(scope, pagexmldata, compareobj);
                    }else{
                        //scope.vectorloader = new VectorLoader(scope, pagexmldata, false);
                        //scope.vectorloader.addVectors(scope, pagexmldata);
                    }


                    //scope.vectorloader.addVectors(scope, pagexmldata);

                    //scope.vectorloader = new VectorLoader(scope, pagexmldata, false);
                };

                this.refreshdraw = function() {
                    scope.parent.redraw();
                };

                this.resetCompare = function(){


                    scope.overlayscale = scope.backupoverlayscale;
                    scope.overlayoffsetx = scope.backupoverlayoffsetx;
                    scope.overlayoffsety = scope.backupoverlayoffsety;
                    scope.overlayrotation = scope.backupoverlayrotation;

                    scope.nOffsetX = 0;
                    scope.nOffsetY = 0;
    
                    scope.refreshdraw();

                };

                this.setcomparescale = function (scalearray) {
                    scope.nudgeScale(2);
                    scope.resetCompare();

                    var distscale = scope.overlayscale;
                    var offsetx = scope.overlayoffsetx;
                    var offsety = scope.overlayoffsety;
                    var bgangle = scope.overlayrotation;
                    //scope.overlayrotation = 0;

                    //scalearray[0].offset.x * scope.overlayscale;
                    //scalearray[0].offset.y * scope.overlayscale;


                    var scalerole = 0;

                    if(scope.parent.loadbackgroundvectors){

                        scalerole = 2;

                    }else{
                        if(scope.parent.loadoverlayvectors){
                            
                            scalerole = 1;

                        }else{
                            
                            scalerole = 2;

                        }
                    }

                    //console.log(scalearray);



                    /*if(scope.pathlist[pindx].role == scalerole){
                            
                        mediax = scope.overlayoffsetx;
                        mediah = scope.overlayoffsety;
                        
                        localscalefactor = scalefactor * scope.overlayscale;

                    }else{
                        mediax = scope.x;
                        mediah = scope.h;
                        localscalefactor = scalefactor;
                    }*/

                    //scalearray[i].dist

                    //scope.backgroundMScale = scalearray[i].dist / scalearray[i].pwidth;
                    //scope.scale = loader.scale;
                    //scope.offsetx = loader.offsetx;
                    //scope.offsety = loader.offsety;

                    if(scalearray.length == 2){

                        if (scalerole == 1){
                            //distscale = (scalearray[1].dist / scalearray[0].dist) * scope.overlayscale;

                            //vector on image
                            //vector on pdf


                            distscale = scalearray[1].scaleinfo.scaledDistance / scalearray[0].scaleinfo.scaledDistance;

                            //offsetx = (scalearray[0].offset.x * scope.overlayscale) - scalearray[1].offset.x;
                            //offsety = (scalearray[0].offset.y * scope.overlayscale) - scalearray[1].offset.y;
                            offsetx = (scalearray[1].offset.x / distscale) - scalearray[0].offset.x;
                            offsety = (scalearray[1].offset.y / distscale) - scalearray[0].offset.y;


                            bgangle = scalearray[0].angle - scalearray[1].angle;

                            scope.overlayscale = distscale;
                            




                            if(offsetx > 0){
                                scope.overlayoffsetx = scope.overlayoffsetx - offsetx;    

                                var bgpw = scalearray[1].scaleinfo.pagedim.origw;
                                var fgpw = (scalearray[0].scaleinfo.pagedim.origw * scope.overlayscale) + (offsetx * scope.overlayscale);

                                /*pw = scalearray[1].scaleinfo.pagedim.origw + offsetx;

                                if((pw * scope.overlayscale) >  scope.width){
                                    scope.width = scope.width + ((pw * scope.overlayscale) - scope.width);
                                }*/
    
                            }else{
                                scope.x = scope.x - (-offsetx * scope.overlayscale);

                                bgpw = scalearray[1].scaleinfo.pagedim.origw + (-offsetx * scope.overlayscale);
                                fgpw = (scalearray[0].scaleinfo.pagedim.origw * scope.overlayscale);


                            }

                            if(offsety > 0){
                                scope.overlayoffsety = scope.overlayoffsety + offsety;

                                var bgph = scalearray[1].scaleinfo.pagedim.origh;
                                var fgph = (scalearray[0].scaleinfo.pagedim.origh * scope.overlayscale) + (offsety * scope.overlayscale);

                                /*ph = scalearray[1].scaleinfo.pagedim.origh + offsety;

                                if((ph * scope.overlayscale) >  scope.height){
                                    scope.height = scope.height + ((ph * scope.overlayscale) - scope.height);
                                }*/
    
                            }else{
                                scope.h = scope.h + (-offsety * scope.overlayscale);

                                bgph = scalearray[1].scaleinfo.pagedim.origh + (-offsety * scope.overlayscale);
                                fgph = (scalearray[0].scaleinfo.pagedim.origh * scope.overlayscale);


                            }


                            
                            //if distscale < 1 the overlay is larger
                            if(scope.overlayscale < 1){
                                var pw = bgpw - fgpw;// + offsetx;
                                //pw = fgpw + pw;
                                if(pw > 0){
                                    pw = fgpw + pw;
                                }else{
                                    pw = fgpw;
                                }
                                
                            }else if (scope.overlayscale > 1){
                                
                                pw = fgpw - bgpw;// + offsetx;
                                if(pw > 0){
                                    pw = bgpw + pw;
                                }else{
                                    pw = bgpw;
                                }


                            }


                            if(scope.overlayscale < 1){
                                
                                var ph = bgph - fgph; // + offsety; 
                                if(ph > 0){
                                    ph = fgph + ph;// + offsety;
                                }else{
                                    ph = fgph;// + ph;// + offsety;
                                }
                                
                                
                            }else if (scope.overlayscale > 1){


                                ph = fgph - bgph;
                                if(ph > 0){
                                    ph = bgph + ph; 
                                }else{
                                    ph = bgph; 
                                }

                                
                            }


                            if(pw > scope.width){
                                scope.width = pw;
                            }
                            

                            if(ph  >  scope.height){
                                scope.height = ph;
                            }

                            /*if((pw * scope.overlayscale) >  scope.width){
                                scope.width = scope.width + ((pw * scope.overlayscale) - scope.width);
                            }else{
                                pw = scalearray[1].scaleinfo.pagedim.origw + (-offsetx * scope.overlayscale);

                                if(pw > scope.width){
                                    //scope.width = scope.width + (pw - scope.width);
                                    scope.width = pw;
                                }

                            }*/


                            

                            /*if((ph * scope.overlayscale) >  scope.height){
                                scope.height = scope.height + ((ph * scope.overlayscale) - scope.height);
                            }else{
                                ph = scalearray[1].scaleinfo.pagedim.origh + (-offsety * scope.overlayscale);

                                if(ph > scope.height){
                                    //scope.height = scope.height + (ph - scope.height);
                                    scope.height = ph;
                                }

                            }*/


                            /*scope.overlayoffsetx = scope.overlayoffsetx - offsetx;
                            scope.overlayoffsety = scope.overlayoffsety + offsety;


                            var pw = scalearray[0].scaleinfo.pagedim.origw + offsetx;
                            var ph = scalearray[0].scaleinfo.pagedim.origh + offsety;


                            if((pw * scope.overlayscale) >  scope.width){
                                scope.width = scope.width + ((pw * scope.overlayscale) - scope.width);
                            }

                            if((ph * scope.overlayscale) >  scope.height){
                                scope.height = scope.height + ((ph * scope.overlayscale) - scope.height);
                            }*/

                            //scope.overlayoffsetx = offsetx;
                            //scope.overlayoffsety = offsety;
                            scope.overlayrotation = bgangle;

        
                        }if (scalerole == 2){


                            //vector on vector
                            //image on image
                            //pdf on pdf

                            //scope.overlayscale = Math.min((scope.width / width), (scope.height / height));
                            distscale = scalearray[0].scaleinfo.scaledDistance / scalearray[1].scaleinfo.scaledDistance;

                            //scope.overlayoffsetx = loader.x;
                            //scope.overlayoffsety = loader.h;


                            //distscale = (scalearray[0].dist / scalearray[1].dist) * scope.overlayscale;

                            offsetx = (scalearray[0].offset.x / distscale) - scalearray[1].offset.x;
                            offsety = (scalearray[0].offset.y / distscale) - scalearray[1].offset.y;

                            bgangle = scalearray[1].angle - scalearray[0].angle;


                            scope.overlayscale = distscale;
                            




                            if(offsetx > 0){
                                //overlay is moved in x dir
                                scope.overlayoffsetx = scope.overlayoffsetx - offsetx;    

                                bgpw = scalearray[0].scaleinfo.pagedim.origw;
                                fgpw = (scalearray[1].scaleinfo.pagedim.origw * scope.overlayscale) + (offsetx * scope.overlayscale);


                                //pw = scalearray[1].scaleinfo.pagedim.origw + offsetx;
                                /*pw = scalearray[1].scaleinfo.pagedim.origw + offsetx;

                                if((pw * scope.overlayscale) >  scope.width){
                                    scope.width = scope.width + ((pw * scope.overlayscale) - scope.width);
                                }*/
    
                            }else{
                                //background is moved in x dir
                                scope.x = scope.x - (-offsetx * scope.overlayscale);

                                bgpw = scalearray[0].scaleinfo.pagedim.origw + (-offsetx * scope.overlayscale);
                                fgpw = (scalearray[1].scaleinfo.pagedim.origw * scope.overlayscale);


                            }

                            if(offsety > 0){
                                //overlay is moved in y dir
                                scope.overlayoffsety = scope.overlayoffsety + offsety;

                                bgph = scalearray[0].scaleinfo.pagedim.origh;
                                fgph = (scalearray[1].scaleinfo.pagedim.origh * scope.overlayscale) + (offsety * scope.overlayscale);

                                
                                //ph = scalearray[1].scaleinfo.pagedim.origh + offsety;
                                //ph = scalearray[1].scaleinfo.pagedim.origh + offsety;

                                /*if((ph * scope.overlayscale) >  scope.height){
                                    scope.height = scope.height + ((ph * scope.overlayscale) - scope.height);
                                }*/
    
                            }else{
                                //background is moved in y dir
                                scope.h = scope.h + (-offsety * scope.overlayscale);
                                
                                bgph = scalearray[0].scaleinfo.pagedim.origh + (-offsety * scope.overlayscale);
                                fgph = (scalearray[1].scaleinfo.pagedim.origh * scope.overlayscale);


                            }

                            
                            //if distscale < 1 the background page is larger
                            if(scope.overlayscale < 1){
                                pw = fgpw - bgpw;// + offsetx;
                                if(pw > 0){
                                    pw = bgpw + pw;
                                }else{
                                    pw = bgpw;
                                }
                                
                            }else if (scope.overlayscale > 1){
                                
                                pw = bgpw - fgpw;// + offsetx;
                                //pw = fgpw + pw;
                                if(pw > 0){
                                    pw = fgpw + pw;
                                }else{
                                    pw = fgpw;
                                }
                            }


                            if(scope.overlayscale < 1){
                                
                                ph = fgph - bgph;
                                if(ph > 0){
                                    ph = bgph + ph; 
                                }else{
                                    ph = bgph; 
                                }
                                
                                
                            }else if (scope.overlayscale > 1){

                                ph = bgph - fgph; // + offsety; 
                                if(ph > 0){
                                    ph = fgph + ph;// + offsety;
                                }else{
                                    ph = fgph;// + ph;// + offsety;
                                }
                                
                            }

                            

                            if(pw > scope.width){
                                scope.width = pw;
                            }else{
                                /*pw = scalearray[0].scaleinfo.pagedim.origw + (-offsetx * scope.overlayscale);

                                if(pw > scope.width){
                                    //scope.width = scope.width + (pw - scope.width);
                                    scope.width = pw;
                                }*/

                            }

                            

                            if(ph  >  scope.height){
                                scope.height = ph;
                            }else{
                                /*ph = scalearray[0].scaleinfo.pagedim.origh + (-offsety * scope.overlayscale);

                                if(ph > scope.height){
                                    //scope.height = scope.height + (ph - scope.height);
                                    scope.height = ph;
                                }*/

                            }
                            
                            //var bgpagerect = 

                            //origw: thispage.originalwidth,
                            //origh: thispage.originalheight,
    
                            //test for now. Need original widht and height for overlay in nonscaled values
                            //scope.height = scope.height + offsety * distscale;
                            scope.overlayrotation = bgangle;

                            //var distscale = scope.overlayscale;
                            //var offsetx = scope.overlayoffsetx;
                            //var offsety = scope.overlayoffsety;
                            //var bgangle = scope.overlayrotation;
        

                        }

                        

                        //scope.backgroundAlOffsetX = scalearray[i].offset.x * pdfscale;
                        //scope.backgroundAlOffsetY = scalearray[i].offset.y * pdfscale;
                        //scope.backgroundAlOffsetXunscaled = scalearray[i].offset.x;
                        //scope.backgroundAlOffsetYunscaled = scalearray[i].offset.y;

                    }


                    /*for (var i = 0; i < scalearray.length; i++) {
                        if (i == 0) {
                            
                        }
                    }*/

                    //for images and pdf the original page width should be reflected in the image object width
                    //for vectors the background and overlay vectors should be calculated from scope width and height and 

                    //these original values
                    //scope.backupoverlayscale
                    //scope.backupoverlayoffsetx
                    //scope.backupoverlayoffsety

                    
                    
                };
                this.nudgeRotate = function (clockwise) {
                    //0.01745329252
                    if (clockwise) {
                        scope.fgdrawangle -= 0.001;
                        //scope.overlayrotation -= 0.001;
                    }
                    else {
                        scope.fgdrawangle += 0.001;
                        //scope.overlayrotation += 0.001;
                    }
                    scope.bnudgeangleused = true;

                    scope.refreshdraw();
                };
                this.nudgeScale = function (updown) {
                    var onepermillescale = scope.overlayscale / 5000;
                    /*0 - up
                    1 - down
                    2 - reset*/
                    switch (updown) {
                        case 0:
                            scope.nudgeScalefactor += onepermillescale;
                            break;
                        case 1:
                            scope.nudgeScalefactor -= onepermillescale;
                            break;
                        case 2:
                            scope.nudgeScalefactor = 0;
                    }
                    
                    scope.refreshdraw();
                };
                this.offsetdirect = function (x, y) {
                    
                    scope.nOffsetX = x;
                    scope.nOffsetY = y;
                    scope.refreshdraw();


                };
                this.nudgeoffset = function (direction) {
                    /*0 - left
                    1 - right
                    2 - up
                    3 - down*/


                    //scope.overlayoffsetx = offsetx;
                    //scope.overlayoffsety = offsety;
                    
                    if(scope.parent.fixedScale != undefined){
                        var scaledIncrement = 0.1 * scope.parent.fixedScale;
                    }else{
                        scaledIncrement = 0.1
                    }


                    switch (direction) {
                        case 0:
                            scope.nOffsetX += scaledIncrement;
                            break;
                        case 1:
                            scope.nOffsetX -= scaledIncrement;
                            break;
                        case 2:
                            scope.nOffsetY -= scaledIncrement;
                            break;
                        case 3:
                            scope.nOffsetY += scaledIncrement;
                            break;
                    }


                    
                    scope.refreshdraw();

                };
                this.scalecomparefinish = function () {
                    
                    DeactivateAll();
                    //scope.parent.DocRef.SetActive();
                    RxCore_setActiveFile(scope.parent.DocRef.fileindex);

                    if (RxCore_GUI_ActivateFile != null) {
                        RxCore_GUI_ActivateFile.setActiveFile(scope.parent.DocRef.fileindex);
                    }

                    //no longer needed as setactive does this.
                    //set_tool('markupedit', { p1: false });

                };
                this.alignpage = function (index) {


                    /*for (var i = 0; i < OpenFiles.length; i++) {
                        if(OpenFiles[i].fileindex == scope.parent.comparebgdocindex){
                            
                            this.bgDocObjref = OpenFiles[i];
                            
                         }
                         if( OpenFiles[i].fileindex == scope.parent.comparefgdocindex ){
                            this.fgDocObjref = OpenFiles[i];
                         }

                    }*/



                    //introduce setActiveFilePage that makes sure the correct page of the activated document is set as currentpage.
                    if(index == 0){

                        RxCore_setActiveFile(scope.parent.comparebgdocindex);
                        scope.bgDocObjref.GotoPage(scope.bgpageindex);

                        scope.bgDocObjref.pages[scope.bgpageindex].enableSnap(true);

                        if (RxCore_GUI_ActivateFile != null) {
                            RxCore_GUI_ActivateFile.setActiveFile(scope.parent.comparebgdocindex);
                        }
    

                        

                        if(scope.bgpageindex != undefined){
                            set_tool('getoverlayscale', { p1: scope.bgDocObjref.pages[scope.bgpageindex], p2: 0 });
                        }
                    }
                    if(index == 1){

                        RxCore_setActiveFile(scope.parent.comparefgdocindex);
                        scope.fgDocObjref.GotoPage(scope.fgpageindex);

                        scope.fgDocObjref.pages[scope.fgpageindex].enableSnap(true);

                        if (RxCore_GUI_ActivateFile != null) {
                            RxCore_GUI_ActivateFile.setActiveFile(scope.parent.comparefgdocindex);
                        }


                        if(scope.fgpageindex != undefined){
                            set_tool('getoverlayscale', { p1: scope.fgDocObjref.pages[scope.fgpageindex], p2: 1 });
                        }

                    }
                    
                    
                    //this.bgpageindex = undefined;
                    //this.fgpageindex = undefined;
    

                    

                    //scope.pages[index].usedincomposite = false;
                    //get file index of page index used for to create this compare
                    //RxCore_setActiveFile(scope.pages[index].DocRef.fileindex);
                    //scope.pages[index].enableSnap(true);
                    
                };
                this.scalecomparepage = function (scalearray) {
                    
                    var scaleindex = 0;

                    if (scalearray.length == 0) {
                        scope.resetCompare();
                        scaleindex = 0;
                        scope.alignpage(scaleindex);
                    }
                    else if (scalearray.length == 1) {
                        scaleindex = 1;
                        scope.alignpage(scaleindex);
                    }
                    else if (scalearray.length == 2) {
                        //scope.scalecomparefinish();
                        scope.setcomparescale(scalearray);
                        scope.scalecomparefinish();
                    }
                };
                this.scalecompare = function () {
                    scope.resetCompare();
                    //scope.pages[scope.scaleindex].usedincomposite = false;
                    //RxCore_setActiveFile(scope.pages[scope.scaleindex].DocRef.fileindex);

                    RxCore_setActiveFile(scope.parent.comparebgdocindex);
                    scope.bgDocObjref.GotoPage(scope.bgpageindex);
                    scope.bgDocObjref.pages[scope.bgpageindex].enableSnap(true);
                    //scope.parent.comparebgdocindex

                    //scope.pages[scope.scaleindex].enableSnap(true);
                    if (RxCore_GUI_CompareAlign != undefined) {
                        RxCore_GUI_CompareAlign.AlignComplete(0);
                    }
                    set_tool('overlayscale', { p1: scope });
                    
                    //RxCore_setActiveFile(indx);
                };


                /*this.scalecompare = function () {
                    scope.resetCompare();
                    scope.pages[scope.scaleindex].usedincomposite = false;
                    RxCore_setActiveFile(scope.pages[scope.scaleindex].DocRef.fileindex);
                    scope.pages[scope.scaleindex].enableSnap(true);
                    if (RxCore_GUI_CompareAlign != undefined) {
                        RxCore_GUI_CompareAlign.AlignComplete(0);
                    }
                    set_tool('overlayscale', { p1: scope });
                    
                    //RxCore_setActiveFile(indx);
                };*/



                this.setColors = function (bgColor, fgColor) {
                    scope.backgroundColor = bgColor;
                    scope.overlayColor = fgColor;


                    scope.setcompareColors();
                    scope.parent.redraw();


                    //scope.invertimages();
                    //scope.draw_compare(true);

                    if (RxCore_GUI_CompareDiag != undefined) {
                        RxCore_GUI_CompareDiag.CompareDialog();
                    }
                };
                this.getColors = function () {
                    return {
                        bg: scope.backgroundColor,
                        fg: scope.overlayColor
                    };
                };


                this.getInterSections = function () {
                    this.intersections = [];
                    for (var i = 0; i < this.pathlist.length; i++) {
                        var blayeron = layeron(i);
                        var bblcckon = blockon(i);
                        if (blayeron && bblcckon && this.pathlist[i].type == "line") {
                            for (var j = 0; j < this.pathlist.length; j++) {
                                if (i != j) {
                                    var blayeron = layeron(j);
                                    var bblcckon = blockon(j);
                                    if (blayeron && bblcckon) {
                                        if (this.pathlist[j].type == "line") {
                                            var intersect = this.pathlist[j].findInterSections(this.pathlist[i]);
                                            if (intersect) {
                                                this.intersections.push(intersect);
                                            }
                                        }
                                    }
                                }
                            }
                        }
                    }
                };
                
                this.getBlockswFill = function(){
                    var fillblocklist = [];

                    for (var bcount = 0; bcount < scope.blocklist.length; bcount++) {
                        
                        if(scope.blocklist[bcount].overridecolor || scope.blocklist[bcount].overridefill){
                            fillblocklist.push(scope.blocklist[bcount]);
                        }
                    }

                    return fillblocklist;

                };

                this.getBlocks = function () {
                    return scope.filterblocklist;
                };
                this.getFilteredAttributes = function () {
                    return scope.filterAttributelist;
                };
                //getFilteredAttributes
                this.restoreBlockStates = function () {
                    for (var bcount = 0; bcount < scope.blocklist.length; bcount++) {
                        scope.blocklist[bcount].overridecolor = false;
                        scope.blocklist[bcount].color = scope.blocklist[bcount].defaultcolor;
                    }
                };
                this.setBlockColor = function (blockid, Color, override) {
                    //var blockindex = 0;
                    var blockindex = getBlockIndex(blockid);
                    scope.blocklist[blockindex].overridecolor = override;
                    if (override) {
                        scope.blocklist[blockindex].color = Color;
                    }
                    else {
                        scope.blocklist[blockindex].color = scope.blocklist[blockindex].defaultcolor;
                    }
                    /*for(var bcount = 0;bcount < scope.blocklist.length;bcount++){
                        if (scope.blocklist[bcount].index == blockid){
                            scope.blocklist[bcount].color = Color;
                            break;
                        }
                    }*/
                };


                this.adddrawfirstlist = function(pathobj){
                    scope.pathlistoverride.push(pathobj);

                    scope.parent.refresh_vectorPath(pathobj);
                    //draw new fill immediatly or refresh

                };

                this.removedrawfirstlist = function(pathobj){

                    var removeix = undefined;

                    for (var i = 0; i < scope.pathlistoverride.length; i++){
                        if(pathobj.index == scope.pathlistoverride[i].index){
                            removeix = i;
                        }
                    }

                    if (removeix != undefined){
                        scope.pathlistoverride.splice(removeix, 1);
                    }

                    //draw new fill immediatly or refresh
                    //refresh_vectorPath(pathobj);
                    scope.parent.refresh_vectorPath(pathobj);

                };

                this.setBlockHatch = function(blockid , hatchname, override){
                    var blockindex = getBlockIndex(blockid);
                    //scope.blocklist[blockindex].overridecolor = override;
                    scope.blocklist[blockindex].overridefill = override;


                    if (override) {
                        //scope.blocklist[blockindex].color = Color;
                        scope.blocklist[blockindex].fillstyle = 3; //hatch
                        scope.blocklist[blockindex].hatchstyle = hatchname;

                        setHatchinUse(hatchname, true);

                    }
                    else {
                        scope.blocklist[blockindex].fillstyle = scope.blocklist[blockindex].defaultfillstyle;
                        setHatchinUse(hatchname, false);
                    }

                };

                this.hasAttribute = function(blockid, attrname){
                    var blockindex = getBlockIndex(blockid);
                    var hasattrib = false;

                    for (var attrc = 0; attrc < scope.attributes.length; attrc++) {
                        if (scope.attributes[attrc].name.toUpperCase() == attrname.toUpperCase() && scope.attributes[attrc].blockref == blockindex) {
                            hasattrib = true;
                            break;
                        }
                    }
                    return hasattrib;

                };

                this.updateAttribute = function(blockid, name, value){
                    var blockindex = getBlockIndex(blockid);
                
                    for (var attrc = 0; attrc < scope.attributes.length; attrc++) {
                        if (scope.attributes[attrc].name.toUpperCase() == name.toUpperCase() && scope.attributes[attrc].blockref == blockindex) {
                            scope.attributes[attrc].value = value;
                            break;
                        }

                    }

                };

                this.appendCustomBlockAttribute = function (blockid, name, value) {
                    var blockindex = getBlockIndex(blockid);
                    var AttributeObject = {
                        name: name,
                        value: value,
                        blockref: blockindex
                    };
                    scope.attributes.push(AttributeObject);
                };

                this.deleteAttribute = function (szName) {

                    var tempattributes = [];

                    for (var attrc = 0; attrc < scope.attributes.length; attrc++) {
                        if (scope.attributes[attrc].name.toUpperCase() != szName.toUpperCase()) {
                            
                            tempattributes.push(scope.attributes[attrc]);
                            //scope.attributes[attrc].value = value;
                        }
                    }
                    scope.attributes = tempattributes.slice();

                };

                this.FindBlockByAttr = function (attrname, attrvalue) {
                    var blockref = -1;
                    var blockindex = -1;
                    //matchRuleShort(block.name, szBlockLoadMask);
                    for (var attrc = 0; attrc < scope.attributes.length; attrc++) {
                        if (scope.attributes[attrc].name.toUpperCase() == attrname.toUpperCase() && scope.attributes[attrc].value == attrvalue) {
                            blockref = scope.attributes[attrc].blockref;
                            break;
                        }
                    }
                    if (blockref != -1) {
                        blockindex = scope.blocklist[blockref].index;
                        /*for(var bcount = 0;bcount < scope.blocklist.length;bcount++){
                            if (scope.blocklist[bcount].index == blockref){
                                blockindex = bcount;
                                break;
                            }
                        }*/
                    }
                    return blockindex;
                };
                this.ViewMode = function (onoff) {
                    scope.viewmode = onoff;
                };
                this.setPenTable = function (onoff) {
                    scope.applypentable = onoff;
                };

                this.getPenTableState = function () {
                    return scope.applypentable;
                };


                this.unSelectAllBlockInserts = function(){

                    for (var i = 0; i < scope.blocklist.length; i++) {
                        
                        scope.blocklist[i].unselect();
                        scope.blocklist[i].mouseon = false;
                    }



                };

                this.unSelectAllBlocks = function (notify) {
                    for (var i = 0; i < scope.pathlist.length; i++) {
                        if (scope.pathlist[i].type == "path") {
                            scope.pathlist[i].unselect();
                        }
                    }
                    var partlist = [];
                    if (notify) {
                        if (RxCore_GUI_2DBlockInfo != undefined) {
                            RxCore_GUI_2DBlockInfo.set2DBlockInfo(partlist);
                        }
                    }
                };
                this.blockAttributes = function (blockid) {
                    var partlist = [];
                    var blockindex = getBlockIndex(blockid);
                    partlist = getAttributes(blockindex);
                    return partlist;
                };
                
                this.selectBlockPath = function (select, blockid) {
                    //var blockindex = getBlockIndex(blockid);
                    for (var i = 0; i < scope.pathlist.length; i++) {
                        var blayeron = layeron(i);
                        var bblcckon = blockon(i);
                        if (blayeron && bblcckon) {
                            if (scope.pathlist[i].blockname == blockid) {
                                if (scope.pathlist[i].type == 'path') {
                                    if (select) {
                                        scope.pathlist[i].select();
                                    }
                                    else {
                                        scope.pathlist[i].unselect();
                                    }
                                }
                            }
                        }
                    }
                };
                this.selectBlock = function (blockid) {
                    var partlist = [];
                    //getBlockIndex(blockid);
                    var blockindex = getBlockIndex(blockid);
                    for (var i = 0; i < scope.pathlist.length; i++) {
                        var blayeron = layeron(i);
                        var bblcckon = blockon(i);
                        if (blayeron && bblcckon) {
                            //within = scope.pathlist[i].insidePolygon(x, y, scalefactor, offsetx, offsety, scope.x, scope.y, scope.h);
                            if (scope.pathlist[i].blockname == blockindex) {
                                if (scope.pathlist[i].type == 'path') {
                                    scope.pathlist[i].toggleselect();
                                }
                                if (scope.pathlist[i].selected) {
                                    partlist = getAttributes(blockindex);
                                }
                                else {
                                    partlist = [];
                                }
                            }
                            else {
                                if (scope.pathlist[i].type == "path") {
                                    scope.pathlist[i].unselect();
                                }
                            }
                        }
                    }
                    if (RxCore_GUI_2DBlockID != undefined) {
                        RxCore_GUI_2DBlockID.set2DBlockID([scope.blocklist[blockindex].index]);
                    }
                    if (RxCore_GUI_2DBlockInfo != undefined) {
                        RxCore_GUI_2DBlockInfo.set2DBlockInfo(partlist);
                    }
                };
                this.getblockcentroid = function (blockid, scalefactor, offsetx, offsety) {
                    //this.getcentroid
                    var blockindex = getBlockIndex(blockid);
                    var centroid = {};
                    for (var i = 0; i < scope.pathlist.length; i++) {
                        var blayeron = layeron(i);
                        var bblcckon = blockon(i);
                        if (blayeron && bblcckon) {
                            if (scope.pathlist[i].blockname == blockindex) {
                                centroid = scope.pathlist[i].getcentroid(scalefactor, offsetx, offsety, scope.x, scope.y, scope.h);
                                break;
                            }
                        }
                    }
                    return centroid;
                };
                this.getBlockRect = function (blockid, scalefactor, offsetx, offsety) {
                    var blockindex = getBlockIndex(blockid);
                    //var rect = {x:0,y:0,w:0,h:0};
                    var rect = {
                        found: false,
                        x: 0,
                        y: 0,
                        w: 0,
                        h: 0,
                        wp: 0,
                        hp: 0
                    };
                    for (var i = 0; i < scope.pathlist.length; i++) {
                        var blayeron = layeron(i);
                        var bblcckon = blockon(i);
                        if (blayeron && bblcckon) {
                            if (scope.pathlist[i].blockname == blockindex) {
                                rect = scope.pathlist[i].getRectangle(scalefactor, offsetx, offsety, scope.x, scope.y, scope.h);
                                break;
                            }
                        }
                    }
                    return rect;
                };
                this.getBlockInsertRect = function (blockid, scalefactor, offsetx, offsety) {
                    var blockindex = getBlockIndex(blockid);
                    //var rect = {x:0,y:0,w:0,h:0};

                    var bblcckon = false;

                    var rect = {
                        found: false,
                        x: 0,
                        y: 0,
                        w: 0,
                        h: 0,
                        wp: 0,
                        hp: 0
                    };

                    for (var bcount = 0; bcount < scope.blocklist.length; bcount++) {
                        if (scope.blocklist[bcount].index == blockid) {

                            bblcckon = (scope.blocklist[bcount].state == 1);

                            if(scope.blocklist[bcount]){
                                rect = scope.blocklist[bcount].getRectangle(scalefactor, offsetx, offsety, scope.x, scope.y, scope.h);
                            }

                            

                            //blockindex = bcount;
                            break;
                        }
                    }

                    /*for (var i = 0; i < scope.pathlist.length; i++) {
                        //var blayeron = layeron(i);
                        //var bblcckon = blockon(i);
                        if (blayeron && bblcckon) {
                        }
                    }*/

                    return rect;
                };


                this.setcompareColors = function(){

                    for (var i = 0; i < scope.pathlist.length; i++) {
                        var blayeron = layeron(i);
                        var bblcckon = blockon(i);

                        if (blayeron && bblcckon) {
                            if (scope.pathlist[i].type == "image") {

                                if( scope.comparemode == 0 ){
                                    if(scope.pathlist[i].role == 1){

                                        if(scope.pathlist[i].image != undefined){
                                            scope.pathlist[i].invert(scope.backgroundColor);
                                        }

                                        
                                    }
                                    if(scope.pathlist[i].role == 2){

                                        if(scope.pathlist[i].image != undefined){
                                            scope.pathlist[i].invert(scope.overlayColor);
                                        }

                                        
                                    }
    
                                }else if(scope.comparemode == 1){
                                    if(scope.pathlist[i].role == 2){

                                        if(scope.pathlist[i].image != undefined){
                                            //scope.pathlist[i].invert(scope.overlayColor);
                                            scope.pathlist[i].invert();
                                        }

                                        
                                    }

                                }
                                //rect = scope.pathlist[i].getRectangle(scalefactor, offsetx, offsety, scope.x, scope.y, scope.h);
                            }
                        }
                    }
                };

                this.setimageColor = function(color){
                    for (var i = 0; i < scope.pathlist.length; i++) {
                        var blayeron = layeron(i);
                        var bblcckon = blockon(i);
                        if (blayeron && bblcckon) {
                            if (scope.pathlist[i].type == "image") {

                                scope.pathlist[i].invert(color);

                                
                                //rect = scope.pathlist[i].getRectangle(scalefactor, offsetx, offsety, scope.x, scope.y, scope.h);

                                
                            }
                        }
                    }
                    
                    

                };

                this.getpathbyindex = function(index){
                    var path = null;
                    
                    var blayeron = layeron(index);
                    var bblcckon = blockon(index);
                    if (blayeron && bblcckon) {
                        path = scope.pathlist[index].getblockPath();
                    }

                    return path;
                };

                this.getpathbybId = function (blockid) {
                    var path = null;
                    var blockindex = getBlockIndex(blockid);
                    for (var i = 0; i < scope.pathlist.length; i++) {
                        var blayeron = layeron(i);
                        var bblcckon = blockon(i);
                        if (blayeron && bblcckon) {
                            if (scope.pathlist[i].blockname == blockindex) {
                                //rect = scope.pathlist[i].getRectangle(scalefactor, offsetx, offsety,scope.x, scope.y, scope.h);
                                path = scope.pathlist[i].getblockPath();
                                break;
                            }
                        }
                    }
                    return path;
                };
                this.getpathPointsBybId = function (blockid) {
                    var pointlist = [];
                    var blockindex = getBlockIndex(blockid);
                    for (var i = 0; i < scope.pathlist.length; i++) {
                        var blayeron = layeron(i);
                        var bblcckon = blockon(i);
                        if (blayeron && bblcckon) {
                            if (scope.pathlist[i].blockname == blockindex) {
                                //rect = scope.pathlist[i].getRectangle(scalefactor, offsetx, offsety,scope.x, scope.y, scope.h);
                                pointlist = scope.pathlist[i].getblockPathPoints();
                                break;
                            }
                        }
                    }
                    return pointlist;
                    /*var points = [];
                    if (thispage.usevectorxml){
                        points =  thispage.VectorPageObj.getpathPoints(x, y, thispage.dscalevector, thispage.dxvector, thispage.dyvector);
                    }
                    
                    return points;*/
                };
                this.getpath = function (x, y, scalefactor, offsetx, offsety) {
                    var rotatedpoint = { x: x, y: y };
                    if (scope.parent.DocRef.pages[scope.parent.DocRef.currentpage].getpageRotation() != 0) {
                        rotatedpoint = mouse_rotated(x, y);
                    }
                    x = rotatedpoint.x;
                    y = rotatedpoint.y;
                    for (var i = 0; i < scope.pathlist.length; i++) {
                        var blayeron = layeron(i);
                        var bblcckon = blockon(i);
                        if (blayeron && bblcckon) {
                            if (scope.pathlist[i].getpath) {
                                if (typeof scope.pathlist[i].getpath === "function") {
                                    var polyPath = scope.pathlist[i].getpath(x, y, scalefactor, offsetx, offsety, scope.x, scope.y, scope.h);
                                    if (polyPath != null) {
                                        return polyPath;
                                    }
                                }
                            }
                            /*if(within){
                                //scope.pathlist[i].toggleselect();
                                foundblock = scope.pathlist[i].blockname;
                                partindex = i;
        
        
                            }else{
                                //foundblock = undefined;
                            }*/
                        }
                    }
                    return null;
                };
                this.getpathPoints = function (x, y, scalefactor, offsetx, offsety) {
                    var rotatedpoint = { x: x, y: y };
                    var pointlist = [];
                    if (scope.parent.DocRef.pages[scope.parent.DocRef.currentpage].getpageRotation() != 0) {
                        rotatedpoint = mouse_rotated(x, y);
                    }
                    x = rotatedpoint.x;
                    y = rotatedpoint.y;
                    for (var i = 0; i < scope.pathlist.length; i++) {
                        var blayeron = layeron(i);
                        var bblcckon = blockon(i);
                        if (blayeron && bblcckon) {
                            if (scope.pathlist[i].getpathPoints) {
                                if (typeof scope.pathlist[i].getpathPoints === "function") {
                                    var polypoints = scope.pathlist[i].getpathPoints(x, y, scalefactor, offsetx, offsety, scope.x, scope.y, scope.h);
                                    if (polypoints.length > 0) {
                                        return polypoints;
                                    }
                                }
                            }
                            /*if(within){
                                //scope.pathlist[i].toggleselect();
                                foundblock = scope.pathlist[i].blockname;
                                partindex = i;
        
        
                            }else{
                                //foundblock = undefined;
                            }*/
                        }
                    }
                    return pointlist;
                };

                this.setBlockArea = function(partindex, foundblock, szAttname){
                    
                    //foundblock = scope.pathlist[i].blockname;
                    //partindex = i;

                    var dimarea = scope.pathlist[partindex].getArea();


                    //this.parent = page;
                    //var AreaUnitlabel = DocObj.getCurrentpage().AreaUnitlabel;

                    var AreaUnitlabel = scope.parent.scaleObject.AreaUnitlabel;
                    var dimPrecision = scope.parent.scaleObject.ndimprecision;
                    


                    var dimtext = getUnitArea(dimarea);
                    //dimtext = dimtext.toFixed(ndimprecision);
                    dimtext = dimtext.toFixed(dimPrecision);
                    dimtext = dimtext + " " + AreaUnitlabel;


                    if (scope.hasAttribute(foundblock,szAttname)){
                        scope.updateAttribute(foundblock, szAttname, dimtext);
                    }else{
                        scope.appendCustomBlockAttribute(foundblock, szAttname, dimtext);
                    }
                }; 

                this.createAreaAttributes = function(szAttname){
                    
                    for (var i = 0; i < scope.pathlist.length; i++) {

                        if(scope.pathlist[i].hasArea){
                            scope.setBlockArea(i, scope.pathlist[i].blockname, szAttname);    
                        }

                    }

                };



                function getvectorType(type){

                    var sztype = "no type";

                    switch(type){
                        case 0 : sztype = "LINE";
                        break;
                        case 1 : sztype = "LWPOLYLINE";
                        break;
                        case 2 : sztype = "POLYLINE";
                        break;
                        case 3 : sztype = "SOLID";
                        break;
                        case 4 : sztype = "ARC";
                        break;
                        case 5 : sztype = "CIRCLE";
                        break;
                        case 6 : sztype = "TEXT";
                        break;
                        case 7 : sztype = "DIMENSION";
                        break;
                        case 8 : sztype = "MTEXT";
                        break;
                        case 9 : sztype = "VERTEX";
                        break;
                        case 10 : sztype = "ELLIPSE";
                        break;
                        case 11 : sztype = "POINT";
                        break;
                        case 12 : sztype = "3DFACE";
                        break;
                        case 13 : sztype = "3DSOLID";
                        break;
                        case 14 : sztype = "BODY";
                        break;
                        case 15 : sztype = "ATTDEF";
                        break;
                        case 16 : sztype = "ATTRIB";
                        break;
                        case 17 : sztype = "HATCH";
                        break;
                        case 18 : sztype = "IMAGE";
                        break;
                        case 19 : sztype = "INSERT";
                        break;
                        case 20 : sztype = "LEADER";
                        break;
                        case 21 : sztype = "MLINE";
                        break;
                        case 22 : sztype = "OLEFRAME";
                        break;
                        case 23 : sztype = "OLE2FRAME";
                        break;
                        case 24 : sztype = "RAY";
                        break;
                        case 25 : sztype = "REGION";
                        break;
                        case 26 : sztype = "SHAPE";
                        break;
                        case 27 : sztype = "SPLINE";
                        break;
                        case 28 : sztype = "TOLERANCE";
                        break;
                        case 29 : sztype = "TRACE";
                        break;
                        case 30 : sztype = "ACAD_PROXY_ENTITY";
                        break;
                        case 31 : sztype = "VIEWPORT";
                        break;
                        case 32 : sztype = "WIPEOUT";
                        break;
                        case 33 : sztype = "XLINE";
                        break;
                        default : sztype = "other type";
              
                      }
              
                             
                      /*15 ATTDEF
                      16 ATTRIB
                      17 HATCH
                      18 IMAGE
                      19 INSERT
                      20 LEADER
                      21 MLINE
                      22 OLEFRAME
                      23 OLE2FRAME
                      24 RAY
                      25 REGION
                      26 SHAPE
                      27 SPLINE
                      28 TOLERANCE
                      29 TRACE
                      30 ACAD_PROXY_ENTITY
                      31 VIEWPORT
                      32 WIPEOUT
                      33 XLINE*/
              
                      return sztype;

                }

                function findsmallestblockinsert(foundblockinserts){

                    var area = foundblockinserts[0].getArea();
                    var curArea = foundblockinserts[0].getArea();
                    var pathindex = 0;

                    for (var i = 0; i < foundblockinserts.length; i++) {

                        curArea = foundblockinserts[i].getArea();
                        
                        if(curArea < area){
                            pathindex = i;
                            area = curArea; 
                        }

                    }



                    return foundblockinserts[pathindex];


                }

                function findsmallest(foundvectors){

                    var area = foundvectors[0].getArea();
                    var curArea = foundvectors[0].getArea();
                    var pathindex = 0;

                    for (var i = 0; i < foundvectors.length; i++) {

                        curArea = foundvectors[i].getArea();
                        
                        if(curArea < area){
                            pathindex = i;
                            area = curArea; 
                        }

                    }



                    return {path : foundvectors[pathindex], area : area};

                }



                this.getvectorinformation = function (x, y, scalefactor, offsetx, offsety, hover){

                    var within = false;
                    var screenmouse  = { x: x, y: y };
                    var rotatedpoint = { x: x, y: y };
                    
                    var partlistAll = [];
                    var selected = [];
                    //var foundblocks = [];
                    var pathindex = {index : null};
                    var vectorobj = undefined;
                    var foundblock = undefined;
                    var foundlayer = undefined;
                    var entity = undefined;
                    
                    var foundArea = 0;
                    var curfoundArea = 0;

                    partlist = [];

                    var partindex = undefined;

                    if (scope.parent.DocRef.pages[scope.parent.DocRef.currentpage].getpageRotation() != 0) {
                        rotatedpoint = mouse_rotated(x, y);
                    }
                    x = rotatedpoint.x;
                    y = rotatedpoint.y;

                    for (var i = 0; i < scope.pathlist.length; i++) {
                        var blayeron = layeron(i);
                        var bblcckon = blockon(i);
                        if (blayeron && bblcckon) {

                            scope.pathlist[i].mouseover = false;


                            within = scope.pathlist[i].iswithin(x, y, scalefactor, offsetx, offsety, scope.x, scope.y, scope.h, hover);

                            if (within) {

                                partlist.push(scope.pathlist[i]);

                                
                            }
                            /*else{
                                
                                partindex = undefined;
                                foundblock = undefined;
                                entity = undefined;

                            }*/
                        }
                    }

                    if(partlist.length > 0){

                        var smallestobj = findsmallest(partlist);
                        vectorobj = smallestobj.path;
                        var area = smallestobj.area;


                        if(vectorobj){
                            //drawmarkupAll(cntximg);
                            vectorobj.mouseover = true;
                            
                            drawmarkupAll(cntximg);
                            //vectorobj.highlight(cntximg, scalefactor, offsetx, offsety, scope.x, scope.y, scope.h);
                        }

                        partindex = vectorobj.index; //this.index
                        foundblock = getBlockObject(vectorobj.blockname);
                        //getBlockName(vectorobj.blockname);

                        /*function getBlockName(blockid) {
                            var blockname = undefined;
        
                            for (var bcount = 0; bcount < scope.blocklist.length; bcount++) {
                                if (scope.blocklist[bcount].index == blockid) {
                                    
                                    blockname = scope.blocklist[bcount].name;
                                    break;
                                }
                            }
                            return blockname;
                        }*/
        
                        if(scope.layerlist[vectorobj.layer] != undefined){
                            foundlayer = scope.layerlist[vectorobj.layer].name;
                        }


                        if(vectorobj.entityType.type == 5){

                            dimensions = vectorobj.getlength();
                            length = Math.abs(dimensions.length) / scope.parent.OriginalScale;
                            radius = Math.abs(dimensions.radius) / scope.parent.OriginalScale;
                            area = Math.abs(dimensions.Area) / (scope.parent.OriginalScale * scope.parent.OriginalScale);
                            //return {radius : arcHandle.r, length : sweep * arcHandle.r};
                            entity = {type : vectorobj.entityType.type, handle : vectorobj.entityType.handleLow, typename : getvectorType(vectorobj.entityType.type), length : length, radius : radius, area : area};

                        }else if (vectorobj.entityType.type == 4){

                        
                            //return {sweep: RadiansTodegrees(sweep), radius : arcHandle.r, length : sweep * arcHandle.r};
                            var dimensions = vectorobj.getlength();
                            length = Math.abs(dimensions.length) / scope.parent.OriginalScale;
                            var sweep = Math.abs(dimensions.sweep);
                            var radius = Math.abs(dimensions.radius) / scope.parent.OriginalScale;


                            entity = {type : vectorobj.entityType.type, handle : vectorobj.entityType.handleLow, typename : getvectorType(vectorobj.entityType.type), length : length, sweep : sweep, radius : radius};
                        }else if(vectorobj.entityType.type == 0){
                        
                        
                            var startpoint = {x : vectorobj.x1, y :vectorobj.y1}; 
                            var endpoint = {x : vectorobj.x2, y :vectorobj.y2}; 


                            var xdiff = Math.max(startpoint.x, endpoint.x) - Math.min(startpoint.x, endpoint.x);
                            var ydiff = Math.max(startpoint.y, endpoint.y) - Math.min(startpoint.y, endpoint.y);
                    
                            var length = getDiagonal(xdiff, ydiff) / scope.parent.OriginalScale;
                                                
                            //getDiagonal()

                            entity = {type : vectorobj.entityType.type, handle : vectorobj.entityType.handleLow, typename : getvectorType(vectorobj.entityType.type), startp : startpoint, endp : endpoint, length : length};
                        }else if (vectorobj.entityType.type == 1){

                           length = vectorobj.getLength() / scope.parent.OriginalScale;

                           area = area / (scope.parent.OriginalScale * scope.parent.OriginalScale);
                           entity = {type : vectorobj.entityType.type, handle : vectorobj.entityType.handleLow, typename : getvectorType(vectorobj.entityType.type), area : area, length : length};

                            
                        }else{
                            entity = {type : vectorobj.entityType.type, handle : vectorobj.entityType.handleLow, typename : getvectorType(vectorobj.entityType.type)};
                        }
                        



                        //partlistAll = {Index : partindex, Attributes : getAttributes(foundblock.name), Block : foundblock, Layername : foundlayer, Entity : entity} 
                        partlistAll = {Index : partindex, Block : foundblock, Layername : foundlayer, Entity : entity} 

                        /*if(entity){
                            partlistAll.push(entity);
                        }*/
                        pathindex.index = partindex;

                    }else{
                        drawmarkupAll(cntximg);
                    }

                    
                    //pathindex = {pathindex : partindex};
                    
                    



                    if (hover){

                        //drawmarkupAll(cntximg);
                        if (RxCore_GUI_2DEntityInfoScreen != undefined) {
                            //RxCore_GUI_2DBlockInfoScreen.set2DBlockInfo(partlist, screenmouse, pathindex);
                            RxCore_GUI_2DEntityInfoScreen.set2DBlockInfo(partlistAll, screenmouse, pathindex);
                        }

                    }else{

                        if (RxCore_GUI_2DEntityInfo != undefined) {
                            RxCore_GUI_2DEntityInfo.set2DBlockInfo(partlistAll, screenmouse, pathindex);
                        }

                        

                    }



                };

                this.getinsidePolygon = function (x, y, scalefactor, offsetx, offsety, multi, hover) {
                    scope.multiselect = multi;
                    var within = false;
                    var screenmouse  = { x: x, y: y };
                    var rotatedpoint = { x: x, y: y };
                    var partlist = [];
                    var partlistAll = [];
                    var selected = [];
                    var foundblocks = [];
                    var pathindex = {index : null};
                    var foundblock = undefined;
                    
                    var foundArea = 0;
                    var curfoundArea = 0;

                    var partindex = undefined;

                    if (scope.parent.DocRef.pages[scope.parent.DocRef.currentpage].getpageRotation() != 0) {
                        rotatedpoint = mouse_rotated(x, y);
                    }
                    x = rotatedpoint.x;
                    y = rotatedpoint.y;
                    for (var i = 0; i < scope.pathlist.length; i++) {
                        var blayeron = layeron(i);
                        var bblcckon = blockon(i);
                        if (blayeron && bblcckon) {
                            within = scope.pathlist[i].insidePolygon(x, y, scalefactor, offsetx, offsety, scope.x, scope.y, scope.h, hover);
                            if (within) {

                                
                                foundArea = curfoundArea;
                                curfoundArea = scope.pathlist[i].getArea();

                                if(foundblock){

                                    if(curfoundArea < foundArea){
                                        foundblock = scope.pathlist[i].blockname;
                                        partindex = i;

                                        foundblocks.unshift({foundblock : foundblock, partindex : partindex, area : curfoundArea});
                                    }else{
                                        foundblock = scope.pathlist[i].blockname;
                                        partindex = i;

                                        foundblocks.push({foundblock : foundblock, partindex : partindex, area : curfoundArea});    
                                    }
                                    

                                }else{
                                    foundblock = scope.pathlist[i].blockname;
                                    partindex = i;

                                    foundblocks.push({foundblock : foundblock, partindex : partindex, area : curfoundArea});
                                }

                                //scope.pathlist[i].toggleselect();
                                //foundblock = scope.pathlist[i].blockname;
                                //partindex = i;
                            }
                            else {
                                //foundblock = undefined;

                                if (multi) {
                                    if (scope.pathlist[i].selected) {
                                        var blockindex = scope.pathlist[i].blockname;
                                        selected.push(scope.blocklist[blockindex].index);
                                    }
                                }
                            }
                        }
                    }
                    if (foundblock) {

                        if(!hover){
                            if (!scope.pathlist[partindex].selected) {
                                scope.selectBlockPath(true, scope.pathlist[partindex].blockname);

                                
                            }
                            else {
                                scope.selectBlockPath(false, scope.pathlist[partindex].blockname);

                            }

                            if (scope.pathlist[partindex].selected) {
                                selected.push(scope.blocklist[foundblock].index);
                                if (!multi) {
                                    partlist = getAttributes(foundblock);
                                    pathindex.index = partindex;
                                    partlist.push(pathindex);

                                }
                            }
                            else {
                                if (!multi) {
                                    partlist = [];
                                }
                            }
    
                        }else{

                            if (!scope.pathlist[partindex].selected) {
                                
                                if (foundblocks.length > 1){
                                    
                                    pathindex.index = foundblocks[0].partindex;

                                    for (var fbi = 0; fbi < foundblocks.length; fbi ++){

                                        var attributes = getAttributes(foundblocks[fbi].foundblock);
                                        attributes.push({index : foundblocks[fbi].partindex});

                                        partlistAll.push(attributes);
    
                                    }
    
                                }else{
                                    partlistAll = getAttributes(foundblock);
                                    //pathindex = {pathindex : partindex};
                                    pathindex.index = partindex;
                                    partlistAll.push(pathindex);
    
                                }

                                //partlist = getAttributes(foundblock);
                                //pathindex = {pathindex : partindex};
                                //pathindex.index = partindex;
                                //partlist.push(pathindex);
                                

                            }
                        }
                    }

                    if(!hover){
                        if (RxCore_GUI_2DBlockID != undefined) {
                            RxCore_GUI_2DBlockID.set2DBlockID(selected);
                        }
                    }
                    if (!multi) {
                        
                        if(!hover){
                            if (RxCore_GUI_2DBlockInfo != undefined) {
                                RxCore_GUI_2DBlockInfo.set2DBlockInfo(partlist);
                            }
                        }

                        if (hover){
                            if (RxCore_GUI_2DBlockInfoScreen != undefined) {
                                //RxCore_GUI_2DBlockInfoScreen.set2DBlockInfo(partlist, screenmouse, pathindex);
                                RxCore_GUI_2DBlockInfoScreen.set2DBlockInfo(partlistAll, screenmouse, pathindex);
                            }
    
                        }

                    }
                    return within;
                };

                this.markblockinsert = function(blockid, scalefactor, offsetx, offsety){

                    //var within = false;
                    //var screenmouse  = { x: x, y: y };
                    //var rotatedpoint = { x: x, y: y };
                    //var partlist = [];
                    
                    var blockinsert = undefined;

                    var foundblock = undefined;
                    var foundlayer = undefined;
                    var entity = undefined;
                    
                    var foundArea = 0;
                    var curfoundArea = 0;

                    var fixedscale = getCurrentPageFixedScale();
                    var drawscale = scalefactor / fixedscale;
                    //var linewidth = nVectorhighlightwidthconstant;

                    nVectorhighlightwidth = nVectorhighlightwidthconstant * drawscale;

                    if(nVectorhighlightwidth > 4){
                        nVectorhighlightwidth = 4;
                    }

                    var partindex = undefined;


                    drawmarkupAll(cntximg);

                    foundblock = getBlockObject(blockid);
                    blockinsert = foundblock.insert;
                    
                    if(foundblock && blockinsert){

                        //this.mouseon = false;
                        //foundblock.highlight(cntximg, scalefactor, offsetx, offsety, scope.x, scope.y, scope.h);
                        foundblock.mouseon = true;
                        //foundblock.select();
                        drawmarkupAll(cntximg);
                    }else{
                        drawmarkupAll(cntximg);
                    }



                };

                this.rendermouseonVectors = function(scalefactor, offsetx, offsety, rotation){

                    var tx = (canvasowidth / 2);
                    var ty = (canvasoheight / 2);


                    var fixedscale = getCurrentPageFixedScale();
                    var drawscale = scalefactor / fixedscale;
                    //var linewidth = nVectorhighlightwidthconstant;

                    nVectorhighlightwidth = nVectorhighlightwidthconstant * drawscale;

                    if(nVectorhighlightwidth > 4){
                        nVectorhighlightwidth = 4;
                    }

                    cntximg.save();
                    cntximg.translate(tx, ty);
                    cntximg.rotate(rotation * (Math.PI / 180));
                    cntximg.translate(-tx, -ty);
                    //this.VectorPageObj.drawall(contexto, this.dscalevector, this.dxvector, this.dyvector, refresh,false);
                    


                    for (var i = 0; i < partlist.length; i++) {

                        
                        if(partlist[i].mouseover){
                            partlist[i].highlight(cntximg, scalefactor, offsetx, offsety, scope.x, scope.y, scope.h);
                        }

                        /*var blayeron = layeron(i);
                        var bblcckon = blockon(i);
                        if (blayeron && bblcckon) {

                        }*/
                    }

                    cntximg.restore();

                }; 

                this.renderSelectedBlocks = function(scalefactor, offsetx, offsety, rotation){

                    var tx = (canvasowidth / 2);
                    var ty = (canvasoheight / 2);

                    var fixedscale = getCurrentPageFixedScale();
                    var drawscale = scalefactor / fixedscale;
                    //var linewidth = nVectorhighlightwidthconstant;

                    nVectorhighlightwidth = nVectorhighlightwidthconstant * drawscale;

                    if(nVectorhighlightwidth > 4){
                        nVectorhighlightwidth = 4;
                    }

                    cntximg.save();
                    cntximg.translate(tx, ty);
                    cntximg.rotate(rotation * (Math.PI / 180));
                    cntximg.translate(-tx, -ty);
                    //this.VectorPageObj.drawall(contexto, this.dscalevector, this.dxvector, this.dyvector, refresh,false);
                    
                    


                    for (var i = 0; i < scope.blocklist.length; i++) {

                        var bblcckon = (scope.blocklist[i].state == 1);
                        if(bblcckon && scope.blocklist[i].insert != undefined){

                            //foundblock.mouseon
                            if(scope.blocklist[i].selected || scope.blocklist[i].mouseon){
                                scope.blocklist[i].highlight(cntximg, scalefactor, offsetx, offsety, scope.x, scope.y, scope.h);
                            }
                            
                            //within = scope.blocklist[i].iswithin(x, y, scalefactor, offsetx, offsety, scope.x, scope.y, scope.h);

    
                        }

                    }
                    cntximg.restore();

                };

                this.getinsideblockinsert = function(x, y, scalefactor, offsetx, offsety, rotation, hover){

                    var within = false;
                    var screenmouse  = { x: x, y: y };
                    var rotatedpoint = { x: x, y: y };
                    //var partlist = [];
                    var objectinsertlist = [];
                    var partlistAll = [];
                    var selected = [];
                    //var foundblocks = [];
                    var pathindex = {index : null};
                    
                    var blockinsert = undefined;

                    var foundblock = undefined;
                    var foundlayer = undefined;
                    var entity = undefined;
                    
                    var foundArea = 0;
                    var curfoundArea = 0;

                    var fixedscale = getCurrentPageFixedScale();
                    var drawscale = scalefactor / fixedscale;
                    //var linewidth = nVectorhighlightwidthconstant;

                    nVectorhighlightwidth = nVectorhighlightwidthconstant * drawscale;

                    if(nVectorhighlightwidth > 4){
                        nVectorhighlightwidth = 4;
                    }

                    var partindex = undefined;

                    if (scope.parent.DocRef.pages[scope.parent.DocRef.currentpage].getpageRotation() != 0) {
                        rotatedpoint = mouse_rotated(x, y);
                    }

                    x = rotatedpoint.x;
                    y = rotatedpoint.y;

                    for (var i = 0; i < scope.blocklist.length; i++) {

                        scope.blocklist[i].mouseon = false;
                        var bblcckon = (scope.blocklist[i].state == 1);
                        if(bblcckon && scope.blocklist[i].insert != undefined){

                            within = scope.blocklist[i].iswithin(x, y, scalefactor, offsetx, offsety, scope.x, scope.y, scope.h);

                            if (within) {
    
                                objectinsertlist.push(scope.blocklist[i]);
    
                                
                            }
    
                        }

                    }


                    if(objectinsertlist.length > 0){


                        foundblock = findsmallestblockinsert(objectinsertlist);
                        

                        if(foundblock && hover && !foundblock.selected){
                            
                            foundblock.mouseon = true;
                            drawmarkupAll(cntximg);
                            //foundblock.highlight(cntximg, scalefactor, offsetx, offsety, scope.x, scope.y, scope.h);
                        }

                        //partindex = vectorobj.index; //this.index
                        //foundblock = getBlockName(vectorobj.blockname);
                        //foundlayer = scope.layerlist[vectorobj.layer].name;
                        
                        
                        //entity = {type : vectorobj.entityType.type, handle : vectorobj.entityType.handleLow, typename : getvectorType(vectorobj.entityType.type)};


                        //partlistAll = {Index : partindex, Attributes : getAttributes(foundblock), Blockname : foundblock, Layername : foundlayer, Entity : entity} 

                        /*if(entity){
                            partlistAll.push(entity);
                        }*/
                        //pathindex.index = partindex;

                    }else{
                        drawmarkupAll(cntximg);
                    }

                    
                    //pathindex = {pathindex : partindex};
                    
                    



                    if (hover){

                        //drawmarkupAll(cntximg);
                        if (RxCore_GUI_2DBlockHover != undefined) {
                            //RxCore_GUI_2DBlockInfoScreen.set2DBlockInfo(partlist, screenmouse, pathindex);
                            if(foundblock){
                                RxCore_GUI_2DBlockHover.set2DBlockID(foundblock, screenmouse);
                            }
                            RxCore_GUI_2DBlockHover.onBlockHover(foundblock, screenmouse);
                            
                        }

                    }else{

                        if (RxCore_GUI_2DBlock != undefined) {
                            if(foundblock){
                                foundblock.select();
                                RxCore_GUI_2DBlock.set2DBlockID(foundblock);
                            }
                        }
    
                    }



                };

                this.resetLayers = function () {
                    for (var i = 0; i < this.layerlist.length; i++) {
                        if (this.layerlist[i] == undefined) {
                            continue;
                        }
                        this.layerlist[i].state = this.layerlist[i].defaultstate;
                    }
                    if (RxCore_GUI_VectorLayers != undefined) {
                        RxCore_GUI_VectorLayers.setVectorLayers(this.layerlist);
                    }
                };
                this.filterAttributes = function (szAttributeLoadMask) {
                    scope.filterAttributelist = scope.attributes.filter(function (attribute) {
                        return matchRuleShort(attribute.name, szAttributeLoadMask);
                    });
                };
                this.filterBlocks = function (szBlockLoadMask) {


                    /*scope.filterblocklist = scope.blocklist.filter(function (block) {
                        
                        return block.listed;

                    });*/


                    scope.filterblocklist = scope.blocklist.filter(function (block) {
                        
                        return matchRuleShort(block.name, szBlockLoadMask) && block.listed;

                    });
                    if (RxCore_GUI_VectorBlocks != undefined) {
                        RxCore_GUI_VectorBlocks.setVectorBlocks(scope.filterblocklist);
                    }
                    //matchRuleShort("bird123", "bird*")
                };
                this.turnBlockAllOnOff = function (OnOff) {
                    for (var i = 0; i < scope.filterblocklist.length; i++) {
                        if (scope.filterblocklist[i] == undefined) {
                            continue;
                        }
                        scope.filterblocklist[i].state = OnOff ? 1 : 0;
                    }
                    /*for (i = 0; i < scope.blocklist.length; i++) {
                        if (scope.blocklist[i] == undefined) {
                            continue;
                        }
                        scope.blocklist[i].state = OnOff ? 1 : 0;
                    }*/
                    if (RxCore_GUI_VectorBlocks != undefined) {
                        RxCore_GUI_VectorBlocks.setVectorBlocks(scope.filterblocklist);
                    }
                };
                this.turnLayerAllOnOff = function (OnOff) {
                    for (var i = 0; i < this.layerlist.length; i++) {
                        if (this.layerlist[i] == undefined) {
                            continue;
                        }
                        this.layerlist[i].state = OnOff ? 1 : 0;
                    }
                    if (RxCore_GUI_VectorLayers != undefined) {
                        RxCore_GUI_VectorLayers.setVectorLayers(scope.layerlist);
                    }
                };
                this.setActive = function () {
                    if (RxCore_GUI_VectorLayers != undefined) {
                        RxCore_GUI_VectorLayers.setVectorLayers(scope.layerlist);
                    }
                    if (RxCore_GUI_VectorBlocks != undefined) {
                        RxCore_GUI_VectorBlocks.setVectorBlocks(scope.filterblocklist);
                    }
                };

                this.getlayerlist = function(){
                    return scope.layerlist;
                };

                this.turnLayerOnOff = function (layerindex) {


                    for (var li = 0;li < this.layerlist.length; li ++){

                        if(this.layerlist[li].index == layerindex){
                            if (this.layerlist[li].state == 1) {
                                this.layerlist[li].state = 0;
                            }
                            else {
                                this.layerlist[li].state = 1;
                            }
        
                        }


                    }

                    /*for (var i=0; i<this.pathlist.length;i++){
                     if(layerindex == this.pathlist[i].layer ){
                     this.pathlist[i].layerstate = this.layerlist[layerindex].state;
                     }
        
                     }*/
                    /*for (i=0;i<this.layerlist.length-1;i++){
                     if (this.layerlist[i].index == layerindex){
                     if (this.layerlist[i].state == 1){
                     this.layerlist[i].state = 0;
                     return;
                     }else{
                     this.layerlist[i].state = 1;
                     return;
                     }
                     }
                     }*/
                };
                this.resetBlocks = function () {
                    for (var i = 0; i < scope.filterblocklist.length; i++) {
                        if (scope.filterblocklist[i] == undefined) {
                            continue;
                        }
                        scope.filterblocklist[i].state = scope.filterblocklist[i].defaultstate;
                    }
                    for (i = 0; i < scope.blocklist.length; i++) {
                        if (scope.blocklist[i] == undefined) {
                            continue;
                        }
                        scope.blocklist[i].state = scope.blocklist[i].defaultstate;
                    }
                    if (RxCore_GUI_VectorBlocks != undefined) {
                        RxCore_GUI_VectorBlocks.setVectorBlocks(scope.filterblocklist);
                    }
                };
                this.turnBlockOnOff = function (index) {
                    for (var i = 0; i < scope.filterblocklist.length; i++) {
                        if (index == scope.filterblocklist[i].index) {
                            var bstate = (scope.filterblocklist[i].state == 1) ? 0 : 1;
                            scope.filterblocklist[i].state = bstate;
                        }
                    }
                    for (i = 0; i < scope.blocklist.length; i++) {
                        if (index == scope.blocklist[i].index) {
                            bstate = (scope.blocklist[i].state == 1) ? 0 : 1;
                            scope.blocklist[i].state = bstate;
                            //this.blocklist[i].state = (this.blocklist[i].state == 1) ? 0 : 1;
                            //(this.blocklist[i].state == 0) ? this.blocklist[i].state = 1 : this.blocklist[i].state = 0;
                            //(this.blocklist[i].state == 1) ? this.blocklist[i].state = 0 : this.blocklist[i].state = 1;
                            //this.blocklist[i].state = 1;
                            /*for (var j=0; j<this.pathlist.length;j++){
                             if(blockName == this.pathlist[j].blockname ){
                             this.pathlist[j].blockstate = 1;
                             }
        
                             }*/
                        } /*else if (index == this.blocklist[i].index && this.blocklist[i].state == 1) {
                 this.blocklist[i].state = 0;

                 }*/
                    }
                };
                function getrotpoint(width, height, x, y, anglerad) {
                    var cosangle = Math.cos(anglerad);
                    var sinangle = Math.sin(anglerad);
                    var hw = x - width;
                    var hh = y - height;
                    var newx = (hw * cosangle) - (hh * sinangle);
                    var newy = (hw * sinangle) + (hh * cosangle);
                    var transpoint = { x: newx, y: newy };
                    transpoint.x = width + transpoint.x;
                    transpoint.y = height + transpoint.y;
                    return transpoint;
                }
                function rotate_pointrad(point, originX, originY, radians) {
                    //var new_x_point = old_x_point * cos(Angle) - old_y_point * sin(Angle);
                    //var new_y_point = old_y_point * cos(Angle) + old_x_point * sin(Angle);
                    var transpx = point.x - originX;
                    var transpy = point.y - originY;
                    return {
                        x: (Math.cos(radians) * (transpx) - Math.sin(radians) * (transpy)) + originX,
                        y: (Math.sin(radians) * (transpx) + Math.cos(radians) * (transpy)) + originY
                    };
                }
                this.rotate_point = function (pointX, pointY, originX, originY, angle) {
                    angle = angle * Math.PI / 180.0;
                    return {
                        x: Math.cos(angle) * (pointX - originX) - Math.sin(angle) * (pointY - originY) + originX,
                        y: Math.sin(angle) * (pointX - originX) + Math.cos(angle) * (pointY - originY) + originY
                    };
                };
                this.gettextpos = function (marerobj, scalefactor, offsetx, offsety) {
                    //scope.x, scope.y, scope.h,layerstate,blockstate
                    var x1scaled = Math.min(((marerobj.x1 - scope.x) * scalefactor), ((marerobj.x2 - scope.x) * scalefactor));
                    var y1scaled = Math.min(((scope.h - marerobj.y1) * scalefactor), ((scope.h - marerobj.y2) * scalefactor));
                    var x2scaled = Math.max(((marerobj.x1 - scope.x) * scalefactor), ((marerobj.x2 - scope.x) * scalefactor));
                    var y2scaled = Math.max(((scope.h - marerobj.y1) * scalefactor), ((scope.h - marerobj.y2) * scalefactor));
                    //marerobj.rot
                    var centerx = x1scaled + ((x2scaled - x1scaled) / 2);
                    var centery = y1scaled + ((y2scaled - y1scaled) / 2);
                    var ulrotpoint = scope.rotate_point(x1scaled, y1scaled, centerx, centery, marerobj.rot);
                    var lrrotpoint = scope.rotate_point(x2scaled, y2scaled, centerx, centery, marerobj.rot);
                    x1scaled = Math.min(ulrotpoint.x, lrrotpoint.x);
                    y1scaled = Math.min(ulrotpoint.y, lrrotpoint.y);
                    x2scaled = Math.max(ulrotpoint.x, lrrotpoint.x);
                    y2scaled = Math.max(ulrotpoint.y, lrrotpoint.y);


                    const pxscale = getPixelRatio();
                    var fntsize = (y2scaled - y1scaled) / pxscale;
                    var py = y1scaled / pxscale;
                    var px = x1scaled / pxscale;
                    var pw = Math.abs(x2scaled - x1scaled) / pxscale;
                    var ph = Math.abs(y2scaled - y1scaled) / pxscale;


                    return {
                        top: py,
                        left: px,
                        width: pw,
                        height: ph,
                        fontsize: fntsize

                        /*top: y1scaled,
                        left: x1scaled,
                        width: Math.abs(x2scaled - x1scaled),
                        height: Math.abs(y2scaled - y1scaled),
                        fontsize: y2scaled - y1scaled*/
                    };
                    /*function textmarkerdiv(marerobj,scalefactor,offsetx,offsety,mediax,mediay,mediah){
        
                     }*/
                };
                this.TextLayerClear = function (texlayerdata) {
                    if (texlayerdata.textLayerDiv != undefined) {
                        while (texlayerdata.textLayerDiv.firstChild) {
                            texlayerdata.textLayerDiv.removeChild(texlayerdata.textLayerDiv.firstChild);
                        }
                    }
                    scope.textlayerset = false;
                };

                function getTextWidth(ctx, text, font) {
                    // re-use canvas object for better performance
                    //const canvas = getTextWidth.canvas || (getTextWidth.canvas = document.createElement("canvas"));
                    //const context = canvas.getContext("2d");
                    ctx.save();
                    ctx.font = font;
                    const metrics = ctx.measureText(text);
                    ctx.restore();


                    return metrics.width;
                }

                function getTexareadim(x,y,w,h){

                    const scale = getPixelRatio();

                    var textleft = (x / scale);
                    var texttop = (y / scale);
                    
                    var textwidth = (w / scale);
                    var textheight = (h / scale);

                    return {x:textleft, y:texttop, w:textwidth, h:textheight};


                }                


                this.TextLayerBuilder = function (texlayerdata) {
                    if (scope.textlayerset) {
                        return;
                    }
                    //var div = texlayerdata.textLayerDiv;
                    //const pxscale = getPixelRatio();

                    for (var i = 0; i < scope.textlist.length; i++) {
                        var textdivpos = scope.gettextpos(scope.textlist[i], texlayerdata.scale, texlayerdata.dx, texlayerdata.dy);
                        var textdiv = document.createElement('div');
                        
                        
            
                        var font = textdivpos.fontsize + "px " + "sans-serif";

                        
                        var txtwidth = getTextWidth(context, scope.textlist[i].text, font  );
                        var textdiff = textdivpos.width - txtwidth;
                        var spacelength = scope.textlist[i].text.length - 1;
                        var pixelspace = textdiff / spacelength;

                        //var roundscale = Math.round(textscale * 10) / 10;
                        if (pixelspace < 1){
                            pixelspace = 1;
                        }else{
                            //pixelspace = Math.floor(pixelspace);
                        }

                        //pixelspace = pixelspace / pxscale;

                        var letterspace = pixelspace + "px";

                        //console.log(txtwidth / textdivpos.width);
                        //textdiv.setAttribute("txtscale", textscale);

                        var localx = textdivpos.left;
                        var localy = textdivpos.top;

                        //var localx = textdivpos.left / pxscale;
                        //var localy = textdivpos.top / pxscale;


                        if (scope.textlist[i].rot != 0) {
                            //var radangle = scope.textlist[i].rot * (Math.PI/180);
                            //localx -= (textdivpos.height/2);
                            //localx = (textdivpos.left*Math.cos(radangle))-(textdivpos.top*Math.sin(radangle));
                            //localy = (textdivpos.left*Math.sin(radangle))+(textdivpos.top*Math.cos(radangle));
                        }
                        //<div data-canvas-width="132.41322" style="left: 191.37px; top: 228.609px; font-size: 14.94px; font-family: sans-serif; transform: scaleX(1.04262);">
                        textdiv.style.left = localx + "px";
                        textdiv.style.top = localy + "px";
                        textdiv.style.width = textdivpos.width + "px";
                        textdiv.style.height = textdivpos.height + "px";
                        textdiv.style.fontFamily = "sans-serif";
                        textdiv.style.fontSize = textdivpos.fontsize + "px";
                        //textdiv.style.fontSize = textdivpos.fontsize + "px";
                        //textdiv.style.textAlign = "justify";

                        textdiv.style.letterSpacing = letterspace;

                        //textdiv.style.transform = "scale(1.3)";
                        textdiv.textContent = scope.textlist[i].text;
                        
                        if (scope.textlist[i].rot != 0) {
                            textdiv.style.transformOrigin = "50% 50%";
                            var normrot = -scope.textlist[i].rot;
                            var szrotation = "scale(1.3) rotate(" + normrot + "deg)";
                            textdiv.style.transform = szrotation;
                        }
                        else {
                            //textdiv.style.fontFamily = "sans-serif";
                            //textdiv.style.fontSize = textdivpos.fontsize + "px";
                            //textdiv.textContent = scope.textlist[i].text;
                        }
                        texlayerdata.textLayerDiv.appendChild(textdiv);
                    }
                    scope.textlayerset = true;
                };
                this.clearSearchAll = function () {
                    for (var i = 0; i < scope.textlist.length; i++) {
                        scope.textlist[i].draw = false;
                    }
                };
                this.textSearch = function (text, start) {
                    for (var i = start; i < scope.textlist.length; i++) {
                        if (scope.textlist[i].text == text) {
                            return {
                                numtexts: scope.textlist.length,
                                position: i,
                                textobject: scope.textlist[i]
                            };
                        }
                    }
                    //return last instance and check for match
                    return {
                        numtexts: scope.textlist.length,
                        position: i,
                        textobject: scope.textlist[i - 1]
                    };
                };
                this.highlightText = function (indx) {
                    scope.textlist[indx].draw = true;
                };
                this.getsnapPoint = function (x, y, scalefactor, offsetx, offsety) {
                    var snappoint = {
                        found: false,
                        change : false,
                        x: 0,
                        y: 0
                    };
                    var linesegments = [];
                    var rotatedpoint = { x: x, y: y };
                    if (scope.parent.DocRef.pages[scope.parent.DocRef.currentpage].getpageRotation() != 0) {
                        rotatedpoint = mouse_rotated(x, y);
                    }
                    x = rotatedpoint.x;
                    y = rotatedpoint.y;
                    var snappoints = [];
                    //var pathlines = [];
                    //get all lines
                    for (var i = 0; i < this.pathlist.length; i++) {
                        //var localoffsetx = this.x * scalefactor;
                        //var localoffsety = this.y * scalefactor;
                        //this.layerlist[this.pathlist[i].layer].state == 1
                        //this.blocklist[this.pathlist[i].blockname].state == 1
                        var blayeron = layeron(i);
                        var bblcckon = blockon(i);
                        //var blayeron = true;
                        //var bblcckon = true;
                        /*if (this.layerlist.length > 0) {
        
                            if (this.pathlist[i].layer != -1 && this.layerlist[this.pathlist[i].layer] != undefined) {
                                blayeron = (this.layerlist[this.pathlist[i].layer].state == 1);
                            }
                        }
                        if (this.blocklist.length > 0) {
                            bblcckon = (this.blocklist[this.pathlist[i].blockname].state == 1);
                        }*/
                        if (blayeron && bblcckon) {
                            snappoint = this.pathlist[i].findsnapPoint(x, y, scalefactor, offsetx, offsety, this.x, this.y, this.h);
                            if (snappoint == undefined) {
                                snappoint = {
                                    found: false,
                                    x: 0,
                                    y: 0,
                                    type: snapEndpoint
                                };
                            }
                            if (snappoint.found) {
                                snappoints.push(snappoint);
                            }
                            if (this.pathlist[i].type == "line") {
                                if (this.pathlist[i].closeToMouse(x, y, scalefactor, offsetx, offsety, this.x, this.y, this.h)) {
                                    linesegments.push(i);
                                }
                            }
                            /*if (this.pathlist[i].type == "line"){
                                for (var j = 0; j < this.pathlist.length; j++) {
                                    var blayeron = layeron(j);
                                    var bblcckon = blockon(j);
                                    
                                    if (blayeron && bblcckon) {
                                        if (this.pathlist[j].type == "line"){
                                           var intersect = this.pathlist[j].getInterSection(x,y,this.pathlist[i],scalefactor, offsetx, offsety, this.x, this.y, this.h);
                                           if(intersect){
                                                snappoints.push({found : true, x : intersect.x, y: intersect.y, type:2});
                                           }
                                        }
                                    }
                                }
                                            
                            }*/
                        }
                    }
                    //this.intersections
                    //this.getInterSection = function(x,y,line, scalefactor, offsetx, offsety, mediax, mediay, mediah){    
                    //var snapradius = 10;
                    for (var ints = 0; ints < linesegments.length; ints++) {
                        for (var intsinner = 0; intsinner < linesegments.length; intsinner++) {
                            if (ints != intsinner) {
                                var intersect = this.pathlist[linesegments[intsinner]].getInterSection(x, y, this.pathlist[linesegments[ints]], scalefactor, offsetx, offsety, this.x, this.y, this.h);
                                if (intersect) {
                                    snappoints.push({ found: true, x: intersect.x, y: intersect.y, type: snapIntersection });
                                }
                            }
                        }
                    }
                    /*for(var ints = 0;ints < this.intersections.length; ints++){
        
                        var xscaled =  (this.intersections[ints].x - this.x) * scalefactor;
                        var yscaled =  (this.h - this.intersections[ints].y) * scalefactor;
        
                        xscaled += offsetx;
                        yscaled += offsety;
        
        
                        if (xscaled > x - snapradius && xscaled < x + snapradius) {
                            if (yscaled > y - snapradius && yscaled < y + snapradius) {
                                
                                snappoints.push({found: true, x: xscaled, y: yscaled, type : 2});
                            }
            
                        }
        
                    }*/
                    if (snappoints.length > 0) {
                        var a = x - snappoints[0].x;
                        var b = y - snappoints[0].y;
                        var curdist = Math.sqrt(a * a + b * b);
                        var tempdist = Math.sqrt(a * a + b * b);
                        var snapindx = 0;
                        for (i = 0; i < snappoints.length; i++) {
                            if (snappoints[i].found) {
                                a = x - snappoints[i].x;
                                b = y - snappoints[i].y;
                                tempdist = Math.sqrt(a * a + b * b);
                                if (tempdist < curdist) {
                                    curdist = tempdist;
                                    snapindx = i;
                                }
                            }
                        }
                        snappoint = snappoints[snapindx];
                        /*if(DocObj.getCurrentpage().getpageRotation() != 0){
                            rotatedpoint = mouse_rotated(snappoint.x,snappoint.y);
                            snappoint.x = rotatedpoint.x;
                            snappoint.y = rotatedpoint.y;
                        }*/
                    }
                    else {
                        snappoint = {
                            found: false,
                            change : false,
                            x: 0,
                            y: 0
                        };
                    }
                    return snappoint;
                };
                this.offscreenscale = function (scalefactor) {
                    if (this.backgroundrender) {
                        return this.offscreen.width / (this.width * scalefactor);
                    }
                    else {
                        return 1;
                    }
                };

                this.invert_images = function(bgcolor) {

                    if (scope.docompare) {
                        return;
                    }
                    
                    var pathcount = 0;

                    while (pathcount < scope.pathlist.length) {

                        if (scope.pathlist[pathcount].type == "image"){
                            scope.pathlist[pathcount].invert(bgcolor);
                        }
                            
                        pathcount++;
                    }
                };


                /*this.drawallmagnifycmpre = function (context, scalefactor, offsetx, offsety, comparecolor, isbackground) {
                    //this.isbackground = isbackground;
                    this.docompare = true;
                    this.comparecolor = comparecolor;
                    //console.log(isbackground);
                    //console.log(comparecolor);
                    //backgroundColor = 'white';
                    if (isbackground) {
                        context.globalCompositeOperation = 'source-over';
                        context.fillStyle = 'white';
                        //context.fillRect(offsetx, offsety, this.width * scalefactor, this.height * scalefactor);
                        //context.fillRect(0, 0, magcanvas.width, magcanvas.height);
                        context.fillRect(0, 0, magnifysize.width, magnifysize.height);
                    }
                    else {
                        if (bShowDiffonly) {
                            context.globalCompositeOperation = szdrawmode;
                        }
                        else {
                            context.globalCompositeOperation = 'darken';
                        }
                    }
                    scope.drawallmagnify(context, scalefactor, offsetx, offsety);
                };*/

                this.drawallmagnify = function (context, scalefactor, offsetx, offsety) {

                    scope.drawDirect(context, scalefactor, offsetx, offsety, true, false, true);
                };

                this.drawallmagnifyold = function (context, scalefactor, offsetx, offsety) {
                    var layerstate = 1;
                    var blockstate = 1;


                    for (var poindx = 0; poindx < scope.pathlistoverride.length; poindx++) {
                        layerstate = layeron(scope.pathlistoverride[poindx].index) ? 1 : 0;
                        blockstate = blockon(scope.pathlistoverride[poindx].index) ? 1 : 0;

                        //console.log(scope.pathlistoverride[poindx].type);

                        if (scope.pathlistoverride[poindx]) {
                            
                            scope.pathlistoverride[poindx].drawmefirst(context, scalefactor, offsetx, offsety, scope.x, scope.y, scope.h, layerstate, blockstate, false, false);
                        }


                    }



                    for (var pindx = 0; pindx < scope.pathlist.length; pindx++) {
                        layerstate = layeron(i) ? 1 : 0;
                        blockstate = blockon(i) ? 1 : 0;
                        /*if (scope.layerlist.length > 0) {
                            if (scope.pathlist[pindx].layer != -1 && scope.layerlist[scope.pathlist[pindx].layer] != undefined) {
                                layerstate = scope.layerlist[scope.pathlist[pindx].layer].state;
                            }
        
                        }
                        if (scope.blocklist.length > 0) {
                            blockstate = scope.blocklist[scope.pathlist[pindx].blockname].state;
                        }*/
                        if (scope.docompare) {
                            if (scope.dooverlay) {
                                scope.pathlist[pindx].drawemeoverlay(context, scalefactor, offsetx, offsety, scope.x, scope.y, scope.h, layerstate, blockstate);
                            }
                            else {
                                scope.pathlist[pindx].drawemecompare(context, scalefactor, offsetx, offsety, scope.x, scope.y, scope.h, scope.comparecolor, layerstate, blockstate);
                            }
                        }
                        else {
                            if (scope.pathlist[pindx]) {
                                if (scope.pathlist[pindx].type == "line") {
                                    //drawvectorline(scope.pathlist[pindx],context, scalefactor, offsetx, offsety, scope.x, scope.y, scope.h,layerstate,blockstate,false);
                                    drawvectorline(scope.pathlist[pindx], context, scalefactor, offsetx, offsety, scope.x, scope.y, scope.h, layerstate, blockstate, false, true);
                                }
                                else {
                                    //scope.pathlist[pindx].drawme(context, scalefactor, offsetx, offsety, scope.x, scope.y, scope.h,layerstate,blockstate,false);
                                    scope.pathlist[pindx].drawme(context, scalefactor, offsetx, offsety, scope.x, scope.y, scope.h, layerstate, blockstate, false, true);
                                }
                            }
                        }
                    }
                };
                /*this.drawallcmpre = function (context, scalefactor, offsetx, offsety, refresh, comparecolor, isbackground) {
        
                    this.isbackground = isbackground;
                    this.docompare = true;
                    this.comparecolor = comparecolor;
                    //backgroundColor = 'white';
                    if (this.isbackground) {
                        context.globalCompositeOperation = 'source-over';
                        context.fillStyle = 'white';
                        context.fillRect(offsetx, offsety, this.width * scalefactor, this.height * scalefactor);
        
                    } else {
                        context.globalCompositeOperation = 'darken';
                    }
        
        
                    this.drawall(context, scalefactor, offsetx, offsety, refresh,false);
        
        
        
                };*/
                /*this.drawallcmpre = function (context, scalefactor, offsetx, offsety, refresh, comparecolor, isbackground, mode) {
                    this.isbackground = isbackground;
                    this.dooverlay = (mode == 1);
                    this.docompare = true;
                    this.comparecolor = comparecolor;
                    //backgroundColor = 'white';
                    if (this.isbackground) {
                        //context.globalCompositeOperation = 'source-over';
                        if (bShowDiffonly) {
                            //context.fillStyle = 'black';
                            context.clearRect(offsetx, offsety, this.width * scalefactor, this.height * scalefactor);
                            context.globalCompositeOperation = szdrawmode;
                        }
                        else {
                            context.globalCompositeOperation = 'darken';
                        }
                    }
                    else {
                        if (bShowDiffonly) {
                            context.globalCompositeOperation = szdrawmode;
                        }
                        else {
                            context.globalCompositeOperation = 'darken';
                        }
                    }
                    this.drawallnew(context, scalefactor, offsetx, offsety, refresh, false);
                };*/

                
                this.drawallnew = function (context, scalefactor, offsetx, offsety, refresh, drawprint) {
                

                    const scale = getPixelRatio();

                    scope.parent.DocRef.bRenderinProgress = true;

                    var vectorstartrender = new Date().getTime();
                    var layerstate = 1;
                    var blockstate = 1;
                    var curdrawmode = 13;



                    if ((scope.width * scalefactor) * (scope.height * scalefactor) > nMaximageArea) {
                        
                        /*if(drawprint){
                            console.log("draw direct");
                        }*/
                        
                        
                        scope.backgroundrender = false;
                        scope.drawDirect(context, scalefactor, offsetx, offsety, refresh, drawprint, false);
                    }
                    else {

                        /*if(drawprint){
                            console.log("background render");
                        }*/

                        context.globalCompositeOperation = 'source-over';

                        /*if(scope.parent.DocRef.bcompare){
                            context.globalCompositeOperation = 'darken';
                        }else{
                            
                        }*/
    

                        scope.backgroundrender = true;
                        //context.drawImage(scope.offscreen, offsetx, offsety, scope.width * scalefactor, scope.height * scalefactor);
                        //context.drawImage(scope.offscreen, offsetx, offsety, scope.width * scalefactor, scope.height * scalefactor);
                        if (refresh) {

                            var offscrcvwidth = 1;
                            var offscrcvheight = 1;


                            if((scope.width * scalefactor) < 1){
                                //scope.offscreen.width = 1;    
                                offscrcvwidth = 1;

                            }else{
                                //scope.offscreen.width = scope.width * scalefactor;    
                                offscrcvwidth = scope.width * scalefactor;
                            }
                            if((scope.height * scalefactor) < 1){
                                //scope.offscreen.height = 1;    
                                offscrcvheight = 1;
                            }else{
                                //scope.offscreen.height = scope.height * scalefactor;
                                offscrcvheight = scope.height * scalefactor;
                            }

                            offscrcvwidth = Math.ceil(offscrcvwidth + 0.5);
                            offscrcvheight = Math.ceil(offscrcvheight + 0.5);

                            
                            if(drawprint){
                                scope.offscreenprint.width = offscrcvwidth;
                                scope.offscreenprint.height = offscrcvheight;
                                setSmoothingEnabledEx(false, scope.offscreenprintctx);

                                scope.offscreenprintctx.save();

                                setCtxTranslatehpx(scope.offscreenprintctx);
    
                                //setSmoothingEnabledEx(true, scope.offscreenctx);
    
    
                                scope.drawDirect(scope.offscreenprintctx,scalefactor, 0, 0, true, drawprint, false );
    
                                scope.offscreenprintctx.restore();
    
                                
                        
                            }else{
                                scope.offscreen.width = offscrcvwidth;
                                scope.offscreen.height = offscrcvheight;    
                                setSmoothingEnabledEx(false, scope.offscreenctx);

                                scope.offscreenctx.save();

                                setCtxTranslatehpx(scope.offscreenctx);
    
                                //setSmoothingEnabledEx(true, scope.offscreenctx);
    
    
                                scope.drawDirect(scope.offscreenctx,scalefactor, 0, 0, true, drawprint, false );
    
                                scope.offscreenctx.restore();
    
    
                            }


                            //offscreenctx = scope.offscreen.getContext('2d');
                            

                            
                        }


                        if(drawprint){
                            if(scope.offscreenprint.width >= 1 && scope.offscreenprint.height >= 1){
                                context.drawImage(scope.offscreenprint, offsetx, offsety, this.width * scalefactor, this.height * scalefactor);
                            }
    
                        }else{
                            if(scope.offscreen.width >= 1 && scope.offscreen.height >= 1){
                                context.drawImage(scope.offscreen, offsetx, offsety, this.width * scalefactor, this.height * scalefactor);
                            }
    
                        }


                        
                    }
                    scope.firstdraw = true;
                    if (scope.docompare) {
                        scope.firstdrawcompare = true;
                    }
                    var vectorendrender = new Date().getTime();
                    scope.vectorpagetime = (vectorendrender - vectorstartrender);
                    if (refresh && scope.vectorpagetime > scope.parent.vectorpagetime) {
                        scope.parent.changeinterval(scope.vectorpagetime);
                    }

                    scope.parent.DocRef.bRenderinProgress = false;

                    if(RxCore_GUI_RenderComplete != undefined){
                        RxCore_GUI_RenderComplete.renderComplete(scope.parent.DocRef.fileindex);
                    }


                };
                this.drawDirect = function (context, scalefactor, offsetx, offsety, refresh, drawprint, magnify) {
                    var layerstate = 1;
                    var blockstate = 1;
                    var curdrawmode = 13;

                    var mediax = scope.x;
                    var mediah = scope.h;
                    var localscalefactor = scalefactor;
                    var scalerole = 0;

                    if(scope.parent.loadbackgroundvectors){

                        scalerole = 2;

                    }else{
                        if(scope.parent.loadoverlayvectors){
                            
                            scalerole = 1;

                        }else{
                            
                            scalerole = 2;

                        }
                    }

                    //scope.x, scope.y, scope.h


                    if (!scope.docompare) {
                        if (drawprint) {
                            context.fillStyle = 'white';
                        }
                        else {
                            context.fillStyle = scope.parent.DocRef.backgroundColor;
                        }
                        context.globalCompositeOperation = 'source-over';

                        var drawWidth = scope.width * scalefactor;
                        var drawHeight = scope.height * scalefactor;

                        if(drawWidth < 1){
                            drawWidth = 1;
                        } 
                        if(drawHeight < 1){
                            drawHeight = 1;
                        } 


                        context.fillRect(offsetx, offsety, drawWidth, drawHeight);
                    }


                    for (var poindx = 0; poindx < scope.pathlistoverride.length; poindx++) {
                        layerstate = layeron(scope.pathlistoverride[poindx].index) ? 1 : 0;
                        blockstate = blockon(scope.pathlistoverride[poindx].index) ? 1 : 0;

                        //console.log(scope.pathlistoverride[poindx].type);

                        if (scope.pathlistoverride[poindx]) {
                            
                            scope.pathlistoverride[poindx].drawmefirst(context, scalefactor, offsetx, offsety, scope.x, scope.y, scope.h, layerstate, blockstate, drawprint, magnify);
                        }


                    }

                    var countbg = 0;
                    var countfg = 0;

                    if(scope.bcompare){
                        //context.globalCompositeOperation = 'darken';

                        if(scope.comparemode == 0){
                            context.globalCompositeOperation = 'multiply';
                        }

                        if(scope.comparemode == 1){
                            context.globalCompositeOperation = 'source-over';    
                        }


                    }else{
                        context.globalCompositeOperation = 'source-over';
                    }


                    for (var pindx = 0; pindx < scope.pathlist.length; pindx++) {
                        layerstate = layeron(pindx) ? 1 : 0;
                        blockstate = blockon(pindx) ? 1 : 0;

                        if (scope.pathlist[pindx]) {

                       
                            if(scope.pathlist[pindx].role == scalerole){
                                
                                mediax = scope.overlayoffsetx + scope.nOffsetX;
                                mediah = scope.overlayoffsety + scope.nOffsetY;
                                
                                localscalefactor = scalefactor * (scope.overlayscale + scope.nudgeScalefactor);
    
                            }else{
                                mediax = scope.x;
                                mediah = scope.h;
                                localscalefactor = scalefactor;
                            }
    


                            if (scope.pathlist[pindx].type == "line") {
                                //drawvectorline(scope.pathlist[pindx],context, scalefactor, offsetx, offsety, scope.x, scope.y, scope.h,layerstate,blockstate,drawprint);

                                drawvectorline(scope.pathlist[pindx], context, localscalefactor, offsetx, offsety, mediax, scope.y, mediah, layerstate, blockstate, drawprint, magnify);
                            }
                            else {
                                //scope.pathlist[pindx].drawme(context, scalefactor, offsetx, offsety, scope.x, scope.y, scope.h,layerstate,blockstate,drawprint);
                                scope.pathlist[pindx].drawme(context, localscalefactor, offsetx, offsety, mediax, scope.y, mediah, layerstate, blockstate, drawprint, magnify);
                            }
                        }
                        
                    }

                    //console.log(countbg, countfg);

                };

                this.refreshPath = function(pathobj, context, scalefactor, offsetx, offsety){

                    var layerstate = 1;
                    var blockstate = 1;
                    //var curdrawmode = 13;
                    if ((scope.width * scalefactor) * (scope.height * scalefactor) > nMaximageArea) {
                        scope.backgroundrender = false;
                        if (scope.layerlist.length > 0) {
                            if (pathobj.layer != -1 && scope.layerlist[pathobj] != undefined) {
                                layerstate = scope.layerlist[pathobj.layer].state;
                            }
                        }
                        if (scope.blocklist.length > 0) {
                            blockstate = scope.blocklist[pathobj.blockname].state;
                            /*if (scope.blocklist[scope.pathlist[pindx].blockname].state == 0) {
                             continue;
                             }*/
                        }
                        if (pathobj) {
                            pathobj.drawmefirst(context, scalefactor, offsetx, offsety, scope.x, scope.y, scope.h, layerstate, blockstate);
                        }
                    }
                    else {
                        scope.backgroundrender = true;
                        //context.drawImage(scope.offscreen, offsetx, offsety, scope.width * scalefactor, scope.height * scalefactor);
                        //context.drawImage(scope.offscreen, offsetx, offsety, scope.width * scalefactor, scope.height * scalefactor);
                        if (scope.layerlist.length > 0) {
                            if (pathobj.layer != -1 && scope.layerlist[pathobj.layer] != undefined) {
                                layerstate = scope.layerlist[pathobj.layer].state;
                            }
                        }
                        if (scope.blocklist.length > 0) {
                            blockstate = scope.blocklist[pathobj.blockname].state;
                        }
                        if (pathobj.type == "line") {
                            drawvectorline(pathobj, scope.offscreenctx, scalefactor, 0, 0, scope.x, scope.y, scope.h, layerstate, blockstate, false);
                        }
                        else {
                            pathobj.drawmefirst(scope.offscreenctx, scalefactor, 0, 0, scope.x, scope.y, scope.h, layerstate, blockstate, false);
                        }
                        /*if (refresh) {
                            scope.offscreen.width = scope.width * scalefactor;
                            scope.offscreen.height = scope.height * scalefactor;
                            setSmoothingEnabledEx(true,scope.offscreenctx);
        
                            if (!scope.docompare) {
                                scope.offscreenctx.fillStyle = DocObj.backgroundColor;
                                scope.offscreenctx.fillRect(0, 0, scope.width * scalefactor, scope.height * scalefactor);
        
                            }
        
                        }*/
                        context.drawImage(scope.offscreen, offsetx, offsety, this.width * scalefactor, this.height * scalefactor);
                    }
                };

                this.drawSelected = function (context, scalefactor, offsetx, offsety) {
                    if (scope.selectedobject == undefined) {
                        return;
                    }
                    var layerstate = 1;
                    var blockstate = 1;
                    var curdrawmode = 13;
                    if ((scope.width * scalefactor) * (scope.height * scalefactor) > nMaximageArea) {
                        scope.backgroundrender = false;
                        if (scope.layerlist.length > 0) {
                            if (scope.selectedobject.layer != -1 && scope.layerlist[scope.selectedobject.layer] != undefined) {
                                layerstate = scope.layerlist[scope.selectedobject.layer].state;
                            }
                        }
                        if (scope.blocklist.length > 0) {
                            blockstate = scope.blocklist[scope.selectedobject.blockname].state;
                            /*if (scope.blocklist[scope.pathlist[pindx].blockname].state == 0) {
                             continue;
                             }*/
                        }
                        if (scope.selectedobject) {
                            scope.selectedobject.drawme(context, scalefactor, offsetx, offsety, scope.x, scope.y, scope.h, layerstate, blockstate);
                        }
                    }
                    else {
                        scope.backgroundrender = true;
                        //context.drawImage(scope.offscreen, offsetx, offsety, scope.width * scalefactor, scope.height * scalefactor);
                        //context.drawImage(scope.offscreen, offsetx, offsety, scope.width * scalefactor, scope.height * scalefactor);
                        if (scope.layerlist.length > 0) {
                            if (scope.selectedobject.layer != -1 && scope.layerlist[scope.selectedobject.layer] != undefined) {
                                layerstate = scope.layerlist[scope.selectedobject.layer].state;
                            }
                        }
                        if (scope.blocklist.length > 0) {
                            blockstate = scope.blocklist[scope.selectedobject.blockname].state;
                        }
                        if (scope.selectedobject.type == "line") {
                            drawvectorline(scope.selectedobject, scope.offscreenctx, scalefactor, 0, 0, scope.x, scope.y, scope.h, layerstate, blockstate, drawprint);
                        }
                        else {
                            scope.selectedobject.drawme(scope.offscreenctx, scalefactor, 0, 0, scope.x, scope.y, scope.h, layerstate, blockstate, drawprint);
                        }
                        /*if (refresh) {
                            scope.offscreen.width = scope.width * scalefactor;
                            scope.offscreen.height = scope.height * scalefactor;
                            setSmoothingEnabledEx(true,scope.offscreenctx);
        
                            if (!scope.docompare) {
                                scope.offscreenctx.fillStyle = DocObj.backgroundColor;
                                scope.offscreenctx.fillRect(0, 0, scope.width * scalefactor, scope.height * scalefactor);
        
                            }
        
                        }*/
                        context.drawImage(scope.offscreen, offsetx, offsety, this.width * scalefactor, this.height * scalefactor);
                    }
                };



                this.Close = function () {

                    
                    if(scope.bgDocObjref){
                        scope.bgDocObjref.setComparelink = undefined;
                        scope.bgDocObjref = undefined;
                    }
                    if(scope.fgDocObjref){
                        scope.fgDocObjref.setComparelink = undefined;
                        scope.fgDocObjref = undefined;
                    }
                    
                    
                    
                    

                    scope.layerlist = [];
                    scope.blocklist = [];
                    scope.filterblocklist = [];
                    /*while (scope.layerlist.length != 0) {
                        scope.layerlist.pop();
                    }
        
                    while (scope.blocklist.length != 0) {
                        scope.blocklist.pop();
                    }*/
                    while (this.pathlist.length != 0) {
                        this.pathlist[this.pathlist.length - 1].Close();
                        this.pathlist.pop();
                    }

                    scope.pathlist = [];

    
                };

                //scope.vectorloader = new VectorLoader(scope, pagexmldata, false);

                if(binary){
                    loadvectors();
                }else{

                    //use simple object to create an empty vector page object
                    //then we can add vectors or images to use as an overaly compare.

                    if (pagexmldata.iscompare){
                        createempty(pagexmldata);
                    }
                }

                

                //loadVectors();
                //scope.vectorloader.load();

                //readheader();
                /*if(binary){
                    parse2Dloop(readheader());
                }*/
                
                /*function yieldingLoop(state, chunksize, callback, finished) {
                    (function chunk() {
                        var i = 0;
                        //var end = Math.min(i + chunksize, count);
                        var end = (i < chunksize);
                        for (; i < chunksize; ++i) {
                            callback.call(null, state);
                            if (state.objectid == 32768) {
                                break;
                            }
                        }
                        if (state.objectid != 32768) {
                            setTimeout(chunk, 0);
                        }
                        else {
                            //console.log(state.objectid);
                            finished.call(null);
                        }
                    })();
                }*/
            }
        }


        class PointerControls{
            constructor() {
                
                
                var scope = this;
                
                //globals
                this.camera = DocObj.getCurrentpage().camera;
                this.domElement = renderer.domElement;
                this.position0 = this.camera.position.clone();
                this.movementSpeed = 1.0;
                this.upmovementSpeed = 1.0;
                this.resetmovementSpeed = 1.0;

                this.autoSpeedFactor = 0.0;
                this.quickSpeed = false;
                this.autoForward = false;
                this.lookdirection = new THREE.Vector3();
                this.modelCenter = new THREE.Vector3(0, 0, 0);

                this.isLocked = false;
                this.walkheight = 1.8;
                this.tempWalkheight = 1.8;
                this.walkspeed = 10;
                this.vectorup = new THREE.Vector3(0, 0, 0);
                this.unit = 1;
                this.maxfloordist = 1.8;
                this.mouseDragOn = false;
                this.reversemouse = false;

                var prevTime = performance.now();

                var moveForward = false;
                var moveBackward = false;
                var moveLeft = false;
                var moveRight = false;

                var moveUp = false;
                var moveDown = false;

                var canJump = false;
                var curdist = 0;
                var deltadist = 0;
                var ticker = 0;
                var camangle = {degrees : 0, radians : 0};
                var campos = new THREE.Vector3();

                var velocity = new THREE.Vector3();
                var direction = new THREE.Vector3();
                
                var origin = new THREE.Vector3();
                
                var intersections = [];

                var previousTouch;                


                //var changeEvent = { type: 'change' };
                //var lockEvent = { type: 'lock' };
                //var unlockEvent = { type: 'unlock' };
            
                var euler = new THREE.Euler( 0, 0, 0, 'ZYX' );
            
                var PI_2 = Math.PI / 2;
                var PI_4 = Math.PI / 4;
            
                var vec = new THREE.Vector3();

                this.setinitial = function(position, size){
                    
                    //scope.movementSpeed = Math.round(size * 0.075);
                    //scope.upmovementSpeed = scope.movementSpeed * 0.25;
                    //scope.resetmovementSpeed = scope.movementSpeed;
                    
                    //scope.lookSpeed = 0.125;
                    //scope.lookVertical = true;

                    scope.modelCenter.copy(position);
                    scope.position0.copy(position);
                    scope.position0.z = 0;
                    
                    scope.maxfloordist =  DocObj.getCurrentpage().Vector3DPageObj.getfloorheight(); 

                };



                this.setDimPos = function (vectorup, maxdist, center) {
                    //scope.firstxyrecieved = false;
                    scope.vectorup = vectorup.clone();

                    //DocObj.setthumcamerapos(position, camangle);

                    //scope.target.copy(scope.target0);
                    //scope.camera.position.copy(scope.position0);
                    
                    //DocObj.setthumcamerapos(position, camangle);
                    //this.object.lookAt( targetPosition );
                    /*if (scope.vectorup.x == 1){
                        scope.lon = 0;
                    }else if(scope.vectorup.y == 1){
                        scope.lon = 270;
                    }else if(scope.vectorup.z == 1){
                        scope.lon = 90;
                    }*/
                    //tool.maxDistance = maxdist;
                    //scope.target.copy(center);
                    //scope.target.set(0, 5, 0);
                    scope.update();
                };

                this.quickspeed = function(onOff){

                    scope.quickSpeed = onOff;

                };
                this.movedirection = function(direction, yesno){
                    switch (direction){
                        case 1 : moveForward = yesno;
                            if(!yesno){
                                //console.log(intersections);
                                scope.updatebirdseye();
                            }else{
                                intersections = DocObj.getCurrentpage().Vector3DPageObj.getBelowIntersections(origin, scope.walkheight);
                                scope.checkmoveup(intersections);
                            }
                                
                        break;
                        case 2 : moveBackward = yesno;
                            if(!yesno){

                                scope.updatebirdseye();
                            }else{
                                intersections = DocObj.getCurrentpage().Vector3DPageObj.getBelowIntersections(origin, scope.walkheight);
                                scope.checkmoveup(intersections);
                            }

                        break;
                        case 3 : moveLeft = yesno;
                            if(!yesno){
                                intersections = DocObj.getCurrentpage().Vector3DPageObj.getBelowIntersections(origin, scope.walkheight);
                                scope.checkmoveup(intersections);
                                scope.updatebirdseye();
                            }

                        break;
                        case 4 : moveRight = yesno;
                            if(!yesno){
                                intersections = DocObj.getCurrentpage().Vector3DPageObj.getBelowIntersections(origin, scope.walkheight);
                                scope.checkmoveup(intersections);
                                scope.updatebirdseye();
                            }

                        break;
                        case 5 : moveUp = yesno;
                        if(!yesno){
                            intersections = DocObj.getCurrentpage().Vector3DPageObj.getBelowIntersections(origin, scope.walkheight);
                            scope.checkmoveup(intersections);
                            scope.updatebirdseye();
                        }

                        break;
                        case 6 : moveDown = yesno;
                        if(!yesno){
                            intersections = DocObj.getCurrentpage().Vector3DPageObj.getBelowIntersections(origin, scope.walkheight);
                            scope.checkmoveup(intersections);
                            scope.updatebirdseye();
                        }
                        break;
                    }
                    
                };
                this.checkMoveToFloorDown = function(){

                    var curfloor = DocObj.getCurrentpage().Vector3DPageObj.checkcurfloor();
                    //console.log('current floor',curfloor);
                    if(DocObj.getCurrentpage().Vector3DPageObj.firstfloor(curfloor)){
                        var prevfloor = curfloor;    
                    }else{
                        prevfloor = curfloor - 1;
                    }
                    
                    var floorelevation = DocObj.getCurrentpage().Vector3DPageObj.getfloorElevation(prevfloor);

                    //console.log('elevation',floorelevation);
                    moveDown = false;

                    return floorelevation;

                };
                this.checkMoveToFloorUp = function(){

                    var curfloor = DocObj.getCurrentpage().Vector3DPageObj.checkcurfloor();
                    if(DocObj.getCurrentpage().Vector3DPageObj.lastfloor(curfloor)){
                        var nextfloor = curfloor;    
                    }else{
                        nextfloor = curfloor + 1;
                    }
                    
                    var floorelevation = DocObj.getCurrentpage().Vector3DPageObj.getfloorElevation(nextfloor);

                    return floorelevation;

                };
                this.checkmoveup = function(intersections){
                    
                    //return {vec : returnVector, dist : distance};

                    var vec = DocObj.getCurrentpage().Vector3DPageObj.checkNewHeight(intersections);
                    if (vec.vec != null){
                        var distance = vec.dist;
                        var deltadist = distance - curdist;
                        curdist = distance;
                        
                        scope.tempWalkheight = vec.vec.z + scope.maxfloordist;

                        scope.tempWalkheight = Math.round((scope.tempWalkheight + Number.EPSILON) * 100) / 100;
                        distance = Math.round((distance + Number.EPSILON) * 100) / 100;

                        //console.log('distance: ', distance, 'posz: ', scope.tempWalkheight);

                        if (distance < scope.maxfloordist){

                            scope.movementSpeed = scope.upmovementSpeed;
                            
                            
        

                            
                            scope.jumpup();
                                    //velocity.z += 350;
                                    //scope.setTempWalkHeight(vec.z);
                                    //intersections = DocObj.getCurrentpage().Vector3DPageObj.getBelowIntersections();
                        }else{
                            scope.movementSpeed = scope.resetmovementSpeed;
                        }

                    }
                    
                    
                    
                }; 

                this.setCameraXYPos = function(campos){

                    var birdseyecampos = new THREE.Vector3(0, 0, 0);
                    birdseyecampos.x = campos.x;
                    birdseyecampos.y = campos.y;
                    
                    if(campos.z == null){
                        birdseyecampos.z =  scope.camera.position.z;
                        
                    }else{
                        birdseyecampos.z = campos.z;
                    }

                    scope.tempWalkheight = birdseyecampos.z;

                    scope.position0.copy(birdseyecampos);
                    scope.camera.position.copy(birdseyecampos);
                    origin.copy(scope.camera.position);

                    intersections = DocObj.getCurrentpage().Vector3DPageObj.getBelowIntersections(origin, scope.walkheight);
                    scope.checkmoveup(intersections);



                    scope.setbirdseye( birdseyecampos, camangle );
                    scope.updatebirdseye();

                    //var camangle = {degrees : scope.lon, radians : scope.theta};
                // DocObj.setthumcamerapos(birdseyecampos, camangle);


                    //scope.update();
                    //DocObj.draw3DNav();

                };

                this.setbirdseye = function(pos, angle){

                    if(RxCore_GUI_CameraPos != undefined){
                        RxCore_GUI_CameraPos.cameraPos(pos, angle, scope.modelCenter);
                    }

                    DocObj.setthumcamerapos(pos, angle);

                    

                };

                this.updatebirdseye = function(){
                
                    DocObj.draw3DNav();


                    if(RxCore_GUI_UpdatebirdsEye != undefined){
                        
                        RxCore_GUI_UpdatebirdsEye.drawbirdseye();

                    }
                    //var camangle = {degrees : scope.lon, radians : scope.theta};
                    //DocObj.setthumcamerapos(scope.object.position, camangle);


                };                
                this.getObject = function () { // retaining this method for backward compatibility

                    return scope.camera;
            
                };

                this.setTempWalkHeight = function ( height ){
                    scope.tempWalkheight = height;
                };
                this.setwalkHeight = function( height ){
                    scope.walkheight = height;
                    //scope.tempWalkheight = height;
                };

                this.setwalkSpeed = function( speed ){
                    scope.walkspeed = speed;
                };

                this.setwalkUnit = function( unit ){
                    scope.unit = unit;


                    scope.movementSpeed = unit * 4;
                    scope.upmovementSpeed = scope.movementSpeed * 0.25;
                    scope.resetmovementSpeed = scope.movementSpeed;

                };


                this.onMouseDown = function ( button ){
                    if (this.domElement !== document) {
                        this.domElement.focus();
                    }

                    this.mouseDragOn = true;

                };
                this.onMouseUp = function (button) {
                    this.mouseDragOn = false;
                    previousTouch = undefined;
                };


                this.onMouseMove = function( event ) {

                    if ( this.mouseDragOn === false ) return;
            
                    var movementX = event.movementX || event.mozMovementX || event.webkitMovementX || 0;
                    var movementY = event.movementY || event.mozMovementY || event.webkitMovementY || 0;


                    if (movementX == 0 && movementY == 0){

                        if (event.touches != undefined){

                            if(event.touches[0] != undefined){

                                const touch = event.touches[0];
    
                                if (previousTouch != undefined) {
                                    // be aware that these only store the movement of the first touch in the touches array
                                    movementX = touch.pageX - previousTouch.pageX;
                                    movementY = touch.pageY - previousTouch.pageY;
                            
                                    //getMovements(e);
                                };
                            
                                previousTouch = touch;
                            }
    
                        }

                    }                    
            
                    euler.setFromQuaternion( scope.camera.quaternion );

                    if(breverse3dXrotate){
                        euler.z -= -(movementX * 0.002);
                        euler.x -= -(movementY * 0.002);
                    }else{
                        euler.z -= movementX * 0.002;
                        euler.x -= movementY * 0.002;
                    }

                    camangle.radians = euler.z;
                    camangle.radians += Math.PI * 0.5;

                    if (camangle.radians < 0){
                        camangle.radians += Math.PI * 2;
                    }

                    //scope.setbirdseye(campos, camangle);
                    scope.updatebirdseye();

                    //console.log('zrot: ', camangle.radians);
            
                    euler.x = Math.max( - 0, Math.min( Math.PI, euler.x ) );
                    
                    scope.camera.quaternion.setFromEuler( euler );
                        
                }

                this.getDirection = function () {
            
                    var direction = new THREE.Vector3( 0, -1, 0 );
            
                    return function ( v ) {
            
                        return v.copy( direction ).applyQuaternion( scope.camera.quaternion );
            
                    };
            
                }();

                this.scrollbackward = function(distance){
                    
                    distance *= 0.5;

                    var onObject = intersections.length > 0;

                    var zlevel = scope.camera.position.z;
                    scope.camera.translateZ(distance * scope.unit);
                    if(onObject){
                        scope.camera.position.z = zlevel;
                    }else{
                        scope.tempWalkheight = scope.camera.position.z;
                    }

                    intersections = DocObj.getCurrentpage().Vector3DPageObj.getBelowIntersections(origin, scope.walkheight);
                    scope.checkmoveup(intersections);

                    campos = scope.camera.position.clone();
                    
                    scope.setbirdseye(campos, camangle);
                    scope.updatebirdseye();
                };

                this.scrollforward = function (distance){
                    
                    distance *= 0.5;

                    var onObject = intersections.length > 0;

                    var zlevel = scope.camera.position.z;
                    scope.camera.translateZ(-distance * scope.unit);

                    if(onObject){
                        scope.camera.position.z = zlevel;
                    }else{
                        scope.tempWalkheight = scope.camera.position.z;
                    }

                    intersections = DocObj.getCurrentpage().Vector3DPageObj.getBelowIntersections(origin, scope.walkheight);
                    scope.checkmoveup(intersections);

                    campos = scope.camera.position.clone();
                    
                    scope.setbirdseye(campos, camangle);
                    scope.updatebirdseye();
                };

                this.moveUp = function ( distance ){

                };

                this.moveDown = function ( distance ){

                };

                this.jumpup = function(){
                    if ( canJump === true ) velocity.z += 30;
                    canJump = false;
                };

                this.moveForward = function ( distance ) {
            
                    
                    // move forward parallel to the xz-plane
                    // assumes camera.up is y-up
            
                    vec.setFromMatrixColumn( scope.camera.matrix, 0 );
            
                    vec.crossVectors( scope.camera.up, vec );
            
                    distance *= scope.unit;
                    scope.camera.position.addScaledVector( vec, distance );
                    
            
                };
            
                this.moveRight = function ( distance ) {
                    
                    //intersections = DocObj.getCurrentpage().Vector3DPageObj.getBelowIntersections();
            
                    vec.setFromMatrixColumn( scope.camera.matrix, 0 );
                    
                    distance *= scope.unit;
                    scope.camera.position.addScaledVector( vec, distance );
            
                };

                this.update = function(){
                    if ( scope.isLocked === true ) {

                        
                        origin.copy(scope.camera.position);

                        //var raycaster = new THREE.raycaster();
                        //raycaster.ray.origin.copy( scope.camera.position );
                        //raycaster.ray.origin.y -= scope.walkheight;

                        //objects are model meshes
                        //var intersections = raycaster.intersectObjects( objects );

                        var onObject = intersections.length > 0;

                        var time = performance.now();
                        var delta = ( time - prevTime ) / 1000;

                        /* alternative position test
                        velocity.x -= velocity.x * scope.walkspeed * delta;
                        velocity.y -= velocity.y * scope.walkspeed * delta;
                        */
                        
                        velocity.z -= (9.8 * 100.0 * delta) * scope.unit; // 100.0 = mass
                        
                        
                        

                        /* alternative position test
                        direction.y = Number( moveForward ) - Number( moveBackward );
                        direction.x = Number( moveRight ) - Number( moveLeft );
                    
                        direction.normalize(); // this ensures consistent movements in all directions
                        */

                        var actualMoveSpeed = delta * scope.movementSpeed;
                        var actualMoveSpeedSide = delta * scope.movementSpeed * 0.8;

                        if(scope.quickSpeed){
                            actualMoveSpeed = delta * scope.movementSpeed * 10;
                            actualMoveSpeedSide = delta * scope.movementSpeed * 0.8 * 10;

                        }

                        if (moveForward || (scope.autoForward && !moveBackward)){
                            
                            //scope.lookdirection.y = 0.0;
                            //console.log(scope.lookdirection);
                            var zlevel = scope.camera.position.z;
                            scope.camera.translateZ(-(actualMoveSpeed + scope.autoSpeedFactor));

                            if(onObject){
                                scope.camera.position.z = zlevel;
                            }else{
                                scope.tempWalkheight = scope.camera.position.z;
                            }

                            //scope.camera.translateOnAxis(scope.lookdirection, actualMoveSpeed);

                        }
                        if (moveBackward){
                            //scope.camera.getWorldDirection(scope.lookdirection);
                            //scope.lookdirection.y = 0.0;
                            //scope.camera.translateOnAxis(scope.lookdirection, -(actualMoveSpeed + scope.autoSpeedFactor));
                            zlevel = scope.camera.position.z;
                            scope.camera.translateZ(actualMoveSpeed);
                            if(onObject){
                                scope.camera.position.z = zlevel;
                            }else{
                                scope.tempWalkheight = scope.camera.position.z;
                            }

                        }
                        if (moveLeft)
                            scope.camera.translateX(-actualMoveSpeedSide);
                        if (moveRight)
                            scope.camera.translateX(actualMoveSpeedSide);
                        if (moveUp){
                            //scope.camera.translateY(actualMoveSpeed);
                            scope.camera.position.z = scope.checkMoveToFloorUp();
                            scope.tempWalkheight = scope.camera.position.z;

                            scope.updatebirdseye();

                        }

                        if (moveDown){
                            //scope.camera.translateY(-actualMoveSpeed);
                            scope.camera.position.z = scope.checkMoveToFloorDown();
                            scope.tempWalkheight = scope.camera.position.z;

                            scope.updatebirdseye();

                        }

                        if ( onObject === true && curdist <= scope.maxfloordist ) {

                            velocity.z = Math.max( 0, velocity.z );
                            canJump = true;

                        }
                        
                        /* alternative position test
                        scope.moveRight( - velocity.x * delta );
                        scope.moveForward( - velocity.y * delta );
                        */
                    if (!moveUp){
                            scope.camera.position.z += ( velocity.z * delta ); // new behavior
                    }

                        if(scope.camera.position.z < scope.tempWalkheight && !moveDown ){
                            velocity.z = 0;
                            scope.camera.position.z = scope.tempWalkheight;
                            
                            canJump = true;

                        }

                        if ( scope.camera.position.z < scope.walkheight && !moveDown ) {
                            
                            
                            velocity.z = 0;
                            scope.camera.position.z = scope.walkheight;
                            
                            canJump = true;

                        }

                        ticker += 1;

                        if (ticker > 10){
                            //var camangle = {degrees : 0, radians : 0};

                            campos = scope.camera.position.clone();
                            scope.setbirdseye(campos, camangle);

                            //imlement as callback to use with other rxcore instance.
                            //console.log('tick');

                            

                            ticker = 0;
                        }

                        scope.position0.copy(scope.camera.position);
                        prevTime = time;

                    }
                }

            
            }
        }

        class FirstPersonControl {
            constructor() {
                var scope = this;
                this.object = DocObj.getCurrentpage().camera;
                this.modelCenter = new THREE.Vector3(0, 0, 0);
                this.domElement = renderer.domElement;
                this.target = new THREE.Vector3(0, 0, 0);
                this.vectorup = new THREE.Vector3(0, 0, 0);
                //this.object.rotation.set(0, 0, 90 * Math.PI / 180);
                this.enabled = false;
                this.firstxyrecieved = false;
                this.movementSpeed = 1.0;
                this.lookSpeed = 0.005;
                this.lookVertical = true;
                this.autoForward = false;
                this.activeLook = true;
                this.heightSpeed = false;
                this.heightCoef = 1.0;
                this.heightMin = 0.0;
                this.heightMax = 1.0;
                this.constrainVertical = false;
                this.verticalMin = 0;
                this.verticalMax = Math.PI;
                this.autoSpeedFactor = 0.0;
                this.mouseX = 0;
                this.mouseY = 0;
                this.lat = 0;
                this.lon = 90;
                this.phi = 0;
                this.theta = 0;
                this.moveForward = false;
                this.moveBackward = false;
                this.moveLeft = false;
                this.moveRight = false;
                this.moveUp = false;
                this.moveDown = false;
                this.mouseMove = false;
                this.mouseDragOn = false;
                this.viewHalfX = 0;
                this.viewHalfY = 0;
                //this.positi;
                this.target0 = this.target.clone();
                this.position0 = this.object.position.clone();
                var ticker = 10;
                
                this.setinitial = function(position, size){
                    scope.movementSpeed = Math.round(size * 0.075);
                    scope.lookSpeed = 0.125;
                    scope.lookVertical = true;

                    scope.modelCenter.copy(position);
                    scope.position0.copy(position); 
                };


                if (this.domElement !== document) {
                    ///this.domElement.setAttribute( 'tabindex', - 1 );
                }
                this.setPartPos = function (targetpos, camerapos) {
                    //scope.firstxyrecieved = false;
                    scope.target0.copy(targetpos);
                    scope.position0.copy(camerapos);
                };
                this.setPos = function () {
                    scope.target.set(0, 5, 0);
                    //scope.object.lookAt( targetPosition );
                    scope.update();
                };
                this.setCameraXYPos = function(campos){

                    var birdseyecampos = new THREE.Vector3(0, 0, 0);
                    birdseyecampos.x = campos.x;
                    birdseyecampos.y = campos.y;
                    
                    if(campos.z == null){
                        birdseyecampos.z =  scope.object.position.z;
                    }else{
                        birdseyecampos.z = campos.z;
                    }

                    scope.position0.copy(birdseyecampos);

                    
                    scope.object.position.copy(birdseyecampos);

                    //var camangle = {degrees : scope.lon, radians : scope.theta};
                // DocObj.setthumcamerapos(birdseyecampos, camangle);

                    
                    

                    //scope.update();
                    //DocObj.draw3DNav();

                };

                
                this.setDimPos = function (vectorup, maxdist, center) {
                    scope.firstxyrecieved = false;
                    scope.vectorup = vectorup.clone();
                    scope.target.copy(scope.target0);
                    scope.object.position.copy(scope.position0);
                    
                    //DocObj.setthumcamerapos(position, camangle);
                    //this.object.lookAt( targetPosition );
                    /*if (scope.vectorup.x == 1){
                        scope.lon = 0;
                    }else if(scope.vectorup.y == 1){
                        scope.lon = 270;
                    }else if(scope.vectorup.z == 1){
                        scope.lon = 90;
                    }*/
                    //tool.maxDistance = maxdist;
                    //scope.target.copy(center);
                    //scope.target.set(0, 5, 0);
                    scope.update();
                };
                this.reset = function () {
                    //this.target.copy(this.target0);
                    //this.object.position.copy(this.position0);
                    this.lat = 0;
                    this.lon = 90;
                    this.phi = 0;
                    this.theta = 0;
                };
                this.setInitial = function (lat, lon, phi, theta) {
                    this.lat = lat;
                    this.lon = lon;
                    this.phi = phi;
                    this.theta = theta;
                };
                this.handleResize = function () {
                    if (this.domElement === document) {
                        this.viewHalfX = window.innerWidth / 2;
                        this.viewHalfY = window.innerHeight / 2;
                    }
                    else {
                        this.viewHalfX = this.domElement.offsetWidth / 2;
                        this.viewHalfY = this.domElement.offsetHeight / 2;
                    }
                };
                this.updatebirdseye = function(){
                
                    DocObj.draw3DNav();


                    if(RxCore_GUI_UpdatebirdsEye != undefined){
                        
                        RxCore_GUI_UpdatebirdsEye.drawbirdseye();

                    }
                    //var camangle = {degrees : scope.lon, radians : scope.theta};
                    //DocObj.setthumcamerapos(scope.object.position, camangle);


                };
                this.movedirection = function(direction, yesno){
                    switch (direction){
                        case 1 : scope.moveForward = yesno;
                            if(yesno){
                                scope.updatebirdseye();
                            }
                                
                        break;
                        case 2 : scope.moveBackward = yesno;
                            if(yesno){
                                scope.updatebirdseye();
                            }

                        break;
                        case 3 : scope.moveLeft = yesno;
                            if(yesno){
                                scope.updatebirdseye();
                            }

                        break;
                        case 4 : scope.moveRight = yesno;
                            if(yesno){
                                scope.updatebirdseye();
                            }

                        break;
                        case 5 : scope.moveUp = yesno;
                            break;
                        case 6 : scope.moveDown = yesno;
                            break;
                    }
                    
                };

                this.onMouseDown = function (button) {
                    if (!scope.firstxyrecieved) {
                        scope.firstxyrecieved = true;
                    }


                    if (this.domElement !== document) {
                        this.domElement.focus();
                    }
                    //event.preventDefault();
                    //event.stopPropagation();
                    if (this.activeLook && this.mouseMove) {
                        switch (button) {
                            case 0:
                                this.moveForward = true;
                                break;
                            case 2:
                                this.moveBackward = true;
                                break;
                        }
                    }
                    this.mouseDragOn = true;
                };
                this.onMouseUp = function (button) {
                    //event.preventDefault();
                    //event.stopPropagation();
                    if (this.activeLook && this.mouseMove) {
                        switch (button) {
                            case 0:
                                this.moveForward = false;
                                break;
                            case 2:
                                this.moveBackward = false;
                                break;
                        }
                    }
                    this.mouseDragOn = false;
                };
                this.onMouseMove = function (x, y) {
                    if (!scope.firstxyrecieved) {
                        //scope.firstxyrecieved = true;
                        return;
                    }
                    if (this.domElement === document) {
                        scope.mouseX = -(x - scope.viewHalfX);
                        scope.mouseY = -(y - scope.viewHalfY);
                    }
                    else {
                        if (scope.mouseDragOn) {
                            
                            //mouseX = - ( event.clientX / renderer.domElement.clientWidth ) * 2 + 1;
                            //mouseY = - ( event.clientY / renderer.domElement.clientHeight ) * 2 + 1;
                        
                            //camera.rotation.x = mouseY / scale;
                            //camera.rotation.y = mouseX / scale;

                            scope.mouseX = x - scope.domElement.offsetLeft - scope.viewHalfX;
                            scope.mouseY = y - scope.domElement.offsetTop - scope.viewHalfY;
                            
                            scope.updatebirdseye();

                            //var camangle = {degrees : scope.lon, radians : scope.theta};
                            //DocObj.setthumcamerapos(scope.object.position, camangle);

                        }
                        else {
                            scope.mouseX = 0;
                            scope.mouseY = 0;
                        }
                    }

                    

                };
                this.update = function (delta) {
                    if (this.enabled === false || scope.firstxyrecieved === false)
                        return;
                    if (this.heightSpeed) {
                        if (scope.vectorup.z == 1) {
                            var z = THREE.Math.clamp(this.object.position.z, this.heightMin, this.heightMax);
                            var heightDelta = z - this.heightMin;
                        }
                        else if (scope.vectorup.y == 1) {
                            var y = THREE.MathUtils.clamp(this.object.position.y, this.heightMin, this.heightMax);
                            heightDelta = y - this.heightMin;
                        }
                        else {
                            z = THREE.Math.clamp(this.object.position.z, this.heightMin, this.heightMax);
                            heightDelta = z - this.heightMin;
                        }
                        this.autoSpeedFactor = delta * (heightDelta * this.heightCoef);
                    }
                    else {
                        this.autoSpeedFactor = 0.0;
                    }
                    
                    var actualMoveSpeed = delta * this.movementSpeed;

                    if (this.moveForward || (this.autoForward && !this.moveBackward))
                        this.object.translateZ(-(actualMoveSpeed + this.autoSpeedFactor));
                    if (this.moveBackward)
                        this.object.translateZ(actualMoveSpeed);
                    if (this.moveLeft)
                        this.object.translateX(-actualMoveSpeed);
                    if (this.moveRight)
                        this.object.translateX(actualMoveSpeed);
                    if (this.moveUp)
                        this.object.translateY(actualMoveSpeed);
                    if (this.moveDown)
                        this.object.translateY(-actualMoveSpeed);
                    
                    
                    var actualLookSpeed = delta * this.lookSpeed;
                    
                    
                    if (!this.activeLook) {
                        actualLookSpeed = 0;
                    }
                    var verticalLookRatio = 1;
                    if (scope.constrainVertical) {
                        verticalLookRatio = Math.PI / (scope.verticalMax - scope.verticalMin);
                    }
                    scope.lon -= scope.mouseX * actualLookSpeed;
                    if (scope.lookVertical)
                        scope.lat -= scope.mouseY * actualLookSpeed * verticalLookRatio;
                    scope.lat = Math.max(-85, Math.min(85, scope.lat));
                    scope.phi = THREE.Math.degToRad(90 - scope.lat);
                    //this.phi = THREE.Math.degToRad(  this.lat );
                    scope.theta = THREE.Math.degToRad(scope.lon);
                    if (scope.constrainVertical) {
                        scope.phi = THREE.Math.mapLinear(scope.phi, 0, Math.PI, scope.verticalMin, scope.verticalMax);
                    }
                    var targetPosition = scope.target;
                    var position = scope.object.position;
                    //targetPosition.y = position.y + 100 * Math.cos( this.phi );
                    //targetPosition.z = position.z + 100 * Math.sin( this.phi ) * Math.sin( this.theta );
                    //targetPosition.y = position.y + 100 * Math.sin( this.phi ) * Math.sin( this.theta );
                    //targetPosition.z = position.z + 100 * Math.cos( this.phi );
                    if (scope.vectorup.z == 1) {
                        targetPosition.x = position.x + 100 * (Math.sin(scope.phi) * Math.cos(scope.theta));
                        targetPosition.y = position.y + 100 * Math.sin(scope.phi) * Math.sin(scope.theta);
                        targetPosition.z = position.z + 100 * Math.cos(scope.phi);
                    }
                    else if (scope.vectorup.y == 1) {
                        targetPosition.x = -(position.x + 100 * (Math.sin(scope.phi) * Math.cos(scope.theta)));
                        targetPosition.y = position.y + 100 * Math.cos(scope.phi);
                        targetPosition.z = position.z + 100 * Math.sin(scope.phi) * Math.sin(scope.theta);
                    }
                    else {
                        targetPosition.x = -(position.x + 100 * (Math.sin(scope.phi) * Math.cos(scope.theta)));
                        targetPosition.y = position.y + 100 * Math.sin(scope.phi) * Math.sin(scope.theta);
                        targetPosition.z = position.z + 100 * Math.cos(scope.phi);
                    }
                    this.object.lookAt(targetPosition);

                    ticker += 1;

                    if (ticker > 2){
                        var camangle = {degrees : scope.lon, radians : scope.theta};

                        //imlement as callback to use with other rxcore instance.

                        //GUI_camerapos

                        if(RxCore_GUI_CameraPos != undefined){
                            RxCore_GUI_CameraPos.cameraPos(position, camangle, scope.modelCenter);
                        }

                        DocObj.setthumcamerapos(position, camangle);
                        ticker = 0;
                    }

                    //set thumbnail x,y and rotation from camera.
                    
                    //DocObj.setthumcamerapos(scope.object.position, scope.object.rotation);

                    scope.target0.copy(targetPosition);
                    scope.position0.copy(position);
                    //console.log(this.object.rotation.x, this.object.rotation.y, this.object.rotation.z);
                    //console.log(targetPosition);
                };
                this.handleResize();
            }
        }

        class Part3DObjectMulti {
                constructor(xmldata, index, parent, binary) {
                    var scope = this;
                    this.id = index;
                    this.selected = false;
                    this.hasmesh = false;
                    this.attributes = [];
                    this.format = DocObj.Format.substring(0, 3);
                    this.meshcolors = [];
                    this.meshrefs = [];
                    this.meshref = 0;
                    this.openmesh = false;
                    this.curTrset = 0;
                    this.isLastBlock = false;
                    this.isLoadComplete = false;
                    this.vcentroid = new THREE.Vector3();
                    this.minx = 1000000;
                    this.maxx = 0;
                    this.miny = 1000000;
                    this.maxy = 0;
                    this.minz = 1000000;
                    this.maxz = 0;
                    this.id = xmldata.id;
                    this.name = xmldata.name;
                    this.visible = xmldata.visible;
                    this.transparent = xmldata.transparent;
                    this.defaultcolor = decimalToHex(xmldata.color);
                    this.facecolor = decimalToHex(xmldata.color);
                    this.curColor = decimalToHex(xmldata.color);
                    this.tempvisible = (this.visible == 1);
                    this.tarray = [];
                    this.addline = function (lineobj, ncolor) {
                        var facecolor = decimalToHex(ncolor);
                        var x1 = lineobj.X1 - parent.x;
                        var y1 = lineobj.Y1 - parent.y;
                        var z1 = lineobj.Z1 - parent.z;
                        var x2 = lineobj.X2 - parent.x;
                        var y2 = lineobj.Y2 - parent.y;
                        var z2 = lineobj.Z2 - parent.z;
                        var linepos = new Float32Array([x1, y1, z1, x2, y2, z2]);
                        var linegeometry = new THREE.BufferGeometry();
                        //linegeometry.addAttribute('position', new THREE.BufferAttribute(linepos, 3));
                        linegeometry.setAttribute('position', new THREE.BufferAttribute(linepos, 3));
                        linegeometry.setIndex(new THREE.BufferAttribute(new Uint16Array([0, 1]), 1));
                        var material = new THREE.LineBasicMaterial({ color: facecolor, linewidth: 2.0 });
                        var linesegment = new THREE.LineSegments(linegeometry, material);
                        linesegment.scale.x = parent.scale;
                        linesegment.scale.y = parent.scale;
                        linesegment.scale.z = parent.scale;
                        parent.scene.add(linesegment);
                    };
                    this.appendtriangles = function (partdv, numt, id, ncolor) {
                        var curlength = scope.tarray[scope.curTrset].vectors.length;
                        var newlength = numt * 9;
                        scope.tarray[scope.curTrset].vectors = new Float32Array(curlength + newlength);
                        scope.tarray[scope.curTrset].dv.push(partdv);
                        scope.tarray[scope.curTrset].triangles.push(numt);
                    };
                    this.addtriangles = function (partdv, numt, id, facecolor) {
                        var dvarr = [];
                        var trianglearr = [];
                        dvarr.push(partdv);
                        trianglearr.push(numt);
                        var newgeometry = new THREE.BufferGeometry();
                        var vertices = new Float32Array(numt * 9);
                        var tobject = {
                            dv: dvarr,
                            triangles: trianglearr,
                            color: facecolor,
                            vindex: 0,
                            dvpos: 0,
                            geometry: newgeometry,
                            vectors: vertices
                        };
                        scope.tarray.push(tobject);
                        this.curTrset = scope.tarray.length - 1;
                    };
                    this.addpart = function (partdv, numt, id, ncolor, newblock) {
                        if (partdv) {
                            var facecolor = decimalToHex(ncolor);
                            //var mstatus = scope.getmeshstatus();
                            var dvarr = [];
                            if (scope.tarray.length > 0) {
                                if (scope.tarray[scope.curTrset].color == facecolor) {
                                    scope.facecolor = facecolor;
                                    scope.appendtriangles(partdv, numt, id, ncolor);
                                }
                                else {
                                    scope.addtriangles(partdv, numt, id, facecolor);
                                }
                            }
                            else {
                                this.curColor = facecolor;
                                scope.addtriangles(partdv, numt, id, facecolor);
                            }
                        }
                    };
                    this.processmesh = function () {
                        scope.curTrset = 0;
                        for (var i = 0; i < scope.tarray.length; i++) {
                            for (var j = 0; j < scope.tarray[i].triangles.length; j++) {
                                var dvsize = scope.tarray[i].triangles[j] * 9 * 4;
                                parsemeshbuffer(0, scope.tarray[i].dv[j], dvsize, i);
                            }
                        }
                        scope.addmesh();
                        bAnimateready = true;
                        /*if(scope.tarray[scope.curTrset] != undefined){
                            yieldingLoop((scope.tarray[scope.curTrset].triangles * 9 * 4), 1332, function() {
                                for(var i = 0;i< scope.tarray.length;i++){
                                    for(var j = 0;j<scope.tarray[i].triangles.length;j++){
                                        parsemeshbuffer(0, scope.tarray[i].dv[j], dvsize,i);
                                    }
                                }
            
            
                            }, function() {
                                scope.addmesh();
                                bAnimateready = true;
            
                            });
            
                        }*/
                        /*(function timer() {
            
                            bAnimateready = false;
            
                            if(scope.tarray[scope.curTrset] != undefined){
                                if (scope.tarray[scope.curTrset].dvpos < (scope.tarray[scope.curTrset].triangles * 9 * 4)) {
                                    setTimeout(timer, 10);
                                }
                            }
                        })();*/
                    };
                    this.resetvisible = function () {
                        scope.tempvisible = (scope.visible == 1);
                    };
                    function maxmin(x, y, z) {
                        if (scope.maxx < x) {
                            scope.maxx = x;
                        }
                        if (scope.maxy < y) {
                            scope.maxy = y;
                        }
                        if (scope.maxz < z) {
                            scope.maxz = z;
                        }
                        if (scope.minx > x) {
                            scope.minx = x;
                        }
                        if (scope.miny > y) {
                            scope.miny = y;
                        }
                        if (scope.minz > z) {
                            scope.minz = z;
                        }
                    }
                    function parsemeshbuffer(dvpos, dv, dvsize, tobjectnum) {
                        while (dvpos < dvsize) {
                            var x = dv.getFloat32(dvpos, true) - parent.x;
                            dvpos += 4;
                            var y = dv.getFloat32(dvpos, true) - parent.y;
                            dvpos += 4;
                            var z = dv.getFloat32(dvpos, true) - parent.z;
                            maxmin(x, y, z);
                            scope.tarray[tobjectnum].vectors[scope.tarray[tobjectnum].vindex] = x;
                            scope.tarray[tobjectnum].vectors[scope.tarray[tobjectnum].vindex + 1] = y;
                            scope.tarray[tobjectnum].vectors[scope.tarray[tobjectnum].vindex + 2] = z;
                            scope.tarray[tobjectnum].vindex += 3;
                            dvpos += 4;
                            x = dv.getFloat32(dvpos, true) - parent.x;
                            dvpos += 4;
                            y = dv.getFloat32(dvpos, true) - parent.y;
                            dvpos += 4;
                            z = dv.getFloat32(dvpos, true) - parent.z;
                            maxmin(x, y, z);
                            scope.tarray[tobjectnum].vectors[scope.tarray[tobjectnum].vindex] = x;
                            scope.tarray[tobjectnum].vectors[scope.tarray[tobjectnum].vindex + 1] = y;
                            scope.tarray[tobjectnum].vectors[scope.tarray[tobjectnum].vindex + 2] = z;
                            scope.tarray[tobjectnum].vindex += 3;
                            dvpos += 4;
                            x = dv.getFloat32(dvpos, true) - parent.x;
                            dvpos += 4;
                            y = dv.getFloat32(dvpos, true) - parent.y;
                            dvpos += 4;
                            z = dv.getFloat32(dvpos, true) - parent.z;
                            dvpos += 4;
                            maxmin(x, y, z);
                            scope.tarray[tobjectnum].vectors[scope.tarray[tobjectnum].vindex] = x;
                            scope.tarray[tobjectnum].vectors[scope.tarray[tobjectnum].vindex + 1] = y;
                            scope.tarray[tobjectnum].vectors[scope.tarray[tobjectnum].vindex + 2] = z;
                            scope.tarray[tobjectnum].vindex += 3;
                        }
                    }
                    this.addmesh = function () {
                        this.curTrset = 0;
                        while (scope.tarray.length != 0) {
                            scope.hasmesh = true;
                            var facecolor = scope.tarray[scope.curTrset].color;
                            scope.material = new THREE.MeshPhongMaterial({
                                color: facecolor,
                                side: THREE.DoubleSide,
                                //vertexColors: THREE.VertexColors,
                                shininess: 20,
                                shading: THREE.SmoothShading
                            });
                            if (scope.transparent != 0) {
                                scope.material.transparent = true;
                                scope.material.opacity = 0.3;
                            }
                            //scope.tarray[scope.curTrset].geometry.addAttribute('position', new THREE.BufferAttribute(scope.tarray[scope.curTrset].vectors, 3));
                            scope.tarray[scope.curTrset].geometry.setAttribute('position', new THREE.BufferAttribute(scope.tarray[scope.curTrset].vectors, 3));
                            //scope.tarray[scope.curTrset].geometry.computeFaceNormals();
                            scope.tarray[scope.curTrset].geometry.computeVertexNormals();
                            scope.tarray[scope.curTrset].geometry.computeBoundingSphere();
                            scope.center = scope.tarray[scope.curTrset].geometry.boundingSphere.center.clone();
                            scope.vcentroid.set(scope.minx, scope.miny, scope.minz);
                            var maxvector = new THREE.Vector3((scope.maxx - scope.minx) * 0.5, (scope.maxy - scope.miny) * 0.5, (scope.maxz - scope.minz) * 0.5);
                            scope.vcentroid.add(maxvector);
                            var partmesh = new THREE.Mesh(scope.tarray[scope.curTrset].geometry, scope.material);
                            partmesh.name = scope.id;
                            partmesh.scale.x = parent.scale;
                            partmesh.scale.y = parent.scale;
                            partmesh.scale.z = parent.scale;
                            if (this.visible == 0) {
                                partmesh.visible = false;
                                //partmesh.visible = false;
                                scope.tempvisible = false;
                            }
                            parent.scene.add(partmesh);
                            scope.tarray.shift();
                        }
                        scope.isLoadComplete = true;
                        if (scope.isLastBlock) {
                            parent.blockreadComplete();
                        }
                    };
                    this.setlastblock = function () {
                        scope.isLastBlock = true;
                        if (scope.isLoadComplete || !scope.hasmesh) {
                            parent.blockreadComplete();
                        }
                    };
                    this.addAtrib = function (attname, attvalue) {
                        var attobj = {
                            name: attname,
                            value: attvalue
                        };
                        this.attributes.push(attobj);
                        var spacecheck = attname.substring(0, 8);
                        if (spacecheck == 'IfcSpace') {
                            this.isSpace = true;
                        }
                    };
                    function yieldingLoop(count, chunksize, callback, finished) {
                        var i = 0;
                        (function chunk() {
                            var end = Math.min(i + chunksize, count);
                            for (; i < end; ++i) {
                                callback.call(null, i);
                            }
                            if (i < count) {
                                setTimeout(chunk, 0);
                            }
                            else {
                                finished.call(null);
                            }
                        })();
                    }
                }
        }

        class Part3DObject {
            constructor(xmldata, index, parent, binary) {
                var scope = this;
                this.id = index;
                this.selected = false;
                this.isSpace = false;
                this.ifctype = 0;
                this.hasmesh = false;
                this.attributes = [];
                this.format = DocObj.Format.substring(0, 3);
                this.meshcolors = [];
                this.meshrefs = [];
                this.meshref = 0;
                this.openmesh = false;
                this.curTrset = 0;
                this.isLastBlock = false;
                this.isLoadComplete = false;
                this.centroid = new THREE.Vector3();
                this.vcentroid = new THREE.Vector3();
                this.minx = 1000000;
                this.maxx = 0;
                this.miny = 1000000;
                this.maxy = 0;
                this.minz = 1000000;
                this.maxz = 0;
                this.tempz = 0;
                this.id = xmldata.id;
                this.name = xmldata.name;
                this.visible = xmldata.visible;
                this.transparent = xmldata.transparent;
                this.defaultcolor = decimalToHex(xmldata.color);
                this.facecolor = decimalToHex(xmldata.color);
                this.tempvisible = (this.visible == 1);
                this.tarray = [];
                /*
                    var opartobj ={
                    id : blockid,
                    visible : nvisbile,
                    transparent : ntransparent,
                    color : ncolor,
                    name : szblockname,
                    };
                    */
                this.addline = function (lineobj, ncolor) {
                    var facecolor = decimalToHex(ncolor);
                    var x1 = lineobj.X1 - parent.x;
                    var y1 = lineobj.Y1 - parent.y;
                    var z1 = lineobj.Z1 - parent.z;
                    var x2 = lineobj.X2 - parent.x;
                    var y2 = lineobj.Y2 - parent.y;
                    var z2 = lineobj.Z2 - parent.z;
                    var linepos = new Float32Array([x1, y1, z1, x2, y2, z2]);
                    var linegeometry = new THREE.BufferGeometry();
                    //linegeometry.addAttribute('position', new THREE.BufferAttribute(linepos, 3));
                    linegeometry.setAttribute('position', new THREE.BufferAttribute(linepos, 3));
                    linegeometry.setIndex(new THREE.BufferAttribute(new Uint16Array([0, 1]), 1));
                    var material = new THREE.LineBasicMaterial({ color: facecolor, linewidth: 2.0 });
                    var linesegment = new THREE.LineSegments(linegeometry, material);
                    linesegment.scale.x = parent.scale;
                    linesegment.scale.y = parent.scale;
                    linesegment.scale.z = parent.scale;
                    parent.scene.add(linesegment);
                };
                this.addpart = function (partdv, numt, id, ncolor, newblock) {
                    if (partdv) {
                        var facecolor = decimalToHex(ncolor);
                        var mstatus = scope.getmeshstatus();
                        var newgeometry = new THREE.BufferGeometry();
                        var vertices = new Float32Array(numt * 9);
                        var tobject = {
                            dv: partdv,
                            triangles: numt,
                            color: facecolor,
                            vindex: 0,
                            dvpos: 0,
                            geometry: newgeometry,
                            vectors: vertices
                        };
                        scope.tarray.push(tobject);
                        /*if (!mstatus.meshexist) {
                            scope.createmesh(partdv, numt, facecolor, id);
                        }else{
                            if (facecolor == mstatus.color) {
                                scope.appendmesh(partdv, numt, facecolor, id);
                            }else{
                                scope.addmesh();
                                scope.createmesh(partdv, numt, facecolor, id);
                            }
                        }*/
                    }
                };
                this.addAtrib = function (attname, attvalue) {
                    var attobj = {
                        name: attname,
                        value: attvalue
                    };
                    this.attributes.push(attobj);
                    var spacecheck = attname.substring(0, 8);
                    if (spacecheck == 'IfcSpace') {
                        this.isSpace = true;
                    }
                };
                this.getmeshstatus = function () {
                    return {
                        meshexist: this.hasmesh,
                        color: this.facecolor,
                        open: this.openmesh
                    };
                };
                this.appendmesh = function (binarydata, numt, facecolor, blockid) {
                    var dvsize = numt * 9 * 4;
                    var dvpos = 0;
                    //var vindex = 0;
                    //var newgeometry =  new THREE.Geometry();
                    //newgeometry = pushmesh(dvpos,dvsize,vindex,binarydata,newgeometry);
                    /*while (dvpos < dvsize) {
                        //dvpos += 4;
                        var x = binarydata.getFloat32(dvpos, true) - parent.x;
                        dvpos += 4;
                        var y = binarydata.getFloat32(dvpos, true) - parent.y;
                        dvpos += 4;
                        var z = binarydata.getFloat32(dvpos, true) - parent.z;
        
                        newgeometry.vertices.push(new THREE.Vector3(x, y, z));
                        dvpos += 4;
                        x = binarydata.getFloat32(dvpos, true) - parent.x;
                        dvpos += 4;
                        y = binarydata.getFloat32(dvpos, true) - parent.y;
                        dvpos += 4;
                        z = binarydata.getFloat32(dvpos, true) - parent.z;
                        newgeometry.vertices.push(new THREE.Vector3(x, y, z));
                        dvpos += 4;
                        x = binarydata.getFloat32(dvpos, true) - parent.x;
                        dvpos += 4;
                        y = binarydata.getFloat32(dvpos, true) - parent.y;
                        dvpos += 4;
                        z = binarydata.getFloat32(dvpos, true) - parent.z;
                        dvpos += 4;
                        newgeometry.vertices.push(new THREE.Vector3(x, y, z));
        
        
                        newgeometry.faces.push(new THREE.Face3(vindex, vindex + 1, vindex + 2));
                        vindex += 3;
        
                    }*/
                };
                this.createmesh = function (binarydata, numt, facecolor, blockid) {
                    /*
                        0	4	LONG	Object ID	9
                        4	4	LONG	NumTriangles		Number of triangles
                        8	4	FLOAT	X1		Triangle coordinates
                        12	4	FLOAT	Y1
                        16	4	FLOAT	Z1
                        20	4	FLOAT	X2
                        24	4	FLOAT	Y2
                        28	4	FLOAT	Z2
                        32	4	FLOAT	X3
                        36	4	FLOAT	Y3
                        40	4	FLOAT	Z3
                        44	4	FLOAT	X[N]
                        */
                    this.openmesh = true;
                    //newgeometry = new THREE.Geometry();
                    this.hasmesh = true;
                    this.facecolor = facecolor;
                    this.meshcolors.push(facecolor);
                    this.meshref++;
                    this.meshrefs.push(this.meshref);
                    if (this.format == 'IFC') {
                        this.material = new THREE.MeshPhongMaterial({
                            color: this.facecolor,
                            side: THREE.DoubleSide,
                            shininess: 5,
                            shading: THREE.SmoothShading
                        });
                    }
                    else {
                        this.material = new THREE.MeshPhongMaterial({
                            color: this.facecolor,
                            side: THREE.DoubleSide,
                            shininess: 40,
                            shading: THREE.SmoothShading
                        });
                        this.material.metal = true;
                    }
                    if (this.transparent != 0) {
                        this.material.transparent = true;
                        this.material.opacity = 0.3;
                    }
                    var dvsize = numt * 9 * 4;
                    var dvpos = 0;
                    //vindex = 0;
                    //newgeometry = pushmesh(dvpos,dvsize,vindex,binarydata,newgeometry);
                    /*while (dvpos < dvsize) {
                        //dvpos += 4;
                        var x = binarydata.getFloat32(dvpos, true) - parent.x;
                        dvpos += 4;
                        var y = binarydata.getFloat32(dvpos, true) - parent.y;
                        dvpos += 4;
                        var z = binarydata.getFloat32(dvpos, true) - parent.z;
        
                        newgeometry.vertices.push(new THREE.Vector3(x, y, z));
                        dvpos += 4;
                        x = binarydata.getFloat32(dvpos, true) - parent.x;
                        dvpos += 4;
                        y = binarydata.getFloat32(dvpos, true) - parent.y;
                        dvpos += 4;
                        z = binarydata.getFloat32(dvpos, true) - parent.z;
                        newgeometry.vertices.push(new THREE.Vector3(x, y, z));
                        dvpos += 4;
                        x = binarydata.getFloat32(dvpos, true) - parent.x;
                        dvpos += 4;
                        y = binarydata.getFloat32(dvpos, true) - parent.y;
                        dvpos += 4;
                        z = binarydata.getFloat32(dvpos, true) - parent.z;
                        dvpos += 4;
                        newgeometry.vertices.push(new THREE.Vector3(x, y, z));
        
        
                        newgeometry.faces.push(new THREE.Face3(vindex, vindex + 1, vindex + 2));
                        vindex += 3;
        
                    }*/
                    //return newgeometry;
                    //parent.scene.updateMatrix();
                    //renderer.render(parent.scene, camera,false);
                };
                this.processmesh = function () {
                    scope.curTrset = 0;
                    if (scope.tarray[scope.curTrset] != undefined) {
                        yieldingLoop((scope.tarray[scope.curTrset].triangles * 9 * 4), 1332, function () {
                            bAnimateready = false;
                            parsemeshbuffer();
                        }, function () {
                            scope.addmesh();
                            bAnimateready = true;
                            //addmesh//document.getElementById('done').innerHTML = 'done!';
                        });
                    }
                    //var t = document.getElementById('timer');
                    (function timer() {
                        //t.innerHTML = j;
                        bAnimateready = true;
                        //parsemesh(scope.tarray[scope.curTrset]);
                        if (scope.tarray[scope.curTrset] != undefined) {
                            if (scope.tarray[scope.curTrset].dvpos < (scope.tarray[scope.curTrset].triangles * 9 * 4)) {
                                setTimeout(timer, 10);
                            }
                        }
                    })();
                };
                this.addmesh = function () {
                    /*
                        0	4	LONG	Object ID	9
                        4	4	LONG	NumTriangles		Number of triangles
                        8	4	FLOAT	X1		Triangle coordinates
                        12	4	FLOAT	Y1
                        16	4	FLOAT	Z1
                        20	4	FLOAT	X2
                        24	4	FLOAT	Y2
                        28	4	FLOAT	Z2
                        32	4	FLOAT	X3
                        36	4	FLOAT	Y3
                        40	4	FLOAT	Z3
                        44	4	FLOAT	X[N]
                        */
                    this.openmesh = true;
                    this.hasmesh = true;
                    //newgeometry = new THREE.Geometry();
                    this.facecolor = scope.tarray[scope.curTrset].color;
                    this.meshcolors.push(scope.tarray[scope.curTrset].color);
                    this.meshref++;
                    this.meshrefs.push(this.meshref);
                    if (this.format == 'IFC') {
                        this.material = new THREE.MeshPhongMaterial({
                            color: this.facecolor,
                            side: THREE.DoubleSide,
                            shininess: 5,
                            shading: THREE.SmoothShading,
                        });
                    }
                    else {
                        this.material = new THREE.MeshPhongMaterial({
                            color: this.facecolor,
                            side: THREE.DoubleSide,
                            shininess: 20,
                            shading: THREE.SmoothShading
                            //emissive: this.facecolor,
                            //emissiveIntensity : 0.5
                        });
                        //this.material.metal = true;
                        //console.log(this.facecolor);
                    }
                    if (this.transparent != 0) {
                        this.material.transparent = true;
                        this.material.opacity = 0.3;
                    }
                    //scope.tarray[scope.curTrset].geometry.addAttribute('position', new THREE.BufferAttribute(scope.tarray[scope.curTrset].vectors, 3));
                    scope.tarray[scope.curTrset].geometry.setAttribute('position', new THREE.BufferAttribute(scope.tarray[scope.curTrset].vectors, 3));
                    //scope.tarray[scope.curTrset].geometry.computeFaceNormals();
                    scope.tarray[scope.curTrset].geometry.computeVertexNormals();
                    //scope.tarray[scope.curTrset].geometry.computeBoundingSphere();
                    //scope.tarray[scope.curTrset].geometry.computeBoundingBox();
                    //scope.center = scope.tarray[scope.curTrset].geometry.boundingSphere.center.clone();
                    //scope.radius = scope.tarray[scope.curTrset].geometry.boundingSphere.radius;
                    //scope.boxmax = scope.tarray[scope.curTrset].geometry.boundingBox.max.clone();
                    //code for wireframe like material
                    //this.wireframe = THREE.SceneUtils.createMultiMaterialObject( newgeometry, materials );
                    var partmesh = new THREE.Mesh(scope.tarray[scope.curTrset].geometry, scope.material);
                    partmesh.name = scope.id;
                    partmesh.scale.x = parent.scale;
                    partmesh.scale.y = parent.scale;
                    partmesh.scale.z = parent.scale;
                    partmesh.geometry.computeBoundingSphere();
                    partmesh.geometry.computeBoundingBox();
                    if (this.visible == 0) {
                        partmesh.visible = false;
                        //partmesh.visible = false;
                        scope.tempvisible = false;
                    }
                    parent.scene.add(partmesh);
                    //partmesh.updateMatrix();
                    //var attribute = partmesh.geometry.attributes.position; // we want the position data
                    //var index = 1; // index is zero-based, so this the the 2nd vertex
                    //scope.centroid.fromAttribute( attribute, index ); // extract the x,y,z coordinates
                    //scope.centroid.applyMatrix4( partmesh.matrixWorld );
                    scope.center = partmesh.geometry.boundingSphere.center.clone();
                    scope.radius = partmesh.geometry.boundingSphere.radius;
                    scope.boxmax = partmesh.geometry.boundingBox.max.clone();
                    scope.vcentroid.set(scope.minx, scope.miny, scope.minz);
                    var maxvector = new THREE.Vector3((scope.maxx - scope.minx) * 0.5, (scope.maxy - scope.miny) * 0.5, (scope.maxz - scope.minz) * 0.5);
                    scope.vcentroid.add(maxvector);
                    //scope.worldpos = partmesh.worldToLocal(scope.centroid);
                    parent.blockadded();
                    scope.isLoadComplete = true;
                    if (scope.isLastBlock) {
                        parent.blockreadComplete();
                    }
                    //var currentcolor = scope.tarray[scope.curTrset].color;
                    if (scope.tarray.length > 1) {
                        //if(currentcolor == scope.tarray[scope.curTrset+1].color){
                        //scope.tarray[scope.curTrset+1].geometry = scope.tarray[scope.curTrset].geometry;
                        //}
                    }
                    scope.tarray.shift();
                    if (scope.tarray.length > 0) {
                        scope.processmesh();
                    }
                };
                this.setlastblock = function () {
                    scope.isLastBlock = true;
                    if (scope.isLoadComplete || !scope.hasmesh) {
                        parent.blockreadComplete();
                    }
                };
                this.resetvisible = function () {
                    scope.tempvisible = (scope.visible == 1);
                };
                function maxmin(x, y, z) {
                    if (scope.maxx < x) {
                        scope.maxx = x;
                    }
                    if (scope.maxy < y) {
                        scope.maxy = y;
                    }
                    if (scope.maxz < z) {
                        scope.maxz = z;
                    }
                    if (scope.minx > x) {
                        scope.minx = x;
                    }
                    if (scope.miny > y) {
                        scope.miny = y;
                    }
                    if (scope.minz > z) {
                        scope.minz = z;
                    }
                }
                function parsemeshbuffer() {
                    var counter = 0;
                    var meshobj = scope.tarray[scope.curTrset];
                    if (meshobj.dvpos < (meshobj.triangles * 9 * 4)) {
                        var x = meshobj.dv.getFloat32(meshobj.dvpos, true) - parent.x;
                        meshobj.dvpos += 4;
                        var y = meshobj.dv.getFloat32(meshobj.dvpos, true) - parent.y;
                        meshobj.dvpos += 4;
                        var z = meshobj.dv.getFloat32(meshobj.dvpos, true) - parent.z;
                        maxmin(x, y, z);
                        meshobj.vectors[meshobj.vindex] = x;
                        meshobj.vectors[meshobj.vindex + 1] = y;
                        meshobj.vectors[meshobj.vindex + 2] = z;
                        meshobj.vindex += 3;
                        //meshobj.geometry.vertices.push(new THREE.Vector3(x, y, z));
                        meshobj.dvpos += 4;
                        x = meshobj.dv.getFloat32(meshobj.dvpos, true) - parent.x;
                        meshobj.dvpos += 4;
                        y = meshobj.dv.getFloat32(meshobj.dvpos, true) - parent.y;
                        meshobj.dvpos += 4;
                        z = meshobj.dv.getFloat32(meshobj.dvpos, true) - parent.z;
                        maxmin(x, y, z);
                        meshobj.vectors[meshobj.vindex] = x;
                        meshobj.vectors[meshobj.vindex + 1] = y;
                        meshobj.vectors[meshobj.vindex + 2] = z;
                        meshobj.vindex += 3;
                        //meshobj.geometry.vertices.push(new THREE.Vector3(x, y, z));
                        meshobj.dvpos += 4;
                        x = meshobj.dv.getFloat32(meshobj.dvpos, true) - parent.x;
                        meshobj.dvpos += 4;
                        y = meshobj.dv.getFloat32(meshobj.dvpos, true) - parent.y;
                        meshobj.dvpos += 4;
                        z = meshobj.dv.getFloat32(meshobj.dvpos, true) - parent.z;
                        meshobj.dvpos += 4;
                        maxmin(x, y, z);
                        //meshobj.geometry.vertices.push(new THREE.Vector3(x, y, z));            }
                        meshobj.vectors[meshobj.vindex] = x;
                        meshobj.vectors[meshobj.vindex + 1] = y;
                        meshobj.vectors[meshobj.vindex + 2] = z;
                        meshobj.vindex += 3;
                    }
                    scope.tarray[scope.curTrset] = meshobj;
                    return meshobj.dvpos;
                }
                function yieldingLoop(count, chunksize, callback, finished) {
                    var i = 0;
                    (function chunk() {
                        var end = Math.min(i + chunksize, count);
                        for (; i < end; ++i) {
                            callback.call(null, i);
                        }
                        if (i < count) {
                            setTimeout(chunk, 0);
                        }
                        else {
                            finished.call(null);
                        }
                    })();
                }
                //var j = 0; //scope.tarray[scope.curTrset].dvpos
                //var count = 1e6; //total meshobj.triangles * 9 * 4
            }
        }

        class Block3DObject {
            constructor(name, index, state, level, listposition) {
                var block3Dref = this;
                this.name = name;
                this.index = index;
                this.state = state;
                this.level = level;
                this.position = listposition;
                this.selected = false;
                this.setSelected = function (selected) {
                    block3Dref.selected = selected;
                };
            }
        }

        class markupLoader {
            constructor(URL, parent) {
                var markupref = this;
                this.parent = parent;
                this.scene = null;
                var loader = new THREE.ObjectLoader();
                this.markuploaded = function (markup) {
                    markupref.parent.setMarkupScene(markup);
                };
                this.markupProgress = function (xhr) {
                    /*if (RxCore_GUI_Upload != undefined) {
                        RxCore_GUI_Upload.setUpload(Math.round(xhr.loaded / xhr.total * 100));
                    }*/
                    console.log((xhr.loaded / xhr.total * 100) + '% loaded');
                };
                this.markupError = function (error) {
                    console.log('An error happened ', error);
                };
                loader.load(URL, markupref.markuploaded, markupref.markupProgress, markupref.markupError);
            }
        }

        class glTFLoader {
            constructor(URL, parent, addscene) {
                //import { GLTFLoader } from 'three/GLTFLoader.js';
                var gltfref = this;
                this.parent = parent;
                this.scene = null;
                var baddscene = addscene;
                var loader = new THREE.GLTFLoader();
                var buseoptimizer = false;

                if (buseoptimizer){
                    loader.setMeshoptDecoder( MeshoptDecoder );
                }


                this.addScene = function(URL, addscene){
                    baddscene = addscene;
                    suspendrender = true;
                    loader.load(URL, gltfref.gltfloaded, gltfref.gltfprogress, gltfref.gltferror);    
                };

                this.gltfloaded = function (gltf) {
                    //gltfref.scene = gltf.scene;
                    renderer.domElement.style.visibility = 'visible';

                    suspendrender = false;

                    if(baddscene){
                        gltfref.parent.setAddtoScene(gltf.scene);
                    }else{
                        gltfref.parent.setScene(gltf.scene);
                    }

                    if (RxCore_GUI_Upload != undefined) {
                        RxCore_GUI_Upload.setUpload("hide");
                    }
                    //gltfref.parent.blockreadComplete();
                };
                this.gltfprogress = function (xhr) {
                    if (RxCore_GUI_Upload != undefined) {
                        RxCore_GUI_Upload.setUpload(Math.round(xhr.loaded / xhr.total * 100));
                    }
                    //console.log( ( xhr.loaded / xhr.total * 100 ) + '% loaded' );
                };
                this.gltferror = function (error) {
                    //console.log('An error happened');
                    console.log('An error happened', error);
                };

                if(buseAuth){

                    var auth = getAuth();

                    if (auth.authset){
                        //xhr.setRequestHeader('Authorization', auth.authString + auth.baseuserstring);
                        var requestHeader = { Authorization: auth.authString + auth.baseuserstring };
                        //
                        
                    }
                    
                }else{
                    requestHeader = undefined;
                }

                loader.load(URL, gltfref.gltfloaded, gltfref.gltfprogress, gltfref.gltferror, requestHeader);

                //loader.load(URL, gltfref.gltfloaded, gltfref.gltfprogress, gltfref.gltferror);
            }
        }

      class Vector3DGLTFObject {
            constructor(URL, parent) {
                var vector3Dref = this;
                var pageobject = parent;
                this.countparts = 0;
                this.selectedpart = -1;
                this.clippingOn = false;
                this.box = null;
                this.boxSize = null;
                this.boxCenter = null;


                this.navbox = null;
                this.navboxSize = null;
                this.navboxCenter = null;


                this.unit = 1;
                this.pointTreshold = 1;
                this.pointSize = 1;
                this.explodebox = null;
                this.explodeboxSize = null;
                this.explodeboxCenter = null;
                this.modeluuid = null;
                this.modelidx = null;
                this.markupidx = null;
                this.hasmarkup = false;
                this.markuplistknown = false;
                this.markuploaded = false;
                this.modelLoaded = false;
                this.orbitcampos = null;
                this.shadowspotLight = null;
                this.orbitcamtarget = null;
                this.effectComposer = null;
                this.renderPass = null;
                this.saoPass = null;
                this.startcampos = null;

                this.originalpos = new THREE.Vector3();
                this.navboxmin = new THREE.Vector3();
                this.navboxmax = new THREE.Vector3();

                this.geometryutil = THREE.BufferGeometryUtils;

                parent.clock = new THREE.Clock();
                this.gltfobject = new glTFLoader(URL, vector3Dref, false);
                this.scene = new THREE.Scene();
                this.navscene = new THREE.Scene();

                this.markupscene = new THREE.Scene();
                this.markupuuid = this.markupscene.uuid;
                this.floorplanTextures = [];
                this.filterAttributelist = [];
                this.addedScenes = [];
                this.MarkupPoints = [];
                this.storeys = [];
                this.temp2DPoints = [];
                this.addedMetaData = [];
                this.cameraSaves = [];
                

                /*const geometry = new THREE.BoxGeometry( 1, 1, 1 );
                const material = new THREE.MeshBasicMaterial( {color: 0x00ff00} );
                const cube = new THREE.Mesh( geometry, material );
                scene.add( cube );*/

                this.viewattributes = [];

                this.cur3Dview = null;
                this.active3DView = false;
                this.topView = null;

                this.isbim = true;

                this.initialwalkthroughPosSet = false;

                this.curfloor = 0;
                this.floorelevation = {min : null, max : null, index : 0};
                
                this.curwidth = 0;
                this.curdepth = 0;
                this.curheight = 0;
                this.curminx = 0;
                this.curmaxx = 0;
                this.curminy = 0;
                this.curmaxy = 0;
                this.curminz = 0;
                this.curmaxz = 0;

                this.font = new FontObject("Helvetica", 12, false, false);

                var szShortformat = DocObj.Format.substring(0, 3);

                if (DocObj.Format == "Autodesk Revit"){
                    szShortformat = 'RVT';
                }

                

                var markuppositions = [];
                const MESHNameGroup = 24;
                const MESHName = 22;
                const RVTMESHName = 6;

                var birseyeEuler = new THREE.Euler( 0, 0, 0, 'ZYX' );
                var birdseyeCampos = new THREE.Vector3();

                
                const directionalLight_1 = new THREE.DirectionalLight(0xffffff, 2.5);
                const directionalLight_2 = new THREE.DirectionalLight(0xffffff, 1);
                const directionalLight_3 = new THREE.DirectionalLight(0xffffff, 0.05);
                
                //var directionalLight_3 = new THREE.DirectionalLight(0xffffff, 0.05);

                const light = new THREE.HemisphereLight( 0xffffff, 0xffffff, 1.5 );

                const hemilight = new THREE.HemisphereLight( 0xffffff, 0x404040, 1 );

                const amblight = new THREE.AmbientLight(0x404040, 4);

                const navlight = new THREE.HemisphereLight( 0xffffff, 0xffffff, 1 );


                if (szShortformat == 'IFC' || szShortformat == 'RVT') {
                    //this.scene.up.set(0, 0, 1);
                    vector3Dref.isbim = true;
                }
                else {
                    //this.scene.up.set(0, 1, 0);
                    vector3Dref.isbim = false;
                }

                
                //directionalLight.intensity = 0.5;
                directionalLight_1.position.set(-500, -500, 500);
                //directionalLight_1.castShadow = true;

                //directionalLight_1.shadow.mapSize.width = 256;  // default / 2
                //directionalLight_1.shadow.mapSize.height = 256; // default / 2
                //directionalLight_1.shadow.camera.near = 0.5;    // default
                //directionalLight_1.shadow.camera.far = 10000;     // default
                //directionalLight_1.shadow.radius = 10;

                //directionalLight_1.shadow.bias = -0.0001;

                
                if (vector3Dref.isbim){
                    directionalLight_1.position.set(-500, -500, 500);
                }else{
                    directionalLight_1.position.set(-500, 500, -500);
                    directionalLight_2.position.set(500, 500, -500);
                    this.scene.add(directionalLight_2);
                }


                this.scene.add(directionalLight_1);



                //var helper = new THREE.CameraHelper( directionalLight_1.shadow.camera );
                //this.scene.add( helper );

                
                //var directionalLight_2 = new THREE.DirectionalLight(0xffffff, 1);
                //directionalLight.intensity = 0.3;
                //directionalLight_2.position.set(1000, 1000, 1000);
                //var helper_2 = new THREE.DirectionalLightHelper(directionalLight_2, 1);
                //this.scene.add(directionalLight_2);
                //this.scene.add( helper_2 );
                
                //directionalLight_3.position.set(0, 0, -1000);


                if (vector3Dref.isbim){
                    directionalLight_3.position.set(0, 0, -1000);
                }else{
                    directionalLight_3.position.set(0, -1000, 0);
                }


                this.scene.add(directionalLight_3);


                if (vector3Dref.isbim){
                    parent.camera = new THREE.PerspectiveCamera(70, canvasowidth / canvasoheight, 0.1, 10000);
                }else{
                    parent.camera = new THREE.PerspectiveCamera(45, canvasowidth / canvasoheight, 0.1, 10000);
                }
                

                //parent.navcamera = new THREE.PerspectiveCamera(45, navthreesize.width / navthreesize.height, 0.1, 10000);

                //parent.navcamera = new THREE.OrthographicCamera(navthreesize.width / - 2, navthreesize.width / 2, navthreesize.height / 2, navthreesize.height / - 2, 0.1, 10000);

                

                //width / - 2, width / 2, height / 2, height / - 2, 1, 1000 

                

                


                this.explodefactor = 0;
                this.pxplodefactor = 0;
                this.sceneoffset = undefined;
                this.sceneoffsetset = false;

                this.partlist = [];
                this.blocklist = [];
                this.navlist = [];

                this.blocklistquick = [];

                this.globalPlaneX = new THREE.Plane(new THREE.Vector3(1, 0, 0), 0);
                this.globalPlaneY = new THREE.Plane(new THREE.Vector3(0, 1, 0), 0);
                this.globalPlaneZ = new THREE.Plane(new THREE.Vector3(0, 0, -1), 0);
                
                //this.helperplaneX = new THREE.PlaneHelper( this.globalPlaneX, 1, 0xffff00 );
                //this.helperplaneY = new THREE.PlaneHelper( this.globalPlaneY, 1, 0xffff00 );
                //this.helperplaneZ = new THREE.PlaneHelper( this.globalPlaneZ, 1, 0xffff00 );

                this.globalPlanes = [this.globalPlaneX, this.globalPlaneY, this.globalPlaneZ],
                    Empty = Object.freeze([]);
                this.xPlaneconst = null;
                this.xPlaneEnd = null;
                this.xPlaneStart = null;
                this.yPlaneconst = null;
                this.yPlaneEnd = null;
                this.yPlaneStart = null;
                this.zPlaneconst = null;
                this.zPlaneEnd = null;
                this.zPlaneStart = null;
                this.markupLoad = function (URL) {
                    //var loader = new THREE.ObjectLoader();

                    
                    this.markupobject = new markupLoader(URL, vector3Dref);

                    
                };

                this.setOrientationScene = function(upvector){


                    vector3Dref.navscene.up.set(upvector.x, upvector.y, upvector.z);
                    parent.navcamera.up.set(upvector.x, upvector.y, upvector.z);

                    //const geometry = new THREE.BoxGeometry( 100, 100, 100 );
                    const geometry = new THREE.BoxGeometry( 1, 1, 1 );
                    const geometrydouble = new THREE.BoxGeometry( 2, 2, 2 );

                    const edges = new THREE.EdgesGeometry( geometry );
                    const line = new THREE.LineSegments( edges, new THREE.LineBasicMaterial( { color: 0xffffff } ) );
                    vector3Dref.navscene.add( line );

                    var toptexture = new THREE.TextureLoader().load( 'images/top.png' );
                    var fronttexture = new THREE.TextureLoader().load( 'images/front.png' );
                    var righttexture = new THREE.TextureLoader().load( 'images/right.png' );
                    var backtexture = new THREE.TextureLoader().load( 'images/back.png' );
                    var lefttexture = new THREE.TextureLoader().load( 'images/left.png' );
                    var bottomtexture = new THREE.TextureLoader().load( 'images/bottom.png' );

                    //fronttexture.rotation = -Math.PI * 0.5;

                    var textureCube3 = [

                        new THREE.MeshStandardMaterial( { color: 0xffffff, map: righttexture } ),
                        new THREE.MeshStandardMaterial( { color: 0xffffff, map: lefttexture } ),
                        new THREE.MeshStandardMaterial( { color: 0xffffff, map: backtexture } ),
                        new THREE.MeshStandardMaterial( { color: 0xffffff, map: fronttexture } ),
                        new THREE.MeshStandardMaterial( { color: 0xffffff, map: toptexture } ),
                        new THREE.MeshStandardMaterial( { color: 0xffffff, map: bottomtexture } )
            
                    ];
            
                    
                    //const material = new THREE.MeshBasicMaterial( {color: 0xff0000} );

                    const material = new THREE.MeshStandardMaterial( {color: 0xff0000} );
                    //const material = new THREE.LineBasicMaterial( {color: 0xff0000} );
                    
                    const doublecube = new THREE.Mesh( geometrydouble, material );

                    //new THREE.Mesh(geometry, textureCube3);


                    const cube = new THREE.Mesh( geometry, textureCube3 );


                    var cmpsmaterial = new THREE.MeshBasicMaterial({color: 0xD3D3D3, opacity : 0.8});
                    var circleGeometry = new THREE.CircleGeometry(1, 32);

                    var circle = new THREE.Mesh(circleGeometry, cmpsmaterial);

                    //circle.position.set()

                    circle.position.x = 0;
                    circle.position.y = 0;
                    circle.position.z = -0.8;
            
            
                    vector3Dref.navscene.add( cube );
                    vector3Dref.navscene.add( circle );

                    var light = new THREE.HemisphereLight( 0xffffff, 0xffffff, 1 );
                    vector3Dref.navscene.add( light );


                    //this.navboxmin = new THREE.Vector3();
                    //this.navboxmax = new THREE.Vector3();
    


                    vector3Dref.navbox = new THREE.Box3().setFromObject(doublecube);
                    vector3Dref.navboxSize = vector3Dref.navbox.getSize(new THREE.Vector3()).length();
                    vector3Dref.navboxCenter = vector3Dref.navbox.getCenter(new THREE.Vector3());

                    vector3Dref.framenavArea();    

                };
               

                this.setMarkupScene = function (markupscene) {
                    vector3Dref.markupscene = markupscene;

                    vector3Dref.markupuuid = vector3Dref.markupscene.uuid;
                    vector3Dref.scene.add(vector3Dref.markupscene);

                    vector3Dref.markupidx = null;


                    for(var i=0;i < vector3Dref.markupscene.children.length; i++){
                        if(vector3Dref.markupscene.children[i].userData.type == "camera"){
                            vector3Dref.markupscene.children[i].visible = false;
                        }

                        if(vector3Dref.markupscene.children[i].userData.name == "resetView"){

                            vector3Dref.markupscene.children[i].geometry.dispose();
                            vector3Dref.markupscene.children[i].material.dispose();

                            vector3Dref.markupscene.remove(vector3Dref.markupscene.children[i]);

                            //delobjects.push(vector3Dref.markupscene.children[i]);

                            //remove from list
                            //vector3Dref.markupscene.children[i].visible = false;
                        }

                    }


                    
                    
                    vector3Dref.createsymbolTextures();

                    vector3Dref.markuploaded = true;

                    vector3Dref.load3Dmarkupcheck();

                    if (RxCore_GUI_3DMarkupList != undefined) {
                        RxCore_GUI_3DMarkupList.setMarkupList(vector3Dref.markupscene.children, DocObj.bActive);
                    }
                    
                };
                
                //thispage.Vector3DPageObj.load3Dmarkupcheck();
                this.load3Dmarkupcheck = function(){

                    
                    var markuplistknown = parent.DocRef.getmarkupfilesread();
                    vector3Dref.hasmarkup = (parent.DocRef.getnummarkupfiles() > 0);

                    if(markuplistknown && vector3Dref.hasmarkup){
                    
                        if(vector3Dref.markuploaded){
                            if(vector3Dref.modelLoaded){
                                //callback
                                if (Rxcore_GUI_CanSaveTop3DView != undefined) {
                                    Rxcore_GUI_CanSaveTop3DView.readComplete();
                                }
    
                            }
    
                        }

                        //RxCore_Show3DViewMarkers(false);
                        
                    }else if(markuplistknown){
                        if(vector3Dref.modelLoaded){
                            //callback
                            if (Rxcore_GUI_CanSaveTop3DView != undefined) {
                                Rxcore_GUI_CanSaveTop3DView.readComplete();
                            }
        
                        }


                    }
                    //turn    

                };
                

                this.addToScene = function(GLBURL, JSONURL){


                    vector3Dref.gltfobject.addScene(GLBURL, true);

                    vector3Dref.addedMetaData.push({metasrc : JSONURL, added : false});

                    //this.gltfobject = new glTFLoader(URL, vector3Dref, false);
                    //vector3Dref.gltfobject = new glTFLoader(URL, vector3Dref, true);

                };

                this.setAddtoScene = function(gltfscene){
                    //var upvector = new THREE.Vector3();
                    
                    

                    vector3Dref.addedScenes.push({modeluuid : gltfscene.uuid, modelidx : null});

                    
                    var szShortformat = DocObj.Format.substring(0, 3);

                    if (DocObj.Format == "Autodesk Revit"){
                        szShortformat = "RVT";
                    }

                    
                    if (szShortformat == 'IFC' || szShortformat == 'RVT') {
                
                        gltfscene.up.set(0, 0, 1);
                        //this.scene.up.set(0, 0, 1);
                    }
                    
                    else {
                    
                        this.scene.up.set(0, 1, 0);
                    
                    }
                    //vector3Dref.scene.up.set(upvector.x,upvector.y, upvector.z);

                    //get current model uid
                    /*var modelscene = vector3Dref.getModelScene();

                    for(var nc = 0; nc < gltfscene.children.length; nc++){
                        modelscene.add(gltfscene.children[nc]);
                    }*/


                    vector3Dref.scene.add(gltfscene);
                    //vector3Dref.getModelScene().add(gltfscene);
                    

                    //vector3Dref.getModelScene().add(gltfscene);
                    
                    //vector3Dref.scene.add(gltfscene);

                    var addmetatsrc = null;

                    for (var mi = 0; mi < vector3Dref.addedMetaData.length; mi++){
                        if(!vector3Dref.addedMetaData[mi].added){
                            addmetatsrc = vector3Dref.addedMetaData[mi].metasrc;
                            vector3Dref.addedMetaData[mi].added = true;

                        }
                    }

                    //vector3Dref.box = new THREE.Box3().setFromObject(gltfscene);
                    //vector3Dref.boxSize = vector3Dref.box.getSize(new THREE.Vector3()).length();
                    //vector3Dref.boxCenter = vector3Dref.box.getCenter(new THREE.Vector3());

                    //parent.glTFloadComplete(planesize, vector3Dref.boxCenter, vector3Dref.unit, vector3Dref.box.min.z);

                    parent.gltfAddLoadComplete(addmetatsrc);

                    //addmetatsrc


                };

                this.getmodelUnit = function(){
                    return vector3Dref.unit;
                }

                this.getmodelBox = function(){
                    return vector3Dref.box;
                };

                this.setLightintensity = function (nlight, nintensity){


                    //const directionalLight_1 = new THREE.DirectionalLight(0xffffff, 0.7);
                    //const directionalLight_3 = new THREE.DirectionalLight(0xffffff, 0.05);
    
                    //const light = new THREE.HemisphereLight( 0xffffff, 0xffffff, 0.5 );
    
                    //const hemilight = new THREE.HemisphereLight( 0xffffff, 0x404040, 1 );
    
                    //const navlight = new THREE.HemisphereLight( 0xffffff, 0xffffff, 1 );

                    switch (nlight){
                        case 1 : 
                            directionalLight_1.intensity = nintensity;
                        break;

                        case 2 : 
                            directionalLight_3.intensity = nintensity;
                        break;

                        case 3 : 
                            light.intensity = nintensity;
                        break;

                        case 4 : 
                            hemilight.intensity = nintensity;
                        break;

                        case 5 :
                            amblight.intensity = nintensity;

                        break;


                    }
                    


                };

                this.setCADmaterial = function(busevmerge){

                    var mesharr = vector3Dref.getMeshChildren();
                    //for var 

                    var creaseAngle = 35 / (180 / Math.PI);

                    //.mergeVertices ( geometry : BufferGeometry, tolerance : Number ) : BufferGeometry

                    for (var i = 0; i < mesharr.length; i++) {
                        if (mesharr[i].type == "Group") {
                            for (var j = 0; j < mesharr[i].children.length; j++) {
                                if (mesharr[i].children[j].type == "Mesh") {
                                    if(mesharr[i].children[j].material.type == "MeshStandardMaterial"){

                                        if(busevmerge){
                                            if(vector3Dref.geometryutil != undefined){
                                                //mesharr[i].children[j].geometry = vector3Dref.geometryutil.mergeVertices(mesharr[i].children[j].geometry);
                                                
                                                
                                                mesharr[i].children[j].geometry = vector3Dref.geometryutil.toCreasedNormals(mesharr[i].children[j].geometry, creaseAngle);
        

                                                //mesharr[i].children[j].geometry = vector3Dref.geometryutil.mergeVertices(mesharr[i].children[j].geometry, 0.0000001);
                                            }
    
                                        }

                                        //mesharr[i].children[j].geometry.computeFaceNormals();
                                        //mesharr[i].children[j].geometry.computeVertexNormals();    

                                        mesharr[i].children[j].material.flatShading = false;

                                        if(!vector3Dref.isbim){
                                            mesharr[i].children[j].material.metalness = 0.1;
                                            mesharr[i].children[j].material.roughness = 0.5;
                                        }
                                        mesharr[i].children[j].material.needsUpdate = true;


                                        mesharr[i].children[j].updateMatrixWorld();
                                    }
                                    //mesharr[i].children[j].geometry.mergeVertices();    
                                }
                            }
                        }
                        if (mesharr[i].type == "Mesh") {

                            

                            if(mesharr[i].material.type == "MeshStandardMaterial"){


                                if(busevmerge){

                                    if(vector3Dref.geometryutil != undefined){
                                        //mesharr[i].geometry = vector3Dref.geometryutil.mergeVertices(mesharr[i].children[j].geometry);

                                        mesharr[i].geometry = vector3Dref.geometryutil.toCreasedNormals(mesharr[i].geometry, creaseAngle);

                                        //.toCreasedNormals ( geometry : BufferGeometry, creaseAngle : Number ) :
                                        //mesharr[i].geometry = vector3Dref.geometryutil.mergeVertices(mesharr[i].geometry);
                                    }
    





                                }




                                //mesharr[i].geometry.computeFaceNormals();
                                //mesharr[i].geometry.computeVertexNormals();


                                mesharr[i].material.flatShading = false;

                                if(!vector3Dref.isbim){
                                    mesharr[i].material.metalness = 0.1;
                                    mesharr[i].material.roughness = 0.5;
                                }
                                
                                mesharr[i].material.needsUpdate = true;

                                mesharr[i].updateMatrixWorld();
                            }

                            //mesharr[i].geometry.mergeVertices();
                        }
                    }                    


                };                




                this.setScene = function (gltfscene) {
                    //get dimensions of scene to set dim params
                    


                    /*gltfscene.traverse(function(n){
                        if(n.isMesh){
                            n.castShadow = true;
                            n.receiveShadow = true;
                            if(n.material.map) n.material.map.anisotropy = 16; 
                        }


                    });*/




                    var upvector = new THREE.Vector3();


                    vector3Dref.modeluuid = gltfscene.uuid;
                    
                    var szShortformat = DocObj.Format.substring(0, 3);

                    if (DocObj.Format == "Autodesk Revit"){
                        szShortformat = "RVT";
                    }
                    

                    if (szShortformat == 'IFC' || szShortformat == 'RVT') {
                
                        vector3Dref.isbim = true;

                        gltfscene.up.set(0, 0, 1);
                        //this.scene.up.set(0, 0, 1);

                    }
                    
                    else {
                    
                        vector3Dref.isbim = false;
                        gltfscene.up.set(0, 1, 0);

                        //this.scene.up.set(0, 1, 0);
                    
                    }
                   

                    upvector.x = gltfscene.up.x;
                    upvector.y = gltfscene.up.y;
                    upvector.z = gltfscene.up.z;
                    //vector3Dref.scene.up.set(upvector.x,upvector.y, upvector.z);
                    vector3Dref.scene.up.set(upvector.x, upvector.y, upvector.z);
                    vector3Dref.scene.add(gltfscene);

                    //var centreobj = new THREE.Object3D();
                    
                    //vector3Dref.scene.add(centreobj);


                    //vector3Dref.setOrientationScene(upvector);
                    

                    
                    vector3Dref.scene.add(vector3Dref.markupscene);
                    //vector3Dref.originalpos = vector3Dref.scene.position.clone();
                    vector3Dref.box = new THREE.Box3().setFromObject(gltfscene);
                    vector3Dref.boxSize = vector3Dref.box.getSize(new THREE.Vector3()).length();
                    vector3Dref.boxCenter = vector3Dref.box.getCenter(new THREE.Vector3());
                    //centreobj.position.copy(vector3Dref.boxCenter);

    
                    //scene.add( light.target );

                    if(!vector3Dref.isbim){
                        
                        vector3Dref.setLightintensity(4,2);
                        vector3Dref.setLightintensity(2,1);
                        vector3Dref.setLightintensity(1,1.2);
                        //RxCore.set3DLightintesity(2,1);
                        
                        

                    }else{

                        vector3Dref.setLightintensity(5,4);
                        vector3Dref.setLightintensity(3,1);

                    }

                    vector3Dref.setCADmaterial(true);

                    vector3Dref.scene.add(directionalLight_1.target);

                    //var light = new THREE.HemisphereLight( 0xffffff, 0xffffff, 0.5 );

                    if(vector3Dref.isbim){
                        
                        vector3Dref.scene.add( light );
                        vector3Dref.scene.add( amblight );

                        renderer.toneMapping = THREE.NoToneMapping; 
                    }else{
                        
                        //light.intensity = 1;
                        //const hemilight = new THREE.AmbientLight( 0x404040 ); // soft white light
                        
                        //amblight.intensity = 1;
                        vector3Dref.scene.add( hemilight );    

                        renderer.toneMapping = THREE.NoToneMapping;
                    }


                    
                    //vector3Dref.scene.add( light )

                    directionalLight_3.position.set(vector3Dref.boxCenter.x, vector3Dref.boxCenter.y, vector3Dref.boxCenter.z);

                    if(vector3Dref.isbim){
                        directionalLight_3.translateZ(-vector3Dref.boxSize*2);
                    }else{
                        directionalLight_3.translateY(-vector3Dref.boxSize*2);
                    }


                    //directionalLight_3.translateZ(-vector3Dref.boxSize*2);
                    directionalLight_3.target.position.set( vector3Dref.boxCenter.x, vector3Dref.boxCenter.y, vector3Dref.boxCenter.z );
                    directionalLight_3.target.updateMatrixWorld();


                    if(vector3Dref.isbim){
                        directionalLight_1.position.set(vector3Dref.box.min.x, vector3Dref.box.min.y, vector3Dref.box.max.z);
                        directionalLight_1.translateZ(vector3Dref.boxSize);
                        directionalLight_1.translateY(-vector3Dref.boxSize);
                        directionalLight_1.translateX(-vector3Dref.boxSize);
                    }else{
                        directionalLight_1.position.set(vector3Dref.box.min.x, vector3Dref.box.max.y, vector3Dref.box.min.z);
                        directionalLight_1.translateZ(-vector3Dref.boxSize);
                        directionalLight_1.translateY(vector3Dref.boxSize);
                        directionalLight_1.translateX(-vector3Dref.boxSize);

                        directionalLight_2.position.set(vector3Dref.box.max.x, vector3Dref.box.max.y, vector3Dref.box.max.z);
                        directionalLight_2.translateZ(vector3Dref.boxSize);
                        directionalLight_2.translateY(vector3Dref.boxSize);
                        directionalLight_2.translateX(vector3Dref.boxSize);

                        directionalLight_2.target.position.set( vector3Dref.boxCenter.x, vector3Dref.boxCenter.y, vector3Dref.boxCenter.z );
                        directionalLight_2.target.updateMatrixWorld();

                    }

                    //directionalLight_1.position.set(vector3Dref.box.min.x, vector3Dref.box.min.y, vector3Dref.box.max.z);
                    //directionalLight_2.position.set((vector3Dref.box.max.x, vector3Dref.box.max.y, vector3Dref.box.max.z);

                    //directionalLight_1.translateZ(vector3Dref.boxSize);
                    //directionalLight_1.translateY(-vector3Dref.boxSize);
                    //directionalLight_1.translateX(-vector3Dref.boxSize);
                    //directionalLight_1.target.position.set( vector3Dref.boxCenter.x, vector3Dref.boxCenter.y, vector3Dref.boxCenter.z );
                    //directionalLight_1.target.updateMatrixWorld();



    
                    /*For future implementation*/

    
                    if(bUsePostprocess){

                        vector3Dref.effectComposer = new THREE.EffectComposer( renderer );
                    
                        vector3Dref.renderPass = new THREE.RenderPass( vector3Dref.scene, parent.camera );
                        vector3Dref.effectComposer.addPass( vector3Dref.renderPass );
                        
                        vector3Dref.saoPass = new THREE.SAOPass( vector3Dref.scene, parent.camera, false, true );
                        //Did you try saoPass.resolution.set(1024,1024)
                        vector3Dref.saoPass.resolution.set(1024,1024);
    
                        vector3Dref.effectComposer.addPass( vector3Dref.saoPass );
    
                        vector3Dref.effectComposer.setSize( canvasowidth, canvasoheight );
    
                    }


                    /*gui.add( vector3Dref.saoPass.params, 'output', {
                        'Beauty': THREE.SAOPass.OUTPUT.Beauty,
                        'Beauty+SAO': THREE.SAOPass.OUTPUT.Default,
                        'SAO': THREE.SAOPass.OUTPUT.SAO,
                        'Depth': THREE.SAOPass.OUTPUT.Depth,
                        'Normal': THREE.SAOPass.OUTPUT.Normal
                    } ).onChange( function ( value ) {
    
                        vector3Dref.saoPass.params.output = parseInt( value );
    
                    } );
                    gui.add( vector3Dref.saoPass.params, 'saoBias', - 1, 1 );
                    gui.add( vector3Dref.saoPass.params, 'saoIntensity', 0, 1 );
                    gui.add( vector3Dref.saoPass.params, 'saoScale', 0, 10 );
                    gui.add( vector3Dref.saoPass.params, 'saoKernelRadius', 1, 100 );
                    gui.add( vector3Dref.saoPass.params, 'saoMinResolution', 0, 1 );
                    gui.add( vector3Dref.saoPass.params, 'saoBlur' );
                    gui.add( vector3Dref.saoPass.params, 'saoBlurRadius', 0, 200 );
                    gui.add( vector3Dref.saoPass.params, 'saoBlurStdDev', 0.5, 150 );
                    gui.add( vector3Dref.saoPass.params, 'saoBlurDepthCutoff', 0.0, 0.1 );*/
                    
                    //var helper_1 = new THREE.DirectionalLightHelper(directionalLight_1, 1);
                    //helper_1.update();
                    //vector3Dref.scene.add( helper_1 );

                    if (vector3Dref.boxSize < 1000) {
                        this.unit = 1;
                        
                        //meter
                    }
                    else {
                        this.unit = 0;
                        //mm
                    }

                    
                    if(pageobject.Unitofmeasure == 2){
                        this.unit = 2;
                        //feet
                    }  
                     
                    if(parseFloat(pageobject.DPI) != NaN ){
                        var DPImm = Math.round(((1 / parseFloat(pageobject.DPI) * 25.4) + Number.EPSILON) * 100) / 100;

                        if(DPImm == 1000 && this.unit == 0){
                            //override for now do not trust dpi
                            DPImm = 1;
                        }

                        
                    }else{
                        DPImm = 1000;
                        //default to unit = 1m
                    }
                    
                    //1000 = m
                    //1 = mm
                    //304.8 = foot
                    //25.4 = inch
                    //10 = cm
                    
                    //1 = metric, 2 = Imperial, 3 = System, 4=Custom
                    //Unitofmeasure;
                    
                    //1 = Millimeter, 2 = Centimeter, 3 = Decimeter, 4=Meter,5=Kilometer,6=Nautical Miles
                    //1 = Inch,2=Feet,3=Yard,4=Mile,5=Nautical Miles
                    //SubmeasureUnit;




                    switch (DPImm){
                            case 1000 : {
                                this.unit = 1;
                                this.pointSize = 0.02;
                                this.pointTreshold = 0.2;
                                RxCore_SetUnit(1);
                                //RxCore_MetricUnit(4);
                                RxCore_MetricUnit('Meter');
                                
                            }
                            break;
                            case 1 : {
                                this.unit = 0;
                                this.pointSize = 20;
                                this.pointTreshold = 200;
                                RxCore_SetUnit(1);
                                RxCore_MetricUnit('Millimeter');
                                //RxCore_MetricUnit(1);
                                
                            }
                            break;
                            case 304.8 :{
                                this.unit = 2;
                                this.pointSize = 0.16;
                                this.pointTreshold = 1.6;
                                RxCore_SetUnit(2);
                                RxCore_ImperialUnit('Feet');
                                //RxCore_ImperialUnit(2);
                                
                            }
                             
                            break;
                            case 25.4 : {
                                this.unit = 3;
                                this.pointSize = 1;
                                this.pointTreshold = 10;
                                RxCore_SetUnit(2);
                                RxCore_ImperialUnit('Inch');
                                //RxCore_ImperialUnit(1);
                            }
                            break;
                            case 10 : {
                                this.unit = 4;
                                this.pointSize = 2;
                                this.pointTreshold = 20;
                                RxCore_SetUnit(1);
                                RxCore_MetricUnit('Centimeter');
                                //RxCore_MetricUnit(2);
                            }
                            break;
                    }
                    
                    //console.log(DPImm);


                    //vector3Dref.scene.position.set(vector3Dref.boxCenter.x, vector3Dref.boxCenter.y, vector3Dref.boxCenter.z);
                    vector3Dref.originalpos = new THREE.Vector3();
                    vector3Dref.originalpos = vector3Dref.scene.position.clone();
                    parent.camera.up.set(upvector.x, upvector.y, upvector.z);
                    vector3Dref.frameArea();
                    //controls.maxDistance = boxSize * 10;
                    //controls.target.copy(boxCenter);
                    //controls.update();
                    var planesize = Math.max((vector3Dref.box.max.x - vector3Dref.box.min.x), (vector3Dref.box.max.y - vector3Dref.box.min.y));
                    //console.log(planesize, vector3Dref.boxSize);

                    //vector3Dref.box.min.z
                    parent.glTFloadComplete(planesize, vector3Dref.boxCenter, vector3Dref.unit, vector3Dref.box.min.z);
                    //set up camera and other stuff here.
                };
                this.setCamera = function(){
                    if (vector3Dref.boxSize == null || vector3Dref.boxCenter == null) {
                        return;
                    }

                    /*code for placing directional light above scene */
                    //var directionZP = new THREE.Vector3(0, 0, 1).normalize();
                    //var lightpos = new THREE.Vector3(vector3Dref.boxCenter.x, vector3Dref.boxCenter.y, vector3Dref.boxCenter.z - 1);
                    //var distlight = boxtop.distanceTo(vector3Dref.boxCenter);
                    //directionalLight_1.position.copy(directionZP.multiplyScalar(distlight * 4).add(boxtop));

                    //var spotLight = new THREE.SpotLight( 0xffffff );

                    /*var light = new 
                    light.position.set( 50, 50, 50 );
                    scene.add( light );*/
                    
                    /*vector3Dref.shadowspotLight = new THREE.PointLight( 0xffffff, 0.3, vector3Dref.boxSize * 0.5, 2);
                    vector3Dref.shadowspotLight.position.copy(lightpos);
                    vector3Dref.shadowspotLight.castShadow = true;

                    vector3Dref.shadowspotLight.shadow.bias = -0.0001;
                    vector3Dref.shadowspotLight.shadow.mapSize.width = 1024*4;
                    vector3Dref.shadowspotLight.shadow.mapSize.height = 1024*4;
                    vector3Dref.shadowspotLight.shadow.camera.near = vector3Dref.boxSize / 300;
                    vector3Dref.shadowspotLight.shadow.camera.far = vector3Dref.boxSize * 10;

                    vector3Dref.shadowspotLight.shadow.radius = 2;

                    vector3Dref.scene.add( vector3Dref.shadowspotLight );*/

                    const origo = new THREE.Vector3(0,0,0);
                    const halfSizeToFitOnScreen = (vector3Dref.boxSize * 0.5) * 0.5;
                    const halfFovY = THREE.MathUtils.degToRad(parent.camera.fov * .5);
                    const distance = halfSizeToFitOnScreen / Math.tan(halfFovY);
                    //distance *= 2;
                    const direction = (new THREE.Vector3())
                        .subVectors(origo, vector3Dref.boxCenter)
                        .multiply(new THREE.Vector3(0, 1, -1))
                        .normalize();

                    //parent.camera.position.set(vector3Dref.box.max.x, vector3Dref.box.min.y, vector3Dref.box.max.z);
                    //parent.camera.translateY(-distance);
                    

                    if(vector3Dref.isbim){
                        parent.camera.position.set(vector3Dref.box.max.x, vector3Dref.box.min.y, vector3Dref.box.max.z);
                        parent.camera.translateY(-distance);
    
                    }else{
                        parent.camera.position.set(vector3Dref.box.max.x, vector3Dref.box.max.y, vector3Dref.box.min.z);
                        parent.camera.translateZ(-distance * 2);

                    }
                    parent.camera.lookAt(vector3Dref.boxCenter.x, vector3Dref.boxCenter.y, vector3Dref.boxCenter.z);


                };

                this.setnavCamera = function(){
                    if (vector3Dref.navboxSize == null || vector3Dref.navboxCenter == null) {
                        return;
                    }

                  

                    const origo = new THREE.Vector3(0,0,0);
                    const halfSizeToFitOnScreen = (vector3Dref.navboxSize * 0.5) * 0.5;
                    const halfFovY = THREE.MathUtils.degToRad(parent.navcamera.fov * 0.5);
                    const distance = halfSizeToFitOnScreen / Math.tan(halfFovY);
                    const doubledistance = distance * 2;
                    
                    //distance *= 2;
                    //const direction = (new THREE.Vector3())
                    //    .subVectors(origo, vector3Dref.navboxCenter)
                    //    .multiply(new THREE.Vector3(0, 1, -1))
                     //   .normalize();

                    parent.navcamera.position.set(vector3Dref.navbox.max.x, vector3Dref.navbox.min.y, vector3Dref.navbox.max.z);
                    parent.navcamera.translateY(-distance);
                    parent.navcamera.lookAt(vector3Dref.navboxCenter.x, vector3Dref.navboxCenter.y, vector3Dref.navboxCenter.z);

                };

                this.framenavArea = function(){
                    if (vector3Dref.navboxSize == null || vector3Dref.navboxCenter == null) {
                        return;
                    }

                    parent.navcamera.near = vector3Dref.navboxSize / 1000;
                    parent.navcamera.far = vector3Dref.navboxSize * 300;

                    parent.navcamera.updateProjectionMatrix();

                    vector3Dref.setnavCamera();

                };

                this.frameArea = function () {
                    if (vector3Dref.boxSize == null || vector3Dref.boxCenter == null) {
                        return;
                    }
                    
                    // pick some near and far values for the frustum that
                    // will contain the box.
                    parent.camera.near = vector3Dref.boxSize / 1000;
                    parent.camera.far = vector3Dref.boxSize * 300;

                    //directionalLight_1.shadow.camera.near = vector3Dref.boxSize / 300;
                    //directionalLight_1.shadow.camera.far = vector3Dref.boxSize * 300;
    
                    //directionalLight_1.shadow.camera.updateProjectionMatrix();
                    //thispage.walkthroughcontrol.movementSpeed = 10;
                    //thispage.walkthroughcontrol.lookSpeed = 0.125;
                    //thispage.walkthroughcontrol.lookVertical = true;
                    parent.camera.updateProjectionMatrix();
                    // point the camera to look at the center of the box
                    
                    vector3Dref.setCamera();
                    

                    var width = vector3Dref.box.max.x - vector3Dref.box.min.x;
                    var depth = vector3Dref.box.max.y - vector3Dref.box.min.y;
                    var height = vector3Dref.box.max.z - vector3Dref.box.min.z;
                    var minx = vector3Dref.box.min.x;
                    var maxx = vector3Dref.box.max.x
                    var miny = vector3Dref.box.min.y;
                    var maxy = vector3Dref.box.max.y;
                    var minz = vector3Dref.box.min.z;
                    var maxz = vector3Dref.box.max.z;

                    vector3Dref.curwidth = width;
                    vector3Dref.curdepth = depth;
                    vector3Dref.curheight = height;
                    vector3Dref.curminx = minx;
                    vector3Dref.curmaxx = maxx;
                    vector3Dref.curminy = miny;
                    vector3Dref.curmaxy = maxy;
                    vector3Dref.curminz = minz;
                    vector3Dref.curmaxz = maxz;
    

                    vector3Dref.setClippingPlanes(width, depth, height, minx, maxx, miny, maxy, minz, maxz);

                    //vector3Dref.helperplaneX.updateMatrixWorld(true);
                    //vector3Dref.helperplaneY.updateMatrixWorld(true);
                    //vector3Dref.helperplaneZ.updateMatrixWorld(true);

                    //vector3Dref.scene.add( vector3Dref.helperplaneX );
                    //vector3Dref.scene.add( vector3Dref.helperplaneY );
                    //vector3Dref.scene.add( vector3Dref.helperplaneZ );

                    
    

                };

                this.setClippingPlanes = function(width, depth, height, minx, maxx, miny, maxy, minz, maxz){
                    //var width = vector3Dref.box.max.x - vector3Dref.box.min.x;
                    //var depth = vector3Dref.box.max.y - vector3Dref.box.min.y;
                    //var height = vector3Dref.box.max.z - vector3Dref.box.min.z;
                    var wscale = renderer.domElement.width / width;
                    var hscale = renderer.domElement.height / height;
                    this.scale = Math.min(wscale, hscale);

                    vector3Dref.xPlaneconst = width;
                    var safeadjust = vector3Dref.xPlaneconst / 20; //10 percent additional space for clipping
                    vector3Dref.xPlaneconst += safeadjust;
                    //vector3Dref.xPlaneEnd = vector3Dref.box.max.x;
                    
                    //vector3Dref.xPlaneStart = vector3Dref.box.min.x;
                    
                    //non inverted
                    //vector3Dref.xPlaneEnd = vector3Dref.box.min.x;
                    //inverted 
                    vector3Dref.xPlaneEnd = -(minx) - width;
                    vector3Dref.xPlaneStart = maxx;
                    vector3Dref.xPlaneEnd -= safeadjust / 2;
                    //vector3Dref.yPlaneconst = height;
                    
                    vector3Dref.yPlaneconst = depth;
                    

                    safeadjust = vector3Dref.yPlaneconst / 20; //10 percent additional space for clipping
                    vector3Dref.yPlaneconst += safeadjust;
                    //vector3Dref.yPlaneEnd = vector3Dref.box.max.y;
                    //vector3Dref.yPlaneStart = vector3Dref.box.min.y;

                    //non inverted
                    //vector3Dref.yPlaneEnd = vector3Dref.box.min.y;
                    //inverted
                    vector3Dref.yPlaneEnd = -(miny) - depth;                       
                    vector3Dref.yPlaneStart = maxy;
                    //console.log(vector3Dref.yPlaneEnd, "plane end y");
                    //console.log(vector3Dref.yPlaneEnd + depth, "plane end y + depth");
                    //console.log(vector3Dref.yPlaneStart, "plane start y");
                    vector3Dref.yPlaneEnd -= safeadjust / 2;
                    //vector3Dref.zPlaneconst = depth;
                    
                    vector3Dref.zPlaneconst = height;
                    
                    safeadjust = vector3Dref.zPlaneconst / 20; //10 percent additional space for clipping
                    vector3Dref.zPlaneconst += safeadjust;
                    //vector3Dref.zPlaneEnd = vector3Dref.box.max.z;
                    //vector3Dref.zPlaneStart = vector3Dref.box.min.z;
                    vector3Dref.zPlaneEnd = minz;
                    vector3Dref.zPlaneStart = maxz;
                    vector3Dref.zPlaneEnd -= safeadjust / 2;

                    //vector3Dref.globalPlaneX.constant = vector3Dref.xPlaneStart;
                    //vector3Dref.globalPlaneY.constant = vector3Dref.yPlaneStart;
                    //vector3Dref.globalPlaneZ.constant = vector3Dref.zPlaneStart;

                    vector3Dref.globalPlaneX.constant = vector3Dref.xPlaneEnd + vector3Dref.xPlaneconst;
                    vector3Dref.globalPlaneY.constant = vector3Dref.yPlaneEnd + vector3Dref.yPlaneconst;
                    vector3Dref.globalPlaneZ.constant = vector3Dref.zPlaneEnd + vector3Dref.zPlaneconst;
                };

                this.setlightPosition = function(x,y,z){
                    //var directionalLight_1 = new THREE.DirectionalLight(0xffffff, 1.2);
                    //directionalLight.intensity = 0.5;
                    directionalLight_1.position.set( x, y, z );
          
                };

                this.closestVertex = function(intersection,triangle){


                    var _faceVertices = triangle.geometry.getAttribute('position');
                    if (_faceVertices.count < 4){
                        return;
                    }
        
                    var _interPoint = intersection.point;
        
                    var apoint = new THREE.Vector3(_faceVertices.array[0],_faceVertices.array[1],_faceVertices.array[2]);
                    var bpoint = new THREE.Vector3(_faceVertices.array[3],_faceVertices.array[4],_faceVertices.array[5]);
                    var cpoint = new THREE.Vector3(_faceVertices.array[6],_faceVertices.array[7],_faceVertices.array[8]);
        
                    //var curx = vector3Dref.boxCenter.x;
                    //var cury = vector3Dref.boxCenter.y;
                    //var curz = vector3Dref.boxCenter.z;
                    
                    //var sceneoffset = new THREE.Vector3(curx,cury,curz);
                    apoint.sub(vector3Dref.boxCenter);
                    bpoint.sub(vector3Dref.boxCenter);
                    cpoint.sub(vector3Dref.boxCenter);
        
        
        
                    //-(vector3Dref.w / 2) * vector3Dref.scale;
        
        
                    //apoint.set(_faceVertices[0],_faceVertices[1],_faceVertices[2]);
                    //bpoint.set(_faceVertices[3],_faceVertices[4],_faceVertices[5]);
                    //cpoint.set(_faceVertices[6],_faceVertices[7],_faceVertices[8]);
                
                    //var curpoint = new THREE.Vector3();
                    //var closestpoint = new THREE.Vector3();
                    //var distancetopoint = 0;
                    //var prevdist = null;
                    //var _closestVertex;
        
                    //_interPoint.normalize();
        
                    /*for(var i=0;i<_faceVertices.count;i+=3){
                        if (i>0){
                            prevdist = distancetopoint;
                        }
                        curpoint.set(i,i+1,i+2);
                        distancetopoint = _interPoint.distanceTo(curpoint);
        
                        if(prevdist != null && distancetopoint > prevdist){
                            distancetopoint = prevdist;
        
                        }else{
                            closestpoint.copy(curpoint);
                        }
        
                    }*/
        
                    var _distToA = _interPoint.distanceTo(apoint);
                    var _distToB = _interPoint.distanceTo(bpoint);
                    var _distToC = _interPoint.distanceTo(cpoint);
        
                    var _closestVertex;
        
                    var _closest = Math.min(_distToA,_distToB);
                    _closest = Math.min(_distToC,_closest);
                    switch(_closest){
                        case _distToA:
                            _closestVertex = apoint;
                            apoint.add(vector3Dref.boxCenter);
                            break;
                        case _distToB:
                            _closestVertex = bpoint;
                            bpoint.add(vector3Dref.boxCenter);
                            break;
                        case _distToC:
                            _closestVertex = cpoint;
                            cpoint.add(vector3Dref.boxCenter);
                            break;
        
                    }
        
        
                    //var _closest = _distToA < _distToB ? (_closestVertex = apoint, _distToA) : (_closestVertex = bpoint, _distToB);
                    //_closest > _distToC && (_closest = _distToC, _closestVertex = cpoint);
        
                    _faceVertices = apoint = bpoint = cpoint = _interPoint = null;
        
                    return {
                        vertex: _closestVertex,
                        distance: _closest
                    }
        
                };

                this.add2DPoints = function(intersections, numpoints){
                    
                    if(numpoints > 2){
                        return;
                    }

                    var vertices = [];

                    var p1 = intersections[0].point.clone();
                    if (intersections[1]){
                        var p2 = intersections[1].point.clone();
                        var dist = p1.distanceTo ( p2 );
                        var midpoint = new THREE.Vector3();
                        midpoint.subVectors( p1, p2 ).setLength( dist *  0.5).add( p2 );
        
                    }


                    for (var pi = 0; pi < intersections.length; pi++){
                        var p = intersections[pi].point.clone();
                        vertices.push( p.x, p.y, p.z );
                    }

                    if (intersections[1]){
                        vertices.push( midpoint.x, midpoint.y, midpoint.z );
                    }
                    

                    var pointgeometry = new THREE.BufferGeometry();
                    pointgeometry.setAttribute( 'position', new THREE.Float32BufferAttribute( vertices, 3 ) );


                    /*var csprite = document.createElement('canvas');
                    csprite.width = 32;
                    csprite.height = 32;
                    var cspritectx = csprite.getContext('2d');

                    cspritectx.fillStyle = "rgba(0, 0, 255, 0.5)";


                    cspritectx.beginPath();
                    cspritectx.arc(16, 16, 32 * 0.5, 0, Math.PI * 2); //full circle
                    cspritectx.closePath();
                    cspritectx.arc(16, 16, 32 * 0.25, 0, Math.PI * 2, true); //full circle
                    cspritectx.closePath();
                    cspritectx.fill();

        
                    var spriteTexture = new THREE.CanvasTexture(csprite);*/

                    //spriteTexture.needsUpdate = true;
                    //var pointmaterial = new THREE.PointsMaterial( { size: 35, sizeAttenuation: false, map: spriteTexture, alphaTest: 0.5, transparent: true } );
                    
                    //set point size to 2 cm
                    var psize = 0.02;
                    //1 = m
                    //0 = mm
                    //2 = foot
                    //3 = inch
                    //4 = cm
                    //trheshold 
                    //
                    /*switch (vector3Dref.unit){
                        case 0 : psize = 20;
                        break;
                        case 1 : psize = 0.02;
                        break;
                        case 2 : psize = 0.16;
                        break;
                        case 3 : psize = 1;
                        break;
                        case 4 : psize = 2;
                        break;
                    }*/


                    psize = vector3Dref.pointSize;

                    var pointmaterial = new THREE.PointsMaterial( { color: 0x888888,  size: psize} );


                    var particles = new THREE.Points( pointgeometry, pointmaterial );
                    
                    particles.userData.pointx = p.x;
                    particles.userData.pointy = p.y;
                    particles.userData.pointz = p.z;

                    if (intersections[1]){
                        particles.userData.dimtext = vector3Dref.setdimvalue(dist);
                        particles.userData.distance = dist;
                        particles.userData.Unit = pageobject.Unitofmeasure;
                        particles.userData.SubUnit = pageobject.SubmeasureUnit;
                    }
                    
                    
                    

                    /*var upvector = new THREE.Vector3();
                    upvector.copy(vector3Dref.scene.up);
                    intersection.object.up.set(upvector.x, upvector.y, upvector.z);
                    intersection.object.geometry.computeVertexNormals();
                    var vec = intersection.face.normal.clone();
                    var tmp = new THREE.Object3D();
                    tmp.up.set(upvector.x, upvector.y, upvector.z);
                    tmp.position.copy(p);
                    //var n = intersects[0].face.normal.clone();
                    vec.transformDirection(intersection.object.matrixWorld);
                    vec.multiplyScalar(1);
                    vec.add(p);
                    tmp.lookAt(vec);

                    

                    particles.rotation.copy(tmp.rotation);*/
                    
                    particles.userData.type = "dimension";

                    particles.userData.signature = signature;
                    particles.userData.id = particles.uuid;


                    vector3Dref.markupscene.add(particles);

                    if (intersections[1]){
                        vector3Dref.addDimLine(p1, p2, particles.uuid);
                    }

                                        
                    if (RxCore_GUI_3DMarkup != undefined) {
                        RxCore_GUI_3DMarkup.setMarkupSelected(particles.userData, {created : true, modified : false, deleted : false});
                    }

                    if (RxCore_GUI_3DMarkupList != undefined) {
                        RxCore_GUI_3DMarkupList.setMarkupList(vector3Dref.markupscene.children, DocObj.bActive);
                    }


                    //var markupmaterial = new THREE.MeshBasicMaterial({ map: spriteTexture, side: THREE.DoubleSide });
                    //markupmaterial.transparent = true;
                    //markupmaterial.opacity = 0.5;

                    //use canvas texture here.
                    //var sprite = new THREE.TextureLoader().load( 'textures/sprites/disc.png' )

                    //use pointcloud and set invisible then extract vector for each point 
                    //and render to 2d using pointtoscreen.

                    //var p = intersection.point.clone();
                    //this.MarkupPoints.push(p);

                };

                this.dimvaluedraw = function (midpoint, ctx, strokecolor, dimtext, bcreate) {
                    ctx.save();

                    var scalefactor = 1;
                
                    var labelsize = { w: 0, h: 0 };
                    
                    //x, y, width, height
                    //, scalefactor, brotate
                    //var dimwidth = width - x;
                    
                    
                    //var dimheight = height - y;
                    //var dimdiag = markupobject.getdiag(dimwidth, dimheight);
                    //var dimanglerad = Math.atan2(dimheight, dimwidth);
                    ctx.textAlign = "start";
                    
                    //need to duplicatge font object 
                    //markupobject.font.setScale(scalefactor);
                    
                    
                    var scaletextheight = vector3Dref.font.height * scalefactor;
                    this.textheight = vector3Dref.font.height;
                    var labelsizefactor = vector3Dref.font.height / 12;
                    var labelstartoffsetx = 10 * labelsizefactor;
                    var labelstartoffsety = 5 * labelsizefactor;
                    var labelwidthtoffset = 20 * labelsizefactor;
                    var labelheightoffset = 10 * labelsizefactor;
                    ctx.font = vector3Dref.font.fontstringScaled;
                    var dimt = ctx.measureText(dimtext);
                    
                    
                   var dimtextwidth = dimt.width;

                   if(bcreate){
                        var dimtextx = midpoint.x + 15;
                        var dimtexty = midpoint.y;

                   } else{
                        dimtextx = midpoint.x - (dimtextwidth / 2);
                        dimtexty = midpoint.y + (scaletextheight / 2);
                    }
                    
                    /*if (markupobject.saved) {
                        var dimtextx = x + (dimwidth / 2) - (dimtextwidth / 2);
                        var dimtexty = y + (dimheight / 2) + (scaletextheight / 2);
                    }                    
                    else {
                        //var dimtextx = x + (dimwidth / 2) - (dimtextwidth / 2);
                        dimtextx = width + (15 * scalefactor);
                        dimtexty = height;
                        //var dimtexty = y + (dimheight / 2) + (scaletextheight / 2);
                    }*/
                    /*if (dimanglerad > (Math.PI * 0.5) || dimanglerad < -(Math.PI * 0.5)) {
                        if (markupobject.pagerotation == 0) {
                            dimanglerad += Math.PI;
                        }
                    }*/
                    //console.log(dimanglerad);
                    /*if (brotate && markupobject.saved) {
                        var tx = x + (dimwidth / 2);
                        var ty = y + (dimheight / 2);
                        ctx.translate(tx, ty);
                        ctx.rotate(dimanglerad);
                        ctx.translate(-tx, -ty);
                    }*/
                    /*if(rotation !=0){
                     }*/
                    
                    ctx.lineWidth = 1;
                    var yrect = dimtexty - scaletextheight;
                    
                    ctx.fillStyle = "white";
                    ctx.strokeStyle = strokecolor;
                    
                    ctx.fillRect(dimtextx - (labelstartoffsetx * scalefactor), yrect - (labelstartoffsety * scalefactor), dimtextwidth + (labelwidthtoffset * scalefactor), scaletextheight + (labelheightoffset * scalefactor));
                    ctx.strokeRect(dimtextx - (labelstartoffsetx * scalefactor), yrect - (labelstartoffsety * scalefactor), dimtextwidth + (labelwidthtoffset * scalefactor), scaletextheight + (labelheightoffset * scalefactor));
                    
                    labelsize.w = dimtextwidth + (labelwidthtoffset * scalefactor);
                    labelsize.h = scaletextheight + (labelheightoffset * scalefactor);
                    
                    ctx.fillStyle = "black";
                    ctx.fillText(dimtext, dimtextx, dimtexty);
                    ctx.restore();
                };

                this.draw2DPoints = function(points){

                    cntximg.save(); // save the context so we don't mess up others
                    cntximg.strokeStyle = 'black';
                    cntximg.fillStyle = "rgba(0, 0, 255, 0.5)";


                    cntximg.beginPath();
                    
                    cntximg.arc(points[0].x, points[0].y, 32 * 0.25, 0, Math.PI * 2); //full circle
                    //ctx.arc(100, 75, 50, 0, 2 * Math.PI);
                    cntximg.closePath();
                    //ctx.beginPath();
                    //ctx.arc(100, 75, 25, 0, 2 * Math.PI, true);
                    cntximg.arc(points[0].x, points[0].y, 32 * 0.125, 0, Math.PI * 2, true); //full circle
                    cntximg.closePath();


                    cntximg.arc(points[1].x, points[1].y, 32 * 0.25, 0, Math.PI * 2); //full circle
                    //ctx.arc(100, 75, 50, 0, 2 * Math.PI);
                    cntximg.closePath();
                    //ctx.beginPath();
                    //ctx.arc(100, 75, 25, 0, 2 * Math.PI, true);
                    cntximg.arc(points[1].x, points[1].y, 32 * 0.125, 0, Math.PI * 2, true); //full circle
                    cntximg.closePath();

                    if(points.length > 1){
                          
                        cntximg.moveTo(points[0].x, points[0].y);
                        cntximg.lineTo(points[1].x, points[1].y);
                        cntximg.closePath();
                        var bdostroke = true;    
                    }


                    if(bdostroke){
                        cntximg.stroke();
                    }

                    //ctx.stroke();
                    cntximg.fill();
                    cntximg.restore(); // restore context to what it was on entry

                };
                
                this.inViewCheck = function(vector){
                    
                    //var inview = false;

                    //THREE.Matrix4: .getInverse() has been removed. Use matrixInv.copy( matrix ).invert(); instead.
                    
                    parent.camera.updateMatrix(); // make sure camera's local matrix is updated
                    parent.camera.updateMatrixWorld(); // make sure camera's world matrix is updated

                    var threerev = parseInt(THREE.REVISION);
                

                    if (threerev <= 127){
                        //var quatInverse = quat.clone().inverse();
                        parent.camera.matrixWorldInverse.getInverse( parent.camera.matrixWorld );
                    }else{
                        ///var matrix4 = 
                        //parent.camera.matrixWorldInverse.invert(parent.camera.matrixWorld);
                        parent.camera.matrixWorldInverse.copy( parent.camera.matrixWorld ).invert();
                        //matrixInv
                    }

                    

                    var frustum = new THREE.Frustum();

                    //setFromProjectionMatrix(
                    frustum.setFromProjectionMatrix( new THREE.Matrix4().multiplyMatrices( parent.camera.projectionMatrix, parent.camera.matrixWorldInverse ) );

                    if(frustum.containsPoint(vector.pos)) {
                        return true;
                        //console.log('within camera view');
                      } else {
                        return false;
                        //console.log('outside camera view');
                      }

                };

                this.addDimLine = function(p1, p2, parentuuid){
                    
                    
                    var linepos = new Float32Array([p1.x,p1.y,p1.z,p2.x,p2.y,p2.z]);

                    var geometry3 = new THREE.BufferGeometry();
                    geometry3.setAttribute('position', new THREE.BufferAttribute(linepos, 3));

                    geometry3.setIndex(new THREE.BufferAttribute(new Uint16Array([0,1]), 1));
                    
                    var material = new THREE.LineBasicMaterial( { color: 0x0000ff,linewidth:2.0 } );
                    var dimension = new THREE.LineSegments(geometry3, material);

                    dimension.userData.type = "dimension";
                    dimension.userData.parentref = parentuuid;


                    vector3Dref.markupscene.add(dimension);


                };
                this.draw2DLine = function(points){
                    cntximg.save(); // save the context so we don't mess up others
                    cntximg.strokeStyle = 'black';

                    cntximg.beginPath();
                    cntximg.moveTo(points[0].x, points[0].y);
                    cntximg.lineTo(points[1].x, points[1].y);
                    cntximg.closePath();
                    cntximg.stroke();

                };
                
                

                this.draw2DPoint = function(point, ctx){
                    
                    ctx.save(); // save the context so we don't mess up others
                    ctx.strokeStyle = 'black';
                    
                    if(point.selected){
                        ctx.fillStyle = "rgba(255, 0, 0, 0.5)";
                    }else{
                        ctx.fillStyle = "rgba(0, 0, 255, 0.5)";
                    }
                    



                    ctx.beginPath();
                    ctx.arc(point.pos.x, point.pos.y, 32 * 0.25, 0, Math.PI * 2); //full circle
                    //ctx.arc(100, 75, 50, 0, 2 * Math.PI);
                    
                    ctx.closePath();
                    //ctx.beginPath();
                    //ctx.arc(100, 75, 25, 0, 2 * Math.PI, true);
                    ctx.arc(point.pos.x, point.pos.y, 32 * 0.125, 0, Math.PI * 2, true); //full circle
                    ctx.closePath();
                    //ctx.stroke();
                    ctx.fill();
                    ctx.restore(); // restore context to what it was on entry

                };


                this.setdimvalue = function (distance) {
                    
                    var dimdiag = distance;
                    
                    var blocalusefraction = false;
                    
                    
                    
                    if (pageobject.Unitofmeasure == 2) {

                        if (bUseFraction) {
                            blocalusefraction = true;
                        }
                        var fractionval = getUnitlengthRemainder(dimdiag);
                        
                        
                        var dimtextfrac = fractionval.quot;
                        var dimtextrem = fractionval.rem;

                        if (pageobject.SubmeasureUnit == 1) {
                            var szdimtext = dimtextfrac + "\"";
                            if (fractionval.numerator != 0 && fractionval.denominator != 0) {
                                if (fractionval.numerator == fractionval.denominator) {
                                    dimtextfrac + 1;
                                    var szdimtext = dimtextfrac + "\"";
                                }
                                else {
                                    var szfraction = fractionval.numerator + "/" + fractionval.denominator;
                                    var szdimtext = dimtextfrac + " " + szfraction + "\"";
                                }
                            }
                        }
                        if (pageobject.SubmeasureUnit == 2) {
                            if (dimtextrem != 0) {
                                var szdimtext = dimtextfrac + "\' - " + dimtextrem + "\"";
                            }
                            else {
                                szdimtext = dimtextfrac + "\'";
                            }
                            if (fractionval.numerator != 0 && fractionval.denominator != 0) {
                                if (fractionval.numerator == fractionval.denominator) {
                                    dimtextrem + 1;
                                    var szdimtext = dimtextfrac + "\' - " + dimtextrem + "\"";
                                }
                                else {
                                    if (dimtextrem != 0) {
                                        var szfraction = fractionval.numerator + "/" + fractionval.denominator;
                                        var szdimtext = dimtextfrac + "\' - " + dimtextrem + " " + szfraction + "\"";
                                    }
                                    else {
                                        var szfraction = fractionval.numerator + "/" + fractionval.denominator;
                                        var szdimtext = dimtextfrac + "\' - " + szfraction + "\"";
                                    }
                                }
                            }
                        }
                    }
                    var dimtext = getUnitlength(dimdiag);

                    dimtext = getUnitlength(dimdiag);
                    dimtext = dimtext.toFixed(ndimprecision);

                    dimtext = dimtext + " " + pageobject.Unitlabel;

                    if (blocalusefraction) {
                        dimtext = szdimtext;
                    }

                    return dimtext;

                };

                this.renderMarkupPoints = function(ctx){

                    if(parent.DocRef == null || parent.DocRef == undefined){
                        return;
                    }

                    var markuparray = vector3Dref.getMarkupChildren();
                    var pointsarray = [];
                    var localpoints = [];

                    ctx.clearRect(0, 0, canvas.width, canvas.height);


                    if(markuparray.length > 0 && parent.DocRef.Drawmarkup){
                        for (var mix = 0; mix < markuparray.length; mix++) {

                            markuparray[mix].updateMatrixWorld();

                            var positions = markuparray[mix].geometry.getAttribute('position');

                            if (markuparray[mix].type == "Points" && positions.count > 1) {
                                //&& markuparray[mix].geometry.attributes.position.array
                                
                                    for (var c=0; c < positions.count; c++){

                                        var vstart = c * positions.itemSize;

                                        var x = positions.array[vstart];
                                        var y = positions.array[vstart + 1];
                                        var z = positions.array[vstart + 2];

                                        var position = new THREE.Vector3(x, y, z);
                                        markuparray[mix].localToWorld(position);
                                        //console.log(vector3Dref.posToScreen(position));
                                        
                                        localpoints.push({pos : position, selected : markuparray[mix].userData.selected});

                                        //var projectedPosition = position.applyMatrix4(markuparray[mix].matrixWorld).project(parent.camera);
                                        pointsarray.push({pos : vector3Dref.posToScreen(position), selected : markuparray[mix].userData.selected});
                                        
    
                                    }    
                                    
                                    if(markuparray[mix].userData.Unit != undefined && markuparray[mix].userData.Unit != pageobject.Unitofmeasure){
                                        if(markuparray[mix].userData.distance != undefined){
                                            markuparray[mix].userData.dimtext = vector3Dref.setdimvalue(markuparray[mix].userData.distance);
                                            markuparray[mix].userData.Unit = pageobject.Unitofmeasure;
                                        }
                                    }
                                    
                                    //particles.userData.SubUnit = SubmeasureUnit;
                                    if(markuparray[mix].userData.SubUnit != undefined && markuparray[mix].userData.SubUnit != pageobject.SubmeasureUnit){
                                        if(markuparray[mix].userData.distance != undefined){
                                            markuparray[mix].userData.dimtext = vector3Dref.setdimvalue(markuparray[mix].userData.distance);
                                            markuparray[mix].userData.SubUnit = pageobject.SubmeasureUnit;
                                        }
                                    }

                                    

                                    var dimtext = markuparray[mix].userData.dimtext;
                                    var drawline = true;

                                    if(vector3Dref.inViewCheck(localpoints[0])){
                                        vector3Dref.draw2DPoint(pointsarray[0], ctx);
                                        drawline = true;
                                    }else{
                                        drawline = false;
                                    }

                                    if(vector3Dref.inViewCheck(localpoints[1])){
                                        vector3Dref.draw2DPoint(pointsarray[1], ctx);
                                    }else{
                                        drawline = false;
                                    }
                                    
                                    if(vector3Dref.inViewCheck(localpoints[2])){
                                        vector3Dref.dimvaluedraw(pointsarray[2].pos,ctx, 'black', dimtext, false);
                                    }else{
                                        drawline = false;
                                    }


                                    /*if(drawline){
                                        vector3Dref.draw2DLine(pointsarray);
                                    }*/
                                    

                                    pointsarray = [];
                                    localpoints = [];
                                  
                                    //count = 2 && 

                                    //console.log(positions);
                                
                                /*for (var pi = 0; pi < markuparray[mix].geometry.attributes.position.array.length; pi++){

                                    

                                    var x = markuparray[mix].geometry.attributes.position.array[pi].x;
                                    var y = markuparray[mix].geometry.attributes.position.array[pi].y;
                                    var z = markuparray[mix].geometry.attributes.position.array[pi].z;

                                    
                                    var projectedPosition = position.applyMatrix4(markuparray[mix].matrixWorld).project(parent.camera);
                                    pointsarray.push(projectedPosition);
                                    
    
                                }*/
                                
                                //vector3Dref.draw2DPoints(pointsarray);

                                //old draw single points:
                                /*var p = markuparray[mix].position.clone();
                                p.set(markuparray[mix].userData.pointx, markuparray[mix].userData.pointy, markuparray[mix].userData.pointz);

                                vector3Dref.draw2DPoint(vector3Dref.posToScreen(p));*/
                                //old draw single points:


                                //vector3Dref.posToScreen(markuparray[mix].userData.pointx;
                                //vector3Dref.posToScreen(markuparray[mix].userData.pointy;
                                //vector3Dref.posToScreen(markuparray[mix].userData.pointz;

                                

                                    //particles.userData.pointx = p.x;
                                    //particles.userData.pointy = p.y;
                                    //particles.userData.pointz = p.z;
                

                            }
                        }
                    }


                };

                this.posToScreen = function(pos){
                    var p = pos.clone();
                    
                    var vector = p.project(pageobject.camera);
                  
                    //vector.x = (vector.x + 1) / 2 * window.innerWidth;
                    //vector.y = -(vector.y - 1) / 2 * window.innerHeight;
                    vector.x = (vector.x + 1) / 2 * canvas.width;
                    vector.y = -(vector.y - 1) / 2 * canvas.height;

                  
                    return vector;

                };
                this.pointToScreen = function(intersection){
                    var p = intersection.point.clone();
                    
                    
                    var vector = p.project(pageobject.camera);
                  
                    //vector.x = (vector.x + 1) / 2 * window.innerWidth;
                    //vector.y = -(vector.y - 1) / 2 * window.innerHeight;
                    vector.x = (vector.x + 1) / 2 * canvas.width;
                    vector.y = -(vector.y - 1) / 2 * canvas.height;

                  
                    return vector;

                };
                this.storeCamera = function(target, position){
                    var storepos = new THREE.Vector3();
                    storepos.copy(position);
                    var storetarget = new THREE.Vector3();
                    storetarget.copy(target); 

                    vector3Dref.orbitcampos = storepos;
                    vector3Dref.orbitcamtarget = storetarget;
                    
                };

                this.getfloorElevation = function(floorindex){

                    if (parent.floorplans.length > 0){
                        return parent.floorplans[floorindex].Elevation;
                    }else{
                        return vector3Dref.box.min.z;
                    }

                };

                this.firstfloor = function(floorindex){

                    if(floorindex == 0){
                        return true;
                    }else{
                        return false;
                    }

                    /*if(parent.floorplans[0].index == floorindex){
                        return true;
                    } else{
                        return false;
                    }*/

                };
                this.lastfloor = function(floorindex){

                    if(floorindex == parent.floorplans.length - 1){
                        return true;
                    }else{
                        return false;
                    }

                    /*if(parent.floorplans[parent.floorplans.length - 1].index == floorindex){
                        return true;
                    } else{
                        return false;
                    }*/

                };
                
                this.checkcurfloor = function(){
                    

                    if(!vector3Dref.isbim){
                        return 0;
                    }

                    var curfloor = 0;
                    var curfloorindex = 0;


                    vector3Dref.floorelevation.max = null;
                    vector3Dref.floorelevation.min = null;
                    var floorindexmin = null; 
                    var floorindexmax = null; 

                    
                    for (var fpi = 0;fpi< parent.floorplans.length;fpi++){

                        
                        if(parent.camera.position.z > parent.floorplans[fpi].Elevation){
                            vector3Dref.floorelevation.min = fpi;
                            floorindexmin = parent.floorplans[fpi].index;
                        }

                        if(parent.camera.position.z < parent.floorplans[fpi].Elevation){
                            vector3Dref.floorelevation.max = fpi;
                            floorindexmax = parent.floorplans[fpi].index;
                        }

                        
                    }

                    if (vector3Dref.floorelevation.max == null && vector3Dref.floorelevation.min != null){
                        vector3Dref.floorelevation.max = vector3Dref.floorelevation.min;
                    }

                    if(vector3Dref.floorelevation.max == null || vector3Dref.floorelevation.min == null){
                        //below or above builiding.
                        curfloor = 0;
                        curfloorindex = parent.floorplans[0].index;
                        //thisthumb.floorelevation.index = 0;

                    }else{
                        curfloor = vector3Dref.floorelevation.min;
                        curfloorindex = floorindexmin;

                        //thisthumb.floorelevation.index = thisthumb.floorelevation.min;
                    }

                    if(vector3Dref.curfloor != curfloor){
                        if (RxCore_GUI_floorlevel != undefined) {
                            RxCore_GUI_floorlevel.setFloorLevel({floorindex : curfloor, storeyindex : curfloorindex});
                        }
                    }
                    
                    vector3Dref.curfloor = curfloor;

                    return vector3Dref.curfloor;


                };
                
                this.setControl = function (tool) {
                    if (vector3Dref.boxSize == null || vector3Dref.boxCenter == null) {
                        return;
                    }
                    if (tool.name == 'orbitControl') {
                        
                        //tool.reset();

                        if(vector3Dref.orbitcampos == null){
                            tool.setDimensions(vector3Dref.boxSize * 10, vector3Dref.boxCenter, vector3Dref.orbitcamtarget, parent.camera.position);
                        }else{
                            tool.setDimensions(vector3Dref.boxSize * 10, vector3Dref.boxCenter, vector3Dref.orbitcamtarget, vector3Dref.orbitcampos);
                        }
                        //.setCamera();
                    }
                    else if (tool.name == 'Walkthrough3D') {
                        //tool.setDimPos
                        parent.walkthroughcontrol.setDimPos(vector3Dref.scene.up, vector3Dref.boxSize * 10, vector3Dref.boxCenter);
                    }
                    //controls.maxDistance = boxSize * 10;
                    //controls.target.copy(boxCenter);
                    //controls.update();
                };
                this.blockreadComplete = function () {
                    if (RxCore_GUI_Read3DComplete != undefined) {
                        RxCore_GUI_Read3DComplete.readComplete();
                    }
                    parent.DocRef.SetActive();
                    //console.log('last block added');
                    /*if (pageobject.has3Dnav) {
                        pageobject.load3dnavigator();
                    }*/
                    //vector3Dref.updateblocklist();
                    /*if(szShortformat == 'IFC'){
        
                    }
        
                    if(szShortformat != 'IFC'){
                        breadbinaryready = false;
                        vector3Dref.updateblocklist();
                    }*/
                };

                //this.filterAttributelist = [];

                this.getModelCenter = function(){

                    return {unit : vector3Dref.unit, boxCenter : vector3Dref.boxCenter};

                     
                };
                this.filterAttributes = function (szAttributeLoadMask) {
                    vector3Dref.filterAttributelist = vector3Dref.blocklist.filter(function (attribute) {
                        return matchRuleShort(attribute.name, szAttributeLoadMask);
                    });

                };
                this.addstoreys = function(jsondata, name, id, idx, elevation){
                    //vector3Dref.storeys = [];
                    //console.log(name);
                    //console.log(id);
                    //vector3Dref.storeys = jsondata.BlockAttributes;

                    vector3Dref.storeys.push({name : name, id : id, attributes : jsondata.ID, index : idx, Elevation : elevation});

                    if(vector3Dref.storeys.length == parent.StoreyMetadata.length){
                        //console.log('storey load complete');

                        parent.glTFstoreyLoadComplete();

                    }

                    //vector3Dref.storeys = jsondata.BlockAttributes;

                };



                this.getstoreyNames = function(){
                    var storeynames = [];

                    if (vector3Dref.storeys[0].Elevation){
                        vector3Dref.storeys.sort(function(a, b) {
                            return a.Elevation - b.Elevation;
                        });
                    }else{

                        vector3Dref.storeys.sort((a,b) => (a.index > b.index) ? 1 : ((b.index > a.index) ? -1 : 0));
                        
                    }
                    

                    for(var si = 0; si < vector3Dref.storeys.length; si++){

                        storeynames.push(vector3Dref.storeys[si].name); 
                        
                    }

                    return storeynames;
                    
                };

                this.addblockscombine = function(JSONdata){

                    var blocklist = JSONdata.BlockAttributes;

                    vector3Dref.blocklist = vector3Dref.blocklist.concat(blocklist);

                    for(var bi = 0; bi < blocklist.length; bi++){
                        vector3Dref.blocklistquick.push(blocklist[bi].GlobalID);
                    }



                };

                this.addNavlist = function (JSONdata){
                    vector3Dref.navlist = [];

                    vector3Dref.navlist = JSONdata;

       
                    if (RxCore_GUI_3DParts != undefined) {
                        RxCore_GUI_3DParts.isupdate = true;
                        RxCore_GUI_3DParts.set3DParts(vector3Dref.navlist);
                     
                    }

                };


                this.addblocks = function (JSONdata) {
                    vector3Dref.blocklist = [];
                                
                    
                    //console.log(vector3Dref.blocklist[objectID].GlobalID);
                    //console.log(vector3Dref.blocklist[pobjectID].GlobalID);
                    //console.log(intersects[i].object.name);

                    
                    vector3Dref.blocklist = JSONdata.BlockAttributes;

                    for(var bi = 0; bi < vector3Dref.blocklist.length; bi++){
                        vector3Dref.blocklistquick.push(vector3Dref.blocklist[bi].GlobalID);
                    }
                    
                    //var mesharray = vector3Dref.getMeshChildren();
                    
                    if (Rxcore_GUI_search3Denabled != undefined) {
                        //Rxcore_GUI_fileLoadComplete.loadComplete(thispage.DocRef.OriginalURL);
                        Rxcore_GUI_search3Denabled.loadComplete(parent.DocRef.OriginalURL, parent.DocRef.bActive);
                    }    


                    /*gltfscene.traverse(function(n){
                        if(n.isMesh){
                            n.castShadow = true;
                            n.receiveShadow = true;
                            if(n.material.map) n.material.map.anisotropy = 16; 
                        }
                    });*/
                    
                };
                this.updateblocklist = function () {
                    //BlocksContainer.init3D();
                    //this.htmlcontent = "<ul style='list-style: none; margin: 0; padding: 0;'>";
                    var level = 0;


                    var mesharray = vector3Dref.getMeshChildren();
                    if (mesharray.length == 0) {
                        return;
                    }
                    for (var i = 0; i < mesharray.length; i++) {
                        if (mesharray[i].type == "Group") {
                            for (var j = 0; j < mesharray[i].children.length; j++) {
                                if (mesharray[i].children[j].type == "Mesh") {
                                    //vector3Dref.setmeshTransp(mesharray[i].children[j], value, false);
                                }
                            }
                        }
                        if (mesharray[i].type == "Mesh") {
                            //vector3Dref.setmeshTransp(mesharray[i], value, false);
                        }
                    }                    


                    var addedmesharray = vector3Dref.getAddedMeshChildren();
    
                    /*if (addedmesharray.length == 0) {
                        return;
                    }*/

                    for (var i = 0; i < addedmesharray.length; i++) {

                        for (var j = 0; j < addedmesharray[i].length; j++) {

                            if (addedmesharray[i][j].type == "Group") {
                                for (var k = 0; k < addedmesharray[i][j].children.length; k++) {
                                    if (addedmesharray[i][j].children[k].type == "Mesh") {
                                        //vector3Dref.setmeshTransp(mesharray[i].children[j], value, false);
                                    }
                                }
                            }
                            if (addedmesharray[i][j].type == "Mesh") {
                                //vector3Dref.setmeshTransp(mesharray[i], value, false);
                            }
    
                        }

                    }                    


                    //var blocklistheight = BlocksContainer.Getheight();
                    /*for (var pc = 0; pc < this.blocklist.length; pc++) {
                        
                        var index = this.blocklist[pc].index;


                        if (this.partlist[index] != undefined) {
                            this.blocklist[pc].state = this.partlist[index].tempvisible;
                        }
                        else {
                            this.blocklist[pc].state = false;
                        }
                        //this.blocklist[pc].state = this.partlist[index].tempvisible;
                        if (this.partlist[index].selected) {
                            // if(this.blocklist[pc].position > (blocklistheight / 2)){
                            //    BlocklistScrollTo = this.blocklist[pc].position - (blocklistheight / 2);
                            //}
                            this.blocklist[pc].setSelected(true);
                        }
                        else {
                            this.blocklist[pc].setSelected(false);
                        }
                        //this.htmlcontent += this.blocklist[pc].htmltext;
                    }*/
                    if (RxCore_GUI_3DParts != undefined) {
                        RxCore_GUI_3DParts.isupdate = true;
                        //RxCore_GUI_3DParts.scrollto = BlocklistScrollTo;
                        //RxCore_GUI_3DParts.set3DParts(this.blocklist);
                        RxCore_GUI_3DParts.set3DParts(this.navlist);
                        //RxCore_GUI_3DParts.scrollto = 0;
                    }
                };

                this.getarrayMeshIDs = function(mesharray, meshids, meshuserData){
                    
                    for (var mi = 0;mi < meshids.length; mi++){
                        for (var i = 0; i < mesharray.length; i++) {
                            if (mesharray[i].type === "Group") {
                                for (var j = 0; j < mesharray[i].children.length; j++) {

                                    
                                    var meshname = getmeshName(mesharray[i].children[j].name, mesharray[i].children[j].type);
                                    //var meshname = mesharray[i].children[j].name.substring(0, mesharray[i].children[j].name.length - 2);

                                    /*if(mesharray[i].children[j].type == "LineSegment"){
                                        var meshname = mesharray[i].children[j].name.substring(0, mesharray[i].children[j].name.length - 2);
    
                                    }else{
                                        meshname = mesharray[i].children[j].name;
                                    }*/
                                    
            
                                    //var meshname = mesharray[i].children[j].name.substring(0, mesharray[i].children[j].name.length - 2);
                                    var meshnamefull = mesharray[i].children[j].name;

                                    if (meshids[mi] == meshname){
                                        meshuserData.push(meshnamefull);    
                                    }
        
    
                                }
                            }else{
                                if (mesharray[i].name === "") {
                                    continue;
                                }
                                
                                if (meshids[mi] == mesharray[i].name){
                                    meshuserData.push(mesharray[i].name);
                                }
    
                            }
    
                        }
    
                    }

                    return meshuserData;

                };

                this.getMeshIDs = function(meshids){
                    
                    var mesharray = vector3Dref.getMeshChildren();
                    var addedmesharray = vector3Dref.getAddedMeshChildren();


                    var meshuserData = [];


                    meshuserData = vector3Dref.getarrayMeshIDs(mesharray, meshids, meshuserData);

                    

                    for (var ami = 0; ami < addedmesharray.length; ami++){
                        meshuserData = vector3Dref.getarrayMeshIDs(addedmesharray[ami], meshids, meshuserData);
                    } 

                    return meshuserData;

                };
                
                this.getmeshUserDataAll = function(mesharray,meshuserData, meshids, bAll){

                    for (var i = 0; i < mesharray.length; i++) {

                        if (mesharray[i].type === "Group") {
                            for (var j = 0; j < mesharray[i].children.length; j++) {
                                

                                var meshname = getmeshName(mesharray[i].children[j].name, mesharray[i].children[j].type);

                                /*if(mesharray[i].children[j].type == "LineSegment"){
                                    var meshname = mesharray[i].children[j].name.substring(0, mesharray[i].children[j].name.length - 2);
                                }else{

                                    meshname = mesharray[i].children[j].name;
                                }*/


                                if(bAll){
                                    if(mesharray[i].children[j].userData.visible == undefined){
                                        mesharray[i].children[j].userData.visible = (mesharray[i].children[j].visible) ? 1 : 0;
                                    }
                                    if(mesharray[i].children[j].userData.facecolor == undefined){
                                        
                                        mesharray[i].children[j].userData.facecolor = mesharray[i].children[j].material.color.getHex();
                                    }

                                    if(mesharray[i].children[j].userData.tempvisible == undefined){
                                        mesharray[i].children[j].userData.tempvisible = (mesharray[i].children[j].userData.visible == 1);
                                    }
 
                                    if (mesharray[i].children[j].userData.transparent == undefined) {
                                        mesharray[i].children[j].userData.transparent = mesharray[i].children[j].material.transparent ? 1 : 0;
                                    }
                                    if(mesharray[i].children[j].userData.opacity == undefined){
                                        mesharray[i].children[j].userData.opacity = mesharray[i].children[j].material.opacity;
                                    }
                                    
                                    if(mesharray[i].children[j].userData.uuid == undefined){
                                        mesharray[i].children[j].userData.uuid = mesharray[i].children[j].uuid;
                                    }
                                    
                                    if(mesharray[i].children[j].userData.name == undefined){
                                        mesharray[i].children[j].userData.name = mesharray[i].children[j].name;
                                        
                                    }
                                   

                                    var meshsobj = {name : meshname, userData : mesharray[i].children[j].userData};
                                    meshuserData.push(meshsobj);
                                }else{
                                    for (var mi = 0;mi < meshids.length; mi++){

                                        //this.setselect(mesharray[i].children[j], name, i, j, true);
                                        
                                        if (meshids[mi].GlobalID == meshname){
                                            

                                            if(mesharray[i].children[j].userData.visible == undefined){
                                                mesharray[i].children[j].userData.visible = (mesharray[i].children[j].visible) ? 1 : 0;
                                            }
                                            if(mesharray[i].children[j].userData.facecolor == undefined){

                                                
                                                mesharray[i].children[j].userData.facecolor = mesharray[i].children[j].material.color.getHex();    
                                            }
                                            if(mesharray[i].children[j].userData.tempvisible == undefined){
                                                mesharray[i].children[j].userData.tempvisible = (mesharray[i].children[j].userData.visible == 1);    
                                            }
                                            if(mesharray[i].children[j].userData.uuid == undefined){
                                                mesharray[i].children[j].userData.uuid = mesharray[i].children[j].uuid;    
                                            }
                                            if(mesharray[i].children[j].userData.name == undefined){
                                                mesharray[i].children[j].userData.name = mesharray[i].children[j].name;    
                                            }
    
                                            //var meshsobj = {name : meshids[mi], userData : mesharray[i].children[j].userData};
                                            var meshsobj = {name : meshids[mi].GlobalID, userData : mesharray[i].children[j].userData, attributes : meshids[mi]};
                                            meshuserData.push(meshsobj);
                                        }                                        
                                    }
                                }
                            }
                        }else{
                            if (mesharray[i].name === "") {
                                continue;
                            }

                            if(bAll){

                                if(mesharray[i].userData.visible == undefined){
                                    mesharray[i].userData.visible = (mesharray[i].visible) ? 1 : 0;
                                }
                                if(mesharray[i].userData.facecolor == undefined){
                                    
                                    mesharray[i].userData.facecolor = mesharray[i].material.color.getHex();
                                }
                                if(mesharray[i].userData.tempvisible == undefined){
                                    mesharray[i].userData.tempvisible = (mesharray[i].userData.visible == 1);
                                }
                                if(mesharray[i].userData.uuid == undefined){
                                    mesharray[i].userData.uuid = mesharray[i].uuid;
                                }
                                if(mesharray[i].userData.name == undefined){
                                    mesharray[i].userData.name = mesharray[i].name;
                                }

                                var meshsobj = {name : mesharray[i].name, userData : mesharray[i].userData};
                                meshuserData.push(meshsobj);

                            }
                            for (var mi = 0;mi < meshids.length; mi++){


                                if (meshids[mi].GlobalID == mesharray[i].name){


                                    if(mesharray[i].userData.visible == undefined){
                                        mesharray[i].userData.visible = (mesharray[i].visible) ? 1 : 0;
                                    }
                                    if(mesharray[i].userData.facecolor == undefined){
                                        
                                        mesharray[i].userData.facecolor = mesharray[i].material.color.getHex();
                                    }
                                    if(mesharray[i].userData.tempvisible == undefined){
                                        mesharray[i].userData.tempvisible = (mesharray[i].userData.visible == 1);
                                    }
                                    if(mesharray[i].userData.uuid == undefined){
                                        mesharray[i].userData.uuid = mesharray[i].uuid;
                                    }
                                    if(mesharray[i].userData.name == undefined){
                                        mesharray[i].userData.name = mesharray[i].name;
                                    }
    

                                    //mesharray[i].userData.visible = (mesharray[i].visible) ? 1 : 0;
                                    //mesharray[i].userData.facecolor = mesharray[i].material.color.getHex();
                                    //mesharray[i].userData.tempvisible = (mesharray[i].userData.visible == 1);
                                    //mesharray[i].userData.uuid = mesharray[i].uuid;
                                    //mesharray[i].userData.name = mesharray[i].name;

                                    var meshsobj = {name : meshids[mi].GlobalID, userData : mesharray[i].userData, attributes : meshids[mi]};
                                    //var meshsobj = {name : meshids[mi], userData : mesharray[i].userData};
                                    meshuserData.push(meshsobj);
                                }

                                //this.setselect(mesharray[i].children[j], name, i, j, true);
                            }
                        }
                    }

                    return meshuserData;
                };



                

                this.getSelectedMesh = function(){

                    var selectedMeshArray = [];
                    var badded = false;

                    var mesharray = vector3Dref.getMeshChildren();
                    var addedmesharray = vector3Dref.getAddedMeshChildren();

                    for (var mi = 0; mi < mesharray.length; mi++){
                        if (mesharray[mi].type === "Group") {
                            for (var j = 0; j < mesharray[mi].children.length; j++) {


                                if(mesharray[mi].children[j].userData.selected){
                                    //add to selected array
                                    selectedMeshArray.push(mesharray[mi].children[j].name);

                                    //getselectedmeshAll()
                                    
                                    //meshdetails = this.setselect(meshdetails, -1, bUseCallback);                                    

                                    //selectedMeshArray.push(meshdetails);

                                }

                            }
                        }else{
                            if (mesharray[mi].name === "") {
                                continue;
                            }

                            if(mesharray[mi].userData.selected){
                                //add to selected array                                                            
                                
                                selectedMeshArray.push(mesharray[mi].name);
                                //selectedMeshArray.push({mesh : mesharray[mi], addedmesh : false});
                                

                            }
                            
                        }

                    }


                    for (var ami = 0; ami < addedmesharray.length; ami++){
                        if (addedmesharray[ami].type === "Group") {
                            for (var j = 0; j < addedmesharray[ami].children.length; j++) {


                                if(addedmesharray[ami].children[j].userData.selected){
                                    //add to selected array
                                    //selectedMeshArray.push({mesh : addedmesharray[ami].children[j], addedmesh : true});
                                    selectedMeshArray.push(addedmesharray[ami].children[j].name);


                                }

                            }
                        }else{
                            if (addedmesharray[ami].name === "") {
                                continue;
                            }

                            if(addedmesharray[ami].userData.selected){
                                //add to selected array                                                            
                                //selectedMeshArray.push(addedmesharray[ami]);
                                //selectedMeshArray.push({mesh : addedmesharray[ami], addedmesh : true});

                                selectedMeshArray.push(addedmesharray[ami].name);

                                
                            }
                            
                        }

                    }

                    return selectedMeshArray;

                };

                this.getmeshUserData = function(meshids, bAll){
                    
                    var mesharray = vector3Dref.getMeshChildren();

                    var addedmesharray = vector3Dref.getAddedMeshChildren();
                    
                    var meshuserData = [];
                    
                    if (mesharray.length == 0) {
                        return meshuserData;
                    }

                    meshuserData = vector3Dref.getmeshUserDataAll(mesharray, meshuserData, meshids, bAll);

                  
                    for (var ami = 0; ami < addedmesharray.length; ami++){
                        
                        //vector3Dref.getarrayMeshIDs(addedmesharray[ami], meshids, meshuserData);
                        meshuserData = vector3Dref.getmeshUserDataAll(addedmesharray[ami], meshuserData, meshids, bAll);
                    } 

                    

                    return meshuserData;
                };
                
                this.getStoreyRealIDs = function(storeyname){
                    var meshids = [];
                    var storeyID = null

                    for(var si = 0; si < vector3Dref.storeys.length; si++){
                        if (vector3Dref.storeys[si].name == storeyname){
                            storeyID = si;
                        }
                    }

                    if (storeyID != null){
                        /*for (var pc = 0; pc < vector3Dref.storeys[storeyID].attributes.length; pc++){

                            meshids.push(vector3Dref.storeys[storeyID].attributes[pc].GlobalID);

                        }*/

                        if(!vector3Dref.storeys[storeyID].idreplaced){
                            vector3Dref.storeys[storeyID].attributes = vector3Dref.getMeshIDs(vector3Dref.storeys[storeyID].attributes);
                            vector3Dref.storeys[storeyID].idreplaced = true;
                        }
                        
                        
                        return vector3Dref.storeys[storeyID].attributes;


                    }else{
                        return meshids;
                    }

                };
                this.getStoreyIDs = function(storeyname){

                    var meshids = [];
                    var storeyID = null

                    for(var si = 0; si < vector3Dref.storeys.length; si++){
                        if (vector3Dref.storeys[si].name == storeyname){
                            storeyID = si;
                        }
                    }

                    if (storeyID != null){
                        /*for (var pc = 0; pc < vector3Dref.storeys[storeyID].attributes.length; pc++){

                            meshids.push(vector3Dref.storeys[storeyID].attributes[pc].GlobalID);

                        }*/
                        return vector3Dref.storeys[storeyID].attributes;
                    }else{
                        return meshids;
                    }
                    
                };

                this.getAttributeStorey = function(storeyname){


                    var meshids = [];
                    var storeyID = null

                    for(var si = 0; si < vector3Dref.storeys.length; si++){
                        if (vector3Dref.storeys[si].name == storeyname){
                            storeyID = si;
                        }
                    }

                    if (storeyID != null){
                        for (var pc = 0; pc < vector3Dref.storeys[storeyID].attributes.length; pc++){

                            //meshids.push(vector3Dref.storeys[storeyID].attributes[pc].GlobalID);
                            meshids.push(vector3Dref.storeys[storeyID].attributes[pc]);

                        }
    
                    }
                    

                    return vector3Dref.getmeshUserData(meshids, false);
                    
                };
                
                this.searchAttributeStoreyEx = function(storeyname, attribval){
                    var meshids = [];
                    var storeyID = null

                    for(var si = 0; si < vector3Dref.storeys.length; si++){
                        if (vector3Dref.storeys[si].name == storeyname){
                            storeyID = si;
                        }
                    }

                    if (storeyID != null){
                        for (var pc = 0; pc < vector3Dref.storeys[storeyID].attributes.length; pc++){

                            for (let [key, value] of Object.entries(vector3Dref.storeys[storeyID].attributes[pc])) {

                                if(value == attribval){
                                    //meshids.push(vector3Dref.storeys[storeyID].attributes[pc].GlobalID);
                                    meshids.push(vector3Dref.storeys[storeyID].attributes[pc]);
                                    break;
                                }
                                //var val = matchRuleShort(value, attribval);
                                //console.log(`${key}: ${value}`);
    
                            }
                            /*if(vector3Dref.blocklist[pc].value == attribval){
                                blockattributes = vector3Dref.blocklist[pc];
                            }*/
                        }
    
                    }
                    
                    return vector3Dref.getmeshUserData(meshids, false);
                };
                this.searchAttributeStorey = function(storeyname, attribval){

                    var meshids = [];
                    var storeyID = null

                    for(var si = 0; si < vector3Dref.storeys.length; si++){
                        if (vector3Dref.storeys[si].name == storeyname){
                            storeyID = si;
                        }
                    }

                    if (storeyID != null){
                        for (var pc = 0; pc < vector3Dref.storeys[storeyID].attributes.length; pc++){

                            for (let [key, value] of Object.entries(vector3Dref.storeys[storeyID].attributes[pc])) {
                            
                                //var val = matchRuleShort(value, attribval);
                                if(matchRuleShort(value, attribval)){
                                    //meshids.push(vector3Dref.storeys[storeyID].attributes[pc].GlobalID);
                                    meshids.push(vector3Dref.storeys[storeyID].attributes[pc]);
                                    continue;
                                }
                                //console.log(`${key}: ${value}`);
    
                            }
                            /*if(vector3Dref.blocklist[pc].value == attribval){
                                blockattributes = vector3Dref.blocklist[pc];
                            }*/
                        }
    
                    }
                    
                    return vector3Dref.getmeshUserData(meshids, false);


                };

                this.searchAttributeJSONEx = function(attribval){
                    //var id = -1;
                    var meshids = [];

                    if(attribval != undefined){
                        for (var pc = 0; pc < vector3Dref.blocklist.length; pc++){

                            for (let [key, value] of Object.entries(vector3Dref.blocklist[pc])) {
                            
                                //var val = matchRuleShort(value, attribval);
                                if(value == attribval){
                                    //meshids.push(vector3Dref.blocklist[pc].GlobalID);
                                    meshids.push(vector3Dref.blocklist[pc]);
                                    break;
    
                                }
                                //console.log(`${key}: ${value}`);
    
                            }
                            /*if(vector3Dref.blocklist[pc].value == attribval){
                                blockattributes = vector3Dref.blocklist[pc];
                            }*/
                        }
    
                    }

                    
                    return vector3Dref.getmeshUserData(meshids, false);
                    
                };
                this.searchAttributeJSON = function(attribval){
                    //var id = -1;
                    var meshids = [];

                    if(attribval != undefined){
                        for (var pc = 0; pc < vector3Dref.blocklist.length; pc++){

                            for (let [key, value] of Object.entries(vector3Dref.blocklist[pc])) {
                            
                                //var val = matchRuleShort(value, attribval);
                                if(matchRuleShort(value, attribval)){
                                    //meshids.push(vector3Dref.blocklist[pc].GlobalID);
                                    meshids.push(vector3Dref.blocklist[pc]);
    
                                    //continue;
                                    break;
                                    
                                }
                                //console.log(`${key}: ${value}`);
    
                            }
                            /*if(vector3Dref.blocklist[pc].value == attribval){
                                blockattributes = vector3Dref.blocklist[pc];
                            }*/
                        }
                    }
                    
                    return vector3Dref.getmeshUserData(meshids, false);
                    

                    /*var blockattributes = undefined;
                    
                    for (var pc = 0; pc < vector3Dref.blocklist.length; pc++){
                        if(vector3Dref.blocklist[pc].GlobalID === name){
                            blockattributes = vector3Dref.blocklist[pc];
                        }
                    }
                    return blockattributes;*/

                    
                    //return id;
                };
                
                this.searchAttribute = function (attribval) {
                    var id = -1;
                    for (var pc = 0; pc < vector3Dref.partlist.length; pc++) {
                        var part3D = vector3Dref.partlist[pc];
                        for (var ac = 0; ac < part3D.attributes.length; ac++) {
                            if (part3D.attributes[ac].value == attribval) {
                                id = vector3Dref.partlist[pc].id;
                            }
                        }
                    }
                    return id;
                };

                this.restoreClipping = function(clippvalues){

                    if(clippvalues == undefined){
                        
                        vector3Dref.clipping(-1, 0, false);
                        return;

                    }

                    var cliponx = ((clippvalues.x > 0) && (clippvalues.x < 100));
                    var clipony = ((clippvalues.y > 0) && (clippvalues.y < 100));
                    var cliponz = ((clippvalues.z > 0) && (clippvalues.z < 100));

                    var clippingon = (cliponx || clipony || cliponz);

                    if(clippingon){
                        vector3Dref.clipping(0, clippvalues.x, true);
                        vector3Dref.clipping(1, clippvalues.y, true);
                        vector3Dref.clipping(2, clippvalues.z, true);
                    }else{
                        vector3Dref.clipping(-1, 0, false);
                    }

                    /*if((clipping.x > 0) && (clipping.x < 100)){
                        vector3Dref.clipping(0, clipping.x, true);
                    }else{
                        //vector3Dref.clipping(0, clipping.x, false);
                    }
                    if((clipping.y > 0) && (clipping.y < 100)){
                        vector3Dref.clipping(1, clipping.y, true);
                    }else{
                        //vector3Dref.clipping(1, clipping.y, false);
                    }
                    if((clipping.z > 0) && (clipping.z < 100)){
                        vector3Dref.clipping(2, clipping.z, true);
                    }else{
                        //vector3Dref.clipping(2, clipping.z, false);
                    }*/

                };

                this.getClipfactor = function(){

                    var percentX = (vector3Dref.globalPlaneX.constant - vector3Dref.xPlaneEnd) / vector3Dref.xPlaneconst;
                    var percentY = (vector3Dref.globalPlaneY.constant - vector3Dref.yPlaneEnd) / vector3Dref.yPlaneconst;
                    var percentZ = (vector3Dref.globalPlaneZ.constant - vector3Dref.zPlaneEnd) / vector3Dref.zPlaneconst;

                    return {
                        xplane : percentX, 
                        yplane : percentY, 
                        zplane : percentZ,
                        xplaneconst : percentX * 100, 
                        yplaneconst : percentY * 100, 
                        zplaneconst : percentZ * 100
                    };

                };

                this.clipping = function (plane, constant, onoff) {
                    
                    if(vector3Dref.active3DView){
                        return;
                    }
                    
                    vector3Dref.clippingOn = onoff;
                    if (onoff) {
                        renderer.clippingPlanes = vector3Dref.globalPlanes;
                    }
                    else {
                        renderer.clippingPlanes = Empty;
                        return;
                    }
                    var percent = constant / 100;


                    switch (plane) {
                        case 0: //x plane
                            var newval = vector3Dref.xPlaneEnd + (vector3Dref.xPlaneconst * percent);
                            vector3Dref.globalPlaneX.constant = newval;
                            
                            break;
                        case 1: //y plane
                            
                            newval = vector3Dref.yPlaneEnd + (vector3Dref.yPlaneconst * percent);
                            vector3Dref.globalPlaneY.constant = newval;
                            
                            break;
                        case 2: //z plane
                            newval = vector3Dref.zPlaneEnd + (vector3Dref.zPlaneconst * percent);
                            vector3Dref.globalPlaneZ.constant = newval;
                            break;
                    }

                    

                    


                };
                this.doexplode = false;


                /*function setthumbnail(camobj){

                    var cameraimage = document.createElement('img');


                    cameraimage.addEventListener('load', cameraimageload, false);
                    cameraimage.src = renderer.domElement.toDataURL();


                }*/

                /*function cameraimageload (ev){
                    
                    
                    var target = ev.target || ev.srcElement;
                    var cameracanvas = document.createElement('canvas');

                    var imagewidth = GetImageWidth(target);
                    var imageheight = GetImageHeight(target);

                    cameracanvas.width = imagewidth;
                    cameracanvas.height = imageheight;

                    var camctx = cameracanvas.getContext('2d');
                    camctx.drawImage(target, 0, 0);
                                     


                }*/

                this.getCameraView = function(name){

                    
                    var camview = null;

                    for (var i = 0; i < vector3Dref.cameraSaves.length; i++) {
                        if (vector3Dref.cameraSaves[i].name == name) {
                            
                            camview = vector3Dref.cameraSaves[i];
                            
                        }
                    }

                    //vector3Dref.cameraSaves.splice(delcounter,1);
                    //parent.delete3DMarkupbyName(name);

                    //deleteMarkup3DView(name);

                    return camview;

                };

                this.deleteCameraView = function(name){
                    var delcounter = null;
                    for (var i = 0; i < vector3Dref.cameraSaves.length; i++) {
                        if (vector3Dref.cameraSaves[i].name == name) {
                            
                            delcounter = i;
                        }
                    }

                    vector3Dref.cameraSaves.splice(delcounter,1);
                    parent.delete3DMarkupbyName(name);

                    deleteMarkup3DView(name);


                    if (RxCore_GUI_3DMarkupList != undefined) {
                        RxCore_GUI_3DMarkupList.setMarkupList(vector3Dref.markupscene.children, DocObj.bActive);
                    }


                    //delete all markup associated with view.


                };
                this.addCameraViews = function(cameraArr){
                    
                    for(var i = 0; i < cameraArr.length; i++){

                        //var camobj = cameraArr[i];

                        if(cameraArr[i].explodefactor == undefined){
                            var explfctr = 0;
                        }else{
                            explfctr = cameraArr[i].explodefactor;
                        }

                        if(cameraArr[i].clipping == undefined){

                            var clippingplanes = {
                                x : 0,
                                y : 0,
                                z : 0
                            };
        

                        }else{
                            var clippingplanes = {
                                x : cameraArr[i].clipping.x,
                                y : cameraArr[i].clipping.y,
                                z : cameraArr[i].clipping.z
                            };

                        }

                        if(cameraArr[i].attributes == undefined){
                            var attribs = [];
                        }else{
                            attribs = cameraArr[i].attributes;
                        }

                        var camobj = {
                            position: cameraArr[i].position,
                            name: cameraArr[i].name, 
                            tool: cameraArr[i].tool, 
                            explodefactor : explfctr,
                            clipping : clippingplanes, 
                            thumbnail: null, 
                            width: 0, 
                            height: 0, 
                            image: document.createElement('img'),
                            selectedmesharray : cameraArr[i].selectedmesharray,
                            attributes : attribs
                        };
                        
                        camobj.image.src = cameraArr[i].image;
                        

                        getThumbcanvas(camobj, nCamerathumbwidth).then(function(cameraobj){
                            vector3Dref.cameraSaves.push(cameraobj);
                        });

                        //vector3Dref.cameraSaves.push(cameraArr[i]);
                    }


                };

                this.getCameraSaves = function () {

                    return vector3Dref.cameraSaves;
                };
                

                this.get3DResetView = function(){
                    return vector3Dref.topView;
                };

                this.saveResetCam = function(){
                    

                    //get3DResetViewName

                    if(vector3Dref.topView != null){
                        //there can be only one.
                        return;
                    }


                    var radius = vector3Dref.getmminUnit(200); //1 mm
                    //console.log(pageobject.DocRef.curcontrol3D);

                    var geometry = new THREE.SphereBufferGeometry(radius, 32, 32);
                    var material = new THREE.MeshBasicMaterial({ color: 0xffff00 });
                    var sphere = new THREE.Mesh(geometry, material);

                    sphere.name = "resetView";

                    var base64String = renderer.domElement.toDataURL();

                    //this.curcontrol3D = 'orbitControl';

                    vector3Dref.topView = {
                        position: pageobject.camera.matrix.toArray(), 
                        name: 'resetView', 
                        tool: 'orbitControl', 
                        explodefactor : 0, 
                        thumbnail: null, 
                        width: 0, 
                        height: 0, 
                        image: document.createElement('img'), 
                        selectedmesharray : [],
                        attributes : []
                    };
                    
                    vector3Dref.topView.image.src = base64String;

                    getThumbcanvas(vector3Dref.topView, nCamerathumbwidth).then(function(cameraobj){

                        vector3Dref.cameraSaves.unshift(cameraobj);

                        //RxCore_SaveTop3DCamera

                       /*if (Rxcore_GUI_3DCameraSave != undefined) {
                         
                            Rxcore_GUI_3DCameraSave.setCameraSave(cameraobj, DocObj.bActive);

                       }*/


                    });                    


                    var cposvector = new THREE.Vector3();
                    cposvector.copy(pageobject.camera.position);
                    cposvector.sub(vector3Dref.scene.position);
                    
                    sphere.position.copy(cposvector);
                    sphere.userData.type = "camera";
                    sphere.userData.name = sphere.name;
                    sphere.userData.signature = signature;

                    sphere.visible = false;

                    vector3Dref.markupscene.add(sphere);

                    if (RxCore_GUI_3DMarkupList != undefined) {
                        RxCore_GUI_3DMarkupList.setMarkupList(vector3Dref.markupscene.children, DocObj.bActive);
                    }


                };

                this.refreshCameraImage = function (name){

                    var camobj = vector3Dref.getCameraView(name);

                    var base64String = renderer.domElement.toDataURL();

                    //var camobj = {position: pageobject.camera.matrix.toArray(), name: sphere.name, tool: pageobject.DocRef.curcontrol3D, thumbnail: null, width: 0, height: 0, image: document.createElement('img'), selectedmesharray : []};
                    //camobj.selectedmesharray = vector3Dref.getSelectedMesh();  

                    //var selectedmesharray = vector3Dref.getSelectedMesh();

                    camobj.image.src = base64String;

                    getThumbcanvas(camobj, nCamerathumbwidth).then(function(cameraobj){

                        //camobj.thumbnail = tcanv;
                        //camobj.width = tcanv.width;
                        //camobj.height = tcanv.height;
                        //vector3Dref.cameraSaves.push(cameraobj);
                        console.log('3D view thumbnail updated');

                        if (RxCore_GUI_3DMarkupList != undefined) {
                            RxCore_GUI_3DMarkupList.setMarkupList(vector3Dref.markupscene.children, DocObj.bActive);
                        }
    


                    });




                };


                function get3DViewIndex(viewname){
                    var viewcounter = null;
                    
                    for (var i = 0; i < vector3Dref.cameraSaves.length; i++) {
                        if (vector3Dref.cameraSaves[i].name == viewname) {
                            
                            viewcounter = i;
                        }
                    }

                    return viewcounter;

                }

                this.findViewAttribute = function (szName, szValue, viewname) {
                    
                    var viewcounter = get3DViewIndex(viewname);

                    var found = false;

                    if(viewcounter != null && vector3Dref.cameraSaves[viewcounter].attributes != undefined){
                        
                        for (var i = 0; i < vector3Dref.cameraSaves[viewcounter].attributes.length; i++) {
                            if (vector3Dref.cameraSaves[viewcounter].attributes[i].name == szName && vector3Dref.cameraSaves[viewcounter].attributes[i].value == szValue) {
                                found = true;
                            }
                        }
    
                    }

                    return found;
                };
                this.updateViewAttribute = function (newszName, newszValue, viewname) {

                    var viewcounter = get3DViewIndex(viewname);


                    if(viewcounter != null && vector3Dref.cameraSaves[viewcounter].attributes != undefined){

                        for (var i = 0; i < vector3Dref.cameraSaves[viewcounter].attributes.length; i++) {
                            if (vector3Dref.cameraSaves[viewcounter].attributes[i].name == newszName) {
                                vector3Dref.cameraSaves[viewcounter].attributes[i].name = newszName;
                                vector3Dref.cameraSaves[viewcounter].attributes[i].value = newszValue;
                            }
                        }
    
                    }

                };

                this.deleteViewAttribute = function (szName, viewname) {

                    var viewcounter = get3DViewIndex(viewname);


                    if(viewcounter != null && vector3Dref.cameraSaves[viewcounter].attributes != undefined){

                        var foundindex = -1;
                        for (var i = 0; i < vector3Dref.cameraSaves[viewcounter].attributes.length; i++) {
                            if (vector3Dref.cameraSaves[viewcounter].attributes[i].name == szName) {
                                foundindex = i;
                            }
                        }
                        if (foundindex != -1) {
                            //splice to remove entry
                            vector3Dref.cameraSaves[viewcounter].attributes.splice(foundindex, 1);
                        }
    

                    }

                };

                this.getViewAttributes = function (viewname) {

                    var viewcounter = get3DViewIndex(viewname);

                    if(viewcounter != null && vector3Dref.cameraSaves[viewcounter].attributes != undefined){
                        return  vector3Dref.cameraSaves[viewcounter].attributes
                    }else{
                        return [];
                    }
                    
                };

                this.addViewAttribute = function (szName, szValue, viewname) {
                    
                    var extendobj = {
                        name: szName,
                        value: szValue
                    };

                    var viewcounter = get3DViewIndex(viewname);

                    if(viewcounter != null && vector3Dref.cameraSaves[viewcounter].attributes != undefined){
                        vector3Dref.cameraSaves[viewcounter].attributes.push(extendobj);  
                    }

                };

                this.getViewAttribute = function(szName, viewname){

                    var attrib = undefined;

                    var viewcounter = get3DViewIndex(viewname);

                    if(viewcounter != null && vector3Dref.cameraSaves[viewcounter].attributes != undefined){

                        for (var i = 0; i < vector3Dref.cameraSaves[viewcounter].attributes.length; i++) {
                            if (vector3Dref.cameraSaves[viewcounter].attributes[i].name == szName) {
                                attrib = vector3Dref.cameraSaves[viewcounter].attributes[i];
                            }
                        }
    
                    }

                    return attrib;
                };


                this.saveCamera = function () {
                    //const savedCamera = JSON.parse( localStorage.getItem( 'savedCamera' ) );
                    //this.cameraSaves = [];
                    //vector3Dref.cameraSaves.push(vector3Dref.parent.camera.position);
                    //vector3Dref.cameraSaves.push(vector3Dref.parent.camera.matrix.toArray());
                    //vector3Dref.cameraSaves.push(pageobject.camera.matrix.toArray());

                    //var curtool = pageobject.DocRef.curcontrol3D;
                    //thispage.smallimage.addEventListener('load', thispage.smallimageloadevent, false);

                    //var imagewidth = renderer.domElement.width;
                    //var imageheight = renderer.domElement.height;

                    var radius = vector3Dref.getmminUnit(200); //1 mm
                    //console.log(pageobject.DocRef.curcontrol3D);

                    var geometry = new THREE.SphereBufferGeometry(radius, 32, 32);
                    var material = new THREE.MeshBasicMaterial({ color: 0xffff00 });
                    var sphere = new THREE.Mesh(geometry, material);
                    var posnum = vector3Dref.cameraSaves.length + 1;

                    sphere.name = "markuppos_" + sphere.uuid + posnum;

                    var cposvector = new THREE.Vector3();
                    cposvector.copy(pageobject.camera.position);
                    cposvector.sub(vector3Dref.scene.position);
                    
                    sphere.position.copy(cposvector);
                    sphere.userData.type = "camera";
                    sphere.userData.name = sphere.name;
                    sphere.userData.signature = signature;
                    //vector3Dref.scene.position
                    //cposvector.x -= vector3Dref.x;
                    //cposvector.y -= vector3Dref.y;
                    //cposvector.z -= vector3Dref.z;
                    //sphere.position.add(vector3Dref.scene.position);
                    //vector3Dref.scene.add(sphere);
                    sphere.visible = true;
                    
                    var base64String = renderer.domElement.toDataURL();

                    if(vector3Dref.pxplodefactor != 0){
                        var explodefactor = vector3Dref.pxplodefactor / 0.2;
                    }else{
                        explodefactor = 0;
                    }

                    //console.log(vector3Dref.getClipfactor());
                    //vector3Dref.getClipfactor()

                    /*
                    return {
                    xplane : percentX, 
                    yplane : percentY, 
                    zplane : percentZ,
                    xplaneconst : percentX * 100, 
                    yplaneconst : percentY * 100, 
                    zplaneconst : percentZ * 100
                    };*/

                    var clipconsts = vector3Dref.getClipfactor();

                    if(vector3Dref.clippingOn){
                        var clippingplanes = {
                            x : clipconsts.xplaneconst,
                            y : clipconsts.yplaneconst,
                            z : clipconsts.zplaneconst
                        };
    
                    }else{
                        var clippingplanes = {
                            x : 0,
                            y : 0,
                            z : 0
                        };
                    }



                    var camobj = {
                        position: pageobject.camera.matrix.toArray(), 
                        name: sphere.name, 
                        tool: pageobject.DocRef.curcontrol3D, 
                        explodefactor : explodefactor, 
                        clipping : clippingplanes, 
                        thumbnail: null, 
                        width: 0, 
                        height: 0, 
                        image: document.createElement('img'), 
                        selectedmesharray : [],
                        attributes : []
                    };

                    //console.log(vector3Dref.pxplodefactor);

                    //distance *= 0.2;

                    camobj.selectedmesharray = vector3Dref.getSelectedMesh();  

                    //var selectedmesharray = vector3Dref.getSelectedMesh();

                    camobj.image.src = base64String;

                    getThumbcanvas(camobj, nCamerathumbwidth).then(function(cameraobj){

                        //camobj.thumbnail = tcanv;
                        //camobj.width = tcanv.width;
                        //camobj.height = tcanv.height;
                        vector3Dref.cameraSaves.push(cameraobj);
                        

                       if (Rxcore_GUI_3DCameraSave != undefined) {
                         
                            Rxcore_GUI_3DCameraSave.setCameraSave(cameraobj, DocObj.bActive);

                        }


                    });

                    //setthumbnail(camobj);



                    vector3Dref.markupscene.add(sphere);

                    /*if (RxCore_GUI_3DCameraSaves != undefined) {
                        RxCore_GUI_3DCameraSaves.setThumbnails(vector3Dref.cameraSaves);
                    }*/
                    
                    if (RxCore_GUI_3DMarkupList != undefined) {
                        RxCore_GUI_3DMarkupList.setMarkupList(vector3Dref.markupscene.children, DocObj.bActive);
                    }
                    

                    /*var x = meshobj.dv.getFloat32(meshobj.dvpos, true) - parent.x;
                    meshobj.dvpos += 4;
                    var y = meshobj.dv.getFloat32(meshobj.dvpos, true) - parent.y;
                    meshobj.dvpos += 4;
                    var z = meshobj.dv.getFloat32(meshobj.dvpos, true) - parent.z;*/
                };
                this.getCameraimage = function(name){

                    var thumbnail = null;

                    for (var i = 0; i < vector3Dref.cameraSaves.length; i++) {
                        if (vector3Dref.cameraSaves[i].name == name) {

                            thumbnail = {thumbnail : vector3Dref.cameraSaves[i].thumbnail, width : vector3Dref.cameraSaves[i].width, height : vector3Dref.cameraSaves[i].height};

                        }
                    }
                    return thumbnail;

                };
                this.setcur3DViewnull = function(){

                    var restoretool = pageobject.DocRef.curcontrol3D;

                    for (var i = 0; i < vector3Dref.cameraSaves.length; i++) {
                    
                        if (vector3Dref.cameraSaves[i].name == vector3Dref.cur3Dview) {
                            restoretool = vector3Dref.cameraSaves[i].tool;
                            
                        }
                    }
                    
                    pageobject.DocRef.curcontrol3D = restoretool;         

                    vector3Dref.cur3Dview = null;
                    
                    //vector3Dref.active3DView = false;
                    

                    var stateobj = {
                        iscompare:false,
                        numOpenFiles:OpenFiles.length,
                        isPDF:false,
                        is3D:true,
                        markupOn3D : false,
                        is2D:false,
                        numpages:pageobject.DocRef.pages.length,
                        currentpage:0,
                        activefile : pageobject.DocRef.bActive,
                        source : 'setcur3DViewnull'
                        
                    };

                    vector3Dref.active3DView = false;

                    if (RxCore_GUI_State != undefined) {
                        RxCore_GUI_State.setGUIState(stateobj);
                    }

                    if (RxCore_GUI_3DView != undefined) {
                        RxCore_GUI_3DView.viewEvent(vector3Dref.cur3Dview);
                    }


                };
                this.getcur3DView = function(){
                    
                    return vector3Dref.cur3Dview;

                };
                this.getRestoreControl = function(){
                    var restoretool = undefined;

                    for (var i = 0; i < vector3Dref.cameraSaves.length; i++) {
                        if (vector3Dref.cameraSaves[i].name == vector3Dref.cur3Dview) {
                            restoretool = vector3Dref.cameraSaves[i].tool;
                        }
                    }

                    return restoretool;

                };

                this.updatebirdseye = function(){
                  
                    DocObj.draw3DNav();


                    if(RxCore_GUI_UpdatebirdsEye != undefined){
                        
                        RxCore_GUI_UpdatebirdsEye.drawbirdseye();

                    }
            

                };  

                this.setbirdseye = function(pos, angle){

                    
                    if(RxCore_GUI_CameraPos != undefined){
                        RxCore_GUI_CameraPos.cameraPos(pos, angle, vector3Dref.getModelCenter());
                    }

                    DocObj.setthumcamerapos(pos, angle);

                    

                };


                this.setbirdseyecamera = function(camera){
                    
                    var camangle = {degrees : 0, radians : 0};

                    birseyeEuler.setFromQuaternion( camera.quaternion );

                    camangle.radians = birseyeEuler.z;
                    camangle.radians += Math.PI * 0.5;

                    if (camangle.radians < 0){
                        camangle.radians += Math.PI * 2;
                    }

                    
                    birdseyeCampos = camera.position.clone();
                    
                    vector3Dref.setbirdseye(birdseyeCampos, camangle);
                    
                    vector3Dref.updatebirdseye();



                };

                this.saveresetCamera = function(){

                    vector3Dref.startcampos = pageobject.camera.matrix.toArray();
                    
                };

                this.restoreresetCam = function(){

                    if(vector3Dref.startcampos != null){

                        pageobject.camera.matrix.fromArray(vector3Dref.startcampos);
                        pageobject.camera.matrix.decompose(pageobject.camera.position, pageobject.camera.quaternion, pageobject.camera.scale);

                    }

                };



                this.restorestartcam = function(){

                    if(vector3Dref.startcampos != null){



                        pageobject.camera.matrix.fromArray(vector3Dref.startcampos);
                        pageobject.camera.matrix.decompose(vector3Dref.startcampos, pageobject.camera.quaternion, pageobject.camera.scale);

                        vector3Dref.orbitcampos = null;

                        vector3Dref.orbitcamtarget = vector3Dref.boxCenter.clone();


                        /*if(vector3Dref.orbitcampos == null){
                            tool.setDimensions(vector3Dref.boxSize * 10, vector3Dref.boxCenter, vector3Dref.orbitcamtarget, parent.camera.position);
                        }else{
                            tool.setDimensions(vector3Dref.boxSize * 10, vector3Dref.boxCenter, vector3Dref.orbitcamtarget, vector3Dref.orbitcampos);
                        }*/


    
                    }

                };

                this.set3DViewPos = function(sphere){


                    //markuparray[i].userData.name
                    

                    for (var i = 0; i < vector3Dref.cameraSaves.length; i++) {
                        if (vector3Dref.cameraSaves[i].name == sphere.userData.name) {

                            sphere.matrix.fromArray(vector3Dref.cameraSaves[i].position);
                            sphere.matrix.decompose(sphere.position, sphere.quaternion, sphere.scale);
                
                            
                        }
                    }

                };

                this.restoreCameraName = function (name) {
                    for (var i = 0; i < vector3Dref.cameraSaves.length; i++) {
                        if (vector3Dref.cameraSaves[i].name == name) {
                            

                            //DocRef.curcontrol3D = 'markupedit'; 

                            vector3Dref.cur3Dview = null;

                            pageobject.unselect3DBlockAll();
                            
                            vector3Dref.cur3Dview = name;

                            vector3Dref.active3DView = false;

                            set_tool('markupedit', {p1 : false}); //use to display markup
                            pageobject.DocRef.curcontrol3D = 'markupedit'; 


                           /* if(vector3Dref.cameraSaves[i].tool == 'Walkthrough3D'){
                                //RxCore_3DWalkThrough(true);
                            }else if(vector3Dref.cameraSaves[i].tool == 'orbitControl'){
                                //RxCore_3DOrbit(true);
                            }*/
                            //vector3Dref.cameraSaves[i].tool;

                            pageobject.camera.matrix.fromArray(vector3Dref.cameraSaves[i].position);
                            pageobject.camera.matrix.decompose(pageobject.camera.position, pageobject.camera.quaternion, pageobject.camera.scale);



                            vector3Dref.setbirdseyecamera(pageobject.camera);


                            if(vector3Dref.cameraSaves[i].selectedmesharray){
                                for(var j = 0;j < vector3Dref.cameraSaves[i].selectedmesharray.length; j++){
                                    if(vector3Dref.cameraSaves[i].selectedmesharray[j]){

                                        var lastchars = vector3Dref.cameraSaves[i].selectedmesharray[j].substr(vector3Dref.cameraSaves[i].selectedmesharray[j].length - 2);

                                        if(lastchars == "_0"){
                                            pageobject.select3DBlock(vector3Dref.cameraSaves[i].selectedmesharray[j], false);
                                        }
                                        if(lastchars != "_0" && lastchars != "_1"){
                                            pageobject.select3DBlock(vector3Dref.cameraSaves[i].selectedmesharray[j], false);
                                        }
                                        
    
    
                                        //select3dobject
                                        //vector3Dref.cameraSaves[i].selectedmesharray[j]
                                    }
                                    
                                }
    
                            }

                            vector3Dref.doexplode = true;
                            vector3Dref.newExplode(0);
                            vector3Dref.doexplode = false;

                            if (vector3Dref.cameraSaves[i].explodefactor != undefined){

                                vector3Dref.doexplode = true;
                                vector3Dref.newExplode(vector3Dref.cameraSaves[i].explodefactor);
                                vector3Dref.doexplode = false;

                            }

                            vector3Dref.restoreClipping(vector3Dref.cameraSaves[i].clipping);

                            //vector3Dref.scene.updateMatrix();

                            vector3Dref.active3DView = true;
                            
                            var stateobj = {
                                iscompare:false,
                                numOpenFiles:OpenFiles.length,
                                isPDF:false,
                                is3D:true,
                                markupOn3D : true,
                                is2D:false,
                                numpages:pageobject.DocRef.pages.length,
                                currentpage:0,
                                activefile : pageobject.DocRef.bActive,
                                source : 'restoreCameraName'
                            };

                            if (RxCore_GUI_State != undefined) {
                                RxCore_GUI_State.setGUIState(stateobj);
                            }

                            if (RxCore_GUI_3DView != undefined) {
                                RxCore_GUI_3DView.viewEvent(name);
                            }

                            

                            //should be unique but make sure.
                            break;
                        }
                    }
                };
                this.set3DViewSelectedMesh = function(name){
                    
                    for (var i = 0; i < vector3Dref.cameraSaves.length; i++) {
                        if (vector3Dref.cameraSaves[i].name == name) {
                            vector3Dref.cameraSaves[i].selectedmesharray = vector3Dref.getSelectedMesh();
                            vector3Dref.refreshCameraImage(name);
                            
                        }
                    }

                };

                this.restoreCamera = function () {
                    //var camerapos = vector3Dref.cameraSaves.pop();
                    //vector3Dref.parent.camera.position.copy(camerapos);
                    //camera.position.copy( savedCamera.cameraPosition );
                    //orbitControls.target.copy( savedCamera.targetPosition );
                    if (vector3Dref.cameraSaves.length >= 1) {
                        var camerapos = vector3Dref.cameraSaves.pop();
                        pageobject.camera.matrix.fromArray(camerapos.position);
                        pageobject.camera.matrix.decompose(pageobject.camera.position, pageobject.camera.quaternion, pageobject.camera.scale);
                    }
                    /*savedCamera = JSON.stringify({
                        cameraPosition: camera.position,
                        targetPosition: orbitControls.target
                    });*/
                };
                this.getmarkupScene = function () {
                    if (vector3Dref.markupidx != null) {
                        return vector3Dref.markupidx;
                    }
                    for (var childix = 0; childix < vector3Dref.scene.children.length; childix++) {
                        if (vector3Dref.scene.children[childix].uuid === vector3Dref.markupuuid) {
                            vector3Dref.markupidx = childix;
                        }
                    }
                    return vector3Dref.markupidx;
                };
                this.getmodelGroup = function () {
                    if (vector3Dref.modelidx != null) {
                        return vector3Dref.modelidx;
                    }
                    for (var childix = 0; childix < vector3Dref.scene.children.length; childix++) {
                        if (vector3Dref.scene.children[childix].uuid === vector3Dref.modeluuid) {
                            vector3Dref.modelidx = childix;
                        }
                    }
                    return vector3Dref.modelidx;
                };
                
                this.getAddeddScenes = function () {


                    if(vector3Dref.addedScenes.length > 0){


                        for(var asi = 0; asi < vector3Dref.addedScenes.length; asi++){
                            if(vector3Dref.addedScenes[asi].modelidx == null){
                                for (var childix = 0; childix < vector3Dref.scene.children.length; childix++) {
                                    if (vector3Dref.scene.children[childix].uuid === vector3Dref.addedScenes[asi].modeluuid) {
                                        vector3Dref.addedScenes[asi].modelidx = childix;
                                    }
                                }
        
                            }
                        }
                        
                    }
                    
                    return vector3Dref.addedScenes;
                };



                this.getMarkupChildren = function () {
                    if (vector3Dref.getmarkupScene() == null) {
                        return [];
                    }
                    else {
                        return vector3Dref.scene.children[vector3Dref.markupidx].children;
                    }
                };
                
                this.setAddedMeshUserData = function(addedindex , index, groupindex, userData){

                    if(groupindex == null){
                        vector3Dref.getAddedMeshChildren()[addedindex][index].userData = userData;
                    }else{
                        vector3Dref.getAddedMeshChildren()[addedindex][index].children[groupindex] = userData;
                    }


                };
                
                this.setMeshUserData = function(index, groupindex, userData){
                    
                    if(groupindex == null){
                        vector3Dref.getMeshChildren()[index].userData = userData;
                    }else{
                        vector3Dref.getMeshChildren()[index].children[groupindex] = userData;
                    }
                    
                    
                };
                
                this.getAddedMeshChildren = function(){
                    
                    var addedChildren = [];


                    if(vector3Dref.getAddeddScenes().length == 0){
                        return addedChildren;
                    }


                    if(vector3Dref.addedScenes.length == 0){
                        return addedChildren;
                    }else{
                        for(var asi = 0; asi < vector3Dref.addedScenes.length; asi++){
                            addedChildren.push(vector3Dref.scene.children[vector3Dref.addedScenes[asi].modelidx].children);
                        }
                        
                        
                    }
                    return addedChildren;
                    
                };

                this.getModelScene = function(){
                    if (vector3Dref.getmodelGroup() == null) {
                        return [];
                    }
                    else {
                        //return vector3Dref.scene.children[vector3Dref.modelidx].children[0].children;
                        return vector3Dref.scene.children[vector3Dref.modelidx];
                    }

                };

                this.getMeshByNameLoop = function(name){

                    var meshObject = null;

                    for (var i = 0; i < mesharray.length; i++) {
                        
                        if (mesharray[i].type == "Group") {
                            for (var j = 0; j < mesharray[i].children.length; j++) {
                                if ( mesharray[i].children[j].type == "Mesh") {

                                    if (mesharray[i].children[j].name == name) {
                                        meshObject = mesharray[i].children[j];      
                                        //name is unique so return early
                                        return meshObject;
                                    }
                                }
                            }
                        }else  if ( mesharray[i].type == "Mesh") {
                            if (mesharray[i].name === "") {
                                continue;
                            }
    
                            if (mesharray[i].name == name) {

                                meshObject = mesharray[i];      
                                //name is unique so return early
                                return meshObject;
                            }
    
                        }

                    }

                    return meshObject;
                };

                this.getmarkupMeshByName = function(name){
                    var meshObject = null;

                    if (vector3Dref.getmodelGroup() == null) {
                        return meshObject;
                    }

                    if (vector3Dref.getmarkupScene() == null) {
                        return meshObject;
                    }

                    //var markuparray = vector3Dref.getMarkupChildren();
                    //var meshObject = markuparray.getObjectByName( name, true );

                    var meshObject = vector3Dref.scene.children[vector3Dref.markupidx].getObjectByName( name, true );
                    
                    
                    return meshObject;

                };





                this.getMeshByName = function(name){
                    
                    var meshObject = null;

                    if (vector3Dref.getmodelGroup() == null) {
                        return meshObject;
                    }

                    var meshObject = vector3Dref.scene.children[vector3Dref.modelidx].getObjectByName( name, true );
                    
                    return meshObject;

                };

                this.getMeshChildren = function () {
                    if (vector3Dref.getmodelGroup() == null) {
                        return [];
                    }
                    else {
                        //return vector3Dref.scene.children[vector3Dref.modelidx].children[0].children;
                        return vector3Dref.scene.children[vector3Dref.modelidx].children;
                    }
                };
                this.getAttachedMarkups = function(uuid){
                    var markupmesharray = [];
                    var markuparray = vector3Dref.getMarkupChildren();

                    for (var mmix = 0; mmix < markuparray.length ; mmix++){
                        if(markuparray[mmix].userData.connectedMesh == uuid){
                            markupmesharray.push(markuparray[mmix]);
                        }
                    }

                    return markupmesharray;

                };
                this.getBelowIntersections = function(origin, walkheight){

                    //var raycaster = new THREE.raycaster();
                    //raycaster.ray.origin.copy( scope.camera.position );
                    //raycaster.ray.origin.y -= scope.walkheight;

                    var intersectFaceObjects = [];
                    var intersectObjects = [];

                    var zoffset = vector3Dref.getfloorheight();


                    var basefloor = walkheight - zoffset;

                    var distbase = origin.z - basefloor;
                    //console.log(distbase);

                    if (distbase > zoffset){
                        var addval = distbase * 0.1;
                        zoffset = distbase + addval;

                    }

                    //raycaster = new THREE.Raycaster( new THREE.Vector3(), new THREE.Vector3( 0, - 1, 0 ), 0, 10 );

                    

                            /*Raycaster( origin : Vector3, direction : Vector3, near : Float, far : Float ) {
                            origin — The origin vector where the ray casts from.
                            direction — The direction vector that gives direction to the ray. Should be normalized.
                            near — All results returned are further away than near. Near can't be negative. Default value is 0.
                            far — All results returned are closer than far. Far can't be lower than near. Default value is Infinity.*/
                            

                    var directionZN = new THREE.Vector3(0, 0, -1).normalize();
                    var directionZP = new THREE.Vector3(0, 0, 1).normalize();

                    var mesharray = vector3Dref.getMeshChildren();

                    var addedmesharray = vector3Dref.getAddedMeshChildren();
                    
                    //raycaster = new THREE.Raycaster( new THREE.Vector3(), new THREE.Vector3( 0, - 1, 0 ), 0, 10 );
                    //raycaster.ray.origin.copy( controls.getObject().position );
                    //raycaster.ray.origin.y -= 10;
                    //origin.z -= zoffset;



                    var raycaster = new THREE.Raycaster( origin, directionZN, 0, zoffset);
                    
                                        
                    intersectObjects = raycaster.intersectObjects(mesharray, true);

                    for (var i = 0; i < intersectObjects.length; i++) {
                            
                        if (intersectObjects[i].object.visible && intersectObjects[i].face != null) {
                            
                            if(intersectObjects[i].distance < zoffset){
                                intersectFaceObjects.push(intersectObjects[i]);
                            } 
                            
                        }

                    }
                    

                    for (var ami = 0; ami < addedmesharray.length; ami++){
                        
                        intersectObjects = raycaster.intersectObjects(addedmesharray[ami], true);

                        for (var i = 0; i < intersectObjects.length; i++) {
                                
                            if (intersectObjects[i].object.visible && intersectObjects[i].face != null) {
                                
                                if(intersectObjects[i].distance < zoffset){
                                    intersectFaceObjects.push(intersectObjects[i]);
                                } 
                                
                            }
    
                        }
    
                    } 


                    
                    return intersectFaceObjects;

                    
                };

                this.getmeterunit = function(){

                    if(vector3Dref.unit == 1){
                        //var zoffset = (1.8 - zdiff) * modelscale;
                        return 1;

                    }else if (vector3Dref.unit == 0){
                        //zoffset = (1800 - zdiff) * modelscale;
                        return 1000;
                        
                    }else if(vector3Dref.unit == 2){
                        return 3.3;
                    }else if(vector3Dref.unit == 3){
                        return 39.37;
                    }else if (vector3Dref.unit == 4){
                        return 100;
                    }

                    /*if(unit == 1){
                        thispage.walkthroughcontrol.setwalkUnit(1);
                    }else if(unit == 0){
                        thispage.walkthroughcontrol.setwalkUnit(1000);
                    }else if(unit == 2){
                        thispage.walkthroughcontrol.setwalkUnit(3.3);
                    }*/


                };
                this.getmminUnit = function(value){

                    if(vector3Dref.unit == 1){
                        //var zoffset = (1.8 - zdiff) * modelscale;
                        return value / 1000;

                    }else if (vector3Dref.unit == 0){
                        //zoffset = (1800 - zdiff) * modelscale;
                        return value;
                        
                    }else if(vector3Dref.unit == 2){
                        return (value / 25.4) / 12;
                        
                    }else if(vector3Dref.unit == 3){
                        return (value / 25.4);
                    }else if(vector3Dref.unit == 4){
                        return (value / 10);
                    }

                };
                this.getfloorheight = function(){


                    if(vector3Dref.unit == 1){
                        //var zoffset = (1.8 - zdiff) * modelscale;
                        return 1.8;

                    }else if (vector3Dref.unit == 0){
                        //zoffset = (1800 - zdiff) * modelscale;
                        return 1800;
                        
                    }else if(vector3Dref.unit == 2){
                        return 5.9;
                    }else if(vector3Dref.unit == 3){
                        return 69;
                    }else if (vector3Dref.unit == 4){
                        return 180;
                    }

                };

                function getmeshName(name, type){

                    var retname = name;

                    var threerev = parseInt(THREE.REVISION);
                

                    if (threerev <= 127){

                        retname = name.substring(0, name.length - 2);
                            
                    }else{

                        if(type == "LineSegments"){
                            retname = name.substring(0, name.length - 2);
                            
                        }else{
                            retname = name;
                        }
    
                    }


                    return retname;


                }


                this.setBlockRef = function(Meshobject){
                    if(Meshobject.name.length == MESHNameGroup){
                        //var meshname = Meshobject.name.substring(0, Meshobject.name.length - 2);

                        var meshname = getmeshName(Meshobject.name, Meshobject.type);


                        var mindex = vector3Dref.blocklistquick.indexOf(meshname);
                        if(mindex != -1){
                            Meshobject.userData.blockRef = mindex;
                        }
                        
                    }else if(Meshobject.name.length == MESHName){
                        var mindex = vector3Dref.blocklistquick.indexOf(Meshobject.name);
                        if(mindex != -1){
                            Meshobject.userData.blockRef = mindex;
                        }

                    }else if(Meshobject.name.length == RVTMESHName){
                        var mindex = vector3Dref.blocklistquick.indexOf(Meshobject.name);
                        if(mindex != -1){
                            Meshobject.userData.blockRef = mindex;
                        }
                    }

                    
                };

                this.checkNewHeight = function(intersects){
                    var returnVector = null;

                    var zoffset = vector3Dref.getfloorheight();

                    //var origin = parent.camera.position.clone();
                    //var directionZN = new THREE.Vector3(0, 0, -1).normalize();
                    //var directionZP = new THREE.Vector3(0, 0, 1).normalize();

                    var distance = 0;
                    var distdiff = 0;
                    var ipoint = null;
                    var rxbimtype = null;

                    if (intersects.length > 0) {
                        for (var i = 0; i < intersects.length; i++) {
                            
                            if (intersects[i].object.visible && intersects[i].face != null ) {

                                //const MESHNameGroup = 24;
                                //const MESHName = 22;
                
                                if(intersects[i].object.userData.blockRef == undefined){

                                    vector3Dref.setBlockRef(intersects[i].object);
    
                                }
                                
                                if(intersects[i].object.userData.blockRef){
                                    var rxBimtype = Number(vector3Dref.blocklist[intersects[i].object.userData.blockRef].RxBimType);
                                    //console.log( ' checkNewHeight RxBimType: ', rxBimtype );

                                    if(rxBimtype >= 31 && rxBimtype <= 36 || rxBimtype == 23 || rxBimtype == 3){
                                        
                                        distance = intersects[i].distance;
                                        ipoint = intersects[i].point.clone();
                                        break;
            
                                    }
        
                                }else{
                                    distance = intersects[i].distance;
                                    ipoint = intersects[i].point.clone();
                                    break;

                                }
                                
                                
    
                            }
                        }
    
                        
                        //distdiff = Math.abs(zoffset - distance);
                        
    
                        if(ipoint != null){
                            returnVector = ipoint;
                        }
        
    
                    }
                    return {vec : returnVector, dist : distance};


                };

                this.checkfloorheightAll = function(intersects, zoffset, origin, directionZP, returnVector){
                    var distance = 0;
                    var distdiff = 0;
                    var ipoint = null;
                    var rxbimtype = null;
                    
                    if (intersects.length > 0) {
                        for (var i = 0; i < intersects.length; i++) {
                            
                            if (intersects[i].object.visible && intersects[i].face != null ) {
                                
                                if(intersects[i].object.userData.blockRef == undefined){

                                    vector3Dref.setBlockRef(intersects[i].object);
    
                                }
                                

                                if(intersects[i].object.userData.blockRef){

                                    var rxBimtype = Number(vector3Dref.blocklist[intersects[i].object.userData.blockRef].RxBimType);
                                    //console.log( 'checkfloorheight RxBimType:', rxBimtype );

                                    if(rxBimtype >= 31 && rxBimtype <= 36 || rxBimtype == 23 || rxBimtype == 3){
                                        
                                        distance = intersects[i].distance;
                                        ipoint = intersects[i].point.clone();
                                        break;
            
                                    }
                                    
        
                                }
                                
                                
    
                            }
                        }
    
                        
                        distdiff = Math.abs(zoffset - distance);
                        
    
                        if(ipoint != null){
                            /*const direction3 = (new THREE.Vector3())
                            .subVectors(DocObj.getCurrentpage().camera.position, ipoint)
                            .normalize();*/                
                            returnVector = new THREE.Vector3().copy(directionZP.multiplyScalar(distdiff).add(origin));
                            //DocObj.getCurrentpage().camera.position.copy(direction2.multiplyScalar(distdiff).add(ipoint));
                            //parent.camera.position.copy();
                            //console.log(parent.camera.position);
                            //parent.camera.lookAt(partcenter);
            
                        }
        
    
                    }
                    return returnVector;
                };

                this.checkfloorheight = function(){
                    
                    var returnVector = null;

                    //var zdiff = 0;
                    //var modelscale = vector3Dref.scale;
    
                    var zoffset = vector3Dref.getfloorheight();

                    var mesharray = vector3Dref.getMeshChildren();
                    var addedmesharray = vector3Dref.getAddedMeshChildren();

                    var raycaster = new THREE.Raycaster();

                    var origin = parent.camera.position.clone();

                    var directionZN = new THREE.Vector3(0, 0, -1).normalize();
                    var directionZP = new THREE.Vector3(0, 0, 1).normalize();
                    
                    raycaster.set ( origin, directionZN);
    
                    var intersects = raycaster.intersectObjects(mesharray, true);


                    returnVector = vector3Dref.checkfloorheightAll(intersects, zoffset, origin, directionZP,  returnVector);

                    for (var ami = 0; ami < addedmesharray.length; ami++){
                        
                        intersects = raycaster.intersectObjects(addedmesharray[ami], true);

                        returnVector = vector3Dref.checkfloorheightAll(intersects, zoffset, origin, directionZP,  returnVector);
                        //vector3Dref.getarrayMeshIDs(addedmesharray[ami], meshids, meshuserData);
                        
                    } 


                    //this.checkfloorheightAll = function(intersects, zoffset, origin,  returnVector){
    
                    
                    return returnVector;

                };                

                function resetmarkupmesh(markupmesh){

                    if (markupmesh.type == "Mesh") {
                        markupmesh.position.x = markupmesh.userData.originalpositionX;
                        markupmesh.position.y = markupmesh.userData.originalpositionY;
                        markupmesh.position.z = markupmesh.userData.originalpositionZ;
                    }

                }

                function resetmesh(mesh){

                    //for (var partix = 0; partix < mesharray.length; partix++) {

                        if(mesh.type == "Group"){
                            for (var gpartix = 0; gpartix < mesh.children.length; gpartix++) {
                                if (mesh.children[gpartix].type == "Mesh" || mesh.children[gpartix].type == "LineSegments") {
                                    mesh.children[gpartix].position.x = 0;
                                    mesh.children[gpartix].position.y = 0;
                                    mesh.children[gpartix].position.z = 0;
                                }
                            }

                        }else{

                            if (mesh.type == "Mesh") {
                                mesh.position.x = 0;
                                mesh.position.y = 0;
                                mesh.position.z = 0;
                            }
    
                            
                        }

                    //}


                }

                
                

                function expandmesh(mesh, distance, pxplodefactor){
                    
                    var bcontinue = false;
                    
                    var meshmarkups = [];


                    if(mesh.type == "Group"){
                        for (var gpartix = 0; gpartix < mesh.children.length; gpartix++) {
                            if (mesh.children[gpartix].type == "Mesh" || mesh.children[gpartix].type == "LineSegments") {
                            
                                if (mesh.children[gpartix].geometry.boundingSphere.center != undefined) {
                                    var partcenter = mesh.children[gpartix].geometry.boundingSphere.center.clone();
                                    
                                    meshmarkups = vector3Dref.getAttachedMarkups(mesh.children[gpartix].uuid);

                                    if (pxplodefactor != 0) {
                                        var ppartcenter = partcenter.clone();
                                        var ppartoffset = ppartcenter.multiplyScalar(pxplodefactor);
                                        
                                        ppartcenter.multiplyScalar(vector3Dref.scale);
                                        
                                        mesh.children[gpartix].position.sub(ppartoffset);
                                        
                                        if(meshmarkups.length > 0){
                                            for (var mmix = 0; mmix < meshmarkups.length ; mmix++){
                                                meshmarkups[mmix].position.sub(ppartoffset);
                                            }
                        
                                        }

                                    }
                                    var partoffset = partcenter.multiplyScalar(distance);
                                    partoffset.multiplyScalar(vector3Dref.scale);
                                    mesh.children[gpartix].position.add(partoffset);

                                    if(meshmarkups.length > 0){
                                        for (mmix = 0; mmix < meshmarkups.length ; mmix++){
                                            meshmarkups[mmix].position.add(partoffset);
                                        }
                    
                                    }
                                }

                            }                            

                        }
                    }else{
                        if (mesh.type == "Mesh") {

                            if (mesh.name === "") {
                                return bcontinue;
                            }

                            if (mesh.geometry.boundingSphere.center != undefined) {
                                var partcenter = mesh.geometry.boundingSphere.center.clone();
                                //console.log(partcenter);

                                meshmarkups = vector3Dref.getAttachedMarkups(mesh.uuid);

                                if (pxplodefactor != 0) {
                                    var ppartcenter = partcenter.clone();
                                    var ppartoffset = ppartcenter.multiplyScalar(pxplodefactor);
                                    ppartcenter.multiplyScalar(vector3Dref.scale);
                                    mesh.position.sub(ppartoffset);
                                    
                                    if(meshmarkups.length > 0){
                                        for (mmix = 0; mmix < meshmarkups.length ; mmix++){
                                            meshmarkups[mmix].position.sub(ppartoffset);
                                        }
                    
                                    }

                                }
                                var partoffset = partcenter.multiplyScalar(distance);
                                partoffset.multiplyScalar(vector3Dref.scale);
                                mesh.position.add(partoffset);
                                
                                if(meshmarkups.length > 0){
                                    for (mmix = 0; mmix < meshmarkups.length ; mmix++){
                                        meshmarkups[mmix].position.add(partoffset);
                                    }
                
                                }

                                

                            }

                        }

                    }

                    return bcontinue;

                }

                this.explode = function (distance) {
                    
                    if (!vector3Dref.doexplode) {
                        return;
                    }
                    var mesharray = vector3Dref.getMeshChildren();

                    if (mesharray.length == 0) {
                        return;
                    }

                    var markuparray = vector3Dref.getMarkupChildren();

                    var meshaddedarray = vector3Dref.getAddedMeshChildren();
                    

                    var scenepos = vector3Dref.originalpos.clone();
                    //var scenepos = vector3Dref.boxCenter.clone();

                    if (distance == 0) {
                        if (mesharray.length > 0) {

                            for (var partix = 0; partix < mesharray.length; partix++) {

                                resetmesh(mesharray[partix]);

                                /*if(mesharray[partix].type == "Group"){
                                    for (var gpartix = 0; gpartix < mesharray[partix].children.length; gpartix++) {
                                        if (mesharray[partix].children[gpartix].type == "Mesh" || mesharray[partix].children[gpartix].type == "LineSegments") {
                                            mesharray[partix].children[gpartix].position.x = 0;
                                            mesharray[partix].children[gpartix].position.y = 0;
                                            mesharray[partix].children[gpartix].position.z = 0;
                                        }
                                    }
                                }*/

                                /*if (mesharray[partix].type == "Mesh") {
                                    mesharray[partix].position.x = 0;
                                    mesharray[partix].position.y = 0;
                                    mesharray[partix].position.z = 0;
                                }*/

                            }
                        }
                        if(markuparray.length > 0){
                            for (var mix = 0; mix < markuparray.length; mix++) {

                                resetmarkupmesh(markuparray[mix]);
                                /*if (markuparray[mix].type == "Mesh") {
                                    markuparray[mix].position.x = markuparray[mix].userData.originalpositionX;
                                    markuparray[mix].position.y = markuparray[mix].userData.originalpositionY;
                                    markuparray[mix].position.z = markuparray[mix].userData.originalpositionZ;
                                }*/
                            }
                        }

                        if(meshaddedarray.length > 0){

                            for (var adix = 0; adix < meshaddedarray.length; adix++) {
                                for (var maix = 0; maix < meshaddedarray[adix].length; maix++) {

                                    resetmesh(meshaddedarray[adix][maix]);

                                }


                            }
                        }

                        if (this.pxplodefactor != 0) {
                            var pscenepos = vector3Dref.originalpos.clone();
                            //var pscenepos = vector3Dref.boxCenter.clone(); 
                            var psceneoffset = pscenepos.multiplyScalar(this.pxplodefactor);
                            vector3Dref.scene.position.sub(psceneoffset);
                        }
                        this.pxplodefactor = 0;
                        return;
                    }
                    //var size = vector3Dref.boxSize;
                    var sizefactor = Math.round(vector3Dref.boxSize);
                    sizefactor *= 0.00015;
                    distance *= sizefactor;
                    //var meshmarkups = [];
                    
                    //- vector3Dref.pxplodefactor;
                    for (partix = 0; partix < mesharray.length; partix++) {
                        //var partcenter = this.partlist[partix].center;

                        //distance, pxplodefactor
                        if (expandmesh(mesharray[partix], distance, this.pxplodefactor)){
                            continue;
                        }

                        
                    }


                    if(meshaddedarray.length > 0){

                        for (var adix = 0; adix < meshaddedarray.length; adix++) {
                            for (var maix = 0; maix < meshaddedarray[adix].length; maix++) {

                                if (expandmesh(meshaddedarray[adix][maix], distance, this.pxplodefactor)){
                                    continue;
                                }

                            }

                        }
                    }



                    vector3Dref.explodebox = new THREE.Box3().setFromObject(vector3Dref.scene);
                    vector3Dref.explodeboxSize = vector3Dref.explodebox.getSize(new THREE.Vector3()).length();
                    vector3Dref.explodeboxCenter = vector3Dref.explodebox.getCenter(new THREE.Vector3());
                    //parent.camera.lookAt(vector3Dref.explodeboxCenter.x, vector3Dref.explodeboxCenter.y, vector3Dref.boxCenter.z);
                    if (this.pxplodefactor != 0) {
                        pscenepos = vector3Dref.originalpos.clone();
                        //pscenepos = vector3Dref.boxCenter.clone(); 
                        psceneoffset = pscenepos.multiplyScalar(this.pxplodefactor);
                        vector3Dref.scene.position.sub(psceneoffset);
                    }
                    /*if (this.pxplodefactor != 0) {
                        //var pscenepos = vector3Dref.originalpos.clone();
                        var pscenepos = vector3Dref.boxCenter.clone();
                        var psceneoffset = pscenepos.multiplyScalar(this.pxplodefactor);
                        vector3Dref.scene.position.sub(psceneoffset);
        
                    }*/
                    var sceneoffset = scenepos.multiplyScalar(distance);
                    //sceneoffset.x /= vector3Dref.scale;
                    //sceneoffset.y /= vector3Dref.scale;
                    //sceneoffset.z /= vector3Dref.scale;
                    //vector3Dref.scene.position.add(sceneoffset);
                    vector3Dref.scene.updateMatrix();
                    vector3Dref.pxplodefactor = distance;
                };

                function getMeshMarkuporigcenter(mesh, center){
                    var positionvector = center.clone();

                    //markuparray[mix].position.x = markuparray[mix].userData.originalpositionX;
                    //markuparray[mix].position.y = markuparray[mix].userData.originalpositionY;
                    //markuparray[mix].position.z = markuparray[mix].userData.originalpositionZ;

                    if(mesh.userData.originalpositionX == undefined){
                        mesh.userData.originalpositionX = center.x;
                    }
                    if(mesh.userData.originalpositionY == undefined){
                        mesh.userData.originalpositionY = center.y;
                    }
                    if(mesh.userData.originalpositionZ == undefined){
                        mesh.userData.originalpositionZ = center.z;
                    }

                    positionvector.set(mesh.userData.originalpositionX,mesh.userData.originalpositionY,mesh.userData.originalpositionZ);

                    return positionvector;

                }
                function getMeshorigcenter(mesh, center){

                    var positionvector = center.clone();

                    if(mesh.userData.centerPosX == undefined){
                        mesh.userData.centerPosX = center.x;
                    }
                    if(mesh.userData.centerPosY == undefined){
                        mesh.userData.centerPosY = center.y;
                    }
                    if(mesh.userData.centerPosZ == undefined){
                        mesh.userData.centerPosZ = center.z;
                    }

                    positionvector.set(mesh.userData.centerPosX,mesh.userData.centerPosY,mesh.userData.centerPosZ);

                    return positionvector;

                }


                function newexpandmesh_ex(mesh, distance, pxplodefactor){

                    var bcontinue = false;
                    var meshmarkups = [];


                    if(mesh.type == "Group"){
                        for (var gpartix = 0; gpartix < mesh.children.length; gpartix++) {
                            if (mesh.children[gpartix].type == "Mesh" || mesh.children[gpartix].type == "LineSegments") {
                                
                                if (mesh.children[gpartix].geometry.boundingSphere.center != undefined) {
                                        
                                    var partcenter = getMeshorigcenter(mesh.children[gpartix], mesh.children[gpartix].geometry.boundingSphere.center);

                                        //var partcenter = mesharray[partix].children[gpartix].geometry.boundingSphere.center.clone();
                                    meshmarkups = vector3Dref.getAttachedMarkups(mesh.children[gpartix].uuid);

                                    var gorigotransform = partcenter.sub( vector3Dref.boxCenter );
                                            
                                    gorigotransform.multiplyScalar(distance);


                                        //mesharray[partix].children[gpartix].position.copy(gorigotransform);
                                    mesh.children[gpartix].position.copy(gorigotransform);

                                    //var movevector = gorigotransform.sub(partcenter);

                                    if(meshmarkups.length > 0){
                                        for (var mmix = 0; mmix < meshmarkups.length ; mmix++){
                                            //meshmarkups[mmix].position.sub(ppartoffset);
                                            
                                            var gmarkupcenter = getMeshMarkuporigcenter(meshmarkups[mmix], meshmarkups[mmix].position);
                                            
                                            gmarkupcenter.add(gorigotransform);

                                            //gorigotransform = gmarkupcenter.sub( vector3Dref.boxCenter );
                                            //gorigotransform.multiplyScalar(distance);
                                            meshmarkups[mmix].position.copy(gmarkupcenter);
    
                                            //meshmarkups[mmix].position.add(direction.clone().multiplyScalar(distance));
                                        }
                    
                                    }
                                }
                            }
                        }
                    }else{
                        if (mesh.type == "Mesh") {

                            if (mesh.name === "") {
                                return bcontinue;
                            }

                            if (mesh.geometry.boundingSphere.center != undefined) {
                                var spartcenter = getMeshorigcenter(mesh, mesh.geometry.boundingSphere.center);
                                meshmarkups = vector3Dref.getAttachedMarkups(mesh.uuid);

                                var origotransform = spartcenter.sub( vector3Dref.boxCenter );

                                origotransform.multiplyScalar(distance);

                                mesh.position.copy(origotransform);

                                //movevector = origotransform.sub(spartcenter);

                                if(meshmarkups.length > 0){
                                    for (mmix = 0; mmix < meshmarkups.length ; mmix++){

                                        //meshmarkups[mmix].position.sub(ppartoffset);
                                        //meshmarkups[mmix].position.add(direction.clone().multiplyScalar(distance));

                                        var markupcenter = getMeshMarkuporigcenter(meshmarkups[mmix], meshmarkups[mmix].position);
                                        
                                        markupcenter.add(origotransform);

                                        //origotransform = markupcenter.sub( vector3Dref.boxCenter );
                                        //origotransform.multiplyScalar(distance);
                                        meshmarkups[mmix].position.copy(markupcenter);

                                    }
                
                                }

                            }
                        }
                    }

                    return bcontinue;

                }


                function newexpandmesh(mesh, distance, pxplodefactor){

                    var bcontinue = false;
                    
                    var meshmarkups = [];


                    if(mesh.type == "Group"){
                        for (var gpartix = 0; gpartix < mesh.children.length; gpartix++) {
                            if (mesh.children[gpartix].type == "Mesh" || mesh.children[gpartix].type == "LineSegments") {


                                if (mesh.children[gpartix].geometry.boundingSphere.center != undefined) {

                                    var partcenter = getMeshorigcenter(mesh.children[gpartix], mesh.children[gpartix].geometry.boundingSphere.center);

                                    //var partcenter = mesharray[partix].children[gpartix].geometry.boundingSphere.center.clone();
                                    meshmarkups = vector3Dref.getAttachedMarkups(mesh.children[gpartix].uuid);

                                    if (pxplodefactor != 0) {
                                        //var ppartcenter = partcenter.clone();
                                        //var ppartoffset = ppartcenter.multiplyScalar(this.pxplodefactor);

                                        var direction = partcenter.sub( vector3Dref.boxCenter ).normalize();
                                        partcenter.add(direction.clone().multiplyScalar(distance));

                                        mesh.children[gpartix].position.copy(partcenter);
                                        


                                        //ppartcenter.multiplyScalar(vector3Dref.scale);
                                        //mesharray[partix].children[gpartix].position.set(ppartoffset);


                                        if(meshmarkups.length > 0){
                                            for (var mmix = 0; mmix < meshmarkups.length ; mmix++){
                                                //meshmarkups[mmix].position.sub(ppartoffset);
                                                meshmarkups[mmix].position.add(direction.clone().multiplyScalar(distance));
                                            }
                        
                                        }

                                    }

                                }

                            }

                        }
                    }else{

                        if (mesh.type == "Mesh") {

                            if (mesh.name === "") {
                                return bcontinue;
                            }
    
                            if (mesh.geometry.boundingSphere.center != undefined) {
                                //var spartcenter = mesharray[partix].geometry.boundingSphere.center.clone();
    
                                var spartcenter = getMeshorigcenter(mesh, mesh.geometry.boundingSphere.center);
                                //console.log(partcenter);
    
                                meshmarkups = vector3Dref.getAttachedMarkups(mesh.uuid);
    
                                if (pxplodefactor != 0) {
                                    //var sppartcenter = spartcenter.clone();
                                    //var sppartoffset = sppartcenter.multiplyScalar(this.pxplodefactor);
    
                                    var direction = spartcenter.sub( vector3Dref.boxCenter ).normalize();
                                    spartcenter.add(direction.clone().multiplyScalar(distance));
                                    mesh.position.copy(spartcenter);
    
    
                                    //ppartcenter.multiplyScalar(vector3Dref.scale);
                                    //mesharray[partix].position.sub(ppartoffset);
    
                                    //mesharray[partix].position.set(sppartoffset);
    
                                    if(meshmarkups.length > 0){
                                        for (mmix = 0; mmix < meshmarkups.length ; mmix++){
    
                                            //meshmarkups[mmix].position.sub(ppartoffset);
                                            meshmarkups[mmix].position.add(direction.clone().multiplyScalar(distance));
                                        }
                    
                                    }
    
                                }
                            }
                        }
                    }

                    return bcontinue;

                }

                this.newExplode = function(distance){
                    if (!vector3Dref.doexplode) {
                        return;
                    }

                    if(vector3Dref.active3DView){
                        return;
                    }

                    var mesharray = vector3Dref.getMeshChildren();
                    if (mesharray.length == 0) {
                        return;
                    }

                    var meshaddedarray = vector3Dref.getAddedMeshChildren();

                    var markuparray = vector3Dref.getMarkupChildren();

                    //var scenepos = vector3Dref.originalpos.clone();
                    //var scenepos = vector3Dref.boxCenter.clone();
                    if (distance == 0) {
                        if (mesharray.length > 0) {
                            for (var partix = 0; partix < mesharray.length; partix++) {
                                if(mesharray[partix].type == "Group"){
                                    for (var gpartix = 0; gpartix < mesharray[partix].children.length; gpartix++) {
                                        if (mesharray[partix].children[gpartix].type == "Mesh" || mesharray[partix].children[gpartix].type == "LineSegments") {
                                            mesharray[partix].children[gpartix].position.x = 0;
                                            mesharray[partix].children[gpartix].position.y = 0;
                                            mesharray[partix].children[gpartix].position.z = 0;
                                        }
                                    }
                                }

                                if (mesharray[partix].type == "Mesh") {
                                    mesharray[partix].position.x = 0;
                                    mesharray[partix].position.y = 0;
                                    mesharray[partix].position.z = 0;
                                }
                            }
                        }
                        if(markuparray.length > 0){
                            for (var mix = 0; mix < markuparray.length; mix++) {
                                if (markuparray[mix].type == "Mesh") {
                                    markuparray[mix].position.x = markuparray[mix].userData.originalpositionX;
                                    markuparray[mix].position.y = markuparray[mix].userData.originalpositionY;
                                    markuparray[mix].position.z = markuparray[mix].userData.originalpositionZ;
                                }
                            }
                        }


                        if(meshaddedarray.length > 0){

                            for (var adix = 0; adix < meshaddedarray.length; adix++) {
                                for (var maix = 0; maix < meshaddedarray[adix].length; maix++) {

                                    resetmesh(meshaddedarray[adix][maix]);

                                }


                            }
                        }

    

                        this.pxplodefactor = 0;
                        return;
                    }
                    //var size = vector3Dref.boxSize;

                    //var meterunit = vector3Dref.getmeterunit();

                    //var sizefactor = Math.round(vector3Dref.boxSize);
                    //sizefactor *= 0.00015;
                    distance *= 0.2;
                    var meshmarkups = [];

                    //console.log(distance);
                    //console.log(vector3Dref.pxplodefactor);
                    //meshmarkups[mmix].position.add(direction.clone().multiplyScalar(distance));

                    //- vector3Dref.pxplodefactor;

                        
                    //calculate new box from current box.


                    for (partix = 0; partix < mesharray.length; partix++) {
                        //var partcenter = this.partlist[partix].center;


                        if (newexpandmesh_ex(mesharray[partix], distance, vector3Dref.pxplodefactor)){
                            continue;
                        }


                        //newexpandmesh_ex()

                        /*if(mesharray[partix].type == "Group"){
                            for (var gpartix = 0; gpartix < mesharray[partix].children.length; gpartix++) {
                                if (mesharray[partix].children[gpartix].type == "Mesh" || mesharray[partix].children[gpartix].type == "LineSegments") {


                                    if (mesharray[partix].children[gpartix].geometry.boundingSphere.center != undefined) {

                                        var partcenter = getMeshorigcenter(mesharray[partix].children[gpartix], mesharray[partix].children[gpartix].geometry.boundingSphere.center);

                                        //var partcenter = mesharray[partix].children[gpartix].geometry.boundingSphere.center.clone();
                                        meshmarkups = vector3Dref.getAttachedMarkups(mesharray[partix].children[gpartix].uuid);

                                        var gorigotransform = partcenter.sub( vector3Dref.boxCenter );
                                            
                                        gorigotransform.multiplyScalar(distance);

                                        mesharray[partix].children[gpartix].position.copy(gorigotransform);


                                        if(meshmarkups.length > 0){
                                            for (var mmix = 0; mmix < meshmarkups.length ; mmix++){
                                                //meshmarkups[mmix].position.sub(ppartoffset);
                                                meshmarkups[mmix].position.add(direction.clone().multiplyScalar(distance));
                                            }
                        
                                        }

                                    }

                                }

                            }
                        }*/

                        /*if (mesharray[partix].type == "Mesh") {

                            if (mesharray[partix].name === "") {
                                continue;
                            }
    
                            if (mesharray[partix].geometry.boundingSphere.center != undefined) {
                                //var spartcenter = mesharray[partix].geometry.boundingSphere.center.clone();

                                var spartcenter = getMeshorigcenter(mesharray[partix], mesharray[partix].geometry.boundingSphere.center);
                                //console.log(partcenter);


                                meshmarkups = vector3Dref.getAttachedMarkups(mesharray[partix].uuid);

                                var origotransform = spartcenter.sub( vector3Dref.boxCenter );

                                origotransform.multiplyScalar(distance);

                                mesharray[partix].position.copy(origotransform);
                                
                                if(meshmarkups.length > 0){
                                    for (mmix = 0; mmix < meshmarkups.length ; mmix++){

                                        //meshmarkups[mmix].position.sub(ppartoffset);
                                        meshmarkups[mmix].position.add(direction.clone().multiplyScalar(distance));
                                    }
                
                                }
                            }
                        }*/
                    }

                    if(meshaddedarray.length > 0){

                        for (var adix = 0; adix < meshaddedarray.length; adix++) {
                            for (var maix = 0; maix < meshaddedarray[adix].length; maix++) {

                                if (newexpandmesh_ex(meshaddedarray[adix][maix], distance, vector3Dref.pxplodefactor)){
                                    continue;
                                }
                            }
                        }
                    }


                    vector3Dref.scene.updateMatrix();

                    //vector3Dref.explodebox = new THREE.Box3().setFromObject(vector3Dref.scene);
                    //vector3Dref.explodeboxSize = vector3Dref.explodebox.getSize(new THREE.Vector3()).length();
                    //vector3Dref.explodeboxCenter = vector3Dref.explodebox.getCenter(new THREE.Vector3());

                    
                    //vector3Dref.sceneoffset = vector3Dref.explodeboxCenter.sub(vector3Dref.boxCenter);

                    //var sceneoffset = vector3Dref.boxCenter.clone();
                    
                    
                    



                    
                    //vector3Dref.sceneoffsetset = !vector3Dref.sceneoffsetset;
                    //var vdistance = vector3Dref.boxCenter.distanceTo(vector3Dref.sceneoffset);
                    
                    //console.log(vector3Dref.sceneoffset);

                    //console.log(vdistance);
                    //console.log(distance);
                    //console.log(vector3Dref.boxCenter);


                    
                    //console.log(vector3Dref.explodeboxCenter);


                    //parent.camera.lookAt(vector3Dref.explodeboxCenter.x, vector3Dref.explodeboxCenter.y, vector3Dref.boxCenter.z);
                    if (this.pxplodefactor != 0) {
                        //pscenepos = vector3Dref.originalpos.clone();
                        //pscenepos = vector3Dref.boxCenter.clone(); 
                        //psceneoffset = pscenepos.multiplyScalar(this.pxplodefactor);
                        //vector3Dref.scene.position.sub(psceneoffset);
                    }
                    /*if (this.pxplodefactor != 0) {
                        //var pscenepos = vector3Dref.originalpos.clone();
                        var pscenepos = vector3Dref.boxCenter.clone();
                        var psceneoffset = pscenepos.multiplyScalar(this.pxplodefactor);
                        vector3Dref.scene.position.sub(psceneoffset);
        
                    }*/
                    //var sceneoffset = scenepos.multiplyScalar(distance);
                    //sceneoffset.x /= vector3Dref.scale;
                    //sceneoffset.y /= vector3Dref.scale;
                    //sceneoffset.z /= vector3Dref.scale;
                    //vector3Dref.scene.position.add(sceneoffset);

                    
                    vector3Dref.pxplodefactor = distance;

                };

                this.setmeshTranspEx = function (mesh, value, reset, bUseParent) {

                    if(reset){

                        if (bUseParent){
                            if(mesh.type == "LineSegments"){
                                mesh.visible = true;
                            }    

                            mesh.material.transparent = (mesh.parent.userData.transparent == 1);
                            //mesh.userData.temptransparent = false;
                            mesh.material.opacity = mesh.parent.userData.opacity;

                        }else{
                            if(mesh.type == "LineSegments"){
                                mesh.visible = true;
                            }    

                            mesh.material.transparent = (mesh.userData.transparent == 1);
                            mesh.material.opacity = mesh.userData.opacity;
                            //mesh.userData.temptransparent = false;
    
                        }

                    }else{

                        if(mesh.type == "LineSegments" && value < 1){
                            mesh.visible = false;
                        }else{

                            /*if(mesh.parent.userData.transparent == 1){

                            }*/
                            

                            if(mesh.userData.transparent != 1 && value < 1){
                                //console.log(mesh.parent.userData.transparent);
                                //mesh.userData.temptransparent = true;
                            }else{
                                //mesh.userData.temptransparent = false;
                            }

                            
                            
                             //== 1
                            //mesh.material.transparent = (mesh.parent.userData.transparent == 1);
                            //mesh.material.opacity = mesh.parent.userData.opacity;


                            mesh.material.transparent = true;
                            //vector3Dref.scene.children[i].material.depthWrite = false;
                            mesh.material.opacity = value;
    
                        }


                    }

                   
                };

                this.setintialTransp = function(parentmesh, childmesh){
                    if (parentmesh.userData.transparent == undefined) {
                        parentmesh.userData.transparent = childmesh.material.transparent ? 1 : 0;
                        //parentmesh.userData.temptransparent = false;
                    }
                    if(parentmesh.userData.opacity == undefined){
                        parentmesh.userData.opacity = childmesh.material.opacity;
                    }

                };

                this.setmeshNoSelect = function(mesh, reset){

                    if(reset){


                        mesh.userData.temptransparent = false;

                    }else{

                        mesh.userData.temptransparent = true;


                    }

                };

                this.setmeshTransp = function (mesh, value, reset, bUseparent) {

                    if(reset){

                        
                        if (bUseparent){
                            if(mesh.type == "LineSegments"){
                                mesh.visible = true;
                            }    

                            mesh.material.transparent = (mesh.parent.userData.transparent == 1);
                            mesh.userData.temptransparent = false;
                            mesh.material.opacity = mesh.parent.userData.opacity;
    
                        }else{

                            if(mesh.type == "LineSegments"){
                                mesh.visible = true;
                            }    

                            mesh.material.transparent = (mesh.userData.transparent == 1);
                            mesh.userData.temptransparent = false;
                            mesh.material.opacity = mesh.userData.opacity;
                            
                        }

                    }else{

                        if (bUseparent){

    
                            if (mesh.parent.userData.transparent == 0) {

                                if(mesh.type == "LineSegments" && value < 1){
                                    mesh.visible = false;
                                }else{
                                    mesh.material.transparent = true;
                                    mesh.userData.temptransparent = true;
                                    //vector3Dref.scene.children[i].material.depthWrite = false;
                                    mesh.material.opacity = value;
            
                                }
    
                            }
                        }else{
                            if (mesh.userData.transparent == 0) {

                                if(mesh.type == "LineSegments" && value < 1){
                                    mesh.visible = false;
                                }else{
                                    mesh.material.transparent = true;
                                    mesh.userData.temptransparent = true;
                                    //vector3Dref.scene.children[i].material.depthWrite = false;
                                    mesh.material.opacity = value;
            
                                }

                            }
                        }
                        

                    }

                };

                this.setBlockTransparencyOld = function(blockid, value, override){
                    var mesharray = vector3Dref.getMeshChildren();
                    if (mesharray.length == 0) {
                        return;
                    }
                    for (var i = 0; i < mesharray.length; i++) {
                        if (mesharray[i].type == "Group") {
                            for (var j = 0; j < mesharray[i].children.length; j++) {
                                if (mesharray[i].children[j].type == "Mesh" || mesharray[i].children[j].type == "LineSegments") {
                                    if(mesharray[i].children[j].name == blockid){
                                        vector3Dref.setintialTransp(mesharray[i], mesharray[i].children[j]);
                                        if(override){
                                            vector3Dref.setmeshTranspEx(mesharray[i].children[j], value, false, true);
                                        }else{
                                            vector3Dref.setmeshTranspEx(mesharray[i].children[j], null, true, true);
                                        }
                                        
                                    }
                                    
                                }
                            }
                        }
                        if (mesharray[i].type == "Mesh" || mesharray[i].type == "LineSegments") {
                            if(mesharray[i].name == blockid){
                                vector3Dref.setintialTransp(mesharray[i],mesharray[i]);
                                if(override){
                                    vector3Dref.setmeshTranspEx(mesharray[i], value, false, false);
                                }else{
                                    vector3Dref.setmeshTranspEx(mesharray[i], null, true, false);
                                }
                                
                            }

                            
                        }
                    }
                    vector3Dref.doexplode = true;
                    if (value == 0) {
                        vector3Dref.explode(0);
                    }
                    else {
                        vector3Dref.explode(0.001);
                    }
                    vector3Dref.doexplode = false;

                    vector3Dref.scene.updateMatrix();

                };

                this.setBlockNoSelect = function(blockid, override){

                    var meshobject = vector3Dref.getMeshByName(blockid);

                    if(meshobject == null){
                        return;
                    }else{


                        if(override){
                            vector3Dref.setmeshNoSelect(meshobject, false);
                        }else{
                            vector3Dref.setmeshNoSelect(meshobject, true);
                        }

                        /*if(meshobject.parent.type == "Group" && meshobject.parent.uuid != vector3Dref.modeluuid){
                            //vector3Dref.setintialTransp(meshobject.parent, meshobject);

    
                        }else{
                            //vector3Dref.setintialTransp(meshobject, meshobject);

                            if(override){
                                vector3Dref.setmeshNoSelect(meshobject, false);
                            }else{
                                vector3Dref.setmeshNoSelect(meshobject, true);
                            }

                        }*/
                        
                        //this.setmeshTranspEx = function (mesh, value, reset, bUseParent) {


                    }
                };

                //setmeshNoSelect

                this.setBlockTransparency = function(blockid, value, override){
                    
                    var meshobject = vector3Dref.getMeshByName(blockid);

                    if(meshobject == null){
                        return;
                    }else{

                        if(meshobject.parent.type == "Group" && meshobject.parent.uuid != vector3Dref.modeluuid){
                            vector3Dref.setintialTransp(meshobject.parent, meshobject);

                            if(override){
                                vector3Dref.setmeshTranspEx(meshobject, value, false, true);
                            }else{
                                vector3Dref.setmeshTranspEx(meshobject, null, true, true);
                            }
    
                        }else{
                            vector3Dref.setintialTransp(meshobject, meshobject);

                            if(override){
                                vector3Dref.setmeshTranspEx(meshobject, value, false, false);
                            }else{
                                vector3Dref.setmeshTranspEx(meshobject, null, true, false);
                            }

                        }
                        


                    }
                    
                    /*vector3Dref.doexplode = true;

                    if (value == 0) {
                        vector3Dref.explode(0);
                    }
                    else {
                        vector3Dref.explode(0.001);
                    }
                    vector3Dref.doexplode = false;

                    vector3Dref.scene.updateMatrix();*/

                };

                this.setTransparencyEx = function(value) {
                    
                    vector3Dref.setTransparency(value, true);

                };

                this.setNoSelectAll = function(reset){

                    vector3Dref.setNoSelect(reset);

                };

                this.setNoSelectArray = function (mesharray , override) {

                    for (var i = 0; i < mesharray.length; i++) {
                        if (mesharray[i].type == "Group") {
                           

                            for (var j = 0; j < mesharray[i].children.length; j++) {
                                if (mesharray[i].children[j].type == "Mesh" || mesharray[i].children[j].type == "LineSegments") {
                                    

                                    if(override){
                                        vector3Dref.setmeshNoSelect(mesharray[i].children[j], false);
                                    }else{
                                        vector3Dref.setmeshNoSelect(mesharray[i].children[j], true);
                                    }

                                    //vector3Dref.setmeshTranspEx(mesharray[i].children[j], value, false);
                                }
                            }
                        }
                        if (mesharray[i].type == "Mesh" || mesharray[i].type == "LineSegments") {
                            //vector3Dref.setmeshTransp(mesharray[i], value, false);
                            

                            if(override){
                                vector3Dref.setmeshNoSelect(mesharray[i], false);
                            }else{
                                vector3Dref.setmeshNoSelect(mesharray[i], true);
                            }

                        }
                    }

                };

                this.setTransparencyArray = function (mesharray , value, override) {

                    for (var i = 0; i < mesharray.length; i++) {
                        if (mesharray[i].type == "Group") {
                           

                            for (var j = 0; j < mesharray[i].children.length; j++) {
                                if (mesharray[i].children[j].type == "Mesh" || mesharray[i].children[j].type == "LineSegments") {
                                    vector3Dref.setintialTransp(mesharray[i], mesharray[i].children[j]);
                                    //vector3Dref.setintialTransp(mesharray[i].children[j]);
                                    if(override){
                                        vector3Dref.setmeshTranspEx(mesharray[i].children[j], value, false, true);
                                    }else{
                                        vector3Dref.setmeshTransp(mesharray[i].children[j], value, false, true);
                                    }
                                    
                                    

                                    //vector3Dref.setmeshTranspEx(mesharray[i].children[j], value, false);
                                }
                            }
                        }
                        if (mesharray[i].type == "Mesh" || mesharray[i].type == "LineSegments") {
                            //vector3Dref.setmeshTransp(mesharray[i], value, false);
                            vector3Dref.setintialTransp(mesharray[i],mesharray[i]);
                            if(override){
                                vector3Dref.setmeshTranspEx(mesharray[i], value, false, false);
                            }else{
                                vector3Dref.setmeshTransp(mesharray[i], value, false, false);
                            }
                            
                        }
                    }
                };

                this.setExplodeTransparency = function(onoff){

                    if(onoff){
                        vector3Dref.doexplode = true;
                        if (value == 0) {
                            vector3Dref.newExplode(0);
                        }
                        else {
                            vector3Dref.newExplode(0.001);
                        }
                        vector3Dref.doexplode = false;
    
                    }else{

                        vector3Dref.doexplode = true;
                        vector3Dref.newExplode(0);
                        vector3Dref.doexplode = false;
    
                    }

                };

                this.setNoSelect = function (override) {

                    var mesharray = vector3Dref.getMeshChildren();
                    
                    if (mesharray.length == 0) {
                        return;
                    }

                    //vector3Dref.setTransparencyArray(mesharray, value, override);
                    vector3Dref.setNoSelectArray(mesharray, override);

                    var addedmesharray = vector3Dref.getAddedMeshChildren();

                    for (var ami = 0; ami < addedmesharray.length; ami++){
                        
                        //vector3Dref.setTransparencyArray(addedmesharray[ami], value, override);
                        vector3Dref.setNoSelectArray(addedmesharray[ami], override);
                    }
                    
                };

                this.setTransparency = function (value, override) {
                    

                    if(value == 1){
                        vector3Dref.resetTransparency();
                        return;
                    }

                    
                    var mesharray = vector3Dref.getMeshChildren();
                    
                    if (mesharray.length == 0) {
                        return;
                    }

                    vector3Dref.setTransparencyArray(mesharray, value, override);

                    var addedmesharray = vector3Dref.getAddedMeshChildren();

                    for (var ami = 0; ami < addedmesharray.length; ami++){
                        
                        vector3Dref.setTransparencyArray(addedmesharray[ami], value, override);
                    }
                    
                    // a small explode used to force correct transparency
                    //workaround for now until a better solution present itself

                    //vector3Dref.setExplodeTransparency(true);

                    vector3Dref.scene.updateMatrix();
                };

                this.resetTransparencyArr = function(mesharray){

                    for (var i = 0; i < mesharray.length; i++) {
                        if (mesharray[i].type == "Group") {
                            

                            for (var j = 0; j < mesharray[i].children.length; j++) {
                                if (mesharray[i].children[j].type == "Mesh" || mesharray[i].children[j].type == "LineSegments") {
                                    vector3Dref.setintialTransp(mesharray[i], mesharray[i].children[j]);        

                                    vector3Dref.setmeshTransp(mesharray[i].children[j], null, true, true);

                                }
                            }
                        }
                        if (mesharray[i].type == "Mesh" || mesharray[i].type == "LineSegments") {
                            vector3Dref.setintialTransp(mesharray[i],mesharray[i]);
                            vector3Dref.setmeshTransp(mesharray[i], null, true, false);
                        }
                    }

                };
                this.resetTransparency = function () {
                    
                    var mesharray = vector3Dref.getMeshChildren();
                    
                    if (mesharray.length == 0) {
                        return;
                    }

                    vector3Dref.resetTransparencyArr(mesharray);

                    var addedmesharray = vector3Dref.getAddedMeshChildren();
                
                    for (var ami = 0; ami < addedmesharray.length; ami++){
                        vector3Dref.resetTransparencyArr(addedmesharray[ami]);
                         
                     } 

                    // a small explode used to force correct transparency
                    //workaround for now until a better solution present itself
                    //disabled until explode is back to operational    

                    //vector3Dref.setExplodeTransparency(false);

                    /*vector3Dref.doexplode = true;
                    vector3Dref.explode(0);
                    vector3Dref.doexplode = false;*/
                    vector3Dref.scene.updateMatrix();
                };
                this.createsymbolTextures = function () {
                    for (var i = 0; i < textureobjects.length; i++) {
                        textureobjects[i].canvas2 = document.createElement('canvas');
                        textureobjects[i].canvas2.width = 128;
                        textureobjects[i].canvas2.height = 128;
                        textureobjects[i].context2 = textureobjects[i].canvas2.getContext('2d');
                        if (textureobjects[i].color == 'red' || textureobjects[i].color == 'blue' || textureobjects[i].color == 'green') {
                            textureobjects[i].context2.drawImage(textureobjects[i].textureimage, 15, 0);
                        }
                        else {
                            textureobjects[i].context2.drawImage(textureobjects[i].textureimage, 0, 0);
                        }
                        textureobjects[i].threeTexture = new THREE.Texture(textureobjects[i].canvas2);
                        textureobjects[i].threeTexture.needsUpdate = true;
                        textureobjects[i].markupmaterial = new THREE.MeshBasicMaterial({ map: textureobjects[i].threeTexture, side: THREE.DoubleSide });
                        textureobjects[i].markupmaterial.transparent = true;

                        textureobjects[i].markupmaterialblank = new THREE.MeshBasicMaterial({side: THREE.DoubleSide });
                        textureobjects[i].markupmaterialblank.transparent = true;

                        
                        //var texture1 = new THREE.Texture(canvas1); 
                        //texture1.needsUpdate = true;
                        //var material1 = new THREE.MeshBasicMaterial( {map: texture1, side:THREE.DoubleSide } );
                        //material1.transparent = true;
                    }
                };
                this.getfloorplantexture = function(floornum){
                    var floorplantexture = null;

                    for (var i = 0; i < vector3Dref.floorplanTextures.length;i++ ){
                        if (vector3Dref.floorplanTextures[i].floornum == floornum){
                            floorplantexture = vector3Dref.floorplanTextures[i];
                        }
                    }
                    return floorplantexture;

                };
                this.addfloorplanTexture = function(floornum, canvas, alignarray){

                    if(canvas.width == 0 || canvas.height == 0){
                        console.log('invalid canvas');
                        return;
                    }

                    vector3Dref.floorplanTextures = [];

                    var hwratio = canvas.width / canvas.height;

                    //var id = 'floor' + floornum;
                    var w = canvas.width;
                    var h = canvas.height;


                    if (canvas.width > canvas.height){
                        w = 4096;
                        h = 2048;
                    }else{
                        w = 2048;
                        h = 4096;

                    }
                    //var floorplan = new textureobject(null, canvas.width, canvas.height, 'floor' + floornum);
                    var floorplan = new textureobject(null, w, h, 'floor' + floornum);

                    floorplan.canvas2 = document.createElement('canvas');
                    floorplan.canvas2.width = canvas.width;
                    floorplan.canvas2.height = canvas.height;
                    //floorplan.canvas2.width = w;
                    //floorplan.canvas2.height = h;

                    floorplan.context2 = floorplan.canvas2.getContext('2d');
                    floorplan.context2.drawImage(canvas, 0, 0);

                    floorplan.threeTexture = new THREE.CanvasTexture(floorplan.canvas2);
                    floorplan.threeTexture.needsUpdate = true;
                    //floorplan.threeTexture.minFilter = THREE.NearestMipmapLinearFilter;
                    floorplan.threeTexture.minFilter = THREE.LinearFilter;
                    floorplan.markupmaterial = new THREE.MeshBasicMaterial({ map: floorplan.threeTexture, side: THREE.DoubleSide });
                    floorplan.markupmaterial.transparent = false;
                    floorplan.markupmaterial.opacity = 1;


                    vector3Dref.floorplanTextures.push({texture : floorplan, floornum : floornum});

                    vector3Dref.addfloorplanPlane(floornum, hwratio, alignarray);

                };

                this.addfloorplanPlane = function(floornum, hwratio, alignarray){
                    

                    var boxwidth = vector3Dref.box.max.x - vector3Dref.box.min.x;
                    var boxdepth = vector3Dref.box.max.y - vector3Dref.box.min.y;
                    var width = boxwidth;
                    var depth = boxdepth;

                    var widthdiff = 0;
                    var depthdiff = 0;
                    var OffsetX = 0;
                    var OffsetY = 0;


                    var height = vector3Dref.box.max.z - vector3Dref.box.min.z;
                    
                    var boxratio = width / depth;

                    var floorscale = parent.floorplans[floornum].Scale;
                    var cratio = boxratio / hwratio;


                    if(hwratio > boxratio){
                        //fit to height
                        if (cratio > 1){
                            
                            width = boxwidth * cratio;
                        }else{
                            //widthdiff = (width - (width / cratio)) * 0.5;
                            width = boxwidth / cratio;
                        }

                        
                    }else{
                        //fit to width
                        if (cratio > 1){
                            
                            depth = boxdepth * cratio;
                            
                        }else{
                            //depthdiff = (depth - (depth / cratio)) * 0.5;
                            depth = boxdepth / cratio;
                        }
                        
                    }

                    if(alignarray){


                        var scaleratio = (alignarray[1].dist / alignarray[1].pwidth) / (alignarray[0].dist / alignarray[0].pwidth);
                        

                        //var bscreendist = alignarray[0].scaledist.scaledDistance;
                        //var oscreendist = alignarray[1].scaledist.scaledDistance;

                        var bscreendist = alignarray[0].scaleinfo.scaledDistance;
                        var oscreendist = alignarray[1].scaleinfo.scaledDistance;

                        //var bscale = alignarray[0].scaledist.curscale;
                        //var oscale = alignarray[1].scaledist.curscale;
                        
                        var bscale = alignarray[0].scaleinfo.curscale;
                        var oscale = alignarray[1].scaleinfo.curscale;
                        


                        var newreplaceScale = (oscreendist * oscale) / (bscreendist * bscale);
                        var testrepScale = ((oscreendist * oscale) / bscale) / (bscreendist);

                        width /= scaleratio;
                        depth /= scaleratio;

                        widthdiff = (boxwidth - width) * 0.5;
                        depthdiff = (boxdepth - depth) * 0.5;


                        OffsetX = -widthdiff;
                        OffsetY = depthdiff;
    
                        
                        
                        var oscalecalc = bscreendist / oscreendist;

                        var oxoffset = (alignarray[1].offset.x * oscalecalc);
                        var oyoffset = (alignarray[1].offset.y * oscalecalc);
                        
                        //oxoffset = (alignarray[1].offset.x * oscale);
                        //oyoffset = (alignarray[1].offset.y * oscale);

                        var bxoffset = (alignarray[0].offset.x);
                        var byoffset = (alignarray[0].offset.y);

                        var offsettdiffx = (oxoffset - bxoffset) / floorscale;
                        var offsettdiffy = (oyoffset - byoffset) / floorscale;
                        //offsettdiffx /= newreplaceScale;
                        //offsettdiffy /= newreplaceScale;


                        OffsetX = OffsetX - offsettdiffx;
                        OffsetY = OffsetY + offsettdiffy;

                                                

                        //var disttocxg = ((alignarray[1].offset.x * oscale) / floorscale);
                        //var disttocyg = ((alignarray[1].offset.y * oscale) / floorscale);

                        //var disttocxr = ((alignarray[0].offset.x * bscale) / floorscale);
                        //var disttocyr = ((alignarray[0].offset.y * bscale) / floorscale);


                        //OffsetX = -(oxoffset - bxoffset) / floorscale;
                        //OffsetY = -(oyoffset - byoffset) / floorscale;

                        //OffsetX = disttocxg - disttocxr;
                        //OffsetY = disttocyg - disttocyr;


                        //plane.position.x += -widthdiff;
                        //plane.position.y += depthdiff;

                        //OffsetX = (disttocxg - disttocxr) + widthdiff;
                        //OffsetY = (disttocyg - disttocyr) + depthdiff;


                        //OffsetX = -1700;
                        //OffsetY = -1500;

                        //var oxoffset = (alignarray[1].offset.x * oscalecalc);
                        //var oyoffset = (alignarray[1].offset.y * oscalecalc);
                        
                        //var bxoffset = alignarray[0].offset.x;
                        //var byoffset = alignarray[0].offset.y;
    
                        //set x and y 
                    }


                    var planegeometry = new THREE.PlaneBufferGeometry( width, depth, 4);
                    for(var fi = 0; fi < vector3Dref.floorplanTextures.length; fi++){
                        if (vector3Dref.floorplanTextures[fi].floornum == floornum){
                            var floortexture = vector3Dref.floorplanTextures[fi].texture;
                        }
                    }

                    //add a mm to make sure the plane is not inside something below

                    var height_adjust = vector3Dref.getmminUnit(1); //1 mm

                    /*if (vector3Dref.unit == 1){
                        //unit = meter
                        var height_adjust = 0.001; //1 mm    
                    }else if (vector3Dref.unit == 0){
                        //unit = mm
                        
                        height_adjust = 1; //1 mm    
                    }*/

                    var plane = new THREE.Mesh(planegeometry, floortexture.markupmaterial);
                    plane.position.copy(vector3Dref.boxCenter);
                    if(parent.floorplans[floornum]){
                        plane.position.z = parent.floorplans[floornum].Elevation + height_adjust;
                        

                    }

                    plane.position.x += OffsetX;
                    plane.position.y += OffsetY;

                    var minx = plane.position.x - (width * 0.5); 
                    var maxx = plane.position.x + (width * 0.5); 
                    var miny = plane.position.y - (depth * 0.5);
                    var maxy = plane.position.y + (depth * 0.5);
                    var minz = vector3Dref.box.min.z;
                    var maxz = vector3Dref.box.max.z;

                    if(vector3Dref.curwidth < width){
                        vector3Dref.curwidth = width;
                    }
                    if(vector3Dref.curdepth < depth){
                        vector3Dref.curdepth = depth;
                    }
                    if(vector3Dref.curheight < height){
                        vector3Dref.curheight = height;
                    }
                    if(vector3Dref.curminx > minx){
                        vector3Dref.curminx = minx;
                    }
                    if(vector3Dref.curmaxx < maxx){
                        vector3Dref.curmaxx = maxx;
                    }
                    if(vector3Dref.curminy > miny){
                        vector3Dref.curminy = miny;
                    }

                    if(vector3Dref.curmaxy < maxy){
                        vector3Dref.curmaxy = maxy;
                    }
                    
                    if(vector3Dref.curminz > minz){
                        vector3Dref.curminz = minz;
                    }

                    if(vector3Dref.curmaxz < maxz){
                        vector3Dref.curmaxz = maxz;
                    }

                    vector3Dref.setClippingPlanes(vector3Dref.curwidth, vector3Dref.curdepth, vector3Dref.curheight, vector3Dref.curminx, vector3Dref.curmaxx, vector3Dref.curminy, vector3Dref.curmaxy, vector3Dref.curminz, vector3Dref.curmaxz);
                    
                    
                    



                    

                    /*if(alignarray != null){
                        console.log(alignarray);
                    }*/
                    //plane.userData.actionStatus = color;
                    plane.userData.signature = signature;
                    plane.userData.id = plane.uuid;
                    plane.userData.type = "floorplan";
                    plane.userData.floornum = floornum;
                    

                    //plane.userData.connectedMesh = intersect.object.uuid;

                    
                    vector3Dref.markupscene.add(plane);
                    vector3Dref.scene.updateMatrix();

                };

                this.scaleSymbol = function(){

                };


                this.add3DSymbol = function (intersect, color) {
                    

                    var pwidth = vector3Dref.getmminUnit(400); //1 mm
                    /*if (vector3Dref.unit == 1){
                        //unit = meter
                        var pwidth = 0.4; //40 cm    
                    }else if (vector3Dref.unit == 0){
                        //unit = mm
                        pwidth = 400; //40 cm
                    }*/

                    //var pwidth = size * 0.005;
                    //var partcenter = getMeshorigcenter(intersect.object, intersect.object.geometry.boundingSphere.center);

                    
                    var zerovector = new THREE.Vector3(0,0,0);

                    //get diff from orig pos
                    var partcenteroffset = zerovector.sub(intersect.object.position);
                    //console.log(partcenteroffset);
                    //mesh.children[gpartix].position.copy(gorigotransform);

                    //var partcenter = getMeshorigcenter(mesh.children[gpartix], mesh.children[gpartix].geometry.boundingSphere.center);

                    var pheight = pwidth;
                    var pdepth = pwidth / 10;
                    var btextureused = false;
                    var texturecolornum = 0;
                    var insertpoint = intersect.point;
                    var upvector = new THREE.Vector3();
                    upvector.copy(vector3Dref.scene.up);
                    vector3Dref.createsymbolTextures();
                    for (var i = 0; i < textureobjects.length; i++) {
                        if (textureobjects[i].color == color) {
                            texturecolornum = i;
                        }
                    }
                    //upvector.x,upvector.y, upvector.z
                    intersect.object.up.set(upvector.x, upvector.y, upvector.z);
                    intersect.object.geometry.computeVertexNormals();
                    var vec = intersect.face.normal.clone();
                    var tmp = new THREE.Object3D();
                    tmp.up.set(upvector.x, upvector.y, upvector.z);
                    tmp.position.copy(insertpoint);
                    //var n = intersects[0].face.normal.clone();
                    vec.transformDirection(intersect.object.matrixWorld);
                    vec.multiplyScalar(1);
                    vec.add(insertpoint);
                    tmp.lookAt(vec);
                    //var m = new THREE.Mesh(new DecalGeometry(mesh, intersects[0].point, tmp.rotation, size), decalMaterial);
                    //var helper = new FaceNormalsHelper( intersect.object, 2, 0x00ff00, 1 );
                    //console.log(vec.x, vec.y, vec.z);
                    //context1.font = "Bold 12px Arial";
                    //context1.fillStyle = "rgba(255,0,0,0.95)";
                    //context1.fillText('Hello, world!', 0, 10);
                    // canvas contents will be used for a texture
                    //var cubegeometry = new THREE.BoxBufferGeometry(  0.5, 0.5, 0.5 );
                    //cubegeometry.translate( vector3Dref.boxCenter.x, vector3Dref.boxCenter.y, vector3Dref.boxCenter.z );
                    //cubegeometry.translate( 0,0,0 );
                    //vector3Dref.boxCenter
                    //cubegeometry.rotateX( Math.PI / 2 );

                    
                    var cubegeometry = new THREE.BoxBufferGeometry(pwidth, pheight, pdepth);
                    //cubegeometry.translate(insertpoint.x,insertpoint.y, insertpoint.z);
                    //cubegeometry.computeBoundingSphere();

                    var cubematerials = [
                        textureobjects[texturecolornum].markupmaterialblank,
                        textureobjects[texturecolornum].markupmaterialblank,
                        textureobjects[texturecolornum].markupmaterialblank,
                        textureobjects[texturecolornum].markupmaterialblank,
                        textureobjects[texturecolornum].markupmaterial,
                        textureobjects[texturecolornum].markupmaterial
                    ];

                    //var cube = new THREE.Mesh(cubegeometry, textureobjects[texturecolornum].markupmaterial);
                    var cube = new THREE.Mesh(cubegeometry, cubematerials);
                    //scene.add( cube );
                    //getGUID3D(cube);
                    //cube.customData.
                    
                    cube.userData.actionStatus = color;
                    cube.userData.signature = signature;
                    cube.userData.id = cube.uuid;
                    cube.userData.type = "symbol";
                    cube.userData.connectedMesh = intersect.object.uuid;

                    //mesh1.position.set(0,50,0);
                    //scene.add( mesh1 );
                    //var geometry = new THREE.PlaneGeometry( pwidth, pheight, 4 );
                    var planecolor = 0xffff00;
                    if (!btextureused) {
                        if (color == 'red') {
                            planecolor = 0xff0000;
                        }
                        else if (color == 'blue') {
                            planecolor = 0x0000ff;
                        }
                        else if (color == 'green') {
                            planecolor = 0x00ff00;
                        }
                        var material = new THREE.MeshBasicMaterial({ color: planecolor, side: THREE.DoubleSide });
                    }
                    //var cent = new THREE.Vector3();
                    //plane.position.copy(insertpoint);
                    //var bbox = new THREE.Box3().setFromObject(plane);
                    //bbox.getCenter(cent);
                    var cdirection = new THREE.Vector3();
                    cdirection.copy(parent.camera.position);
                    //plane.lookAt(cdirection);
                    //const distance = 0.05;
                    //plane.position.add( cdirection.multiplyScalar(distance) );
                    //plane.scale.set(0.05,0.05,0.05);
                    //vector3Dref.scene.add(gltfscene);
                    //vector3Dref.scene.add(vector3Dref.markupscene);
                    //vector3Dref.scene.add(plane);
                    //mesh1.position.copy(insertpoint);
                    cube.rotation.copy(tmp.rotation);
                    //applyMatrixOfMesh(intersect.object);
                    //cu.rotation.getRotationFromMatrix( mat, cu.scale );
                    //cube.rotation.getRotationFromMatrix( mat, cube.scale );
                    //cube.setRotationFromMatrix(mat);            
                    //var nhelper = new THREE.FaceNormalsHelper( intersect.object, 2, 0x00ff00, 1 );
                    //cube.lookAt(intersect.face.normal);
                    cube.position.copy(insertpoint);
                    cube.geometry.computeBoundingSphere();

                    //var partcenteroffset = zerovector.sub(intersect.object.position);
                    partcenteroffset = partcenteroffset.add(cube.position.clone());

                    cube.userData.originalpositionX = partcenteroffset.x;
                    cube.userData.originalpositionY = partcenteroffset.y;
                    cube.userData.originalpositionZ = partcenteroffset.z;

                    
                    //cube.userData.originalpositionX = cube.position.x;
                    //cube.userData.originalpositionY = cube.position.y;
                    //cube.userData.originalpositionZ = cube.position.z;

                    //mesh1.lookAt(cdirection);
                    //mesh1.position.add( cdirection.multiplyScalar(distance) );
                    //vector3Dref.scene.add( mesh1 );
                    //vector3Dref.scene.add( cube );
                    vector3Dref.markupscene.add(cube);
                    vector3Dref.scene.updateMatrix();
                    

                    if (RxCore_GUI_3DMarkup != undefined) {
                        RxCore_GUI_3DMarkup.setMarkupSelected(cube.userData, {created : true, modified : false, deleted : false});
                    }

                    if (RxCore_GUI_3DMarkupList != undefined) {
                        RxCore_GUI_3DMarkupList.setMarkupList(vector3Dref.markupscene.children, DocObj.bActive);
                    }


                };
                this.setSelectedPart = function (part) {
                    this.selectedpart = part;
                };
                this.getPartAttributes = function(name){
                    var blockattributes = undefined;
                    
                    for (var pc = 0; pc < vector3Dref.blocklist.length; pc++){
                        if(vector3Dref.blocklist[pc].GlobalID === name){
                            blockattributes = vector3Dref.blocklist[pc];
                        }
                    }
                    return blockattributes;

                };
                this.getSelectedPart = function () {
                    return this.selectedpart;
                };
                this.Close = function () {

                    renderer.clippingPlanes = Empty;

                    while (this.partlist.length != 0) {
                        this.partlist.pop();
                    }
                    while (this.blocklist.length != 0) {
                        this.blocklist.pop();
                    }
                    while (this.scene.children.length != 0) {
                        var obj = this.scene.children[this.scene.children.length - 1];
                        this.scene.remove(obj);
                        obj = null;
                    }
                    renderer.render(this.scene, parent.camera);
                };
            }
        }        
        class Vector3DPageObject {
            constructor(pagexmldata, binary, parent) {
                var vector3Dref = this;
                var breadbinaryready = false;
                var pageobject = parent;
                var dv = new DataView(pagexmldata);
                var header = String.fromCharCode(dv.getUint8(0));
                var i = 1;
                while (i <= 15) {
                    header += String.fromCharCode(dv.getUint8(i));
                    i++;
                }
                this.countparts = 0;
                this.binaryreadcomplete = false;
                this.selectedpart = -1;
                this.version = parseInt(dv.getInt32(16, true));
                this.clippingOn = false;
                this.x = parseFloat(dv.getFloat32(24, true));
                this.y = parseFloat(dv.getFloat32(28, true));
                this.z = parseFloat(dv.getFloat32(32, true));
                this.w = parseFloat(dv.getFloat32(36, true)) - this.x;
                this.h = parseFloat(dv.getFloat32(40, true)) - this.y;
                this.d = parseFloat(dv.getFloat32(44, true)) - this.z;
                if (this.w < 1000) {
                    this.unit = 1;
                    //meter
                }
                else {
                    this.unit = 0;
                    //mm
                }
                this.orignalscale = parseFloat(dv.getFloat32(48, true));
                var headeroffset = 52;
                dv = null;
                var wscale = renderer.domElement.width / this.w;
                var hscale = renderer.domElement.height / this.h;
                this.scale = Math.min(wscale, hscale);
                parent.clock = new THREE.Clock();
                //this.scene = new JSC3D.Scene();
                this.scene = new THREE.Scene();
                var szShortformat = DocObj.Format.substring(0, 3);

                if (szShortformat == 'IFC') {
                    this.scene.up.set(0, 0, 1);
                }
                else {
                    this.scene.up.set(0, 1, 0);
                }
                //this.scene.up.set(0,0,1);
                //this.scene.rotation.set(0,1.57,0,'XYZ');
                this.explodefactor = 0;
                this.pxplodefactor = 0;
                this.partlist = [];
                this.blocklist = [];
                var curx = this.scene.position.x;
                var cury = this.scene.position.y;
                var curz = this.scene.position.z;
                curx += -(this.w / 2) * this.scale;
                cury += -(this.h / 2) * this.scale;
                curz += -(this.d / 2) * this.scale;
                this.scene.position.set(curx, cury, curz);
                this.originalpos = new THREE.Vector3();
                this.originalpos = this.scene.position.clone();
                this.scene.updateMatrix();
                this.globalPlaneX = new THREE.Plane(new THREE.Vector3(1, 0, 0), 0);
                this.globalPlaneY = new THREE.Plane(new THREE.Vector3(0, 1, 0), 0);
                this.globalPlaneZ = new THREE.Plane(new THREE.Vector3(0, 0, -1), 0);
                this.globalPlanes = [this.globalPlaneX, this.globalPlaneY, this.globalPlaneZ],
                    Empty = Object.freeze([]);
                this.xPlaneconst = this.w * this.scale;
                var safeadjust = this.xPlaneconst / 20; //10 percent additional space for clipping
                this.xPlaneconst += safeadjust;
                this.xPlaneEnd = curx;
                this.xPlaneStart = this.xPlaneconst - curx;
                this.xPlaneEnd -= safeadjust / 2;
                this.yPlaneconst = this.h * this.scale;
                safeadjust = this.yPlaneconst / 20; //10 percent additional space for clipping
                this.yPlaneconst += safeadjust;
                this.yPlaneEnd = cury;
                this.yPlaneStart = this.yPlaneconst - cury;
                this.yPlaneEnd -= safeadjust / 2;
                this.zPlaneconst = this.d * this.scale;
                safeadjust = this.zPlaneconst / 20; //10 percent additional space for clipping
                this.zPlaneconst += safeadjust;
                this.zPlaneEnd = curz;
                this.zPlaneStart = this.zPlaneconst - curz;
                this.zPlaneEnd -= safeadjust / 2;
                this.globalPlaneX.constant = this.xPlaneStart;
                this.globalPlaneY.constant = this.yPlaneStart;
                this.globalPlaneZ.constant = this.zPlaneStart;
                this.cameraSaves = [];
                //var pointLight = new THREE.PointLight(0xFFFFFF);
                //pointLight.intensity = 0.8;
                //this.scene.add(pointLight);
                //const skyColor = 0xB1E1FF;  // light blue
                //const groundColor = 0xB97A20;  // brownish orange
                /*const skyColor = 0xFFFFFF;  // white
                const groundColor = 0xFFFFFF;  // white
        
                const intensity = 1;
                const lighth = new THREE.HemisphereLight(skyColor, groundColor, intensity);
                this.scene.add(lighth);
        
                const color = 0xFFFFFF;
                const intensity2 = 1;
                const lightd = new THREE.DirectionalLight(color, intensity2);
                lightd.position.set(5, 10, 2);
                this.scene.add(lightd);
                this.scene.add(lightd.target);*/
                var directionalLight_1 = new THREE.DirectionalLight(0xffffff, 1.2);
                //directionalLight.intensity = 0.5;
                directionalLight_1.position.set(-500, -500, 500);
                //var helper_1 = new THREE.DirectionalLightHelper(directionalLight_1, 1);
                this.scene.add(directionalLight_1);
                //this.scene.add( helper_1 );
                var directionalLight_2 = new THREE.DirectionalLight(0xffffff, 1);
                //directionalLight.intensity = 0.3;
                directionalLight_2.position.set(1000, 1000, 1000);
                //var helper_2 = new THREE.DirectionalLightHelper(directionalLight_2, 1);
                this.scene.add(directionalLight_2);
                //this.scene.add( helper_2 );
                var directionalLight_3 = new THREE.DirectionalLight(0xffffff, 0.3);
                directionalLight_3.position.set(0, 0, -1000);
                this.scene.add(directionalLight_3);
                parent.camera = new THREE.PerspectiveCamera(45, canvasowidth / canvasoheight, 0.1, 10000);
                var format = DocObj.Format.substring(0, 3);
                if (format == 'IFC') {
                    var camerax = (vector3Dref.w / 2) * vector3Dref.scale;
                    var cameray = -(vector3Dref.h) * vector3Dref.scale;
                    var cameraz = (vector3Dref.d / 2) * vector3Dref.scale;
                    var camerayorig = cameray;
                    camerax = 0;
                    cameray *= 2;
                    cameraz = 0;
                    parent.camera.up.set(0, 0, 1);
                    //parent.camera.rotation.set(90 * Math.PI / 180, 0, 0);
                }
                else {
                    camerax = (vector3Dref.w / 2) * vector3Dref.scale;
                    cameray = (vector3Dref.h / 2) * vector3Dref.scale;
                    cameraz = (vector3Dref.d) * vector3Dref.scale;
                    camerax = 0;
                    cameray = 0;
                    cameraz *= 2;
                    parent.camera.up.set(0, 1, 0);
                    //parent.camera.rotation.set(0, 90 * Math.PI / 180, 0);
                }
                parent.camera.position.set(camerax, cameray, cameraz);
                parent.camera.lookAt(new THREE.Vector3(0, 0, 0));
                this.scene.updateMatrix();
                bAnimateready = false;
                //    this.scene = new JSC3D.Scene();
                //    this.partlist = [];
                var maindv = new DataView(pagexmldata, headeroffset);
                var dvpos = 0;
                var objid = parseInt(maindv.getInt32(dvpos, true));
                var newblock = false;
                var blockid = -1;
                var bimtype = -1;
                var facecolor = 0;
                var blockcounter = 0;
                var tottriangles = 0;
                if (pageobject.has3Dnav) {
                    pageobject.load3dnavigator();
                }
                else {
                    breadbinaryready = true;
                }
                //parse3Dloop();
                function parse3Dloop() {
                    var readstate = {
                        objectid: objid,
                        blockpart: blockid,
                        bnewblock: newblock,
                        maindvpos: dvpos,
                        facecolor: 0
                    };
                    yieldingLoop(readstate, 200, function () {
                        readstate = parse3Dbinary(readstate);
                    }, function () {
                        readbinarycomplete();
                    });
                    (function timer() {
                        if (maindv)
                            if (readstate.objectid != 32768) {
                                setTimeout(timer, 10);
                            }
                    })();
                }
                function parse3Dbinary(readstate) {
                    switch (readstate.objectid) {
                        case 9:
                            //trianglelist
                            readstate.maindvpos += 4;
                            var numtriangles = maindv.getInt32(readstate.maindvpos, true);
                            readstate.maindvpos += 4;
                            var endrange = 9 * 4 * numtriangles;
                            var dvtriangles = new DataView(pagexmldata, headeroffset + readstate.maindvpos, endrange);
                            tottriangles += numtriangles;
                            /*if (blockcounter > 20) {
                                blockcounter = 0;
                                vector3Dref.scene.updateMatrix();
                                renderer.render(vector3Dref.scene, camera);
                                }*/
                            if (vector3Dref.partlist[readstate.blockpart] != undefined) {
                                vector3Dref.partlist[readstate.blockpart].addpart(dvtriangles, numtriangles, readstate.blockpart, readstate.facecolor, readstate.bnewblock);
                                vector3Dref.countparts++;
                            }
                            //console.log(tottriangles);
                            /*vector3Dref.partlist[blockpart].promise.then(function(){
                                vector3Dref.scene.updateMatrix();
                                renderer.render(vector3Dref.scene, camera);
        
                                });*/
                            /*this.parse(maindv).then(function() {
                                //console.log("Success!", response);
                                maindv = null;
                                //console.log('read finished');
                                bAnimateready = true;
                                if (RxCore_GUI_Download != undefined) {
                                RxCore_GUI_Download.setDownload("hide");
                                }
        
        
                                }, function(id, position) {
                                //console.log(objid);
                                //console.log(maindvpos);
        
                                console.error("Failed!", id, position);
                                });*/
                            //getbpartdiredct(dvtriangles, numtriangles, blockpart, facecolor, bnewblock);
                            /*if(numtriangles < 1000 && tottriangles < 2000000 ){
                                getbpartdiredct(dvtriangles, numtriangles, blockpart, facecolor, bnewblock);
                                }else{
                                getbpartdelay(dvtriangles, numtriangles, blockpart, facecolor, 100);
                                }*/
                            //ncolor  = decimalToHex(facecolor);
                            //vector3Dref.partlist[blockpart].createmesh(dvtriangles,numtriangles,ncolor);
                            readstate.maindvpos += endrange;
                            readstate.bnewblock = false;
                            //maindvpos += 4;
                            readstate.objectid = parseInt(maindv.getInt32(readstate.maindvpos, true));
                            break;
                        case 10:
                            //Attribute
                            readstate.maindvpos += 4;
                            var nAttrbutenl = parseInt(maindv.getInt32(readstate.maindvpos, true));
                            readstate.maindvpos += 4;
                            var battrncount = readstate.maindvpos;
                            var szatrrname = "";
                            while (battrncount < readstate.maindvpos + nAttrbutenl * 2) {
                                if (maindv.getUint8(battrncount) != 0) {
                                    szatrrname += String.fromCharCode(maindv.getUint16(battrncount, true));
                                }
                                battrncount += 2;
                            }
                            readstate.maindvpos = battrncount;
                            var nAttrbutevl = parseInt(maindv.getInt32(readstate.maindvpos, true));
                            readstate.maindvpos += 4;
                            var battrvcount = readstate.maindvpos;
                            var szatrtvalue = "";
                            while (battrvcount < readstate.maindvpos + nAttrbutevl * 2) {
                                if (maindv.getUint8(battrvcount) != 0) {
                                    szatrtvalue += String.fromCharCode(maindv.getUint16(battrvcount, true));
                                }
                                battrvcount += 2;
                            }
                            readstate.maindvpos = battrvcount;
                            vector3Dref.partlist[readstate.blockpart].addAtrib(szatrrname, szatrtvalue);
                            /*
                                0	4	LONG	Object ID	10
                                4	4	LONG	NameLength		Number of characters in name
                                8	N	CHAR[N]	Name		Attribute Name
                                8+NameLength	4	LONG	ValueLength		Number of characters in value
                                8+NameLen+4	N	CHAR[N]	Value		Attribute Value
        
                                */
                            readstate.objectid = parseInt(maindv.getInt32(readstate.maindvpos, true));
                            break;
                        case 33:
                            readstate.maindvpos += 4;
                            var lineX1 = maindv.getFloat32(readstate.maindvpos, true);
                            readstate.maindvpos += 4;
                            var lineY1 = maindv.getFloat32(readstate.maindvpos, true);
                            readstate.maindvpos += 4;
                            var lineZ1 = maindv.getFloat32(readstate.maindvpos, true);
                            readstate.maindvpos += 4;
                            var lineX2 = maindv.getFloat32(readstate.maindvpos, true);
                            readstate.maindvpos += 4;
                            var lineY2 = maindv.getFloat32(readstate.maindvpos, true);
                            readstate.maindvpos += 4;
                            var lineZ2 = maindv.getFloat32(readstate.maindvpos, true);
                            var lineobj = {
                                X1: lineX1,
                                Y1: lineY1,
                                Z1: lineZ1,
                                X2: lineX2,
                                Y2: lineY2,
                                Z2: lineZ2
                            };
                            if (vector3Dref.partlist[readstate.blockpart] != undefined) {
                                vector3Dref.partlist[readstate.blockpart].addline(lineobj, readstate.facecolor);
                                vector3Dref.countparts++;
                            }
                            /*4	4	FLOAT	X1		Line coordinates
                                8	4	FLOAT	Y1
                                12	4	FLOAT	Z1
                                16	4	FLOAT	X2
                                20	4	FLOAT	Y2
                                24	4	FLOAT	Z2*/
                            readstate.maindvpos += 4;
                            readstate.objectid = parseInt(maindv.getInt32(readstate.maindvpos, true));
                            break;
                        case 74:
                            readstate.maindvpos += 4;
                            readstate.facecolor = parseInt(maindv.getInt32(readstate.maindvpos, true));
                            /*
                                0	4	LONG	Object ID	74
                                4	4	LONG	Draw color		#RRGGBB
        
                                */
                            readstate.maindvpos += 4;
                            readstate.objectid = parseInt(maindv.getInt32(readstate.maindvpos, true));
                            break;
                        case 75:
                            //facecolor
                            readstate.maindvpos += 4;
                            bimtype = parseInt(maindv.getInt32(readstate.maindvpos, true));
                            vector3Dref.partlist[readstate.blockpart].ifctype = bimtype;
                            //console.log(bimtype);
                            /*if (bimtype == 34){
                                var visibility = 0;
                                }*/
                            /*
                                0	4	LONG	Object ID	75
                                4	4	LONG	BimType		#RRGGBB
        
                                */
                            readstate.maindvpos += 4;
                            readstate.objectid = parseInt(maindv.getInt32(readstate.maindvpos, true));
                            break;
                        case 12288:
                            readstate.maindvpos += 4;
                            readstate.objectid = parseInt(maindv.getInt32(readstate.maindvpos, true));
                            //objid = 12288;
                            if (vector3Dref.partlist.length > 0 && szShortformat == 'IFC') {
                                switch (vector3Dref.partlist[readstate.blockpart].ifctype) {
                                    case 34:
                                        vector3Dref.partlist[readstate.blockpart].visible = 0;
                                        break;
                                    case 21:
                                        if (vector3Dref.partlist[readstate.blockpart].tarray[vector3Dref.partlist[readstate.blockpart].curTrset].triangles > 5000) {
                                            vector3Dref.partlist[readstate.blockpart].visible = 0;
                                            vector3Dref.partlist[readstate.blockpart].tempvisible = false;
                                        }
                                        break;
                                }
                            }
                            if (readstate.blockpart != -1 && vector3Dref.partlist.length > 0) {
                                if (vector3Dref.partlist[readstate.blockpart].tarray.length > 0) {
                                    vector3Dref.partlist[readstate.blockpart].processmesh();
                                }
                                /*var mstatus = vector3Dref.partlist[blockpart].getmeshstatus();
                                    if (mstatus.open && mstatus.meshexist) {
                                    //setTimeout(vector3Dref.partlist[blockpart].addmesh(),2);
                                    vector3Dref.partlist[blockpart].addmesh();
        
                                    }*/
                            }
                            //end of block;
                            break;
                        case 4096:
                            readstate.maindvpos += 4;
                            /*if(blockpart != -1 && vector3Dref.partlist.length > 0){
                                var mstatus = vector3Dref.partlist[blockpart].getmeshstatus();
                                if(mstatus.open && mstatus.meshexist ){
                                vector3Dref.partlist[blockpart].addmesh();
                                console.log(blockpart);
                                }
                                }*/
                            readstate.blockpart = parseInt(maindv.getInt32(readstate.maindvpos, true));
                            readstate.bnewblock = true;
                            readstate.maindvpos += 4;
                            readstate.maindvpos += 4;
                            var nvisbile = parseInt(maindv.getInt32(readstate.maindvpos, true));
                            readstate.maindvpos += 4;
                            var ntransparent = parseInt(maindv.getInt32(readstate.maindvpos, true));
                            readstate.maindvpos += 4;
                            var ncolor = parseInt(maindv.getInt32(readstate.maindvpos, true));
                            readstate.maindvpos += 4;
                            var nNamelength = parseInt(maindv.getInt32(readstate.maindvpos, true));
                            readstate.maindvpos += 4;
                            var bnamecount = readstate.maindvpos;
                            var szblockname = "";
                            while (bnamecount < readstate.maindvpos + nNamelength * 2) {
                                if (maindv.getUint8(bnamecount) != 0) {
                                    szblockname += String.fromCharCode(maindv.getUint16(bnamecount, true));
                                }
                                bnamecount += 2;
                            }
                            readstate.maindvpos = bnamecount;
                            var mesharr = [];
                            var opartobj = {
                                id: readstate.blockpart,
                                visible: nvisbile,
                                transparent: ntransparent,
                                color: ncolor,
                                name: szblockname
                            };
                            if (szShortformat == 'IFC') {
                                var part = new Part3DObject(opartobj, readstate.blockpart, vector3Dref, true);
                            }
                            else {
                                part = new Part3DObjectMulti(opartobj, readstate.blockpart, vector3Dref, true);
                                part.addAtrib("Part name", szblockname);
                                //var blockitem = new Block3DObject(szblockname, readstate.blockpart, nvisbile, 0, 0);
                                //vector3Dref.blocklist.push(blockitem);
                            }
                            vector3Dref.partlist[readstate.blockpart] = part;
                            blockcounter++;
                            /*
                                0	4	LONG	Object ID	0x1000
                                4	4	LONG	Block ID
                                8	4	LONG	Flags		Reserved
                                12	4	LONG	Visible		Visible if non zero
                                16	4	LONG	Transparent		Transparent if non zero.
                                20	4	LONG	Color		Default block color
                                24	4	LONG	Length of name		Length of block name in Unicode characters including zero terminator.
                                28	[]	WCHAR	Name of block		Unicode
                                */
                            //block start
                            //objid = 4096;
                            //maindvpos += 4;
                            readstate.objectid = parseInt(maindv.getInt32(readstate.maindvpos, true));
                            break;
                        case 32768:
                            //console.log('read finished');
                            //resolve();
                            //counter = 200;
                            //vector3Dref.partlist[vector3Dref.partlist.length-1].isLastBlock = true;
                            vector3Dref.partlist[vector3Dref.partlist.length - 1].setlastblock();
                            /*if (pageobject.has3Dnav) {
                                pageobject.load3dnavigator();
                                }*/
                            break;
                        default:
                            //console.log('default');
                            //console.log(objid);
                            //console.log(maindvpos);
                            readstate.objectid = 32768;
                            //reject(objid,maindvpos);
                            //counter = 200;
                            console.error("Failed!", readstate.objectid, readstate.maindvpos);
                            break;
                    }
                    return readstate;
                }
                function readbinarycomplete() {
                    vector3Dref.partlist[vector3Dref.partlist.length - 1].isLastBlock = true;
                    vector3Dref.binaryreadcomplete = true;
                    maindv = null;
                    bAnimateready = true;
                    if (RxCore_GUI_Download != undefined) {
                        RxCore_GUI_Download.setDownload("hide");
                    }
                    if (vector3Dref.partlist[vector3Dref.partlist.length - 1].isLoadComplete) {
                        vector3Dref.blockreadComplete();
                    }
                    vector3Dref.updateblocklist();
                }
                /*this.checkpartload = function(){
                    vector3Dref.partlist[vector3Dref.partlist.length-1].isLastBlock = true;
        
                    if(vector3Dref.partlist[vector3Dref.partlist.length-1].isLoadComplete){
                        vector3Dref.blockreadComplete();
                    }
                    console.log('part complete');
        
        
                };*/
                this.blockadded = function () {
                    vector3Dref.countparts--;
                    if (vector3Dref.binaryreadcomplete) {
                        if (vector3Dref.countparts == 0) {
                            vector3Dref.blockreadComplete();
                            vector3Dref.updateblocklist();
                        }
                    }
                };
                this.blockreadComplete = function () {
                    if (RxCore_GUI_Read3DComplete != undefined) {
                        RxCore_GUI_Read3DComplete.readComplete();
                    }
                    parent.DocRef.SetActive();
                    //console.log('last block added');
                    /*if (pageobject.has3Dnav) {
                        pageobject.load3dnavigator();
                    }*/
                    //vector3Dref.updateblocklist();
                    /*if(szShortformat == 'IFC'){
        
                    }
        
                    if(szShortformat != 'IFC'){
                        breadbinaryready = false;
                        vector3Dref.updateblocklist();
                    }*/
                };
                this.addblocks = function (pagexmldata) {
                    this.blocklist = [];
                    var rootnode = pagexmldata.getElementsByTagName('navigator');
                    var rc = 0;
                    var level = 0;
                    var curlev = level;
                    var partnodes = pagexmldata.getElementsByTagName('partref');
                    for (var pc = 0; pc < partnodes.length; pc++) {
                        var curnode = partnodes[pc];
                        curlev = level;
                        level = 0;
                        while (curnode.parentNode.nodeName == 'partref') {
                            curnode = curnode.parentNode;
                            level++;
                        }
                        if (level < curlev) {
                            //BlocksContainer.AddULend();
                            //this.htmlcontent += "</ul>";
                        }
                        if (level > curlev) {
                            //BlocksContainer.AddULstart();
                            //this.htmlcontent += "<ul style='list-style: none; margin: 0; padding: 0;'>";
                        }
                        if (RxCore_GUI_3DParts != undefined) {
                            var listposition = pc * RxCore_GUI_3DParts.listheight;
                        }
                        else {
                            listposition = 0;
                        }
                        //var listposition = pc * BlocksContainer.listheight;
                        //BlocksContainer.AddLIelement();
                        //this.htmlcontent += "<li style='list-style: none; padding-left: 10px;'>";
                        var blockname = partnodes[pc].attributes.getNamedItem('name').value;
                        var partindex = partnodes[pc].attributes.getNamedItem('index').value;
                        var partstate = partnodes[pc].attributes.getNamedItem('state').value;
                        var blockitem = new Block3DObject(blockname, partindex, partstate, level, listposition);
                        this.blocklist.push(blockitem);
                    }
                    //this.updateblocklist();
                    parse3Dloop();
                    /*if (RxCore_GUI_3DParts != undefined) {
                        RxCore_GUI_3DParts.isupdate = false;
                        RxCore_GUI_3DParts.set3DParts(this.blocklist);
                    }*/
                };
                this.updateblocklist = function () {
                    //BlocksContainer.init3D();
                    //this.htmlcontent = "<ul style='list-style: none; margin: 0; padding: 0;'>";
                    var level = 0;
                    //var blocklistheight = BlocksContainer.Getheight();
                    for (var pc = 0; pc < this.blocklist.length; pc++) {
                        //curlev = level;
                        //level = this.blocklist[pc].level;
                        //var curnode =  this.blocklist[pc].;
                        /*while(curnode.parentNode.nodeName == 'partref'){
                            curnode = curnode.parentNode;
                            level++;
                            }*/
                        /*if (level < curlev ){
                            BlocksContainer.AddULend();
                            //this.htmlcontent += "</ul>";
                            }
                            if (level > curlev ){
                            BlocksContainer.AddULstart();
                            //this.htmlcontent += "<ul style='list-style: none; margin: 0; padding: 0;'>";
                            }*/
                        //BlocksContainer.AddLIelement();
                        //this.htmlcontent += "<li style='list-style: none; padding-left: 10px;'>";
                        //var blockname = partnodes[pc].attributes.getNamedItem('name').value;
                        //var partindex = partnodes[pc].attributes.getNamedItem('index').value;
                        //var partstate = partnodes[pc].attributes.getNamedItem('state').value;
                        //var blockitem  = new Block3DObject(blockname,partindex,partstate,level);
                        //this.blocklist.push(blockitem);
                        var index = this.blocklist[pc].index;
                        if (this.partlist[index] != undefined) {
                            this.blocklist[pc].state = this.partlist[index].tempvisible;
                        }
                        else {
                            this.blocklist[pc].state = false;
                        }
                        //this.blocklist[pc].state = this.partlist[index].tempvisible;
                        if (this.partlist[index].selected) {
                            /* if(this.blocklist[pc].position > (blocklistheight / 2)){
                                BlocklistScrollTo = this.blocklist[pc].position - (blocklistheight / 2);
                                }*/
                            this.blocklist[pc].setSelected(true);
                        }
                        else {
                            this.blocklist[pc].setSelected(false);
                        }
                        //this.htmlcontent += this.blocklist[pc].htmltext;
                    }
                    if (RxCore_GUI_3DParts != undefined) {
                        RxCore_GUI_3DParts.isupdate = true;
                        //RxCore_GUI_3DParts.scrollto = BlocklistScrollTo;
                        RxCore_GUI_3DParts.set3DParts(this.blocklist);
                        //RxCore_GUI_3DParts.scrollto = 0;
                    }
                };
                this.searchAttribute = function (attribval) {
                    var id = -1;
                    for (var pc = 0; pc < vector3Dref.partlist.length; pc++) {
                        var part3D = vector3Dref.partlist[pc];
                        for (var ac = 0; ac < part3D.attributes.length; ac++) {
                            if (part3D.attributes[ac].value == attribval) {
                                id = vector3Dref.partlist[pc].id;
                            }
                        }
                    }
                    return id;
                };
                this.addpartbinary = function (bstream, lastpart) {
                };
                this.addpart = function (partxml, lastpart) {
                    var partobj = partxml.getElementsByTagName('part');
                    var i = 0;
                    //var curlength = vector3Dref.partlist.length - 1;
                    function getpart(partxml) {
                        setTimeout(function () {
                            if (partxml.attributes.getNamedItem('id') != undefined) {
                                var pid = partxml.attributes.getNamedItem('id').value;
                            }
                            var part = new Part3DObject(partxml, pid, vector3Dref);
                            vector3Dref.partlist[pid] = part;
                            //vector3Dref.partlist.push(part);
                            if (part.hasmesh) {
                                try {
                                    //vector3Dref.scene.add( part.mesh );
                                }
                                catch (e) {
                                    alert(e.message);
                                }
                                try {
                                    //renderer.render(vector3Dref.scene, camera,false);
                                }
                                catch (e) {
                                    alert(e.message);
                                }
                                //console.log(pid);
                            }
                            bAnimateready = lastpart;
                            /*if(lastpart){
                                bAnimateready = true;
                                renderer.preserveDrawingBuffer = false;
                                renderer.autoClear = true;
        
                                }else{
                                renderer.render(vector3Dref.scene, camera,false);
                                }*/
                            //alert(i);
                        }, 10);
                    }
                    while (i < partobj.length) {
                        //bAnimateready = true;
                        getpart(partobj[i]);
                        i++;
                    }
                    /*
                        if(lastpart){
                        bAnimateready = true;
                        renderer.preserveDrawingBuffer = false;
                        renderer.autoClear = true;
        
                        }else{
                        renderer.render(vector3Dref.scene, camera,false);
                        }
                        */
                    //bAnimateready = true;
                    //alert(vector3Dref.partlist.length);
                    //renderer.render(vector3Dref.scene, camera,false);
                };
                this.clipping = function (plane, constant, onoff) {
                    vector3Dref.clippingOn = onoff;
                    if (onoff) {
                        renderer.clippingPlanes = vector3Dref.globalPlanes;
                    }
                    else {
                        renderer.clippingPlanes = Empty;
                        return;
                    }
                    var percent = constant / 100;
                    switch (plane) {
                        case 0: //x plane
                            var newval = vector3Dref.xPlaneEnd + (vector3Dref.xPlaneconst * percent);
                            vector3Dref.globalPlaneX.constant = newval;
                            break;
                        case 1: //y plane
                            newval = vector3Dref.yPlaneEnd + (vector3Dref.yPlaneconst * percent);
                            vector3Dref.globalPlaneY.constant = newval;
                            break;
                        case 2: //z plane
                            newval = vector3Dref.zPlaneEnd + (vector3Dref.zPlaneconst * percent);
                            vector3Dref.globalPlaneZ.constant = newval;
                            break;
                    }
                };
                this.doexplode = false;
                this.saveCamera = function () {
                    //const savedCamera = JSON.parse( localStorage.getItem( 'savedCamera' ) );
                    //this.cameraSaves = [];
                    //vector3Dref.cameraSaves.push(vector3Dref.parent.camera.position);
                    //vector3Dref.cameraSaves.push(vector3Dref.parent.camera.matrix.toArray());
                    //vector3Dref.cameraSaves.push(pageobject.camera.matrix.toArray());
                    var geometry = new THREE.SphereBufferGeometry(10, 32, 32);
                    var material = new THREE.MeshBasicMaterial({ color: 0xffff00 });
                    var sphere = new THREE.Mesh(geometry, material);
                    var posnum = vector3Dref.cameraSaves.length + 1;
                    sphere.name = "markuppos_" + posnum;
                    vector3Dref.cameraSaves.push({ position: pageobject.camera.matrix.toArray(), name: sphere.name });
                    var cposvector = new THREE.Vector3();
                    cposvector.copy(pageobject.camera.position);
                    cposvector.sub(vector3Dref.scene.position);
                    sphere.position.copy(cposvector);
                    //vector3Dref.scene.position
                    //cposvector.x -= vector3Dref.x;
                    //cposvector.y -= vector3Dref.y;
                    //cposvector.z -= vector3Dref.z;
                    //sphere.position.add(vector3Dref.scene.position);
                    vector3Dref.scene.add(sphere);
                    /*var x = meshobj.dv.getFloat32(meshobj.dvpos, true) - parent.x;
                    meshobj.dvpos += 4;
                    var y = meshobj.dv.getFloat32(meshobj.dvpos, true) - parent.y;
                    meshobj.dvpos += 4;
                    var z = meshobj.dv.getFloat32(meshobj.dvpos, true) - parent.z;*/
                };
                this.restoreCameraName = function (name) {
                    for (var i = 0; i < vector3Dref.cameraSaves.length; i++) {
                        if (vector3Dref.cameraSaves[i].name == name) {
                            pageobject.camera.matrix.fromArray(vector3Dref.cameraSaves[i].position);
                            pageobject.camera.matrix.decompose(pageobject.camera.position, pageobject.camera.quaternion, pageobject.camera.scale);
                        }
                    }
                };
                this.restoreCamera = function () {
                    //var camerapos = vector3Dref.cameraSaves.pop();
                    //vector3Dref.parent.camera.position.copy(camerapos);
                    //camera.position.copy( savedCamera.cameraPosition );
                    //orbitControls.target.copy( savedCamera.targetPosition );
                    if (vector3Dref.cameraSaves.length >= 1) {
                        var camerapos = vector3Dref.cameraSaves.pop();
                        pageobject.camera.matrix.fromArray(camerapos.position);
                        pageobject.camera.matrix.decompose(pageobject.camera.position, pageobject.camera.quaternion, pageobject.camera.scale);
                    }
                    /*savedCamera = JSON.stringify({
                        cameraPosition: camera.position,
                        targetPosition: orbitControls.target
                    });*/
                };
                this.explode = function (distance) {
                    if (!vector3Dref.doexplode) {
                        return;
                    }
                    var scenepos = vector3Dref.originalpos.clone();
                    if (distance == 0) {
                        for (var partix = 0; partix < vector3Dref.scene.children.length; partix++) {
                            if (vector3Dref.scene.children[partix].type == "Mesh") {
                                vector3Dref.scene.children[partix].position.x = 0;
                                vector3Dref.scene.children[partix].position.y = 0;
                                vector3Dref.scene.children[partix].position.z = 0;
                            }
                        }
                        if (this.pxplodefactor != 0) {
                            var pscenepos = vector3Dref.originalpos.clone();
                            var psceneoffset = pscenepos.multiplyScalar(this.pxplodefactor);
                            vector3Dref.scene.position.sub(psceneoffset);
                        }
                        this.pxplodefactor = 0;
                        return;
                    }
                    distance /= 10;
                    //- vector3Dref.pxplodefactor;
                    for (partix = 0; partix < vector3Dref.scene.children.length; partix++) {
                        //var partcenter = this.partlist[partix].center;
                        if (vector3Dref.scene.children[partix].name === "") {
                            continue;
                        }
                        if (vector3Dref.scene.children[partix].type == "Mesh") {
                            if (vector3Dref.partlist[vector3Dref.scene.children[partix].name].center != undefined) {
                                //original center stored in the partlist.
                                var partcenter = vector3Dref.partlist[vector3Dref.scene.children[partix].name].vcentroid.clone();
                                if (this.pxplodefactor != 0) {
                                    var ppartcenter = partcenter.clone();
                                    var ppartoffset = ppartcenter.multiplyScalar(this.pxplodefactor);
                                    ppartcenter.multiplyScalar(vector3Dref.scale);
                                    vector3Dref.scene.children[partix].position.sub(ppartoffset);
                                }
                                //get original center from partlist
                                var partoffset = partcenter.multiplyScalar(distance);
                                partoffset.multiplyScalar(vector3Dref.scale);
                                vector3Dref.scene.children[partix].position.add(partoffset);
                            }
                        }
                    }
                    if (this.pxplodefactor != 0) {
                        pscenepos = vector3Dref.originalpos.clone();
                        psceneoffset = pscenepos.multiplyScalar(this.pxplodefactor);
                        vector3Dref.scene.position.sub(psceneoffset);
                    }
                    var sceneoffset = scenepos.multiplyScalar(distance);
                    //sceneoffset.x /= vector3Dref.scale;
                    //sceneoffset.y /= vector3Dref.scale;
                    //sceneoffset.z /= vector3Dref.scale;
                    vector3Dref.scene.position.add(sceneoffset);
                    vector3Dref.scene.updateMatrix();
                    vector3Dref.pxplodefactor = distance;
                };
                this.setTransparency = function (value) {
                    for (var i = 0; i < vector3Dref.scene.children.length; i++) {
                        if (vector3Dref.scene.children[i].type == "Mesh") {
                            if (vector3Dref.partlist[vector3Dref.scene.children[i].name].transparent == 0) {
                                vector3Dref.scene.children[i].material.transparent = true;
                                //vector3Dref.scene.children[i].material.depthWrite = false;
                                vector3Dref.scene.children[i].material.opacity = value;
                                //renderer.sortObjects = false;
                            }
                        }
                    }
                    // a small explode used to force correct transparency
                    //workaround for now until a better solution present itself
                    vector3Dref.doexplode = true;

                    if (value == 0) {
                        vector3Dref.explode(0);
                    }
                    else {
                        vector3Dref.explode(0.001);
                    }
                    vector3Dref.doexplode = false;
                };
                this.resetTransparency = function () {
                    for (var i = 0; i < vector3Dref.scene.children.length; i++) {
                        if (vector3Dref.scene.children[i].type == "Mesh") {
                            if (vector3Dref.partlist[vector3Dref.scene.children[i].name].transparent == 0) {
                                vector3Dref.scene.children[i].material.transparent = false;
                                vector3Dref.scene.children[i].material.opacity = 1;
                            }
                        }
                    }
                    // a small explode used to force correct transparency
                    //workaround for now until a better solution present itself
                    //vector3Dref.doexplode = true;
                    //vector3Dref.explode(0);
                    //vector3Dref.doexplode = false;
                };
                this.add3DSymbol = function (insertpoint, color) {
                    var geometry = new THREE.PlaneGeometry(5, 20, 32);
                    var material = new THREE.MeshBasicMaterial({ color: 0xffff00, side: THREE.DoubleSide });
                    var plane = new THREE.Mesh(geometry, material);
                    //plane.position
                };
                this.setSelectedPart = function (name) {
                    this.selectedpart = name;
                };
                this.getSelectedPart = function () {
                    return this.selectedpart;
                };
                this.Close = function () {
                    while (this.partlist.length != 0) {
                        this.partlist.pop();
                    }
                    while (this.blocklist.length != 0) {
                        this.blocklist.pop();
                    }
                    while (this.scene.children.length != 0) {
                        var obj = this.scene.children[this.scene.children.length - 1];
                        this.scene.remove(obj);
                        obj = null;
                    }
                    renderer.render(this.scene, parent.camera);
                };
                //parse3D(objid,blockid,newblock,dvpos);
                if (breadbinaryready) {
                    parse3Dloop();
                }
                function yieldingLoop(state, chunksize, callback, finished) {
                    (function chunk() {
                        var i = 0;
                        //var end = Math.min(i + chunksize, count);
                        var end = (i < chunksize);
                        for (; i < chunksize; ++i) {
                            callback.call(null, state);
                        }
                        if (state.objectid != 32768) {
                            setTimeout(chunk, 0);
                        }
                        else {
                            //console.log(state.objectid);
                            finished.call(null);
                        }
                    })();
                }
            }
        }

        class birdseyemarker {
            constructor(bcanvas) {
                var birdseyemark = this;
                this.cx = 0;
                this.cy = 0;
                this.x = 0;
                this.y = 0;
                this.r = 0;
                this.w = bcanvas.width;
                this.h = bcanvas.height;
                this.boxcenter = null;
                this.camerapos = null;
                this.cameraset = false;
                this.canvas = bcanvas;
                //this.ctx = bctx;
                this.strokecolor = "rgba(211, 211, 211, 0.5)";
                //var zoomcolor = "rgba(211, 211, 211, 0.3)";
                this.xscaled = 0;
                this.yscaled = 0;
                this.wscaled = 0;
                this.hscaled = 0;
                this.pagerotation = 0;
                this.linewidth = Math.max(this.w, this.h); //set to largest dimension of window 
                this.selected = false;
                //0 = no action, 1 = move 
                this.editaction = 0;
                this.setpageRotation = function (rot) {
                    birdseyemark.pagerotation = rot;
                };
                this.getcamera = function (size, x, y) {
                    var xdiff = size.x - x;
                    var ydiff = size.y - y;
                    xdiff = xdiff / size.scale;
                    ydiff = ydiff / size.scale;
                    var xdiff = size.boxcenter.x - xdiff;
                    var ydiff = size.boxcenter.y + ydiff;
                    return { x: xdiff, y: ydiff, z: null };
                    //birdseyemark.x = size.x - (xdiff * size.scale);
                    //birdseyemark.y = size.y + (ydiff * size.scale);
                };
                this.setcamera = function (size) {
                    //this.birdseyedispeye = { x: 0, y: 0, r: 0, boxcenter : null, camerapos : null, rotation : 0};
                    //var scale = 7;                    
                    birdseyemark.cx = size.x;
                    birdseyemark.cy = size.y;
                    birdseyemark.x = size.x;
                    birdseyemark.y = size.y;
                    birdseyemark.r = size.r;
                    birdseyemark.rotation = size.rotation;
                    //this.boxcenter = size.boxcenter;
                    //this.camerapos = size.camerapos;
                    var xdiff = size.boxcenter.x - size.camerapos.x;
                    var ydiff = size.boxcenter.y - size.camerapos.y;
                    birdseyemark.x = size.x - (xdiff * size.scale);
                    birdseyemark.y = size.y + (ydiff * size.scale);
                    birdseyemark.cameraset = true;
                    //this.distancefromCenter = size.distance;
                    //this.h = size.h;
                };
                this.setSize = function (size) {
                    //doubles as move function
                    this.x = size.x;
                    this.y = size.y;
                    this.w = size.w;
                    this.h = size.h;
                };
                this.getRotatedMarkup = function (width, height, x, y, anglerad) {
                    var cosangle = Math.cos(anglerad);
                    var sinangle = Math.sin(anglerad);
                    var hw = x - width;
                    var hh = y - height;
                    var newx = (hw * cosangle) - (hh * sinangle);
                    var newy = (hw * sinangle) + (hh * cosangle);
                    var transpoint = new point(newx, newy);
                    transpoint.x = width + transpoint.x;
                    transpoint.y = height + transpoint.y;
                    return transpoint;
                };
                this.eye = function (ctx, x, y, radius, rotation, distance, linewidth, strokecolor) {
                    if (!birdseyemark.cameraset) {
                        return;
                    }
                    ctx.save(); // save the context so we don't mess up others
                    ctx.strokeStyle = 'black';
                    ctx.fillStyle = "rgba(0, 0, 255, 0.5)";
                    ctx.lineWidth = 1;
                    var cwidth = birdseyemark.canvas.width;
                    var cheight = birdseyemark.canvas.height;
                    var startrotation = (Math.PI + (Math.PI * 0.375));
                    //var eyerotation = startrotation + rotation._z;
                    //var heading = rotation._y;
                    //var xrot = rotation._x;
                    const radiansy = rotation.radians > 0 ? rotation.radians : (2 * Math.PI) + rotation.radians;
                    //const radiansx = xrot > 0 ? xrot : (2 * Math.PI) + xrot;
                    //console.log(radians);
                    //const degreesy = THREE.Math.radToDeg(radiansy);
                    //const degrees = THREE.Math.radToDeg(rotation._z);
                    //console.log(radiansy);
                    var radiansoffset = radiansy - (Math.PI * 0.5);
                    /*if(rotation._y < 0){
                        zrotation = rotation._y + (Math.PI * 2);
                    }else if (zrotation > (Math.PI * 2)){
                        zrotation = rotation._y - (Math.PI * 2);
                    }*/
                    //console.log(rotation._z);
                    /*if (startrotation >= (Math.PI * 2)){
                        startrotation = startrotation - Math.PI * 2;
                    }*/
                    startrotation -= radiansoffset;
                    if (startrotation < 0) {
                        startrotation = startrotation + (Math.PI * 2);
                    }
                    else if (startrotation > (Math.PI * 2)) {
                        startrotation = startrotation - (Math.PI * 2);
                    }
                    var endrotation = startrotation + (Math.PI * 0.25);
                    ctx.beginPath();
                    ctx.moveTo(x, y);
                    ctx.arc(x, y, radius, startrotation, endrotation); //45 degree
                    ctx.lineTo(x, y);
                    ctx.arc(x, y, radius * 0.25, 0, Math.PI * 2); //full circle
                    //ctx.arc(birdseyemark.cx, birdseyemark.cy, radius * 0.25, 0, Math.PI * 2); //full circle
                    ctx.fill();
                    //ctx.stroke();
                    /*ctx.beginPath();
                    ctx.arc(birdseyemark.cx, birdseyemark.cy, radius * 0.25, 0, Math.PI * 2); //full circle
                    ctx.fill();*/
                    ctx.restore(); // restore context to what it was on entry
                };
                this.Rect = function (ctx, x, y, width, height, linewidth, strokecolor) {
                    ctx.save(); // save the context so we don't mess up others
                    ctx.strokeStyle = 'black';
                    ctx.fillStyle = strokecolor;
                    ctx.lineWidth = 1;
                    var cwidth = birdseyemark.canvas.width;
                    var cheight = birdseyemark.canvas.height;
                    ctx.beginPath();
                    ctx.moveTo(0, 0);
                    ctx.lineTo(cwidth, 0);
                    ctx.lineTo(cwidth, cheight);
                    ctx.lineTo(0, cheight);
                    ctx.lineTo(0, 0);
                    ctx.closePath();
                    ctx.moveTo(x, y);
                    ctx.lineTo(x, y + height);
                    ctx.lineTo(x + width, y + height);
                    //ctx.lineTo(x, height);
                    //ctx.lineTo(x + width, height);
                    ctx.lineTo(x + width, y);
                    ctx.lineTo(x, y);
                    ctx.closePath();
                    ctx.fill();
                    ctx.stroke();
                    ctx.restore(); // restore context to what it was on entry
                };
                this.drawoffset = function(ctx, offsetx, offsety){

                };
                this.drawme = function (ctx, beye) {
                    var cwidth = birdseyemark.canvas.width;
                    var cheight = birdseyemark.canvas.height;
                    if (birdseyemark.pagerotation != 0) {
                        var txcanv = (cwidth / 2);
                        var tycanv = (cheight / 2);
                        ctx.save();
                        ctx.translate(txcanv, tycanv);
                        ctx.rotate(birdseyemark.pagerotation * (Math.PI / 180));
                        ctx.translate(-txcanv, -tycanv);
                    }
                    ctx.save();
                    if (beye) {
                        var radius = 1;
                        birdseyemark.eye(ctx, this.x, this.y, this.r, this.rotation, this.distancefromCenter, birdseyemark.linewidth, birdseyemark.strokecolor);
                    }
                    else {
                        birdseyemark.Rect(ctx, this.x, this.y, this.w, this.h, birdseyemark.linewidth, birdseyemark.strokecolor);
                    }
                    //birdseyemark.Rect(ctx, this.x, this.y, this.w, this.h, birdseyemark.linewidth, birdseyemark.strokecolor);
                    //(ctx, x, y, width, height, linewidth, strokecolor)
                    ctx.restore();
                    if (birdseyemark.pagerotation != 0) {
                        ctx.restore();
                    }
                };
                this.moveShape = function (x, y) {
                    var localx = x;
                    var localy = y;
                    switch (birdseyemark.pagerotation) {
                        case 0:
                            localx = x;
                            localy = y;
                            break;
                        case 90:
                            localx = y;
                            localy = -x;
                            break;
                        case 180:
                            localx = -x;
                            localy = -y;
                            break;
                        case 270:
                            localx = -y;
                            localy = x;
                            break;
                    }
                    var cwidth = birdseyemark.canvas.width;
                    var cheight = birdseyemark.canvas.height;
                    
                    var startx = 0;
                    var endx = cwidth;
                    var starty = 0;
                    var endy = cheight;


                    this.x = this.x - localx;
                    this.y = this.y - localy;
                    //birdseyemark.pagerotation
                    var rrect = { x: this.x, y: this.y, w: this.w, h: this.h };

                    if (bLimMarkupExtent) {
                        if ((rrect.x - localx) < startx || (rrect.x + localx) > endx) {
                            this.x = this.x + localx;
                        }
                        if (rrect.y - localy < starty || rrect.y - localy > endy) {
                            this.y = this.y + localy;
                        }
                    }
                    if (bLimMarkupExtent) {
                        if ((rrect.x + rrect.w) - localx < startx || (rrect.x + rrect.w) - localx > endx) {
                            this.x = this.x + localx;
                        }
                        if ((rrect.y + rrect.h) - localy < starty || (rrect.y + rrect.h) - localy > endy) {
                            this.y = this.y + localy;
                        }
                    }
                };
                this.Getwithin = function (x, y, x1, x2, y1, y2) {
                    var within = false;
                    if (x > x1 && x < x2) {
                        if (y > y1 && y < y2) {
                            within = true;
                        }
                    }
                    return within;
                };
                this.GetwithinRect = function (x, y, rect) {
                    var within = false;
                    if (x > rect.x && x < rect.w) {
                        if (y > rect.y && y < rect.h) {
                            within = true;
                        }
                    }
                    return within;
                };
                this.iswithin = function (x, y) {
                    var cwidth = birdseyemark.canvas.width;
                    var cheight = birdseyemark.canvas.height;
                    var centercanvX = (cwidth / 2);
                    var centercanvY = (cheight / 2);
                    var CanvRotRad = birdseyemark.pagerotation * (Math.PI / 180);
                    switch (birdseyemark.pagerotation) {
                        case 0:
                            CanvRotRad = birdseyemark.pagerotation * (Math.PI / 180);
                            break;
                        case 90:
                            CanvRotRad = (birdseyemark.pagerotation + 180) * (Math.PI / 180);
                            break;
                        case 270:
                            CanvRotRad = (birdseyemark.pagerotation - 180) * (Math.PI / 180);
                            break;
                        case 180:
                            CanvRotRad = birdseyemark.pagerotation * (Math.PI / 180);
                            break;
                    }
                    var within = false;
                    var orgmousepoint = { x: x, y: y };
                    //var pagerotation = DocObj.getCurrentpage().getpageRotation();
                    if (birdseyemark.pagerotation != 0) {
                        var rotmousepoint = birdseyemark.getRotatedMarkup(centercanvX, centercanvY, x, y, CanvRotRad);
                        x = rotmousepoint.x;
                        y = rotmousepoint.y;
                    }
                    else {
                        //rotmousepoint.x = x;
                        //rotmousepoint.y = y;
                    }
                    var extentx = Math.min(this.x, this.x + this.w);
                    var extentw = Math.max(this.x, this.x + this.w);
                    var extenty = Math.min(this.y, this.y + this.h);
                    var extenth = Math.max(this.y, this.y + this.h);
                    if (birdseyemark.Getwithin(x, y, extentx, extentw, extenty, extenth)) {
                        within = true;
                        //this.seteditaction(1);
                        this.editaction = 1;
                    }
                    var cwidth = birdseyemark.canvas.width - 2;
                    var cheight = birdseyemark.canvas.height - 2;
                    if ((birdseyemark.h) >= cheight && (birdseyemark.w) >= cwidth) {
                        within = false;
                    }
                    /*if((birdseyemark.w + birdseyemark.x) > cwidth){
                        birdseyemark.x = cwidth - birdseyemark.w;
        
                        within = false;
        
                    }
                    if ((birdseyemark.h + birdseyemark.y) > cheight){
                        birdseyemark.y = cheight - birdseyemark.h;
                        
        
                        within = false;
                    }
        
                    if ((birdseyemark.y) < 0){
                        birdseyemark.y = 0;
                        
                        within = false;
                    }
        
                    if ((birdseyemark.x) < 0){
                        
                        birdseyemark.x = 0;
                        within = false;
                    }*/
                    return within;
                };
            }
        }
    
        class birdseyeEvents {
            constructor(thumbnail) {
                var tool = this;
                //ev.preventDefault();
                this.thumb = thumbnail;
                this.x0 = 0;
                this.y0 = 0;
                var x = 0, y = 0, w = 0, h = 0;
                var x0 = 0, y0 = 0;
                var withinrect = false;
                var prevx = 0, prevy = 0;
                this.started = false;
                this.operation = 1;
                /*this.birdseyeEventStates = {
                    started : false,
                    operation : 1,
                };*/
                this.zoomUpdate = function (x, y, w, h) {
                    //tool.zoomUpdate
                    
                    if (tool.thumb.DocRef.bActive) {
                        tool.thumb.zoom_update(x, y, w, h);
                    }
                    //tool.thumb.birdseyeGUIindctx.clearRect(0, 0, tool.thumb.birdseyeGUIindcnv.width, tool.thumb.birdseyeGUIindcnv.height);
                };
                this.panupdate = function (xdiff, ydiff) {
                    if (tool.thumb.DocRef.bActive) {
                        tool.thumb.pan_update(xdiff, ydiff);
                    }
                };
                this.pointermove = function (ev) {
                    tool.MSPointerMove(ev);
                };
                this.pointerdown = function (ev) {
                    tool.MSPointerDown(ev);
                };
                this.pointerup = function (ev) {
                    tool.MSPointerUp(ev);
                };

                function SetCampos(mousePos){
                    if(tool.thumb.DocRef.pages[0].usevector3DGLTF && (tool.thumb.floorplans.length > 0)){
                        // set camera pos
                        /*var campos = tool.thumb.birdseyemarker.getcamera(tool.thumb.birdseyedispeye, touchPos.x, touchPos.y);
                        tool.thumb.set3dcamerapos(campos);*/

                        if(tool.thumb.DocRef.curcontrol3D == 'Walkthrough3D'){
                            var floorindex = tool.thumb.checkcurfloor();
                            var cameraz = tool.thumb.getCurfloorElevation(floorindex);

                            //var campos = tool.thumb.birdseyemarker.getcamera(tool.thumb.birdseyedispeye, mousePos.x, mousePos.y);
                            var campos = tool.thumb.birdseyemarker.getcamera(tool.thumb.birdseyedispeye, mousePos.xus, mousePos.yus);
                            campos.z = cameraz;
                            tool.thumb.set3dcamerapos(campos);
                            tool.thumb.enableFloorcheck();

                        }else if(tool.thumb.DocRef.curcontrol3D == 'markupedit'){
                                //do not update when in markup mode
                        }else{
                            set_tool('Walkthrough3D', {});
                            
                            floorindex = tool.thumb.checkcurfloor();
                            cameraz = tool.thumb.getCurfloorElevation(floorindex);

                            tool.thumb.setbirdseyecamera(null, null,null);
                            //campos = tool.thumb.birdseyemarker.getcamera(tool.thumb.birdseyedispeye, mousePos.x, mousePos.y);
                            campos = tool.thumb.birdseyemarker.getcamera(tool.thumb.birdseyedispeye, mousePos.xus, mousePos.yus);
                            campos.z = cameraz;
                            tool.thumb.set3dcamerapos(campos);
                            tool.thumb.enableFloorcheck();
                            tool.thumb.DocRef.curcontrol3D = 'Walkthrough3D'; 

                            //tool.thumb.setCurmousePos(mousePos.x, mousePos.y);
                            //var campos = tool.thumb.birdseyemarker.getcamera(tool.thumb.birdseyedispeye, mousePos.x, mousePos.y);
                            //tool.thumb.set3dcamerapos(campos);

                        }

                    }
                }



                this.MSPointerDown = function (ev) {
                    if (!tool.thumb.birdseyeGUIindcnv) {
                        return;
                    }
                    if (!tool.thumb.DocRef.bActive) {
                        return;
                    }
                    ev.stopPropagation();
                    var mousePos = getMousePos(tool.thumb.birdseyeGUIindcnv, ev);
                    //tool.x0 = mousePos.x;
                    //tool.y0 = mousePos.y;

                    tool.x0 = mousePos.xus;
                    tool.y0 = mousePos.yus;
    
                    withinrect = false;
                    if (ev.button == 2) {
                        tool.started = true;
                        tool.operation = 2;
                        ev.target.style.cursor = 'crosshair';
                        withinrect = false;
                    }
                    else {
                        //move marker rectangle.
                        if (tool.thumb.birdseyemarker) {
                            //withinrect = tool.thumb.birdseyemarker.iswithin(mousePos.x, mousePos.y);
                            withinrect = tool.thumb.birdseyemarker.iswithin(mousePos.xus, mousePos.yus);
                            if (withinrect) {
                                tool.operation = 1;
                                tool.started = true;
                            }
                        }
                        SetCampos(mousePos);


                        /*if (tool.thumb.DocRef.pages[0].usevector3DGLTF && (tool.thumb.floorplans.length > 0)) {
                            // set camera pos
                            if (tool.thumb.DocRef.curcontrol3D == 'Walkthrough3D') {
                                var campos = tool.thumb.birdseyemarker.getcamera(tool.thumb.birdseyedispeye, mousePos.x, mousePos.y);
                                tool.thumb.set3dcamerapos(campos);
                            }
                            else {
                                set_tool('Walkthrough3D', {});
                                tool.thumb.setbirdseyecamera(null, null, null);
                                var floorindex = tool.thumb.checkcurfloor();
                                var cameraz = tool.thumb.getCurfloorElevation(floorindex);
                                var campos = tool.thumb.birdseyemarker.getcamera(tool.thumb.birdseyedispeye, mousePos.x, mousePos.y);
                                campos.z = cameraz;
                                tool.thumb.set3dcamerapos(campos);
                                //var campos = tool.thumb.birdseyemarker.getcamera(tool.thumb.birdseyedispeye, mousePos.x, mousePos.y);
                                //tool.thumb.set3dcamerapos(campos);
                            }
                        }*/
                        //tool.operation = 1;
                        //tool.started = true;
                        prevx = tool.x0;
                        prevy = tool.y0;
                        //tool.operation = 1;
                        //tool.started = true;
                        //prevx = tool.x0;
                        //prevy = tool.y0;
                    }
                };
                this.mousedown = function (ev) {
                    if (!tool.thumb.birdseyeGUIindcnv) {
                        return;
                    }
                    if (!tool.thumb.DocRef.bActive) {
                        return;
                    }
                    ev.preventDefault();
                    ev.stopPropagation();
                    var mousePos = getMousePos(tool.thumb.birdseyeGUIindcnv, ev);
                    //tool.x0 = mousePos.x;
                    //tool.y0 = mousePos.y;

                    tool.x0 = mousePos.xus;
                    tool.y0 = mousePos.yus;
    

                    withinrect = false;
                    if (ev.button == 2) {
                        //zoom mode
                        ev.target.style.cursor = 'crosshair';
                        tool.started = true;
                        tool.operation = 2;
                        withinrect = false;
                    }
                    else {
                        //move marker rectangle.
                        //ev.target.style.cursor = 'default';
                        //tool.operation = 1;     
                        if (tool.thumb.birdseyemarker) {
                            //withinrect = tool.thumb.birdseyemarker.iswithin(mousePos.x, mousePos.y);
                            withinrect = tool.thumb.birdseyemarker.iswithin(mousePos.xus, mousePos.yus);
                            if (withinrect) {
                                tool.operation = 1;
                                tool.started = true;
                            }
                            SetCampos(mousePos);

                            /*if (tool.thumb.DocRef.pages[0].usevector3DGLTF && (tool.thumb.floorplans.length > 0)) {
                                // set camera pos
                                var campos = tool.thumb.birdseyemarker.getcamera(tool.thumb.birdseyedispeye, mousePos.x, mousePos.y);
                                tool.thumb.set3dcamerapos(campos);
                                if (tool.thumb.DocRef.curcontrol3D == 'Walkthrough3D') {
                                    var campos = tool.thumb.birdseyemarker.getcamera(tool.thumb.birdseyedispeye, mousePos.x, mousePos.y);
                                    tool.thumb.set3dcamerapos(campos);
                                }
                                else {
                                    set_tool('Walkthrough3D', {});
                                    tool.thumb.setbirdseyecamera(null, null, null);
                                    var floorindex = tool.thumb.checkcurfloor();
                                    var cameraz = tool.thumb.getCurfloorElevation(floorindex);
                                    var campos = tool.thumb.birdseyemarker.getcamera(tool.thumb.birdseyedispeye, mousePos.x, mousePos.y);
                                    campos.z = cameraz;
                                    tool.thumb.set3dcamerapos(campos);
                                    //var campos = tool.thumb.birdseyemarker.getcamera(tool.thumb.birdseyedispeye, mousePos.x, mousePos.y);
                                    //tool.thumb.set3dcamerapos(campos);
                                }
                            }*/
                        }
                        //tool.started = true;
                        //tool.operation = 1;
                        prevx = tool.x0;
                        prevy = tool.y0;
                    }
                };
                this.touchstart = function (ev) {
                    if (!tool.thumb.DocRef.bActive) {
                        return;
                    }
                    ev.preventDefault();
                    tool.started = true;
                    var touchPos = getTouchPos(tool.thumb.birdseyeGUIindcnv, ev);
                    tool.operation = 1;
                    if (tool.thumb.birdseyemarker) {


                        withinrect = tool.thumb.birdseyemarker.iswithin(touchPos.xus, touchPos.yus);


                        if (withinrect) {
                            tool.operation = 1;
                            tool.started = true;
                        }
                        SetCampos(mousePos);

                        /*if (tool.thumb.DocRef.pages[0].usevector3DGLTF && (tool.thumb.floorplans.length > 0)) {
                            // set camera pos
                            var campos = tool.thumb.birdseyemarker.getcamera(tool.thumb.birdseyedispeye, touchPos.x, touchPos.y);
                            tool.thumb.set3dcamerapos(campos);
                            if (tool.thumb.DocRef.curcontrol3D == 'Walkthrough3D') {
                                var campos = tool.thumb.birdseyemarker.getcamera(tool.thumb.birdseyedispeye, mousePos.x, mousePos.y);
                                tool.thumb.set3dcamerapos(campos);
                            }
                            else {
                                set_tool('Walkthrough3D', {});
                                var floorindex = tool.thumb.checkcurfloor();
                                var cameraz = tool.thumb.getCurfloorElevation(floorindex);
                                tool.thumb.setbirdseyecamera(null, null, null);
                                var campos = tool.thumb.birdseyemarker.getcamera(tool.thumb.birdseyedispeye, mousePos.x, mousePos.y);
                                campos.z = cameraz;
                                tool.thumb.set3dcamerapos(campos);
                                //tool.thumb.setCurmousePos(mousePos.x, mousePos.y);
                                //var campos = tool.thumb.birdseyemarker.getcamera(tool.thumb.birdseyedispeye, mousePos.x, mousePos.y);
                                //tool.thumb.set3dcamerapos(campos);
                            }
                        }*/
                    }
                    //move rectangle code goes here
                    tool.x0 = touchPos.xus;
                    tool.y0 = touchPos.yus;


                    prevx = tool.x0;
                    prevy = tool.y0;
                    //x = tool.x0;
                    //y = tool.y0;
                };
                this.MSPointerMove = function (ev) {
                    if (!tool.thumb.DocRef.bActive) {
                        return;
                    }
                    ev.preventDefault();
                    ev.stopPropagation();
                    if (tool.operation == 2) {
                        ev.target.style.cursor = 'crosshair';
                    }
                    if (!tool.started) {
                        return;
                    }
                    var mousePos = getMousePos(tool.thumb.birdseyeGUIindcnv, ev);
                    //x = Math.min(mousePos.x, tool.x0);
                    //y = Math.min(mousePos.y, tool.y0);
                    //w = Math.abs(mousePos.x - tool.x0);
                    //h = Math.abs(mousePos.y - tool.y0);

                    x = Math.min(mousePos.xus, tool.x0);
                    y = Math.min(mousePos.yus, tool.y0);
                    w = Math.abs(mousePos.xus - tool.x0);
                    h = Math.abs(mousePos.yus - tool.y0);
    

                    if (!w || !h) {
                        return;
                    }
                    if (tool.operation == 2 && !tool.thumb.DocRef.pages[0].usevector3DGLTF) {
                        var zoomcolor = "rgba(175, 175, 175, 0.3)";
                        tool.thumb.birdseyeGUIindctx.lineWidth = 1;
                        tool.thumb.birdseyeGUIindctx.strokeStyle = zoomcolor;
                        tool.thumb.birdseyeGUIindctx.fillStyle = zoomcolor;
                        tool.thumb.birdseyeGUIindctx.clearRect(0, 0, tool.thumb.birdseyeGUIindcnv.width, tool.thumb.birdseyeGUIindcnv.height);
                        tool.thumb.birdseyeGUIindctx.fillRect(x, y, w, h);
                    }
                    else if (tool.operation == 1) {
                        //move rectangle code goes here.
                        //var xdiff = prevx - mousePos.x;
                        //var ydiff = prevy - mousePos.y;

                        var xdiff = prevx - mousePos.xus;
                        var ydiff = prevy - mousePos.yus;
    

                        if (tool.thumb.birdseyemarker) {
                            tool.panupdate(xdiff, ydiff);
                            //use scaled pan instead,
                        }
                        //prevx = mousePos.x;
                        //prevy = mousePos.y;
                        prevx = mousePos.xus;
                        prevy = mousePos.yus;
    
                    }
                };
                this.mousemove = function (ev) {
                    if (!tool.thumb.DocRef.bActive) {
                        return;
                    }
                    ev.stopPropagation();
                    if (!tool.started) {
                        return;
                    }
                    if (tool.operation == 2 && !tool.thumb.DocRef.pages[0].usevector3DGLTF) {
                        ev.target.style.cursor = 'crosshair';
                    }
                    var mousePos = getMousePos(tool.thumb.birdseyeGUIindcnv, ev);
                    //x = Math.min(mousePos.x, tool.x0);
                    //y = Math.min(mousePos.y, tool.y0);
                    //w = Math.abs(mousePos.x - tool.x0);
                    //h = Math.abs(mousePos.y - tool.y0);

                    x = Math.min(mousePos.xus, tool.x0);
                    y = Math.min(mousePos.yus, tool.y0);
                    w = Math.abs(mousePos.xus - tool.x0);
                    h = Math.abs(mousePos.yus - tool.y0);
    

                    if (!w || !h) {
                        return;
                    }
                    if (tool.operation == 2 && !tool.thumb.DocRef.pages[0].usevector3DGLTF) {
                        var zoomcolor = "rgba(175, 175, 175, 0.3)";
                        tool.thumb.birdseyeGUIindctx.lineWidth = 1;
                        tool.thumb.birdseyeGUIindctx.strokeStyle = zoomcolor;
                        tool.thumb.birdseyeGUIindctx.fillStyle = zoomcolor;
                        tool.thumb.birdseyeGUIindctx.clearRect(0, 0, tool.thumb.birdseyeGUIindcnv.width, tool.thumb.birdseyeGUIindcnv.height);
                        tool.thumb.birdseyeGUIindctx.fillRect(x, y, w, h);
                    }
                    else if (tool.operation == 1) {
                        //move rectangle code goes here.
                        //var xdiff = prevx - mousePos.x;
                        //var ydiff = prevy - mousePos.y;

                        var xdiff = prevx - mousePos.xus;
                        var ydiff = prevy - mousePos.yus;
    
                        //need to modify        
                        //DocObj.getCurrentpage().pan_update(xdiff, ydiff);
                        if (tool.thumb.birdseyemarker) {
                            tool.panupdate(xdiff, ydiff);
                            //use scaled pan instead,
                        }
                        //prevx = mousePos.x;
                        //prevy = mousePos.y;

                        prevx = mousePos.xus;
                        prevy = mousePos.yus;
    

                    }
                };
                this.touchmove = function (ev) {
                    if (!tool.thumb.DocRef.bActive) {
                        return;
                    }
                    ev.preventDefault();
                    if (!tool.started) {
                        return;
                    }
                    var touchPos = getTouchPos(tool.thumb.birdseyeGUIindcnv, ev);

                    x = Math.min(touchPos.xus, tool.x0);
                    y = Math.min(touchPos.yus, tool.y0);
                    w = Math.abs(touchPos.xus - tool.x0);
                    h = Math.abs(touchPos.yus - tool.y0);


                    //var xdiff = prevx - touchPos.x;
                    //var ydiff = prevy - touchPos.y;

                    var xdiff = prevx - touchPos.xus;
                    var ydiff = prevy - touchPos.yus;


                    //need to modify
                    //DocObj.getCurrentpage().pan_update(xdiff, ydiff);
                    if (tool.thumb.birdseyemarker && tool.operation == 1) {
                        tool.panupdate(xdiff, ydiff);
                        //use scaled pan instead,
                    }
                    //prevx = touchPos.x;
                    //prevy = touchPos.y;

                    prevx = touchPos.xus;
                    prevy = touchPos.yus;

                    //pan only
                    //bctx.clearRect(0, 0, params.p2.width, params.p2.height);
                    //bctx.lineWidth = 1;
                    //bctx.strokeStyle = zoomcolor;
                    //bctx.fillRect(x, y, w, h);
                };
                this.mouseout = function (ev) {
                    if (!tool.thumb.DocRef.bActive) {
                        return;
                    }
                    ev.target.style.cursor = 'default';
                    if (tool.started) {
                        tool.started = false;
                        if (tool.operation == 2) {
                            tool.thumb.birdseyeGUIindctx.clearRect(0, 0, tool.thumb.birdseyeGUIindcnv.width, tool.thumb.birdseyeGUIindcnv.height);
                        }
                    }
                };
                this.MSPointerOut = function (ev) {
                    if (!tool.thumb.DocRef.bActive) {
                        return;
                    }
                    ev.target.style.cursor = 'default';
                    if (tool.started) {
                        tool.started = false;
                        if (tool.operation == 2) {
                            tool.thumb.birdseyeGUIindctx.clearRect(0, 0, tool.thumb.birdseyeGUIindcnv.width, tool.thumb.birdseyeGUIindcnv.height);
                        }
                    }
                };
                this.MSPointerUp = function (ev) {
                    if (!tool.thumb.DocRef.bActive) {
                        return;
                    }
                    ev.target.style.cursor = 'default';
                    if (tool.started) {
                        tool.started = false;
                        if (tool.operation == 2) {
                            //zoom update
                            tool.zoomUpdate(x, y, w, h);
                        }
                    }
                };
                this.mouseup = function (ev) {
                    if (!tool.thumb.DocRef.bActive) {
                        return;
                    }
                    ev.target.style.cursor = 'default';
                    if (tool.started) {
                        tool.started = false;
                        if (tool.operation == 2) {
                            //zoom update
                            tool.zoomUpdate(x, y, w, h);
                        }
                    }
                };
                this.touchcancel = function (ev) {
                    if (!tool.thumb.DocRef.bActive) {
                        return;
                    }
                    if (tool.started) {
                        tool.started = false;
                    }
                };
                this.touchend = function (ev) {
                    if (!tool.thumb.DocRef.bActive) {
                        return;
                    }
                    if (tool.started) {
                        tool.started = false;
                    }
                };
            }
        }
    
        class ThumbnailObject {
            constructor(pagexml, layoutname, DocRef, pagenum) {
                var thisthumb = this;
                var relpath = xmlurlrel;
                this.DocRef = DocRef;
                this.pagenumber = pagenum;
                this.dxthumb = 0.0;
                this.dythumb = 0.0;
                this.dxthumbtemp = 0.0;
                this.dythumbtemp = 0.0;
                this.pageRendering = false;
                this.dscalethumb = 1.0;
                this.bserverthumbnail = false;
                this.dscalebirdseye = 1.0;
                this.pdftempthumbscale = 1.0;
                this.pdfpagewidth = 1;
                this.pdfpageheight = 1;
                this.usefoxitthumb = false;
                this.bautoloadthumbnails = bAutoloadThumbnails;
                this.bfinalsize = false;
                this.floorplanWidth = 4096;
                this.floorplanHeight = 2048;
                this.birdseyeWidth = 350;
                this.birdseyeHeight = 275;
                this.floorplanimageWidth = 1000;
                this.floorplanimageHeight = 1000;
                this.floorplanreplaceScale = 1;
                this.nav3dSize = { width: 0, height: 0 };
                this.birdseyeGUIimgcnv = null;
                this.birdseyeGUIindcnv = null;
                this.birdseyemarkupcnv = null;
                this.birdseyeGUIimgctx = null;
                this.birdseyeGUIindctx = null;
                this.birdseyemarkupctx = null;
                this.birdseyemarker = null;
                this.beventsSet = false;
                this.birdseyedrawn = false;
                this.floorplandrawn = false;
                this.birdseyeEvents = undefined;
                this.floorelevation = { min: null, max: null, index: 0 };
                this.camerapos = new THREE.Vector3(0, 0, 0);
                this.camerarot = new THREE.Euler(0, 1, 1.57, 'XYZ');
                this.boxcenter = new THREE.Vector3(0, 0, 0);
                this.floorplans = [];
                this.floorplanCallback = null;
                this.replacefloornavCallback = null;
                this.targetfloor = null;
                this.alignarray = null;
                this.floorplan3dobject = null;
                this.usefloorcheck = true;

                this.birdseyedisprect = { x: 0, y: 0, w: 0, h: 0 };
                this.birdseyedispeye = { x: 0, y: 0, r: 0, scale: 1, boxcenter: null, camerapos: null, rotation: 0 };
                this.birdseyeEventStates = {
                    started: false,
                    operation: 1,
                };
                this.birdseyeviewport = null;
                this.thumbnailviewport = null;
                this.floorplanviewport = null;
                this.floorplanrepviewport = null;
                this.LayoutName = layoutname;
                this.title = this.LayoutName;

                if (pagexml.getElementsByTagName("PageName")[0] != undefined) {
                    this.PageName = pagexml.getElementsByTagName("PageName")[0].firstChild.nodeValue;
                }
                else {
                    this.PageName = LayoutName;
                }

                //do not use unless we have layoutname
                //this.title = pagenum + 1 + "/" + this.DocRef.pages.length;
                if (pagexml.getElementsByTagName("ThumbnailImageSRC")[0] != undefined) {
                    if (pagexml.getElementsByTagName("ThumbnailImageSRC")[0].firstChild != undefined) {
                        this.ThumbnailImageSRC = relpath + pagexml.getElementsByTagName("ThumbnailImageSRC")[0].firstChild.nodeValue;
                        this.bserverthumbnail = true;
                        this.bautoloadthumbnails = true;

                    }else{
                        this.bautoloadthumbnails = false;
                    }
                }else{
                    this.bautoloadthumbnails = false;
                }
                if (pagexml.getElementsByTagName("VectorGLTF3DSRC")[0] != undefined) {
                    this.Vector3DSRC = encodeURI(relpath + pagexml.getElementsByTagName("VectorGLTF3DSRC")[0].firstChild.nodeValue);
                    this.usevector3Dxml = true;
                    //thisthumb.DocRef.getCurrentpage().Vector3DPageObj
                }
                if (pagexml.getElementsByTagName("Vector3DSRC")[0] != undefined) {
                    this.Vector3DSRC = encodeURI(relpath + pagexml.getElementsByTagName("Vector3DSRC")[0].firstChild.nodeValue);
                    this.usevector3Dxml = true;
                }
                if (pagexml.getElementsByTagName("ThumbnailWidth")[0] != undefined) {
                    this.ThumbnailWidth = pagexml.getElementsByTagName("ThumbnailWidth")[0].firstChild.nodeValue;
                }
                else {
                    this.ThumbnailWidth = ThumbnailWidth;

                    
            

                }
                if (pagexml.getElementsByTagName("ThumbnailWidth")[0] != undefined) {
                    this.ThumbnailHeight = pagexml.getElementsByTagName("ThumbnailHeight")[0].firstChild.nodeValue;
                }
                else {
                    this.ThumbnailHeight = ThumbnailHeight;
                }
                var szformatshort = thisthumb.DocRef.Format.substring(0, 9);
                if (szformatshort == "Adobe PDF") {
                    this.bautoloadthumbnails = false;
                }
                //this.bautoloadthumbnails = false;
                //this.ThumbnailWidth = 180;
                this.thumbcanvas = document.createElement('canvas');
                this.thumbcanvas.width = parseInt(this.ThumbnailWidth);
                this.thumbcanvas.height = parseInt(this.ThumbnailHeight);
                this.thumbctx = this.thumbcanvas.getContext('2d', { willReadFrequently: true });
                this.thumbloaded = this.bautoloadthumbnails;
                //thisthumb.birdseyeobj.ctx
                this.thumbnailobj = {
                    created: false,
                    thumbnail: document.createElement('canvas'),
                    ctx: null,
                    rotation: 0,
                    image: document.createElement('img'),
                    imageData: null,
                    canvasSource: document.createElement('img'),
                    source: this.bautoloadthumbnails ? this.ThumbnailImageSRC : null,
                    name: this.PageName,
                    number: pagenum,
                    displaynum: pagenum + 1,
                    draw: function (ctx) {
                        thisthumb.thumbctx = ctx;
                        thisthumb.draw_thumbnail();
                    }
                };
                this.thumbnailobj.thumbnail.width = parseInt(this.ThumbnailWidth);
                this.thumbnailobj.thumbnail.height = parseInt(this.ThumbnailHeight);
                this.thumbnailobj.ctx = this.thumbnailobj.thumbnail.getContext('2d', { willReadFrequently: true });

                this.birdseyeobj = {
                    created: false,
                    birdseye: document.createElement('canvas'),
                    imageData: null,
                    ctx: null,
                    rotation: 0,
                    ignorerotation : false,
                    dscalebirdseye: 1.0,
                    number: pagenum
                };
                this.birdseyeobj.ctx = this.birdseyeobj.birdseye.getContext('2d');
                this.floorplanobj = {
                    created: false,
                    floorplan: document.createElement('canvas'),
                    ctx: null,
                    rotation: 0,
                    dscalefloorplan: 1.0,
                    number: pagenum
                };
                this.floorplanobj.ctx = this.floorplanobj.floorplan.getContext('2d');
                this.ev_birdseye = function (ev) {
                    if (ev.layerX || ev.layerY == 0) { // Firefox
                        ev._x = ev.layerX;
                        ev._y = ev.layerY;
                    }
                    else if (ev.offsetX || ev.offsetY == 0) { // Opera
                        ev._x = ev.offsetX;
                        ev._y = ev.offsetY;
                    }
                    //DocObj.thisdocument.thumbnails[thisdocument.currentpage].setBirdseyeTool();
                    if (thisthumb.birdseyeEvents == undefined) {
                        return;
                    }
                    var func = thisthumb.birdseyeEvents[ev.type];
                    if (func) {
                        func(ev);
                    }
                };
                this.disableFloorcheck = function(){
                    thisthumb.usefloorcheck = false;
                };

                this.enableFloorcheck = function(){
                    thisthumb.usefloorcheck = true;
                };
                this.connectCampos = function (Vector3DPageObj) {
                    if (Vector3DPageObj.walkthroughcontrol) {
                        //this.camdirection = Vector3DPageObj.walkthroughcontrol.getDirection();
                    }
                };
                this.setBirdseyeFloor = function(floornum){

                    if(thisthumb.floorelevation.index != floornum){
                        thisthumb.floorelevation.index = floornum;
                        thisthumb.birdseyedrawn = false;
                        thisthumb.set3DNavImage(Drawbirdseye, true);
                    }
                    thisthumb.disableFloorcheck();

                };
                this.setbirdseyecamera = function (position, rotation, boxcenter) {
                    if (!thisthumb.birdseyeGUIindcnv) {
                        return;
                    }
                    if (!thisthumb.DocRef.bActive) {
                        return;
                    }

                    if(thisthumb.floorplans.length == 0){
                        return;
                    }

                    if (boxcenter == null) {
                        boxcenter = thisthumb.DocRef.getBoxcenter();
                        if (boxcenter == null) {
                            return;
                        }
                    }
                    if (position == null) {
                        position = thisthumb.DocRef.getBoxcenter();
                        if (position == null) {
                            return;
                        }
                    }
                    if (rotation == null) {
                        rotation = { degrees: 90, radians: (Math.PI * 0.5) };
                    }
                    //var directionv = new THREE.Vector3().copy(position);
                    //var dirfunc = thisthumb.DocRef.pages[thisthumb.pagenumber].walkthroughcontrol.getDirection();
                    //directionv = dirfunc(directionv);
                    //var direction = new THREE.Vector3( 0, 0, -1 );
                    //directionv.copy( direction ).applyQuaternion( thisthumb.DocRef.pages[thisthumb.pagenumber].walkthroughcontrol.getObject().quaternion );
                    //console.log("x: ", directionv.x, "y: ", directionv.y, "z: ", directionv.z  );
                    //rotation.radians = directionv.z;
                    //var directionv =  thisthumb.camdirection(directionv);
                    thisthumb.birdseyedispeye.distance = position.distanceTo(boxcenter);
                    thisthumb.birdseyedispeye.rotation = rotation;
                    var width = thisthumb.birdseyeGUIindcnv.width;
                    var height = thisthumb.birdseyeGUIindcnv.height;
                    //var planscale = thisthumb.birdseyeobj.dscalebirdseye * thisthumb.floorplans[thisthumb.floorelevation.index].Scale;
                    //console.log(thisthumb.birdseyeobj.dscalebirdseye);
                    //console.log(thisthumb.floorplans[thisthumb.floorelevation.index].Scale);
                    thisthumb.birdseyedispeye.r = 32;
                    thisthumb.birdseyedispeye.x = width * 0.5;
                    thisthumb.birdseyedispeye.y = height * 0.5;
                    thisthumb.birdseyedispeye.scale = thisthumb.birdseyeobj.dscalebirdseye * thisthumb.floorplans[thisthumb.floorelevation.index].Scale;
                    if (thisthumb.floorplans[thisthumb.floorelevation.index].replaced) {
                        //var wscale = thisthumb.floorplans[thisthumb.floorelevation.index].width / thisthumb.floorplans[thisthumb.floorelevation.index].originalWidth;
                        //var hscale = thisthumb.floorplans[thisthumb.floorelevation.index].height / thisthumb.floorplans[thisthumb.floorelevation.index].originalHeight;
                        thisthumb.birdseyedispeye.scale *= thisthumb.floorplans[thisthumb.floorelevation.index].replaceScale;
                        thisthumb.birdseyedispeye.x += -(thisthumb.floorplans[thisthumb.floorelevation.index].offsetX);
                        thisthumb.birdseyedispeye.y -= -(thisthumb.floorplans[thisthumb.floorelevation.index].offsetY);
                    }
                    //thisthumb.floorplans[thisthumb.floorelevation.index].Scale
                    thisthumb.birdseyedispeye.boxcenter = boxcenter;
                    thisthumb.birdseyedispeye.camerapos = position;
                    thisthumb.birdseyemarker.setcamera(thisthumb.birdseyedispeye);
                    var floor = thisthumb.checkcurfloor();
                    if (thisthumb.floorelevation.index != floor) {
                        thisthumb.floorelevation.index = floor;
                        thisthumb.birdseyedrawn = false;
                        thisthumb.set3DNavImage(Drawbirdseye, false);
                    }
                };
                this.birdseyecamera = function () {
                    if (!thisthumb.birdseyeGUIindcnv) {
                        return;
                    }
                    if (!thisthumb.DocRef.bActive) {
                        return;
                    }
                    //var scale = thisthumb.birdseyeGUIindcnv.width / pagerect.w;
                    if (thisthumb.birdseyeGUIindctx) {
                        thisthumb.birdseyeGUIindctx.clearRect(0, 0, thisthumb.birdseyeGUIindcnv.width, thisthumb.birdseyeGUIindcnv.height);
                        thisthumb.birdseyemarker.drawme(thisthumb.birdseyeGUIindctx, true);
                    }
                };
                this.birdseyerect = function (pagerect) {
                    if (!thisthumb.birdseyeGUIindcnv) {
                        return;
                    }
                    if (!thisthumb.DocRef.bActive) {
                        return;
                    }
                    /*if (babs){
                        thispage.startx = sx;
                        thispage.starty = sy;
                        thispage.endx = ex;
                        thispage.endy = ey;
            
                    }else{
                        thispage.startx = sx;
                        thispage.starty = sy;
                        thispage.endx = ex + sx;
                        thispage.endy = ey + sy;
        
                    }*/
                    var scale = thisthumb.birdseyeGUIindcnv.width / pagerect.w;
                    var width = thisthumb.birdseyeGUIindcnv.width;
                    var height = thisthumb.birdseyeGUIindcnv.height;

                    var x = 0;
                    var y = 0;
                    //var w = canvasowidth - pagerect.w;
                    //var h = canvasoheight - pagerect.h;

                    var w = canvasowidth;
                    var h = canvasoheight;

                    if(thisthumb.DocRef.pages[thisthumb.pagenumber].usepdfjs){
                        w = canvasowidthsize;
                        h = canvasoheightsize;
    
                    }

                    //thisthumb.birdseyedisprect.w = canvasowidth * scale;
                    //thisthumb.birdseyedisprect.h = canvasoheight * scale;


                    // = {x : 0, y : 0, w : 0, h : 0};
                    if (w > 0) {
                        thisthumb.birdseyedisprect.w = width;
                    }
                    else {
                        thisthumb.birdseyedisprect.w = width - (Math.abs(w) * scale);
                    }

                    thisthumb.birdseyedisprect.w = w * scale;

                    if (h > 0) {
                        thisthumb.birdseyedisprect.h = height;
                    }
                    else {
                        thisthumb.birdseyedisprect.h = height - (Math.abs(h) * scale);
                    }

                    thisthumb.birdseyedisprect.h = h * scale;


                    if (pagerect.x > 0) {
                        thisthumb.birdseyedisprect.x = 0;
                        thisthumb.birdseyedisprect.w = thisthumb.birdseyedisprect.w - Math.abs(pagerect.x) * scale;

                    }
                    else {
                        thisthumb.birdseyedisprect.x = Math.abs(pagerect.x) * scale;
                    }
                    if (pagerect.y > 0) {
                        thisthumb.birdseyedisprect.y = 0;
                        thisthumb.birdseyedisprect.h = thisthumb.birdseyedisprect.h - Math.abs(pagerect.y) * scale;
                    }
                    else {
                        thisthumb.birdseyedisprect.y = Math.abs(pagerect.y) * scale;
                    }


                    if(pagerect.w < canvasowidth){
                        thisthumb.birdseyedisprect.w = width;
                    }

                    if(pagerect.h < canvasoheight){
                        thisthumb.birdseyedisprect.h = height;
                    }


                    thisthumb.birdseyemarker.setSize(thisthumb.birdseyedisprect);
                    if (thisthumb.birdseyeGUIindctx) {
                        thisthumb.birdseyeGUIindctx.clearRect(0, 0, thisthumb.birdseyeGUIindcnv.width, thisthumb.birdseyeGUIindcnv.height);
                        thisthumb.birdseyemarker.drawme(thisthumb.birdseyeGUIindctx, false);
                    }
                };
                this.set3dcamerapos = function (campos) {
                    if (thisthumb.DocRef.pages.lenght == 0 || thisthumb.DocRef.pages[thisthumb.pagenumber] == undefined) {
                        return;
                    }
                    if (!thisthumb.DocRef.bActive) {
                        return;
                    }
                    if (thisthumb.DocRef.pages[0].usevector3DGLTF) {
                        thisthumb.DocRef.pages[thisthumb.pagenumber].walkthroughcontrol.setCameraXYPos(campos);
                    }
                };
                this.pan_update = function (xdiff, ydiff) {
                    if (thisthumb.DocRef.pages.lenght == 0 || thisthumb.DocRef.pages[thisthumb.pagenumber] == undefined) {
                        return;
                    }
                    if (!thisthumb.DocRef.bActive) {
                        return;
                    }
                    
                    


                    if(thisthumb.DocRef.pages[thisthumb.pagenumber].usepdfjs){

                        var pagerect = thisthumb.DocRef.pages[thisthumb.pagenumber].getpagerectdirect()
                        pagerect.w = pagerect.w - pagerect.x;

                    }else{
                        //thispage.DocRef.thumbnails[thispage.pagenumber].birdseyerect(thispage.getpagerect());
                        pagerect = thisthumb.DocRef.pages[thisthumb.pagenumber].getpagerect();
                        
                    }



                    var scale = pagerect.w / thisthumb.birdseyeGUIindcnv.width;
                    //if (thisthumb.DocRef.foxitdoc )
                    if (thisthumb.DocRef.foxitdoc != null) {
                        scale /= PixelToPoint;
                    }
                    xdiff *= scale;
                    ydiff *= scale;
                    thisthumb.DocRef.pages[thisthumb.pagenumber].pan_update(-xdiff, -ydiff);
                };
                this.zoom_update = function (x, y, w, h) {
                    if (thisthumb.DocRef.pages.lenght == 0 || thisthumb.DocRef.pages[thisthumb.pagenumber] == undefined) {
                        return;
                    }
                    if (!thisthumb.DocRef.bActive) {
                        return;
                    }
                    //var pagerect = thisthumb.DocRef.pages[thisthumb.pagenumber].getpagerect();

                    if(thisthumb.DocRef.pages[thisthumb.pagenumber].usepdfjs){

                        var pagerect = thisthumb.DocRef.pages[thisthumb.pagenumber].getpagerectdirect()
                        pagerect.w = pagerect.w - pagerect.x;

                    }else{
                        //thispage.DocRef.thumbnails[thispage.pagenumber].birdseyerect(thispage.getpagerect());
                        pagerect = thisthumb.DocRef.pages[thisthumb.pagenumber].getpagerect();
                    }


                    var scale = pagerect.w / thisthumb.birdseyeGUIindcnv.width;
                    x *= scale;
                    y *= scale;
                    w *= scale;
                    h *= scale;
                    x += pagerect.x;
                    y += pagerect.y;
                    thisthumb.DocRef.pages[thisthumb.pagenumber].zoom_update(x, y, w, h);
                    //console.log('birds Scale',scale);
                    //var width = thisthumb.birdseyeGUIindcnv.width;
                    //var height = thisthumb.birdseyeGUIindcnv.width;
                };
                this.setEventlisteners = function () {
                    if (thisthumb.birdseyeGUIindcnv != null) {
                        thisthumb.birdseyeEvents = new birdseyeEvents(thisthumb);
                        regEventsgeneric(thisthumb.birdseyeGUIindcnv, thisthumb.ev_birdseye);
                        thisthumb.beventsSet = true;
                    }
                };
                this.setBirdseyeTool = function () {
                    if (thisthumb.birdseyeGUIindcnv == null) {
                        return;
                    }
                    if (!thisthumb.beventsSet) {
                        thisthumb.setEventlisteners();
                    }
                    //set_tool('birdseye', {p1 : thisthumb.birdseyeGUIindcnv, p2 : thisthumb.birdseyeGUIindctx});
                };
                this.setGUIbirdseyeCnv = function (birdseyeGUIimgcnv, birdseyeGUIindcnv, birdseyemarkupcnv) {
                    if (thisthumb.birdseyeGUIimgcnv == null) {
                        thisthumb.birdseyeGUIimgcnv = birdseyeGUIimgcnv;
                        thisthumb.birdseyeGUIindcnv = birdseyeGUIindcnv;
                        thisthumb.birdseyemarkupcnv = birdseyemarkupcnv;
                        thisthumb.birdseyeGUIimgctx = thisthumb.birdseyeGUIimgcnv.getContext('2d');
                        thisthumb.birdseyeGUIindctx = thisthumb.birdseyeGUIindcnv.getContext('2d');
                        thisthumb.birdseyemarkupctx = thisthumb.birdseyemarkupcnv.getContext('2d');
                    }
                    thisthumb.setBirdseyeTool();
                    if (thisthumb.birdseyemarker == null) {
                        thisthumb.birdseyemarker = new birdseyemarker(thisthumb.birdseyeGUIindcnv);
                    }
                    //var birdseyemarker = function(x,y,w,h, bcanvas){
                    /*if(!thisthumb.beventsSet){
                        thisthumb.setEventlisteners();
                    }*/
                };
                
                this.getThumbnail = function () {
                    return thisthumb.thumbnailobj;
                };
                this.imageloadevent = function (ev) {
                    thisthumb.thumbloaded = true;
                    thisthumb.bfinalsize = true;
                    thisthumb.thumbnailobj.ctx.drawImage(thisthumb.thumbnailobj.image, 0,0);
                    thisthumb.draw_thumbnail();
                };
                this.thumbload = function(){
            
                    //thisthumb.draw_thumbnail();
        
                    if (this.thumbnailobj.source){
                        var oReq = new XMLHttpRequest();
                        oReq.withCredentials = bUseCredentials;
        
                        oReq.open("GET", this.thumbnailobj.source, true);
        
                        oReq.responseType = "blob";
                        var __this = this;
                        oReq.onload = function (oEvent) {
                            var url = URL.createObjectURL(oReq.response);
                                if (url) {
                                
                                    thisthumb.thumbnailobj.image.addEventListener('load', thisthumb.imageloadevent, false);
                                    thisthumb.thumbnailobj.image.src = url;
                    
                                }
                        };
                        oReq.send(null);
        
                    }
        
                    //thumbnail image loaded
                    //thisthumb.draw_thumbnail();
                    //thisthumb.thumbloaded = true;

                };

                this.setPDFdim = function (pagewidth, pageheight) {
                    thisthumb.DocRef.getfoxitThumbnail(thisthumb.pagenumber);
                    thisthumb.pdfpagewidth = pagewidth;
                    thisthumb.pdfpageheight = pageheight;
                    var wscale = thisthumb.thumbnailobj.thumbnail.width / pagewidth;
                    var hscale = thisthumb.thumbnailobj.thumbnail.height / pageheight;
                    thisthumb.pdftempthumbscale = Math.min(wscale, hscale);
                    thisthumb.setthumbnail(0, 0, thisthumb.pdftempthumbscale);
                };
                this.setthumbnail = function (dx, dy, dscale) {
                    thisthumb.dxthumb = dx;
                    thisthumb.dythumb = dy;
                    thisthumb.dscalethumb = dscale;
                    if (!thisthumb.usevector3Dxml) {
                        //thisthumb.draw_thumbnail();
                    }
                };
                this.drawcamerapos = function () {
                    thisthumb.birdseyecamera();
                };
                this.camdirection = function (v) {
                };
                this.setcamerapos = function (position, rotation, boxcenter) {
                    thisthumb.camerapos = position.clone();
                    //thisthumb.camerarot = rotation.clone();
                    thisthumb.boxcenter = boxcenter.clone();
                    //const euler = new THREE.Euler();
                    //const camerarotation = euler.setFromQuaternion(thisthumb.camerarot);
                    //const radians = camerarotation.y > 0 ? camerarotation.y : (2 * Math.PI) + camerarotation.y; 
                    //const degrees = THREE.Math.radToDeg(radians);
                    //thisthumb.camerarot.order = 'YXZ';
                    thisthumb.setbirdseyecamera(thisthumb.camerapos, rotation, thisthumb.boxcenter);
                };
                this.getCurfloorElevation = function (floorindex) {
                    if (thisthumb.DocRef.pages[thisthumb.pagenumber].floorplans != undefined) {
                        var zoffset = thisthumb.DocRef.getCurrentpage().Vector3DPageObj.getfloorheight();
                        /*if(thisthumb.DocRef.getCurrentpage().Vector3DPageObj.unit == 1){
                            //var offset = 2.5;
                            //var zoffset = (1.8 - zdiff) * modelscale;
                            var zoffset = 1.8;
                            //var raynear = 0.01;
                        }else if(thisthumb.DocRef.getCurrentpage().Vector3DPageObj.unit == 0){
                            //offset = 2500;// * modelscale;
                            //zoffset = (1800 - zdiff) * modelscale;
                            zoffset = 1800;
                            //raynear = 10;
                        }else if(thisthumb.DocRef.getCurrentpage().Vector3DPageObj.unit == 2){
                            zoffset = 5.9;
        
                        }*/
                        return thisthumb.floorplans[floorindex].Elevation + zoffset;
                    }
                    else {
                        return null;
                    }
                };
                this.checkcurfloor = function () {
                    var curfloor = 0;
                    if (thisthumb.DocRef.pages[thisthumb.pagenumber].Vector3DPageObj && thisthumb.DocRef.pages[thisthumb.pagenumber].usevector3DGLTF) {

                        if(thisthumb.usefloorcheck){
                            curfloor = thisthumb.DocRef.pages[thisthumb.pagenumber].Vector3DPageObj.checkcurfloor();
                        }else{
                            curfloor = thisthumb.floorelevation.index; 
                        }                            
                    }
                    return curfloor;
                };
                this.getfloor = function (floorplans) {
                    if (floorplans == undefined) {
                        return;
                    }
                    if (floorplans.length == 0) {
                        return;
                    }
                    thisthumb.floorplans = floorplans.slice();
                    thisthumb.floorelevation.index = thisthumb.checkcurfloor();
                };
                this.getfloorplanBsale = function (iwidth, iheight, pwidth, pheight) {
                    if (pwidth > pheight) {
                        var wscale = (iwidth) / pwidth;
                        var hscale = (iheight) / pheight;
                    }
                    else {
                        wscale = (iheight) / pwidth;
                        hscale = (iwidth) / pheight;
                    }
                    var scale = Math.max(wscale, hscale);
                    return Math.max(wscale, hscale);
                };
                this.setFloorNavScaleOffset = function (floornum, alignarray) {
                    //calculate scale and offset here.
                    thisthumb.getfloor(thisthumb.DocRef.pages[thisthumb.pagenumber].floorplans);
                    thisthumb.floorplans[floornum].alignarray = alignarray.slice();
                    var bxdist = (alignarray[0].dist / alignarray[0].pwidth) * thisthumb.floorplans[floornum].originalWidth;
                    var oxdist = (alignarray[1].dist / alignarray[1].pwidth) * thisthumb.floorplans[floornum].width;
                    var scaleratio = (alignarray[1].dist / alignarray[1].pwidth) / (alignarray[0].dist / alignarray[0].pwidth);
                    //scope.overlayMScale = (scalearray[i].dist / scalearray[i].pwidth) / scope.backgroundMScale;
                    var replaceScale = oxdist / bxdist;
                    var oxoffset = alignarray[1].offset.x * thisthumb.floorplans[floornum].RScale;
                    var oyoffset = alignarray[1].offset.y * thisthumb.floorplans[floornum].RScale;
                    thisthumb.floorplans[floornum].BScale = thisthumb.getfloorplanBsale(thisthumb.birdseyeWidth, thisthumb.birdseyeHeight, thisthumb.floorplans[floornum].originalWidth, thisthumb.floorplans[floornum].originalHeight);
                    var bxoffset = alignarray[0].offset.x * replaceScale;
                    var byoffset = alignarray[0].offset.y * replaceScale;
                    //var bxoffset = alignarray[0].offset.x * thisthumb.floorplans[floornum].BScale;
                    //var byoffset = alignarray[0].offset.y * thisthumb.floorplans[floornum].BScale;
                    //var OffsetX = -((oxoffset) - (bxoffset * scaleratio));
                    //var OffsetY = -((oyoffset) - (byoffset * scaleratio));
                    var OffsetX = -((oxoffset) - (bxoffset));
                    var OffsetY = -((oyoffset) - (byoffset));
                    thisthumb.floorplans[floornum].offsetX = OffsetX;
                    thisthumb.floorplans[floornum].offsetY = OffsetY;
                    thisthumb.floorplans[floornum].replaceScale = replaceScale;
                    //console.log(replaceScale, oxoffset, oyoffset, bxoffset, byoffset);
                    //console.log(OffsetX, OffsetY);
                    //console.log(thisthumb.floorplans[floornum].BScale);
                    //console.log(scaleratio);
                    /*oxdist / 644 = 1421 / 1567
                    oxdist =  (1421 / 1567) * 644
                    583,9974473516273
        
                    bxdist / 1000 = 1491,5 / 1567
                    bxidst = (1491,5 / 1567) * 1000
                    951,8187619655392
                    scale = oxdist / bxdist = 0.61*/
                    /*this.floorplans.push({
                        index : j,
                        SRC : fsrc,
                        Elevation : felev,
                        Scale : fscale,
                        BScale : fscale,
                        RScale : fscale,
                        image :floorplanimage,
                        canvas : floorplancnv,
                        ctx : floorplanctx,
                        replaced : false,
                        originalWidth : 0,
                        originalHeight : 0,
                        width : 0,
                        height : 0,
                        offsetX : 0,
                        offsetY : 0,
                        alignarray : []
                    });*/
                    //thisthumb.floorplans[floornum]
                };
                this.setfloorplanreplace = function (floorplanobject) {
                    thisthumb.floorplan3dobject = floorplanobject;
                };
                this.removefloorplanobject = function () {
                    thisthumb.floorplan3dobject = null;
                };
                this.setreplacefloornavCallback = function (callback, floornum) {
                    thisthumb.replacefloornavCallback = callback;
                    thisthumb.targetfloor = floornum;
                };
                this.removereplacefloornavCallback = function () {
                    thisthumb.replacefloornavCallback = null;
                    thisthumb.targetfloor = null;
                };
                this.setfloorplanCallback = function (callback, floornum, alignarray) {
                    thisthumb.floorplanCallback = callback;
                    thisthumb.targetfloor = floornum;
                    thisthumb.alignarray = alignarray;
                };
                this.removefloorplanCallback = function () {
                    thisthumb.floorplanCallback = null;
                    thisthumb.targetfloor = null;
                    thisthumb.alignarray = null;
                };
                this.set3DNavImage = function (drawtype, boverride) {
                    //var image = thisthumb.DocRef.pages[thisthumb.pagenumber].floorplansrc;
                    if (thisthumb.DocRef.pages[thisthumb.pagenumber].floorplans == undefined) {
                        return;
                    }
                    if (thisthumb.DocRef.pages[thisthumb.pagenumber].floorplans.length == 0) {
                        return;
                    }

                    if(!boverride){
                        thisthumb.getfloor(thisthumb.DocRef.pages[thisthumb.pagenumber].floorplans);
                    }

                    if(thisthumb.floorplans[thisthumb.floorelevation.index] == undefined){
                        return;
                    }

                    if (drawtype == Drawbirdseye) {
                        var imgwidth = thisthumb.birdseyeWidth;
                        var imgheight = thisthumb.birdseyeHeight;
                    }
                    else if (drawtype == Floorplanreplace) {
                        imgwidth = thisthumb.floorplans[thisthumb.floorelevation.index].width;
                        imgheight = thisthumb.floorplans[thisthumb.floorelevation.index].height;
                    }
                    //var pageheight = thisthumb.DocRef.pages[thisthumb.pagenumber].SmallImageHeight;
                    var pageheight = thisthumb.floorplans[thisthumb.floorelevation.index].height;
                    var pagewidth = thisthumb.floorplans[thisthumb.floorelevation.index].width;
                    //var pagewidth = thisthumb.DocRef.pages[thisthumb.pagenumber].SmallImageWidth;
                    if (pagewidth > pageheight) {
                        var wscale = (imgwidth) / pagewidth;
                        var hscale = (imgheight) / pageheight;
                    }
                    else {
                        wscale = (imgheight) / pagewidth;
                        hscale = (imgwidth) / pageheight;
                    }
                    var scale = Math.max(wscale, hscale);
                    thisthumb.birdseyeobj.dscalebirdseye = scale;
                    thisthumb.DocRef.pages[thisthumb.pagenumber].floorplans[thisthumb.floorelevation.index].BScale = scale;
                    if (drawtype == Drawbirdseye) {
                        if (!thisthumb.birdseyedrawn) {
                            thisthumb.birdseyeobj.birdseye.width = pagewidth * scale;
                            thisthumb.birdseyeobj.birdseye.height = pageheight * scale;
                            //thisthumb.birdseyeobj.ctx
                            thisthumb.birdseyeobj.ctx.drawImage(thisthumb.floorplans[thisthumb.floorelevation.index].canvas, 0, 0, thisthumb.birdseyeobj.birdseye.width, thisthumb.birdseyeobj.birdseye.height);
                            thisthumb.birdseyedrawn = true;
                        }
                    }
                    else if (drawtype == Floorplanreplace) {
                        thisthumb.birdseyeobj.birdseye.width = pagewidth;
                        thisthumb.birdseyeobj.birdseye.height = pageheight;
                        //thisthumb.birdseyeobj.ctx
                        thisthumb.birdseyeobj.ctx.drawImage(thisthumb.floorplans[thisthumb.floorelevation.index].canvas, 0, 0, thisthumb.birdseyeobj.birdseye.width, thisthumb.birdseyeobj.birdseye.height);
                        thisthumb.birdseyedrawn = true;
                        //drawtype == Drawbirdseye
                    }
                    thisthumb.drawGUIBirdseye(drawtype);
                };
                this.renderImage = function (drawtype) {
                    if (drawtype == Drawbirdseye) {
                        var imgwidth = thisthumb.birdseyeWidth;
                        var imgheight = thisthumb.birdseyeHeight;
                    }
                    else if (drawtype == Drawthumbnail) {
                        imgwidth = thisthumb.ThumbnailWidth;
                        imgheight = thisthumb.ThumbnailHeight;
                    }
                    else if (drawtype == Drawfloorplan) {
                        imgwidth = thisthumb.floorplanWidth;
                        imgheight = thisthumb.floorplanHeight;
                    }
                    else if (drawtype == Floorplanreplace) {
                        imgwidth = thisthumb.birdseyeWidth;
                        imgheight = thisthumb.birdseyeHeight;
                    }
                    //var pageheight = thisthumb.DocRef.pages[thisthumb.pagenumber].Mainimage.height;
                    //var pagewidth = thisthumb.DocRef.pages[thisthumb.pagenumber].Mainimage.width;
                    //var drawscale = thisthumb.DocRef.pages[thisthumb.pagenumber].dscale / thisthumb.DocRef.pages[thisthumb.pagenumber].bitmapratio;
                    var pageheight = thisthumb.DocRef.pages[thisthumb.pagenumber].MainImageHeight;
                    var pagewidth = thisthumb.DocRef.pages[thisthumb.pagenumber].MainImageWidth;
                    thisthumb.birdseyeobj.rotation = thisthumb.DocRef.pages[thisthumb.pagenumber].getpageRotation();
                    if (pagewidth > pageheight) {
                        var wscale = (imgwidth) / pagewidth;
                        var hscale = (imgheight) / pageheight;
                    }
                    else {
                        wscale = (imgheight) / pagewidth;
                        hscale = (imgwidth) / pageheight;
                    }
                    var scale = Math.max(wscale, hscale);
                    thisthumb.birdseyeobj.dscalebirdseye = scale;
                    if (drawtype == Drawbirdseye) {
                        if (!thisthumb.birdseyedrawn) {
                            thisthumb.birdseyeobj.birdseye.width = pagewidth * scale;
                            thisthumb.birdseyeobj.birdseye.height = pageheight * scale;
                            //thisthumb.birdseyeobj.ctx
                            thisthumb.birdseyeobj.ctx.drawImage(thisthumb.DocRef.pages[thisthumb.pagenumber].smallimagecnv, 0, 0, thisthumb.birdseyeobj.birdseye.width, thisthumb.birdseyeobj.birdseye.height);
                            thisthumb.birdseyedrawn = true;
                        }
                    }
                    else if (drawtype == Drawthumbnail) {
                        scale = (imgheight) / pageheight;
                        //scale = (imgwidth) / pagewidth;

                        if(!thisthumb.bserverthumbnail){
                                
                            scale = (imgwidth) / pagewidth;
                            thisthumb.thumbnailobj.thumbnail.width = pagewidth * scale;
                            thisthumb.thumbnailobj.thumbnail.height = pageheight * scale;
                            thisthumb.thumbnailobj.ctx.drawImage(thisthumb.DocRef.pages[thisthumb.pagenumber].smallimagecnv, 0, 0, thisthumb.thumbnailobj.thumbnail.width, thisthumb.thumbnailobj.thumbnail.height);

                        }


                        //thisthumb.thumbnailobj.thumbnail.width = pagewidth * scale;
                        //thisthumb.thumbnailobj.thumbnail.height = pageheight * scale;
                        thisthumb.dxthumb = 0;
                        thisthumb.dythumb = 0;
                        thisthumb.dscalethumb = scale;
                        thisthumb.bfinalsize = true;
                        thisthumb.thumbloaded = true;
                        //thisthumb.thumbnailobj.ctx.drawImage(thisthumb.DocRef.pages[thisthumb.pagenumber].smallimagecnv, 0, 0, thisthumb.thumbnailobj.thumbnail.width, thisthumb.thumbnailobj.thumbnail.height);
                        thisthumb.draw_thumbnail();
                    }
                    else if (drawtype == Drawfloorplan) {
                        thisthumb.floorplanobj.floorplan.width = pagewidth * scale;
                        thisthumb.floorplanobj.floorplan.height = pageheight * scale;
                        //thisthumb.birdseyeobj.ctx
                        thisthumb.floorplanobj.ctx.drawImage(thisthumb.DocRef.pages[thisthumb.pagenumber].smallimagecnv, 0, 0, thisthumb.floorplanobj.floorplan.width, thisthumb.floorplanobj.floorplan.height);
                        thisthumb.floorplandrawn = true;
                    }
                    else if (drawtype == Floorplanreplace) {
                        if (!thisthumb.birdseyedrawn) {
                            thisthumb.birdseyeobj.birdseye.width = pagewidth * scale;
                            thisthumb.birdseyeobj.birdseye.height = pageheight * scale;
                            //thisthumb.birdseyeobj.ctx
                            thisthumb.birdseyeobj.ctx.drawImage(thisthumb.DocRef.pages[thisthumb.pagenumber].smallimagecnv, 0, 0, thisthumb.birdseyeobj.birdseye.width, thisthumb.birdseyeobj.birdseye.height);
                            thisthumb.birdseyedrawn = true;
                            thisthumb.floorplanreplaceScale = scale;
                        }
                        /*if(thisthumb.floorplan3dobject){
        
                            thisthumb.floorplan3dobject.width = pagewidth * scale;
                            thisthumb.floorplan3dobject.height = pageheight * scale;
        
                            thisthumb.floorplan3dobject.canvas.width = pagewidth * scale;
                            thisthumb.floorplan3dobject.canvas.height = pageheight * scale;
        
                            //thisthumb.birdseyeobj.ctx
                            thisthumb.floorplan3dobject.ctx.drawImage(thisthumb.DocRef.pages[thisthumb.pagenumber].smallimagecnv, 0, 0, thisthumb.floorplan3dobject.width, thisthumb.floorplan3dobject.height);
                        }*/
                    }
                    thisthumb.drawGUIBirdseye(drawtype);
                };
                this.refreshbirdseye = function () {
                    thisthumb.birdseyedrawn = false;
                };
                this.renderVector = function (drawtype) {
                    if (thisthumb.DocRef.pages[thisthumb.pagenumber] == undefined) {
                        return;
                    }
                    switch (drawtype) {
                        case 1:
                            var imgwidth = thisthumb.ThumbnailWidth;
                            var imgheight = thisthumb.ThumbnailHeight;
                            break;
                        case 2:
                            imgwidth = thisthumb.birdseyeWidth;
                            imgheight = thisthumb.birdseyeHeight;
                            break;
                        case 3:
                            imgwidth = thisthumb.floorplanWidth;
                            imgheight = thisthumb.floorplanHeight;
                            break;
                        case 4:
                            imgwidth = thisthumb.birdseyeWidth;
                            imgheight = thisthumb.birdseyeHeight;
                            break;
                    }


                    if(thisthumb.DocRef.pages[thisthumb.pagenumber].VectorPageObj){
                        var pageheight = thisthumb.DocRef.pages[thisthumb.pagenumber].VectorPageObj.height;
                        var pagewidth = thisthumb.DocRef.pages[thisthumb.pagenumber].VectorPageObj.width;
                        thisthumb.birdseyeobj.rotation = thisthumb.DocRef.pages[thisthumb.pagenumber].getpageRotation();

                    }else{
                        return;
                    }

                    if (pagewidth > pageheight) {
                        var wscale = (imgwidth) / pagewidth;
                        var hscale = (imgheight) / pageheight;
                    }
                    else {
                        wscale = (imgheight) / pagewidth;
                        hscale = (imgwidth) / pageheight;
                    }
                    var scale = Math.max(wscale, hscale);
                    thisthumb.birdseyeobj.dscalebirdseye = scale;
                    if (drawtype == Drawbirdseye) {
                        if (!thisthumb.birdseyedrawn) {
                            //rotate ctx if page is rotated
                            thisthumb.birdseyeobj.birdseye.width = pagewidth * scale;
                            thisthumb.birdseyeobj.birdseye.height = pageheight * scale;
                            //thisthumb.birdseyeobj.ctx
                            thisthumb.DocRef.pages[thisthumb.pagenumber].VectorPageObj.drawDirect(thisthumb.birdseyeobj.ctx, scale, 0, 0, false, false);
                            thisthumb.birdseyedrawn = true;
                        }
                    }
                    else if (drawtype == Drawthumbnail) {

                        scale = (imgheight) / pageheight;

                        if(!thisthumb.bserverthumbnail){
                            scale = (imgwidth) / pagewidth;

                            thisthumb.thumbnailobj.thumbnail.width = pagewidth * scale;
                            thisthumb.thumbnailobj.thumbnail.height = pageheight * scale;

                            if(thisthumb.thumbnailobj.rotation != 0){
                                var tx = (thisthumb.thumbnailobj.thumbnail.width / 2);
                                var ty = (thisthumb.thumbnailobj.thumbnail.height / 2);
                                thisthumb.thumbnailobj.ctx.save();
                                thisthumb.thumbnailobj.ctx.translate(tx, ty);
                                thisthumb.thumbnailobj.ctx.rotate(thisthumb.thumbnailobj.rotation * (Math.PI / 180));
                                thisthumb.thumbnailobj.ctx.translate(-tx, -ty);
        
                            }

                            thisthumb.DocRef.pages[thisthumb.pagenumber].VectorPageObj.drawDirect(thisthumb.thumbnailobj.ctx, scale, 0, 0, false, false);

                            if(thisthumb.thumbnailobj.rotation != 0){
                                thisthumb.thumbnailobj.ctx.restore();
                            }
                            
                        }

                        thisthumb.dxthumb = 0;
                        thisthumb.dythumb = 0;
                        thisthumb.dscalethumb = scale;
                        thisthumb.bfinalsize = true;
                        thisthumb.thumbloaded = true;
                        thisthumb.draw_thumbnail();
                    }
                    else if (drawtype == Drawfloorplan) {
                        thisthumb.floorplanobj.floorplan.width = pagewidth * scale;
                        thisthumb.floorplanobj.floorplan.height = pageheight * scale;
                        //thisthumb.birdseyeobj.ctx
                        thisthumb.DocRef.pages[thisthumb.pagenumber].VectorPageObj.drawDirect(thisthumb.floorplanobj.ctx, scale, 0, 0, false, false);
                        drawFloorplanMarkupAll(thisthumb.floorplanobj.ctx, scale, 0,0);
                        thisthumb.floorplandrawn = true;
                    }
                    else if (drawtype == Floorplanreplace) {
                        //get page object floorplanarray and draw 
                        //pageobject.draw 
                        if (!thisthumb.birdseyedrawn) {
                            //rotate ctx if page is rotated
                            thisthumb.birdseyeobj.birdseye.width = pagewidth * scale;
                            thisthumb.birdseyeobj.birdseye.height = pageheight * scale;
                            //thisthumb.birdseyeobj.ctx
                            thisthumb.DocRef.pages[thisthumb.pagenumber].VectorPageObj.drawDirect(thisthumb.birdseyeobj.ctx, scale, 0, 0, false, false);
                            thisthumb.birdseyedrawn = true;
                            thisthumb.floorplanreplaceScale = scale;
                        }
                        /*if(thisthumb.floorplan3dobject){
        
                            thisthumb.floorplan3dobject.width = imagewidth;
                            thisthumb.floorplan3dobject.height = imageheight;
        
                            thisthumb.floorplan3dobject.canvas.width = imagewidth;
                            thisthumb.floorplan3dobject.canvas.height = imageheight;
                            
                            //thisthumb.birdseyeobj.ctx
                            thisthumb.DocRef.pages[thisthumb.pagenumber].VectorPageObj.drawDirect(thisthumb.floorplan3dobject.ctx, scale, 0, 0, false, false);
        
                        }*/
                    }
                    thisthumb.drawGUIBirdseye(drawtype);
                    //contexto, this.dscalevector, this.dxvector, this.dyvector, refresh,false
                    //thisthumb.DocRef.pages[thisthumb.pagenumber].VectorPageObj.drawallnew(contexto, this.dscalevector, this.dxvector, this.dyvector, refresh,false);
                    //this.VectorPageObj.drawallnew(contexto, this.dscalevector, this.dxvector, this.dyvector, refresh,false);
                };
                this.drawGUIBirdseye = function (drawtype) {
                    if (drawtype == Drawbirdseye) {
                        if (RxCore_GUI_birdseye != undefined) {
                            RxCore_GUI_birdseye.setThumbnail(thisthumb.pagenumber, thisthumb);
                        }
                    }
                    else if (drawtype == Drawthumbnail) {
                        if (RxCore_GUI_pagethumb != undefined) {
                            RxCore_GUI_pagethumb.setThumbnail(thisthumb.pagenumber, thisthumb);
                        }
                        thisthumb.DocRef.refreshThumbnails();
                    }
                    else if (drawtype == Drawfloorplan) {
                        if (thisthumb.floorplanCallback && thisthumb.targetfloor != null) {
                            thisthumb.floorplanCallback(thisthumb.targetfloor, thisthumb.floorplanobj.floorplan, thisthumb.alignarray);
                        }
                    }
                    else if (drawtype == Floorplanreplace) {
                        //pass on birdseye canvas to 3D page object here.
                        if (thisthumb.replacefloornavCallback && thisthumb.targetfloor != null) {
                            thisthumb.replacefloornavCallback(thisthumb.targetfloor, thisthumb);
                        }
                    }
                    /*this.birdseyeobj = {
                            created : false,
                            birdseye : document.createElement('canvas'),
                            ctx : null,
                            number :pagenum
                        };*/
                    /*if(!thisthumb.birdseyedrawn){
                        //thisthumb.birdseyeobj.ctx
                        thisthumb.birdseyeGUIimgctx.drawImage(thisthumb.birdseyeobj.birdseye, 0,0);
                        thisthumb.birdseyedrawn = true;
                        
                    }*/
                    /*if (thisthumb.birdseyeGUIimgctx != null){
        
                    
                        thisthumb.birdseyeGUIimgctx.drawImage(thisthumb.birdseyeobj.birdseye, 0,0);
                        thisthumb.birdseyedrawn = true;
                    
        
                    }*/
                };
                this.getBirdseyeDim = function (pw, ph) {
                    var imgwidth = thisthumb.birdseyeWidth;
                    var imgheight = thisthumb.birdseyeHeight;
                    if (pw > ph) {
                        var wscale = (imgwidth) / pw;
                        var hscale = (imgheight) / ph;
                    }
                    else {
                        wscale = (imgheight) / pw;
                        hscale = (imgwidth) / ph;
                    }
                    var scale = Math.max(wscale, hscale);
                    return { width: pw * scale, height: ph * scale, birdscale: scale };
                    //thisthumb.birdseyeobj.birdseye.width = pagewidth * scale;
                    //thisthumb.birdseyeobj.birdseye.height = pageheight * scale;
                };
                this.foxitcreateBirdseye = function (thumbnail) {
                    
                    
                    
                    if (!thisthumb.birdseyedrawn) {
                        
                        thisthumb.birdseyeobj.rotation = thisthumb.DocRef.pages[thisthumb.pagenumber].getpageRotation();    
                        
                        thisthumb.birdseyeobj.birdseye.width = thumbnail.width;
                        thisthumb.birdseyeobj.birdseye.height = thumbnail.height;

                        /*if(thisthumb.birdseyeobj.rotation == 90 || thisthumb.birdseyeobj.rotation == 270){
                            thisthumb.birdseyeobj.birdseye.width = thumbnail.height;
                            thisthumb.birdseyeobj.birdseye.height = thumbnail.width;
                        }*/
                        


                        thisthumb.birdseyeobj.ignorerotation = true;
                        //var imgData = thumbfctx.createImageData(thumbnail.width,thumbnail.height);
                        var tdv = new Uint8ClampedArray(thumbnail.buffer);
                        //var imgData = new ImageData(tdv, thumbnail.width,thumbnail.height);

                        const icanvas = document.createElement('canvas');
                        const ictx = icanvas.getContext('2d');
                
                        const cimageData = ictx.createImageData(thumbnail.width,thumbnail.height);
                        
                        if(tdv){
                            cimageData.data.set(tdv);
                            thisthumb.birdseyeobj.imageData = cimageData;
                        } 
                        //thisthumb.birdseyeobj.imageData = new ImageData(tdv, thumbnail.width, thumbnail.height);
                        /*this.birdseyeobj = {
                            created : false,
                            birdseye : document.createElement('canvas'),
                            imageData : null,
                            ctx : null,
                            rotation : 0,
                            dscalebirdseye : 1.0,
                            number :pagenum
                        };*/
                        //var dv = new DataView(thumbnail.buffer);

                        var wscale = thisthumb.birdseyeobj.birdseye.width / thisthumb.pdfpagewidth;
                        var hscale = thisthumb.birdseyeobj.birdseye.height / thisthumb.pdfpageheight;

                        /*if(thisthumb.birdseyeobj.rotation == 90 || thisthumb.birdseyeobj.rotation == 270){
                            wscale = thisthumb.birdseyeobj.birdseye.width / thisthumb.pdfpageheight;
                            hscale = thisthumb.birdseyeobj.birdseye.height / thisthumb.pdfpagewidth;
                        }*/

                        //thisthumb.pdftempthumbscale = Math.min(wscale,hscale);
                        thisthumb.birdseyeobj.dscalebirdseye = Math.min(wscale, hscale);
                        thisthumb.birdseyeobj.ctx.clearRect(0, 0, thisthumb.birdseyeobj.birdseye.width, thisthumb.birdseyeobj.birdseye.height);
                        //thisthumb.birdseyeobj.ctx.fillStyle = "rgb(255,255,255)";
                        //thisthumb.birdseyeobj.ctx.fillRect(0, 0, thisthumb.birdseyeobj.birdseye.width, thisthumb.birdseyeobj.birdseye.height);
                        thisthumb.birdseyeobj.ctx.putImageData(thisthumb.birdseyeobj.imageData, 0, 0, 0, 0, thisthumb.birdseyeobj.birdseye.width, thisthumb.birdseyeobj.birdseye.height);
                        thisthumb.birdseyedrawn = true;
                    }

                    this.drawGUIBirdseye(Drawbirdseye);
                };
                this.foxitcreatethumb = function (thumbnail) {
                    var thumbcnv = document.createElement('canvas');
                    thumbcnv.width = parseInt(thumbnail.width);
                    thumbcnv.height = parseInt(thumbnail.height);
                    var thumbfctx = thumbcnv.getContext('2d');
                    thisthumb.thumbnailobj.thumbnail.width = thumbnail.width;
                    thisthumb.thumbnailobj.thumbnail.height = thumbnail.height;
                    thisthumb.thumbcanvas.width = thumbnail.width;
                    thisthumb.thumbcanvas.height = thumbnail.height;
                    //var imgData = thumbfctx.createImageData(thumbnail.width,thumbnail.height);
                    var tdv = new Uint8ClampedArray(thumbnail.buffer);


                    const icanvas = document.createElement('canvas');
                    const ictx = icanvas.getContext('2d');
                
                    const cimageData = ictx.createImageData(thumbnail.width,thumbnail.height);
                
                    if(tdv){
                        cimageData.data.set(tdv);
                        thisthumb.thumbnailobj.imageData = cimageData;
                    } 

                    //var imgData = new ImageData(tdv, thumbnail.width,thumbnail.height);
                    //thisthumb.thumbnailobj.imageData = new ImageData(tdv, thumbnail.width, thumbnail.height);
                    //var dv = new DataView(thumbnail.buffer);
                    /*for(var i = 0; i < thumbnail.buffer.byteLength; i++) {
                        imgData.data[4*i] = thumbnail.buffer[i];// red
        
                        dv.getUint8(i)
                        imgData.data[4*i + 1] = thumbnail.buffer[i]; // green
                        imgData.data[4*i + 2] = thumbnail.buffer[i];// blue
                        imgData.data[4*i + 3] = 255; //alpha
                    }*/
                    //this.thumbnailobj.thumbnail.width = parseInt(this.ThumbnailWidth);
                    //this.thumbnailobj.thumbnail.height = parseInt(this.ThumbnailHeight);
                    //this.thumbctx = this.thumbnailobj.thumbnail.getContext('2d');
                    //thisthumb.thumbctx.putImageData(thisthumb.thumbnailobj.imageData, 0, 0, 0, 0,thumbnail.width,thumbnail.height);
                    thisthumb.usefoxitthumb = true;
                    thisthumb.thumbloaded = true;
                    //thumbfctx.putImageData(imgData, 0, 0, 0, 0,thumbnail.width,thumbnail.height);
                    //thisthumb.thumbnailobj.image.src = thumbcnv.toDataURL();
                    thisthumb.thumbnailobj.created = true;
                    var wscale = thisthumb.thumbnailobj.thumbnail.width / thisthumb.pdfpagewidth;
                    var hscale = thisthumb.thumbnailobj.thumbnail.height / thisthumb.pdfpageheight;
                    thisthumb.pdftempthumbscale = Math.min(wscale, hscale);
                    thisthumb.setthumbnail(0, 0, thisthumb.pdftempthumbscale);
                    thisthumb.bfinalsize = true;
                    //thisthumb.thumbloaded = true;
                    //this.thumbloaded
                    thisthumb.draw_thumbnail();
                    //thisthumb.DocRef.thumbnails[thisthumb.pagenumber] = thisthumb.thumbnailobj;
                    if (RxCore_GUI_pagethumb != undefined) {
                        //RxCore_GUI_pagethumb.setThumbnail(thisthumb.pagenumber, thisthumb.thumbnailobj);
                        RxCore_GUI_pagethumb.setThumbnail(thisthumb.pagenumber, thisthumb);
                    }
                };
                this.clearBirdseyeMarkupctx = function () {
                    if (thisthumb.birdseyemarkupctx != null) {
                        thisthumb.birdseyemarkupctx.clearRect(0, 0, thisthumb.birdseyeobj.birdseye.width, thisthumb.birdseyeobj.birdseye.height);
                    }
                };
                this.draw_thumbnail = function () {
                    //thispage.thumbctx = this.thumbcanvas.getContext('2d');
                    /*
                    draw to thumbnail once with the appropriate method.
                    */
                    if (thisthumb.thumbloaded) {
                        if (thisthumb.usefoxitthumb && thisthumb.thumbnailobj.imageData != null) {
                            thisthumb.thumbctx.clearRect(0, 0, thisthumb.thumbnailobj.thumbnail.width, thisthumb.thumbnailobj.thumbnail.height);
                            thisthumb.thumbctx.putImageData(thisthumb.thumbnailobj.imageData, 0, 0, 0, 0, thisthumb.thumbnailobj.thumbnail.width, thisthumb.thumbnailobj.thumbnail.height);
                        }
                        else {
                            if (!thisthumb.thumbnailobj.image.complete) {
                                //thisthumb.thumbload();
                                return;
                            }

                            if(thisthumb.thumbnailobj.thumbnail.width > 0 &&  thisthumb.thumbnailobj.thumbnail.height > 0){

                                var offsetx = 0;
                                var offsety = 0;


                                if(thisthumb.thumbnailobj.rotation != 0){
                                    var tx = (thisthumb.thumbnailobj.thumbnail.width / 2);
                                    var ty = (thisthumb.thumbnailobj.thumbnail.height / 2);


                                    if(thisthumb.thumbnailobj.rotation == 90){
                                        offsetx = tx - ty;
                                        offsety = tx - ty;
                                    }else if (thisthumb.thumbnailobj.rotation == 270){
                                        offsetx = ty - tx;
                                        offsety = ty - tx;
                                    }
                                            
                                    thisthumb.thumbctx.save();
                                    thisthumb.thumbctx.translate(tx, ty);
                                    thisthumb.thumbctx.rotate(thisthumb.thumbnailobj.rotation * (Math.PI / 180));
                                    thisthumb.thumbctx.translate(-tx, -ty);

            
                                }
    

                                thisthumb.thumbctx.clearRect(0, 0, thisthumb.thumbnailobj.thumbnail.width, thisthumb.thumbnailobj.thumbnail.height);
                                thisthumb.thumbctx.drawImage(thisthumb.thumbnailobj.thumbnail, offsetx, offsety);

                                if(thisthumb.thumbnailobj.rotation != 0){
                                    thisthumb.thumbctx.restore();
                                }

 
                            }
                            
                        }
                    }
                    //thumbnailobj
                    //thisdocument.thumbnails.push(PageObject.getThumbnail());
                };
                if (this.bautoloadthumbnails) {
                    thisthumb.thumbload();
                }
                //return this.thumbnailobj;
            }
        }

        function sharpencanvastiles(callback, tileArray, transScale){

            setTimeout(function() {

                for (var i= 0;i < tileArray.length;i++){
                    
                    for(var j = 0; j < tileArray[i].length; j++){
                    
                        
                            if (transScale != 1 && !tileArray[i][j].sharpend){
                                //tileArray[i][j].ctx.;

                                var th = tileArray[i][j].cnv.height;
                                var tw = tileArray[i][j].cnv.width;

                                sharpen(tileArray[i][j].ctx, tw, th, 0.5);
                                tileArray[i][j].sharpend = true;

                                //page.bgCanvtileArray[i][j].ctx.drawImage(foxitpage.canvases[page.bgCanvtileArray[i][j].tilenumber], 0,0);
                            }   
                        
                    }
                }

                callback();

            }, 0);
        }

        function foxitcreateAnnotArray(callback, foxitpage, page){
            
            page.annotArray = [];

            setTimeout(function() {
                
                if(foxitpage.annots){
                    if (foxitpage.annots.length > 0){
                        for (var i = 0;i < foxitpage.annots.length;i++){
                            
                            page.annotArray.push({
                                cnv : document.createElement('canvas'),
                                pos : foxitpage.annots[i].pos 
                                //annotobj
                            });

                            
                            

                            page.annotArray[i].cnv.width = foxitpage.annots[i].tiles[0].width;
                            page.annotArray[i].cnv.height = foxitpage.annots[i].tiles[0].height;

                            //grab the context from your destination canvas
                            //call its drawImage() function passing it the source canvas directly
                            var tctx = page.annotArray[i].cnv.getContext('2d');
                            tctx.drawImage(foxitpage.annots[i].tiles[0], 0, 0);

                            /*var annotobj = {
                                cnv : tcanvas,
                                //ctx : tctx,
                                pos : foxitpage.annots[i].pos 
                            }*/
                            
                        }
                    }
                }

                callback();

            }, 0);
        }

        function foxitCalculatePageSize(callback, foxitpage, tileArray){
            var totalwidth = 0;
            var totalheight = 0;
            var currentheight = 0;
            var currentwidth = 0;

            var bskip = false;

            setTimeout(function() {
                
                if(foxitpage.canvases){
                    
                    if (foxitpage.canvases.length > 0){
                        var canvcount = 0;
                        
                        /*for(var hc = 0;hc < foxitpage.hidden.length;hc++){
                            if(canvcount == foxitpage.hidden[hc].ct){
                                bskip = true;
                                canvcount++;
                            }
                        }*/

                        for (var i = 0;i < tileArray.length;i++){

                            if(foxitpage.canvases[canvcount].tagName.toLowerCase() == "canvas"){
                            
                                if(i < tileArray.length - 1){
                                    totalheight += foxitpage.tileSize;
                                    currentheight = foxitpage.tileSize;    
                                }else{
                                    totalheight += foxitpage.canvases[canvcount].height;
                                    currentheight = foxitpage.canvases[canvcount].height;
                                }
                    
                                totalwidth = 0;
                            }


                            for(var j = 0;j < tileArray[i].length;j++){
                                if(foxitpage.canvases[canvcount]){
                                    if(foxitpage.canvases[canvcount].tagName.toLowerCase() == "canvas"){
                                        if(j < tileArray[i].length - 1){
                                            totalwidth += foxitpage.tileSize;
                                            currentwidth = foxitpage.tileSize;    
                                        }else{
                                            totalwidth += foxitpage.canvases[canvcount].width;
                                            currentwidth = foxitpage.canvases[canvcount].width;
                                        }
            
                                        if (currentheight < foxitpage.canvases[canvcount].height){
                                            totalheight += foxitpage.canvases[canvcount].height - currentheight;
                                        }

                                        if (foxitpage.transScale == 1){
                                            tileArray[i][j].cnv.height = foxitpage.canvases[canvcount].height;
                                            tileArray[i][j].cnv.width = foxitpage.canvases[canvcount].width;
                                            tileArray[i][j].tilenumber = canvcount;
                                            
                                            tileArray[i][j].xoffset = foxitpage.tileSize;
                                            tileArray[i][j].yoffset = foxitpage.tileSize;
                                            
        
                                        }else{
                                            tileArray[i][j].cnv.height = Math.round(foxitpage.canvases[canvcount].height * foxitpage.transScale); 
                                            tileArray[i][j].cnv.width = Math.round(foxitpage.canvases[canvcount].width * foxitpage.transScale);
                                            tileArray[i][j].tilenumber = canvcount;

                                            tileArray[i][j].xoffset = foxitpage.tileSize * foxitpage.transScale;
                                            tileArray[i][j].yoffset = foxitpage.tileSize  * foxitpage.transScale;
                                
                                        }
                                        
                                        canvcount ++;
                                        /*for(var hc = 0;hc < foxitpage.hidden.length;hc++){
                                            if(canvcount == foxitpage.hidden[hc].ct){
                                                bskip = true;
                                                canvcount++;
                                            }
                                        }*/
                                        
                                    }
                                }
                            }
                        }
                    }
                }

                totalwidth *= foxitpage.transScale;
                totalheight *= foxitpage.transScale;


                callback({width : totalwidth, height : totalheight}, foxitpage);

            }, 0);
        }

        function foxitcreatecanvasArray(callback, foxitpage, tileArray){
            
            var totalwidth = 0;
            var totalheight = 0;
            var currentheight = 0;
            var currentwidth = 0;

            var bskip = false;

            setTimeout(function() {
                
                if(foxitpage.canvases){
                    
                    if (foxitpage.canvases.length > 0){
                        var canvcount = 0;
                        
                        /*for(var hc = 0;hc < foxitpage.hidden.length;hc++){
                            if(canvcount == foxitpage.hidden[hc].ct){
                                bskip = true;
                                canvcount++;
                            }
                        }*/

                        for (var i = 0;i < tileArray.length;i++){

                            if(foxitpage.canvases[canvcount].tagName.toLowerCase() == "canvas"){
                            
                                if(i < tileArray.length - 1){
                                    totalheight += foxitpage.tileSize;
                                    currentheight = foxitpage.tileSize;    
                                }else{
                                    totalheight += foxitpage.canvases[canvcount].height;
                                    currentheight = foxitpage.canvases[canvcount].height;
                                }
                    
                                totalwidth = 0;
                            }


                            for(var j = 0;j < tileArray[i].length;j++){
                                if(foxitpage.canvases[canvcount]){
                                    if(foxitpage.canvases[canvcount].tagName.toLowerCase() == "canvas"){
                                        if(j < tileArray[i].length - 1){
                                            totalwidth += foxitpage.tileSize;
                                            currentwidth = foxitpage.tileSize;    
                                        }else{
                                            totalwidth += foxitpage.canvases[canvcount].width;
                                            currentwidth = foxitpage.canvases[canvcount].width;
                                        }
            
                                        if (currentheight < foxitpage.canvases[canvcount].height){
                                            totalheight += foxitpage.canvases[canvcount].height - currentheight;
                                        }

                                        if (foxitpage.transScale == 1){
                                            tileArray[i][j].cnv.height = foxitpage.canvases[canvcount].height;
                                            tileArray[i][j].cnv.width = foxitpage.canvases[canvcount].width;
                                            tileArray[i][j].tilenumber = canvcount;
                                            
                                            tileArray[i][j].xoffset = foxitpage.tileSize;
                                            tileArray[i][j].yoffset = foxitpage.tileSize;

                                            
        
                                        }else{
                                            tileArray[i][j].cnv.height = Math.round(foxitpage.canvases[canvcount].height * foxitpage.transScale); 
                                            tileArray[i][j].cnv.width = Math.round(foxitpage.canvases[canvcount].width * foxitpage.transScale);
                                            tileArray[i][j].tilenumber = canvcount;

                                            tileArray[i][j].xoffset = foxitpage.tileSize * foxitpage.transScale;
                                            tileArray[i][j].yoffset = foxitpage.tileSize  * foxitpage.transScale;
                                
                                        }
                                        
                                        canvcount ++;
                                        /*for(var hc = 0;hc < foxitpage.hidden.length;hc++){
                                            if(canvcount == foxitpage.hidden[hc].ct){
                                                bskip = true;
                                                canvcount++;
                                            }
                                        }*/
                                        
                                    }
                                }
                            }
                        }
                    }
                }

                totalwidth *= foxitpage.transScale;
                totalheight *= foxitpage.transScale;


                callback({width : totalwidth, height : totalheight}, foxitpage);

            }, 0);        


        }

        function setCanvasTiles(callback, tileArray,foxitpage, visibletiles){
        

            if(visibletiles){
                if (visibletiles.length > 0){


                    return;
                }

            }
            setTimeout(function() {
                
                //thispage.visibleTiles

                var rows = foxitpage.rows;
                var columns = foxitpage.columns;

                for (var rc = 0;rc < rows; rc++){
                    
                    tileArray.push([]);

                    for (var cc = 0;cc < columns; cc++){
                        
                        tileArray[rc].push({
                            cnv : document.createElement('canvas'),
                            ctx : null,
                            tilenumber : 0,
                            visible : false,
                            xoffset : 0,
                            yoffset : 0,
                            sharpend : false  
                        });
                        
                        tileArray[rc][cc].ctx = tileArray[rc][cc].cnv.getContext('2d');

                    }

                }

                callback(tileArray, foxitpage);

            }, 10);
            
        }

        function drawTiles(callback, drawntiles, foxitpage, page){
            
            function drawtilesother(readstate){
                
                if(!readstate.bcanvasvalid ||  !readstate.bmulticanvas){
                    return readstate;
                }

                for(var cdt = 0; cdt < drawntiles.length; cdt++){
                    if(readstate.tilerow == drawntiles[cdt].r && readstate.tilecolumn == drawntiles[cdt].c){
                        bdraw = false;
                    }
                }

                if (readstate.bdraw){
                    if (foxitpage.transScale == 1){
                        readstate.canvtileArray[readstate.tilerow][readstate.tilecolumn].ctx.drawImage(foxitpage.canvases[readstate.canvtileArray[readstate.tilerow][readstate.tilecolumn].tilenumber], 0,0);
        
                    }else{
                        var scaledcnv = downScaleCanvas(foxitpage.canvases[readstate.canvtileArray[readstate.tilerow][readstate.tilecolumn].tilenumber], readstate.transScale);
                        readstate.canvtileArray[readstate.tilerow][readstate.tilecolumn].ctx.drawImage(scaledcnv,0,0);
                        scaledcnv = null;
                        
                    }
        
                }

                return readstate;

        }
            
            function drawTiles(){

                var bcanvasvalid = false;    
                var bmulticanvas = false;
        
                if (foxitpage.canvases){
                    bcanvasvalid = true;    
                    if (foxitpage.canvases.length > 0){
                        bmulticanvas = true;
                    }
                }
        
                var readstate = {
                    tilerow : 0,
                    tilecolumn : 0,
                    bcanvasvalid : bcanvasvalid,
                    bmulticanvas : bmulticanvas,
                    bdraw : true,
                    scaleupdate : foxitpage.scaleupdate,
                    page : page,
                    drawntiles : drawntiles,
                    nondrawtilecount : 0,
                    transScale  : foxitpage.transScale,
                    canvtileArray : page.canvtileArray
                };

                if (foxitpage.scrollupdate){
                    readstate.canvtileArray = page.bgCanvtileArray;
                }

                if (foxitpage.scaleupdate){
                    readstate.canvtileArray = page.canvtileArray;
                }
                

                yieldingLoop(readstate, 1, function() {
                    
                    readstate = drawtilesother(readstate);
                    readstate.bdraw = true;

                    if(readstate.tilerow < readstate.canvtileArray.length){
                        if(readstate.tilecolumn < readstate.canvtileArray[readstate.tilerow].length){
                            readstate.tilecolumn ++;
                            
                        }else{
                            readstate.tilecolumn = 0;
                            readstate.tilerow ++;
                        }
                    }
                    
                    

                }, function() {
                    callback(foxitpage);
                });

                (function timer() {


                    setTimeout(timer, 10);

                    
                })();


            }   

            drawTiles();

            function yieldingLoop(state,chunksize, ycallback, finished) {
                var i = 0;
                (function chunk() {
                    //var end = Math.min(i + chunksize, count);
                    //var end = (i < chunksize );

                    /*while((state.tilerow < state.canvtileArray.length && state.tilecolumn < state.canvtileArray[state.tilerow].length)) {
                        
                    }*/
                    for (var i = 0; i < chunksize; ++i) {
                        ycallback.call(null, state);
                    }

                    if (state.tilerow < state.canvtileArray.length && state.tilecolumn < state.canvtileArray[state.tilerow].length && state.bcanvasvalid && state.bmulticanvas) {
                        setTimeout(chunk, 0);
                    } else {
                        finished.call(null);
                    }
                })();
            }

        }
    
        function drawpriorityTiles(callback, foxitpage, page, imagedim) {

            function drawtiles(readstate){
                
                    if(!readstate.bcanvasvalid ||  !readstate.bmulticanvas){
                        return readstate;
                    }

                    var offscreenwidth = readstate.canvtileArray[readstate.tilerow][readstate.tilecolumn].cnv.width;
                    var offscreenheight = readstate.canvtileArray[readstate.tilerow][readstate.tilecolumn].cnv.height;

                    if((readstate.ulx < canvasowidth && readstate.uly < canvasoheight) && ((readstate.ulx + (offscreenwidth * page.dscalepdf)) > 0 && (readstate.uly + (offscreenheight * page.dscalepdf)) > 0)){
                                    
                        if (readstate.transScale == 1){

                            readstate.canvtileArray[i][j].ctx.drawImage(foxitpage.canvases[readstate.canvtileArray[readstate.tilerow][readstate.tilecolumn].tilenumber], 0,0);

                        }else{
                            
                            var scaledcnv = downScaleCanvas(foxitpage.canvases[readstate.canvtileArray[readstate.tilerow][readstate.tilecolumn].tilenumber], readstate.transScale);

                            readstate.canvtileArray[readstate.tilerow][readstate.tilecolumn].ctx.drawImage(scaledcnv,0,0);
                            scaledcnv = null;

                        }
                        readstate.drawntiles.push({r : readstate.tilerow, c : readstate.tilecolumn});

                    }else{
                        readstate.nondrawtilecount ++;
                    }
                    readstate.ulx += (offscreenwidth * page.dscalepdf) - 1;

                    return readstate;

            }


            function drawpTiles(){

                var bcanvasvalid = false;    
                var bmulticanvas = false;
        
                if (foxitpage.canvases){
                    bcanvasvalid = true;    
                    if (foxitpage.canvases.length > 0){
                        bmulticanvas = true;
                    }
                }
                
                var readstate = {
                    tilerow : 0,
                    tilecolumn : 0,
                    ulx : page.dxpdf,
                    uly : page.dypdf,
                    bcanvasvalid : bcanvasvalid,
                    bmulticanvas : bmulticanvas,
                    scaleupdate : foxitpage.scaleupdate,
                    page : page,
                    drawntiles : [],
                    nondrawtilecount : 0,
                    transScale  : foxitpage.transScale,
                    canvtileArray : page.canvtileArray
                };

                if (foxitpage.scrollupdate){
                    readstate.canvtileArray = page.bgCanvtileArray;
                }

                if (foxitpage.scaleupdate){
                    readstate.canvtileArray = page.canvtileArray;
                }
                
                

                yieldingLoop(readstate, 1, function() {
                    
                    readstate = drawtiles(readstate);

                    if(readstate.tilerow < readstate.canvtileArray.length){
                        if(readstate.tilecolumn < readstate.canvtileArray[readstate.tilerow].length){
                            readstate.tilecolumn ++;
                            
                        }else{
                            readstate.tilecolumn = 0;
                            readstate.tilerow ++;
                        }
                    }
                    
                }, function() {
                    callback({dtiles : readstate.drawntiles, ndtilesnum : readstate.nondrawtilecount}, foxitpage, imagedim);
                });

                (function timer() {


                    setTimeout(timer, 10);

                    
                })();


            }   

            drawpTiles();

            function yieldingLoop(state,chunksize, ycallback, finished) {
                var i = 0;
                (function chunk() {
                    //var end = Math.min(i + chunksize, count);
                    //var end = (i < chunksize );

                    /*while((state.tilerow < state.canvtileArray.length && state.tilecolumn < state.canvtileArray[state.tilerow].length)) {
                        
                    }*/
                    for (var i = 0; i < chunksize; ++i) {
                        ycallback.call(null, state);
                    }

                    if (state.tilerow < state.canvtileArray.length && state.tilecolumn < state.canvtileArray[state.tilerow].length && state.bcanvasvalid && state.bmulticanvas) {
                        setTimeout(chunk, 0);
                    } else {
                        finished.call(null);
                    }
                })();
            }
        

        }

        class printpdfpageobject {
            constructor(pagenum, paperobj) {            
                var thispage = this;
                this.pagenum = pagenum;
                this.pagenumber = pagenum;
                this.pageRendering = true;
                this.pagePrinting = false;
                this.visible = false;
                this.pagescale = 0;
                this.curpagescale = 0;
                this.pageNumPending = 0;
                this.pageloaded = true;
                this.dxprint = 0;
                this.dyprint = 0;
                this.dscaleprint = 1;
                this.startx = 0;
                this.starty = 0;
                this.endx = 0;    
                this.endy = 0;
                this.paperobj = paperobj;
                this.pagecanvas = document.createElement('canvas');
                this.contextpg = undefined;

                if( this.paperobj ){
                    var pwidth = (paperobj.width / 25.4)* 96;
                    var pheight = (paperobj.height / 25.4)* 96;
            
                    var pwidthpx = pwidth + "px";
                    var pheightpx = pheight + "px";
            
                    var canvashireswidth = (paperobj.width / 25.4)* 300;
                    var canvashiresheight = (paperobj.height / 25.4)* 300;
    
                }else{

                    this.paperobj = {
                        width : PaperWidth,
                        height : PaperHeight,  
                        printrect : PrintRect
                    };

                    pwidth = (paperobj.width / 25.4)* 96;
                    pheight = (paperobj.height / 25.4)* 96;
            
                    pwidthpx = pwidth + "px";
                    pheightpx = pheight + "px";
            
                    canvashireswidth = (paperobj.width / 25.4)* 300;
                    canvashiresheight = (paperobj.height / 25.4)* 300;

                }


                this.printcanvas = document.createElement('canvas');
                this.printctx = this.printcanvas.getContext('2d');
                //this.print3Dimage = document.createElement('img');

                this.pdfprintcanvas = document.createElement('canvas');
                this.pdfprintctx = this.pdfprintcanvas.getContext('2d');


                this.printobj = {
                    pagenumber: thispage.pagenumber,
                    paperimage: thispage.printcanvas,
                    paperwidth: 297,
                    paperheight: 210,
                    docwidth: 1,
                    docheight: 1,
                    inchtomm: 25.4,
                    DPI: 300,
                    scrDPI: 96,
                    scaleSet: false,
                    pdx: 0,
                    pdy: 0,
                    pscale: 1,
                    printctx: thispage.printctx,
                    setRes: function (dpi) {
                        this.DPI = dpi;
                    },
                    setPaperSize: function (width, height) {
                        this.paperwidth = width;
                        this.paperheight = height;
                        this.paperimage.width = (width / this.inchtomm) * this.DPI;
                        this.paperimage.height = (height / this.inchtomm) * this.DPI;
                    },
                    setDocSize: function (width, height) {
                        this.docwidth = width;
                        this.docheight = height;
                    },
                    setScale: function (printrect) {

                        if(printrect != undefined){
                            
                            //console.log(printrect);
                            //do some scaling to print selection

                            var rectCenterX = printrect.x + (printrect.w / 2);
                            var rectCenterY = printrect.y + (printrect.h / 2);

                            //var ox1 = -((rectCenterX - stats.dx));
                            //var oy1 = -((rectCenterY - stats.dy));

        

                            var xscale = this.paperimage.width / printrect.w; //thispage.MainImageWidth;
                            var yscale = this.paperimage.height / printrect.h; // thispage.MainImageHeight;
                            this.pscale = Math.min(xscale, yscale);


                            //this.pdx = (this.paperimage.width - (this.docwidth * this.pscale)) / 2;
                            //this.pdy = (this.paperimage.height - (this.docheight * this.pscale)) / 2;

                            this.pdx = (-rectCenterX * this.pscale) + (this.paperimage.width / 2);
                            this.pdy = (-rectCenterY * this.pscale) + (this.paperimage.height / 2);


                            //this.pdx = (printrect.x * this.pscale);
                            //this.pdy = (printrect.y * this.pscale);
                            
    
                        }else{

                            var xscale = this.paperimage.width / this.docwidth; //thispage.MainImageWidth;
                            var yscale = this.paperimage.height / this.docheight; // thispage.MainImageHeight;
                            this.pscale = Math.min(xscale, yscale);
                            this.pdx = (this.paperimage.width - (this.docwidth * this.pscale)) / 2;
                            this.pdy = (this.paperimage.height - (this.docheight * this.pscale)) / 2;
                            
                        }
                        this.scaleSet = true;

                    },
                    print: function () {
                        if (!this.scaleSet) {
                            return;
                        }


                        thispage.printPDFpage();


                        /*if (thispage.usepdfjs) {
                            //thispage.renderPDFpagePrint();
                        }*/
                        
                    }
                };



                this.setPDFdim = function(dx, dy,dscale){
        
                    thispage.pageloaded = true;
                    thispage.dxprint = dx;
                    thispage.dyprint = dy;
                    thispage.dscaleprint = dscale;
                    
                    //this.dscalepdf = dscale;
                    //this.initialscale = dscale;
                    //this.vectorloaded = true;
            
                    thispage.startx = thispage.dxprint;
                    thispage.starty = thispage.dyprint;
                    //dxlocal = (canvaso.width - (thispage.VectorPageObj.width*dscalelocal)) / 2;
                    //dylocal = (canvaso.height - (thispage.VectorPageObj.height*dscalelocal)) / 2;
            
            
                        //var canvashireswidth = (paperobj.width / 25.4)* 300;
                        //var canvashiresheight = (paperobj.height / 25.4)* 300;


                    thispage.endx = (canvashireswidth*thispage.dscaleprint) + thispage.startx;
                    thispage.endy = (canvashiresheight*thispage.dscaleprint) + thispage.starty;
            
                    /*if (firstpage && !markuploaded){
                        if(bUsemarkupbyref){
            
                            getMarkupbyReference(path);
                        }else{
                            getMarkupFilelist(path);
                        }
            
                    }*/
            
                    /*if(firstpage){
                        //this.draw_canvas(true);
                        thispage.visible = true;
                    }*/
            
            
                    //drawmarkupAll(thispage.contextpg,thispage);
                    drawmarkupPrint(thispage.contextpg,thispage.pagenum,thispage.dxprint,thispage.dyprint,thispage.dscaleprint);
                    
                    //usepdfjs
                    //hideDownloadDialog();
                    //RxCore_default();
            
                    if(debugmarkuprect){
                        console.log(thispage.startx, thispage.starty, thispage.endx, thispage.endy);
                    }
            
                };                    

                this.printPDFpage = function(){

                    /*if (RxCore_printhelper().docObj.foxitdoc == null){
                        return
                    }*/


                    //var pagewidth = RxCore_printhelper().docObj.pages[thispage.pagenum].originalwidth;
                    //var pageheigth = RxCore_printhelper().docObj.pages[thispage.pagenum].originalheight;


                    

                    //var tempwscale = canvashireswidth / pagewidth;
                    //var temphscale = canvashiresheight / pageheigth;

    
                        
                        //var tempwscale = pageCanvases[thispage.pagenum].width / thispage.pagewidth;
                        //var temphscale = pageCanvases[thispage.pagenum].height / thispage.pageheight;
                        
                    //thispage.pagescale = Math.min(tempwscale,temphscale);



                    //RxCore_printhelper().docObj.foxitdoc.getBitmap(thispage.pagenumber, thispage.printobj.pscale, thispage.setPageBitmapPrint);

                    //RxCore_printhelper().docObj.foxitdoc.getBitmap(thispage.pagenum, thispage.pagescale, thispage.setPageBitmapPrint);

                    if (RxCore_printhelper().docObj.foxitdoc != null) {
                        RxCore_printhelper().docObj.foxitdoc.getBitmap(thispage.pagenumber, thispage.printobj.pscale, thispage.setPageBitmapPrint);
                    }


                };

                this.setPageBitmapPrint = function (bitmap) {
                    
                    if(bitmap.error){

                        return;
                        //check for error

                    }

                    
                    var tdv = new Uint8ClampedArray(bitmap.buffer);
                    var imageData = new ImageData(tdv, bitmap.width, bitmap.height);

                    thispage.pdfprintcanvas.width = bitmap.width;
                    thispage.pdfprintcanvas.height = bitmap.height;


                    //thispage.pdfprintctx = thispage.pdfprintcanvas.getContext('2d');
                    //thispage.offscreenwidth = bitmap.width;
                    //thispage.offscreenheight = bitmap.height;

                    thispage.pdfprintctx.putImageData(imageData, 0, 0);
                    
                    thispage.printobj.printctx.drawImage(thispage.pdfprintcanvas, thispage.printobj.pdx, thispage.printobj.pdy, thispage.printobj.docwidth * thispage.printobj.pscale, thispage.printobj.docheight * thispage.printobj.pscale);
                    drawmarkupPrint(thispage.printobj.printctx, thispage.pagenumber, thispage.printobj.pdx, thispage.printobj.pdy, thispage.printobj.pscale);

                    //thispage.pagescale
                    //thispage.setPDFdim(thispage.pdfdxtemp,thispage.pdfdytemp,thispage.pagescale);
                    //thispage.setPDFdim()
                    
                };

                /*this.setPageBitmapPrint = function (bitmap) {
                    
                    if(bitmap.error){

                        return;
                        //check for error

                    }

                    thispage.pdfdxtemp = 0;
                    thispage.pdfdytemp = 0;

                    thispage.pageRendering = false;
                    var tdv = new Uint8ClampedArray(bitmap.buffer);
                    var imageData = new ImageData(tdv, bitmap.width, bitmap.height);
                    //this.role = 0;
                    // 0 - no role.
                    // 1 - backgroundpage.
                    // 2 - overlaypage.
                    //thispage.dscalepdf = 1;
                    thispage.pagecanvas.width = bitmap.width;
                    thispage.pagecanvas.height = bitmap.height;


                    thispage.contextpg = thispage.pagecanvas.getContext('2d');
                    //thispage.offscreenwidth = bitmap.width;
                    //thispage.offscreenheight = bitmap.height;

                    thispage.contextpg.putImageData(imageData, 0, 0);
                    thispage.pdfisrendered = true;


                    //thispage.pagescale
                    //thispage.setPDFdim(thispage.pdfdxtemp,thispage.pdfdytemp,thispage.pagescale);
                    //thispage.setPDFdim()
                    
                };*/


                /*this.queueRenderPage = function () {
                    thispage.renderPDFpage();
                };*/

                /*this.printPDFpage = function(){

                    if (RxCore_printhelper().docObj.foxitdoc == null){
                        return
                    }

                    var pagewidth = RxCore_printhelper().docObj.pages[thispage.pagenum].originalwidth;
                    var pageheigth = RxCore_printhelper().docObj.pages[thispage.pagenum].originalheight;


                    

                    var tempwscale = canvashireswidth / pagewidth;
                    var temphscale = canvashiresheight / pageheigth;

    
                        
                        //var tempwscale = pageCanvases[thispage.pagenum].width / thispage.pagewidth;
                        //var temphscale = pageCanvases[thispage.pagenum].height / thispage.pageheight;
                        
                    thispage.pagescale = Math.min(tempwscale,temphscale);



                    RxCore_printhelper().docObj.foxitdoc.getBitmap(thispage.pagenum, thispage.pagescale, thispage.setPageBitmapPrint);


                };*/

            }

        }        

        class PageObject {
            constructor(pagexml, LayoutName, firstpage, path, pagenum, DocRef) {
                var thispage = this;
                var relpath = xmlurlrel;
                if (bUseCustomrelpath) {
                    relpath = xmlurlrelcustom;
                }
                this.DocRef = DocRef;
                
                this.pagenumber = pagenum;
                this.firstpage = firstpage;
                // 0 = large 1=small
                this.camera = null;
                this.navcamera = null;
                this.birdseyemarker = null;
                this.bis3Dnavigator = false;
                this.navfloorplan = null;
                this.camerapos = new THREE.Vector3(0, 0, 0);
                this.camerarot = new THREE.Euler(0, 1, 1.57, 'XYZ');
                this.boxcenter = new THREE.Vector3(0, 0, 0);
                this.birdseyedispeye = { x: 0, y: 0, r: 0, scale: 1, boxcenter: null, camerapos: null, rotation: 0 };
                this.walkthroughcontrol = {};
                this.currentimage = 1;
                //large image scale and offset
                this.dx = 0.0;
                this.dy = 0.0;
                this.dscale = 1.0;
                this.initialscale = 1.0;

                this.scaleObject = new ScaleObject();
                
                //this.measurescale = MeasureScale;
                //this.Unitofmeasure = Unitofmeasure;
                //this.unitname = "Metric";
                //this.SubmeasureUnit = SubmeasureUnit;
                //this.Unitlabel = Unitlabel;
                //this.AreaUnitlabel = AreaUnitlabel;

                //this.unitscale = unitscale;


                //unit : Unitofmeasure,
                //unitname : uname,
                //subunit : SubmeasureUnit,
                //label : Unitlabel,
                //Arealabel : AreaUnitlabel

                this.invalid = false;
                //vector image scale and offset
                this.dxvector = 0.0;
                this.dyvector = 0.0;
                
                this.dxpdf = 0.0;
                this.dypdf = 0.0;

                this.dxpdfnonrot = 0.0;
                this.dypdfnonrot = 0.0;

                this.dxpdfrot = 0.0;
                this.dypdfrot = 0.0;

                this.dxpdfzerorot = 0.0;
                this.dypdfzerorot = 0.0;


                this.pdfstartx = 0.0;
                this.pdfstarty = 0.0;
                this.pdfendx = 0.0;
                this.pdfendy = 0.0;


                this.foxitdx = 0.0;
                this.foxitdy = 0.0;
                this.dscalepdf = 1.0;
                this.dscalebackup = 1.0;
                this.pdfpageref = null;
                this.renderTask = null;
                this.rendercancel = false;
                this.magnifyrestscale = 1.0;
                this.dscalepdfabs = 1.0;
                this.dxbackup = 0.0;
                this.dybackup = 0.0;
                this.resizepan = false;
                this.curpagescale = 1.0;
                this.curfoxitpagescale = 1.0;
                this.foxitpagerot = 0;
                this.foxitpageorigrot = 0;
                this.curpagescalebackup = 1.0;

                this.pdfpageheight = 0.0;
                this.pdfpagewidth = 0.0;

                this.originalPDFwidth = 0;
                this.originalPDFheight = 0;

                this.bpagesizeupdated = false;

                this.curzoomfunction = 0;

                this.role = 0;
                this.bgpagewidth = 0.0;
                this.bgpageheight = 0.0;
                this.ovpageheight = 0.0;
                this.ovpagewidth = 0.0;
                this.overlayScale = 1.0;
                this.overlayOffsetX = 0.0;
                this.overlayOffsetY = 0.0;
                this.backgroundScale = 1.0;
                this.backgroundOffsetX = 0.0;
                this.backgroundOffsetY = 0.0;
                this.bMaxzoom = false;
                this.bMinzoom = false;


                this.nPDFMaxFactor = 4.0;
                this.pdfisrendered = false;
                this.textareaset = false;
                this.pdfrenderopque = 0;
                this.pdfisfirstrendered = false;
                this.iscollapsed = true;
                this.offscreenwidth = 0.0;
                this.offscreenheight = 0.0;
                this.pdfstartrender = -1;
                this.pdfendrender = -1;
                this.pdfpagetime = -1;
                this.rxdrawtimestart = 0.0;
                this.rxdrawtimeend = 0.0;
                this.scrollzoomtime = 0.0;
                this.scrollzoominterval = 0.0;
                this.scrollzoomarray = [];
                this.debounce_timer = undefined;
                this.vectordebounce_timer = undefined;
                this.bautoloadthumbnails = bAutoloadThumbnails;
                this.pdftimervar = null;
                this.PDFTextArea = undefined;
                this.PDFPointArray = [];
                this.PDFSnapArray = [];
                this.PDFSnapPoint = undefined;
                this.pdfsnapready = true;

                this.textselectobj = {
                    rectarray : undefined,
                    start : -1,
                    end : -1,
                    bstart : false,
                    bend : false,
                    startpos : null,
                    endpos : null,
                    $handler : null,
                    complete : false
                };

                this.StoreyMetadata = [];
                this.visibleTiles = [];
                this.tilevisibiltychanged = false;
                this.PDFsnapTransform = null;
                this.bpointlistSet = false;
                this.PDFinternalScale = 1.0;
                this.PDFpageRotate = 0;
                this.pdfSnapScale = 1.0;
                this.TextSelectArea = undefined;
                this.snapEnabled = false;
                this.dscalevector = 1.0;
                this.fixedScale = 1.0;
                this.vectorisrendered = false;
                this.vectorischanged = false;
                this.vectorstartrender = -1;
                this.vectorendrender = -1;
                this.vectorpagetime = -1;
                this.vectortimervar = null;
                this.bDorescaleonsizeChange = bDoReScaleOnSize;
                this.width = 0;
                this.height = 0;
                //universal page original widht and height
                this.originalwidth = 0;
                this.originalheight = 0;
                this.OriginalScale = 1;
                //this.dscalevector = 1.0;
                this.initialzoom = 1;
                this.visible = false;
                //composite properties and reference.
                this.usedincomposite = false;
                this.compositereference = undefined;
                this.compositePrintreference = undefined;
                this.isbackground = false;
                this.isoverlay = false;
                //small image scale and offset
                this.dxextent = 0.0;
                this.dyextent = 0.0;
                this.startx = 0.0;
                this.starty = 0.0;
                this.endx = 0.0;
                this.endy = 0.0;
                this.dscaleextent = 1.0;
                this.bitmapratio = 1.0;
                this.pagedimset = false;
                //print scale and offset factors.
                this.dxprint = 0.0;
                this.dyprint = 0.0;
                this.dscaleprint = 1.0;
                //canvas thumbnail scale and offset.
                this.dxthumb = 0.0;
                this.dythumb = 0.0;
                this.dxthumbtemp = 0.0;
                this.dythumbtemp = 0.0;
                this.dscalethumb = 1.0;
                this.pdftempthumbscale = 1.0;
                this.drotation = 0;
                this.pagelockrotation = 0;
                this.textsearchindx = 0;
                this.usevectorxml = false;
                this.usevectorbinary = false;

                this.usevectorbinarycompare = false;
                this.loadbackgroundvectors = false;
                this.loadoverlayvectors = false;

                this.usepdfjs = false;
                this.drawtextSearchRect = false;
                this.usevector3Dxml = false;
                this.usevector3Dbinary = false;
                this.usevector3DGLTF = false;
                this.has3Dnav = false;
                this.pageloaded = false;
                this.markuplistloaded = false;
                this.pageloading = false;
                this.pdfdiminitset = false;
                this.VectorPageObj = undefined;
                this.Vector3DPageObj = undefined;
                this.VectorPartfilelist = [];
                this.VectorBPartfilelist = [];
                this.largeimage = document.createElement('img'); //new Image();
                this.smallimage = document.createElement('img'); //new Image();
                this.smallimageloaded = false;
                this.largeimageloaded = false;
                this.vectorloaded = false;
                this.rendercallback = false;
                this.thumbnnailloaded = false;
                //this.imagethumb = document.createElement('img');//new Image();
                this.CurrentMarkup = 0;
                //MarkupZoom
                this.LayoutName = LayoutName;
                var arrayBuffer = null;

                if (pagexml == 'ComparePage') {
                    thispage.compositereference = DocRef;
                    thispage.usedincomposite = true;
                    //set compare page values here.
                    thispage.PageName = LayoutName;
                    if (thispage.PageName == 'overlay') {
                        thispage.role = 2;
                        thispage.DocRef = thispage.compositereference.pages[1].DocRef;
                    }
                    else if (thispage.PageName == 'background') {
                        thispage.role = 1;
                        thispage.DocRef = thispage.compositereference.pages[0].DocRef;
                    }
                    //thispage.compositereference.pages[pagenum].DocRef
                    thispage.usedincomposite = true;
                    //set compare page values here.
                    thispage.PageName = LayoutName;
                }
                else {
                    if (pagexml.getElementsByTagName("PageName")[0] != undefined) {
                        this.PageName = pagexml.getElementsByTagName("PageName")[0].firstChild.nodeValue;
                    }
                    else {
                        this.PageName = LayoutName;
                    }
                    if (pagexml.getElementsByTagName("Compression")[0] != undefined) {
                        this.Compression = pagexml.getElementsByTagName("Compression")[0].firstChild.nodeValue;
                    }
                    if (pagexml.getElementsByTagName("DPI")[0] != undefined) {
                        this.DPI = pagexml.getElementsByTagName("DPI")[0].firstChild.nodeValue;
                    }
                    if (pagexml.getElementsByTagName("OffsetX")[0] != undefined) {
                        this.OffsetX = pagexml.getElementsByTagName("OffsetX")[0].firstChild.nodeValue;
                    }
                    if (pagexml.getElementsByTagName("OffsetY")[0] != undefined) {
                        this.OffsetY = pagexml.getElementsByTagName("OffsetY")[0].firstChild.nodeValue;
                    }
                    if (pagexml.getElementsByTagName("OriginalScale")[0] != undefined) {
                        this.OriginalScale = pagexml.getElementsByTagName("OriginalScale")[0].firstChild.nodeValue;
                    }
                    if (pagexml.getElementsByTagName("VectorBinary2DSRC")[0] != undefined) {
                        this.Vector2DBinarySRC = encodeURI(relpath + pagexml.getElementsByTagName("VectorBinary2DSRC")[0].firstChild.nodeValue);
                        this.usevectorbinary = true;
                    }

                    if (pagexml.getElementsByTagName("OverlayComparemode")[0] != undefined){
                        thispage.OverlayComparemode = parseInt(pagexml.getElementsByTagName("OverlayComparemode")[0].firstChild.nodeValue);
                    }


                    if (pagexml.getElementsByTagName("BckGrndDocindex")[0] != undefined){

                        thispage.comparebgdocindex = parseInt(pagexml.getElementsByTagName("BckGrndDocindex")[0].firstChild.nodeValue);
                        if (pagexml.getElementsByTagName("BckGrndPageindex")[0] != undefined){
                            thispage.comparebgpageindex = parseInt(pagexml.getElementsByTagName("BckGrndPageindex")[0].firstChild.nodeValue);
                        }


                    }

                    if (pagexml.getElementsByTagName("FrGrndDocindex")[0] != undefined){

                        thispage.comparefgdocindex = parseInt(pagexml.getElementsByTagName("FrGrndDocindex")[0].firstChild.nodeValue);
                        if (pagexml.getElementsByTagName("FrGrndPageindex")[0] != undefined){
                            thispage.comparefgpageindex = parseInt(pagexml.getElementsByTagName("FrGrndPageindex")[0].firstChild.nodeValue);
                        }


                    }

                    
                    if (pagexml.getElementsByTagName("Vector2DBinarySRCBckGrnd")[0] != undefined){

                        this.Vector2DBinarySRCBckGrnd = encodeURI(pagexml.getElementsByTagName("Vector2DBinarySRCBckGrnd")[0].firstChild.nodeValue);
                        thispage.usevectorbinarycompare = true;
                        thispage.loadbackgroundvectors = true;

                        //Vector2DBinarySRCBckGrnd
                    }
                    if (pagexml.getElementsByTagName("Vector2DBinarySRCFrGrnd")[0] != undefined){

                        this.Vector2DBinarySRCFrGrnd = encodeURI(pagexml.getElementsByTagName("Vector2DBinarySRCFrGrnd")[0].firstChild.nodeValue);
                        thispage.usevectorbinarycompare = true;
                        thispage.loadoverlayvectors = true;
                        //Vector2DBinarySRCBckGrnd



                    }

                    if (pagexml.getElementsByTagName("Vector2DImageSRCBckGrnd")[0] != undefined){

                        this.Vector2DImageSRCBckGrnd = encodeURI(pagexml.getElementsByTagName("Vector2DImageSRCBckGrnd")[0].firstChild.nodeValue);
                        thispage.usevectorbinarycompare = true;
                        thispage.loadbackgroundvectors = false;
                        thispage.loadbackgroundimage = true;


                        if (pagexml.getElementsByTagName("MainImageWidthBckGrnd")[0] != undefined){
                            thispage.MainImageWidthBckGrnd = parseInt(pagexml.getElementsByTagName("MainImageWidthBckGrnd")[0].firstChild.nodeValue);
                        }
                        if (pagexml.getElementsByTagName("MainImageHeightBckGrnd")[0] != undefined){
                            thispage.MainImageHeightBckGrnd = parseInt(pagexml.getElementsByTagName("MainImageHeightBckGrnd")[0].firstChild.nodeValue);
                        }

                        if (pagexml.getElementsByTagName("MainImageScalingBckGrnd")[0] != undefined){
                            thispage.MainImageScalingBckGrnd = parseInt(pagexml.getElementsByTagName("MainImageScalingBckGrnd")[0].firstChild.nodeValue);
                        }

                        if (pagexml.getElementsByTagName("MainImageOffsetXBckGrnd")[0] != undefined){
                            thispage.MainImageOffsetXBckGrnd = parseInt(pagexml.getElementsByTagName("MainImageOffsetXBckGrnd")[0].firstChild.nodeValue);
                        }

                        if (pagexml.getElementsByTagName("MainImageOffsetYBckGrnd")[0] != undefined){
                            thispage.MainImageOffsetYBckGrnd = parseInt(pagexml.getElementsByTagName("MainImageOffsetYBckGrnd")[0].firstChild.nodeValue);
                        }

                        if (pagexml.getElementsByTagName("SmallImageWidthBckGrnd")[0] != undefined){
                            thispage.SmallImageWidthBckGrnd = parseInt(pagexml.getElementsByTagName("SmallImageWidthBckGrnd")[0].firstChild.nodeValue);
                        }

                        if (pagexml.getElementsByTagName("SmallImageHeightBckGrnd")[0] != undefined){
                            thispage.SmallImageHeightBckGrnd = parseInt(pagexml.getElementsByTagName("SmallImageHeightBckGrnd")[0].firstChild.nodeValue);
                        }
                        
                        


                        //Vector2DBinarySRCBckGrnd
                    }

                    if (pagexml.getElementsByTagName("Vector2DImageSRCFrGrnd")[0] != undefined){

                        this.Vector2DImageSRCFrGrnd = encodeURI(pagexml.getElementsByTagName("Vector2DImageSRCFrGrnd")[0].firstChild.nodeValue);
                        thispage.usevectorbinarycompare = true;
                        thispage.loadoverlayvectors = false;
                        thispage.loadoverlayimage = true;


                        if (pagexml.getElementsByTagName("MainImageWidthFrGrnd")[0] != undefined){
                            thispage.MainImageWidthFrGrnd = parseInt(pagexml.getElementsByTagName("MainImageWidthFrGrnd")[0].firstChild.nodeValue);
                        }
                        if (pagexml.getElementsByTagName("MainImageHeightFrGrnd")[0] != undefined){
                            thispage.MainImageHeightFrGrnd = parseInt(pagexml.getElementsByTagName("MainImageHeightFrGrnd")[0].firstChild.nodeValue);
                        }

                        if (pagexml.getElementsByTagName("MainImageScalingFrGrnd")[0] != undefined){
                            thispage.MainImageScalingFrGrnd = parseFloat(pagexml.getElementsByTagName("MainImageScalingFrGrnd")[0].firstChild.nodeValue);
                        }

                        if (pagexml.getElementsByTagName("MainImageOffsetXFrGrnd")[0] != undefined){
                            thispage.MainImageOffsetXFrGrnd = parseInt(pagexml.getElementsByTagName("MainImageOffsetXFrGrnd")[0].firstChild.nodeValue);
                        }

                        if (pagexml.getElementsByTagName("MainImageOffsetYFrGrnd")[0] != undefined){
                            thispage.MainImageOffsetYFrGrnd = parseInt(pagexml.getElementsByTagName("MainImageOffsetYFrGrnd")[0].firstChild.nodeValue);
                        }

                        if (pagexml.getElementsByTagName("SmallImageWidthFrGrnd")[0] != undefined){
                            thispage.SmallImageWidthFrGrnd = parseInt(pagexml.getElementsByTagName("SmallImageWidthFrGrnd")[0].firstChild.nodeValue);
                        }

                        if (pagexml.getElementsByTagName("SmallImageHeightFrGrnd")[0] != undefined){
                            thispage.SmallImageHeightFrGrnd = parseInt(pagexml.getElementsByTagName("SmallImageHeightFrGrnd")[0].firstChild.nodeValue);
                        }
                        //Vector2DBinarySRCBckGrnd
                    }

                    
                    if(pagexml.getElementsByTagName("PDFBckGrnd")[0] != undefined){

                        thispage.usevectorbinarycompare = true;
                        //thispage.loadoverlayvectors = false;
                        thispage.loadbackgroundvectors = false;
                        thispage.loadbackgroundpdf = true;


                        if (pagexml.getElementsByTagName("PDFWidthBckGrnd")[0] != undefined){
                            thispage.PDFWidthBckGrnd = parseInt(pagexml.getElementsByTagName("PDFWidthBckGrnd")[0].firstChild.nodeValue);
                        }
                        if (pagexml.getElementsByTagName("PDFHeightBckGrnd")[0] != undefined){
                            thispage.PDFHeightBckGrnd = parseInt(pagexml.getElementsByTagName("PDFHeightBckGrnd")[0].firstChild.nodeValue);
                        }


                        //createAndAppendElement(pageNode, "PDFBckGrnd", fggmainimagewidth);
                    }
                    if(pagexml.getElementsByTagName("PDFFrGrnd")[0] != undefined){

                        thispage.usevectorbinarycompare = true;
                        thispage.loadoverlayvectors = false;
                        thispage.loadoverlaypdf = true;

                        if (pagexml.getElementsByTagName("PDFWidthFrGrnd")[0] != undefined){
                            thispage.PDFWidthFrGrnd = parseInt(pagexml.getElementsByTagName("PDFWidthFrGrnd")[0].firstChild.nodeValue);
                        }
                        if (pagexml.getElementsByTagName("PDFHeightFrGrnd")[0] != undefined){
                            thispage.PDFHeightFrGrnd = parseInt(pagexml.getElementsByTagName("PDFHeightFrGrnd")[0].firstChild.nodeValue);
                        }

                        //createAndAppendElement(pageNode, "PDFFrGrnd", fggmainimagewidth);
                    }                    

                    if (pagexml.getElementsByTagName("Vector2DSRC")[0] != undefined) {
                        this.Vector2DSRC = encodeURI(relpath + pagexml.getElementsByTagName("Vector2DSRC")[0].firstChild.nodeValue);
                        if (!this.usevectorbinary) {
                            this.usevectorxml = true;
                        }
                        //this.VectorDisplaylist = new VectorDisplaylist(this.Vector2DSRC);
                    }
                    //VectorBinary3DSRC
                    var busegltf = false;

                    if(this.DocRef.PublisherVersion){
                        var version = this.DocRef.PublisherVersion.split(".");

                        var build = parseInt(version[3].trim());
    
                        if (build > 129){
                            busegltf = true;
                        }
    
                    }else{
                        busegltf = false;
                    }

                    /*if (this.DocRef.PublisherVersion == "19.0.0.130" || this.DocRef.PublisherVersion == "19.0.0.131" || this.DocRef.PublisherVersion == "19.0.0.132" || this.DocRef.PublisherVersion == "19.0.0.133") {
                        busegltf = true;
                    }*/

                    if (pagexml.getElementsByTagName("VectorBinary3DSRC")[0] != undefined) {
                        this.Vector3DBinarySRC = encodeURI(relpath + pagexml.getElementsByTagName("VectorBinary3DSRC")[0].firstChild.nodeValue);
                        this.usevector3Dbinary = true;
                        if (pagexml.getElementsByTagName("Navigator3DSRC")[0] != undefined) {
                            this.Navigator3DSRC = encodeURI(relpath + pagexml.getElementsByTagName("Navigator3DSRC")[0].firstChild.nodeValue);
                            this.has3Dnav = true;
                        }
                        /*if(pagexml.getElementsByTagName("Parts3D")[0] != undefined){
                         var part3Dbinaryobj = pagexml.getElementsByTagName("SRC");
                         for(j=0;j<part3Dbinaryobj.length;j++){
                         this.VectorBPartfilelist[j] = encodeURI(xmlurlrel + part3Dxmlobj[j].firstChild.nodeValue);
                         }
                         }*/
                        //this.VectorDisplaylist = new VectorDisplaylist(this.Vector2DSRC);
                    }
                    if (pagexml.getElementsByTagName("VectorGLTF3DSRC")[0] != undefined) {
                        //this.Vector3DBinarySRC = encodeURI(pagexml.getElementsByTagName("VectorGLTF3DSRC")[0].firstChild.nodeValue);

                        if(pagexml.getElementsByTagName("VectorMetadataSRC")[0] != undefined){
                            this.Vector3DBinarySRC = encodeURI(relpath + pagexml.getElementsByTagName("VectorGLTF3DSRC")[0].firstChild.nodeValue);
                        }
                        
                        //var cachfolder = getURLPath(thispage.Vector3DBinarySRC);
                        //this.FloorplanSRC = cachfolder + "floorplan0.png";
                        this.VectorMetadataSRC = encodeURI(relpath + pagexml.getElementsByTagName("VectorMetadataSRC")[0].firstChild.nodeValue);
                        this.usevector3DGLTF = true;
                        this.usevector3Dxml = true;
                        this.usevector3Dbinary = false;
                        this.floorplans = [];
                        if (pagexml.getElementsByTagName("Floorplan")[0] != undefined) {
                            var floorplansxml = pagexml.getElementsByTagName('Floorplan');
                            for (j = 0; j < floorplansxml.length; j++) {
                                var fsrc = floorplansxml[j].getElementsByTagName('SRC')[0].firstChild.nodeValue;
                                var fname = "";

                                if(floorplansxml[j].getElementsByTagName('Name')[0] != undefined){

                                    if(floorplansxml[j].getElementsByTagName('Name')[0].firstChild.nodeValue != undefined){
                                        fname = floorplansxml[j].getElementsByTagName('Name')[0].firstChild.nodeValue;
                                    }
    
                                }

                                var fsrc = relpath + fsrc;
                                var felev = parseFloat(floorplansxml[j].getElementsByTagName('Elevation')[0].firstChild.nodeValue);
                                var fscale = parseFloat(floorplansxml[j].getElementsByTagName('Scale')[0].firstChild.nodeValue);
                                var floorplancnv = document.createElement('canvas');
                                var floorplanctx = floorplancnv.getContext('2d');
                                var floorplanthmbcnv = document.createElement('canvas');
                                var floorplanthmbctx = floorplanthmbcnv.getContext('2d');
                                var floorplanimage = document.createElement('img');
                                //this.smallimage = document.createElement('img'); //new Image();
                                this.floorplans.push({
                                    index: j,
                                    SRC: fsrc,
                                    Elevation: felev,
                                    Name : fname,
                                    Scale: fscale,
                                    BScale: fscale,
                                    RScale: fscale,
                                    replaceScale: 1,
                                    image: floorplanimage,
                                    canvas: floorplancnv,
                                    ctx: floorplanctx,
                                    thumbcanvas: floorplanthmbcnv,
                                    thumbctx: floorplanthmbctx,
                                    thumbwidht: ThumbnailWidth,
                                    thumbheight: ThumbnailHeight,
                                    replaced: false,
                                    originalWidth: 0,
                                    originalHeight: 0,
                                    width: 0,
                                    height: 0,
                                    offsetX: 0,
                                    offsetY: 0,
                                    alignarray: []
                                });
                                //var floorplan = new PageObject(, layoutname, true, path, j, DocObj);
                            }
                            //sort by elevation
                            this.floorplans.sort(function (a, b) {
                                return a.Elevation - b.Elevation;
                            });
                        }
                        if(pagexml.getElementsByTagName("Storey")[0] != undefined){
                            var storeys = pagexml.getElementsByTagName('Storey');
                            for (var si = 0; si < storeys.length; si++) {
                                
                                if(storeys[si].getElementsByTagName("IDSRC")[0] != undefined){
                                    var metasrc = encodeURI(relpath + storeys[si].getElementsByTagName("IDSRC")[0].firstChild.nodeValue);

                                    if(storeys[si].getElementsByTagName("Name")[0] != undefined){
                                        var storeyname = storeys[si].getElementsByTagName("Name")[0].firstChild.nodeValue;
                                    }

                                    if(storeys[si].getElementsByTagName("GlobalID")[0] != undefined){
                                        var GlobalID = storeys[si].getElementsByTagName("GlobalID")[0].firstChild.nodeValue;
                                        
                                    }
                                    if(storeys[si].getElementsByTagName("Elevation")[0] != undefined){
                                        var elevation = storeys[si].getElementsByTagName("Elevation")[0].firstChild.nodeValue;
                                        
                                    }

                                    if(storeyname && GlobalID){
                                        this.StoreyMetadata.push({src : metasrc, name : storeyname, id : GlobalID, index : si, Elevation : elevation});
                                    }
                                }
                            }                            
                        }

                        if (pagexml.getElementsByTagName("NavigatorJSONSRC")[0] != undefined) {
                            this.NavigatorJSONSRC = encodeURI(relpath + pagexml.getElementsByTagName("NavigatorJSONSRC")[0].firstChild.nodeValue);
                            this.has3Dnav = true;
                        }


                        if (pagexml.getElementsByTagName("Navigator3DSRC")[0] != undefined) {
                            this.Navigator3DSRC = encodeURI(relpath + pagexml.getElementsByTagName("Navigator3DSRC")[0].firstChild.nodeValue);
                            this.has3Dnav = true;
                        }
                    }
                    if (pagexml.getElementsByTagName("Vector3DSRC")[0] != undefined) {
                        this.Vector3DSRC = encodeURI(relpath + pagexml.getElementsByTagName("Vector3DSRC")[0].firstChild.nodeValue);
                        if (!this.usevector3Dbinary) {
                            this.usevector3Dxml = true;
                        }
                        if (pagexml.getElementsByTagName("Parts3D")[0] != undefined) {
                            var part3Dxmlobj = pagexml.getElementsByTagName("SRC");
                            for (var j = 0; j < part3Dxmlobj.length; j++) {
                                this.VectorPartfilelist[j] = encodeURI(relpath + part3Dxmlobj[j].firstChild.nodeValue);
                            }
                        }
                        //this.VectorDisplaylist = new VectorDisplaylist(this.Vector2DSRC);
                    }
                }
                if (thispage.DocRef.Type == "3" || thispage.DocRef.Type == "7") {
                    this.TextSelectArea = document.createElement('div');
                    this.TextSelectArea.className = "textLayer";
                    //this.TextSelectArea.id = "Page" + this.pagenumber;
                    this.TextSelectArea.addEventListener('wheel', ev_canvas, true);
                    this.TextSelectArea.style.display = "none";
                }
                var szformatshort = thispage.DocRef.Format.substring(0, 9);
                if (szformatshort == "Adobe PDF") {
                    this.bautoloadthumbnails = false;
                    this.tilespending = false;
                    this.prioritytilespending = false;
                    this.pagecanvas = document.createElement('canvas');
                    this.pagectx = this.pagecanvas.getContext('2d');
                    this.canvtileArray = [];
                    this.bgCanvtileArray = [];
                    //this.tempcanvtileArray = [];
                    //this.tempfoxitpage = null;
                    this.tempfoxitpage = {
                        canvases: [],
                        imagedim: { width: 0, height: 0 },
                        havecanvases: false
                    };
                    this.busetempcanvases = false;
                    this.annotArray = [];
                    this.usepdfjs = true;
                    //this.pagedrawCanvas = document.createElement('canvas');
                    //this.pagedrawctx = this.pagedrawCanvas.getContext('2d');
                    //var canvasrow = [];
                    /*var canvobj = {
                        cnv : this.pagedrawCanvas,
                        ctx : this.pagedrawctx
                    }*/
                    //canvasrow.push(canvobj); 
                    //canvasrow.push
                    //this.canvtileArray.push(canvasrow);
                    //this.pagecanvas = document.createElement('canvas');
                    //this.pagectx = this.pagecanvas.getContext('2d');
                    //setSmoothingEnabledEx(false, this.pagectx);
                    this.magnifycanvas = document.createElement('canvas');
                    this.magnifypagectx = this.magnifycanvas.getContext('2d');
                    this.pageRendering = false;
                    this.pagePrinting = false;
                    this.pageNumPending = null;
                    this.pagescale = 1.5;
                    this.markupscaleadjust = 1.0;
                    this.pdfdxtemp = 0.0;
                    this.pdfdytemp = 0.0;
                    this.pdfdscaletemp = 1.0;
                    this.PDFTextArea = document.createElement('div');
                    this.PDFTextArea.style.zIndex = "4";
                    this.PDFTextArea.className = "textLayer";
                    this.PDFTextArea.addEventListener('wheel', ev_canvas, true);
                    this.pdfpagetime = 10;
                    /*thispage.pdftimervar = setInterval(function () {
                        thispage.pdftimer()
                    }, thispage.pdfpagetime);*/
                    this.viewport = null;
                    this.magviewport = null;
                    this.compareviewport = null;
                }
                else {
                    if (pagexml == 'ComparePage') {
                        //set compare page values here.
                    }
                    else {
                        if (pagexml.getElementsByTagName("MainImageSRC")[0] != undefined) {
                            this.MainImageSRC = encodeURI(relpath + pagexml.getElementsByTagName("MainImageSRC")[0].firstChild.nodeValue);
                        }
                        if (pagexml.getElementsByTagName("SmallImageSRC")[0] != undefined) {
                            this.SmallImageSRC = encodeURI(relpath + pagexml.getElementsByTagName("SmallImageSRC")[0].firstChild.nodeValue);
                        }
                        if (pagexml.getElementsByTagName("SmallImageWidth")[0] != undefined) {
                            this.SmallImageWidth = pagexml.getElementsByTagName("SmallImageWidth")[0].firstChild.nodeValue;
                            this.SmallImageWidth = parseInt(this.SmallImageWidth);
                        }
                        if (pagexml.getElementsByTagName("SmallImageHeight")[0] != undefined) {
                            this.SmallImageHeight = pagexml.getElementsByTagName("SmallImageHeight")[0].firstChild.nodeValue;
                            this.SmallImageHeight = parseInt(this.SmallImageHeight);
                        }
                        if (pagexml.getElementsByTagName("SmallImageScaling")[0] != undefined) {
                            this.SmallImageScaling = pagexml.getElementsByTagName("SmallImageScaling")[0].firstChild.nodeValue;
                            this.SmallImageScaling = parseFloat(this.SmallImageScaling);
                        }
                    }
                }
                //this.MainImageSRC = encodeURI(relpath  + pagexml.getElementsByTagName("MainImageSRC")[0].firstChild.nodeValue);
                //this.SmallImageSRC = encodeURI(relpath + pagexml.getElementsByTagName("SmallImageSRC")[0].firstChild.nodeValue);
                if (pagexml == 'ComparePage') {
                    //set compare page values here.
                }
                else {
                    if (pagexml.getElementsByTagName("ThumbnailImageSRC")[0] != undefined) {
                        if (pagexml.getElementsByTagName("ThumbnailImageSRC")[0].firstChild != undefined) {
                            this.ThumbnailImageSRC = relpath + pagexml.getElementsByTagName("ThumbnailImageSRC")[0].firstChild.nodeValue;
                        }
                    }
                    if (pagexml.getElementsByTagName("MainImageWidth")[0] != undefined) {
                        this.MainImageWidth = pagexml.getElementsByTagName("MainImageWidth")[0].firstChild.nodeValue;
                        this.MainImageWidth = parseInt(this.MainImageWidth);
                    }
                    if (pagexml.getElementsByTagName("MainImageHeight")[0] != undefined) {
                        this.MainImageHeight = pagexml.getElementsByTagName("MainImageHeight")[0].firstChild.nodeValue;
                        this.MainImageHeight = parseInt(this.MainImageHeight);
                    }
                    if (pagexml.getElementsByTagName("MainImageScaling")[0] != undefined) {
                        this.MainImageScaling = pagexml.getElementsByTagName("MainImageScaling")[0].firstChild.nodeValue;
                        this.MainImageScaling = parseFloat(this.MainImageScaling);
                    }
                    if (pagexml.getElementsByTagName("MainImageOffsetX")[0] != undefined) {
                        this.MainImageOffsetX = pagexml.getElementsByTagName("MainImageOffsetX")[0].firstChild.nodeValue;

                    }
                    if (pagexml.getElementsByTagName("MainImageOffsetY")[0] != undefined) {
                        this.MainImageOffsetY = pagexml.getElementsByTagName("MainImageOffsetY")[0].firstChild.nodeValue;
                    }
                }
                //this.SmallImageWidth = pagexml.getElementsByTagName("SmallImageWidth")[0].firstChild.nodeValue;
                //this.SmallImageHeight = pagexml.getElementsByTagName("SmallImageHeight")[0].firstChild.nodeValue;
                //this.SmallImageScaling = pagexml.getElementsByTagName("SmallImageScaling")[0].firstChild.nodeValue;
                this.largeimagecnv = document.createElement('canvas');
                this.largeimagectx = this.largeimagecnv.getContext('2d', {willReadFrequently :true});

                this.smallimagecnv = document.createElement('canvas');
                this.smallimagectx = this.smallimagecnv.getContext('2d', {willReadFrequently :true});
                

                if (pagexml == 'ComparePage') {
                    this.ThumbnailWidth = ThumbnailWidth;
                    this.ThumbnailHeight = ThumbnailHeight;
                    //set compare page values here.
                }
                else {
                    if (pagexml.getElementsByTagName("ThumbnailWidth")[0] != undefined) {
                        this.ThumbnailWidth = pagexml.getElementsByTagName("ThumbnailWidth")[0].firstChild.nodeValue;
                    }
                    else {
                        this.ThumbnailWidth = ThumbnailWidth;
                    }
                    if (pagexml.getElementsByTagName("ThumbnailWidth")[0] != undefined) {
                        this.ThumbnailHeight = pagexml.getElementsByTagName("ThumbnailHeight")[0].firstChild.nodeValue;
                    }
                    else {
                        this.ThumbnailHeight = ThumbnailHeight;
                    }
                }
                this.printcanvas = document.createElement('canvas');
                this.printctx = this.printcanvas.getContext('2d');
                this.print3Dimage = document.createElement('img');

                this.pdfprintcanvas = document.createElement('canvas');
                this.pdfprintctx = this.pdfprintcanvas.getContext('2d');


                this.printobj = {
                    pagenumber: thispage.pagenumber,
                    paperimage: thispage.printcanvas,
                    paperwidth: 297,
                    paperheight: 210,
                    docwidth: 1,
                    docheight: 1,
                    inchtomm: 25.4,
                    DPI: 300,
                    scrDPI: 96,
                    scaleSet: false,
                    pdx: 0,
                    pdy: 0,
                    pscale: 1,
                    printctx: thispage.printctx,
                    setRes: function (dpi) {
                        this.DPI = dpi;
                    },
                    setPaperSize: function (width, height) {
                        this.paperwidth = width;
                        this.paperheight = height;
                        this.paperimage.width = (width / this.inchtomm) * this.DPI;
                        this.paperimage.height = (height / this.inchtomm) * this.DPI;
                    },
                    setDocSize: function (width, height) {
                        this.docwidth = width;
                        this.docheight = height;
                    },
                    setScale: function () {
                        var xscale = this.paperimage.width / this.docwidth; //thispage.MainImageWidth;
                        var yscale = this.paperimage.height / this.docheight; // thispage.MainImageHeight;
                        this.pscale = Math.min(xscale, yscale);
                        this.pdx = (this.paperimage.width - (this.docwidth * this.pscale)) / 2;
                        this.pdy = (this.paperimage.height - (this.docheight * this.pscale)) / 2;
                        this.scaleSet = true;
                    },
                    print: function () {
                        if (!this.scaleSet) {
                            return;
                        }
                        if (thispage.usepdfjs) {
                            //thispage.renderPDFpagePrint();
                            thispage.printPDFpage();

                        }
                        else if (thispage.usevectorxml) {
                            thispage.draw_vectorPrint();
                        }
                        else if (thispage.usevector3Dxml) {
                            //get imageurl.
                            thispage.print3Dimage.addEventListener('load', thispage.load3Dimage, false);
                            thispage.print3Dimage.src = renderer.domElement.toDataURL();
                        }
                        else {
                            thispage.draw_imagePrint();
                        }
                    }
                };
                //this.thumbnailobj.image.addEventListener('load',this.thumbload,false);
                //this.thumbnailobj.image.src = this.thumbnailobj.source;
                var bisLastPart = false;
                var nPartfilesLoaded = 0;
                this.setpagedimsize = function (sx, sy, ex, ey, babs) {
                    if (babs) {
                        thispage.startx = sx;
                        thispage.starty = sy;
                        thispage.endx = ex;
                        thispage.endy = ey;
                    }
                    else {
                        thispage.startx = sx;
                        thispage.starty = sy;
                        thispage.endx = ex + sx;
                        thispage.endy = ey + sy;
                    }
                    //x,y, w,h
                    //thispage.DocRef.thumbnails.

                    if(debugmarkuprect){
                        console.log(thispage.startx, thispage.starty, thispage.endx, thispage.endy);
                    }


                    if(thispage.DocRef.thumbnails[thispage.pagenumber]){
                        //thispage.DocRef.thumbnails[thispage.pagenumber].birdseyerect({ x: thispage.startx, y: thispage.starty, w: thispage.endx, h: thispage.endy });
                        thispage.DocRef.thumbnails[thispage.pagenumber].birdseyerect({ x: thispage.startx, y: thispage.starty, w: ex, h: ey });
                    }

                };
                this.getwidth = function () {
                    return thispage.offscreenwidth;
                };
                this.getheight = function () {
                    return thispage.offscreenheight;
                };
                this.draw_tile_background = function (ctx, dx, dy, dscalepdf) {
                    thispage.draw_tiled_cavas(ctx, dx, dy, 1);
                };
                this.draw_tile_overlay = function (ctx, dx, dy, dscalepdf) {
                    thispage.draw_tiled_cavas(ctx, dx, dy, 1);
                };
                this.transparentpdfcompare = function (binvert) {
                    var error = 0;
                    for (var i = 0; i < thispage.canvtileArray.length; i++) {
                        for (var j = 0; j < thispage.canvtileArray[i].length; j++) {
                            try {
                                var imageData = thispage.canvtileArray[i][j].ctx.getImageData(0, 0, thispage.canvtileArray[i][j].cnv.width, thispage.canvtileArray[i][j].cnv.height);
                            }
                            catch (e) {
                                alert("Error 1 - " + e);
                                console.log(e, thispage.canvtileArray[i][j].cnv.width, thispage.canvtileArray[i][j].cnv.height);
                                error = -1;
                            }
                            if (binvert) {
                                thispage.setinvert(imageData.data);
                            }
                            else {
                                thispage.transparent(imageData.data);
                            }
                            if (error == 0) {
                                thispage.canvtileArray[i][j].ctx.putImageData(imageData, 0, 0);
                            }
                        }
                    }
                };
                this.invertpdfcompare = function (binvert) {
                    var error = 0;
                    for (var i = 0; i < thispage.canvtileArray.length; i++) {
                        for (var j = 0; j < thispage.canvtileArray[i].length; j++) {
                            try {
                                var imageData = thispage.canvtileArray[i][j].ctx.getImageData(0, 0, thispage.canvtileArray[i][j].cnv.width, thispage.canvtileArray[i][j].cnv.height);
                            }
                            catch (e) {
                                alert("Error 1 - " + e);
                                console.log(e, thispage.canvtileArray[i][j].cnv.width, thispage.canvtileArray[i][j].cnv.height);
                                error = -1;
                            }
                            //this.role = 0;
                            // 0 - no role.
                            // 1 - backgroundpage.
                            // 2 - overlaypage.
                            var cmprcolors = thispage.compositereference.getColors();
                            if (binvert) {
                                thispage.setinvert(imageData.data);
                            }
                            else {
                                switch (thispage.role) {
                                    case 1:
                                        thispage.setcolor(imageData.data, cmprcolors.bg);
                                        break;
                                    case 2:
                                        thispage.setcolor(imageData.data, cmprcolors.fg);
                                        break;
                                }
                            }
                            if (error == 0) {
                                thispage.canvtileArray[i][j].ctx.putImageData(imageData, 0, 0);
                            }
                        }
                    }
                };
                this.copyCanvasTiles = function (tileArray) {
                    for (var rc = 0; rc < tileArray.length; rc++) {
                        thispage.canvtileArray.push([]);
                        for (var cc = 0; cc < tileArray[rc].length; cc++) {
                            var tcanvas = document.createElement('canvas');
                            var tctx = tcanvas.getContext('2d');
                            var canvobj = {
                                cnv: tcanvas,
                                ctx: tctx,
                                tilenumber: 0
                            };
                            canvobj.cnv.height = tileArray[rc][cc].cnv.height;
                            canvobj.cnv.width = tileArray[rc][cc].cnv.width;
                            canvobj.tilenumber = tileArray[rc][cc].tilenumber;
                            canvobj.ctx.drawImage(tileArray[rc][cc].cnv, 0, 0);
                            thispage.canvtileArray[rc].push(canvobj);
                        }
                    }
                };
                this.gettilesize = function () {
                    var totalwidth = 0;
                    var totalheight = 0;
                    for (var i = 0; i < thispage.canvtileArray.length; i++) {
                        for (var j = 0; j < thispage.canvtileArray[i].length; j++) {
                            totalwidth += thispage.canvtileArray[i][j].cnv.width;
                            totalheight += thispage.canvtileArray[i][j].cnv.height;
                        }
                    }
                    thispage.setpagesize(totalwidth, totalheight);
                };
                this.scaleCompare = function (scale) {


                    if(thispage.checkFoxitMaxZoom(scale)){
                        thispage.DocRef.foxitdoc.getBitmap(thispage.pagenumber, scale, thispage.setPageBitmap);
                    }

                    
                };
                this.initCompare = function () {
                    //thispage.bIsPDF
                    //thispage.role = thispage.pagenumber + 1;
                    thispage.canvtileArray = [];
                    if (thispage.role == 1) {
                        //thispage.copyCanvasTiles(thispage.compositereference.pages[0].canvtileArray);
                        //thispage.gettilesize();
                        thispage.pdfpagewidth = thispage.compositereference.pages[0].pdfpagewidth;
                        thispage.pdfpageheight = thispage.compositereference.pages[0].pdfpageheight;
                        thispage.bgpagewidth = thispage.compositereference.pages[0].pdfpagewidth;
                        thispage.bgpageheight = thispage.compositereference.pages[0].pdfpageheight;
                        thispage.backgroundScale = thispage.compositereference.pages[0].getdscale();
                        thispage.backgroundOffsetX = thispage.compositereference.pages[0].getdx();
                        thispage.backgroundOffsetY = thispage.compositereference.pages[0].getdy();
                        thispage.offscreenwidth = thispage.compositereference.pages[0].offscreenwidth;
                        thispage.offscreenheight = thispage.compositereference.pages[0].offscreenheight;
                        thispage.dxpdf = thispage.backgroundOffsetX;
                        thispage.dypdf = thispage.backgroundOffsetY;
                        thispage.DocRef.foxitdoc.getBitmap(thispage.pagenumber, thispage.backgroundScale, thispage.setPageBitmap);
                    }
                    else if (thispage.role == 2) {
                        //thispage.copyCanvasTiles(thispage.compositereference.pages[1].canvtileArray);
                        //thispage.gettilesize();
                        thispage.pdfpagewidth = thispage.compositereference.pages[1].pdfpagewidth;
                        thispage.pdfpageheight = thispage.compositereference.pages[1].pdfpageheight;
                        thispage.ovpageheight = thispage.compositereference.pages[1].pdfpageheight;
                        thispage.ovpagewidth = thispage.compositereference.pages[1].pdfpagewidth;
                        thispage.overlayScale = thispage.compositereference.pages[1].getdscale();
                        thispage.overlayOffsetX = thispage.compositereference.pages[1].getdx();
                        thispage.overlayOffsetY = thispage.compositereference.pages[1].getdy();
                        thispage.offscreenwidth = thispage.compositereference.pages[1].offscreenwidth;
                        thispage.offscreenheight = thispage.compositereference.pages[1].offscreenheight;
                        thispage.dxpdf = thispage.overlayOffsetX;
                        thispage.dypdf = thispage.overlayOffsetY;
                        thispage.DocRef.foxitdoc.getBitmap(thispage.pagenumber, thispage.overlayScale, thispage.setPageBitmap);
                    }
                    if (thispage.compositereference.compareMode == 0) {
                        thispage.invertpdfcompare(false);
                    }
                    else if (thispage.compositereference.compareMode == 1) {
                        thispage.transparentpdfcompare(false);
                    }
                };
                this.movepage = function (pagenum) {
                    
                    thispage.pagenumber = pagenum;
                    this.dxpdf = 0.0;
                    this.dypdf = 0.0;
                    this.foxitdx = 0.0;
                    this.foxitdy = 0.0;
                    this.dscalepdf = 1.0;
                    this.dscalebackup = 1.0;
                    this.pdfpageref = null;
                    this.renderTask = null;
                    this.rendercancel = false;
                    this.magnifyrestscale = 1.0;
                    this.dscalepdfabs = 1.0;
                    this.dxbackup = 0.0;
                    this.dybackup = 0.0;
                    this.resizepan = false;
                    this.curpagescale = 1.0;
                    this.curfoxitpagescale = 1.0;
                    this.curpagescalebackup = 1.0;
                    this.pdfpageheight = 0.0;
                    this.pdfpagewidth = 0.0;
                    this.bMaxzoom = false;
                    this.bMinzoom = false;
                    this.nPDFMaxFactor = 4.0;
                    this.pdfisrendered = false;
                    this.textareaset = false;
                    this.pdfrenderopque = 0;
                    this.pdfisfirstrendered = false;
                    this.iscollapsed = true;
                    this.offscreenwidth = 0.0;
                    this.offscreenheight = 0.0;
                    this.pdfstartrender = -1;
                    this.pdfendrender = -1;
                    this.pdfpagetime = -1;
                    this.rxdrawtimestart = 0.0;
                    this.rxdrawtimeend = 0.0;
                    this.scrollzoomtime = 0.0;
                    this.scrollzoominterval = 0.0;
                    this.scrollzoomarray = [];
                    this.debounce_timer = undefined;
                    this.vectordebounce_timer = undefined;
                    this.bautoloadthumbnails = bAutoloadThumbnails;
                    this.pdftimervar = null;
                    this.PDFTextArea = undefined;
                    this.PDFPointArray = [];
                    this.PDFsnapTransform = null;

                    this.textselectobj = {
                        rectarray : undefined,
                        start : -1,
                        end : -1,
                        bstart : false,
                        bend : false,
                        startpos : null,
                        endpos : null,
                        $handler : null,
                        complete : false
                    };

                    this.bpointlistSet = false;
                    this.PDFinternalScale = 1.0;
                    this.PDFpageRotate = 0;
                    this.pdfSnapScale = 1.0;
                    this.TextSelectArea = undefined;
                    this.snapEnabled = false;
                    this.bDorescaleonsizeChange = bDoReScaleOnSize;
                    this.width = 0;
                    this.height = 0;
                    //universal page original widht and height
                    this.originalwidth = 0;
                    this.originalheight = 0;
                    this.OriginalScale = 1;
                    //this.dscalevector = 1.0;
                    this.initialzoom = 1;
                    this.visible = false;
                    //small image scale and offset
                    this.startx = 0.0;
                    this.starty = 0.0;
                    this.endx = 0.0;
                    this.endy = 0.0;
                    
                    if(this.DocRef.pagerotations[thispage.pagenumber] != undefined){
                        this.drotation = this.DocRef.pagerotations[thispage.pagenumber];
                    }
                    
                    this.pagelockrotation = 0;
                    this.textsearchindx = 0;
                    this.drawtextSearchRect = false;
                    this.pageloaded = false;
                    this.pageloading = false;
                    this.pdfdiminitset = false;
                    if (this.usepdfjs) {
                        this.visibleTiles = [];
                        this.bautoloadthumbnails = false;
                        this.tilespending = false;
                        this.prioritytilespending = false;
                        this.canvtileArray = [];
                        this.bgCanvtileArray = [];
                        //this.tempcanvtileArray = [];
                        this.annotArray = [];
                        this.tempfoxitpage = {
                            canvases: [],
                            imagedim: { width: 0, height: 0 },
                            havecanvases: false
                        };
                        this.busetempcanvases = false;
                        //this.magnifycanvas = document.createElement('canvas');
                        //this.magnifypagectx = this.magnifycanvas.getContext('2d');
                        this.pageRendering = false;
                        this.pagePrinting = false;
                        this.pageNumPending = null;
                        this.pagescale = 1.5;
                        this.markupscaleadjust = 1.0;
                        this.pdfdxtemp = 0.0;
                        this.pdfdytemp = 0.0;
                        this.pdfdscaletemp = 1.0;
                        this.PDFTextArea = document.createElement('div');
                        this.PDFTextArea.className = "textLayer";
                        this.PDFTextArea.addEventListener('wheel', ev_canvas, true);
                        this.pdfpagetime = 10;
                        this.viewport = null;
                        this.magviewport = null;
                        this.compareviewport = null;

                        if (thispage.DocRef.foxitdoc != null) {
                            
                            thispage.DocRef.foxitdoc.getpageinfodim(thispage.pagenumber, thispage.setfoxitPageSizeNonRender);

                            //returnobj.pagepos = pagepos;
                            //returnobj.pgscale = pgscale;
  
                            //thispage.foxitcalibratepagemarkupEx(pgobj.pagepos, pgobj.pgscale);
                            //thispage.DocRef.foxitdoc.setnotrendered(thispage.pagenumber);
                        }
    

                        //call foxit to get page dim here,
                    }
                };
                this.setNotCollapsedArray = function () {
                    if (thispage.DocRef.isNotCollapsed && thispage.DocRef.isNotCollapsed.length) {
                        if (thispage.iscollapsed) {
                            var isNotCollapsedIndex = thispage.DocRef.isNotCollapsed.indexOf(thispage.pagenumber);
                            if (isNotCollapsedIndex !== -1) {
                                thispage.DocRef.isNotCollapsed.splice(isNotCollapsedIndex, 1);
                            }
                        }
                        else {
                            if (thispage.DocRef.isNotCollapsed.indexOf(thispage.pagenumber) === -1) {
                                thispage.DocRef.isNotCollapsed.push(thispage.pagenumber);
                            }
                        }
                    }
                    else {
                        if (!thispage.iscollapsed) {
                            thispage.DocRef.isNotCollapsed = [thispage.pagenumber];
                        }
                    }
                };
                /*this.createTempcanvArray = function(foxitpage){
                    
                    thispage.tempcanvtileArray = [];
        
                    if (thispage.tempcanvtileArray.length == 0){
                        thispage.tempcanvtileArray = thispage.setCanvasTiles(thispage.tempcanvtileArray, foxitpage);
                    }
        
                };*/
                this.setCanvasTiles = function (tileArray, foxitpage) {
                    //thispage.canvtileArray = [];
                    /*for (var indxc = 0; indxc < foxitpage.indexarray.length;indxc++){
                       //var tile = {row : parseInt(szdataset[0]), column : parseInt(szdataset[1])};
                        var columns = foxitpage.indexarray[indxc].column;
                        var rows = foxitpage.indexarray[indxc].row;
                    }*/
                    var rows = foxitpage.rows;
                    var columns = foxitpage.columns;
                    //rows += 1;
                    //columns += 1;
                    for (var rc = 0; rc < rows; rc++) {
                        tileArray.push([]);
                        for (var cc = 0; cc < columns; cc++) {
                            /*var tcanvas = document.createElement('canvas');
                            var tctx = tcanvas.getContext('2d');
                            var canvobj = {
                                cnv : tcanvas,
                                ctx : tctx,
                                tilenumber : 0
                            }*/
                            tileArray[rc].push({
                                cnv: document.createElement('canvas'),
                                ctx: null,
                                tilenumber: 0,
                                visible: false,
                                xoffset: 0,
                                yoffset: 0
                            });
                            tileArray[rc][cc].ctx = tileArray[rc][cc].cnv.getContext('2d');
                        }
                        /*if(rc > 0 && cc > 0 && canvasrow.length > 0){
                            thispage.canvtileArray.push(canvasrow);
                        }*/
                    }
                    return tileArray;
                };
                this.sharpencomplete = function () {
                    //
                };
                this.annotComplete = function () {
                    thispage.DocRef.getpageloadSearch(thispage.pagenumber);
                };
                this.canvasArraySizeSet = function (imagedim, foxitpage) {
                    //thispage.setfoxitpagedim(foxitpage.width, foxitpage.height, imagedim,foxitpage.pagescale);

                    //callback({width : totalwidth, height : totalheight}, foxitpage);
                    imagedim.width = foxitpage.pagepos.width;
                    imagedim.height = foxitpage.pagepos.height;
                    thispage.prioritytilespending = true;
                    thispage.tilespending = true;
                    //drawpriorityTiles(thispage.drawpriorityTileComplete, foxitpage, thispage, imagedim);
                    //thispage.setfoxitpagedim(foxitpage.width, foxitpage.height, imagedim, foxitpage.pagescale);
                    //console.log(thispage.pagenumber);

                    //thispage.DocRef

                    /*if(thispage.DocRef.foxitsearchobjrect.active){
                        thispage.DocRef.foxitsearchobjrect.setpagerect(foxitpage.pageindex);   
                        //foxitpage
                    }*/

                    /*if(thisdocument.foxitsearchobjrect.active){
                        thisdocument.foxitsearchobjrect.setpagerect(foxitpageobj.pageindex);   
                    }*/


                };
                this.tilesCreated = function (tileArray, foxitpage) {
                    thispage.canvtileArray = tileArray;
                    foxitcreatecanvasArray(thispage.canvasArraySizeSet, foxitpage, thispage.canvtileArray);
                    //foxitcreatecanvasArray(thispage.canvasArraySizeSet,  foxitpage, thispage.canvtileArray);
                    //var imagedim = thispage.foxitcreatecanvasArray(foxitpage, thispage.canvtileArray);
                };
                this.drawpriorityTileComplete = function (drawntiles, foxitpage, imagedim) {
                    //callback({dtiles : drawntiles, ndtilesnum : nondrawtilecount}, foxitpage);
                    if (foxitpage.scrollupdate) {
                        for (var dti = 0; dti < drawntiles.length; dti++) {
                            //drawntiles.push({r : i, c : j});
                            thispage.canvtileArray[drawntiles[dti].r][drawntiles[dti].c] = thispage.bgCanvtileArray[drawntiles[dti].r][drawntiles[dti].c];
                        }
                    }
                    thispage.setfoxitpagedim(foxitpage.width, foxitpage.height, imagedim, foxitpage.pagescale);
                    sharpencanvastiles(thispage.sharpencomplete, thispage.canvtileArray, foxitpage.transScale);
                    if (thispage.compositereference && thispage.role != 0) {
                        if (thispage.compositereference.compareMode == 0) {
                            thispage.invertpdfcompare(false);
                        }
                        else if (thispage.compositereference.compareMode == 1) {
                            thispage.transparentpdfcompare(false);
                        }
                        thispage.compositereference.canvasdrawn(thispage.role);
                        if (thispage.compositereference.canvasdrawready) {
                            thispage.compositereference.draw_compare(true);
                        }
                    }
                    else {
                        thispage.DocRef.draw_mpagepdf(false);
                    }
                    if (drawntiles.ndtilesnum > 0) {
                        //return {dtiles : drawntiles, ndtilesnum : nondrawtilecount};
                        //thispage.foxitdrawcanvasArray(drawntiles, canvases, transScale);
                        drawTiles(thispage.drawTilesComplete, drawntiles, foxitpage, thispage);
                    }
                    //thispage.foxitcreateAnnotArray(foxitpage);
                    foxitcreateAnnotArray(thispage.annotComplete, foxitpage, thispage);
                };

                this.drawTilesComplete = function (foxitpage) {
                    if (foxitpage.scrollupdate) {
                        for (var i = 0; i < thispage.bgCanvtileArray.length; i++)
                            thispage.canvtileArray[i] = thispage.bgCanvtileArray[i].slice();
                    }
                    sharpencanvastiles(thispage.sharpencomplete, thispage.canvtileArray, foxitpage.transScale);
                    //thispage.canvtileArray[drawntiles[dti].r][drawntiles[dti].c] = thispage.bgCanvtileArray[drawntiles[dti].r][drawntiles[dti].c];
                    /*if (thispage.usedincomposite && thispage.compositereference != undefined) {
                    }
                    else {
                        
                    }*/
                    thispage.DocRef.draw_mpagepdf(false);
                };


                this.foxitpagescalecheck = function(pagescale){

                    
                    var scalefactor = thispage.curpagescale * PixelToPoint;
                    
                    var fromrender = pagescale.toFixed(4);
                    var frompage = scalefactor.toFixed(4);

                    if(fromrender != frompage){
                        //console.log("page scale render", fromrender);
                        //console.log("current page scale", frompage);

                        thispage.curpagescale = pagescale / PixelToPoint;

                        foxitredrawmarkup();

                        //redrawMarkup();


                        //console.log("new page scale", thispage.curpagescale);


                    }


                };


                this.foxitonScrollupdate = function (pagepos) {

                    if (pagepos) {
                        
                        
                        
                        thispage.setpagedimfoxit(pagepos);

                        if (pagepos.x != undefined && pagepos.y != undefined){
                            //thispage.setpagedimsize(pagepos.x, pagepos.y, pagepos.width, pagepos.height, true);
                            thispage.setpagedimsize(pagepos.x, pagepos.y, pagepos.width, pagepos.height, false);

                        }else if(pagepos.left != undefined && pagepos.top != undefined){
                            //thispage.setpagedimsize(pagepos.left, pagepos.top, pagepos.width, pagepos.height, true);
                            thispage.setpagedimsize(pagepos.left, pagepos.top, pagepos.width, pagepos.height, false);
                        }

                        thispage.setpagerotationdimfoxit(pagepos);
                        
                    }

                };
                function setFoxitpageRect(pagepos){

                    thispage.pdfstartx = pagepos.x;
                    thispage.pdfstarty = pagepos.y;

                    thispage.pdfendx = pagepos.width + thispage.pdfstartx;
                    thispage.pdfendy = pagepos.height + thispage.pdfstarty;


                     thispage.starty = pagepos.y;
                     thispage.startx = pagepos.x;

                     thispage.endx =  pagepos.width + pagepos.x;
                     thispage.endy = pagepos.height + pagepos.y;

                     if(debugmarkuprect){
                        console.log(thispage.startx, thispage.starty, thispage.endx, thispage.endy);
                    }


                    //setpdfrect({x : thispage.foxitdx, y : thispage.foxitdy, w : pagepos.width + thispage.foxitdx, h : pagepos.height + thispage.foxitdy});

                    //this.endx = (thispage.VectorPageObj.width * this.dscalevector) + this.startx;
                    //this.endy = (thispage.VectorPageObj.height * this.dscalevector) + this.starty;


                    //var origw = origx + pagepos.width;
                    //var origh = origy + pagepos.height;

                    //thispage.foxitdx=pagepos.x;
                    //thispage.foxitdy=pagepos.y;

                }                

                function getrotrect(rect, angle){
                    var origx = rect.x;
                    var origy = rect.y;

                    var origw = origx + rect.w;
                    var origh = origy + rect.h;

                    var CanvRotRad = angle * (Math.PI / 180);
                    var cosangle = Math.cos(CanvRotRad);
                    var sinangle = Math.sin(CanvRotRad);

                    var centercanvX = (canvasowidth / 2);
                    var centercanvY = (canvasoheight / 2);
                    
                    var hw = origx - centercanvX;
                    var hh = origy - centercanvY;

                    var newx = (hw * cosangle) - (hh * sinangle);
                    var newy = (hw * sinangle) + (hh * cosangle);

                    newx = centercanvX + newx;
                    newy = centercanvY + newy;
                    hw = origw - centercanvX;
                    hh = origh - centercanvY;
                    var neww = (hw * cosangle) - (hh * sinangle);
                    var newh = (hw * sinangle) + (hh * cosangle);
                    neww = centercanvX + neww;
                    newh = centercanvY + newh;
                    
                    return {
                        x: Math.min(newx, neww),
                        y: Math.min(newy, newh),
                        w: Math.max(neww, newx),
                        h: Math.max(newh, newy)
                    };

                    
                }


                function getfoxitrotrect(pagepos, angle){
                    var origx = pagepos.x;
                    var origy = pagepos.y;

                      //thispage.dypdf = thispage.foxitdy - pagepos.height;
                      //thispage.dxpdf = thispage.foxitdx - pagepos.width;

                    var origw = origx + pagepos.width;
                    var origh = origy + pagepos.height;
                    var CanvRotRad = angle * (Math.PI / 180);
                    var cosangle = Math.cos(CanvRotRad);
                    var sinangle = Math.sin(CanvRotRad);
                    var centercanvX = (canvasowidth / 2);
                    var centercanvY = (canvasoheight / 2);
                    var hw = origx - centercanvX;
                    var hh = origy - centercanvY;
                    var newx = (hw * cosangle) - (hh * sinangle);
                    var newy = (hw * sinangle) + (hh * cosangle);
                    newx = centercanvX + newx;
                    newy = centercanvY + newy;
                    hw = origw - centercanvX;
                    hh = origh - centercanvY;
                    var neww = (hw * cosangle) - (hh * sinangle);
                    var newh = (hw * sinangle) + (hh * cosangle);
                    neww = centercanvX + neww;
                    newh = centercanvY + newh;
                    return {
                        x: Math.min(newx, neww),
                        y: Math.min(newy, newh),
                        w: Math.max(neww, newx),
                        h: Math.max(newh, newy)
                    };

                }

                /*this.foxitrotrect = function(pagepos, angle){
                };*/

                function setpdfrect(rotrect){

                    /*if (this.usedincomposite && this.compositereference != undefined) {
                        return;
                    }*/


                    thispage.dypdf = rotrect.y;
                    thispage.dxpdf = rotrect.x;

                    thispage.starty = rotrect.y;
                    thispage.startx = rotrect.x;

                    //thispage.endx = rotrect.w - thispage.startx;
                    //thispage.endy = rotrect.h - thispage.starty;

                    thispage.endx = rotrect.w;
                    thispage.endy = rotrect.h;


                    //var rotrect = { x1: this.startx, y1: this.starty, x2: this.endx, y2: this.endy };
                    //thispage.pdfendx = pagepos.width + thispage.pdfstartx;
                    //thispage.pdfendy = pagepos.height + thispage.pdfstarty;

                    if(debugmarkuprect){
                        console.log(thispage.startx, thispage.starty, thispage.endx, thispage.endy);
                    }


                }
                
                function setpdfdxdyzeroRot(rotrect){

                    thispage.dxpdfzerorot = rotrect.x;
                    thispage.dypdfzerorot = rotrect.y;

                }


                this.setpagerotationdimfoxitrot = function(pagepos){


                    var realangle = getcombinedangle(thispage.getpageRotation(), thispage.foxitpageorigrot);
                   

                    switch (realangle) {
                        case 0:
                            //thispage.dypdf = thispage.foxitdy;
                            //thispage.dxpdf = thispage.foxitdx;

                            thispage.dxpdfzerorot = thispage.foxitdx;
                            thispage.dypdfzerorot = thispage.foxitdy;
            

                        break;

                        case 90:
                            
                           var rotrect = getfoxitrotrect(pagepos, 270);
                           setpdfdxdyzeroRot(rotrect);      

                        break;

                        case 180:
                            
                            rotrect = getfoxitrotrect(pagepos, 180);
                            setpdfdxdyzeroRot(rotrect);       

                        break;

                        case 270:

                            rotrect = getfoxitrotrect(pagepos, 90);

                            setpdfdxdyzeroRot(rotrect);

                        break;

                    }

                    


                };

                this.setpagerotationdimfoxit = function(pagepos){

                    /*if (this.usedincomposite && this.compositereference != undefined) {
                        return;
                    }*/

                    switch (thispage.getpageRotation()) {
                        case 0:
                            thispage.dypdf = thispage.foxitdy;
                            thispage.dxpdf = thispage.foxitdx;


                            

                        break;

                        case 90:

                            //thispage.dypdf = thispage.foxitdx;
                            //thispage.dxpdf = thispage.foxitdy;

                            var rotrect = getfoxitrotrect(pagepos, 270);
                            //console.log(rotrect);
                            
                            //thispage.dypdf = rotrect.y;
                            //thispage.dxpdf = rotrect.x;
                            setpdfrect(rotrect);

                        break;

                        case 180:
                            //thispage.dypdf = thispage.foxitdy;
                            //thispage.dxpdf = thispage.foxitdx;

                            rotrect = getfoxitrotrect(pagepos, 180);
                            //console.log(rotrect);

                            
                            //thispage.dypdf = rotrect.y;
                            //thispage.dxpdf = rotrect.x;

                            setpdfrect(rotrect);

                        break;

                        case 270:

                            //thispage.dypdf = thispage.foxitdx;
                            //thispage.dypdf = thispage.foxitdx;
                            //thispage.dxpdf = thispage.foxitdy;
                            //thispage.dxpdf = -thispage.foxitdy;

                            rotrect = getfoxitrotrect(pagepos, 90);
                            //console.log(rotrect);
                            setpdfrect(rotrect);
                            
                            //thispage.dypdf = rotrect.y;
                            //thispage.dxpdf = rotrect.x;

                            
                            //thispage.dxpdf =  -thispage.foxitdy + (pagepos.height * 0.5);

                        break;

                    }

                    

                    thispage.setpagerotationdimfoxitrot(pagepos);

                    

                };

                this.setpagedimfoxit = function(pagepos){
                    if (pagepos) {

                        if (pagepos.x != undefined && pagepos.y != undefined){
                            thispage.foxitdx=pagepos.x;
                            thispage.foxitdy=pagepos.y;
                        }else if(pagepos.left != undefined && pagepos.top != undefined){
                            thispage.foxitdx=pagepos.left;
                            thispage.foxitdy=pagepos.top;
                        }

                        if(thispage.getpageRotation() != 0){
                            setFoxitpageRect(pagepos);
                        }

                        thispage.dypdfnonrot = thispage.foxitdy;
                        thispage.dxpdfnonrot = thispage.foxitdx;


                        
                        thispage.setpagerotationdimfoxit(pagepos);






                        //foxit rotation test
                        //console.log('setpagedimfoxit', thispage.dxpdf, thispage.dypdf);
                    }
                }

                this.setpagedimdirect = function(pagepos){
                    if (pagepos) {

                        if (pagepos.x != undefined && pagepos.y != undefined){
                            thispage.foxitdx=pagepos.x;
                            thispage.foxitdy=pagepos.y;
                        }else if(pagepos.left != undefined && pagepos.top != undefined){
                            thispage.foxitdx=pagepos.left;
                            thispage.foxitdy=pagepos.top;
                        }

                        if(thispage.getpageRotation() != 0){
                            setFoxitpageRect(pagepos);
                        }

                        thispage.dypdfnonrot = thispage.foxitdy;
                        thispage.dxpdfnonrot = thispage.foxitdx;



                        thispage.setpagerotationdimfoxit(pagepos);

                        
                        //pagepos.width: 1434
                        //pagepos.height: 2029.28125
                        foxitredrawmarkup();
                    }
                };

                this.foxitcalibratepagemarkupEx = function (pagepos, scale){
                    if (pagepos) {

                        if(scale && (thispage.offscreenwidth != 0) && (thispage.pdfpagewidth != 0)){

                            //var pgscalefactor = (thispage.offscreenwidth / thispage.pdfpagewidth);

                            var pgscalefactor = thispage.getPDFpageScale();

                            var scalefactor = pgscalefactor * PixelToPoint;
                            
                            if(scale == scalefactor * thispage.dscalepdf){

                            }else{
                                //dscalepdf modified
                                thispage.dscalepdf = scale / scalefactor;
                            }

                            //var pgscalefactor = (thispage.offscreenwidth / thispage.pdfpagewidth);
                            //var scalefactor = pgscalefactor * PixelToPoint;
                            //scalefactor *= scale;
                            //thispage.foxitdirectZoom(thispage.dscalepdf);
                            
                        }


                        if (pagepos.x != undefined && pagepos.y != undefined){
                            thispage.foxitdx=pagepos.x;
                            thispage.foxitdy=pagepos.y;
                        }else if(pagepos.left != undefined && pagepos.top != undefined){
                            thispage.foxitdx=pagepos.left;
                            thispage.foxitdy=pagepos.top;
                        }

                        //check converted scale against current scale here.
                        //if different make adjustments.

                        //var pageorigo = getrotatedorigo(thispage.foxitdx, thispage.foxitdy);

                        //thispage.dxpdf = pageorigo.x;
                        //thispage.dypdf = pageorigo.y;

                        if(thispage.getpageRotation() != 0){
                            setFoxitpageRect(pagepos);
                        }

                        thispage.dypdfnonrot = thispage.foxitdy;
                        thispage.dxpdfnonrot = thispage.foxitdx;

                        thispage.setpagerotationdimfoxit(pagepos);

                        

                        
                        

                        foxitredrawmarkup();



                        /*switch (thispage.getpageRotation()) {
                            case 0:

                            thispage.dypdf = thispage.foxitdy;
                            thispage.dxpdf = thispage.foxitdx;
                            break;

                            case 90:

                            thispage.dypdf = thispage.foxitdx;
                            thispage.dxpdf = thispage.foxitdy;

                            //this.dxpdf -= sy;
                            //this.dypdf += sx;

                            break;

                            case 180:

                            thispage.dypdf = thispage.foxitdy;
                            thispage.dxpdf = thispage.foxitdx;

                            case 270:

                            thispage.dypdf = thispage.foxitdx;
                            thispage.dxpdf = thispage.foxitdy;

                            break;
                                
                        }*/

                        //thispage.dypdf = thispage.foxitdy;
                        //thispage.dxpdf = thispage.foxitdx;



                        //pagepos.width: 1434
                        //pagepos.height: 2029.28125
                        //add a scale check using page widht and height


                        


                    }


                };

                function getrotatedorigo(x,y){

                    var centercanvX = (canvasowidth / 2);
                    var centercanvY = (canvasoheight / 2);
                    var CanvRotRad = thispage.getpageRotation() * (Math.PI / 180);
                    
                    var pageorigo = {x:x, y:y};

                    switch (thispage.getpageRotation()) {
                        case 0:
                            CanvRotRad = thispage.getpageRotation() * (Math.PI / 180);
                            break;
                        case 90:
                            CanvRotRad = (thispage.getpageRotation() + 180) * (Math.PI / 180);
                            break;
                        case 270:
                            CanvRotRad = (thispage.getpageRotation() - 180) * (Math.PI / 180);
                            break;
                        case 180:
                            CanvRotRad = thispage.getpageRotation() * (Math.PI / 180);
                            break;
                    }

                    if (thispage.getpageRotation() != 0) {
                        //pageorigo = thispage.getRotatedPoint(centercanvX, centercanvY, x ,y, CanvRotRad);

                        //x = rotmousepoint.x;
                        //y = rotmousepoint.y;
                    }

                    return pageorigo;

                }


                this.foxitcalibratepagemarkup = function (pagepos) {
                    if (pagepos) {

                        if (pagepos.x != undefined && pagepos.y != undefined){
                            thispage.foxitdx=pagepos.x;
                            thispage.foxitdy=pagepos.y;
                        }else if(pagepos.left != undefined && pagepos.top != undefined){
                            thispage.foxitdx=pagepos.left;
                            thispage.foxitdy=pagepos.top;
                        }

                        //thispage.dypdf = thispage.foxitdy;
                        //thispage.dxpdf = thispage.foxitdx;
                        //pagepos.width: 1434
                        //pagepos.height: 2029.28125
                        //add a scale check using page widht and height

                        if(thispage.getpageRotation() != 0){
                            setFoxitpageRect(pagepos);
                        }
                        
                        thispage.dypdfnonrot = thispage.foxitdy;
                        thispage.dxpdfnonrot = thispage.foxitdx;


                        thispage.setpagerotationdimfoxit(pagepos);



                        //var pageorigo = getrotatedorigo(thispage.foxitdx, thispage.foxitdy);

                        //thispage.dxpdf = pageorigo.x;
                        //thispage.dypdf = pageorigo.y;

                        
                        

                        foxitredrawmarkup();
                    }
                };
                this.foxitcalibratepagePosY = function () {
                    if (thispage.pagenumber == 0) {
                        thispage.dypdf = 0;
                        thispage.starty = 0;
                        thispage.endy = (thispage.offscreenheight * thispage.dscalepdf) + thispage.starty;
                    }
                    if (thispage.pagenumber == thispage.DocRef.pages.length - 1) {
                        thispage.endy = canvasoheight;
                        thispage.dypdf = thispage.endy - (thispage.offscreenheight * thispage.dscalepdf);
                        thispage.starty = thispage.dypdf;
                    }

                    //foxit rotation test
                    //console.log('foxitcalibratepagePosY', thispage.dxpdf, thispage.dypdf);

                };
                this.foxitcalibratepagePosX = function (bsetend) {

                    /*if (this.usedincomposite && this.compositereference != undefined) {
                        return;
                    }*/


                    if (bsetend) {
                        //center if smaller than width
                        if (thispage.offscreenwidth * thispage.dscalepdf < canvasowidth) {
                            thispage.dxpdf = (canvasowidth - (thispage.offscreenwidth * thispage.dscalepdf)) / 2;
                            thispage.startx = thispage.dxpdf;
                            thispage.endx = (thispage.offscreenwidth * thispage.dscalepdf) + thispage.startx;
                        }
                        else {
                            thispage.endx = canvasowidth;
                            thispage.dxpdf = thispage.endx - (thispage.offscreenwidth * thispage.dscalepdf);
                            thispage.startx = thispage.dxpdf;
                        }
                        //this.dxpdf = (canvasowidth - (pcanvwidth * this.dscalepdf)) / 2;
                        //this.dypdf = (canvasoheight - (pcanvheight * this.dscalepdf)) / 2;
                    }
                    else {
                        if (thispage.offscreenwidth * thispage.dscalepdf < canvasowidth) {
                            thispage.dxpdf = (canvasowidth - (thispage.offscreenwidth * thispage.dscalepdf)) / 2;
                            thispage.startx = thispage.dxpdf;
                            thispage.endx = (thispage.offscreenwidth * thispage.dscalepdf) + thispage.startx;
                        }
                        else {
                            thispage.dxpdf = 0;
                            thispage.startx = 0;
                            thispage.endx = (thispage.offscreenwidth * thispage.dscalepdf) + thispage.startx;
                        }
                    }

                    if(debugmarkuprect){
                        console.log(thispage.startx, thispage.starty, thispage.endx, thispage.endy);
                    }

                };

                this.setfoxitPageSizeEx = function (foxitpage) {
                    thispage.curfoxitpagescale = foxitpage.pagescale;
                    thispage.curfoxitpagescale = foxitpage.pagescale / PixelToPoint;
                    foxitpage.pagescale = foxitpage.pagescale / PixelToPoint;

                    thispage.setfoxitpagedimnew(foxitpage);

                };

                this.setfoxitPageSizeNonRender = function(foxitpage){

                    thispage.foxitpagerot = foxitpage.rotation;
                    thispage.foxitpageorigrot = foxitpage.originalrotation;

                    thispage.pdfpagewidth = foxitpage.width;
                    thispage.pdfpageheight = foxitpage.height;

                    var pgwidth = canvasowidth - 42;

                    switch (thispage.getpageRotation()) {
                        case 0:

                            thispage.originalPDFwidth = foxitpage.pdfwidth;
                            thispage.originalPDFheight = foxitpage.pdfheight;
        
    
                            break;

                        case 90:

                            thispage.originalPDFwidth = foxitpage.pdfheight;
                            thispage.originalPDFheight = foxitpage.pdfwidth;
        


                            break;

                        case 180:
                            thispage.originalPDFwidth = foxitpage.pdfwidth;
                            thispage.originalPDFheight = foxitpage.pdfheight;
        
                            
                            break;


                        case 270:
                            thispage.originalPDFwidth = foxitpage.pdfheight;
                            thispage.originalPDFheight = foxitpage.pdfwidth;
        
                            break;
                    }

                    //pdfwidth : PDFpagewidth,
                    //pdfheight : PDFpageheight,


                    var yscale = fixedScaleSize.height / thispage.pdfpageheight; //thispage.MainImageHeight;
                    var xscale = fixedScaleSize.width / thispage.pdfpagewidth; // thispage.MainImageWidth;
                    
                    
                    thispage.fixedScale = Math.min(xscale, yscale);
                    thispage.curpagescale = foxitpage.pagescale;

                    thispage.width = foxitpage.pagepos.width;
                    thispage.height = foxitpage.pagepos.height;


                    var wscale = pgwidth / foxitpage.pdfwidth;
                    //var hscale = thispage.height / foxitpage.pagepos.height;
                    thispage.pdfdscaletemp = wscale * PixelToPoint;//Math.min(wscale, hscale);


                    var pagewscaled = foxitpage.pagepos.width * (thispage.pdfdscaletemp / PixelToPoint);
                    var pagehscaled = foxitpage.pagepos.height * (thispage.pdfdscaletemp / PixelToPoint);

                    thispage.pdfdxtemp = (canvasowidth - pagewscaled) / 2;
                    thispage.pdfdxtemp = thispage.pdfdxtemp / 2;
                    //thispage.pdfdytemp = (canvasoheight - pagehscaled) / 2;
                    thispage.pdfdytemp = 8;


                    setmarkupdimensions(thispage);

                    if(thispage.DocRef.markupsave != undefined){
                        thispage.DocRef.markupsave.setpage(thispage);
                    }



                };

                this.setfoxitPageSize = function (foxitpage) {
                    thispage.canvtileArray = [];
                    thispage.curfoxitpagescale = foxitpage.pagescale;

                    //imagedim.width = foxitpage.pagepos.width;
                    //imagedim.height = foxitpage.pagepos.height;

                    thispage.setfoxitpagedimnew(foxitpage);


                    if(foxitpage.pagepos == undefined){
                        return;
                    }

                    //thispage.setfoxitpagedim(foxitpage.width, foxitpage.height, imagedim, foxitpage.pagescale);


                    var imagedim = {width : foxitpage.pagepos.width, height : foxitpage.pagepos.height};
                    
                    //callback({width : totalwidth, height : totalheight}, foxitpage);
                    //imagedim.width = foxitpage.pagepos.width;
                    //imagedim.height = foxitpage.pagepos.height;
                    //thispage.prioritytilespending = true;
                    //thispage.tilespending = true;


                    thispage.canvasArraySizeSet(imagedim, foxitpage);


                    if (thispage.pagenumber == thispage.DocRef.pages[thispage.DocRef.currentpage].pagenumber && thispage.rendercallback) {
                        if (Rxcore_GUI_PDFPageRenderComplete != undefined) {
                            //thispage.rendercallback = false;

                            Rxcore_GUI_PDFPageRenderComplete.RenderComplete(thispage.pagenumber);
                        }

                    }


                    //setCanvasTiles(thispage.tilesCreated, thispage.canvtileArray, foxitpage);


                    
                    //foxitcreatecanvasArray(thispage.canvasArraySizeSet,  foxitpage, thispage.canvtileArray);
                };
                this.createCanvasTiles = function (foxitpage) {
                    /*var foxitpage = {
                        pageindex : pgindex,
                        canvases : [],
                        transScale : transcale,
                        indexarray : [],
                        pagescale : pgscale,
                        width : pagewidth,
                        height : pageheight
                    };*/
                    //thispage.canvtileArray = [];
                    if (foxitpage.scaleupdate) {
                        thispage.canvtileArray = [];
                        thispage.bgCanvtileArray = [];
                    }
                    else if (foxitpage.scrollupdate) {
                        thispage.bgCanvtileArray = [];
                    }
                    thispage.curfoxitpagescale = foxitpage.pagescale;
                    /*if(thispage.curpagescale == foxitpage.pagescale){
                        //add missing canvases to existing array.
        
                    }*/
                    //thispage.rxdrawtimestart = new Date().getTime();
                    /*if (thispage.usedincomposite && thispage.compositereference != undefined) {
                        if (thispage.isbackground){
                            thispage.compositereference.backgroundgpage.createCanvasTiles(foxitpage);
        
                        }else if(thispage.isoverlay){
                            thispage.compositereference.overlaypage.createCanvasTiles(foxitpage);
                        }
                        return;
                    }*/
                    //this.tilesCreated
                    if (foxitpage.scaleupdate) {
                        setCanvasTiles(thispage.tilesCreated, thispage.canvtileArray, foxitpage);
                        //thispage.canvtileArray = thispage.setCanvasTiles(thispage.canvtileArray,foxitpage);
                        //var imagedim = thispage.foxitcreatecanvasArray(foxitpage, thispage.canvtileArray);
                        //thispage.bgCanvtileArray = thispage.setCanvasTiles(thispage.bgCanvtileArray,foxitpage);
                    }
                    if (foxitpage.scrollupdate) {
                        setCanvasTiles(thispage.tilesCreated, thispage.bgCanvtileArray, foxitpage, thispage.visibleTiles);
                        //thispage.visibleTiles
                        //thispage.bgCanvtileArray = thispage.setCanvasTiles(thispage.bgCanvtileArray,foxitpage);
                        //var imagedim = thispage.foxitcreatecanvasArray(foxitpage, thispage.bgCanvtileArray);
                    }
                    //
                    //width : pagewidth,
                    //height : pageheight
                    //thispage.tempcanvtileArray = [];    
                    /*if(thispage.tempfoxitpage.canvases.length == 0){
                        
                    }else{
                        var imagedim = thispage.foxitcreatecanvasArray(thispage.tempfoxitpage, thispage.canvtileArray, true);
                    }*/
                    //thispage.DocRef.rxfoxitpagedim(thispage.pagenumber, imagedim);
                    //thispage.setfoxitpagedim(foxitpage.width, foxitpage.height, imagedim,foxitpage.pagescale);
                    //thispage.prioritytilespending = true;
                    //thispage.tilespending = true;
                    //checktoDrawPrioritytiles(foxitpage.canvases, foxitpage.transScale);
                    //{dtiles : drawntiles, ndtilesnum : nondrawtilecount}
                    //var drawntiles = thispage.foxitdrawPrioritycanvasArray(foxitpage.canvases, foxitpage.transScale);
                    //drawpriorityTiles(thispage.drawpriorityTileComplete, foxitpage, thispage);
                    //thispage.drawpriorityTileComplete
                    /*if(thispage.tempfoxitpage.canvases.length == 0){
                    }else{
                        var drawntiles = thispage.foxitdrawPrioritycanvasArray(thispage.tempfoxitpage.canvases, foxitpage.transScale);
                    }*/
                    //var drawntiles = thispage.foxitdrawAllcanvasArray(foxitpage.canvases, foxitpage.transScale);
                    //thispage.tilespending = true;
                    /*if (drawntiles.length > 0 ){
                        
                        
                    }*/
                    //comment out for now until we get our multiple tiles back.
                    //thispage.DocRef.draw_mpagepdf();
                    //thispage.foxitdrawcanvasArray(drawntiles, foxitpage.canvases, foxitpage.transScale);
                    //thispage.DocRef.draw_mpagepdf();
                    //this.foxitdrawcanvasArray = function(drawntiles, canvases, transscale){
                    //thispage.renderpage(foxitpage):
                };
                this.setfoxitpagedimnew = function(foxitpage){

                    thispage.foxitpagerot = foxitpage.rotation;
                    thispage.foxitpageorigrot = foxitpage.originalrotation;

                    thispage.pdfpagewidth = foxitpage.width;
                    thispage.pdfpageheight = foxitpage.height;

                    thispage.originalwidth = foxitpage.pdfwidth;
                    thispage.originalheight = foxitpage.pdfheight;

                    thispage.printobj.setDocSize(thispage.originalwidth, thispage.originalheight);


                    switch (thispage.getpageRotation()) {
                        case 0:

                            thispage.originalPDFwidth = foxitpage.pdfwidth;
                            thispage.originalPDFheight = foxitpage.pdfheight;

                            thispage.bpagesizeupdated = true;
    
                            break;

                        case 90:

                            thispage.originalPDFwidth = foxitpage.pdfheight;
                            thispage.originalPDFheight = foxitpage.pdfwidth;
        
                            thispage.bpagesizeupdated = true;

                            break;

                        case 180:
                            thispage.originalPDFwidth = foxitpage.pdfwidth;
                            thispage.originalPDFheight = foxitpage.pdfheight;
        
                            thispage.bpagesizeupdated = true;
                            
                            break;


                        case 270:
                            thispage.originalPDFwidth = foxitpage.pdfheight;
                            thispage.originalPDFheight = foxitpage.pdfwidth;

                            thispage.bpagesizeupdated = true;
        
                            break;
                    }   

                    //pdfwidth : PDFpagewidth,
                    //pdfheight : PDFpageheight,

                    //thispage.originalPDFwidth = foxitpage.pdfwidth;
                    //thispage.originalPDFheight = foxitpage.pdfheight;

                    var yscale = fixedScaleSize.height / thispage.pdfpageheight; //thispage.MainImageHeight;
                    var xscale = fixedScaleSize.width / thispage.pdfpagewidth; // thispage.MainImageWidth;
                    
                    
                    thispage.fixedScale = Math.min(xscale, yscale);
                    thispage.curpagescale = foxitpage.pagescale;

                    if(foxitpage.pagepos != undefined){
                        thispage.setpagesize(foxitpage.pagepos.width, foxitpage.pagepos.height);
                    }
                    
                    //foxitpage.pagepos = pagecontainers[pgdc].getBoundingClientRect();
                    
                    
                    //imagedim.width = foxitpage.pagepos.width;
                    //imagedim.height = foxitpage.pagepos.height;

                    thispage.curpagescale = thispage.offscreenwidth / thispage.pdfpagewidth;

                    //console.log("page scale", thispage.curpagescale);

                    if(foxitpage.pagepos != undefined){
                        thispage.width = foxitpage.pagepos.width;
                        thispage.height = foxitpage.pagepos.height;
                    }

                    
                    thispage.pdfrenderopque -= 1;
                    if (thispage.pdfrenderopque <= 0) {
                        thispage.pdfrenderopque = 0;
                        thispage.pdfisrendered = true;
                    }

                    if(foxitpage.pagepos != undefined){
                        var wscale = thispage.width / foxitpage.pagepos.width;
                        var hscale = thispage.height / foxitpage.pagepos.height;
                        
                        thispage.pdfdscaletemp = Math.min(wscale, hscale);
                        thispage.pdfdxtemp = (canvasowidth - foxitpage.pagepos.width) / 2;
                        thispage.pdfdytemp = (canvasoheight - foxitpage.pagepos.height) / 2;
                        
                        thispage.pdfisrendered = true;
                        thispage.iscollapsed = false;
                        
                        thispage.pdfisfirstrendered = true;

                        thispage.setPDFdim(thispage.pdfdxtemp, 0, thispage.pdfdscaletemp);
    
                    }

                    
                    if(thispage.DocRef.foxitsearchobjrect.active){
                        thispage.DocRef.foxitsearchobjrect.setpagerect(thispage.pagenumber);   
                        //foxitpage
                    }

                    if(thispage.DocRef.foxitsearchobj.active){
                        thispage.DocRef.getpageloadSearch(thispage.pagenumber);
                    }
                    


                    if (thispage.drawtextSearchRect) {
                        thispage.drawsearchRect();
                        //thispage.showFoxitSearch();
                        thispage.drawtextSearchRect = false;

                    }

                    //thispage.setNotCollapsedArray();
                    
                    
                };
                this.setfoxitpagedim = function (pagewidth, pageheight, imagedim, pagescale) {
                    thispage.pdfpagewidth = pagewidth;
                    thispage.pdfpageheight = pageheight;
                    thispage.originalwidth = pagewidth;
                    thispage.originalheight = pageheight;
                    var yscale = fixedScaleSize.height / thispage.pdfpageheight; //thispage.MainImageHeight;
                    var xscale = fixedScaleSize.width / thispage.pdfpagewidth; // thispage.MainImageWidth;
                    thispage.fixedScale = Math.min(xscale, yscale);
                    thispage.curpagescale = pagescale;
                    //console.log('setpagesize', imagedim.width, imagedim.height);
                    thispage.setpagesize(imagedim.width, imagedim.height);
                    //console.log('setfoxitpagedim after setpagesize?');
                    thispage.curpagescale = thispage.offscreenwidth / thispage.pdfpagewidth;
                    thispage.width = imagedim.width;
                    thispage.height = imagedim.height;
                    thispage.pdfrenderopque -= 1;

                    if (thispage.pdfrenderopque <= 0) {
                        thispage.pdfrenderopque = 0;
                        thispage.pdfisrendered = true;
                    }

                    var wscale = thispage.width / imagedim.width;
                    var hscale = thispage.height / imagedim.height;
                    thispage.pdfdscaletemp = Math.min(wscale, hscale);
                    thispage.pdfdxtemp = (canvasowidth - imagedim.width) / 2;
                    thispage.pdfdytemp = (canvasoheight - imagedim.height) / 2;
                    thispage.pdfisrendered = true;
                    thispage.iscollapsed = false;
                    thispage.pdfisfirstrendered = true;
                    thispage.setPDFdim(thispage.pdfdxtemp, 0, thispage.pdfdscaletemp);
                    //thispage.setNotCollapsedArray();
                };
                this.foxitcreateAnnotArray = function (foxitpage) {
                    //annotobjs.push({pos : annotpos, tiles : allannottiles});
                    thispage.annotArray = [];
                    if (foxitpage.annots) {
                        if (foxitpage.annots.length > 0) {
                            for (var i = 0; i < foxitpage.annots.length; i++) {
                                thispage.annotArray.push({
                                    cnv: document.createElement('canvas'),
                                    pos: foxitpage.annots[i].pos
                                    //annotobj
                                });
                                //var tcanvas = document.createElement('canvas');
                                thispage.annotArray[i].cnv.width = foxitpage.annots[i].tiles[0].width;
                                thispage.annotArray[i].cnv.height = foxitpage.annots[i].tiles[0].height;
                                //tcanvas.width = foxitpage.annots[i].tiles[0].width; 
                                //tcanvas.height = foxitpage.annots[i].tiles[0].height; 
                                //grab the context from your destination canvas
                                var tctx = thispage.annotArray[i].cnv.getContext('2d');
                                //foxitpage.annots[i].tiles[0];
                                tctx.drawImage(foxitpage.annots[i].tiles[0], 0, 0);
                                /*var annotobj = {
                                    cnv : tcanvas,
                                    //ctx : tctx,
                                    pos : foxitpage.annots[i].pos
                                }*/
                                //var destCtx = destinationCanvas.getContext('2d');
                                //call its drawImage() function passing it the source canvas directly
                                //thispage.annotArray.push(annotobj);
                            }
                        }
                    }
                };
                this.foxitcreatecanvasArray = function (foxitpage, tileArray) {
                    var totalwidth = 0;
                    var totalheight = 0;
                    var currentheight = 0;
                    var currentwidth = 0;
                    var bskip = false;
                    if (foxitpage.canvases) {
                        if (foxitpage.canvases.length > 0) {
                            var canvcount = 0;
                            for (var hc = 0; hc < foxitpage.hidden.length; hc++) {
                                if (canvcount == foxitpage.hidden[hc].ct) {
                                    bskip = true;
                                    canvcount++;
                                }
                            }
                            for (var i = 0; i < tileArray.length; i++) {
                                /*if (busetemp){
                                    var curcanvas = foxitpage.canvases[canvcount].cnv;
                                }else{
                                    curcanvas = foxitpage.canvases[canvcount];
                                }*/
                                //curcanvas = foxitpage.canvases[canvcount];
                                if (foxitpage.canvases[canvcount].tagName.toLowerCase() == "canvas") {
                                    if (i < tileArray.length - 1) {
                                        totalheight += foxitpage.tileSize;
                                        currentheight = foxitpage.tileSize;
                                    }
                                    else {
                                        totalheight += foxitpage.canvases[canvcount].height;
                                        currentheight = foxitpage.canvases[canvcount].height;
                                    }
                                    totalwidth = 0;
                                }
                                for (var j = 0; j < tileArray[i].length; j++) {
                                    /*if (busetemp){
                                        var curcanvas = foxitpage.canvases[canvcount].cnv;
                                    }else{
                                        curcanvas = foxitpage.canvases[canvcount];
                                    }*/
                                    if (foxitpage.canvases[canvcount]) {
                                        if (foxitpage.canvases[canvcount].tagName.toLowerCase() == "canvas") {
                                            if (j < tileArray[i].length - 1) {
                                                totalwidth += foxitpage.tileSize;
                                                currentwidth = foxitpage.tileSize;
                                            }
                                            else {
                                                totalwidth += foxitpage.canvases[canvcount].width;
                                                currentwidth = foxitpage.canvases[canvcount].width;
                                            }
                                            if (currentheight < foxitpage.canvases[canvcount].height) {
                                                totalheight += foxitpage.canvases[canvcount].height - currentheight;
                                            }
                                            //thispage.canvtileArray[i][j].cnv.width = foxitpage.canvases[canvcount].width;
                                            if (foxitpage.transScale == 1) {
                                                tileArray[i][j].cnv.height = foxitpage.canvases[canvcount].height;
                                                tileArray[i][j].cnv.width = foxitpage.canvases[canvcount].width;
                                                tileArray[i][j].tilenumber = canvcount;
                                                tileArray[i][j].xoffset = foxitpage.tileSize;
                                                tileArray[i][j].yoffset = foxitpage.tileSize;
                                                //thispage.canvtileArray[i][j].ctx.drawImage(foxitpage.canvases[canvcount], 0,0);
                                            }
                                            else {
                                                //var scaledcnv = downScaleCanvas(foxitpage.canvases[canvcount], foxitpage.transScale);
                                                //scaledcnv.height;
                                                //scaledcnv.width;
                                                tileArray[i][j].cnv.height = Math.round(foxitpage.canvases[canvcount].height * foxitpage.transScale);
                                                tileArray[i][j].cnv.width = Math.round(foxitpage.canvases[canvcount].width * foxitpage.transScale);
                                                tileArray[i][j].tilenumber = canvcount;
                                                tileArray[i][j].xoffset = foxitpage.tileSize * foxitpage.transScale;
                                                tileArray[i][j].yoffset = foxitpage.tileSize * foxitpage.transScale;
                                                //thispage.canvtileArray[i][j].ctx.drawImage(scaledcnv,0,0);
                                                //console.log(Math.round(foxitpage.canvases[canvcount].height * foxitpage.transScale), scaledcnv.height);
                                                //console.log(Math.round(foxitpage.canvases[canvcount].width * foxitpage.transScale), scaledcnv.width);
                                            }
                                            canvcount++;
                                            for (var hc = 0; hc < foxitpage.hidden.length; hc++) {
                                                if (canvcount == foxitpage.hidden[hc].ct) {
                                                    bskip = true;
                                                    canvcount++;
                                                }
                                            }
                                        }
                                    }
                                }
                            }
                        }
                    }
                    totalwidth *= foxitpage.transScale;
                    totalheight *= foxitpage.transScale;
                    return { width: totalwidth, height: totalheight };
                };
                this.foxitdrawcanvasArray = function (drawntiles, canvases, transscale) {
                    var bcanvasvalid = false;
                    var bmulticanvas = false;
                    if (this.busetempcanvases) {
                        if (thispage.tempfoxitpage.canvases) {
                            bcanvasvalid = true;
                            if (thispage.tempfoxitpage.canvases.length > 0) {
                                bmulticanvas = true;
                            }
                        }
                    }
                    else {
                        if (canvases) {
                            bcanvasvalid = true;
                            if (canvases.length > 0) {
                                bmulticanvas = true;
                            }
                        }
                    }
                    if (bcanvasvalid) {
                        if (bmulticanvas) {
                            var bdraw = true;
                            for (var i = 0; i < thispage.canvtileArray.length; i++) {
                                bdraw = true;
                                for (var j = 0; j < thispage.canvtileArray[i].length; j++) {
                                    bdraw = true;
                                    for (var cdt = 0; cdt < drawntiles.length; cdt++) {
                                        if (i == drawntiles[cdt].r && j == drawntiles[cdt].c) {
                                            bdraw = false;
                                        }
                                    }
                                    if (bdraw) {
                                        if (transscale == 1) {
                                            if (this.busetempcanvases) {
                                                thispage.canvtileArray[i][j].ctx.drawImage(thispage.tempfoxitpage.canvases[thispage.canvtileArray[i][j].tilenumber].cnv, 0, 0);
                                            }
                                            else {
                                                thispage.canvtileArray[i][j].ctx.drawImage(canvases[thispage.canvtileArray[i][j].tilenumber], 0, 0);
                                            }
                                            //thispage.rxdrawtimeend = new Date().getTime();
                                            //console.log('rx canvas unscaled tile copy', thispage.canvtileArray[i][j].tilenumber, thispage.rxdrawtimeend - thispage.rxdrawtimestart);
                                        }
                                        else {
                                            if (this.busetempcanvases) {
                                                var scaledcnv = downScaleCanvas(thispage.tempfoxitpage.canvases[thispage.canvtileArray[i][j].tilenumber].cnv, transscale);
                                            }
                                            else {
                                                var scaledcnv = downScaleCanvas(canvases[thispage.canvtileArray[i][j].tilenumber], transscale);
                                            }
                                            //var scaledcnv = downScaleCanvas(canvases[thispage.canvtileArray[i][j].tilenumber], transscale);
                                            thispage.canvtileArray[i][j].ctx.drawImage(scaledcnv, 0, 0);
                                            scaledcnv = null;
                                            //thispage.rxdrawtimeend = new Date().getTime();
                                            //console.log('rx canvas scaled tile copy', thispage.canvtileArray[i][j].tilenumber, thispage.rxdrawtimeend - thispage.rxdrawtimestart);
                                        }
                                    }
                                }
                            }
                        }
                    }
                    if (this.busetempcanvases) {
                        thispage.tempfoxitpage.canvases = [];
                        this.busetempcanvases = false;
                    }
                };
                this.foxitdrawAllcanvasArray = function (canvases, transscale) {
                    if (canvases) {
                        if (canvases.length > 0) {
                            var offscreenwidth = thispage.canvtileArray[0][0].cnv.width;
                            var offscreenheight = thispage.canvtileArray[0][0].cnv.height;
                            var ulx = thispage.dxpdf;
                            var uly = thispage.dypdf;
                            var drawntiles = [];
                            //draw tile in view first.    
                            for (var i = 0; i < thispage.canvtileArray.length; i++) {
                                for (var j = 0; j < thispage.canvtileArray[i].length; j++) {
                                    offscreenwidth = thispage.canvtileArray[i][j].cnv.width;
                                    offscreenheight = thispage.canvtileArray[i][j].cnv.height;
                                    if (transscale == 1) {
                                        thispage.canvtileArray[i][j].ctx.drawImage(canvases[thispage.canvtileArray[i][j].tilenumber], 0, 0);
                                        //thispage.rxdrawtimeend = new Date().getTime();
                                        //console.log('rx priority canvas unscaled tile copy', thispage.canvtileArray[i][j].tilenumber, thispage.rxdrawtimeend - thispage.rxdrawtimestart);
                                    }
                                    else {
                                        var scaledcnv = downScaleCanvas(canvases[thispage.canvtileArray[i][j].tilenumber], transscale);
                                        //thispage.canvtileArray[i][j].cnv.height = scaledcnv.height; 
                                        //thispage.canvtileArray[i][j].cnv.width = scaledcnv.width;
                                        thispage.canvtileArray[i][j].ctx.drawImage(scaledcnv, 0, 0);
                                        //thispage.rxdrawtimeend = new Date().getTime();
                                        //console.log('rx priority canvas scaled tile copy', thispage.canvtileArray[i][j].tilenumber, thispage.rxdrawtimeend - thispage.rxdrawtimestart);
                                    }
                                    drawntiles.push({ r: i, c: j });
                                    /*if((ulx < canvasowidth && uly < canvasoheight) && ((ulx + (offscreenwidth * thispage.dscalepdf)) > 0 && (uly + (offscreenheight * thispage.dscalepdf)) > 0)){
                                    
        
                                    }*/
                                    ulx += (offscreenwidth * thispage.dscalepdf) - 1;
                                }
                                uly += (offscreenheight * thispage.dscalepdf) - 1;
                                ulx = thispage.dxpdf;
                            }
                        }
                    }
                    //thispage.DocRef.draw_mpagepdf(false);
                    thispage.draw_canvas(true);
                    return drawntiles;
                
                };


                
                this.rotatedRect = function () {
                    var origx = this.startx;
                    var origy = this.starty;
                    var origw = this.endx;
                    var origh = this.endy;
                    var CanvRotRad = this.getpageRotation() * (Math.PI / 180);
                    var cosangle = Math.cos(CanvRotRad);
                    var sinangle = Math.sin(CanvRotRad);
                    var centercanvX = (canvasowidth / 2);
                    var centercanvY = (canvasoheight / 2);
                    var hw = origx - centercanvX;
                    var hh = origy - centercanvY;
                    var newx = (hw * cosangle) - (hh * sinangle);
                    var newy = (hw * sinangle) + (hh * cosangle);
                    newx = centercanvX + newx;
                    newy = centercanvY + newy;
                    hw = origw - centercanvX;
                    hh = origh - centercanvY;
                    var neww = (hw * cosangle) - (hh * sinangle);
                    var newh = (hw * sinangle) + (hh * cosangle);
                    neww = centercanvX + neww;
                    newh = centercanvY + newh;
                    return {
                        x: Math.min(newx, neww),
                        y: Math.min(newy, newh),
                        w: Math.max(neww, newx),
                        h: Math.max(newh, newy)
                    };
                };
                /*this.smallimageprogress = function (ev) {
                    if (ev.lengthComputable) {
                        //showDownloadDialog();
                        if (RxCore_GUI_Download != undefined) {
                            //RxCore_GUI_Download.setDownload("show");
                        }
        
                        var percentComplete = Math.round(ev.loaded * 100 / ev.total);
                        if (percentComplete == 100) {
                            //hideDownloadDialog();
                            if (RxCore_GUI_Download != undefined) {
                                RxCore_GUI_Download.setDownload("hide");
                            }
        
                        }
                    } else {
                        //document.getElementById('progressNumber').innerHTML = 'unable to compute';
                    }
        
                };*/
                /*this.largeimageprogress = function (ev) {
                    if (ev.lengthComputable) {
                        //showDownloadDialog();
                        if (RxCore_GUI_Download != undefined) {
                            //RxCore_GUI_Download.setDownload("show");
                        }
        
                        var percentComplete = Math.round(ev.loaded * 100 / ev.total);
                        if (percentComplete == 100) {
                            //hideDownloadDialog();
                            if (RxCore_GUI_Download != undefined) {
                                RxCore_GUI_Download.setDownload("hide");
                            }
        
                        }
        
                    } else {
                        //document.getElementById('progressNumber').innerHTML = 'unable to compute';
                    }
        
        
                };*/

                this.printPDFpage = function(){

                    /*if (RxCore_printhelper().docObj.foxitdoc == null){
                        return
                    }*/


                    //var pagewidth = RxCore_printhelper().docObj.pages[thispage.pagenum].originalwidth;
                    //var pageheigth = RxCore_printhelper().docObj.pages[thispage.pagenum].originalheight;


                    

                    //var tempwscale = canvashireswidth / pagewidth;
                    //var temphscale = canvashiresheight / pageheigth;

    
                        
                        //var tempwscale = pageCanvases[thispage.pagenum].width / thispage.pagewidth;
                        //var temphscale = pageCanvases[thispage.pagenum].height / thispage.pageheight;
                        
                    //thispage.pagescale = Math.min(tempwscale,temphscale);



                    //RxCore_printhelper().docObj.foxitdoc.getBitmap(thispage.pagenumber, thispage.printobj.pscale, thispage.setPageBitmapPrint);

                    if (thispage.DocRef.foxitdoc != null) {
                        thispage.DocRef.foxitdoc.getBitmap(thispage.pagenumber, thispage.printobj.pscale, thispage.setPageBitmapPrint);
                    }


                };

                this.setPageBitmapPrint = function (bitmap) {
                    
                    if(bitmap.error){

                        return;
                        //check for error

                    }

                    if(bitmap == undefined){
                        return;
                    }

                    
                    var tdv = new Uint8ClampedArray(bitmap.buffer);
                    var imageData = new ImageData(tdv, bitmap.width, bitmap.height);

                    thispage.pdfprintcanvas.width = bitmap.width;
                    thispage.pdfprintcanvas.height = bitmap.height;


                    //thispage.pdfprintctx = thispage.pdfprintcanvas.getContext('2d');
                    //thispage.offscreenwidth = bitmap.width;
                    //thispage.offscreenheight = bitmap.height;

                    thispage.pdfprintctx.putImageData(imageData, 0, 0);
                    
                    thispage.printobj.printctx.drawImage(thispage.pdfprintcanvas, thispage.printobj.pdx, thispage.printobj.pdy, thispage.printobj.docwidth * thispage.printobj.pscale, thispage.printobj.docheight * thispage.printobj.pscale);
                    drawmarkupPrint(thispage.printobj.printctx, thispage.pagenumber, thispage.printobj.pdx, thispage.printobj.pdy, thispage.printobj.pscale);

                    //thispage.pagescale
                    //thispage.setPDFdim(thispage.pdfdxtemp,thispage.pdfdytemp,thispage.pagescale);
                    //thispage.setPDFdim()
                    
                };

                this.load3Dimage = function (ev) {
                    thispage.printobj.printctx.drawImage(ev.currentTarget, thispage.printobj.pdx, thispage.printobj.pdy, thispage.printobj.docwidth * thispage.printobj.pscale, thispage.printobj.docheight * thispage.printobj.pscale);
                    
                    drawmarkupPrint(thispage.printobj.printctx, thispage.pagenumber, thispage.printobj.pdx, thispage.printobj.pdy, thispage.printobj.pscale);
                };
                //this.smallimage.addEventListener('load', this.smallimageload, false);
                this.floorplanloadevent = function (ev) {
                    var yscale = 0.0;
                    var xscale = 0.0;
                    var dxlocal = 0.0;
                    var dylocal = 0.0;
                    var dscalelocal = 0.0;
                    var imagewidth = 0;
                    var imageheight = 0;
                    imagewidth = ev.srcElement.naturalWidth;
                    imageheight = ev.srcElement.naturalHeight;
                    thispage.SmallImageWidth = imagewidth;
                    thispage.SmallImageHeight = imageheight;
                    thispage.smallimagecnv.width = imagewidth;
                    thispage.smallimagecnv.height = imageheight;
                    thispage.smallimagectx.drawImage(thispage.smallimage, 0, 0, imagewidth, imageheight);
                    //console.log(canvasoheight);
                    /*yscale = canvasoheight / thispage.SmallImageHeight;
                    xscale = canvasowidth / thispage.SmallImageWidth;
                    dscalelocal = Math.min(xscale, yscale);
                    dxlocal = (canvasowidth - (thispage.SmallImageWidth * dscalelocal)) / 2;
                    dylocal = (canvasoheight - (thispage.SmallImageHeight * dscalelocal)) / 2;
                    thispage.setimagedimsmall(dxlocal, dylocal, dscalelocal);*/
                };
                this.smallimagecreatefromlarge = function(){
                    var yscale = 0.0;
                    var xscale = 0.0;
                    var dxlocal = 0.0;
                    var dylocal = 0.0;
                    var dscalelocal = 0.0;
                    var imagewidth = 0;
                    var imageheight = 0;

                    //var thumbcnv = downScaleCanvas(cameracanvas, scale);

                    
                    
                    imagewidth = thispage.SmallImageWidth;
                    imageheight = thispage.SmallImageHeight;

                    var dwnscale = thispage.SmallImageWidth / thispage.originalwidth;
                    
                    thispage.smallimagecnv.width = imagewidth;
                    thispage.smallimagecnv.height = imageheight;

                    if(dwnscale < 1){
                        thispage.smallimagecnv = downScaleCanvas(thispage.largeimagecnv, dwnscale);
                    }else{
                        thispage.smallimagectx.drawImage(thispage.largeimagecnv, 0, 0, imagewidth, imageheight);
                    }
                    

                    //thispage.smallimagectx.drawImage(thispage.smallimage, 0, 0, imagewidth, imageheight);
                    //console.log(canvasoheight);
                    yscale = canvasoheight / thispage.SmallImageHeight;
                    xscale = canvasowidth / thispage.SmallImageWidth;
                    dscalelocal = Math.min(xscale, yscale);
                    dxlocal = (canvasowidth - (thispage.SmallImageWidth * dscalelocal)) / 2;
                    dylocal = (canvasoheight - (thispage.SmallImageHeight * dscalelocal)) / 2;
                    thispage.setimagedimsmall(dxlocal, dylocal, dscalelocal);



                    if (RxCore_GUI_Download != undefined) {
                        RxCore_GUI_Download.setDownload("hide");
                    }

                };
                this.smallimageloadevent = function (ev) {
                    var yscale = 0.0;
                    var xscale = 0.0;
                    var dxlocal = 0.0;
                    var dylocal = 0.0;
                    var dscalelocal = 0.0;
                    var imagewidth = 0;
                    var imageheight = 0;
                    imagewidth = thispage.SmallImageWidth;
                    imageheight = thispage.SmallImageHeight;
                    thispage.smallimagecnv.width = imagewidth;
                    thispage.smallimagecnv.height = imageheight;
                    thispage.smallimagectx.drawImage(thispage.smallimage, 0, 0, imagewidth, imageheight);
                    //console.log(canvasoheight);
                    yscale = canvasoheight / thispage.SmallImageHeight;
                    xscale = canvasowidth / thispage.SmallImageWidth;
                    dscalelocal = Math.min(xscale, yscale);
                    dxlocal = (canvasowidth - (thispage.SmallImageWidth * dscalelocal)) / 2;
                    dylocal = (canvasoheight - (thispage.SmallImageHeight * dscalelocal)) / 2;
                    thispage.setimagedimsmall(dxlocal, dylocal, dscalelocal);
                    if (RxCore_GUI_Download != undefined) {
                        RxCore_GUI_Download.setDownload("hide");
                    }
                };
                this.replacefloorplan = function (floornum, thumbnail) {
                    var width = thumbnail.birdseyeobj.birdseye.width;
                    var height = thumbnail.birdseyeobj.birdseye.height;
                    //thumbnail.
                    thispage.floorplans[floornum].width = width;
                    thispage.floorplans[floornum].height = height;
                    thispage.floorplans[floornum].canvas.width = width;
                    thispage.floorplans[floornum].canvas.height = height;
                    //birdseye.thumbnail.birdseyeGUIimgctx.drawImage(birdseye.thumbnail.birdseyeobj.birdseye, 0,0);
                    thispage.floorplans[floornum].ctx.drawImage(thumbnail.birdseyeobj.birdseye, 0, 0, width, height);
                    thispage.floorplans[floornum].replaced = true;
                    thispage.floorplans[floornum].RScale = thumbnail.floorplanreplaceScale;
                    //thispage.DocRef.thumbnails[thispage.pagenumber].set3DNavImage(Floorplanreplace);
                    //thisdocument.thumbnails[thisdocument.currentpage].set3DNavImage(Floorplanreplace);
                };
                this.getFloorplans = function () {
                    return thispage.floorplans;
                };
                this.floorplanlolad = function (index) {
                    //var oReq = new XMLHttpRequest();
                    //oReq.withCredentials = bUseCredentials;
                    //var floorplan = thispage.FloorplanSRC
                    
                    if (busegltf) {
                        var floorplan = thispage.floorplans[index].SRC;
                    }
                    else {
                        return;
                    }

                    sendHttpRequest('GET', floorplan,'blob').then(function (response){
                        var url = response; // Note: not oReq.responseText
                        if (url) {
                            
                            thispage.floorplans[index].image.addEventListener('load', function(ev){
                                var imagewidth = 0;
                                var imageheight = 0;

                                imagewidth = ev.srcElement.naturalWidth;
                                imageheight = ev.srcElement.naturalHeight;

                                thispage.floorplans[index].width = imagewidth;
                                thispage.floorplans[index].height = imageheight;

                                thispage.floorplans[index].originalWidth = imagewidth;
                                thispage.floorplans[index].originalHeight = imageheight;

                                thispage.floorplans[index].canvas.width = imagewidth; 
                                thispage.floorplans[index].canvas.height = imageheight; 

                                thispage.floorplans[index].ctx.drawImage(thispage.floorplans[index].image, 0, 0, imagewidth, imageheight);
                                
                                var thumbscale = (thispage.floorplans[index].thumbwidht) / imagewidth;
                                thispage.floorplans[index].thumbcanvas.width = imagewidth * thumbscale;
                                thispage.floorplans[index].thumbcanvas.height = imageheight * thumbscale;

                                thispage.floorplans[index].thumbctx.drawImage(thispage.floorplans[index].image, 0, 0, imagewidth * thumbscale, imageheight * thumbscale);


                                if(RxCore_GUI_floorthumb != undefined){
                                    RxCore_GUI_floorthumb.setThumbnail(index, thispage.floorplans[index]);

                                }
                                //thisthumb.thumbnailobj.thumbnail.height = pageheight * scale;
                                //thumbcanvas : floorplanthmbcnv,
                                //thumbctx : floorplanthmbctx,
                                //thumbwidht : 250,
                                //thumbheight : 180,



                            });
                            //thispage.smallimage.addEventListener('load', thispage.floorplanloadevent, false);
                            //thispage.smallimage.src = URL.createObjectURL(url);
                            thispage.floorplans[index].image.src = URL.createObjectURL(url);   
                        }
                        
                    
                    }).catch(function(error){
           
                        if(error.status){
                
                        }else{
                            console.log(error);
                        }
                        
                    });                    



                    //oReq.open("GET", floorplan, true);
                    //oReq.responseType = "blob";
                    //var __this = this;
                    /*oReq.onload = function (oEvent) {
                        var url = oReq.response; // Note: not oReq.responseText
                        if (url) {
                            //thispage.floorplans[index].image.addEventListener('load', thispage.floorplanloadevent, false);
                            thispage.floorplans[index].image.addEventListener('load', function (ev) {
                                var imagewidth = 0;
                                var imageheight = 0;
                                imagewidth = ev.srcElement.naturalWidth;
                                imageheight = ev.srcElement.naturalHeight;
                                thispage.floorplans[index].width = imagewidth;
                                thispage.floorplans[index].height = imageheight;
                                thispage.floorplans[index].originalWidth = imagewidth;
                                thispage.floorplans[index].originalHeight = imageheight;
                                thispage.floorplans[index].canvas.width = imagewidth;
                                thispage.floorplans[index].canvas.height = imageheight;
                                thispage.floorplans[index].ctx.drawImage(thispage.floorplans[index].image, 0, 0, imagewidth, imageheight);
                                var thumbscale = (thispage.floorplans[index].thumbwidht) / imagewidth;
                                thispage.floorplans[index].thumbcanvas.width = imagewidth * thumbscale;
                                thispage.floorplans[index].thumbcanvas.height = imageheight * thumbscale;
                                thispage.floorplans[index].thumbctx.drawImage(thispage.floorplans[index].image, 0, 0, imagewidth * thumbscale, imageheight * thumbscale);
                                if (RxCore_GUI_floorthumb != undefined) {
                                    RxCore_GUI_floorthumb.setThumbnail(index, thispage.floorplans[index]);
                                }
                                //thisthumb.thumbnailobj.thumbnail.height = pageheight * scale;
                                //thumbcanvas : floorplanthmbcnv,
                                //thumbctx : floorplanthmbctx,
                                //thumbwidht : 250,
                                //thumbheight : 180,
                            });
                            //thispage.smallimage.addEventListener('load', thispage.floorplanloadevent, false);
                            //thispage.smallimage.src = URL.createObjectURL(url);
                            thispage.floorplans[index].image.src = URL.createObjectURL(url);
                        }
                    };*/
                    //oReq.send(null);
                };
                this.smallimageload = function () {
                    var oReq = new XMLHttpRequest();
                    oReq.withCredentials = bUseCredentials;
                    oReq.open("GET", thispage.SmallImageSRC, true);


                    if(buseAuth){

                        var auth = getAuth();
        
                        if (auth.authset){
                            oReq.setRequestHeader('Authorization', auth.authString + auth.baseuserstring);
                        }
                        
                    }


                    oReq.responseType = "blob";
                    //var __this = this;
                    oReq.onload = function (oEvent) {
                        var url = oReq.response; // Note: not oReq.responseText
                        if (url) {
                            thispage.smallimage.addEventListener('load', thispage.smallimageloadevent, false);
                            thispage.smallimage.src = URL.createObjectURL(url);
                        }
                    };
                    oReq.onprogress = function (ev) {
                        if (ev.lengthComputable) {
                            //showDownloadDialog();
                            if (RxCore_GUI_Download != undefined) {
                                //RxCore_GUI_Download.setDownload("show");
                            }
                            var percentComplete = Math.round(ev.loaded * 100 / ev.total);
                            if (percentComplete == 100) {
                                //hideDownloadDialog();
                                if (RxCore_GUI_Download != undefined) {
                                    RxCore_GUI_Download.setDownload("hide");
                                }
                            }
                        }
                        else {
                            //document.getElementById('progressNumber').innerHTML = 'unable to compute';
                        }
                    };
                    oReq.send(null);
                };
                this.largeimageloadevent = function (ev) {
                    var yscale = 0.0;
                    var xscale = 0.0;
                    var dxlocal = 0.0;
                    var dylocal = 0.0;
                    var dscalelocal = 0.0;
                    var imagewidth = 0;
                    var imageheight = 0;
                    imagewidth = thispage.MainImageWidth;
                    imageheight = thispage.MainImageHeight;
                    thispage.originalwidth = thispage.MainImageWidth;
                    thispage.originalheight = thispage.MainImageHeight;
                    thispage.largeimagecnv.width = imagewidth;
                    thispage.largeimagecnv.height = imageheight;
                    thispage.largeimagectx.drawImage(thispage.largeimage, 0, 0, imagewidth, imageheight);
                    //set image fixed scale for markup
                    yscale = fixedScaleSize.height / thispage.MainImageHeight; //thispage.MainImageHeight;
                    xscale = fixedScaleSize.width / thispage.MainImageWidth; // thispage.MainImageWidth;
                    thispage.fixedScale = Math.min(xscale, yscale);
                    //console.log(canvasoheight);
                    yscale = canvasoheight / thispage.MainImageHeight;
                    xscale = canvasowidth / thispage.MainImageWidth;
                    thispage.printobj.setDocSize(thispage.MainImageWidth, thispage.MainImageHeight);
                    thispage.width = imagewidth;
                    thispage.height = imageheight;
                    dscalelocal = Math.min(xscale, yscale);
                    dxlocal = (canvasowidth - (thispage.MainImageWidth * dscalelocal)) / 2;
                    dylocal = (canvasoheight - (thispage.MainImageHeight * dscalelocal)) / 2;
                    thispage.setimagedimlarge(dxlocal, dylocal, dscalelocal);
                    //thispage.DocRef.setthumbnail(thispage.MainImageWidth, thispage.MainImageHeight, thispage.pagenumber);
                    /*
                    rescale small image  to ensure we are using the same canvas size.
        
                     */

                    thispage.smallimagecreatefromlarge();

                    //imagewidth = thispage.SmallImageWidth;
                    //imageheight = thispage.SmallImageHeight;
                    //thispage.smallimagecnv.width = imagewidth;
                    //thispage.smallimagecnv.height = imageheight;
                    //thispage.smallimagectx.drawImage(thispage.smallimage, 0, 0, imagewidth, imageheight);
                    //console.log(canvasoheight);
                    /*yscale = canvasoheight / thispage.SmallImageHeight;
                    xscale = canvasowidth / thispage.SmallImageWidth;
                    dscalelocal = Math.min(xscale, yscale);
                    dxlocal = (canvasowidth - (thispage.SmallImageWidth * dscalelocal)) / 2;
                    dylocal = (canvasoheight - (thispage.SmallImageHeight * dscalelocal)) / 2;
                    thispage.setimagedimsmall(dxlocal, dylocal, dscalelocal);
                    //hideDownloadDialog();
                    if (RxCore_GUI_Download != undefined) {
                        RxCore_GUI_Download.setDownload("hide");
                    }*/
                };
                this.largeimageload = function () {
                    var oReq = new XMLHttpRequest();
                    oReq.withCredentials = bUseCredentials;
                    oReq.open("GET", thispage.MainImageSRC, true);

                    if(buseAuth){

                        var auth = getAuth();
        
                        if (auth.authset){
                            oReq.setRequestHeader('Authorization', auth.authString + auth.baseuserstring);
                        }
                        
                    }


                    oReq.responseType = "blob";
                    //var __this = thispage;
                    oReq.onload = function (oEvent) {
                        var url = oReq.response; // Note: not oReq.responseText
                        if (url) {
                            thispage.largeimage.addEventListener('load', thispage.largeimageloadevent, false);
                            thispage.largeimage.src = URL.createObjectURL(url);
                        }
                    };
                    oReq.onprogress = function (ev) {
                        if (ev.lengthComputable) {
                            //showDownloadDialog();
                            /*if (RxCore_GUI_Download != undefined) {
                                //RxCore_GUI_Download.setDownload("show");
                            }*/
                            var percentComplete = Math.round(ev.loaded * 100 / ev.total);
                            if (percentComplete == 100) {
                                //hideDownloadDialog();
                                if (RxCore_GUI_Download != undefined) {
                                    RxCore_GUI_Download.setDownload("hide");
                                }
                            }
                        }
                        else {
                            //document.getElementById('progressNumber').innerHTML = 'unable to compute';
                        }
                    };
                    oReq.send(null);
                };

                this.getlayerlist = function(){
                    return thispage.VectorPageObj.getlayerlist();
                };

                this.turnLayerOnOff = function (index) {
                    thispage.DocRef.refreshBirdsEye();
                    thispage.DocRef.renderBirdsEye();
                    thispage.VectorPageObj.turnLayerOnOff(index);
                    
                    /*if (thispage.usedincomposite && thispage.compositereference != undefined) {
                        //this.compositereference.draw_compare(false);
                        thispage.compositereference.scaleToBackground(false);
                    }
                    else {
                        
                    }*/

                    thispage.draw_vector(false);

                    thispage.vectorischanged = true;
                };
                this.turnBlockAllOnOff = function (OnOff) {
                    thispage.VectorPageObj.turnBlockAllOnOff(OnOff);
                    
                    /*if (thispage.usedincomposite && thispage.compositereference != undefined) {
                        //this.compositereference.draw_compare(false);
                        thispage.compositereference.scaleToBackground(false);
                    }
                    else {
                        
                    }*/

                    thispage.draw_vector(false);

                    thispage.vectorischanged = true;
                    thispage.DocRef.refreshBirdsEye();
                    thispage.DocRef.renderBirdsEye();
                };
                this.turnLayerAllOnOff = function (OnOff) {
                    thispage.VectorPageObj.turnLayerAllOnOff(OnOff);
                    
                    /*if (this.usedincomposite && this.compositereference != undefined) {
                        //this.compositereference.draw_compare(false);
                        this.compositereference.scaleToBackground(false);
                    }
                    else {
                        
                    }*/

                    thispage.draw_vector(false);
                    thispage.vectorischanged = true;
                    thispage.DocRef.refreshBirdsEye();
                    thispage.DocRef.renderBirdsEye();
                };
                this.resetLayers = function () {
                    thispage.VectorPageObj.resetLayers();

                    /*if (thispage.usedincomposite && thispage.compositereference != undefined) {
                        //this.compositereference.draw_compare(false);
                        thispage.compositereference.scaleToBackground(false);
                    }
                    else {
                        
                    }*/
                    thispage.draw_vector(false);
                    thispage.vectorischanged = true;
                    thispage.DocRef.refreshBirdsEye();
                    thispage.DocRef.renderBirdsEye();
                };
                this.getBlocks = function () {

                    if(thispage.VectorPageObj == undefined){
                        return [];    
                    }
                    return thispage.VectorPageObj.getBlocks();
                };

                this.getPenTable = function(){
                    if(thispage.VectorPageObj == undefined){
                        return [];    
                    }

                    return thispage.VectorPageObj.getpenTable();

                };


                this.getBlockswFill = function(){
                    return thispage.VectorPageObj.getBlockswFill();
                };

                this.getFilteredAttributes = function () {
                    return thispage.VectorPageObj.getFilteredAttributes();
                };
                this.getpageRef = function () {
                    return thispage.pdfpageref;
                };

                this.getpageRotation = function () {

                    var retrot = thispage.drotation;
                    
                    if (thispage.DocRef.foxitdoc != null) {
                        if(thispage.DocRef.documentrotation != 0){
                            retrot = thispage.DocRef.documentrotation;
                        }
                    }
                    
                    return retrot;
                };


                this.filterBlocks = function (szBlockLoadMask) {
                    thispage.VectorPageObj.filterBlocks(szBlockLoadMask);
                };
                this.filterAttributes = function (szAttributeLoadMask) {
                    thispage.VectorPageObj.filterAttributes(szAttributeLoadMask);
                };
                this.turnBlockOnOff = function (index) {
                    thispage.VectorPageObj.turnBlockOnOff(index);
                    /*if (thispage.usedincomposite && thispage.compositereference != undefined) {
                        //this.compositereference.draw_compare(false);
                        thispage.compositereference.scaleToBackground(false);
                    }
                    else {
                        
                    }*/
                    thispage.draw_vector(false);
                    thispage.vectorischanged = true;
                    thispage.DocRef.refreshBirdsEye();
                    thispage.DocRef.renderBirdsEye();
                };
                this.state3dBlockAll = function (state, bUseCallback) {
                    if (thispage.usevector3DGLTF) {
                        thispage.set3DBlockStateNewAll(state, bUseCallback);
                    }
                };
                this.state3dBlock = function (name, state, bUseCallback) {
                    if (thispage.usevector3DGLTF) {
                        //thispage.set3DBlockStateNew(name, state, bUseCallback);
                        thispage.set3DBlockStateNewGroup(name, state, bUseCallback);
                    }
                };
                this.turn3DBlockOnOff = function (name, bUseCallback) {
                    if (thispage.usevector3DGLTF) {
                        thispage.turn3DBlockOnOffNew(name, bUseCallback);
                    }
                    else {
                        thispage.turn3DBlockOnOffOld(name);
                    }
                };
                /*this.turn3DBlockOnOff = function (name) {
                    for (var i = 0; i < thispage.Vector3DPageObj.scene.children.length; i++) {
                        if (thispage.Vector3DPageObj.scene.children[i].name == name) {
                            if (!thispage.Vector3DPageObj.scene.children[i].visible) {
                                thispage.Vector3DPageObj.scene.children[i].visible = true;
                                this.Vector3DPageObj.partlist[thispage.Vector3DPageObj.scene.children[i].name].tempvisible = true;
                            } else {
                                thispage.Vector3DPageObj.scene.children[i].visible = false;
                                this.Vector3DPageObj.partlist[thispage.Vector3DPageObj.scene.children[i].name].tempvisible = false;
                            }
                            //thispage.Vector3DPageObj.scene.updateMatrix();
                            //Viewer3D.update();
                            if (Rxcore_GUI_3DPartInfo != undefined) {
                                Rxcore_GUI_3DPartInfo.set3DPartInfo(this.Vector3DPageObj.partlist[name]);
                            }
        
                            this.Vector3DPageObj.updateblocklist();
                            return;
                        }
                    }
        
        
                    //Viewer3D.update();
                };*/
                this.change3DMarkupSize = function (size) {
                    var markuparray = thispage.Vector3DPageObj.getMarkupChildren();
                    var pwidth = thispage.Vector3DPageObj.getmminUnit(400);
                    /*if (thispage.Vector3DPageObj.unit == 1){
                        //unit = meter
                        var pwidth = 0.4; //40 cm
                    }else if (thispage.Vector3DPageObj.unit == 0){
                        //unit = mm
                        pwidth = 400; //40 cm
                    }*/
                    //var pwidth = size;      
                    //var pheight = size;
                    //var pdepth = size / 10;
                    //var scaleSizeX = size / pwidth;
                    //var scaleSizeY = size / pwidth;
                    //var scaleSizeZ = (size / pwidth);

                    var scaleSizeX = size;
                    var scaleSizeY = size;
                    var scaleSizeZ = size;


                    var texturecolornum = null;
                    if (markuparray.length == 0) {
                        return;
                    }
                    for (var i = 0; i < markuparray.length; i++) {
                        if (markuparray[i].userData.selected) {
                            //remove mesh from markup scene
                            //var scaleSizeX = size / pwidth;
                            //var scaleSizeY = size / pwidth;
                            //var scaleSizeZ = (size / pwidth) / 10;

                            if(markuparray[i].userData.type == "symbol"){
                                markuparray[i].scale.set(scaleSizeX, scaleSizeY, scaleSizeZ);
                            }

                            
                            //var cubegeometry = new THREE.BoxBufferGeometry(pwidth, pheight, pdepth);
                            //markuparray[i].geometry.dispose();
                            //markuparray[i].geometry.
                            /*if (texturecolornum != null && textureobjects[texturecolornum].markupmaterial != null)  {
        
                                //markuparray[i].material = textureobjects[texturecolornum].markupmaterial;
                                markuparray[i].material[4] = textureobjects[texturecolornum].markupmaterial;
                                markuparray[i].material[5] = textureobjects[texturecolornum].markupmaterial;
                                markuparray[i].userData.actionStatus = newstatus;
                            }*/
                            //thispage.Vector3DPageObj
                        }
                        //thispage.Vector3DPageObj.scene.updateMatrix();
                        //Viewer3D.update();
                    }
                    this.Vector3DPageObj.updateblocklist();
                    return;
                };
                this.change3DMarkup = function (newstatus) {
                    var markuparray = thispage.Vector3DPageObj.getMarkupChildren();
                    var texturecolornum = null;
                    if (markuparray.length == 0) {
                        return;
                    }
                    for (var i = 0; i < textureobjects.length; i++) {
                        if (textureobjects[i].color == newstatus) {
                            texturecolornum = i;
                        }
                    }
                    for (var i = 0; i < markuparray.length; i++) {
                        if (markuparray[i].userData.selected) {
                            //remove mesh from markup scene
                            //markuparray[i].geometry.dispose();
                            if (texturecolornum != null && textureobjects[texturecolornum].markupmaterial != null) {
                                //markuparray[i].material = textureobjects[texturecolornum].markupmaterial;
                                markuparray[i].material[4] = textureobjects[texturecolornum].markupmaterial;
                                markuparray[i].material[5] = textureobjects[texturecolornum].markupmaterial;
                                markuparray[i].userData.actionStatus = newstatus;
                            }
                            //thispage.Vector3DPageObj
                        }
                        //thispage.Vector3DPageObj.scene.updateMatrix();
                        //Viewer3D.update();
                    }
                    this.Vector3DPageObj.updateblocklist();
                    return;
                };                
                this.updatedimline = function(object, p1, p2){
                    var markuparray = thispage.Vector3DPageObj.getMarkupChildren();

                    if (markuparray.length == 0) {
                        return;
                    }
                    
                    for (var i = 0; i < markuparray.length; i++) {
                        if(markuparray[i].userData.type == "dimension"){
                            if(markuparray[i].userData.parentref == object && markuparray[i].type == "LineSegments"){
                                
                                var linepos = new Float32Array([p1.x, p1.y, p1.z, p2.x, p2.y, p2.z]);
                                markuparray[i].geometry.setAttribute('position', new THREE.BufferAttribute(linepos, 3));                                                                        
                                markuparray[i].geometry.attributes.position.needsUpdate = true;

                            }
                        }

                    }

                };

                this.move3DMarkupdimPoint = function(object, position, pointindex){
                    var markuparray = thispage.Vector3DPageObj.getMarkupChildren();
                    if (markuparray.length == 0) {
                        return;
                    }
                    if (pointindex == null){
                        return;
                    }

                    var bmoved = false;

                    for (var i = 0; i < markuparray.length; i++) {
                        if (markuparray[i].userData.selected) {
                            if(markuparray[i].userData.type == "dimension"){

                                var points = [];

                                if(markuparray[i].uuid == object && markuparray[i].type == "Points" ){
                                    
                                    markuparray[i].updateMatrixWorld();
                                    
                                    var positions = markuparray[i].geometry.getAttribute('position');
                                    

                                    var p1 = position.clone();

                                    for (var c=0; c < positions.count - 1; c++){

                                        var vstart = c * positions.itemSize;

                                        var x = positions.array[vstart];
                                        var y = positions.array[vstart + 1];
                                        var z = positions.array[vstart + 2];


                                        var pos = new THREE.Vector3(x, y, z);

                                        markuparray[i].localToWorld(pos);

                                        if(pointindex == c){
                                            points.push(p1);

                                        }else{
                                            points.push(pos);
                                        }

                                        
                                    }    

                                    var dist = points[0].distanceTo ( points[1] );
                                    var midpoint = new THREE.Vector3();
                                    midpoint.subVectors( points[0], points[1] ).setLength( dist *  0.5).add( points[1] );
                                    points.push(midpoint);

                                    var pointspos = new Float32Array([points[0].x, points[0].y, points[0].z, points[1].x, points[1].y, points[1].z,points[2].x, points[2].y, points[2].z]);
                                    markuparray[i].geometry.setAttribute( 'position', new THREE.Float32BufferAttribute( pointspos, 3 ) );

                                    markuparray[i].userData.dimtext = thispage.Vector3DPageObj.setdimvalue(dist);

                                    markuparray[i].userData.distance = dist;

                                    thispage.updatedimline(object, points[0], points[1]);

                                    markuparray[i].geometry.attributes.position.needsUpdate = true;

                                    markuparray[i].updateMatrixWorld();

                                    //markuparray[i].userData.Unit = Unitofmeasure;
                                    //markuparray[i].userData.SubUnit = SubmeasureUnit;
            
                                    bmoved = true;

                                }
                                
                            }
                            //check if object is the same

                            //remove mesh from markup scene
                            //markuparray[i].position.set(position);
                            //thispage.Vector3DPageObj
                        }
                        //thispage.Vector3DPageObj.scene.updateMatrix();
                        //Viewer3D.update();
                    }
                    if(bmoved){
                        if (RxCore_GUI_3DMarkupList != undefined) {
                            RxCore_GUI_3DMarkupList.setMarkupList(markuparray, DocObj.bActive);
                        }
                        this.Vector3DPageObj.updateblocklist();
                    }

                };

                this.moveSelected3DMarkup = function(position, pointindex){

                    var markuparray = thispage.Vector3DPageObj.getMarkupChildren();
                    var markupsceneindex = thispage.Vector3DPageObj.getmarkupScene();
                    if (markuparray.length == 0) {
                        return;
                    }

                    for (var i = 0; i < markuparray.length; i++) {
                        if (markuparray[i].userData.selected) {
                            if(markuparray[i].userData.type == "symbol"){

                                thispage.move3DMarkup(markuparray[i].uuid, position);
                                break;
                                
                            }
                            if(markuparray[i].type == "Points"){

                                thispage.move3DMarkupdimPoint(markuparray[i].uuid, position, pointindex);
                                break;

                            }
                            

                            //check if object is the same

                            //remove mesh from markup scene
                            //markuparray[i].position.set(position);
                            //thispage.Vector3DPageObj
                        }
                        //thispage.Vector3DPageObj.scene.updateMatrix();
                        //Viewer3D.update();
                    }
                                        
                };                

                
                this.move3DMarkup = function (object, position) {
                    //var mesharray = thispage.Vector3DPageObj.getMeshChildren();
                    //replace with get markup mesh array
                    var markuparray = thispage.Vector3DPageObj.getMarkupChildren();
                    var markupsceneindex = thispage.Vector3DPageObj.getmarkupScene();
                    if (markuparray.length == 0) {
                        return;
                    }
                    var bmoved = false;

                    for (var i = 0; i < markuparray.length; i++) {
                        if (markuparray[i].userData.selected) {

                            if(markuparray[i].userData.type == "symbol"){
                                if (markuparray[i].uuid == object) {
                                    //markuparray[i].position.set
                                    markuparray[i].position.copy(position);
                                    bmoved = true;
                                    //console.log(position); 
                                }
                            }

                            //check if object is the same
                            //remove mesh from markup scene
                            //markuparray[i].position.set(position);
                            //thispage.Vector3DPageObj
                        }
                        //thispage.Vector3DPageObj.scene.updateMatrix();
                        //Viewer3D.update();
                    }

                    if(bmoved){
                        if (RxCore_GUI_3DMarkupList != undefined) {
                            RxCore_GUI_3DMarkupList.setMarkupList(markuparray, DocObj.bActive);
                        }
                        this.Vector3DPageObj.updateblocklist();
                    }
                    
                    return;
                };
                this.displayfloorplan = function(floornum, onOff, bAll){
                    var markuparray = thispage.Vector3DPageObj.getMarkupChildren();
                    var markupsceneindex = thispage.Vector3DPageObj.getmarkupScene();
                    if (markuparray.length == 0) {
                        return;
                    }

                    for (var i = 0; i < markuparray.length; i++) {
                        if (markuparray[i].userData.floornum == floornum || bAll) {

                            if (thispage.Vector3DPageObj.getmarkupScene() != null) {
                                if (thispage.Vector3DPageObj.scene.children[markupsceneindex]) {

                                    if(markuparray[i].userData.type = "floorplan"){
                                        markuparray[i].visible = onOff;
                                    }
                                }
                            }
                        }
                    }

                };
                this.removefloorplan = function(floornum){
                    var markuparray = thispage.Vector3DPageObj.getMarkupChildren();
                    var markupsceneindex = thispage.Vector3DPageObj.getmarkupScene();
                    if (markuparray.length == 0) {
                        return;
                    }
                    for (var i = 0; i < markuparray.length; i++) {
                        if (markuparray[i].userData.floornum == floornum) {
                            markuparray[i].geometry.dispose();
                            markuparray[i].material.dispose();

                            if (thispage.Vector3DPageObj.getmarkupScene() != null) {
                                if (thispage.Vector3DPageObj.scene.children[markupsceneindex]) {
                                    thispage.Vector3DPageObj.scene.children[markupsceneindex].remove(markuparray[i]);
                                }
                            }
                        }
                    }
                };
                this.delete3DMarkupbyName = function(name){
                    var markuparray = thispage.Vector3DPageObj.getMarkupChildren();
                    var markupsceneindex = thispage.Vector3DPageObj.getmarkupScene();
                    if (markuparray.length == 0) {
                        return;
                    }

                    var delobjects = [];

                    for (var i = 0; i < markuparray.length; i++) {
                        //var deleteid = markuparray[i].uuid;

                        if (markuparray[i].userData.name == name) {

                            
                            //remove mesh from markup scene
                            markuparray[i].geometry.dispose();

                            if(markuparray[i].userData.type == "symbol"){
                                markuparray[i].material[0].dispose();
                                markuparray[i].material[1].dispose();
                                markuparray[i].material[2].dispose();
                                markuparray[i].material[3].dispose();
                                markuparray[i].material[4].dispose();
                                markuparray[i].material[5].dispose();
                            }

                            if(markuparray[i].userData.type == "dimension"){
                                markuparray[i].material.dispose();
                            }

                            if(markuparray[i].userData.type == "camera"){
                                markuparray[i].material.dispose();
                            }


                            delobjects.push(markuparray[i]);

                            //thispage.Vector3DPageObj
                        }
                        //thispage.Vector3DPageObj.scene.updateMatrix();
                        //Viewer3D.update();
                    }

                    for (var di = 0; di < delobjects.length; di++) {

                        if (thispage.Vector3DPageObj.getmarkupScene() != null) {
                            if (thispage.Vector3DPageObj.scene.children[markupsceneindex]) {
                                thispage.Vector3DPageObj.scene.children[markupsceneindex].remove(delobjects[di]);
                            }
                        }
    
                    }

                    delobjects = [];

                    if (RxCore_GUI_3DMarkupList != undefined) {
                        RxCore_GUI_3DMarkupList.setMarkupList(markuparray, DocObj.bActive);
                    }
                    this.Vector3DPageObj.updateblocklist();
                    return;


                };
                this.delete3DMarkup = function () {
                    //var mesharray = thispage.Vector3DPageObj.getMeshChildren();
                    //replace with get markup mesh array
                    var markuparray = thispage.Vector3DPageObj.getMarkupChildren();
                    var markupsceneindex = thispage.Vector3DPageObj.getmarkupScene();
                    if (markuparray.length == 0) {
                        return;
                    }
                    
                    var delobjects = [];

                    for (var i = 0; i < markuparray.length; i++) {
                        //var deleteid = markuparray[i].uuid;

                        if (markuparray[i].userData.selected) {

                            
                            //remove mesh from markup scene
                            markuparray[i].geometry.dispose();

                            if(markuparray[i].userData.type == "symbol"){
                                markuparray[i].material[0].dispose();
                                markuparray[i].material[1].dispose();
                                markuparray[i].material[2].dispose();
                                markuparray[i].material[3].dispose();
                                markuparray[i].material[4].dispose();
                                markuparray[i].material[5].dispose();
                            }

                            if(markuparray[i].userData.type == "dimension"){
                                markuparray[i].material.dispose();
                            }
                            delobjects.push(markuparray[i]);

                            //thispage.Vector3DPageObj
                        }
                        //thispage.Vector3DPageObj.scene.updateMatrix();
                        //Viewer3D.update();
                    }

                    for (var di = 0; di < delobjects.length; di++) {

                        if (thispage.Vector3DPageObj.getmarkupScene() != null) {
                            if (thispage.Vector3DPageObj.scene.children[markupsceneindex]) {
                                thispage.Vector3DPageObj.scene.children[markupsceneindex].remove(delobjects[di]);
                            }
                        }
    
                    }

                    delobjects = [];

                    if (RxCore_GUI_3DMarkupList != undefined) {
                        RxCore_GUI_3DMarkupList.setMarkupList(markuparray, DocObj.bActive);
                    }
                    this.Vector3DPageObj.updateblocklist();
                    return;
                };                
                
                this.setblockstateMeshAll = function (mesh, state) {
                    if (mesh.userData.visible == undefined) {
                        (mesh.visible) ? 1 : 0;
                    }
                    if (mesh.userData.tempvisible == undefined) {
                        mesh.userData.tempvisible = mesh.visible;
                    }
                    mesh.visible = state;
                    mesh.userData.tempvisible = state;
                };
                this.setblockstateMesh = function (mesh, name, state, bUseCallback) {
                    var found = false;
                    if (mesh.userData.visible == undefined) {
                        (mesh.visible) ? 1 : 0;
                    }
                    if (mesh.userData.tempvisible == undefined) {
                        mesh.userData.tempvisible = mesh.visible;
                    }
                    if (mesh.name == name) {
                        found = true;
                        mesh.visible = state;
                        mesh.userData.tempvisible = state;
                        /*if (!mesh.visible) {
                            mesh.visible = true;
                            mesh.userData.tempvisible = true;
                            //this.Vector3DPageObj.partlist[thispage.Vector3DPageObj.scene.children[modelidx].children[i].name].tempvisible = true;
                        }
                        else {
                            mesh.visible = false;
                            mesh.userData.tempvisible = false;
                            //this.Vector3DPageObj.partlist[thispage.Vector3DPageObj.scene.children[modelidx].children[i].name].tempvisible = false;
                        }*/
                        //thispage.Vector3DPageObj.scene.updateMatrix();
                        //Viewer3D.update();

                        if (Rxcore_GUI_3DPartState != undefined && bUseCallback) {
                            Rxcore_GUI_3DPartState.set3DPartInfo(mesh.userData);
                        }


                        /*if (Rxcore_GUI_3DPartInfo != undefined && bUseCallback) {
                            Rxcore_GUI_3DPartInfo.set3DPartInfo(mesh.userData);
                        }*/
                        //return;
                    }
                    return found;
                };

                this.setStateMeshGroup = function(mesh, name, state, bgroup ,bUseCallback){
                    var found = false;

                    if(mesh.userData.visible == undefined){
                        (mesh.visible) ? 1 : 0;
                    }
                    if (mesh.userData.tempvisible == undefined){
                        mesh.userData.tempvisible = mesh.visible;
                    }
                    if(bgroup){
                        //var selectname = name.substring(0, name.length - 2);
                        //var meshname = mesh.name.substring(0, name.length - 2);

                        var selectname = getmeshName(name, mesh.type);
                        var meshname = getmeshName(mesh.name, mesh.type);


                    }else{
                        meshname = mesh.name;
                        selectname = name;
                    }

                    if (meshname == selectname) {
                        found = true;

                        mesh.visible = state;
                        mesh.userData.tempvisible = state;

                        /*if (!mesh.visible) {
                            mesh.visible = true;
                            mesh.userData.tempvisible = true;
                            //this.Vector3DPageObj.partlist[thispage.Vector3DPageObj.scene.children[modelidx].children[i].name].tempvisible = true;
                        }
                        else {
                            mesh.visible = false;
                            mesh.userData.tempvisible = false;
                            //this.Vector3DPageObj.partlist[thispage.Vector3DPageObj.scene.children[modelidx].children[i].name].tempvisible = false;
                        }*/
                        //thispage.Vector3DPageObj.scene.updateMatrix();
                        //Viewer3D.update();

                        if(mesh.userData.blockRef){
                            var partattribs = thispage.Vector3DPageObj.blocklist[mesh.userData.blockRef];
                        }else{
                            thispage.Vector3DPageObj.setBlockRef(mesh);
                            partattribs = thispage.Vector3DPageObj.blocklist[mesh.userData.blockRef];   
                        }


                        if(partattribs != undefined){

                            partattribs.visible = mesh.userData.tempvisible;
                            Rxcore_GUI_3DPartState.set3DPartInfo(partattribs);

                        }else{
                            if (Rxcore_GUI_3DPartState != undefined && bUseCallback) {
                                Rxcore_GUI_3DPartState.set3DPartInfo(mesh.userData);
                            }
    
                        }


                        //return;
                    }
                    return found;
                };


                this.turnbloconoffMesh = function (mesh, name, bgroup ,bUseCallback) {
                    var found = false;
                    if (mesh.userData.visible == undefined) {
                        (mesh.visible) ? 1 : 0;
                    }
                    if (mesh.userData.tempvisible == undefined) {
                        mesh.userData.tempvisible = mesh.visible;
                    }
                    if(bgroup){
                        //var selectname = name.substring(0, name.length - 2);
                        //var meshname = mesh.name.substring(0, name.length - 2);

                        var selectname = name;
                        var meshname = getmeshName(mesh.name, mesh.type);


                    }else{
                        meshname = mesh.name;
                        selectname = name;
                    }                    
                    if (meshname == selectname) {
                        found = true;
                        if (!mesh.visible) {
                            mesh.visible = true;
                            mesh.userData.tempvisible = true;
                            //this.Vector3DPageObj.partlist[thispage.Vector3DPageObj.scene.children[modelidx].children[i].name].tempvisible = true;
                        }
                        else {
                            mesh.visible = false;
                            mesh.userData.tempvisible = false;
                            //this.Vector3DPageObj.partlist[thispage.Vector3DPageObj.scene.children[modelidx].children[i].name].tempvisible = false;
                        }
                        //thispage.Vector3DPageObj.scene.updateMatrix();
                        //Viewer3D.update();

                        if(mesh.userData.blockRef){
                            var partattribs = thispage.Vector3DPageObj.blocklist[mesh.userData.blockRef];
                        }else{
                            thispage.Vector3DPageObj.setBlockRef(mesh);
                            partattribs = thispage.Vector3DPageObj.blocklist[mesh.userData.blockRef];   
                        }


                        if(partattribs != undefined){

                            partattribs.visible = mesh.userData.tempvisible;
                            Rxcore_GUI_3DPartState.set3DPartInfo(partattribs);

                        }else{
                            if (Rxcore_GUI_3DPartState != undefined && bUseCallback) {
                                Rxcore_GUI_3DPartState.set3DPartInfo(mesh.userData);
                            }
    
                        }

                        /*if (Rxcore_GUI_3DPartInfo != undefined && bUseCallback) {
                            Rxcore_GUI_3DPartInfo.set3DPartInfo(mesh.userData);
                        }*/
                        //return;
                    }
                    return found;
                };
                this.set3DBlockStateNewAllArray = function(mesharray, state){
                    

                    for (var i = 0; i < mesharray.length; i++) {
                        if (mesharray[i].type === "Group") {
                            for (var j = 0; j < mesharray[i].children.length; j++) {
                                thispage.setblockstateMeshAll(mesharray[i].children[j], state);
                            }
                        }else{

                            thispage.setblockstateMeshAll(mesharray[i], state);
                        }
                    }
                };
                this.set3DBlockStateNewAll = function(state, bUseCallback){
                    var mesharray = thispage.Vector3DPageObj.getMeshChildren();

                    if (mesharray.length == 0) {
                        return;
                    }

                    thispage.set3DBlockStateNewAllArray(mesharray, state);


                    var addedmesharray = DocObj.getCurrentpage().Vector3DPageObj.getAddedMeshChildren();

                    for (var i = 0; i < addedmesharray.length; i++) {
                        
                        thispage.set3DBlockStateNewAllArray(addedmesharray[i], state);
                        
                    }


                    
                };
                this.set3DBlockStateOld = function (name, state, bUseCallback) {
                    var mesharray = thispage.Vector3DPageObj.getMeshChildren();
                    if (mesharray.length == 0) {
                        return;
                    }
                    for (var i = 0; i < mesharray.length; i++) {
                        if (mesharray[i].type === "Group") {
                            for (var j = 0; j < mesharray[i].children.length; j++) {
                                if (thispage.setblockstateMesh(mesharray[i].children[j], name, state), bUseCallback) {
                                    break;
                                }
                            }
                        }
                        if (thispage.setblockstateMesh(mesharray[i], name, state), bUseCallback) {
                            break;
                        }
                    }
                    //disconnected to prevent property page updating.
                    //this.Vector3DPageObj.updateblocklist();
                };

                this.set3DBlockStateNewGroup = function (name, state, bUseCallback) {

                    var mesharray = thispage.Vector3DPageObj.getMeshChildren();

                    if (mesharray.length == 0) {
                        return;
                    }

                    //setStateMeshGroup

                    thispage.set3DBlockstateGroupAll(mesharray, name, state, bUseCallback);

                    //thispage.turn3DBlockOnOffNewAll(mesharray, name, bUseCallback);

                    var addedmesharray = DocObj.getCurrentpage().Vector3DPageObj.getAddedMeshChildren();

                    for (var i = 0; i < addedmesharray.length; i++) {
                        thispage.set3DBlockstateGroupAll(addedmesharray[i], name, state, bUseCallback);
                    }

                    if(bUseCallback){
                        this.Vector3DPageObj.updateblocklist();  
                    }



                };


                this.set3DBlockStateNew = function (name, state, bUseCallback) {
                    var meshobject = thispage.Vector3DPageObj.getMeshByName(name);
                    if (meshobject == null) {
                        return;
                    }
                    else {
                        if (bUseCallback) {
                            var found = thispage.setblockstateMesh(meshobject, name, state, bUseCallback);
                        }
                        else {
                            thispage.setblockstateMeshAll(meshobject, state);
                        }
                    }
                    //disconnected to prevent property page updating.
                    //this.Vector3DPageObj.updateblocklist();
                    if (found && bUseCallback) {
                        this.Vector3DPageObj.updateblocklist();
                    }
                };

                this.set3DBlockstateGroupAll = function(mesharray, name, state, bUseCallback){ 


                    var bfound = false;

                    for (var i = 0; i < mesharray.length; i++) {
                        if (mesharray[i].type === "Group") {
                            for (var j = 0; j < mesharray[i].children.length; j++) {
                                
                                
                                if (thispage.setStateMeshGroup(mesharray[i].children[j], name, state, true, bUseCallback)) {
                                    bfound = true;
                                }
                            }
                        }
                        
                        if (bfound){
                            break;
                        }

                        if (thispage.setStateMeshGroup(mesharray[i], name, state, false, bUseCallback)) {
                            break;
                        }
                    }



                };

                this.turn3DBlockOnOffNewAll = function(mesharray, name, bUseCallback){


                    var bfound = false;

                    for (var i = 0; i < mesharray.length; i++) {
                        if (mesharray[i].type === "Group") {
                            for (var j = 0; j < mesharray[i].children.length; j++) {
                                
                                if (thispage.turnbloconoffMesh(mesharray[i].children[j], name, true, bUseCallback)) {
                                    bfound = true;
                                }
                            }
                        }
                        
                        if (bfound){
                            break;
                        }
                        if (thispage.turnbloconoffMesh(mesharray[i], name, false, bUseCallback)) {
                            break;
                        }
                    }
                };
                this.turn3DBlockOnOffNew = function (name, bUseCallback) {
                    var mesharray = thispage.Vector3DPageObj.getMeshChildren();

                    if (mesharray.length == 0) {
                        return;
                    }
                    
                    thispage.turn3DBlockOnOffNewAll(mesharray, name, bUseCallback);

                    var addedmesharray = DocObj.getCurrentpage().Vector3DPageObj.getAddedMeshChildren();

                    for (var i = 0; i < addedmesharray.length; i++) {
                        thispage.turn3DBlockOnOffNewAll(addedmesharray[i], name, bUseCallback);
                    }


                    this.Vector3DPageObj.updateblocklist();
                };
                this.turn3DBlockOnOffOld = function (name) {
                    for (var i = 0; i < thispage.Vector3DPageObj.scene.children.length; i++) {
                        if (thispage.Vector3DPageObj.scene.children[i].name == name) {
                            if (!thispage.Vector3DPageObj.scene.children[i].visible) {
                                thispage.Vector3DPageObj.scene.children[i].visible = true;
                                this.Vector3DPageObj.partlist[thispage.Vector3DPageObj.scene.children[i].name].tempvisible = true;
                            }
                            else {
                                thispage.Vector3DPageObj.scene.children[i].visible = false;
                                this.Vector3DPageObj.partlist[thispage.Vector3DPageObj.scene.children[i].name].tempvisible = false;
                            }
                            //thispage.Vector3DPageObj.scene.updateMatrix();
                            //Viewer3D.update();
                            if (Rxcore_GUI_3DPartInfo != undefined) {
                                Rxcore_GUI_3DPartInfo.set3DPartInfo(this.Vector3DPageObj.partlist[name]);
                            }
                            this.Vector3DPageObj.updateblocklist();
                            return;
                        }
                    }
                    //Viewer3D.update();
                };
                this.restoreBlockStates = function () {
                    thispage.VectorPageObj.restoreBlockStates();
                    /*if (thispage.usedincomposite && thispage.compositereference != undefined) {
                        //this.compositereference.draw_compare(false);
                        thispage.compositereference.scaleToBackground(false);
                    }
                    else {
                        
                    }*/
                    thispage.draw_vector(false);
                    thispage.vectorischanged = true;
                    thispage.DocRef.refreshBirdsEye();
                    thispage.DocRef.renderBirdsEye();
                };
                this.resetBlocks = function () {
                    thispage.VectorPageObj.resetBlocks();
                    /*if (thispage.usedincomposite && thispage.compositereference != undefined) {
                        //this.compositereference.draw_compare(false);
                        thispage.compositereference.scaleToBackground(false);
                    }
                    else {
                        
                    }*/

                    thispage.draw_vector(false);
                    thispage.vectorischanged = true;
                    thispage.DocRef.refreshBirdsEye();
                    thispage.DocRef.renderBirdsEye();
                };
                this.reset3DParts = function () {
                    if (thispage.usevector3DGLTF) {
                        thispage.reset3DPartsnew();
                    }
                    else {
                        thispage.reset3DPartsold();
                    }
                };
                this.resetMesh = function (mesh) {
                    var meshindex = 0;
                    if (mesh.userData.visible == undefined) {
                        (mesh.visible) ? 1 : 0;
                    }
                    if (mesh.userData.tempvisible == undefined) {
                        mesh.userData.tempvisible = mesh.visible;
                    }
                    if (!mesh.userData.isSpace) {
                        //this.Vector3DPageObj.partlist[thispage.Vector3DPageObj.scene.children[modelidx].children[i].name].resetvisible();
                        if (mesh.userData.visible) {
                            mesh.userData.tempvisible = (mesh.userData.visible == 1);
                        }
                        else {
                            //mesharray[i].userData.tempvisible = (mesharray[i].userData.visible == 1);
                            mesh.userData.visible = (mesh.visible) ? 1 : 0;
                        }
                        //scope.tempvisible = (scope.visible == 1);
                        if (mesh.userData.tempvisible) {
                            mesh.visible = mesh.userData.tempvisible;
                        }
                        else {
                            mesh.userData.tempvisible = mesh.visible;
                        }
                        mesh.userData.selected = false;
                        mesh.userData.usetempfacecolor = false;
                        if (mesh.userData.facecolor != undefined) {
                            if (mesh.type == "LineSegments") {
                                //console.log(mesh.userData.facecolor);
                            }
                            mesh.material.color.setHex(mesh.userData.facecolor);
                        }
                        if (mesh.userData.meshcolors) {
                            if (mesh.userData.meshcolors.length > 1) {
                                mesh.material.color = new THREE.Color(mesh.userData.meshcolors[meshindex]);
                                if (meshindex < mesh.userData.meshcolors.length) {
                                    meshindex++;
                                }
                            }
                            else if (mesh.userData.facecolor) {
                                mesh.material.color = setHex(mesh.userData.facecolor);
                            }
                        }
                    }
                };                

                this.reset3DPartsnewAll = function(mesharray){

                    for (var i = 0; i < mesharray.length; i++) {
                        if (mesharray[i].type == "Group") {
                            for (var j = 0; j < mesharray[i].children.length; j++) {
                                if ( mesharray[i].children[j].type == "Mesh" || mesharray[i].children[j].type == "LineSegments") {
                                    thispage.resetMesh(mesharray[i].children[j]);
                                }
                            }
                        }
                        
                        if (mesharray[i].type == "Mesh" || mesharray[i].type == "LineSegments") {
                            thispage.resetMesh(mesharray[i]);
                            //thispage.Vector3DPageObj.scene.children[i].visible = this.Vector3DPageObj.partlist[thispage.Vector3DPageObj.scene.children[i].name].tempvisible;
                            //thispage.Vector3DPageObj.partlist[thispage.Vector3DPageObj.scene.children[i].name].tempvisible = thispage.Vector3DPageObj.partlist[thispage.Vector3DPageObj.scene.children[i].name].visible;
                        }
                    }


                };
                this.reset3DPartsnew = function () {
                    var mesharray = thispage.Vector3DPageObj.getMeshChildren();
                    if (mesharray.length == 0) {
                        return;
                    }

                    thispage.reset3DPartsnewAll(mesharray);
                    
                    var addedmesharray = DocObj.getCurrentpage().Vector3DPageObj.getAddedMeshChildren();

                    for (var i = 0; i < addedmesharray.length; i++) {
                        thispage.reset3DPartsnewAll(addedmesharray[i]);
                    }

                    //Viewer3D.update();
                    this.Vector3DPageObj.updateblocklist();
                };
                this.reset3DPartsold = function () {
                    var meshindex = 0;
                    for (var i = 0; i < thispage.Vector3DPageObj.scene.children.length; i++) {
                        if (thispage.Vector3DPageObj.scene.children[i].type == "Mesh") {
                            if (!this.Vector3DPageObj.partlist[thispage.Vector3DPageObj.scene.children[i].name].isSpace) {
                                this.Vector3DPageObj.partlist[thispage.Vector3DPageObj.scene.children[i].name].resetvisible();
                                thispage.Vector3DPageObj.scene.children[i].visible = this.Vector3DPageObj.partlist[thispage.Vector3DPageObj.scene.children[i].name].tempvisible;
                                thispage.Vector3DPageObj.partlist[thispage.Vector3DPageObj.scene.children[i].name].selected = false;
                                if (thispage.Vector3DPageObj.partlist[thispage.Vector3DPageObj.scene.children[i].name].meshcolors.length > 1) {
                                    thispage.Vector3DPageObj.scene.children[i].material.color = new THREE.Color(this.Vector3DPageObj.partlist[thispage.Vector3DPageObj.scene.children[i].name].meshcolors[meshindex]);
                                    if (meshindex < thispage.Vector3DPageObj.partlist[thispage.Vector3DPageObj.scene.children[i].name].meshcolors.length) {
                                        meshindex++;
                                    }
                                }
                                else {
                                    thispage.Vector3DPageObj.scene.children[i].material.color = new THREE.Color(this.Vector3DPageObj.partlist[thispage.Vector3DPageObj.scene.children[i].name].facecolor);
                                }
                            }
                            //thispage.Vector3DPageObj.scene.children[i].visible = this.Vector3DPageObj.partlist[thispage.Vector3DPageObj.scene.children[i].name].tempvisible;
                            //thispage.Vector3DPageObj.partlist[thispage.Vector3DPageObj.scene.children[i].name].tempvisible = thispage.Vector3DPageObj.partlist[thispage.Vector3DPageObj.scene.children[i].name].visible;
                        }
                    }
                    //Viewer3D.update();
                    this.Vector3DPageObj.updateblocklist();
                };

                this.deleteVectorAttribute = function(szAttribName){
                    
                    if (thispage.usevectorxml) {
                        thispage.VectorPageObj.deleteAttribute(szAttribName);
                    }
                    
                };


                /*this.reset3DParts = function () {
                    var meshindex = 0;
                    for (var i = 0; i < thispage.Vector3DPageObj.scene.children.length; i++) {
                        if (thispage.Vector3DPageObj.scene.children[i].type == "Mesh") {
        
                            if (!this.Vector3DPageObj.partlist[thispage.Vector3DPageObj.scene.children[i].name].isSpace) {
                                this.Vector3DPageObj.partlist[thispage.Vector3DPageObj.scene.children[i].name].resetvisible();
                                thispage.Vector3DPageObj.scene.children[i].visible = this.Vector3DPageObj.partlist[thispage.Vector3DPageObj.scene.children[i].name].tempvisible;
                                thispage.Vector3DPageObj.partlist[thispage.Vector3DPageObj.scene.children[i].name].selected = false;
                                if (thispage.Vector3DPageObj.partlist[thispage.Vector3DPageObj.scene.children[i].name].meshcolors.length > 1) {
                                    thispage.Vector3DPageObj.scene.children[i].material.color = new THREE.Color(this.Vector3DPageObj.partlist[thispage.Vector3DPageObj.scene.children[i].name].meshcolors[meshindex]);
                                    if (meshindex < thispage.Vector3DPageObj.partlist[thispage.Vector3DPageObj.scene.children[i].name].meshcolors.length) {
                                        meshindex++;
                                    }
        
                                } else {
                                    thispage.Vector3DPageObj.scene.children[i].material.color = new THREE.Color(this.Vector3DPageObj.partlist[thispage.Vector3DPageObj.scene.children[i].name].facecolor);
                                }
        
                            }
                            //thispage.Vector3DPageObj.scene.children[i].visible = this.Vector3DPageObj.partlist[thispage.Vector3DPageObj.scene.children[i].name].tempvisible;
                            //thispage.Vector3DPageObj.partlist[thispage.Vector3DPageObj.scene.children[i].name].tempvisible = thispage.Vector3DPageObj.partlist[thispage.Vector3DPageObj.scene.children[i].name].visible;
        
                        }
        
                    }
        
                    //Viewer3D.update();
                    this.Vector3DPageObj.updateblocklist();
        
                };*/
                
                this.appendCustomBlockAttribute = function (blockid, name, value) {
                    if (thispage.usevectorxml) {
                        thispage.VectorPageObj.appendCustomBlockAttribute(blockid, name, value);
                    }
                };
                this.setBlockColor = function (blockid, Color, override) {
                    if (thispage.usevectorxml) {
                        thispage.VectorPageObj.setBlockColor(blockid, Color, override);
                        thispage.draw_vector(false);
                        thispage.vectorischanged = true;
                    }
                };
                this.setBlockHatch = function(blockid , hatchname, override){
                    if (thispage.usevectorxml) {
                        thispage.VectorPageObj.setBlockHatch(blockid, hatchname, override);
                        thispage.draw_vector(false);
                        thispage.vectorischanged = true;
                    }
                    
                };
                this.FindBlockByAttr = function (attrname, attrvalue) {
                    if (thispage.usevectorxml) {
                        return thispage.VectorPageObj.FindBlockByAttr(attrname, attrvalue);
                    }
                };
                this.search3DAttribute = function (expr) {
                    return thispage.Vector3DPageObj.searchAttribute(expr);
                };
                this.get3dVectorBlocks = function () {
                    var meshids = [];
                    return thispage.Vector3DPageObj.getmeshUserData(meshids, true);
                };
                this.search3DAttributesEx = function(expr){
                    return thispage.Vector3DPageObj.searchAttributeJSONEx(expr);
                };
                this.search3DAttributes = function (expr) {
                    return thispage.Vector3DPageObj.searchAttributeJSON(expr);
                };
                this.getstoreyNames = function(){
                    
                    var storeynames = null;

                    if(thispage.Vector3DPageObj){
                        storeynames = thispage.Vector3DPageObj.getstoreyNames();
                    }
                    return storeynames;

                };

                this.getStoreyRealIDs = function(storeyname){
                    return thispage.Vector3DPageObj.getStoreyRealIDs(storeyname);
                };
                this.getStoreyIDs = function(storeyname){
                    return thispage.Vector3DPageObj.getStoreyIDs(storeyname);
                };
                this.getStoreyAttributes = function(storeyname){
                    return thispage.Vector3DPageObj.getAttributeStorey(storeyname);
                };
                this.searchStoreyAttributesEx = function (storeyname, expr) {

                    return thispage.Vector3DPageObj.searchAttributeStoreyEx(storeyname,expr);
                };
                this.searchStoreyAttributes = function (storeyname, expr) {

                    return thispage.Vector3DPageObj.searchAttributeStorey(storeyname,expr);
                };

                this.set3DBlockTransparency = function (blockid, value, override) {
                    thispage.Vector3DPageObj.setBlockTransparency(blockid, value, override);
                };
                this.set3DBlockNoSelect = function (blockid, override){
                    thispage.Vector3DPageObj.setBlockNoSelect(blockid, override);
                };

                this.reset3DBlockColorsAll = function(mesharray){

                    for (var i = 0; i < mesharray.length; i++) {
                        
                        if (mesharray[i].type == "Group") {
                            for (var j = 0; j < mesharray[i].children.length; j++) {
                                if ( mesharray[i].children[j].type == "Mesh") {
                                    
                                    if(mesharray[i].children[j].userData.facecolor == undefined){
                                        
                                        
                                        mesharray[i].children[j].userData.facecolor = mesharray[i].children[j].material.color.getHex();
                                    }else{

                                        mesharray[i].children[j].material.color.setHex(mesharray[i].children[j].userData.facecolor);
                                    }

                                }
                            }
                        }else  if ( mesharray[i].type == "Mesh") {
                            if (mesharray[i].name === "") {
                                continue;
                            }
                                
                            if(mesharray[i].userData.facecolor == undefined){
                                
                                mesharray[i].userData.facecolor = mesharray[i].material.color.getHex();
                                
                            }else{
                                mesharray[i].material.color.setHex(mesharray[i].userData.facecolor);
                            }
                            
                        }

                    }

                };
                this.reset3DBlockColors = function(){
                    var mesharray = thispage.Vector3DPageObj.getMeshChildren();
                    if (mesharray.length == 0) {
                        return;
                    }
                    thispage.reset3DBlockColorsAll(mesharray);

                    var addedmesharray = DocObj.getCurrentpage().Vector3DPageObj.getAddedMeshChildren();

                    if (addedmesharray.length == 0) {
                        return;
                    }

                    for (var i = 0; i < addedmesharray.length; i++) {
                        thispage.reset3DBlockColorsAll(addedmesharray[i]);
                    }
                };

                this.set3DBlockColorOld = function (name, color, override) {
                    var mesharray = thispage.Vector3DPageObj.getMeshChildren();
                    if (mesharray.length == 0) {
                        return;
                    }
                    for (var i = 0; i < mesharray.length; i++) {
                        if (mesharray[i].type == "Group") {
                            for (var j = 0; j < mesharray[i].children.length; j++) {
                                if (mesharray[i].children[j].type == "Mesh") {
                                    if (mesharray[i].children[j].userData.facecolor == undefined) {
                                        mesharray[i].children[j].userData.facecolor = mesharray[i].children[j].material.color.getHex();
                                    }
                                    if (mesharray[i].children[j].name == name && mesharray[i].children[j].visible) {
                                        if (override) {
                                            mesharray[i].children[j].material.color = new THREE.Color(color);
                                        }
                                        else {
                                            mesharray[i].children[j].material.color.setHex(mesharray[i].children[j].userData.facecolor);
                                        }
                                    }
                                }
                            }
                        }
                        else if (mesharray[i].type == "Mesh") {
                            if (mesharray[i].name === "") {
                                continue;
                            }
                            if (mesharray[i].userData.facecolor == undefined) {
                                mesharray[i].userData.facecolor = mesharray[i].material.color.getHex();
                            }
                            if (mesharray[i].name == name && mesharray[i].visible) {
                                if (override) {
                                    mesharray[i].material.color = new THREE.Color(color);
                                }
                                else {
                                    mesharray[i].material.color.setHex(mesharray[i].userData.facecolor);
                                }
                            }
                        }
                    }
                };
                this.set3DBlockDistance = function(name, distance, direction, override){
                    var meshobject = thispage.Vector3DPageObj.getMeshByName(name);

                    var directionvector = new THREE.Vector3(direction.x, direction.y, direction.z).normalize();
                    directionvector.multiplyScalar ( distance );

                    var positionvector = new THREE.Vector3();


                    if(meshobject == null){
                        return;
                    }else{
                        if(meshobject.userData.posX == undefined){
                            meshobject.userData.posX = meshobject.position.x;
                        }
                        if(meshobject.userData.posY == undefined){
                            meshobject.userData.posY = meshobject.position.y;
                        }
                        if(meshobject.userData.posZ == undefined){
                            meshobject.userData.posZ = meshobject.position.z;
                        }



                        if (meshobject.visible) {
                            if (override) {
                                
                                //positionvector.set(position.x, position.y, position.z);
                                meshobject.position.add(directionvector);

                            }
                            else {
                                positionvector.set(meshobject.userData.posX,meshobject.userData.posY,meshobject.userData.posZ);
                                meshobject.position.copy(positionvector);
                                
                            }
                        }

                    }

                };                
                this.set3DBlockPosition = function(name, position, override){
                    var meshobject = thispage.Vector3DPageObj.getMeshByName(name);
                    var positionvector = new THREE.Vector3();

                    if(meshobject == null){
                        return;
                    }else{
                        if(meshobject.userData.posX == undefined){
                            meshobject.userData.posX = meshobject.position.x;
                        }
                        if(meshobject.userData.posY == undefined){
                            meshobject.userData.posY = meshobject.position.y;
                        }
                        if(meshobject.userData.posZ == undefined){
                            meshobject.userData.posZ = meshobject.position.z;
                        }

                        if (meshobject.visible) {
                            if (override) {
                                
                                positionvector.set(position.x, position.y, position.z);
                                meshobject.position = positionvector.clone();

                            }
                            else {
                                positionvector.set(meshobject.userData.posX,meshobject.userData.posY,meshobject.userData.posZ);
                                meshobject.position = positionvector.clone();
                                
                            }
                        }

                    }

                };                
                this.set3DBlockColor = function (name, color, override) {
                    var meshobject = thispage.Vector3DPageObj.getMeshByName(name);
                    if (meshobject == null) {
                        return;
                    }
                    else {
                        if (meshobject.userData.facecolor == undefined) {
                            meshobject.userData.facecolor = meshobject.material.color.getHex();
                        }
                        if (meshobject.visible) {
                            if (override) {
                                meshobject.material.color = new THREE.Color(color);
                                meshobject.userData.tempfacecolor = meshobject.material.color.getHex();
                                meshobject.userData.usetempfacecolor = true;

                            }
                            else {
                                if (meshobject.userData.facecolor != undefined) {
                                    meshobject.material.color.setHex(meshobject.userData.facecolor);
                                    meshobject.userData.usetempfacecolor = false;
                                }
                            }
                        }
                    }
                };
                /*this.set3DBlockColor = function (name, color, override){
                    for (var i = 0; i < thispage.Vector3DPageObj.scene.children.length; i++) {
                        if(thispage.Vector3DPageObj.scene.children[i].name === ""){
                            continue;
                        }
                        if (thispage.Vector3DPageObj.scene.children[i].name == name && thispage.Vector3DPageObj.scene.children[i].visible) {
                            if (override){
                                thispage.Vector3DPageObj.scene.children[i].material.color = new THREE.Color(color);
                            }else{
                                thispage.Vector3DPageObj.scene.children[i].material.color = new THREE.Color(this.Vector3DPageObj.partlist[thispage.Vector3DPageObj.scene.children[i].name].facecolor);
                            }
        
                        }
                    }
                };*/
                this.getScreenPoint = function (insertpoint) {
                    return thispage.Vector3DPageObj.pointToScreen(insertpoint);
                };
                this.add3DSymbol = function (insertpoint, color) {
                    thispage.Vector3DPageObj.add3DSymbol(insertpoint, color);
                };
                this.get3DCamerasJSON = function(){
                    var stringData = null;
                    var cameraArray = thispage.Vector3DPageObj.getCameraSaves();
                    var saveArray = [];
                    
                    for(var csi = 0;csi < cameraArray.length; csi++){

                        //var camobj = {position: pageobject.camera.matrix.toArray(), name: sphere.name, tool: pageobject.DocRef.curcontrol3D, thumbnail: null, width: 0, height: 0, image: base64String};

                        //var camobj = {position: cameraArr[i].position, name: cameraArr[i].name, tool: cameraArr[i].tool, thumbnail: null, width: 0, height: 0, image: document.createElement('img'), selectedmesharray : cameraArr[i].selectedmesharray};

                        if(cameraArray[csi].name != "resetView"){
                            //var camobj = {position: null, name: null, tool: null, explodefactor : 0, thumbnail: null, width: 0, height: 0, image: null, selectedmesharray : []};

                            var camobj = {
                                position: null, 
                                name: null, 
                                tool: null, 
                                explodefactor : 0, 
                                clipping : null, 
                                thumbnail: null, 
                                width: 0, 
                                height: 0, 
                                image: null, 
                                selectedmesharray : [],
                                attributes : []
                            };
        

                            camobj.position = cameraArray[csi].position;
                            camobj.name = cameraArray[csi].name;
                            camobj.tool = cameraArray[csi].tool;

                            camobj.explodefactor = cameraArray[csi].explodefactor;
                            camobj.clipping = cameraArray[csi].clipping;
                            camobj.attributes = cameraArray[csi].attributes;
    
                            //camobj.thumbnail = getBase64ImageString(cameraArray[csi].thumbnail);
                            //camobj.width = cameraArray[csi].width;
                            //camobj.height = cameraArray[csi].height;
                            camobj.image = getBase64ImageString(cameraArray[csi].image);
    
                            if(cameraArray[csi].selectedmesharray){
                                camobj.selectedmesharray = cameraArray[csi].selectedmesharray.slice();
                            }
    
    
                            saveArray.push(camobj);
    
                        }

                    }
                    

                    if (saveArray.length > 0) {
                        stringData = JSON.stringify(saveArray);
                    }
                    
                    return stringData;

                };
                this.get3DMarkupJSON = function () {
                    var stringData = null;
                    var markuparray = thispage.Vector3DPageObj.getMarkupChildren();
                    var markupsceneindex = thispage.Vector3DPageObj.getmarkupScene();
                    if (markuparray.length == 0) {
                        return;
                    }
                    if (thispage.Vector3DPageObj.getmarkupScene() != null) {
                        stringData = JSON.stringify(thispage.Vector3DPageObj.scene.children[markupsceneindex].toJSON());
                    }
                    return stringData;
                };
                this.select3DBlock = function (name, bUseCallback, bAdded) {
                    if (thispage.usevector3DGLTF) {
                        thispage.select3DBlocknew(name, bUseCallback, bAdded);
                    }
                    else {
                        thispage.select3DBlockold(name);
                    }
                };                
                this.show3DViewMarker = function(onoff){
                    var markuparray = thispage.Vector3DPageObj.getMarkupChildren();
                    if (markuparray.length == 0) {
                        return;
                    }
                    for (var i = 0; i < markuparray.length; i++) {

                        if(markuparray[i].userData.type == "camera"){

                            //var pos = thispage.Vector3DPageObj.get3DViewPos(markuparray[i].userData.name);
                            //set3DViewPos

                           thispage.Vector3DPageObj.set3DViewPos(markuparray[i]);

                            markuparray[i].visible = onoff;
                            //markuparray[i].position.copy(pos);
                            //markuparray[i].updateMatrixWorld();

                        }
                        
                    }

                    //thispage.Vector3DPageObj.scene.updateMatrixWorld();

                    /*if (thispage.Vector3DPageObj.getmarkupScene() != null) {
                        thispage.Vector3DPageObj.getmarkupScene().updateMatrixWorld();    
                    }*/

                    //getmarkupScene()
                    //thispage.Vector3DPageObj.scene.updateMatrixWorld();


                };

                
                this.show3Dmarkup = function(onoff){
                    var markuparray = thispage.Vector3DPageObj.getMarkupChildren();
                    if (markuparray.length == 0) {
                        return;
                    }
                    for (var i = 0; i < markuparray.length; i++) {

                        if(markuparray[i].userData.type != "camera"){
                            markuparray[i].visible = onoff;
                        }
                                                
                    }

                }; 
                this.unSelect3DmarkupAll = function(){
                    var markuparray = thispage.Vector3DPageObj.getMarkupChildren();
                    if (markuparray.length == 0) {
                        return;
                    }
                    for (var i = 0; i < markuparray.length; i++) {
                        markuparray[i].userData.selected = false;

                        if(markuparray[i].userData.type == "symbol"){
                            markuparray[i].material[0].transparent = true;
                            markuparray[i].material[1].transparent = true;
                            markuparray[i].material[2].transparent = true;
                            markuparray[i].material[3].transparent = true;
                            markuparray[i].material[4].transparent = true;
                            markuparray[i].material[5].transparent = true;
                        }
                        if(markuparray[i].userData.type == "camera"){

                            if (markuparray[i].userData.facecolor != undefined) {
                                markuparray[i].material.color.setHex(markuparray[i].userData.facecolor);
                            }
    
                            //markuparray[i].material.color = new THREE.Color(0xf781f3);
                        }

                        if(markuparray[i].userData.type == "dimension"){
                            //unselect dimension line as well in a separate function.
    
                            thispage.select3Ddimline(markuparray[i].uuid, false);
                            
    
                        }


                    }

                    if (RxCore_GUI_3DMarkupList != undefined) {
                        RxCore_GUI_3DMarkupList.setMarkupList(markuparray, DocObj.bActive);

                    }


                };
                this.select3Ddimline = function(objectid, bselect){

                    var markuparray = thispage.Vector3DPageObj.getMarkupChildren();

                    if (markuparray.length == 0) {
                        return;
                    }

                    for (var i = 0; i < markuparray.length; i++) {
                        if(markuparray[i].userData.type == "dimension"){
                            if(markuparray[i].userData.parentref == objectid && markuparray[i].type == "LineSegments"){
                                
                                markuparray[i].userData.selected = bselect;
                    
                            }
                        }


                    }
                };

                this.resize3DSymbol = function(){

                    var markuparray = thispage.Vector3DPageObj.getMarkupChildren();
                    if (markuparray.length == 0) {
                        return;
                    }
                    for (var i = 0; i < markuparray.length; i++) {
                        
                        if (markuparray[i].userData.selected){
                            if(markuparray[i].userData.type == "symbol"){
                                //markuparray[i].scale 

                            }

                        }
                        
                    }

                };
                this.get3DResetView = function(){

                    var cameraimage = null;

                    if (thispage.usevector3DGLTF) {
                        if(thispage.Vector3DPageObj){
                            cameraimage = thispage.Vector3DPageObj.get3DResetView();
                        }
                    }
                    return cameraimage;
                };
                this.getCameraimage = function(name){

                    var cameraimage = undefined;

                    if (thispage.usevector3DGLTF) {
                        if(thispage.Vector3DPageObj){
                            cameraimage = thispage.Vector3DPageObj.getCameraimage(name);
                        }
                    }
                    return cameraimage;
                }; 

                this.restoreCameraByName = function(name){

                    if (thispage.usevector3DGLTF) {
                        if(thispage.Vector3DPageObj){
                            thispage.Vector3DPageObj.restoreCameraName(name);
                        }

                    }

                };

                this.set3DViewSelectedMesh = function(name){
                    if (thispage.usevector3DGLTF) {
                        if(thispage.Vector3DPageObj){

                            thispage.Vector3DPageObj.set3DViewSelectedMesh(name);
                        }

                    }

                };

                /*this.select3DBlock = function (name) {
        
                    this.Vector3DPageObj.setSelectedPart(-1);
                    for (var i = 0; i < thispage.Vector3DPageObj.scene.children.length; i++) {
        
                        if(thispage.Vector3DPageObj.scene.children[i].name === ""){
                            continue;
                        }
                        if(thispage.Vector3DPageObj.scene.children[i].name == name && isNaN(name)){
                            this.Vector3DPageObj.restoreCameraName(name);
                            //console.log(name);
                        }else{
                            if (thispage.Vector3DPageObj.scene.children[i].name == name && thispage.Vector3DPageObj.scene.children[i].visible) {
        
                                if (!this.Vector3DPageObj.partlist[name].selected) {
                                    thispage.Vector3DPageObj.scene.children[i].material.color = new THREE.Color(0xf781f3);
            
                                    this.Vector3DPageObj.partlist[name].selected = true;
                                    this.Vector3DPageObj.setSelectedPart(name);
                                    if (Rxcore_GUI_3DPartInfo != undefined) {
                                        Rxcore_GUI_3DPartInfo.set3DPartInfo(this.Vector3DPageObj.partlist[name]);
                                    }
            
                                } else {
                                    this.Vector3DPageObj.partlist[thispage.Vector3DPageObj.scene.children[i].name].selected = false;
                                    thispage.Vector3DPageObj.scene.children[i].material.color = new THREE.Color(this.Vector3DPageObj.partlist[thispage.Vector3DPageObj.scene.children[i].name].facecolor);
                                }
            
                            } else {
                                if (thispage.Vector3DPageObj.scene.children[i].type == "Mesh" && !isNaN(thispage.Vector3DPageObj.scene.children[i].name)) {
                                    if (thispage.Vector3DPageObj.scene.children[i].visible && this.Vector3DPageObj.partlist[thispage.Vector3DPageObj.scene.children[i].name].selected) {
                                        thispage.Vector3DPageObj.scene.children[i].material.color = new THREE.Color(this.Vector3DPageObj.partlist[thispage.Vector3DPageObj.scene.children[i].name].facecolor);
            
                                        this.Vector3DPageObj.partlist[thispage.Vector3DPageObj.scene.children[i].name].selected = false;
                                    }
            
                                }
            
                            }
                        }
                        
        
                    }
                    this.Vector3DPageObj.updateblocklist();
        
                };*/
                this.select3Dmarkup = function (id) {

                    if (!thispage.usevector3DGLTF) {
                        return;
                    }

                    if(!thispage.Vector3DPageObj){
                        return;
                    }

                    var markuparray = thispage.Vector3DPageObj.getMarkupChildren();
                    if (markuparray.length == 0) {
                        return;
                    }
                    for (var i = 0; i < markuparray.length; i++) {
                        if (markuparray[i].uuid === "") {
                            continue;
                        }
                        else {
                            //var bdimmatch = false;

                            /*if(markuparray[i].userData.parentref){
                                bdimmatch = (markuparray[i].userData.parentref == id);
                            }*/

                            if ((markuparray[i].uuid == id) && markuparray[i].visible) {
                                if (!markuparray[i].userData.selected) {
                                    /*if(!markuparray[i].userData.facecolor){
                                        markuparray[i].userData.facecolor = markuparray[i].material.color.getHex();
                                    }*/

                                    //markuparray[i].material.transparent = false;

                                    /*if(markuparray[i].userData.type == "dimension"  && markuparray[i].type == "LineSegments"){
                                        markuparray[i].material.color = new THREE.Color(0xf781f3);
                                    }*/

                                    if(markuparray[i].userData.type == "symbol"){
                                        markuparray[i].material[0].transparent = false;
                                        markuparray[i].material[1].transparent = false;
                                        markuparray[i].material[2].transparent = false;
                                        markuparray[i].material[3].transparent = false;
                                        markuparray[i].material[4].transparent = false;
                                        markuparray[i].material[5].transparent = false;
                                    }

                                    if(markuparray[i].userData.type == "dimension"){
                                        //select dimension line as well in a separate function.
                                        thispage.select3Ddimline(id, true);

                                    }
                                    if(markuparray[i].userData.type == "camera"){

                                        if (markuparray[i].userData.facecolor == undefined) {
                                            markuparray[i].userData.facecolor = markuparray[i].material.color.getHex();
                                        }
                                        markuparray[i].material.color = new THREE.Color(0xf781f3);
                                        //thispage.Vector3DPageObj.restoreCameraName(markuparray[i].name);
                                    }


                                    //markuparry[i].material.color = new THREE.Color(0xf781f3);
                                    //set transparency to false
                                    markuparray[i].userData.selected = true;

                                    var setpart = { name: markuparray[i].name, index: i, group: false, groupindex: -1, markup : true};
                                    thispage.Vector3DPageObj.setSelectedPart(setpart);
        
                                    //this.Vector3DPageObj.setSelectedPart(name);
                                    //this.Vector3DPageObj.setSelectedMarkup(id);
                                    if (Rxcore_GUI_3DMarkupInfo != undefined) {
                                        Rxcore_GUI_3DMarkupInfo.set3DPartInfo(markuparray[i].userData);
                                    }
                                    /*if (Rxcore_GUI_3DPartInfo != undefined) {
                                        Rxcore_GUI_3DPartInfo.set3DPartInfo(mesharray[i].userData);
                                    }*/
                                }
                                else {
                                    markuparray[i].userData.selected = false;
                                    //markuparray[i].material.transparent = true;

                                    /*if(markuparray[i].userData.type == "dimension"){
                                        markuparray[i].material.color = new THREE.Color(0x0000ff);
                                        
                                    }*/


                                    if(markuparray[i].userData.type == "symbol"){
                                        markuparray[i].material[0].transparent = true;
                                        markuparray[i].material[1].transparent = true;
                                        markuparray[i].material[2].transparent = true;
                                        markuparray[i].material[3].transparent = true;
                                        markuparray[i].material[4].transparent = true;
                                        markuparray[i].material[5].transparent = true;
                                    }

                                    //mesh.material.color = new THREE.Color(0xf781f3);

                                    if(markuparray[i].userData.type == "dimension"){
                                        //unselect dimension line as well in a separate function.

                                        //thispage.select3Ddimline(id, false);
                                        thispage.select3Ddimline(markuparray[i].uuid, false);
                                        

                                    }

                                    if(markuparray[i].userData.type == "camera"){

                                        if (markuparray[i].userData.facecolor != undefined) {
                                            markuparray[i].material.color.setHex(markuparray[i].userData.facecolor);
                                        }

                                        //markuparray[i].material.color = new THREE.Color(0xf781f3);
                                    }


                                    //markuparry[i].material.color = new THREE.Color(mesharray[i].userData.facecolor);
                                    //set transparency to true
                                }
                            }
                            else {
                                if (markuparray[i].type == "Mesh") {
                                    if (markuparray[i].visible) {
                                        //mesharray[i].userData.selected
                                        /*if(!markuparray[i].userData.facecolor){
                                            markuparray[i].userData.facecolor = markuparray[i].material.color.getHex();
                                        }else{
                                            markuparray[i].material.color = new THREE.Color(markuparray[i].userData.facecolor);
                                        }*/
                                        markuparray[i].material.transparent = true;
                                        markuparray[i].userData.selected = false;
                                    }
                                }
                            }
                        }
                        /*if(vector3Dref.scene.children[vector3Dref.modelidx].children[i].name == name && isNaN(name)){
                            this.Vector3DPageObj.restoreCameraName(name);
                            //console.log(name);
                        }else{
                            
                        }*/
                    }
                    if (RxCore_GUI_3DMarkupList != undefined) {
                        RxCore_GUI_3DMarkupList.setMarkupList(markuparray, DocObj.bActive);
                    }
                };

                this.setunselect = function(mesh){

                    if (mesh.userData.selected) {


                        if(mesh.userData.usetempfacecolor){
                            if (mesh.userData.tempfacecolor != undefined){
                                mesh.material.color.setHex(mesh.userData.tempfacecolor);
                            }
                        }else{
                            if (mesh.userData.facecolor != undefined){
                                mesh.material.color.setHex(mesh.userData.facecolor);
                            }else{
                                mesh.userData.facecolor = mesh.material.color.getHex();    
                            }

                        }

                        //mesharray[i].userData.selected
                        
                        mesh.userData.selected = false;
                    }

                };

                this.getMeshName = function (name, type){
                    var retname = name;

                    var threerev = parseInt(THREE.REVISION);
                

                    if (threerev <= 127){

                        retname = name.substring(0, name.length - 2);
                            
                    }else{

                        if(type == "LineSegments"){
                            retname = name.substring(0, name.length - 2);
                            
                        }else{
                            retname = name;
                        }
    
                    }

                    return retname;

                }

                function getmeshName(name, type){

                    var retname = name;

                    var threerev = parseInt(THREE.REVISION);
                

                    if (threerev <= 127){

                        retname = name.substring(0, name.length - 2);
                            
                    }else{

                        if(type == "LineSegments"){
                            retname = name.substring(0, name.length - 2);
                            
                        }else{
                            retname = name;
                        }
    
                    }

                    return retname;

                }                
                
                this.setselect = function (meshdetails, bUseCallback) {
                    
                    thispage.unSelect3DmarkupAll();

                    //mesh, name, mi, gi, bgroup

                    
                    if(meshdetails.isgroup){
                        //var selectname = meshdetails.name.substring(0, meshdetails.name.length - 2);
                        //var meshname = meshdetails.mesh.name.substring(0, meshdetails.mesh.name.length - 2);

                        var selectname = getmeshName(meshdetails.name, meshdetails.mesh.type);
                        var meshname = getmeshName(meshdetails.mesh.name, meshdetails.mesh.type);


                    }else{
                        meshname = meshdetails.mesh.name;
                        selectname = meshdetails.name;
                    }

                    if (meshname == selectname && meshdetails.mesh.visible) {
                        if (!meshdetails.mesh.userData.selected) {
                            if (meshdetails.mesh.userData.facecolor == undefined) {
                                
                                meshdetails.mesh.userData.facecolor = meshdetails.mesh.material.color.getHex();



                            }

                            if(meshdetails.mesh.type != "LineSegments"){
                                meshdetails.mesh.material.color = new THREE.Color(0xf781f3);

                            }
                            
                            meshdetails.mesh.userData.selected = true;

                            var setpart = { 
                                name : meshdetails.name,
                                index : meshdetails.meshindex,
                                group : meshdetails.isgroup,
                                groupindex : meshdetails.groupindex,
                                isadded : meshdetails.isadded,
                                addedmeshindex : meshdetails.addedmeshindex,
                                markup : false
                            };
                            thispage.Vector3DPageObj.setSelectedPart(setpart);

                            if(meshdetails.mesh.userData.blockRef){
                                var partattribs = thispage.Vector3DPageObj.blocklist[meshdetails.mesh.userData.blockRef];
                            }else{
                                thispage.Vector3DPageObj.setBlockRef(meshdetails.mesh);
                                partattribs = thispage.Vector3DPageObj.blocklist[meshdetails.mesh.userData.blockRef];   






                            }
                            
                            //var partattribs = thispage.Vector3DPageObj.getPartAttributes(meshname);
                            //mesh.userData.RxBimType = partattribs.RxBimType;

                            if (Rxcore_GUI_3DPartInfo != undefined && bUseCallback) {
                                if(partattribs != undefined){
                                    Rxcore_GUI_3DPartInfo.set3DPartInfo(partattribs);

                                    //mesh.userData.RxBimType = Number(partattribs.RxBimType);
                                }else{
                                    Rxcore_GUI_3DPartInfo.set3DPartInfo(meshdetails.mesh.userData);

                                }

                                
                            }
                        }
                        else {
                            meshdetails.mesh.userData.selected = false;

                            if(meshdetails.mesh.userData.usetempfacecolor){

                                if (meshdetails.mesh.userData.tempfacecolor != undefined){
                                    //meshobject.userData.tempfacecolor
                                    meshdetails.mesh.material.color.setHex(meshdetails.mesh.userData.tempfacecolor);
                                }
    
                            }else{
                                if (meshdetails.mesh.userData.facecolor != undefined){
                                    meshdetails.mesh.material.color.setHex(meshdetails.mesh.userData.facecolor);
                                }
    
                            }

                            
                            /*if (meshdetails.mesh.userData.facecolor != undefined){
                                meshdetails.mesh.material.color.setHex(meshdetails.mesh.userData.facecolor);
                            }*/
                            

                            
                        }
                    }
                    else {

                        if(!bmulti3dSelect){

                            if (meshdetails.mesh.visible && meshdetails.mesh.userData.selected) {
                                //mesharray[i].userData.selected
    
                                if(meshdetails.mesh.userData.usetempfacecolor){
                                    if (meshdetails.mesh.userData.tempfacecolor != undefined){
                                        //meshobject.userData.tempfacecolor
                                        meshdetails.mesh.material.color.setHex(meshdetails.mesh.userData.tempfacecolor);
                                    }
                                }else{
                                    if (meshdetails.mesh.userData.facecolor == undefined) {
                                    
                                        meshdetails.mesh.userData.facecolor = meshdetails.mesh.material.color.getHex();
                                    }
                                    else {
                                        //mesh.material.color = new THREE.Color(mesh.userData.facecolor);
                                        meshdetails.mesh.material.color.setHex(meshdetails.mesh.userData.facecolor);
        
        
                                    }
                                }                                
    
                                meshdetails.mesh.userData.selected = false;
                            }                            
                        }
                        /*if (meshdetails.mesh.visible && meshdetails.mesh.userData.selected) {
                            //mesharray[i].userData.selected
                            if (meshdetails.mesh.userData.facecolor == undefined) {
                                
                                meshdetails.mesh.userData.facecolor = meshdetails.mesh.material.color.getHex();
                            }
                            else {
                                //mesh.material.color = new THREE.Color(mesh.userData.facecolor);
                                meshdetails.mesh.material.color.setHex(meshdetails.mesh.userData.facecolor);


                            }
                            meshdetails.mesh.userData.selected = false;
                        }*/

                        //removed check to enable linesegment unselect
                        /*if (mesh.type == "Mesh") {
                        }*/
                    }

                    return meshdetails;
                };

                this.unselect3DBlockAllAdd = function(mesharray){

                    for (var i = 0; i < mesharray.length; i++) {
                        if (mesharray[i].type === "Group") {
                            for (var j = 0; j < mesharray[i].children.length; j++) {
                                //this.setunselect(mesharray[i].children[j], name, i, j, true);
                                this.setunselect(mesharray[i].children[j]);
                            }
                        }else{
                            //this.setunselect(mesharray[i], name, i, -1, false);
                            this.setunselect(mesharray[i]);
                        }

                    }

                };
                this.unselect3DBlockAll = function(){
                    
                    if(thispage.Vector3DPageObj.cur3Dview != null){
                        return;
                    }

                    var mesharray = thispage.Vector3DPageObj.getMeshChildren();

                    thispage.unselect3DBlockAllAdd(mesharray);

                    var addedmesharray = DocObj.getCurrentpage().Vector3DPageObj.getAddedMeshChildren();

                    for (var i = 0; i < addedmesharray.length; i++) {
                        thispage.unselect3DBlockAllAdd(addedmesharray[i]);
                    }


                };

                this.select3DBlocknewAll = function(mesharray, badded, addedindex, name, bUseCallback){


                    var setpart = { name: "", index: -1, group: false, groupindex: -1, markup : false, addedmesh : false, addedmeshindex : -1};
                    this.Vector3DPageObj.setSelectedPart(setpart);


                    for (var i = 0; i < mesharray.length; i++) {

                        if (mesharray[i].type === "Group") {
                            for (var j = 0; j < mesharray[i].children.length; j++) {

                                var meshdetails = {
                                    mesh : mesharray[i].children[j],
                                    name : name,
                                    meshindex : i,
                                    groupindex : j,
                                    isgroup : true,
                                    isadded : badded,
                                    addedmeshindex : addedindex
                                };

                                
                                //meshdetails = this.setselect(meshdetails, -1, bUseCallback);
                                
                                meshdetails = this.setselect(meshdetails, bUseCallback);
                                /*if (mesharray[i].children[j].name === name){
                                    
                                }*/
                                
                                /*if (mesharray[i].children[j].name === "") {
                                    continue;
                                }
                                else {
                                    
                                }*/

                            }
                        }else{
                            if (mesharray[i].name === "") {
                                continue;
                            }

                            meshdetails = {
                                mesh : mesharray[i],
                                name : name,
                                meshindex : i,
                                groupindex : -1,
                                isgroup : false,
                                isadded : badded,
                                addedmeshindex : addedindex
                            };

                            meshdetails = this.setselect(meshdetails, bUseCallback);

                            /*if (mesharray[i].name === name){
                                
                            }*/

                            
                            
                        }
                        /*if(vector3Dref.scene.children[vector3Dref.modelidx].children[i].name == name && isNaN(name)){
                            this.Vector3DPageObj.restoreCameraName(name);
                            //console.log(name);
                        }else{
                            
                        }*/
                    }

                };

                this.select3DBlocknew = function (name, bUseCallback, bAdded) {
                    var mesharray = thispage.Vector3DPageObj.getMeshChildren();

                    if (mesharray.length == 0) {
                        return;
                    }
                    //var setpart  = {name : "", index :  -1};


                    //this.Vector3DPageObj.scene.children
                    
                    //if found here do not look further
                    if(!bAdded){
                        thispage.select3DBlocknewAll(mesharray, false, -1, name, bUseCallback);
                    }else{
                        var addedmesharray = thispage.Vector3DPageObj.getAddedMeshChildren();

                        if (addedmesharray.length != 0) {
                            for(var i = 0; i < addedmesharray.length; i++){
                                thispage.select3DBlocknewAll(addedmesharray[i], true, i, name, bUseCallback);
                            }
        
                        }

                    }
                    

                    this.Vector3DPageObj.updateblocklist();


                };                
                
                
                this.select3DBlockold = function (name) {
                    this.Vector3DPageObj.setSelectedPart(-1);
                    //this.Vector3DPageObj.scene.children
                    for (var i = 0; i < thispage.Vector3DPageObj.scene.children.length; i++) {
                        if (thispage.Vector3DPageObj.scene.children[i].name === "") {
                            continue;
                        }
                        if (thispage.Vector3DPageObj.scene.children[i].name == name && isNaN(name)) {
                            this.Vector3DPageObj.restoreCameraName(name);
                            //console.log(name);
                        }
                        else {
                            if (thispage.Vector3DPageObj.scene.children[i].name == name && thispage.Vector3DPageObj.scene.children[i].visible) {
                                if (!this.Vector3DPageObj.partlist[name].selected) {
                                    thispage.Vector3DPageObj.scene.children[i].material.color = new THREE.Color(0xf781f3);
                                    this.Vector3DPageObj.partlist[name].selected = true;
                                    this.Vector3DPageObj.setSelectedPart(name);
                                    if (Rxcore_GUI_3DPartInfo != undefined) {
                                        Rxcore_GUI_3DPartInfo.set3DPartInfo(this.Vector3DPageObj.partlist[name]);
                                    }
                                }
                                else {
                                    this.Vector3DPageObj.partlist[thispage.Vector3DPageObj.scene.children[i].name].selected = false;
                                    thispage.Vector3DPageObj.scene.children[i].material.color = new THREE.Color(this.Vector3DPageObj.partlist[thispage.Vector3DPageObj.scene.children[i].name].facecolor);
                                }
                            }
                            else {
                                if (thispage.Vector3DPageObj.scene.children[i].type == "Mesh" && !isNaN(thispage.Vector3DPageObj.scene.children[i].name)) {
                                    if (thispage.Vector3DPageObj.scene.children[i].visible && this.Vector3DPageObj.partlist[thispage.Vector3DPageObj.scene.children[i].name].selected) {
                                        thispage.Vector3DPageObj.scene.children[i].material.color = new THREE.Color(this.Vector3DPageObj.partlist[thispage.Vector3DPageObj.scene.children[i].name].facecolor);
                                        this.Vector3DPageObj.partlist[thispage.Vector3DPageObj.scene.children[i].name].selected = false;
                                    }
                                }
                            }
                        }
                    }
                    this.Vector3DPageObj.updateblocklist();
                };

                this.vectorbinaryloadBckGrnd = function(ev) {
                    var yscale = 0.0;
                    var xscale = 0.0;
                    var dxlocal = 0.0;
                    var dylocal = 0.0;
                    var dscalelocal = 0.0;
                    var dscalelocalOne = 0.0;
                    var imagewidth = 0;
                    var imageheight = 0;

                    if (ev.currentTarget.status == 200) {
                        var arrayBuffer = ev.currentTarget.response;
                        if (arrayBuffer) {
                            //var dv = new DataView(arrayBuffer);

                            

                            var compareobject = {role : 1, bgColor : overlayBGColor, fgColor : overlayFGColor, fvonly : false};

                            //this.backgroundColor = compareobj.bgColor;
                            //this.overlayColor = compareobj.fgColor;
        

                            thispage.VectorPageObj = new VectorPageObject(thispage, arrayBuffer, true, compareobject);

                            if(thispage.VectorPageObj == undefined){
                                return;
                            }

                            if (thispage.VectorPageObj.height == 0 || thispage.VectorPageObj.width == 0) {
                                return;
                            }
                            
                            /*if (RxCore_GUI_Download != undefined) {
                                RxCore_GUI_Download.setDownload("hide");
                            }*/

                            thispage.originalwidth = thispage.VectorPageObj.width;
                            thispage.originalheight = thispage.VectorPageObj.height;
                            yscale = fixedScaleSize.height / thispage.VectorPageObj.height; //thispage.MainImageHeight;
                            xscale = fixedScaleSize.width / thispage.VectorPageObj.width; // thispage.MainImageWidth;
                            dscalelocalOne = Math.min(xscale, yscale);
                            thispage.fixedScale = dscalelocalOne;
                            yscale = canvasoheight / thispage.VectorPageObj.height; //thispage.MainImageHeight;
                            xscale = canvasowidth / thispage.VectorPageObj.width; // thispage.MainImageWidth;
                            dscalelocal = Math.min(xscale, yscale);
                            dxlocal = (canvasowidth - (thispage.VectorPageObj.width * dscalelocal)) / 2;
                            dylocal = (canvasoheight - (thispage.VectorPageObj.height * dscalelocal)) / 2;
                            
                            thispage.width = thispage.VectorPageObj.width;
                            thispage.height = thispage.VectorPageObj.height;
                            
                            thispage.setvectordim(dxlocal, dylocal, dscalelocal);
                            thispage.printobj.setDocSize(thispage.VectorPageObj.width, thispage.VectorPageObj.height);
                            
                            if(thispage.loadoverlayvectors){
                                thispage.loadfgvectors();
                            }

                            /*if(thispage.loadbackgroundimage){
                        

                                var imagebgobject = {
                                    dlayer : 0,
                                    drawmode : 13,
                                    lstate : 1,
                                    x1 : 0,
                                    y1 : 0,
                                    x2 : thispage.MainImageWidthBckGrnd,
                                    y2 : thispage.MainImageHeightBckGrnd,
                                    imagestr : thispage.Vector2DImageSRCBckGrnd
                                };
        
                                thispage.VectorPageObj.addimage(imagebgobject, true);
        
        
        
                            }*/

                            if (thispage.loadoverlayimage){
                                
                                var imagebgobject = {
                                    dlayer : 1,
                                    drawmode : 13,
                                    lstate : 1,
                                    x1 : 0,
                                    y1 : 0,
                                    x2 : thispage.MainImageWidthFrGrnd,
                                    y2 : thispage.MainImageHeightFrGrnd,
                                    imagestr : thispage.Vector2DImageSRCFrGrnd
                                };
        
                                //var compareobject = {role : 1, bgColor : overlayBGColor, fgColor : overlayFGColor};
                                thispage.VectorPageObj.addimage(imagebgobject, false, compareobject);
                                
                            }

                            if(thispage.loadoverlaypdf){

                                var imagebgobject = {
                                    dlayer : 1,
                                    drawmode : 13,
                                    lstate : 1,
                                    x1 : 0,
                                    y1 : 0,
                                    x2 : thispage.PDFWidthFrGrnd,
                                    y2 : thispage.PDFHeightFrGrnd,
                                    //imagestr : thispage.Vector2DImageSRCBckGrnd
                                };

                                thispage.VectorPageObj.addpdfimage(imagebgobject, false, compareobject);

                                //thispage.loadbackgroundpdf = true;

                            }
                            
                            
                        }
                    }


                };

                this.vectorbinaryloadFrGrnd = function (ev) {

                    var yscale = 0.0;
                    var xscale = 0.0;
                    var dxlocal = 0.0;
                    var dylocal = 0.0;
                    var dscalelocal = 0.0;
                    var dscalelocalOne = 0.0;
                    var imagewidth = 0;
                    var imageheight = 0;
                    if (ev.currentTarget.status == 200) {
                        var arrayBuffer = ev.currentTarget.response;
                        if (arrayBuffer) {
                            
                            var compareobject = {role : 2, bgColor : overlayBGColor, fgColor : overlayFGColor};

                            //thispage.VectorPageObj = 

                            if(thispage.loadbackgroundvectors){
                                thispage.VectorPageObj.addvectors(arrayBuffer, compareobject);
                            }else{
                                thispage.VectorPageObj = new VectorPageObject(thispage, arrayBuffer, true, compareobject);
                            }
                            

                            
                            //new VectorPageObject(thispage, arrayBuffer, true);

                            if(thispage.VectorPageObj == undefined){
                                return;
                            }

                            if (thispage.VectorPageObj.height == 0 || thispage.VectorPageObj.width == 0) {
                                return;
                            }
                            if (RxCore_GUI_Download != undefined) {
                                RxCore_GUI_Download.setDownload("hide");
                            }
                            thispage.originalwidth = thispage.VectorPageObj.width;
                            thispage.originalheight = thispage.VectorPageObj.height;
                            yscale = fixedScaleSize.height / thispage.VectorPageObj.height; //thispage.MainImageHeight;
                            xscale = fixedScaleSize.width / thispage.VectorPageObj.width; // thispage.MainImageWidth;
                            dscalelocalOne = Math.min(xscale, yscale);
                            thispage.fixedScale = dscalelocalOne;
                            yscale = canvasoheight / thispage.VectorPageObj.height; //thispage.MainImageHeight;
                            xscale = canvasowidth / thispage.VectorPageObj.width; // thispage.MainImageWidth;
                            dscalelocal = Math.min(xscale, yscale);
                            dxlocal = (canvasowidth - (thispage.VectorPageObj.width * dscalelocal)) / 2;
                            dylocal = (canvasoheight - (thispage.VectorPageObj.height * dscalelocal)) / 2;
                            thispage.width = thispage.VectorPageObj.width;
                            thispage.height = thispage.VectorPageObj.height;
                            thispage.setvectordim(dxlocal, dylocal, dscalelocal);
                            thispage.printobj.setDocSize(thispage.VectorPageObj.width, thispage.VectorPageObj.height);

                            if(thispage.loadbackgroundimage){
                        

                                var imagebgobject = {
                                    dlayer : 0,
                                    drawmode : 13,
                                    lstate : 1,
                                    x1 : 0,
                                    y1 : 0,
                                    x2 : thispage.MainImageWidthBckGrnd,
                                    y2 : thispage.MainImageHeightBckGrnd,
                                    imagestr : thispage.Vector2DImageSRCBckGrnd
                                };
        
                                
                                thispage.VectorPageObj.addimage(imagebgobject, true, compareobject);
        
        
        
                            }

                            if(thispage.loadbackgroundpdf){

                                var imagebgobject = {
                                    dlayer : 0,
                                    drawmode : 13,
                                    lstate : 1,
                                    x1 : 0,
                                    y1 : 0,
                                    x2 : thispage.PDFWidthBckGrnd,
                                    y2 : thispage.PDFHeightBckGrnd,
                                    //imagestr : thispage.Vector2DImageSRCBckGrnd
                                };

                                thispage.VectorPageObj.addpdfimage(imagebgobject, true, compareobject);

                                //thispage.loadbackgroundpdf = true;

                            }
                            
                            
                            
                            /*else if (thispage.loadoverlayimage){
                                
                                var imagebgobject = {
                                    dlayer : 1,
                                    drawmode : 13,
                                    lstate : 1,
                                    x1 : 0,
                                    y1 : 0,
                                    x2 : thispage.MainImageWidthFrGrnd,
                                    y2 : thispage.MainImageHeightFrGrnd,
                                    imagestr : thispage.Vector2DImageSRCFrGrnd
                                };
        
                                thispage.VectorPageObj.addimage(imagebgobject, false);
                                
                            }*/


                        }
                    }                    

                };

                this.vectorbinaryload = function (ev) {
                    var yscale = 0.0;
                    var xscale = 0.0;
                    var dxlocal = 0.0;
                    var dylocal = 0.0;
                    var dscalelocal = 0.0;
                    var dscalelocalOne = 0.0;
                    var imagewidth = 0;
                    var imageheight = 0;
                    if (ev.currentTarget.status == 200) {
                        var arrayBuffer = ev.currentTarget.response;
                        if (arrayBuffer) {
                            //var dv = new DataView(arrayBuffer);
                            thispage.VectorPageObj = new VectorPageObject(thispage, arrayBuffer, true);

                            if(thispage.VectorPageObj == undefined || thispage.VectorPageObj.invalid){
                                //return;
                                thispage.invalid = true;
                            }

                            if (thispage.VectorPageObj.height == 0 || thispage.VectorPageObj.width == 0) {
                                
                                thispage.invalid = true;
                            }

                            if (RxCore_GUI_Download != undefined) {
                                RxCore_GUI_Download.setDownload("hide");
                            }

                            if(thispage.invalid){
                                
                                //if(thispage.thispage.DocRef.)

                                thispage.DocRef.removepage(thispage);

                                //thispage.DocRef.GotoPage(thispage.DocRef.prevpage);

                                /*if (PageObject.VectorPageObj.height == 0 || PageObject.VectorPageObj.width == 0) {
                                    thisdocument.pages.pop();
                                    //return;
                                }*/
    
                                
                            }else{
                                thispage.originalwidth = thispage.VectorPageObj.width;
                                thispage.originalheight = thispage.VectorPageObj.height;
                                yscale = fixedScaleSize.height / thispage.VectorPageObj.height; //thispage.MainImageHeight;
                                xscale = fixedScaleSize.width / thispage.VectorPageObj.width; // thispage.MainImageWidth;
                                dscalelocalOne = Math.min(xscale, yscale);
                                thispage.fixedScale = dscalelocalOne;
                                yscale = canvasoheight / thispage.VectorPageObj.height; //thispage.MainImageHeight;
                                xscale = canvasowidth / thispage.VectorPageObj.width; // thispage.MainImageWidth;
                                dscalelocal = Math.min(xscale, yscale);
                                dxlocal = (canvasowidth - (thispage.VectorPageObj.width * dscalelocal)) / 2;
                                dylocal = (canvasoheight - (thispage.VectorPageObj.height * dscalelocal)) / 2;
                                thispage.width = thispage.VectorPageObj.width;
                                thispage.height = thispage.VectorPageObj.height;
                                thispage.setvectordim(dxlocal, dylocal, dscalelocal);
                                thispage.printobj.setDocSize(thispage.VectorPageObj.width, thispage.VectorPageObj.height);

                            }

                            //this.GotoPage = function (pagenum, nofoxitupate) {

                            //thispage.DocRef.setthumbnail(thispage.VectorPageObj.width, thispage.VectorPageObj.height, thispage.pagenumber);
                            //yscale = 1080 / thispage.VectorPageObj.height; //thispage.MainImageHeight;
                            //xscale = 1920 / thispage.VectorPageObj.width; // thispage.MainImageWidth;
                            //dscalelocalOne = Math.min(xscale, yscale);
                            /*var byteArray = new Uint8Array(arrayBuffer);
                             for (var i = 0; i < byteArray.byteLength; i++) {
                             // do something with each byte in the array
                             }*/
                        }
                    }
                };
                /*this.vectorload = function (ev) {
                    var yscale = 0.0;
                    var xscale = 0.0;
                    var dxlocal = 0.0;
                    var dylocal = 0.0;
                    var dscalelocal = 0.0;
                    var imagewidth = 0;
                    var imageheight = 0;
        
                    if (ev.currentTarget.status == 200) {
        
                        var xmlDoc = ev.currentTarget.responseXML;
                        if (xmlDoc == null || xmlDoc.documentElement == null) {
                            try {
                                xmlDoc = $.parseXML(this.responseText).documentElement;
                            } catch (e) {
                                alert("Error 1 - " + e);
                                console('file failed to load');
                                return;
                            }
                        }
        
                        thispage.VectorPageObj = new VectorPageObject(xmlDoc, false);
        
                        yscale = canvasoheight / thispage.VectorPageObj.height; //thispage.MainImageHeight;
                        xscale = canvasowidth / thispage.VectorPageObj.width; // thispage.MainImageWidth;
        
                        dscalelocal = Math.min(xscale, yscale);
        
                        dxlocal = (canvasowidth - (thispage.VectorPageObj.width * dscalelocal)) / 2;
                        dylocal = (canvasoheight - (thispage.VectorPageObj.height * dscalelocal)) / 2;
        
                        thispage.width = thispage.VectorPageObj.width;
                        thispage.height = thispage.VectorPageObj.height;
        
                        thispage.setvectordim(dxlocal, dylocal, dscalelocal);
        
                    }
        
                };*/
                this.vectorbinprogress = function (ev) {
                    if (ev.lengthComputable) {
                        //showDownloadDialog();
                        var percentComplete = Math.round(ev.loaded * 100 / ev.total);
                        if (percentComplete == 100) {
                            //hideDownloadDialog();
                            if (RxCore_GUI_Download != undefined) {
                                RxCore_GUI_Download.setDownload("hide");
                            }
                        }
                    }
                };
                this.vectorprogress = function (ev) {
                    var contentSize = ev.currentTarget.getResponseHeader("Content-Length");
                    if (contentSize > 10000000) {
                        //console.log(contentSize);
                        if ((navigator.userAgent.match(/iPhone/i)) || (navigator.userAgent.match(/iPad/i))) {
                            alert("This file is currently too large for your device. Check back for an update shortly ");
                            //hideDownloadDialog();
                            if (RxCore_GUI_Download != undefined) {
                                RxCore_GUI_Download.setDownload("hide");
                            }
                            ev.currentTarget.abort();
                            //return;
                        }
                    }
                    if (ev.lengthComputable) {
                        //showDownloadDialog();
                        var percentComplete = Math.round(ev.loaded * 100 / ev.total);
                        if (percentComplete == 100) {
                            //hideDownloadDialog();
                            if (RxCore_GUI_Download != undefined) {
                                RxCore_GUI_Download.setDownload("hide");
                            }
                        }
                    }
                    else {
                        //document.getElementById('progressNumber').innerHTML = 'unable to compute';
                    }
                };
                this.vectorabort = function (ev) {
                    //RxCore_Closedocument();
                    internalClosedoc();
                };
                this.vector3Dbinprogress = function (ev) {
                    /*var contentSize = ev.currentTarget.getResponseHeader ("Content-Length");
                     if (contentSize > 10000000){
                     if((navigator.userAgent.match(/iPhone/i)) || (navigator.userAgent.match(/iPad/i))) {
                     alert("This file is currently too large for your device. Check back for an update shortly ");
                     hideDownloadDialog();
                     ev.currentTarget.abort();
                     }
        
                     }*/
                    /*if(RxCore_GUI_Download != undefined){
                     RxCore_GUI_Download.setDownload("hide");
                     }*/
                    renderer.domElement.style.visibility = 'visible';
                    if (ev.lengthComputable) {
                        //showDownloadDialog();
                        var percentComplete = Math.round(ev.loaded * 100 / ev.total);
                        if (percentComplete == 100) {
                            if (RxCore_GUI_Download != undefined) {
                                RxCore_GUI_Download.setDownload("hide");
                            }
                            //hideDownloadDialog();
                            /*if(RxCore_GUI_Download != undefined){
                             RxCore_GUI_Download.setDownload("hide");
                             }*/
                        }
                    }
                    else {
                        //document.getElementById('progressNumber').innerHTML = 'unable to compute';
                    }
                };
                this.vector3Dprogress = function (ev) {
                    /*var contentSize = ev.currentTarget.getResponseHeader ("Content-Length");
                     if (contentSize > 10000000){
                     if((navigator.userAgent.match(/iPhone/i)) || (navigator.userAgent.match(/iPad/i))) {
                     alert("This file is currently too large for your device. Check back for an update shortly ");
                     hideDownloadDialog();
                     ev.currentTarget.abort();
                     }
        
                     }*/
                    if (ev.lengthComputable) {
                        //showDownloadDialog();
                        var percentComplete = Math.round(ev.loaded * 100 / ev.total);
                        if (percentComplete == 100) {
                            //hideDownloadDialog();
                            if (RxCore_GUI_Download != undefined) {
                                RxCore_GUI_Download.setDownload("hide");
                            }
                        }
                    }
                    else {
                        //document.getElementById('progressNumber').innerHTML = 'unable to compute';
                    }
                };
                this.vector3Dabort = function (ev) {
                    //RxCore_Closedocument();
                    internalClosedoc();
                };

                this.vector3DblocknavloadJSON = function (ev){

                    if (ev.currentTarget.status == 200) {
                        
                        var xmlDoc = JSON.parse(ev.currentTarget.responseText);
                        
                        
                    }


                };


                this.vector3Dblocknavload = function (ev) {
                    /*if(RxCore_GUI_Download != undefined){
                     RxCore_GUI_Download.setDownload("hide");
                     }*/
                    if (ev.currentTarget.status == 200) {
                        var xmlDoc = ev.currentTarget.responseXML;
                        if (xmlDoc == null || xmlDoc.documentElement == null) {
                            try {
                                xmlDoc = parseXML(this.responseText);
                            }
                            catch (e) {
                                alert("Error 1 - " + e);
                                return;
                            }
                        }
                        thispage.Vector3DPageObj.addblocks(xmlDoc);
                    }
                    //thispage.Vector3DPageObj.
                };
                this.glTFstoreyLoadComplete = function(){

                    if (Rxcore_GUI_fileLoadComplete != undefined) {
                        //Rxcore_GUI_fileLoadComplete.loadComplete(thispage.DocRef.OriginalURL);
                        Rxcore_GUI_fileLoadComplete.loadComplete(thispage.DocRef.OriginalURL, thispage.DocRef.bActive);
                    }        

                };
                this.gltfAddLoadComplete = function(metasrc){

                    if(metasrc != null){
                        thispage.loadAddIFCJSON(metasrc);
                    }

                };
                this.glTFloadComplete = function (size, center, unit, boxzmin) {
                    //thispage.walkthroughcontrol = new FirstPersonControl();
                    thispage.walkthroughcontrol = new PointerControls();

                    var elevation = thispage.Vector3DPageObj.getfloorElevation(0);
                    var zoffset = thispage.Vector3DPageObj.getfloorheight();
                    //zoffset *= 1000;
                    var walkspeed = thispage.Vector3DPageObj.getmeterunit();
                    //walkspeed = 1000;
                    var boxcenter = thispage.Vector3DPageObj.boxCenter.z;
                    

                    thispage.walkthroughcontrol.setwalkHeight(elevation + zoffset);
                    //thispage.walkthroughcontrol.setwalkHeight(zoffset);
                    //thispage.walkthroughcontrol.setTempWalkHeight(thispage.camera.position.z);
                    thispage.walkthroughcontrol.setTempWalkHeight(boxcenter);

    
                    thispage.walkthroughcontrol.setwalkUnit(walkspeed);
                    thispage.walkthroughcontrol.setwalkSpeed(50);

                    
                    thispage.walkthroughcontrol.setinitial(center, size);


                    

                    //commented out until JSON data is fixed 18.05.2020
                    thispage.loadIFCJSON();

                    if (thispage.StoreyMetadata.length > 0){
                        for (var si = 0; si < thispage.StoreyMetadata.length; si++){
                            thispage.loadStoreyJSON(thispage.StoreyMetadata[si]);
                        }
                    }

                    thispage.loadIFCNavigator();

                    if (firstpage && !thispage.DocRef.markuploaded) {
                        if (bUsemarkupbyref) {
                            getMarkupbyReference(path, thispage.DocRef);
                        }
                        else if (bUsemarkupbyrefEx) {
                            getMarkupxmlurl(thispage.DocRef);
                        }
                        else {
                            getMarkupFilelist(path, thispage.DocRef);
                        }
                        //getMarkupFilelist(path);
                    }
                    thispage.printobj.setDocSize(canvasowidth,canvasoheight);
                    bAnimateready = true;
                    documentopen = true;
                    doResize(true);
                    RxCore_3DOrbit();

                    RxCore_saveresetCam();

                    //if(vector3Dref.modelLoaded){

                    thispage.Vector3DPageObj.modelLoaded = true;

                    thispage.Vector3DPageObj.load3Dmarkupcheck();
                    
                    
                    if(thispage.StoreyMetadata.length == 0){
                        if (Rxcore_GUI_fileLoadComplete != undefined) {
                            //Rxcore_GUI_fileLoadComplete.loadComplete(thispage.DocRef.OriginalURL);
                            Rxcore_GUI_fileLoadComplete.loadComplete(thispage.DocRef.OriginalURL, thispage.DocRef.bActive);


                        }        
    
                    }

                    

                };
                this.IFCJSONloadAdd = function(ev){

                    if (ev.currentTarget.status == 200) {

                        try {
                            var myArr = JSON.parse(ev.currentTarget.responseText);
                        }
                        catch (e) {
                            alert("Error 1 - " + e);
                            return;
                        }

                        thispage.Vector3DPageObj.addblockscombine(myArr);
                    }

                };


                this.loadAddIFCJSON = function (metasrc) {

                    /*if(!metasrc){

                   }*/


                   var eventlist = [
                        {event : "loadend", func : thispage.IFCJSONloadAdd}
                   ];
        
                    sendHttpRequestwevents('GET', metasrc, '', eventlist);


                };


                this.IFCJSONNavload = function(ev){

                    if (ev.currentTarget.status == 200) {

                        try {
                            var myArr = JSON.parse(ev.currentTarget.responseText);
                        }
                        catch (e) {
                            alert("Error 1 - " + e);
                            return;
                        }

                        thispage.Vector3DPageObj.addNavlist(myArr);
                    }


                };

                this.loadIFCNavigator = function(){

                    

                    if(!thispage.NavigatorJSONSRC){
                        var cachfolder = getURLPath(thispage.Vector3DBinarySRC);
                        this.NavigatorJSONSRC = cachfolder + "navigator.json";
                    }


                    var eventlist = [
                        {event : "loadend", func : thispage.IFCJSONNavload}
                    ];


                    sendHttpRequestwevents('GET', thispage.NavigatorJSONSRC, '', eventlist);

                };



                this.IFCJSONload = function (ev) {
                    if (ev.currentTarget.status == 200) {
                        try {
                            var myArr = JSON.parse(ev.currentTarget.responseText);
                        }
                        catch (e) {
                            alert("Error 1 - " + e);
                            return;
                        }
                        thispage.Vector3DPageObj.addblocks(myArr);
                    }
                };
                this.loadIFCJSON = function () {


                    if (!thispage.VectorMetadataSRC) {
                        var cachfolder = getURLPath(thispage.Vector3DBinarySRC);
                        thispage.VectorMetadataSRC = cachfolder + "1_1.json";
                    }

                    var eventlist = [
                        {event : "loadend", func : thispage.IFCJSONload}
                    ];
            
                    sendHttpRequestwevents('GET', thispage.VectorMetadataSRC, '', eventlist);

                };
                this.storeyJSONload = function(ev){

                    if (ev.currentTarget.status == 200) {

                        try {
                            var myArr = JSON.parse(ev.currentTarget.responseText);
                        }
                        catch (e) {
                            alert("Error 1 - " + e);
                            return;
                        }

                        thispage.Vector3DPageObj.addstoreys(myArr);
                    }

                };                
                this.loadStoreyJSON = function(storeydata){

                    /*if (thispage.StoreyMetadata.length > 0){

                    }*/
                    var rtype = '';

                    sendHttpRequest('GET', storeydata.src, rtype).then(function (response){
            
                        var myArr = JSON.parse(response);

                        thispage.Vector3DPageObj.addstoreys(myArr, storeydata.name, storeydata.id, storeydata.index, storeydata.Elevation );

                    }).catch(function(error){
           
                       if(error.status){
               
                       }else{
                        console.log(error);
                       }
           
           
                   });
                    

                };
                this.loadGLTFSpaces = function(){
                    //this.VectorMetadataSRC

                    if (thispage.usevector3DGLTF) {
                        if(thispage.Vector3DPageObj){
                            var cachfolder = getURLPath(thispage.Vector3DBinarySRC);
                            thispage.VectorSpaceDataSRC = cachfolder + "spaces.glb";
                            thispage.addtoGLTFScene(thispage.VectorSpaceDataSRC);
                        }
    
                    }

                };

                this.addtoGLTFScene = function(GLBURL, JSONURL){
                    if (thispage.usevector3DGLTF) {
                        if(thispage.Vector3DPageObj){
                            thispage.Vector3DPageObj.addToScene(GLBURL, JSONURL);
                        }
                    }
                };

                this.vector3DGltfload = function () {
                    if (RxCore_GUI_Upload != undefined) {
                        RxCore_GUI_Upload.setUpload("show");
                    }
                    thispage.Vector3DPageObj = new Vector3DGLTFObject(thispage.Vector3DBinarySRC, thispage);
                    var cachfolder = getURLPath(thispage.Vector3DBinarySRC);
                    //thispage.floorplansrc = cachfolder + "floorplan0.png"; 
                    //thispage.DocRef.thumbails[thispage.pagenumber].set3DNavImage();
                    //thispage.walkthroughcontrol = new FirstPersonControl();
                    //thispage.walkthroughcontrol.movementSpeed = 200;
                    //thispage.walkthroughcontrol.lookSpeed = 0.125;
                    //thispage.walkthroughcontrol.lookVertical = true;
                    //thispage.printobj.setDocSize(canvasowidth,canvasoheight);
                    //documentopen = true;
                    //doResize(true);
                    //RxCore_3DOrbit();
                    //Vector3DGLTFObject = function(URL,parent){
                };
                this.vector3DBinaryload = function (ev) {
                    var yscale = 0.0;
                    var xscale = 0.0;
                    var dxlocal = 0.0;
                    var dylocal = 0.0;
                    var dscalelocal = 0.0;
                    var imagewidth = 0;
                    var imageheight = 0;
                    //hideDownloadDialog();
                    if (ev.currentTarget.status == 200) {
                        //console.log(contentSize);
                        arrayBuffer = ev.currentTarget.response;
                        if (arrayBuffer) {
                            thispage.Vector3DPageObj = new Vector3DPageObject(arrayBuffer, true, thispage);
                            thispage.walkthroughcontrol = new FirstPersonControl();
                            //thispage.walkthroughcontrol.movementSpeed = 50;
                            thispage.walkthroughcontrol.movementSpeed = 10;
                            thispage.walkthroughcontrol.lookSpeed = 0.125;
                            thispage.walkthroughcontrol.lookVertical = true;
                            thispage.printobj.setDocSize(canvasowidth, canvasoheight);
                            documentopen = true;
                            doResize(true);
                            RxCore_3DOrbit();
                            /*if (thispage.has3Dnav) {
                                thispage.load3dnavigator();
                            }*/
                        }
                    }
                };
                this.vectorpart3Dload = function (ev) {
                    if (ev.currentTarget.status == 200) {
                        var xmlDoc = ev.currentTarget.responseXML;
                        if (xmlDoc == null || xmlDoc.documentElement == null) {
                            try {
                                xmlDoc = parseXML(this.responseText);
                            }
                            catch (e) {
                                alert("Error 1 - " + e);
                                return;
                            }
                        }
                        nPartfilesLoaded++;
                        if (nPartfilesLoaded == thispage.VectorPartfilelist.length) {
                            bisLastPart = true;
                        }
                        //thispage.Vector3DPageObj.addpart(xmlDoc, bisLastPart);
                        //renderer.render(thispage.Vector3DPageObj.scene, camera,false);
                        //alert('new page loaded');
                    }
                };
                this.vector3Dpartprogress = function (ev) {
                };
                this.load3dvectorPart = function () {
                    //console.log(thispage.VectorPartfilelist.length);
                    for (var i = 0; i < thispage.VectorPartfilelist.length; i++) {


                        var eventlist = [
                            {event : "load", func : thispage.vectorpart3Dload},
                            {event : "progress", func : thispage.vector3Dpartprogress}
                        ];
                
                        sendHttpRequestwevents('GET', thispage.VectorPartfilelist[i], '', eventlist);

                    }
                    //bAnimateready = true;
                };
                this.load3dnavigator = function () {
                    /*if(RxCore_GUI_Download != undefined){
                     RxCore_GUI_Download.setDownload("show");
                     }*/

                     var eventlist = [
                        {event : "load", func : thispage.vector3Dblocknavload},
                        {event : "progress", func : thispage.vector3Dprogress},
                        {event : "abort", func : thispage.vector3Dabort}
                    ];
            
                    sendHttpRequestwevents('GET', thispage.Navigator3DSRC, '', eventlist);

                };
                this.loadvectors3DBinary = function () {
                    if (RxCore_GUI_Download != undefined) {
                        RxCore_GUI_Download.setDownload("show");
                    }

                    var eventlist = [
                        {event : "loadend", func : thispage.vector3DBinaryload},
                        {event : "progress", func : thispage.vector3Dbinprogress},
                        {event : "abort", func : thispage.vector3Dabort}
                    ];

                    sendHttpRequestwevents('GET', thispage.Vector3DBinarySRC, "arraybuffer", eventlist);

                };
                this.loadvectors3D = function () {
                    //showDownloadDialog();
                    /*if (RxCore_GUI_Download != undefined) {
                        RxCore_GUI_Download.setDownload("show");
                    }*/


                    var eventlist = [
                        {event : "load", func : thispage.vector3Dload},
                        {event : "progress", func : thispage.vector3Dprogress},
                        {event : "abort", func : thispage.vector3Dabort}
                    ];

                    sendHttpRequestwevents('GET', thispage.Vector3DSRC, '', eventlist);

                };

                this.loadcomparevectors = function(){

                    if(thispage.loadbackgroundvectors){
                        thispage.loadbgvectors();
                    }else if(thispage.loadoverlayvectors){
                        thispage.loadfgvectors();
                    }else{
                        
                        
                        thispage.createEmptyVectorpage();
                    }

                }

                this.createEmptyVectorpage = function(){

                    var yscale = 0.0;
                    var xscale = 0.0;
                    var dxlocal = 0.0;
                    var dylocal = 0.0;
                    var dscalelocal = 0.0;
                    var dscalelocalOne = 0.0;
                    var imagewidth = 0;
                    var imageheight = 0;

                    var compareobject = {role : 1, bgColor : overlayBGColor, fgColor : overlayFGColor};

                    var vectorpageobj = {
                        x : 0,
                        y : 0,
                        w : 0,
                        h : 0,
                        scale : 1,
                        offsetx : 0,
                        offsety : 0,
                        iscompare : true,
                        bgcolor : overlayBGColor,
                        fgcolor : overlayFGColor
                    };

                    
                    /*if (pagexmldata.iscompare){
                        createempty(pagexmldata);
                    }*/


                    if(thispage.loadbackgroundimage){
                        
                        if (thispage.MainImageWidthBckGrnd != undefined ) {
                            vectorpageobj.w = parseInt(thispage.MainImageWidthBckGrnd);
                            vectorpageobj.h = parseInt(thispage.MainImageHeightBckGrnd);
                            vectorpageobj.scaling = parseFloat(thispage.MainImageScalingBckGrnd);

                        }

                    }else if (thispage.loadoverlayimage){
                        
                            if(thispage.MainImageWidthFrGrnd != undefined){
                                vectorpageobj.w = parseInt(thispage.MainImageWidthFrGrnd);
                                vectorpageobj.h = parseInt(thispage.MainImageHeightFrGrnd);
                                vectorpageobj.scaling = parseFloat(thispage.MainImageScalingFrGrnd);
    
                            }
                        
                    }else if(thispage.loadbackgroundpdf){

                        if(thispage.PDFWidthBckGrnd != undefined){
                            vectorpageobj.w = thispage.PDFWidthBckGrnd;
                            vectorpageobj.h = thispage.PDFHeightBckGrnd;
                            vectorpageobj.scaling = 1;

                        }

                        
                    }else if(thispage.loadoverlaypdf){

                        if(thispage.PDFWidthFrGrnd != undefined){
                            vectorpageobj.w = thispage.PDFWidthFrGrnd;
                            vectorpageobj.h = thispage.PDFHeightFrGrnd;
                            vectorpageobj.scaling = 1;

                        }


                            //get pdf info to set vectorpage object width and heigh
                    }else{
                        
                        return;

                    }


                    


                    /*var backroundlayer = {
                        index : 0,
                        color : pageobject.bgcolor,
                        state : 1,
                        name : "backgroundlayer"
                    };

                    var overlaylayer = {
                        index : 1,
                        color : pageobject.fgcolor,
                        state : 1,
                        name : "overlaylayer"
                    };*/

                    //scope.flag = 1;

                    /*scope.x = pageobject.x;
                    scope.y = pageobject.y;
                    scope.w = pageobject.w;
                    scope.h = pageobject.h;

                    scope.scale = pageobject.scale;
                    scope.offsetx = pageobject.offsetx;
                    scope.offsety = pageobject.offsety;
    
                    scope.width = vectorobj.w - vectorobj.x;
                    scope.height = vectorobj.h - vectorobj.y;*/


                    thispage.VectorPageObj = new VectorPageObject(thispage, vectorpageobj, false, compareobject);


                    if(thispage.VectorPageObj == undefined){
                        return;
                    }

                    if (thispage.VectorPageObj.height == 0 || thispage.VectorPageObj.width == 0) {
                        return;
                    }

                    thispage.originalwidth = thispage.VectorPageObj.width;
                    thispage.originalheight = thispage.VectorPageObj.height;
                    yscale = fixedScaleSize.height / thispage.VectorPageObj.height; //thispage.MainImageHeight;
                    xscale = fixedScaleSize.width / thispage.VectorPageObj.width; // thispage.MainImageWidth;
                    dscalelocalOne = Math.min(xscale, yscale);
                    thispage.fixedScale = dscalelocalOne;
                    yscale = canvasoheight / thispage.VectorPageObj.height; //thispage.MainImageHeight;
                    xscale = canvasowidth / thispage.VectorPageObj.width; // thispage.MainImageWidth;
                    dscalelocal = Math.min(xscale, yscale);
                    dxlocal = (canvasowidth - (thispage.VectorPageObj.width * dscalelocal)) / 2;
                    dylocal = (canvasoheight - (thispage.VectorPageObj.height * dscalelocal)) / 2;
                    
                    thispage.width = thispage.VectorPageObj.width;
                    thispage.height = thispage.VectorPageObj.height;
                    
                    thispage.setvectordim(dxlocal, dylocal, dscalelocal);
                    thispage.printobj.setDocSize(thispage.VectorPageObj.width, thispage.VectorPageObj.height);
                    

                    //addimages for background and overlay

                    //var compareobject = {role : 1, bgColor : overlayBGColor, fgColor : overlayFGColor};
                    if(thispage.loadbackgroundimage){
                        

                        var imagebgobject = {
                            dlayer : 0,
                            drawmode : 13,
                            lstate : 1,
                            x1 : 0,
                            y1 : 0,
                            x2 : thispage.MainImageWidthBckGrnd,
                            y2 : thispage.MainImageHeightBckGrnd,
                            imagestr : thispage.Vector2DImageSRCBckGrnd
                        };

                        thispage.VectorPageObj.addimage(imagebgobject, true, compareobject);



                    }
                    if (thispage.loadoverlayimage){
                        
                        var imagebgobject = {
                            dlayer : 1,
                            drawmode : 13,
                            lstate : 1,
                            x1 : 0,
                            y1 : 0,
                            x2 : thispage.MainImageWidthFrGrnd,
                            y2 : thispage.MainImageHeightFrGrnd,
                            imagestr : thispage.Vector2DImageSRCFrGrnd
                        };

                        thispage.VectorPageObj.addimage(imagebgobject, false, compareobject);
                        
                    }


                    if(thispage.loadbackgroundpdf){

                        var imagebgobject = {
                            dlayer : 0,
                            drawmode : 13,
                            lstate : 1,
                            x1 : 0,
                            y1 : 0,
                            x2 : thispage.PDFWidthBckGrnd,
                            y2 : thispage.PDFHeightBckGrnd
                            //imagestr : thispage.Vector2DImageSRCBckGrnd
                        };

                        thispage.VectorPageObj.addpdfimage(imagebgobject, true, compareobject);
                        
                    }

                    if(thispage.loadoverlaypdf){
                        
                        var imagebgobject = {
                            dlayer : 1,
                            drawmode : 13,
                            lstate : 1,
                            x1 : 0,
                            y1 : 0,
                            x2 : thispage.PDFWidthFrGrnd,
                            y2 : thispage.PDFHeightFrGrnd
                            //imagestr : thispage.Vector2DImageSRCFrGrnd
                        };

                        thispage.VectorPageObj.addpdfimage(imagebgobject, false, compareobject);


                    }


                    /*if(thispage.loadoverlayvectors){
                        thispage.loadfgvectors();
                    }*/


                };

                this.loadbgvectors = function (){

                    var eventlist = [
                        {event : "load", func : thispage.vectorbinaryloadBckGrnd},
                        {event : "progress", func : thispage.vectorbinprogress},
                        {event : "abort", func : thispage.vectorabort}
                    ];

                    sendHttpRequestwevents('GET', thispage.Vector2DBinarySRCBckGrnd, "arraybuffer", eventlist);


                };

                this.loadfgvectors = function (){

                    var eventlist = [
                        {event : "load", func : thispage.vectorbinaryloadFrGrnd},
                        {event : "progress", func : thispage.vectorbinprogress},
                        {event : "abort", func : thispage.vectorabort}
                    ];

                    sendHttpRequestwevents('GET', thispage.Vector2DBinarySRCFrGrnd, "arraybuffer", eventlist);


                };

                this.loadbinvectors = function () {
                    if (RxCore_GUI_Download != undefined) {
                        RxCore_GUI_Download.setDownload("show");
                    }

                    var eventlist = [
                        {event : "load", func : thispage.vectorbinaryload},
                        {event : "progress", func : thispage.vectorbinprogress},
                        {event : "abort", func : thispage.vectorabort}
                    ];

                    sendHttpRequestwevents('GET', thispage.Vector2DBinarySRC, "arraybuffer", eventlist);

                    
                };
                this.loadvectors = function () {
                    //showDownloadDialog();


                    var eventlist = [
                        {event : "load", func : thispage.vectorload},
                        {event : "progress", func : thispage.vectorprogress},
                        {event : "abort", func : thispage.vectorabort}
                    ];

                    sendHttpRequestwevents('GET', thispage.Vector2DSRC, "", eventlist);

                };
                this.backupScaleAndOffset = function () {
                    if (thispage.usevectorxml) {
                        thispage.dscalebackup = thispage.dscalevector;
                        thispage.dxbackup = thispage.dxvector;
                        thispage.dybackup = thispage.dyvector;
                    }
                    else if (thispage.usepdfjs) {
                        thispage.dscalebackup = thispage.dscalepdf;
                        this.curpagescalebackup = thispage.curpagescale;
                        thispage.dxbackup = thispage.dxpdf;
                        thispage.dybackup = thispage.dypdf;
                    }
                    else {
                        thispage.dscalebackup = thispage.dscale;
                        thispage.dxbackup = thispage.dx;
                        thispage.dybackup = thispage.dy;
                    }
                };
                this.getCurPageScale = function () {
                    return thispage.curpagescale;
                };
                this.restoreScaleAndOffset = function () {
                    if (thispage.usevectorxml) {
                        thispage.dscalevector = thispage.dscalebackup;
                        thispage.dxvector = thispage.dxbackup;
                        thispage.dyvector = thispage.dybackup;
                    }
                    else if (thispage.usepdfjs) {

                        //dscalepdf modified
                        thispage.dscalepdf = thispage.dscalebackup;
                        thispage.curpagescale = thispage.curpagescalebackup;
                        //call foxit zoom direct to restore the scale that was used when deactivated
                        //var pagescalefactor = thispage.curpagescalebackup * PixelToPoint;
                        //thispage.DocRef.foxitdoc.zoomdirect(thispage.pagenumber, pagescalefactor);
                        //zoomdirect
                        thispage.dxpdf = thispage.dxbackup;
                        thispage.dypdf = thispage.dybackup;
                    }
                    else {
                        thispage.dscalebackup = thispage.dscale = thispage.dscalebackup;
                        thispage.dx = thispage.dxbackup;
                        thispage.dy = thispage.dybackup;
                    }
                };
                this.setPDFDiminit = function (dx, dy, dscale) {
                    this.pageloaded = false;
                    this.dxpdf = dx;
                    this.dypdf = dy;

                    //dscalepdf modified
                    this.dscalepdf = dscale;
                    //this.initialscale = dscale;
                    //this.vectorloaded = true;
                    thispage.setpagedimsize(this.dxpdf, this.dypdf, (thispage.offscreenwidth * this.dscalepdf), (thispage.offscreenheight * this.dscalepdf), false);
                    this.pagedimset = true;
                    this.pdfdiminitset = true;
                    if (firstpage) {
                        if (doCompare) {
                            comparecheckFileLoad(thispage.DocRef.FileName);
                            comparecheck();
                        }
                        else {
                            DeactivateAll();
                            thispage.DocRef.SetActive();
                        }
                    }
                    //var numpages = thispage.Docref.pages.length - 1;
                    if (thispage.pagenumber == thispage.DocRef.pages.length - 1) {
                        //add new get all pagedim callback here.
                        if (Rxcore_GUI_pagedimLoadComplete != undefined) {
                            Rxcore_GUI_pagedimLoadComplete.loadComplete(thispage.DocRef.FileName, thispage.DocRef.bActive);
                        }
                    }
                };
                this.setPFDdimnodraw = function (dx, dy, dscale, bSetscale) {
                    //this.pageloaded = true;
                    this.dxpdf = dx;
                    this.dypdf = dy;
                    if (bSetscale) {

                        //dscalepdf modified

                        this.dscalepdf = dscale;
                    }
                    thispage.setpagedimsize(this.dxpdf, this.dypdf, (thispage.offscreenwidth * this.dscalepdf), (thispage.offscreenheight * this.dscalepdf), false);
                    thispage.pagedimset = true;
                    /*if (firstpage){
                        thispage.DocRef.setDocumentPageScale(thispage.offscreenwidth, thispage.offscreenheight, thispage.dscalepdf);
                        
                    }*/
                };
                this.setPDFdim = function (dx, dy, dscale) {
                    thispage.pageloaded = true;
                    if (!thispage.pdfdiminitset) {
                        thispage.dxpdf = dx;
                        thispage.dypdf = dy;
                        thispage.setpagedimsize(thispage.dxpdf, thispage.dypdf, (thispage.offscreenwidth * thispage.dscalepdf), (thispage.offscreenheight * thispage.dscalepdf), false);
                        this.pagedimset = true;
                        thispage.pdfdiminitset = true;
                        thispage.pageloading = false;
                        thispage.backupScaleAndOffset();
                        if (firstpage) {
                            //make sure this is only called once.
                            if (doCompare) {
                                comparecheckFileLoad(thispage.DocRef.FileName);
                                comparecheck();
    
                                
                            }
                            else {
                                DeactivateAll();
                                thispage.DocRef.SetActive();
                            }
                            if (thispage.DocRef.foxitdoc != null) {
                                thispage.DocRef.foxitdoc.getLayerNodes();
                                //thispage.DocRef.foxitdoc.addLastPage();
                                //foxitgetLayerNodes();
                                //thispage.DocRef.foxitdoc.getBookmarks();
                            }
                            thispage.visible = true;
                            if (RxCore_GUI_Download != undefined) {
                                RxCore_GUI_Download.setDownload("hide");
                            }
                            //make sure this is only called once.
                            if (Rxcore_GUI_fileLoadComplete != undefined) {
                                //Rxcore_GUI_fileLoadComplete.loadComplete(thispage.DocRef.OriginalURL);
                                Rxcore_GUI_fileLoadComplete.loadComplete(thispage.DocRef.OriginalURL, thispage.DocRef.bActive);
                            }
                            if (RxCore_GUI_HasText != undefined) {
                                thispage.DocRef.hastext = true;
                                RxCore_GUI_HasText.hasText(thispage.DocRef.hastext, thispage.DocRef.bActive);
                            }
                            /*if (thispage.usedincomposite && thispage.compositereference != undefined) {
                                //dont draw
                            }
                            else {
                                
                            }*/
                            thispage.draw_canvas(true);
                            RxCore_default();
                            //should probably be moved somewhere else
                            thispage.DocRef.getfoxitThumbnail(0);
                        }
                    }
                    //dscalepdf modified
                    thispage.dscalepdf = 1;
                    //console.log('if I get here somthing is wrong');
                    if (firstpage && !thispage.DocRef.markuploaded && !thispage.markuplistloaded) {
                        if (bUsemarkupbyref) {
                            //getMarkupbyReference(path);
                            getMarkupbyReference(path, thispage.DocRef);
                        }
                        else if (bUsemarkupbyrefEx) {
                            getMarkupxmlurl(thispage.DocRef);
                            //getMarkupFilelist(path);
                        }
                        else {


                            thispage.markuplistloaded = true;
                            getMarkupFilelist(path, thispage.DocRef);

                        }
                    }
                    //if (firstpage && !thispage.pdfdiminitset) 
                    if (firstpage && !thispage.pdfdiminitset) {
                    }
                    if (firstpage && !thispage.pdfdiminitset) {
                        //thispage.DocRef.setDocumentPageScale(thispage.offscreenwidth, thispage.offscreenheight, thispage.dscalepdf);
                        /*if (RxCore_GUI_VectorBlocksLoaded != undefined) {
                            RxCore_GUI_VectorBlocksLoaded.setVectorBlocks([]);
                        }*/
                    }
                    thispage.DocRef.setthumbnail(thispage.pdfpagewidth, thispage.pdfpageheight, thispage.pagenumber);

                    
                    if (Rxcore_GUI_pageLoadComplete != undefined) {
                        Rxcore_GUI_pageLoadComplete.loadComplete(thispage.pagenumber);
                        thispage.DocRef.loadPageEvt(thispage.pagenumber);
                    }
                    
                    thispage.DocRef.draw_mpagepdf(false);

                    /*if (thispage.compositereference && thispage.role != 0) {
                        //don't draw
                    }
                    else {
                        
                    }*/
                    //thispage.DocRef.draw_mpagepdf(false);
                    thispage.backupScaleAndOffset();
                    //usepdfjs
                    //hideDownloadDialog();
                    //thispage.rxdrawtimeend = new Date().getTime();
                    //console.log('rx rendertime', thispage.rxdrawtimeend - thispage.rxdrawtimestart);
                };
                this.istextvisible = function (x, y) {
                    var isvisble = false;
                    var left = Math.max(0, x);
                    var right = Math.min(canvasowidth, x);
                    var bottom = Math.max(canvasoheight, y);
                    var top = Math.min(0, y);
                    if (x > 0 && x < canvasowidth) {
                        if (y > 0 && y < canvasoheight) {
                            isvisble = true;
                        }
                    }
                    /*if(left <= right && bottom > top){
        
                    }*/
                    return isvisble;
                };
                this.ispagevisible = function () {
                    var isvisble = false;
                    if (thispage.usepdfjs) {


                        if(thispage.DocRef.pages[thispage.DocRef.currentpage] != undefined){

                            var left = Math.max(0, thispage.DocRef.pages[thispage.DocRef.currentpage].startx);
                            var right = Math.min(canvasowidth, thispage.DocRef.pages[thispage.DocRef.currentpage].endx);
                            var bottom = thispage.DocRef.pages[thispage.DocRef.currentpage].endy;// Math.max(canvasoheight, );
                            var top = thispage.DocRef.pages[thispage.DocRef.currentpage].starty; //Math.min(0, );


                            if(thispage.DocRef.currentpage == thispage.pagenumber){
                                isvisble = true;
                            }else{
                                if(thispage.pagenumber == thispage.DocRef.currentpage + 1){
                                    if(bottom < canvasoheight - 2){
                                        isvisble = true;
                                    }
                                                                
                                }

                                if(thispage.pagenumber == thispage.DocRef.currentpage - 1){
                                    if(top > 2){
                                        isvisble = true;
                                    }

                                }

                                

                            }
                            /*if (thispage.getpageRotation() != 0 && thispage.DocRef.currentpage != thispage.pagenumber) {
                                isvisble = false;
                            }*/
    
                        }

                        /*var left = Math.max(0, thispage.dxpdf);
                        var right = Math.min(canvasowidth, thispage.endx);
                        var bottom = Math.max(canvasoheight, thispage.endy);
                        var top = Math.min(0, thispage.dypdf);
                        if (left < right && bottom > top) {

                            if(thispage.visible){
                                isvisble = true;
                            }
                        }*/
                    }else{
                        
                        if(thispage.DocRef.currentpage == thispage.pagenumber){
                            isvisble = true;
                        }else{
                            isvisble = false;
                        }
                        
                    }

                    return isvisble;
                };
                this.changeinterval = function (newinterval) {
                    clearInterval(thispage.vectortimervar);
                    thispage.vectorpagetime = newinterval;
                    thispage.vectortimervar = setInterval(function () {
                        thispage.vectortimer();
                    }, thispage.vectorpagetime);
                };
                this.vectortimer = function () {
                    if (thispage.DocRef != null && thispage.DocRef.bActive) {
                        //console.log(thispage.pagenumber);
                        //console.log(DocObj.currentpage);

                        if(thispage == undefined){
                            return;
                        }

                        if(thispage.DocRef.pages[thispage.DocRef.currentpage] == undefined){
                            return;
                        }

                        if (thispage.pagenumber == thispage.DocRef.pages[thispage.DocRef.currentpage].pagenumber) {


                            /*if (thispage.usedincomposite && thispage.compositereference != undefined) {
                                if (thispage.compositereference.printing && thispage.compositePrintreference != undefined) {
                                    if (thispage.compositereference.bisdvector) {
                                        var vectorscale = thispage.VectorPageObj.offscreenscale(thispage.compositePrintreference.overlayCScale);
                                    }
                                    else if (thispage.compositereference.oisvector) {
                                        vectorscale = thispage.VectorPageObj.offscreenscale(thispage.compositePrintreference.overlayScale);
                                    }
                                    else {
                                        vectorscale = thispage.VectorPageObj.offscreenscale(thispage.dscalevector);
                                    }
                                }
                                else {
                                    if (thispage.compositereference.bisdvector) {
                                        vectorscale = thispage.VectorPageObj.offscreenscale(thispage.compositereference.overlayCScale);
                                    }
                                    else if (thispage.compositereference.oisvector) {
                                        vectorscale = thispage.VectorPageObj.offscreenscale(thispage.compositereference.overlayScale);
                                    }
                                    else {
                                        vectorscale = thispage.VectorPageObj.offscreenscale(thispage.dscalevector);
                                    }
                                }
                            }
                            else {
                                
                            }*/

                            var vectorscale = thispage.VectorPageObj.offscreenscale(thispage.dscalevector);
                            //var vectorscale = thispage.VectorPageObj.offscreenscale(thispage.dscalevector);
                            //console.log(vectorscale);
                            //vectorscale < 0.95 || vectorscale > 1.05 || 
                            if (thispage.vectorischanged) {
                                //if( vectorscale != 1 || thispage.vectorischanged ){
                                /*if (thispage.usedincomposite && thispage.compositereference != undefined) {
                                    if (thispage.compositereference.printing && thispage.compositePrintreference != undefined) {
                                        thispage.compositePrintreference.scaleToBackground(true);
                                    }
                                    else {
                                        thispage.compositereference.scaleToBackground(true);
                                    }
                                    //this.compositereference.draw_compare(true);
                                }
                                else {
                                    //thispage.cleartextdivs();
                                    
                                    //thispage.settextdivs();
                                }*/
                                thispage.draw_vector(true);
                                thispage.vectorischanged = false;
                            }
                            //comparedrawcheck();
                        }
                    }
                };
                this.pdftimer = function () {
                    if (thispage.DocRef.pages.length == 0) {
                        return;
                    }
                    //var biscompare  = (thispage.usedincomposite && thispage.compositereference != undefined);
                    
                    //var bhascompref = (thispage.usedincomposite && thispage.compositereference != undefined);

                    var comprefActive = false;

                    /*if (bhascompref) {
                        comprefActive = thispage.compositereference.bActive;
                    }*/

                    /*if (thispage.DocRef != null) {
                        if (comprefActive) {
                            if (thispage.pagenumber == thispage.DocRef.pages[thispage.DocRef.currentpage].pagenumber) {
                                //console.log(thispage.dscalepdf);
                                //thispage.dscalepdf != 1 &&
                                if (!thispage.pdfisrendered) {
                                    if (!thispage.pageRendering) {
                                        if (thispage.DocRef.foxitdoc != null) {
                                            thispage.pageRendering = true;
                                            //thispage.DocRef.foxitdoc.getBitmap(thispage.pagenumber, thispage.dscalepdf, thispage.setPageBitmap);
                                        }
                                        //thispage.DocRef.draw_mpagepdf();
                                        //thispage.DocRef.renderPDFscale();
                                        //thispage.renderPDFpagescale();
                                    }
                                }
                            }
                        }
                    }*/
                };
                /*this.pdftimer = function () {
                    if (thispage.DocRef != null && thispage.DocRef.bActive) {
                        if (thispage.pagenumber == thispage.DocRef.currentpage) {
                            //console.log(thispage.dscalepdf);
                            if (thispage.dscalepdf != 1 || !thispage.pdfisrendered) {
                                thispage.DocRef.renderPDFscale();
                            }
                        }
        
                    }
                };*/
                this.queRenderCompareScale = function (num) {
                    if (thispage.pageRendering) {
                        thispage.pageNumPending = thispage.pagenumber + 1;
                        //setTimeout(function(){thispage.pageRendering = false},3000);
                        //console.log(thispage.pageNumPending);
                    }
                    else {
                        thispage.renderPDFComparescale(num);
                    }
                };
                this.continueCallback = function (cont) {
                    var bnotcurrentpage = (thispage.pagenumber != thispage.DocRef.pages[thispage.DocRef.currentpage].pagenumber);
                    /*if (thispage.DocRef.pages[thispage.DocRef.currentpage].pageRendering && bnotcurrentpage){
                        thispage.pageRendering = false;
                        thispage.rendercancel = true;
                        console.log('halt rendering');
                       return;
                    }*/
                    cont();
                };
                /*this.drawcanvasext = function(cnv, transscale, pagescale, pagewidth, pageheight){
                    
                    thispage.pdfpagewidth = pagewidth;
                    thispage.pdfpageheight = pageheight;
        
                    thispage.originalwidth = pagewidth;
                    thispage.originalheight = pageheight;
        
                    var yscale = fixedScaleSize.height / thispage.pdfpageheight; //thispage.MainImageHeight;
                    var xscale = fixedScaleSize.width / thispage.pdfpagewidth; // thispage.MainImageWidth;
                    thispage.fixedScale = Math.min(xscale, yscale);
        
        
                    if(cnv){
        
                        var imagewidth = cnv.width;
                        var imageheight = cnv.height;
        
                        imagewidth *= transscale;
                        imageheight *= transscale;
            
                        thispage.setpagesize(imagewidth, imageheight);
        
                        thispage.width = imagewidth;
                        thispage.height = imageheight;
            
                        //thispage.pagecanvas.width = imagewidth;
                        //thispage.pagecanvas.height = imageheight;
                        
                        //thispage.pagectx.translate(0.5, 0.5);
        
                        if (transscale == 1){
                            //thispage.pagectx.drawImage(cnv, 0,0);
                        }else{
                            //var scaledcnv = downScaleCanvas(cnv, transscale);
                            //thispage.pagectx.drawImage(scaledcnv,0,0);
                        }
                        console.log('transformation', transscale);
                        
                        
                        thispage.pdfrenderopque -= 1;
        
                        if(thispage.pdfrenderopque <= 0){
                            thispage.pdfrenderopque = 0;
                            thispage.pdfisrendered = true;
                        }
        
        
                        
                        //should be 1
                        var wscale = thispage.width / imagewidth;
                        var hscale = thispage.height / imageheight;
                        thispage.pdfdscaletemp = Math.min(wscale, hscale);
        
        
                        thispage.pdfdxtemp = (canvasowidth - imagewidth) / 2;
                        thispage.pdfdytemp = (canvasoheight - imageheight) / 2;
        
                        thispage.curpagescale = pagescale;
                        
                        thispage.pdfisrendered = true;
                        thispage.iscollapsed = false;
                        thispage.pdfisfirstrendered = true;
        
                        thispage.setNotCollapsedArray();
                        
                        thispage.setPDFdim(thispage.pdfdxtemp, 0, thispage.pdfdscaletemp);
        
                    }
        
                };*/
                this.foxitRenderPageScaled = function () {
                    //thispage.renderfoxitPDFpagescale(true);
                    var wscale = (thispage.offscreenwidth * thispage.dscalepdf) / pagewidth;
                    var hscale = (thispage.offscreenheight * thispage.dscalepdf) / pageheight;
                    var scale = Math.min(wscale, hscale);
                    if (thispage.DocRef.foxitdoc != null) {
                        thispage.DocRef.foxitdoc.renderScale(thispage.pagenumber, scale);
                        //foxitrenderScale(pagenum, scale);
                    }
                };
                this.setpagesize = function (w, h) {
                    //thispage.pagedrawCanvas.width = w;
                    //thispage.pagedrawCanvas.height = h;
                    thispage.offscreenwidth = w;
                    thispage.offscreenheight = h;

                    //foxitpage.pagepos = pagecontainers[pgdc].getBoundingClientRect();
                    //console.log(thispage.pagenumber);
                    //console.log(thispage.offscreenwidth);
                };
                this.collapsetileArray = function () {
                    for (var i = 0; i < thispage.canvtileArray.length; i++) {
                        for (var j = 0; j < thispage.canvtileArray[i].length; j++) {
                            thispage.canvtileArray[i][j].cnv.width = 1;
                            thispage.canvtileArray[i][j].cnv.height = 1;
                            thispage.canvtileArray[i][j].ctx = null;
                            thispage.canvtileArray[i][j].cnv = null;
                        }
                    }
                    thispage.canvtileArray = [];
                    thispage.annotArray = [];
                    /*for (var i= 0;i < thispage.canvtileArray.length;i++){
                        for(var j = 0; j < thispage.canvtileArray[i].length; j++){
                            thispage.canvtileArray[i][j].cnv.width = 1;
                            thispage.canvtileArray[i][j].cnv.height = 1;
        
                        }
                    }*/
                };
                this.collapsepage = function () {
                    //thispage.pdfisfirstrendered = false;
                    //thispage.pdfisrendered = false;
                    if (thispage.iscollapsed) {
                        return;
                    }
                    //console.log('page collapsed', thispage.pagenumber);
                    thispage.iscollapsed = true;
                    thispage.setNotCollapsedArray();
                    //thispage.pagectx.clearRect(0,0,thispage.offscreenwidth,thispage.offscreenheight);
                    //thispage.pagedrawctx.clearRect(0,0,thispage.offscreenwidth,thispage.offscreenheight);
                    //console.log(thispage.pagenumber);
                    //thispage.offscreenwidth = thispage.pagecanvas.width;
                    //thispage.offscreenheight = thispage.pagecanvas.height;
                    thispage.collapsetileArray();
                    //canvas.width = canvas.width;
                    //thispage.pagecanvas.width = thispage.pagecanvas.width;
                    //thispage.pagecanvas.width = 1;
                    //thispage.pagecanvas.height = 1;
                    //thispage.pagedrawCanvas.width = thispage.pagedrawCanvas.width;
                    //thispage.pagedrawCanvas.width = 1;
                    //thispage.pagedrawCanvas.height = 1;
                    thispage.PDFPointArray = [];
                    thispage.bpointlistSet = false;
                };
                this.setPDFTextAra = function (num) {
                    thispage.PDFTextArea.id = "PDFpage" + num;
                    //thispage.PDFTextArea.style.top = thispage.pdfdytemp + "px";
                    thispage.PDFTextArea.style.left = thispage.pdfdxtemp + "px";
                    thispage.PDFTextArea.style.width = thispage.width + "px";
                    thispage.PDFTextArea.style.height = thispage.height + "px";
                    thispage.PDFTextArea.style.zIndex = "4";

                    rxcontainer.appendChild(thispage.PDFTextArea);
                    if (num == 1 && thispage.DocRef.textselect) {
                        thispage.PDFTextArea.style.display = szdispvalue;
                    }
                    else {
                        thispage.PDFTextArea.style.display = "none";
                    }
                    thispage.textareaset = true;
                };
                this.drawpoints = function (pointarray, scale) {
                    /*if(thispage.dscalepdf != 1){
                        scale /= thispage.dscalepdf;
                    }*/
                    if (thispage.PDFinternalScale.x > 0) {
                        var localscalex = scale * thispage.PDFinternalScale.x;
                    }
                    else {
                        localscalex = scale * 0.12;
                    }
                    if (thispage.PDFinternalScale.y > 0) {
                        var localscaley = scale * thispage.PDFinternalScale.y;
                    }
                    else {
                        localscaley = scale * 0.12;
                    }
                    //thispage.
                    var cheight = thispage.offscreenheight * thispage.dscalepdf;
                    var cwidth = thispage.offscreenwidth * thispage.dscalepdf;
                    //context.save();
                    //context.translate(thispage.startx, canvas.height);
                    //context.scale(1,-1);
                    if (thispage.PDFpageRotate != 0) {
                        //page.pageInfo.rotate
                        //localscalex *= 0.1;
                        //localscaley *= 0.1;
                        //var txcanv = (canvasowidth / 2);
                        //var tycanv = (canvasoheight / 2);
                        //context.save();
                        //context.translate(txcanv, tycanv);
                        //context.rotate((360-thispage.PDFpageRotate) * (Math.PI / 180));
                        //context.translate(-txcanv, -tycanv);
                    }
                    for (var i = 0; i < pointarray.length; i++) {
                        if (thispage.PDFpageRotate != 0) {
                            if (thispage.PDFpageRotate == 90) {
                                var transy = thispage.starty + (-(pointarray[i].y * scale));
                                var transx = thispage.startx + (pointarray[i].x * scale);
                            }
                            else {
                                transy = thispage.starty + (-(pointarray[i].y * scale) + cheight);
                                transx = thispage.startx + (pointarray[i].x * scale) + cwidth;
                            }
                            //var transy = thispage.starty + (-(pointarray[i].y*scale));
                            //var transy = thispage.startx + (-(pointarray[i].y*scale) + cheight);
                            //var transx = thispage.starty + (pointarray[i].x*scale);
                            //var transx = thispage.starty + (-(pointarray[i].y*localscaley) + cheight);
                            //var transx = thispage.starty + (-(pointarray[i].y*scale) + cheight);
                            //var transy = thispage.startx + (pointarray[i].x*localscalex);
                            //var transy = thispage.startx + (pointarray[i].x*scale);
                        }
                        else {
                            if (pointarray[i].y < 0) {
                                transy = thispage.starty + (-(pointarray[i].y * scale));
                            }
                            else {
                                transy = thispage.starty + (-(pointarray[i].y * scale) + cheight);
                            }
                            if (pointarray[i].x < 0) {
                                transx = thispage.startx + (pointarray[i].x * scale) + cwidth;
                            }
                            else {
                                transx = thispage.startx + (pointarray[i].x * scale);
                            }
                            //transy = thispage.starty + (-(pointarray[i].y*localscaley) + cheight);
                            //transy = thispage.starty + (-(pointarray[i].y*scale) + cheight);
                            /*if (thispage.PDFinternalScale.y > 0){
        
                            }else{
        
                                transy = thispage.starty + ((pointarray[i].y*localscaley));
                            }*/
                            //transx = thispage.startx + (pointarray[i].x*localscalex);
                            //transx = thispage.startx + (pointarray[i].x*scale);
                        }
                        //var transy = thispage.starty + (-(pointarray[i].y*scale) + cheight);
                        //var transy = thispage.starty + (-(pointarray[i].y*scale));
                        //var transx = thispage.startx + (pointarray[i].x*scale) + cwidth;
                        drawpoints(context, 0, { x: transx, y: transy });
                        //drawsnap(context,{x:transx,y:transy});
                        //drawpoints(context,thispage.PDFpageRotate,{x:transx,y:transy});
                    }
                    //context.restore();
                };
                this.getdivpos = function (indx) {
                    var divpos = {
                        x: 0,
                        y: 0,
                        w: 0,
                        h: 0
                    };
                    //console.log('panupdate getdivpos');
                    thispage.pan_update(0, 0);
                    if (thispage.PDFTextArea != undefined) {
                        if (thispage.PDFTextArea.children[indx] != undefined) {
                            var divtext = thispage.PDFTextArea.children[indx];
                            var topdivy = thispage.PDFTextArea.offsetTop;
                            var topdivx = thispage.PDFTextArea.offsetLeft;
                            divpos.y = divtext.offsetTop + topdivy;
                            divpos.x = divtext.offsetLeft + topdivx;
                            divpos.w = divtext.offsetWidth;
                            divpos.h = divtext.offsetHeight;

                        }
                    }
                    if (thispage.TextSelectArea != undefined) {
                        if (thispage.TextSelectArea.children[indx] != undefined) {
                            var vdivtext = thispage.TextSelectArea.children[indx];
                            var vtopdivy = thispage.TextSelectArea.offsetTop;
                            var vtopdivx = thispage.TextSelectArea.offsetLeft;
                            divpos.y = vdivtext.offsetTop + vtopdivy;
                            divpos.x = vdivtext.offsetLeft + vtopdivx;
                            divpos.w = vdivtext.offsetWidth;
                            divpos.h = vdivtext.offsetHeight;

                        }
                    }
                    return divpos;
                };
                this.indexdivTexts = function () {
                    var pagetexts = [];
                    if (thispage.PDFTextArea != undefined) {
                        var children = thispage.PDFTextArea.children;
                        for (var i = 0; i < children.length; i++) {
                            children[i].style.backgroundColor = null;
                            var textChild = children[i];
                            pagetexts.push(textChild.textContent);
                            // Do stuff
                        }
                    }
                    if (thispage.TextSelectArea != undefined) {
                        var vchildren = thispage.TextSelectArea.children;
                        for (var vi = 0; vi < vchildren.length; vi++) {
                            vchildren[vi].style.backgroundColor = null;
                            var vtextChild = vchildren[vi];
                            pagetexts.push(vtextChild.textContent);
                        }
                    }
                    return pagetexts;
                };
                this.clearmarktextdiv = function (indx, str) {
                    if (thispage.PDFTextArea != undefined) {
                        if (thispage.PDFTextArea.children[indx] != undefined) {
                            thispage.PDFTextArea.children[indx].innerHTML = "";
                            var textnode = document.createTextNode(str);
                            thispage.PDFTextArea.children[indx].appendChild(textnode);
                        }
                    }
                    if (thispage.TextSelectArea != undefined) {
                        if (thispage.TextSelectArea.children[indx] != undefined) {
                            thispage.TextSelectArea.children[indx].innerHTML = "";
                            var vtextnode = document.createTextNode(str);
                            thispage.TextSelectArea.children[indx].appendChild(vtextnode);
                        }
                    }
                };
                this.marktextdiv = function (indx, str, pos, length) {
                    if (thispage.PDFTextArea != undefined) {
                        var spantext = str.substring(pos, length + pos);
                        var splittext = str.split(" ");
                        if (thispage.PDFTextArea.children[indx] != undefined) {
                            thispage.PDFTextArea.children[indx].innerHTML = "";
                            var numwords = 0;
                            for (var i = 0; i < splittext.length; i++) {
                                var subpos = splittext[i].search(spantext);
                                if (subpos == -1) {
                                    if (numwords > 0) {
                                        var textnode = document.createTextNode(splittext[i] + " ");
                                    }
                                    else {
                                        textnode = document.createTextNode(splittext[i]);
                                    }
                                    thispage.PDFTextArea.children[indx].appendChild(textnode);
                                    numwords++;
                                }
                                else if (subpos >= 0) {
                                    var node = document.createElement("span");
                                    node.className = "highlight"; // Create a <span> node
                                    textnode = document.createTextNode(spantext); // Create a text node
                                    node.appendChild(textnode); // Append the text to <li>
                                    thispage.PDFTextArea.children[indx].appendChild(node);
                                    numwords = 0;
                                }
                            }
                        }
                    }
                    if (thispage.TextSelectArea != undefined) {
                        var vspantext = str.substring(pos, length + pos);
                        //var vsplittext = str.split(" ");
                        var vsplittextarr = str.split(vspantext);


                        if (thispage.TextSelectArea.children[indx] != undefined) {
                            thispage.TextSelectArea.children[indx].innerHTML = "";
                            
                            
                            for (var via = 0; via < vsplittextarr.length; via++) {

                                if(via > 0 ){
                                    var vnode = document.createElement("span");
                                    vnode.className = "highlight"; // Create a <span> node
                                    var vtextnode = document.createTextNode(vspantext);
                                    vnode.appendChild(vtextnode);
                                    thispage.TextSelectArea.children[indx].appendChild(vnode);

                                }
                                if(vsplittextarr[via] != ''){
                                    vtextnode = document.createTextNode(vsplittextarr[via]);
                                    thispage.TextSelectArea.children[indx].appendChild(vtextnode);

                                }

                            }
                            
                            /* var vnumwords = 0;
                            for (var vi = 0; vi < vsplittext.length; vi++) {
                                var vsubpos = vsplittext[vi].search(vspantext);
                                if (vsubpos == -1) {
                                    if (vnumwords > 0) {
                                        var vtextnode = document.createTextNode(vsplittext[vi] + " ");
                                    }
                                    else {
                                        vtextnode = document.createTextNode(vsplittext[vi]);
                                    }
                                    thispage.TextSelectArea.children[indx].appendChild(vtextnode);
                                    vnumwords++;
                                }
                                else if (vsubpos >= 0) {
                                    var vnode = document.createElement("span");
                                    vnode.className = "highlight"; // Create a <span> node
                                    vtextnode = document.createTextNode(vspantext); // Create a text node
                                    vnode.appendChild(vtextnode); // Append the text to <li>
                                    thispage.TextSelectArea.children[indx].appendChild(vnode);
                                    vnumwords = 0;
                                }
                            } */
                        }
                    }
                };

                this.getVectorSearchList = function () {
                    
                    thispage.getVectorSearchListSearch(thispage.DocRef.searchobj);
                   
                };

                this.getVectorSearchListSearch = function(searchobj){
                    
                    var searcharray = thispage.indexdivTexts();
                    var casesense = searchobj.casesens;
                    var str = searchobj.expr;

                    for (var txtcnt = 0; txtcnt < searcharray.length; txtcnt++) {
                        if (!casesense) {
                            var searchexpr = str.toLowerCase();
                            var searchstr = searcharray[txtcnt].toLowerCase();
                        }
                        else {
                            searchexpr = str;
                            searchstr = searcharray[txtcnt];
                        }
                        var found = false;
                        var pos = searchstr.search(searchexpr);
                        if (pos == -1) {
                            found = false;
                        }
                        else if (pos >= 0) {
                            found = true;
                        }
                        if (found) {
                            var resultobj = {
                                page: thispage.pagenumber,
                                indx: txtcnt,
                                searchtext: searcharray[txtcnt],
                                pos: pos,
                                length: str.length,
                                rect : {}
                            };
                            searchobj.matcharray.push(resultobj);
                        }
                    }

                };

                this.getVectorSearchListRect = function(searchobj){
                    var searcharray = thispage.indexdivTexts();
                    var casesense = searchobj.casesens;
                    var str = searchobj.expr;
                    for (var txtcnt = 0; txtcnt < searcharray.length; txtcnt++) {
                        if (!casesense) {
                            var searchexpr = str.toLowerCase();
                            var searchstr = searcharray[txtcnt].toLowerCase();
                        }
                        else {
                            searchexpr = str;
                            searchstr = searcharray[txtcnt];
                        }
                        var found = false;
                        var pos = searchstr.search(searchexpr);
                        if (pos == -1) {
                            found = false;
                        }
                        else if (pos >= 0) {
                            found = true;
                        }
                        if (found) {
                            var resultobj = {
                                page: thispage.pagenumber,
                                indx: txtcnt,
                                searchtext: searcharray[txtcnt],
                                pos: pos,
                                length: str.length,
                                rect : {}
                            };
                            searchobj.matcharray.push(resultobj);
                        }
                    }

                };
                this.getPDFSearchList = function () {
                    //var resultarray = [];
                    var searcharray = thispage.indexdivTexts();
                    var casesense = thispage.DocRef.searchobj.casesens;
                    var str = thispage.DocRef.searchobj.expr;
                    for (var txtcnt = 0; txtcnt < searcharray.length; txtcnt++) {
                        if (!casesense) {
                            var searchexpr = str.toLowerCase();
                            var searchstr = searcharray[txtcnt].toLowerCase();
                        }
                        else {
                            searchexpr = str;
                            searchstr = searcharray[txtcnt];
                        }
                        var found = false;
                        var pos = searchstr.search(searchexpr);
                        if (pos == -1) {
                            found = false;
                        }
                        else if (pos >= 0) {
                            found = true;
                        }
                        if (found) {
                            var resultobj = {
                                page: thispage.pagenumber,
                                indx: txtcnt,
                                searchtext: searcharray[txtcnt],
                                pos: pos,
                                length: str.length
                            };
                            thispage.DocRef.searchobj.matcharray.push(resultobj);
                        }
                    }
                    //return resultarray;
                };
                this.getrotcoords = function (cx, cy, x, y, angle) {
                    var radians = (Math.PI / 180) * angle, cos = Math.cos(radians), sin = Math.sin(radians), nx = (cos * (x - cx)) + (sin * (y - cy)) + cx, ny = (cos * (y - cy)) - (sin * (x - cx)) + cy;
                    return {
                        x: nx,
                        y: ny
                    };
                };
                this.getSearchTextRect = function(){

                    if (thispage.DocRef.extractsearchobj.page == thispage.pagenumber) {

                        if (thispage.usevectorxml) {
                            thispage.settextdivs();
                            var text = thispage.DocRef.extractsearchobj.matcharray[thispage.DocRef.extractsearchobj.indx].searchtext;
                            var counter = thispage.DocRef.extractsearchobj.matcharray[thispage.DocRef.extractsearchobj.indx].indx;
                            var pos = thispage.DocRef.extractsearchobj.matcharray[thispage.DocRef.extractsearchobj.indx].pos;
                            var length = thispage.DocRef.extractsearchobj.matcharray[thispage.DocRef.extractsearchobj.indx].length;
    
    
                        }
                        if (thispage.usepdfjs) {
                            
                            text = thispage.DocRef.extractsearchobj.matcharray[thispage.DocRef.extractsearchobj.indx].searchtext;
                            counter = thispage.DocRef.extractsearchobj.matcharray[thispage.DocRef.extractsearchobj.indx].indx;
                            pos = thispage.DocRef.extractsearchobj.matcharray[thispage.DocRef.extractsearchobj.indx].pos;
                            length = thispage.DocRef.extractsearchobj.matcharray[thispage.DocRef.extractsearchobj.indx].length;
                         
                        }
    
                        var divpos = thispage.getdivpos(counter);

                        thispage.DocRef.extractsearchobj.matcharray[thispage.DocRef.extractsearchobj.indx].rect = divpos;
                    }

                    var centery = canvasoheight / 2;
                    var centerx = canvasowidth / 2;
                    var panx = 0;
                    var pany = 0;
                    var pagerect = thispage.getpagerect();

                    if (thispage.getpageRotation() != 0) {
                        var rotpos = thispage.getrotcoords(centerx, centery, divpos.x, divpos.y, -thispage.getpageRotation());
                        
                    }else{
                        //var distancexright = Math.max(divpos.x, canvasowidth) - Math.min(divpos.x, canvasowidth);
                    }

                    //thispage.DocRef.extractsearchobj.matcharray[thispage.DocRef.extractsearchobj.indx].rect = divpos;
                    
                    /*if (thispage.gettextdivrect(counter, text, pos, length)){
                        thispage.DocRef.extractsearchobj.matcharray[thispage.DocRef.extractsearchobj.indx].rect = divpos;
                        thispage.DocRef.extractsearchobj.foundrects.push(thispage.DocRef.extractsearchobj.matcharray[thispage.DocRef.extractsearchobj.indx]);

                    }*/
                    
                    //thispage.marktextdiv(counter, text, pos, length);

                };
                this.getsearchpanupdate = function (x, y) {
                    //var centery = canvasoheight / 2;
                    //var centerx = canvasowidth / 2;


                    var centery = canvasoheightsize / 2;
                    var centerx = canvasowidthsize / 2;
                    var canvasowidth = canvasowidthsize;
                    var canvasoheight = canvasoheightsize;


                    var panx = 0;
                    var pany = 0;
                    if (thispage.getpageRotation() != 0) {
                        var rotpos = thispage.getrotcoords(centerx, centery, x, y, -thispage.getpageRotation());
                        distancexright = Math.max(rotpos.x, canvasowidth) - Math.min(rotpos.x, canvasowidth);
                        if (Math.max(rotpos.x, canvasowidth) == rotpos.x) {
                            move = 1;
                            //panx =  distancexright + 300;
                            panx = rotpos.x - centerx;
                        }
                        else if (distancexright < 300) {
                            move = 1;
                            panx = rotpos.x - centerx;
                        }
                        distanceybottom = Math.max(rotpos.y, canvasoheight) - Math.min(rotpos.y, canvasoheight);
                        if (Math.max(rotpos.y, canvasoheight) == rotpos.y) {
                            move = 2;
                            pany = rotpos.y - centery;
                        }
                        else if (distanceybottom < 100) {
                            move = 2;
                            pany = rotpos.y - centery;
                        }
                        distancexleft = Math.max(rotpos.x, 0) - Math.min(rotpos.x, 0);
                        if (Math.min(rotpos.x, 0) == rotpos.x) {
                            move = 0;
                            panx = -(centerx - rotpos.x);
                        }
                        else if (distancexleft < 100) {
                            move = 0;
                            panx = -(centerx - rotpos.x);
                        }
                        distanceytop = Math.max(rotpos.y, 0) - Math.min(rotpos.y, 0);
                        if (Math.min(rotpos.y, 0) == rotpos.y) {
                            move = 3;
                            pany = -(centery - rotpos.y);
                        }
                        else if (distanceytop < 100) {
                            move = 3;
                            pany = -(centery - rotpos.y);
                        }
                        /*switch (thispage.getpageRotation()) {
                            case 90:
                                //this.dxvector -= sy;
                                //this.dyvector += sx;
                                thispage.pan_update(pany,-panx);
        
                                break;
                            case 180:
                                //this.dxvector += sx;
                                //this.dyvector += sy;
                                thispage.pan_update(-panx,-pany);
                                break;
                            case 270:
                                //this.dxvector += sy;
                                //this.dyvector -= sx;
                                thispage.pan_update(-pany,panx);
                                break;
                        }*/
                        //thispage.pan_update(panx,pany);
                    }
                    else {
                        var distancexright = Math.max(x, canvasowidth) - Math.min(x, canvasowidth);
                        if (Math.max(x, canvasowidth) == x) {
                            move = 1;
                            //panx =  distancexright + 300;
                            panx = x - centerx;
                        }
                        else if (distancexright < 300) {
                            move = 1;
                            panx = x - centerx;
                        }
                        var distanceybottom = Math.max(y, canvasoheight) - Math.min(y, canvasoheight);
                        if (Math.max(y, canvasoheight) == y) {
                            move = 2;
                            pany = y - centery;
                        }
                        else if (distanceybottom < 100) {
                            move = 2;
                            pany = y - centery;
                        }
                        var distancexleft = Math.max(x, 0) - Math.min(x, 0);
                        if (Math.min(x, 0) == x) {
                            var move = 0;
                            panx = -(centerx - x);
                            //var panx = distancexleft - 200;
                        }
                        else if (distancexleft < 100) {
                            move = 0;
                            panx = -(centerx - x);
                        }
                        var distanceytop = Math.max(y, 0) - Math.min(y, 0);
                        if (Math.min(y, 0) == y) {
                            move = 3;
                            pany = -(centery - y);
                        }
                        else if (distanceytop < 100) {
                            move = 3;
                            pany = -(centery - y);
                        }
                        //console.log('panupdate show search');
                        //thispage.pan_update(panx,pany);
                    }


                    return { x: panx, y: pany };
                };
                this.loadfloorplan = function () {
                    for (var findx = 0; findx < thispage.floorplans.length; findx++) {
                        thispage.floorplanlolad(findx);
                        //floorplan = thispage.floorplans[0].SRC;
                    }
                };
                this.drawcamerapos = function () {
                    if (thispage.bis3Dnavigator && bDrawNavCam) {
                        thispage.birdseyecamera();
                    }
                };
                this.setFloorNavScaleOffset = function (alignarray) {
                    if (!thispage.bis3Dnavigator) {
                        return;
                    }
                    
                    //var bscreendist = alignarray[0].scaledist.scaledDistance;
                    //var oscreendist = alignarray[1].scaledist.scaledDistance;
                    //var bscale = alignarray[0].scaledist.curscale;
                    //var oscale = alignarray[1].scaledist.curscale;

                    var bscreendist = alignarray[0].scaleinfo.scaledDistance;
                    var oscreendist = alignarray[1].scaleinfo.scaledDistance;

                    var bscale = alignarray[0].scaleinfo.curscale;
                    var oscale = alignarray[1].scaleinfo.curscale;


                    var bxdist = (alignarray[0].dist / alignarray[0].pwidth);
                    var oxdist = (alignarray[1].dist / alignarray[1].pwidth);
                    var replaceScale = oxdist / bxdist;
                    var newreplaceScale = (oscreendist * oscale) / (bscreendist * bscale);
                    var testrepScale = ((oscreendist * oscale) / bscale) / (bscreendist);
                    var oscalecalc = bscreendist / oscreendist;
                    var roscale = (testrepScale * bscale * bscreendist) / oscreendist;
                    //var newreplaceScale =  (bscreendist * bscale) /(oscreendist * oscale); 
                    //var bxdist = (alignarray[0].dist / alignarray[0].pwidth) * thispage.navfloorplan.originalWidth;
                    //var oxdist = (alignarray[1].dist / alignarray[1].pwidth) * thispage.originalwidth;
                    var scaleratio = (alignarray[1].dist / alignarray[1].pwidth) / (alignarray[0].dist / alignarray[0].pwidth);
                    //var replaceScale = oxdist / bxdist;
                    thispage.navfloorplan.RScale = oscalecalc;
                    var oxoffset = (alignarray[1].offset.x * oscalecalc);
                    var oyoffset = (alignarray[1].offset.y * oscalecalc);
                    //nned a new method for this.
                    //need scale from image with floorplan place in alignarray
                    //thispage.navfloorplan.BScale = thisthumb.getfloorplanBsale(thisthumb.birdseyeWidth, thisthumb.birdseyeHeight, thisthumb.floorplans[floornum].originalWidth, thisthumb.floorplans[floornum].originalHeight);      
                    var bxoffset = alignarray[0].offset.x;
                    var byoffset = alignarray[0].offset.y;
                    //bxoffset *= bscale;
                    //byoffset *= bscale;
                    //var OffsetX = -((oxoffset) - (bxoffset));
                    //var OffsetY = -((oyoffset) - (byoffset));
                    //OffsetX *= bscale;
                    //OffsetY *= bscale;
                    //OffsetX *= testrepScale;
                    //OffsetY *= testrepScale;
                    //var tstOffsetX = -(oxoffset - bxoffset);
                    //var tstOffsetY = -(oyoffset - byoffset);
                    var OffsetX = -(oxoffset - bxoffset);
                    var OffsetY = -(oyoffset - byoffset);
                    thispage.navfloorplan.offsetX = OffsetX;
                    thispage.navfloorplan.offsetY = OffsetY;
                    thispage.navfloorplan.replaceScale = testrepScale;
                    thispage.navfloorplan.replaced = true;
                    /*this.floorplans.push({
                        index : j,
                        SRC : fsrc,
                        Elevation : felev,
                        Scale : fscale,
                        BScale : fscale,
                        RScale : fscale,
                        image :floorplanimage,
                        canvas : floorplancnv,
                        ctx : floorplanctx,
                        replaced : false,
                        originalWidth : 0,
                        originalHeight : 0,
                        width : 0,
                        height : 0,
                        offsetX : 0,
                        offsetY : 0,
                        alignarray : []
                    });*/
                };
                this.setAs3DNavigator = function (floorplanobjects, floorindex) {
                    var FloorElevations = [];
                    for (var fpi = 0; fpi < floorplanobjects.length; fpi++) {
                        FloorElevations.push({ Elevation: floorplanobjects[fpi].Elevation, index: fpi });
                        
                    }
                    if (floorindex != null) {
                        thispage.bis3Dnavigator = true;
                        thispage.navfloorplan = {
                            index: floorplanobjects[floorindex].index,
                            SRC: floorplanobjects[floorindex].SRC,
                            Elevation: floorplanobjects[floorindex].Elevation,
                            Elevations: FloorElevations,
                            Scale: floorplanobjects[floorindex].Scale,
                            BScale: floorplanobjects[floorindex].BScale,
                            RScale: floorplanobjects[floorindex].RScale,
                            image: floorplanobjects[floorindex].image,
                            canvas: floorplanobjects[floorindex].canvas,
                            ctx: floorplanobjects[floorindex].ctx,
                            replaced: floorplanobjects[floorindex].replaced,
                            originalWidth: floorplanobjects[floorindex].originalWidth,
                            originalHeight: floorplanobjects[floorindex].originalHeight,
                            width: floorplanobjects[floorindex].width,
                            height: floorplanobjects[floorindex].height,
                            offsetX: floorplanobjects[floorindex].offsetX,
                            offsetY: floorplanobjects[floorindex].offsetY,
                            alignarray: floorplanobjects[floorindex].alignarray
                        };
                        //floorplanobjects[floorindex];
                        if (thispage.birdseyemarker == null) {
                            thispage.birdseyemarker = new birdseyemarker(canvas);
                        }
                    }
                };
                this.birdseyecamera = function () {
                    if (!thispage.bis3Dnavigator) {
                        return;
                    }
                    if (!thispage.DocRef.bActive) {
                        return;
                    }
                    if (!thispage.navfloorplan) {
                        return;
                    }
                    context.clearRect(0, 0, canvas.width, canvas.height);
                    thispage.birdseyemarker.drawme(context, true);
                };
                this.getFloorLevel = function () {
                    var floorlevel = null;
                    if (thispage.navfloorplan) {
                        floorlevel = { floorindex: thispage.navfloorplan.index, replaced: thispage.navfloorplan.replaced };
                    }
                    return floorlevel;
                };
                this.get3DNavElevation = function (floorlevelobj) {
                    if (thispage.navfloorplan.index == floorlevelobj.max) {
                        return true;
                    }
                    else {
                        return false;
                    }
                };
                this.setcamerapos = function (position, rotation, boxcenter) {
                    thispage.camerapos = position.clone();
                    //thisthumb.camerarot = rotation.clone();
                    thispage.boxcenter = boxcenter.clone();
                    //const euler = new THREE.Euler();
                    //const camerarotation = euler.setFromQuaternion(thisthumb.camerarot);
                    //const radians = camerarotation.y > 0 ? camerarotation.y : (2 * Math.PI) + camerarotation.y; 
                    //const degrees = THREE.Math.radToDeg(radians);
                    //thisthumb.camerarot.order = 'YXZ';
                    thispage.setbirdseyecamera(thispage.camerapos, rotation, boxcenter);
                };
                this.getnavcampos = function (modelunit, boxcenter, mousePos) {
                    if (!thispage.DocRef.bActive) {
                        return;
                    }
                    if (!thispage.navfloorplan) {
                        return;
                    }
                    if (boxcenter == null) {
                        return;
                    }
                    thispage.birdseyedispeye.distance = 0;
                    thispage.birdseyedispeye.rotation = { degrees: 90, radians: (Math.PI * 0.5) };

                    //this.setbirdseyecamera
                    var pagerect = thispage.getpagedim();
                    thispage.birdseyedispeye.r = 32;
                    if (!thispage.navfloorplan.replaced) {
                        thispage.birdseyedispeye.x = (pagerect.w * 0.5) + pagerect.x;
                        thispage.birdseyedispeye.y = (pagerect.h * 0.5) + pagerect.y;
                        thispage.birdseyedispeye.scale = pagerect.scale * thispage.navfloorplan.Scale;
                    }
                    else {
                        var curdisplayscale = (pagerect.scale / thispage.navfloorplan.RScale);
                        thispage.birdseyedispeye.x = (thispage.navfloorplan.originalWidth * 0.5 * curdisplayscale) + pagerect.x;
                        thispage.birdseyedispeye.y = (thispage.navfloorplan.originalHeight * 0.5 * curdisplayscale) + pagerect.y;
                        thispage.birdseyedispeye.scale = curdisplayscale * thispage.navfloorplan.Scale;
                        //thispage.birdseyedispeye.scale *= thispage.navfloorplan.replaceScale;
                        //var offsetscale = curdisplayscale / thispage.navfloorplan.replaceScale;
                        thispage.birdseyedispeye.x += -(thispage.navfloorplan.offsetX * curdisplayscale);
                        thispage.birdseyedispeye.y += -(thispage.navfloorplan.offsetY * curdisplayscale);
                        //thispage.birdseyemarker.drawme(context, true);
                        //thispage.birdseyedispeye.x += -(thispage.navfloorplan.offsetX);
                        //thispage.birdseyedispeye.y -= -(thispage.navfloorplan.offsetY);
                        //thispage.birdseyedispeye.scale = (thispage.navfloorplan.RScale / pagerect.scale)  * thispage.navfloorplan.Scale;
                    }
                    /*if(!thispage.navfloorplan.replaced){
                        thispage.birdseyedispeye.scale = pagerect.scale * thispage.navfloorplan.Scale;
                    }else{
                        thispage.birdseyedispeye.scale = thispage.navfloorplan.Scale;
        
                        thispage.birdseyedispeye.scale *= thispage.navfloorplan.replaceScale;
                        thispage.birdseyedispeye.x += -(thispage.navfloorplan.offsetX);
                        thispage.birdseyedispeye.y -= -(thispage.navfloorplan.offsetY);
        
                    }*/
                    //thispage.birdseyedispeye.scale = pagerect.scale * thispage.navfloorplan.Scale;
                    thispage.birdseyedispeye.boxcenter = boxcenter;
                    thispage.birdseyedispeye.camerapos = boxcenter;
                    var floorindex = thispage.navfloorplan.index;
                    //thispage.floorplans[fpi].Elevation
                    var cameraz = thispage.getCurfloorElevation(floorindex, modelunit);
                    //var campos = thispage.birdseyemarker.getcamera(thispage.birdseyedispeye, mousePos.x, mousePos.y);
                    var campos = thispage.birdseyemarker.getcamera(thispage.birdseyedispeye, mousePos.xus, mousePos.yus);

                    campos.z = cameraz;
                    //thispage.birdseyemarker.setcamera(thispage.birdseyedispeye);
                    return campos;
                };
                this.getfloorheight = function (modelunit) {
                    if (modelunit == 1) {
                        //var zoffset = (1.8 - zdiff) * modelscale;
                        return 1.8;
                    }
                    else if (modelunit == 0) {
                        //zoffset = (1800 - zdiff) * modelscale;
                        return 1800;
                    }
                    else if (modelunit == 2) {
                        return 5.9;
                    }
                    else if (modelunit == 3) {
                        return 69;
                    }
                    else if (modelunit == 4) {
                        return 180;
                    }
                };
                this.getCurfloorElevation = function (floorindex, modelunit) {
                    var elevation = null;
                    if (thispage.navfloorplan != undefined) {
                        if (thispage.navfloorplan.index == floorindex) {
                            var zoffset = thispage.getfloorheight(modelunit);
                            elevation = thispage.navfloorplan.Elevation + zoffset;
                        }
                    }
                    return elevation;
                };
                this.setbirdseyecamera = function (position, rotation, boxcenter) {
                    if (!thispage.DocRef.bActive) {
                        return;
                    }
                    if (!thispage.navfloorplan) {
                        return;
                    }
                    if (boxcenter == null) {
                        //boxcenter = thisthumb.DocRef.getBoxcenter();
                        if (boxcenter == null) {
                            return;
                        }
                    }
                    if (position == null) {
                        //position = thisthumb.DocRef.getBoxcenter();
                        if (position == null) {
                            return;
                        }
                    }
                    if (rotation == null) {
                        rotation = { degrees: 90, radians: (Math.PI * 0.5) };
                    }
                    thispage.birdseyedispeye.distance = position.distanceTo(boxcenter);
                    thispage.birdseyedispeye.rotation = rotation;
                    //this.setbirdseyecamera
                    var pagerect = thispage.getpagedim();
                    thispage.birdseyedispeye.r = 32;
                    if (!thispage.navfloorplan.replaced) {
                        thispage.birdseyedispeye.scale = pagerect.scale * thispage.navfloorplan.Scale;
                        thispage.birdseyedispeye.x = (pagerect.w * 0.5) + pagerect.x;
                        thispage.birdseyedispeye.y = (pagerect.h * 0.5) + pagerect.y;
                    }
                    else {
                        var curdisplayscale = (pagerect.scale / thispage.navfloorplan.RScale);
                        thispage.birdseyedispeye.x = (thispage.navfloorplan.originalWidth * 0.5 * curdisplayscale) + pagerect.x;
                        thispage.birdseyedispeye.y = (thispage.navfloorplan.originalHeight * 0.5 * curdisplayscale) + pagerect.y;
                        thispage.birdseyedispeye.scale = curdisplayscale * thispage.navfloorplan.Scale;
                        //thispage.birdseyedispeye.scale = (thispage.navfloorplan.RScale / pagerect.scale)  * thispage.navfloorplan.Scale;
                        thispage.birdseyedispeye.x += -(thispage.navfloorplan.offsetX * curdisplayscale);
                        thispage.birdseyedispeye.y += -(thispage.navfloorplan.offsetY * curdisplayscale);
                    }
                    thispage.birdseyedispeye.boxcenter = boxcenter;
                    thispage.birdseyedispeye.camerapos = position;
                    thispage.birdseyemarker.setcamera(thispage.birdseyedispeye);
                    //var width = thispage.birdseyeGUIindcnv.width;
                    //var height = thispage.birdseyeGUIindcnv.height;
                    //thispage.birdseyedispeye = { x: 0, y: 0, r: 0, scale : 1,boxcenter : null, camerapos : null, rotation : 0};
                };
                this.showFoxitSearchOnUpdate = function(){
                    thispage.drawtextSearchRect = true;
                };
                this.showFoxitSearch = function () {
                    if (thispage.DocRef.foxitsearchobj.page == thispage.pagenumber) {
                        var curmatch = thispage.DocRef.foxitsearchobj.getcurmatch();
                        var x = ((curmatch.rectArray[0].left * (thispage.curpagescale * thispage.dscalepdf)) + thispage.dxpdf);
                        var y = (((thispage.pdfpageheight - curmatch.rectArray[0].top) * (thispage.curpagescale * thispage.dscalepdf)) + thispage.dypdf);
                        //var right = ((curmatch.rectArray[0].right * (thispage.curpagescale * thispage.dscalepdf)) + thispage.dxpdf);
                        //var bottom = (((thispage.pdfpageheight - curmatch.rectArray[0].bottom) * (thispage.curpagescale * thispage.dscalepdf)) + thispage.dypdf);
                        //var width = right - x;
                        //var height = bottom - y;
                        //cntximg.fillRect(x, y, width, height);
                        var panto = thispage.getsearchpanupdate(x, y);
                        thispage.pan_update(panto.x, panto.y);
                        thispage.drawtextSearchRect = true;
                        thispage.drawsearchRect();
                        //get current text rect from double foxit search array and navigate to that location 
                        //then draw the rectangle on top of text.
                        //var text = thispage.DocRef.foxitsearchobj.matcharray[thispage.DocRef.searchobj.indx].searchtext;
                        //var counter = thispage.DocRef.searchobj.matcharray[thispage.DocRef.searchobj.indx].indx;
                        //var pos = thispage.DocRef.searchobj.matcharray[thispage.DocRef.searchobj.indx].pos;
                        //var length = thispage.DocRef.searchobj.matcharray[thispage.DocRef.searchobj.indx].length;
                        //thispage.marktextdiv(counter,text,pos,length);
                        //var rectpos = thispage.getdivpos(counter);
                        //var panto = thispage.getsearchpanupdate(divpos.x,divpos.y);
                        //thispage.pan_update(panto.x, panto.y);
                    }
                };
                this.showBookmark = function (bookmark) {
                    if (bookmark.page == thispage.pagenumber) {
                        //left: 22
                        //page: 1 //0 indexed page number
                        //top: 617
                        var x = ((bookmark.left * (thispage.curpagescale * thispage.dscalepdf)) + thispage.dxpdf);
                        var y = (((thispage.pdfpageheight - bookmark.top) * (thispage.curpagescale * thispage.dscalepdf)) + thispage.dypdf);
                        //var right = ((curmatch.rectArray[0].right * (thispage.curpagescale * thispage.dscalepdf)) + thispage.dxpdf);
                        //var bottom = (((thispage.pdfpageheight - curmatch.rectArray[0].bottom) * (thispage.curpagescale * thispage.dscalepdf)) + thispage.dypdf);
                        //var width = right - x;
                        //var height = bottom - y;
                        //cntximg.fillRect(x, y, width, height);
                        //var panto = thispage.getsearchpanupdate(x,y);
                        thispage.pan_update(x, y);
                    }
                };
                this.showSearch = function () {
                    if (thispage.DocRef.searchobj.page == thispage.pagenumber) {
                        if (thispage.usevectorxml) {
                            thispage.settextdivs();
                        }
                        var text = thispage.DocRef.searchobj.matcharray[thispage.DocRef.searchobj.indx].searchtext;
                        var counter = thispage.DocRef.searchobj.matcharray[thispage.DocRef.searchobj.indx].indx;
                        var pos = thispage.DocRef.searchobj.matcharray[thispage.DocRef.searchobj.indx].pos;
                        var length = thispage.DocRef.searchobj.matcharray[thispage.DocRef.searchobj.indx].length;
                        //thispage.marktextdiv(counter,text,pos,length);
                        var divpos = thispage.getdivpos(counter);
                        //var visible = this.istextvisible(divpos.x, divpos.y);
                        /*
                        0 = right
                        1 = left
                        2 = up
                        3 = down
                       */
                        //var centery = canvasoheight / 2;
                        //var centerx = canvasowidth / 2;
                        //var panx = 0;
                        //var pany = 0;
                        var panto = thispage.getsearchpanupdate(divpos.x, divpos.y);
                        thispage.pan_update(panto.x, panto.y);
                        /*if(thispage.getpageRotation() != 0){
                            var rotpos = thispage.getrotcoords(centerx,centery,divpos.x,divpos.y, -thispage.getpageRotation());
                            //divpos.x = rotpos.x;
                            //divpos.y = rotpos.y;
                            distancexright = Math.max(rotpos.x,canvasowidth) - Math.min(rotpos.x,canvasowidth);
                            if (Math.max(rotpos.x,canvasowidth) == rotpos.x){
        
                                move = 1;
                                //panx =  distancexright + 300;
                                panx = rotpos.x - centerx;
                            }else if(distancexright < 300) {
                                move = 1;
                                panx = rotpos.x - centerx;
                            }
                            distanceybottom = Math.max(rotpos.y,canvasoheight) - Math.min(rotpos.y,canvasoheight);
                            if (Math.max(rotpos.y,canvasoheight) == rotpos.y){
                                move = 2;
                                pany = rotpos.y - centery;
                            }else if(distanceybottom < 100){
                                move = 2;
                                pany = rotpos.y - centery;
        
                            }
                            distancexleft = Math.max(rotpos.x,0) - Math.min(rotpos.x,0);
                            if (Math.min(rotpos.x,0) == rotpos.x){
                                move = 0;
                                panx = -(centerx - rotpos.x);
        
                            }else if(distancexleft < 100) {
                                move = 0;
                                panx = -(centerx - rotpos.x);
                            }
                            distanceytop = Math.max(rotpos.y,0) - Math.min(rotpos.y,0);
                            if (Math.min(rotpos.y,0) == rotpos.y){
                                move = 3;
                                pany = -(centery - rotpos.y);
                            }else if(distanceytop < 100){
                                move = 3;
                                pany = -(centery - rotpos.y);
                            }
        
                            /*switch (thispage.getpageRotation()) {
                                case 90:
                                    //this.dxvector -= sy;
                                    //this.dyvector += sx;
                                    thispage.pan_update(pany,-panx);
        
                                    break;
                                case 180:
                                    //this.dxvector += sx;
                                    //this.dyvector += sy;
                                    thispage.pan_update(-panx,-pany);
                                    break;
                                case 270:
                                    //this.dxvector += sy;
                                    //this.dyvector -= sx;
                                    thispage.pan_update(-pany,panx);
                                    break;
                            }*/
                        //thispage.pan_update(panx,pany);
                        /*}else{
                            var distancexright = Math.max(divpos.x,canvasowidth) - Math.min(divpos.x,canvasowidth);
                            if (Math.max(divpos.x,canvasowidth) == divpos.x){
        
                                move = 1;
                                //panx =  distancexright + 300;
                                panx = divpos.x - centerx;
        
        
                            }else if(distancexright < 300) {
                                move = 1;
                                panx = divpos.x - centerx;
        
                            }
                            var distanceybottom = Math.max(divpos.y,canvasoheight) - Math.min(divpos.y,canvasoheight);
                            if (Math.max(divpos.y,canvasoheight) == divpos.y){
                                move = 2;
                                pany = divpos.y - centery;
        
                            }else if(distanceybottom < 100){
                                move = 2;
                                pany = divpos.y - centery;
                            }
        
                            var distancexleft = Math.max(divpos.x,0) - Math.min(divpos.x,0);
                            if (Math.min(divpos.x,0) == divpos.x){
        
                                var move = 0;
                                panx = -(centerx - divpos.x);
                                //var panx = distancexleft - 200;
        
                            }else if(distancexleft < 100) {
                                move = 0;
                                panx = -(centerx - divpos.x);
                           }
        
                            var distanceytop = Math.max(divpos.y,0) - Math.min(divpos.y,0);
                            if (Math.min(divpos.y,0) == divpos.y){
                                move = 3;
                                pany = -(centery - divpos.y);
                            }else if(distanceytop < 100){
                                move = 3;
                                pany = -(centery - divpos.y);
                            }
                            console.log('panupdate show search');
                            thispage.pan_update(panx,pany);
                            
                        }*/
                        //thispage.pan_update(panx,pany);
                        thispage.marktextdiv(counter, text, pos, length);
                        /*if(visible){
                            thispage.pan_update();
                        }*/
                        //console.log(visible);
                        //console.log(divpos.y);
                        //console.log(canvas.height);
                    }
                };
                this.foxitClearSearch = function () {
                    if (thispage.DocRef.foxitsearchobj.page == thispage.pagenumber) {
                        thispage.drawtextSearchRect = false;
                        //remove marked text.
                    }
                };
                this.clearSearch = function () {
                    if (thispage.DocRef.searchobj.page == thispage.pagenumber) {
                        var text = thispage.DocRef.searchobj.matcharray[thispage.DocRef.searchobj.indx].searchtext;
                        var counter = thispage.DocRef.searchobj.matcharray[thispage.DocRef.searchobj.indx].indx;
                        thispage.clearmarktextdiv(counter, text);
                    }
                };
                /*this.getPDFpagetext = function (){
        
                    var resultarray = [];
                    var searcharray = thispage.indexdivTexts();
        
                    var casesense = thispage.DocRef.searchobj.casesens;
                    var str = thispage.DocRef.searchobj.expr;
        
                    for (var txtcnt = DocRef.searchobj.indx;txtcnt < searcharray.length;txtcnt++){
        
                        if(!casesense){
                            var searchexpr = str.toLowerCase();
                            var searchstr = searcharray[txtcnt].toLowerCase();
                        }else{
                            searchexpr = str;
                            searchstr = searcharray[txtcnt];
        
                        }
                        var found = false;
                        var pos = searchstr.search(searchexpr);
                        if (pos == -1) {
                            found = false
                        } else if (pos >= 0) {
                            found = true;
                        }
        
                        if (found){
                            //console.log(thispage.pagenumber + 1,txtcnt, searchstr);
                            var resultobj = {
                              page : thispage.pagenumber,
                              indx : txtcnt,
                              searchtext : searcharray[txtcnt],
                              pos : pos,
                              length : str.length
                            };
                            resultarray.push(resultobj);
        
                            if(thispage.DocRef.searchobj.page == thispage.pagenumber){
                                thispage.marktextdiv(txtcnt,searcharray[txtcnt],pos,str.length);
                            }
                            DocRef.searchobj.indx = txtcnt;
        
        
                        }
                    }
        
                };*/
                this.loadimages = function () {
                    //get_image(this.SmallImageSRC,this.smallimage);
                    //showDownloadDialog();
                    if (RxCore_GUI_Download != undefined) {
                        RxCore_GUI_Download.setDownload("show");
                    }
                    //this.smallimage.addEventListener('load', this.smallimageload, false);
                    //this.smallimage.addEventListener('progress', this.smallimageprogress, false);
                    //this.smallimage.src = this.SmallImageSRC;
                    //thispage.smallimageload();
                    //draw_image(ev.srcElement);
                    //get_image(this.MainImageSRC,this.largeimage);
                    //showDownloadDialog();
                    /*if (RxCore_GUI_Download != undefined) {
                        RxCore_GUI_Download.setDownload("show");
                    }*/
                    thispage.largeimageload();
                    //this.largeimage.addEventListener('load', this.largeimageload, false);
                    //this.largeimage.addEventListener('progress', this.largeimageprogress, false);
                    //this.largeimage.src = this.MainImageSRC;
                    //get_image(this.ThumbnailImageSRC,this.imagethumb);
                };
                //this.loadvector = function(this.Vector2DSRC){
                //  DrawVectors(this.VectorSRC);
                //};
                this.setvectordim = function (dx, dy, dscale) {
                    
                    if(thispage.VectorPageObj == undefined){
                        return;
                    }

                    this.dxvector = dx;
                    this.dyvector = dy;
                    this.dscalevector = dscale;
                    this.initialscale = dscale;
                    this.vectorloaded = true;
                    thispage.setpagedimsize(this.dxvector, this.dyvector, (thispage.VectorPageObj.width * this.dscalevector), (thispage.VectorPageObj.height * this.dscalevector), false);
                    this.pagedimset = true;
                    //thispage.vectorstartrender = new Date().getTime();
                    /*if (this.usedincomposite && this.compositereference != undefined) {
                        //this.compositereference.draw_compare(true);
                        this.compositereference.scaleToBackground(true);
                    }
                    else {
                        
                    }*/
                    thispage.draw_vector(true);
                    //this.draw_vector(true);
                    //thispage.vectorendrender = new Date().getTime();
                    //thispage.vectorpagetime = (thispage.vectorendrender - thispage.vectorstartrender);
                    thispage.vectorpagetime = thispage.VectorPageObj.vectorpagetime;
                    thispage.vectortimervar = setInterval(function () {
                        thispage.vectortimer();
                    }, thispage.vectorpagetime);
                    var textwidth = thispage.endx - thispage.startx;
                    var textheight = thispage.endy - thispage.starty;
                    if (thispage.VectorPageObj.textlist.length > 0) {
                        thispage.settextdivs(true);
                    }
                    if (thispage.TextSelectArea != undefined) {

                        var txtdim = getTexareadim(thispage.startx,thispage.starty, textwidth, textheight);

                        thispage.TextSelectArea.style.top = txtdim.y + "px";
                        thispage.TextSelectArea.style.left = txtdim.x + "px";
                        thispage.TextSelectArea.style.width = txtdim.w + "px";
                        thispage.TextSelectArea.style.height = txtdim.h + "px";


                        //thispage.TextSelectArea.style.top = thispage.starty + "px";
                        //thispage.TextSelectArea.style.left = thispage.startx + "px";
                        //thispage.TextSelectArea.style.width = textwidth + "px";
                        //thispage.TextSelectArea.style.height = textheight + "px";

                        rxcontainer.appendChild(thispage.TextSelectArea);
                        //thispage.TextSelectArea.parentNode = rxcontainer;
                        if (firstpage && thispage.DocRef.textselect) {
                            thispage.TextSelectArea.style.display = szdispvalue;
                        }
                        else {
                            thispage.TextSelectArea.style.display = "none";
                        }
                    }
                    if (firstpage && !thispage.DocRef.markuploaded) {
                        if (bUsemarkupbyref) {
                            getMarkupbyReference(path, thispage.DocRef);
                        }
                        else if (bUsemarkupbyrefEx) {
                            getMarkupxmlurl(thispage.DocRef);
                        }
                        else {
                            getMarkupFilelist(path, thispage.DocRef);
                        }
                        //getMarkupFilelist(path);
                    }
                    if (doCompare) {
                        
                        comparecheckFileLoad(thispage.DocRef.FileName);
                        comparecheck();

                    }
                    else {
                        DeactivateAll();
                        thispage.DocRef.SetActive();
                    }
                    if (thispage.DocRef.bDoPrint) {
                        if (thispage.DocRef.checkprintready()) {
                            printCanvas(PrintPageURL);
                        }
                    }
                    RxCore_default();
                    if (firstpage) {
                        if (bUseFloorplanLoad) {
                            if (Rxcore_GUI_floorLoadComplete != undefined) {
                                var docpage = { docindex: thispage.DocRef.fileindex, pageindex: thispage.pagenumber, floorindex: nFloorplanIndex };
                                Rxcore_GUI_floorLoadComplete.loadComplete(thispage.DocRef.FileName, thispage.DocRef.bActive, docpage);
                            }
                            bUseFloorplanLoad = false;
                            nFloorplanIndex = null;
                        }
                        if (Rxcore_GUI_fileLoadComplete != undefined) {
                            //Rxcore_GUI_fileLoadComplete.loadComplete(thispage.DocRef.FileName);
                            Rxcore_GUI_fileLoadComplete.loadComplete(thispage.DocRef.FileName, thispage.DocRef.bActive);
                        }
                    }
                    if (Rxcore_GUI_pageLoadComplete != undefined) {
                        Rxcore_GUI_pageLoadComplete.loadComplete(thispage.pagenumber);
                    }
                };
                this.removetext = function () {
                    if (thispage.TextSelectArea == undefined) {
                        return;
                    }
                    thispage.TextSelectArea.removeEventListener('wheel', ev_canvas, true);
                    //rxcontainer.removeChild(thispage.TextSelectArea);
                    thispage.TextSelectArea.remove();
                };
                this.selecttext = function () {
                    if (thispage.TextSelectArea == undefined) {
                        return;
                    }
                    if (thispage.DocRef.textselect) {
                        thispage.TextSelectArea.style.display = szdispvalue;
                    }
                    else {
                        thispage.TextSelectArea.style.display = "none";
                    }
                };
                function getTexareadim(x,y,w,h){

                    //const scale = 1;
                    const scale = getPixelRatio();

                    var textleft = (x / scale);
                    var texttop = (y / scale);
                    
                    var textwidth = (w / scale);
                    var textheight = (h / scale);

                    return {x:textleft, y:texttop, w:textwidth, h:textheight};

                }

                this.movetextdivs = function () {
                    if (thispage.TextSelectArea == undefined) {
                        return;
                    }

                    const scale = getPixelRatio();

                    var centery = canvasoheight / 2;
                    var centerx = canvasowidth / 2;
                    thispage.TextSelectArea.removeAttribute("style");


                    var textwidth = thispage.endx - thispage.startx;
                    var textheight = thispage.endy - thispage.starty;
                    var centerxoff = centerx - thispage.startx;
                    var centeryoff = centery - thispage.starty;

                    var txtdim = getTexareadim(thispage.startx,thispage.starty, textwidth, textheight);



                    thispage.TextSelectArea.style.top = txtdim.y + "px";
                    thispage.TextSelectArea.style.left = txtdim.x + "px";
                    thispage.TextSelectArea.style.width = txtdim.w + "px";
                    thispage.TextSelectArea.style.height = txtdim.h + "px";


                    //thispage.TextSelectArea.style.top = thispage.starty + "px";
                    //thispage.TextSelectArea.style.left = thispage.startx + "px";
                    //thispage.TextSelectArea.style.width = textwidth + "px";
                    //thispage.TextSelectArea.style.height = textheight + "px";


                    if (thispage.DocRef.textselect) {
                        thispage.TextSelectArea.style.display = szdispvalue;
                    }
                    else {
                        thispage.TextSelectArea.style.display = "none";
                    }
                    if (this.getpageRotation() != 0) {
                        var szcentertrans = centerxoff + "px" + " " + centeryoff + "px";
                        //thispage.TextSelectArea.style.transformOrigin="50% 50%";
                        thispage.TextSelectArea.style.transformOrigin = szcentertrans;
                        thispage.TextSelectArea.style.transform = "rotate(" + this.getpageRotation() + "deg)";
                    }
                };
                this.settextdivs = function () {
                    if (thispage.TextSelectArea == undefined) {
                        return;
                    }
                    
                    thispage.TextSelectArea.removeAttribute("style");

                    var stats = getPagestats();

                    var textwidth = thispage.endx - thispage.startx;
                    var textheight = thispage.endy - thispage.starty;

                    var txtdim = getTexareadim(stats.dx,stats.dy, textwidth, textheight);

                    thispage.TextSelectArea.style.top = txtdim.y + "px";
                    thispage.TextSelectArea.style.left = txtdim.x + "px";
                    thispage.TextSelectArea.style.width = txtdim.w + "px";
                    thispage.TextSelectArea.style.height = txtdim.h + "px";

                    //thispage.TextSelectArea.style.top = stats.dy + "px";
                    //thispage.TextSelectArea.style.left = stats.dx + "px";

                    //thispage.TextSelectArea.style.top = thispage.starty + "px";
                    //thispage.TextSelectArea.style.left = thispage.startx + "px";
                    
                    //thispage.TextSelectArea.style.width = textwidth + "px";
                    //thispage.TextSelectArea.style.height = textheight + "px";
                    
                    if (thispage.DocRef.textselect) {
                        thispage.TextSelectArea.style.display = szdispvalue;
                    }
                    else {
                        thispage.TextSelectArea.style.display = "none";
                    }
                    if (this.getpageRotation() != 0) {
                        thispage.TextSelectArea.style.transformOrigin = "50% 50%";
                        thispage.TextSelectArea.style.transform = "rotate(" + this.getpageRotation() + "deg)";
                    }
                    var textdata = {
                        textLayerDiv: thispage.TextSelectArea,
                        scale: this.dscalevector,
                        dx: this.dxvector,
                        dy: this.dyvector
                    };
                    thispage.VectorPageObj.TextLayerBuilder(textdata);
                };
                this.cleartextdivs = function () {
                    if (thispage.TextSelectArea == undefined) {
                        return;
                    }
                    var textdata = {
                        textLayerDiv: thispage.TextSelectArea,
                        scale: this.dscalevector,
                        dx: this.dxvector,
                        dy: this.dyvector
                    };
                    thispage.VectorPageObj.TextLayerClear(textdata);
                };
                this.setimagedimlarge = function (dx, dy, dscale) {
                    this.dx = dx;
                    this.dy = dy;
                    this.dscale = dscale;
                    this.largeimageloaded = true;
                    //redraw markup here in case scaling is available after markup is loaded.
                    //documentopen = true;
                    //drawmarkupAll(cntximg);
                    if (firstpage && !thispage.DocRef.markuploaded) {
                        if (bUsemarkupbyref) {
                            getMarkupbyReference(path, thispage.DocRef);
                        }
                        else if (bUsemarkupbyrefEx) {
                            getMarkupxmlurl(thispage.DocRef);
                        }
                        else {
                            getMarkupFilelist(path, thispage.DocRef);
                        }
                        //getMarkupFilelist(path);
                    }
                    if (this.initialzoom == 1) {
                    }
                    else {
                        this.ZoomRealSize();
                    }
                    //this.draw_image(true);
                    if (RxCore_GUI_Download != undefined) {
                        RxCore_GUI_Download.setDownload("hide");
                    }
                    if (doCompare) {
                        
                        comparecheckFileLoad(thispage.DocRef.FileName);
                        comparecheck();

                    }
                    else {
                        DeactivateAll();
                        thispage.DocRef.SetActive();
                    }
                    //this.draw_image();
                    if (thispage.DocRef.bDoPrint) {
                        if (thispage.DocRef.checkprintready()) {
                            printCanvas(PrintPageURL);
                        }
                    }
                    RxCore_default();
                    if (firstpage) {
                        if (bUseFloorplanLoad) {
                            if (Rxcore_GUI_floorLoadComplete != undefined) {
                                var docpage = { docindex: thispage.DocRef.fileindex, pageindex: thispage.pagenumber, floorindex: nFloorplanIndex };
                                Rxcore_GUI_floorLoadComplete.loadComplete(thispage.DocRef.FileName, thispage.DocRef.bActive, docpage);
                            }
                            bUseFloorplanLoad = false;
                            nFloorplanIndex = null;
                        }
                        if (Rxcore_GUI_fileLoadComplete != undefined) {
                            Rxcore_GUI_fileLoadComplete.loadComplete(thispage.DocRef.FileName, thispage.DocRef.bActive);
                        }
                    }
                    if (Rxcore_GUI_pageLoadComplete != undefined) {
                        Rxcore_GUI_pageLoadComplete.loadComplete(thispage.pagenumber);
                    }
                    if (RxCore_GUI_VectorBlocksLoaded != undefined) {
                        RxCore_GUI_VectorBlocksLoaded.setVectorBlocks([]);
                    }
                };
                /*this.setthumbnail = function(dx,dy,dscale){
        
                    thispage.dxthumb=dx;
                    thispage.dythumb=dy;
                    thispage.dscalethumb=dscale;
        
                    if(!thispage.usevector3Dxml){
                        thispage.draw_thumbnail();
                    }
        
                };*/
                this.setimagedimsmall = function (dx, dy, dscale) {
                    this.dxextent = dx;
                    this.dyextent = dy;
                    this.dscaleextent = dscale;
                    this.initialscale = dscale;
                    thispage.setpagedimsize(this.dxextent, this.dyextent, (this.SmallImageWidth * this.dscaleextent), (this.SmallImageHeight * this.dscaleextent), false);
                    this.pagedimset = true;
                    this.smallimageloaded = true;
                    //draw image on screen for the first time after loading here.
                    if (thispage.largeimageloaded) {
                        thispage.dscale = (thispage.smallimagecnv.width * thispage.dscaleextent) / thispage.largeimagecnv.width;
                        thispage.dx = thispage.dxextent;
                        thispage.dy = thispage.dyextent;
                        thispage.bitmapratio = thispage.dscale / thispage.dscaleextent;
                    }
                    else {
                        thispage.dscale = thispage.dscaleextent;
                        thispage.dx = thispage.dxextent;
                        thispage.dy = thispage.dyextent;
                        thispage.bitmapratio = 1;
                    }
                    this.draw_image(true);
                    //this.draw_image();
                    //drawmarkupAll(cntximg);
                };
                this.checkimageswitch = function () {
                    /*if (this.usedincomposite && this.compositereference != undefined && this.isbackground) {
                        if (this.currentimage == 1) {
                            var tempfactor = imageswitchfactor;
                        }
                        else {
                            tempfactor = imageswitchfactor / (this.MainImageWidth / this.SmallImageWidth);
                        }
                        if (this.compositereference.backgroundScale < tempfactor) {
                            this.currentimage = 1;
                        }
                        else {
                            this.currentimage = 0;
                            //alert("switching to large image");
                        }
                        thispage.setpagedimsize(this.dx, this.dy, (this.MainImageWidth * this.dscale), (this.MainImageHeight * this.dscale), false);
                    }
                    else {
                    }*/

                    if ((this.dscale / thispage.bitmapratio) < imageswitchfactor) {
                        this.currentimage = 1;
                    }
                    else {
                        this.currentimage = 0;
                        //alert("switching to large image");
                    }
                    thispage.setpagedimsize(this.dx, this.dy, (this.MainImageWidth * this.dscale), (this.MainImageHeight * this.dscale), false);

                };
                /*this.checkimageswitch = function () {
                    if (this.dscaleextent < imageswitchfactor) {
                        this.currentimage = 1;
                        this.startx = this.dxextent;
                        this.starty = this.dyextent;
                        this.endx = (this.SmallImageWidth * this.dscaleextent) + this.startx;
                        this.endy = (this.SmallImageHeight * this.dscaleextent) + this.starty;
        
        
                    } else {
                        this.currentimage = 0;
                        this.startx = this.dx;
                        this.starty = this.dy;
                        this.endx = (this.MainImageWidth * this.dscale) + this.startx;
                        this.endy = (this.MainImageHeight * this.dscale) + this.starty;
        
                        //alert("switching to large image");
                    }
        
                    /*if (this.dscale > 0.3){
                     this.currentimage = 0;
                     //alert("switching to large image");
                     }else{
                     this.currentimage = 1;
                     }
        
                };*/
                this.ZoomRealSize = function () {
                    //xscale = canvasowidth / thispage.MainImageWidth;
                    var originalwidthpx = thispage.MainImageWidth / this.MainImageScaling;
                    var originalinch = originalwidthpx / thispage.DPI;
                    //assume 96 dpi for canvas
                    var canvaswidthpx = originalinch * 96;
                    this.dscale = canvaswidthpx / thispage.MainImageWidth;
                    /*originalwidthpx = thispage.SmallImageWidth / this.SmallImageScaling;
                    originalinch = originalwidthpx / thispage.DPI;
                    canvaswidthpx = originalinch * 96;
                    this.dscaleextent = canvaswidthpx / this.SmallImageWidth;*/
                    this.dx = (canvasowidth - (this.MainImageWidth * this.dscale)) / 2;
                    this.dy = 0;
                    /*this.dxextent = (canvasowidth - (this.SmallImageWidth * this.dscaleextent)) / 2;
                    this.dyextent = 0;*/
                };
                this.getdx = function () {
                    if (this.usevectorxml) {
                        return this.dxvector;
                    }
                    else if (this.usepdfjs) {
                        return this.dxpdf;
                    }
                    else {
                        return this.dx;
                    }
                };
                this.getdy = function () {
                    if (this.usevectorxml) {
                        return this.dyvector;
                    }
                    else if (this.usepdfjs) {
                        return this.dypdf;
                    }
                    else {
                        return this.dy;
                    }
                };
                this.getdscale = function () {
                    var scale = 1;

                    if (this.usevectorxml) {
                        scale = this.dscalevector;
                    }
                    else if (this.usepdfjs) {
                        scale = this.dscalepdf * this.curpagescale;
                    }
                    else {
                        scale = this.dscale;
                        /*if (this.currentimage == 0){
        
                        }else{
                            return this.dscaleextent;
                        }*/
                    }

                    return scale;
                };

                this.getClipRect = function(rect, callback){

                    var imagedata = undefined;

                    if (thispage.usevector3DGLTF) {
                        
                        var threectx = renderer.getContext();

                        rect.y = canvasoheight - rect.y - rect.h;
                        
                        var pixels = new Uint8Array(rect.w * rect.h * 4);
                        threectx.readPixels(rect.x, rect.y, rect.w, rect.h, threectx.RGBA, threectx.UNSIGNED_BYTE, pixels);

                        var imgpxls = new Uint8ClampedArray(pixels);
                        imagedata = new ImageData(imgpxls, rect.w, rect.h);

                        imagedata = flipImageData(imagedata);

                        //var tdv = new Uint8ClampedArray(bitmap.buffer);
                        //var imageData = new ImageData(tdv, bitmap.width, bitmap.height);

                        //imagedata = pixels;


                    }else if(thispage.usepdfjs){
                        
                        //use new foxitdoc getbitmap rect function here to get bitmap
                        thispage.getPDFcliprect(rect, callback);

                    }else{
                        imagedata = contexto.getImageData(rect.x, rect.y, rect.w, rect.h);
                    }

                    //getClipRect

                    return imagedata;
                    

                };

                this.screentoDocRect = function(rect){

                    var xdoc = (rect.x - thispage.getdx()) / thispage.getdscale();
                    var ydoc = (rect.y - thispage.getdy()) / thispage.getdscale();
                    var wdoc = (rect.w / thispage.getdscale());
                    var hdoc = (rect.h / thispage.getdscale());

                    return {x : xdoc, y : ydoc, w : wdoc, h : hdoc};
    
                };
                this.getcur3DView = function(){
                    var View3D = null;

                    if (thispage.usevector3DGLTF) {
                        if(thispage.Vector3DPageObj){
                            View3D = thispage.Vector3DPageObj.getcur3DView();
                            //cameraimage = thispage.Vector3DPageObj.getCameraimage(name);
                        }
                    }

                    return View3D;

                };
                this.setpagedim = function (height, width, x, y) {
                    var pgdim = thispage.getpagedim();
                    var scalew = width / pgdim.w;
                    var scaleh = height / pgdim.h;
                    if (scalew > 1) {
                        thispage.ZoomIn(scalew, true, true);
                    }
                    else {
                        scalew = 1 / scalew;
                        thispage.ZoomOut(scalew, true, true);
                    }
                    thispage.pan_position(x, y);
                    //console.log(scaleh, scalew);
                };
                this.getpagedim = function () {
                    var dpi = thispage.DPI;
                    if (thispage.usepdfjs) {
                        dpi = 72;
                    }
                    var pagerect = thispage.getpagerect();
                    var pgdim = {
                        x: pagerect.x,
                        y: pagerect.y,
                        w: pagerect.w,
                        h: pagerect.h,
                        origw: thispage.originalwidth,
                        origh: thispage.originalheight,
                        scale: thispage.getdscale(),
                        dpi: dpi,
                        rotation: thispage.getpageRotation()
                    };
                    return pgdim;
                };
                this.redraw = function () {
                    if (this.usevectorxml) {
                        this.draw_vector(true);
                    }
                    else if (this.usepdfjs) {

                        //thispage.forcefoxitRedraw();
                        thispage.DocRef.forcefoxitRedraw();

                        //thispage.pdfisrendered = false;
                        //console.log('redraw invalidation');
                        //thispage.pdfrenderopque += 1;

                        //forceRedraw
                    }
                    else {
                        this.draw_image(true);
                    }
                    redrawMarkup();
                };

                this.getPDFcliprect = function(rect, callback){

                    var pgscalefactor = thispage.getPDFpageScale();
                    //var scale = pgscalefactor; 

                    var scale = pgscalefactor;// * PixelToPoint;

                    rect.x = rect.x - (thispage.dxpdf);
                    rect.y = rect.y - (thispage.dypdf);

                    //rect.w = rect.w + thispage.dxpdf;
                    //rect.h = rect.h + thispage.dypdf;


                    //rect.x = rect.x * scale;
                    //rect.y = rect.y * scale;
                    //rect.w = rect.w * scale;
                    //rect.h = rect.h * scale;

                    //console.log(scale);


                    //rect.x = rect.x * PixelToPoint;
                    //rect.y = rect.y * PixelToPoint;
                    //rect.w = rect.w * PixelToPoint;
                    //rect.h = rect.h * PixelToPoint;


                    /*if (thispage.DocRef.foxitdoc != null) {
                        scale = thispage.DocRef.foxitdoc.getcurPageScale();
                    }*/

                    //var rect = {x : scope.x1, y : (scope.parent.h - scope.y1) - scope.height, w : scope.width, h : scope.height};

                    var pdfpagewidth = thispage.originalPDFwidth;
                    var pdfpageheight = thispage.originalPDFheight;

                    var nPDFMaxFactor = Math.sqrt(nMaximageArea / (pdfpagewidth * pdfpageheight));

                    if (scale > nPDFMaxFactor) {
                        var restscale = scale / nPDFMaxFactor;
                        scale = nPDFMaxFactor;
                        thispage.bMaxzoom = true;
                    }
                    else {
                        restscale = 1;
                        thispage.bMaxzoom = false;
                    }

                    
                    if (thispage.DocRef.foxitdoc != null) {
                        thispage.DocRef.foxitdoc.getBitmapRect(thispage.pagenumber, scale, rect, callback);
                    }

                };
                this.getfoxitbitmap = function (rect) {
                    //var pgscalefactor = (thispage.offscreenwidth / thispage.pdfpagewidth);


                    var pgscalefactor = thispage.getPDFpageScale();
                    var scale = pgscalefactor; 

                    if (thispage.DocRef.foxitdoc != null) {
                        scale = thispage.DocRef.foxitdoc.getcurPageScale();
                    }

                    //var rect = {x : scope.x1, y : (scope.parent.h - scope.y1) - scope.height, w : scope.width, h : scope.height};

                    var pdfpagewidth = thispage.originalPDFwidth;
                    var pdfpageheight = thispage.originalPDFheight;

                    var nPDFMaxFactor = Math.sqrt(nMaximageArea / (pdfpagewidth * pdfpageheight));

                    if (scale > scope.nPDFMaxFactor) {
                        var restscale = scale / nPDFMaxFactor;
                        scale = nPDFMaxFactor;
                        thispage.bMaxzoom = true;
                    }
                    else {
                        restscale = 1;
                        thispage.bMaxzoom = false;
                    }

                    
                    if (thispage.DocRef.foxitdoc != null) {
                        docref.foxitdoc.getBitmapRect(thispage.pagenumber, scale, rect, thispage.setPageBitmap);
                    }

                    


                    //var scalefactor = pgscalefactor * PixelToPoint;
                    //scalefactor *= scale;

                    
                    /*if (thispage.usedincomposite && thispage.compositereference != undefined) {
                        if (thispage.role == 1 || thispage.role == 2) {
                            if (thispage.DocRef.foxitdoc != null) {
    //                                if(thispage.role == 1){
        
      //                              var scaledoffscreen = thispage.offscreenwidth  / thispage.compositereference.scaleratio;
        
        //                            pgscalefactor = (scaledoffscreen / thispage.pdfpagewidth) * thispage.compositereference.scaleratio;
                                    
          //                          scale /= thispage.compositereference.scaleratio;
                                    //scale *= thispage.compositereference.scaleratio;
            //                    }
                                var bitmapscale = pgscalefactor * scale;

                                if(thispage.checkFoxitMaxZoom(scale)){
                                    thispage.DocRef.foxitdoc.getBitmap(thispage.pagenumber, bitmapscale, thispage.setPageBitmap);
                                }
                               
                            }
                        }
                    }*/
                };

                this.checkFoxitMaxZoom = function(scale){


                    var performZoom = false;

                    //var pgscalefactor = (thispage.offscreenwidth / thispage.pdfpagewidth);
                    var pgscalefactor = thispage.getPDFpageScale();
                    var scalefactor = pgscalefactor * PixelToPoint;

                    scalefactor *= scale;

                    if (thispage.DocRef.foxitdoc != null) {
                        //get max zoom factor
                        var maxscale = thispage.DocRef.foxitdoc.nMaxScale;

                        if(maxscale != undefined &&  scalefactor <= maxscale){
                            performZoom = true;
                        }

                    }

                    return performZoom;

                };

                this.foxitpanToPoint = function(point){

                    if (thispage.DocRef.foxitdoc != null) {
                        thispage.DocRef.foxitdoc.pantopoint(thispage.pagenumber,point);
                    }

                };

                this.foxitdirectZoomToEx = function(scale, point){

                    var centerx = canvasowidth * 0.5;
                    var centery = canvasoheight * 0.5;

                    /*var pgscalefactor = (thispage.offscreenwidth / thispage.pdfpagewidth);

                    if(thispage.getpageRotation() != 0){
                        pgscalefactor = (thispage.originalPDFwidth / thispage.pdfpagewidth);
                    }*/

                    var pgscalefactor = thispage.getPDFpageScale();

                    var scalefactor = pgscalefactor * PixelToPoint;
                    
                    scalefactor *= scale;

                    if (thispage.DocRef.foxitdoc != null) {
                        //get max zoom factor
                        var maxscale = thispage.DocRef.foxitdoc.nMaxScale;

                    }

                    if(maxscale != undefined && scalefactor >= maxscale){
                        //need to check if page pdf scale need to be reset to previous value.                        
                        return;
                    }

                    if (thispage.DocRef.foxitdoc != null) {

                        thispage.DocRef.foxitdoc.zoomtopointdirect(thispage.pagenumber, scalefactor, point);

                    }



                };

                this.foxitdirectZoomTo = function(scale, factor, point, offset, center, bIn){
                    
                    var pixelRatio = getPixelRatio();

                    var busezoom = true;

                    var centerx = canvasowidth * 0.5;
                    var centery = canvasoheight * 0.5;

                    var pgscalefactor = thispage.getPDFpageScale();

                    //var pgscalefactor = (thispage.offscreenwidth / thispage.pdfpagewidth);
                    
                    /*if(thispage.getpageRotation() != 0){

                        if(thispage.bpagesizeupdated){
                            pgscalefactor = (thispage.offscreenwidth / thispage.pdfpagewidth);
                        }else{
                            
                            pgscalefactor = (thispage.offscreenwidth / thispage.pdfpageheight);
                        }

                        //pgscalefactor = (thispage.originalPDFwidth / thispage.pdfpagewidth);
                    }*/

                    var scalefactor = pgscalefactor * PixelToPoint;
                    
                    scalefactor *= scale;

                    point.x /= pixelRatio;
                    point.y /= pixelRatio;


                    if (thispage.DocRef.foxitdoc != null) {
                        //get max zoom factor
                        var maxscale = thispage.DocRef.foxitdoc.nMaxScale;

                    }

                    if(maxscale != undefined &&  scalefactor >= maxscale){
                        //need to check if page pdf scale need to be reset to previous value.                        
                        return;
                    }


                    
                    //var xp = point.x - centerx;
                    //var yp = point.y - centery;

                    //point.x = point.x - centerx;
                    //point.y = point.y - centery;
                    //console.log(point);
                    //var pdfpoint = thispage.getPDFcoords(xp,yp);

                    //console.log(pdfpoint);

                    
                    /*if (thispage.usedincomposite && thispage.compositereference != undefined) {
                        if (thispage.role == 1 || thispage.role == 2) {
                            busezoom = false;
                        }
                    }*/
                    if (thispage.DocRef.foxitdoc != null && busezoom) {
                        //var scrolldata = { pagerect: thispage.getpagerect(), cw: canvasowidth, ch: canvasoheight, pagenum: thispage.pagenumber, visiblechanged: false };
                        thispage.DocRef.foxitdoc.zoomToPoint(thispage.pagenumber, scalefactor, factor, point, offset, center, bIn);
                        /*if(thispage.DocRef.foxitdoc.getRenderStatus()){
                            //thispage.DocRef.foxitdoc.forcefoxitRedraw();
                            thispage.DocRef.foxitdoc.forceRedraw();
                        }*/
                    }
                };
                this.getPDFpageScale = function(){

                    var pgscalefactor = (thispage.offscreenwidth / thispage.pdfpagewidth);



                    if(thispage.getpageRotation() != 0){


                        if(thispage.bpagesizeupdated){
                                
                            if(thispage.getpageRotation() == 90 || thispage.getpageRotation() == 270){
                                //pgscalefactor = (thispage.originalPDFwidth / thispage.pdfpageheight);
                                
                                pgscalefactor = (thispage.offscreenwidth / thispage.pdfpagewidth);
                            }else{
                                pgscalefactor = (thispage.offscreenwidth / thispage.pdfpagewidth);
                            }
                                
                        }else{
                            //pgscalefactor = (thispage.originalPDFwidth / thispage.pdfpagewidth);
                            //pgscalefactor = (thispage.offscreenwidth / thispage.pdfpagewidth);
                            //pgscalefactor = (thispage.offscreenwidth / thispage.pdfpageheight);
                            pgscalefactor = (thispage.offscreenwidth / thispage.pdfpagewidth);
                        }


                        /*switch (thispage.curzoomfunction){

                            case 1: //width
                                pgscalefactor = (thispage.originalPDFwidth / thispage.pdfpagewidth);
                                thispage.curzoomfunction = 0;
                                break; 
                            case 2: //height
                                pgscalefactor = (thispage.originalPDFheight / thispage.pdfpageheight);
                                thispage.curzoomfunction = 0;
                                break;
                            default:


                                if(thispage.bpagesizeupdated){
                                
                                    if(thispage.getpageRotation() == 90 || thispage.getpageRotation() == 270){
                                        pgscalefactor = (thispage.originalPDFwidth / thispage.pdfpageheight);
                                    }else{
                                        pgscalefactor = (thispage.originalPDFwidth / thispage.pdfpagewidth);    
                                    }
                                        
                                }else{
                                    pgscalefactor = (thispage.originalPDFwidth / thispage.pdfpagewidth);
                                }
                                

                                
                                thispage.curzoomfunction = 0;
                                break; 

                        }*/


                        
                        

                    }
                    return pgscalefactor;

                };
                this.foxitdirectZoom = function (scale) {
                    var busezoom = true;
                    var maxscale = undefined;
                    
                    var pgscalefactor = thispage.getPDFpageScale();
                    

                    /*if(thispage.getpageRotation() != 0){
                        pgscalefactor = (thispage.originalPDFwidth / thispage.pdfpagewidth);
                        pgscalefactor = (thispage.originalPDFheight / thispage.pdfpageheight);
                    }*/


                    var scalefactor = pgscalefactor * PixelToPoint;
                    scalefactor *= scale;

                    if (thispage.DocRef.foxitdoc != null) {
                        //get max zoom factor

                        var maxscale = thispage.DocRef.foxitdoc.nMaxScale;

                    }

                    if(maxscale != undefined &&  scalefactor >= maxscale){
                        //need to check if page pdf scale need to be reset to previous value.                        
                        return;
                    }

                    /*if (thispage.usedincomposite && thispage.compositereference != undefined) {
                        if (thispage.role == 1 || thispage.role == 2) {
                            busezoom = false;
                        }
                    }*/
                    if (thispage.DocRef.foxitdoc != null && busezoom) {
                        
                        var scrolldata = { pagerect: thispage.getpagerect(), cw: canvasowidth, ch: canvasoheight, pagenum: thispage.pagenumber, visiblechanged: false };

                        scrolldata = { pagerect: thispage.getpagerect(), cw: canvasowidthsize, ch: canvasoheightsize, pagenum: thispage.pagenumber, visiblechanged: false };

                        //thispage.DocRef.foxitdoc.zoomdirect(thispage.pagenumber, scalefactor, scrolldata);
                        return thispage.DocRef.foxitdoc.zoomdirect(thispage.pagenumber, scalefactor, scrolldata);
                    }else{
                        return Promise.resolve();
                    }

                    //from scrollbuffer
                    /*var scalefactor = scrollobj.ps * PixelToPoint;
                    
                    var scale = scrollobj.s;
                    scalefactor *= scale;*/
                    //from setpagestats
                    /*if(thispage.DocRef.foxitdoc != null){
        
                        var scalefactor = pgscalefactor * PixelToPoint;
                        scalefactor *= thispage.dscalepdf;
                        
                        var scrolldata = {pagerect  : thispage.getpagerect(), cw : canvasowidth, ch : canvasoheight, pagenum : thispage.pagenumber, visiblechanged : false};
                        thispage.DocRef.foxitdoc.zoomdirect(thispage.pagenumber, scalefactor, scrolldata);
        
                    }*/
                    //from zoom all
                    /*if(thispage.DocRef.foxitdoc != null && thispage.DocRef.pages[thispage.DocRef.currentpage].pagenumber == thispage.pagenumber){
                            
        
        
                        var pgscalefactor = (thispage.offscreenwidth / thispage.pdfpagewidth) * PixelToPoint;
                        var scalefactor =  pgscalefactor * thispage.dscalepdf;
                        
                        
                        var scrolldata = {pagerect  : thispage.getpagerect(), cw : canvasowidth, ch : canvasoheight,pagenum : thispage.pagenumber, visiblechanged :false};
                        thispage.DocRef.foxitdoc.zoomdirect(thispage.pagenumber, scalefactor, scrolldata);*/
                    //thispage.DocRef.foxitdoc.renderScale(thispage.pagenumber, thispage.dscalepdf);
                    /*if (thispage.usedincomposite && thispage.compositereference != undefined){
                        thispage.compositereference.pages[0].DocRef.foxitdoc.renderScale(thispage.compositereference.pages[0].pagenumber, thispage.dscalepdf);
                    }*/
                    //foxitrenderScale(thispage.pagenumber, thispage.dscalepdf);        
                    //}            
                };
                this.zoomall = function (bexternal) {
                    //SetImageDim(myimage);
                    if (RxCore_GUI_ZoomUpdate != undefined && bexternal == undefined) {
                        var pagerect = thispage.getpagerect();
                        var zoomparams = { rect: pagerect };
                        RxCore_GUI_ZoomUpdate.zoomUpdate(zoomparams, 3);
                    }
                    if (this.usevector3Dxml) {
                        return;
                    }

                    if (this.usepdfjs) {

                        if (thispage.offscreenheight == 0 || thispage.offscreenwidth == 0 || !thispage.pdfdiminitset) {
                            return;
                        }
                        var pcanvheight = thispage.offscreenheight;
                        var pcanvwidth = thispage.offscreenwidth;

                        /*if (thispage.usedincomposite && thispage.compositereference != undefined) {
                            //var pcanvheight = thispage.compositereference.ovpagecanvas.height;
                            var pcanvheight = thispage.compositereference.overlaypage.getheight();
                            var pcanvwidth = thispage.compositereference.overlaypage.getwidth();
                            //var pcanvwidth = thispage.compositereference.ovpagecanvas.width;
                        }
                        else {

                        }*/
                    }
                    else {
                        var stats = getPagestats();
                        if(stats != undefined){
                            var scaleoffset = ScaleZoomAll(stats);
                            setPagestats(scaleoffset, false);
    
                        }
                    }
                    if (this.usepdfjs) {
                        var yscale = this.dscalepdf;
                        var xscale = this.dscalepdf;
                        //yscale = this.dypdf;
                        //xscale = this.dxpdf;
                        switch (this.getpageRotation()) {
                            case 0:
                                //should result in 1 if zoom all already called
                                yscale = canvasoheight / pcanvheight;
                                xscale = canvasowidth / pcanvwidth;

                                yscale = canvasoheightsize / pcanvheight;
                                xscale = canvasowidthsize / pcanvwidth;

                                //dscalepdf modified
                                this.dscalepdf = Math.min(xscale, yscale);
                                this.curzoomfunction = 0;


                                break;
                            case 90:


                                if(thispage.bpagesizeupdated){

                                    yscale = canvasoheight / pcanvheight;
                                    xscale = canvasowidth / pcanvwidth;

                                    yscale = canvasoheightsize / pcanvheight;
                                    xscale = canvasowidthsize / pcanvwidth;
    
                                    //xscale = canvwidth / pcanvwidth;

                                }else{
                                    //xscale = canvwidth / pcanvheight;
                                    yscale = canvasoheight / pcanvwidth;
                                    xscale = canvasowidth / pcanvheight;

                                    yscale = canvasoheightsize / pcanvwidth;
                                    xscale = canvasowidthsize / pcanvheight;
    

                                }

                                //dscalepdf modified
                                this.dscalepdf = Math.min(xscale, yscale);



                                if(this.dscalepdf == xscale){
                                    thispage.curzoomfunction = 1;
                                }else if(this.dscalepdf == yscale){
                                    thispage.curzoomfunction = 2;
                                }
                                

                                break;
                            case 180:
                                yscale = canvasoheight / pcanvheight;
                                xscale = canvasowidth / pcanvwidth;

                                yscale = canvasoheightsize / pcanvheight;
                                xscale = canvasowidthsize / pcanvwidth;

                                //dscalepdf modified
                                this.dscalepdf = Math.min(xscale, yscale);

                                this.curzoomfunction = 0;

                                break;
                            case 270:

                                if(thispage.bpagesizeupdated){

                                    yscale = canvasoheight / pcanvheight;
                                    xscale = canvasowidth / pcanvwidth;

                                    yscale = canvasoheightsize / pcanvheight;
                                    xscale = canvasowidthsize / pcanvwidth;
    
                                    //xscale = canvwidth / pcanvwidth;

                                }else{
                                    //xscale = canvwidth / pcanvheight;
                                    yscale = canvasoheight / pcanvwidth;
                                    xscale = canvasowidth / pcanvheight;

                                    yscale = canvasoheightsize / pcanvwidth;
                                    xscale = canvasowidthsize / pcanvheight;

                                }

                                //yscale = canvasoheight / pcanvwidth;
                                //xscale = canvasowidth / pcanvheight;
                                
                                //dscalepdf modified
                                this.dscalepdf = Math.min(xscale, yscale);

                                if(this.dscalepdf == xscale){
                                    thispage.curzoomfunction = 1;
                                }else if(this.dscalepdf == yscale){
                                    thispage.curzoomfunction = 2;
                                }


                                break;
                        }
                        thispage.pdfisrendered = false;
                        //console.log('zoomall invalidation');
                        thispage.pdfrenderopque += 1;
                        if (thispage.DocRef.pages[thispage.DocRef.currentpage].pagenumber == thispage.pagenumber) {
                            thispage.foxitdirectZoom(thispage.dscalepdf);
                        }
                        this.dxpdf = (canvasowidthsize - (pcanvwidth * this.dscalepdf)) / 2;
                        this.dypdf = (canvasoheightsize - (pcanvheight * this.dscalepdf)) / 2;


                        //yscale = canvasoheightsize / pcanvheight;
                        //xscale = canvasowidthsize / pcanvwidth;


                    }
                    //this.currentimage = 1;

                    if (this.usepdfjs) {
                        //DocObj.draw_mpagepdf();
                        thispage.setpagedimsize(this.dxpdf, this.dypdf, (pcanvwidth * this.dscalepdf), (pcanvheight * this.dscalepdf), false);
                        if (thispage.pagenumber == thispage.DocRef.pages[thispage.DocRef.currentpage].pagenumber) {

                            //not to be used with foxit
                            //thispage.DocRef.zoomall();
                            thispage.DocRef.draw_mpagepdf(false);

                            /*if (thispage.usedincomposite && thispage.compositereference != undefined) {
                                //commented out 28.04
                                if (thispage.role == 1) {
                                    thispage.dscalepdf = thispage.compositereference.getoverlayScaleFactor(3);
                                    thispage.compositereference.scaleToBackground(true);
                                }
                                thispage.getfoxitbitmap(thispage.dscalepdf);
                            }
                            else {
                            }*/
                        }
                    }
                    redrawMarkup();
                    thispage.postscaleRefresh();

                    if (RxCore_GUI_ZoomUpdated != undefined && bexternal == undefined) {
                        var pagerect = thispage.getpagerect();
                        var zoomparams = { rect: pagerect };
                        RxCore_GUI_ZoomUpdated.zoomUpdate(zoomparams, 3);
                    }
                };
                this.getPageScale = function () {
                    return thispage.curpagescale;
                };
                this.ZoomPageUpdate = function (zoomparams, type) {
                    switch (type) {
                        case 0:
                            thispage.ZoomIn(zoomparams.factor, zoomparams.center, true, zoomparams.mousepos);
                            break;
                        case 1:
                            thispage.ZoomOut(zoomparams.factor, zoomparams.center, true, zoomparams.mousepos);
                            break;
                        case 2:
                            thispage.zoom_update(zoomparams.sx, zoomparams.sy, zoomparams.sWide, zoomparams.sHi, true);
                            //zoom window
                            break;
                        case 3:
                            thispage.zoomall(true);
                            //console.log('zoom all');
                            break;
                        case 4:
                            thispage.zoomwidth(true);
                            //zoom width
                            break;
                        case 5:
                            thispage.zoomheight(true);
                            break;
                    }
                };
                function foxitredrawmarkup() {
                    thispage.DocRef.draw_mpagepdf(false);


                    drawmarkupAll(cntximg);
                    DrawMarkupSelected(context);

                    /*if(tool.name == 'area' && tool.started){
                        tool.refreshDrawFoxit();
                    }*/


                    if(thispage.DocRef.foxitsearchobj.active){
                        thispage.DocRef.drawsearchRect();
                    }

                    /*if(tool.refreshSupport){
                        tool.refreshDraw();
                    }*/


                }
                /*this.foxitredrawmarkup = function(){
                    //adjust markup position to foxit page.
        
                    drawmarkupAll(cntximg);
                    DrawMarkupSelected(context);
        
                };*/
                function redrawMarkupPan() {

                    if(thispage.DocRef.thumbnails[thispage.pagenumber]){
                        


                        if(thispage.usepdfjs){
                            thispage.DocRef.thumbnails[thispage.pagenumber].birdseyerect(thispage.getpagerectdirect());
                        }else{
                            thispage.DocRef.thumbnails[thispage.pagenumber].birdseyerect(thispage.getpagerect());
                        }
                        
                        

                    }

                    if (thispage.DocRef.foxitdoc != null) {
                        //cntximg.clearRect(0, 0, canvasowidth, canvasoheight);
                        //get page position from foxit.
                        //adjust markup to foxit page pos.
                        //thispage.DocRef.foxitdoc.
                    }
                    else {
                        drawmarkupAll(cntximg);
                        DrawMarkupSelected(context);
                    }
                }
                function redrawMarkup() {

                    if(thispage.DocRef.thumbnails[thispage.pagenumber]){
                        //thispage.DocRef.thumbnails[thispage.pagenumber].birdseyerect(thispage.getpagerect());

                        if(thispage.usepdfjs){
                            thispage.DocRef.thumbnails[thispage.pagenumber].birdseyerect(thispage.getpagerectdirect());
                        }else{
                            thispage.DocRef.thumbnails[thispage.pagenumber].birdseyerect(thispage.getpagerect());
                        }


                    }
                    
                    //get page dim from foxit here.

                    /*if (thispage.DocRef.foxitdoc != null) {
                        var pagepos = thispage.DocRef.foxitdoc.getPagePos(thispage.pagenumber);
                        thispage.foxitonScrollupdate(pagepos);
                    }*/

                    if (thispage.DocRef.foxitdoc != null) {
                        cntximg.clearRect(0, 0, canvasowidth, canvasoheight);


                        if(thispage.DocRef.foxitsearchobj.active){
                            thispage.DocRef.drawsearchRect();
                        }
                        

                        //get page position from foxit.
                        //adjust markup to foxit page pos.
                        //thispage.DocRef.foxitdoc.
                    }
                    else {
                        drawmarkupAll(cntximg);
                        DrawMarkupSelected(context);

                        //thispage.DocRef.thumbnails[thispage.pagenumber].birdseyerect(thispage.getpagerect());
                    }
                }
                function setPagestats(stats, bscroll) {
                    if (thispage.usevectorxml) {

                        thispage.dxvector = stats.dx;
                        thispage.dyvector = stats.dy;
                        thispage.dscalevector = stats.dscale;

                        thispage.startx = stats.startx;
                        thispage.starty = stats.starty;
                        thispage.endx = stats.endx;
                        thispage.endy = stats.endy;

                        thispage.draw_vector(false);
                        /*if (thispage.usedincomposite && thispage.compositereference != undefined) {
                            thispage.compositereference.scaleToBackground(true);
                        }
                        else {
                            
                        }*/
                        if (bscroll) {
                            thispage.vectorscrollbuffer(thispage.dscalevector, thispage.dxvector, thispage.dyvector);
                        }
                        else {
                            thispage.vectorischanged = true;
                        }

                        if(thispage.DocRef.textselect){
                            thispage.cleartextdivs();
                            thispage.settextdivs();
                        }


                    }
                    else if (thispage.usepdfjs) {

                        
                        if(thispage.checkFoxitMaxZoom(stats.dscale)){
                            thispage.dxpdf = stats.dx;
                            thispage.dypdf = stats.dy;

                            //dscalepdf modified
                            thispage.dscalepdf = stats.dscale;
                            thispage.startx = stats.startx;
                            thispage.starty = stats.starty;
                            //thispage.endx = stats.endx;
                            //thispage.endy = stats.endy;
                        }
                        

                        if(debugmarkuprect){
                            console.log(thispage.startx, thispage.starty, thispage.endx, thispage.endy);
                        }
    
                        
                        thispage.pdfrenderopque += 1;
                        thispage.pdfisrendered = false;
                        //var pgscalefactor = (thispage.offscreenwidth / thispage.pdfpagewidth);
                        if (bscroll) {
                            
                            if(stats.mousepoint != undefined){
                                thispage.foxitdirectZoomTo(thispage.dscalepdf, stats.dfactor, stats.mousepoint, stats.offsetpoint, stats.offsetcenter, stats.bIn);

                            //mousepoint : stats.mousepoint,
                            //bIn : bIn,
                            //dfactor : factor 


                                //thispage.foxitdirectZoomTo(thispage.dscalepdf, factor, pdfpoint, false);    
                            }else{
                                //thispage.foxitdirectZoom(thispage.dscalepdf);

            
                                var zoomtopoint = stats.offsetcenter;
                                //pdfscaleoffset.offsetcenter = pdfcenter;

                                var centerx = canvasowidth * 0.5;
                                var centery = canvasoheight * 0.5;
            
        
                                //var zoomtopoint = thispage.getPDFCoordsRot(pdfcenternorotate);

                                var realangle = getcombinedangle(thispage.getpageRotation(), thispage.foxitpageorigrot);

                                var distx = thispage.getPDFDistScale(centerx, thispage.dscalepdf);
                                var disty = thispage.getPDFDistScale(centery, thispage.dscalepdf);
        
        
                                switch (realangle){
                                    case 0 :
                                        zoomtopoint.x = zoomtopoint.x - distx;
                                        zoomtopoint.y = zoomtopoint.y + disty;
                                        break;
                                    case 90 :
                                        zoomtopoint.x = zoomtopoint.x - disty;
                                        zoomtopoint.y = zoomtopoint.y - distx;
        
                                        break;
                                    case 180 :
                                        zoomtopoint.x = zoomtopoint.x + distx;
                                        zoomtopoint.y = zoomtopoint.y - disty;
        
                                        break;
                                    case 270 :
        
                                        zoomtopoint.x = zoomtopoint.x + disty;
                                        zoomtopoint.y = zoomtopoint.y + distx;
        
                                    break;
        
                                }                                

                                thispage.foxitdirectZoomToEx(thispage.dscalepdf, zoomtopoint);  
                            }
                            
                            //thispage.foxitdirectZoom(thispage.dscalepdf);
                            //thispage.scrollzoombuffer(thispage.dscalepdf,pgscalefactor, thispage.dxpdf, thispage.dypdf);
                        }
                        if (thispage.pagenumber == thispage.DocRef.pages[thispage.DocRef.currentpage].pagenumber) {
                            
                            thispage.DocRef.draw_mpagepdf(false);
                            /*if (thispage.usedincomposite && thispage.compositereference != undefined) {
                                if (thispage.role == 1) {
                                    thispage.dscalepdf = thispage.compositereference.getoverlayScaleFactor(3);
                                    thispage.compositereference.scaleToBackground(true);
                                }
                                thispage.getfoxitbitmap(thispage.dscalepdf);
                                //thispage.DocRef.foxitdoc.getBitmap(thispage.pagenumber, thispage.dscalepdf, thispage.setPageBitmap);
                                //thispage.compositereference.scaleToBackground(true);
                            }
                            else {
                                
                            }*/
                        }
                        /*stats = {
                            dscale : thispage.dscalepdf,
                            dx : thispage.dxpdf,
                            dy : thispage.dypdf,
                            width : thispage.pdfpagewidth,
                            height : thispage.pdfpageheight
                        };*/
                    }
                    else {
                        thispage.dx = stats.dx;
                        thispage.dy = stats.dy;
                        thispage.dscale = stats.dscale;
                        thispage.startx = stats.startx;
                        thispage.starty = stats.starty;
                        thispage.endx = stats.endx;
                        thispage.endy = stats.endy;
                        thispage.checkimageswitch();
                        if (thispage.DocRef.Type == 0) {
                            thispage.DocRef.draw_mpage();
                        }
                        else {
                            thispage.draw_image(true);
                            /*if (thispage.usedincomposite && thispage.compositereference != undefined) {
                                thispage.compositereference.scaleToBackground(true);
                            }
                            else {
                                
                            }*/
                        }
                    }
                }
                function getPagestats() {
                    if (thispage.usevectorxml) {

                        if(thispage.VectorPageObj == undefined){
                            return;
                        }

                        var stats = {
                            dscale: thispage.dscalevector,
                            dx: thispage.dxvector,
                            dy: thispage.dyvector,
                            width: thispage.VectorPageObj.width,
                            height: thispage.VectorPageObj.height
                        };
                    }
                    else if (thispage.usepdfjs) {
                        stats = {
                            dscale: thispage.dscalepdf,
                            dx: thispage.dxpdf,
                            dy: thispage.dypdf,
                            width: thispage.pdfpagewidth,
                            height: thispage.pdfpageheight,
                            mousepoint : {x: 0, y: 0},
                            offsetpoint : {x: 0, y: 0},
                            offsetcenter : {x: 0, y: 0},
                            bIn : false,
                            dfactor : 1 
                        };
                    }
                    else {
                        stats = {
                            dscale: thispage.dscale,
                            dx: thispage.dx,
                            dy: thispage.dy,
                            width: thispage.MainImageWidth,
                            height: thispage.MainImageHeight
                        };
                    }
                    return stats;
                }
                function ScaleZoomInPDF(center, factor, stats, bIn) {

                    var pcanvheight = thispage.offscreenheight;
                    var pcanvwidth = thispage.offscreenwidth;

                    /*if (thispage.usedincomposite && thispage.compositereference != undefined) {
                        //var pcanvheight = thispage.compositereference.ovpagecanvas.height;
                        //var pcanvwidth = thispage.compositereference.ovpagecanvas.width;
                        var pcanvheight = thispage.compositereference.overlaypage.getheight();
                        var pcanvwidth = thispage.compositereference.overlaypage.getwidth();
                    }
                    else {

                        if(thispage.getpageRotation() != 0){

                            pcanvwidth = thispage.originalPDFwidth;
                            pcanvheight = thispage.originalPDFheight;


                        }

                    }*/
                    var bScaleChanged = false;
                    thispage.bMinzoom = false;
                    var bWidthlocked = false;
                    var prevcenterxpdf = (canvasowidth - (pcanvwidth * stats.dscale)) / 2;
                    var prevcenterypdf = (canvasoheight - (pcanvheight * stats.dscale)) / 2;

                    prevcenterxpdf = (canvasowidthsize - (pcanvwidth * stats.dscale)) / 2;
                    prevcenterypdf = (canvasoheightsize - (pcanvheight * stats.dscale)) / 2;


                    var diffxpdf = prevcenterxpdf - stats.dx;
                    var diffypdf = prevcenterypdf - stats.dy;

                    var wscale = ((pcanvwidth * stats.dscale) / thispage.pdfpagewidth) * factor;
                    var hscale = ((pcanvheight * stats.dscale) / thispage.pdfpageheight) * factor;


                    if(thispage.getpageRotation() != 0){

                        if(thispage.getpageRotation() == 90 || thispage.getpageRotation() == 270){

                            if(thispage.bpagesizeupdated){
                                wscale = ((pcanvwidth * stats.dscale) / thispage.pdfpagewidth) * factor;
                                hscale = ((pcanvheight * stats.dscale) / thispage.pdfpageheight) * factor;
            
                            }else{
                                wscale = ((pcanvwidth * stats.dscale) / thispage.pdfpageheight) * factor;
                                hscale = ((pcanvheight * stats.dscale) / thispage.pdfpagewidth) * factor;
    
                            }
    

                        }

                        

                    }
                    var temppdfscale = Math.min(wscale, hscale);
                    temppdfscale *= factor;
                    stats.dscale = stats.dscale * factor;
                    bScaleChanged = true;

                    var centerxpdf = ((canvasowidth - (pcanvwidth * stats.dscale)) / 2);
                    var centerypdf = ((canvasoheight - (pcanvheight * stats.dscale)) / 2);

                    centerxpdf = ((canvasowidthsize - (pcanvwidth * stats.dscale)) / 2);
                    centerypdf = ((canvasoheightsize - (pcanvheight * stats.dscale)) / 2);


                    if(thispage.getpageRotation() != 0){
                    
                        if(thispage.bpagesizeupdated){
                            if(thispage.getpageRotation() == 90 || thispage.getpageRotation() == 270){
                                centerxpdf = ((canvasoheight - (pcanvwidth * stats.dscale)) / 2);
                                centerypdf = ((canvasowidth - (pcanvheight * stats.dscale)) / 2);

                                centerxpdf = ((canvasoheightsize - (pcanvwidth * stats.dscale)) / 2);
                                centerypdf = ((canvasowidthsize - (pcanvheight * stats.dscale)) / 2);
            
                            }
                        }

                    }


                    
                    if (center) {
                        stats.dx = centerxpdf;
                        stats.dy = centerypdf;
                    }
                    else {
                        if (bScaleChanged) {
                            stats.dx = centerxpdf - (diffxpdf * factor);
                            stats.dy = centerypdf - (diffypdf * factor);
                        }
                        else {
                            stats.dx = centerxpdf - diffxpdf;
                            stats.dy = centerypdf - diffypdf;
                        }
                    }
                    /*if (pcanvwidth <= canvasowidth) {
                        if (!thispage.DocRef.pagelocked) {
                            stats.dx = centerxpdf;
                        }
                        bWidthlocked = true;
                    }*/

                    if (pcanvwidth <= canvasowidthsize) {
                        if (!thispage.DocRef.pagelocked) {
                            stats.dx = centerxpdf;
                        }
                        bWidthlocked = true;

                    }   

                    /*if (pcanvwidth * stats.dscale <= canvasowidth) {
                        if (!thispage.DocRef.pagelocked) {
                            stats.dx = (canvasowidth - (pcanvwidth * stats.dscale)) / 2;
                        }
                        bWidthlocked = true;
                    }*/

                    if (pcanvwidth * stats.dscale <= canvasowidthsize) {
                        if (!thispage.DocRef.pagelocked) {
                            stats.dx = (canvasowidthsize - (pcanvwidth * stats.dscale)) / 2;
                        }
                        bWidthlocked = true;
                    }

                    
                    thispage.setpagedimsize(stats.dx, stats.dy, (pcanvwidth * stats.dscale), (pcanvheight * stats.dscale), false);
                    
                    //DocObj.draw_mpagepdf();
                    //thispage.renderPDFpagescale();
                    var startx = stats.dx;
                    var starty = stats.dy;
                    var endx = (stats.width * stats.dscale) + startx;
                    var endy = (stats.height * stats.dscale) + starty;
                    return {
                        dscale: stats.dscale,
                        dx: stats.dx,
                        dy: stats.dy,
                        startx: startx,
                        starty: starty,
                        endx: endx,
                        endy: endy,
                        bWidthlocked: bWidthlocked,
                        mousepoint : stats.mousepoint,
                        offsetpoint : stats.offsetpoint,
                        offsetcenter : stats.offsetcenter,
                        bIn : bIn,
                        dfactor : factor 
                    };
                }
                function ScaleZoomUpdate(stats, sx, sy, sWide, sHi) {
                    var newscale = 1.0;
                    var factor = 0.0;
                    var pointXtoCenter = 0;
                    var pointYtoCenter = 0;
                    var canvdiv2H = canvasoheight / 2;
                    var canvdiv2W = canvasowidth / 2;
                    var CanvRotRad = thispage.getpageRotation() * (Math.PI / 180);
                    //point to align to centre in display coordinates.
                    var rectCenterX = sx + (sWide / 2);
                    var rectCenterY = sy + (sHi / 2);
                    var ox1 = sx / stats.dscale;
                    var oy1 = sy / stats.dscale;
                    var ox2 = (sx + sWide) / stats.dscale;
                    var oy2 = (sy + sHi) / stats.dscale;
                    var hscale = sHi / stats.dscale;
                    var wscale = sWide / stats.dscale;
                    var yscale = canvasoheight / stats.height;
                    var xscale = canvasowidth / stats.width;
                    var imagescaleheight = stats.height * stats.dscale;
                    switch (thispage.getpageRotation()) {
                        case 90:
                            pointXtoCenter = rectCenterX - canvdiv2W;
                            pointYtoCenter = rectCenterY - canvdiv2H;
                            oy1 = (stats.dy + pointXtoCenter);
                            ox1 = (stats.dx - pointYtoCenter);
                            stats.dx = ox1;
                            stats.dy = oy1;
                            if (hscale > 0 && wscale > 0) {
                                yscale = stats.dscale * canvasoheight / sHi;
                                xscale = stats.dscale * canvasowidth / sWide;
                            }
                            newscale = Math.min(yscale, xscale);
                            factor = newscale / stats.dscale;
                            var tempstats = {
                                dscale: stats.dscale,
                                dx: stats.dx,
                                dy: stats.dy,
                                startx: stats.dx,
                                starty: stats.dy,
                                endx: (stats.width * stats.dscale) + stats.dx,
                                endy: (stats.height * stats.dscale) + stats.dy
                            };
                            setPagestats(tempstats, false);
                            thispage.ZoomIn(factor, false, false);
                            break;
                        case 270:
                            pointXtoCenter = rectCenterX - canvdiv2W;
                            pointYtoCenter = rectCenterY - canvdiv2H;
                            oy1 = (stats.dy - pointXtoCenter);
                            ox1 = (stats.dx + pointYtoCenter);
                            stats.dx = ox1;
                            stats.dy = oy1;
                            if (hscale > 0 && wscale > 0) {
                                yscale = stats.dscale * canvasoheight / sHi;
                                xscale = stats.dscale * canvasowidth / sWide;
                            }
                            newscale = Math.min(yscale, xscale);
                            factor = newscale / stats.dscale;
                            tempstats = {
                                dscale: stats.dscale,
                                dx: stats.dx,
                                dy: stats.dy,
                                startx: stats.dx,
                                starty: stats.dy,
                                endx: (stats.width * stats.dscale) + stats.dx,
                                endy: (stats.height * stats.dscale) + stats.dy
                            };
                            setPagestats(tempstats, false);
                            thispage.ZoomIn(factor, false, false);
                            break;
                        case 180:
                            //move point to center
                            pointXtoCenter = rectCenterX - canvdiv2W;
                            pointYtoCenter = rectCenterY - canvdiv2H;
                            oy1 = (stats.dy + pointYtoCenter);
                            ox1 = (stats.dx + pointXtoCenter);
                            stats.dx = ox1;
                            stats.dy = oy1;
                            if (hscale > 0 && wscale > 0) {
                                yscale = stats.dscale * canvasoheight / sHi;
                                xscale = stats.dscale * canvasowidth / sWide;
                            }
                            newscale = Math.min(yscale, xscale);
                            factor = newscale / stats.dscale;
                            tempstats = {
                                dscale: stats.dscale,
                                dx: stats.dx,
                                dy: stats.dy,
                                startx: stats.dx,
                                starty: stats.dy,
                                endx: (stats.width * stats.dscale) + stats.dx,
                                endy: (stats.height * stats.dscale) + stats.dy
                            };
                            setPagestats(tempstats, false);
                            thispage.ZoomIn(factor, false, false);
                            break;
                        case 0:
                            //offset in current scale
                            ox1 = -((rectCenterX - stats.dx) / stats.dscale);
                            oy1 = -((rectCenterY - stats.dy) / stats.dscale);
                            if (hscale > 0 && wscale > 0) {
                                yscale = stats.dscale * canvasoheight / sHi;
                                xscale = stats.dscale * canvasowidth / sWide;
                            }
                            stats.dscale = Math.min(yscale, xscale);
                            stats.dx = (ox1 * stats.dscale) + (canvasowidth / 2);
                            stats.dy = (oy1 * stats.dscale) + (canvasoheight / 2);
                            break;
                    }
                    var startx = stats.dx;
                    var starty = stats.dy;
                    var endx = (stats.width * stats.dscale) + startx;
                    var endy = (stats.height * stats.dscale) + starty;
                    return {
                        dscale: stats.dscale,
                        dx: stats.dx,
                        dy: stats.dy,
                        startx: startx,
                        starty: starty,
                        endx: endx,
                        endy: endy
                    };
                }
                function ScaleZoomHeight(stats) {
                    var yscale = 0.0;
                    switch (thispage.getpageRotation()) {
                        case 0:
                            yscale = canvasoheight / stats.height;
                            stats.dscale = yscale;
                            //xscale = canvasowidth / stats.width;
                            stats.dx = (canvasowidth - (stats.width * stats.dscale)) / 2;
                            stats.dy = (canvasoheight - (stats.height * stats.dscale)) / 2;
                            break;
                        case 90:
                            yscale = canvasoheight / stats.width;
                            stats.dscale = yscale;
                            stats.dx = (canvasowidth - (stats.width * stats.dscale)) / 2;
                            stats.dy = (canvasoheight - (stats.height * stats.dscale)) / 2;
                            break;
                        case 180:
                            yscale = canvasoheight / stats.height;
                            stats.dscale = yscale;
                            stats.dx = (canvasowidth - (stats.width * stats.dscale)) / 2;
                            stats.dy = (canvasoheight - (stats.height * stats.dscale)) / 2;
                            break;
                        case 270:
                            yscale = canvasoheight / stats.width;
                            stats.dscale = yscale;
                            stats.dx = (canvasowidth - (stats.width * stats.dscale)) / 2;
                            stats.dy = (canvasoheight - (stats.height * stats.dscale)) / 2;
                            break;
                    }
                    var startx = stats.dx;
                    var starty = stats.dy;
                    var endx = (stats.width * stats.dscale) + startx;
                    var endy = (stats.height * stats.dscale) + starty;
                    return {
                        dscale: stats.dscale,
                        dx: stats.dx,
                        dy: stats.dy,
                        startx: startx,
                        starty: starty,
                        endx: endx,
                        endy: endy
                    };
                }
                function ScaleZoomAll(stats) {
                    var yscale = stats.dscale;
                    var xscale = stats.dscale;
                    switch (thispage.getpageRotation()) {
                        case 0:
                            yscale = canvasoheight / stats.height;
                            xscale = canvasowidth / stats.width;
                            break;
                        case 90:
                            yscale = canvasoheight / stats.width;
                            xscale = canvasowidth / stats.height;
                            break;
                        case 180:
                            yscale = canvasoheight / stats.height;
                            xscale = canvasowidth / stats.width;
                            break;
                        case 270:
                            yscale = canvasoheight / stats.width;
                            xscale = canvasowidth / stats.height;
                            break;
                    }
                    stats.dscale = Math.min(xscale, yscale);
                    stats.dx = (canvasowidth - (stats.width * stats.dscale)) / 2;
                    stats.dy = (canvasoheight - (stats.height * stats.dscale)) / 2;
                    var startx = stats.dx;
                    var starty = stats.dy;
                    var endx = (stats.width * stats.dscale) + startx;
                    var endy = (stats.height * stats.dscale) + starty;
                    return {
                        dscale: stats.dscale,
                        dx: stats.dx,
                        dy: stats.dy,
                        startx: startx,
                        starty: starty,
                        endx: endx,
                        endy: endy
                    };
                }
                function ScaleZoomWidth(stats) {
                    var xscale = 0.0;
                    switch (thispage.getpageRotation()) {
                        case 0:
                            //xscale = canvasowidth / this.MainImageWidth;
                            xscale = canvasowidth / stats.width;
                            stats.dscale = xscale;
                            stats.dx = (canvasowidth - (stats.width * stats.dscale)) / 2;
                            stats.dy = (canvasoheight - (stats.height * stats.dscale)) / 2;
                            break;
                        case 90:
                            xscale = canvasowidth / stats.height;
                            stats.dscale = xscale;
                            stats.dx = (canvasowidth - (stats.width * stats.dscale)) / 2;
                            stats.dy = (canvasoheight - (stats.height * stats.dscale)) / 2;
                            break;
                        case 180:
                            xscale = canvasowidth / stats.width;
                            stats.dscale = xscale;
                            stats.dx = (canvasowidth - (stats.width * stats.dscale)) / 2;
                            stats.dy = (canvasoheight - (stats.height * stats.dscale)) / 2;
                            break;
                        case 270:
                            xscale = canvasowidth / stats.height;
                            stats.dscale = xscale;
                            stats.dx = (canvasowidth - (stats.width * stats.dscale)) / 2;
                            stats.dy = (canvasoheight - (stats.height * stats.dscale)) / 2;
                            break;
                        default:
                            xscale = canvasowidth / stats.width;
                            stats.dscale = xscale;
                            stats.dx = (canvasowidth - (stats.width * stats.dscale)) / 2;
                            stats.dy = (canvasoheight - (stats.height * stats.dscale)) / 2;
                            break;
                    }
                    var startx = stats.dx;
                    var starty = stats.dy;
                    var endx = (stats.width * stats.dscale) + startx;
                    var endy = (stats.height * stats.dscale) + starty;
                    return {
                        dscale: stats.dscale,
                        dx: stats.dx,
                        dy: stats.dy,
                        startx: startx,
                        starty: starty,
                        endx: endx,
                        endy: endy
                    };
                }
                function ScaleZoom(center, factor, stats, bIn) {
                    //var dscale = stats.dscale;
                    //var dx = stats.dx;
                    //var dy = stats.dy;
                    /*var imagestats = {
                        dscale : this.dscale,
                        dx : this.dx,
                        dy : this.dy,
                        width : this.MainImageWidth,
                        height : this.MainImageHeight
                    }; */
                    var prevcenterx = (canvasowidth - (stats.width * stats.dscale)) / 2;
                    var prevcentery = (canvasoheight - (stats.height * stats.dscale)) / 2;
                    var diffx = prevcenterx - stats.dx;
                    var diffy = prevcentery - stats.dy;
                    if (bIn) {
                        stats.dscale = stats.dscale * factor;
                    }
                    else {
                        stats.dscale = stats.dscale / factor;
                    }
                    var centerx = ((canvasowidth - (stats.width * stats.dscale)) / 2);
                    var centery = ((canvasoheight - (stats.height * stats.dscale)) / 2);
                    if (center) {
                        stats.dx = centerx;
                        stats.dy = centery;
                    }
                    else {
                        if (bIn) {
                            stats.dx = centerx - (diffx * factor);
                            stats.dy = centery - (diffy * factor);
                        }
                        else {
                            stats.dx = centerx - (diffx / factor);
                            stats.dy = centery - (diffy / factor);
                        }
                    }
                    var startx = stats.dx;
                    var starty = stats.dy;
                    var endx = (stats.width * stats.dscale) + startx;
                    var endy = (stats.height * stats.dscale) + starty;
                    thispage.setpagedimsize(startx, starty, endx, endy, true);
                    return {
                        dscale: stats.dscale,
                        dx: stats.dx,
                        dy: stats.dy,
                        startx: startx,
                        starty: starty,
                        endx: endx,
                        endy: endy
                    };
                }

                function redrawMarkup() {
                    drawmarkupAll(cntximg);
                    DrawMarkupSelected(context);


                    //get page rect and update birdseye

                    var thumbnail = thispage.DocRef.getThumb(thispage.pagenumber);

                    if(thumbnail == undefined){
                        return;
                    }

                    if(thispage.usepdfjs){
                        
                        thumbnail.birdseyerect(thispage.getpagerectdirect());
                    }else{
                        //thispage.DocRef.thumbnails[thispage.pagenumber].birdseyerect(thispage.getpagerect());
                        thumbnail.birdseyerect(thispage.getpagerect());    
                    }


                    //thispage.DocRef.thumbnails[thispage.pagenumber].birdseyerect(thispage.getpagerectdirect());

                }

                //thispage.scrollzoombuffer(thispage.dscalepdf,pgscalefactor, thispage.dxpdf, thispage.dypdf);
                this.vectorscrollbuffer = function (scale, x, y) {
                    var scrollobj = { s: scale, x: x, y: y };
                    if (thispage.vectordebounce_timer) {
                        //console.log('scrollzoom', 'clear');
                        window.clearTimeout(thispage.vectordebounce_timer);
                    }
                    thispage.vectordebounce_timer = window.setTimeout(function () {
                        // run your actual function here
                        if (scrollobj) {
                            //var scalefactor = scrollobj.s;
                            var scale = scrollobj.s;
                            thispage.vectorischanged = true;
                            //scalefactor *= scale;
                            //thispage.DocRef.foxitdoc.zoomdirect(thispage.pagenumber, scalefactor);
                            scrollobj = null;
                        }
                    }, 100);
                };
                this.scrollzoombuffer = function (scale, pscale, x, y) {
                    //var debounce_timer;
                    var scrollobj = { s: scale, ps: pscale, x: x, y: y };
                    //thispage.scrollzoomarray.push(scrollobj);
                    if (thispage.debounce_timer) {
                        //console.log('scrollzoom', 'clear');
                        window.clearTimeout(thispage.debounce_timer);
                    }
                    thispage.debounce_timer = window.setTimeout(function () {
                        // run your actual function here
                        if (thispage.DocRef.pages[thispage.DocRef.currentpage].pagenumber == thispage.pagenumber) {
                            if (scrollobj) {
                                thispage.foxitdirectZoom(scrollobj.s);
                                scrollobj = null;
                            }
                        }
                        scrollobj = null;
                    }, 1000);
                    //thispage.scrollzoomtime = date(now);
                };
                this.getBlockCentroid = function (blockId) {
                    if (this.usevectorxml) {
                        var centroid = thispage.VectorPageObj.getblockcentroid(blockId, thispage.dscalevector, thispage.dxvector, thispage.dyvector);
                    }
                    return centroid;
                };
                this.ZoomToBlock = function (blockId) {
                    
                    if (this.usevectorxml) {
                        var zoomrect = thispage.VectorPageObj.getBlockRect(blockId, thispage.dscalevector, thispage.dxvector, thispage.dyvector);
                        if (zoomrect.found) {
                            this.zoom_update(zoomrect.x - zoomrect.wp, zoomrect.y - zoomrect.hp, zoomrect.w + (zoomrect.wp * 2), zoomrect.h + (zoomrect.hp * 2));
                        }
                    }

                    /*if (this.usevectorxml) {
                        var zoomrect = thispage.VectorPageObj.getBlockInsertRect(blockId, thispage.dscalevector, thispage.dxvector, thispage.dyvector);
                        if (zoomrect.found) {
                            this.zoom_update(zoomrect.x - zoomrect.wp, zoomrect.y - zoomrect.hp, zoomrect.w + (zoomrect.wp * 2), zoomrect.h + (zoomrect.hp * 2));
                        }
                    }*/

                    
                    /*return  {
                        found : true,
                        x : minx,
                        y : miny,
                        w : maxx - minx,
                        h : maxy - miny,
                        wp : (maxx - minx) / 10,
                        hp : (maxy - miny) / 10
                    };*/
                };
                this.ZoomToBlockInsert = function (blockId) {

                    if (this.usevectorxml) {
                        var zoomrect = thispage.VectorPageObj.getBlockInsertRect(blockId, thispage.dscalevector, thispage.dxvector, thispage.dyvector);
                        var rotrect = zoomrect;

                        if(thispage.getpageRotation() != 0){
                            var rotrect = blockrectrotated(zoomrect, thispage.getpageRotation());
                        }
                        
                        

                        if (zoomrect.found) {
                            this.zoom_update(rotrect.x, rotrect.y, rotrect.w, rotrect.h, true);
                            if((thispage.dscalevector * 0.1) > thispage.fixedScale){
                                this.ZoomOut(10);
                            }else{
                                this.ZoomOut(2);
                            }
                            
                        }
                    }


                };


                this.ZoomOutTest = function (factor, center, mousepos) {
                    if (this.usevector3Dxml) {
                        return;
                    }
                    if (mousepos) {
                        var rotatedpoint = { x: mousepos.x, y: mousepos.y };
                        if (thispage.getpageRotation() != 0) {
                            rotatedpoint = mouse_rotated(mousepos.x, mousepos.y);
                        }
                        var mouseposdiffx = (((canvasowidth / 2) - rotatedpoint.x) / factor) - ((canvasowidth / 2) - rotatedpoint.x);
                        var mouseposdiffy = (((canvasoheight / 2) - rotatedpoint.y) / factor) - ((canvasoheight / 2) - rotatedpoint.y);

                        if (this.usepdfjs) {

                            rotatedpoint = { x: mousepos.xus, y: mousepos.yus };
                            if (thispage.getpageRotation() != 0) {
                                rotatedpoint = mouse_rotated(mousepos.xus, mousepos.yus);
                            }
    
                            mouseposdiffx = (((canvasowidthsize / 2) - rotatedpoint.x) / factor) - ((canvasowidthsize / 2) - rotatedpoint.x);
                            mouseposdiffy = (((canvasoheightsize / 2) - rotatedpoint.y) / factor) - ((canvasoheightsize / 2) - rotatedpoint.y);
    
                        }


                    }
                    var prevcenterx = (canvasowidth - (this.MainImageWidth * this.dscale)) / 2;
                    var prevcentery = (canvasoheight - (this.MainImageHeight * this.dscale)) / 2;
                    var diffx = prevcenterx - this.dx;
                    var diffy = prevcentery - this.dy;
                    var localdscale = this.dscale / factor;
                    var centerx = (canvasowidth - (this.MainImageWidth * localdscale)) / 2;
                    var centery = (canvasoheight - (this.MainImageHeight * localdscale)) / 2;
                    if (center) {
                        var localdx = centerx;
                        var localdy = centery;
                    }
                    else {
                        localdx = centerx - (diffx / factor);
                        localdy = centery - (diffy / factor);
                    }
                    if (mousepos) {
                        localdx += mouseposdiffx;
                        localdy += mouseposdiffy;
                    }
                    if (this.usevectorxml && thispage.VectorPageObj != undefined) {
                        var prevcenterxv = (canvasowidth - (thispage.VectorPageObj.width * this.dscalevector)) / 2;
                        var prevcenteryv = (canvasoheight - (thispage.VectorPageObj.height * this.dscalevector)) / 2;
                        var diffxv = prevcenterxv - this.dxvector;
                        var diffyv = prevcenteryv - this.dyvector;
                        var localdscalevector = this.dscalevector / factor;
                        var centerxv = ((canvasowidth - (thispage.VectorPageObj.width * localdscalevector)) / 2);
                        var centeryv = ((canvasoheight - (thispage.VectorPageObj.height * localdscalevector)) / 2);
                        if (center) {
                            var localdxvector = centerxv;
                            var localdyvector = centeryv;
                        }
                        else {
                            localdxvector = centerxv - (diffxv / factor);
                            localdyvector = centeryv - (diffyv / factor);
                        }
                        if (mousepos) {
                            localdxvector += mouseposdiffx;
                            localdyvector += mouseposdiffy;
                        }
                    }
                    if (this.usepdfjs) {
                        var pcanvheight = thispage.offscreenheight;
                        var pcanvwidth = thispage.offscreenwidth;

                        if(thispage.getpageRotation() != 0){
                            pcanvwidth = thispage.originalPDFwidth;
                            pcanvheight = thispage.originalPDFheight;
                        }

                        /*if (thispage.usedincomposite && thispage.compositereference != undefined) {
                            //var pcanvheight = thispage.compositereference.ovpagecanvas.height;
                            //var pcanvwidth = thispage.compositereference.ovpagecanvas.width;
                            var pcanvheight = thispage.compositereference.overlaypage.getheight();
                            var pcanvwidth = thispage.compositereference.overlaypage.getwidth();
                        }
                        else {

                        }*/
                        var prevcenterxpdf = (canvasowidth - (pcanvwidth * this.dscalepdf)) / 2;
                        var prevcenterypdf = (canvasoheight - (pcanvheight * this.dscalepdf)) / 2;

                        prevcenterxpdf = (canvasowidthsize - (pcanvwidth * this.dscalepdf)) / 2;
                        prevcenterypdf = (canvasoheightsize - (pcanvheight * this.dscalepdf)) / 2;


                        var diffxpdf = prevcenterxpdf - this.dxpdf;
                        var diffypdf = prevcenterypdf - this.dypdf;
                        var tempdx = this.dxpdf;
                        var tempdy = this.dypdf;
                        var yscale = canvasoheight / pcanvheight;
                        var xscale = canvasowidth / pcanvwidth;

                        yscale = canvasoheightsize / pcanvheight;
                        xscale = canvasowidthsize / pcanvwidth;


                        var dscale = Math.min(xscale, yscale);
                        var wscale = ((pcanvwidth * this.dscalepdf) / thispage.pdfpagewidth) * factor;
                        var hscale = ((pcanvheight * this.dscalepdf) / thispage.pdfpageheight) * factor;
                        var scale = Math.min(wscale, hscale);
                        /*if (scale < this.nPDFMaxFactor) {
                            this.bMaxzoom = false;
                            //console.log('maxzoom ' + this.bMaxzoom);
                        } else {
                            if (!this.bMaxzoom) {
                                thispage.DocRef.renderPDFscale();
                            }
        
                            this.bMaxzoom = true;
                            //console.log('maxzoom ' + this.bMaxzoom);
                        }*/
                        //01.11.2018 disabled for new zoom control
                        var singlepage = (thispage.DocRef.pages.length == 1);

                        if (singlepage) {
                            var localdscalepdf = this.dscalepdf / factor;
                            this.bMinzoom = false;
                        }
                        else {
                            if (this.dscalepdf / factor < dscale) {
                                localdscalepdf = dscale;
                                //this.bMinzoom = true;
                            }
                            else {
                                localdscalepdf = this.dscalepdf / factor;
                                //this.bMinzoom = false;
                            }
                        }


                        /*if ((thispage.usedincomposite && thispage.compositereference != undefined) || singlepage) {
                            var localdscalepdf = this.dscalepdf / factor;
                            this.bMinzoom = false;
                        }
                        else {
                            if (this.dscalepdf / factor < dscale) {
                                localdscalepdf = dscale;
                                //this.bMinzoom = true;
                            }
                            else {
                                localdscalepdf = this.dscalepdf / factor;
                                //this.bMinzoom = false;
                            }
                        }*/
                        var centerxpdf = ((canvasowidth - (pcanvwidth * this.dscalepdf)) / 2);
                        var centerypdf = ((canvasoheight - (pcanvheight * this.dscalepdf)) / 2);

                        centerxpdf = ((canvasowidthsize - (pcanvwidth * this.dscalepdf)) / 2);
                        centerypdf = ((canvasoheightsize - (pcanvheight * this.dscalepdf)) / 2);


                        if (center) {
                            var localdxpdf = centerxpdf;
                            var localdypdf = centerypdf;
                        }
                        else {
                            localdxpdf = centerxpdf - (diffxpdf / factor);
                            localdypdf = centerypdf - (diffypdf / factor);
                        }
                        /*if (pcanvwidth <= canvasowidth) {
                            localdxpdf = centerxpdf;
                            var bWidthlocked = true;
                        }
                        if (pcanvwidth * this.dscalepdf <= canvasowidth) {
                            localdxpdf = (canvasowidth - (pcanvwidth * this.dscalepdf)) / 2;
                            bWidthlocked = true;
                        }*/

                        if (pcanvwidth <= canvasowidthsize) {
                            localdxpdf = centerxpdf;
                            var bWidthlocked = true;
                        }
                        if (pcanvwidth * this.dscalepdf <= canvasowidthsize) {
                            localdxpdf = (canvasowidthsize - (pcanvwidth * this.dscalepdf)) / 2;
                            bWidthlocked = true;
                        }


                        if (mousepos) {
                            if (!bWidthlocked) {
                                localdxpdf += mouseposdiffx;
                            }
                            localdypdf += mouseposdiffy;
                        }
                    }
                    if (!this.usevectorxml) {
                        if (this.usepdfjs) {
                            var localstartx = localdxpdf;
                            var localstarty = localdypdf;
                            var localendx = (pcanvwidth * localdscalepdf) + localstartx;
                            var localendy = (pcanvheight * localdscalepdf) + localstarty;
                            
                        }
                        /*else {
                        }*/
                    }
                    else {
                        if(thispage.VectorPageObj != undefined){
                            localstartx = localdxvector;
                            localstarty = localdyvector;
                            localendx = (thispage.VectorPageObj.width * localdscalevector) + localstartx;
                            localendy = (thispage.VectorPageObj.height * localdscalevector) + localstarty;
                        }
                    }
                    //new method to work on local start and end variables.
                    var pagerect = thispage.getpagetestrect({ startx: localstartx, starty: localstarty, endx: localendx, endy: localendy });
                    return pagerect;
                    //var rotrect = {x1:rect.startx,y1: rect.starty,x2: rect.endx,y2:rect.endy};
                };


                


                this.textselectreturn = function(retobj){

                    thispage.textselectobj = retobj;

                    if(thispage.textselectobj.bend){

                        //thispage.textselectobj.bend = false;
                        thispage.textselectobj.complete = true; 

                        if(RxCore_GUI_TextCopied != undefined){
                            if(thispage.textselectobj.rectarray != undefined){
                                if(thispage.textselectobj.rectarray[0] != undefined){
                                    RxCore_GUI_TextCopied.TextCopied(thispage.DocRef.OriginalURL, thispage.textselectobj.rectarray[0].text);   
                                }
                                
                            }
                            
                        }

                    }

                };

                this.SelectText = function(bstart, bend, startpos, mousepos){

                    thispage.textselectobj.bstart = bstart;
                    thispage.textselectobj.bend = bend;
                    //thispage.textselectobj.bend = complete;


                    if(bstart){
                        thispage.textselectobj.rectarray = undefined;
                        thispage.textselectobj.start = -1;
                        thispage.textselectobj.end = -1;
                        thispage.textselectobj.$handler = null;
                    }

                    //this.selectText = function(npagenum, bstart, startpoint, movepoint){

                    if (mousepos) {
                        var rotatedpoint = { x: mousepos.x, y: mousepos.y };
                        var rotatedspoint = { x: startpos.x, y: startpos.y };

                        rotatedpoint = { x: mousepos.xus, y: mousepos.yus };
                        rotatedspoint = { x: startpos.xus, y: startpos.yus };

                        
                        if (thispage.getpageRotation() != 0) {
                            rotatedpoint = mouse_rotated(mousepos.x, mousepos.y);
                            rotatedspoint = mouse_rotated(startpos.x, startpos.y);

                            rotatedpoint = mouse_rotated(mousepos.xus, mousepos.yus);
                            rotatedspoint = mouse_rotated(startpos.xus, startpos.yus);

                        }
                        
                        
                        //var pdfpoint = thispage.getPDFcoords(rotatedpoint.x, rotatedpoint.y);
                        var pdfpoint = thispage.getPDFCoordsRot(rotatedpoint);


                        //var pdfspoint = thispage.getPDFcoords(rotatedspoint.x, rotatedspoint.y);
                        var pdfspoint = thispage.getPDFCoordsRot(rotatedspoint);

                        

                        thispage.textselectobj.startpos = [pdfspoint.x, pdfspoint.y];
                        thispage.textselectobj.endpos = [pdfpoint.x, pdfpoint.y];
    

                        if (thispage.DocRef.foxitdoc != null) {
                        
                            thispage.DocRef.foxitdoc.selectText(thispage.pagenumber, thispage.textselectobj, thispage.textselectreturn);
                        }
                        
                    }

                };

                function getPDFOrigo(){

                    var origox = 0;
                    var origoy = 0;

                    if(thispage.dxpdf > 0){
                        origox = 8;
                    }
                    if(thispage.dypdf > 0){
                        origoy = 8;
                    }

                    var origo = {x: origox, y: origoy};


                    switch(thispage.getpageRotation()){
                        case 0 : 
                            origo = {x: origox, y: origoy};
                            break;
                        case 90 :    
                            origo = {x: canvasowidthsize, y : origoy};
                            //origo = {x: origox, y: canvasoheightsize};
                            break;
                        case 180 :    
                            origo = {x: canvasowidthsize, y: canvasoheightsize};
                        break;
                        case 270 :    
                            origo = {x: origox, y: canvasoheightsize};
                            //origo = {x: canvasowidthsize, y : origoy};

                        break;


                    }
                    return origo;

                }
                
                
                this.postscaleRefresh = function(){

                    window.setTimeout(function () {
                        // run your actual function here
                        thispage.DocRef.forcefoxitRedraw();

                    }, 500);


                };

                this.ZoomIn = function (factor, center, bexternal, mousepos) {
                    if (this.usevector3Dxml) {
                        return;
                    }

                    if(thispage.DocRef.textselect){
                        mousepos = undefined;
                    }

                    var centerx = canvasowidth * 0.5;
                    var centery = canvasoheight * 0.5;

                    var centerxus = canvasowidthsize * 0.5;
                    var centeryus = canvasoheightsize * 0.5;


                    var pdfcenterrotate = { x: centerxus, y: centeryus };
                    var pdfcenternorotate = { x: centerxus, y: centeryus };

                    if (thispage.getpageRotation() != 0) {
                        pdfcenterrotate = mouse_rotated(centerxus, centeryus);
                    }


                    //
                    

                    


                    if (RxCore_GUI_ZoomUpdate != undefined && bexternal == false) {
                        var pagerect = thispage.getpagerect();
                        var zoomparams = { factor: factor, center: center, mousepos: mousepos, rect: pagerect };
                        RxCore_GUI_ZoomUpdate.zoomUpdate(zoomparams, 0);
                    }

                    if (mousepos) {
                        var rotatedpoint = { x: mousepos.x, y: mousepos.y };
                        var rotatedpointus = { x: mousepos.xus, y: mousepos.yus };
                        //mouse rotated before calling getPDFcoords
                        if (thispage.getpageRotation() != 0 && !thispage.usepdfjs) {
                            rotatedpoint = mouse_rotated(mousepos.x, mousepos.y);
                            rotatedpointus = mouse_rotated(mousepos.xus, mousepos.yus);
                        }

                        var mouseposdiffx = (((canvasowidth / 2) - rotatedpoint.x) * factor) - ((canvasowidth / 2) - rotatedpoint.x);
                        var mouseposdiffy = (((canvasoheight / 2) - rotatedpoint.y) * factor) - ((canvasoheight / 2) - rotatedpoint.y);
                        
                        //var pdfpoint = thispage.getPDFcoords(rotatedpoint.x, rotatedpoint.y);

                        
                        

                        
                        //var pdfoffset = thispage.getPDFcoords(mouseposdiffx, mouseposdiffy, thispage.dxpdf, thispage.dypdf, (1 / factor));
                        
                        //console.log(mouseposdiffx, mouseposdiffy);

                        
                    }

                    var stats = getPagestats();

                    if (this.usepdfjs) {
                        var pdfscaleoffset = ScaleZoomInPDF(center, factor, stats, true);
                        //console.log('zoomin invalidation');

                        var pdfcenter = thispage.getPDFCoordsRot(pdfcenternorotate);


                        if (mousepos) {


                            var pdfpoint = thispage.getPDFCoordsRot(rotatedpointus);
                            //var pdfpoint = thispage.getPDFcoords(mousepos.x, mousepos.y);
    
    
                            var origox = 0;
                            var origoy = 0;
    
                            if(this.dxpdf > 0){
                                origox = 8;
                            }
                            if(this.dypdf > 0){
                                origoy = 8;
                            }
    
                            var rotatedorigo = { x: origox, y: origoy };
                            var norotatedorigo = { x: origox, y: origoy };
    
                            if (thispage.getpageRotation() != 0) {
                                rotatedorigo = mouse_rotated(rotatedorigo.x,rotatedorigo.y);
                            }
    
    
                            //var pdfoffset = thispage.getPDFcoords(rotatedorigo.x,rotatedorigo.y);
                            var pdfoffset = thispage.getPDFCoordsRot(norotatedorigo);

                            
                            

                            if (!pdfscaleoffset.bWidthlocked || (thispage.DocRef.pagelocked && !bRestrictPan)) {
                                //this.dxpdf += mouseposdiffx;
                                pdfscaleoffset.dx += mouseposdiffx;
                                
                                pdfscaleoffset.startx += mouseposdiffx;
                                pdfscaleoffset.endx += mouseposdiffx;

                            }
                            pdfscaleoffset.dy += mouseposdiffy;

                            pdfscaleoffset.starty += mouseposdiffy;
                            pdfscaleoffset.endy += mouseposdiffy;


                            //var xp = mousepos.x - centerx;
                            //var yp = mousepos.y - centery;

                            
                            //var orgpoint = {x : rotatedpoint.x, y: rotatedpoint.y};

                            //thispage.getPDFcoords(pdfscaleoffset.dx, pdfscaleoffset.dx);

                            //var pdfpoint = thispage.getPDFcoords(mousepos.x,mousepos.y);
                            pdfscaleoffset.mousepoint = pdfpoint;
                            pdfscaleoffset.offsetpoint = pdfoffset;
                            

                            pdfscaleoffset.bIn = true;
                            pdfscaleoffset.dfactor = factor;

                            //mousepoint : stats.mousepoint,
                            //bIn : bIn,
                            //dfactor : factor 

                            //pdfscaleoffset.bIn = true

                            //thispage.foxitdirectZoomTo(thispage.dscalepdf, factor, pdfpoint, false);

                            //this.dypdf += mouseposdiffy;
                        }else{
                            pdfscaleoffset.mousepoint = undefined;
                        }
                        
                        pdfscaleoffset.offsetcenter = pdfcenter;

                        setPagestats(pdfscaleoffset, true);

                        //not to be used with foxit
                        
                        /*if (thispage.pagenumber == thispage.DocRef.pages[thispage.DocRef.currentpage].pagenumber) {
                            
                            thispage.DocRef.ZoomIn(factor, center);

                        }*/

                        //console.log('zoomin invalidation');
                    }
                    else {
                        if(stats != undefined){
                            var scaleoffset = ScaleZoom(center, factor, stats, true);
                            if (mousepos) {
                                scaleoffset.dx += mouseposdiffx;
                                scaleoffset.dy += mouseposdiffy;

                                scaleoffset.startx += mouseposdiffx;
                                scaleoffset.endx += mouseposdiffx;
                                
                                scaleoffset.starty += mouseposdiffy;
                                scaleoffset.endy += mouseposdiffy;

                            }
                            setPagestats(scaleoffset, true);
                        }
                    }
                    //vectorscale
                    //thispage.pan_update(0,0, false);
                    redrawMarkup();
                    var pagerect = thispage.getpagerect();

                    if(pagerect.x > 5 && pagerect.y > 5 ){
                        thispage.postscaleRefresh();
                    }

                    
                    //thispage.pan_update(0,0, false);

                    if (RxCore_GUI_ZoomUpdated != undefined && bexternal == false) {
                        var pagerect = thispage.getpagerect();
                        var zoomparams = { factor: factor, center: center, mousepos: mousepos, rect: pagerect };
                        RxCore_GUI_ZoomUpdated.zoomUpdate(zoomparams, 0);
                    }
                };
                this.ZoomOut = function (factor, center, bexternal, mousepos) {
                    if (this.usevector3Dxml) {
                        return;
                    }

                    var pixelRatio = getPixelRatio();

                    if(thispage.DocRef.textselect){
                        mousepos = undefined;
                    }

                    var centerx = canvasowidth * 0.5;
                    var centery = canvasoheight * 0.5;


                    var centerxus = canvasowidthsize * 0.5;
                    var centeryus = canvasoheightsize * 0.5;


                    var centernorot = { x: centerx, y: centery };
                    var centernorotus = { x: centerxus, y: centeryus };

                    if (RxCore_GUI_ZoomUpdate != undefined && bexternal == false) {
                        var pagerect = thispage.getpagerect();
                        var zoomparams = { factor: factor, center: center, mousepos: mousepos, rect: pagerect };
                        RxCore_GUI_ZoomUpdate.zoomUpdate(zoomparams, 1);
                    }
                    if (mousepos) {
                        var rotatedpoint = { x: mousepos.x, y: mousepos.y };
                        if (thispage.getpageRotation() != 0 && !thispage.usepdfjs) {
                            rotatedpoint = mouse_rotated(mousepos.x, mousepos.y);
                        }
                        var mouseposdiffx = (((canvasowidth / 2) - rotatedpoint.x) / factor) - ((canvasowidth / 2) - rotatedpoint.x);
                        var mouseposdiffy = (((canvasoheight / 2) - rotatedpoint.y) / factor) - ((canvasoheight / 2) - rotatedpoint.y);


                        if (this.usepdfjs) {
                            rotatedpoint = { x: mousepos.xus, y: mousepos.yus };
                            /*if (thispage.getpageRotation() != 0) {
                                rotatedpoint = mouse_rotated(mousepos.xus, mousepos.yus);
                            }*/
                            mouseposdiffx = (((canvasowidthsize / 2) - rotatedpoint.x) / factor) - ((canvasowidthsize / 2) - rotatedpoint.x);
                            mouseposdiffy = (((canvasoheightsize / 2) - rotatedpoint.y) / factor) - ((canvasoheightsize / 2) - rotatedpoint.y);
    
                        }
                        
                        //var pdfpoint = thispage.getPDFcoords(rotatedpoint.x,rotatedpoint.y);
                        

                    }
                    /*var prevcenterx = (canvasowidth - (this.MainImageWidth * this.dscale)) / 2;
                    var prevcentery = (canvasoheight - (this.MainImageHeight * this.dscale)) / 2;
        
                    var diffx = prevcenterx - this.dx;
                    var diffy = prevcentery - this.dy;
        
                    this.dscale = this.dscale / factor;
        
                    var centerx = (canvasowidth - (this.MainImageWidth * this.dscale)) / 2;
                    var centery = (canvasoheight - (this.MainImageHeight * this.dscale)) / 2;
                
                    if (center) {
                        this.dx = centerx;
                        this.dy = centery;
                    } else {
                        this.dx = centerx - (diffx / factor);
                        this.dy = centery - (diffy / factor);
                    }*/
                    /*if (mousepos) {
                        this.dx += mouseposdiffx;
                        this.dy += mouseposdiffy;
                    }*/
                    if (this.usevectorxml) {
                        /*var prevcenterxv = (canvasowidth - (thispage.VectorPageObj.width * this.dscalevector)) / 2;
                        var prevcenteryv = (canvasoheight - (thispage.VectorPageObj.height * this.dscalevector)) / 2;
        
                        var diffxv = prevcenterxv - this.dxvector;
                        var diffyv = prevcenteryv - this.dyvector;
        
        
                        this.dscalevector = this.dscalevector / factor;
        
                        var centerxv = ((canvasowidth - (thispage.VectorPageObj.width * this.dscalevector)) / 2);
                        var centeryv = ((canvasoheight - (thispage.VectorPageObj.height * this.dscalevector)) / 2);
        
        
                        if (center) {
                            this.dxvector = centerxv;
                            this.dyvector = centeryv;
                        } else {
                            this.dxvector = centerxv - (diffxv / factor);
                            this.dyvector = centeryv - (diffyv / factor);
                        }*/
                    }
                    if (this.usepdfjs) {
                        var pcanvheight = thispage.offscreenheight;
                        var pcanvwidth = thispage.offscreenwidth;



                        var pdfcenter = thispage.getPDFCoordsRot(centernorotus);

                        if (mousepos) {
                            var pdfpoint = thispage.getPDFCoordsRot(rotatedpoint);

                            var rotatedorigo = { x: 0, y: 0 };
                            var norotatedorigo = { x: 0, y: 0 };
            
                            if (thispage.getpageRotation() != 0) {
                                rotatedorigo = mouse_rotated(rotatedorigo.x,rotatedorigo.y);
                            }
    
                            //var pdfoffset = thispage.getPDFcoords(rotatedorigo.x,rotatedorigo.y);
                            var pdfoffset = thispage.getPDFCoordsRot(norotatedorigo);

                        }


                        var prevcenterxpdf = (canvasowidth - (pcanvwidth * this.dscalepdf)) / 2;
                        var prevcenterypdf = (canvasoheight - (pcanvheight * this.dscalepdf)) / 2;

                        prevcenterxpdf = (canvasowidthsize - (pcanvwidth * this.dscalepdf)) / 2;
                        prevcenterypdf = (canvasoheightsize - (pcanvheight * this.dscalepdf)) / 2;

                        var diffxpdf = prevcenterxpdf - this.dxpdf;
                        var diffypdf = prevcenterypdf - this.dypdf;
                        var tempdx = this.dxpdf;
                        var tempdy = this.dypdf;


                        var yscale = canvasoheight / pcanvheight;
                        var xscale = canvasowidth / pcanvwidth;

                        yscale = canvasoheightsize / pcanvheight;
                        xscale = canvasowidthsize / pcanvwidth;

                        var dscale = Math.min(xscale, yscale);


                        var wscale = ((pcanvwidth * this.dscalepdf) / thispage.pdfpagewidth) * factor;
                        var hscale = ((pcanvheight * this.dscalepdf) / thispage.pdfpageheight) * factor;


                        var temppdfscale = Math.min(wscale, hscale);
                        /*if (scale < this.nPDFMaxFactor) {
                            this.bMaxzoom = false;
                            //console.log('maxzoom ' + this.bMaxzoom);
                        } else {
                            if (!this.bMaxzoom) {
                                thispage.DocRef.renderPDFscale();
                            }
        
                            this.bMaxzoom = true;
                            //console.log('maxzoom ' + this.bMaxzoom);
                        }*/
                        //01.11.2018 disabled for new zoom control
                        var singlepage = (thispage.DocRef.pages.length == 1);

                        if (singlepage) {

                            //dscalepdf modified
                            this.dscalepdf = this.dscalepdf / factor;
                            this.bMinzoom = false;
                        }
                        else {
                            if (this.dscalepdf / factor < dscale) {
                                
                                //dscalepdf modified
                                this.dscalepdf = dscale;
                                this.bMinzoom = true;
                            }
                            else {
                                //dscalepdf modified
                                this.dscalepdf = this.dscalepdf / factor;
                                this.bMinzoom = false;
                            }
                        }
                        
                        /*if ((thispage.usedincomposite && thispage.compositereference != undefined) || singlepage) {
                            this.dscalepdf = this.dscalepdf / factor;
                            this.bMinzoom = false;
                        }
                        else {
                            if (this.dscalepdf / factor < dscale) {
                                this.dscalepdf = dscale;
                                this.bMinzoom = true;
                            }
                            else {
                                this.dscalepdf = this.dscalepdf / factor;
                                this.bMinzoom = false;
                            }
                        }*/
                        var centerxpdf = ((canvasowidth - (pcanvwidth * this.dscalepdf)) / 2);
                        var centerypdf = ((canvasoheight - (pcanvheight * this.dscalepdf)) / 2);

                        centerxpdf = ((canvasowidthsize - (pcanvwidth * this.dscalepdf)) / 2);
                        centerypdf = ((canvasoheightsize - (pcanvheight * this.dscalepdf)) / 2);


                        if (center) {
                            this.dxpdf = centerxpdf;
                            this.dypdf = centerypdf;
                        }
                        else {
                            this.dxpdf = centerxpdf - (diffxpdf / factor);
                            this.dypdf = centerypdf - (diffypdf / factor);
                        }
                        /*if (pcanvwidth <= canvasowidth) {
                            if (!thispage.DocRef.pagelocked) {
                                this.dxpdf = centerxpdf;
                            }
                            var bWidthlocked = true;
                        }
                        if (pcanvwidth * this.dscalepdf <= canvasowidth) {
                            if (!thispage.DocRef.pagelocked) {
                                thispage.dxpdf = (canvasowidth - (pcanvwidth * this.dscalepdf)) / 2;
                            }
                            bWidthlocked = true;
                        }*/

                        if (pcanvwidth <= canvasowidthsize) {
                            if (!thispage.DocRef.pagelocked) {
                                this.dxpdf = centerxpdf;
                            }
                            var bWidthlocked = true;
                        }
                        if (pcanvwidth * this.dscalepdf <= canvasowidthsize) {
                            if (!thispage.DocRef.pagelocked) {
                                thispage.dxpdf = (canvasowidthsize - (pcanvwidth * this.dscalepdf)) / 2;
                            }
                            bWidthlocked = true;
                        }


                        if (mousepos) {


                            
    
                            //var pdfoffset = thispage.getPDFcoords(mouseposdiffx, mouseposdiffy);
                            //var pdfoffset = thispage.getPDFcoords(0,0);
    
    
    
    
                            //var pdfcenter = thispage.getPDFcoords(canvasowidth / 2, canvasoheight / 2);
                            
    

                            if (!bWidthlocked || (thispage.DocRef.pagelocked && !bRestrictPan)) {
                                //this.dxpdf += mouseposdiffx;
                                this.startx += mouseposdiffx;
                                this.endx += mouseposdiffx;
    
                            }
                            //this.dypdf += mouseposdiffy;
                            this.starty += mouseposdiffy;
                            this.endy += mouseposdiffy;

                        }

                        //not to be used with foxit
                        /*if (thispage.pagenumber == thispage.DocRef.pages[thispage.DocRef.currentpage].pagenumber) {

                            
                            thispage.DocRef.ZoomOut(factor, center, false);
                            
                            
                        }*/
                        //DocObj.draw_mpagepdf();
                        //thispage.renderPDFpagescale();
                        thispage.pdfrenderopque += 1;
                        thispage.pdfisrendered = false;

                        //this.getPDFpageScale = function(){

                        //var pgscalefactor = thispage,(thispage.offscreenwidth / thispage.pdfpagewidth);
                        var pgscalefactor = thispage.getPDFpageScale();
                        //thispage.scrollzoombuffer(thispage.dscalepdf,pgscalefactor, thispage.dxpdf, thispage.dypdf);

                        //mousepos
                        if(mousepos){

                            //var xp = mousepos.x - centerx;
                            //var yp = mousepos.y - centery;
                            //console.log(this.dxpdf,this.dypdf);
                            
                            //var orgpoint = {x : rotatedpoint.x, y: rotatedpoint.y};
                            //pdfscaleoffset.mousepoint = pdfpoint;

                            //var pdfoffset = thispage.getPDFcoords(0,0);
                            //var pdfcenter = thispage.getPDFcoords(canvasowidth / 2, canvasoheight / 2);
    


                            thispage.foxitdirectZoomTo(thispage.dscalepdf, factor, pdfpoint, pdfoffset, pdfcenter, false);
                            //this.foxitdirectZoomTo = function(scale, factor, point, bin){
                            //bIn
                        }else{
                            //thispage.foxitdirectZoom(thispage.dscalepdf);

                            var centerx = canvasowidth * 0.5;
                            var centery = canvasoheight * 0.5;
        
    
                            var realangle = getcombinedangle(thispage.getpageRotation(), thispage.foxitpageorigrot);
                            var distx = thispage.getPDFDistScale(centerx, thispage.dscalepdf);
                            var disty = thispage.getPDFDistScale(centery, thispage.dscalepdf);


                            

                            switch (realangle){
                                case 0 :
                                    pdfcenter.x = pdfcenter.x - distx;
                                    pdfcenter.y = pdfcenter.y + disty;
                                    break;
                                case 90 :
                                    pdfcenter.x = pdfcenter.x - disty;
                                    pdfcenter.y = pdfcenter.y - distx;
    
                                    break;
                                case 180 :
                                    pdfcenter.x = pdfcenter.x + distx;
                                    pdfcenter.y = pdfcenter.y - disty;
    
                                    break;
                                case 270 :
    
                                    pdfcenter.x = pdfcenter.x + disty;
                                    pdfcenter.y = pdfcenter.y + distx;
    
                                break;
    
                            }     

                            //var pdfcenternorotate = { x: centerx, y: centery };
    
                            //var zoomtopoint = thispage.getPDFCoordsRot(pdfcenternorotate);

                            //var  = thispage.getPDFCoordsRot(centernorot);

                            //pdfcenter = thispage.getPDFCoordsRot(centernorot);

                            thispage.foxitdirectZoomToEx(thispage.dscalepdf, pdfcenter);

                        }

                        
                        //console.log('zoomout invalidation');
                    }
                    else {
                        var stats = getPagestats();
                        var scaleoffset = ScaleZoom(center, factor, stats, false);
                        if (mousepos) {
                            scaleoffset.dx += mouseposdiffx;
                            scaleoffset.dy += mouseposdiffy;

                            scaleoffset.startx += mouseposdiffx;
                            scaleoffset.endx += mouseposdiffx;
                            
                            scaleoffset.starty += mouseposdiffy;
                            scaleoffset.endy += mouseposdiffy;

                        }
                        setPagestats(scaleoffset, true);
                    }
                    if (!this.usevectorxml) {
                        if (this.usepdfjs) {
                            thispage.setpagedimsize(this.dxpdf, this.dypdf, (pcanvwidth * this.dscalepdf), (pcanvheight * this.dscalepdf), false);
                            if (thispage.pagenumber == thispage.DocRef.pages[thispage.DocRef.currentpage].pagenumber) {
                                
                                thispage.DocRef.draw_mpagepdf(false);
                            }
                        }
                    }
                    redrawMarkup();

                    //thispage.pan_update(0,0, false);

                    if (RxCore_GUI_ZoomUpdated != undefined && bexternal == false) {
                        var pagerect = thispage.getpagerect();
                        var zoomparams = { factor: factor, center: center, mousepos: mousepos, rect: pagerect };
                        RxCore_GUI_ZoomUpdated.zoomUpdate(zoomparams, 1);
                    }
                };
                this.zoomwidth = function (bexternal) {

                    if (this.usevector3Dxml) {
                        return;
                    }

                    if (RxCore_GUI_ZoomUpdate != undefined && bexternal == undefined) {
                        var pagerect = thispage.getpagerect();
                        var zoomparams = { rect: pagerect };
                        RxCore_GUI_ZoomUpdate.zoomUpdate(zoomparams, 4);
                    }
                    var xscale = 0.0;
                    var canvwidth = canvasowidth;
                    var canvheight = canvasoheight;

                    if (thispage.DocRef.foxitdoc != null) {
                        
                        canvwidth = canvasowidthsize - 40;
                        canvheight = canvasoheightsize - 40;
                    }

                    if (this.usepdfjs) {

                        var pcanvheight = thispage.offscreenheight;
                        var pcanvwidth = thispage.offscreenwidth;

                        /*if (thispage.usedincomposite && thispage.compositereference != undefined) {
                            var pcanvheight = thispage.compositereference.overlaypage.getheight();
                            var pcanvwidth = thispage.compositereference.overlaypage.getwidth();
                            //var pcanvheight = thispage.compositereference.ovpagecanvas.height;
                            //var pcanvwidth = thispage.compositereference.ovpagecanvas.width;
                        }
                        else {

                            if(thispage.getpageRotation() != 0){
                                pcanvwidth = thispage.originalPDFwidth;
                                pcanvheight = thispage.originalPDFheight;
                            }

                        }*/
                    }
                    else {
                        var stats = getPagestats();
                        var scaleoffset = ScaleZoomWidth(stats);
                        setPagestats(scaleoffset, false);
                    }
                    switch (this.getpageRotation()) {
                        case 0:
                            if (this.usepdfjs) {
                                xscale = canvwidth / pcanvwidth;
                                thispage.curzoomfunction = 0;

                                //dscalepdf modified
                                this.dscalepdf = xscale;
                                this.dxpdf = (canvasowidth - (pcanvwidth * this.dscalepdf)) / 2;
                                this.dypdf = (canvasoheight - (pcanvheight * this.dscalepdf)) / 2;

                                this.dxpdf = (canvasowidthsize - (pcanvwidth * this.dscalepdf)) / 2;
                                this.dypdf = (canvasoheightsize - (pcanvheight * this.dscalepdf)) / 2;

                            }
                            break;
                        case 90:
                            if (this.usepdfjs) {

                                if(thispage.bpagesizeupdated){
                                    xscale = canvwidth / pcanvwidth;
                                }else{
                                    xscale = canvwidth / pcanvheight;
                                }

                                thispage.curzoomfunction = 1;

                                //dscalepdf modified
                                this.dscalepdf = xscale;
                                this.dxpdf = (canvasowidth - (pcanvwidth * this.dscalepdf)) / 2;
                                this.dypdf = (canvasoheight - (pcanvheight * this.dscalepdf)) / 2;

                                this.dxpdf = (canvasowidthsize - (pcanvwidth * this.dscalepdf)) / 2;
                                this.dypdf = (canvasoheightsize - (pcanvheight * this.dscalepdf)) / 2;

                            }
                            break;
                        case 180:
                            if (this.usepdfjs) {
                                xscale = canvwidth / pcanvwidth;
                                
                                thispage.curzoomfunction = 0;

                                //dscalepdf modified
                                this.dscalepdf = xscale;
                                this.dxpdf = (canvasowidth - (pcanvwidth * this.dscalepdf)) / 2;
                                this.dypdf = (canvasoheight - (pcanvheight * this.dscalepdf)) / 2;

                                this.dxpdf = (canvasowidthsize - (pcanvwidth * this.dscalepdf)) / 2;
                                this.dypdf = (canvasoheightsize - (pcanvheight * this.dscalepdf)) / 2;

                            }
                            break;
                        case 270:
                            if (this.usepdfjs) {

                                if(thispage.bpagesizeupdated){
                                    xscale = canvwidth / pcanvwidth;
                                }else{
                                    
                                    xscale = canvwidth / pcanvheight;
                                }

                                thispage.curzoomfunction = 1;

                                //dscalepdf modified
                                this.dscalepdf = xscale;
                                this.dxpdf = (canvasowidth - (pcanvwidth * this.dscalepdf)) / 2;
                                this.dypdf = (canvasoheight - (pcanvheight * this.dscalepdf)) / 2;

                                this.dxpdf = (canvasowidthsize - (pcanvwidth * this.dscalepdf)) / 2;
                                this.dypdf = (canvasoheightsize - (pcanvheight * this.dscalepdf)) / 2;

                            }
                            break;
                        default:
                            if (this.usepdfjs) {
                                xscale = canvwidth / pcanvwidth;
                                
                                thispage.curzoomfunction = 0;

                                //dscalepdf modified
                                this.dscalepdf = xscale;
                                this.dxpdf = (canvasowidth - (pcanvwidth * this.dscalepdf)) / 2;
                                this.dypdf = (canvasoheight - (pcanvheight * this.dscalepdf)) / 2;

                                this.dxpdf = (canvasowidthsize - (pcanvwidth * this.dscalepdf)) / 2;
                                this.dypdf = (canvasoheightsize - (pcanvheight * this.dscalepdf)) / 2;



                            }
                            break;
                    }

                    if (!this.usevectorxml) {
                        if (this.usepdfjs) {
                            //DocObj.draw_mpagepdf();
                            thispage.setpagedimsize(this.dxpdf, this.dypdf, (pcanvwidth * this.dscalepdf), (pcanvheight * this.dscalepdf), false);
                            if (thispage.pagenumber == thispage.DocRef.pages[thispage.DocRef.currentpage].pagenumber) {

                                //not to be used with foxit
                                /*if (thispage.DocRef.foxitdoc == null) {
                                    thispage.DocRef.zoomwidth();
                                }*/

                                thispage.DocRef.draw_mpagepdf(false);

                                /*if (this.usedincomposite && this.compositereference != undefined) {
                                    //commented out 28.04
                                    //this.dscalepdf = this.compositereference.getoverlayScaleFactor(4);                            
                                    //this.compositereference.scaleToBackground(true);
                                    if (thispage.role == 1) {
                                        thispage.dscalepdf = thispage.compositereference.getoverlayScaleFactor(4);
                                        thispage.compositereference.scaleToBackground(true);
                                    }
                                    thispage.getfoxitbitmap(thispage.dscalepdf);
                                }
                                else {
                                }*/
                            }
                        }
                    }
                    if (this.usepdfjs) {
                        thispage.pdfrenderopque += 1;
                        thispage.pdfisrendered = false;
                        if (thispage.DocRef.pages[thispage.DocRef.currentpage].pagenumber == thispage.pagenumber) {
                            thispage.foxitdirectZoom(thispage.dscalepdf);
                        }
                        //console.log('zoomwidth invalidation');
                    }
                    redrawMarkup();
                    thispage.postscaleRefresh();
                    if (RxCore_GUI_ZoomUpdated != undefined && bexternal == undefined) {
                        var pagerect = thispage.getpagerect();
                        var zoomparams = { rect: pagerect };
                        RxCore_GUI_ZoomUpdated.zoomUpdate(zoomparams, 4);
                    }
                };
                this.zoomheight = function (bexternal) {

                    if (this.usevector3Dxml) {
                        return;
                    }

                    if (RxCore_GUI_ZoomUpdate != undefined && bexternal == undefined) {
                        var pagerect = thispage.getpagerect();
                        var zoomparams = { rect: pagerect };
                        RxCore_GUI_ZoomUpdate.zoomUpdate(zoomparams, 5);
                    }
                    var yscale = 0.0;

                    if (this.usepdfjs) {

                        var pcanvheight = thispage.offscreenheight;
                        var pcanvwidth = thispage.offscreenwidth;

                        /*if (thispage.usedincomposite && thispage.compositereference != undefined) {
                            //var pcanvheight = thispage.compositereference.ovpagecanvas.height;
                            //var pcanvwidth = thispage.compositereference.ovpagecanvas.width;
                            var pcanvheight = thispage.compositereference.overlaypage.getheight();
                            var pcanvwidth = thispage.compositereference.overlaypage.getwidth();
                        }
                        else {

                            if(thispage.getpageRotation() != 0){
                                pcanvwidth = thispage.originalPDFwidth;
                                pcanvheight = thispage.originalPDFheight;
                            }

                        }*/
                        switch (this.getpageRotation()) {
                            case 0:
                                yscale = canvasoheight / pcanvheight;
                                yscale = canvasoheightsize / pcanvheight;

                                thispage.curzoomfunction = 0;

                                //dscalepdf modified
                                this.dscalepdf = yscale;
                                this.dxpdf = (canvasowidth - (pcanvwidth * this.dscalepdf)) / 2;
                                this.dypdf = (canvasoheight - (pcanvheight * this.dscalepdf)) / 2;

                                this.dxpdf = (canvasowidthsize - (pcanvwidth * this.dscalepdf)) / 2;
                                this.dypdf = (canvasoheightsize - (pcanvheight * this.dscalepdf)) / 2;


                                break;
                            case 90:
                                //yscale = canvasoheight / pcanvwidth;

                                if(thispage.bpagesizeupdated){
                                    yscale = canvasoheight / pcanvwidth;
                                    yscale = canvasoheightsize / pcanvwidth;
                                }else{
                                    yscale = canvasoheight / pcanvheight;
                                    yscale = canvasoheightsize / pcanvheight;
                                }     
                                

                                thispage.curzoomfunction = 2;

                                //dscalepdf modified
                                this.dscalepdf = yscale;
                                this.dxpdf = (canvasowidth - (pcanvwidth * this.dscalepdf)) / 2;
                                this.dypdf = (canvasoheight - (pcanvheight * this.dscalepdf)) / 2;

                                this.dxpdf = (canvasowidthsize - (pcanvwidth * this.dscalepdf)) / 2;
                                this.dypdf = (canvasoheightsize - (pcanvheight * this.dscalepdf)) / 2;


                                break;
                            case 180:
                                yscale = canvasoheight / pcanvheight;
                                yscale = canvasoheightsize / pcanvheight;

                                thispage.curzoomfunction = 0;

                                //dscalepdf modified
                                this.dscalepdf = yscale;
                                this.dxpdf = (canvasowidth - (pcanvwidth * this.dscalepdf)) / 2;
                                this.dypdf = (canvasoheight - (pcanvheight * this.dscalepdf)) / 2;

                                this.dxpdf = (canvasowidthsize - (pcanvwidth * this.dscalepdf)) / 2;
                                this.dypdf = (canvasoheightsize - (pcanvheight * this.dscalepdf)) / 2;

                                break;
                            case 270:
                                //yscale = canvasoheight / pcanvwidth;

                                if(thispage.bpagesizeupdated){
                                    yscale = canvasoheight / pcanvheight;
                                    yscale = canvasoheightsize / pcanvheight;
                                }else{
                                    yscale = canvasoheight / pcanvwidth;
                                    yscale = canvasoheightsize / pcanvwidth;
                                }                                

                                thispage.curzoomfunction = 2;

                                //dscalepdf modified
                                this.dscalepdf = yscale;
                                this.dxpdf = (canvasowidth - (pcanvwidth * this.dscalepdf)) / 2;
                                this.dypdf = (canvasoheight - (pcanvheight * this.dscalepdf)) / 2;

                                this.dxpdf = (canvasowidthsize - (pcanvwidth * this.dscalepdf)) / 2;
                                this.dypdf = (canvasoheightsize - (pcanvheight * this.dscalepdf)) / 2;

                                break;
                        }
                    }
                    else {
                        var stats = getPagestats();
                        var scaleoffset = ScaleZoomHeight(stats);
                        setPagestats(scaleoffset, false);
                    }

                    if (this.usepdfjs) {
                        //DocObj.draw_mpagepdf();
                        thispage.setpagedimsize(this.dxpdf, this.dypdf, (pcanvwidth * this.dscalepdf), (pcanvheight * this.dscalepdf), false);
                        if (thispage.pagenumber == thispage.DocRef.pages[thispage.DocRef.currentpage].pagenumber) {

                            //not to be used with foxit
                            //thispage.DocRef.zoomheight();
                            thispage.DocRef.draw_mpagepdf(false);

                            /*if (this.usedincomposite && this.compositereference != undefined) {
                                //commented out 28.04
                                //this.dscalepdf = this.compositereference.getoverlayScaleFactor(5);
                                //this.compositereference.scaleToBackground(true);
                                if (thispage.role == 1) {
                                    thispage.dscalepdf = thispage.compositereference.getoverlayScaleFactor(5);
                                    thispage.compositereference.scaleToBackground(true);
                                }
                                thispage.getfoxitbitmap(thispage.dscalepdf);
                            }
                            else {
                            }*/
                        }
                    }
                    if (this.usepdfjs) {
                        thispage.pdfrenderopque += 1;
                        thispage.pdfisrendered = false;
                        if (thispage.DocRef.pages[thispage.DocRef.currentpage].pagenumber == thispage.pagenumber) {
                            thispage.foxitdirectZoom(thispage.dscalepdf);
                        }
                        //console.log('zoomheight invalidation');
                    }
                    redrawMarkup();
                    thispage.postscaleRefresh();

                    if (RxCore_GUI_ZoomUpdated != undefined && bexternal == undefined) {
                        var pagerect = thispage.getpagerect();
                        var zoomparams = { rect: pagerect };
                        RxCore_GUI_ZoomUpdated.zoomUpdate(zoomparams, 5);
                    }
                };
                this.mtzoom_update = function (diagdiff, startscalelarge, startscalesmall) {
                    //change to calculate change factor and maintain centre the same way as with zoom in and out.
                    //image
                    var curdiffh = (startscalelarge * this.MainImageHeight) - canvasoheight;
                    var currdiffw = (startscalelarge * this.MainImageWidth) - canvasowidth;
                    var mainimagediag = Math.sqrt((Math.pow(this.MainImageHeight, 2) + Math.pow(this.MainImageWidth, 2)));
                    var canvasdiag = Math.sqrt((Math.pow(canvasoheight, 2) + Math.pow(canvasowidth, 2)));
                    var curdiagdiff = (startscalelarge * mainimagediag) - canvasdiag;
                    this.dscale = (canvasdiag + curdiagdiff + diagdiff) / mainimagediag;
                    //vector
                    if (this.usevectorxml && thispage.VectorPageObj != undefined) {
                        curdiffh = (startscalesmall * thispage.VectorPageObj.height) - canvasoheight;
                        currdiffw = (startscalesmall * thispage.VectorPageObj.width) - canvasowidth;
                        var vectordiag = Math.sqrt((Math.pow(thispage.VectorPageObj.height, 2) + Math.pow(thispage.VectorPageObj.width, 2)));
                        curdiagdiff = (startscalesmall * vectordiag) - canvasdiag;
                        this.dscalevector = (canvasdiag + curdiagdiff + diagdiff) / vectordiag;
                    }
                    if (this.usepdfjs) {

                        var pcanvheight = thispage.offscreenheight;
                        var pcanvwidth = thispage.offscreenwidth;

                        if(thispage.getpageRotation() != 0){
                            pcanvwidth = thispage.originalPDFwidth;
                            pcanvheight = thispage.originalPDFheight;
                        }

                        /*if (thispage.usedincomposite && thispage.compositereference != undefined) {
                            //var pcanvheight = thispage.compositereference.ovpagecanvas.height;
                            //var pcanvwidth = thispage.compositereference.ovpagecanvas.width;
                            var pcanvheight = thispage.compositereference.overlaypage.getheight();
                            var pcanvwidth = thispage.compositereference.overlaypage.getwidth();
                        }
                        else {


                        }*/
                        curdiffh = (startscalesmall * pcanvheight) - canvasoheight;
                        currdiffw = (startscalesmall * pcanvwidth) - canvasowidth;
                        var pdfdiag = Math.sqrt((Math.pow(pcanvheight, 2) + Math.pow(pcanvheight, 2)));
                        curdiagdiff = (startscalesmall * pdfdiag) - canvasdiag;

                        //dscalepdf modified
                        this.dscalepdf = (canvasdiag + curdiagdiff + diagdiff) / pdfdiag;
                        thispage.pdfrenderopque += 1;
                        thispage.pdfisrendered = false;
                        if (thispage.DocRef.pages[thispage.DocRef.currentpage].pagenumber == thispage.pagenumber) {
                            thispage.foxitdirectZoom(thispage.dscalepdf);
                        }
                        //console.log('mtzoom_update invalidation');
                        //thispage.renderPDFpagescale();
                    }
                    if (this.usevector3Dxml) {
                        return;
                    }
                    if (!this.usevectorxml) {
                        if (this.usepdfjs) {
                            //DocObj.draw_mpagepdf();
                            thispage.setpagedimsize(this.dxpdf, this.dypdf, (pcanvwidth * this.dscalepdf), (pcanvheight * this.dscalepdf), false);
                            if (thispage.pagenumber == thispage.DocRef.pages[thispage.DocRef.currentpage].pagenumber) {
                                
                                thispage.DocRef.draw_mpagepdf(false);
                                /*if (this.usedincomposite && this.compositereference != undefined) {
                                    this.compositereference.scaleToBackground(true);
                                }
                                else {
                                    
                                }*/
                            }
                        }
                        else {
                            this.checkimageswitch();
                            if (thispage.DocRef.Type == 0) {
                                thispage.DocRef.draw_mpage();
                            }
                            else {
                                thispage.draw_image(true);
                                /*if (this.usedincomposite && this.compositereference != undefined) {
                                    this.compositereference.scaleToBackground(true);
                                }
                                else {
                                    
                                }*/
                            }
                        }
                    }
                    else {

                        if(thispage.VectorPageObj != undefined){
                            this.startx = this.dxvector;
                            this.starty = this.dyvector;
                            this.endx = (thispage.VectorPageObj.width * this.dscalevector) + this.startx;
                            this.endy = (thispage.VectorPageObj.height * this.dscalevector) + this.starty;
                            
                            thispage.draw_vector(false);
                            /*if (this.usedincomposite && this.compositereference != undefined) {
                                this.compositereference.scaleToBackground(true);
                            }
                            else {
                                
                            }*/
                            thispage.vectorischanged = true;
                        }

                    }
                    redrawMarkup();
                    //context.clearRect(0, 0, canvas.width, canvas.height);
                };
                this.getRotatedPoint = function (width, height, x, y, anglerad) {
                    var cosangle = Math.cos(anglerad);
                    var sinangle = Math.sin(anglerad);
                    var hw = x - width;
                    var hh = y - height;
                    var newx = (hw * cosangle) - (hh * sinangle);
                    var newy = (hw * sinangle) + (hh * cosangle);
                    var transpoint = new point(newx, newy);
                    transpoint.x = width + transpoint.x;
                    transpoint.y = height + transpoint.y;
                    return transpoint;
                };

                this.setpdfscaleNoOffset = function(dscale){
                    if(thispage.checkFoxitMaxZoom(dscale)){

                        //dscalepdf modified
                        this.dscalepdf = dscale;
                        //this.dxpdf = dx;
                        //this.dypdf = dy;
                    }

                };
                this.setpdfscaleoffset = function(dscale, dx, dy){
                    
                    if(thispage.checkFoxitMaxZoom(dscale)){

                        //dscalepdf modified
                        this.dscalepdf = dscale;
                        this.dxpdf = dx;
                        this.dypdf = dy;
                    }

                };

                this.zoom_update = function (sx, sy, sWide, sHi, bexternal) {

                    if (this.usevector3Dxml) {
                        return;
                    }

                    var pixelRatio = getPixelRatio();
                    var revpixelRatio = 1 / pixelRatio;

                    var rc = {x: sx, y: sy, w : sWide, h : sHi};

                    rc.x = rc.x * revpixelRatio;
                    rc.y = rc.y * revpixelRatio;
                    rc.w = rc.w * revpixelRatio;
                    rc.h = rc.h * revpixelRatio;

                    /*if(this.getpageRotation() != 0 && bexternal == undefined){
                        var rcrot = zoomrectrotated(rc, this.getpageRotation());
                    }else{
                        rcrot = rc;
                    }*/
                    
                    var rcrot = rc;


                    if (RxCore_GUI_ZoomUpdate != undefined && bexternal == undefined) {
                        var pagerect = thispage.getpagerect();
                        var zoomparams = { sx: sx, sy: sy, sWide: sWide, sHi: sHi, rect: pagerect };
                        RxCore_GUI_ZoomUpdate.zoomUpdate(zoomparams, 2);
                    }
                    var newscale = 1.0;
                    var newscaleext = 1.0;
                    var newscalev = 1.0;
                    var newscalepdf = 1.0;
                    var factor = 0.0;
                    var pointXtoCenter = 0;
                    var pointYtoCenter = 0;

                    var canvdiv2H = canvasoheight / 2;
                    var canvdiv2W = canvasowidth / 2;

                    canvdiv2H = canvasoheightsize / 2;
                    canvdiv2W = canvasowidthsize / 2;


                    var CanvRotRad = this.getpageRotation() * (Math.PI / 180);
                    //point to align to centre in display coordinates.
                    var rectCenterX = sx + (sWide / 2);
                    var rectCenterY = sy + (sHi / 2);

                    rectCenterX = rcrot.x + (rcrot.w / 2);
                    rectCenterY = rcrot.y + (rcrot.h / 2);


                    var rectCenternorot = {x: rectCenterX, y : rectCenterY };


                    var ox1pdf = sx / this.dscalepdf;
                    var oy1pdf = sy / this.dscalepdf;
                    var ox2pdf = (sx + sWide) / this.dscalepdf;
                    var oy2pdf = (sy + sHi) / this.dscalepdf;
                    var hscalepdf = sHi / this.dscalepdf;
                    var wscalepdf = sWide / this.dscalepdf;
                    var pdfoverlayx = 0;
                    var pdfoverlayy = 0;

                    ox1pdf = rcrot.x / this.dscalepdf;
                    oy1pdf = rcrot.y / this.dscalepdf;
                    ox2pdf = (rcrot.x + rcrot.w) / this.dscalepdf;
                    oy2pdf = (rcrot.y + rcrot.h) / this.dscalepdf;
                    hscalepdf = rcrot.h / this.dscalepdf;
                    wscalepdf = rcrot.w / this.dscalepdf;


                    if (this.usepdfjs) {

                        var pcanvheight = thispage.offscreenheight;
                        var pcanvwidth = thispage.offscreenwidth;

                        /*if (thispage.usedincomposite && thispage.compositereference != undefined) {
                            //var pcanvheight = thispage.compositereference.ovpagecanvas.height;
                            //var pcanvwidth = thispage.compositereference.ovpagecanvas.width;
                            var pcanvheight = thispage.compositereference.overlaypage.getheight();
                            var pcanvwidth = thispage.compositereference.overlaypage.getwidth();
                        }
                        else {

                            //if(thispage.getpageRotation() != 0){
                            //    pcanvwidth = thispage.originalPDFwidth;
                            //    pcanvheight = thispage.originalPDFheight;
                            //}

                        }*/
                        var zoomtopoint = thispage.getPDFcoords(0,0);

    
                        switch (this.getpageRotation()) {
                            case 90:
                                if (this.usepdfjs) {



                                    pointXtoCenter = rectCenterX - canvdiv2W;
                                    pointYtoCenter = rectCenterY - canvdiv2H;
                                    oy1pdf = (this.dypdf + rectCenterX) - canvdiv2W;
                                    ox1pdf = (this.dxpdf - rectCenterY) + canvdiv2H;
                                    //this.dxpdf = ox1pdf;
                                    //this.dypdf = oy1pdf;

                                    if (hscalepdf > 0 && wscalepdf > 0) {
                                        var yscale = this.dscalepdf * canvasoheight / sHi;
                                        var xscale = this.dscalepdf * canvasowidth / sWide;

                                        yscale = this.dscalepdf * canvasoheightsize / rc.h;
                                        xscale = this.dscalepdf * canvasowidthsize / rc.w;

                                    }

                                    newscalepdf = Math.min(yscale, xscale);
                                    factor = newscalepdf / this.dscalepdf;
                                    var prevcenterxpdf = (canvasowidth - (pcanvwidth * this.dscalepdf)) / 2;
                                    var prevcenterypdf = (canvasoheight - (pcanvheight * this.dscalepdf)) / 2;

                                    prevcenterxpdf = (canvasowidthsize - (pcanvwidth * this.dscalepdf)) / 2;
                                    prevcenterypdf = (canvasoheightsize - (pcanvheight * this.dscalepdf)) / 2;


                                    var diffxpdf = prevcenterxpdf - this.dxpdf;
                                    var diffypdf = prevcenterypdf - this.dypdf;

                                    

                                    //this.dscalepdf = newscalepdf;
                                    var centerxpdf = ((canvasowidth - (pcanvwidth * this.dscalepdf)) / 2);
                                    var centerypdf = ((canvasoheight - (pcanvheight * this.dscalepdf)) / 2);

                                    centerxpdf = ((canvasowidthsize - (pcanvwidth * this.dscalepdf)) / 2);
                                    centerypdf = ((canvasoheightsize - (pcanvheight * this.dscalepdf)) / 2);


                                    var rotcenter = mouse_rotated(rectCenterX,rectCenterY);

                                    //zoomtopoint = thispage.getPDFcoords(rotcenter.x,rotcenter.y);
                                    //zoomtopoint = thispage.getPDFcoords(rotcenter.x,rotcenter.y);
                                    zoomtopoint = thispage.getPDFCoordsRot(rectCenternorot);
                                    

                                    thispage.setpdfscaleNoOffset(newscalepdf);

                                    //var distx = thispage.getPDFDist((canvasowidth / 2));
                                    //var disty = thispage.getPDFDist((canvasoheight / 2));
    
                                    


                                    //this.dxpdf = centerxpdf - (diffxpdf * factor);
                                    //this.dypdf = centerypdf - (diffypdf * factor);

                                    pdfoverlayx = centerxpdf - (diffxpdf * factor);
                                    pdfoverlayy = centerypdf - (diffypdf * factor);
                

                                    //thispage.setpdfscaleoffset(newscalepdf, centerxpdf - (diffxpdf * factor), centerypdf - (diffypdf * factor));
                                }
                                break;
                            case 270:
                                if (this.usepdfjs) {


                                    /*if(thispage.bpagesizeupdated){
                                        xscale = canvwidth / pcanvwidth;
                                    }else{
                                        xscale = canvwidth / pcanvheight;
                                    }*/



                                    pointXtoCenter = rectCenterX - canvdiv2W;
                                    pointYtoCenter = rectCenterY - canvdiv2H;

                                    //in this case use non rotated dx and dy
                                    //thispage.dypdfnonrot = thispage.foxitdy;
                                    //thispage.dxpdfnonrot = thispage.foxitdx;

                                    //oy1pdf = (this.dypdf - rectCenterX) + canvdiv2W;
                                    //ox1pdf = (this.dxpdf + rectCenterY) - canvdiv2H;


                                    ox1pdf = -((rectCenterX - thispage.dxpdfnonrot) / this.dscalepdf);
                                    oy1pdf = -((rectCenterY - thispage.dypdfnonrot) / this.dscalepdf);
    
            

                                    //we can not set the rotated dx and dy based on calculations using non rotated offsets.

                                    //this.dxpdf = ox1pdf;
                                    //this.dypdf = oy1pdf;

                                    //thispage.dxpdfnonrot = ox1pdf;
                                    //thispage.dypdfnonrot = oy1pdf; 


                                    if (hscalepdf > 0 && wscalepdf > 0) {
                                        yscale = this.dscalepdf * canvasoheight / sHi;
                                        xscale = this.dscalepdf * canvasowidth / sWide;

                                        yscale = this.dscalepdf * canvasoheightsize / rc.h;
                                        xscale = this.dscalepdf * canvasowidthsize / rc.w;

                                    }

                                    newscalepdf = Math.min(yscale, xscale);
                                    factor = newscalepdf / this.dscalepdf;
                                    
                                    if(thispage.bpagesizeupdated){
                                        prevcenterxpdf = (canvasowidth - (pcanvwidth * this.dscalepdf)) / 2;
                                        prevcenterypdf = (canvasoheight - (pcanvheight * this.dscalepdf)) / 2;

                                        prevcenterxpdf = (canvasowidthsize - (pcanvwidth * this.dscalepdf)) / 2;
                                        prevcenterypdf = (canvasoheightsize - (pcanvheight * this.dscalepdf)) / 2;
    

                                    }else{
                                        prevcenterxpdf = (canvasowidth - (pcanvheight * this.dscalepdf)) / 2;
                                        prevcenterypdf = (canvasoheight - (pcanvwidth * this.dscalepdf)) / 2;

                                        prevcenterxpdf = (canvasowidthsize - (pcanvheight * this.dscalepdf)) / 2;
                                        prevcenterypdf = (canvasoheightsize - (pcanvwidth * this.dscalepdf)) / 2;
    

                                    }
                                    

                                    //in this case use non rotated dx and dy

                                    diffxpdf = prevcenterxpdf - this.dxpdf;
                                    diffypdf = prevcenterypdf - this.dypdf;
                                    //this.dscalepdf = newscalepdf;

                                    if(thispage.bpagesizeupdated){
                                        centerxpdf = ((canvasowidth - (pcanvwidth * this.dscalepdf)) / 2);
                                        centerypdf = ((canvasoheight - (pcanvheight * this.dscalepdf)) / 2);

                                        centerxpdf = ((canvasowidthsize - (pcanvwidth * this.dscalepdf)) / 2);
                                        centerypdf = ((canvasoheightsize - (pcanvheight * this.dscalepdf)) / 2);
    

                                    }else{
                                        centerxpdf = ((canvasowidth - (pcanvheight * this.dscalepdf)) / 2);
                                        centerypdf = ((canvasoheight - (pcanvwidth * this.dscalepdf)) / 2);

                                        centerxpdf = ((canvasowidthsize - (pcanvheight * this.dscalepdf)) / 2);
                                        centerypdf = ((canvasoheightsize - (pcanvwidth * this.dscalepdf)) / 2);
    
                                    }

                                    var rotcenter = mouse_rotated(rectCenterX,rectCenterY);

                                    //zoomtopoint = thispage.getPDFcoords(rotcenter.x,rotcenter.y);
                                    zoomtopoint = thispage.getPDFCoordsRot(rectCenternorot);

                                    thispage.setpdfscaleNoOffset(newscalepdf);

                                    //var distx = thispage.getPDFDist((canvasowidth / 2));
                                    //var disty = thispage.getPDFDist((canvasoheight / 2));
    
                                    

                                    //this.dxpdf = centerxpdf - (diffxpdf * factor);
                                    //this.dypdf = centerypdf - (diffypdf * factor);

                                    pdfoverlayx = centerxpdf - (diffxpdf * factor);
                                    pdfoverlayy = centerypdf - (diffypdf * factor);


                                    //thispage.setpdfscaleoffset(newscalepdf, centerxpdf - (diffxpdf * factor), centerypdf - (diffypdf * factor));

                                    
                                }
                                break;
                            case 180:
                                if (this.usepdfjs) {
                                    pointXtoCenter = rectCenterX - canvdiv2W;
                                    pointYtoCenter = rectCenterY - canvdiv2H;
                                    oy1pdf = (this.dypdf + rectCenterY) - canvdiv2H;
                                    ox1pdf = (this.dxpdf + rectCenterX) - canvdiv2W;
                                    
                                    //this.dxpdf = ox1pdf;
                                    //this.dypdf = oy1pdf;
                                    
                                    
                                    if (hscalepdf > 0 && wscalepdf > 0) {
                                        yscale = this.dscalepdf * canvasoheight / sHi;
                                        xscale = this.dscalepdf * canvasowidth / sWide;

                                        yscale = this.dscalepdf * canvasoheightsize / rc.h;
                                        xscale = this.dscalepdf * canvasowidthsize / rc.w;

                                    }

                                    newscalepdf = Math.min(yscale, xscale);
                                    
                                    factor = newscalepdf / this.dscalepdf;


                                    prevcenterxpdf = (canvasowidth - (pcanvwidth * this.dscalepdf)) / 2;
                                    prevcenterypdf = (canvasoheight - (pcanvheight * this.dscalepdf)) / 2;

                                    prevcenterxpdf = (canvasowidthsize - (pcanvwidth * this.dscalepdf)) / 2;
                                    prevcenterypdf = (canvasoheightsize - (pcanvheight * this.dscalepdf)) / 2;


                                    diffxpdf = prevcenterxpdf - this.dxpdf;
                                    diffypdf = prevcenterypdf - this.dypdf;
                                    //this.dscalepdf = newscalepdf;
                                    centerxpdf = ((canvasowidth - (pcanvwidth * this.dscalepdf)) / 2);
                                    centerypdf = ((canvasoheight - (pcanvheight * this.dscalepdf)) / 2);

                                    centerxpdf = ((canvasowidthsize - (pcanvwidth * this.dscalepdf)) / 2);
                                    centerypdf = ((canvasoheightsize - (pcanvheight * this.dscalepdf)) / 2);



                                    var rotcenter = mouse_rotated(rectCenterX,rectCenterY);

                                    //zoomtopoint = thispage.getPDFcoords(rotcenter.x,rotcenter.y);
                                    zoomtopoint = thispage.getPDFCoordsRot(rectCenternorot);

                                    thispage.setpdfscaleNoOffset(newscalepdf);

                                    //var distx = thispage.getPDFDist((canvasowidth / 2));
                                    //var disty = thispage.getPDFDist((canvasoheight / 2));
    
                                    
    

                                    //this.dxpdf = centerxpdf - (diffxpdf * factor);
                                    //this.dypdf = centerypdf - (diffypdf * factor);

                                    pdfoverlayx = centerxpdf - (diffxpdf * factor);
                                    pdfoverlayy = centerypdf - (diffypdf * factor);


                                    //thispage.setpdfscaleoffset(newscalepdf, centerxpdf - (diffxpdf * factor), centerypdf - (diffypdf * factor));
                                }
                                else {
                                    //move point to center
                                    pointXtoCenter = rectCenterX - canvdiv2W;
                                    pointYtoCenter = rectCenterY - canvdiv2H;
                                    ox1pdf = (this.dypdf + pointYtoCenter);
                                    oy1pdf = (this.dxpdf + pointXtoCenter);
                                    this.dxpdf = ox1pdf;
                                    this.dypdf = oy1pdf;

                                    pdfoverlayx = ox1pdf;
                                    pdfoverlayy = oy1pdf;


                                    if (hscalepdf > 0 && wscalepdf > 0) {
                                        yscale = this.dscalepdf * canvasoheight / sHi;
                                        xscale = this.dscalepdf * canvasowidth / sWide;

                                        yscale = this.dscalepdf * canvasoheightsize / rc.h;
                                        xscale = this.dscalepdf * canvasowidthsize / rc.w;

                                    }
                                    newscalepdf = Math.min(yscale, xscale);
                                    if (this.usepdfjs) {
                                        factor = newscalepdf / this.dscalepdf;
                                    }
                                    this.ZoomIn(factor, false, false);
                                }
                                break;
                            case 0:
                                //offset in current scale
                                ox1pdf = -((rectCenterX - this.dxpdf) / this.dscalepdf);
                                oy1pdf = -((rectCenterY - this.dypdf) / this.dscalepdf);



                                if (hscalepdf > 0 && wscalepdf > 0) {
                                    yscale = this.dscalepdf * canvasoheight / sHi;
                                    xscale = this.dscalepdf * canvasowidth / sWide;

                                    yscale = this.dscalepdf * canvasoheightsize / rcrot.h;
                                    xscale = this.dscalepdf * canvasowidthsize / rcrot.w;

                                }

                                var tempscalepdf = Math.min(yscale, xscale);
                                //var tempdxpdf = (ox1pdf * tempscalepdf) + (canvasowidth / 2);
                                //var tempdypdf = (oy1pdf * tempscalepdf) + (canvasoheight / 2);

                                var tempdxpdf = (rectCenterX - this.dxpdf) + (canvasowidth / 2);
                                var tempdypdf = (rectCenterY - this.dypdf) + (canvasoheight / 2);

                                tempdxpdf = (rectCenterX - this.dxpdf) + (canvasowidthsize / 2);
                                tempdypdf = (rectCenterY - this.dypdf) + (canvasoheightsize / 2);



                                //var centerpoint = thispage.getPDFcoords(canvasowidth / 2,canvasoheight / 2);

                                
                                //this.dscalepdf = Math.min(yscale, xscale);
                                //this.dxpdf = (ox1pdf * this.dscalepdf) + (canvasowidth / 2);
                                //this.dypdf = (oy1pdf * this.dscalepdf) + (canvasoheight / 2);

                                pdfoverlayx = (ox1pdf * tempscalepdf) + (canvasowidth / 2);
                                pdfoverlayy = (oy1pdf * tempscalepdf) + (canvasoheight / 2);

                                pdfoverlayx = (ox1pdf * tempscalepdf) + (canvasowidthsize / 2);
                                pdfoverlayy = (oy1pdf * tempscalepdf) + (canvasoheightsize / 2);


                                //zoomtopoint.x = tempdxpdf;
                                //zoomtopoint.y = tempdypdf;

                                //zoomtopoint = thispage.getPDFcoords(rectCenterX,rectCenterY);
                                zoomtopoint = thispage.getPDFCoordsRot(rectCenternorot);

                                thispage.setpdfscaleNoOffset(tempscalepdf);


                                //console.log(zoomtopoint.x  - distx);

                                //thispage.setpdfscaleoffset(tempscalepdf, tempdxpdf, tempdypdf);
                                //var screencenter = thispage.getPDFcoords(canvasowidth * 0.5,canvasoheight * 0.5);
                                
                                /*if(thispage.foxitpageorigrot == 0){
                                    zoomtopoint.x = zoomtopoint.x - distx;
                                    zoomtopoint.y = zoomtopoint.y + disty;
                                }else if(thispage.foxitpageorigrot == 90){
                                    zoomtopoint.x = zoomtopoint.x - disty;
                                    zoomtopoint.y = zoomtopoint.y - distx;

                                }else if(thispage.foxitpageorigrot == 180){
                                    zoomtopoint.x = zoomtopoint.x + distx;
                                    zoomtopoint.y = zoomtopoint.y - disty;

                                }else if(thispage.foxitpageorigrot == 270){
                                    zoomtopoint.x = zoomtopoint.x + disty;
                                    zoomtopoint.y = zoomtopoint.y + distx;

                                }*/



                                break;
                        }

                        var realangle = getcombinedangle(thispage.getpageRotation(), thispage.foxitpageorigrot);

                        var distx = thispage.getPDFDist((canvasowidth / 2));
                        var disty = thispage.getPDFDist((canvasoheight / 2));


                        distx = thispage.getPDFDist((canvasowidthsize / 2));
                        disty = thispage.getPDFDist((canvasoheightsize / 2));



                        switch (realangle){
                            case 0 :
                                zoomtopoint.x = zoomtopoint.x - distx;
                                zoomtopoint.y = zoomtopoint.y + disty;
                                break;
                            case 90 :
                                zoomtopoint.x = zoomtopoint.x - disty;
                                zoomtopoint.y = zoomtopoint.y - distx;

                                break;
                            case 180 :
                                zoomtopoint.x = zoomtopoint.x + distx;
                                zoomtopoint.y = zoomtopoint.y - disty;

                                break;
                            case 270 :

                                zoomtopoint.x = zoomtopoint.x + disty;
                                zoomtopoint.y = zoomtopoint.y + distx;

                            break;

                        }

                    }
                    else {
                        var stats = getPagestats();
                        var scaleoffset = ScaleZoomUpdate(stats, sx, sy, sWide, sHi);
                        
                        if (thispage.getpageRotation() == 0) {
                            setPagestats(scaleoffset, false);
                        }

                        
                    }
                    //context.clearRect(0, 0, canvas.width, canvas.height);

                    if (this.usepdfjs) {
  
                        //test remove page size update to check markup position update when using rotation

                        if(thispage.getpageRotation() == 0){
                            thispage.setpagedimsize(this.dxpdf, this.dypdf, (pcanvwidth * this.dscalepdf), (pcanvheight * this.dscalepdf), false);
                        }
                        

                        if (thispage.pagenumber == thispage.DocRef.pages[thispage.DocRef.currentpage].pagenumber) {
                            
                            //not to be used with foxit
                            //thispage.DocRef.zoom_update(sx, sy, sWide, sHi);
                            thispage.DocRef.draw_mpagepdf(false);

                            
                        }
                    }
                    if (this.usepdfjs) {

                            thispage.pdfrenderopque += 1;
                            thispage.pdfisrendered = false;
                            if (thispage.DocRef.pages[thispage.DocRef.currentpage].pagenumber == thispage.pagenumber) {
                                //thispage.foxitdirectZoom(thispage.dscalepdf);
    
                                thispage.foxitdirectZoomToEx(thispage.dscalepdf, zoomtopoint);
    
    
                            }
                        /*if (this.usedincomposite && this.compositereference != undefined) {
                             //do not perform foxit zoom   
                        }else{
                            
    
                        }*/
                        //console.log('zoom_update invalidation');
                    }

                    redrawMarkup();
                    
                    if (RxCore_GUI_ZoomUpdated != undefined && bexternal == undefined) {
                        var pagerect = thispage.getpagerect();
                        var zoomparams = { sx: sx, sy: sy, sWide: sWide, sHi: sHi, rect: pagerect };
                        RxCore_GUI_ZoomUpdated.zoomUpdate(zoomparams, 2);
                    }
                };
                this.MarkupFindText = function(SearchExpr){

                    var searchmarkuplist = [];

                    for(var mi = 0; mi < thispage.DocRef.markuplist.length; mi++){
                        if (thispage.DocRef.markuplist[mi].FindText(SearchExpr)) {
                            searchmarkuplist.push(thispage.DocRef.markuplist[mi]);
                        }
                    }

                    return searchmarkuplist;
                };

                this.getMarkupPos = function(markup){

                    var hscaled = markup.hscaled;
                    var wscaled = markup.wscaled;
                    var xscaled = markup.xscaled;
                    var yscaled = markup.yscaled;

                    if(markup.bisTextArrow && markup.textBoxConnected != null){

                        hscaled = Math.max(markup.hscaled, markup.textBoxConnected.hscaled);
                        wscaled = Math.max(markup.wscaled, markup.textBoxConnected.wscaled);
                        xscaled = Math.min(markup.xscaled, markup.textBoxConnected.xscaled);
                        yscaled = Math.min(markup.yscaled, markup.textBoxConnected.yscaled);
    

                    }

                    
                    var npage = markup.pagenumber;

                    switch (this.getpageRotation()) {
                        case 0:
                            switch (markup.type) {
                                case 0:
                                    wscaled = wscaled - xscaled;
                                    hscaled = hscaled - yscaled;
                                    break;
                                case 1:
                                    wscaled = wscaled - xscaled;
                                    hscaled = hscaled - yscaled;
                                    break;
                                case 2:
                                    wscaled = wscaled - xscaled;
                                    hscaled = hscaled - yscaled;
                                    break;
                                case 6:
                                    wscaled = wscaled - xscaled;
                                    hscaled = hscaled - yscaled;
                                    break;
                                case 7:
                                    wscaled = wscaled - xscaled;
                                    hscaled = hscaled - yscaled;
                                    break;
                                case 8:
                                    wscaled = wscaled - xscaled;
                                    hscaled = hscaled - yscaled;
                                    break;
                                case 9:
                                    //yscaled = yscaled - hscaled;
                                    break;

                                case 13:
                                    wscaled = wscaled - xscaled;
                                    hscaled = hscaled - yscaled;
                                    break;

                                default:
                                    break;
                            }
                            break;
                        case 90:
                            //all values are absolute for all markup types.
                            hscaled = markup.rotatedrect.h;
                            wscaled = markup.rotatedrect.w;
                            xscaled = markup.rotatedrect.x;
                            yscaled = markup.rotatedrect.y;
                            //switch rect values to comply with orientation
                            //switch x and w
                            var wxtemp = xscaled;
                            xscaled = wscaled;
                            wscaled = wxtemp;
                            //change absolute rect values to relative
                            wscaled = wscaled - xscaled;
                            hscaled = hscaled - yscaled;
                            break;
                        case 180:
                            //all values are absolute for all markup types.
                            hscaled = markup.rotatedrect.h;
                            wscaled = markup.rotatedrect.w;
                            xscaled = markup.rotatedrect.x;
                            yscaled = markup.rotatedrect.y;
                            //switch rect values to comply with orientation
                            //both x,w and y,h is switched
                            var xtemp = xscaled;
                            var ytemp = yscaled;
                            xscaled = wscaled;
                            yscaled = hscaled;
                            wscaled = xtemp;
                            hscaled = ytemp;
                            //change absolute rect values to relative
                            wscaled = wscaled - xscaled;
                            hscaled = hscaled - yscaled;
                            break;
                        case 270:
                            //all values are absolute for all markup types.
                            hscaled = markup.rotatedrect.h;
                            wscaled = markup.rotatedrect.w;
                            xscaled = markup.rotatedrect.x;
                            yscaled = markup.rotatedrect.y;
                            //switch rect values to comply with orientation
                            var wytemp = yscaled;
                            yscaled = hscaled;
                            hscaled = wytemp;
                            //yscaled -= wscaled;
                            //var hwtemp = wscaled;
                            //wscaled = hscaled;
                            //hscaled = hwtemp;
                            //change absolute rect values to relative
                            wscaled = wscaled - xscaled;
                            hscaled = hscaled - yscaled;
                            break;
                    }

                    return {x : xscaled, y : yscaled, w : wscaled, h : hscaled};

                };                

                this.panToMarkup = function(markup){

                        //var divpos = thispage.getdivpos(counter);

                        if (markup.selected || markup.selectedit) {
                            markup.drawmescaled(context);
                        }else{
                            markup.drawmescaled(cntximg);
                        }



                        var mpos = thispage.getMarkupPos(markup);

                        thispage.pan_update(0, 0);
                        //console.log(divpos);
                        //var visible = this.istextvisible(divpos.x, divpos.y);
                        /*
                        0 = right
                        1 = left
                        2 = up
                        3 = down
                       */
                        var centery = canvasoheight / 2;
                        var centerx = canvasowidth / 2;
                        var panx = 0;
                        var pany = 0;
                        var pagerect = thispage.getpagerect();

                        var mcenterx = (mpos.x + (mpos.w * 0.5));
                        var mcentery = (mpos.y + (mpos.h * 0.5));



                        if (thispage.getpageRotation() != 0) {
                            var rotpos = thispage.getrotcoords(centerx, centery, mcenterx, mcentery, -thispage.getpageRotation());
                            //divpos.x = rotpos.x;
                            //divpos.y = rotpos.y;
                            distancexright = Math.max(rotpos.x, canvasowidth) - Math.min(rotpos.x, canvasowidth);
                            if (Math.max(rotpos.x, canvasowidth) == rotpos.x) {
                                move = 1;
                                //panx =  distancexright + 300;
                                panx = rotpos.x - centerx;
                            }
                            else if (distancexright < 300) {
                                move = 1;
                                panx = rotpos.x - centerx;
                            }
                            distanceybottom = Math.max(rotpos.y, canvasoheight) - Math.min(rotpos.y, canvasoheight);
                            if (Math.max(rotpos.y, canvasoheight) == rotpos.y) {
                                move = 2;
                                pany = rotpos.y - centery;
                            }
                            else if (distanceybottom < 100) {
                                move = 2;
                                pany = rotpos.y - centery;
                            }
                            distancexleft = Math.max(rotpos.x, 0) - Math.min(rotpos.x, 0);
                            if (Math.min(rotpos.x, 0) == rotpos.x) {
                                move = 0;
                                panx = -(centerx - rotpos.x);
                            }
                            else if (distancexleft < 100) {
                                move = 0;
                                panx = -(centerx - rotpos.x);
                            }
                            distanceytop = Math.max(rotpos.y, 0) - Math.min(rotpos.y, 0);
                            if (Math.min(rotpos.y, 0) == rotpos.y) {
                                move = 3;
                                pany = -(centery - rotpos.y);
                            }
                            else if (distanceytop < 100) {
                                move = 3;
                                pany = -(centery - rotpos.y);
                            }
                            /*switch (thispage.getpageRotation()) {
                                case 90:
                                    //this.dxvector -= sy;
                                    //this.dyvector += sx;
                                    thispage.pan_update(pany,-panx);
        
                                    break;
                                case 180:
                                    //this.dxvector += sx;
                                    //this.dyvector += sy;
                                    thispage.pan_update(-panx,-pany);
                                    break;
                                case 270:
                                    //this.dxvector += sy;
                                    //this.dyvector -= sx;
                                    thispage.pan_update(-pany,panx);
                                    break;
                            }*/
                            thispage.pan_update(panx, pany);
                        }
                        else {
                            var distancexright = Math.max(mcenterx, canvasowidth) - Math.min(mcenterx, canvasowidth);
                            if (Math.max(mcenterx, canvasowidth) == mcenterx) {
                                move = 1;
                                //panx =  distancexright + 300;
                                panx = mcenterx - centerx;
                            }
                            else if (distancexright < 300) {
                                move = 1;
                                panx = mcenterx - centerx;
                            }
                            var distanceybottom = Math.max(mcentery, canvasoheight) - Math.min(mcentery, canvasoheight);
                            if (Math.max(mcentery, canvasoheight) == mcentery) {
                                move = 2;
                                pany = mcentery - centery;
                            }
                            else if (distanceybottom < 100) {
                                move = 2;
                                pany = mcentery - centery;
                            }
                            var distancexleft = Math.max(mcenterx, 0) - Math.min(mcenterx, 0);
                            if (Math.min(mcenterx, 0) == mcenterx) {
                                var move = 0;
                                panx = -(centerx - mcenterx);
                                //var panx = distancexleft - 200;
                            }
                            else if (distancexleft < 100) {
                                move = 0;
                                panx = -(centerx - mcenterx);
                            }
                            var distanceytop = Math.max(mcentery, 0) - Math.min(mcentery, 0);
                            if (Math.min(mcentery, 0) == mcentery) {
                                move = 3;
                                pany = -(centery - mcentery);
                            }
                            else if (distanceytop < 100) {
                                move = 3;
                                pany = -(centery - mcentery);
                            }
                            if (pagerect.w <= canvasowidth && thispage.usepdfjs) {
                                //thispage.pan_update(0,pany);
                                panx = 0;
                            }
                            /*if (thispage.pagenumber == 0 && thispage.usepdfjs) {
                                if (pany < 0) {
                                    pany = 0;
                                }
                            }*/
                            thispage.pan_update(panx, pany);
                        }

                };

                /*this.panToMarkup = function(markup){

                    var hscaled = markup.hscaled;
                    var wscaled = markup.wscaled;
                    var xscaled = markup.xscaled;
                    var yscaled = markup.yscaled;
                    
                    var npage = markup.pagenumber;

                    switch (this.getpageRotation()) {
                        case 0:
                            switch (markup.type) {
                                case 0:
                                    wscaled = wscaled - xscaled;
                                    hscaled = hscaled - yscaled;
                                    break;
                                case 1:
                                    wscaled = wscaled - xscaled;
                                    hscaled = hscaled - yscaled;
                                    break;
                                case 2:
                                    wscaled = wscaled - xscaled;
                                    hscaled = hscaled - yscaled;
                                    break;
                                case 6:
                                    wscaled = wscaled - xscaled;
                                    hscaled = hscaled - yscaled;
                                    break;
                                case 7:
                                    wscaled = wscaled - xscaled;
                                    hscaled = hscaled - yscaled;
                                    break;
                                case 8:
                                    wscaled = wscaled - xscaled;
                                    hscaled = hscaled - yscaled;
                                    break;
                                case 9:
                                    //yscaled = yscaled - hscaled;
                                    break;
                                default:
                                    break;
                            }
                            break;
                        case 90:
                            //all values are absolute for all markup types.
                            hscaled = markup.rotatedrect.h;
                            wscaled = markup.rotatedrect.w;
                            xscaled = markup.rotatedrect.x;
                            yscaled = markup.rotatedrect.y;
                            //switch rect values to comply with orientation
                            //switch x and w
                            var wxtemp = xscaled;
                            xscaled = wscaled;
                            wscaled = wxtemp;
                            //change absolute rect values to relative
                            wscaled = wscaled - xscaled;
                            hscaled = hscaled - yscaled;
                            break;
                        case 180:
                            //all values are absolute for all markup types.
                            hscaled = markup.rotatedrect.h;
                            wscaled = markup.rotatedrect.w;
                            xscaled = markup.rotatedrect.x;
                            yscaled = markup.rotatedrect.y;
                            //switch rect values to comply with orientation
                            //both x,w and y,h is switched
                            var xtemp = xscaled;
                            var ytemp = yscaled;
                            xscaled = wscaled;
                            yscaled = hscaled;
                            wscaled = xtemp;
                            hscaled = ytemp;
                            //change absolute rect values to relative
                            wscaled = wscaled - xscaled;
                            hscaled = hscaled - yscaled;
                            break;
                        case 270:
                            //all values are absolute for all markup types.
                            hscaled = markup.rotatedrect.h;
                            wscaled = markup.rotatedrect.w;
                            xscaled = markup.rotatedrect.x;
                            yscaled = markup.rotatedrect.y;
                            //switch rect values to comply with orientation
                            var wytemp = yscaled;
                            yscaled = hscaled;
                            hscaled = wytemp;
                            //yscaled -= wscaled;
                            //var hwtemp = wscaled;
                            //wscaled = hscaled;
                            //hscaled = hwtemp;
                            //change absolute rect values to relative
                            wscaled = wscaled - xscaled;
                            hscaled = hscaled - yscaled;
                            break;
                    }

                    var panto = thispage.getsearchpanupdate(xscaled, yscaled);
                    thispage.pan_update(panto.x, panto.y);


                };*/

                this.zoomToMarkup = function(markup, padding){

                    this.zoomall();
                            
                    var hscaled = markup.hscaled;
                    var wscaled = markup.wscaled;
                    var xscaled = markup.xscaled;
                    var yscaled = markup.yscaled;
                    
                    var npage = markup.pagenumber;

                    switch (this.getpageRotation()) {
                        case 0:
                            switch (markup.type) {
                                case 0:
                                    wscaled = Math.abs(wscaled - xscaled);
                                    hscaled = Math.abs(hscaled - yscaled);
                                    break;
                                case 1:
                                    wscaled = Math.abs(wscaled - xscaled);
                                    hscaled = Math.abs(hscaled - yscaled);
                                    break;
                                case 2:
                                    wscaled = Math.abs(wscaled - xscaled);
                                    hscaled = Math.abs(hscaled - yscaled);
                                    break;
                                case 6:
                                    wscaled = Math.abs(wscaled - xscaled);
                                    hscaled = Math.abs(hscaled - yscaled);
                                    if(wscaled == 0){
                                        wscaled = 1;
                                    }
                                    if(hscaled == 0){
                                        hscaled = 1;
                                    }

                                    break;
                                case 7:
                                    wscaled = Math.abs(wscaled - xscaled);
                                    hscaled = Math.abs(hscaled - yscaled);

                                    if(wscaled == 0){
                                        wscaled = 1;
                                    }
                                    if(hscaled == 0){
                                        hscaled = 1;
                                    }


                                    break;
                                case 8:
                                    wscaled = Math.abs(wscaled - xscaled);
                                    hscaled = Math.abs(hscaled - yscaled);
                                    break;
                                case 9:
                                    //yscaled = yscaled - hscaled;
                                    break;
                                default:
                                    break;
                            }
                            break;
                        case 90:
                            //all values are absolute for all markup types.
                            hscaled = markup.rotatedrect.h;
                            wscaled = markup.rotatedrect.w;
                            xscaled = markup.rotatedrect.x;
                            yscaled = markup.rotatedrect.y;
                            //switch rect values to comply with orientation
                            //switch x and w
                            var wxtemp = xscaled;
                            xscaled = wscaled;
                            wscaled = wxtemp;
                            //change absolute rect values to relative
                            wscaled = wscaled - xscaled;
                            hscaled = hscaled - yscaled;
                            break;
                        case 180:
                            //all values are absolute for all markup types.
                            hscaled = markup.rotatedrect.h;
                            wscaled = markup.rotatedrect.w;
                            xscaled = markup.rotatedrect.x;
                            yscaled = markup.rotatedrect.y;
                            //switch rect values to comply with orientation
                            //both x,w and y,h is switched
                            var xtemp = xscaled;
                            var ytemp = yscaled;
                            xscaled = wscaled;
                            yscaled = hscaled;
                            wscaled = xtemp;
                            hscaled = ytemp;
                            //change absolute rect values to relative
                            wscaled = wscaled - xscaled;
                            hscaled = hscaled - yscaled;
                            break;
                        case 270:
                            //all values are absolute for all markup types.
                            hscaled = markup.rotatedrect.h;
                            wscaled = markup.rotatedrect.w;
                            xscaled = markup.rotatedrect.x;
                            yscaled = markup.rotatedrect.y;
                            //switch rect values to comply with orientation
                            var wytemp = yscaled;
                            yscaled = hscaled;
                            hscaled = wytemp;
                            //yscaled -= wscaled;
                            //var hwtemp = wscaled;
                            //wscaled = hscaled;
                            //hscaled = hwtemp;
                            //change absolute rect values to relative
                            wscaled = wscaled - xscaled;
                            hscaled = hscaled - yscaled;
                            break;
                    }

                    if(padding){

                        //p*k = 100/a = padding.h*hscaled = 100/a
                        //10 = 10 * 1000/100
                        //a = (padding.h * hscaled) / 100
                        //hscaled = hscaled + padding.h;

                        hscaled = hscaled + ((padding.h * hscaled) / 100);
                        wscaled = wscaled + ((padding.w * wscaled) / 100);
                        xscaled = xscaled - ((padding.x * wscaled) / 100);
                        yscaled = yscaled - ((padding.y * hscaled) / 100);

                    }else{
                        hscaled = hscaled + 100;
                        wscaled = wscaled + 50;
                        xscaled = xscaled - 50;
                        yscaled = yscaled - 50;
                    }

                    this.zoom_update(xscaled, yscaled, wscaled, hscaled);

                };

                this.MarkupFind = function (SearchExpr) {
                    var textfound = false;
                    while (this.CurrentMarkup < thispage.DocRef.nummarkups && !textfound) {
                        if (thispage.DocRef.markuplist[this.CurrentMarkup].FindText(SearchExpr)) {
                            textfound = true;
                            this.zoomall();
                            //console.log('zoom all');
                            var hscaled = thispage.DocRef.markuplist[this.CurrentMarkup].hscaled;
                            var wscaled = thispage.DocRef.markuplist[this.CurrentMarkup].wscaled;
                            var xscaled = thispage.DocRef.markuplist[this.CurrentMarkup].xscaled;
                            var yscaled = thispage.DocRef.markuplist[this.CurrentMarkup].yscaled;
                            var npage = thispage.DocRef.markuplist[this.CurrentMarkup].pagenumber;
                            switch (this.getpageRotation()) {
                                case 0:
                                    switch (thispage.DocRef.markuplist[this.CurrentMarkup].type) {
                                        case 0:
                                            wscaled = wscaled - xscaled;
                                            hscaled = hscaled - yscaled;
                                            break;
                                        case 1:
                                            wscaled = wscaled - xscaled;
                                            hscaled = hscaled - yscaled;
                                            break;
                                        case 2:
                                            wscaled = wscaled - xscaled;
                                            hscaled = hscaled - yscaled;
                                            break;
                                        case 6:
                                            wscaled = wscaled - xscaled;
                                            hscaled = hscaled - yscaled;
                                            break;
                                        case 7:
                                            wscaled = wscaled - xscaled;
                                            hscaled = hscaled - yscaled;
                                            break;
                                        case 8:
                                            wscaled = wscaled - xscaled;
                                            hscaled = hscaled - yscaled;
                                            break;
                                        case 9:
                                            yscaled = yscaled - hscaled;
                                            break;
                                        default:
                                            break;
                                    }
                                    break;
                                case 90:
                                    //all values are absolute for all markup types.
                                    hscaled = thispage.DocRef.markuplist[this.CurrentMarkup].rotatedrect.h;
                                    wscaled = thispage.DocRef.markuplist[this.CurrentMarkup].rotatedrect.w;
                                    xscaled = thispage.DocRef.markuplist[this.CurrentMarkup].rotatedrect.x;
                                    yscaled = thispage.DocRef.markuplist[this.CurrentMarkup].rotatedrect.y;
                                    //switch rect values to comply with orientation
                                    //switch x and w
                                    var wxtemp = xscaled;
                                    xscaled = wscaled;
                                    wscaled = wxtemp;
                                    //change absolute rect values to relative
                                    wscaled = wscaled - xscaled;
                                    hscaled = hscaled - yscaled;
                                    break;
                                case 180:
                                    //all values are absolute for all markup types.
                                    hscaled = thispage.DocRef.markuplist[this.CurrentMarkup].rotatedrect.h;
                                    wscaled = thispage.DocRef.markuplist[this.CurrentMarkup].rotatedrect.w;
                                    xscaled = thispage.DocRef.markuplist[this.CurrentMarkup].rotatedrect.x;
                                    yscaled = thispage.DocRef.markuplist[this.CurrentMarkup].rotatedrect.y;
                                    //switch rect values to comply with orientation
                                    //both x,w and y,h is switched
                                    var xtemp = xscaled;
                                    var ytemp = yscaled;
                                    xscaled = wscaled;
                                    yscaled = hscaled;
                                    wscaled = xtemp;
                                    hscaled = ytemp;
                                    //change absolute rect values to relative
                                    wscaled = wscaled - xscaled;
                                    hscaled = hscaled - yscaled;
                                    break;
                                case 270:
                                    //all values are absolute for all markup types.
                                    hscaled = thispage.DocRef.markuplist[this.CurrentMarkup].rotatedrect.h;
                                    wscaled = thispage.DocRef.markuplist[this.CurrentMarkup].rotatedrect.w;
                                    xscaled = thispage.DocRef.markuplist[this.CurrentMarkup].rotatedrect.x;
                                    yscaled = thispage.DocRef.markuplist[this.CurrentMarkup].rotatedrect.y;
                                    //switch rect values to comply with orientation
                                    var wytemp = yscaled;
                                    yscaled = hscaled;
                                    hscaled = wytemp;
                                    //yscaled -= wscaled;
                                    //var hwtemp = wscaled;
                                    //wscaled = hscaled;
                                    //hscaled = hwtemp;
                                    //change absolute rect values to relative
                                    wscaled = wscaled - xscaled;
                                    hscaled = hscaled - yscaled;
                                    break;
                            }
                            hscaled = hscaled + 100;
                            wscaled = wscaled + 50;
                            xscaled = xscaled - 50;
                            yscaled = yscaled - 50;
                            this.zoom_update(xscaled, yscaled, wscaled, hscaled);
                        }
                        this.CurrentMarkup++;
                    }
                    this.CurrentMarkup = 0;
                };
                this.SetMarkupZoom = function (dx, dy, dscale) {
                    
                    if (this.usevectorxml && thispage.VectorPageObj != undefined) {
                        if (thispage.vectorloaded) {
                            this.dscalevector = dscale;
                            this.dxvector = dx;
                            this.dyvector = dy;
                            thispage.setpagedimsize(this.dxvector, this.dyvector, (thispage.VectorPageObj.width * this.dscalevector), (thispage.VectorPageObj.height * this.dscalevector), false);
                            this.draw_vector(false);
                        }
                        else {
                            thispage.loadbinvectors();
                        }
                    }
                    else if (this.usepdfjs) {

                        var pcanvheight = thispage.offscreenheight;
                        var pcanvwidth = thispage.offscreenwidth;

                        if(thispage.getpageRotation() != 0){
                            pcanvwidth = thispage.originalPDFwidth;
                            pcanvheight = thispage.originalPDFheight;

                        }

                        /*if (thispage.usedincomposite && thispage.compositereference != undefined) {
                            //var pcanvheight = thispage.compositereference.ovpagecanvas.height;
                            //var pcanvwidth = thispage.compositereference.ovpagecanvas.width;
                            var pcanvheight = thispage.compositereference.overlaypage.getheight();
                            var pcanvwidth = thispage.compositereference.overlaypage.getwidth();
                        }
                        else {

                        }*/

                        //dscalepdf modified
                        this.dscalepdf = dscale;
                        this.dxpdf = dx;
                        this.dypdf = dy;
                        thispage.setpagedimsize(this.dxpdf, this.dypdf, (pcanvwidth * this.dscalepdf), (pcanvheight * this.dscalepdf), false);
                        thispage.pdfrenderopque += 1;
                        thispage.pdfisrendered = false;
                        //console.log('SetMarkupZoom invalidation');
                        if (thispage.pagenumber == thispage.DocRef.pages[thispage.DocRef.currentpage].pagenumber) {
                            thispage.DocRef.draw_mpagepdf(false);

                            /*if (this.usedincomposite && this.compositereference != undefined) {
                                this.compositereference.scaleToBackground(true);
                            }
                            else {
                                
                            }*/
                        }
                    }
                    else {
                        this.dscale = dscale;
                        this.dx = dx;
                        this.dy = dy;
                        this.checkimageswitch();
                        if (thispage.DocRef.Type == 0) {
                            thispage.DocRef.draw_mpage();
                        }
                        else {

                            /*thispage.draw_image(true);

                            if (this.usedincomposite && this.compositereference != undefined) {
                                this.compositereference.scaleToBackground(true);
                            }
                            else {
                                
                            }*/
                        }
                    }
                    //drawmarkupAll(cntximg);
                    //DrawMarkupSelected(context);
                };
                this.MarkupZoombyMarkup = function (markupobj, padding) {
                    this.zoomall();
                    var zoomout = 3;
                    //console.log('zoom all');
                    //var npage = markupobj.pagenumber;
                    var scalefactor = this.dscale / markupobj.scaling;
                    if (this.usevectorxml) {
                        scalefactor = this.dscalevector / markupobj.scaling;
                    }
                    else if (this.usepdfjs) {
                        scalefactor = this.dscalepdf / markupobj.scaling;
                    }
                    else {
                        scalefactor = this.dscale / markupobj.scaling;
                    }
                    var xscaled = markupobj.xscaled;
                    var wscaled = markupobj.wscaled;
                    var yscaled = markupobj.yscaled;
                    var hscaled = markupobj.hscaled;
                    switch (this.getpageRotation()) {
                        case 0:
                            switch (markupobj.type) {
                                case 0:
                                    xscaled = Math.min(markupobj.xscaled, markupobj.wscaled);
                                    wscaled = Math.max(markupobj.xscaled, markupobj.wscaled);
                                    yscaled = Math.min(markupobj.yscaled, markupobj.hscaled);
                                    hscaled = Math.max(markupobj.yscaled, markupobj.hscaled);
                                    wscaled = wscaled - xscaled;
                                    hscaled = hscaled - yscaled;
                                    break;
                                case 1:
                                    xscaled = Math.min(markupobj.xscaled, markupobj.wscaled);
                                    wscaled = Math.max(markupobj.xscaled, markupobj.wscaled);
                                    yscaled = Math.min(markupobj.yscaled, markupobj.hscaled);
                                    hscaled = Math.max(markupobj.yscaled, markupobj.hscaled);
                                    wscaled = wscaled - xscaled;
                                    hscaled = hscaled - yscaled;
                                    break;
                                case 2:
                                    xscaled = Math.min(markupobj.xscaled, markupobj.wscaled);
                                    wscaled = Math.max(markupobj.xscaled, markupobj.wscaled);
                                    yscaled = Math.min(markupobj.yscaled, markupobj.hscaled);
                                    hscaled = Math.max(markupobj.yscaled, markupobj.hscaled);
                                    wscaled = wscaled - xscaled;
                                    hscaled = hscaled - yscaled;
                                    break;
                                case 6:
                                    xscaled = Math.min(markupobj.xscaled, markupobj.wscaled);
                                    wscaled = Math.max(markupobj.xscaled, markupobj.wscaled);
                                    yscaled = Math.min(markupobj.yscaled, markupobj.hscaled);
                                    hscaled = Math.max(markupobj.yscaled, markupobj.hscaled);
                                    wscaled = wscaled - xscaled;
                                    hscaled = hscaled - yscaled;
                                    if (hscaled == 0) {
                                        hscaled = 1;
                                    }
                                    if (wscaled == 0) {
                                        wscaled = 1;
                                    }
                                    break;
                                case 7:
                                    xscaled = Math.min(markupobj.xscaled, markupobj.wscaled);
                                    wscaled = Math.max(markupobj.xscaled, markupobj.wscaled);
                                    yscaled = Math.min(markupobj.yscaled, markupobj.hscaled);
                                    hscaled = Math.max(markupobj.yscaled, markupobj.hscaled);
                                    wscaled = wscaled - xscaled;
                                    hscaled = hscaled - yscaled;
                                    if (hscaled == 0) {
                                        hscaled = 1;
                                    }
                                    if (wscaled == 0) {
                                        wscaled = 1;
                                    }
                                    break;
                                case 8:
                                    xscaled = Math.min(markupobj.xscaled, markupobj.wscaled);
                                    wscaled = Math.max(markupobj.xscaled, markupobj.wscaled);
                                    yscaled = Math.min(markupobj.yscaled, markupobj.hscaled);
                                    hscaled = Math.max(markupobj.yscaled, markupobj.hscaled);
                                    wscaled = wscaled - xscaled;
                                    hscaled = hscaled - yscaled;
                                    break;
                                case 9:
                                    if (markupobj.textrotate == 0) {
                                        xscaled = markupobj.rotatedrect.x;
                                        wscaled = markupobj.rotatedrect.w;
                                        yscaled = markupobj.rotatedrect.y;
                                        hscaled = markupobj.rotatedrect.h;
                                    }
                                    if (markupobj.textrotate / (Math.PI / 180) == 90) {
                                        xscaled = markupobj.xscaled;
                                        yscaled = markupobj.yscaled;
                                        wscaled = markupobj.wscaled;
                                        hscaled = markupobj.hscaled;
                                        var switchval = wscaled;
                                        wscaled = hscaled;
                                        hscaled = switchval;
                                        //xscaled = xscaled + wscaled;
                                        //yscaled = yscaled + hscaled;
                                    }
                                    if (markupobj.textrotate / (Math.PI / 180) == 270) {
                                        xscaled = markupobj.xscaled;
                                        yscaled = markupobj.yscaled;
                                        wscaled = markupobj.wscaled;
                                        hscaled = markupobj.hscaled;
                                        switchval = wscaled;
                                        wscaled = hscaled;
                                        hscaled = switchval;
                                        xscaled = xscaled - wscaled;
                                        yscaled = yscaled - hscaled;
                                    }
                                    if (markupobj.textrotate / (Math.PI / 180) == 180) {
                                        xscaled = markupobj.xscaled;
                                        yscaled = markupobj.yscaled;
                                        wscaled = markupobj.wscaled;
                                        hscaled = markupobj.hscaled;
                                        xscaled = xscaled - wscaled;
                                        yscaled = yscaled - hscaled;
                                    }
                                    //yscaled = yscaled - hscaled;
                                    break;
                                default:
                                    break;
                            }
                            break;
                        case 90:
                            //all values are absolute for all markup types.
                            xscaled = Math.min(markupobj.rotatedrect.x, markupobj.rotatedrect.w);
                            wscaled = Math.max(markupobj.rotatedrect.x, markupobj.rotatedrect.w);
                            yscaled = Math.min(markupobj.rotatedrect.y, markupobj.rotatedrect.h);
                            hscaled = Math.max(markupobj.rotatedrect.y, markupobj.rotatedrect.h);
                            //switch rect values to comply with orientation
                            //switch x and w
                            /*var wxtemp = xscaled;
                             xscaled = wscaled;
                             wscaled = wxtemp;*/
                            //change absolute rect values to relative
                            wscaled = wscaled - xscaled;
                            hscaled = hscaled - yscaled;
                            if (hscaled == 0) {
                                hscaled = 1;
                            }
                            if (wscaled == 0) {
                                wscaled = 1;
                            }
                            break;
                        case 180:
                            //all values are absolute for all markup types.
                            xscaled = Math.min(markupobj.rotatedrect.x, markupobj.rotatedrect.w);
                            wscaled = Math.max(markupobj.rotatedrect.x, markupobj.rotatedrect.w);
                            yscaled = Math.min(markupobj.rotatedrect.y, markupobj.rotatedrect.h);
                            hscaled = Math.max(markupobj.rotatedrect.y, markupobj.rotatedrect.h);
                            //switch rect values to comply with orientation
                            //both x,w and y,h is switched
                            /*var xtemp = xscaled;
                             var ytemp = yscaled;
                             xscaled = wscaled;
                             yscaled = hscaled;
                             wscaled = xtemp;
                             hscaled = ytemp;*/
                            //change absolute rect values to relative
                            wscaled = wscaled - xscaled;
                            hscaled = hscaled - yscaled;
                            if (hscaled == 0) {
                                hscaled = 1;
                            }
                            if (wscaled == 0) {
                                wscaled = 1;
                            }
                            break;
                        case 270:
                            //all values are absolute for all markup types.
                            xscaled = Math.min(markupobj.rotatedrect.x, markupobj.rotatedrect.w);
                            wscaled = Math.max(markupobj.rotatedrect.x, markupobj.rotatedrect.w);
                            yscaled = Math.min(markupobj.rotatedrect.y, markupobj.rotatedrect.h);
                            hscaled = Math.max(markupobj.rotatedrect.y, markupobj.rotatedrect.h);
                            //switch rect values to comply with orientation
                            /*var wytemp = yscaled;
                             yscaled = hscaled;
                             hscaled = wytemp;*/
                            //yscaled -= wscaled;
                            //var hwtemp = wscaled;
                            //wscaled = hscaled;
                            //hscaled = hwtemp;
                            //change absolute rect values to relative
                            wscaled = wscaled - xscaled;
                            hscaled = hscaled - yscaled;
                            if (hscaled == 0) {
                                hscaled = 1;
                            }
                            if (wscaled == 0) {
                                wscaled = 1;
                            }
                            break;
                    }



                    //Call zoom update with values.
                    this.zoom_update(xscaled, yscaled, wscaled, hscaled);
                    if(padding){

                        zoomout = padding.h / 50;
                        
                        //hscaled = hscaled + ((padding.h * hscaled) / 100);
                        //wscaled = wscaled + ((padding.w * wscaled) / 100);
                        //xscaled = xscaled - ((padding.x * wscaled) / 100);
                        //yscaled = yscaled - ((padding.y * hscaled) / 100);

                    }
                    
                    this.ZoomOut(zoomout, false, false);
                };
                this.MarkupZoom = function () {
                    //get current markup from list
                    //GetCurMarkup(this.CurrentMarkup);
                    if (this.CurrentMarkup > thispage.DocRef.nummarkups - 1) {
                        this.CurrentMarkup = 0;
                    }
                    var npage = thispage.DocRef.markuplist[this.CurrentMarkup].pagenumber;
                    while (npage != this.pagenumber && this.CurrentMarkup >= thispage.DocRef.nummarkups - 1) {
                        this.CurrentMarkup++;
                        npage = thispage.DocRef.markuplist[this.CurrentMarkup].pagenumber;
                    }
                    if (this.CurrentMarkup <= thispage.DocRef.nummarkups - 1) {
                        this.MarkupZoombyMarkup(thispage.DocRef.markuplist[this.CurrentMarkup]);
                        this.CurrentMarkup++;
                        if (this.CurrentMarkup <= thispage.DocRef.nummarkups - 1) {
                            npage = thispage.DocRef.markuplist[this.CurrentMarkup].pagenumber;
                            while (npage != this.pagenumber && this.CurrentMarkup <= thispage.DocRef.nummarkups - 1) {
                                this.CurrentMarkup++;
                                if (this.CurrentMarkup >= thispage.DocRef.nummarkups - 1) {
                                    break;
                                }
                                npage = thispage.DocRef.markuplist[this.CurrentMarkup].pagenumber;
                            }
                            if (this.CurrentMarkup >= thispage.DocRef.nummarkups - 1) {
                                this.CurrentMarkup = 0;
                            }
                        }
                    }
                };
                this.resize = function () {
                    if (this.usevector3Dxml) {
                        return;
                    }
                    if (thispage.bDorescaleonsizeChange) {
                        var yscale = canvasoheight / this.MainImageHeight;
                        var xscale = canvasowidth / this.MainImageWidth;
                        this.dscale = Math.min(xscale, yscale);
                        this.dx = (canvasowidth - (this.MainImageWidth * this.dscale)) / 2;
                        this.dy = (canvasoheight - (this.MainImageHeight * this.dscale)) / 2;
                    }
                    if (this.usevectorxml && thispage.bDorescaleonsizeChange && thispage.VectorPageObj != undefined) {
                        yscale = canvasoheight / thispage.VectorPageObj.height;
                        xscale = canvasowidth / thispage.VectorPageObj.width;
                        this.dscalevector = Math.min(xscale, yscale);
                        this.dxvector = (canvasowidth - (thispage.VectorPageObj.width * this.dscalevector)) / 2;
                        this.dyvector = (canvasoheight - (thispage.VectorPageObj.height * this.dscalevector)) / 2;
                    }
                    if (this.usepdfjs) {
                        //yscale = canvasoheight / thispage.pagecanvas.height;
                        //xscale = canvasowidth / thispage.pagecanvas.width;
                        //this.dscalepdf = Math.min(xscale,yscale);
                        //this.dxpdf = (canvasowidth - (thispage.pagecanvas.width * this.dscalepdf)) / 2;
                        //this.dypdf = (canvasoheight - (thispage.pagecanvas.height * this.dscalepdf)) / 2;

                        var pcanvheight = thispage.offscreenheight;
                        var pcanvwidth = thispage.offscreenwidth;

                        if(thispage.getpageRotation() != 0){
                            pcanvwidth = thispage.originalPDFwidth;
                            pcanvheight = thispage.originalPDFheight;

                        }

                        /*if (thispage.usedincomposite && thispage.compositereference != undefined) {
                            //var pcanvheight = thispage.compositereference.ovpagecanvas.height;
                            //var pcanvwidth = thispage.compositereference.ovpagecanvas.width;
                            var pcanvheight = thispage.compositereference.overlaypage.getheight();
                            var pcanvwidth = thispage.compositereference.overlaypage.getwidth();
                        }
                        else {

                        }*/


                        if (this.dxpdf > 1 && (pcanvwidth * this.dscalepdf) < canvasowidth) {
                            //console.log('resize to pan update');
                            this.pan_update_resize(((canvasowidth - (pcanvwidth * this.dscalepdf)) / 2) - this.dxpdf, 0);
                        }
                        //DocObj.draw_mpagepdf();
                    }
                    if (!this.usevectorxml) {
                        if (this.usepdfjs) {
                            //DocObj.draw_mpagepdf();
                            thispage.setpagedimsize(this.dxpdf, this.dypdf, (pcanvwidth * this.dscalepdf), (pcanvheight * this.dscalepdf), false);
                            if (thispage.DocRef.pages[thispage.DocRef.currentpage] != undefined) {
                                if (thispage.pagenumber == thispage.DocRef.pages[thispage.DocRef.currentpage].pagenumber) {
                                    if (canvasoheight > pcanvheight && pcanvheight > pcanvwidth && thispage.bDorescaleonsizeChange) {
                                        //this.zoomheight();
                                        //console.log('zoom height');
                                    }
                                    else if (canvasowidth > pcanvwidth && pcanvheight < pcanvwidth && thispage.bDorescaleonsizeChange) {
                                        //this.zoomwidth();
                                        //console.log('zoom width');
                                    }
                                    else {
                                        thispage.DocRef.draw_mpagepdf(false);
                                        
                                        /*if (this.usedincomposite && this.compositereference != undefined) {
                                            if (thispage.role == 1) {
                                                thispage.dscalepdf = thispage.compositereference.getoverlayScaleFactor(6);
                                                thispage.compositereference.scaleToBackground(true);
                                            }
                                            thispage.getfoxitbitmap(thispage.dscalepdf);
                                            //commented out 28.04
                                            //this.dscalepdf = this.compositereference.getoverlayScaleFactor(6);
                                            //this.compositereference.scaleToBackground(true);
                                        }
                                        else {
                                            
                                        }*/
                                    }
                                }
                            }
                        }
                        else {
                            this.checkimageswitch();
                            if (thispage.DocRef.Type == 0) {
                                thispage.DocRef.draw_mpage();
                            }
                            else {
                                thispage.draw_image(true);
                                
                                /*if (this.usedincomposite && this.compositereference != undefined) {
                                    //this.dscale = this.compositereference.getoverlayScaleFactor(6);
                                    this.compositereference.scaleToBackground(true);
                                }
                                else {
                                    
                                }*/
                            }
                        }
                    }
                    else {
                        if (thispage.VectorPageObj == undefined) {
                            return;
                        }
                        thispage.setpagedimsize(this.dxvector, this.dyvector, (thispage.VectorPageObj.width * this.dscalevector), (thispage.VectorPageObj.height * this.dscalevector), false);
                        
                        thispage.draw_vector(false);

                        /*if (this.usedincomposite && this.compositereference != undefined) {
                            //this.dscalevector = this.compositereference.getoverlayScaleFactor(6);
                            this.compositereference.scaleToBackground(true);
                        }
                        else {
                            
                        }*/
                    }
                    redrawMarkup();
                };
                this.pan_page = function (sx, sy, bexternal) {
                    thispage.pan_update(sx, sy, bexternal);
                };
                this.pan_page_pos = function (sx, sy, bexternal) {
                    thispage.pan_position(sx, sy, bexternal);
                };
                this.pan_position = function (sx, sy, bexternal) {
                    var pgdim = thispage.getpagedim();
                    var diffx = pgdim.x - sx;
                    var diffy = pgdim.y - sy;
                    //console.log('panupdate pan position');
                    thispage.pan_update(diffx, diffy, true);
                };
                this.getpagetestrect = function (rect) {
                    var rotrect = { x1: rect.startx, y1: rect.starty, x2: rect.endx, y2: rect.endy };
                    var pagerect = {
                        x: rotrect.x1,
                        y: rotrect.y1,
                        w: rotrect.x2 - rotrect.x1,
                        h: rotrect.y2 - rotrect.y1
                    };
                    if (thispage.getpageRotation() != 0) {
                        rotrect = rect_rotated(rect.startx, rect.starty, rect.endx, rect.endy);
                        pagerect.x = rotrect.x1;
                        pagerect.y = rotrect.y1;
                        pagerect.w = rotrect.x2 - rotrect.x1;
                        pagerect.h = rotrect.y2 - rotrect.y1;
                        switch (thispage.getpageRotation()) {
                            case 90:
                                pagerect.y = -((pagerect.y + pagerect.h) - canvasoheight);
                                pagerect.x = -((pagerect.x + pagerect.w) - canvasowidth);
                                break;
                            case 270:
                                pagerect.y = -((pagerect.y + pagerect.h) - canvasoheight);
                                pagerect.x = -((pagerect.x + pagerect.w) - canvasowidth);
                                break;
                        }
                    }
                    return pagerect;
                };
                this.getrotatedpagerect = function(busepixelratio){
                    
                    var rotrect = { x1: this.startx, y1: this.starty, x2: this.endx, y2: this.endy };
                    var pdfnonrotrect = { x1: this.pdfstartx, y1: this.pdfstarty, x2: this.pdfendx, y2: this.pdfendy };

                    //this.pdfstartx = 0.0;
                    //this.pdfstarty = 0.0;
                    //this.pdfendx = 0.0;
                    //this.pdfendy = 0.0;

                    var pagerect = {
                        x: rotrect.x1,
                        y: rotrect.y1,
                        w: rotrect.x2 - rotrect.x1,
                        h: rotrect.y2 - rotrect.y1,
                        usepixelscale : false
                    };

                    var pdfpagerectnorot = {
                        x: pdfnonrotrect.x1,
                        y: pdfnonrotrect.y1,
                        w: pdfnonrotrect.x2 - pdfnonrotrect.x1,
                        h: pdfnonrotrect.y2 - pdfnonrotrect.y1,
                        usepixelscale : false
                    };


                    if (thispage.getpageRotation() != 0) {
                        rotrect = rect_rotated(this.startx, this.starty, this.endx, this.endy);
                        pagerect.x = rotrect.x1;
                        pagerect.y = rotrect.y1;
                        pagerect.w = rotrect.x2 - rotrect.x1;
                        pagerect.h = rotrect.y2 - rotrect.y1;
                        switch (thispage.getpageRotation()) {
                            case 90:
                                pagerect.y = -((pagerect.y + pagerect.h) - canvasoheight);
                                pagerect.x = -((pagerect.x + pagerect.w) - canvasowidth);
                                break;
                            case 270:
                                pagerect.y = -((pagerect.y + pagerect.h) - canvasoheight);
                                pagerect.x = -((pagerect.x + pagerect.w) - canvasowidth);
                                break;
                        }
                    }

                    if (this.usepdfjs) {

                        //pagerect = pdfpagerectnorot;

                        pagerect.usepixelscale = true;

                    }

                    if(busepixelratio){
                        var pixelRatio = getPixelRatio();
                        pagerect.x *= pixelRatio;
                        pagerect.y *= pixelRatio;
                        pagerect.w *= pixelRatio;
                        pagerect.h *= pixelRatio;

                    }

                    return pagerect;

                };                
                this.getpagerectdirect = function(busepixelratio){

                    var pagerect = { x: this.startx, y: this.starty, w: this.endx, h: this.endy, usepixelscale : false};


                    /*if (thispage.getpageRotation() == 0) {
                        pagerect.w = pagerect.w - pagerect.x;
                        pagerect.h = pagerect.h - pagerect.y;
                    }*/

                    if(busepixelratio){
                        var pixelRatio = getPixelRatio();
                        pagerect.x *= pixelRatio;
                        pagerect.y *= pixelRatio;
                        pagerect.w *= pixelRatio;
                        pagerect.h *= pixelRatio;

                    }

                    if (this.usepdfjs) {
                        
                        pagerect.usepixelscale = true;

                    }

                    if(debugmarkuprect){
                        console.log(pagerect);
                    }

                    

                    return pagerect;


                };
                this.getpagerect = function (busepixelratio) {



                    var rotrect = { x1: this.startx, y1: this.starty, x2: this.endx, y2: this.endy };
                    var pdfnonrotrect = { x1: this.pdfstartx, y1: this.pdfstarty, x2: this.pdfendx, y2: this.pdfendy };

                    var pagerect = {
                        x: rotrect.x1,
                        y: rotrect.y1,
                        w: rotrect.x2 - rotrect.x1,
                        h: rotrect.y2 - rotrect.y1,
                        usepixelscale : false
                    };

                    var pdfpagerectnorot = {
                        x: pdfnonrotrect.x1,
                        y: pdfnonrotrect.y1,
                        w: pdfnonrotrect.x2 - pdfnonrotrect.x1,
                        h: pdfnonrotrect.y2 - pdfnonrotrect.y1,
                        usepixelscale : false
                    };

                    if (thispage.getpageRotation() != 0) {
                        rotrect = rect_rotated(this.startx, this.starty, this.endx, this.endy);
                        pagerect.x = rotrect.x1;
                        pagerect.y = rotrect.y1;
                        pagerect.w = rotrect.x2 - rotrect.x1;
                        pagerect.h = rotrect.y2 - rotrect.y1;
                        switch (thispage.getpageRotation()) {
                            case 90:
                                pagerect.y = -((pagerect.y + pagerect.h) - canvasoheight);
                                pagerect.x = -((pagerect.x + pagerect.w) - canvasowidth);
                                break;
                            case 270:
                                pagerect.y = -((pagerect.y + pagerect.h) - canvasoheight);
                                pagerect.x = -((pagerect.x + pagerect.w) - canvasowidth);
                                break;
                        }
                    }
                    if (this.usepdfjs) {

                        //pagerect = pdfpagerectnorot;

                        pagerect.usepixelscale = true;

                    }

                    if(busepixelratio){
                        var pixelRatio = getPixelRatio();
                        pagerect.x *= pixelRatio;
                        pagerect.y *= pixelRatio;
                        pagerect.w *= pixelRatio;
                        pagerect.h *= pixelRatio;

                    }

                    return pagerect;
                };
                this.pan_update_resize = function (sx, sy) {
                    thispage.resizepan = true;
                    thispage.pan_update(sx, sy);
                    //console.log('pan update',  sx);
                };
                this.setPagePosBelow = function (page) {

                    if(thispage.getpageRotation() == 0){
                        thispage.setPagePos(thispage.startx, page.starty + (page.offscreenheight * page.dscalepdf) + 10);
                    }
                    
                    //thispage.draw_canvas(false);
                };
                this.setPagePosAbove = function (page) {

                    if(thispage.getpageRotation() == 0){
                        thispage.setPagePos(thispage.startx, (page.starty - (thispage.offscreenheight * thispage.dscalepdf)) - 10);            
                    }
                    
                    /*if(this.getpageRotation() == 0 || this.getpageRotation() == 180){
                        
                    }else if (this.getpageRotation() == 90 || this.getpageRotation() == 270){
                        thispage.setPagePos(thispage.startx, (page.starty - (thispage.offscreenheight * thispage.dscalepdf)) - 10);            
                    }*/

                    
                    //if(thispage.)

                    //thispage.draw_canvas(false);
                };
                this.setPageBitmap = function (bitmap) {
                    
                    if(bitmap.error){

                        return;
                        //check for error

                    }
                    thispage.pageRendering = false;
                    var tdv = new Uint8ClampedArray(bitmap.buffer);
                    var imageData = new ImageData(tdv, bitmap.width, bitmap.height);
                    //this.role = 0;
                    // 0 - no role.
                    // 1 - backgroundpage.
                    // 2 - overlaypage.

                    //dscalepdf modified
                    thispage.dscalepdf = 1;
                    thispage.pagecanvas.width = bitmap.width;
                    thispage.pagecanvas.height = bitmap.height;
                    thispage.offscreenwidth = bitmap.width;
                    thispage.offscreenheight = bitmap.height;


                    var cmprcolors = thispage.compositereference.getColors();

                    //test if setting color affects stripes
                    switch (thispage.role) {
                        case 1:
                            thispage.setcolor(imageData.data, cmprcolors.bg);
                            break;
                        case 2:
                            thispage.setcolor(imageData.data, cmprcolors.fg);
                            break;
                    }
                    thispage.pagectx.putImageData(imageData, 0, 0);
                    thispage.pdfisrendered = true;
                    thispage.compositereference.draw_compare(true);
                    //thispage.canvtileArray[i][j].ctx.putImageData(imageData, 0, 0);
                    /*if(binvert){
                        thispage.setinvert(imageData.data);
                    }else{
                        thispage.transparent(imageData.data);
            
                    }*/
                };

                this.setpagerect = function(startx, starty, endx, endy){
                    
                    thispage.startx = startx;
                    thispage.starty = starty;
                    thispage.endx = endx + startx;
                    thispage.endy = endy + starty;

                    if(debugmarkuprect){
                        console.log(thispage.startx, thispage.starty, thispage.endx, thispage.endy);
                    }

                    
                    //thispage.setpagerect(this.dxpdf, this.dypdf, (thispage.offscreenheight * this.dscalepdf) + this.dxpdf, (thispage.offscreenwidth * this.dscalepdf) + this.dypdf);

                    return {x : thispage.startx, y : thispage.starty, w : thispage.endx, h : thispage.endy};
                };


                this.setPagePos = function (sx, sy) {
                    switch (this.getpageRotation()) {
                        case 0:
                            this.dx = sx;
                            this.dy = sy;
                            this.dxvector = sx;
                            this.dyvector = sy;
                            this.dxpdf = sx;
                            this.dypdf = sy;
                            break;
                        case 90:
                            this.dx = sy;
                            this.dy = sx;
                            this.dxvector = sy;
                            this.dyvector = sx;
                            this.dxpdf = sy;
                            this.dypdf = sx;
                            break;
                        case 180:
                            this.dx = sx;
                            this.dy = sy;
                            this.dxvector = sx;
                            this.dyvector = sy;
                            this.dxpdf = sx;
                            this.dypdf = sy;
                            break;
                        case 270:
                            this.dx = sy;
                            this.dy = sx;
                            this.dxvector = sy;
                            this.dyvector = sx;
                            this.dxpdf = sy;
                            this.dypdf = sx;
                            break;
                    }
                    if (!this.usevectorxml) {
                        if (this.usepdfjs) {
                            //pcanvheight = thispage.offscreenheight;
                            //pcanvwidth = thispage.offscreenwidth;

                            this.dxpdf = sx;
                            this.dypdf = sy;

                            var rect = thispage.setpagerect(this.dxpdf, this.dypdf, (thispage.offscreenwidth * this.dscalepdf), (thispage.offscreenheight * this.dscalepdf));
                            //var rect = thispage.setpagerect(this.dxpdf, this.dypdf, (thispage.offscreenheight * this.dscalepdf), (thispage.offscreenwidth * this.dscalepdf));

                            if(this.getpageRotation() == 0 || this.getpageRotation() == 180){
                                
                            }else if (this.getpageRotation() == 90 || this.getpageRotation() == 270){
                                
                            }

                            

                            thispage.PDFTextArea.style.left = rect.x + "px";
                            thispage.PDFTextArea.style.top = rect.y + "px";

                            //thispage.PDFTextArea.style.left = thispage.startx + "px";
                            //thispage.PDFTextArea.style.top = thispage.starty + "px";

                            /*this.startx = this.dxpdf;
                            this.starty = this.dypdf;
                            this.endx = (thispage.offscreenheight * this.dscalepdf) + this.startx;
                            this.endy = (thispage.offscreenwidth * this.dscalepdf) + this.starty;*/

                            var left = Math.max(0, rect.x);
                            var right = Math.min(canvasowidth, rect.w);
                            var bottom = Math.max(canvasoheight, rect.h);
                            var top = Math.min(0, rect.y);


                            /*var left = Math.max(0, thispage.dxpdf);
                            var right = Math.min(canvasowidth, thispage.endx);
                            var bottom = Math.max(canvasoheight, thispage.endy);
                            var top = Math.min(0, thispage.dypdf);*/

                            if (left < right && bottom > top) {
                                thispage.visible = true;
                                if (thispage.DocRef.textselect) {
                                    thispage.PDFTextArea.style.display = szdispvalue;
                                    thispage.PDFTextArea.style.zIndex = "4";
                                }
                            }
                            else {
                                thispage.visible = false;
                                if (thispage.DocRef.textselect) {
                                    thispage.PDFTextArea.style.display = "none";
                                }
                            }
                        }
                        else {
                            this.checkimageswitch();
                            this.draw_image(true);
                        }
                    }
                    else {

                        if(thispage.VectorPageObj != undefined){

                            rect = thispage.setpagerect(this.dxvector, this.dyvector, (thispage.VectorPageObj.width * this.dscalevector), (thispage.VectorPageObj.height * this.dscalevector));
                            
/*                             this.startx = this.dxvector;
                            this.starty = this.dyvector;
                            this.endx = (thispage.VectorPageObj.width * this.dscalevector) + this.startx;
                            this.endy = (thispage.VectorPageObj.height * this.dscalevector) + this.starty;
 */
                            
                            //thispage.TextSelectArea.style.left = thispage.startx + "px";
                            //thispage.TextSelectArea.style.top = thispage.starty + "px";
                            thispage.movetextdivs();

                            /* left = Math.max(0, thispage.dxvector);
                            right = Math.min(canvasowidth, thispage.endx);
                            bottom = Math.max(canvasoheight, thispage.endy);
                            top = Math.min(0, thispage.dyvector); */

                            left = Math.max(0, rect.x);
                            right = Math.min(canvasowidth, rect.w);
                            bottom = Math.max(canvasoheight, rect.h);
                            top = Math.min(0, rect.y);



                            if (left < right && bottom > top) {
                                thispage.visible = true;
                                if (thispage.DocRef.textselect) {
                                    thispage.TextSelectArea.style.display = szdispvalue;
                                }
                                this.draw_vector(false);
                            }
                            else {
                                thispage.visible = false;
                                if (thispage.DocRef.textselect) {
                                    thispage.TextSelectArea.style.display = "none";
                                }
                            }
    
                        }

                    }
                    redrawMarkup();
                };

                this.getcanvasdim = function(){

                    //var left = Math.max(0, thispage.dxpdf);
                    //var right = Math.min(canvasowidth, thispage.endx);
                    //var bottom = Math.max(canvasoheight, thispage.endy);
                    //var top = Math.min(0, thispage.dypdf);

                    
                    //right = Math.min(canvasowidthsize, thispage.endx);
                    //bottom = Math.max(canvasoheightsize, thispage.endy);

                    var cnvdim = {w : canvasowidth, h : canvasoheight};

                    if (this.usepdfjs) {
                        cnvdim = {w : canvasowidthsize, h : canvasoheightsize};
                    }else{
                        cnvdim = {w : canvasowidth, h : canvasoheight};
                    }

                    return cnvdim;
                    
                };


                this.pan_update = function (sx, sy, bexternal) {
                    if (this.usevector3Dxml) {
                        return;
                    }
                    var curpdfdx = this.dxpdf;

                    var scrollposy = sy;
                    var scrollposx = sx;


                    var pixelRatio = getPixelRatio();
                    var revpixelRatio = 1 / pixelRatio;

                    var rc = {x: sx, y: sy};
                    rc.x = rc.x * revpixelRatio;
                    rc.y = rc.y * revpixelRatio;

                    scrollposy = rc.y;
                    scrollposx = rc.x;


                    var busescrollpos = false;
                    if (thispage.DocRef.foxitdoc != null) {
                        if ((thispage.DocRef.foxitdoc.onHBarGrip && thispage.DocRef.foxitdoc.hBarOn) || (thispage.DocRef.foxitdoc.onVBarGrip && thispage.DocRef.foxitdoc.vBarOn)) {
                            busescrollpos = true;
                            scrollposy = -(sy * thispage.DocRef.foxitdoc.scrollHeightfactor);
                            scrollposx = -(sx * thispage.DocRef.foxitdoc.scrollWidthfactor);

                            scrollposy = -(rc.y * thispage.DocRef.foxitdoc.scrollHeightfactor);
                            scrollposx = -(rc.x * thispage.DocRef.foxitdoc.scrollWidthfactor);

                        }
                        else {
                            busescrollpos = false;
                        }
                    }
                    else {
                        busescrollpos = false;
                    }
                    switch (this.getpageRotation()) {
                        case 0:
                            if (busescrollpos) {
                                this.dx = this.dx - sx;
                                this.dy = this.dy - sy;
                                this.dxvector -= sx;
                                this.dyvector -= sy;
                                this.dxpdf -= scrollposx;
                                this.dypdf -= scrollposy;
                            }
                            else {
                                this.dx = this.dx - sx;
                                this.dy = this.dy - sy;
                                this.dxvector -= sx;
                                this.dyvector -= sy;
                                //this.dxpdf -= sx;
                                //this.dypdf -= sy;
                                this.dxpdf -= rc.x;
                                this.dypdf -= rc.y;

                            }
                            break;
                        case 90:

                            if (busescrollpos) {
                                this.dx = this.dx - sy;
                                this.dy = this.dy + sx;
                                this.dxvector -= sy;
                                this.dyvector += sx;

                                this.dxpdf -= scrollposy;
                                this.dypdf += scrollposx;
            
                            }else{
                                this.dx = this.dx - sy;
                                this.dy = this.dy + sx;
                                this.dxvector -= sy;
                                this.dyvector += sx;

                                //this.dxpdf -= sy;
                                //this.dypdf += sx;

                                this.dxpdf -= rc.y;
                                this.dypdf += rc.x;

                            }

                            break;
                        case 180:
                            if (busescrollpos) {
                                this.dx = this.dx + sx;
                                this.dy = this.dy + sy;
                                this.dxvector += sx;
                                this.dyvector += sy;

                                this.dxpdf += scrollposx;
                                this.dypdf += scrollposy;

    
                            }else{
                                this.dx = this.dx + sx;
                                this.dy = this.dy + sy;
                                this.dxvector += sx;
                                this.dyvector += sy;

                                //this.dxpdf += sx;
                                //this.dypdf += sy;

                                this.dxpdf += rc.x;
                                this.dypdf += rc.y;

                            }

                            

                            break;
                        case 270:

                            if (busescrollpos) {
                                this.dx = this.dx + sy;
                                this.dy = this.dy - sx;
                                this.dxvector += sy;
                                this.dyvector -= sx;

                                //this.dxpdf -= scrollposx;
                                //this.dypdf -= scrollposy;

                                this.dxpdf += scrollposy;
                                this.dypdf -= scrollposx;


    
                            }else{
                                this.dx = this.dx + sy;
                                this.dy = this.dy - sx;
                                this.dxvector += sy;
                                this.dyvector -= sx;

                                //this.dxpdf += sy;
                                //this.dypdf -= sx;

                                this.dxpdf += rc.y;
                                this.dypdf -= rc.x;


                            }
                            //this.dxpdf += sy;
                            //this.dypdf -= sx;

                            break;
                    }
                    if (this.usepdfjs) {
                    }
                    if (!this.usevectorxml) {
                        if (this.usepdfjs) {
                            var pcanvheight = thispage.offscreenheight;
                            var pcanvwidth = thispage.offscreenwidth;

                            if(thispage.getpageRotation() != 0){
                                pcanvwidth = thispage.originalPDFwidth;
                                pcanvheight = thispage.originalPDFheight;
                            }

                            /*if (thispage.usedincomposite && thispage.compositereference != undefined){
                                //var pcanvheight = thispage.compositereference.ovpagecanvas.height;
                                //var pcanvwidth = thispage.compositereference.ovpagecanvas.width;
                                pcanvheight = thispage.compositereference.overlaypage.getheight();
                                pcanvwidth = thispage.compositereference.overlaypage.getwidth();
            
                            }*/
                            //0;
                            thispage.setpagedimsize(this.dxpdf, this.dypdf, (pcanvwidth * this.dscalepdf), (pcanvheight * this.dscalepdf), false);
                            thispage.PDFTextArea.style.left = thispage.startx + "px";
                            thispage.PDFTextArea.style.top = thispage.starty + "px";

                            /*if ((thispage.usedincomposite && thispage.compositereference != undefined) || (thispage.DocRef.pagelocked && !bRestrictPan)) {
                                //allow free position for PDF when used with overlay/compare
                            }
                            else {
                            }*/

                            /*if (pcanvwidth * this.dscalepdf <= canvasowidth && this.getpageRotation() == 0) {
                                thispage.dxpdf = (canvasowidth - (pcanvwidth * this.dscalepdf)) / 2;
                            }*/

                            if (pcanvwidth * this.dscalepdf <= canvasowidthsize && this.getpageRotation() == 0) {
                                thispage.dxpdf = (canvasowidthsize - (pcanvwidth * this.dscalepdf)) / 2;
                            }


                            if (thispage.DocRef.pages[thispage.DocRef.currentpage] != undefined) {
                                if (thispage.pagenumber == thispage.DocRef.pages[thispage.DocRef.currentpage].pagenumber) {


                                    /*if (this.usedincomposite && this.compositereference != undefined) {
                                        if (thispage.role == 0 && documentcompare) {
                                            thispage.compositereference.pan_update(sx, sy);
                                        }
                                        this.compositereference.scaleToBackground(false);
                                    }
                                    else {
                                        //thispage.DocRef.pan_update(sx, sy);
                                    }*/

                                    if (thispage.resizepan) {
                                        thispage.DocRef.draw_mpagepdf(false);
                                        thispage.resizepan = false;
                                    }
                                    else {
                                        thispage.DocRef.draw_mpagepdf(true);
                                    }

                                }
                            }
                            //DocObj.draw_mpagepdf();
                            var left = Math.max(0, thispage.dxpdf);
                            var right = Math.min(canvasowidth, thispage.endx);
                            var bottom = Math.max(canvasoheight, thispage.endy);
                            var top = Math.min(0, thispage.dypdf);


                            right = Math.min(canvasowidthsize, thispage.endx);
                            bottom = Math.max(canvasoheightsize, thispage.endy);


                            if (left < right && bottom > top) {
                                thispage.visible = true;
                                if (thispage.DocRef.textselect) {
                                    thispage.PDFTextArea.style.display = szdispvalue;
                                    thispage.PDFTextArea.style.zIndex = "4";
                                }
                            }
                            else {
                                thispage.visible = false;
                                if (thispage.DocRef.textselect) {
                                    thispage.PDFTextArea.style.display = "none";
                                }
                            }
                            if (thispage.DocRef.foxitdoc != null) {
                                var pagerect = thispage.getpagerect();
                                if (thispage.pagenumber === true) {
                                    //console.log(thispage.pagenumber);
                                    thispage.pagenumber = thispage.DocRef.currentpage;
                                }
                                //var mousedirect = { mx: sx, my: sy };
                                var mousedirect = { mx: rc.x, my: rc.y };

                                //this.dxpdf -= sx;
                                //this.dypdf -= sy;
                                var pdfoffsetpos = { x: thispage.dxpdf, y: thispage.dypdf };
                                var pdfoffset = thispage.getPDFCoordsRot(pdfoffsetpos);

                                //thispage.foxitpanToPoint(pdfoffset);
                                
                                thispage.DocRef.foxitdoc.scrolldeltaupdate(mousedirect, thispage.pagenumber);


                                //thispage.DocRef.foxitdoc.scrollupdate(pagerect, canvasowidth, canvasoheight, thispage.pagenumber, thispage.tilevisibiltychanged);
                            }
                            /*if ((thispage.dxpdf < canvasowidth && thispage.dxpdf > 0) || (thispage.endx > 0 && thispage.endx < canvasowidth)) {
                                if ((thispage.dypdf < canvasoheight && thispage.dypdf > 0) || (thispage.endy > 0 && thispage.endy < canvasoheight)) {
                                    thispage.visible = true;
                                    thispage.PDFTextArea.style.display = "initial";
                                    console.log('pan visible on');
                                    //console.log('visible ' + thispage.pagenumber);
                                } else {
                                    thispage.visible = false;
                                    thispage.PDFTextArea.style.display = "none";
                                    console.log('pan visible off');
        
                                }
                            }*/
                        }
                        else {
                            this.checkimageswitch();
                            if (thispage.DocRef.Type == 0) {
                                thispage.DocRef.draw_mpage();
                            }
                            else {
                                /*if (this.usedincomposite && this.compositereference != undefined) {
                                    this.compositereference.scaleToBackground(false);
                                }
                                else {
                                    
                                }*/
                                thispage.draw_image(true);
                            }
                        }
                    }
                    else {

                        if(thispage.VectorPageObj != undefined){
                            thispage.setpagedimsize(this.dxvector, this.dyvector, (thispage.VectorPageObj.width * this.dscalevector), (thispage.VectorPageObj.height * this.dscalevector), false);
                            //thispage.TextSelectArea.style.left = thispage.startx + "px";
                            //thispage.TextSelectArea.style.top = thispage.starty + "px";
                            thispage.movetextdivs();
                            /*if (this.usedincomposite && this.compositereference != undefined) {
                                this.compositereference.scaleToBackground(false);
                            }
                            else {
                                
                            }*/
                            this.draw_vector(false);

                            left = Math.max(0, thispage.dxvector);
                            right = Math.min(canvasowidth, thispage.endx);
                            bottom = Math.max(canvasoheight, thispage.endy);
                            top = Math.min(0, thispage.dyvector);
                            if (left < right && bottom > top) {
                                thispage.visible = true;
                                if (thispage.DocRef.textselect) {
                                    thispage.TextSelectArea.style.display = szdispvalue;
                                }
                            }
                            else {
                                thispage.visible = false;
                                if (thispage.DocRef.textselect) {
                                    thispage.TextSelectArea.style.display = "none";
                                }
                            }
    
                        }

                    }
                    //redrawMarkup();
                    redrawMarkupPan();
                    if (RxCore_GUI_PanUpdated != undefined && bexternal == undefined) {
                        var pagerect = thispage.getpagerect();
                        RxCore_GUI_PanUpdated.panUpdate(sx, sy, pagerect);
                    }
                    /*if(RxCore_GUI_PanUpdate != undefined && bexternal == undefined){
                        
                        var pagerect = thispage.getpagerect();
                        
                        RxCore_GUI_PanUpdate.panUpdate(sx,sy, pagerect);
        
                    }*/
                };
                this.rotateCanvas = function () {
                    var localdx = 0.0;
                    var localdy = 0.0;
                    var localdscale = 0.0;
                    contexto.save();
                    var tx = (canvasowidth / 2);
                    var ty = (canvasoheight / 2);
                    contexto.translate(tx, ty);
                    contexto.rotate(this.getpageRotation() * (Math.PI / 180));
                    contexto.translate(-tx, -ty);
                    contexto.fillStyle = "rgb(238,243,250)";
                    contexto.fillRect(0, 0, canvasowidth, canvasoheight);
                    switch (this.currentimage) {
                        case 0:
                            //localdx = this.dx;
                            //localdy = this.dy;
                            //localdscale = this.dscale;
                            contexto.drawImage(this.largeimage, this.dx, this.dy, this.MainImageWidth * this.dscale, this.MainImageHeight * this.dscale);
                            break;
                        case 1:
                            //localdx = this.dxextent;
                            //localdy = this.dyextent;
                            //localdscale = this.dscaleextent;
                            localdscale = this.dscale / thispage.bitmapratio;
                            contexto.drawImage(this.smallimage, this.dx, this.dy, this.SmallImageWidth * localdscale, this.SmallImageHeight * localdscale);
                            break;
                    }
                    
                    contexto.restore();
                    redrawMarkup();
                };

                this.rotatepage = function (degree) {

                    //no page lock check needed for Foxit.

                    if (thispage.DocRef.foxitdoc != null) {

                        var foxitdegree = degree;
                        if(thispage.foxitpageorigrot != 0){
                            switch (thispage.foxitpageorigrot){
                                case 90 : 
                                    foxitdegree = degree - 90;

                                    if (foxitdegree < 0){
                                        foxitdegree = foxitdegree + 360;                             
                                    }

                                    break;
                                case 270 :
                                    foxitdegree = degree - 90;


                                    if (foxitdegree < 0){
                                        foxitdegree = foxitdegree + 360;                             
                                    }

                                    /*if (foxitdegree == 360){
                                        foxitdegree = 0;                             
                                    }*/
            

                                    break;    
                                case 180 :
                                    foxitdegree = degree + 180;

                                    if (foxitdegree >= 360){
                                        foxitdegree = foxitdegree - 360;                             
                                    }


                                    break;    

                            }
                        }



                        thispage.DocRef.foxitdoc.rotatePage(thispage.pagenumber, foxitdegree, this.foxitonRotate);
                        thispage.bpagesizeupdated = false;
                    }

                    var i = 0;

                    switch (this.getpageRotation()) {
                        case 0:
                            if (degree == 90) {
                                for (i = 0; i <= 90; i = i + 10) {
                                    this.drotation = i;
                                    //pagerotations

                                    if(this.DocRef.pagerotations[thispage.pagenumber] != undefined){
                                        this.DocRef.pagerotations[thispage.pagenumber] = this.drotation;
                                    }
                

                                    this.rotateCanvas();
                                }
                            }
                            if (degree == 180) {
                                for (i = 0; i <= 180; i = i + 10) {
                                    this.drotation = i;
                                    if(this.DocRef.pagerotations[thispage.pagenumber] != undefined){
                                        this.DocRef.pagerotations[thispage.pagenumber] = this.drotation;
                                    }

                                    this.rotateCanvas();
                                }
                            }
                            if (degree == 270) {
                                for (i = 0; i <= 270; i = i + 10) {
                                    this.drotation = i;
                                    if(this.DocRef.pagerotations[thispage.pagenumber] != undefined){
                                        this.DocRef.pagerotations[thispage.pagenumber] = this.drotation;
                                    }

                                    this.rotateCanvas();
                                }
                            }

                            

                            break;
                        case 90:
                            if (degree == 0) {
                                for (i = 90; i >= 0; i = i - 10) {

                                    this.drotation = i;
                                    if(this.DocRef.pagerotations[thispage.pagenumber] != undefined){
                                        this.DocRef.pagerotations[thispage.pagenumber] = this.drotation;
                                    }

                                    this.rotateCanvas();
                                }
                            }
                            if (degree == 180) {
                                for (i = 90; i <= 180; i = i + 10) {
                                    this.drotation = i;
                                    if(this.DocRef.pagerotations[thispage.pagenumber] != undefined){
                                        this.DocRef.pagerotations[thispage.pagenumber] = this.drotation;
                                    }

                                    this.rotateCanvas();
                                }
                            }
                            if (degree == 270) {
                                for (i = 90; i <= 270; i = i + 10) {
                                    this.drotation = i;
                                    if(this.DocRef.pagerotations[thispage.pagenumber] != undefined){
                                        this.DocRef.pagerotations[thispage.pagenumber] = this.drotation;
                                    }

                                    this.rotateCanvas();
                                }
                            }
                            break;
                        case 180:
                            if (degree == 0) {
                                for (i = 180; i >= 0; i = i - 10) {
                                    this.drotation = i;
                                    if(this.DocRef.pagerotations[thispage.pagenumber] != undefined){
                                        this.DocRef.pagerotations[thispage.pagenumber] = this.drotation;
                                    }

                                    this.rotateCanvas();
                                }
                            }
                            if (degree == 270) {
                                for (i = 180; i <= 270; i = i + 10) {
                                    this.drotation = i;
                                    if(this.DocRef.pagerotations[thispage.pagenumber] != undefined){
                                        this.DocRef.pagerotations[thispage.pagenumber] = this.drotation;
                                    }

                                    this.rotateCanvas();
                                }
                            }
                            if (degree == 90) {
                                for (i = 180; i >= 90; i = i - 10) {
                                    this.drotation = i;
                                    if(this.DocRef.pagerotations[thispage.pagenumber] != undefined){
                                        this.DocRef.pagerotations[thispage.pagenumber] = this.drotation;
                                    }

                                    this.rotateCanvas();
                                }
                            }
                            break;
                        case 270:
                            if (degree == 0) {
                                for (i = 270; i >= 0; i = i - 10) {
                                    this.drotation = i;
                                    if(this.DocRef.pagerotations[thispage.pagenumber] != undefined){
                                        this.DocRef.pagerotations[thispage.pagenumber] = this.drotation;
                                    }

                                    this.rotateCanvas();
                                }
                            }
                            if (degree == 180) {
                                for (i = 270; i >= 180; i = i - 10) {
                                    this.drotation = i;
                                    if(this.DocRef.pagerotations[thispage.pagenumber] != undefined){
                                        this.DocRef.pagerotations[thispage.pagenumber] = this.drotation;
                                    }

                                    this.rotateCanvas();
                                }
                            }
                            if (degree == 90) {
                                for (i = 270; i >= 90; i = i - 10) {
                                    this.drotation = i;
                                    if(this.DocRef.pagerotations[thispage.pagenumber] != undefined){
                                        this.DocRef.pagerotations[thispage.pagenumber] = this.drotation;
                                    }

                                    this.rotateCanvas();
                                }
                            }
                            break;
                    }
                    //this.draw_image();
                    if (this.usevector3Dxml) {
                        return;
                    }
                    if (!this.usevectorxml) {
                        if (this.usepdfjs) {
                            /*if (thispage.usedincomposite && thispage.compositereference != undefined) {
                                //var pcanvheight = thispage.compositereference.ovpagecanvas.height;
                                //var pcanvwidth = thispage.compositereference.ovpagecanvas.width;
                                var pcanvheight = thispage.compositereference.overlaypage.getheight();
                                var pcanvwidth = thispage.compositereference.overlaypage.getwidth();
                            }
                            else {
                            }*/
                            var pcanvheight = thispage.offscreenheight;
                            var pcanvwidth = thispage.offscreenwidth;

                            if(thispage.getpageRotation() != 0){
                                pcanvwidth = thispage.originalPDFwidth;
                                pcanvheight = thispage.originalPDFheight;
                            }


                            var centery = canvasoheight / 2;
                            var centerx = canvasowidth / 2;
                            thispage.setpagedimsize(this.dxpdf, this.dypdf, (pcanvwidth * this.dscalepdf), (pcanvheight * this.dscalepdf), false);
                            thispage.PDFTextArea.style.left = thispage.startx + "px";
                            thispage.PDFTextArea.style.top = thispage.starty + "px";
                            var centerxoff = centerx - thispage.startx;
                            var centeryoff = centery - thispage.starty;
                            //var centerx = this.startx + ((this.endx - this.startx)/2);
                            //var centery = this.starty + ((this.endy - this.starty)/2);
                            thispage.PDFTextArea.style.transformOrigin = centerxoff + "px" + " " + centeryoff + "px";
                            thispage.PDFTextArea.style.transform = "rotate(" + this.getpageRotation() + "deg)";
                            if (thispage.pagenumber == thispage.DocRef.pages[thispage.DocRef.currentpage].pagenumber) {
                                /*if (this.usedincomposite && this.compositereference != undefined) {
                                    this.compositereference.scaleToBackground(false);
                                }
                                else {
                                    
                                }*/
                                thispage.DocRef.draw_mpagepdf(false);
                            }
                        }
                        else {
                            if (thispage.DocRef.Type == 0) {
                                thispage.DocRef.draw_mpage();
                            }
                            else {
                                /*if (this.usedincomposite && this.compositereference != undefined) {
                                    this.compositereference.scaleToBackground(false);
                                }
                                else {
                                    
                                }*/
                                thispage.draw_image(true);
                            }
                            //this.checkimageswitch();
                        }
                    }
                    else {
                        if(thispage.VectorPageObj != undefined){
                            thispage.setpagedimsize(this.dxvector, this.dyvector, (thispage.VectorPageObj.width * this.dscalevector), (thispage.VectorPageObj.height * this.dscalevector), false);
                            thispage.movetextdivs();
                            /*if (this.usedincomposite && this.compositereference != undefined) {
                                this.compositereference.scaleToBackground(false);
                            }
                            else {
                                
                            }*/
                            thispage.draw_vector(false);
                        }
                    }

                    /*if (thispage.DocRef.foxitdoc != null) {
                        thispage.DocRef.foxitdoc.pagerotate(thispage.pagenumber, thispage.getpageRotation());
                        //this.pagerotate = function(pagenum,  degrees){
                    }*/

                    if (RxCore_GUI_RotatePage != undefined) {
                        RxCore_GUI_RotatePage.onRotate(degree, thispage.pagenumber);
                    }

                    thispage.DocRef.refreshBirdsEye();
                    thispage.DocRef.renderBirdsEye();

                };

                this.foxitonRotate = function(degree, pagenumber){

                    if (RxCore_GUI_RotatePage != undefined) {
                        RxCore_GUI_RotatePage.onRotate(degree, pagenumber);
                    }

                    thispage.DocRef.refreshBirdsEye();
                    thispage.DocRef.renderBirdsEye();

                }

                this.rotateimage = function (degree) {
                    var bsinglepage = (this.DocRef.pages.length == 1);
                    //console.log(bsinglepage); 
                    if ((this.usepdfjs && !this.DocRef.pagelocked) && (this.usepdfjs && !bsinglepage)) {
                        this.pagelockrotation = degree;
                        return;
                    }

                    if (thispage.DocRef.foxitdoc != null) {

                        var foxitdegree = degree;
                        if(thispage.foxitpageorigrot != 0){
                            switch (thispage.foxitpageorigrot){
                                case 90 : 
                                    foxitdegree = degree - 90;

                                    if (foxitdegree < 0){
                                        foxitdegree = foxitdegree + 360;                             
                                    }

                                    break;
                                case 270 :
                                    foxitdegree = degree - 90;


                                    if (foxitdegree < 0){
                                        foxitdegree = foxitdegree + 360;                             
                                    }

                                    /*if (foxitdegree == 360){
                                        foxitdegree = 0;                             
                                    }*/
            

                                    break;    
                                case 180 :
                                    foxitdegree = degree + 180;

                                    if (foxitdegree >= 360){
                                        foxitdegree = foxitdegree - 360;                             
                                    }


                                    break;    

                            }
                        }



                        thispage.DocRef.foxitdoc.rotatePage(thispage.pagenumber, foxitdegree, this.foxitonRotate);
                        thispage.bpagesizeupdated = false;
                    }

                    var i = 0;

                    switch (this.getpageRotation()) {
                        case 0:
                            if (degree == 90) {
                                for (i = 0; i <= 90; i = i + 10) {

                                    this.drotation = i;

                                    if(this.DocRef.pagerotations[thispage.pagenumber] != undefined){
                                        this.DocRef.pagerotations[thispage.pagenumber] = this.drotation;
                                    }

                                    this.rotateCanvas();
                                }
                            }
                            if (degree == 180) {
                                for (i = 0; i <= 180; i = i + 10) {
                                    this.drotation = i;
                                    if(this.DocRef.pagerotations[thispage.pagenumber] != undefined){
                                        this.DocRef.pagerotations[thispage.pagenumber] = this.drotation;
                                    }

                                    this.rotateCanvas();
                                }
                            }
                            if (degree == 270) {
                                for (i = 0; i <= 270; i = i + 10) {
                                    this.drotation = i;
                                    if(this.DocRef.pagerotations[thispage.pagenumber] != undefined){
                                        this.DocRef.pagerotations[thispage.pagenumber] = this.drotation;
                                    }

                                    this.rotateCanvas();
                                }
                            }

                            

                            break;
                        case 90:
                            if (degree == 0) {
                                for (i = 90; i >= 0; i = i - 10) {
                                    this.drotation = i;
                                    if(this.DocRef.pagerotations[thispage.pagenumber] != undefined){
                                        this.DocRef.pagerotations[thispage.pagenumber] = this.drotation;
                                    }

                                    this.rotateCanvas();
                                }
                            }
                            if (degree == 180) {
                                for (i = 90; i <= 180; i = i + 10) {
                                    this.drotation = i;
                                    if(this.DocRef.pagerotations[thispage.pagenumber] != undefined){
                                        this.DocRef.pagerotations[thispage.pagenumber] = this.drotation;
                                    }

                                    this.rotateCanvas();
                                }
                            }
                            if (degree == 270) {
                                for (i = 90; i <= 270; i = i + 10) {
                                    this.drotation = i;
                                    if(this.DocRef.pagerotations[thispage.pagenumber] != undefined){
                                        this.DocRef.pagerotations[thispage.pagenumber] = this.drotation;
                                    }

                                    this.rotateCanvas();
                                }
                            }
                            break;
                        case 180:
                            if (degree == 0) {
                                for (i = 180; i >= 0; i = i - 10) {
                                    this.drotation = i;
                                    if(this.DocRef.pagerotations[thispage.pagenumber] != undefined){
                                        this.DocRef.pagerotations[thispage.pagenumber] = this.drotation;
                                    }

                                    this.rotateCanvas();
                                }
                            }
                            if (degree == 270) {
                                for (i = 180; i <= 270; i = i + 10) {
                                    this.drotation = i;
                                    if(this.DocRef.pagerotations[thispage.pagenumber] != undefined){
                                        this.DocRef.pagerotations[thispage.pagenumber] = this.drotation;
                                    }

                                    this.rotateCanvas();
                                }
                            }
                            if (degree == 90) {
                                for (i = 180; i >= 90; i = i - 10) {
                                    this.drotation = i;
                                    if(this.DocRef.pagerotations[thispage.pagenumber] != undefined){
                                        this.DocRef.pagerotations[thispage.pagenumber] = this.drotation;
                                    }

                                    this.rotateCanvas();
                                }
                            }
                            break;
                        case 270:
                            if (degree == 0) {
                                for (i = 270; i >= 0; i = i - 10) {
                                    this.drotation = i;
                                    if(this.DocRef.pagerotations[thispage.pagenumber] != undefined){
                                        this.DocRef.pagerotations[thispage.pagenumber] = this.drotation;
                                    }

                                    this.rotateCanvas();
                                }
                            }
                            if (degree == 180) {
                                for (i = 270; i >= 180; i = i - 10) {
                                    this.drotation = i;
                                    if(this.DocRef.pagerotations[thispage.pagenumber] != undefined){
                                        this.DocRef.pagerotations[thispage.pagenumber] = this.drotation;
                                    }

                                    this.rotateCanvas();
                                }
                            }
                            if (degree == 90) {
                                for (i = 270; i >= 90; i = i - 10) {
                                    this.drotation = i;
                                    if(this.DocRef.pagerotations[thispage.pagenumber] != undefined){
                                        this.DocRef.pagerotations[thispage.pagenumber] = this.drotation;
                                    }

                                    this.rotateCanvas();
                                }
                            }
                            break;
                    }
                    //this.draw_image();
                    if (this.usevector3Dxml) {
                        return;
                    }
                    if (!this.usevectorxml) {
                        if (this.usepdfjs) {
                            /*if (thispage.usedincomposite && thispage.compositereference != undefined) {
                                //var pcanvheight = thispage.compositereference.ovpagecanvas.height;
                                //var pcanvwidth = thispage.compositereference.ovpagecanvas.width;
                                var pcanvheight = thispage.compositereference.overlaypage.getheight();
                                var pcanvwidth = thispage.compositereference.overlaypage.getwidth();
                            }
                            else {
                            }*/
                            var pcanvheight = thispage.offscreenheight;
                            var pcanvwidth = thispage.offscreenwidth;

                            if(thispage.getpageRotation() != 0){
                                pcanvwidth = thispage.originalPDFwidth;
                                pcanvheight = thispage.originalPDFheight;
                            }


                            var centery = canvasoheight / 2;
                            var centerx = canvasowidth / 2;

                            thispage.setpagedimsize(this.dxpdf, this.dypdf, (pcanvwidth * this.dscalepdf), (pcanvheight * this.dscalepdf), false);
                            thispage.PDFTextArea.style.left = thispage.startx + "px";
                            thispage.PDFTextArea.style.top = thispage.starty + "px";
                            var centerxoff = centerx - thispage.startx;
                            var centeryoff = centery - thispage.starty;
                            //var centerx = this.startx + ((this.endx - this.startx)/2);
                            //var centery = this.starty + ((this.endy - this.starty)/2);
                            thispage.PDFTextArea.style.transformOrigin = centerxoff + "px" + " " + centeryoff + "px";
                            thispage.PDFTextArea.style.transform = "rotate(" + this.getpageRotation() + "deg)";
                            if (thispage.pagenumber == thispage.DocRef.pages[thispage.DocRef.currentpage].pagenumber) {
                                /*if (this.usedincomposite && this.compositereference != undefined) {
                                    this.compositereference.scaleToBackground(false);
                                }
                                else {
                                    
                                }*/
                                thispage.DocRef.draw_mpagepdf(false);
                            }
                        }
                        else {
                            if (thispage.DocRef.Type == 0) {
                                thispage.DocRef.draw_mpage();
                            }
                            else {
                                /*if (this.usedincomposite && this.compositereference != undefined) {
                                    this.compositereference.scaleToBackground(false);
                                }
                                else {
                                    
                                }*/
                                thispage.draw_image(true);
                            }
                            //this.checkimageswitch();
                        }
                    }
                    else {
                        if(thispage.VectorPageObj != undefined){
                            thispage.setpagedimsize(this.dxvector, this.dyvector, (thispage.VectorPageObj.width * this.dscalevector), (thispage.VectorPageObj.height * this.dscalevector), false);
                            thispage.movetextdivs();
                            /*if (this.usedincomposite && this.compositereference != undefined) {
                                this.compositereference.scaleToBackground(false);
                            }
                            else {
                                
                            }*/
                            thispage.draw_vector(false);
                        }
                    }

                    /*if (thispage.DocRef.foxitdoc != null) {
                        thispage.DocRef.foxitdoc.pagerotate(thispage.pagenumber, thispage.getpageRotation());
                        //this.pagerotate = function(pagenum,  degrees){
                    }*/

                    if (thispage.DocRef.foxitdoc == null) {

                        if (RxCore_GUI_RotatePage != undefined) {
                            RxCore_GUI_RotatePage.onRotate(degree, thispage.pagenumber);
                        }
    
                        thispage.DocRef.refreshBirdsEye();
                        thispage.DocRef.renderBirdsEye();
    
                    }


                };
                this.get_layers_html = function () {
                    return this.VectorPageObj.layerhtml;
                };
                this.enableSnap = function (state) {
                    thispage.snapEnabled = state;
                    /*if (state && thispage.usevectorxml){
                        thispage.VectorPageObj.getInterSections();
                    }*/
                };
                this.getSnapState = function () {
                    return thispage.snapEnabled;
                };
                this.setpentableUnit = function(munit){

                    if (thispage.usevectorxml) {
                        thispage.VectorPageObj.setpentableUnit(munit);
                        thispage.draw_vector(false);
                        thispage.vectorischanged = true;
                    }
                    
                };

                this.setpentableScaled = function(bscaled){

                    if (thispage.usevectorxml) {
                        thispage.VectorPageObj.setpentableScaled(bscaled);
                        thispage.draw_vector(false);
                        thispage.vectorischanged = true;
                    }
                };


                this.setPenTable = function(OnOff){
                    if (thispage.usevectorxml) {
                        thispage.VectorPageObj.setPenTable(OnOff);
                        thispage.draw_vector(false);
                        thispage.vectorischanged = true;
                    }

                };



                this.setPenwidth = function(penobj){
                    if (thispage.usevectorxml) {
                        thispage.VectorPageObj.setPenwidth(penobj);
                        //thispage.draw_vector(false);
                        //thispage.vectorischanged = true;

                    }

                };

                this.setPenTablepen = function(penobj){
                    if (thispage.usevectorxml) {
                        thispage.VectorPageObj.setPenTablepen(penobj);
                        thispage.draw_vector(false);
                        thispage.vectorischanged = true;
                    }

                };

                this.resetPenTablepen = function(penobj){
                    if (thispage.usevectorxml) {
                        thispage.VectorPageObj.resetPenTablepen(penobj);
                        thispage.draw_vector(false);
                        thispage.vectorischanged = true;
                    }

                };

                this.setpencolor = function(penindex, color){

                    if (thispage.usevectorxml) {
                        thispage.VectorPageObj.setpencolor(penindex, color);
                        //thispage.draw_vector(false);
                        //thispage.vectorischanged = true;
                    }


                };

                this.resetpencolor = function(penindex){

                    if (thispage.usevectorxml) {
                        thispage.VectorPageObj.resetpencolor(penindex);
                        //thispage.draw_vector(false);
                        //thispage.vectorischanged = true;
                    }

                };

                /*this.setpenwidth = function(penindex, uwidth){
                    if (thispage.usevectorxml) {
                        thispage.VectorPageObj.setpenwidth(penindex, uwidth);
                        //thispage.draw_vector(false);
                        //thispage.vectorischanged = true;
                    }

                };*/
                
                this.resetpenwidth = function(penindex){

                    if (thispage.usevectorxml) {
                        thispage.VectorPageObj.resetpenwidth(penindex);
                        //thispage.draw_vector(false);
                        //thispage.vectorischanged = true;
                    }
                    
                };                
                this.setViewMode = function (OnOff) {
                    if (thispage.usevectorxml) {
                        thispage.VectorPageObj.ViewMode(OnOff);
                        thispage.draw_vector(false);
                        thispage.vectorischanged = true;
                    }
                };
                this.blockAttributes = function (blockid) {
                    if (thispage.usevectorxml) {
                        return thispage.VectorPageObj.blockAttributes(blockid);
                    }
                };

                this.markVectorBlock = function(blockid){
                    if (thispage.usevectorxml) {
                        thispage.VectorPageObj.markblockinsert(blockid, thispage.dscalevector, thispage.dxvector, thispage.dyvector);
                        //thispage.draw_vector(false);
                        //thispage.vectorischanged = true;
                    }

                };
                this.selectBlock = function (blockid) {
                    if (thispage.usevectorxml) {
                        thispage.VectorPageObj.selectBlock(blockid);
                        thispage.draw_vector(false);
                        thispage.vectorischanged = true;
                    }
                };
                this.getpixeldist = function (length) {
                    if (thispage.usevectorxml) {
                        var origlength = length * thispage.VectorPageObj.scale;
                        var pixelLength = origlength * thispage.dscalevector;
                        return pixelLength;
                    }
                    else {
                        return length;
                    }
                };
                this.getpolygonPointsbyblockID = function (blockid) {
                    var points = [];
                    if (thispage.usevectorxml) {
                        points = thispage.VectorPageObj.getpathPointsBybId(blockid);
                    }
                    return points;
                };
                this.getpolygonPathbypathIndex = function (index) {
                    var path = null;
                    if (thispage.usevectorxml) {
                        path = thispage.VectorPageObj.getpathbyindex(index);
                    }
                    return path;
                };
                this.getpolygonPathbyblockID = function (blockid) {
                    var path = null;
                    if (thispage.usevectorxml) {
                        path = thispage.VectorPageObj.getpathbybId(blockid);
                    }
                    return path;
                };
                this.getpolygonPath = function (x, y) {
                    var path = null;
                    if (thispage.usevectorxml) {
                        path = thispage.VectorPageObj.getpath(x, y, thispage.dscalevector, thispage.dxvector, thispage.dyvector);
                    }
                    return path;
                };
                this.getpolygonPoints = function (x, y) {
                    var points = [];
                    if (thispage.usevectorxml) {
                        points = thispage.VectorPageObj.getpathPoints(x, y, thispage.dscalevector, thispage.dxvector, thispage.dyvector);
                    }
                    return points;
                };
                this.createAreaAttributes = function(szAttname){
                    if (thispage.usevectorxml) {
                        thispage.VectorPageObj.createAreaAttributes(szAttname);
                    }
                };
                this.setVectorBlockArea = function(partindex, foundblock, szAttname){
                    if (thispage.usevectorxml) {
                        
                        thispage.VectorPageObj.setBlockArea(partindex, foundblock, szAttname);
                    }
                };

                this.getinsideHyperlink = function(x, y, scalefactor, offsetx, offsety, hover){

                    
                    var bwithin = false;
                    var screenmouse  = { x: x, y: y };
                    var rotatedpoint = { x: x, y: y };
                    
                    
                    if(thispage.VectorPageObj == undefined){
                        return false;

                    }

                    if (thispage.drotation != 0) {
                        rotatedpoint = mouse_rotated(x, y);
                    }
                    x = rotatedpoint.x;
                    y = rotatedpoint.y;


                    for (var i = 0; i < thispage.hyperlinks.length; i++) {
                        
                        var within = thispage.hyperlinks[i].insideHyperlink(x, y, scalefactor, offsetx, offsety, thispage.VectorPageObj.x, thispage.VectorPageObj.y, thispage.VectorPageObj.h, thispage.pagenumber, hover);
                            if (within) {
                                bwithin = true;
                            }
                            else {
                                

                            }

                            //thispage.hyperlinks[i].drawhyperlink(contexto, scalefactor, offsetx, offsety,  thispage.VectorPageObj.x, thispage.VectorPageObj.y, thispage.VectorPageObj.h);
                        
                    }
                    

                    /*if(!hover){
                        if (RxCore_GUI_2DBlockID != undefined) {
                            RxCore_GUI_2DBlockID.set2DBlockID(selected);
                        }
                    }
                    if (!multi) {
                        
                        if(!hover){
                            if (RxCore_GUI_2DBlockInfo != undefined) {
                                RxCore_GUI_2DBlockInfo.set2DBlockInfo(partlist);
                            }
                        }

                        if (hover){
                            if (RxCore_GUI_2DBlockInfoScreen != undefined) {
                                //RxCore_GUI_2DBlockInfoScreen.set2DBlockInfo(partlist, screenmouse, pathindex);
                                RxCore_GUI_2DBlockInfoScreen.set2DBlockInfo(partlistAll, screenmouse, pathindex);
                            }
    
                        }

                    }*/
                    return bwithin;

                };


                this.gethyperlink = function(x,y){
                    var within = false;

                    if (thispage.usevectorxml) {
                        within = thispage.getinsideHyperlink(x, y, thispage.dscalevector, thispage.dxvector, thispage.dyvector, false);
                        //thispage.vectorischanged = true;
                    }
                    return within;



                };

                this.gethyperlinkhover = function(x,y){
                    var within = false;

                    if (thispage.usevectorxml) {
                        within = thispage.getinsideHyperlink(x, y, thispage.dscalevector, thispage.dxvector, thispage.dyvector, true);
                        //thispage.vectorischanged = true;
                    }
                    return within;

                };                

                this.getvectorentity = function(x,y){

                    var within = false;

                    if (thispage.usevectorxml) {
                        within = thispage.VectorPageObj.getvectorinformation(x, y, thispage.dscalevector, thispage.dxvector, thispage.dyvector, false);
                        thispage.draw_vector(false);
                        thispage.vectorischanged = true;
                        //thispage.vectorischanged = true;
                    }

                    return within;


                };


                this.getvectorentityhover = function(x,y){

                    var within = false;

                    if (thispage.usevectorxml) {
                        within = thispage.VectorPageObj.getvectorinformation(x, y, thispage.dscalevector, thispage.dxvector, thispage.dyvector, true);
                        //this.getvectorinformation = function (x, y, scalefactor, offsetx, offsety, hover){
                        thispage.draw_vector(false);
                        thispage.vectorischanged = true;
                        //thispage.vectorischanged = true;
                    }

                    return within;


                };

                this.getpolygonhover = function (x, y) {

                    var within = false;

                    if (thispage.usevectorxml) {
                        within = thispage.VectorPageObj.getinsidePolygon(x, y, thispage.dscalevector, thispage.dxvector, thispage.dyvector, false, true);
                        thispage.draw_vector(false);
                        thispage.vectorischanged = true;
                        //thispage.vectorischanged = true;
                    }

                    return within;
                };

                this.getpolygon = function (x, y, multi) {
                    
                    var within = false;
                    if (thispage.usevectorxml) {
                        within = thispage.VectorPageObj.getinsidePolygon(x, y, thispage.dscalevector, thispage.dxvector, thispage.dyvector, multi, false);
                        thispage.draw_vector(false);
                        thispage.vectorischanged = true;
                        //thispage.vectorischanged = true;
                    }
                    return within;
                };

                this.rendervectormouseover = function(){

                    if (thispage.usevectorxml) {

                        if(thispage.VectorPageObj != undefined){
                            thispage.VectorPageObj.rendermouseonVectors(thispage.dscalevector, thispage.dxvector, thispage.dyvector, thispage.drotation);
                        }
                        
                    }

                };

                this.renderSelectedBlocks = function(){

                    if (thispage.usevectorxml) {

                        if(thispage.VectorPageObj != undefined){
                            thispage.VectorPageObj.renderSelectedBlocks(thispage.dscalevector, thispage.dxvector, thispage.dyvector, thispage.drotation);
                        }

                        
                    }

                    

                };

                this.getblockinserthover = function(x,y){
                    var within = false;


                    if (thispage.usevectorxml) {
                        within = thispage.VectorPageObj.getinsideblockinsert(x, y, thispage.dscalevector, thispage.dxvector, thispage.dyvector, thispage.drotation, true);
                        //this.getvectorinformation = function (x, y, scalefactor, offsetx, offsety, hover){
                        thispage.draw_vector(false);
                        thispage.vectorischanged = true;
                        //thispage.vectorischanged = true;
                    }
                    
                    return within;

                };

                this.getblockinsert = function(x,y){
                    var within = false;


                    if (thispage.usevectorxml) {
                        within = thispage.VectorPageObj.getinsideblockinsert(x, y, thispage.dscalevector, thispage.dxvector, thispage.dyvector, thispage.drotation, false);
                        //this.getvectorinformation = function (x, y, scalefactor, offsetx, offsety, hover){
                        thispage.draw_vector(false);
                        thispage.vectorischanged = true;
                        //thispage.vectorischanged = true;
                    }

                    
                    return within;

                    

                }

                this.unSelectAllBlockInserts = function(){
                    if (thispage.usevectorxml) {

                        if(thispage.VectorPageObj){
                            thispage.VectorPageObj.unSelectAllBlockInserts();
                        }
                        
                    }

                };

                this.unSelectAllBlocks = function (notify) {
                    if (thispage.usevectorxml) {
                        thispage.VectorPageObj.unSelectAllBlocks(notify);
                        thispage.draw_vector(false);
                        thispage.vectorischanged = true;
                    }
                };

                this.getPDFcoordsPos = function(x,y, offsetx, offsety, scale){
                    var pdfx = x;
                    var pdfy = y;
                                    
                    if(thispage.getpageRotation() == 0){
                        //SearcMatches[sm][pm].match.rectArray[0].left = (x - thispage.dxpdf) / (thispage.curpagescale * thispage.dscalepdf); 
                        pdfx = (x - offsetx) / (thispage.curpagescale * scale); 
                        pdfy = -(((y - offsety) / (thispage.curpagescale * scale)) - thispage.pdfpageheight);
                        
                    }else{
                        // calculate rotated point.
                        pdfx = (x - offsetx) / (thispage.curpagescale * scale); 
                        pdfy = -(((y - offsety) / (thispage.curpagescale * scale)) - thispage.pdfpageheight);
                    }

                    if(pdfx < 0 ){
                        pdfx = 0;
                    }

                    if(pdfy < 0 ){
                        pdfy = 0;
                    }

                    return {x : pdfx, y : pdfy};
                };

                this.getPDFDist = function(dist){
                    //get screeen to PDF distance

                    return dist / (thispage.curpagescale * thispage.dscalepdf);


                }

                this.getPDFDistScale = function(dist, scale){
                    //get screeen to PDF distance

                    return dist / (thispage.curpagescale * scale);


                }



                function getcombinedangle(pagerot, pdfrot){
                    var realangle = 0;
                    

                    
                    if((pdfrot + pagerot) >=360 ){
                        realangle = (pagerot + pdfrot) - 360;
                    }else{
                        realangle = pagerot + pdfrot
                    }

                    return realangle;


                }

                this.returnFoxitpageCoords = function(pdfpoint){

                    console.log(pdfpoint);

                };
                
                this.getFoxitpageCoords = function(mousePos){
                    
                    if (thispage.DocRef.foxitdoc != null) {
                        thispage.DocRef.foxitdoc.getPDFpagecoordinates(mousePos, thispage.pagenumber, thispage.returnFoxitpageCoords);
                    }   

                };

                this.getPDFCoordsRot = function(mousePos){

                    var pdfx = mousePos.x;
                    var pdfy = mousePos.y;
                    var pdfytemp = mousePos.y;

                    pdfx = mousePos.x;
                    pdfy = mousePos.y;
                    pdfytemp = mousePos.y;


                    var rotatedpoint = { x: mousePos.x, y: mousePos.y };



                    var realangle = getcombinedangle(thispage.getpageRotation(), thispage.foxitpageorigrot);

                    //rotatedpoint = mouse_rotatedAngle(rotatedpoint.x, rotatedpoint.y, realangle);
                    

                    var dxpdfrot = thispage.dxpdfzerorot;
                    var dypdfrot = thispage.dypdfzerorot;



                    pdfx = (rotatedpoint.x - dxpdfrot) / (thispage.curpagescale * thispage.dscalepdf); 
                    pdfy = (rotatedpoint.y - dypdfrot) / (thispage.curpagescale * thispage.dscalepdf);
                    pdfytemp = -((pdfy) - thispage.pdfpageheight);

                    

                    /*if(realangle == 90 || realangle == 270){
                        if(thispage.bpagesizeupdated){

                            if(realangle == 270){
                                pdfytemp = -((pdfy) - thispage.pdfpagewidth);
                            }else if(realangle == 90) {
                                pdfytemp = -((pdfy) - thispage.pdfpagewidth);
                            }

                            
                        }
                    }*/

                    pdfy = pdfytemp;


                    return {x : pdfx, y : pdfy, mx : rotatedpoint.x, my : rotatedpoint.y};

                };

                this.getPDFcoords = function(x,y){

                    //var curpage = DocObj.getPageObject(thispage.pagenumber);
                    var pdfx = x;
                    var pdfy = y;
                    var pdfytemp = y;

                    var pdfxnorot = x;
                    var pdfynorot = y;



                    var pdfcenter = {x : thispage.originalPDFwidth * 0.5 , y : thispage.originalPDFheight * 0.5 };
                    var widthtoheightdiff = thispage.originalPDFwidth - thispage.originalPDFheight;
                    var rotatedpoint = { x: x, y: y };


                    /*if(thispage.getpageRotation() == 90 || thispage.getpageRotation() == 270){
                        if(thispage.bpagesizeupdated){
                            pdfcenter = {x : thispage.originalPDFheight * 0.5 , y : thispage.originalPDFwidth * 0.5 };
                            widthtoheightdiff = thispage.originalPDFheight - thispage.originalPDFwidth;

                        }
                    }*/




                    //this.pdfpageheight = 0.0;
                    //this.pdfpagewidth = 0.0;

                    var dxpdfrot = thispage.dxpdf;
                    var dypdfrot = thispage.dypdf;

                    var dxpdfnorot = thispage.dxpdfnonrot;
                    var dypdfnorot = thispage.dypdfnonrot;

                    
                    var offsetpos = { x: x - dxpdfrot, y: y - dypdfrot };

                    var offsetposnorot = { x: x - dxpdfnorot, y: y - dypdfnorot };
                    
                    pdfx = (x - dxpdfrot) / (thispage.curpagescale * thispage.dscalepdf); 
                    pdfy = (y - dypdfrot) / (thispage.curpagescale * thispage.dscalepdf);
                    pdfytemp = -((pdfy) - thispage.pdfpageheight);

                    if(thispage.getpageRotation() == 90 || thispage.getpageRotation() == 270){
                        if(thispage.bpagesizeupdated){

                            if(thispage.getpageRotation() == 270){
                                pdfytemp = -((pdfy) - thispage.pdfpagewidth);
                            }else if(thispage.getpageRotation() == 90) {
                                pdfytemp = -((pdfy) - thispage.pdfpagewidth);
                            }

                            
                        }
                    }

                    pdfy = pdfytemp;



                    pdfxnorot = (x - dxpdfnorot) / (thispage.curpagescale * thispage.dscalepdf); 
                    pdfynorot = (y - dypdfnorot) / (thispage.curpagescale * thispage.dscalepdf);
                    pdfynorot = -((pdfynorot) - thispage.pdfpageheight);

                    if(thispage.foxitpageorigrot == 270 && thispage.getpageRotation() == 90){
                        //use non rotated pdfx and pdfy
                        pdfy = pdfynorot;
                        pdfx = pdfxnorot;

                    }

                    if(thispage.foxitpageorigrot == 90 && thispage.getpageRotation() == 270){
                        //use non rotated pdfx and pdfy
                        pdfy = pdfynorot;
                        pdfx = pdfxnorot;

                    }


                    /*if(thispage.getpageRotation() != 0){

                        return {x : pdfx, y : pdfy, mx : x, my : y};

                    }*/

                    /*var currentrotation = thispage.foxitpageorigrot - thispage.getpageRotation();
                    currentrotation = currentrotation + 180;*/

                    if(thispage.getpageRotation() == 0){

                        switch (thispage.foxitpageorigrot){

                            case 270 :
    
                                pdfx = pdfx - pdfcenter.x;
                                pdfy = pdfy - pdfcenter.y;
    
                                var swap = pdfx;
                                pdfx = pdfy;
                                pdfy = -swap;
    
                                pdfx = pdfx + pdfcenter.x;
                                pdfy = pdfy + pdfcenter.y;
    
                                pdfx -= (widthtoheightdiff * 0.5);
                                pdfy += (widthtoheightdiff * 0.5);
    
                                //norotation assumed for pdf
                                pdfxnorot = pdfxnorot - pdfcenter.x;
                                pdfynorot = pdfxnorot - pdfcenter.y;
            
                                swap = pdfxnorot;
                                pdfxnorot = pdfynorot;
                                pdfynorot = -swap;
    
                                pdfxnorot = pdfxnorot + pdfcenter.x;
                                pdfynorot = pdfynorot + pdfcenter.y;
    
                                pdfxnorot -= (widthtoheightdiff * 0.5);
                                pdfynorot += (widthtoheightdiff * 0.5);
    
                                //norotation assumed for pdf
    
                                break;
                            case 90 : 
    
                                pdfx = pdfx - pdfcenter.x;
                                pdfy = pdfy - pdfcenter.y;
    
                                swap = pdfx;
                                pdfx = -pdfy;
                                pdfy = swap;
    
                                pdfx = pdfx + pdfcenter.x;
                                pdfy = pdfy + pdfcenter.y;
    
                                pdfx -= (widthtoheightdiff * 0.5);
                                pdfy += (widthtoheightdiff * 0.5);
    
    
                                //norotation assumed for pdf
                                pdfxnorot = pdfxnorot - pdfcenter.x;
                                pdfynorot = pdfxnorot - pdfcenter.y;
            
                                swap = pdfxnorot;
                                pdfxnorot = -pdfynorot;
                                pdfynorot = swap;
                                                                
                                pdfxnorot = pdfxnorot + pdfcenter.x;
                                pdfynorot = pdfynorot + pdfcenter.y;
    
                                pdfxnorot -= (widthtoheightdiff * 0.5);
                                pdfynorot += (widthtoheightdiff * 0.5);
    
                                //norotation assumed for pdf
                                
    
    
                                /*pdfx = pdfx - pdfcenter.x;
                                pdfy = pdfy - pdfcenter.y;
    
                                swap = pdfx;
                                pdfx = pdfy;
                                pdfy = -swap;
    
                                pdfx = pdfx + pdfcenter.x;
                                pdfy = pdfy + pdfcenter.y;
    
                                pdfx -= (widthtoheightdiff * 0.5);
                                pdfy += (widthtoheightdiff * 0.5);*/
    
                                break;
                            case 180 : 
    
                                pdfx = pdfx - pdfcenter.x;
                                pdfy = pdfy - pdfcenter.y;
    
                                //swap = pdfx;
                                pdfx = -pdfx;
                                pdfy = -pdfy;
    
                                pdfx = pdfx + pdfcenter.x;
                                pdfy = pdfy + pdfcenter.y;
    
                                //norotation assumed for pdf
                                pdfxnorot = pdfxnorot - pdfcenter.x;
                                pdfynorot = pdfxnorot - pdfcenter.y;
            
                                //swap = pdfxnorot;
                                pdfxnorot = -pdfxnorot;
                                pdfynorot = -pdfynorot;
                                                                
                                pdfxnorot = pdfxnorot + pdfcenter.x;
                                pdfynorot = pdfynorot + pdfcenter.y;
    
                                //pdfxnorot -= (widthtoheightdiff * 0.5);
                                //pdfynorot += (widthtoheightdiff * 0.5);
    
                                //norotation assumed for pdf
    
    
                                //pdfx -= (widthtoheightdiff * 0.5);
                                //pdfy += (widthtoheightdiff * 0.5);
            
                                break;
    
                        }
                    }
                    
                                    
                    if(thispage.foxitpagerot == 0){

                        
                        //SearcMatches[sm][pm].match.rectArray[0].left = (x - thispage.dxpdf) / (thispage.curpagescale * thispage.dscalepdf); 
                        //pdfx = (x - dxpdfrot) / (thispage.curpagescale * thispage.dscalepdf); 
                        //pdfy = -(((y - dypdfrot) / (thispage.curpagescale * thispage.dscalepdf)) - thispage.pdfpageheight);
                        
                        
                    }else{

                        

                        //var scrcentre = getcentrepoint();
                        //var radrot = getrotrad(thispage.foxitpagerot);
                        //var rotatedpoint = mouse_rotatedPage(pdfx,pdfy, radrot, pdfcenter);

                        //console.log(rotatedpoint.x - dypdfrot, rotatedpoint.y - dxpdfrot);

                        //pdfx = rotatedpoint.x;
                        //pdfy = rotatedpoint.y;

                        //thispage.pdfpageheight is still height even with rotation same as pdfpagewidth

                        if(thispage.getpageRotation() == 0){

                            
                        }

                        


                        //thispage.originalPDFwidth = 0;
                        //this.originalPDFheight = 0;
        
                        // calculate rotated point.

                        
                    }

                    //console.log({x : pdfx, y : pdfy, mx : x, my : y});
                    //console.log({x : pdfxnorot, y : pdfynorot, mx : x, my : y});


                    return {x : pdfx, y : pdfy, mx : x, my : y};
                    //return {x : pdfxnorot, y : pdfynorot, mx : x, my : y};


                }; 


                this.getPDFSnapPointArray = function(x,y){

                    
                    //var pdfsnap = thispage.PDFSnapArray.pop();

                    //thispage.PDFSnapArray.pop();

                    var rotatedpoint = { x: x, y: y };

                    //assume pdf rotation not used
                    /*if (thispage.getpageRotation() != 0) {
                        rotatedpoint = mouse_rotated(x, y);
                    }*/

                    x = rotatedpoint.x;
                    y = rotatedpoint.y;

                    var snapradius = 20;


                    var pdfsnap = thispage.PDFSnapPoint;

                    var snappoint = {
                        found: false,
                        x: 0,
                        y: 0,
                        type: 1
                    };
                    
                    if (pdfsnap != undefined){

                        var a = x - pdfsnap.x;
                        var b = y - pdfsnap.y;

                        var tempdist = Math.sqrt(a * a + b * b);
                        if (tempdist < snapradius) {
                            //snappoints.push({ x: transx, y: transy });
                            snappoint = pdfsnap;
                            //console.log(snappoint);
                            //this.getPDFSnapPointArray
                            
                        }
                       
                    }
                    //console.log(snappoint);
                    return snappoint;


                };

                this.setPDFSnapPointArray = function(pdfsnapPoint){

                    //thispage.PDFSnapArray.push(pdfsnapPoint);

                    thispage.PDFSnapPoint = pdfsnapPoint;

                };

                this.adjustpointpdfsnap = function(pdfsnapPoint){
                    //var pdfx = pdfsnapPoint.x;
                    //var pdfy = pdfsnapPoint.y;
                    var xpadd = 2;
                    var ypadd = 1;
                                    
                    if(thispage.getpageRotation() == 0){
                        //pdfx += thispage.dxpdf; 
                        //pdfx += thispage.dypdf; 
                        //pdfsnapPoint.x = pdfsnapPoint.x + thispage.dxpdf + xpadd;
                        //pdfsnapPoint.y = pdfsnapPoint.y + thispage.dypdf + ypadd;

                        pdfsnapPoint.x = pdfsnapPoint.x + thispage.dxpdf;// + xpadd;
                        pdfsnapPoint.y = pdfsnapPoint.y + thispage.dypdf;// + ypadd;

                            
                    }else{
                        // calculate rotated point.

                        switch(thispage.getpageRotation()){
                            case 90 : 
                                xpadd = 2;
                                ypadd = 1;
    
                                break;
                            case 270 :
                                xpadd = 2;
                                ypadd = 1;

                                break;
                            case 180 :
                                xpadd = 2;
                                ypadd = 1;

                                break;
                        }

                        //pdfsnapPoint.x = pdfsnapPoint.x + thispage.dxpdf;
                        //pdfsnapPoint.y = pdfsnapPoint.y + thispage.dypdf;

                        pdfsnapPoint.x = pdfsnapPoint.x + thispage.dxpdfnonrot;// + xpadd;
                        pdfsnapPoint.y = pdfsnapPoint.y + thispage.dypdfnonrot;// + ypadd;

                        //pdfx += thispage.dxpdf; 
                        //pdfx += thispage.dypdf;
                        //pdfsnapPoint.x = pdfx;
                        //pdfsnapPoint.y = pdfy;
                    }

                    return pdfsnapPoint;

                };

                this.foxitsnapCallback = function(pdfsnapPoint){
                    

                    /*var snappoint = {
                        found: true,
                        x: pdfsnapPoint.x,
                        y: pdfsnapPoint.y,
                        type: 1
                    };*/

                    if(pdfsnapPoint.found){
                        //console.log(pdfsnapPoint);
                        pdfsnapPoint = thispage.adjustpointpdfsnap(pdfsnapPoint);

                        //thispage.setPDFSnapPointArray(pdfsnapPoint);

                    }else{
                        //clearpoint here.
                    }

                    thispage.setPDFSnapPointArray(pdfsnapPoint);
                    thispage.pdfsnapready = true;

                    /*var snappoint = {
                        found: false,
                        x: 0,
                        y: 0,
                        type: 1
                    };*/


                };


                this.getPDFsnapPoint = function (x, y) {
                    var snappoint = {
                        found: false,
                        change : false,
                        x: 0,
                        y: 0,
                        type: snapEndpoint
                    };

                    /*var rotatedpoint = { x: x, y: y };
                    var norotpoint = { x: x, y: y };

                    if (thispage.getpageRotation() != 0) {

                        if(thispage.foxitpageorigrot == 270 && thispage.getpageRotation() == 90){
                            rotatedpoint.x = x;
                            rotatedpoint.y = y;
                        }else if(thispage.foxitpageorigrot == 90 && thispage.getpageRotation() == 270){
                            rotatedpoint.x = x;
                            rotatedpoint.y = y;
                        }else{
                            rotatedpoint = mouse_rotated(x, y);
                        }
                        
                    }

                    x = rotatedpoint.x;
                    y = rotatedpoint.y;*/

                    var point = { x: x, y: y };


                    if (thispage.DocRef.foxitdoc != null && buseFoxitSnap) {

                        //var pdfpoint = thispage.getPDFcoords(x,y);
                        //var pdfpoint = thispage.getPDFCoordsRot(norotpoint);

                        //thispage.getFoxitpageCoords(norotpoint);

                        if(thispage.pdfsnapready){

                            var page = thispage.DocRef.foxitdoc.pdfViewer.getCurrentPDFDoc().getPageByIndex(thispage.pagenumber);
                            var internalRotation = thispage.getpageRotation();


                            if(internalRotation == 90 || internalRotation == 270){
                                thispage.DocRef.foxitdoc.getSnapPointRotate(
                                    thispage.pagenumber, 
                                    point.x, 
                                    point.y,  
                                    thispage.foxitsnapCallback
                                );    
                            } else {
                                var pdfpoint = thispage.getPDFCoordsRot(point);
                                thispage.DocRef.foxitdoc.getSnapPoint(
                                    thispage.pagenumber, 
                                    pdfpoint.x, 
                                    pdfpoint.y, 
                                    thispage.foxitsnapCallback
                                );
                            }




                            /*if(buseFoxitrotateTo){
                                thispage.DocRef.foxitdoc.getSnapPointRotate(thispage.pagenumber, pdfpoint.x, pdfpoint.y, thispage.getpageRotation(), thispage.foxitsnapCallback);    
                            }else{
                                thispage.DocRef.foxitdoc.getSnapPoint(thispage.pagenumber, pdfpoint.x, pdfpoint.y, thispage.foxitsnapCallback);
                            }*/


                            thispage.pdfsnapready = false;

                            //return thispage.getPDFSnapPointArray(norotpoint.x, norotpoint.y);
                            return thispage.getPDFSnapPointArray(point.x, point.y);
                            //return thispage.getPDFSnapPointArray(x, y);
    
                        }


                        //thispage.DocRef.foxitdoc.getBitmap(thispage.pagenumber, scale, thispage.setPageBitmap);
                        
                        
                    }

                    var rotatey = false;
                    var snappoints = [];
                    
                    //var rotatedpoint = { x: x, y: y };

                    /*if (thispage.getpageRotation() != 0) {
                        rotatedpoint = mouse_rotated(x, y);
                    }*/


                    //x = rotatedpoint.x;
                    //y = rotatedpoint.y;
                    /*if(thispage.PDFinternalScale == undefined || thispage.PDFPointArray == undefined){
                        return snappoint;
                    }*/
                    if (thispage.PDFPointArray == undefined) {
                        return snappoint;
                    }
                    var snapradius = 10;

                    if (thispage.PDFinternalScale.x > 0) {
                        var localscalex = thispage.pdfSnapScale * thispage.PDFinternalScale.x;
                    }
                    else {
                        localscalex = thispage.pdfSnapScale;
                    }
                    if (thispage.PDFinternalScale.y > 0) {
                        var localscaley = thispage.pdfSnapScale * thispage.PDFinternalScale.y;
                    }
                    else {
                        localscaley = thispage.pdfSnapScale;
                        rotatey = true;
                    }
                    var cheight = thispage.offscreenheight * thispage.dscalepdf;
                    var cwidth = thispage.offscreenwidth * thispage.dscalepdf;
                    //var cheight = thispage.offscreenheight;
                    //var cwidth = thispage.offscreenwidth;
                    for (var i = 0; i < thispage.PDFPointArray.length; i++) {
                        /*if (rotatey){
                            //var transy = thispage.starty + ((thispage.PDFPointArray[i].y*localscaley) + cheight);
                            var transy = thispage.starty + ((thispage.PDFPointArray[i].y*thispage.pdfSnapScale) + cheight);
                        }else{
                            //transy = thispage.starty + (-(thispage.PDFPointArray[i].y*localscaley) + cheight);
                            transy = thispage.starty + (-(thispage.PDFPointArray[i].y*thispage.pdfSnapScale) + cheight);
                        }
        
                        //var transx = thispage.startx + (thispage.PDFPointArray[i].x*localscalex);
                        if(thispage.PDFpageRotate != 0){
                            var transx = thispage.startx + (thispage.PDFPointArray[i].x*thispage.pdfSnapScale) + cwidth;
                        }else{
                            transx = thispage.startx + (thispage.PDFPointArray[i].x*thispage.pdfSnapScale);
                        }*/
                        if (thispage.PDFpageRotate != 0) {
                            if (thispage.PDFpageRotate == 90) {
                                var transy = thispage.starty + (-(thispage.PDFPointArray[i].y * thispage.pdfSnapScale));
                                var transx = thispage.startx + (thispage.PDFPointArray[i].x * thispage.pdfSnapScale);
                            }
                            else {
                                transy = thispage.starty + (-(thispage.PDFPointArray[i].y * thispage.pdfSnapScale) + cheight);
                                transx = thispage.startx + (thispage.PDFPointArray[i].x * thispage.pdfSnapScale) + cwidth;
                            }
                        }
                        else {
                            if (thispage.PDFPointArray[i].y < 0) {
                                transy = thispage.starty + (-(thispage.PDFPointArray[i].y * thispage.pdfSnapScale));
                            }
                            else {
                                transy = thispage.starty + (-(thispage.PDFPointArray[i].y * thispage.pdfSnapScale) + cheight);
                            }
                            if (thispage.PDFPointArray[i].x < 0) {
                                transx = thispage.startx + (thispage.PDFPointArray[i].x * thispage.pdfSnapScale) + cwidth;
                            }
                            else {
                                transx = thispage.startx + (thispage.PDFPointArray[i].x * thispage.pdfSnapScale);
                            }
                        }
                        var a = x - transx;
                        var b = y - transy;
                        var tempdist = Math.sqrt(a * a + b * b);
                        if (tempdist < snapradius) {
                            snappoints.push({ x: transx, y: transy });
                        }
                    }
                    if (snappoints.length > 0) {
                        a = x - snappoints[0].x;
                        b = y - snappoints[0].y;
                        var curdist = Math.sqrt(a * a + b * b);
                        tempdist = Math.sqrt(a * a + b * b);
                        var snapindx = 0;
                        for (i = 0; i < snappoints.length; i++) {
                            a = x - snappoints[i].x;
                            b = y - snappoints[i].y;
                            tempdist = Math.sqrt(a * a + b * b);
                            if (tempdist < curdist) {
                                curdist = tempdist;
                                snapindx = i;
                            }
                        }
                        snappoint = {
                            found: true,
                            x: snappoints[snapindx].x,
                            y: snappoints[snapindx].y,
                            type: snapEndpoint
                        };
                    }
                    else {
                        snappoint = {
                            found: false,
                            change : false,
                            x: 0,
                            y: 0,
                            type: snapEndpoint
                        };
                    }
                    return snappoint;
                };

                this.getsnap = function (mousepos) {

                    if (thispage.usevectorxml && thispage.snapEnabled) {

                        if(thispage.VectorPageObj != undefined){
                            return thispage.VectorPageObj.getsnapPoint(mousepos.x, mousepos.y, thispage.dscalevector, thispage.dxvector, thispage.dyvector);
                            //return thispage.VectorPageObj.getsnapPoint(x, y, thispage.dscalevector, thispage.dxvector, thispage.dyvector);
                        }else{
                            //return { found: false, x: 0, y: 0 };
                            return { found: false,change: false, x: 0, y: 0 };
                        }
                        
                    }
                    else if (thispage.usepdfjs && thispage.snapEnabled) {
                        //return thispage.getPDFsnapPoint(x, y);
                        var snappoint = thispage.getPDFsnapPoint(mousepos.xus, mousepos.yus);

                        var pixelRatio = getPixelRatio();

                        snappoint.x *= pixelRatio; 
                        snappoint.y *= pixelRatio; 

                        return snappoint;
                    }
                    else {
                        //return { found: false, x: 0, y: 0 };
                        return { found: false, change: false, x: 0, y: 0 };
                    }
                    //var snapPoint = this.VectorPageObj.getsnapPoint(x,y,this.dscalevector,this.dxvector,this.dyvector);
                    //return this.VectorPageObj.getsnapPoint(x, y, this.dscalevector, this.dxvector, this.dyvector);
                };
                this.draw_vectorPrint = function () {
                    thispage.printobj.printctx.fillStyle = "rgb(255,255,255)";
                    thispage.printobj.printctx.fillRect(0, 0, thispage.printobj.paperimage.width, thispage.printobj.paperimage.height);
                    if (this.VectorPageObj == undefined) {
                        return;
                    }
                    this.VectorPageObj.drawallnew(thispage.printobj.printctx, thispage.printobj.pscale, thispage.printobj.pdx, thispage.printobj.pdy, true, true);
                    //this.VectorPageObj.drawall(thispage.printobj.printctx,thispage.printobj.pscale,thispage.printobj.pdx,thispage.printobj.pdy,true,true);
                    drawmarkupPrint(thispage.printobj.printctx, thispage.pagenumber, thispage.printobj.pdx, thispage.printobj.pdy, thispage.printobj.pscale);
                    //possible callback here.
                };
                this.magnifyoff = function () {
                    if (thispage.usevector3Dxml) {
                        return;
                    }
                    //context.clearRect(0, 0, canvas.width, canvas.height);
                    /*if (thispage.usedincomposite && thispage.compositereference != undefined) {
                        //thispage.compositereference.draw_compare(false);
                        thispage.compositereference.scaleToBackground(true);
                    }
                    else {
                    }*/

                    if (thispage.usevectorxml) {
                        thispage.draw_vector(true);
                    }
                    else if (thispage.usepdfjs) {
                        thispage.DocRef.draw_mpagepdf(false);
                    }
                    else {
                        if (thispage.DocRef.Type == 0) {
                            thispage.DocRef.draw_mpage();
                        }
                        else {
                            thispage.draw_image(true);
                        }
                    }

                };
                
                this.drawmarkupcliprect = function (rect, ctx) {

                    if (thispage.getpageRotation() == 0) {
                        var rectCenterX = rect.x;
                        var rectCenterY = rect.y;

                    }
                    else {
                        var centerx = rect.x + (rect.w *0.5);
                        var centery = rect.y + (rect.h *0.5);
                        var rotpos = rotate_point({x : centerx, y : centery }, (canvasowidth / 2), (canvasoheight / 2), (360 - thispage.getpageRotation()));
                        rectCenterX = rotpos.x - (rect.w *0.5);
                        rectCenterY = rotpos.y - (rect.h *0.5);
                    }

                    if (thispage.usevectorxml) {
                        var drawscale = thispage.dscalevector;
                        var xoffset = -(rectCenterX) + (thispage.dxvector); // - (thispage.dxvector + wscale);
                        var yoffset = -(rectCenterY) + (thispage.dyvector); // - (thispage.dyvector + hscale);
                        
                    } else if (thispage.usepdfjs) {
                        drawscale = thispage.dscalepdf * thispage.curpagescale;
                        //var drawwidth = thispage.magnifycanvas.width * thispage.magnifyrestscale;
                        //var drawheight = thispage.magnifycanvas.height * thispage.magnifyrestscale;
                        //DocObj.getCurrentpage().curpagescale
                        xoffset = -(rectCenterX) + (thispage.dxpdf);
                        yoffset = -(rectCenterY) + (thispage.dypdf);

                    }else{
                        switch (thispage.currentimage) {
                            case 0:
                                drawscale = thispage.dscale;
                                xoffset = -(rectCenterX) + (thispage.dx);
                                yoffset = -(rectCenterY) + (thispage.dy);

                                break;
                            case 1:
                                drawscale = (thispage.dscale / thispage.bitmapratio);
                                //var drawscale = thispage.dscale / thispage.bitmapratio;
                                xoffset = -(rectCenterX) + (thispage.dx);
                                yoffset = -(rectCenterY) + (thispage.dy);
                                break;
                        }
                        drawscale = thispage.dscale;
                    }

                    drawmarkupMagnify(ctx, thispage.pagenumber, xoffset, yoffset, drawscale, thispage.getpageRotation());
                    
                };

                this.drawmagnify = function (mousepos, ctx, magnificationScale) {
                    if (thispage.usevector3Dxml) {
                        return;
                    }
                    if (thispage.getpageRotation() == 0) {
                        var rectCenterX = (mousepos.x - ((magnifysize.width / 2) / magnificationScale));
                        var rectCenterY = (mousepos.y - ((magnifysize.height / 2) / magnificationScale));
                    }
                    else {
                        var rotpos = rotate_point(mousepos, (canvasowidth / 2), (canvasoheight / 2), (360 - thispage.getpageRotation()));
                        rectCenterX = (rotpos.x - ((magnifysize.width / 2) / magnificationScale));
                        rectCenterY = (rotpos.y - ((magnifysize.height / 2) / magnificationScale));
                    }
                    ctx.fillStyle = thispage.DocRef.backgroundColor;
                    ctx.fillRect(0, 0, magnifysize.width, magnifysize.height);
                    var tx = (magnifysize.width / 2);
                    var ty = (magnifysize.height / 2);
                    
                    if (thispage.usevectorxml) {
                        var drawscale = thispage.dscalevector * magnificationScale;
                        var xoffset = -(rectCenterX) + (thispage.dxvector); // - (thispage.dxvector + wscale);
                        var yoffset = -(rectCenterY) + (thispage.dyvector); // - (thispage.dyvector + hscale);
                        xoffset *= magnificationScale;
                        yoffset *= magnificationScale;
                        if (thispage.getpageRotation() == 0) {
                            thispage.VectorPageObj.drawallmagnify(ctx, drawscale, xoffset, yoffset);
                        }
                        else {
                            ctx.save();
                            ctx.translate(tx, ty);
                            ctx.rotate(thispage.getpageRotation() * (Math.PI / 180));
                            ctx.translate(-tx, -ty);
                            thispage.VectorPageObj.drawallmagnify(ctx, drawscale, xoffset, yoffset);
                            //ctx.restore();
                        }
                    }
                    else if (thispage.usepdfjs) {
                        drawscale = thispage.dscalepdf * thispage.curpagescale * magnificationScale;
                        var drawwidth = thispage.magnifycanvas.width * thispage.magnifyrestscale;
                        var drawheight = thispage.magnifycanvas.height * thispage.magnifyrestscale;
                        //DocObj.getCurrentpage().curpagescale
                        xoffset = -(rectCenterX) + (thispage.dxpdf);
                        yoffset = -(rectCenterY) + (thispage.dypdf);
                        xoffset *= magnificationScale;
                        yoffset *= magnificationScale;
                        if (thispage.getpageRotation() == 0) {
                            ctx.drawImage(thispage.magnifycanvas, xoffset, yoffset);
                        }
                        else {
                            ctx.save();
                            ctx.translate(tx, ty);
                            ctx.rotate(thispage.getpageRotation() * (Math.PI / 180));
                            ctx.translate(-tx, -ty);
                            ctx.drawImage(thispage.magnifycanvas, xoffset, yoffset);
                            //ctx.restore();
                        }
                    }
                    else {
                        switch (thispage.currentimage) {
                            case 0:
                                drawscale = thispage.dscale * magnificationScale;
                                xoffset = -(rectCenterX) + (thispage.dx);
                                yoffset = -(rectCenterY) + (thispage.dy);
                                xoffset *= magnificationScale;
                                yoffset *= magnificationScale;
                                if (thispage.getpageRotation() == 0) {
                                    ctx.drawImage(thispage.largeimagecnv, xoffset, yoffset, thispage.MainImageWidth * drawscale, thispage.MainImageHeight * drawscale);
                                }
                                else {
                                    ctx.save();
                                    ctx.translate(tx, ty);
                                    ctx.rotate(thispage.getpageRotation() * (Math.PI / 180));
                                    ctx.translate(-tx, -ty);
                                    ctx.drawImage(thispage.largeimagecnv, xoffset, yoffset, thispage.MainImageWidth * drawscale, thispage.MainImageHeight * drawscale);
                                    //ctx.restore();
                                }
                                break;
                            case 1:
                                drawscale = (thispage.dscale / thispage.bitmapratio) * magnificationScale;
                                //var drawscale = thispage.dscale / thispage.bitmapratio;
                                xoffset = -(rectCenterX) + (thispage.dx);
                                yoffset = -(rectCenterY) + (thispage.dy);
                                xoffset *= magnificationScale;
                                yoffset *= magnificationScale;
                                if (thispage.getpageRotation() == 0) {
                                    ctx.drawImage(thispage.smallimagecnv, xoffset, yoffset, thispage.SmallImageWidth * drawscale, thispage.SmallImageHeight * drawscale);
                                }
                                else {
                                    ctx.save();
                                    ctx.translate(tx, ty);
                                    ctx.rotate(thispage.getpageRotation() * (Math.PI / 180));
                                    ctx.translate(-tx, -ty);
                                    ctx.drawImage(thispage.smallimagecnv, xoffset, yoffset, thispage.SmallImageWidth * drawscale, thispage.SmallImageHeight * drawscale);
                                }
                                break;
                        }
                        drawscale = thispage.dscale * magnificationScale;
                    }
                    if (thispage.getpageRotation() == 0) {
                        drawmarkupMagnify(ctx, thispage.pagenumber, xoffset, yoffset, drawscale, thispage.getpageRotation());
                    }
                    else {
                        drawmarkupMagnify(ctx, thispage.pagenumber, xoffset, yoffset, drawscale, thispage.getpageRotation());
                        ctx.restore();
                    }
                    //drawmarkupPrint(ctx,thispage.pagenumber,xoffset,yoffset,drawscale);
                    //drawscale = thispage.dscalevector;
                    //var wscale = ((thispage.VectorPageObj.width * drawscale) - (thispage.VectorPageObj.width * thispage.dscalevector)) / 2;
                    //var hscale = ((thispage.VectorPageObj.height * drawscale) - (thispage.VectorPageObj.height * thispage.dscalevector)) / 2;
                    //ctx.clearRect(0, 0, magcanvas.width, magcanvas.height);
                    //this.VectorPageObj.drawall(ctx, drawscale, xoffset, yoffset, false,false);
                };
                this.refresh_vectorPath = function(path){
                    thispage.backupScaleAndOffset();
                    contexto.fillRect(0, 0, canvasowidth, canvasoheight);
                    var tx = (canvasowidth / 2);
                    var ty = (canvasoheight / 2);
                    if (this.VectorPageObj == undefined) {
                        return;
                    }
                    if (this.getpageRotation() == 0) {
                        //this.VectorPageObj.drawall(contexto, this.dscalevector, this.dxvector, this.dyvector, refresh,false);
                        this.VectorPageObj.refreshPath(path, contexto, this.dscalevector, this.dxvector, this.dyvector);
                    }
                    else {
                        contexto.save();
                        contexto.translate(tx, ty);
                        contexto.rotate(this.getpageRotation() * (Math.PI / 180));
                        contexto.translate(-tx, -ty);
                        //this.VectorPageObj.drawall(contexto, this.dscalevector, this.dxvector, this.dyvector, refresh,false);
                        this.VectorPageObj.refreshPath(path, contexto, this.dscalevector, this.dxvector, this.dyvector);
                        contexto.restore();
                    }

                };
                this.draw_vector_selected = function () {
                    thispage.backupScaleAndOffset();
                    contexto.fillRect(0, 0, canvasowidth, canvasoheight);
                    var tx = (canvasowidth / 2);
                    var ty = (canvasoheight / 2);
                    if (this.VectorPageObj == undefined) {
                        return;
                    }
                    if (this.getpageRotation() == 0) {
                        //this.VectorPageObj.drawall(contexto, this.dscalevector, this.dxvector, this.dyvector, refresh,false);
                        this.VectorPageObj.drawSelected(contexto, this.dscalevector, this.dxvector, this.dyvector);
                    }
                    else {
                        contexto.save();
                        contexto.translate(tx, ty);
                        contexto.rotate(this.getpageRotation() * (Math.PI / 180));
                        contexto.translate(-tx, -ty);
                        //this.VectorPageObj.drawall(contexto, this.dscalevector, this.dxvector, this.dyvector, refresh,false);
                        this.VectorPageObj.drawSelected(contexto, this.dscalevector, this.dxvector, this.dyvector);
                        contexto.restore();
                    }
                };
                this.invert_images = function(bgcolor) {
                    this.VectorPageObj.invert_images(bgcolor);
                };
                this.draw_vector = function (refresh) {
                    thispage.backupScaleAndOffset();
                    if (thispage.DocRef.bSopenSilent) {
                        return;
                    
                    }
                    if (!thispage.DocRef.bActive) {
                        return;
                    }

                    contexto.fillStyle = displayBGColor;
                    contexto.fillRect(0, 0, canvasowidth, canvasoheight);
                    var tx = (canvasowidth / 2);
                    var ty = (canvasoheight / 2);
                    documentopen = true;
                    if (this.VectorPageObj == undefined) {
                        return;
                    }
                    //thispage.cleartextdivs();
                    if (bSuspendDraw) {
                        return;
                    }
                    if (this.getpageRotation() == 0) {
                        //this.VectorPageObj.drawall(contexto, this.dscalevector, this.dxvector, this.dyvector, refresh,false);
                        this.VectorPageObj.drawallnew(contexto, this.dscalevector, this.dxvector, this.dyvector, refresh, false);
                    }
                    else {
                        contexto.save();
                        contexto.translate(tx, ty);
                        contexto.rotate(this.getpageRotation() * (Math.PI / 180));
                        contexto.translate(-tx, -ty);
                        //this.VectorPageObj.drawall(contexto, this.dscalevector, this.dxvector, this.dyvector, refresh,false);
                        this.VectorPageObj.drawallnew(contexto, this.dscalevector, this.dxvector, this.dyvector, refresh, false);
                        contexto.restore();
                    }
                    //thispage.settextdivs();
                };
                /*this.draw_vector = function (refresh) {
                    contexto.fillStyle = "rgb(62,62,62)";
                    //contexto.fillStyle = "rgb(160,160,160)";
                    contexto.fillRect(0, 0, canvasowidth, canvasoheight);
                    var tx = (canvasowidth / 2);
                    var ty = (canvasoheight / 2);
                    documentopen = true;
        
                    if (thispage.VectorPageObj == undefined) {
                        return;
                    }
                    //context,scalefactor,offsetx,offsety
                    //this.dxvector = dx;
                    //this.dyvector = dy;
                    //this.dscalevector = dscale;
        
        
                    if (this.getpageRotation() == 0) {
        
                        if(refresh || !thispage.VectorPageObj.backgroundrender ){
        
        
                            thispage.VectorPageObj.drawall(contexto, thispage.dscalevector, thispage.dxvector, thispage.dyvector, refresh);
        
                        }
                        if(thispage.VectorPageObj.backgroundrender){
                            contexto.drawImage(thispage.VectorPageObj.offscreen, thispage.dxvector, thispage.dyvector, thispage.VectorPageObj.width * thispage.dscalevector, thispage.VectorPageObj.height * thispage.dscalevector);
                        }
        
        
                    } else {
                        contexto.save();
                        contexto.translate(tx, ty);
                        contexto.rotate(thispage.getpageRotation() * (Math.PI / 180));
                        contexto.translate(-tx, -ty);
        
                        if(refresh || !thispage.VectorPageObj.backgroundrender){
        
                            thispage.VectorPageObj.drawall(contexto, thispage.dscalevector, thispage.dxvector, thispage.dyvector, refresh);
                        }
                        if(thispage.VectorPageObj.backgroundrender){
                            contexto.drawImage(thispage.VectorPageObj.offscreen, thispage.dxvector, thispage.dyvector, thispage.VectorPageObj.width * thispage.dscalevector, thispage.VectorPageObj.height * thispage.dscalevector);
                        }
        
        
        
        
                        contexto.restore();
        
                    }
                    if(refresh && thispage.VectorPageObj.vectorpagetime > thispage.vectorpagetime){
                        thispage.changeinterval(thispage.VectorPageObj.vectorpagetime);
        
                    }
        
        
                };*/
                this.removevisibleTile = function (r, c) {
                    for (var i = 0; i < thispage.visibleTiles.length; i++) {
                        if (thispage.visibleTiles[i].r == r && thispage.visibleTiles[i].c == c) {
                            //remove from array
                            thispage.visibleTiles.splice(i, 1);
                        }
                    }
                };
                this.addvisibleTile = function (r, c) {
                    thispage.visibleTiles = [];
                    thispage.visibleTiles.push({ row: r, column: c });
                };
                this.draw_tiled_cavas = function (ctx, dx, dy, dscalepdf) {
                    if (thispage.canvtileArray.length == 0) {
                        return;
                    }
                    if (thispage.canvtileArray[0][0] == undefined || thispage.canvtileArray[0][0].cnv == undefined) {
                        return;
                    }
                    var offscreenwidth = thispage.canvtileArray[0][0].cnv.width;
                    var offscreenheight = thispage.canvtileArray[0][0].cnv.height;
                    var ulx = dx;
                    var uly = dy;
                    var xoffset = 0;
                    var yoffset = 0;
                    thispage.tilevisibiltychanged = false;
                    for (var i = 0; i < thispage.canvtileArray.length; i++) {
                        for (var j = 0; j < thispage.canvtileArray[i].length; j++) {
                            offscreenwidth = thispage.canvtileArray[i][j].cnv.width;
                            offscreenheight = thispage.canvtileArray[i][j].cnv.height;
                            xoffset = thispage.canvtileArray[i][j].xoffset;
                            yoffset = thispage.canvtileArray[i][j].yoffset;
                            var nowidth = (thispage.canvtileArray[i][j].cnv.width != 0);
                            var noheight = (thispage.canvtileArray[i][j].cnv.height != 0);


                            if ((ulx < canvasowidth && uly < canvasoheight) && ((ulx + (offscreenwidth * dscalepdf)) > 0 && (uly + (offscreenheight * dscalepdf)) > 0)) {
                                
                                if(nowidth && noheight){
                                    ctx.drawImage(thispage.canvtileArray[i][j].cnv, ulx, uly, offscreenwidth * dscalepdf, offscreenheight * dscalepdf);
                                }
                                
                                if (!thispage.canvtileArray[i][j].visible) {
                                    //console.log('visibility changed to true', i, j);
                                    thispage.tilevisibiltychanged = true;
                                    //addvisibleTile(i, j);
                                }
                                else {
                                    //thispage.tilevisibiltychanged = false;
                                }
                                thispage.canvtileArray[i][j].visible = true;
                                //thispage.addvisibleTile(i,j);    
                                if (thispage.tilespending) {
                                    thispage.tilespending = false;
                                }
                                if (thispage.prioritytilespending) {
                                    thispage.prioritytilespending = false;
                                }
                            }
                            else {
                                if (thispage.canvtileArray[i][j].visible) {
                                    //console.log('visibility changed to false', i, j);
                                    thispage.tilevisibiltychanged = true;
                                }
                                else {
                                    //thispage.tilevisibiltychanged = false;
                                }
                                thispage.canvtileArray[i][j].visible = false;
                            }
                            /*if (i = 0 && j == 0){
                                
                            }else{
                                contexto.drawImage(thispage.canvtileArray[i][j].cnv, ulx, uly, offscreenwidth * this.dscalepdf, offscreenheight * this.dscalepdf);
                            }*/
                            //ulx += (offscreenwidth * dscalepdf) - 1;
                            /*if (j < thispage.canvtileArray[i].length - 1){
                                ulx += (xoffset * dscalepdf);
                            }*/
                            ulx += (xoffset * dscalepdf);
                        }
                        //uly += (offscreenheight * dscalepdf) - 1;
                        uly += (yoffset * dscalepdf);
                        ulx = dx;
                    }
                };
                this.draw_foxit_annots = function (ctx, dx, dy, dscalepdf) {
                    //var offscreenwidth = thispage.annotArray[0].cnv.width;
                    //var offscreenheight = thispage.annotArray[0].cnv.height;
                    var ulx = dx;
                    var uly = dy;
                    for (var i = 0; i < thispage.annotArray.length; i++) {
                        var offscreenwidth = thispage.annotArray[i].cnv.width;
                        var offscreenheight = thispage.annotArray[i].cnv.height;
                        var offsetx = thispage.annotArray[i].pos.left * dscalepdf;
                        var offsety = thispage.annotArray[i].pos.top * dscalepdf;
                        //var offsetx = thispage.annotArray[i].pos.left + ulx;
                        //var offsety = thispage.annotArray[i].pos.top + uly;
                        offsetx += ulx;
                        offsety += uly;
                        ctx.drawImage(thispage.annotArray[i].cnv, offsetx, offsety, thispage.annotArray[i].pos.width * dscalepdf * thispage.annotArray[i].pos.scale, thispage.annotArray[i].pos.height * dscalepdf * thispage.annotArray[i].pos.scale);
                    }
                };

                function matchsearchRect(rect, currect, sm, pm){


                    var match = false;

                    if(rect.left == currect.left){
                        if(rect.top == currect.top){
                            if(rect.right == currect.right){
                                if(rect.bottom == currect.bottom){

                                    match = true;       

                                }
                            }
                        }

                    }

                    return match;

                    /*if(match){
                        return {outer : sm, inner : pm};
                    }*/

                }

                this.removesamerects = function(SearcMatches){

                    var removetextrects = [];

                    var curRect = {left : 0, top : 0, right : 0, bottom : 0};

                    for (var sm = 0; sm < SearcMatches.length; sm++) {
                        //nummatches += thisdocument.foxitSearcMatches[sm].length;
                        for (var pm = 0; pm < SearcMatches[sm].length; pm++) {
                            if (SearcMatches[sm][pm].match.pageIndex == thispage.pagenumber) {

                                if(curRect, SearcMatches[sm][pm].match.rectArray[0], sm, pm){
                                    removetextrects.push({outer : sm, inner : pm});
                                }

                                
                                

                                //console.log(SearcMatches[sm][pm].match.rectArray[0]);
                                //var x = ((SearcMatches[sm][pm].match.rectArray[0].left * (thispage.curpagescale * thispage.dscalepdf)) + thispage.dxpdf);
                                //var y = ((SearcMatches[sm][pm].match.rectArray[0].top * (thispage.curpagescale * thispage.dscalepdf)) + thispage.dypdf);
                                //var y = (((thispage.pdfpageheight - SearcMatches[sm][pm].match.rectArray[0].top) * (thispage.curpagescale * thispage.dscalepdf)) + thispage.dypdf);
                                //console.log(thispage.pdfpageheight);
                                //console.log(thispage.dypdf);
                                //var y = (((thispage.pdfpageheight - curmatch.rectArray[0].top) * (thispage.curpagescale * thispage.dscalepdf)) + thispage.dypdf);
                                //y = 
                                //var right = ((SearcMatches[sm][pm].match.rectArray[0].right * (thispage.curpagescale * thispage.dscalepdf)) + thispage.dxpdf);
                                //var bottom = (((thispage.pdfpageheight - SearcMatches[sm][pm].match.rectArray[0].bottom) * (thispage.curpagescale * thispage.dscalepdf)) + thispage.dypdf);
                                //var x = ((SearcMatches[sm][pm].match.rectArray[0].left ) + thispage.dxpdf);
                                //var y = ((SearcMatches[sm][pm].match.rectArray[0].top ) + thispage.dypdf);
                                //var right = ((SearcMatches[sm][pm].match.rectArray[0].right ) + thispage.dxpdf);
                                //var bottom = ((SearcMatches[sm][pm].match.rectArray[0].bottom ) + thispage.dypdf);
                                //var width = right - x;
                                //var height = bottom - y;

                                //pagetextrects.push({x : x, y : y, w : width, h : height, pagenum : SearcMatches[sm][pm].match.pageIndex, pageheight : thispage.pdfpageheight});

                                curRect = SearcMatches[sm][pm].match.rectArray[0];
                            }
                        }
                    }



                    //var x = ((rect.left * (thispage.curpagescale * thispage.dscalepdf)) + thispage.dxpdf);
                    //var y = (((thispage.pdfpageheight - rect.top) * (thispage.curpagescale * thispage.dscalepdf)) + thispage.dypdf);
                    //var right = ((rect.right * (thispage.curpagescale * thispage.dscalepdf)) + thispage.dxpdf);
                    //var bottom = (((thispage.pdfpageheight - rect.bottom) * (thispage.curpagescale * thispage.dscalepdf)) + thispage.dypdf);
                    //var width = right - x;
                    //var height = bottom - y;

                    return removetextrects;


                };

                this.getpagerectdim = function(SearcMatches){

                    var pagetextrects = [];

                    for (var sm = 0; sm < SearcMatches.length; sm++) {
                        //nummatches += thisdocument.foxitSearcMatches[sm].length;
                        for (var pm = 0; pm < SearcMatches[sm].length; pm++) {
                            if (SearcMatches[sm][pm].match.pageIndex == thispage.pagenumber) {
                                //console.log(SearcMatches[sm][pm].match.rectArray[0]);
                                var x = ((SearcMatches[sm][pm].match.rectArray[0].left * (thispage.curpagescale * thispage.dscalepdf)) + thispage.dxpdf);
                                //var y = ((SearcMatches[sm][pm].match.rectArray[0].top * (thispage.curpagescale * thispage.dscalepdf)) + thispage.dypdf);
                                var y = (((thispage.pdfpageheight - SearcMatches[sm][pm].match.rectArray[0].top) * (thispage.curpagescale * thispage.dscalepdf)) + thispage.dypdf);
                                //console.log(thispage.pdfpageheight);
                                //console.log(thispage.dypdf);
                                //var y = (((thispage.pdfpageheight - curmatch.rectArray[0].top) * (thispage.curpagescale * thispage.dscalepdf)) + thispage.dypdf);
                                //y = 
                                var right = ((SearcMatches[sm][pm].match.rectArray[0].right * (thispage.curpagescale * thispage.dscalepdf)) + thispage.dxpdf);
                                var bottom = (((thispage.pdfpageheight - SearcMatches[sm][pm].match.rectArray[0].bottom) * (thispage.curpagescale * thispage.dscalepdf)) + thispage.dypdf);
                                //var x = ((SearcMatches[sm][pm].match.rectArray[0].left ) + thispage.dxpdf);
                                //var y = ((SearcMatches[sm][pm].match.rectArray[0].top ) + thispage.dypdf);
                                //var right = ((SearcMatches[sm][pm].match.rectArray[0].right ) + thispage.dxpdf);
                                //var bottom = ((SearcMatches[sm][pm].match.rectArray[0].bottom ) + thispage.dypdf);
                                var width = right - x;
                                var height = bottom - y;

                                pagetextrects.push({x : x, y : y, w : width, h : height, pagenum : SearcMatches[sm][pm].match.pageIndex, pageheight : thispage.pdfpageheight});
                            }
                        }
                    }



                    //var x = ((rect.left * (thispage.curpagescale * thispage.dscalepdf)) + thispage.dxpdf);
                    //var y = (((thispage.pdfpageheight - rect.top) * (thispage.curpagescale * thispage.dscalepdf)) + thispage.dypdf);
                    //var right = ((rect.right * (thispage.curpagescale * thispage.dscalepdf)) + thispage.dxpdf);
                    //var bottom = (((thispage.pdfpageheight - rect.bottom) * (thispage.curpagescale * thispage.dscalepdf)) + thispage.dypdf);
                    //var width = right - x;
                    //var height = bottom - y;

                    return pagetextrects;


                };
                this.drawsearchRect = function () {

                    var pxscale = getPixelRatio();

                    if (thispage.DocRef.foxitsearchobj.page == thispage.pagenumber && thispage.DocRef.foxitsearchobj.active) {
                        //contexto.save();
                        cntximg.save();
                        var curmatch = thispage.DocRef.foxitsearchobj.getcurmatch();
                        if(!curmatch) return;

                        var x = ((curmatch.rectArray[0].left * (thispage.curpagescale * thispage.dscalepdf)) + thispage.dxpdf);
                        var y = (((thispage.pdfpageheight - curmatch.rectArray[0].top) * (thispage.curpagescale * thispage.dscalepdf)) + thispage.dypdf);
                        var right = ((curmatch.rectArray[0].right * (thispage.curpagescale * thispage.dscalepdf)) + thispage.dxpdf);
                        var bottom = (((thispage.pdfpageheight - curmatch.rectArray[0].bottom) * (thispage.curpagescale * thispage.dscalepdf)) + thispage.dypdf);
                        var width = right - x;
                        var height = bottom - y;

                        x = x * pxscale;
                        y = y * pxscale;

                        width = width * pxscale;
                        height = height * pxscale;

                        cntximg.clearRect(x, y, width, height);
                        //contexto.fillStyle = "rgba(238, 238, 0, 0.3)";
                        cntximg.fillStyle = "rgba(238, 238, 0, 0.3)";
                        //contexto.fillRect(x, y, width, height);

                        cntximg.fillRect(x, y, width, height);
                        //contexto.restore();
                        //console.log(x, y, width, height);

                        cntximg.restore();
                    }
                };
                this.drawsearchRects = function (SearcMatches) {

                    const pageNum = thispage.DocRef.currentpage;
                    const currentPage = thispage.DocRef.pages[pageNum];
                    var pxscale = getPixelRatio();


                    cntximg.save();
                    for (var sm = 0; sm < SearcMatches.length; sm++) {
                        //nummatches += thisdocument.foxitSearcMatches[sm].length;
                        for (var pm = 0; pm < SearcMatches[sm].length; pm++) {
                            if (SearcMatches[sm][pm].match.pageIndex == pageNum) {
                                //console.log(SearcMatches[sm][pm].match.rectArray[0]);


                                const curmatch = SearcMatches[sm][pm].match;
                                var x = ((curmatch.rectArray[0].left * (thispage.curpagescale * thispage.dscalepdf)) + thispage.dxpdf);
                                var y = (((thispage.pdfpageheight - curmatch.rectArray[0].top) * (thispage.curpagescale * thispage.dscalepdf)) + thispage.dypdf);
                                var right = ((curmatch.rectArray[0].right * (thispage.curpagescale * thispage.dscalepdf)) + thispage.dxpdf);
                                var bottom = (((thispage.pdfpageheight - curmatch.rectArray[0].bottom) * (thispage.curpagescale * thispage.dscalepdf)) + thispage.dypdf);
        

                                //var x = ((SearcMatches[sm][pm].match.rectArray[0].left * (thispage.curpagescale * thispage.dscalepdf)) + thispage.dxpdf);
                                //var y = ((SearcMatches[sm][pm].match.rectArray[0].top * (thispage.curpagescale * thispage.dscalepdf)) + thispage.dypdf);
                                //var y = (((thispage.pdfpageheight - SearcMatches[sm][pm].match.rectArray[0].top) * (thispage.curpagescale * thispage.dscalepdf)) + thispage.dypdf);
                                //y = 
                                //var right = ((SearcMatches[sm][pm].match.rectArray[0].right * (thispage.curpagescale * thispage.dscalepdf)) + thispage.dxpdf);
                                //var bottom = (((thispage.pdfpageheight - SearcMatches[sm][pm].match.rectArray[0].bottom) * (thispage.curpagescale * thispage.dscalepdf)) + thispage.dypdf);
                                //var x = ((SearcMatches[sm][pm].match.rectArray[0].left ) + thispage.dxpdf);
                                //var y = ((SearcMatches[sm][pm].match.rectArray[0].top ) + thispage.dypdf);
                                //var right = ((SearcMatches[sm][pm].match.rectArray[0].right ) + thispage.dxpdf);
                                //var bottom = ((SearcMatches[sm][pm].match.rectArray[0].bottom ) + thispage.dypdf);
                                var width = right - x;
                                var height = bottom - y;

                                x = x * pxscale;
                                y = y * pxscale;
        
                                width = width * pxscale;
                                height = height * pxscale;

                                if ( SearcMatches[sm][pm] == thispage.DocRef.foxitsearchobj.orangeItem ) {
                                    cntximg.fillStyle = "rgba(255, 165, 0, 0.3)";
                                } else {
                                    cntximg.fillStyle = "rgba(238, 238, 0, 0.3)";
                                }
                                    
                                cntximg.fillRect(x, y, width, height);
                                
                            

                                //cntximg.fillStyle = "rgba(238, 238, 0, 0.3)";
                                //cntximg.fillRect(x, y, width, height);
                            }
                        }
                    }
                    cntximg.restore();
                };
                this.draw_canvas_compare = function (ctx, dx, dy, dscalepdf) {
                    //contexto.drawImage(this.pagecanvas, this.dxpdf, this.dypdf, thispage.offscreenwidth * this.dscalepdf, thispage.offscreenheight * this.dscalepdf);
                    /*if (!thispage.pdfdiminitset){
                        return;
                    }*/
                    var offscreenwidth = thispage.pagecanvas.width;
                    var offscreenheight = thispage.pagecanvas.height;
                    var ulx = dx;
                    var uly = dy;
                    var xoffset = 0;
                    var yoffset = 0;
                    ctx.drawImage(thispage.pagecanvas, ulx, uly, offscreenwidth * thispage.dscalepdf, offscreenheight * thispage.dscalepdf);
                };
                this.draw_canvas = function (clear) {
                    thispage.backupScaleAndOffset();
                    if (thispage.DocRef.bSopenSilent || !thispage.DocRef.bActive) {
                        return;
                    }
                    if (clear) {
                        contexto.fillStyle = displayBGColor; //contexto.fillStyle = "rgb(238,243,250)";
                        contexto.fillRect(0, 0, canvasowidth, canvasoheight);
                    }
                    //console.log(this.pagenumber);
                    var tx = (canvasowidth / 2);
                    var ty = (canvasoheight / 2);
                    documentopen = true;
                    var bsinglepage = (this.DocRef.pages.length == 1);
                    if (this.DocRef.pagelocked || bsinglepage) {
                        var rotation = this.getpageRotation();
                    }
                    else {
                        rotation = 0;
                    }
                    if (bSuspendDraw) {
                        return;
                    }
                    if (!thispage.pdfdiminitset) {
                        var dytemppdf = 0;
                        if (thispage.DocRef.pages[thispage.pagenumber - 1] && thispage.DocRef.pages[thispage.pagenumber - 1].endy) {
                            //dytemppdf = thispage.DocRef.pages[thispage.pagenumber - 1].endy + 10;
                        }
                        contexto.fillStyle = "rgb(255,255,255)"; //contexto.fillStyle = "rgb(238,243,250)";                
                        contexto.fillRect(thispage.DocRef.pages[0].dxpdf, dytemppdf, thispage.DocRef.pages[0].offscreenwidth * thispage.DocRef.pages[0].dscalepdf, thispage.DocRef.pages[0].offscreenheight * thispage.DocRef.pages[0].dscalepdf);
                        return;
                    }
                    if (rotation == 0) {
                        if (thispage.iscollapsed || !thispage.pdfisfirstrendered) {
                            if (!thispage.pdfisfirstrendered && bAnimatePDFrender) {
                                contexto.fillStyle = "rgb(255,255,255)"; //contexto.fillStyle = "rgb(238,243,250)";
                                if (!thispage.pdfisfirstrendered) {
                                    var scale = 1;
                                }
                                else {
                                    scale = this.dscalepdf;
                                }
                                contexto.fillRect(this.dxpdf, this.dypdf, thispage.offscreenwidth * scale, thispage.offscreenheight * scale);
                                //new tile draw method called here. but since no progressive rendering may not be needed.
                                //contexto.drawImage(thispage.pagedrawCanvas, this.dxpdf, this.dypdf, thispage.offscreenwidth * scale, thispage.offscreenheight * scale);
                            }
                            else {
                                contexto.fillStyle = "rgb(255,255,255)"; //contexto.fillStyle = "rgb(238,243,250)";
                                contexto.fillRect(this.dxpdf, this.dypdf, thispage.offscreenwidth * this.dscalepdf, thispage.offscreenheight * this.dscalepdf);
                                //console.log(this.dscalepdf);
                            }
                        }
                        else {
                            contexto.fillStyle = "rgb(255,255,255)"; //contexto.fillStyle = "rgb(238,243,250)";
                            contexto.fillRect(this.dxpdf, this.dypdf, thispage.offscreenwidth * this.dscalepdf, thispage.offscreenheight * this.dscalepdf);
                            //new tile draw method called here.
                            //contexto.drawImage(this.pagecanvas, this.dxpdf, this.dypdf, thispage.offscreenwidth * this.dscalepdf, thispage.offscreenheight * this.dscalepdf);
                            thispage.draw_tiled_cavas(contexto, this.dxpdf, this.dypdf, this.dscalepdf);
                            //thispage.draw_tiled_tempcavas(contexto, this.dxpdf, this.dypdf, this.dscalepdf);
                            thispage.draw_foxit_annots(contexto, this.dxpdf, this.dypdf, this.dscalepdf);
                        }
                        /*if (thispage.pdfisrendered){
                            contexto.drawImage(this.pagecanvas, this.dxpdf, this.dypdf, thispage.offscreenwidth * this.dscalepdf, thispage.offscreenheight * this.dscalepdf);
                        }else{
                            contexto.fillStyle = "rgb(255,255,255)"; //contexto.fillStyle = "rgb(238,243,250)";
                            contexto.fillRect(this.dxpdf, this.dypdf, thispage.offscreenwidth * this.dscalepdf, thispage.offscreenheight * this.dscalepdf);
        
                        }*/
                        //contexto.fillStyle = "rgb(255,255,255)"; //contexto.fillStyle = "rgb(238,243,250)";
                        //contexto.fillRect(this.dxpdf, this.dypdf, thispage.offscreenwidth * this.dscalepdf, thispage.offscreenheight * this.dscalepdf);
                        if (thispage.drawtextSearchRect) {
                            thispage.drawsearchRect();
                        }
                    }
                    else {
                        contexto.save();
                        contexto.translate(tx, ty);
                        contexto.rotate(rotation * (Math.PI / 180));
                        contexto.translate(-tx, -ty);
                        if (thispage.pdfisrendered && !thispage.iscollapsed) {
                            //contexto.drawImage(this.pagecanvas, this.dxpdf, this.dypdf, thispage.offscreenwidth * this.dscalepdf, thispage.offscreenheight * this.dscalepdf);
                            contexto.fillStyle = "rgb(255,255,255)"; //contexto.fillStyle = "rgb(238,243,250)";
                            contexto.fillRect(this.dxpdf, this.dypdf, thispage.offscreenwidth * this.dscalepdf, thispage.offscreenheight * this.dscalepdf);
                            thispage.draw_tiled_cavas(contexto, this.dxpdf, this.dypdf, this.dscalepdf);
                            thispage.draw_foxit_annots(contexto, this.dxpdf, this.dypdf, this.dscalepdf);
                        }
                        else {
                            contexto.fillStyle = "rgb(255,255,255)"; //contexto.fillStyle = "rgb(238,243,250)";
                            contexto.fillRect(this.dxpdf, this.dypdf, thispage.offscreenwidth * this.dscalepdf, thispage.offscreenheight * this.dscalepdf);
                        }
                        contexto.restore();
                    }
                };
                this.transparent = function (data) {
                    for (var i = 0; i < data.length; i += 4) {
                        if (data[i] == 255 && data[i + 1] == 255 && data[i + 2] == 255) {
                            data[i + 3] = 100; // - data[i + 2]; // transparent
                            //do nothting with white
                        }
                    }
                };
                this.setcolor = function (data, color) {
                    //var c = color; // 16712640
                    /*var components = {
                        r: (color & 0xff0000) >> 16,
                        g: (color & 0x00ff00) >> 8,
                        b: (color & 0x0000ff)
                    };*/
                    var components = convertHexrgb(color);
                    for (var i = 0; i < data.length; i += 4) {
                        if (data[i] == 255 && data[i + 1] == 255 && data[i + 2] == 255) {
                            data[i + 3] = 100; // - data[i + 2]; // transparent
                            //do nothting with white
                        }else if(data[i] == 254 && data[i + 1] == 254 && data[i + 2] == 254){
                            data[i + 3] = 100; // - data[i + 2]; // transparent
                        }
                        else {
                            //console.log(data[i], data[i + 1], data[i + 2]);
                            data[i] = components.r; //255 - data[i];     // red
                            data[i + 1] = components.g; //255 - data[i + 1]; // green
                            data[i + 2] = components.b; // - data[i + 2]; // blue
                            //this.overlayColor = '#ff8000';
                        }
                    }
                };
                this.setred = function (data) {
                    for (var i = 0; i < data.length; i += 4) {
                        if (data[i] == 255 && data[i + 1] == 255 && data[i + 2] == 255) {
                            data[i + 3] = 100; // - data[i + 2]; // transparent
                            //do nothting with white
                        }
                        else {
                            data[i] = 255; //255 - data[i];     // red
                            data[i + 1] = 128; //255 - data[i + 1]; // green
                            data[i + 2] = 0; // - data[i + 2]; // blue
                            //this.overlayColor = '#ff8000';
                        }
                    }
                };
                this.setblue = function (data) {
                    for (var i = 0; i < data.length; i += 4) {
                        if (data[i] == 255 && data[i + 1] == 255 && data[i + 2] == 255) {
                            //do nothting with white
                            //data[i + 3] = 100;// - data[i + 2]; // transparent
                        }
                        else {
                            data[i] = 0; //255 - data[i];     // red
                            data[i + 1] = 0; //255 - data[i + 1]; // green
                            data[i + 2] = 255; // - data[i + 2]; // blue
                        }
                    }
                };
                this.setinvert = function (data) {
                    for (var i = 0; i < data.length; i += 4) {
                        data[i] = 255 - data[i]; // red
                        data[i + 1] = 255 - data[i + 1]; // green
                        data[i + 2] = 255 - data[i + 2]; // blue
                    }
                };
                this.invertpdfmagnify = function (background) {
                    //var data = imageData.data;
                    try {
                        var imageData = thispage.magnifypagectx.getImageData(0, 0, thispage.magnifycanvas.width, thispage.magnifycanvas.height);
                    }
                    catch (e) {
                        alert("Error 1 - " + e);
                        console.log(e, thispage.magnifycanvas.width, thispage.magnifycanvas.height);
                        background = -1;
                    }
                    var cmprcolors = thispage.compositereference.getColors();
                    switch (background) {
                        case 0:
                            thispage.setinvert(imageData.data);
                            break;
                        case 1:
                            //thispage.setblue(imageData.data);
                            thispage.setcolor(imageData.data, cmprcolors.bg);
                            break;
                        case 2:
                            //thispage.setred(imageData.data);
                            thispage.setcolor(imageData.data, cmprcolors.fg);
                            break;
                    }
                    if (background != -1) {
                        thispage.magnifypagectx.putImageData(imageData, 0, 0);
                    }
                };
                this.invertpdfprint = function (background) {
                    switch (background) {
                        case 1:
                            try {
                                var imageData = thispage.compositePrintreference.bgpagectx.getImageData(0, 0, thispage.compositePrintreference.bgpagecanvas.width, thispage.compositePrintreference.bgpagecanvas.height);
                            }
                            catch (e) {
                                alert("Error 1 - " + e);
                                console.log(e, thispage.compositePrintreference.bgpagecanvas.width, thispage.compositePrintreference.bgpagecanvas.height);
                                background = -1;
                            }
                            //thispage.compositereference.bgpagecanvas
                            break;
                        case 2:
                            try {
                                imageData = thispage.compositePrintreference.ovpagectx.getImageData(0, 0, thispage.compositePrintreference.ovpagecanvas.width, thispage.compositePrintreference.ovpagecanvas.height);
                            }
                            catch (e) {
                                alert("Error 1 - " + e);
                                console.log(e, thispage.compositePrintreference.ovpagecanvas.width, thispage.compositePrintreference.ovpagecanvas.height);
                                background = -1;
                            }
                            //thispage.compositereference.ovpagecanvas
                            break;
                    }
                    //var data = imageData.data;
                    var cmprcolors = thispage.compositereference.getColors();
                    switch (background) {
                        case 0:
                            thispage.setinvert(imageData.data);
                            break;
                        case 1:
                            thispage.setcolor(imageData.data, cmprcolors.bg);
                            //thispage.setblue(imageData.data);
                            break;
                        case 2:
                            thispage.setcolor(imageData.data, cmprcolors.fg);
                            //thispage.setred(imageData.data);
                            break;
                    }
                    if (background != -1) {
                        switch (background) {
                            case 1:
                                thispage.compositePrintreference.bgpagectx.putImageData(imageData, 0, 0);
                                break;
                            case 2:
                                thispage.compositePrintreference.ovpagectx.putImageData(imageData, 0, 0);
                                break;
                        }
                    }
                    /*if (this.usedincomposite && this.compositereference != undefined) {
                        //this.compositereference.draw_compare(false);
                        //console.log('draw compare invert pdf' );
                    }
                    else {
                        //this.draw_image(true);
                    }*/
                    //imageData = null;
                };
                this.transparentpdf = function (background) {
                    switch (background) {
                        case 1:
                            try {
                                var imageData = thispage.compositereference.bgpagectx.getImageData(0, 0, thispage.compositereference.bgpagecanvas.width, thispage.compositereference.bgpagecanvas.height);
                            }
                            catch (e) {
                                alert("Error 1 - " + e);
                                console.log(e, thispage.compositereference.bgpagecanvas.width, thispage.compositereference.bgpagecanvas.height);
                                background = -1;
                            }
                            //thispage.compositereference.bgpagecanvas
                            break;
                        case 2:
                            try {
                                imageData = thispage.compositereference.ovpagectx.getImageData(0, 0, thispage.compositereference.ovpagecanvas.width, thispage.compositereference.ovpagecanvas.height);
                            }
                            catch (e) {
                                alert("Error 1 - " + e);
                                console.log(e, thispage.compositereference.ovpagecanvas.width, thispage.compositereference.ovpagecanvas.height);
                                background = -1;
                            }
                            //thispage.compositereference.ovpagecanvas
                            break;
                    }
                    switch (background) {
                        case 0:
                            thispage.setinvert(imageData.data);
                            break;
                        case 1:
                            thispage.transparent(imageData.data);
                            break;
                        case 2:
                            thispage.transparent(imageData.data);
                            break;
                    }
                    if (background != -1) {
                        switch (background) {
                            case 1:
                                thispage.compositereference.bgpagectx.putImageData(imageData, 0, 0);
                                break;
                            case 2:
                                thispage.compositereference.ovpagectx.putImageData(imageData, 0, 0);
                                break;
                        }
                    }
                };
                this.invertpdf = function (background) {
                    switch (background) {
                        case 1:
                            try {
                                var imageData = thispage.compositereference.bgpagectx.getImageData(0, 0, thispage.compositereference.bgpagecanvas.width, thispage.compositereference.bgpagecanvas.height);
                            }
                            catch (e) {
                                alert("Error 1 - " + e);
                                console.log(e, thispage.compositereference.bgpagecanvas.width, thispage.compositereference.bgpagecanvas.height);
                                background = -1;
                            }
                            //thispage.compositereference.bgpagecanvas
                            break;
                        case 2:
                            try {
                                imageData = thispage.compositereference.ovpagectx.getImageData(0, 0, thispage.compositereference.ovpagecanvas.width, thispage.compositereference.ovpagecanvas.height);
                            }
                            catch (e) {
                                alert("Error 1 - " + e);
                                console.log(e, thispage.compositereference.ovpagecanvas.width, thispage.compositereference.ovpagecanvas.height);
                                background = -1;
                            }
                            //thispage.compositereference.ovpagecanvas
                            break;
                    }
                    //var data = imageData.data;
                    var cmprcolors = thispage.compositereference.getColors();
                    switch (background) {
                        case 0:
                            thispage.setinvert(imageData.data);
                            break;
                        case 1:
                            thispage.setcolor(imageData.data, cmprcolors.bg);
                            //thispage.setblue(imageData.data);
                            break;
                        case 2:
                            thispage.setcolor(imageData.data, cmprcolors.fg);
                            //thispage.setred(imageData.data);
                            break;
                    }
                    if (background != -1) {
                        switch (background) {
                            case 1:
                                thispage.compositereference.bgpagectx.putImageData(imageData, 0, 0);
                                break;
                            case 2:
                                thispage.compositereference.ovpagectx.putImageData(imageData, 0, 0);
                                break;
                        }
                    }
                    /*if (this.usedincomposite && this.compositereference != undefined) {
                        //this.compositereference.draw_compare(false);
                        //console.log('draw compare invert pdf' );
                    }
                    else {
                        //this.draw_image(true);
                    }*/
                    //imageData = null;
                };
                this.transparentimage = function (background) {
                    var imageDatalarge = thispage.largeimagectx.getImageData(0, 0, thispage.largeimagecnv.width, thispage.largeimagecnv.height);
                    var imageDatasmall = thispage.smallimagectx.getImageData(0, 0, thispage.smallimagecnv.width, thispage.smallimagecnv.height);
                    var data = imageDatasmall.data;
                    switch (background) {
                        case 0:
                            thispage.setinvert(data);
                            break;
                        case 1:
                            //thispage.setblue(data);
                            thispage.transparent(data);
                            break;
                        case 2:
                            //thispage.setred(data);
                            thispage.transparent(data);
                            break;
                    }
                    thispage.smallimagectx.putImageData(imageDatasmall, 0, 0);
                    data = imageDatalarge.data;
                    switch (background) {
                        case 0:
                            thispage.setinvert(data);
                            break;
                        case 1:
                            //thispage.setblue(data);
                            thispage.transparent(data);
                            break;
                        case 2:
                            thispage.transparent(data);
                            //thispage.setred(data);
                            break;
                    }
                    thispage.largeimagectx.putImageData(imageDatalarge, 0, 0);
                    thispage.largeimagectx.putImageData(imageDatalarge, 0, 0);
                    /*if (this.usedincomposite && this.compositereference != undefined) {
                        //this.compositereference.draw_compare(false);
                    }
                    else {
                        
                    }*/
                    thispage.draw_image(true);
                };
                this.invert = function (background) {
                    var imageDatalarge = thispage.largeimagectx.getImageData(0, 0, thispage.largeimagecnv.width, thispage.largeimagecnv.height);
                    var imageDatasmall = thispage.smallimagectx.getImageData(0, 0, thispage.smallimagecnv.width, thispage.smallimagecnv.height);
                    //var imageData = contexto.getImageData(0, 0, canvasowidth, canvasoheight);
                    var data = imageDatasmall.data;
                    //thispage.setinvert(data);
                    var cmprcolors = thispage.compositereference.getColors();
                    switch (background) {
                        case 0:
                            thispage.setinvert(data);
                            break;
                        case 1:
                            //thispage.setblue(data);
                            thispage.setcolor(data, cmprcolors.bg);
                            break;
                        case 2:
                            //thispage.setred(data);
                            thispage.setcolor(data, cmprcolors.fg);
                            break;
                    }
                    thispage.smallimagectx.putImageData(imageDatasmall, 0, 0);
                    data = imageDatalarge.data;
                    switch (background) {
                        case 0:
                            thispage.setinvert(data);
                            break;
                        case 1:
                            //thispage.setblue(data);
                            thispage.setcolor(data, cmprcolors.bg);
                            break;
                        case 2:
                            thispage.setcolor(data, cmprcolors.fg);
                            //thispage.setred(data);
                            break;
                    }
                    thispage.largeimagectx.putImageData(imageDatalarge, 0, 0);
                    /*if (this.usedincomposite && this.compositereference != undefined) {
                        //this.compositereference.draw_compare(false);
                    }
                    else {
                        
                    }*/
                    thispage.draw_image(true);
                    //this.draw_image(true);
                };
                this.resetimage = function () {
                    var imagewidth = thispage.MainImageWidth;
                    var imageheight = thispage.MainImageHeight;
                    thispage.largeimagecnv.width = imagewidth;
                    thispage.largeimagecnv.height = imageheight;
                    thispage.largeimagectx.drawImage(thispage.largeimage, 0, 0, imagewidth, imageheight);

                    this.smallimagecreatefromlarge();

                    /*imagewidth = thispage.SmallImageWidth;
                    imageheight = thispage.SmallImageHeight;
                    thispage.smallimagecnv.width = imagewidth;
                    thispage.smallimagecnv.height = imageheight;
                    thispage.smallimagectx.drawImage(thispage.smallimage, 0, 0, imagewidth, imageheight);*/
                };
                this.draw_imagePrint = function () {
                    thispage.printobj.printctx.drawImage(this.largeimage, thispage.printobj.pdx, thispage.printobj.pdy, thispage.printobj.docwidth * thispage.printobj.pscale, thispage.printobj.docheight * thispage.printobj.pscale);
                    drawmarkupPrint(thispage.printobj.printctx, thispage.pagenumber, thispage.printobj.pdx, thispage.printobj.pdy, thispage.printobj.pscale);
                };
                this.draw_image = function (clear) {
                    thispage.backupScaleAndOffset();
                    if (thispage.DocRef.bSopenSilent) {
                        return;
                    }
                    if (clear) {
                        contexto.fillStyle = displayBGColor;
                        contexto.fillRect(0, 0, canvasowidth, canvasoheight);
                    }
                    var tx = (canvasowidth / 2);
                    var ty = (canvasoheight / 2);
                    documentopen = true;
                    if (bSuspendDraw) {
                        return;
                    }
                    switch (this.currentimage) {
                        case 0:
                            if (this.getpageRotation() == 0) {
                                contexto.drawImage(this.largeimagecnv, this.dx, this.dy, this.MainImageWidth * this.dscale, this.MainImageHeight * this.dscale);
                            }
                            else {
                                contexto.save();
                                contexto.translate(tx, ty);
                                contexto.rotate(this.getpageRotation() * (Math.PI / 180));
                                contexto.translate(-tx, -ty);
                                contexto.drawImage(this.largeimagecnv, this.dx, this.dy, this.MainImageWidth * this.dscale, this.MainImageHeight * this.dscale);
                                contexto.restore();
                            }
                            break;
                        case 1:
                            var drawscale = thispage.dscale / thispage.bitmapratio;
                            if (this.getpageRotation() == 0) {
                                contexto.drawImage(this.smallimagecnv, this.dx, this.dy, this.SmallImageWidth * drawscale, this.SmallImageHeight * drawscale);
                            }
                            else {
                                contexto.save();
                                contexto.translate(tx, ty);
                                contexto.rotate(this.getpageRotation() * (Math.PI / 180));
                                contexto.translate(-tx, -ty);
                                contexto.drawImage(this.smallimagecnv, this.dx, this.dy, this.SmallImageWidth * drawscale, this.SmallImageHeight * drawscale);
                                contexto.restore();
                            }
                            break;
                    }
                };
                //prevent automatic loading of thumbnails SO175
            }
        }

        

        class DocumentObject {
            constructor(xmlDoc) {
                //File Information section
                var thisdocument = this;
                var relpath = xmlurlrel;
                var nummarkupfiles = 0;
                var nummarkupfilesread = false;
                var markupfilesreceived = 0;
                var bMultiMarkupselected = false;

                if (bUseCustomrelpath) {
                    relpath = xmlurlrelcustom;
                }
                
                this.bRenderinProgress = false;
                this.bcompare = false; 
                this.usepagesload = false;
                this.bdelayedClose = false;
                this.fileindex = 0;
                this.relativepath = undefined;
                this.bActive = true;
                //SetActive
                this.bSopenSilent = false;
                this.textselect = false;

                this.id = Math.ceil(Math.random() * 10000);

                this.hastext = false;
                this.markuploaded = false;
                this.pagelocked = false;
                this.lockedpage = thisdocument.currentpage;
                this.prevpage = 0;
                this.currentpage = 0;
                this.scrollbarwidth = 15;
                this.restorecurpage = 0;
                this.backgroundColor = backgroundColor;
                this.monoChrome = false;
                this.bMarkupchanged = false;
                this.curcontrol3D = 'orbitControl';
                this.bMarkupLocked = false;
                this.bDoPrint = false;
                this.bLargePDF = false;
                this.FileLocal = false;
                this.largePDFPagerange = 4;
                this.endpagerange = 4;
                this.startpagerange = 0;
                this.pagerangelimit = 1;
                this.pdfLoadingTask = null;
                this.PDFWrapper = null;
                this.foxitdoc = null;
                this.documentrotation = 0;
                this.foxitannotations = undefined;
                this.bdestroyPDFonClose = false;
                this.PDFpagescale = { width: 1, height: 1, scale: 1 };
                this.PDFPassword = null;

                this.markup3DSavepath = null;
                this.views3DSavepath = null;

                this.setComparelink = undefined;

                this.tempPage = undefined;

                this.markupsave = undefined;
                this.markupselectedChangeSaved = false;

                this.xrefs = [];
                this.fonts = [];
                this.markupfileURLs = [];
                this.scalesOptions = [];

                //this.foxitSearcMatches = [];
                this.markupsearchobj = {
                    matcharray: [],
                    active: false,
                    forward: true,
                    casesens: false,
                    expr: "",
                    indx: 0,
                    page: 0,
                    padding : {x:50, y:50, w: 50, h:100},
                    next: function () {
                        if (this.matcharray.length == 0) {
                            return;
                        }
                        this.indx++;
                        if (this.indx > this.matcharray.length - 1) {
                            this.indx = 0;
                        }
                        this.page = this.matcharray[this.indx].pagenumber;
                    },
                    previous: function () {
                        if (this.matcharray.length == 0) {
                            return;
                        }
                        this.indx--;
                        if (this.indx < 0) {
                            this.indx = this.matcharray.length - 1;
                        }
                        this.page = this.matcharray[this.indx].pagenumber;
                    },
                    start: function () {
                        if (this.matcharray.length == 0) {
                            return;
                        }
                        this.page = this.matcharray[this.indx].pagenumber;
                    }
                };
                this.foxitsearchobjrect = {
                    matcharray: [],
                    pagedims : [],
                    curRect: { left: 0, right: 0, top: 0, bottom: 0 },
                    active: false,
                    forward: true,
                    casesens: false,
                    expr: "",
                    outerindx: 0,
                    innerindx: 0,
                    page: 0,
                    firstpage : 0,
                    lastpage : 0,
                    pageindex : 0,
                    foxit : true,

                    getpages: function(){

                        for (var sm = 0; sm < this.matcharray.length; sm++) {
                            //nummatches += thisdocument.foxitSearcMatches[sm].length;

                            this.pagedims.push({page : this.matcharray[sm][0].match.pageIndex, done : false});
                            /*for (var pm = 0; pm < this.matcharray[sm].length; pm++) {
                                this.pagedims.push({page : this.matcharray[sm][pm].match.pageIndex, done : false});
                            }*/
                        }

                        return this.pagedims;

                    },
                    next: function () {
                        if (this.matcharray.length == 0) {
                            return;
                        }
                        if (this.innerindx < this.matcharray[this.outerindx].length - 1) {
                            this.innerindx++;
                        }
                        else if (this.outerindx < this.matcharray.length - 1) {
                            this.innerindx = 0;
                            this.outerindx++;
                        }
                        else {
                            this.outerindx = 0;
                            this.innerindx = 0;
                        }
                        if (this.matcharray[this.outerindx][this.innerindx].match == undefined) {
                            this.outerindx = 0;
                            this.innerindx = 0;
                        }
                        else {
                            this.page = this.matcharray[this.outerindx][this.innerindx].match.pageIndex;
                        }

                    },
                    previous: function () {
                        if (this.matcharray[this.outerindx].length == 0) {
                            return;
                        }
                        if (this.matcharray.length == 0) {
                            return;
                        }
                        if (this.innerindx > 0) {
                            this.innerindx--;
                        }
                        else if (this.outerindx > 0) {
                            this.innerindx = this.matcharray[this.outerindx].length - 1;
                            this.outerindx--;
                        }
                        else {
                            this.outerindx = this.matcharray.length - 1;
                            this.innerindx = this.matcharray[this.outerindx].length - 1;
                        }
                        if (this.matcharray[this.outerindx][this.innerindx].match == undefined) {
                            this.outerindx = 0;
                            this.innerindx = 0;
                        }
                        else {
                            this.page = this.matcharray[this.outerindx][this.innerindx].match.pageIndex;
                        }

                    },
                    start: function () {
                        if (this.matcharray.length == 0) {
                            return;
                        }

                        for (var sm = 0; sm < this.matcharray.length; sm++) {
                            //nummatches += thisdocument.foxitSearcMatches[sm].length;

                            this.pagedims.push({page : this.matcharray[sm][0].match.pageIndex, done : false});
                            /*for (var pm = 0; pm < this.matcharray[sm].length; pm++) {
                                this.pagedims.push({page : this.matcharray[sm][pm].match.pageIndex, done : false});
                            }*/
                        }
                        
                        this.pageindex = 0;
                        this.firstpage = this.pagedims[this.pageindex].page;
                        this.lastpage = this.pagedims[this.pagedims.length - 1].page;
                        //if thisdocument.currentpage
                        this.page = this.firstpage;

                        if(thisdocument.currentpage == this.pagedims[this.pageindex].page)
                        if(this.active){
                            this.setpagerect(this.pagedims[this.pageindex].page);   
                            //foxitpage
                        }else{
                            thisdocument.GotoPage(this.firstpage);
                        }

                        //get all pages from matcharray.
                        //get current page

                    },
                    getcurmatch: function () {

                        return this.matcharray[this.outerindx][this.innerindx] ? this.matcharray[this.outerindx][this.innerindx].match : null;
                        //return this.matcharray[this.outerindx][this.innerindx].match;


                    },
                    getpagerects  : function(pagenum){
                        //var curmatch = this.matcharray[this.outerindx][this.innerindx].match;
                        if(pagenum && thisdocument.pages[this.page] != undefined){
                            var pageobj = thisdocument.pages[pagenum];
                        }else{
                            pageobj = thisdocument.pages[this.page];
                        }

                        return pageobj.getpagerectdim(this.matcharray); 

                    },
                    setpagerect : function(pagenum){


                        if(pagenum == this.pagedims[this.pageindex].page && !this.pagedims[this.pageindex].done ){
                            
                            //thisdocument.pages[pagenum].getpagerectdim(this.matcharray);
                            if(RxCore_GUI_MathcesRectsPage != undefined){

                                this.page = pagenum;
                                RxCore_GUI_MathcesRectsPage.numTextFound(thisdocument.pages[this.page].getpagerectdim(this.matcharray), pagenum);

                            }

                            this.pagedims[this.pageindex].done = true;


                            if(this.pageindex < this.pagedims.length - 1){
                                this.pageindex++;
                                thisdocument.GotoPage(this.pagedims[this.pageindex].page, true);
                            }else if(this.pageindex == this.pagedims.length - 1){
                                //complete

                                thisdocument.foxitSearchTextRectEnd();
                            }



                        }else{
                            //thisdocument.GotoPage(this.lastpage, true);
                            thisdocument.GotoPage(this.pagedims[this.pageindex].page, true);
                        }

                        //setfoxitPageSize
                    },
                    getpagesdone : function(){
                        return this.pagedims.length;
                    } 
                };

                this.foxitsearchobj = {
                    matcharray: [],
                    clonearray: [],
                    curRect: { left: 0, right: 0, top: 0, bottom: 0 },
                    active: false,
                    forward: true,
                    casesens: false,
                    expr: "",
                    outerindx: 0,
                    innerindx: 0,
                    page: 0,
                    wholedocument: false,
                    wholeword: false,
                    orangeItem: null,
                    
                    next: function () {
                        if (this.matcharray.length == 0) {
                            return;
                        }
                        if (this.innerindx < this.matcharray[this.outerindx].length - 1) {
                            this.innerindx++;
                        }
                        else if (this.outerindx < this.matcharray.length - 1) {
                            this.innerindx = 0;
                            this.outerindx++;
                        }
                        else {
                            this.outerindx = 0;
                            this.innerindx = 0;
                        }
                        //if(this.innerindx > this.matcharray[this.outerindx][this.innerindx].length - 1){
                        /*if(this.innerindx > this.matcharray[this.outerindx].length - 1){
                            if (this.outerindx > this.matcharray.length - 1){
                                this.outerindx = 0;
                                this.innerindx = 0;
                            }else{
                                this.outerindx ++;
                            }
        
                            //this.innerindx = 0;
                        }else{
                                //more matches in next array
                            if (this.outerindx > this.matcharray.length - 1){
                                this.outerindx = 0;
                                this.innerindx = 0;
                            }else{
                                this.outerindx ++;
                            }
                
                                //this.innerindx = 0;
                        }*/
                        /*if (this.outerindx > this.matcharray.length - 1){
                            this.outerindx = 0;
                            this.innerindx = 0;
                        }else{
                            this.outerindx ++;
                        }*/
                        /*if(this.innerindx > this.matcharray[this.outerindx].length - 1){
                            if (this.outerindx > this.matcharray.length - 1){
                                //last entry start over.
                                this.outerindx = 0;
                                this.innerindx = 0;
                            }else{
                                //more matches in next array
                            }
                            
                        }*/
                        //SearcMatches[page][indx]
                        if (this.matcharray[this.outerindx][this.innerindx] != undefined) {
                            if (this.matcharray[this.outerindx][this.innerindx].match == undefined) {
                                this.outerindx = 0;
                                this.innerindx = 0;
                            }
                            else {
                                this.page = this.matcharray[this.outerindx][this.innerindx].match.pageIndex;
                            }
    
                        }else{
                            this.outerindx = 0;
                            this.innerindx = 0;
                        }


                        //SearcMatches[sm][pm].match.pageIndex
                    },
                    previous: function () {
                        if (this.matcharray[this.outerindx].length == 0) {
                            return;
                        }
                        if (this.matcharray.length == 0) {
                            return;
                        }
                        
                        if (this.innerindx > this.matcharray[this.outerindx].length - 1) {
                            this.innerindx = this.matcharray[this.outerindx].length - 1;
                        }

                        if (this.innerindx > 0) {
                            this.innerindx--;
                        }
                        else if (this.outerindx > 0) {
                            this.outerindx--;
                            this.innerindx = this.matcharray[this.outerindx].length - 1;
                        }
                        else {
                            this.outerindx = this.matcharray.length - 1;
                            this.innerindx = this.matcharray[this.outerindx].length - 1;
                        }
                        /*this.innerindx--;
        
                        if(this.innerindx < 0){
                            if (this.outerindx < 0){
                                //last entry start over.
                                this.outerindx = this.matcharray.length - 1;
                                this.innerindx = this.matcharray[this.outerindx].length - 1;
                            }else{
                                //more matches in next array
                                this.outerindx --;
                                this.innerindx = this.matcharray[this.outerindx].length - 1;
                            }
                            
                        }*/
                        /*if(this.indx < 0){
                            this.indx = this.matcharray.length - 1;
                        }*/
                        //SearcMatches[page][indx]
                        //this.page = this.matcharray[this.innerindx].page;


                        if (this.matcharray[this.outerindx][this.innerindx].match == undefined) {
                            this.outerindx = 0;
                            this.innerindx = 0;
                        }
                        else {
                            this.page = this.matcharray[this.outerindx][this.innerindx].match.pageIndex;
                        }
                        //SearcMatches[sm][pm].match.pageIndex
                    },
                    start: function () {
                        if (this.matcharray.length == 0) {
                            return;
                        }
                        this.page = this.matcharray[this.outerindx][this.innerindx].match.pageIndex;
                        //SearcMatches[sm][pm].match.pageIndex
                    },
                    getcurmatch: function () {

                        return this.matcharray[this.outerindx][this.innerindx] ? this.matcharray[this.outerindx][this.innerindx].match : null;

                        //return this.matcharray[this.outerindx][this.innerindx].match;
                    }
                };
                this.searchobj = {
                    matcharray: [],
                    active: false,
                    forward: true,
                    casesens: false,
                    expr: "",
                    indx: 0,
                    page: 0,
                    next: function () {
                        if (this.matcharray.length == 0) {
                            return;
                        }
                        this.indx++;
                        if (this.indx > this.matcharray.length - 1) {
                            this.indx = 0;
                        }
                        this.page = this.matcharray[this.indx].page;
                    },
                    previous: function () {
                        if (this.matcharray.length == 0) {
                            return;
                        }
                        this.indx--;
                        if (this.indx < 0) {
                            this.indx = this.matcharray.length - 1;
                        }
                        this.page = this.matcharray[this.indx].page;
                    },
                    start: function () {
                        if (this.matcharray.length == 0) {
                            return;
                        }
                        this.page = this.matcharray[this.indx].page;
                    }
                };
                this.extractsearchobj = {
                    matcharray: [],
                    foundrects:[],
                    active: false,
                    casesens: false,
                    expr: "",
                    indx: 0,
                    page: 0,
                    start: function () {
                        if (this.matcharray.length == 0) {
                            return;
                        }
                        this.page = this.matcharray[this.indx].page;
                    },
                    next: function () {
                        if (this.matcharray.length == 0) {
                            return;
                        }
                        this.indx++;
                        if (this.indx > this.matcharray.length - 1) {
                            this.indx = 0;
                        }
                        this.page = this.matcharray[this.indx].page;
                    }
                };

                if(xmlDoc.getElementsByTagName('Version')[0] != undefined){
                    this.Version = xmlDoc.getElementsByTagName('Version')[0].firstChild.nodeValue;   
                }

                //this.Version = xmlDoc.getElementsByTagName('Version')[0].firstChild.nodeValue;

                if(xmlDoc.getElementsByTagName('NumPages')[0] != undefined){
                    this.NumPages = xmlDoc.getElementsByTagName('NumPages')[0].firstChild.nodeValue;
                }

                if(xmlDoc.getElementsByTagName('NumLayouts')[0] != undefined){
                    this.NumLayouts = xmlDoc.getElementsByTagName('NumLayouts')[0].firstChild.nodeValue;
                }


                if(xmlDoc.getElementsByTagName('FileName')[0] != undefined){
                    this.FileName = xmlDoc.getElementsByTagName('FileName')[0].firstChild.nodeValue;
                }
                

                
                this.FileName = decodeURIComponent(this.FileName);
                this.CacheFileName = this.FileName;
                var szFileName = getFileName(this.FileName);
                this.CachePath = getPath(this.FileName);
                if (bUseCustomrelpath) {
                    this.FileName = szFileName;
                }
                this.openFileName = this.FileName;

                if(xmlDoc.getElementsByTagName('OriginalURL')[0] != undefined){
                    this.OriginalURL = xmlDoc.getElementsByTagName('OriginalURL')[0].firstChild.nodeValue;
                    this.OriginalURL = decodeURIComponent(this.OriginalURL);
                }
                
                if(xmlDoc.getElementsByTagName('Format')[0] != undefined){
                    this.Format = xmlDoc.getElementsByTagName('Format')[0].firstChild.nodeValue;

                }

                if(xmlDoc.getElementsByTagName('Filter')[0] != undefined){
                    this.Filter = xmlDoc.getElementsByTagName('Filter')[0].firstChild.nodeValue;
                }

                if(xmlDoc.getElementsByTagName('FileSizeLow')[0] != undefined){
                    this.FileSizeLow = xmlDoc.getElementsByTagName('FileSizeLow')[0].firstChild.nodeValue;
                }

                if(xmlDoc.getElementsByTagName('FileSizeHigh')[0] != undefined){
                    this.FileSizeHigh = xmlDoc.getElementsByTagName('FileSizeHigh')[0].firstChild.nodeValue;
                }
                
                
                if (xmlDoc.getElementsByTagName('PublisherVersion')[0] != undefined) {
                    this.PublisherVersion = xmlDoc.getElementsByTagName('PublisherVersion')[0].firstChild.nodeValue;
                }

                if (xmlDoc.getElementsByTagName('Type')[0] != undefined) {
                    this.Type = xmlDoc.getElementsByTagName('Type')[0].firstChild.nodeValue;

                }
                
                if (xmlDoc.getElementsByTagName('XREFS') != undefined) {
                    var xmlxrefs = xmlDoc.getElementsByTagName('XREF');
                    for (var i = 0; i < xmlxrefs.length; i++) {
                        if (xmlxrefs[i].getElementsByTagName('FileName')[0].firstChild != null) {
                            var szpath = xmlxrefs[i].getElementsByTagName('FileName')[0].firstChild.nodeValue;
                            var bfound = (xmlxrefs[i].getElementsByTagName('Found')[0].firstChild.nodeValue == 'true');
                            var xrefobject = {
                                path: szpath,
                                found: bfound
                            };
                            this.xrefs.push(xrefobject);
                        }
                    }
                }
                if (xmlDoc.getElementsByTagName('Fonts') != undefined) {
                    var xmlfonts = xmlDoc.getElementsByTagName('Font');
                    for (i = 0; i < xmlfonts.length; i++) {
                        var szfontpath = xmlfonts[i].getElementsByTagName('FileName')[0].firstChild.nodeValue;
                        var bffound = (xmlfonts[i].getElementsByTagName('Found')[0].firstChild.nodeValue == 'true');
                        var fontobject = {
                            path: szfontpath,
                            found: bffound
                        };
                        this.fonts.push(fontobject);
                    }
                }
                if (xmlDoc.getElementsByTagName('Summary') != undefined) {
                    var summaryprops = xmlDoc.getElementsByTagName('Property');
                    for (i = 0; i < summaryprops.length; i++) {
                        if (summaryprops[i].getElementsByTagName('Name') != undefined && summaryprops[i].getElementsByTagName('Name')[0].firstChild != null) {
                            var szname = summaryprops[i].getElementsByTagName('Name')[0].firstChild.nodeValue;
                        }
                        else {
                            szname = "";
                        }
                        if (summaryprops[i].getElementsByTagName('Value') != undefined && summaryprops[i].getElementsByTagName('Value')[0].firstChild != null) {
                            var szvalue = summaryprops[i].getElementsByTagName('Value')[0].firstChild.nodeValue;
                        }
                        else {
                            szvalue = "";
                        }
                        if (szname == "ACAD_MEASUREMENT") {
                            this.CadMeasurement = parseInt(szvalue);
                            //0 = inch
                            //1 = metric
                        }
                    }
                }
                //docobjxmltxt += "<FileLocal>" + "1" + "</FileLocal>\n";
                if (xmlDoc.getElementsByTagName('FileLocal')[0] != undefined) {
                    this.FileLocal = true;
                    for(var li = 0; li < localfilesPending.length; li++){
                        if(localfilesPending[li].filenameonly == thisdocument.FileName){
                            updatelocalDoc(localfilesPending[li], thisdocument.fileindex);
                        }
                    }
        

                }

                //xmlDoc.getElementsByTagName('FileName')
                
                if (xmlDoc.getElementsByTagName('FileNameSRC')[0] != undefined) {
                    this.FileNameSRC = encodeURI(relpath + xmlDoc.getElementsByTagName('FileNameSRC')[0].firstChild.nodeValue);
                    this.CacheURL = getURLPath(this.FileNameSRC);
                }
                this.thumbnailobects = [];
                this.thumbnails = [];
                this.hoverspace = null;
                this.Drawmarkup = true;
                this.thumbnailhtmlsource = "";
                this.layerhtmlsource = "";
                //Arrays for layouts and pages
                this.layouts = [];
                this.pages = [];
                this.pagerotations = [];

                this.markuplist = [];
                this.markupdraworder = [];
                this.markupdraworderChange = false;
                //this.selectedmarkup = [];
                this.selectedmarkup = {
                    id: -1,
                    selected: false,
                    edit: false
                };
                this.rangeupdated = {
                    start: 0,
                    end: 0,
                    operation: 'none'
                };
                
                this.markupundolist = [];
                this.markupredolist = [];
                this.tempundolist = [];

                this.documentproperties = [];
                this.pageSearchArr = [];
                this.nummarkups = 0;
                this.pdfDoc = null;
                this.pdfURL = "";
                this.pageNum = 1;
                this.pageRendering = false;
                this.pageNumPending = null;
                this.scale = 0.8;
                this.pdftimervar = null;
                this._pagesRefCache = Object.create(null);
                this.pdfoutline = undefined;
                this.scrollbuttonStats = { topscrbutton: false, bottomscrbutton: false, rightscrbutton: false, leftscrbutton: false };
                this.mouseOffScroll = function () {
                    if (thisdocument.foxitdoc == null) {
                        return;
                    }
                    thisdocument.foxitdoc.onScollV(false);
                    thisdocument.foxitdoc.onScollVGrip(false);
                    thisdocument.scrollbuttonStats.bottomscrbutton = false;
                    thisdocument.scrollbuttonStats.topscrbutton = false;
                    thisdocument.scrollbuttonStats.leftscrbutton = false;
                    thisdocument.scrollbuttonStats.rightscrbutton = false;
                };
                this.holdOnScrollbutton = function () {
                    if (thisdocument.foxitdoc == null) {
                        return;
                    }
                    if (thisdocument.scrollbuttonStats.bottomscrbutton) {
                        thisdocument.foxitdoc.onScrollVbuttonBottom();
                        //thisdocument.pages[thisdocument.currentpage].dypdf -= 40;
                    }
                    else if (thisdocument.scrollbuttonStats.topscrbutton) {
                        thisdocument.foxitdoc.onScrollVbuttonTop();
                        //thisdocument.pages[thisdocument.currentpage].pan_update(0, -40);
                    }
                    else if (thisdocument.scrollbuttonStats.leftscrbutton) {
                        thisdocument.foxitdoc.onScrollHbuttonLeft();
                        //thisdocument.pages[thisdocument.currentpage].pan_update(-40, 0);
                    }
                    else if (thisdocument.scrollbuttonStats.rightscrbutton) {
                        thisdocument.foxitdoc.onScrollHbuttonRight();
                        //thisdocument.pages[thisdocument.currentpage].pan_update(40, 0);
                    }
                };
                this.foxitpagescalecheck = function(pagescale){
                    if (thisdocument.foxitdoc == null) {
                        return;
                    }

                    thisdocument.pages[thisdocument.currentpage].foxitpagescalecheck(pagescale);


                };

                this.scalecomparepage = function(scalearray){
                    if(thisdocument.bcompare){
                        if(thisdocument.pages[thisdocument.currentpage].VectorPageObj){
                            thisdocument.pages[thisdocument.currentpage].VectorPageObj.scalecomparepage(scalearray);
                        }
                    }

                    
                };


                this.scalecompare = function(){
                    if(thisdocument.bcompare){
                        if(thisdocument.pages[thisdocument.currentpage].VectorPageObj){
                            thisdocument.pages[thisdocument.currentpage].VectorPageObj.scalecompare();
                        }
                    }
                };

                this.setColors = function (bgColor, fgColor) {


                    if(thisdocument.bcompare){
                        if(thisdocument.pages[thisdocument.currentpage].VectorPageObj){
                            thisdocument.pages[thisdocument.currentpage].VectorPageObj.setColors(bgColor, fgColor);
                        }
                    }


                };
                this.getColors = function () {

                    if(thisdocument.bcompare){
                        if(thisdocument.pages[thisdocument.currentpage].VectorPageObj){
                            return thisdocument.pages[thisdocument.currentpage].VectorPageObj.getColors();
                        }
                    }

                };

                this.nudgeRotate = function(clockwise){
                    if(thisdocument.bcompare){
                        if(thisdocument.pages[thisdocument.currentpage].VectorPageObj){
                            return thisdocument.pages[thisdocument.currentpage].VectorPageObj.nudgeRotate(clockwise);
                        }
                    }

                };

                this.nudgeScale = function (updown) {
                    if(thisdocument.bcompare){
                        if(thisdocument.pages[thisdocument.currentpage].VectorPageObj){
                            return thisdocument.pages[thisdocument.currentpage].VectorPageObj.nudgeScale(updown);
                        }
                    }

                };

                this.offsetdirect = function (x,y) {
                    if(thisdocument.bcompare){
                        if(thisdocument.pages[thisdocument.currentpage].VectorPageObj){
                            thisdocument.pages[thisdocument.currentpage].VectorPageObj.offsetdirect(x,y);
                        }
                    }


                };
                this.nudgeoffset = function (direction) {
                    if(thisdocument.bcompare){
                        if(thisdocument.pages[thisdocument.currentpage].VectorPageObj){
                            thisdocument.pages[thisdocument.currentpage].VectorPageObj.nudgeoffset(direction);
                        }
                    }


                };

                this.setcomparescale = function (scalearray) {
                    if(thisdocument.bcompare){
                        if(thisdocument.pages[thisdocument.currentpage].VectorPageObj){
                            thisdocument.pages[thisdocument.currentpage].VectorPageObj.setcomparescale(scalearray);
                        }
                    }


                };                

                this.checkRender = function() {

                    if(RxCore_GUI_RenderComplete != undefined){
                        RxCore_GUI_RenderComplete.renderComplete(thisdocument.fileindex);
                    }


                };

                this.setmarkupfilepaths = function(markupfilepath){

                    thisdocument.markupfileURLs.push(markupfilepath);
                };

                this.getmarkupfilepaths = function(markupfilepath){
                    
                    return thisdocument.markupfileURLs;
                    
                };


                this.setMarkup3DSavefile = function(fileName){
                    thisdocument.markup3DSavepath = fileName;
                };

                this.getMarkup3DSavefile = function(){
                    return thisdocument.markup3DSavepath;
                };

                this.setViews3DSavefile = function(fileName){
                    thisdocument.views3DSavepath = fileName;
                };

                this.getViews3DSavefile = function(){
                    return thisdocument.views3DSavepath;
                };


                this.ScrollBarCheck = function () {
                    if (thisdocument.foxitdoc == null) {
                        return;
                    }
                    var canvaswidth = canvasowidth;
                    var canvasheight = canvasoheight;

                    canvaswidth = canvasowidthsize;
                    canvasheight = canvasoheightsize;


                    var scrolldata = thisdocument.foxitdoc.getScrollBarStatus();
                    
                    thisdocument.scrollbarwidth = thisdocument.foxitdoc.getScrollbarWidth();
                    thisdocument.foxitdoc.setmarkupPosition(thisdocument.currentpage);

                    var mousedirect = { mx: 0, my: 0 };

                    //experimental ineight paging issue
                    //thisdocument.foxitdoc.scrolldeltaupdate(mousedirect, thisdocument.currentpage);
                    if (scrolldata.vBarOn) {
                        canvaswidth = canvasowidth - thisdocument.scrollbarwidth;
                        canvaswidth = canvasowidthsize - thisdocument.scrollbarwidth;
                    }
                    else {
                        //canvaswidth = canvasowidth;
                        canvaswidth = canvasowidthsize;
                    }
                    if (scrolldata.hBarOn) {
                        canvasheight = canvasoheight - thisdocument.scrollbarwidth;
                        canvasheight = canvasoheightsize - thisdocument.scrollbarwidth;

                    }
                    else {
                        //canvasheight = canvasoheight;
                        canvasheight = canvasoheightsize;
                    }

                    if (thisdocument.foxitdoc != null) {

                        var pagepos = thisdocument.foxitdoc.getPagePos(thisdocument.currentpage);

                        if(thisdocument.pages[thisdocument.currentpage] != undefined){
                            thisdocument.pages[thisdocument.currentpage].foxitonScrollupdate(pagepos);
                        }
                        

                    }
                    

                    resizeFoxitScroll(canvaswidth, canvasheight);
                };
                this.mouseOnScroll = function (mousepos) {
                    if (thisdocument.foxitdoc == null) {
                        return;
                    }
                    if (thisdocument.foxitdoc != null) {
                        return;
                    }
                    var canvaswidth = canvasowidth;
                    var canvasheight = canvasoheight;
                    var fromedgex = canvasowidth - mousepos.x;
                    var fromedgey = canvasoheight - mousepos.y;
                    thisdocument.scrollbuttonStats.bottomscrbutton = false;
                    thisdocument.scrollbuttonStats.topscrbutton = false;
                    thisdocument.scrollbuttonStats.leftscrbutton = false;
                    thisdocument.scrollbuttonStats.rightscrbutton = false;
                    //var scrolldata = thisdocument.foxitdoc.getScrollbarData(canvasowidth, canvasoheight);
                    thisdocument.foxitdoc.setScrollFactors(canvasowidth, canvasoheight);
                    var scrolldata = thisdocument.foxitdoc.getScrollBarStatus();
                    var scrgripdata = thisdocument.foxitdoc.getScrollGrip(canvasowidth, canvasoheight);
                    //return {onVBar : foxview.onVBar, onHBar : foxview.onHBar, vBarOn : foxview.vBarOn, hBarOn : foxview.hBarOn, ScrollWfactor : ScrollWidthfactor, ScrollHfactor : ScrollHeightfactor};
                    if (scrolldata.vBarOn) {
                        //resize  width of canvases
                        thisdocument.scrollbarwidth = thisdocument.foxitdoc.getScrollbarWidth();
                        canvaswidth = canvasowidth - thisdocument.scrollbarwidth;
                        //var canvaswidth = canvasowidth;
                        //var canvasheight = canvasoheight;
                        //var canvasheight = canvasoheight;
                        if (fromedgex < thisdocument.scrollbarwidth && fromedgex > 0) {
                            //return {sizeH : gripsizeH,sizeV : gripsizeV, posy : scrollposy, posx : scrollposx};
                            var distfromgrip = scrgripdata.posy - mousepos.y;
                            var distfromgripneg = mousepos.y - scrgripdata.posy;
                            //var disfromtop = mousepos.y 
                            if (scrolldata.hBarOn) {
                                var btnheight = thisdocument.scrollbarwidth * 2;
                            }
                            else {
                                btnheight = thisdocument.scrollbarwidth;
                            }
                            if (fromedgey > 0 && fromedgey < btnheight) {
                                //console.log('bottom scroll button');
                                //bottomscrbutton = true;
                                thisdocument.scrollbuttonStats.bottomscrbutton = true;
                            }
                            else {
                                thisdocument.scrollbuttonStats.bottomscrbutton = false;
                            }
                            if (mousepos.y > 0 && mousepos.y < thisdocument.scrollbarwidth) {
                                //console.log('top scroll button');
                                //topscrbutton = true;
                                thisdocument.scrollbuttonStats.topscrbutton = true;
                            }
                            else {
                                thisdocument.scrollbuttonStats.topscrbutton = false;
                            }
                            if (distfromgripneg < scrgripdata.sizeV && distfromgripneg > 0) {
                                //console.log('onVscrollgrip', distfromgrip);    
                                //console.log('onVscrollgrip', distfromgripneg); 
                                thisdocument.foxitdoc.onScollVGrip(true);
                            }
                            else {
                                if (distfromgripneg < 0) {
                                    //console.log('abovegrip', distfromgripneg);
                                    if (thisdocument.scrollbuttonStats.topscrbutton) {
                                        //thisdocument.foxitdoc.onScrollVbuttonTop();    
                                    }
                                    else {
                                        thisdocument.foxitdoc.onScrollVPageUp(canvasowidth, canvasoheight);
                                    }
                                }
                                else if (distfromgripneg > 0) {
                                    //console.log('belowgrip', distfromgripneg);
                                    if (thisdocument.scrollbuttonStats.bottomscrbutton) {
                                        //thisdocument.foxitdoc.onScrollVbuttonBottom();    
                                    }
                                    else {
                                        thisdocument.foxitdoc.onScrollVPageDown(canvasowidth, canvasoheight);
                                    }
                                    //thisdocument.scrollbuttonStats.bottomscrbutton = false;
                                }
                                thisdocument.foxitdoc.onScollVGrip(false);
                            }
                            thisdocument.foxitdoc.onScollV(true);
                            //if (fromedgey )
                            //console.log('onVscroll', scrgripdata);    
                        }
                        else {
                            thisdocument.foxitdoc.onScollV(false);
                            thisdocument.foxitdoc.onScollVGrip(false);
                        }
                    }
                    if (scrolldata.hBarOn) {
                        //resize  height of canvases
                        thisdocument.scrollbarwidth = thisdocument.foxitdoc.getScrollbarWidth();
                        //canvaswidth = canvasowidth - thisdocument.scrollbarwidth;
                        //var canvaswidth = canvasowidth;
                        canvasheight = canvasoheight - thisdocument.scrollbarwidth;
                        if (fromedgey < thisdocument.scrollbarwidth && fromedgey > 0) {
                            var distfromgrip = scrgripdata.posx - mousepos.x;
                            var distfromgripneg = mousepos.x - scrgripdata.posx;
                            if (scrolldata.vBarOn) {
                                var btnwidth = thisdocument.scrollbarwidth * 2;
                            }
                            else {
                                btnwidth = thisdocument.scrollbarwidth;
                            }
                            if (fromedgex > 0 && fromedgex < btnwidth) {
                                //console.log('bottom scroll button');
                                //rightscrbutton = true;
                                thisdocument.scrollbuttonStats.rightscrbutton = true;
                                //{topscrbutton : false, bottomscrbutton : false, rightscrbutton :false, leftscrbutton : false};
                            }
                            else {
                                thisdocument.scrollbuttonStats.rightscrbutton = false;
                            }
                            if (mousepos.x > 0 && mousepos.x < thisdocument.scrollbarwidth) {
                                //console.log('top scroll button');
                                thisdocument.scrollbuttonStats.leftscrbutton = true;
                                //leftscrbutton = true;
                            }
                            else {
                                thisdocument.scrollbuttonStats.leftscrbutton = false;
                            }
                            if (distfromgripneg < scrgripdata.sizeH && distfromgripneg > 0) {
                                thisdocument.foxitdoc.onScollHGrip(true);
                            }
                            else {
                                if (distfromgripneg < 0) {
                                    //console.log('abovegrip', distfromgripneg);
                                    if (thisdocument.scrollbuttonStats.leftscrbutton) {
                                        //thisdocument.foxitdoc.onScrollHbuttonLeft();    
                                    }
                                    else {
                                        thisdocument.foxitdoc.onScrollHPageLeft(canvasowidth, canvasoheight);
                                    }
                                    //thisdocument.scrollbuttonStats.leftscrbutton = false;
                                }
                                else if (distfromgripneg > 0) {
                                    //console.log('belowgrip', distfromgripneg);
                                    if (thisdocument.scrollbuttonStats.rightscrbutton) {
                                        //thisdocument.foxitdoc.onScrollHbuttonRight();    
                                    }
                                    else {
                                        thisdocument.foxitdoc.onScrollHPageRight(canvasowidth, canvasoheight);
                                    }
                                    //thisdocument.scrollbuttonStats.rightscrbutton = false;
                                }
                                thisdocument.foxitdoc.onScollHGrip(false);
                            }
                            thisdocument.foxitdoc.onScollH(true);
                            //console.log('onHscroll');    
                        }
                        else {
                            thisdocument.foxitdoc.onScollH(false);
                            thisdocument.foxitdoc.onScollHGrip(false);
                        }
                    }
                    //var canvaswidth = canvasowidth;
                    //var canvasheight = canvasoheight;
                    resizeFoxitScroll(canvaswidth, canvasheight);
                    return scrolldata;
                };
                this.ScrollFoxitDiv = function () {
                    if (thisdocument.foxitdoc != null) {
                        thisdocument.foxitdoc.scrolltoPageCorner();
                    }
                };
                this.updateURL = function (xmlDoc) {
                    if (xmlDoc.getElementsByTagName('OriginalURL')[0] != undefined) {
                        thisdocument.OriginalURL = xmlDoc.getElementsByTagName('OriginalURL')[0].firstChild.nodeValue;
                        thisdocument.OriginalURL = decodeURIComponent(thisdocument.OriginalURL);
                    }
                    if (xmlDoc.getElementsByTagName('FileNameSRC')[0] != undefined) {
                        thisdocument.FileNameSRC = encodeURI(relpath + xmlDoc.getElementsByTagName('FileNameSRC')[0].firstChild.nodeValue);
                        thisdocument.CacheURL = getURLPath(thisdocument.FileNameSRC);
                    }
                };
                this.setPassword = function (password) {
                    thisdocument.PDFPassword = password;
                };
                this.setfileindex = function (index) {
                    thisdocument.fileindex = index;
                    if (thisdocument.foxitdoc != null) {
                        thisdocument.foxitdoc.rxindex = index;
                    }
                };
                this.setnummarkupfiles = function (num) {
                    nummarkupfiles = num;
                    nummarkupfilesread = true;
                };
                this.getnummarkupfiles = function () {
                    return nummarkupfiles;
                };
                this.getmarkupfilesread = function () {
                    return nummarkupfilesread;
                };
                this.setmarkupfilesreceived = function () {
                    markupfilesreceived++;
                };
                this.getmarkupfilesreceived = function () {
                    return markupfilesreceived;
                };

                this.setFoxitAnnotlist = function(annotlist){

                    if (thisdocument.foxitdoc != null) {

                        thisdocument.foxitannotations = annotlist;
                        
                    }


                };

                this.getFoxitAnnot = function (ref) {

                    var annot = undefined;
          
                    if (thisdocument.foxitdoc != null) {
                      annot = thisdocument.foxitannotations[ref]
                    }
          
                    return annot;
          
                  };
          

                this.getmarkuplist = function(){

                    var result = thisdocument.markuplist.filter( function(markup){

                       return !(markup.type == 8 && markup.subtype == 2);

                    });
                    
                    return result;
                    
                };


                

                this.foxitSearchTextRectEnd = function(){
                    //foxitsearchobjrect

                    thisdocument.foxitsearchobjrect.matcharray = [];
                    thisdocument.foxitsearchobjrect.pagedims = [];
                    thisdocument.foxitsearchobjrect.expr = "";
                    thisdocument.foxitsearchobjrect.forward = true;
                    thisdocument.foxitsearchobjrect.casesens = false;
                    thisdocument.foxitsearchobjrect.active = false;
                    thisdocument.foxitsearchobjrect.outerindx = 0;
                    thisdocument.foxitsearchobjrect.innerindx = 0;

                    //thisdocument.foxitsearchobjrect.indx = 0;
                    thisdocument.foxitsearchobjrect.page = 0;

                    if (RxCore_GUI_NumMathcesRect != undefined) {
                        RxCore_GUI_NumMathcesRect.numTextFound(thisdocument.foxitsearchobjrect, thisdocument.currentpage);
                    }

                };
                
                this.endMarkupTextSearch = function(){


                    thisdocument.markupsearchobj.matcharray = [];
                    thisdocument.markupsearchobj.expr = "";
                    thisdocument.markupsearchobj.forward = true;
                    thisdocument.markupsearchobj.casesens = false;
                    thisdocument.markupsearchobj.active = false;
                    thisdocument.markupsearchobj.indx = 0;
                    thisdocument.markupsearchobj.page = 0;
                    thisdocument.markupsearchobj.padding = {x:50, y:50, w: 50, h:100};


                    if (RxCore_GUI_NumMathcesMarkup != undefined) {
                        RxCore_GUI_NumMathcesMarkup.numTextFound(thisdocument.markupsearchobj.matcharray.length);
                    }

                };


                this.textrectsearchend = function(){

                    if (thisdocument.foxitdoc != null) {
                        thisdocument.foxitSearchTextRectEnd();
                        return;
                    }


                    thisdocument.extractsearchobj.matcharray = [];
                    thisdocument.extractsearchobj.expr = "";
                    thisdocument.extractsearchobj.forward = true;
                    thisdocument.extractsearchobj.casesens = false;
                    thisdocument.extractsearchobj.active = false;
                    thisdocument.extractsearchobj.indx = 0;
                    thisdocument.extractsearchobj.page = 0;

                    if (thisdocument.pages[thisdocument.currentpage].usepdfjs) {
                        thisdocument.enableTextSelect(false);
                    }
                    if (thisdocument.pages[thisdocument.currentpage].usevectorxml) {
                        thisdocument.enableTextSelect(false);
                    }
                    
                    if (RxCore_GUI_NumMathcesRect != undefined) {
                        RxCore_GUI_NumMathcesRect.numTextFound(thisdocument.extractsearchobj, thisdocument.currentpage);
                    }



                };
                this.textsearchend = function () {
                    if (thisdocument.foxitdoc != null) {
                        thisdocument.foxitSearchTextEnd();
                        return;
                    }
                    //thisdocument.foxitSearcMatches = [];
                    //thisdocument.foxitsearchobj.matcharray = [];
                    if (thisdocument.searchobj.active) {
                        thisdocument.pages[thisdocument.currentpage].clearSearch();
                    }
                    thisdocument.searchobj.matcharray = [];
                    thisdocument.searchobj.expr = "";
                    thisdocument.searchobj.forward = true;
                    thisdocument.searchobj.casesens = false;
                    thisdocument.searchobj.active = false;
                    thisdocument.searchobj.indx = 0;
                    thisdocument.searchobj.page = 0;
                    if (thisdocument.pages[thisdocument.currentpage].usepdfjs) {
                        thisdocument.enableTextSelect(false);
                    }
                    if (thisdocument.pages[thisdocument.currentpage].usevectorxml) {
                        thisdocument.enableTextSelect(false);
                    }
                    if (RxCore_GUI_NumMathces != undefined) {
                        RxCore_GUI_NumMathces.numTextFound(thisdocument.searchobj.matcharray.length);
                    }
                };

                this.getsearchresultrect = function(searchres, text, pgindex){

                    var baddlist = false;
                    for (var si = 0; si < searchres.list.length; si++) {
                        var spage = searchres.list[si].match.pageIndex;
                        baddlist = (spage == pgindex);
                    }
                    
                    /*if (baddlist) {
                        thisdocument.foxitsearchobjrect.matcharray.push(searchres.list);
                    }*/

                    thisdocument.foxitsearchobjrect.matcharray.push(searchres.list);

                    if (searchres.hasMore && pgindex < thisdocument.pages.length) {
                        thisdocument.foxitdoc.getTextSearch(text, pgindex + 1, thisdocument.getsearchresultrect, thisdocument.foxitsearchobjrect.casesens);
                    }else {
                        var nummatches = 0;
                        for (var sm = 0; sm < thisdocument.foxitsearchobjrect.matcharray.length; sm++) {
                            
                            nummatches += thisdocument.foxitsearchobjrect.matcharray[sm].length;
                            
                            /*for(var pm = 0; pm < thisdocument.foxitSearcMatches[sm].length;pm++){
        
                            }*/
                        }


                        if (nummatches > 0) {
                            thisdocument.foxitsearchobjrect.active = true;

                        }

                        thisdocument.foxitsearchobjrect.start();

                        if (RxCore_GUI_NumMathcesRect != undefined) {
                            RxCore_GUI_NumMathcesRect.numTextFound(thisdocument.foxitsearchobjrect, thisdocument.currentpage);
                        }

                        
                        
                        /*if (RxCore_GUI_NumMathces != undefined) {
                            RxCore_GUI_NumMathces.numTextFound(thisdocument.foxitsearchobjrect, thisdocument.currentpage);
                        }*/


                        /*if (nummatches > 0) {
                            thisdocument.foxitsearchobjrect.active = true;
                            thisdocument.foxitsearchobjrect.start();
                            
                            if (thisdocument.foxitsearchobjrect.page != thisdocument.currentpage) {
                                thisdocument.GotoPage(thisdocument.foxitsearchobjrect.page);
                                thisdocument.pageSearchArr.push(thisdocument.foxitsearchobjrect.page);
                            }
                            else {


                                //calculate rect not show rect.
                                //thisdocument.pages[thisdocument.currentpage].showFoxitSearch();
                            }
                            //make new showSearch method.
                        }*/
                    }

                };

                this.drawselectedBlocks = function(){

                    if(thisdocument.pages[thisdocument.currentpage] != undefined){
                        thisdocument.pages[thisdocument.currentpage].renderSelectedBlocks();
                        thisdocument.pages[thisdocument.currentpage].rendervectormouseover();
                        

                    }

                    


                }

                this.unselectallBlocks = function(){
                    if(thisdocument.pages[thisdocument.currentpage] != undefined){
                        thisdocument.pages[thisdocument.currentpage].unSelectAllBlockInserts();
                    }

                };
                
                this.drawsearchRect = function(){


                    if (thisdocument.foxitsearchobj.wholedocument) {

                        if(thisdocument.pages[thisdocument.currentpage] != undefined){
                            thisdocument.pages[thisdocument.currentpage].drawsearchRects(
                                thisdocument.foxitsearchobj.clonearray
                            );
    
                        }

                    }


                    if(thisdocument.foxitsearchobj.active){

                        if(thisdocument.pages[thisdocument.currentpage] != undefined){
                            thisdocument.pages[thisdocument.currentpage].drawsearchRect();
                        }
                        
                    }

                    //thisdocument.pages[thisdocument.currentpage].drawsearchRect();
                };    

                this.getsearchresult = function (searchres, text, pgindex) {
                    //console.log(searchres);
                    var baddlist = false;

                    for (var si = 0; si < searchres.list.length; si++) {
                        var spage = searchres.list[si].match.pageIndex;
                        baddlist = (spage == pgindex);
                    }
                    
                    thisdocument.foxitsearchobj.matcharray.push(searchres.list);

                    /*if (baddlist) {
                        thisdocument.foxitsearchobj.matcharray.push(searchres.list);
                    }*/

                    /*this.foxitsearchobj = {
                        matcharray : [],*/
                    //thisdocument.foxitSearcMatches.push(searchres.list);
                    //var pgindex = 0;
                    if (searchres.hasMore && pgindex < thisdocument.pages.length) {
                        //thisdocument.foxitdoc.getTextSearch(text, pgindex + 1, thisdocument.getsearchresult);
                        thisdocument.foxitdoc.getTextSearch(text, pgindex + 1, thisdocument.getsearchresult, thisdocument.foxitsearchobj.casesens);
                    }
                    else {
                        var nummatches = 0;
                        for (var sm = 0; sm < thisdocument.foxitsearchobj.matcharray.length; sm++) {
                            nummatches += thisdocument.foxitsearchobj.matcharray[sm].length;
                            /*for(var pm = 0; pm < thisdocument.foxitSearcMatches[sm].length;pm++){
        
                            }*/
                        }
                        if (RxCore_GUI_NumMathces != undefined) {
                            RxCore_GUI_NumMathces.numTextFound(nummatches);
                        }
                        if (nummatches > 0) {
                            thisdocument.foxitsearchobj.active = true;
                            thisdocument.foxitsearchobj.start();
                            if (thisdocument.foxitsearchobj.page != thisdocument.currentpage) {
                                thisdocument.GotoPage(thisdocument.foxitsearchobj.page);
                                thisdocument.pageSearchArr.push(thisdocument.foxitsearchobj.page);
                            }
                            else {
                                thisdocument.pages[thisdocument.currentpage].showFoxitSearch();
                                //thisdocument.pages[thisdocument.currentpage].showFoxitSearchOnUpdate();
                            }
                            //make new showSearch method.
                        }
                        /*if(thisdocument.foxitsearchobj.forward){
        
                        }*/
                        //thisdocument.pages[thisdocument.currentpage].drawsearchRects(thisdocument.foxitsearchobj.matcharray);
                    }
                    /*while(searchres.hasMore && pgindex < thisdocument.pages.length){
                        
                        pgindex++;
                    }*/
                };
                this.getpageloadSearch = function (pagenum) {
                    for (var pgi = 0; pgi < thisdocument.pageSearchArr.length; pgi++) {
                        if (thisdocument.pageSearchArr[pgi] == pagenum) {
                            thisdocument.pages[pagenum].showFoxitSearch();
                            //thisdocument.pages[pagenum].showFoxitSearchOnUpdate();
                            thisdocument.pageSearchArr.splice(pgi, 1);
                        }
                    }
                };
                this.foxitSearchTextEnd = function () {
                    if (thisdocument.foxitsearchobj.active) {
                        thisdocument.pages[thisdocument.currentpage].foxitClearSearch();
                    }
                    thisdocument.foxitsearchobj.matcharray = [];
                    thisdocument.foxitsearchobj.forward = true;
                    thisdocument.foxitsearchobj.expr = "";
                    thisdocument.foxitsearchobj.casesens = false;
                    thisdocument.foxitsearchobj.active = false;
                    thisdocument.foxitsearchobj.outerindx = 0;
                    thisdocument.foxitsearchobj.innerindx = 0;
                    thisdocument.foxitsearchobj.page = 0;
                    if (RxCore_GUI_NumMathces != undefined) {
                        RxCore_GUI_NumMathces.numTextFound(thisdocument.searchobj.matcharray.length);
                    }
                };
                this.forcefoxitCanvas = function () {
                    if (thisdocument.foxitdoc != null) {
                        thisdocument.foxitdoc.forceCanvas();
                    }
                };

                this.postscaleRefresh = function(){

                    window.setTimeout(function () {
                        // run your actual function here
                        thisdocument.forcefoxitRedraw();
                    }, 500);


                };


                this.forcefoxitRedraw = function(){
                    if (thisdocument.foxitdoc != null) {
                        thisdocument.foxitdoc.forceRedraw();
                    }
                };

                this.markupSearchComplete = function(){
                    if (RxCore_GUI_NumMathcesMarkup != undefined) {
                        RxCore_GUI_NumMathcesMarkup.numTextFound(thisdocument.markupsearchobj.matcharray.length);
                    }

                    if (thisdocument.markupsearchobj.matcharray.length > 0) {
                        thisdocument.markupsearchobj.active = true;
                        thisdocument.markupsearchobj.start();
                    
                        if (thisdocument.markupsearchobj.page != thisdocument.currentpage) {
                            thisdocument.GotoPage(thisdocument.markupsearchobj.page);
                        }
                        //thisdocument.pages[thisdocument.currentpage].showSearch();
                        thisdocument.pages[thisdocument.currentpage].zoomToMarkup(thisdocument.markupsearchobj.matcharray[thisdocument.markupsearchobj.indx], thisdocument.markupsearchobj.padding);
                    }

                };

                this.searchComplete = function () {
                    if (RxCore_GUI_NumMathces != undefined) {
                        RxCore_GUI_NumMathces.numTextFound(thisdocument.searchobj.matcharray.length);
                    }
                    if (thisdocument.searchobj.matcharray.length > 0) {
                        thisdocument.searchobj.active = true;
                        thisdocument.searchobj.start();
                        
                        if (thisdocument.searchobj.page != thisdocument.currentpage) {
                            thisdocument.GotoPage(thisdocument.searchobj.page);
                        }

                        thisdocument.pages[thisdocument.currentpage].showSearch();
                    }
                };

                this.searchCompleteRect = function(){

                    if (thisdocument.extractsearchobj.matcharray.length > 0) {
                        thisdocument.extractsearchobj.active = true;
                        thisdocument.extractsearchobj.start();
                        /*if (thisdocument.extractsearchobj.page != thisdocument.currentpage) {
                            thisdocument.GotoPage(thisdocument.extractsearchobj.page);
                        }*/

                        thisdocument.pages[thisdocument.currentpage].getSearchTextRect();
                        thisdocument.extractsearchobj.next();
                        
                        while(thisdocument.extractsearchobj.indx != 0){

                            /*if (thisdocument.extractsearchobj.page != thisdocument.currentpage) {
                                thisdocument.GotoPage(thisdocument.extractsearchobj.page);
                            }*/

                            thisdocument.pages[thisdocument.currentpage].getSearchTextRect();
                            thisdocument.extractsearchobj.next();
                        }

                        //while(extractsearchobj.index <= )
                        //thisdocument.searchobj.next();

                        if (RxCore_GUI_NumMathcesRect != undefined) {
                            RxCore_GUI_NumMathcesRect.numTextFound(thisdocument.extractsearchobj, thisdocument.currentpage);
                        }

                        //console.log(thisdocument.extractsearchobj.matcharray);
                    }

                };

                this.foxitSearchTextRect = function (text, casesens) {

                    thisdocument.foxitsearchobjrect.matcharray = [];
                    thisdocument.foxitsearchobjrect.pagedims = [];
                    thisdocument.foxitsearchobjrect.forward = true;
                    thisdocument.foxitsearchobjrect.expr = text;
                    thisdocument.foxitsearchobjrect.casesens = casesens;
                    thisdocument.foxitsearchobjrect.active = false;
                    thisdocument.foxitsearchobjrect.outerindx = 0;
                    thisdocument.foxitsearchobjrect.innerindx = 0;
                    thisdocument.foxitsearchobjrect.page = 0;

                    if (thisdocument.foxitdoc != null) {
                        var pgindex = 0;
                        //thisdocument.foxitdoc.getTextSearch(text, pgindex, thisdocument.getsearchresultrect);
                        thisdocument.foxitdoc.getTextSearch(text, pgindex, thisdocument.getsearchresultrect, thisdocument.foxitsearchobjrect.casesens);
                    }

                };
                this.foxitSearchText = function (text, forward, casesens) {
                    if (text == thisdocument.foxitsearchobj.expr && thisdocument.foxitsearchobj.active) {
                        if (forward) {
                            thisdocument.pages[thisdocument.currentpage].foxitClearSearch();
                            thisdocument.foxitsearchobj.next();
                            if (thisdocument.foxitsearchobj.page != thisdocument.currentpage) {
                                thisdocument.GotoPage(thisdocument.foxitsearchobj.page);
                                thisdocument.pageSearchArr.push(thisdocument.foxitsearchobj.page);
                            }
                            else {
                                //thisdocument.pages[thisdocument.currentpage].clearSearch();
                                thisdocument.pages[thisdocument.currentpage].showFoxitSearch();
                                //thisdocument.pages[thisdocument.currentpage].showFoxitSearchOnUpdate();
                                
                            }
                        }
                        if (!forward) {
                            thisdocument.pages[thisdocument.currentpage].foxitClearSearch();
                            thisdocument.foxitsearchobj.previous();
                            if (thisdocument.foxitsearchobj.page != thisdocument.currentpage) {
                                thisdocument.GotoPage(thisdocument.foxitsearchobj.page);
                                thisdocument.pageSearchArr.push(thisdocument.foxitsearchobj.page);
                            }
                            else {
                                thisdocument.pages[thisdocument.currentpage].showFoxitSearch();
                                //thisdocument.pages[thisdocument.currentpage].showFoxitSearchOnUpdate();
                            }
                        }
                    }
                    else {
                        if (thisdocument.foxitsearchobj.active) {
                            thisdocument.pages[thisdocument.currentpage].foxitClearSearch();
                        }
                        thisdocument.foxitsearchobj.matcharray = [];
                        thisdocument.foxitsearchobj.forward = forward;
                        thisdocument.foxitsearchobj.expr = text;
                        thisdocument.foxitsearchobj.casesens = casesens;
                        thisdocument.foxitsearchobj.active = false;
                        thisdocument.foxitsearchobj.outerindx = 0;
                        thisdocument.foxitsearchobj.innerindx = 0;
                        thisdocument.foxitsearchobj.page = 0;
                        if (thisdocument.foxitdoc != null) {
                            var pgindex = 0;
                            //thisdocument.foxitdoc.getTextSearch(text, pgindex, thisdocument.getsearchresult);
                            thisdocument.foxitdoc.getTextSearch(text, pgindex, thisdocument.getsearchresult, thisdocument.foxitsearchobj.casesens);
                        }
                    }
                    //thisdocument.foxitSearcMatches = [];
                };


                this.getTextRects = function(text, casesens){

                    
                    if (thisdocument.foxitdoc != null) {
                        thisdocument.foxitSearchTextRect(text, casesens);
                        return;
                    }

                    
                    //var casesense = casesens;
                    //var str = text;

                    /*if(text = thisdocument.extractsearchobj.expr && thisdocument.extractsearchobj.active){

                    }else{
                        
                    }*/

                    if (thisdocument.extractsearchobj.active) {
                        //thisdocument.pages[thisdocument.currentpage].clearSearchRects();
                    }
                    thisdocument.extractsearchobj.matcharray = [];
                    thisdocument.extractsearchobj.foundrects = [];
                    thisdocument.extractsearchobj.expr = text;
                    thisdocument.extractsearchobj.casesens = casesens;
                    thisdocument.extractsearchobj.active = false;
                    thisdocument.extractsearchobj.indx = 0;
                    thisdocument.extractsearchobj.page = 0;

                    /*if (thisdocument.pages[thisdocument.currentpage].usepdfjs) {
                        if (!thisdocument.textselect) {
                            thisdocument.enableTextSelect(true);
                        }
                        thisdocument.DocumentTextArrayRect = [];

                        
                        for (var pagecnt = 0; pagecnt < thisdocument.pages.length; pagecnt++) {
                            
                            var notlastpage = (pagecnt < thisdocument.pages.length);

                            thisdocument.searchTextPagesRect(pagecnt);
                        }
                        //thisdocument.searchobj.matcharray
                    }*/
                    if (thisdocument.pages[thisdocument.currentpage].usevectorxml) {
                        if (!thisdocument.textselect) {
                            thisdocument.enableTextSelect(true);
                        }
                        for (var vpagecnt = 0; vpagecnt < thisdocument.pages.length; vpagecnt++) {
                            if (thisdocument.pages[vpagecnt] != undefined) {
                                thisdocument.pages[vpagecnt].getVectorSearchListRect(thisdocument.extractsearchobj);
                            }
                        }
                        thisdocument.searchCompleteRect();
                    }

                };

                this.clearDocumentTextSearch = function () {
                    if (thisdocument.foxitdoc != null) {
                        thisdocument.searchMatches = {};
                        thisdocument.foxitsearchobj.clonearray = [];
                        thisdocument.foxitsearchobj.forward = true;
                        thisdocument.foxitsearchobj.expr = "";
                        thisdocument.foxitsearchobj.casesens = false;
                        thisdocument.foxitsearchobj.active = false;
                        thisdocument.foxitsearchobj.outerindx = 0;
                        thisdocument.foxitsearchobj.innerindx = 0;
                        thisdocument.foxitsearchobj.page = 0;
                        thisdocument.foxitsearchobj.wholeword = "";
                        thisdocument.foxitsearchobj.wholedocument = false;
                    }
                };

                this.getSearchWholeDocument = function (searchres, text, pgindex) {
                    let list = searchres.list;
                    const isCaseSensitive = thisdocument.foxitsearchobj.casesens;
                    const isWholeText = thisdocument.foxitsearchobj.wholeword;

                    thisdocument.searchMatches[pgindex] = { text, pgindex, list: [] };

                    list.forEach((item) => {
                        const pageIndex = item.match.pageIndex;
                        
                        if (!thisdocument.searchMatches[pageIndex]) {
                            thisdocument.searchMatches[pageIndex] = {text, pgindex: pageIndex, list: []};
                        }
                        thisdocument.searchMatches[pageIndex].list.push(item);
                    });

                    if (pgindex < thisdocument.pages.length - 1) {
                        thisdocument.foxitdoc.getTextSearch( text, pgindex + 1, thisdocument.getSearchWholeDocument, isCaseSensitive, isWholeText );
                    } else {
                        const result = Object.values(thisdocument.searchMatches);
                        RxCore_GUI_DocumentSearch.matchesFound(result);
                        thisdocument.foxitsearchobj.active = true;
                        thisdocument.foxitsearchobj.wholedocument = true;
                        thisdocument.foxitsearchobj.matcharray = result;
                        thisdocument.foxitsearchobj.clonearray = result.map((item) => item.list);
                    }
                };

                this.documentTextSearch = function (text, casesens, wholeword) {
                    if (thisdocument.foxitdoc != null) {
                        if (thisdocument.foxitsearchobj.active) {
                            thisdocument.pages[thisdocument.currentpage].foxitClearSearch();
                        }

                        thisdocument.searchMatches = {};
                        thisdocument.foxitsearchobj.forward = true;
                        thisdocument.foxitsearchobj.expr = text;
                        thisdocument.foxitsearchobj.casesens = casesens;
                        thisdocument.foxitsearchobj.active = false;
                        thisdocument.foxitsearchobj.outerindx = 0;
                        thisdocument.foxitsearchobj.innerindx = 0;
                        thisdocument.foxitsearchobj.page = 0;
                        thisdocument.foxitsearchobj.wholeword = wholeword;
                        thisdocument.foxitsearchobj.wholedocument = true;
                        if (thisdocument.foxitdoc != null) {
                            var pgindex = 0;
                            thisdocument.foxitdoc.getTextSearch(text, pgindex, thisdocument.getSearchWholeDocument, casesens, wholeword);
                        }
                    }
                };                

                this.markupTextWithOrange = function (item) {
                    if (thisdocument.foxitdoc != null) {
                        thisdocument.foxitsearchobj.orangeItem = item;
                        const page = thisdocument.pages[thisdocument.currentpage];
                        var x = item.match.rectArray[0].left * (page.curpagescale * page.dscalepdf) + page.dxpdf;
                        var y = (page.pdfpageheight - item.match.rectArray[0].top) * (page.curpagescale * page.dscalepdf) + page.dypdf;
                        var panto = page.getsearchpanupdate(x, y);
                        page.pan_update(panto.x, panto.y);
                    }
                };                

                this.movePageTo = function (pageRange, destIndex) {
                    if (thisdocument.foxitdoc != null) {
                        thisdocument.foxitdoc.movePageTo(pageRange, destIndex).then((pages) => {

                            const array = this.findChangedIndices(pageRange, destIndex);
                            const pagexmlobj = xmlDoc.getElementsByTagName("Page");
                            
                            array.forEach((id) => {
                                if (id < thisdocument.thumbnails.length) {
                                    const thumbnail = new ThumbnailObject( pagexmlobj[0], "Default", thisdocument, id );
                                    thisdocument.thumbnails[id] = thumbnail;
                                    thisdocument.foxitdoc.getThumbnail(id);
                                }
                            });

                            RxCore_GUI_pagethumbs.setThumbnails(thisdocument.thumbnails);

                            if (thisdocument.foxitsearchobj.clonearray != []) {
                                thisdocument.foxitsearchobj.clonearray = [];
                                this.documentTextSearch( thisdocument.foxitsearchobj.expr, thisdocument.foxitsearchobj.casesens, thisdocument.foxitsearchobj.wholeword);
                            }
                        });
                    }
                };

                this.removePage = function (pageRange) {
                    if (thisdocument.foxitdoc != null) {
                    
                        thisdocument.foxitdoc.removePage(pageRange).then(() => {
                        
                            const count = this.countItemsInRanges(pageRange);
                        
                            thisdocument.pages.splice(thisdocument.pages.length - count, count );
                            thisdocument.thumbnails = this.removeItemsByIndices( thisdocument.thumbnails, pageRange );
                        
                            RxCore_GUI_pagethumbs.setThumbnails(thisdocument.thumbnails);

                            RxCore_GUI_PageCountChanged.countChanged( thisdocument.pages.length );

                            if (thisdocument.foxitsearchobj.clonearray != []) {
                                thisdocument.foxitsearchobj.matcharray = this.removeItemsByIndices( thisdocument.foxitsearchobj.matcharray, pageRange );
                                this.refreshSearchResult();
                            }
                        });
                    }
                };

                this.extractPage = function (pageRange, remove) {
                    if (thisdocument.foxitdoc != null) {
                        thisdocument.foxitdoc.extractPage(pageRange).then(() => {
                            if (remove) {
                                this.removePage(pageRange);
                            }
                        });
                    }
                };

                this.refreshPages = function () {
                    if (thisdocument.foxitdoc != null) {
                        thisdocument.pages = thisdocument.pages.map((page, id) => {
                            if (page != undefined) {
                                return { ...page, PageName: `Page ${id + 1}`, firstpage: id === 0, pagenumber: id, printobj: { ...page.printobj, pagenumber: id} };
                            } else {
                                return undefined;
                            }
                        });
                    }
                };

                this.refreshSearchResult = function () {
                    thisdocument.foxitsearchobj.matcharray = thisdocument.foxitsearchobj.matcharray.map((item, id) => {
                        let list = [];
                        if (item && item.list) {
                            list = item.list.map((item) => ({ match: { ...item.match, pageIndex: id}}));
                        }
                        return {
                            text: thisdocument.foxitsearchobj.expr,
                            pgindex: id,
                            list
                        };
                    });
                    thisdocument.foxitsearchobj.clonearray = thisdocument.foxitsearchobj.matcharray.map((item) => item.list);
                    RxCore_GUI_DocumentSearch.matchesFound( thisdocument.foxitsearchobj.matcharray );

                };

                this.insertBlankPages = function (pageRange, count, width, height) {
                    if (thisdocument.foxitdoc != null) {
                        thisdocument.foxitdoc.insertBlankPages(pageRange, count, width, height).then(() => {
                            var pagexmlobj = xmlDoc.getElementsByTagName("Page");
                            const flattenRanges = this.flattenRanges(pageRange);
                            const rangeCount = this.countItemsInRanges(pageRange);
                            new Array(rangeCount).fill(0).forEach((item, id) => {
                            
                                new Array(count).fill(0).forEach(() => {
                                    console.log(flattenRanges[id]);
                                    const PageObj = new PageObject( pagexmlobj[0], "Default", false, "", flattenRanges[id], thisdocument );
                                    DocObj.addpage(PageObj, false);
                                });
                            });
                        
                            this.refreshThumbnails();

                            this.refreshPages();

                            thisdocument.pages.forEach((item, id) => {
                                thisdocument.foxitdoc.getThumbnail(id);
                            });
                            if (thisdocument.foxitsearchobj.clonearray != []) {
                                thisdocument.foxitsearchobj.clonearray = [];
                                this.documentTextSearch( thisdocument.foxitsearchobj.expr, thisdocument.foxitsearchobj.casesens, thisdocument.foxitsearchobj.wholeword );
                            }
                        });
                    }
                };

                this.importPages = function ( file, pageRange, pageArray, isReplace, count, width, height ) {
                    if (thisdocument.foxitdoc != null) {

                        return thisdocument.foxitdoc.importPages( file, pageRange, pageArray, isReplace, count, width, height ).then(() => {

                            var pagexmlobj = xmlDoc.getElementsByTagName("Page");
                            
                            if (isReplace) {
                                const deleteCount = this.countItemsInRanges(pageRange);
                                thisdocument.pages.splice( thisdocument.pages.length - deleteCount, deleteCount );
                            }
                        
                            new Array(count).fill(0).forEach((item, id) => {
                                const PageObj = new PageObject( pagexmlobj[0], "Default", false, "", pageRange[0][0] + id, thisdocument );
                                DocObj.addpage(PageObj, false);
                            });
                        
                            this.refreshThumbnails();
                            this.refreshPages();
                            thisdocument.pages.forEach((item, id) => {
                                thisdocument.foxitdoc.getThumbnail(id);
                            });

                            if (thisdocument.foxitsearchobj.clonearray != []) {
                                thisdocument.foxitsearchobj.clonearray = [];
                                this.documentTextSearch(thisdocument.foxitsearchobj.expr, thisdocument.foxitsearchobj.casesens, thisdocument.foxitsearchobj.wholeword );
                            }

                            RxCore_GUI_PageCountChanged.countChanged( thisdocument.pages.length );
                            return Promise.resolve();
                        });
                    } else return Promise.reject();
                };

                this.copyPage = function (pageRange) {
                    if (thisdocument.foxitdoc != null) {
                        thisdocument.foxitdoc.copyPage(pageRange).then((blob) => {
                            copiedBlob = blob;
                        });
                    }
                };

                this.getAllThumbnailsFromFile = function (file) {
                    if (thisdocument.foxitdoc != null) {
                        return thisdocument.foxitdoc.getAllThumbnailsFromFile(file);
                    }
                };

                this.uploadPDFCustom = function(endpoint){

                    if (thisdocument.foxitdoc != null) {
                        thisdocument.foxitdoc.exportPDFupload(true, endpoint);
                    }

                }


                this.uploadPDF = function(){

                    if (thisdocument.foxitdoc != null) {
                        thisdocument.foxitdoc.exportPDFupload(false, undefined);
                    }

                };

                this.exportPDF = function () {
                    if (thisdocument.foxitdoc != null) {
                        thisdocument.foxitdoc.exportPDF1();
                    }
                };

                this.pastePage = function (pasteId) {
                    if (thisdocument.foxitdoc != null && copiedBlob != null) {
                        
                        thisdocument.foxitdoc.pastePage(copiedPageIndex, pasteId, copiedBlob).then(() => {

                            var pagexmlobj = xmlDoc.getElementsByTagName("Page");
                            const count = this.countItemsInRanges(copiedPageIndex);
                            new Array(count).fill(0).forEach((item, id) => {
                                const PageObj = new PageObject( pagexmlobj[0], "Default", false, "", pasteId + id, thisdocument);
                                thisdocument.addpage(PageObj, false);
                                thisdocument.thumbnails.splice( pasteId, 0, thisdocument.thumbnails[0] );
                            });

                            this.refreshThumbnails();

                            this.refreshPages();

                            thisdocument.pages.forEach((item, id) => {
                                thisdocument.foxitdoc.getThumbnail(id);
                            });
                        
                            RxCore_GUI_PageCountChanged.countChanged( thisdocument.pages.length );
                        
                            if (thisdocument.foxitsearchobj.clonearray != []) {
                                thisdocument.foxitsearchobj.clonearray = [];
                                this.documentTextSearch( thisdocument.foxitsearchobj.expr, thisdocument.foxitsearchobj.casesens, thisdocument.foxitsearchobj.wholeword );
                            }
                            copiedBlob = null;
                        });
                    }
                };

                function rangesToSet(indicesRanges) {
                    const indicesToRemove = new Set();
                    indicesRanges.forEach((range) => {
                        if (range.length === 1) {
                            indicesToRemove.add(range[0]);
                        } else {
                            for (let i = range[0]; i <= range[1]; i++) {
                                indicesToRemove.add(i);
                            }
                        }
                    });
                    return indicesToRemove;
                }

                this.removeItemsByIndices = function (array, indicesRanges) {
                    const indicesToRemove = rangesToSet(indicesRanges);
                    return array.filter((item, index) => !indicesToRemove.has(index));
                };

                this.itemsInRange = function (array, indicesRanges) {
                    const indicesToRemove = rangesToSet(indicesRanges);
                    return array.filter((item, index) => indicesToRemove.has(index));
                };

                this.countItemsInRanges = function (indicesRanges) {
                    return indicesRanges.reduce((count, range) => {
                        return count + (range.length === 1 ? 1 : range[1] - range[0] + 1);
                    }, 0);
                };

                this.flattenRanges = function (ranges) {
                    let flatArray = [];
                    ranges.forEach((range) => {
                        if (range.length === 2) {
                            for (let i = range[0]; i <= range[1]; i++) {
                                flatArray.push(i);
                            }
                        } else {
                            flatArray.push(range[0]);
                        }
                    });
                    return flatArray;
                };

                function rangesToSet(indicesRanges) {
                    const indicesToRemove = new Set();
                    indicesRanges.forEach((range) => {
                        if (range.length === 1) {
                            indicesToRemove.add(range[0]);
                        } else {

                            for (let i = range[0]; i <= range[1]; i++) {
                                indicesToRemove.add(i);
                            }
                        }
                    });
                    return indicesToRemove;
                }

                this.findChangedIndices = function (subarrays, newPosition) {
                    let affectedIndices = [];

                    const count = this.countItemsInRanges(subarrays);
                    if (subarrays[0][0] <= newPosition) {
                        for (let i = subarrays[0][0]; i < newPosition + count; i++) {
                            affectedIndices.push(i);
                        }
                    } else {
                        for (let i = newPosition; i < subarrays[0][0] + count; i++) {
                            affectedIndices.push(i);
                        }
                    }

                    return affectedIndices;
                };                


                this.textsearch = function (text, forward, casesens) {
                    if (thisdocument.foxitdoc != null) {
                        thisdocument.foxitSearchText(text, forward, casesens);
                        return;
                    }
                    if (text == thisdocument.searchobj.expr && thisdocument.searchobj.active) {
                        if (forward) {
                            thisdocument.pages[thisdocument.currentpage].clearSearch();
                            thisdocument.searchobj.next();
                            if (thisdocument.searchobj.page != thisdocument.currentpage) {
                                thisdocument.GotoPage(thisdocument.searchobj.page);
                            }
                            //thisdocument.pages[thisdocument.currentpage].clearSearch();
                            thisdocument.pages[thisdocument.currentpage].showSearch();
                        }
                        if (!forward) {
                            thisdocument.pages[thisdocument.currentpage].clearSearch();
                            thisdocument.searchobj.previous();
                            if (thisdocument.searchobj.page != thisdocument.currentpage) {
                                thisdocument.GotoPage(thisdocument.searchobj.page);
                            }
                            thisdocument.pages[thisdocument.currentpage].showSearch();
                        }
                    }
                    else {
                        if (thisdocument.searchobj.active) {
                            thisdocument.pages[thisdocument.currentpage].clearSearch();
                        }
                        thisdocument.searchobj.matcharray = [];
                        thisdocument.searchobj.expr = text;
                        thisdocument.searchobj.forward = forward;
                        thisdocument.searchobj.casesens = casesens;
                        thisdocument.searchobj.active = false;
                        thisdocument.searchobj.indx = 0;
                        thisdocument.searchobj.page = 0;
                        if (thisdocument.pages[thisdocument.currentpage].usepdfjs) {
                            
                            if (!thisdocument.textselect) {
                                thisdocument.enableTextSelect(true);
                            }
                            for (var pagecnt = 0; pagecnt < thisdocument.pages.length; pagecnt++) {
                                thisdocument.pages[pagecnt].getPDFSearchList();
                            }
                            if (RxCore_GUI_NumMathces != undefined) {
                                RxCore_GUI_NumMathces.numTextFound(thisdocument.searchobj.matcharray.length);
                            }
                            //thisdocument.searchobj.matcharray
                        }
                        if (thisdocument.pages[thisdocument.currentpage].usevectorxml) {
                            if (!thisdocument.textselect) {
                                thisdocument.enableTextSelect(true);
                            }
                            for (var vpagecnt = 0; vpagecnt < thisdocument.pages.length; vpagecnt++) {
                                if (thisdocument.pages[vpagecnt] != undefined) {
                                    thisdocument.pages[vpagecnt].getVectorSearchList();
                                }
                            }

                            thisdocument.searchComplete();


                            /*if (RxCore_GUI_NumMathces != undefined) {
                                RxCore_GUI_NumMathces.numTextFound(thisdocument.searchobj.matcharray.length);
                            }*/
                        }



                        /*if (thisdocument.searchobj.matcharray.length > 0) {
                            thisdocument.searchobj.active = true;
                            thisdocument.searchobj.start();
                            if (thisdocument.searchobj.page != thisdocument.currentpage) {
                                thisdocument.GotoPage(thisdocument.searchobj.page);
                            }
                            thisdocument.pages[thisdocument.currentpage].showSearch();
                        }*/
                    }
                };

                this.ZoomToMarkup = function(markupobj) {


                    if(markupcurrentview(markupobj)){
                        thisdocument.pages[thisdocument.currentpage].zoomToMarkup(markupobj);
                    }else{
                        thisdocument.GotoPage(markupobj.pagenumber);

                        thisdocument.pages[thisdocument.currentpage].zoomToMarkup(markupobj);
                    }

                    
                };


                this.panToMarkup = function(markupobj){


                    if(markupcurrentview(markupobj)){
                        thisdocument.pages[thisdocument.currentpage].panToMarkup(markupobj);
                    }else{
                        thisdocument.GotoPage(markupobj.pagenumber);

                        thisdocument.pages[thisdocument.currentpage].panToMarkup(markupobj);
                    }


                    

                };

                this.markupTextSearch = function(text, forward, casesens, padding){


                    if (text == thisdocument.markupsearchobj.expr && thisdocument.markupsearchobj.active) {
                        if (forward) {
                            
                            thisdocument.markupsearchobj.next();
                            if (thisdocument.markupsearchobj.page != thisdocument.currentpage) {
                                
                                thisdocument.GotoPage(thisdocument.markupsearchobj.page);
                            }
                            //thisdocument.pages[thisdocument.currentpage].clearSearch();
                            //thisdocument.pages[thisdocument.currentpage].showSearch();
                            thisdocument.pages[thisdocument.currentpage].zoomToMarkup(thisdocument.markupsearchobj.matcharray[thisdocument.markupsearchobj.indx], thisdocument.markupsearchobj.padding);
                        }
                        if (!forward) {
                            //thisdocument.pages[thisdocument.currentpage].clearSearch();

                            thisdocument.markupsearchobj.previous();
                            if (thisdocument.markupsearchobj.page != thisdocument.currentpage) {
                                thisdocument.GotoPage(thisdocument.markupsearchobj.page);
                            }
                            thisdocument.pages[thisdocument.currentpage].zoomToMarkup(thisdocument.markupsearchobj.matcharray[thisdocument.markupsearchobj.indx], thisdocument.markupsearchobj.padding);
                            //thisdocument.pages[thisdocument.currentpage].showSearch();
                            //gotomarkupobject
                        }
                    }
                    else {
                        thisdocument.markupsearchobj.matcharray = [];
                        thisdocument.markupsearchobj.expr = text;
                        thisdocument.markupsearchobj.forward = forward;
                        thisdocument.markupsearchobj.casesens = casesens;
                        thisdocument.markupsearchobj.active = false;
                        thisdocument.markupsearchobj.indx = 0;
                        thisdocument.markupsearchobj.page = 0;
                        if(padding){
                            thisdocument.markupsearchobj.padding = padding;
                        }
                        


                        if (thisdocument.markupsearchobj.expr != ""){
                            for(var mi = 0; mi < thisdocument.markuplist.length; mi++){
                                if (thisdocument.markuplist[mi].FindText(thisdocument.markupsearchobj.expr, thisdocument.markupsearchobj.casesens)) {
                                    thisdocument.markupsearchobj.matcharray.push(thisdocument.markuplist[mi]);
                                }
                            }

                            thisdocument.markupSearchComplete();
                        }

                        
    
                    }
                    
                };
                

                this.getcurpageScale = function (indx) {
                    var curpage = thisdocument.currentpage;
                    if (thisdocument.usepagesload) {
                        //curpage = thisdocument.pages[indx].pagenumber;
                        curpage = thisdocument.pages[thisdocument.pages[indx].pagenumber];
                    }
                    else {
                        curpage = thisdocument.pages[indx];
                    }
                    return curpage.curpagescale;
                };
                this.getcur3DView = function(){
                    var cur3dview = null;

                    if(thisdocument.pages[thisdocument.currentpage] != undefined){
                        cur3dview = thisdocument.pages[thisdocument.currentpage].getcur3DView();
                    }

                    return cur3dview;

                };
                this.getcurPage = function () {
                    var curpage = thisdocument.currentpage;
                    if (thisdocument.usepagesload) {
                        curpage = thisdocument.pages[thisdocument.currentpage].pagenumber;
                    }
                    else {
                        curpage = thisdocument.currentpage;
                    }
                    return curpage;
                };
                this.setDocumentPageScale = function (width, height, scale) {
                    //this.PDFpagescale = {width : 1, height: 1, pdfscale : 1};
                    thisdocument.PDFpagescale.width = width;
                    thisdocument.PDFpagescale.height = height;
                    thisdocument.PDFpagescale.scale = scale;
                    //thispage.offscreenwidth, thispage.offscreenheight, thispage.dscalepdf
                };
                this.getAllPageDim = function () {
                    var pagedims = [];
                    for (var i = 0; i < thisdocument.pages.length; i++) {
                        if (thisdocument.pages[i] != undefined) {
                            pagedims.push(thisdocument.pages[i].getpagedim());
                        }
                    }
                    return pagedims;
                };
                this.getPageDim = function (pagenumber) {
                    var pgobj = thisdocument.getPageObject(pagenumber);
                    if (pgobj != undefined) {
                        var pgdim = pgobj.getpagedim();
                    }
                    else {
                        pgdim = undefined;
                    }
                    return pgdim;
                };
                this.setPageDim = function (pagenumber, height, width, x, y) {
                    var pgobj = thisdocument.getPageObject(pagenumber);
                    if (pgobj != undefined) {
                        var pgdim = pgobj.setpagedim(height, width, x, y);
                    }
                };
                this.getFirstLoadedPage = function () {
                    var curpageobj = undefined;
                    curpageobj = thisdocument.pages[0];
                    return curpageobj;
                };
                this.getPageObjectLoad = function(pagenumber){
                    var curpageobj = undefined;
                    if (!thisdocument.usepagesload) {
                        curpageobj = thisdocument.pages[pagenumber];
                    }
                    else {
                        for (var i = 0; i < thisdocument.pages.length; i++) {
                            if (thisdocument.pages[i] != undefined) {
                                if (thisdocument.pages[i].pagenumber == pagenumber) {
                                    curpageobj = thisdocument.pages[i];
                                }
                            }
                        }
                    }

                    if(curpageobj == undefined && thisdocument.tempPage != undefined){
                        thisdocument.tempPage.movepage(pagenumber);
                        curpageobj = thisdocument.tempPage;

                    }

                    return curpageobj;
                };
                this.getPageObject = function (pagenumber) {
                    var curpageobj = undefined;

                    if (!thisdocument.usepagesload) {
                        curpageobj = thisdocument.pages[pagenumber];
                    }

                    else {
                        for (var i = 0; i < thisdocument.pages.length; i++) {

                            if (thisdocument.pages[i] != undefined) {
                                if (thisdocument.pages[i].pagenumber == pagenumber) {
                                    curpageobj = thisdocument.pages[i];
                                }
                            }

                        }
                    }
                    return curpageobj;
                };
                this.addProperties = function (properties) {
                    var format = thisdocument.Format.substring(0, 9);
                    if (format == 'Adobe PDF') {
                        for (var k in properties.info) {
                            if (typeof properties.info[k] !== 'function') {
                                thisdocument.documentproperties.push({ name: k, value: properties.info[k] });
                            }
                        }
                    }
                };
                this.inpagerange = function (pagenum) {
                    var pagerange = thisdocument.pagerange();
                    return (pagenum > pagerange.start && pagenum < pagerange.end);
                };
                /*function shortpgupdateloop(){
        
                    var pagesstate = {
                        pagenum : thisdocument.currentpage,
                        pagerange : thisdocument.pagerange(),
                        bpageshort : true,
                        pagearray : thisdocument.pagerangearray()
                    };
        
                    yieldingLoop(pagesstate, 10, function() {
                        pagesstate = pagechangeloop(pagesstate);
        
                    }, function() {
                        pageupdatecomplete();
                    });
        
                }*/
                this.loadPageEvt = function (pagenumber) {
                    if (thisdocument.pages[thisdocument.currentpage] != undefined) {
                        var currentPage = thisdocument.pages[thisdocument.currentpage].pagenumber;
                    }
                    if (pagenumber === currentPage) {
                        //test to remove unnecessary rendering
                        //thisdocument.getPageObject(pagenumber).redraw();
                        //thisdocument.pgChangeLoop();
                    }
                };
                this.pgChangeLoop = function () {
                    var pagesstate = {
                        pagenum: 0,
                        pagerange: thisdocument.pagerange(),
                        bpageshort: false,
                    };
                    /*var list = [];
        
                    for (var i = pagesstate.pagerange.start; i <= pagesstate.pagerange.end; i++) {
                        list.push(i);
                    }
                    var newList = [...new Set([...list, ...thisdocument.isNotCollapsed])];
                  
                    for(var e=0;e<newList.length;e++){
                        pagesstate.pagenum = newList[e];
                        pagechangeloop(pagesstate);
                    }*/
                    var list = thisdocument.isNotCollapsed || [];
                    for (var i = pagesstate.pagerange.start; i <= pagesstate.pagerange.end; i++) {
                        if (list.indexOf(i) == -1) {
                            list.push(i);
                        }
                    }
                    for (var e = 0; e < list.length; e++) {
                        pagesstate.pagenum = list[e];
                        //pagechangeloop(pagesstate);
                    }
                    /*newList.forEach(function(e){
                        pagesstate.pagenum = e;
                        pagechangeloop(pagesstate);
                    });*/
                    /*newList.forEach(e => {
                        pagesstate.pagenum = e
                        pagechangeloop(pagesstate);
                    });*/
                };
                function pgupdateloop() {
                    //var parr  = pagechangeloopshort();
                    var pagesstate = {
                        pagenum: 0,
                        pagerange: thisdocument.pagerange(),
                        bpageshort: false,
                    };
                    //added code for quick update of current page
                    if (thisdocument.pages[thisdocument.currentpage]) {
                        if (!thisdocument.pages[thisdocument.currentpage].pdfisfirstrendered) {
                            //thisdocument.pages[i].queueRenderPage(i + 1);
                            //thisdocument.pages[thisdocument.currentpage].queueRenderPage(thisdocument.currentpage + 1);
                            //thisdocument.pages[thisdocument.currentpage].foxitRenderPageScaled();
                            //thisdocument.pages[thisdocument.currentpage].queRenderPageScaled();
                            //thisdocument.pages[pagestate.pagenum].queRenderPageScaled();
                        }
                        else {
                            if (thisdocument.pages[thisdocument.currentpage].iscollapsed) {
                                thisdocument.pages[thisdocument.currentpage].pdfisrendered = false;
                                //console.log('pgupdateloop invalidation');
                                if (thisdocument.foxitdoc != null) {
                                    thisdocument.foxitdoc.restorePage(thisdocument.currentpage);
                                }
                                //thisdocument.pages[pagestate.pagenum].queRenderPageScaled();
                            }
                        }
                    }
                    /*yieldingLoop(pagesstate, 10, function() {
                        pagesstate = pagechangeloop(pagesstate);
        
                    }, function() {
                        pageupdatecomplete();
                    });*/
                }
                function pageupdatecomplete() {
                    //console.log('page range update complete');
                    thisdocument.draw_mpagepdf(false);
                }
                this.pagerangearray = function () {
                    var curpage = this.currentpage;
                    var halfrange = thisdocument.largePDFPagerange / 2;
                    var endtrigger = thisdocument.endpagerange - thisdocument.pagerangelimit;
                    var starttrigger = thisdocument.startpagerange + thisdocument.pagerangelimit;
                    if (thisdocument.bLargePDF) {
                        if (curpage <= halfrange) {
                            thisdocument.startpagerange = 0;
                            thisdocument.endpagerange = thisdocument.largePDFPagerange;
                        }
                        else if (curpage >= (thisdocument.pages.length - halfrange)) {
                            thisdocument.startpagerange = thisdocument.pages.length - thisdocument.largePDFPagerange;
                            thisdocument.endpagerange = thisdocument.pages.length;
                        }
                        else if (curpage == endtrigger) {
                            thisdocument.startpagerange = endtrigger - halfrange;
                            thisdocument.endpagerange = endtrigger + halfrange;
                        }
                        else if (curpage == starttrigger) {
                            thisdocument.startpagerange = starttrigger - halfrange;
                            thisdocument.endpagerange = starttrigger + halfrange;
                        }
                        else if (curpage > thisdocument.endpagerange) {
                            thisdocument.startpagerange = curpage - halfrange;
                            thisdocument.endpagerange = curpage + halfrange;
                        }
                        else if (curpage < thisdocument.startpagerange) {
                            thisdocument.startpagerange = curpage - halfrange;
                            thisdocument.endpagerange = curpage + halfrange;
                        }
                    }
                    else {
                        thisdocument.startpagerange = 0;
                        thisdocument.endpagerange = thisdocument.pages.length;
                    }
                    var pagearray = [thisdocument.currentpage];
                    for (var pc = 1; pc < halfrange; pc++) {
                        pagearray.push(thisdocument.currentpage + pc);
                        if ((thisdocument.currentpage - pc) >= 0) {
                            pagearray.push(thisdocument.currentpage - pc);
                        }
                    }
                    return pagearray;
                };
                this.pagerange = function () {
                    var curpage = this.currentpage;
                    //this.largePDFPagerange = 30;
                    //this.endpagerange = 30;
                    //this.startpagerange = 0;
                    //this.pagerangelimit = 2;
                    var halfrange = thisdocument.largePDFPagerange / 2;
                    var endtrigger = thisdocument.endpagerange - thisdocument.pagerangelimit;
                    var starttrigger = thisdocument.startpagerange + thisdocument.pagerangelimit;
                    if (thisdocument.bLargePDF) {
                        if (curpage <= halfrange) {
                            thisdocument.startpagerange = 0;
                            thisdocument.endpagerange = thisdocument.largePDFPagerange;
                        }
                        else if (curpage >= (thisdocument.pages.length - halfrange)) {
                            thisdocument.startpagerange = thisdocument.pages.length - thisdocument.largePDFPagerange;
                            thisdocument.endpagerange = thisdocument.pages.length;
                        }
                        else if (curpage == endtrigger) {
                            thisdocument.startpagerange = endtrigger - halfrange;
                            thisdocument.endpagerange = endtrigger + halfrange;
                        }
                        else if (curpage == starttrigger) {
                            thisdocument.startpagerange = starttrigger - halfrange;
                            thisdocument.endpagerange = starttrigger + halfrange;
                        }
                        else if (curpage > thisdocument.endpagerange) {
                            thisdocument.startpagerange = curpage - halfrange;
                            thisdocument.endpagerange = curpage + halfrange;
                        }
                        else if (curpage < thisdocument.startpagerange) {
                            thisdocument.startpagerange = curpage - halfrange;
                            thisdocument.endpagerange = curpage + halfrange;
                        }
                    }
                    else {
                        thisdocument.startpagerange = 0;
                        thisdocument.endpagerange = thisdocument.pages.length;
                    }
                    //console.log(thisdocument.startpagerange,'-' ,thisdocument.endpagerange);
                    return {
                        start: thisdocument.startpagerange,
                        end: thisdocument.endpagerange,
                        nextstart: thisdocument.endpagerange + 1,
                        nextend: thisdocument.endpagerange + thisdocument.largePDFPagerange + 1
                    };
                };
                this.pagelock = function (onOff) {
                    thisdocument.pagelocked = onOff;
                    thisdocument.lockedpage = thisdocument.currentpage;
                };
                this.mpzoom = function (dx, dy, dscale) {
                    if (thisdocument.pagelocked) {
                        return;
                    }
                    //not to be used with foxit
                    /*for (var i = 0; i < thisdocument.pages.length; i++) {
                        if (i != thisdocument.currentpage && thisdocument.pages[i] != undefined) {
                            thisdocument.pages[i].dypdf = dy;
                            thisdocument.pages[i].dxpdf = dx;

                            //dscalepdf modified
                            thisdocument.pages[i].dscalepdf = dscale;
                            thisdocument.pages[i].pdfisrendered = false;
                            //console.log('mpzoom invalidation');
                        }
                    }*/
                };
                this.zoom_update = function (sx, sy, sWide, sHi) {
                    if (thisdocument.pagelocked) {
                        return;
                    }

                    //not to be used with foxit
                    /*for (var i = 0; i < this.pages.length; i++) {
                        if (i != thisdocument.currentpage && thisdocument.pages[i] != undefined) {
                            thisdocument.pages[i].zoom_update(sx, sy, sWide, sHi);
                        }
                    }*/
                };
                this.zoomall = function () {
                    if (thisdocument.pagelocked) {
                        return;
                    }

                    //not to be used with foxit
                    /*for (var i = 0; i < thisdocument.pages.length; i++) {
                        if (i != thisdocument.currentpage && thisdocument.pages[i] != undefined) {
                            thisdocument.pages[i].zoomall();
                            //console.log('zoom all');
                        }
                        //console.log(i,this.pages[i].dscalepdf);
                    }*/
                };
                this.pan_position = function (sx, sy) {
                    if (thisdocument.pagelocked) {
                        return;
                    }
                    if (this.currentpage == 0) {
                        var startrange = 0;
                    }
                    else {
                        startrange = thisdocument.currentpage - 1;
                    }
                    if (thisdocument.currentpage == thisdocument.pages.length - 1) {
                        var endrange = thisdocument.pages.length - 1;
                    }
                    else {
                        endrange = this.currentpage + 1;
                    }

                    //not to be used with foxit
                    /*for (var i = startrange; i < endrange; i++) {
                        if (i != thisdocument.currentpage && thisdocument.pages[i] != undefined) {
                            thisdocument.pages[i].pan_position(sx, sy);
                            if (thisdocument.pages[i].usepdfjs) {
                                if (thisdocument.textselect) {
                                    thisdocument.pages[i].PDFTextArea.style.display = "none";
                                }
                            }
                        }
                    }*/
                };
                this.pan_update = function (sx, sy) {
                    //return;
                    
                    if (thisdocument.pagelocked) {
                        return;
                    }
                    //var pagerange = thisdocument.pagerange();
                    if (this.currentpage == 0) {
                        var startrange = 0;
                    }
                    else {
                        startrange = this.currentpage - 1;
                    }
                    if (this.currentpage == this.pages.length - 1) {
                        var endrange = this.pages.length - 1;
                    }
                    else {
                        endrange = this.currentpage + 1;
                    }
                    //not to be used with foxit
                    /*for (var i = startrange; i < endrange; i++) {
                        if (i != thisdocument.currentpage && thisdocument.pages[i] != undefined) {
                            thisdocument.pages[i].pan_update(sx, sy);
                            if (thisdocument.pages[i].usepdfjs) {
                                if (thisdocument.textselect) {
                                    thisdocument.pages[i].PDFTextArea.style.display = "none";
                                }
                            }
                        }
                    }*/
                    //for (var i = 0; i < this.pages.length; i++) {
                    /*for (var i = pagerange.start; i < pagerange.end; i++) {
                        if (i != this.currentpage) {
                            this.pages[i].pan_update(sx, sy);
                            if (this.pages[i].usepdfjs) {
                                if(this.textselect){
                                    this.pages[i].PDFTextArea.style.display = "none";
                                }
        
                            }
        
                        }
                    }*/
                };
                this.ZoomIn = function (factor, center) {

                    return;

                    if (thisdocument.pagelocked) {
                        return;
                    }

                    //not to be used with foxit
                    /*for (var i = 0; i < thisdocument.pages.length; i++) {
                        if (i != thisdocument.currentpage && thisdocument.pages[i] != undefined) {
                            thisdocument.pages[i].ZoomIn(factor, center, false);
                        }
                    }*/

                };
                this.zoomheight = function () {
                    if (thisdocument.pagelocked) {
                        return;
                    }

                    //not to be used with foxit
                    /*for (var i = 0; i < thisdocument.pages.length; i++) {
                        if (i != thisdocument.currentpage && thisdocument.pages[i] != undefined) {
                            thisdocument.pages[i].zoomheight();
                        }
                    }*/
                };
                this.zoomwidth = function () {
                    if (thisdocument.pagelocked) {
                        return;
                    }

                    //not to be used with foxit
                    /*for (var i = 0; i < thisdocument.pages.length; i++) {
                        if (i != thisdocument.currentpage && thisdocument.pages[i] != undefined) {
                            thisdocument.pages[i].zoomwidth();
                        }
                    }*/
                };
                this.ZoomOut = function (factor, center) {
                    if (thisdocument.pagelocked) {
                        return;
                    }

                    //not to be used with foxit
                    /*for (var i = 0; i < thisdocument.pages.length; i++) {
                        if (i != thisdocument.currentpage && thisdocument.pages[i] != undefined) {
                            thisdocument.pages[i].ZoomOut(factor, center, false);
                        }
                    }*/

                };
                this.recalcPages = function (factor, zoomin, xdiff) {
                    if (thisdocument.pagelocked) {
                        return;
                    }
                    if (thisdocument.pages[thisdocument.currentpage] != undefined) {
                        var dscale = thisdocument.pages[thisdocument.currentpage].dscalepdf;
                        var dx = thisdocument.pages[thisdocument.currentpage].dxpdf;
                        var dy = thisdocument.pages[thisdocument.currentpage].dypdf;
                    }
                    for (var i = 0; i < this.pages.length; i++) {
                        if (i != thisdocument.currentpage && thisdocument.pages[i] != undefined) {
                            //this.pages[i].dscalepdf = (this.pages[this.currentpage].pagecanvas.width * dscale) / this.pages[i].pagecanvas.width;
                            if (zoomin) {

                                //dscalepdf modified
                                thisdocument.pages[i].dscalepdf = thisdocument.pages[i].dscalepdf * factor;
                            }
                            else {
                                //dscalepdf modified
                                thisdocument.pages[i].dscalepdf = thisdocument.pages[i].dscalepdf / factor;
                            }
                            thisdocument.pages[i].dxpdf = thisdocument.pages[i].dxpdf - xdiff;
                            thisdocument.pages[i].pdfisrendered = false;
                            //console.log('recalcPages invalidation');
                            //this.dscalepdf = this.dscalepdf / factor;
                            /*if(this.pages[i].pagecanvas.width * this.pages[i].dscalepdf != this.pages[this.currentpage].pagecanvas.width * dscale){
                             if(this.pages[i].pagecanvas.width != this.pages[this.currentpage].pagecanvas.width){
                             console.log(this.pages[i].dscalepdf);
                             }
                             }*/
                            //this.pages[i].dscalepdf = dscale;
                            //this.pages[i].dxpdf = dx;
                            //var wscale = (this.pages[this.currentpage].pagecanvas.width * this.pages[this.currentpage].dscalepdf) / Math.abs(page.view[2]);
                            //var hscale = (this.pages[this.currentpage].pagecanvas.height * this.pages[this.currentpage].dscalepdf) / Math.abs(page.view[3]);
                        }
                    }
                };
                //this.returbookmarknpage = function()
                this.turnPDFLayerOnOff = function (id, visible) {
                    if (thisdocument.foxitdoc != null) {
                        thisdocument.foxitdoc.setLayerState(id, visible);
                        //thisdocument.foxitdoc.forceRedraw();

                        thisdocument.postscaleRefresh();
                    }
                };
                this.get_bookmark_pageref = function (destRef) {
                    var pageidx = null;
                    var destrefpage = destRef[0];
                    for (var i = 0; i < thisdocument.pages.length; i++) {
                        if (thisdocument.pages[i] != undefined) {
                            var pageref = thisdocument.pages[i].getpageRef();
                            if (isObjectEqual(destrefpage, pageref)) {
                                pageidx = thisdocument.pages[i].pagenumber;
                            }
                        }
                        //thisdocument.pages[i].getPDF_PageRef(destRef)
                        //thisdocument.pages[i].queueGetPageDim(thisdocument.pages[i].pagenumber + 1);
                    }
                    /*var cacheKey = destRef.num + ' ' + destRef.gen + ' R';
                    thisdocument.pdfDoc.getPageIndex(cacheKey).then(function (pageIndex) {
                        return pageIndex;
                    });*/
                    return pageidx;
                };
                this.getPDF_PageRef = function (destRef, last) {
                    thisdocument.pdfDoc.getPageIndex(destRef).then(function (pageIndex) {
                        //var pageNum = pageIndex;
                        var cacheKey = destRef.num + ' ' + destRef.gen + ' R';
                        thisdocument._pagesRefCache[cacheKey] = pageIndex;
                        if (RxCore_GUI_PDFBookmarks != undefined) {
                            RxCore_GUI_PDFBookmarks.setPageRef(thisdocument._pagesRefCache);
                        }
                        //pdfoutline
                        if (last) {
                            if (RxCore_GUI_PDFBookmarks != undefined) {
                                RxCore_GUI_PDFBookmarks.setPDFBookmarks(thisdocument.pdfoutline);
                            }
                        }
                        //goToDestination(destRef);
                    });
                };
                this.getPDFDocument = function () {
                    return thisdocument.pdfDoc;
                };
                this.navigateBookmark = function (bookmark) {
                    if (bookmark.page != thisdocument.currentpage) {
                        thisdocument.GotoPage(bookmark.page);
                    }
                    if (thisdocument.pages[thisdocument.currentpage] != undefined) {
                        thisdocument.pages[thisdocument.currentpage].showBookmark(bookmark);
                    }
                };

                this.getPDFBookmarks = function(){
                    if(thisdocument.foxitdoc != null){

                        thisdocument.foxitdoc.getBookmarks();

                    }

                };
                this.setPDFBookmarks = function (PDFBookmarks) {
                    if (PDFBookmarks) {
                        if (PDFBookmarks.length > 0) {
                            if (RxCore_GUI_PDFBookmarks != undefined) {
                                RxCore_GUI_PDFBookmarks.setPDFBookmarks(PDFBookmarks);
                            }
                        }
                    }else{
                        if (RxCore_GUI_PDFBookmarks != undefined) {
                            RxCore_GUI_PDFBookmarks.setPDFBookmarks([]);
                        }

                    }
                };
                this.setPDFLayers = function (PDFLayers) {
                    if (PDFLayers) {
                        /*if (PDFLayers.length > 0) {
                        }*/
                        if (RxCore_GUI_VectorLayers != undefined) {
                            RxCore_GUI_VectorLayers.setVectorLayers(PDFLayers);
                        }

                    }
                };
                this.rxfoxitpagedim = function (pagenumber, imagedim) {
                    if (thisdocument.foxitdoc != null) {
                        thisdocument.foxitdoc.pagedimcompare(pagenumber, imagedim);
                    }
                };
                this.createTempcanvArray = function (foxitpageobj) {
                    if (thisdocument.fileindex == foxitpageobj.docindex) {
                        if (thisdocument.pages[foxitpageobj.pageindex]) {
                            thisdocument.pages[foxitpageobj.pageindex].createTempcanvArray(foxitpageobj);
                        }
                    }
                };
                this.foxitonScrollupdate = function (pagepos, pagenumber) {
                    if (thisdocument.pages[pagenumber]) {
                        thisdocument.pages[pagenumber].foxitonScrollupdate(pagepos);
                    }
                };

                this.foxitcalibratepagemarkupEx = function(pagepos, scale, pagenumber){

                    if (thisdocument.foxitdoc == null) {
                        return;
                    }

                    if (thisdocument.pages[pagenumber]) {
                        thisdocument.pages[pagenumber].foxitcalibratepagemarkupEx(pagepos, scale);
                    }
                };
                this.foxitcalibratepagemarkup = function (pagepos, pagenumber) {

                    if (thisdocument.foxitdoc == null) {
                        return;
                    }


                    if (thisdocument.pages[pagenumber]) {
                        thisdocument.pages[pagenumber].foxitcalibratepagemarkup(pagepos);
                    }
                };
                this.foxitcalibratepagePosX = function (pagenumber, bsetend) {

                    if (thisdocument.foxitdoc == null) {
                        return;
                    }


                    if (thisdocument.pages[pagenumber]) {
                        //thisdocument.pages[pagenumber].foxitcalibratepagePosX(bsetend);
                    }
                };
                this.foxitcalibratepagePosY = function (pagenumber) {

                    if (thisdocument.foxitdoc == null) {
                        return;
                    }

                    if (thisdocument.pages[pagenumber]) {
                        thisdocument.pages[pagenumber].foxitcalibratepagePosY();
                    }
                };
                this.setfoxitPageSize = function (foxitpageobj) {

                    if (thisdocument.foxitdoc == null) {
                        return;
                    }


                    if (thisdocument.fileindex == foxitpageobj.docindex) {
                        if (thisdocument.pages[foxitpageobj.pageindex]) {
                            thisdocument.pages[foxitpageobj.pageindex].setfoxitPageSize(foxitpageobj);
                            
                        }
                    }
                };

                this.setfoxitPageSizeEx = function(foxitpageobj){

                    if (thisdocument.foxitdoc == null) {
                        return;
                    }


                    if (thisdocument.fileindex == foxitpageobj.docindex) {
                        if (thisdocument.pages[foxitpageobj.pageindex]) {
                            thisdocument.pages[foxitpageobj.pageindex].setfoxitPageSizeEx(foxitpageobj);
                        }
                    }
                };
                this.createPageTileArray = function (foxitpageobj) {
                    if (thisdocument.fileindex == foxitpageobj.docindex) {
                        if (thisdocument.pages[foxitpageobj.pageindex]) {
                            thisdocument.pages[foxitpageobj.pageindex].createCanvasTiles(foxitpageobj);
                        }
                    }
                    /*var foxitpage = {
                        pageindex : pgindex,
                        canvases : [],
                        transScale : transcale,
                        indexarray : [],
                        pagescale : pgscale,
                        width : pagewidth,
                        height : pageheight
                    };*/
                };
                /*this.drawfoxitPages = function(cnv, transscale, pagescale, pagewidth, pageheight){
                    
                    if(cnv.length){
                        if (cnv.length > 0){
                            for (var cc = 0; cc < cnv.length; cc++){
                                if (cnv[cc].tagName.toLowerCase() == "canvas"){
                                    
                                    if (cc < thisdocument.pages.length){
                                        thisdocument.pages[cc].drawcanvasext(cnv[cc], transscale, pagescale, pagewidth, pageheight);
                                    }
        
                                    //xoffset += cnv[cc].width;
                                }
                            }
                        }
                    }
        
                };*/

                this.getpageinfodim = function(pagenum){
                    if(thisdocument.pages[pagenum] != undefined){
                        if (thisdocument.foxitdoc != null) {
                            thisdocument.foxitdoc.getpageinfodim(pagenum, thisdocument.pages[pagenum].setfoxitPageSizeNonRender);
                        }
                    }
            
                }

                this.setLargePDF = function (numpages) {
                    if (numpages > nLargePDF) {
                        thisdocument.bLargePDF = true;
                        for (var i = 0; i < thisdocument.pages.length; i++) {
                            if(thisdocument.pages[i] != undefined){
                                if (thisdocument.foxitdoc != null) {
                                    //need to check for possible NaNs
                                    thisdocument.foxitdoc.getpageinfodim(i, thisdocument.pages[i].setfoxitPageSizeNonRender);
                                }
                            }
                            //set page dim for all pages not undefined
                        }
                    }
                };
                this.loadPDFpages = function () {
                    if (thisdocument.pages.length > nLargePDF) {
                        //thisdocument.pages[0].queueRenderPage(thisdocument.pages[0].pagenumber + 1);
                        //thisdocument.pages[0].foxitRenderPageScaled();
                        if (bAbortPageload) {
                            console.log('page load terminated', i);
                            bAbortPageload = false;
                        }
                        thisdocument.bLargePDF = true;


                        for (var i = 0; i < thisdocument.pages.length; i++) {
                            //thisdocument.pages[i].queueGetPageDim(thisdocument.pages[i].pagenumber + 1);
                            if (bAbortPageload) {
                                console.log('page load terminated', i);
                                bAbortPageload = false;
                                break;
                            }
                        }
                    }
                    else {
                        //thisdocument.pages[0].foxitRenderPageScaled();
                        //thisdocument.pages[0].queueRenderPage(thisdocument.pages[0].pagenumber + 1);
                        if (bAbortPageload) {
                            console.log('page load terminated', i);
                            bAbortPageload = false;
                        }
                        for (var i = 0; i < thisdocument.pages.length; i++) {
                            //thisdocument.pages[i].queueGetPageDim(thisdocument.pages[i].pagenumber + 1);
                            if (bAbortPageload) {
                                console.log('page load terminated', i);
                                bAbortPageload = false;
                                break;
                            }
                        }
                    }
                    /*thisdocument.pdfDoc.getMetadata().then(function(properties){
                          //console.log(properties);
                          thisdocument.addProperties(properties);
                    });*/
                    /*thisdocument.pdfDoc.getOutline().then(function (outline) {
        
                        if (outline) {
                            thisdocument.pdfoutline = outline;
        
        
                            if (RxCore_GUI_PDFBookmarks != undefined) {
                                RxCore_GUI_PDFBookmarks.setPDFBookmarks(thisdocument.pdfoutline);
                            }
        
        
        
                        }
                    });*/
                    thisdocument.SetActive();
                };
                this.setPDFdocument = function (PDFDoc, pagesloaded) {
                    thisdocument.busesetpdfdoc = true;
                    thisdocument.pdfDoc = PDFDoc;
                    if (pagesloaded) {
                        thisdocument.loadPDFpages();
                    }
                    /*if(thisdocument.pages.length > nLargePDF){
                                        
                        thisdocument.pages[0].queueRenderPage(thisdocument.pages[0].pagenumber + 1);
                        if (bAbortPageload){
                            console.log('page load terminated', i);
                            bAbortPageload = false;
                        }
             
                        thisdocument.bLargePDF = true;
                        for ( var i = 0; i < thisdocument.pages.length; i++) {
        
                            thisdocument.pages[i].queueGetPageDim(thisdocument.pages[i].pagenumber + 1);
                            if (bAbortPageload){
                                console.log('page load terminated', i);
                                bAbortPageload = false;
                                break;
                            }
        
                        }
        
                    }else{
                        
                        thisdocument.pages[0].queueRenderPage(thisdocument.pages[0].pagenumber + 1);
                        if (bAbortPageload){
                             console.log('page load terminated', i);
                             bAbortPageload = false;
                        }
              
                        
                        for ( var i = 0; i < thisdocument.pages.length; i++) {
        
                            thisdocument.pages[i].queueGetPageDim(thisdocument.pages[i].pagenumber + 1);
                            if (bAbortPageload){
                                console.log('page load terminated', i);
                                bAbortPageload = false;
                                break;
                            }
                            
                        }
        
                        
                    }
        
                    
                    thisdocument.pdfDoc.getMetadata().then(function(properties){
                          //console.log(properties);
                          thisdocument.addProperties(properties);
                    });
        
        
                    thisdocument.pdfDoc.getOutline().then(function (outline) {
        
                        if (outline) {
                            thisdocument.pdfoutline = outline;
        
        
                            if (RxCore_GUI_PDFBookmarks != undefined) {
                                RxCore_GUI_PDFBookmarks.setPDFBookmarks(thisdocument.pdfoutline);
                            }
        
        
        
                        }
                    });
        
                    thisdocument.SetActive();*/
                };

                this.getThumb = function (pagenumber) {

                    var thubmnailobj = undefined;

                    for(var pi=0; pi < thisdocument.thumbnails.length; pi++ ){
                        if (thisdocument.thumbnails[pi].pagenumber == pagenumber){
                            thubmnailobj = thisdocument.thumbnails[pi];
                        }
                    }
                    
                    return thubmnailobj;

                };

                this.loadThumbnailPDF = function (pagenum) {

                    if (thisdocument.pages[0].usevector3Dbinary) {
                        return;
                    }
                    if (thisdocument.pages[0].usevector3DGLTF) {
                        return;
                    }
                    
                    if (thisdocument.foxitdoc != null) {
                        thisdocument.foxitdoc.getThumbnail(pagenum);
                    }

                };

                this.loadThumbnail = function (pagenum) {
                    if (thisdocument.pages[0].usevector3Dbinary) {
                        return;
                    }
                    if (thisdocument.pages[0].usevector3DGLTF) {
                        return;
                    }
                    
                    if (thisdocument.foxitdoc != null) {
                        thisdocument.foxitdoc.getThumbnail(pagenum);
                    }
                    else {
                        if (thisdocument.pages[0].usevectorxml) {
                            //thisdocument.thumbnails[pagenum].renderVector(false);


                                  if(thisdocument.thumbnails[pagenum].thumbnailobj.rotation == 90 || thisdocument.thumbnails[pagenum].thumbnailobj.rotation == 270){
                        
                                        thisdocument.thumbnails[pagenum].thumbcanvas.width = parseInt(thisdocument.thumbnails[pagenum].ThumbnailHeight);
                                        thisdocument.thumbnails[pagenum].thumbcanvas.height = parseInt(thisdocument.thumbnails[pagenum].ThumbnailWidth);

                                    }else{

                                        thisdocument.thumbnails[pagenum].thumbcanvas.width = parseInt(thisdocument.thumbnails[pagenum].ThumbnailWidth);
                                        thisdocument.thumbnails[pagenum].thumbcanvas.height = parseInt(thisdocument.thumbnails[pagenum].ThumbnailHeight);

                                    }



                            if(thisdocument.thumbnails[pagenum].renderVector != undefined){
                                thisdocument.thumbnails[pagenum].renderVector(Drawthumbnail);
                            }

                            
                        }
                        else {

                            if(thisdocument.thumbnails[pagenum].thumbnailobj.rotation == 90 || thisdocument.thumbnails[pagenum].thumbnailobj.rotation == 270){
                        
                                thisdocument.thumbnails[pagenum].thumbcanvas.width = parseInt(thisdocument.thumbnails[pagenum].ThumbnailHeight);
                                thisdocument.thumbnails[pagenum].thumbcanvas.height = parseInt(thisdocument.thumbnails[pagenum].ThumbnailWidth);

                            }else{

                                thisdocument.thumbnails[pagenum].thumbcanvas.width = parseInt(thisdocument.thumbnails[pagenum].ThumbnailWidth);
                                thisdocument.thumbnails[pagenum].thumbcanvas.height = parseInt(thisdocument.thumbnails[pagenum].ThumbnailHeight);

                            }


                            //thisdocument.thumbnails[pagenum].renderImage(false);
                            if(thisdocument.thumbnails[pagenum].renderImage != undefined){
                                thisdocument.thumbnails[pagenum].renderImage(Drawthumbnail);
                            }
                            
                        }
                    }
                };
                this.getfoxitThumbnail = function (pagenum) {
                    if (thisdocument.foxitdoc != null) {
                        thisdocument.foxitdoc.getThumbnail(pagenum);
                    }
                };
                this.getFoxitThumbnails = function () {
                    if (thisdocument.foxitdoc != null) {
                        thisdocument.foxitdoc.getThumbnails();
                    }
                };
                this.refreshThumbnails = function () {
                    //thisdocument.thumbnails = [];

                    if (thisdocument.foxitdoc != null) {

                        var pagexmlobj = xmlDoc.getElementsByTagName("Page");
                        const pageXML = pagexmlobj[0];
    
                        thisdocument.thumbnails = [];
    
                        new Array(thisdocument.pages.length).fill(0).forEach((item, id) => {
                            const thumbnail = new ThumbnailObject( pageXML, "Default", thisdocument, id );
                            thisdocument.thumbnails.push(thumbnail);
                        });
    
                    }


                    if (RxCore_GUI_pagethumbs != undefined) {
                        RxCore_GUI_pagethumbs.setThumbnails(thisdocument.thumbnails);
                    }

                };


                this.onFoxitRotateDocument = function(degree){

                    thisdocument.documentrotation = degree;

                    for (var pi = 0; pi < thisdocument.pages.length; pi++) {

                        if(RxCore_GUI_RotatePage != undefined){
                            RxCore_GUI_RotatePage.onRotate(degree, pi);
                        }
    
                    }

                   

                };

                this.rotateDocument = function(degree){


                    if(thisdocument.foxitdoc != null){
                        thisdocument.foxitdoc.rotateDocument(degree, this.onFoxitRotateDocument);

                    }
                    


                };


                this.rotateThumbnail = function(pagenumber, degree){

                    //rotateThumbnail

                    var thumbnail = thisdocument.getThumb(pagenumber);
                    
                    if(thumbnail == undefined){
                        return;
                    }
                    
                    thumbnail.thumbnailobj.rotation = degree;


                };
                this.setthumbnail = function (width, height, pagenumber) {


                    var thumbnail = thisdocument.getThumb(pagenumber);

                    if(thumbnail == undefined){
                        return;
                    }

                    var yscale = thumbnail.thumbnailobj.thumbnail.height / height;
                    var xscale = thumbnail.thumbnailobj.thumbnail.width / width;
                    var dscalelocal = Math.min(xscale, yscale);

                    //var dxlocal = (thisdocument.thumbnails[pagenumber].thumbnailobj.thumbnail.width - (width*dscalelocal)) / 2;
                    //var dylocal = (thisdocument.thumbnails[pagenumber].thumbnailobj.thumbnail.height - (height*dscalelocal)) / 2;
                    thumbnail.setthumbnail(0, 0, dscalelocal);
                    
                    //thispage.setthumbnail(dxlocal,dylocal,dscalelocal);
                    if (thisdocument.pages[0] != undefined) {
                        if (thisdocument.pages[0].usepdfjs) {
                            thisdocument.thumbnails[pagenumber].setPDFdim(width, height);
                        }
                    }
                };
                this.addthumbnail = function (ThumbnailObject) {
                    thisdocument.thumbnails.push(ThumbnailObject);
                    //thisdocument.thumbnails.push(PageObject.getThumbnail());
                };
                this.setBirdseyeTool = function () {

                    var thumbnail = thisdocument.getThumb(thisdocument.currentpage);

                    if(thumbnail == undefined){
                        return;
                    }

                    thumbnail.setBirdseyeTool();

                };
                this.refreshBirdsEye = function () {

                    var thumbnail = thisdocument.getThumb(thisdocument.currentpage);

                    if(thumbnail == undefined){
                        return;
                    }

                    thumbnail.refreshbirdseye();

                };
                this.draw3DNav = function () {

                    var thumbnail = thisdocument.getThumb(thisdocument.currentpage);

                    if(thumbnail == undefined){
                        return;
                    }

                    if(thisdocument.pages[thisdocument.currentpage] == undefined){
                        return;
                    }

                    
                    if(thisdocument.pages[0].usevector3DGLTF){

                        //thispage.Vector3DPageObj.boxCenter;
                        thisdocument.pages[thisdocument.currentpage].Vector3DPageObj.checkcurfloor();

                        thumbnail.drawcamerapos();

                    }else if(thisdocument.pages[thisdocument.currentpage].bis3Dnavigator){
                        
                        thumbnail.drawcamerapos();
                    }
    
                    
                    
                };
                this.setpagecamerapos = function (position, rotation, boxCenter) {
                    if (thisdocument.pages[thisdocument.currentpage].bis3Dnavigator) {
                        thisdocument.pages[thisdocument.currentpage].setcamerapos(position, rotation, boxCenter);
                    }
                };
                this.getBoxcenter = function () {
                    if (thisdocument.pages[0].usevector3DGLTF) {
                        return thisdocument.pages[thisdocument.currentpage].Vector3DPageObj.boxCenter;
                        //thispage.Vector3DPageObj.boxCenter;
                    }
                    else {
                        return null;
                    }
                };
                this.setthumbcamdirfunction = function () {

                    var thumbnail = thisdocument.getThumb(thisdocument.currentpage);

                    if(thumbnail == undefined){
                        return;
                    }

                    if(thisdocument.pages[0].usevector3DGLTF){

                        thumbnail.connectCampos( thisdocument.pages[thisdocument.currentpage].Vector3DPageObj );
                    }

                };
                this.setthumcamerapos = function (position, rotation) {
                    
                    var thumbnail = thisdocument.getThumb(thisdocument.currentpage);

                    if(thumbnail == undefined){
                        return;
                    }


                    if(thisdocument.pages[0].usevector3DGLTF){
                        //thispage.Vector3DPageObj.boxCenter;
                        thumbnail.setcamerapos(position, rotation, thisdocument.pages[thisdocument.currentpage].Vector3DPageObj.boxCenter);
                    }

                };
                this.setFloorNavScaleOffset = function (floornum, alignarray) {


                    var thumbnail = thisdocument.getThumb(thisdocument.currentpage);

                    if(thumbnail == undefined){
                        return;
                    }

                    if(thisdocument.pages[0].usevector3DGLTF){

                        thumbnail.setFloorNavScaleOffset(floornum, alignarray);

                        //thisdocument.thumbnails[thisdocument.currentpage].renderImage(Floorplanreplace);

                        //this.floorplans                        
                        //thisdocument.thumbnails[thisdocument.currentpage].set3DNavImage(Floorplanreplace);
                        
                    }

                };
                this.setfloorplanNav = function (floornum, thumbnail) {



                    if (thisdocument.pages[0].usevector3DGLTF) {
                        thisdocument.pages[0].replacefloorplan(floornum, thumbnail);
                        //this.floorplans                        
                        //thisdocument.thumbnails[thisdocument.currentpage].set3DNavImage(Floorplanreplace);
                    }
                };
                this.getFloorplans = function () {
                    return thisdocument.pages[0].getFloorplans();
                };
                this.create3DFloorplan = function (floornum, canvas, alignarray) {
                    if (thisdocument.pages[0].usevector3DGLTF) {
                        thisdocument.pages[0].Vector3DPageObj.addfloorplanTexture(floornum, canvas, alignarray);
                    }
                };
                this.get3DNavElevationPages = function (floorelvobj) {
                    var nav3dpages = [];
                    for (var pi = 0; pi < thisdocument.pages.length; pi++) {
                        if (thisdocument.pages[pi].bis3Dnavigator) {
                            if (thisdocument.pages[pi].get3DNavElevation(floorelvobj)) {
                                nav3dpages.push({ index: pi, replaced: thisdocument.pages[pi].navfloorplan.replaced });
                            }
                        }
                    }
                    return nav3dpages;
                };
                this.replaceFloorNav = function (callback, floornum) {
                    if (thisdocument.pages[0].usevector3Dbinary) {
                        return;
                    }
                    //floorplanpageobject is the pageobject from 3d file.
                    //get the birdseye image from
                    //var bcnvs = thisdocument.thumbnails[thisdocument.currentpage].getbirdseyeCanvas();
                    if (!thisdocument.pages[0].usevector3DGLTF) {
                        thisdocument.thumbnails[thisdocument.currentpage].removereplacefloornavCallback();
                        thisdocument.thumbnails[thisdocument.currentpage].setreplacefloornavCallback(callback, floornum);
                    }
                    /*.width = imagewidth;
                    thispage.floorplans[index].height = imageheight;
        
                    thispage.floorplans[index].canvas.width = imagewidth;
                    thispage.floorplans[index].canvas.height = imageheight;*/
                    thisdocument.refreshBirdsEye();
                    if (thisdocument.pages[0].usevectorxml) {
                        thisdocument.thumbnails[thisdocument.currentpage];
                        thisdocument.thumbnails[thisdocument.currentpage].renderVector(Floorplanreplace);
                    }
                    else if (thisdocument.pages[0].usepdfjs) {
                        //thisdocument.thumbnails[thisdocument.currentpage].renderPDF(Floorplanreplace);
                    }
                    else if (thisdocument.pages[0].usevector3DGLTF) {
                        //thisdocument.thumbnails[thisdocument.currentpage].set3DNavImage(Floorplanreplace);
                    }
                    else {
                        thisdocument.thumbnails[thisdocument.currentpage].renderImage(Floorplanreplace);
                    }
                };
                this.renderFloorplan = function (callback, floornum, alignarray) {
                    if (thisdocument.pages[0].usevector3Dbinary) {
                        return;
                    }

                    var thumbnail = thisdocument.getThumb(thisdocument.currentpage);

                    if(thumbnail == undefined){
                        return;
                    }


                    /*else if(thisdocument.pages[0].usevector3DGLTF){
                        thisdocument.thumbnails[thisdocument.currentpage].set3DNavImage(true);
                    }*/

                    thumbnail.removefloorplanCallback();
                    thumbnail.setfloorplanCallback(callback, floornum, alignarray);


                    if (thisdocument.pages[0].usevectorxml) {

                        thumbnail.renderVector(Drawfloorplan);

                    }
                    else if (thisdocument.pages[0].usepdfjs) {
                        //thisdocument.thumbnails[thisdocument.currentpage].renderPDF(Drawfloorplan);
                        if (thisdocument.foxitdoc != null) {
                            //thisdocument.foxitdoc.getThumbnail(pagenum);
                            //thisdocument.foxitdoc.getBirdsEye(thisdocument.currentpage, thisdocument.pages[thisdocument.currentpage].getpageRotation());
                            thisdocument.foxitdoc.getBirdsEye(thisdocument.currentpage);
                            //thisdocument.foxitdoc.getBitmap(thisdocument.currentpage);
                        }
                    }
                    else {
                        
                        thumbnail.renderImage(Drawfloorplan);
                    }
                    /*if (thisdocument.pages[thisdocument.currentpage] != undefined) {
                        thisdocument.pages[thisdocument.currentpage].pan_update(0, 0);
                    }*/
                };
                this.setbirdseyefloor = function(floornum){

                    var thumbnail = thisdocument.getThumb(thisdocument.currentpage);

                    if(thumbnail == undefined){
                        return;
                    }

                    if(thisdocument.pages[0].usevector3DGLTF){
                        thumbnail.setBirdseyeFloor(floornum);
                    }

                };
                this.renderBirdsEye = function () {
                    if (thisdocument.pages[0].usevector3Dbinary) {
                        return;
                    }

                    var thumbnail = thisdocument.getThumb(thisdocument.currentpage);
                    if(thumbnail == undefined){
                        return;
                    }

                    else if(thisdocument.pages[0].usevector3DGLTF){
                        thumbnail.set3DNavImage(Drawbirdseye, false);
                    }
                    else if (thisdocument.pages[0].usevectorxml) {
                        thumbnail.renderVector(Drawbirdseye);
                    }
                    else if (thisdocument.pages[0].usepdfjs) {
                        if (thisdocument.foxitdoc != null) {
                            //thisdocument.foxitdoc.getThumbnail(pagenum);
                            //thisdocument.foxitdoc.getBirdsEye(thisdocument.currentpage);

                            if (!thumbnail.birdseyedrawn) {
                                thisdocument.foxitdoc.getBirdsEye(thisdocument.currentpage, thisdocument.pages[thisdocument.currentpage].getpageRotation());
                            }
                            
                            //this.foxitcreateBirdseye
                            //thisdocument.foxitdoc.getBitmap(thisdocument.currentpage);
                        }
                        //thisdocument.thumbnails[thisdocument.currentpage].renderPDF(true);
                        //replace with foxit create birdseye image.
                    }
                    else {
                        //thisdocument.thumbnails[thisdocument.currentpage].renderImage(true);
                        thumbnail.renderImage(Drawbirdseye);
                    }
                    if (thisdocument.pages[thisdocument.currentpage] != undefined) {
                        thisdocument.pages[thisdocument.currentpage].pan_update(0, 0);
                    }
                    //call rect draw
                };

                this.removepage = function(PageObject){

                    var pageindex = -1;

                    for(var pi = 0 ; pi < thisdocument.pages.length;pi++){
                        if(thisdocument.pages[pi].pagenumber == PageObject.pagenumber){
                            pageindex = pi;
                            
                        }
                    }

                    if(pageindex > -1){
                        thisdocument.pages.splice(pageindex, 1);
                    }
                    
                    if(thisdocument.pages.length == 0){
                        //no pages clase doc
                        //internalClosedoc(thisdocument.fileindex);
                        documentopen = true;
                        internalClosedocOrg(thisdocument.fileindex);
                        
                    }else{
                        thisdocument.GotoPage(thisdocument.prevpage);
                    }
                    

                };

                this.addpage = function (PageObject, loadimage) {
                    if (PageObject == undefined) {
                        thisdocument.pages.push(PageObject);
                        thisdocument.pagerotations.push(0);
                        var pagnum = thisdocument.pages.length - 1;
                        var displaypage = pagnum + 1;
                        return;
                    }

                    PageObject.DocRef = thisdocument;
                    thisdocument.pages.push(PageObject);
                    thisdocument.pagerotations.push(0);
                    pagnum = thisdocument.pages.length - 1;
                    displaypage = pagnum + 1;

                    //this.thumbnailhtmlsource += "<img src='" + PageObject.ThumbnailImageSRC + "'" + " alt='"+ this.PageName + "' onclick='getpage(" + pagnum + ")'>" + "<p style='text-indent: 80px;color:white'>"  + displaypage + "</p>";
                    //ThumbnailpagesContainer.Addpage(PageObject.ThumbnailImageSRC,this.PageName, pagnum, displaypage);
                    //PageObject.loadvector();
                    /*if (PageObject.usevectorxml){
                     loadimage = false;
                     PageObject.loadvectors();
                     }*/

                     //thisdocument.foxitdoc.getpageinfodim(PageObject.pagenumber, PageObject.setfoxitPageSize);                     
                     
                    if (loadimage) {
                        if (PageObject.usevector3Dbinary) {
                            PageObject.loadvectors3DBinary();
                            /*if(PageObject.has3Dnav){
                             PageObject.load3dnavigator();
                             }*/
                        }
                        else if (PageObject.usevector3DGLTF) {
                            PageObject.vector3DGltfload();
                            PageObject.loadfloorplan();
                        }
                        else if (PageObject.usevector3Dxml) {
                            //PageObject.loadvectors3D();
                        }
                        else if (PageObject.usevectorbinary) {
                            setSmoothingEnabled(false);
                            // contexto.imageSmoothingEnabled = false;
                            // contexto.mozImageSmoothingEnabled = false;
                            // contexto.msImageSmoothingEnabled = false;
                            thisdocument.bRenderinProgress = true;
                            PageObject.loadbinvectors();


                            //PageObject.loadbinvectors();
                        }else if(PageObject.usevectorbinarycompare){
                            setSmoothingEnabled(false);
                            thisdocument.bRenderinProgress = true;
                            PageObject.loadcomparevectors();
                        }
                        else if (PageObject.usevectorxml && PageObject.VectorPageObj != undefined) {
                            setSmoothingEnabled(false);
                            // contexto.imageSmoothingEnabled = false;
                            // contexto.mozImageSmoothingEnabled = false;
                            // //contexto.webkitImageSmoothingEnabled = false;
                            // contexto.msImageSmoothingEnabled = false;
                            thisdocument.bRenderinProgress = true;
                            PageObject.loadbinvectors();

                            if (PageObject.VectorPageObj.height == 0 || PageObject.VectorPageObj.width == 0) {
                                thisdocument.pages.pop();
                                //return;
                            }
                            //PageObject.loadvectors();
                        }
                        else if (PageObject.usepdfjs) {
                            setSmoothingEnabled(false);
                            //PageObject.setNotCollapsedArray();
                            // contexto.imageSmoothingEnabled = false;
                            // contexto.mozImageSmoothingEnabled = false;
                            // //contexto.webkitImageSmoothingEnabled = false;
                            // contexto.msImageSmoothingEnabled = false;
                            //backgroundColor = "#FFFFFF";
                            //this.backgroundColor = backgroundColor;
                            thisdocument.backgroundColor = "#FFFFFF";
                            var file = getFileName(thisdocument.FileName);
                            if (thisdocument.FileLocal) {
                                var cachfolder = getURLPath(PageObject.ThumbnailImageSRC);
                                //var cachfolder = RxConfig.UploadServerfolder;

                            }
                            else {
                                cachfolder = getURLPath(thisdocument.FileNameSRC);
                            }
                            if (thisdocument.relativepath != undefined) {
                                cachfolder = thisdocument.relativepath;
                            }
                            var noCachFolder = RxConfig.noCachFolder;
                            if (noCachFolder) {
                                thisdocument.pdfURL = RxConfig.xmlurlrel + thisdocument.FileNameSRC;
                            }
                            else {
                                thisdocument.pdfURL = cachfolder + file;
                            }
                            if (PageObject.firstpage) {
                                //PDFJS.cMapUrl = '/pdfjs/web/cmaps/';
                                /*if (thisdocument.pdfDoc == null || !thisdocument.busesetpdfdoc){
        
                                    thisdocument.PDFWrapper = new PDFDocObject(thisdocument);
                                    thisdocument.PDFWrapper.initiate(RxConfig.PDFcmap);
                                    thisdocument.PDFWrapper.openDocument(thisdocument.pdfURL);
                                    thisdocument.bdestroyPDFonClose = true;
        
                                }else{
                                    thisdocument.loadPDFpages();
        
                                }*/
                                //intitFoxit();
                                if (cachfolder != "") {
                                    //iframe implement 

                                    this.foxitdoc = foxitGetDoc();

            

                                    //foxitGetDoc().openPDF(file);
                                    if (this.foxitdoc != null) {

                                        comparecheckFoxit(false);
                                        this.foxitdoc.openPDFURL(thisdocument.pdfURL, thisdocument.openFileName);
                                        this.foxitdoc.rxindex = this.fileindex;
                                        addfoxitdoc();
                                    }
                                    //iframe implement 
                                    /*if (foxitgetOpenDocs() == foxitgetnumDocs()){
                                        
                                        addfoxitdoc();
                                        
                                    }*/
                                    //iframe implement 
                                    //addfoxitdoc();
                                    thisdocument.bdestroyPDFonClose = true;
                                }
                                //this.foxitdoc = addfoxitdoc();
                                //this.foxitdoc.openPDFURL(thisdocument.pdfURL);
                                //if(this.foxitdoc != null){
                                //this.foxitdoc.openPDFURL(thisdocument.pdfURL);
                                //openfoxitPDFURL(thisdocument.pdfURL);
                                //}
                                thisdocument.loadPDFpages();
                                //PDFJS.cMapUrl = RxConfig.PDFcmap;
                                //PDFJS.cMapPacked = true;
                                //PDFJS.disableWorker = true;
                                /*if (RxCore_GUI_Download != undefined) {
                                    RxCore_GUI_Download.setDownload("show");
                                }*/
                                //this.pdfLoadingTask = PDFJS.getDocument({url:thisdocument.pdfURL,worker:docworker});
                            }
                        }
                        else {
                            if (thisdocument.Type == 0) {
                                PageObject.initialzoom = 2;
                            }
                            //backgroundColor = "#FFFFFF";
                            //this.backgroundColor = backgroundColor;
                            thisdocument.backgroundColor = "#FFFFFF";
                            setSmoothingEnabled(true);
                            // contexto.imageSmoothingEnabled = true;
                            // contexto.mozImageSmoothingEnabled = true;
                            // //contexto.webkitImageSmoothingEnabled = true;
                            // contexto.msImageSmoothingEnabled = true;
                            //PageObject.loadimages(thisdocument.pageNum);
                            PageObject.loadimages(thisdocument.pageNum);
                            //PageObject.loadimages();
                        }
                    }

                    /*if (PageObject.usepdfjs &&  thisdocument.pdfDoc != null){
                     PageObject.loadimages();
                     }*/
                    /*if (PageObject.usepdfjs && thisdocument.pdfDoc != null) {
                       PageObject.loadimages(thisdocument.pageNum);
                   }*/
                    if (PageObject.usevectorbinary) {
                        PageObject.usevectorxml = true;
                    }
                    if(PageObject.usevectorbinarycompare){
                        PageObject.usevectorxml = true;
                    }
                    if (PageObject.usevector3Dbinary) {
                        PageObject.usevector3Dxml = true;
                    }
                    if (PageObject.usevector3DGLTF) {
                        PageObject.usevector3Dxml = true;
                    }
                };
                this.addlayout = function (Name) {
                    thisdocument.layouts.push(Name);
                };
                this.setmovepagesthird = function(targetsource, pgpos, targetpos){

                    var bmovewholerange = false;

                    bmovewholerange = true;

                    targetsource.push({ from: pgpos.midpage, to: targetpos });

                    var pgindex = targetpos + 1;
                    for (var ei = 0; ei < pgpos.last.length; ei++) {
                        targetsource.push({ from: pgpos.last[ei], to: pgindex + ei });
                        //thisdocument.movepage(this.pages, pgindex + ei , pgpos.last[ei]);
                        //thisdocument.pages[targetpos.end + ei].movepage(targetpos.end + ei);
                    }
                    pgindex = targetpos - pgpos.first.length;
                    for (var fi = 0; fi < pgpos.first.length; fi++) {
                        targetsource.push({ from: pgpos.first[fi], to: pgindex + fi });
                        //thisdocument.movepage(this.pages, pgindex + fi, pgpos.first[fi]);
                        //thisdocument.pages[targetpos.start + fi].movepage(targetpos.start + fi);
                    }

                    return targetsource;

                };
                this.setmovepages = function(targetsource, pgpos, targetpos, bfdirection){

                    var bmovewholerange = false;

                    bmovewholerange = true;

                    var bmediumsize = (thisdocument.pages.length <= (nMaxpages * 2));
                    

                    if(bfdirection){

                        var moveindex = targetpos;

                        if(bmediumsize){
                            
                            var positioncopy = pgpos.positions.slice();
                            var unusedpos = [];

                            var startpos = 0;
                            

                            while ((positioncopy.length > 0) && (moveindex < thisdocument.pages.length)){
                                

                                var position = positioncopy.shift(); //  removes the first element

                                if(position != moveindex){
                                    targetsource.push({ from: position, to: moveindex });
                            

                                }else{
                                    unusedpos.unshift(position);
                                }

                                moveindex++;

                            }

                            if(targetpos - positioncopy[positioncopy.length - 1] > 1){

                                var nummoves = targetpos - positioncopy[positioncopy.length - 1] - 1;
                                var counter = 0;
                                moveindex = targetsource[0].to - nummoves;
                                //lastpage = positioncopy[positioncopy.length - 1];

                                while (counter < nummoves){
                                

                                    var position = positioncopy.shift(); //  removes the first element
    
                                    if(position != moveindex){
                                        targetsource.push({ from: position, to: moveindex });
                                    }
    
                                    moveindex++;
                                    counter++
    
                                }
    

                            }
                            
                            
                            
                        }else{
                            targetsource.push({ from: pgpos.midpage, to: targetpos });
                            var pgindex = targetpos + 1;
        
                            for (var ei = 0; ei < pgpos.last.length; ei++) {
                                targetsource.push({ from: pgpos.last[ei], to: pgindex + ei });
                                //thisdocument.movepage(this.pages, pgindex + ei , pgpos.last[ei]);
                                //thisdocument.pages[targetpos.end + ei].movepage(targetpos.end + ei);
                            }
                            pgindex = targetpos - pgpos.first.length;
        
                            for (var fi = 0; fi < pgpos.first.length; fi++) {
                                targetsource.push({ from: pgpos.first[fi], to: pgindex + fi });
                                //thisdocument.movepage(this.pages, pgindex + fi, pgpos.first[fi]);
                                //thisdocument.pages[targetpos.start + fi].movepage(targetpos.start + fi);
                            }
    
                        }

    
                    }else{

                        var moveindex = targetpos;

                        if(bmediumsize){

                            var positioncopy = pgpos.positions.slice();
                            var unusedpos = [];

                            var startpos = 0;
                            

                            while ((positioncopy.length > 0) && (moveindex < thisdocument.pages.length)){
                                

                                var position = positioncopy.shift(); //  removes the first element

                                if(position != moveindex){
                                    targetsource.push({ from: position, to: moveindex });
                            

                                }else{
                                    unusedpos.unshift(position);
                                }

                                moveindex++;

                            }

                            if(targetpos - positioncopy[positioncopy.length - 1] > 1){

                                var nummoves = targetpos - positioncopy[positioncopy.length - 1] - 1;
                                var counter = 0;
                                moveindex = targetsource[0].to - nummoves;
                                //lastpage = positioncopy[positioncopy.length - 1];

                                while (counter < nummoves){
                                

                                    var position = positioncopy.shift(); //  removes the first element
    
                                    if(position != moveindex){
                                        targetsource.push({ from: position, to: moveindex });
                                    }
    
                                    moveindex++;
                                    counter++
    
                                }
    

                            }                            


                        } else{
                            pgindex = targetpos - pgpos.first.length;
    
                            for (var fi = 0; fi < pgpos.first.length; fi++) {
                                targetsource.push({ from: pgpos.first[fi], to: pgindex + fi });
                                //thisdocument.movepage(this.pages, pgindex + fi, pgpos.first[fi]);
                                //thisdocument.pages[targetpos.start + fi].movepage(targetpos.start + fi);
                            }
    
    
                            targetsource.push({ from: pgpos.midpage, to: targetpos });
                            var pgindex = targetpos + 1;
        
                            for (var ei = 0; ei < pgpos.last.length; ei++) {
                                targetsource.push({ from: pgpos.last[ei], to: pgindex + ei });
                                //thisdocument.movepage(this.pages, pgindex + ei , pgpos.last[ei]);
                                //thisdocument.pages[targetpos.end + ei].movepage(targetpos.end + ei);
                            }
    
    
                        }


                    }


                    return targetsource;

                };
                this.getTargetPositions = function (targetpos, pgpos) {
                    //var pagepositions = [];
                    //var counter = 0;
                    var withinpagerange = false;
                    var bmovetofront = false;
                    var bmovetoend = false;
                    var numpagestomove = 0;
                    var bmovewholerange = false;
                    var targetsource = [];
                    
                    var lastPage = thisdocument.pages.length - 1;
                    //check if new page is within pageobject range
                    //then return the needed shift.
                    //if within range move pages 
                    for (var pgi = 0; pgi < pgpos.positions.length; pgi++) {
                        if (targetpos == pgpos.positions[pgi]) {
                            withinpagerange = true;
                            var relativepos = pgi;
                        }
                    }
                    numpagestomove = Math.abs(targetpos - pgpos.midpage);
                    if (numpagestomove > nMaxpages - 1) {
                        numpagestomove = nMaxpages - 1;
                    }
                    if ((targetpos - pgpos.midpage) < 0) {
                        bmovetofront = true;
                        bmovetoend = false;
                    }
                    else {
                        bmovetoend = true;
                        bmovetofront = false;
                    }
                    if (withinpagerange) {
                        for (var i = 0; i < numpagestomove; i++) {
                            var indx = i + 1;
                            if (bmovetoend) {
                                if (pgpos.positions[pgpos.positions.length - 1] + indx <= lastPage) {
                                    targetsource.push({ from: pgpos.positions[0] + i, to: pgpos.positions[pgpos.positions.length - 1] + indx });
                                }
                            }
                            else if (bmovetofront) {
                                if (pgpos.positions[0] - indx >= 1) {
                                    targetsource.push({ from: pgpos.positions[pgpos.positions.length - 1] - i, to: pgpos.positions[0] - indx });
                                }
                            }
                        }
                    }else{
                        //if (targetpos >= nMaxpages && targetpos <= (thisdocument.pages.length - 1) - nMaxpages) {
                            //testing with lenght instead of lenth -1


                            if (targetpos >= nMaxpages && targetpos <= (thisdocument.pages.length) - nMaxpages) {
                                //target page number lower than page range
                                //move whole range
                                var pgindex = targetpos + 1;
    
                                targetsource = thisdocument.setmovepages(targetsource, pgpos, targetpos, bmovetoend);
    
                                /*bmovewholerange = true;
                                targetsource.push({ from: pgpos.midpage, to: targetpos });
                                var pgindex = targetpos + 1;
                                for (var ei = 0; ei < pgpos.last.length; ei++) {
                                    targetsource.push({ from: pgpos.last[ei], to: pgindex + ei });
                                    //thisdocument.movepage(this.pages, pgindex + ei , pgpos.last[ei]);
                                    //thisdocument.pages[targetpos.end + ei].movepage(targetpos.end + ei);
                                }
                                pgindex = targetpos - pgpos.first.length;
                                for (var fi = 0; fi < pgpos.first.length; fi++) {
                                    targetsource.push({ from: pgpos.first[fi], to: pgindex + fi });
                                    //thisdocument.movepage(this.pages, pgindex + fi, pgpos.first[fi]);
                                    //thisdocument.pages[targetpos.start + fi].movepage(targetpos.start + fi);
                                }*/
                                //thisdocument.movepage(this.pages, targetpagenum, pgpos.midpage);
                                //move last index of pgpos to last page.
                                //targetsource.push({from : pgpos.midpage, to : targetpagenum);   
                                //thisdocument.movepage(this.pages, targetpagenum, pgpos.midpage);   
                            }
                            else if (targetpos < nMaxpages) {
                                pgindex = 1;
                                for (var wri = 0; wri < pgpos.positions.length; wri++) {
                                    //thisdocument.movepage(this.pages, pgindex + wri, pgpos.positions[wri]);
                                    targetsource.push({ from: pgpos.positions[wri], to: pgindex + wri });
                                }
                            }
                            //testing with lenght instead of lenth -1
                            //else if (targetpos > (thisdocument.pages.length - 1) - nMaxpages) {
                                
                            else if (targetpos > (thisdocument.pages.length) - nMaxpages) {
    
                                if(targetpos >= (thisdocument.pages.length - pgpos.last.length)){
                                    var startpage = (thisdocument.pages.length - 1) - pgpos.last.length;
                                }else{
                                    startpage = targetpos;
                                }
                                
                                /*if(startpage < nMaxpages){
                                    pgpos.midpage -= nMaxpages - startpage;
                                    //startpage = targetpos;
                                }*/
    
                                var bmediumsize = (thisdocument.pages.length <= (nMaxpages * 2));
                                if(bmediumsize){
                                    targetsource = thisdocument.setmovepages(targetsource, pgpos, targetpos, bmovetoend);
                                }else{
                                    targetsource = thisdocument.setmovepages(targetsource, pgpos, startpage, bmovetoend);
                                }
    
                                
    
                                /*pgindex = thisdocument.pages.length - pgpos.positions.length;
                                for (var wrei = 0; wrei < pgpos.positions.length; wrei++) {
                                    //thisdocument.movepage(this.pages, pgindex + wrei, pgpos.positions[wrei]);
                                    targetsource.push({ from: pgpos.positions[wrei], to: pgindex + wrei });
                                }*/
                            }
                    }


                    /*for (var i = 0; i < numpagestomove; i++) {
                        var indx = i + 1;
                        if (bmovetoend) {
                            targetsource.push({ from: pgpos.positions[0] + i, to: pgpos.positions[pgpos.positions.length - 1] + indx });
                        }
                        else if (bmovetofront) {
                            targetsource.push({ from: pgpos.positions[pgpos.positions.length - 1] - i, to: pgpos.positions[0] - indx });
                        }
                    }*/
                    //var targetsource = {from : 0, to : 29};
                    var startpage = 1;
                    /*if ((targetpos - pgpos.first.length) <= 1){
                        startpage = 1;
                    }else{
                        startpage = targetpos -  pgpos.first.length;
                    }*/
                    return {
                        within: withinpagerange,
                        toend: bmovetoend,
                        tofront: bmovetofront,
                        pagestomove: numpagestomove,
                        fromto: targetsource
                    };
                };
                this.getpagePositions = function (pagenum) {
                    var pagepositions = [];
                    var  doublemaxp = nMaxpages * 2;

                    var counter = 1;
                    while (counter < thisdocument.pages.length) {
                        if (thisdocument.pages[counter] != undefined) {
                            pagepositions.push(counter);
                        }
                        counter++;
                    }
                    var quotient = Math.floor(pagepositions.length / 2);
                    var firstrange = [];
                    var endrange = [];
                    var midpage = 0;


                    for (var pgi = 0; pgi < pagepositions.length; pgi++) {
                        if (pgi < quotient) {
                            firstrange.push(pagepositions[pgi]);
                        }
                        else if (pgi > quotient) {
                            endrange.push(pagepositions[pgi]);
                        }
                    }
                    /*if(pagenum > nMaxpages && pagenum < (thisdocument.pages.length - 1) - nMaxpages){
                    }else if(pagenum < nMaxpages){
        
        
                    }else if(pagenum > (thisdocument.pages.length - 1) - nMaxpages){
        
                    }*/
                    return {
                        positions: pagepositions,
                        midpage: pagepositions[quotient],
                        first: firstrange,
                        last: endrange,
                    };
                };
                this.checkpageposnumbers = function () {
                    for (var pi = 0; pi < thisdocument.pages.length; pi++) {
                        if (thisdocument.pages[pi] != undefined) {
                            if (pi != thisdocument.pages[pi].pagenumber) {
                                thisdocument.pages[pi].movepage(pi);
                            }
                        }
                    }
                };
                this.movepage = function (arr, new_index, old_index) {
                    var swap = arr[old_index];
                    arr[old_index] = arr[new_index];
                    arr[new_index] = swap;
                    if (arr[new_index] != undefined) {
                        arr[new_index].movepage(new_index);
                    }
                    //thisdocument.pages[targetpos.fromto[mi].to].movepage(targetpos.fromto[mi].to);
                };
                this.oldmovepage = function (arr, new_index, old_index) {
                    if (new_index >= arr.length) {
                        var k = new_index - arr.length + 1;
                        while (k--) {
                            arr.push(undefined);
                        }
                    }
                    arr.splice(new_index, 0, arr.splice(old_index, 1)[0]);
                    return arr;
                };
                this.movePageObjects = function (targetpagenum) {

                    if(!thisdocument.bLargePDF){
                        return;
                    }
                    //check for first pageobject in pages array except first page.
                    var bfirstpagefound = false;
                    var blastpagefound = false;
                    //get array of pageobjects position.
                    var pgpos = thisdocument.getpagePositions(targetpagenum);
                    if (pgpos.positions[0] == 1 && targetpagenum < pgpos.midpage) {
                        //page range in start position and middle of page range not yet reached exit
                        return;
                    }
                    if (pgpos.positions[nMaxpages - 2] == thisdocument.pages.length - 1 && targetpagenum > pgpos.midpage) {
                        //page range in end position and middle of page range not yet reached exit
                        return;
                    }
                    if (pgpos.positions.length != 0) {
                        var targetpos = thisdocument.getTargetPositions(targetpagenum, pgpos);
                    }

                    /*if (targetpagenum == 0 && pgpos.positions[0] != 1) {
                        pgindex = 1;
                        for (var wri = 0; wri < pgpos.positions.length; wri++) {
                            thisdocument.movepage(this.pages, pgindex + wri, pgpos.positions[wri]);
                        }
                        return;
                    }*/
                    if (targetpos.within) {
                        /*return {
                            within : withinpagerange,
                            toend : bmovetoend,
                            tofront : bmovetofront,
                            pagestomove : numpagestomove,
                            fromto : targetsource
                        }*/
                        for (var mi = 0; mi < targetpos.fromto.length; mi++) {
                            thisdocument.movepage(this.pages, targetpos.fromto[mi].to, targetpos.fromto[mi].from);
                            //thisdocument.pages[targetpos.fromto[mi].to].movepage(targetpos.fromto[mi].to);
                        }
                    }
                    else {

                        for (var mi = 0; mi < targetpos.fromto.length; mi++) {
                            thisdocument.movepage(this.pages, targetpos.fromto[mi].to, targetpos.fromto[mi].from);
                            //thisdocument.pages[targetpos.fromto[mi].to].movepage(targetpos.fromto[mi].to);
                        }

                        /*if (targetpagenum > nMaxpages && targetpagenum < (thisdocument.pages.length - 1) - nMaxpages) {
                            //move mid page to target page first
                            thisdocument.movepage(this.pages, targetpagenum, pgpos.midpage);
                            //thisdocument.pages[targetpagenum].movepage(targetpagenum);
                            //move pages after target page second
                            var pgindex = targetpagenum + 1;
                            for (var ei = 0; ei < pgpos.last.length; ei++) {
                                thisdocument.movepage(this.pages, pgindex + ei, pgpos.last[ei]);
                                //thisdocument.pages[targetpos.end + ei].movepage(targetpos.end + ei);
                            }
                            //move pages before mid page to target pages last
                            pgindex = targetpagenum - pgpos.first.length;
                            for (var fi = 0; fi < pgpos.first.length; fi++) {
                                thisdocument.movepage(this.pages, pgindex + fi, pgpos.first[fi]);
                                //thisdocument.pages[targetpos.start + fi].movepage(targetpos.start + fi);
                            }
                        }
                        else if (targetpagenum < nMaxpages) {
                            pgindex = 1;
                            for (var wri = 0; wri < pgpos.positions.length; wri++) {
                                thisdocument.movepage(this.pages, pgindex + wri, pgpos.positions[wri]);
                            }
                        }
                        else if (targetpagenum > (thisdocument.pages.length - 1) - nMaxpages) {
                            pgindex = thisdocument.pages.length - pgpos.positions.length;
                            for (var wrei = 0; wrei < pgpos.positions.length; wrei++) {
                                thisdocument.movepage(this.pages, pgindex + wrei, pgpos.positions[wrei]);
                            }
                        }*/
                        thisdocument.checkpageposnumbers();
                    }
                };
                this.loadpage = function (pagenum) {
                    //console.log('is loadpage called?');
                    if (!thisdocument.pages[thisdocument.currentpage].usevectorxml && !thisdocument.pages[thisdocument.currentpage].usevectorbinary) {
                        if (pagenum != 0) {
                            if (thisdocument.pages[thisdocument.currentpage].usepdfjs) {
                                if (thisdocument.bLargePDF) {
                                    //this.pages[pagenum].queRenderPageScaled();
                                    //this.pages[pagenum].foxitRenderPageScaled();
                                }
                                else {
                                    //this.pages[pagenum].queueRenderPage(pagenum + 1);
                                    //this.pages[pagenum].foxitRenderPageScaled();
                                }
                            }
                            else {
                                if (!thisdocument.pages[pagenum].largeimageloaded || !thisdocument.pages[pagenum].smallimageloaded) {
                                    if (thisdocument.Type == 0) {
                                        thisdocument.pages[pagenum].initialzoom = 2;
                                    }
                                    thisdocument.pages[pagenum].loadimages();
                                }
                            }
                            //this.pages[pagenum].dscale = this.pages[0].dscale;
                            //this.pages[pagenum].dx = this.pages[0].dx;
                            //this.pages[pagenum].dy = this.pages[0].dy;
                            //this.pages[pagenum].dscaleextent = this.pages[0].dscaleextent;
                            //this.pages[pagenum].dxextent = this.pages[0].dxextent;
                            //this.pages[pagenum].dyextent = this.pages[0].dyextent;
                            //this.pages[pagenum].checkimageswitch();
                            //this.pages[this.currentpage].currentimage = this.pages[this.currentpage-1].currentimage;
                        }
                        //this.pages[this.currentpage].draw_image(true);
                        /*if (this.Type == 0){
                         this.draw_allpages();
                         }else{
                         this.pages[this.currentpage].draw_image();
                         }*/
                    }
                    else {
                        if (!thisdocument.pages[thisdocument.currentpage].vectorloaded) {
                            thisdocument.pages[thisdocument.currentpage].loadbinvectors();
                        }
                        else {
                            if(this.pages[this.currentpage].VectorPageObj != undefined){
                                if (thisdocument.pages[thisdocument.currentpage].VectorPageObj.width == 0 || thisdocument.pages[thisdocument.currentpage].VectorPageObj.height == 0) {
                                    //this.currentpage = prevpage;
                                    return;
                                }
                                thisdocument.pages[thisdocument.currentpage].draw_vector(true);
                            }
                        }
                        //this.pages[this.currentpage].draw_vector(true);
                    }
                    //drawmarkupAll(cntximg);
                };

                this.getpageThumb = function (pagenum) {

                    return thisdocument.getThumb(pagenum);

                };


                this.getCurrentThumb = function () {

                    return thisdocument.getThumb(thisdocument.currentpage);

                };
                this.getHoverSpace = function(){
                    return thisdocument.hoverspace;
                };
                this.addHoverSpace = function(hoverobj){

                    if(thisdocument.hoverspace == null){
                        thisdocument.hoverspace = hoverobj;
                    }
                    
                };
                this.clearHoverSpace = function(){
                    thisdocument.hoverspace = null;

                };
                this.drawHoverSpace = function(ctx){
                    if(thisdocument.hoverspace != null){
                        
                        var scalefactor = thisdocument.getCurrentpage().dscalevector;
                        var xoffset = thisdocument.getCurrentpage().dxvector;
                        var yoffset = thisdocument.getCurrentpage().dyvector;
                        var mediax = thisdocument.getCurrentpage().VectorPageObj.x;
                        var mediay = thisdocument.getCurrentpage().VectorPageObj.y;
                        var mediah = thisdocument.getCurrentpage().VectorPageObj.h;

                        var xscaled = (thisdocument.hoverspace.path[0].x - mediax) * scalefactor;
                        var yscaled = (mediah - thisdocument.hoverspace.path[0].y) * scalefactor;

                        xscaled += xoffset;
                        yscaled += yoffset;

                        ctx.save();
                        ctx.lineWidth = 0;
                        ctx.fillStyle = thisdocument.hoverspace.color;
                        ctx.beginPath();

                        ctx.moveTo(xscaled, yscaled);

                        for (var counter = 1; counter < thisdocument.hoverspace.path.length; counter++) {
                            xscaled = ((thisdocument.hoverspace.path[counter].x - mediax) * scalefactor);
                            yscaled = ((mediah - thisdocument.hoverspace.path[counter].y) * scalefactor);

                            xscaled += xoffset;
                            yscaled += yoffset;
    
                            ctx.lineTo(xscaled, yscaled);
                        }

                        ctx.closePath();
                        ctx.fill();
                        ctx.restore();
                    }
                    
                };
                this.setPageBitmap = function (bitmap, index) {
                    if (thisdocument.pages[index] != undefined) {
                        thisdocument.pages[index].setPageBitmap(bitmap);
                    }
                };

                this.visiblepage = function(pagenum){
                    //do check here.

                    var pagevisible = false;

                    if(thisdocument.pages[pagenum] != undefined){
                        pagevisible = thisdocument.pages[pagenum].ispagevisible();
                        //pagevisible = thisdocument.pages[pagenum].visible;
                        //thispage.visible = true;
                        
                    }else{
                        pagevisible = false;
                    }

                    return pagevisible;

                };

                this.getCurrentpage = function () {
                    if (thisdocument.pages[thisdocument.currentpage] == undefined) {
                        return thisdocument.pages[0];
                    }
                    else {
                        return thisdocument.pages[thisdocument.currentpage];
                        //thisdocument.movePageObjects(thisdocument.currentpage);
                        //return thisdocument.pages[thisdocument.currentpage];
                    }
                };
                this.setCurrentpage = function (curpage) {
                    var szformatshort = thisdocument.Format.substring(0, 9);
                    var bPDF = (szformatshort == "Adobe PDF");
                    //var curx = thisdocument.pages[curpage].dxpdf;
                    //var cury = thisdocument.pages[curpage].dypdf;
                    if (thisdocument.pages.length > nMaxpages && bPDF) {
                        thisdocument.movePageObjects(curpage);
                        if (thisdocument.foxitdoc != null) {
                            //thisdocument.foxitdoc.gotoPage(curpage);
                            //thisdocument.foxitdoc.setCurPage(curpage);
                        }
                    }
                    else if (bPDF) {
                        if (thisdocument.foxitdoc != null) {
                            //thisdocument.foxitdoc.setCurPage(curpage);
                            //thisdocument.foxitdoc.gotoPage(curpage);
                        }
                    }
                    thisdocument.currentpage = curpage;
                    thisdocument.pages[thisdocument.currentpage].visible = true;


                    if(bMultimarkupadd){

                        if(thisdocument.selectedmarkup.id != -1){
                            if (thisdocument.markuplist[thisdocument.selectedmarkup.id] != undefined){
                                thisdocument.markuplist[thisdocument.selectedmarkup.id].setselectedit(false);
                                thisdocument.markuplist[thisdocument.selectedmarkup.id].setselect(false);
                
                            }
                        }
            
                        drawmarkupAll(cntximg);
                        DrawMarkupSelected(context);
                
                    }


                    //thisdocument.pages[thisdocument.currentpage].setPagePos(curx,cury);
                };
                this.PageDown = function () {
                    /*if (thisdocument.pagelocked){
                        return;
                    }*/
                    var prevpage = this.currentpage;
                    if (thisdocument.pages[thisdocument.currentpage].usepdfjs) {
                        if (thisdocument.textselect) {
                            thisdocument.pages[thisdocument.currentpage].PDFTextArea.style.display = "none";
                        }
                    }
                    if (this.currentpage + 1 <= this.pages.length - 1) {
                        thisdocument.setCurrentpage(this.currentpage + 1);
                        //this.currentpage++;
                        if (thisdocument.pages[thisdocument.currentpage].usevector3Dxml) {
                            if (thisdocument.pages[thisdocument.currentpage].Vector3DPageObj == undefined) {
                                thisdocument.pages[thisdocument.currentpage].loadvectors3DBinary();
                            }
                            else {
                                /*if (this.pages[this.currentpage].VectorPageObj.width == 0 || this.pages[this.currentpage].VectorPageObj.height == 0) {
                                 this.currentpage = prevpage;
                                 return;
                                 }*/
                                thisdocument.restorecurpage = thisdocument.currentpage;
                                thisdocument.SetActive();
                            }
                        }
                        else if (thisdocument.pages[thisdocument.currentpage].usevectorxml || thisdocument.pages[thisdocument.currentpage].usevectorbinary) {
                            if (!thisdocument.pages[thisdocument.currentpage].vectorloaded) {
                                thisdocument.pages[thisdocument.currentpage].loadbinvectors();
                            }
                            else {
                                if (thisdocument.pages[thisdocument.currentpage].VectorPageObj.width == 0 || thisdocument.pages[thisdocument.currentpage].VectorPageObj.height == 0) {
                                    thisdocument.setCurrentpage(prevpage);
                                    //this.currentpage = prevpage;
                                    return;
                                }
                                thisdocument.restorecurpage = this.currentpage;
                                thisdocument.SetActive();
                                //this.pages[this.currentpage].draw_vector(true);
                            }
                            //this.pages[this.currentpage].draw_vector(true);
                        }
                        else if (thisdocument.pages[thisdocument.currentpage].usepdfjs) {
                            if (!thisdocument.pagelocked) {
                                thisdocument.pages[thisdocument.currentpage].dxpdf = thisdocument.pages[prevpage].dxpdf;
                                thisdocument.pages[thisdocument.currentpage].dypdf = 0;
                            }
                            if (!thisdocument.pages[thisdocument.currentpage].pageloaded) {
                                thisdocument.loadpage(thisdocument.currentpage);
                            }
                            thisdocument.pages[thisdocument.currentpage].draw_canvas(true);
                            //shortpgupdateloop();
                            if (!thisdocument.pages[thisdocument.currentpage].iscollapsed) {
                                //thisdocument.pgChangeLoop();
                            }
                            //pgupdateloop();
                            if (thisdocument.textselect) {
                                thisdocument.pages[thisdocument.currentpage].PDFTextArea.style.display = szdispvalue;
                                thisdocument.pages[thisdocument.currentpage].PDFTextArea.style.zIndex = "4";
                            }
                        }
                        else {
                            //if (!this.pages[this.currentpage].usevectorxml){
                            if (thisdocument.currentpage != 0) {
                                if (!thisdocument.pages[thisdocument.currentpage].largeimageloaded || !thisdocument.pages[thisdocument.currentpage].smallimageloaded) {
                                    if (thisdocument.Type == 0) {
                                        thisdocument.pages[thisdocument.currentpage].initialzoom = 2;
                                    }
                                    thisdocument.pages[thisdocument.currentpage].loadimages();
                                }
                                thisdocument.pages[thisdocument.currentpage].dscale = thisdocument.pages[thisdocument.currentpage - 1].dscale;
                                thisdocument.pages[thisdocument.currentpage].dx = thisdocument.pages[thisdocument.currentpage - 1].dx;
                                thisdocument.pages[thisdocument.currentpage].dy = thisdocument.pages[thisdocument.currentpage - 1].dy;
                                thisdocument.pages[thisdocument.currentpage].checkimageswitch();
                                //this.pages[this.currentpage].currentimage = this.pages[this.currentpage-1].currentimage;
                            }
                            thisdocument.pages[thisdocument.currentpage].draw_image(true);
                        }
                        /*else{
                         if (!this.pages[this.currentpage].vectorloaded){
                         this.pages[this.currentpage].loadvectors();
                         }
        
                         this.pages[this.currentpage].draw_vector(true);
                         }*/
                        drawmarkupAll(cntximg);
                        


                        var stateobj = {
                            iscompare: documentcompare,
                            numOpenFiles: OpenFiles.length,
                            isPDF: thisdocument.pages[thisdocument.currentpage].usepdfjs,
                            is3D: thisdocument.pages[thisdocument.currentpage].usevector3Dxml,
                            markupOn3D : false,
                            is2D: thisdocument.pages[thisdocument.currentpage].usevectorxml,
                            numpages: thisdocument.pages.length,
                            currentpage: thisdocument.currentpage,
                            activefile: thisdocument.bActive
                        };
                        /*if (RxCore_GUI_State != undefined) {
                            RxCore_GUI_State.setGUIState(stateobj);
                        }*/
                        var pagingobject = {
                            numpages: this.pages.length,
                            currentpage: this.currentpage
                        };
                        if (RxCore_GUI_Page != undefined) {
                            RxCore_GUI_Page.pageEvent(pagingobject);
                        }
                    }
                    this.restorecurpage = this.currentpage;
                };
                this.checkprintready = function () {
                    var allpagesloaded = true;
                    for (var i = 0; i < thisdocument.pages.length; i++) {
                        if (thisdocument.pages[i] != undefined) {
                            if (thisdocument.pages[i].usevector3Dxml) {
                                allpagesloaded = true;
                            }
                            else if (thisdocument.pages[i].usevectorxml) {
                                if (!thisdocument.pages[i].vectorloaded) {
                                    allpagesloaded = false;
                                }
                            }
                            else if (thisdocument.pages[i].usepdfjs) {
                                allpagesloaded = true;
                            }
                            else {
                                if (!thisdocument.pages[i].largeimageloaded || !thisdocument.pages[i].smallimageloaded) {
                                    allpagesloaded = false;
                                }
                            }
                        }
                    }
                    return allpagesloaded;
                };
                this.loadAllPages = function () {
                    //var allpagesloaded = true;
                    thisdocument.bDoPrint = true;
                    for (var i = 0; i < thisdocument.pages.length; i++) {
                        if (thisdocument.pages[i].usevectorxml) {
                            if (!thisdocument.pages[i].vectorloaded) {
                                thisdocument.GotoPage(i);
                                //allpagesloaded = false;
                            }
                        }
                        else if (thisdocument.pages[i].usepdfjs) {
                        }
                        else {
                            if (!thisdocument.pages[i].largeimageloaded || !thisdocument.pages[i].smallimageloaded) {
                                thisdocument.GotoPage(i);
                            }
                        }
                    }
                    thisdocument.GotoPage(0);
                    //return allpagesloaded;
                };
                this.printDocument = function (paper) {
                    var i = 0;

                    /*if (thisdocument.foxitdoc != null){
                        do{
                            //canvaspages[i]
                            canvaspages[i].printobj.setDocSize(thisdocument.pages[0].originalwidth, thisdocument.pages[0].originalheight);
                            canvaspages[i].printobj.setRes(300);
                            canvaspages[i].printobj.setPaperSize(paper.width, paper.height);
                            canvaspages[i].printobj.setScale();
                            canvaspages[i].printobj.print();
                            i++;
                        }while (i < canvaspages.length);

                    }else{
                        
    
                    }*/

                    do {
                            
                        if(thisdocument.pages[i] == undefined){
                            //set drawpages array to true

                            aDrawpages[i] = true;

                        }else{
                            thisdocument.pages[i].printobj.setRes(300);
                            thisdocument.pages[i].printobj.setPaperSize(paper.width, paper.height);
                            thisdocument.pages[i].printobj.setScale();
                            thisdocument.pages[i].printobj.print();

                        }
                        
                        i++;
                    } while (i < thisdocument.pages.length);

                    thisdocument.bDoPrint = false;
                    /*for (var i = 0; i < this.pages.length-1; i++) {
                     this.pages[i].printobj.setRes(300);
                     this.pages[i].printobj.setPaperSize(paper.width,paper.height);
                     this.pages[i].printobj.setScale();
                     this.pages[i].printobj.print();
                     }*/
                };

                this.scrolltestfoxit = function(x,y){

                    if (thisdocument.foxitdoc != null) {
                        
                            //normally not passed
                            
                            thisdocument.foxitdoc.scrollpostest(x,y);
                            
                            //thisdocument.foxitdoc.forceRedraw();

                       
                    }

                };

                this.foxitpagEvent = function (pagenum) {

                    var pagingobject = {
                        numpages: thisdocument.pages.length,
                        currentpage: pagenum - 1
                    };

                    if (thisdocument.currentpage == pagenum - 1){
                        //
                    }

                    thisdocument.currentpage = pagenum - 1;
                    if (thisdocument.currentpage < 0){
                        thisdocument.currentpage = 0;
                    }
                    
                    if (RxCore_GUI_Page != undefined) {
                        RxCore_GUI_Page.pageEvent(pagingobject);
                    }

                };

                this.GotoPage = function (pagenum, nofoxitupate) {
                    /*if (thisdocument.pagelocked){
                        return;
                    }*/
                    if (isNaN(pagenum)) {
                        return;
                    }
                    if (pagenum > thisdocument.pages.length - 1 || pagenum < 0) {
                        return;
                    }
                    if (pagenum == thisdocument.currentpage) {

                        return;
                    }
                    var prevpage = thisdocument.currentpage;
                    thisdocument.prevpage = thisdocument.currentpage;

                    if (thisdocument.pages[thisdocument.currentpage] != undefined) {
                        if (thisdocument.pages[thisdocument.currentpage].usepdfjs) {
                            if (thisdocument.textselect) {
                                thisdocument.pages[thisdocument.currentpage].PDFTextArea.style.display = "none";
                            }
                            thisdocument.pages[thisdocument.currentpage].visible = false;

                        }
                    }
                    thisdocument.setCurrentpage(pagenum);
                    //this.currentpage = pagenum;
                    if (thisdocument.pages[thisdocument.currentpage] == undefined) {
                        return;
                        //thisdocument.movePageObject(this.currentpage);
                        //reuse existing page object
                    }
                    if (thisdocument.pages[thisdocument.currentpage].usevector3Dxml) {
                        if (thisdocument.pages[thisdocument.currentpage].Vector3DPageObj == undefined) {
                            thisdocument.pages[thisdocument.currentpage].loadvectors3DBinary();
                        }
                        else {
                            /*if (this.pages[this.currentpage].VectorPageObj.width == 0 || this.pages[this.currentpage].VectorPageObj.height == 0) {
                             this.currentpage = prevpage;
                             return;
                             }*/
                            thisdocument.restorecurpage = thisdocument.currentpage;
                            thisdocument.SetActive();
                        }
                    }
                    else if (thisdocument.pages[thisdocument.currentpage].usevectorxml) {
                        if (!thisdocument.pages[thisdocument.currentpage].vectorloaded) {
                            thisdocument.pages[thisdocument.currentpage].loadbinvectors();
                        }
                        else {

                            if(thisdocument.pages[thisdocument.currentpage].VectorPageObj != undefined || thisdocument.pages[thisdocument.currentpage].VectorPageObj.invalid){
                                if (thisdocument.pages[thisdocument.currentpage].VectorPageObj.width == 0 || thisdocument.pages[thisdocument.currentpage].VectorPageObj.height == 0) {
                                    thisdocument.setCurrentpage(prevpage);
                                    //this.currentpage = prevpage;
                                    return;
                                }
                                thisdocument.restorecurpage = thisdocument.currentpage;
                                thisdocument.SetActive();
    
                            }

                        }
                        //this.pages[this.currentpage].draw_vector(true);
                    }
                    else if (thisdocument.pages[thisdocument.currentpage].usepdfjs) {
                        //this.pages[this.currentpage].dscalepdf = this.pages[prevpage].dscalepdf;
                        if (thisdocument.bLargePDF) {
                            //this.pages[this.currentpage].dxpdf = this.pages[prevpage].dxpdf;
                            //this.pages[this.currentpage].dypdf = 0;
                            if (!thisdocument.pagelocked) {
                                thisdocument.pages[thisdocument.currentpage].setPFDdimnodraw(thisdocument.pages[0].dxpdf, 0, 1, false);
                            }
                            else {
                                thisdocument.pages[thisdocument.currentpage].dypdf = 0;
                                //thisdocument.pages[this.currentpage].dypdf = 0;
                            }
                        }
                        else {
                            if (!thisdocument.pagelocked) {
                                thisdocument.pages[thisdocument.currentpage].setPFDdimnodraw(thisdocument.pages[0].dxpdf, 0, 1, false);
                            }
                            else {
                                thisdocument.pages[thisdocument.currentpage].dypdf = 0;
                            }
                        }
                        if (!thisdocument.pages[thisdocument.currentpage].pageloaded) {
                            thisdocument.loadpage(thisdocument.currentpage);
                        }
                        thisdocument.pages[thisdocument.currentpage].draw_canvas(true);
                        //shortpgupdateloop();
                        if (!thisdocument.pages[thisdocument.currentpage].iscollapsed) {
                            //thisdocument.pgChangeLoop();
                        }
                        //pgupdateloop();
                        if (thisdocument.bLargePDF) {
                            thisdocument.pages[thisdocument.currentpage].pdfisrendered = false;
                            
                            //console.log('gotopage invalidation');
                        }
                        if (thisdocument.textselect) {
                            thisdocument.pages[thisdocument.currentpage].PDFTextArea.style.display = szdispvalue;
                        }
                        if (thisdocument.foxitdoc != null) {
                            
                            thisdocument.pages[thisdocument.currentpage].rendercallback = true;
                            if (nofoxitupate) {
                                //normally not passed
                                thisdocument.foxitdoc.gotoPage(thisdocument.currentpage);
                                thisdocument.foxitdoc.forceRedraw();

                            }
                            else {
                                thisdocument.foxitdoc.gotoPage(thisdocument.currentpage);
                                

            

                            }

                            thisdocument.pages[prevpage].visible = false;
                            thisdocument.foxitdoc.setmarkupPosition(prevpage);
                            thisdocument.foxitdoc.setmarkupPosition(thisdocument.currentpage);

                        }
                    }
                    else {
                        if (!thisdocument.pages[thisdocument.currentpage].largeimageloaded || !thisdocument.pages[thisdocument.currentpage].smallimageloaded) {
                            if (thisdocument.Type == 0) {
                                thisdocument.pages[thisdocument.currentpage].initialzoom = 2;
                            }
                            thisdocument.pages[thisdocument.currentpage].loadimages();
                        }
                        if (thisdocument.currentpage != 0) {
                            thisdocument.pages[thisdocument.currentpage].dscale = thisdocument.pages[0].dscale;
                            thisdocument.pages[thisdocument.currentpage].dx = thisdocument.pages[0].dx;
                            thisdocument.pages[thisdocument.currentpage].dy = thisdocument.pages[0].dy;
                            thisdocument.pages[thisdocument.currentpage].checkimageswitch();
                            //this.pages[this.currentpage].currentimage = this.pages[this.currentpage-1].currentimage;
                        }
                        thisdocument.pages[thisdocument.currentpage].draw_image(true);
                    }
                    /*if (!this.pages[this.currentpage].usevectorxml){
        
        
        
                     }else{
                     if (!this.pages[this.currentpage].vectorloaded){
                     this.pages[this.currentpage].loadvectors();
                     }
        
                     this.pages[this.currentpage].draw_vector(true);
                     }*/
                    drawmarkupAll(cntximg);




                    var stateobj = {
                        iscompare: documentcompare,
                        numOpenFiles: OpenFiles.length,
                        isPDF: thisdocument.pages[thisdocument.currentpage].usepdfjs,
                        is3D: thisdocument.pages[thisdocument.currentpage].usevector3Dxml,
                        markupOn3D : false,
                        is2D: thisdocument.pages[thisdocument.currentpage].usevectorxml,
                        numpages: thisdocument.pages.length,
                        currentpage: thisdocument.currentpage,
                        activefile: thisdocument.bActive
                    };
                    /*if (RxCore_GUI_State != undefined) {
                        RxCore_GUI_State.setGUIState(stateobj);
                    }*/
                    var pagingobject = {
                        numpages: thisdocument.pages.length,
                        currentpage: thisdocument.currentpage
                    };
                    if (thisdocument.currentpage == thisdocument.pages.length - 1) {
                        //console.log('last page', thisdocument.currentpage);
                        /*if(thisdocument.foxitdoc != null){
                            thisdocument.foxitdoc.addLastPage();
                        }*/
                        //foxitGetDoc().addLastPage();
                    }
                    if (RxCore_GUI_Page != undefined) {
                        RxCore_GUI_Page.pageEvent(pagingobject);
                    }
                    thisdocument.restorecurpage = thisdocument.currentpage;
                };
                /*this.GotoPage = function(pagenum){
                 var prevpage = this.currentpage;
                 this.currentpage = pagenum;
                 if (!this.pages[this.currentpage].usevectorxml){
                 //this.checkimageswitch();
                 if (!this.pages[this.currentpage].largeimageloaded || !this.pages[this.currentpage].smallimageloaded){
                 if (this.Type == 0){
                 this.pages[this.currentpage].initialzoom = 2;
                 }
                 this.pages[this.currentpage].loadimages();
        
                 }
        
                 if (this.currentpage != 0){
                 this.pages[this.currentpage].dscale = this.pages[0].dscale;
                 this.pages[this.currentpage].dx = this.pages[0].dx;
                 this.pages[this.currentpage].dy = this.pages[0].dy;
                 this.pages[this.currentpage].dscaleextent = this.pages[0].dscaleextent;
                 this.pages[this.currentpage].dxextent = this.pages[0].dxextent;
                 this.pages[this.currentpage].dyextent = this.pages[0].dyextent;
                 this.pages[this.currentpage].checkimageswitch();
                 //this.pages[this.currentpage].currentimage = this.pages[this.currentpage-1].currentimage;
                 }
        
                 this.pages[this.currentpage].draw_image();
                 }else{
                 if (!this.pages[this.currentpage].vectorloaded){
                 this.pages[this.currentpage].loadvectors();
                 }
        
                 this.pages[this.currentpage].draw_vector(true);
                 }
        
                 drawmarkupAll(cntximg);
        
        
                 };*/
                this.PageUp = function () {
                    /*if (thisdocument.pagelocked){
                        return;
                    }*/
                    var prevpage = thisdocument.currentpage;
                    if (thisdocument.pages[thisdocument.currentpage].usepdfjs) {
                        if (thisdocument.textselect) {
                            thisdocument.pages[thisdocument.currentpage].PDFTextArea.style.display = "none";
                        }
                    }
                    if (thisdocument.currentpage - 1 >= 0) {
                        thisdocument.setCurrentpage(thisdocument.currentpage - 1);
                        //this.currentpage--;
                        if (thisdocument.pages[thisdocument.currentpage].usevector3Dxml) {
                            if (thisdocument.pages[thisdocument.currentpage].Vector3DPageObj == undefined) {
                                thisdocument.pages[thisdocument.currentpage].loadvectors3DBinary();
                            }
                            else {
                                /*if (this.pages[this.currentpage].VectorPageObj.width == 0 || this.pages[this.currentpage].VectorPageObj.height == 0) {
                                 this.currentpage = prevpage;
                                 return;
                                 }*/
                                //this.pages[this.currentpage].SetActive();
                                thisdocument.restorecurpage = thisdocument.currentpage;
                                thisdocument.SetActive();
                            }
                        }
                        else if (thisdocument.pages[thisdocument.currentpage].usevectorxml) {
                            if (!thisdocument.pages[thisdocument.currentpage].vectorloaded) {
                                thisdocument.pages[thisdocument.currentpage].loadbinvectors();
                            }
                            else {
                                if (thisdocument.pages[thisdocument.currentpage].VectorPageObj.width == 0 || thisdocument.pages[thisdocument.currentpage].VectorPageObj.height == 0) {
                                    thisdocument.setCurrentpage(prevpage);
                                    return;
                                }
                                thisdocument.restorecurpage = thisdocument.currentpage;
                                thisdocument.SetActive();
                                //this.pages[this.currentpage].draw_vector(true);
                            }
                            //this.pages[this.currentpage].draw_vector(true);
                        }
                        else if (thisdocument.pages[thisdocument.currentpage].usepdfjs) {
                            //this.pages[this.currentpage].dscalepdf = this.pages[prevpage].dscalepdf;
                            if (!thisdocument.pagelocked) {
                                thisdocument.pages[thisdocument.currentpage].setPagePos(thisdocument.pages[prevpage].dxpdf, 0);
                                thisdocument.pages[thisdocument.currentpage].dxpdf = thisdocument.pages[prevpage].dxpdf;
                                thisdocument.pages[thisdocument.currentpage].dypdf = 0;
                            }
                            if (!thisdocument.pages[thisdocument.currentpage].pageloaded) {
                                thisdocument.loadpage(thisdocument.currentpage);
                            }
                            thisdocument.pages[thisdocument.currentpage].draw_canvas(true);
                            if (!thisdocument.pages[thisdocument.currentpage].iscollapsed) {
                                //thisdocument.pgChangeLoop();
                            }
                            //shortpgupdateloop();
                            //pgupdateloop();
                            if (thisdocument.textselect) {
                                thisdocument.pages[thisdocument.currentpage].PDFTextArea.style.display = szdispvalue;
                            }
                        }
                        else {
                            if (!thisdocument.pages[thisdocument.currentpage].largeimageloaded || !thisdocument.pages[thisdocument.currentpage].smallimageloaded) {
                                if (thisdocument.Type == 0) {
                                    thisdocument.pages[thisdocument.currentpage].initialzoom = 2;
                                }
                                thisdocument.pages[thisdocument.currentpage].loadimages();
                            }
                            thisdocument.pages[thisdocument.currentpage].checkimageswitch();
                            //this.checkimageswitch();
                            thisdocument.pages[thisdocument.currentpage].draw_image(true);
                        }
                        //this.pages[this.currentpage].draw_image();
                        drawmarkupAll(cntximg);


                        var stateobj = {
                            iscompare: documentcompare,
                            numOpenFiles: OpenFiles.length,
                            isPDF: thisdocument.pages[thisdocument.currentpage].usepdfjs,
                            is3D: thisdocument.pages[thisdocument.currentpage].usevector3Dxml,
                            markupOn3D : false,
                            is2D: thisdocument.pages[thisdocument.currentpage].usevectorxml,
                            numpages: thisdocument.pages.length,
                            currentpage: thisdocument.currentpage,
                            activefile: thisdocument.bActive
                        };
                        /*if (RxCore_GUI_State != undefined) {
                            RxCore_GUI_State.setGUIState(stateobj);
                        }*/
                        var pagingobject = {
                            numpages: this.pages.length,
                            currentpage: this.currentpage
                        };
                        if (RxCore_GUI_Page != undefined) {
                            RxCore_GUI_Page.pageEvent(pagingobject);
                        }
                    }
                    this.restorecurpage = this.currentpage;
                };
                this.getpage = function (pagenum) {
                    return thisdocument.pages[pagenum];
                };
                this.lockMarkupbyGUID = function (GUID, onOff) {
                    var i = 0;
                    for (i = 0; i < this.markuplist.length; i++) {
                        if (this.markuplist[i].uniqueID == GUID) {
                            this.markuplist[i].locked = onOff;
                        }
                    }
                    //return markupobj;
                };
                /*this.getmarkupbyGUIDXML = function (GUID) {
                    var i = 0;
                    //var markupobj = -1;
                    var xmlmarkup = "";
                    for (i = 0; i < this.markuplist.length; i++) {
                        if (this.markuplist[i].uniqueID == GUID) {
                            xmlmarkup = createxmlmarkupentity(this.markuplist[i], thisdocument, false);
                        }
                    }
                    return xmlmarkup;
                };*/
                this.getmarkupbyGUIDXML = function (GUID) {
                    var i = 0;
                    //var markupobj = -1;
                    var xmlmarkup = "";
                    var found = false;
                    for (i = 0; i < this.markuplist.length; i++) {
                        if (this.markuplist[i].uniqueID == GUID) {
                            xmlmarkup = createxmlmarkupentity(this.markuplist[i], thisdocument, false);
                            found = true;
                        }
                    }
                    return {document : xmlmarkup, found : found};
                };
                this.getmarkupSelectedXML = function(markup){
                    //var selectedmarkup = {isempty : true};)
                    var xmlmarkup = undefined;

                    if (markup.isempty) {
                        xmlmarkup = undefined;
                    }else{
                        var xmlmarkup = createxmlmarkupentity(markup, thisdocument, false);
                    }
                    
                    return xmlmarkup;

                };
                this.getmarkupobjbyGUID = function (GUID) {
                    var i = 0;
                    
                    var markupobj = null;

                    for (i = 0; i < this.markuplist.length; i++) {
                        if (this.markuplist[i].uniqueID == GUID && this.markuplist[i].uniqueID != 0) {
                            markupobj = this.markuplist[i];
                        }
                    }
                    return markupobj;
                };

                this.selectmarkupbyGUIDs = function (GUIDs) {   
                    var i = 0;
                    var gi = 0;
                    //var markupobjects = [];
        
                    for (i = 0; i < this.markuplist.length; i++) {
                
                        gi = 0;

                        while (gi < GUIDs.length){

                            if(GUIDs[gi] != undefined){
                                if (this.markuplist[i].uniqueID == GUIDs[gi]) {
                                
                                    this.markuplist[i].selectSet();
                                    //markupobjects.push(i);
    
                                }
    
                            }
                            gi++;
                        }
                        
                        
                        /*for(gi = 0;gi < GUIDs.length;gi++ ){
                            if (this.markuplist[i].uniqueID == GUIDs[gi]) {
                                
                                this.markuplist[i].selectSet();
                                //markupobjects.push(i);

                            }
                        }*/
                    }
                };

                this.unselectmarkupbyGUIDs = function (GUIDs) {
                    var i = 0;
                    var gi = 0;
                    //var markupobjects = [];

                    for (i = 0; i < this.markuplist.length; i++) {

                        gi = 0;

                        while (gi < GUIDs.length){

                            if(GUIDs[gi] != undefined){
                                if (this.markuplist[i].uniqueID == GUIDs[gi]) {
                                
                                    this.markuplist[i].unselect();
                                    //markupobjects.push(i);
    
                                }
    
                            }
                            gi++;
                        }
                
                        /*for(gi = 0;gi < GUIDs.length;gi++ ){
                            if (this.markuplist[i].uniqueID == GUIDs[gi]) {
                                
                                this.markuplist[i].unselect();
                                //markupobjects.push(i);

                                //unselect.


                            }
                        }*/
                    }

                    //return markupobjects;

                };

                this.getannotGUIDs = function(){

                    var i = 0;
                    var gi = 0;
                    var markupobjects = [];

                    for (i = 0; i < this.markuplist.length; i++) {
                
                        markupobjects.push(this.markuplist[i].uniqueID);

                    }

                    return markupobjects;

                };
                this.getmarkupbyGUIDs = function (GUIDs) {
                    var i = 0;
                    var gi = 0;
                    var markupobjects = [];

                    for (i = 0; i < this.markuplist.length; i++) {
                
                        for(gi = 0;gi < GUIDs.length;gi++ ){
                            if (this.markuplist[i].uniqueID == GUIDs[gi]) {
                                markupobjects.push(i);

                            }
                        }
                    }

                    return markupobjects;

                };

                this.getmarkupbyGUID = function (GUID) {
                    var i = 0;
                    var markupobj = -1;
                    for (i = 0; i < this.markuplist.length; i++) {
                        if (this.markuplist[i].uniqueID == GUID) {
                            markupobj = i;
                        }
                    }
                    return markupobj;
                };
                this.getmarkupbynumber = function (markupnumber) {
                    var i = 0;
                    var markupobj = -1;
                    for (i = 0; i < this.markuplist.length; i++) {
                        if (this.markuplist[i].markupnumber == markupnumber) {
                            markupobj = i;
                        }
                    }
                    return markupobj;
                };

                this.getmarkupobjbynumber = function (markupnumber) {
                    var i = 0;
                    var markupobj = null;

                    for (i = 0; i < this.markuplist.length; i++) {
                        if (this.markuplist[i].markupnumber == markupnumber) {
                            markupobj = this.markuplist[i];
                        }
                    }
                    return markupobj;
                };


                this.getmarkupobjbyID = function (ID){

                    var i = 0;
                    var markupobj = null;

                    for (i = 0; i < this.markuplist.length; i++) {
                        if (this.markuplist[i].markupID == ID) {
                            markupobj = this.markuplist[i];
                        }
                    }
                    return markupobj;


                    //markupobj.markupID
                };


                /*this.selectedmarkuprect = function(){
                    var i = 0;
                    var combinedrect = undefined;
        
                    for (i = 0; i < this.markuplist.length; i++) {
                        if (this.markuplist[i].selected && this.markuplist[i].pagenumber == this.currentpage) {
                            var combinedrect = this.markuplist[i].getrect(combinedrect);
                        }
                    }
                };*/
                this.setGroupSelectedMarkup = function (add) {
                    var i = 0;
                    if (!add) {
                        for (i = 0; i < this.markuplist.length; i++) {
                            this.markuplist[i].bispartofGroup = false;
                            this.markuplist[i].groupRect = { x: 0, y: 0, w: 0, h: 0 };
                        }
                        //console.log(thisdocument.markuplist[i-1].groupRect);
                    }
                    else {
                        for (i = 0; i < this.markuplist.length; i++) {
                            var btextarrow = (this.markuplist[i].bhasArrow && this.markuplist[i].type == 9);
                            var barrowtext = (this.markuplist[i].subtype == 6 && this.markuplist[i].type == 6);
                            var btexboxwarrow = (barrowtext || barrowtext);
                            var bseledit = (this.markuplist[i].selectedit && this.markuplist[i].type != 9);
                            var bselected = (this.markuplist[i].selected || bseledit);
                            if (btexboxwarrow && bselected && this.markuplist[i].pagenumber == thisdocument.getcurPage()) {
                                this.markuplist[i].bispartofGroup = true;
                                this.markuplist[i].groupRect = thisdocument.getmarkupGroupRect();
                            }
                        }
                        //console.log(thisdocument.markuplist[i-1].groupRect);
                    }
                };
                this.getNumSelectedMarkup = function () {
                    var i = 0;
                    var numselected = 0;


                    for (i = 0; i < this.markuplist.length; i++) {

                        if( this.markuplist[i].isSelected(thisdocument.getcurPage()) ){
                            numselected++;
                        }

                        /*                         var bseledit = (this.markuplist[i].selectedit && this.markuplist[i].type != 9);
                        var bselected = (this.markuplist[i].selected || bseledit);
                        var btextArrow = (this.markuplist[i].type == 9 && this.markuplist[i].bhasArrow);
                        if (!btextArrow && bselected && this.markuplist[i].pagenumber == thisdocument.getcurPage()) {
                            numselected++;
                        }
                         */                    
                    }                   
                    return numselected;
                };
                this.getMarkupMultiselected = function () {
                    //console.log(bMultiMarkupselected);
                    return bMultiMarkupselected;
                };
                this.setMarkupMultiselected = function () {
                    //console.log(thisdocument.getNumSelectedMarkup());
                    if (thisdocument.getNumSelectedMarkup() > 1) {
                        bMultiMarkupselected = true;
                    }
                    else {
                        bMultiMarkupselected = false;
                    }
                    thisdocument.setGroupSelectedMarkup(bMultiMarkupselected);
                };
                this.getmarkupGroupRect = function () {
                    var i = 0;
                    var counter = 0;
                    var points = [];
                    var minx = 0;
                    var miny = 0;
                    var maxx = 0;
                    var maxy = 0;
                    var mgrouparray = thisdocument.getSelectedMarkup();
                    if (mgrouparray.length > 1) {
                        var brelSize = (mgrouparray[0].type == 3 || mgrouparray[0].type == 4 || mgrouparray[0].type == 5 || mgrouparray[0].type == 9 || mgrouparray[0].type == 10 || mgrouparray[0].type == 11 || mgrouparray[0].type == 12);
                        if (brelSize) {
                            if (mgrouparray[0].rotation != 0) {
                                var rrect = mgrouparray[0].rotrect(mgrouparray[0].xscaled, mgrouparray[0].yscaled, mgrouparray[0].xscaled + mgrouparray[0].wscaled, mgrouparray[0].yscaled + mgrouparray[0].hscaled);
                            }
                            else {
                                rrect = { x: mgrouparray[0].xscaled, y: mgrouparray[0].yscaled, w: mgrouparray[0].wscaled, h: mgrouparray[0].hscaled };
                            }
                            minx = rrect.x;
                            miny = rrect.y;
                            maxx = rrect.x + rrect.w;
                            maxy = rrect.y + rrect.h;
                            if (mgrouparray[0].type == 4 && mgrouparray[0].subtype == 1) {
                                var xdiff = Math.max(mgrouparray[0].xscaled, mgrouparray[0].wscaled) - Math.min(mgrouparray[0].xscaled, mgrouparray[0].wscaled);
                                var ydiff = Math.max(mgrouparray[0].yscaled, mgrouparray[0].hscaled) - Math.min(mgrouparray[0].yscaled, mgrouparray[0].hscaled);
                                var radius = mgrouparray[0].getdiag(xdiff, ydiff);
                                minx = mgrouparray[0].xscaled - radius;
                                miny = mgrouparray[0].yscaled - radius;
                                maxx = mgrouparray[0].xscaled + radius;
                                maxy = mgrouparray[0].yscaled + radius;
                            }
                        }
                        else {
                            minx = mgrouparray[0].xscaled;
                            miny = mgrouparray[0].yscaled;
                            maxx = mgrouparray[0].wscaled;
                            maxy = mgrouparray[0].hscaled;
                        }
                        for (i = 0; i < mgrouparray.length; i++) {
                            var brelSize = (mgrouparray[i].type == 3 || mgrouparray[i].type == 4 || mgrouparray[i].type == 5 || mgrouparray[i].type == 9 || mgrouparray[i].type == 10 || mgrouparray[i].type == 11 || mgrouparray[i].type == 12);
                            if (brelSize) {
                                if (mgrouparray[i].rotation != 0) {
                                    var rrect = mgrouparray[i].rotrect(mgrouparray[i].xscaled, mgrouparray[i].yscaled, mgrouparray[i].xscaled + mgrouparray[i].wscaled, mgrouparray[i].yscaled + mgrouparray[i].hscaled);
                                }
                                else {
                                    rrect = { x: mgrouparray[i].xscaled, y: mgrouparray[i].yscaled, w: mgrouparray[i].wscaled, h: mgrouparray[i].hscaled };
                                }
                                if (mgrouparray[i].type == 4 && mgrouparray[i].subtype == 1) {
                                    var xdiff = Math.max(mgrouparray[i].xscaled, mgrouparray[i].wscaled) - Math.min(mgrouparray[i].xscaled, mgrouparray[i].wscaled);
                                    var ydiff = Math.max(mgrouparray[i].yscaled, mgrouparray[i].hscaled) - Math.min(mgrouparray[i].yscaled, mgrouparray[i].hscaled);
                                    var radius = mgrouparray[i].getdiag(xdiff, ydiff);
                                    points.push({ x: mgrouparray[i].xscaled - radius, y: mgrouparray[i].yscaled - radius });
                                    points.push({ x: mgrouparray[i].xscaled + radius, y: mgrouparray[i].yscaled + radius });
                                }
                                else {
                                    points.push({ x: rrect.x, y: rrect.y });
                                    points.push({ x: rrect.x + rrect.w, y: rrect.y + rrect.h });
                                }
                            }
                            else {
                                points.push({ x: mgrouparray[i].xscaled, y: mgrouparray[i].yscaled });
                                points.push({ x: mgrouparray[i].wscaled, y: mgrouparray[i].hscaled });
                            }
                        }
                        for (counter = 0; counter < points.length; counter++) {
                            if (points[counter].x < minx) {
                                minx = points[counter].x;
                            }
                            if (points[counter].y < miny) {
                                miny = points[counter].y;
                            }
                            if (points[counter].x > maxx) {
                                maxx = points[counter].x;
                            }
                            if (points[counter].y > maxy) {
                                maxy = points[counter].y;
                            }
                        }
                    }
                    return { x: minx, y: miny, w: maxx, h: maxy };
                };
                this.getSelectedMarkup = function () {
                    var i = 0;
                    const allMarkupsSelected = [];
                    for (i = 0; i < this.markuplist.length; i++) {
                        var bseledit = (this.markuplist[i].selectedit && this.markuplist[i].type != 9);
                        var bselected = (this.markuplist[i].selected || bseledit);
                        if (bselected && this.markuplist[i].pagenumber == thisdocument.getcurPage()) {
                            if (this.markuplist[i].bhasArrow && this.markuplist[i].markupArrowConnected != null) {
                                allMarkupsSelected.push(this.markuplist[i].markupArrowConnected);
                            }
                            else {
                                allMarkupsSelected.push(this.markuplist[i]);
                            }
                        }
                    }
                    return allMarkupsSelected;
                };
                this.moveselected = function (xdiff, ydiff) {
                    var i = 0;
                    for (i = 0; i < this.markuplist.length; i++) {
                        var bseledit = (this.markuplist[i].selectedit && this.markuplist[i].type != 9);
                        var bselected = (this.markuplist[i].selected || bseledit);
                        if (bselected && this.markuplist[i].pagenumber == thisdocument.getcurPage()) {
                            this.markuplist[i].move(xdiff, ydiff);
                        }
                    }
                };
                this.draw_mpage = function () {
                    var ty = (canvasoheight / 2);
                    switch (thisdocument.pages[this.currentpage].currentimage) {
                        case 0:
                            if (thisdocument.pages[this.currentpage].dy > ty && this.currentpage > 0) {
                                thisdocument.setCurrentpage(this.currentpage - 1);
                                //this.currentpage -= 1;
                            }
                            else if (thisdocument.pages[this.currentpage].endy < ty && this.currentpage < this.NumPages - 1) {
                                thisdocument.setCurrentpage(this.currentpage + 1);
                                //this.currentpage += 1;
                            }
                            thisdocument.pages[this.currentpage].draw_image(true);
                            if (thisdocument.pages[this.currentpage].endy < canvasoheight && this.currentpage < this.NumPages - 1) {
                                if (thisdocument.pages[this.currentpage + 1].largeimageloaded) {
                                    thisdocument.pages[this.currentpage + 1].dy = thisdocument.pages[this.currentpage].endy + 10;
                                    thisdocument.pages[this.currentpage + 1].dx = thisdocument.pages[this.currentpage].dx;
                                    thisdocument.pages[this.currentpage + 1].dscale = thisdocument.pages[this.currentpage].dscale;
                                    thisdocument.pages[this.currentpage + 1].draw_image(false);
                                }
                                else {
                                    this.loadpage(this.currentpage + 1);
                                    thisdocument.setCurrentpage(this.currentpage + 1);
                                    //this.currentpage += 1;
                                }
                            }
                            else if (thisdocument.pages[this.currentpage].dy > 10 && this.currentpage > 0) {
                                if (thisdocument.pages[this.currentpage - 1].largeimageloaded) {
                                    thisdocument.pages[this.currentpage - 1].dy = thisdocument.pages[this.currentpage].dy - (thisdocument.pages[this.currentpage].MainImageHeight * thisdocument.pages[this.currentpage].dscale) - 10;
                                    thisdocument.pages[this.currentpage - 1].dx = thisdocument.pages[this.currentpage].dx;
                                    thisdocument.pages[this.currentpage - 1].dscale = thisdocument.pages[this.currentpage].dscale;
                                    thisdocument.pages[this.currentpage - 1].draw_image(false);
                                }
                                else {
                                    this.loadpage(this.currentpage - 1);
                                    thisdocument.setCurrentpage(this.currentpage - 1);
                                    //this.currentpage -= 1;
                                }
                            }
                            /*else if(thispage.dy > ty && thispage.pagenumber > 0){
                             DocObj.currentpage = thispage.pagenumber - 1;
                             }else if(thispage.dy < ty && thispage.pagenumber < DocObj.NumPages){
                             DocObj.currentpage = thispage.pagenumber + 1;
                             }*/
                            break;
                        case 1:
                            //this.pages[this.currentpage].draw_image(true);
                            if (thisdocument.pages[this.currentpage].dyextent > ty && this.currentpage > 0) {
                                thisdocument.setCurrentpage(this.currentpage - 1);
                                //this.currentpage -= 1;
                            }
                            else if (thisdocument.pages[this.currentpage].endy < ty && this.currentpage < this.NumPages - 1) {
                                thisdocument.setCurrentpage(this.currentpage + 1);
                                //this.currentpage += 1;
                            }
                            thisdocument.pages[this.currentpage].draw_image(true);
                            if (thisdocument.pages[this.currentpage].endy < canvasoheight && this.currentpage < this.NumPages - 1) {
                                if (thisdocument.pages[this.currentpage + 1].smallimageloaded) {
                                    thisdocument.pages[this.currentpage + 1].dy = thisdocument.pages[this.currentpage].endy + 10;
                                    thisdocument.pages[this.currentpage + 1].dx = thisdocument.pages[this.currentpage].dx;
                                    thisdocument.pages[this.currentpage + 1].dscale = thisdocument.pages[this.currentpage].dscale;
                                    thisdocument.pages[this.currentpage + 1].draw_image(false);
                                }
                                else {
                                    this.loadpage(this.currentpage + 1);
                                    thisdocument.setCurrentpage(this.currentpage + 1);
                                    //this.currentpage += 1;
                                }
                            }
                            else if (thisdocument.pages[this.currentpage].dyextent > 10 && this.currentpage > 0) {
                                if (thisdocument.pages[this.currentpage - 1].smallimageloaded) {
                                    thisdocument.pages[this.currentpage - 1].dy = thisdocument.pages[this.currentpage].dy - (thisdocument.pages[this.currentpage].MainImageHeight * thisdocument.pages[this.currentpage].dscale) - 10;
                                    thisdocument.pages[this.currentpage - 1].dx = thisdocument.pages[this.currentpage].dx;
                                    thisdocument.pages[this.currentpage - 1].dscale = thisdocument.pages[this.currentpage].dscale;
                                    thisdocument.pages[this.currentpage - 1].draw_image(false);
                                }
                                else {
                                    this.loadpage(this.currentpage - 1);
                                    thisdocument.setCurrentpage(this.currentpage - 1);
                                    //this.currentpage -= 1;
                                }
                            }
                            break;
                    }
                    var stateobj = {
                        iscompare: documentcompare,
                        numOpenFiles: OpenFiles.length,
                        isPDF: thisdocument.pages[this.currentpage].usepdfjs,
                        is3D: thisdocument.pages[this.currentpage].usevector3Dxml,
                        markupOn3D : false,
                        is2D: thisdocument.pages[this.currentpage].usevectorxml,
                        numpages: thisdocument.pages.length,
                        currentpage: thisdocument.currentpage,
                        activefile: thisdocument.bActive
                    };
                    this.restorecurpage = this.currentpage;
                    /*if (RxCore_GUI_State != undefined) {
                        RxCore_GUI_State.setGUIState(stateobj);
                    }*/
                    var pagingobject = {
                        numpages: this.pages.length,
                        currentpage: this.currentpage
                    };
                    if (RxCore_GUI_Page != undefined) {
                        RxCore_GUI_Page.pageEvent(pagingobject);
                    }
                };
                /*this.updateadjpages = function () {
                    if (this.currentpage == 0) {
                        if (!this.pages[this.currentpage + 1].pdfisrendered) {
                            this.pages[this.currentpage + 1].queRenderPageScaled();
                        }
        
                    }
        
                    if (this.currentpage == this.NumPages - 1) {
                        if (!this.pages[this.currentpage - 1].pdfisrendered) {
                            this.pages[this.currentpage - 1].queRenderPageScaled();
                        }
        
                    }
        
                    if (this.currentpage > 0 && this.currentpage < this.NumPages - 1) {
                        if (!this.pages[this.currentpage + 1].pdfisrendered) {
                            this.pages[this.currentpage + 1].queRenderPageScaled();
                        }
                        if (!this.pages[this.currentpage - 1].pdfisrendered) {
                            this.pages[this.currentpage - 1].queRenderPageScaled();
                        }
        
                    }
        
                };*/
                this.get_visible_pages = function () {
                    var ty = (canvasoheight / 2);
                };
                function checkpagestate(newpagenum) {
                    //no page object in posistion.
                    var bpgeundefined = (thisdocument.pages[newpagenum] == undefined);
                    if (bpgeundefined) {
                        var pageloaded = false;
                        var pdfdiminitset = false;
                    }
                    else {
                        pageloaded = thisdocument.pages[newpagenum].pageloaded;
                        pdfdiminitset = thisdocument.pages[newpagenum].pdfdiminitset;
                    }
                    return { havepage: !bpgeundefined, loaded: pageloaded, pdfdimset: pdfdiminitset };
                }
                function drawdummypagesabove(dx, dy, w, h, curpagenum, lastpage) {
                    /*var pgedbpos = getpagepos(dytemppdf, dytemppdf + heighttemppdf, thisdocument.currentpage - 1, thisdocument.pages.length - 1);
                    if(pgedbpos.bt){
                        drawdummypage(dxtemppdf, (dytemppdf + heighttemppdf) - 10, widthtemppdf, heighttemppdf);
                    }*/
                    //drawdummypage(dx, dy, w, h);
                    var nxtpage = 1;
                    var pagesep = 10;
                    var curpage = curpagenum;
                    var visiblepage = lastpage;
                    var pagestart = dy;
                    var pageend = pagestart + h;
                    //var pagestart = (dy - pagesep) - h;
                    //var pageend = pagestart + h;
                    //var curpage = curpagenum - nxtpage;
                    //var lastpage = lastpage;
                    //var pagepos = getpagepos(pagestart, pageend,curpage, lastpage);
                    while (curpage >= 0) {
                        if (pageend > pagesep) {
                            //drawdummypage(dx, pagestart, w, h);
                            visiblepage = curpage;
                        }
                        curpage = curpage - nxtpage;
                        pagestart = (pagestart - pagesep) - h;
                        pageend = pagestart + h;
                        //pageend = pagestart + h;
                        //var pagepos = getpagepos(pagestart, pageend,curpage, lastpage);
                    }
                    return visiblepage;
                }
                function drawdummypagesbelow(dx, dy, w, h, curpagenum, lastpage) {
                    //var pgedapos = getpagepos(dytemppdf, dytemppdf + heighttemppdf, thisdocument.currentpage + 1, thisdocument.pages.length - 1);
                    //if(pgedapos.ab){
                    //drawdummypage(dxtemppdf, dytemppdf + heighttemppdf + 10, widthtemppdf, heighttemppdf);
                    //}
                    //drawdummypage(dx, dy, w, h);
                    var pagestart = dy;
                    var curpage = curpagenum;
                    var visiblepage = 0;
                    //console.log('dummypage', pagestart);
                    var nxtpage = 1;
                    var pagesep = 10;
                    //var pagestart = dy + h + pagesep;
                    //var pageend = pagestart + h;
                    //var curpage = curpagenum + nxtpage;
                    //var lastpage = lastpage;
                    //var pagepos = getpagepos(pagestart, pageend,curpage, lastpage);
                    while (curpage <= lastpage) {
                        if (pagestart < canvasoheight) {
                            //drawdummypage(dx, pagestart, w, h);
                            //var curpos =  getpagepos(pagestart, pagestart + h, curpage, lastpage);
                            visiblepage = curpage;
                        }
                        curpage = curpage + nxtpage;
                        pagestart = pagestart + h + pagesep;
                        //pageend = pagestart + h;
                        //var pagepos = getpagepos(pagestart, pageend,curpage, lastpage);
                    }
                    return visiblepage;
                }
                function getpagepos(pagestart, pageend, curpagenum, lastpage) {
                    var ty = (canvasoheightsize / 2);
                    
                    
                    var bBelowTop = false;
                    var bBelowCenter = false;
                    var bAboveBottom = false;
                    var bAboveCenter = false;

                    var pixelRatio = getPixelRatio();
                    pagestart = pagestart * pixelRatio;
                    pageend = pageend * pixelRatio;

                    //var canvasowidthsize = 0;
                    //var canvasoheightsize = 0;
                

                    //page below top = prev page visible
                    bBelowTop = (pagestart > 10 && curpagenum > 0);
                    //page below center switch to previous page
                    bBelowCenter = (pagestart - 10 > ty && curpagenum > 0);
                    //page above bottom = next page visible
                    bAboveBottom = (pageend < canvasoheightsize && curpagenum < lastpage);
                    //page above center switch to next page
                    bAboveCenter = (pageend + 10 < ty && curpagenum < lastpage);
                    return { bt: bBelowTop, bc: bBelowCenter, ab: bAboveBottom, ac: bAboveCenter };
                }
                function pagechange(pagepos, curpagenum) {
                    var newpage = curpagenum;
                    if (pagepos.bc) {
                        newpage -= 1;
                    }
                    if (pagepos.ac) {
                        newpage += 1;
                    }
                    return newpage;
                }
                function drawdummypage(dx, dy, w, h) {
                    contexto.fillStyle = "rgb(255,255,255)"; //contexto.fillStyle = "rgb(238,243,250)";
                    contexto.fillRect(dx, dy, w, h);
                }
                this.draw_mpagepdf = function (allowpaging) {
                    if (!thisdocument.bActive || documentcompare) {
                        return;
                    }
                    //var ty = (canvasoheight / 2);
                    //console.log(this.currentpage);
                    var curpage = this.currentpage;
                    this.restorecurpage = this.currentpage;
                    if (thisdocument.pages[this.currentpage] == undefined) {
                        return;
                    }
                    
                    var pgendpos = thisdocument.pages[this.currentpage].dypdf + (thisdocument.pages[this.currentpage].offscreenheight * thisdocument.pages[this.currentpage].dscalepdf);
                    var pgstartpos = thisdocument.pages[this.currentpage].dypdf;

                    var dytemppdf = thisdocument.pages[this.currentpage].dypdf - (thisdocument.pages[0].offscreenheight * thisdocument.pages[0].dscalepdf) - 10;
                    var dxtemppdf = thisdocument.pages[0].dxpdf;
                    var widthtemppdf = thisdocument.pages[0].offscreenwidth * thisdocument.pages[0].dscalepdf;
                    var heighttemppdf = thisdocument.pages[0].offscreenheight * thisdocument.pages[0].dscalepdf;
                    var pgepos = getpagepos(pgstartpos, pgendpos, thisdocument.currentpage, thisdocument.pages.length - 1);
                    var setpage = pagechange(pgepos, this.currentpage);
                    /*if (setpage != this.currentpage){
                        var pagestate = checkpagestate(setpage);
                        //return {undefined :  bpgeundefined, loaded : pageloaded, pdfdimset : pdfdiminitset};
        
                    }*/
                    //function pagechange(pagepos, curpagenum){
                    //function pagepos(pagestart, pageend, curpagenum, lastpage){
                    if (thisdocument.pages.length > 1 && !thisdocument.pagelocked) {
                        //return {undefined :  bpgeundefined, loaded : pageloaded, pdfdimset : pdfdiminitset};
                        thisdocument.pages[this.currentpage].draw_canvas(true);
                        //var pgendpos = thisdocument.pages[this.currentpage].dypdf + (thisdocument.pages[this.currentpage].offscreenheight * thisdocument.pages[this.currentpage].dscalepdf);
                        //return {bt :  bBelowTop, bc : bBelowCenter, ab : bAboveBottom, ac : bAboveCenter};
                        if (pgepos.ab) {
                            pagestate = checkpagestate(this.currentpage + 1);
                            //return {havepage :  !bpgeundefined, loaded : pageloaded, pdfdimset : pdfdiminitset};
                            dytemppdf = thisdocument.pages[this.currentpage].dypdf + heighttemppdf + 10;
                            //var heighttemppdf = thisdocument.pages[0].offscreenheight * thisdocument.pages[0].dscalepdf;
                            var visiblepage = drawdummypagesbelow(dxtemppdf, dytemppdf, widthtemppdf, heighttemppdf, thisdocument.currentpage + 1, thisdocument.pages.length - 1);
                            if (pagestate.havepage) {
                                if (pagestate.loaded && pagestate.pdfdimset) {
                                    thisdocument.pages[this.currentpage + 1].setPagePosBelow(thisdocument.pages[this.currentpage]);
                                    thisdocument.pages[this.currentpage + 1].draw_canvas(false);
                                }
                                else {
                                    //drawdummypage(dxtemppdf, dytemppdf, widthtemppdf, heighttemppdf);
                                    //drawdummypagesbelow(dxtemppdf, dytemppdf, widthtemppdf, heighttemppdf, thisdocument.currentpage + 1, thisdocument.pages.length - 1);
                                    /*var pgedapos = getpagepos(dytemppdf, dytemppdf + heighttemppdf, thisdocument.currentpage + 1, thisdocument.pages.length - 1);
                                    if(pgedapos.ab){
                                        drawdummypage(dxtemppdf, dytemppdf + heighttemppdf + 10, widthtemppdf, heighttemppdf);
                                    }*/
                                    if (thisdocument.foxitdoc != null) {
                                        if (visiblepage > (this.currentpage + 1)) {
                                            //thisdocument.foxitdoc.gotoPage(visiblepage);
                                            //setpage = visiblepage;
                                        }
                                        else {
                                            //thisdocument.foxitdoc.gotoPage(this.currentpage + 1);
                                        }
                                    }
                                }
                            }
                            else {
                                //drawdummypage(dxtemppdf, dytemppdf, widthtemppdf, heighttemppdf);
                                //drawdummypagesbelow(dxtemppdf, dytemppdf, widthtemppdf, heighttemppdf, thisdocument.currentpage + 1, thisdocument.pages.length - 1);
                                /*pgedapos = getpagepos(dytemppdf, dytemppdf + heighttemppdf, thisdocument.currentpage + 1, thisdocument.pages.length - 1);
        
                                if(pgedapos.ab){
                                    drawdummypage(dxtemppdf, dytemppdf + heighttemppdf, widthtemppdf, heighttemppdf);
                                }*/
                                if (thisdocument.foxitdoc != null) {
                                    if (visiblepage > (this.currentpage + 1)) {
                                        //thisdocument.foxitdoc.gotoPage(visiblepage);
                                        //setpage = visiblepage;
                                    }
                                    else {
                                        //thisdocument.foxitdoc.gotoPage((this.currentpage + 1));
                                    }
                                }
                            }
                        }
                        else if (pgepos.bt) {
                            pagestate = checkpagestate(this.currentpage - 1);
                            dytemppdf = (thisdocument.pages[this.currentpage].dypdf - 10) - heighttemppdf;
                            visiblepage = drawdummypagesabove(dxtemppdf, dytemppdf, widthtemppdf, heighttemppdf, thisdocument.currentpage - 1, thisdocument.pages.length - 1);
                            if (pagestate.havepage) {
                                if (pagestate.loaded && pagestate.pdfdimset) {
                                    thisdocument.pages[this.currentpage - 1].setPagePosAbove(thisdocument.pages[this.currentpage]);
                                    thisdocument.pages[this.currentpage - 1].draw_canvas(false);
                                }
                                else {
                                    //drawdummypage(dxtemppdf, dytemppdf, widthtemppdf, heighttemppdf);
                                    //drawdummypagesabove(dxtemppdf, dytemppdf, widthtemppdf, heighttemppdf, thisdocument.currentpage - 1, thisdocument.pages.length - 1);
                                    /*var pgedbpos = getpagepos(dytemppdf, dytemppdf + heighttemppdf, thisdocument.currentpage - 1, thisdocument.pages.length - 1);
                                    if(pgedbpos.bt){
                                        drawdummypage(dxtemppdf, (dytemppdf + heighttemppdf) - 10, widthtemppdf, heighttemppdf);
                                    }*/
                                    if (thisdocument.foxitdoc != null) {
                                        //thisdocument.foxitdoc.gotoPage(this.currentpage - 1);
                                        if (visiblepage < (this.currentpage - 1)) {
                                            //thisdocument.foxitdoc.gotoPage(visiblepage);
                                            //setpage = visiblepage;
                                        }
                                        else {
                                            //thisdocument.foxitdoc.gotoPage((this.currentpage - 1));
                                        }
                                    }
                                }
                            }
                            else {
                                //drawdummypage(dxtemppdf, dytemppdf, widthtemppdf, heighttemppdf);
                                //drawdummypagesabove(dxtemppdf, dytemppdf, widthtemppdf, heighttemppdf, thisdocument.currentpage - 1, thisdocument.pages.length - 1);
                                /*pgedbpos = getpagepos(dytemppdf, dytemppdf + heighttemppdf, thisdocument.currentpage - 1, thisdocument.pages.length - 1);
        
                                if(pgedbpos.bt){
                                    drawdummypage(dxtemppdf, (dytemppdf + heighttemppdf) - 10, widthtemppdf, heighttemppdf);
                                }*/
                                if (thisdocument.foxitdoc != null) {
                                    //thisdocument.foxitdoc.gotoPage(this.currentpage - 1);
                                    if (visiblepage < (this.currentpage - 1)) {
                                        //thisdocument.foxitdoc.gotoPage(visiblepage);
                                        //setpage = visiblepage;
                                    }
                                    else {
                                        //thisdocument.foxitdoc.gotoPage((this.currentpage - 1));
                                    }
                                }
                            }
                        }
                        if (setpage != this.currentpage) {
                            var pagestate = checkpagestate(setpage);
                            //return {havepage :  !bpgeundefined, loaded : pageloaded, pdfdimset : pdfdiminitset};
                            if (allowpaging) {
                                if (pagestate.havepage) {
                                    if (pagestate.loaded && pagestate.pdfdimset) {
                                        thisdocument.setCurrentpage(setpage);
                                    }
                                    else {
                                        //thisdocument.setCurrentpage(setpage);
                                        //drawdummypage(dxtemppdf, dytemppdf, widthtemppdf, heighttemppdf);
                                    }
                                }
                            }
                        }
                        /*if(curpage != this.currentpage){
                            
                            if(this.foxitdoc != null){
                                this.foxitdoc.gotoPage(this.currentpage);
                                //foxitGotoPage(this.currentpage + 1);
                            }
                            //this.pagechange();
                            //shortpgupdateloop();
                            //pgupdateloop();
                        }*/
                    }
                    else {
                        thisdocument.pages[this.currentpage].draw_canvas(true);
                    }
                    if (this.textselect) {
                        thisdocument.pages[this.currentpage].PDFTextArea.style.display = szdispvalue;
                    }
                    /*var stateobj = {
                        iscompare:documentcompare,
                        numOpenFiles:OpenFiles.length,
                        isPDF:thisdocument.pages[this.currentpage].usepdfjs,
                        is3D:thisdocument.pages[this.currentpage].usevector3Dxml,
                        is2D:thisdocument.pages[this.currentpage].usevectorxml,
                        numpages:thisdocument.pages.length,
                        currentpage:thisdocument.currentpage,
                        activefile : thisdocument.bActive
                    };*/
                    /*if (RxCore_GUI_State != undefined) {
                        RxCore_GUI_State.setGUIState(stateobj);
                    }*/
                    var pagingobject = {
                        numpages: this.pages.length,
                        currentpage: this.currentpage
                    };
                    if (this.currentpage == this.pages.length - 1) {
                        /*if(thisdocument.foxitdoc != null){
                            thisdocument.foxitdoc.addLastPage();
                        }*/
                        //foxitGetDoc().addLastPage();
                    }
                    if (RxCore_GUI_Page != undefined) {
                        if (curpage != this.currentpage) {
                            RxCore_GUI_Page.pageEvent(pagingobject);
                        }
                    }
                };
                this.Suspend = function () {
                    if (thisdocument.pages[thisdocument.currentpage].usevector3Dxml) {
                        thisdocument.pages[thisdocument.currentpage].clock.stop();
                        renderer.clippingPlanes = Empty;
                        renderer.domElement.style.visibility = 'hidden';
                        bAnimateready = false;
                        if (RxCore_GUI_3DParts != undefined) {
                            RxCore_GUI_3DParts.isupdate = false;
                            RxCore_GUI_3DParts.set3DParts([]);
                        }
                    }
                };
                this.forcepagesState = function (numpages) {
                    if (OpenFiles.length == 0) {
                        var openFiles = 1;
                    }
                    else {
                        openFiles = OpenFiles.length;
                    }
                    var stateobj = {
                        iscompare: documentcompare,
                        numOpenFiles: openFiles,
                        isPDF: true,
                        is3D: thisdocument.pages[thisdocument.currentpage].usevector3Dxml,
                        markupOn3D : false,
                        is2D: thisdocument.pages[thisdocument.currentpage].usevectorxml,
                        numpages: numpages,
                        currentpage: thisdocument.currentpage,
                        activefile: thisdocument.bActive,
                        disableMenu: thisdocument.disableMenu || false,
                        source: 'forcepagestateDoc'
                    };
                    if (RxCore_GUI_State != undefined) {
                        RxCore_GUI_State.setGUIState(stateobj);
                    }
                };
                this.SetActive = function (ref) {
                    if (thisdocument.bSopenSilent) {
                        return;
                    }
                    var numstatepages = thisdocument.pages.length;
                    var compare = false;
                    thisdocument.setCurrentpage(thisdocument.restorecurpage);
                    //thisdocument.currentpage = thisdocument.restorecurpage;
                    if (thisdocument.pages.length == 0) {
                        return false;
                    }
                    if (ref) {
                        if (ref.Type == 'Compare') {
                            compare = true;
                            this.disableMenu = true;
                            this.Drawmarkup = false;
                            thisdocument.restorecurpage = thisdocument.currentpage;
                            thisdocument.setCurrentpage(ref.currentpage);
                            //thisdocument.currentpage = ref.currentpage;
                        }
                    }
                    //backgroundColor = this.backgroundColor;
                    this.bActive = true;
                    renderer.clippingPlanes = Empty;
                    renderer.domElement.style.visibility = 'hidden';
                    bAnimateready = false;

                    documentcompare = compare;

                    
                    if (thisdocument.pages[thisdocument.currentpage].usevectorxml) {
                        setSmoothingEnabled(false);
                        if (!compare && thisdocument.pages[thisdocument.currentpage].VectorPageObj) {
                            thisdocument.pages[thisdocument.currentpage].restoreScaleAndOffset();
                            thisdocument.pages[thisdocument.currentpage].VectorPageObj.setActive();
                            /*if (RxCore_GUI_VectorLayers != undefined) {
                                RxCore_GUI_VectorLayers.setVectorLayers(thisdocument.pages[thisdocument.currentpage].VectorPageObj.layerlist);
                            }
                            if (RxCore_GUI_VectorBlocks != undefined) {
                                RxCore_GUI_VectorBlocks.setVectorBlocks(thisdocument.pages[thisdocument.currentpage].VectorPageObj.blocklist);
                            }*/
                            for (var p = 0; p < thisdocument.pages.length; p++) {
                                thisdocument.pages[p].usedincomposite = false;
                                if (thisdocument.pages[p].VectorPageObj) {
                                    thisdocument.pages[p].VectorPageObj.docompare = false;
                                }
                            }

                            if(this.bcompare){
                                this.disableMenu = true;    
                            }else{
                                this.disableMenu = false;
                            }

                            this.setactive = false;
                            this.Drawmarkup = true;
                            thisdocument.pages[thisdocument.currentpage].draw_vector(true);
                        }
                        // contexto.imageSmoothingEnabled = false;
                        // contexto.mozImageSmoothingEnabled = false;
                        // //contexto.webkitImageSmoothingEnabled = false;
                        // contexto.msImageSmoothingEnabled = false;
                        drawmarkupAll(cntximg);



                        RxCore_default();
                    }
                    else if (thisdocument.pages[thisdocument.currentpage].usevector3Dxml) {
                        thisdocument.pages[thisdocument.currentpage].clock.start();
                        renderer.domElement.style.visibility = 'visible';
                        //check if this is needed.
                        documentcompare = false;
                        if (thisdocument.pages[thisdocument.currentpage].Vector3DPageObj.clippingOn) {
                            renderer.clippingPlanes = thisdocument.pages[thisdocument.currentpage].Vector3DPageObj.globalPlanes;
                        }
                        else {
                            renderer.clippingPlanes = Empty;
                        }
                        bAnimateready = true;
                        if (RxCore_GUI_3DParts != undefined) {
                            RxCore_GUI_3DParts.isupdate = false;
                            //RxCore_GUI_3DParts.set3DParts(thisdocument.pages[thisdocument.currentpage].Vector3DPageObj.blocklist);

                            //use new navlist instead.
                            RxCore_GUI_3DParts.set3DParts(thisdocument.pages[thisdocument.currentpage].Vector3DPageObj.navlist);
                        }
                        switch (thisdocument.curcontrol3D) {
                            case 'orbitControl':
                                //RxCore_3DOrbit();
                                if (tool == undefined) {
                                    set_tool('orbitControl', {});
                                    //tool = new tools['orbitControl']();
                                    thisdocument.curcontrol3D = 'orbitControl';
                                }
                                else {
                                    set_tool('orbitControl', {});
                                    //tool = new tools['orbitControl']();
                                    thisdocument.curcontrol3D = 'orbitControl';
                                }
                                if (RxCore_GUI_3DWalkthrough != undefined) {
                                    if(bIsmobile){
                                        RxCore_GUI_3DWalkthrough.setWalkthroughGUI(false);
                                    }
                        

                                    
                                }
                                break;
                            case 'Walkthrough3D':
                                if (tool == undefined) {
                                }
                                set_tool('Walkthrough3D', {});
                                //tool = new tools['Walkthrough3D']();
                                thisdocument.curcontrol3D = 'Walkthrough3D';
                                if (RxCore_GUI_3DWalkthrough != undefined) {
                                    if(bIsmobile){
                                        RxCore_GUI_3DWalkthrough.setWalkthroughGUI(true);
                                    }
                                    
                                }
                                //RxCore_3DWalkThrough(true);
                                break;
                        }
                        //var toolType = thisdocument.curcontrol3D;
                        //tool = new tools[toolType]();
                    }
                    else if (thisdocument.pages[thisdocument.currentpage].usepdfjs) {
                        setSmoothingEnabled(false);
                        // contexto.imageSmoothingEnabled = false;
                        // contexto.mozImageSmoothingEnabled = false;
                        // //contexto.webkitImageSmoothingEnabled = false;
                        // contexto.msImageSmoothingEnabled = false;

                        if (thisdocument.foxitdoc != null) {

                            if(thisdocument.foxitdoc.fileOpen){
                                thisdocument.foxitdoc.getLayerNodes();
                                //thisdocument.foxitdoc.getBookmarks();
                            }

                        }

                        if (!compare) {
                            thisdocument.pages[thisdocument.currentpage].restoreScaleAndOffset();
                            if (!bUsequickPageload) {
                                thisdocument.pages[thisdocument.currentpage].draw_canvas(true);
                                bUsequickPageload = false;
                            }
                            this.disableMenu = false;
                            this.Drawmarkup = true;
                            drawmarkupAll(cntximg);
    
                        }
                        //thisdocument.pages[thisdocument.currentpage].draw_canvas(true);
                        //drawmarkupAll(cntximg);
                        RxCore_default();
                    }
                    else {
                        if(!thisdocument.pages[thisdocument.currentpage].usevectorbinarycompare){
                            setSmoothingEnabled(true);
                            if (!compare) {
                                thisdocument.pages[thisdocument.currentpage].restoreScaleAndOffset();
                                //thisdocument.pages[thisdocument.currentpage].resetimage();
                                this.disableMenu = false;
                                this.Drawmarkup = true;
                                drawmarkupAll(cntximg);
                            }
                            thisdocument.pages[thisdocument.currentpage].draw_image(true);
                            RxCore_default();
                        }

                    }

                    var compare = thisdocument.bcompare;
                    thisdocument.disableMenu = thisdocument.bcompare;

                    if (RxCore_GUI_Markuplist != undefined) {


                        RxCore_GUI_Markuplist.setMarkupList(thisdocument.getmarkuplist(), thisdocument.bActive);
                    }
                    if (RxCore_GUI_pagethumbs != undefined) {
                        RxCore_GUI_pagethumbs.setThumbnails(thisdocument.thumbnails);
                    }
                    if (thisdocument.foxitdoc != null) {
                        if (thisdocument.pages.length != thisdocument.foxitdoc.numpages) {
                            numstatepages = thisdocument.foxitdoc.numpages;
                        }
                    }
                    var stateobj = {
                        iscompare: documentcompare,
                        numOpenFiles: OpenFiles.length,
                        isPDF: thisdocument.pages[this.currentpage].usepdfjs,
                        is3D: thisdocument.pages[this.currentpage].usevector3Dxml,
                        markupOn3D : false,
                        is2D: thisdocument.pages[this.currentpage].usevectorxml,
                        numpages: numstatepages,
                        currentpage: thisdocument.currentpage,
                        activefile: thisdocument.bActive,
                        disableMenu: thisdocument.disableMenu || false,
                        source: 'setActiveDocument'
                    };
                    if (RxCore_GUI_State != undefined) {
                        RxCore_GUI_State.setGUIState(stateobj);
                    }
                    if (RxCore_GUI_HasText != undefined) {
                        RxCore_GUI_HasText.hastext = thisdocument.hastext;
                        RxCore_GUI_HasText.hasText(thisdocument.hastext, thisdocument.bActive);
                    }
                    var pagingobject = {
                        numpages: this.pages.length,
                        currentpage: this.currentpage
                    };
                    if (RxCore_GUI_Page != undefined) {
                        RxCore_GUI_Page.pageEvent(pagingobject);
                    }
                    return true;
                };
                this.enableTextSelect = function (onoff) {
                    thisdocument.textselect = onoff;
                    var curpage = 0;
                    while (curpage < thisdocument.pages.length) {
                        if (thisdocument.pages[curpage] != undefined) {
                            if (thisdocument.pages[curpage].usepdfjs) {
                                if (!onoff) {
                                    //turn visibilty off for all page text divs.
                                    thisdocument.pages[curpage].PDFTextArea.style.display = "none";
                                }
                            }
                            if (thisdocument.pages[curpage].usevectorxml) {
                                if (!onoff) {

                                    if(!thisdocument.pages[thisdocument.currentpage].usevectorbinarycompare){
                                        thisdocument.pages[curpage].TextSelectArea.style.display = "none";
                                    }
                                    
                                }
                            }
                        }
                        curpage++;
                    }
                    if (onoff) {
                        if (thisdocument.pages[thisdocument.currentpage] != undefined) {
                            if (thisdocument.pages[thisdocument.currentpage].usepdfjs) {
                                if (thisdocument.foxitdoc != null) {
                                    //thisdocument.foxitdoc.getTextRect();
                                    set_tool('selectText', {p1 : false});
                                }
                                thisdocument.pages[thisdocument.currentpage].PDFTextArea.style.display = szdispvalue;
                            }
                            if (thisdocument.pages[thisdocument.currentpage].usevectorxml) {
                                thisdocument.pages[thisdocument.currentpage].cleartextdivs();
                                thisdocument.pages[thisdocument.currentpage].settextdivs();
                                thisdocument.pages[thisdocument.currentpage].TextSelectArea.style.display = szdispvalue;
                            }
                        }
                    }else{
                        if (thisdocument.foxitdoc != null) {

                            thisdocument.foxitdoc.removeTextSelect(thisdocument.pages[thisdocument.currentpage].textselectobj);
                            
                        }

                        RxCore_default();
                    }
                };
                this.closePages = function (bClearMarkup) {
                    while (thisdocument.pages.length != 0) {
                        if (thisdocument.pages[thisdocument.pages.length - 1] != undefined) {
                            if (thisdocument.pages[thisdocument.pages.length - 1].usepdfjs) {
                                thisdocument.pages[thisdocument.pages.length - 1].PDFPointArray = [];
                                if (thisdocument.pages[thisdocument.pages.length - 1].pdfisfirstrendered) {
                                    clearInterval(thisdocument.pages[thisdocument.pages.length - 1].pdftimervar);
                                    //this.pages[this.currentpage].PDFTextArea.style.display = "initial";
                                    

                                    if(rxcontainer != undefined && thisdocument.pages[thisdocument.pages.length - 1].PDFTextArea != undefined){
                                        thisdocument.pages[thisdocument.pages.length - 1].PDFTextArea.removeEventListener('wheel', ev_canvas, true);
                                        rxcontainer.removeChild(thisdocument.pages[thisdocument.pages.length - 1].PDFTextArea);
                                    }

                                    
                                }
                            }
                            if (thisdocument.pages[thisdocument.pages.length - 1].usevectorxml) {
                                clearInterval(thisdocument.pages[thisdocument.pages.length - 1].vectortimervar);
                                thisdocument.textselect = false;
                                //thisdocument.pages[thisdocument.pages.length - 1].selecttext();

                                if(rxcontainer != undefined && thisdocument.pages[thisdocument.pages.length - 1].TextSelectArea != undefined){
                                    rxcontainer.appendChild(thisdocument.pages[thisdocument.pages.length - 1].TextSelectArea);
                                    rxcontainer.removeChild(thisdocument.pages[thisdocument.pages.length - 1].TextSelectArea);
                                }
                                //thisdocument.pages[thisdocument.pages.length - 1].removetext();
                                if (thisdocument.pages[thisdocument.pages.length - 1].VectorPageObj != undefined) {
                                    thisdocument.pages[thisdocument.pages.length - 1].VectorPageObj.Close();
                                }
                                var emptylist = [];
                                if (RxCore_GUI_VectorLayers != undefined) {
                                    RxCore_GUI_VectorLayers.setVectorLayers(emptylist);
                                }
                                if (RxCore_GUI_VectorBlocks != undefined) {
                                    RxCore_GUI_VectorBlocks.setVectorBlocks(emptylist);
                                }
                            }
                            if (thisdocument.pages[thisdocument.pages.length - 1].usevector3Dxml) {
                                //renderer.clear();
                                renderer.domElement.style.visibility = 'hidden';
                                if (RxCore_GUI_3DWalkthrough != undefined) {
                                    if (bIsmobile){
                                        RxCore_GUI_3DWalkthrough.setWalkthroughGUI(false);
                                    }
                                    
                                }
                                bAnimateready = false;
                                if (thisdocument.pages[thisdocument.pages.length - 1].Vector3DPageObj != undefined) {
                                    thisdocument.pages[thisdocument.pages.length - 1].Vector3DPageObj.Close();
                                    thisdocument.pages[thisdocument.pages.length - 1].Vector3DPageObj = null;
                                }
                                var empty3dpartlist = [];
                                if (RxCore_GUI_3DParts != undefined) {
                                    RxCore_GUI_3DParts.isupdate = false;
                                    RxCore_GUI_3DParts.set3DParts(empty3dpartlist);
                                }
                            }
                        }
                        thisdocument.pages.pop();
                    }
                    if (bClearMarkup) {
                        while (thisdocument.markupundolist.length != 0) {
                            thisdocument.markupundolist.pop();
                        }
                        while (thisdocument.markupredolist.length != 0) {
                            thisdocument.markupredolist.pop();
                        }
                        while (thisdocument.markuplist.length != 0) {
                            thisdocument.markuplist.pop();
                        }
                        while (thisdocument.markupdraworder.length != 0) {
                            thisdocument.markupdraworder.pop();
                        }
                    }
                    if (RxCore_GUI_Markuplist != undefined) {


                        RxCore_GUI_Markuplist.setMarkupList(thisdocument.getmarkuplist(), thisdocument.bActive);


                        //RxCore_GUI_Markuplist.setMarkupList(thisdocument.markuplist, thisdocument.bActive);
                        // RxCore_GUI_Markuplist.notify();
                    }
                    if (RxCore_GUI_pagethumbs != undefined) {
                        RxCore_GUI_pagethumbs.setThumbnails(thisdocument.thumbnails);
                    }
                };

                this.setdelayedClose = function(onOff){
                    thisdocument.bdelayedClose = onOff;
                };
                this.getdelayedClose = function(){
                    return thisdocument.bdelayedClose;
                };


                this.bgFileClosed = function(){
                    //close compare doc
                    internalClosedoc(thisdocument.fileindex);
                };

                this.fgFileClosed = function(){
                    //close compare doc
                    internalClosedoc(thisdocument.fileindex);
                };

                this.Close = function () {
                    //console.log('Close');
                    thisdocument.bActive = false;
                    bPDFtemprender = false;




                    while (thisdocument.pages.length != 0) {
                        if (thisdocument.pages[thisdocument.pages.length - 1] != undefined) {
                            if (thisdocument.pages[thisdocument.pages.length - 1].usevectorxml) {
                                clearInterval(thisdocument.pages[thisdocument.pages.length - 1].vectortimervar);
                                thisdocument.textselect = false;
                                //thisdocument.pages[thisdocument.pages.length - 1].selecttext();
                                if(rxcontainer != undefined && thisdocument.pages[thisdocument.pages.length - 1].TextSelectArea != undefined){
                                    rxcontainer.appendChild(thisdocument.pages[thisdocument.pages.length - 1].TextSelectArea);
                                    rxcontainer.removeChild(thisdocument.pages[thisdocument.pages.length - 1].TextSelectArea);
                                }
                                //thisdocument.pages[thisdocument.pages.length - 1].removetext();
                                if (thisdocument.pages[thisdocument.pages.length - 1].VectorPageObj != undefined) {
                                    thisdocument.pages[thisdocument.pages.length - 1].VectorPageObj.Close();
                                }
                                var emptylist = [];
                                if (RxCore_GUI_VectorLayers != undefined) {
                                    RxCore_GUI_VectorLayers.setVectorLayers(emptylist);
                                }
                                if (RxCore_GUI_VectorBlocks != undefined) {
                                    RxCore_GUI_VectorBlocks.setVectorBlocks(emptylist);
                                }
                            }
                            if (thisdocument.pages[thisdocument.pages.length - 1].usevector3Dxml) {
                                //renderer.clear();
                                renderer.domElement.style.visibility = 'hidden';
                                if (RxCore_GUI_3DWalkthrough != undefined) {
                                    if (bIsmobile){
                                        RxCore_GUI_3DWalkthrough.setWalkthroughGUI(false);
                                    }
                                    
                                }
                                bAnimateready = false;
                                if (thisdocument.pages[thisdocument.pages.length - 1].Vector3DPageObj != undefined) {
                                    thisdocument.pages[thisdocument.pages.length - 1].Vector3DPageObj.Close();
                                    thisdocument.pages[thisdocument.pages.length - 1].Vector3DPageObj = null;
                                }
                                var empty3dpartlist = [];
                                if (RxCore_GUI_3DParts != undefined) {
                                    RxCore_GUI_3DParts.isupdate = false;
                                    RxCore_GUI_3DParts.set3DParts(empty3dpartlist);
                                }
                            }
                        }
                        thisdocument.pages.pop();
                    }
                    if (thisdocument.bdestroyPDFonClose) {
                        if (thisdocument.pdfDoc != null) {
                            thisdocument.pdfDoc.destroy();
                        }
                        thisdocument.pdfDoc = null;
                        if (thisdocument.foxitdoc != null) {
                            thisdocument.foxitdoc.closeFile();
                            if (foxitgetnumDocs() > 2) {
                                removefoxitdoc();
                            }
                            //foxitClose();
                        }
                    }
                    while (this.markupundolist.length != 0) {
                        this.markupundolist.pop();
                    }
                    while (this.markupredolist.length != 0) {
                        this.markupredolist.pop();
                    }
                    while (this.markuplist.length != 0) {
                        this.markuplist.pop();
                    }
                    while (this.markupdraworder.length != 0) {
                        this.markupdraworder.pop();
                    }
                    while (this.layouts.length != 0) {
                        this.layouts.pop();
                    }
                    while (this.thumbnails.length != 0) {
                        this.thumbnails.pop();
                    }
                    if (RxCore_GUI_Markuplist != undefined) {
                        RxCore_GUI_Markuplist.setMarkupList(thisdocument.getmarkuplist(), thisdocument.bActive);
                        // RxCore_GUI_Markuplist.notify();
                    }
                    if (RxCore_GUI_3DMarkupList != undefined) {
                        RxCore_GUI_3DMarkupList.setMarkupList([], thisdocument.bActive);
                        // RxCore_GUI_Markuplist.notify();
                    }
                    if (RxCore_GUI_pagethumbs != undefined) {
                        RxCore_GUI_pagethumbs.setThumbnails(thisdocument.thumbnails);
                    }

                    if(thisdocument.setComparelink != undefined){
                        thisdocument.setComparelink.onClose();
                    }

                    thisdocument.setComparelink = undefined;

                };
                function yieldingLoop(state, chunksize, callback, finished) {
                    (function chunk() {
                        var i = 0;
                        var endloop = false;
                        if (thisdocument.pagelocked) {
                            var pagelength = thisdocument.pages.length;
                        }
                        else {
                            pagelength = thisdocument.pages.length - 1;
                        }
                        //var end = Math.min(i + chunksize, count);
                        var end = (i < chunksize);
                        for (; i < chunksize; ++i) {
                            callback.call(null, state);
                            if (state.bpageshort) {
                                if (state.pagearray.length == 0) {
                                    endloop = true;
                                    break;
                                }
                            }
                            else {
                                if (state.pagenum == pagelength) {
                                    endloop = true;
                                    break;
                                }
                            }
                        }
                        if (state.pagenum != (pagelength) || !endloop) {
                            setTimeout(chunk, 10);
                        }
                        else {
                            finished.call(null);
                        }
                    })();
                }
            }
        }


        class ScaleObject {
            constructor() {
                var thisscale = this;                
                                            
                this.measurescale = MeasureScale;
                this.Unitofmeasure = Unitofmeasure;
                this.unitname = "Metric";
                this.SubmeasureUnit = SubmeasureUnit;
                this.Unitlabel = Unitlabel;
                this.AreaUnitlabel = AreaUnitlabel;
                this.unitscale = unitscale;
                this.scaleValue = "1:1";
                this.scaleLabel = "1 Millimeter : 1 Millimeter";

                this.ndimprecision = ndimprecision;
                
                this.setUnit = function(value){

                    //function RxCore_SetUnit(value) {
                        /*for (i=0;i<this.childElementCount;i++){
                         if(this.children[i].className == 'selected'){
                         var szUnit = this.children[i].textContent;
                         }
                         }*/
                        /*if (value=='Metric'){Unitofmeasure=1;}
                         if (value=='Imperial'){Unitofmeasure=2;}
                         if (value=='System'){Unitofmeasure=3;}
                         if (value=='Custom'){Unitofmeasure=4;}*/
                        //var unitdropdwn = document.getElementById('unitdropdown');

                        /*unit : this.Unitofmeasure,
                        unitname : uname,
                        subunit : this.SubmeasureUnit,
                        label : this.Unitlabel,
                        Arealabel : this.AreaUnitlabel*/

                
                        switch (value) {
                            case 1:
                                this.Unitlabel = "mm";
                                this.AreaUnitlabel = "mm\u00B2";
                                //unitdropdwn.setAttribute('ref','metricunits');
                                //unitdropdwn.children[0].value = "Millimeter";
                                break;
                            case 2:
                                this.Unitlabel = "in.";
                                this.AreaUnitlabel = "sq.in.";
                                //unitdropdwn.setAttribute('ref','imperialunits');
                                //unitdropdwn.children[0].value = "Inch";
                                break;
                            case 3:
                                this.Unitlabel = "Units";
                                this.AreaUnitlabel = "Units\u00B2";
                                break;
                        }
                        this.Unitofmeasure = value;
                    //}

                } 


                this.getUnitScale = function(){
                    return thisscale.unitscale;
                }

                this.getUnit = function(){

                        /*if (value=='Metric'){Unitofmeasure=1;}
                         if (value=='Imperial'){Unitofmeasure=2;}
                         if (value=='System'){Unitofmeasure=3;}
                         if (value=='Custom'){Unitofmeasure=4;}*/
                
                         var uname = 'Metric';
                
                         switch (this.Unitofmeasure) {
                            case 1:
                                uname = 'Metric';
                
                                break;
                            case 2:
                                uname = 'Imperial';
                                break;
                            case 3:
                                uname = 'System';
                                break;
                
                        }
                
                        //1 = Millimeter, 2 = Centimeter, 3 = Decimeter, 4=Meter,5=Kilometer,6=Nautical Miles
                        //1 = Inch,2=Feet,3=Yard,4=Mile,5=Nautical Miles
                    
                        var unitobj = {
                            unit : thisscale.Unitofmeasure,
                            unitname : uname,
                            subunit : thisscale.SubmeasureUnit,
                            label : thisscale.Unitlabel,
                            Arealabel : thisscale.AreaUnitlabel
                        };
                
                        return unitobj;
                

                };


                this.imperialUnit = function(unit){

                    if (unit == 'Inch') {
                        thisscale.SubmeasureUnit = 1;
                    }
                    if (unit == 'Feet') {
                        thisscale.SubmeasureUnit = 2;
                    }
                    if (unit == 'Yard') {
                        thisscale.SubmeasureUnit = 3;
                    }
                    if (unit == 'Mile') {
                        thisscale.SubmeasureUnit = 4;
                    }
                    if (unit == 'Nautical Miles') {
                        thisscale.SubmeasureUnit = 5;
                    }
            
                    switch (thisscale.SubmeasureUnit) {
                        case 1:
                            thisscale.Unitlabel = "in.";
                            thisscale.AreaUnitlabel = "sq.in.";
                            thisscale.unitscale = 1;
                            break;
                        case 2:
                            thisscale.Unitlabel = "ft.";
                            thisscale.AreaUnitlabel = "sq.ft.";
                            thisscale.unitscale = 12;
                            break;
                        case 3:
                            thisscale.Unitlabel = "yd.";
                            thisscale.AreaUnitlabel = "sq.yd.";
                            thisscale.unitscale = 36;
                            break;
                        case 4:
                            thisscale.Unitlabel = "mi.";
                            thisscale.AreaUnitlabel = "sq.mi";
                            thisscale.unitscale = 63360;
                            break;
                        case 5:
                            thisscale.Unitlabel = "nm";
                            thisscale.AreaUnitlabel = "sq.nm";
                            thisscale.unitscale = 72913.3858;
                            break;
            
                    }
                };

                this.metricUnit = function(unit){

                    if (unit == 'Millimeter') {
                        thisscale.SubmeasureUnit = 1;
                    }
                    if (unit == 'Centimeter') {
                        thisscale.SubmeasureUnit = 2;
                    }
                    if (unit == 'Decimeter') {
                        thisscale.SubmeasureUnit = 3;
                    }
                    if (unit == 'Meter') {
                        thisscale.SubmeasureUnit = 4;
                    }
                    if (unit == 'Kilometer') {
                        thisscale.SubmeasureUnit = 5;
                    }
                    if (unit == 'Nautical Miles') {
                        thisscale.SubmeasureUnit = 6;
                    }
            
                    switch (thisscale.SubmeasureUnit) {
                        case 1:
                            thisscale.Unitlabel = "mm";
                            thisscale.AreaUnitlabel = "mm\u00B2";
                            thisscale.unitscale = 1;
                            break;
                        case 2:
                            thisscale.Unitlabel = "cm";
                            thisscale.AreaUnitlabel = "cm\u00B2";
                            thisscale.unitscale = 10;
                            break;
                        case 3:
                            thisscale.Unitlabel = "dm";
                            thisscale.AreaUnitlabel = "dm\u00B2";
                            thisscale.unitscale = 100;
                            break;
                        case 4:
                            thisscale.Unitlabel = "m";
                            thisscale.AreaUnitlabel = "m\u00B2";
                            thisscale.unitscale = 1000;
                            break;
                        case 5:
                            thisscale.Unitlabel = "km";
                            thisscale.AreaUnitlabel = "km\u00B2";
                            thisscale.unitscale = 1000000;
                            break;
                        case 6:
                            thisscale.Unitlabel = "nmi";
                            thisscale.AreaUnitlabel = "nmi\u00B2";
                            thisscale.unitscale = 185200000;
                            break;
            
                    }
                }

                this.setMeasureScaledirect = function (value) {
                    thisscale.measurescale = value;
                };
                this.getMeasureScale = function () {
                    return thisscale.measurescale;
                };
                this.getScaleValue = function () {
                    return thisscale.scaleValue;
                };
                this.setCalibration = function (val) {
                    if (val) {
                        if (nCalibrateSet != 0) {
                            nCalibrateScale = nCalibrateMeasured / nCalibrateSet;
                            nCalibrateScale = 1 / nCalibrateScale;
                            //setCalibratebtn();
                            thisscale.measurescale = nCalibrateScale;
                        }
                    }
                    else {
                        nCalibrateScale = 1;
                    }
                    // redrawMarkup();
                    RxCore_MarkupRedraw();
                    return nCalibrateScale;
                };
                this.setCalibrateScale = function (scale) {
                    thisscale.measurescale = scale;
                    // redrawMarkup();
                    RxCore_MarkupRedraw();
                };
                this.setMeasureScale = function (scale) {
                    thisscale.scaleValue = scale;
                    var numerator = 0;
                    var denomintaor = 0;
                    if (scale != 'Calibration') {
                        var numarr = scale.split(":");
                        numerator = numarr[0];
                        denomintaor = numarr[1];
                        if (bReverseScale) {
                            thisscale.measurescale = denomintaor / numerator;
                        }
                        else {
                            thisscale.measurescale = numerator / denomintaor;
                        }
                    }
                    else {
                        thisscale.measurescale = nCalibrateScale;
                    }
                    //redrawMarkup();
                    RxCore_MarkupRedraw();
                }; 
                
                this.setDimPrecision = function(value) {
                    thisscale.ndimprecision = value;
                }

                this.getDimPrecision = function(value) {
                    return thisscale.ndimprecision;
                }

                this.setScaleLabel = function(label) {
                    thisscale.scaleLabel = label;
                }

                this.getScaleLabel = function() {
                    return thisscale.scaleLabel;
                }

                this.resetToDefaultValue = function() {
                    thisscale.measurescale = MeasureScale;
                    thisscale.Unitofmeasure = Unitofmeasure;
                    thisscale.unitname = "Metric";
                    thisscale.SubmeasureUnit = SubmeasureUnit;
                    thisscale.Unitlabel = Unitlabel;
                    thisscale.AreaUnitlabel = AreaUnitlabel;
                    thisscale.unitscale = unitscale;
                    thisscale.scaleValue = "1:1";
                    thisscale.scaleLabel = "1 Millimeter : 1 Millimeter";
                    thisscale.ndimprecision = ndimprecision;
                }

                
            }
        }        



    function xhrProgress(evt) {
        if (evt.lengthComputable) {
            //var percentComplete = Math.round(evt.loaded * 100 / evt.total);
            //showDownloadDialog();
            if (RxCore_GUI_Download != undefined) {
                RxCore_GUI_Download.setDownload("show");
            }

            //document.getElementById('progressbar').value = Math.round(evt.loaded * 100 / evt.total);//percentComplete;//.toString() + '%';
            //if (document.getElementById('progressbar').value == 100){
            //document.getElementById('progressbar').value = 0;
            //hideDownloadDialog();

            //}
        } else {
            //document.getElementById('progressNumber').innerHTML = 'unable to compute';
        }
    }

    function PDFuploadProgress(ev) {

    }

    function ImageUploadComplete(ev) {
        //hideUploadDialog();
        if (RxCore_GUI_Upload != undefined) {
            RxCore_GUI_Upload.setUpload("hide");
        }

        if (ev.currentTarget.status == 200) {
            var xmlDoc = ev.currentTarget.responseXML;
            if (xmlDoc == null || xmlDoc.documentElement == null) {
                xmlDoc = parseXML(ev.currentTarget.responseText);
                //xmlDoc = $.parseXML(ev.currentTarget.responseText).documentElement;
            }
            var PDFFileUrl = xmlDoc.getElementsByTagName('File')[0].firstChild.nodeValue;
            var cachfolder = getURLPath(DocObj.FileNameSRC);
            PDFFileUrl = cachfolder + PDFFileUrl;
            
            if (Rxcore_GUI_exportComplete != undefined){
                Rxcore_GUI_exportComplete.loadComplete(PDFFileUrl);
            }
        

            //get pdf url here.
        } else if (this.status == 404) {
            
            console.log("Image upload failed error 404");
        } else if (this.status == 503) {
            
            console.log("Image upload failed error 503");
        }

    }

    function ImageUploadProgress(ev) {
        if (ev.lengthComputable) {
            //var percentComplete = Math.round(evt.loaded * 100 / evt.total);
            //showUploadDialog();
            if (RxCore_GUI_Upload != undefined) {
                RxCore_GUI_Upload.setUpload(Math.round(ev.loaded * 100 / ev.total));
            }

            /*document.getElementById('progressbar').value = Math.round(ev.loaded * 100 / ev.total); //percentComplete;//.toString() + '%';
            if (document.getElementById('progressbar').value == 100) {
                document.getElementById('progressbar').value = 0;
                //hideUploadDialog();

            }*/
        } else {
            //document.getElementById('progressNumber').innerHTML = 'unable to compute';
        }

    }

    function post3DImageData() {
        //CanvasSaveUrl

        //var xhr = new XMLHttpRequest();
        //xhr.withCredentials = bUseCredentials;
        //xhr.addEventListener("progress", ImageUploadProgress, false);
        //xhr.addEventListener("load", ImageUploadComplete, false);
        //xhr.addEventListener("error", ImageuploadFailed, false);
        //xhr.addEventListener("abort", ImageuploadCanceled, false);

        //xhr.upload.addEventListener("progress", ImageUploadProgress, false);

        var path = getPath(DocObj.OriginalURL);
        var file = getFileName(DocObj.OriginalURL);


        //var InageCreateURL = CanvasSaveUrl + "?" + path + "&" + file + "&" + "PDF&200";

        if(bUseCustomrelpath){
            var InageCreateURL = CanvasSaveUrl + "&" + path + "&" + file + "&" + "PDF&200";
        }else{
            InageCreateURL = CanvasSaveUrl + "?" + path + "&" + file + "&" + "PDF&200";
        }


        //var dataURL = canvas.toDataURL();
        //var canvas = document.getElementById("canvas");
        var dataURL = renderer.domElement.toDataURL();

        var eventlist = [
            {event : "load", func : ImageUploadComplete},
            {event : "progress", func : ImageUploadProgress, bupload : true}
        ];

        sendHttpRequestwevents('POST', InageCreateURL, "", eventlist, dataURL);


        /*try {
            xhr.open('POST', InageCreateURL, true);
        } catch (e) {
            alert("Error 1 - " + e);
        }
        try {
            xhr.responseType = '';
        } catch (e) {
            //alert("Error 2 - " + e);
        }

        xhr.send(dataURL);*/

    }

    function xmlescape(str){
        
        //str.replace(/&/g, '&amp;');
        //str.replace(/</g, '&lt;');
        //str.replace(/>/g, '&gt;');
        var locstr = "";
        locstr = str.replace(/&(?!amp;)(?!quot;)(?!lt;)(?!gt;)(?!apos;)/g,"&amp;");
        locstr = locstr.replace(/"/g,"&quot;");m0=m0.replace(/'/g,"&apos;");
        
        locstr = locstr.replace(/</g,"&lt;");
        locstr = locstr.replace(/>/g,"&gt;");
        
        return locstr;
        
    }


    function getUserInfo(){


        sendHttpRequestXML('GET', userInfoURL).then(function (xmlDoc){
            
            if (xmlDoc.getElementsByTagName('ID')[0] != undefined) {
                signature = xmlDoc.getElementsByTagName('ID')[0].firstChild.nodeValue;
                signature = xmlescape(signature);
            }
            if (xmlDoc.getElementsByTagName('name')[0] != undefined) {
                DisplayName = xmlDoc.getElementsByTagName('name')[0].firstChild.nodeValue;
                DisplayName = xmlescape(DisplayName);
            }
            if (xmlDoc.getElementsByTagName('color')[0] != undefined) {
                markupcolor = xmlDoc.getElementsByTagName('color')[0].firstChild.nodeValue;
            }
            if (xmlDoc.getElementsByTagName('layer')[0] != undefined) {
                markuplayer = xmlDoc.getElementsByTagName('layer')[0].firstChild.nodeValue;
            }

            if (xmlDoc.getElementsByTagName('canChangeLayer')[0] != undefined) {
                bCanChangeLayer = (xmlDoc.getElementsByTagName('canChangeLayer')[0].firstChild.nodeValue == '1');
            }

            Userlist.push(new Users(signature, DisplayName, markuplayer, markupcolor, false));
            //Userlist[0] = new Users(signature, DisplayName, markuplayer, markupcolor, false);

            if (RxCore_GUI_Users != undefined) {
                RxCore_GUI_Users.setUserlist(Userlist, true);
            }

        }).catch(function(error){

           if(error.status){
   
            if (error.status == 404) {
                console.log("XML could not be found");
            } else if (error.status == 503) {
                console.log("Server is down");
            }
           }else{
                console.log(error);
           }


       });            
       
    }

    function exportFilePrint(paperSize, rect){

        var params = {
            consolidateonly : false,
            format : 'PDF',
            UPI : "0",
            paperSize : paperSize,
            markupFlag : "1",
            rectangle : rect,
            useRaster : false
        }
        
        exportFileSendPrint(params);

    }

    function exportFilepage(consolidateonly, pagenum, format,UPI,paperSize,markupFlag){
        
        //exportFilepage(consolidate,pagenum, format,UPI,paperSize,markupFlag);
        var params = {
            consolidateonly : consolidateonly,
            format : format,
            UPI : UPI,
            paperSize : paperSize,
            markupFlag : markupFlag,
            page : pagenum,
            useRaster : false,
            singlepage : true,
        }


        var markupxml = createxmlmarkup(false, consolidateonly, bexportmarkupincludeAll, signature, signature);

        exportFileSend(params, markupxml);


    }

    function exportFileExParams(params){


        var markupsave = new markupsaveObj(DocObj);

        markupsave.setOncomplete(function(xmlmarkup){
            
            
            exportFileSend(params, xmlmarkup);  
            //console.log(xmlmarkup);
            
            //sendMarkupReq(xmlurlsave, signature, dispname, xmlmarkup);
        });

        //markupsave.createxml(signature, dispname);

        
        markupsave.createxmlexport(signature, DisplayName, params.consolidateonly);

        //var markupxml = createxmlmarkup(false, params.consolidateonly, bexportmarkupincludeAll, signature, signature);

        //var markupxml = createxmlmarkup(false, consolidateonly, bexportmarkupincludeAll, signature, signature);

        //exportFileSend(params, markupxml);


    }    


    function exportFileEx(consolidateonly,format,UPI,paperSize,markupFlag, sign, disp){

        var params = {
            consolidateonly : consolidateonly,
            format : format,
            UPI : UPI,
            paperSize : paperSize,
            markupFlag : markupFlag,
            useRaster : false
        }

        var markupsave = new markupsaveObj(DocObj);

        markupsave.setOncomplete(function(xmlmarkup){
            
            
            exportFileSend(params, xmlmarkup);  
            //console.log(xmlmarkup);
            
            //sendMarkupReq(xmlurlsave, signature, dispname, xmlmarkup);
        });

        //markupsave.createxml(signature, dispname);
        markupsave.createxmlexport(sign, disp, consolidateonly);

        //var markupxml = createxmlmarkup(false, consolidateonly, bexportmarkupincludeAll, signature, signature);

        //exportFileSend(params, markupxml);


    }    

    function exportFileSign(consolidateonly,format,UPI,paperSize,markupFlag, sign, location, reason){

        var params = {
            consolidateonly : consolidateonly,
            format : format,
            UPI : UPI,
            paperSize : paperSize,
            markupFlag : markupFlag,
            sign : sign,
            location : location,
            reason : reason,
            useRaster : false
        }

        //var nUseSign = params.sign ? "1" : "0";
        //var szLocation = params.location ? params.location : "";
        //var szReason = params.reason ? params.reason : "";


        var markupxml = createxmlmarkup(false, consolidateonly, bexportmarkupincludeAll, signature, signature);

        exportFileSendJSON(params, markupxml);
    }


    function exportfileUpload(params){

        unselelectallmarkup();
        unselecteditmarkupall();

        if(DocObj.bLargePDF){
            
            exportFileExParams(params);

        }else{
        
            exportFileParams(params);
            
        }


    }

    function exportFileParams(params){
        /*var params = {
            consolidateonly : consolidateonly,
            format : format,
            UPI : UPI,
            paperSize : paperSize,
            markupFlag : markupFlag,
            useRaster : false
        }*/

        var markupxml = createxmlmarkup(false, params.consolidateonly, bexportmarkupincludeAll, signature, signature);

        exportFileSend(params, markupxml);

    }


    function exportFile(consolidateonly,format,UPI,paperSize,markupFlag){
        var params = {
            consolidateonly : consolidateonly,
            format : format,
            UPI : UPI,
            paperSize : paperSize,
            markupFlag : markupFlag,
            useRaster : false
        }

        var markupxml = createxmlmarkup(false, consolidateonly, bexportmarkupincludeAll, signature, signature);

        exportFileSend(params, markupxml);

    }

    function exportFileRaster(consolidateonly,format,UPI,paperSize,markupFlag){

        var params = {
            consolidateonly : consolidateonly,
            format : format,
            UPI : UPI,
            paperSize : paperSize,
            markupFlag : markupFlag,
            useRaster : true
        }

        var markupxml = createxmlmarkup(false, consolidateonly, bexportmarkupincludeAll, signature, signature);

        exportFileSend(params, markupxml);

    }

    function exportFileSendPrint(params){

        if(docpagNotReady()){
            return;
        }

        var consolidateonly = params.consolidateonly;
        var format = params.format
        var UPI = params.UPI;
        var paperSize = params.paperSize;
        var markupFlag = params.markupFlag;
        //var bUseRaster = params.useRaster;
        var nUseRaster = params.useRaster ? "1" : "0";

        var curmarkup = 0;
        var savetime = new Date().getTime();
        var internalscale = 1;
        if (DocObj.getCurrentpage().usevector3Dxml) {
            post3DImageData();
            return;
        }
        if (DocObj.getCurrentpage().usepdfjs) {
            internalscale = DocObj.getCurrentpage().curpagescale * DocObj.getCurrentpage().dscalepdf;
        }

        
    }

    function exportFilePrintFinal(params){

        var consolidateonly = params.consolidateonly;
        var format = params.format
        var UPI = params.UPI;
        var paperSize = params.paperSize;
        var markupFlag = params.markupFlag;
        var nUseRaster = params.useRaster ? "1" : "0";
        var reference = params.reference;


        var markupxml = createxmlmarkup(false, consolidateonly,true,signature, signature);


        var path = getPath(DocObj.OriginalURL);
        var file = getFileName(DocObj.OriginalURL);
        

        if(bUseCustomrelpath){
            //var PDFCreateURL = PDFPrintPrepare + "&" + path + "&" + file + "&" + format + "&" + UPI + "&" + paperSize + "&" + markupFlag + "&" + nUseRaster;
            var PDFCreateURL = PDFPrintCreate + "&" + path + "&" + file + "&" + reference + "&" + paperSize;
        }else{
            //PDFCreateURL = PDFPrintPrepare + "?" + path + "&" + file + "&" + format + "&" + UPI + "&" + paperSize + "&" + markupFlag + "&" + nUseRaster;
            PDFCreateURL = PDFPrintCreate + "?" + path + "&" + file + "&" + reference + "&" + paperSize;
        }

        
        sendHttpRequestXML('POST', PDFCreateURL, markupxml).then(function (response){

            //console.log(response);

            //var xmlDoc = parseXML(response);
            if(response.getElementsByTagName('PDF')[0] != undefined){
                var PDFFileUrl = response.getElementsByTagName('PDF')[0].firstChild.nodeValue;

                var cachfolder = getURLPath(DocObj.FileNameSRC);

                if(bUseCustomrelpath){
                    cachfolder = xmlurlrelcustom;
                }

                PDFFileUrl = cachfolder + PDFFileUrl;


                if (Rxcore_GUI_exportComplete != undefined){
                    Rxcore_GUI_exportComplete.loadComplete(PDFFileUrl);
                }

            }
            
            //if(response.getElementsByTagName('PDF')[0].firstChild.nodeValue != null){

                //console.log(refval);

                //params.reference = refval;

                //exportFilePrintFinal(params);

            //}





        }).catch(function(error){
           
            if(error.status){
                
                console.log("exportFilePrintFinal", error.status);
            }else{
                
                console.log(error);
            }

        });


    }

    function exportFileSendPrintPDF(params){
        
        var consolidateonly = params.consolidateonly;
        var format = params.format
        var UPI = params.UPI;
        var paperSize = params.paperSize;
        var markupFlag = params.markupFlag;
        var nUseRaster = params.useRaster ? "1" : "0";
        
        //var markupxml = createxmlmarkup(false, consolidateonly,true,signature, signature);

        var path = getPath(DocObj.OriginalURL);
        var file = getFileName(DocObj.OriginalURL);
        

        if(bUseCustomrelpath){
            //var PDFCreateURL = PDFPrintPrepare + "&" + path + "&" + file + "&" + format + "&" + UPI + "&" + paperSize + "&" + markupFlag + "&" + nUseRaster;
            var PDFCreateURL = PDFPrintPrepare + "&" + path + "&" + file;
        }else{
            //PDFCreateURL = PDFPrintPrepare + "?" + path + "&" + file + "&" + format + "&" + UPI + "&" + paperSize + "&" + markupFlag + "&" + nUseRaster;
            PDFCreateURL = PDFPrintPrepare + "?" + path + "&" + file;   
        }

        var fillarraycopy = [];

        var fillarray = getHatches();

         
        for (var i=0; i < fillarray.length; i++){
            
            fillarraycopy.push(
                {
                    type : fillarray[i].type,
                    name : fillarray[i].name, 
                    ptrn : fillarray[i].ptrn, 
                    tilesize : fillarray[i].tilesize, 
                    color : fillarray[i].color, 
                    description : fillarray[i].description,
                    inuse : fillarray[i].inuse
                }
            );
        }

        var printstruct = {
            pagerect : RxCore_getScreenRect(),
            fillarray : fillarraycopy,
            layers : RxCore_getlayerlist(), 
            filledblocks : RxCore_get2DVectorBlockswFill()
        };




        for (var i=0; i < printstruct.fillarray.length; i++){
            if(printstruct.fillarray[i].type == 'hatch'){
                var cdata = getBase64Image(printstruct.fillarray[i].ptrn);
                printstruct.fillarray[i].ptrn = cdata;
            }

        }

        var jsonprintstruct = JSON.stringify(printstruct);
        
        //console.log(jsonprintstruct);

        var rtype = '';
        
        sendHttpRequest('POST', PDFCreateURL, '', jsonprintstruct ).then(function (response){

            //console.log(response);
            var xmlDoc = parseXML(response);

            if(xmlDoc.getElementsByTagName('reference')[0] != undefined){
                
                var refval = xmlDoc.getElementsByTagName('reference')[0].firstChild.nodeValue;
                //console.log(refval);
                params.reference = refval;

                exportFilePrintFinal(params);

            }

            //var PDFFileUrl = xmlDoc.getElementsByTagName('File')[0].firstChild.nodeValue;
            
            //if()


            //<?xml version="1.0" encoding="UTF-8"?><reference>3B967A60-F395-446A-9B56-027931349A21</reference>

            


        }).catch(function(error){
           
            if(error.status){
                
                console.log("exportFileSendPrintPDF", error.status);
            }else{
                
                console.log(error);
            }

        });


        //var jsonFilledBlocks = JSON.stringify(Filledblocks);
    

    }

    function exportFileSendJSON(params, markupxml){
        if(docpagNotReady()){
            return;
        }
        
        if(CompareFilesServer == undefined){
            return;
        }


        var path = getPath(DocObj.OriginalURL);
        var file = getFileName(DocObj.OriginalURL);

        var markupxmltext = new XMLSerializer().serializeToString(markupxml);
        var markuphex64 = window.btoa(markupxmltext);


        var nUseSign = params.sign ? "1" : "0";
        var szLocation = params.location ? params.location : "";
        var szReason = params.reason ? params.reason : "";

        var markupFlag = params.markupFlag;


        var JSONExportFile = [{
            Command : 'Export',
            LicenseID : LicenseID,
            File : path + file,
            Paper : params.paperSize,
            Format : params.format,
            Sign : nUseSign,
            Reason : szReason, 
            Location : szLocation,
            Markup : markupFlag,
            MarkupData : markuphex64
        }];


        var jsonstring = JSON.stringify(JSONExportFile);

        sendHttpRequest('POST', CompareFilesServer, '', jsonstring).then(function (response){

            
            var xmlDoc = response;
            if (xmlDoc == null || xmlDoc.documentElement == null) {
                xmlDoc = parseXML(response);
            }

            var bIsPDF = true;
            var PDFFileUrl = xmlDoc.getElementsByTagName('File')[0].firstChild.nodeValue;
            if (xmlDoc.getElementsByTagName('isPDF')[0] != undefined) {
                bIsPDF = (xmlDoc.getElementsByTagName('isPDF')[0].firstChild.nodeValue == '1');
            }

            var cachfolder = getURLPath(DocObj.FileNameSRC);

            if(bUseCustomrelpath){
                cachfolder = xmlurlrelcustom;
            }


            if(!bPDFExportfullPath){
                if(bIsPDF){
                    PDFFileUrl = cachfolder + PDFFileUrl;
                }else{
                    PDFFileUrl = cachfolder + PDFFileUrl;
                    //PDFFileUrl;
                }
            } 

            if (Rxcore_GUI_exportComplete != undefined){
                Rxcore_GUI_exportComplete.loadComplete(PDFFileUrl);
            }
    


            
        }).catch(function(error){

            if(error.status){

                if(error.status == 404){
                    console.log("Export of PDF failed, error 404");            
                }else if(error.status == 503){
                    console.log("Export of PDF failed, error 503");            
                }else{
                    console.log(error.status);
                }
                
            }else{
        
                console.log(error);
        
            }
        

        });


    /*} else if (this.status == 404) {
                
        console.log("Export of PDF failed, error 404");
    } else if (this.status == 503) {
        console.log("Export of PDF failed, error 503");
    }*/


        /*
        
        var JSONGetCompare = [{
            Command : 'Compare', 
            LicenseID : LicenseID, 
            BackURL : fileurlbackground, 
            FrontURL : fileurloverlay, 
            CompareDPI : 200, 
            BackColor : 'rgb(255,0,0)', 
            FrontColor : 'rgb(0,0,255)', 
            EqualColor : 'rgb(128,128,128)', 
            ReturnData :'text'
        }];

        if(alignarray != undefined){
            if(alignarray.length > 1 ){
                JSONGetCompare.push(alignarray[0]);
                JSONGetCompare.push(alignarray[1]);
            }
        }

        var jsonstring = JSON.stringify(JSONGetCompare);

        */


        /*[
        {
            "Command": "Export",
            "Format": "params.format",
            "LicenseID": "6",
            "File": "C:\\\\Rasterex\\\\Upload\\\\test.pdf",
            "Paper": "A3",
            "Markup": "PD94bWwgdmVyc2lvbj0iMS4wIiBlbmNvZGluZ",  
            "Sign": 1        

                    "Command": "Export",
        "LicenseID": "6",
        "File": "C:\\\\Rasterex\\\\Upload\\\\test.pdf",
        "Paper": "A3",
        "Format": "PDF",
        "Markup": "PD94bWwgdmVyc2lvbj0iMS4wIiBlbmNvZGluZ.....",
        "Sign": 1,
        "Reason" : "Approved",
        "Location" : "Oslo",


        }
        ]*/




    }

    function exportFileSend(params, markupxml){

        if(docpagNotReady()){
            return;
        }

    


        var consolidateonly = params.consolidateonly;
        var format = params.format;
        var UPI = params.UPI;
        var paperSize = params.paperSize;
        var markupFlag = params.markupFlag;

        var singlepage = params.singlepage;
        var pagenumber = params.page;

        //var bUseRaster = params.useRaster;
        var nUseRaster = params.useRaster ? "1" : "0";

        
        
        var curmarkup = 0;
        var savetime = new Date().getTime();
        var internalscale = 1;
        if (DocObj.getCurrentpage().usevector3Dxml) {
            post3DImageData();
            return;
        }
        if (DocObj.getCurrentpage().usepdfjs) {
            internalscale = DocObj.getCurrentpage().curpagescale * DocObj.getCurrentpage().dscalepdf;
        }


        var xhr = new XMLHttpRequest();
        xhr.withCredentials = bUseCredentials;
        xhr.upload.addEventListener("progress", PDFuploadProgress, false);


        //var markupxml = createxmlmarkup(false, consolidateonly, bexportmarkupincludeAll, signature, signature);
        //var markupxml = createxmlmarkup(false, consolidateonly,true,signature, signature);
        //var markupxml = parseXML(markupxmltxt);

        



        var path = getPath(DocObj.OriginalURL);
        /*if(DocObj.FileLocal){
            path = RxConfig.UploadServerfolder;
        }*/

        var file = getFileName(DocObj.OriginalURL);

        if(params.overridefilename){
            file = params.overridefilename;
        }


        //WebClientSaveAs?filepath&filename&PDF&UPI


        if(singlepage && pagenumber != undefined){
            var PDFCreateURL = PDFExportPageURL + "&" + path + "&" + file + "&" + pagenumber + "&" + format + "&" + UPI + "&" + paperSize + "&" + markupFlag + "&" + nUseRaster;
        }else{
            if(bUseCustomrelpath){
                var PDFCreateURL = PDFExportURL + "&" + path + "&" + file + "&" + format + "&" + UPI + "&" + paperSize + "&" + markupFlag + "&" + nUseRaster;
            }else{
                PDFCreateURL = PDFExportURL + "?" + path + "&" + file + "&" + format + "&" + UPI + "&" + paperSize + "&" + markupFlag + "&" + nUseRaster;
            }
        }


        /*if(bUseCustomrelpath){
            var PDFCreateURL = xmlurlrelcustom + "?" + path + "&" + file + "&" + format + "&" + UPI + "&" + paperSize + "&" + markupFlag;
        }else{

        }*/
        //xmlurlrelcustom = fileurl;



        //PDFExportURL?path&file&PDF&200

        try {
            xhr.open('POST', PDFCreateURL, true);
        } catch (e) {
            alert("Error 1 - " + e);
        }
        try {
            xhr.responseType = '';
        } catch (e) {
            //alert("Error 2 - " + e);
        }

        if(buseAuth){

            var auth = getAuth();

            if (auth.authset){
                xhr.setRequestHeader('Authorization', auth.authString + auth.baseuserstring);
            }
            
        }


        xhr.onload = function (e) {

            if (this.status == 200) {
                var xmlDoc = this.responseXML;
                if (xmlDoc == null || xmlDoc.documentElement == null) {
                    xmlDoc = parseXML(this.responseText);
                }

                var bIsPDF = true;
                var PDFFileUrl = xmlDoc.getElementsByTagName('File')[0].firstChild.nodeValue;
                if (xmlDoc.getElementsByTagName('isPDF')[0] != undefined) {
                    bIsPDF = (xmlDoc.getElementsByTagName('isPDF')[0].firstChild.nodeValue == '1');
                }

                var cachfolder = getURLPath(DocObj.FileNameSRC);

                if(bUseCustomrelpath){
                    cachfolder = xmlurlrelcustom;
                }


                if(!bPDFExportfullPath){
                    if(bIsPDF){
                        PDFFileUrl = cachfolder + PDFFileUrl;
                    }else{
                        PDFFileUrl = cachfolder + PDFFileUrl;
                        //PDFFileUrl;
                    }
                } 

                if (Rxcore_GUI_exportComplete != undefined){
                    Rxcore_GUI_exportComplete.loadComplete(PDFFileUrl);
                }
                

                //get pdf url here.
            } else if (this.status == 404) {
                
                console.log("Export of PDF failed, error 404");
            } else if (this.status == 503) {
                console.log("Export of PDF failed, error 503");
            }

        };
        xhr.send(markupxml);
    }


    function PDExportnew() {

        if(docpagNotReady()){
            return;
        }

        var curmarkup = 0;
        var savetime = new Date().getTime();
        var internalscale = 1;
        if (DocObj.getCurrentpage().usevector3Dxml) {
            post3DImageData();
            return;
        }
        if (DocObj.getCurrentpage().usepdfjs) {
            internalscale = DocObj.getCurrentpage().curpagescale * DocObj.getCurrentpage().dscalepdf;
        }


        var xhr = new XMLHttpRequest();
        xhr.withCredentials = bUseCredentials;
        xhr.upload.addEventListener("progress", PDFuploadProgress, false);

        var markupxml = createxmlmarkup(false,false,true,signature, signature);
        //var markupxml = parseXML(markupxmltxt);



        var path = getPath(DocObj.OriginalURL);
        var file = getFileName(DocObj.OriginalURL);
        //WebClientSaveAs?filepath&filename&PDF&UPI
        var PDFCreateURL = PDFExportURL + "?" + path + "&" + file + "&" + "PDF&200";

        //PDFExportURL?path&file&PDF&200

        try {
            xhr.open('POST', PDFCreateURL, true);
        } catch (e) {
            alert("Error 1 - " + e);
        }
        try {
            xhr.responseType = '';
        } catch (e) {
            //alert("Error 2 - " + e);
        }


        if(buseAuth){

            var auth = getAuth();

            if (auth.authset){
                xhr.setRequestHeader('Authorization', auth.authString + auth.baseuserstring);
            }
            
        }

        xhr.onload = function (e) {

            if (this.status == 200) {
                var xmlDoc = this.responseXML;
                if (xmlDoc == null || xmlDoc.documentElement == null) {
                    xmlDoc = parseXML(this.responseText);
                }
                var PDFFileUrl = xmlDoc.getElementsByTagName('File')[0].firstChild.nodeValue;
                var cachfolder = getURLPath(DocObj.FileNameSRC);
                PDFFileUrl = cachfolder + PDFFileUrl;

                if (Rxcore_GUI_exportComplete != undefined){
                    Rxcore_GUI_exportComplete.loadComplete(PDFFileUrl, true);
                }
                

                //get pdf url here.
            } else if (this.status == 404) {
                
                console.log("Export of PDF failed, error 404");
            } else if (this.status == 503) {
                console.log("Export of PDF failed, error 503");
            }

        };
        xhr.send(markupxml);

    }


    function getxmlurlEx(filepath){
        bAbortPageload = false;
        //showDownloadDialog();

        sendHttpRequestXML('GET', filepath).then(function (xmlDoc){

            var imageinfo = xmlDoc.getElementsByTagName('IMAGE_INFO');
            var fileurl = xmlDoc.getElementsByTagName("URL")[0].firstChild.nodeValue;
            var markupurls = xmlDoc.getElementsByTagName('MARKUP_URL');
            var saveurl = xmlDoc.getElementsByTagName('USER_MARKUPPOSTURL');
            //var xcmfiles = [];


            for (var i = 0; i < markupurls.length; i++) {

                byrefmarkupfiles[i] = markupurls[i].firstChild.nodeValue;

            }
            bUsemarkupbyrefEx = true;
            if (RxCore_GUI_Download != undefined) {
                RxCore_GUI_Download.setDownload("show");
            }

            var openfileobj = {filename: fileurl, displayname : null};
            getFile(openfileobj);

        }).catch(function(error){

           if(error.status){
            if (error.status == 404) {
                console.log("XML could not be found");
            } else if (error.status == 503) {
                console.log("Server is down");
            }
           }else{
               console.log(error);
           }



       });



        
    }

    function getxmlurl(filepath) {
        bAbortPageload = false;


        sendHttpRequestXML('GET', filepath).then(function (xmlDoc){

            var imageinfo = xmlDoc.getElementsByTagName('IMAGE_INFO');
                var fileurl = xmlDoc.getElementsByTagName("URL")[0].firstChild.nodeValue;
                var markupurls = xmlDoc.getElementsByTagName('MARKUP_URL');
                var saveurl = xmlDoc.getElementsByTagName('USER_MARKUPPOSTURL');
                //var xcmfiles = [];


                for (var i = 0; i < markupurls.length; i++) {

                    byrefmarkupfiles[i] = markupurls[i].firstChild.nodeValue;

                }
                
                bUsemarkupbyref = true;

                if (RxCore_GUI_Download != undefined) {
                    RxCore_GUI_Download.setDownload("show");
                }

                var openfileobj = {filename: fileurl, displayname : null};

                getFile(openfileobj);

        }).catch(function(error){

           if(error.status){
            if (error.status == 404) {
                
                console.log("XML could not be found");
            } else if (error.status == 503) {
                console.log("Server is down");
            }
           }else{
               console.log(error);
           }



       });



    }

    function getFileCustom(Filepath){
        
        bAbortPageload = false;
        /*if(bIsmobile){
            GetLicense();
        }*/

        if (!bLicenseAquired) {
            return;
        }

        if (RxCore_GUI_HasText != undefined){
            RxCore_GUI_HasText.hastext = false;
            RxCore_GUI_HasText.hasText(RxCore_GUI_HasText.hastext, true);
        }

        cntximg.clearRect(0, 0, canvas.width, canvas.height);
        context.clearRect(0, 0, canvas.width, canvas.height);
        contexto.clearRect(0, 0, canvas.width, canvas.height);
        numUsers = 0;
        //Userlist = [];
        
        clearUsers();

        addCurrentUser();

        //Userlist[0] = new Users(signature, DisplayName, markuplayer, markupcolor, true);
        //numUsers++;

        var dataxml = Filepath + "data.xml";
        var PageObj;


        sendHttpRequestXML('GET', dataxml).then(function (xmlDoc){

            var i, j = 0;

            if (documentopen && bSingleDocmode) {
                //RxCore_Closedocument();
                internalClosedoc();
            }

            DocXMLPrint = xmlDoc;
            var totalpages = 0;
            if (DocObj == null || DocObj == undefined) {
                nCurdocindex = OpenFiles.push(new DocumentObject(xmlDoc));

                DocObj = OpenFiles[nCurdocindex - 1];
                DocObj.setfileindex(nCurdocindex - 1);
                //DocObj.fileindex = nCurdocindex - 1;
                DocObj.relativepath = Filepath;

            } else {
                //DocObj.Suspend();
                nCurdocindex = OpenFiles.push(new DocumentObject(xmlDoc));
                DocObj.enableTextSelect(false);
                DocObj = OpenFiles[nCurdocindex - 1];
                //DocObj.fileindex = nCurdocindex - 1;
                DocObj.setfileindex(nCurdocindex - 1);
                DocObj.relativepath = Filepath;
            }

            var path = addSlash(DocObj.OriginalURL);

            var szformatshort = DocObj.Format.substring(0, 9);
            var bPDF = (szformatshort == "Adobe PDF");

    

            var layoutxmlobj = xmlDoc.getElementsByTagName('Layout');
            for (i = 0; i < layoutxmlobj.length; i++) {
                var layoutname = layoutxmlobj[i].getElementsByTagName("LayoutName")[0].firstChild.nodeValue;
                DocObj.addlayout(layoutname);
                var pagexmlobj = layoutxmlobj[i].getElementsByTagName('Page');
                for (j = 0; j < pagexmlobj.length; j++) {

                    if (j == 0 && i == 0) {
                        PageObj = new PageObject(pagexmlobj[j], layoutname, true, path, j, DocObj);
                        
                        DocObj.addthumbnail(new ThumbnailObject(pagexmlobj[j],layoutname ,DocObj, j));
                        
                        DocObj.addpage(PageObj, true);

                    } else {
                        
                        
                        DocObj.addthumbnail(new ThumbnailObject(pagexmlobj[j],layoutname, DocObj, j + totalpages));

                        if(j < nMaxpages || !bPDF){
                            PageObj = new PageObject(pagexmlobj[j], layoutname, false, '', j + totalpages, DocObj);
                            DocObj.addpage(PageObj, false);
                        }else{
                            DocObj.addpage(undefined, false);
                        }

                    }

                }
                totalpages += j;


            }

            //DocObj.FileName = szFileName;

            if(hasDocPage()){
                
                OpenFiles[nCurdocindex - 1] = DocObj;
                if (RxCore_GUI_pagethumbs != undefined) {
                    RxCore_GUI_pagethumbs.setThumbnails(DocObj.thumbnails);
                }
    
                var stateobj = {
                    iscompare:documentcompare,
                    numOpenFiles:OpenFiles.length,
                    isPDF:DocObj.getCurrentpage().usepdfjs,
                    is3D:DocObj.getCurrentpage().usevector3Dxml,
                    markupOn3D : false,
                    is2D:DocObj.getCurrentpage().usevectorxml,
                    numpages:DocObj.pages.length,
                    currentpage:DocObj.getcurPage(),
                    activefile : DocObj.bActive,
                    source : 'getfileCustom'
                };
    
                if (RxCore_GUI_State != undefined) {
                    RxCore_GUI_State.setGUIState(stateobj);
                }
    
                var pagingobject = {
                    numpages:DocObj.pages.length,
                    currentpage:DocObj.getcurPage()
                };
    
                if(RxCore_GUI_Page != undefined){
                    RxCore_GUI_Page.pageEvent(pagingobject);
                }
            }

            /*if(bIsmobile){
                FreeLicense();
            }*/


        }).catch(function(error){

            commonErrorCatch(error);

        });

        
        
    }


    function getfilePagesPDFdoc(Filepath,pdfdoc, pages, callback){
        bAbortPageload = false;
        
        if (!bLicenseAquired) {
            return;
        }

        if (RxCore_GUI_HasText != undefined){
            RxCore_GUI_HasText.hastext = false;
            RxCore_GUI_HasText.hasText(RxCore_GUI_HasText.hastext, true);

        }

        var szFileName = getFileName(Filepath);


        cntximg.clearRect(0, 0, canvas.width, canvas.height);
        context.clearRect(0, 0, canvas.width, canvas.height);
        contexto.clearRect(0, 0, canvas.width, canvas.height);
        numUsers = 0;

        clearUsers();
        addCurrentUser();

        //Userlist = [];
        //Userlist[0] = new Users(signature, DisplayName, markuplayer, markupcolor);
        //numUsers++;

        var PageObj;

        var XMLGetFile = "<?xml version=\"1.0\" encoding=\"UTF-8\"?>";
        XMLGetFile += "<RxViewServer>";
        XMLGetFile += "<Command>GetFile</Command>";
        XMLGetFile += "<LicenseID>" + LicenseID + "</LicenseID>";
        XMLGetFile += "<FileURL>" + Filepath + "</FileURL>";
        XMLGetFile += "</RxViewServer>";

        var callbacklist = [
            {event : "callback", func : callback}
        ];


        sendHttpRequestXML('POST', xmlurl, XMLGetFile, callbacklist).then(function (xmlDoc){

            var i, j = 0;

            if (RxCore_GUI_Download != undefined) {
                RxCore_GUI_Download.setDownload("hide");
            }

            var totalpages = 0;
            var pagesadded = 0;
            if (DocObj == null || DocObj == undefined) {
                nCurdocindex = OpenFiles.push(new DocumentObject(xmlDoc));
                DocObj = OpenFiles[nCurdocindex - 1];
                //DocObj.fileindex = nCurdocindex - 1;
                DocObj.setfileindex(nCurdocindex - 1);
                DocObj.usepagesload = true;

            } else {

                nCurdocindex = OpenFiles.push(new DocumentObject(xmlDoc));
                DocObj = OpenFiles[nCurdocindex - 1];
                //DocObj.fileindex = nCurdocindex - 1;
                DocObj.setfileindex(nCurdocindex - 1);
                DocObj.usepagesload = true;
            }

            DocObj.setPDFdocument(pdfdoc, false);

            var path = addSlash(DocObj.OriginalURL);
            var layoutxmlobj = xmlDoc.getElementsByTagName('Layout');

            for (i = 0; i < layoutxmlobj.length; i++) {
                var layoutname = layoutxmlobj[i].getElementsByTagName("LayoutName")[0].firstChild.nodeValue;
                DocObj.addlayout(layoutname);
                var pagexmlobj = layoutxmlobj[i].getElementsByTagName('Page');

                //pages

                for (j = 0; j < pagexmlobj.length; j++) {

                    if (j == 0 && i == 0) {

                        if (pages.indexOf(j) >= 0){
                            //PageObj = new PageObject(pagexmlobj[j], layoutname, true, path, j, DocObj);
                            DocObj.addthumbnail(new ThumbnailObject(pagexmlobj[j],layoutname, DocObj, j));
                            DocObj.addpage(new PageObject(pagexmlobj[j], layoutname, true, path, j, DocObj), true);
                            pagesadded ++;
                        }
                    } else {
                        if (pages.indexOf(j) >= 0){
                            
                            
                            if(pagesadded == 0){
                            
                                //PageObj = new PageObject(pagexmlobj[j], layoutname, true, path, j + totalpages, DocObj);
                                DocObj.addpage(new PageObject(pagexmlobj[j], layoutname, true, path, j + totalpages, DocObj), true);
                                
                                DocObj.addthumbnail(new ThumbnailObject(pagexmlobj[j],layoutname, DocObj, j + totalpages));
                                pagesadded ++;

                            }else{
                                //PageObj = new PageObject(pagexmlobj[j], layoutname, false, '', j + totalpages, DocObj);
                                DocObj.addpage(new PageObject(pagexmlobj[j], layoutname, false, '', j + totalpages, DocObj), false);
                                DocObj.addthumbnail(new ThumbnailObject(pagexmlobj[j],layoutname, DocObj, j + totalpages));
                                
                                pagesadded ++;
                            }
                        }
                    }

                }
                totalpages += j;

            }



            if(hasDocPage()){
                DocObj.FileName = szFileName;


                OpenFiles[nCurdocindex - 1] = DocObj;
                if (RxCore_GUI_pagethumbs != undefined) {
                    RxCore_GUI_pagethumbs.setThumbnails(DocObj.thumbnails);
                }
    
            }

            //bsetcallbackfilepages = true;


            /*var stateobj = {
                iscompare:documentcompare,
                numOpenFiles:OpenFiles.length,
                isPDF:DocObj.getCurrentpage().usepdfjs,
                is3D:DocObj.getCurrentpage().usevector3Dxml,
                is2D:DocObj.getCurrentpage().usevectorxml,
                numpages:DocObj.pages.length,
                currentpage:DocObj.getcurPage(),
                activefile : DocObj.bActive,
                source : 'getfilePagesPDFdoc'
            };

            if (RxCore_GUI_State != undefined) {
                RxCore_GUI_State.setGUIState(stateobj);
            }*/

            /*var pagingobject = {
                numpages:DocObj.pages.length,
                currentpage:DocObj.getcurPage()
            };*/

            /*if(RxCore_GUI_Page != undefined){
                RxCore_GUI_Page.pageEvent(pagingobject);
            }*/


        }).catch(function(error){

            commonErrorCatchEx("getfilePagesPDFdoc", error);
            
        });
     
    }

    function switchDocPages(Filepath, pages, callback){

        if (RxCore_GUI_HasText != undefined){
            RxCore_GUI_HasText.hastext = false;
            RxCore_GUI_HasText.hasText(RxCore_GUI_HasText.hastext, true);

        }

        var szFileName = getFileName(Filepath);

        cntximg.clearRect(0, 0, canvas.width, canvas.height);
        context.clearRect(0, 0, canvas.width, canvas.height);
        contexto.clearRect(0, 0, canvas.width, canvas.height);

        var PageObj;

        var XMLGetFile = "<?xml version=\"1.0\" encoding=\"UTF-8\"?>";
        XMLGetFile += "<RxViewServer>";
        XMLGetFile += "<Command>GetFile</Command>";
        XMLGetFile += "<LicenseID>" + LicenseID + "</LicenseID>";
        XMLGetFile += "<FileURL>" + Filepath + "</FileURL>";
        XMLGetFile += "</RxViewServer>";

        var callbacklist = [
            {event : "callback", func : callback}
        ];

        sendHttpRequestXML('POST', xmlurl, XMLGetFile, callbacklist).then(function (xmlDoc){

            var i, j = 0;


            if (RxCore_GUI_Download != undefined) {
                RxCore_GUI_Download.setDownload("hide");
            }

            var totalpages = 0;
            var pagesadded = 0;
            if (DocObj == null || DocObj == undefined) {
                return;
            }else{
                DocObj.closePages(false);
                //close existing page(s)
            }

            var path = addSlash(DocObj.OriginalURL);
            var layoutxmlobj = xmlDoc.getElementsByTagName('Layout');

            for (i = 0; i < layoutxmlobj.length; i++) {
                var layoutname = layoutxmlobj[i].getElementsByTagName("LayoutName")[0].firstChild.nodeValue;
                //DocObj.addlayout(layoutname);
                var pagexmlobj = layoutxmlobj[i].getElementsByTagName('Page');

                //pages

                for (j = 0; j < pagexmlobj.length; j++) {

                    if (j == 0 && i == 0) {

                        if (pages.indexOf(j) >= 0){
                            PageObj = new PageObject(pagexmlobj[j], layoutname, false, path, j, DocObj);
                            
                            DocObj.addthumbnail(new ThumbnailObject(pagexmlobj[j],layoutname, DocObj, j));
                            DocObj.addpage(PageObj, true);
                            pagesadded ++;
                        }
                    } else {
                        if (pages.indexOf(j) >= 0){
                            if(pagesadded == 0){
                                PageObj = new PageObject(pagexmlobj[j], layoutname, false, path, j + totalpages, DocObj);
                                
                                DocObj.addthumbnail(new ThumbnailObject(pagexmlobj[j],layoutname, DocObj,  j + totalpages));
                                DocObj.addpage(PageObj, true);
                                pagesadded ++;

                            }else{
                                PageObj = new PageObject(pagexmlobj[j], layoutname, false, '', j + totalpages, DocObj);
                                
                                DocObj.addthumbnail(new ThumbnailObject(pagexmlobj[j],layoutname, DocObj, j + totalpages));
                                DocObj.addpage(PageObj, false);
                                pagesadded ++;
                            }
                        }
                    }

                }
                totalpages += j;

            }

            if(hasDocPage()){
                if(PageObj.usepdfjs){
                
                    DocObj.loadPDFpages();
                    
                }
    
                if (RxCore_GUI_pagethumbs != undefined) {
                    RxCore_GUI_pagethumbs.setThumbnails(DocObj.thumbnails);
                }
    
                var stateobj = {
                    iscompare:documentcompare,
                    numOpenFiles:OpenFiles.length,
                    isPDF:DocObj.getCurrentpage().usepdfjs,
                    is3D:DocObj.getCurrentpage().usevector3Dxml,
                    markupOn3D : false,
                    is2D:DocObj.getCurrentpage().usevectorxml,
                    numpages:DocObj.pages.length,
                    currentpage:DocObj.getcurPage(),
                    activefile : DocObj.bActive,
                    source : 'switchdocpages' 
                };
    
                if (RxCore_GUI_State != undefined) {
                    RxCore_GUI_State.setGUIState(stateobj);
                }
    
                var pagingobject = {
                    numpages:DocObj.pages.length,
                    currentpage:DocObj.getcurPage()
                };
    
                if(RxCore_GUI_Page != undefined){
                    RxCore_GUI_Page.pageEvent(pagingobject);
                }
            }

            


        }).catch(function(error){
            
            commonErrorCatchEx("switchDocPages", error);

        });


    }


    function getszFilename(filepath){

        
        if(Filepath.displayname != null){
            var szFileName = Filepath.displayname;
        }else{
            szFileName = getFileName(Filepath.filename);
        }

        return szFileName;
    }

    function getFileCommand(Filepath){


        var XMLGetFile = "<?xml version=\"1.0\" encoding=\"UTF-8\"?>";
        XMLGetFile += "<RxViewServer>";
        XMLGetFile += "<Command>GetFile</Command>";
        XMLGetFile += "<LicenseID>" + LicenseID + "</LicenseID>";
        XMLGetFile += "<FileURL>" + Filepath.filename + "</FileURL>";

        if(Filepath.xrefurl != null){
            XMLGetFile += "<XREFURL>" + Filepath.xrefurl + "</XREFURL>";
        }

        if(Filepath.cacheid != null){
            XMLGetFile += "<CacheID>" + Filepath.cacheid + "</CacheID>";
        }

        //openfileobj.mime = fileurl.mime;
        
        if(Filepath.mime != null){
            XMLGetFile += "<Mime>" + Filepath.mime + "</Mime>";
        }

        if(Filepath.auth != null){
            XMLGetFile += "<AutHeader>" + Filepath.auth + "</AutHeader>";
        }


        if(Filepath.password != null){
            XMLGetFile += "<password>" + Filepath.password + "</password>";
        }

        XMLGetFile += "</RxViewServer>";

        return XMLGetFile;

    }

    function getFilePagesEx(Filepath, pages, blayout, callback){

        bAbortPageload = false;
        /*if(bIsmobile){
            GetLicense();
        }*/

        //var TDocObj;
        if (!bLicenseAquired) {
            return;
        }

        if (RxCore_GUI_HasText != undefined){
            RxCore_GUI_HasText.hastext = false;
            RxCore_GUI_HasText.hasText(RxCore_GUI_HasText.hastext, true);

        }

        if(Filepath.displayname != null){
            var szFileName = Filepath.displayname;
        }else{
            szFileName = getFileName(Filepath.filename);
        }


        //var szFileName = getszFilename(Filepath);

        var XMLGetFile = getFileCommand(Filepath);

        //var szFileName = getFileName(Filepath);


        cntximg.clearRect(0, 0, canvas.width, canvas.height);
        context.clearRect(0, 0, canvas.width, canvas.height);
        contexto.clearRect(0, 0, canvas.width, canvas.height);
        //numUsers = 0;

        clearUsers();
        addCurrentUser();


        //Userlist = [];
        //Userlist[0] = new Users(signature, DisplayName, markuplayer, markupcolor);
        //numUsers++;

        var PageObj;
       

        var callbacklist = [
            {event : "callback", func : callback}
        ];

        sendHttpRequestXML('POST', xmlurl, XMLGetFile, callbacklist).then(function (xmlDoc){

            var i, j = 0;


            if (RxCore_GUI_Download != undefined) {
                RxCore_GUI_Download.setDownload("hide");
            }

            var totalpages = 0;
            var pagesadded = 0;
            if (DocObj == null || DocObj == undefined) {
                nCurdocindex = OpenFiles.push(new DocumentObject(xmlDoc));
                DocObj = OpenFiles[nCurdocindex - 1];
                //DocObj.fileindex = nCurdocindex - 1;
                DocObj.setfileindex(nCurdocindex - 1);
                DocObj.usepagesload = true;

            } else {

                nCurdocindex = OpenFiles.push(new DocumentObject(xmlDoc));
                DocObj = OpenFiles[nCurdocindex - 1];
                //DocObj.fileindex = nCurdocindex - 1;
                DocObj.setfileindex(nCurdocindex - 1);
                DocObj.usepagesload = true;
            }

            var path = addSlash(DocObj.OriginalURL);
            var layoutxmlobj = xmlDoc.getElementsByTagName('Layout');

            for (i = 0; i < layoutxmlobj.length; i++) {
                var layoutname = layoutxmlobj[i].getElementsByTagName("LayoutName")[0].firstChild.nodeValue;
                DocObj.addlayout(layoutname);
                var pagexmlobj = layoutxmlobj[i].getElementsByTagName('Page');

                //pages
                if(pages.indexOf(i) >= 0){
                    for (j = 0; j < pagexmlobj.length; j++) {

                        if (j == 0 && i == 0) {
    
                            var layoutexist = blayout && (pages.indexOf(i) >= 0);
    
                            if (pages.indexOf(j) >= 0 || blayout){
                            //if (pages.indexOf(j + totalpages) >= 0){
                                PageObj = new PageObject(pagexmlobj[j], layoutname, true, path, j, DocObj);
                                DocObj.addpage(PageObj, true);
                                DocObj.addthumbnail(new ThumbnailObject(pagexmlobj[j],layoutname, DocObj, j));
                                pagesadded ++;
                            }
                        } else {
                            if (pages.indexOf(j) >= 0 || blayout){
                                if(pagesadded == 0){
                                    PageObj = new PageObject(pagexmlobj[j], layoutname, true, path, j + totalpages, DocObj);
                                    
                                    DocObj.addthumbnail(new ThumbnailObject(pagexmlobj[j], layoutname, DocObj, j + totalpages));
                                    
                                    DocObj.addpage(PageObj, true);
                                    pagesadded ++;
    
                                }else{
                                    PageObj = new PageObject(pagexmlobj[j], layoutname, false, '', j + totalpages, DocObj);
                                    
                                    DocObj.addthumbnail(new ThumbnailObject(pagexmlobj[j],layoutname, DocObj, j + totalpages));
                                    DocObj.addpage(PageObj, false);
                                    pagesadded ++;
                                }
                            }
                        }
    
                    }
                }

                totalpages += j;

            }


            if(hasDocPage()){
                DocObj.FileName = szFileName;

                OpenFiles[nCurdocindex - 1] = DocObj;
                if (RxCore_GUI_pagethumbs != undefined) {
                    RxCore_GUI_pagethumbs.setThumbnails(DocObj.thumbnails);
                }
                    
                var stateobj = {
                    iscompare:documentcompare,
                    numOpenFiles:OpenFiles.length,
                    isPDF:DocObj.getCurrentpage().usepdfjs,
                    is3D:DocObj.getCurrentpage().usevector3Dxml,
                    markupOn3D : false,
                    is2D:DocObj.getCurrentpage().usevectorxml,
                    numpages:DocObj.pages.length,
                    currentpage:DocObj.getcurPage(),
                    activefile : DocObj.bActive,
                    source : 'getfilepages' 
                };

                if (RxCore_GUI_State != undefined) {
                    RxCore_GUI_State.setGUIState(stateobj);
                }
    
                var pagingobject = {
                    numpages:DocObj.pages.length,
                    currentpage:DocObj.getcurPage()
                };
    
                if(RxCore_GUI_Page != undefined){
                    RxCore_GUI_Page.pageEvent(pagingobject);
                }

            }else{
                var stateobj = {
                    iscompare:documentcompare,
                    numOpenFiles:OpenFiles.length,
                    isPDF:false,
                    is3D:false,
                    markupOn3D : false,
                    is2D:false,
                    numpages:DocObj.pages.length,
                    currentpage:0,
                    activefile : DocObj.bActive,
                    source : 'getfilepages'
                };

                if (RxCore_GUI_State != undefined) {
                    RxCore_GUI_State.setGUIState(stateobj);
                }

            }




        }).catch(function(error){

            commonErrorCatch(error);

        });
        

    }

    function getFilePages(Filepath, pages, blayout, callback){
        bAbortPageload = false;
        /*if(bIsmobile){
            GetLicense();
        }*/

        //var TDocObj;
        if (!bLicenseAquired) {
            return;
        }

        if (RxCore_GUI_HasText != undefined){
            RxCore_GUI_HasText.hastext = false;
            RxCore_GUI_HasText.hasText(RxCore_GUI_HasText.hastext, true);

        }

        var szFileName = getFileName(Filepath);


        cntximg.clearRect(0, 0, canvas.width, canvas.height);
        context.clearRect(0, 0, canvas.width, canvas.height);
        contexto.clearRect(0, 0, canvas.width, canvas.height);

        clearUsers();
        addCurrentUser();

        //numUsers = 0;
        //Userlist = [];
        //Userlist[0] = new Users(signature, DisplayName, markuplayer, markupcolor);
        //numUsers++;

        var PageObj;

        var XMLGetFile = "<?xml version=\"1.0\" encoding=\"UTF-8\"?>";
        XMLGetFile += "<RxViewServer>";
        XMLGetFile += "<Command>GetFile</Command>";
        XMLGetFile += "<LicenseID>" + LicenseID + "</LicenseID>";
        XMLGetFile += "<FileURL>" + Filepath + "</FileURL>";
        XMLGetFile += "</RxViewServer>";

        var callbacklist = [
            {event : "callback", func : callback}
        ];

        sendHttpRequestXML('POST', xmlurl, XMLGetFile, callbacklist).then(function (xmlDoc){

            var i, j = 0;


            if (RxCore_GUI_Download != undefined) {
                RxCore_GUI_Download.setDownload("hide");
            }

            var totalpages = 0;
            var pagesadded = 0;
            if (DocObj == null || DocObj == undefined) {
                nCurdocindex = OpenFiles.push(new DocumentObject(xmlDoc));
                DocObj = OpenFiles[nCurdocindex - 1];
                //DocObj.fileindex = nCurdocindex - 1;
                DocObj.setfileindex(nCurdocindex - 1);
                DocObj.usepagesload = true;

            } else {

                nCurdocindex = OpenFiles.push(new DocumentObject(xmlDoc));
                
                DocObj.enableTextSelect(false);

                DocObj = OpenFiles[nCurdocindex - 1];
                //DocObj.fileindex = nCurdocindex - 1;
                DocObj.setfileindex(nCurdocindex - 1);
                DocObj.usepagesload = true;
            }

            var path = addSlash(DocObj.OriginalURL);
            var layoutxmlobj = xmlDoc.getElementsByTagName('Layout');

            for (i = 0; i < layoutxmlobj.length; i++) {
                var layoutname = layoutxmlobj[i].getElementsByTagName("LayoutName")[0].firstChild.nodeValue;
                DocObj.addlayout(layoutname);
                var pagexmlobj = layoutxmlobj[i].getElementsByTagName('Page');

                //pages
                if(pages.indexOf(i) >= 0){
                    for (j = 0; j < pagexmlobj.length; j++) {

                        if (j == 0 && i == 0) {
    
                            var layoutexist = blayout && (pages.indexOf(i) >= 0);
    
                            if (pages.indexOf(j) >= 0 || blayout){
                            //if (pages.indexOf(j + totalpages) >= 0){
                                PageObj = new PageObject(pagexmlobj[j], layoutname, true, path, j, DocObj);
                                DocObj.addpage(PageObj, true);
                                DocObj.addthumbnail(new ThumbnailObject(pagexmlobj[j],layoutname, DocObj, j));
                                pagesadded ++;
                            }
                        } else {
                            if (pages.indexOf(j) >= 0 || blayout){
                                if(pagesadded == 0){
                                    PageObj = new PageObject(pagexmlobj[j], layoutname, true, path, j + totalpages, DocObj);
                                    
                                    DocObj.addthumbnail(new ThumbnailObject(pagexmlobj[j], layoutname, DocObj, j + totalpages));
                                    
                                    DocObj.addpage(PageObj, true);
                                    pagesadded ++;
    
                                }else{
                                    PageObj = new PageObject(pagexmlobj[j], layoutname, false, '', j + totalpages, DocObj);
                                    
                                    DocObj.addthumbnail(new ThumbnailObject(pagexmlobj[j],layoutname, DocObj, j + totalpages));
                                    DocObj.addpage(PageObj, false);
                                    pagesadded ++;
                                }
                            }
                        }
    
                    }
                }

                totalpages += j;

            }


            if(hasDocPage()){
                DocObj.FileName = szFileName;

                OpenFiles[nCurdocindex - 1] = DocObj;
                if (RxCore_GUI_pagethumbs != undefined) {
                    RxCore_GUI_pagethumbs.setThumbnails(DocObj.thumbnails);
                }
                    
                var stateobj = {
                    iscompare:documentcompare,
                    numOpenFiles:OpenFiles.length,
                    isPDF:DocObj.getCurrentpage().usepdfjs,
                    is3D:DocObj.getCurrentpage().usevector3Dxml,
                    markupOn3D : false,
                    is2D:DocObj.getCurrentpage().usevectorxml,
                    numpages:DocObj.pages.length,
                    currentpage:DocObj.getcurPage(),
                    activefile : DocObj.bActive,
                    source : 'getfilepages' 
                };

                if (RxCore_GUI_State != undefined) {
                    RxCore_GUI_State.setGUIState(stateobj);
                }
    
                var pagingobject = {
                    numpages:DocObj.pages.length,
                    currentpage:DocObj.getcurPage()
                };
    
                if(RxCore_GUI_Page != undefined){
                    RxCore_GUI_Page.pageEvent(pagingobject);
                }

            }else{
                var stateobj = {
                    iscompare:documentcompare,
                    numOpenFiles:OpenFiles.length,
                    isPDF:false,
                    is3D:false,
                    markupOn3D : false,
                    is2D:false,
                    numpages:DocObj.pages.length,
                    currentpage:0,
                    activefile : DocObj.bActive,
                    source : 'getfilepages'  
                };

                if (RxCore_GUI_State != undefined) {
                    RxCore_GUI_State.setGUIState(stateobj);
                }

            }




        }).catch(function(error){

            commonErrorCatchEx("getFilePages", error);

        });

    }

    /* Andriy added callback to getFile */

    function updatelocalDoc(Filepath, docindex){
        bAbortPageload = false;

        if (!bLicenseAquired) {
            return;
        }

        if(Filepath.displayname != null){
            var szFileName = Filepath.displayname;
        }else{
            szFileName = getFileName(Filepath.filename);
        }

        var XMLGetFile = "<?xml version=\"1.0\" encoding=\"UTF-8\"?>";
        XMLGetFile += "<RxViewServer>";
        XMLGetFile += "<Command>GetFile</Command>";
        XMLGetFile += "<LicenseID>" + LicenseID + "</LicenseID>";
        XMLGetFile += "<FileURL>" + Filepath.filename + "</FileURL>";
        XMLGetFile += "</RxViewServer>";


        sendHttpRequestXML('POST', xmlurl, XMLGetFile).then(function (xmlDoc){

            if(OpenFiles.length > 0){
                OpenFiles[docindex].updateURL(xmlDoc);
            }

            var path = addSlash(DocObj.OriginalURL);

            var szformatshort = DocObj.Format.substring(0, 9);
            var bPDF = (szformatshort == "Adobe PDF");

            //var layoutxmlobj = xmlDoc.getElementsByTagName('Layout');

            DocObj.FileName = szFileName;


        }).catch(function(error){

            commonErrorCatch(error);
            
            
        });

    }
    
    function getFileaddtoGLTF(Filepath){
        
        var PageObj;
                

        var XMLGetFile = "<?xml version=\"1.0\" encoding=\"UTF-8\"?>";
        XMLGetFile += "<RxViewServer>";
        XMLGetFile += "<Command>GetFile</Command>";
        XMLGetFile += "<LicenseID>" + LicenseID + "</LicenseID>";
        XMLGetFile += "<FileURL>" + Filepath + "</FileURL>";
        XMLGetFile += "</RxViewServer>";


        sendHttpRequestXML('POST', xmlurl, XMLGetFile).then(function (xmlDoc){
        
            var i, j = 0;

            var totalpages = 0;

            var relpath = xmlurlrel;

            var layoutxmlobj = xmlDoc.getElementsByTagName('Layout');
            for (i = 0; i < layoutxmlobj.length; i++) {
                var layoutname = layoutxmlobj[i].getElementsByTagName("LayoutName")[0].firstChild.nodeValue;
                //DocObj.addlayout(layoutname);

                var pagexmlobj = layoutxmlobj[i].getElementsByTagName('Page');

                for (j = 0; j < pagexmlobj.length; j++) {

                    if (j == 0 && i == 0) {
                        //PageObj = new PageObject(pagexmlobj[j], layoutname, true, path, j, DocObj);
                        //DocObj.addthumbnail(new ThumbnailObject(pagexmlobj[j],layoutname, DocObj,  j));
                        //DocObj.addpage(PageObj, true);
                        //<VectorGLTF3DSRC>/cache/20160125westriverside hospital-ifc2x3-autodesk_hospital_metric_plumbing_2015-17d7408-5172689d/1_1.glb</VectorGLTF3DSRC>
                        if (pagexmlobj[j].getElementsByTagName("VectorGLTF3DSRC")[0] != undefined) {
                            var Vector3DBinarySRC = encodeURI(relpath + pagexmlobj[j].getElementsByTagName("VectorGLTF3DSRC")[0].firstChild.nodeValue);
                            var VectorMetadataSRC = encodeURI(relpath + pagexmlobj[j].getElementsByTagName("VectorMetadataSRC")[0].firstChild.nodeValue);

                            DocObj.getCurrentpage().addtoGLTFScene(Vector3DBinarySRC, VectorMetadataSRC);
                        }

                    } 

                }

                totalpages += j;

            }
        
        }).catch(function(error){

            commonErrorCatchEx("getFileaddtoGLTF", error);
  
        }); 

        
    }

    function createContent(Filepath, callback, progressCallBack){
        bAbortPageload = false;
        
        /*if(bIsmobile){
            Get_License();
        }*/

        //var TDocObj;
        if (!bLicenseAquired) {

            if (RxCore_GUI_Download != undefined) {
                RxCore_GUI_Download.setDownload("hide");
            }
            return;
        }

        var XMLGetFile = "<?xml version=\"1.0\" encoding=\"UTF-8\"?>";
        XMLGetFile += "<RxViewServer>";
        XMLGetFile += "<Command>GetFile</Command>";
        XMLGetFile += "<LicenseID>" + LicenseID + "</LicenseID>";
        XMLGetFile += "<FileURL>" + Filepath.filename + "</FileURL>";

        if(Filepath.xrefurl != null){
            XMLGetFile += "<XREFURL>" + Filepath.xrefurl + "</XREFURL>";
        }

        
        if(Filepath.cacheid != null){
            XMLGetFile += "<CacheID>" + Filepath.cacheid + "</CacheID>";
        }

        //openfileobj.mime = fileurl.mime;
        
        if(Filepath.mime != null){
            XMLGetFile += "<Mime>" + Filepath.mime + "</Mime>";
        }

        if(Filepath.auth != null){
            XMLGetFile += "<AutHeader>" + Filepath.auth + "</AutHeader>";
        }

        if(Filepath.password != null){
            XMLGetFile += "<password>" + Filepath.password + "</password>";
        }

        if(Filepath.refresh){

            XMLGetFile += "<RefreshCache>" + 1 + "</RefreshCache>";
            
        }

        XMLGetFile += "</RxViewServer>";


        //XMLGetFile += "<FileURL>" + Filepath.filename + "</FileURL>";

        var callbacklist = [
            {event : "callback", func : callback},
            {event : "progressCallBack", func : progressCallBack}
        ];

        sendHttpRequestXML('POST', xmlurl, XMLGetFile, callbacklist).then(function (xmlDoc){

                        
            var xmltext = new XMLSerializer().serializeToString(xmlDoc);
            //var xmlcontent = new XMLSerializer().serializeToString(xmlDoc.documentElement);

            if (RxCore_GUI_CreationComplete != undefined) {
                RxCore_GUI_CreationComplete.setXMLreturn(xmlDoc, xmltext);
            }

            
            //DocXMLPrint = xmlDoc;

            
        }).catch(function(error){

            commonErrorCatchEx("createContent", error);

        });



        

    }    

    function getFile(Filepath, callback, progressCallBack) {
        bAbortPageload = false;
        
        /*if(bIsmobile){
            GetLicense();
        }*/

        //var TDocObj;
        if (!bLicenseAquired) {

            if (RxCore_GUI_Download != undefined) {
                RxCore_GUI_Download.setDownload("hide");
            }

            return;
        }
        //showDownloadDialog();
       /*if (RxCore_GUI_Download != undefined) {
            RxCore_GUI_Download.setDownload("show");
       }*/

        if (RxCore_GUI_HasText != undefined){
            RxCore_GUI_HasText.hastext = false;
            RxCore_GUI_HasText.hasText(RxCore_GUI_HasText.hastext, true);

        }

        if(Filepath.displayname != null){
            var szFileName = Filepath.displayname;
        }else{
            szFileName = getFileName(Filepath.filename);
        }
        

        cntximg.clearRect(0, 0, canvas.width, canvas.height);
        context.clearRect(0, 0, canvas.width, canvas.height);
        contexto.clearRect(0, 0, canvas.width, canvas.height);

        clearUsers();
        addCurrentUser();


        /*numUsers = 0;
        Userlist = [];
        Userlist[0] = new Users(signature, DisplayName, markuplayer, markupcolor);
        numUsers++;*/
        
        var PageObj;
        
        

        var XMLGetFile = "<?xml version=\"1.0\" encoding=\"UTF-8\"?>";
        XMLGetFile += "<RxViewServer>";
        XMLGetFile += "<Command>GetFile</Command>";
        XMLGetFile += "<LicenseID>" + LicenseID + "</LicenseID>";
        XMLGetFile += "<FileURL>" + Filepath.filename + "</FileURL>";

        if(Filepath.xrefurl != null){
            XMLGetFile += "<XREFURL>" + Filepath.xrefurl + "</XREFURL>";
        }

        if(Filepath.cacheid != null){
            XMLGetFile += "<CacheID>" + Filepath.cacheid + "</CacheID>";
        }

        //openfileobj.mime = fileurl.mime;
        
        if(Filepath.mime != null){
            XMLGetFile += "<Mime>" + Filepath.mime + "</Mime>";
        }

        if(Filepath.auth != null){
            XMLGetFile += "<AutHeader>" + Filepath.auth + "</AutHeader>";
        }


        if(Filepath.password != null){
            XMLGetFile += "<password>" + Filepath.password + "</password>";
        }

        
        if(Filepath.displayname != null){
            XMLGetFile += "<displayname>" + Filepath.displayname + "</displayname>";
        }
        
        
        XMLGetFile += "</RxViewServer>";



        var callbacklist = [
            {event : "callback", func : callback},
            {event : "progressCallBack", func : progressCallBack}
        ];

        sendHttpRequestXML('POST', xmlurl, XMLGetFile, callbacklist).then(function (xmlDoc){
        
            var i, j = 0;

            if (documentopen && bSingleDocmode) {
                //RxCore_Closedocument();
                internalClosedoc();
            }

            /*if (RxCore_GUI_Download != undefined) {
                RxCore_GUI_Download.setDownload("hide");
            }*/

            //ThumbnailpagesContainer.setEmpty();
            //save for use with print.
            DocXMLPrint = xmlDoc;

            if (xmlDoc.getElementsByTagName( 'Error' ).length != 0) {
                if(xmlDoc.getElementsByTagName( 'Error' )[0].firstChild != undefined){
                    var resp = xmlDoc.getElementsByTagName( 'Error' )[0].firstChild.nodeValue;
                    password_required(resp, Filepath);
                    return;
                }

            }

            var totalpages = 0;
            if (DocObj == null || DocObj == undefined) {
                nCurdocindex = OpenFiles.push(new DocumentObject(xmlDoc));
                DocObj = OpenFiles[nCurdocindex - 1];
                //DocObj.fileindex = nCurdocindex - 1;
                DocObj.setfileindex(nCurdocindex - 1);

            } else {
                //DocObj.Suspend();
                nCurdocindex = OpenFiles.push(new DocumentObject(xmlDoc));
                
                DocObj.enableTextSelect(false);

                DocObj = OpenFiles[nCurdocindex - 1];
                //DocObj.fileindex = nCurdocindex - 1;
                DocObj.setfileindex(nCurdocindex - 1);
            }

            if(Filepath.password != null){
                DocObj.setPassword(Filepath.password);
                //XMLGetFile += "<password>" + Filepath.password + "</password>";
            }

            DocObj.ScrollBarCheck();

            if(DocObj.OriginalURL != undefined){
                var path = addSlash(DocObj.OriginalURL);
            }
            

            var szformatshort = DocObj.Format.substring(0, 9);
            var bPDF = (szformatshort == "Adobe PDF");

            DocObj.openFileName = szFileName;
            
            //szFileName

            var layoutxmlobj = xmlDoc.getElementsByTagName('Layout');


            for (i = 0; i < layoutxmlobj.length; i++) {
                var layoutname = layoutxmlobj[i].getElementsByTagName("LayoutName")[0].firstChild.nodeValue;
                
                DocObj.addlayout(layoutname);

                var pagexmlobj = layoutxmlobj[i].getElementsByTagName('Page');

                for (j = 0; j < pagexmlobj.length; j++) {

                    if (j == 0 && i == 0) {
                        PageObj = new PageObject(pagexmlobj[j], layoutname, true, path, j, DocObj);
                        var tempPageXML = pagexmlobj[j];
                        var tempLayoutName = layoutname;
                        DocObj.addthumbnail(new ThumbnailObject(pagexmlobj[j],layoutname, DocObj,  j));
                        DocObj.addpage(PageObj, true);
                    } else {
                        
                        DocObj.addthumbnail(new ThumbnailObject(pagexmlobj[j],layoutname, DocObj, j + totalpages));

                        if(j < nMaxpages || !bPDF){
                            PageObj = new PageObject(pagexmlobj[j], layoutname, false, '', j + totalpages, DocObj);
                            
                            DocObj.addpage(PageObj, false);
                        }else{
                            DocObj.addpage(undefined, false);
                        }


                    }

                }
                totalpages += j;

            }

            if(hasDocPage()){
                if(bPDF && tempPageXML && tempLayoutName){

                    DocObj.tempPage = new PageObject(tempPageXML, tempLayoutName, false, '', 0, DocObj);
                }
    
    
                DocObj.FileName = szFileName;
                
    
                //var szstyle = "width: 200px; height: " + canvasoheight + "px; position:relative; top:-100px; left:15px"
    
                //Thumbnailpopup = DocObj.thumbnailhtmlsource;
                //layerhtmlsource
                //setContentThumb(Thumbnailpopup);
    
                //ThumbnailpagesContainer.innerHTML = Thumbnailpopup;
                //ThumbnailpagesContainer.Setheight(canvasoheight);
                //ThumbnailpagesContainer.setContent();
    
                OpenFiles[nCurdocindex - 1] = DocObj;
                if (RxCore_GUI_pagethumbs != undefined) {
                    RxCore_GUI_pagethumbs.setThumbnails(DocObj.thumbnails);
                }
    
                var stateobj = {
                    iscompare:documentcompare,
                    numOpenFiles:OpenFiles.length,
                    isPDF:DocObj.getCurrentpage().usepdfjs,
                    is3D:DocObj.getCurrentpage().usevector3Dxml,
                    markupOn3D : false,
                    is2D:DocObj.getCurrentpage().usevectorxml,
                    numpages:DocObj.pages.length,
                    currentpage:DocObj.getcurPage(),
                    activefile : DocObj.bActive,
                    source : 'getfile' 
                };
    
                if (RxCore_GUI_State != undefined) {
                    RxCore_GUI_State.setGUIState(stateobj);
                }
    
                var pagingobject = {
                    numpages:DocObj.pages.length,
                    currentpage:DocObj.getcurPage()
                };
    
                if(RxCore_GUI_Page != undefined){
                    RxCore_GUI_Page.pageEvent(pagingobject);
                }
            }

            

            //ThumbnailpagesContainer.parentNode.parentNode.style.height = canvasoheight + "px";
            //DocObj.getCurrentpage().loadimages();
            //documentopen = true;
            //hideDownloadDialog();
            //var path = addSlash(DocObj.OriginalURL);
            //var path = getPath(DocObj.OriginalURL);
            //getMarkupFilelist(path);
            
            //moved to attempt to secure that file is loaded.
            //var path = addSlash(DocObj.OriginalURL);
            //getMarkupFilelist(path);

            /*if(bIsmobile){
                FreeLicense();
            }*/
        
        }).catch(function(error){

            commonErrorCatchEx("getFile", error);

        });


    }

    function getFileNotActive(Filepath, callback, progressCallBack) {
        bAbortPageload = false;
        
        /*if(bIsmobile){
            GetLicense();
        }*/

        //var TDocObj;
        if (!bLicenseAquired) {
            return;
        }
        //showDownloadDialog();
       /*if (RxCore_GUI_Download != undefined) {
            RxCore_GUI_Download.setDownload("show");
       }*/

        if (RxCore_GUI_HasText != undefined){
            RxCore_GUI_HasText.hastext = false;
            RxCore_GUI_HasText.hasText(RxCore_GUI_HasText.hastext, false);

        }


        var szFileName = getFileName(Filepath);

        //cntximg.clearRect(0, 0, canvas.width, canvas.height);
        //context.clearRect(0, 0, canvas.width, canvas.height);
        //contexto.clearRect(0, 0, canvas.width, canvas.height);

        clearUsers();
        addCurrentUser();


        /*numUsers = 0;
        Userlist = [];
        Userlist[0] = new Users(signature, DisplayName, markuplayer, markupcolor);
        numUsers++;*/
        
        var PageObj;
        

        var XMLGetFile = "<?xml version=\"1.0\" encoding=\"UTF-8\"?>";
        XMLGetFile += "<RxViewServer>";
        XMLGetFile += "<Command>GetFile</Command>";
        XMLGetFile += "<LicenseID>" + LicenseID + "</LicenseID>";
        XMLGetFile += "<FileURL>" + Filepath + "</FileURL>";
        XMLGetFile += "</RxViewServer>";

        var callbacklist = [
            {event : "callback", func : callback},
            {event : "progressCallBack", func : progressCallBack}
        ];
        
        
        sendHttpRequestXML('POST', xmlurl, XMLGetFile, callbacklist).then(function (xmlDoc){

            var i, j = 0;

            if (documentopen && bSingleDocmode) {

                internalClosedoc();

                //RxCore_Closedocument();
            }

            /*if (RxCore_GUI_Download != undefined) {
                RxCore_GUI_Download.setDownload("hide");
            }*/

            //ThumbnailpagesContainer.setEmpty();
            //save for use with print.
            DocXMLPrint = xmlDoc;

            var totalpages = 0;
            if (DocObj == null || DocObj == undefined) {
                nCurdocindex = OpenFiles.push(new DocumentObject(xmlDoc));
                DocObj = OpenFiles[nCurdocindex - 1];
                //DocObj.fileindex = nCurdocindex - 1;
                DocObj.setfileindex(nCurdocindex - 1);

            } else {
                //DocObj.Suspend();
                nCurdocindex = OpenFiles.push(new DocumentObject(xmlDoc));
                //DocObj = OpenFiles[nCurdocindex - 1];
                //OpenFiles[nCurdocindex - 1].fileindex = nCurdocindex - 1;
                OpenFiles[nCurdocindex - 1].setfileindex(nCurdocindex - 1);
                OpenFiles[nCurdocindex - 1].bSopenSilent = true;
                //DocObj.fileindex = nCurdocindex - 1;
            }

            var path = addSlash(OpenFiles[nCurdocindex - 1].OriginalURL);
            var layoutxmlobj = xmlDoc.getElementsByTagName('Layout');
            for (i = 0; i < layoutxmlobj.length; i++) {
                var layoutname = layoutxmlobj[i].getElementsByTagName("LayoutName")[0].firstChild.nodeValue;
                OpenFiles[nCurdocindex - 1].addlayout(layoutname);
                var pagexmlobj = layoutxmlobj[i].getElementsByTagName('Page');
                for (j = 0; j < pagexmlobj.length; j++) {

                    if (j == 0 && i == 0) {
                        PageObj = new PageObject(pagexmlobj[j], layoutname, true, path, j, OpenFiles[nCurdocindex - 1]);
                        DocObj.addthumbnail(new ThumbnailObject(pagexmlobj[j],layoutname, OpenFiles[nCurdocindex - 1], j));
                        OpenFiles[nCurdocindex - 1].addpage(PageObj, true);
                    } else {
                        PageObj = new PageObject(pagexmlobj[j], layoutname, false, '', j + totalpages, OpenFiles[nCurdocindex - 1]);
                        
                        DocObj.addthumbnail(new ThumbnailObject(pagexmlobj[j],layoutname, OpenFiles[nCurdocindex - 1], j + totalpages));
                        OpenFiles[nCurdocindex - 1].addpage(PageObj, false);
                    }

                }
                totalpages += j;

            }


            if(OpenFiles[nCurdocindex - 1] != undefined){
                OpenFiles[nCurdocindex - 1].FileName = szFileName;

                //var szstyle = "width: 200px; height: " + canvasoheight + "px; position:relative; top:-100px; left:15px"
    
                //Thumbnailpopup = DocObj.thumbnailhtmlsource;
                //layerhtmlsource
                //setContentThumb(Thumbnailpopup);
    
                //ThumbnailpagesContainer.innerHTML = Thumbnailpopup;
                //ThumbnailpagesContainer.Setheight(canvasoheight);
                //ThumbnailpagesContainer.setContent();
    
                //OpenFiles[nCurdocindex - 1] = DocObj;
                if (RxCore_GUI_pagethumbs != undefined) {
                    RxCore_GUI_pagethumbs.setThumbnails(OpenFiles[nCurdocindex - 1].thumbnails);
                }
    
                var stateobj = {
                    iscompare:documentcompare,
                    numOpenFiles:OpenFiles.length,
                    isPDF:OpenFiles[nCurdocindex - 1].pages[OpenFiles[nCurdocindex - 1].currentpage].usepdfjs,
                    is3D:OpenFiles[nCurdocindex - 1].pages[OpenFiles[nCurdocindex - 1].currentpage].usevector3Dxml,
                    markupOn3D : false,
                    is2D:OpenFiles[nCurdocindex - 1].pages[OpenFiles[nCurdocindex - 1].currentpage].usevectorxml,
                    numpages:OpenFiles[nCurdocindex - 1].pages.length,
                    currentpage:OpenFiles[nCurdocindex - 1].getcurPage(),
                    activefile : false,
                    source : 'noActiveFile'
                };
    
                if (RxCore_GUI_State != undefined) {
                    RxCore_GUI_State.setGUIState(stateobj);
                }
    
                var pagingobject = {
                    numpages:OpenFiles[nCurdocindex - 1].pages.length,
                    currentpage:OpenFiles[nCurdocindex - 1].getcurPage()
                };
    
                if(RxCore_GUI_Page != undefined){
                    RxCore_GUI_Page.pageEvent(pagingobject);
                }            
            }
            

        }).catch(function(error){

            commonErrorCatchEx("getFileNotActive", error);
            
        });

    }

    function getMarkupxmlurl(DocRef){

        DocRef.setnummarkupfiles(byrefmarkupfiles.length);

        for (var i = 0; i < byrefmarkupfiles.length; i++) {
            var last = (i == byrefmarkupfiles.length - 1);
            getMarkup(byrefmarkupfiles[i],DocRef);
        }

        drawmarkupAll(cntximg);
        DocRef.markuploaded = true;
        if (markupfilepathobj.length == 0) {
            if (RxCore_GUI_Markuplist != undefined) {
                RxCore_GUI_Markuplist.setMarkupList(DocRef.getmarkuplist(), DocRef.bActive);
                /* Andriy */
                // RxCore_GUI_Markuplist.notify();
            }

        }
        bUsemarkupbyrefEx = false;


    }


    function getMarkupbyReference(Filepath, DocRef) {

        var relpath = xmlurlrel;
        if(bUseCustomrelpath){
            relpath = xmlurlrelcustom;
        }

        var file = getFileName(DocRef.CacheFileName);

        var XMLGetFile = "<?xml version=\"1.0\" encoding=\"UTF-8\"?>";
        XMLGetFile += "<RxViewServer>";
        XMLGetFile += "<Command>GetMarkupByRef</Command>";
        XMLGetFile += "<LicenseID>" + LicenseID + "</LicenseID>";
        XMLGetFile += "<FileURL>" + Filepath + file + "</FileURL>";
        //XMLGetFile += "<FileURL>" + Filepath + "</FileURL>";
        XMLGetFile += "<MARKUP_INFO>";
        for (var i = 0; i < byrefmarkupfiles.length; i++) {
            XMLGetFile += "<MARKUP_URL>" + byrefmarkupfiles[i] + "</MARKUP_URL>";
        }
        XMLGetFile += "</MARKUP_INFO>";
        XMLGetFile += "</RxViewServer>";

        sendHttpRequestXML('POST', xmlurl, XMLGetFile).then(function (xmlDoc){

            var MarkupFilePath = 0;
            var i = 0;

            var markupfilepathobj = xmlDoc.getElementsByTagName('File');
            
            DocRef.setnummarkupfiles(markupfilepathobj.length);

            for (i = 0; i < markupfilepathobj.length; i++) {
                MarkupFilePath = markupfilepathobj[i].firstChild.nodeValue;
                MarkupFilePath = relpath + MarkupFilePath;
                var last = (i == markupfilepathobj.length - 1);
                getMarkup(MarkupFilePath, DocRef);
                //markupfiles.push(MarkupFilePath);
            }
            drawmarkupAll(cntximg);
            DocRef.markuploaded = true;
            if (markupfilepathobj.length == 0) {
                if (RxCore_GUI_Markuplist != undefined) {
                    RxCore_GUI_Markuplist.setMarkupList(DocRef.getmarkuplist(), DocRef.bActive);
                    /* Andriy */
                    // RxCore_GUI_Markuplist.notify();
                }
            }
            bUsemarkupbyref = false;            

        }).catch(function(error){


            if(error.status){
                if (error.status == 404) {
                    
                    console.log("getMarkupbyReference error 404");
                } else if (error.status == 503) {
                    console.log("getMarkupbyReference error 503");
                } else if (error.status == 501) {
                    console.log("getMarkupbyReference error 501");
                }
    
            }else{
                console.log(error);
            }


        });

    }

    function getMarkupFileUrls(Filepath, DocRef){
        var file = getFileName(DocRef.FileName);

        var XMLGetFile = "<?xml version=\"1.0\" encoding=\"UTF-8\"?>";
        XMLGetFile += "<RxViewServer>";
        XMLGetFile += "<Command>GetMarkup</Command>";
        XMLGetFile += "<LicenseID>" + LicenseID + "</LicenseID>";
        XMLGetFile += "<FileURL>" + Filepath + "</FileURL>";
        XMLGetFile += "</RxViewServer>";

        sendHttpRequestXML('POST', xmlurlmarkup, XMLGetFile).then(function (xmlDoc){

            var MarkupFilePath = 0;
            var MarkupFilePaths = [];
            var i = 0;

            var markupfilepathobj = xmlDoc.getElementsByTagName('File');
                            
            //DocRef.setnummarkupfiles(markupfilepathobj.length);


            /*if (markupfilepathobj.length == 0) {
                if (RxCore_GUI_MarkupPaths != undefined){
                
                }
    
            }*/

            for (var i = 0; i < markupfilepathobj.length; i++) {
                MarkupFilePath = markupfilepathobj[i].firstChild.nodeValue;
                if(bUseCustomrelpath){

                    MarkupFilePath = xmlurlrelcustom + MarkupFilePath;

                    MarkupFilePaths.push(MarkupFilePath);

                }else{
                    MarkupFilePath = xmlurlrelmarkup + MarkupFilePath;

                    MarkupFilePaths.push(MarkupFilePath);
                }
                                    

            }
            
            if (RxCore_GUI_MarkupPaths != undefined){
                RxCore_GUI_MarkupPaths.markupPaths(MarkupFilePaths);
            }
            //RxCore_GUI_MarkupFileUrls    



        }).catch(function(error){

            commonErrorCatch(error);

        });        


    }

    function getMarkupFilelist(Filepath, DocRef) {
        //var TDocObj;


        if (busesharepointmarkup || busedbmarkup){
            return;
        }

        var PageObj;
        //var pages = new Array();

        //alert("XMLHttpRequest created");

        var file = getFileName(DocRef.FileName);

        var XMLGetFile = "<?xml version=\"1.0\" encoding=\"UTF-8\"?>";
        XMLGetFile += "<RxViewServer>";
        XMLGetFile += "<Command>GetMarkup</Command>";
        XMLGetFile += "<LicenseID>" + LicenseID + "</LicenseID>";
        XMLGetFile += "<FileURL>" + Filepath + "</FileURL>";
        XMLGetFile += "</RxViewServer>";

        sendHttpRequestXML('POST', xmlurlmarkup, XMLGetFile).then(function (xmlDoc){

            var MarkupFilePath = 0;
            var i = 0;
            
            try {
                var SzReadonlymarkup = xmlDoc.getElementsByTagName("readonlymarkup")[0].firstChild.nodeValue;
            } catch (e) {
                //ignore and continue
                SzReadonlymarkup = "";
            }

            if (!readonlymode){
                readonlymode = (SzReadonlymarkup != "" && SzReadonlymarkup == "True");
            }


            var markupfilepathobj = xmlDoc.getElementsByTagName('File');
                            
            DocRef.setnummarkupfiles(markupfilepathobj.length);

            if(DocRef.pages[0].usevector3DGLTF){


                if(DocRef.pages[0].Vector3DPageObj){
                    DocRef.pages[0].Vector3DPageObj.load3Dmarkupcheck();
                }

                //get3DMarkup(MarkupFilePath, DocRef);
                //getMarkup(MarkupFilePath, DocRef);
            }


            for (var i = 0; i < markupfilepathobj.length; i++) {
                MarkupFilePath = markupfilepathobj[i].firstChild.nodeValue;
                if(bUseCustomrelpath){

                    MarkupFilePath = xmlurlrelcustom + MarkupFilePath;
                }else{
                    MarkupFilePath = xmlurlrelmarkup + MarkupFilePath;
                }
                                    
                if(DocRef.pages[0].usevector3DGLTF){
                    //get3DMarkup(MarkupFilePath, DocRef);
                    getMarkup(MarkupFilePath, DocRef);

                    DocRef.setmarkupfilepaths(MarkupFilePath);


                }else{
                    getMarkup(MarkupFilePath, DocRef);
                    DocRef.setmarkupfilepaths(MarkupFilePath);
                }

                
                //markupfiles.push(MarkupFilePath);
            }
            drawmarkupAll(cntximg);
            DocRef.markuploaded = true;


            if (markupfilepathobj.length == 0) {
                
                if (RxCore_GUI_Markuplist != undefined) {
                    RxCore_GUI_Markuplist.setMarkupList(DocRef.getmarkuplist(), DocRef.bActive);
                    /* Andriy */
                    // RxCore_GUI_Markuplist.notify();
                }

                if (Rxcore_GUI_markupLoadComplete != undefined) {
                    Rxcore_GUI_markupLoadComplete.loadComplete(DocRef.markuplist, DocRef.fileindex, DocRef.bActive);
                }


            }

        }).catch(function(error){

            commonErrorCatch(error);

        });

    }

    function singleMarkupToLocalStorage(MarkupObj){
        localStorage.clear();
        licTolocalStorage(LicenseID);
        userToLocalStorage("","",false);

        MarkupToLocalStorage(MarkupObj, false);

    }
    function multiMarkupToLocalStorage(MarkupObj){
        MarkupToLocalStorage(MarkupObj, true);

    }

    function MarkupToLocalStorage(MarkupObj, multi) {
        if (typeof(Storage) !== "undefined") {
            if (multi){
                var postfix = MarkupObj.markupnumber;
                if (localStorage.getItem("MarkupPostfixlist") != null) {
                    var postfixes = localStorage.MarkupPostfixlist;
                    localStorage.setItem("MarkupPostfixlist", postfixes + "," + postfix);
                }else{
                    localStorage.setItem("MarkupPostfixlist", postfix);
                }

            }else{
                var postfix = '';
            }
            
            //localStorage.Type = MarkupObj.type;
            localStorage.setItem("Type" + postfix , MarkupObj.type);
            //localStorage.Subtype = MarkupObj.subtype;
            localStorage.setItem("Subtype" + postfix , MarkupObj.subtype);
            localStorage.setItem("Alternative" + postfix , MarkupObj.alternative);
            localStorage.setItem("Redaction" + postfix , MarkupObj.bRedact ? 1:0);
            //createAndAppendElement(entityNode, "Redaction", DocObj.markuplist[curmarkup].bRedact ? 1:0);
            localStorage.setItem("Rotation" + postfix , MarkupObj.rotation);
            localStorage.setItem("fillcolor" + postfix , MarkupObj.fillcolor);
            localStorage.setItem("strokecolor" + postfix , MarkupObj.strokecolor);
            localStorage.setItem("textcolor" + postfix , MarkupObj.textcolor);
            localStorage.setItem("markercolor" + postfix , MarkupObj.markercolor);
            localStorage.setItem("Color" + postfix , MarkupObj.color);

            localStorage.setItem("FontName" + postfix , MarkupObj.font.fontName);
            localStorage.setItem("FontHeight" + postfix , MarkupObj.font.height);

            localStorage.setItem("bold" + postfix , MarkupObj.font.bold ? 1 : 0);
            localStorage.setItem("italic" + postfix , MarkupObj.font.italic ? 1 : 0);

            localStorage.setItem("Transparency" + postfix , MarkupObj.transparency);

            
            if(MarkupObj.leaderoffset != undefined){
                localStorage.setItem("LeaderOffset" + postfix , MarkupObj.leaderoffset);
            }

            if(MarkupObj.labeloffsetx != undefined){
                localStorage.setItem("LabelOffsetX" + postfix , MarkupObj.labeloffsetx);
            }

            if(MarkupObj.labeloffsety != undefined){
                localStorage.setItem("LabelOffsetY" + postfix , MarkupObj.labeloffsety);
            }


            if(MarkupObj.arrowlength != undefined){
                localStorage.setItem("Arrowlength" + postfix , MarkupObj.arrowlength);
            }

            //localStorage.Alternative = MarkupObj.alternative;
            //localStorage.Rotation = MarkupObj.rotation;
            //localStorage.fillcolor = MarkupObj.fillcolor;
            //localStorage.strokecolor = MarkupObj.strokecolor;
            //localStorage.textcolor = MarkupObj.textcolor;
            //localStorage.markercolor = MarkupObj.markercolor;
            //localStorage.Color = MarkupObj.color;
            //localStorage.FontName = MarkupObj.font.fontName;
            //localStorage.FontHeight = MarkupObj.font.height;
            if (MarkupObj.text == "") {
                //localStorage.Text = ".";
                localStorage.setItem("Text" + postfix , "");
            } else {
                //localStorage.Text = MarkupObj.text;
                localStorage.setItem("Text" + postfix , MarkupObj.text);
            }
            if (localStorage.DimText == "") {
                //localStorage.DimText = ".";
                localStorage.setItem("DimText" + postfix , "");
            } else {
                //localStorage.DimText = MarkupObj.dimtext;
                localStorage.setItem("DimText" + postfix , MarkupObj.dimtext);
            }

            //localStorage.TextWidth = MarkupObj.textwidth;
            localStorage.setItem("TextWidth" + postfix , MarkupObj.textwidth);

            if (MarkupObj.type == 12) {
                //localStorage.Stampsmalltheight = MarkupObj.stampsmalltheight;
                localStorage.setItem("Stampsmalltheight" + postfix , MarkupObj.stampsmalltheight);
            }

            if (MarkupObj.type == 11  ) {
                localStorage.setItem("image" + postfix , MarkupObj.image.src);
                //localStorage.image = MarkupObj.image.src;
                                
            }
            if (MarkupObj.type == 20) {
                localStorage.setItem("image" + postfix , MarkupObj.image.src);
                //localStorage.image = MarkupObj.image.src;
                                
            }

            localStorage.setItem("Scaling" + postfix , MarkupObj.scaling);
            //localStorage.Scaling = MarkupObj.scaling;

            //localStorage.Xoffset = MarkupObj.xoffset;
            localStorage.setItem("Xoffset" + postfix , MarkupObj.xoffset);

            //localStorage.Yoffset = MarkupObj.yoffset;
            localStorage.setItem("Yoffset" + postfix , MarkupObj.yoffset);


            localStorage.setItem("Pageheight" + postfix , MarkupObj.pageheight);

            localStorage.setItem("Pagewidth" + postfix , MarkupObj.pagewidth);


            localStorage.setItem("LineWidth" + postfix , MarkupObj.linewidth);


            localStorage.setItem("OuterRadius" + postfix , MarkupObj.outerRadius);

            localStorage.setItem("InnerRadius" + postfix , MarkupObj.innerRadius);


            //localStorage.LineWidth = MarkupObj.linewidth;

            if (MarkupObj.points.length > 0) {
                //localStorage.Points = JSON.stringify(MarkupObj.points);
                localStorage.setItem("Points" + postfix , JSON.stringify(MarkupObj.points));

            }
            if (MarkupObj.pointlist.length > 0) {
                localStorage.setItem("Pointlist" + postfix , JSON.stringify(MarkupObj.pointlist));
                //localStorage.Pointlist = JSON.stringify(MarkupObj.pointlist);

            }

            if (MarkupObj.sides.length > 0) {
                localStorage.setItem("Sides" + postfix , JSON.stringify(MarkupObj.sides));
            }

            if (MarkupObj.countshapes.length > 0) {
                localStorage.setItem("Countshapes" + postfix , JSON.stringify(MarkupObj.countshapes));
            }


            localStorage.setItem("Rect" + postfix , "x=" + MarkupObj.x + " y=" + MarkupObj.y + " w=" + MarkupObj.w + " h=" + MarkupObj.h);

            //localStorage.Rect = "x=" + MarkupObj.x + " y=" + MarkupObj.y + " w=" + MarkupObj.w + " h=" + MarkupObj.h;
        } else {
            // Sorry! No web storage support..
        }

    }

    function convertToTargetDoc(markupobj, targetDoc){


        //this.dimtext = getUnitlength(dimdiag / this.scaling);
        //var widthvalue = getScreenDim(scalewidth);
        //var heighthvalue = getScreenDim(scaleheight);
    

        return markupobj;

    }


    function MarkupFromLocalStorage() {
        if (typeof(Storage) !== "undefined") {
            if (localStorage.getItem("MarkupPostfixlist") != null) {
                var Postfixlist = localStorage.MarkupPostfixlist;
                var Postfixarray = Postfixlist.split(",");

            }else{
                Postfixarray = [''];
            }

            var bmultiop = false;

            if(Postfixarray.length > 1){
                //make sure undo is set to multi
                bmultiop = true;
                nMarkupUndoIdstart += 1;

            }

            var page = DocObj.getCurrentpage();

            if(page){
                var pagerect = page.getpagerect();
            }

            var pasteoffsetx = 10;
            var pasteoffsety = 10;

            for(var pfixc = 0;pfixc < Postfixarray.length; pfixc++){
                var postfix = Postfixarray[pfixc];

                if (localStorage.getItem("Type" + postfix) != null) {
                    var type = parseInt(localStorage.getItem("Type" + postfix));
                    if (localStorage.getItem("Subtype" + postfix) != null) {
                        var subtype = parseInt(localStorage.getItem("Subtype" + postfix));

                        if (localStorage.getItem("Alternative" + postfix) != null) {
                            if (localStorage.getItem("Rect" + postfix) != null) {
                                
                                var alternative = parseInt(localStorage.getItem("Alternative" + postfix));
                                var NewMarkupobj = new MarkupObject(type, subtype, alternative);

                                NewMarkupobj.bRedact = (parseInt(localStorage.getItem("Alternative" + postfix)) == 1);
                                //parseInt(localStorage.getItem("Alternative" + postfix));
                                NewMarkupobj.pagenumber = DocObj.getcurPage();

                                NewMarkupobj.viewname = DocObj.getcur3DView();

                                if(NewMarkupobj.viewname == null){
                                    NewMarkupobj.bfor3D = false;
                                }else{
                                    NewMarkupobj.bfor3D = true;
                                    NewMarkupobj.pageheight = canvasoheight;
                                    NewMarkupobj.pagewidth = canvasowidth;

                                }


                                NewMarkupobj.fillcolor = localStorage.getItem("fillcolor" + postfix);
                                
                                NewMarkupobj.strokecolor = localStorage.getItem("strokecolor" + postfix);
                                NewMarkupobj.textcolor = localStorage.getItem("textcolor" + postfix);
                                NewMarkupobj.markercolor = localStorage.getItem("markercolor" + postfix);
                                
                                NewMarkupobj.color = localStorage.getItem("Color" + postfix);
                                
                                NewMarkupobj.dimtext = localStorage.getItem("DimText" + postfix);

                                
                                NewMarkupobj.font.setFontname(localStorage.getItem("FontName" + postfix));
                                NewMarkupobj.font.setHeight(localStorage.getItem("FontHeight" + postfix));


                                NewMarkupobj.font.bold = (parseInt(localStorage.getItem("bold" + postfix)) == 1);
                                NewMarkupobj.font.italic = (parseInt(localStorage.getItem("italic" + postfix)) == 1);


                                NewMarkupobj.transparency = localStorage.getItem("Transparency" + postfix);

                                NewMarkupobj.rotation = localStorage.getItem("Rotation" + postfix);
                                
                                //NewMarkupobj.textheight = localStorage.FontHeight;
                                if (type == 12) {
                                    NewMarkupobj.stampsmalltheight = localStorage.getItem("Stampsmalltheight" + postfix);
                                }
    
                                if (type == 11) {
                                    var createimage = new Image();
                                    NewMarkupobj.markupcdataload(createimage, localStorage.getItem("image" + postfix));
                                    

                                }

                                /*if (type == 20) {
                                    var createimage = new Image();
                                    NewMarkupobj.markupcdataload(createimage, localStorage.getItem("image" + postfix));
                                }*/
                    
                                if(localStorage.getItem("LeaderOffset" + postfix) != null){
                                    NewMarkupobj.leaderoffset = parseFloat(localStorage.getItem("LeaderOffset" + postfix));
                                }
                                
                                if(localStorage.getItem("LabelOffsetX" + postfix) != null){
                                    NewMarkupobj.labeloffsetx = parseFloat(localStorage.getItem("LabelOffsetX" + postfix));
                                }

                                if(localStorage.getItem("LabelOffsetY" + postfix) != null){
                                    NewMarkupobj.labeloffsety = parseFloat(localStorage.getItem("LabelOffsetY" + postfix));
                                }

                                if(localStorage.getItem("LabelColor" + postfix) != null){
                                    NewMarkupobj.labelfillcolor = localStorage.getItem("LabelColor" + postfix);
                                }


                                if(localStorage.getItem("Arrowlength" + postfix) != null){
                                    NewMarkupobj.arrowlength = parseFloat(localStorage.getItem("Arrowlength" + postfix));
                                }

                                
                                NewMarkupobj.text = localStorage.getItem("Text" + postfix);
                                
                                
                                NewMarkupobj.dimtext = localStorage.getItem("DimText" + postfix);

                                NewMarkupobj.textwidth = parseFloat(localStorage.getItem("TextWidth" + postfix));

                                NewMarkupobj.scaling = parseFloat(localStorage.getItem("Scaling" + postfix));

                                NewMarkupobj.xoffset = parseFloat(localStorage.getItem("Xoffset" + postfix));
                                
                                NewMarkupobj.yoffset = parseFloat(localStorage.getItem("Yoffset" + postfix));

                                NewMarkupobj.pageheight = parseFloat(localStorage.getItem("Pageheight" + postfix));

                                NewMarkupobj.pagewidth = parseFloat(localStorage.getItem("Pagewidth" + postfix));
                                
                                NewMarkupobj.linewidth = parseInt(localStorage.getItem("LineWidth" + postfix));

                                NewMarkupobj.outerRadius = parseInt(localStorage.getItem("OuterRadius" + postfix));
                                
                                NewMarkupobj.innerRadius = parseInt(localStorage.getItem("InnerRadius" + postfix));

                                
    
                                var Rectlist = localStorage.getItem("Rect" + postfix);

                                var Rectarray = Rectlist.split(" ");
                                var xval = Rectarray[0].split("=");
                                var yval = Rectarray[1].split("=");
                                var wval = Rectarray[2].split("=");
                                var hval = Rectarray[3].split("=");
                                NewMarkupobj.x = parseInt(xval[1]);
                                NewMarkupobj.y = parseInt(yval[1]);
                                NewMarkupobj.w = parseInt(wval[1]);
                                NewMarkupobj.h = parseInt(hval[1]);


                                if(pagerect){

                                    //var centerx = (pagerect.w * 0.5) + pagerect.x);
                                    //var centery = (pagerect.h - pagerect.y) * 0.5;

                                    var centerx = (pagerect.w) * 0.5;
                                    var centery = (pagerect.h) * 0.5;

                                    var centerx = centerx + pagerect.x;
                                    var centery = centery + pagerect.y;



                                    if( NewMarkupobj.x < centerx && NewMarkupobj.w < centerx ){
                                        pasteoffsetx = 10;
                                    }else{
                                        pasteoffsetx = -10;
                                    }

                                    if( NewMarkupobj.y < centery && NewMarkupobj.h < centery ){
                                        pasteoffsety = 10;
                                    }else{
                                        pasteoffsety = -10;
                                    }


                                    /*var pagerect = {
                                        x: rotrect.x1,
                                        y: rotrect.y1,
                                        w: rotrect.x2 - rotrect.x1,
                                        h: rotrect.y2 - rotrect.y1
                                    };*/
                
                                }

                                var bOval = (NewMarkupobj.type == 4 && NewMarkupobj.subtype == 0);
                                if (NewMarkupobj.type == 3 || bOval || NewMarkupobj.type == 5 || NewMarkupobj.type == 9 || NewMarkupobj.type == 10 || NewMarkupobj.type == 11 || NewMarkupobj.type == 12 || NewMarkupObj.type == 20) {
                                    NewMarkupobj.x += pasteoffsetx;
                                    NewMarkupobj.y += pasteoffsety;
    
                                } else {
                                    NewMarkupobj.x += pasteoffsetx;
                                    NewMarkupobj.y += pasteoffsety;
                                    NewMarkupobj.w += pasteoffsetx;
                                    NewMarkupobj.h += pasteoffsety;
                                }
        
                                if (localStorage.getItem("Points" + postfix) != null) {
    
                                    var Pointlist = localStorage.getItem("Points" + postfix);
                                    NewMarkupobj.points = JSON.parse(Pointlist);
                                    //var content_array = JSON.parse(str);
                                    //var pointarray = Pointlist.split(",");
                                    for (var i = 0; i < NewMarkupobj.points.length; i++) {
                                        NewMarkupobj.points[i].x += pasteoffsetx;
                                        NewMarkupobj.points[i].y += pasteoffsety;
                                        //var point = pointarray[i];
                                        //var pointpair = point.split(" ");
                                        //var xp = pointpair[0].split("=");
                                        //var yp = pointpair[1].split("=");
                                        //NewMarkupobj.addpoint(parseInt(xp[1]),parseInt(yp[1]));
                                        //NewMarkupobj.addpoint(parseInt(xp[1]) + 10, parseInt(yp[1]) + 10);
                                        //+= "x=" + MarkupObj.points[i].x + " y=" + MarkupObj.points[i].y + ",";
                                    }
    
                                }
                                if (localStorage.getItem("Countshapes" + postfix) != null) {
                                    var Countlist = localStorage.getItem("Countshapes" + postfix);
                                    NewMarkupobj.countshapes = JSON.parse(Countlist);
                                    //var content_array = JSON.parse(str);
                                    //var pointarray = Pointlist.split(",");
                                    for (var i = 0; i < NewMarkupobj.countshapes.length; i++) {
                                        NewMarkupobj.countshapes[i].x += pasteoffsetx;
                                        NewMarkupobj.countshapes[i].y += pasteoffsety;
                                        //var point = pointarray[i];
                                        //var pointpair = point.split(" ");
                                        //var xp = pointpair[0].split("=");
                                        //var yp = pointpair[1].split("=");
                                        //NewMarkupobj.addpoint(parseInt(xp[1]),parseInt(yp[1]));
                                        //NewMarkupobj.addpoint(parseInt(xp[1]) + 10, parseInt(yp[1]) + 10);
                                        //+= "x=" + MarkupObj.points[i].x + " y=" + MarkupObj.points[i].y + ",";
                                    }

                                }
                                if (localStorage.getItem("Pointlist" + postfix) != null) {
                                    Pointlist = localStorage.getItem("Pointlist" + postfix);
                                    NewMarkupobj.pointlist = JSON.parse(Pointlist);


                                    var lcounter = 0;
                                    var counter = 0;


                                    for (lcounter = 0; lcounter < NewMarkupobj.pointlist.length; lcounter++) {
                                        for (counter = 0; counter < NewMarkupobj.pointlist[lcounter].length; counter++) {
                
                                            NewMarkupobj.pointlist[lcounter][counter].x += pasteoffsetx;
                                            NewMarkupobj.pointlist[lcounter][counter].y += pasteoffsety;
                                        }
                                    }


                                }

                                if (localStorage.getItem("Sides" + postfix) != null) {
                                    var Sides = localStorage.getItem("Sides" + postfix);
                                    NewMarkupobj.sides = JSON.parse(Sides);

                                }

                                

                                nMarkupcreated = DocObj.markuplist.length;
    
                                NewMarkupobj.savemetolistLoad(DocObj);
                                //undoMarkupdeletobj.display = true;
                                NewMarkupobj.display = true;
                                NewMarkupobj.setfirstDraw();

                                if(bmultiop){
                                    DocObj.markuplist[nMarkupcreated].setmultiValues(nMarkupUndoIdstart);
                                }

                                DocObj.markuplist[nMarkupcreated].savemarkupstate(DocObj, undoCreate);

                                bMarkupcrated = true;

                                /*if(Postfixarray.length == 1){//single markup check removed on request from bigslicetech 21.02.2022
                                }*/

                                if (RxCore_GUI_Markup != undefined) {
                                    RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[nMarkupcreated], {created : true, modified : false, deleted : false});
                                }


                            }
    
                        }
    
                    }
    
                }
            }
            

        } else {
            // Sorry! No web storage support..
        }

    }

    function createCustomStamp(image, sname){


            var stampinfo = { type : 5, data : image.image, index : sname, width : image.width, height: image.height};

            if (RxCore_GUI_CustomStamps != undefined){
                RxCore_GUI_CustomStamps.onStampReceived(stampinfo);
            }

    }
    
    function createCustomStamps(stampsobject){

        /*var stampinfo = { type : 1, data : "1"};

        if (RxCore_GUI_CustomStamps != undefined){
            RxCore_GUI_CustomStamps.onStampReceived(stampinfo);
        }*/

        var stampinfo = {type : 2, data : "Custom Stamps" };

        if (RxCore_GUI_CustomStamps != undefined){
            RxCore_GUI_CustomStamps.onStampReceived(stampinfo);
        }

        stampinfo = {type : 3, data : stampsobject.numstamps};
        if (RxCore_GUI_CustomStamps != undefined){
            RxCore_GUI_CustomStamps.onStampReceived(stampinfo);
        }


        for (var i = 0; i < stampsobject.numstamps; i++){
            createCustomStamp(stampsobject.stamps[i],i);
        }

    }

    function getSymbolPNGDataold(num,sname){
        //Command=symbolpngdata&Libnumber=n&Symbolnumber=s;

        var xhr = new XMLHttpRequest();
        xhr.withCredentials = bUseCredentials;
        var urlEncodedData = "";
        var urlEncodedDataPairs = [];

        urlEncodedDataPairs.push(encodeURIComponent("MfcISAPICommand") + '=' + encodeURIComponent("FormRequest"));
        urlEncodedDataPairs.push(encodeURIComponent("Command") + '=' + encodeURIComponent("symbolpng"));
        urlEncodedDataPairs.push(encodeURIComponent("Libnumber") + '=' + encodeURIComponent(num));
        urlEncodedDataPairs.push(encodeURIComponent("Symbolnumber") + '=' + encodeURIComponent(sname));

        urlEncodedData = urlEncodedDataPairs.join('&').replace(/%20/g, '+');

        try {
            xhr.open('POST', xmlurldirect + "?FormRequest", true);
            xhr.responseType = "arraybuffer";
        } catch (e) {
            alert("Error 1 - " + e);
        }


        xhr.onload = function (e) {
            if (this.status == 200) {

                //parse symbol xml here.
                //create callback to push symbol library to GUI
                var symbolPNGData = new Blob([xhr.response], {type: "image/png"});
                //var symbolPNGData = this.responseText;

                var symbolinfo = { type : 5, data : symbolPNGData, index : sname};

                if (RxCore_GUI_Symbols != undefined){
                    RxCore_GUI_Symbols.onSymbolReceived(symbolinfo);
                }


            }else if (this.status == 404) {
                console.log("PNG data could not be found");
            } else if (this.status == 503) {
                console.log("Server is down error 503");
            } else if (this.status == 501) {
                
                console.log("Server error 501");
            }
        };

        xhr.send(urlEncodedData);



    }

    function getSymbolPNGData(num,sname){
        //Command=symbolpngdata&Libnumber=n&Symbolnumber=s;

        var urlEncodedData = "";
        var urlEncodedDataPairs = [];

        urlEncodedDataPairs.push(encodeURIComponent("MfcISAPICommand") + '=' + encodeURIComponent("FormRequest"));
        urlEncodedDataPairs.push(encodeURIComponent("Command") + '=' + encodeURIComponent("symbolpng"));
        urlEncodedDataPairs.push(encodeURIComponent("Libnumber") + '=' + encodeURIComponent(num));
        urlEncodedDataPairs.push(encodeURIComponent("Symbolnumber") + '=' + encodeURIComponent(sname));

        urlEncodedData = urlEncodedDataPairs.join('&').replace(/%20/g, '+');


        sendHttpRequest('POST', xmlurldirect + "?FormRequest","arraybuffer", urlEncodedData).then(function (response){
            
            var symbolPNGData = new Blob([response], {type: "image/png"});
            //var symbolPNGData = this.responseText;

            var symbolinfo = { type : 5, data : symbolPNGData, index : sname};

            if (RxCore_GUI_Symbols != undefined){
                RxCore_GUI_Symbols.onSymbolReceived(symbolinfo);
            }


        }).catch(function(error){

           if(error.status){
            if (error.status == 404) {
                console.log("PNG source not found error 404");
            } else if (error.status == 503) {
                console.log("Server is down error 503");
            } else if (error.status == 501) {
                
                console.log("Server error 501");
            }
           }else{
            console.log(error);
           }
       });

    }

    function getSymbolName(num, sname){
        //Command=Symbolname&Libnumber=nSymbolnumber=
        var urlEncodedData = "";
        var urlEncodedDataPairs = [];

        urlEncodedDataPairs.push(encodeURIComponent("MfcISAPICommand") + '=' + encodeURIComponent("FormRequest"));
        urlEncodedDataPairs.push(encodeURIComponent("Command") + '=' + encodeURIComponent("Symbolname"));
        urlEncodedDataPairs.push(encodeURIComponent("Libnumber") + '=' + encodeURIComponent(num));
        urlEncodedDataPairs.push(encodeURIComponent("Symbolnumber") + '=' + encodeURIComponent(sname));

        urlEncodedData = urlEncodedDataPairs.join('&').replace(/%20/g, '+');

        sendHttpRequest('POST', xmlurldirect + "?FormRequest",'', urlEncodedData).then(function (response){

            var symbolName = response;

            var symbolinfo = { type : 4, data : symbolName};


            if (RxCore_GUI_Symbols != undefined){
                RxCore_GUI_Symbols.onSymbolReceived(symbolinfo);
            }


        }).catch(function(error){

            if(error.status){
             if (error.status == 404) {
                 console.log("Symbol name not found error 404");
             } else if (error.status == 503) {
                 
                 console.log("Symbol name not found error 503");
             } else if (error.status == 501) {
                console.log("Symbol name not found error 501");
             }
            }else{
                console.log(error);
            }
 
        });

    }

    function getnumSymbols(num){
        //Command=Numsymbols&Libnumber=n
        var urlEncodedData = "";
        var urlEncodedDataPairs = [];

        urlEncodedDataPairs.push(encodeURIComponent("MfcISAPICommand") + '=' + encodeURIComponent("FormRequest"));
        urlEncodedDataPairs.push(encodeURIComponent("Command") + '=' + encodeURIComponent("Numsymbols"));
        urlEncodedDataPairs.push(encodeURIComponent("Libnumber") + '=' + encodeURIComponent(num));

        urlEncodedData = urlEncodedDataPairs.join('&').replace(/%20/g, '+');

        
        sendHttpRequest('POST', xmlurldirect + "?FormRequest", '',urlEncodedData).then(function (repsonse){
            var numsymbols = repsonse;

            var symbolinfo = { type : 3, data : numsymbols};

            if (RxCore_GUI_Symbols != undefined){
                RxCore_GUI_Symbols.onSymbolReceived(symbolinfo);
            }


        }).catch(function(error){

            if(error.status){
             if (error.status == 404) {
                 
                 console.log("number of symbols not returned error 404")
             } else if (error.status == 503) {
                 
                 console.log("number of symbols not returned error 503")
             } else if (error.status == 501) {
                 
                 console.log("number of symbols not returned error 501")
             }
            }else{
                console.log(error);
            }
        });


    }

    function selectSymbname(num){
        //Command=Libname&Libnumber=n

        var urlEncodedData = "";
        var urlEncodedDataPairs = [];

        urlEncodedDataPairs.push(encodeURIComponent("MfcISAPICommand") + '=' + encodeURIComponent("FormRequest"));
        urlEncodedDataPairs.push(encodeURIComponent("Command") + '=' + encodeURIComponent("Libname"));
        urlEncodedDataPairs.push(encodeURIComponent("Libnumber") + '=' + encodeURIComponent(num));

        urlEncodedData = urlEncodedDataPairs.join('&').replace(/%20/g, '+');

        sendHttpRequest('POST', xmlurldirect + "?FormRequest", '', urlEncodedData).then(function (response){

            var libname = response;

            var symbolinfo = { type : 2, data : libname};

            if (RxCore_GUI_Symbols != undefined){
                RxCore_GUI_Symbols.onSymbolReceived(symbolinfo);
            }

        }).catch(function(error){

            if(error.status){
             if (error.status == 404) {
                 

                 console.log("library name not returned error 404");
             } else if (error.status == 503) {
                 
                 console.log("library name not returned error 503");
             } else if (error.status == 501) {
                 
                 console.log("library name not returned error 501");
             }
            }else{
                console.log(error);
            }
        });
        

    }


    function selectSymblib(num){
        //Command=Selectlib&Libnumber=n
        var urlEncodedData = "";
        var urlEncodedDataPairs = [];

        urlEncodedDataPairs.push(encodeURIComponent("MfcISAPICommand") + '=' + encodeURIComponent("FormRequest"));
        urlEncodedDataPairs.push(encodeURIComponent("Command") + '=' + encodeURIComponent("Selectlib"));
        urlEncodedDataPairs.push(encodeURIComponent("Libnumber") + '=' + encodeURIComponent(num));

        urlEncodedData = urlEncodedDataPairs.join('&').replace(/%20/g, '+');


        sendHttpRequest('POST', xmlurldirect + "?FormRequest", '', urlEncodedData).then(function (response){

            var symbolinfo = { type : 6, data : num};

            if (RxCore_GUI_Symbols != undefined){
                RxCore_GUI_Symbols.onSymbolReceived(symbolinfo);
            }

        }).catch(function(error){

            if(error.status){
             if (error.status == 404) {
                console.log("symbol data not returned error 404");
             } else if (error.status == 503) {
                
                console.log("symbol data not returned error 503");
             } else if (error.status == 501) {
                 
                 console.log("symbol data not returned error 501");
             }
            }else{
                console.log(error);
            }
 
 
        });

    }

    function getNumSymblibs(){
        var urlEncodedData = "";
        var urlEncodedDataPairs = [];

        urlEncodedDataPairs.push(encodeURIComponent("MfcISAPICommand") + '=' + encodeURIComponent("FormRequest"));
        urlEncodedDataPairs.push(encodeURIComponent("Command") + '=' + encodeURIComponent("Numlibs"));

        urlEncodedData = urlEncodedDataPairs.join('&').replace(/%20/g, '+');

        sendHttpRequest('POST', xmlurldirect + "?FormRequest", '', urlEncodedData).then(function (response){

            var Numlibs = response;

            var symbolinfo = { type : 1, data : Numlibs};

            if (RxCore_GUI_Symbols != undefined){
                RxCore_GUI_Symbols.onSymbolReceived(symbolinfo);
            }



        }).catch(function(error){

            if(error.status){
             if (error.status == 404) {
                console.log("number of symbol libraries not returned error 404");


             } else if (error.status == 503) {
                console.log("number of symbol libraries not returned error 503");
             } else if (error.status == 501) {
                console.log("number of symbol libraries not returned error 501");
             }
            }else{
                console.log(error);
            }
 
 
        });
        
    }

    function getGUID3D(markup3Dobject){

        var XMLGetGUID = "<?xml version=\"1.0\" encoding=\"UTF-8\"?>";
        XMLGetGUID += "<RxViewServer>";
        XMLGetGUID += "<Command>GetGUID</Command>";
        XMLGetGUID += "<LicenseID>" + LicenseID + "</LicenseID>";
        XMLGetGUID += "</RxViewServer>";


        sendHttpRequest('POST', xmlurl, '',XMLGetGUID).then(function (response){

            var GUID = response;

            markup3Dobject.name = GUID;


        }).catch(function(error){

            if(error.status){
    
            }else{
                console.log(error);
            }
 
 
        });


    }

    
    function getGUIDEx(operation, markupobject){
        
            //GetGUID

            if(!operation.created && markupobject.uniqueID != 0){

                if(RxCore_GUI_GuidRecieved != undefined){
                    RxCore_GUI_GuidRecieved.guidcomplete(markupobject.uniqueID, operation, true);
                }
                return;

            }

            
            var XMLGetGUID = "<?xml version=\"1.0\" encoding=\"UTF-8\"?>";
            XMLGetGUID += "<RxViewServer>";
            XMLGetGUID += "<Command>GetGUID</Command>";
            XMLGetGUID += "<LicenseID>" + LicenseID + "</LicenseID>";
            XMLGetGUID += "</RxViewServer>";
    
            sendHttpRequest('POST', xmlurl, '', XMLGetGUID).then(function (response){
                var GUID = response;

                markupobject.setUniqueID(GUID);
                
                if(RxCore_GUI_GuidRecieved != undefined){
                    RxCore_GUI_GuidRecieved.guidcomplete(GUID, operation, true);
                }
    
            }).catch(function(error){
    
                if(error.status){
                     console.log(error.status); 
                }else{
                    console.log(error);
                }
                if(RxCore_GUI_GuidRecieved != undefined){
                    RxCore_GUI_GuidRecieved.guidcomplete(error,operation, false);
                }

                
            });
    
    }


    function getGUID(markupobject){
        //GetGUID
        
        var XMLGetGUID = "<?xml version=\"1.0\" encoding=\"UTF-8\"?>";
        XMLGetGUID += "<RxViewServer>";
        XMLGetGUID += "<Command>GetGUID</Command>";
        XMLGetGUID += "<LicenseID>" + LicenseID + "</LicenseID>";
        XMLGetGUID += "</RxViewServer>";

        sendHttpRequest('POST', xmlurl, '', XMLGetGUID).then(function (response){
            var GUID = response;
            markupobject.setUniqueID(GUID);

            

        }).catch(function(error){

            if(error.status){
                 console.log(error.status); 
            }else{
                console.log(error);
            }
 
        });

    }

    function modifyMarkup(GUID,markupxml){


        var xmlmarkupobj = findUniqueMarkup(GUID);
        if(xmlmarkupobj.isempty){
            return;
        }

        MarkupSaveStateByref(xmlmarkupobj);
        //MarkupSaveStateGUID(GUID);

        var xmldoc = parseXML(markupxml);

        if (xmldoc.getElementsByTagName('FileVersion') != null && xmldoc.getElementsByTagName('FileVersion').length != 0) {
            var fileVersion = xmldoc.getElementsByTagName('FileVersion')[0].firstChild.nodeValue;
        }

        if (xmldoc.getElementsByTagName('Entity') != null) {
            var markupentityobj = xmldoc.getElementsByTagName('Entity');
            for (var i = 0; i < markupentityobj.length; i++) {
                //var markupType = markupentityobj[i].getElementsByTagName("Type")[0].firstChild.nodeValue;
                //var markupSubtype = markupentityobj[i].getElementsByTagName("Subtype")[0].firstChild.nodeValue;
                //if (markupentityobj[i].getElementsByTagName("Alternative")[0].firstChild.nodeValue != undefined)

                if ( markupentityobj[i].getElementsByTagName('HaveLeader')[0] != undefined) {
                    var bhasArrow = (parseInt(markupentityobj[i].getElementsByTagName('HaveLeader')[0].firstChild.nodeValue) == 1);
                }
                
                if(bhasArrow){
                    if (markupentityobj[i].getElementsByTagName('Entity')[0] != undefined){
                        var arrowleader = markupentityobj[i].getElementsByTagName("Entity")[0];
                        //var leadermarkupType = arrowleader.getElementsByTagName("Type")[0].firstChild.nodeValue;
                        //var leadermarkupSubtype = arrowleader.getElementsByTagName("Subtype")[0].firstChild.nodeValue;
                        var leadermarkupAlternative = arrowleader.getElementsByTagName("Alternative")[0].firstChild.nodeValue;
                        //var leadermarkupID = !arrowleader.getAttribute('ID') ? null : arrowleader.getAttribute('ID');
                        //leadermarkupType = parseInt(leadermarkupType);
                        //leadermarkupSubtype = parseInt(leadermarkupSubtype);
                        //leadermarkupAlternative = parseInt(leadermarkupAlternative);
                        //var leaderxmlmarkupobj = new MarkupObject(leadermarkupType, leadermarkupSubtype, leadermarkupAlternative);
                        //var blOK = leaderxmlmarkupobj.SetfromXML(DocObj,arrowleader, fileVersion);
                        var blOK = xmlmarkupobj.SetfromXML(DocObj,arrowleader, fileVersion);


                        if(blOK){
                            markupentityobj[i].removeChild(arrowleader);
                            xmlmarkupobj.alternative = leadermarkupAlternative;

                            if(xmlmarkupobj.textBoxConnected != null){
                                xmlmarkupobj = xmlmarkupobj.textBoxConnected;
                            }
                            //var markupfiles = DocRef.getnummarkupfiles();
                            //var received = DocRef.getmarkupfilesreceived();
                            //var lastmarkup = (i == markupentityobj.length - 1 && (markupfiles == received));
                            
                            //leaderxmlmarkupobj.savemetolistLoad(DocObj,false);

                        }

                    }

                }

                var markupAlternative = markupentityobj[i].getElementsByTagName("Alternative")[0].firstChild.nodeValue;
                markupAlternative = parseInt(markupAlternative);
                //var markupID = !markupentityobj[i].getAttribute('ID') ? null : markupentityobj[i].getAttribute('ID');
                //markupType = parseInt(markupType);
                //markupSubtype = parseInt(markupSubtype);
                //markupAlternative = parseInt(markupAlternative);
                //var xmlmarkupobj = new MarkupObject(markupType, markupSubtype, markupAlternative);
                //xmlmarkupobj.markupID = markupID;

                var bOK = xmlmarkupobj.SetfromXML(DocObj, markupentityobj[i], fileVersion);
                if (bOK) {
                    
                    xmlmarkupobj.alternative = markupAlternative;

                    //xmlmarkupobj.savemetolist(true);
                }

            }

            /* Andriy call callback only once */

            if (xmlmarkupobj) xmlmarkupobj.notify();

            if (markupentityobj.length == 0) {
                if (RxCore_GUI_Markuplist != undefined) {
                    RxCore_GUI_Markuplist.setMarkupList(DocObj.getmarkuplist(), DocObj.bActive);
                    /* Andriy */
                    // RxCore_GUI_Markuplist.notify();
                }

            }

            drawmarkupAll(cntximg);

        }
    }

    function addMarkupPage(markupxml, pagenum){

        var xmldoc = parseXML(markupxml);


        if (xmldoc.getElementsByTagName('FileVersion') != null && xmldoc.getElementsByTagName('FileVersion').length != 0) {
            var fileVersion = xmldoc.getElementsByTagName('FileVersion')[0].firstChild.nodeValue;

        }

        if (xmldoc.getElementsByTagName('Entity') != null) {
            var markupentityobj = xmldoc.getElementsByTagName('Entity');
            for (var i = 0; i < markupentityobj.length; i++) {
                var markupType = markupentityobj[i].getElementsByTagName("Type")[0].firstChild.nodeValue;
                var markupSubtype = markupentityobj[i].getElementsByTagName("Subtype")[0].firstChild.nodeValue;
                var markupAlternative = markupentityobj[i].getElementsByTagName("Alternative")[0].firstChild.nodeValue;

                var markupID = !markupentityobj[i].getAttribute('ID') ? null : markupentityobj[i].getAttribute('ID');

                if ( markupentityobj[i].getElementsByTagName('HaveLeader')[0] != undefined) {
                    var bhasArrow = (parseInt(markupentityobj[i].getElementsByTagName('HaveLeader')[0].firstChild.nodeValue) == 1);
                }
                
                if(bhasArrow){
                    if (markupentityobj[i].getElementsByTagName('Entity')[0] != undefined){
                        var arrowleader = markupentityobj[i].getElementsByTagName("Entity")[0];
                        var leadermarkupType = arrowleader.getElementsByTagName("Type")[0].firstChild.nodeValue;
                        var leadermarkupSubtype = arrowleader.getElementsByTagName("Subtype")[0].firstChild.nodeValue;
                        var leadermarkupAlternative = arrowleader.getElementsByTagName("Alternative")[0].firstChild.nodeValue;
                        var leadermarkupID = !arrowleader.getAttribute('ID') ? null : arrowleader.getAttribute('ID');
                        leadermarkupType = parseInt(leadermarkupType);
                        leadermarkupSubtype = parseInt(leadermarkupSubtype);
                        leadermarkupAlternative = parseInt(leadermarkupAlternative);
                        var leaderxmlmarkupobj = new MarkupObject(leadermarkupType, leadermarkupSubtype, leadermarkupAlternative);
                        var blOK = leaderxmlmarkupobj.SetfromXML(DocObj,arrowleader, fileVersion);

                        if(blOK){
                            markupentityobj[i].removeChild(arrowleader);
                            //var markupfiles = DocRef.getnummarkupfiles();
                            //var received = DocRef.getmarkupfilesreceived();
                            //var lastmarkup = (i == markupentityobj.length - 1 && (markupfiles == received));
                            
                            leaderxmlmarkupobj.pagenumber = pagenum;

                            leaderxmlmarkupobj.savemetolistLoad(DocObj,false);

                        }

                    }

                }

                markupType = parseInt(markupType);
                markupSubtype = parseInt(markupSubtype);
                markupAlternative = parseInt(markupAlternative);

                var xmlmarkupobj = new MarkupObject(markupType, markupSubtype, markupAlternative);
                xmlmarkupobj.markupID = markupID;
                

                var bOK = xmlmarkupobj.SetfromXML(DocObj,markupentityobj[i], fileVersion);
                if (bOK) {

                    xmlmarkupobj.pagenumber = pagenum;
                    
                    if(blOK && bhasArrow && leaderxmlmarkupobj != null){
                        xmlmarkupobj.bhasArrow = bhasArrow;
                        xmlmarkupobj.markupArrowConnected = leaderxmlmarkupobj;
                        leaderxmlmarkupobj.bisTextArrow = true;
                        leaderxmlmarkupobj.textBoxConnected = xmlmarkupobj;
                        leaderxmlmarkupobj.setArrowPoint(xmlmarkupobj);

                        blOK = false;
                        bhasArrow = false;
                    }



                    xmlmarkupobj.savemetolistLoad(DocObj);
                }


                if (markupentityobj[i].getElementsByTagName("Signature")[0].firstChild.nodeValue != undefined){
                    var sign = markupentityobj[i].getElementsByTagName("Signature")[0].firstChild.nodeValue;

                    if (markupentityobj[i].getElementsByTagName("Name")[0].firstChild.nodeValue != undefined){
                        var dispname = markupentityobj[i].getElementsByTagName("Name")[0].firstChild.nodeValue;
                    }
                    
                    if (sign && dispname){
                        if (GetDisplayName(sign) == 'default user'){
                            numUsers = Userlist.length;
                            var layerColor = getMarkupLayerColor(xmlmarkupobj.layer);
                            Userlist.push(new Users(sign, dispname, xmlmarkupobj.layer, layerColor, false));
                            //Userlist[numUsers] = new Users(sign, dispname, xmlmarkupobj.layer, layerColor, false);
                            numUsers = Userlist.length;
                        }

                    }
                    
                }


            }


            if (xmlmarkupobj) xmlmarkupobj.notify();

            if (markupentityobj.length == 0) {
                if (RxCore_GUI_Markuplist != undefined) {
                    RxCore_GUI_Markuplist.setMarkupList(DocObj.getmarkuplist(),DocObj.bActive);
                    /* Andriy */
                    // RxCore_GUI_Markuplist.notify();
                }

            }


            drawmarkupAll(cntximg);

        }

    }

    function addMarkup(markupxml){

        var xmldoc = parseXML(markupxml);


        if (xmldoc.getElementsByTagName('FileVersion') != null && xmldoc.getElementsByTagName('FileVersion').length != 0) {
            var fileVersion = xmldoc.getElementsByTagName('FileVersion')[0].firstChild.nodeValue;

        }

        if (xmldoc.getElementsByTagName('Entity') != null) {
            var markupentityobj = xmldoc.getElementsByTagName('Entity');
            for (var i = 0; i < markupentityobj.length; i++) {
                var markupType = markupentityobj[i].getElementsByTagName("Type")[0].firstChild.nodeValue;
                var markupSubtype = markupentityobj[i].getElementsByTagName("Subtype")[0].firstChild.nodeValue;
                var markupAlternative = markupentityobj[i].getElementsByTagName("Alternative")[0].firstChild.nodeValue;

                var markupID = !markupentityobj[i].getAttribute('ID') ? null : markupentityobj[i].getAttribute('ID');

                if ( markupentityobj[i].getElementsByTagName('HaveLeader')[0] != undefined) {
                    var bhasArrow = (parseInt(markupentityobj[i].getElementsByTagName('HaveLeader')[0].firstChild.nodeValue) == 1);
                }
                
                if(bhasArrow){
                    if (markupentityobj[i].getElementsByTagName('Entity')[0] != undefined){
                        var arrowleader = markupentityobj[i].getElementsByTagName("Entity")[0];
                        var leadermarkupType = arrowleader.getElementsByTagName("Type")[0].firstChild.nodeValue;
                        var leadermarkupSubtype = arrowleader.getElementsByTagName("Subtype")[0].firstChild.nodeValue;
                        var leadermarkupAlternative = arrowleader.getElementsByTagName("Alternative")[0].firstChild.nodeValue;
                        var leadermarkupID = !arrowleader.getAttribute('ID') ? null : arrowleader.getAttribute('ID');
                        leadermarkupType = parseInt(leadermarkupType);
                        leadermarkupSubtype = parseInt(leadermarkupSubtype);
                        leadermarkupAlternative = parseInt(leadermarkupAlternative);
                        var leaderxmlmarkupobj = new MarkupObject(leadermarkupType, leadermarkupSubtype, leadermarkupAlternative);
                        var blOK = leaderxmlmarkupobj.SetfromXML(DocObj,arrowleader, fileVersion);

                        if(blOK){
                            markupentityobj[i].removeChild(arrowleader);
                            //var markupfiles = DocRef.getnummarkupfiles();
                            //var received = DocRef.getmarkupfilesreceived();
                            //var lastmarkup = (i == markupentityobj.length - 1 && (markupfiles == received));
                            
                            leaderxmlmarkupobj.savemetolistLoad(DocObj,false);

                        }

                    }

                }

                markupType = parseInt(markupType);
                markupSubtype = parseInt(markupSubtype);
                markupAlternative = parseInt(markupAlternative);



                var xmlmarkupobj = new MarkupObject(markupType, markupSubtype, markupAlternative);
                xmlmarkupobj.markupID = markupID;
                

                var bOK = xmlmarkupobj.SetfromXML(DocObj,markupentityobj[i], fileVersion);
                if (bOK) {
                    
                    if(blOK && bhasArrow && leaderxmlmarkupobj != null){
                        xmlmarkupobj.bhasArrow = bhasArrow;
                        xmlmarkupobj.markupArrowConnected = leaderxmlmarkupobj;
                        leaderxmlmarkupobj.bisTextArrow = true;
                        leaderxmlmarkupobj.textBoxConnected = xmlmarkupobj;
                        leaderxmlmarkupobj.setArrowPoint(xmlmarkupobj);

                        blOK = false;
                        bhasArrow = false;
                    }


                    xmlmarkupobj.savemetolistLoad(DocObj);
                }

                //xmlmarkupobj.markupnumber = parseInt(markupID);

                if (markupentityobj[i].getElementsByTagName("Signature")[0].firstChild.nodeValue != undefined){
                    var sign = markupentityobj[i].getElementsByTagName("Signature")[0].firstChild.nodeValue;

                    if (markupentityobj[i].getElementsByTagName("Name")[0].firstChild.nodeValue != undefined){
                        var dispname = markupentityobj[i].getElementsByTagName("Name")[0].firstChild.nodeValue;
                    }
                    
                    if (sign && dispname){
                        if (GetDisplayName(sign) == 'default user'){
                            numUsers = Userlist.length;
                            var layerColor = getMarkupLayerColor(xmlmarkupobj.layer);
                            Userlist.push(new Users(sign, dispname, xmlmarkupobj.layer, layerColor, false));
                            //Userlist[numUsers] = new Users(sign, dispname, xmlmarkupobj.layer, layerColor);
                            numUsers = Userlist.length;
                        }

                    }
                    
                }


            }

            /* Andriy call callback only once */
            /*for(var mc = 0; mc < DocObj.markuplist.length; mc++){
                if(DocObj.markuplist[mc].bhasArrow && !isNaN(DocObj.markuplist[mc].markupArrowConnected)){
                    var markupnum = DocObj.markuplist[mc].markupArrowConnected;
                    for(var ac = 0; ac < DocObj.markuplist.length; ac++){
                        if (DocObj.markuplist[ac].markupnumber == markupnum){
                            DocObj.markuplist[mc].markupArrowConnected = DocObj.markuplist[ac];
                        }
                    }
                }

            }*/

            //connect markup text box with leader arrow
            
            

            if (xmlmarkupobj) xmlmarkupobj.notify();

            if (markupentityobj.length == 0) {
                if (RxCore_GUI_Markuplist != undefined) {
                    RxCore_GUI_Markuplist.setMarkupList(DocObj.getmarkuplist(),DocObj.bActive);
                    /* Andriy */
                    // RxCore_GUI_Markuplist.notify();
                }

            }

            /*if (RxCore_GUI_Users != undefined) {
                RxCore_GUI_Users.setUserlist(Userlist);
            }*/

            /*if (Rxcore_GUI_fileLoadComplete != undefined) {
                Rxcore_GUI_fileLoadComplete.loadComplete(MarkupFile);
            }*/

            drawmarkupAll(cntximg);

        }

    }

    function get3DViews(ViewsFile, DocRef){

        if(DocRef.getCurrentpage()){
            if(DocRef.getCurrentpage().Vector3DPageObj){

                //DocRef.pages[0].Vector3DPageObj.viewsLoad(ViewsFile);
    
                var rtype = '';
                sendHttpRequest('GET', ViewsFile, rtype).then(function (response){
                    var myArr = JSON.parse(response);
    
                    DocRef.getCurrentpage().Vector3DPageObj.addCameraViews(myArr);
    
                }).catch(function(error){
               
                    if(error.status){
            
                    }else{
                      
                        console.log(error);
                        
                    }
        
        
                });
    
            }
        }

        
    }

    function get3DMarkup(MarkupFile, DocRef) {

        if(DocRef.getCurrentpage()){

            if(DocRef.getCurrentpage().Vector3DPageObj){
    
                DocRef.getCurrentpage().Vector3DPageObj.markupLoad(MarkupFile);
            }
    
        }

                
    }

    function getMarkup(MarkupFile, DocRef) {

        var PageObj;

        sendHttpRequestXML('GET', MarkupFile).then(function (xmlDoc){



            markupXMLhandler(xmlDoc, DocRef, MarkupFile);

               


        }).catch( function(error) {

            if(error.status){
                if (error.status == 404) {
                    console.log("XML could not be found");
                } else if (error.status == 503) {
                    console.log("Server is down");
                } else if (error.status == 501) {
                    console.log("Markup could not be loaded");
                }
            }else{
                console.log(error);
            }
        });

    }


    /* Andriy: According to ECMAScript standard,function statement not in the top level prohibited */
    /* function f() {
     function g() {
     // IT IS OK!
     }
     }

     function f() {
     if () {
     function g() {
     // NOT OK! As it is inside if statement, as it was with createAndAppendElement below.
     }
     }
     }
     It does not change logic, just lift up function declaration to ensure it works correctly in modern browsers.
     Update: Lift it up to the top level as it was declared twice inside sendMarkup and
     */

    function createAppendElementret(source, name, value){
        var node = source.ownerDocument.createElement(name);
        node.appendChild(source.ownerDocument.createTextNode(value));
        source.appendChild(node);
        return node;
    }

    function createAndAppendElementwithAttributes(source, name, value, attributes) {
        
        
        var node = source.ownerDocument.createElement(name);

        for(var ai = 0; ai < attributes.length; ai++){
            node.setAttribute(attributes[ai].name, attributes[ai].value);
        }

        
        

        node.appendChild(source.ownerDocument.createTextNode(value));
        source.appendChild(node);
    }

    function createAndAppendElementwithAttribute(source, name, value, attribute) {
        
        
        var node = source.ownerDocument.createElement(name);

        node.setAttribute(attribute.name, attribute.value);
        

        node.appendChild(source.ownerDocument.createTextNode(value));
        source.appendChild(node);
    }


    function createAndAppendElement(source, name, value) {
        var node = source.ownerDocument.createElement(name);
        node.appendChild(source.ownerDocument.createTextNode(value));
        source.appendChild(node);
    }
    

    function createJSONmarkupentitynode(markupobj, DocRef, pageobject, bgetArrow, operation){

        var savetime = new Date().getTime();
        var internalscale = 1;
        var fixedscale = 1;
        var imagescale = 1;
        var leaders = [];
        var leaderobj = {};

        var entityJSONNode = {
            operation : null,
            Entity : {
                ID : markupobj.markupnumber, 
                Image : null,
                UniqueID : null,
                Imagehref : null,
                SvgContent: null,
                Type : null,
                Subtype : null,
                Alternative : null,
                Redaction : null,
                Layer : null,
                Rotation : null,
                TextRotation : null,
                PageRotation : null,
                FileName : null,
                Layout : null,
                View : null,
                ViewName3D : null,
                For3DView : null,
                PageHeight3D : null,
                PageWidth3D : null,
                ViewName : null,
                Handle : null,
                FillColor : null,
                LineColor : null,
                TextColor : null,
                Transparency : null,
                Consolidated : null,
                HaveLink : null,
                HaveLeader : null,
                LeaderReference : null,
                Entity : null,
                FixedScale : null,
                LineStyleScale : null,
                LinkURL : null,
                Font : null,
                Text : null,
                Status : null,
                SmallText : null,
                DimText : null,
                CustomLabelOn : null,
                CustomLabelText : null,
                TextWidth : null,
                Signature : null,
                Name : null,
                TimeStamp : null,
                Scaling : null,
                LoadScaling : null,
                Printscale : null,
                Xoffset : null,
                Yoffset : null,
                LineWidth : null,
                LineWidthSegment : null,
                ArrowSize : null,
                LineStyle : null,
                MainImageScaling : null,
                MainImageOffsetX : null,
                MainImageOffsetY : null,
                DocScale : null,
                DocOffsetX : null,
                DocOffsetY : null,
                Points : {Point : []},
                Line : [],
                Holes : [],
                outerRadius : null,
                innerRadius : null,
                startX : null,
                startY : null,
                secondX : null,
                secondY : null,
                endX : null,
                endY : null,

                LeaderLineOffset : null,
                LabelOffsetX : null,
                LabelOffsetY : null,
                LabelColor : null,
                Rect : null,
                Extended : [],
                Comments : []

            }
        };
        




        if(markupobj.markupID == 0 || markupobj.markupID == null){
            markupobj.markupID = markupobj.markupnumber;
        }

        

        if(operation){
            entityJSONNode.operation = operation;
                //{created : false, modified : false, deleted : false}
        }

            
        
        

        if (markupobj != null && !markupobj.pdfannotproxy) {

            var arrowleader = null;

            if(!bgetArrow){
                if(markupobj.bisTextArrow && markupobj.textBoxConnected != null){
                    //arrowleader = createJSONmarkupentitynode(markupobj, DocRef, pageobject, true);
                    arrowleader = JSON.parse(createJSONmarkupentitynode(markupobj, DocRef, pageobject, true));
                    //markupobj, DocRef, pageobject, bgetArrow, operation
                    markupobj = markupobj.textBoxConnected;
                }
            }

            

            var pagenumber = markupobj.pagenumber;
            //var pageobject = DocRef.getPageObject(pagenumber);
            var thumbnailobject = DocRef.getThumb(pagenumber);


            var bpageExist = (pageobject != undefined);
            var viewname3D = markupobj.viewname;
            var bisdrawn = markupobj.isdrawn();

            
            if(bpageExist){

                if (pageobject.usepdfjs || pageobject.usevectorxml) {
                    internalscale = markupobj.scaling;
                }else{
                    imagescale = markupobj.scaling;
                }
                
                fixedscale = pageobject.fixedScale;

                if(fixedscale == 1){

                    if(pageobject.pdfpageheight == 0 && pageobject.pdfpagewidth == 0){
                        //no page info
                    }else{
                        var yscale = fixedScaleSize.height / pageobject.pdfpageheight; //thispage.MainImageHeight;
                        var xscale = fixedScaleSize.width / pageobject.pdfpagewidth; // thispage.MainImageWidth;
                        
                        fixedscale = Math.min(xscale, yscale);
    
                    }

                }

                var bscalefinite = isFinite(internalscale);

                var bsfcalefinite = isFinite(fixedscale);

                if(!bscalefinite){
                    internalscale = 1;
                }

                if(!bsfcalefinite){
                    fixedscale = 1;
                }


                if(internalscale == 0){
                    internalscale = 1;
                }

                if(isNaN(internalscale)){
                    internalscale = 1;
                }


                if(fixedscale == undefined){
                    fixedscale = 1;
                }

                if(isNaN(fixedscale)){
                    fixedscale = 1;
                }

                if(fixedscale == 0){
                    fixedscale = 1;
                }                

                var bRedact = (markupobj.type == 3 && markupobj.subtype == 4);
                entityJSONNode.Entity.ID = markupobj.markupnumber;

                //entityJSONNode = {Entity : {ID : markupobj.markupnumber }};
                //entityNode = markupxml.createElement("Entity");
                //entityNode.setAttribute("ID", markupobj.markupnumber);

                if(markupobj.type == 11 || markupobj.type == 20){

                    

                    
                    entityJSONNode.Entity.Image = {image : getBase64Image(markupobj.image), type : "png", encoding : "base64" };

                    

                    /*
                    getBase64Image(markupobj.image),
                    type : "png", 
                    encoding : "base64"
                    }
                    */

                    if(markupobj.svgContent) {
                        entityJSONNode.Entity.SvgContent = markupobj.svgContent;
                    }



                }

                entityJSONNode.Entity.UniqueID = markupobj.uniqueID;


                //createAndAppendElement(entityNode, "UniqueID", markupobj.uniqueID);
                if((markupobj.type == 11 || markupobj.type == 20) && markupobj.imagehref != ""){                
                //if(markupobj.type == 11 && markupobj.imagehref != ""){

                    
                    entityJSONNode.Entity.Imagehref = markupobj.imagehref;
                    


                    //createAndAppendElement(entityNode, "Imagehref", markupobj.imagehref);    
                }

                //createAndAppendElement(entityNode, "Type", markupobj.type);
                //createAndAppendElement(entityNode, "Subtype", markupobj.subtype);
                //createAndAppendElement(entityNode, "Alternative", markupobj.alternative);
                //createAndAppendElement(entityNode, "Redaction", markupobj.bRedact ? 1:0);
                //createAndAppendElement(entityNode, "Layer", markupobj.layer);
                //createAndAppendElement(entityNode, "Rotation", markupobj.rotation);
                //createAndAppendElement(entityNode, "TextRotation", markupobj.textrotate);
                //createAndAppendElement(entityNode, "PageRotation", markupobj.pagerotation);
                //createAndAppendElement(entityNode, "FileName", markupobj.documentName);
                //createAndAppendElement(entityNode, "Layout", pageobject.LayoutName);
                //createAndAppendElement(entityNode, "View", pagenumber);

                entityJSONNode.Entity.Type = markupobj.type;
                entityJSONNode.Entity.Subtype = markupobj.subtype;
                entityJSONNode.Entity.Alternative = markupobj.alternative;
                entityJSONNode.Entity.Redaction = markupobj.bRedact ? 1:0;
                entityJSONNode.Entity.Layer = markupobj.layer;
                entityJSONNode.Entity.Rotation = markupobj.rotation;
                entityJSONNode.Entity.TextRotation = markupobj.textrotate;
                entityJSONNode.Entity.PageRotation = markupobj.pagerotation;
                entityJSONNode.Entity.FileName = markupobj.documentName;
                entityJSONNode.Entity.Layout = pageobject.LayoutName;
                entityJSONNode.Entity.View = pagenumber;


                if(viewname3D != null){
                    //createAndAppendElement(entityNode, "ViewName3D", viewname3D);
                    //createAndAppendElement(entityNode, "For3DView", markupobj.bfor3D ? 1:0);
                    //createAndAppendElement(entityNode, "PageHeight3D", markupobj.pageheight);
                    //createAndAppendElement(entityNode, "PageWidth3D", markupobj.pagewidth);

                    entityJSONNode.Entity.ViewName3D = viewname3D;
                    entityJSONNode.Entity.For3DView = markupobj.bfor3D ? 1:0;
                    entityJSONNode.Entity.PageHeight3D = markupobj.pageheight;
                    entityJSONNode.Entity.PageWidth3D = markupobj.pagewidth;




                }

                //createAndAppendElement(entityNode, "ViewName", pageobject.PageName);
                
                //createAndAppendElement(entityNode, "ViewName", thumbnailobject.PageName);

                //createAndAppendElement(entityNode, "Handle", markupobj.markupnumber);
                //createAndAppendElement(entityNode, "Color", DocObj.markuplist[curmarkup].color);
                //createAndAppendElement(entityNode, "FillColor", markupobj.fillcolor);


                entityJSONNode.Entity.ViewName = thumbnailobject.PageName;
                entityJSONNode.Entity.Handle = markupobj.bfor3D ? 1:0;
                entityJSONNode.Entity.FillColor = markupobj.fillcolor;







                if(bRedact){
                    //createAndAppendElement(entityNode, "LineColor", markupobj.fillcolor);

                    /*LineColor : markupobj.strokecolor,
                    TextColor : markupobj.textcolor,
                    Transparency : markupobj.transparency,
                    Consolidated : markupobj.consolidated ? 1:0,
                    HaveLink : markupobj.bhaveLink ? 1:0,
                    HaveLeader : markupobj.bhasArrow ? 1:0*/
                    entityJSONNode.Entity.LineColor = markupobj.fillcolor;
    

                }else{
                    //createAndAppendElement(entityNode, "LineColor", markupobj.strokecolor);
                    entityJSONNode.Entity.LineColor = markupobj.strokecolor;
                }


                
                //createAndAppendElement(entityNode, "TextColor", markupobj.textcolor);
                //createAndAppendElement(entityNode, "Transparency", markupobj.transparency);
                //createAndAppendElement(entityNode, "Consolidated", markupobj.consolidated ? 1:0);
                //createAndAppendElement(entityNode, "HaveLink", markupobj.bhaveLink ? 1:0);
                //createAndAppendElement(entityNode, "HaveLeader", markupobj.bhasArrow ? 1:0);

                entityJSONNode.Entity.TextColor = markupobj.textcolor;
                entityJSONNode.Entity.Transparency = markupobj.transparency;
                entityJSONNode.Entity.Consolidated = markupobj.consolidated ? 1:0;
                entityJSONNode.Entity.HaveLink = markupobj.bhaveLink ? 1:0;
                entityJSONNode.Entity.HaveLeader = markupobj.bhasArrow ? 1:0;




                if (markupobj.bhasArrow && markupobj.markupArrowConnected != null){
                    //createAndAppendElement(entityNode, "LeaderReference", markupobj.markupArrowConnected.markupnumber);

                    entityJSONNode.Entity.LeaderReference = markupobj.markupArrowConnected.markupnumber;

                    if(arrowleader != null){
                        //entityNode.appendChild(arrowleader);

                        entityJSONNode.Entity.Entity = arrowleader;



                    }else{
                        //createJSONmarkupentitynode(markupobj, DocRef, pageobject, bgetArrow, operation)
                        arrowleader = JSON.parse(createJSONmarkupentitynode(markupobj.markupArrowConnected, DocRef, pageobject, true, operation));
                        entityJSONNode.Entity.Entity = arrowleader;
                    }
                }

                var areahole = null;

                if(markupobj.holes.length > 0){
    
                    for (var h = 0; h < markupobj.holes.length; h++){
                        areahole = JSON.parse(createJSONmarkupentitynode(markupobj.holes[h], DocRef, pageobject, false));
                        entityJSONNode.Entity.Holes.push(areahole);
                    }
                    
                }
    
                

                /*if (markupobj.bhasArrow && markupobj.markupArrowConnected != null){

                    createAndAppendElement(entityNode, "LeaderReference", markupobj.markupArrowConnected.markupnumber);
                    //leaders.push({entity : entityNode, mnumber : DocObj.markuplist[curmarkup].markupnumber});
                    for(var lcount = 0; lcount < leaders.length; lcount++){
                        if(leaders[lcount].mnumber == markupobj.markupArrowConnected.markupnumber){
                            entityNode.appendChild(leaders[lcount].entity);
                        }
                    }

                }*/

                entityJSONNode.Entity.FixedScale = markupobj.bUseFixedScale ? 1:0;
                entityJSONNode.Entity.FixedScaleFactor = markupobj.fixedscaleFactor;
                entityJSONNode.Entity.LineStyleScale = fixedscale;
                entityJSONNode.Entity.LinkURL = markupobj.linkURL;
                

                //createAndAppendElement(entityNode, "FixedScale", bUseFixedScale ? 1:0);
                //createAndAppendElement(entityNode, "LineStyleScale", fixedscale);
                //createAndAppendElement(entityNode, "LinkURL", markupobj.linkURL);

                //var fontNode = markupxml.createElement("Font");

                var fontobj = {Font : {facename:null, height:null, bold:null, italic:null}};
                
                fontobj.Font.facename = markupobj.font.fontName;

                //fontNode.setAttribute("facename", markupobj.font.fontName);



                if(bisdrawn){
                    if (pageobject.usepdfjs || pageobject.usevectorxml) {
                        if(markupobj.type != 12){
                            if(markupobj.bUseFixedScale){
                                //fontNode.setAttribute("height", markupobj.font.height / fixedscale);
                                fontobj.Font.height = markupobj.font.height / fixedscale;
                                
                            }else{
                                //fontNode.setAttribute("height", markupobj.font.height / internalscale);        
                                fontobj.Font.height = markupobj.font.height / internalscale;
                            }  
                        }else{
                            //fontNode.setAttribute("height", markupobj.font.height / internalscale);
                            fontobj.Font.height = markupobj.font.height / internalscale;
                        }
                        
                    } else {
                        if(markupobj.type != 12){
                            if(markupobj.bUseFixedScale){
                                //fontNode.setAttribute("height", (markupobj.font.height / fixedscale) * imagescale);
                                //fontNode.setAttribute("height", markupobj.font.height / fixedscale);
                                fontobj.Font.height = markupobj.font.height / fixedscale;
                            }else{
                                //fontNode.setAttribute("height", markupobj.font.height);    
                                //fontNode.setAttribute("height", markupobj.font.height / imagescale);

                                fontobj.Font.height = markupobj.font.height / imagescale;
                            }
                            
                        }else{
                            //fontNode.setAttribute("height", markupobj.font.height);
                            //fontNode.setAttribute("height", markupobj.font.height / imagescale);

                            fontobj.Font.height = markupobj.font.height / imagescale;
    
                        }
                        
                    }

                }else{
                    //fontNode.setAttribute("height", markupobj.font.height);

                    fontobj.Font.height = markupobj.font.height;
                }

                

                //fontNode.setAttribute("height", markupobj.font.height);
                                
                //fontNode.setAttribute("bold", markupobj.font.bold ? 1 : 0);
                //fontNode.setAttribute("italic", markupobj.font.italic ? 1 : 0);

                //var fontobj = {Font : {facename=null height=null bold=null italic=null}};

                fontobj.Font.bold = markupobj.font.bold ? 1 : 0;
                fontobj.Font.bold = markupobj.font.italic ? 1 : 0;

                entityJSONNode.Entity.Font = fontobj;

                //entityNode.appendChild(fontNode);




                //createAndAppendElement(entityNode, "Text", markupobj.text == "" ? "." : markupobj.text);
                //createAndAppendElement(entityNode, "SmallText", markupobj.smalltext == "" ? "." : markupobj.smalltext);
                //createAndAppendElement(entityNode, "DimText", markupobj.dimtext == "" ? "." : markupobj.dimtext);

                //createAndAppendElement(entityNode, "CustomLabelOn", markupobj.busecustomlabel ? 1:0);
                //createAndAppendElement(entityNode, "CustomLabelText", markupobj.customlabeltext == "" ? "." : markupobj.customlabeltext);

                //createAndAppendElement(entityNode, "TextWidth", markupobj.textwidth);
                //createAndAppendElement(entityNode, "Signature", markupobj.signature);


                var displayname = GetDisplayName(markupobj.signature);
                //createAndAppendElement(entityNode, "Name", displayname);
    

                //createAndAppendElement(entityNode, "TimeStamp", markupobj.timestamp);

                entityJSONNode.Entity.Text = markupobj.text == "" ? "." : markupobj.text;
                entityJSONNode.Entity.SmallText = markupobj.smalltext == "" ? "." : markupobj.smalltext;
                entityJSONNode.Entity.DimText = markupobj.dimtext == "" ? "." : markupobj.dimtext;

                entityJSONNode.Entity.Status = markupobj.status;




                entityJSONNode.Entity.CustomLabelOn = markupobj.busecustomlabel ? 1:0;
                entityJSONNode.Entity.CustomLabelText = markupobj.signature;

                entityJSONNode.Entity.TextWidth = markupobj.textwidth;
                entityJSONNode.Entity.Signature = markupobj.signature;
                entityJSONNode.Entity.Name = displayname;
                entityJSONNode.Entity.TimeStamp = markupobj.timestamp;

                
                


                var bmscalefinite = isFinite(markupobj.scaling);

                
                if(!bmscalefinite){
                    markupobj.scaling = 1;
                }


                if(markupobj.scaling == 0){
                    markupobj.scaling = 1;
                }

                if(isNaN(markupobj.scaling)){
                    markupobj.scaling = 1;
                }                

                

                //createAndAppendElement(entityNode, "Scaling", markupobj.scaling);

                entityJSONNode.Entity.Scaling = markupobj.scaling;

                if(markupobj.loadscaling != undefined){
                    entityJSONNode.Entity.LoadScaling = markupobj.loadscaling;
                }
                



                //new property for Lydia to handle ledgend images
                //createAndAppendElement(entityNode, "Printscale", markupobj.printscale);

                entityJSONNode.Entity.Printscale = markupobj.printscale;


                var bxoffsetfinite = isFinite(markupobj.xoffset);
                var byoffsetfinite = isFinite(markupobj.yoffset);

                
                if(!bxoffsetfinite){
                    markupobj.xoffset = 0;
                }
                
                if(!byoffsetfinite){
                    markupobj.yoffset = 0;
                }

                if(isNaN(markupobj.xoffset)){
                    markupobj.xoffset = 0;
                }

                if(isNaN(markupobj.yoffset)){
                    markupobj.yoffset = 0;
                }

                
                if(bisdrawn){
                    if (pageobject.usepdfjs || pageobject.usevectorxml) {
                        //createAndAppendElement(entityNode, "Xoffset", markupobj.xoffset / internalscale);
                        //createAndAppendElement(entityNode, "Yoffset", markupobj.yoffset / internalscale);


                        entityJSONNode.Entity.Xoffset = markupobj.xoffset / internalscale;
                        entityJSONNode.Entity.Yoffset = markupobj.yoffset / internalscale;
        

    
                    } else {
                        //createAndAppendElement(entityNode, "Xoffset", markupobj.xoffset);
                        //createAndAppendElement(entityNode, "Yoffset", markupobj.yoffset);

                        entityJSONNode.Entity.Xoffset = markupobj.xoffset;
                        entityJSONNode.Entity.Yoffset = markupobj.yoffset;

                    }
    
                }else{
                    //createAndAppendElement(entityNode, "Xoffset", markupobj.xoffset);
                    //createAndAppendElement(entityNode, "Yoffset", markupobj.yoffset);

                    entityJSONNode.Entity.Xoffset = markupobj.xoffset;
                    entityJSONNode.Entity.Yoffset = markupobj.yoffset;


                }

                var localLinewidth = markupobj.linewidth;
                var localLinewidthSegment = markupobj.linewidthSegment;




                if(bisdrawn){
                    if (pageobject.usepdfjs || pageobject.usevectorxml) {
                        if(bRedact){
                            //createAndAppendElement(entityNode, "LineWidth", 1);

                            entityJSONNode.Entity.LineWidth = 1;
                            
                        }else{
                            if(markupobj.bUseFixedScale){
                            
                                
                                var linescale = ( localLinewidth / fixedscale);
                                var linescalesegment = ( localLinewidthSegment / fixedscale);
    
                                //createAndAppendElement(entityNode, "LineWidth", linescale);
                                //createAndAppendElement(entityNode, "LineWidthSegment", linescalesegment);

                                entityJSONNode.Entity.LineWidth = linescale;
                                entityJSONNode.Entity.LineWidthSegment = linescalesegment;
    
    
                            }else{
                                //createAndAppendElement(entityNode, "LineWidth", localLinewidth / internalscale);
                                //createAndAppendElement(entityNode, "LineWidthSegment", localLinewidthSegment / internalscale);

                                entityJSONNode.Entity.LineWidth = localLinewidth / internalscale;
                                entityJSONNode.Entity.LineWidthSegment = localLinewidthSegment / internalscale;

                            }
    
                        }
                        
                    } else {
                        
                        if(bRedact){
                            //createAndAppendElement(entityNode, "LineWidth", 1);

                            entityJSONNode.Entity.LineWidth = 1;
                            
                        }else{
                            if(markupobj.bUseFixedScale){
                                //linescale = (localLinewidth / fixedscale) * imagescale;
                                linescale = (localLinewidth / fixedscale);
                                
                                linescalesegment = ( localLinewidthSegment / fixedscale);

                                //createAndAppendElement(entityNode, "LineWidth", linescale);
                                //createAndAppendElement(entityNode, "LineWidthSegment", linescalesegment);

                                entityJSONNode.Entity.LineWidth = linescale;
                                entityJSONNode.Entity.LineWidthSegment = linescalesegment;

    
                            }else{
                                //createAndAppendElement(entityNode, "LineWidth", localLinewidth);
                                //createAndAppendElement(entityNode, "LineWidth", localLinewidth / imagescale);
                                //createAndAppendElement(entityNode, "LineWidthSegment", localLinewidthSegment / imagescale );

                                entityJSONNode.Entity.LineWidth = localLinewidth / imagescale;
                                entityJSONNode.Entity.LineWidthSegment = localLinewidthSegment / imagescale;

                            }
    
                        }
                        
                    }       
                }else{
                    //createAndAppendElement(entityNode, "LineWidth", localLinewidth);
                    //createAndAppendElement(entityNode, "LineWidthSegment", localLinewidthSegment);
                }

                         
                

                var localArrowSize = markupobj.arrowlength - markupobj.linewidth;
                //createAndAppendElement(entityNode, "ArrowSize", localArrowSize);

                entityJSONNode.Entity.ArrowSize = localArrowSize;
                

                

                



                //createAndAppendElement(entityNode, "LineStyle", markupobj.linestyle);
                entityJSONNode.Entity.LineStyle = markupobj.linestyle;

                //createAndAppendElement(entityNode, "MainImageScaling", pageobject.MainImageScaling);
                //createAndAppendElement(entityNode, "MainImageOffsetX", pageobject.MainImageOffsetX);
                //createAndAppendElement(entityNode, "MainImageOffsetY", pageobject.MainImageOffsetY);

                //createAndAppendElement(entityNode, "DocScale", markupobj.docdscale);
                //createAndAppendElement(entityNode, "DocOffsetX", markupobj.docdx);
                //createAndAppendElement(entityNode, "DocOffsetY", markupobj.docdy);

                entityJSONNode.Entity.MainImageScaling = pageobject.MainImageScaling;
                entityJSONNode.Entity.MainImageOffsetX = pageobject.MainImageOffsetX;
                entityJSONNode.Entity.MainImageOffsetY = pageobject.MainImageOffsetY;


                entityJSONNode.Entity.DocScale = markupobj.docdscale;
                entityJSONNode.Entity.DocOffsetX = markupobj.docdx;
                entityJSONNode.Entity.DocOffsetY = markupobj.docdy;


                //var pointarray = [];

                //var pointsobj = {}

                if(bisdrawn){
                    var bsegmentmarkup = (markupobj.type == 1 && markupobj.subtype == 6);

                    if(markupobj.type != 0 && !bsegmentmarkup ){
                        if (markupobj.points.length != -1) {
                            //var pointsNode = markupxml.createElement("Points");


                            for (var i = 0; i < markupobj.points.length; i++) {
                                //var pointNode = markupxml.createElement("Point");

                                
    
                                if (pageobject.usepdfjs || pageobject.usevectorxml) {
                                    var xpoint = (markupobj.points[i].x - markupobj.xoffset) / internalscale;
                                    var ypoint = (markupobj.points[i].y - markupobj.yoffset) / internalscale;

                                    
                                    entityJSONNode.Entity.Points.Point.push({x: xpoint, y : ypoint});
                                    
    
                                    //pointNode.setAttribute("x", xpoint);
                                    //pointNode.setAttribute("y", ypoint);
    
                                } else {

                                    xpoint = (markupobj.points[i].x - markupobj.xoffset) / imagescale;
                                    ypoint = (markupobj.points[i].y - markupobj.yoffset) / imagescale;

                                    entityJSONNode.Entity.Points.Point.push({x: xpoint, y : ypoint});
                                    

                                    //pointNode.setAttribute("x", xpoint);
                                    //pointNode.setAttribute("y", ypoint);

                                    //pointNode.setAttribute("x", markupobj.points[i].x);
                                    //pointNode.setAttribute("y", markupobj.points[i].y);
    
                                }
                                //pointsNode.appendChild(pointNode);

                            }
                            //entityNode.appendChild(pointsNode);
                        }
                    }else{
                        

                        if (markupobj.pointlist.length != 0) {
                            entityJSONNode.Entity.Line = [];

                            for(var j = 0; j < markupobj.pointlist.length;j++){
                                
                                

                                //var linesNode = markupxml.createElement("Line");

                                //var Point = [];
                                //entityJSONNode.Entity.Line.push(Point);
                                entityJSONNode.Entity.Line.push({Point : []});
                                
                                

                                var pointarray = {}


                                for (i = 0; i < markupobj.pointlist[j].length; i++) {
                                    //pointNode = markupxml.createElement("Point");
                                    if (pageobject.usepdfjs || pageobject.usevectorxml) {
                                        xpoint = (markupobj.pointlist[j][i].x - markupobj.xoffset) / internalscale;
                                        ypoint = (markupobj.pointlist[j][i].y - markupobj.yoffset) / internalscale;
    
                                        //entityJSONNode.Points.Point.push({x: xpoint, y : ypoint});

                                        entityJSONNode.Entity.Line[j].Point.push({x: xpoint, y : ypoint}); //=              Line : { {Point : []};

                                        //pointNode.setAttribute("x", xpoint);
                                        //pointNode.setAttribute("y", ypoint);
    
                                    } else {

                                        xpoint = (markupobj.pointlist[j][i].x - markupobj.xoffset) / imagescale;
                                        ypoint = (markupobj.pointlist[j][i].y - markupobj.yoffset) / imagescale;

                                        entityJSONNode.Entity.Line[j].Point.push({x: xpoint, y : ypoint});

                                        //pointNode.setAttribute("x", xpoint);
                                        //pointNode.setAttribute("y", ypoint);


                                        //pointNode.setAttribute("x", markupobj.pointlist[j][i].x);
                                        //pointNode.setAttribute("y", markupobj.pointlist[j][i].y);
    
                                    }
                                    //linesNode.appendChild(pointNode);
                                }
                                //entityNode.appendChild(linesNode);
                            }
    
    
                        }
    
                    }               
                }else{
                    if(markupobj.type != 0 ){
                        if (markupobj.points.length != -1) {
                            
                            //pointsNode = markupxml.createElement("Points");


                            for (var i = 0; i < markupobj.points.length; i++) {
                                //var pointNode = markupxml.createElement("Point");

                                entityJSONNode.Entity.Points.Point.push({x: markupobj.points[i].x, y : markupobj.points[i].y});

                                //pointNode.setAttribute("x", markupobj.points[i].x);
                                //pointNode.setAttribute("y", markupobj.points[i].y);

                                //pointsNode.appendChild(pointNode);
                            }
                            //entityNode.appendChild(pointsNode);
                        }
                    }else{
                        if (markupobj.pointlist.length != 0) {

                            entityJSONNode.Entity.Line = [];

                            for(var j = 0; j < markupobj.pointlist.length;j++){

                                
                                //entityJSONNode.Entity.Line.push({Point : []]);
                                entityJSONNode.Entity.Line.push({Point : []});
                                //var linesNode = markupxml.createElement("Line");
                                
                                for (i = 0; i < markupobj.pointlist[j].length; i++) {
                                    //pointNode = markupxml.createElement("Point");

                                    entityJSONNode.Entity.Line[i].Point.push({x: markupobj.pointlist[j][i].x, y : markupobj.pointlist[j][i].y});

                                    //pointNode.setAttribute("x", markupobj.pointlist[j][i].x);
                                    //pointNode.setAttribute("y", markupobj.pointlist[j][i].y);

                                    //linesNode.appendChild(pointNode);
                                }

                                //entityNode.appendChild(linesNode);
                            }
    
                        }
    
                    }     
                }

                

                if(bisdrawn){
                    if(markupobj.type == 13){
                        if (markupobj.countshapes.length != -1) {
    
                            /*modified to same logic as linewidth*/
    
                            var localouterRadius = markupobj.outerRadius;
                            var localinnerRadius = markupobj.innerRadius;
                          
                            if (pageobject.usepdfjs || pageobject.usevectorxml) {
                            
                                if(markupobj.bUseFixedScale){
                                    var outerRadiusscale = ( localouterRadius / fixedscale);
                                    var innerRadiusscale = ( localinnerRadius / fixedscale);
                                    
                                    //createAndAppendElement(entityNode, "outerRadius", outerRadiusscale);
                                    //createAndAppendElement(entityNode, "innerRadius", innerRadiusscale);

                                    entityJSONNode.Entity.outerRadius = outerRadiusscale;
                                    entityJSONNode.Entity.innerRadius = innerRadiusscale;


                                    
                                }else{
            
                                    //createAndAppendElement(entityNode, "outerRadius", localouterRadius / internalscale);    
                                    //createAndAppendElement(entityNode, "innerRadius", localinnerRadius / internalscale);    
                                
                                    entityJSONNode.Entity.outerRadius = localouterRadius / internalscale;
                                    entityJSONNode.Entity.innerRadius = localinnerRadius / internalscale;

                                }
                            
                            }else{
                            
                                if(markupobj.bUseFixedScale){
    
                                    //outerRadiusscale = ( localouterRadius / fixedscale) * imagescale;
                                    //innerRadiusscale = ( localinnerRadius / fixedscale) * imagescale;

                                    outerRadiusscale = ( localouterRadius / fixedscale);
                                    innerRadiusscale = ( localinnerRadius / fixedscale);

                            
                                    //createAndAppendElement(entityNode, "outerRadius", outerRadiusscale);
                                    //createAndAppendElement(entityNode, "innerRadius", innerRadiusscale);

                                    entityJSONNode.Entity.outerRadius = outerRadiusscale;
                                    entityJSONNode.Entity.innerRadius = innerRadiusscale;

                                    
                                    
                                }else{

                                    //createAndAppendElement(entityNode, "outerRadius", localouterRadius / imagescale);
                                    //createAndAppendElement(entityNode, "innerRadius", localinnerRadius / imagescale);

                                    entityJSONNode.Entity.outerRadius = localouterRadius / imagescale;
                                    entityJSONNode.Entity.innerRadius = localinnerRadius / imagescale;
        

                                    //createAndAppendElement(entityNode, "outerRadius", localouterRadius);
                                    //createAndAppendElement(entityNode, "innerRadius", localinnerRadius);
                                }
                            
                            }
                            /*modified to same logic as linewidth*/
    
    
                            //var pointsNode = markupxml.createElement("Points");
                            for (var i = 0; i < markupobj.countshapes.length; i++) {
                                //var pointNode = markupxml.createElement("Point");
    
                                if (pageobject.usepdfjs || pageobject.usevectorxml) {
                                    xpoint = (markupobj.countshapes[i].x - markupobj.xoffset) / internalscale;
                                    ypoint = (markupobj.countshapes[i].y - markupobj.yoffset) / internalscale;
    
                                    //pointNode.setAttribute("x", xpoint);
                                    //pointNode.setAttribute("y", ypoint);

                                    entityJSONNode.Entity.Points.Point.push({x: xpoint, y : ypoint});
    
                                } else {

                                    xpoint = (markupobj.countshapes[i].x - markupobj.xoffset) / imagescale;
                                    ypoint = (markupobj.countshapes[i].y - markupobj.yoffset) / imagescale;
    
                                    //pointNode.setAttribute("x", xpoint);
                                    //pointNode.setAttribute("y", ypoint);

                                    entityJSONNode.Entity.Points.Point.push({x: xpoint, y : ypoint});


                                    //pointNode.setAttribute("x", markupobj.countshapes[i].x);
                                    //pointNode.setAttribute("y", markupobj.countshapes[i].y);
    
                                }
                                //pointsNode.appendChild(pointNode);
                            }
                            //entityNode.appendChild(pointsNode);
                        }
                    }           


                    if(markupobj.type == 14){

                        //this.startX = null;
                        //this.startY = null;
                        //this.secondX = null;
                        //this.secondY = null;
                        //this.endX = null;
                        //this.endY = null;


                        var localstartX = markupobj.startX;
                        var localstartY = markupobj.startY;
                        var localsecondX = markupobj.secondX;
                        var localsecondY = markupobj.secondY;
                        var localendX = markupobj.endX;
                        var localendY = markupobj.endY;


                        if (pageobject.usepdfjs || pageobject.usevectorxml) {

                            
                            localstartX = ((localstartX - markupobj.xoffset) / internalscale);
                            localstartY = ((localstartY - markupobj.yoffset) / internalscale);

                            localsecondX = (localsecondX - markupobj.xoffset) / internalscale;
                            localsecondY = (localsecondY - markupobj.yoffset)  / internalscale;

                            localendX = (localendX - markupobj.xoffset) / internalscale;
                            localendY = (localendY - markupobj.yoffset) / internalscale;



                            /*if(bUseFixedScale){
                                //var xorig = (markupobj.x - markupobj.xoffset) / internalscale;
                                //var yorig = (markupobj.y - markupobj.yoffset) / internalscale;
        
                                localstartX = ((localstartX - markupobj.xoffset) / fixedscale);
                                localstartY = ((localstartY - markupobj.yoffset) / fixedscale);

                                localsecondX = (localsecondX - markupobj.xoffset) / fixedscale;
                                localsecondY = (localsecondY - markupobj.yoffset) / fixedscale;

                                localendX = (localendX - markupobj.xoffset) / fixedscale;
                                localendY = (localendY - markupobj.yoffset) / fixedscale;
        
                                
                            }else{

        
                            
                            }*/
                        
                        }else{
                        
                            /*if(bUseFixedScale){


                                localstartX = ( (localstartX - markupobj.xoffset) / fixedscale);
                                localstartY = ( (localstartY - markupobj.yoffset) / fixedscale);

                                localsecondX = (localsecondX - markupobj.xoffset) / fixedscale;
                                localsecondY = (localsecondY - markupobj.yoffset) / fixedscale;

                                localendX = (localendX - markupobj.xoffset) / fixedscale;
                                localendY = (localendY - markupobj.yoffset) / fixedscale;

                                
                                
                            }else{


                            }*/

                            localstartX = ((localstartX - markupobj.xoffset) / imagescale) / mainimagescale;
                            localstartY = ((localstartY - markupobj.yoffset) / imagescale) / mainimagescale;

                            localsecondX = ((localsecondX - markupobj.xoffset) / imagescale) / mainimagescale;
                            localsecondY = ((localsecondY - markupobj.yoffset) / imagescale) / mainimagescale;

                            localendX = ((localendX - markupobj.xoffset) / imagescale) / mainimagescale;
                            localendY = ((localendY - markupobj.yoffset) / imagescale) / mainimagescale;


                        }


                        entityJSONNode.Entity.startX = localstartX;
                        entityJSONNode.Entity.startY = localstartY;
                        

                        entityJSONNode.Entity.secondX = localsecondX;
                        entityJSONNode.Entity.secondY = localsecondY;


                        entityJSONNode.Entity.endX = localendX;
                        entityJSONNode.Entity.endY = localendY;




        
                    }

                }else{
                    if(markupobj.type == 13){
                        if (markupobj.countshapes.length != -1) {
    
                            /*modified to same logic as linewidth*/
    
                            var localouterRadius = markupobj.outerRadius;
                            var localinnerRadius = markupobj.innerRadius;

                            //createAndAppendElement(entityNode, "outerRadius", localouterRadius);
                            //createAndAppendElement(entityNode, "innerRadius", localinnerRadius);

                            //outerRadius : null,
                            //innerRadius : null,
            
                            entityJSONNode.Entity.outerRadius = localouterRadius;
                            entityJSONNode.Entity.innerRadius = localinnerRadius;
                            
                            /*modified to same logic as linewidth*/
                            //var pointsNode = markupxml.createElement("Points");
                            for (var i = 0; i < markupobj.countshapes.length; i++) {

                                //var pointNode = markupxml.createElement("Point");

                                entityJSONNode.Entity.Points.Point.push({x: markupobj.countshapes[i].x, y : markupobj.countshapes[i].y});

                                //pointNode.setAttribute("x", markupobj.countshapes[i].x);
                                //pointNode.setAttribute("y", markupobj.countshapes[i].y);

                                //pointsNode.appendChild(pointNode);
                            }
                            //entityNode.appendChild(pointsNode);
                        }
                    }

                    if(markupobj.type == 14){

                        localstartX = markupobj.startX;
                        localstartY = markupobj.startY;
                        localsecondX = markupobj.secondX;
                        localsecondY = markupobj.secondY;
                        localendX = markupobj.endX;
                        localendY = markupobj.endY;

                        entityJSONNode.Entity.startX = localstartX;
                        entityJSONNode.Entity.startY = localstartY;
                        

                        entityJSONNode.Entity.secondX = localsecondX;
                        entityJSONNode.Entity.secondY = localsecondY;


                        entityJSONNode.Entity.endX = localendX;
                        entityJSONNode.Entity.endY = localendY;



                    }
                    
                    


                }

                

                if(markupobj.type == 7){
                

                    if(bisdrawn){
                        if (pageobject.usepdfjs || pageobject.usevectorxml) {
                            //createAndAppendElement(entityNode, "LeaderLineOffset", markupobj.leaderoffset / internalscale);    

                            entityJSONNode.Entity.LeaderLineOffset = markupobj.leaderoffset / internalscale;
                        }else{
                            //createAndAppendElement(entityNode, "LeaderLineOffset", markupobj.leaderoffset);
                            entityJSONNode.Entity.LeaderLineOffset = markupobj.leaderoffset;

                        }
    
                    }else{
                        //createAndAppendElement(entityNode, "LeaderLineOffset", markupobj.leaderoffset);

                        entityJSONNode.Entity.LeaderLineOffset = markupobj.leaderoffset;

                        //LeaderLineOffset : null,
                    }

                    
                }

                

                if(markupobj.type == 8){

                    if(bisdrawn){
                        if (pageobject.usepdfjs || pageobject.usevectorxml) {
                            //createAndAppendElement(entityNode, "LabelOffsetX", markupobj.labeloffsetx / internalscale);
                            //createAndAppendElement(entityNode, "LabelOffsetY", markupobj.labeloffsety / internalscale);

                            entityJSONNode.Entity.LabelOffsetX = markupobj.labeloffsetx / internalscale;
                            entityJSONNode.Entity.LabelOffsetY = markupobj.labeloffsety / internalscale;

    
                        }else{
                            //createAndAppendElement(entityNode, "LabelOffsetX", markupobj.labeloffsetx);
                            //createAndAppendElement(entityNode, "LabelOffsetY", markupobj.labeloffsety);

                            entityJSONNode.Entity.LabelOffsetX = markupobj.labeloffsetx;
                            entityJSONNode.Entity.LabelOffsetY = markupobj.labeloffsety;
    
                        }
    
                    }else{
                        //createAndAppendElement(entityNode, "LabelOffsetX", markupobj.labeloffsetx);
                        //createAndAppendElement(entityNode, "LabelOffsetY", markupobj.labeloffsety);

                        entityJSONNode.Entity.LabelOffsetX = markupobj.labeloffsetx;
                        entityJSONNode.Entity.LabelOffsetY = markupobj.labeloffsety;

        

                    }

                    entityJSONNode.Entity.LabelColor = markupobj.labelfillcolor;
                    //createAndAppendElement(entityNode, "LabelColor", markupobj.labelfillcolor);

                    //LabelColor : null,
                    


                }  

                
                
                //var rectangleNode = markupxml.createElement("Rect");

                var bxfinite = isFinite(markupobj.x);
                var byfinite = isFinite(markupobj.y);
                var bwfinite = isFinite(markupobj.w);
                var bhfinite = isFinite(markupobj.h);

                
                if(!bxfinite){
                    markupobj.x = 0;
                }
                if(!byfinite){
                    markupobj.y = 0;
                }

                if(!bwfinite){
                    markupobj.w = 100;
                }

                if(!bhfinite){
                    markupobj.h = 100;
                }

                if(isNaN(markupobj.x)){
                    markupobj.x = 0;
                }
                if(isNaN(markupobj.y)){
                    markupobj.y = 0;
                }

                if(isNaN(markupobj.w)){
                    markupobj.w = 100;
                }
                
                if(isNaN(markupobj.h)){
                    markupobj.h = 100;
                }


                if(bisdrawn){
                    if (pageobject.usepdfjs || pageobject.usevectorxml) {


                        var xorig = (markupobj.x - markupobj.xoffset) / internalscale;
                        var yorig = (markupobj.y - markupobj.yoffset) / internalscale;
    
                        var bCircle = (markupobj.type == 4 && markupobj.subtype); 
                        if (markupobj.type == 0 || markupobj.type == 1 || markupobj.type == 2 || markupobj.type == 6 || markupobj.type == 7 || markupobj.type == 8 || bCircle) {
                            var worig = (markupobj.w - markupobj.xoffset) / internalscale;
                            var horig = (markupobj.h - markupobj.yoffset) / internalscale;
    
                        } else {
                            if (markupobj.type == 9 && markupobj.subtype == 0) {
                                worig = (markupobj.w / internalscale);
                                horig = (markupobj.textheight / internalscale);
                            } else if(markupobj.type == 10 && markupobj.bUseFixedScale) {
                                worig = (markupobj.w / fixedscale);
                                horig = (markupobj.h / fixedscale);
                            }else{
                                worig = (markupobj.w / internalscale);
                                horig = (markupobj.h / internalscale);
                            }
                        }
    
                        var bxorigfinite = isFinite(xorig);
                        var byorigfinite = isFinite(yorig);
                        var bworigfinite = isFinite(worig);
                        var bhorigfinite = isFinite(horig);
                        
                        
                        if(!bxorigfinite){
                            xorig = 0;
                        }
                        if(!byorigfinite){
                            yorig = 0;
                        }

                        if(!bworigfinite){
                            worig = 100;
                        }

                        if(!bhorigfinite){
                            horig = 100;
                        }
        
                        if(isNaN(xorig)){
                            xorig = 0;
                        }
                        if(isNaN(yorig)){
                            yorig = 0;
                        }

                        if(isNaN(worig)){
                            worig = 100;
                        }
                        
                        if(isNaN(horig)){
                            horig = 100;
                        }                        
    

                        //rectangleNode.setAttribute("x", xorig);
                        //rectangleNode.setAttribute("y", yorig);
                        //rectangleNode.setAttribute("w", worig);
                        //rectangleNode.setAttribute("h", horig);

                        entityJSONNode.Entity.Rect = {x : xorig, y: yorig, w: worig, h : horig};
    
                    } else {

                        var xorig = (markupobj.x - markupobj.xoffset) / imagescale;
                        var yorig = (markupobj.y - markupobj.yoffset) / imagescale;

    
                        var bCircle = (markupobj.type == 4 && markupobj.subtype); 
                        if (markupobj.type == 0 || markupobj.type == 1 || markupobj.type == 2 || markupobj.type == 6 || markupobj.type == 7 || markupobj.type == 8 || bCircle) {
                            var worig = (markupobj.w - markupobj.xoffset) / imagescale;
                            var horig = (markupobj.h - markupobj.yoffset) / imagescale;
    
                        } else {
                            if (markupobj.type == 9 && markupobj.subtype == 0) {
                                worig = (markupobj.w / imagescale);
                                horig = (markupobj.textheight / imagescale);
                            } else if(markupobj.type == 10 && markupobj.bUseFixedScale) {
                                worig = (markupobj.w / fixedscale);
                                horig = (markupobj.h / fixedscale);
                            }else{
                                worig = (markupobj.w / imagescale);
                                horig = (markupobj.h / imagescale);
                            }


                        }

                        bxorigfinite = isFinite(xorig);
                        byorigfinite = isFinite(yorig);
                        bworigfinite = isFinite(worig);
                        bhorigfinite = isFinite(horig);
                        
                        
                        if(!bxorigfinite){
                            xorig = 0;
                        }
                        if(!byorigfinite){
                            yorig = 0;
                        }

                        if(!bworigfinite){
                            worig = 100;
                        }

                        if(!bhorigfinite){
                            horig = 100;
                        }
        
                        if(isNaN(xorig)){
                            xorig = 0;
                        }
                        if(isNaN(yorig)){
                            yorig = 0;
                        }

                        if(isNaN(worig)){
                            worig = 100;
                        }
                        
                        if(isNaN(horig)){
                            horig = 100;
                        }                           

                        //rectangleNode.setAttribute("x", xorig);
                        //rectangleNode.setAttribute("y", yorig);
                        //rectangleNode.setAttribute("w", worig);
                        //rectangleNode.setAttribute("h", horig);

                        entityJSONNode.Entity.Rect = {x : xorig, y: yorig, w: worig, h : horig};


                        //rectangleNode.setAttribute("x", markupobj.x);
                        //rectangleNode.setAttribute("y", markupobj.y);
                        //rectangleNode.setAttribute("w", markupobj.w);
                        //rectangleNode.setAttribute("h", markupobj.h);
    
                    }
                }else{

                    //rectangleNode.setAttribute("x", markupobj.x);
                    //rectangleNode.setAttribute("y", markupobj.y);
                    //rectangleNode.setAttribute("w", markupobj.w);
                    //rectangleNode.setAttribute("h", markupobj.h);

                    entityJSONNode.Entity.Rect = {x : markupobj.x, y: markupobj.y, w: markupobj.w, h : markupobj.h};

                }
    
                //entityNode.appendChild(rectangleNode);

                

                if(markupobj.customattributes.length > 0){
                    //var extendedNode = markupxml.createElement("Extended");


                    var attribNode = {tagname : null, tagvalue : null};

                    for (var cacnt = 0;cacnt < markupobj.customattributes.length; cacnt++){
                        var sztagname  = markupobj.customattributes[cacnt].name;
                        var sztagvalue  = markupobj.customattributes[cacnt].value;

                        attribNode.tagname = sztagname;
                        attribNode.tagvalue = sztagvalue;


                        //createAndAppendElement(extendedNode, sztagname, sztagvalue);

                        entityJSONNode.Entity.Extended.push(attribNode);
                    }
                    //entityNode.appendChild(extendedNode);

                }

                if(markupobj.comments.length > 0){
                    //var commentNode = markupxml.createElement("Comments");


                    for (var cacnt = 0;cacnt < markupobj.comments.length; cacnt++){

                        var commentNode = {
                            id: markupobj.comments[cacnt].id,
                            signature: markupobj.comments[cacnt].signature,
                            value: markupobj.comments[cacnt].value
                        };
                
                                            
                        //var signattrib = {name : 'Signature',  value : markupobj.comments[cacnt].signature};

                        commentNode.signature = markupobj.comments[cacnt].signature;

                        commentNode.value = markupobj.comments[cacnt].value;
                        
                        if(markupobj.comments[cacnt].timestamp != undefined){
                            commentNode.timestamp = markupobj.comments[cacnt].timestamp;
                        }
                        

                        entityJSONNode.Entity.Comments.push(commentNode);

                        //createAndAppendElementwithAttribute(commentNode, sztagname, sztagvalue, signattrib);


                    }
                    //entityNode.appendChild(commentNode);

                }

                /*if(markupobj.type == 6 && markupobj.subtype == 6){
                    //var leaders = [];
                    //var leaderobj = {};
                    //leaders.push({entity : entityNode, mnumber : markupobj.markupnumber});
                    //var leaderNode = entityNode;
                }else{
                    //markupxml.getElementsByTagName("Entities")[0].appendChild(entityNode);    
                }*/
            }

        }

        
        return JSON.stringify(entityJSONNode);


    }

    function createxmlmarkupentitynode(markupobj, DocRef, markupxml, pageobject, bgetArrow){
        
        var savetime = new Date().getTime();
        var internalscale = 1;
        var fixedscale = 1;
        var imagescale = 1;
        var mainimagescale = 1;
        var leaders = [];
        var leaderobj = {};
        var entityNode = undefined;

        //var markupxmltxt += "<Entity><Entity/>";
        //var entityNode = parseXML(markupxmltxt);
        //var entityNode = markupxml.createElement("Entity");
        

        if (markupobj != null && !markupobj.pdfannotproxy) {

            var arrowleader = null;

            if(!bgetArrow){
                if(markupobj.bisTextArrow && markupobj.textBoxConnected != null){
                    arrowleader = createxmlmarkupentitynode(markupobj, DocRef, markupxml, pageobject, true);
                    markupobj = markupobj.textBoxConnected;
                }
            }

            var pagenumber = markupobj.pagenumber;
            //var pageobject = DocRef.getPageObject(pagenumber);
            var thumbnailobject = DocRef.getThumb(pagenumber);


            var bpageExist = (pageobject != undefined);
            var viewname3D = markupobj.viewname;
            var bisdrawn = markupobj.isdrawn();

            
            if(bpageExist){

                if (pageobject.usepdfjs || pageobject.usevectorxml) {
                    internalscale = markupobj.scaling;
                }else{
                    imagescale = markupobj.scaling;
                }
                
                fixedscale = pageobject.fixedScale;

                mainimagescale = pageobject.MainImageScaling;


                if(fixedscale == 1){

                    if(pageobject.pdfpageheight == 0 && pageobject.pdfpagewidth == 0){
                        //no page info
                    }else{
                        var yscale = fixedScaleSize.height / pageobject.pdfpageheight; //thispage.MainImageHeight;
                        var xscale = fixedScaleSize.width / pageobject.pdfpagewidth; // thispage.MainImageWidth;
                        
                        fixedscale = Math.min(xscale, yscale);
    
                    }

                }

                var bscalefinite = isFinite(internalscale);

                var bsfcalefinite = isFinite(fixedscale);

                if(!bscalefinite){
                    internalscale = 1;
                }

                if(!bsfcalefinite){
                    fixedscale = 1;
                }


                if(internalscale == 0){
                    internalscale = 1;
                }

                if(isNaN(internalscale)){
                    internalscale = 1;
                }


                if(fixedscale == undefined){
                    fixedscale = 1;
                }

                if(isNaN(fixedscale)){
                    fixedscale = 1;
                }

                if(fixedscale == 0){
                    fixedscale = 1;
                }                

                var bRedact = (markupobj.type == 3 && markupobj.subtype == 4);

                entityNode = markupxml.createElement("Entity");

                entityNode.setAttribute("ID", markupobj.markupnumber);

                if(markupobj.type == 11 || markupobj.type == 20){

                    var imagenode = createAppendElementret(entityNode, "Image", getBase64Image(markupobj.image));
                    //type="png" encoding="base64"
                    imagenode.setAttribute("type", "png");
                    imagenode.setAttribute("encoding", "base64");

                    if (markupobj.svgContent) {
                        createAndAppendElement(entityNode, "SvgContent", markupobj.svgContent);
                    }


                }
                createAndAppendElement(entityNode, "UniqueID", markupobj.uniqueID);
                
                //if(markupobj.type == 11 && markupobj.imagehref != ""){
                if((markupobj.type == 11 || markupobj.type == 20) && markupobj.imagehref != ""){
                    createAndAppendElement(entityNode, "Imagehref", markupobj.imagehref);    
                }
                createAndAppendElement(entityNode, "Type", markupobj.type);
                createAndAppendElement(entityNode, "Subtype", markupobj.subtype);
                createAndAppendElement(entityNode, "Alternative", markupobj.alternative);
                createAndAppendElement(entityNode, "Redaction", markupobj.bRedact ? 1:0);
                createAndAppendElement(entityNode, "Layer", markupobj.layer);
                createAndAppendElement(entityNode, "Rotation", markupobj.rotation);
                createAndAppendElement(entityNode, "TextRotation", markupobj.textrotate);
                createAndAppendElement(entityNode, "PageRotation", markupobj.pagerotation);
                createAndAppendElement(entityNode, "FileName", markupobj.documentName);
                createAndAppendElement(entityNode, "Layout", pageobject.LayoutName);
                createAndAppendElement(entityNode, "View", pagenumber);

                if(viewname3D != null){
                    createAndAppendElement(entityNode, "ViewName3D", viewname3D);
                    createAndAppendElement(entityNode, "For3DView", markupobj.bfor3D ? 1:0);
                    createAndAppendElement(entityNode, "PageHeight3D", markupobj.pageheight);
                    createAndAppendElement(entityNode, "PageWidth3D", markupobj.pagewidth);

                }

                //createAndAppendElement(entityNode, "ViewName", pageobject.PageName);
                
                createAndAppendElement(entityNode, "ViewName", thumbnailobject.PageName);

                createAndAppendElement(entityNode, "Handle", markupobj.markupnumber);
                //createAndAppendElement(entityNode, "Color", DocObj.markuplist[curmarkup].color);

                if(markupobj.fillcolor == undefined){
                    markupobj.fillcolor = "rgba(255, 0, 0, 1)";
                }
                createAndAppendElement(entityNode, "FillColor", markupobj.fillcolor);

                if(markupobj.strokecolor == undefined){
                    markupobj.strokecolor = "rgba(255, 0, 0, 1)";
                }


                if(bRedact){
                    createAndAppendElement(entityNode, "LineColor", markupobj.fillcolor);
                }else{
                    createAndAppendElement(entityNode, "LineColor", markupobj.strokecolor);
                }
                
                createAndAppendElement(entityNode, "TextColor", markupobj.textcolor);
                createAndAppendElement(entityNode, "Transparency", markupobj.transparency);
                createAndAppendElement(entityNode, "Consolidated", markupobj.consolidated ? 1:0);
                createAndAppendElement(entityNode, "HaveLink", markupobj.bhaveLink ? 1:0);
                createAndAppendElement(entityNode, "HaveLeader", markupobj.bhasArrow ? 1:0);


                if (markupobj.bhasArrow && markupobj.markupArrowConnected != null){
                    createAndAppendElement(entityNode, "LeaderReference", markupobj.markupArrowConnected.markupnumber);
                    if(arrowleader != null){
                        entityNode.appendChild(arrowleader);
                    }
                }

                var areahole = null;

                if(markupobj.holes.length > 0){
    
                    for (var h = 0; h < markupobj.holes.length; h++){
                        areahole = createxmlmarkupentitynode(markupobj.holes[h], DocRef, markupxml, pageobject, false);
                        
                        createAndAppendElement(entityNode, "Hole", markupobj.holes[h].markupnumber);

                        if(areahole != null){
                            entityNode.appendChild(areahole);
                        }

                        //arrowleader = createxmlmarkupentitynode(markupobj, DocRef, markupxml, pageobject, true);
                        //entityJSONNode.Entity.Holes.push(areahole);
                    }
                    
                }

    

                /*if (markupobj.bhasArrow && markupobj.markupArrowConnected != null){

                    createAndAppendElement(entityNode, "LeaderReference", markupobj.markupArrowConnected.markupnumber);
                    //leaders.push({entity : entityNode, mnumber : DocObj.markuplist[curmarkup].markupnumber});
                    for(var lcount = 0; lcount < leaders.length; lcount++){
                        if(leaders[lcount].mnumber == markupobj.markupArrowConnected.markupnumber){
                            entityNode.appendChild(leaders[lcount].entity);
                        }
                    }

                }*/

                createAndAppendElement(entityNode, "FixedScale", markupobj.bUseFixedScale ? 1:0);
                createAndAppendElement(entityNode, "LineStyleScale", fixedscale);
                createAndAppendElement(entityNode, "FixedScaleFactor", markupobj.fixedscaleFactor);
                
                createAndAppendElement(entityNode, "LinkURL", markupobj.linkURL);

                var fontNode = markupxml.createElement("Font");

                
                fontNode.setAttribute("facename", markupobj.font.fontName);

                if(bisdrawn){
                    if (pageobject.usepdfjs || pageobject.usevectorxml) {
                        if(markupobj.type != 12){
                            if(markupobj.bUseFixedScale){
                                fontNode.setAttribute("height", markupobj.font.height / fixedscale);
                                
                            }else{
                                fontNode.setAttribute("height", markupobj.font.height / internalscale);        
                            }  
                        }else{
                            fontNode.setAttribute("height", markupobj.font.height / internalscale);
                        }
                        
                    } else {
                        if(markupobj.type != 12){
                            if(markupobj.bUseFixedScale){
                                //fontNode.setAttribute("height", (markupobj.font.height / fixedscale) * imagescale);
                                fontNode.setAttribute("height", markupobj.font.height / fixedscale);
                            }else{
                                //fontNode.setAttribute("height", markupobj.font.height);    
                                fontNode.setAttribute("height", (markupobj.font.height / imagescale) / mainimagescale);
                            }
                            
                        }else{
                            //fontNode.setAttribute("height", markupobj.font.height);
                            fontNode.setAttribute("height", (markupobj.font.height / imagescale) / mainimagescale);
    
                        }
                        
                    }

                }else{
                    fontNode.setAttribute("height", markupobj.font.height);
                }

                //fontNode.setAttribute("height", markupobj.font.height);
                                
                fontNode.setAttribute("bold", markupobj.font.bold ? 1 : 0);
                fontNode.setAttribute("italic", markupobj.font.italic ? 1 : 0);
                entityNode.appendChild(fontNode);

                createAndAppendElement(entityNode, "Text", markupobj.text == "" ? "." : markupobj.text);

                createAndAppendElement(entityNode, "Status", markupobj.status);
                createAndAppendElement(entityNode, "SmallText", markupobj.smalltext == "" ? "." : markupobj.smalltext);
                createAndAppendElement(entityNode, "DimText", markupobj.dimtext == "" ? "." : markupobj.dimtext);

                createAndAppendElement(entityNode, "CustomLabelOn", markupobj.busecustomlabel ? 1:0);
                createAndAppendElement(entityNode, "CustomLabelText", markupobj.customlabeltext == "" ? "." : markupobj.customlabeltext);

                createAndAppendElement(entityNode, "TextWidth", markupobj.textwidth);
                createAndAppendElement(entityNode, "Signature", markupobj.signature);


                var displayname = GetDisplayName(markupobj.signature);
                createAndAppendElement(entityNode, "Name", displayname);
    

                createAndAppendElement(entityNode, "TimeStamp", markupobj.timestamp);


                var bmscalefinite = isFinite(markupobj.scaling);

                
                if(!bmscalefinite){
                    markupobj.scaling = 1;
                }


                if(markupobj.scaling == 0){
                    markupobj.scaling = 1;
                }

                if(isNaN(markupobj.scaling)){
                    markupobj.scaling = 1;
                }                

                createAndAppendElement(entityNode, "Scaling", markupobj.scaling);
                
                if(markupobj.loadscaling != undefined){
                    
                    createAndAppendElement(entityNode, "LoadScaling", markupobj.scaling);

                }
                

                //new property for Lydia to handle ledgend images
                createAndAppendElement(entityNode, "Printscale", markupobj.printscale);

                var bxoffsetfinite = isFinite(markupobj.xoffset);
                var byoffsetfinite = isFinite(markupobj.yoffset);

                
                if(!bxoffsetfinite){
                    markupobj.xoffset = 0;
                }
                
                if(!byoffsetfinite){
                    markupobj.yoffset = 0;
                }

                if(isNaN(markupobj.xoffset)){
                    markupobj.xoffset = 0;
                }

                if(isNaN(markupobj.yoffset)){
                    markupobj.yoffset = 0;
                }


                if(bisdrawn){
                    if (pageobject.usepdfjs || pageobject.usevectorxml) {
                        createAndAppendElement(entityNode, "Xoffset", markupobj.xoffset / internalscale);
                        createAndAppendElement(entityNode, "Yoffset", markupobj.yoffset / internalscale);
    
                    } else {
                        createAndAppendElement(entityNode, "Xoffset", markupobj.xoffset);
                        createAndAppendElement(entityNode, "Yoffset", markupobj.yoffset);
                    }
    
                }else{
                    createAndAppendElement(entityNode, "Xoffset", markupobj.xoffset);
                    createAndAppendElement(entityNode, "Yoffset", markupobj.yoffset);

                }

                var localLinewidth = markupobj.linewidth;
                var localLinewidthSegment = markupobj.linewidthSegment;

                

                if(bisdrawn){
                    if (pageobject.usepdfjs || pageobject.usevectorxml) {
                        if(bRedact){
                            createAndAppendElement(entityNode, "LineWidth", 1);
                            
                        }else{
                            if(markupobj.bUseFixedScale){
                            
                                
                                var linescale = ( localLinewidth / fixedscale);
                                var linescalesegment = ( localLinewidthSegment / fixedscale);
    
                                createAndAppendElement(entityNode, "LineWidth", linescale);
                                createAndAppendElement(entityNode, "LineWidthSegment", linescalesegment);
    
    
                            }else{
                                createAndAppendElement(entityNode, "LineWidth", localLinewidth / internalscale);
                                createAndAppendElement(entityNode, "LineWidthSegment", localLinewidthSegment / internalscale);
                            }
    
                        }
                        
                    } else {
                        
                        if(bRedact){
                            createAndAppendElement(entityNode, "LineWidth", 1);
                            
                        }else{
                            if(markupobj.bUseFixedScale){
                                //linescale = (localLinewidth / fixedscale) * imagescale;
                                linescale = (localLinewidth / fixedscale);
                                
                                linescalesegment = ( localLinewidthSegment / fixedscale);

                                createAndAppendElement(entityNode, "LineWidth", linescale);
                                createAndAppendElement(entityNode, "LineWidthSegment", linescalesegment);
    
                            }else{
                                //createAndAppendElement(entityNode, "LineWidth", localLinewidth);
                                createAndAppendElement(entityNode, "LineWidth", (localLinewidth / imagescale) / mainimagescale);
                                createAndAppendElement(entityNode, "LineWidthSegment", (localLinewidthSegment / imagescale) / mainimagescale );
                            }
    
                        }
                        
                    }       
                }else{
                    createAndAppendElement(entityNode, "LineWidth", localLinewidth);
                    createAndAppendElement(entityNode, "LineWidthSegment", localLinewidthSegment);
                }

                         


                var localArrowSize = markupobj.arrowlength - markupobj.linewidth;
                createAndAppendElement(entityNode, "ArrowSize", localArrowSize);

                createAndAppendElement(entityNode, "LineStyle", markupobj.linestyle);

                createAndAppendElement(entityNode, "MainImageScaling", pageobject.MainImageScaling);
                createAndAppendElement(entityNode, "MainImageOffsetX", pageobject.MainImageOffsetX);
                createAndAppendElement(entityNode, "MainImageOffsetY", pageobject.MainImageOffsetY);

                createAndAppendElement(entityNode, "DocScale", markupobj.docdscale);
                createAndAppendElement(entityNode, "DocOffsetX", markupobj.docdx);
                createAndAppendElement(entityNode, "DocOffsetY", markupobj.docdy);


                createAndAppendElement(entityNode, "HasScale", markupobj.hasScale);

                var scaleObj = pageobject.scaleObject;

                if(markupobj.hasScale) {
                    scaleObj = markupobj.scaleObject;
                }

                createAndAppendElement(entityNode, "Measurescale", scaleObj.measurescale);
                createAndAppendElement(entityNode, "ScaleLabel", scaleObj.scaleLabel);                    
                createAndAppendElement(entityNode, "Unitofmeasure", scaleObj.Unitofmeasure);
                createAndAppendElement(entityNode, "SubmeasureUnit", scaleObj.SubmeasureUnit);
                createAndAppendElement(entityNode, "Unitlabel", scaleObj.Unitlabel);
                createAndAppendElement(entityNode, "AreaUnitlabel", scaleObj.AreaUnitlabel);
                createAndAppendElement(entityNode, "Unitscale", scaleObj.unitscale);
                createAndAppendElement(entityNode, "ScaleValue", scaleObj.scaleValue);
                createAndAppendElement(entityNode, "Ndimprecision", scaleObj.ndimprecision);


                if(bisdrawn){
                    var bsegmentmarkup = (markupobj.type == 1 && markupobj.subtype == 6);

                    if(markupobj.type != 0 && !bsegmentmarkup ){
                        if (markupobj.points.length != -1) {
                            var pointsNode = markupxml.createElement("Points");
                            for (var i = 0; i < markupobj.points.length; i++) {
                                var pointNode = markupxml.createElement("Point");
    
                                if (pageobject.usepdfjs || pageobject.usevectorxml) {
                                    var xpoint = (markupobj.points[i].x - markupobj.xoffset) / internalscale;
                                    var ypoint = (markupobj.points[i].y - markupobj.yoffset) / internalscale;
    
                                    pointNode.setAttribute("x", xpoint);
                                    pointNode.setAttribute("y", ypoint);
    
                                } else {

                                    xpoint = ((markupobj.points[i].x - markupobj.xoffset) / imagescale) / mainimagescale;
                                    ypoint = ((markupobj.points[i].y - markupobj.yoffset) / imagescale) / mainimagescale;

                                    pointNode.setAttribute("x", xpoint);
                                    pointNode.setAttribute("y", ypoint);

                                    //pointNode.setAttribute("x", markupobj.points[i].x);
                                    //pointNode.setAttribute("y", markupobj.points[i].y);
    
                                }
                                pointsNode.appendChild(pointNode);
                            }
                            entityNode.appendChild(pointsNode);
                        }
                    }else{
                        if (markupobj.pointlist.length != 0) {
                            for(var j = 0; j < markupobj.pointlist.length;j++){
                                var linesNode = markupxml.createElement("Line");
                                for (i = 0; i < markupobj.pointlist[j].length; i++) {
                                    pointNode = markupxml.createElement("Point");
                                    if (pageobject.usepdfjs || pageobject.usevectorxml) {
                                        xpoint = (markupobj.pointlist[j][i].x - markupobj.xoffset) / internalscale;
                                        ypoint = (markupobj.pointlist[j][i].y - markupobj.yoffset) / internalscale;
    
                                        pointNode.setAttribute("x", xpoint);
                                        pointNode.setAttribute("y", ypoint);
    
                                    } else {

                                        xpoint = ((markupobj.pointlist[j][i].x - markupobj.xoffset) / imagescale) / mainimagescale;
                                        ypoint = ((markupobj.pointlist[j][i].y - markupobj.yoffset) / imagescale) / mainimagescale;

                                        pointNode.setAttribute("x", xpoint);
                                        pointNode.setAttribute("y", ypoint);


                                        //pointNode.setAttribute("x", markupobj.pointlist[j][i].x);
                                        //pointNode.setAttribute("y", markupobj.pointlist[j][i].y);
    
                                    }
                                    linesNode.appendChild(pointNode);
                                }
                                entityNode.appendChild(linesNode);
                            }
    
    
                        }
    
                    }               
                }else{
                    if(markupobj.type != 0 ){
                        if (markupobj.points.length != -1) {
                            pointsNode = markupxml.createElement("Points");
                            for (var i = 0; i < markupobj.points.length; i++) {
                                var pointNode = markupxml.createElement("Point");

                                pointNode.setAttribute("x", markupobj.points[i].x);
                                pointNode.setAttribute("y", markupobj.points[i].y);

                                pointsNode.appendChild(pointNode);
                            }
                            entityNode.appendChild(pointsNode);
                        }
                    }else{
                        if (markupobj.pointlist.length != 0) {
                            for(var j = 0; j < markupobj.pointlist.length;j++){
                                var linesNode = markupxml.createElement("Line");
                                for (i = 0; i < markupobj.pointlist[j].length; i++) {
                                    pointNode = markupxml.createElement("Point");

                                    pointNode.setAttribute("x", markupobj.pointlist[j][i].x);
                                    pointNode.setAttribute("y", markupobj.pointlist[j][i].y);

                                    linesNode.appendChild(pointNode);
                                }

                                entityNode.appendChild(linesNode);
                            }
    
                        }
    
                    }     
                }

                if(bisdrawn){
                    if(markupobj.type == 13){
                        if (markupobj.countshapes.length != -1) {
    
                            /*modified to same logic as linewidth*/
    
                            var localouterRadius = markupobj.outerRadius;
                            var localinnerRadius = markupobj.innerRadius;
                          
                            if (pageobject.usepdfjs || pageobject.usevectorxml) {
                            
                                if(markupobj.bUseFixedScale){
                                    var outerRadiusscale = ( localouterRadius / fixedscale);
                                    var innerRadiusscale = ( localinnerRadius / fixedscale);
                                    
                                    createAndAppendElement(entityNode, "outerRadius", outerRadiusscale);
                                    createAndAppendElement(entityNode, "innerRadius", innerRadiusscale);
                                    
                                }else{
            
                                    createAndAppendElement(entityNode, "outerRadius", localouterRadius / internalscale);    
                                    createAndAppendElement(entityNode, "innerRadius", localinnerRadius / internalscale);    
                                
                                }
                            
                            }else{
                            
                                if(markupobj.bUseFixedScale){
    
                                    //outerRadiusscale = ( localouterRadius / fixedscale) * imagescale;
                                    //innerRadiusscale = ( localinnerRadius / fixedscale) * imagescale;

                                    outerRadiusscale = ( localouterRadius / fixedscale);
                                    innerRadiusscale = ( localinnerRadius / fixedscale);

                            
                                    createAndAppendElement(entityNode, "outerRadius", outerRadiusscale);
                                    createAndAppendElement(entityNode, "innerRadius", innerRadiusscale);
                                    
                                    
                                }else{

                                    createAndAppendElement(entityNode, "outerRadius", (localouterRadius / imagescale) / mainimagescale);
                                    createAndAppendElement(entityNode, "innerRadius", (localinnerRadius / imagescale) / mainimagescale);

                                    //createAndAppendElement(entityNode, "outerRadius", localouterRadius);
                                    //createAndAppendElement(entityNode, "innerRadius", localinnerRadius);
                                }
                            
                            }
                            /*modified to same logic as linewidth*/
    
    
                            //var pointsNode = markupxml.createElement("Points");
                            for (var i = 0; i < markupobj.countshapes.length; i++) {
                                var pointNode = markupxml.createElement("Point");
    
                                if (pageobject.usepdfjs || pageobject.usevectorxml) {
                                    xpoint = (markupobj.countshapes[i].x - markupobj.xoffset) / internalscale;
                                    ypoint = (markupobj.countshapes[i].y - markupobj.yoffset) / internalscale;
    
                                    pointNode.setAttribute("x", xpoint);
                                    pointNode.setAttribute("y", ypoint);
    
                                } else {

                                    xpoint = ((markupobj.countshapes[i].x - markupobj.xoffset) / imagescale) / mainimagescale;
                                    ypoint = ((markupobj.countshapes[i].y - markupobj.yoffset) / imagescale) / mainimagescale;
    
                                    pointNode.setAttribute("x", xpoint);
                                    pointNode.setAttribute("y", ypoint);


                                    //pointNode.setAttribute("x", markupobj.countshapes[i].x);
                                    //pointNode.setAttribute("y", markupobj.countshapes[i].y);
    
                                }
                                pointsNode.appendChild(pointNode);
                            }
                            entityNode.appendChild(pointsNode);
                        }
                    }
                    if(markupobj.type == 14){

                        //this.startX = null;
                        //this.startY = null;
                        //this.secondX = null;
                        //this.secondY = null;
                        //this.endX = null;
                        //this.endY = null;


                        var localstartX = markupobj.startX;
                        var localstartY = markupobj.startY;
                        var localsecondX = markupobj.secondX;
                        var localsecondY = markupobj.secondY;
                        var localendX = markupobj.endX;
                        var localendY = markupobj.endY;


                        if (pageobject.usepdfjs || pageobject.usevectorxml) {
                            
                            if(markupobj.bUseFixedScale){
                                localstartX = ( localstartX / fixedscale);
                                localstartY = ( localstartY / fixedscale);
                                
                                createAndAppendElement(entityNode, "startX", localstartX);
                                createAndAppendElement(entityNode, "startY", localstartY);

                                localsecondX = localsecondX / fixedscale;
                                localsecondY = localsecondY / fixedscale;

                                createAndAppendElement(entityNode, "secondX", localsecondX);
                                createAndAppendElement(entityNode, "secondY", localsecondY);

                                localendX = localendX / fixedscale;
                                localendY = localendY / fixedscale;

                                createAndAppendElement(entityNode, "endX", localendX);
                                createAndAppendElement(entityNode, "endY", localendY);


        
        
                                
                            }else{

                                createAndAppendElement(entityNode, "startX", localstartX / internalscale);
                                createAndAppendElement(entityNode, "startY", localstartY / internalscale);

                                createAndAppendElement(entityNode, "secondX", localsecondX / internalscale);
                                createAndAppendElement(entityNode, "secondY", localsecondY / internalscale);

                                createAndAppendElement(entityNode, "endX", localendX / internalscale);
                                createAndAppendElement(entityNode, "endY", localendY / internalscale);


        
                            
                            }
                        
                        }else{
                        
                            if(markupobj.bUseFixedScale){


                                createAndAppendElement(entityNode, "startX", localstartX / fixedscale);
                                createAndAppendElement(entityNode, "startY", localstartY / fixedscale);

                                createAndAppendElement(entityNode, "secondX", localsecondX / fixedscale);
                                createAndAppendElement(entityNode, "secondY", localsecondY / fixedscale);

                                createAndAppendElement(entityNode, "endX", localendX / fixedscale);
                                createAndAppendElement(entityNode, "endY", localendY / fixedscale);

                                
                                
                            }else{

                                createAndAppendElement(entityNode, "startX", (localstartX / imagescale) / mainimagescale);
                                createAndAppendElement(entityNode, "startY", (localstartY / imagescale) / mainimagescale);

                                createAndAppendElement(entityNode, "secondX", (localsecondX / imagescale) / mainimagescale);
                                createAndAppendElement(entityNode, "secondY", (localsecondY / imagescale) / mainimagescale);

                                createAndAppendElement(entityNode, "endX", (localendX / imagescale) / mainimagescale);
                                createAndAppendElement(entityNode, "endY", (localendY / imagescale) / mainimagescale);


                            }
                        }



        
                    }
                }else{
                    if(markupobj.type == 13){
                        if (markupobj.countshapes.length != -1) {
    
                            /*modified to same logic as linewidth*/
    
                            var localouterRadius = markupobj.outerRadius;
                            var localinnerRadius = markupobj.innerRadius;

                            createAndAppendElement(entityNode, "outerRadius", localouterRadius);
                            createAndAppendElement(entityNode, "innerRadius", localinnerRadius);

                          
                            
                            /*modified to same logic as linewidth*/
                            //var pointsNode = markupxml.createElement("Points");
                            for (var i = 0; i < markupobj.countshapes.length; i++) {

                                var pointNode = markupxml.createElement("Point");

                                pointNode.setAttribute("x", markupobj.countshapes[i].x);
                                pointNode.setAttribute("y", markupobj.countshapes[i].y);

                                pointsNode.appendChild(pointNode);
                            }
                            entityNode.appendChild(pointsNode);
                        }
                    }       

                    if(markupobj.type == 14){


                        var localstartX = markupobj.startX;
                        var localstartY = markupobj.startY;
                        var localsecondX = markupobj.secondX;
                        var localsecondY = markupobj.secondY;
                        var localendX = markupobj.endX;
                        var localendY = markupobj.endY;

                        createAndAppendElement(entityNode, "startX", localstartX);
                        createAndAppendElement(entityNode, "startY", localstartY);

                        createAndAppendElement(entityNode, "secondX", localsecondX);
                        createAndAppendElement(entityNode, "secondY", localsecondY);

                        createAndAppendElement(entityNode, "endX", localendX);
                        createAndAppendElement(entityNode, "endY", localendY);
                        
                    }       

                }

                if(markupobj.type == 7){
                

                    if(bisdrawn){
                        if (pageobject.usepdfjs || pageobject.usevectorxml) {
                            createAndAppendElement(entityNode, "LeaderLineOffset", markupobj.leaderoffset / internalscale);    
                        }else{
                            createAndAppendElement(entityNode, "LeaderLineOffset", markupobj.leaderoffset);
                        }
    
                    }else{
                        createAndAppendElement(entityNode, "LeaderLineOffset", markupobj.leaderoffset);
                    }

                    
                }

                if(markupobj.type == 8){

                    if(bisdrawn){
                        if (pageobject.usepdfjs || pageobject.usevectorxml) {
                            createAndAppendElement(entityNode, "LabelOffsetX", markupobj.labeloffsetx / internalscale);
                            createAndAppendElement(entityNode, "LabelOffsetY", markupobj.labeloffsety / internalscale);
    
                        }else{
                            createAndAppendElement(entityNode, "LabelOffsetX", markupobj.labeloffsetx);
                            createAndAppendElement(entityNode, "LabelOffsetY", markupobj.labeloffsety);
                        }
    
                    }else{
                        createAndAppendElement(entityNode, "LabelOffsetX", markupobj.labeloffsetx);
                        createAndAppendElement(entityNode, "LabelOffsetY", markupobj.labeloffsety);

                    }

                    
                    createAndAppendElement(entityNode, "LabelColor", markupobj.labelfillcolor);

                }  
                
                var rectangleNode = markupxml.createElement("Rect");

                var bxfinite = isFinite(markupobj.x);
                var byfinite = isFinite(markupobj.y);
                var bwfinite = isFinite(markupobj.w);
                var bhfinite = isFinite(markupobj.h);

                
                if(!bxfinite){
                    markupobj.x = 0;
                }
                if(!byfinite){
                    markupobj.y = 0;
                }

                if(!bwfinite){
                    markupobj.w = 100;
                }

                if(!bhfinite){
                    markupobj.h = 100;
                }

                if(isNaN(markupobj.x)){
                    markupobj.x = 0;
                }
                if(isNaN(markupobj.y)){
                    markupobj.y = 0;
                }

                if(isNaN(markupobj.w)){
                    markupobj.w = 100;
                }
                
                if(isNaN(markupobj.h)){
                    markupobj.h = 100;
                }


                if(bisdrawn){
                    if (pageobject.usepdfjs || pageobject.usevectorxml) {


                        var xorig = (markupobj.x - markupobj.xoffset) / internalscale;
                        var yorig = (markupobj.y - markupobj.yoffset) / internalscale;
    
                        var bCircle = (markupobj.type == 4 && markupobj.subtype); 
                        if (markupobj.type == 0 || markupobj.type == 1 || markupobj.type == 2 || markupobj.type == 6 || markupobj.type == 7 || markupobj.type == 8 || bCircle) {
                            var worig = (markupobj.w - markupobj.xoffset) / internalscale;
                            var horig = (markupobj.h - markupobj.yoffset) / internalscale;
    
                        } else {
                            if (markupobj.type == 9 && markupobj.subtype == 0) {
                                worig = (markupobj.w / internalscale);
                                horig = (markupobj.textheight / internalscale);
                            } else if(markupobj.type == 10 && markupobj.bUseFixedScale) {
                                worig = (markupobj.w / fixedscale);
                                horig = (markupobj.h / fixedscale);
                            }else{
                                worig = (markupobj.w / internalscale);
                                horig = (markupobj.h / internalscale);
                            }
                        }
    
                        var bxorigfinite = isFinite(xorig);
                        var byorigfinite = isFinite(yorig);
                        var bworigfinite = isFinite(worig);
                        var bhorigfinite = isFinite(horig);
                        
                        
                        if(!bxorigfinite){
                            xorig = 0;
                        }
                        if(!byorigfinite){
                            yorig = 0;
                        }

                        if(!bworigfinite){
                            worig = 100;
                        }

                        if(!bhorigfinite){
                            horig = 100;
                        }
        
                        if(isNaN(xorig)){
                            xorig = 0;
                        }
                        if(isNaN(yorig)){
                            yorig = 0;
                        }

                        if(isNaN(worig)){
                            worig = 100;
                        }
                        
                        if(isNaN(horig)){
                            horig = 100;
                        }                        
    
                        rectangleNode.setAttribute("x", xorig);
                        rectangleNode.setAttribute("y", yorig);
                        rectangleNode.setAttribute("w", worig);
                        rectangleNode.setAttribute("h", horig);
    
                    } else {

                        var xorig = ((markupobj.x - markupobj.xoffset) / imagescale) / mainimagescale;
                        var yorig = ((markupobj.y - markupobj.yoffset) / imagescale) / mainimagescale;

    
                        var bCircle = (markupobj.type == 4 && markupobj.subtype); 
                        if (markupobj.type == 0 || markupobj.type == 1 || markupobj.type == 2 || markupobj.type == 6 || markupobj.type == 7 || markupobj.type == 8 || bCircle) {
                            var worig = ((markupobj.w - markupobj.xoffset) / imagescale) / mainimagescale;
                            var horig = ((markupobj.h - markupobj.yoffset) / imagescale) / mainimagescale;
    
                        } else {
                            if (markupobj.type == 9 && markupobj.subtype == 0) {
                                worig = (markupobj.w / imagescale) / mainimagescale;
                                horig = (markupobj.textheight / imagescale) / mainimagescale;
                            } else if(markupobj.type == 10 && markupobj.bUseFixedScale) {
                                worig = (markupobj.w / fixedscale);
                                horig = (markupobj.h / fixedscale);
                            }else{
                                worig = (markupobj.w / imagescale) / mainimagescale;
                                horig = (markupobj.h / imagescale) / mainimagescale;
                            }


                        }

                        bxorigfinite = isFinite(xorig);
                        byorigfinite = isFinite(yorig);
                        bworigfinite = isFinite(worig);
                        bhorigfinite = isFinite(horig);
                        
                        
                        if(!bxorigfinite){
                            xorig = 0;
                        }
                        if(!byorigfinite){
                            yorig = 0;
                        }

                        if(!bworigfinite){
                            worig = 100;
                        }

                        if(!bhorigfinite){
                            horig = 100;
                        }
        
                        if(isNaN(xorig)){
                            xorig = 0;
                        }
                        if(isNaN(yorig)){
                            yorig = 0;
                        }

                        if(isNaN(worig)){
                            worig = 100;
                        }
                        
                        if(isNaN(horig)){
                            horig = 100;
                        }                           

                        rectangleNode.setAttribute("x", xorig);
                        rectangleNode.setAttribute("y", yorig);
                        rectangleNode.setAttribute("w", worig);
                        rectangleNode.setAttribute("h", horig);


                        //rectangleNode.setAttribute("x", markupobj.x);
                        //rectangleNode.setAttribute("y", markupobj.y);
                        //rectangleNode.setAttribute("w", markupobj.w);
                        //rectangleNode.setAttribute("h", markupobj.h);
    
                    }
                }else{

                    rectangleNode.setAttribute("x", markupobj.x);
                    rectangleNode.setAttribute("y", markupobj.y);
                    rectangleNode.setAttribute("w", markupobj.w);
                    rectangleNode.setAttribute("h", markupobj.h);

                }
    
                entityNode.appendChild(rectangleNode);

                if(markupobj.customattributes.length > 0){
                    var extendedNode = markupxml.createElement("Extended");
                    for (var cacnt = 0;cacnt < markupobj.customattributes.length; cacnt++){
                        var sztagname  = markupobj.customattributes[cacnt].name;
                        var sztagvalue  = markupobj.customattributes[cacnt].value;
                        createAndAppendElement(extendedNode, sztagname, sztagvalue);
                    }
                    entityNode.appendChild(extendedNode);

                }

                if(markupobj.comments.length > 0){
                    var commentNode = markupxml.createElement("Comments");
                    var attribs = [];
                    

                    for (var cacnt = 0;cacnt < markupobj.comments.length; cacnt++){


                        
                        //var sztagname  = markupobj.comments[cacnt].id;
                        var sztagname = "id_" + markupobj.comments[cacnt].id;
                        
                        var signattrib = {name : 'Signature',  value : markupobj.comments[cacnt].signature};
                        attribs.push(signattrib);
                        
                        if(markupobj.comments[cacnt].timestamp != undefined){
                            var timeattrib = {name : 'Time',  value : markupobj.comments[cacnt].timestamp};
                            attribs.push(timeattrib);
                        }
                        

                        var sztagvalue  = markupobj.comments[cacnt].value;

                        createAndAppendElementwithAttributes(commentNode, sztagname, sztagvalue, attribs);
                        //createAndAppendElement(commentNode, )


                    }
                    entityNode.appendChild(commentNode);

                }

                /*if(markupobj.type == 6 && markupobj.subtype == 6){
                    //var leaders = [];
                    //var leaderobj = {};
                    //leaders.push({entity : entityNode, mnumber : markupobj.markupnumber});
                    //var leaderNode = entityNode;
                }else{
                    //markupxml.getElementsByTagName("Entities")[0].appendChild(entityNode);    
                }*/
            }

        }

        return entityNode;
    }    

    function createmarkupJSON(markupsave,consolidate,includeall,signature, dispname, markupsaveobj){
        if(docpagNotReady()){
            return;
        }

        var markupjson = [];

        var curmarkup = 0;
        var savetime = new Date().getTime();
        var internalscale = 1;
        var fixedscale = 1;
        var imagescale = 1;
        var leaders = [];
        var leaderobj = {};
        var nonloadedpages = [];


        if (DocObj.markuplist.length > 0) {


            for (curmarkup = 0; curmarkup < DocObj.markuplist.length; curmarkup++) {

                if (DocObj.markuplist[curmarkup] != null && !DocObj.markuplist[curmarkup].pdfannotproxy) {

                    var pagenumber = DocObj.markuplist[curmarkup].pagenumber;

                    var pageobject = DocObj.getPageObject(pagenumber);

                    var bpageExist = (pageobject != undefined);
            
                    if ((DocObj.markuplist[curmarkup].signature == signature || includeall) && (DocObj.markuplist[curmarkup].display || markupsave) && (DocObj.markuplist[curmarkup].consolidated || !consolidate)) {


                        var bhole = (DocObj.markuplist[curmarkup].type == 8 && DocObj.markuplist[curmarkup].subtype == 2);

                        if(bpageExist){
                            if (DocObj.markuplist[curmarkup].bhasArrow && DocObj.markuplist[curmarkup].markupArrowConnected != null){
                                //var entityNode = createxmlmarkupentitynode(DocObj.markuplist[curmarkup].markupArrowConnected,DocObj,markupxml, false);    
                            }else if (bhole) {
                                //var entityNode = createxmlmarkupentitynode(DocObj.markuplist[curmarkup].markupArrowConnected,DocObj,markupxml, false);    
    
                            }else{
                                var jsonentityNode = JSON.parse(createJSONmarkupentitynode(DocObj.markuplist[curmarkup],DocObj, pageobject, false));

                                //arrowleader = JSON.parse(createJSONmarkupentitynode(markupobj, DocRef, pageobject, true));

                            }

                            if(jsonentityNode != undefined){
                                //markupjson.getElementsByTagName("Entities")[0].appendChild(entityNode);    
                                markupjson.push(jsonentityNode);    
                            }

    
                        }else{
                            if(markupsaveobj != undefined){

                                markupsaveobj.addpage(pagenumber);
                                //markupsaveobj.pages.push(pagenumber);
                            }

                        }

                    }
                }
            } //end for
            //SendData(markupxml,"http://viewserver.rasterex.com/demo/markupserver.dll?webmarkupsave");
        } //end if
        
        return JSON.stringify(markupjson);
        

        
    }

    function createxmlmarkup(markupsave,consolidate,includeall,signature, dispname, markupsaveobj){
        
        if(docpagNotReady()){
            return;
        }

        
        var curmarkup = 0;
        var savetime = new Date().getTime();
        var internalscale = 1;
        var fixedscale = 1;
        var imagescale = 1;
        var leaders = [];
        var leaderobj = {};
        var nonloadedpages = [];

        var markupxmltxt = "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n";

        markupxmltxt += "<RasterexMarkup>\n";
        markupxmltxt += "<Header>\n";
        markupxmltxt += "<FileVersion>C370</FileVersion>\n";
        if (DocObj.getCurrentpage().usepdfjs) {
            markupxmltxt += "<Format>pdfjs</Format>\n";
        } else if (DocObj.getCurrentpage().usevectorxml) {
            markupxmltxt += "<Format>vector</Format>\n";
        } else if(DocObj.getCurrentpage().usevector3Dxml){

            if(DocObj.getCurrentpage().get3DMarkupJSON() != undefined){
                var markupjsonfile = DocObj.getMarkup3DSavefile();
                markupxmltxt += "<Markup3D>" + markupjsonfile + "</Markup3D>\n";
            }

            if(DocObj.getCurrentpage().get3DCamerasJSON() != null){
                var viewsjsonfile = DocObj.getViews3DSavefile();
                markupxmltxt += "<Views3D>" + viewsjsonfile + "</Views3D>\n";
            }

            markupxmltxt += "<Format>3D</Format>\n";
        
        }else {
            markupxmltxt += "<Format>image</Format>\n";
        }
        markupxmltxt += "<Scale>1.0000</Scale>\n";
        markupxmltxt += "<OrigoX>0.0000</OrigoX>\n";
        markupxmltxt += "<OrigoY>0.0000</OrigoY>\n";
        markupxmltxt += "<DirectionY>1</DirectionY>\n";
        markupxmltxt += "<DirectionX>1</DirectionX>\n";
        markupxmltxt += "</Header>\n";
        markupxmltxt += "<Users>\n";
        markupxmltxt += "<User>\n";
        markupxmltxt += "<ID>" + signature + "</ID>\n";
        markupxmltxt += "<Name>" + dispname + "</Name>\n";
        markupxmltxt += "<Color>" + markupcolor + "</Color>\n";
        markupxmltxt += "<Layer>" + markuplayer + "</Layer>\n";
        markupxmltxt += "</User>\n";
        markupxmltxt += "</Users>\n";
        markupxmltxt += "<Scales/>\n";
        markupxmltxt += "<Entities/></RasterexMarkup>\n";
        //jquery function
        //var markupxml = $.parseXML(markupxmltxt);
        var markupxml = parseXML(markupxmltxt);

        //var found = false;
        var id = -1;


        if (DocObj.markuplist.length > 0) {


            for (curmarkup = 0; curmarkup < DocObj.markuplist.length; curmarkup++) {

                if (DocObj.markuplist[curmarkup] != null) {

                    var pagenumber = DocObj.markuplist[curmarkup].pagenumber;

                    var pageobject = DocObj.getPageObject(pagenumber);

                    var bpageExist = (pageobject != undefined);
            
                    if ((DocObj.markuplist[curmarkup].signature == signature || includeall) && (DocObj.markuplist[curmarkup].display || markupsave) && (DocObj.markuplist[curmarkup].consolidated || !consolidate)) {


                        var bhole = (DocObj.markuplist[curmarkup].type == 8 && DocObj.markuplist[curmarkup].subtype == 2);

                        if(bpageExist){
                            if (DocObj.markuplist[curmarkup].bhasArrow && DocObj.markuplist[curmarkup].markupArrowConnected != null){
                                //var entityNode = createxmlmarkupentitynode(DocObj.markuplist[curmarkup].markupArrowConnected,DocObj,markupxml, false);    
                            }else if (bhole) {
                                //var entityNode = createxmlmarkupentitynode(DocObj.markuplist[curmarkup].markupArrowConnected,DocObj,markupxml, false);    
    
                            }else{
                                var entityNode = createxmlmarkupentitynode(DocObj.markuplist[curmarkup],DocObj,markupxml, pageobject, false);
                            }

                            if(entityNode != undefined){
                                markupxml.getElementsByTagName("Entities")[0].appendChild(entityNode);    
                            }

    
                        }else{
                            if(markupsaveobj != undefined){

                                markupsaveobj.addpage(pagenumber);
                                //markupsaveobj.pages.push(pagenumber);
                            }

                        }



                        


                    }
                }
            } //end for
            //SendData(markupxml,"http://viewserver.rasterex.com/demo/markupserver.dll?webmarkupsave");
        } //end if

        /*if(markupsaveobj != undefined){

            markupsaveobj.getnonloadedpages();

            //markupsaveobj.pages = nonloadedpages;
        }*/

        //const scaleList = getScales();
        
        const scaleList = DocObj.scalesOptions;

        if (scaleList.length > 0) {

            for (var idx = 0; idx < scaleList.length; idx++) {
                var scaleNode = markupxml.createElement("ScaleItem");                
                createAndAppendElement(scaleNode, "Value", scaleList[idx].value);
                createAndAppendElement(scaleNode, "Label", scaleList[idx].label);
                createAndAppendElement(scaleNode, "Metric", scaleList[idx].metric);
                createAndAppendElement(scaleNode, "MetricUnit", scaleList[idx].metricUnit);
                createAndAppendElement(scaleNode, "DimPrecision", scaleList[idx].dimPrecision);
                createAndAppendElement(scaleNode, "IsSelected", scaleList[idx].isSelected);
                markupxml.getElementsByTagName("Scales")[0].appendChild(scaleNode);
            } 
            
        }



        return markupxml;
    }


    function createxmlmarkupentity(markupobj,Docref,bgetArrow){
        
        /*var arrowleader = null;

        if(!bgetArrow){
            if(markupobj.bisTextArrow && markupobj.textBoxConnected != null){
                arrowleader = createxmlmarkupentity(markupobj, Docref, true);
                markupobj = markupobj.textBoxConnected;
            }
        }*/

        var savetime = new Date().getTime();
        var internalscale = 1;
        var fixedscale = 1;
        var imagescale = 1;


        var markupxmltxt = "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n";
        markupxmltxt += "<RasterexMarkup>\n";
        markupxmltxt += "<FileVersion>C370</FileVersion>\n";
        markupxmltxt += "<Entities/></RasterexMarkup>\n";
        var markupxml = parseXML(markupxmltxt);
        
        if (markupobj != null) {

            var pageobject = Docref.getPageObject(markupobj.pagenumber);
            var bpageExist = (pageobject != undefined);


            if (markupobj.bhasArrow && markupobj.markupArrowConnected != null){
                //var entityNode = createxmlmarkupentitynode(DocObj.markuplist[curmarkup].markupArrowConnected,DocObj,markupxml, false);
                //do not create node for callout directly.    
            }else{

                var entityNode = createxmlmarkupentitynode(markupobj,Docref,markupxml, pageobject, false);

                if(entityNode != undefined){
                    markupxml.getElementsByTagName("Entities")[0].appendChild(entityNode);    
                }
    
            }



        }
        
        return markupxml;
        


    }

    /*function getScales(){
        const retrievedScalesString = localStorage.getItem('scalesOptions');
        let retrievedScalesArray = [];
        if(retrievedScalesString) {      
            retrievedScalesArray = JSON.parse(retrievedScalesString);            
        } 
        return retrievedScalesArray;   
    }*/

    /*function setScalesToLocalStorage(scales){
        const jsonString = JSON.stringify(scales);
        localStorage.setItem("scalesOptions", jsonString);
    }*/


    function RxCore_getDocScales(){

        var scalesoptions = undefined;
        if(docpagNotReady()){
            return;
        }

        if(DocObj){
            scalesoptions = DocObj.scalesOptions;
        }
        return scalesoptions;
        
    }


    function RxCore_updateScaleList(scalesOptions) {
        DocObj.scalesOptions = scalesOptions;

    }


    function send3DViews(xmlurlsave, filePath){
        var camJSON = "";

        if(docpagNotReady()){
            return;
        }


        if (DocObj.getCurrentpage().usevector3Dxml) {
            camJSON = DocObj.getCurrentpage().get3DCamerasJSON();
        }

        //var path = getPath(DocObj.OriginalURL);

        //var viewssendurl = xmlurlsave + "&" + path + "&" + fileName + "&" + DisplayName;
        var viewssendurl = xmlurlsave + "&" + filePath; //+ "&" + DisplayName;

        sendHttpRequest('POST', viewssendurl, '', camJSON).then(function (response){
            
            /*if (RxCore_GUI_MarkupSave != undefined){
                RxCore_GUI_MarkupSave.markupSaved();
            }*/


        }).catch(function(error){

           if(error.status){
            if (error.status == 404) {
                console.log("Post of 3D views failed error 404");
            } else if (error.status == 503) {
                console.log("Post of 3D views failed error 503");
            }
           }else{
            console.log(error);
           }

       });


    }


    function send3DMarkup(xmlurlsave, filePath) {
        
        var markupJSON = "";

        if(docpagNotReady()){
            return;
        }

        

        if (DocObj.getCurrentpage().usevector3Dxml) {
            markupJSON = DocObj.getCurrentpage().get3DMarkupJSON();
        }

        var markupsendurl = xmlurlsave + "&" + filePath;// + "&" + signature + "&" + DisplayName;


        /*if (bMarkupPostOnly){
            var markupsendurl = xmlurlsave;// + "&" + path + "&" + signature;
        }else{
            //markupsendurl = xmlurlsave + "&" + path + "&" + fileName + "&" + signature + "&" + DisplayName;
            
        }*/

        //const sendHttpRequest = function(method, url, rtype, data, reqheader){

        sendHttpRequest('POST', markupsendurl, '', markupJSON).then(function (response){

            /*if (RxCore_GUI_MarkupSave != undefined){
                RxCore_GUI_MarkupSave.markupSaved();
            }*/

            //store path to use with a tag in the markup file.
            //store the temp path to the DocObj.


        }).catch(function(error){

           if(error.status){
            if (error.status == 404) {
                
                console.log("post of 3D markup failed error 404");
            } else if (error.status == 503) {

                console.log("post of 3D markup failed error 503");
            }
           }else{
               
               console.log(error);
           }

       });

              
        
    }

    
    function listFoxit_Annots(){

        if(docpagNotReady()){
            return;
        }

        if (DocObj.foxitdoc != null){
            DocObj.foxitdoc.listAnnotations();
        }

    }

    function annotFoxit_Add(annotobj, pagenum){

        //annotFoxit_Add(annotobj, pagenum);
        if(docpagNotReady()){
            return;
        }

        if (DocObj.foxitdoc != null){
            DocObj.foxitdoc.addAnnotation(annotobj, pagenum);
        }

    }


    function importFDF(url){

        if(docpagNotReady()){
            return;
        }

        var oReq = new XMLHttpRequest();
        oReq.open("GET", url, true);
        oReq.responseType = "arraybuffer";

        oReq.onload = function (oEvent) {
            var arrayBuffer = oReq.response; // Note: not oReq.responseText
            if (arrayBuffer) {
                
                if (DocObj.foxitdoc != null){
                    DocObj.foxitdoc.importFDF(arrayBuffer);
                }

            }
        };

        oReq.send(null);

    }
    function sendFDF(xmlurlsave, blob){
        var oReq = new XMLHttpRequest();
        
        //var markupsendurl = xmlurlsave;// + "&" + path + "&" + signature;

        oReq.open("POST", xmlurlsave, true);

        oReq.onload = function (oEvent) {
                // Uploaded.
        };

        //var blob = new Blob(['abc123'], {type: 'text/plain'});

        oReq.send(blob);
    }

    function sendMarkupReq(xmlurlsave, signature, dispname, markupxml){


        if(DocObj == null || DocObj == undefined){
            return;
        }

        var path = getPath(DocObj.OriginalURL);

        if (bMarkupPostOnly){
            var markupsendurl = xmlurlsave;// + "&" + path + "&" + signature;
        }else{
            markupsendurl = xmlurlsave + "&" + path + "&" + signature;
        }

        //const sendHttpRequest = function(method, url, rtype, data, reqheader){
        sendHttpRequest('POST', markupsendurl, '', markupxml).then(function (response){
            
            if (bRefreshmarkup) {
                DocObj.markuplist = [];


                clearUsers();
                addCurrentUser();
    
                /*numUsers = 0;
                Userlist = [];
                Userlist[0] = new Users(signature, DisplayName, markuplayer, markupcolor);
                numUsers++;*/
                DocObj.nummarkups = 0;
                getMarkupFilelist('');
            }

            if(DocObj){
                DocObj.bMarkupchanged = false;
                if(DocObj.getdelayedClose()){
                    //closefile
                    DocObj.setdelayedClose(false);
                    internalClosedoc();
                    
                }
            }

            if (RxCore_GUI_MarkupSave != undefined){
                RxCore_GUI_MarkupSave.markupSaved();
            }

            if(DocObj){
                var path = addSlash(DocObj.OriginalURL);
                getMarkupFileUrls(path, DocObj);
            }

            //getMarkupFileUrls(Filepath, DocRef){

        }).catch(function(error){

           if(error.status){
            if (error.status == 404) {
                
                console.log("post of markup to server failed error 404");
            } else if (error.status == 503) {
                
                console.log("post of markup to server failed error 503");
            }
           }else{
            console.log(error);
           }

       });
    }    

    function getMarkupJSONEx(signature, dispname) {

        var markupsave = new markupsaveObjJSON(DocObj);

        markupsave.setOncomplete(function(jsonmarkup){
            
            //console.log(xmlmarkup);
            //sendMarkupReq(xmlurlsave, signature, dispname, xmlmarkup);

            RxCore_GUI_MarkuplistJSON.setMarkupList(jsonmarkup, DocObj.bActive);


        });

        markupsave.createjson(signature, dispname);


    }

    function getMarkupJSON(signature, dispname) {

        if(docpagNotReady()){
            return;
        }

        var jsonmarkup = createmarkupJSON(true, false,true,signature, dispname);
        RxCore_GUI_MarkuplistJSON.setMarkupList(jsonmarkup, DocObj.bActive);


    }



    function sendMarkupEx(xmlurlsave, signature, dispname){

        var markupsave = new markupsaveObj(DocObj);

        markupsave.setOncomplete(function(xmlmarkup){
            
            //console.log(xmlmarkup);
            
            sendMarkupReq(xmlurlsave, signature, dispname, xmlmarkup);
        });

        markupsave.createxml(signature, dispname);
        
    }

    function sendMarkup(xmlurlsave, signature, dispname) {
        /*if(DocObj.getCurrentpage().usepdfjs){
         internalscale = DocObj.getCurrentpage().curpagescale*DocObj.getCurrentpage().dscalepdf;
         }*/


         if(docpagNotReady()){
            return;
        }


        if (DocObj.getCurrentpage().usevector3Dxml) {

            //need to add additional saves for camera and 2D markup
            //send3DMarkup(xmlurlmarkupsave, signature, DisplayName);

            var markupfilePath = DocObj.getMarkup3DSavefile();  
            var viewsfilePath = DocObj.getViews3DSavefile();  

            if(markupfilePath == null){
                DocObj.setMarkup3DSavefile('markup.json');
            }
            if(viewsfilePath == null){
                DocObj.setViews3DSavefile('views.json');
            }

            var path = getPath(DocObj.OriginalURL);

            markupfilePath = path + DocObj.getMarkup3DSavefile();  
            viewsfilePath = path + DocObj.getViews3DSavefile();  

            send3DMarkup(savefileEx, markupfilePath);
            send3DViews(savefileEx, viewsfilePath);

        }    

        
        var markupxml = createxmlmarkup(true, false,false,signature, dispname);
        sendMarkupReq(xmlurlsave, signature, dispname, markupxml);


        


    } // end sendmarkup


    function getConfigJSONXML(){

        if(serverJSONcommand == undefined){
            return false;
        }

        if(ConfigurationJSON == null){
            return false;
        }


        /*var GetConfigJSON = [{
            Command : 'GetConfig'
        }];*/


        var jsonstring = JSON.stringify(ConfigurationJSON);


        sendHttpRequestXML('POST', serverJSONcommand, jsonstring).then(function (response){
                
            configcompleteResp(response);

        }).catch(function(error){
            if(error.status){
                console.log(error.status);

            }else{
                console.log(error);
            }
        });

        return true;

    }

    function getConfigJSON(){

        if(serverJSONcommand != undefined){

        }

        
        var XMLGetConfig = "<?xml version=\"1.0\" encoding=\"UTF-8\"?>";
        XMLGetConfig += "<RxViewServer>";
        XMLGetConfig += "<Command>GetConfiguration</Command>";
        XMLGetConfig += "<LicenseID>" + LicenseID + "</LicenseID>";
        if (locationSearchSet) {
            XMLGetConfig += "<CallingPageUrl>" + htmlviewerurl + szLocationSearch + "</CallingPageUrl>";
        } else {
            XMLGetConfig += "<CallingPageUrl>" + htmlviewerurl + "</CallingPageUrl>";
        }

        XMLGetConfig += "</RxViewServer>";



        if(bUsecustomConfig){
            sendHttpRequest('GET', configurationLocation, '').then(function (response){
                configJSON = Object.freeze(JSON.parse(response));
                
                configcompleteJSON(configJSON);

            }).catch(function(error){
                if(error.status){
                     console.log(error.status);
                }else{
                    

                    console.log(error);
                }
     
            });
 

        }else{

            sendHttpRequest('POST', configurationLocation, '', XMLGetConfig).then(function (response){
                
                configJSON = Object.freeze(JSON.parse(response));
                configcompleteJSON(configJSON);


            }).catch(function(error){
                if(error.status){
                     
                     console.log(error.status);
                }else{
                    
                    console.log(error);
                }
            });
 

        }

        
    }

    function getConfiguration(configurationLocation) {
        var XMLGetConfig = "<?xml version=\"1.0\" encoding=\"UTF-8\"?>";
        XMLGetConfig += "<RxViewServer>";
        XMLGetConfig += "<Command>GetConfiguration</Command>";
        XMLGetConfig += "<LicenseID>" + LicenseID + "</LicenseID>";
        if (locationSearchSet) {
            XMLGetConfig += "<CallingPageUrl>" + htmlviewerurl + szLocationSearch + "</CallingPageUrl>";
        } else {
            XMLGetConfig += "<CallingPageUrl>" + htmlviewerurl + "</CallingPageUrl>";
        }

        XMLGetConfig += "</RxViewServer>";


        if(bUsecustomConfig){
            sendHttpRequestXML('GET', configurationLocation).then(function (response){

                configcompleteResp(response);

            }).catch(function(error){
                if(error.status){
                    console.log(error.status);
                }else{
                    console.log(error);
                }
            });

        }else{
            sendHttpRequestXML('POST', configurationLocation, XMLGetConfig).then(function (response){
                
                configcompleteResp(response);

            }).catch(function(error){
                if(error.status){
                    console.log(error.status);

                }else{
                    console.log(error);
                }
            });

        }


    }


    function licFromlocalStorage(){
        if (typeof(Storage) !== "undefined") {

            if (localStorage.getItem("RxLic") != null) {
                var existingLic = localStorage.getItem("RxLic");
                localStorage.removeItem("RxLic");
                FreeLicenseInternal(existingLic, true);
            }else{
                GetLicense();
            }

        }else{
            // Sorry! No web storage support..
        }
    }

    function userFromLocalStorage(){

        var user = undefined;
        var displayname = undefined;

        if (typeof(Storage) !== "undefined") {

            if (localStorage.getItem("User") != null) {
                user = localStorage.getItem("User");

            }

            if (localStorage.getItem("UserName") != null) {
                displayname = localStorage.getItem("UserName");

            }


        }else{
            // Sorry! No web storage support..
        }

        return {user : user, displayname : displayname};

    }

    function userToLocalStorage(sign, displ, boverride){

        if (typeof(Storage) !== "undefined") {

            if(boverride){
                localStorage.setItem("User", displ);            
                localStorage.setItem("UserName", displ);
            }else{
                localStorage.setItem("User", signature);            
                localStorage.setItem("UserName", GetDisplayName(signature));

                openDatabase('User').then(() => {
                    saveDataToIndexedDB({store : 'User', User : signature, UserName : GetDisplayName(signature)});
                }).catch((err) => console.error("Error opening database:", err));


            }


        }else{
            // Sorry! No web storage support..
        }

        
    }

    function licTolocalStorage(LicID){

        if (typeof(Storage) !== "undefined") {
            localStorage.setItem("RxLic", LicID);         

            /*openDatabase('Rxlic').then(() => {
                saveDataToIndexedDB({store : 'Rxlic', Rxlic : LicID});
            }).catch(() => console.error("Error opening database:", err));*/

        }else{
            // Sorry! No web storage support..
        }


    } 

    async function openDatabase(storeName) {
        //debugger
        return new Promise((resolve, reject) => {
          const request = indexedDB.open("myAppDatabase", 1);
      
          // Upgrade database if necessary
          request.onupgradeneeded = function(event) {
            const db = event.target.result;
      //debugger
            // Create an object store for saving images, stamps, symbols, and signatures
            if (!db.objectStoreNames.contains(storeName)) {
                if (storeName === 'Rxlic') {
                    const objectStore = db.createObjectStore(storeName, { keyPath: "id", autoIncrement: true });
                        objectStore.createIndex("Rxlic", "Rxlic", { unique: false });
                } else if (storeName === 'User') {
                    const objectStore = db.createObjectStore(storeName, { keyPath: "id", autoIncrement: true });
                        objectStore.createIndex("User", "User", { unique: false });
                        objectStore.createIndex("UserName", "UserName", { unique: false });
                }
            }
          };
      
          request.onsuccess = function(event) {
            resolve(event.target.result);
          };
      
          request.onerror = function(event) {
            reject("Database error: " + event.target.errorCode);
          };
        });
      }
      
      // Function to save data in IndexedDB
      async function saveDataToIndexedDB(data) {
        //debugger
        const db = await openDatabase(data.store);
      
        const transaction = db.transaction(data.store, "readwrite");
        const store = transaction.objectStore(data.store);
      
        // Add the data to the store
        const request = store.add({ fileData: data });
      
        request.onsuccess = function() {
          console.log("Data successfully saved to IndexedDB!");
        };
      
        request.onerror = function(event) {
          console.error("Error saving data to IndexedDB: ", event.target.errorCode);
        };
      }
      
      // Function to retrieve data from IndexedDB
      async function getDataFromIndexedDB(id) {
        let db = await openDatabase();
      
        let transaction = db.transaction("assetsStore", "readonly");
        let store = transaction.objectStore("assetsStore");
      
        let request = store.get(id);
      
        return new Promise((resolve, reject) => {
          request.onsuccess = function(event) {
            resolve(event.target.result);
          };
      
          request.onerror = function(event) {
            reject("Error retrieving data from IndexedDB: " + event.target.errorCode);
          };
        });
      }

    function RxCore_useMarkupscaling(onOff){

        bUseMarkupScaling = onOff;
    }


    function RxCore_useCompanyFeature(feature){

        //var openUsessionurlEx = RxConfig.opensessionurlEx;
        bUseCompanyFeature = true;
        szLicenCompanyFeature = feature;
     
    }


    function GetLicense() {


        /*if(bLicenseAquired){
            return;
        }*/

        /* Andriy TODO: REMOVE, prevent checking licenses */

        //LicenseID = 50942768;
        //bLicenseAquired = true;

        //return;
        /*var formData = new FormData();

         //text/plain
         formData.append("MfcISAPICommand", "FormRequest");
         formData.append("Command", "OpenSession");
         formData.append("LicenseType", "Normal");
         formData.enctype = "text/plain";*/
        var licmode = 'LicenseMode' + 'RxView360' + OEMFlag;
        
        
        if(bUseCompanyFeature){
            
            //var geturl = openUsessionurl + '&' + 'RxView360' + szLicenCompanyFeature + '&2.0' + '&' + signature;
            
            var geturl = openUsessionurlEx + '&' + 'RxView360' + '&2.0' + '&' + signature + '&' + szLicenCompanyFeature;


        }else{
            geturl = openUsessionurl + '&' + 'RxView360' + OEMFlag + '&2.0' + '&' + signature;

        }
                

        if(OEMFlag == 'bypass'){
            LicenseID = 50942768;
            bLicenseAquired = true;
            return;
        }

        sendHttpRequest('GET', geturl, '' ).then(function (xmlDoc){

            var responsArray = xmlDoc.split(':');

            if (responsArray[0] == 'OK') {
                var licensearr = responsArray[1].split(',');
                LicenseID = licensearr[0];
    
                licTolocalStorage(LicenseID);
                
    
                bLicenseAquired = true;

                readycheck();
                if (OEMFlag == 'OEM') {
                    //$('.disableOpenClose').remove();
                }
            } else {
                alert(xmlDoc);
            }



        }).catch(function(error){

            if(error.status){
                console.log(error.status);

            }else{
                console.log(error);
            }

        });


    }

    function FreeLicense(){
        
        if(!bLicenseAquired){
            return;
        }

        FreeLicenseInternal(LicenseID, false);

    }

    
    function FreeLicenseInternal(licid, bstartup) {
        

        var geturl = closesessionurl + '&' + licid;

        sendHttpRequest('GET', geturl, '').then(function (xmlDoc){

            bLicenseAquired = false;
            if(bstartup){
                GetLicense();
            }


        }).catch(function(error){
            if(error.status){
    
            }else{
                console.log(error);
            }

        });



    }


    function printfile(prwin) {
        prwin.print();

    }


    function checkprintload() {
        if (printWin.document.readyState == 'complete' && bPrintpageloaded) {
            var printbtn = printWin.document.getElementById('printbtn');
            printbtn.click();
        } else {
            window.setTimeout(checkprintload, 1000);
        }
    }


    function printloadcomplete(ev) {
        //ev.url
        //alert('try to print');
        bPrintpageloaded = true;
        //var printbtn = printWin.document.getElementById('printbtn');
        //printbtn.click();
        //checkprintload();
    }

    function GetPrintRect(){
        return PrintRect;
    }

    function GetPaperWidth() {
        return PaperWidth;
    }

    function ClosePrint() {
        printWin.close();
    }

    function GetPaperHeight() {
        return PaperHeight;
    }

    function GetPrintObject() {
        return DocObj;
    }

    function GetPageObject() {
        return DocXMLPrint;
    }


    function PrintWindow() {
        window.print();
    
        //setTimeout("RxCore_printhelper().printfocus()", 100);
        /*window.onfocus=function(){
            window.close();
        }*/
        //opener.RxCore.printhelper().printfocus();
        //CheckWindowState();
        //opener.RxCore.printhelper().printfocus();
    
    }

    function doprintcheck(){
        var canprint = true;
    
    
        for (var i = 0; i < aDrawpages.length;i++){
            if(!aDrawpages[i]){
                canprint = false;
            }
            /*reinstate this when markup is implemented.
            for(var j = 0;j < opener.RxCore.printhelper().docObj.markuplist.length;j++){
                if(opener.RxCore.printhelper().docObj.markuplist){
                    if(!aDrawpages[i] || !opener.RxCore.printhelper().docObj.markuplist[j].drawn){
    
                        canprint = false;
    
                    }
    
                }else{
                    if(!aDrawpages[i]){
                        canprint = false;
                    }
    
                }
    
            }*/
    
        }
    
        /*for(var j = 0;aDrawmarkup.length;j++){
            if(!aDrawmarkup[j]){
                console.log(aDrawmarkup[j]);
                //canprint = false;
            }
        }*/
    
    
        if (canprint && hasnotprinted){
    
            hasnotprinted = false;
            PrintWindow();
    
        }
    }
    
    function onPrintpage(printobj){
    
    
        //var localcanvas = printobj.paperimage;
        canvimages[printobj.pagenumber] = document.createElement('img');
        canvimages[printobj.pagenumber].src = printobj.paperimage.toDataURL();
        canvimages[printobj.pagenumber].addEventListener('load', printimageloaded, false);
    
        function printimageloaded(ev){
          try{
            pageCanvases[printobj.pagenumber].drawImage(canvimages[printobj.pagenumber],0,0);
          }
        
          catch(err) {
           console.log(err.message);
          }
         aDrawpages[printobj.pagenumber] = true;
         doprintcheck();
    
    
        }     
         
    
    
    }        

    function clearPrintDiv(){

        if(rxprintcontainer == undefined){
            return;
        }

        while (rxprintcontainer.firstChild) {
            rxprintcontainer.removeChild(rxprintcontainer.lastChild);
          }


          pageCanvases = [];
          aDrawpages = [];
          canvimages = [];
          //var aDrawmarkup = [];
          //var canvimages = [];
          canvaspages = [];
  

          /*<p>this is the print div</p>
          <img src="images/welcome.jpg">*/
    }

    function printDiv(){
    
        hasnotprinted = true;
        

        if(rxprintcontainer == undefined){
            return;
        }

        if(RxCore_printhelper().docObj == null){
            return;
        }

        //pageCanvases = [];
        //aDrawpages = [];
        //canvimages = [];
        //var aDrawmarkup = [];
        //var canvimages = [];
        //canvaspages = [];
        clearPrintDiv();

        //const paperwidthA4px = 2460;
        //const paperheightA4px = 3480;

        try {
            PaperHeight = RxCore_printhelper().getPaperHeight();
             //PaperHeight = 3480;
         }
         catch (e) {
             PaperHeight = paperheightA4px;
         }

         try {
            PaperWidth = RxCore_printhelper().getPaperWidth();
            //PaperWidth = 2460;
        }
        catch (e) {
            PaperWidth = paperwidthA4px;
       
        }
  
        try {
          PrintRect = RxCore_printhelper().getPrintRect();
  
        }
        catch(e){
          PrintRect = undefined;
        }

        var paperobj = {
            width : PaperWidth,
            height : PaperHeight,  
            printrect : PrintRect
          };
          
        var pwidth = (paperobj.width / 25.4)* 96;
        var pheight = (paperobj.height / 25.4)* 96;

        var pwidthpx = pwidth + "px";
        var pheightpx = pheight + "px";

        var canvashireswidth = (paperobj.width / 25.4)* 300;
        var canvashiresheight = (paperobj.height / 25.4)* 300;

        //canvaso.width = canvashireswidth;
        //canvaso.height = canvashiresheight;

        //canvaso.style.width = pwidth + "px";
        //canvaso.style.height = pheight + "px";


        var numpages = RxCore_printhelper().docObj.pages.length;

        

        for(var i = 0; i < numpages; i++){
            //var container = rxprintcontainer;

            var canvpage = document.createElement('canvas');
            if(!canvpage){
            alert('Error: I cannot create a new canvas element!');
                return;
            }
            
            canvpage.id     = 'page' + i;

            canvpage.width  = canvashireswidth;
            canvpage.height = canvashiresheight;

            canvpage.style.width  = pwidthpx;
            canvpage.style.height = pheightpx;
            
            rxprintcontainer.appendChild(canvpage);

            aDrawpages[i] = false;

            var contextpg = canvpage.getContext('2d');

            if (!contextpg) {
                alert('Error: failed to getContext!');
                return;
            }
         
            pageCanvases.push(contextpg);
        }

        

        /*if (RxCore_printhelper().docObj.foxitdoc != null){




            for (var pg = 0; pg < numpages; pg++) {
                           
                canvaspages.push(new printpdfpageobject(pg, paperobj));

                //canvaspages[pg].queueRenderPage();
    
            }
    

        }*/

        RxCore_printhelper().onPrintpage.connect(onPrintpage);
        RxCore_printhelper().docObj.printDocument(paperobj);

        /*if (RxCore_printhelper().docObj.pdfDoc != null){

            for (var pg = 0; pg < numpages; pg++) {
                           
                canvaspages.push(new printpdfpageobject(pg, paperobj));
                canvaspages[pg].queueRenderPage();
    
            }
    

        }else{
            
            

        }*/



    }

    function setrxprintdiv(printdiv){
        
        rxprintcontainer = printdiv;

    }

    

    function printCanvas(imagedataurl) {



        printDiv();
        //printWin = window.open(imagedataurl, "Print");

        /*if(DocObj.getCurrentpage().usevectorxml){
         printWin = window.open('printsvg.htm',"Print");
         }else{
         printWin = window.open(imagedataurl,"Print");
         }*/

        //    printWin.focus();

    }

    function printfocus() {
        //printWin = window.open(imagedataurl,"Print");
        if(CompareObj != undefined){
            if(CompareObj.printref != undefined){
                CompareObj.printClose();
            }
        }
        printWin.focus();
        printWin.close();


    }



    /*function PDFIframeLoad(printwindow){
     var iframe = printwindow.document.getElementById('iframe_a');
     if(iframe.src) {
     var frm = iframe.contentWindow;

     frm.focus();// focus on contentWindow is needed on some ie versions
     frm.print();
     return false;
     }
     }*/


    /*function printPDF(printwindow){
     if (navigator.appName == 'Microsoft Internet Explorer') {

     //Wait until PDF is ready to print
     if (typeof printwindow.document.getElementById("pdfDocument").print == 'undefined') {

     setTimeout(function(){printPDF("pdfDocument");}, 1000);

     } else {

     var x = printwindow.document.getElementById("pdfDocument");
     x.print();
     }

     } else{

     PDFIframeLoad(printwindow);  // for chrome
     }
     }*/


    /*function sendMarkup(xmlurl,signature) {
     var curmarkup = 0;
     var savetime = new Date().getTime();
     var xhr = new XMLHttpRequest();


     var markupxml = "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n";

     markupxml += "<RasterexMarkup>\n";
     markupxml += "<Header>\n";
     markupxml += "<FileVersion>C350</FileVersion>\n";
     markupxml += "<Scale>1.0000</Scale>\n";
     markupxml += "<OrigoX>0.0000</OrigoX>\n";
     markupxml += "<OrigoY>0.0000</OrigoY>\n";
     markupxml += "<DirectionY>1</DirectionY>\n";
     markupxml += "<DirectionX>1</DirectionX>\n";
     markupxml += "</Header>\n";

     markupxml += "<Users>\n";
     markupxml += "<Name>"+ signature +"</Name>\n";
     markupxml += "</Users>\n";
     markupxml += "<Entities>\n";

     //var found = false;
     var id = -1;
     if (DocObj.markuplist.length > 0){
     for (curmarkup=0;curmarkup<DocObj.markuplist.length;curmarkup++){
     if (DocObj.markuplist[curmarkup] != null){
     if(DocObj.markuplist[curmarkup].signature == signature){
     var pagenumber = DocObj.markuplist[curmarkup].pagenumber;
     markupxml += "<Entity>\n";
     markupxml += "<Type>" + DocObj.markuplist[curmarkup].type + "</Type>\n";
     markupxml += "<Subtype>" + DocObj.markuplist[curmarkup].subtype + "</Subtype>\n";
     markupxml += "<Alternative>" + DocObj.markuplist[curmarkup].alternative + "</Alternative>\n";
     markupxml += "<Layer>" + DocObj.markuplist[curmarkup].layer + "</Layer>\n";
     markupxml += "<Rotation>" + DocObj.markuplist[curmarkup].rotation + "</Rotation>\n";
     markupxml += "<FileName>" + DocObj.markuplist[curmarkup].documentName + "</FileName>\n";
     markupxml += "<Layout>" + pageobject.LayoutName + "</Layout>\n";
     markupxml += "<View>" + pagenumber + "</View>\n";
     markupxml += "<Handle>" + curmarkup + "</Handle>\n";
     markupxml += "<Color>" + DocObj.markuplist[curmarkup].color + "</Color>\n";
     markupxml += "<FontName>"+ DocObj.markuplist[curmarkup].fontname +"</FontName>\n";
     markupxml += "<FontHeight>"+ DocObj.markuplist[curmarkup].textheight +"</FontHeight>\n";
     if (DocObj.markuplist[curmarkup].text == ""){DocObj.markuplist[curmarkup].text = ".";}
     markupxml += "<Text>"+ DocObj.markuplist[curmarkup].text +"</Text>\n";
     if (DocObj.markuplist[curmarkup].dimtext == ""){DocObj.markuplist[curmarkup].dimtext = ".";}
     markupxml += "<DimText>"+ DocObj.markuplist[curmarkup].dimtext +"</DimText>\n";
     //<Font height='-16' width='0' escapement='0' orientation='0' weight='400' italic='0' underline='0' 		strikeout='0' charset='1' outprecision='3' clipprecision='2' quality='1' pitch='34' 		facename='Arial'/>
     markupxml += "<TextWidth>" + DocObj.markuplist[curmarkup].textwidth + "</TextWidth>\n";
     markupxml += "<Signature>" + DocObj.markuplist[curmarkup].signature + "</Signature>\n";
     markupxml += "<TimeStamp>" + DocObj.markuplist[curmarkup].timestamp + "</TimeStamp>\n";
     markupxml += "<Scaling>" + DocObj.markuplist[curmarkup].scaling + "</Scaling>\n";
     markupxml += "<Xoffset>" + DocObj.markuplist[curmarkup].xoffset + "</Xoffset>\n";
     markupxml += "<Yoffset>" + DocObj.markuplist[curmarkup].yoffset + "</Yoffset>\n";
     markupxml += "<LineWidth>" + DocObj.markuplist[curmarkup].linewidth + "</LineWidth>\n";

     if (DocObj.markuplist[curmarkup].numpoints != -1){
     markupxml += "<Points>\n";
     for (i =0; i<DocObj.markuplist[curmarkup].points.length;i++){
     markupxml += "<Point x='"+DocObj.markuplist[curmarkup].points[i].x + "' y='" +DocObj.markuplist[curmarkup].points[i].y + "'/>\n";
     }
     markupxml += "</Points>\n";
     }

     markupxml += "<Rect x='"+ DocObj.markuplist[curmarkup].x + "' y='" + DocObj.markuplist[curmarkup].y + "' w='" + DocObj.markuplist[curmarkup].w + "' h='" + DocObj.markuplist[curmarkup].h + "' />\n";
     markupxml += "</Entity>\n";

     }
     }
     }//end for
     //SendData(markupxml,"http://viewserver.rasterex.com/demo/markupserver.dll?webmarkupsave");
     }else{
     markupxml += "<Entity/>"
     }//end if

     markupxml += "</Entities>\n";
     markupxml += "</RasterexMarkup>\n";

     var path = getPath(DocObj.OriginalURL);
     var markupsendurl = xmlurl + "&" + path + "&" + signature;
     try{
     xhr.open('POST', markupsendurl, true);
     }
     catch(e){
     alert("Error 1 - " + e);
     }
     try{
     xhr.responseType = 'text/xml';
     }
     catch(e){
     //alert("Error 2 - " + e);
     }

     xhr.onload = function(e) {
     if (this.status == 200) {
     alert("Markup Saved");
     }
     else if (this.status == 404){
     alert("server could not be found");
     }
     else if (this.status == 503){
     alert("Server is down");
     }

     };
     xhr.send(markupxml);


     }// end sendmarkup*/


    function img_update() {
        cntximg.drawImage(canvas, 0, 0);
        context.clearRect(0, 0, canvas.width, canvas.height);
        
    }


    function draw_image(image, dx, dy, dscale) {

        /*var dxextent=0.0, dyextent=0.0;
         var imagewidth = 0, imageheight = 0;
         var dscale=1;
         var dscaleextent=1;*/


        var localdx = 0.0;
        var localdy = 0.0;
        var localdscale = 0.0;

        contexto.fillStyle = "rgb(238,243,250)";
        contexto.fillRect(0, 0, canvasowidth, canvasoheight);
        switch (imagesize) {
            case 0:
                localdx = dx;
                localdy = dy;
                localdscale = dscale;
                break;
            case 1:
                localdx = dx;
                localdy = dy;
                localdscale = dscale;

                break;
        }


        if (drotation == 0) {
            contexto.drawImage(image, localdx, localdy, imagewidth * localdscale, imageheight * localdscale);

        } else {
            contexto.save();
            var tx = (canvasowidth / 2);
            var ty = (canvasoheight / 2);
            contexto.translate(tx, ty);
            contexto.rotate(drotation * (Math.PI / 180));
            contexto.translate(-tx, -ty);
            contexto.drawImage(image, localdx, localdy, imagewidth * localdscale, imageheight * localdscale);
            contexto.restore();

        }
    }

    /*function rotateCanvas(){
     var localdx = 0.0;
     var localdy = 0.0;
     var localdscale = 0.0;



     contexto.save();
     var tx = (canvasowidth/2);
     var ty = (canvasoheight/2);
     contexto.translate(tx,ty);
     contexto.rotate(drotation*(Math.PI/180));
     contexto.translate(-tx,-ty);
     contexto.fillStyle = "rgb(238,243,250)";
     contexto.fillRect(0, 0, canvasowidth, canvasoheight);
     switch(imagesize){
     case 0:
     localdx = dx;
     localdy = dy;
     localdscale = dscale;
     contexto.drawImage(myimage, localdx, localdy,imagewidth*dscale, imageheight*dscaleextent);
     break;
     case 1:
     localdx = dxextent;
     localdy = dyextent;
     localdscale = dscaleextent;
     contexto.drawImage(myimagesmall, localdx, localdy,imagewidth*dscale, imageheight*dscaleextent);
     break;
     }




     contexto.restore();

     }*/

    /*function rotateimage(degree){
     var i = 0;
     switch(drotation){
     case 0:
     if (degree == 90){
     for(i=0;i<=90;i=i+10){
     drotation = i;
     rotateCanvas();
     }
     }
     if (degree == 180){
     for(i=0;i<=180;i=i+10){
     drotation = i;
     rotateCanvas();
     }
     }
     if (degree == 270){
     for(i=0;i<=270;i=i+10){
     drotation = i;
     rotateCanvas();
     }
     }
     break;
     case 90:
     if (degree == 0){
     for(i=90;i>=0;i=i-10){
     drotation = i;
     rotateCanvas();
     }
     }
     if (degree == 180){
     for(i=90;i<=180;i=i+10){
     drotation = i;
     rotateCanvas();
     }
     }
     if (degree == 270){
     for(i=90;i<=270;i=i+10){
     drotation = i;
     rotateCanvas();
     }
     }

     break;
     case 180:
     if (degree == 0){
     for(i=180;i>=0;i=i-10){
     drotation = i;
     rotateCanvas();
     }
     }
     if (degree == 270){
     for(i=180;i<=270;i=i+10){
     drotation = i;
     rotateCanvas();
     }
     }
     if (degree == 90){
     for(i=180;i>=90;i=i-10){
     drotation = i;
     rotateCanvas();
     }
     }

     break;
     case 270:
     if (degree == 0){
     for(i=270;i>=0;i=i-10){
     drotation = i;
     rotateCanvas();
     }
     }
     if (degree == 180){
     for(i=270;i>=180;i=i-10){
     drotation = i;
     rotateCanvas();
     }
     }
     if (degree == 90){
     for(i=270;i>=90;i=i-10){
     drotation = i;
     rotateCanvas();
     }
     }

     break;
     }

     DocObj.getCurrentpage().draw_image();



     //draw_image(myimage);

     }*/

    function GetImageWidth(image) {

        var nimagewidth = image.width;
        nimagewidth = image.naturalWidth;

        return imagewidth;
    }

    function GetImageHeight(image) {

        var nimageheight = image.width;
        nimageheight = image.naturalWidth;

        return imageheight;
    }

    function SetImageDim(image) {
        imagewidth = image.width;
        imageheight = image.height;

        imagewidth = image.naturalWidth;
        imageheight = image.naturalHeight;

    }

    function splashload() {


        imageloaded = true;
    }

    function imageloadprogress(ev) {
        if (ev.lengthComputable) {
            //var percentComplete = Math.round(evt.loaded * 100 / evt.total);


            /*document.getElementById('progressbar').value = Math.round(ev.loaded * 100 / ev.total);//percentComplete;//.toString() + '%';
             if (document.getElementById('progressbar').value == 100){
             document.getElementById('progressbar').value = 0;
             //hideUploadDialog();

             }*/
        } else {
            //document.getElementById('progressNumber').innerHTML = 'unable to compute';
        }

    }

    function imageload(ev) {
        var yscale = 0.0;
        var xscale = 0.0;
        var dxlocal = 0.0;
        var dylocal = 0.0;
        var dscalelocal = 0.0;
        var imagewidth = 0;
        var imageheight = 0;

        var target = ev.target || ev.srcElement;

        //if (ev.srcElement.src.toLowerCase() == DocObj.getCurrentpage().MainImageSRC.toLowerCase()){
        if (target.src.toLowerCase() == DocObj.getCurrentpage().MainImageSRC.toLowerCase()) {
            //largeimageloaded = true;
            //SetImageDim(ev.srcElement); //use width and height from xml instead of calculating
            imagewidth = DocObj.getCurrentpage().MainImageWidth;
            imageheight = DocObj.getCurrentpage().MainImageHeight;
            
            //set image fixed scale for markup
            yscale = fixedScaleSize.height / DocObj.getCurrentpage().MainImageHeight; //thispage.MainImageHeight;
            xscale = fixedScaleSize.width / DocObj.getCurrentpage().MainImageWidth; // thispage.MainImageWidth;
            DocObj.getCurrentpage().fixedScale = Math.min(xscale, yscale);

            
            yscale = canvasoheight / imageheight;
            xscale = canvasowidth / imagewidth;
            dscalelocal = Math.min(xscale, yscale);
            //DocObj.getCurrentpage().dscale = dscalelocal;

            dxlocal = (canvasowidth - (imagewidth * dscalelocal)) / 2;
            dylocal = (canvasoheight - (imageheight * dscalelocal)) / 2;

            //DocObj.getCurrentpage().dx = dxlocal;
            //DocObj.getCurrentpage().dy = dylocal;

            DocObj.getCurrentpage().setimagedimlarge(dxlocal, dylocal, dscalelocal);
            //hideDownloadDialog();
            if (RxCore_GUI_Download != undefined) {
                RxCore_GUI_Download.setDownload("hide");
            }


        }

        //if (ev.srcElement.src.toLowerCase() == DocObj.getCurrentpage().SmallImageSRC.toLowerCase()){
        if (target.src.toLowerCase() == DocObj.getCurrentpage().SmallImageSRC.toLowerCase()) {
            //smallimageloaded = true;
            //SetImageDim(ev.srcElement); //use width and height from xml instead of calculating
            imagewidth = DocObj.getCurrentpage().SmallImageWidth;
            imageheight = DocObj.getCurrentpage().SmallImageHeight;

            yscale = canvasoheight / imageheight;
            xscale = canvasowidth / imagewidth;
            dscalelocal = Math.min(xscale, yscale);

            dxlocal = (canvasowidth - (imagewidth * dscalelocal)) / 2;
            dylocal = (canvasoheight - (imageheight * dscalelocal)) / 2;


            DocObj.getCurrentpage().setimagedimsmall(dxlocal, dylocal, dscalelocal);
            //hideDownloadDialog();
            if (RxCore_GUI_Download != undefined) {
                RxCore_GUI_Download.setDownload("hide");
            }

            //draw_image(ev.srcElement);
            //DocObj.getCurrentpage().draw_image();

        }

        //imageloaded = true;
        //drawmarkupAll(cntximg);


    }

    function get_splash(url, image) {
        image.addEventListener('load', splashload, false);
        image.src = url;
    }


    function get_image(url, image) {

        //showDownloadDialog();

        if (RxCore_GUI_Download != undefined) {
            RxCore_GUI_Download.setDownload("show");
        }

        image.addEventListener('load', imageload, false);
        image.addEventListener('progress', imageloadprogress, false);
        image.src = url;


    }

    /*function toolswitchstate(btool,toolname){

     if (toolname == 'zoomwindow' && !btool){
     bZoomWindow = true;
     }else{
     bZoomWindow = false;
     }

     if (toolname == 'pan' && !btool){
     bPan = true;
     }else{
     bPan = false;
     }

     if (toolname == 'markupedit' && !btool){
     bMarkupEdit = true;
     }else{
     bMarkupEdit = false;
     }


     if (toolname == 'calibrate' && !btool){
     bCalibrate = true;
     }else{
     bCalibrate = false;
     }

     //bMarkupHide = false;

     if (toolname == 'erase' && !btool){
     bMarkuperase = true;
     }else{
     bMarkuperase = false;
     }


     if (toolname == 'text' && !btool){
     bMarkupText = true;
     }else{
     bMarkupText = false;
     }


     if (toolname == 'note' && !btool){
     bMarkupNote = true;
     }else{
     bMarkupNote = false;
     }

     //bMarkupStamp = false;

     if (toolname == 'line' && !btool){
     bMarkupLine = true;
     }else{
     bMarkupLine = false;
     }


     if (toolname == 'arrow' && !btool){
     bMarkupArrow = true;
     }else{
     bMarkupArrow = false;
     }

     if (toolname == 'shape' && !btool){
     bMarkupShape = true;
     }else{
     bMarkupShape = false;
     }


     if (toolname == 'measure' && !btool){
     bMarkupDimension = true;
     }else{
     bMarkupDimension = false;
     }


     if (toolname == 'area' && !btool){
     bMarkupArea = true;
     }else{
     bMarkupArea = false;
     }

     if (toolname == 'marker' && !btool){
     bMarkupMarker = true;
     }else{
     bMarkupMarker = false;
     }

     //bMarkupOutline = true;
     //bMarkupEdged = false;
     //bMarkupFilled = false;
     //bMarkupHatched = false;
     if (toolname == 'measuretool' && !btool){
     bMeasure = true;
     }else{
     bMeasure = false;
     }

     }*/

    function uploadFiledragdrop(dragdropfile) {
        //var fd = new FormData();
        var file = dragdropfile;
        dragdropfilename = file.name;
        //fd.append("fileToUpload", file);
        var urifilename = encodeURI(file.name);

        var eventlist = [
            {event : "load", func : uploaddragComplete},
            {event : "progress", func : uploadProgress, bupload : true},
            {event : "error", func : uploadFailed},
            {event : "abort", func : uploadCanceled}
        ];

        sendHttpRequestwevents('POST', FileuploadURL + "?" + UploadServerfolder + "&" + file.name + "&" + file.lastModified + "&" + file.size, "", eventlist, file);

       
    }

    function createCompareXML(xmlDoc){
        bAbortPageload = false;

        /*if(bIsmobile){
            Get_License();
        }*/

        if (!bLicenseAquired) {
            return;
        }


        if (RxCore_GUI_HasText != undefined){
            RxCore_GUI_HasText.hastext = false;
            RxCore_GUI_HasText.hasText(RxCore_GUI_HasText.hastext, true);

        }

        cntximg.clearRect(0, 0, canvas.width, canvas.height);
        context.clearRect(0, 0, canvas.width, canvas.height);
        contexto.clearRect(0, 0, canvas.width, canvas.height);

        clearUsers();
        addCurrentUser();

        /*numUsers = 0;
        Userlist = [];
        Userlist[0] = new Users(signature, DisplayName, markuplayer, markupcolor);
        numUsers++;*/
        
        var PageObj;

        if (documentopen && bSingleDocmode) {
            
            internalClosedoc();
         }
 
        if (DocObj == null || DocObj == undefined) {
              nCurdocindex = OpenFiles.push(new DocumentObject(xmlDoc));

              DocObj = OpenFiles[nCurdocindex - 1];
              //DocObj.fileindex = nCurdocindex - 1;
              DocObj.setfileindex(nCurdocindex - 1);
 
         } else {
                     //DocObj.Suspend();
              nCurdocindex = OpenFiles.push(new DocumentObject(xmlDoc));
              DocObj.enableTextSelect(false);
              DocObj = OpenFiles[nCurdocindex - 1];
              //DocObj.fileindex = nCurdocindex - 1;
              DocObj.setfileindex(nCurdocindex - 1);
         }

        OpenFiles[nCurdocindex - 1] = DocObj;

        //DocObj.setPDFdocument(pdfdoc, false);


        var pagexmlobj = xmlDoc.getElementsByTagName('Page');

        for (var j = 0; j < pagexmlobj.length; j++) {
            
            if (j == 0) {
                PageObj = new PageObject(pagexmlobj[j], "Default", true, '', j, DocObj);
                DocObj.addthumbnail(new ThumbnailObject(pagexmlobj[j],"Default", DocObj, j));
                DocObj.addpage(PageObj, true);
            } else {
                DocObj.addthumbnail(new ThumbnailObject(pagexmlobj[j],"Default", DocObj, j));
                if(j < nMaxpages){
                    PageObj = new PageObject(pagexmlobj[j], "Default", false, '', j, DocObj);
                    DocObj.addpage(PageObj, false);
                    
                }else{
                    DocObj.addpage(undefined, false);
                }
            }
        }
        
        if(hasDocPage()){
            DocObj.pages[0].firstpage = true;
            //DocObj.bglTF = true;
            DocObj.bcompare = true;

            var stateobj = {
                iscompare:true,
                numOpenFiles:OpenFiles.length,
                isPDF:DocObj.getCurrentpage().usepdfjs,
                is3D:DocObj.getCurrentpage().usevector3Dxml,
                markupOn3D : false,
                is2D:DocObj.getCurrentpage().usevectorxml,
                numpages:DocObj.pages.length,
                currentpage:DocObj.getcurPage(),
                activefile : DocObj.bActive,
                source : 'createCompareXML' 
            };
    
            if (RxCore_GUI_State != undefined) {
                RxCore_GUI_State.setGUIState(stateobj);
            }
    
            var pagingobject = {
                numpages:DocObj.pages.length,
                currentpage:DocObj.getcurPage()
            };
    
            if(RxCore_GUI_Page != undefined){
                RxCore_GUI_Page.pageEvent(pagingobject);
            }
    
            if (Rxcore_GUI_fileLoadComplete != undefined) {
                //Rxcore_GUI_fileLoadComplete.loadComplete(thispage.DocRef.OriginalURL);
                Rxcore_GUI_fileLoadComplete.loadComplete(DocObj.OriginalURL, DocObj.bActive);
            }        
           
            if (RxCore_GUI_Download != undefined) {
                RxCore_GUI_Download.setDownload("hide");
            }
    
    
        }
        
        
        /*for (i = 0; i < layoutxmlobj.length; i++) {
            var layoutname = layoutxmlobj[i].getElementsByTagName("LayoutName")[0].firstChild.nodeValue;
            DocObj.addlayout(layoutname);
            var pagexmlobj = layoutxmlobj[i].getElementsByTagName('Page');
            
            totalpages += j;

        }*/        
        
        /*if (RxCore_GUI_pagethumbs != undefined) {
            RxCore_GUI_pagethumbs.setThumbnails(DocObj.thumbnails);
        }*/



        /*if(bIsmobile){
            FreeLicense();
        }*/
    }

    
    function createGLTFDirect(xmlDoc, URL){
        bAbortPageload = false;

        /*if(bIsmobile){
            GetLicense();
        }*/

        if (!bLicenseAquired) {
            return;
        }


        if (RxCore_GUI_HasText != undefined){
            RxCore_GUI_HasText.hastext = false;
            RxCore_GUI_HasText.hasText(RxCore_GUI_HasText.hastext, true);

        }

        cntximg.clearRect(0, 0, canvas.width, canvas.height);
        context.clearRect(0, 0, canvas.width, canvas.height);
        contexto.clearRect(0, 0, canvas.width, canvas.height);


        clearUsers();
        addCurrentUser();


        /*numUsers = 0;
        Userlist = [];
        Userlist[0] = new Users(signature, DisplayName, markuplayer, markupcolor);
        numUsers++;*/
        
        var PageObj;

        if (documentopen && bSingleDocmode) {

            internalClosedoc();
            //RxCore_Closedocument();
         }
 
        if (DocObj == null || DocObj == undefined) {
              nCurdocindex = OpenFiles.push(new DocumentObject(xmlDoc));
              
              DocObj = OpenFiles[nCurdocindex - 1];
              //DocObj.fileindex = nCurdocindex - 1;
              DocObj.setfileindex(nCurdocindex - 1);
 

         } else {
                     //DocObj.Suspend();
              nCurdocindex = OpenFiles.push(new DocumentObject(xmlDoc));
              
              DocObj.enableTextSelect(false);

              DocObj = OpenFiles[nCurdocindex - 1];
              //DocObj.fileindex = nCurdocindex - 1;
              DocObj.setfileindex(nCurdocindex - 1);
         }

        OpenFiles[nCurdocindex - 1] = DocObj;

        //DocObj.setPDFdocument(pdfdoc, false);

        var pagexmlobj = xmlDoc.getElementsByTagName('Page');

        for (var j = 0; j < pagexmlobj.length; j++) {
            
            if (j == 0) {
                PageObj = new PageObject(pagexmlobj[j], "Default", true, '', j, DocObj);
                DocObj.addthumbnail(new ThumbnailObject(pagexmlobj[j],"Default", DocObj, j));

                DocObj.addpage(PageObj, true);
            } else {


                DocObj.addthumbnail(new ThumbnailObject(pagexmlobj[j],"Default", DocObj, j));

                if(j < nMaxpages){
                    PageObj = new PageObject(pagexmlobj[j], "Default", false, '', j, DocObj);
                    DocObj.addpage(PageObj, false);
                    
                }else{
                    DocObj.addpage(undefined, false);
                }


            }
        }


        if(hasDocPage()){
            DocObj.pages[0].firstpage = true;

            //DocObj.loadPDFpages();
            //DocObj.directPDF = true;
            DocObj.bglTF = true;
            
            /*for (i = 0; i < layoutxmlobj.length; i++) {
                var layoutname = layoutxmlobj[i].getElementsByTagName("LayoutName")[0].firstChild.nodeValue;
                DocObj.addlayout(layoutname);
                var pagexmlobj = layoutxmlobj[i].getElementsByTagName('Page');
                
                totalpages += j;
    
            }*/        
            
            /*if (RxCore_GUI_pagethumbs != undefined) {
                RxCore_GUI_pagethumbs.setThumbnails(DocObj.thumbnails);
            }*/
    
            var stateobj = {
                iscompare:documentcompare,
                numOpenFiles:OpenFiles.length,
                isPDF:DocObj.getCurrentpage().usepdfjs,
                is3D:DocObj.getCurrentpage().usevector3Dxml,
                markupOn3D : false,
                is2D:DocObj.getCurrentpage().usevectorxml,
                numpages:DocObj.pages.length,
    
                currentpage:DocObj.getcurPage(),
                activefile : DocObj.bActive,
                source : 'createGLTFDirect' 
            };
    
            if (RxCore_GUI_State != undefined) {
                RxCore_GUI_State.setGUIState(stateobj);
            }
    
            var pagingobject = {
                numpages:DocObj.pages.length,
                currentpage:DocObj.getcurPage()
            };
    
            if(RxCore_GUI_Page != undefined){
                RxCore_GUI_Page.pageEvent(pagingobject);
            }
    
            if (Rxcore_GUI_fileLoadComplete != undefined) {
                //Rxcore_GUI_fileLoadComplete.loadComplete(thispage.DocRef.OriginalURL);
                Rxcore_GUI_fileLoadComplete.loadComplete(DocObj.OriginalURL, DocObj.bActive);
            }        
           
            if (RxCore_GUI_Download != undefined) {
                RxCore_GUI_Download.setDownload("hide");
            }
        }

        

        /*if(bIsmobile){
            FreeLicense();
        }*/

    }

    function createFoxitPDF(xmlDoc, foxitdoc){
        bAbortPageload = false;

        /*if(bIsmobile){
            GetLicense();
        }*/

        if (!bLicenseAquired) {
            return;
        }


        if (RxCore_GUI_HasText != undefined){
            RxCore_GUI_HasText.hastext = false;
            RxCore_GUI_HasText.hasText(RxCore_GUI_HasText.hastext, true);

        }

        cntximg.clearRect(0, 0, canvas.width, canvas.height);
        context.clearRect(0, 0, canvas.width, canvas.height);
        //contexto.clearRect(0, 0, canvas.width, canvas.height);

        clearUsers();
        addCurrentUser();

        /*numUsers = 0;
        Userlist = [];
        Userlist[0] = new Users(signature, DisplayName, markuplayer, markupcolor);
        numUsers++;*/
        
        var PageObj;

        if (documentopen && bSingleDocmode) {
            //RxCore_Closedocument();
            internalClosedoc();
         }
 
        if (DocObj == null || DocObj == undefined) {
              nCurdocindex = OpenFiles.push(new DocumentObject(xmlDoc));
              DocObj = OpenFiles[nCurdocindex - 1];
              //DocObj.fileindex = nCurdocindex - 1;
              DocObj.setfileindex(nCurdocindex - 1);
              
 
         } else {
                     //DocObj.Suspend();
              nCurdocindex = OpenFiles.push(new DocumentObject(xmlDoc));
              DocObj = OpenFiles[nCurdocindex - 1];
              //DocObj.fileindex = nCurdocindex - 1;
              DocObj.setfileindex(nCurdocindex - 1);
         }

  

        OpenFiles[nCurdocindex - 1] = DocObj;

        //DocObj.setPDFdocument(pdfdoc, false);

        DocObj.foxitdoc = foxitdoc;

        //var szformatshort = DocObj.Format.substring(0, 9);
        //var bPDF = (szformatshort == "Adobe PDF");

        //var szformatshort = DocObj.Format.substring(0, 9);
        //var bPDF = (szformatshort == "Adobe PDF");

        /*var layoutxmlobj = xmlDoc.getElementsByTagName('Layout');

        for (i = 0; i < layoutxmlobj.length; i++) {
            var layoutname = layoutxmlobj[i].getElementsByTagName("LayoutName")[0].firstChild.nodeValue;
            
            DocObj.addlayout(layoutname);

            var pagexmlobj = layoutxmlobj[i].getElementsByTagName('Page');

            for (j = 0; j < pagexmlobj.length; j++) {

                if (j == 0 && i == 0) {
                    PageObj = new PageObject(pagexmlobj[j], layoutname, true, '', j, DocObj);
                    var tempPageXML = pagexmlobj[j];
                    var tempLayoutName = layoutname;
                    DocObj.addthumbnail(new ThumbnailObject(pagexmlobj[j],layoutname, DocObj,  j));
                    DocObj.addpage(PageObj, true);
                } else {
                    
                    DocObj.addthumbnail(new ThumbnailObject(pagexmlobj[j],layoutname, DocObj, j + totalpages));

                    if(j < nMaxpages){
                        PageObj = new PageObject(pagexmlobj[j], layoutname, false, '', j + totalpages, DocObj);
                        
                        DocObj.addpage(PageObj, false);
                    }else{
                        DocObj.addpage(undefined, false);
                    }


                }

            }
            totalpages += j;

        }*/

        
        var layoutxmlobj = xmlDoc.getElementsByTagName('Layout');


        var pagexmlobj = xmlDoc.getElementsByTagName('Page');

        for (var j = 0; j < pagexmlobj.length; j++) {
            
            if (j == 0) {
                PageObj = new PageObject(pagexmlobj[j], "Default", true, '', j, DocObj);

                var tempPageXML = pagexmlobj[j];
                var tempLayoutName = "Default";


                DocObj.addthumbnail(new ThumbnailObject(pagexmlobj[j],"Default", DocObj, j));
                //DocObj.addthumbnail(new ThumbnailObject(pagexmlobj[j],layoutname ,DocObj, j));
                DocObj.addpage(PageObj, true);
            } else {
                //PageObj = new PageObject(pagexmlobj[j], "Default", false, '', j, DocObj);
                //DocObj.addpage(PageObj, false);
                DocObj.addthumbnail(new ThumbnailObject(pagexmlobj[j],"Default", DocObj, j));

                if(j < nMaxpages){
                    PageObj = new PageObject(pagexmlobj[j], "Default", false, '', j, DocObj);
                    DocObj.addpage(PageObj, false);
    
                }else{
                    DocObj.addpage(undefined, false);
                }
                
            }
        }


        if(hasDocPage()){

            if(tempPageXML && tempLayoutName){
                DocObj.tempPage = new PageObject(tempPageXML, tempLayoutName, false, '', 0, DocObj);
            }

            DocObj.pages[0].firstpage = true;
            DocObj.bdestroyPDFonClose = true;
            DocObj.setLargePDF(foxitdoc.numpages);

            DocObj.ScrollBarCheck();

            var stateobj = {
                iscompare:documentcompare,
                numOpenFiles:OpenFiles.length,
                isPDF:DocObj.getCurrentpage().usepdfjs,
                is3D:DocObj.getCurrentpage().usevector3Dxml,
                markupOn3D : false,
                is2D:DocObj.getCurrentpage().usevectorxml,
                //numpages:DocObj.pages.length,
                numpages: foxitdoc.numpages,
                currentpage:DocObj.getcurPage(),
                activefile : DocObj.bActive
            };

            var pagingobject = {
                numpages:DocObj.pages.length,
                currentpage:DocObj.getcurPage()
            };
    
            if(RxCore_GUI_Page != undefined){
                RxCore_GUI_Page.pageEvent(pagingobject);
            }
    
            for(var li = 0; li < localfilesPending.length; li++){
                if(localfilesPending[li].filenameonly == foxitdoc.filename){
                    updatelocalDoc(localfilesPending[li], DocObj.fileindex);
                }
            }
            return DocObj.fileindex;    
            
        }else{
            return;
        }
        
        
        
        //DocObj.loadPDFpages();
        
        /*for (i = 0; i < layoutxmlobj.length; i++) {
            var layoutname = layoutxmlobj[i].getElementsByTagName("LayoutName")[0].firstChild.nodeValue;
            DocObj.addlayout(layoutname);
            var pagexmlobj = layoutxmlobj[i].getElementsByTagName('Page');
            
            totalpages += j;

        }*/        
        
        /*if (RxCore_GUI_pagethumbs != undefined) {
            RxCore_GUI_pagethumbs.setThumbnails(DocObj.thumbnails);
        }*/


        /*if (RxCore_GUI_State != undefined) {
            RxCore_GUI_State.setGUIState(stateobj);
        }*/

        /*if (Rxcore_GUI_fileLoadComplete != undefined) {
            //Rxcore_GUI_fileLoadComplete.loadComplete(thispage.DocRef.OriginalURL);
            Rxcore_GUI_fileLoadComplete.loadComplete(DocObj.OriginalURL, DocObj.bActive);
        }*/        
       

        /*if(bIsmobile){
            FreeLicense();
        }*/

        
        /*if (foxitgetOpenDocs() == foxitgetnumDocs()){
            addfoxitdoc();
        }*/
        //localfilesPending.push(openfileobj);
        //updatelocalDoc(openfileobj);

        
        
    }

    function createPDFDirect(xmlDoc, pdfdoc){

        bAbortPageload = false;

        /*if(bIsmobile){
            GetLicense();
        }*/

        if (!bLicenseAquired) {
            return;
        }


        if (RxCore_GUI_HasText != undefined){
            RxCore_GUI_HasText.hastext = false;
            RxCore_GUI_HasText.hasText(RxCore_GUI_HasText.hastext, true);

        }

        cntximg.clearRect(0, 0, canvas.width, canvas.height);
        context.clearRect(0, 0, canvas.width, canvas.height);
        contexto.clearRect(0, 0, canvas.width, canvas.height);

        clearUsers();
        addCurrentUser();


        /*numUsers = 0;
        Userlist = [];
        Userlist[0] = new Users(signature, DisplayName, markuplayer, markupcolor);
        numUsers++;*/
        
        var PageObj;

        if (documentopen && bSingleDocmode) {
            //RxCore_Closedocument();
            internalClosedoc();
         }
 
        if (DocObj == null || DocObj == undefined) {
              nCurdocindex = OpenFiles.push(new DocumentObject(xmlDoc));
              DocObj = OpenFiles[nCurdocindex - 1];
              //DocObj.fileindex = nCurdocindex - 1;
              DocObj.setfileindex(nCurdocindex - 1);
 
         } else {
                     //DocObj.Suspend();
              nCurdocindex = OpenFiles.push(new DocumentObject(xmlDoc));
              
              DocObj.enableTextSelect(false);

              DocObj = OpenFiles[nCurdocindex - 1];
              //DocObj.fileindex = nCurdocindex - 1;
              DocObj.setfileindex(nCurdocindex - 1);
         }

        OpenFiles[nCurdocindex - 1] = DocObj;

        DocObj.setPDFdocument(pdfdoc, false);


        var pagexmlobj = xmlDoc.getElementsByTagName('Page');


        for (var j = 0; j < pagexmlobj.length; j++) {
            
            if (j == 0) {
                PageObj = new PageObject(pagexmlobj[j], "Default", false, '', j, DocObj);

                
                DocObj.addthumbnail(new ThumbnailObject(pagexmlobj[j],"Default", DocObj, j));
                //pagexml, DocRef, pagenum
                DocObj.addpage(PageObj, true);
            } else {
                
                
                DocObj.addthumbnail(new ThumbnailObject(pagexmlobj[j],"Default", DocObj, j));
                
                if(j < nMaxpages){
                    PageObj = new PageObject(pagexmlobj[j], "Default", false, '', j, DocObj);
                    
                    DocObj.addpage(PageObj, false);
                }else{
                    DocObj.addpage(undefined, false);
                }
            }
        }


        if(hasDocPage()){
            DocObj.pages[0].firstpage = true;
            DocObj.bdestroyPDFonClose = true;
            DocObj.loadPDFpages();
            DocObj.directPDF = true;
            
            /*for (i = 0; i < layoutxmlobj.length; i++) {
                var layoutname = layoutxmlobj[i].getElementsByTagName("LayoutName")[0].firstChild.nodeValue;
                DocObj.addlayout(layoutname);
                var pagexmlobj = layoutxmlobj[i].getElementsByTagName('Page');
                
                totalpages += j;
    
            }*/        
            
            /*if (RxCore_GUI_pagethumbs != undefined) {
                RxCore_GUI_pagethumbs.setThumbnails(DocObj.thumbnails);
            }*/
    
            var stateobj = {
                iscompare:documentcompare,
                numOpenFiles:OpenFiles.length,
                isPDF:DocObj.getCurrentpage().usepdfjs,
                is3D:DocObj.getCurrentpage().usevector3Dxml,
                markupOn3D : false,
                is2D:DocObj.getCurrentpage().usevectorxml,
                numpages:DocObj.pages.length,
                currentpage:DocObj.getcurPage(),
                activefile : DocObj.bActive,
                source : 'createPDFDirect'
            };
    
            if (RxCore_GUI_State != undefined) {
                RxCore_GUI_State.setGUIState(stateobj);
            }
    
            var pagingobject = {
                numpages:DocObj.pages.length,
                currentpage:DocObj.getcurPage()
            };
    
            if(RxCore_GUI_Page != undefined){
                RxCore_GUI_Page.pageEvent(pagingobject);
            }
    
            if (Rxcore_GUI_fileLoadComplete != undefined) {
                //Rxcore_GUI_fileLoadComplete.loadComplete(thispage.DocRef.OriginalURL);
                Rxcore_GUI_fileLoadComplete.loadComplete(DocObj.OriginalURL, DocObj.bActive);
            }        
           
            if (RxCore_GUI_Download != undefined) {
                RxCore_GUI_Download.setDownload("hide");
            }
    
    
            var bdocupdated = false;
            for(var li = 0; li < localfilesPending.length; li++){
                if(localfilesPending[li].filenameonly == DocObj.FileName.toLowerCase()){
                    
                    updatelocalDoc(localfilesPending[li], DocObj.fileindex);
                    bdocupdated = true;
    
                    break;
                    
                }
            }
            //DocObj
            if(bdocupdated){
                localfilesPending.splice(li, 1);
            }
        }

        /*if(bIsmobile){
            FreeLicense();
        }*/


    }

    function createCompare(pagebg, pageov, mode){

        

        //Only vectors for now until it works.
        var bgis3D = (pagebg.usevector3DGLTF || pagebg.usevector3Dbinary || pagebg.usevector3Dxml);
        var fgis3D = (pageov.usevector3DGLTF || pageov.usevector3Dbinary || pageov.usevector3Dxml);

        if(bgis3D || fgis3D){
            //no support for 3D overlay compare
            return;
        }

        /*if(pagebg.usepdf || pageov.usepdfjs){
            //handle later
            return;
        }*/

        if(pagebg.usevectorbinarycompare || pageov.usevectorbinarycompare){
            //page already a compare
            return;
        }

        //if we get here only vector pages or images

        var bgisimage = (!pagebg.usevectorbinary && !pagebg.usevectorxml);
        var fgisimage = (!pageov.usevectorbinary && !pageov.usevectorxml);


        //isPDF:DocObj.getCurrentpage().usepdfjs,
        var bgispdf = (pagebg.usepdfjs);

        var fgispdf = (pageov.usepdfjs);


        //usevectorbinary : false  : false usevectorxml : false 


        var originalURLbg = pagebg.DocRef.OriginalURL;
        var originalURLfg = pageov.DocRef.OriginalURL;
        var originalURL = originalURLbg + " " + originalURLfg;

        var FileNamesrcbg = pagebg.DocRef.FileNameSRC;
        var FileNamesrcfg = pageov.DocRef.FileNameSRC;
        var FileNameSRC = FileNamesrcbg + " " + FileNamesrcfg;

        var FileNamebg = pagebg.DocRef.FileNameSRC;
        var FileNamefg = pageov.DocRef.FileNameSRC;

        var bgdocindex = pagebg.DocRef.fileindex;
        var bgpageindex = pagebg.pagenumber;

        var fgdocindex = pageov.DocRef.fileindex;
        var fgpageindex = pageov.pagenumber;


        //var FileName = FileNamebg + " " + FileNamefg;
        var FileName = "Compare";

        //if(pagebg.usevector)

        //var bgbinsource = pagebg.Vector2DBinarySRC;
        //var ovrlbinsource = pageov.Vector2DBinarySRC;

        var bgbinsource = decodeURI(pagebg.Vector2DBinarySRC);
        var ovrlbinsource = decodeURI(pageov.Vector2DBinarySRC);


        if (bgisimage){
            var bgimageourceMain = pagebg.MainImageSRC;
            var bgimageourceSmall = pagebg.SmallImageSRC;

            var bgmainimagewidth = pagebg.MainImageWidth;
            var bgmainimageheight = pagebg.MainImageHeight;
            
            var bgmainimagescaling = pagebg.MainImageScaling;

            var bgsmallimagewidth = pagebg.SmallImageWidth;
            var bgsmallimageheight = pagebg.SmallImageHeight;

            var bgsmallimagescaling = pagebg.SmallImageScaling;
            
        }

        if (fgisimage){
            var fgimageourceMain = pageov.MainImageSRC;
            var fgimageourceSmall = pageov.SmallImageSRC;


            var fggmainimagewidth = pageov.MainImageWidth;
            var fgmainimageheight = pageov.MainImageHeight;
            
            var fgmainimagescaling = pageov.MainImageScaling;

            var fgsmallimagewidth = pageov.SmallImageWidth;
            var fgsmallimageheight = pageov.SmallImageHeight;

            var fgsmallimagescaling = pageov.SmallImageScaling;


        }

        if(bgispdf){

            var bgPDFwidth = pagebg.originalPDFwidth;
            var bgPDFheight = pagebg.originalPDFheight;

        }


        if(fgispdf){
    
            var fgPDFwidth = pageov.originalPDFwidth;
            var fgPDFheight = pageov.originalPDFheight;


        }



        //this.MainImageSRC = encodeURI(relpath + pagexml.getElementsByTagName("MainImageSRC")[0].firstChild.nodeValue);
        //this.SmallImageSRC = encodeURI(relpath + pagexml.getElementsByTagName("SmallImageSRC")[0].firstChild.nodeValue);
        //this.SmallImageWidth = pagexml.getElementsByTagName("SmallImageWidth")[0].firstChild.nodeValue;
        //this.SmallImageHeight = pagexml.getElementsByTagName("SmallImageHeight")[0].firstChild.nodeValue;
        //this.SmallImageScaling = pagexml.getElementsByTagName("SmallImageScaling")[0].firstChild.nodeValue;
        //this.MainImageWidth = pagexml.getElementsByTagName("MainImageWidth")[0].firstChild.nodeValue;
        //this.MainImageHeight = pagexml.getElementsByTagName("MainImageHeight")[0].firstChild.nodeValue;
        //this.MainImageScaling = pagexml.getElementsByTagName("MainImageScaling")[0].firstChild.nodeValue;
        //this.MainImageOffsetX = pagexml.getElementsByTagName("MainImageOffsetX")[0].firstChild.nodeValue;
        //this.MainImageOffsetY = pagexml.getElementsByTagName("MainImageOffsetY")[0].firstChild.nodeValue;


        var originalURL = originalURLbg + " " + originalURLfg;



        //docobj..FileNameSRC



        //var bgbinsource = pagebg.Vector2DBinarySRC;
        //var fgbinsource = pageov.Vector2DBinarySRC;
        
        var docobjxmltxt = "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n";
        docobjxmltxt += "<RxViewServer>\n";
        docobjxmltxt += "<NumLayouts>1</NumLayouts>\n";
        docobjxmltxt += "<Version>1</Version>\n";
        docobjxmltxt += "<Format>Compare</Format>\n";
        docobjxmltxt += "<FileName>" + FileName + "</FileName>\n";

        docobjxmltxt += "<OriginalURL>" + originalURL + "</OriginalURL>\n";
        docobjxmltxt += "<FileNameSRC>" + FileNameSRC + "</FileNameSRC>\n";

        docobjxmltxt += "<Filter>Composite</Filter>\n";
        docobjxmltxt += "<FileSizeLow>89192</FileSizeLow>\n";
        docobjxmltxt += "<FileSizeHigh>0</FileSizeHigh>\n";
        docobjxmltxt += "<Type>5</Type>\n";
        docobjxmltxt += "<Layout>\n";
        docobjxmltxt += "<LayoutName>Default</LayoutName>\n";
        docobjxmltxt += "</Layout>\n";
        docobjxmltxt += "</RxViewServer>\n";

        var docobjxml = parseXML(docobjxmltxt);

        var numpagenode = docobjxml.createElement("NumPages");

        var pageNode = docobjxml.createElement("Page");

        var szpagenum = docobjxml.createTextNode("1");
        numpagenode.appendChild(szpagenum);

        docobjxml.getElementsByTagName("RxViewServer")[0].appendChild(numpagenode);

        createAndAppendElement(pageNode, "PageName", "Default");
        createAndAppendElement(pageNode, "Compression", "---");
        createAndAppendElement(pageNode, "DPI", "0.02540000");
        createAndAppendElement(pageNode, "OffsetX", "0.00000");
        createAndAppendElement(pageNode, "OffsetY", "0.00000");
        createAndAppendElement(pageNode, "OriginalScale", "1.00000");

        createAndAppendElement(pageNode, "OriginalScale", "1.00000");


        createAndAppendElement(pageNode, "BckGrndDocindex", bgdocindex);
        createAndAppendElement(pageNode, "BckGrndPageindex", bgpageindex);


        createAndAppendElement(pageNode, "FrGrndDocindex", fgdocindex);
        createAndAppendElement(pageNode, "FrGrndPageindex", fgpageindex);

        createAndAppendElement(pageNode, "OverlayComparemode", mode);

        

        //var bgbinsource = pagebg.Vector2DBinarySRC;
        //var ovrlbinsource = pageov.Vector2DBinarySRC;


        if(bgbinsource != undefined){
            createAndAppendElement(pageNode, "Vector2DBinarySRCBckGrnd", bgbinsource);
        }
        
        if(ovrlbinsource != undefined){
            createAndAppendElement(pageNode, "Vector2DBinarySRCFrGrnd", ovrlbinsource);
        }

        




        /*if (bgisimage){
            bgimageourceMain = pagebg.MainImageSRC;
            bgimageourceSmall = pagebg.SmallImageSRC;
            
        }

        if (fgisimage){
            fgimageourceMain = pagebg.MainImageSRC;
            fgimageourceSmall = pagebg.SmallImageSRC;
            
        }*/

        

        //createAndAppendElement(pageNode, "VectorGLTF3DSRC", URL);
        //<VectorBinary3DSRC>/cache/ac14-fzk-haus-3f4b12-506b5070/1_1.rx3b</VectorBinary3DSRC>


        if(bgimageourceMain){
            createAndAppendElement(pageNode, "Vector2DImageSRCBckGrnd", bgimageourceMain);
            if(bgimageourceSmall){
                createAndAppendElement(pageNode, "Vector2DImageSRCBckGrndSmall", bgimageourceSmall);

            }
        }

        if(fgimageourceMain){
            createAndAppendElement(pageNode, "Vector2DImageSRCFrGrnd", fgimageourceMain);

            if(fgimageourceSmall){
                createAndAppendElement(pageNode, "Vector2DImageSRCFrGrndSmall", fgimageourceSmall);

            }

        }
        
        
        if (bgisimage){

            

            createAndAppendElement(pageNode, "MainImageWidthBckGrnd", bgmainimagewidth);
            createAndAppendElement(pageNode, "MainImageHeightBckGrnd", bgmainimageheight);
            
            createAndAppendElement(pageNode, "MainImageScalingBckGrnd", bgmainimagescaling);
    
            createAndAppendElement(pageNode, "MainImageOffsetXBckGrnd", "0.00000000");
            createAndAppendElement(pageNode, "MainImageOffsetYBckGrnd", "0.00000000");

            createAndAppendElement(pageNode, "SmallImageWidthBckGrnd", bgmainimagewidth);
            createAndAppendElement(pageNode, "SmallImageHeightBckGrnd", bgmainimageheight);
            
    
        }

        if (fgisimage){
            

            createAndAppendElement(pageNode, "MainImageWidthFrGrnd", fggmainimagewidth);
            createAndAppendElement(pageNode, "MainImageHeightFrGrnd", fgmainimageheight);
            
            createAndAppendElement(pageNode, "MainImageScalingFrGrnd", fgmainimagescaling);
    
            createAndAppendElement(pageNode, "MainImageOffsetXFrGrnd", "0.00000000");
            createAndAppendElement(pageNode, "MainImageOffsetYFrGrnd", "0.00000000");

            createAndAppendElement(pageNode, "SmallImageWidthFrGrnd", fggmainimagewidth);
            createAndAppendElement(pageNode, "SmallImageHeightFrGrnd", fgmainimageheight);


        }

        if(bgispdf){
            createAndAppendElement(pageNode, "PDFBckGrnd", fggmainimagewidth);


            createAndAppendElement(pageNode, "PDFWidthBckGrnd", bgPDFwidth);
            createAndAppendElement(pageNode, "PDFHeightBckGrnd", bgPDFheight);




        }
        if(fgispdf){
            createAndAppendElement(pageNode, "PDFFrGrnd", fggmainimagewidth);


            createAndAppendElement(pageNode, "PDFWidthFrGrnd", fgPDFwidth);
            createAndAppendElement(pageNode, "PDFHeightFrGrnd", fgPDFheight);

    



        }





        docobjxml.getElementsByTagName("Layout")[0].appendChild(pageNode);
        
        createCompareXML(docobjxml);

        //createGLTFDirect(docobjxml, URL);



    }


    function readGLTF(URL){

        var docobjxmltxt = "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n";
        docobjxmltxt += "<RxViewServer>\n";
        docobjxmltxt += "<NumLayouts>1</NumLayouts>\n";
        docobjxmltxt += "<NumLayouts>1</NumLayouts>\n";
        docobjxmltxt += "<Version>1</Version>\n";
        docobjxmltxt += "<Format>IFC 2X3</Format>\n";
        docobjxmltxt += "<FileName>" + URL + "</FileName>\n";
        docobjxmltxt += "<OriginalURL>" + URL + "</OriginalURL>\n";
        docobjxmltxt += "<FileNameSRC>" + URL + "</FileNameSRC>\n";
        docobjxmltxt += "<Filter>RxFilterIFC</Filter>\n";
        docobjxmltxt += "<FileSizeLow>89192</FileSizeLow>\n";
        docobjxmltxt += "<FileSizeHigh>0</FileSizeHigh>\n";
        docobjxmltxt += "<Type>4</Type>\n";
        docobjxmltxt += "<Layout>\n";
        docobjxmltxt += "<LayoutName>Default</LayoutName>\n";
        docobjxmltxt += "</Layout>\n";
        docobjxmltxt += "</RxViewServer>\n";

        var docobjxml = parseXML(docobjxmltxt);

        var numpagenode = docobjxml.createElement("NumPages");

        var pageNode = docobjxml.createElement("Page");

        var szpagenum = docobjxml.createTextNode("1");
        numpagenode.appendChild(szpagenum);

        docobjxml.getElementsByTagName("RxViewServer")[0].appendChild(numpagenode);

        createAndAppendElement(pageNode, "PageName", "Default");
        createAndAppendElement(pageNode, "Compression", "---");
        createAndAppendElement(pageNode, "DPI", "0.02540000");
        createAndAppendElement(pageNode, "OffsetX", "0.00000");
        createAndAppendElement(pageNode, "OffsetY", "0.00000");
        createAndAppendElement(pageNode, "OriginalScale", "1.00000");
        createAndAppendElement(pageNode, "VectorGLTF3DSRC", URL);
        //<VectorBinary3DSRC>/cache/ac14-fzk-haus-3f4b12-506b5070/1_1.rx3b</VectorBinary3DSRC>
        
        createAndAppendElement(pageNode, "MainImageWidth", "0");
        createAndAppendElement(pageNode, "MainImageHeight", "0");
        
        createAndAppendElement(pageNode, "MainImageScaling", "1.00000000");

        createAndAppendElement(pageNode, "MainImageOffsetX", "0.00000000");
        createAndAppendElement(pageNode, "MainImageOffsetY", "0.00000000");


        docobjxml.getElementsByTagName("Layout")[0].appendChild(pageNode);
        
        createGLTFDirect(docobjxml, URL);

    };    
    function readPDFDirect(file){
        var fileReader = new FileReader();
        
        var docobjxmltxt = "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n";
        docobjxmltxt += "<RxViewServer>\n";
        docobjxmltxt += "<NumLayouts>1</NumLayouts>\n";
        docobjxmltxt += "<Version>1</Version>\n";
        docobjxmltxt += "<Format>Adobe PDF 1.2</Format>\n";
        docobjxmltxt += "<FileName>" + file.name + "</FileName>\n";
        docobjxmltxt += "<OriginalURL>" + file.name + "</OriginalURL>\n";
        docobjxmltxt += "<FileNameSRC>" + file.name + "</FileNameSRC>\n";
        docobjxmltxt += "<Filter>RxFilterDynaPDF</Filter>\n";
        docobjxmltxt += "<FileSizeLow>89192</FileSizeLow>\n";
        docobjxmltxt += "<FileSizeHigh>0</FileSizeHigh>\n";
        docobjxmltxt += "<Type>0</Type>\n";
        docobjxmltxt += "<Layout>\n";
        docobjxmltxt += "<LayoutName>Default</LayoutName>\n";
        docobjxmltxt += "</Layout>\n";
        docobjxmltxt += "</RxViewServer>\n";

        //this.FileName = xmlDoc.getElementsByTagName('FileName')[0].firstChild.nodeValue;

        var docobjxml = parseXML(docobjxmltxt);
        var numpagenode = docobjxml.createElement("NumPages");


        fileReader.onloadend = function (e) {
            var arrayBuffer = e.target.result;
            

            pdfjsLib.cMapUrl = RxConfig.PDFcmap;
            pdfjsLib.cMapPacked = true;

            var loadingTask = pdfjsLib.getDocument({data : new Uint8Array(arrayBuffer)});
            
            loadingTask.promise.then(function(pdf) {
              // you can now use *pdf* here
              var numpages = pdf._pdfInfo.numPages;
                
        
                var szpagenum = docobjxml.createTextNode(numpages.toString());
                numpagenode.appendChild(szpagenum);
                docobjxml.getElementsByTagName("RxViewServer")[0].appendChild(numpagenode);

                //docobjxmltxt += "<NumPages>" + numpages + "</NumPages>\n";
                
                
                for (var pc = 0; pc < numpages; pc++){
                    
                    var pageNode = docobjxml.createElement("Page");
                    var pgnum = pc + 1;
                    createAndAppendElement(pageNode, "PageName", "Page" + pgnum);
                    createAndAppendElement(pageNode, "Compression", "---");
                    createAndAppendElement(pageNode, "DPI", "600");
                    createAndAppendElement(pageNode, "OffsetX", "0.00000");
                    createAndAppendElement(pageNode, "OffsetY", "0.00000");
                    createAndAppendElement(pageNode, "OriginalScale", "1.00000");
                    
                    createAndAppendElement(pageNode, "MainImageWidth", "0");
                    createAndAppendElement(pageNode, "MainImageHeight", "0");
                    
                    createAndAppendElement(pageNode, "MainImageScaling", "1.00000000");

                    createAndAppendElement(pageNode, "MainImageOffsetX", "0.00000000");
                    createAndAppendElement(pageNode, "MainImageOffsetY", "0.00000000");

                    //<ThumbnailImageSRC>/cache/02.03.007_t01_writer-15c68-4f2d68cf/1_1T.PNG</ThumbnailImageSRC>
                    //<MainImageScaling>1.00000000</MainImageScaling>
                    //<MainImageOffsetX>0.00000000</MainImageOffsetX>
                    //<MainImageOffsetY>0.00000000</MainImageOffsetY>
                    //<ThumbnailWidth>114</ThumbnailWidth>
                    //<//ThumbnailHeight>160</ThumbnailHeight>                            

                    docobjxml.getElementsByTagName("Layout")[0].appendChild(pageNode);

                }
                    
                


                createPDFDirect(docobjxml, pdf);
            });

            /*pdfjsLib.getDocument({data : arrayBuffer}).then(function (pdfDoc_) {
                
                var numpages = pdfDoc_._pdfInfo.numPages;
                
        
                var szpagenum = docobjxml.createTextNode(numpages.toString());
                numpagenode.appendChild(szpagenum);
                docobjxml.getElementsByTagName("RxViewServer")[0].appendChild(numpagenode);

                //docobjxmltxt += "<NumPages>" + numpages + "</NumPages>\n";
                
                
                for (var pc = 0; pc < numpages; pc++){
                    
                    var pageNode = docobjxml.createElement("Page");
                    var pgnum = pc + 1;
                    createAndAppendElement(pageNode, "PageName", "Page" + pgnum);
                    createAndAppendElement(pageNode, "Compression", "---");
                    createAndAppendElement(pageNode, "DPI", "600");
                    createAndAppendElement(pageNode, "OffsetX", "0.00000");
                    createAndAppendElement(pageNode, "OffsetY", "0.00000");
                    createAndAppendElement(pageNode, "OriginalScale", "1.00000");
                    
                    createAndAppendElement(pageNode, "MainImageWidth", "0");
                    createAndAppendElement(pageNode, "MainImageHeight", "0");
                    
                    createAndAppendElement(pageNode, "MainImageScaling", "1.00000000");

                    createAndAppendElement(pageNode, "MainImageOffsetX", "0.00000000");
                    createAndAppendElement(pageNode, "MainImageOffsetY", "0.00000000");

                    //<ThumbnailImageSRC>/cache/02.03.007_t01_writer-15c68-4f2d68cf/1_1T.PNG</ThumbnailImageSRC>
                    //<MainImageScaling>1.00000000</MainImageScaling>
                    //<MainImageOffsetX>0.00000000</MainImageOffsetX>
                    //<MainImageOffsetY>0.00000000</MainImageOffsetY>
                    //<ThumbnailWidth>114</ThumbnailWidth>
                    //<//ThumbnailHeight>160</ThumbnailHeight>                            

                    docobjxml.getElementsByTagName("Layout")[0].appendChild(pageNode);

                }
                    
                


                createPDFDirect(docobjxml, pdfDoc_);



                
                //thisdocument.pdfDoc = pdfDoc_;

                //thisdocument.pdfFindController = new PDFJS.PDFFindController();
                // Initial/first page rendering

                //thisdocument.document.setPDFdocument(thisdocument.pdfDoc, true);

                
                
            });*/

        };

        fileReader.readAsArrayBuffer(file);



    }


    /*function quickPage(foxitdoc){


    }*/

    const sendHttpRequestwevents = function(method, url, rtype, eventlist, data){

        const xhr = new XMLHttpRequest();

        xhr.withCredentials = bUseCredentials;

        /*var eventlist = [
            {event : "progress", func : uploadPDFSilent},
            {event : "load", func : uploadPDFSilentComplete},
            {event : "error", func : uploadFailed},
            {event : "abort", func : uploadCanceled}
        ];*/

        for(var ei = 0; ei < eventlist.length; ei++ ){
            
            if(eventlist[ei].event == "progress" && eventlist[ei].bupload){
                xhr.upload.addEventListener(eventlist[ei].event, eventlist[ei].func, false);      
            }else{
                xhr.addEventListener(eventlist[ei].event, eventlist[ei].func, false);      
            }

        }

        try {
            xhr.open(method,url);
        } catch (e) {
            reject("Error 1 - " + e);
        }

        try {
            xhr.responseType = rtype;
        } catch (e) {
            reject("Error 2 - " + e);
        }

        if(buseAuth){
            var auth = getAuth();

            if (auth.authset){
                xhr.setRequestHeader('Authorization', auth.authString + auth.baseuserstring);
            }
            
        }

        if(data){
            xhr.send(data);
        }else{
            xhr.send();
        }

        //xhr.upload.addEventListener("progress", uploadPDFSilent, false);
        //xhr.addEventListener("load", uploadPDFSilentComplete, false);
        //xhr.addEventListener("error", uploadFailed, false);
        //xhr.addEventListener("abort", uploadCanceled, false);

    }

    function getAuth(){

        var auth = {authString : "", baseuserstring : "", authset : false};

        if(buseAuth){

            auth.authString = AuthType + " ";

            if (buseEncToken){

                auth.baseuserstring = AuthToken;
                auth.authset = true;

            }else{
                
                var userString = AuthName;
                userString += ":";
                userString += AuthPass;
                auth.baseuserstring = window.btoa(userString);
                auth.authset = true;
            }
            
        }

        return auth;
    }

    const sendHttpRequestXML = function(method, url, data, callbacklist){
        const xhrpromise = new Promise(function(resolve,reject){
            const xhr = new XMLHttpRequest();

            //callbacklist

            xhr.withCredentials = bUseCredentials;

            try {
                xhr.open(method,url);
            } catch (e) {
                reject("Error 1 - " + e);
            }
            try {
                xhr.responseType = '';
            } catch (e) {
                reject("Error 2 - " + e);
            }


            if(buseAuth){

                var auth = getAuth();

                if (auth.authset){
                    xhr.setRequestHeader('Authorization', auth.authString + auth.baseuserstring);
                }
                
            }

            xhr.onload = function(ev){
                if (this.status == 200) {

                    var xmlDoc = this.responseXML;


                    if (xmlDoc == null || xmlDoc.documentElement == null) {


                        xmlDoc = parseXML(this.responseText);

                        /*XMLParser(this.responseText).then(function (xml){
                            
                            xmlDoc = xml;
                            resolve(xmlDoc);    

                        }).catch(function (error){

                            reject('Unable to get XML');

                        });*/

                        //xmlDoc = $.parseXML(this.responseText).documentElement;
                    }

                    szLatestresponseHeaders = this.getAllResponseHeaders();
                
                    if(RxCore_GUI_getResponsHeaders != undefined){
                        RxCore_GUI_getResponsHeaders.setRespHeaders(szLatestresponseHeaders);
                    }

                    if(xmlDoc == null || xmlDoc.documentElement == null){
                        reject('Unable to get XML');
                    }else{
                        resolve(xmlDoc);
                    }

                }else{
                    reject(this);
                }

                if(callbacklist){
                    for(var i = 0;i < callbacklist.length; i++){
                        if(callbacklist[i].event == "callback"){
                            if (callbacklist[i].func && typeof callbacklist[i].func === "function") {
                                callbacklist[i].func(this);
                            }
                        }
                    }
    
                }


    
            };

            xhr.onerror = function(ev){
                //reject('Something went wrong');
                reject('Failed to connect to ' + url);

            };

            //Download progress
            xhr.addEventListener("progress", function (evt) {
                if (evt.lengthComputable) {
                    var percentComplete = evt.loaded / evt.total;
    
                    if(callbacklist){
                        for(var i = 0;i < callbacklist.length; i++){
                            if(callbacklist[i].event == "progressCallBack"){
                                if (callbacklist[i].func && typeof callbacklist[i].func === "function") {
                                    callbacklist[i].func(percentComplete);
                                }
                            }
                        }
                    }
                }
            }, false);

            xhr.send(data);

        });

        return xhrpromise;
    };
    
    const getRxUniqueID = function(reqheader){
        
        
        const xhrpromise = new Promise(function(resolve,reject){
            const xhr = new XMLHttpRequest();
            
            var XMLGetGUID = "<?xml version=\"1.0\" encoding=\"UTF-8\"?>";
            XMLGetGUID += "<RxViewServer>";
            XMLGetGUID += "<Command>GetGUID</Command>";
            XMLGetGUID += "<LicenseID>" + LicenseID + "</LicenseID>";
            XMLGetGUID += "</RxViewServer>";
    
            
            
            xhr.open('POST', xmlurl);

            if(buseAuth){

                var auth = getAuth();

                if (auth.authset){
                    xhr.setRequestHeader('Authorization', auth.authString + auth.baseuserstring);
                }
                
            }

            try {
                xhr.responseType = '';
            } catch (e) {
                reject("Error 2 - " + e);
            }

            if(reqheader){
                xhr.setRequestHeader(reqheader.name, reqheader.value);
            }
            
            xhr.onload = function(ev){
                if (this.status == 200) {
                    resolve(xhr.response);
                }else{
                    reject(this);
                }
            };

            xhr.onerror = function(ev){
                reject('Something went wrong');

            };

            xhr.send(XMLGetGUID);



        });

        return xhrpromise;
    }

    const sendHttpRequest = function(method, url, rtype, data, reqheader){

        const xhrpromise = new Promise(function(resolve,reject){
            const xhr = new XMLHttpRequest();
            
            xhr.open(method,url);

            if(buseAuth){

                var auth = getAuth();

                if (auth.authset){
                    xhr.setRequestHeader('Authorization', auth.authString + auth.baseuserstring);
                }
                
            }

            try {
                xhr.responseType = rtype;
            } catch (e) {
                reject("Error 2 - " + e);
            }

            if(reqheader){
                xhr.setRequestHeader(reqheader.name, reqheader.value);
            }
            
            xhr.onload = function(ev){
                if (this.status == 200) {
                    resolve(xhr.response);
                }else{
                    reject(this);
                }
            };

            xhr.onerror = function(ev){
                reject('Something went wrong');

            };


            if(data){
                xhr.send(data);
            }else{
                xhr.send();
            }
            
        });

        return xhrpromise;

    };


    function downloadSignature(username, binitial){

        if(signatureGet == undefined){
            return;
        }

        if(binitial && initialGet == undefined){
            return;
        }



        var downloadURL = signatureGet + '&' + username; 

        if(binitial){
            downloadURL = initialGet + '&' + username; 
        }

        
        sendHttpRequest('GET', downloadURL,"arraybuffer").then(function (response){
                    
            //var url = URL.createObjectURL(response);

            var singaturePNGData = new Blob([response], {type: "image/png"});

            if (response) {

                const img = new Image();
                img.src = URL.createObjectURL(singaturePNGData);
                
                img.onload = function() {

                  //console.log(img.width, img.height)    

                  var signatureinfo = { data : singaturePNGData, width : img.width, height : img.height,  username : username, initials : binitial};

                  if (RxCore_GUI_getsignatureComplete != undefined){
                      RxCore_GUI_getsignatureComplete.onSignReceived(signatureinfo);
                  }
  
                }

    
            }
        
        }).catch(function(error){

            if(error.status){
                console.log("signatureload", error.status);
            }else{
                console.log("signatureload", error);
            }
            
        });


    }

    function uploadSignature(image, username, binitial){

        if(signaturePut == undefined){
            return;
        }

        if(binitial && initialPut == undefined){
            return;
        }


        var uploadURL = signaturePut + '&' + username; 

        if(binitial){
            uploadURL = initialPut + '&' + username; 
        }
        

        var cdata = getBase64Image(image)
        //var singaturePNGData = new Blob([image], {type: "image/png"});


        sendHttpRequest('POST', uploadURL, '', cdata).then(function (response){


            //var newresponse = response.substr(0,response.length -2);
            //response.trim()

            //var penTableJSON = Object.freeze(JSON.parse(newresponse));

            /*if (RxCore_GUI_Upload != undefined) {
                RxCore_GUI_Upload.setUpload("hide");
            }*/
    
            if (RxCore_GUI_putsignatureComplete != undefined){
                RxCore_GUI_putsignatureComplete.oncomplete(username, binitial);
            }

            


        }).catch(function(error){
           
            if(error.status){
                
                console.log("uploadSignature", error.status);
            }else{

                console.log("uploadSignature", error);
            }

        });

        
    }

    function loadPenTable(file){


        if(PenTableFile == undefined){
            return;
        }

        //D:\\Support\\Plotstil_bis_2019.ct

        //var file = document.getElementById('fileToUpload').files[0];

        /*if (RxCore_GUI_Upload != undefined) {
            RxCore_GUI_Upload.setUpload("show");
        }*/
        
        var rtype = '';

        var penurl = PenTableFile + "&" + file;

        //var PenTableFile = RxConfig.PenTableFile;

        //PenTableFile

        sendHttpRequest('GET', penurl, rtype).then(function (response){


            var newresponse = response.substr(0,response.length -2);
            //response.trim()

            DefaultpentableJSON = Object.freeze(JSON.parse(newresponse));

            /*if (RxCore_GUI_Upload != undefined) {
                RxCore_GUI_Upload.setUpload("hide");
            }*/
        

            //save internal pen table default structure here.

            if (RxCore_GUI_penTableJSON != undefined){
                RxCore_GUI_penTableJSON.setPentablelist(DefaultpentableJSON, true);
            }

            


        }).catch(function(error){
           
            if(error.status){
                
                /*if (RxCore_GUI_Upload != undefined) {
                    RxCore_GUI_Upload.setUpload("hide");
                }*/
    
                console.log("defaultpentable", error.status);
            }else{

                /*if (RxCore_GUI_Upload != undefined) {
                    RxCore_GUI_Upload.setUpload("hide");
                }*/

                console.log(error);
            }

        });

    }    

    function uploadPenTable(){


        if(PenTableUpload == undefined){
            return;
        }

        var file = document.getElementById('fileToUpload').files[0];

        if (RxCore_GUI_Upload != undefined) {
            RxCore_GUI_Upload.setUpload("show");
        }
        
        var rtype = '';
        

        sendHttpRequest('POST', PenTableUpload, rtype, file).then(function (response){


            var newresponse = response.substr(0,response.length -2);
            //response.trim()

            var penTableJSON = Object.freeze(JSON.parse(newresponse));

            if (RxCore_GUI_Upload != undefined) {
                RxCore_GUI_Upload.setUpload("hide");
            }
    

    

            if (RxCore_GUI_penTableJSON != undefined){
                RxCore_GUI_penTableJSON.setPentablelist(penTableJSON, DocObj.bActive);
            }

            


        }).catch(function(error){
           
            if(error.status){
                
                if (RxCore_GUI_Upload != undefined) {
                    RxCore_GUI_Upload.setUpload("hide");
                }
    
                console.log("uploadpentable", error.status);
            }else{

                if (RxCore_GUI_Upload != undefined) {
                    RxCore_GUI_Upload.setUpload("hide");
                }

                console.log(error);
            }

        });


    }


    function createFoxitDoc(foxitdoc){
        
        //createFoxitDoc

        //var urlfilename = urlencode
        var urlfilename = encodeURIComponent(foxitdoc.filename);
        //this.filename = '';
        var docobjxmltxt = "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n";
        docobjxmltxt += "<RxViewServer>\n";
        docobjxmltxt += "<NumLayouts>1</NumLayouts>\n";
        docobjxmltxt += "<Version>1</Version>\n";
        docobjxmltxt += "<Format>Adobe PDF 1.2</Format>\n";
        docobjxmltxt += "<FileName>" + urlfilename + "</FileName>\n";
        docobjxmltxt += "<OriginalURL>" + urlfilename + "</OriginalURL>\n";
        docobjxmltxt += "<FileNameSRC>" + urlfilename + "</FileNameSRC>\n";
        docobjxmltxt += "<FileLocal>" + "1" + "</FileLocal>\n";
        docobjxmltxt += "<Filter>RxFilterDynaPDF</Filter>\n";
        docobjxmltxt += "<FileSizeLow>89192</FileSizeLow>\n";
        docobjxmltxt += "<FileSizeHigh>0</FileSizeHigh>\n";
        docobjxmltxt += "<Type>0</Type>\n";
        docobjxmltxt += "<Layout>\n";
        docobjxmltxt += "<LayoutName>Default</LayoutName>\n";
        docobjxmltxt += "</Layout>\n";
        docobjxmltxt += "</RxViewServer>\n";

        

        var docobjxml = parseXML(docobjxmltxt);
        
        
        var numpagenode = docobjxml.createElement("NumPages");


        var szpagenum = docobjxml.createTextNode(foxitdoc.numpages.toString());
        numpagenode.appendChild(szpagenum);
        
        docobjxml.getElementsByTagName("RxViewServer")[0].appendChild(numpagenode);



        for (var pc = 0; pc < foxitdoc.numpages; pc++){
                    
            var pageNode = docobjxml.createElement("Page");
            var pgnum = pc + 1;
            createAndAppendElement(pageNode, "PageName", "Page " + pgnum);
            createAndAppendElement(pageNode, "Compression", "---");
            createAndAppendElement(pageNode, "DPI", "600");
            createAndAppendElement(pageNode, "OffsetX", "0.00000");
            createAndAppendElement(pageNode, "OffsetY", "0.00000");
            createAndAppendElement(pageNode, "OriginalScale", "1.00000");
            
            createAndAppendElement(pageNode, "MainImageWidth", "0");
            createAndAppendElement(pageNode, "MainImageHeight", "0");
            
            createAndAppendElement(pageNode, "MainImageScaling", "1.00000000");

            createAndAppendElement(pageNode, "MainImageOffsetX", "0.00000000");
            createAndAppendElement(pageNode, "MainImageOffsetY", "0.00000000");

            //<ThumbnailImageSRC>/cache/02.03.007_t01_writer-15c68-4f2d68cf/1_1T.PNG</ThumbnailImageSRC>
            //<MainImageScaling>1.00000000</MainImageScaling>
            //<MainImageOffsetX>0.00000000</MainImageOffsetX>
            //<MainImageOffsetY>0.00000000</MainImageOffsetY>
            //<ThumbnailWidth>114</ThumbnailWidth>
            //<//ThumbnailHeight>160</ThumbnailHeight>                            

            docobjxml.getElementsByTagName("Layout")[0].appendChild(pageNode);

        }

        //createPDFDirect(docobjxml, pdf);
        
        if (RxCore_GUI_Download != undefined) {
            RxCore_GUI_Download.setDownload("hide");
        }


        return createFoxitPDF(docobjxml, foxitdoc);

    }
    
    function uploadPDFCustom(uploadPDFfile, endpoint){

        var xhr = new XMLHttpRequest();
        xhr.withCredentials = bUseCredentials;

        
        xhr.upload.addEventListener("progress", uploadPDFCustomProgress, false);
        xhr.addEventListener("load", uploadCustomPDFComplete, false);
        xhr.addEventListener("error", uploadFailed, false);
        xhr.addEventListener("abort", uploadCanceled, false);
        //var urifilename = encodeURI(file.name);

        var urifilename = encodeURIComponent(uploadPDFfile.filename);
        const filetime = Date.now();

        var path = getPath(DocObj.OriginalURL);
        var file = getFileName(DocObj.OriginalURL);

        file = "new_" + file;

        var cacheid = "&" + path + "&" + file;
        
        xhr.open("POST", endpoint, true);

        //xhr.open("POST", FileuploadURL + "&" + UploadServerfolder + "&" + urifilename + "&" + file.lastModified + "&" + file.size, true);
        
        xhr.send(uploadPDFfile.binfile);

    }
    
    function uploadPDF(uploadPDFfile){

        //RxCore.uploadCustomPDF(uploadPDF{filename : uploadname, binfile : blob});

        var xhr = new XMLHttpRequest();
        xhr.withCredentials = bUseCredentials;

        
        xhr.upload.addEventListener("progress", uploadPDFCustomProgress, false);
        xhr.addEventListener("load", uploadCustomPDFComplete, false);
        xhr.addEventListener("error", uploadFailed, false);
        xhr.addEventListener("abort", uploadCanceled, false);
        //var urifilename = encodeURI(file.name);

        var urifilename = encodeURIComponent(uploadPDFfile.filename);
        const filetime = Date.now();

        var path = getPath(DocObj.OriginalURL);
        var file = getFileName(DocObj.OriginalURL);

        file = "new_" + file;

        var cacheid = "&" + path + "&" + file;

        
        xhr.open("POST", WebClientPDFUploadURL + cacheid, true);
        //xhr.open("POST", FileuploadURL + "&" + UploadServerfolder + "&" + urifilename + "&" + file.lastModified + "&" + file.size, true);
        
        xhr.send(uploadPDFfile.binfile);

        

    }

    function uploadFile() {
        //var fd = new FormData();
        var file = document.getElementById('fileToUpload').files[0];

        uploadTempFile = file;

        if (file.type == "application/pdf"){
            
            
            //intitFoxit();

            //var numdocs = foxitgetnumDocs();
            //if (numdocs )
            //addfoxitdoc();

            foxitGetDoc().openPDF(file);

            if (RxCore_GUI_Download != undefined) {
                RxCore_GUI_Download.setDownload("show");
            }
    
            
            addfoxitdoc();

            //addfoxitdoc().openPDF(file);

            //openfoxitPDF(file);
            
            //return;
        }

        //fd.append("fileToUpload", file);
        var xhr = new XMLHttpRequest();
        xhr.withCredentials = bUseCredentials;

        if (file.type == "application/pdf"){
            xhr.upload.addEventListener("progress", uploadPDFSilent, false);
            xhr.addEventListener("load", uploadPDFSilentComplete, false);
            xhr.addEventListener("error", uploadFailed, false);
            xhr.addEventListener("abort", uploadCanceled, false);
            //var urifilename = encodeURI(file.name);

            var urifilename = encodeURIComponent(file.name);
            
            //xhr.open("POST", FileuploadURL + "?" + UploadServerfolder + "&" + file.name,true);
            xhr.open("POST", FileuploadURL + "&" + UploadServerfolder + "&" + urifilename + "&" + file.lastModified + "&" + file.size, true);
            xhr.send(file);

        }else{
            if (RxCore_GUI_Upload != undefined) {
                RxCore_GUI_Upload.setUpload("show");
            }
    
    
            xhr.upload.addEventListener("progress", uploadProgress, false);
            xhr.addEventListener("load", uploadComplete, false);
            xhr.addEventListener("error", uploadFailed, false);
            xhr.addEventListener("abort", uploadCanceled, false);
            //var urifilename = encodeURI(file.name);
            urifilename = encodeURIComponent(file.name);
            //xhr.open("POST", FileuploadURL + "?" + UploadServerfolder + "&" + file.name,true);
            xhr.open("POST", FileuploadURL + "&" + UploadServerfolder + "&" + urifilename + "&" + file.lastModified + "&" + file.size, true);
            xhr.send(file);
    
        }


    }

    function uploadPDFCustomProgress(evt){

        if (evt.lengthComputable) {
            //var percentComplete = Math.round(evt.loaded * 100 / evt.total);
            //showUploadDialog();
            if (RxCore_GUI_Upload != undefined) {
                RxCore_GUI_Upload.setUpload(50);
            }

        }

    }

    function uploadPDFSilent(evt){
        //console.log(Math.round(evt.loaded * 100 / evt.total));

        if (evt.lengthComputable) {
            //var percentComplete = Math.round(evt.loaded * 100 / evt.total);
            //showUploadDialog();
            if (RxCore_GUI_Upload != undefined) {
                RxCore_GUI_Upload.setUpload(Math.round(evt.loaded * 100 / evt.total));
            }

        }


    };
    function uploadProgress(evt) {
        if (evt.lengthComputable) {
            //var percentComplete = Math.round(evt.loaded * 100 / evt.total);
            //showUploadDialog();
            if (RxCore_GUI_Upload != undefined) {
                RxCore_GUI_Upload.setUpload(Math.round(evt.loaded * 100 / evt.total));
            }

            /*document.getElementById('progressbar').value = Math.round(evt.loaded * 100 / evt.total); //percentComplete;//.toString() + '%';
            if (document.getElementById('progressbar').value == 100) {
                document.getElementById('progressbar').value = 0;
                //hideUploadDialog();

            }*/
        } else {
            //document.getElementById('progressNumber').innerHTML = 'unable to compute';
        }
    }

    function uploadCustomPDFComplete(evt) {
        /* This event is raised when the server send back a response */
        //hideUploadDialog();


        

        //call standard export here.


        if (RxCore_GUI_Upload != undefined) {
            RxCore_GUI_Upload.setUpload(100);
        }

        //console.log(evt.target.responseText);

        /*var params = {
            consolidateonly : false,
            format : "PDF",
            UPI : "0",
            paperSize : "A3",
            markupFlag : "1",
            overridefilename : evt.target.responseText
        }*/

        defaultexportparams.overridefilename = evt.target.responseText;

        exportfileUpload(defaultexportparams);

        /*if (RxCore_GUI_Upload != undefined) {
            RxCore_GUI_Upload.setUpload("hide");
        }
        if (RxCore_GUI_Download != undefined) {
            RxCore_GUI_Download.setDownload("show");
        }*/

        

        //var urifilename = encodeURI(openfile);
        
        //alert(evt.target.responseText);
    }


    function uploadPDFSilentComplete(evt) {

        /* This event is raised when the server send back a response */

        if(document.getElementById('fileToUpload')){
            var file = document.getElementById('fileToUpload').files[0];
        }else if(uploadTempFile != undefined){
            file = uploadTempFile;
        }

        

        
        
        var urifilename = encodeURI(file.name);
        var openfile = UploadServerfolderd + file.name;
        var openfileobj = {filename: openfile, displayname : null, filenameonly : file.name.toLowerCase()};

        //szFileName = getFileName(Filepath.filename);

        localfilesPending.push(openfileobj);

        if(DocObj){
            for(var li = 0; li < localfilesPending.length; li++){

                //DocObj.FileName.toLowerCase()
                if(localfilesPending[li].filenameonly == file.name.toLowerCase()){
                    updatelocalDoc(localfilesPending[li], DocObj.fileindex);
                }
            }
    
        }




        /*if(DocObj.FileLocal){
        }*/

        //updatelocalDoc(openfileobj);
        
    }

    function uploadPenTableComplete(evt){

        var file = document.getElementById('fileToUpload').files[0];
        var urifilename = encodeURI(file.name);
        var openfile = UploadServerfolderd + file.name;
        //hideUploadDialog();
        if (RxCore_GUI_Upload != undefined) {
            RxCore_GUI_Upload.setUpload("hide");
        }
        if (RxCore_GUI_Download != undefined) {
            RxCore_GUI_Download.setDownload("show");
        }

        
    }    

    function uploadComplete(evt) {
        /* This event is raised when the server send back a response */
        var file = document.getElementById('fileToUpload').files[0];
        var urifilename = encodeURI(file.name);
        var openfile = UploadServerfolderd + file.name;
        //hideUploadDialog();
        if (RxCore_GUI_Upload != undefined) {
            RxCore_GUI_Upload.setUpload("hide");
        }
        if (RxCore_GUI_Download != undefined) {
            RxCore_GUI_Download.setDownload("show");
        }

        //var urifilename = encodeURI(openfile);
        var openfileobj = {filename: openfile, displayname : null};

        getFile(openfileobj);
        
        set_tool('markupedit', {p1 : false});
        
        //alert(evt.target.responseText);
    }

    function uploaddragComplete(evt) {
        /* This event is raised when the server send back a response */
        //var file = evt.dataTransfer.files[0];
        if (dragdropfilename == "") {
            return;
        }
        var urifilename = encodeURI(dragdropfilename);
        var openfile = UploadServerfolderd + dragdropfilename;
        //hideUploadDialog();

        if (RxCore_GUI_Upload != undefined) {
            RxCore_GUI_Upload.setUpload("hide");
        }

        if (RxCore_GUI_Download != undefined) {
            RxCore_GUI_Download.setDownload("show");
        }

        //var urifilename = encodeURI(openfile);
        var openfileobj = {filename: openfile, displayname : null};
        getFile(openfileobj);
        
        set_tool('markupedit', {p1 : false});

        dragdropfilename = "";
        //alert(evt.target.responseText);
    }


    function uploadFailed(evt) {
        //hideUploadDialog();
        if (RxCore_GUI_Upload != undefined) {
            RxCore_GUI_Upload.setUpload("hide");
        }

        alert("There was an error attempting to upload the file.");

    }

    function uploadCanceled(evt) {
        //hideUploadDialog();
        if (RxCore_GUI_Upload != undefined) {
            RxCore_GUI_Upload.setUpload("hide");
        }

        alert("The upload has been canceled by the user or the browser dropped the connection.");

    }

    function usePentableUpload(){

        bloadpentable = true;
    }

    function useConvertUpload(){
        buploadconvert = true;
    }


    function fileSelected() {

        if(bloadpentable){
            bloadpentable = false;
            uploadPenTable();

        }else if(buploadconvert){

            buploadconvert = false;
            convertFile();

            
        }else{
            uploadFile();
        }

        
    }

    function touchEvents(ev) {

        if(!bIsmobile){
            return;
        }
        
        ev.preventDefault();

        var posX = 0;
        var posY = 0;
        var moveint = 0;

        if (RxCore_GUI_3DWalkthrough != undefined) {
            var _wheelImg = RxCore_GUI_3DWalkthrough._wheelImg;
            if (_wheelImg != undefined) {
                if (_wheelImg.x == undefined) {
                    var elemRect = _wheelImg.getBoundingClientRect();
                    var _wheelImgX = elemRect.left + 100;
                    var _wheelImgY = elemRect.top + 100;
                } else {
                    _wheelImgX = _wheelImg.x + 100;
                    _wheelImgY = _wheelImg.y + 100;

                }
            }

        } else {
            _wheelImgX = 0;
            _wheelImgY = 0;
        }


        switch (ev.type) {
            case 'mousedown':
                ev.preventDefault();
                posX = ev.pageX;
                posY = ev.pageY;
                moveint = 1;

                getAngleWithXAxis(100, posX - _wheelImgX, -posY + _wheelImgY, moveint);

                break;
            case 'MSPointerDown':
                ev.preventDefault();
                posX = ev.pageX;
                posY = ev.pageY;
                moveint = 1;
                getAngleWithXAxis(100, posX - _wheelImgX, -posY + _wheelImgY, moveint);

                break;
            case 'pointerdown':
                ev.preventDefault();
                posX = ev.pageX;
                posY = ev.pageY;
                moveint = 1;
                getAngleWithXAxis(100, posX - _wheelImgX, -posY + _wheelImgY, moveint);

                break;
            case 'mousemove':
                break;
            case 'touchstart':
                ev.preventDefault();
                posX = ev.targetTouches[0].pageX;
                posY = ev.targetTouches[0].pageY;
                moveint = 1;
                getAngleWithXAxis(100, posX - _wheelImgX, -posY + _wheelImgY, moveint);
                break;
            case 'touchmove':
                break;
            case 'mouseup':
                posX = ev.pageX;
                posY = ev.pageY;
                moveint = 0;
                getAngleWithXAxis(100, posX - _wheelImgX, -posY + _wheelImgY, moveint);
                break;
            case 'MSPointerUp':
                posX = ev.pageX;
                posY = ev.pageY;
                moveint = 0;
                getAngleWithXAxis(100, posX - _wheelImgX, -posY + _wheelImgY, moveint);
                break;
            case 'pointerup':
                posX = ev.pageX;
                posY = ev.pageY;
                moveint = 0;
                getAngleWithXAxis(100, posX - _wheelImgX, -posY + _wheelImgY, moveint);

                break;
            case 'mouseout':

                for (var diri = 1; diri < 7;diri++){
                    if(DocObj.getCurrentpage().walkthroughcontrol.movedirection){
                        DocObj.getCurrentpage().walkthroughcontrol.movedirection(diri,false);
                    }
                
                }   
                /*DocObj.getCurrentpage().walkthroughcontrol.moveForward = false;
                DocObj.getCurrentpage().walkthroughcontrol.moveBackward = false;
                DocObj.getCurrentpage().walkthroughcontrol.moveLeft = false;
                DocObj.getCurrentpage().walkthroughcontrol.moveRight = false;
                DocObj.getCurrentpage().walkthroughcontrol.moveUp = false;
                DocObj.getCurrentpage().walkthroughcontrol.moveDown = false;*/

                moveint = 0;
                break;
            case 'MSPointerOut':

                for (var diri = 1; diri < 7;diri++){
                    if(DocObj.getCurrentpage().walkthroughcontrol.movedirection){
                        DocObj.getCurrentpage().walkthroughcontrol.movedirection(diri,false);
                    }
                
                }   

                /*DocObj.getCurrentpage().walkthroughcontrol.moveForward = false;
                DocObj.getCurrentpage().walkthroughcontrol.moveBackward = false;
                DocObj.getCurrentpage().walkthroughcontrol.moveLeft = false;
                DocObj.getCurrentpage().walkthroughcontrol.moveRight = false;
                DocObj.getCurrentpage().walkthroughcontrol.moveUp = false;
                DocObj.getCurrentpage().walkthroughcontrol.moveDown = false;*/

                moveint = 0;
                break;
            case 'pointerout':

                for (var diri = 1; diri < 7;diri++){
                    if(DocObj.getCurrentpage().walkthroughcontrol.movedirection){
                        DocObj.getCurrentpage().walkthroughcontrol.movedirection(diri,false);
                    }
                
                }   

                /*DocObj.getCurrentpage().walkthroughcontrol.moveForward = false;
                DocObj.getCurrentpage().walkthroughcontrol.moveBackward = false;
                DocObj.getCurrentpage().walkthroughcontrol.moveLeft = false;
                DocObj.getCurrentpage().walkthroughcontrol.moveRight = false;
                DocObj.getCurrentpage().walkthroughcontrol.moveUp = false;
                DocObj.getCurrentpage().walkthroughcontrol.moveDown = false;*/

                moveint = 0;
                break;

            case 'touchcancel':
                ev.preventDefault();

                for (var diri = 1; diri < 7;diri++){
                    if(DocObj.getCurrentpage().walkthroughcontrol.movedirection){
                        DocObj.getCurrentpage().walkthroughcontrol.movedirection(diri,false);
                    }
                
                }   
                /*DocObj.getCurrentpage().walkthroughcontrol.moveForward = false;
                DocObj.getCurrentpage().walkthroughcontrol.moveBackward = false;
                DocObj.getCurrentpage().walkthroughcontrol.moveLeft = false;
                DocObj.getCurrentpage().walkthroughcontrol.moveRight = false;
                DocObj.getCurrentpage().walkthroughcontrol.moveUp = false;
                DocObj.getCurrentpage().walkthroughcontrol.moveDown = false;*/
                moveint = 0;
                break;
            case 'touchend':
                //alert('touchend');
                ev.preventDefault();
                for (var diri = 1; diri < 7;diri++){
                    if(DocObj.getCurrentpage().walkthroughcontrol.movedirection){
                        DocObj.getCurrentpage().walkthroughcontrol.movedirection(diri,false);
                    }
                
                }   
                //posX = ev.targetTouches[0].pageX;
                //posY = ev.targetTouches[0].pageY;
                //posX = ev.changedTouches[0].pageX;
                //posY = ev.changedTouches[0].pageY;
                /*DocObj.getCurrentpage().walkthroughcontrol.moveForward = false;
                DocObj.getCurrentpage().walkthroughcontrol.moveBackward = false;
                DocObj.getCurrentpage().walkthroughcontrol.moveLeft = false;
                DocObj.getCurrentpage().walkthroughcontrol.moveRight = false;
                DocObj.getCurrentpage().walkthroughcontrol.moveUp = false;
                DocObj.getCurrentpage().walkthroughcontrol.moveDown = false;*/




                moveint = 0;
                //getAngleWithXAxis(100, posX - _wheelImgX, - posY + _wheelImgY, moveint);

                break;
        }
        //DocObj.getCurrentpage().walkthroughcontrol.updateMovementVector();
        //DocObj.getCurrentpage().walkthroughcontrol.updateRotationVector();


    }

    function regEventsgeneric(element, func){
        //ev_canvas
        element.addEventListener("touchstart", func, false);
        element.addEventListener("touchmove", func, true);
        element.addEventListener("touchend", func, false);
        element.addEventListener("touchcancel", func, false);

        element.oncontextmenu = function () {
            return false;
        };

        //canvas.addEventListener("dbltap", ev_canvas, false);
        //canvas.addEventListener("dblclick", ev_canvas, false);

        //canvas.addEventListener('wheel', ev_canvas, true);
        //canvas.addEventListener('mousewheel', ev_canvas, true);
        //canvas.addEventListener('DOMMouseScroll', ev_canvas, true);

        //canvas.addEventListener("drop", ev_canvas, false);
        //canvas.addEventListener("dragover", ev_canvas, false);

        
        
        if(bDisableMSIE11Eventcheck){

            element.addEventListener('mousemove', func, false);
            element.addEventListener('mousedown', func, false);
            element.addEventListener('mouseup', func, false);
            element.addEventListener('mouseout', func, false);
            


        }else{
            if (window.navigator.msPointerEnabled) {
                element.addEventListener('MSPointerMove', func, false);
                element.addEventListener('MSPointerDown', func, false);
                element.addEventListener('MSPointerUp', func, false);
                element.addEventListener('MSPointerOut', func, false);
                


            } else {
                element.addEventListener('mousemove', func, false);
                element.addEventListener('mousedown', func, false);
                element.addEventListener('mouseup', func, false);
                element.addEventListener('mouseout', func, false);

            }
        }

    }    

    function regEvents(element) {

        element.addEventListener('touchstart', touchEvents);
        element.addEventListener('touchmove', touchEvents);
        element.addEventListener('touchcancel', touchEvents);
        element.addEventListener('touchend', touchEvents);

        if(bDisableMSIE11Eventcheck){
            element.addEventListener('mousemove', touchEvents);
            element.addEventListener('mousedown', touchEvents);
            element.addEventListener('mouseup', touchEvents);
            element.addEventListener('mouseout', touchEvents);

        }else{
            if (window.navigator.msPointerEnabled) {
                element.addEventListener('MSPointerMove', touchEvents);
                element.addEventListener('MSPointerDown', touchEvents);
                element.addEventListener('MSPointerUp', touchEvents);
                element.addEventListener('MSPointerOut', touchEvents);
    
            }else{
                element.addEventListener('mousemove', touchEvents);
                element.addEventListener('mousedown', touchEvents);
                element.addEventListener('mouseup', touchEvents);
                element.addEventListener('mouseout', touchEvents);
    
            }    
        }
        


    }

        class Rx_CustStampGUI {
            constructor(callback) {
                var scope = this;
                this.callback = callback;
                this.connect = function (callback) {
                    scope.callback = callback;
                };
                this.onStampReceived = function (stampinfo) {
                    if (typeof scope.callback === "function") {
                        scope.callback(stampinfo);
                    }
                };
            }
        }

        class Rx_SymbolGUI {
            constructor(callback) {
                var scope = this;
                this.callback = callback;
                this.connect = function (callback) {
                    scope.callback = callback;
                };
                this.onSymbolReceived = function (symbolinfo) {
                    if (typeof scope.callback === "function") {
                        scope.callback(symbolinfo);
                    }
                };
            }
        }


        class Rx_CanvasResize {
            constructor(callback) {
                var scope = this;
                this.callback = callback;
                this.connect = function (callback) {
                    scope.callback = callback;
                };
                this.onResize = function (canvassize) {
                    if (typeof scope.callback === "function") {
                        scope.callback(canvassize);
                    }
                };
            }
        }

        class Rx_TextInputConn {
            constructor(callback) {
                var scope = this;
                this.callback = callback;
                this.operation = {
                    start: false,
                    create: false,
                    edit: false,
                    save: false
                };
                this.connect = function (callback) {
                    scope.callback = callback;
                };
                this.getText = function (text) {

                    if(DocObj == undefined){
                        return null;
                    }
                    if (DocObj.markuplist == undefined){
                        return null;
                    }


                    if (DocObj.markuplist[GetTempID().id] != undefined) {
                        return DocObj.markuplist[GetTempID().id];
                    }
                    else {
                        return null;
                    }
                };

                this.getTextinitset = function(){
                    if(DocObj == undefined){
                        return false;
                    }
                    if (DocObj.markuplist == undefined){
                        return false;
                    }

                    if (DocObj.markuplist[GetTempID().id] != undefined) {
                        return DocObj.markuplist[GetTempID().id].intialtextset;
                    }
           

                };                

                this.setText = function (text) {

                    if(DocObj == undefined){
                        return;
                    }
                    if (DocObj.markuplist == undefined){
                        return;
                    }


                    if (DocObj.markuplist[GetTempID().id] != undefined) {
                        scope.operation.edit = false;
                        scope.operation.start = false;
                        scope.operation.create = false;
                        scope.operation.save = false;
                        DocObj.markuplist[GetTempID().id].setText(text);
                        DocObj.markuplist[GetTempID().id].intialtextset = true;
                        //DocObj.markuplist[GetTempID().id].AdjustForRotation();
                        //drawmarkupAll(cntximg);
                        //DrawMarkupSelected(context);
                    }
                };
                this.setSize = function (size) {

                    if(DocObj == undefined){
                        return;
                    }
                    if (DocObj.markuplist == undefined){
                        return;
                    }


                    if (DocObj.markuplist[GetTempID().id] != undefined) {
                        scope.operation.edit = false;
                        scope.operation.start = false;
                        scope.operation.create = false;
                        scope.operation.save = false;
                        DocObj.markuplist[GetTempID().id].setSize(size);
                        //DocObj.markuplist[GetTempID().id].AdjustForRotation();
                        //drawmarkupAll(cntximg);
                        //DrawMarkupSelected(context);
                    }
                };
                this.setTextInput = function (textrect) {
                    if (typeof scope.callback === "function") {
                        scope.callback(textrect, scope.operation);
                    }
                };
            }
        }

        class Rx_TextdiagConn {
            constructor(callback) {
                var scope = this;
                this.callback = callback;
                this.connect = function (callback) {
                    scope.callback = callback;
                };
                this.setText = function (text) {
                    if (DocObj.markuplist[GetTempID().id] != undefined) {
                        DocObj.markuplist[GetTempID().id].text = text;
                        DocObj.markuplist[GetTempID().id].AdjustForRotation(false);


                        drawmarkupAll(cntximg);
                        DrawMarkupSelected(context);
                    }
                };
                this.setTextdiag = function (textdata) {
                    if (typeof scope.callback === "function") {
                        if (textdata == "" || textdata == ".") {
                            DocObj.markuplist[GetTempID().id].text = scope.callback("");
                            DocObj.markuplist[GetTempID().id].AdjustForRotation(false);
                            drawmarkupAll(cntximg);
                            DrawMarkupSelected(context);
                        }
                        else {
                            scope.callback(textdata);
                        }
                    }
                };
            }
        }

        class Rx_NotediagConn {
            constructor(callback) {
                var scope = this;
                this.callback = callback;
                this.connect = function (callback) {
                    scope.callback = callback;
                };
                this.isAvailable = function () {
                    if (GetTempID() == undefined || GetTempID().id == -1) {
                        return false;
                    }
                    return (DocObj.markuplist[GetTempID().id].type == 10);
                };
                this.getText = function () {
                    if (scope.isAvailable) {
                        return DocObj.markuplist[GetTempID().id];
                    }
                    else {
                        return null;
                    }
                };
                this.setText = function (text) {
                    if (scope.isAvailable) {


                        if(DocObj.markuplist[GetTempID().id] != undefined){
                            var markupobject = DocObj.markuplist[GetTempID().id];
                            markupobject.text = text;

                            if(RxCore_GUI_MarkupChanged != undefined){
                                var operation = markupobject.editaction;
                                RxCore_GUI_MarkupChanged.setMarkupSelected(markupobject, operation);
    
                            }

                        }
                        drawmarkupAll(cntximg);
                        DrawMarkupSelected(context);



                    }
                };
                this.setNotediag = function (notedata, readonly) {
                    if (typeof scope.callback === "function") {
                        if (notedata == "" || notedata == ".") {
                            if (GetTempID() == undefined) {
                                return;
                            }
                            DocObj.markuplist[GetTempID().id].text = scope.callback("");
                        }
                        else {
                            scope.callback(notedata, readonly);
                        }
                    }
                };
            }
        }


        /*Rx_GUITextdialog = function(callback){
        
             var scope = this;
             this.callback = callback;
        
        
        
             this.getText = function(text){
             if (typeof scope.callback === "function") {
             callback(text);
             }
        
             };
        
             };*/
        class Rx_ZoomUpdate {
            constructor(callback) {
                var scope = this;
                this.callback = callback;
                this.connect = function (callback) {
                    scope.callback = callback;
                };
                this.zoomUpdate = function (factor, center, type) {
                    if (typeof scope.callback === "function") {
                        scope.callback(factor, center, type);
                    }
                };
            }
        }

        class Rx_docPrintRect {
            constructor(callback) {
                var scope = this;
                this.callback = callback;
                this.connect = function (callback) {
                    scope.callback = callback;
                };
                this.zoomUpdate = function (docrect) {
                    if (typeof scope.callback === "function") {
                        scope.callback(docrect);
                    }
                };
            }
        }

    
        class Rx_PanUpdate {
            constructor(callback) {
                var scope = this;
                this.callback = callback;
                this.connect = function (callback) {
                    scope.callback = callback;
                };
                this.panUpdate = function (x, y, pagerect) {
                    if (typeof scope.callback === "function") {
                        scope.callback(x, y, pagerect);
                    }
                };
            }
        }

        class Rx_MarkupPath {
            constructor(callback) {
                var scope = this;
                this.callback = callback;
                this.connect = function (callback) {
                    scope.callback = callback;
                };
                this.markupPaths = function (paths) {
                    if (typeof scope.callback === "function") {
                        scope.callback(paths);
                    }
                };
            }
        }


        class Rx_MarkupSave {
            constructor(callback) {
                var scope = this;
                this.callback = callback;
                this.connect = function (callback) {
                    scope.callback = callback;
                };
                this.markupSaved = function () {
                    if (typeof scope.callback === "function") {
                        scope.callback();
                    }
                };
            }
        }

        class Rx_Ready {
            constructor(callback) {
                var scope = this;
                this.callback = callback;
                this.connect = function (callback) {
                    scope.callback = callback;
                };
                this.setupComplete = function (openInitDoc) {
                    if (typeof scope.callback === "function") {
                        scope.callback(openInitDoc);
                    }
                };
            }
        }

        class Rx_RotateEvent {
            constructor(callback) {
                var scope = this;
                this.callback = callback;
                this.connect = function (callback) {
                    scope.callback = callback;
                };
                this.onRotate = function (angle, pagenum) {
                    if (typeof scope.callback === "function") {
                        scope.callback(angle, pagenum);
                    }
                };
            }
        }

        class Rx_GUIUpload {
            constructor(callback) {
                var scope = this;
                this.callback = callback;
                this.progress = 0;
                this.connect = function (callback) {
                    scope.callback = callback;
                };
                this.setUpload = function (upload) {
                    if (typeof scope.callback === "function") {
                        scope.callback(upload);
                    }
                };
            }
        }

        class Rx_GUIUndoListEmpty {
            constructor(callback) {
                var scope = this;
                this.callback = callback;
                
                this.connect = function (callback) {
                    scope.callback = callback;
                };
                this.listempty = function (bundoempty, bredoemtpy) {
                    if (typeof scope.callback === "function") {
                        
                        scope.callback(bundoempty, bredoemtpy);
                    }
                };
            }

        }

        class Rx_GUIhasText {
            constructor(callback) {
                var scope = this;
                //Set to true if a page in document has text. Do not set back to false
                this.hastext = false;
                this.callback = callback;
                this.connect = function (callback) {
                    scope.callback = callback;
                };
                this.hasText = function (hastext, activefile) {
                    /*if(hastext && DocObj){
                        DocObj.hastext = true;
                    }*/
                    if (typeof scope.callback === "function") {
                        scope.callback(hastext, activefile);
                    }
                };
            }
        }

        class RxCore_GUIHatchChange{
            constructor (callback){
                var scope = this;

                this.callback = callback;

                this.connect = function (callback) {
                    scope.callback = callback;
                };

                this.setHatchchange = function (hatch) {

                    if (typeof scope.callback === "function") {
                        scope.callback(hatch);
                    }

                };

            }

        }
        

        class RxCore_GUIinput {
            constructor(callback) {
                var scope = this;
                
                this.callback = callback;

                this.connect = function (callback) {
                    scope.callback = callback;
                };

                this.setInput = function (inputobj) {
                    if (typeof scope.callback === "function") {
                        scope.callback(inputobj);
                    }
                };
            }

        }


        class Rx_GUITextFound {
            constructor(callback) {
                var scope = this;
                //Set to true if a page in document has text. Do not set back to false
                this.hastext = false;
                this.callback = callback;
                this.connect = function (callback) {
                    scope.callback = callback;
                };
                this.numTextFound = function (nummatches, currentpage) {
                    if (typeof scope.callback === "function") {
                        scope.callback(nummatches, currentpage);
                    }
                };
            }
        }

        class Rx_GUIRemovePage {
            constructor(callback) {
                var scope = this;
                this.callback = [callback];
                this.connect = function (callback) {
                    scope.callback = [...scope.callback, callback];
                };
                this.onRemovePage = function (pageIndex) {
                    scope.callback.forEach((callback) => {
                        if (typeof callback === "function") {
                            callback(pageIndex);
                        }
                    });
                };
           }
        }

        class RxCore_GUIPageCountChanged {
            constructor(callback) {
                var scope = this;
                this.callback = callback;
                this.showContext = false;
                this.connect = function (callback) {
                    scope.callback = callback;
                };
                this.countChanged = function (markup) {
                    if (typeof scope.callback === "function") {
                    scope.callback(markup);
                    }
                };
            }
        }

        class Rx_GUIDocumentSearch {
        constructor(callback) {
          var scope = this;
          //Set to true if a page in document has text. Do not set back to false
          this.hastext = false;
          this.seachText = "";
          this.wholeText = false;
          this.casesens = false;
          this.callback = [callback];
          this.connect = function (callback) {
            scope.callback = [...scope.callback, callback];
          };
          this.setFilter = function (text, casesens, wholeText) {
            this.seachText = text;
            this.casesens = casesens;
            this.wholeText = wholeText;
          };
          this.matchesFound = function (matches) {
            scope.callback.forEach((callback) => {
              if (typeof callback === "function") {
                callback(matches);
              }
            });
          };
          this.isCaseSensitive = function () {
            return this.casesens;
          };
        }
      }        


        class Rx_GUIResponseHeaders {
            constructor(callback) {
                var scope = this;
                this.callback = callback;
                this.connect = function (callback) {
                    scope.callback = callback;
                };
                this.setRespHeaders = function (szheaders) {
                    if (typeof scope.callback === "function") {
                        scope.callback(szheaders);
                    }
                };
            }
        }

        class Rx_GUIContentConn {
            constructor(callback) {
                var scope = this;
                this.callback = callback;
                this.connect = function (callback) {
                    scope.callback = callback;
                };
                this.setXMLreturn = function (doc, text) {
                    if (typeof scope.callback === "function") {
                        scope.callback(doc, text);
                    }
                };
            }
        }


        class Rx_GUIDownload {
            constructor(callback) {
                var scope = this;
                this.callback = callback;
                this.connect = function (callback) {
                    scope.callback = callback;
                };
                this.setDownload = function (download) {
                    if (typeof scope.callback === "function") {
                        scope.callback(download);
                        //file load complete event to use for doing stuff when loaded.
                        if (download == 'hide') {
                            //nothing
                        }
                    }
                };
            }
        }

        class Rx_GUIMarkupUnselectConn {
            constructor(callback) {
                var scope = this;
                this.callback = callback;
                this.connect = function (callback) {
                    scope.callback = callback;
                };
                this.setMarkupunSelected = function (markup) {
                    if (typeof scope.callback === "function") {
                        scope.callback(markup);
                    }
                };
            }
        }

        class Rx_GUIMarkupLinkConn {
            constructor(callback) {
                var scope = this;
                this.callback = callback;
                this.showContext = false;
                this.connect = function (callback) {
                    scope.callback = callback;
                };
                this.markupLink = function (markup) {
                    if (typeof scope.callback === "function") {
                        scope.callback(markup);
                    }
                };
            }
        }

        class Rx_GUIMarkupdrawValConn {
            constructor(callback) {
                var scope = this;
                this.callback = callback;
                this.showContext = false;
                this.connect = function (callback) {
                    scope.callback = callback;
                };
                this.setdimtext = function (dimtext) {
                    if (typeof scope.callback === "function") {
                        scope.callback(dimtext);
                    }
                };
            }
        }


        class Rx_GUIMarkupHoverConn {
            constructor(callback) {
                var scope = this;
                this.callback = callback;
                this.showContext = false;
                this.connect = function (callback) {
                    scope.callback = callback;
                };
                this.setMarkupHover = function (markup, x, y) {
                    if (typeof scope.callback === "function") {
                        scope.callback(markup,x,y);
                    }
                };
            }
        }



        class Rx_GUIMarkupConn {
            constructor(callback) {
                var scope = this;
                this.callback = callback;
                this.showContext = false;
                this.connect = function (callback) {
                    scope.callback = callback;
                };
                this.setMarkupSelected = function (markup, operation) {
                    if (typeof scope.callback === "function") {
                        scope.callback(markup, operation);
                        
                    }
                };
            }
        }

        class Rx_GUIPDFBookmarsConn {
            constructor(callback) {
                var scope = this;
                this._pagesRefCache = Object.create(null);
                this.callback = callback;
                this.connect = function (callback) {
                    scope.callback = callback;
                };
                /*this.createPageRefCache = function(outline){
        
                 for(var i= 0;i<outline.length;i++){
                 if(outline[i].count > 0){
                 var destRef = outline[i].dest[0];
        
                 if(DocObj.getCurrentpage().usepdfjs){
                 DocObj.getPDF_PageRef(scope._pagesRefCache,destRef);
                 }
                 //RxCore_GUI_PDFBookmarks.getPageRef(scope._pagesRefCache,destRef);
        
                 /*for (var j=0;j<outline[i].count;j++){
                 //ulstring += '<li>' + outline[i].items[j].title + '</li>\n';
                 }
                 }
        
                 }
        
                 };*/
                this.getPageRef = function (destRef) {
                    var pagenum = -1;
                    if (DocObj.getCurrentpage().usepdfjs) {
                        var cacheKey = destRef.num + ' ' + destRef.gen + ' R';
                        if (scope._pagesRefCache[cacheKey] != undefined) {
                            pagenum = scope._pagesRefCache[cacheKey];
                        }
                    }
                    return pagenum;
                };
                this.setPageRef = function (object) {
                    scope._pagesRefCache = object;
                };
                this.setPDFBookmarks = function (outline) {
                    if (typeof scope.callback === "function") {
                        scope.callback(outline);
                    }
                };
            }
        }

        class RxPrintCompareConn {
            constructor() {
                var scope = this;
                this.callback = undefined;
                this.connect = function (callback) {
                    scope.callback = callback;
                };
                this.printCompareComplete = function (printobj) {
                    if (typeof scope.callback === "function" && scope.callback !== undefined) {
                        scope.callback(printobj);
                    }
                };
            }
        }

        class Rx_PrintPageConn {
            constructor() {
                var scope = this;
                this.callback = undefined;
                this.connect = function (callback) {
                    scope.callback = callback;
                };
                this.printpageComplete = function (printobj) {
                    if (typeof scope.callback === "function" && scope.callback !== undefined) {
                        scope.callback(printobj);
                    }
                };
            }
        }

        class Rx_GUI3DViewConn {
            constructor(callback) {
                var scope = this;
                this.callback = callback;
                this.connect = function (callback) {
                    scope.callback = callback;
                };
                this.viewEvent = function (viewobject) {
                    if (typeof scope.callback === "function" && scope.callback !== undefined) {
                        scope.callback(viewobject);
                    }
                };
            }
        }


        class Rx_GUIPageConn {
            constructor(callback) {
                var scope = this;
                this.callback = callback;
                this.connect = function (callback) {
                    scope.callback = callback;
                };
                this.pageEvent = function (pageobject) {
                    if (typeof scope.callback === "function" && scope.callback !== undefined) {
                        scope.callback(pageobject);
                    }
                };
            }
        }

        class Rx_GUIStateConn {
            constructor(callback) {
                var scope = this;
                this.callback = callback;
                this.connect = function (callback) {
                    scope.callback = callback;
                };
                this.setGUIState = function (stateojb) {
                    this.state = stateojb;
                    if (typeof scope.callback === "function" && scope.callback !== undefined) {
                        scope.callback(stateojb);
                    }
                };
            }
        }

        class Rx_PenTableListConn {
            constructor(callback) {
                var scope = this;
                this.callback = callback;
                this.connect = function (callback) {
                    scope.callback = callback;
                };
                this.setPentablelist = function (penlist, activefile) {
                    if (typeof scope.callback === "function") {
                        scope.callback(penlist, activefile);
                    }
                };
            }
        }

        //RxCore_GUI_getsignatureComplete = new Rx_getSignatureConn();
        //RxCore_GUI_putsignatureComplete = new Rx_putSignatureConn();
    

        class Rx_getSignatureConn {
            constructor(callback) {
                var scope = this;
                this.callback = callback;
                this.connect = function (callback) {
                    scope.callback = callback;
                };
                this.onSignReceived = function (signatureinfo) {
                    if (typeof scope.callback === "function") {
                        scope.callback(signatureinfo);
                    }
                };
            }
        }

        class Rx_putSignatureConn {
            constructor(callback) {
                var scope = this;
                this.callback = callback;
                this.connect = function (callback) {
                    scope.callback = callback;
                };
                //RxCore_GUI_putsignatureComplete.complete(username);
                this.oncomplete = function (username, binitial) {
                    if (typeof scope.callback === "function") {
                        scope.callback(username,binitial);
                    }
                };
            }
        }


        class Rx_UserlistConn {
            constructor(callback) {
                var scope = this;
                this.callback = callback;
                this.connect = function (callback) {
                    scope.callback = callback;
                };
                this.getSignature = function () {
                    return signature;
                };
                this.setSignature = function (sign) {
                    signature = sign;
                };
                this.getCanChangeSign = function () {
                    return bCanChangeSignature;
                };
                this.SetUserMarkupdisplay = function (numuser, state) {
                    var curmarkup = 0;
                    Userlist[numuser].display = state;
                    for (curmarkup = 0; curmarkup < DocObj.markuplist.length; curmarkup++) {
                        var layerdisplay = getMarkupLayerState(DocObj.markuplist[curmarkup].layer);
                        if (DocObj.markuplist[curmarkup].signature == Userlist[numuser].Signature && layerdisplay) {
                            DocObj.markuplist[curmarkup].setdisplay(Userlist[numuser].display);
                            //DocObj.markuplist[curmarkup].display = Userlist[numuser].display;
                        }
                    }
                    drawmarkupAll(cntximg);
                    DrawMarkupSelected(context);
                };
                this.setUserlist = function (userlist, activefile) {
                    if (typeof scope.callback === "function") {
                        scope.callback(userlist, activefile);
                    }
                };
            }
        }

        class Rx_MarkupLayerConn {
            constructor(callback) {
                var scope = this;
                this.callback = callback;
                this.connect = function (callback) {
                    scope.callback = callback;
                };
                this.getCurMarkupLayer = function () {
                    return markuplayer;
                };
                this.getCanChangelayer = function () {
                    return bCanChangeLayer;
                };
                this.SetLayerMarkupdisplay = function (numlayer, state) {
                    var curmarkup = 0;
                    Layerlist[numlayer].display = state;
                    for (curmarkup = 0; curmarkup < DocObj.markuplist.length; curmarkup++) {
                        var signdisplay = getSignState(DocObj.markuplist[curmarkup].signature);
                        if (DocObj.markuplist[curmarkup].layer == Layerlist[numlayer].Layer && signdisplay) {
                            //DocObj.markuplist[curmarkup].display = Layerlist[numlayer].display;
                            DocObj.markuplist[curmarkup].setdisplay(Layerlist[numlayer].display);
                        }
                    }
                    drawmarkupAll(cntximg);
                    DrawMarkupSelected(context);
                };
                this.setMarkupLayers = function (markuplayers) {
                    if (typeof scope.callback === "function") {
                        scope.callback(markuplayers);
                    }
                };
            }
        }

        class Rx_CanvasContext{
            constructor(callback) {
                var scope = this;
                this.callback = callback;
                
                this.connect = function (callback) {
                    scope.callback = callback;
                };

                this.setEvent = function(cnv){
                    if (typeof scope.callback === "function") {
                        cnv.addEventListener("contextmenu", scope.callback, false);

                    }

                }
            }
            
        } 


        class Rx_FileInfoConn {
            constructor(callback) {
                var scope = this;
                this.callback = callback;
                this.connect = function (callback) {
                    scope.callback = callback;
                };
                this.getUnitlabel = function () {

                    
                    //return DocObj.getCurrentpage().Unitlabel;

                    return DocObj.getCurrentpage().scaleObject.Unitlabel;
                };
                this.setFileInfo = function (fileinfo) {
                    this.fileinfo = fileinfo;
                    if (typeof scope.callback === "function") {
                        scope.callback(fileinfo);
                    }
                };
            }
        }

    class Rx_FloorplanCanvasConn {
        constructor(callback) {
            var scope = this;

            this.callback = callback;

            this.connect = function (callback) {
                scope.callback = callback;
            };

            this.setFloorCanvas = function (floornum, canvas, alignarray) {
                
                if (typeof scope.callback === "function") {
                    scope.callback(floornum, canvas, alignarray);
                }
            };

        }
    }


        class Rx_PageThumbnailConn {
            constructor(callback) {
                var scope = this;
                this.callback = callback;
                this.connect = function (callback) {
                    scope.callback = callback;
                };
                this.getHeight = function () {
                    return canvasoheight;
                };
                this.getCurrentPage = function () {
                    return DocObj.getcurPage();
                };
                this.hasmarkup = function (page) {
                    var hasmarkup = false;
                    for (var i = 0; i < DocObj.markuplist.length; i++) {
                        if (DocObj.markuplist[i].pagenumber == page) {
                            hasmarkup = true;
                        }
                    }
                    return hasmarkup;
                };
                this.notify = function () {
                    if (typeof scope.callback === "function") {
                        scope.callback(this.pageindex, this.thumbnail);
                    }
                };
                this.setThumbnail = function (pgindex, thumbnail) {
                    this.thumbnail = thumbnail;
                    this.pageindex = pgindex;
                    this.notify();
                };
            }
        }

        class Rx_PageBirdseyeConn {
            constructor(callback) {
                var scope = this;
                this.callback = callback;
                this.connect = function (callback) {
                    scope.callback = callback;
                };
                this.getHeight = function () {
                    return canvasoheight;
                };
                this.getCurrentPage = function () {
                    return DocObj.getcurPage();
                };
                this.hasmarkup = function (page) {
                    var hasmarkup = false;
                    for (var i = 0; i < DocObj.markuplist.length; i++) {
                        if (DocObj.markuplist[i].pagenumber == page) {
                            hasmarkup = true;
                        }
                    }
                    return hasmarkup;
                };
                this.notify = function () {
                    if (typeof scope.callback === "function") {
                        scope.callback(this.pageindex, this.thumbnail);
                    }
                };
                this.setThumbnail = function (pgindex, thumbnail) {
                    this.thumbnail = thumbnail;
                    this.pageindex = pgindex;
                    this.notify();
                };
            }
        }

        class Rx_PageThumbnailsConn {
            constructor(callback) {
                var scope = this;
                this.callback = callback;
                this.connect = function (callback) {
                    scope.callback = callback;
                };
                this.getHeight = function () {
                    return canvasoheight;
                };
                this.getCurrentPage = function () {
                    return DocObj.getcurPage();
                };
                this.hasmarkup = function (page) {
                    var hasmarkup = false;
                    for (var i = 0; i < DocObj.markuplist.length; i++) {
                        if (DocObj.markuplist[i].pagenumber == page) {
                            hasmarkup = true;
                        }
                    }
                    return hasmarkup;
                };
                this.notify = function () {
                    if (typeof scope.callback === "function") {
                        scope.callback(this.thumbnails);
                    }
                };
                this.setThumbnails = function (thumbnails) {
                    this.thumbnails = thumbnails;
                    this.notify();
                };
            }
        }

        class Rx_VectorLayersConn {
            constructor(callback) {
                var scope = this;
                this.callback = callback;
                this.connect = function (callback) {
                    scope.callback = callback;
                };
                this.getHeight = function () {
                    return canvasoheight;
                };
                this.setVectorLayers = function (vectorlayers) {
                    if (typeof scope.callback === "function") {
                        scope.callback(vectorlayers);
                    }
                };
            }
        }

        class Rx_VectorBlocksConn {
            constructor(callback) {
                var scope = this;
                this.callback = callback;
                this.connect = function (callback) {
                    scope.callback = callback;
                };
                this.getHeight = function () {
                    return canvasoheight;
                };
                this.setVectorBlocks = function (vectorblocks) {
                    if (typeof scope.callback === "function") {
                        scope.callback(vectorblocks);
                    }
                };
            }
        }

        class Rx_3DPartsReadComplete {
            constructor(callback) {
                var scope = this;
                this.connect = function (callback) {
                    scope.callback = callback;
                };
                this.readComplete = function () {
                    if (typeof scope.callback === "function") {
                        scope.callback();
                    }
                };
            }
        }


        class Rx_3DPartsConn {
            constructor(callback) {
                var scope = this;
                this.listheight = 0;
                this.isupdate = false;
                this.scrollto = 0;
                this.callback = callback;
                this.connect = function (callback) {
                    scope.callback = callback;
                };
                this.getHeight = function () {
                    return canvasoheight;
                };
                this.setlistHeight = function (height) {
                    scope.listheight = height;
                };
                this.set3DParts = function (Parts) {
                    this.parts = Parts;
                    if (typeof scope.callback === "function") {
                        scope.callback(Parts);
                    }
                };
            }
        }

        class Rx_2DBlockConn {
            constructor(callback) {
                var scope = this;
                this.callback = callback;
                this.connect = function (callback) {
                    scope.callback = callback;
                };
                this.set2DBlockID = function (blockid) {
                    if (typeof scope.callback === "function") {
                        scope.callback(blockid);
                    }
                };
            }
        }

        class Rx_2DBlockConnHover {
            constructor(callback) {
                var scope = this;
                this.callback = callback;
                this.connect = function (callback) {
                    scope.callback = callback;
                };
                this.set2DBlockID = function (blockid, mousepos) {
                    if (typeof scope.callback === "function") {
                        scope.callback(blockid, mousepos);
                    }
                };
                this.onBlockHover = function (blockid, mousepos) {
                    if (scope.blockHoverCallback) {
                        scope.blockHoverCallback(blockid, mousepos);
                    }
                }

            }
        }


        class Rx_2DBlockAttributeConnScreen {
            constructor(callback) {
                var scope = this;
                this.callback = callback;
                this.connect = function (callback) {
                    scope.callback = callback;
                };
                this.set2DBlockInfo = function (PartInfo, mousepos, pathindex) {
                    this.info = PartInfo;
                    if (typeof scope.callback === "function") {
                        scope.callback(PartInfo, mousepos, pathindex);
                        //partlist, screenmouse, pathindex
                    }
                };
            }
        }



        class Rx_2DBlockAttributeConn {
            constructor(callback) {
                var scope = this;
                this.callback = callback;
                this.connect = function (callback) {
                    scope.callback = callback;
                };
                this.set2DBlockInfo = function (PartInfo) {
                    this.info = PartInfo;
                    if (typeof scope.callback === "function") {
                        scope.callback(PartInfo);
                    }
                };
            }
        }
        class Rx_3DPartInfoConn {
            constructor(callback) {
                var scope = this;
                this.callback = callback;
                this.connect = function (callback) {
                    scope.callback = callback;
                };
                this.set3DPartInfo = function (PartInfo) {
                    this.info = PartInfo;
                    if (typeof scope.callback === "function") {
                        scope.callback(PartInfo);
                    }
                };
            }
        }

    class Rx_PDFPassWordHandler {
        constructor(callback) {
            var scope = this;
            this.callback = callback;
            this.connect = function (callback) {
                scope.callback = callback;
            };
            this.PassWordRequest = function (passfunc, reason, Filepath) {
                if (typeof scope.callback === "function") {
                    scope.callback(passfunc, reason, Filepath);
                }
            };
        }
    }


        class Rx_PDFRenderComplete {
            constructor(callback) {
                var scope = this;
                this.callback = callback;
                this.connect = function (callback) {
                    scope.callback = callback;
                };
                this.RenderComplete = function (pagedata) {
                    if (typeof scope.callback === "function") {
                        scope.callback(pagedata);
                    }
                };
            }
        }

        class Rx_PageLoadComplete {
            constructor(callback) {
                var scope = this;
                this.callback = callback;
                this.connect = function (callback) {
                    scope.callback = callback;
                };
                this.loadComplete = function (pagenumber) {
                    if (typeof scope.callback === "function") {
                        scope.callback(pagenumber);
                        //test to remove unnecessary rendering
                        //DocObj.getCurrentpage().redraw();
                    }
                };
            }
        }

        class Rx_ScaleListLoadComplete {
            constructor(callback) {
                var scope = this;
                this.callback = callback;
                this.connect = function (callback) {
                    scope.callback = callback;
                };
                this.loadComplete = function (pagenumber) {
                    if (typeof scope.callback === "function") {
                        scope.callback(pagenumber);
                    }
                };
            }
        }        

        class Rx_MarkupSVGLoadConn {
            constructor(callback) {
                var scope = this;
                this.callback = callback;
                this.connect = function (callback) {
                    scope.callback = callback;
                };
                this.loadComplete = function (svgstring, markupobject) {
                    if (typeof scope.callback === "function") {
                        scope.callback(svgstring, markupobject);
                    }
                };
            }

            
        }
        class Rx_MarkupLoadComplete {
            constructor(callback) {
                var scope = this;
                this.callback = callback;
                this.connect = function (callback) {
                    scope.callback = callback;
                };
                this.loadComplete = function (markups, fileindex, activefile) {
                    if (typeof scope.callback === "function") {
                        scope.callback(markups, fileindex, activefile);
                    }
                };
            }
        }

    class Rx_floorClickPosEvent {
        constructor(callback) {
            var scope = this;
            this.callback = callback;
            this.connect = function (callback) {
                scope.callback = callback;
            };
            this.navClick = function (navinfo) {
                if (typeof scope.callback === "function") {
                    scope.callback(navinfo);
                }
            };
        }
    }

    //RxCore_GUI_floorlevel
    class Rx_FloorLevelConn {
        constructor(callback) {
            var scope = this;
            this.callback = callback;

            this.connect = function (callback) {
                scope.callback = callback;
            };
            this.setFloorLevel = function (fileurl, bActiveFile, docpage) {


                if (typeof scope.callback === "function") {
                    scope.callback(fileurl, bActiveFile, docpage);
                }
            };
        }

    }
    class Rx_FloorLoadComplete {
        constructor(callback) {
            var scope = this;
            this.callback = callback;
            this.connect = function (callback) {
                scope.callback = callback;
            };
            this.loadComplete = function (fileurl, bActiveFile, docpage) {
                if (typeof scope.callback === "function") {
                    scope.callback(fileurl, bActiveFile, docpage);
                }
            };
        }
    }

        class Rx_ActiveFileConn {
            constructor(callback) {
                var scope = this;
                this.callback = callback;
                this.connect = function (callback) {
                    scope.callback = callback;
                };
                this.setActiveFile = function (fileindex) {
                    if (typeof scope.callback === "function") {
                        scope.callback(fileindex);
                    }
                };
            }
        }

        class Rx_FileCloseComplete {
            constructor(callback) {
                var scope = this;
                this.callback = callback;
                this.connect = function (callback) {
                    scope.callback = callback;
                };
                this.closeComplete = function (fileindex, bclosed) {
                    if (typeof scope.callback === "function") {
                        scope.callback(fileindex, bclosed);
                    }
                };
            }
        }

        class Rx_GUIDreceived{
            constructor(callback) {
                var scope = this;
                this.callback = callback;
                this.connect = function (callback) {
                    scope.callback = callback;
                };
                this.guidcomplete = function (response, operation, bok) {
                    if (typeof scope.callback === "function") {
                        scope.callback(response,operation, bok);
                    }
                };
            }

        }

        class Rx_RenderComplete {
            constructor(callback) {
                var scope = this;
                this.callback = callback;
                this.connect = function (callback) {
                    scope.callback = callback;
                };
                this.renderComplete = function (fileindex) {
                    if (typeof scope.callback === "function") {
                        scope.callback(fileindex);
                    }
                };
            }

        }

        class Rx_CompareCreated {
            constructor(callback) {
                var scope = this;
                this.callback = callback;
                this.connect = function (callback) {
                    scope.callback = callback;
                };
                this.compareComplete = function (fileindex) {
                    if (typeof scope.callback === "function") {
                        scope.callback(fileindex);
                    }
                };
            }

        }

        class Rx_FileLoadComplete {
            constructor(callback) {
                var scope = this;
                this.callback = callback;
                this.connect = function (callback) {
                    scope.callback = callback;
                };
                this.loadComplete = function (fileurl, bActiveFile) {
                    if (typeof scope.callback === "function") {
                        scope.callback(fileurl, bActiveFile);
                    }
                };
            }
        }


        class Rx_MarkupDrawErrorEvt {
            constructor(callback) {
                var scope = this;
                this.callback = callback;
                this.connect = function (callback) {
                    scope.callback = callback;
                };
                this.onDrawError = function (error) {
                    if (typeof scope.callback === "function") {
                        scope.callback(error);
                    }
                };
            }
        }

        class Rx_MarkupDrawEvent {
            constructor(callback) {
                var scope = this;
                this.callback = callback;
                this.connect = function (callback) {
                    scope.callback = callback;
                };
                this.onDrawEvent = function (distAngle) {
                    if (typeof scope.callback === "function") {
                        scope.callback(distAngle);
                    }
                };
            }
        }

        class Rx_MarkupListConn {
            constructor(callback) {
                var scope = this;
                this.callback = callback;
                this.pageonly = false;
                this.connect = function (callback) {
                    scope.callback = callback;
                };
                this.getDisplayName = function (sign) {
                    return GetDisplayName(sign);
                };
                this.getDisplayDate = function (date) {
                    return GetDateTime(date);
                };
                /* Andriy notify listeners */
                this.notify = function (markupList, fileActive) {
                    if (typeof scope.callback === "function") {
                        scope.callback(markupList, fileActive);
                    }
                };
                this.setMarkupList = function (markupList, fileActive, preventNotification) {
                    /* Andriy */
                    this.markupList = markupList;
                    if (!preventNotification) {
                        this.notify(markupList, fileActive);
                    }
                };
            }
        }

        class Rx_3DCameraSave {
            constructor(callback) {
                var scope = this;
                this.callback = callback;
                this.connect = function (callback) {
                    scope.callback = callback;
                };
                this.notify = function (cameraobject, fileActive) {
                    if (typeof scope.callback === "function") {
                        scope.callback(cameraobject, fileActive);
                    }
                };
                this.setCameraSave = function (cameraobject, fileActive, preventNotification) {
                    if (!preventNotification) {
                       this.notify(cameraobject, fileActive);
                    }
                };

            }                
        }

        class Rx_Markup3DListConn {
            constructor(callback) {
                var scope = this;
                this.callback = callback;
                this.connect = function (callback) {
                    scope.callback = callback;
                };
                this.getDisplayName = function (sign) {
                    return GetDisplayName(sign);
                };
                this.getDisplayDate = function (date) {
                    return GetDateTime(date);
                };
                /* Andriy notify listeners */
                this.notify = function (markupList, fileActive) {
                    if (typeof scope.callback === "function") {
                        scope.callback(markupList, fileActive);
                    }
                };
                this.setMarkupList = function (markupList, fileActive, preventNotification) {
                    /* Andriy */
                    this.markupList = markupList;
                    if (!preventNotification) {
                        this.notify(markupList, fileActive);
                    }
                };
            }
        }

        class Rx_MarkupStampsConn {
            constructor(callback) {
                var scope = this;
                this.callback = callback;
                this.connect = function (callback) {
                    scope.callback = callback;
                };
                this.setMarkupStamps = function (stamps) {
                    if (typeof scope.callback === "function") {
                        scope.callback(stamps);
                    }
                };
            }
        }

        class Rx_MeasureConn {
            constructor(callback) {
                var scope = this;
                this.callback = callback;
                this.connect = function (callback) {
                    scope.callback = callback;
                };
                this.getLength = function (length) {
                    // Andriy: moved to a separate function
                    return RxCore_helper.getLength(length);
                };
                this.getUnitlabel = function () {
                    //return DocObj.getCurrentpage().Unitlabel;
                    return DocObj.getCurrentpage().scaleObject.Unitlabel;

                };
                this.getAreaUnitlabel = function () {

                    //return DocObj.getCurrentpage().AreaUnitlabel;
                    return DocObj.getCurrentpage().scaleObject.AreaUnitlabel;
                };
                this.setMeasureresult = function (measureresult) {
                    if (typeof scope.callback === "function") {
                        scope.callback(measureresult);
                    }
                };
            }
        }


        class Rx_CalibrateComplete {
            constructor(callback) {
                var scope = this;
                this.callback = callback;
                this.connect = function (callback) {
                    scope.callback = callback;
                };
                this.onCalibrate = function (calibratedata) {
                    if (typeof scope.callback === "function") {
                        scope.callback(calibratedata);
                    }
                };
            }
        }

    class Rx_BirdsEyeDrawEventConn{
        constructor(callback) {
            var scope = this;
            this.callback = callback;
            
            this.connect = function (callback) {
                scope.callback = callback;
            };
            
            this.drawbirdseye = function () {
                if (typeof scope.callback === "function") {
                    scope.callback();
                }
            };

        };            
    }
    class Rx_CameraPosEventConn{
        constructor(callback) {
            var scope = this;
            this.callback = callback;
            
            this.connect = function (callback) {
                scope.callback = callback;
            };
            
            this.cameraPos = function (position, camangle, center) {
                if (typeof scope.callback === "function") {
                    scope.callback(position, camangle, center);
                }
            };


        };


    }    

        class Rx_CalibrateConn {
            constructor(callback) {
                var scope = this;
                this.callback = callback;
                this.connect = function (callback) {
                    scope.callback = callback;
                };
                this.getUnitlabel = function () {
                    
                    //DocObj.getCurrentpage().scaleObject
                    //return DocObj.getCurrentpage().Unitlabel;

                    return DocObj.getCurrentpage().scaleObject.Unitlabel;

                    

                };
                this.setCalibration = function (val) {
                    var pagenumber = -1;
                    if (documentopen) {
                        //nCalibrateScale = DocObj.getCurrentpage().setCalibration(val);
                        nCalibrateScale = DocObj.getCurrentpage().scaleObject.setCalibration(val);

                        pagenumber = DocObj.getCurrentpage().pagenumber;
                    }
                    else {
                        if (val) {
                            if (nCalibrateSet != 0) {
                                nCalibrateScale = nCalibrateMeasured / nCalibrateSet;
                                nCalibrateScale = 1 / nCalibrateScale;
                                //setCalibratebtn();
                                MeasureScale = nCalibrateScale;
                            }
                        }
                        else {
                            nCalibrateScale = 1;
                        }
                    }
                    var calibratedata = {
                        calibratescale: nCalibrateScale,
                        pagenumber: pagenumber
                    };
                    if (RxCore_GUI_CalibrateComplete != undefined) {
                        RxCore_GUI_CalibrateComplete.onCalibrate(calibratedata);
                    }
                };
                this.SetTempCal = function (value) {
                    nCalibrateSet = value;
                };

                this.setCalibrateScaleByLength = function () {
                    if (nCalibrateSet != 0) {
                        nCalibrateScale = nCalibrateMeasured / nCalibrateSet;
                        nCalibrateScale = 1 / nCalibrateScale;                        
                    }
                };

                this.setCalibrateScale = function (val) {
                    nCalibrateScale = val;
                };
                this.getMeasureScale = function () {
                    return nCalibrateScale;
                };                

                this.setCalibrate = function (calibratedata) {
                    if (typeof scope.callback === "function") {
                        scope.callback(calibratedata);
                    }
                };


            }
        }


        class Rx_StateGUIConn {
            constructor(callback) {
                var scope = this;
                this.callback = callback;
                this.connect = function (callback) {
                    scope.callback = callback;
                };
                this.setStateGUI = function (RxCorestate) {
                    if (typeof scope.callback === "function") {
                        scope.callback(RxCorestate);
                    }
                };
            }
        }

        class Rx_Walkthrough3DConn {
            constructor(callback) {
                var scope = this;
                this.callback = callback;
                this.elementSet = false;
                this._wheelImg = undefined;

                this.connect = function (callback) {
                    scope.callback = callback;
                };
                this.setDivElement = function (HTMLElement) {
                    if(bIsmobile){
                        regEvents(HTMLElement);
                        this.elementSet = true;
                    }
                };
                this.setWalkthroughGUI = function (setvisible) {
                    if (typeof scope.callback === "function") {
                        scope.callback(setvisible);
                    }
                };
            }
        }

        class Rx_CompareMeasureConn {
            constructor(callback) {
                var scope = this;
                this.callback = callback;
                this.connect = function (callback) {
                    scope.callback = callback;
                };
                this.AlignMeasure = function (distance, angle, offset, pagewidth, scaleinfo) {
                    if (typeof scope.callback === "function") {
                        scope.callback(distance, angle, offset, pagewidth, scaleinfo);
                    }
                };
            }
        }

        class Rx_CompareAlignConn {
            constructor(callback) {
                var scope = this;
                this.callback = callback;
                this.connect = function (callback) {
                    scope.callback = callback;
                };
                this.AlignComplete = function (pageobject) {
                    if (typeof scope.callback === "function") {
                        scope.callback(pageobject);
                    }
                };
            }
        }

        var onRotateEvent = function(callback){
            var scope = this;
        };

        class Rx_ConsolidateConnMarkup {
            constructor(callback) {
                var scope = this;
                this.callback = callback;
                this.connect = function (callback) {
                    scope.callback = callback;
                };
                this.Consolidatevt = function (markupobj) {
                    if (typeof scope.callback === "function") {
                        scope.callback(markupobj);
                    }
                };
            }
        }


        class Rx_ConsolidateConn {
            constructor(callback) {
                var scope = this;
                this.callback = callback;
                this.connect = function (callback) {
                    scope.callback = callback;
                };
                this.Consolidatevt = function () {
                    if (typeof scope.callback === "function") {
                        scope.callback(consolidateObj);
                    }
                };
            }
        }

        class Rx_PermissionsConn {
            constructor(callback) {
                var scope = this;
                this.callback = callback;
                this.connect = function (callback) {
                    scope.callback = callback;
                };
                this.UserPermissions = function (permissions) {
                    if (typeof scope.callback === "function") {
                        scope.callback(permissions);
                    }
                };
            }
        }

        class Rx_TextCopyConn{
            constructor(callback) {
                var scope = this;
                this.callback = callback;
                this.connect = function (callback) {
                    scope.callback = callback;
                };
                this.TextCopied = function (fileurl, text) {
                    if (typeof scope.callback === "function") {
                        scope.callback(fileurl, text);
                    }
                };
            }
        }

        class onCloseCallback {
            constructor(callback) {
                var scope = this;
                this.callback = callback;
                this.connect = function (callback) {
                    scope.callback = callback;
                };
                this.onClose = function () {
                    if (typeof scope.callback === "function") {
                        scope.callback();
                    }
                };
            }
        }

        class Rx_CompareDialogConn {
            constructor(callback) {
                var scope = this;
                this.callback = callback;
                this.connect = function (callback) {
                    scope.callback = callback;
                };
                this.CompareDialog = function () {
                    if (typeof scope.callback === "function") {
                        scope.callback(OpenfileNames);
                    }
                };
            }
        }

        class Rx_GUIMarkupMeasureRealTimeDataConn {
            constructor(callback) {
                var scope = this;
                this.callback = callback;
                this.showContext = false;
                this.connect = function (callback) {
                    scope.callback = callback;
                };
                this.setMarkupMeasureRealTimeData = function (markup, operation) {
                    if (typeof scope.callback === "function") {
                        scope.callback(markup, operation);
                        
                    }
                };
            }
        }


        function commonErrorCatchEx(szfuncname, error){
            if(error.status){
                console.log(szfuncname, error.status);
            }else{
                console.log(szfuncname, error);
            }

            if (RxCore_GUI_Download != undefined) {
                RxCore_GUI_Download.setDownload("hide");
            }


        }


        function commonErrorCatch(error){
            
            if(error.status){
                if (error.status == 404) {
                    console.log("XML could not be found error 404");

                    if (RxCore_GUI_Download != undefined) {
                        RxCore_GUI_Download.setDownload("hide");
                    }
    
                } else if (error.status == 503) {
                    console.log("Server is down error 503");
                    if (RxCore_GUI_Download != undefined) {
                        RxCore_GUI_Download.setDownload("hide");
                    }
    

                } else if (error.status == 501) {
                    console.log("Server is not responding refresh the page error 501");
                    if (RxCore_GUI_Download != undefined) {
                        RxCore_GUI_Download.setDownload("hide");
                    }
                    /*if(!bIsmobile){
                        Get_License();
                    }*/
    
                }
               }else{
                console.log(error);

                if (RxCore_GUI_Download != undefined) {
                    RxCore_GUI_Download.setDownload("hide");
                }

               }

            /*if(error.status){
            if (error.status == 404) {
                alert("XML could not be found");
                if (RxCore_GUI_Download != undefined) {
                    RxCore_GUI_Download.setDownload("hide");
                }

            } else if (error.status == 503) {
                alert("Server is down");
                if (RxCore_GUI_Download != undefined) {
                    RxCore_GUI_Download.setDownload("hide");
                }

            } else if (error.status == 501) {
                alert("Server is not responding refresh the page");
                if (RxCore_GUI_Download != undefined) {
                    RxCore_GUI_Download.setDownload("hide");
                }

            }
           }else{
            console.log(error);
           }*/


        }        

        function hasDocPage(){
            var docpageNotready = true;

            if(DocObj == null || DocObj == undefined){
                docpageNotready = false;
            }else{
                var page = DocObj.getCurrentpage();

                if(page == undefined){
                    docpageNotready = false;
                }
            }

            return docpageNotready;

        }


        function docpagNotReady(){

            var docpageNotready = false;

            if (!documentopen) {
                docpageNotready = true;
            }
    
            if(DocObj == null || DocObj == undefined){
                docpageNotready = true;
            }else{
                var page = DocObj.getCurrentpage();

                if(page == undefined){
                    docpageNotready = true;
                }
            }

            return docpageNotready;
    
        }        

        function RxCore_getMarkupTypebyname(sztypename){
            var markuptypes = RxCore_getMarkupTypes();

            var returntype = undefined;

            for(var ti = 0; ti < markuptypes; ti++){
                if (markuptypes[ti].typename == sztypename){
                    returntype = markuptypes[ti];
                }
            }

            return returntype;

        }



        function RxCore_getMarkupTypes(){

            var markuptypes = [
                {label : "Freehand pen", typename : 'PEN', type : 0, subtype : 0},
                {label : "Eraser", typename : 'ERASER', type : 0, subtype : 1},
                {label : "Polyline", typename : 'POLYLINE', type : 1, subtype : 1},
                {label : "Polygon", typename : 'POLYGON', type : 1, subtype : 2},
                {label : "Measure Path", typename : 'PATH', type : 1, subtype : 3},
                {label : "Measure angle", typename : 'ANGLE', type : 1, subtype : 4},
                {label : "Measure angle", typename : 'ANGLEBCKWRD', type : 1, subtype : 5},
                {label : "Measure segment", typename : 'SEGMENT', type : 1, subtype : 6},
                //{label : "Polycurve", typename : 'POLYCURVE', type : 2, subtype : 0},
                {label : "Rectangle", typename : 'RECTANGLE', type : 3, subtype : 0},
                
                {label : "Rounded Rectangle", typename : 'ROUNDED_RECTANGLE', type : 3, subtype : 1},
                {label : "Marker", typename : 'MARKER', type : 3, subtype : 3},
                {label : "Rectanglular Area", typename : 'AREARECT', type : 3, subtype : 6},
                {label : "Oval", typename : 'ELLIPSE', type : 4, subtype : 0},
                {label : "Circle", typename : 'CIRCLE', type : 4, subtype : 1},
                {label : "Revision Cloud", typename : 'CLOUD', type : 5, subtype : 0},
                {label : "Line", typename : 'LINE', type : 6, subtype : 9},
                {label : "Arrow", typename : 'ARROW', type : 6, subtype : 0},
                {label : "Dimension Line", typename : 'DIMENSION', type : 7, subtype : 0},
                {label : "Space", typename : 'SPACE', type : 8, subtype : 1},
                {label : "Area", typename : 'AREA', type : 8, subtype : 0},
                {label : "Callout", typename : 'CALLOUT', type : 9, subtype : 2},
                {label : "Text", typename : 'TEXT', type : 9, subtype : 1},
                {label : "Note", typename : 'NOTE', type : 10, subtype : 0},
                {label : "Image", typename : 'IMAGE', type : 11, subtype : 0},
                {label : "Symbol", typename : 'SYMBOL', type : 11, subtype : 1},
                {label : "Button", typename : 'BUTTON', type : 11, subtype : 3},
                {label : "Signature", typename : 'SIGNATURE', type : 11, subtype : 4},
                {label : "Signature", typename : 'SIGNATUREMOVEABLE', type : 11, subtype : 5},
                {label : "Stamp", typename : 'IMAGESTAMP', type : 11, subtype : 12},
                {label : "link", typename : 'link', type :11 , subtype : 20},
                {label : "interactiveStamp", typename : 'interactiveStamp', type : 11, subtype : 21},
                {label : "Stamp", typename : 'STAMP', type : 12, subtype : 0},
                {label : "Count", typename : 'COUNT', type : 13, subtype : 0},
                {label : "MeasureArc", typename : 'MEASUREARC', type : 14, subtype : 0}
            ];



            return markuptypes;

        }

        function RxCore_getMarkupProperties(type){


            var properties = {
                lineColor : false,
                fillColor : false,
                textColor : false,
                lineWidth : false,
                lineStyle : false,
                clockwise : false,
                customlabel : false,
                display : true,
                countsize : false,
                countshape : false,
                showperimeter : false,
                enablelink : false,
                endpoint_open_single : false,
                endpoint_open_both : false,
                endpoint_filled_single : false,
                endpoint_filled_both : false,
                endpoints_bar : false,
                endpoints_arrow : false,
                lineSegmentWidth : false,
                transpacency : false,
                rotate : false,
                snap : false,
                fill : false,
                fontName : false,
                fontHeight : false,
                labelsize : false, 
                fontBold : false,
                fontItalic : false
            };

            
            

            //{label : "Freehand pen", typename : 'PEN', type : 0, subtype : 0},
            if(type.typename == 'PEN'){
                properties.lineColor = true;
                properties.lineWidth = true;
            } 

            //{label : "Eraser", typename : 'ERASER', type : 0, subtype : 1},
            if(type.typename == 'ERASER'){
                properties.lineWidth = true;
                properties.display = false;
            } 

            //{label : "Polyline", typename : 'POLYLINE', type : 1, subtype : 1},
            if(type.typename == 'POLYLINE'){
                properties.lineColor = true;
                properties.lineWidth = true;
                properties.snap = true;
            } 

            //{label : "Polygon", typename : 'POLYGON', type : 1, subtype : 2},
            if(type.typename == 'POLYGON'){
                //labelType = "Polygon";
                properties.lineColor = true;
                properties.fillColor = true;
                properties.lineWidth = true;
                properties.transpacency = true;
                properties.snap = true;
                properties.fill = true;

            } 

            //{label : "Measure path", typename : 'PATH', type : 1, subtype : 3},
            if(type.typename == 'PATH'){
                //labelType = "Measure Path";
                properties.lineColor = true;
                properties.lineWidth = true;
                properties.snap = true;
                properties.labelsize = true;
            }

            //{label : "Measure angle", typename : 'ANGLE', type : 1, subtype : 4, 5},
            if(type.typename == 'ANGLE'){
                //labelType = "Measure angle";
                properties.lineColor = true;
                properties.lineWidth = true;
                properties.snap = true;
                properties.clockwise = true,
                properties.labelsize = true;
            }

            //{label : "Measure segment", typename : 'SEGMENT', type : 1, subtype : 6},
            if(type.typename == 'SEGMENT'){
                //labelType = "Measure segment";
                properties.lineColor = true;
                properties.lineWidth = true;
                properties.lineSegmentWidth = true;
                properties.snap = true;            
            }

            //{label : "Polycurve", typename : 'POLYCURVE', type : 2, subtype : 0},
            if(type.typename == 'POLYCURVE'){
                //labelType = "Polycurve";
                properties.lineColor = true;
                properties.lineWidth = true;
            }
            
            //{label : "Rectangle", typename : 'RECTANGLE', type : 3, subtype : 0},
            if(type.typename == 'RECTANGLE'){
                //labelType = "Rectangle";
                properties.lineColor = true;
                properties.fillColor = true;
                properties.lineWidth = true;
                properties.transpacency = true;
                properties.snap = true;
                properties.fill = true;
                properties.rotate = true;
            }

            //{label : "Rounded rectangle", typename : 'ROUNDED_RECTANGLE', type : 3, subtype : 1},
            if(type.typename == 'ROUNDED_RECTANGLE'){
                //labelType = "Rounded Rectangle";
                properties.lineColor = true;
                properties.fillColor = true;
                properties.lineWidth = true;
                properties.transpacency = true;
                properties.snap = true;
                properties.fill = true;
                properties.rotate = true;
            }


            //{label : "Marker", typename : 'MARKER', type : 3, subtype : 3},
            if(type.typename == 'MARKER'){
                //labelType.label = "Marker";
                properties.fillColor = true;
                properties.rotate = true;
            }

            //{label : "Measure rectangle", typename : 'AREARECT', type : 3, subtype : 6},
            if(type.typename == 'AREARECT'){
                //labelType = "Measure rectangle";
                properties.lineColor = true;
                properties.fillColor = true;
                properties.lineWidth = true;
                properties.transpacency = true;
                properties.snap = true;
                properties.fill = true;
                properties.labelsize = true;
                //properties.rotate = true;
            }



            //{label : "Oval", typename : 'ELLIPSE', type : 4, subtype : 0},
            if(type.typename == 'ELLIPSE'){
                //labelType = "Oval";

                properties.lineColor = true;
                properties.fillColor = true;
                properties.lineWidth = true;
                properties.transpacency = true;
                properties.snap = true;
                properties.fill = true;
                properties.rotate = true;
            }


            //{label : "Circle", typename : 'CIRCLE', type : 4, subtype : 1},
            if(type.typename == 'CIRCLE'){
                //labelType = "Circle";

                properties.lineColor = true;
                properties.fillColor = true;
                properties.lineWidth = true;
                properties.transpacency = true;
                properties.snap = true;
                properties.fill = true;
    
                //properties.rotate = true;
            }

            //{label : "Revision Cloud", typename : 'CLOUD', type : 5, subtype : 0},
            if(type.typename == 'CLOUD'){
                //labelType = "Revision Cloud";
                properties.lineColor = true;
                properties.fillColor = true;
                properties.lineWidth = true;
                properties.transpacency = true;
                properties.snap = true;
                properties.fill = true;
                properties.rotate = true;
            }

            //{label : "Line", typename : 'LINE', type : 6, subtype : 9},
            if(type.typename == 'LINE'){
                //labelType = "Line";
                properties.lineColor = true;
                properties.lineWidth = true;
                properties.snap = true;
    
            }


            //{label : "Arrow", typename : 'ARROW', type : 6, subtype : 0},
            if(type.typename == 'ARROW'){
                //labelType = "Arrow";
                properties.lineColor = true;
                properties.lineWidth = true;
                properties.snap = true;

                properties.endpoint_open_single = true;
                properties.endpoint_open_both = true;
                properties.endpoint_filled_single = true;
                properties.endpoint_filled_both = true;

            }

            //{label : "Dimension Line", typename : 'DIMENSION', type : 7, subtype : 0},
            if(type.typename == 'DIMENSION'){
                //labelType = "Dimension Line";

                properties.lineColor = true;
                properties.lineWidth = true;
                properties.snap = true;

                properties.endpoints_bar = true;
                properties.endpoints_arrow = true;
                properties.labelsize = true;


            }

            
            
            
            //{label : "Space", typename : 'SPACE', type : 8, subtype : 1},
            if(type.typename == 'SPACE'){
                //labelType = "Space";
                properties.lineColor = true;
                properties.fillColor = true;
                properties.lineWidth = true;
                properties.transpacency = true;
                properties.snap = true;
                properties.fill = true;
                properties.customlabel = true;
                properties.labelsize = true;

            }

            //{label : "Area", typename : 'AREA', type : 8, subtype : 0},
            if(type.typename == 'AREA'){
                //labelType = "Area";
                properties.lineColor = true;
                properties.fillColor = true;
                properties.lineWidth = true;
                properties.transpacency = true;
                properties.snap = true;
                properties.fill = true;
                properties.customlabel = true;
                properties.labelsize = true;
                properties.showperimeter = true;


            }

            
            //{label : "Callout", typename : 'CALLOUT', type : 9, subtype : 1},
            if(type.typename == 'CALLOUT'){
                //labelType = "Callout";
                properties.lineColor = true;
                properties.fillColor = true;
                properties.lineWidth = true;
                properties.transpacency = true;
                properties.fill = true;
                properties.textColor = true;
                
                properties.fontName = true;
                properties.fontHeight = true;
                properties.fontBold = true;
                properties.fontItalic = true;
            }



            //{label : "Text", typename : 'TEXT', type : 9, subtype : 1},
            if(type.typename == 'TEXT'){
                //labelType = "Text";
                properties.lineColor = true;
                properties.fillColor = true;
                properties.lineWidth = true;
                properties.transpacency = true;
                properties.fill = true;
                properties.textColor = true;
                properties.rotate = true;
                
                properties.fontName = true;
                properties.fontHeight = true;
                properties.fontBold = true;
                properties.fontItalic = true;



            }
            
            //{label : "Note", typename : 'NOTE', type : 10, subtype : 0},
            if(type.typename == 'NOTE'){
                //labelType = "Note";


            }


            /*switch(subtype){
                case 0:
                    labelType.label = "Image";
                    labelType.type = 'IMAGE';
                    break;
                case 1:
                    labelType.label = "Symbol";
                    labelType.type = 'SYMBOL';
                    break;
                case 3:
                    labelType.label = "Button";
                    labelType.type = 'BUTTON';
                    break;
                case 4:
                    labelType.label = "Signature";
                    labelType.type = 'SIGNATURE';
                    break;
                case 12:
                    labelType.label = "Stamp";
                    labelType.type = 'IMAGESTAMP';
                    break;

                case 20:
                    labelType.label = "Link";
                    labelType.type = 'LINK';

                    break;

                case 21:
                    labelType.label = "InteractiveStamp";
                    labelType.type = 'InteractiveStamp';

                    break;
    
            }*/

            //{label : "Image", typename : 'IMAGE', type : 11, subtype : 0},
            if(type.typename == 'IMAGE' || type.typename == 'IMAGESTAMP' || type.typename == 'SYMBOL' ){
                //labelType = "Image";
                properties.rotate = true;

            }

           

            //{label : "Button", typename : 'BUTTON', type : 11, subtype : 4},
            
            
            //{label : "Stamp", typename : 'STAMP', type : 12, subtype : 0},
            if(type.typename == 'STAMP'){
                //labelType = "Stamp";
                properties.lineColor = true;
                properties.fillColor = true;
                properties.lineWidth = true;
                properties.transpacency = true;
                properties.fill = true;
                properties.rotate = true;

            }


            //{label : "Count", typename : 'COUNT', type : 13, subtype : 0}

            if(type.typename == 'COUNT'){
                //labelType = "Count";
                //properties.rotate = true;
                properties.fillColor = true;
                properties.countsize = true;
                properties.countshape = true;

            }

            if(type.typename == 'MEASUREARC'){
                properties.lineWidth = true;
                properties.lineColor = true;
            }
            //labelType.label = "MeasureArc";
            //labelType.type = 'MEASUREARC';


        

            return properties;

        
        }            

        function RxCore_getMarkupType(type, subtype){


        
            var labelType = {label : "", type : "", groups :[]};

            switch (type) {
                case 0:
                    if (subtype == 0) {
                        labelType.label = "Freehand pen";
                        labelType.type = 'PEN';
                    }
                    if (subtype == 1) {
                        labelType.label = "Eraser";
                        labelType.type = 'ERASER';
                    }
                    break;
                case 1:
                    if (subtype == 0) {
                        labelType.label = "Polygon";
                        labelType.type = 'POLYGON';
                    }
                    if (subtype == 1) {
                        labelType.label = "Polyline";
                        labelType.type = ['PEN','POLYLINE'];

                    }
                    if (subtype == 2) {
                        labelType.label = "Polygon";
                        labelType.type = 'POLYGON';
                    }
                    if (subtype == 3) {
                        labelType.label = "Measure Path";
                        labelType.type = 'PATH';
                    }

                    if (subtype == 4) {
                        labelType.label = "Measure angle";
                        labelType.type = 'ANGLE';
                    }

                    if (subtype == 5) {
                        labelType.label = "Measure angle";
                        labelType.type = 'ANGLEBCKWRD';
                    }
                    if (subtype == 6) {
                        labelType.label = "Measure segment";
                        labelType.type = 'SEGMENT';
                    }

                    if (subtype == 7) {
                        labelType.label = "Revision Cloud";
                        labelType.type = 'REVCLOUD';
                    }


                    break;
                case 2:
                    labelType.label = "Polycurve";
                    labelType.type = ['SHAPE', 'POLYCURVE'];

                    break;
                case 3:
                    if (subtype == 0) {
                        labelType.label = "Rectangle";
                        labelType.type = ['SHAPE', 'RECTANGLE'];
                    }
                    if (subtype == 1) {
                        labelType.label = "Rounded Rectangle";
                        labelType.type = ['SHAPE', 'ROUNDED_RECTANGLE'];
                    }
                    if (subtype == 3) {
                        labelType.label = "Marker";
                        labelType.type = 'MARKER';
                    }
                    if (subtype == 6) {
                        labelType.label = "Rectanglular";
                        labelType.type = 'AREARECT';
                    }
                    

                    break;
                case 4:
                    if (subtype == 0) {
                        labelType.label = "Oval";
                        labelType.type = ['SHAPE', 'ELLIPSE'];
                    }
                    if (subtype == 1) {
                        labelType.label = "Circle";
                        labelType.type = ['SHAPE', 'CIRCLE'];
                    }
                    break;
                case 5:
                    labelType.label = "Revision Cloud";
                    labelType.type = ['SHAPE', 'CLOUD'];

                    break;
                case 6:
                    if (subtype == 9) {
                        labelType.label = "Line";
                        labelType.type = ['NO_ENDS','ARROW'];
                    } else if(subtype == 6){

                        labelType.label = "Callout";
                        labelType.type = 'TEXT';

                    }else if (subtype == 0){
                        labelType.label = "Arrow";
                        labelType.type = ['OPEN_SINGLE_END', 'ARROW'];
                    }else if (subtype == 1){
                        labelType.label = "Arrow";
                        labelType.type = ['FILLED_SINGLE_END', 'ARROW'];
                    }else if (subtype == 2){
                        labelType.label = "Arrow";    
                        labelType.type = ['OPEN_BOTH_ENDS', 'ARROW'];
                    }else if (subtype == 3){
                        labelType.label = "Arrow";
                        labelType.type = ['FILLED_BOTH_ENDS', 'ARROW'];
                    }
                    else {
                        labelType.label = "Arrow";
                    }

                    break;
                case 7:
                    labelType.label = "Dimension Line";

                    if (subtype == 0){
                        labelType.type = ["BAR", 'DIMENSION'];
                    }else if (subtype == 1){
                        labelType.type = ["OPEN_ARROW",'DIMENSION'];
                    }
                    break;
                case 8:
                    if (subtype == 1) {
                        labelType.label = "Space";
                        labelType.type = 'SPACE';
                    }
                    else {
                        labelType.label = "Area";
                        labelType.type = 'AREA';
                    }
                    break;
                case 9:
                    
                    /*if (this.bhasArrow) {
                        labelType.label = "Callout";
                        labelType.type = 'TEXT';
                    }else{
                    }*/

                    if (subtype == 1) {
                        labelType.label = "Text";
                        labelType.type = 'TEXT';
                    }
                    else if (subtype == 2) {
                        labelType.label = "Callout";
                        labelType.type = 'CALLOUT';
                    }



                    break;
                case 11:


                    switch(subtype){
                        case 0:
                            labelType.label = "Image";
                            labelType.type = 'IMAGE';
                            break;
                        case 1:
                            labelType.label = "Symbol";
                            labelType.type = 'SYMBOL';
                            break;
                        case 3:
                            labelType.label = "Button";
                            labelType.type = 'BUTTON';
                            break;
                        case 4:
                            labelType.label = "Signature";
                            labelType.type = 'SIGNATURE';
                            break;
                        case 12:
                            labelType.label = "Stamp";
                            labelType.type = 'IMAGESTAMP';
                            break;

                        case 20:
                            labelType.label = "Link";
                            labelType.type = 'LINK';

                            break;
        
                        case 21:
                            labelType.label = "InteractiveStamp";
                            labelType.type = 'InteractiveStamp';

                            break;
            
                    }
                    //labelType.label = "Image";
                    //labelType.type = 'IMAGE';
                    

                    break;
                case 10:
                    labelType.label = "Note";
                    labelType.type = 'NOTE';
                    
                    break;
                case 12:
                    
                    labelType.label = "Stamp";
                    labelType.type = 'STAMP';

                    break;
                case 13:    
                    labelType.label = "Count";
                    labelType.type = 'COUNT';

                    //labelType.groups = [{"items": [{"type": "FILL","title": "Count","tooltip": "Count options","options":  {"groups":  [ {"items":  [ {"type": "FILL_COLOR","title": "Color","tooltip": "Fill Color"}]}]}}]}];
                    break;

                case 14:

                    labelType.label = "MeasureArc";
                    labelType.type = 'MEASUREARC';

                    //{label : "MeasureArc", typename : 'MEASUREARC', type : 14, subtype : 0}
                    break;


            }

        return labelType;

        
    }    

    function RxCore_getSymbolLibNum(){
        getNumSymblibs();
    }

    function RxCore_selectSymblib(num){
        selectSymblib(num);
    }

    function RxCore_selectSymbName(num){
        selectSymbname(num);
    }

    function RxCore_getnumSymbols(num){
        getnumSymbols(num);
    }

    function RxCore_getSymbolName(num,sname){
        getSymbolName(num,sname);

    }

    function RxCore_getSymbolLibPNGData(num,sname){
        getSymbolPNGData(num,sname);

    }


    function RxCore_UseCredentials(onOff){

        bUseCredentials = onOff;
    }

    function RxCore_getdivcontainer(){

        return rxcontainer;
    }

    function RxCore_ConsolidatedOnly(onOff){

        if(docpagNotReady()){
            return;
        }

        for (var curmarkup = 0; curmarkup < DocObj.markuplist.length; curmarkup++) {

            //var signdisplay = getSignState(DocObj.markuplist[curmarkup].signature);

            if (!DocObj.markuplist[curmarkup].consolidated) {
                DocObj.markuplist[curmarkup].display = onOff;
            }
        }
        drawmarkupAll(cntximg);
        DrawMarkupSelected(context);

    }

    function RxCore_getpagethumbnail(index){
        if(docpagNotReady()){
            return;
        }
        
        return DocObj.pages[index].thumbnailobj;


    }

    function RxCore_loadThumbnail(pagenumber){
        if(docpagNotReady()){
            return;
        }
        
        DocObj.loadThumbnail(pagenumber);        

    }

    function RxCore_loadThumbnailPDF(pagenumber){
        if(docpagNotReady()){
            return;
        }
        
        DocObj.loadThumbnailPDF(pagenumber);        
        

    }


    

    function RxCore_refreshThumbnails(){
        if(docpagNotReady()){
            return;
        }

        DocObj.refreshThumbnails();
    }

    function RxCore_set3DmodelcurFloor(floornum){

        if(docpagNotReady()){
            return;
        }

        DocObj.setbirdseyefloor(floornum);

    }

    function RxCore_setBirdseyeFloor(floornum){
        if(docpagNotReady()){
            return;
        }
        
        DocObj.setbirdseyefloor(floornum);

    }

    function RxCore_renderBirdseye(){

        if(docpagNotReady()){
            return;
        }
    
        DocObj.renderBirdsEye();

    } 
    function RxCore_SetBirdseyeCanvas(imgcnv, indcnv, mrkupcnv){
        if(docpagNotReady()){
            return;
        }

        var thumbnail = DocObj.getThumb(DocObj.currentpage);

        if(thumbnail == undefined){
            return;
        }

        thumbnail.setGUIbirdseyeCnv(imgcnv, indcnv, mrkupcnv);
     
    }    

    function RxCore_getfoxitThumbnail(pagenum){
        if(docpagNotReady()){
            return;
        }
        
        DocObj.getFoxitThumbnail(pagenum);        
    }

    function RxCore_rotateThumbnail(pagenum, degree){

        if(docpagNotReady()){
            return;
        }
        
        DocObj.rotateThumbnail(pagenum, degree);        


    }

    function RxCore_getfoxitThumbnails(){
        if(docpagNotReady()){
            return;
        }
        //DocObj.getFoxitThumbnails();
        
        DocObj.getFoxitThumbnails();
    }





    //RxCore.setBirdsEyeFoxit(thumbnail, pagenum);

    function RxCore_getBirdseyeDim(index, pw, ph){
        if(docpagNotReady()){
            return;
        }
        
        //DocObj.pages[index].foxitcreatethumb(thumbnail);

        return DocObj.thumbnails[index].getBirdseyeDim(pw, ph);

    }

    function RxCore_setPageBitmap(bitmap, index){
        if(docpagNotReady()){
            return;
        }
        
        if (documentcompare){

            CompareObj.setPageBitmap(bitmap, index);

        }else{
            
            DocObj.setPageBitmap(bitmap, index);        
            
        }


        //DocObj.pages[index].foxitcreatethumb(thumbnail);

        
    }

    function RxCore_setBirdsEyeFoxit(bitmap, index){
        if(docpagNotReady()){
            return;
        }
        
        //DocObj.pages[index].foxitcreatethumb(thumbnail);

        DocObj.thumbnails[index].foxitcreateBirdseye(bitmap);

    }

    function RxCore_setThumbnailFoxit(thumbnail, index){
        if(docpagNotReady()){
            return;
        }
        
        //DocObj.pages[index].foxitcreatethumb(thumbnail);

        DocObj.thumbnails[index].foxitcreatethumb(thumbnail);

    }

    function RxCore_SetThumbnailctx(ctx,index){
        if(docpagNotReady()){
            return;
        }
        //DocObj.pages[index].thumbctx = ctx;
        DocObj.thumbnails[index].thumbctx = ctx;

    }

    function RxCore_setThumbnailSize(w,h){
        ThumbnailWidth = w;
        ThumbnailHeight = h;

    }

    function RxCore_AbortPDFload(ok){
        bAbortPageload = ok;
        
        //RxCore_Closedocument();
        internalClosedoc();
        
    }

    function RxCore_deleteVectorAttribute(szAttribName){

        if(docpagNotReady()){
            return;
        }

        if (documentcompare) {

            return;
        }

        if (DocObj.getCurrentpage().usevectorxml) {
            DocObj.getCurrentpage().deleteVectorAttribute(szAttribName);
        }

        
    }

    function RxCore_AppendCustomBlockAttribute(blockid, name, value){
        if(docpagNotReady()){
            return;
        }

        if (documentcompare) {
            return;
        }

        if (DocObj.getCurrentpage().usevectorxml) {
            DocObj.getCurrentpage().appendCustomBlockAttribute(blockid , name, value);
        }


    }

    function RxCore_setRxSpaceColor(blockid , Color, override){
        if(docpagNotReady()){
            return;
        }
        if (documentcompare) {
            return;
        }

        var exist = setMarkupSpaceColor(blockid, Color, override);

    }

    function set3DBlockTransparency(blockid, value, override){
        if(docpagNotReady()){
            return;
        }
        if (documentcompare) {
            return;
        }

        if (DocObj.getCurrentpage().usevector3Dxml) {
            DocObj.getCurrentpage().set3DBlockTransparency(blockid , value, override);
        }

    }

    function set3DBlockNoSelect(blockid, override){
        if(docpagNotReady()){
            return;
        }
        
        /*if (!documentopen) {
            return;
        }*/

        if (documentcompare) {
            return;
        }

        if (DocObj.getCurrentpage().usevector3Dxml) {
            DocObj.getCurrentpage().set3DBlockNoSelect(blockid , override);
        }

    }


    function RxCore_setBlockFill(blockid , hatchname, override){

        if(docpagNotReady()){
            return;
        }
        if (documentcompare) {
            return;
        }

        var filltype = getFillTypeByName(hatchname);

        if(filltype == 'hatch'){
            RxCore_setBlockHatch(blockid , hatchname, override);
        }else if (filltype == 'color'){
            
            var Color = getFillColorByName(hatchname);
            setHatchinUse(hatchname, override);

            RxCore_setBlockColor(blockid , Color, override);
        }

    }


    function RxCore_setBlockHatch(blockid , hatchname, override){

        if(docpagNotReady()){
            return;
        }

        if (documentcompare) {
            return;
        }

        //var exist = setMarkupSpaceColor(blockid, Color, override);
        //var exist = setMarkupSpaceHatch(blockid, Color, override);
        var exist = setMarkupSpaceHatch(blockid, hatchname, override);


        if (!exist){
            if (DocObj.getCurrentpage().usevectorxml) {
                //DocObj.getCurrentpage().setBlockColor(blockid , Color, override);
                DocObj.getCurrentpage().setBlockHatch(blockid , hatchname, override);
            }

        }
        
    }

    function RxCore_set3DBlockDistance(blockid, distance, direction, override){
        if(docpagNotReady()){
            return;
        }
        if (documentcompare) {
            return;
        }

        if (DocObj.getCurrentpage().usevector3Dxml) {
            //DocObj.getCurrentpage().set3DBlockColor(blockid , Color, override);
            DocObj.getCurrentpage().set3DBlockDistance(blockid , distance, direction, override);
            
        }

    }


    function RxCore_set3DBlockPosition(blockid, position, override){
        if(docpagNotReady()){
            return;
        }
        if (documentcompare) {
            return;
        }

        if (DocObj.getCurrentpage().usevector3Dxml) {
            //DocObj.getCurrentpage().set3DBlockColor(blockid , Color, override);
            DocObj.getCurrentpage().set3DBlockPosition(blockid , position, override);
            
        }

    }


    function RxCore_setBlockColor(blockid , Color, override){
        if(docpagNotReady()){
            return;
        }
        if (documentcompare) {
            return;
        }

        var exist = setMarkupSpaceColor(blockid, Color, override);

        if (!exist){
            if (DocObj.getCurrentpage().usevectorxml) {
                DocObj.getCurrentpage().setBlockColor(blockid , Color, override);
            }
        }

        if (DocObj.getCurrentpage().usevector3Dxml) {
            DocObj.getCurrentpage().set3DBlockColor(blockid , Color, override);
        }

        

    }

    function RxCore_changeSpaceCustomLabel(blockid, newText){
        
        if(docpagNotReady()){
            return;
        }

        if (documentcompare) {
            return;
        }

        if (DocObj.getCurrentpage().usevector3Dxml) {
            return;
        }

        var markupobj = findSpaceMarkup(blockid);
        
        if(markupobj){   
            if(markupobj.busecustomlabel){
              markupobj.customlabeltext = newText;
            }
            
        }

        drawmarkupAll(cntximg);
        DrawMarkupSelected(context);

    }

    function RxCore_setCustomLabelText(text, onoff){
        if(docpagNotReady()){
            return;
        }

        if(readonlymode){
            return;
        }

        var markupobject = getSelectedmarkup();

        if(!markupobject.isempty){
            markupobject.customlabeltext = text;
            markupobject.busecustomlabel = onoff;
            
        }

        drawmarkupAll(cntximg);
        DrawMarkupSelected(context);

    }

    function RxCore_setCompareColors(bgcolor, fgcolor){

        if(docpagNotReady()){
            return;
        }


        overlayBGColor = bgcolor;
        overlayFGColor = fgcolor;


        var bhaveCompare = false;

        var lCompareObj = getCompareFile();
        if (lCompareObj != undefined){
            bhaveCompare = true;

            lCompareObj.setColors(bgcolor, fgcolor);
            //lCompareObj.scalecomparepage(alignarray);

        }

        /*var bhaveCompare = false;
        for (var i = 0; i < OpenFiles.length; i++) {
            if (OpenFiles[i].Type == 'Compare') {
                bhaveCompare = true;
            }
        }

        if(bhaveCompare){
            CompareObj.setColors(bgcolor, fgcolor);
        }*/




    }


    function RxCore_SetCompareFiles(index, state) {

        if(index == undefined){
            return;
        }
        //var filelist = RxCore_GetOpenFiles();

        if (OpenfileNames.length < index + 1 || OpenfileNames.length == 0) {
            var filelist = RxCore_GetOpenFiles();
            if(filelist.length > index){
                OpenfileNames[index].state = state;
            }
            
        }else{
            OpenfileNames[index].state = state;
            //var filelist = RxCore_GetOpenFiles();
        }

        //make sure only 1 background and 1 overlay
        for (var i = 0; i < OpenfileNames.length; i++) {
            if (OpenfileNames[i].state == state && i != index) {
                OpenfileNames[i].state = 0;
            }
        }

        if (RxCore_GUI_CompareDiag != undefined) {
            RxCore_GUI_CompareDiag.CompareDialog();
        }


    }

    function RxCore_draw3DBirdseye(){
        if(docpagNotReady()){
            return;
        }

        if(DocObj.getCurrentpage().bis3Dnavigator){
            //DocObj.getCurrentpage().setAs3DNavigator(floorplanobjects, docpage.floorindex);
            DocObj.getCurrentpage().drawcamerapos();

        }
    }
    

    function RxCore_drawFoxitAnnotation(onOff){

        bDrawToFoxitonly = onOff;
    }

    function RxCore_setpageAs3DNavigator(docpage, floorplanobjects){
        if(docpagNotReady()){
            return;
        }

        if(OpenFiles[docpage.docindex].pages[docpage.pageindex]){
            OpenFiles[docpage.docindex].pages[docpage.pageindex].setAs3DNavigator(floorplanobjects, docpage.floorindex);
        }
    }

    function RxCore_getFloorplanList(fileindex){
        var floorplans = [];
        
        if (!documentopen) {
            return floorplans;
        }
        if(OpenFiles[fileindex].pages[0].usevector3DGLTF){
            var floorplans = OpenFiles[fileindex].getFloorplans();
            
        }
        return floorplans;
    }
    
    function RxCore_openFloorplanFile(fileindex, floornum){
        if(docpagNotReady()){
            return;
        }

        if(OpenFiles[fileindex].pages[0].usevector3DGLTF){
            var floorplans = OpenFiles[fileindex].getFloorplans();
            if(floorplans.length > 0){
                for(var fi = 0; fi < floorplans.length;fi++){
                    if(floorplans[fi].index == floornum){
                        RxCore_openFile(floorplans[fi].SRC);
                        break;
                    }
                }
            }

        }
    }


    function RxCore_GetOpenFiles() {

        /*
         state
         0 = unused;
         1 = background;  //
         2 = overlay;
         */
        var fileExist = false;

        for (var i = 0; i < OpenFiles.length; i++) {

            fileExist = false;

            var isImage = (!OpenFiles[i].pages[0].DocRef.pages[OpenFiles[i].pages[0].DocRef.currentpage].usepdfjs
                && !OpenFiles[i].pages[0].DocRef.pages[OpenFiles[i].pages[0].DocRef.currentpage].usevector3Dxml
                && !OpenFiles[i].pages[0].DocRef.pages[OpenFiles[i].pages[0].DocRef.currentpage].usevectorxml);

            if (OpenFiles[i].Type == 'Compare') {
                var OpenFileObject = {
                    index:OpenFiles[i].pages[0].DocRef.fileindex,
                    Name:OpenFiles[i].pages[0].DocRef.FileName,
                    Type:OpenFiles[i].pages[0].DocRef.Type,
                    Is2D:OpenFiles[i].pages[0].DocRef.pages[OpenFiles[i].pages[0].DocRef.currentpage].usevectorxml,
                    is3D:OpenFiles[i].pages[0].DocRef.pages[OpenFiles[i].pages[0].DocRef.currentpage].usevector3Dxml,
                    isImage : isImage,
                    isPDF : OpenFiles[i].pages[0].DocRef.pages[OpenFiles[i].pages[0].DocRef.currentpage].usepdfjs,
                    state:1
                };
                OpenfileNames.push(OpenFileObject);
                isImage = (!OpenFiles[i].pages[0].DocRef.pages[OpenFiles[i].pages[0].DocRef.currentpage].usepdfjs
                    && !OpenFiles[i].pages[0].DocRef.pages[OpenFiles[i].pages[0].DocRef.currentpage].usevector3Dxml
                    && !OpenFiles[i].pages[0].DocRef.pages[OpenFiles[i].pages[0].DocRef.currentpage].usevectorxml);

                OpenFileObject = {
                    index:OpenFiles[i].pages[1].DocRef.fileindex,
                    Name:OpenFiles[i].pages[1].DocRef.FileName,
                    Type:OpenFiles[i].pages[1].DocRef.Type,
                    Is2D:OpenFiles[i].pages[1].DocRef.pages[OpenFiles[i].pages[1].DocRef.currentpage].usevectorxml,
                    is3D:OpenFiles[i].pages[0].DocRef.pages[OpenFiles[i].pages[0].DocRef.currentpage].usevector3Dxml,
                    isImage : isImage,
                    isPDF : OpenFiles[i].pages[0].DocRef.pages[OpenFiles[i].pages[0].DocRef.currentpage].usepdfjs,
                    state:2
                };
                OpenfileNames.push(OpenFileObject);
            } else {
                var statecmp = 0;
                for (var j = 0; j < CompareFiles.length; j++) {
                    if (OpenFiles[i].FileName == CompareFiles[j].Name) {
                        statecmp = CompareFiles[j].state;
                    }
                }
                isImage = (!OpenFiles[i].pages[OpenFiles[i].currentpage].usepdfjs
                    && !OpenFiles[i].pages[OpenFiles[i].currentpage].usevector3Dxml
                    && !OpenFiles[i].pages[OpenFiles[i].currentpage].usevectorxml);

                OpenFileObject = {
                    index:OpenFiles[i].fileindex,
                    Name:OpenFiles[i].FileName,
                    Type:OpenFiles[i].Type,
                    Is2D:OpenFiles[i].pages[OpenFiles[i].currentpage].usevectorxml,
                    is3D:OpenFiles[i].pages[OpenFiles[i].currentpage].usevector3Dxml,
                    isImage : isImage,
                    isPDF : OpenFiles[i].pages[0].DocRef.pages[OpenFiles[i].pages[0].DocRef.currentpage].usepdfjs,
                    state:statecmp
                };

                for (var k = 0; k < OpenfileNames.length; k++) {
                    if (OpenFileObject.index == OpenfileNames[k].index) {
                        fileExist = true;
                    }
                }

                if (!fileExist) {
                    OpenfileNames.push(OpenFileObject);
                    fileExist = false;
                }


            }

        }


        return OpenfileNames;

    }

    function RxCore_getActiveFileFloorlevel(){
        
        var curfloor = null;

        if (documentopen) {
            if(DocObj.getCurrentpage().bis3Dnavigator){
                curfloor = DocObj.getCurrentpage().getFloorLevel();
            }
        }

        return curfloor;        

    } 

    function RxCore_getActiveFile(){
        var activefile = null;

        for (var i = 0; i < OpenFiles.length; i++) {
            if(OpenFiles[i].bActive){
                activefile = i;
             }
        }
        return activefile;
    }


    function RxCore_showDrawCanvas(){
        //hidedisplayCanvas
    }
    

    function RxCore_bringIframeToFront(iframename){

        bringIframeToFront(iframename); 

    }



    function RxCore_getFoxitIframeID(indx){
        
        var iframename = null;

        if(OpenFiles.length == 0){
            return null;
        }
        
        if (OpenFiles[indx].foxitdoc != null){     
            iframename = OpenFiles[indx].foxitdoc.iframename;
        }else{
            iframename = null;
            //set all iframes to invisible
        }
        
        return iframename;

    }

    function RxCore_getDocFileName(){

        if(DocObj){
            return DocObj.FileName;
        }else{
            return "empty";
        }


    }

    function RxCore_getOpenFilesList() {
        var openFilesList = [];

        for (var i = 0; i < OpenFiles.length; i++) {
            openFilesList.push({
                id: OpenFiles[i].id,
                index: OpenFiles[i].fileindex,
                name: OpenFiles[i].FileName || OpenFiles[i].Type,
                isActive: OpenFiles[i].bActive,
            })
        }
        return openFilesList;
    }

    function RxCore_forcepagesState(pagestate){
        
      //pagestate.numOpenFiles = OpenFiles.length;
         
      if (RxCore_GUI_State != undefined) {
          RxCore_GUI_State.setGUIState(pagestate);
      }


    }

    function RxCore_getfloorplanDocs(floorelvobj){
        
        //find all 3d navigation pages and check elevation

        var docpages = [];
        var doc = null;
        var page = null;
        var curfloor = null;

        for (var i = 0; i < OpenFiles.length; i++) {


            if (OpenFiles[i].pages.length > 0){

                for(var pi = 0; pi < OpenFiles[i].pages.length; pi++){
                    
                    curfloor = OpenFiles[i].pages[pi].getFloorLevel();
                    if(floorelvobj.floorindex == curfloor.floorindex){
                        docpages.push({docindex : i, pageindex : pi, floorindex : curfloor.floorindex, replaced : curfloor.replaced});
                    }
                    //docpages.push({docindex : i, page : pages[pi]});
                }
            }

        }

        return docpages;

    }    

    function setActiveFileEx(indx){

        CheckAllfilesRendering().then(RxCore_setActiveFile(indx));

    }


    function RxCore_setActiveFile(indx){
        context.clearRect(0, 0, canvas.width, canvas.height);
        cntximg.clearRect(0, 0, canvas.width, canvas.height);

        var activefileindex = RxCore_getActiveFile();
        
        if(activefileindex != null){
            if (OpenFiles[activefileindex].Type == 'Compare') {
                //OpenFiles[activefileindex].storePDFScales();
            }
        }

        if(DocObj){
            DocObj.enableTextSelect(false);
        }
        

        if (OpenFiles.length >= 1 && OpenFiles[indx] != undefined) {


            bAnimateready = true;
            DocObj = OpenFiles[indx];
            
            DocObj.bSopenSilent = false;

            if (bPDFtemprender && !DocObj.pdfDoc) {
                bPDFtemprender = false;
            }
            if (DocObj.pages && DocObj.pages.length) {
                DocObj.pages[0].usedincomposite = false;
            }
            
            if (!DocObj.getCurrentpage().usevector3Dxml){
                if (RxCore_GUI_3DWalkthrough != undefined) {

                    if(bIsmobile){
                        RxCore_GUI_3DWalkthrough.setWalkthroughGUI(false);
                        //$('div.wheelPanel').css('visibility', 'hidden');
                    }
                }
            }


            DeactivateAll();
            DocObj.SetActive();
            //if using foxit turn display canvas and 3d canvas off
            if (DocObj.foxitdoc != null){
                RxCore_hidedisplayCanvas(true);
            }else{
                RxCore_hidedisplayCanvas(false);


            }
            /*if (OpenFiles[indx].Type == 'Compare') {
                CompareObj = OpenFiles[indx];
                CompareObj.bSopenSilent = false;
                
                for (var i = 0; i < OpenFiles.length; i++) {
                    if (OpenFiles[i].Type !== 'Compare' && OpenFiles[i].pages.length) {
                        for (var j = 0; j < OpenFiles[i].pages.length; j++) {
                            OpenFiles[i].pages[j].usedincomposite = true;
                        }
                    }

                }
                DeactivateAll();
                CompareObj.SetActive();

            } else{
               

            }*/

        }

    }

    function RxCore_setWalkthroughDiv(HTMLElement) {

        if(docpagNotReady()){
            return;
        }

        regEvents(HTMLElement);
        //regEvents($(this.touchCake)[0]);

    }

    function RxCore_3DOrbit() {
        if(docpagNotReady()){
            return;
        }
        if (DocObj.getCurrentpage().usevector3Dxml) {
            //only reset camera if current tool is walkthrough
            if (tool == undefined) {
                set_tool('orbitControl', {});
                //tool = new tools['orbitControl']();
                DocObj.curcontrol3D = 'orbitControl';
            }

            if (tool.name == 'Walkthrough3D') {

                //DocObj.getCurrentpage().walkthroughcontrol.enabled = false;
                DocObj.getCurrentpage().walkthroughcontrol.isLocked = false;

                /*camerax = 0;
                cameray *= 2;
                cameraz = 0;
                var format = DocObj.Format.substring(0, 3);
                if (format == 'IFC') {
                    var camerax = (DocObj.getCurrentpage().Vector3DPageObj.w / 2) * DocObj.getCurrentpage().Vector3DPageObj.scale;
                    var cameray = -(DocObj.getCurrentpage().Vector3DPageObj.h) * DocObj.getCurrentpage().Vector3DPageObj.scale;
                    var cameraz = (DocObj.getCurrentpage().Vector3DPageObj.d / 2) * DocObj.getCurrentpage().Vector3DPageObj.scale;

                    camerax = 0;
                    cameray *= 2;
                    cameraz = 0;


                    DocObj.getCurrentpage().camera.up.set(0, 0, 1);
                    //DocObj.getCurrentpage().camera.rotation.set(90 * Math.PI / 180, 0, 0);

                } else {

                    camerax = (DocObj.getCurrentpage().Vector3DPageObj.w / 2) * DocObj.getCurrentpage().Vector3DPageObj.scale;
                    cameray = (DocObj.getCurrentpage().Vector3DPageObj.h / 2) * DocObj.getCurrentpage().Vector3DPageObj.scale;
                    cameraz = (DocObj.getCurrentpage().Vector3DPageObj.d) * DocObj.getCurrentpage().Vector3DPageObj.scale;

                    camerax = 0;
                    cameray = 0;
                    cameraz *= 2;

                    DocObj.getCurrentpage().camera.up.set(0, 1, 0);
                    //DocObj.getCurrentpage().camera.rotation.set(0, 90 * Math.PI / 180, 0);


                }


                DocObj.getCurrentpage().camera.position.set(camerax, cameray, cameraz);
                DocObj.getCurrentpage().camera.lookAt(new THREE.Vector3(0,0,0));*/

            }
            if (RxCore_GUI_3DWalkthrough != undefined) {
                if(bIsmobile){
                    RxCore_GUI_3DWalkthrough.setWalkthroughGUI(false);
                }
                
                //$('div.wheelPanel').css('visibility', 'hidden');
            }

            set_tool('orbitControl', {});
            //tool = new tools['orbitControl']();
            DocObj.curcontrol3D = 'orbitControl';
        }

    }

    function RxCore_3DWalkThrough(selected) {
        if(docpagNotReady()){
            return;
        }

        if (DocObj.getCurrentpage().usevector3Dxml) {
            if (selected) {
                if (RxCore_GUI_3DWalkthrough != undefined) {

                    if(bIsmobile){
                        RxCore_GUI_3DWalkthrough.setWalkthroughGUI(true);
                        //$('div.wheelPanel').css('visibility', 'visible');
    
                    }
                }

                //DocObj.getCurrentpage().walkthroughcontrol.reset();
                set_tool('Walkthrough3D', {});
                //tool = new tools['Walkthrough3D']();



                /*var format = DocObj.Format.substring(0, 3);
                if (format == 'IFC') {
                    var camerax = (DocObj.getCurrentpage().Vector3DPageObj.w / 2) * DocObj.getCurrentpage().Vector3DPageObj.scale;
                    var cameray = -(DocObj.getCurrentpage().Vector3DPageObj.h) * DocObj.getCurrentpage().Vector3DPageObj.scale;
                    var cameraz = (DocObj.getCurrentpage().Vector3DPageObj.d / 2) * DocObj.getCurrentpage().Vector3DPageObj.scale;

                    camerax = 0;
                    //cameray *= 2;
                    cameraz = 0;


                    DocObj.getCurrentpage().camera.up.set(0, 0, 1);
                    //DocObj.getCurrentpage().camera.rotation.set(90 * Math.PI / 180, 0, 0);

                } else {
                    camerax = (DocObj.getCurrentpage().Vector3DPageObj.w / 2) * DocObj.getCurrentpage().Vector3DPageObj.scale;
                    cameray = (DocObj.getCurrentpage().Vector3DPageObj.h / 2) * DocObj.getCurrentpage().Vector3DPageObj.scale;
                    cameraz = (DocObj.getCurrentpage().Vector3DPageObj.d) * DocObj.getCurrentpage().Vector3DPageObj.scale;

                    camerax = 0;
                    cameray = 0;
                    //cameraz = 0;

                    DocObj.getCurrentpage().camera.up.set(0, 1, 0);
                    //DocObj.getCurrentpage().camera.rotation.set(0, 90 * Math.PI / 180, 0);


                }


                DocObj.getCurrentpage().camera.position.set(camerax, cameray, cameraz);
                DocObj.getCurrentpage().camera.lookAt(new THREE.Vector3(0,0,0));*/
                DocObj.curcontrol3D = 'Walkthrough3D';
            } else {
                DocObj.getCurrentpage().walkthroughcontrol.firstxyrecieved = false;
                RxCore_3DOrbit();
            }


        }
    }
    function RxCore_NoSelect3D(reset){

        if(docpagNotReady()){
            return;
        }
        
        /*if (!documentopen) {
            return;
        }*/

        if (DocObj.getCurrentpage().usevector3Dxml) {
            DocObj.getCurrentpage().Vector3DPageObj.setNoSelectAll(reset);
        }


    }    
    function RxCore_3DTransparencyEx(value) {
        if(docpagNotReady()){
            return;
        }
        if (DocObj.getCurrentpage().usevector3Dxml) {
            DocObj.getCurrentpage().Vector3DPageObj.setTransparencyEx(value);
        }

    }

    function RxCore_3DTransparency(value) {
        if(docpagNotReady()){
            return;
        }
        if (DocObj.getCurrentpage().usevector3Dxml) {
            DocObj.getCurrentpage().Vector3DPageObj.setTransparency(value);
        }

    }

    function RxCore_3DTransparentOn() {
        if(docpagNotReady()){
            return;
        }
        if (DocObj.getCurrentpage().usevector3Dxml) {
            DocObj.getCurrentpage().Vector3DPageObj.setTransparency(0.5);
        }

    }


    function RxCore_3DExplodedistance(value) {
        if(docpagNotReady()){
            return;
        }
        if (DocObj.getCurrentpage().usevector3Dxml && DocObj.getCurrentpage().Vector3DPageObj.doexplode) {

            DocObj.getCurrentpage().Vector3DPageObj.newExplode(value);
            //DocObj.getCurrentpage().Vector3DPageObj.explode(value);
        }

    }

    function RxCore_restore3DInput(name){
        
        if(docpagNotReady()){
            return;
        }
        
        /*if (!documentopen) {
            return;
        }*/


        if (DocObj.getCurrentpage().usevector3Dxml) {

            var restorectrl = DocObj.getCurrentpage().Vector3DPageObj.getRestoreControl();
            if(restorectrl){

                switch (restorectrl) {
                    case 'Walkthrough3D' : 
                        RxCore_3DWalkThrough(true);
                        break;
                    case 'orbitControl' :
                        RxCore_3DOrbit(true);
                        break;
                }
                
            }
            //DocObj.getCurrentpage().getrestoretool()

        }

    }    

    function RxCore_create3DMarkup(select){
        
        if(docpagNotReady()){
            return;
        }
        
        /*if (!documentopen) {
            return;
        }*/
        
        var bnot3dmarkup = get3DViewNotActive();

        if(bnot3dmarkup){
            RxCore_Save3DCamera();
        }else{
            RxCore_restore3DInput();
        }
        return !bnot3dmarkup;

    }

    function RxCore_getCameraView(name){


        var camview = undefined;

        if(docpagNotReady()){
            return camview;
        }

        if (DocObj.getCurrentpage().usevector3Dxml) {

            camview = DocObj.getCurrentpage().Vector3DPageObj.getCameraView(name);
            
        }

        return camview;

        
    }    

    function RxCore_deleteCamera(name){

        if(docpagNotReady()){

            return;
        }

        /*if (!documentopen) {
            return;
        }*/

        if (DocObj.getCurrentpage().usevector3Dxml) {

            DocObj.getCurrentpage().Vector3DPageObj.deleteCameraView(name);
            
        }

        
    }

    function RxCore_SaveTop3DCamera(){
        
        if(docpagNotReady()){
            return;
        }
        
        /*if (!documentopen) {
            return;
        }*/


        var page = DocObj.getCurrentpage();

        if(page){

            if (page.usevector3Dxml) {
                page.Vector3DPageObj.saveResetCam();
                
            }
    
        }

    }
    
    function RxCore_findViewAttribute(szName, szValue, viewname){

        if(docpagNotReady()){
            return;
        }
        
        /*if (!documentopen) {
            return;
        }*/

        var page = DocObj.getCurrentpage();

        if(page){

            if (page.usevector3Dxml) {
                page.Vector3DPageObj.findViewAttribute(szName, szValue, viewname);
            }
        }

    }

    function RxCore_updateViewAttribute(szName, newszValue, viewname){

        if(docpagNotReady()){
            return;
        }
        
        /*if (!documentopen) {
            return;
        }*/

        var page = DocObj.getCurrentpage();

        if(page){

            if (page.usevector3Dxml) {
                page.Vector3DPageObj.updateViewAttribute(szName, newszValue, viewname);
            }
        }

    }

    function RxCore_deleteViewAttribute(szName, viewname){

        if(docpagNotReady()){
            return;
        }
        
        /*if (!documentopen) {
            return;
        }*/

        var page = DocObj.getCurrentpage();

        if(page){

            if (page.usevector3Dxml) {
                page.Vector3DPageObj.deleteViewAttribute(szName, viewname);
            }
        }

    }    

    function RxCore_getViewAttributes(viewname){

        if(docpagNotReady()){
            return;
        }
        
        /*if (!documentopen) {
            return;
        }*/

        var page = DocObj.getCurrentpage();

        var attribs = undefined;

        if(page){


            if (page.usevector3Dxml) {

                attribs = page.Vector3DPageObj.getViewAttributes(viewname);
                
            }
        }

        return attribs;

    }
    function RxCore_addViewAttribute(szName, szValue, viewname){

        if(docpagNotReady()){
            return;
        }
        
        /*if (!documentopen) {
            return;
        }*/

        var page = DocObj.getCurrentpage();

        if(page){

            if (page.usevector3Dxml) {
                page.Vector3DPageObj.addViewAttribute(szName, szValue, viewname);
            }
        }

    }    

    function RxCore_getViewAttribute(szName, viewname){

        if(docpagNotReady()){
            return;
        }
        
        /*if (!documentopen) {
            return;
        }*/

        var page = DocObj.getCurrentpage();

        if(page){

            if (page.usevector3Dxml) {
                page.Vector3DPageObj.getViewAttribute(szName, viewname);
            }
        }

    }        


    function RxCore_Save3DCamera(){
        if(docpagNotReady()){
            return;
        }

        var page = DocObj.getCurrentpage();

        if(page){

            if (page.usevector3Dxml) {
                page.Vector3DPageObj.saveCamera();
            }
        }

    }

    function RxCore_restoreCamera(){
        if(docpagNotReady()){
            return;
        }
        if (DocObj.getCurrentpage().usevector3Dxml) {

            DocObj.getCurrentpage().Vector3DPageObj.restoreCamera();
            //tool = new tools['select3d']();
        }


    }

    function RxCore_get3DResetView(){
        if(docpagNotReady()){
            return;
        }
        
        if (DocObj.getCurrentpage().usevector3Dxml) {
            return DocObj.getCurrentpage().get3DResetView();
        }


    }

    function RxCore_getCameraimage(name){


        var camview = null;

        if(docpagNotReady()){
            return camview;
        }

        if (DocObj.getCurrentpage().usevector3Dxml) {

            camview = DocObj.getCurrentpage().Vector3DPageObj.getCameraimage(name);
            
        }

        return camview;


        /*if(docpagNotReady()){
            return;
        }*/

        /*if (!documentopen) {
            return;
        }*/

        /*if (DocObj.getCurrentpage().usevector3Dxml) {
            return DocObj.getCurrentpage().getCameraimage(name);
        }*/

    }    

    function RxCore_set3DViewSelectedMesh(name){

        if(docpagNotReady()){
            return;
        }

        var page = DocObj.getCurrentpage();

        if(page){

    
            if (page.usevector3Dxml) {
                page.set3DViewSelectedMesh(name);
                
            }
    
        }


        
    }

    function RxCore_restoreCameraByName(name){
        
        if(docpagNotReady()){
            return;
        }
        
        /*if (!documentopen) {
            return;
        }*/

        var page = DocObj.getCurrentpage();

        if(page){

            if (page.usevector3Dxml) {
                page.unSelect3DmarkupAll();
            }
    
            if (page.usevector3Dxml) {
                page.restoreCameraByName(name);
            }
    
        }


    }


    function RxCore_3DExplode(onoff) {
        if(docpagNotReady()){
            return;
        }

        if (DocObj.getCurrentpage().usevector3Dxml) {

            DocObj.getCurrentpage().Vector3DPageObj.doexplode = onoff;
            //tool = new tools['select3d']();
        }


    }


    function RxCore_3dClipping(onoff,plane,value){
        if(docpagNotReady()){
            return;
        }
        if (DocObj.getCurrentpage().usevector3Dxml) {

            DocObj.getCurrentpage().Vector3DPageObj.clipping(plane,value,onoff);
            //tool = new tools['select3d']();
        }

    }

    function RxCore_getScreenRect(){
        
        var screenrect = undefined;
        
        if(docpagNotReady()){
            return;
        }

        if (DocObj.getCurrentpage().usevectorxml) {

            screenrect = DocObj.getCurrentpage().screentoDocRect({x: 0, y: 0, w: canvasowidth, h :canvasoheight});
            
        }
        return screenrect;
    }

    function RxCore_getlayerlist(){

        var layerlist = undefined;

        if(docpagNotReady()){
            return;
        }

        if (DocObj.getCurrentpage().usevectorxml) {
            
            layerlist = DocObj.getCurrentpage().getlayerlist();
        }

        return layerlist;
    }



    function RxCore_ChangePDFLayer(id, visible){
        if(docpagNotReady()){
            return;
        }
        
        DocObj.turnPDFLayerOnOff(id, visible);

    }



    function RxCore_ChangeVectorLayer(index) {
        if(docpagNotReady()){
            return;
        }
        DocObj.getCurrentpage().turnLayerOnOff(index);
    }

    function RxCore_resetLayers() {
        if(docpagNotReady()){
            return;
        }
        if (DocObj.getCurrentpage().usevectorxml) {
            DocObj.getCurrentpage().resetLayers();
        }

    }

    function RxCore_LayersAll(OnOff){
        if(docpagNotReady()){
            return;
        }
        if (DocObj.getCurrentpage().usevectorxml) {
            DocObj.getCurrentpage().turnLayerAllOnOff(OnOff);
        }


    }

    function RxCore_BlocksAll(OnOff){
        if(docpagNotReady()){
            return;
        }
        if (DocObj.getCurrentpage().usevectorxml) {
            DocObj.getCurrentpage().turnBlockAllOnOff(OnOff);
        }


    }

    function RxCore_BlockLoadMask(szBlockLoadMask){
        if(docpagNotReady()){
            return;
        }
        if (DocObj.getCurrentpage().usevectorxml) {
            DocObj.getCurrentpage().filterBlocks(szBlockLoadMask);
        }

    }

    function RxCore_AttributeMask(szAttributeLoadMask){
        if(docpagNotReady()){
            return;
        }
        if (DocObj.getCurrentpage().usevectorxml) {
            DocObj.getCurrentpage().filterAttributes(szAttributeLoadMask);
        }

    }    

    function RxCore_get2DVectorfilterAttributes(){
        if(docpagNotReady()){
            return;
        }
        if (DocObj.getCurrentpage().usevectorxml) {
            return DocObj.getCurrentpage().getFilteredAttributes();
        }

    }

    function RxCore_get2DVectorBlocks(){
        if(docpagNotReady()){
            return;
        }

        /*if (documentcompare) {
            return;
        }*/


        if (DocObj.getCurrentpage().usevectorxml) {
            return DocObj.getCurrentpage().getBlocks();
        }

    }

    function RxCore_get2DVectorPentable(){
        if(docpagNotReady()){
            return;
        }
        
        /*if (!documentopen) {
            return;
        }*/

        if (DocObj.getCurrentpage().usevectorxml) {

            return DocObj.getCurrentpage().getPenTable();
        }

    }


    function RxCore_get2DVectorBlockswFill(){
        
        var blocklistwfill = undefined;
        
        if(docpagNotReady()){
            return;
        }

        if (documentcompare) {
            return;
        }

        if (DocObj.getCurrentpage().usevectorxml) {
            blocklistwfill = DocObj.getCurrentpage().getBlockswFill();
            
        }

        return blocklistwfill;

    }


    function RxCore_get3DVectorBlocks() {
        if(docpagNotReady()){
            return;
        }

        if (DocObj.getCurrentpage().usevector3Dxml) {
            
            return DocObj.getCurrentpage().get3dVectorBlocks();
            
        }

    }    

    function RxCore_markVectorBlock(blockid){
        if(docpagNotReady()){
            return;
        }
        if (DocObj.getCurrentpage().usevectorxml) {
            DocObj.getCurrentpage().markVectorBlock(blockid);
        }

    }

    function RxCore_getBlockAttributes(blockid){
        if(docpagNotReady()){
            return;
        }
        if (DocObj.getCurrentpage().usevectorxml) {
            return DocObj.getCurrentpage().blockAttributes(blockid);
        }

    }

    function RxCore_createAreaAttributes(szAttname){
        if(docpagNotReady()){
            return;
        }
        if (DocObj.getCurrentpage().usevectorxml) {
            DocObj.getCurrentpage().createAreaAttributes(szAttname);
        }
    }

    function RxCore_setVectorBlockArea(partindex, foundblock, szAttname){
        if(docpagNotReady()){
            return;
        }
        if (DocObj.getCurrentpage().usevectorxml) {
            DocObj.getCurrentpage().setVectorBlockArea(partindex, foundblock, szAttname);
        }
    }


    function RxCore_replacefloorplan(sourceindex, targetdocindex, floornum){
        if(docpagNotReady()){
            return;
        }
        if(OpenFiles[targetdocindex].pages[0].usevector3DGLTF){
                //OpenFiles[targetdocindex].create3DFloorplan;
                //var callback = OpenFiles[targetdocindex].create3DFloorplan;
                //DocObj.replaceFloorNav(OpenFiles[targetdocindex].pages[0], floornum);
                OpenFiles[sourceindex].replaceFloorNav(OpenFiles[targetdocindex].setfloorplanNav, floornum);
        }

    }

    function RxCore_get3DmodelboxCenter(targetdocindex){
        var boxCenter = null;


        if(!docpagNotReady()){
            if(OpenFiles[targetdocindex].pages[0].usevector3DGLTF){
                //OpenFiles[targetdocindex].create3DFloorplan;
                //var callback = OpenFiles[targetdocindex].create3DFloorplan;
                //DocObj.replaceFloorNav(OpenFiles[targetdocindex].pages[0], floornum);
                boxCenter = OpenFiles[targetdocindex].pages[0].Vector3DPageObj.boxCenter;
                
             }

        }

        return boxCenter;

    }
    
    function RxCore_set3dNavcameraPos(position, rotation,boxCenter){
        if(docpagNotReady()){
            return;
        }
        if(!boxCenter){
            return;
        }
        
        DocObj.setpagecamerapos(position, rotation,boxCenter);

    }

    function RxCore_get3DmodelcurFloor(){
        if(docpagNotReady()){
            return;
        }

        if (DocObj.getCurrentpage().usevector3Dxml) {
            
            return DocObj.getCurrentpage().Vector3DPageObj.checkcurfloor();
            
        }
        
    }


    function RxCore_getmodelBox(){
        if(docpagNotReady()){
            return;
        }

        if(DocObj.getCurrentpage().usevector3DGLTF){
            return DocObj.getCurrentpage().Vector3DPageObj.getmodelBox();
        }

    }

    function RxCore_getmodelUnit(){
        if(docpagNotReady()){
            return;
        }

        if(DocObj.getCurrentpage().usevector3DGLTF){
            return DocObj.getCurrentpage().Vector3DPageObj.getmodelUnit();
        }

    }


    function RxCore_getModelCenter(){

        if(docpagNotReady()){
            return;
        }

        if(DocObj.getCurrentpage().usevector3DGLTF){
            return DocObj.getCurrentpage().Vector3DPageObj.getModelCenter();
        }
        
        
    }
    function RxCore_getCamNavPosition(targetdocindex, pagenum, modelinfo, mousePos){

        if(docpagNotReady()){
            return;
        }


        return OpenFiles[targetdocindex].pages[pagenum].getnavcampos(modelinfo.unit, modelinfo.boxCenter, mousePos);
        



    }


    function RxCore_setnavScaleOffsetPage(alignarray){
        if(docpagNotReady()){
            return;
        }

        DocObj.getCurrentpage().setFloorNavScaleOffset(alignarray);

    }
    function RxCore_setnavigationScaleOffset(targetdocindex, floornum, alignarray){
        if(docpagNotReady()){
            return;
        }

        if(targetdocindex == undefined){
            return;
        }
        if(floornum == undefined){
            return;
        }

        if(OpenFiles[targetdocindex].pages[0].usevector3DGLTF){
                //OpenFiles[targetdocindex].create3DFloorplan;
                //var callback = OpenFiles[targetdocindex].create3DFloorplan;
                //DocObj.replaceFloorNav(OpenFiles[targetdocindex].pages[0], floornum);
                OpenFiles[targetdocindex].setFloorNavScaleOffset(floornum, alignarray);
        }
    }
    function RxCore_replacefloorplanfromPage(targetdocindex, floornum){
        if(docpagNotReady()){
            return;
        }

        if(OpenFiles[targetdocindex].pages[0].usevector3DGLTF){
                //OpenFiles[targetdocindex].create3DFloorplan;
                //var callback = OpenFiles[targetdocindex].create3DFloorplan;
                //DocObj.replaceFloorNav(OpenFiles[targetdocindex].pages[0], floornum);
                DocObj.replaceFloorNav(OpenFiles[targetdocindex].setfloorplanNav, floornum);
        }

    }

    function RxCore_get3dFloorplanCanvas(floornum, alignarray){
        if(docpagNotReady()){
            return;
        }

        //OpenFiles[targetdocindex].create3DFloorplan

        if(RxCore_GUI_FloorplanCanvas != undefined){
            DocObj.renderFloorplan(RxCore_GUI_FloorplanCanvas.setFloorCanvas, floornum, alignarray);
        }
        

    }
    function RxCore_Create3Dfloorplan(sourceindex, targetdocindex, floornum){
        if(docpagNotReady()){
            return;
        }
        if(OpenFiles[targetdocindex].pages[0].usevector3DGLTF){
                //OpenFiles[targetdocindex].create3DFloorplan;
                //var callback = OpenFiles[targetdocindex].create3DFloorplan;

                OpenFiles[sourceindex].renderFloorplan(OpenFiles[targetdocindex].create3DFloorplan, floornum, null);
        }

    }

    function RxCore_Create3DfloorplanfromCanvas(canvas, floornum, alignarray){
        if(docpagNotReady()){
            return;
        }
        if(DocObj.pages[0].usevector3DGLTF){
                //OpenFiles[targetdocindex].create3DFloorplan;
                //var callback = OpenFiles[targetdocindex].create3DFloorplan;
                //DocObj.renderFloorplan(OpenFiles[targetdocindex].create3DFloorplan, floornum);

                DocObj.create3DFloorplan(canvas, floornum, alignarray);

        }


    }
    function RxCore_Create3DfloorplanfromPage(targetdocindex, floornum){
        if(docpagNotReady()){
            return;
        }
        if(OpenFiles[targetdocindex].pages[0].usevector3DGLTF){
                //OpenFiles[targetdocindex].create3DFloorplan;
                //var callback = OpenFiles[targetdocindex].create3DFloorplan;
                DocObj.renderFloorplan(OpenFiles[targetdocindex].create3DFloorplan, floornum, null);
        }

    }

    function RxCore_addHoverSpaceforBlock(blockindx, color){
        if(docpagNotReady()){
            return;
        }

        if(!blockindx){
            return;
        }

        if (DocObj.getCurrentpage().usevectorxml) {
            
            
            var polypath = DocObj.getCurrentpage().getpolygonPathbypathIndex(blockindx);
            

            if(polypath != null){

                var hoverobj = RxCore_createSpaceHover(color, polypath);
                DocObj.addHoverSpace(hoverobj);
                drawmarkupAll(cntximg);
                
            }


        }

    }

    function RxCore_clearHoverSpace(){
        if(docpagNotReady()){
            return;
        }

        if (DocObj.getCurrentpage().usevectorxml) {
            
            DocObj.clearHoverSpace();
            drawmarkupAll(cntximg);

        }

    }


    function RxCore_createSpacefromBlock(blockid){
        if(docpagNotReady()){
            return;
        }

        if (DocObj.getCurrentpage().usevectorxml) {


            var scalefactor = DocObj.getCurrentpage().dscalevector;
            var xoffset = DocObj.getCurrentpage().dxvector;
            var yoffset = DocObj.getCurrentpage().dyvector;
            var mediax = DocObj.getCurrentpage().VectorPageObj.x;
            var mediay = DocObj.getCurrentpage().VectorPageObj.y;
            var mediah = DocObj.getCurrentpage().VectorPageObj.h;

            var attribs = DocObj.getCurrentpage().blockAttributes(blockid);
            
            //var polypoints = DocObj.getCurrentpage().getpolygonPointsbyblockID(blockid);

            var polypath = DocObj.getCurrentpage().getpolygonPathbyblockID(blockid);
            

            //attribs
            
            if(polypath != null){
                RxCore_createSpacemarkupAttribsPath(blockid, polypath, attribs, scalefactor, xoffset, yoffset, mediax, mediay, mediah);
            }
            
            //RxCore_createSpacemarkupAttribs(blockid, polypoints, attribs, scalefactor, xoffset, yoffset, mediax, mediay, mediah);

            
        }


    }

    function RxCore_getBookMarkPage(destref){
        if(docpagNotReady()){
            return;
        }
        return DocObj.get_bookmark_pageref(destref);
    }

    function RxCore_getCacheURL(){
        if(docpagNotReady()){
            return;
        }

        return DocObj.CacheURL;

    }

    function RxCore_getOriginalPath(){
        if(docpagNotReady()){
            return;
        }

        return DocObj.OriginalURL;

    }


    function RxCore_ChangeVectorBlock(index) {
        if(docpagNotReady()){
            return;
        }

        if (DocObj.getCurrentpage().usevectorxml) {
            DocObj.getCurrentpage().turnBlockOnOff(index);
        }

    }

    function RxCore_RestoreBlockStates(){
        if(docpagNotReady()){
            return;
        }

        restoreSpacemarkupStates();

        if (DocObj.getCurrentpage().usevectorxml) {
            DocObj.getCurrentpage().restoreBlockStates();
        }

    }

    function RxCore_resetBlocks() {
        if(docpagNotReady()){
            return;
        }
        if (DocObj.getCurrentpage().usevectorxml) {
            DocObj.getCurrentpage().resetBlocks();
        }

    }
    function RxCore_set3DBlockStateAll(state){
        if(docpagNotReady()){
            return;
        }

        if (DocObj.getCurrentpage().usevector3Dxml) {
            DocObj.getCurrentpage().state3dBlockAll(state, false);
        }

    };

    function RxCore_set3DBlockState(name, state){
        if(docpagNotReady()){
            return;
        }

        if (DocObj.getCurrentpage().usevector3Dxml) {
            DocObj.getCurrentpage().state3dBlock(name, state, false);
        }

    }


    function RxCore_Change3DVectorBlock(name) {
        if(docpagNotReady()){
            return;
        }

        if (DocObj.getCurrentpage().usevector3Dxml) {
            
            DocObj.getCurrentpage().turn3DBlockOnOff(name, false);
        }

    }

    function RxCore_findMarkupByAttr(attrname, attrvalue){
        if(docpagNotReady()){
            return;
        }
        

    }

    function RxCore_FindBlockByAttr(attrname, attrvalue){
        if(docpagNotReady()){
            return;
        }

        if (DocObj.getCurrentpage().usevectorxml) {
            return DocObj.getCurrentpage().FindBlockByAttr(attrname, attrvalue);
        }


    }

    function RxCore_getStoreyNames(){
        if(docpagNotReady()){
            return -1;
        }


        if (DocObj.getCurrentpage().usevector3Dxml) {
            return DocObj.getCurrentpage().getstoreyNames();
        }

    }

    function RxCore_getStoreyRealIDs(storeyname){
        if(docpagNotReady()){
            return -1;
        }

        if (DocObj.getCurrentpage().usevector3Dxml) {
            return DocObj.getCurrentpage().getStoreyRealIDs(storeyname);
        }

    }

    function RxCore_getStoreyIDs(storeyname){
        if(docpagNotReady()){
            return -1;
        }

        if (DocObj.getCurrentpage().usevector3Dxml) {
            return DocObj.getCurrentpage().getStoreyIDs(storeyname);
        }

    }

    function RxCore_getStoreyAttributes(storeyname){
        if(docpagNotReady()){
            return -1;
        }

        if (DocObj.getCurrentpage().usevector3Dxml) {
            return DocObj.getCurrentpage().getStoreyAttributes(storeyname);
        }


    }
    function RxCore_SearchStoreyAttributes(storeyname, expr){
        if(docpagNotReady()){
            return -1;
        }

        if (DocObj.getCurrentpage().usevector3Dxml) {
            return DocObj.getCurrentpage().searchStoreyAttributes(storeyname, expr);
        }

    }

    function RxCore_SearchStoreyAttributesEx(storeyname, expr, useregexp){
        if(docpagNotReady()){
            return -1;
        }

        if (DocObj.getCurrentpage().usevector3Dxml) {
            if(useregexp){
                return DocObj.getCurrentpage().searchStoreyAttributes(storeyname, expr);
            }else{
                return DocObj.getCurrentpage().searchStoreyAttributesEx(storeyname, expr);
            }

        }

    }

    function RxCore_Search3dAttributesEx(expr, useregexp){
        if(docpagNotReady()){
            return -1;
        }

        if (DocObj.getCurrentpage().usevector3Dxml) {
            if(useregexp){
                return DocObj.getCurrentpage().search3DAttributes(expr);
            }else{
                return DocObj.getCurrentpage().search3DAttributesEx(expr);
            }

            
        }

    }    

    function RxCore_Search3dAttributes(expr){
        if(docpagNotReady()){
            return -1;
        }

        if (DocObj.getCurrentpage().usevector3Dxml) {
            return DocObj.getCurrentpage().search3DAttributes(expr);
        }

    }

    function RxCore_Search3dAttribute(expr){
        if(docpagNotReady()){
            return -1;
        }

        if (DocObj.getCurrentpage().usevector3Dxml) {
            return DocObj.getCurrentpage().search3DAttribute(expr);
        }

    }
    function RxCore_LoadIFCSpaces(){
        if(docpagNotReady()){
            return -1;
        }

        if (DocObj.getCurrentpage().usevector3Dxml) {
            DocObj.getCurrentpage().loadGLTFSpaces();
            
        }

    }

    function RxCore_addGltftoDoc(URL){
        if(docpagNotReady()){
            return;
        }

        if (DocObj.getCurrentpage().usevector3Dxml) {
            //DocObj.getCurrentpage().addtoGLTFScene(URL);
            getFileaddtoGLTF(URL);
        }

    }

    function RxCore_Select3DVectorBlock(name) {
        if(docpagNotReady()){
            return;
        }

        if (DocObj.getCurrentpage().usevector3Dxml) {
            
            DocObj.getCurrentpage().select3DBlock(name, true);
        }

    }

    function RxCore_SelectVectorBlock(blockid) {
        if(docpagNotReady()){
            return;
        }

        if (DocObj.getCurrentpage().usevectorxml) {
            DocObj.getCurrentpage().selectBlock(blockid);
        }

    }

    function RxCore_UnselectAllBlocks() {
        if(docpagNotReady()){
            return;
        }

        unselectallBlocks();

    }


    function RxCore_moveTo3DNavPos(campos){

        if(docpagNotReady()){
            return;
        }


        if (DocObj.getCurrentpage().usevector3Dxml) {
           
            if (RxCore_GUI_3DWalkthrough != undefined) {
                if(bIsmobile){
                    RxCore_GUI_3DWalkthrough.setWalkthroughGUI(true);
                    //$('div.wheelPanel').css('visibility', 'visible');
    
                }
            }
            set_tool('Walkthrough3D', {});
            //tool = new tools['Walkthrough3D']();
            DocObj.curcontrol3D = 'Walkthrough3D';
            
            DocObj.getCurrentpage().walkthroughcontrol.setCameraXYPos(campos);
        
        }
        
    }

    function new_moveto3dpart(){


        if (DocObj.getCurrentpage().usevector3Dxml) {

            if (DocObj.getCurrentpage().Vector3DPageObj.getcur3DView() != null){
                return;
            } 

            var mesharray = DocObj.getCurrentpage().Vector3DPageObj.getMeshChildren();
            var markuparray = DocObj.getCurrentpage().Vector3DPageObj.getMarkupChildren();
            var addedmesharray = DocObj.getCurrentpage().Vector3DPageObj.getAddedMeshChildren();


            if (mesharray.length == 0){
                return;
            } 

            var partidobj = DocObj.getCurrentpage().Vector3DPageObj.getSelectedPart();


            if (partidobj.index !=-1){
                
                var raycaster = new THREE.Raycaster();

                DocObj.getCurrentpage().Vector3DPageObj.scene.updateMatrixWorld();


                var origox = DocObj.getCurrentpage().Vector3DPageObj.scene.position.x;
                var origoy = DocObj.getCurrentpage().Vector3DPageObj.scene.position.y;
                var origoz = DocObj.getCurrentpage().Vector3DPageObj.scene.position.z;

                var zdiff = 0;
                var modelscale = DocObj.getCurrentpage().Vector3DPageObj.scale;

                var offset = DocObj.getCurrentpage().Vector3DPageObj.getmminUnit(2500);
                var raynear = DocObj.getCurrentpage().Vector3DPageObj.getmminUnit(10);
                var zoffset = DocObj.getCurrentpage().Vector3DPageObj.getfloorheight();
                
                /*if(DocObj.getCurrentpage().Vector3DPageObj.unit == 1){
                    var offset = 2.5;
                    //var zoffset = (1.8 - zdiff) * modelscale;
                    var zoffset = 1.8;
                    var raynear = 0.01;
                }else if(DocObj.getCurrentpage().Vector3DPageObj.unit == 0){
                    offset = 2500;// * modelscale;
                    //zoffset = (1800 - zdiff) * modelscale;
                    zoffset = 1800;
                    raynear = 10;
                }else if(DocObj.getCurrentpage().Vector3DPageObj.unit == 2){
                    offset = 8.2;// * modelscale;
                    //zoffset = (1800 - zdiff) * modelscale;
                    zoffset = 5.9;
                    raynear = 0.03;

                }*/

                var pingarray = [];

                if(partidobj.isadded){

                    if (partidobj.group){
                        
                        var partcenter = addedmesharray[partidobj.addedmeshindex][partidobj.index].children[partidobj.groupindex].geometry.boundingSphere.center.clone();
                    }else{
                        partcenter = addedmesharray[partidobj.addedmeshindex][partidobj.index].geometry.boundingSphere.center.clone();
                    }

                }else{

                    if (partidobj.group){
                        var partcenter = mesharray[partidobj.index].children[partidobj.groupindex].geometry.boundingSphere.center.clone();
                    }else if(partidobj.markup) {
                        
                        
                        partcenter = markuparray[partidobj.index].position.clone();
                    }else{
                        partcenter = mesharray[partidobj.index].geometry.boundingSphere.center.clone();
                    }
    
                }
                
                var directionPX = new THREE.Vector3(1, 0, 0).normalize();
                var directionNX = new THREE.Vector3(-1, 0, 0).normalize();
                var directionNY = new THREE.Vector3(0, -1, 0).normalize();
                var directionPY = new THREE.Vector3(0, 1, 0).normalize();
                //var raynear = DocObj.getCurrentpage().camera.near;
                
                var rayfar = DocObj.getCurrentpage().camera.far;

                //const dirNX = 0;
                //const dirPX = 1;
                //const dirNY = 2;
                //const dirPY = 3;

                pingarray.push({direction : directionNX, raycaster : new THREE.Raycaster(partcenter, directionNX, raynear, rayfar), intersects : null, distance : 0}); //negative x
                pingarray.push({direction : directionPX, raycaster : new THREE.Raycaster(partcenter, directionPX, raynear, rayfar ), intersects : null, distance : 0}); //postive x
                pingarray.push({direction : directionNY, raycaster : new THREE.Raycaster(partcenter, directionNY, raynear, rayfar), intersects : null, distance : 0}); //negative y
                pingarray.push({direction : directionPY, raycaster : new THREE.Raycaster(partcenter, directionPY, raynear, rayfar), intersects : null, distance : 0}); //postive y

                var maxdist = 0.0;
                var dirindex = 0;

                for(var pi = 0;pi < pingarray.length;pi++){
                        
                    //pingarray[pi].raycaster.set ( partcenter, pingarray[pi].direction);
                    //pingarray[pi].intersects = pingarray[pi].raycaster.intersectObjects(mesharray, true);

                    if(partidobj.isadded){
                        pingarray[pi].intersects = pingarray[pi].raycaster.intersectObjects(addedmesharray, true);
                    }else{
                        pingarray[pi].intersects = pingarray[pi].raycaster.intersectObjects(mesharray, true);
                    }

                    
                    if (pingarray[pi].intersects.length > 0) {

                        for (var is = 0; is < pingarray[pi].intersects.length; is++) {
                            if (pingarray[pi].intersects[is].object.visible && pingarray[pi].intersects[is].face != null ) {
                                
                                if (partidobj.markup){
                                    pingarray[pi].distance = pingarray[pi].intersects[is].distance;   
                                    if(pingarray[pi].distance > maxdist){
                                        maxdist = pingarray[pi].distance;
                                        dirindex = pi;
                                    } 
                                    
                                    break;
    
                                }else{
                                    if(!pingarray[pi].intersects[is].object.userData.selected){
                                        pingarray[pi].distance = pingarray[pi].intersects[is].distance;   
                                        if(pingarray[pi].distance > maxdist){
                                            maxdist = pingarray[pi].distance;
                                            dirindex = pi;
                                        } 
                                        
                                        break;

                                    }
                                }
                            }
                        }
                    }
                }

                /*var szdirection = "none";
                if(dirNX == dirindex){
                    szdirection = "negative X";
                }else if (dirPX == dirindex){
                    szdirection = "postive X";
                }else if(dirNY == dirindex){
                    szdirection = "negative Y";
                }else if(dirPY == dirindex){
                    szdirection = "positive Y";
                }*/

                //console.log(pingarray[dirindex].distance, szdirection);

                DocObj.getCurrentpage().camera.position.copy(partcenter);

                DocObj.getCurrentpage().camera.lookAt(partcenter);

                const direction = (new THREE.Vector3())
                .subVectors(DocObj.getCurrentpage().camera.position, partcenter)
                .normalize();                

                //.multiply(new THREE.Vector3(0, 1, -1))
                //DocObj.getCurrentpage().camera.position.copy(direction.multiplyScalar(offset).add(partcenter));
                if (pingarray[dirindex].distance >= offset){
                    DocObj.getCurrentpage().camera.position.copy(pingarray[dirindex].direction.multiplyScalar(offset).add(partcenter));
                }else{
                    DocObj.getCurrentpage().camera.position.copy(pingarray[dirindex].direction.multiplyScalar(pingarray[dirindex].distance).add(partcenter));
                }
                
                DocObj.getCurrentpage().camera.lookAt(partcenter);


                var camerapos = DocObj.getCurrentpage().Vector3DPageObj.checkfloorheight();

                if (camerapos != null){

                    DocObj.getCurrentpage().camera.position.copy(camerapos);
                    //console.log(DocObj.getCurrentpage().camera.position);
                    DocObj.getCurrentpage().camera.lookAt(partcenter);
                    DocObj.getCurrentpage().walkthroughcontrol.setTempWalkHeight(camerapos.z);
                    DocObj.getCurrentpage().camera.lookAt(partcenter);
                    var curfloor = DocObj.getCurrentpage().Vector3DPageObj.checkcurfloor();
                    var floorelevation = DocObj.getCurrentpage().Vector3DPageObj.getfloorElevation(0);




                }
                


                //DocObj.getCurrentpage().camera.position.copy(origin);

                //DocObj.getCurrentpage().walkthroughcontrol.setPartPos(partcenter, DocObj.getCurrentpage().camera.position);

                //var zdiff = partcenter.z - DocObj.getCurrentpage().Vector3DPageObj.partlist[partid].minz;

                //var camerax = origox - partx; //- (DocObj.getCurrentpage().Vector3DPageObj.partlist[partid].radius * 2);
                //var cameray = origoy - party; //- (DocObj.getCurrentpage().Vector3DPageObj.partlist[partid].radius * 2);
                //var cameraz = origoz - partz;

                //partcenter
                var partPos = new THREE.Vector3(partcenter.x, partcenter.y, partcenter.z);


                //DocObj.getCurrentpage().camera.position.x = camerax;
                //DocObj.getCurrentpage().camera.position.y = cameray - offset;
                //DocObj.getCurrentpage().camera.position.z = cameraz + zoffset;
                //DocObj.getCurrentpage().camera.position.x = partcenter.x;
                //DocObj.getCurrentpage().camera.position.y = partcenter.y;
                //DocObj.getCurrentpage().camera.position.z = partcenter.z;

                //DocObj.getCurrentpage().camera.lookAt(partPos);
                //DocObj.getCurrentpage().camera.lookAt(partcenter);
                

                var xrot = DocObj.getCurrentpage().camera.rotation.x;
                var yrot = DocObj.getCurrentpage().camera.rotation.y;
                var zrot = DocObj.getCurrentpage().camera.rotation.z;

                
                //DocObj.getCurrentpage().walkthroughcontrol.firstxyrecieved = false;
                //DocObj.getCurrentpage().walkthroughcontrol.setInitial(-(xrot * (180 / Math.PI)),90,0,0);




                //var partx = -DocObj.getCurrentpage().Vector3DPageObj.partlist[partid].vcentroid.x * modelscale;
                //var party = -DocObj.getCurrentpage().Vector3DPageObj.partlist[partid].vcentroid.y * modelscale;
                //var partz = -DocObj.getCurrentpage().Vector3DPageObj.partlist[partid].vcentroid.z * modelscale;
                //var zdiff = DocObj.getCurrentpage().Vector3DPageObj.partlist[partid].vcentroid.z - DocObj.getCurrentpage().Vector3DPageObj.partlist[partid].minz;



            }
                
        }

        if (RxCore_GUI_3DWalkthrough != undefined) {
            if(bIsmobile){
                RxCore_GUI_3DWalkthrough.setWalkthroughGUI(true);
                //$('div.wheelPanel').css('visibility', 'visible');
    
            }
        }
        set_tool('Walkthrough3D', {});
        //tool = new tools['Walkthrough3D']();
        DocObj.curcontrol3D = 'Walkthrough3D';


    }    


    function RxCore_MoveTo3DPart(){
        if(docpagNotReady()){
            return;
        }

        if (DocObj.getCurrentpage().usevector3DGLTF){
            new_moveto3dpart();
            return;
        }


        if (DocObj.getCurrentpage().usevector3Dxml) {
            var partid = DocObj.getCurrentpage().Vector3DPageObj.getSelectedPart();
            if (partid !=-1){
                DocObj.getCurrentpage().Vector3DPageObj.scene.updateMatrixWorld();

                var origox = DocObj.getCurrentpage().Vector3DPageObj.scene.position.x;
                var origoy = DocObj.getCurrentpage().Vector3DPageObj.scene.position.y;
                var origoz = DocObj.getCurrentpage().Vector3DPageObj.scene.position.z;
                var modelscale = DocObj.getCurrentpage().Vector3DPageObj.scale;

                //var partx = -(origox - DocObj.getCurrentpage().Vector3DPageObj.partlist[partid].center.x) * modelscale;
                //var party = -(origoy - DocObj.getCurrentpage().Vector3DPageObj.partlist[partid].center.y) * modelscale;
                //var partz = -(origoz - DocObj.getCurrentpage().Vector3DPageObj.partlist[partid].center.z) * modelscale;

                //vcentroid
                //centroid

                var partx = -DocObj.getCurrentpage().Vector3DPageObj.partlist[partid].vcentroid.x * modelscale;
                var party = -DocObj.getCurrentpage().Vector3DPageObj.partlist[partid].vcentroid.y * modelscale;
                var partz = -DocObj.getCurrentpage().Vector3DPageObj.partlist[partid].vcentroid.z * modelscale;
                var zdiff = DocObj.getCurrentpage().Vector3DPageObj.partlist[partid].vcentroid.z - DocObj.getCurrentpage().Vector3DPageObj.partlist[partid].minz;


                var camerax = origox - partx; //- (DocObj.getCurrentpage().Vector3DPageObj.partlist[partid].radius * 2);
                var cameray = origoy - party; //- (DocObj.getCurrentpage().Vector3DPageObj.partlist[partid].radius * 2);
                var cameraz = origoz - partz;
                var partPos = new THREE.Vector3(camerax, cameray, cameraz);



                var format = DocObj.Format.substring(0, 3);
                if (format == 'IFC') {
                    DocObj.getCurrentpage().camera.up.set(0, 0, 1);
                    //DocObj.getCurrentpage().camera.rotation.set(90 * Math.PI / 180, 0, 0);

                } else {
                    //DocObj.getCurrentpage().camera.rotation.set(0, 90 * Math.PI / 180, 0);
                    DocObj.getCurrentpage().camera.up.set(0, 1, 0);
                }

                var floorheight = DocObj.getCurrentpage().Vector3DPageObj.getfloorheight();
                var meterunit = DocObj.getCurrentpage().Vector3DPageObj.getmeterunit();
                var offset = modelscale * meterunit;
                var zoffset = (floorheight - zdiff) * modelscale;

                DocObj.getCurrentpage().camera.position.x = camerax;
                DocObj.getCurrentpage().camera.position.y = cameray - offset;
                DocObj.getCurrentpage().camera.position.z = cameraz + zoffset;
                DocObj.getCurrentpage().camera.lookAt(partPos);

                var xrot = DocObj.getCurrentpage().camera.rotation.x;
                var yrot = DocObj.getCurrentpage().camera.rotation.y;
                var zrot = DocObj.getCurrentpage().camera.rotation.z;


                DocObj.getCurrentpage().walkthroughcontrol.setInitial(-(xrot * (180 / Math.PI)),90,0,0);


            }
        }

        if (RxCore_GUI_3DWalkthrough != undefined) {
            if(bIsmobile){
                RxCore_GUI_3DWalkthrough.setWalkthroughGUI(true);
            }
            
            //$('div.wheelPanel').css('visibility', 'visible');
        }
        set_tool('Walkthrough3D', {});
        //tool = new tools['Walkthrough3D']();
        DocObj.curcontrol3D = 'Walkthrough3D';
    }

    function RxCore_saveresetCam(){
        
        if(docpagNotReady()){
            return;
        }

        
        if (!documentopen) {
            return;
        }

        var curpage = DocObj.getCurrentpage();

        if(curpage == undefined || curpage == null){
            return;
        }

        if (curpage.usevector3Dxml) {

            curpage.Vector3DPageObj.saveresetCamera();


        }

    }

    function RxCore_setresetCam(){
        
        if(docpagNotReady()){
            return;
        }


        if (!documentopen) {
            return;
        }

        var curpage = DocObj.getCurrentpage();

        if(curpage == undefined || curpage == null){
            return;
        }

        if (curpage.usevector3Dxml) {

            curpage.Vector3DPageObj.restoreresetCam();


        }


    }
    
    
    function RxCore_restoreStartCam(){
        
        if(docpagNotReady()){
            return;
        }


        if (!documentopen) {
            return;
        }
        
        if (DocObj.getCurrentpage().usevector3Dxml) {
            DocObj.getCurrentpage().Vector3DPageObj.restorestartcam();
            //DocObj.getCurrentpage().Vector3DPageObj.setCamera();

        }
                
    }


    function RxCore_Reset3DModel() {
        if(docpagNotReady()){
            return;
        }

        if (DocObj.getCurrentpage().usevector3Dxml) {
            DocObj.getCurrentpage().reset3DParts();

            DocObj.getCurrentpage().Vector3DPageObj.active3DView = false;

            DocObj.getCurrentpage().Vector3DPageObj.doexplode = true;
            DocObj.getCurrentpage().Vector3DPageObj.newExplode(0);
            DocObj.getCurrentpage().Vector3DPageObj.doexplode = false;


            //DocObj.getCurrentpage().Vector3DPageObj.doexplode = true;
            //DocObj.getCurrentpage().Vector3DPageObj.explode(0);
            //DocObj.getCurrentpage().Vector3DPageObj.doexplode = false;
            DocObj.getCurrentpage().Vector3DPageObj.resetTransparency();
            DocObj.getCurrentpage().Vector3DPageObj.clipping(-1,0,false);
            //RxCore.clipping3D(item.state,-1,0);

            /*var format = DocObj.Format.substring(0, 3);
            if (format == 'IFC') {

                var camerax = (DocObj.getCurrentpage().Vector3DPageObj.w / 2) * DocObj.getCurrentpage().Vector3DPageObj.scale;
                var cameray = -(DocObj.getCurrentpage().Vector3DPageObj.h) * DocObj.getCurrentpage().Vector3DPageObj.scale;
                var cameraz = (DocObj.getCurrentpage().Vector3DPageObj.d / 2) * DocObj.getCurrentpage().Vector3DPageObj.scale;

                camerax = 0;
                cameray *= 2;
                cameraz = 0;

                DocObj.getCurrentpage().camera.up.set(0, 0, 1);
                //DocObj.getCurrentpage().camera.rotation.set(90 * Math.PI / 180, 0, 0);
            } else {

                camerax = (DocObj.getCurrentpage().Vector3DPageObj.w / 2) * DocObj.getCurrentpage().Vector3DPageObj.scale;
                cameray = (DocObj.getCurrentpage().Vector3DPageObj.h / 2) * DocObj.getCurrentpage().Vector3DPageObj.scale;
                cameraz = (DocObj.getCurrentpage().Vector3DPageObj.d) * DocObj.getCurrentpage().Vector3DPageObj.scale;

                camerax = 0;
                cameray = 0;
                cameraz *= 2;

                DocObj.getCurrentpage().camera.up.set(0, 1, 0);
                //DocObj.getCurrentpage().camera.rotation.set(0, 90 * Math.PI / 180, 0);

            }


            //camera.rotation.set(90 * Math.PI / 180,0,0);
            DocObj.getCurrentpage().camera.position.set(camerax, cameray, cameraz);
            DocObj.getCurrentpage().camera.lookAt(new THREE.Vector3(0,0,0));*/

            DocObj.curcontrol3D = 'orbitControl';
            RxCore_3DOrbit();

            RxCore_setresetCam();

            //Viewer3D.resetScene();
            //Viewer3D.update();
        }

    }

    function RxCore_enableCamNavDraw(onOff){
        
        bDrawNavCam = onOff;

    }

    function RxCore_EnableZoomOnScrollMultipage(onOff){
        enableZoomOnScrollMultipage = onOff;
    }

    function RxCore_Multiselect3Dpart(onOff){
        
        bmulti3dSelect = onOff;
        
        //if more than 1 markup is currently selected keep multiselect on.

        //if()
    }

    function RxCore_usePanToMarkup(onOff){
        busepantomarkup = onOff;
    }

    function RxCore_MarkupMultiselect(onOff){
        
        bMultiselect = onOff;
        
        //if more than 1 markup is currently selected keep multiselect on.

        //if()
    }

    function RxCore_changeSnapState(state) {
        if(docpagNotReady()){
            return;
        }

        DocObj.getCurrentpage().enableSnap(state);
    }

    function RxCore_getSnapState(){
        
        if(docpagNotReady()){
            return;
        }

        return DocObj.getCurrentpage().getSnapState();


    }


    function RxCore_markupAngleSwitch(){

        if(docpagNotReady()){
            return;
        }

        if(readonlymode){
            return;
        }


        ChangeMarkupSelected(18);


    }

    function RxCore_markupDisplay(onOff){
        if(docpagNotReady()){
            return;
        }

        if(readonlymode){
            return;
        }

        if(onOff){
            ChangeMarkupSelected(15);
        }else{
            ChangeMarkupSelected(14);
        }
        

    }    
    
    function RxCore_changeLabelTransp(val){
        if(docpagNotReady()){
            return;
        }

        if(readonlymode){
            return;
        }


        var temptransp = globaltransparency;
        globaltransparency = val;
        ChangeMarkupSelected(12);

        if (!bSetGlobal) {
            globaltransparency = temptransp;
        }

    }

    function RxCore_ChangeTranspMarkup(markup,val) {

        var temptransp = globaltransparency;
        globaltransparency = val;

        if(docpagNotReady()){
            return;
        }

        if(readonlymode){
            return;
        }

        if (markup){

            ChangeMarkup(markup, 6);
            if (!bSetGlobal) {
                globaltransparency = temptransp;
            }
    
        }

    }

    function RxCore_ChangeTransp(val) {
        
        var temptransp = globaltransparency;
        globaltransparency = val;

        
        if(docpagNotReady()){
            return;
        }

        if(readonlymode){
            return;
        }

        ChangeMarkupSelected(6);
        if (!bSetGlobal) {
            globaltransparency = temptransp;
        }

    }



    function RxCore_ChangeColorByIndex(indx,val,strokefill){
        if(docpagNotReady()){
            return;
        }

        if(readonlymode){
            return;
        }


        if(val == undefined){
            return;
        }


        if (typeof(val) == 'string'){
            
            if (val.match(/rgb/gi) != null) {
                val = rgbToHex(val);
            }
    
        }else{
            return;
        }


        var tempcolor = markupcolor;

        switch (strokefill) {
            case 1:
                tempcolor = markuplinecolor;
                markuplinecolor = val;
                ChangeMarkupByindx(indx,1);
                if (!bSetGlobal) {
                    markuplinecolor = tempcolor;
                }

                break;
            case 0:
                tempcolor = markupfillcolor;
                markupfillcolor = val;
                ChangeMarkupByindx(indx,7);
                if (!bSetGlobal) {
                    markupfillcolor = tempcolor;
                }

                break;
            case 2:
                tempcolor = markuptextcolor;
                markuptextcolor = val;
                ChangeMarkupByindx(indx,8);
                if (!bSetGlobal) {
                    markuptextcolor = tempcolor;
                }

                break;
        }


    }
    
    function RxCore_ChangeColorMarkup(markup, val, strokefill){
        if(docpagNotReady()){
            return;
        }

        if(readonlymode){
            return;
        }


        if(val == undefined){
            return;
        }

        if (typeof(val) == 'string'){
            
            if (val.match(/rgb/gi) != null) {
                val = rgbToHex(val);
            }
    
        }else{
            return;
        }


        /*if (val.match(/rgb/gi) != null) {
            val = rgbToHex(val);
        }*/

        var tempcolor = markupcolor;
        
        switch (strokefill) {
            case 1:
                tempcolor = markuplinecolor;
                markuplinecolor = val;

                ChangeMarkup(markup,1);
                if (!bSetGlobal) {
                    markuplinecolor = tempcolor;
                }

                break;
            case 0:
                tempcolor = markupfillcolor;
                markupfillcolor = val;

                ChangeMarkup(markup,7);

                if (!bSetGlobal) {
                    markupfillcolor = tempcolor;
                }

                break;
            case 2:
                tempcolor = markuptextcolor;
                markuptextcolor = val;
                
                ChangeMarkup(markup,8);
                if (!bSetGlobal) {
                    markuptextcolor = tempcolor;
                }

                break;
        }
        
    }    

    function RxCore_ChangeColorAll(val, strokefill) {

        if(docpagNotReady()){
            return;
        }

        if(readonlymode){
            return;
        }

        if(val == undefined){
            return;
        }

        if (typeof(val) == 'string'){
            
            if (val.match(/rgb/gi) != null) {
                val = rgbToHex(val);
            }
    
        }else{
            return;
        }

        var tempcolor = markupcolor;
        //markupcolor = val;

        switch (strokefill) {
            case 1:
                tempcolor = markuplinecolor;
                markuplinecolor = val;

                ChangeMarkupSelected(1, true);
                if (!bSetGlobal) {
                    markuplinecolor = tempcolor;
                }

                break;
            case 0:
                tempcolor = markupfillcolor;
                markupfillcolor = val;

                ChangeMarkupSelected(7, true);
                if (!bSetGlobal) {
                    markupfillcolor = tempcolor;
                }

                break;
            case 2:
                tempcolor = markuptextcolor;
                markuptextcolor = val;

                ChangeMarkupSelected(8, true);
                if (!bSetGlobal) {
                    markuptextcolor = tempcolor;
                }

                break;
        }

        /*if (!bSetGlobal) {
            markupcolor = tempcolor;
        }*/

    }

    function RxCore_ClearMarkupComments(){

        var markup = getSelectedmarkup();

        if(!markup.isempty){
            markup.ClearComments();
        }

    }

    function RxCore_ChangeColor(val, strokefill) {
        if(docpagNotReady()){
            return;
        }

        if(readonlymode){
            return;
        }
        
        if(val == undefined){
            return;
        }

        if (typeof(val) == 'string'){
            
            if (val.match(/rgb/gi) != null) {
                val = rgbToHex(val);
            }
    
        }else{
            return;
        }


        var tempcolor = markupcolor;
        //markupcolor = val;

        switch (strokefill) {
            case 1:
                tempcolor = markuplinecolor;
                markuplinecolor = val;

                ChangeMarkupSelected(1);
                if (!bSetGlobal) {
                    markuplinecolor = tempcolor;
                }

                break;
            case 0:
                tempcolor = markupfillcolor;
                markupfillcolor = val;

                ChangeMarkupSelected(7);
                if (!bSetGlobal) {
                    markupfillcolor = tempcolor;
                }

                break;
            case 2:
                tempcolor = markuptextcolor;
                markuptextcolor = val;

                ChangeMarkupSelected(8);
                if (!bSetGlobal) {
                    markuptextcolor = tempcolor;
                }

                break;
        }

        /*if (!bSetGlobal) {
            markupcolor = tempcolor;
        }*/

    }


    function RxCore_ForceMarkupCallback(OnOff){
        bAlwaysMarkupCallback = OnOff;
    }

    function RxCore_MarkupAddMulti(OnOff){
        
        bMultimarkupadd = OnOff;
    }


    function RxCore_MarkupSaveCheck(OnOff){
        bMarkupSavecheck = OnOff;
    }


    function RxCore_GetColor() {
        return markupcolor;
    }

    function RxCore_GetCompareColors() {

        var comparecolors = {
            bg:overlayBGColor,
            fg:overlayFGColor
        };

        var bhaveCompare = false;

        var lCompareObj = getCompareFile();
        if (lCompareObj != undefined){
            bhaveCompare = true;

            comparecolors = lCompareObj.getColors();

        }


        /*var bhaveCompare = false;
        for (var i = 0; i < OpenFiles.length; i++) {
            if (OpenFiles[i].Type == 'Compare') {
                bhaveCompare = true;
            }
        }
        if(bhaveCompare){
            comparecolors = CompareObj.getColors();
        }*/

        return comparecolors;

    }


    function RxCore_GetlineColor() {
        return markuplinecolor;
    }

    function RxCore_GetfillColor() {
        return markupfillcolor;
    }

    function RxCore_GetTextColor() {
        return markuptextcolor;
    }


    function RxCore_GetCanvasSize(){

        var scale = getPixelRatio();

        return {
            w:canvasowidth / scale,
            h:canvasoheight / scale
        };

    }

    function RxCore_overlaymode(szblendmode){

        bShowDiffonly = !bShowDiffonly;
        szdrawmode = szblendmode;

        return bShowDiffonly;
    }

    function RxCore_hideLabels(onoff){
        
        bMarkupNoLabel = onoff;
                
    }

    function RxCore_AllowPolygonMove(onoff){
        bmovepolyallowed = onoff;
    }

    function RxCore_reverseNavMouse(onoff){
        
        breverse3dXrotate = onoff;
    }

    function RxCore_blockhoverevent(onoff){
        benableblockhover = onoff;
    }

    function RxCore_markuphoverevent(onoff){
        benablemarkuhover = onoff;
    }

    function RxCore_showMarkupLabels(onoff){
        bmarkupLabels = onoff;
        enablemarkupLabels = onoff;
    }

    function RxCore_lockMarkup(onoff){

        if(docpagNotReady()){
            return;
        }

        DocObj.bMarkupLocked = onoff;
    }

    function RxCore_MarkupChanged(){
        var retvalue = false;

        if(docpagNotReady()){
            return;
        }

        (DocObj.bMarkupchanged && !readonlymode) ? retvalue = true : retvalue = false;

        return retvalue;

    }
    
    function RxCore_MarkupRedraw(){
        if(docpagNotReady()){
            return;
        }


        drawmarkupAll(cntximg);
        DrawMarkupSelected(context);



    }
    function RxCore_MarkupRotate(degree) {
        if(docpagNotReady()){
            return;
        }

        if(readonlymode){
            return;
        }


        var markupobject = getSelectedmarkup();

        if(!markupobject.isempty){
            markupobject.rotation = degree * (Math.PI / 180);
        }


        drawmarkupAll(cntximg);
        DrawMarkupSelected(context);

        //ChangeMarkupSelected(degree);
    }

    function RxCore_MarkupSubtype(num) {
        if(docpagNotReady()){
            return;
        }

        if(readonlymode){
            return;
        }
        var markupobject = getSelectedmarkup();

        if(!markupobject.isempty){
            markupobject.subtype = num;
        }


        //getSelectedmarkup().rotation = degree*( Math.PI /180);

        drawmarkupAll(cntximg);
        DrawMarkupSelected(context);

    }

    function RxCore_OpenRecent(fileurl) {
        if (RxCore_GUI_Download != undefined) {
            RxCore_GUI_Download.setDownload("show");
        }
        
        var openfileobj = {filename: fileurl, displayname : null};
        
        var retval = getFile(openfileobj);
        if (retval != undefined){
            if (retval == "Require Password"){
                console.log("Require Password");
            }
        }

        


    }

    function RxCore_OpenMarkup(fileurl){
        if(docpagNotReady()){
            return;
        }

        getMarkup(fileurl,DocObj);
    }

    function RxCore_ClearMarkup(){

        if(docpagNotReady()){
            return;
        }

        DocObj.markuplist = [];
        DocObj.markupdraworder = [];

        clearUsers();
        addCurrentUser();


        /*numUsers = 0;
        Userlist = [];
        Userlist[0] = new Users(signature, DisplayName, markuplayer, markupcolor);
        numUsers++;*/

        DocObj.nummarkups = 0;

        if (RxCore_GUI_Markuplist != undefined) {
            RxCore_GUI_Markuplist.setMarkupList(DocObj.getmarkuplist(), DocObj.bActive);
        }

        if (RxCore_GUI_Users != undefined) {
            RxCore_GUI_Users.setUserlist(Userlist, DocObj.bActive);
        }

        drawmarkupAll(cntximg);
        DrawMarkupSelected(context);


    }

    function RxCore_OpenFileCustom(fileurl) {

        if (RxCore_GUI_Download != undefined) {
            RxCore_GUI_Download.setDownload("show");
        }

        bUseCustomrelpath = true;
        xmlurlrelcustom = fileurl;
        
        getFileCustom(fileurl);
        
    }

    function RxCore_OpenPages(fileurl,pages){
        if (RxCore_GUI_Download != undefined) {
            RxCore_GUI_Download.setDownload("show");
        }

        bUseCustomrelpath = false;

        //getFilePages(fileurl,pages);
        getFilePages(fileurl,pages, false);

    }

    function RxCore_open3DFloorplanPage(fileurl, floorindex){

        if (RxCore_GUI_Download != undefined) {
            RxCore_GUI_Download.setDownload("show");
        }

        bUseCustomrelpath = false;
        bUseFloorplanLoad = true;
        nFloorplanIndex = floorindex;


        if (typeof(fileurl) == 'string'){
            var openfileobj = {filename: fileurl, displayname : null};
            getFile(openfileobj);
        }

        
    }

    function RxCore_Open3DFloorplan(fileurl, callback, progressCallBack) {
        if (RxCore_GUI_Download != undefined) {
            RxCore_GUI_Download.setDownload("show");
        }
        bUseCustomrelpath = false;
        bUseFloorplanLoad = true;

        getFileNotActive(fileurl, callback, progressCallBack);
        
    }

    function RxCore_OpenFileSilent(fileurl, callback, progressCallBack) {
        if (RxCore_GUI_Download != undefined) {
            RxCore_GUI_Download.setDownload("show");
        }
        bUseCustomrelpath = false;

        getFileNotActive(fileurl, callback, progressCallBack);
        
    }

    function RxCore_openFileGLTF(fileurl){
        if (RxCore_GUI_Download != undefined) {
            RxCore_GUI_Download.setDownload("show");
        }
        if (typeof(fileurl) == 'object'){
            var szquery = "";
            var bopenfile = false;

            if(fileurl.filepath != undefined){
                if(fileurl.filepath != null){
                    szquery = fileurl.filepath;
                    bopenfile = true;
                }
            }

            if(fileurl.mime != undefined){
                if (fileurl.mime != null){
                    szquery = szquery + "?x-mimetype=" + fileurl.mime;
                    //"?x-mimetype=" + mime + "&amp;cacheid=" + cacheid
                }
            }

            if(fileurl.cacheid != undefined){
                if (fileurl.cacheid != null){
                    szquery = szquery + "&amp;cacheid=" + fileurl.cacheid;
                    //szquery += fileurl.cacheid;
                }
            }
            
            var openfileobj = {filename: szquery, displayname : null};    

            if(fileurl.displayname != undefined){
                if (fileurl.displayname != null){
                    openfileobj.displayname = fileurl.displayname;    
                }
            }

            if(bopenfile){
                //getFile(openfileobj, callback, progressCallBack);
                readGLTF(fileurl.filepath);
            }else{
                if (RxCore_GUI_Download != undefined) {
                    RxCore_GUI_Download.setDownload("hide");
                }

            }
            

        }else if (typeof(fileurl) == 'string'){
            openfileobj = {filename: fileurl, displayname : null};
            //getFile(openfileobj, callback, progressCallBack);
            readGLTF(fileurl);
        }


    }

    function passwordCallback(password, filepath){

        filepath.password = password;
        getFile(filepath);

    }

    function password_required(retval, Filepath){

        if (RxCore_GUI_Download != undefined) {
            RxCore_GUI_Download.setDownload("hide");
        }

        if (retval == "Require Password"){

            if(Rxcore_GUI_PDFPassWord != undefined){
                Rxcore_GUI_PDFPassWord.PassWordRequest(passwordCallback, 1, Filepath);
            }

        }

    }


    function RxCore_CreateServerContent(fileurl, refresh, callback, progressCallBack) {
        
        bUseCustomrelpath = false;
        

        if (typeof(fileurl) == 'object'){
            var szquery = "";
            var bopenfile = false;

            if(fileurl.filepath != undefined){
                if(fileurl.filepath != null){
                    szquery = fileurl.filepath;
                    bopenfile = true;
                }
            }

            //CacheID

            var openfileobj = {filename: szquery, displayname : null, auth : null, xrefurl : null, cacheid : null, refresh : false};

            if(refresh){
                openfileobj.refresh = true;
            }


            if(fileurl.mime != undefined){
                if (fileurl.mime != null){

                    //openfileobj.filename = openfileobj.filename + "?x-mimetype=" + fileurl.mime;
                    openfileobj.mime = fileurl.mime;
                    //szquery = szquery + "?x-mimetype=" + fileurl.mime;
                    //"?x-mimetype=" + mime + "&amp;cacheid=" + cacheid
                }
            }

            if(fileurl.cacheid != undefined){
                if (fileurl.cacheid != null){
                    //szquery = szquery + "&amp;cacheid=" + fileurl.cacheid;
                    //openfileobj.filename = openfileobj.filename + "&amp;cacheid=" + fileurl.cacheid;
                    openfileobj.cacheid = fileurl.cacheid;
                    //szquery += fileurl.cacheid;
                }
            }
            if(fileurl.auth != undefined){
                if (fileurl.auth != null){
                    openfileobj.auth = fileurl.auth;
                }
            }

            if(fileurl.xrefurl != undefined){
                if (fileurl.xrefurl != null){
                    openfileobj.xrefurl = fileurl.xrefurl;
                }
            }


            if(fileurl.requestheaders != undefined){
                //console.log(requestheders);
            }
            
            

            if(fileurl.displayname != undefined){
                if (fileurl.displayname != null){
                    openfileobj.displayname = fileurl.displayname;    
                }
            }

            if(bopenfile){
                //getFile(openfileobj, callback, progressCallBack);
                
                createContent(openfileobj, callback, progressCallBack);

            }
            

        }else if (typeof(fileurl) == 'string'){

            openfileobj = {filename: fileurl, displayname : null, refresh : false};
            
            if(refresh){
                openfileobj.refresh = true;
            }
            
            createContent(openfileobj, callback, progressCallBack);

        }
    }

    function getFileObject (fileurl){
        

        var openfileobj = undefined;

        if (typeof(fileurl) == 'object'){
        
            var szquery = "";
            //var bopenfile = false;

            if(fileurl.filename != undefined){
                if(fileurl.filename != null){
                    szquery = fileurl.filename;
                    //bopenfile = true;
                }
            }

            if(fileurl.filepath != undefined){
                if(fileurl.filepath != null){
                    szquery = fileurl.filepath;
                    //bopenfile = true;
                }
            }

            openfileobj = {filename: szquery, displayname : null, auth : null, xrefurl : null, cacheid : null};



            if(fileurl.mime != undefined){
                if (fileurl.mime != null){
                    //szquery = szquery + "?x-mimetype=" + fileurl.mime;
                    openfileobj.mime = fileurl.mime;
                    //"?x-mimetype=" + mime + "&amp;cacheid=" + cacheid
                }
            }

            if(fileurl.cacheid != undefined){
                if (fileurl.cacheid != null){
                    openfileobj.cacheid = fileurl.cacheid;
                    //szquery = szquery + "&amp;cacheid=" + fileurl.cacheid;
                    //szquery += fileurl.cacheid;
                }
            }

            if(fileurl.auth != undefined){
                if (fileurl.auth != null){
                    openfileobj.auth = fileurl.auth;
                }
            }

            if(fileurl.xrefurl != undefined){
                if (fileurl.xrefurl != null){
                    openfileobj.xrefurl = fileurl.xrefurl;
                }
            }


            if(fileurl.requestheaders != undefined){
                //console.log(requestheders);
            }

            
            //var openfileobj = {filename: szquery, displayname : null};    

            if(fileurl.displayname != undefined){
                if (fileurl.displayname != null){
                    openfileobj.displayname = fileurl.displayname;    
                }
            }

            

        }else if (typeof(fileurl) == 'string'){

            openfileobj = {filename: fileurl, displayname : null};

            //getFile(openfileobj, callback, progressCallBack);
        }

        return openfileobj;
    }

    function RxCore_setinitFile(fileurl){

        var openfileobj = getFileObject(fileurl);

        if(openfileobj != undefined){
            if(openfileobj.filename != null){

                openfileobj.filepath = openfileobj.filename;
                //if(fileurl.filepath != undefined){
             
                initialDoc.open = true;
                initialDoc.openfileobj = openfileobj;

                /*if(readycalled){
                    RxCore_OpenFile(initialDoc.openfileobj);
                }*/

                //readycheck();

/*                 if(readycalled && foxitreadycalled)
                var readycalled = false;
                var foxitreadycalled = false;
 */            

            }


        }

        /*var initialDoc = {
            open : bOpeninitialdoc,
            url : OpeninitialdocURL,
            custom : false,
            filepath: OpeninitialdocURL,
            mime: null,
            cacheid: null,
            displayname: null
        };*/
    



    }

    function RxCore_OpenFile(fileurl, callback, progressCallBack) {
        if (RxCore_GUI_Download != undefined) {
            RxCore_GUI_Download.setDownload("show");
        }
        bUseCustomrelpath = false;

        var bopenfile = false;

        var openfileobj = getFileObject(fileurl);
        
        if(openfileobj != undefined){
            if(openfileobj.filename != null){
                bopenfile = true;
            }

            if(bopenfile){
                getFile(openfileobj, callback, progressCallBack);
            }else{
                if (RxCore_GUI_Download != undefined) {
                    RxCore_GUI_Download.setDownload("hide");
                }

            }

        }

        /*if (typeof(fileurl) == 'object'){
            var szquery = "";
            var bopenfile = false;

            if(fileurl.filepath != undefined){
                if(fileurl.filepath != null){
                    szquery = fileurl.filepath;
                    bopenfile = true;
                }
            }

            var openfileobj = {filename: szquery, displayname : null, auth : null, xrefurl : null, cacheid : null};



            if(fileurl.mime != undefined){
                if (fileurl.mime != null){
                    //szquery = szquery + "?x-mimetype=" + fileurl.mime;
                    openfileobj.mime = fileurl.mime;
                    //"?x-mimetype=" + mime + "&amp;cacheid=" + cacheid
                }
            }

            if(fileurl.cacheid != undefined){
                if (fileurl.cacheid != null){
                    openfileobj.cacheid = fileurl.cacheid;
                    //szquery = szquery + "&amp;cacheid=" + fileurl.cacheid;
                    //szquery += fileurl.cacheid;
                }
            }

            if(fileurl.auth != undefined){
                if (fileurl.auth != null){
                    openfileobj.auth = fileurl.auth;
                }
            }

            if(fileurl.xrefurl != undefined){
                if (fileurl.xrefurl != null){
                    openfileobj.xrefurl = fileurl.xrefurl;
                }
            }


            if(fileurl.requestheaders != undefined){
                //console.log(requestheders);
            }

            
            //var openfileobj = {filename: szquery, displayname : null};    

            if(fileurl.displayname != undefined){
                if (fileurl.displayname != null){
                    openfileobj.displayname = fileurl.displayname;    
                }
            }

            if(bopenfile){
                getFile(openfileobj, callback, progressCallBack);
            }else{
                if (RxCore_GUI_Download != undefined) {
                    RxCore_GUI_Download.setDownload("hide");
                }

            }
            

        }else if (typeof(fileurl) == 'string'){
            openfileobj = {filename: fileurl, displayname : null};
            getFile(openfileobj, callback, progressCallBack);
        }*/

        
        
    }


    function RxCore_OpenFilePagesPDFdoc(fileurl, pdfdoc, pages, callback) {
        if (RxCore_GUI_Download != undefined) {
            RxCore_GUI_Download.setDownload("show");
        }

        bUseCustomrelpath = false;

        getfilePagesPDFdoc(fileurl, pdfdoc, pages, callback);

    }


    function RxCore_OpenFileLayoutsEx(fileurl, pages, callback) {
        
        if (RxCore_GUI_Download != undefined) {
            RxCore_GUI_Download.setDownload("show");
        }

        bUseCustomrelpath = false;


        var bopenfile = false;

        var openfileobj = getFileObject(fileurl);
        
        if(openfileobj != undefined){
            if(openfileobj.filename != null){
                bopenfile = true;
            }

            if(bopenfile){
                getFilePagesEx(openfileobj, pages, true, callback);
            }else{
                if (RxCore_GUI_Download != undefined) {
                    RxCore_GUI_Download.setDownload("hide");
                }

            }

        }


        //getFilePagesEx(fileurl,pages, true, callback);

    }


    function RxCore_OpenFileLayouts(fileurl, pages, callback) {
        if (RxCore_GUI_Download != undefined) {
            RxCore_GUI_Download.setDownload("show");
        }



        bUseCustomrelpath = false;
        getFilePages(fileurl,pages, true, callback);

    }


    function RxCore_OpenFilePages(fileurl, pages, callback) {
        if (RxCore_GUI_Download != undefined) {
            RxCore_GUI_Download.setDownload("show");
        }

        bUseCustomrelpath = false;
        //getFilePages(fileurl,pages, callback);
        getFilePages(fileurl,pages, false, callback);

    }

    function RxCore_switchpages(fileurl, pages, callback){
        if (RxCore_GUI_Download != undefined) {
            RxCore_GUI_Download.setDownload("show");
        }

        bUseCustomrelpath = false;
        switchDocPages(fileurl,pages, callback);

    }

    function comparedrawcheck(){

        var page1isvector = false;
        var page0isvector = false;

        if(CompareObj.pages[1].VectorPageObj){

            if(CompareObj.pages[1].VectorPageObj.firstdrawcompare){
                CompareObj.pages[1].VectorPageObj.firstdrawcompare = false;
                //CompareObj.pages[0].VectorPageObj.firstdrawcompare = false;
                page1isvector = true;
                
            }

        }

        if(CompareObj.pages[0].VectorPageObj){
            if (CompareObj.pages[0].VectorPageObj.firstdrawcompare){
                CompareObj.pages[0].VectorPageObj.firstdrawcompare = false;
                
                page0isvector = true;
            }

        }

        if(page1isvector && page0isvector){
            CompareObj.draw_compare(true);
        }

    }

    function comparecheckFoxit(bready){
        if (doCompare) {

            if(CompareFiles.length != 0){
                for (var ci = 0; ci < CompareFiles.length; ci++){
                    if(CompareFiles[ci].state == 2){
                        CompareFiles[ci].requirefoxit = true;
                        CompareFiles[ci].foxitready = bready;
                    }
                }
            }
            
        }

    }

    function comparecheckFileLoad(fileName){
        if (doCompare) {

            if(CompareFiles.length != 0){

    
                for (var ci = 0; ci < CompareFiles.length; ci++){
                    if (fileName == CompareFiles[ci].Name) {
                        CompareFiles[ci].loaded = true;
                    }
                }
            }
            
        }

    }

    function comparecheck() {
        var compareReady = true;

        if(CompareFiles.length == 0 && !doCompare){
            compareReady = false;
        }

        for (var i = 0; i < CompareFiles.length; i++) {

            if(CompareFiles[i].state == 2 && !CompareFiles[i].loaded){

                if(CompareFiles[i].requirefoxit){
                    if(CompareFiles[i].foxitready){
                        getFile(CompareFiles[i].openobj);
                    }

                }else{
                    getFile(CompareFiles[i].openobj);
                }
                
            }

            if (compareReady) {
                compareReady = CompareFiles[i].loaded;
            }

        }

        if (compareReady) {
            RxCore_GetOpenFiles();
            doCompare = false;
            CompareFiles = [];
            //RxCore_Compare(true);
            RxCore_CompareEx(true);


        }

    }

    function RxCore_consolidate(select){

        //bConsolidate = true;
        if(bCanConsolidate){

            consolidateObj.isactive = select;

            if(RxCore_GUI_Consolidate != undefined){
                RxCore_GUI_Consolidate.Consolidatevt();
            }

        }


    }

    /*function RxCore_consolidateObj(){

        return consolidateObj;

    }*/

    function getCompareFile(){

        var compareObject = undefined;

        for (var i = 0; i < OpenFiles.length; i++) {
            if (OpenFiles[i].bcompare) {
                //bhaveCompare = true;
                compareObject = OpenFiles[i];
                //OpenFiles[i].scalecomparepage();
                break;
            }
        }

        return compareObject;

    }

    function RxCore_alignCompareEx(alignarray, fileindex){

        if(docpagNotReady()){
            return;
        }

        if (!documentopen) {
            return;
        }

        unselelectallmarkup();
        unselecteditmarkupall();

        for (var i = 0; i < OpenFiles.length; i++) {
            if (OpenFiles[i].fileindex == fileindex) {
                //bhaveCompare = true;
                OpenFiles[i].scalecomparepage(alignarray);
                break;
            }
        }

    }


    function RxCore_alignCompare(alignarray){
        if(docpagNotReady()){
            return;
        }

        unselelectallmarkup();
        unselecteditmarkupall();


        var bhaveCompare = false;

        var lCompareObj = getCompareFile();
        if (lCompareObj != undefined){
            bhaveCompare = true;
            lCompareObj.scalecomparepage(alignarray);

        }

        /*var bhaveCompare = false;
        for (var i = 0; i < OpenFiles.length; i++) {
            if (OpenFiles[i].Type == 'Compare') {
                bhaveCompare = true;
            }
        }

        CompareObj.scalecomparepage(alignarray);*/
        

    }

    function RxCore_compareScale(){
        if(docpagNotReady()){
            return;
        }

        unselelectallmarkup();
        unselecteditmarkupall();

        var bhaveCompare = false;

        var lCompareObj = getCompareFile();

        if (lCompareObj != undefined){
            bhaveCompare = true;
            lCompareObj.scalecompare();

        }

        /*var bhaveCompare = false;
        for (var i = 0; i < OpenFiles.length; i++) {
            if (OpenFiles[i].Type == 'Compare') {
                bhaveCompare = true;
            }
        }

        CompareObj.scalecompare();*/

        //find background file index
        //RxCore_setActiveFile(indx);
        //tool = new tools['overlayscale']();


    }

    function RxCore_getCompareScale(){
        if(docpagNotReady()){
            return;
        }

        unselelectallmarkup();
        unselecteditmarkupall();

        //DocObj.getCurrentpage().scalecompare();

    }

    function RxCore_CompareNudgeRotate(clockwise){
        if(docpagNotReady()){
            return;
        }

        unselelectallmarkup();
        unselecteditmarkupall();

        var bhaveCompare = false;

        var lCompareObj = getCompareFile();
        if (lCompareObj != undefined){
            bhaveCompare = true;
            lCompareObj.nudgeRotate(clockwise);

        }

        /*var bhaveCompare = false;
        for (var i = 0; i < OpenFiles.length; i++) {
            if (OpenFiles[i].Type == 'Compare') {
                bhaveCompare = true;
            }
        }
        CompareObj.nudgeRotate(clockwise);*/

    }

    function RxCore_CompareNudgeScale(updown){
        if(docpagNotReady()){
            return;
        }

        unselelectallmarkup();
        unselecteditmarkupall();

        var bhaveCompare = false;

        var lCompareObj = getCompareFile();
        if (lCompareObj != undefined){
            bhaveCompare = true;
            lCompareObj.nudgeScale(updown);

        }

        /*var bhaveCompare = false;
        for (var i = 0; i < OpenFiles.length; i++) {
            if (OpenFiles[i].Type == 'Compare') {
                bhaveCompare = true;
            }
        }
        CompareObj.nudgeScale(updown);*/


    }

    function RxCore_CompareOffset(x,y){
        if(docpagNotReady()){
            return;
        }

        unselelectallmarkup();
        unselecteditmarkupall();

        var bhaveCompare = false;

        var lCompareObj = getCompareFile();
        if (lCompareObj != undefined){
            bhaveCompare = true;
            lCompareObj.offsetdirect(x,y);

        }

        /*var bhaveCompare = false;
        for (var i = 0; i < OpenFiles.length; i++) {
            if (OpenFiles[i].Type == 'Compare') {
                bhaveCompare = true;
            }
        }

        CompareObj.offsetdirect(x,y);*/
        
    }

    function RxCore_CompareNudgeOffset(direction){
        if(docpagNotReady()){
            return;
        }

        unselelectallmarkup();
        unselecteditmarkupall();

        var bhaveCompare = false;

        var lCompareObj = getCompareFile();
        if (lCompareObj != undefined){
            bhaveCompare = true;
            lCompareObj.nudgeoffset(direction);

        }

        /*var bhaveCompare = false;
        for (var i = 0; i < OpenFiles.length; i++) {
            if (OpenFiles[i].Type == 'Compare') {
                bhaveCompare = true;
            }
        }
        CompareObj.nudgeoffset(direction);*/
    }

    function RxCore_setCompareScale(scalearray){

        if(docpagNotReady()){
            return;
        }

        unselelectallmarkup();
        unselecteditmarkupall();

        var bhaveCompare = false;

        var lCompareObj = getCompareFile();
        if (lCompareObj != undefined){
            bhaveCompare = true;
            lCompareObj.setcomparescale(scalearray);

        }


        /*var bhaveCompare = false;
        for (var i = 0; i < OpenFiles.length; i++) {
            if (OpenFiles[i].Type == 'Compare') {
                bhaveCompare = true;
            }
        }

        CompareObj.setcomparescale(scalearray);*/

    }


    function compareOverlaySaveTo(fileurlbackground, fileurloverlay, destFolder, outputName, colors, dpi, alignarray){

            /*if(xmlurl == undefined){
            return;
        }*/

        if(CompareFilesServer == undefined){
            return;
        }

        if(colors == undefined ){
            colors = {BackColor : 'rgb(255,0,0)', FrontColor : 'rgb(0,0,255)', EqualColor : 'rgb(128,128,128)'};
        }

        //C:\\Rasterex\\Upload\\file1.pdf
        //C:\\Rasterex\\Upload\\file2.pdf

        /*var XMLGetFile = "<?xml version=\"1.0\" encoding=\"UTF-8\"?>";

        XMLGetFile += "<RxViewServer>";
        XMLGetFile += "<Command>Compare</Command>";
        XMLGetFile += "<LicenseID>" + LicenseID + "</LicenseID>";
        XMLGetFile += "<BackURL>" + fileurlbackground + "</BackURL>";
        XMLGetFile += "<FrontURL>" + fileurloverlay + "</FrontURL>";
        XMLGetFile += "<CompareDPI>200</CompareDPI>";
        XMLGetFile += "<BackColor>rgb(255,0,0)</BackColor>";
        XMLGetFile += "<FrontColor>rgb(0,0,255)</FrontColor>";
        XMLGetFile += "<EqualColor>rgb(0,0,0)</EqualColor>";
        XMLGetFile += "<ReturnData>text</ReturnData>";
        XMLGetFile += "</RxViewServer>";*/

        if(dpi == undefined){
            dpi = 200;
        }


        //"DestFolder" : "C:\\\\Rasterex\\\\Upload\\\\",
        //"OutputName" : "Comparison 1.pdf",


        var JSONGetCompare = [{
            Command : 'Compare', 
            LicenseID : LicenseID, 
            BackURL : fileurlbackground, 
            FrontURL : fileurloverlay,
            DestFolder : destFolder,
            OutputName :outputName,
            CompareDPI : dpi, 
            BackColor : colors.BackColor, 
            FrontColor : colors.FrontColor, 
            EqualColor : colors.EqualColor,
            ReturnData :'text'
        }];

        if(alignarray != undefined){
            if(alignarray.length > 1 ){
                JSONGetCompare.push(alignarray[0]);
                JSONGetCompare.push(alignarray[1]);
            }
        }

        var jsonstring = JSON.stringify(JSONGetCompare);


        sendHttpRequest('POST', CompareFilesServer, '', jsonstring).then(function (response){

            
            /*if (checkcomparefiles()){
        
                enablebutton();
                //console.log('files selected');
                //var buttonelem = 
            }else{
                disablebutton();
            }*/
        
            var pdflink = xmlurlrel + response;
            //console.log(pdflink);

            if(RxCore_GUI_Servercompare != undefined){

                RxCore_GUI_Servercompare.loadComplete(pdflink, true);
                
            }

            /*var openpdfrange = {
                range:{
                    url: pdflink
                }
            };*/
          
            //xmlurlrel

            //var openpdfilename = {fileName:'compare.pdf'};
            //pdfui.openPDFByHttpRangeRequest(openpdfrange, openpdfilename);

            //document.getElementById('pdfviewer').src = pdflink;

            
        }).catch(function(error){

            if(error.status){
                alert(error.status);
            }else{
        
                alert(error);
        
            }
        

        });

    }


    function compareOverlayServerJSON(fileurlbackground, fileurloverlay, colors, dpi, alignarray){
        /*if(xmlurl == undefined){
            return;
        }*/

        if(CompareFilesServer == undefined){
            return;
        }

        if(colors == undefined ){
            colors = {BackColor : 'rgb(255,0,0)', FrontColor : 'rgb(0,0,255)', EqualColor : 'rgb(128,128,128)'};
        }

        //C:\\Rasterex\\Upload\\file1.pdf
        //C:\\Rasterex\\Upload\\file2.pdf

        /*var XMLGetFile = "<?xml version=\"1.0\" encoding=\"UTF-8\"?>";

        XMLGetFile += "<RxViewServer>";
        XMLGetFile += "<Command>Compare</Command>";
        XMLGetFile += "<LicenseID>" + LicenseID + "</LicenseID>";
        XMLGetFile += "<BackURL>" + fileurlbackground + "</BackURL>";
        XMLGetFile += "<FrontURL>" + fileurloverlay + "</FrontURL>";
        XMLGetFile += "<CompareDPI>200</CompareDPI>";
        XMLGetFile += "<BackColor>rgb(255,0,0)</BackColor>";
        XMLGetFile += "<FrontColor>rgb(0,0,255)</FrontColor>";
        XMLGetFile += "<EqualColor>rgb(0,0,0)</EqualColor>";
        XMLGetFile += "<ReturnData>text</ReturnData>";
        XMLGetFile += "</RxViewServer>";*/

        if(dpi == undefined){
            dpi = 200;
        }


        var JSONGetCompare = [{
            Command : 'Compare', 
            LicenseID : LicenseID, 
            BackURL : fileurlbackground, 
            FrontURL : fileurloverlay, 
            CompareDPI : dpi, 
            BackColor : colors.BackColor, 
            FrontColor : colors.FrontColor, 
            EqualColor : colors.EqualColor,
            ReturnData :'text'
        }];

        if(alignarray != undefined){
            if(alignarray.length > 1 ){
                JSONGetCompare.push(alignarray[0]);
                JSONGetCompare.push(alignarray[1]);
            }
        }

        var jsonstring = JSON.stringify(JSONGetCompare);


        sendHttpRequest('POST', CompareFilesServer, '', jsonstring).then(function (response){

            
            /*if (checkcomparefiles()){
        
                enablebutton();
                //console.log('files selected');
                //var buttonelem = 
            }else{
                disablebutton();
            }*/
        
            var pdflink = xmlurlrel + response;
            //console.log(pdflink);

            if(RxCore_GUI_Servercompare != undefined){

                RxCore_GUI_Servercompare.loadComplete(pdflink, true);
                
            }

            /*var openpdfrange = {
                range:{
                    url: pdflink
                }
            };*/
          
            //xmlurlrel

            //var openpdfilename = {fileName:'compare.pdf'};
            //pdfui.openPDFByHttpRangeRequest(openpdfrange, openpdfilename);

            //document.getElementById('pdfviewer').src = pdflink;

            
        }).catch(function(error){

            if(error.status){
                alert(error.status);
            }else{
        
                alert(error);
        
            }
        

        });


    }

    function compareOverlayServer(fileurlbackground, fileurloverlay, alignarray){


        if(CompareFilesServer == undefined){
            return;
        }


        /*<?xml version="1.0" encoding="UTF-8"?>
        <RxViewServer>
            <Command>Compare</Command>
            <LicenseID>2</LicenseID>
            <BackURL>C:\\Rasterex\\Upload\\file1.pdf</BackURL>
            <FrontURL>C:\\Rasterex\\Upload\\file2.pdf</FrontURL>
            <CompareDPI>200</CompareDPI>
            <BackColor>rgb(255,0,0)</BackColor>
            <FrontColor>rgb(0,0,255)</FrontColor>
            <EqualColor>0</EqualColor>
            <ReturnData>text</ReturnData>
        </RxViewServer>*/

        //sendHttpRequestXML('POST', xmlurl, XMLGetFile, callbacklist).then(function (xmlDoc){


            /*var XMLGetFile = "<?xml version=\"1.0\" encoding=\"UTF-8\"?>";
            XMLGetFile += "<RxViewServer>";
            XMLGetFile += "<Command>GetFile</Command>";
            XMLGetFile += "<LicenseID>" + LicenseID + "</LicenseID>";
            XMLGetFile += "<FileURL>" + Filepath + "</FileURL>";
            XMLGetFile += "</RxViewServer>";*/
    
            //var url = CompareFilesServer;

            /* url += "&";
            url += fileurlbackground;
            url += "&";
            url += fileurloverlay;*/
    


        var JSONGetCompare = [{
            Command : 'Compare', 
            LicenseID : LicenseID, 
            BackURL : fileurlbackground, 
            FrontURL : fileurloverlay, 
            CompareDPI : 200, 
            BackColor : 'rgb(255,0,0)', 
            FrontColor : 'rgb(0,0,255)', 
            EqualColor : 'rgb(128,128,128)', 
            ReturnData :'text'
        }];

        if(alignarray != undefined){
            if(alignarray.length > 1 ){
                JSONGetCompare.push(alignarray[0]);
                JSONGetCompare.push(alignarray[1]);
            }
        }

        var jsonstring = JSON.stringify(JSONGetCompare);

        sendHttpRequest('POST', CompareFilesServer, '', jsonstring).then(function (response){

            
            /*if (checkcomparefiles()){
        
                enablebutton();
                //console.log('files selected');
                //var buttonelem = 
            }else{
                disablebutton();
            }*/
        
            var pdflink = xmlurlrel + response;
            //console.log(pdflink);

            if(RxCore_GUI_Servercompare != undefined){

                RxCore_GUI_Servercompare.loadComplete(pdflink, true);
                
            }

            /*var openpdfrange = {
                range:{
                    url: pdflink
                }
            };*/
          
            //xmlurlrel

            //var openpdfilename = {fileName:'compare.pdf'};
            //pdfui.openPDFByHttpRangeRequest(openpdfrange, openpdfilename);

            //document.getElementById('pdfviewer').src = pdflink;

            
        }).catch(function(error){

            if(error.status){
                alert(error.status);
            }else{
        
                alert(error);
        
            }
        

        });

    }

    function compareOverlay(fileurlbackground, fileurloverlay, mode){
        doCompare = true;
        compareMode = mode;
        for (var i = 0; i < OpenFiles.length; i++) {
            if (OpenFiles[i].Type == 'Compare') {
                OpenFiles.splice(i, 1);
            }
        }

        var openfileobjbg = {filename: fileurlbackground, displayname : null};
        var openfileobjov = {filename: fileurloverlay, displayname : null};

        var OpenFileObject = {
            Name:getFileName(fileurlbackground),
            loaded:false,
            requirefoxit : false,
            disableMenu: true,
            state:1,
            openobj : openfileobjbg,
            foxitready : false,
        };

        CompareFiles.push(OpenFileObject);

        OpenFileObject = {
            Name:getFileName(fileurloverlay),
            loaded:false,
            requirefoxit : false,
            disableMenu: true,
            state:2,
            openobj : openfileobjov,
            foxitready : false,

        };

        CompareFiles.push(OpenFileObject);

        if (RxCore_GUI_Download != undefined) {
            RxCore_GUI_Download.setDownload("show");
        }


        getFile(openfileobjbg);
        //getFile(openfileobjov);
    }

    function RxCore_OverlayFiles(fileurlbackground, fileurloverlay) {

        compareOverlay(fileurlbackground, fileurloverlay,1);

    }

    function RxCore_CompareFiles(fileurlbackground, fileurloverlay) {

        compareOverlay(fileurlbackground, fileurloverlay,0);

    }

    function RxCore_Nav3Dreplacediag(){
        if (RxCore_GUI_Nav3DreplaceDiag != undefined) {
            if (OpenfileNames.length == 0) {
                RxCore_GetOpenFiles();
            }
            RxCore_GUI_Nav3DreplaceDiag.CompareDialog();
        }
    }


    function RxCore_Comparediag() {

        if (RxCore_GUI_CompareDiag != undefined) {
            if (OpenfileNames.length == 0) {
                RxCore_GetOpenFiles();
            }
            RxCore_GUI_CompareDiag.CompareDialog();
        }

    }

    function CheckPageloaded(pagenum){

        const renderpagePromise = new Promise(function(resolve,reject){
            Rxcore_GUI_PDFPageRenderComplete.connect(function(pagenumber){
                
                

                /*var pagingobject = {
                    numpages: this.pages.length,
                    currentpage: this.currentpage
                };*/

                /*var pagedata = {
                    page: thispage.pagenumber,
                    start: renderstart,
                    end: renderend
                };*/

                if(pagenumber == pagenum){
                    DocObj.pages[pagenumber].rendercallback = false;
                    resolve(pagenum);
                }


            });

            if(DocObj.pages[pagenum] != undefined){
                DocObj.pages[pagenum].rendercallback = true;
            }
            
            DocObj.GotoPage(pagenum);
            DocObj.draw_mpagepdf();
            


        }); 

        return renderpagePromise;

    }

    function CheckAllfilesRendering(){
        

        const renderPromise = new Promise(function(resolve,reject){

            RxCore_GUI_RenderComplete.connect(function(fileindex){
                
                var brendering = false;

                for (var i = 0; i < OpenFiles.length; i++) {
                    
                    if(OpenFiles[i].bRenderinProgress){
                        brendering = true;
                    }
                    
                }

                if(!brendering){
                    resolve(fileindex);
                }else{
                    //reject(index);
                }

            });

            for (var i = 0; i < OpenFiles.length; i++) {
                OpenFiles[i].checkRender();
            }
    

            //internalClosedoc(index);

        });        

        return renderPromise;

    }

    

    function DeactivateAll() {
        for (var i = 0; i < OpenFiles.length; i++) {
            OpenFiles[i].bActive = false;
        }

    }

    /*function RxCore_CompareEx(filelist,okcancel){
        var bindx = -1;
        var oindx = -1;

        if (OpenfileNames.length > 1 && okcancel){

        }


    }*/

    function RxCore_exicompareMeasure(){
        if(docpagNotReady()){
            return;
        }

        if(documentcompare){
            return;
        }

        DocObj.getCurrentpage().enableSnap(false);
        
        set_tool('markupedit', {p1 : false});

    }
    function RxCore_CompareMesure(index){
        if(docpagNotReady()){
            return;
        }

        if(documentcompare){
            return;
        }

        DocObj.getCurrentpage().enableSnap(true);
        set_tool('getoverlayscale', {p1 : DocObj.getCurrentpage(), p2 : index});
        //tool = new tools['getoverlayscale'](DocObj.getCurrentpage(), index);

    }

    function CreateCompareOverlayEx(okcancel,mode){

        if(docpagNotReady()){
            return;
        }

        var bindx = -1;
        var oindx = -1;

        if(documentcompare){
            return;
        }

        if (OpenfileNames.length > 1 && okcancel) {
            for (var i = 0; i < OpenfileNames.length; i++) {
                if (OpenfileNames[i].state == 1) {
                    var backgroundFileName = OpenfileNames[i].Name;
                    var backgroundindex = OpenfileNames[i].index;
                }
                if (OpenfileNames[i].state == 2) {
                    var overlayFileName = OpenfileNames[i].Name;
                    var overlayindex = OpenfileNames[i].index;
                }


            }
            for (i = 0; i < OpenFiles.length; i++) {
                if (OpenFiles[i].fileindex == backgroundindex) {
                    bindx = i;
                    OpenFiles[i].disableMenu = true;
                }
                if (OpenFiles[i].fileindex == overlayindex) {

                    oindx = i;
                    OpenFiles[i].disableMenu = true;
                }

            }

            if (bindx != -1 && oindx != -1 && bindx != oindx) {

                //CompareObj = new CompareObject(OpenFiles[bindx].pages[OpenFiles[bindx].currentpage], OpenFiles[oindx].pages[OpenFiles[oindx].currentpage]);

                createCompare(OpenFiles[bindx].pages[OpenFiles[bindx].currentpage], OpenFiles[oindx].pages[OpenFiles[oindx].currentpage], mode);


                var stateobj = {
                    iscompare:true,
                    numOpenFiles:OpenFiles.length,
                    isPDF:DocObj.getCurrentpage().usepdfjs,
                    is3D:DocObj.getCurrentpage().usevector3Dxml,
                    markupOn3D : false,
                    is2D:DocObj.getCurrentpage().usevectorxml,
                    numpages:DocObj.pages.length,
                    currentpage:DocObj.getcurPage(),
                    activefile : DocObj.bActive,
                    source : 'createcompareoverlay'
                };
                if (RxCore_GUI_State != undefined) {
                    RxCore_GUI_State.setGUIState(stateobj);
                }

                if (RxCore_GUI_Download != undefined) {
                    RxCore_GUI_Download.setDownload("hide");
                }

                if(Rxcore_GUI_fileLoadComplete != undefined){
                    Rxcore_GUI_fileLoadComplete.loadComplete('compare', true);
                }

                if(RxCore_GUI_CompareDiag != undefined){
                    RxCore_GUI_CompareDiag.CompareDialog();
                }

            }


        } else {
            OpenfileNames = [];
        }
        
    }    

    

    function RxCore_Overlay(okcancel){
        //CreateCompareOverlay(okcancel, 1);
        CreateCompareOverlayEx(okcancel, 1);
    }

    function RxCore_Compare(okcancel) {

        //CreateCompareOverlay(okcancel, 0);
        CreateCompareOverlayEx(okcancel, 0);

        //DocObj.PageDown();


    }

    function RxCore_CompareEx(okcancel) {

        CreateCompareOverlayEx(okcancel, 0);

        //DocObj.PageDown();


    }


    

    function getMarkupfromXML(xmldata){
        var MarkupFilePath = 0;
        var i = 0;

        //var xmlDoc = $.parseXML(xmldata).documentElement;
        var xmlDoc = XMLParser(xmldata);

        if (xmlDoc.getElementsByTagName('FileVersion') != null && xmlDoc.getElementsByTagName('FileVersion').length != 0) {
            var fileVersion = xmlDoc.getElementsByTagName('FileVersion')[0].firstChild.nodeValue;
        }

        if (xmlDoc.getElementsByTagName('User') != null && xmlDoc.getElementsByTagName('User').length != 0) {
            var UserID = xmlDoc.getElementsByTagName('User');
            for (i = 0; i < UserID.length; i++) {

                var MarkupUserDispName = UserID[i].getElementsByTagName("Name")[0].firstChild.nodeValue;
                var MarkupUserID = UserID[i].getElementsByTagName("ID")[0].firstChild.nodeValue;

                if (UserID[i].getElementsByTagName("Layer")[0] != undefined) {
                    var MarkupUserLayer = UserID[i].getElementsByTagName("Layer")[0].firstChild.nodeValue;
                } else {
                    MarkupUserLayer = markuplayer;
                }
                if (UserID[i].getElementsByTagName("Color")[0] != undefined) {
                    var MarkupUserColor = UserID[i].getElementsByTagName("Color")[0].firstChild.nodeValue;
                } else {
                    MarkupUserColor = markupcolor;
                }

                if (Userlist[0].Signature == MarkupUserID) {
                    //Userlist[0] = new Users(MarkupUserID,MarkupUserDispName,MarkupUserLayer,MarkupUserColor);
                    //numUsers++;
                } else {
                    Userlist.push(new Users(MarkupUserID, MarkupUserDispName, MarkupUserLayer, MarkupUserColor, false));
                    numUsers++;
                }
            }
        } else {
            if (xmlDoc.getElementsByTagName('Name') != null) {
                var sign = xmlDoc.getElementsByTagName('Name');
                if (Userlist[0].Signature != sign[0].firstChild.nodeValue) {

                    Userlist.push(new Users(sign[0].firstChild.nodeValue, sign[0].firstChild.nodeValue, markuplayer, markupcolor, false));
                    
                    //Userlist[numUsers] = new Users(sign[0].firstChild.nodeValue, sign[0].firstChild.nodeValue, markuplayer, markupcolor);
                    numUsers++;

                }
            }
        }
        if (xmlDoc.getElementsByTagName('Entity') != null) {
            var markupentityobj = xmlDoc.getElementsByTagName('Entity');

            for (i = 0; i < markupentityobj.length; i++) {
                var markupType = markupentityobj[i].getElementsByTagName("Type")[0].firstChild.nodeValue;
                var markupSubtype = markupentityobj[i].getElementsByTagName("Subtype")[0].firstChild.nodeValue;
                var markupAlternative = markupentityobj[i].getElementsByTagName("Alternative")[0].firstChild.nodeValue;
                var markupID = !markupentityobj[i].getAttribute('ID') ? null : markupentityobj[i].getAttribute('ID');

                if ( markupentityobj[i].getElementsByTagName('HaveLeader')[0] != undefined) {
                    var bhasArrow = (parseInt(markupentityobj[i].getElementsByTagName('HaveLeader')[0].firstChild.nodeValue) == 1);
                }

                if(bhasArrow){
                    if (markupentityobj[i].getElementsByTagName('Entity')[0] != undefined){
                        var arrowleader = markupentityobj[i].getElementsByTagName("Entity")[0];
                        var leadermarkupType = arrowleader.getElementsByTagName("Type")[0].firstChild.nodeValue;
                        var leadermarkupSubtype = arrowleader.getElementsByTagName("Subtype")[0].firstChild.nodeValue;
                        var leadermarkupAlternative = arrowleader.getElementsByTagName("Alternative")[0].firstChild.nodeValue;
                        var leadermarkupID = !arrowleader.getAttribute('ID') ? null : arrowleader.getAttribute('ID');
                        leadermarkupType = parseInt(leadermarkupType);
                        leadermarkupSubtype = parseInt(leadermarkupSubtype);
                        leadermarkupAlternative = parseInt(leadermarkupAlternative);
                        var leaderxmlmarkupobj = new MarkupObject(leadermarkupType, leadermarkupSubtype, leadermarkupAlternative);
                        var blOK = leaderxmlmarkupobj.SetfromXML(DocObj,arrowleader, fileVersion);

                        if(blOK){
                            markupentityobj[i].removeChild(arrowleader);
                            leaderxmlmarkupobj.savemetolistLoad(DocObj,false);

                        }
                    }
                }

                markupType = parseInt(markupType);
                markupSubtype = parseInt(markupSubtype);
                markupAlternative = parseInt(markupAlternative);
                var xmlmarkupobj = new MarkupObject(markupType, markupSubtype, markupAlternative);
                xmlmarkupobj.markupID = markupID;
                

                var bOK = xmlmarkupobj.SetfromXML(DocObj, markupentityobj[i], fileVersion);
                if (bOK) {
                    if(blOK && bhasArrow && leaderxmlmarkupobj != null){
                        xmlmarkupobj.bhasArrow = bhasArrow;
                        xmlmarkupobj.markupArrowConnected = leaderxmlmarkupobj;

                        leaderxmlmarkupobj.bisTextArrow = true;
                        leaderxmlmarkupobj.textBoxConnected = xmlmarkupobj;
                        leaderxmlmarkupobj.setArrowPoint(xmlmarkupobj);


                        blOK = false;
                        bhasArrow = false;
                    }

                    xmlmarkupobj.savemetolistLoad(DocObj);
                }

            }

            /* Andriy call callback only once */

            /*for(var mc = 0; mc < DocObj.markuplist.length; mc++){
                if(DocObj.markuplist[mc].bhasArrow && !isNaN(DocObj.markuplist[mc].markupArrowConnected)){
                    var markupnum = DocObj.markuplist[mc].markupArrowConnected;
                    for(var ac = 0; ac < DocObj.markuplist.length; ac++){
                        if (DocObj.markuplist[ac].markupnumber == markupnum){
                            DocObj.markuplist[mc].markupArrowConnected = DocObj.markuplist[ac];
                        }
                    }
                }
            }*/


            if (xmlmarkupobj) xmlmarkupobj.notify();

            if (markupentityobj.length == 0) {
                if (RxCore_GUI_Markuplist != undefined) {
                    RxCore_GUI_Markuplist.setMarkupList(DocObj.getmarkuplist(),DocObj.bActive);
                    /* Andriy */
                    // RxCore_GUI_Markuplist.notify();
                }

            }

            drawmarkupAll(cntximg);

        }


    }

    function getJSONmarkuplist(){
        if(docpagNotReady()){
            return;
        }

        if (documentcompare) {
            return;
        }

        //return createxmlmarkup(true, false,true,signature, DisplayName);
    }

    function RxCore_getMarkupXMLData(){
        if(docpagNotReady()){
            return;
        }

        if (documentcompare) {
            return;
        }

        return createxmlmarkup(true, false,true,signature, DisplayName);
     }

    function RxCore_setMarkupXMLData(xmldata){
        if(docpagNotReady()){
            return;
        }

        if (documentcompare) {
            return;
        }

        getMarkupfromXML(xmldata);
    }




    function RxCore_xmlurlEx(fileurl){
        getxmlurlEx(fileurl);
    }


    function RxCore_xmlurl(fileurl) {

        getxmlurl(fileurl);
    }

    function RxCore_getAllPageDimensions(){
        if(docpagNotReady()){
            return;
        }

        return DocObj.getAllPageDim();

    }
    function RxCore_getPageDimensions(pagenum){
        if(docpagNotReady()){
            return;
        }

        return DocObj.getPageDim(pagenum);

    }

    function getCurrentVectorPageObject(){
        return DocObj.getCurrentpage().getVectorPageObject();
    }


    function RxCore_handlePDFPassword(passwordFunc, reason){

        //callback(password);

        if (reason === 1) { // need a password
            if(Rxcore_GUI_PDFPassWord != undefined){
                Rxcore_GUI_PDFPassWord.PassWordRequest(passwordFunc, reason);
            }
            //var new_password= confirm('Please enter a password:')


          } else { // Invalid password
            
            //var new_password= confirm('Invalid! Please enter a password:')

            if(Rxcore_GUI_PDFPassWord != undefined){
                Rxcore_GUI_PDFPassWord.PassWordRequest(passwordFunc, reason);
            }

          }
          //passwordFunc('test');

    }    

    function RxCore_setPageDimensions(pagenum, height, width,  x, y){
        if(docpagNotReady()){
            return;
        }

        DocObj.setPageDim(pagenum, height, width,  x, y);


    }



    function RxCore_getPageRotation(){
        var rotation = 0;

        if(docpagNotReady()){
            rotation = 0;
            //return;
        }else{
            rotation = DocObj.getCurrentpage().getpageRotation();
        }

        return rotation;

    }


    function RxCore_redrawCurrentPage(){
        if(docpagNotReady()){
            return;
        }

        if (!documentopen) {
            return;
        }


        DocObj.getCurrentpage().redraw();

    }

    function RxCore_redrawPage(pagenum){
        if(docpagNotReady()){
            return;
        }

        if (!documentopen) {
            return;
        }

        if(DocObj.getPageObject(pagenum) == undefined){
            return;
        }

        DocObj.getPageObject(pagenum).redraw();
    }

    function RxCore_suspendDraw(onOff){

        if(docpagNotReady()){
            return;
        }


        bSuspendDraw = onOff;

        if(!onOff){
            //need to get current page.
            var curpage = DocObj.getcurPage();

            if(DocObj.getPageObject(curpage) == undefined){
                return;
            }


            DocObj.getPageObject(curpage).redraw();

        }
    }

    function iterate_rotate(pagenumarr, clockwise){

        for(var pi = 0; pi < pagenumarr.length; pi++){

            
            RxCore_rotateClockwise(pagenumarr[pi], clockwise);

        }

    }

    function RxCore_rotateClockwise(pagenum, clockwise){

        if(docpagNotReady()){
            return;
        }


        if (pagenum.constructor === Array){

            iterate_rotate(pagenum, clockwise);
            return;

        }

        if(DocObj.getPageObject(pagenum) == undefined){
            return;
        }


        var degrees = 0;

        if(clockwise){

            switch (DocObj.getPageObject(pagenum).getpageRotation()) {
                case 0:
                    
                    degrees = 90;
                    break;
                case 90:
                    degrees = 180;
                    
                    break;
                case 270:
                    degrees = 0;
                    break;
                case 180:
                    degrees = 270;
                    
                    break;
            }


        }else{

            switch (DocObj.getPageObject(pagenum).getpageRotation()) {
                case 0:
                    degrees = 270;
                    
                    break;
                case 90:
                    degrees = 0;
                    
                    break;
                case 270:
                    degrees = 180;
                    
                    break;
                case 180:

                    degrees = 90;
    
                    break;
            }


        }

        DocObj.getPageObject(pagenum).rotatepage(degrees);

        if(pagenum == 0){
            DocObj.getPageObject(pagenum).redraw();

        }


    }

    function RxCore_RotatePage(pagenum, degrees){
        if(docpagNotReady()){
            return;
        }

        DocObj.getPageObject(pagenum).rotateimage(degrees);

        if(pagenum == 0){
            DocObj.getPageObject(pagenum).redraw();

        }
        


    }
    
    function RxCore_RotateDrawing(cycle, szrotatetool) {
        if(docpagNotReady()){
            return;
        }


        if (documentcompare){

            CompareObj.rotateimage(cycle, szrotatetool);

        }else{

            /*if(DocObj.pages.length > 1){
                if(DocObj.foxitdoc != null) {
                    RxCore_rotateDocument(cycle, szrotatetool);
                    return;
                }
            }*/

            if (cycle) {
                //unselelectallmarkup();
                //unselecteditmarkupall();
    
    
                switch (DocObj.getCurrentpage().getpageRotation()) {
                    case 0:
                        DocObj.getCurrentpage().rotateimage(90);
                        break;
                    case 90:
                        DocObj.getCurrentpage().rotateimage(180);
                        break;
                    case 270:
                        DocObj.getCurrentpage().rotateimage(0);
                        break;
                    case 180:
                        DocObj.getCurrentpage().rotateimage(270);
                        break;
                }
    
            } else {
                
                if (szrotatetool == '0') {
                    DocObj.getCurrentpage().rotateimage(0);
                }
                if (szrotatetool == '90') {
                    DocObj.getCurrentpage().rotateimage(90);
                }
                if (szrotatetool == '180') {
                    DocObj.getCurrentpage().rotateimage(180);
                }
                if (szrotatetool == '270') {
                    DocObj.getCurrentpage().rotateimage(270);
                }
    
            }
        }


        

    }

    function RxCore_rotateDocument(cycle, szrotatetool){
        if(docpagNotReady()){
            return;
        }

        if(DocObj.foxitdoc == null) {
            return;
        }

        if (cycle) {


            switch (DocObj.documentrotation) {
                case 0:
                    DocObj.rotateDocument(90);
                    break;
                case 90:
                    DocObj.rotateDocument(180);
                    break;
                case 270:
                    DocObj.rotateDocument(0);
                    break;
                case 180:
                    DocObj.rotateDocument(270);
                    break;
            }

        } else {
            
            if (szrotatetool == '0') {
                DocObj.rotateDocument(0);
            }
            if (szrotatetool == '90') {
                DocObj.rotateDocument(90);
            }
            if (szrotatetool == '180') {
                DocObj.rotateDocument(180);
            }
            if (szrotatetool == '270') {
                DocObj.rotateDocument(270);
            }

        }



    }

    function RxCore_Show3DViewMarkers(onoff){

        if(docpagNotReady()){
            return;
        }

        /*if (!documentopen) {
            return;
        }*/

        /*if(DocObj == null || DocObj == undefined){
            return;
        }*/


        var page = DocObj.getCurrentpage();

        if(page){

            if (page.usevector3Dxml) {
                page.show3DViewMarker(onoff);
            }
        }
        
    }


    function RxCore_HideMarkup() {
        if(docpagNotReady()){
            return;
        }

        var bnot3dmarkup = get3DViewNotActive();

        if (DocObj.getCurrentpage().usevector3Dxml) {
            DocObj.getCurrentpage().unSelect3DmarkupAll();
        }
        
        unselelectallmarkup();
        unselecteditmarkupall();

        
        if (DocObj.Drawmarkup) {
            
            DocObj.Drawmarkup = false;

            if (DocObj.getCurrentpage().usevector3Dxml) {
                DocObj.getCurrentpage().show3Dmarkup(false);
            
            }

            hidemarkupAll(false);
            drawmarkupAll(cntximg);
            DocObj.bMarkupLocked = true;

            
            /*else{
                
                hidemarkupAll(false);
                drawmarkupAll(cntximg);
                DocObj.bMarkupLocked = true;
            }*/

        } else {

            DocObj.Drawmarkup = true;
            
            if (DocObj.getCurrentpage().usevector3Dxml) {
                DocObj.getCurrentpage().show3Dmarkup(true);
            }

            hidemarkupAll(true);
            drawmarkupAll(cntximg);
            DocObj.bMarkupLocked = false;


            /*else{
                hidemarkupAll(true);
                drawmarkupAll(cntximg);
                DocObj.bMarkupLocked = false;
            }*/

        }

    }


    function RxCore_hideMarkupByMarkupNumbers(markupNumbers, onOff){ 

        var curmarkup = 0; 

        for (curmarkup = 0; curmarkup < markupNumbers.length; curmarkup++) { 

            if (DocObj.markuplist[markupNumbers[curmarkup]-1] != null) { 

                DocObj.markuplist[markupNumbers[curmarkup]-1].setdisplay(onOff); 


            } 

        } 

    } 

    function RxCore_limitZoomOut(onOff){

        bLimitZoomOut = onOff;
    }


    function RxCore_PageLock(onOff){
        if(docpagNotReady()){
            return;
        }


        DocObj.pagelock(onOff);
    }

    function RxCore_PageonlyMarkup(toggle) {
        if(docpagNotReady()){
            return;
        }


        if (RxCore_GUI_Markuplist != undefined) {
            if (toggle) {
                if (RxCore_GUI_Markuplist.pageonly) {
                    RxCore_GUI_Markuplist.pageonly = false;
                } else {
                    RxCore_GUI_Markuplist.pageonly = true;
                }

            } else {
                RxCore_GUI_Markuplist.pageonly = true;
            }

            RxCore_GUI_Markuplist.setMarkupList(DocObj.getmarkuplist(),DocObj.bActive);
            /* Andriy */
            // RxCore_GUI_Markuplist.notify();
        }

    }

    function RxCore_MarkupRedo() {
        
        if(docpagNotReady()){
            return;
        }
        if (documentcompare) {
            return;
        }
        
        unselelectallmarkup();
        unselecteditmarkupall();
        MarkupRedo();
        drawmarkupAll(cntximg);

        if (RxCore_GUI_Markuplist != undefined) {
            RxCore_GUI_Markuplist.setMarkupList(DocObj.getmarkuplist(), DocObj.bActive);
        }


    }    

    function RxCore_MarkupUndo() {
        if(docpagNotReady()){
            return;
        }
        if (documentcompare) {
            return;
        }

        unselelectallmarkup();
        unselecteditmarkupall();
        MarkupUndo();
        drawmarkupAll(cntximg);

        if (RxCore_GUI_Markuplist != undefined) {
            RxCore_GUI_Markuplist.setMarkupList(DocObj.getmarkuplist(), DocObj.bActive);
        }

    }


    function RxCore_CutMarkup(){

        if(docpagNotReady()){
            return;
        }
        if (documentcompare) {
            return;
        }

        CutmarkupSelected();
    }

    function RxCore_CopyMarkup() {
        if(docpagNotReady()){
            return;
        }
        if (documentcompare) {
            return;
        }

        CopymarkupSelected();
    }

    function RxCore_PasteMarkup() {
        if(docpagNotReady()){
            return;
        }
        if (documentcompare) {
            return;
        }

        MarkupFromLocalStorage();
        drawmarkupAll(cntximg);

    }

    function RxCore_NextMarkup() {
        if(docpagNotReady()){
            return;
        }

        if (documentcompare) {
            return;
        }

        unselelectallmarkup();
        unselecteditmarkupall();
        DocObj.getCurrentpage().MarkupZoom();

    }

    function RxCore_ZoomMarkup(dx, dy, dscale) {
        if(docpagNotReady()){
            return;
        }

        if (documentcompare) {
            return;
        }

        DocObj.getCurrentpage().SetMarkupZoom(dx, dy, dscale);

    }


    function RxCore_FindMarkup() {
        if(docpagNotReady()){
            return;
        }

        if (documentcompare) {
            return;
        }


        unselelectallmarkup();
        unselecteditmarkupall();
        var markuptext = prompt("Search markup text", "");
        if (markuptext != null && markuptext != "") {
            DocObj.getCurrentpage().MarkupFind(markuptext);
        }

    }

    function RxCore_TextSelect(onoff){
        if(docpagNotReady()){
            return;
        }

        if (documentcompare) {
            return;
        }

        DocObj.enableTextSelect(onoff);

    }

    function RxCore_endGetTextRects(){

        if(docpagNotReady()){
            return;
        }

        if (documentcompare) {
            return;
        }
        DocObj.textrectsearchend();

    }

    function RxCore_drawselectedBlocks(){

        if(docpagNotReady()){
            return;
        }
        if (documentcompare) {
            return;
        }

        DocObj.drawselectedBlocks();


    }

    function RxCore_forcetextsearchmarker(){

        if(docpagNotReady()){
            return;
        }
        if (documentcompare) {
            return;
        }
        DocObj.drawsearchRect();

    }

    function RxCore_endTextSearch(){
        if(docpagNotReady()){
            return;
        }
        if (documentcompare) {
            return;
        }
        DocObj.textsearchend();
        /*if (DocObj.getCurrentpage().usepdfjs){
            DocObj.textsearchend();
        }*/

    }

    function RxCore_foxitForceRedraw(){
        if(docpagNotReady()){
            return;
        }

        DocObj.forcefoxitRedraw();

    }

    function RxCore_foxitForceCanvas(){
        
        if(docpagNotReady()){
            return;
        }

        DocObj.forcefoxitCanvas();


    }

    function RxCore_getTextRects(text, casesens){
        if(docpagNotReady()){
            return;
        }
        if (documentcompare) {
            return;
        }

        DocObj.getTextRects(text, casesens);

        //DocObj.textsearch(text,direction,casesens);        
    }


    function RxCore_textSearch(text,direction,casesens){
        if(docpagNotReady()){
            return;
        }
        if (documentcompare) {
            return;
        }

        DocObj.textsearch(text,direction,casesens);



    }

function RxCore_documentTextSearch(text, casesense, wholeword) {
        if (docpagNotReady()) {
          return;
        }
        if (documentcompare) {
          return;
        }

        DocObj.documentTextSearch(text, casesense, wholeword);
      }

    function RxCore_clearDocumentTextSearch() {
    if (docpagNotReady()) {
        return;
    }
    if (documentcompare) {
        return;
    }
    DocObj.clearDocumentTextSearch();
    }

    function RxCore_markupTextWithOrange(item) {
    if (docpagNotReady()) {
        return;
    }
    if (documentcompare) {
        return;
    }
    DocObj.markupTextWithOrange(item);
    }

    function RxCore_removePage(pageIndex) {
        if (docpagNotReady()) {
          return;
        }
        if (documentcompare) {
          return;
        }
        DocObj.removePage(pageIndex);
    }

    function RxCore_movePageTo(pageIndex, destIndex) {
        if (docpagNotReady()) {
          return;
        }
        if (documentcompare) {
          return;
        }
        DocObj.movePageTo(pageIndex, destIndex);
    }

    function RxCore_extractPage(pageIndex, remove) {
        if (docpagNotReady()) {
          return;
        }
        if (documentcompare) {
          return;
        }
        DocObj.extractPage(pageIndex, remove);
    }

    function RxCore_downloadPDF() {
        if (docpagNotReady()) {
          return;
        }
        if (documentcompare) {
          return;
        }

        DocObj.exportPDF();
        //DocObj.extractPage(pageIndex, remove);

    }

    function RxCore_uploadPDF() {
        if (docpagNotReady()) {
          return;
        }
        if (documentcompare) {
          return;
        }

        DocObj.uploadPDF();
        //DocObj.extractPage(pageIndex, remove);

    }

    function RxCore_uploadPDFCustom(endpoint) {
        if (docpagNotReady()) {
          return;
        }
        if (documentcompare) {
          return;
        }

        DocObj.uploadPDFCustom(endpoint);
        //DocObj.extractPage(pageIndex, remove);

    }



    function RxCore_uploadCustomPDF(uploadPDFfile, bcustom, endpoint){

        if(bcustom){
            uploadPDFCustom(uploadPDFfile, endpoint);
        }else{
            uploadPDF(uploadPDFfile);
        }
        

    }



    function RxCore_insertBlankPages(pageIndex, count, width, height) {
        if (docpagNotReady()) {
          return;
        }
        if (documentcompare) {
          return;
        }
        DocObj.insertBlankPages(pageIndex, count, width, height);
      }

    function RxCore_getDPI() {
        const dpi = {
            x: 96,
            y: 96,
        };

        // For browsers that support deviceXDPI and deviceYDPI (mainly Internet Explorer)
        if (
            window.screen.deviceXDPI !== undefined &&
            window.screen.deviceYDPI !== undefined
        ) {
            dpi.x = window.screen.deviceXDPI;
            dpi.y = window.screen.deviceYDPI;
        } else {
            // For other modern browsers
            const div = document.createElement("div");
            div.style.width = "1in";
            div.style.height = "1in";
            div.style.position = "fixed";
            div.style.left = "-100%";
            document.body.appendChild(div);
            dpi.x = div.offsetWidth;
            dpi.y = div.offsetHeight;
            //console.log(div.offsetHeight, div.offsetWidth);
            document.body.removeChild(div);
        }
        return dpi;
    }
    
      function RxCore_importPages(
        file,
        pageIndex,
        pageArray,
        isReplace,
        count,
        width,
        height
      ) {
        if (docpagNotReady()) {
          return;
        }

        if (!documentopen) {
          return;
        }

        return DocObj.importPages(
          file,
          pageIndex,
          pageArray,
          isReplace,
          count,
          width,
          height
        );
      }

      function RxCore_copyPage(pagenum) {
        if (docpagNotReady()) {
          return;
        }

        if (!documentopen) {
          return;
        }

        copiedPageIndex = pagenum;
        copieddocumentIndex = DocObj.fileId;
        DocObj.copyPage(pagenum);
      }

      function RxCore_pastePage(pagenum) {
        if (docpagNotReady()) {
          return;
        }

        if (!documentopen) {
          return;
        }

        DocObj.pastePage(pagenum);
      }

    function RxCore_getAllThumbnailsFromFile(file) {
        if (docpagNotReady()) {
          return;
        }

        if (!documentopen) {
          return;
        }

        return DocObj.getAllThumbnailsFromFile(file);
      }

      function RxCore_getCurrentFileInfo() {

        if (docpagNotReady()) {
            return;
        }
  
        if (!documentopen) {
            return;
        }

        if(DocObj.thumbnails != undefined){

            const thumbnailobj = DocObj.thumbnails[0].thumbnailobj;
            if (thumbnailobj.imageData != null) {
                return {
                    path: DocObj.FileNameSRC,
                    name: DocObj.FileName,
                    date: new Date(),
                    width: thumbnailobj.thumbnail.width,
                    height: thumbnailobj.thumbnail.height,
                    buffer: thumbnailobj.imageData.data
                }
            } else {
                //this.imagectx = this.imagecnv.getContext('2d', { willReadFrequently: true });
                //const thctx = thumbnailobj.thumbnail.getContext('2d', );

                const buffer = thumbnailobj.ctx.getImageData(0, 0, thumbnailobj.thumbnail.width, thumbnailobj.thumbnail.height);
                
                //const buffer = thumbnailobj.thumbnail.getContext('2d', { willReadFrequently: true }).getImageData(0, 0, thumbnailobj.thumbnail.width, thumbnailobj.thumbnail.height)

                return {
                    path: DocObj.FileNameSRC,
                    name: DocObj.FileName,
                    date: new Date(),
                    width: thumbnailobj.thumbnail.width,
                    height: thumbnailobj.thumbnail.height,
                    buffer: buffer.data
                }
            }

        }else{

                return {
                    path: DocObj.FileNameSRC,
                    name: DocObj.FileName,
                    date: new Date(),
                }

        }
        
        


      }

    function RxCore_zoomWithMarkup(markup) {
        return new Promise((resolve, reject) => {
          if (docpagNotReady()) {
            reject()
          }
          if (!documentopen) {
            reject();
          }

          if(DocObj.getCurrentpage().usepdfjs) {
            DocObj.pages[DocObj.currentpage].foxitdirectZoom(markup.documentScale / DocObj.getCurrentpage().getPDFpageScale()).then(() => {
                DocObj.pages[DocObj.currentpage].redrawMarkup()
                setTimeout(() => {
                    resolve()
                }, 50)
                
            })
          } else {
            DocObj.getCurrentpage().setPagestats(markup.documentScale)
            DocObj.pages[DocObj.currentpage].redrawMarkup()
            setTimeout(() => {
                resolve()
            }, 50)
          }
        })
      }    

    

    function RxCore_setMarkupSearchPadding(padding){
        msearchpadding.x = padding.x;
        msearchpadding.y = padding.y;
        msearchpadding.w = padding.w;
        msearchpadding.h = padding.h;
    }

    function RxCore_getMarkupSearchPadding(){
        return msearchpadding;
    }

    function RxCore_panTomarkup(markupobj){

        if(docpagNotReady()){
            return;
        }

        if (!documentopen) {
            return;
        }
        if (documentcompare) {
            return;
        }
        
        DocObj.panToMarkup(markupobj);

    }


    function RxCore_endMarkupTextSearch(){
        if(docpagNotReady()){
            return;
        }

        if (documentcompare) {
            return;
        }
        
        DocObj.endMarkupTextSearch();

        //DocObj.textsearchend();

    }    

    function RxCore_markupTextSearch(text,direction,casesens, padding){
        if(docpagNotReady()){
            return;
        }

        if (documentcompare) {
            return;
        }

        DocObj.markupTextSearch(text,direction,casesens, padding);
        
        //DocObj.textsearch(text,direction,casesens);

    }

    function RxCore_MarkupOutlined(markup) {
        if(docpagNotReady()){
            return;
        }
        if (readonlymode) {
            return;
        }
        if (documentcompare) {
            return;
        }
        var tempfillstyle = fillstyle;
        fillstyle = 0;
        
        if(markup){
            ChangeMarkup(markup,3);    
        }else{
            ChangeMarkupSelected(3);
        }


        if (!bSetGlobal) {
            fillstyle = tempfillstyle;
        }

    }

    function RxCore_MarkupEdged() {

        if(docpagNotReady()){
            return;
        }

        if (readonlymode) {
            return;
        }
        if (documentcompare) {
            return;
        }

        var tempfillstyle = fillstyle;
        fillstyle = 2;
		
        ChangeMarkupSelected(3);

        if (!bSetGlobal) {
            fillstyle = tempfillstyle;
        }


    }


    function RxCore_MarkupFilled(markup) {
        if(docpagNotReady()){
            return;
        }

        if (readonlymode) {
            return;
        }
        if (documentcompare) {
            return;
        }

        var tempfillstyle = fillstyle;
        fillstyle = 1;

        if(markup){
            ChangeMarkup(markup,3);    
        }else{
            ChangeMarkupSelected(3);
        }

        if (!bSetGlobal) {
            fillstyle = tempfillstyle;
        }


    }


    function RxCore_MarkupHatched() {
        if(docpagNotReady()){
            return;
        }
        if (readonlymode) {
            return;
        }
        if (documentcompare) {
            return;
        }

        var tempfillstyle = fillstyle;
        fillstyle = 3 + HatchStyle;
        ChangeMarkupSelected(3);
        if (!bSetGlobal) {
            fillstyle = tempfillstyle;
        }

    }

    function RxCore_GetFillstyle() {
        return fillstyle;
    }

    function RxCore_GetHatchStyle() {
        return HatchStyle;
    }

    function RxCore_gethatchimagebyType(ntype, color){

        return createHatchtypeImage(ntype, color);

    }
    function RxCore_CreateHatchStyles(){

        createHatch(0, markupcolor);
        createHatch(1, markupcolor);
        createHatch(2, markupcolor);
        createHatch(3, markupcolor);
        createHatch(4, markupcolor);
        createHatch(5, markupcolor);

    }


    function RxCore_SetMarkupLayer(layer) {
        if(docpagNotReady()){
            return;
        }

        if (documentcompare) {
            return;
        }

        markuplayer = layer;
        markupcolor = Layerlist[layer].Color;

        markuplinecolor = markupcolor;
        markuptextcolor = markupcolor;


    }
    function RxCore_ChangeMarkupLayer(layer){
        if(docpagNotReady()){
            return;
        }

        if (documentcompare) {
            return;
        }
        var templayer = markuplayer;
        var tempcolor = markupcolor;
        //var tempfillcolor = markupfillcolor;
        var templinecolor = markuplinecolor;
        var temptextcolor = markuptextcolor;

        markuplayer = layer;
        markupcolor = Layerlist[layer].Color;
        markuplinecolor = markupcolor;
        markuptextcolor = markupcolor;


        ChangeMarkupSelected(0);

        if (!bSetGlobal) {
            markuplayer = templayer;
            markupcolor = tempcolor;
            markuplinecolor = templinecolor;
            markuptextcolor = temptextcolor;

        }


    }


    function RxCore_MarkupLayerdialog() {
        if(docpagNotReady()){
            return;
        }

        if (documentcompare) {
            return;
        }

        if (RxCore_GUI_MarkupLayers != undefined) {
            RxCore_GUI_MarkupLayers.setMarkupLayers(Layerlist);
        }


    }

    function RxCore_GetMarkupLayers(){
        return Layerlist;
    }


    function RxCore_getAllSelectedMarkup(){

        if(docpagNotReady()){
            return;
        }
        if(readonlymode){
            return;
        }
        
        return DocObj.getSelectedMarkup();
        
    
    }

    function RxCore_getSelectedMarkup() {
        if(docpagNotReady()){
            return;
        }

        if(readonlymode){
            return;
        }

        return getSelectedmarkup(); 
     
    }

    function RxCore_getLastMarkup() {
        if(docpagNotReady()){
            return;
        }

        return getLastMarkup(); 
    }



    function RxCore_GetUsers(){
        return Userlist;
    }

    function RxCore_MarkupTools(selected) {
        if(docpagNotReady()){
            return;
        }
        if (documentcompare) {
            return;
        }

        if (!selected) {
            
            set_tool('markupedit', {p1 : false});
            
            return;
        }

        unselelectallmarkup();
        
        set_tool('markupedit', {p1 : false});
        

        if (RxCore_GUI_Users != undefined) {
            RxCore_GUI_Users.setUserlist(Userlist, DocObj.bActive);
        }

        if (RxCore_GUI_MarkupLayers != undefined) {
            RxCore_GUI_MarkupLayers.setMarkupLayers(Layerlist);
        }


    }


    function RxCore_MarkupUserdialog() {
        if(docpagNotReady()){
            return;
        }
        //unselelectallmarkup();
        

        if (RxCore_GUI_Users != undefined) {
            RxCore_GUI_Users.setUserlist(Userlist, DocObj.bActive);
        }


    }

    function RxCore_PenTabledialog(){

        if(docpagNotReady()){
            return;
        }


        if (DocObj.getCurrentpage().usevectorxml) {

            

            if (RxCore_GUI_penTable != undefined) {
                var pentable = DocObj.getCurrentpage().getPenTable();
                RxCore_GUI_penTable.setPentablelist(pentable.pens, DocObj.bActive);
            }

        }

    }

    function RxCore_FileInfodialog() {
        if(docpagNotReady()){
            return;
        }

        var ImageWidth = Math.floor(DocObj.getCurrentpage().MainImageWidth / DocObj.getCurrentpage().MainImageScaling);
        var imagewidthunits = getUnitlength(DocObj.getCurrentpage().MainImageWidth);
        var ImageHeight = Math.floor(DocObj.getCurrentpage().MainImageHeight / DocObj.getCurrentpage().MainImageScaling);
        var imageheightunits = getUnitlength(DocObj.getCurrentpage().MainImageHeight);
        var ImageDepth = undefined;
        var ImageDepthunits = undefined;

        if (DocObj.getCurrentpage().usevectorxml && DocObj.getCurrentpage().VectorPageObj) {
            ImageWidth = Math.floor(DocObj.getCurrentpage().VectorPageObj.width);
            imagewidthunits = getUnitlength(ImageWidth);
            ImageHeight = Math.floor(DocObj.getCurrentpage().VectorPageObj.height);
            imageheightunits = getUnitlength(ImageHeight);

        } else if (DocObj.getCurrentpage().usepdfjs) {
            ImageWidth = Math.floor(DocObj.getCurrentpage().pdfpagewidth);
            imagewidthunits = getUnitlength(ImageWidth);
            ImageHeight = Math.floor(DocObj.getCurrentpage().pdfpageheight);
            imageheightunits = getUnitlength(ImageHeight);

        } else if (DocObj.getCurrentpage().usevector3Dxml && DocObj.getCurrentpage().Vector3DPageObj) {
            
            if(DocObj.getCurrentpage().usevector3DGLTF ){

                ImageWidth = Math.floor(DocObj.getCurrentpage().Vector3DPageObj.curwidth);
                imagewidthunits = getUnitlength(ImageWidth);
                ImageHeight = Math.floor(DocObj.getCurrentpage().Vector3DPageObj.curheight);
                imageheightunits = getUnitlength(ImageHeight);
                ImageDepth = Math.floor(DocObj.getCurrentpage().Vector3DPageObj.curdepth);
                ImageDepthunits = getUnitlength(ImageDepth);
        
                //vector3Dref.curwidth = width;
                //vector3Dref.curdepth = depth;
                //vector3Dref.curheight = height;

            }else{
                ImageWidth = Math.floor(DocObj.getCurrentpage().Vector3DPageObj.w);
                imagewidthunits = getUnitlength(ImageWidth);
                ImageHeight = Math.floor(DocObj.getCurrentpage().Vector3DPageObj.h);
                imageheightunits = getUnitlength(ImageHeight);
    
            }
        } 

        var OffsetX = DocObj.getCurrentpage().OffsetX;
        var OffsetY = DocObj.getCurrentpage().OffsetY;
        var OriginalScale = DocObj.getCurrentpage().OriginalScale;
        var fileformat = DocObj.Format;
        var fileURL = DocObj.FileName;
        var fileSize = Math.floor(DocObj.FileSizeLow / 1024);
        var fileCompression = DocObj.getCurrentpage().Compression;
        var fileDPI = DocObj.getCurrentpage().DPI;
        var filePages = DocObj.NumPages;

        

        var FileInformation = {
            FileFormat:fileformat,
            FileName:fileURL,
            FileSize:fileSize,
            Compression:fileCompression,
            DPI:fileDPI,
            Pages:filePages,
            ImageWidth:ImageWidth,
            imagewidthunits:imagewidthunits,
            ImageHeight:ImageHeight,
            imageheightunits:imageheightunits,
            OffsetX:OffsetX,
            OffsetY:OffsetY,
            OriginalScale:OriginalScale,
            XRefs:DocObj.xrefs,
            Fonts:DocObj.fonts,
            Properties : DocObj.documentproperties
        };

        if (ImageDepth){
            FileInformation.ImageDepth = ImageDepth;
        }
        if (ImageDepthunits){
            FileInformation.ImageDepthunits = ImageDepthunits;
        }   

        if (DocObj.CadMeasurement != undefined){
            FileInformation.CADMeasurement = DocObj.CadMeasurement;
        }


        if (documentcompare) {
            FileInformation = {
                FileFormat:'Overlay Composite',
                FileName:CompareObj.pages[0].DocRef.FileName + "-" + CompareObj.pages[1].DocRef.FileName,
                FileSize:"-",
                Compression:"-",
                DPI:"-",
                Pages:"-",
                ImageWidth:"-",
                imagewidthunits:imagewidthunits,
                ImageHeight:"-",
                imageheightunits:imageheightunits,
                OffsetX:"-",
                OffsetY:"-",
                OriginalScale:"-"
            };
        }

        if (RxCore_GUI_FileInfo != undefined) {
            RxCore_GUI_FileInfo.setFileInfo(FileInformation);
        }


    }


    function RxCore_Measuredistance(selected){
        if(docpagNotReady()){
            return;
        }

        unselelectallmarkup();
        unselecteditmarkupall();
        if (selected) {
            set_tool('measuretool', {twoopoints : true});
            //tool = new tools['measuretool']();
        } else {
            
            set_tool('markupedit', {p1 : false});
            
        }


    }
    function RxCore_Measure(selected) {
        if(docpagNotReady()){
            return;
        }

        unselelectallmarkup();
        unselecteditmarkupall();
        if (selected) {
            set_tool('measuretool', {twoopoints : false});
            //tool = new tools['measuretool']();
        } else {
            
            set_tool('markupedit', {p1 : false});
            
        }
    }

    function RxCore_CloseAll() {
        if(docpagNotReady()){
            return;
        }

        while (OpenFiles.length > 0) {
            //RxCore_Closedocument();
            internalClosedoc();

        }
    }


    function CloseCompare() {
        documentcompare = false;

        OpenfileNames = [];

        CompareObj.Close();
        for (var i = 0; i < OpenFiles.length; i++) {
            if (OpenFiles[i].Type == 'Compare') {
                OpenFiles.splice(i, 1);
            }
        }


        CompareObj = {};

        /*DocObj = OpenFiles[OpenFiles.length - 1];
         DocObj.SetActive();*/

        /*if (DocObj.getCurrentpage().usevectorxml){
         DocObj.getCurrentpage().draw_vector(true);
         }
         drawmarkupAll(cntximg);*/

        //DocObj.SetActive();
    }

    function RxCore_Closedocument(index) {


        return internalClosedocOrg(index);
        
    }

    function RxCore_ClosedocumentEx(index){
        
        const closepromise = new Promise(function(resolve,reject){

            RxCore_GUI_CloseDocInternal.connect(function(index, bclosed){
                

                if(bclosed){
                    resolve(index);
                }else{
                    //reject(index);
                }
                

            });

            internalClosedoc(index);


        });

        return closepromise;

    }    

    function internalClosedocOrg(index){
        var documentObject = OpenFiles[index] || DocObj;
        var fileindex = index || undefined;
        var closeindex = fileindex;

        


        if (!documentopen) {

            /*if(RxCore_GUI_CloseDocInternal != undefined){
                RxCore_GUI_CloseDocInternal.closeComplete('no file open', false);
            }*/

            return false;
        }


        if (documentObject){
            fileindex = documentObject.fileindex;
            closeindex = fileindex;
            
        }
        

        /*if(CompareObj != undefined && documentcompare){
         CloseCompare();
         return;
         }*/

        if(DocObj){

            /*if(documentObject.getdelayedClose()){
                documentObject.setdelayedClose(false);
            }*/

            if (documentObject.bMarkupchanged && !readonlymode) {
                        
                    if (bMarkupSavecheck){
                        
                        if(bmarkupsaveconfirm){
                            if (confirm('Save Markup?')) {
                                //sendMarkup(xmlurlmarkupsave, signature, DisplayName);
    
                                
                                //documentObject.setdelayedClose(true);
                                
                            } else {
                                //return;
                                //just close
                                //documentObject.setdelayedClose(false);
                            }
    
                        }else{
                            sendMarkupEx(xmlurlmarkupsave, signature, DisplayName);
                        }

                        
        
                    }
    
                
            }
        }


        /*if(documentObject.getdelayedClose()){

            if(RxCore_GUI_CloseDocInternal != undefined){
                RxCore_GUI_CloseDocInternal.closeComplete('delayed', false);
            }
    
            return false;
        }*/

        if (documentObject === DocObj) {
            context.clearRect(0, 0, canvas.width, canvas.height);
            cntximg.clearRect(0, 0, canvas.width, canvas.height);
            contexto.clearRect(0, 0, canvas.width, canvas.height);

        }
        

        //CompareObj.Close();
        /*for (var i=0;i< OpenFiles.length;i++){
         if(OpenFiles[i].Type == 'Compare'){
         OpenFiles.splice(i,1);
         }
         }*/

        if (OpenFiles[OpenFiles.length - 1].Type == 'Compare') {
            CloseCompare();

        } else {
            documentObject.Close();
            if (documentObject === DocObj) {
                documentObject = DocObj = null;
            }
            OpenFiles.splice(fileindex, 1);

            //var dindex = DocObj.fileindex;
            //DocObj = {};
            //DocObj = null;
            //OpenFiles.splice(dindex, 1);
            //OpenFiles.pop();
        }


        if(DocObj){
            DocObj.bMarkupchanged = false;
        }

        bUsemarkupbyref = false;
        bUsemarkupbyrefEx = false;

        /*if (Viewer3D != null){
         Viewer3D = null;
         }*/
        /*contexto = canvaso.getContext('2d');
         if (!contexto) {
         alert('Error: failed to getContext!');
         return;
         }*/


        //var szstyle = "width: 200px; height: " + canvasoheight + "px; position:relative; top:-100px; left:15px"
        //setContentThumb("<p>No pages open</p>");
        //ThumbnailpagesContainer.innerHTML = "<p>No pages open</p>";
        //ThumbnailpagesContainer.Close();
        //LayersContainer.setEmpty();
        //BlocksContainer.setEmpty();
        //LayersContainer.innerHTML = "<table><tr><td>Name</td><td>Color</td><td>State</td></tr></table>";
        //BlocksContainer.innerHTML = "<table><tr><td>Name</td><td>State</td></tr></table>";


        if (OpenFiles.length < 1) {
            documentopen = false;
            //DocObj.markuploaded = false;
            if(!bwelcomedisabled){
                imagewidth = splashimage.width;
                imageheight = splashimage.height;
    
                imagewidth = splashimage.naturalWidth;
                imageheight = splashimage.naturalHeight;
    
                var yscale = canvasoheight / imageheight;
                var xscale = canvasowidth / imagewidth;
                var dscale = Math.min(xscale, yscale);
    
                var dx = (canvasowidth - (imagewidth * dscale)) / 2;
                var dy = (canvasoheight - (imageheight * dscale)) / 2;
    
            }
            
            numUsers = 0;

            clearUsers();
            addCurrentUser();

            //Userlist = [];
            //Userlist[0] = new Users(signature, DisplayName, markuplayer, markupcolor);

            

            numUsers++;

            if (RxCore_GUI_Users != undefined) {
                RxCore_GUI_Users.setUserlist(Userlist, true);
            }

            if(!bwelcomedisabled){
                draw_image(splashimage, dx, dy, dscale);
            }
            
            
            set_tool('markupedit', {p1 : false});
            
            var stateobj = {
                iscompare:false,
                numOpenFiles:OpenFiles.length,
                isPDF:false,
                is3D:false,
                markupOn3D : false,
                is2D:false,
                numpages:0,
                currentpage:0,
                activefile : false,
                source : 'internalClosedocOrg'
            };
            if (RxCore_GUI_State != undefined) {
                RxCore_GUI_State.setGUIState(stateobj);
            }


        } else {
            if (OpenFiles[OpenFiles.length - 1].Type == 'Compare') {
                CompareObj = OpenFiles[OpenFiles.length - 1];
                DeactivateAll();
                CompareObj.bSopenSilent = false;
                CompareObj.SetActive();


            } else {
                if (!DocObj) {
                    DocObj = OpenFiles[OpenFiles.length - 1];

                }
                DeactivateAll();
                DocObj.bSopenSilent = false;
                DocObj.SetActive();

            }

        }

        // Rebuild fileindex
        for (var i = 0; i < OpenFiles.length; i++) {
            //OpenFiles[i].fileindex = i;
            OpenFiles[i].setfileindex(i);
        }


        /*const closepromise = new Promise(function(resolve,reject){
        })*/ 
        //const closedocument = new Promise 

        /*if(RxCore_GUI_CloseDocInternal != undefined){
            RxCore_GUI_CloseDocInternal.closeComplete(closeindex, true);
        }*/

        return true;
    }

    function usedbmarkup(onoff){
        busedbmarkup = onoff;
    }

    


    function usesharepointmarkup(onoff){
        //bUsemarkupbyref = false;
        busesharepointmarkup = onoff;
    }

    function internalClosedoc(index) {
        
        
        
        var documentObject = OpenFiles[index] || DocObj;
        var fileindex = index || undefined;
        var closeindex = fileindex;

        


        if (!documentopen) {

            if(RxCore_GUI_CloseDocInternal != undefined){
                RxCore_GUI_CloseDocInternal.closeComplete('no file open', false);
            }

            return false;
        }


        if (documentObject){
            fileindex = documentObject.fileindex;
            closeindex = fileindex;
            
        }
        

        /*if(CompareObj != undefined && documentcompare){
         CloseCompare();
         return;
         }*/

        if(DocObj){

            if(documentObject.getdelayedClose()){
                documentObject.setdelayedClose(false);
            }

            if (documentObject.bMarkupchanged && !readonlymode) {
                        
                    if (bMarkupSavecheck){


                        if(bmarkupsaveconfirm){
                            if (confirm('Save Markup?')) {
                                //sendMarkup(xmlurlmarkupsave, signature, DisplayName);
    
                                if(DocObj.bLargePDF){
                                    documentObject.setdelayedClose(true);
                                    sendMarkupEx(xmlurlmarkupsave, signature, DisplayName);
        
                                }else{
                                    
                                    sendMarkup(xmlurlmarkupsave, signature, DisplayName);
                                }
    
                        }else{
                            if(DocObj.bLargePDF){
                                documentObject.setdelayedClose(true);
                                sendMarkupEx(xmlurlmarkupsave, signature, DisplayName);
    
                            }else{
                                
                                sendMarkup(xmlurlmarkupsave, signature, DisplayName);
                            }

                        }


                            
                        } else {
                            //return;
                            //just close
                            documentObject.setdelayedClose(false);
                        }
        
                    }
    
                
            }
        }


        if(documentObject.getdelayedClose()){

            if(RxCore_GUI_CloseDocInternal != undefined){
                RxCore_GUI_CloseDocInternal.closeComplete('delayed', false);
            }
    
            return false;
        }

        if (documentObject === DocObj) {
            context.clearRect(0, 0, canvas.width, canvas.height);
            cntximg.clearRect(0, 0, canvas.width, canvas.height);
            contexto.clearRect(0, 0, canvas.width, canvas.height);

        }
        

        //CompareObj.Close();
        /*for (var i=0;i< OpenFiles.length;i++){
         if(OpenFiles[i].Type == 'Compare'){
         OpenFiles.splice(i,1);
         }
         }*/

        if (OpenFiles[OpenFiles.length - 1].Type == 'Compare') {
            CloseCompare();

        } else {
            documentObject.Close();
            if (documentObject === DocObj) {
                documentObject = DocObj = null;
            }

            var OpenfileNamestemp = [];
            var delindex = -1;

            for (var k = 0; k < OpenfileNames.length; k++) {


                if (fileindex == OpenfileNames[k].index) {
                    delindex = k;
                    
                    //do not copy                    
                }


            }

            if(delindex >= 0){
                OpenfileNames.splice(delindex, 1);
            }

            OpenFiles.splice(fileindex, 1);

            //var dindex = DocObj.fileindex;
            //DocObj = {};
            //DocObj = null;
            //OpenFiles.splice(dindex, 1);
            //OpenFiles.pop();
        }


        if(DocObj){
            DocObj.bMarkupchanged = false;
        }

        bUsemarkupbyref = false;
        bUsemarkupbyrefEx = false;

        /*if (Viewer3D != null){
         Viewer3D = null;
         }*/
        /*contexto = canvaso.getContext('2d');
         if (!contexto) {
         alert('Error: failed to getContext!');
         return;
         }*/


        //var szstyle = "width: 200px; height: " + canvasoheight + "px; position:relative; top:-100px; left:15px"
        //setContentThumb("<p>No pages open</p>");
        //ThumbnailpagesContainer.innerHTML = "<p>No pages open</p>";
        //ThumbnailpagesContainer.Close();
        //LayersContainer.setEmpty();
        //BlocksContainer.setEmpty();
        //LayersContainer.innerHTML = "<table><tr><td>Name</td><td>Color</td><td>State</td></tr></table>";
        //BlocksContainer.innerHTML = "<table><tr><td>Name</td><td>State</td></tr></table>";


        if (OpenFiles.length < 1) {
            documentopen = false;
            //DocObj.markuploaded = false;
            if(!bwelcomedisabled){
                imagewidth = splashimage.width;
                imageheight = splashimage.height;
    
                imagewidth = splashimage.naturalWidth;
                imageheight = splashimage.naturalHeight;
    
                var yscale = canvasoheight / imageheight;
                var xscale = canvasowidth / imagewidth;
                var dscale = Math.min(xscale, yscale);
    
                var dx = (canvasowidth - (imagewidth * dscale)) / 2;
                var dy = (canvasoheight - (imageheight * dscale)) / 2;
    
            }
            
            numUsers = 0;

            clearUsers();
            addCurrentUser();

            //Userlist = [];
            //Userlist[0] = new Users(signature, DisplayName, markuplayer, markupcolor);
            numUsers++;

            if (RxCore_GUI_Users != undefined) {
                RxCore_GUI_Users.setUserlist(Userlist, true);
            }

            if(!bwelcomedisabled){
                draw_image(splashimage, dx, dy, dscale);
            }
            
            
            set_tool('markupedit', {p1 : false});
            
            var stateobj = {
                iscompare:false,
                numOpenFiles:OpenFiles.length,
                isPDF:false,
                is3D:false,
                markupOn3D : false,
                is2D:false,
                numpages:0,
                currentpage:0,
                activefile : false,
                source : 'internalClosedoc'
            };
            if (RxCore_GUI_State != undefined) {
                RxCore_GUI_State.setGUIState(stateobj);
            }


        } else {
            if (OpenFiles[OpenFiles.length - 1].Type == 'Compare') {
                CompareObj = OpenFiles[OpenFiles.length - 1];
                DeactivateAll();
                CompareObj.bSopenSilent = false;
                CompareObj.SetActive();


            } else {
                if (!DocObj) {
                    DocObj = OpenFiles[OpenFiles.length - 1];

                }
                DeactivateAll();
                DocObj.bSopenSilent = false;
                DocObj.SetActive();

            }

        }

        // Rebuild fileindex
        for (var i = 0; i < OpenFiles.length; i++) {
            //OpenFiles[i].fileindex = i;
            OpenFiles[i].setfileindex(i);
        }


        /*const closepromise = new Promise(function(resolve,reject){
        })*/ 
        //const closedocument = new Promise 

        if(RxCore_GUI_CloseDocInternal != undefined){
            RxCore_GUI_CloseDocInternal.closeComplete(closeindex, true);
        }

        return true;
    }    

    function RxCore_Printdocument() {
        if(docpagNotReady()){
            return;
        }

        var pheight = parseInt(DocObj.getCurrentpage().height);
        var pwidth = parseInt(DocObj.getCurrentpage().width);

        if (pheight > pwidth) {
            PaperWidth = 210;
            PaperHeight = 297;
        } else {
            PaperWidth = 297;
            PaperHeight = 210;
        }

        MarkupPrintSave();

        if(!documentcompare){
            if(DocObj.checkprintready()){
                printCanvas(PrintPageURL);
            }else{
               //load all pages.
                DocObj.loadAllPages();
            }
        }else{
            //CompareObj.print(PrintPageURL);
        }

        /*if(!documentcompare){
            printCanvas(PrintPageURL);
        }else{
            CompareObj.print(PrintPageURL);
        }*/


    }

    function RxCore_getPDFDocobj(){

        if(docpagNotReady()){
            return null;
        }


        return DocObj.getPDFDocument();

    }

    function RxCore_GetdocInfo() {

        if(docpagNotReady()){
            return {
                iscompare:false,
                isPDF:false,
                is3D:false,
                is2D:false,
                numpages:0,
                currentpage:0
            }

        }else{
            return {
                iscompare:documentcompare,
                isPDF:DocObj.getCurrentpage().usepdfjs,
                is3D:DocObj.getCurrentpage().usevector3Dxml,
                is2D:DocObj.getCurrentpage().usevectorxml,
                numpages:DocObj.pages.length,
                currentpage:DocObj.getcurPage(),
                type : DocObj.Type
            }

        }

    }

    function RxCore_SetViewMode(OnOff){
        if(docpagNotReady()){
            return;
        }

        if(documentcompare){
            return;
        }

        DocObj.getCurrentpage().setViewMode(OnOff);
    }

    function RxCore_setPenTable(OnOff){
        if (!documentopen) {
            return;
        }

        if(documentcompare){
            return;
        }

        DocObj.getCurrentpage().setPenTable(OnOff);
    }

    function RxCore_setPenTableScaled(OnOff){
        if (!documentopen) {
            return;
        }

        if(documentcompare){
            return;
        }

        DocObj.getCurrentpage().setpentableScaled(OnOff);
    }

    function RxCore_setPenwidth(penobj){
        if (!documentopen) {
            return;
        }

        if(documentcompare){
            return;
        }

        DocObj.getCurrentpage().setPenwidth(penobj);
    }


    function RxCore_setpentablePen(penobj, breset){


        if (!documentopen) {
            return;
        }

        if(documentcompare){
            return;
        }

        if(breset){

            DocObj.getCurrentpage().resetPenTablepen(penobj);


        }else{

            DocObj.getCurrentpage().setPenTablepen(penobj);
            
        }

        

        /*this.setpencolor = function(penindex, color){

            if (thispage.usevectorxml) {
                thispage.VectorPageObj.setpencolor(penindex, color);
                thispage.draw_vector(false);
                thispage.vectorischanged = true;
            }


        };*/

        /*this.resetpencolor = function(penindex){

            if (thispage.usevectorxml) {
                thispage.VectorPageObj.resetpencolor(penindex);
                thispage.draw_vector(false);
                thispage.vectorischanged = true;
            }

        };*/

        /*this.setpenwidth = function(penindex, uwidth){
            if (thispage.usevectorxml) {
                thispage.VectorPageObj.setpenwidth(penindex, uwidth);
                thispage.draw_vector(false);
                thispage.vectorischanged = true;
            }

        };*/
        
        /*this.resetpenwidth = function(penindex){

            if (thispage.usevectorxml) {
                thispage.VectorPageObj.resetpenwidth(penindex);
                thispage.draw_vector(false);
                thispage.vectorischanged = true;
            }
            
        };*/
    }    

    function RxCore_setPDFinitalZoomAll(OnOff){
        bPDFZoomAllinit = OnOff;
    }

    function RxCore_setPDFAnimateRender(OnOff){
        bAnimatePDFrender = OnOff;
    }

    function RxCore_SetScrollZoomKey(OnOff,keys){
        bUseScrollKey = OnOff;
        nScrollKeyNum = keys;
        
    }

    function RxCore_SinglePageScrollPan(onOff){
        bScrollSingleDisable = onOff;
    }


    function RxCore_SetSingleDocument(OnOff) {
        bSingleDocmode = OnOff;

    }

    function PrintCommon(PrintPgURL, PaperSize){
        


        MarkupPrintSave();

        PaperWidth = PaperSize.width;
        PaperHeight = PaperSize.height;
        
        PrintRect = PaperSize.printrect;

        if(!documentcompare){
            if(DocObj.checkprintready()){
                printCanvas(PrintPgURL);
            }else{
                //load all pages.
                DocObj.loadAllPages();
            }
        }else{

            //CompareObj.print(PrintPgURL,PaperSize);
        }
    }

    function RxCore_PrintOptionsSize(PrintPgURL, PaperSize){
        if(docpagNotReady()){
            return;
        }

        //auto

        if(PaperSize.mode == 0){
            if (DocObj.getCurrentpage().height > DocObj.getCurrentpage().width) { // drawing portrait
                if(PaperSize.width > PaperSize.height){
                    var tempwidth = PaperSize.width;
                    PaperSize.width = PaperSize.height;
                    PaperSize.height = tempwidth;
                }
                
            } else { // drawing landscape
                if(PaperSize.height > PaperSize.width){
                    tempwidth = PaperSize.width;
                    PaperSize.width = PaperSize.height;
                    PaperSize.height = tempwidth;
                }
            }
    
        }else if(PaperSize.mode == 1) { //force portrait
            if(PaperSize.width > PaperSize.height){
                var tempwidth = PaperSize.width;
                PaperSize.width = PaperSize.height;
                PaperSize.height = tempwidth;
            }

        }else if(PaperSize.mode == 2) { //force landscape
            if(PaperSize.height > PaperSize.width){
                tempwidth = PaperSize.width;
                PaperSize.width = PaperSize.height;
                PaperSize.height = tempwidth;
            }

        }

        PrintCommon(PrintPgURL, PaperSize); 
        
        
        /*if(DocObj.foxitdoc != null){
            DocObj.foxitdoc.printDocument();
        }else{
            
        }*/

        //PrintCommon(PrintPgURL, PaperSize);
    }

    function RXCore_PrintdocOptions(PrintPgURL) {

        if(docpagNotReady()){
            return;
        }


        if (DocObj.getCurrentpage().height > DocObj.getCurrentpage().width) {
            var PWidth = 210;
            var PHeight = 297;
        } else {
            PWidth = 297;
            PHeight = 210;
        }
        
        PrintCommon(PrintPgURL, {width : PWidth, height : PHeight});
        



        /*if(!documentcompare){
            printCanvas(PrintPgURL);
        }else{
            CompareObj.print(PrintPgURL,{width : PaperWidth,height : PaperHeight});
        }*/


    }

    function RxCore_MoveToBack() {
        if(docpagNotReady()){
            return;
        }
        if (readonlymode || documentcompare) {
            return;
        }

        moveToBack();
    }

    function RxCore_MoveToFront() {
        if(docpagNotReady()){
            return;
        }
        if (readonlymode || documentcompare) {
            return;
        }
        moveToFront();
    }

    function RxCore_toggleCycleSelect() {
        if(docpagNotReady()){
            return;
        }
        if (readonlymode || documentcompare) {
            return;
        }
        toggleCycleSelect();
    }


    function deleteMarkupbyGUID(GUID){
        if(docpagNotReady()){
            return;
        }
        if (readonlymode || documentcompare) {
            return;
        }

        deleteUniqueMarkup(GUID);

        drawmarkupAll(cntximg);
        DrawMarkupSelected(context);


    }

    function RxCore_change3DMarkupSize(size){
        if(docpagNotReady()){
            return;
        }
        if (readonlymode || documentcompare) {
            return;
        }

        if (DocObj.getCurrentpage().usevector3Dxml) {
            
            DocObj.getCurrentpage().change3DMarkupSize(size);
            //return;
        }

    }
    function RxCore_change3DMarkup(newstatus){
        if(docpagNotReady()){
            return;
        }
        if (readonlymode || documentcompare) {
            return;
        }

        if (DocObj.getCurrentpage().usevector3Dxml) {
            
            DocObj.getCurrentpage().change3DMarkup(newstatus);
            //return;
        }

    }    
    function RxCore_displayFloorplan(floornum, onOff, bAll){
        if(docpagNotReady()){
            return;
        }
        if (readonlymode || documentcompare) {
            return;
        }
        if (DocObj.getCurrentpage().usevector3Dxml) {
            
            DocObj.getCurrentpage().displayfloorplan(floornum, onOff, bAll);
            //return;
        }


    }

    function RxCore_removeFloorplan(floornum){
        if(docpagNotReady()){
            return;
        }
        if (readonlymode || documentcompare) {
            return;
        }

        if (DocObj.getCurrentpage().usevector3Dxml) {
            
            DocObj.getCurrentpage().removefloorplan(floornum);
            //return;
        }

    }

    function RxCore_DeleteMarkup() {
        if(docpagNotReady()){
            return;
        }
        if (readonlymode || documentcompare) {
            return;
        }

        if (DocObj.getCurrentpage().usevector3Dxml) {
            
            DocObj.getCurrentpage().delete3DMarkup();
            //return;
        }else{
            deletemarkup();
        }


    }

    function RxCore_getDocRect(selected) {

        if(docpagNotReady()){
            return;
        }
        if (DocObj.getCurrentpage().usevector3Dxml) {
            return;
        }

        unselelectallmarkup();
        unselecteditmarkupall();

        if (selected) {

            //set_tool('zoomwindow', {p1 : true});
            set_tool('zoomwindow', {p1 : true, p2 : false});
            
            //tool = new tools['zoomwindow']();
        } else {
            
            set_tool('markupedit', {p1 : false});
            
        }


    }

    async function imageFromClipboard(){



        try {
            const permission = await navigator.permissions.query({
              name: "clipboard-read",
            });
            if (permission.state === "denied") {
              throw new Error("Not allowed to read clipboard.");
            }
            const clipboardContents = await navigator.clipboard.read();
            for (const item of clipboardContents) {
              if (!item.types.includes("image/png")) {
                throw new Error("Clipboard contains non-image data.");
              }
              const blob = await item.getType("image/png");
              //destinationImage.src = URL.createObjectURL(blob);

              set_tool('clipboardimage', {p1 : 1});

              if(tool.name === 'clipboardimage'){
                  tool.setimage(blob);
              }



            }
          } catch (error) {
            console.error(error.message);
          }

        /*try {
            const clipboardItems = await navigator.clipboard.read();
            for (const clipboardItem of clipboardItems) {
              const imageTypes = clipboardItem.types.find(type => type.startsWith('image/'));
              for (const imageType of imageTypes) {
                const blob = await clipboardItem.getType(imageType);
                
                // Do something with the image blob.


              }
            }
          } catch (err) {
            console.error(err.name, err.message);
          }*/


    }    

    function RxCore_imageFromClipboard(){
        
        imageFromClipboard();
    }    

    function copyToClipboard(canvas) {
        // navigator clipboard api needs a secure context (https)

        /*if (location.protocol !== 'https:') {
            location.replace(`https:${location.href.substring(location.protocol.length)}`);
        }*/

        if (navigator.clipboard && window.isSecureContext) {
            // navigator clipboard api method'

            canvas.toBlob(function(blob) { 

                //const item = new ClipboardItem({ "image/png": blob });

                RxCore_defaultAll();
                //RxCore_default();
                //RxCore_restore3DInput();
                
                return navigator.clipboard.write([new ClipboardItem({ "image/png": blob })]); 
                
            });
            //return navigator.clipboard.writeText(textToCopy);

        } else {
            // text area method

            console.log("no secure context");
            /* let img = document.createElement('img'); 
            img.src = canvas.toDataURL();

            let div = document.createElement('div');
            div.contentEditable = true;
            div.appendChild( img );
            document.body.appendChild( div );

            div.style.position = "absolute";
                div.style.left = "-999999px";
                div.style.top = "-999999px";
            div.focus();
            window.getSelection().selectAllChildren( div );
            //document.execCommand('Copy');  // technically deprecated
            //document.body.removeChild( div );
      
            return new Promise((res, rej) => {
                // here the magic happens
                document.execCommand('Copy') ? res() : rej();

                document.body.removeChild( div );
                //textArea.remove();
            }); */
        }
    }

    function flipImageData(imagedata){
        var canvsource = document.createElement('canvas');
        var canvtarget = document.createElement('canvas');
        var ctxsource = canvsource.getContext('2d');
        var ctxtarget = canvtarget.getContext('2d');

        canvsource.width = imagedata.width;
        canvsource.height = imagedata.height;

        canvtarget.width = imagedata.width;
        canvtarget.height = imagedata.height;

        //clipboardctx.scale(-1, 1);
        ctxsource.putImageData(imagedata, 0, 0);
        
        ctxtarget.save();
        ctxtarget.translate(0, canvsource.height);
        ctxtarget.scale(1, -1);


        /*var tx = canvsource.width * 0.5; 
        var ty = canvsource.height * 0.5;
        
        ctxtarget.save();
        ctxtarget.translate(tx, ty);
        ctxtarget.rotate(180 * (Math.PI / 180));
        ctxtarget.translate(-tx, -ty);*/
        
        ctxtarget.drawImage(canvsource, 0, 0);

        ctxtarget.restore();

        
        return ctxtarget.getImageData(0, 0, imagedata.width, imagedata.height);

    }


    function RxCore_getClipRect(selected) {

        if(docpagNotReady()){
            return;
        }
        /*if (DocObj.getCurrentpage().usevector3Dxml) {
            return;
        }*/

        unselelectallmarkup();
        unselecteditmarkupall();

        if (selected) {

            //set_tool('zoomwindow', {p2 : true});
            set_tool('zoomwindow', {p1 : false, p2 : true});
            
            //tool = new tools['zoomwindow']();
        } else {
            
            set_tool('markupedit', {p1 : false});
            
        }


    }


    function RxCore_ZoomWindow(selected) {
        if(docpagNotReady()){
            return;
        }
        if (DocObj.getCurrentpage().usevector3Dxml) {
            return;
        }

        unselelectallmarkup();
        unselecteditmarkupall();

        if (selected) {
            
            set_tool('zoomwindow', {p1 : false, p2 : false});

            //tool = new tools['zoomwindow']();
        } else {
            
            set_tool('markupedit', {p1 : false});
            
        }

    }

    /*function RxCore_Zoom(x,y){
        if (!documentopen) {
            return;
        }
        if (DocObj.getCurrentpage().usevector3Dxml) {
            return;
        }

        DocObj.getCurrentpage().pan_page(x, y,true);
    }*/

    function RxCore_ZoomPageUpdate(zoomparams, type){
        if(docpagNotReady()){
            return;
        }
        if (DocObj.getCurrentpage().usevector3Dxml) {
            return;
        }

        DocObj.getCurrentpage().ZoomPageUpdate(zoomparams, type);
    }


    function RxCore_PagePos(x,y){
        if(docpagNotReady()){
            return;
        }
        if (DocObj.getCurrentpage().usevector3Dxml) {
            return;
        }

        DocObj.getCurrentpage().pan_page_pos(x, y);
    }

    function RxCore_PanPage(x,y){
        if(docpagNotReady()){
            return;
        }
        if (DocObj.getCurrentpage().usevector3Dxml) {
            return;
        }

        DocObj.getCurrentpage().pan_page(x, y,true);
        //DocObj.getCurrentpage().pan_page_pos(x, y,true);
        
    }


    function RxCore_PanWindow(selected) {
        if(docpagNotReady()){
            return;
        }
        if (DocObj.getCurrentpage().usevector3Dxml) {
            return;
        }

        unselelectallmarkup();
        unselecteditmarkupall();
        if (selected) {
            set_tool('pan', {});
            //tool = new tools['pan']();
        } else {
            
            set_tool('markupedit', {p1 : false});
            
        }

    }

    function RxCore_MagnifyGlass(selected) {
        if(docpagNotReady()){
            return;
        }
        if (DocObj.getCurrentpage().usevector3Dxml) {
            return;
        }

        unselelectallmarkup();
        unselecteditmarkupall();
        if (selected) {
            magcanvas.addEventListener('mousemove', ev_canvas, false);
            magcanvas.addEventListener('mousedown', ev_canvas, false);
            magcanvas.addEventListener('mouseup', ev_canvas, false);
            magcanvas.addEventListener('mouseout', ev_canvas, false);
            magcanvas.addEventListener('wheel', ev_canvas, false);
            set_tool('magnify', {});
            //tool = new tools['magnify']();
        } else {
            magcanvas.style.visibility = 'hidden';
            magcanvas.removeEventListener('mousemove', ev_canvas, false);
            magcanvas.removeEventListener('mousedown', ev_canvas, false);
            magcanvas.removeEventListener('mouseup', ev_canvas, false);
            magcanvas.removeEventListener('mouseout', ev_canvas, false);
            magcanvas.removeEventListener('wheel', ev_canvas, false);
            set_tool('markupedit', {p1 : false});
            
        }
    }

    function RxCore_ZoomFit() {
        if(docpagNotReady()){
            return;
        }
        if (DocObj.getCurrentpage().usevector3Dxml) {
            return;
        }

        if (documentcompare){

            CompareObj.zoomall();

        }else{
            DocObj.getCurrentpage().zoomall();
        }

        
        //unselelectallmarkup();
        //unselecteditmarkupall();
        
        

        //console.log('zoom all');
        

    }

    function RxCore_ZoomHeight() {
        if(docpagNotReady()){
            return;
        }
        if (DocObj.getCurrentpage().usevector3Dxml) {
            return;
        }

        if (documentcompare){

            CompareObj.zoomheight();

        }else{
            DocObj.getCurrentpage().zoomheight();
        }


        //unselelectallmarkup();
        //unselecteditmarkupall();
        
        

    }

    function RxCore_ZoomWidth() {
        if(docpagNotReady()){
            return;
        }
        if (DocObj.getCurrentpage().usevector3Dxml) {
            return;
        }

        if (documentcompare){

            CompareObj.zoomwidth();



        }else{
            DocObj.getCurrentpage().zoomwidth();
        }


        //unselelectallmarkup();
        //unselecteditmarkupall();
        
        
    }

    function RxCore_ZoomIn() {
        if(docpagNotReady()){
            return;
        }

        if (DocObj.getCurrentpage().usevector3Dxml) {
            return;
        }

        if (documentcompare){

            CompareObj.ZoomIn(1.1, false, false);

            /*if(CompareObj.overlaypage){
                CompareObj.overlaypage.ZoomIn(1.1, false, false);

            }

            if(CompareObj.backgroundgpage){
                CompareObj.backgroundgpage.ZoomIn(1.1, false, false);
            }*/

            //return;
            
        }else{
            DocObj.getCurrentpage().ZoomIn(1.1, false, false);
        }

        
        //unselelectallmarkup();
        //unselecteditmarkupall();
        
        

    }

    function belowlimitExtentEx(zoomscalefactor, mousepoint){
        var belowlimit = true;
                    

        //var pixelRatio = getPixelRatio();

        var pagedim = DocObj.getCurrentpage().getpagedim();


        if (DocObj.getCurrentpage().usepdfjs){

            var wlimit = canvasowidthsize - 60;
            var hlimit = canvasoheightsize - 60;

            //var wlimit = canvasowidth - 60;
            //var hlimit = canvasoheight - 60;

    
        }else{
            wlimit = canvasowidth - 60;
            hlimit = canvasoheight - 60;

        }



        if((pagedim.w / zoomscalefactor) < wlimit && (pagedim.h / zoomscalefactor) < hlimit){
            belowlimit = false;
            //scalechanged = false;
            //DocObj.getCurrentpage().zoomall();
        }

        var pagerect = DocObj.getCurrentpage().ZoomOutTest(zoomscalefactor, false, mousepoint);
        

        if (DocObj.getCurrentpage().usepdfjs){
            var heightlimit = canvasoheightsize - (nPanlimit*2);
            var widthlimit = canvasowidthsize - (nPanlimit*2);
            var widthheightratio = (canvasowidthsize / canvasoheightsize);

            //var heightlimit = canvasoheight - (nPanlimit*2);
            //var widthlimit = canvasowidth - (nPanlimit*2);
            //var widthheightratio = (canvasowidth / canvasoheight);

    
        }else{
            heightlimit = canvasoheight - (nPanlimit*2);
            widthlimit = canvasowidth - (nPanlimit*2);
            widthheightratio = (canvasowidth / canvasoheight);

        }

        //var toplimit = 15;
        var bottomlimit = ((pagerect.y + pagerect.h) <= heightlimit);
        var toplimit = (pagerect.y >= nPanlimit);


        //pixelRatio

        

        // need to return these to reset the top and bottom positions.scroll|| bottomlimit || toplimit
        if ((pagerect.w / pagerect.h) < widthheightratio){
            if(pagerect.h <= heightlimit){
                belowlimit = false;
            }

        }else{
            if(pagerect.w <= widthlimit){
                belowlimit = false;
            }

        }


        if(!bLimitZoomOut){
            belowlimit = true;
        }

        return belowlimit;
    }    

    function belowlimitExtent(zoomscalefactor){
        var belowlimit = true;

        var pagedim = DocObj.getCurrentpage().getpagedim();

        var wlimit = canvasowidth - 30;
        var hlimit = canvasoheight - 30;


        if (DocObj.getCurrentpage().usepdfjs) {
            wlimit = canvasowidthsize - 30;
            hlimit = canvasoheightsize - 30;
    
        }



        //var widthheightratio = (canvasowidthsize / canvasoheightsize);


        if((pagedim.w / zoomscalefactor) < wlimit && (pagedim.h / zoomscalefactor) < hlimit){
            belowlimit = false;
            //scalechanged = false;
            //DocObj.getCurrentpage().zoomall();
        }


        /*if (DocObj.getCurrentpage().usepdfjs) {

        }else if (DocObj.getCurrentpage().usevectorxml) {

        
            if (DocObj.getCurrentpage().initialscale > DocObj.getCurrentpage().dscalevector) {
                //scalechanged = false;
                //DocObj.getCurrentpage().zoomall();
                belowlimit = false;

            }


        } else {
            if (DocObj.getCurrentpage().initialscale > (DocObj.getCurrentpage().dscale / DocObj.getCurrentpage().bitmapratio)) {
                //scalechanged = false;
                //DocObj.getCurrentpage().zoomall();
                belowlimit = false;
            }

        }*/

        if(!bLimitZoomOut){
            belowlimit = true;
        }

        
        return belowlimit;
    }

    function RxCore_ZoomOut() {
        if(docpagNotReady()){
            return;
        }
        if (DocObj.getCurrentpage().usevector3Dxml) {
            return;
        }

        //unselelectallmarkup();
        //unselecteditmarkupall();

        if (documentcompare){
            
            CompareObj.ZoomOut(1.1, false, false);
            
        }else{
         
            if (belowlimitExtent(1.1)){
                DocObj.getCurrentpage().ZoomOut(1.1, false, false);
            }
        }
    
    }

    function RxCore_selectRxSpaceEdit(blockId){

        if(docpagNotReady()){
            return;
        }

        if (documentcompare) {
            return;
        }


        if (DocObj.getCurrentpage().usevector3Dxml) {
            return;
        }

        var spacemarkup = findSpaceMarkup(blockId);
        

        if(spacemarkup == undefined){
            return;
        }

        var bselected = spacemarkup.selectedit;

        if(!bMultiselect && !DocObj.getMarkupMultiselected()){
            unselelectallmarkup();
            unselecteditmarkupall();
        }
        if (DocObj.getcurPage() != spacemarkup.pagenumber) {
            DocObj.GotoPage(spacemarkup.pagenumber);
        }

        if (bselected) {
            
            spacemarkup.selectedit = false;
            var mindex = -1;

            /*if (RxCore_GUI_Markup != undefined) {
                RxCore_GUI_Markup.setMarkupSelected(mindex, {created : false, modified : false, deleted : false});
            }*/



        } else {
            spacemarkup.selectedit = true;
            var mindex = DocObj.getmarkupbynumber(spacemarkup.markupnumber);

            if (RxCore_GUI_Markup != undefined) {
                RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[mindex], {created : false, modified : true, deleted : false});
            }
    

            //spacemarkup.setselect(true);
            //DocObj.markuplist[arrindx].lengthangleCallbackSelect();
		
        }

        drawmarkupAll(cntximg);
        DrawMarkupSelected(context);



        if(RxCore_GUI_Markuplist != undefined){
            
            RxCore_GUI_Markuplist.setMarkupList(DocObj.getmarkuplist());

        }

        //selectSpaceMarkup(blockId, multi);

    }

    function RxCore_selectRxSpace(blockId, multi){
        
        if(docpagNotReady()){
            return;
        }

        if (documentcompare) {
            return;
        }

        if (DocObj.getCurrentpage().usevector3Dxml) {
            return;
        }

        selectSpaceMarkup(blockId, multi);


    }


    function RxCore_ZoomToMarkup(markupobj){
     
        if(docpagNotReady()){
            return;
        }

        if (documentcompare) {
            return;
        }
        if (DocObj.getCurrentpage().usevector3Dxml) {
            return;
        }
        
        if(markupobj){
            DocObj.getCurrentpage().MarkupZoombyMarkup(markupobj);
         
        }


    }


    function RxCore_ZoomToBlock(blockId){

        if(docpagNotReady()){
            return;
        }

        if (documentcompare) {
            return;
        }
        if (DocObj.getCurrentpage().usevector3Dxml) {
            return;
        }

        var markupobj = findSpaceMarkup(blockId);
        
        if(markupobj){
            DocObj.getCurrentpage().MarkupZoombyMarkup(markupobj);
            return;
        }

        if (DocObj.getCurrentpage().usevectorxml) {
            DocObj.getCurrentpage().ZoomToBlock(blockId);
        }

    }


    function RxCore_ZoomToBlockInsert(blockId){

        if(docpagNotReady()){
            return;
        }

        if (documentcompare) {
            return;
        }
        if (DocObj.getCurrentpage().usevector3Dxml) {
            return;
        }

        var markupobj = findSpaceMarkup(blockId);
        
        if(markupobj){
            DocObj.getCurrentpage().MarkupZoombyMarkup(markupobj);
            return;
        }

        if (DocObj.getCurrentpage().usevectorxml) {
            DocObj.getCurrentpage().ZoomToBlockInsert(blockId);
        }

    }

    

    function RxCore_NextPage() {

        if(docpagNotReady()){
            return;
        }

        if (documentcompare) {
            return;
        }

        unselelectallmarkup();
        unselecteditmarkupall();
        
        DocObj.GotoPage(DocObj.currentpage + 1);
        
        set_tool('markupedit', {p1 : false});
        

    }

    function RxCore_PrevPage() {
        if(docpagNotReady()){
            return;
        }

        if (documentcompare) {
            return;
        }

        unselelectallmarkup();
        unselecteditmarkupall();
        
        DocObj.GotoPage(DocObj.currentpage - 1);
        
        set_tool('markupedit', {p1 : false});
        
    }

    function RxCore_getPageScale(indx){
        var curpagescale = 1;

        if (documentcompare) {
            curpagescale = 1;
        }

        if(docpagNotReady()){
            curpagescale = 1;
        }else{
            curpagescale = DocObj.getcurpageScale(indx);

            
        }

        return curpagescale;
        
    }

    function RxCore_GetcurPage() {
        var curpage = 0;
        if(docpagNotReady()){
            curpage = 0;
        } else {
            curpage = DocObj.getcurPage();
        }

        return curpage;
    }

    function RxCore_GetPage(index) {

        var curpage = undefined;
        
        if(docpagNotReady()){
            curpage = undefined;
        } else {
            curpage = DocObj.getPageObject(index);
        }

        return curpage;

    }


    function RxCore_getPageScaleObject(indx){
        var curpagescale = undefined;

        /*if (documentcompare) {
            curpagescale = 1;
        }*/

        if(docpagNotReady()){
            curpagescale = undefined;
        }else{
            
            //curpage = DocObj.getPageObject(index);
            curpagescale = DocObj.getPageObject(indx).scaleObject;


        }

        return curpagescale;
        
    }



    function RxCore_testScroll(x,y){
        if(docpagNotReady()){
            return;
        }

        if (documentcompare) {
            return;
        }

        //unselelectallmarkup();
        //unselecteditmarkupall();
        DocObj.scrolltestfoxit(x,y);

    }

    function RxCore_gotoPage(pagenum) {
        if(docpagNotReady()){
            return;
        }

        if (documentcompare) {
            return;
        }

        unselelectallmarkup();
        unselecteditmarkupall();
        DocObj.GotoPage(pagenum);
    }

    function RxCore_foxitPageEvt(pagenum){

        if(docpagNotReady()){
            return;
        }

        if (documentcompare) {
            return;
        }

        //unselelectallmarkup();
        //unselecteditmarkupall();
        
              
        DocObj.foxitpagEvent(pagenum);

    }


    function RxCore_MarkupSelect(selected) {
        if(docpagNotReady()){
            return;
        }
        if (readonlymode) {
            return;
        }
        var bIsSpace = false;

        unselelectallmarkup();
        unselecteditmarkupall();
        if (selected) {
                        
            if (bMarkupcrated) {
                //var bpointtype = (DocObj.markuplist[nMarkupcreated].type == 1 || DocObj.markuplist[nMarkupcreated].type == 8);
                var bpointtype = DocObj.markuplist[nMarkupcreated].multipoint();

                if(bpointtype){
                    DocObj.markuplist[nMarkupcreated].setselectedit(true);
                }else{
                    DocObj.markuplist[nMarkupcreated].setselect(true);
                }
                
                var bIsSpace = (DocObj.markuplist[nMarkupcreated].type == 8 && DocObj.markuplist[nMarkupcreated].subtype == 1);
                drawmarkupAll(cntximg);
                DrawMarkupSelected(context);
                bMarkupcrated = false;
                nMarkupcreated = -1;
            }
            
            set_tool('markupedit', {p1 : bIsSpace});

        } else {
            
            set_tool('markupedit', {p1 : false});
            
        }
        
        //DocObj.setMarkupMultiselected();
    }

    function RxCore_NoteFocus(onOff){
        if(docpagNotReady()){
            return;
        }

        bNoteFocus = onOff;

    }

    function RxCore_MarkupNote(selected) {
        if(docpagNotReady()){
            return;
        }
        if (readonlymode || documentcompare) {
            return;
        }
        unselelectallmarkup();
        unselecteditmarkupall();
        if (selected) {

            set_tool('note', {});
            //tool = new tools['note']();
        } else {
            
            set_tool('markupedit', {p1 : false});
            
        }

    }

    function RxCore_MarkupText(selected) {
        if(docpagNotReady()){
            return;
        }
        if (readonlymode || documentcompare) {
            return;
        }
        unselelectallmarkup();
        unselecteditmarkupall();
        if (selected) {
            set_tool('text', {});
            //tool = new tools['text']();
        } else {
            
            set_tool('markupedit', {p1 : false});
            
        }

    }

    function RxCore_MarkupErase(selected) {
        if(docpagNotReady()){
            return;
        }
        if (readonlymode || documentcompare) {
            return;
        }
        unselelectallmarkup();
        unselecteditmarkupall();
        if (selected) {
            set_tool('pencil', {p1 : 1});
            //tool = new tools['pencil'](1);
        } else {

            if(tool.name == 'pencil' && tool.started){
                tool.apply();
            }
            
            set_tool('markupedit', {p1 : false});
            
        }

    }

    function RxCore_setViews3DSavefile(szfilename){

        if(docpagNotReady()){
            return;
        }
        if (DocObj.getCurrentpage().usevector3Dxml) {
            
            //DocObj.setMarkup3DSavefile('markup.json');
            DocObj.setViews3DSavefile(szfilename);
        }

    }

    function RxCore_setMarkup3DSavefile(szfilename){

        if(docpagNotReady()){
            return;
        }
        if (DocObj.getCurrentpage().usevector3Dxml) {
            
            DocObj.setMarkup3DSavefile(szfilename);
            //DocObj.setViews3DSavefile('views.json');

            
        }

    }


    function RxCore_addAnnotationFoxit(annotobj, pagenum){

        if(docpagNotReady()){
            return;
        }
        if (readonlymode || documentcompare) {
            return;
        }

        annotFoxit_Add(annotobj, pagenum);

    }

    function RxCore_listFoxitAnnots(){

        if(docpagNotReady()){
            return;
        }
        if (readonlymode || documentcompare) {
            return;
        }

        listFoxit_Annots();

    }

    function RxCore_importFDF(url){
        if(docpagNotReady()){
            return;
        }
        if (readonlymode || documentcompare) {
            return;
        }

        importFDF(url);
        
    }

    function RxCore_foxitexportFDF(filetype){

        if(docpagNotReady()){
            return;
        }
        if (readonlymode || documentcompare) {
            return;
        }

        if (DocObj.foxitdoc != null){
            DocObj.foxitdoc.exportFDF(filetype);
        }


    }

    function RxCore_exportFDF(blob){
        
        if(docpagNotReady()){
            return;
        }
        if (readonlymode || documentcompare) {
            return;
        }

        
        sendFDF(xmlurlmarkupsave, blob);

    }


    function setUniqueMarkupfromJSON(markupJSON, DocRef){

        if(DocRef == undefined || DocRef == null ){
            DocRef = DocObj;
        }


            var MarkupFilePath = 0;
            var i = 0;
            var bcreate = false;
            var bdelete = false;
            var bmodify = false;

            var markup = JSON.parse(markupJSON);


            if(markup.operation != null){
                if (markup.operation.created){
                    bcreate = true;
                }else if(markup.operation.deleted){
                
                    bdelete = true;
                }else{
                    bmodify = true;
                }
                

            }else{
                bmodify = true;
            }

            var markupType = markup.Entity.Type; //markupentityobj[i].getElementsByTagName("Type")[0].firstChild.nodeValue;
            var markupSubtype = markup.Entity.Subtype; //markupentityobj[i].getElementsByTagName("Subtype")[0].firstChild.nodeValue;
            var markupAlternative = markup.Entity.Alternative;// markupentityobj[i].getElementsByTagName("Alternative")[0].firstChild.nodeValue;


            if(bcreate){

                if(markup.Entity.Signature != null){
                    var sign = markup.Entity.Signature;

                    if (Userlist[0].Signature != sign) {

                        
                        markupcolor = Layerlist[markup.Entity.Layer].Color;

                        Userlist.push(new Users(sign, markup.Entity.Name, markup.Entity.Layer, markupcolor, false));
                        //Userlist[numUsers] = new Users(sign, markup.Entity.Name, markup.Entity.Layer, markupcolor);
                        numUsers++;

                    }

                }


                //var markupType = markup.Entity.Type; //markupentityobj[i].getElementsByTagName("Type")[0].firstChild.nodeValue;
                //var markupSubtype = markup.Entity.Subtype; //markupentityobj[i].getElementsByTagName("Subtype")[0].firstChild.nodeValue;
                //var markupAlternative = markup.Entity.Alternative;// markupentityobj[i].getElementsByTagName("Alternative")[0].firstChild.nodeValue;
                var markupID = !markup.Entity.ID ? null : markup.Entity.ID;
                var markupuniqueID = !markup.Entity.UniqueID ? null : markup.Entity.UniqueID;

                /* prevent duplicates*/
                markupobj = DocRef.getmarkupobjbyGUID(markupuniqueID);
                if(markupobj != null){
                    return;
                }
                /* prevent duplicates*/

                var bcallout = (markupType == 9 && markupSubtype == 2);


                if ( markup.Entity.HaveLeader != null) {
                    var bhasArrow = (markup.Entity.HaveLeader == 1);
                }
                
                if(bhasArrow || bcallout){
                    if (markup.Entity.Entity != null){
                        
                        
                        var arrowleader = markup.Entity.Entity;


                        var leadermarkupType = arrowleader.Entity.Type;
                        var leadermarkupSubtype = arrowleader.Entity.Subtype; //getElementsByTagName("Subtype")[0].firstChild.nodeValue;
                        var leadermarkupAlternative = arrowleader.Entity.Alternative; // getElementsByTagName("Alternative")[0].firstChild.nodeValue;
                        //var leadermarkupID = !arrowleader.getAttribute('ID') ? null : arrowleader.getAttribute('ID');
                        var leadermarkupID = !arrowleader.Entity.ID ? null : arrowleader.Entity.ID;
                        var leadermarkupuniqueID = !arrowleader.Entity.UniqueID ? null : arrowleader.Entity.UniqueID;
                        
                        var leadermarkupobj = new MarkupObject(leadermarkupType, leadermarkupSubtype, leadermarkupAlternative);

                        arrowleader = JSON.stringify(arrowleader);

                        var blOK = leadermarkupobj.setfromJSON(DocRef,arrowleader);

                        if(blOK){
                            //markupentityobj[i].removeChild(arrowleader);
                            //var markupfiles = DocRef.getnummarkupfiles();
                            //var received = DocRef.getmarkupfilesreceived();
                            //var lastmarkup = (i == markupentityobj.length - 1 && (markupfiles == received));
                            leadermarkupobj.savemetolistLoad(DocRef,false);
                            leadermarkupobj.markupnumber = leadermarkupID;
                            leadermarkupobj.uniqueID = leadermarkupuniqueID;

                            //console.log("adding markup failed ", markupobj.markupID);

                        }

                    }

                }

                //markupType = parseInt(markupType);
                //markupSubtype = parseInt(markupSubtype);
                //markupAlternative = parseInt(markupAlternative);
                var markupobj = new MarkupObject(markupType, markupSubtype, markupAlternative);

                markupobj.markupID = markupID;
                markupobj.uniqueID = markupuniqueID;
                

                var bOK = markupobj.setfromJSON(DocRef, markupJSON );
                if (bOK) {
                    if(blOK && bhasArrow && leadermarkupobj != null){
                        markupobj.bhasArrow = bhasArrow;
                        markupobj.markupArrowConnected = leadermarkupobj;

                        leadermarkupobj.bisTextArrow = true;
                        leadermarkupobj.textBoxConnected = markupobj;
                        leadermarkupobj.setArrowPoint(markupobj);
                        
                        blOK = false;
                        bhasArrow = false;

                        drawmarkupAll(cntximg);
                    }
                    //var markupfiles = DocRef.getnummarkupfiles();
                    //var received = DocRef.getmarkupfilesreceived();
                    //var lastmarkup = (i == markupentityobj.length - 1 && (markupfiles == received));
                    markupobj.savemetolistLoad(DocRef,false);
                }else{
                    console.log("adding markup failed ", markupobj.uniqueID);
                }

                var bhole = (markupType == 8 && markupSubtype == 2);


                if ( markup.Entity.Hole != null) {
                    var bhasHole = true;
                }


                var tempholearray = [];
                var tempholeOKarr = [];

                if(bhasHole){


                    if (markup.Entity.Holes != null){
                    
                        var hole = markup.Entity.Holes;


                            var h = 0
                            while(hole.length > h){

                                var holemarkup = hole[0].Entity;

                                var holemarkupType = holemarkup.Type;
                                var holemarkupSubtype = holemarkup.Subtype;
                                var holemarkupAlternative = holemarkup.Alternative;

                                var holemarkupID = !holemarkup.ID ? null : holemarkup.ID;


                                holemarkupType = parseInt(holemarkupType);
                                holemarkupSubtype = parseInt(holemarkupSubtype);
                                holemarkupAlternative = parseInt(holemarkupAlternative);
    
                                tempholearray.push(new MarkupObject(holemarkupType, holemarkupSubtype, holemarkupAlternative));
    
                                holemarkup = JSON.stringify(holemarkup);

                                //var holexmlmarkupobj = new MarkupObject(holemarkupType, holemarkupSubtype, holemarkupAlternative);
                                tempholeOKarr.push(tempholearray[h].setfromJSON(DocRef,holemarkup));
        
                                if(tempholeOKarr[h]){
                                    //markupentityobj[i].removeChild(hole[0]);
                                    //var markupfiles = DocRef.getnummarkupfiles();
                                    //var received = DocRef.getmarkupfilesreceived();
                                    //var lastmarkup = (i == markupentityobj.length - 1 && (markupfiles == received));
                                    tempholearray[h].savemetolistLoad(DocRef,false);
                                    tempholearray[h].setDrawdim();


                                    tempholearray[h].markupnumber = holemarkupID;
        
        
                                }                            
                                h++;

                            }
                        
                    }


                    markupobj.holes = [];

                    for(var h=0;h < tempholearray.length; h++){

                        if(tempholeOKarr[h]){
                            tempholearray[h].parent = markupobj;
                            markupobj.holes.push(tempholearray[h]);
                        }
                        
                    }


                }                

            }else if (bdelete){

                if(markup.Entity.UniqueID == 0){
                    console.log("not a unique markup id");
                    return;    
                }else{
                    //deletemarkupbyGUID(markup.Entity.UniqueID);
                    deleteUniqueMarkup(markup.Entity.UniqueID);
                }
                


            }else if (bmodify){

                if(markup.Entity.UniqueID == 0){
                    markupobj = null; 
                    return;
                }else{
                    markupobj = DocRef.getmarkupobjbyGUID(markup.Entity.UniqueID);
                }
                
                

                //markupobj.markupID

                if(markupobj != null){


                    markupobj.subtype = markupSubtype;
                    markupobj.alternative = markupAlternative;

                    var bOK = markupobj.setfromJSON(DocRef, markupJSON);


                    //var markupType = markup.Entity.Type; //markupentityobj[i].getElementsByTagName("Type")[0].firstChild.nodeValue;
                    //var markupSubtype = markup.Entity.Subtype; //markupentityobj[i].getElementsByTagName("Subtype")[0].firstChild.nodeValue;
                    //var markupAlternative = markup.Entity.Alternative;// markupentityobj[i].getElementsByTagName("Alternative")[0].firstChild.nodeValue;
        

                    var bcallout = (markupobj.type == 9 && markupobj.subtype == 2);


                    if ( markupobj.bhasArrow) {
                        var bhasArrow = (markup.Entity.HaveLeader == 1);
                    }
                    
                    if(markupobj.bhasArrow || bcallout){
                        if (markup.Entity.Entity != null){
                            
                            
                            var arrowleader = markup.Entity.Entity;
                            var leadermarkupobj = markupobj.markupArrowConnected;
                            
                            
    
    
                            //var leadermarkupType = arrowleader.Entity.Type;
                            //var leadermarkupSubtype = arrowleader.Entity.Subtype; //getElementsByTagName("Subtype")[0].firstChild.nodeValue;
                            //var leadermarkupAlternative = arrowleader.Entity.Alternative; // getElementsByTagName("Alternative")[0].firstChild.nodeValue;
                            //var leadermarkupID = !arrowleader.getAttribute('ID') ? null : arrowleader.getAttribute('ID');
                            //var leadermarkupID = !arrowleader.Entity.ID ? null : arrowleader.Entity.ID;
                            
                            //var leadermarkupobj = new MarkupObject(leadermarkupType, leadermarkupSubtype, leadermarkupAlternative);
    
                            arrowleader = JSON.stringify(arrowleader);
    
                            var blOK = leadermarkupobj.setfromJSON(DocRef,arrowleader);
                            leadermarkupobj.setfromJSON(DocRef,arrowleader);
                            leadermarkupobj.setArrowPoint(markupobj);
                               
                       }
    
                    }
                    
                    var bhole = (markupType == 8 && markupSubtype == 2);


                    if ( markup.Entity.Holes != null) {
                        var bhasHole = true;
                    }


                    var tempholearray = [];
                    var tempholeOKarr = [];

                    if(bhasHole){


                        if (markup.Entity.Holes != null){
                        
                            var hole = markup.Entity.Holes;

                            var h = 0;
                            
                            while(hole.length > h){

                                var holemarkup = hole[0];

                                var holemarkupType = holemarkup.Entity.Type;
                                var holemarkupSubtype = holemarkup.Entity.Subtype;
                                var holemarkupAlternative = holemarkup.Entity.Alternative;

                                var holemarkupID = !holemarkup.Entity.ID ? null : holemarkup.Entity.ID;


                                holemarkupType = parseInt(holemarkupType);
                                holemarkupSubtype = parseInt(holemarkupSubtype);
                                holemarkupAlternative = parseInt(holemarkupAlternative);
    
                                tempholearray.push(new MarkupObject(holemarkupType, holemarkupSubtype, holemarkupAlternative));

                                holemarkup = JSON.stringify(holemarkup);
    
                                //var holexmlmarkupobj = new MarkupObject(holemarkupType, holemarkupSubtype, holemarkupAlternative);
                                tempholeOKarr.push(tempholearray[h].setfromJSON(DocRef,holemarkup));
        
                                if(tempholeOKarr[h]){
                                    //markupentityobj[i].removeChild(hole[0]);
                                    //var markupfiles = DocRef.getnummarkupfiles();
                                    //var received = DocRef.getmarkupfilesreceived();
                                    //var lastmarkup = (i == markupentityobj.length - 1 && (markupfiles == received));
                                    tempholearray[h].savemetolistLoad(DocRef,false);
                                    tempholearray[h].setDrawdim();


                                    tempholearray[h].markupnumber = holemarkupID;
        
        
                                }                            
                                h++;

                            }
                            
                        }

                        markupobj.holes = [];

                        for(var h=0;h < tempholearray.length; h++){
    
                            if(tempholeOKarr[h]){
                                tempholearray[h].parent = markupobj;
                                markupobj.holes.push(tempholearray[h]);
                            }
                            
                        }




                    }          
                    

                    //leadermarkupobj.setArrowPoint(markupobj);
                    
    


                    
                }else if(markup.Entity.UniqueID != 0){


                    /*if(markup.operation != null){
                        if (markup.operation.created){
                            bcreate = true;
                        }else if(markup.operation.deleted){
                        
                            bdelete = true;
                        }else{
                            bmodify = true;
                        }
                        
        
                    }else{
                        bmodify = true;
                    }*/

                    if (!markup.operation) {
                        markup.operation = {};
                    }


                    markup.operation.created = true;
                    markup.operation.deleted = false;
                    markup.operation.modified = false;

                    var newmarkupjson = JSON.stringify(markup);

                    setUniqueMarkupfromJSON(newmarkupjson, DocRef);
                    return;
                }else{
                    console.log("adding or modifying markup failed ", markupobj.markupID);
                }

                

            }
            
            if (RxCore_GUI_Markuplist != undefined) {
                RxCore_GUI_Markuplist.setMarkupList(DocRef.getmarkuplist(),DocRef.bActive);
            }

            if (RxCore_GUI_Users != undefined) {
                RxCore_GUI_Users.setUserlist(Userlist, DocRef.bActive);
            }


            drawmarkupAll(cntximg);

    }

    function addWatermark(text, settings) {
        if(DocObj) {

            if (DocObj.foxitdoc != null){
                DocObj.foxitdoc.addWatermarkToAllPages(text, settings);
            }
    

            
        }
    }

    function addWatermarkpage(pagenum, text, settings) {
        if(DocObj) {

            if (DocObj.foxitdoc != null){
                DocObj.foxitdoc.addWatermarkToPage(pagenum, text, settings);
            }
                
        }
    }



    function removeWatermark() {
        if(DocObj ) {

            if (DocObj.foxitdoc != null){
                DocObj.foxitdoc.removeWatermarkFromAllPages();
            }

            
        }
    }

    function removeWatermarkpage(pagenum) {
        if(DocObj ) {

            if (DocObj.foxitdoc != null){
                DocObj.foxitdoc.removeWatermarkFromPage(pagenum);
            }
        }
    }


    /*this.addWatermarkToPage = function(npagenum, text, settings = {}) {
        
        const watermarkOptions = this.createWatermarkOptions(text, settings);

        foxview.pdfViewer.getCurrentPDFDoc().getPageByIndex(npagenum).then(function (page) {

            page.addWatermark(watermarkOptions);
                
        });

    };*/

    /*this.removeWatermarkFromPage = function(npagenum) {
        
        foxview.pdfViewer.getCurrentPDFDoc().getPageByIndex(npagenum).then(function (page) {
            page.removeAllWatermarks();
        });
        
        
    };*/

        


    function setMarkupfromJSON(markupJSON, DocRef){


        if(DocRef == undefined || DocRef == null ){
            DocRef = DocObj;
        }


            var MarkupFilePath = 0;
            var i = 0;
            var bcreate = false;
            var bdelete = false;
            var bmodify = false;

            var markup = JSON.parse(markupJSON);


            if(markup.operation != null){
                if (markup.operation.created){
                    bcreate = true;
                }else if(markup.operation.deleted){
                
                    bdelete = true;
                }else{
                    bmodify = true;
                }
                

            }else{
                bmodify = true;
            }

            var markupType = markup.Entity.Type; //markupentityobj[i].getElementsByTagName("Type")[0].firstChild.nodeValue;
            var markupSubtype = markup.Entity.Subtype; //markupentityobj[i].getElementsByTagName("Subtype")[0].firstChild.nodeValue;
            var markupAlternative = markup.Entity.Alternative;// markupentityobj[i].getElementsByTagName("Alternative")[0].firstChild.nodeValue;


            if(bcreate){

                if(markup.Entity.Signature != null){
                    var sign = markup.Entity.Signature;

                    if (Userlist[0].Signature != sign) {

                        
                        markupcolor = Layerlist[markup.Entity.Layer].Color;
                        Userlist.push(new Users(sign, markup.Entity.Name, markup.Entity.Layer, markupcolor, false));
                        //Userlist[numUsers] = new Users(sign, markup.Entity.Name, markup.Entity.Layer, markupcolor);
                        numUsers++;

                    }

                }


                //var markupType = markup.Entity.Type; //markupentityobj[i].getElementsByTagName("Type")[0].firstChild.nodeValue;
                //var markupSubtype = markup.Entity.Subtype; //markupentityobj[i].getElementsByTagName("Subtype")[0].firstChild.nodeValue;
                //var markupAlternative = markup.Entity.Alternative;// markupentityobj[i].getElementsByTagName("Alternative")[0].firstChild.nodeValue;
                var markupID = !markup.Entity.ID ? null : markup.Entity.ID;


                var bcallout = (markupType == 9 && markupSubtype == 2);


                if ( markup.Entity.HaveLeader != null) {
                    var bhasArrow = (markup.Entity.HaveLeader == 1);
                }
                
                if(bhasArrow || bcallout){
                    if (markup.Entity.Entity != null){
                        
                        
                        var arrowleader = markup.Entity.Entity;


                        var leadermarkupType = arrowleader.Entity.Type;
                        var leadermarkupSubtype = arrowleader.Entity.Subtype; //getElementsByTagName("Subtype")[0].firstChild.nodeValue;
                        var leadermarkupAlternative = arrowleader.Entity.Alternative; // getElementsByTagName("Alternative")[0].firstChild.nodeValue;
                        //var leadermarkupID = !arrowleader.getAttribute('ID') ? null : arrowleader.getAttribute('ID');
                        var leadermarkupID = !arrowleader.Entity.ID ? null : arrowleader.Entity.ID;
                        
                        var leadermarkupobj = new MarkupObject(leadermarkupType, leadermarkupSubtype, leadermarkupAlternative);

                        arrowleader = JSON.stringify(arrowleader);

                        var blOK = leadermarkupobj.setfromJSON(DocRef,arrowleader);

                        if(blOK){
                            //markupentityobj[i].removeChild(arrowleader);
                            //var markupfiles = DocRef.getnummarkupfiles();
                            //var received = DocRef.getmarkupfilesreceived();
                            //var lastmarkup = (i == markupentityobj.length - 1 && (markupfiles == received));
                            leadermarkupobj.savemetolistLoad(DocRef,false);
                            leadermarkupobj.markupnumber = leadermarkupID;

                        }

                    }

                }


                var bhole = (markupType == 8 && markupSubtype == 2);


                if ( markup.Entity.Hole != null) {
                    var bhasHole = true;
                }


                var tempholearray = [];
                var tempholeOKarr = [];

                if(bhasHole){


                    if (markup.Entity.Holes != null){
                    
                        var hole = markup.Entity.Holes;


                        var h = 0
                            while(hole.length > 0){
                                var holemarkupType = hole[0].Type;
                                var holemarkupSubtype = hole[0].Subtype;
                                var holemarkupAlternative = hole[0].Alternative;

                                var holemarkupID = !hole[0].ID ? null : hole[0].ID;


                                holemarkupType = parseInt(holemarkupType);
                                holemarkupSubtype = parseInt(holemarkupSubtype);
                                holemarkupAlternative = parseInt(holemarkupAlternative);
    
                                tempholearray.push(new MarkupObject(holemarkupType, holemarkupSubtype, holemarkupAlternative));
    
                                //var holexmlmarkupobj = new MarkupObject(holemarkupType, holemarkupSubtype, holemarkupAlternative);
                                tempholeOKarr.push(tempholearray[h].setfromJSON(DocRef,hole[0]));
        
                                if(tempholeOKarr[h]){
                                    //markupentityobj[i].removeChild(hole[0]);
                                    //var markupfiles = DocRef.getnummarkupfiles();
                                    //var received = DocRef.getmarkupfilesreceived();
                                    //var lastmarkup = (i == markupentityobj.length - 1 && (markupfiles == received));
                                    tempholearray[h].savemetolistLoad(DocRef,false);


                                    tempholearray[h].markupnumber = holemarkupID;
        
        
                                }                            
                                h++;

                            }
                        
                    }


                }


                //markupType = parseInt(markupType);
                //markupSubtype = parseInt(markupSubtype);
                //markupAlternative = parseInt(markupAlternative);
                var markupobj = new MarkupObject(markupType, markupSubtype, markupAlternative);

                markupobj.markupID = markupID;

                var bOK = markupobj.setfromJSON(DocRef, markupJSON );
                if (bOK) {
                    if(blOK && bhasArrow && leadermarkupobj != null){
                        markupobj.bhasArrow = bhasArrow;
                        markupobj.markupArrowConnected = leadermarkupobj;

                        leadermarkupobj.bisTextArrow = true;
                        leadermarkupobj.textBoxConnected = markupobj;
                        leadermarkupobj.setArrowPoint(markupobj);
                        
                        blOK = false;
                        bhasArrow = false;


                        if(bhasHole){
                            for(var h=0;h < tempholearray.length; h++){
    
                                if(tempholeOKarr[h]){
                                    tempholearray[h].parent = markupobj;
                                    markupobj.holes.push(tempholearray[h]);
                                }
                                
                            }
    
                        }


                        drawmarkupAll(cntximg);
                    }
                    //var markupfiles = DocRef.getnummarkupfiles();
                    //var received = DocRef.getmarkupfilesreceived();
                    //var lastmarkup = (i == markupentityobj.length - 1 && (markupfiles == received));
                    markupobj.savemetolistLoad(DocRef,false);
                }else{
                    console.log("adding markup failed ", markupobj.markupID);
                }



                

            }else if (bdelete){

                deletemarkupbyID(markup.Entity.ID);


            }else if (bmodify){

                markupobj = DocRef.getmarkupobjbyID(markup.Entity.ID);
                

                //markupobj.markupID

                if(markupobj != null){


                    markupobj.subtype = markupSubtype;
                    markupobj.alternative = markupAlternative;

                    var bOK = markupobj.setfromJSON(DocRef, markupJSON);


                    //var markupType = markup.Entity.Type; //markupentityobj[i].getElementsByTagName("Type")[0].firstChild.nodeValue;
                    //var markupSubtype = markup.Entity.Subtype; //markupentityobj[i].getElementsByTagName("Subtype")[0].firstChild.nodeValue;
                    //var markupAlternative = markup.Entity.Alternative;// markupentityobj[i].getElementsByTagName("Alternative")[0].firstChild.nodeValue;
        

                    var bcallout = (markupobj.type == 9 && markupobj.subtype == 2);


                    if ( markupobj.bhasArrow) {
                        var bhasArrow = (markup.Entity.HaveLeader == 1);
                    }
                    
                    if(markupobj.bhasArrow || bcallout){
                        if (markup.Entity.Entity != null){
                            
                            
                            var arrowleader = markup.Entity.Entity;
                            var leadermarkupobj = markupobj.markupArrowConnected;
                            
                            
    
    
                            //var leadermarkupType = arrowleader.Entity.Type;
                            //var leadermarkupSubtype = arrowleader.Entity.Subtype; //getElementsByTagName("Subtype")[0].firstChild.nodeValue;
                            //var leadermarkupAlternative = arrowleader.Entity.Alternative; // getElementsByTagName("Alternative")[0].firstChild.nodeValue;
                            //var leadermarkupID = !arrowleader.getAttribute('ID') ? null : arrowleader.getAttribute('ID');
                            //var leadermarkupID = !arrowleader.Entity.ID ? null : arrowleader.Entity.ID;
                            
                            //var leadermarkupobj = new MarkupObject(leadermarkupType, leadermarkupSubtype, leadermarkupAlternative);
    
                            arrowleader = JSON.stringify(arrowleader);
    
                            var blOK = leadermarkupobj.setfromJSON(DocRef,arrowleader);
                            leadermarkupobj.setfromJSON(DocRef,arrowleader);
                            leadermarkupobj.setArrowPoint(markupobj);
                               
                       }
    
                    }


                    


                    //leadermarkupobj.setArrowPoint(markupobj);
                    
    


                    
                }else{


                    /*if(markup.operation != null){
                        if (markup.operation.created){
                            bcreate = true;
                        }else if(markup.operation.deleted){
                        
                            bdelete = true;
                        }else{
                            bmodify = true;
                        }
                        
        
                    }else{
                        bmodify = true;
                    }*/

                    if(markup.operation == null){
                        markup.operation = {};
                   }

                    markup.operation.created = true;
                    markup.operation.deleted = false;
                    markup.operation.modified = false;

                    var newmarkupjson = JSON.stringify(markup);

                    setMarkupfromJSON(newmarkupjson, DocRef);
                    return;
                }

                

            }
            
            if (RxCore_GUI_Markuplist != undefined) {
                RxCore_GUI_Markuplist.setMarkupList(DocRef.getmarkuplist(),DocRef.bActive);
            }

            if (RxCore_GUI_Users != undefined) {
                RxCore_GUI_Users.setUserlist(Userlist, DocRef.bActive);
            }


            drawmarkupAll(cntximg);


            //if(markup.operation.create)




    }



    function markupXMLhandler(xmlDoc, DocRef, MarkupFile){

        if(DocRef == undefined){
            DocRef = DocObj;
        }
        
        var MarkupFilePath = 0;
        var i = 0;

        DocRef.setmarkupfilesreceived();

        var received = DocRef.getmarkupfilesreceived();
        var markupfiles = DocRef.getnummarkupfiles();


            //DocObj = new DocumentObject(xmlDoc);
        if (xmlDoc.getElementsByTagName('FileVersion') != null && xmlDoc.getElementsByTagName('FileVersion').length != 0) {
            var fileVersion = xmlDoc.getElementsByTagName('FileVersion')[0].firstChild.nodeValue;

        }

        if (xmlDoc.getElementsByTagName('Format') != null && xmlDoc.getElementsByTagName('Format').length != 0) {

            var format = xmlDoc.getElementsByTagName('Format')[0].firstChild.nodeValue;

            if(MarkupFile != undefined){
                var cachfolder = getURLPath(MarkupFile);
            }
            

            if (xmlDoc.getElementsByTagName('Markup3D') != null && xmlDoc.getElementsByTagName('Markup3D').length != 0){
                //read markup 3d json file here.
                var markupfile3d = xmlDoc.getElementsByTagName('Markup3D')[0].firstChild.nodeValue;

                if(MarkupFile != undefined){
                    markupfile3d = cachfolder + markupfile3d;
                    get3DMarkup(markupfile3d, DocRef);
                }


            }

            if (xmlDoc.getElementsByTagName('Views3D') != null && xmlDoc.getElementsByTagName('Views3D').length != 0){
                
                var viewsfile3d = xmlDoc.getElementsByTagName('Views3D')[0].firstChild.nodeValue;

                if(MarkupFile != undefined){
                    viewsfile3d = cachfolder + viewsfile3d;
                    get3DViews(viewsfile3d, DocRef);
    
                }


                //read views 3d json file here.
            }


        }


        if (xmlDoc.getElementsByTagName('User') != null && xmlDoc.getElementsByTagName('User').length != 0) {
            var UserID = xmlDoc.getElementsByTagName('User');
            for (i = 0; i < UserID.length; i++) {

                var MarkupUserDispName = UserID[i].getElementsByTagName("Name")[0].firstChild.nodeValue;
                var MarkupUserID = UserID[i].getElementsByTagName("ID")[0].firstChild.nodeValue;
                if (UserID[i].getElementsByTagName("Layer")[0] != undefined) {
                    var MarkupUserLayer = UserID[i].getElementsByTagName("Layer")[0].firstChild.nodeValue;
                } else {
                    MarkupUserLayer = markuplayer;
                }
                if (UserID[i].getElementsByTagName("Color")[0] != undefined) {
                    var MarkupUserColor = UserID[i].getElementsByTagName("Color")[0].firstChild.nodeValue;
                } else {
                    MarkupUserColor = markupcolor;
                }


                //var MarkupUserLayer = UserID[i].getElementsByTagName("Layer")[0].firstChild.nodeValue;
                //var MarkupUserColor = UserID[i].getElementsByTagName("Color")[0].firstChild.nodeValue;


                if (Userlist[0].Signature == MarkupUserID) {
                    //Userlist[0] = new Users(MarkupUserID,MarkupUserDispName,MarkupUserLayer,MarkupUserColor);
                    //numUsers++;
                } else {
                    Userlist.push(new Users(MarkupUserID, MarkupUserDispName, MarkupUserLayer, MarkupUserColor, false));
                    //Userlist[numUsers] = new Users(MarkupUserID, MarkupUserDispName, MarkupUserLayer, MarkupUserColor);
                    numUsers++;
                }
            }
        } else {
            if (xmlDoc.getElementsByTagName('Name').length > 0) {

                var sign = xmlDoc.getElementsByTagName('Name');
                if (Userlist[0].Signature != sign[0].firstChild.nodeValue) {

                    Userlist.push(new Users(sign[0].firstChild.nodeValue, sign[0].firstChild.nodeValue, markuplayer, markupcolor, false));
                    //Userlist[numUsers] = new Users(sign[0].firstChild.nodeValue, sign[0].firstChild.nodeValue, markuplayer, markupcolor);
                    numUsers++;

                }
            }
        }
        if (xmlDoc.getElementsByTagName('Entity').length > 0) {

            var markupentityobj = xmlDoc.getElementsByTagName('Entity');
            for (i = 0; i < markupentityobj.length; i++) {
                var markupType = markupentityobj[i].getElementsByTagName("Type")[0].firstChild.nodeValue;
                var markupSubtype = markupentityobj[i].getElementsByTagName("Subtype")[0].firstChild.nodeValue;
                var markupAlternative = markupentityobj[i].getElementsByTagName("Alternative")[0].firstChild.nodeValue;
                var markupID = !markupentityobj[i].getAttribute('ID') ? null : markupentityobj[i].getAttribute('ID');

                /*if(markupentityobj[i].getElementsByTagName('View')[0] != undefined){
                    var pagenumber = parseInt(markupentityobj[i].getElementsByTagName('View')[0].firstChild.nodeValue);
                }else{
                    pagenumber = 0;
                }*/

                var bcallout = (markupType == 9 && markupSubtype == 2);



                if ( markupentityobj[i].getElementsByTagName('HaveLeader')[0] != undefined) {
                    var bhasArrow = (parseInt(markupentityobj[i].getElementsByTagName('HaveLeader')[0].firstChild.nodeValue) == 1);
                }
                
                if(bhasArrow || bcallout){
                    if (markupentityobj[i].getElementsByTagName('Entity')[0] != undefined){
                        var arrowleader = markupentityobj[i].getElementsByTagName("Entity")[0];
                        var leadermarkupType = arrowleader.getElementsByTagName("Type")[0].firstChild.nodeValue;
                        var leadermarkupSubtype = arrowleader.getElementsByTagName("Subtype")[0].firstChild.nodeValue;
                        var leadermarkupAlternative = arrowleader.getElementsByTagName("Alternative")[0].firstChild.nodeValue;
                        var leadermarkupID = !arrowleader.getAttribute('ID') ? null : arrowleader.getAttribute('ID');
                        leadermarkupType = parseInt(leadermarkupType);
                        leadermarkupSubtype = parseInt(leadermarkupSubtype);
                        leadermarkupAlternative = parseInt(leadermarkupAlternative);
                        var leaderxmlmarkupobj = new MarkupObject(leadermarkupType, leadermarkupSubtype, leadermarkupAlternative);
                        var blOK = leaderxmlmarkupobj.SetfromXML(DocRef,arrowleader, fileVersion);

                        if(blOK){
                            markupentityobj[i].removeChild(arrowleader);
                            //var markupfiles = DocRef.getnummarkupfiles();
                            //var received = DocRef.getmarkupfilesreceived();
                            //var lastmarkup = (i == markupentityobj.length - 1 && (markupfiles == received));
                            leaderxmlmarkupobj.savemetolistLoad(DocRef,false);

                        }

                    }else{
                        //console.log("callout without arrow");
                        continue;
                    }

                }

                var bhole = (markupType == 8 && markupSubtype == 2);


                if ( markupentityobj[i].getElementsByTagName('Hole')[0] != undefined) {
                    var bhasHole = true;
                }

                var tempholearray = [];
                var tempholeOKarr = [];

                if(bhasHole){
                    if (markupentityobj[i].getElementsByTagName('Entity')[0] != undefined){
                        var hole = markupentityobj[i].getElementsByTagName("Entity");

                        if(hole.length == undefined){

                            var holemarkupType = hole.getElementsByTagName("Type")[0].firstChild.nodeValue;
                            var holemarkupSubtype = hole.getElementsByTagName("Subtype")[0].firstChild.nodeValue;
                            var holemarkupAlternative = hole.getElementsByTagName("Alternative")[0].firstChild.nodeValue;
                            var holemarkupID = !hole.getAttribute('ID') ? null : hole.getAttribute('ID');
                            holemarkupType = parseInt(holemarkupType);
                            holemarkupSubtype = parseInt(holemarkupSubtype);
                            holemarkupAlternative = parseInt(holemarkupAlternative);

                            tempholearray.push(new MarkupObject(holemarkupType, holemarkupSubtype, holemarkupAlternative));

                            //var holexmlmarkupobj = new MarkupObject(holemarkupType, holemarkupSubtype, holemarkupAlternative);
                            tempholeOKarr.push(tempholearray[h].SetfromXML(DocRef,hole[h], fileVersion));
    
                            if(tempholeOKarr[0]){
                                markupentityobj[i].removeChild(hole);
                                //var markupfiles = DocRef.getnummarkupfiles();
                                //var received = DocRef.getmarkupfilesreceived();
                                //var lastmarkup = (i == markupentityobj.length - 1 && (markupfiles == received));
                                tempholearray[0].savemetolistLoad(DocRef,false);
    
                            }                            

                            //we only have one entity
                        }else{

                            var h = 0;
                            while(hole.length > 0){
                                var holemarkupType = hole[0].getElementsByTagName("Type")[0].firstChild.nodeValue;
                                var holemarkupSubtype = hole[0].getElementsByTagName("Subtype")[0].firstChild.nodeValue;
                                var holemarkupAlternative = hole[0].getElementsByTagName("Alternative")[0].firstChild.nodeValue;
                                var holemarkupID = !hole[0].getAttribute('ID') ? null : hole[0].getAttribute('ID');
                                holemarkupType = parseInt(holemarkupType);
                                holemarkupSubtype = parseInt(holemarkupSubtype);
                                holemarkupAlternative = parseInt(holemarkupAlternative);
    
                                tempholearray.push(new MarkupObject(holemarkupType, holemarkupSubtype, holemarkupAlternative));
    
                                //var holexmlmarkupobj = new MarkupObject(holemarkupType, holemarkupSubtype, holemarkupAlternative);
                                tempholeOKarr.push(tempholearray[h].SetfromXML(DocRef,hole[0], fileVersion));
        
                                if(tempholeOKarr[h]){
                                    markupentityobj[i].removeChild(hole[0]);
                                    //var markupfiles = DocRef.getnummarkupfiles();
                                    //var received = DocRef.getmarkupfilesreceived();
                                    //var lastmarkup = (i == markupentityobj.length - 1 && (markupfiles == received));
                                    tempholearray[h].savemetolistLoad(DocRef,false);
        
                                }                            
                                h++;

                            }
                            
                            /*for(var h=0;h < hole.length; h++){

    
                            }*/
                        }



                        

                    }else{
                        //console.log("callout without arrow");
                        continue;
                    }


                }


                markupType = parseInt(markupType);
                markupSubtype = parseInt(markupSubtype);
                markupAlternative = parseInt(markupAlternative);
                var xmlmarkupobj = new MarkupObject(markupType, markupSubtype, markupAlternative);
                xmlmarkupobj.markupID = markupID;
                

                var bOK = xmlmarkupobj.SetfromXML(DocRef,markupentityobj[i], fileVersion);
                if (bOK) {
                    if(blOK && bhasArrow && leaderxmlmarkupobj != null){
                        xmlmarkupobj.bhasArrow = bhasArrow;
                        xmlmarkupobj.markupArrowConnected = leaderxmlmarkupobj;

                        leaderxmlmarkupobj.bisTextArrow = true;
                        leaderxmlmarkupobj.textBoxConnected = xmlmarkupobj;
                        leaderxmlmarkupobj.setArrowPoint(xmlmarkupobj);
                        
                        blOK = false;
                        bhasArrow = false;

                        drawmarkupAll(cntximg);

                    }
                    if(bhasHole){
                        for(var h=0;h < tempholearray.length; h++){

                            if(tempholeOKarr[h]){
                                tempholearray[h].parent = xmlmarkupobj;
                                xmlmarkupobj.holes.push(tempholearray[h]);
                            }
                            
                        }

                    }

                    //var markupfiles = DocRef.getnummarkupfiles();
                    //var received = DocRef.getmarkupfilesreceived();
                    var lastmarkup = (i == markupentityobj.length - 1 && (markupfiles == received));
                    xmlmarkupobj.savemetolistLoad(DocRef,lastmarkup);
                }else{
                    console.log("adding markup failed ", xmlmarkupobj.markupID);
                }




                //xmlmarkupobj.markupnumber = parseInt(markupID);
            }

            /* Andriy call callback only once */
            /*if(lastmarkup){
                for(var mc = 0; mc < DocObj.markuplist.length; mc++){
                    if(DocObj.markuplist[mc].bhasArrow && !isNaN(DocObj.markuplist[mc].markupArrowConnected)){
                        var markupnum = DocObj.markuplist[mc].markupArrowConnected;
                        for(var ac = 0; ac < DocObj.markuplist.length; ac++){
                            if (DocObj.markuplist[ac].markupnumber == markupnum){
                                DocObj.markuplist[mc].markupArrowConnected = DocObj.markuplist[ac];
                            }
                        }
                    }
    
                }

            }*/
        

            if (xmlmarkupobj) xmlmarkupobj.notify();

            if (markupentityobj.length == 0) {
                if (RxCore_GUI_Markuplist != undefined) {
                    RxCore_GUI_Markuplist.setMarkupList(DocRef.getmarkuplist(),DocRef.bActive);
                    /* Andriy */
                    // RxCore_GUI_Markuplist.notify();
                }

            }

            if (RxCore_GUI_Users != undefined) {
                RxCore_GUI_Users.setUserlist(Userlist, DocRef.bActive);
            }

            if (Rxcore_GUI_fileLoadComplete != undefined) {
                //Rxcore_GUI_fileLoadComplete.loadComplete(MarkupFile);
            }

            drawmarkupAll(cntximg);
        }else{


            if(markupfiles == received){

                drawmarkupAll(cntximg);

                if (RxCore_GUI_Markuplist != undefined) {
                    RxCore_GUI_Markuplist.setMarkupList(DocRef.getmarkuplist(),DocRef.bActive);
                    /* Andriy */
                    // RxCore_GUI_Markuplist.notify();
                }

            }

        }     

        if (xmlDoc.getElementsByTagName('ScaleItem').length > 0) {

            var scaleobj = xmlDoc.getElementsByTagName('ScaleItem');
            
            let scales = [];
            for (i = 0; i < scaleobj.length; i++) {
                
                var value = scaleobj[i].getElementsByTagName("Value")[0].firstChild.nodeValue;
                var label = scaleobj[i].getElementsByTagName("Label")[0].firstChild.nodeValue;
                var metric = scaleobj[i].getElementsByTagName("Metric")[0].firstChild.nodeValue;
                var metricUnit = scaleobj[i].getElementsByTagName("MetricUnit")[0].firstChild.nodeValue;
                var dimPrecision = scaleobj[i].getElementsByTagName("DimPrecision")[0].firstChild.nodeValue;
                var isSelected = scaleobj[i].getElementsByTagName("IsSelected")[0].firstChild.nodeValue === 'true';

                const obj = {
                    value: value, 
                    label: label,
                    metric: metric,
                    metricUnit: metricUnit,
                    dimPrecision: dimPrecision,
                    isSelected: isSelected
                }
                scales.push(obj);

                if(isSelected) {
                    loadPageScaleSettings(obj);
                }

            }
            //setScalesToLocalStorage(scales);
            DocObj.scalesOptions = scales;
        }

        if(DocRef.markuplist.length) {
            //load markup from xml
            loadMarkupScaleSettings(xmlDoc, DocRef);
        }
        
        Rxcore_GUI_scaleListLoadComplete.loadComplete();

    }

    function loadPageScaleSettings(selectedScaleObj) {
        //set page surrent scale
        if(selectedScaleObj.metric === '0'){
            RxCore_SetUnit(1);
        } else {
            RxCore_SetUnit(2);
        }
        
        if (selectedScaleObj.metric === '0') {          
            RxCore_MetricUnit(selectedScaleObj.metricUnit);          
        } else {
            RxCore_ImperialUnit(selectedScaleObj.metricUnit);
        } 

        RxCore_setDimPrecisionForPage(selectedScaleObj.dimPrecision);
        RxCore_scale(selectedScaleObj.value);
        RxCore_setScaleLabel(selectedScaleObj.label);
    }


    function loadMarkupScaleSettings(xmlDoc, DocRef) {

        var pageobject = DocRef.getPageObject(DocRef.markuplist[0].pagenumber);
        if (pageobject != undefined) {            
            
            if (xmlDoc.getElementsByTagName('HasScale')[0] !== undefined) {
                var indexDefaultScale = 0;
                var isSetPageDefaultScale = false;
                for(var i = 0; i <xmlDoc.getElementsByTagName('HasScale').length; i++){

                    if(xmlDoc.getElementsByTagName('HasScale')[i].firstChild.nodeValue === 'true') {
                        DocRef.markuplist[i].hasScale = xmlDoc.getElementsByTagName('HasScale')[i].firstChild.nodeValue;
                        DocRef.markuplist[i].scaleObject.measurescale = parseFloat(xmlDoc.getElementsByTagName('Measurescale')[i].firstChild.nodeValue);
                        DocRef.markuplist[i].scaleObject.scaleLabel = xmlDoc.getElementsByTagName('ScaleLabel')[i].firstChild.nodeValue;
                        DocRef.markuplist[i].scaleObject.Unitofmeasure = parseFloat(xmlDoc.getElementsByTagName('Unitofmeasure')[i].firstChild.nodeValue);
                        DocRef.markuplist[i].scaleObject.SubmeasureUnit = parseFloat(xmlDoc.getElementsByTagName('SubmeasureUnit')[i].firstChild.nodeValue);
                        DocRef.markuplist[i].scaleObject.Unitlabel = xmlDoc.getElementsByTagName('Unitlabel')[i].firstChild.nodeValue;
                        DocRef.markuplist[i].scaleObject.AreaUnitlabel = xmlDoc.getElementsByTagName('AreaUnitlabel')[i].firstChild.nodeValue;
                        DocRef.markuplist[i].scaleObject.unitscale = parseFloat(xmlDoc.getElementsByTagName('Unitscale')[i].firstChild.nodeValue);
                        DocRef.markuplist[i].scaleObject.scaleValue = xmlDoc.getElementsByTagName('ScaleValue')[i].firstChild.nodeValue;
                        DocRef.markuplist[i].scaleObject.ndimprecision = xmlDoc.getElementsByTagName('Ndimprecision')[i].firstChild.nodeValue;
                        
                    }

                    /*if(isSetPageDefaultScale === false && xmlDoc.getElementsByTagName('HasScale')[i].firstChild.nodeValue === 'false') {
                        indexDefaultScale = i;
                        isSetPageDefaultScale = true;
                    }*/
                    
                }
            }
            
            
            /*if (xmlDoc.getElementsByTagName('Measurescale')[indexDefaultScale] != undefined) {
                pageobject.scaleObject.measurescale = parseFloat(xmlDoc.getElementsByTagName('Measurescale')[indexDefaultScale].firstChild.nodeValue);
            }
            if (xmlDoc.getElementsByTagName('ScaleLabel')[indexDefaultScale] != undefined) {
                pageobject.scaleObject.scaleLabel = xmlDoc.getElementsByTagName('ScaleLabel')[indexDefaultScale].firstChild.nodeValue;
            }                
            if (xmlDoc.getElementsByTagName('Unitofmeasure')[indexDefaultScale] != undefined) {
                pageobject.scaleObject.Unitofmeasure = parseFloat(xmlDoc.getElementsByTagName('Unitofmeasure')[indexDefaultScale].firstChild.nodeValue);
            }
            if (xmlDoc.getElementsByTagName('SubmeasureUnit')[indexDefaultScale] != undefined) {
                pageobject.scaleObject.SubmeasureUnit = parseFloat(xmlDoc.getElementsByTagName('SubmeasureUnit')[indexDefaultScale].firstChild.nodeValue);
            }
            if (xmlDoc.getElementsByTagName('Unitlabel')[indexDefaultScale] != undefined) {
                pageobject.scaleObject.Unitlabel = xmlDoc.getElementsByTagName('Unitlabel')[indexDefaultScale].firstChild.nodeValue;
            }
            if (xmlDoc.getElementsByTagName('AreaUnitlabel')[indexDefaultScale] != undefined) {
                pageobject.scaleObject.AreaUnitlabel = xmlDoc.getElementsByTagName('AreaUnitlabel')[indexDefaultScale].firstChild.nodeValue;
            }
            if (xmlDoc.getElementsByTagName('Unitscale')[indexDefaultScale] != undefined) {
                pageobject.scaleObject.unitscale = parseFloat(xmlDoc.getElementsByTagName('Unitscale')[indexDefaultScale].firstChild.nodeValue);
            }
            if (xmlDoc.getElementsByTagName('ScaleValue')[indexDefaultScale] != undefined) {
                pageobject.scaleObject.scaleValue = xmlDoc.getElementsByTagName('ScaleValue')[indexDefaultScale].firstChild.nodeValue;
            }*/            

            RxCore_MarkupRedraw();
        }
    }    

    function RxCore_LoadMarkupJSON(folder, fileName, DocRef){

        if(docpagNotReady()){
            return;
        }
        
        if(CompareFilesServer == undefined){
            return;
        }

        if(DocRef == undefined){
            DocRef = DocObj;
        }


        /*{
            "Command": "GetMarkup",
            "LicenseID": "6",
            "FileName" : "testest.rxml",
            "Folder" : "C:\\\\Rasterex\\\\Upload\\\\"
        }*/


        var JSONSaveMarkupFile = [{
            Command : 'GetMarkup',
            LicenseID : LicenseID,
            FileName : fileName,
            Folder : folder
        }];

        var jsonstring = JSON.stringify(JSONSaveMarkupFile);

    
        sendHttpRequestXML('POST', CompareFilesServer, jsonstring).then(function (response){

            
            markupXMLhandler(response, DocRef);

            /*if (RxCore_GUI_MarkupSave != undefined){
                RxCore_GUI_MarkupSave.markupSaved();
            }*/

            /*if (Rxcore_GUI_exportComplete != undefined){
                Rxcore_GUI_exportComplete.loadComplete(PDFFileUrl);
            }*/
    


            
        }).catch(function(error){

            if(error.status){

                if(error.status == 404){
                    console.log("Get Markup failed, error 404");            
                }else if(error.status == 503){
                    console.log("Get Markup failed, error 503");            
                }else{
                    console.log(error.status);
                }
                
            }else{
        
                console.log(error);
        
            }
        

        });
    

    }

    function RxCore_MarkupSaveJSON(DestFolder, OutputName){

        if(docpagNotReady()){
            return;
        }
        
        if(CompareFilesServer == undefined){
            return;
        }


        var markupxml = createxmlmarkup(true, false,false,signature, DisplayName);
        var markupxmltext = new XMLSerializer().serializeToString(markupxml);
        var markuphex64 = window.btoa(markupxmltext);


        var JSONSaveMarkupFile = [{
            Command : 'SaveMarkup',
            LicenseID : LicenseID,
            OutputName : OutputName,
            DestFolder : DestFolder,
            MarkupData : markuphex64
        }];


        var jsonstring = JSON.stringify(JSONSaveMarkupFile);

        sendHttpRequest('POST', CompareFilesServer, '', jsonstring).then(function (response){

            
            if (RxCore_GUI_MarkupSave != undefined){
                RxCore_GUI_MarkupSave.markupSaved();
            }

            /*if (Rxcore_GUI_exportComplete != undefined){
                Rxcore_GUI_exportComplete.loadComplete(PDFFileUrl);
            }*/
    


            
        }).catch(function(error){

            if(error.status){

                if(error.status == 404){
                    console.log("Markup save failed, error 404");            
                }else if(error.status == 503){
                    console.log("Markup save failed, error 503");            
                }else{
                    console.log(error.status);
                }
                
            }else{
        
                console.log(error);
        
            }
        

        });

        /*[
            {
                 "Command": "SaveMarkup",
                 "LicenseID": "6",
                 "OutputName" : "test.rxml",
                 "DestFolder": "C:\\\\Rasterex\\\\Upload\\\\",
                 "MarkupData": "PD94bWwgdmVyc2lvbj0iMS4wIiBlbmNvZGluZz0iVVRGLTgiPz48UmFzdGVyZXhNYXJrdXA+CjxIZWFkZXI"
            }
         ]*/


 
         
    }

    function RxCore_MarkupGetJSON(unselect){

        if(docpagNotReady()){
            return;
        }
        if (readonlymode || documentcompare) {
            return;
        }

        if(!unselect) {
            unselelectallmarkup();
            unselecteditmarkupall();
        }

        if(DocObj.bLargePDF){
            getMarkupJSONEx(signature, DisplayName);    
        }else{
            getMarkupJSON(signature, DisplayName);
        }

    }

    function RxCore_MarkupSave(unselect) {
        if(docpagNotReady()){
            return;
        }
        if (readonlymode || documentcompare) {
            return;
        }
        if (DocObj.getCurrentpage().usevector3Dxml) {


            if(!unselect) {
                unselelectallmarkup();
                unselecteditmarkupall();
            }

            sendMarkup(xmlurlmarkupsave, signature, DisplayName);

            //send3DMarkup(xmlurlmarkupsave, signature, DisplayName);
        }else{
            if(!unselect) {
                unselelectallmarkup();
                unselecteditmarkupall();
            }

            if(DocObj.bLargePDF){
                sendMarkupEx(xmlurlmarkupsave, signature, DisplayName);    
            }else{
                sendMarkup(xmlurlmarkupsave, signature, DisplayName);
            }
    
        }

    }

    function RxCore_canvToImage() {
        if(docpagNotReady()){
            return;
        }
        unselelectallmarkup();
        unselecteditmarkupall();

        if (DocObj.getCurrentpage().usevector3Dxml) {
            post3DImageData();
        }

    }

    function RxCore_ExportFilePrintPDF(paperSize){

        if(docpagNotReady()){
            return;
        }
        unselelectallmarkup();
        unselecteditmarkupall();


        var params = {
            consolidateonly : false,
            format : 'PDF',
            UPI : "0",
            paperSize : paperSize,
            markupFlag : "1",
            useRaster : false
        }

        exportFileSendPrintPDF(params);

    }

    function RxCore_ExportFilePrint(paperSize){
        if(docpagNotReady()){
            return;
        }

        unselelectallmarkup();
        unselecteditmarkupall();

        exportFilePrint(paperSize);

        //exportFile(consolidate,format,UPI,paperSize,markupFlag);

    }    

    function RxCore_ExportFilePrintRect(paperSize, rect){
        if(docpagNotReady()){
            return;
        }
        unselelectallmarkup();
        unselecteditmarkupall();

        exportFilePrint(paperSize, rect);


        //exportFile(consolidate,format,UPI,paperSize,markupFlag);

    }

    function RxCore_exportSettings(settings){

        bexportmarkupincludeAll = settings.includeAllMarkup;

    }

    function RxCore_ExportFilePage(consolidate, pagenum, format,UPI,paperSize,markupFlag){


        //RxCore.exportFilePage(false,"2","PDF","0","A3","1");

        if (!documentopen) {
            return;
        }
        unselelectallmarkup();
        unselecteditmarkupall();
        exportFilepage(consolidate,pagenum, format,UPI,paperSize,markupFlag);

        
    }

    function RxCore_setDefultExportparams(consolidate,format,UPI,paperSize,markupFlag){

        /*var defaultexportparams = {
            consolidateonly : false,
            format : "PDF",
            UPI : "0",
            paperSize : "A3",
            markupFlag : "1",
        };*/

        defaultexportparams.consolidateonly = consolidate;
        defaultexportparams.format = format;
        defaultexportparams.UPI = UPI;
        defaultexportparams.paperSize = paperSize;
        defaultexportparams.markupFlag = markupFlag;
        



    };


    function RxCore_ExportFile(consolidate,format,UPI,paperSize,markupFlag ){
        if(docpagNotReady()){
            return;
        }
        unselelectallmarkup();
        unselecteditmarkupall();

        if(DocObj.bLargePDF){
            //sendMarkupEx(xmlurlmarkupsave, signature, DisplayName);    
            exportFileEx(consolidate,format,UPI,paperSize,markupFlag, signature, DisplayName);

        }else{
        
            exportFile(consolidate,format,UPI,paperSize,markupFlag);
             //sendMarkup(xmlurlmarkupsave, signature, DisplayName);
        }
        

    }

    function RxCore_ExportFileRaster(consolidate,format,UPI,paperSize,markupFlag){
        if(docpagNotReady()){
            return;
        }
        unselelectallmarkup();
        unselecteditmarkupall();
        exportFileRaster(consolidate,format,UPI,paperSize,markupFlag);


    }

    function RxCore_PDFExportBinary(){
        if(docpagNotReady()){
            return;
        }
        
        if (DocObj.getCurrentpage().usepdfjs){
            DocObj.sendPDFData();
        }

    }
    function RxCore_PDFExport() {
        if(docpagNotReady()){
            return;
        }
        unselelectallmarkup();
        unselecteditmarkupall();
        PDExportnew();

    }

    function RxCore_moveLabelEnable(onOff){
        if(tool.name == 'markupedit'){
            tool.enableMoveLabel(onOff);
        }

    }

    function RxCore_InsertPoint(){
    
        if(tool.name == 'markupedit'){
            tool.enablePointInsert();
        }

    }

    function RxCore_DeletePoint(){
        
        if(tool.name == 'markupedit'){
            tool.enablePointDelete();
        }

    }

    function RxCore_removeLastPoint(){

        if(tool.multipoint && tool.started){
            tool.removelastPoint();
        }

    }

    function RxCore_ApplyMarkupSegment(){

        if(tool.anglelengthsupport && tool.started){
            tool.apply();
        }
            
    }

    function RxCore_ApplyMarkup(){
        
        if(tool.anglelengthsupport && tool.started){
            tool.apply();
        }

    }

    function RxCore_applyRadiusSelected(radius){
        RxCore_applyAngleLengthSelected(0,radius);
    }



    function RxCore_applyWidthHeightSelected(w,h){
        
        var markupobject = getSelectedmarkup();
        if (!markupobject.isempty){
            if(markupobject.widthHeightsupport){
                var curx2 = markupobject.w;
                var cury2 = markupobject.h;
                
                var secondpoint = markupobject.setWidthHeightEdit(w,h);
                var abspoint = {x : markupobject.x + secondpoint.w, y:markupobject.y + secondpoint.h};
                context.clearRect(0, 0, canvas.width, canvas.height);
                var bWithin = (MousePosdrwext(abspoint));
                if (bWithin){
                    markupobject.w = secondpoint.w;
                    markupobject.h = secondpoint.h;

                    drawmarkupAll(cntximg);
                    DrawMarkupSelected(context);

                    markupobject.lengthangleCallbackSelect();                    
                }else{
                    drawmarkupAll(cntximg);
                    DrawMarkupSelected(context);

                    if (RxCore_GUI_markupParamsError != undefined){
                        RxCore_GUI_markupParamsError.onDrawError('outside');
                    }

                }


            }
        }

    }


    function RxCore_applyAngleLengthSelected(angle, length, bAdvance, type){
        var markupobject = getSelectedmarkup();
        if (!markupobject.isempty){

            if(markupobject.anglelengthsupport || markupobject.setRadiusSupport){
                var curx2 = markupobject.w;
                var cury2 = markupobject.h;

                var curx1 = markupobject.x;
                var cury1 = markupobject.y;

                var secondpoint = markupobject.setAngleLenghtEdit(length, angle, type);
                context.clearRect(0, 0, canvas.width, canvas.height);

                var bCircle = (markupobject.type == 4 && markupobject.subtype == 1);
                
                if (bCircle){
                    var bWithin = (MousePosradiusdrwext({x:markupobject.x,y:markupobject.y},{x:secondpoint.w,y:secondpoint.h}));
                }else{
                    bWithin = (MousePosdrwext({x:secondpoint.w,y:secondpoint.h}));
                }

                if (bWithin){
                    
                    markupobject.w = secondpoint.w;
                    markupobject.h = secondpoint.h;
                    //markupobject.AdjustForRotation(true);
                    //markupobject.x = curx1;
                    //markupobject.y = cury1;

                    drawmarkupAll(cntximg);
                    DrawMarkupSelected(context);

                    markupobject.lengthangleCallbackSelect();

    

                }else{
                    //markupobject.w = curx2;
                    //markupobject.h = cury2;

                    drawmarkupAll(cntximg);
                    DrawMarkupSelected(context);

                    if (RxCore_GUI_markupParamsError != undefined){
                        RxCore_GUI_markupParamsError.onDrawError('outside');
                    }

                }                
                
            }else if (markupobject.setPointSupport){
                var secondpoint = markupobject.setAngleLengthPointsEdit(length, angle, type);
                var bpolyline = (markupobject.type == 1 && (markupobject.subtype == 1 || markupobject.subtype == 3));
                context.clearRect(0, 0, canvas.width, canvas.height);

                if (MousePosdrwext({x:secondpoint.w,y:secondpoint.h})){
                    if (bAdvance){
                        markupobject.movepoint(secondpoint.w, secondpoint.h, secondpoint.p);
                    }else{
                        if(secondpoint.p == 0 && !bpolyline){
                            markupobject.setCurPoint(markupobject.points.length - 1); 
                        }else{
                            markupobject.setCurPoint(secondpoint.p - 1); 
                        }
                        
                        markupobject.movepoint(secondpoint.w, secondpoint.h, secondpoint.p);

                    }
                    
                    markupobject.lengthangleCallbackPointSelect();
                    
                    //markupobject.

                    drawmarkupAll(cntximg);
                    DrawMarkupSelected(context);

                }else{
                    drawmarkupAll(cntximg);
                    DrawMarkupSelected(context);

                    if (RxCore_GUI_markupParamsError != undefined){
                        RxCore_GUI_markupParamsError.onDrawError('outside');
                    }

                }
                markupobject.findrectangle();
            }
    
        }

    }


    function RxCore_applyWidthHeight(w,h){
        if(tool.anglelengthsupport && tool.started){
            tool.setWidthHeight(w, h);
        }

    }


    function RxCore_restoreMouseInput(){
        if(tool.anglelengthsupport && tool.started && tool.name == 'area'){
            tool.useMouseInput();
        }
    }

    function RxCore_applyAngleLength(angle, length, bAdvance , type){
       
        if(tool.anglelengthsupport && tool.started){
            tool.setAngleLength(angle, length, bAdvance,  type);

        }

    }

    function pencilcheck(){
        if(tool.name == 'pencil' && tool.started){
            tool.apply();
        }

    }

    function RxCore_CompareScrollZoom(onOff){

        bScrollZoomOverlay = onOff;
    }

    function RxCore_RestrictPan(onOff){

        bRestrictPan = onOff;
    }

    function RxCore_RedactRect(selected) {
        
        
        if(docpagNotReady()){
            return;
        }

        if (readonlymode || documentcompare) {
            return;
        }
        unselelectallmarkup();
        unselecteditmarkupall();
        if (selected) {
            set_tool('rect', {p1 : 3, p2 : 4, redact : true});
        } else {
            
            set_tool('markupedit', {p1 : false});
        }
    }

    function RxCore_MarkupHighlight(selected) {
        if(docpagNotReady()){
            return;
        }
        if (readonlymode || documentcompare) {
            return;
        }
        unselelectallmarkup();
        unselecteditmarkupall();
        if (selected) {
            set_tool('rect', {p1 : 3, p2 : 3, redact : false});
            //set_tool('marker', {});
            //tool = new tools['marker']();
        } else {
            
            set_tool('markupedit', {p1 : false});
            
        }

    }

    const setimageButton = function (target, source, markupobj){
        const loadpromise = new Promise(function(resolve,reject){
            

            target.onload = function(ev){
                
                markupobj.savemetolistDraw();

                img_update();
                drawmarkupAll(cntximg);

                resolve(markupobj);
                
                /*if (this.status == 200) {
                    resolve(xhr.response);
                }else{
                    reject(this);
                }*/
            };

            target.onerror = function(ev){
                
                reject();

            };


            target.src = source.src;

        });

        return loadpromise;

    }


    function RxCore_markupButtonFromMatch(rect, pagenum, dropdata, padding){


        //function RxCore_createImageMarkup(dropdata, x, y){
        if(docpagNotReady()){
            return;
        }

        if (readonlymode || documentcompare) {
            return;
        }

        unselelectallmarkup();
        unselecteditmarkupall();


        /*var mydragobject= {
            src: imageobject.src,
            width: 100,
            height: 100,
            id: ev.target.id,
        };*/      

        
        var imagemarkupobj = new MarkupObject(11, 3, 0);

        imagemarkupobj.scaling = getCurrentPageScale();
        var offset = getCurrentPageOffset(pagenum);
        imagemarkupobj.xoffset = offset.x;
        imagemarkupobj.yoffset = offset.y;

        //imagemarkupobj.printscale = dropdata.scale;
        
        imagemarkupobj.pagenumber = pagenum;
        imagemarkupobj.seteditaction(0);
        //var bWithin = MousePosdrwext(mousePos);
        
        //imagemarkupobj.x = (canvasowidth * 0.5) - (dropdata.width/2);
        //imagemarkupobj.y = (canvasoheight * 0.5) - (dropdata.height/2);

        imagemarkupobj.x = rect.x - padding.left;
        imagemarkupobj.y = rect.y - padding.top;

        
        imagemarkupobj.w = dropdata.width;
        imagemarkupobj.h = dropdata.height;
        //var dropdata = ev.dataTransfer.getData("Text");

        imagemarkupobj.image = new Image();
        //createimage.src = ev.dataTransfer.getData("Text");
        

        //imagemarkupobj.image.onload
        //imagemarkupobj.image.src = dropdata.src;
        
        //imagemarkupobj.image = createimage;

        //imagemarkupobj.imageloaded = true;

        context.clearRect(0, 0, canvas.width, canvas.height);

        setSmoothingEnabledEx(false, cntximg);

        /*if (!bMultimarkupadd) {
            bMarkupcrated = true;
            nMarkupcreated = DocObj.markuplist.length;
        }*/

        bMarkupcrated = true;
        nMarkupcreated = DocObj.markuplist.length;

        imagemarkupobj.savemetolistDraw();

        var returnpromise = setimageButton(imagemarkupobj.image, dropdata, DocObj.markuplist[nMarkupcreated]);
        
        imagemarkupobj = null;

        /*if (!bMultimarkupadd) {
            
            if (RxCore_GUI_Markup != undefined) {
            
                var operation = {created : true, modified : false, deleted : false};
                RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[nMarkupcreated], operation);
            }

        }*/

        return returnpromise;


    }

    function RxCore_MarkupButton(selected){

        if(docpagNotReady()){
            return;
        }

        if (readonlymode || documentcompare) {
            return;
        }

        unselelectallmarkup();
        unselecteditmarkupall();

        if (selected) {
            set_tool('symbol', {p1 : 3});
            
        }else{
            
            set_tool('markupedit', {p1 : false});
            
        }

        
    }   
    
    function Rxcore_AddSignature(selected){
        if(docpagNotReady()){
            return;
        }
        if (readonlymode || documentcompare) {
            return;
        }
        if (DocObj.getCurrentpage().usevector3Dxml) {
            var params = {p1 : 'drag'};
            set3DToolType(selected, 'add3dsymbol',params);

        }else{
            unselelectallmarkup();
            unselecteditmarkupall();
    
            if (selected) {
                set_tool('symbol', {p1: 4});
                //tool = new tools['symbol']();
    
            }else{
                
                set_tool('markupedit', {p1 : false});
                
            }
    
        }


    }


    function Rxcore_MarkupImageStamp(selected){
        if(docpagNotReady()){
            return;
        }
        if (readonlymode || documentcompare) {
            return;
        }
        if (DocObj.getCurrentpage().usevector3Dxml) {
            var params = {p1 : 'drag'};
            set3DToolType(selected, 'add3dsymbol',params);

        }else{
            unselelectallmarkup();
            unselecteditmarkupall();
    
            if (selected) {
                set_tool('symbol', {p1: 12});
                //tool = new tools['symbol']();
    
            }else{
                
                set_tool('markupedit', {p1 : false});
                
            }
    
        }


    }


    function Rxcore_MarkupSymbol(selected){
        if(docpagNotReady()){
            return;
        }
        if (readonlymode || documentcompare) {
            return;
        }
        if (DocObj.getCurrentpage().usevector3Dxml) {
            var params = {p1 : 'drag'};
            set3DToolType(selected, 'add3dsymbol',params);

        }else{
            unselelectallmarkup();
            unselecteditmarkupall();
    
            if (selected) {
                set_tool('symbol', {p1: 1});
                //tool = new tools['symbol']();
    
            }else{
                
                set_tool('markupedit', {p1 : false});
                
            }
    
        }


    }
    function Rxcore_MarkupmarkupinteractiveStamp(selected){
        if(docpagNotReady()){
            return;
        }
        if (readonlymode || documentcompare) {
            return;
        }
        if (DocObj.getCurrentpage().usevector3Dxml) {
            var params = {p1 : 'drag'};
            set3DToolType(selected, 'add3dsymbol',params);

        }else{
            unselelectallmarkup();
            unselecteditmarkupall();
    
            if (selected) {
                set_tool('interactiveStamp', {p1: 21});
                //tool = new tools['symbol']();
    
            }else{
                
                set_tool('markupedit', {p1 : false});
                
            }
    
        }


    }    

    function Rxcore_MarkupLink(selected){
        
        if(docpagNotReady()){
            return;
        }
        if (readonlymode || documentcompare) {
            return;
        }
        if (DocObj.getCurrentpage().usevector3Dxml) {
            var params = {p1 : 'drag'};
            set3DToolType(selected, 'add3dsymbol',params);

        }else{
            unselelectallmarkup();
            unselecteditmarkupall();
    
            if (selected) {
                set_tool('link', {p1: 20});
                //tool = new tools['symbol']();
    
            }else{
                
                set_tool('markupedit', {p1 : false});
                
            }
    
        }


    }


    function RxCore_MarkupLinkRect(bmarker, rect, pagenum, link, padding){
        if(docpagNotReady()){
            return;
        }
        if (readonlymode || documentcompare) {
            return;
        }
        
        unselelectallmarkup();
        unselecteditmarkupall();

        var type = 3;
        var subtype = 0;
        var fillstyle = 0;
           
        if(bmarker){
           subtype = 3;
        } 
       
        var rectmarkupobj = new MarkupObject(type, subtype, fillstyle);

       if(bmarker){
            rectmarkupobj.transparency = 30;
            rectmarkupobj.fillcolor = setTransp(rectmarkupobj.fillcolor,rectmarkupobj.transparency);
       }

       rectmarkupobj.scaling = getCurrentPageScale();
       var offset = getCurrentPageOffset(pagenum);
       rectmarkupobj.xoffset = offset.x;
       rectmarkupobj.yoffset = offset.y;
       rectmarkupobj.pagenumber = pagenum;

       rectmarkupobj.viewname = getCurrentViewName();
        
       if(rectmarkupobj.viewname != null){
           rectmarkupobj.bfor3D = true;
           rectmarkupobj.pageheight = canvasoheight;
           rectmarkupobj.pagewidth = canvasowidth;
       }



        if (padding){

            rectmarkupobj.x = rect.x - padding.left;
            rectmarkupobj.y = rect.y - padding.top;
    
            rectmarkupobj.w = rect.w + padding.right;
            rectmarkupobj.h = rect.h + padding.bottom;
    
        }else{
            rectmarkupobj.x = rect.x;
            rectmarkupobj.y = rect.y;
    
            rectmarkupobj.w = rect.w;
            rectmarkupobj.h = rect.h;
    
        }

        rectmarkupobj.savemetolistDraw();

        if(link && link != ""){
            rectmarkupobj.setLink(link, true);
        }
        
        img_update();
        drawmarkupAll(cntximg);

        rectmarkupobj = null;

    } 

    function RxCore_MarkupShape(selected, type, subtype) {
        if(docpagNotReady()){
            return;
        }
        if (readonlymode || documentcompare) {
            return;
        }
        unselelectallmarkup();
        unselecteditmarkupall();
        if (selected) {

            switch (type) {
                case 0:
                    set_tool('rect', {p1 : 3, p2 : subtype,redact : false});
                    //tool = new tools['rect'](subtype);
                    break;
                case 1:
                    //set_tool('oval', {p1 : subtype});
                    set_tool('rect', {p1 : 4, p2 : subtype, redact : false});    
                
                    //tool = new tools['oval']();
                    break;
                case 2:
                    set_tool('rect', {p1 : 5, p2 : subtype, redact : false});

                    //set_tool('cloud', {});
                    //tool = new tools['cloud']();
                    break;
                case 3:
                    //set_tool('polygon', {});
                    set_tool('area', {p1 : 1, p2 : 2});
                    //tool = new tools['polygon']();
                    break;
            }


        } else {
            
            if (DocObj.getCurrentpage().usevector3Dxml) {
                
                switch (DocObj.curcontrol3D) {
                    case 'Walkthrough3D' : 
                        RxCore_3DWalkThrough(true);
                        break;
                    case 'orbitControl' :
                        RxCore_3DOrbit(true);
                        break;
                    case 'markupedit' :
                        set_tool('markupedit', {p1 : false});
                        break;


                }
                //DocObj.getCurrentpage().select3Dmarkup(id);

            }else{
                set_tool('markupedit', {p1 : false});
            }

        }

    }

    function RxCore_MarkupLine(selected){
        if(docpagNotReady()){
            return;
        }
        if (readonlymode || documentcompare) {
            return;
        }
        unselelectallmarkup();
        unselecteditmarkupall();

        if (selected) {
            set_tool('arrow', {p1 : 9});
            //tool = new tools['arrow'](type);

        } else {
            
            set_tool('markupedit', {p1 : false});
            
        }


    }

    function RxCore_MarkupCounter(selected, shape){
        if(docpagNotReady()){
            return;
        }
        if (readonlymode || documentcompare) {
            return;
        }
        
        unselelectallmarkup();
        unselecteditmarkupall();

        if (selected) {

            if(tool.name == 'count' && tool.started){
                tool.apply();
            }


            set_tool('count', {p1 : 13, p2 : shape});
            //tool = new tools['arrow'](type);

        } else {
            if(tool.name == 'count' && tool.started){
                tool.apply();
            }

            set_tool('markupedit', {p1 : false});
            
        }

    }

    function RxCore_MarkupCircle(selected){
        if(docpagNotReady()){
            return;
        }
        if (readonlymode || documentcompare) {
            return;
        }
        
        unselelectallmarkup();
        unselecteditmarkupall();

        if (selected) {
            set_tool('arrow', {p1 : 10});
            //tool = new tools['arrow'](type);

        } else {
            
            set_tool('markupedit', {p1 : false});
            
        }

    }

    function RxCore_MeasureArc(selected){

        if(docpagNotReady()){
            return;
        }
        if (readonlymode || documentcompare) {
            return;
        }
        unselelectallmarkup();
        unselecteditmarkupall();

        //'measurearc'

        if (selected) {
            set_tool('measurearc', {p1 : 0});
            //tool = new tools['arrow'](type);

        } else {
            
            set_tool('markupedit', {p1 : false});
            
        }

        
    }

    function RxCore_MarkupArrow(selected, type) {
        if(docpagNotReady()){
            return;
        }
        if (readonlymode || documentcompare) {
            return;
        }
        unselelectallmarkup();
        unselecteditmarkupall();
        if (selected) {
            set_tool('arrow', {p1 : type});
            //tool = new tools['arrow'](type);

        } else {
            
            set_tool('markupedit', {p1 : false});
            
        }

    }

    function RxCore_markupAreaRect(selected){
        if(docpagNotReady()){
            return;
        }
        if (readonlymode || documentcompare) {
            return;
        }
        unselelectallmarkup();
        unselecteditmarkupall();

        if (selected) {
            
            set_tool('measure', {p1 : 3, p2 : 6});
            
        } else {
            set_tool('markupedit', {p1 : false});
        }        

    }

    function RxCore_markupRectToAreaSwitch(markup){
        if(docpagNotReady()){
            return;
        }
        if (readonlymode || documentcompare) {
            return;
        }
        //unselelectallmarkup();
        //unselecteditmarkupall();
        //RxCore_MarkupSelect(true);
        RxCore_DeleteMarkup();
        startAreaTool(markup);        
    }    

    function startAreaTool(markup) {                
        var type = 8;
        var subtype = 0;
        var rotatedrect = markup.rotatedrect;
        var mousePos = { 
            x:rotatedrect.x,
            y:rotatedrect.y
        };
 
        var areamarkupobj = new MarkupObject(type, subtype, fillstyle);
        areamarkupobj.pagenumber = DocObj.getcurPage();
        areamarkupobj.viewname = DocObj.getcur3DView();
 
        if(areamarkupobj.viewname != null){
            areamarkupobj.bfor3D = true;
            areamarkupobj.pageheight = canvasoheight;
            areamarkupobj.pagewidth = canvasowidth;
        }
        var scaleoffset = {scale : undefined, xoffset : undefined, yoffset : undefined};
        scaleoffset.scale = getCurrentPageScale();
        var offset = getCurrentPageOffset();
        scaleoffset.xoffset = offset.x;
        scaleoffset.yoffset = offset.y;
 
        areamarkupobj.scaling = scaleoffset.scale;
        areamarkupobj.xoffset = scaleoffset.xoffset;
        areamarkupobj.yoffset = scaleoffset.yoffset;
        areamarkupobj.x = mousePos.x;
        areamarkupobj.y = mousePos.y;
        areamarkupobj.seteditaction(0);
        if(DocObj){
            DocObj.bMarkupchanged = true;
        }                    
        completeRectToArea(markup, areamarkupobj);
    }

    function completeRectToArea(markup, areamarkupobj) {
 
        if (areamarkupobj == null){
            return;
        }            
        let rotatedrect = markup.rotatedrect;
        let points = [{x: rotatedrect.x, y: rotatedrect.y},
            {x: rotatedrect.x+rotatedrect.w, y: rotatedrect.y},
            {x: rotatedrect.x+rotatedrect.w, y: rotatedrect.y+rotatedrect.h},
            {x: rotatedrect.x, y: rotatedrect.y+rotatedrect.h}];
        areamarkupobj.points = points;
        areamarkupobj.findrectangle();
        if (!bMultimarkupadd) {
            bMarkupcrated = true;                
        }
 
        nMarkupcreated = DocObj.markuplist.length;
 
        areamarkupobj.savemetolistDraw();
        var bsaved = areamarkupobj.saved;
        areamarkupobj = null;
        img_update();
        drawmarkupAll(cntximg);
 
        RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[nMarkupcreated], {created : true, modified : false, deleted : false});
    }


    function RxCore_MarkupDimension(selected, type) {
        if(docpagNotReady()){
            return;
        }
        if (readonlymode || documentcompare) {
            return;
        }
        unselelectallmarkup();
        unselecteditmarkupall();
        if (selected) {
            
            if (DocObj.getCurrentpage().usevector3Dxml) {
                set3DToolType(selected, 'measure3D', null);
            }else{
                set_tool('measure', {p1 : 7, p2 : type});
                //set_tool('measure', {p1 : type});
            }

            //tool = new tools['measure'](type);
        } else {

            if (DocObj.getCurrentpage().usevector3Dxml) {
                set3DToolType(selected, 'measure3D', null);
            }else{
                set_tool('markupedit', {p1 : false});
            }
            
        }

    }

    function RxCore_MarkupSpace(selected){
        if(docpagNotReady()){
            return;
        }
        if (readonlymode || documentcompare) {
            return;
        }
        unselelectallmarkup();
        unselecteditmarkupall();
        if (selected) {
            set_tool('area', {p1 : 8, p2 : 1});
            //tool = new tools['area']();
        } else {
            
            set_tool('markupedit', {p1 : false});
        }
    }

    function RxCore_setSpaceID(idvalue){

        var szvalue = idvalue.toString();
        SpaceidAttribute('SpaceID', szvalue);

    }

    function RxCore_MarkupAreaHole(selected){
        if(docpagNotReady()){
            return;
        }
        if (readonlymode || documentcompare) {
            return;
        }

        var markupobject = getSelectedmarkup();

        if(markupobject.isempty || markupobject.type != 8){
            set_tool('markupedit', {p1 : false});

            unselelectallmarkup();
            unselecteditmarkupall();
            return;

        }

        unselelectallmarkup();
        unselecteditmarkupall();


        if (selected) {
            set_tool('area', {p1 : 8, p2 : 2, p3 : markupobject});
            //currentParentMarkupNumber = parentmarkupnumber;
            //tool = new tools['area']();
        } else {
            
            set_tool('markupedit', {p1 : false});
            
        }



    }

    function RxCore_MarkupArea(selected, parentmarkupnumber) {
        if(docpagNotReady()){
            return;
        }
        if (readonlymode || documentcompare) {
            return;
        }
        unselelectallmarkup();
        unselecteditmarkupall();
        if (selected) {
            set_tool('area', {p1 : 8, p2 : 0});
            currentParentMarkupNumber = parentmarkupnumber;
            //tool = new tools['area']();
        } else {
            
            set_tool('markupedit', {p1 : false});
            
        }

    }

    function RxCore_MarkupMeasureSegment(selected){
        if(docpagNotReady()){
            return;
        }
        if (readonlymode || documentcompare) {
            return;
        }
        unselelectallmarkup();
        unselecteditmarkupall();
        if (selected) {
            //set_tool('polyline', {p1 : 3});
            set_tool('area', {p1 : 1, p2 : 6});
            //tool = new tools['polyline'](3);
        } else {
            if(tool.subtype == 6 && tool.started){
                tool.apply();
            }

            set_tool('markupedit', {p1 : false});
            
        }


    }


    function RxCore_MarkupMeasurePath(selected){
        if(docpagNotReady()){
            return;
        }
        if (readonlymode || documentcompare) {
            return;
        }
        unselelectallmarkup();
        unselecteditmarkupall();
        if (selected) {
            //set_tool('polyline', {p1 : 3});
            set_tool('area', {p1 : 1, p2 : 3});
            //tool = new tools['polyline'](3);
        } else {
            
            set_tool('markupedit', {p1 : false});
            
        }


    }

    function RxCore_MarkupAngle(selected, bcounterclock){
        if(docpagNotReady()){
            return;
        }
        if (readonlymode || documentcompare) {
            return;
        }
        unselelectallmarkup();
        unselecteditmarkupall();

        if (selected) {
            //set_tool('polyline', {p1 : 3});
            if(bcounterclock){
                set_tool('area', {p1 : 1, p2 : 5});
            }else{
                set_tool('area', {p1 : 1, p2 : 4});
            }
            
            //tool = new tools['polyline'](3);
        } else {
            
            set_tool('markupedit', {p1 : false});
            
        }
    }

    function RxCore_MarkuprevCloud(selected){
        if(docpagNotReady()){
            return;
        }
        if (readonlymode || documentcompare) {
            return;
        }
        unselelectallmarkup();
        unselecteditmarkupall();
        if (selected) {
            //set_tool('polyline', {p1 : 1});
            set_tool('area', {p1 : 1, p2 : 7});
            //tool = new tools['polyline'](1);
        } else {
            
            set_tool('markupedit', {p1 : false});
            
        }

    }

    function RxCore_MarkupPolyline(selected) {
        if(docpagNotReady()){
            return;
        }
        if (readonlymode || documentcompare) {
            return;
        }
        unselelectallmarkup();
        unselecteditmarkupall();
        if (selected) {
            //set_tool('polyline', {p1 : 1});
            set_tool('area', {p1 : 1, p2 : 1});
            //tool = new tools['polyline'](1);
        } else {
            
            set_tool('markupedit', {p1 : false});
            
        }

    }

    function RxCore_MarkupPolycurve(selected) {
        if(docpagNotReady()){
            return;
        }
        if (readonlymode || documentcompare) {
            return;
        }
        unselelectallmarkup();
        unselecteditmarkupall();
        if (selected) {
            set_tool('Polycurve', {p1 : 1});
            //tool = new tools['Polycurve'](1);
        } else {
            
            set_tool('markupedit', {p1 : false});
            
        }

    }

    function RxCore_MarkupFreepen(selected) {
        if(docpagNotReady()){
            return;
        }
        if (readonlymode || documentcompare) {
            return;
        }
        unselelectallmarkup();
        unselecteditmarkupall();

        if (selected) {
            set_tool('pencil', {p1 : 0});
            //tool = new tools['pencil'](0);
        } else {
            if(tool.name == 'pencil' && tool.started){
                tool.apply();
            }
            
            set_tool('markupedit', {p1 : false});
            
        }

    }


    function RxCore_Calibrate(selected) {
        if(docpagNotReady()){
            return;
        }
        unselelectallmarkup();
        unselecteditmarkupall();
        if (selected) {
            DocObj.getCurrentpage().enableSnap(true);
            set_tool('calibrate', {});
            //tool = new tools['calibrate']();
        } else {
            DocObj.getCurrentpage().enableSnap(true);
            
            set_tool('markupedit', {p1 : false});
            
        }

    }


    function Rxcore_setLargePDFpageLimit(num){

        nLargePDF = num;
    }

    function RxCore_setRotateOffsetbirdseye(x,y){
     
        
        mbirdseyeoffsetx = x;
        mbirdseyeoffsety = y;



    }


    function RxCore_setLabelSizeAll(num){
        var tempsize = nLabelTextsize;
        nLabelTextsize = num;

        if(docpagNotReady()){
            return;
        }
        if (readonlymode) {
            return;
        }

        ChangeMarkupSelected(11, true);

        if (!bSetGlobal) {
            nLabelTextsize = tempsize;
        }                
        
    }

    function RxCore_setCountSizeAll(num){

        var tempsize = nCounterSize;
        nCounterSize = num;

        if(docpagNotReady()){
            return;
        }
        if (readonlymode) {
            return;
        }

        ChangeMarkupSelected(13, true);

        if (!bSetGlobal) {
            nCounterSize = tempsize;
        }                

        
    }    

    function RxCore_setCountSize(num){

        var tempsize = nCounterSize;

        nCounterSize = num;
        if(docpagNotReady()){
            return;
        }
        if (readonlymode) {
            return;
        }

        ChangeMarkupSelected(13);

        if (!bSetGlobal) {
            nCounterSize = tempsize;
        }                

        
    }

    function RxCore_setCameraTumbWidth(num){

        nCamerathumbwidth = num;
    }


    function RxCore_setLabelSize(num){
        var tempsize = nLabelTextsize;
        nLabelTextsize = num;
        if(docpagNotReady()){
            return;
        }
        if (readonlymode) {
            return;
        }

        ChangeMarkupSelected(11);
        if (!bSetGlobal) {
            nLabelTextsize = tempsize;
        }                


    }
    function RxCore_setArrowSize(num){

        var tempsize = nArrowSize;
        nArrowSize = num;
        if(docpagNotReady()){
            return;
        }
        if (readonlymode) {
            return;
        }

        ChangeMarkupSelected(10);
        if (!bSetGlobal) {
            nArrowSize = tempsize;
        }                
       

    }

    function RxCore_setTextheightFactor(dnum){
        lineheightfactor = dnum;
    }


    function RxCore_animateLineStyle(onoff){

        bAnimateLineStyle = onoff;

        if(enablemarkupLabels){
            bmarkupLabels = !onoff;
        }

        if(!onoff){
            RxCore_setLineStyleSpace(0, true);
        }

    }


    function RxCore_setLineStyleSpace(linestyle, reset){
        if(docpagNotReady()){
            return;
        }
        if (readonlymode) {
            return;
        }

        var curmarkup = 0;

        for (curmarkup = 0; curmarkup < DocObj.markuplist.length; curmarkup++) {
            if (DocObj.markuplist[curmarkup].spaceselected || reset) {
                if(DocObj.markuplist[curmarkup].type == 8 && DocObj.markuplist[curmarkup].subtype == 1){
                    DocObj.markuplist[curmarkup].linestyle = linestyle;
                }


            }
        }        
        
        
        drawmarkupAll(cntximg);
        drawSpaceSelected(context);
        DrawMarkupSelected(context);

    }
    function RxCore_SetLineStyle(linestyle) {
        /*if (!documentopen) {
            return;
        }
        if (readonlymode) {
            return;
        }*/
        
        var tempstyle = nlinestyle;
        nlinestyle = linestyle;
        
        if(docpagNotReady()){
            return;
        }
        if (readonlymode) {
            return;
        }
        
        ChangeMarkupSelected(9);

        if (!bSetGlobal) {
            linewidthvalue = tempstyle;
        }


        /*if (linestyle == 'Ã¢â‚¬â€Ã¢â‚¬â€Ã¢â‚¬â€Ã¢â‚¬â€') {
            nlinestyle = 0;

        } //solid
        if (linestyle == 'Ã¢â‚¬â€œ Ã¢â‚¬â€œ Ã¢â‚¬â€œ Ã¢â‚¬â€œ Ã¢â‚¬â€œ ') {
            nlinestyle = 1;
        } //dashed
        if (linestyle == 'Ã‚Â· Ã‚Â· Ã‚Â· Ã‚Â· Ã‚Â· Ã‚Â· Ã‚Â· ') {
            nlinestyle = 2;
        } //dotted
        if (linestyle == 'Ã¢â‚¬â€œ Ã‚Â· Ã‚Â· Ã¢â‚¬â€œ Ã‚Â· Ã‚Â· ') {
            nlinestyle = 3;
        } //dash dot dot dash
        if (linestyle == 'Ã¢â‚¬â€œ Ã‚Â· Ã¢â‚¬â€œ Ã‚Â· Ã¢â‚¬â€œ Ã‚Â· ') {
            nlinestyle = 4;
        } //dash dot*/

    }


    function RxCore_SetSegmentLineWidthUnits(value){
        
        var tempwidht = linewidthvalueSegment;
        var fixedscale = DocObj.getCurrentpage().fixedScale;

        linewidthvalueSegment = getScreenDim(value);
        linewidthvalueSegment *= fixedscale;

        if(docpagNotReady()){
            return;
        }
        if (readonlymode) {
            return;
        }

        ChangeMarkupSelected(19);

        if (!bSetGlobal) {
            linewidthvalueSegment = tempwidht;
        }


    }

    function RxCore_setSnapSize(size){

        nSnapSize = size;
    
    }

    function RxCore_overrideLinewidth(onoff, thickness){

        bOverridelineWidth = onoff;
        dOveridelinewidth = thickness;
    
    }

    function RxCore_autoHyperlink(onoff){

        bautohyperlink = onoff;
    
    }


    function RxCore_SetLineWidthUnits(value, type){
        

        var tempwidht = linewidthvalue;

        //var scale = DocObj.getCurrentpage().getdscale();
        var fixedscale = DocObj.getCurrentpage().fixedScale;

        //value = (value * (scale / fixedscale)) / (scale / fixedscale);

        linewidthvalue = getScreenDim(value, type);
        linewidthvalue *= fixedscale;

        if(docpagNotReady()){
            return;
        }
        if (readonlymode) {
            return;
        }


        ChangeMarkupSelected(2);

        if (!bSetGlobal) {
            linewidthvalue = tempwidht;
        }


    }

    function RxCore_ScrollFoxitDiv(){
        if(docpagNotReady()){
            return;
        }

        DocObj.ScrollFoxitDiv();

    }

    function RxCore_SetDimOffset(value) {
        if(docpagNotReady()){
            return;
        }

        if(readonlymode){
            return;
        }

        var markupobject = getSelectedmarkup();

        if(!markupobject.isempty && markupobject.type == 7){
            markupobject.leaderoffset = value;
        }

        drawmarkupAll(cntximg);
        DrawMarkupSelected(context);

    }

    function RxCore_setAreaPerimeter(onoff){
        if(docpagNotReady()){
            return;
        }
        if (readonlymode) {
            return;
        }


        if(onoff){
            ChangeMarkupSelected(16);
        }else{
            ChangeMarkupSelected(17);
        }

    }    

    function RxCore_setdisplay(onoff){
        if(docpagNotReady()){
            return;
        }
        if (readonlymode) {
            return;
        }

        ChangeMarkupSelectedDisplay(onoff);
        
    }

    function RxCore_SetLineWidthMarkup(markup, value){

        var tempwidht = linewidthvalue;
        linewidthvalue = value;
        if(docpagNotReady()){
            return;
        }
        if (readonlymode) {
            return;
        }

        ChangeMarkup(markup,2);
        if (!bSetGlobal) {
            linewidthvalue = tempwidht;
        }

    }

    function RxCore_SetLineWidthAll(value){
        var tempwidht = linewidthvalue;
        linewidthvalue = value;

        if(docpagNotReady()){
            return;
        }
        if (readonlymode) {
            return;
        }


        ChangeMarkupSelected(2, true);

        if (!bSetGlobal) {
            linewidthvalue = tempwidht;
        }

    }

    function RxCore_SetLineWidth(value) {

        var tempwidht = linewidthvalue;
        linewidthvalue = value;

        if(docpagNotReady()){
            return;
        }
        if (readonlymode) {
            return;
        }


        ChangeMarkupSelected(2);
        if (!bSetGlobal) {
            linewidthvalue = tempwidht;
        }
    }

    function RxCore_GetLineWidth() {
        return linewidthvalue;
    }

    function RxCore_getUnit(){


        /*if (value=='Metric'){Unitofmeasure=1;}
         if (value=='Imperial'){Unitofmeasure=2;}
         if (value=='System'){Unitofmeasure=3;}
         if (value=='Custom'){Unitofmeasure=4;}*/

        if(docpagNotReady()){
            var uname = 'Metric';

            switch (Unitofmeasure) {
                case 1:
                    uname = 'Metric';

                    break;
                case 2:
                    uname = 'Imperial';
                    break;
                case 3:
                    uname = 'System';
                    break;

            }

            //1 = Millimeter, 2 = Centimeter, 3 = Decimeter, 4=Meter,5=Kilometer,6=Nautical Miles
            //1 = Inch,2=Feet,3=Yard,4=Mile,5=Nautical Miles
    
            var unitobj = {
                unit : Unitofmeasure,
                unitname : uname,
                subunit : SubmeasureUnit,
                label : Unitlabel,
                Arealabel : AreaUnitlabel
            };

            return unitobj;

        }


        return DocObj.getCurrentpage().getUnit(); 

        
    }

    function RxCore_getpageUnit(){

        if(docpagNotReady()){
            return RxCore_getUnit();
        }

        return DocObj.getCurrentpage().getUnit();


        
    }

    function RxCore_getCurrentPageScaleValue(){
        if(docpagNotReady()){
            return;
        }
        
        return DocObj.getCurrentpage().scaleObject.getScaleValue();
    }  

    function RxCore_getCurrentPageScaleLabel(){
        if(docpagNotReady()){
            return;
        }
        return DocObj.getCurrentpage().scaleObject.getScaleLabel();
    } 


    function RxCore_SetUnit(value) {
        /*for (i=0;i<this.childElementCount;i++){
         if(this.children[i].className == 'selected'){
         var szUnit = this.children[i].textContent;
         }
         }*/
        /*if (value=='Metric'){Unitofmeasure=1;}
         if (value=='Imperial'){Unitofmeasure=2;}
         if (value=='System'){Unitofmeasure=3;}
         if (value=='Custom'){Unitofmeasure=4;}*/
        //var unitdropdwn = document.getElementById('unitdropdown');


        if(docpagNotReady()){
            switch (value) {
                case 1:
                    Unitlabel = "mm";
                    AreaUnitlabel = "mm\u00B2";
                    //unitdropdwn.setAttribute('ref','metricunits');
                    //unitdropdwn.children[0].value = "Millimeter";
                    break;
                case 2:
                    Unitlabel = "in.";
                    AreaUnitlabel = "sq.in.";
                    //unitdropdwn.setAttribute('ref','imperialunits');
                    //unitdropdwn.children[0].value = "Inch";
                    break;
                case 3:
                    Unitlabel = "Units";
                    AreaUnitlabel = "Units\u00B2";
                    break;
            }
            Unitofmeasure = value;
            return;
        }


        //DocObj.getCurrentpage().setUnit(value);
        DocObj.getCurrentpage().scaleObject.setUnit(value);


        
    }

    function setDefaultUser(sign, Disp, layer, color){


        /*class Users {
            constructor(Signature, DispName, Layer, Color) {
                this.Signature = Signature;
                this.DisplayName = DispName;
                this.Layer = Layer;
                this.Color = Color;
                this.display = true;
            }
        }*/


        //setDefaultUser(signature, DisplayName, markuplayer, markupcolor);
        var buserexist = false;
        var nuserindex = 0;

        for (var ui = 0; ui < Userlist.length; ui ++){
            if(Userlist[ui].Signature == sign){
                buserexist = true;
                nuserindex = ui;
                Userlist[ui].DisplayName = Disp;
                Userlist[ui].Layer = layer;
                Userlist[ui].Color = color;
                Userlist[ui].default = true;
            }
        }

        if (buserexist){
            //signature = Userlist[nuserindex].Signature;
            //DisplayName = Userlist[nuserindex].DisplayName;
        }else{
            //Userlist[0] = new Users(signature, DisplayName, markuplayer, markupcolor);
            Userlist.push(new Users(sign, Disp, layer, color, true));
            numUsers ++;

        }
    }

    function addCurrentUser(){


        var buserexist = false;
        var nuserindex = 0;

        for (var ui = 0; ui < Userlist.length; ui ++){
            if(Userlist[ui].Signature == signature){
                buserexist = true;
                nuserindex = ui;
                Userlist[ui].DisplayName = DisplayName;
            }
        }

        if (buserexist){
            //signature = Userlist[nuserindex].Signature;
            //DisplayName = Userlist[nuserindex].DisplayName;
        }else{
            //Userlist[0] = new Users(signature, DisplayName, markuplayer, markupcolor);
            Userlist.push(new Users(signature, DisplayName, markuplayer, markupcolor, false));
            numUsers ++;

        }

        //Userlist[0] = new Users(signature, DisplayName, markuplayer, markupcolor, true);
    }

    function clearUsers(){

        var curUserSign = signature;
        var curDisplayName = DisplayName;  
        numUsers = 0;

        for (var ui = 0; ui < Userlist.length; ui ++){
            if(Userlist[ui].default){
                curUserSign = Userlist[ui].Signature;
                curDisplayName = Userlist[ui].DisplayName;
            }
        }
        
        Userlist = [];

        Userlist.push(new Users(curUserSign, curDisplayName, markuplayer, markupcolor, true));
        numUsers = 1;

    }

    function restoreDefaultUser(){

        var curUserSign = signature;
        var curDisplayName = DisplayName;  

        for (var ui = 0; ui < Userlist.length; ui ++){
            if(Userlist[ui].default){
                curUserSign = Userlist[ui].Signature;
                curDisplayName = Userlist[ui].DisplayName;
            }
        }

        signature = curUserSign;
        DisplayName =curDisplayName;

    }

    function RxCore_setUser(sign, disp){


    

        if(bCanChangeSignature){

            unselelectallmarkup();
            unselecteditmarkupall();

            if (sign != "") {
                signature = sign;
            }else{
                restoreDefaultUser();
                return;
            }
            if (disp != "") {
                DisplayName = disp;
            }
            var buserexist = false;
            var nuserindex = 0;


            for (var ui = 0; ui < Userlist.length; ui ++){
                if(Userlist[ui].Signature == sign){
                    buserexist = true;
                    nuserindex = ui;
                }
            }

            if (buserexist){
                signature = Userlist[nuserindex].Signature;
                DisplayName = Userlist[nuserindex].DisplayName;
            }else{
                //Userlist[0] = new Users(signature, DisplayName, markuplayer, markupcolor);
                Userlist.push(new Users(signature, DisplayName, markuplayer, markupcolor, false));

            }

        
            if (RxCore_GUI_Users != undefined) {
                RxCore_GUI_Users.setUserlist(Userlist, true);
            }

        }


    }

    function RxCore_SetElementUnit(value) {
        
        var markup = RxCore_getSelectedMarkup();

        if(!markup.isempty){
            markup.scaleObject.setUnit(value);  
        }

        
    }

    
    function RxCore_setpageUnit(value){

        if(docpagNotReady()){
            RxCore_SetUnit(value);
            return;
        }

        //DocObj.getCurrentpage().setUnit(value);

        DocObj.getCurrentpage().scaleObject.setUnit(value);


    }



    function RxCore_allowLayerChange(onoff){

    }

    function RxCore_getUser(){

        //signature = szCurrentUser;
        //DisplayName = szDisplayName;


        getUserInfo();
        


    }

    function RxCore_setRxResize (onoff){

        if(docpagNotReady()){
            return;
        }

        //setRxResize
        if (DocObj){
            if (DocObj.foxitdoc != null){
                DocObj.foxitdoc.setRxResize(onoff);
            }   
        }


    }

    function RxCore_foxitpagescalecheck(pagescale){

        if(docpagNotReady()){
            return;
        }


        if (DocObj){
            DocObj.foxitpagescalecheck(pagescale);
        }   

    }

    function RxCore_scrollBarCheck(){
        
        if(docpagNotReady()){
            return;
        }

        if (DocObj){
            DocObj.ScrollBarCheck();
        }   

    }

    function RxCore_setEndPoints(szXmlUrlmarkup,szMarkupSaveUrl,szFileExport,sz3DImage,szgetUserInfo){

        xmlurlmarkup = szXmlUrlmarkup;
        xmlurlmarkupsave = szMarkupSaveUrl;
        PDFExportURL = szFileExport;
        CanvasSaveUrl = sz3DImage;
        userInfoURL = szgetUserInfo;
        bPDFExportfullPath = true;

    }

    function RxCore_SetGlobalStyle(global) {
        bSetGlobal = global;
    }

    function RxCore_setFontFull(fontobject){
        
        var tempfont = {name : defaultFont.fontName, size : defaultFont.height, bold : defaultFont.bold, italic : defaultFont.italic };

        defaultFont.setFontname(fontobject.font);
        defaultFont.setHeight(fontobject.size);
        defaultFont.setBold(fontobject.style.bold);
        defaultFont.setItalic(fontobject.style.italic);
        

        if(docpagNotReady()){
            return;
        }
        if (readonlymode) {
            return;
        }

        ChangeMarkupSelected(4);
    
        if (!bSetGlobal) {
            defaultFont.setFontname(tempfont.name);
            defaultFont.setHeight(tempfont.size);
            defaultFont.setBold(tempfont.bold);
            defaultFont.setItalic(tempfont.italic);
    
        }

    }    
    function RxCore_SetFontBold(onoff) {
        var tempbold = defaultFont.bold;
        
        defaultFont.setBold(onoff);

        //defaultFont.bold ? defaultFont.setBold(false) : defaultFont.setBold(true);
        //defaultFont.setBold(onoff);

        if(docpagNotReady()){
            return;
        }
        if (readonlymode) {
            return;
        }

        ChangeMarkupSelected(4);
        if (!bSetGlobal) {
            defaultFont.setBold(tempbold);

        }

    }

    function RxCore_SetFontItalic(onoff) {
        var tempitalic = defaultFont.italic;
        
        defaultFont.setItalic(onoff);
        //defaultFont.italic ? defaultFont.setItalic(false) : defaultFont.setItalic(true);
        //defaultFont.setItalic(onoff);

        if(docpagNotReady()){
            return;
        }
        if (readonlymode) {
            return;
        }

        ChangeMarkupSelected(4);
        if (!bSetGlobal) {
            defaultFont.setItalic(tempitalic);

        }

    }

    function RxCore_SetFontHeight(value) {
		//console.log('RxCore_SetFontHeight', value);
        var tempheight = defaultFont.height;
        defaultFont.setHeight(value);

        if(docpagNotReady()){
            return;
        }
        if (readonlymode) {
            return;
        }

        ChangeMarkupSelected(4);
        if (!bSetGlobal) {
            defaultFont.setHeight(tempheight);

        }

    }


    function RxCore_SetFont(value) {

        var tempfont = defaultFont.fontName;
        defaultFont.setFontname(value);

        if(docpagNotReady()){
            return;
        }
        if (readonlymode) {
            return;
        }

        ChangeMarkupSelected(4);
        if (!bSetGlobal) {
            defaultFont.setFontname(tempfont);
            //fontstylevalue = tempfont;
        }


    }

    function RxCore_GetFont() {
        return defaultFont;
    }


    function RxCore_GetFixedScale(){

        if(docpagNotReady()){
            return 1;
        }



        var scalefactor = getCurrentPageScale();


        var fixedScale = scalefactor / DocObj.getCurrentpage().fixedScale;
        if (bUseFixedScale){
            return fixedScale;
        }else{
            return 1;
        }
        

    }

    function RxCore_MarkupFillName(szpattern){
        if(docpagNotReady()){
            return;
        }
        if (readonlymode || documentcompare) {
            return;
        }
        
        var filltype = getFillTypeByName(szpattern);
        var type = getHatcTypeforName(szpattern);

        //getHatchbyName


        if(filltype == 'hatch'){
            
            //RxCore_setBlockHatch(blockid , hatchname, override);

        }else if (filltype == 'color'){
            
            var Color = getFillColorByName(szpattern);
            
            //setHatchinUse(hatchname, override);
            //RxCore_setBlockColor(blockid , Color, override);
        }

        var tempstyle = HatchStyle;


        HatchStyle = style;
        ChangeMarkupSelected(3);
        if (!bSetGlobal) {
            HatchStyle = tempstyle;
        }

    }

    function RxCore_MarkupHatch(style) {
        if(docpagNotReady()){
            return;
        }
        if (readonlymode || documentcompare) {
            return;
        }
        var tempstyle = HatchStyle;
        HatchStyle = style;
        ChangeMarkupSelected(3);
        if (!bSetGlobal) {
            HatchStyle = tempstyle;
        }


        /*

         if (szhatchpattern == 'Forward Diagonal'){ HatchStyle = 0;}
         if (szhatchpattern == 'Backward Diagonal'){ HatchStyle = 1;}
         if (szhatchpattern == 'Diagonal Cross'){ HatchStyle = 2;}
         if (szhatchpattern == 'Horizontal'){ HatchStyle = 3;}
         if (szhatchpattern == 'vertical'){ HatchStyle = 4;}
         if (szhatchpattern == 'Cross'){ HatchStyle = 5;}
         // change hatch style for selected markup if applicable.


         }*/

    }


    function RxCore_GetMarkupHatch() {
        return HatchStyle;
    }


    function RxCore_MetricUnit(unit) {


        if(docpagNotReady()){

            if (unit == 'Millimeter') {
                SubmeasureUnit = 1;
            }
            if (unit == 'Centimeter') {
                SubmeasureUnit = 2;
            }
            if (unit == 'Decimeter') {
                SubmeasureUnit = 3;
            }
            if (unit == 'Meter') {
                SubmeasureUnit = 4;
            }
            if (unit == 'Kilometer') {
                SubmeasureUnit = 5;
            }
            if (unit == 'Nautical Miles') {
                SubmeasureUnit = 6;
            }
    
            switch (SubmeasureUnit) {
                case 1:
                    Unitlabel = "mm";
                    AreaUnitlabel = "mm\u00B2";
                    unitscale = 1;
                    break;
                case 2:
                    Unitlabel = "cm";
                    AreaUnitlabel = "cm\u00B2";
                    unitscale = 10;
                    break;
                case 3:
                    Unitlabel = "dm";
                    AreaUnitlabel = "dm\u00B2";
                    unitscale = 100;
                    break;
                case 4:
                    Unitlabel = "m";
                    AreaUnitlabel = "m\u00B2";
                    unitscale = 1000;
                    break;
                case 5:
                    Unitlabel = "km";
                    AreaUnitlabel = "km\u00B2";
                    unitscale = 1000000;
                    break;
                case 6:
                    Unitlabel = "nmi";
                    AreaUnitlabel = "nmi\u00B2";
                    unitscale = 185200000;
                    break;
    
            }
    
            return;
        }else{
            
            DocObj.getCurrentpage().scaleObject.metricUnit(unit);
            //DocObj.getCurrentpage().metricUnit(unit);

            //RxCore_metricUnit
        }


    }

    function RxCore_ElementMetricUnit(unit) {
        
        var markup = RxCore_getSelectedMarkup();

        if(markup != undefined){
            if(markup.scaleObject != undefined){
                markup.scaleObject.metricUnit(unit);
            }
            
        }
        
    }


    function RxCore_ImperialUnit(unit) {


        if(docpagNotReady()){

            if (unit == 'Inch') {
                SubmeasureUnit = 1;
            }
            if (unit == 'Feet') {
                SubmeasureUnit = 2;
            }
            if (unit == 'Yard') {
                SubmeasureUnit = 3;
            }
            if (unit == 'Mile') {
                SubmeasureUnit = 4;
            }
            if (unit == 'Nautical Miles') {
                SubmeasureUnit = 5;
            }
    
            switch (SubmeasureUnit) {
                case 1:
                    Unitlabel = "in.";
                    AreaUnitlabel = "sq.in.";
                    unitscale = 1;
                    break;
                case 2:
                    Unitlabel = "ft.";
                    AreaUnitlabel = "sq.ft.";
                    unitscale = 12;
                    break;
                case 3:
                    Unitlabel = "yd.";
                    AreaUnitlabel = "sq.yd.";
                    unitscale = 36;
                    break;
                case 4:
                    Unitlabel = "mi.";
                    AreaUnitlabel = "sq.mi";
                    unitscale = 63360;
                    break;
                case 5:
                    Unitlabel = "nm";
                    AreaUnitlabel = "sq.nm";
                    unitscale = 72913.3858;
                    break;
    
            }

            return;
        }else{
            
            DocObj.getCurrentpage().scaleObject.imperialUnit(unit);
            //DocObj.getCurrentpage().imperialUnit(unit);
        }




    }

    function RxCore_ElementImperialUnit(unit) {        
        var markup = RxCore_getSelectedMarkup();

        if(!markup.isempty){
            markup.scaleObject.imperialUnit(unit);
        }
        
    }


    function RxCore_setCalibration(scale){
        

        if(docpagNotReady()){
            return;
        }else{
            DocObj.getCurrentpage().setCalibrateScale(scale);
            
        }


    }

    function RxCore_scale(scale) {
        var numerator = 0;
        var denomintaor = 0;
        /*for (i=0;i<this.childElementCount;i++){
         if(this.children[i].className == 'selected'){
         var szScale = this.children[i].textContent;
         }
         }*/

         if(docpagNotReady()){
            if (scale != 'Calibration') {
                var numarr = scale.split(":");
                numerator = numarr[0];
                denomintaor = numarr[1];
                if (bReverseScale) {
                    MeasureScale = denomintaor / numerator;
                } else {
                    MeasureScale = numerator / denomintaor;
                }

            } else {
                MeasureScale = nCalibrateScale;
            }

         }else{
            DocObj.getCurrentpage().scaleObject.setMeasureScale(scale);
            DocObj.bMarkupchanged = true;
            //DocObj.getCurrentpage().setMeasureScale(scale);
         }


    }

    function RxCore_elementScale(scale) {
        var markup = RxCore_getSelectedMarkup();

        if(markup.isempty){
            return;
        }

        if(markup.scaleObject != undefined){
            markup.scaleObject.setMeasureScale(scale);
            markup.hasScale = true;
            DocObj.bMarkupchanged = true;
        }



    }

    function RxCore_setScaleLabel(label) {
        DocObj.getCurrentpage().scaleObject.setScaleLabel(label);        
    }

    function RxCore_setElementScaleLabel(label) {
        var markup = RxCore_getSelectedMarkup();

        if(markup.isempty){
            return;
        }


        
        if(markup.scaleObject != undefined){
            markup.scaleObject.setScaleLabel(label);      
        }
        


        
    }
    
    function RxCore_setElementDimPrecision(value) {
        var markup = RxCore_getSelectedMarkup();

        if(markup.isempty){
            return;
        }

        if(markup.scaleObject != undefined){
            markup.scaleObject.setDimPrecision(value);
        }
        

        //{isempty : true};



        
    }

    function RxCore_resetToDefaultScaleValueForMarkup(scaleLabel) {
        var markupList = DocObj.markuplist;
        for (var i = 0; i < markupList.length; i++) {
            var markup = markupList[i];

            if(markup.hasScale && markup.scaleObject.scaleLabel === scaleLabel) {
                markup.scaleObject.resetToDefaultValue();
                markup.hasScale = false;
            }
        }
        
        DocObj.bMarkupchanged = true;
        RxCore_MarkupRedraw();
    }  

    
    function RxCore_setElementDimPrecision(value) {

        

        var markup = RxCore_getSelectedMarkup();

        if(markup.isempty){
            return;
        }

        
        if(markup.scaleObject != undefined){
            markup.scaleObject.setDimPrecision(value);
        }
        

    }


    function getTextdim(ctx, text, font){

        var textwidth = 0;
        var textheight = font.height;
        var textarray = text.split('\n');
        ctx.save();
        ctx.font = font.fontstringScaled;


        for (var i = 0; i < textarray.length; i++) {
            var dimsel = ctx.measureText(textarray[i]);
            if (textwidth < dimsel.width){
                textwidth = dimsel.width;
            }

        }

        var hpadding = textheight / 2 * i;
        textheight = (textheight * i) + hpadding;
        textwidth += 10;

        ctx.restore();

        return {w:textwidth,h:textheight};
    }

    function RxCore_MarkupAddBlockText(BlockId,height,text,color){
        if(docpagNotReady()){
            return;
        }

        if (readonlymode || documentcompare) {
            return;
        }


        if (DocObj.getCurrentpage().usevectorxml) {
            var centroid = DocObj.getCurrentpage().getBlockCentroid(BlockId);
            var textsize = DocObj.getCurrentpage().getpixeldist(height);
            var textsizept = textsize * 0.75; //convert px to pt.

            var textmarkupobj = new MarkupObject(9, 1, 0);

            textmarkupobj.scaling = DocObj.getCurrentpage().getdscale();
            textmarkupobj.xoffset = DocObj.getCurrentpage().getdx();
            textmarkupobj.yoffset = DocObj.getCurrentpage().getdy();


            textmarkupobj.font.setFontname(defaultFont.fontName);
            textmarkupobj.font.setHeight(textsizept);
            textmarkupobj.font.setBold(defaultFont.bold);
            textmarkupobj.font.setItalic(defaultFont.italic);

            var textdim = getTextdim(cntximg,text, textmarkupobj.font);

            textmarkupobj.linewidth = 0.5;
            textmarkupobj.w = textdim.w;
            textmarkupobj.h = textdim.h;

            textmarkupobj.x = centroid.x - (textmarkupobj.w/2);
            textmarkupobj.y = centroid.y - (textmarkupobj.h/2);


            textmarkupobj.text = text;



            textmarkupobj.fillcolor = "rgba(255,255,255, 0.9)";
            textmarkupobj.textcolor = color;
            textmarkupobj.strokecolor = color;
            textmarkupobj.pagerotation = DocObj.getCurrentpage().getpageRotation();
            textmarkupobj.pagenumber = DocObj.getcurPage();

            textmarkupobj.viewname = DocObj.getcur3DView();

            if(textmarkupobj.viewname != null){
                textmarkupobj.bfor3D = true;
                textmarkupobj.pageheight = canvasoheight;
                textmarkupobj.pagewidth = canvasowidth;
            }


            textmarkupobj.savemetolistDraw();

        }

        drawmarkupAll(cntximg);


    }

    function CreateTextRectdirect(x, y, w, h, markuparrow, text) {

        if (markuparrow != null) {
          var textmarkupobj = new MarkupObject(9, 2, 0);
  
          textmarkupobj.settxtboxArrow(markuparrow);
        } else {
          textmarkupobj = new MarkupObject(9, 1, 0);
        }
  
        textmarkupobj.x = x;
        textmarkupobj.y = y;
  
        switch (DocObj.getCurrentpage().getpageRotation()) {
          case 0:
            textmarkupobj.w = w;
            textmarkupobj.h = h;
  
            break;
          case 90:
            textmarkupobj.w = w;
            textmarkupobj.h = h;
  
            break;
          case 180:
            textmarkupobj.w = w;
            textmarkupobj.h = h;
  
            break;
          case 270:
            textmarkupobj.w = w;
            textmarkupobj.h = h;
  
            break;
        }
  
        textmarkupobj.pagerotation = DocObj.getCurrentpage().getpageRotation();
        textmarkupobj.pagenumber = DocObj.getcurPage();
  
        textmarkupobj.font.setFontname(defaultFont.fontName);
        textmarkupobj.font.setHeight(defaultFont.height);
        textmarkupobj.font.setBold(defaultFont.bold);
        textmarkupobj.font.setItalic(defaultFont.italic);
  
        //textmarkupobj.font = defaultFont;
        textmarkupobj.fillcolor = "rgba(255,255,255, 0.9)";
  
        
        textmarkupobj.text = text;
  
        //textmarkupobj.rotation = -(DocObj.getCurrentpage().getpageRotation() * (Math.PI / 180));
        
  
        return textmarkupobj;
  
      }


    function CreateTextRect(x, y, w, h, markuparrow, text){
        var sizePos = {
            x : x + w,
            y : y + h
        };

        var startpos = {
            x : x,
            y : y
        };

        var bWithin = MousePosdrwext(startpos);
        if (!bWithin && bLimMarkupExtent) {
            return;
        }
        
        var bWithin = MousePosdrwext(sizePos);
        if (!bWithin && bLimMarkupExtent) {
            return;
        }

        
        
        if(markuparrow != null){
            var textmarkupobj = new MarkupObject(9, 2, 0);
            
            textmarkupobj.settxtboxArrow(markuparrow);
        }else{
            textmarkupobj = new MarkupObject(9, 1, 0);
        }

        textmarkupobj.x = x;
        textmarkupobj.y = y;

        switch(DocObj.getCurrentpage().getpageRotation()){
            case 0:
                textmarkupobj.w = w;
                textmarkupobj.h = h;

                break;
            case 90:
                textmarkupobj.w = w;
                textmarkupobj.h = h;

                break;
            case 180:
                textmarkupobj.w = w;
                textmarkupobj.h = h;

                break;
            case 270:
                textmarkupobj.w = w;
                textmarkupobj.h = h;

                break;
        }


        textmarkupobj.pagerotation = DocObj.getCurrentpage().getpageRotation();
        textmarkupobj.pagenumber = DocObj.getcurPage();

        textmarkupobj.viewname = DocObj.getcur3DView();

        if(textmarkupobj.viewname != null){
            textmarkupobj.bfor3D = true;
            textmarkupobj.pageheight = canvasoheight;
            textmarkupobj.pagewidth = canvasowidth;
        }


        textmarkupobj.font.setFontname(defaultFont.fontName);
        textmarkupobj.font.setHeight(defaultFont.height);
        textmarkupobj.font.setBold(defaultFont.bold);
        textmarkupobj.font.setItalic(defaultFont.italic);

        //textmarkupobj.font = defaultFont;
        textmarkupobj.fillcolor = "rgba(255,255,255, 0.9)";

        textmarkupobj.scaling = getCurrentPageScale();
        var offset = getCurrentPageOffset();
        textmarkupobj.xoffset = offset.x;
        textmarkupobj.yoffset = offset.y;

        
        textmarkupobj.seteditaction(0);
        textmarkupobj.setselectedit(true);
        textmarkupobj.setselect(false);
        textmarkupobj.text = text;

        //textmarkupobj.rotation = -(DocObj.getCurrentpage().getpageRotation() * (Math.PI / 180));
        textmarkupobj.savemetolistDraw();

        if(bMultimarkupadd){

            if(DocObj.selectedmarkup.id != -1){
                if (DocObj.markuplist[DocObj.selectedmarkup.id] != undefined){
                    DocObj.markuplist[DocObj.selectedmarkup.id].setselectedit(false);
                    DocObj.markuplist[DocObj.selectedmarkup.id].setselect(false);
    
                }
            }

            drawmarkupAll(cntximg);
            DrawMarkupSelected(context);
    
        }


        DocObj.selectedmarkup.id = DocObj.getmarkupbynumber(textmarkupobj.markupnumber);
        DocObj.selectedmarkup.edit = true;
        DocObj.selectedmarkup.selected = true;

        return textmarkupobj;
    }

    function RxCore_CreateTextRect(x, y, w, h,text){

        CreateTextRect(x, y, w, h,null,text);

        //DocObj.setMarkupMultiselected();

    }


    function terminatecurrentText(){


        if (DocObj.selectedmarkup.id != -1 && DocObj.markuplist[DocObj.selectedmarkup.id] != undefined) {
            if(DocObj.markuplist[DocObj.selectedmarkup.id].type == 9){
                if (RxCore_GUI_TextInput != undefined) {
                    if(DocObj.selectedmarkup.edit){
                        RxCore_GUI_TextInput.operation.edit = false;
                        RxCore_GUI_TextInput.operation.start = false;
                        RxCore_GUI_TextInput.operation.create = false;
                        RxCore_GUI_TextInput.operation.save = true;
                        RxCore_GUI_TextInput.operation.markup = DocObj.markuplist[DocObj.selectedmarkup.id];


                        var rect = canvas.getBoundingClientRect();
                        var xrect  = DocObj.markuplist[DocObj.selectedmarkup.id].rotatedrect.x + rect.left;
                        var yrect = DocObj.markuplist[DocObj.selectedmarkup.id].rotatedrect.y + rect.top;

                        //var xrect  = DocObj.markuplist[DocObj.selectedmarkup.id].xscaled + rect.left;
                        //var yrect = DocObj.markuplist[DocObj.selectedmarkup.id].yscaled + rect.top;
                        var wrect = DocObj.markuplist[DocObj.selectedmarkup.id].wscaled;
                        var hrect = DocObj.markuplist[DocObj.selectedmarkup.id].hscaled;

                        var centercanvX = (canvasowidth / 2);
                        var centercanvY = (canvasoheight / 2);


                        var centerx = xrect + (wrect*0.5);
                        var centery = yrect + (hrect*0.5);

                        var CanvRotRad = DocObj.getCurrentpage().drotation * (Math.PI / 180);

                        if(DocObj.getCurrentpage().drotation != 0){
                            var rectcenter = DocObj.markuplist[DocObj.selectedmarkup.id].getRotatedMarkup(centercanvX, centercanvY, centerx, centery, CanvRotRad);
                            centerx = rectcenter.x;
                            centery = rectcenter.y;
                        }

                        centerx += rect.left;
                        centery += rect.top;


                        var txtrect = new Rectangle(xrect,yrect,wrect,hrect);
                        txtrect.rotation = DocObj.getCurrentpage().drotation;

                        var pixelRatio = getPixelRatio();
                        //txtrect.x = txtrect.x / pixelRatio;
                        //txtrect.y = txtrect.y / pixelRatio;

                        txtrect.x = ((txtrect.x - rect.left) / pixelRatio) + rect.left;
                        txtrect.y = ((txtrect.y - rect.top) / pixelRatio) + rect.top;


                        txtrect.w = txtrect.w / pixelRatio;
                        txtrect.h = txtrect.h / pixelRatio;


                        RxCore_GUI_TextInput.setTextInput(txtrect);


                        DocObj.selectedmarkup.edit = false;
                    }
                }
                //DocObj.selectedmarkup.id = listentrynum;

            }



        }

        if(DocObj.selectedmarkup.id != -1){
            if (DocObj.markuplist[DocObj.selectedmarkup.id] != undefined){
                DocObj.markuplist[DocObj.selectedmarkup.id].setselectedit(false);
                DocObj.markuplist[DocObj.selectedmarkup.id].setselect(false);

            }
        }


        drawmarkupAll(cntximg);
        DrawMarkupSelected(context);


    }


    function textRect(x, y, w, h, markuparrow){
        
        
        if (w < 20) {
            w = 20;
        }

        if (h < 12) {
            h = 12;
        }

        var sizePos = {
            x : x + w,
            y : y + h
        };

        var bWithin = MousePosdrwext(sizePos);
        if (!bWithin && bLimMarkupExtent) {
            return;
        }


        var textmarkup = CreateTextRect(x, y, w, h, markuparrow,"");

        if (RxCore_GUI_TextInput != undefined) {
            var rect = canvas.getBoundingClientRect();

            x += w*0.5;
            y += h*0.5;

            x += rect.left;
            y += rect.top;

            RxCore_GUI_TextInput.operation.start = true;
            RxCore_GUI_TextInput.operation.create = false;
            RxCore_GUI_TextInput.operation.edit = false;
            RxCore_GUI_TextInput.operation.save = false;
            RxCore_GUI_TextInput.operation.markup = textmarkup;

            var txtrect = new Rectangle(x,y,w,h);
            //txtrect.rotation = 0;
            txtrect.rotation = DocObj.getCurrentpage().getpageRotation();

            var pixelRatio = getPixelRatio();
            //txtrect.x = txtrect.x / pixelRatio;
            //txtrect.y = txtrect.y / pixelRatio;

            txtrect.x = ((txtrect.x - rect.left) / pixelRatio) + rect.left;
            txtrect.y = ((txtrect.y - rect.top) / pixelRatio) + rect.top;

            txtrect.w = txtrect.w / pixelRatio;
            txtrect.h = txtrect.h / pixelRatio;


            RxCore_GUI_TextInput.setTextInput(txtrect);

        }

        //RxCore_TextRect(false);

        if(!bMultimarkupadd){
            set_tool('markupedit', {p1 : false});
        }


        /*if(!bMultimarkupadd){
            
        }*/
        

        /*if (RxCore_GUI_TextInput != undefined) {
            RxCore_GUI_TextInput.operation.start = false;
            RxCore_GUI_TextInput.operation.create = false;
            RxCore_GUI_TextInput.operation.edit = true;
            RxCore_GUI_TextInput.operation.save = false;
        }*/

    }

    function stampRect(x, y, w, h, Text) {

        if (w < 200) {
            w = 200;
        }

        if (h < 124) {
            h = 124;
        }

        var sizePos = {
            x : x + w,
            y : y + h
        };

        var bWithin = MousePosdrwext(sizePos);
        if (!bWithin && bLimMarkupExtent) {
          return;
        }



        var stamptype = 0;
        for (var i = 0; i < Stamplist.length; i++) {
            if (Stamplist[i] == Text) {
                stamptype = i;
            }
        }
        var stampmarkupobj;
        stampmarkupobj = new MarkupObject(12, stamptype, 1);
        stampmarkupobj.fillcolor = "rgba(255,0,0,0.3)";
        stampmarkupobj.transparency = 30;

        if (stamptype < Stamplist.length - 2) {


            stampmarkupobj.text = Stamplist[stamptype];

        }else{
            if (Stamplist[stamptype] == 'Date') {
                stampmarkupobj.text = stampmarkupobj.GetDateTime(false);
                //ntexwtsize = 90;
            }

            if (Stamplist[stamptype] == 'User Name') {
                stampmarkupobj.text = GetDisplayName(stampmarkupobj.signature);
            }

        }

        stampmarkupobj.w = w;
        stampmarkupobj.h = h;
        var endx = 0;
        var startx = 0;
        var endy = 0;
        var starty = 0;

        stampmarkupobj.scaling = getCurrentPageScale();
        var offset = getCurrentPageOffset();
        stampmarkupobj.xoffset = offset.x;
        stampmarkupobj.yoffset = offset.y;
        
        startx = offset.x;
        starty = offset.y;
        endx = DocObj.getCurrentpage().endx;
        endy = DocObj.getCurrentpage().endy;


        stampmarkupobj.font.setFontname("Times New Roman");
        stampmarkupobj.font.setBold(true);

        stampmarkupobj.pagenumber = DocObj.getcurPage();


        stampmarkupobj.viewname = DocObj.getcur3DView();

        if(stampmarkupobj.viewname != null){
            stampmarkupobj.bfor3D = true;
            stampmarkupobj.pageheight = canvasoheight;
            stampmarkupobj.pagewidth = canvasowidth;
        }


        
        stampmarkupobj.seteditaction(0);


        if ((stampmarkupobj.w*0.6) >= stampmarkupobj.h){
            //stampmarkupobj.textheight = (stampmarkupobj.h / 4);
            stampmarkupobj.font.setHeight(stampmarkupobj.h / 4);
        }else{
            //stampmarkupobj.textheight = stampmarkupobj.w * 0.16;
            stampmarkupobj.font.setHeight(stampmarkupobj.w * 0.16);
        }


        //stampmarkupobj.textheight = h / 4;
        stampmarkupobj.stampsmalltheight = 16;

        stampmarkupobj.x = x;
        stampmarkupobj.y = y;


        if (!bMultimarkupadd) {

            bMarkupcrated = true;

            //nMarkupcreated = DocObj.markuplist.length;
            //ft 08.08.2018 changed from separate index to direct array length            
            //nMarkupcreated = DocObj.nummarkups;
        }

        nMarkupcreated = DocObj.markuplist.length;

        stampmarkupobj.drawme(context);
        stampmarkupobj.savemetolistDraw();

        img_update();
        if(DocObj){
            DocObj.bMarkupchanged = true;
        }


        stampmarkupobj = null;

        if(bAlwaysMarkupCallback){
            if (RxCore_GUI_Markup != undefined) {
                RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[nMarkupcreated], {created : true, modified : false, deleted : false, multi : bMultimarkupadd});
            }

        }else{

            if (!bMultimarkupadd) {

                //need to move to connection object.
                //markupcreated();
                if (RxCore_GUI_Markup != undefined) {
    
                    RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[nMarkupcreated], {created : true, modified : false, deleted : false});
                }
    
            }
    
        }

    }


    function RxCore_TextRectArrow(selected){

        //callout tool
        if(docpagNotReady()){
            return;
        }
        if (readonlymode || documentcompare) {
            return;
        }

        if (selected) {
            unselelectallmarkup();
            unselecteditmarkupall();

            set_tool('arrow', {p1 : 6});
            //set_tool('rectText',{});

            //tool = new tools['rectText']();
        } else {

            set_tool('markupedit',{p1 : false});
            
        }

    }

    function RxCore_TextRect(selected, callback){
        if(docpagNotReady()){
            return;
        }
        if (readonlymode || documentcompare) {
            return;
        }

        if (selected) {
            unselelectallmarkup();
            unselecteditmarkupall();

            set_tool('rectText',{});

            //tool = new tools['rectText']();
        } else {

            set_tool('markupedit',{p1: false});
            
        }
		

        if (RxCore_GUI_TextInput && callback && typeof callback === "function") {
            RxCore_GUI_TextInput.connect(callback);
        }

    }

    function RxCore_setFoxitSnap(onoff){
        buseFoxitSnap = onoff;
    }

    function RxCore_useFoxitRotateTo(onoff){
        buseFoxitrotateTo = onoff;
    }

    

    function RxCore_setAuth(type, token){
       
        buseAuth = true;
        buseEncToken = true;
        
        //ex Basic or Bearer
        AuthType = type;
 
        //base64encoded token
        AuthToken = token;
 
     }
 
    function RxCore_setJSONConfiguration(JSONconfig){

        bUseJSONConfig = true;
        ConfigurationJSON = JSONconfig;


    }

    function RxCore_setConfiguration(configURL){

        configurationLocation = configURL;
        bUsecustomConfig = true;

    }

    function RxCore_replaceImageMarkup(dropdata, index){

        //var tempwidht = linewidthvalue;
        //linewidthvalue = value;


        if(docpagNotReady()){
            return;
        }
        if (readonlymode) {
            return;
        }


    }

    function pentagram( ctx, x, y, radius, rotate, circle ){
        ctx.beginPath();

         for ( var i = 0; i <= 4 * Math.PI; i += ( 4 * Math.PI ) / 5 ) {
             ctx.lineTo( x + radius * Math.cos(i + rotate), y + radius * Math.sin(i + rotate));
         }
     
         if ( circle ) {
             ctx.moveTo( x + radius, y );
             ctx.arc(x, y, radius, 0, Math.PI * 2, false);
         }
     
         ctx.stroke();

    }

    function drawCountShape(ctx, x, y, shape, outerRadius, innerRadius, linewidth, fillcolor, strokecolor){

        switch (shape){
            case 0: //star
                drawStar(ctx, x, y, outerRadius, innerRadius, linewidth, fillcolor, strokecolor);
                break;

        }

    }
    function drawCircle(ctx, cx, cy, outerRadius, innerRadius, linewidth, fillcolor, strokecolor){
        ctx.save(); // save the context so we don't mess up others

        ctx.beginPath();

        ctx.arc(cx, cy, outerRadius, 0, 2 * Math.PI);

        ctx.closePath();

        ctx.lineWidth=linewidth;   
        ctx.fillStyle = fillcolor;   
        ctx.strokeStyle = strokecolor

        ctx.fill();   
        ctx.stroke();   


        ctx.restore(); // restore context to what it was on entry
    }


    function drawDiamond(ctx, cx, cy, outerRadius, innerRadius, linewidth, fillcolor, strokecolor){
        ctx.save(); // save the context so we don't mess up others
        ctx.beginPath();

        ctx.moveTo(cx, cy - outerRadius);
        ctx.lineTo(cx + outerRadius, cy);

        ctx.lineTo(cx, cy + outerRadius);

        ctx.lineTo(cx - outerRadius, cy);

        ctx.lineTo(cx, cy - outerRadius);
        
        ctx.closePath();

        ctx.lineWidth=linewidth;   
        ctx.fillStyle = fillcolor;   
        ctx.strokeStyle = strokecolor

        ctx.fill();   
        ctx.stroke();   

        ctx.restore(); // restore context to what it was on entry


    }

    function drawTriangle(ctx, cx, cy, outerRadius, innerRadius, linewidth, fillcolor, strokecolor){
        ctx.save(); // save the context so we don't mess up others
        ctx.beginPath();

        ctx.moveTo(cx - outerRadius, cy - outerRadius);
        ctx.lineTo(cx + outerRadius, cy - outerRadius);
        ctx.lineTo(cx, cy + outerRadius); //bottom point
        ctx.lineTo(cx - outerRadius, cy - outerRadius);
        
        ctx.closePath();

        ctx.lineWidth=linewidth;   
        ctx.fillStyle = fillcolor;   
        ctx.strokeStyle = strokecolor

        ctx.fill();   
        ctx.stroke();   

        ctx.restore(); // restore context to what it was on entry


    }

    function drawSquare(ctx, cx, cy, outerRadius, innerRadius, linewidth, fillcolor, strokecolor){
        ctx.save(); // save the context so we don't mess up others
        ctx.beginPath();

        ctx.moveTo(cx - outerRadius, cy - outerRadius);
        ctx.lineTo(cx + outerRadius, cy - outerRadius);
        ctx.lineTo(cx + outerRadius, cy + outerRadius);
        ctx.lineTo(cx - outerRadius, cy + outerRadius);
        ctx.lineTo(cx - outerRadius, cy - outerRadius);
        
        ctx.closePath();

        ctx.lineWidth=linewidth;   
        ctx.fillStyle = fillcolor;   
        ctx.strokeStyle = strokecolor

        ctx.fill();   
        ctx.stroke();   

        ctx.restore(); // restore context to what it was on entry


    }

    function drawDiagonalCross(ctx, cx, cy, outerRadius, innerRadius, linewidth, fillcolor, strokecolor){
        ctx.save(); // save the context so we don't mess up others
        
        //draw tick
        ctx.beginPath();

        //var halfrad = outerRadius * 0.5;

        ctx.moveTo(cx - outerRadius, cy - outerRadius);

        ctx.lineTo(cx + outerRadius, cy + outerRadius);

        ctx.moveTo(cx - outerRadius, cy + outerRadius );

        ctx.lineTo(cx + outerRadius, cy - outerRadius);
        
        //ctx.closePath();

        ctx.lineWidth = linewidth;
        ctx.strokeStyle = strokecolor;
        ctx.stroke();  

        ctx.restore(); // restore context to what it was on entry
    }


    function drawCross(ctx, cx, cy, outerRadius, innerRadius, linewidth, fillcolor, strokecolor){
        ctx.save(); // save the context so we don't mess up others
        
        //draw tick
        ctx.beginPath();

        //var halfrad = outerRadius * 0.5;

        ctx.moveTo(cx, cy - outerRadius);

        ctx.lineTo(cx, cy + outerRadius);

        ctx.moveTo(cx - outerRadius, cy);

        ctx.lineTo(cx + outerRadius, cy);
        
        //ctx.closePath();

        ctx.lineWidth = linewidth;
        ctx.strokeStyle = strokecolor;
        ctx.stroke();  

        ctx.restore(); // restore context to what it was on entry
    }

    function drawTick(ctx, cx, cy, outerRadius, innerRadius, linewidth, fillcolor, strokecolor){
        ctx.save(); // save the context so we don't mess up others
        
        //draw tick
        ctx.beginPath();

        //var halfrad = outerRadius * 0.5;

        ctx.moveTo(cx - innerRadius, cy);

        ctx.lineTo(cx, cy + innerRadius);

        ctx.lineTo(cx + outerRadius, cy - innerRadius);
        
        //ctx.closePath();

        ctx.lineWidth = linewidth;
        ctx.strokeStyle = strokecolor;
        ctx.stroke();  

        ctx.restore(); // restore context to what it was on entry
    }

    function drawStar(ctx, cx, cy, outerRadius, innerRadius, linewidth, fillcolor, strokecolor){
        ctx.save(); // save the context so we don't mess up others

        var spikes = 5;
        var i = 0;
        //var rot= (Math.PI/2) * 3;
        var rot = Math.PI / 2 * 3;
        var x=cx;
        var y=cy;
        var step=Math.PI/spikes;
        
        ctx.beginPath();

        ctx.moveTo(cx,cy-outerRadius);

        for(i=0;i<spikes;i++){
            
            x=cx+Math.cos(rot)*outerRadius;
            y=cy+Math.sin(rot)*outerRadius;
            ctx.lineTo(x,y);
            rot+=step;
    
            x=cx+Math.cos(rot)*innerRadius;
            y=cy+Math.sin(rot)*innerRadius;
            ctx.lineTo(x,y);
            rot+=step;
        }


        ctx.lineTo(cx,cy-outerRadius);
        ctx.closePath();

        ctx.lineWidth=linewidth;   
        ctx.fillStyle = fillcolor;   
        ctx.strokeStyle = strokecolor;   
        
        ctx.fill();   
        ctx.stroke();   

        ctx.restore(); // restore context to what it was on entry
    }    

    function RxCore_drawRoundedRect(ctx, x, y, width, height, rotation, radius, linewidth, fill, stroke, fillcolor, strokecolor){

        ctx.save(); // save the context so we don't mess up others
        ctx.strokeStyle = strokecolor;
        ctx.fillStyle = fillcolor;
        ctx.lineWidth = linewidth;
        if (rotation != 0) {
            var tx = x + (width / 2);
            var ty = y + (height / 2);
            ctx.translate(tx, ty);
            ctx.rotate(markupobject.rotation);
            ctx.translate(-tx, -ty);
        }
        //alternative 2 start
        ctx.beginPath();
        ctx.moveTo(x + radius, y);
        ctx.lineTo(x + width - radius, y);
        ctx.arcTo(x + width, y, x + width, y + radius, radius);
        ctx.lineTo(x + width, y + height - radius);
        ctx.arcTo(x + width, y + height, x + width - radius, y + height, radius);
        ctx.lineTo(x + radius, y + height);
        ctx.arcTo(x, y + height, x, y + height - radius, radius);
        ctx.lineTo(x, y + radius);
        ctx.arcTo(x, y, x + radius, y, radius);
        //alternative 2 end
        /*ctx.beginPath();

         // draw top and top right corner
         ctx.moveTo(x+radius,y);
         ctx.arcTo(x+width,y,x+width,y+radius,radius);

         // draw right side and bottom right corner
         ctx.arcTo(x+width,y+height,x+width-radius,y+height,radius);

         // draw bottom and bottom left corner
         ctx.arcTo(x,y+height,x,y+height-radius,radius);

         // draw left and top left corner
         ctx.arcTo(x,y,x+radius,y,radius);*/
        if (fill) {
            ctx.fill();
        }
        if (stroke) {
            ctx.stroke();
        }
        ctx.restore(); // restore context to what it was on entry
    }
    
    function RxCore_getCanvasFont(FontName, Height, bold, italic){

        var font = new FontObject(FontName, Height, bold, italic);

        return font.fontstringpx;

    }

    function RxCore_moveimageMarkup(x, y){

        if(docpagNotReady()){
            return;
        }
        if (readonlymode || documentcompare) {
            return;
        }

        unselelectallmarkup();
        unselecteditmarkupall();

        var markupobject = findImagemarkup(2);

        if (markupobject != null){
            markupobject.movedirect(x, y);
            //markupobject.moveimagemarkup(x, y);
        }


    }

    const getThumbcanvas = function(source, thumbwidth){

                
        const loadpromise = new Promise(function(resolve,reject){
        
            var target = document.createElement('img');
            var cameracanvas = document.createElement('canvas');

            target.onload = function(ev){

                var image = ev.target || ev.srcElement;

                //var imagewidth = GetImageWidth(image);
                //var imageheight = GetImageHeight(image);

                cameracanvas.width = image.naturalWidth;
                cameracanvas.height = image.naturalHeight;

                //naturalHeight: 874
                //naturalWidth: 1612

                if(cameracanvas.width > 0){
                    var scale = thumbwidth / cameracanvas.width;
                }else{
                    scale = 1;
                }

                var camctx = cameracanvas.getContext('2d');
                camctx.drawImage(target, 0, 0);

                var thumbcnv = downScaleCanvas(cameracanvas, scale);


                source.thumbnail = thumbcnv;
                source.width = thumbcnv.width;
                source.height = thumbcnv.height;


                resolve(source);
                
            };

            target.onerror = function(ev){
                
                reject();

            };


            target.src = source.image.src;

            //camobj.image.src

        });

        return loadpromise;



    }    

    const setimage = function (target, source){
        const loadpromise = new Promise(function(resolve,reject){
            

            target.onload = function(ev){
                
                resolve();
                
                /*if (this.status == 200) {
                    resolve(xhr.response);
                }else{
                    reject(this);
                }*/
            };

            target.onerror = function(ev){
                
                reject();

            };


            target.src = source.src;

        });

        return loadpromise;

    }

    function RxCore_createImageMarkup(dropdata, x, y){

        if(docpagNotReady()){
            return;
        }
        if (readonlymode || documentcompare) {
            return;
        }

        unselelectallmarkup();
        unselecteditmarkupall();

        //there can be only one.
        deleteImagemarkups(2);

        var imagemarkupobj = new MarkupObject(11, 2, 0);

        imagemarkupobj.scaling = getCurrentPageScale();
        var offset = getCurrentPageOffset();
        imagemarkupobj.xoffset = offset.x;
        imagemarkupobj.yoffset = offset.y;


        imagemarkupobj.printscale = dropdata.scale;
        
        imagemarkupobj.pagenumber = DocObj.getcurPage();

        imagemarkupobj.viewname = DocObj.getcur3DView();

        if(imagemarkupobj.viewname != null){
            imagemarkupobj.bfor3D = true;
            imagemarkupobj.pageheight = canvasoheight;
            imagemarkupobj.pagewidth = canvasowidth;
        }


        
        imagemarkupobj.seteditaction(0);
        //var bWithin = MousePosdrwext(mousePos);
        
        //imagemarkupobj.x = (canvasowidth * 0.5) - (dropdata.width/2);
        //imagemarkupobj.y = (canvasoheight * 0.5) - (dropdata.height/2);

        imagemarkupobj.x = x;
        imagemarkupobj.y = y;

        
        imagemarkupobj.w = dropdata.width;
        imagemarkupobj.h = dropdata.height;
        //var dropdata = ev.dataTransfer.getData("Text");

        imagemarkupobj.image = new Image();
        //createimage.src = ev.dataTransfer.getData("Text");
        

        //imagemarkupobj.image.onload
        //imagemarkupobj.image.src = dropdata.src;
        
        //imagemarkupobj.image = createimage;

        //imagemarkupobj.imageloaded = true;

        context.clearRect(0, 0, canvas.width, canvas.height);

        setSmoothingEnabledEx(false, cntximg);

        if (!bMultimarkupadd) {
            bMarkupcrated = true;
            
        }

        nMarkupcreated = DocObj.markuplist.length;

        imagemarkupobj.savemetolistDraw();
        var returnpromise = setimage(imagemarkupobj.image, dropdata);

        img_update();
        imagemarkupobj = null;

        if(bAlwaysMarkupCallback){
            if (RxCore_GUI_Markup != undefined) {
                
                var operation = {created : true, modified : false, deleted : false, multi : bMultimarkupadd};
                RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[nMarkupcreated], operation);
                
            }

        }else{

            if (!bMultimarkupadd) {
            
                if (RxCore_GUI_Markup != undefined) {
                
                    var operation = {created : true, modified : false, deleted : false};
                    RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[nMarkupcreated], operation);
                }
    
            }
    
        }



        return returnpromise;
    }
    

    function RxCore_StampMarkup(Text){
        if (!documentopen) {
            return;
        }
        if (readonlymode || documentcompare) {
            return;
        }

        unselelectallmarkup();
        unselecteditmarkupall();
        set_tool('rectStamp', {p1 : Text});
        //tool = new tools['rectStamp'](Text);

    }

    function RxCore_markupDisplayOnload(onoff){
    
        markupdisplayonload = onoff;
        
    }


    function RxCore_MarkupStamp(Text) {
        if (!documentopen) {
            return;
        }
        if (readonlymode || documentcompare) {
            return;
        }

        unselelectallmarkup();
        unselecteditmarkupall();
        var stamptype = 0;
        //var szstamp = Text;

        for (var i = 0; i < Stamplist.length; i++) {
            if (Stamplist[i] == Text) {
                stamptype = i;
            }
        }


        //width of stamp = 200 pixels.
        //1/3 from top of display.
        //height of stamp = 1/4 of width
        //font size = 62 for main text 31 for small text.
        //height of stamp = height of text + 30 percent

        var stampmarkupobj;
        if (stamptype < Stamplist.length - 2) {
            stampmarkupobj = new MarkupObject(12, stamptype, 0);
            stampmarkupobj.w = 200;
            stampmarkupobj.h = 124;
            var endx = 0;
            var startx = 0;
            var endy = 0;
            var starty = 0;

            stampmarkupobj.scaling = getCurrentPageScale();
            var offset = getCurrentPageOffset();
            stampmarkupobj.xoffset = offset.x;
            stampmarkupobj.yoffset = offset.y;
            startx = offset.x;
            endx = DocObj.getCurrentpage().endx;
            starty = offset.y;
            endy = DocObj.getCurrentpage().endy;


            stampmarkupobj.pagenumber = DocObj.getcurPage();
            stampmarkupobj.viewname = DocObj.getcur3DView();

            if(stampmarkupobj.viewname != null){
                stampmarkupobj.bfor3D = true;
                stampmarkupobj.pageheight = canvasoheight;
                stampmarkupobj.pagewidth = canvasowidth;
                
            }

            stampmarkupobj.seteditaction(0);


            stampmarkupobj.textheight = 32;
            stampmarkupobj.stampsmalltheight = 16;

            stampmarkupobj.x = (startx + ((endx - startx) / 2)) - (stampmarkupobj.w / 2);
            stampmarkupobj.y = (starty + ((endy - starty) / 2)) - (stampmarkupobj.h / 2);


            if (!bMultimarkupadd) {
                bMarkupcrated = true;
                //ft 08.08.2018 changed from separate index to direct array length                            
                //nMarkupcreated = DocObj.nummarkups;
                
            }

            nMarkupcreated = DocObj.markuplist.length;

            stampmarkupobj.drawme(context);
            stampmarkupobj.savemetolistDraw();


        } else {
            stampmarkupobj = new MarkupObject(9, 0, 0);
            if (DocObj.getCurrentpage().usevectorxml) {
                stampmarkupobj.scaling = DocObj.getCurrentpage().dscalevector;
                stampmarkupobj.xoffset = DocObj.getCurrentpage().dxvector;
                stampmarkupobj.yoffset = DocObj.getCurrentpage().dyvector;
                stampmarkupobj.x = (DocObj.getCurrentpage().dxvector + (DocObj.getCurrentpage().endx / 2)) - ntexwtsize; // - (stampmarkupobj.w / 2);
                stampmarkupobj.y = (DocObj.getCurrentpage().dyvector + (DocObj.getCurrentpage().endy / 2)) + (stampmarkupobj.textheight / 2);


            } else if (DocObj.getCurrentpage().usepdfjs) {
                stampmarkupobj.scaling = (DocObj.getCurrentpage().curpagescale * DocObj.getCurrentpage().dscalepdf);
                stampmarkupobj.xoffset = DocObj.getCurrentpage().dxpdf;
                stampmarkupobj.yoffset = DocObj.getCurrentpage().dypdf;
                stampmarkupobj.x = (DocObj.getCurrentpage().dxpdf + (DocObj.getCurrentpage().endx / 2)) - ntexwtsize; // - (stampmarkupobj.w / 2);
                stampmarkupobj.y = (DocObj.getCurrentpage().dypdf + (DocObj.getCurrentpage().endy / 2)) + (stampmarkupobj.textheight / 2);


            } else {
                stampmarkupobj.scaling = DocObj.getCurrentpage().dscale;
                stampmarkupobj.xoffset = DocObj.getCurrentpage().dx;
                stampmarkupobj.yoffset = DocObj.getCurrentpage().dy;
                stampmarkupobj.x = (DocObj.getCurrentpage().dx + (DocObj.getCurrentpage().endx / 2)) - ntexwtsize; // - (stampmarkupobj.w / 2);
                stampmarkupobj.y = (DocObj.getCurrentpage().dy + (DocObj.getCurrentpage().endy / 2)) + (stampmarkupobj.textheight / 2);


            }
            stampmarkupobj.pagenumber = DocObj.getcurPage();
            stampmarkupobj.viewname = DocObj.getcur3DView();

            if(stampmarkupobj.viewname != null){
                stampmarkupobj.bfor3D = true;
                stampmarkupobj.pageheight = canvasoheight;
                stampmarkupobj.pagewidth = canvasowidth;
            }

            stampmarkupobj.seteditaction(0);
            stampmarkupobj.textheight = 32;
            var ntexwtsize = 100;
            if (Stamplist[stamptype] == 'Date') {
                stampmarkupobj.text = stampmarkupobj.GetDateTime(false);
                ntexwtsize = 90;
            }

            if (Stamplist[stamptype] == 'User Name') {
                stampmarkupobj.text = GetDisplayName(stampmarkupobj.signature);
            }

            //stampmarkupobj.text = Stamplist[stamptype];

            //stampmarkupobj.w = 200;
            //stampmarkupobj.h = 124;

            //stampmarkupobj.x = (canvas.width / 2)-ntexwtsize;// - (stampmarkupobj.w / 2);
            //stampmarkupobj.y = (canvas.height / 2) + (stampmarkupobj.textheight / 2);

            //stampmarkupobj.stampsmalltheight = 16;


            stampmarkupobj.drawme(context);
            if (!bMultimarkupadd) {

                bMarkupcrated = true;
                //ft 08.08.2018 changed from separate index to direct array length                            
                //nMarkupcreated = DocObj.nummarkups;
                
            }
            nMarkupcreated = DocObj.markuplist.length;

            stampmarkupobj.savemetolistDraw();

        }
        img_update();
        if(DocObj){
            DocObj.bMarkupchanged = true;
        }


        stampmarkupobj = null;


        if(bAlwaysMarkupCallback){
            if (RxCore_GUI_Markup != undefined) {
                RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[nMarkupcreated], {created : true, modified : false, deleted : false, multi : bMultimarkupadd});
            }

        }else{

            if (!bMultimarkupadd) {

                //need to move to connection object.
                //markupcreated();
                if (RxCore_GUI_Markup != undefined) {
    
                    RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[nMarkupcreated], {created : true, modified : false, deleted : false});
                }
    
            }
    
        }



    }

    function RxCore_MSIEeventCheck(onoff){
        bDisableMSIE11Eventcheck = onoff;
    }

    function RxCore_createSpaceHover(color, path){
        var absxscaled = path.points[0];
        var absyscaled = path.points[1];

        //absxscaled += xoffset;
        //absyscaled += yoffset;

        //spacemarkup.addpoint(absxscaled, absyscaled);        

        var temppointarraymain = [];

        temppointarraymain.push({x:absxscaled,y:absyscaled});

        var relxscaled = 0;
        var relyscaled = 0;

        for (var counter = 2; counter < path.points.length; counter += 2) {

            relxscaled = path.points[counter];
            relyscaled = path.points[counter + 1];
            //relxscaled += xoffset;
            //relyscaled += yoffset;
            //spacemarkup.addpoint(relxscaled, relyscaled);
            temppointarraymain.push({x:relxscaled,y:relyscaled});

        }

        var areapoints = getpolygonarea(temppointarraymain);
        var temppointarray = [];

        if(path.gotsubpath){
            for (var scount = 0; scount < path.subpaths.length; scount++) {
                for ( counter = 0; counter < path.subpaths[scount].points.length; counter += 2) {

                    relxscaled = path.subpaths[scount].points[counter]; // ((path.subpaths[scount].points[counter] - mediax) * scalefactor);
                    relyscaled = path.subpaths[scount].points[counter + 1]; //((mediah - path.subpaths[scount].points[counter + 1]) * scalefactor);

                    //relxscaled += xoffset;
                    //relyscaled += yoffset;

                    temppointarray.push({x:relxscaled,y:relyscaled});
                    //spacemarkup.addpoint(relxscaled, relyscaled);
                }

                var parea = getpolygonarea(temppointarray);

                if(parea > areapoints){
                    temppointarraymain = temppointarray.slice();
                    temppointarray = [];
                    areapoints = parea;
                }

            }
        }

        return {path : temppointarraymain, color : color};
        
        //drawmarkupAll(cntximg);

    }

    function RxCore_createSpacemarkupAttribsPath(blockid, path, attributes, scalefactor, xoffset, yoffset, mediax, mediay, mediah){
        var absxscaled = (path.points[0] - mediax) * scalefactor;
        var absyscaled = (mediah - path.points[1]) * scalefactor;

        absxscaled += xoffset;
        absyscaled += yoffset;

        var spacemarkup = new MarkupObject(8, 1, 0);
        spacemarkup.linewidth = 1;

        for (var ac=0; ac <  attributes.length ; ac++){
           spacemarkup.AddAttribute(attributes[ac].name, attributes[ac].value);
        }

        spacemarkup.AddAttribute('Rx_Area', '0');
        var spbid = blockid.toString();
        spacemarkup.AddAttribute('SpaceID', spbid);
        spacemarkup.scaling = scalefactor;
        spacemarkup.xoffset = xoffset;
        spacemarkup.yoffset = yoffset;

        spacemarkup.addpoint(absxscaled, absyscaled);        

        var relxscaled = 0;
        var relyscaled = 0;

        for (var counter = 2; counter < path.points.length; counter += 2) {

            relxscaled = ((path.points[counter] - mediax) * scalefactor);
            relyscaled = ((mediah - path.points[counter + 1]) * scalefactor);
            relxscaled += xoffset;
            relyscaled += yoffset;
            spacemarkup.addpoint(relxscaled, relyscaled);

        }

        var areapoints = spacemarkup.PolygonArea();
        var temppointarray = [];

        if(path.gotsubpath){
            for (var scount = 0; scount < path.subpaths.length; scount++) {
                for ( counter = 0; counter < path.subpaths[scount].points.length; counter += 2) {

                    relxscaled = ((path.subpaths[scount].points[counter] - mediax) * scalefactor);
                    relyscaled = ((mediah - path.subpaths[scount].points[counter + 1]) * scalefactor);

                    relxscaled += xoffset;
                    relyscaled += yoffset;

                    temppointarray.push({x:relxscaled,y:relyscaled});
                    //spacemarkup.addpoint(relxscaled, relyscaled);
                }

                var parea = getpolygonarea(temppointarray);

                if(parea > areapoints){
                    spacemarkup.points = temppointarray.slice();
                    temppointarray = [];
                    areapoints = parea;
                }

            }
        }


        spacemarkup.findrectangle();

        bMarkupcrated = true;
        nMarkupcreated = DocObj.markuplist.length;

        spacemarkup.markupID = getlastmarkupnumber();
        spacemarkup.savemetolistLoad(DocObj);

        img_update();
        drawmarkupAll(cntximg);

        if (RxCore_GUI_Markup != undefined) {
            //RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[index],true);
            RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[nMarkupcreated], {created : true, modified : false, deleted : false});
        }

    }

    function RxCore_createSpacemarkupAttribs(blockid, polypoints, attributes, scalefactor, xoffset, yoffset, mediax, mediay, mediah){
        
        var absxscaled = (polypoints[0] - mediax) * scalefactor;
        var absyscaled = (mediah - polypoints[1]) * scalefactor;

        absxscaled += xoffset;
        absyscaled += yoffset;

        var spacemarkup = new MarkupObject(8, 1, 0);
            spacemarkup.linewidth = 1;

            for (var ac=0; ac <  attributes.length ; ac++){

                spacemarkup.AddAttribute(attributes[ac].name, attributes[ac].value);
    
            }

            spacemarkup.AddAttribute('Rx_Area', '0');
            var spbid = blockid.toString();
            spacemarkup.AddAttribute('SpaceID', spbid);
            spacemarkup.scaling = scalefactor;
            spacemarkup.xoffset = xoffset;
            spacemarkup.yoffset = yoffset;

        spacemarkup.addpoint(absxscaled, absyscaled);

        var relxscaled = 0;
        var relyscaled = 0;

        for (var counter = 2; counter < polypoints.length; counter += 2) {

            relxscaled = ((polypoints[counter] - mediax) * scalefactor);
            relyscaled = ((mediah - polypoints[counter + 1]) * scalefactor);
            relxscaled += xoffset;
            relyscaled += yoffset;
            spacemarkup.addpoint(relxscaled, relyscaled);

        }

        spacemarkup.findrectangle();

        bMarkupcrated = true;
        nMarkupcreated = DocObj.markuplist.length;

        spacemarkup.markupID = getlastmarkupnumber();
        spacemarkup.savemetolistLoad(DocObj);

        img_update();
        drawmarkupAll(cntximg);

        if (RxCore_GUI_Markup != undefined) {
            //RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[index],true);
            RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[nMarkupcreated], {created : true, modified : false, deleted : false});
        }


    }

    function RxCore_createSpacemarkupPath(path, scalefactor, xoffset, yoffset, mediax, mediay, mediah){

        var absxscaled = (path.points[0] - mediax) * scalefactor;
        var absyscaled = (mediah - path.points[1]) * scalefactor;

        absxscaled += xoffset;
        absyscaled += yoffset;

        var spacemarkupArray = [];
        
        var spacemarkup = new MarkupObject(8, 1, 0);
        spacemarkup.linewidth = 1;
        spacemarkup.AddAttribute('Rx_Area', '0');
        spacemarkup.AddAttribute('SpaceID', '0');
        spacemarkup.scaling = scalefactor;
        spacemarkup.xoffset = xoffset;
        spacemarkup.yoffset = yoffset;

        spacemarkup.addpoint(absxscaled, absyscaled);

        var relxscaled = 0;
        var relyscaled = 0;

        for (var counter = 2; counter < path.points.length; counter += 2) {

            relxscaled = ((path.points[counter] - mediax) * scalefactor);
            relyscaled = ((mediah - path.points[counter + 1]) * scalefactor);
            relxscaled += xoffset;
            relyscaled += yoffset;
            spacemarkup.addpoint(relxscaled, relyscaled);

        }

         
        var areapoints = spacemarkup.PolygonArea();
        
        //areaArray.push(areapoints);


        var temppointarray = [];

        //spacemarkup.points = [];
        
        if(path.gotsubpath){
            for (var scount = 0; scount < path.subpaths.length; scount++) {
                for ( counter = 0; counter < path.subpaths[scount].points.length; counter += 2) {

                    relxscaled = ((path.subpaths[scount].points[counter] - mediax) * scalefactor);
                    relyscaled = ((mediah - path.subpaths[scount].points[counter + 1]) * scalefactor);

                    relxscaled += xoffset;
                    relyscaled += yoffset;

                    temppointarray.push({x:relxscaled,y:relyscaled});
                    //spacemarkup.addpoint(relxscaled, relyscaled);
                }

                var parea = getpolygonarea(temppointarray);

                if(parea > areapoints){
                    spacemarkup.points = temppointarray.slice();
                    temppointarray = [];
                    areapoints = parea;
                }

            }
        }
        
        spacemarkup.findrectangle();

        //remove holes for now

        /*var aremovepoints = [];
        var temppointarray = [];

        var mrkscale = scalefactor /spacemarkup.scaling;

        for (var mpcounter = 0; mpcounter < spacemarkup.points.length; mpcounter++) {

            var xscaledpoint = (spacemarkup.points[mpcounter].x - spacemarkup.xoffset) * mrkscale;
            var yscaledpoint = (spacemarkup.points[mpcounter].y - spacemarkup.yoffset) * mrkscale;

            xscaledpoint = xscaledpoint + xoffset;
            yscaledpoint = yscaledpoint + yoffset;

            if(spacemarkup.iswithin(xscaledpoint,yscaledpoint)){
                temppointarray.push(spacemarkup.points[mpcounter]);
            }else{
                aremovepoints.push(mpcounter);
            }    
        }

        spacemarkup.points = temppointarray.slice();*/
        //var temppointarray = spacemarkup.points.slice();
        /*for (var rmpcount = 0;rmpcount < aremovepoints.length;rmpcount++){
            
            spacemarkup.deletepoint(aremovepoints[rmpcount]);
        }*/
        
        //spacemarkup

        //spacemarkup.findrectangle();

        bMarkupcrated = true;
        nMarkupcreated = DocObj.markuplist.length;

        spacemarkup.savemetolistLoad(DocObj);

        img_update();
        drawmarkupAll(cntximg);

        if (RxCore_GUI_Markup != undefined) {
            //RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[index],true);
            RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[nMarkupcreated], {created : true, modified : false, deleted : false});
        }        

    }

    function RxCore_createSpacemarkup(polypoints, scalefactor, xoffset, yoffset, mediax, mediay, mediah){
        
        var absxscaled = (polypoints[0] - mediax) * scalefactor;
        var absyscaled = (mediah - polypoints[1]) * scalefactor;

        absxscaled += xoffset;
        absyscaled += yoffset;

        var spacemarkup = new MarkupObject(8, 1, 0);
            spacemarkup.linewidth = 1;
            spacemarkup.AddAttribute('Rx_Area', '0');
            spacemarkup.AddAttribute('SpaceID', '0');
            spacemarkup.scaling = scalefactor;
            spacemarkup.xoffset = xoffset;
            spacemarkup.yoffset = yoffset;

        spacemarkup.addpoint(absxscaled, absyscaled);

        var relxscaled = 0;
        var relyscaled = 0;

        for (var counter = 2; counter < polypoints.length; counter += 2) {

            relxscaled = ((polypoints[counter] - mediax) * scalefactor);
            relyscaled = ((mediah - polypoints[counter + 1]) * scalefactor);
            relxscaled += xoffset;
            relyscaled += yoffset;
            spacemarkup.addpoint(relxscaled, relyscaled);

        }

        spacemarkup.findrectangle();

        bMarkupcrated = true;
        nMarkupcreated = DocObj.markuplist.length;

        spacemarkup.savemetolistLoad(DocObj);

        img_update();
        drawmarkupAll(cntximg);

        if (RxCore_GUI_Markup != undefined) {
            //RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[index],true);
            RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[nMarkupcreated], {created : true, modified : false, deleted : false});
        }



    }

    function RxCore_CreateSpaceFromPolygon(selected){
        if(docpagNotReady()){
            return;
        }
        if (documentcompare) {
            return;
        }

        if (!selected) {
            
            set_tool('markupedit', {p1 : false});
            
            return;
        }

        unselelectallmarkup();
        set_tool('pickPolygon', {p1 : false, p2 : true, p3: true, p4 : false});
        //tool = new tools['pickPolygon'](multi);

    }

    function RxCore_pickRxSpace(selected,multi){
        if(docpagNotReady()){
            return;
        }
        if (documentcompare) {
            return;
        }

        if (!selected) {
            
            set_tool('markupedit', {p1 : false});
            
            return;
        }

        unselelectallmarkup();
        set_tool('pickPolygon', {p1 : multi, p2 :  false, p3: false, p4 : false});


    }


    function RxCore_GetBlockInsert(selected){
        if(docpagNotReady()){
            return;
        }
        if (documentcompare) {
            return;
        }

        if (!selected) {
            
            set_tool('markupedit', {p1 : false});
            unselectallBlocks();
            
            return;
        }

        unselelectallmarkup();
        
        
        set_tool('pickPolygon', {p1 : false, p2 :  false, p3: true, p4 : false, p5 : true});
        //tool = new tools['pickPolygon'](multi);
    }


    function RxCore_GetVectorEntity(selected){
        if(docpagNotReady()){
            return;
        }
        if (documentcompare) {
            return;
        }

        if (!selected) {
            
            set_tool('markupedit', {p1 : false});
            
            return;
        }

        unselelectallmarkup();
        
        set_tool('pickPolygon', {p1 : false, p2 :  false, p3: true, p4 : true, p5 : false});
        //tool = new tools['pickPolygon'](multi);
    }

    function RxCore_PickPolygon(selected,multi){
        if(docpagNotReady()){
            return;
        }
        if (documentcompare) {
            return;
        }

        if (!selected) {
            
            set_tool('markupedit', {p1 : false});
            
            return;
        }

        unselelectallmarkup();
        set_tool('pickPolygon', {p1 : multi, p2 :  false, p3: true, p4 : false, p5 : false});
        //tool = new tools['pickPolygon'](multi);
    }


    function RxCore_SpaceEdit(){
        if(docpagNotReady()){
            return;
        }

        if(DocObj.getCurrentpage().usevector3Dxml){
            //can't use this with 3D
            return;
        }
        
        
        
        set_tool('markupedit', {p1 : true});

    }

    function RxCore_birdseye(){
        if(docpagNotReady()){
            return;
        }

        if(DocObj.getCurrentpage().usevector3Dxml){
            //can't use this with 3D
            return;
        }

        DocObj.setBirdseyeTool();
        //set_tool('markupedit', {p1 : false});

    }

    function RxCore_defaultAll(){

        if(docpagNotReady()){
            return;
        }


        /*if(DocObj == null || DocObj == undefined){
            return;
        }

        if(DocObj.getCurrentpage() == undefined ){
            return;
        }*/

        //var bnot3dmarkup = get3DViewNotActive();

        if(DocObj.getCurrentpage().usevector3Dxml){
        

            switch (DocObj.curcontrol3D) {
                case 'Walkthrough3D' : 
                    RxCore_3DWalkThrough(true);
                    break;
                case 'orbitControl' :
                    RxCore_3DOrbit(true);
                    break;
                case 'markupedit' :
                    set_tool('markupedit', {p1 : false});
                    break;
            }

            //if(DocObj.curcontrol3D == 
            //RxCore_restore3DInput();

            //can't use this with 3D
            return;
        }
                
        set_tool('markupedit', {p1 : false});

        
    }

    function RxCore_default() {

        if(docpagNotReady()){
            return;
        }


        /*if(DocObj == null || DocObj == undefined){
            return;
        }

        if(DocObj.getCurrentpage() == undefined ){
            return;
        }*/

        var bnot3dmarkup = get3DViewNotActive();

        if(DocObj.getCurrentpage().usevector3Dxml && bnot3dmarkup){
        
            //can't use this with 3D
            return;
        }

        unselectallBlocks();
                
        set_tool('markupedit', {p1 : false});
        
    }

    function RxCore_KeepvectorColor(OnOff){
        /*if (!documentopen) {
         return;
         }*/

        bKeepVectorColor = OnOff;

    }



    function RxCore_DrawPoints(){
        if(docpagNotReady()){
            return;
        }
        if (DocObj.getCurrentpage().usepdfjs) {

            DocObj.getCurrentpage().drawpoints(DocObj.getCurrentpage().PDFPointArray,DocObj.getCurrentpage().pdfSnapScale);

        }


    }

    function Rxcore_BackgroundColor(color){
        backgroundColor = color;
    }

    function RxCore_BackgroundCustomColor(color){
        if(docpagNotReady()){
            return;
        }

        backgroundCustomColor = color;
        bCustomColor = true;

        DocObj.backgroundColor = backgroundCustomColor;

        if (DocObj.getCurrentpage().usevectorxml) {
            DocObj.getCurrentpage().draw_vector(true);
        }
        drawmarkupAll(cntximg);


    }


    function RxCore_setdisplayBackground(color){
        displayBGColor = color;

        if(docpagNotReady()){
            return;
        }

        if (DocObj.getCurrentpage().usevectorxml) {
            DocObj.getCurrentpage().draw_vector(true);
        }
        drawmarkupAll(cntximg);


    }

    function RxCore_setMaxImageArea(num){
     
        nMaximageArea = num;
        
    }


    function RxCore_setMonoChrome(OnOff){
        if(docpagNotReady()){
            return;
        }

        if(documentcompare){
            return;
        }

        
        if (DocObj.getCurrentpage().usevectorxml) {

            RxCore_SetViewMode(OnOff);
            
            /*if (DocObj.backgroundColor == "#FFFFFF" || DocObj.backgroundColor == "#000000") {
                DocObj.getCurrentpage().invert_images(DocObj.backgroundColor);
            }*/

            DocObj.refreshBirdsEye();
            DocObj.renderBirdsEye();

        }
        drawmarkupAll(cntximg);

    }    



    function RxCore_togglebackground(changekeepvectorcolor) {
        if(docpagNotReady()){
            return;
        }


        if (DocObj.getCurrentpage().usepdfjs) {
            DocObj.backgroundColor = "#FFFFFF";
        } else {
            if(bCustomColor){
                if (DocObj.backgroundColor == backgroundCustomColor) {
                    DocObj.backgroundColor = "#A0A0A0";
                }else if (DocObj.backgroundColor == "#A0A0A0") {
                    DocObj.backgroundColor = "#FFFFFF";
                } else if (DocObj.backgroundColor == "#FFFFFF") {

                    DocObj.backgroundColor = "#000000";

                } else if (DocObj.backgroundColor == "#000000") {
                    DocObj.backgroundColor = backgroundCustomColor;

                    if(changekeepvectorcolor){
                        bKeepVectorColor = !bKeepVectorColor;
                    }

                    
                }
            }else{
                if (DocObj.backgroundColor == "#A0A0A0") {
                    DocObj.backgroundColor = "#FFFFFF";
                } else if (DocObj.backgroundColor == "#FFFFFF") {

                    DocObj.backgroundColor = "#000000";

                } else if (DocObj.backgroundColor == "#000000") {
                    DocObj.backgroundColor = "#A0A0A0";

                    if(changekeepvectorcolor){
                        bKeepVectorColor = !bKeepVectorColor;
                    }

                    
                }

            }

        }
        if (DocObj.getCurrentpage().usevectorxml) {

            if (DocObj.backgroundColor == "#FFFFFF" || DocObj.backgroundColor == "#000000") {
                DocObj.getCurrentpage().invert_images(DocObj.backgroundColor);
            }

            DocObj.getCurrentpage().draw_vector(true);
            DocObj.refreshBirdsEye();
            DocObj.renderBirdsEye();

        }
        drawmarkupAll(cntximg);
    }

    function resetCursor(){
        //set for all canvas elements
        
        //document.body.style.cursor = 'default';

        canvaso.style.cursor = 'default';
        renderer.domElement.style.cursor = 'default';
        canvimg.style.cursor = 'default';
        canvas.style.cursor = 'default';

    }



    function setCursor(cursor){

        canvaso.style.cursor = cursor;
        renderer.domElement.style.cursor = cursor;
        canvimg.style.cursor = cursor;
        canvas.style.cursor = cursor;

    }


    function set3DToolType(selected, type, params) {
        if(docpagNotReady()){
            return;
        }

        if (DocObj.getCurrentpage().usevector3Dxml) {
            //unselelectallmarkup();

            var has3dSelect = ((DocObj.curcontrol3D == 'Walkthrough3D') || (DocObj.curcontrol3D == 'orbitControl'));

            

            if(has3dSelect && type == 'select3d'){

                if(tool.name != 'Walkthrough3D' && tool.name != 'orbitControl'){
                    
                    if (params == null){
                        set_tool(DocObj.curcontrol3D, {});
                    }else{
                        set_tool(DocObj.curcontrol3D, params);
                    }
    
                }

                if(!selected){
                    //unselect all meshes.
                    DocObj.getCurrentpage().unselect3DBlockAll();
                }

                if (tool){
                    tool.setSelect(selected, params.p1);
                }


            }else{
                var toolType = (selected) ? type : DocObj.curcontrol3D;

                resetCursor();
    
                if(toolType != 'select3d'){
                    //unselect all meshes.
                    DocObj.getCurrentpage().unselect3DBlockAll();
                }
    
    
                unselelectallmarkup();
                unselecteditmarkupall();
                DocObj.getCurrentpage().unSelect3DmarkupAll();
    
                if(toolType == 'markupedit'){
                    params = {p1 : false};
                }

                //tool = new tools[toolType]();
                if (params == null){
                    set_tool(toolType, {});
                }else{
                    set_tool(toolType, params);
                }
    
            }

            
        }
    }

    function getMarkupCursor(sztool){

        var cursor = 'default';

        for(var ci = 0; ci < toolcursorArr.length; ci++){
            
            if(sztool == toolcursorArr[ci].name){
                cursor = toolcursorArr[ci].cursor;
            }
            //{name : 'hide3d', cursor : 'crosshair'},
        }

        return cursor;
    }

    function RxCore_setMarkupCursor(sztool, szcursor){

        for(var ci = 0; ci < toolcursorArr.length; ci++){
            
            if(sztool == toolcursorArr[ci].name){
                toolcursorArr[ci].cursor = szcursor;
            }
            //{name : 'hide3d', cursor : 'crosshair'},
        }

    }

    function RxCore_measure3D(selected){
        set3DToolType(selected, 'measure3D', null);

    }

    function RxCore_3DSelect(selected) {
        
        var params = {p1 : 0};

        set3DToolType(selected, 'select3d', params);
        
    }

    function RxCore_3DMarkupSelect(selected) {

        var params = {p1 : 1};

        set3DToolType(selected, 'select3d', params);
    }


    function RxCore_create3DTexture(image, w, h, id){
        
        textureobjects.push(new textureobject(image, w, h, id));


    }

    function RxCore_add3Dsymbol(selected, color){
        var params = {p1 : color};
        set3DToolType(selected, 'add3dsymbol',params);
    }

    function RxCore_3DToggleVisible(selected) {
        
        set3DToolType(selected, 'hide3d', null);
    }

    function RxCore_unSelectAllMarkup(){
        if(docpagNotReady()){
            return;
        }


        unselelectallmarkup();
        unselecteditmarkupall();

        if(RxCore_GUI_Markuplist != undefined){
            RxCore_GUI_Markuplist.setMarkupList(DocObj.getmarkuplist(),DocObj.bActive);
        }


    }

    function RxCore_setdimprecision(num){
        ndimprecision = num;
    }

    function RxCore_setDimPrecisionForPage(num){
        DocObj.getCurrentpage().scaleObject.setDimPrecision(num);
    }


    function RxCore_useNoScale(onoff){
        
        bUseNoScale = onoff;

    }

    function RxCore_usetextscale(onoff){
        
        bdotextscale = onoff;
    }


    function RxCore_UseFixedScale(onoff){
        
        bUseFixedScale = onoff;

    }

    function RxCore_UseFraction(onoff){
        
        bUseFraction = onoff;

    }
    
    function RxCore_UseOrthoEdit(onoff, degree){
        
        bOrthoOnEdit = onoff;
        nOrthoDegree = degree;

    }    


    function RxCore_UseOrtho(onoff, degree){
        
        bOrthoOn = onoff;
        nOrthoDegree = degree;

    }    

    function RxCore_disablewelcome(onoff){
        bwelcomedisabled = onoff;   
    }

    function RxCore_forceUniqueMarkup(onoff){
        bAlwaysassignUniqueID = onoff;
    }

    function RxCore_SetGUIDMarkupSelected(){
        if(docpagNotReady()){
            return;
        }
        if(readonlymode){
            return;
        }

        var markupobject = getSelectedmarkup();

        if(!markupobject.isempty){
            markupobject.setRxUniqueID();
        }


    }

    function RxCore_AutoLoadThumbnails(onoff){
        bAutoloadThumbnails = onoff;
    }



    function RxCore_LoadThumbnails(){
        if(docpagNotReady()){
            return;
        }

        //DocObj.thumbnails = [];

        for(var i = 0; i < DocObj.thumbnails.length; i++){

            
            DocObj.thumbnails[i].thumbnailobj.source = DocObj.thumbnails[i].ThumbnailImageSRC;
            DocObj.thumbnails[i].thumbloaded = true;
            DocObj.thumbnails[i].thumbload();
            //DocObj.thumbnails.push(DocObj.pages[i].getThumbnail);

            //DocObj.pages[i].thumbnailobj.source = DocObj.pages[i].ThumbnailImageSRC;
            //DocObj.pages[i].thumbloaded = true;
            //DocObj.pages[i].thumbload();
            //DocObj.thumbnails.push(DocObj.pages[i].getThumbnail);
            
        }

       if (RxCore_GUI_pagethumbs != undefined) {
            RxCore_GUI_pagethumbs.setThumbnails(DocObj.thumbnails);
       }


    };

    function RxCore_LockMarkupbyGUID(GUID,onOff){
        if(docpagNotReady()){
            return;
        }

        for (var curdoc = 0;curdoc < OpenFiles.length;curdoc++){
            OpenFiles[curdoc].lockMarkupbyGUID(GUID, onOff);
        }


    }

    function RxCore_getMarkupXMLSelected(){

        if(docpagNotReady()){
            return;
        }

        var markupobject = getSelectedmarkup();

        //var selectedmarkup = {isempty : true};

        var markupxml = DocObj.getmarkupSelectedXML(markupobject);
        return markupxml;
        //var markupxml = OpenFiles[curdoc].getmarkupbyGUIDXML(GUID);
        

    }


    function RxCore_getMarkupByGUID(GUID){
        if(docpagNotReady()){
            return;
        }

        for (var curdoc = 0;curdoc < OpenFiles.length;curdoc++){
            var markupxml = OpenFiles[curdoc].getmarkupbyGUIDXML(GUID);
        }


    }
    /*function RxCore_getMarkupXMLByGUID(GUID){
        if (!documentopen) {
            return;
        }

        for (var curdoc = 0;curdoc < OpenFiles.length;curdoc++){
            var markupxml = OpenFiles[curdoc].getmarkupbyGUIDXML(GUID);
        }

        return markupxml;
    }*/

    function RxCore_getMarkupXMLByGUID(GUID){
        if(docpagNotReady()){
            return;
        }

        var document = '';
        for (var curdoc = 0;curdoc < OpenFiles.length;curdoc++){
            var markupxml = OpenFiles[curdoc].getmarkupbyGUIDXML(GUID);
            if(markupxml.found){
                document = markupxml.document;
            }
        }

        return document;
    }


    /*function RxCore_getmarkupbyGUID(GUID){
        if (!documentopen) {
            return;
        }

        for (var curdoc = 0;curdoc < OpenFiles.length;curdoc++){
            var markupxml = OpenFiles[curdoc].getmarkupbyGUIDXML(GUID);
        }



    }*/
    

    function RxCore_getmarkupobjByGUID(GUID){
        if(docpagNotReady()){
            return;
        }
        return DocObj.getmarkupobjbyGUID(GUID);

    } 


    function RxCore_unSelectMarkupbyGUIDs(GUIDs){
        if(docpagNotReady()){
            return;
        }


        DocObj.unselectmarkupbyGUIDs(GUIDs);

        if (RxCore_GUI_Markuplist != undefined) {
            RxCore_GUI_Markuplist.setMarkupList(DocObj.getmarkuplist(), DocObj.bActive);
        }

        drawmarkupAll(cntximg);
        DrawMarkupSelected(context);

        /*var markupindxarr = DocObj.getmarkupbyGUIDs(GUIDs);

        for(var mi = 0;mi <  markupindxarr.length; mi++){
            //var markupnumber = DocObj.markuplist[markupindxarr[mi]].markupnumber;    
            RxCore_unSelectMarkupbyIndex(markupindxarr[mi], true);

        }*/

    }

    function RxCore_unSelectMarkupbyGUID(GUID){
        if(docpagNotReady()){
            return;
        }

        var indx = DocObj.getmarkupbyGUID(GUID);
        
        if(indx != -1){
            
            var markupnumber = DocObj.markuplist[indx].markupnumber;
            RxCore_unSelectMarkupbyIndex(markupnumber);

            

            /*if(RxCore_GUI_MarkupUnselect != undefined){
                var btextboxleader = (DocObj.markuplist[indx].type == 9 && DocObj.markuplist[indx].subtype == 2);

                if(btextboxleader){
                    RxCore_GUI_MarkupUnselect.setMarkupunSelected(DocObj.markuplist[indx].markupArrowConnected);
                }else{
                    RxCore_GUI_MarkupUnselect.setMarkupunSelected(DocObj.markuplist[indx]);
                }                

                RxCore_GUI_MarkupUnselect.setMarkupunSelected(DocObj.markuplist[indx]);
            }*/

        }
    }

    function RxCore_getmarkupGUIDs(){
        if(docpagNotReady()){
            return;
        }


        return DocObj.getannotGUIDs();

    }

    function RxCore_SelectMarkupbyGUIDs(GUIDs){
        if(docpagNotReady()){
            return;
        }

        if(!bMultiselect){
            return;
        }

        DocObj.selectmarkupbyGUIDs(GUIDs);

        drawmarkupAll(cntximg);
        DrawMarkupSelected(context);

        if (RxCore_GUI_Markuplist != undefined) {
            RxCore_GUI_Markuplist.setMarkupList(DocObj.getmarkuplist(), DocObj.bActive);
        }


        /*var markupindxarr = DocObj.getmarkupbyGUIDs(GUIDs);

        for(var mi = 0;mi <  markupindxarr.length; mi++){
            //var markupnumber = DocObj.markuplist[markupindxarr[mi]].markupnumber;    
            RxCore_SelectMarkupbyIndex(markupindxarr[mi], true);

        }*/

        
    }

    function RxCore_SelectMarkupbyGUID(GUID){
        if(docpagNotReady()){
            return;
        }

        var indx = DocObj.getmarkupbyGUID(GUID);
        

        if(indx == -1 || DocObj.markuplist[indx].markupnumber == undefined){
            RxCore_SelectMarkupbyIndex(-1);
        }else{
            var markupnumber = DocObj.markuplist[indx].markupnumber;
            RxCore_SelectMarkupbyIndex(markupnumber);

        }


        

    }

    function RxCore_unSelectMarkupbyIndex(indx, buseindex){
        if(docpagNotReady()){
            return;
        }



        if(buseindex){
            var arrindx = indx;
            if(DocObj.markuplist[arrindx] == undefined){
                return;
            }


        }else{
            arrindx = DocObj.getmarkupbynumber(indx);
        }

        
        if (DocObj.markuplist.length - 1 < arrindx ){
            return;
        }
        if(DocObj.markuplist[arrindx].locked){
            return;
        }

        if(buseindex){
            unselectmarkupindex(indx);
        }else{
            unselectmarkup(indx);
        }


        if (RxCore_GUI_Markuplist != undefined) {
            RxCore_GUI_Markuplist.setMarkupList(DocObj.getmarkuplist(), DocObj.bActive);
        }

        drawmarkupAll(cntximg);
        DrawMarkupSelected(context);

        

    }

    function RxCore_replaceMarkupImage(number, dropdata){

        if(docpagNotReady()){
            return;
        }

        if (DocObj.markuplist.length - 1 < indx ){
            return;
        }

        var markupobject = DocObj.getmarkupobjbynumber(number);

        if(markupobject != null){
            markupobject.replaceImage(dropdata);
        }


    }


    /*function RxCore_GetmarkupbyNumber(number){

        if(docpagNotReady()){
            return;
        }

        if (DocObj.markuplist.length < number ){
            return;
        }

        return DocObj.getmarkupobjbynumber(number);

    }*/

    function RxCore_GetmarkupbyNumber(number){
        if(docpagNotReady()){
            return;
        }
 
        // if (DocObj.markuplist.length < number ){
        //     return;
        // }
        let isFound = DocObj.markuplist.find(obj => obj.markupnumber === number);
        if (isFound === false){
            return;
        }
 
        return DocObj.getmarkupobjbynumber(number);
 
    }


    function RxCore_SelectMarkupIndex(indx){
        if(docpagNotReady()){
            return;
        }

        if (DocObj.markuplist.length - 1 < indx ){
            return;
        }
        
        /*if(DocObj.markuplist[indx].locked){
            return;
        }*/

        if(consolidateObj.isactive){
            unselectmarkup(indx);
        }else{
            if(!bMultiselect && !DocObj.getMarkupMultiselected()){
                unselelectallmarkup();
            }
            
        }

        if (DocObj.getcurPage() != DocObj.markuplist[indx].pagenumber) {
            DocObj.GotoPage(DocObj.markuplist[indx].pagenumber);
        }

        if (DocObj.markuplist[indx].selectedit) {
            DocObj.markuplist[indx].setselect(false);
        } else {
            DocObj.markuplist[indx].setselect(true);
            DocObj.markuplist[indx].lengthangleCallbackSelect();
			/* Andriy TODO: Commented as Temporarely fix */
//            DocObj.getCurrentpage().SetMarkupZoom(DocObj.markuplist[indx].docdx, DocObj.markuplist[indx].docdy, DocObj.markuplist[indx].docdscale);
            /*if (RxCore_GUI_Markup != undefined) {
                if(DocObj.markuplist[indx].signature == signature){
                    var operation = {created : false, modified : false, deleted : false};
                    var btextboxleader = (DocObj.markuplist[indx].type == 9 &&  DocObj.markuplist[indx].subtype == 2);
                    if(!btextboxleader){
                        //RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[indx], operation);
                    }
                    
                }


                /*if(RxCore_GUI_Markuplist != undefined){
                    RxCore_GUI_Markuplist.setMarkupList(DocObj.markuplist);
                }*/

                /* Andriy */
                //RxCore_GUI_Markuplist.notify();
            //}
            /*if (RxCore_GUI_Markuplist != undefined) {

                RxCore_GUI_Markuplist.setMarkupList(DocObj.markuplist,DocObj.bActive);

            }*/


        }

        drawmarkupAll(cntximg);
        DrawMarkupSelected(context);
    }

    function RxCore_getinitPDFScale(pwidth, pheight){
                
                var yscale = canvasoheight / (pheight);
                var xscale = canvasowidth / (pwidth);
                var dscale = Math.min(xscale, yscale);

                return dscale;
    }

    

    function drawcanvasext (cnv, transscale, xoffset, yoffset){
        if(cnv){
            imagewidth = cnv.width;
            imageheight = cnv.height;

            var dscale = 1.0;

            //imagewidth = splashimage.naturalWidth;
            //imageheight = splashimage.naturalHeight;


            //calculate zoom to extent initial scaling and offset
            //zoom to fit
            
            var dx = (canvasowidth - (imagewidth * dscale * transscale)) / 2;
            //var dy = (canvasoheight - (imageheight * dscale)) / 2;
            //var dx = 0.0;
            var dy = 0.0;

            if (!documentopen) {
                
                var localdx = 0.0;
                var localdy = 0.0;
                var localdscale = 1.0;
        
                
                localdx = dx + xoffset;
                localdy = dy + yoffset;
                localdscale = dscale;
                //contexto.save();
                //contexto.transform(transscale,0,0,transscale,0,0);
                if (transscale == 1){
                    contexto.drawImage(cnv, localdx, localdy, imagewidth * transscale, imageheight * transscale);
                }else{
                    var scaledcnv = downScaleCanvas(cnv, transscale);
                    contexto.drawImage(scaledcnv, localdx, localdy, imagewidth * transscale, imageheight * transscale);

                    scaledcnv = null;
                }
                

                
                //contexto.restore();
        
                
            }

        }
    }
    
    function RxCore_navigateBookmark(bookmark){

        if(docpagNotReady()){
            return;
        }


        if (documentopen){
            DocObj.navigateBookmark(bookmark);
        }

    }

    function RxCore_getBookmarks(){

        if(docpagNotReady()){
            return;
        }

        if (documentopen){
            DocObj.getPDFBookmarks();
        }

    }

    function RxCore_setBookmarks(bookmarks){
        if(docpagNotReady()){
            return;
        }

        if (documentopen){
            DocObj.setPDFBookmarks(bookmarks);
        }
        
    }

    function RxCore_setPDFLayers(PDFLayers){

        if(docpagNotReady()){
            return;
        }

        if (documentopen){
            DocObj.setPDFLayers(PDFLayers);
        }

    }


    function RxCore_checknewDoc(){

        if (foxitgetOpenDocs() == foxitgetnumDocs()){
            addfoxitdoc();
        }


    }

    function RxCore_setPageInfoDim(pagenum){
        if(docpagNotReady()){
            return;
        }

        
        if (DocObj){
            DocObj.getpageinfodim(pagenum);
        }

    }

    function RxCore_usePDFAnnotProxy(onOff){
        proxyPDFAnnots = onOff;
    }

    function RxCore_convertPDFAnnots(onOff){

        convertPDFAnnots = onOff;
  
      }
  


    function RxCore_FoxitAnnotlist(annotationlist) {
        if (docpagNotReady()) {
          return;
        }
  
        if(!convertPDFAnnots){
          return;
        }
  
        if (DocObj) {
          DocObj.setFoxitAnnotlist(annotationlist);
  
          if (RxCore_GUI_FoxitAnnotlist != undefined) {
            let annotList = [];
            let ct = 0;
            let annotreplies = [];
  
            for (let item of annotationlist) {
              //console.log(item);
              let annotItem;
              const jsonInfo = item.exportToJSON();
              //console.log(jsonInfo);
              const annotRect = item.getPage().getDeviceRect(item.getRect(),PixelToPoint);
              //console.log(annotRect);
  
  
              let txtcontent = jsonInfo.contents.replace("\r\r", '\n\n');
              txtcontent = txtcontent.replace("\r", '\n');
  
  
              switch (item.getType()) {
                case "freetext":
  
  
                if(jsonInfo.intent == "FreeTextCallout"){
  
                    if(item.info.calloutLinePoints){
                      
                      const startPointtxt = item.info.calloutLinePoints[0];
                      const endPointtxt = item.info.calloutLinePoints[2];
  
                      var startpointarrayclout = [startPointtxt.x, startPointtxt.y];
                      var endpointarrayclout = [endPointtxt.x, endPointtxt.y];
      
                      const deviceStartPointclout = item.getPage().getDevicePoint(startpointarrayclout,PixelToPoint);
                      const deviceEndPointclout = item.getPage().getDevicePoint(endpointarrayclout,PixelToPoint);
                      
                      var calloutarrow = new MarkupObject(6, 6, 0);
  
                      calloutarrow.linewidth = jsonInfo.width;
                      calloutarrow.arrowlength = 10 + calloutarrow.linewidth;
                      calloutarrow.x = deviceStartPointclout[0];
                      calloutarrow.y = deviceStartPointclout[1];
                      calloutarrow.w = deviceEndPointclout[0];
                      calloutarrow.h = deviceEndPointclout[1];
                      calloutarrow.pagenumber = jsonInfo.page;
                      calloutarrow.signature = Object.keys(jsonInfo).includes("title")  ? jsonInfo.title : null;
  
                      calloutarrow.pdfannotref = ct;
                      calloutarrow.pdfannot = true;
        
                      if(proxyPDFAnnots){
                        calloutarrow.pdfannotproxy = true;
                    }
    
    
                      calloutarrow.savemetolistLoad(DocObj);

                      if(!calloutarrow.pdfannotproxy){
                        item.getPage().removeAnnotById(item.getUniqueID());
                      }
    
                      

                      
  
  
                    }
  
  
                    if(item.info.innerRect){
                      const annotRectinner = item.getPage().getDeviceRect(item.info.innerRect,PixelToPoint);
  
  
    
                      annotItem = CreateTextRectdirect(
                        annotRectinner.left,
                        annotRectinner.top,
                        annotRectinner.right - annotRectinner.left,
                        annotRectinner.bottom - annotRectinner.top,
                        calloutarrow, 
                        txtcontent
                      );
  
                      annotItem.textcolor = jsonInfo.fontColor;
                      annotItem.fillcolor = "rgba(255,255,255, 0)";
                      annotItem.font = new FontObject(item.info.fontName, (item.info.defaultAppearance.textSize * PixelToPoint));
  
                    }
  
  
                  }else{
                    annotItem = new MarkupObject(9, 1, 0);
                    annotItem.text = txtcontent;
                    annotItem.textcolor = jsonInfo.fontColor;
                    annotItem.strokecolor = jsonInfo.color;
                    annotItem.fillcolor = "rgba(255,255,255, 0)";
                    annotItem.font = new FontObject(item.info.fontName, (item.info.defaultAppearance.textSize * PixelToPoint));
    
                    annotItem.x = annotRect.left;
                    annotItem.y = annotRect.top;
                    annotItem.w = annotRect.right - annotRect.left;
                    annotItem.h = annotRect.bottom - annotRect.top;
    
                  }
                  
  
  
                  break;
  
                case "text":
                  if(jsonInfo.inreplyto == undefined){
                    annotItem = new MarkupObject(10, 0, 0);
                    annotItem.text = txtcontent;
    
                    annotItem.x = annotRect.left;
                    annotItem.y = annotRect.top;  
    
                    annotItem.w = noteimage.width;
                    annotItem.h = noteimage.height;
                  }else{
                    annotreplies.push({inreplyto : jsonInfo.inreplyto, text : txtcontent});
                  }
  
                  break;
  
                case "line":
                  const startPoint = item.getStartPoint();
                  const endPoint = item.getEndPoint();
                  var startpointarray = [startPoint.x, startPoint.y];
                  var endpointarray = [endPoint.x, endPoint.y];
  
                  const deviceStartPoint = item.getPage().getDevicePoint(startpointarray,PixelToPoint);
                  const deviceEndPoint = item.getPage().getDevicePoint(endpointarray,PixelToPoint);
  
  
                  if(jsonInfo.intent == "LineArrow"){
  
                    var subtype = 0;
                    
  
                    if((jsonInfo.tail == "OpenArrow") && (jsonInfo.head == "OpenArrow")){
                      subtype = 2;
                    }
                      
                    annotItem = new MarkupObject(6, subtype, 0);
                    annotItem.linewidth = jsonInfo.width;
                    annotItem.arrowlength = 10 + annotItem.linewidth;
                    annotItem.x = deviceStartPoint[0];
                    annotItem.y = deviceStartPoint[1];
                    annotItem.w = deviceEndPoint[0];
                    annotItem.h = deviceEndPoint[1];
  
                    annotItem.strokecolor = jsonInfo.color;
      
                  } else if(jsonInfo.intent == "LineDimension") {
                    annotItem = new MarkupObject(7, 0, 0);
                    
                    annotItem.x = deviceStartPoint[0];
                    annotItem.y = deviceStartPoint[1];
                    annotItem.w = deviceEndPoint[0];
                    annotItem.h = deviceEndPoint[1];
  
                    annotItem.strokecolor = jsonInfo.color;
  
                    DocObj.scalesOptions = [{value: jsonInfo.measureRatio, label: jsonInfo.measureRatio}];
    
                  } else{
                    annotItem = new MarkupObject(1, 1, 0);
    
                    annotItem.addpoint(deviceStartPoint[0], deviceStartPoint[1]);
                    annotItem.addpoint(deviceEndPoint[0], deviceEndPoint[1]);
  
                    annotItem.strokecolor = jsonInfo.color;
    
                  }
  
                  break;
                case "circle":
                  annotItem = new MarkupObject(4, 0, 0);
  
                  annotItem.x = annotRect.left;
                  annotItem.y = annotRect.top;
                  annotItem.w = annotRect.right - annotRect.left;
                  annotItem.h = annotRect.bottom - annotRect.top;
  
                  annotItem.strokecolor = jsonInfo.color;
    
  
                  break;
                case "square":
  
                //console.log(jsonInfo);
                //break;
                  if(jsonInfo.intent != "SquareImage"){
                    annotItem = new MarkupObject(3, 0, 0);
  
                    annotItem.x = annotRect.left;
                    annotItem.y = annotRect.top;
                    annotItem.w = annotRect.right - annotRect.left;
                    annotItem.h = annotRect.bottom - annotRect.top;
  
                    if(jsonInfo.style == "solid" && jsonInfo.interiorColor){
                      //annotItem.fillcolor = jsonInfo.interior-color;
                      annotItem.fillcolor = convertHex(jsonInfo.interiorColor, jsonInfo.opacity * 80);
                      annotItem.alternative = 1;
                      
                    }
  
  
                    annotItem.strokecolor = jsonInfo.color;
  
                    //if(jsonInfo.solid)
  
                  }else{
                    annotItem = new MarkupObject(3, 0, 0);

                    annotItem.x = annotRect.left;
                    annotItem.y = annotRect.top;
                    annotItem.w = annotRect.right - annotRect.left;
                    annotItem.h = annotRect.bottom - annotRect.top;

                    annotItem.strokecolor = jsonInfo.color;
                    annotItem.pdfannotproxy = true;
                    annotItem.pdfannotproxyType = "image";

                  }
  
                 break;
  
                case "ink":
                  annotItem = new MarkupObject(0, 0, 0);
                  const inkList = item.getInkList();
                  for(let index = 0 ; index < inkList.length ; ++ index) {
                    const point = inkList[index];
                    const pointarray = [point.x, point.y];
                    const devicePoint = item.getPage().getDevicePoint(pointarray, PixelToPoint);
                    annotItem.addpoint(devicePoint[0], devicePoint[1]);
                  }
                  annotItem.addline();
                  console.log(annotItem.pointlist);
  
                  annotItem.strokecolor = jsonInfo.color;
  
                  break;
  
                case "polygon":
                  if(jsonInfo.intent === "PolygonDimension") {
                    annotItem = new MarkupObject(8, 0, 0);
                  }
                  else if(jsonInfo.style === "solid") {
                    
                    if(jsonInfo.interiorColor){
                      annotItem = new MarkupObject(1, 2, 1);
                      annotItem.fillcolor = jsonInfo.interiorColor;
                    }else{
                      annotItem = new MarkupObject(1, 2, 0);
                      annotItem.fillcolor = jsonInfo.interiorColor;
  
                    }
                    
                    
                    
  
  
                  } else if(jsonInfo.style === "cloudy") {
                    annotItem = new MarkupObject(1, 7, 0);
                    
                  }
  
                  const vertexes = item.getVertexes();
                  for(let index = 0 ; index < vertexes.length ; ++ index) {
                    const point = vertexes[index];
                    const pointarray = [point.x, point.y];
                    const devicePoint = item.getPage().getDevicePoint(pointarray, PixelToPoint);
                    annotItem.addpoint(devicePoint[0], devicePoint[1]);
                  }
  
                  annotItem.strokecolor = jsonInfo.color;
                  
                  break;
  
                case "polyline":
                  if(jsonInfo.intent === "PolyLineDimension") {
                    annotItem = new MarkupObject(1, 3, 0);
                  } else {
                    annotItem = new MarkupObject(1, 1, 0);
                  }
                  const vertices = item.getVertexes();
                    for(let index = 0 ; index < vertices.length ; ++ index) {
                      const point = vertices[index];
                      const pointarray = [point.x, point.y];
                      const devicePoint = item.getPage().getDevicePoint(pointarray, PixelToPoint);
                      annotItem.addpoint(devicePoint[0], devicePoint[1]);
                    }
                    
                    annotItem.strokecolor = jsonInfo.color;
  
                  break;
  
                case "stamp":


                    annotItem = new MarkupObject(3, 0, 0);

                    annotItem.x = annotRect.left;
                    annotItem.y = annotRect.top;
                    annotItem.w = annotRect.right - annotRect.left;
                    annotItem.h = annotRect.bottom - annotRect.top;

                    annotItem.strokecolor = jsonInfo.color;
                    annotItem.pdfannotproxy = true;
                    annotItem.pdfannotproxyType = "stamp";


                  //annotItem = new MarkupObject(11, 1, 0);
  
                  //var imgbuf = item.getImage('buffer');
  
                  //const offscreen = new OffscreenCanvas(256, 256);
  
                  //DocObj.foxitdoc.getStampImage(item);
  
  
                  /*const imageBuf = item.getImage("buffer");
                  const blob = new Blob([imageBuf], {type: 'image/png'});
                  const url = URL.createObjectURL(blob);
                  console.log(url);
                  annotItem.imagehref = url;*/
                  break;
                case "strikeout" :

                    annotItem = new MarkupObject(3, 0, 0);

                    annotItem.x = annotRect.left;
                    annotItem.y = annotRect.top;
                    annotItem.w = annotRect.right - annotRect.left;
                    annotItem.h = annotRect.bottom - annotRect.top;

                    annotItem.strokecolor = jsonInfo.color;
                    annotItem.pdfannotproxy = true;
                    annotItem.pdfannotproxyType = "strikeout";


                    break;
                case "highlight" :
                    annotItem = new MarkupObject(3, 0, 0);

                    annotItem.x = annotRect.left;
                    annotItem.y = annotRect.top;
                    annotItem.w = annotRect.right - annotRect.left;
                    annotItem.h = annotRect.bottom - annotRect.top;

                    annotItem.strokecolor = jsonInfo.color;
                    annotItem.pdfannotproxy = true;
                    annotItem.pdfannotproxyType = "highlight";


                    break;
                case "underline" :

                    annotItem = new MarkupObject(3, 0, 0);

                    annotItem.x = annotRect.left;
                    annotItem.y = annotRect.top;
                    annotItem.w = annotRect.right - annotRect.left;
                    annotItem.h = annotRect.bottom - annotRect.top;

                    annotItem.strokecolor = jsonInfo.color;
                    annotItem.pdfannotproxy = true;
                    annotItem.pdfannotproxyType = "underline";

                    break;
        
              }
              
  
              if(annotItem != undefined){
                annotItem.pdfannotref = ct;
                annotItem.pdfannot = true;
                annotItem.title = Object.keys(jsonInfo).includes("title")  ? jsonInfo.title : null;
  
                if(!GetUserexist(annotItem.title)){
                  Userlist.push(new Users(annotItem.title, annotItem.title, markuplayer, markupcolor, false));
                }
  
                annotItem.signature = Object.keys(jsonInfo).includes("title")  ? jsonInfo.title : null;
                annotItem.timestamp = item.info.creationTimestamp;
                annotItem.uniqueID = item.getUniqueID();
                annotItem.pagenumber = jsonInfo.page;
                
                annotItem.linewidth = jsonInfo.width;

                if(jsonInfo.style == "solid"){
                  annotItem.linestyle = 0;
                }
                if(jsonInfo.style == "dashed"){
                  annotItem.linestyle = 1;
                  annotItem.linewidth = 1;
                }
                
                if(proxyPDFAnnots){
                    annotItem.pdfannotproxy = true;
                }
                
                annotItem.color = jsonInfo.color;
  
                annotItem.savemetolistLoad(DocObj);

                if(!annotItem.pdfannotproxy){
                    item.getPage().removeAnnotById(item.getUniqueID());
                }

                
  
              }
              ct++;
            }
  
            for (let repl of annotreplies) {
              var markup = RxCore_getmarkupobjByGUID(repl.inreplyto);
              if(markup) {
                markup.AddCommentInternal(markup.comments.length, markup.signature, repl.text);
              } 
            }
          }
        }
    }
    function RxCore_checkLargePDF(numpages){
        if(docpagNotReady()){
            return;
        }

        if (DocObj){
            DocObj.setLargePDF(numpages);
        }
        
    }

    function RxCore_createTempcanvArray(foxitpageobj){
        if (documentcompare) {

            if(CompareObj){
                //CompareObj.createTempcanvArray(foxitpageobj);
            }

            //return foxitpageobj;
        }
        if(docpagNotReady()){
            return;
        }


        if (DocObj){
            DocObj.createTempcanvArray(foxitpageobj);
        
           //RxCore_default();
        }
        
        
    }

    function RxCore_foxitonScrollupdate(pagepos,pagenumber){
        if(docpagNotReady()){
            return;
        }

        if (DocObj){
            if(pagepos){
                DocObj.foxitonScrollupdate(pagepos, pagenumber);
            }
            
        }   

    }

    function RxCore_foxitcalibratepagemarkupEx(pagepos, scale, pagenumber){
        if(docpagNotReady()){
            return;
        }

        if (DocObj){
            DocObj.foxitcalibratepagemarkupEx(pagepos, scale, pagenumber);
        }   

        
    }

    function RxCore_foxitcalibratepagemarkup(pagepos,pagenumber){
        if(docpagNotReady()){
            return;
        }

        if (DocObj){
            DocObj.foxitcalibratepagemarkup(pagepos, pagenumber);
        }

        
    }

    function RxCore_setfoxitPageSizeEx(foxitpageobj){
        if(docpagNotReady()){
            return;
        }

        if (DocObj){
            DocObj.setfoxitPageSizeEx(foxitpageobj);
            
        }
   
    }


    function RxCore_setfoxitPageSize(foxitpageobj){
        if(docpagNotReady()){
            return;
        }

        if (DocObj){
            DocObj.setfoxitPageSize(foxitpageobj);
            //RxCore_default();
        }
   
    }

    function RxCore_foxitcalibratepagePosX(pagenum, bsetend){
        if(docpagNotReady()){
            return;
        }

        if (DocObj){
            DocObj.foxitcalibratepagePosX(pagenum, bsetend);
            //RxCore_default();
        }


    }


    function RxCore_foxitcalibratepagePosY(pagenum){
        if(docpagNotReady()){
            return;
        }

        if (DocObj){
            DocObj.foxitcalibratepagePosY(pagenum);
            //RxCore_default();
        }


    }

    function RxCore_createPageTileArray(foxitpageobj){
        
        if (documentcompare) {
            
            if(CompareObj){
                CompareObj.createPageTileArray(foxitpageobj);
            }
    
            
            RxCore_default();

            return;
        }
        if(docpagNotReady()){
            return;
        }


        if (DocObj){
            DocObj.createPageTileArray(foxitpageobj);
            //RxCore_default();
        }

    }

    /*function RxCore_drafoxitPages(cnv, transscale, pagescale, pagewidth, pageheight){
        if (documentopen){
            DocObj.drawfoxitPages(cnv, transscale, pagescale, pagewidth, pageheight);
            RxCore_default();
        }

    }*/

    function RxCore_drawextcanvas(cnv, transscale){

            contexto.fillStyle = "rgb(238,243,250)";
            contexto.fillRect(0, 0, canvasowidth, canvasoheight);

            var xoffset = 0.0;
            var yoffset = 0.0;
            

            if(cnv.length){
                if (cnv.length > 0){
                    for (var cc = 0; cc < cnv.length; cc++){
                        if (cnv[cc].tagName.toLowerCase() == "canvas"){
                            drawcanvasext(cnv[cc], transscale, xoffset, yoffset);
                            xoffset += cnv[cc].width;
                        }
                    }
                }
            }

            

        
    }

    function RxCore_select3DMarkUpByID(id){
        if(docpagNotReady()){
            return;
        }


        if (DocObj.getCurrentpage().usevector3Dxml) {
            DocObj.getCurrentpage().select3Dmarkup(id);
        }

    }

    function RxCore_hideAllCanvas(onOff){

        if(onOff){
            //canvaso.style.visibility = 'hidden';
            //canvaso.style.zIndex = "0";

            canvimg.style.visibility = 'hidden';
            canvimg.style.zIndex = "0";

            canvas.style.visibility = 'hidden';
            canvas.style.zIndex = "0";

            magcanvas.style.zIndex = "0";

            
        }else{
            //canvaso.style.visibility = 'visible';
            //canvaso.style.zIndex = "3";

            canvimg.style.visibility = 'visible';
            canvimg.style.zIndex = "3";

            canvas.style.visibility = 'visible';
            canvas.style.zIndex = "3";

            //canvas.style.visibility = 'visible';
            magcanvas.style.zIndex = "3";


        }        

    }
    function RxCore_hidedisplayCanvas(onOff){
        if(onOff){
            canvaso.style.visibility = 'hidden';
            canvaso.style.zIndex = "0";

            
        }else{
            canvaso.style.visibility = 'visible';
            canvaso.style.zIndex = "3";

        }
        

    }
    

    function RxCore_savePDF(){
        if(docpagNotReady()){
            return;
        }

        if (DocObj){
        
            if(DocObj.foxitdoc != null){
                
                DocObj.foxitdoc.savePDF();
            }
            
        
           
        }

    }   


    function RxCore_getCanvasContextMenuEvent(){

        if(RxCore_GUI_ContextMenuEvt != undefined){
            RxCore_GUI_ContextMenuEvt.setEvent(canvas);
        }
        //return  canvas.oncontextmenu;
    }

    function RxCore_setMarkupMarkedColor(color){
        szmarkColor = color;
    }

    function delayedPan(markupobject){

        if (DocObj.getcurPage() != markupobject.pagenumber) {
                
            CheckPageloaded(markupobject.pagenumber).then(function(pagenum){

        
                DocObj.panToMarkup(markupobject);

            });
            //DocObj.GotoPage(DocObj.markuplist[arrindx].pagenumber);

        }else{
            DocObj.panToMarkup(markupobject);
        }


    }



    function RxCore_SelectMarkupbyIndex(indx, buseindex) {
        if(docpagNotReady()){
            return;
        }

        /*if (DocObj.markuplist.length - 1 < indx ){
            return;
        }*/

        var bnoselecttool = true;

        if(buseindex){
            var arrindx = indx;
            if(DocObj.markuplist[arrindx] == undefined){
                return;
            }


        }else{
            arrindx = DocObj.getmarkupbynumber(indx);
        }
                
        if(arrindx == -1){
            return;
        }

        var bmselected = (DocObj.markuplist[arrindx].selected || DocObj.markuplist[arrindx].selectedit);

        if(DocObj.markuplist[arrindx].locked){
            return;
        }


        if (tool){
            if(tool.name == 'markupedit'){
                
                bnoselecttool = false;

            }else{
                bnoselecttool = true;

            }

        }

        if(bnoselecttool){
            return;
        }


        //if(tool.name == )

        /*if(DocObj.markuplist[arrindx].bisTextArrow || DocObj.markuplist[arrindx].bhasArrow){
            return;
        }*/

        /*if(DocObj.markuplist[indx].markupnumber == indx){
            RxCore_SelectMarkupIndex(indx);
            return;
        }else{
            

        }*/


        if(consolidateObj.isactive){
            unselectmarkup(indx);

            if(!bMultiselect && !DocObj.getMarkupMultiselected()){
                unselelectallmarkup();
                unselecteditmarkupall();
            }


        }else{
            if(!bMultiselect && !DocObj.getMarkupMultiselected()){
                unselelectallmarkup();
                unselecteditmarkupall();
            }
            
        }

        /*if (DocObj.getcurPage() != DocObj.markuplist[arrindx].pagenumber) {
            
            if(DocObj.foxitdoc != null){
                DocObj.GotoPage(DocObj.markuplist[arrindx].pagenumber);

            }else{
                DocObj.GotoPage(DocObj.markuplist[arrindx].pagenumber);
            }
            
        }*/

        if(DocObj.markuplist[arrindx].viewname != null && DocObj.markuplist[arrindx].bfor3D){
            
            var curviewname = DocObj.getcur3DView();
            
            if(curviewname != DocObj.markuplist[arrindx].viewname){
                DocObj.getCurrentpage().restoreCameraByName(DocObj.markuplist[arrindx].viewname);
            }

            //rectmarkupobj.bfor3D = true;

        }


        if(DocObj.markuplist[arrindx].signature != signature){


            DocObj.markuplist[arrindx].setmarked(true);

            if (RxCore_GUI_Markuplist != undefined) {

                RxCore_GUI_Markuplist.setMarkupList(DocObj.getmarkuplist(),DocObj.bActive);
    
            }

            //set new markup selected indicator here.

            drawmarkupAll(cntximg);
            DrawMarkupSelected(context);
    

            if(busepantomarkup){
                delayedPan(DocObj.markuplist[arrindx]);
            }
            

            return;

        }



        if(DocObj.markuplist[arrindx].multipoint()){

            if (bmselected) {
                //DocObj.markuplist[arrindx].setselectedit(false);
                //unselectmarkup(arrindx);

            } else {
                DocObj.markuplist[arrindx].setselectedit(true);
                //DocObj.markuplist[arrindx].lengthangleCallbackSelect();
            }    
        }else{
            if (bmselected) {
                //DocObj.markuplist[arrindx].setselect(false);
                //unselectmarkup(arrindx);
            } else {
                DocObj.markuplist[arrindx].setselect(true);
                DocObj.markuplist[arrindx].lengthangleCallbackSelect();
            }    
        }

        if (RxCore_GUI_MarkupIndex != undefined && DocObj.markuplist[arrindx] != undefined) {

            var btextboxleader = (DocObj.markuplist[arrindx].type == 9 && DocObj.markuplist[arrindx].subtype == 2);
                
            if(btextboxleader){
                RxCore_GUI_MarkupIndex.setMarkupSelected(DocObj.markuplist[arrindx].markupArrowConnected, {created : false, modified : false, deleted : false});
            }else{
                RxCore_GUI_MarkupIndex.setMarkupSelected(DocObj.markuplist[arrindx], {created : false, modified : false, deleted : false});
            }

        }        


        //DocObj.pages[DocObj.markuplist[arrindx].pagenumber].panToMarkup(DocObj.markuplist[arrindx]);

        /*if (DocObj.markuplist[arrindx].selectedit) {
            DocObj.markuplist[arrindx].setselect(false);
        } else {
            DocObj.markuplist[arrindx].setselect(true);
            DocObj.markuplist[arrindx].lengthangleCallbackSelect();

        }*/            
			/* Andriy TODO: Commented as Temporarely fix */
//            DocObj.getCurrentpage().SetMarkupZoom(DocObj.markuplist[indx].docdx, DocObj.markuplist[indx].docdy, DocObj.markuplist[indx].docdscale);
            /*if (RxCore_GUI_Markup != undefined) {
                if(DocObj.markuplist[indx].signature == signature){
                    var operation = {created : false, modified : false, deleted : false};
                    var btextboxleader = (DocObj.markuplist[indx].type == 9 &&  DocObj.markuplist[indx].subtype == 2);
                    if(!btextboxleader){
                        //test remove callback to prevent multiple calls
                        RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[indx], operation);
                    }
                    
                }


                /*if(RxCore_GUI_Markuplist != undefined){
                    RxCore_GUI_Markuplist.setMarkupList(DocObj.markuplist);
                }*/

                /* Andriy */
                //RxCore_GUI_Markuplist.notify();
            //}
            /*if (RxCore_GUI_Markuplist != undefined) {

                RxCore_GUI_Markuplist.setMarkupList(DocObj.markuplist,DocObj.bActive);

            }*/

            if (RxCore_GUI_Markuplist != undefined) {

                RxCore_GUI_Markuplist.setMarkupList(DocObj.getmarkuplist(),DocObj.bActive);
    
            }
    


        

        drawmarkupAll(cntximg);
        DrawMarkupSelected(context);

        if(busepantomarkup){
            delayedPan(DocObj.markuplist[arrindx]);
        }




        //DocObj.setMarkupMultiselected();
        /*if(RxCore_GUI_Markuplist != undefined){
         RxCore_GUI_Markuplist.setMarkupList(DocObj.markuplist);
         }*/

        //console.log('selectmarkupbyindex');
        
        

    }


    function getTouchPos(canvas, evt, num) {
        
        const scale = getPixelRatio();
        var rect = canvas.getBoundingClientRect();

        var x;// = evt.targetTouches[num].pageX - rect.left;
        var y;// = evt.targetTouches[num].pageY - rect.top;

        if(evt && evt.targetTouches && evt.targetTouches[num]) {
            x = evt.targetTouches[num].pageX - rect.left;
            y = evt.targetTouches[num].pageY - rect.top;
        } else if(evt && evt.changedTouches && evt.changedTouches[num]) {
            x = evt.changedTouches[num].pageX - rect.left;
            y = evt.changedTouches[num].pageY - rect.top;
        } 

        return {
            x: x * scale,
            y: y * scale,
            xus:x,
            yus:y,
            xpadding:x * scale,
            ypadding:y * scale,
            busepadding : false
        };
    }


    function getPointPos(canvas, point){
        
        const scale = getPixelRatio();
        var rect = canvas.getBoundingClientRect();

        
        
        return {
            x: (point.x - rect.left) * scale,
            y: (point.y - rect.top) * scale,
            xus : point.x - rect.left,
            yus : point.y - rect.top,
            xpadding:(evt.clientX - rect.left) * scale,
            ypadding:(evt.clientY - rect.top) * scale,
            busepadding : false
        };

    }


    function getMousePos(canvas, evt) {
        
        const scale = getPixelRatio();
        var rect = canvas.getBoundingClientRect();
        
        return {
            x:(evt.clientX - rect.left) * scale,
            y:(evt.clientY - rect.top) * scale,
            xus:(evt.clientX - rect.left),
            yus:(evt.clientY - rect.top),
            xpadding:(evt.clientX - rect.left) * scale,
            ypadding:(evt.clientY - rect.top) * scale,
            //x:evt.clientX - rect.left,
            //y:evt.clientY - rect.top,
            //xpadding:evt.clientX - rect.left,
            //ypadding:evt.clientY - rect.top,
            busepadding : false
        };
    }


    function getRotPoint(width, height, x, y, anglerad){
        var cosangle = Math.cos(anglerad);
        var sinangle = Math.sin(anglerad);

        var hw = x - width;
        var hh = y - height;

        var newx = (hw * cosangle) - (hh * sinangle);
        var newy = (hw * sinangle) + (hh * cosangle);

        var transpoint = new point(newx, newy);
        transpoint.x = width + transpoint.x;
        transpoint.y = height + transpoint.y;
        return transpoint;


    }

function getMarkupDrawRotPoint(markuprect){

    if(docpagNotReady()){
        return markuprect;
    }


    var centercanvX = (canvasowidth / 2);
    var centercanvY = (canvasoheight / 2);
    var CanvRotRad =  (90 - DocObj.getCurrentpage().getpageRotation()) * (Math.PI / 180);


    if (DocObj.getCurrentpage().getpageRotation() != 0) {
        var startpoint = getRotPoint(centercanvX, centercanvY, markuprect.x, markuprect.y, CanvRotRad);
        markuprect.x = startpoint.x;
        markuprect.y = startpoint.y;
        var endpoint = getRotPoint(centercanvX, centercanvY, markuprect.w, markuprect.h, CanvRotRad);
        markuprect.w = endpoint.x;
        markuprect.y = endpoint.y;

    }


    return markuprect;

}

function getPageRotCorner(cornerpoint){
    if (!documentopen) {
        return cornerpoint;
    }

    var centercanvX = (canvasowidth / 2);
    var centercanvY = (canvasoheight / 2);

    var CanvRotRad = DocObj.getCurrentpage().getpageRotation() * (Math.PI / 180);

    if (DocObj.getCurrentpage().getpageRotation() != 0) {
        var rotmousepoint = getRotPoint(centercanvX, centercanvY, cornerpoint.x, cornerpoint.y, CanvRotRad);
        cornerpoint.x = rotmousepoint.x;
        cornerpoint.y = rotmousepoint.y;
    }

    return cornerpoint;
}

function getDocRotMousePoint(mousepoint){

        if (!documentopen) {
            return mousepoint;
        }

        var centercanvX = (canvasowidth / 2);
        var centercanvY = (canvasoheight / 2);

        var pagerot = DocObj.getCurrentpage().getpageRotation();

        var CanvRotRad = pagerot * (Math.PI / 180);

        switch(pagerot){
            case 0:
                CanvRotRad = pagerot * (Math.PI / 180);
                break;
            case 90:
                CanvRotRad = (pagerot + 180) * (Math.PI / 180);
                break;
            case 270:
                CanvRotRad = (pagerot - 180) * (Math.PI / 180);
                break;
            case 180:
                CanvRotRad = pagerot * (Math.PI / 180);
                break;
        }

        
        if (pagerot != 0) {
            var rotmousepoint = getRotPoint(centercanvX, centercanvY, mousepoint.x, mousepoint.y, CanvRotRad);
            mousepoint.x = rotmousepoint.x;
            mousepoint.y = rotmousepoint.y;
        }

        return mousepoint;

    }


    function drawpoints(ctx, PDFpageRotate, snapPoint){
        if (DocObj.getCurrentpage().getpageRotation() != 0) {
            var txcanv = (canvasowidth / 2);
            var tycanv = (canvasoheight / 2);
            ctx.translate(txcanv, tycanv);
            ctx.rotate(DocObj.getCurrentpage().getpageRotation() * (Math.PI / 180));
            ctx.translate(-txcanv, -tycanv);

        }
        if(PDFpageRotate != 0){
            var txcanv = (canvasowidth / 2);
            var tycanv = (canvasoheight / 2);
            ctx.translate(txcanv, tycanv);
            ctx.rotate(PDFpageRotate * (Math.PI / 180));
            ctx.translate(-txcanv, -tycanv);
        } 

        ctx.lineWidth = 1;
        ctx.strokeStyle = 'blue';
        //ctx.clearRect(snapPoint.x - 50,snapPoint.y - 50,100,100);
        ctx.strokeRect(snapPoint.x - 5, snapPoint.y - 5, 10, 10);


    }

    function drawsnap(ctx,snapPoint){
        ctx.save();
        if (DocObj.getCurrentpage().getpageRotation() != 0) {

            if(DocObj.foxitdoc != null){
                //draw non rotated for Foxit.
            }else{
                var txcanv = (canvasowidth / 2);
                var tycanv = (canvasoheight / 2);
                ctx.translate(txcanv, tycanv);
                ctx.rotate(DocObj.getCurrentpage().getpageRotation() * (Math.PI / 180));
                ctx.translate(-txcanv, -tycanv);
            }

        }
        ctx.lineWidth = 2;
        ctx.strokeStyle = 'blue';
        ctx.clearRect(snapPoint.x - 50,snapPoint.y - 50,100,100);

        var doublesnapsize = nSnapSize * 2;

        //nSnapSize

        if(snapPoint.type == snapEndpoint){
            
            ctx.strokeRect(snapPoint.x - nSnapSize, snapPoint.y - nSnapSize, doublesnapsize, doublesnapsize);
        }
        if(snapPoint.type == snapIntersection){
            ctx.beginPath();
            
            ctx.moveTo(snapPoint.x - nSnapSize, snapPoint.y - nSnapSize);
            ctx.lineTo(snapPoint.x + nSnapSize, snapPoint.y + nSnapSize);
            
            ctx.moveTo(snapPoint.x + nSnapSize, snapPoint.y - nSnapSize);
            ctx.lineTo(snapPoint.x - nSnapSize, snapPoint.y + nSnapSize);

            ctx.stroke();
        }

        if(snapPoint.type == snapMidpoint){
            ctx.beginPath();
            
            ctx.moveTo(snapPoint.x, snapPoint.y - nSnapSize);
            ctx.lineTo(snapPoint.x + nSnapSize, snapPoint.y + nSnapSize);
            
            ctx.lineTo(snapPoint.x - nSnapSize, snapPoint.y + nSnapSize);
            ctx.lineTo(snapPoint.x, snapPoint.y - nSnapSize);

            ctx.stroke();
        }


        ctx.restore();

    }


    function set_tool(toolname,params){

        
        //for (var i = 0; i < params.keys(obj).length; i++);
        if (tool){
            if(tool.name == 'pencil' && tool.started){
                tool.apply();

                unselelectallmarkup();
                unselecteditmarkupall();

            }

        }

        var bnot3dmarkup = get3DViewNotActive();
        var docis3d = get3DDoc();
        var bdefaulttool = (toolname == 'markupedit');



        if(toolname == 'orbitControl' || toolname == 'Walkthrough3D'){
            bCameramove = true;

            if(DocObj.getCurrentpage().Vector3DPageObj != null){
                DocObj.getCurrentpage().Vector3DPageObj.setcur3DViewnull();
            }


        }else{
            
            if(bnot3dmarkup && docis3d){
                bCameramove = true;    
            }else{
                bCameramove = false;
            }
            
            
        }

        if(bnot3dmarkup && docis3d && bdefaulttool){
            
            toolname = DocObj.curcontrol3D;
        }


        
        tool = new tools[toolname](params);
        
        if(tool.name == 'orbitControl'){
            //tool.setinit();
            if(DocObj.getCurrentpage().Vector3DPageObj != null){
                DocObj.getCurrentpage().Vector3DPageObj.setControl(tool);    
            }
        }
        if(tool.name == 'Walkthrough3D'){
            //tool.setPos();
            if(DocObj.getCurrentpage().Vector3DPageObj != null){
                DocObj.getCurrentpage().Vector3DPageObj.setControl(tool);    
            }
            
        }

    }

    function ev_canvas(ev) {
        //var touch_event = document.getElementById('shape');
        if (ev.layerX || ev.layerY == 0) { // Firefox
            ev._x = ev.layerX;
            ev._y = ev.layerY;
        } else if (ev.offsetX || ev.offsetY == 0) { // Opera
            ev._x = ev.offsetX;
            ev._y = ev.offsetY;
        }

        //touch_event.value = ev.type;
        if (tool == undefined) {
            return;
        }
        // Call the event handler of the tool.
        var func = tool[ev.type];
        if (func) {
            func(ev);
        }
    }

    function webglAvailable() {
        try {
            var canvas = document.createElement('canvas');
            return !!(window.WebGLRenderingContext && (
                canvas.getContext('webgl') ||
                    canvas.getContext('experimental-webgl')));
        } catch (e) {
            return false;
        }
    }

    function instanceReset(){
        initialized = false;
        readycalled = false;

        document.getElementById("rxcanvas").remove();
        document.getElementById("canv3D").remove();
        document.getElementById("imageDisp").remove();
        document.getElementById("imageTemp").remove();
        document.getElementById("imagemag").remove();
        //foxitreadycalled = false;
    

        //defaultLayout.offsetWidth = 0;
        //defaultLayout.offsetHeight = 0;


    }

    function readycheck(){

        if(configset && bLicenseAquired){
            console.log('rxcorejs ' + rxcoreversion);

            if (RxCore_GUI_Ready != undefined){
                RxCore_GUI_Ready.setupComplete(initialDoc);
                readycalled = true;
            }


            if(foxitreadycalled){
                if(initialDoc.index){
                    initFoxit(initialDoc.index);
                }else{
                    initFoxit();
                }

            }


            
    
            if (RxCore_GUI_Permissions != undefined){
                var permissions = {
                    consolidate : bCanConsolidate
                };
    
                RxCore_GUI_Permissions.UserPermissions(permissions);
            }
            
            configset = false;
        }
        
        
        //var configset = false;

    }

    function configcompleteJSON(configJSON){
        //set global variables from JSON TBD
        //not complete.

        if (SzContext != "") {
            //
        }
        /*if (szLicMode != "") {
            OEMFlag = szLicMode;
        }*/

        if (configJSON.imageswitchfactor) {
            imageswitchfactor = configJSON.imageswitchfactor;    
        }
        if (configJSON.signature) {
            signature = configJSON.signature;
        }
        if (configJSON.DisplayName) {
            DisplayName = configJSON.DisplayName;
        }

        if (configJSON.markuplayer) {
            markuplayer = configJSON.markuplayer;
        }

        if (configJSON.markupcolor) {
            markupcolor = configJSON.markupcolor;
            markuplinecolor = configJSON.markupcolor;
            markuptextcolor = configJSON.markupcolor;
        }

        setDefaultUser(signature, DisplayName, markuplayer, markupcolor);

        //Userlist[0] = new Users(signature, DisplayName, markuplayer, markupcolor, true);
        //numUsers++;


        if (RxCore_GUI_Users != undefined) {
            RxCore_GUI_Users.setUserlist(Userlist, true);
        }


        /*if (SzXmlUrl != "") {
            xmlurl = SzXmlUrl;
        }

        if (SzXmlUrlmarkup != "") {
            xmlurlmarkup = SzXmlUrlmarkup;
        }*/


        /*if (SzXMLURLRel != "") {
            xmlurlrel = SzXMLURLRel;
        }
        if (SzMarkupSaveUrl != "") {
            xmlurlmarkupsave = SzMarkupSaveUrl;
        }

        if(SzFileExport != ""){
            PDFExportURL = SzFileExport;
        }

        if(Sz3DImage != ""){
            CanvasSaveUrl = Sz3DImage;
        }
        //

        if (SzPrintPageURL != "") {
            PrintPageURL = SzPrintPageURL;
        }*/


        try {
            //var xmllayers = configxml.getElementsByTagName('layer');
            if (configJSON.layers.layer.length > 0) {
                for (var l = 0; l < configJSON.layers.layer.length; l++) {
                    //get layer attributes and create layer list.
                    var LayerName = configJSON.layers.layer[l].name;
                    var LayerColor = configJSON.layers.layer[l].color;
                    var LayerNumber = configJSON.layers.layer[l].number;
                    var LayerState = configJSON.layers.layer[l].state;
                    if (LayerState == 'on') {
                        var bState = true;
                    } else {
                        bState = false;
                    }
                    Layerlist[l] = new Layers(LayerNumber, LayerColor, LayerName, bState);
                }
            }
        } catch (e) {
            //ignore and continue
            createLayers();
        }
        readonlymode = configJSON.Readonlymarkup;


        //Paper = new PaperSize(SzPaperWidth, SzPaperHeight);

        configset = true;
        readycheck();

    }    

    function configcompleteResp(configxml){
        
        try {
            var SzContext = configxml.getElementsByTagName("Context")[0].firstChild.nodeValue;
        } catch (e) {
            //alert("Error getting Context - " + e);
            //getting context failed ignore and proceed
            //added try cactch for safari compliance
            SzContext = "";
        }
        try {
            var szLicMode = configxml.getElementsByTagName("Licensemode")[0].firstChild.nodeValue;

        } catch (e) {
            szLicMode = "";
        }

        try {
            var szLicCompany = configxml.getElementsByTagName("CompanyFeature")[0].firstChild.nodeValue;

        } catch (e) {
            szLicCompany = "";
            var bCompFeature = false;
        }

        bCompFeature = (szLicCompany != "");

        if(bCompFeature){
            bUseCompanyFeature = true;
            szLicenCompanyFeature = szLicCompany;
        }

        
        //<CompanyFeature>

        try {
            var szImgSwitchFactor = configxml.getElementsByTagName("ImgSwitchFactor")[0].firstChild.nodeValue;

        } catch (e) {
            imageswitchfactor = 1.5;
        }

        try {
            var SzCurrentUser = configxml.getElementsByTagName("CurrentUserName")[0].firstChild.nodeValue;
        } catch (e) {
            //ignore and continue
            SzCurrentUser = "";
        }
        try {
            var SzDisplayName = configxml.getElementsByTagName("DisplayName")[0].firstChild.nodeValue;
        } catch (e) {
            //ignore and continue
            SzDisplayName = "";
        }


        try {
            var SzMarkupLayer = configxml.getElementsByTagName("MarkupLayer")[0].firstChild.nodeValue;
        } catch (e) {
            //ignore and continue
            SzMarkupLayer = "";
        }

        try {
            var SzMarkupColor = configxml.getElementsByTagName("MarkupColor")[0].firstChild.nodeValue;
        } catch (e) {
            //ignore and continue
            SzMarkupColor = "";
        }


        try {
            var SzXmlUrl = configxml.getElementsByTagName("XmlUrl")[0].firstChild.nodeValue;
        } catch (e) {
            //ignore and continue
            SzXmlUrl = "";
        }

        try {
            var SzXmlUrlmarkup = configxml.getElementsByTagName("XmlUrlmarkup")[0].firstChild.nodeValue;
        } catch (e) {
            //ignore and continue
            SzXmlUrlmarkup = "";
        }

        try {
            var szMarkupPostonly = configxml.getElementsByTagName("MarkupSavePOSTOnly")[0].firstChild.nodeValue;
        } catch (e) {
            //ignore and continue
            szMarkupPostonly = "";
        }

        bMarkupPostOnly = (szMarkupPostonly != "" && szMarkupPostonly == "True");

        try {
            var SzXMLURLRel = configxml.getElementsByTagName("XmlUrlRel")[0].firstChild.nodeValue;
        } catch (e) {
            //ignore and continue
            SzXMLURLRel = "";
        }

        try {
            var SzMarkupSaveUrl = configxml.getElementsByTagName("MarkupSaveUrl")[0].firstChild.nodeValue;
        } catch (e) {
            //ignore and continue
            SzMarkupSaveUrl = "";
        }

        try {
            var SzFileExport = configxml.getElementsByTagName("ExportFile")[0].firstChild.nodeValue;
        } catch (e) {
            //ignore and continue
            SzFileExport = "";
        }

        try {
            var Sz3DImage = configxml.getElementsByTagName("Image3DSave")[0].firstChild.nodeValue;
        } catch (e) {
            //ignore and continue
            Sz3DImage = "";
        }

        try{
            /*<UseAuth>True</UseAuth>*/
            var SzUseAuth = configxml.getElementsByTagName("UseAuth")[0].firstChild.nodeValue;
        }catch (e){
            SzUseAuth = "";
        }

        try{
            /*<AuthName>Basic</AuthName>*/
            var SzAuthType = configxml.getElementsByTagName("AuthType")[0].firstChild.nodeValue;
        }catch (e){
            SzAuthType = "";
        }


        try{
            /*<AuthName>Basic</AuthName>*/
            var SzAuthName = configxml.getElementsByTagName("AuthName")[0].firstChild.nodeValue;
        }catch (e){
            SzAuthName = "";
        }

        try{
            /*<AuthPass>xxxxxx</AuthPass>*/           
            var SzAuthPass = configxml.getElementsByTagName("AuthPass")[0].firstChild.nodeValue;
        }catch (e){
            SzAuthPass = "";
        }


        try {
            var SzPrintPageURL = configxml.getElementsByTagName("PrintPageURL")[0].firstChild.nodeValue;
        } catch (e) {
            SzPrintPageURL = "printcanvas.htm";
        }

        try {
            var SzEnableMarkupEdit = configxml.getElementsByTagName("EnableMarkupEdit")[0].firstChild.nodeValue;
        } catch (e) {
            //ignore and continue
            SzEnableMarkupEdit = "";
        }
        try {
            var SzCanChangeSign = configxml.getElementsByTagName("CanChangeSignature")[0].firstChild.nodeValue;
        } catch (e) {
            //ignore and continue
            SzCanChangeSign = "";
        }
        try {
            var SzCanConsolidate = configxml.getElementsByTagName("CanConsolidate")[0].firstChild.nodeValue;
        } catch (e) {
            //ignore and continue
            SzCanConsolidate = "";
        }
        try {
            var SzCanChangeLayer = configxml.getElementsByTagName("CanChangeLayer")[0].firstChild.nodeValue;
        } catch (e) {
            //ignore and continue
            SzCanChangeLayer = "";
        }
        try {
            var SzReadonlymarkup = configxml.getElementsByTagName("Readonlymarkup")[0].firstChild.nodeValue;
        } catch (e) {
            //ignore and continue
            SzReadonlymarkup = "";
        }
        try {
            var SzEnableFileIOpen = configxml.getElementsByTagName("EnableFileOpen")[0].firstChild.nodeValue;
        } catch (e) {
            //ignore and continue
            SzEnableFileIOpen = "";
        }

        try {
            var SzReverseScale = configxml.getElementsByTagName("ReverseScale")[0].firstChild.nodeValue;
        } catch (e) {
            //ignore and continue
            SzReverseScale = "";
        }

        try {
            var SzDefaultPenTable = configxml.getElementsByTagName("DefaultPenTable")[0].firstChild.nodeValue;
        } catch (e) {
            //ignore and continue
            SzDefaultPenTable = "";
        }


        try {
            var SzIntitialDocument = configxml.getElementsByTagName("InitialDocument")[0].firstChild.nodeValue;
        } catch (e) {
            //ignore and continue
            SzIntitialDocument = "";
        }

        try {
            var SzUseCustom = configxml.getElementsByTagName("UseCustom")[0].firstChild.nodeValue;
        } catch (e) {
            //ignore and continue
            SzUseCustom = "";
        }

        try {
            var SzPaperWidth = configxml.getElementsByTagName("Paperwidth")[0].firstChild.nodeValue;
        } catch (e) {
            SzPaperWidth = "794";
        }
        try {
            var SzPaperHeight = configxml.getElementsByTagName("Paperheight")[0].firstChild.nodeValue;
        } catch (e) {
            SzPaperHeight = "1122";
        }


        try {
            var SzArrowSize = configxml.getElementsByTagName("ArrowSize")[0].firstChild.nodeValue;
        } catch (e) {
            SzArrowSize = "20";
        }

        nArrowSize = parseInt(SzArrowSize);

        try {
            var SzLabelSize = configxml.getElementsByTagName("LabelSize")[0].firstChild.nodeValue;
        } catch (e) {
            SzLabelSize = "12";
        }

        nLabelTextsize = parseInt(SzLabelSize);

        try {
            var SzrefreshMarkup = configxml.getElementsByTagName("RefreshMarkup")[0].firstChild.nodeValue;
        } catch (e) {
            //ignore and continue
            SzrefreshMarkup = "";
        }

        try {
            var szPDFZoomAll = configxml.getElementsByTagName("PDFZoomAll")[0].firstChild.nodeValue;
        } catch (e) {
            szPDFZoomAll = "";
        }
        bPDFZoomAllinit = (szPDFZoomAll != "" && szPDFZoomAll == "True");

        try {
            var szPDFanimateRender = configxml.getElementsByTagName("PDFAnimateRender")[0].firstChild.nodeValue;
        } catch (e) {
            szPDFanimateRender = "";
        }
        bAnimatePDFrender = (szPDFanimateRender != "" && szPDFanimateRender == "True");



        try {
            var SzNoRotateMarkup = configxml.getElementsByTagName("NoRotateMarkup")[0].firstChild.nodeValue;
        } catch (e) {
            //ignore and continue
            SzNoRotateMarkup = "";
        }



        try {
            var xmllayers = configxml.getElementsByTagName('layer');
            if (xmllayers.length > 0) {
                for (var l = 0; l < xmllayers.length; l++) {
                    //get layer attributes and create layer list.
                    var LayerName = xmllayers[l].getElementsByTagName("name")[0].firstChild.nodeValue;
                    var LayerColor = xmllayers[l].getElementsByTagName("color")[0].firstChild.nodeValue;
                    var LayerNumber = xmllayers[l].getElementsByTagName("number")[0].firstChild.nodeValue;
                    var LayerState = xmllayers[l].getElementsByTagName("state")[0].firstChild.nodeValue;
                    if (LayerState == 'on') {
                        var bState = true;
                    } else {
                        bState = false;
                    }
                    Layerlist[l] = new Layers(LayerNumber, LayerColor, LayerName, bState);
                }
            }
        } catch (e) {
            //ignore and continue
            createLayers();
        }

        try {
            var stamptags = configxml.getElementsByTagName('stamp');
            if (stamptags.length > 0) {
                for (var i = 0; i < stamptags.length; i++) {
                    Stamplist[i] = stamptags[i].firstChild.nodeValue;
                }
                Stamplist[i] = "Date";
                Stamplist[i + 1] = "User Name";
            } else {
                Stamplist = ["Approved", "Draft", "Received", "Rejected", "Reviewed", "Revised", "Date", "User Name"];
            }
        } catch (e) {
            //ignore error and add default values
            Stamplist = ["Approved", "Draft", "Received", "Rejected", "Reviewed", "Revised", "Date", "User Name"];

        }


        if (Stamplist.length > 0) {

            if (RxCore_GUI_Stamps != undefined) {
                RxCore_GUI_Stamps.setMarkupStamps(Stamplist);
            }

        }

        //set global variables from xml
        if (SzContext != "") {
            //
        }
        if (szLicMode != "") {
            OEMFlag = szLicMode;
        }
        if (szImgSwitchFactor != "") {
            imageswitchfactor = parseFloat(szImgSwitchFactor);
        }
        if (SzCurrentUser != "") {
            signature = SzCurrentUser;
        }
        if (SzDisplayName != "") {
            DisplayName = SzDisplayName;
        }

        if (SzMarkupLayer != "") {
            markuplayer = parseInt(SzMarkupLayer);
        }

        if (SzMarkupColor != "") {
            markupcolor = SzMarkupColor;
            markuplinecolor = SzMarkupColor;
            markuptextcolor = SzMarkupColor;
        }

        setDefaultUser(signature, DisplayName, markuplayer, markupcolor);

        //Userlist[0] = new Users(signature, DisplayName, markuplayer, markupcolor);
        //numUsers++;


        if (RxCore_GUI_Users != undefined) {
            RxCore_GUI_Users.setUserlist(Userlist, true);
        }

        userToLocalStorage("","",false);

        if (SzXmlUrl != "") {
            xmlurl = SzXmlUrl;
        }

        if (SzXmlUrlmarkup != "") {
            xmlurlmarkup = SzXmlUrlmarkup;
        }


        if (SzXMLURLRel != "") {
            xmlurlrel = SzXMLURLRel;
        }
        if (SzMarkupSaveUrl != "") {
            xmlurlmarkupsave = SzMarkupSaveUrl;
        }

        if(SzFileExport != ""){
            PDFExportURL = SzFileExport;
        }

        if(Sz3DImage != ""){
            CanvasSaveUrl = Sz3DImage;
        }
        //

        if (SzPrintPageURL != "") {
            PrintPageURL = SzPrintPageURL;
        }

        readonlymode = (SzReadonlymarkup != "" && SzReadonlymarkup == "True");

        bNoMarkuprotate = (SzNoRotateMarkup != "" && SzNoRotateMarkup == "True");


        if(SzUseAuth != ""){
            buseAuth = (SzUseAuth == "True");
        }
        
        if (SzAuthType != "") {
            AuthType = SzAuthType;
        }

        if (SzAuthName != "") {
            AuthName = SzAuthName;
        }

        if (SzAuthPass != "") {
            AuthPass = SzAuthPass;
        }


        /*if (SzReadonlymarkup != "" && SzReadonlymarkup == "True") {
            readonlymode = true;
            //$('.editOnly').remove();
            //$('#View').css('display', '');
            //$('li[ref="View"]').addClass('selected');

        } else {
            readonlymode = false;
        }*/
        bCanChangeSignature = (SzCanChangeSign != "" && SzCanChangeSign == "True");

        bCanConsolidate = (SzCanConsolidate != "" && SzCanConsolidate == "True");

        bCanChangeLayer = (SzCanChangeLayer != "" && SzCanChangeLayer == "True");

        /*if (SzEnableFileIOpen != "" && SzEnableFileIOpen == "False") {
            $('.disableOpenClose').remove();
        } else {
            //bCanChangeLayer = false;
        }*/
        bReverseScale = (SzReverseScale != "" && SzReverseScale == "True");
        bRefreshmarkup = (SzrefreshMarkup != "" && SzrefreshMarkup == "True");

        if(SzDefaultPenTable != ""){
            bDefaultpentable = true;
            DefaultpentableURL = SzDefaultPenTable;

            loadPenTable(DefaultpentableURL);



        }


        if (SzIntitialDocument != "") {
            //getFile(SzIntitialDocument);
            bOpeninitialdoc = true;
            OpeninitialdocURL = SzIntitialDocument;

            initialDoc.open = bOpeninitialdoc;
            initialDoc.url = OpeninitialdocURL;
        }

        if(SzUseCustom != "" && SzUseCustom == "True"){
            initialDoc.custom = true;
        }else{
            initialDoc.custom = false;
        }


        Paper = new PaperSize(SzPaperWidth, SzPaperHeight);

        configset = true;
        readycheck();
    }

    function initFoxit(index){


        if (RxCore_GUI_FoxitReady != undefined){

            if(index){
                initialDoc.index = index;
            }

            if(readycalled){
                RxCore_GUI_FoxitReady.setupComplete(initialDoc);
                comparecheckFoxit(true);
                comparecheck();
    
            }
            
            foxitreadycalled = true;
        }

        //RxCore_GUI_FoxitReady
        //console.log("foxit ready");

    }


    function configcomplete(ev){
        //var xmlDoc = ev.responseText;

        var xmlDoc = ev.srcElement.responseText;

        var configxml = parseXML(xmlDoc);
    
        configcompleteResp(configxml);
    

    }




    // Keep everything in anonymous function, called on window load.
    function initialize (layout,divid,divclass) {

        if (initialized) return;
        initialized = true;

        nstartuppixelratio = window.devicePixelRatio;
       
        defaultLayout = layout || defaultLayout;
        msieversion();

        //if (window.addEventListener) {
        var readycallback = function(){
//window.addEventListener('load', function () {

            // The active tool instance.
            //var tool;
            //var tool_default = 'pan';

            function init() {

                //find container and get size.
                if(divid != undefined){
                    rxcontainer = document.getElementById(divid);
                }else{
                    rxcontainer = document.getElementById('rxcontainer');
                }

                if (divclass != undefined){
                    rxcontainer.className = divclass;
                }else{
                    rxcontainer.className = 'rxcontainer';
                }

                //var col1 = document.getElementById('leftcol');
                //var colwidth = col1.clientWidth;
                //szcolwidth = szcolwidth.replace("px","");
                //var szcanvwidth = Containerdiv.style.width;
                //var szcanheight = Containerdiv.style.height;
                //szcanvwidth = szcanvwidth.replace("px","");
                //szcanheight = szcanheight.replace("px","");

                //Canvaswidth = szcanvwidth;
                //Canvasheight = szcanheight;

                const scale = getPixelRatio();

                if ((navigator.userAgent.match(/iPhone/i)) || (navigator.userAgent.match(/iPad/i)) || (navigator.userAgent.match(/Android/i))) {
                    bIsmobile = true;
                    nMaximageArea = nMaximageAreaIPad;
                    //alert("mobile platform");
                }


                // Find the canvas element.
                canvaso = document.createElement('canvas');
                if(divid != undefined){
                    canvaso.id = divid + "rxcanvas";
                    canvaso.className = 'rxcanvas';
                }else{
                    canvaso.id = "rxcanvas";
                }


                //canvaso = document.getElementById('imageView');
                if (!canvaso) {
                    alert('Error: I cannot find the canvas element!');
                    return;
                }
                canvaso.innerHTML = "<p>Unfortunately, your browser is currently unsupported by our web \n";
                canvaso.innerHTML += "application.  We are sorry for the inconvenience. Please use one of the\n";
                canvaso.innerHTML += "supported browsers listed below, or draw the image you want using an\n";
                canvaso.innerHTML += "offline tool.</p>\n";

                canvaso.innerHTML += "<p>Supported browsers: <a href='http://www.opera.com'>Opera</a>,\n";
                canvaso.innerHTML += "<a href='http://www.mozilla.com'>Firefox</a>,\n";
                canvaso.innerHTML += "<a href='http://www.apple.com/safari'>Safari</a>, and\n";
                canvaso.innerHTML += "<a href='https://www.google.com/chrome'>Chrome</a>.</p>";

                // canvaso.style.border = "1px solid #000";

                resizeCanvas();


                /*if (Canvaswidth == "" && Canvasheight == ""){

                 }else{
                 canvasowidth  = Canvaswidth;
                 canvasoheight = Canvasheight-bannerheight;
                 }*/

                rxcontainer.appendChild(canvaso);

                if (!canvaso.getContext) {
                    alert('Error: no canvas.getContext!');
                    return;
                }

                canvaso.oncontextmenu = function () {
                    return false;
                };

                // Get the 2D canvas context.
                contexto = canvaso.getContext('2d');
                if (!contexto) {
                    alert('Error: failed to getContext!');
                    return;
                }

                //const scale = getPixelRatio();

                contexto.scale(scale, scale);

                setSmoothingEnabled(false);
                // contexto.imageSmoothingEnabled = false;
                // contexto.mozImageSmoothingEnabled = false;
                // //contexto.webkitImageSmoothingEnabled = false;
                // contexto.msImageSmoothingEnabled = false;


                // Add temporary draw and markup display canvas.
                var container = canvaso.parentNode;
                // Add markup display canvas.




                /*canvas3D = document.createElement('canvas');
                 if(!canvas3D){
                 alert('Error: I cannot create a new canvas element!');
                 return;
                 }
                 canvas3D.id  = 'canv3D';
                 canvas3D.width  = canvasowidth;
                 canvas3D.height = canvasoheight;
                 container.appendChild(canvas3D);*/


                /*if ( webglAvailable() ) {
                 renderer = new THREE.WebGLRenderer({alpha:true,canvas:canvas3D});
                 //alert('bruker webgl rendering');
                 } else {

                 renderer = new THREE.CanvasRenderer({alpha:true,canvas:canvas3D});
                 //alert('bruker canvas rendering');
                 }*/

                if (webglAvailable()) {
                    //renderer = new THREE.WebGLRenderer({alpha:true,antialias:true,precision:"highp"});
                    renderer = new THREE.WebGLRenderer({
                        alpha:true,
                        antialias:true,
                        precision:"highp",
                        preserveDrawingBuffer:true
                    });

                    renderer.clippingPlanes = Empty; // GUI sets it to globalPlanes
                    renderer.localClippingEnabled = false;


                    
                    if(divid != undefined){
                        renderer.domElement.id = divid + 'canv3D';
                        renderer.domElement.className = 'canv3D';

                    }else{
                        renderer.domElement.id = 'canv3D';
                    }

                    console.log('webgl enabled');

                    renderer.physicallyCorrectLights = true;

                    //alert('bruker webgl rendering');
                } else {

                    /*renderer = new THREE.CanvasRenderer({
                        alpha:true
                    });
                    if(divid != undefined){
                        renderer.domElement.id = divid + 'canv3D';
                        renderer.domElement.className = 'canv3D';
                    }else{
                        renderer.domElement.id = 'canv3D';
                    }*/

                    console.log('webgl not available');
                    console.log('No 3D support');
                    //console.log('using canvas 3D');
                    //alert('bruker canvas rendering');
                }


                container.appendChild(renderer.domElement);

                renderer.domElement.oncontextmenu = function () {
                    return false;
                };

                renderer.setSize(canvasowidth, canvasoheight);
                //renderer.setSize(canvasowidthsize, canvasoheightsize);
                

                renderer.setPixelRatio(scale);

                renderer.domElement.style.visibility = 'hidden';

                canvimg = document.createElement('canvas');
                if (!canvimg) {
                    alert('Error: I cannot create a new canvas element!');
                    return;
                }

                if(divid != undefined){
                    canvimg.id = divid + 'imageDisp';
                    canvimg.className = 'imageDisp';
                    canvimg.style.zIndex = "3";
                }else{
                    canvimg.id = 'imageDisp';
                    canvimg.style.zIndex = "3";
                }

                setCanvasSize(canvimg, canvasowidthsize, canvasoheightsize);

                //canvimg.width = canvasowidth;
                //canvimg.height = canvasoheight;
                container.appendChild(canvimg);

                canvimg.oncontextmenu = function () {
                    return false;
                };


                cntximg = canvimg.getContext('2d');

                cntximg.scale(scale, scale);

                //sprite = document.getElementById("sprite");

                // Add the temporary draw canvas.
                canvas = document.createElement('canvas');
                if (!canvas) {
                    alert('Error: I cannot create a new canvas element!');
                    return;
                }

                if(divid != undefined){
                    canvas.id = divid + 'imageTemp';
                    canvas.className = 'imageTemp';
                    canvas.style.zIndex = "3";
                }else{
                    canvas.id = 'imageTemp';
                    canvas.style.zIndex = "3";

                }

                setCanvasSize(canvas, canvasowidthsize, canvasoheightsize);

                //canvas.width = canvasowidth;
                //canvas.height = canvasoheight;
                container.appendChild(canvas);

                canvas.oncontextmenu = function () {
                    return false;
                };

                context = canvas.getContext('2d');

                context.scale(scale, scale);

                
                //magnify canvas
                magcanvas = document.createElement('canvas');
                if (!magcanvas) {
                    alert('Error: I cannot create a new canvas element!');
                    return;
                }

                if(divid != undefined){
                    magcanvas.id = divid + 'imagemag';
                    magcanvas.className = 'imagemag';
                }else{
                    magcanvas.id = 'imagemag';
                }


                magnifysize.width = magnifysize.width / scale;
                magnifysize.height = magnifysize.height / scale;

                //set magnifyer size
                //magcanvas.width = magnifysize.width;
                //magcanvas.height = magnifysize.height;
                setCanvasSize(magcanvas, magnifysize.width, magnifysize.height);

                //magcanvas.style.width = (magnifysize.width * scale) + "px";
                //magcanvas.style,height = (magnifysize.height * scale) + "px";


                container.appendChild(magcanvas);


                
                magcanvas.oncontextmenu = function () {
                    return false;
                };

                magctx = magcanvas.getContext('2d');
                magctx.scale(scale, scale);

                magctx.fillStyle = "white";
                magctx.fillRect(0,0,magnifysize.width,magnifysize.height);

                magcanvas.style.visibility = 'hidden';
                magcanvas.style.zIndex = "3";

                clipboardcnv = document.createElement('canvas');

                if (!clipboardcnv) {
                    alert('Error: I cannot create a new canvas element!');
                    //return;
                }
                clipboardctx = clipboardcnv.getContext('2d');


                //iframe implement 
                //addfoxitdoc();
                //iframe implement 
                if(!bwelcomedisabled){
                    get_splash(splashscreen, splashimage);


                    function check() {
                        if (imageloaded) {
                            set_image();
                        } else {
                            window.setTimeout(check, 500);
                        }
                    }
    
                    check();
                }
                

                function set_image() {
                    imagewidth = splashimage.width;
                    imageheight = splashimage.height;

                    imagewidth = splashimage.naturalWidth;
                    imageheight = splashimage.naturalHeight;


                    //calculate zoom to extent initial scaling and offset
                    //zoom to fit
                    var yscale = canvasoheight / imageheight;
                    var xscale = canvasowidth / imagewidth;
                    var dscale = Math.min(xscale, yscale);

                    var dx = (canvasowidth - (imagewidth * dscale)) / 2;
                    var dy = (canvasoheight - (imageheight * dscale)) / 2;

                    if (!documentopen) {
                        draw_image(splashimage, dx, dy, dscale);
                        doResize(true);
                    }


                }

                //SetGUIConnection();


                // Attach the mousedown, mousemove and mouseup event listeners.


                canvas.addEventListener("touchstart", ev_canvas, false);
                renderer.domElement.addEventListener("touchstart", ev_canvas, false);

                canvas.addEventListener("touchmove", ev_canvas, true);
                renderer.domElement.addEventListener("touchmove", ev_canvas, true);

                canvas.addEventListener("touchend", ev_canvas, false);
                canvas.addEventListener("touchcancel", ev_canvas, false);
                renderer.domElement.addEventListener("touchend", ev_canvas, false);
                renderer.domElement.addEventListener("touchcancel", ev_canvas, false);
                canvas.addEventListener("dbltap", ev_canvas, false);
                canvas.addEventListener("dblclick", ev_canvas, false);

                window.addEventListener('keydown', ev_canvas, true);
                window.addEventListener('keyup', ev_canvas, true);
                window.addEventListener('keypress', ev_canvas, true);
                canvas.addEventListener('wheel', ev_canvas, true);
                canvas.addEventListener('mousewheel', ev_canvas, true);
                renderer.domElement.addEventListener('wheel', ev_canvas, true);
                renderer.domElement.addEventListener('mousewheel', ev_canvas, true);
                canvas.addEventListener('DOMMouseScroll', ev_canvas, true);
                renderer.domElement.addEventListener('DOMMouseScroll', ev_canvas, true);

                renderer.domElement.addEventListener("drop", ev_canvas, false);
                renderer.domElement.addEventListener("dragover", ev_canvas, false);

                canvas.addEventListener("drop", ev_canvas, false);
                canvas.addEventListener("dragover", ev_canvas, false);
                


                
                
                if(bDisableMSIE11Eventcheck){

                    canvas.addEventListener('mousemove', ev_canvas, false);
                    renderer.domElement.addEventListener('mousemove', ev_canvas, false);
                    canvas.addEventListener('mousedown', ev_canvas, false);
                    renderer.domElement.addEventListener('mousedown', ev_canvas, false);
                    canvas.addEventListener('mouseup', ev_canvas, false);
                    renderer.domElement.addEventListener('mouseup', ev_canvas, false);
                    canvas.addEventListener('mouseout', ev_canvas, false);
                    renderer.domElement.addEventListener('mouseout', ev_canvas, false);


                }else{
                    if (window.navigator.msPointerEnabled) {
                        canvas.addEventListener('MSPointerMove', ev_canvas, false);
                        renderer.domElement.addEventListener('MSPointerMove', ev_canvas, false);
                        canvas.addEventListener('MSPointerDown', ev_canvas, false);
                        renderer.domElement.addEventListener('MSPointerDown', ev_canvas, false);
                        canvas.addEventListener('MSPointerUp', ev_canvas, false);
                        renderer.domElement.addEventListener('MSPointerUp', ev_canvas, false);
    
                        canvas.addEventListener('MSPointerOut', ev_canvas, false);
                        renderer.domElement.addEventListener('MSPointerOut', ev_canvas, false);
    
    
                    } else {
                        canvas.addEventListener('mousemove', ev_canvas, false);
                        renderer.domElement.addEventListener('mousemove', ev_canvas, false);
                        canvas.addEventListener('mousedown', ev_canvas, false);
                        renderer.domElement.addEventListener('mousedown', ev_canvas, false);
                        canvas.addEventListener('mouseup', ev_canvas, false);
                        renderer.domElement.addEventListener('mouseup', ev_canvas, false);
                        canvas.addEventListener('mouseout', ev_canvas, false);
                        renderer.domElement.addEventListener('mouseout', ev_canvas, false);
    
    
                    }
                }
                
                //canvas.addEventListener("pressHold", ev_canvas, false);

                //canvas.addEventListener("touchcancel", ev_canvas, false);
                //document.body.addEventListener("touchcancel", touchUp, false);
                //draw_image();
                //createLayers();
                //GetLicense();


                if (bGetconfig) {
                    szLocationSearch = location.search;
                    //szLocationSearch = getLocationSearch();

                    if (szLocationSearch != "") {
                        locationSearchSet = true;
                    }

                    if(bUseJSONConfig){
                       var res = getConfigJSONXML();

                       if(!res){
                        getConfiguration(configurationLocation);
                       }

                    }else{
                        getConfiguration(configurationLocation);
                    }

                    

    
                    //getConfigJSON(configurationLocation);
                } else {
                    //set stamp defaults
                    createLayers();
                    Stamplist = ["Approved", "Draft", "Received", "Rejected", "Reviewed", "Revised", "Date", "User Name"];
                    //var stampul = document.getElementById("stamp");
                    //var stampGUIobject = new Stampmenu(stampul);
                    if (RxCore_GUI_Stamps != undefined) {
                        RxCore_GUI_Stamps.setMarkupStamps(Stamplist);
                    }

    
                    /*if(stampul != undefined){
                     for(i=0;i<Stamplist.length;i++)
                     {

                     if (i==0){
                     stampGUIobject.addstamp(Stamplist[i],true);

                     }else{
                     stampGUIobject.addstamp(Stamplist[i],false);
                     }

                     }

                     }*/
                    //create default user
                    //Userlist[0] = new Users(signature, DisplayName, markuplayer, markupcolor);
                    //numUsers++;

                    configset = true;
                }

                licFromlocalStorage();
    

                /*if(!bIsmobile){

                    licFromlocalStorage();
                    GetLicense();

                }


                console.log('rxcorejs ' + rxcoreversion);*/


                /*if (noteImgSrc){

                    noteimageload(noteImgSrc, noteimage).then(function() {
                        bnoteimageloaded = true;
                    });
            
                    //.markupimageload(imageurl, createimage);
            
                    
                }*/

            } //end init


            // The general-purpose event handler. This function just determines the mouse
            // position relative to the canvas element.
            /*function ev_canvas (ev) {
             //var touch_event = document.getElementById('shape');
             if (ev.layerX || ev.layerY == 0) { // Firefox
             ev._x = ev.layerX;
             ev._y = ev.layerY;
             } else if (ev.offsetX || ev.offsetY == 0) { // Opera
             ev._x = ev.offsetX;
             ev._y = ev.offsetY;
             }

             //touch_event.value = ev.type;
             if(tool == undefined){
             return;
             }
             // Call the event handler of the tool.
             var func = tool[ev.type];
             if (func) {
             func(ev);
             }
             }*/


            // This function draws the #imageTemp canvas on top of #imageView, after which
            // #imageTemp is cleared. This function is called each time when the user
            // completes a drawing operation.
            /*function zoom_update (sx,sy,sWide,sHi) {
             var factorwh = canvasoheight / canvasowidth;


             var tempx = 0.0;
             var tempy = 0.0;
             var tempxext = 0.0;
             var tempyext = 0.0;

             var ox1 = sx / dscale;
             var oy1 = sy / dscale;
             var ox2 = (sx + sWide) / dscale;
             var oy2 = (sy + sHi) / dscale;

             var ox1ext = sx / dscaleextent;
             var oy1ext = sy / dscaleextent;
             var ox2ext = (sx + sWide) / dscaleextent;
             var oy2ext = (sy + sHi) / dscaleextent;

             imagesize = 0;
             SetImageDim(myimage);

             var yscale = canvasoheight / imageheight;
             var xscale = canvasowidth / imagewidth;

             // var yscaleext = canvasoheight / imageheight;
             //var xscaleext = canvasowidth / imagewidth;


             tempx=(sx/dscale)-(dx/dscale);
             tempy=(sy/dscale)-(dy/dscale);

             tempxext = (sx/dscaleextent)-(dxextent/dscaleextent);
             tempyext = (sy/dscaleextent)-(dyextent/dscaleextent);


             if(Math.abs(oy2 - oy1) > 0 && Math.abs(ox2 - ox1) > 0 ){
             yscale = dscale * canvasoheight / sHi;
             xscale = dscale * canvasowidth / sWide;
             }

             dscale = Math.min(yscale, xscale);

             //SetImageDim(myimagesmall);
             //yscale = canvasoheight / imageheight;
             //xscale = canvasowidth / imagewidth;


             if(Math.abs(oy2ext - oy1ext) > 0 && Math.abs(ox2ext - ox1ext) > 0 ){
             yscale = dscaleextent * canvasoheight / sHi;
             xscale = dscaleextent * canvasowidth / sWide;
             }
             dscaleextent = Math.min(yscale, xscale);


             dx=-(tempx*dscale);
             dy=-(tempy*dscale);

             dxextent=-(tempxext*dscaleextent);
             dyextent=-(tempyext*dscaleextent);

             switch(imagesize){
             case 0:
             draw_image(myimage);
             break;
             case 1:
             draw_image(myimagesmall);
             break;
             }


             //draw_image(myimage);

             drawmarkupAll(cntximg);

             context.clearRect(0, 0, canvas.width, canvas.height);
             }*/


            /*function pan_update (sx,sy) {

             switch(drotation){
             case 0:
             dx = dx - sx;
             dy = dy - sy;
             dxextent = dxextent - sx;
             dyextent = dyextent - sy;

             break;
             case 90:
             dx = dx - sy;
             dy = dy + sx;
             dxextent = dxextent - sx;
             dyextent = dyextent + sy;
             break;
             case 180:
             dx = dx + sx;
             dy = dy + sy;
             dxextent = dxextent + sx;
             dyextent = dyextent + sy;
             break;
             case 270:
             dx = dx - sy;
             dy = dy - sx;
             dxextent = dxextent - sx;
             dyextent = dyextent - sy;
             break;

             }


             switch(imagesize){
             case 0:
             draw_image(myimage);
             break;
             case 1:
             draw_image(myimagesmall);
             break;
             }

             //draw_image(myimage);

             drawmarkupAll(cntximg);

             }*/
            /*tools.hide3d = function(){
             var tool = this;
             var x=0, y=0, w=0, h=0;
             var prevx=0, prevy=0;
             //Array for MSIE10 touchpoints
             var numtouchpoints = -1;
             var touchpointarr = [];
             this.name = 'hide3d';
             this.mousedown = function (ev) {

             ev.preventDefault();
             ev.stopPropagation();

             ev.target.style.cursor = 'crosshair';

             tool.started = true;
             var mousePos = getMousePos(canvaso, ev);

             if(documentopen){
             if(DocObj.getCurrentpage().usevector3Dxml){
             //Viewer3D.mouseX = mousePos.x;
             //Viewer3D.mouseY = mousePos.y;

             var info = Viewer3D.pick(ev.clientX, ev.clientY);
             if (info.mesh != null){
             var meshid = info.mesh.name;
             //DocObj.getCurrentpage().select3DBlock(meshid);
             DocObj.getCurrentpage().turn3DBlockOnOff(meshid);
             }

             }


             }


             };

             this.mouseup = function (ev) {
             ev.preventDefault();
             ev.stopPropagation();
             ev.target.style.cursor = 'default';
             if (tool.started) {
             tool.started = false;
             }
             };

             this.pointerdown = function(ev){
             tool.MSPointerDown(ev);
             };


             this.MSPointerDown = function(ev){
             ev.preventDefault();
             ev.stopPropagation();

             ev.target.style.cursor = 'crosshair';
             var mousePos = getMousePos(canvas, ev);
             tool.started = true;
             if(documentopen){
             if(DocObj.getCurrentpage().usevector3Dxml){
             //Viewer3D.mouseX = mousePos.x;
             //Viewer3D.mouseY = mousePos.y;

             var info = Viewer3D.pick(ev.clientX, ev.clientY);
             if (info.mesh != null){
             var meshid = info.mesh.name;
             //DocObj.getCurrentpage().select3DBlock(meshid);
             DocObj.getCurrentpage().turn3DBlockOnOff(meshid);
             }

             }


             }


             };

             this.MSPointerUp = function(ev){
             ev.target.style.cursor = 'default';
             if (tool.started) {

             tool.started = false;


             }

             };

             this.touchstart = function (ev) {
             ev.preventDefault();
             tool.started = true;
             //getTouchPos(canvas,ev,0);
             var touchPos = {
             x: ev.targetTouches[0].pageX,
             y: ev.targetTouches[0].pageY
             };




             if(documentopen){
             if(DocObj.getCurrentpage().usevector3Dxml){
             //Viewer3D.mouseX = mousePos.x;
             //Viewer3D.mouseY = mousePos.y;

             var info = Viewer3D.pick(touchPos.x, touchPos.y);
             if (info.mesh != null){
             var meshid = info.mesh.name;
             //DocObj.getCurrentpage().select3DBlock(meshid);
             DocObj.getCurrentpage().turn3DBlockOnOff(meshid);
             }

             }


             }

             };

             this.touchend = function (ev) {
             ev.preventDefault();
             if (tool.started) {

             tool.started = false;
             }
             };


             };*/

           tools.measurearc = function(params)  {

            var tool = this;
            var prevx = 0,
                prevy = 0;
            var arcmarkupobj;
            //var drawrectangle = new markuprectangle();
            //ft 08.08.2018 changed from separate index to direct array length
            //var curmarkup = DocObj.nummarkups;
            var curmarkup = DocObj.markuplist.length;
            var havesnap = false;
            var snapPoint;
            var rotatedPoint;
            
            
            this.started = false;
            var type = params.p1;
            //var subtype = params.p2;

            this.orgscale = 1;
            this.orgdx = 0;
            this.orgdy = 0;
            this.startx = 0;
            this.starty = 0;


            
            this.startX = null;
            this.startY = null;
            this.secondX = null;
            this.secondY = null;
            this.endX = null;
            this.endY = null;
            this.isDrawing = false; // Flag to track if the tool is in drawing mode
            this.customType = "arc3point"; // Custom type for this tool

            var numtouchpoints = -1;
            var touchpointarr = [];

            this.name = 'measurearc';
            this.anglelengthsupport = false;
            this.refreshSupport = false;

            setCursor(getMarkupCursor('measurearc'));


            this.setdimoffset = function(initial){

                if(initial){

                    arcmarkupobj.scaling = getCurrentPageScale();
                    var offset = getCurrentPageOffset();
                    arcmarkupobj.xoffset = offset.x;
                    arcmarkupobj.yoffset = offset.y;

                    tool.orgscale = arcmarkupobj.scaling;
                    tool.orgdx = arcmarkupobj.xoffset;
                    tool.orgdy = arcmarkupobj.yoffset;

                    
                }

                //move operation to markup object.

                var curscale = DocObj.getCurrentpage().getdscale();
                var pagedx = DocObj.getCurrentpage().getdx();
                var pagedy = DocObj.getCurrentpage().getdy();


                if(arcmarkupobj){
                    var scalediff = (curscale / arcmarkupobj.scaling);
                    var changex = ((arcmarkupobj.w - pagedx) / scalediff) + arcmarkupobj.xoffset;
                    var changey = ((arcmarkupobj.h - pagedy) / scalediff) + arcmarkupobj.yoffset;
                }

                //arrowmarkupobj.w = changex;
                //arrowmarkupobj.h = changey;

            };           

            /*this.setWidthHeight = function(width, height){
                tool.bUsemouseinput = false;
                if (rectmarkupobj != undefined){
                    rectmarkupobj.x = tool.x0;
                    rectmarkupobj.y = tool.y0;
                    var curx2 = rectmarkupobj.w;
                    var cury2 = rectmarkupobj.h;
                    
                    var secondpoint = rectmarkupobj.setWidthHeight(width, height);
                    var abspoint = {x : rectmarkupobj.x + secondpoint.w, y:rectmarkupobj.y + secondpoint.h};

                    context.clearRect(0, 0, canvas.width, canvas.height);
                    
                    if (MousePosdrwext(abspoint)){
                        rectmarkupobj.w = secondpoint.w;
                        rectmarkupobj.h = secondpoint.h;
                        
                        rectmarkupobj.drawme(context);

                    }else{
                        rectmarkupobj.w = curx2;
                        rectmarkupobj.h = cury2;

                        tool.bUsemouseinput = true;
                        rectmarkupobj.drawme(context);
                        rectmarkupobj.bUsemouseinput = true;
                        if (RxCore_GUI_markupParamsError != undefined){
                            RxCore_GUI_markupParamsError.onDrawError('outside');
                        }

                    }


                }

            };*/


            this.startArc = function(bWithin, mousePos){

                if (bWithin || !bLimMarkupExtent) {




                    if(DocObj){
                        DocObj.bMarkupchanged = true;
                    }

                    //rectmarkupobj.scaling = getCurrentPageScale();
                    //var offset = getCurrentPageOffset();
                    //rectmarkupobj.xoffset = offset.x;
                    //rectmarkupobj.yoffset = offset.y;

                    

                    tool.x0 = mousePos.x;
                    tool.y0 = mousePos.y;


                    arcmarkupobj = new MarkupObject(14, 0, 0);

                    arcmarkupobj.startX = tool.x0;
                    arcmarkupobj.startY = tool.y0;

                    arcmarkupobj.pagerotation = DocObj.getCurrentpage().getpageRotation();


                    arcmarkupobj.pagenumber = DocObj.getcurPage();

                    arcmarkupobj.viewname = DocObj.getcur3DView();

                    if(arcmarkupobj.viewname != null){
                        arcmarkupobj.bfor3D = true;
                        arcmarkupobj.pageheight = canvasoheight;
                        arcmarkupobj.pagewidth = canvasowidth;
                    }


                    tool.setdimoffset(true);

                    arcmarkupobj.setsecond = true;
                    //this.setsecond = false;
                    //this.setthird = false;
    

                    tool.startx = tool.x0;
                    tool.starty = tool.y0;


                    arcmarkupobj.seteditaction(0);

                    //this.startX = mousePos.x;
                    //this.startY = mousePos.y;
                    tool.isDrawing = true;


                }

            };

            this.endArc = function () {
                /*if(rectmarkupobj.w==0 || rectmarkupobj.h==0){
                 return;
                 }*/
                //tool.started = false;

                if (!bMultimarkupadd) {

                    bMarkupcrated = true;
                    //ft 08.08.2018 changed from separate index to direct array length
                    //nMarkupcreated = DocObj.markuplist.length;
                    //nMarkupcreated = DocObj.nummarkups;
                }

                nMarkupcreated = DocObj.markuplist.length;

                arcmarkupobj.savemetolistDraw();
                arcmarkupobj = null;


                //tool.started = false;
                //markuplist.add(drawrectangle);
                img_update();
                drawmarkupAll(cntximg);

                if(bAlwaysMarkupCallback){
                    if (RxCore_GUI_Markup != undefined) {
                        var operation = {created : true, modified : false, deleted : false, multi : bMultimarkupadd};
                        RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[nMarkupcreated], operation);
                    }
        
                }else{
                    if (!bMultimarkupadd) {
                        //need to move to connection object.
                        //markupcreated();
                        if (RxCore_GUI_Markup != undefined) {
                            //RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[index],true);
                            var operation = {created : true, modified : false, deleted : false};
                            RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[nMarkupcreated], operation);
                        }

                    }

                }



            };

            this.setSnap = function (mousePos) {
                
                snapPoint = DocObj.getCurrentpage().getsnap(mousePos);

                if(snapPoint == undefined){
                    havesnap = false;
                    context.clearRect(0, 0, canvas.width, canvas.height);
                }

                if (snapPoint.found ) {
                    if(DocObj.getCurrentpage().getpageRotation() != 0){
                        rotatedPoint = snap_rotated(snapPoint);
                    }else{
                        rotatedPoint = snapPoint;
                    }

                    
                    havesnap = true;
                    drawsnap(context,snapPoint);
                    //context.lineWidth = 1;
                    //context.strokeStyle = 'blue';
                    //context.strokeRect(snapPoint.x - 5, snapPoint.y - 5, 10, 10);
                } else {
                    havesnap = false;
                    context.clearRect(0, 0, canvas.width, canvas.height);
                }

            };

            

            this.mousedown = function (ev) {
                ev.preventDefault();
                ev.stopPropagation();

                
                
                var mousePos = getMousePos(canvas, ev);

                /*if(arrowmarkupobj){
                    mousePos = arrowmarkupobj.getPadding(mousePos);
                }*/
                
                if(havesnap && rotatedPoint){
                    var adjustedPos = rotatedPoint;
                }else{
                    var adjustedPos = mousePos;
                }




                var bWithin = MousePosdrwext(mousePos);



                if(ev.button == 0){


                    if (!tool.isDrawing) {

                        tool.startArc(bWithin, adjustedPos);
                        return;
                    }
                  

                        // Second click: set the second point
                    if (tool.isDrawing && arcmarkupobj.setsecond) {

                        arcmarkupobj.startX = tool.x0;
                        arcmarkupobj.startY = tool.y0;
    
                        arcmarkupobj.secondX = adjustedPos.x;
                        arcmarkupobj.secondY = adjustedPos.y;

                        arcmarkupobj.setsecond = false;
                        arcmarkupobj.setthird = true;
                        //this.setsecond = false;
                        //this.setthird = false;
    
                        return;
                    }

                    if (tool.isDrawing && arcmarkupobj.setthird) {
                        arcmarkupobj.endX = adjustedPos.x;
                        arcmarkupobj.endY = adjustedPos.y;
                  
                        tool.isDrawing = false; // Reset the drawing state

                        tool.endArc();



                        //tools.addTool(this); // Add the finished tool to the tools array
                        //tools.drawAll();
                        //tools.setTool(null); // Reset the current tool to null
                      }
                  


                }else{
                    prevx = mousePos.x;
                    prevy = mousePos.y;

                }

                
            };

            this.touchstart = function (ev) {

                ev.preventDefault();
                tool.started = true;
                if(DocObj){
                    DocObj.bMarkupchanged = true;
                }



                var touchPos = getTouchPos(canvas, ev, 0);

                var bWithin = MousePosdrwext(touchPos);
                //touchPos = getDocRotMousePoint(touchPos);
                
                if (!bWithin && bLimMarkupExtent) {
                    tool.started = false;
                    if(DocObj){
                        DocObj.bMarkupchanged = false;
                    }

                    return;
                }


                tool.x0 = touchPos.x;
                tool.y0 = touchPos.y;


                //rectmarkupobj = new MarkupObject(type, subtype,  fillstyle);
                

                //rectmarkupobj.scaling = getCurrentPageScale();
                var offset = getCurrentPageOffset();
                //rectmarkupobj.xoffset = offset.x;
                //rectmarkupobj.yoffset = offset.y;


                //rectmarkupobj.pagenumber = DocObj.getcurPage();
                //rectmarkupobj.viewname = DocObj.getcur3DView();

                /*if(rectmarkupobj.viewname != null){
                    rectmarkupobj.bfor3D = true;
                    rectmarkupobj.pageheight = canvasoheight;
                    rectmarkupobj.pagewidth = canvasowidth;
                }*/

                //rectmarkupobj.seteditaction(0);

            };

            

            this.mousemove = function (ev) {
                ev.preventDefault();
                ev.stopPropagation();
                
                var mousePos = getMousePos(canvas, ev);

                tool.setSnap(mousePos);

                if (!tool.isDrawing) {
                    return;
                }

                var bWithin = MousePosdrwext(mousePos);
                //mousePos = getDocRotMousePoint(mousePos);

                if (bWithin || !bLimMarkupExtent) {
                    //tool.drawRect(mousePos);

                    if(arcmarkupobj.setsecond){
                        arcmarkupobj.secondX = mousePos.x;
                        arcmarkupobj.secondY = mousePos.y;
    
                    }else if (arcmarkupobj.setthird){
                        arcmarkupobj.endX = mousePos.x;
                        arcmarkupobj.endY = mousePos.y;

                    }

                } else {

                    if(arcmarkupobj.setsecond){
                        arcmarkupobj.secondX = mousePos.x;
                        arcmarkupobj.secondY = mousePos.y;
    
                    }else if (arcmarkupobj.setthird){
                        arcmarkupobj.endX = mousePos.x;
                        arcmarkupobj.endY = mousePos.y;

                    }

                }
                context.clearRect(0, 0, canvas.width, canvas.height);
                if (havesnap) {
                    drawsnap(context,snapPoint);
                }

                arcmarkupobj.drawme(context);


            };

            this.touchmove = function (ev) {
                ev.preventDefault();
                if (!tool.started) {
                    return;
                }
                var touchPos = getTouchPos(canvas, ev, 0);
                var bWithin = MousePosdrwext(touchPos);
                //touchPos = getDocRotMousePoint(touchPos);
                if (bWithin || !bLimMarkupExtent) {

                    prevx = touchPos.x;
                    prevy = touchPos.y;

                } else {

                }
                if (tool.bUsemouseinput){

                }


                context.clearRect(0, 0, canvas.width, canvas.height);

                //rectmarkupobj.drawme(context);

            };

            this.mouseup = function (ev) {
                
                /*if (tool.started) {

                    tool.mousemove(ev);
                    tool.endRect();

                    tool.started = false;


                }*/
            };
            this.touchcancel = function(ev){
                ev.preventDefault();
                if (tool.started) {

                    //tool.endRect();
                    tool.started = false;

                }

            };  

            this.touchend = function (ev) {
                ev.preventDefault();
                if (tool.started) {

                    //tool.endRect();
                    tool.started = false;

                }
            };            



           };

            tools.hide3d = function () {
                var tool = this;
                var x = 0,
                    y = 0,
                    w = 0,
                    h = 0;
                var prevx = 0,
                    prevy = 0;
                //Array for MSIE10 touchpoints
                var numtouchpoints = -1;
                var touchpointarr = [];
                this.name = 'hide3d';
                this.anglelengthsupport = false;
                this.refreshSupport = false;

                var raycaster = new THREE.Raycaster();
                var mouse = new THREE.Vector2();
                var mvector = new THREE.Vector3();

                

                setCursor(getMarkupCursor('hide3d'));


                this.getInterSects = function(mouse){

                    raycaster.setFromCamera(mouse, DocObj.getCurrentpage().camera);

                    // calculate objects intersecting the picking ray
                    if(DocObj.getCurrentpage().usevector3DGLTF ){
                        var mesharray = DocObj.getCurrentpage().Vector3DPageObj.getMeshChildren();
                        
                        if (mesharray.length == 0){
                            return;
                        } 

                        var addedmesharray = DocObj.getCurrentpage().Vector3DPageObj.getAddedMeshChildren();

                        if( addedmesharray.length == 0 ){
                            var addedintersects = [];
                        }else{
                            addedintersects = [];
                            for(var adi = 0; adi < addedmesharray.length; adi ++){
                                addedintersects.push(raycaster.intersectObjects(addedmesharray[adi], true));
                            }
                            
                        }

                        var intersects = raycaster.intersectObjects(mesharray, true);
                    }else{
                        intersects = raycaster.intersectObjects(DocObj.getCurrentpage().Vector3DPageObj.scene.children);
                    }
                    
                    if (addedintersects.length > 0 ){
                        for (var i = 0; i < addedintersects.length; i++) {

                            
                            for (var j = 0; j < addedintersects[i].length; i++) {
                                
                                if(addedintersects[i][j].object.type == "LineSegments"){
                                    continue;
                                }

                                var addedintersection = null;
    
                                var bPlaneX = true;
                                var bPlaneY = true;
                                var bPlaneZ = true;
    
                                if(DocObj.getCurrentpage().Vector3DPageObj.clippingOn){
                                    bPlaneX = (addedintersects[i][j].point.x > -DocObj.getCurrentpage().Vector3DPageObj.globalPlaneX.constant);
                                    bPlaneY = (addedintersects[i][j].point.y > -DocObj.getCurrentpage().Vector3DPageObj.globalPlaneY.constant);
                                    bPlaneZ = (addedintersects[i][j].point.z < DocObj.getCurrentpage().Vector3DPageObj.globalPlaneZ.constant);
                                }
    
                                if (addedintersects[i][j].object.visible && bPlaneX && bPlaneY && bPlaneZ) {

                                    //DocObj.getCurrentpage().add3DSymbol(intersects[i], id);
                                    addedintersection = addedintersects[i][j];

                                    
                                    break;
                                    
                                }
                            }
                            
                        }
                    }                        


                    if (intersects.length > 0) {



                        for (var i = 0; i < intersects.length; i++) {
                            //var bPlaneX = (intersects[i].point.x > -DocObj.getCurrentpage().Vector3DPageObj.globalPlaneX.constant);
                            //var bPlaneY = (intersects[i].point.y > -DocObj.getCurrentpage().Vector3DPageObj.globalPlaneY.constant);
                            //var bPlaneZ = (intersects[i].point.z < DocObj.getCurrentpage().Vector3DPageObj.globalPlaneZ.constant);
                            //&& bPlaneX && bPlaneY && bPlaneZ

                            if(intersects[i].object.type == "LineSegments"){
                                continue;
                            }

                            var bPlaneX = true;
                            var bPlaneY = true;
                            var bPlaneZ = true;

                            if(DocObj.getCurrentpage().Vector3DPageObj.clippingOn){
                                bPlaneX = (intersects[i].point.x > -DocObj.getCurrentpage().Vector3DPageObj.globalPlaneX.constant);
                                bPlaneY = (intersects[i].point.y > -DocObj.getCurrentpage().Vector3DPageObj.globalPlaneY.constant);
                                bPlaneZ = (intersects[i].point.z < DocObj.getCurrentpage().Vector3DPageObj.globalPlaneZ.constant);
                            }


                            if (intersects[i].object.visible && bPlaneX && bPlaneY && bPlaneZ) {

                                var buseadded = false;
                                if(addedintersection != null){
    
                                    if(addedintersection.distance < intersects[i].distance){
                                        buseadded = true;
                                    }else{
                                        buseadded = false;
                                    }
                                    //console.log(addedintersection);
                                } 
    
                                if(buseadded){
                                    var meshid = addedintersection;
                                }else{
                                    var meshid = intersects[i];
                                }

                                if (meshid.object.userData.visible == undefined || meshid.object.userData.tempvisible){
                                
                                    meshid.object.userData.visible = (meshid.object.visible) ? 1 : 0;
                                    meshid.object.userData.tempvisible = meshid.object.visible;

                                    
                                }
    
                                
                                if (meshid.object.visible ) {
                                    var meshidname = meshid.object.name;
                                    var meshidtype = meshid.object.type;

                                    meshidname = DocObj.getCurrentpage().getMeshName(meshidname, meshidtype);

                                    //DocObj.getCurrentpage().turn3DBlockOnOff(meshidname);
                                    DocObj.getCurrentpage().turn3DBlockOnOff(meshidname, true);
                                    //DocObj.getCurrentpage().select3DBlock(meshid);
                                    break;
                                }

                            }                            
                        }
                    }
                };

                this.mousedown = function (ev) {

                    ev.preventDefault();
                    ev.stopPropagation();


                    tool.started = true;
                    //var mousePos = getMousePos(canvaso, ev);
                    var mousePos = getMousePos(renderer.domElement, ev);

                    if (documentopen) {
                        if (DocObj.getCurrentpage().usevector3Dxml) {

                            mouse.x = (mousePos.x / canvasowidth) * 2 - 1;
                            mouse.y = -(mousePos.y / canvasoheight) * 2 + 1;

                            tool.getInterSects(mouse);
                            // update the picking ray with the camera and mouse position

                            /*raycaster.setFromCamera(mouse, DocObj.getCurrentpage().camera);

                            // calculate objects intersecting the picking ray
                            var intersects = raycaster.intersectObjects(DocObj.getCurrentpage().Vector3DPageObj.scene.children);

                            if (intersects.length > 0) {
                                for (var i = 0; i < intersects.length; i++) {
                                    if (intersects[i].object.visible) {
                                        var meshid = intersects[i].object.name;
                                        DocObj.getCurrentpage().turn3DBlockOnOff(meshid);
                                        break;
                                    }
                                }

                            }*/


                        }


                    }


                };

                this.mouseup = function (ev) {
                    ev.preventDefault();
                    ev.stopPropagation();
                    
                    if (tool.started) {
                        tool.started = false;
                    }
                };

                this.pointerdown = function (ev) {
                    tool.MSPointerDown(ev);
                };
                this.MSPointerDown = function (ev) {
                    ev.preventDefault();
                    ev.stopPropagation();

                    var mousePos = getMousePos(canvas, ev);
                    tool.started = true;
                    if (documentopen) {
                        if (DocObj.getCurrentpage().usevector3Dxml) {

                            mouse.x = (mousePos.x / canvasowidth) * 2 - 1;
                            mouse.y = -(mousePos.y / canvasoheight) * 2 + 1;

                            tool.getInterSects(mouse);
                            // update the picking ray with the camera and mouse position

                            /*raycaster.setFromCamera(mouse, DocObj.getCurrentpage().camera);

                            // calculate objects intersecting the picking ray
                            var intersects = raycaster.intersectObjects(DocObj.getCurrentpage().Vector3DPageObj.scene.children);

                            if (intersects.length > 0) {
                                for (var i = 0; i < intersects.length; i++) {
                                    if (intersects[i].object.visible) {
                                        var meshid = intersects[i].object.name;
                                        DocObj.getCurrentpage().turn3DBlockOnOff(meshid);
                                        break;
                                    }
                                }

                            }*/

                        }


                    }


                };

                this.touchstart = function (ev) {
                    ev.preventDefault();
                    tool.started = true;
                    var touchPos = getTouchPos(renderer.domElement, ev, 0);


                    /*var touchPos = {
                     x: ev.targetTouches[0].pageX,
                     y: ev.targetTouches[0].pageY
                     };*/


                    if (documentopen) {
                        if (DocObj.getCurrentpage().usevector3Dxml) {
                            //Viewer3D.mouseX = mousePos.x;
                            //Viewer3D.mouseY = mousePos.y;
                            mouse.x = (touchPos.x / canvasowidth) * 2 - 1;
                            mouse.y = -(touchPos.y / canvasoheight) * 2 + 1;

                            // update the picking ray with the camera and mouse position

                            tool.getInterSects(mouse);

                            /*raycaster.setFromCamera(mouse, DocObj.getCurrentpage().camera);

                            // calculate objects intersecting the picking ray
                            var intersects = raycaster.intersectObjects(DocObj.getCurrentpage().Vector3DPageObj.scene.children);

                            if (intersects.length > 0) {
                                for (var i = 0; i < intersects.length; i++) {
                                    if (intersects[i].object.visible) {
                                        var meshid = intersects[i].object.name;
                                        DocObj.getCurrentpage().turn3DBlockOnOff(meshid);
                                        break;
                                    }
                                }

                            }*/

                            /*var info = Viewer3D.pick(touchPos.x, touchPos.y);
                             if (info.mesh != null){
                             var meshid = info.mesh.name;
                             DocObj.getCurrentpage().select3DBlock(meshid);
                             }*/

                        }


                    }

                };
                this.touchcancel = function(ev){
                    ev.preventDefault();
                    if (tool.started) {

                        tool.started = false;
                    }

                };
                this.touchend = function (ev) {
                    ev.preventDefault();
                    if (tool.started) {

                        tool.started = false;
                    }
                };


            };

            tools.add3dsymbol = function(params){
                var tool = this;
                var x = 0,
                    y = 0,
                    w = 0,
                    h = 0;
                var prevx = 0,
                    prevy = 0;

                    
                var raycaster = new THREE.Raycaster();
                var mouse = new THREE.Vector2();
                var mvector = new THREE.Vector3();

                this.color = params.p1;

                this.name = 'add3dsymbol';
                this.anglelengthsupport = false;
                this.refreshSupport = false;

                //Array for MSIE10 touchpoints
                var numtouchpoints = -1;
                var touchpointarr = [];

                this.getInterSects = function(mouse, id){

                    raycaster.setFromCamera(mouse, DocObj.getCurrentpage().camera);

                    // calculate objects intersecting the picking ray
                    if(DocObj.getCurrentpage().usevector3DGLTF ){
                        var mesharray = DocObj.getCurrentpage().Vector3DPageObj.getMeshChildren();
                        var addedmesharray = DocObj.getCurrentpage().Vector3DPageObj.getAddedMeshChildren();

                        
                        if (mesharray.length == 0){
                            return;
                        } 


                        if( addedmesharray.length == 0 ){
                            var addedintersects = [];
                        }else{
                            addedintersects = [];
                            for(var adi = 0; adi < addedmesharray.length; adi ++){
                                addedintersects.push(raycaster.intersectObjects(addedmesharray[adi], true));
                            }
                        }


                        var intersects = raycaster.intersectObjects(mesharray, true);
                    }else{
                        intersects = raycaster.intersectObjects(DocObj.getCurrentpage().Vector3DPageObj.scene.children);
                    }
                    
                    if (addedintersects.length > 0 ){
                        for (var i = 0; i < addedintersects.length; i++) {

                            if (addedintersects[i].length > 0){
                                for (var j = 0; j < addedintersects[i].length; i++) {
                                    
                                    if(addedintersects[i][j].object.type == "LineSegments"){
                                        continue;
                                    }

                                    var addedintersection = null;
        
                                    var bPlaneX = true;
                                    var bPlaneY = true;
                                    var bPlaneZ = true;
        
                                    if(DocObj.getCurrentpage().Vector3DPageObj.clippingOn){
                                        bPlaneX = (addedintersects[i][j].point.x > -DocObj.getCurrentpage().Vector3DPageObj.globalPlaneX.constant);
                                        bPlaneY = (addedintersects[i][j].point.y > -DocObj.getCurrentpage().Vector3DPageObj.globalPlaneY.constant);
                                        bPlaneZ = (addedintersects[i][j].point.z < DocObj.getCurrentpage().Vector3DPageObj.globalPlaneZ.constant);
                                    }
        
                                    if (addedintersects[i][j].object.visible && bPlaneX && bPlaneY && bPlaneZ) {

                                        //DocObj.getCurrentpage().add3DSymbol(intersects[i], id);
                                        addedintersection = addedintersects[i][j];

                                        
                                        break;
                                        
                                    }
                                }
                            }
                        }
                    }                    


                    if (intersects.length > 0) {
                        for (var i = 0; i < intersects.length; i++) {

                            if(intersects[i].object.type == "LineSegments"){
                                continue;
                            }

                            var bPlaneX = true;
                            var bPlaneY = true;
                            var bPlaneZ = true;

                            if(DocObj.getCurrentpage().Vector3DPageObj.clippingOn){

                                bPlaneX = (intersects[i].point.x > -DocObj.getCurrentpage().Vector3DPageObj.globalPlaneX.constant);
                                bPlaneY = (intersects[i].point.y > -DocObj.getCurrentpage().Vector3DPageObj.globalPlaneY.constant);
                                bPlaneZ = (intersects[i].point.z < DocObj.getCurrentpage().Vector3DPageObj.globalPlaneZ.constant);

                            }

                            if (intersects[i].object.visible && bPlaneX && bPlaneY && bPlaneZ) {
                                //get intersection point rather than object.
                                //var insertpoint = intersects[i].point;
                                //DocObj.getCurrentpage().select3DBlock(meshid);

                                //DocObj.getCurrentpage().add3DSymbol(intersects[i], id);

                                var buseadded = false;
                                if(addedintersection != null){

                                    if(addedintersection.distance < intersects[i].distance){
                                        buseadded = true;
                                    }else{
                                        buseadded = false;
                                    }
                                    //console.log(addedintersection);
                                } 

                                if(buseadded){
                                    var meshid = addedintersection;
                                }else{
                                    var meshid = intersects[i];
                                }


                                DocObj.getCurrentpage().add3DSymbol(meshid, id);


                                break;
                            }
                        }


                    }

                    //intersects[0].object.material.color = new THREE.Color( 0xf781f3 );

                    /*for ( var intersect in intersects ) {

                     var meshid = intersects[intersect].object.name;
                     intersects[intersect].object.material.color = new THREE.Color( 0xf781f3 );

                     }*/


                    /*var info = Viewer3D.pick(ev.clientX, ev.clientY);
                     if (info.mesh != null){
                     var meshid = info.mesh.name;
                     DocObj.getCurrentpage().select3DBlock(meshid);
                     }*/

                   
                };

                this.dragover = function(ev){
                    ev.preventDefault();
                };

                this.drop = function(ev){
                    ev.preventDefault();

                  //var mousePos = getMousePos(canvas, ev);
                  var mousePos = getMousePos(renderer.domElement, ev);

                  var dropdata = JSON.parse(ev.dataTransfer.getData("Text"));
                  
                  //RxCore.create3DTexture("http://win12sql/rxweb/images/pinsymbolred.png",99, 128, "red");
                  //RxCore.create3DTexture("http://win12sql/rxweb/images/pinsymbolblue.png",99, 128, "blue");
                  //RxCore.create3DTexture("http://win12sql/rxweb/images/pinsymbolgreen.png",99, 128, "green");
                  //imagemarkupobj.w = dropdata.width;
                  //imagemarkupobj.h = dropdata.height;
                   var baddsymbol = true; 

                  for(var tix = 0; tix < textureobjects.lenght; tix ++){
                      if (textureobjects[tix].id == dropdata.id){
                        baddsymbol = false;
                      }
                  }

                  if (baddsymbol){

                    RxCore_create3DTexture(dropdata.src, dropdata.width, dropdata.height, dropdata.id);
                    //textureobjects.push(new textureobject(dropdata.src, dropdata.width, dropdata.height, dropdata.id));
                  }
                  
                        
                  //var createimage = new Image();
                  //createimage.src = dropdata.src;

                  if (documentopen) {
                        if (DocObj.getCurrentpage().usevector3Dxml) {

                            mouse.x = (mousePos.x / canvasowidth) * 2 - 1;
                            mouse.y = -(mousePos.y / canvasoheight) * 2 + 1;

                            tool.getInterSects(mouse,dropdata.id);
                            // update the picking ray with the camera and mouse position



                        }


                    }


                };                

                this.mousedown = function (ev) {

                    ev.preventDefault();
                    ev.stopPropagation();

                    

                    tool.started = true;
                    //var mousePos = getMousePos(canvaso, ev);
                    var mousePos = getMousePos(renderer.domElement, ev);

                    if (documentopen) {
                        if (DocObj.getCurrentpage().usevector3Dxml) {

                            mouse.x = (mousePos.x / canvasowidth) * 2 - 1;
                            mouse.y = -(mousePos.y / canvasoheight) * 2 + 1;

                            tool.getInterSects(mouse, tool.color);
                            // update the picking ray with the camera and mouse position



                        }


                    }


                };

                this.mouseup = function (ev) {
                    ev.preventDefault();
                    ev.stopPropagation();
                    
                    if (tool.started) {
                        tool.started = false;
                    }
                };

                this.pointerdown = function (ev) {
                    tool.MSPointerDown(ev);
                };


                this.MSPointerDown = function (ev) {
                    ev.preventDefault();
                    ev.stopPropagation();

                    var mousePos = getMousePos(canvas, ev);
                    tool.started = true;
                    if (documentopen) {
                        if (DocObj.getCurrentpage().usevector3Dxml) {
                            //Viewer3D.mouseX = mousePos.x;
                            //Viewer3D.mouseY = mousePos.y;

                            mouse.x = (mousePos.x / canvasowidth) * 2 - 1;
                            mouse.y = -(mousePos.y / canvasoheight) * 2 + 1;

                            tool.getInterSects(mouse);


                            // update the picking ray with the camera and mouse position

                            

                        }


                    }


                };

                this.MSPointerUp = function (ev) {
                    
                    if (tool.started) {

                        tool.started = false;


                    }

                };

                this.touchstart = function (ev) {
                    ev.preventDefault();
                    tool.started = true;
                    var touchPos = getTouchPos(renderer.domElement, ev, 0);


                    /*var touchPos = {
                     x: ev.targetTouches[0].pageX,
                     y: ev.targetTouches[0].pageY
                     };*/


                    if (documentopen) {
                        if (DocObj.getCurrentpage().usevector3Dxml) {
                            //Viewer3D.mouseX = mousePos.x;
                            //Viewer3D.mouseY = mousePos.y;
                            mouse.x = (touchPos.x / canvasowidth) * 2 - 1;
                            mouse.y = -(touchPos.y / canvasoheight) * 2 + 1;

                            // update the picking ray with the camera and mouse position
                            tool.getInterSects(mouse);

                        }


                    }

                };

                this.touchcancel = function(ev){
                    ev.preventDefault();
                    if (tool.started) {

                        tool.started = false;
                    }

                };                
                this.touchend = function (ev) {
                    ev.preventDefault();
                    if (tool.started) {

                        tool.started = false;
                    }
                };
            };
            
            tools.measure3D = function(){
                var tool = this;
                var x = 0,
                    y = 0,
                    w = 0,
                    h = 0;
                var prevx = 0,
                    prevy = 0;

                    
                var raycaster = new THREE.Raycaster();
                var mouse = new THREE.Vector2();
                var mvector = new THREE.Vector3();

                
                


                //this.color = params.p1;

                this.name = 'measure3D';
                this.anglelengthsupport = false;
                
                this.refreshSupport = false;

                this.numpoints = 0;
                this.screenPoints = [];


                //Array for MSIE10 touchpoints
                var numtouchpoints = -1;
                var touchpointarr = [];
                var intersectsArr = [];

                
                setCursor(getMarkupCursor('measure3D'));


                this.drawLine = function(point1, point2){


                    context.strokeStyle = 'black';
                    context.beginPath();
                    context.moveTo(point1.x, point1.y);
                    context.lineTo(point2.x, point2.y);
                    context.closePath();

                    context.stroke();
                    //var bdostroke = true;    
  
                };

                this.drawLabel = function(point, bclear){
                    if(bclear){
                        context.clearRect(0, 0, canvas.width, canvas.height);
                    }


                };
                this.drawPoints = function(point, bclear){
                    
                                    
                    if(bclear){
                        context.clearRect(0, 0, canvas.width, canvas.height);
                    }
                    


                    context.save(); // save the context so we don't mess up others

                    
                    context.fillStyle = "rgba(0, 0, 255, 0.5)";

                    context.beginPath();
                    
                    context.arc(point.x, point.y, 32 * 0.25, 0, Math.PI * 2); //full circle
                    //ctx.arc(100, 75, 50, 0, 2 * Math.PI);
                    
                    context.closePath();
                    //ctx.beginPath();
                    //ctx.arc(100, 75, 25, 0, 2 * Math.PI, true);
                    context.arc(point.x, point.y, 32 * 0.125, 0, Math.PI * 2, true); //full circle
                    context.closePath();
                    //ctx.stroke();
                    context.fill();

                    /*if(bdostroke){
                        context.stroke();
                    }*/


                    context.restore(); // restore context to what it was on entry


                };
                
                this.handlePoints = function(bmove, intersection){

                    if (bmove){

                        var point = DocObj.getCurrentpage().Vector3DPageObj.pointToScreen(intersection);
                        tool.drawPoints(point, true);
                        var p2 = intersection.point.clone();
                        

                        if(intersectsArr.length > 0){
                            var point0 = DocObj.getCurrentpage().Vector3DPageObj.pointToScreen(intersectsArr[0]);           

                            tool.drawPoints(point0, false);
                            tool.drawLine(point0, point);

                            var p1 = intersectsArr[0].point.clone();

                            var dist = p1.distanceTo ( p2 );
                            var dimtext = DocObj.getCurrentpage().Vector3DPageObj.setdimvalue(dist);
                            DocObj.getCurrentpage().Vector3DPageObj.dimvaluedraw(point,context, 'black', dimtext, true);

        
                        }

    
                        //var newpoint = getPointPos(canvas, point);
                    }else{

                        intersectsArr.push(intersection);
                        
                        tool.screenPoints.push(point);

                        tool.numpoints += 1; 


                        if (tool.numpoints == 2){
                            DocObj.getCurrentpage().Vector3DPageObj.add2DPoints(intersectsArr, tool.numpoints);
                            intersectsArr = [];
                            tool.numpoints = 0; 
                            tool.started = false;

                            set3DToolType(false, 'measure3D', null);

                        }else{
                            //DocObj.getCurrentpage().Vector3DPageObj.add2DtempPoints(intersectsArr, tool.numpoints);
                        }

                    }


                };                

                this.getInterSects = function(mouse, id, bmove){

                    raycaster.setFromCamera(mouse, DocObj.getCurrentpage().camera);

                    // calculate objects intersecting the picking ray
                    if(DocObj.getCurrentpage().usevector3DGLTF ){
                        var mesharray = DocObj.getCurrentpage().Vector3DPageObj.getMeshChildren();

                        var addedmesharray = DocObj.getCurrentpage().Vector3DPageObj.getAddedMeshChildren();
                        
                        if (mesharray.length == 0){
                            return;
                        } 

                        if( addedmesharray.length == 0 ){
                            var addedintersects = [];
                        }else{
                            addedintersects = [];
                            for(var adi = 0; adi < addedmesharray.length; adi ++){
                                addedintersects.push(raycaster.intersectObjects(addedmesharray[adi], true));
                            }
                            
                        }

                        var intersects = raycaster.intersectObjects(mesharray, true);
                    }else{
                        intersects = raycaster.intersectObjects(DocObj.getCurrentpage().Vector3DPageObj.scene.children);
                    }
                    

                    if (addedintersects.length > 0){

                        for (var i = 0; i < addedintersects.length; i++) {

                            if (addedintersects[i].length > 0){
                                for (var j = 0; j < addedintersects[i].length; i++) {
                                    
                                    if(addedintersects[i][j].object.type == "LineSegments"){
                                        continue;
                                    }

                                    var addedintersection = null;
        
                                    var bPlaneX = true;
                                    var bPlaneY = true;
                                    var bPlaneZ = true;
        
                                    if(DocObj.getCurrentpage().Vector3DPageObj.clippingOn){
                                        bPlaneX = (addedintersects[i][j].point.x > -DocObj.getCurrentpage().Vector3DPageObj.globalPlaneX.constant);
                                        bPlaneY = (addedintersects[i][j].point.y > -DocObj.getCurrentpage().Vector3DPageObj.globalPlaneY.constant);
                                        bPlaneZ = (addedintersects[i][j].point.z < DocObj.getCurrentpage().Vector3DPageObj.globalPlaneZ.constant);
                                    }
        
                                    if (addedintersects[i][j].object.visible && bPlaneX && bPlaneY && bPlaneZ) {

                                        addedintersection = addedintersects[i][j];

                                        //tool.handlePoints(bmove,addedintersects[i][j]);

                                        //if (tool.started){
                                            //context.clearRect(0, 0, canvas.width, canvas.height);
                                        //}
                                        
                                        break;
                                        
                                    }
                                }
                            }
                        }
                    }

                    if (intersects.length > 0) {
                        for (var i = 0; i < intersects.length; i++) {

                            if(intersects[i].object.type == "LineSegments"){
                                continue;
                            }

                            var bPlaneX = true;
                            var bPlaneY = true;
                            var bPlaneZ = true;

                            if(DocObj.getCurrentpage().Vector3DPageObj.clippingOn){
                                bPlaneX = (intersects[i].point.x > -DocObj.getCurrentpage().Vector3DPageObj.globalPlaneX.constant);
                                bPlaneY = (intersects[i].point.y > -DocObj.getCurrentpage().Vector3DPageObj.globalPlaneY.constant);
                                bPlaneZ = (intersects[i].point.z < DocObj.getCurrentpage().Vector3DPageObj.globalPlaneZ.constant);
                            }

                            if (intersects[i].object.visible && bPlaneX && bPlaneY && bPlaneZ) {
                                //get intersection point rather than object.
                                //var insertpoint = intersects[i].point;
                                //DocObj.getCurrentpage().select3DBlock(meshid);

                                //DocObj.getCurrentpage().add3DSymbol(intersects[i], id);

                                var buseadded = false;
                                if(addedintersection != null){

                                    if(addedintersection.distance < intersects[i].distance){
                                        buseadded = true;
                                    }else{
                                        buseadded = false;
                                    }
                                    //console.log(addedintersection);
                                } 

                                if(buseadded){
                                    //var meshid = addedintersection;
                                    tool.handlePoints(bmove,addedintersection);
                                }else{
                                    tool.handlePoints(bmove,intersects[i]);

                                }

                                //tool.handlePoints(bmove,intersects[i]);
                                
                                if (tool.started){
                                    context.clearRect(0, 0, canvas.width, canvas.height);
                                }
                                //context.arc(point.x, point.y, 32 * 0.125, 0, Math.PI * 2); //full circle
                                //context.fill();
                                //console.log(point);

                                break;
                            }
                        }


                    }

                    //intersects[0].object.material.color = new THREE.Color( 0xf781f3 );

                    /*for ( var intersect in intersects ) {

                     var meshid = intersects[intersect].object.name;
                     intersects[intersect].object.material.color = new THREE.Color( 0xf781f3 );

                     }*/


                    /*var info = Viewer3D.pick(ev.clientX, ev.clientY);
                     if (info.mesh != null){
                     var meshid = info.mesh.name;
                     DocObj.getCurrentpage().select3DBlock(meshid);
                     }*/

                   
                };                

                

                this.mousemove = function (ev) {
                    ev.preventDefault();
                    ev.stopPropagation();


                    //var mousePos = getMousePos(canvaso, ev);
                    var mousePos = getMousePos(renderer.domElement, ev);


                    if (!tool.started) {
                        //if(DocObj.getCurrentpage().usevector3DGLTF)

                        

                        mouse.x = (mousePos.x / canvasowidth) * 2 - 1;
                        mouse.y = -(mousePos.y / canvasoheight) * 2 + 1;

                        tool.getInterSects(mouse, tool.color, true);

                    }

                };        

                this.mousedown = function (ev) {

                    ev.preventDefault();
                    ev.stopPropagation();

                    

                    //tool.started = true;
                    //var mousePos = getMousePos(canvaso, ev);
                    var mousePos = getMousePos(renderer.domElement, ev);

                    if (documentopen) {
                        if (DocObj.getCurrentpage().usevector3Dxml) {
                            
                            //tool.started = true;

                            mouse.x = (mousePos.x / canvasowidth) * 2 - 1;
                            mouse.y = -(mousePos.y / canvasoheight) * 2 + 1;
                            //ev.target.style.cursor = 'default';
                            
                            if(tool.numpoints < 2){
                                tool.getInterSects(mouse, tool.color, false);
                            }else{

                                tool.started = false;
                            }
                            
                            // update the picking ray with the camera and mouse position



                        }


                    }


                };

                this.mouseup = function (ev) {
                    //ev.preventDefault();
                    //ev.stopPropagation();
                    //ev.target.style.cursor = 'default';
                    /*if (tool.started) {
                        tool.started = false;
                    }*/
                };

                this.pointerdown = function (ev) {
                    tool.MSPointerDown(ev);
                };


                this.MSPointerDown = function (ev) {
                    ev.preventDefault();
                    ev.stopPropagation();

                    var mousePos = getMousePos(canvas, ev);
                    tool.started = true;
                    if (documentopen) {
                        if (DocObj.getCurrentpage().usevector3Dxml) {
                            //Viewer3D.mouseX = mousePos.x;
                            //Viewer3D.mouseY = mousePos.y;

                            mouse.x = (mousePos.x / canvasowidth) * 2 - 1;
                            mouse.y = -(mousePos.y / canvasoheight) * 2 + 1;


                            if(tool.numpoints < 2){
                                tool.getInterSects(mouse, tool.color, false);
                            }else{
                                tool.started = false;
                            }

                            

                            // update the picking ray with the camera and mouse position

                            

                        }


                    }


                };

                this.MSPointerUp = function (ev) {
                    

                    /*if (tool.started) {

                        tool.started = false;


                    }*/

                };

                this.touchstart = function (ev) {
                    ev.preventDefault();
                    tool.started = true;
                    var touchPos = getTouchPos(renderer.domElement, ev, 0);


                    /*var touchPos = {
                     x: ev.targetTouches[0].pageX,
                     y: ev.targetTouches[0].pageY
                     };*/


                    if (documentopen) {
                        if (DocObj.getCurrentpage().usevector3Dxml) {
                            //Viewer3D.mouseX = mousePos.x;
                            //Viewer3D.mouseY = mousePos.y;
                            mouse.x = (touchPos.x / canvasowidth) * 2 - 1;
                            mouse.y = -(touchPos.y / canvasoheight) * 2 + 1;

                            // update the picking ray with the camera and mouse position

                            if(tool.numpoints < 2){
                                tool.getInterSects(mouse, tool.color, false);
                            }else{
                                tool.started = false;
                            }

                            

                        }


                    }

                };

                this.touchcancel = function(ev){
                    //ev.preventDefault();
                    /*if (tool.started) {

                        tool.started = false;
                    }*/

                };                
                this.touchend = function (ev) {
                    //ev.preventDefault();
                    /*if (tool.started) {

                        tool.started = false;
                    }*/
                };
            };            

            tools.select3d = function (params) {
                var tool = this;
                var x = 0,
                    y = 0,
                    w = 0,
                    h = 0;
                var prevx = 0,
                    prevy = 0;

                var raycaster = new THREE.Raycaster();
                var mouse = new THREE.Vector2();
                var mvector = new THREE.Vector3();
                var bselectmesh = false;
                var pointindex = null;

                var threshold = 0.5;
                //set treshold to 10 cm

                threshold = DocObj.getCurrentpage().Vector3DPageObj.pointTreshold;
                raycaster.params.Points.threshold = threshold;

                /*switch (DocObj.getCurrentpage().Vector3DPageObj.unit){
                    case 0 : threshold = 20;
                    break;
                    case 1 : threshold = 0.02;
                    break;
                    case 2 : threshold = 0.16;
                    break;
                    case 3 : threshold = 1;
                    break;
                    case 4 : threshold = 2;
                    break;
                }*/
                //var threshold = 0.5;
                
                this.selectType = params.p1;

                this.name = 'select3d';
                this.anglelengthsupport = false;
                
                this.refreshSupport = false;

                this.started = false;

                //Array for MSIE10 touchpoints
                var numtouchpoints = -1;
                var touchpointarr = [];

                setCursor(getMarkupCursor('select3d'));


                this.getInterSects = function(mouse, bmove){

                    var markupmeshid = null;
                    var markuptype = null;
                    
                    //DocObj.getCurrentpage().Vector3DPageObj.unit

                    raycaster.setFromCamera(mouse, DocObj.getCurrentpage().camera);

                    // calculate objects intersecting the picking ray
                    if(DocObj.getCurrentpage().usevector3DGLTF ){
                        var mesharray = DocObj.getCurrentpage().Vector3DPageObj.getMeshChildren();
                        
                        var addedmesharray = DocObj.getCurrentpage().Vector3DPageObj.getAddedMeshChildren();

                        var markuparray = DocObj.getCurrentpage().Vector3DPageObj.getMarkupChildren();

                        
                        if (mesharray.length == 0){
                            return;
                        }
                        
                        var addedintersects = [];

                        if( addedmesharray.length == 0 ){
                            addedintersects = [];
                        }else{
                            
                            for(var adi = 0; adi < addedmesharray.length; adi ++){
                                addedintersects.push(raycaster.intersectObjects(addedmesharray[adi], true));
                            }
                            
                        }

                        if(markuparray.length == 0){
                            var markupintersects = null;
                        }else{
                            markupintersects = raycaster.intersectObjects(markuparray);
                        }
                        
                        var intersects = raycaster.intersectObjects(mesharray, true);


                    }else{
                        intersects = raycaster.intersectObjects(DocObj.getCurrentpage().Vector3DPageObj.scene.children);
                    }
                    
                    if(markupintersects){
                        if(markupintersects.length > 0){
                            for (var i = 0; i < markupintersects.length; i++) {

                                var bPlaneX = true;
                                var bPlaneY = true;
                                var bPlaneZ = true;

                                if(DocObj.getCurrentpage().Vector3DPageObj.clippingOn){
                                    bPlaneX = (markupintersects[i].point.x > -DocObj.getCurrentpage().Vector3DPageObj.globalPlaneX.constant);
                                    bPlaneY = (markupintersects[i].point.y > -DocObj.getCurrentpage().Vector3DPageObj.globalPlaneY.constant);
                                    bPlaneZ = (markupintersects[i].point.z < DocObj.getCurrentpage().Vector3DPageObj.globalPlaneZ.constant);
                                }

                                if (markupintersects[i].object.visible && bPlaneX && bPlaneY && bPlaneZ) {

                                    if(markupintersects[i].object.type == "LineSegments"){
                                        //userData.type == "dimension"

                                        continue;
                                        /*if (markupintersects[i].object.userData.type == "dimension"){
                                            if(markupintersects.length == 1){
                                                markupmeshid = markupintersects[i].object.uuid;
                                                if(!bmove){
                                                    DocObj.getCurrentpage().select3Dmarkup(markupmeshid, true);
                                                }
                                                
                                                break;
                                            }else if(markupintersects.length > 1){
                                                continue;
                                            }
                                        }*/
                                        
                                    }
    
                                    
                                    if(markupintersects[i].object.userData.type == "floorplan"){
                                        bselectmesh = true;
                                    }else if(markupintersects[i].object.type == "Points"){
                                        
                                        //get index here.

                                        markuptype = "dimension";
                                        bselectmesh = bmove;
                                        
                                        
                                        markupmeshid = markupintersects[i].object.uuid;

                                        //tool.selectType 

                                        if(!bmove){

                                            if(tool.selectType == 1){
                                                DocObj.getCurrentpage().select3Dmarkup(markupmeshid, true);
                                                pointindex = markupintersects[i].index;
                                            }

                                        }
                                        

                                    }else if(markupintersects[i].object.userData.type == "symbol"){

                                        markupmeshid = markupintersects[i].object.uuid;
                                        markuptype = "symbol";


                                        bselectmesh = bmove;
                                        if(!bmove){

                                            if(tool.selectType == 1){
                                                DocObj.getCurrentpage().select3Dmarkup(markupmeshid, true);
                                            }

                                            
                                        }
                                        break;
                                    }else if(markupintersects[i].object.userData.type == "camera"){
                                        
                                        markupmeshid = markupintersects[i].object.uuid;
                                        markuptype = "camera";
    
                                        if(tool.selectType == 1){
                                            DocObj.getCurrentpage().select3Dmarkup(markupmeshid, true);
                                        }

                                        break;

                                    }

                                }else{
                                    bselectmesh = true;
                                }
                            }
                            /*if(DocObj.getCurrentpage().getSelectedmarkup){

                            }*/
                        }else{
                            bselectmesh = true;
                        }
                    }else{
                        bselectmesh = true;
                    }
                    

                    if (addedintersects.length > 0 && bselectmesh){

                        for (var i = 0; i < addedintersects.length; i++) {

                            if (addedintersects[i].length > 0 && bselectmesh){
                                for (var j = 0; j < addedintersects[i].length; j++) {
                                    
                                    if(addedintersects[i][j].object.type == "LineSegments"){
                                        continue;
                                    }

                                    var addedintersection = null;
        
                                    var bPlaneX = true;
                                    var bPlaneY = true;
                                    var bPlaneZ = true;
        
                                    if(DocObj.getCurrentpage().Vector3DPageObj.clippingOn){
                                        bPlaneX = (addedintersects[i][j].point.x > -DocObj.getCurrentpage().Vector3DPageObj.globalPlaneX.constant);
                                        bPlaneY = (addedintersects[i][j].point.y > -DocObj.getCurrentpage().Vector3DPageObj.globalPlaneY.constant);
                                        bPlaneZ = (addedintersects[i][j].point.z < DocObj.getCurrentpage().Vector3DPageObj.globalPlaneZ.constant);
                                    }
        
                                    if (addedintersects[i][j].object.visible && bPlaneX && bPlaneY && bPlaneZ) {
                                        
                                        if(bmove){
                                            //var meshid = markupintersects[i].object.uuid;
                                            //console.log(markupmeshid);
        
                                            //move all selected markups 
        
        
                                            DocObj.getCurrentpage().moveSelected3DMarkup(addedintersects[i][j].point, pointindex);
        
                                            /*if(markupmeshid != null && markuptype == "symbol"){
                                                DocObj.getCurrentpage().move3DMarkup(markupmeshid, intersects[i].point);
                                            }else if(markupmeshid != null && markuptype == "dimension"){
                                                DocObj.getCurrentpage().move3DMarkupdimPoint(markupmeshid,intersects[i].point, pointindex);
                                            }*/
        
        
                                            break;
                                        }else{
        
                                            if(tool.selectType == 0){
                                                addedintersection = addedintersects[i][j];
                                                break;
                                            }
            
                                        }                        
                                    }
                                }
                            }
                        }
                    }


                    //type: "LineSegments"
                    if (intersects.length > 0 && bselectmesh) {
                        for (var i = 0; i < intersects.length; i++) {
                            
                            if(intersects[i].object.type == "LineSegments"){
                                continue;
                            }

                            var bPlaneX = true;
                            var bPlaneY = true;
                            var bPlaneZ = true;

                            if(DocObj.getCurrentpage().Vector3DPageObj.clippingOn){
                                bPlaneX = (intersects[i].point.x > -DocObj.getCurrentpage().Vector3DPageObj.globalPlaneX.constant);
                                bPlaneY = (intersects[i].point.y > -DocObj.getCurrentpage().Vector3DPageObj.globalPlaneY.constant);
                                bPlaneZ = (intersects[i].point.z < DocObj.getCurrentpage().Vector3DPageObj.globalPlaneZ.constant);
                            }

                            if (intersects[i].object.visible && bPlaneX && bPlaneY && bPlaneZ) {
                                
                                if(bmove){
                                    //var meshid = markupintersects[i].object.uuid;
                                    //console.log(markupmeshid);

                                    //move all selected markups 


                                    DocObj.getCurrentpage().moveSelected3DMarkup(intersects[i].point, pointindex);

                                    /*if(markupmeshid != null && markuptype == "symbol"){
                                        DocObj.getCurrentpage().move3DMarkup(markupmeshid, intersects[i].point);
                                    }else if(markupmeshid != null && markuptype == "dimension"){
                                        DocObj.getCurrentpage().move3DMarkupdimPoint(markupmeshid,intersects[i].point, pointindex);
                                    }*/


                                    break;
                                }else{

                                    if(tool.selectType == 0){
                                        var buseadded = false;
                                        if(addedintersection != null){

                                            if(addedintersection.distance < intersects[i].distance){
                                                buseadded = true;
                                            }else{
                                                buseadded = false;
                                            }
                                            //console.log(addedintersection);
                                        } 

                                        if(buseadded){
                                            var meshid = addedintersection.object.name;
                                        }else{
                                            var meshid = intersects[i].object.name;



                                        }

                                        DocObj.getCurrentpage().select3DBlock(meshid, true, buseadded);
                                        pointindex = null;

                                        
                                        break;
                                    }
    
                                }                        
                            }
                        }
                    }else{
                        if(addedintersection != null){

                            meshid = addedintersection.object.name;
                            meshtype = addedintersection.object.type;
                            
                            meshid = DocObj.getCurrentpage().getMeshName(meshid, meshtype);

                            //var meshid = addedintersection.object.name;
                            DocObj.getCurrentpage().select3DBlock(meshid, true, buseadded);
                            pointindex = null;

                        }
                    }

                   
                };

                this.mousedown = function (ev) {

                    ev.preventDefault();
                    ev.stopPropagation();

                    DocObj.getCurrentpage().camera.updateProjectionMatrix();

                    tool.started = true;
                    //var mousePos = getMousePos(canvaso, ev);
                    var mousePos = getMousePos(renderer.domElement, ev);

                    if (documentopen) {
                        if (DocObj.getCurrentpage().usevector3Dxml) {

                            mouse.x = (mousePos.x / canvasowidth) * 2 - 1;
                            mouse.y = -(mousePos.y / canvasoheight) * 2 + 1;

                            tool.getInterSects(mouse, false);
                            // update the picking ray with the camera and mouse position

                        }

                    }

                };

                this.mouseup = function (ev) {
                    ev.preventDefault();
                    ev.stopPropagation();
                    
                    if (tool.started) {
                        tool.started = false;
                    }
                };

                this.mousemove = function(ev){
                    ev.preventDefault();
                    ev.stopPropagation();
                    
                    //ev.target.style.cursor = 'default';

                    //ev.target.style.cursor = 'crosshair';
                    
                    /*if (!tool.started) {
                        ev.target.style.cursor = 'default';
                    }*/
                    

                    var mousePos = getMousePos(renderer.domElement, ev);

                    if (documentopen) {
                        if (DocObj.getCurrentpage().usevector3Dxml) {

                            //if markup selected
                            mouse.x = (mousePos.x / canvasowidth) * 2 - 1;
                            mouse.y = -(mousePos.y / canvasoheight) * 2 + 1;

                            //implement later when markup selected check in place.
                            //tool.getInterSects(mouse);
                            // update the picking ray with the camera and mouse position
                            if (tool.started) {
                                tool.getInterSects(mouse, true);
                            }
                            if(DocObj.curcontrol3D == 'markupedit'){
                                //render 3D points.
                                //DocObj.getCurrentpage().Vector3DPageObj.renderMarkupPoints(cntximg);
                                drawmarkupAll(cntximg);
                            }

                            
                            //DocObj.getCurrentpage().delete3DMarkup();


                        }


                    }

                };

                this.pointerdown = function (ev) {
                    tool.MSPointerDown(ev);
                };


                this.MSPointerDown = function (ev) {
                    ev.preventDefault();
                    ev.stopPropagation();
                    
                    DocObj.getCurrentpage().camera.updateProjectionMatrix();

                    var mousePos = getMousePos(canvas, ev);
                    tool.started = true;
                    if (documentopen) {
                        if (DocObj.getCurrentpage().usevector3Dxml) {
                            //Viewer3D.mouseX = mousePos.x;
                            //Viewer3D.mouseY = mousePos.y;

                            mouse.x = (mousePos.x / canvasowidth) * 2 - 1;
                            mouse.y = -(mousePos.y / canvasoheight) * 2 + 1;

                            tool.getInterSects(mouse);


                            // update the picking ray with the camera and mouse position

                            

                        }


                    }


                };

                this.MSPointerUp = function (ev) {
                    
                    if (tool.started) {

                        tool.started = false;


                    }

                };

                this.touchstart = function (ev) {
                    ev.preventDefault();
                    tool.started = true;

                    DocObj.getCurrentpage().camera.updateProjectionMatrix();

                    var touchPos = getTouchPos(renderer.domElement, ev, 0);


                    /*var touchPos = {
                     x: ev.targetTouches[0].pageX,
                     y: ev.targetTouches[0].pageY
                     };*/


                    if (documentopen) {
                        if (DocObj.getCurrentpage().usevector3Dxml) {
                            //Viewer3D.mouseX = mousePos.x;
                            //Viewer3D.mouseY = mousePos.y;
                            mouse.x = (touchPos.x / canvasowidth) * 2 - 1;
                            mouse.y = -(touchPos.y / canvasoheight) * 2 + 1;

                            // update the picking ray with the camera and mouse position
                            tool.getInterSects(mouse);

                        }

                    }

                };

                this.touchcancel = function(ev){
                    ev.preventDefault();
                    if (tool.started) {

                        tool.started = false;
                    }

                };                
                this.touchend = function (ev) {
                    ev.preventDefault();
                    if (tool.started) {

                        tool.started = false;
                    }
                };


                this.keydown = function (ev) {
                    //ev.preventDefault();

                    if(DocObj == null || DocObj == undefined){
                        return;
                    }
            
                    if (!documentopen || DocObj.bMarkupLocked) {
                        return;
                    }

                    if(readonlymode){
                        DocObj.bMarkupLocked = readonlymode;
                    }




                    switch (ev.keyCode) {
                        case 13:
                            break;
                        case 8:
                            break;
                        case 46:
                            //add funciton for delete selected 3D markup here.
                            //deletemarkup();

                            DocObj.getCurrentpage().delete3DMarkup();
                            break;

                    }

                };

            };

            tools.Walkthrough3D = function(){

                var tool = this;

                this.name = 'Walkthrough3D';
                this.anglelengthsupport = false;
                this.refreshSupport = false;
                this.started = false;

                this.enabled = true;
                this.noZoom = false;
                this.enableSelect = false;


                const moveForward = 1;
                const moveBackward = 2;
                const moveLeft = 3;
                const moveRight = 4;
                const moveUp = 5;
                const moveDown = 6;

                this.selectType = null;

                var raycaster = new THREE.Raycaster();
                var mouse = new THREE.Vector2();
                var mvector = new THREE.Vector3();
                var bselectmesh = false;
                var pointindex = null;

                var threshold = 0.5;
                //set treshold to 10 cm

                threshold = DocObj.getCurrentpage().Vector3DPageObj.pointTreshold;
                raycaster.params.Points.threshold = threshold;

                

                DocObj.getCurrentpage().walkthroughcontrol.isLocked = true;


                setCursor(getMarkupCursor('Walkthrough3D'));


                this.setSelect = function(onoff,type){
                    
                    tool.enableSelect = onoff;
                    tool.selectType = type;
                    
                    //0 = block
                    //1 = markup


                };

                this.getInterSects = function(mouse, bmove){

                    var markupmeshid = null;
                    var markuptype = null;
                    
                    //DocObj.getCurrentpage().Vector3DPageObj.unit

                    

                    raycaster.setFromCamera(mouse, DocObj.getCurrentpage().camera);

                    // calculate objects intersecting the picking ray
                    if(DocObj.getCurrentpage().usevector3DGLTF ){
                        var mesharray = DocObj.getCurrentpage().Vector3DPageObj.getMeshChildren();
                        
                        var addedmesharray = DocObj.getCurrentpage().Vector3DPageObj.getAddedMeshChildren();

                        var markuparray = DocObj.getCurrentpage().Vector3DPageObj.getMarkupChildren();

                        
                        if (mesharray.length == 0){
                            return;
                        }
                        
                        var addedintersects = [];

                        if( addedmesharray.length == 0 ){
                            addedintersects = [];
                        }else{
                            
                            for(var adi = 0; adi < addedmesharray.length; adi ++){
                                addedintersects.push(raycaster.intersectObjects(addedmesharray[adi], true));
                            }
                            
                        }

                        if(markuparray.length == 0){
                            var markupintersects = null;
                        }else{
                            markupintersects = raycaster.intersectObjects(markuparray);
                        }
                        
                        var intersects = raycaster.intersectObjects(mesharray, true);


                    }else{
                        intersects = raycaster.intersectObjects(DocObj.getCurrentpage().Vector3DPageObj.scene.children);
                    }
                    
                    if(markupintersects){
                        if(markupintersects.length > 0){
                            for (var i = 0; i < markupintersects.length; i++) {

                                var bPlaneX = true;
                                var bPlaneY = true;
                                var bPlaneZ = true;

                                if(DocObj.getCurrentpage().Vector3DPageObj.clippingOn){
                                    bPlaneX = (markupintersects[i].point.x > -DocObj.getCurrentpage().Vector3DPageObj.globalPlaneX.constant);
                                    bPlaneY = (markupintersects[i].point.y > -DocObj.getCurrentpage().Vector3DPageObj.globalPlaneY.constant);
                                    bPlaneZ = (markupintersects[i].point.z < DocObj.getCurrentpage().Vector3DPageObj.globalPlaneZ.constant);
                                }
                                
    

                                if (markupintersects[i].object.visible && bPlaneX && bPlaneY && bPlaneZ) {

                                    if(markupintersects[i].object.type == "LineSegments"){
                                        //userData.type == "dimension"

                                        continue;
                                        /*if (markupintersects[i].object.userData.type == "dimension"){
                                            if(markupintersects.length == 1){
                                                markupmeshid = markupintersects[i].object.uuid;
                                                if(!bmove){
                                                    DocObj.getCurrentpage().select3Dmarkup(markupmeshid, true);
                                                }
                                                
                                                break;
                                            }else if(markupintersects.length > 1){
                                                continue;
                                            }
                                        }*/
                                        
                                    }
    
                                    
                                    if(markupintersects[i].object.userData.type == "floorplan"){
                                        bselectmesh = true;
                                    }else if(markupintersects[i].object.type == "Points"){
                                        
                                        //get index here.

                                        markuptype = "dimension";
                                        bselectmesh = bmove;
                                        
                                        
                                        markupmeshid = markupintersects[i].object.uuid;

                                        //tool.selectType 

                                        if(!bmove){

                                            if(tool.selectType == 1){
                                                DocObj.getCurrentpage().select3Dmarkup(markupmeshid, true);
                                                pointindex = markupintersects[i].index;
                                            }
                                        }
                                        

                                    }else if(markupintersects[i].object.userData.type == "symbol"){

                                        markupmeshid = markupintersects[i].object.uuid;
                                        markuptype = "symbol";


                                        bselectmesh = bmove;
                                        if(!bmove){

                                            if(tool.selectType == 1){
                                                DocObj.getCurrentpage().select3Dmarkup(markupmeshid, true);
                                            }
                                            
                                        }
                                        

                                        break;

    
                                    }else if(markupintersects[i].object.userData.type == "camera"){
                                        markupmeshid = markupintersects[i].object.uuid;
                                        markuptype = "camera";
    
                                        if(tool.selectType == 1){
                                            DocObj.getCurrentpage().select3Dmarkup(markupmeshid, true);
                                        }

                                        break;
                                    }

                                }else{
                                    bselectmesh = true;
                                }
                            }
                            /*if(DocObj.getCurrentpage().getSelectedmarkup){

                            }*/
                        }else{
                            bselectmesh = true;
                        }
                    }else{
                        bselectmesh = true;
                    }
                    

                    if (addedintersects.length > 0 && bselectmesh){

                        for (var i = 0; i < addedintersects.length; i++) {

                            if (addedintersects[i].length > 0 && bselectmesh){
                                for (var j = 0; j < addedintersects[i].length; j++) {
                                    
                                    if(addedintersects[i][j].object.type == "LineSegments"){
                                        continue;
                                    }

                                    var addedintersection = null;
        
                                    var bPlaneX = true;
                                    var bPlaneY = true;
                                    var bPlaneZ = true;
        
                                    if(DocObj.getCurrentpage().Vector3DPageObj.clippingOn){
                                        bPlaneX = (addedintersects[i][j].point.x > -DocObj.getCurrentpage().Vector3DPageObj.globalPlaneX.constant);
                                        bPlaneY = (addedintersects[i][j].point.y > -DocObj.getCurrentpage().Vector3DPageObj.globalPlaneY.constant);
                                        bPlaneZ = (addedintersects[i][j].point.z < DocObj.getCurrentpage().Vector3DPageObj.globalPlaneZ.constant);
                                    }
        
                                    if (addedintersects[i][j].object.visible && bPlaneX && bPlaneY && bPlaneZ) {
                                        
                                        if(bmove){
                                            //var meshid = markupintersects[i].object.uuid;
                                            //console.log(markupmeshid);
        
                                            //move all selected markups 
        
        
                                            DocObj.getCurrentpage().moveSelected3DMarkup(addedintersects[i][j].point, pointindex);
        
                                            /*if(markupmeshid != null && markuptype == "symbol"){
                                                DocObj.getCurrentpage().move3DMarkup(markupmeshid, intersects[i].point);
                                            }else if(markupmeshid != null && markuptype == "dimension"){
                                                DocObj.getCurrentpage().move3DMarkupdimPoint(markupmeshid,intersects[i].point, pointindex);
                                            }*/
        
        
                                            break;
                                        }else{
        
                                            if(tool.selectType == 0){
                                                addedintersection = addedintersects[i][j];
                                                break;
                                            }
            
                                        }                        
                                    }
                                }
                            }
                        }
                    }


                    //type: "LineSegments"
                    if (intersects.length > 0 && bselectmesh) {
                        for (var i = 0; i < intersects.length; i++) {
                            
                            if(intersects[i].object.type == "LineSegments"){
                                continue;
                            }

                            var bPlaneX = true;
                            var bPlaneY = true;
                            var bPlaneZ = true;

                            if(DocObj.getCurrentpage().Vector3DPageObj.clippingOn){
                                bPlaneX = (intersects[i].point.x > -DocObj.getCurrentpage().Vector3DPageObj.globalPlaneX.constant);
                                bPlaneY = (intersects[i].point.y > -DocObj.getCurrentpage().Vector3DPageObj.globalPlaneY.constant);
                                bPlaneZ = (intersects[i].point.z < DocObj.getCurrentpage().Vector3DPageObj.globalPlaneZ.constant);
                            }

                            if (intersects[i].object.visible && bPlaneX && bPlaneY && bPlaneZ) {
                                
                                if(bmove){
                                    //var meshid = markupintersects[i].object.uuid;
                                    //console.log(markupmeshid);

                                    //move all selected markups 


                                    DocObj.getCurrentpage().moveSelected3DMarkup(intersects[i].point, pointindex);

                                    /*if(markupmeshid != null && markuptype == "symbol"){
                                        DocObj.getCurrentpage().move3DMarkup(markupmeshid, intersects[i].point);
                                    }else if(markupmeshid != null && markuptype == "dimension"){
                                        DocObj.getCurrentpage().move3DMarkupdimPoint(markupmeshid,intersects[i].point, pointindex);
                                    }*/


                                    break;
                                }else{

                                    if(tool.selectType == 0){
                                        var buseadded = false;
                                        if(addedintersection != null){

                                            if(addedintersection.distance < intersects[i].distance){
                                                buseadded = true;
                                            }else{
                                                buseadded = false;
                                            }
                                            //console.log(addedintersection);
                                        } 

                                        if(buseadded){
                                            var meshid = addedintersection.object.name;
                                            var meshtype = addedintersection.object.type;

                                        }else{
                                            meshid = intersects[i].object.name;
                                            meshtype = intersects[i].object.type;

                                        }
                                        
                                        meshid = DocObj.getCurrentpage().getMeshName(meshid, meshtype);

                                        DocObj.getCurrentpage().select3DBlock(meshid, true, buseadded);
                                        pointindex = null;

                                        
                                        break;
                                    }
    
                                }                        
                            }
                        }
                    }else{
                        if(addedintersection != null){

                            meshid = addedintersection.object.name;
                            meshtype = addedintersection.object.type;

                            meshid = DocObj.getCurrentpage().getMeshName(meshid, meshtype);
                            
                            DocObj.getCurrentpage().select3DBlock(meshid, true, buseadded);
                            pointindex = null;

                        }
                    }

                    //intersects[0].object.material.color = new THREE.Color( 0xf781f3 );

                    /*for ( var intersect in intersects ) {

                     var meshid = intersects[intersect].object.name;
                     intersects[intersect].object.material.color = new THREE.Color( 0xf781f3 );

                     }*/


                    /*var info = Viewer3D.pick(ev.clientX, ev.clientY);
                     if (info.mesh != null){
                     var meshid = info.mesh.name;
                     DocObj.getCurrentpage().select3DBlock(meshid);
                     }*/

                   
                };



                //PointerControls
                this.commonMouseDown = function(ev, button){

                    if (documentopen) {
                        if (DocObj.getCurrentpage().usevector3Dxml) {

                            var walktrhroughcntrl = DocObj.getCurrentpage().walkthroughcontrol;

                            if(tool.started){

                                walktrhroughcntrl.onMouseDown(button);

                                
                            }else{
                                walktrhroughcntrl.onMouseDown(button);
                                tool.started = true;
                            }
                            
                            if(tool.enableSelect){

                                tool.selectstarted = true;

                                var mousePos = getMousePos(renderer.domElement, ev);
                                DocObj.getCurrentpage().camera.updateProjectionMatrix();
                                mouse.x = (mousePos.x / canvasowidth) * 2 - 1;
                                mouse.y = -(mousePos.y / canvasoheight) * 2 + 1;
                                tool.getInterSects(mouse, false);
                            }

                        }
                    }

                };

                this.commonMouseMove = function(ev, x, y){
                    
                    if (documentopen) {

                        if (DocObj.getCurrentpage().usevector3Dxml) {

                            var walktrhroughcntrl = DocObj.getCurrentpage().walkthroughcontrol;
                            
                            //DocObj.getCurrentpage().walkthroughcontrol.onMouseMove(x, y);
                            walktrhroughcntrl.onMouseMove(ev);
                            

                            if(tool.enableSelect){
                                //var mousePos = getMousePos(renderer.domElement, ev);
                                mouse.x = (x / canvasowidth) * 2 - 1;
                                mouse.y = -(y / canvasoheight) * 2 + 1;

                                if (tool.selectstarted) {
                                    tool.getInterSects(mouse, true);
                                }
    
                            }
                        }
                    }

                };

                this.commonMouseOut = function(){
                    
                    if (documentopen) {
                        if (DocObj.getCurrentpage().usevector3Dxml) {

                            //tool.selectstarted = false;

                            for (var diri = 1; diri < 7;diri++){
                                DocObj.getCurrentpage().walkthroughcontrol.movedirection(diri,false);
                            }   
                        }
                    }

                };


                this.commonMouseUp = function(ev){
                    
                    
                    if (documentopen) {
                        if (DocObj.getCurrentpage().usevector3Dxml) {
                            
                            tool.selectstarted = false;
                            DocObj.getCurrentpage().walkthroughcontrol.onMouseUp(ev.button);

                        }
                    }

                    


                };

                    
    

                //touch down mouse down
                this.mousedown = function (ev) {

                    ev.preventDefault();
                    tool.commonMouseDown(ev,ev.button);

                    //ev.stopPropagation();
                };


                this.touchstart = function (ev) {
                    ev.preventDefault();
                    tool.commonMouseDown(ev,0);

                };                


                this.pointerdown = function (ev) {
                    scope.MSPointerDown(ev);
                };


                this.MSPointerDown = function (ev) {
                    ev.preventDefault();
                    tool.commonMouseDown(ev,ev.button);


                };
                //touch down mouse down

                //touch move mouse move
                this.pointermove = function (ev) {
                    scope.MSPointerMove(ev);
                };



                this.mousemove = function (ev) {

                    var mousePos = getMousePos(renderer.domElement, ev);
                    tool.commonMouseMove(ev, mousePos.x, mousePos.y);
                    
                };


                this.MSPointerMove = function (ev) {
                    ev.preventDefault();
                    var mousePos = getMousePos(renderer.domElement, ev);
                    tool.commonMouseMove(ev, mousePos.x, mousePos.y);
                };


                this.touchmove = function (ev) {
                    ev.preventDefault();
                    var mousePos = getTouchPos(renderer.domElement, ev, 0);
                    tool.commonMouseMove(ev, mousePos.x, mousePos.y);
                };                
                //touch move mouse move




                //touch up mouse up
                this.mouseup = function (ev) {

                    ev.preventDefault();
                    tool.commonMouseUp(ev);                    
                    tool.commonMouseMove(0,0);

                };
                this.pointerup = function (ev) {
                    scope.MSPointerUp(ev);

                };


                this.MSPointerUp = function (ev) {
                    ev.preventDefault();
                    tool.commonMouseUp(ev);                    
                    tool.commonMouseMove(0,0);
                };

                this.pointerout = function (ev) {
                    scope.MSPointerOut(ev);
                };

                this.touchcancel = function(ev){
                    ev.preventDefault();
                    tool.commonMouseUp(ev);
                    tool.commonMouseMove(0,0);

                };                
    
                this.touchend = function (ev) {
                    ev.preventDefault();
                    tool.commonMouseUp(ev);
                    tool.commonMouseMove(0,0);

                };
                //touch up mouse up

                

                this.MSPointerOut = function (ev) {
                    ev.preventDefault();

                    tool.commonMouseOut();


                };


                this.mouseout = function (ev) {
                    ev.preventDefault();
                    tool.commonMouseOut();
                };                




                this.keydown = function (ev) {
                    //if ( scope.enabled === false || scope.noKeys === true || scope.noPan === true ) return;

                    if (ev.altKey) {
                        return;
                    }

                    if (documentopen) {
                        if (DocObj.getCurrentpage().usevector3Dxml) {
                            switch (ev.keyCode) {

                                //case 16: /* shift */ DocObj.getCurrentpage().walkthroughcontrol.movementSpeedMultiplier = .1; break;
                                case 16:
                                    //DocObj.getCurrentpage().walkthroughcontrol.mouseMove = true;
                                    DocObj.getCurrentpage().walkthroughcontrol.quickspeed(true);
                                    break;
                                case 87:
                                    /*W*/
                                    DocObj.getCurrentpage().walkthroughcontrol.movedirection(moveForward,true);
                                    break;
                                case 83:
                                    /*S*/
                                    DocObj.getCurrentpage().walkthroughcontrol.movedirection(moveBackward,true);
                                    break;

                                case 65:
                                    /*A*/
                                    DocObj.getCurrentpage().walkthroughcontrol.movedirection(moveLeft,true);
                                    break;
                                case 68:
                                    /*D*/
                                    DocObj.getCurrentpage().walkthroughcontrol.movedirection(moveRight,true);
                                    break;
                                case 82:
                                    /*R*/
                                    DocObj.getCurrentpage().walkthroughcontrol.movedirection(moveUp,true);
                                    break;
                                case 70:
                                    /*F*/
                                    DocObj.getCurrentpage().walkthroughcontrol.movedirection(moveDown,true);
                                    break;
                                case 33:
                                    /*up*/
                                    DocObj.getCurrentpage().walkthroughcontrol.movedirection(moveUp,true);
                                    break;
                                case 34:
                                    /*down*/
                                    DocObj.getCurrentpage().walkthroughcontrol.movedirection(moveDown,true);
                                    //console.log("forward");
                                    break;
                                case 38:
                                    /*up*/
                                    DocObj.getCurrentpage().walkthroughcontrol.movedirection(moveForward,true);

                                    break;
                                case 40:
                                    /*down*/
                                    DocObj.getCurrentpage().walkthroughcontrol.movedirection(moveBackward,true);
                                    break;
                                //case 38: /*up*/ DocObj.getCurrentpage().walkthroughcontrol.moveState.pitchUp = 1; break;
                                //case 40: /*down*/ DocObj.getCurrentpage().walkthroughcontrol.moveState.pitchDown = 1; break;

                                case 37:
                                    /*left*/
                                    DocObj.getCurrentpage().walkthroughcontrol.movedirection(moveLeft,true);

                                    break;
                                case 39:
                                    /*right*/
                                    DocObj.getCurrentpage().walkthroughcontrol.movedirection(moveRight,true);

                                    break;

                                //case 37: /*left*/ DocObj.getCurrentpage().walkthroughcontrol.moveState.yawLeft = 1; break;
                                //case 39: /*right*/ DocObj.getCurrentpage().walkthroughcontrol.moveState.yawRight = 1; break;

                                case 81:
                                    /*Q*/
                                    break;
                                case 69:
                                    /*E*/
                                    break;

                            }
                        }
                    }

                };


                this.keyup = function (ev) {
                    //if ( scope.enabled === false || scope.noKeys === true || scope.noPan === true ) return;

                    if (documentopen) {
                        if (DocObj.getCurrentpage().usevector3Dxml) {
                            switch (ev.keyCode) {
                                
                                //case 16: /* shift */ DocObj.getCurrentpage().walkthroughcontrol.movementSpeedMultiplier = .1; break;
                                case 16:
                                    DocObj.getCurrentpage().walkthroughcontrol.quickspeed(false);
                                    //DocObj.getCurrentpage().walkthroughcontrol.mouseMove = false;
                                    break;
                                case 87:
                                    /*W*/
                                    DocObj.getCurrentpage().walkthroughcontrol.movedirection(moveForward,false);
                                    break;
                                case 83:
                                    /*S*/
                                    DocObj.getCurrentpage().walkthroughcontrol.movedirection(moveBackward,false);
                                    break;
                                case 65:
                                    /*A*/
                                    DocObj.getCurrentpage().walkthroughcontrol.movedirection(moveLeft,false);
                                    break;
                                case 68:
                                    /*D*/
                                    DocObj.getCurrentpage().walkthroughcontrol.movedirection(moveRight,false);
                                    break;
                                case 82:
                                    /*R*/
                                    DocObj.getCurrentpage().walkthroughcontrol.movedirection(moveUp,false);
                                    break;
                                case 70:
                                    /*F*/
                                    DocObj.getCurrentpage().walkthroughcontrol.movedirection(moveDown,false);
                                    break;
                                case 33:
                                    /*up*/
                                    DocObj.getCurrentpage().walkthroughcontrol.movedirection(moveUp,false);
                                    break;
                                case 34:
                                    /*down*/
                                    DocObj.getCurrentpage().walkthroughcontrol.movedirection(moveDown,false);
                                    break;
                                case 38:
                                    /*up*/
                                    DocObj.getCurrentpage().walkthroughcontrol.movedirection(moveForward,false);
                                    break;
                                case 40:
                                    /*down*/
                                    DocObj.getCurrentpage().walkthroughcontrol.movedirection(moveBackward,false);
                                    break;
                                case 37:
                                    /*left*/
                                    DocObj.getCurrentpage().walkthroughcontrol.movedirection(moveLeft,false);
                                    break;
                                case 39:
                                    /*right*/
                                    DocObj.getCurrentpage().walkthroughcontrol.movedirection(moveRight,false);
                                    break;
                                case 81:
                                    /*Q*/
                                    //DocObj.getCurrentpage().walkthroughcontrol.moveState.rollLeft = 0;
                                    break;
                                case 69:
                                    /*E*/
                                    //DocObj.getCurrentpage().walkthroughcontrol.moveState.rollRight = 0;
                                    break;

                            }
                        }
                    }

                };

                this.mousewheel = function(ev){
                    tool.onMouseWheel(ev);
                };

                this.wheel = function (ev) {
                    tool.onMouseWheel(ev)
                };


                this.DOMMouseScroll = function (ev) {
                    //scope.onMouseWheel(ev)
                };

                this.onMouseWheel = function (ev) {
                    if (tool.enabled === false || tool.noZoom === true) return;
                    ev.preventDefault();
                    ev.stopPropagation();


                    if (documentopen) {
                        if (DocObj.getCurrentpage().usevector3Dxml) {
                            var delta = 0;
                            if (ev.wheelDelta !== undefined) { // WebKit / Opera / Explorer 9

                                delta = ev.wheelDelta;

                            } else if (ev.deltaY !== undefined) { // Firefox

                                delta = -ev.deltaY;

                            }

                            if (delta > 0) {

                                DocObj.getCurrentpage().walkthroughcontrol.scrollforward(1);
                                
                                


                            } else {

                                DocObj.getCurrentpage().walkthroughcontrol.scrollbackward(1);
                                


                                //scope.dollyIn();

                            }

                            //scope.update();


                        }
                    }
                };

            };            

            tools.orbitControl = function () {
                var tool = this;
                var x = 0,
                    y = 0,
                    w = 0,
                    h = 0;
                var prevx = 0,
                    prevy = 0;
                //Array for MSIE10 touchpoints
                var numtouchpoints = -1;
                var touchpointarr = [];

                this.name = 'orbitControl';
                this.anglelengthsupport = false;
                this.refreshSupport = false;

                this.object = DocObj.getCurrentpage().camera;
                this.domElement = renderer.domElement;

                //this.domElement = ( domElement !== undefined ) ? domElement : document;

                // API

                // Set to false to disable this control
                this.enabled = true;

                // "target" sets the location of focus, where the control orbits around
                // and where it pans with respect to.
                this.target = new THREE.Vector3();
                //this.target.set(0, 5, 0);
                //this.update();

                // center is old, deprecated; use "target" instead
                this.center = this.target;

                this.noZoom = false;
                this.zoomSpeed = 0.5;

                // Limits to how far you can dolly in and out
                this.minDistance = 0;
                this.maxDistance = Infinity;

                // Set to true to disable this control
                this.noRotate = false;
                this.rotateSpeed = 1.0;

                // Set to true to disable this control
                this.noPan = false;
                this.keyPanSpeed = 7.0; // pixels moved per arrow key push

                // Set to true to automatically rotate around the target
                this.autoRotate = false;
                this.autoRotateSpeed = 2.0; // 30 seconds per round when fps is 60

                // How far you can orbit vertically, upper and lower limits.
                // Range is 0 to Math.PI radians.
                this.minPolarAngle = 0; // radians
                this.maxPolarAngle = Math.PI; // radians

                //this.curmousepos  = { x : 0, y : 0 };
                this.curmousepos = {x:0,y:0,xus:0,yus:0};

                // Set to true to disable use of the keys
                this.noKeys = false;

                this.keys = {
                    LEFT:37,
                    UP:38,
                    RIGHT:39,
                    BOTTOM:40
                };

                ////////////
                // internals

                var scope = this;

                var EPS = 0.000001;

                var rotateStart = new THREE.Vector2();
                var rotateEnd = new THREE.Vector2();
                var rotateDelta = new THREE.Vector2();

                var panStart = new THREE.Vector2();
                var panEnd = new THREE.Vector2();
                var panDelta = new THREE.Vector2();
                var panOffset = new THREE.Vector3();

                var offset = new THREE.Vector3();

                var dollyStart = new THREE.Vector2();
                var dollyEnd = new THREE.Vector2();
                var dollyDelta = new THREE.Vector2();

                var phiDelta = 0;
                var thetaDelta = 0;
                var scale = 1;
                var pan = new THREE.Vector3();

                var lastPosition = new THREE.Vector3();
                var lastQuaternion = new THREE.Quaternion();

                var STATE = {
                    NONE:-1,
                    ROTATE:0,
                    DOLLY:1,
                    PAN:2,
                    TOUCH_ROTATE:3,
                    TOUCH_DOLLY:4,
                    TOUCH_PAN:5
                };

                var state = STATE.NONE;

                // for reset

                //this.target0 = this.target.clone();
                //this.position0 = this.object.position.clone();

                // so camera.up is the orbit axis


                var quat = new THREE.Quaternion().setFromUnitVectors(DocObj.getCurrentpage().camera.up, new THREE.Vector3(0, 1, 0));

                var threerev = parseInt(THREE.REVISION);
                

                if (threerev <= 127){
                    var quatInverse = quat.clone().inverse();
                }else{
                    quatInverse = quat.clone().invert();
                }

                var changeEvent = {
                    type:'change'
                };
                var startEvent = {
                    type:'start'
                };
                var endEvent = {
                    type:'end'
                };

                this.selectstarted = false;

                this.enableSelect = false;

                this.selectType = null;

                var raycaster = new THREE.Raycaster();
                var mouse = new THREE.Vector2();
                var mvector = new THREE.Vector3();
                var bselectmesh = false;
                var pointindex = null;

                var threshold = 0.5;
                //set treshold to 10 cm

                threshold = DocObj.getCurrentpage().Vector3DPageObj.pointTreshold;
                raycaster.params.Points.threshold = threshold;
                
                setCursor(getMarkupCursor('orbitControl'));

                this.setSelect = function(onoff,type){
                    tool.enableSelect = onoff;
                    tool.selectType = type;
                    
                    //0 = block
                    //1 = markup


                };

                this.getInterSects = function(mouse, bmove){

                    var markupmeshid = null;
                    var markuptype = null;
                    
                    //DocObj.getCurrentpage().Vector3DPageObj.unit

                    

                    raycaster.setFromCamera(mouse, DocObj.getCurrentpage().camera);

                    // calculate objects intersecting the picking ray
                    if(DocObj.getCurrentpage().usevector3DGLTF ){
                        var mesharray = DocObj.getCurrentpage().Vector3DPageObj.getMeshChildren();
                        
                        var addedmesharray = DocObj.getCurrentpage().Vector3DPageObj.getAddedMeshChildren();

                        var markuparray = DocObj.getCurrentpage().Vector3DPageObj.getMarkupChildren();

                        
                        if (mesharray.length == 0){
                            return;
                        }
                        
                        var addedintersects = [];

                        if( addedmesharray.length == 0 ){
                            addedintersects = [];
                        }else{
                            
                            for(var adi = 0; adi < addedmesharray.length; adi ++){
                                addedintersects.push(raycaster.intersectObjects(addedmesharray[adi], true));
                            }
                            
                        }

                        if(markuparray.length == 0){
                            var markupintersects = null;
                        }else{
                            markupintersects = raycaster.intersectObjects(markuparray);
                        }
                        
                        var intersects = raycaster.intersectObjects(mesharray, true);


                    }else{
                        intersects = raycaster.intersectObjects(DocObj.getCurrentpage().Vector3DPageObj.scene.children);
                    }
                    
                    if(markupintersects){
                        if(markupintersects.length > 0){
                            for (var i = 0; i < markupintersects.length; i++) {

                                var bPlaneX = true;
                                var bPlaneY = true;
                                var bPlaneZ = true;

                                if(DocObj.getCurrentpage().Vector3DPageObj.clippingOn){
                                    bPlaneX = (markupintersects[i].point.x > -DocObj.getCurrentpage().Vector3DPageObj.globalPlaneX.constant);
                                    bPlaneY = (markupintersects[i].point.y > -DocObj.getCurrentpage().Vector3DPageObj.globalPlaneY.constant);
                                    bPlaneZ = (markupintersects[i].point.z < DocObj.getCurrentpage().Vector3DPageObj.globalPlaneZ.constant);
                                }
                                
    

                                if (markupintersects[i].object.visible && bPlaneX && bPlaneY && bPlaneZ) {

                                    if(markupintersects[i].object.type == "LineSegments"){
                                        //userData.type == "dimension"

                                        continue;
                                        /*if (markupintersects[i].object.userData.type == "dimension"){
                                            if(markupintersects.length == 1){
                                                markupmeshid = markupintersects[i].object.uuid;
                                                if(!bmove){
                                                    DocObj.getCurrentpage().select3Dmarkup(markupmeshid, true);
                                                }
                                                
                                                break;
                                            }else if(markupintersects.length > 1){
                                                continue;
                                            }
                                        }*/
                                        
                                    }
    
                                    
                                    if(markupintersects[i].object.userData.type == "floorplan"){
                                        bselectmesh = true;
                                    }else if(markupintersects[i].object.type == "Points"){
                                        
                                        //get index here.

                                        markuptype = "dimension";
                                        bselectmesh = bmove;
                                        
                                        
                                        markupmeshid = markupintersects[i].object.uuid;

                                        //tool.selectType 

                                        if(!bmove){

                                            if(tool.selectType == 1){
                                                DocObj.getCurrentpage().select3Dmarkup(markupmeshid, true);
                                                pointindex = markupintersects[i].index;
                                            }
                                        }
                                        

                                    }else if(markupintersects[i].object.userData.type == "symbol"){

                                        markupmeshid = markupintersects[i].object.uuid;
                                        markuptype = "symbol";


                                        bselectmesh = bmove;
                                        if(!bmove){

                                            if(tool.selectType == 1){
                                                DocObj.getCurrentpage().select3Dmarkup(markupmeshid, true);
                                            }
                                            
                                        }
                                        

                                        break;

    
                                    }else if(markupintersects[i].object.userData.type == "camera"){
                                        markupmeshid = markupintersects[i].object.uuid;
                                        markuptype = "camera";
    
                                        if(tool.selectType == 1){
                                            DocObj.getCurrentpage().select3Dmarkup(markupmeshid, true);
                                        }

                                        break;
                                    }

                                }else{
                                    bselectmesh = true;
                                }
                            }
                            /*if(DocObj.getCurrentpage().getSelectedmarkup){

                            }*/
                        }else{
                            bselectmesh = true;
                        }
                    }else{
                        bselectmesh = true;
                    }
                    

                    if (addedintersects.length > 0 && bselectmesh){

                        for (var i = 0; i < addedintersects.length; i++) {

                            if (addedintersects[i].length > 0 && bselectmesh){
                                for (var j = 0; j < addedintersects[i].length; j++) {
                                    
                                    if(addedintersects[i][j].object.type == "LineSegments"){
                                        continue;
                                    }
                                    if(addedintersects[i][j].object.userData.temptransparent){
                                        continue;
                                    }

                                    


                                    var addedintersection = null;
        
                                    var bPlaneX = true;
                                    var bPlaneY = true;
                                    var bPlaneZ = true;
        
                                    if(DocObj.getCurrentpage().Vector3DPageObj.clippingOn){
                                        bPlaneX = (addedintersects[i][j].point.x > -DocObj.getCurrentpage().Vector3DPageObj.globalPlaneX.constant);
                                        bPlaneY = (addedintersects[i][j].point.y > -DocObj.getCurrentpage().Vector3DPageObj.globalPlaneY.constant);
                                        bPlaneZ = (addedintersects[i][j].point.z < DocObj.getCurrentpage().Vector3DPageObj.globalPlaneZ.constant);
                                    }
        
                                    if (addedintersects[i][j].object.visible && bPlaneX && bPlaneY && bPlaneZ) {
                                        
                                        if(bmove){
                                            //var meshid = markupintersects[i].object.uuid;
                                            //console.log(markupmeshid);
        
                                            //move all selected markups 
        
        
                                            DocObj.getCurrentpage().moveSelected3DMarkup(addedintersects[i][j].point, pointindex);
        
                                            /*if(markupmeshid != null && markuptype == "symbol"){
                                                DocObj.getCurrentpage().move3DMarkup(markupmeshid, intersects[i].point);
                                            }else if(markupmeshid != null && markuptype == "dimension"){
                                                DocObj.getCurrentpage().move3DMarkupdimPoint(markupmeshid,intersects[i].point, pointindex);
                                            }*/
        
        
                                            break;
                                        }else{
        
                                            if(tool.selectType == 0){
                                                addedintersection = addedintersects[i][j];
                                                break;
                                            }
            
                                        }                        
                                    }
                                }
                            }
                        }
                    }


                    //type: "LineSegments"
                    if (intersects.length > 0 && bselectmesh) {
                        for (var i = 0; i < intersects.length; i++) {
                            
                            if(intersects[i].object.type == "LineSegments"){
                                continue;
                            }
                            if(intersects[i].object.userData.temptransparent){
                                continue;
                            }


                            var bPlaneX = true;
                            var bPlaneY = true;
                            var bPlaneZ = true;

                            if(DocObj.getCurrentpage().Vector3DPageObj.clippingOn){
                                bPlaneX = (intersects[i].point.x > -DocObj.getCurrentpage().Vector3DPageObj.globalPlaneX.constant);
                                bPlaneY = (intersects[i].point.y > -DocObj.getCurrentpage().Vector3DPageObj.globalPlaneY.constant);
                                bPlaneZ = (intersects[i].point.z < DocObj.getCurrentpage().Vector3DPageObj.globalPlaneZ.constant);
                            }

                            if (intersects[i].object.visible && bPlaneX && bPlaneY && bPlaneZ) {
                                
                                if(bmove){
                                    //var meshid = markupintersects[i].object.uuid;
                                    //console.log(markupmeshid);

                                    //move all selected markups 


                                    DocObj.getCurrentpage().moveSelected3DMarkup(intersects[i].point, pointindex);

                                    /*if(markupmeshid != null && markuptype == "symbol"){
                                        DocObj.getCurrentpage().move3DMarkup(markupmeshid, intersects[i].point);
                                    }else if(markupmeshid != null && markuptype == "dimension"){
                                        DocObj.getCurrentpage().move3DMarkupdimPoint(markupmeshid,intersects[i].point, pointindex);
                                    }*/


                                    break;
                                }else{

                                    if(tool.selectType == 0){
                                        var buseadded = false;
                                        if(addedintersection != null){

                                            if(addedintersection.distance < intersects[i].distance){
                                                buseadded = true;
                                            }else{
                                                buseadded = false;
                                            }
                                            //console.log(addedintersection);
                                        } 

                                        /*if(buseadded){
                                            var meshid = addedintersection.object.name;
                                        }else{
                                            var meshid = intersects[i].object.name;
                                        }*/


                                        if(buseadded){
                                            var meshid = addedintersection.object.name;
                                            var meshtype = addedintersection.object.type;
                                        }else{
                                            meshid = intersects[i].object.name;
                                            meshtype = intersects[i].object.type;
                                        }

                                        meshid = DocObj.getCurrentpage().getMeshName(meshid, meshtype);


                                        DocObj.getCurrentpage().select3DBlock(meshid, true, buseadded);
                                        pointindex = null;

                                        
                                        break;
                                    }
    
                                }                        
                            }
                        }
                    }else{
                        if(addedintersection != null){
                            meshid = addedintersection.object.name;
                            meshtype = addedintersection.object.type;

                            meshid = DocObj.getCurrentpage().getMeshName(meshid, meshtype);

                            DocObj.getCurrentpage().select3DBlock(meshid, true, buseadded);
                            pointindex = null;

                        }
                    }

                    //intersects[0].object.material.color = new THREE.Color( 0xf781f3 );

                    /*for ( var intersect in intersects ) {

                     var meshid = intersects[intersect].object.name;
                     intersects[intersect].object.material.color = new THREE.Color( 0xf781f3 );

                     }*/


                    /*var info = Viewer3D.pick(ev.clientX, ev.clientY);
                     if (info.mesh != null){
                     var meshid = info.mesh.name;
                     DocObj.getCurrentpage().select3DBlock(meshid);
                     }*/

                   
                };                         

                this.resetCam = function(campos){

                    if(campos != null){

                        this.object.matrix.fromArray(campos);
                        this.object.matrix.decompose(campos, this.object.quaternion, this.object.scale);

                    }
                };


                this.restorelast = function(){
                    this.object.position.copy(lastPosition);//.copy(this.object.position);
                    this.object.quaternion.copy(lastQuaternion);//.copy(this.object.quaternion);

                };

                this.setDimensions = function(maxdist, center, target, pos){
                    tool.maxDistance = maxdist;
                    tool.target.copy(center);

                    if (pos != null){
                        tool.object.position.copy(pos);
                    }

                    tool.update();
                    //controls.target.copy(boxCenter);
                    //controls.update();
                };

                this.setinit = function(){
                    tool.target.set(0, 5, 0);
                    tool.update();
                };

                this.rotateLeft = function (angle) {

                    if (angle === undefined) {

                        angle = getAutoRotationAngle();

                    }

                    thetaDelta -= angle;

                };

                this.rotateUp = function (angle) {

                    if (angle === undefined) {

                        angle = getAutoRotationAngle();

                    }

                    phiDelta -= angle;

                };

                // pass in distance in world space to move left
                this.panLeft = function (distance) {

                    var te = this.object.matrix.elements;

                    // get X column of matrix
                    panOffset.set(te[0], te[1], te[2]);
                    panOffset.multiplyScalar(-distance);

                    pan.add(panOffset);

                };

                // pass in distance in world space to move up
                this.panUp = function (distance) {

                    var te = this.object.matrix.elements;

                    // get Y column of matrix
                    panOffset.set(te[4], te[5], te[6]);
                    panOffset.multiplyScalar(distance);

                    pan.add(panOffset);

                };

                // pass in x,y of change desired in pixel space,
                // right and down are positive
                this.pan = function (deltaX, deltaY) {

                    var element = scope.domElement === document ? scope.domElement.body : scope.domElement;

                    if (scope.object.fov !== undefined) {

                        // perspective
                        var position = scope.object.position;
                        var offset = position.clone().sub(scope.target);
                        var targetDistance = offset.length();

                        // half of the fov is center to top of screen
                        targetDistance *= Math.tan((scope.object.fov / 2) * Math.PI / 180.0);

                        // we actually don't use screenWidth, since perspective camera is fixed to screen height
                        scope.panLeft(2 * deltaX * targetDistance / element.clientHeight);
                        scope.panUp(2 * deltaY * targetDistance / element.clientHeight);

                    } else if (scope.object.top !== undefined) {

                        // orthographic
                        scope.panLeft(deltaX * (scope.object.right - scope.object.left) / element.clientWidth);
                        scope.panUp(deltaY * (scope.object.top - scope.object.bottom) / element.clientHeight);

                    } else {

                        // camera neither orthographic or perspective
                        console.warn('WARNING: OrbitControls.js encountered an unknown camera type - pan disabled.');

                    }

                };

                this.dollyIn = function (dollyScale) {

                    if (dollyScale === undefined) {

                        dollyScale = getZoomScale();

                    }

                    scale /= dollyScale;

                };

                this.dollyOut = function (dollyScale) {

                    if (dollyScale === undefined) {

                        dollyScale = getZoomScale();

                    }

                    scale *= dollyScale;

                };

                this.update = function () {


                    var position = this.object.position;

                    offset.copy(position).sub(this.target);
                    //offset.set(0,0,0);
                    //console.log( 'target:', [ this.target.x, this.target.y, this.target.z ] );
                    //console.log( 'offset:', [ offset.x, offset.y, offset.z ] );

                    // rotate offset to "y-axis-is-up" space
                    offset.applyQuaternion(quat);

                    // angle from z-axis around y-axis

                    var theta = Math.atan2(offset.x, offset.z);


                    // angle from y-axis

                    var phi = Math.atan2(Math.sqrt(offset.x * offset.x + offset.z * offset.z), offset.y);


                    if (this.autoRotate) {

                        this.rotateLeft(getAutoRotationAngle());

                    }

                    theta += thetaDelta;
                    phi += phiDelta;

                    // restrict phi to be between desired limits
                    phi = Math.max(this.minPolarAngle, Math.min(this.maxPolarAngle, phi));

                    // restrict phi to be betwee EPS and PI-EPS
                    phi = Math.max(EPS, Math.min(Math.PI - EPS, phi));

                    var radius = offset.length() * scale;

                    // restrict radius to be between desired limits
                    radius = Math.max(this.minDistance, Math.min(this.maxDistance, radius));
                    //console.log( 'radius:', radius );


                    // move target to panned location
                    this.target.add(pan);

                    offset.x = radius * Math.sin(phi) * Math.sin(theta);
                    offset.y = radius * Math.cos(phi);
                    offset.z = radius * Math.sin(phi) * Math.cos(theta);


                    // rotate offset back to "camera-up-vector-is-up" space
                    offset.applyQuaternion(quatInverse);

                    position.copy(this.target).add(offset);

                    //console.log( 'camera:', [ position.x, position.y, position.z ] );

                    this.object.lookAt(this.target);

                    //this.target0.copy(this.target);
                    //this.position0.copy(position);


                    //console.log( 'phi:', phi );
                    //console.log( 'theta:', theta );

                    //console.log( 'camera:', [ this.object.rotation.x, this.object.rotation.y, this.object.rotation.z ] );

                    thetaDelta = 0;
                    phiDelta = 0;
                    scale = 1;
                    pan.set(0, 0, 0);

                    // update condition is:
                    // min(camera displacement, camera rotation in radians)^2 > EPS
                    // using small-angle approximation cos(x/2) = 1 - x^2 / 8

                    if (lastPosition.distanceToSquared(this.object.position) > EPS || 8 * (1 - lastQuaternion.dot(this.object.quaternion)) > EPS) {

                        //scope.dispatchEvent( changeEvent );

                        lastPosition.copy(this.object.position);
                        lastQuaternion.copy(this.object.quaternion);

                    }


                    //DocObj.getCurrentpage().Vector3DPageObj.storeCamera(tool.target, tool.object.position);

                    //tool.target0 = tool.target.clone();
                    //tool.position0 = tool.object.position.clone();

                    //this.target0 = this.target.clone();
                    //this.position0 = this.object.position.clone();

    

                };

                this.reset = function () {

                    state = STATE.NONE;

                    //this.target0 = this.target.clone();
                    //this.position0 = this.object.position.clone();

                    if(tool.target0 != undefined){
                        tool.target.copy(tool.target0);
                    }
                    if (tool.position0 != undefined){
                        tool.object.position.copy(tool.position0);
                    }
                    
                    this.enabled = true;

                    this.update();

                };

                function getAutoRotationAngle() {

                    return 2 * Math.PI / 60 / 60 * scope.autoRotateSpeed;

                }

                function getZoomScale() {

                    return Math.pow(0.95, scope.zoomSpeed);

                }


                this.mousedown = function (ev) {
                    if (scope.enabled === false) return;

                    ev.preventDefault();
                    ev.stopPropagation();

                    

                    tool.started = true;
                    var mousePos = getMousePos(scope.domElement, ev);

                    if (documentopen) {
                        if (DocObj.getCurrentpage().usevector3Dxml) {

                            if (ev.button === 0) {
                                if (scope.noRotate === true) return;

                                state = STATE.ROTATE;

                                rotateStart.set(mousePos.x, mousePos.y);

                            } else if (event.button === 1) {
                                if (scope.noZoom === true) return;

                                state = STATE.DOLLY;

                                dollyStart.set(mousePos.x, mousePos.y);

                            } else if (event.button === 2) {
                                if (scope.noPan === true) return;

                                state = STATE.PAN;

                                panStart.set(mousePos.x, mousePos.y);

                            }

                            if(tool.enableSelect){

                                tool.selectstarted = true;

                                var mousePos = getMousePos(renderer.domElement, ev);
                                DocObj.getCurrentpage().camera.updateProjectionMatrix();
                                mouse.x = (mousePos.x / canvasowidth) * 2 - 1;
                                mouse.y = -(mousePos.y / canvasoheight) * 2 + 1;
                                tool.getInterSects(mouse, false);
                            }
                            //scope.dispatchEvent( startEvent );
                        }


                    }


                };

                this.mousemove = function (ev) {
                    if (scope.enabled === false) return;
                    ev.preventDefault();
                    var mousePos = getMousePos(scope.domElement, ev);

                    tool.curmousepos = mousePos;
                    var element = scope.domElement === document ? scope.domElement.body : scope.domElement;

                    if (documentopen) {
                        if (DocObj.getCurrentpage().usevector3Dxml) {
                            if (state === STATE.ROTATE) {

                                if (scope.noRotate === true) return;

                                rotateEnd.set(mousePos.x, mousePos.y);
                                rotateDelta.subVectors(rotateEnd, rotateStart);

                                // rotating across whole screen goes 360 degrees around
                                scope.rotateLeft(2 * Math.PI * rotateDelta.x / element.clientWidth * scope.rotateSpeed);

                                // rotating up and down along whole screen attempts to go 360, but limited to 180
                                scope.rotateUp(2 * Math.PI * rotateDelta.y / element.clientHeight * scope.rotateSpeed);

                                rotateStart.copy(rotateEnd);

                            } else if (state === STATE.DOLLY) {

                                if (scope.noZoom === true) return;

                                dollyEnd.set(mousePos.x, mousePos.y);
                                dollyDelta.subVectors(dollyEnd, dollyStart);

                                if (dollyDelta.y > 0) {

                                    scope.dollyIn();

                                } else {

                                    scope.dollyOut();

                                }

                                dollyStart.copy(dollyEnd);

                            } else if (state === STATE.PAN) {

                                if (scope.noPan === true) return;

                                panEnd.set(mousePos.x, mousePos.y);
                                panDelta.subVectors(panEnd, panStart);

                                scope.pan(panDelta.x, panDelta.y);

                                panStart.copy(panEnd);

                            }

                            scope.update();

                            if(tool.enableSelect){
                                //var mousePos = getMousePos(renderer.domElement, ev);
                                mouse.x = (x / canvasowidth) * 2 - 1;
                                mouse.y = -(y / canvasoheight) * 2 + 1;

                                if (tool.selectstarted) {
                                    tool.getInterSects(mouse, true);
                                }
    
                            }


                        }
                    }


                };

                this.mouseup = function (ev) {

                    if (scope.enabled === false) return;
                    ev.preventDefault();
                    ev.stopPropagation();
                    
                    if (tool.started) {
                        tool.started = false;
                        state = STATE.NONE;

                    }
                    tool.selectstarted = false;
                    //scope.dispatchEvent( endEvent );
                };

                this.mousewheel = function(ev){
                    scope.onMouseWheel(ev);
                };

                this.wheel = function (ev) {
                    scope.onMouseWheel(ev)
                };


                this.DOMMouseScroll = function (ev) {
                    //scope.onMouseWheel(ev)
                };

                this.panscroll = function(){

                    var centerx = canvasowidthsize * 0.5; 
                    var centery = canvasoheightsize * 0.5;
                    
                    var deltax = centerx - tool.curmousepos.xus;
                    var deltay = centery - tool.curmousepos.yus;
                    
                    panStart.set(deltax * scale, deltay * scale);
                    panEnd.set(deltax, deltay);


                    panDelta.subVectors(panEnd, panStart);

                    scope.pan(panDelta.x, panDelta.y);

                    panStart.copy(panEnd);

                    //var mouseposdiffx = (((canvasowidth / 2) - rotatedpoint.x) * factor) - ((canvasowidth / 2) - rotatedpoint.x);
                    //var mouseposdiffy = (((canvasoheight / 2) - rotatedpoint.y) * factor) - ((canvasoheight / 2) - rotatedpoint.y);


                };

                this.onMouseWheel = function (ev) {
                    if (scope.enabled === false || scope.noZoom === true) return;
                    ev.preventDefault();
                    ev.stopPropagation();


                    if (documentopen) {
                        if (DocObj.getCurrentpage().usevector3Dxml) {
                            var delta = 0;
                            if (ev.wheelDelta !== undefined) { // WebKit / Opera / Explorer 9

                                delta = ev.wheelDelta;

                            } else if (ev.deltaY !== undefined) { // Firefox

                                delta = -ev.deltaY;

                            }

                            if (delta > 0) {

                                scope.dollyOut();
                                scope.panscroll();

                            } else {

                                scope.dollyIn();
                                scope.panscroll();

                            }

                            scope.update();


                        }
                    }
                };

                this.keydown = function (ev) {
                    if (scope.enabled === false || scope.noKeys === true || scope.noPan === true) return;

                    if (documentopen) {
                        if (DocObj.getCurrentpage().usevector3Dxml) {
                            switch (ev.keyCode) {

                                case scope.keys.UP:
                                    scope.pan(0, scope.keyPanSpeed);
                                    scope.update();
                                    break;

                                case scope.keys.BOTTOM:
                                    scope.pan(0, -scope.keyPanSpeed);
                                    scope.update();
                                    break;

                                case scope.keys.LEFT:
                                    scope.pan(scope.keyPanSpeed, 0);
                                    scope.update();
                                    break;

                                case scope.keys.RIGHT:
                                    scope.pan(-scope.keyPanSpeed, 0);
                                    scope.update();
                                    break;

                            }

                        }

                    }

                };


                this.pointerdown = function (ev) {
                    tool.MSPointerDown(ev);
                };


                this.MSPointerDown = function (ev) {
                    ev.preventDefault();
                    ev.stopPropagation();

                    
                    var mousePos = getMousePos(scope.domElement, ev);


                    numtouchpoints++;
                    touchpointarr[numtouchpoints] = new TouchPoint(ev.pointerId, mousePos.x, mousePos.y);


                    tool.started = true;
                    if (documentopen) {
                        if (DocObj.getCurrentpage().usevector3Dxml) {
                            switch (numtouchpoints) {
                                case 0: //one point only pan and markup labels
                                    if (ev.button === 0) {
                                        if (scope.noRotate === true) return;

                                        state = STATE.ROTATE;

                                        rotateStart.set(mousePos.x, mousePos.y);

                                    } else if (event.button === 1) {
                                        if (scope.noZoom === true) return;

                                        state = STATE.DOLLY;

                                        dollyStart.set(mousePos.x, mousePos.y);

                                    } else if (event.button === 2) {
                                        if (scope.noPan === true) return;

                                        state = STATE.PAN;

                                        panStart.set(mousePos.x, mousePos.y);

                                    }
                                    break;
                                case 1:
                                    if (scope.noZoom === true) return;
                                    state = STATE.TOUCH_DOLLY;

                                    var dx = Math.max(touchpointarr[0].x, touchpointarr[1].x) - Math.min(touchpointarr[0].x, touchpointarr[1].x);
                                    var dy = Math.max(touchpointarr[0].y, touchpointarr[1].y) - Math.min(touchpointarr[0].y, touchpointarr[1].y);

                                    var distance = Math.sqrt(dx * dx + dy * dy);
                                    dollyStart.set(0, distance);


                                    break;
                            }


                        }


                    }


                };

                this.pointermove = function (ev) {
                    tool.MSPointerMove(ev);
                };


                this.MSPointerMove = function (ev) {
                    if (!documentopen) {
                        return;
                    }


                    if (scope.enabled === false) return;
                    ev.preventDefault();
                    var mousePos = getMousePos(scope.domElement, ev);
                    var i = -1;
                    while (i < numtouchpoints) {
                        i++;
                        if (touchpointarr[i].pointID == ev.pointerId) {
                            touchpointarr[i].x = mousePos.x;
                            touchpointarr[i].y = mousePos.y;
                        }

                    }

                    var element = scope.domElement === document ? scope.domElement.body : scope.domElement;

                    if (documentopen) {
                        if (DocObj.getCurrentpage().usevector3Dxml) {
                            switch (numtouchpoints) {
                                case 0:
                                    if (state === STATE.ROTATE) {

                                        if (scope.noRotate === true) return;

                                        rotateEnd.set(mousePos.x, mousePos.y);
                                        rotateDelta.subVectors(rotateEnd, rotateStart);

                                        // rotating across whole screen goes 360 degrees around
                                        scope.rotateLeft(2 * Math.PI * rotateDelta.x / element.clientWidth * scope.rotateSpeed);

                                        // rotating up and down along whole screen attempts to go 360, but limited to 180
                                        scope.rotateUp(2 * Math.PI * rotateDelta.y / element.clientHeight * scope.rotateSpeed);

                                        rotateStart.copy(rotateEnd);

                                    } else if (state === STATE.DOLLY) {

                                        if (scope.noZoom === true) return;

                                        dollyEnd.set(mousePos.x, mousePos.y);
                                        dollyDelta.subVectors(dollyEnd, dollyStart);

                                        if (dollyDelta.y > 0) {

                                            scope.dollyIn();

                                        } else {

                                            scope.dollyOut();

                                        }

                                        dollyStart.copy(dollyEnd);

                                    } else if (state === STATE.PAN) {

                                        if (scope.noPan === true) return;

                                        panEnd.set(mousePos.x, mousePos.y);
                                        panDelta.subVectors(panEnd, panStart);

                                        scope.pan(panDelta.x, panDelta.y);

                                        panStart.copy(panEnd);

                                    }

                                    scope.update();

                                    break;
                                case 1:
                                    if (scope.noZoom === true) return;
                                    if (state !== STATE.TOUCH_DOLLY) return;

                                    var dx = Math.max(touchpointarr[0].x, touchpointarr[1].x) - Math.min(touchpointarr[0].x, touchpointarr[1].x);
                                    var dy = Math.max(touchpointarr[0].y, touchpointarr[1].y) - Math.min(touchpointarr[0].y, touchpointarr[1].y);


                                    var distance = Math.sqrt(dx * dx + dy * dy);

                                    dollyEnd.set(0, distance);
                                    dollyDelta.subVectors(dollyEnd, dollyStart);

                                    if (dollyDelta.y > 0) {

                                        scope.dollyOut();

                                    } else if (dollyDelta.y < 0) {

                                        scope.dollyIn();

                                    }

                                    dollyStart.copy(dollyEnd);

                                    scope.update();

                                    break;
                                case 2:
                                    if (scope.noPan === true) return;
                                    if (state !== STATE.TOUCH_PAN) return;

                                    panEnd.set(touchpointarr[0].x, touchpointarr[0].y);
                                    panDelta.subVectors(panEnd, panStart);

                                    scope.pan(panDelta.x, panDelta.y);

                                    panStart.copy(panEnd);

                                    scope.update();

                                    break;
                                default:

                                    state = STATE.NONE;

                            }
                        }
                    }


                };

                this.pointerup = function (ev) {
                    tool.MSPointerUp(ev);

                };


                this.MSPointerUp = function (ev) {
                    numtouchpoints--;

                    ev.preventDefault();
                    
                    if (scope.enabled === false) return;
                    state = STATE.NONE;

                    if (tool.started) {
                        tool.started = false;
                    }


                };

                this.touchstart = function (ev) {
                    if (scope.enabled === false) return;
                    ev.preventDefault();
                    tool.started = true;
                    //getTouchPos(canvas,ev,0);
                    if (documentopen) {
                        if (DocObj.getCurrentpage().usevector3Dxml) {
                            var touchPos = getTouchPos(renderer.domElement, ev, 0);


                            /*var touchPos = {
                             x: ev.targetTouches[0].pageX,
                             y: ev.targetTouches[0].pageY
                             };*/


                            switch (ev.touches.length) {
                                case 1: // one-fingered touch: rotate

                                    if (scope.noRotate === true) return;

                                    state = STATE.TOUCH_ROTATE;

                                    rotateStart.set(touchPos.x, touchPos.y);
                                    break;
                                case 2: // two-fingered touch: dolly

                                    if (scope.noZoom === true) return;

                                    var touchPos2 = getTouchPos(renderer.domElement, ev, 1);
                                    /*var touchPos2 = {
                                     x: ev.targetTouches[1].pageX,
                                     y: ev.targetTouches[1].pageY
                                     };*/

                                    state = STATE.TOUCH_DOLLY;

                                    //var dx = touchPos.x - touchPos2.x;
                                    //var dy = touchPos.y - touchPos2.y;

                                    var dx = Math.max(touchPos.x, touchPos2.x) - Math.min(touchPos.x, touchPos2.x);
                                    var dy = Math.max(touchPos.y, touchPos2.y) - Math.min(touchPos.y, touchPos2.y);


                                    var distance = Math.sqrt(dx * dx + dy * dy);
                                    dollyStart.set(0, distance);
                                    break;

                                case 3: // three-fingered touch: pan

                                    if (scope.noPan === true) return;

                                    state = STATE.TOUCH_PAN;

                                    panStart.set(touchPos.x, touchPos.y);
                                    break;

                                default:

                                    state = STATE.NONE;

                            }

                        }


                    }

                };

                this.touchmove = function (ev) {
                    if (scope.enabled === false) return;

                    ev.preventDefault();
                    ev.stopPropagation();
                    var element = scope.domElement === document ? scope.domElement.body : scope.domElement;

                    if (documentopen) {
                        if (DocObj.getCurrentpage().usevector3Dxml) {

                            var touchPos = getTouchPos(renderer.domElement, ev, 0);
                            /*var touchPos = {
                             x: ev.targetTouches[0].pageX,
                             y: ev.targetTouches[0].pageY
                             };*/


                            switch (event.touches.length) {

                                case 1: // one-fingered touch: rotate

                                    if (scope.noRotate === true) return;
                                    if (state !== STATE.TOUCH_ROTATE) return;

                                    rotateEnd.set(touchPos.x, touchPos.y);
                                    rotateDelta.subVectors(rotateEnd, rotateStart);

                                    // rotating across whole screen goes 360 degrees around
                                    scope.rotateLeft(2 * Math.PI * rotateDelta.x / element.clientWidth * scope.rotateSpeed);
                                    // rotating up and down along whole screen attempts to go 360, but limited to 180
                                    scope.rotateUp(2 * Math.PI * rotateDelta.y / element.clientHeight * scope.rotateSpeed);

                                    rotateStart.copy(rotateEnd);

                                    scope.update();
                                    break;

                                case 2: // two-fingered touch: dolly

                                    var touchPos2 = getTouchPos(renderer.domElement, ev, 1);

                                    /*var touchPos2 = {
                                     x: ev.targetTouches[1].pageX,
                                     y: ev.targetTouches[1].pageY
                                     };*/

                                    if (scope.noZoom === true) return;
                                    if (state !== STATE.TOUCH_DOLLY) return;

                                    //var dx = touchPos.x - touchPos2.x;
                                    //var dy = touchPos.y - touchPos2.y;
                                    var dx = Math.max(touchPos.x, touchPos2.x) - Math.min(touchPos.x, touchPos2.x);
                                    var dy = Math.max(touchPos.y, touchPos2.y) - Math.min(touchPos.y, touchPos2.y);


                                    var distance = Math.sqrt(dx * dx + dy * dy);

                                    dollyEnd.set(0, distance);
                                    dollyDelta.subVectors(dollyEnd, dollyStart);

                                    if (dollyDelta.y > 0) {

                                        scope.dollyOut();

                                    } else if (dollyDelta.y < 0) {

                                        scope.dollyIn();

                                    }

                                    dollyStart.copy(dollyEnd);

                                    scope.update();
                                    break;

                                case 3: // three-fingered touch: pan

                                    if (scope.noPan === true) return;
                                    if (state !== STATE.TOUCH_PAN) return;

                                    panEnd.set(touchPos.x, touchPos.y);
                                    panDelta.subVectors(panEnd, panStart);

                                    scope.pan(panDelta.x, panDelta.y);

                                    panStart.copy(panEnd);

                                    scope.update();
                                    break;

                                default:

                                    state = STATE.NONE;

                            }


                        }
                    }
                };

                this.touchcancel = function(ev){
                    ev.preventDefault();

                    if (scope.enabled === false) return;
                    //scope.dispatchEvent( endEvent );
                    state = STATE.NONE;
                    if (tool.started) {
                        tool.started = false;
                    }

                };                
                this.touchend = function (ev) {
                    ev.preventDefault();

                    if (scope.enabled === false) return;
                    //scope.dispatchEvent( endEvent );
                    state = STATE.NONE;
                    if (tool.started) {
                        tool.started = false;
                    }
                };


            };            
            

            tools.magnify = function () {
                var tool = this;
                var x = 0,
                    y = 0,
                    w = 0,
                    h = 0;
                var prevx = 0,
                    prevy = 0;
                var xcur1 = 0,
                    xcur2 = 0,
                    ycur1 = 0,
                    ycur2 = 0;
                var startx = 0,
                    starty = 0,
                    startx2 = 0,
                    starty2 = 0,
                    startw = 0,
                    starth = 0;
                var startcenterx = 0,
                    startcentery = 0;
                var centerx = 0,
                    centery = 0;
                var prevcenterx = 0,
                    prevcentery = 0;
                var prevh = 0,
                    prevw = 0;
                var startdiagonal = 0;
                var prevdiagonal = 0;
                var initdiagonal = 0;
                this.name = 'magnify';
                this.anglelengthsupport = false;
                this.refreshSupport = false;

                this.started = false;

                var magnificationScale = 1.5;
                var magnifyingGlassRadius = 100;


                

                setCursor(getMarkupCursor('magnify'));
                /*var ImageGlassRadius = 100 / DocObj.getCurrentpage().dscaleextent;

                if (DocObj.getCurrentpage().usevectorxml) {
                    ImageGlassRadius = 100 / DocObj.getCurrentpage().dscalevector;
                }*/
                /*if (DocObj.getCurrentpage().usedincomposite && DocObj.getCurrentpage().compositereference != undefined) {
                    //DocObj.getCurrentpage().compositereference.drawmagnify(mousepos,ctx,magnificationScale);
                    DocObj.getCurrentpage().compositereference.checkPDFmagnify(magnificationScale);
                }else{

                }*/
                if (DocObj.getCurrentpage().usepdfjs){
                    DocObj.getCurrentpage().renderPDFMagnify(magnificationScale);
                }



                var imageData = null;
                var mrkimageData = null;
                var scrimageData = null;
                var magimage = 1;

                this.drawMagnify = function(mousepos){
                    /*if (DocObj.getCurrentpage().usedincomposite && DocObj.getCurrentpage().compositereference != undefined) {
                        DocObj.getCurrentpage().compositereference.drawmagnify(mousepos,magctx,magnificationScale);
                    }else{
                        
                    }*/
                    DocObj.getCurrentpage().drawmagnify(mousepos,magctx,magnificationScale);

                };


                this.wheel = function (ev) {
                    if (!tool.started) {
                        return;
                    }
                    if (documentopen) {
                        var delta = 0;
                        if (ev.wheelDelta !== undefined) { // WebKit / Opera / Explorer 9

                            delta = ev.wheelDelta;

                        } else if (ev.deltaY !== undefined) { // Firefox
                            if(bIsMSIE ){
                                delta = -ev.deltaY;
                            }else{
                                delta = -ev.deltaY*50;
                            }
                        }
                        var movey = delta;
                        if (movey < 0) {
                            if (magnificationScale > 1.5) {
                                magnificationScale -= 0.2;
                            }

                        } else {
                            if (magnificationScale < 10) {
                                magnificationScale += 0.2;
                            }
                        }
                        if (magnificationScale < 2.5) {
                            magimage = 1;
                        } else {
                            magimage = 0;
                        }

                        var mousePos = getMousePos(canvas, ev);
                        tool.x0 = mousePos.x;
                        tool.y0 = mousePos.y;
                        /*if (DocObj.getCurrentpage().usedincomposite && DocObj.getCurrentpage().compositereference != undefined) {
                            //DocObj.getCurrentpage().compositereference.drawmagnify(mousepos,ctx,magnificationScale);
                            DocObj.getCurrentpage().compositereference.checkPDFmagnify(magnificationScale);
                        }else{

                        }*/

                        if (DocObj.getCurrentpage().usepdfjs){
                            DocObj.getCurrentpage().renderPDFMagnify(magnificationScale);
                        }

                        tool.drawMagnify({x:tool.x0,y:tool.y0},magctx,magnificationScale);

                    }
                };

                this.pointermove = function (ev) {
                    tool.MSPointerMove(ev);
                };

                this.pointerdown = function (ev) {
                    tool.MSPointerDown(ev);
                };

                this.pointerup = function (ev) {
                    tool.MSPointerUp(ev);
                };

                this.MSPointerDown = function (ev) {
                    ev.preventDefault();
                    ev.stopPropagation();

                    
                    var mousePos = getMousePos(canvas, ev);
                    tool.started = true;
                    tool.x0 = mousePos.x;
                    tool.y0 = mousePos.y;
                    prevx = tool.x0;
                    prevy = tool.y0;
                    //magcanvas.style.top = mousePos.y - (magnifysize.height / 2) + "px";
                    //magcanvas.style.left = mousePos.x - (magnifysize.width / 2) + "px";

                    magcanvas.style.top = mousePos.yus - (magnifysize.height / 2) + "px";
                    magcanvas.style.left = mousePos.xus - (magnifysize.width / 2) + "px";


                    magcanvas.style.visibility = 'visible';
                };

                this.mousedown = function (ev) {
                    ev.preventDefault();
                    ev.stopPropagation();
                    
                    tool.started = true;
                    var mousePos = getMousePos(canvas, ev);
                    tool.x0 = mousePos.x;
                    tool.y0 = mousePos.y;
                    prevx = tool.x0;
                    prevy = tool.y0;

                    //magcanvas.style.top = mousePos.y - (magnifysize.height / 2) + "px";
                    //magcanvas.style.left = mousePos.x - (magnifysize.width / 2) + "px";

                    magcanvas.style.top = mousePos.yus - (magnifysize.height / 2) + "px";
                    magcanvas.style.left = mousePos.xus - (magnifysize.width / 2) + "px";


                    magcanvas.style.visibility = 'visible';
                };

                this.touchstart = function (ev) {
                    ev.preventDefault();
                    tool.started = true;
                    //var touchPos = getTouchPos(canvas,ev,0);
                    var touchPos0 = getTouchPos(canvas, ev, 0);
                    if(ev.targetTouches.length > 1){
                        var touchPos1 = getTouchPos(canvas, ev, 1);
                    }else{
                        touchPos1 = getTouchPos(canvas, ev, 0);
                    }

                    startx = touchPos0.x;
                    starty = touchPos0.y;
                    startx2 = touchPos1.x;
                    starty2 = touchPos1.y;

                    startw = Math.abs(startx2 - startx);
                    starth = Math.abs(starty2 - starty);

                    var startwsq = Math.pow(startw, 2);
                    var starthsq = Math.pow(starth, 2);

                    startdiagonal = Math.sqrt((startwsq + starthsq));
                    prevdiagonal = startdiagonal;
                    initdiagonal = startdiagonal;

                    startcenterx = startx + (startw / 2);
                    startcentery = starty + (starth / 2);
                    prevcenterx = startcenterx;
                    prevcentery = startcentery;

                    var canvcenterx = touchPos0.x + (magnifysize.width / 2);
                    var canvcentery = touchPos0.y - (magnifysize.height / 2);
                    //magcanvas.style.top = startcentery - (magcanvas.height / 2) + "px";
                    //magcanvas.style.left = startcenterx - (magcanvas.width / 2) + "px";
                    //magcanvas.style.top = touchPos0.y - magnifysize.height + "px";
                    //magcanvas.style.left = touchPos0.x + "px";

                    magcanvas.style.top = touchPos0.yus - magnifysize.height + "px";
                    magcanvas.style.left = touchPos0.xus + "px";

                    
                    magcanvas.style.visibility = 'visible';

                    tool.drawMagnify({x:canvcenterx,y: canvcentery});
                };

                this.MSPointerMove = function (ev) {
                    if (!tool.started) {
                        return;
                    }
                    var mousePos = getMousePos(canvas, ev);
                    var xdiff = prevx - mousePos.x;
                    var ydiff = prevy - mousePos.y;

                    //magcanvas.style.top = mousePos.y - (magnifysize.height / 2) + "px";
                    //magcanvas.style.left = mousePos.x - (magnifysize.width / 2) + "px";

                    magcanvas.style.top = mousePos.yus - (magnifysize.height / 2) + "px";
                    magcanvas.style.left = mousePos.xus - (magnifysize.width / 2) + "px";


                    tool.drawMagnify(mousePos);
                    prevx = mousePos.x;
                    prevy = mousePos.y;

                };

                this.mousemove = function (ev) {
                    if (!tool.started) {
                        return;
                    }
                    var mousePos = getMousePos(canvas, ev);
                    var xdiff = prevx - mousePos.x;
                    var ydiff = prevy - mousePos.y;

                    magcanvas.style.top = mousePos.yus - (magnifysize.height / 2) + "px";
                    magcanvas.style.left = mousePos.xus - (magnifysize.width / 2) + "px";

                    tool.drawMagnify(mousePos);

                    prevx = mousePos.x;
                    prevy = mousePos.y;

                };

                this.touchmove = function (ev) {
                    ev.preventDefault();
                    if (!tool.started) {
                        return;
                    }
                    //var touchPos = getTouchPos(canvas,ev,0);
                    var touchPos0 = getTouchPos(canvas, ev, 0);
                    if(ev.targetTouches.length > 1){
                        var touchPos1 = getTouchPos(canvas, ev, 1);
                    }else{
                        touchPos1 = getTouchPos(canvas, ev, 0);
                    }

                    xcur1 = touchPos0.x;
                    ycur1 = touchPos0.y;
                    xcur2 = touchPos1.x;
                    ycur2 = touchPos1.y;

                    x = Math.min(xcur1, xcur2);
                    y = Math.min(ycur1, ycur2);
                    w = Math.abs(xcur2 - xcur1);
                    h = Math.abs(ycur2 - ycur1);


                    centerx = x + (w / 2);
                    centery = y + (h / 2);

                    var canvcenterx = touchPos0.x + (magnifysize.width / 2);
                    var canvcentery = touchPos0.y - (magnifysize.height / 2);

                    //magcanvas.style.top = centery - (magcanvas.height / 2) + "px";
                    //magcanvas.style.left = centerx - (magcanvas.width / 2) + "px";
                    magcanvas.style.top = touchPos0.yus - magnifysize.height + "px";
                    magcanvas.style.left = touchPos0.xus + "px";

                    tool.drawMagnify({x:canvcenterx,y: canvcentery});

                    var startwsq = Math.pow(w, 2);
                    var starthsq = Math.pow(h, 2);

                    startdiagonal = Math.sqrt((startwsq + starthsq));

                    var diffwidth = prevdiagonal - startdiagonal;

                    if (initdiagonal >= startdiagonal){
                        magnificationScale = 1.5;
                    }else{
                        if (diffwidth > 0 ) {
                            if (magnificationScale > 1.5) {
                                magnificationScale -= 0.2;
                            }

                        } else {
                            if (magnificationScale < 10) {
                                magnificationScale += 0.2;
                            }
                        }

                    }

                    prevdiagonal = startdiagonal;

                    prevcenterx = centerx;
                    prevcentery = centery;
                    prevw = w;
                    prevh = h;
                    //prevx = touchPos.x;
                    //prevy = touchPos.y;

                };

                this.MSPointerUp = function (ev) {
                    
                    if (tool.started) {
                        tool.mousemove(ev);
                        tool.started = false;
                        //tool.eraseMagnifyingGlass();
                        magcanvas.style.visibility = 'hidden';
                        imageData = null;
                        scrimageData = null;
                        mrkimageData = null;

                        DocObj.getCurrentpage().magnifyoff();

                        drawmarkupAll(cntximg);
                    }

                };
                this.mouseup = function (ev) {
                    
                    if (tool.started) {
                        tool.mousemove(ev);
                        tool.started = false;
                        //tool.eraseMagnifyingGlass();
                        magcanvas.style.visibility = 'hidden';
                        imageData = null;
                        scrimageData = null;
                        mrkimageData = null;

                        DocObj.getCurrentpage().magnifyoff();

                        drawmarkupAll(cntximg);

                    }
                };
                this.touchcancel = function(ev){
                    ev.preventDefault();
                    /*if(ev.targetTouches.length > 1){
                     var touchPos1 = getTouchPos(canvas, ev, 1);
                     }else{
                     touchPos1 = getTouchPos(canvas, ev, 0);
                     }*/

                    if (tool.started && ev.targetTouches.length < 1) {
                        //tool.touchmove(ev);
                        tool.started = false;
                        //tool.eraseMagnifyingGlass();
                        magcanvas.style.visibility = 'hidden';
                        imageData = null;
                        scrimageData = null;
                        mrkimageData = null;

                        DocObj.getCurrentpage().magnifyoff();

                        drawmarkupAll(cntximg);

                    }
                };
                this.touchend = function (ev) {
                    ev.preventDefault();
                    /*if(ev.targetTouches.length > 1){
                     var touchPos1 = getTouchPos(canvas, ev, 1);
                     }else{
                     touchPos1 = getTouchPos(canvas, ev, 0);
                     }*/

                    if (tool.started && ev.targetTouches.length < 1) {
                        //tool.touchmove(ev);
                        tool.started = false;
                        //tool.eraseMagnifyingGlass();
                        magcanvas.style.visibility = 'hidden';
                        imageData = null;
                        scrimageData = null;
                        mrkimageData = null;

                        DocObj.getCurrentpage().magnifyoff();

                        drawmarkupAll(cntximg);

                    }
                };

            };



            //the note tool
            tools.note = function () {
                var tool = this;
                var prevx = 0,
                    prevy = 0;
                this.started = false;
                this.name = 'note';
                this.anglelengthsupport = false;
                this.refreshSupport = false;

                //ft 08.08.2018 changed from separate index to direct array length
                //var curmarkup = DocObj.nummarkups;
                var curmarkup = DocObj.markuplist.length;
                var notemarkupobj;

                

                setCursor(getMarkupCursor('note'));

                this.pointerdown = function (ev) {
                    tool.MSPointerDown(ev);
                };

                

                this.MSPointerDown = function (ev) {
                    tool.started = true;
                    if(DocObj){
                        DocObj.bMarkupchanged = true;
                    }


                    var mousePos = getMousePos(canvas, ev);
                    var bWithin = MousePosdrwext(mousePos);

                    if (!bWithin && bLimMarkupExtent) {
                        tool.started = false;
                        if(DocObj){
                            DocObj.bMarkupchanged = false;
                        }

                        return;
                    }

                    tool.x0 = mousePos.x;
                    tool.y0 = mousePos.y;

                    notemarkupobj = new MarkupObject(10, 0, 0);
                    notemarkupobj.pagenumber = DocObj.getcurPage();
                    notemarkupobj.viewname = DocObj.getcur3DView();

                    if(notemarkupobj.viewname != null){
                        notemarkupobj.bfor3D = true;
                        notemarkupobj.pageheight = canvasoheight;
                        notemarkupobj.pagewidth = canvasowidth;
                    }


                    notemarkupobj.scaling = getCurrentPageScale();
                    var offset = getCurrentPageOffset();
                    notemarkupobj.xoffset = offset.x;
                    notemarkupobj.yoffset = offset.y;

                    notemarkupobj.x = tool.x0;
                    notemarkupobj.y = tool.y0;
                    notemarkupobj.seteditaction(0);

                    notemarkupobj.drawme(context);
                    notemarkupobj.savemetolistDraw();
                    //TempID = notemarkupobj.markupnumber;
                    //ft 08.08.2018 changed from markupnumber to array length
                    //DocObj.selectedmarkup.id = notemarkupobj.markupnumber;
                    DocObj.selectedmarkup.id = DocObj.markuplist.length - 1;

                    if (!bMultimarkupadd) {
                        bMarkupcrated = true;
                        //ft 08.08.2018 changed from separate index to direct array length
                        //nMarkupcreated = DocObj.markuplist.length - 1;
                        //nMarkupcreated = notemarkupobj.markupnumber;
                    }
                    //nMarkupcreated = DocObj.markuplist.length;
                    nMarkupcreated = DocObj.markuplist.length - 1;

                    img_update();
                    tool.started = false;
                    notemarkupobj = null;
                    //setNContent(DocObj.markuplist[TempID].text);
                    //showNDialog(false);
                    if (RxCore_GUI_Notediag != undefined) {
                        RxCore_GUI_Notediag.setNotediag(DocObj.markuplist[nMarkupcreated].text, true);
                    }

                    if(bAlwaysMarkupCallback){
                        if (RxCore_GUI_Markup != undefined) {
                            var operation = {created : true, modified : false, deleted : false, multi : bMultimarkupadd };
                            
                            RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[nMarkupcreated], operation);

                            
                        }
            
                    }else{
                        if (!bMultimarkupadd) {
                            if (RxCore_GUI_Markup != undefined) {
                                //RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[index],true);
                                var operation = {created : true, modified : false, deleted : false};
                                RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[nMarkupcreated], operation);
                            }
    
                        }
    
                    }
                    

                };
                this.mousedown = function (ev) {

                    tool.started = true;
                    if(DocObj){
                        DocObj.bMarkupchanged = true;
                    }

                    var mousePos = getMousePos(canvas, ev);

                    var bWithin = MousePosdrwext(mousePos);
                    
                    if (!bWithin && bLimMarkupExtent) {
                        tool.started = false;
                        if(DocObj){
                            DocObj.bMarkupchanged = false;
                        }
                        return;
                    }

                    tool.x0 = mousePos.x;
                    tool.y0 = mousePos.y;

                    notemarkupobj = new MarkupObject(10, 0, 0);
                    notemarkupobj.pagenumber = DocObj.getcurPage();

                    notemarkupobj.viewname = DocObj.getcur3DView();

                    if(notemarkupobj.viewname != null){
                        notemarkupobj.bfor3D = true;
                        notemarkupobj.pageheight = canvasoheight;
                        notemarkupobj.pagewidth = canvasowidth;
                    }


                    notemarkupobj.scaling = getCurrentPageScale();
                    var offset = getCurrentPageOffset();
                    notemarkupobj.xoffset = offset.x;
                    notemarkupobj.yoffset = offset.y;


                    notemarkupobj.x = tool.x0;
                    notemarkupobj.y = tool.y0;
                    notemarkupobj.seteditaction(0);

                    notemarkupobj.drawme(context);
                    notemarkupobj.savemetolistDraw();

                    //ft 08.08.2018 changed from markupnumber to array length
                    //DocObj.selectedmarkup.id = notemarkupobj.markupnumber;
                    DocObj.selectedmarkup.id = DocObj.markuplist.length - 1;
                    //nMarkupcreated = notemarkupobj.markupnumber;
                    nMarkupcreated = DocObj.markuplist.length - 1;
                    if (!bMultimarkupadd) {
                        bMarkupcrated = true;
                        //ft 08.08.2018 changed from separate index to direct array length
                        //nMarkupcreated = DocObj.markuplist.length - 1;
                        //nMarkupcreated = notemarkupobj.markupnumber;
                    }
                    nMarkupcreated = DocObj.markuplist.length - 1;

                    img_update();
                    tool.started = false;
                    notemarkupobj = null;
                    if (RxCore_GUI_Notediag != undefined) {
                        RxCore_GUI_Notediag.setNotediag(DocObj.markuplist[nMarkupcreated].text, true);
                    }

                    if(bAlwaysMarkupCallback){
                        if (RxCore_GUI_Markup != undefined) {

                            var operation = {created : true, modified : false, deleted : false, multi : bMultimarkupadd};
                            RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[nMarkupcreated], operation);

                            
                        }
            
                    }else{

                        if (!bMultimarkupadd) {
                            if (RxCore_GUI_Markup != undefined) {
                                //RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[index],true);
                                var operation = {created : true, modified : false, deleted : false};
                                RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[nMarkupcreated], operation);
                            }
    
                        }
                    }

                    //setNContent(DocObj.markuplist[TempID].text);
                    //showNDialog(false);


                };

                this.touchstart = function (ev) {
                    tool.started = true;
                    if(DocObj){
                        DocObj.bMarkupchanged = true;
                    }

                    var touchPos = getTouchPos(canvas, ev, 0);

                    var bWithin = MousePosdrwext(touchPos);
                    if (!bWithin && bLimMarkupExtent) {
                        tool.started = false;
                        if(DocObj){
                            DocObj.bMarkupchanged = false;
                        }

                        return;
                    }

                    tool.x0 = touchPos.x;
                    tool.y0 = touchPos.y;
                    notemarkupobj = new MarkupObject(10, 0, 0);
                    notemarkupobj.pagenumber = DocObj.getcurPage();

                    notemarkupobj.viewname = DocObj.getcur3DView();

                    if(notemarkupobj.viewname != null){
                        notemarkupobj.bfor3D = true;
                        notemarkupobj.pageheight = canvasoheight;
                        notemarkupobj.pagewidth = canvasowidth;
                    }


                    notemarkupobj.scaling = getCurrentPageScale();
                    var offset = getCurrentPageOffset();
                    notemarkupobj.xoffset = offset.x;
                    notemarkupobj.yoffset = offset.y;

                    notemarkupobj.x = tool.x0;
                    notemarkupobj.y = tool.y0;
                    notemarkupobj.seteditaction(0);

                    notemarkupobj.drawme(context);
                    notemarkupobj.savemetolistDraw();

                    //ft 08.08.2018 changed from markupnumber to array length
                    //DocObj.selectedmarkup.id = notemarkupobj.markupnumber;
                    DocObj.selectedmarkup.id = DocObj.markuplist.length - 1;

                    
                    if (!bMultimarkupadd) {

                        bMarkupcrated = true;
                        //ft 08.08.2018 changed from separate index to direct array length
                        //nMarkupcreated = DocObj.markuplist.length - 1;
                        //nMarkupcreated = notemarkupobj.markupnumber;
                    }
                    nMarkupcreated = DocObj.markuplist.length - 1;

                    img_update();
                    tool.started = false;
                    notemarkupobj = null;
                    //setNContent(DocObj.markuplist[TempID].text);
                    //showNDialog(false);

                    if (RxCore_GUI_Notediag != undefined) {
                        RxCore_GUI_Notediag.setNotediag(DocObj.markuplist[nMarkupcreated].text, true);
                    }

                    if(bAlwaysMarkupCallback){
                     
                        if (RxCore_GUI_Markup != undefined) {
                            var operation = {created : true, modified : false, deleted : false, multi : bMultimarkupadd};
                            RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[nMarkupcreated], operation);
                        }
            
                    }else{
                        if (!bMultimarkupadd) {
                            if (RxCore_GUI_Markup != undefined) {
                                //RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[index],true);
                                var operation = {created : true, modified : false, deleted : false};
                                RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[nMarkupcreated], operation);
                            }
                        }
                    }


                };

                this.mouseup = function (ev) {

                };

                this.touchend = function (ev) {

                };

            };

            // the text tool
            tools.text = function () {
                var tool = this;
                var blinker;
                this.name = 'text';
                this.anglelengthsupport = false;
                this.refreshSupport = false;

                this.started = false;
                //ft 08.08.2018 changed from separate index to direct array length
                //var curmarkup = DocObj.nummarkups;
                var curmarkup = DocObj.markuplist.length;
                var textmarkupobj;

                this.pointerdown = function (ev) {
                    tool.MSPointerDown(ev);
                };

                this.MSPointerDown = function (ev) {
                    ev.preventDefault();
                    ev.stopPropagation();
                    

                    //Draw blinking "cursor" centered on coordinates.
                    tool.started = true;

                    if(DocObj){
                        DocObj.bMarkupchanged = true;
                    }

                    var mousePos = getMousePos(canvas, ev);
                    var bWithin = MousePosdrwext(mousePos);
                    if (!bWithin && bLimMarkupExtent) {
                        tool.started = false;
                        if(DocObj){
                            DocObj.bMarkupchanged = false;
                        }
                        
                        return;
                    }

                    tool.x0 = mousePos.x;
                    tool.y0 = mousePos.y;
                    textmarkupobj = new MarkupObject(9, 0, 0);
                    textmarkupobj.pagenumber = DocObj.getcurPage();

                    textmarkupobj.viewname = DocObj.getcur3DView();

                    if(textmarkupobj.viewname != null){
                        textmarkupobj.bfor3D = true;
                        textmarkupobj.pageheight = canvasoheight;
                        textmarkupobj.pagewidth = canvasowidth;
                    }

                    textmarkupobj.font.setFontname(defaultFont.fontName);

                    textmarkupobj.scaling = getCurrentPageScale();
                    var offset = getCurrentPageOffset();
                    textmarkupobj.xoffset = offset.x;
                    textmarkupobj.yoffset = offset.y;                    

                    //textmarkupobj.fontname = fontstylevalue;

                    textmarkupobj.x = tool.x0;
                    textmarkupobj.y = tool.y0;
                    textmarkupobj.seteditaction(0);
                    var markuptext = "";
                    textmarkupobj.text = markuptext;

                    textmarkupobj.savemetolistDraw();
                    DocObj.selectedmarkup.id = textmarkupobj.markupnumber;
                    //img_update();
                    tool.started = false;
                    textmarkupobj = null;
                    if (RxCore_GUI_Textdiag != undefined) {
                        RxCore_GUI_Textdiag.setTextdiag("");
                    }

                    //setTextContent(DocObj.markuplist[TempID].text);
                    //showTextDialog(false);


                    /*markuptext = prompt("Enter markup text","");
                     if (markuptext != null && markuptext != ""){
                     textmarkupobj.text = markuptext;
                     textmarkupobj.drawme(context);
                     textmarkupobj.savemetolist();
                     img_update();
                     tool.started = false;
                     textmarkupobj = null;
                     }*/

                };
                this.mousedown = function (ev) {
                    ev.preventDefault();
                    ev.stopPropagation();
                    

                    /*context.beginPath();
                     context.moveTo(ev._x, ev._y);
                     context.strokeStyle = "red";*/
                    //Draw blinking "cursor" centered on coordinates.
                    tool.started = true;
                    if(DocObj){
                        DocObj.bMarkupchanged = true;
                    }

                    var mousePos = getMousePos(canvas, ev);
                    var bWithin = MousePosdrwext(mousePos);
                    if (!bWithin && bLimMarkupExtent) {
                        tool.started = false;
                        if(DocObj){
                            DocObj.bMarkupchanged = false;
                        }

                        
                        return;
                    }

                    tool.x0 = mousePos.x;
                    tool.y0 = mousePos.y;
                    textmarkupobj = new MarkupObject(9, 0, 0);
                    textmarkupobj.pagenumber = DocObj.getcurPage();

                    textmarkupobj.viewname = DocObj.getcur3DView();

                    if(textmarkupobj.viewname != null){
                        textmarkupobj.bfor3D = true;
                        textmarkupobj.pageheight = canvasoheight;
                        textmarkupobj.pagewidth = canvasowidth;
                        
                    }


                    textmarkupobj.font.setFontname(defaultFont.fontName);


                    textmarkupobj.scaling = getCurrentPageScale();
                    var offset = getCurrentPageOffset();
                    textmarkupobj.xoffset = offset.x;
                    textmarkupobj.yoffset = offset.y;


                    textmarkupobj.x = tool.x0;
                    textmarkupobj.y = tool.y0;
                    textmarkupobj.seteditaction(0);

                    var markuptext = "";

                    textmarkupobj.savemetolistDraw();
                    DocObj.selectedmarkup.id = textmarkupobj.markupnumber;
                    //img_update();
                    tool.started = false;
                    textmarkupobj = null;
                    if (RxCore_GUI_Textdiag != undefined) {
                        RxCore_GUI_Textdiag.setTextdiag("");
                    }

                    //markuptext = prompt("Enter markup text","");

                    /*if (markuptext != null && markuptext != ""){
                     textmarkupobj.text = markuptext;
                     textmarkupobj.drawme(context);
                     textmarkupobj.savemetolist();
                     img_update();
                     tool.started = false;
                     textmarkupobj = null;
                     }*/

                };

                this.touchstart = function (ev) {
                    tool.started = true;
                    if(DocObj){
                        DocObj.bMarkupchanged = true;
                    }

                    var touchPos = getTouchPos(canvas, ev, 0);

                    var bWithin = MousePosdrwext(touchPos);
                    if (!bWithin && bLimMarkupExtent) {
                        tool.started = false;
                        if(DocObj){
                            DocObj.bMarkupchanged = false;
                        }

                        return;
                    }

                    tool.x0 = touchPos.x;
                    tool.y0 = touchPos.y;
                    textmarkupobj = new MarkupObject(9, 0, 0);
                    textmarkupobj.pagenumber = DocObj.getcurPage();

                    textmarkupobj.viewname = DocObj.getcur3DView();

                    if(textmarkupobj.viewname != null){
                        textmarkupobj.bfor3D = true;
                        textmarkupobj.pageheight = canvasoheight;
                        textmarkupobj.pagewidth = canvasowidth;
                    }

                    textmarkupobj.font.setFontname(defaultFont.fontName);

                    textmarkupobj.scaling = getCurrentPageScale();
                    var offset = getCurrentPageOffset();
                    textmarkupobj.xoffset = offset.x;
                    textmarkupobj.yoffset = offset.y;

                    textmarkupobj.x = tool.x0;
                    textmarkupobj.y = tool.y0;
                    textmarkupobj.seteditaction(0);

                    var markuptext = "";

                    textmarkupobj.savemetolistDraw();
                    DocObj.selectedmarkup.id = textmarkupobj.markupnumber;
                    //img_update();
                    tool.started = false;
                    textmarkupobj = null;
                    if (RxCore_GUI_Textdiag != undefined) {
                        RxCore_GUI_Textdiag.setTextdiag("");
                    }


                    /*var markuptext = prompt("Enter markup text","");
                     if (markuptext != null && markuptext != ""){
                     textmarkupobj.text = markuptext;
                     textmarkupobj.drawme(context);
                     textmarkupobj.savemetolist();
                     img_update();
                     tool.started = false;
                     textmarkupobj = null;
                     }*/


                };



                // This is called when you release the mouse button.
                this.mouseup = function (ev) {
                    if (tool.started) {

                    }
                };

                this.touchend = function (ev) {

                };

            };

            // The drawing pencil.
            tools.pencil = function (params) {
                var tool = this;
                this.started = false;
                this.name = 'pencil';
                this.anglelengthsupport = false;
                this.refreshSupport = false;

                this.draw = false;
                //ft 08.08.2018 changed from separate index to direct array length                
                //var curmarkup = DocObj.nummarkups;
                var curmarkup = DocObj.markuplist.length;
                var pencilmarkupobj;
                var numlines = 0;
                var subtype = params.p1


                setCursor(getMarkupCursor('pencil'));

                this.newpencil = function(subtype,mousePos){

                    pencilmarkupobj = new MarkupObject(0, subtype, 0);
                    pencilmarkupobj.pagenumber = DocObj.getcurPage();

                    pencilmarkupobj.viewname = DocObj.getcur3DView();

                    if(pencilmarkupobj.viewname != null){
                        pencilmarkupobj.bfor3D = true;
                        pencilmarkupobj.pageheight = canvasoheight;
                        pencilmarkupobj.pagewidth = canvasowidth;
                    }

                    pencilmarkupobj.scaling = getCurrentPageScale();
                    var offset = getCurrentPageOffset();
                    pencilmarkupobj.xoffset = offset.x;
                    pencilmarkupobj.yoffset = offset.y;                    

                    pencilmarkupobj.x = mousePos.x;
                    pencilmarkupobj.y = mousePos.y;
                    pencilmarkupobj.seteditaction(0);

                };

                // This is called when you start holding down the mouse button.
                // This starts the pencil drawing.

                this.mousedown = function (ev) {

                    var mousePos = getMousePos(canvas, ev);
                    var bWithin = MousePosdrwext(mousePos);

                    tool.draw = true;

                    if (numlines == 0 && bWithin){
                        tool.newpencil(subtype,mousePos);
                        tool.started = true;
                        pencilmarkupobj.addpoint(mousePos.x, mousePos.y);
                        if(DocObj){
                            DocObj.bMarkupchanged = true;
                        }

                        //pencilmarkupobj.startdraw(context);


                    }else{
                        if(bWithin){
                            pencilmarkupobj.addline();
                            tool.started = true;
                            pencilmarkupobj.addpoint(mousePos.x, mousePos.y);
                            if(DocObj){
                                DocObj.bMarkupchanged = true;
                            }
    
                        }

                        //pencilmarkupobj.startdraw(context);

                    }
                    numlines++;


                    // if (subtype == 1) {
                    //     pencilmarkupobj.linewidth = 2;
                    // }

                    /*pencilmarkupobj.pagenumber = DocObj.currentpage;
                    if (DocObj.getCurrentpage().usevectorxml) {
                        pencilmarkupobj.scaling = DocObj.getCurrentpage().dscalevector;
                        pencilmarkupobj.xoffset = DocObj.getCurrentpage().dxvector;
                        pencilmarkupobj.yoffset = DocObj.getCurrentpage().dyvector;
                    } else if (DocObj.getCurrentpage().usepdfjs) {
                        pencilmarkupobj.scaling = (DocObj.getCurrentpage().curpagescale * DocObj.getCurrentpage().dscalepdf);
                        pencilmarkupobj.xoffset = DocObj.getCurrentpage().dxpdf;
                        pencilmarkupobj.yoffset = DocObj.getCurrentpage().dypdf;

                    } else {
                        pencilmarkupobj.scaling = DocObj.getCurrentpage().dscale;
                        pencilmarkupobj.xoffset = DocObj.getCurrentpage().dx;
                        pencilmarkupobj.yoffset = DocObj.getCurrentpage().dy;

                    }*/

                    //var mousePos = getMousePos(canvas, ev);
                    /*pencilmarkupobj.x = mousePos.x;
                    pencilmarkupobj.y = mousePos.y;
                    pencilmarkupobj.seteditaction(0);

                    pencilmarkupobj.addpoint(mousePos.x, mousePos.y);
                    tool.started = true;
                    DocObj.bMarkupchanged = true;
                    pencilmarkupobj.startdraw(context);*/
                    //context.beginPath();
                    //context.moveTo(pencilmarkupobj.x, pencilmarkupobj.y);


                };
                this.pointerdown = function (ev) {
                    tool.MSPointerDown(ev);
                };

                this.MSPointerDown = function (ev) {

                    var mousePos = getMousePos(canvas, ev);
                    tool.draw = true;
                    if (numlines == 0){
                        tool.newpencil(subtype,mousePos);
                        tool.started = true;
                        pencilmarkupobj.addpoint(mousePos.x, mousePos.y);
                        if(DocObj){
                            DocObj.bMarkupchanged = true;
                        }

                        //pencilmarkupobj.startdraw(context);


                    }else{
                        pencilmarkupobj.addline();
                        tool.started = true;
                        pencilmarkupobj.addpoint(mousePos.x, mousePos.y);
                        if(DocObj){
                            DocObj.bMarkupchanged = true;
                        }

                        //pencilmarkupobj.startdraw(context);

                    }
                    numlines++;
                    //pencilmarkupobj = new MarkupObject(0, subtype, 0);
                    // if (subtype == 1) {
                    //       pencilmarkupobj.linewidth = 2;
                    //   }

                    /*pencilmarkupobj.pagenumber = DocObj.currentpage;
                    if (DocObj.getCurrentpage().usevectorxml) {
                        pencilmarkupobj.scaling = DocObj.getCurrentpage().dscalevector;
                        pencilmarkupobj.xoffset = DocObj.getCurrentpage().dxvector;
                        pencilmarkupobj.yoffset = DocObj.getCurrentpage().dyvector;
                    } else if (DocObj.getCurrentpage().usepdfjs) {
                        pencilmarkupobj.scaling = (DocObj.getCurrentpage().curpagescale * DocObj.getCurrentpage().dscalepdf);
                        pencilmarkupobj.xoffset = DocObj.getCurrentpage().dxpdf;
                        pencilmarkupobj.yoffset = DocObj.getCurrentpage().dypdf;

                    } else {
                        pencilmarkupobj.scaling = DocObj.getCurrentpage().dscale;
                        pencilmarkupobj.xoffset = DocObj.getCurrentpage().dx;
                        pencilmarkupobj.yoffset = DocObj.getCurrentpage().dy;

                    }*/

                   /* var mousePos = getMousePos(canvas, ev);
                    pencilmarkupobj.x = mousePos.x;
                    pencilmarkupobj.y = mousePos.y;
                    pencilmarkupobj.seteditaction(0);
                    pencilmarkupobj.addpoint(mousePos.x, mousePos.y);
                    tool.started = true;
                    DocObj.bMarkupchanged = true;
                    pencilmarkupobj.startdraw(context);*/

                };

                // This function is called every time you move the mouse. Obviously, it only
                // draws if the tool.started state is set to true (when you are holding down
                // the mouse button).
                this.mousemove = function (ev) {
                    ev.preventDefault();
                    ev.stopPropagation();
                    
                    var mousePos = getMousePos(canvas, ev);

                    var bWithin = MousePosdrwext(mousePos);

                    if (tool.started && tool.draw && bWithin) {
                        pencilmarkupobj.addpoint(mousePos.x, mousePos.y);
                        pencilmarkupobj.drawme(context);
                        //context.lineTo(ev._x, ev._y);
                        //context.stroke();
                    }
                };
                this.pointermove = function (ev) {
                    tool.MSPointerMove(ev);
                };

                this.MSPointerMove = function (ev) {
                    ev.preventDefault();
                    ev.stopPropagation();
                    
                    var mousePos = getMousePos(canvas, ev);
                    if (tool.started && tool.draw) {
                        pencilmarkupobj.addpoint(mousePos.x, mousePos.y);
                        pencilmarkupobj.drawme(context);
                    }

                };
                this.pointerup = function (ev) {
                    tool.MSPointerUp(ev);
                };

                this.MSPointerUp = function (ev) {
                    ev.preventDefault();
                    tool.draw = false;
                    /*if (tool.started) {
                        tool.mousemove(ev);
                        tool.started = false;
                        pencilmarkupobj.findrectangle();
                        pencilmarkupobj.savemetolist();
                        pencilmarkupobj = null;

                        img_update();
                    }*/

                };

                // This is called when you release the mouse button.
                this.mouseup = function (ev) {
                    ev.preventDefault();
                    tool.draw = false;
                    /*if (tool.started) {
                        tool.mousemove(ev);
                        tool.started = false;
                        pencilmarkupobj.findrectangle();
                        pencilmarkupobj.savemetolist();
                        pencilmarkupobj = null;

                        img_update();
                    }*/
                };

                this.touchstart = function (ev) {
                    ev.preventDefault();
                    tool.draw = true;
                    var touchPos = getTouchPos(canvas, ev, 0);
                    var bWithin = MousePosdrwext(touchPos);

                    if (numlines == 0 && bWithin){
                        tool.newpencil(subtype,touchPos);
                        tool.started = true;
                        pencilmarkupobj.addpoint(touchPos.x, touchPos.y);
                        if(DocObj){
                            DocObj.bMarkupchanged = true;
                        }

                        //pencilmarkupobj.startdraw(context);


                    }else{
                        if(bWithin){
                            pencilmarkupobj.addline();
                            tool.started = true;
                            pencilmarkupobj.addpoint(touchPos.x, touchPos.y);
                            if(DocObj){
                                DocObj.bMarkupchanged = true;
                            }
                        }

                        //pencilmarkupobj.startdraw(context);

                    }
                    numlines++;


                    /*if (numlines == 0){
                        pencilmarkupobj = new MarkupObject(0, subtype, 0);
                        pencilmarkupobj.pagenumber = DocObj.currentpage;
                        if (DocObj.getCurrentpage().usevectorxml) {
                            pencilmarkupobj.scaling = DocObj.getCurrentpage().dscalevector;
                            pencilmarkupobj.xoffset = DocObj.getCurrentpage().dxvector;
                            pencilmarkupobj.yoffset = DocObj.getCurrentpage().dyvector;
                        } else if (DocObj.getCurrentpage().usepdfjs) {
                            pencilmarkupobj.scaling = (DocObj.getCurrentpage().curpagescale * DocObj.getCurrentpage().dscalepdf);
                            pencilmarkupobj.xoffset = DocObj.getCurrentpage().dxpdf;
                            pencilmarkupobj.yoffset = DocObj.getCurrentpage().dypdf;

                        } else {
                            pencilmarkupobj.scaling = DocObj.getCurrentpage().dscale;
                            pencilmarkupobj.xoffset = DocObj.getCurrentpage().dx;
                            pencilmarkupobj.yoffset = DocObj.getCurrentpage().dy;

                        }

                        pencilmarkupobj.x = touchPos.x;
                        pencilmarkupobj.y = touchPos.y;
                        pencilmarkupobj.seteditaction(0);
                    }else{
                        pencilmarkupobj.addline();
                    }
                    numlines++;*/

                    //pencilmarkupobj = new MarkupObject(0, subtype, 0);
                    // if (subtype == 1) {
                    //            pencilmarkupobj.linewidth = 2;
                    //        }

                    /*pencilmarkupobj.addpoint(touchPos.x, touchPos.y);
                    tool.started = true;
                    DocObj.bMarkupchanged = true;
                    pencilmarkupobj.startdraw(context);*/

                    //context.strokeStyle = "red";
                    //context.beginPath();
                    //context.moveTo(ev.targetTouches[0].pageX - canvas.offsetLeft, ev.targetTouches[0].pageY - canvas.offsetTop - bannerheight);
                    //tool.started = true;

                };
                this.touchmove = function (ev) {
                    ev.preventDefault();

                    var touchPos = getTouchPos(canvas, ev, 0);
                    var bWithin = MousePosdrwext(touchPos);

                    if (tool.started && tool.draw && bWithin) {
                        
                        pencilmarkupobj.addpoint(touchPos.x, touchPos.y);
                        pencilmarkupobj.drawme(context);

                        //context.lineTo(, );
                        //context.stroke();
                    }
                };
                this.touchcancel = function(ev){
                    ev.preventDefault();
                    tool.draw = false;
                };

                this.touchend = function (ev) {
                    ev.preventDefault();
                    tool.draw = false;
                    /*if (tool.started) {
                        //add new function to add current pointarray to existing
                        //pencilmakrupobject.

                        pencilmarkupobj.findrectangle();
                        pencilmarkupobj.savemetolist();
                        img_update();
                        //alert("touchend");
                        //tool.touchmove(ev);
                        tool.started = false;

                        pencilmarkupobj = null;
                    }*/
                };

                this.apply = function () {
                    tool.draw = false;
                    if (!bMultimarkupadd) {
                        bMarkupcrated = true;
                        //ft 08.08.2018 changed from separate index to direct array length                        
                        //nMarkupcreated = DocObj.nummarkups;
                        //nMarkupcreated = DocObj.markuplist.length;
                    }
                    nMarkupcreated = DocObj.markuplist.length;

                    pencilmarkupobj.addline();
                    pencilmarkupobj.findrectangle();
                    pencilmarkupobj.savemetolistDraw();
                    img_update();
                    tool.started = false;
                    pencilmarkupobj = null;

                    if(bAlwaysMarkupCallback){
                        if (RxCore_GUI_Markup != undefined) {
                            var operation = {created : true, modified : false, deleted : false, multi : bMultimarkupadd};
                            RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[nMarkupcreated], operation);

                            
                        }
            
                    }else{
                        if (!bMultimarkupadd) {
                            //need to move to connection object.
                            //markupcreated();
                            if (RxCore_GUI_Markup != undefined) {
                                //RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[index],true);
                                var operation = {created : true, modified : false, deleted : false};
                                RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[nMarkupcreated], operation);
                            }
    
                        }
    
                    }



                };

            };


            // The rectangle tool.
            tools.rect = function (params) {

                var tool = this;
                var prevx = 0,
                    prevy = 0;
                var rectmarkupobj;
                //var drawrectangle = new markuprectangle();
                //ft 08.08.2018 changed from separate index to direct array length
                //var curmarkup = DocObj.nummarkups;
                var curmarkup = DocObj.markuplist.length;
                var havesnap = false;
                var snapPoint;
                this.name = 'rect';
                this.anglelengthsupport = true;
                
                this.refreshSupport = false;

                this.bUsemouseinput = true;

                this.started = false;
                var type = params.p1;
                var subtype = params.p2;
                var redact = params.redact;


                setCursor(getMarkupCursor('rect'));


                this.setWidthHeight = function(width, height){
                    tool.bUsemouseinput = false;
                    if (rectmarkupobj != undefined){
                        rectmarkupobj.x = tool.x0;
                        rectmarkupobj.y = tool.y0;
                        var curx2 = rectmarkupobj.w;
                        var cury2 = rectmarkupobj.h;
                        
                        var secondpoint = rectmarkupobj.setWidthHeight(width, height);
                        var abspoint = {x : rectmarkupobj.x + secondpoint.w, y:rectmarkupobj.y + secondpoint.h};

                        context.clearRect(0, 0, canvas.width, canvas.height);
                        
                        if (MousePosdrwext(abspoint)){
                            rectmarkupobj.w = secondpoint.w;
                            rectmarkupobj.h = secondpoint.h;
                            
                            rectmarkupobj.drawme(context);

                        }else{
                            rectmarkupobj.w = curx2;
                            rectmarkupobj.h = cury2;

                            tool.bUsemouseinput = true;
                            rectmarkupobj.drawme(context);
                            rectmarkupobj.bUsemouseinput = true;
                            if (RxCore_GUI_markupParamsError != undefined){
                                RxCore_GUI_markupParamsError.onDrawError('outside');
                            }

                        }


                    }

                };

                this.apply = function(){
                    context.clearRect(0, 0, canvas.width, canvas.height);
                    rectmarkupobj.drawme(context);
                    tool.endRect();
                };

                this.pointermove = function (ev) {
                    tool.MSPointerMove(ev);
                };
                this.pointerdown = function (ev) {
                    tool.MSPointerDown(ev);
                };

                this.pointerup = function (ev) {
                    tool.MSPointerUp(ev);
                };


                this.startRect = function (mousePos) {
                    tool.started = true;
                    if(DocObj){
                        DocObj.bMarkupchanged = true;
                    }



                    if (havesnap) {
                        if(DocObj.getCurrentpage().getpageRotation() != 0){
                            var rotatedpoint = snap_rotated(snapPoint);
                        }else{
                            rotatedpoint = snapPoint;
                        }

                        tool.x0 = rotatedpoint.x;
                        tool.y0 = rotatedpoint.y;
                    } else {
                        tool.x0 = mousePos.x;
                        tool.y0 = mousePos.y;
                    }
                    

                    rectmarkupobj = new MarkupObject(type, subtype, fillstyle);
                    rectmarkupobj.bRedact = redact;
    
                    if (subtype == 3){
                    
                        rectmarkupobj.transparency = 30;
                        rectmarkupobj.fillcolor = setTransp(rectmarkupobj.fillcolor,rectmarkupobj.transparency);
                    }

                    rectmarkupobj.scaling = getCurrentPageScale();
                    var offset = getCurrentPageOffset();
                    rectmarkupobj.xoffset = offset.x;
                    rectmarkupobj.yoffset = offset.y;

                    rectmarkupobj.pagenumber = DocObj.getcurPage();

                    rectmarkupobj.viewname = DocObj.getcur3DView();

                    if(rectmarkupobj.viewname != null){
                        rectmarkupobj.bfor3D = true;
                        rectmarkupobj.pageheight = canvasoheight;
                        rectmarkupobj.pagewidth = canvasowidth;
                    }


                    rectmarkupobj.seteditaction(0);


                };

                this.drawRect = function (mousePos) {
                    if (!tool.bUsemouseinput){
                        return;
                    }

                    if (havesnap) {
                        if(DocObj.getCurrentpage().getpageRotation() != 0){
                            var rotatedpoint = snap_rotated(snapPoint);
                        }else{
                            rotatedpoint = snapPoint;
                        }

                        rectmarkupobj.x = Math.min(rotatedpoint.x, tool.x0);
                        rectmarkupobj.y = Math.min(rotatedpoint.y, tool.y0);

                        rectmarkupobj.w = Math.abs(rotatedpoint.x - tool.x0);
                        rectmarkupobj.h = Math.abs(rotatedpoint.y - tool.y0);


                        prevx = rotatedpoint.x;
                        prevy = rotatedpoint.y;


                    } else {
                        rectmarkupobj.x = Math.min(mousePos.x, tool.x0);
                        rectmarkupobj.y = Math.min(mousePos.y, tool.y0);
                        rectmarkupobj.w = Math.abs(mousePos.x - tool.x0);
                        rectmarkupobj.h = Math.abs(mousePos.y - tool.y0);

                        prevx = mousePos.x;
                        prevy = mousePos.y;


                    }
                };

                this.endRect = function () {
                    /*if(rectmarkupobj.w==0 || rectmarkupobj.h==0){
                     return;
                     }*/
                    tool.started = false;

                    if (!bMultimarkupadd) {

                        bMarkupcrated = true;
                        //ft 08.08.2018 changed from separate index to direct array length
                        //nMarkupcreated = DocObj.markuplist.length;
                        //nMarkupcreated = DocObj.nummarkups;
                    }

                    nMarkupcreated = DocObj.markuplist.length;

                    rectmarkupobj.savemetolistDraw();
                    rectmarkupobj = null;


                    //tool.started = false;
                    //markuplist.add(drawrectangle);
                    img_update();
                    drawmarkupAll(cntximg);

                    if(bAlwaysMarkupCallback){
                        if (RxCore_GUI_Markup != undefined) {
                            var operation = {created : true, modified : false, deleted : false, multi : bMultimarkupadd};
                            RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[nMarkupcreated], operation);
                        }
            
                    }else{
                        if (!bMultimarkupadd) {
                            //need to move to connection object.
                            //markupcreated();
                            if (RxCore_GUI_Markup != undefined) {
                                //RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[index],true);
                                var operation = {created : true, modified : false, deleted : false};
                                RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[nMarkupcreated], operation);
                            }
    
                        }
    
                    }



                };

                this.setSnap = function (mousePos) {
                    
                    snapPoint = DocObj.getCurrentpage().getsnap(mousePos);

                    if(snapPoint == undefined){
                        havesnap = false;
                        context.clearRect(0, 0, canvas.width, canvas.height);
                    }

                    if (snapPoint.found ) {
                        havesnap = true;
                        drawsnap(context,snapPoint);
                        //context.lineWidth = 1;
                        //context.strokeStyle = 'blue';
                        //context.strokeRect(snapPoint.x - 5, snapPoint.y - 5, 10, 10);
                    } else {
                        havesnap = false;
                        context.clearRect(0, 0, canvas.width, canvas.height);
                    }

                };

                this.MSPointerDown = function (ev) {
                    ev.preventDefault();
                    ev.stopPropagation();

                    

                    tool.started = true;
                    if(DocObj){
                        DocObj.bMarkupchanged = true;
                    }

                    var mousePos = getMousePos(canvas, ev);
                    var bWithin = MousePosdrwext(mousePos);
                    
                    //mousePos = getDocRotMousePoint(mousePos);

                    if (!bWithin && bLimMarkupExtent) {
                        tool.started = false;
                        if(DocObj){
                            DocObj.bMarkupchanged = false;
                        }

                        
                        return;

                    }
                    tool.startRect(mousePos);

                };

                this.mousedown = function (ev) {
                    ev.preventDefault();
                    ev.stopPropagation();

                    
                    if (!tool.started) {
                        tool.started = true;

                        if(DocObj){
                            DocObj.bMarkupchanged = true;
                        }
    
                        var mousePos = getMousePos(canvas, ev);
                        var bWithin = MousePosdrwext(mousePos);
                        //mousePos = getDocRotMousePoint(mousePos);
    
                        if (!bWithin && bLimMarkupExtent) {
                            tool.started = false;
                            if(DocObj){
                                DocObj.bMarkupchanged = false;
                            }
                            
                            return;
                        }
                        //tool.mousemove(ev);
                        tool.startRect(mousePos);
    
                    }else{
                        tool.mousemove(ev);
                        tool.endRect();

                        tool.started = false;

                    }

                };

                this.touchstart = function (ev) {

                    ev.preventDefault();
                    tool.started = true;
                    if(DocObj){
                        DocObj.bMarkupchanged = true;
                    }



                    var touchPos = getTouchPos(canvas, ev, 0);

                    var bWithin = MousePosdrwext(touchPos);
                    //touchPos = getDocRotMousePoint(touchPos);
                    
                    if (!bWithin && bLimMarkupExtent) {
                        tool.started = false;
                        if(DocObj){
                            DocObj.bMarkupchanged = false;
                        }

                        return;
                    }


                    tool.x0 = touchPos.x;
                    tool.y0 = touchPos.y;


                    rectmarkupobj = new MarkupObject(type, subtype,  fillstyle);
                    rectmarkupobj.bRedact = redact;

                    if (subtype == 3){
                        rectmarkupobj.transparency = 30;
                        rectmarkupobj.fillcolor = setTransp(rectmarkupobj.fillcolor,rectmarkupobj.transparency);
                    }

                    rectmarkupobj.scaling = getCurrentPageScale();
                    var offset = getCurrentPageOffset();
                    rectmarkupobj.xoffset = offset.x;
                    rectmarkupobj.yoffset = offset.y;


                    rectmarkupobj.pagenumber = DocObj.getcurPage();
                    rectmarkupobj.viewname = DocObj.getcur3DView();

                    if(rectmarkupobj.viewname != null){
                        rectmarkupobj.bfor3D = true;
                        rectmarkupobj.pageheight = canvasoheight;
                        rectmarkupobj.pagewidth = canvasowidth;
                    }

                    rectmarkupobj.seteditaction(0);

                };

                this.MSPointerMove = function (ev) {
                    ev.preventDefault();
                    ev.stopPropagation();
                    
                    var mousePos = getMousePos(canvas, ev);

                    tool.setSnap(mousePos);

                    

                    if (!tool.started) {
                        return;
                    }
                    var bWithin = MousePosdrwext(mousePos);
                    //mousePos = getDocRotMousePoint(mousePos);

                    if (bWithin || !bLimMarkupExtent) {
                        tool.drawRect(mousePos);


                    } else {
                        if (tool.bUsemouseinput){

                            rectmarkupobj.x = Math.min(prevx, tool.x0);
                            rectmarkupobj.y = Math.min(prevy, tool.y0);
                            rectmarkupobj.w = Math.abs(prevx - tool.x0);
                            rectmarkupobj.h = Math.abs(prevy - tool.y0);
                        }


                    }
                    context.clearRect(0, 0, canvas.width, canvas.height);
                    if (havesnap) {
                        drawsnap(context,snapPoint);
                    }

                    if (!rectmarkupobj.w || !rectmarkupobj.h) {
                        return;
                    }
                    rectmarkupobj.drawme(context);

                };

                this.mousemove = function (ev) {
                    ev.preventDefault();
                    ev.stopPropagation();
                    
                    var mousePos = getMousePos(canvas, ev);

                    tool.setSnap(mousePos);

                    if (!tool.started) {
                        return;
                    }

                    var bWithin = MousePosdrwext(mousePos);
                    //mousePos = getDocRotMousePoint(mousePos);

                    if (bWithin || !bLimMarkupExtent) {
                        tool.drawRect(mousePos);


                    } else {

                        if (tool.bUsemouseinput){

                            rectmarkupobj.x = Math.min(prevx, tool.x0);
                            rectmarkupobj.y = Math.min(prevy, tool.y0);
                            rectmarkupobj.w = Math.abs(prevx - tool.x0);
                            rectmarkupobj.h = Math.abs(prevy - tool.y0);

                            
                        }



                    }
                    context.clearRect(0, 0, canvas.width, canvas.height);
                    if (havesnap) {
                        drawsnap(context,snapPoint);
                    }

                    rectmarkupobj.drawme(context);

                    if (!rectmarkupobj.w || !rectmarkupobj.h) {
                        return;
                    }

                };

                this.touchmove = function (ev) {
                    ev.preventDefault();
                    if (!tool.started) {
                        return;
                    }
                    var touchPos = getTouchPos(canvas, ev, 0);
                    var bWithin = MousePosdrwext(touchPos);
                    //touchPos = getDocRotMousePoint(touchPos);
                    if (bWithin || !bLimMarkupExtent) {
                        var x = Math.min(touchPos.x, tool.x0),
                            y = Math.min(touchPos.y, tool.y0),
                            w = Math.abs(touchPos.x - tool.x0),
                            h = Math.abs(touchPos.y - tool.y0);

                        prevx = touchPos.x;
                        prevy = touchPos.y;

                    } else {
                        x = Math.min(prevx, tool.x0);
                        y = Math.min(prevy, tool.y0);
                        w = Math.abs(prevx - tool.x0);
                        h = Math.abs(prevy - tool.y0);

                    }
                    if (tool.bUsemouseinput){
                        rectmarkupobj.x = x;
                        rectmarkupobj.y = y;
                        rectmarkupobj.w = w;
                        rectmarkupobj.h = h;
                    }


                    context.clearRect(0, 0, canvas.width, canvas.height);

                    if (!w || !h) {
                        return;
                    }
                    rectmarkupobj.drawme(context);

                };

                this.MSPointerUp = function (ev) {
                    
                    if (tool.started) {
                        tool.mousemove(ev);
                        tool.endRect();

                        tool.started = false;


                    }

                };
                this.mouseup = function (ev) {
                    
                    /*if (tool.started) {

                        tool.mousemove(ev);
                        tool.endRect();

                        tool.started = false;


                    }*/
                };
                this.touchcancel = function(ev){
                    ev.preventDefault();
                    if (tool.started) {

                        tool.endRect();
                        tool.started = false;

                    }

                };  

                this.touchend = function (ev) {
                    ev.preventDefault();
                    if (tool.started) {

                        tool.endRect();
                        tool.started = false;

                    }
                };

            };


            // The count tool.
            tools.count = function(params){

                var tool = this;

                var prevx = 0, 
                    prevy = 0;

                this.draw = false;
                this.name = 'count';

                this.anglelengthsupport = false;
                this.refreshSupport = false;

                var countmarkupobj;
                var curmarkup = DocObj.markuplist.length;


                var type = params.p1; //13
                var subtype = params.p2; //shape

                var numcounts = 0;

                //this.curmousepos = {x:0,y:0};
                this.curmousepos = {x:0,y:0,xus:0,yus:0};

                //var shape = params.


                setCursor(getMarkupCursor('count'));


                this.setdimoffset = function(initial){

                    if(countmarkupobj == undefined){
                        return;
                    }

                    if(initial){

                       
                        countmarkupobj.scaling = getCurrentPageScale();
                        

                        var offset = getCurrentPageOffset();
                        countmarkupobj.xoffset = offset.x;
                        countmarkupobj.yoffset = offset.y;

                        tool.orgscale = countmarkupobj.scaling;
                        tool.orgdx = countmarkupobj.xoffset;
                        tool.orgdy = countmarkupobj.yoffset;
                        
                    }
                    

                    if(countmarkupobj.countshapes.length > 0){
                        countmarkupobj.drawme(context);
                    }


                };                        

                this.newshape = function(subtype,mousePos){

                    countmarkupobj = new MarkupObject(type, subtype, 0);

                    countmarkupobj.strokecolor = markupfillcolor;
                    countmarkupobj.fillcolor = markupfillcolor;


                    countmarkupobj.outerRadius = nCounterSize;

                    countmarkupobj.innerRadius = nCounterSize * 0.4;

                    countmarkupobj.pagenumber = DocObj.getcurPage();

                    countmarkupobj.viewname = DocObj.getcur3DView();

                    if(countmarkupobj.viewname != null){
                        countmarkupobj.bfor3D = true;
                        countmarkupobj.pageheight = canvasoheight;
                        countmarkupobj.pagewidth = canvasowidth;
                    }


                    tool.setdimoffset(true);

                    countmarkupobj.seteditaction(0);

                    countmarkupobj.addcount(mousePos);

                    if(countmarkupobj.countshapes.length > 0){
                        countmarkupobj.drawme(context);
                    }




                    numcounts++;

                };




                this.apply = function () {
                    tool.draw = false;
                    if (!bMultimarkupadd) {
                        bMarkupcrated = true;
                        //ft 08.08.2018 changed from separate index to direct array length                        
                        //nMarkupcreated = DocObj.nummarkups;
                        //nMarkupcreated = DocObj.markuplist.length;
                    }
                    nMarkupcreated = DocObj.markuplist.length;
                    //countmarkupobj.addcount();

                    countmarkupobj.findrectangle();
                    countmarkupobj.savemetolistDraw();
                    img_update();
                    tool.started = false;
                    countmarkupobj = null;
                    numcounts = 0;


                    if(bAlwaysMarkupCallback){
                        if (RxCore_GUI_Markup != undefined) {
                            var operation = {created : true, modified : false, deleted : false, multi : bMultimarkupadd};
                            RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[nMarkupcreated], operation);
                            
                        }
            
                    }else{

                        if (!bMultimarkupadd) {
                            //need to move to connection object.
                            //markupcreated();
                            if (RxCore_GUI_Markup != undefined) {
                                //RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[index],true);
                                var operation = {created : true, modified : false, deleted : false};
                                RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[nMarkupcreated], operation);
                            }
    
                        }
    
                    }



                };

                this.scrollhandling = function (movey) {
                    if (!documentopen || DocObj.getCurrentpage().usevector3Dxml) {
                        return;
                    }

                    if (movey < 0) {


                        //if (DocObj.getCurrentpage().usepdfjs && DocObj.pages.length > 1) {
                        if (DocObj.getCurrentpage().usepdfjs && DocObj.pages.length > 1 && !enableZoomOnScrollMultipage) {                            
                            //DocObj.getCurrentpage().pan_update(0,-movey);
                            if (DocObj.currentpage == DocObj.NumPages - 1) {
                                if (DocObj.getCurrentpage().endy <= canvas.height) {
                                    movey = 0;
                                }
                            }
                            DocObj.getCurrentpage().pan_update(0, -movey);


                        } else {

                            if(belowlimitExtentEx(1.1, tool.curmousepos) || bAllowfreeZoomOut){
                                DocObj.getCurrentpage().ZoomOut(1.1, false, false, tool.curmousepos);
                            }else{
                                RxCore_foxitForceRedraw();
                            }

                            if(tool.started){

                                tool.setdimoffset(false);

                            }



                        }


                    } else {

                        //if (DocObj.getCurrentpage().usepdfjs && DocObj.pages.length > 1) {
                        if (DocObj.getCurrentpage().usepdfjs && DocObj.pages.length > 1 && !enableZoomOnScrollMultipage) {
                            if (DocObj.currentpage == 0) {
                                if (DocObj.getCurrentpage().starty >= 0) {
                                    movey = 0;
                                }

                            }
                            DocObj.getCurrentpage().pan_update(0, -movey);


                        } else {


                            DocObj.getCurrentpage().ZoomIn(1.1, false, false, tool.curmousepos);



                            if(tool.started){
                                tool.setdimoffset(false);


                            }

                        }

                    }


                };                   

                this.belowlimitExtent = function(zoomscalefactor){
                    var belowlimit = true;
                    

                    var pagedim = DocObj.getCurrentpage().getpagedim();

                    var wlimit = canvasowidth - 60;
                    var hlimit = canvasoheight - 60;

                    if((pagedim.w / zoomscalefactor) < wlimit && (pagedim.h / zoomscalefactor) < hlimit){
                        belowlimit = false;
                        //scalechanged = false;
                        //DocObj.getCurrentpage().zoomall();
                    }

                    var pagerect = DocObj.getCurrentpage().ZoomOutTest(zoomscalefactor, false, tool.curmousepos);
                    
                    var heightlimit = canvasoheight - (nPanlimit*2);
                    var widthlimit = canvasowidth - (nPanlimit*2);
                    //var toplimit = 15;
                    var bottomlimit = ((pagerect.y + pagerect.h) <= heightlimit);
                    var toplimit = (pagerect.y >= nPanlimit);

                    // need to return these to reset the top and bottom positions.scroll|| bottomlimit || toplimit
                    if ((pagerect.w / pagerect.h) < (canvasowidth / canvasoheight)){
                        if(pagerect.h <= heightlimit){
                            belowlimit = false;
                        }
    
                    }else{
                        if(pagerect.w <= widthlimit){
                            belowlimit = false;
                        }

                    }


                    if(!bLimitZoomOut){
                        belowlimit = true;
                    }

                    return belowlimit;
                };                



                this.wheel = function (ev) {

                    var delta = 0;
                    if (ev.wheelDelta !== undefined) { // WebKit / Opera / Explorer 9

                        delta = ev.wheelDelta;

                    } else if (ev.deltaY !== undefined) { // Firefox

                        delta = -ev.deltaY*50;

                    }

                    tool.scrollhandling(delta);


                };

                this.mousedown = function (ev) {

                    ev.preventDefault();
                    ev.stopPropagation();

                    var mousePos = getMousePos(canvas, ev);
                    var bWithin = MousePosdrwext(mousePos);

                    

                    tool.draw = true;

                    if(ev.button == 0){

                        if (!bWithin && bLimMarkupExtent) {
                            
                            tool.started = false;
        
                            if(DocObj){
                                DocObj.bMarkupchanged = false;
                            }
                            return;
                            
                        }

                        if(numcounts == 0){
                            tool.started = true;
                            tool.newshape(subtype, mousePos);
    
                        }else{
    
                            countmarkupobj.addcount(mousePos);

                            if(countmarkupobj.countshapes.length > 0){
                                countmarkupobj.drawme(context);
                            }
        
        
                            numcounts++;
                        }
    
                    }else{
                        prevx = mousePos.x;
                        prevy = mousePos.y;
                    }


                };                


                this.mousemove = function (ev) {
                    ev.preventDefault();
                    ev.stopPropagation();
                    
                    
                    
                    var mousePos = getMousePos(canvas, ev);
                    tool.curmousepos = mousePos;

                    var xdiff = prevx - mousePos.x;
                    var ydiff = prevy - mousePos.y;

                    
                    if (ev.button == 2 || ev.buttons == 2) {

                        DocObj.getCurrentpage().pan_update(xdiff, ydiff);
                        tool.setdimoffset(false);
                        prevx = mousePos.x;
                        prevy = mousePos.y;

                    }


                };                


                this.pointerdown = function (ev) {
                    tool.MSPointerDown(ev);
                };

                this.pointermove = function (ev) {
                    tool.MSPointerMove(ev);
                };


                this.MSPointerDown = function (ev) {
                    
                    var mousePos = getMousePos(canvas, ev);
                    var bWithin = MousePosdrwext(mousePos);

                    tool.draw = true;


                    if(ev.button == 0){
                        if (!bWithin && bLimMarkupExtent) {
                            
                            tool.started = false;
        
                            if(DocObj){
                                DocObj.bMarkupchanged = false;
                            }
                            return;
                            
                        }
    
    
                        if(numcounts == 0){
                            tool.started = true;
                            tool.newshape(subtype, mousePos);
    
                        }else{
    
                            countmarkupobj.addcount(mousePos);
                            if(countmarkupobj.countshapes.length > 0){
                                countmarkupobj.drawme(context);
                            }
        
        

        
                            numcounts++;
                        }     

                    }else{

                        prevx = mousePos.x;
                        prevy = mousePos.y;

                    }


                                   
                };


                this.MSPointerMove = function (ev) {
                    ev.preventDefault();
                    ev.stopPropagation();
                    
                    var mousePos = getMousePos(canvas, ev);
                    tool.curmousepos = mousePos;

                    var xdiff = prevx - mousePos.x;
                    var ydiff = prevy - mousePos.y;

                    if (ev.buttons == 2) {
                        DocObj.getCurrentpage().pan_update(xdiff, ydiff);
                        prevx = mousePos.x;
                        prevy = mousePos.y;
                    }

                };                


                this.touchstart = function (ev) {

                    ev.preventDefault();
                    ev.stopPropagation();
                    
                    var touchPos = getTouchPos(canvas, ev, 0);
                    var bWithin = MousePosdrwext(touchPos);

                    tool.draw = true;


                    if (!bWithin && bLimMarkupExtent) {
                        tool.started = false;
                        
                        if(DocObj){
                            DocObj.bMarkupchanged = false;
                        }
                        
                        return;
                    }
    
                    if(numcounts == 0){
                        tool.started = true;
                        tool.newshape(subtype, touchPos);

                    }else{

                        countmarkupobj.addcount(touchPos);

                        if(countmarkupobj.countshapes.length > 0){
                            countmarkupobj.drawme(context);
                        }
    
    

    
                        numcounts++;
                    }


                };                

                this.touchmove = function (ev) {
                    ev.preventDefault();

                    var touchPos = getTouchPos(canvas, ev, 0);

                    prevx = touchPos.x;
                    prevy = touchPos.y;

                };



            };

            tools.clipboardimage = function(params){
                var tool = this;
                var canvasLeft = canvas.offsetLeft;
                var canvasTop = canvas.offsetTop;
                var startOffsetX = 0;
                var startOffsetY = 0;
                var imagemarkupobj;

                var dropdata = {
                    width : 1,
                    height : 1
                }

                var imgrect = {x : 0, y : 0, w : dropdata.width, h : dropdata.height};


                var curmarkup = DocObj.markuplist.length;

                this.name = 'clipboardimage';
                this.anglelengthsupport = false;

                this.started = false;

                this.clipboardimage = undefined;

                if(params.p1 != undefined){
                    this.subtype = params.p1;
                }else{
                    this.subtype = 1;
                }

                


                this.setimage = function(blob){


                    var createimage = new Image();

                    imagemarkupobj = new MarkupObject(11, tool.subtype, 0);


                    imagemarkupobj.scaling = getCurrentPageScale();
                    var offset = getCurrentPageOffset();
                    imagemarkupobj.xoffset = offset.x;
                    imagemarkupobj.yoffset = offset.y;

                    imagemarkupobj.pagenumber = DocObj.getcurPage();
                    imagemarkupobj.viewname = DocObj.getcur3DView();

                    if(imagemarkupobj.viewname != null){
                        imagemarkupobj.bfor3D = true;
                        imagemarkupobj.pageheight = canvasoheight;
                        imagemarkupobj.pagewidth = canvasowidth;
                    }
                    //imagemarkupobj.editaction = 0;
                    imagemarkupobj.seteditaction(0);

                    //var createimage = new Image();


                    imagemarkupobj.markupimageblob(blob, createimage).then(function(image){

                        dropdata = {
                            width : image.naturalWidth,
                            height : image.naturalHeight
                        };
    
                        imgrect = {x : 0, y : 0, w : dropdata.width, h : dropdata.height};

    
                    });



                    //tool.clipboardimage = blob;
                    //createimage.src = ev.dataTransfer.getData("Text");
                    //createimage.src = dropdata.src;
                    //imagemarkupobj.image = tool.clipboardimage;
                    //imagemarkupobj.imageloaded = true;

                    

                };

                this.mousemove = function (ev) {
                    ev.preventDefault();
                    ev.stopPropagation();

                    if(imagemarkupobj){

                        if(imagemarkupobj.imageloaded){

                            var mousePos = getMousePos(canvas, ev);

                            imgrect = {x : mousePos.x, y : mousePos.y, w : dropdata.width, h : dropdata.height};


                            var bWithin = MousePosdrwextRect(imgrect);

                            //countmarkupobj.drawme(context);
                            if(bWithin) {
                                ev.target.style.cursor = 'copy';
                            }else{
                                ev.target.style.cursor = 'not-allowed';
                            }
                            

                            context.clearRect(0, 0, canvas.width, canvas.height);
                            context.beginPath();
                            context.rect(imgrect.x, imgrect.y, imgrect.w, imgrect.h);
                            context.stroke();
                            
                            

                        }else{
                            ev.target.style.cursor = 'default';
                        }
    
                        
                    }


                };


                this.mousedown = function (ev) {


                    ev.preventDefault();

                    if(!imagemarkupobj){
                        return;
                    }

                    if(!imagemarkupobj.imageloaded){
                        return;
                    }


                    var mousePos = getMousePos(canvas, ev);
                    //var dropX = ev.clientX;// - startOffsetX;
                    //var dropY = ev.clientY;// - startOffsetY;

                    
                    var bWithin = MousePosdrwextRect(imgrect);


                    if (bWithin || !bLimMarkupExtent) {
                        


                        imagemarkupobj.x = mousePos.x;// - (dropdata.width/2);
                        imagemarkupobj.y = mousePos.y;// - (dropdata.height/2);
                        imagemarkupobj.w = dropdata.width;
                        imagemarkupobj.h = dropdata.height;
                        //var dropdata = ev.dataTransfer.getData("Text");

                        context.clearRect(0, 0, canvas.width, canvas.height);

                        if (!bMultimarkupadd) {
                            bMarkupcrated = true;
                            //ft 08.08.2018 changed from separate index to direct array length                  
                            //nMarkupcreated = DocObj.nummarkups;
                            nMarkupcreated = DocObj.markuplist.length;
                        }

                        imagemarkupobj.savemetolistDraw();

                    }

                    img_update();
                    imagemarkupobj = null;
                    if (!bMultimarkupadd && bWithin) {
                        //need to move to connection object.
                        //markupcreated();
                        if (RxCore_GUI_Markup != undefined) {
                            //RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[index],true);
                            var operation = {created : true, modified : false, deleted : false};
                            RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[nMarkupcreated], operation);
                        }

                    }
                    //var mousePos = getMousePos(canvas, ev);
                    //startOffsetX = mousePos.x;
                    //startOffsetY = mousePos.y;

                };


            };                        

            // The symbol tool.
            tools.symbol = function(params){
                var tool = this;
                var canvasLeft = canvas.offsetLeft;
                var canvasTop = canvas.offsetTop;
                var startOffsetX = 0;
                var startOffsetY = 0;
                var imagemarkupobj;
                //ft 08.08.2018 changed from separate index to direct array length
                //var curmarkup = DocObj.nummarkups;
                var curmarkup = DocObj.markuplist.length;



                this.name = 'symbol';
                this.anglelengthsupport = false;
                
                this.refreshSupport = false;

                this.started = false;

                if(params.p1 != undefined){
                    this.subtype = params.p1;
                }else{
                    this.subtype = 1;
                }


                setCursor(getMarkupCursor('symbol'));

                //rxcontainer.ondrop = tool.drop;
                //rxcontainer.ondragover = tool.dragover;




                this.dragover = function(ev){
                    ev.preventDefault();
                };

                this.drop = function(ev){
                    ev.preventDefault();

                    var mousePos = getMousePos(canvas, ev);
                    //var dropX = ev.clientX;// - startOffsetX;
                    //var dropY = ev.clientY;// - startOffsetY;

                    imagemarkupobj = new MarkupObject(11, tool.subtype, 0);

                    imagemarkupobj.scaling = getCurrentPageScale();
                    var offset = getCurrentPageOffset();
                    imagemarkupobj.xoffset = offset.x;
                    imagemarkupobj.yoffset = offset.y;

                    imagemarkupobj.pagenumber = DocObj.getcurPage();

                    imagemarkupobj.viewname = DocObj.getcur3DView();

                    if(imagemarkupobj.viewname != null){
                        imagemarkupobj.bfor3D = true;
                        imagemarkupobj.pageheight = canvasoheight;
                        imagemarkupobj.pagewidth = canvasowidth;
                    }

                    imagemarkupobj.seteditaction(0);
                    var bWithin = MousePosdrwext(mousePos);
                    //var  mousePos.x + dropdata.width/2;

                    
                    var dropdata = JSON.parse(ev.dataTransfer.getData("Text"));
                    /*var xpos = rect.x;
                    var ypos = rect.y;
                    var xwidth = rect.x + rect.w;
                    var yheight = rect.y + rect.h;*/
            
                    var rect = {x : mousePos.x - (dropdata.width/2), y :  mousePos.y - (dropdata.height/2), w : dropdata.width, h: dropdata.height};
                    bWithin = MousePosdrwextRect(rect);

                    var bLoadImage = false;

                    if (bWithin || !bLimMarkupExtent) {
                        
                        imagemarkupobj.x = mousePos.x - (dropdata.width/2);
                        imagemarkupobj.y = mousePos.y - (dropdata.height/2);
                        imagemarkupobj.w = dropdata.width;
                        imagemarkupobj.h = dropdata.height;
                        //var dropdata = ev.dataTransfer.getData("Text");

                        if (dropdata.svgContent) {
                            imagemarkupobj.svgContent = dropdata.svgContent;
                        }
                        bLoadImage = true;

                        var createimage = new Image();

                        imagemarkupobj.markupcdataset(createimage, dropdata.src, (image) => {
                            context.clearRect(0, 0, canvas.width, canvas.height);
                            if (!bMultimarkupadd) {
                                bMarkupcrated = true;
                                //ft 08.08.2018 changed from separate index to direct array length                  
                                //nMarkupcreated = DocObj.nummarkups;
                                //nMarkupcreated = DocObj.markuplist.length;
                            }
                            nMarkupcreated = DocObj.markuplist.length;
    
                            imagemarkupobj.savemetolistDraw();

                            img_update();
                            imagemarkupobj = null;

                            if(bAlwaysMarkupCallback && bWithin){
                                if (RxCore_GUI_Markup != undefined) {
        
                                    var operation = {created : true, modified : false, deleted : false, multi : bMultimarkupadd};
                                    RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[nMarkupcreated], operation);
                                    
                                }
                    
                            }else{
                                if (!bMultimarkupadd && bWithin) {
                                    //need to move to connection object.
                                    //markupcreated();
                                    if (RxCore_GUI_Markup != undefined) {
                                        //RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[index],true);
                                        var operation = {created : true, modified : false, deleted : false};
                                        RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[nMarkupcreated], operation);
                                    }
            
                                }
                            }
        

                        });

                        //createimage.src = ev.dataTransfer.getData("Text");
                        //createimage.src = dropdata.src;
                        //imagemarkupobj.image = createimage;
                        //imagemarkupobj.imageloaded = true;
                        


                    }

                    if(!bWithin){

                        if (Rxcore_GUI_SymbolAddFailed != undefined) {
                            Rxcore_GUI_SymbolAddFailed.markupSaved();
                        }
                        
                    }

                    if (!bLoadImage) {
                        img_update();
                        imagemarkupobj = null;
                    }





                };

                this.mousedown = function (ev) {
                    //var mousePos = getMousePos(canvas, ev);
                    //startOffsetX = mousePos.x;
                    //startOffsetY = mousePos.y;

                };


            };

            tools.interactiveStamp = function(params) {
                var tool = this;
                var canvasLeft = canvas.offsetLeft;
                var canvasTop = canvas.offsetTop;
                var startOffsetX = 0;
                var startOffsetY = 0;
                var imagemarkupobj;
                var curmarkup = DocObj.markuplist.length;
            
                this.name = 'interactiveStamp';
                this.anglelengthsupport = false;
                this.refreshSupport = false;
                this.started = false;
            
                if (params.p1 !== undefined) {
                    this.subtype = params.p1;
                } else {
                    this.subtype = 21;
                }
            
                setCursor(getMarkupCursor('interactiveStamp'));
            
                this.dragover = function(ev) {
                    ev.preventDefault();
                };
            
                this.drop = function(ev) {
                    //debugger
                    ev.preventDefault();
            
                    var mousePos = getMousePos(canvas, ev);
                    imagemarkupobj = new MarkupObject(11, tool.subtype, 0);
            
                    imagemarkupobj.scaling = getCurrentPageScale();
                    var offset = getCurrentPageOffset();
                    imagemarkupobj.xoffset = offset.x;
                    imagemarkupobj.yoffset = offset.y;
            
                    imagemarkupobj.pagenumber = DocObj.getcurPage();
                    imagemarkupobj.viewname = DocObj.getcur3DView();
            
                    if (imagemarkupobj.viewname != null) {
                        imagemarkupobj.bfor3D = true;
                        imagemarkupobj.pageheight = canvasoheight;
                        imagemarkupobj.pagewidth = canvasowidth;
                    }
            
                    imagemarkupobj.seteditaction(0);
                    var bWithin = MousePosdrwext(mousePos);
            
                    var dropdata = JSON.parse(ev.dataTransfer.getData("Text"));
                    var rect = { x: mousePos.x - (dropdata.width / 2), y: mousePos.y - (dropdata.height / 2), w: dropdata.width, h: dropdata.height };
                    bWithin = MousePosdrwextRect(rect);
            
                    if (bWithin || !bLimMarkupExtent) {
                        imagemarkupobj.x = mousePos.x - (dropdata.width / 2);
                        imagemarkupobj.y = mousePos.y - (dropdata.height / 2);
                        imagemarkupobj.w = dropdata.width;
                        imagemarkupobj.h = dropdata.height;
            
                        var createimage = new Image();
                        createimage.src = dropdata.src;
                        imagemarkupobj.image = createimage;
                        imagemarkupobj.imageloaded = true;
                        context.clearRect(0, 0, canvas.width, canvas.height);
            
                        if (!bMultimarkupadd) {
                            bMarkupcrated = true;
                            nMarkupcreated = DocObj.markuplist.length;
                        }
            
                        nMarkupcreated = DocObj.markuplist.length;
                        imagemarkupobj.savemetolistDraw();
            
                        // Insert the SVG into the iframe for editing
                        var iframe = document.getElementById('foxitframe');
                        var iframeDoc = iframe.contentDocument || iframe.contentWindow.document;
                        iframeDoc.open();
                        iframeDoc.write('<html><body>' + createimage.outerHTML + '</body></html>');
                        iframeDoc.close();
            
                        // Make the SVG editable inside the iframe
                        var svgElement = iframeDoc.querySelector('svg');
                        if (svgElement) {
                            svgElement.setAttribute('contenteditable', 'true');
                        }
                    }
            
                    if (!bWithin) {
                        if (Rxcore_GUI_SymbolAddFailed !== undefined) {
                            Rxcore_GUI_SymbolAddFailed.markupSaved();
                        }
                    }
            
                    img_update();
                    imagemarkupobj = null;
            
                    if (bAlwaysMarkupCallback && bWithin) {
                        if (RxCore_GUI_Markup !== undefined) {
                            var operation = { created: true, modified: false, deleted: false, multi: bMultimarkupadd };
                            RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[nMarkupcreated], operation);
                        }
                    } else {
                        if (!bMultimarkupadd && bWithin) {
                            if (RxCore_GUI_Markup !== undefined) {
                                var operation = { created: true, modified: false, deleted: false };
                                RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[nMarkupcreated], operation);
                            }
                        }
                    }
                };
            
                this.mousedown = function(ev) {
                    // Custom logic for mousedown
                };
            };

            
            tools.link = function(params){
                var tool = this;
                var canvasLeft = canvas.offsetLeft;
                var canvasTop = canvas.offsetTop;
                var startOffsetX = 0;
                var startOffsetY = 0;
                var imagemarkupobj;
                //ft 08.08.2018 changed from separate index to direct array length
                //var curmarkup = DocObj.nummarkups;
                var curmarkup = DocObj.markuplist.length;



                this.name = 'link';
                this.anglelengthsupport = false;
                
                this.refreshSupport = false;

                this.started = false;

                if(params.p1 != undefined){
                    this.subtype = params.p1;
                }else{
                    this.subtype = 20;
                }


                setCursor(getMarkupCursor('link'));

                //rxcontainer.ondrop = tool.drop;
                //rxcontainer.ondragover = tool.dragover;




                this.dragover = function(ev){
                    ev.preventDefault();
                };

                this.drop = function(ev){
                    ev.preventDefault();

                    var mousePos = getMousePos(canvas, ev);
                    //var dropX = ev.clientX;// - startOffsetX;
                    //var dropY = ev.clientY;// - startOffsetY;

                    imagemarkupobj = new MarkupObject(11, tool.subtype, 0);

                    imagemarkupobj.scaling = getCurrentPageScale();
                    var offset = getCurrentPageOffset();
                    imagemarkupobj.xoffset = offset.x;
                    imagemarkupobj.yoffset = offset.y;

                    imagemarkupobj.pagenumber = DocObj.getcurPage();

                    imagemarkupobj.viewname = DocObj.getcur3DView();

                    if(imagemarkupobj.viewname != null){
                        imagemarkupobj.bfor3D = true;
                        imagemarkupobj.pageheight = canvasoheight;
                        imagemarkupobj.pagewidth = canvasowidth;
                    }

                    imagemarkupobj.seteditaction(0);
                    var bWithin = MousePosdrwext(mousePos);
                    //var  mousePos.x + dropdata.width/2;

                    
                    var dropdata = JSON.parse(ev.dataTransfer.getData("Text"));
                    /*var xpos = rect.x;
                    var ypos = rect.y;
                    var xwidth = rect.x + rect.w;
                    var yheight = rect.y + rect.h;*/
            
                    var rect = {x : mousePos.x - (dropdata.width/2), y :  mousePos.y - (dropdata.height/2), w : dropdata.width, h: dropdata.height};
                    bWithin = MousePosdrwextRect(rect);

                    if (bWithin || !bLimMarkupExtent) {
                        
                        imagemarkupobj.x = mousePos.x - (dropdata.width/2);
                        imagemarkupobj.y = mousePos.y - (dropdata.height/2);
                        imagemarkupobj.w = dropdata.width;
                        imagemarkupobj.h = dropdata.height;
                        //var dropdata = ev.dataTransfer.getData("Text");

                        var createimage = new Image();
                        //createimage.src = ev.dataTransfer.getData("Text");
                        createimage.src = dropdata.src;
                        imagemarkupobj.image = createimage;
                        imagemarkupobj.imageloaded = true;
                        imagemarkupobj.linkURL = dropdata.text;
                        context.clearRect(0, 0, canvas.width, canvas.height);

                        if (!bMultimarkupadd) {
                            bMarkupcrated = true;
                            //ft 08.08.2018 changed from separate index to direct array length                  
                            //nMarkupcreated = DocObj.nummarkups;
                            //nMarkupcreated = DocObj.markuplist.length;
                        }
                        nMarkupcreated = DocObj.markuplist.length;

                        imagemarkupobj.savemetolistDraw();

                    }

                    if(!bWithin){

                        if (Rxcore_GUI_SymbolAddFailed != undefined) {
                            Rxcore_GUI_SymbolAddFailed.markupSaved();
                        }
                        
                    }

                    img_update();
                    imagemarkupobj = null;

                    if(bAlwaysMarkupCallback && bWithin){
                        if (RxCore_GUI_Markup != undefined) {

                            var operation = {created : true, modified : false, deleted : false, multi : bMultimarkupadd};
                            RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[nMarkupcreated], operation);
                            
                        }
            
                    }else{
                        if (!bMultimarkupadd && bWithin) {
                            //need to move to connection object.
                            //markupcreated();
                            if (RxCore_GUI_Markup != undefined) {
                                //RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[index],true);
                                var operation = {created : true, modified : false, deleted : false};
                                RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[nMarkupcreated], operation);
                            }
    
                        }
                    }



                };

                this.mousedown = function (ev) {
                    //var mousePos = getMousePos(canvas, ev);
                    //startOffsetX = mousePos.x;
                    //startOffsetY = mousePos.y;

                };


            };            
            //Stamp rectangle tool
            tools.rectText = function (){
                var tool = this;

                this.x = 0;
                this.y = 0;
                this.w = 0;
                this.h = 0;
                var x = 0,
                    y = 0,
                    w = 0,
                    h = 0;
                this.name = 'rectText';
                this.anglelengthsupport = false;
                this.refreshSupport = false;

                this.started = false;
                var zoomcolor = "rgba(175, 175, 175, 0.3)";
                context.lineWidth = 2;
                context.strokeStyle = zoomcolor;
                context.fillStyle = zoomcolor;

                
                setCursor(getMarkupCursor('rectText'));
                

                this.pointermove = function (ev) {
                    tool.MSPointerMove(ev);
                };
                this.pointerdown = function (ev) {
                    tool.MSPointerDown(ev);
                };

                this.pointerup = function (ev) {
                    tool.MSPointerUp(ev);
                };


                this.MSPointerDown = function (ev) {
                    ev.preventDefault();
                    ev.stopPropagation();

                    
                    var mousePos = getMousePos(canvas, ev);

                    var bWithin = MousePosdrwext(mousePos);
                    if (!bWithin && bLimMarkupExtent) {
                        tool.started = false;
                        if(DocObj){
                            DocObj.bMarkupchanged = false;
                        }

                        return;

                    }


                    if (ev.button != 2) {
                        tool.started = true;
                        tool.x0 = mousePos.x;
                        tool.y0 = mousePos.y;
                    }

                };
                this.mousedown = function (ev) {
                    ev.preventDefault();
                    ev.stopPropagation();

                    
                    var mousePos = getMousePos(canvas, ev);

                    var bWithin = MousePosdrwext(mousePos);
                    if (!bWithin && bLimMarkupExtent) {
                        tool.started = false;
                        if(DocObj){
                            DocObj.bMarkupchanged = false;
                        }

                        
                        return;

                    }

                    if (ev.button != 2) {
                        tool.started = true;
                        tool.x0 = mousePos.x;
                        tool.y0 = mousePos.y;
                    }
                };

                this.touchstart = function (ev) {
                    ev.preventDefault();
                    tool.started = true;
                    var touchPos = getTouchPos(canvas, ev, 0);
                    var bWithin = MousePosdrwext(touchPos);
                    if (!bWithin && bLimMarkupExtent) {
                        tool.started = false;
                        if(DocObj){
                            DocObj.bMarkupchanged = false;
                        }

                        
                        return;

                    }


                    tool.x0 = touchPos.x;
                    tool.y0 = touchPos.y;

                };

                this.MSPointerMove = function (ev) {
                    ev.preventDefault();
                    ev.stopPropagation();
                    
                    

                    if (!tool.started) {
                        return;
                    }
                    var mousePos = getMousePos(canvas, ev);

                    var bWithin = MousePosdrwext(mousePos);
                    if (bWithin || !bLimMarkupExtent) {
                        x = Math.min(mousePos.x, tool.x0);
                        y = Math.min(mousePos.y, tool.y0);
                        w = Math.abs(mousePos.x - tool.x0);
                        h = Math.abs(mousePos.y - tool.y0);

                    } else {
                        x = Math.min(x, tool.x0);
                        y = Math.min(y, tool.y0);
                        w = Math.abs(x - tool.x0);
                        h = Math.abs(y - tool.y0);
                    }
                    /*x = Math.min(mousePos.x, tool.x0);
                    y = Math.min(mousePos.y, tool.y0);
                    w = Math.abs(mousePos.x - tool.x0);
                    h = Math.abs(mousePos.y - tool.y0);*/

                    if (!w || !h) {
                        return;
                    }

                    zoomcolor = "rgba(175, 175, 175, 0.3)";
                    context.clearRect(0, 0, canvas.width, canvas.height);
                    context.lineWidth = 1;
                    
                    context.strokeStyle = zoomcolor;
                    context.fillStyle = zoomcolor;
    
                    context.fillRect(x, y, w, h);

                };
                this.mousemove = function (ev) {
                    ev.preventDefault();
                    ev.stopPropagation();
                    

                    if (!tool.started) {
                        return;
                    }
                    var mousePos = getMousePos(canvas, ev);
                    var bWithin = MousePosdrwext(mousePos);
                    if (bWithin || !bLimMarkupExtent) {
                        x = Math.min(mousePos.x, tool.x0);
                        y = Math.min(mousePos.y, tool.y0);
                        w = Math.abs(mousePos.x - tool.x0);
                        h = Math.abs(mousePos.y - tool.y0);

                    } else {
                        x = Math.min(x, tool.x0);
                        y = Math.min(y, tool.y0);
                        w = Math.abs(x - tool.x0);
                        h = Math.abs(y - tool.y0);
                    }


                   /* x = Math.min(mousePos.x, tool.x0);
                    y = Math.min(mousePos.y, tool.y0);
                    w = Math.abs(mousePos.x - tool.x0);
                    h = Math.abs(mousePos.y - tool.y0);*/

                    if (!w || !h) {
                        return;
                    }

                    zoomcolor = "rgba(175, 175, 175, 0.3)";
                    context.clearRect(0, 0, canvas.width, canvas.height);
                    context.lineWidth = 1;
                    context.strokeStyle = zoomcolor;
                    context.fillStyle = zoomcolor;
    
                    context.fillRect(x, y, w, h);
                };

                this.touchmove = function (ev) {
                    ev.preventDefault();
                    if (!tool.started) {
                        return;
                    }
                    var touchPos = getTouchPos(canvas, ev, 0);
                    var bWithin = MousePosdrwext(touchPos);
                    if (bWithin || !bLimMarkupExtent) {
                        x = Math.min(touchPos.x, tool.x0);
                        y = Math.min(touchPos.y, tool.y0);
                        w = Math.abs(touchPos.x - tool.x0);
                        h = Math.abs(touchPos.y - tool.y0);

                    } else {
                        x = Math.min(x, tool.x0);
                        y = Math.min(y, tool.y0);
                        w = Math.abs(x - tool.x0);
                        h = Math.abs(y - tool.y0);
                    }



                    /*x = Math.min(touchPos.x, tool.x0);
                    y = Math.min(touchPos.y, tool.y0);
                    w = Math.abs(touchPos.x - tool.x0);
                    h = Math.abs(touchPos.y - tool.y0);*/


                    if (!w || !h) {
                        return;
                    }

                    context.clearRect(0, 0, canvas.width, canvas.height);
                    
                    zoomcolor = "rgba(175, 175, 175, 0.3)";
                    context.lineWidth = 1;
                    context.strokeStyle = zoomcolor;
                    context.fillStyle = zoomcolor;
    
                    context.fillRect(x, y, w, h);

                };

                this.MSPointerUp = function (ev) {
                    ev.preventDefault();

                    

                    if (ev.button != 2) {
                        if (tool.started) {
                            //tool.mousemove(ev);
                            tool.started = false;
                            if (!w || !h) {
                                return;
                            }
                            //DocObj.getCurrentpage().zoom_update(x, y, w, h);
                            textRect(x, y, w, h, null);
                            tool.x = x;
                            tool.y = y;
                            tool.w = w;
                            tool.h = h;

                            context.clearRect(0, 0, canvas.width, canvas.height);
                        }

                    }

                };
                this.mouseup = function (ev) {
                    ev.preventDefault();

                    

                    if (ev.button != 2) {
                        if (tool.started) {
                            //tool.mousemove(ev);
                            tool.started = false;
                            if (!w || !h) {

                                if(bMultimarkupadd){
                                    terminatecurrentText();
                                }


                                return;
                            }
                            //DocObj.getCurrentpage().zoom_update(x, y, w, h);
                            textRect(x, y, w, h, null);
                            //tool.x = x;
                            //tool.y = y;

                            tool.w = 0;
                            tool.h = 0;
                            w = 0;
                            h = 0;

                            //tool.w = w;
                            //tool.h = h;

                            context.clearRect(0, 0, canvas.width, canvas.height);
                        }

                    }
                };

                this.touchcancel = function(ev){
                    ev.preventDefault();
                    if (tool.started) {
                        tool.started = false;
                        if (!w || !h) {
                            return;
                        }
                        textRect(x, y, w, h, null);
                        tool.x = x;
                        tool.y = y;
                        tool.w = w;
                        tool.h = h;

                        //DocObj.getCurrentpage().zoom_update(x, y, w, h);
                        //tool.touchmove(ev);

                        context.clearRect(0, 0, canvas.width, canvas.height);
                    }
                };

                this.touchend = function (ev) {
                    ev.preventDefault();
                    if (tool.started) {
                        tool.started = false;
                        if (!w || !h) {
                            return;
                        }
                        textRect(x, y, w, h, null);
                        tool.x = x;
                        tool.y = y;
                        tool.w = w;
                        tool.h = h;

                        //DocObj.getCurrentpage().zoom_update(x, y, w, h);
                        //tool.touchmove(ev);

                        context.clearRect(0, 0, canvas.width, canvas.height);
                    }
                };
            };


                //Stamp rectangle tool
            tools.rectStamp = function (params){
                var tool = this;
                var x = 0,
                    y = 0,
                    w = 0,
                    h = 0;
                this.name = 'rectStamp';
                this.anglelengthsupport = false;
                this.refreshSupport = false;

                this.started = false;
                var zoomcolor = "rgba(175, 175, 175, 0.3)";
                context.lineWidth = 2;
                context.strokeStyle = zoomcolor;
                context.fillStyle = zoomcolor;
                var Text = params.p1;

                setCursor(getMarkupCursor('rectStamp'));


                this.pointermove = function (ev) {
                    tool.MSPointerMove(ev);
                };
                this.pointerdown = function (ev) {
                    tool.MSPointerDown(ev);
                };

                this.pointerup = function (ev) {
                    tool.MSPointerUp(ev);
                };


                this.MSPointerDown = function (ev) {
                    ev.preventDefault();
                    ev.stopPropagation();

                    
                    
                    var mousePos = getMousePos(canvas, ev);

                    var bWithin = MousePosdrwext(mousePos);
                    if (!bWithin && bLimMarkupExtent) {
                        tool.started = false;
                        if(DocObj){
                            DocObj.bMarkupchanged = false;
                        }

                        
                        return;

                    }


                    if (ev.button != 2) {
                        tool.started = true;
                        tool.x0 = mousePos.x;
                        tool.y0 = mousePos.y;
                    }

                };
                this.mousedown = function (ev) {
                    ev.preventDefault();
                    ev.stopPropagation();

                    
                    var mousePos = getMousePos(canvas, ev);
                    var bWithin = MousePosdrwext(mousePos);
                    if (!bWithin && bLimMarkupExtent) {
                        tool.started = false;
                        if(DocObj){
                            DocObj.bMarkupchanged = false;
                        }
                        
                        return;

                    }

                    if (ev.button != 2) {
                        tool.started = true;
                        tool.x0 = mousePos.x;
                        tool.y0 = mousePos.y;
                    }
                };

                this.touchstart = function (ev) {
                    ev.preventDefault();
                    tool.started = true;
                    var touchPos = getTouchPos(canvas, ev, 0);

                    var bWithin = MousePosdrwext(touchPos);
                    if (!bWithin && bLimMarkupExtent) {
                        tool.started = false;
                        if(DocObj){
                            DocObj.bMarkupchanged = false;
                        }

                        return;

                    }

                    tool.x0 = touchPos.x;
                    tool.y0 = touchPos.y;

                };

                this.MSPointerMove = function (ev) {
                    ev.preventDefault();
                    ev.stopPropagation();
                    

                    if (!tool.started) {
                        return;
                    }
                    var mousePos = getMousePos(canvas, ev);
                    var bWithin = MousePosdrwext(mousePos);
                    if (bWithin || !bLimMarkupExtent) {
                        x = Math.min(mousePos.x, tool.x0);
                        y = Math.min(mousePos.y, tool.y0);
                        w = Math.abs(mousePos.x - tool.x0);
                        h = Math.abs(mousePos.y - tool.y0);

                    } else {
                        x = Math.min(x, tool.x0);
                        y = Math.min(y, tool.y0);
                        w = Math.abs(x - tool.x0);
                        h = Math.abs(y - tool.y0);
                    }


                    /*x = Math.min(mousePos.x, tool.x0);
                    y = Math.min(mousePos.y, tool.y0);
                    w = Math.abs(mousePos.x - tool.x0);
                    h = Math.abs(mousePos.y - tool.y0);*/

                    if (!w || !h) {
                        return;
                    }


                    context.clearRect(0, 0, canvas.width, canvas.height);
                    context.lineWidth = 1;
                    context.strokeStyle = zoomcolor;
                    context.fillRect(x, y, w, h);

                };
                this.mousemove = function (ev) {
                    ev.preventDefault();
                    ev.stopPropagation();
                    

                    if (!tool.started) {
                        return;
                    }
                    var mousePos = getMousePos(canvas, ev);
                    var bWithin = MousePosdrwext(mousePos);
                    if (bWithin || !bLimMarkupExtent) {
                        x = Math.min(mousePos.x, tool.x0);
                        y = Math.min(mousePos.y, tool.y0);
                        w = Math.abs(mousePos.x - tool.x0);
                        h = Math.abs(mousePos.y - tool.y0);

                    } else {
                        x = Math.min(x, tool.x0);
                        y = Math.min(y, tool.y0);
                        w = Math.abs(x - tool.x0);
                        h = Math.abs(y - tool.y0);
                    }
                    /*x = Math.min(mousePos.x, tool.x0);
                    y = Math.min(mousePos.y, tool.y0);
                    w = Math.abs(mousePos.x - tool.x0);
                    h = Math.abs(mousePos.y - tool.y0);*/

                    if (!w || !h) {
                        return;
                    }


                    context.clearRect(0, 0, canvas.width, canvas.height);
                    context.lineWidth = 1;
                    context.strokeStyle = zoomcolor;
                    context.fillRect(x, y, w, h);
                };

                this.touchmove = function (ev) {
                    ev.preventDefault();
                    if (!tool.started) {
                        return;
                    }
                    var touchPos = getTouchPos(canvas, ev, 0);
                    var bWithin = MousePosdrwext(touchPos);
                    if (bWithin || !bLimMarkupExtent) {
                        x = Math.min(touchPos.x, tool.x0);
                        y = Math.min(touchPos.y, tool.y0);
                        w = Math.abs(touchPos.x - tool.x0);
                        h = Math.abs(touchPos.y - tool.y0);

                    } else {
                        x = Math.min(x, tool.x0);
                        y = Math.min(y, tool.y0);
                        w = Math.abs(x - tool.x0);
                        h = Math.abs(y - tool.y0);
                    }

                    /*x = Math.min(touchPos.x, tool.x0);
                    y = Math.min(touchPos.y, tool.y0);
                    w = Math.abs(touchPos.x - tool.x0);
                    h = Math.abs(touchPos.y - tool.y0);*/


                    if (!w || !h) {
                        return;
                    }

                    context.clearRect(0, 0, canvas.width, canvas.height);

                    context.lineWidth = 1;
                    context.strokeStyle = zoomcolor;
                    context.fillRect(x, y, w, h);

                };

                this.MSPointerUp = function (ev) {
                    ev.preventDefault();

                    

                    if (ev.button != 2) {
                        if (tool.started) {
                            //tool.mousemove(ev);
                            tool.started = false;
                            if (!w || !h) {
                                return;
                            }
                            //DocObj.getCurrentpage().zoom_update(x, y, w, h);
                            stampRect(x, y, w, h, Text);
                            context.clearRect(0, 0, canvas.width, canvas.height);
                        }

                    }

                };
                this.mouseup = function (ev) {
                    ev.preventDefault();


                    if (ev.button != 2) {
                        if (tool.started) {
                            //tool.mousemove(ev);
                            tool.started = false;
                            if (!w || !h) {
                                return;
                            }
                            //DocObj.getCurrentpage().zoom_update(x, y, w, h);
                            stampRect(x, y, w, h, Text);
                            context.clearRect(0, 0, canvas.width, canvas.height);
                        }

                    }
                };
                
                this.touchcancel = function(ev){
                    ev.preventDefault();
                    if (tool.started) {
                        tool.started = false;
                        if (!w || !h) {
                            return;
                        }
                        stampRect(x, y, w, h, Text);
                        //DocObj.getCurrentpage().zoom_update(x, y, w, h);
                        //tool.touchmove(ev);

                        context.clearRect(0, 0, canvas.width, canvas.height);
                    }
                };

                this.touchend = function (ev) {
                    ev.preventDefault();
                    if (tool.started) {
                        tool.started = false;
                        if (!w || !h) {
                            return;
                        }
                        stampRect(x, y, w, h, Text);
                        //DocObj.getCurrentpage().zoom_update(x, y, w, h);
                        //tool.touchmove(ev);

                        context.clearRect(0, 0, canvas.width, canvas.height);
                    }
                };

            };
            // The zoom tool.
            tools.zoomwindow = function (params) {

                var tool = this;
                var x = 0,
                    y = 0,
                    w = 0,
                    h = 0;
                
                var bdocrect = params.p1;
                var bcliprect = params.p2;

                this.name = 'zoomwindow';
                this.anglelengthsupport = false;
                this.refreshSupport = false;

                this.started = false;

                this.curcliprect = {x:0,y:0,w:0,h:0};

                var zoomcolor = "rgba(175, 175, 175, 0.3)";
                context.lineWidth = 2;
                context.strokeStyle = zoomcolor;
                context.fillStyle = zoomcolor;

                
                setCursor(getMarkupCursor('zoomwindow'));

                this.pdfcliprect = function(bitmap){

                    var tdv = new Uint8ClampedArray(bitmap.buffer);
                    var imageData = new ImageData(tdv, bitmap.width, bitmap.height);

                    //scope.imagecnv.width = bitmap.width;
                    //scope.imagecnv.height = bitmap.height;

                    clipboardcnv.width = bitmap.width;
                    clipboardcnv.height = bitmap.height;


                    //scope.setinvert(imageData.data);

                    //scope.imagectx.putImageData(imageData, 0, 0);
                    clipboardctx.putImageData(imageData, 0, 0);

                    DocObj.getCurrentpage().drawmarkupcliprect(tool.curcliprect,clipboardctx);

                    copyToClipboard(clipboardcnv);

                };


                this.zoomUpdate = function(x,y,w,h){
                    //tool.zoomUpdate

                    if (bdocrect){

                        var docrect = DocObj.getCurrentpage().screentoDocRect({x: x, y: y, w: w, h :h});
                    
                        if (RxCore_GUI_PrintRect != undefined){
                            RxCore_GUI_PrintRect.zoomUpdate(docrect);
                        }
    
                    }else if(bcliprect){
                        
                        var clipimage = DocObj.getCurrentpage().getClipRect({x: x, y: y, w: w, h :h}, tool.pdfcliprect);

                        tool.curcliprect.x = x;
                        tool.curcliprect.y = y;
                        tool.curcliprect.w = w;
                        tool.curcliprect.h = h;
                        

                        if(clipimage != undefined){
                            //clipboardcnv, clipboardctx

                            clipboardcnv.width = clipimage.width;
                            clipboardcnv.height = clipimage.height;
                            //clipboardctx.scale(-1, 1);
                            clipboardctx.putImageData(clipimage, 0, 0);


                            //this.drawmarkupcliprect = function (rect, ctx) {

                            //var pgscale = DocObj.getCurrentpage().getdscale();
                            //var pagenumber = DocObj.getCurrentpage().pagenumber;
                            //var rotation = DocObj.getCurrentpage().getpageRotation();

                            DocObj.getCurrentpage().drawmarkupcliprect({x: x, y: y, w: w, h :h},clipboardctx);

                            //drawmarkupMagnify(clipboardctx, pagenumber, x, y, pgscale, rotation);

                            //clipboardcnv.toBlob(blob => navigator.clipboard.write([new ClipboardItem({'image/png': blob})]));

                            copyToClipboard(clipboardcnv);

                            /*copyToClipboard(clipboardcnv).then(function(){
                                //console.log('OK');
            
                            }).catch(function(error){
                                console.log('copy to clipboard failed');
                            });*/

                            /*clipboardcnv.toBlob(function(blob) { 

                                //const item = new ClipboardItem({ "image/png": blob });
                                
                                navigator.clipboard.write([new ClipboardItem({ "image/png": blob })]); 

                            });*/
    
                        }


                    
                        /*if (RxCore_GUI_ClipRect != undefined){
                            RxCore_GUI_ClipRect.zoomUpdate(clipimage);
                        }*/
    
                    }else{
                        if (documentcompare){
                            CompareObj.zoom_update(x, y, w, h);
    
                        }else{
                            DocObj.getCurrentpage().zoom_update(x, y, w, h);    
                        }

                    }
                    
                    context.clearRect(0, 0, canvas.width, canvas.height);
                };

                this.pointermove = function (ev) {
                    tool.MSPointerMove(ev);
                };
                this.pointerdown = function (ev) {
                    tool.MSPointerDown(ev);
                };

                this.pointerup = function (ev) {
                    tool.MSPointerUp(ev);
                };


                this.MSPointerDown = function (ev) {
                    ev.preventDefault();
                    ev.stopPropagation();

                    var mousePos = getMousePos(canvas, ev);
                    if (ev.button != 2) {
                        tool.started = true;
                        tool.x0 = mousePos.x;
                        tool.y0 = mousePos.y;
                    }

                };
                this.mousedown = function (ev) {
                    ev.preventDefault();
                    ev.stopPropagation();

                    
                    var mousePos = getMousePos(canvas, ev);
                    if (ev.button != 2) {
                        tool.started = true;
                        tool.x0 = mousePos.x;
                        tool.y0 = mousePos.y;
                    }
                };

                this.touchstart = function (ev) {
                    ev.preventDefault();
                    tool.started = true;
                    var touchPos = getTouchPos(canvas, ev, 0);
                    tool.x0 = touchPos.x;
                    tool.y0 = touchPos.y;

                };

                this.MSPointerMove = function (ev) {
                    ev.preventDefault();
                    ev.stopPropagation();
                    

                    if (!tool.started) {
                        return;
                    }
                    var mousePos = getMousePos(canvas, ev);
                    x = Math.min(mousePos.x, tool.x0);
                    y = Math.min(mousePos.y, tool.y0);
                    w = Math.abs(mousePos.x - tool.x0);
                    h = Math.abs(mousePos.y - tool.y0);

                    if (!w || !h) {
                        return;
                    }


                    context.clearRect(0, 0, canvas.width, canvas.height);
                    context.lineWidth = 1;
                    //context.strokeStyle = zoomcolor;

                    context.strokeStyle = zoomcolor;
                    context.fillStyle = zoomcolor;
    
                    context.fillRect(x, y, w, h);

                };
                this.mousemove = function (ev) {
                    ev.preventDefault();
                    ev.stopPropagation();
                    

                    if (!tool.started) {
                        return;
                    }
                    var mousePos = getMousePos(canvas, ev);
                    x = Math.min(mousePos.x, tool.x0);
                    y = Math.min(mousePos.y, tool.y0);
                    w = Math.abs(mousePos.x - tool.x0);
                    h = Math.abs(mousePos.y - tool.y0);

                    if (!w || !h) {
                        return;
                    }


                    context.clearRect(0, 0, canvas.width, canvas.height);
                    context.lineWidth = 1;

                    context.strokeStyle = zoomcolor;
                    context.fillStyle = zoomcolor;
    
                    context.fillRect(x, y, w, h);
                };

                this.touchmove = function (ev) {
                    ev.preventDefault();
                    if (!tool.started) {
                        return;
                    }
                    var touchPos = getTouchPos(canvas, ev, 0);
                    x = Math.min(touchPos.x, tool.x0);
                    y = Math.min(touchPos.y, tool.y0);
                    w = Math.abs(touchPos.x - tool.x0);
                    h = Math.abs(touchPos.y - tool.y0);


                    if (!w || !h) {
                        return;
                    }

                    context.clearRect(0, 0, canvas.width, canvas.height);

                    context.lineWidth = 1;

                    context.strokeStyle = zoomcolor;
                    context.fillStyle = zoomcolor;
    

                    context.fillRect(x, y, w, h);

                };

                this.MSPointerUp = function (ev) {
                    ev.preventDefault();

                    

                    if (ev.button != 2) {
                        if (tool.started) {
                            //tool.mousemove(ev);
                            tool.started = false;
                            if (!w || !h) {
                                return;
                            }
                            //DocObj.getCurrentpage().zoom_update(x, y, w, h);
                            tool.zoomUpdate(x, y, w, h);
                            //context.clearRect(0, 0, canvas.width, canvas.height);
                            //RxCore_default();
                        }

                    }


                };
                this.mouseup = function (ev) {
                    ev.preventDefault();

                    

                    if (ev.button != 2) {
                        if (tool.started) {
                            //tool.mousemove(ev);
                            tool.started = false;
                            if (!w || !h) {
                                return;
                            }
                            
                            tool.zoomUpdate(x, y, w, h);
                            //DocObj.getCurrentpage().zoom_update(x, y, w, h);
                            //context.clearRect(0, 0, canvas.width, canvas.height);
                            //RxCore_default();
                        }

                    }
                };

                this.mouseout = function(ev){
                    ev.preventDefault();
                    

                    if (tool.started) {
                    
                       tool.started = false;
                       if (!w || !h) {
                            return;
                       }
                       
                       tool.zoomUpdate(x, y, w, h);

                       //DocObj.getCurrentpage().zoom_update(x, y, w, h);
                       //context.clearRect(0, 0, canvas.width, canvas.height);
                    
                    }

                };

                this.touchcancel = function(ev){
                    ev.preventDefault();
                    if (tool.started) {
                        tool.started = false;
                        if (!w || !h) {
                            return;
                        }
                        
                        tool.zoomUpdate(x, y, w, h);

                        //DocObj.getCurrentpage().zoom_update(x, y, w, h);
                        //tool.touchmove(ev);

                        //context.clearRect(0, 0, canvas.width, canvas.height);
                        //RxCore_default();
                    }

                };

                this.touchend = function (ev) {
                    ev.preventDefault();
                    if (tool.started) {
                        tool.started = false;
                        if (!w || !h) {
                            return;
                        }
                        tool.zoomUpdate(x, y, w, h);

                        //DocObj.getCurrentpage().zoom_update(x, y, w, h);
                        //tool.touchmove(ev);

                        //context.clearRect(0, 0, canvas.width, canvas.height);
                        //RxCore_default();
                    }
                };

            };


            tools.pickPolygon = function (params) {

                var tool = this;
                var x = 0,
                    y = 0,
                    w = 0,
                    h = 0;
                var prevx = 0,
                    prevy = 0;
                this.started = false;
                var multi = params.p1;
                this.multiselect = multi;
                this.createSpaceMarkup = params.p2;
                this.allowVectorSelect = params.p3;
                this.useVectorInfoSelect = params.p4;
                this.useBlockSelect = params.p5;

                this.name = 'PickPolygon';
                this.anglelengthsupport = false;
                this.refreshSupport = false;
                //this.curmousepos = {x:0,y:0};
                this.curmousepos = {x:0,y:0,xus:0,yus:0};

                this.curkey = null;

                setCursor(getMarkupCursor('PickPolygon'));

                this.findvectorentityhover = function(mousePos){

                    if(benableblockhover){
                        var polyfound = DocObj.getCurrentpage().getvectorentityhover(mousePos.x, mousePos.y);
                    }
                    
                };

                this.findvectorentity = function(mousePos){

                    var polyfound = DocObj.getCurrentpage().getvectorentity(mousePos.x, mousePos.y);

                };

                this.findBlockhover = function(mousePos){

                    var polyfound = DocObj.getCurrentpage().getblockinserthover(mousePos.x, mousePos.y);

                };

                this.findBlock = function(mousePos){

                    var polyfound = DocObj.getCurrentpage().getblockinsert(mousePos.x, mousePos.y);

                };

                

                this.findspacehover = function(mousePos){

                    if(tool.useVectorInfoSelect){
                        tool.findvectorentityhover(mousePos);
                        return;
                    }

                    if(tool.useBlockSelect){
                        tool.findBlockhover(mousePos);

                        return;
                    }
                    
                    if(tool.allowVectorSelect && benableblockhover){
                        var polyfound = DocObj.getCurrentpage().getpolygonhover(mousePos.x, mousePos.y);
                    }



                };

                this.findspace = function(mousePos){

                    if(tool.useVectorInfoSelect){
                        tool.findvectorentity(mousePos);
                        return;
                    }

                    if(tool.useBlockSelect){
                        tool.findBlock(mousePos);

                        return;
                    }


                    var spacefound = findspacemarkup(mousePos.x, mousePos.y, tool.multiselect);
                    
                    if(!spacefound.found){
                        RxCore_animateLineStyle(false);
                        
                    }else{
                        if(!tool.multiselect){
                            DocObj.getCurrentpage().unSelectAllBlocks(false);
                        }
                        
                        RxCore_animateLineStyle(true);
                    }

                    if(!spacefound.within && tool.allowVectorSelect){
                        var polyfound = DocObj.getCurrentpage().getpolygon(mousePos.x, mousePos.y,tool.multiselect);
                    }
                    return (polyfound || spacefound);

                };

                this.createSpace = function(mousePos){
                    //var polypoints = DocObj.getCurrentpage().getpolygonPoints(mousePos.x, mousePos.y);

                    var polygonpath = DocObj.getCurrentpage().getpolygonPath(mousePos.x, mousePos.y);

                    if(polygonpath == null){
                        return;
                    }
                    
                    if(polygonpath.points.length > 0){
                        if(DocObj.getCurrentpage().VectorPageObj){
                            
                            var scalefactor = DocObj.getCurrentpage().dscalevector;
                            var xoffset = DocObj.getCurrentpage().dxvector;
                            var yoffset = DocObj.getCurrentpage().dyvector;
                            var mediax = DocObj.getCurrentpage().VectorPageObj.x;
                            var mediay = DocObj.getCurrentpage().VectorPageObj.y;
                            var mediah = DocObj.getCurrentpage().VectorPageObj.h;

                            //RxCore_createSpacemarkup(polypoints, scalefactor, xoffset, yoffset, mediax, mediay, mediah);
                            RxCore_createSpacemarkupPath(polygonpath, scalefactor, xoffset, yoffset, mediax, mediay, mediah);
                        }
                        

                    }
                };

                this.set3DModelPos = function(mousePos){
                    //add code for sending callback data about clicked position here.
                    //if page is3dnav page.
                    var curpage = DocObj.getCurrentpage();
                    if (curpage.bis3Dnavigator){
                        if(RxCore_GUI_3DNavPosition != undefined){
                            
                            //curpage.setbirdseyecamera(null, null,null);

                            var navigationpos = {docindex : DocObj.fileindex, pageindex : curpage.pagenumber, cameraPos : mousePos};

                            RxCore_GUI_3DNavPosition.navClick(navigationpos);
                        }
                    }



                };


                this.panlimit = function(){
                    var pagedim = DocObj.getCurrentpage().getpagedim();

                    if ((pagedim.w / pagedim.h) < (canvasowidth / canvasoheight)){
                        if ((pagedim.y + pagedim.h) < (canvasoheight - nPanlimit)){
                            //RxCore_PagePos(pagedim.x, -nPanlimit);
                            var ypos = (canvasoheight - nPanlimit) - pagedim.h;
                            RxCore_PagePos(pagedim.x, pagedim.y);
                            //reset position to -15
                        }
    
                        if (pagedim.y > nPanlimit){
                            //reset position to 15
                            RxCore_PagePos(pagedim.x, nPanlimit);
                        }

                    }else{
                        if ((pagedim.x + pagedim.w) < (canvasowidth - nPanlimit)){
                            RxCore_PagePos(-nPanlimit, pagedim.y);
                            //reset position to -15
                        }
    
                        if (pagedim.x > nPanlimit){
                            //reset position to 15
                            RxCore_PagePos(nPanlimit, pagedim.y);
                        }                    
    
                    }


                    /*if(pagedim.w >= canvasowidth){
                    }*/

                };

                this.belowlimitExtent = function(zoomscalefactor){
                    var belowlimit = true;
                    

                    var pagedim = DocObj.getCurrentpage().getpagedim();

                    var wlimit = canvasowidth - 60;
                    var hlimit = canvasoheight - 60;

                    if((pagedim.w / zoomscalefactor) < wlimit && (pagedim.h / zoomscalefactor) < hlimit){
                        belowlimit = false;
                        //scalechanged = false;
                        //DocObj.getCurrentpage().zoomall();
                    }

                    var pagerect = DocObj.getCurrentpage().ZoomOutTest(zoomscalefactor, false, tool.curmousepos);
                    
                    var heightlimit = canvasoheight - (nPanlimit*2);
                    var widthlimit = canvasowidth - (nPanlimit*2);
                    //var toplimit = 15;
                    var bottomlimit = ((pagerect.y + pagerect.h) <= heightlimit);
                    var toplimit = (pagerect.y >= nPanlimit);

                    // need to return these to reset the top and bottom positions.scroll|| bottomlimit || toplimit
                    if ((pagerect.w / pagerect.h) < (canvasowidth / canvasoheight)){
                        if(pagerect.h <= heightlimit){
                            belowlimit = false;
                        }
    
                    }else{
                        if(pagerect.w <= widthlimit){
                            belowlimit = false;
                        }

                    }

                    

                    if(!bLimitZoomOut){
                        belowlimit = true;
                    }

                    return belowlimit;
                };

                this.scrollhandling = function (movey) {
                    if (!documentopen || DocObj.getCurrentpage().usevector3Dxml) {
                        return;
                    }

                    if (movey < 0) {

                        if (DocObj.getCurrentpage().usepdfjs && DocObj.pages.length > 1 && !DocObj.pagelocked) {

                            //DocObj.getCurrentpage().pan_update(0,-movey);
                            /*if (DocObj.currentpage == DocObj.pages.length - 1) {
                                if (DocObj.getCurrentpage().endy <= canvas.height) {
                                    movey = 0;
                                }
                            }*/
                            DocObj.getCurrentpage().pan_update(0, -movey);
                            

                            if (DocObj.getCurrentpage().pagenumber == DocObj.pages.length - 1 && DocObj.getCurrentpage().getpageRotation() == 0) {
                                /*while (DocObj.getCurrentpage().endy < canvas.height) {
                                    DocObj.getCurrentpage().pan_update(0, -1);
                                    
                                }*/
    
                            }
    

                        } else {
                            if(bUseScrollKey){
                                if (tool.scrollkeyOn){
                                    if (!DocObj.getCurrentpage().usedincomposite || bScrollZoomOverlay){
                                        
                                        if(belowlimitExtentEx(1.1, tool.curmousepos)){

                                            DocObj.getCurrentpage().ZoomOut(1.1, false, false, tool.curmousepos);
                                            if(bRestrictPan){
                                                tool.panlimit();
                                            }    
                                            
                                        }else{
                                            RxCore_foxitForceRedraw();
                                        }
                                        
                                        //tool.limitExtent(1.1);
                                    }
                                    
                                    //DocObj.getCurrentpage().ZoomOutmouse(1.1, tool.curmousepos);
                                }

                            }else{
                                if (!DocObj.getCurrentpage().usedincomposite || bScrollZoomOverlay){

                                    if(belowlimitExtentEx(1.1, tool.curmousepos)){
                                        DocObj.getCurrentpage().ZoomOut(1.1, false, false, tool.curmousepos);
                                        if(bRestrictPan){
                                            tool.panlimit();
                                        }
                                        
                                         
                                    }else{
                                        RxCore_foxitForceRedraw();
                                    }
                                        
                                    
                                    
                                    //tool.limitExtent(1.1);

                                }
                                
                                //DocObj.getCurrentpage().ZoomOutmouse(1.1, tool.curmousepos);
                            }

                        }


                    } else {
                        if (DocObj.getCurrentpage().usepdfjs && DocObj.pages.length > 1 && !DocObj.pagelocked) {

                            if (DocObj.currentpage == 0) {
                                /*if (DocObj.getCurrentpage().starty >= 0) {
                                    movey = 0;
                                }*/

                            }
                            DocObj.getCurrentpage().pan_update(0, -movey);
                            

                            if (DocObj.getCurrentpage().pagenumber == 0 && DocObj.getCurrentpage().getpageRotation() == 0) {
                                /*while (DocObj.getCurrentpage().starty > 0) {
                                    DocObj.getCurrentpage().pan_update(0, 1);
                                    
                                }*/
    
                            }
    

                        } else {
                            if(bUseScrollKey){
                                if (tool.scrollkeyOn){
                                    if (!DocObj.getCurrentpage().usedincomposite || bScrollZoomOverlay){

                                        DocObj.getCurrentpage().ZoomIn(1.1, false, false, tool.curmousepos);
                                        //tool.limitExtent(1.1);
                                    }
                                    
                                    //DocObj.getCurrentpage().ZoomInMouse(1.1, tool.curmousepos);
                                }
                            }else{
                                if (!DocObj.getCurrentpage().usedincomposite || bScrollZoomOverlay){
                                    DocObj.getCurrentpage().ZoomIn(1.1, false, false, tool.curmousepos);
                                    //tool.limitExtent(1.1);
                                }
                                
                                //DocObj.getCurrentpage().ZoomInMouse(1.1, tool.curmousepos);
                            }

                        }

                    }
                    
                };


                this.mousewheel = function(ev){
                    tool.wheel(ev);
                };


                this.wheel = function (ev) {

                    var delta = 0;
                    if (ev.wheelDelta !== undefined) { // WebKit / Opera / Explorer 9

                        delta = ev.wheelDelta;

                    } else if (ev.deltaY !== undefined) { // Firefox
                        if(bIsMSIE ){
                            delta = -ev.deltaY;
                        }else{
                            delta = -ev.deltaY*50;

                        }


                    }

                    tool.scrollhandling(delta);

                };                


                this.pointermove = function (ev) {
                    tool.MSPointerMove(ev);
                };
                this.pointerdown = function (ev) {
                    tool.MSPointerDown(ev);
                };

                this.pointerup = function (ev) {
                    tool.MSPointerUp(ev);
                };

                this.MSPointerDown = function (ev) {
                    ev.preventDefault();
                    ev.stopPropagation();

                    var found = false;

                    
                    var mousePos = getMousePos(canvas, ev);

                    if (RxCore_GUI_inputEvent != undefined){
                        
                        var inputobj = {
                            mousepos : mousePos,
                            mousebutton : ev.button,
                            dblclick : false,
                            key : tool.curkey,
                            tool : tool.name
                        };
                        
                        RxCore_GUI_inputEvent.setInput(inputobj);
                    }

                    if(tool.createSpaceMarkup){
                        
                        tool.createSpace(mousePos);

                    }else{
                        
                        tool.findspace(mousePos);
                        
                    }
                    
                    tool.started = !tool.started;
                    tool.x0 = mousePos.x;
                    tool.y0 = mousePos.y;
                    prevx = tool.x0;
                    prevy = tool.y0;


                    /*var polyfound = DocObj.getCurrentpage().getpolygon(mousePos.x, mousePos.y,tool.multiselect);
                    if(!polyfound){
                        var spacefound = findspacemarkup(mousePos.x, mousePos.y, tool.multiselect);
                    }*/
                    //tool.started = true;
                    /*tool.x0 = mousePos.x;
                     tool.y0 = mousePos.y;
                     prevx = tool.x0;
                     prevy = tool.y0;*/

                };
                this.mousedown = function (ev) {
                    ev.preventDefault();
                    ev.stopPropagation();

                    var found = false;

                    

                    //tool.started = true;
                    var mousePos = getMousePos(canvas, ev);

                    if (RxCore_GUI_inputEvent != undefined){
                        
                        var inputobj = {
                            mousepos : mousePos,
                            mousebutton : ev.button,
                            dblclick : false,
                            key : tool.curkey,
                            tool : tool.name
                        };
                        
                        RxCore_GUI_inputEvent.setInput(inputobj);
                    }

                    if(tool.createSpaceMarkup){
                        
                        tool.createSpace(mousePos);


                    }else{
                        tool.findspace(mousePos);
                        
                        
    
                    }

                    tool.started = !tool.started;
                    tool.x0 = mousePos.x;
                    tool.y0 = mousePos.y;
                    prevx = tool.x0;
                    prevy = tool.y0;
                    


                    //page.getpolygon(x,y)
                    /*tool.x0 = mousePos.x;
                     tool.y0 = mousePos.y;
                     prevx = tool.x0;
                     prevy = tool.y0;*/

                };

                this.touchstart = function (ev) {
                    ev.preventDefault();

                    var found = false;

                    //tool.started = true;
                    var touchPos = getTouchPos(canvas, ev, 0);

                    if (RxCore_GUI_inputEvent != undefined){
                        
                        var inputobj = {
                            mousepos : touchPos,
                            mousebutton : null,
                            dblclick : false,
                            key : tool.curkey,
                            tool : tool.name
                        };
                        
                        RxCore_GUI_inputEvent.setInput(inputobj);
                    }


                    if(tool.createSpaceMarkup){
                        
                        tool.createSpace(touchPos);

                    }else{
                        tool.findspace(touchPos);
    
                    }

                    tool.started = !tool.started;
                    tool.x0 = touchPos.x;
                    tool.y0 = touchPos.y;
                    prevx = tool.x0;
                    prevy = tool.y0;
                    x = tool.x0;
                    y = tool.y0;


                    /*var polyfound = DocObj.getCurrentpage().getpolygon(touchPos.x, touchPos.y,tool.multiselect);

                    if(!polyfound){
                        var spacefound = findspacemarkup(touchPos.x, touchPos.y, tool.multiselect);
                    }*/

                    /*tool.x0 = touchPos.x;
                     tool.y0 = touchPos.y;
                     prevx = tool.x0;
                     prevy = tool.y0;
                     x = tool.x0;
                     y = tool.y0;*/
                };

                this.MSPointerMove = function (ev) {
                    /*if (!tool.started) {
                     return;
                     }*/
                    var mousePos = getMousePos(canvas, ev);

                    tool.findspacehover(mousePos);
                    
                    tool.curmousepos = mousePos;
                    if (!tool.started) {
                        return;
                    }
                    
                    var xdiff = prevx - mousePos.x;
                    var ydiff = prevy - mousePos.y;

                    DocObj.getCurrentpage().pan_update(xdiff, ydiff);
                    
                    


                    prevx = mousePos.x;
                    prevy = mousePos.y;

                    //var xdiff = prevx - mousePos.x;
                    //var ydiff = prevy - mousePos.y;

                    //var polyfound = DocObj.getCurrentpage().getpolygon(mousePos.x, mousePos.y);
                    //DocObj.getCurrentpage().pan_update(xdiff, ydiff);


                    //prevx = mousePos.x;
                    //prevy = mousePos.y;

                };
                this.mousemove = function (ev) {
                    ev.preventDefault();
                    ev.stopPropagation();

                    var mousePos = getMousePos(canvas, ev);

                    tool.findspacehover(mousePos);

                    tool.curmousepos = mousePos;
                    if (!tool.started) {
                        return;
                    }
                    
                    var xdiff = prevx - mousePos.x;
                    var ydiff = prevy - mousePos.y;

                    DocObj.getCurrentpage().pan_update(xdiff, ydiff);
                    

                    prevx = mousePos.x;
                    prevy = mousePos.y;                    
                    /*if (!tool.started) {
                     return;
                     }*/
                    //var mousePos = getMousePos(canvas, ev);
                    //var polyfound = DocObj.getCurrentpage().getpolygon(mousePos.x, mousePos.y);
                    //var xdiff = prevx - mousePos.x;
                    //var ydiff = prevy - mousePos.y;


                    //prevx = mousePos.x;
                    //prevy = mousePos.y;

                };

                this.touchmove = function (ev) {
                    
                    ev.preventDefault();
                    /*if (!tool.started) {
                     return;
                     }*/
                    var touchPos = getTouchPos(canvas, ev, 0);
                    tool.curmousepos = touchPos;

                    if (!tool.started) {
                        return;
                    }
                    
                    var xdiff = prevx - touchPos.x;
                    var ydiff = prevy - touchPos.y;

                    DocObj.getCurrentpage().pan_update(xdiff, ydiff);
                    
                    


                    prevx = touchPos.x;
                    prevy = touchPos.y;

                    //var polyfound = DocObj.getCurrentpage().getpolygon(touchPos.x, touchPos.y);
                    //var xdiff = prevx - touchPos.x;
                    //var ydiff = prevy - touchPos.y;

                    //prevx = touchPos.x;
                    //prevy = touchPos.y;


                };

                this.MSPointerUp = function (ev) {
                   
                    if (tool.started) {
                        tool.MSPointerMove(ev);
                        tool.started = false;
                    }

                };

                this.mouseup = function (ev) {
                    
                    if (tool.started) {
                        tool.mousemove(ev);
                        tool.started = false;

                    }                    
                };

                this.touchend = function (ev) {
                    ev.preventDefault();
                    if (tool.started) {
                        tool.touchmove(ev);
                        tool.started = false;
                    }

                    /*if (tool.started) {
                     tool.touchmove(ev);
                     tool.started = false;
                     }*/
                };

                this.touchcancel = function(ev){
                    ev.preventDefault();
                    if (tool.started) {
                        tool.touchmove(ev);
                        tool.started = false;
                    }

                };                

                this.keyup = function (ev){


                    tool.curkey = null;

                };
                this.keydown = function (ev) {
                    //ev.preventDefault();

                    //GUI_mouse_ev
                    //ev.button
                    //mousePos
                    //ev.keyCode
                    tool.curkey = ev.keyCode;

                    if (RxCore_GUI_inputEvent != undefined){

                        var inputobj = {
                            mousepos : null,
                            mousebutton : null,
                            dblclick : false,
                            key : tool.curkey,
                            tool : tool.name
                        };
                        
                        RxCore_GUI_inputEvent.setInput(inputobj);
                    }

                };                


            };

            tools.pan = function () {

                var tool = this;
                var x = 0,
                    y = 0,
                    w = 0,
                    h = 0;
                var prevx = 0,
                    prevy = 0;
                this.started = false;
                this.name = 'pan';
                this.anglelengthsupport = false;
                this.refreshSupport = false;

                
                setCursor(getMarkupCursor('pan'));

                this.pointermove = function (ev) {
                    tool.MSPointerMove(ev);
                };
                this.pointerdown = function (ev) {
                    tool.MSPointerDown(ev);
                };

                this.pointerup = function (ev) {
                    tool.MSPointerUp(ev);
                };

                this.MSPointerDown = function (ev) {
                    ev.preventDefault();
                    ev.stopPropagation();

                    
                    var mousePos = getMousePos(canvas, ev);
                    tool.started = true;
                    tool.x0 = mousePos.x;
                    tool.y0 = mousePos.y;
                    prevx = tool.x0;
                    prevy = tool.y0;

                };
                this.mousedown = function (ev) {
                    ev.preventDefault();
                    ev.stopPropagation();

                    

                    tool.started = true;
                    var mousePos = getMousePos(canvas, ev);
                    tool.x0 = mousePos.x;
                    tool.y0 = mousePos.y;
                    prevx = tool.x0;
                    prevy = tool.y0;

                };

                this.touchstart = function (ev) {
                    ev.preventDefault();
                    tool.started = true;
                    var touchPos = getTouchPos(canvas, ev, 0);
                    tool.x0 = touchPos.x;
                    tool.y0 = touchPos.y;
                    prevx = tool.x0;
                    prevy = tool.y0;
                    x = tool.x0;
                    y = tool.y0;
                };

                this.MSPointerMove = function (ev) {
                    if (!tool.started) {
                        return;
                    }
                    var mousePos = getMousePos(canvas, ev);
                    var xdiff = prevx - mousePos.x;
                    var ydiff = prevy - mousePos.y;


                    DocObj.getCurrentpage().pan_update(xdiff, ydiff);
                    

                    prevx = mousePos.x;
                    prevy = mousePos.y;

                };
                this.mousemove = function (ev) {
                    if (!tool.started) {
                        return;
                    }
                    var mousePos = getMousePos(canvas, ev);
                    var xdiff = prevx - mousePos.x;
                    var ydiff = prevy - mousePos.y;


                    DocObj.getCurrentpage().pan_update(xdiff, ydiff);
                    

                    prevx = mousePos.x;
                    prevy = mousePos.y;

                };

                this.touchmove = function (ev) {
                    ev.preventDefault();
                    if (!tool.started) {
                        return;
                    }
                    var touchPos = getTouchPos(canvas, ev, 0);
                    var xdiff = prevx - touchPos.x;
                    var ydiff = prevy - touchPos.y;

                    DocObj.getCurrentpage().pan_update(xdiff, ydiff);
                    


                    prevx = touchPos.x;
                    prevy = touchPos.y;


                };

                this.MSPointerUp = function (ev) {
                    
                    if (tool.started) {
                        tool.mousemove(ev);
                        tool.started = false;


                    }

                };
                this.mouseup = function (ev) {
                    
                    if (tool.started) {
                        tool.mousemove(ev);
                        tool.started = false;


                    }
                };
                this.touchcancel = function(ev){
                    ev.preventDefault();
                    if (tool.started) {
                        tool.touchmove(ev);
                        tool.started = false;
                    }

                };                
                this.touchend = function (ev) {
                    ev.preventDefault();
                    if (tool.started) {
                        tool.touchmove(ev);
                        tool.started = false;
                    }
                };

            };

            tools.selectText = function (params){
                var tool = this;
                var x = 0,
                    y = 0,
                    w = 0,
                    h = 0;
                var prevx = 0,
                    prevy = 0;
                this.started = false;
                this.name = 'selectText';
                this.anglelengthsupport = false;
                this.refreshSupport = false;
                
                this.refreshSupport = false;

                this.startPos = {x:0,y:0};


                setCursor(getMarkupCursor('selectText'));


                this.pointermove = function (ev) {
                    tool.MSPointerMove(ev);
                };
                this.pointerdown = function (ev) {
                    tool.MSPointerDown(ev);
                };

                this.pointerup = function (ev) {
                    tool.MSPointerUp(ev);
                };

                this.MSPointerDown = function (ev) {
                    ev.preventDefault();
                    ev.stopPropagation();
                    
                    var mousePos = getMousePos(canvas, ev);
                    tool.started = true;
                    //tool.x0 = mousePos.x;
                    //tool.y0 = mousePos.y;
                    tool.x0 = mousePos.xus;
                    tool.y0 = mousePos.yus;

                    tool.startPos = mousePos;
                    prevx = tool.x0;
                    prevy = tool.y0;

                    
                    DocObj.getCurrentpage().SelectText(tool.started, false, tool.startPos, mousePos);

                };
                this.mousedown = function (ev) {
                    ev.preventDefault();
                    ev.stopPropagation();


                    tool.started = true;
                    var mousePos = getMousePos(canvas, ev);
                    //tool.x0 = mousePos.x;
                    //tool.y0 = mousePos.y;

                    tool.x0 = mousePos.xus;
                    tool.y0 = mousePos.yus;


                    tool.startPos = mousePos;
                    prevx = tool.x0;
                    prevy = tool.y0;

                    
                    DocObj.getCurrentpage().SelectText(tool.started, false, tool.startPos, mousePos);
                };

                this.touchstart = function (ev) {
                    ev.preventDefault();
                    tool.started = true;
                    var touchPos = getTouchPos(canvas, ev, 0);
                    
                    //tool.x0 = touchPos.x;
                    //tool.y0 = touchPos.y;

                    tool.x0 = touchPos.xus;
                    tool.y0 = touchPos.yus;


                    tool.startPos = mousePos;
                    prevx = tool.x0;
                    prevy = tool.y0;
                    x = tool.x0;
                    y = tool.y0;

                    DocObj.getCurrentpage().SelectText(tool.started, false, tool.startPos, mousePos);
                };

                this.MSPointerMove = function (ev) {
                    if (!tool.started) {
                        return;
                    }
                    var mousePos = getMousePos(canvas, ev);
                    //var xdiff = prevx - mousePos.x;
                    //var ydiff = prevy - mousePos.y;

                    var xdiff = prevx - mousePos.xus;
                    var ydiff = prevy - mousePos.yus;

                    //tool.x0 = mousePos.x;
                    //tool.y0 = mousePos.y;

                    tool.x0 = mousePos.xus;
                    tool.y0 = mousePos.yus;

                    
                    DocObj.getCurrentpage().SelectText(false, false, tool.startPos, mousePos);
                    //DocObj.getCurrentpage().pan_update(xdiff, ydiff);
                    

                    //prevx = mousePos.x;
                    //prevy = mousePos.y;

                    prevx = mousePos.xus;
                    prevy = mousePos.yus;


                };
                this.mousemove = function (ev) {
                    if (!tool.started) {
                        return;
                    }
                    var mousePos = getMousePos(canvas, ev);
                    //var xdiff = prevx - mousePos.x;
                    //var ydiff = prevy - mousePos.y;

                    var xdiff = prevx - mousePos.xus;
                    var ydiff = prevy - mousePos.yus;


                    
                    DocObj.getCurrentpage().SelectText(false, false, tool.startPos, mousePos);

                    //DocObj.getCurrentpage().pan_update(xdiff, ydiff);

                    //prevx = mousePos.x;
                    //prevy = mousePos.y;

                    prevx = mousePos.xus;
                    prevy = mousePos.yus;


                };

                this.touchmove = function (ev) {
                    ev.preventDefault();
                    if (!tool.started) {
                        return;
                    }
                    var touchPos = getTouchPos(canvas, ev, 0);
                    //var xdiff = prevx - touchPos.x;
                    //var ydiff = prevy - touchPos.y;

                    var xdiff = prevx - touchPos.xus;
                    var ydiff = prevy - touchPos.yus;

                    //DocObj.getCurrentpage().pan_update(xdiff, ydiff);
                    
                    DocObj.getCurrentpage().SelectText(false, false, tool.startPos, touchPos);
                    
                    //prevx = touchPos.x;
                    //prevy = touchPos.y;

                    prevx = touchPos.xus;
                    prevy = touchPos.yus;



                };

                this.MSPointerUp = function (ev) {
                    
                    if (tool.started) {
                        tool.mousemove(ev);
                        var mousePos = getMousePos(canvas, ev);
                        tool.started = false;

                        DocObj.getCurrentpage().SelectText(tool.started, true, tool.startPos, mousePos);


                    }

                };
                this.mouseup = function (ev) {
                    
                    if (tool.started) {
                        tool.mousemove(ev);
                        var mousePos = getMousePos(canvas, ev);
                        tool.started = false;
                        DocObj.getCurrentpage().SelectText(tool.started, true, tool.startPos, mousePos);

                    }
                };
                this.touchcancel = function(ev){
                    ev.preventDefault();
                    if (tool.started) {
                        tool.touchmove(ev);
                        var touchPos = getTouchPos(canvas, ev, 0);
                        tool.started = false;

                        DocObj.getCurrentpage().SelectText(tool.started, true, tool.startPos, touchPos);
                    }

                };                
                this.touchend = function (ev) {
                    ev.preventDefault();
                    if (tool.started) {
                        tool.touchmove(ev);
                        var touchPos = getTouchPos(canvas, ev, 0);
                        DocObj.getCurrentpage().SelectText(tool.started, true, tool.startPos, touchPos);
                        tool.started = false;
                    }
                };                

            };

            tools.markupedit = function (params) {

                var tool = this;
                var x = 0,
                    y = 0,
                    w = 0,
                    h = 0;
                var prevx = 0,
                    prevy = 0;
                var xcur1 = 0,
                    xcur2 = 0,
                    ycur1 = 0,
                    ycur2 = 0;
                var startx = 0,
                    starty = 0,
                    startx2 = 0,
                    starty2 = 0,
                    startw = 0,
                    starth = 0;
                var startwsq = 0.0;
                var starthsq = 0.0;
                var wsq = 0.0;
                var hsq = 0.0;

                var startdiagonal = 0.0;
                var diagonal = 0.0;
                var prevdiagonal = 0.0;
                var prevh = 0,
                    prevw = 0;

                var startcenterx = 0,
                    startcentery = 0;
                var centerx = 0,
                    centery = 0;
                var prevcenterx = 0,
                    prevcentery = 0;

                var id = -1;
                var fmarkup = {};
                var listentrynum = -1;

                var numtouchpoints = -1;
                var touchpointarr = [];
                var mstouchzoom = false;
                var mstouchpan = false;

                var startdeltax = 0;
                var startdeltay = 0;
                var diffangle = 0;
                var startangle = 0;
                var curangle = 0;

                var startscalesmall = 0;
                var startscalelarge = 0;
                var bCanSelectSpace = params.p1;

                this.LastTouchdata = -1;
                this.started = false;
                this.name = 'markupedit';
                this.anglelengthsupport = false;
                this.refreshSupport = false;

                this.savestate = true;
                this.changedstate = false;
                this.scrollkeyOn = false;
                this.curkey = null;

                //this.curmousepos = {x:0,y:0};
                this.curmousepos = {x:0,y:0,xus:0,yus:0};


                var havesnap = false;
                var snapPoint;
                
                this.bdeletepoint = false;
                this.binsertpoint = false;
                
                this.bmovemousebutton = false;

                

                setCursor(getMarkupCursor('markupedit'));

                //dragaction 1 = move, 2 = scale, 3= rotate, 4 = editpoint
                //var dragaction = 1;

                this.findspacehover = function(mousePos){
                    
                    if(benableblockhover){
                        var polyfound = DocObj.getCurrentpage().getpolygonhover(mousePos.x, mousePos.y);
                    }

                };


                this.enablePointDelete = function(){
                  tool.bdeletepoint = true;  
                };

                this.enablePointInsert = function(){
                    tool.binsertpoint = true;  
                };
  
                this.disablePointInsert = function(){
                    tool.binsertpoint = false;  
                };


                this.enableMoveLabel = function(onOff){
                    tool.bmovelabel = onOff;
                };


                this.scrollhandling = function (movey) {
                    if(docpagNotReady()){
                        return;
                    }

                    if (!documentopen || DocObj.getCurrentpage().usevector3Dxml) {
                        return;
                    }

                    RxCore_hideTextInput();
                    var bmultipage = (DocObj.getCurrentpage().usepdfjs && DocObj.pages.length > 1);

                    if (movey < 0) {
                        
                        if ((bmultipage || bScrollSingleDisable) && !enableZoomOnScrollMultipage && !DocObj.pagelocked) {
                        

                            //DocObj.getCurrentpage().pan_update(0,-movey);
                            if (DocObj.currentpage == DocObj.pages.length - 1) {
                                /*if (DocObj.getCurrentpage().endy <= canvas.height) {
                                    movey = 0;
                                }*/
                            }
                            DocObj.getCurrentpage().pan_update(0, -movey);
                            

                            if (DocObj.getCurrentpage().pagenumber == DocObj.pages.length - 1 && DocObj.getCurrentpage().getpageRotation() == 0) {
                                /*while (DocObj.getCurrentpage().endy < canvas.height) {
                                    DocObj.getCurrentpage().pan_update(0, -1);
                                    
                                }*/
    
                            }
    

                        } else {
                            if(bUseScrollKey){
                                if (tool.scrollkeyOn){
                                    if (!DocObj.getCurrentpage().usedincomposite || bScrollZoomOverlay){
                                        
                                        if(belowlimitExtentEx(1.1, tool.curmousepos)){

                                            DocObj.getCurrentpage().ZoomOut(1.1, false, false, tool.curmousepos);


                                            if(bRestrictPan){
                                                tool.panlimit();
                                            }
                                            
                                        }else{
                                            RxCore_foxitForceRedraw();
                                        }
                                        
                                        //tool.limitExtent(1.1);
                                    }
                                    
                                    //DocObj.getCurrentpage().ZoomOutmouse(1.1, tool.curmousepos);
                                }

                            }else{
                                if (!DocObj.getCurrentpage().usedincomposite || bScrollZoomOverlay){

                                    if(belowlimitExtentEx(1.1, tool.curmousepos)){
                                        DocObj.getCurrentpage().ZoomOut(1.1, false, false, tool.curmousepos);
                                        if(bRestrictPan){
                                            tool.panlimit();
                                        }
                                        
                                         
                                    }else{
                                        RxCore_foxitForceRedraw();
                                    }
                                        
                                    
                                    
                                    //tool.limitExtent(1.1);

                                }
                                
                                //DocObj.getCurrentpage().ZoomOutmouse(1.1, tool.curmousepos);
                            }

                        }


                    } else {
                        
                        
                        if ((bmultipage || bScrollSingleDisable) && !enableZoomOnScrollMultipage && !DocObj.pagelocked) {
                        //if ((bmultipage || bScrollSingleDisable) && !DocObj.pagelocked) {

                            if (DocObj.currentpage == 0) {
                                /*if (DocObj.getCurrentpage().starty >= 0) {
                                    movey = 0;
                                }*/

                            }
                            DocObj.getCurrentpage().pan_update(0, -movey);
                            

                            if (DocObj.getCurrentpage().pagenumber == 0 && DocObj.getCurrentpage().getpageRotation() == 0) {
                                /*while (DocObj.getCurrentpage().starty > 0) {
                                    DocObj.getCurrentpage().pan_update(0, 1);
                                    
                                }*/
    
                            }
    

                        } else {
                            if(bUseScrollKey){
                                if (tool.scrollkeyOn){
                                    if (!DocObj.getCurrentpage().usedincomposite || bScrollZoomOverlay){

                                        DocObj.getCurrentpage().ZoomIn(1.1, false, false, tool.curmousepos);
                                        //tool.limitExtent(1.1);
                                    }
                                    
                                    //DocObj.getCurrentpage().ZoomInMouse(1.1, tool.curmousepos);
                                }
                            }else{
                                if (!DocObj.getCurrentpage().usedincomposite || bScrollZoomOverlay){
                                    DocObj.getCurrentpage().ZoomIn(1.1, false, false, tool.curmousepos);
                                    //tool.limitExtent(1.1);
                                }
                                
                                //DocObj.getCurrentpage().ZoomInMouse(1.1, tool.curmousepos);
                            }

                        }

                    }
                    
                };

                this.belowlimitExtent = function(zoomscalefactor){
                    var belowlimit = true;
                    

                    var pagedim = DocObj.getCurrentpage().getpagedim();

                    //var wlimit = canvasowidth - 60;
                    //var hlimit = canvasoheight - 60;

                    var wlimit = canvasowidthsize - 60;
                    var hlimit = canvasoheightsize - 60;


                    if((pagedim.w / zoomscalefactor) < wlimit && (pagedim.h / zoomscalefactor) < hlimit){
                        belowlimit = false;
                        //scalechanged = false;
                        //DocObj.getCurrentpage().zoomall();
                    }

                    var pagerect = DocObj.getCurrentpage().ZoomOutTest(zoomscalefactor, false, tool.curmousepos);
                    
                    //var heightlimit = canvasoheight - (nPanlimit*2);
                    //var widthlimit = canvasowidth - (nPanlimit*2);

                    var heightlimit = canvasoheightsize - (nPanlimit*2);
                    var widthlimit = canvasowidthsize - (nPanlimit*2);


                    //var toplimit = 15;
                    var bottomlimit = ((pagerect.y + pagerect.h) <= heightlimit);
                    var toplimit = (pagerect.y >= nPanlimit);

                    // need to return these to reset the top and bottom positions.scroll|| bottomlimit || toplimit
                    //if ((pagerect.w / pagerect.h) < (canvasowidth / canvasoheight)){
                    if ((pagerect.w / pagerect.h) < (canvasowidthsize / canvasoheightsize)){                        
                        if(pagerect.h <= heightlimit){
                            belowlimit = false;
                        }
    
                    }else{
                        if(pagerect.w <= widthlimit){
                            belowlimit = false;
                        }

                    }

                    /*if (DocObj.getCurrentpage().usepdfjs) {
                        //var curscale = DocObj.getCurrentpage().curpagescale * DocObj.getCurrentpage().dscalepdf;
                        


                    }else if (DocObj.getCurrentpage().usevectorxml) {

                    
                        if (DocObj.getCurrentpage().initialscale > DocObj.getCurrentpage().dscalevector) {
                            //scalechanged = false;
                            //DocObj.getCurrentpage().zoomall();
                            belowlimit = false;

                        }


                    } else {
                        if (DocObj.getCurrentpage().initialscale > (DocObj.getCurrentpage().dscale / DocObj.getCurrentpage().bitmapratio)) {
                            //scalechanged = false;
                            //DocObj.getCurrentpage().zoomall();
                            belowlimit = false;
                        }

                    }*/

                    if(!bLimitZoomOut){
                        belowlimit = true;
                    }

                    return belowlimit;
                };

                this.set3DModelPos = function(mousePos){
                    //add code for sending callback data about clicked position here.
                    //if page is3dnav page.
                    var curpage = DocObj.getCurrentpage();
                    if (curpage.bis3Dnavigator){
                        if(RxCore_GUI_3DNavPosition != undefined){
                            
                            //curpage.setbirdseyecamera(null, null,null);

                            var navigationpos = {docindex : DocObj.fileindex, pageindex : curpage.pagenumber, cameraPos : mousePos};

                            RxCore_GUI_3DNavPosition.navClick(navigationpos);
                        }
                    }

                };


                this.panlimit = function(){
                    var pagedim = DocObj.getCurrentpage().getpagedim();

                    if ((pagedim.w / pagedim.h) < (canvasowidth / canvasoheight)){
                        if ((pagedim.y + pagedim.h) < (canvasoheight - nPanlimit)){
                            var ypos = (canvasoheight - nPanlimit) - pagedim.h;
                            RxCore_PagePos(pagedim.x, ypos);
                            //RxCore_PagePos(pagedim.x, -nPanlimit);
                            //reset position to -15
                        }
    
                        if (pagedim.y > nPanlimit){
                            //reset position to 15
                            RxCore_PagePos(pagedim.x, nPanlimit);
                        }

                    }else{
                        if ((pagedim.x + pagedim.w) < (canvasowidth - nPanlimit)){
                            RxCore_PagePos(-nPanlimit, pagedim.y);
                            //reset position to -15
                        }
    
                        if (pagedim.x > nPanlimit){
                            //reset position to 15
                            RxCore_PagePos(nPanlimit, pagedim.y);
                        }                    
    
                    }


                    /*if(pagedim.w >= canvasowidth){
                    }*/


                };

                this.limitPan = function(x,y){
                    
                    var pagedim = DocObj.getCurrentpage().getpagedim();
                    var direction = {up:false, down:false, left: false, right : false};

                    direction.down = (y < 0);
                    direction.up = (y > 0);
                    direction.left = (x > 0);
                    direction.right = (x < 0);
                    
                    /*switch(pagedim.rotation){
                        case 90:
                            pagedim.y = -((pagedim.y + pagedim.h) - canvasoheight);
                            pagedim.x = -((pagedim.x + pagedim.w) - canvasowidth);
                            break;

                        case 270:
                            pagedim.y = -((pagedim.y + pagedim.h) - canvasoheight);
                            pagedim.x = -((pagedim.x + pagedim.w) - canvasowidth);
                            break;
                    }*/

                    var moveyul = pagedim.y - y;
                    var movexul = pagedim.x - x;
                    var moveylr = moveyul + pagedim.h;
                    var movexlr = movexul + pagedim.w;


                    var allowpan = {x:true,y:true, diffx : x, diffy:y};

                    if(!bRestrictPan){
                        return allowpan;
                    }

                    if(pagedim.h <= canvasoheight - 1){
                        allowpan.y = false;
                    }else{
                        if(pagedim.y > 15 && direction.down){
                            allowpan.y = false;
                        }

                        if(pagedim.y + pagedim.h < (canvasoheight - 15) && direction.up){
                            allowpan.y = false;
                        }

                    }
                    
                    if(pagedim.w <= canvasowidth - 1){
                        allowpan.x = false;
                    }else{
                
                        if(pagedim.x > 15 && direction.right){
                            allowpan.x = false;
                        }

                        if(pagedim.x + pagedim.w < (canvasowidth - 15) && direction.left){
                            allowpan.x = false;
                        }

                    }
                    
                    /*if (direction.down && moveyul > 15){
                        allowpan.diffy = moveyul - 15;
                    }

                    if(direction.up && moveylr > 15){
                        allowpan.diffy = moveylr - (canvasoheight - 15);
                        allowpan.diffy = moveylr - (canvasoheight - 15);
                    }*/

                    if(direction.up && allowpan.y){
                        
                        if(moveylr < (canvasoheight - 15)){
                            if(y > 0){
                                allowpan.diffy = y - ((canvasoheight - 15) - moveylr);
                            }
                        }

                    }

                    if(direction.down && allowpan.y){
                        if(moveyul > 15){
                            if(y < 0){
                                allowpan.diffy = y + (moveyul - 15);
                            }
                        }
                        
                        
                    }

                    if(direction.left && allowpan.x ){
                        if(movexlr < (canvasowidth - 15)){
                            if(x > 0){
                                allowpan.diffx = x - ((canvasowidth - 15) - movexlr);
                            }
                        }

                    }

                    if(direction.right && allowpan.x){
                        if(movexul > 15){
                            if(x < 0){
                                allowpan.diffx = x + (movexul - 15);  
                            }
                            
                        }
                        
                        
                    }

                    return allowpan;

                };

                this.limitExtent = function(zoomscalefactor){

                    if (DocObj.getCurrentpage().usepdfjs) {
                        //var curscale = DocObj.getCurrentpage().curpagescale * DocObj.getCurrentpage().dscalepdf;
                        
                        var pagedim = DocObj.getCurrentpage().getpagedim();

                        if((pagedim.w * zoomscalefactor) < canvasowidth && (pagedim.h * zoomscalefactor) < canvasoheight){
                            //scalechanged = false;
                            DocObj.getCurrentpage().zoomall();
                            //console.log('zoom all');
                        }

                        //var cwidth = DocObj.getCurrentpage().endx
                        //if (DocObj.getCurrentpage().endx - )
                        
                        /*if (DocObj.getCurrentpage().initialscale > curscale) {
                            scalechanged = false;
                            DocObj.getCurrentpage().zoomall();
                        }*/

                    }else if (DocObj.getCurrentpage().usevectorxml) {

                    
                        if (DocObj.getCurrentpage().initialscale > DocObj.getCurrentpage().dscalevector) {
                            //scalechanged = false;
                            DocObj.getCurrentpage().zoomall();

                        }


                    } else {
                        if (DocObj.getCurrentpage().initialscale > (DocObj.getCurrentpage().dscale / DocObj.getCurrentpage().bitmapratio)) {
                            //scalechanged = false;
                            DocObj.getCurrentpage().zoomall();
                        }

                    }
                };


                this.DOMMouseScroll = function (ev) {

                    //tool.scrollhandling(1, ev.detail*50);

                };

                this.mousewheel = function(ev){
                    tool.wheel(ev);
                };


                this.wheel = function (ev) {

                    var delta = 0;
                    if (ev.wheelDelta !== undefined) { // WebKit / Opera / Explorer 9

                        delta = ev.wheelDelta;

                    } else if (ev.deltaY !== undefined) { // Firefox
                        if(bIsMSIE ){
                            delta = -ev.deltaY;
                        }else{
                            delta = -ev.deltaY*50;

                        }


                    }

                    tool.scrollhandling(delta);


                };




                this.mousedownhandling = function (ev, mousePos) {
                    if (!documentopen || DocObj.getCurrentpage().usevector3Dxml) {
                        return;
                    }

                    var scrolldata = null;

                    //pressingDown(ev, mousePos);


                    if(DocObj.getCurrentpage().usepdfjs){
                        var scrolldata = DocObj.mouseOnScroll(mousePos);
                    }

                    if(scrolldata == null){
                        ev.target.style.cursor = 'move';
                    }else{
                        if (!scrolldata.hBarOn && !scrolldata.vBarOn){
                            ev.target.style.cursor = 'move';
                        }
                    }
                    


                    mstouchpan = true;
                    tool.started = true;

                    tool.x0 = mousePos.x;
                    tool.y0 = mousePos.y;
                    prevx = tool.x0;
                    prevy = tool.y0;

                    /*if(DocObj.getCurrentpage().usepdfjs){
                        DocObj.mouseOnScroll(mousePos);
                    }*/

                    
                    tool.set3DModelPos(mousePos);

                    //console.log('mousedownhandling', prevx, prevy);

                    /*id = findmarkupHover(mousePos.x,mousePos.y);
                     var listentrynum = DocObj.getmarkupbynumber(id);

                     if (id != -1){
                     if(DocObj.markuplist[listentrynum].type==10){
                     if(RxCore_GUI_Notediag != undefined){
                     RxCore_GUI_Notediag.setNotediag(DocObj.markuplist[listentrynum].text,true);
                     }

                     tool.started = false;
                     }else{

                     }


                     }*/

                };

                this.mouseuphandler = function (ev) {
                    if (!documentopen) {
                        return;
                    }

                    //notPressingDown(ev);

                    //ev.target.style.cursor = 'default';
                    


                    /*if(DocObj.getCurrentpage().usepdfjs){
                        DocObj.mouseOffScroll();
                    }*/


                    if (tool.started || mstouchpan) {
                        //tool.mousemove(ev);
                        tool.started = false;
                        
                        mstouchpan = false;
                        if (DocObj.Type == 0 && !documentcompare) {
                          if(!DocObj.pagelocked){
                            if (DocObj.foxitdoc == null){
                                if (DocObj.currentpage == 0 && DocObj.getCurrentpage().getpageRotation() == 0) {
                                    while (DocObj.getCurrentpage().starty > 0) {
                                        DocObj.getCurrentpage().pan_update(0, 1);
                                        
                                    }
                                }
                                if (DocObj.currentpage == DocObj.NumPages - 1 && DocObj.getCurrentpage().getpageRotation() == 0) {
                                    while (DocObj.getCurrentpage().endy < canvas.height) {
                                        DocObj.getCurrentpage().pan_update(0, -1);
                                        
                                    }
    
                                }
    
                            }  
                          }
                        }
                    }


                };

                this.movehandler = function (mousePos, ev) {
                    if (!documentopen) {
                        return;
                    }
                    if(DocObj == null || DocObj == undefined){
                        return;
                    }
            
                    var cvdim = DocObj.getCurrentpage().getcanvasdim();//canvas.width

                    if (!tool.started && !mstouchpan) {


                        cntximg.clearRect(0, 0, canvas.width, canvas.height);
                        context.clearRect(0, 0, canvas.width, canvas.height);


                        id = findmarkupHover(mousePos.x, mousePos.y);
                        var listmnum = DocObj.getmarkupbynumber(id);
                        if (id != -1 && DocObj.markuplist[listmnum].pagenumber == DocObj.getcurPage() && DocObj.markuplist[listmnum].display && DocObj.Drawmarkup) {

                            if (id != -1 && DocObj.markuplist[listmnum].bhaveLink && (DocObj.bMarkupLocked || DocObj.markuplist[listmnum].locked)){
                                ev.target.style.cursor = 'pointer';
                                context.clearRect(0, 0, canvas.width, canvas.height);
                                DocObj.markuplist[listmnum].displayurl(context, mousePos.x, mousePos.y);

                            }else if (id != -1 && DocObj.markuplist[listmnum].type == 10) {

                                ev.target.style.cursor = 'pointer';
                                context.clearRect(0, 0, canvas.width, canvas.height);
                                DocObj.markuplist[listmnum].displaylabel(context, mousePos.x, mousePos.y);

                            } else {
                                ev.target.style.cursor = 'default';
                                context.clearRect(0, 0, canvas.width, canvas.height);
                                DocObj.markuplist[listmnum].displaylabel(context, mousePos.x, mousePos.y);

                            }

                        } else {
                            ev.target.style.cursor = 'default';
                            context.clearRect(0, 0, canvas.width, canvas.height);

                            if(RxCore_GUI_MarkupHover != undefined){
                                RxCore_GUI_MarkupHover.setMarkupHover(null, x, y);
                            }


                        }
                        return;

                    }


                    var xdiff = prevx - mousePos.x;
                    var ydiff = prevy - mousePos.y;

                    if (DocObj.Type == 0 && !documentcompare && !DocObj.pagelocked) {

                        if (DocObj.getCurrentpage() == undefined){
                            return;
                        }

                        if (DocObj.getCurrentpage().startx < 0 || DocObj.getCurrentpage().endx > cvdim.w) {
                            DocObj.getCurrentpage().pan_update(xdiff, ydiff);
                            
                        } else {
                            if (DocObj.getCurrentpage().getpageRotation() == 0) {
                                DocObj.getCurrentpage().pan_update(0, ydiff);
                                
                            } else {
                                DocObj.getCurrentpage().pan_update(xdiff, ydiff);
                                
                            }

                        }
                        //if(DocObj.getCurrentpage().starty > 0)


                    } else {
                        //tool.panhandle(xdiff,ydiff);
                        var allowpan = tool.limitPan(xdiff, ydiff);
                        //var allowpan = {x:true,y:true, diffx : x, diffy:y};
                        var movex = allowpan.x ? allowpan.diffx : 0;
                        var movey = allowpan.y ? allowpan.diffy : 0;
                        
                        if (DocObj.getCurrentpage() == undefined){
                            return;
                        }

                        DocObj.getCurrentpage().pan_update(movex, movey);

                        
                        /*if (allowpan.x && allowpan.y){
                            DocObj.getCurrentpage().pan_update(xdiff, ydiff);
                        }else if(allowpan.x && !allowpan.y){
                            DocObj.getCurrentpage().pan_update(xdiff, 0);
                        }else if (!allowpan.x && allowpan.y){
                            DocObj.getCurrentpage().pan_update(0, ydiff);
                        }else {
                            DocObj.getCurrentpage().pan_update(0, 0);
                        }*/

                        
                    }

                    prevx = mousePos.x;
                    prevy = mousePos.y;

                };


                this.panhandle = function(xdiff,ydiff){
                    
                    var cvdim = DocObj.getCurrentpage().getcanvasdim();//canvas.width

                    if (DocObj.getCurrentpage().usevectorxml) {
                        if (DocObj.getCurrentpage().initialscale < DocObj.getCurrentpage().dscalevector) {
                            if(xdiff < 50 && ydiff < 50){
                                //limit check here.
                                DocObj.getCurrentpage().pan_update(xdiff, ydiff);
                            }

                        }

                    } else {
                        if (DocObj.Type == 0) {

                            if (DocObj.getCurrentpage().startx < 0 || DocObj.getCurrentpage().endx > cvdim.w) {                                
                                if(xdiff < 50 && ydiff < 50){
                                    
                                    DocObj.getCurrentpage().pan_update(xdiff, ydiff);
                                    

                                }

                            } else {
                                if(documentcompare){
                                    DocObj.getCurrentpage().pan_update(xdiff, ydiff);
                                    
                                }else{
                                    DocObj.getCurrentpage().pan_update(0, ydiff);
                                    
                                }

                            }

                        }
                        if (DocObj.getCurrentpage().initialscale < (DocObj.getCurrentpage().dscale / DocObj.getCurrentpage().bitmapratio)) {
                            if (DocObj.Type == 0) {
                                if(documentcompare){
                                    DocObj.getCurrentpage().pan_update(xdiff, ydiff);
                                    
                                }else{
                                    DocObj.getCurrentpage().pan_update(0, ydiff);
                                }

                            } else {
                                if(xdiff < 50 && ydiff < 50){
                                    DocObj.getCurrentpage().pan_update(xdiff, ydiff);
                                    
                                }

                            }

                        }

                    }

                };
                
                this.labelhandler = function (mousePos, ev) {
                    if(bLabelsOff){
                        return;
                    }
                    if (!documentopen) {
                        return;
                    }
                    if(DocObj == null || DocObj == undefined){
                        return;
                    }
                    if (tool.started) {
                        return;
                    }

                    id = findmarkupHover(mousePos.x, mousePos.y);
                    var listmnum = DocObj.getmarkupbynumber(id);
                    if (id != -1 && DocObj.markuplist[listmnum].pagenumber == DocObj.getcurPage() && DocObj.markuplist[listmnum].display && DocObj.Drawmarkup) {

                        if (id != -1 && DocObj.markuplist[listmnum].bhaveLink && (DocObj.bMarkupLocked || DocObj.markuplist[listmnum].locked)){
                            ev.target.style.cursor = 'pointer';
                            context.clearRect(0, 0, canvas.width, canvas.height);
                            drawmarkupAll(cntximg);
                            DrawMarkupSelected(context);

                            DocObj.markuplist[listmnum].displayurl(context, mousePos.x, mousePos.y);

                        }else if (id != -1 && DocObj.markuplist[listmnum].type == 10) {
                            ev.target.style.cursor = 'pointer';
                            context.clearRect(0, 0, canvas.width, canvas.height);

                            drawmarkupAll(cntximg);
                            DrawMarkupSelected(context);
                            DocObj.markuplist[listmnum].displaylabel(context, mousePos.x, mousePos.y);

                        } else {
                            ev.target.style.cursor = 'default';
                            context.clearRect(0, 0, canvas.width, canvas.height);

                            drawmarkupAll(cntximg);
                            DrawMarkupSelected(context);
                            DocObj.markuplist[listmnum].displaylabel(context, mousePos.x, mousePos.y);
                        }
                        DocObj.draw3DNav();


                    } else {
                        ev.target.style.cursor = 'default';
                        context.clearRect(0, 0, canvas.width, canvas.height);

                        if(RxCore_GUI_MarkupHover != undefined){
                            RxCore_GUI_MarkupHover.setMarkupHover(null, x, y);
                        }

                        drawmarkupAll(cntximg);
                        DrawMarkupSelected(context);
                        
                        DocObj.draw3DNav();

                    }
                };

                /*this.pressHold = function (ev){

                    //console.log("pressHold event fired!");

                    if(DocObj.getCurrentpage() == undefined){
                        return;
                    }

                    if(DocObj.getCurrentpage().usepdfjs){
                        var scrolldata = DocObj.holdOnScrollbutton();
                    }




                };*/

                this.pointermove = function (ev) {
                    tool.MSPointerMove(ev);
                };
                this.pointerdown = function (ev) {
                    tool.MSPointerDown(ev);
                };

                this.pointerup = function (ev) {
                    tool.MSPointerUp(ev);
                };


                this.MSPointerDown = function (ev) {
                    if(docpagNotReady()){
                        return;
                    }

                    if(readonlymode){
                        DocObj.bMarkupLocked = readonlymode;
                    }

                    ev.preventDefault();
                    ev.stopPropagation();

                    tool.started = true;
                    var mousePos = getMousePos(canvas, ev);
                    var bWithin = MousePosdrwext(mousePos);

                    tool.x0 = mousePos.x;
                    tool.y0 = mousePos.y;
                    prevx = tool.x0;
                    prevy = tool.y0;

                    if (RxCore_GUI_inputEvent != undefined){

                        var inputobj = {
                            mousepos : mousePos,
                            mousebutton : ev.button,
                            dblclick : false,
                            key : tool.curkey,
                            tool : tool.name
                        };
                        
                        RxCore_GUI_inputEvent.setInput(inputobj);
                    }

                    
                    if(DocObj.bMarkupLocked){
                        //id = -1;
                        fmarkup = findmarkup(tool.x0, tool.y0, bCanSelectSpace);
                        id = fmarkup.id;

                        if(fmarkup.blink){
                            tool.started = false;
                            
                            if (RxCore_GUI_MarkupLink != undefined) {
                                RxCore_GUI_MarkupLink.markupLink(DocObj.markuplist[fmarkup.index]);
                            }

                            return;
                        }
                        listentrynum = DocObj.getmarkupbynumber(id);
                        
                    }else{


                        if(tool.binsertpoint && bWithin){
                            setpointbeforeunselect(mousePos);
                        }

                        fmarkup = findmarkup(tool.x0, tool.y0, bCanSelectSpace);
                        id = fmarkup.id;
                        
                        if(fmarkup.locked && fmarkup.blink){
                            tool.started = false;
                            

                            if (RxCore_GUI_MarkupLink != undefined) {
                                RxCore_GUI_MarkupLink.markupLink(DocObj.markuplist[fmarkup.index]);
                            }
                            return;
                        }

                        /*if(fmarkup.blink){
                            tool.started = false;
                            

                            if (RxCore_GUI_MarkupLink != undefined) {
                                RxCore_GUI_MarkupLink.markupLink(DocObj.markuplist[fmarkup.index]);
                            }
                            return;
                        }*/

                        listentrynum = DocObj.getmarkupbynumber(id);
                        
                    }

                    touchpointarr.push(new TouchPoint(ev.pointerId, mousePos.x, mousePos.y));

                    switch (touchpointarr.length) {
                        case 1:
                            if (id == -1) {
                                // tool.started = false;
                                drawmarkupAll(cntximg);
                                DrawMarkupSelected(context);
                                tool.mousedownhandling(ev, mousePos);
                                DocObj.selectedmarkup.selected = false;
                                DocObj.selectedmarkup.edit = false;

                            } else {
                                if (tool.savestate) {
                                    //MarkupSaveState(id, undoModify);

                                    MarkupSaveStateChanged(id, undoModify);
                                    //if markupobject.bmodyfied
                                    //markupobject.bmodyfied = false;

                                }
                                tool.savestate = false;
                                drawmarkupAll(cntximg);
                                DrawMarkupSelected(context);
                                if(DocObj){
                                    DocObj.bMarkupchanged = true;
                                }


                                if (DocObj.markuplist[listentrynum].type == 10) {
                                    DocObj.selectedmarkup.id = listentrynum;
                                    DocObj.selectedmarkup.selected = true;
                                }

    
                                if (DocObj.markuplist[listentrynum].type == 9) {
                                    if (RxCore_GUI_TextInput != undefined) {
                                        if(DocObj.selectedmarkup.edit){
                                            RxCore_GUI_TextInput.operation.edit = false;
                                            RxCore_GUI_TextInput.operation.start = false;
                                            RxCore_GUI_TextInput.operation.create = false;
                                            RxCore_GUI_TextInput.operation.save = true;
                                            RxCore_GUI_TextInput.operation.markup = DocObj.markuplist[DocObj.selectedmarkup.id];

                                            var rect = canvas.getBoundingClientRect();
                                            var xrect  = DocObj.markuplist[DocObj.selectedmarkup.id].rotatedrect.x + rect.left;
                                            var yrect = DocObj.markuplist[DocObj.selectedmarkup.id].rotatedrect.y + rect.top;
                                            //var xrect  = DocObj.markuplist[DocObj.selectedmarkup.id].xscaled + rect.left;
                                            //var yrect = DocObj.markuplist[DocObj.selectedmarkup.id].yscaled + rect.top;

                                            var wrect = DocObj.markuplist[DocObj.selectedmarkup.id].wscaled;
                                            var hrect = DocObj.markuplist[DocObj.selectedmarkup.id].hscaled;

                                            var centercanvX = (canvasowidth / 2);
                                            var centercanvY = (canvasoheight / 2);


                                            var centerx = xrect + (wrect*0.5);
                                            var centery = yrect + (hrect*0.5);

                                            var CanvRotRad = DocObj.getCurrentpage().getpageRotation() * (Math.PI / 180);

                                            if(DocObj.getCurrentpage().getpageRotation() != 0){
                                                var rectcenter = DocObj.markuplist[DocObj.selectedmarkup.id].getRotatedMarkup(centercanvX, centercanvY, centerx, centery, CanvRotRad);
                                                centerx = rectcenter.x;
                                                centery = rectcenter.y;
                                            }

                                            centerx += rect.left;
                                            centery += rect.top;


                                            var txtrect = new Rectangle(xrect,yrect,wrect,hrect);
                                            txtrect.rotation = DocObj.getCurrentpage().getpageRotation();


                                            var pixelRatio = getPixelRatio();
                                            //txtrect.x = txtrect.x / pixelRatio;
                                            //txtrect.y = txtrect.y / pixelRatio;

                                            txtrect.x = ((txtrect.x - rect.left) / pixelRatio) + rect.left;
                                            txtrect.y = ((txtrect.y - rect.top) / pixelRatio) + rect.top;
            
                                            txtrect.w = txtrect.w / pixelRatio;
                                            txtrect.h = txtrect.h / pixelRatio;


                                            RxCore_GUI_TextInput.setTextInput(txtrect);


                                            /*var xrect  = DocObj.markuplist[DocObj.selectedmarkup.id].xscaled + rect.left;
                                            var yrect = DocObj.markuplist[DocObj.selectedmarkup.id].yscaled + rect.top;
                                            var wrect = DocObj.markuplist[DocObj.selectedmarkup.id].wscaled;
                                            var hrect = DocObj.markuplist[DocObj.selectedmarkup.id].hscaled;

                                            var txtrect = new Rectangle(xrect,yrect,wrect,hrect);
                                            RxCore_GUI_TextInput.setTextInput(txtrect);*/

                                            RxCore_GUI_TextInput.operation.edit = false;
                                            DocObj.selectedmarkup.edit = false;
                                        }
                                    }

                                    DocObj.selectedmarkup.id = listentrynum;

                                }

                                if (DocObj.markuplist[listentrynum].editaction == 4 && tool.bdeletepoint) {
                                    DocObj.markuplist[listentrynum].deletepoint(DocObj.markuplist[listentrynum].selectedpoint);
                                    drawmarkupAll(cntximg);
                                    DrawMarkupSelected(context);
                                    tool.bdeletepoint = false;                
                                }
        
                                var binsert = (DocObj.markuplist[listentrynum].editaction == 7);

                                if (binsert && tool.binsertpoint) {
                                    if(!DocObj.markuplist[listentrynum].locked && !DocObj.bMarkupLocked){
                                        DocObj.markuplist[listentrynum].insertpoint();
                                        drawmarkupAll(cntximg);
                                        DrawMarkupSelected(context);
                                        tool.binsertpoint = false;                
    
                                    }
        
                                }
    
                            }


    

                            if (ev.button == 1 && !tool.started) {
                                tool.started = true;
                            }
                            break;
                        case 2:
                            mstouchzoom = true;
                            startx = touchpointarr[0].x;
                            starty = touchpointarr[0].y;
                            startx2 = touchpointarr[1].x;
                            starty2 = touchpointarr[1].y;

                            //establish rectangle from the two points.
                            var xmin = Math.min(startx, startx2);
                            var ymin = Math.min(starty, startx2);
                            startw = Math.abs(startx2 - startx);
                            starth = Math.abs(starty2 - starty);

                            //calculate diagonal between points
                            startwsq = Math.pow(startw, 2);
                            starthsq = Math.pow(starth, 2);
                            startdiagonal = Math.sqrt((startwsq + starthsq));
                            prevdiagonal = startdiagonal;

                            startcenterx = xmin + (startw / 2);
                            startcentery = ymin + (starth / 2);
                            prevcenterx = startcenterx;
                            prevcentery = startcentery;
                            prevh = starth;
                            prevw = startw;

                            startscalesmall = getCurrentPageScale();
                            startscalelarge = getCurrentPageScale();


                            break;

                    }


                };

                this.mousedown = function (ev) {
					
                    ev.preventDefault();
                    ev.stopPropagation();

                    if(docpagNotReady()){
                        return;
                    }
            
                    if(readonlymode){
                        DocObj.bMarkupLocked = readonlymode;
                    }

                    var bnot3dmarkup = get3DViewNotActive();

                    if((DocObj.getCurrentpage().usevector3Dxml && bnot3dmarkup)){
                        return;
                    }

                    tool.started = true;

                    var mousePos = getMousePos(canvas, ev);
                    var bWithin = MousePosdrwext(mousePos);

                    tool.x0 = mousePos.x;
                    tool.y0 = mousePos.y;
                    prevx = tool.x0;
                    prevy = tool.y0;

                    if (RxCore_GUI_inputEvent != undefined){

                        var inputobj = {
                            mousepos : mousePos,
                            mousebutton : ev.button,
                            dblclick : false,
                            key : tool.curkey,
                            tool : tool.name
                        };
                        
                        RxCore_GUI_inputEvent.setInput(inputobj);
                    }


                    if(DocObj.bMarkupLocked){
                        //id = -1;
                        fmarkup = findmarkup(tool.x0, tool.y0, bCanSelectSpace);
                        id = fmarkup.id;
                        if(fmarkup.blink){
                            tool.started = false;
                            
                            
                            if (RxCore_GUI_MarkupLink != undefined) {
                                RxCore_GUI_MarkupLink.markupLink(DocObj.markuplist[fmarkup.index]);
                            }
                            return;
                        }

                        listentrynum = DocObj.getmarkupbynumber(id);
                        
                    }else{

                        if(tool.binsertpoint && bWithin){
                            setpointbeforeunselect(mousePos);
                        }


                        fmarkup = findmarkup(tool.x0, tool.y0, bCanSelectSpace);
                        id = fmarkup.id;


                        if(fmarkup.locked && fmarkup.blink){
                            tool.started = false;
                            

                            if (RxCore_GUI_MarkupLink != undefined) {
                                RxCore_GUI_MarkupLink.markupLink(DocObj.markuplist[fmarkup.index]);
                            }
                            return;
                        }

                        /*if(fmarkup.blink){
                            tool.started = false;
                            
                            if (RxCore_GUI_MarkupLink != undefined) {
                                RxCore_GUI_MarkupLink.markupLink(DocObj.markuplist[fmarkup.index]);
                            }
                            return;
                        }*/

                        listentrynum = DocObj.getmarkupbynumber(id);
                        
                        
                        
                    }

                    if (id == -1) {
                        tool.started = false;
                        
                        if (RxCore_GUI_Markup != undefined) {
                            RxCore_GUI_Markup.showContext = false;
                        }

                        DocObj.selectedmarkup.edit = false;
                        DocObj.selectedmarkup.selected = false;
                        drawmarkupAll(cntximg);
                        DrawMarkupSelected(context);
                        tool.mousedownhandling(ev, mousePos);

                    } else {
                        if (tool.savestate) {

                            if(!bMultiselect){
                                MarkupSaveStateChanged(id, undoModify);
                            }

                            //MarkupSaveState(id, undoModify);
                            
                            //if markupobject.bmodyfied
                            //markupobject.bmodyfied = false;

                        }
                        if(bMultiselect){
                            MarkupSaveStateChangedMulti(undoModify);
                        }

                        tool.savestate = false;
                        drawmarkupAll(cntximg);
                        DrawMarkupSelected(context);
                        if(DocObj){
                            DocObj.bMarkupchanged = true;
                        }

                                                

                        if (DocObj.selectedmarkup.id != -1 && DocObj.markuplist[DocObj.selectedmarkup.id] != undefined) {
                            if(DocObj.markuplist[DocObj.selectedmarkup.id].type == 9){
                                if (RxCore_GUI_TextInput != undefined) {
                                    if(DocObj.selectedmarkup.edit){
                                        RxCore_GUI_TextInput.operation.edit = false;
                                        RxCore_GUI_TextInput.operation.start = false;
                                        RxCore_GUI_TextInput.operation.create = false;
                                        RxCore_GUI_TextInput.operation.save = true;
                                        RxCore_GUI_TextInput.operation.markup = DocObj.markuplist[DocObj.selectedmarkup.id];

                                        var rect = canvas.getBoundingClientRect();
                                        var xrect  = DocObj.markuplist[DocObj.selectedmarkup.id].rotatedrect.x + rect.left;
                                        var yrect = DocObj.markuplist[DocObj.selectedmarkup.id].rotatedrect.y + rect.top;

                                        //var xrect  = DocObj.markuplist[DocObj.selectedmarkup.id].xscaled + rect.left;
                                        //var yrect = DocObj.markuplist[DocObj.selectedmarkup.id].yscaled + rect.top;
                                        var wrect = DocObj.markuplist[DocObj.selectedmarkup.id].wscaled;
                                        var hrect = DocObj.markuplist[DocObj.selectedmarkup.id].hscaled;

                                        var centercanvX = (canvasowidth / 2);
                                        var centercanvY = (canvasoheight / 2);


                                        var centerx = xrect + (wrect*0.5);
                                        var centery = yrect + (hrect*0.5);

                                        var CanvRotRad = DocObj.getCurrentpage().getpageRotation() * (Math.PI / 180);

                                        if(DocObj.getCurrentpage().getpageRotation() != 0){
                                            var rectcenter = DocObj.markuplist[DocObj.selectedmarkup.id].getRotatedMarkup(centercanvX, centercanvY, centerx, centery, CanvRotRad);
                                            centerx = rectcenter.x;
                                            centery = rectcenter.y;
                                        }

                                        centerx += rect.left;
                                        centery += rect.top;


                                        var txtrect = new Rectangle(xrect,yrect,wrect,hrect);
                                        txtrect.rotation = DocObj.getCurrentpage().getpageRotation();


                                        var pixelRatio = getPixelRatio();
                                        //txtrect.x = txtrect.x / pixelRatio;
                                        //txtrect.y = txtrect.y / pixelRatio;

                                        txtrect.x = ((txtrect.x - rect.left) / pixelRatio) + rect.left;
                                        txtrect.y = ((txtrect.y - rect.top) / pixelRatio) + rect.top;
        

                                        txtrect.w = txtrect.w / pixelRatio;
                                        txtrect.h = txtrect.h / pixelRatio;


                                        RxCore_GUI_TextInput.setTextInput(txtrect);


                                        /*var xrect  = DocObj.markuplist[DocObj.selectedmarkup.id].xscaled + rect.left;
                                         var yrect = DocObj.markuplist[DocObj.selectedmarkup.id].yscaled + rect.top;
                                         var wrect = DocObj.markuplist[DocObj.selectedmarkup.id].wscaled;
                                         var hrect = DocObj.markuplist[DocObj.selectedmarkup.id].hscaled;
                                         var txtrect = new Rectangle(xrect,yrect,wrect,hrect);
                                         RxCore_GUI_TextInput.setTextInput(txtrect);*/

                                        DocObj.selectedmarkup.edit = false;
                                    }
                                }
                                DocObj.selectedmarkup.id = listentrynum;

                            }



                        }


                        tool.pointhandler(DocObj.markuplist[listentrynum]);

                        

                        if (DocObj.markuplist[listentrynum].type == 10) {
                            DocObj.selectedmarkup.id = listentrynum;
                        }


                    }
                    if (ev.button == 1 && !tool.started) {
                        tool.started = true;
                    }
                    if (ev.button == 2 && tool.started) {

                        tool.bmovemousebutton = true;

                        //tool.started = true;
                        /*if (RxCore_GUI_Markup != undefined) {
                            RxCore_GUI_Markup.showContext = true;
                            var operation = {created : false, modified : false, deleted : false};
                            var btextboxleader = (DocObj.markuplist[listentrynum].type == 9 &&  DocObj.markuplist[listentrynum].subtype == 2);
                            if(!btextboxleader){
                                RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[listentrynum], operation);
                            }
                            
                        }*/

                    }
                    

                };
                
                this.dblclick = function (ev) {

                    if(docpagNotReady()){
                        return;
                    }
            
                    if(readonlymode){
                        DocObj.bMarkupLocked = readonlymode;
                    }


                    tool.started = true;
                    var mousePos = getMousePos(canvas, ev);
                    tool.x0 = mousePos.x;
                    tool.y0 = mousePos.y;

                    if (RxCore_GUI_inputEvent != undefined){

                        var inputobj = {
                            mousepos : mousePos,
                            mousebutton : ev.button,
                            dblclick : true,
                            key : tool.curkey,
                            tool : tool.name
                        };
                        
                        RxCore_GUI_inputEvent.setInput(inputobj);
                    }


                    var bnot3dmarkup = get3DViewNotActive();


                    if ((DocObj.getCurrentpage().usevector3Dxml && bnot3dmarkup)) {

                        return;
                    }

                    if(DocObj.bMarkupLocked){
                        id = -1;
                        //id = finddoublemarkup(tool.x0, tool.y0);
                        //listentrynum = DocObj.getmarkupbynumber(id);
                    }else{
                        id = finddoublemarkup(tool.x0, tool.y0);
                        listentrynum = DocObj.getmarkupbynumber(id);
                        

                    }

                    if (id != -1 && !DocObj.markuplist[listentrynum].locked) {
                        //MarkupSaveState(id, undoModify);
                        MarkupSaveStateChanged(id, undoModify);
                        //if markupobject.bmodyfied
                        //markupobject.bmodyfied = false;


                        if(DocObj){
                            DocObj.bMarkupchanged = true;
                        }

                        if (DocObj.markuplist[listentrynum].type == 9) {
                            var curmarkuptext = DocObj.markuplist[listentrynum].text;
                            DocObj.selectedmarkup.id = listentrynum;
                            DocObj.selectedmarkup.edit = true;

                            if (RxCore_GUI_Textdiag != undefined) {
                                RxCore_GUI_Textdiag.setTextdiag(DocObj.markuplist[DocObj.selectedmarkup.id].text);
                            }

                            DocObj.markuplist[DocObj.selectedmarkup.id].editTextmarkupRect();

                            /*if (RxCore_GUI_TextInput != undefined) {

                                RxCore_GUI_TextInput.operation.start = false;
                                RxCore_GUI_TextInput.operation.create = false;
                                RxCore_GUI_TextInput.operation.edit = true;
                                RxCore_GUI_TextInput.operation.save = false;

                                var rect = canvas.getBoundingClientRect();

                                var xrect  = DocObj.markuplist[DocObj.selectedmarkup.id].rotatedrect.x + rect.left;
                                var yrect = DocObj.markuplist[DocObj.selectedmarkup.id].rotatedrect.y + rect.top;
                                //var xrect  = DocObj.markuplist[DocObj.selectedmarkup.id].xscaled + rect.left;
                                //var yrect = DocObj.markuplist[DocObj.selectedmarkup.id].yscaled + rect.top;

                                var wrect = DocObj.markuplist[DocObj.selectedmarkup.id].wscaled;
                                var hrect = DocObj.markuplist[DocObj.selectedmarkup.id].hscaled;

                                var centercanvX = (canvasowidth / 2);
                                var centercanvY = (canvasoheight / 2);

                                var centerx = xrect + (wrect*0.5);
                                var centery = yrect + (hrect*0.5);

                                var CanvRotRad = DocObj.getCurrentpage().getpageRotation() * (Math.PI / 180);



                                if(DocObj.getCurrentpage().getpageRotation() != 0){
                                    var rectcenter = DocObj.markuplist[DocObj.selectedmarkup.id].getRotatedMarkup(centercanvX, centercanvY, centerx, centery, CanvRotRad);
                                    centerx = rectcenter.x;
                                    centery = rectcenter.y;

                                }


                                centerx += rect.left;
                                centery += rect.top;


                                //DocObj.getCurrentpage().getpageRotation();
                                var txtrect = new Rectangle(xrect,yrect,wrect,hrect);
                                txtrect.rotation = DocObj.getCurrentpage().getpageRotation();
                                txtrect.rotation = DocObj.markuplist[DocObj.selectedmarkup.id].pagerotation;

                                var pixelRatio = getPixelRatio();
                                txtrect.x = ((txtrect.x - rect.left) / pixelRatio) + rect.left;
                                txtrect.y = ((txtrect.y - rect.top) / pixelRatio) + rect.top;
                                txtrect.w = txtrect.w / pixelRatio;
                                txtrect.h = txtrect.h / pixelRatio;


                                RxCore_GUI_TextInput.setTextInput(txtrect);
                                DocObj.selectedmarkup.edit = false;
                            }*/


                            //setTextContent(DocObj.markuplist[listentrynum].text);
                            //showTextDialog();
                            //MarkupUndoObject.add(textedit,id,oldtext);
                            DrawMarkupSelected(context);
                            drawmarkupAll(cntximg);
                            tool.started = false;
                            

                            //var markuptext = prompt("Edit markup text",curmarkuptext);
                            /*if (markuptext != null && markuptext != ""){
                             //DocObj.markuplist[listentrynum].text = markuptext;
                             }*/

                        }
                        if (DocObj.markuplist[listentrynum].type == 10) {
                            DocObj.selectedmarkup.id = listentrynum;
                            /*DocObj.selectedmarkup.edit = true;
                            if (RxCore_GUI_Notediag != undefined) {
                                RxCore_GUI_Notediag.setNotediag(DocObj.markuplist[listentrynum].text, false);
                            }*/

                            //setNContent(DocObj.markuplist[listentrynum].text);
                            //setNContent(DocObj.markuplist[TempID].text);
                            //showNDialog(false);
                            /*DocObj.markuplist[listentrynum].selected = false;
                            drawmarkupAll(cntximg);
                            DrawMarkupSelected(context);
                            tool.started = false;*/


                        }

                    } else {
                        tool.started = false;
                        
                        tool.savestate = true;

                    }


                };

                this.doubletap = function (touchPos) {
                    //if markup selected go to editmode for polygons and text.

                    if(docpagNotReady()){
                        return;
                    }
            
                    if(readonlymode){
                        DocObj.bMarkupLocked = readonlymode;
                    }

                    if (RxCore_GUI_inputEvent != undefined){

                        var inputobj = {
                            mousepos : touchPos,
                            mousebutton : null,
                            dblclick : true,
                            key : tool.curkey,
                            tool : tool.name
                        };
                        
                        RxCore_GUI_inputEvent.setInput(inputobj);
                    }                    

                    if(DocObj.bMarkupLocked){
                        id = -1;
                        //id = finddoublemarkup(touchPos.x, touchPos.y);
                    }else{
                        id = finddoublemarkup(touchPos.x, touchPos.y);
                        listentrynum = DocObj.getmarkupbynumber(id);
                        

                    }
                    
                    if (id == -1 && !DocObj.markuplist[listentrynum].locked) {
                        tool.started = false;
                        
                        context.clearRect(0, 0, canvas.width, canvas.height);
                        tool.savestate = true;
                    } else {
                        if (tool.savestate) {

                            //MarkupSaveState(id, undoModify);

                            MarkupSaveStateChanged(id, undoModify);
                            //if markupobject.bmodyfied
                            //markupobject.bmodyfied = false;


                            if(DocObj){
                                DocObj.bMarkupchanged = true;
                            }

                        }
                        tool.savestate = false;

                        if (DocObj.markuplist[listentrynum].type == 9) {
                            var curmarkuptext = DocObj.markuplist[listentrynum].text;
                            if (RxCore_GUI_TextInput != undefined) {

                                RxCore_GUI_TextInput.operation.start = false;
                                RxCore_GUI_TextInput.operation.create = false;
                                RxCore_GUI_TextInput.operation.edit = true;
                                RxCore_GUI_TextInput.operation.save = false;
                                RxCore_GUI_TextInput.operation.markup = DocObj.markuplist[DocObj.selectedmarkup.id];

                                var rect = canvas.getBoundingClientRect();
                                /*var xrect  = DocObj.markuplist[DocObj.selectedmarkup.id].xscaled + rect.left;
                                var yrect = DocObj.markuplist[DocObj.selectedmarkup.id].yscaled + rect.top;
                                var wrect = DocObj.markuplist[DocObj.selectedmarkup.id].wscaled;
                                var hrect = DocObj.markuplist[DocObj.selectedmarkup.id].hscaled;

                                var txtrect = new Rectangle(xrect,yrect,wrect,hrect);
                                RxCore_GUI_TextInput.setTextInput(txtrect);*/

                                var xrect = DocObj.markuplist[DocObj.selectedmarkup.id].rotatedrect.x + rect.left;
                                var yrect = DocObj.markuplist[DocObj.selectedmarkup.id].rotatedrect.y + rect.top;

                                //var xrect  = DocObj.markuplist[DocObj.selectedmarkup.id].xscaled + rect.left;
                                //var yrect = DocObj.markuplist[DocObj.selectedmarkup.id].yscaled + rect.top;

                                var wrect = DocObj.markuplist[DocObj.selectedmarkup.id].wscaled;
                                var hrect = DocObj.markuplist[DocObj.selectedmarkup.id].hscaled;

                                var txtrect = new Rectangle(xrect,yrect,wrect,hrect);
                                txtrect.rotation = DocObj.getCurrentpage().getpageRotation();

                                var pixelRatio = getPixelRatio();
                                txtrect.x = ((txtrect.x - rect.left) / pixelRatio) + rect.left;
                                txtrect.y = ((txtrect.y - rect.top) / pixelRatio) + rect.top;
                                txtrect.w = txtrect.w / pixelRatio;
                                txtrect.h = txtrect.h / pixelRatio;


                                RxCore_GUI_TextInput.setTextInput(txtrect);

                                DocObj.selectedmarkup.edit = false;

                                //RxCore_GUI_TextInput.setTextInput(xrect,yrect,wrect,hrect);
                            }

                            /*var markuptext = prompt("Edit markup text", curmarkuptext);
                            if (markuptext != null && markuptext != "") {
                                DocObj.markuplist[listentrynum].text = markuptext;
                                drawmarkupAll(cntximg);
                                DrawMarkupSelected(context);
                                tool.started = false;
                            }*/
                        }
                        if (DocObj.markuplist[listentrynum].type == 10) {
                            DocObj.selectedmarkup.id = listentrynum;
                            //setNContent(DocObj.markuplist[listentrynum].text);
                            //showNDialog(false);
                            /*if (RxCore_GUI_Notediag != undefined) {
                                RxCore_GUI_Notediag.setNotediag(DocObj.markuplist[listentrynum].text, false);
                            }*/

                            /*DocObj.markuplist[listentrynum].selected = false;
                            drawmarkupAll(cntximg);
                            DrawMarkupSelected(context);
                            tool.started = false;*/

                        }

                    }
                };

                this.touchstart = function (ev) {
                    if(docpagNotReady()){
                        return;
                    }
            
                    if(readonlymode){
                        DocObj.bMarkupLocked = readonlymode;
                    }


                    ev.preventDefault();
                    tool.started = true;
                    var touchPos0 = getTouchPos(canvas, ev, 0);
                    var bWithin = MousePosdrwext(touchPos0);

                    var touchx = touchPos0.x;
                    var touchy = touchPos0.y;

                    if (RxCore_GUI_inputEvent != undefined){

                        var inputobj = {
                            mousepos : touchPos0,
                            mousebutton : null,
                            dblclick : false,
                            key : tool.curkey,
                            tool : tool.name
                        };
                        
                        RxCore_GUI_inputEvent.setInput(inputobj);
                    }


                    startx = touchPos0.x;
                    starty = touchPos0.y;

                    prevx = touchx;
                    prevy = touchy;
                    x = touchx;
                    y = touchy;

                    var lastTouch = 0;
                    var now = new Date().getTime();
                    if (tool.LastTouchdata == -1) {
                        lastTouch = now + 1;
                    } else {
                        lastTouch = tool.LastTouchdata;
                    }
                    var delta = now - lastTouch;
                    tool.LastTouchdata = now;


                    switch (ev.targetTouches.length) {
                        case 1:
                            if (delta < 500 && delta > 0) {
                                tool.doubletap(touchPos0);
                            } else {
                                
                                if(DocObj.bMarkupLocked){
                                    //id = -1;
                                    fmarkup = findmarkup(touchx, touchy, bCanSelectSpace);
                                    id = fmarkup.id;

                                    if(fmarkup.blink){
                                        tool.started = false;
                                        
                                        if (RxCore_GUI_MarkupLink != undefined) {
                                            RxCore_GUI_MarkupLink.markupLink(DocObj.markuplist[fmarkup.index]);
                                        }
                                        return;
                                    }

                                    listentrynum = DocObj.getmarkupbynumber(id);
                                    
                                }else{

                                    if(tool.binsertpoint && bWithin){
                                        setpointbeforeunselect(touchPos0);
                                    }


                                    fmarkup = findmarkup(touchx, touchy, bCanSelectSpace);
                                    id = fmarkup.id;

                                    if(fmarkup.blink && fmarkup.locked){
                                        tool.started = false;
                                        
                                        if (RxCore_GUI_MarkupLink != undefined) {
                                            RxCore_GUI_MarkupLink.markupLink(DocObj.markuplist[fmarkup.index]);
                                        }
                                        return;

                                    }
                                    /*if(fmarkup.blink){
                                        tool.started = false;
                                        
                                        if (RxCore_GUI_MarkupLink != undefined) {
                                            RxCore_GUI_MarkupLink.markupLink(DocObj.markuplist[fmarkup.index]);
                                        }
                                        return;
                                    }*/

                                    listentrynum = DocObj.getmarkupbynumber(id);
                                    
                                }
                                
                                if (id == -1) {
                                    tool.started = false;
                                    
                                    //context.clearRect(0, 0, canvas.width, canvas.height);
                                    drawmarkupAll(cntximg);
                                    DrawMarkupSelected(context);
                                    tool.mousedownhandling(ev, touchPos0);

                                    tool.savestate = true;
                                } else {
                                    if (tool.savestate) {
                                        
                                        //MarkupSaveState(id, undoModify);
                                        MarkupSaveStateChanged(id, undoModify);
                                        //if markupobject.bmodyfied
                                        //markupobject.bmodyfied = false;

                                        if(DocObj){
                                            DocObj.bMarkupchanged = true;
                                        }

                                    }
                                    tool.savestate = false;

                                    drawmarkupAll(cntximg);
                                    DrawMarkupSelected(context);
                                    if (DocObj.markuplist[listentrynum].type == 9) {
                                        if (RxCore_GUI_TextInput != undefined) {
                                            if(DocObj.selectedmarkup.edit){
                                                RxCore_GUI_TextInput.operation.edit = false;
                                                RxCore_GUI_TextInput.operation.start = false;
                                                RxCore_GUI_TextInput.operation.create = false;
                                                RxCore_GUI_TextInput.operation.save = true;

                                                RxCore_GUI_TextInput.operation.markup = DocObj.markuplist[DocObj.selectedmarkup.id];

                                                var rect = canvas.getBoundingClientRect();
                                                var xrect  = DocObj.markuplist[DocObj.selectedmarkup.id].rotatedrect.x + rect.left;
                                                var yrect = DocObj.markuplist[DocObj.selectedmarkup.id].rotatedrect.y + rect.top;
                                                //var xrect  = DocObj.markuplist[DocObj.selectedmarkup.id].xscaled + rect.left;
                                                //var yrect = DocObj.markuplist[DocObj.selectedmarkup.id].yscaled + rect.top;

                                                var wrect = DocObj.markuplist[DocObj.selectedmarkup.id].wscaled;
                                                var hrect = DocObj.markuplist[DocObj.selectedmarkup.id].hscaled;

                                                var txtrect = new Rectangle(xrect,yrect,wrect,hrect);
                                                txtrect.rotation = DocObj.getCurrentpage().getpageRotation();

                                                var pixelRatio = getPixelRatio();
                                                txtrect.x = ((txtrect.x - rect.left) / pixelRatio) + rect.left;
                                                txtrect.y = ((txtrect.y - rect.top) / pixelRatio) + rect.top;
                                                txtrect.w = txtrect.w / pixelRatio;
                                                txtrect.h = txtrect.h / pixelRatio;
                

                                                RxCore_GUI_TextInput.setTextInput(txtrect);

                                                /*var xrect  = DocObj.markuplist[DocObj.selectedmarkup.id].xscaled + rect.left;
                                                var yrect = DocObj.markuplist[DocObj.selectedmarkup.id].yscaled + rect.top;
                                                var wrect = DocObj.markuplist[DocObj.selectedmarkup.id].wscaled;
                                                var hrect = DocObj.markuplist[DocObj.selectedmarkup.id].hscaled;
                                                var txtrect = new Rectangle(xrect,yrect,wrect,hrect);
                                                RxCore_GUI_TextInput.setTextInput(txtrect);*/

                                                //RxCore_GUI_TextInput.setTextInput(xrect,yrect,wrect,hrect);
                                            }
                                        }

                                        DocObj.selectedmarkup.id = listentrynum;
                                    }

                                    tool.pointhandler(DocObj.markuplist[listentrynum]);

                                    
            
                                    
                                    if (DocObj.markuplist[listentrynum].type == 10) {
                                        DocObj.selectedmarkup.id = listentrynum;
                                    }

                                }
                            }

                            break;

                        case 2:
                            var touchPos1 = getTouchPos(canvas, ev, 1);
                            startx2 = touchPos1.x;
                            starty2 = touchPos1.y;
                            startw = Math.abs(startx2 - startx);
                            starth = Math.abs(starty2 - starty);
                            startdeltax = startx2 - startx;
                            startdeltay = starty2 - starty;
                            startangle = Math.atan2(startdeltay, startdeltax) * 180 / Math.PI;

                            startwsq = Math.pow(startw, 2);
                            starthsq = Math.pow(starth, 2);

                            startdiagonal = Math.sqrt((startwsq + starthsq));
                            prevdiagonal = startdiagonal;

                            //center of two touchpoints.
                            startcenterx = startx + (startw / 2);
                            startcentery = starty + (starth / 2);
                            prevcenterx = startcenterx;
                            prevcentery = startcentery;
                            prevh = starth;
                            prevw = startw;

                            startscalesmall = getCurrentPageScale();
                            startscalelarge = getCurrentPageScale();

                            break;
                    }


                };


                this.pointhandler = function(markup){

                    var binsert = (markup.editaction == 7);


                    //var bholepointedit = {editaction : undefined, holeindex : undefined, listentrynum : undefined};

                    /*if(markup.holes.length > 0){
                        for(var hi = 0; hi < markup.holes.length; hi++){


                            bholepointedit.editaction = markup.holes[hi].editaction;
                            bholepointedit.holeindex = hi;
                            binsert = (bholepointedit.editaction == 7);
                            //bholepointedit.listentrynum = listentrynum;
                            
                        }
                    }*/

                    /*if (bholepointedit.editaction == 4 && bholepointedit.holeindex != undefined && tool.bdeletepoint) {
                        var hole = markup.holes[bholepointedit.holeindex];
                        hole.deletepoint(hole.selectedpoint);
                        drawmarkupAll(cntximg);
                        DrawMarkupSelected(context);
                        tool.bdeletepoint = false;                


                    }*/


                    
                    if (markup.editaction == 4 && tool.bdeletepoint) {
                        markup.deletepoint(markup.selectedpoint);
                        drawmarkupAll(cntximg);
                        DrawMarkupSelected(context);
                        tool.bdeletepoint = false;                
                    }

                    

                    if (binsert && tool.binsertpoint) {

                        if(!markup.locked && !DocObj.bMarkupLocked){
                            markup.insertpoint();
                            drawmarkupAll(cntximg);
                            DrawMarkupSelected(context);
                            tool.binsertpoint = false;                
                        }

                        /*if (bholepointedit.holeindex != undefined) {

                            var hole = markup.holes[bholepointedit.holeindex];

    
                            if(!hole.locked && !DocObj.bMarkupLocked){
                                hole.insertpoint();
                                drawmarkupAll(cntximg);
                                DrawMarkupSelected(context);
                                tool.binsertpoint = false;                
                            }
    
                        }*/


                    }

                };

                this.MSPointerMove = function (ev) {
                    if(docpagNotReady()){
                        return;
                    }
            
                    if(readonlymode){
                        DocObj.bMarkupLocked = readonlymode;
                    }


                    var snapoverride = false;
                    var i = 0;
                    var mousePos = getMousePos(canvas, ev);
                    
                    tool.findspacehover(mousePos);

                    tool.curmousepos = mousePos;
                    while (i < touchpointarr.length) {

                        if (touchpointarr[i].pointID == ev.pointerId) {
                            touchpointarr[i].x = mousePos.x;
                            touchpointarr[i].y = mousePos.y;
                        }
                        i++;
                    }
                    switch (touchpointarr.length) {
                        case 0:
                            tool.labelhandler(mousePos, ev);
                            if (!tool.started) {
                                return;
                            }

                            break;
                        case 1:
                            if (!tool.started) {
                                return;
                            }
                            var xdiff = prevx - mousePos.x;
                            var ydiff = prevy - mousePos.y;


                            if (ev.buttons == 1) {

                                if (id != -1 && !DocObj.bMarkupLocked && !DocObj.markuplist[listentrynum].locked) {
                                    snapPoint = DocObj.getCurrentpage().getsnap(mousePos);

                                    if (!snapPoint || !snapPoint.found || snapPoint == undefined){
                                        havesnap = false;
                                    }else{
                                        havesnap = snapPoint.found || snapPoint != undefined;
                                    }

                                    if (DocObj.markuplist[listentrynum].editaction == 4) {
                                        if (havesnap) {
                                            if(DocObj.getCurrentpage().getpageRotation() != 0){
                                                rotatedpoint = snap_rotated(snapPoint);
                                            }else{
                                                rotatedpoint = snapPoint;
                                            }

                                            xdiff = prevx - rotatedpoint.x;
                                            ydiff = prevy - rotatedpoint.y;
                                            snapoverride = true;
                                        }


                                        var bWithin = MousePosdrwext(mousePos);
                                        if (bWithin || !bLimMarkupExtent) {
                                            DocObj.markuplist[listentrynum].editpoint(xdiff, ydiff, DocObj.markuplist[listentrynum].selectedpoint);

                                            /*if(RxCore_GUI_MarkupAreaEdit != undefined && DocObj.markuplist[listentrynum].type == 8){
                                                RxCore_GUI_MarkupAreaEdit.onDrawEvent(DocObj.markuplist[listentrynum].dimtext);
                                            }*/
    
                                        }

                                        DrawMarkupSelected(context);
                                        //drawmarkupAll(cntximg);
                                        tool.changedstate = true;

                                    }                                    


                                    var bmoveminsert = (!tool.binsertpoint && DocObj.markuplist[listentrynum].editaction == 7);
                                    var bmovelabel = (!tool.bmovelabel && DocObj.markuplist[listentrynum].editaction == 8);
        
                                    if (DocObj.markuplist[listentrynum].editaction == 1 || bmoveminsert || bmovelabel) {
                                        //modify to allow multiple move
                                        ev.target.style.cursor = 'move';
                                        
                                        if (bMultiselect || DocObj.getMarkupMultiselected()){
                                            //new method in document object to move all selected if multi select is on.
                                            DocObj.moveselected(xdiff, ydiff);

                                        }else{
                                            DocObj.markuplist[listentrynum].move(xdiff, ydiff);

                                        }
                                        
                                        //drawmarkupAll(cntximg);
                                        DrawMarkupSelected(context);

                                       

                                        tool.changedstate = true;


                                    }

                                    if (DocObj.markuplist[listentrynum].editaction == 2) {
                                        if (havesnap) {
                                            if(DocObj.getCurrentpage().getpageRotation() != 0){
                                                var rotatedpoint = snap_rotated(snapPoint);
                                            }else{
                                                rotatedpoint = snapPoint;
                                            }

                                            xdiff = prevx - rotatedpoint.x;
                                            ydiff = prevy - rotatedpoint.y;
                                            snapoverride = true;
                                        }

                                        ev.target.style.cursor = 'nw-resize';
                                        DocObj.markuplist[listentrynum].scale(xdiff, ydiff, DocObj.markuplist[listentrynum].scalecorner, context);

                                        DrawMarkupSelected(context);
                                        //drawmarkupAll(cntximg);
                                        tool.changedstate = true;


                                    }
                                    if (DocObj.markuplist[listentrynum].editaction == 3) {
                                        DocObj.markuplist[listentrynum].rotate(mousePos.x, mousePos.y, DocObj.markuplist[listentrynum].scalecorner);
                                        DrawMarkupSelected(context);
                                        //drawmarkupAll(cntximg);
                                        tool.changedstate = true;

                                    }
                                    
                                    if(DocObj.markuplist[listentrynum].editaction == 6){
                                        ev.target.style.cursor = 'move';
                                        DocObj.markuplist[listentrynum].dimextend(mousePos.x, mousePos.y);
                                        
                                        //drawmarkupAll(cntximg);
                                        DrawMarkupSelected(context);
                                        tool.changedstate = true;

                                    }

                                    if(DocObj.markuplist[listentrynum].editaction == 8 && tool.bmovelabel && !bMarkupNoLabel && !DocObj.markuplist[listentrynum].hidevaluelabel){
                                        ev.target.style.cursor = 'move';
                                        DocObj.markuplist[listentrynum].movelabel(xdiff, ydiff);
                                        
                                        //drawmarkupAll(cntximg);
                                        DrawMarkupSelected(context);
                                        tool.changedstate = true;

                                    }



                                } else {
                                    //cntximg.clearRect(0, 0, canvas.width, canvas.height);
                                    //context.clearRect(0, 0, canvas.width, canvas.height);

                                    tool.movehandler(mousePos, ev);
                                }

                                if (snapoverride && havesnap) {
                                    if(DocObj.getCurrentpage().getpageRotation() != 0){
                                        rotatedpoint = snap_rotated(snapPoint);
                                    }else{
                                        rotatedpoint = snapPoint;
                                    }

                                    prevx = rotatedpoint.x;
                                    prevy = rotatedpoint.y;

                                } else {
                                    prevx = mousePos.x;
                                    prevy = mousePos.y;

                                }
                            } else if (ev.buttons == 4) {
                                DocObj.getCurrentpage().pan_update(xdiff, ydiff);
                                
                                prevx = mousePos.x;
                                prevy = mousePos.y;
                                DrawMarkupSelected(context);
                            } else {

                            }
                            break;
                        case 2:
                            if (!mstouchzoom) {
                                return;
                            }
                            xcur1 = touchpointarr[0].x;
                            ycur1 = touchpointarr[0].y;
                            xcur2 = touchpointarr[1].x;
                            ycur2 = touchpointarr[1].y;

                            //establish rectangle from two points
                            x = Math.min(xcur1, xcur2);
                            y = Math.min(ycur1, ycur2);
                            w = Math.abs(xcur2 - xcur1);
                            h = Math.abs(ycur2 - ycur1);

                            //calculate diagonal from width and height of rectangle
                            wsq = Math.pow(w, 2);
                            hsq = Math.pow(h, 2);
                            diagonal = Math.sqrt((wsq + hsq));
                            //if previous diagonal smaller than current zoomfactor > 1 else smaller

                            var diffdiagonal = diagonal - prevdiagonal;
                            var diagratio = 1 / prevdiagonal;
                            var zoomscalefactor = diagonal * diagratio;

                            //center of two touchpoints.
                            centerx = x + (w / 2);
                            centery = y + (h / 2);

                            xdiff = prevcenterx - centerx;
                            ydiff = prevcentery - centery;
                            var wdiff = prevw - w;
                            var hdiff = prevh - h;

                            if (diffdiagonal > 2 || diffdiagonal < -2) {
                                DocObj.getCurrentpage().ZoomIn(zoomscalefactor, false, false);

                            }


                            prevdiagonal = diagonal;
                            prevcenterx = centerx;
                            prevcentery = centery;
                            prevw = w;
                            prevh = h;
                            break;
                    }


                    //console.log(ev.buttons);
                    //pan_update(xdiff, ydiff);
                    //movemarkup(id,xdiff,ydiff)


                };


                this.mouseout = function(ev){

                    //notPressingDown(ev);
                    
                    tool.bmovemousebutton = false;

                    tool.mouseuphandler(ev);
                    //ev.target.style.cursor = 'default';
                    /*if(DocObj.getCurrentpage().usepdfjs){
                        DocObj.mouseOffScroll();
                    }*/


                    /*var mousePos = getMousePos(canvas, ev);
                    tool.x0 = mousePos.x;
                    tool.y0 = mousePos.y;
                    
                    prevx = tool.x0;
                    prevy = tool.y0;*/
                    id = -1;

                };

                this.mousemove = function (ev) {
                    if(docpagNotReady()){
                        return;
                    }
            
                    if(readonlymode){
                        DocObj.bMarkupLocked = readonlymode;
                    }

                    var bwithinbounds = true;
                    var snapoverride = false;
                    var mousePos = getMousePos(canvas, ev);

                    tool.findspacehover(mousePos);


                    tool.curmousepos = mousePos;
                    tool.labelhandler(mousePos, ev);
                    

                    if (!tool.started) {
                        return;
                        
                    }

                    /*if(DocObj.markuplist[listentrynum] && DocObj.markuplist[listentrynum].parentmarkupnumber !== 0) {
                        var parentmarkupobj = DocObj.markuplist.find(obj => obj.markupnumber === DocObj.markuplist[listentrynum].parentmarkupnumber);
 
                        let iswithin = true;
                        for(let pointIdx=0; pointIdx < parentmarkupobj.points.length; pointIdx++) {
                            let point = parentmarkupobj.points[pointIdx];
                            if(parentmarkupobj.ispointwithinarea(mousePos.x, mousePos.y) === false) {
                                iswithin = false;
                                break;
                            }
                        }
                        if(iswithin === false) {
                            return;
                        }
                    }*/                    



                    
                    var xdiff = prevx - mousePos.x;
                    var ydiff = prevy - mousePos.y;


                    if (ev.button == 0) {

                        if (id != -1 && !DocObj.bMarkupLocked && !DocObj.markuplist[listentrynum].locked && !tool.bmovemousebutton) {

                            snapPoint = DocObj.getCurrentpage().getsnap(mousePos);
                            if (!snapPoint || !snapPoint.found || snapPoint == undefined){
                                havesnap = false;
                            }else{
                                havesnap = snapPoint.found || snapPoint != undefined;
                            }


                            /*var bholepointedit = {editaction : undefined, holeindex : undefined, listentrynum : undefined};
                            if(DocObj.markuplist[listentrynum].holes.length > 0){
                                for(var hi = 0; hi < DocObj.markuplist[listentrynum].holes.length; hi++){

                                    if (DocObj.markuplist[listentrynum].holes[hi].editaction == 4) {

                                        bholepointedit.editaction = 4;
                                        bholepointedit.holeindex = hi;
                                        bholepointedit.listentrynum = listentrynum;



                                    }

                                    
                                }
                            }*/

                            


                            if (DocObj.markuplist[listentrynum].editaction == 4) {
                                if (havesnap) {
                                    if(DocObj.getCurrentpage().getpageRotation() != 0){
                                        rotatedpoint = snap_rotated(snapPoint);
                                    }else{
                                        rotatedpoint = snapPoint;
                                    }

                                    xdiff = prevx - rotatedpoint.x;
                                    ydiff = prevy - rotatedpoint.y;
                                    //xdiff = snapPoint.x;
                                    //ydiff = snapPoint.y;

                                    snapoverride = true;
                                }

                                var bWithin = MousePosdrwext(mousePos);
                                if (bWithin || !bLimMarkupExtent) {
                                    //xdiff = prevx - mousePos.x;
                                    //ydiff = prevy - mousePos.y;
                                    if(DocObj.getCurrentpage().getpageRotation() == 0){
                                        DocObj.markuplist[listentrynum].snapTo(mousePos.x, mousePos.y, DocObj.markuplist[listentrynum].selectedpoint);

/*                                         if(DocObj.markuplist[listentrynum].holes.length > 0){
                                            for(var hi = 0; hi < DocObj.markuplist[listentrynum].holes.length; hi++){
                                                DocObj.markuplist[listentrynum].holes[hi].snapTo(mousePos.x, mousePos.y, DocObj.markuplist[listentrynum].holes[hi].selectedpoint);
                                            }
                                        }
 */
                                    }else{
                                        var rotmousepoint = mouse_rotated(mousePos.x, mousePos.y);
                                        //function mouse_rotated(x,y){

                                        DocObj.markuplist[listentrynum].snapTo(rotmousepoint.x, rotmousepoint.y, DocObj.markuplist[listentrynum].selectedpoint);

/*                                         if(DocObj.markuplist[listentrynum].holes.length > 0){
                                            for(var hi = 0; hi < DocObj.markuplist[listentrynum].holes.length; hi++){
                                                DocObj.markuplist[listentrynum].holes[hi].snapTo(rotmousepoint.x, rotmousepoint.y, DocObj.markuplist[listentrynum].holes[hi].selectedpoint);
                                            }
                                        }
 */

                                    }

                                                                        
                                    if (havesnap) {
                                        if(DocObj.getCurrentpage().getpageRotation() != 0){
                                            //rotatedpoint = snap_rotated(snapPoint);
                                            rotatedpoint = snapPoint;

                                            if (DocObj.foxitdoc != null) {
                        
                                                rotatedpoint = DocObj.markuplist[listentrynum].AdjustPointForRotation({x : rotatedpoint.x, y : rotatedpoint.y});
                                                //x = snappoint.x;
                                                //y = snappoint.y;
                                            
                                            }                    


                                        }else{
                                            rotatedpoint = snapPoint;
                                        }
                                        DocObj.markuplist[listentrynum].snapTo(rotatedpoint.x, rotatedpoint.y, DocObj.markuplist[listentrynum].selectedpoint);

/*                                         if(DocObj.markuplist[listentrynum].holes.length > 0){
                                            for(var hi = 0; hi < DocObj.markuplist[listentrynum].holes.length; hi++){
                                                DocObj.markuplist[listentrynum].holes[hi].snapTo(rotatedpoint.x, rotatedpoint.y, DocObj.markuplist[listentrynum].holes[hi].selectedpoint);
                                            }
                                        }
 */

                                    }else{
                                        DocObj.markuplist[listentrynum].editpoint(xdiff, ydiff, DocObj.markuplist[listentrynum].selectedpoint);

/*                                         if(DocObj.markuplist[listentrynum].holes.length > 0){
                                            for(var hi = 0; hi < DocObj.markuplist[listentrynum].holes.length; hi++){
                                                DocObj.markuplist[listentrynum].holes[hi].editpoint(xdiff, ydiff, DocObj.markuplist[listentrynum].holes[hi].selectedpoint);
                                            }
                                        }
 */

                                        /*if(RxCore_GUI_MarkupAreaEdit != undefined && DocObj.markuplist[listentrynum].type == 8){
                                            
                                            RxCore_GUI_MarkupAreaEdit.onDrawEvent(DocObj.markuplist[listentrynum].dimtext);
                                        }*/

                                    }
                                }
                                DrawMarkupSelected(context);
                                //drawmarkupAll(cntximg);
                                tool.changedstate = true;
                            }

                            var bmoveminsert = (!tool.binsertpoint && DocObj.markuplist[listentrynum].editaction == 7);
                            var bmovelabel = (!tool.bmovelabel && DocObj.markuplist[listentrynum].editaction == 8);

                            

                            if (DocObj.markuplist[listentrynum].editaction == 1 || bmoveminsert || bmovelabel) {
                                
                                //modify to allow multiple move
                                ev.target.style.cursor = 'move';

                                if (bMultiselect || DocObj.getMarkupMultiselected()){
                                    //new method in document object to move all selected if multi select is on.
                                    DocObj.moveselected(xdiff, ydiff);
                                }else{
                                    DocObj.markuplist[listentrynum].move(xdiff, ydiff);
                                }


                                //drawmarkupAll(cntximg);
                                DrawMarkupSelected(context);

                                


                                tool.changedstate = true;


                            }

                            if (DocObj.markuplist[listentrynum].editaction == 2) {

                                if (havesnap) {
                                    if(DocObj.getCurrentpage().getpageRotation() != 0){
                                        var rotatedpoint = snap_rotated(snapPoint);
                                    }else{
                                        rotatedpoint = snapPoint;
                                    }

                                    xdiff = prevx - rotatedpoint.x;
                                    ydiff = prevy - rotatedpoint.y;
                                    snapoverride = true;
                                }

                                if (DocObj.markuplist[listentrynum].scalecorner == 1 || DocObj.markuplist[listentrynum].scalecorner == 4) {
                                    ev.target.style.cursor = 'nw-resize';
                                } else {
                                    ev.target.style.cursor = 'ne-resize';
                                }

                                if (!havesnap){
                                    DocObj.markuplist[listentrynum].scale(xdiff, ydiff, DocObj.markuplist[listentrynum].scalecorner, context);
                                }else{
                                    if (DocObj.markuplist[listentrynum].type == 6 || DocObj.markuplist[listentrynum].type == 7){
                                        DocObj.markuplist[listentrynum].snapToCorner(snapPoint.x, snapPoint.y, DocObj.markuplist[listentrynum].scalecorner);
                                        //new snapto here.
                                    }else{
                                        DocObj.markuplist[listentrynum].scale(xdiff, ydiff, DocObj.markuplist[listentrynum].scalecorner, context);
                                    }
                                    
                                }
                                

                                DrawMarkupSelected(context);
                                //drawmarkupAll(cntximg);
                                tool.changedstate = true;


                            }
                            if (DocObj.markuplist[listentrynum].editaction == 3) {
                                DocObj.markuplist[listentrynum].rotate(mousePos.x, mousePos.y, DocObj.markuplist[listentrynum].scalecorner);
                                DrawMarkupSelected(context);
                                //drawmarkupAll(cntximg);
                                tool.changedstate = true;

                            }
                            
                            if(DocObj.markuplist[listentrynum].editaction == 6){
                                ev.target.style.cursor = 'move';
                                DocObj.markuplist[listentrynum].dimextend(mousePos.x, mousePos.y);
                                
                                //drawmarkupAll(cntximg);
                                DrawMarkupSelected(context);
                                tool.changedstate = true;

                            }

                            if(DocObj.markuplist[listentrynum].editaction == 8 && tool.bmovelabel && !DocObj.markuplist[listentrynum].hidevaluelabel){
                                ev.target.style.cursor = 'move';
                                DocObj.markuplist[listentrynum].movelabel(xdiff, ydiff);
                                
                                //drawmarkupAll(cntximg);
                                DrawMarkupSelected(context);
                                tool.changedstate = true;

                            }


                        } else {
                            
                            //cntximg.clearRect(0, 0, canvas.width, canvas.height);
                            //context.clearRect(0, 0, canvas.width, canvas.height);
                            tool.movehandler(mousePos, ev);
                        }

                        if (snapoverride && havesnap) {
                            if(DocObj.getCurrentpage().getpageRotation() != 0){
                                rotatedpoint = snap_rotated(snapPoint);
                            }else{
                                rotatedpoint = snapPoint;
                            }

                            prevx = rotatedpoint.x;
                            prevy = rotatedpoint.y;

                        } else {
                            prevx = mousePos.x;
                            prevy = mousePos.y;

                        }
                    } else if (ev.button == 1) {
                        DocObj.getCurrentpage().pan_update(xdiff, ydiff);
                        
                        prevx = mousePos.x;
                        prevy = mousePos.y;
                        DrawMarkupSelected(context);

                    } else {

                    }
                    //pan_update(xdiff, ydiff);
                    //movemarkup(id,xdiff,ydiff)


                    //prevx = mousePos.x;
                    //prevy = mousePos.y;

                };

                this.touchmove = function (ev) {
                    if(docpagNotReady()){
                        return;
                    }
          
                    if(readonlymode){
                        DocObj.bMarkupLocked = readonlymode;
                    }

                    ev.preventDefault();


                    var touchPos0 = getTouchPos(canvas, ev, 0);


                    var eventx = touchPos0.x;
                    var eventy = touchPos0.y;
                    xcur1 = touchPos0.x;
                    ycur1 = touchPos0.y;

                    var xdiff = prevx - xcur1;
                    var ydiff = prevy - ycur1;

                    switch (ev.targetTouches.length) {
                        case 1:
                            if (id != -1 && !DocObj.bMarkupLocked && !DocObj.markuplist[listentrynum].locked) {
                                if (!tool.started) {
                                    return;
                                }


                                var bmoveminsert = (!tool.binsertpoint && DocObj.markuplist[listentrynum].editaction == 7);
                                var bmovelabel = (!tool.bmovelabel && DocObj.markuplist[listentrynum].editaction == 8);
    

                                if (DocObj.markuplist[listentrynum].editaction == 1 || bmoveminsert || bmovelabel) {
                                    //modify to allow multiple move
                                    if (bMultiselect || DocObj.getMarkupMultiselected()){
                                        //new method in document object to move all selected if multi select is on.
                                        DocObj.moveselected(xdiff, ydiff);
                                    }else{
                                        DocObj.markuplist[listentrynum].move(xdiff, ydiff);
                                    }

                                    DrawMarkupSelected(context);


                                    //drawmarkupAll(cntximg);
                                    tool.changedstate = true;
                                }

                                if (DocObj.markuplist[listentrynum].editaction == 2) {
                                    DocObj.markuplist[listentrynum].scale(xdiff, ydiff, DocObj.markuplist[listentrynum].scalecorner, context);
                                    DrawMarkupSelected(context);
                                    //drawmarkupAll(cntximg);
                                    tool.changedstate = true;
                                }
                                if (DocObj.markuplist[listentrynum].editaction == 3) {
                                    DocObj.markuplist[listentrynum].rotate(eventx, eventy, DocObj.markuplist[listentrynum].scalecorner);
                                    DrawMarkupSelected(context);
                                    //drawmarkupAll(cntximg);
                                    tool.changedstate = true;

                                }
                                if (DocObj.markuplist[listentrynum].editaction == 4) {
                                    DocObj.markuplist[listentrynum].editpoint(xdiff, ydiff, DocObj.markuplist[listentrynum].selectedpoint);
                                    
                                    /*if(RxCore_GUI_MarkupAreaEdit != undefined && DocObj.markuplist[listentrynum].type == 8){
                                        RxCore_GUI_MarkupAreaEdit.onDrawEvent(DocObj.markuplist[listentrynum].dimtext);
                                    }*/


                                    DrawMarkupSelected(context);
                                    //drawmarkupAll(cntximg);
                                    tool.changedstate = true;

                                }
                                if(DocObj.markuplist[listentrynum].editaction == 6){
                                    ev.target.style.cursor = 'move';
                                    DocObj.markuplist[listentrynum].dimextend(touchPos0.x, touchPos0.y);
                                    
                                    //drawmarkupAll(cntximg);
                                    DrawMarkupSelected(context);
                                    tool.changedstate = true;
    
                                }
                                
                                if(DocObj.markuplist[listentrynum].editaction == 8 && tool.bmovelabel && !bMarkupNoLabel && !DocObj.markuplist[listentrynum].hidevaluelabel){
                                    ev.target.style.cursor = 'move';
                                    DocObj.markuplist[listentrynum].movelabel(xdiff, ydiff);
                                    
                                    //drawmarkupAll(cntximg);
                                    DrawMarkupSelected(context);
                                    tool.changedstate = true;

                                }



                            } else {
                                //cntximg.clearRect(0, 0, canvas.width, canvas.height);
                                //context.clearRect(0, 0, canvas.width, canvas.height);
                                //tool.movehandler(touchPos0,ev);
                                //tool.panhandle(xdiff,ydiff);
                                if (DocObj.getCurrentpage().usevectorxml) {
                                    if (DocObj.getCurrentpage().initialscale < DocObj.getCurrentpage().dscalevector) {
                                        if(xdiff < 50 && ydiff < 50){
                                            DocObj.getCurrentpage().pan_update(xdiff, ydiff);
                                        }
            
                                    }
            
                                } else {
                                    if (DocObj.Type == 0) {
                                        if (DocObj.getCurrentpage().startx < 0 || DocObj.getCurrentpage().endx > canvas.width) {
                                            if(xdiff < 50 && ydiff < 50){
                                                DocObj.getCurrentpage().pan_update(xdiff, ydiff);
                                                
                                            }
            
                                        } else {
                                            if(documentcompare){
                                                DocObj.getCurrentpage().pan_update(xdiff, ydiff);
                                                
                                            }else{
                                                DocObj.getCurrentpage().pan_update(0, ydiff);
                                                
                                            }
            
                                        }
            
                                    }
                                    if (DocObj.getCurrentpage().initialscale < (DocObj.getCurrentpage().dscale / DocObj.getCurrentpage().bitmapratio)) {
                                        if (DocObj.Type == 0) {
                                            if(documentcompare){
                                                DocObj.getCurrentpage().pan_update(xdiff, ydiff);
                                                
                                            }else{
                                                DocObj.getCurrentpage().pan_update(0, ydiff);
                                                
                                            }
            
                                        } else {
                                            if(xdiff < 50 && ydiff < 50){
                                                DocObj.getCurrentpage().pan_update(xdiff, ydiff);
                                                
                                            }
            
                                        }
            
                                    }
            
                                }                                
                                
                            }


                            prevx = touchPos0.x;
                            prevy = touchPos0.y;
                            break;

                        case 2:
                            var touchPos1 = getTouchPos(canvas, ev, 1);
                            xcur2 = touchPos1.x;
                            ycur2 = touchPos1.y;
                            w = Math.abs(xcur2 - xcur1);
                            h = Math.abs(ycur2 - ycur1);
                            wsq = Math.pow(w, 2);
                            hsq = Math.pow(h, 2);
                            diagonal = Math.sqrt((wsq + hsq));
                            var diffdiagonal = diagonal - prevdiagonal;

                            //

                            /* Andriy:: Do we really need scalechanged var? */
                            var scalechanged = false;

                            var diagratio = 1 / prevdiagonal;
                            var zoomscalefactor = diagonal * diagratio;
                            var deltax = xcur2 - xcur1;
                            var deltay = ycur2 - ycur1;
                            curangle = Math.atan2(deltay, deltax) * 180 / Math.PI;
                            diffangle = curangle - startangle;
                            //center of two touchpoints.
                            centerx = xcur1 + (w / 2);
                            centery = ycur1 + (h / 2);
                            var zoomcenter = {x:centerx, y:centery};

                            var wdiff = prevw - w;
                            var hdiff = prevh - h;

                            if (diffdiagonal > 2 || diffdiagonal < -2) {

                                if (zoomscalefactor < 1){
                                    var invertzoomscale = 1 / zoomscalefactor;
                                    var bUseZoomOut = true;
                                }else{
                                    invertzoomscale = zoomscalefactor;
                                    bUseZoomOut = false;
                                }

                                if (bUseZoomOut){
                                    if(tool.belowlimitExtent(invertzoomscale)){
                                        DocObj.getCurrentpage().ZoomOut(invertzoomscale, false, false, zoomcenter);
                                        scalechanged = true;
                                    }else{
                                        scalechanged = false;    
                                    }
                                }else{
                                    DocObj.getCurrentpage().ZoomIn(invertzoomscale, false, false, zoomcenter);
                                }


                                /*if(tool.belowlimitExtent(invertzoomscale)){
                                    
                                    scalechanged = true;
                                }else{
                                    scalechanged = false;
                                }*/    
                                
                                prevdiagonal = diagonal;
                                //scalechanged = true;
                                /*if (DocObj.getCurrentpage().usepdfjs) {
                                    //var curscale = DocObj.getCurrentpage().curpagescale * DocObj.getCurrentpage().dscalepdf;
                                    
                                    var pagedim = DocObj.getCurrentpage().getpagedim();

                                    if((pagedim.w * zoomscalefactor) < canvasowidth && (pagedim.h * zoomscalefactor) < canvasoheight){
                                        scalechanged = false;
                                        DocObj.getCurrentpage().zoomall();
                                    }

                                    //var cwidth = DocObj.getCurrentpage().endx
                                    //if (DocObj.getCurrentpage().endx - )
                                    


                                }else if (DocObj.getCurrentpage().usevectorxml) {

                                
                                    if (DocObj.getCurrentpage().initialscale > DocObj.getCurrentpage().dscalevector) {
                                        scalechanged = false;
                                        DocObj.getCurrentpage().zoomall();

                                    }


                                } else {
                                    if (DocObj.getCurrentpage().initialscale > (DocObj.getCurrentpage().dscale / DocObj.getCurrentpage().bitmapratio)) {
                                        scalechanged = false;
                                        DocObj.getCurrentpage().zoomall();
                                    }

                                }*/

                            }
                            if (Math.abs(diffangle) > 10) {
                                // DocObj.getCurrentpage().drotation = diffangle;
                            }


                            break;

                    }

                    prevcenterx = centerx;
                    prevcentery = centery;
                    prevw = w;
                    prevh = h;
                    prevx = xcur1;
                    prevy = ycur1;


                };

                this.keyup = function (ev){
                    if (typeof nScrollKeyNum.indexOf === "function"){
                        if (nScrollKeyNum.indexOf(ev.keyCode) >= 0){
                            tool.scrollkeyOn = false;
                        }
    
                    }

                   
                    switch (ev.keyCode) {
                        case nScrollKeyNum:
                            tool.scrollkeyOn = false;
                            break;

                    }

                    tool.curkey = null;

                };
                this.keydown = function (ev) {
                    //ev.preventDefault();
                    
                    if(docpagNotReady()){
                        return;
                    }
            

                    tool.curkey = ev.keyCode;

                    if (RxCore_GUI_inputEvent != undefined){

                        var inputobj = {
                            mousepos : null,
                            mousebutton : null,
                            dblclick : false,
                            key : tool.curkey,
                            tool : tool.name
                        };
                        
                        RxCore_GUI_inputEvent.setInput(inputobj);
                    }

                    
                    if(readonlymode){
                        DocObj.bMarkupLocked = readonlymode;
                    }


                    if (DocObj.bMarkupLocked) {
                        return;
                    }

                    if (typeof nScrollKeyNum.indexOf === "function"){
                        if (nScrollKeyNum.indexOf(ev.keyCode) >= 0){
                            tool.scrollkeyOn = true;
                        }
                    }


                    switch (ev.keyCode) {
                        case nScrollKeyNum:
                            tool.scrollkeyOn = true;
                            break;
                        case 13:
                            break;
                        case 8:
                            break;
                        case 46:
                            if(!bMultiselect){
                                if(listentrynum == - 1){
                                    break;
                                }

                                if(DocObj.markuplist[listentrynum] == undefined){
                                    break;
                                }

                            }

                            if(listentrynum != - 1){
                                if(DocObj.markuplist[listentrynum].type == 9 && DocObj.markuplist[listentrynum].selectedit){
                                    break;
                                }
                            }

                            if(bNoteFocus){
                                break;
                            }

                            deletemarkup();
                            break;

                    }

                };

                this.MSPointerUp = function (ev) {

                    if(docpagNotReady()){
                        return;
                    }


                    tool.bmovemousebutton = false;
                    //ev.target.style.cursor = 'default';
                    touchpointarr.pop();

                    if (tool.started) {
                        //tool.mousemove(ev);
                        if (id != -1) {
                            if (tool.changedstate) {
                                tool.savestate = true;



                            }
                            if (DocObj.markuplist[listentrynum].editaction == 4) {
                                DocObj.markuplist[listentrynum].findrectangle();
                                //drawmarkupAll(cntximg);

                            }
                            tool.started = false;
                            
                        } else {
                            tool.mouseuphandler(ev);
                        }


                    }

                };
                this.mouseup = function (ev) {
                    if(docpagNotReady()){
                        return;
                    }

                    //ev.target.style.cursor = 'default';

                    tool.bmovemousebutton = false;

                    if (tool.started) {
                        //tool.mousemove(ev);
                        if (id != -1) {
                            if (tool.changedstate) {
                                tool.savestate = true;


                            }
                            if (DocObj.markuplist[listentrynum].editaction == 4) {
                                DocObj.markuplist[listentrynum].findrectangle();
                                //drawmarkupAll(cntximg);

                            }
                            tool.started = false;
                            
                        } else {
                            tool.mouseuphandler(ev);
                        }

                        //tool.started = false;

                    }
                };
                
                this.touchcancel = function(ev){
                    if(docpagNotReady()){
                        return;
                    }
                    ev.preventDefault();
                    if (tool.started) {
                        if (id != -1) {
                            if (tool.changedstate) {
                                tool.savestate = true;


                            }
                            if (DocObj.markuplist[listentrynum].editaction == 4) {
                                DocObj.markuplist[listentrynum].findrectangle();
                                //drawmarkupAll(cntximg);

                            }
                            tool.started = false;
                            
                            if (DocObj.Type == 0 && !documentcompare) {

                                if (DocObj.currentpage == 0) {
                                    /*while (DocObj.getCurrentpage().starty > 0) {
                                        DocObj.getCurrentpage().pan_update(0, 1);
                                        
                                    }*/


                                }
                                if (DocObj.currentpage == DocObj.NumPages - 1) {
                                    /*while (DocObj.getCurrentpage().endy < canvas.height) {
                                        DocObj.getCurrentpage().pan_update(0, -1);
                                        
                                    }*/

                                }
                            }
                        } else {
                            tool.mouseuphandler(ev);
                        }
                        //tool.touchmove(ev);


                    }
                };                

                this.touchend = function (ev) {
                    if(docpagNotReady()){
                        return;
                    }
                    ev.preventDefault();
                    if (tool.started) {
                        if (id != -1) {
                            if (tool.changedstate) {
                                tool.savestate = true;


                            }
                            if (DocObj.markuplist[listentrynum].editaction == 4) {
                                DocObj.markuplist[listentrynum].findrectangle();
                                //drawmarkupAll(cntximg);

                            }
                            tool.started = false;
                            
                            if (DocObj.Type == 0 && !documentcompare) {

                                if (DocObj.currentpage == 0) {
                                    /*while (DocObj.getCurrentpage().starty > 0) {
                                        DocObj.getCurrentpage().pan_update(0, 1);
                                        
                                    }*/


                                }
                                if (DocObj.currentpage == DocObj.NumPages - 1) {
                                    /*while (DocObj.getCurrentpage().endy < canvas.height) {
                                        DocObj.getCurrentpage().pan_update(0, -1);
                                        
                                    }*/

                                }
                            }
                        } else {
                            tool.mouseuphandler(ev);
                        }
                        //tool.touchmove(ev);


                    }
                };

            };

            // The arrow tool.
            tools.arrow = function (params) {

                var tool = this;
                var prevx = 0,
                    prevy = 0;

                var limprevx = 0,
                    limprevy = 0;
                    
                var arrowmarkupobj;
                //ft 08.08.2018 changed from separate index to direct array length
                var curmarkup = DocObj.markuplist.length;
                //var curmarkup = DocObj.nummarkups;
                this.started = false;
                this.name = 'arrow';
                

                this.anglelengthsupport = true;
                this.refreshSupport = false;

                var havesnap = false;
                var tempOrtho = false;
                var snapPoint;
                var type = params.p1;

                this.startx = 0;
                this.starty = 0;
                this.orgscale = 1.0;
                this.orgdx = 0.0;
                this.orgdy = 0.0;
                this.bfirstpointset = false;

                this.bUsemouseinput = true;
                
                //this.curmousepos = {x:0,y:0};

                this.curmousepos = {x:0,y:0,xus:0,yus:0};

                if(type == 6){
                    //ev.target.style.cursor = 'default';
                    
                    setCursor(getMarkupCursor('callout'));

                }else{
                    //ev.target.style.cursor = 'crosshair';
                    
                    setCursor(getMarkupCursor('arrow'));
                }



                this.setAngleLength = function(angle, length, bAutoAdvance, type){
                    this.bUsemouseinput = false;
                    if (arrowmarkupobj != undefined){

                        arrowmarkupobj.x = tool.x0;
                        arrowmarkupobj.y = tool.y0;
                        var curx2 = arrowmarkupobj.w;
                        var cury2 = arrowmarkupobj.h;

                        var secondpoint = arrowmarkupobj.setAnglelength(length, angle, type);
                        //add state to markup object to change rotate adjustment bUsemouseinput
                        context.clearRect(0, 0, canvas.width, canvas.height);
                        
                        if (MousePosdrwext({x:secondpoint.w,y:secondpoint.h})){
                            arrowmarkupobj.w = secondpoint.w;
                            arrowmarkupobj.h = secondpoint.h;
                            tool.setdimoffset(false);
                            arrowmarkupobj.drawme(context);
    
                        }else{
                            arrowmarkupobj.w = curx2;
                            arrowmarkupobj.h = cury2;

                            this.bUsemouseinput = true;
                            arrowmarkupobj.drawme(context);
                            arrowmarkupobj.bUsemouseinput = true;
                            if (RxCore_GUI_markupParamsError != undefined){
                                RxCore_GUI_markupParamsError.onDrawError('outside');
                            }

                        }

                    }


                };

                this.apply = function(){
                    context.clearRect(0, 0, canvas.width, canvas.height);
                    tool.setdimoffset(false);
                    arrowmarkupobj.drawme(context);
                    if (!bMultimarkupadd) {

                        bMarkupcrated = true;
                        //ft 08.08.2018 changed from separate index to direct array length
                        //nMarkupcreated = DocObj.nummarkups;
                        //nMarkupcreated = DocObj.markuplist.length;
                    }
                    nMarkupcreated = DocObj.markuplist.length;
                    
                    arrowmarkupobj.savemetolistDraw();
                    //arrowmarkupobj.savemetolistLoad();
                    arrowmarkupobj = null;
                    tool.started = false;
                    //markupobject.markupnumber = DocObj.nummarkups;         DocObj.markuplist[markupobject.markupnumber] = this;
                    img_update();


                    if(bAlwaysMarkupCallback){
                        if (RxCore_GUI_Markup != undefined) {
                            
                            RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[nMarkupcreated], {created : true, modified : false, deleted : false, multi : bMultimarkupadd});
                        }
            
                    }else{
                        if (!bMultimarkupadd) {
                            //need to move to connection object.
                            //markupcreated();
                            if (RxCore_GUI_Markup != undefined) {
                                //var operation = {created : true, modified : false, deleted : false};
                                RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[nMarkupcreated], {created : true, modified : false, deleted : false});
                                //RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[index],true);
                            }
                        }
                    }

                };
                
                this.scrollhandling = function (movey) {
                    if (!documentopen || DocObj.getCurrentpage().usevector3Dxml) {
                        return;
                    }

                    if (movey < 0) {


                        if (DocObj.getCurrentpage().usepdfjs && DocObj.pages.length > 1) {
                            
                            if (DocObj.currentpage == DocObj.NumPages - 1) {
                                /*if (DocObj.getCurrentpage().endy <= canvas.height) {
                                    movey = 0;
                                }*/
                            }
                            DocObj.getCurrentpage().pan_update(0, -movey);

                        } else {

                            if(belowlimitExtentEx(1.1, tool.curmousepos)){
                                DocObj.getCurrentpage().ZoomOut(1.1, false, false, tool.curmousepos);

                                if(tool.started){
                                    tool.setdimoffset(false);
                                }
                            }else{
                                RxCore_foxitForceRedraw();
                            }
                            



                            if(tool.started){
                                
                                //arrowmarkupobj.w = tool.curmousepos.x;
                                //arrowmarkupobj.h = tool.curmousepos.y;

                                tool.drawArrow({x:tool.curmousepos.x,y:tool.curmousepos.y});

                                if(arrowmarkupobj != null){

                                    context.clearRect(0, 0, canvas.width, canvas.height);
                                    arrowmarkupobj.drawme(context);
                                }
                                

                            }



                        }


                    } else {

                        if (DocObj.getCurrentpage().usepdfjs && DocObj.pages.length > 1) {

                            if (DocObj.currentpage == 0) {
                                /*if (DocObj.getCurrentpage().starty >= 0) {
                                    movey = 0;
                                }*/

                            }
                            DocObj.getCurrentpage().pan_update(0, -movey);
                            
                            


                        } else {


                            DocObj.getCurrentpage().ZoomIn(1.1, false, false, tool.curmousepos);



                            if(tool.started){
                                tool.setdimoffset(false);

                                //arrowmarkupobj.w = tool.curmousepos.x;
                                //arrowmarkupobj.h = tool.curmousepos.y;

                                tool.drawArrow({x:tool.curmousepos.x,y:tool.curmousepos.y});


                                if(arrowmarkupobj != null){
                                    arrowmarkupobj.drawme(context);
                                }



                            }

                        }

                    }


                };

                this.belowlimitExtent = function(zoomscalefactor){
                    var belowlimit = true;
                    

                    var pagedim = DocObj.getCurrentpage().getpagedim();

                    var wlimit = canvasowidth - 60;
                    var hlimit = canvasoheight - 60;

                    if((pagedim.w / zoomscalefactor) < wlimit && (pagedim.h / zoomscalefactor) < hlimit){
                        belowlimit = false;
                        //scalechanged = false;
                        //DocObj.getCurrentpage().zoomall();
                    }

                    var pagerect = DocObj.getCurrentpage().ZoomOutTest(zoomscalefactor, false, tool.curmousepos);
                    
                    var heightlimit = canvasoheight - (nPanlimit*2);
                    var widthlimit = canvasowidth - (nPanlimit*2);
                    //var toplimit = 15;
                    var bottomlimit = ((pagerect.y + pagerect.h) <= heightlimit);
                    var toplimit = (pagerect.y >= nPanlimit);

                    // need to return these to reset the top and bottom positions.scroll|| bottomlimit || toplimit
                    if ((pagerect.w / pagerect.h) < (canvasowidth / canvasoheight)){
                        if(pagerect.h <= heightlimit){
                            belowlimit = false;
                        }
    
                    }else{
                        if(pagerect.w <= widthlimit){
                            belowlimit = false;
                        }

                    }


                    if(!bLimitZoomOut){
                        belowlimit = true;
                    }

                    return belowlimit;
                };                

                this.wheel = function (ev) {

                    var delta = 0;
                    if (ev.wheelDelta !== undefined) { // WebKit / Opera / Explorer 9

                        delta = ev.wheelDelta;

                    } else if (ev.deltaY !== undefined) { // Firefox

                        delta = -ev.deltaY*50;

                    }

                    tool.scrollhandling(delta);


                };

                this.setdimoffset = function(initial){

                    if(initial){

                        arrowmarkupobj.scaling = getCurrentPageScale();
                        arrowmarkupobj.loadscaling = getCurrentPageScale(); 
                        var offset = getCurrentPageOffset();
                        arrowmarkupobj.xoffset = offset.x;
                        arrowmarkupobj.yoffset = offset.y;

                        tool.orgscale = arrowmarkupobj.scaling;
                        tool.orgdx = arrowmarkupobj.xoffset;
                        tool.orgdy = arrowmarkupobj.yoffset;

                        
                    }

                    //move operation to markup object.

                    var curscale = DocObj.getCurrentpage().getdscale();
                    var pagedx = DocObj.getCurrentpage().getdx();
                    var pagedy = DocObj.getCurrentpage().getdy();


                    if(arrowmarkupobj){
                        var scalediff = (curscale / arrowmarkupobj.scaling);
                        var changex = ((arrowmarkupobj.w - pagedx) / scalediff) + arrowmarkupobj.xoffset;
                        var changey = ((arrowmarkupobj.h - pagedy) / scalediff) + arrowmarkupobj.yoffset;
                    }

                    //arrowmarkupobj.w = changex;
                    //arrowmarkupobj.h = changey;

                };                
                
                this.pointermove = function (ev) {
                    tool.MSPointerMove(ev);
                };
                this.pointerdown = function (ev) {
                    tool.MSPointerDown(ev);
                };

                this.pointerup = function (ev) {
                    tool.MSPointerUp(ev);
                };


                this.MSPointerDown = function (ev) {
                    ev.preventDefault();
                    ev.stopPropagation();
                    
                    var mousePos = getMousePos(canvas, ev);

                    if(arrowmarkupobj){

                        mousePos = arrowmarkupobj.getPadding(mousePos);

                    }

                    var bWithin = MousePosdrwext(mousePos);

                    if(ev.button == 0){
                        if (!tool.started) {

                            tool.startArrow(bWithin, mousePos);
    
                        } else {

                            tool.completeArrow(bWithin, mousePos);
                            
                            
    
                        }
                    }else{
                        prevx = mousePos.x;
                        prevy = mousePos.y;

                    }
                    

                };
                this.startArrow = function(bWithin, mousePos){

                    if (bWithin || !bLimMarkupExtent) {
                        tool.started = true;
                        if(DocObj){
                            DocObj.bMarkupchanged = true;
                        }

                        if (havesnap) {
                            if(DocObj.getCurrentpage().getpageRotation() != 0){
                                var rotatedpoint = snap_rotated(snapPoint);
                            }else{
                                rotatedpoint = snapPoint;
                            }

                            tool.x0 = rotatedpoint.x;
                            tool.y0 = rotatedpoint.y;

                        } else {
                            tool.x0 = mousePos.x;
                            tool.y0 = mousePos.y;

                        }
                        if (type == 10){
                            //circle
                            arrowmarkupobj = new MarkupObject(4, 1, fillstyle);


                        }else{
                            arrowmarkupobj = new MarkupObject(6, type, 0);
                            arrowmarkupobj.pagerotation = DocObj.getCurrentpage().getpageRotation();
                        }




                        arrowmarkupobj.pagenumber = DocObj.getcurPage();

                        arrowmarkupobj.viewname = DocObj.getcur3DView();

                        if(arrowmarkupobj.viewname != null){
                            arrowmarkupobj.bfor3D = true;
                            arrowmarkupobj.pageheight = canvasoheight;
                            arrowmarkupobj.pagewidth = canvasowidth;
                        }


                        tool.setdimoffset(true);

                        tool.startx = tool.x0;
                        tool.starty = tool.y0;


                        arrowmarkupobj.seteditaction(0);

                    }
                };
                this.completeArrow = function(bWithin, mousePos){
                    
                    if(!tool.bfirstpointset){
                        return;
                    }
                    
                    
                    var ccentre = {x : tool.x0, y : tool.y0};
                    
                    if (arrowmarkupobj.type == 4 && arrowmarkupobj.subtype == 1){
                        var bWithinradius = MousePosradiusdrwext(ccentre,mousePos);
                    }else{
                        bWithinradius = true;
                    }
                    
                    
                    if(bOrthoOn){
                        bOrthoOn = false;
                        tempOrtho = true;
                    }

                    if ((bWithin && bWithinradius) || !bLimMarkupExtent) {

                        tool.started = false;

                        if (havesnap) {

                            if(DocObj.getCurrentpage().getpageRotation() != 0){
                                var rotatedpoint = snap_rotated(snapPoint);
                            }else{
                                rotatedpoint = snapPoint;
                            }
                            if (tool.bUsemouseinput){
                                arrowmarkupobj.w = rotatedpoint.x;
                                arrowmarkupobj.h = rotatedpoint.y;
                            }

                        } else {
                            if (tool.bUsemouseinput){
                                if(DocObj.getCurrentpage().getpageRotation() != 0){
                                    //var rotpoint = ortho_rotate(mousePos);
                                    //arrowmarkupobj.w = rotpoint.x;
                                    //arrowmarkupobj.h = rotpoint.y;

                                    arrowmarkupobj.w = mousePos.x;
                                    arrowmarkupobj.h = mousePos.y;

                                }else{

                                    arrowmarkupobj.w = mousePos.x;
                                    arrowmarkupobj.h = mousePos.y;

                                }
                            }

                        }

                        context.clearRect(0, 0, canvas.width, canvas.height);
                        tool.setdimoffset(false);
                        arrowmarkupobj.drawme(context);

                        if (!bMultimarkupadd) {

                            bMarkupcrated = true;
                            //ft 08.08.2018 changed from separate index to direct array length
                            //nMarkupcreated = DocObj.nummarkups;
                            
                        }
                        nMarkupcreated = DocObj.markuplist.length;

                        if (type == 6){

                            if(bOrthoOn){
                                bOrthoOn = true;
                                tempOrtho = false;
                            }

                            var textrect = arrowmarkupobj.getdrawBox(mousePos);
                            
                            //fixed for rotated page
                            //var textby = arrowmarkupobj.h - ((defaultFont.height / 0.75)*0.5); 
                            //var textby = mousePos.y - ((defaultFont.height / 0.75)*0.5); 
                            
                            //var textbh = (defaultFont.height / 0.75);

                            //fixed for rotated page
                            //var textrect = {x : arrowmarkupobj.w, y :  textby, w : 100, h : textbh};
                            //var textrect = {x : mousePos.x, y :  textby, w : 100, h : textbh};


                        }
                        
                        arrowmarkupobj.savemetolistDraw();

                        if (tempOrtho) {

                            var curpoint = arrowmarkupobj.calculateOrthoDraw({x: arrowmarkupobj.x, y: arrowmarkupobj.y}, { x: arrowmarkupobj.w, y: arrowmarkupobj.h });

                            arrowmarkupobj.w = curpoint.w;
                            arrowmarkupobj.h = curpoint.h;

                        }

                        arrowmarkupobj = null;
                        
                        //markupobject.markupnumber = DocObj.nummarkups;         DocObj.markuplist[markupobject.markupnumber] = this;
                        img_update();

                        if (type == 6){
                            textRect(textrect.x, textrect.y, textrect.w, textrect.h, DocObj.markuplist[nMarkupcreated]);
                        }

                        RxCore_MarkupRedraw();

                        if(bAlwaysMarkupCallback){
                            if (RxCore_GUI_Markup != undefined) {

                                if (type == 6){
                                    RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[nMarkupcreated].textBoxConnected, {created : true, modified : false, deleted : false, multi : bMultimarkupadd});    
                                }else{
                                    RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[nMarkupcreated], {created : true, modified : false, deleted : false, multi : bMultimarkupadd});
                                }
                                
                                
                            }
                
                        }else{
                            if (!bMultimarkupadd) {
                                //need to move to connection object.
                                //markupcreated();
                                if (RxCore_GUI_Markup != undefined) {
                                    //var operation = {created : true, modified : false, deleted : false};


                                    if (type == 6){
                                        RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[nMarkupcreated].textBoxConnected, {created : true, modified : false, deleted : false, multi : bMultimarkupadd});    
                                    }else{
                                        RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[nMarkupcreated], {created : true, modified : false, deleted : false, multi : bMultimarkupadd});
                                    }
    

                                    //RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[nMarkupcreated], {created : true, modified : false, deleted : false});
                                    //RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[index],true);
                                }
    
                            }
    
                        }

                        if(tempOrtho){
                            bOrthoOn = true;
                            tempOrtho = false;
                        }

                    }
                };


                this.drawArrow = function(mousePos){



                    if (tool.bUsemouseinput){
                        arrowmarkupobj.x = tool.x0;
                        arrowmarkupobj.y = tool.y0;

                        tool.bfirstpointset = true;

                    }

                    if (havesnap) {

                        if(DocObj.getCurrentpage().getpageRotation() != 0){
                            var rotatedpoint = snap_rotated(snapPoint);
                        }else{
                            rotatedpoint = snapPoint;
                        }

                        if (tool.bUsemouseinput){
                            arrowmarkupobj.w = rotatedpoint.x;
                            arrowmarkupobj.h = rotatedpoint.y;
                            tool.bfirstpointset = true;
                        }

                        limprevx = rotatedpoint.x;
                        limprevy = rotatedpoint.y;
                        //prevx = rotatedpoint.x;
                        //prevy = rotatedpoint.y;
                        


                    } else {
                        if (tool.bUsemouseinput){
                            arrowmarkupobj.w = mousePos.x;
                            arrowmarkupobj.h = mousePos.y;
                            tool.bfirstpointset = true;
                        }                            

                        limprevx = mousePos.x;
                        limprevy = mousePos.y;
                        
                        //prevx = mousePos.x;
                        //prevy = mousePos.y;

                    }


                };

                this.mousedown = function (ev) {
                    ev.preventDefault();
                    ev.stopPropagation();

                    
                    
                    var mousePos = getMousePos(canvas, ev);

                    if(arrowmarkupobj){
                        mousePos = arrowmarkupobj.getPadding(mousePos);
                    }
                    
                    var bWithin = MousePosdrwext(mousePos);

                    if(ev.button == 0){
                        if (!tool.started) {

                            tool.startArrow(bWithin, mousePos);
    
    
                        } else {

                            tool.completeArrow(bWithin, mousePos);
                            
                        }

                    }else{
                        prevx = mousePos.x;
                        prevy = mousePos.y;

                    }

                    
                };

                this.touchstart = function (ev) {
                    ev.preventDefault();
                    //context.strokeStyle = "red";

                    var touchPos = getTouchPos(canvas, ev, 0);

                    if(arrowmarkupobj){

                        touchPos = arrowmarkupobj.getPadding(touchPos);

                    }

                    var bWithin = MousePosdrwext(touchPos);




                    tool.started = true;
                    if(DocObj){
                        DocObj.bMarkupchanged = true;
                    }

                    if (!bWithin && bLimMarkupExtent) {
                        tool.started = false;
                        if(DocObj){
                            DocObj.bMarkupchanged = false;
                        }

                        return;
                    }

                    tool.x0 = touchPos.x;
                    tool.y0 = touchPos.y;


                    if (type == 10){
                        arrowmarkupobj = new MarkupObject(4, 1, fillstyle);
                    }else{
                        arrowmarkupobj = new MarkupObject(6, type, 0);
                    }

                    arrowmarkupobj.scaling = getCurrentPageScale();
                    arrowmarkupobj.loadscaling = getCurrentPageScale(); 
                    var offset = getCurrentPageOffset();
                    arrowmarkupobj.xoffset = offset.x;
                    arrowmarkupobj.yoffset = offset.y;

                    arrowmarkupobj.pagenumber = DocObj.getcurPage();
                    arrowmarkupobj.viewname = DocObj.getcur3DView();

                    if(arrowmarkupobj.viewname != null){
                        arrowmarkupobj.bfor3D = true;
                        arrowmarkupobj.pageheight = canvasoheight;
                        arrowmarkupobj.pagewidth = canvasowidth;
                    }

                    arrowmarkupobj.seteditaction(0);

                };


                this.MSPointerMove = function (ev) {
                    ev.preventDefault();
                    ev.stopPropagation();
                    
                    var mousePos = getMousePos(canvas, ev);
                    tool.curmousepos = mousePos;


                    var xdiff = prevx - mousePos.x;
                    var ydiff = prevy - mousePos.y;

                    snapPoint = DocObj.getCurrentpage().getsnap(mousePos);

                    if (!snapPoint.found || snapPoint == undefined) {
                        havesnap = false;
                        context.clearRect(0, 0, canvas.width, canvas.height);
                    } else {
                        havesnap = true;
                        drawsnap(context,snapPoint);

                    }

                    if (ev.buttons == 2) {

                        DocObj.getCurrentpage().pan_update(xdiff, ydiff);
                        

                        tool.setdimoffset(false);
                        prevx = mousePos.x;
                        prevy = mousePos.y;

                    }
                    
                    if (!tool.started) {

                        return;
                    }

                    if(arrowmarkupobj){

                        mousePos = arrowmarkupobj.getPadding(mousePos);

                    }

                    var bWithin = MousePosdrwext(mousePos);

                    var ccentre = {x : tool.x0, y : tool.y0};
                    if (arrowmarkupobj.type == 4 && arrowmarkupobj.subtype == 1){
                        var bWithinradius = MousePosradiusdrwext(ccentre,mousePos);
                    }else{
                        bWithinradius = true;
                    }


                    if ((bWithin && bWithinradius) || !bLimMarkupExtent) {
                        context.clearRect(0, 0, canvas.width, canvas.height);
                        arrowmarkupobj.x = tool.x0;
                        arrowmarkupobj.y = tool.y0;

                        if (havesnap) {

                            if(DocObj.getCurrentpage().getpageRotation() != 0){
                                var rotatedpoint = snap_rotated(snapPoint);
                            }else{
                                rotatedpoint = snapPoint;
                            }

                            if (tool.bUsemouseinput){
                               arrowmarkupobj.w = rotatedpoint.x;
                               arrowmarkupobj.h = rotatedpoint.y;
                            }

                            limprevx = rotatedpoint.x;
                            limprevy = rotatedpoint.y;

                            //prevx = rotatedpoint.x;
                            //prevy = rotatedpoint.y;
                            drawsnap(context,snapPoint);



                        } else {

                            if (tool.bUsemouseinput){
                                arrowmarkupobj.w = mousePos.x;
                                arrowmarkupobj.h = mousePos.y;
                            }

                            limprevx = mousePos.x;
                            limprevy = mousePos.y;
                            
                            //prevx = mousePos.x;
                            //prevy = mousePos.y;

                        }
                        tool.setdimoffset(false);


                        arrowmarkupobj.drawme(context);


                    } else {
                        context.clearRect(0, 0, canvas.width, canvas.height);
                        arrowmarkupobj.x = tool.x0;
                        arrowmarkupobj.y = tool.y0;
                        
                        if (tool.bUsemouseinput){
                            arrowmarkupobj.w = limprevx;
                            arrowmarkupobj.h = limprevy;
                        }                        

                        tool.setdimoffset(false);

                        


                        arrowmarkupobj.drawme(context);


                    }


                };


                this.mousemove = function (ev) {
                    ev.preventDefault();
                    ev.stopPropagation();
                    

                    var mousePos = getMousePos(canvas, ev);
                    tool.curmousepos = mousePos;

                    var xdiff = prevx - mousePos.x;
                    var ydiff = prevy - mousePos.y;


                    snapPoint = DocObj.getCurrentpage().getsnap(mousePos);

                    if (!snapPoint.found || snapPoint == undefined) {
                        havesnap = false;
                        context.clearRect(0, 0, canvas.width, canvas.height);
                    } else {
                        havesnap = true;
                        drawsnap(context,snapPoint);

                    }

                    if (ev.button == 2 || ev.buttons == 2) {

                        DocObj.getCurrentpage().pan_update(xdiff, ydiff);
                        
                        tool.setdimoffset(false);
                        prevx = mousePos.x;
                        prevy = mousePos.y;

                    }

                    if (!tool.started) {
                        return;
                    }

                    if(arrowmarkupobj){

                        mousePos = arrowmarkupobj.getPadding(mousePos);

                    }

                    var bWithin = MousePosdrwext(mousePos);

                    var ccentre = {x : tool.x0, y : tool.y0};
                    if (arrowmarkupobj.type == 4 && arrowmarkupobj.subtype == 1){
                        var bWithinradius = MousePosradiusdrwext(ccentre,mousePos);
                    }else{
                        bWithinradius = true;
                    }

                    if ((bWithin && bWithinradius) || !bLimMarkupExtent) {


                        context.clearRect(0, 0, canvas.width, canvas.height);
                        if (tool.bUsemouseinput){
                            arrowmarkupobj.x = tool.x0;
                            arrowmarkupobj.y = tool.y0;
    
                        }

                        tool.drawArrow(mousePos);


                        /*if (havesnap) {

                            if(DocObj.getCurrentpage().getpageRotation() != 0){
                                var rotatedpoint = snap_rotated(snapPoint);
                            }else{
                                rotatedpoint = snapPoint;
                            }

                            if (tool.bUsemouseinput){
                                arrowmarkupobj.w = rotatedpoint.x;
                                arrowmarkupobj.h = rotatedpoint.y;
                            }

                            limprevx = rotatedpoint.x;
                            limprevy = rotatedpoint.y;
                            //prevx = rotatedpoint.x;
                            //prevy = rotatedpoint.y;
                            drawsnap(context,snapPoint);


                        } else {
                            if (tool.bUsemouseinput){
                                arrowmarkupobj.w = mousePos.x;
                                arrowmarkupobj.h = mousePos.y;
    
                            }                            

                            limprevx = mousePos.x;
                            limprevy = mousePos.y;
                            
                            //prevx = mousePos.x;
                            //prevy = mousePos.y;

                        }*/
                        tool.setdimoffset(false);

                        if (havesnap) {
                            drawsnap(context,snapPoint);
                        }
    

                        //comment out test to check problem
                        arrowmarkupobj.drawme(context);
                        
                        
                        

                    } else {
                        context.clearRect(0, 0, canvas.width, canvas.height);
    
                        if (tool.bUsemouseinput){
                            arrowmarkupobj.x = tool.x0;
                            arrowmarkupobj.y = tool.y0;
                        }
                        
                        if (tool.bUsemouseinput){
                            arrowmarkupobj.w = limprevx;
                            arrowmarkupobj.h = limprevy;
                        }                        

                        tool.setdimoffset(false);
                        
                        //comment out test to check problem
                        arrowmarkupobj.drawme(context);


                        //drawsnap(context,snapPoint);
                        //if (tool.bUsemouseinput){
                        
                        //}

                    }


                };

                this.touchmove = function (ev) {
                    ev.preventDefault();
                    if (!tool.started) {
                        return;
                    }
                    var touchPos = getTouchPos(canvas, ev, 0);

                    if(arrowmarkupobj){
                        touchPos = arrowmarkupobj.getPadding(touchPos);
                    }

                    var bWithin = MousePosdrwext(touchPos);
                    if (bWithin || !bLimMarkupExtent) {

                        arrowmarkupobj.x = tool.x0;
                        arrowmarkupobj.y = tool.y0;

                        if (tool.bUsemouseinput){
                            arrowmarkupobj.w = touchPos.x;
                            arrowmarkupobj.h = touchPos.y;
                        }                        
                        tool.bfirstpointset = true;

                        prevx = touchPos.x;
                        prevy = touchPos.y;

                    } else {
                        arrowmarkupobj.x = tool.x0;
                        arrowmarkupobj.y = tool.y0;
                        
                        if (tool.bUsemouseinput){
                            arrowmarkupobj.w = prevx;
                            arrowmarkupobj.h = prevy;
                        }                        
                        
                        tool.bfirstpointset = true;

                    }


                    context.clearRect(0, 0, canvas.width, canvas.height);
                    arrowmarkupobj.drawme(context);


                };

                this.MSPointerUp = function (ev) {
                    ev.preventDefault();
                    /*ev.target.style.cursor = 'default';
                     if (tool.started) {
                     tool.mousemove(ev);
                     arrowmarkupobj.savemetolist();
                     arrowmarkupobj = null;
                     tool.started = false;

                     img_update();

                     }*/

                };
                this.mouseup = function (ev) {
                    ev.preventDefault();
                    /* ev.target.style.cursor = 'default';
                     if (tool.started) {
                     tool.mousemove(ev);
                     arrowmarkupobj.savemetolist();
                     arrowmarkupobj = null;
                     tool.started = false;

                     img_update();

                     }*/
                };
                this.touchcancel = function(ev){
                    ev.preventDefault();
                    if (tool.started) {
                        if (!bMultimarkupadd) {

                            bMarkupcrated = true;
                            //ft 08.08.2018 changed from separate index to direct array length
                            
                            //nMarkupcreated = DocObj.nummarkups;
                        }
                        nMarkupcreated = DocObj.markuplist.length;

                        if (type == 6){
                            var textby = arrowmarkupobj.h - ((defaultFont.height / 0.75)*0.5); 
                            
                            //var textbh = (defaultFont.height / 0.75);

                            //var textrect = {x : arrowmarkupobj.w, y :  textby, w : 100, h : textbh};
                            var textrect = arrowmarkupobj.getdrawBox();


                        }

                        arrowmarkupobj.savemetolistDraw();
                        img_update();
                        tool.touchmove(ev);
                        tool.started = false;
                        arrowmarkupobj = null;

                        tool.bfirstpointset = false;

                        if (type == 6){
                            textRect(textrect.x, textrect.y, textrect.w, textrect.h, DocObj.markuplist[nMarkupcreated]);
                        }

                        if(bAlwaysMarkupCallback){
                            if (RxCore_GUI_Markup != undefined) {
                                
                                RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[nMarkupcreated], {created : true, modified : false, deleted : false, multi : bMultimarkupadd});
                            }
                
                        }else{
                            if (!bMultimarkupadd) {
                                //need to move to connection object.
                                //markupcreated();
                                if (RxCore_GUI_Markup != undefined) {
                                    //RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[index],true);
                                    RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[nMarkupcreated], {created : true, modified : false, deleted : false});
                                }
    
                            }
    
                        }


                    }
                };
                
                this.touchend = function (ev) {
                    ev.preventDefault();
                    if (tool.started) {
                        if (!bMultimarkupadd) {

                            bMarkupcrated = true;
                            //ft 08.08.2018 changed from separate index to direct array length
                            
                            //nMarkupcreated = DocObj.nummarkups;
                        }
                        nMarkupcreated = DocObj.markuplist.length;

                        arrowmarkupobj.savemetolistDraw();
                        img_update();

                        tool.bfirstpointset = false;
                        tool.touchmove(ev);

                        if (type == 6){

                            var textby = arrowmarkupobj.h - ((defaultFont.height / 0.75)*0.5); 
                            //var textbh = (defaultFont.height / 0.75); 

                            //var textrect = {x : arrowmarkupobj.w, y :  textby, w : 100, h : textbh};
                            var textrect = arrowmarkupobj.getdrawBox();


                        }


                        tool.started = false;
                        arrowmarkupobj = null;

                        if (type == 6){
                            textRect(textrect.x, textrect.y, textrect.w, textrect.h, DocObj.markuplist[nMarkupcreated]);
                        }

                        if(bAlwaysMarkupCallback){
                            if (RxCore_GUI_Markup != undefined) {
                                
                                RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[nMarkupcreated], {created : true, modified : false, deleted : false, multi : bMultimarkupadd});
                            }
                
                        }else{
                            if (!bMultimarkupadd) {
                                //need to move to connection object.
                                //markupcreated();
                                if (RxCore_GUI_Markup != undefined) {
                                    //RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[index],true);
                                    RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[nMarkupcreated], {created : true, modified : false, deleted : false});
                                }
                            }
                        }


                    }
                };


            };


            tools.Polycurve = function (params) {
                var tool = this;
                var prevx = 0,
                    prevy = 0;
                this.started = false;
                this.name = 'Polycurve';
                this.anglelengthsupport = false;
                this.refreshSupport = false;

                this.terminated = false;
                this.LastTouchdata = -1;
                //ft 08.08.2018 changed from separate index to direct array length
                //var curmarkup = DocObj.nummarkups;
                var curmarkup = DocObj.markuplist.length;
                var polycurvemarkupobj;
                var subtype = params.p1;

                setCursor('crosshair');
                
                

                this.pointermove = function (ev) {
                    tool.MSPointerMove(ev);
                };
                this.pointerdown = function (ev) {
                    tool.MSPointerDown(ev);
                };

                /*this.pointerup = function(ev){
                 tool.MSPointerUp(ev);
                 };*/


                this.MSPointerDown = function (ev) {
                    ev.preventDefault();
                    ev.stopPropagation();
                    
                    var mousePos = getMousePos(canvas, ev);
                    var bWithin = MousePosdrwext(mousePos);
                    if (!bWithin && bLimMarkupExtent) {
                        tool.started = false;
                        if(DocObj){
                            DocObj.bMarkupchanged = false;
                        }

                        return;
                    }


                    if (!tool.started) {

                        polycurvemarkupobj = new MarkupObject(2, subtype, 0);
                        polycurvemarkupobj.pagenumber = DocObj.getcurPage();

                        polycurvemarkupobj.scaling = getCurrentPageScale();
                        var offset = getCurrentPageOffset();
                        polycurvemarkupobj.xoffset = offset.x;
                        polycurvemarkupobj.yoffset = offset.y;

                        polycurvemarkupobj.x = mousePos.x;
                        polycurvemarkupobj.y = mousePos.y;
                        polycurvemarkupobj.seteditaction(0);
                        polycurvemarkupobj.addpoint(mousePos.x, mousePos.y);
                        tool.started = true;
                        if(DocObj){
                            DocObj.bMarkupchanged = true;
                        }

                        polycurvemarkupobj.startdraw(context);
                        polycurvemarkupobj.addpoint(mousePos.x, mousePos.y);
                    } else {

                        polycurvemarkupobj.addpoint(mousePos.x, mousePos.y);
                    }

                };
                this.mousedown = function (ev) {
                    ev.preventDefault();
                    ev.stopPropagation();

                    
                    var mousePos = getMousePos(canvas, ev);
                    var bWithin = MousePosdrwext(mousePos);
                    if (!bWithin && bLimMarkupExtent) {
                        tool.started = false;
                        if(DocObj){
                            DocObj.bMarkupchanged = false;
                        }

                        
                        return;
                    }

                    if (!tool.started) {
                        polycurvemarkupobj = new MarkupObject(2, subtype, 0);
                        polycurvemarkupobj.pagenumber = DocObj.getcurPage();

                        polycurvemarkupobj.scaling = getCurrentPageScale();
                        var offset = getCurrentPageOffset();
                        polycurvemarkupobj.xoffset = offset.x;
                        polycurvemarkupobj.yoffset = offset.y;

                        polycurvemarkupobj.x = mousePos.x;
                        polycurvemarkupobj.y = mousePos.y;
                        polycurvemarkupobj.seteditaction(0);
                        polycurvemarkupobj.addpoint(mousePos.x, mousePos.y);
                        tool.started = true;
                        if(DocObj){
                            DocObj.bMarkupchanged = true;
                        }

                        polycurvemarkupobj.startdraw(context);
                        polycurvemarkupobj.addpoint(mousePos.x, mousePos.y);
                    } else {

                        polycurvemarkupobj.addpoint(mousePos.x, mousePos.y);
                    }


                };

                this.MSPointerMove = function (ev) {
                    ev.preventDefault();
                    ev.stopPropagation();
                    
                    var mousePos = getMousePos(canvas, ev);
                    var bWithin = MousePosdrwext(mousePos);
                    if (!tool.started) {
                        return;
                    }
                    if (bWithin || !bLimMarkupExtent) {

                        context.clearRect(0, 0, canvas.width, canvas.height);
                        polycurvemarkupobj.setlastpoint(mousePos.x, mousePos.y);
                        polycurvemarkupobj.drawme(context);

                        prevx = mousePos.x;
                        prevy = mousePos.y;

                    } else {

                        context.clearRect(0, 0, canvas.width, canvas.height);
                        polycurvemarkupobj.setlastpoint(prevx, prevy);
                        polycurvemarkupobj.drawme(context);


                    }

                };

                this.mousemove = function (ev) {
                    ev.preventDefault();
                    ev.stopPropagation();
                    
                    var mousePos = getMousePos(canvas, ev);
                    if (!tool.started) {
                        return;
                    }
                    var bWithin = MousePosdrwext(mousePos);
                    if (bWithin || !bLimMarkupExtent) {

                        context.clearRect(0, 0, canvas.width, canvas.height);
                        polycurvemarkupobj.setlastpoint(mousePos.x, mousePos.y);
                        polycurvemarkupobj.drawme(context);

                        prevx = mousePos.x;
                        prevy = mousePos.y;

                    } else {

                        context.clearRect(0, 0, canvas.width, canvas.height);
                        polycurvemarkupobj.setlastpoint(prevx, prevy);
                        polycurvemarkupobj.drawme(context);


                    }

                };

                this.mouseup = function (ev) {
                    //polycurvemarkupobj.addpoint(mousePos.x, ev._y);

                };

                this.dblclick = function (ev) {
                    if (tool.started) {
                        tool.mousemove(ev);
                        //tool.started = false;
                        tool.started = false;
                        polycurvemarkupobj.points.splice(polycurvemarkupobj.numpoints - 1, 2);
                        polycurvemarkupobj.findrectangle();
                        polycurvemarkupobj.savemetolistDraw();
                        polycurvemarkupobj = null;
                        img_update();
                        
                    }

                };
                this.doubletap = function (ev) {
                    if (tool.started) {
                        tool.started = false;
                        polycurvemarkupobj.points.splice(polycurvemarkupobj.points.length - 1, 1);
                        polycurvemarkupobj.findrectangle();
                        polycurvemarkupobj.savemetolistDraw();
                        polycurvemarkupobj = null;
                        img_update();

                        //tool.terminated = true;

                    }

                };


                this.touchstart = function (ev) {
                    ev.preventDefault();
                    var touchPos = getTouchPos(canvas, ev, 0);
                    var touchx = touchPos.x;
                    var touchy = touchPos.y;
                    var lastTouch = 0;

                    var now = new Date().getTime();
                    if (tool.LastTouchdata == -1) {
                        lastTouch = now + 1;
                    } else {
                        lastTouch = tool.LastTouchdata;

                    }
                    var delta = now - lastTouch;
                    tool.LastTouchdata = now;

                    if (delta < 500 && delta > 0) {
                        tool.doubletap(ev);


                    } else {
                        var bWithin = MousePosdrwext(touchPos);
                        if (!bWithin && bLimMarkupExtent) {
                            tool.started = false;
                            if(DocObj){
                                DocObj.bMarkupchanged = false;
                            }

                            return;
                        }

                        if (!tool.started) {
                            polycurvemarkupobj = new MarkupObject(2, subtype, 0);
                            polycurvemarkupobj.pagenumber = DocObj.getcurPage();

                            polycurvemarkupobj.scaling = getCurrentPageScale();
                            var offset = getCurrentPageOffset();
                            polycurvemarkupobj.xoffset = offset.x;
                            polycurvemarkupobj.yoffset = offset.y;

                            polycurvemarkupobj.x = touchx;
                            polycurvemarkupobj.y = touchy;
                            polycurvemarkupobj.seteditaction(0);
                            polycurvemarkupobj.addpoint(touchx, touchy);
                            polycurvemarkupobj.startdraw(context);
                            polycurvemarkupobj.addpoint(touchx, touchy);
                            tool.started = true;
                            if(DocObj){
                                DocObj.bMarkupchanged = true;
                            }


                        } else {
                            polycurvemarkupobj.addpoint(touchx, touchy);
                            //polygonmarkupobj.setlastpoint(touchx,touchy);
                        }

                    }

                };


                this.touchmove = function (ev) {
                    ev.preventDefault();
                    var touchPos = getTouchPos(canvas, ev, 0);
                    var touchx = touchPos.x;
                    var touchy = touchPos.y;
                    if (!tool.started) {
                        return;
                    }
                    var bWithin = MousePosdrwext(touchPos);
                    if (bWithin || !bLimMarkupExtent) {

                        context.clearRect(0, 0, canvas.width, canvas.height);
                        polycurvemarkupobj.setlastpoint(touchPos.x, touchPos.y);
                        polycurvemarkupobj.drawme(context);

                        prevx = touchPos.x;
                        prevy = touchPos.y;

                    } else {

                        context.clearRect(0, 0, canvas.width, canvas.height);
                        polycurvemarkupobj.setlastpoint(prevx, prevy);
                        polycurvemarkupobj.drawme(context);


                    }

                };
                this.touchcancel = function(ev){
                    ev.preventDefault();
                    var touchPos = getTouchPos(canvas, ev, 0);
                    var touchx = touchPos.x;
                    var touchy = touchPos.y;

                    if (tool.started) {
                        var bWithin = MousePosdrwext(touchPos);
                        if (bWithin || !bLimMarkupExtent) {
                            polycurvemarkupobj.addpoint(touchx, touchy);
                        }

                    }                    
                };

                this.touchend = function (ev) {
                    ev.preventDefault();
                    var touchPos = getTouchPos(canvas, ev, 0);
                    var touchx = touchPos.x;
                    var touchy = touchPos.y;

                    if (tool.started) {
                        var bWithin = MousePosdrwext(touchPos);
                        if (bWithin || !bLimMarkupExtent) {
                            polycurvemarkupobj.addpoint(touchx, touchy);
                        }

                    }


                };


            };


            tools.area = function (params) {

                var tool = this;
                var prevx = 0,
                    prevy = 0;

                var limprevx = 0,
                    limprevy = 0;

                var havesnap = false;
                var snapPoint;
                //ft 08.08.2018 changed from separate index to direct array length
                var curmarkup = DocObj.markuplist.length;
                //var curmarkup = DocObj.nummarkups;
                var areamarkupobj;
                
                var type = params.p1;
                var subtype = params.p2;

                this.name = 'area';
                this.subtype = params.p2;
                this.markupobject = params.p3;

                this.anglelengthsupport = true;
                this.refreshSupport = true;
                
                this.multipoint = true;

                this.bUsemouseinput = true;

                

                this.started = false;
                this.terminated = false;
                this.LastTouchdata = -1;
                this.startx = 0;
                this.starty = 0;
                this.orgscale = 1.0;
                this.orgdx = 0.0;
                this.orgdy = 0.0;
                //this.curmousepos = {x:0,y:0};
                this.curmousepos = {x:0,y:0,xus:0,yus:0};
                this.scaleoffset = undefined;


                setCursor(getMarkupCursor('area'));


                this.getdimoffset = function(){

                    var dimoffset = {scale : undefined, xoffset : undefined, yoffset : undefined};

                    dimoffset.scale = getCurrentPageScale();
                    var offset = getCurrentPageOffset();
                    dimoffset.xoffset = offset.x;
                    dimoffset.yoffset = offset.y;


                    return dimoffset;

                };

                this.refreshDraw = function(mousepos){

                    if(tool.started){
                        tool.setdimoffset(false);

                        if (tool.bUsemouseinput){

                            if(mousepos){
                                areamarkupobj.setlastpoint(mousepos.x, mousepos.y);
                            }else{
                                areamarkupobj.setlastpoint(tool.curmousepos.x, tool.curmousepos.y);
                            }
                            
                        }

                        if(areamarkupobj.getnumpoints() > 0){
                            areamarkupobj.drawme(context);
                            
                        }


                    }

                };

                this.refreshMouseUp = function(mousepos){

                    /*if(areamarkupobj.points.length > 0){
                        areamarkupobj.drawme(context);
                        //console.log("refreshed");
                    }*/
                    
                    if(tool.started){
                        tool.setdimoffset(false);

                        if (tool.bUsemouseinput){

                            if(mousepos){
                                areamarkupobj.setlastpoint(mousepos.x, mousepos.y);
                            }else{
                                areamarkupobj.setlastpoint(tool.curmousepos.x, tool.curmousepos.y);
                            }
                            
                        }

                        

                        if(areamarkupobj.getnumpoints() > 0){
                            areamarkupobj.drawme(context);
                        }


                    }


                };



                this.setAngleLength = function(angle, length, bAutoAdvance,  type){
                    this.bUsemouseinput = false;
                    
                    var btwoormorepoints = (areamarkupobj.points.length >= 2); 

                    if (areamarkupobj != undefined && btwoormorepoints){

                        //replace with points.

                        //areamarkupobj.points[areamarkupobj.points.length - 2].x = tool.x0;
                        //areamarkupobj.points[areamarkupobj.points.length - 2].y = tool.y0;
            
                        //areamarkupobj.x = tool.x0;
                        //areamarkupobj.y = tool.y0;
                        var curx2 = areamarkupobj.points[areamarkupobj.points.length - 1].x;
                        var cury2 = areamarkupobj.points[areamarkupobj.points.length - 1].y;


                        var secondpoint = areamarkupobj.setAnglelengthPoints(length, angle, type);
                        //add state to markup object to change rotate adjustment bUsemouseinput
                        context.clearRect(0, 0, canvas.width, canvas.height);
                        
                        if (MousePosdrwext({x:secondpoint.w,y:secondpoint.h})){
                            
                            //set last point.
                            areamarkupobj.setlastpoint(secondpoint.w, secondpoint.h);
                            if(bAutoAdvance){
                                areamarkupobj.addpoint(secondpoint.w, secondpoint.h);
                            }
                            


                            tool.setdimoffset(false);
                            
                            if(areamarkupobj.getnumpoints() > 0){
                                areamarkupobj.drawme(context);
                            }

    
                        }else{
                            //set last point.
                            areamarkupobj.setlastpoint(curx2, cury2);
                            /*if(bAutoAdvance){
                                areamarkupobj.addpoint(curx2, cury2);
                            }*/
                            

                            this.bUsemouseinput = true;
                            if(areamarkupobj.getnumpoints() > 0){
                                areamarkupobj.drawme(context);
                            }


                            areamarkupobj.bUsemouseinput = true;
                            if (RxCore_GUI_markupParamsError != undefined){
                                RxCore_GUI_markupParamsError.onDrawError('outside');
                            }

                        }

                    }

                };

                this.useMouseInput = function(){
                    tool.bUsemouseinput = true;
                };


                this.removelastPoint = function(){
                    
                    if(areamarkupobj == undefined){
                        return;
                    }

                    context.clearRect(0, 0, canvas.width, canvas.height);


                    if(areamarkupobj.getnumpoints() > 0){
                        areamarkupobj.drawme(context);
                    }


                    
                    areamarkupobj.points.splice(areamarkupobj.points.length - 1, 1);
                    areamarkupobj.setlastpoint(tool.curmousepos.x, tool.curmousepos.y);
                    
                    
                    context.clearRect(0, 0, canvas.width, canvas.height);
                    if(areamarkupobj.getnumpoints() > 0){
                        areamarkupobj.drawme(context);
                    }



                };

                this.newsegment = function() {
                    //add new segment

                    areamarkupobj.addline();


                };

                this.apply = function(){

                    //this.applySegment = function(){
                    //context.clearRect(0, 0, canvas.width, canvas.height);
    
                    var bsegmentmeasure = (areamarkupobj.type == 1 && areamarkupobj.subtype == 6);

                    if(bsegmentmeasure){
                        tool.applySegment();
                        return;
                    }
    

                    context.clearRect(0, 0, canvas.width, canvas.height);
                    
                    if(!tool.started){
                        return;
                    }
                    
                    tool.started = false;
                    tool.setdimoffset(false);
                    if(areamarkupobj.getnumpoints() > 0){
                        areamarkupobj.drawme(context);
                    }


                    areamarkupobj.points.splice(areamarkupobj.points.length - 1, 1);
                    areamarkupobj.findrectangle();
                    areamarkupobj.resetcupoint();


                    if (!bMultimarkupadd) {

                        bMarkupcrated = true;
                        //ft 08.08.2018 changed from separate index to direct array length
                        //nMarkupcreated = DocObj.nummarkups;
                        
                    }
                    nMarkupcreated = DocObj.markuplist.length;

                    areamarkupobj.savemetolistDraw();
                    //arrowmarkupobj.savemetolistLoad();
                    areamarkupobj = null;
                    
                    drawmarkupAll(cntximg);

                    //markupobject.markupnumber = DocObj.nummarkups;         DocObj.markuplist[markupobject.markupnumber] = this;
                    img_update();


                    if(bAlwaysMarkupCallback){
                     
                        if (RxCore_GUI_Markup != undefined) {
                            
                            RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[nMarkupcreated], {created : true, modified : false, deleted : false, multi : bMultimarkupadd});
                        }
            
                    }else{
                        if (!bMultimarkupadd) {
                            //need to move to connection object.
                            //markupcreated();
                            if (RxCore_GUI_Markup != undefined) {
                                //var operation = {created : true, modified : false, deleted : false};
                                RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[nMarkupcreated], {created : true, modified : false, deleted : false});
                                //RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[index],true);
                            }
    
                        }
    
                    }

                };                


                this.scrollhandling = function (movey) {
                    if (!documentopen || DocObj.getCurrentpage().usevector3Dxml) {
                        return;
                    }

                    if (movey < 0) {

                        if (DocObj.getCurrentpage().usepdfjs && DocObj.pages.length > 1 && !enableZoomOnScrollMultipage){
                        //if (DocObj.getCurrentpage().usepdfjs && DocObj.pages.length > 1) {
                            
                            if (DocObj.currentpage == DocObj.NumPages - 1) {
                                /*if (DocObj.getCurrentpage().endy <= canvas.height) {
                                    movey = 0;
                                }*/
                            }
                            DocObj.getCurrentpage().pan_update(0, -movey);
                            
                            if(areamarkupobj.getnumpoints() > 0){
                                areamarkupobj.drawme(context);
                            }



                        } else {

                            if(belowlimitExtentEx(1.1, tool.curmousepos)){

                                DocObj.getCurrentpage().ZoomOut(1.1, false, false, tool.curmousepos);

                            }else{
                                RxCore_foxitForceRedraw();
                            }
                            


                            if(tool.started){
                                tool.setdimoffset(false);

                                if (tool.bUsemouseinput){
                                    areamarkupobj.setlastpoint(tool.curmousepos.x, tool.curmousepos.y);
                                }

                                if(areamarkupobj.getnumpoints() > 0){
                                    areamarkupobj.drawme(context);
                                }
    


                            }

                        }

                    } else {

                        //if (DocObj.getCurrentpage().usepdfjs && DocObj.pages.length > 1) {
                        if (DocObj.getCurrentpage().usepdfjs && DocObj.pages.length > 1 && !enableZoomOnScrollMultipage){

                            if (DocObj.currentpage == 0) {
                                /*if (DocObj.getCurrentpage().starty >= 0) {
                                    movey = 0;
                                }*/

                            }
                            DocObj.getCurrentpage().pan_update(0, -movey);


                            if(areamarkupobj.getnumpoints() > 0){
                                areamarkupobj.drawme(context);
                            }

                            

                        } else {

                            /*if(tool.belowlimitExtent(1.1)){
                                
                            }*/
                            
                            DocObj.getCurrentpage().ZoomIn(1.1, false, false, tool.curmousepos);


                            if(tool.started){
                                tool.setdimoffset(false);

                                if (tool.bUsemouseinput){
                                    areamarkupobj.setlastpoint(tool.curmousepos.x, tool.curmousepos.y);
                                }

                                if(areamarkupobj.getnumpoints() > 0){
                                    areamarkupobj.drawme(context);
                                }
    
                            }

                        }

                    }

                };

                this.belowlimitExtent = function(zoomscalefactor){
                    var belowlimit = true;
                    

                    var pagedim = DocObj.getCurrentpage().getpagedim();

                    var wlimit = canvasowidth - 60;
                    var hlimit = canvasoheight - 60;

                    if((pagedim.w / zoomscalefactor) < wlimit && (pagedim.h / zoomscalefactor) < hlimit){
                        belowlimit = false;
                        //scalechanged = false;
                        //DocObj.getCurrentpage().zoomall();
                    }

                    var pagerect = DocObj.getCurrentpage().ZoomOutTest(zoomscalefactor, false, tool.curmousepos);
                    
                    var heightlimit = canvasoheight - (nPanlimit*2);
                    var widthlimit = canvasowidth - (nPanlimit*2);
                    //var toplimit = 15;
                    var bottomlimit = ((pagerect.y + pagerect.h) <= heightlimit);
                    var toplimit = (pagerect.y >= nPanlimit);

                    // need to return these to reset the top and bottom positions.scroll|| bottomlimit || toplimit
                    if ((pagerect.w / pagerect.h) < (canvasowidth / canvasoheight)){
                        if(pagerect.h <= heightlimit){
                            belowlimit = false;
                        }
    
                    }else{
                        if(pagerect.w <= widthlimit){
                            belowlimit = false;
                        }

                    }


                    if(!bLimitZoomOut){
                        belowlimit = true;
                    }

                    return belowlimit;
                };                

                this.wheel = function (ev) {

                    var delta = 0;
                    if (ev.wheelDelta !== undefined) { // WebKit / Opera / Explorer 9

                        delta = ev.wheelDelta;

                    } else if (ev.deltaY !== undefined) { // Firefox

                        delta = -ev.deltaY*50;

                    }

                    tool.scrollhandling(delta);


                };

                this.setdimoffset = function(initial){

                    if(initial){


                        tool.scaleoffset = tool.getdimoffset();

                        areamarkupobj.scaling = tool.scaleoffset.scale;
                        areamarkupobj.xoffset = tool.scaleoffset.xoffset;
                        areamarkupobj.yoffset = tool.scaleoffset.yoffset;

                        tool.orgscale = tool.scaleoffset.scale;
                        tool.orgdx = tool.scaleoffset.xoffset;
                        tool.orgdy = tool.scaleoffset.yoffset;

                    }

                    tool.scaleoffset = tool.getdimoffset();

                    var curscale = DocObj.getCurrentpage().getdscale();
                    var pagedx = DocObj.getCurrentpage().getdx();
                    var pagedy = DocObj.getCurrentpage().getdy();

                    if(areamarkupobj){
                        var scalediff = (curscale / areamarkupobj.scaling);
                    }
                    

                    /*if(areamarkupobj.points.length > 0){
                        areamarkupobj.drawme(context);
                    }*/
                    

                    /*if(tool.started){
                        var lastpoint = areamarkupobj.getlastpoint();
                    
                        if(lastpoint){
                            var changex = ((lastpoint.x - pagedx) / scalediff) + areamarkupobj.xoffset;
                            var changey = ((lastpoint.y - pagedy) / scalediff) + areamarkupobj.yoffset;
        
                            areamarkupobj.setlastpoint(changex, changey);
        
                        }
    

                    }*/







                };

                // This is called when you start holding down the mouse button.
                // This starts the pencil drawing.
                this.pointermove = function (ev) {
                    tool.MSPointerMove(ev);
                };
                this.pointerdown = function (ev) {
                    tool.MSPointerDown(ev);
                };

                this.pointerup = function (ev) {
                    tool.MSPointerUp(ev);
                };


                this.MSPointerDown = function (ev) {
                    ev.preventDefault();
                    ev.stopPropagation();

                    
                    var mousePos = getMousePos(canvas, ev);
                    if(ev.button == 0){
                        var bWithin = MousePosdrwext(mousePos);
                        if (!bWithin && bLimMarkupExtent) {
                            return;
                        }

                        if (!tool.started) {
                            areamarkupobj = new MarkupObject(type, subtype, fillstyle);
                            if (subtype == 1){
                                areamarkupobj.linewidth = 1;
                                areamarkupobj.AddAttribute('Rx_Area', '0');
                                areamarkupobj.AddAttribute('SpaceID', '0');
                            }


                            areamarkupobj.pagenumber = DocObj.getcurPage();
                            areamarkupobj.viewname = DocObj.getcur3DView();

                            if(areamarkupobj.viewname != null){
                                areamarkupobj.bfor3D = true;
                                areamarkupobj.pageheight = canvasoheight;
                                areamarkupobj.pagewidth = canvasowidth;
                            }


                            tool.setdimoffset(true);

                            if (havesnap) {
                                if(DocObj.getCurrentpage().getpageRotation() != 0){
                                    var rotatedpoint = snap_rotated(snapPoint);
                                }else{
                                    rotatedpoint = snapPoint;
                                }

                                areamarkupobj.x = rotatedpoint.x;
                                areamarkupobj.y = rotatedpoint.y;
                                areamarkupobj.seteditaction(0);
                                areamarkupobj.addpoint(rotatedpoint.x, rotatedpoint.y);
                                tool.started = true;
                                if(DocObj){
                                    DocObj.bMarkupchanged = true;
                                }
                                //areamarkupobj.startdraw(context);
                                areamarkupobj.addpoint(rotatedpoint.x, rotatedpoint.y);


                            } else {
                                areamarkupobj.x = mousePos.x;
                                areamarkupobj.y = mousePos.y;
                                areamarkupobj.seteditaction(0);
                                areamarkupobj.addpoint(mousePos.x, mousePos.y);
                                tool.started = true;
                                if(DocObj){
                                    DocObj.bMarkupchanged = true;
                                }
                                //areamarkupobj.startdraw(context);
                                areamarkupobj.addpoint(mousePos.x, mousePos.y);

                            }

                        } else {
                            if (havesnap) {
                                if(DocObj.getCurrentpage().getpageRotation() != 0){
                                    rotatedpoint = snap_rotated(snapPoint);
                                }else{
                                    rotatedpoint = snapPoint;
                                }
                                
                                if (tool.bUsemouseinput){
                                    areamarkupobj.addpoint(rotatedpoint.x, rotatedpoint.y);
                                }
                                
                            } else {
                                
                                if (tool.bUsemouseinput){
                                    areamarkupobj.addpoint(mousePos.x, mousePos.y);
                                }
                                
                            }

                        }
                    }else{
                        prevx = mousePos.x;
                        prevy = mousePos.y;

                    }


                };

                this.mousedown = function (ev) {
                    ev.preventDefault();
                    ev.stopPropagation();

                    
                    var mousePos = getMousePos(canvas, ev);

                    //subtype 2 is a hole.
                    if(tool.subtype == 2 && tool.markupobject != undefined){

                        let iswithin = true;

                        for(let pointIdx=0; pointIdx < tool.markupobject.points.length; pointIdx++) {
                            let point = tool.markupobject.points[pointIdx];


                            if(tool.markupobject.ispointwithinarea(mousePos.x, mousePos.y) === false) {
                                iswithin = false;
                                break;
                            }
                        }
                        if(iswithin === false) {
                            return;
                        }

                        
                    }

                    /*if(currentParentMarkupNumber !== 0) {
                        var parentmarkupobj = DocObj.markuplist.find(obj => obj.markupnumber === currentParentMarkupNumber);
 
                        let iswithin = true;
                        for(let pointIdx=0; pointIdx < parentmarkupobj.points.length; pointIdx++) {
                            let point = parentmarkupobj.points[pointIdx];


                            if(parentmarkupobj.ispointwithinarea(mousePos.x, mousePos.y) === false) {
                                iswithin = false;
                                break;
                            }
                        }
                        if(iswithin === false) {
                            return;
                        }
                    }*/

                    if(ev.button == 0){
                        var bWithin = MousePosdrwext(mousePos);
                        if (!bWithin && bLimMarkupExtent) {
                            return;
                        }

                        if (!tool.started) {


                            areamarkupobj = new MarkupObject(type, subtype, fillstyle);
                            
                            if (type == 8 &&subtype == 1){
                                areamarkupobj.linewidth = 1;
                                areamarkupobj.AddAttribute('Rx_Area', '0');
                                areamarkupobj.AddAttribute('SpaceID', '0');
                            }

                            areamarkupobj.pagenumber = DocObj.getcurPage();
                            areamarkupobj.viewname = DocObj.getcur3DView();

                            if(areamarkupobj.viewname != null){
                                areamarkupobj.bfor3D = true;
                                areamarkupobj.pageheight = canvasoheight;
                                areamarkupobj.pagewidth = canvasowidth;
                            }

                            tool.setdimoffset(true);

                            if (havesnap) {
                                if(DocObj.getCurrentpage().getpageRotation() != 0){
                                    var rotatedpoint = snap_rotated(snapPoint);
                                }else{
                                    rotatedpoint = snapPoint;
                                }

                                areamarkupobj.x = rotatedpoint.x;
                                areamarkupobj.y = rotatedpoint.y;
                                areamarkupobj.seteditaction(0);
                                areamarkupobj.addpoint(rotatedpoint.x, rotatedpoint.y);
                                tool.started = true;
                                if(DocObj){
                                    DocObj.bMarkupchanged = true;
                                }
                                //areamarkupobj.startdraw(context);
                                areamarkupobj.addpoint(rotatedpoint.x, rotatedpoint.y);


                            } else {
                                areamarkupobj.x = mousePos.x;
                                areamarkupobj.y = mousePos.y;
                                areamarkupobj.seteditaction(0);
                                areamarkupobj.addpoint(mousePos.x, mousePos.y);
                                tool.started = true;
                                if(DocObj){
                                    DocObj.bMarkupchanged = true;
                                }
                                //areamarkupobj.startdraw(context);
                                areamarkupobj.addpoint(mousePos.x, mousePos.y);

                            }

                        } else {

                            var banglemeasure = (areamarkupobj.type == 1 && (areamarkupobj.subtype == 4 || areamarkupobj.subtype == 5));

                            

                            if (havesnap) {
                                if(DocObj.getCurrentpage().getpageRotation() != 0){
                                    rotatedpoint = snap_rotated(snapPoint);
                                }else{
                                    rotatedpoint = snapPoint;
                                }
                                if (tool.bUsemouseinput){

                                    if(banglemeasure){
                                        if(areamarkupobj.points.length < 3){
                                            areamarkupobj.addpoint(rotatedpoint.x, rotatedpoint.y);
                                        }else{
                                            tool.completeArea(0);
                                        }
    
                                    }else{

                                        //pencilmarkupobj.addline();
                                        //tool.started = true;
                                        //pencilmarkupobj.addpoint(mousePos.x, mousePos.y);

                                        
                                        areamarkupobj.addpoint(rotatedpoint.x, rotatedpoint.y);
                                        
                                    }


                                    
                                }
                                
                            } else {
                                if (tool.bUsemouseinput){

                                    if(banglemeasure){

                                        if(areamarkupobj.points.length < 3){
                                            areamarkupobj.addpoint(mousePos.x, mousePos.y);
                                        }else{
                                            tool.completeArea(0);
                                        }

                                        
                                    }else{

                                        areamarkupobj.addpoint(mousePos.x, mousePos.y);
                                        
                                    }
                                    
                                }
                                
                            }


                        }
                    }else{
                       prevx = mousePos.x;
                       prevy = mousePos.y;
                       

                    }


                };

                // This function is called every time you move the mouse. Obviously, it only
                // draws if the tool.started state is set to true (when you are holding down
                // the mouse button).
                this.MSPointerMove = function (ev) {
                    ev.preventDefault();
                    ev.stopPropagation();
                    
                    var mousePos = getMousePos(canvas, ev);

                    tool.curmousepos = mousePos;


                    var xdiff = prevx - mousePos.x;
                    var ydiff = prevy - mousePos.y;

                    snapPoint = DocObj.getCurrentpage().getsnap(mousePos);

                    if (!snapPoint.found || snapPoint == undefined) {
                        havesnap = false;
                        if (!tool.started) {
                            context.clearRect(0, 0, canvas.width, canvas.height);
                        }

                    } else {
                        havesnap = true;
                        if (!tool.started) {
                            drawsnap(context,snapPoint);
                        }


                    }


                    var bWithin = MousePosdrwext(mousePos);

                    if (ev.button == 2 || ev.buttons == 2) {
                        DocObj.getCurrentpage().pan_update(xdiff, ydiff);
                        
                        prevx = mousePos.x;
                        prevy = mousePos.y;
                        
                        if (tool.started) {
                            tool.setdimoffset(false);
                        }

                    }

                    if (!tool.started) {
                        return;
                    }
                    if (bWithin || !bLimMarkupExtent) {
                        context.clearRect(0, 0, canvas.width, canvas.height);
                        if (havesnap) {
                            if(DocObj.getCurrentpage().getpageRotation() != 0){
                                var rotatedpoint = snap_rotated(snapPoint);
                            }else{
                                rotatedpoint = snapPoint;
                            }
                            drawsnap(context,snapPoint);

                            if (tool.bUsemouseinput){
                                areamarkupobj.setlastpoint(rotatedpoint.x, rotatedpoint.y);
                            }
                            
                            tool.setdimoffset(false);
                            if(areamarkupobj.getnumpoints() > 0){
                                areamarkupobj.drawme(context);
                            }



                            limprevx = rotatedpoint.x;
                            limprevy = rotatedpoint.y;

                        } else {

                            if (tool.bUsemouseinput){
                                areamarkupobj.setlastpoint(mousePos.x, mousePos.y);
                            }
                            
                            tool.setdimoffset(false);
                            if(areamarkupobj.getnumpoints() > 0){
                                areamarkupobj.drawme(context);
                            }



                            limprevx = mousePos.x;
                            limprevy = mousePos.y;

                        }
                        if (havesnap) {
                            //drawsnap(context,snapPoint);
                        }
                        
                        if (ev.button == 2 || ev.buttons == 2) {

                        }


                    } else {

                        context.clearRect(0, 0, canvas.width, canvas.height);

                        if (tool.bUsemouseinput){
                            areamarkupobj.setlastpoint(limprevx, limprevy);
                        }
                        
                        if(areamarkupobj.getnumpoints() > 0){
                            areamarkupobj.drawme(context);
                        }




                    }


                };

                this.mousemove = function (ev) {
                    ev.preventDefault();
                    ev.stopPropagation();
                    
                    var mousePos = getMousePos(canvas, ev);
                    tool.curmousepos = mousePos;

                    var xdiff = prevx - mousePos.x;
                    var ydiff = prevy - mousePos.y;

                    snapPoint = DocObj.getCurrentpage().getsnap(mousePos);

                    if (!snapPoint.found || snapPoint == undefined) {
                        havesnap = false;
                        if (!tool.started) {
                            context.clearRect(0, 0, canvas.width, canvas.height);
                        }

                    } else {
                        havesnap = true;
                        if (!tool.started) {
                            context.clearRect(0, 0, canvas.width, canvas.height);
                            drawsnap(context,snapPoint);
                        }


                    }


                    var bWithin = MousePosdrwext(mousePos);

                    if (ev.button == 2 || ev.buttons == 2) {
                        DocObj.getCurrentpage().pan_update(xdiff, ydiff);
                        
                        prevx = mousePos.x;
                        prevy = mousePos.y;

                        if (tool.started) {
                            tool.setdimoffset(false);

                            /*if(areamarkupobj.points.length > 0){
                                areamarkupobj.drawme(context);
                            }*/

                        }

                    }

                    if (!tool.started) {
                        return;
                    }

                    if(tool.subtype == 2 && tool.markupobject != undefined){

                        let iswithin = true;

                        for(let pointIdx=0; pointIdx < tool.markupobject.points.length; pointIdx++) {
                            let point = tool.markupobject.points[pointIdx];


                            if(tool.markupobject.ispointwithinarea(mousePos.x, mousePos.y) === false) {
                                iswithin = false;
                                break;
                            }
                        }
                        if(iswithin === false) {
                            return;
                        }

                        
                    }


                    /*if(currentParentMarkupNumber !== 0) {
                        var parentmarkupobj = DocObj.markuplist.find(obj => obj.markupnumber === currentParentMarkupNumber);
 
                        let iswithin = true;
                        for(let pointIdx=0; pointIdx < parentmarkupobj.points.length; pointIdx++) {
                            let point = parentmarkupobj.points[pointIdx];
                            if(parentmarkupobj.ispointwithinarea(mousePos.x, mousePos.y) === false) {
                                iswithin = false;
                                break;
                            }
                        }
                        if(iswithin === false) {
                            return;
                        }
                    }*/


                    if (bWithin || !bLimMarkupExtent) {

                        context.clearRect(0, 0, canvas.width, canvas.height);
                        if (havesnap) {
                            if(DocObj.getCurrentpage().getpageRotation() != 0){
                                var rotatedpoint = snap_rotated(snapPoint);
                            }else{
                                rotatedpoint = snapPoint;
                            }
                            drawsnap(context,snapPoint);

                            if (tool.bUsemouseinput){
                                areamarkupobj.setlastpoint(rotatedpoint.x, rotatedpoint.y);
                            }
                            
                            tool.setdimoffset(false);
                            if(areamarkupobj.getnumpoints() > 0){
                                areamarkupobj.drawme(context);
                            }



                            prevx = rotatedpoint.x;
                            prevy = rotatedpoint.y;

                        } else {
                            if (tool.bUsemouseinput){
                                areamarkupobj.setlastpoint(mousePos.x, mousePos.y);
                            }
                            
                            tool.setdimoffset(false);
                            if(areamarkupobj.getnumpoints() > 0){
                                areamarkupobj.drawme(context);
                            }



                            prevx = mousePos.x;
                            prevy = mousePos.y;

                        }
                        if (havesnap) {
                            //drawsnap(context,snapPoint);
                        }
                        if (ev.button == 2 || ev.buttons == 2) {

                        }



                    } else {

                        context.clearRect(0, 0, canvas.width, canvas.height);
                        if (tool.bUsemouseinput){
                            areamarkupobj.setlastpoint(prevx, prevy);
                        }
                        
                        tool.setdimoffset(false);
                        if(areamarkupobj.getnumpoints() > 0){
                            areamarkupobj.drawme(context);
                        }




                    }

                };

                // This is called when you release the mouse button.
                this.MSPointerUp = function (ev) {
                    //areamarkupobj.addpoint(mousePos.x, ev._y);

                    var mousePos = getMousePos(canvas, ev);

                    tool.refreshMouseUp(mousePos);
                };
                this.mouseup = function (ev) {
                    //areamarkupobj.addpoint(mousePos.x, ev._y);

                    var mousePos = getMousePos(canvas, ev);

                    tool.refreshMouseUp(mousePos);
                };

                this.completeSegment = function(nsplice){
                    if (areamarkupobj == null){
                        return;
                    }

                    //tool.started = false;
                    if(nsplice > 0){
                        areamarkupobj.points.splice(areamarkupobj.points.length - nsplice, nsplice);
                    }

                    var bpolylinesegment = (areamarkupobj.type == 1 && areamarkupobj.subtype == 6);

                    
                    if(bpolylinesegment && areamarkupobj.points.length < 2){
                        //areamarkupobj = null;
                        return;
                    }   

                    areamarkupobj.addline();

                };

                this.applySegment = function(){

                    
                    //context.clearRect(0, 0, canvas.width, canvas.height);
                 

                    
                    if(!tool.started){
                        return;
                    }

                    if (areamarkupobj == null){
                        return;
                    }

                    var bsegmentmeasure = (areamarkupobj.type == 1 && areamarkupobj.subtype == 6);

                    if(!bsegmentmeasure){
                        return;
                    }

                    tool.started = false;


                    areamarkupobj.findrectangle();

                    if (!bMultimarkupadd) {

                        bMarkupcrated = true;
                        //ft 08.08.2018 changed from separate index to direct array length
                        nMarkupcreated = DocObj.markuplist.length;
                        //nMarkupcreated = DocObj.nummarkups;
                    }


                    areamarkupobj.savemetolistDraw();
                    var bsaved = areamarkupobj.saved;
                     
                    areamarkupobj = null;
                    img_update();
                    drawmarkupAll(cntximg);


                    if(bsaved){


                        if (RxCore_GUI_Markup != undefined) {
                            //RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[index],true);
                            RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[nMarkupcreated], {created : true, modified : false, deleted : false, multi : bMultimarkupadd});
                        }

                    }else{
                        bMarkupcrated = bsaved;
                        nMarkupcreated = -1;

                        if (!bMultimarkupadd) {
                            
                            //ft 08.08.2018 changed from separate index to direct array length
                            //nMarkupcreated = DocObj.nummarkups;

                            RxCore_MarkupSelect(true);

                        }
    
                    }

                };

                this.completeArea = function(nsplice){

                    if (areamarkupobj == null){
                        return;
                    }
                        //tool.mousemove(ev);
                        //tool.started = false;
                        tool.started = false;


                        if(nsplice > 0){
                            areamarkupobj.points.splice(areamarkupobj.points.length - nsplice, nsplice);
                        }
                        
                        var bpolygon = ((areamarkupobj.type == 1 && areamarkupobj.subtype == 2) || (areamarkupobj.type == 8) );
                        var bpolyline = ((areamarkupobj.type == 1 && areamarkupobj.subtype == 3) || (areamarkupobj.type == 1 && areamarkupobj.subtype == 1));

                        if(bpolygon && areamarkupobj.points.length < 3){
                            areamarkupobj = null;
                            return;
                        }                      
                        if(bpolyline && areamarkupobj.points.length < 2){
                            areamarkupobj = null;
                            return;
                        }   


                        areamarkupobj.findrectangle();
                        if (!bMultimarkupadd) {

                            bMarkupcrated = true;
                            //ft 08.08.2018 changed from separate index to direct array length
                            
                            //nMarkupcreated = DocObj.nummarkups;
                        }

                        nMarkupcreated = DocObj.markuplist.length;


                        if(tool.subtype == 2 && tool.markupobject != undefined){

                            areamarkupobj.parent = tool.markupobject;
                            //var parentmarkupobj = tool.markupobject;
                            tool.markupobject.holes.push(areamarkupobj);
                            areamarkupobj.isStartedHole = false;

                            //areamarkupobj.saved = true;
    
                            
                            
                        }
                        
                        areamarkupobj.savemetolistDraw();
                        var bsaved = areamarkupobj.saved;

    







                        /*areamarkupobj.parentmarkupnumber = currentParentMarkupNumber;
 
                        if(areamarkupobj.parentmarkupnumber !== 0) {
                            var parentmarkupobj = DocObj.markuplist.find(obj => obj.markupnumber === areamarkupobj.parentmarkupnumber);
                            parentmarkupobj.holes.push(areamarkupobj.markupnumber);
                            areamarkupobj.isStartedHole = false;
                            currentParentMarkupNumber = 0;
                        }*/                        
                        
                        areamarkupobj = null;
                        img_update();
                        drawmarkupAll(cntximg);


                        if(bAlwaysMarkupCallback && bsaved){
                            if (RxCore_GUI_Markup != undefined) {


                                if(tool.subtype == 2 && tool.markupobject != undefined){
                                    RxCore_GUI_Markup.setMarkupSelected(tool.markupobject, {created : true, modified : false, deleted : false, multi : bMultimarkupadd});
                                }else{
                                    RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[nMarkupcreated], {created : true, modified : false, deleted : false, multi : bMultimarkupadd});
                                }

                                
                            }
                
                        }else{
                            if(!bMultimarkupadd && bsaved){
                                if (RxCore_GUI_Markup != undefined) {
                                    //RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[index],true);

                                    if(tool.subtype == 2 && tool.markupobject != undefined){
                                        RxCore_GUI_Markup.setMarkupSelected(tool.markupobject, {created : true, modified : false, deleted : false});    
                                    }else{
                                        RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[nMarkupcreated], {created : true, modified : false, deleted : false});
                                    }
    
                                }
    
                            }
                        }

                        /*if(bsaved){

                            if (RxCore_GUI_Markup != undefined) {
                                //RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[index],true);
                                RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[nMarkupcreated], {created : true, modified : false, deleted : false, multi : bMultimarkupadd});
                            }

                        }else{
                            bMarkupcrated = bsaved;
                            nMarkupcreated = -1;

                            if (!bMultimarkupadd) {
                                RxCore_MarkupSelect(true);
                            }
    
                        }*/

                };

                this.dblclick = function (ev) {
                    
                    
                    var banglemeasure = (areamarkupobj.type == 1 && (areamarkupobj.subtype == 4 || areamarkupobj.subtype == 5));

                    var bsegmentmeasure = (areamarkupobj.type == 1 && areamarkupobj.subtype == 6);

                    if(banglemeasure){
                        return;
                    }

                    if (!tool.bUsemouseinput){
                        return;
                    }

                    

                    if (tool.started) {
                        
                        
                        if (bsegmentmeasure){
                            tool.completeSegment(2);
                        }else{
                            tool.completeArea(2);
                        }
                        
                        
                        //tool.mousemove(ev);
                        //tool.started = false;
                        /*tool.started = false;
                        areamarkupobj.points.splice(areamarkupobj.points.length - 2, 2);
                        areamarkupobj.findrectangle();
                        if (!bMultimarkupadd) {

                            bMarkupcrated = true;
                            //ft 08.08.2018 changed from separate index to direct array length
                            nMarkupcreated = DocObj.markuplist.length;
                            //nMarkupcreated = DocObj.nummarkups;
                        }

                        areamarkupobj.savemetolistDraw();
                        areamarkupobj = null;
                        img_update();
                        drawmarkupAll(cntximg);
                        

                        if (!bMultimarkupadd) {
                            //need to move to connection object.
                            //markupcreated();
                            if (RxCore_GUI_Markup != undefined) {
                                //RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[index],true);
                                RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[nMarkupcreated], {created : true, modified : false, deleted : false});
                            }

                        }*/

                    }

                };


                this.doubletap = function (ev) {
                    
                    
                    var banglemeasure = (areamarkupobj.type == 1 && (areamarkupobj.subtype == 4 || areamarkupobj.subtype == 5));

                    var bsegmentmeasure = (areamarkupobj.type == 1 && areamarkupobj.subtype == 6);

                    if(banglemeasure){
                        return;
                    }

                    if (!tool.bUsemouseinput){
                        return;
                    }

                    if (tool.started) {


                        if (bsegmentmeasure){
                            tool.completeSegment(1);
                        }else{
                            
                            tool.completeArea(1);
                        }


                        /*tool.started = false;
                        areamarkupobj.points.splice(areamarkupobj.points.length - 1, 1);
                        areamarkupobj.findrectangle();
                        if (!bMultimarkupadd) {

                            bMarkupcrated = true;
                            //ft 08.08.2018 changed from separate index to direct array length
                            nMarkupcreated = DocObj.markuplist.length;
                            //nMarkupcreated = DocObj.nummarkups;
                        }

                        areamarkupobj.savemetolistDraw();
                        areamarkupobj = null;
                        img_update();
                        drawmarkupAll(cntximg);
                        //tool.terminated = true;

                        if (!bMultimarkupadd) {
                            //need to move to connection object.
                            //markupcreated();
                            if (RxCore_GUI_Markup != undefined) {
                                //RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[index],true);
                                RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[nMarkupcreated], {created : true, modified : false, deleted : false});
                            }

                        }*/

                    }

                };

                this.touchstart = function (ev) {
                    ev.preventDefault();
                    var touchPos = getTouchPos(canvas, ev, 0);
                    var touchx = touchPos.x;
                    var touchy = touchPos.y;
                    var lastTouch = 0;

                    //doubletap

                    var now = new Date().getTime();
                    if (tool.LastTouchdata == -1) {
                        lastTouch = now + 1;
                    } else {
                        lastTouch = tool.LastTouchdata;

                    }
                    var delta = now - lastTouch;
                    tool.LastTouchdata = now;

                    if (delta < 500 && delta > 0) {
                        tool.doubletap(ev);
                        //alert("double tap");
                        return;
                    }

                    //doubletap

                    var bWithin = MousePosdrwext(touchPos);
                    if (!bWithin && bLimMarkupExtent) {
                        return;
                    }

                    if (!tool.started) {
                        areamarkupobj = new MarkupObject(type, subtype, fillstyle);

                        if (type == 8 && subtype == 1){
                            areamarkupobj.linewidth = 1;
                            areamarkupobj.AddAttribute('Rx_Area', '0');
                            areamarkupobj.AddAttribute('SpaceID', '0');
                        }

                        areamarkupobj.pagenumber = DocObj.getcurPage();
                        areamarkupobj.viewname = DocObj.getcur3DView();

                        if(areamarkupobj.viewname != null){
                            areamarkupobj.bfor3D = true;
                            areamarkupobj.pageheight = canvasoheight;
                            areamarkupobj.pagewidth = canvasowidth;
                        }

                        tool.setdimoffset(true);

                        areamarkupobj.x = touchx;
                        areamarkupobj.y = touchy;
                        areamarkupobj.seteditaction(0);
                        areamarkupobj.addpoint(touchx, touchy);
                        //areamarkupobj.startdraw(context);
                        areamarkupobj.addpoint(touchx, touchy);
                        tool.started = true;
                        if(DocObj){
                            DocObj.bMarkupchanged = true;
                        }

                    } else {
                        
                        var banglemeasure = (areamarkupobj.type == 1 && (areamarkupobj.subtype == 4 || areamarkupobj.subtype == 5));

                        
                        if (tool.bUsemouseinput){

                            if(banglemeasure){
                                if(areamarkupobj.points.length < 3){
                                    areamarkupobj.addpoint(touchx, touchy);        
                                }else{
                                    tool.completeArea(0);
                                }
                            }else{
                                areamarkupobj.addpoint(touchx, touchy);
                            }

                            
                        }
                        
                        //polygonmarkupobj.setlastpoint(touchx,touchy);
                    }


                };

                this.touchmove = function (ev) {
                    ev.preventDefault();
                    var touchPos = getTouchPos(canvas, ev, 0);
                    var touchx = touchPos.x;
                    var touchy = touchPos.y;
                    if (!tool.started) {
                        return;
                    }
                    var bWithin = MousePosdrwext(touchPos);
                    if (bWithin || !bLimMarkupExtent) {

                        context.clearRect(0, 0, canvas.width, canvas.height);
                        if (tool.bUsemouseinput){
                            areamarkupobj.setlastpoint(touchPos.x, touchPos.y);
                        }
                        
                        if(areamarkupobj.getnumpoints() > 0){
                            areamarkupobj.drawme(context);
                        }



                        prevx = touchPos.x;
                        prevy = touchPos.y;

                    } else {

                        context.clearRect(0, 0, canvas.width, canvas.height);
                        
                        if (tool.bUsemouseinput){
                            areamarkupobj.setlastpoint(prevx, prevy);
                        }
                        
                        
                        if(areamarkupobj.getnumpoints() > 0){
                            areamarkupobj.drawme(context);
                        }



                    }
                    /*if (tool.started) {
                     context.clearRect(0, 0, canvas.width, canvas.height);
                     areamarkupobj.setlastpoint(touchx,touchy);
                     areamarkupobj.drawme(context);

                     }*/
                };
                
                this.touchcancel = function(ev){
                    ev.preventDefault();

                    var touchPos = getTouchPos(canvas, ev, 0);
                    tool.refreshMouseUp(touchPos);

                    var touchPos = getTouchPos(canvas, ev, 0);
                    var touchx = touchPos.x;
                    var touchy = touchPos.y;

                    if (tool.started) {

                        
                        var banglemeasure = (areamarkupobj.type == 1 && (areamarkupobj.subtype == 4 || areamarkupobj.subtype == 5));

                        if (tool.bUsemouseinput){

                            if(banglemeasure){
                                if(areamarkupobj.points.length < 3){
                                    areamarkupobj.addpoint(touchx, touchy);    
                                }else{
                                    tool.completeArea(0);
                                }
    
                            }else{
                                areamarkupobj.addpoint(touchx, touchy);
                            }
                            
                        }                        
                        
                    }

                };

                this.touchend = function (ev) {
                    ev.preventDefault();
                    var touchPos = getTouchPos(canvas, ev, 0);
                    tool.refreshMouseUp(touchPos);


                    var touchPos = getTouchPos(canvas, ev, 0);
                    var touchx = touchPos.x;
                    var touchy = touchPos.y;

                    if (tool.started) {

                        
                        var banglemeasure = (areamarkupobj.type == 1 && (areamarkupobj.subtype == 4 || areamarkupobj.subtype == 5));

                        if (tool.bUsemouseinput){

                            if(banglemeasure){
                                if(areamarkupobj.points.length < 3){
                                    areamarkupobj.addpoint(touchx, touchy);    
                                }else{
                                    tool.completeArea(0);
                                }
    
                            }else{
                                areamarkupobj.addpoint(touchx, touchy);
                            }
                            
                        }
                        
                    }


                };

                

                this.keydown = function (ev) {
                    //ev.preventDefault();

                    if(docpagNotReady()){
                        return;
                    }
            

                    if(areamarkupobj == null || areamarkupobj == undefined){
                        return;
                    }
                                
                    var banglemeasure = (areamarkupobj.type == 1 && (areamarkupobj.subtype == 4 || areamarkupobj.subtype == 5));

                    var bsegmentmeasure = (areamarkupobj.type == 1 && areamarkupobj.subtype == 6);

                    if(readonlymode){
                        DocObj.bMarkupLocked = readonlymode;
                    }

                    /*if(DocObj.bMarkupLocked){
                        return;
                    }*/


                    switch (ev.keyCode) {
                        case 13:
                            ev.stopPropagation();    
                        //tool.apply();//complete markup

                            if(bsegmentmeasure){
                                tool.completeSegment(1);

                            }else{
                                tool.started = false;
                                if(!banglemeasure){
                                    tool.completeArea(1);
                                }
                            }
                            break;
                        case 8:
                            tool.removelastPoint();
                            //remove last point
                            break;
                        case 46:
                          //delete last point  
                          break;
                    }

                };


                /*this.touchend = function (ev) {
                 ev.preventDefault();
                 if (tool.started) {
                 pencilmarkupobj.findrectangle();
                 pencilmarkupobj.savemetolist();
                 img_update();
                 //alert("touchend");
                 //tool.touchmove(ev);
                 tool.started = false;

                 pencilmarkupobj = null;
                 }
                 };*/


            };

            //the measure tool
            tools.measuretool = function (params) {

                var tool = this;
                this.name = 'measuretool';
                this.anglelengthsupport = false;
                this.refreshSupport = false;

                this.started = false;
                var Unit = "mm";
                var AreaUnit = "mm\u00B2";
                this.Area = 0.0;
                this.totaldistance = 0;
                //dialog content html
                this.LastTouchdata = -1;

                var havesnap = false;
                var snapPoint;
                var btwoopoints = params.twoopoints;


                /*this.dialogtextstart = "<p><strong>Measurement Result</strong></p>\n" +
                 "        <div style='height:200px;overflow-y:scroll;border:1px solid #769dc4;padding:0 10px;width:360px;'>\n" +
                 "            <p>\n" +
                 "                <table id='MeasureInfo'>\n" +
                 "                <TR>\n" +
                 "                    <TH nowrap>Points</TH><TH nowrap align='left'>Angle</TH><TH nowrap align='left'>Distance</TH>\n" +
                 "                </TR>\n" +
                 "                <TR>\n";
                 this.tabletext = "";
                 this.dialogtextend = "                </TR>\n" +
                 "            </table>\n" +
                 "            </p>\n" +
                 "            </div>\n";

                 this.dialogfieldtext = "";

                 this.dialogcontent = "";*/
                //dialog content html

                this.points = [];
                this.numpoints = -1;

                this.lines = [];
                this.numlines = -1;

                this.lengths = [];
                this.numlengths = -1;

                this.angles = [];
                this.numangles = -1;
                this.result = {
                    points:[],
                    lines:[],
                    lengths:[],
                    angles:[],
                    numpoints:-1,
                    numlines:-1,
                    numlengths:-1,
                    numangles:-1,
                    area:0,
                    totaldistance:0
                };

                
                setCursor(getMarkupCursor('measuretool'));


                this.addToTable = function () {
                    var lcounter = 0;
                    var point1 = 0;
                    var point2 = 0;
                    var pointstext = "";
                    var angletext = "";
                    var length = 0;
                    var texttotable = "";
                    var trueangle = 0;

                    var markupscalesq = DocObj.getCurrentpage().dscale * DocObj.getCurrentpage().dscale;
                    if (DocObj.getCurrentpage().usevectorxml) {
                        markupscalesq = DocObj.getCurrentpage().dscalevector * DocObj.getCurrentpage().dscalevector;
                    } else if (DocObj.getCurrentpage().usepdfjs) {
                        markupscalesq = (DocObj.getCurrentpage().curpagescale * DocObj.getCurrentpage().dscalepdf) * (DocObj.getCurrentpage().curpagescale * DocObj.getCurrentpage().dscalepdf);
                    }
                    //tool.tabletext = texttotable;
                    tool.result.area = tool.PolygonArea();
                    tool.result.area = getUnitArea(tool.result.area / markupscalesq);
                    //tool.result.area = tool.result.area.toFixed(2);
                    if (DocObj.getCurrentpage().usevectorxml) {
                        tool.result.totaldistance = getUnitlength(tool.result.totaldistance / DocObj.getCurrentpage().dscalevector);
                    } else if (DocObj.getCurrentpage().usepdfjs) {
                        tool.result.totaldistance = getUnitlength(tool.result.totaldistance / (DocObj.getCurrentpage().curpagescale * DocObj.getCurrentpage().dscalepdf));
                    } else {
                        tool.result.totaldistance = getUnitlength(tool.result.totaldistance / DocObj.getCurrentpage().dscale);
                    }


                    //Unit = Unitlabel;
                    //  AreaUnit = AreaUnitlabel;


                    /*for(lcounter = 0;lcounter<tool.lengths.length;lcounter++){
                     if(lcounter>0){
                     trueangle = tool.angles[lcounter-1]*( 180 / Math.PI);
                     angletext = trueangle.toFixed(0);
                     angletext = angletext + "&deg;";
                     }else{
                     angletext = "-";
                     }


                     point1 = lcounter + 1;
                     point2 = lcounter + 2;
                     pointstext = point1 + "-" + point2;
                     length = tool.lengths[lcounter];
                     if(DocObj.getCurrentpage().usevectorxml){
                     length = getUnitlength(length/DocObj.getCurrentpage().dscalevector);
                     } else if (DocObj.getCurrentpage().usepdfjs){
                     length = getUnitlength(length/(DocObj.getCurrentpage().curpagescale*DocObj.getCurrentpage().dscalepdf));
                     } else {
                     length = getUnitlength(length/DocObj.getCurrentpage().dscale);
                     }

                     length = length.toFixed(2);

                     texttotable = texttotable + "<TR>";
                     texttotable = texttotable + "                    <TD nowrap>" + pointstext + "</TD>\n";
                     texttotable = texttotable + "                    <TD>\n" + angletext + "</TD>\n";
                     texttotable = texttotable + "                    <TD>\n" + length + Unit + " </TD>\n";
                     texttotable = texttotable + "</TR>";

                     }*/

                    /*var markupscalesq = DocObj.getCurrentpage().dscale*DocObj.getCurrentpage().dscale;
                     if(DocObj.getCurrentpage().usevectorxml){
                     markupscalesq = DocObj.getCurrentpage().dscalevector*DocObj.getCurrentpage().dscalevector;
                     }else if(DocObj.getCurrentpage().usepdfjs){
                     markupscalesq = (DocObj.getCurrentpage().curpagescale*DocObj.getCurrentpage().dscalepdf)*(DocObj.getCurrentpage().curpagescale*DocObj.getCurrentpage().dscalepdf);
                     }
                     tool.tabletext = texttotable;
                     tool.Area = tool.PolygonArea();
                     tool.Area = getUnitArea(tool.Area/markupscalesq);
                     tool.Area = tool.Area.toFixed(2);
                     if(DocObj.getCurrentpage().usevectorxml){
                     tool.totaldistance = getUnitlength(tool.totaldistance/DocObj.getCurrentpage().dscalevector);
                     }else if (DocObj.getCurrentpage().usepdfjs){
                     tool.totaldistance = getUnitlength(tool.totaldistance/(DocObj.getCurrentpage().curpagescale*DocObj.getCurrentpage().dscalepdf));
                     }else{
                     tool.totaldistance = getUnitlength(tool.totaldistance/DocObj.getCurrentpage().dscale);
                     }

                     tool.totaldistance = tool.totaldistance.toFixed(2);

                     this.dialogfieldtext = "<br>" +
                     "       <table>" +
                     "        <tr>" +
                     "	     <td>Distance: </td>" + "<td><input type='text' value='" + tool.totaldistance + "'> " + Unit + "</td>\n" +
                     "          </tr>" +
                     "           <tr>" +
                     "	     <td>Area: </td>" + "<td><input type='text' value='" + tool.Area + "'> " + AreaUnit + "</td>\n" +
                     "        </tr>" +
                     "        </table>" +
                     "        <br />\n" +
                     "        <br />\n";*/
                    //"        <button data-dojo-type='dijit/form/Button' data-dojo-props='onClick:hideMDialog'>OK</button>\n";


                };

                this.PolygonArea = function () {
                    var i = 0;
                    var area = 0.0;
                    var n = tool.result.points.length;


                    for (i = 0; i < n - 1; i++) {
                        area += (tool.result.points[i].x * tool.result.points[i + 1].y) - (tool.result.points[i + 1].x * tool.result.points[i].y);
                    }
                    area += (tool.result.points[n - 1].x * tool.result.points[0].y) - (tool.result.points[0].x * tool.result.points[n - 1].y);
                    return Math.abs(area / 2.0);
                };


                this.getdiag = function (width, height) {


                    var dimwsq = Math.pow(width, 2);
                    var dimhsq = Math.pow(height, 2);
                    var dimdiag = Math.sqrt((dimwsq + dimhsq));

                    return dimdiag;

                };


                this.addpoint = function (x, y) {
                    tool.result.points.push(new point(x, y));
                    tool.result.numpoints++;

                    if (btwoopoints && tool.result.numpoints === 2 && RxCore_GUI_Measurediag != undefined) {
                        
                        tool.started = false;
                        
                        tool.result.points.splice(2, 1);
                        tool.result.totaldistance = 0;
                        tool.calculatedistance();
                        tool.addToTable();

                        RxCore_GUI_Measurediag.setMeasureresult(tool.result);

                        tool.result.points = [];
                        tool.result.numpoints = -1;
                        tool.result.lengths = [];
                        tool.result.numlengths = -1;
                        tool.result.angles = [];
                        tool.result.numangles = -1;
                        tool.result.lines = [];
                        tool.result.numlines = -1;

                        context.clearRect(0, 0, canvas.width, canvas.height);
                        RxCore_default();



                    }
                    

                };



                this.setlastpoint = function (x, y) {
                    tool.result.points[tool.result.points.length-1].x = x;
                    tool.result.points[tool.result.points.length-1].y = y;

                };

                this.startdraw = function (ctx) {
                    ctx.beginPath();
                    ctx.moveTo(tool.result.points[tool.result.points.length-1].x, tool.result.points[tool.result.points.length-1].y);

                };


                this.getangleofline = function (startx, starty, endx, endy, useabs) {

                    var width = 0;
                    var height = 0;
                    
                    if (useabs) {
                        width = Math.abs(endx - startx);
                        height = Math.abs(endy - starty);
                    } else {
                        width = endx - startx;
                        height = endy - starty;
                    }

                    return Math.atan2(height, width);
                };


                this.findanglebetweenlines = function () {
                    var counter = 0;
                    var startx = 0;
                    var starty = 0;
                    var endx = 0;
                    var endy = 0;
                    var reangle = false;

                    var startAngle = 0;
                    var endAngle = 0;
                    var sumAngle = 0;

                    for (counter = 0; counter < tool.result.lines.length; counter++) {
                        if (counter > 0) {

                            if (tool.result.lines[counter - 1].height <= 0 && tool.result.lines[counter - 1].width >= 0) {
                                startAngle = tool.result.lines[counter - 1].rewangle;
                            } else if (tool.result.lines[counter - 1].width <= 0 && tool.result.lines[counter - 1].height >= 0) {
                                startAngle = tool.result.lines[counter - 1].rewangle;
                            } else if (tool.result.lines[counter - 1].width >= 0 && tool.result.lines[counter - 1].height >= 0) {
                                startAngle = tool.result.lines[counter - 1].angle;
                            } else if (tool.result.lines[counter - 1].width <= 0 && tool.result.lines[counter - 1].height <= 0) {
                                startAngle = tool.result.lines[counter - 1].angle;
                            }

                            if (tool.result.lines[counter].height <= 0 && tool.result.lines[counter].width >= 0) {

                                endAngle = tool.result.lines[counter].rewangle;
                            } else if (tool.result.lines[counter].width <= 0 && tool.result.lines[counter].height >= 0) {
                                endAngle = tool.result.lines[counter].rewangle;
                            } else if (tool.result.lines[counter].width >= 0 && tool.result.lines[counter].height >= 0) {
                                endAngle = tool.result.lines[counter].angle;
                            } else if (tool.result.lines[counter].width <= 0 && tool.result.lines[counter].height <= 0) {
                                endAngle = tool.result.lines[counter].angle;
                            }

                            sumAngle = Math.PI - Math.abs(startAngle + endAngle);
                            tool.result.angles.push(Math.abs(sumAngle));
                            tool.result.numangles++;


                        }
                    }


                };


                this.calculatedistance = function () {
                    var acounter = 0;
                    var startx = 0;
                    var starty = 0;
                    var endx = 0;
                    var endy = 0;
                    var width = 0;
                    var height = 0;
                    var length = 0;
                    var curline;


                    if (tool.result.points.length >= 2) {
                        startx = tool.result.points[0].x;
                        starty = tool.result.points[0].y;

                        for (acounter = 1; acounter < tool.result.points.length; acounter++) {

                            endx = tool.result.points[acounter].x;
                            endy = tool.result.points[acounter].y;


                            if (acounter != tool.result.points.length) {
                                width = Math.abs(endx - startx);
                                height = Math.abs(endy - starty);
                                length = tool.getdiag(width, height);
                                if (length != 0) {
                                    tool.result.lengths.push(length);
                                    tool.result.numlengths++;
                                    curline = new lineangle(startx, starty, endx, endy);
                                    tool.result.lines.push(curline);
                                    tool.result.numlines++;
                                    tool.result.totaldistance = tool.result.totaldistance + length;
                                }
                            }
                            startx = endx;
                            starty = endy;


                        }
                        if (tool.result.points.length >= 3) {
                            endx = tool.result.points[0].x;
                            endy = tool.result.points[0].y;
                            width = Math.abs(endx - startx);
                            height = Math.abs(endy - starty);
                            length = tool.getdiag(width, height);
                            curline = new lineangle(startx, starty, endx, endy);
                            tool.result.lines.push(curline);
                            tool.result.numlines++;
                            tool.result.lengths.push(length);
                            tool.result.numlengths++;
                            tool.result.totaldistance = tool.result.totaldistance + length;

                        }

                    }


                };

                this.draw = function (ctx) {
                    var counter = 0;
                    var startAngle = 0.0;
                    var endAngle = 0.0;
                    var startx = 0;
                    var starty = 0;
                    var endx = 0;
                    var endy = 0;
                    var width = 0;
                    var height = 0;
                    var arcx = 0;
                    var arcy = 0;
                    var drawangle = false;

                    //var dimarea = 0;

                    ctx.strokeStyle = "red";
                    ctx.beginPath();
                    ctx.moveTo(tool.result.points[0].x, tool.result.points[0].y);

                    for (counter = 1; counter < tool.result.points.length; counter++) {
                        ctx.lineTo(tool.result.points[counter].x, tool.result.points[counter].y);
                        if (counter > 1) {
                            startx = tool.result.points[counter - 1].x;
                            starty = tool.result.points[counter - 1].y;
                            endx = tool.result.points[counter].x;
                            endy = tool.result.points[counter].y;

                            startAngle = tool.getangleofline(startx, starty, endx, endy, false);
                            //startAngle = startAngle;

                            startx = tool.result.points[counter - 2].x;
                            starty = tool.result.points[counter - 2].y;
                            endx = tool.result.points[counter - 1].x;
                            endy = tool.result.points[counter - 1].y;
                            arcx = endx;
                            arcy = endy;

                            endAngle = tool.getangleofline(startx, starty, endx, endy, false);
                            endAngle = endAngle - Math.PI;

                            drawangle = true;


                        }
                    }
                    ctx.strokeStyle = "red";
                    ctx.lineWidth = 1;
                    ctx.closePath();
                    ctx.stroke();
                    if (drawangle) {
                        ctx.beginPath();
                        ctx.arc(arcx, arcy, 50, startAngle, endAngle, false);
                        ctx.strokeStyle = "blue";
                        ctx.lineWidth = 4;
                        ctx.stroke();

                    }


                };


                // This is called when you start holding down the mouse button.
                // This starts the measure area drawing.
                this.pointermove = function (ev) {
                    tool.MSPointerMove(ev);
                };
                this.pointerdown = function (ev) {
                    tool.MSPointerDown(ev);
                };

                this.pointerup = function (ev) {
                    tool.MSPointerUp(ev);
                };

                this.MSPointerDown = function (ev) {
                    ev.preventDefault();
                    ev.stopPropagation();

                    
                    var mousePos = getMousePos(canvas, ev);
                    if (!tool.started) {

                        if (havesnap) {
                            if(DocObj.getCurrentpage().getpageRotation() != 0){
                                var rotatedpoint = snap_rotated(snapPoint);
                            }else{
                                rotatedpoint = snapPoint;
                            }

                            tool.addpoint(rotatedpoint.x, rotatedpoint.y);
                            tool.addpoint(rotatedpoint.x, rotatedpoint.y);
                        } else {
                            tool.addpoint(mousePos.x, mousePos.y);
                            tool.addpoint(mousePos.x, mousePos.y);
                        }
                        tool.started = true;

                    } else {
                        if (havesnap) {
                            if(DocObj.getCurrentpage().getpageRotation() != 0){
                                rotatedpoint = snap_rotated(snapPoint);
                            }else{
                                rotatedpoint = snapPoint;
                            }

                            tool.addpoint(rotatedpoint.x, rotatedpoint.y);
                        } else {
                            tool.addpoint(mousePos.x, mousePos.y);
                        }

                        /*if(tool.points[tool.numpoints].x != mousePos.x && tool.points[tool.numpoints].y != ev._y){
                         tool.addpoint(mousePos.x, ev._y);
                         }*/

                    }

                };
                this.mousedown = function (ev) {
                    ev.preventDefault();
                    ev.stopPropagation();

                    var mousePos = getMousePos(canvas, ev);
                    if (!tool.started) {

                        if (havesnap) {
                            if(DocObj.getCurrentpage().getpageRotation() != 0){
                                var rotatedpoint = snap_rotated(snapPoint);
                            }else{
                                rotatedpoint = snapPoint;
                            }

                            tool.addpoint(rotatedpoint.x, rotatedpoint.y);
                            tool.addpoint(rotatedpoint.x, rotatedpoint.y);
                        } else {
                            tool.addpoint(mousePos.x, mousePos.y);
                            tool.addpoint(mousePos.x, mousePos.y);
                        }
                        tool.started = true;


                    } else {
                        if (havesnap) {
                            if(DocObj.getCurrentpage().getpageRotation() != 0){
                                rotatedpoint = snap_rotated(snapPoint);
                            }else{
                                rotatedpoint = snapPoint;
                            }

                            tool.addpoint(rotatedpoint.x, rotatedpoint.y);
                        } else {
                            tool.addpoint(mousePos.x, mousePos.y);
                        }


                        /*if(tool.points[tool.numpoints].x != ev._x && tool.points[tool.numpoints].y != ev._y){
                         tool.addpoint(ev._x, ev._y);
                         }*/

                    }


                };

                // This function is called every time you move the mouse. Obviously, it only
                // draws if the tool.started state is set to true (when you are holding down
                // the mouse button).
                this.MSPointerMove = function (ev) {
                    ev.preventDefault();
                    ev.stopPropagation();

                    var mousePos = getMousePos(canvas, ev);
                    snapPoint = DocObj.getCurrentpage().getsnap(mousePos);

                    if (!snapPoint.found || snapPoint == undefined) {
                        havesnap = false;
                        context.clearRect(0, 0, canvas.width, canvas.height);
                    } else {
                        havesnap = true;
                        drawsnap(context,snapPoint);

                    }


                    if (tool.started) {
                        context.clearRect(0, 0, canvas.width, canvas.height);
                        if (havesnap) {
                            if(DocObj.getCurrentpage().getpageRotation() != 0){
                                var rotatedpoint = snap_rotated(snapPoint);
                            }else{
                                rotatedpoint = snapPoint;
                            }

                            tool.setlastpoint(rotatedpoint.x, rotatedpoint.y);
                            drawsnap(context,snapPoint);

                        } else {
                            tool.setlastpoint(mousePos.x, mousePos.y);
                        }
                        tool.draw(context);
                    }

                };

                this.mousemove = function (ev) {
                    ev.preventDefault();
                    ev.stopPropagation();
                    
                    var mousePos = getMousePos(canvas, ev);

                    snapPoint = DocObj.getCurrentpage().getsnap(mousePos);

                    if (!snapPoint.found || snapPoint == undefined) {
                        havesnap = false;
                        context.clearRect(0, 0, canvas.width, canvas.height);
                    } else {
                        havesnap = true;
                        drawsnap(context,snapPoint);

                    }

                    if (tool.started) {
                        context.clearRect(0, 0, canvas.width, canvas.height);

                        if (havesnap) {

                            if(DocObj.getCurrentpage().getpageRotation() != 0){
                                var rotatedpoint = snap_rotated(snapPoint);
                            }else{
                                rotatedpoint = snapPoint;
                            }

                            tool.setlastpoint(rotatedpoint.x, rotatedpoint.y);
                            drawsnap(context,snapPoint);

                        } else {
                            tool.setlastpoint(mousePos.x, mousePos.y);
                        }


                        tool.draw(context);
                    }

                };

                // This is called when you release the mouse button.
                this.MSPointerUp = function (ev) {
                    //this.addToTable();
                };
                this.mouseup = function (ev) {
                    //this.addToTable();
                };



                this.dblclick = function (ev) {
                    if (tool.started) {
                        tool.mousemove(ev);
                        tool.started = false;

                        tool.result.points.splice(tool.result.numpoints - 1, 2);
                        tool.result.totaldistance = 0;

                        tool.calculatedistance();
                        tool.findanglebetweenlines();
                        tool.addToTable();

                        if (RxCore_GUI_Measurediag != undefined) {
                            RxCore_GUI_Measurediag.setMeasureresult(tool.result);
                        }

                        tool.result.points = [];
                        tool.result.numpoints = -1;
                        tool.result.lengths = [];
                        tool.result.numlengths = -1;
                        tool.result.angles = [];
                        tool.result.numangles = -1;
                        tool.result.lines = [];
                        tool.result.numlines = -1;


                        //show dialog here.
                        //tool.dialogcontent = tool.dialogtextstart + tool.tabletext + tool.dialogtextend  + tool.dialogfieldtext;
                        //setMContent(tool.dialogcontent);

                        context.clearRect(0, 0, canvas.width, canvas.height);


                        //img_update();

                    }

                };

                this.doubletap = function (ev) {
                    if (tool.started) {

                        //img_update();
                        tool.started = false;
                        tool.result.points.splice(tool.result.numpoints - 1, 1);
                        tool.result.totaldistance = 0;

                        tool.calculatedistance();
                        tool.findanglebetweenlines();
                        tool.addToTable();

                        //show dialog here.

                        if (RxCore_GUI_Measurediag != undefined) {
                            RxCore_GUI_Measurediag.setMeasureresult(tool.result);
                        }

                        tool.result.points = [];
                        tool.result.numpoints = -1;
                        tool.result.lengths = [];
                        tool.result.numlengths = -1;
                        tool.result.angles = [];
                        tool.result.numangles = -1;
                        tool.result.lines = [];
                        tool.result.numlines = -1;


                        //tool.dialogcontent = tool.dialogtextstart + tool.tabletext + tool.dialogtextend  + tool.dialogfieldtext;
                        //setMContent(tool.dialogcontent);

                        context.clearRect(0, 0, canvas.width, canvas.height);


                    }


                };

                this.touchstart = function (ev) {
                    ev.preventDefault();
                    var touchPos = getTouchPos(canvas, ev, 0);
                    //alert('touchdown');
                    var touchx = touchPos.x;
                    var touchy = touchPos.y;

                    //double tap
                    var lastTouch = 0;

                    var now = new Date().getTime();

                    if (tool.LastTouchdata == -1) {
                        lastTouch = now + 1;
                    } else {
                        lastTouch = tool.LastTouchdata;

                    }
                    var delta = now - lastTouch;
                    tool.LastTouchdata = now;

                    if (delta < 500 && delta > 0) {
                        tool.doubletap(ev);
                        return;
                        //alert("double tap");
                    }
                    //double tap

                    if (!tool.started) {

                        tool.addpoint(touchPos.x, touchPos.y);
                        //tool.startdraw(context);
                        tool.addpoint(touchPos.x, touchPos.y);
                        tool.started = true;


                    } else {
                        tool.addpoint(touchPos.x, touchPos.y);
                        //tool.addToTable();
                        //areamarkupobj.addpoint(touchx, touchy);
                        //polygonmarkupobj.setlastpoint(touchx,touchy);
                    }


                };

                this.touchmove = function (ev) {
                    ev.preventDefault();

                    var touchPos = getTouchPos(canvas, ev, 0);
                    //var touchx = touchPos.x;
                    //var touchy = touchPos.y;

                    if (tool.started) {
                        context.clearRect(0, 0, canvas.width, canvas.height);
                        tool.setlastpoint(touchPos.x, touchPos.y);
                        tool.draw(context);

                    }
                };
                this.touchcancel = function(ev){
                    ev.preventDefault();
                    var touchPos = getTouchPos(canvas, ev, 0);
                    var touchx = touchPos.x;
                    var touchy = touchPos.y;

                    if (tool.started) {
                        //tool.addToTable();
                        //tool.addpoint(touchx, touchy);
                    }

                };

                this.touchend = function (ev) {
                    ev.preventDefault();
                    var touchPos = getTouchPos(canvas, ev, 0);
                    var touchx = touchPos.x;
                    var touchy = touchPos.y;

                    if (tool.started) {
                        //tool.addToTable();
                        //tool.addpoint(touchx, touchy);
                    }


                };


            };


            // The dimension line tool.
            tools.measure = function (params) {

                var tool = this;
                var prevx = 0,
                    prevy = 0;

                var limprevx = 0,
                    limprevy = 0;
                    
                var dimensionmarkupobj;
                var havesnap = false;
                var tempOrtho = false;
                var snapPoint;
                var type = params.p1;
                var subtype = params.p2;
                this.started = false;
                this.name = 'measure';
                this.anglelengthsupport = false;
                this.refreshSupport = false;

                this.startx = 0;
                this.starty = 0;
                this.orgscale = 1.0;
                this.orgdx = 0.0;
                this.orgdy = 0.0;
                this.bfirstpointset = false;
                
                //this.curmousepos = {x:0,y:0};
                this.curmousepos = {x:0,y:0,xus:0,yus:0};

                
                setCursor(getMarkupCursor('measure'));


                this.scrollhandling = function (movey) {
                    if (!documentopen || DocObj.getCurrentpage().usevector3Dxml) {
                        return;
                    }

                    if (movey < 0) {


                        //if (DocObj.getCurrentpage().usepdfjs && DocObj.pages.length > 1) {
                        if (DocObj.getCurrentpage().usepdfjs && DocObj.pages.length > 1 && !enableZoomOnScrollMultipage) {
                            //DocObj.getCurrentpage().pan_update(0,-movey);
                            if (DocObj.currentpage == DocObj.NumPages - 1) {
                                /*if (DocObj.getCurrentpage().endy <= canvas.height) {
                                    movey = 0;
                                }*/
                            }
                            DocObj.getCurrentpage().pan_update(0, -movey);
                            


                        } else {

                            if(belowlimitExtentEx(1.1, tool.curmousepos)){
                                DocObj.getCurrentpage().ZoomOut(1.1, false, false, tool.curmousepos);

                                if(tool.started){
                                    tool.setdimoffset(false);
                                }    

                            }else{
                                RxCore_foxitForceRedraw();
                            }
                            


                            if(tool.started){

                                //tool.setdimoffset(false);

                                /*dimensionmarkupobj.w = tool.curmousepos.x;
                                dimensionmarkupobj.h = tool.curmousepos.y;

                                if(type == 3){
                                    //dimensionmarkupobj.w = Math.abs(tool.curmousepos.x - dimensionmarkupobj.x);
                                    //dimensionmarkupobj.h = Math.abs(tool.curmousepos.y - dimensionmarkupobj.y);

                                    dimensionmarkupobj.w = dimensionmarkupobj.w - dimensionmarkupobj.x;
                                    dimensionmarkupobj.h = dimensionmarkupobj.h - dimensionmarkupobj.y;
            
                                }*/
                                
                                //tool.mousemove();

                                tool.drawDimension({x : tool.curmousepos.x, y : tool.curmousepos.y});
    

                                if(dimensionmarkupobj != null){
                                    context.clearRect(0, 0, canvas.width, canvas.height);
                                    dimensionmarkupobj.drawme(context);
                                }

                            }



                        }


                    } else {

                        //if (DocObj.getCurrentpage().usepdfjs && DocObj.pages.length > 1) {
                        if (DocObj.getCurrentpage().usepdfjs && DocObj.pages.length > 1 && !enableZoomOnScrollMultipage) {                            

                            if (DocObj.currentpage == 0) {
                                /*if (DocObj.getCurrentpage().starty >= 0) {
                                    movey = 0;
                                }*/

                            }
                            DocObj.getCurrentpage().pan_update(0, -movey);
                            

                        } else {


                            DocObj.getCurrentpage().ZoomIn(1.1, false, false, tool.curmousepos);


                            if(tool.started){

                                tool.setdimoffset(false);

                                /*dimensionmarkupobj.w = tool.curmousepos.x;
                                dimensionmarkupobj.h = tool.curmousepos.y;


                                if(type == 3){
                                    //dimensionmarkupobj.w = Math.abs(tool.curmousepos.x - tool.x0);
                                    //dimensionmarkupobj.h = Math.abs(tool.curmousepos.y - tool.y0);
                                    
                                    //dimensionmarkupobj.w = Math.abs(tool.curmousepos.x - dimensionmarkupobj.x);
                                    //dimensionmarkupobj.h = Math.abs(tool.curmousepos.y - dimensionmarkupobj.y);

                                    dimensionmarkupobj.w = dimensionmarkupobj.w - dimensionmarkupobj.x;
                                    dimensionmarkupobj.h = dimensionmarkupobj.h - dimensionmarkupobj.y;
                    
                                }*/

                                tool.drawDimension({x : tool.curmousepos.x, y : tool.curmousepos.y});
                                //tool.mousemove();

                                //dimensionmarkupobj.w = tool.curmousepos.x;
                                //dimensionmarkupobj.h = tool.curmousepos.y;

                                if(dimensionmarkupobj != null){
                                    dimensionmarkupobj.drawme(context);
                                }
                                


                            }


                        }

                    }


                };

                this.belowlimitExtent = function(zoomscalefactor){
                    var belowlimit = true;
                    

                    var pagedim = DocObj.getCurrentpage().getpagedim();

                    var wlimit = canvasowidth - 60;
                    var hlimit = canvasoheight - 60;

                    if((pagedim.w / zoomscalefactor) < wlimit && (pagedim.h / zoomscalefactor) < hlimit){
                        belowlimit = false;
                        //scalechanged = false;
                        //DocObj.getCurrentpage().zoomall();
                    }

                    var pagerect = DocObj.getCurrentpage().ZoomOutTest(zoomscalefactor, false, tool.curmousepos);
                    
                    var heightlimit = canvasoheight - (nPanlimit*2);
                    var widthlimit = canvasowidth - (nPanlimit*2);
                    //var toplimit = 15;
                    var bottomlimit = ((pagerect.y + pagerect.h) <= heightlimit);
                    var toplimit = (pagerect.y >= nPanlimit);

                    // need to return these to reset the top and bottom positions.scroll|| bottomlimit || toplimit
                    if ((pagerect.w / pagerect.h) < (canvasowidth / canvasoheight)){
                        if(pagerect.h <= heightlimit){
                            belowlimit = false;
                        }
    
                    }else{
                        if(pagerect.w <= widthlimit){
                            belowlimit = false;
                        }

                    }


                    if(!bLimitZoomOut){
                        belowlimit = true;
                    }

                    return belowlimit;
                };                

                this.wheel = function (ev) {

                    var delta = 0;
                    if (ev.wheelDelta !== undefined) { // WebKit / Opera / Explorer 9

                        delta = ev.wheelDelta;

                    } else if (ev.deltaY !== undefined) { // Firefox

                        delta = -ev.deltaY*50;

                    }

                    tool.scrollhandling(delta);


                };

                this.setdimoffset = function(initial){

                    if(initial){


                        dimensionmarkupobj.scaling = getCurrentPageScale();
                        dimensionmarkupobj.loadscaling = getCurrentPageScale(); 
                        var offset = getCurrentPageOffset();
                        dimensionmarkupobj.xoffset = offset.x;
                        dimensionmarkupobj.yoffset = offset.y;


                        tool.orgscale = dimensionmarkupobj.scaling;
                        tool.orgdx = dimensionmarkupobj.xoffset;
                        tool.orgdy = dimensionmarkupobj.yoffset;

                        
                    }

                    var curscale = DocObj.getCurrentpage().getdscale();
                    var pagedx = DocObj.getCurrentpage().getdx();
                    var pagedy = DocObj.getCurrentpage().getdy();

                    if(dimensionmarkupobj){

                        var scalediff = (curscale / dimensionmarkupobj.scaling);

                        var changex = ((dimensionmarkupobj.w - pagedx) / scalediff) + dimensionmarkupobj.xoffset;
                        var changey = ((dimensionmarkupobj.h - pagedy) / scalediff) + dimensionmarkupobj.yoffset;
    
                    }
                    
                    //var newx = (tool.startx - tool.orgdx) * scalediff;
                    //var newy = (tool.starty - tool.orgdy) * scalediff;
                    //newx += pagedx;
                    //newy += pagedy;

                    //this.wscaled = (this.w - this.xoffset) * scalefactor;
                    //this.hscaled = (this.h - this.yoffset) * scalefactor;

                    //this.wscaled += pagedx;
                    //this.hscaled += pagedy;
                    //changex = ((this.wscaled + pagedx) / scalefactor) + this.xoffset;


                    //dimensionmarkupobj.w = changex;
                    //dimensionmarkupobj.h = changey;



                };                

                this.pointermove = function (ev) {
                    tool.MSPointerMove(ev);
                };
                this.pointerdown = function (ev) {
                    tool.MSPointerDown(ev);
                };

                this.pointerup = function (ev) {
                    tool.MSPointerUp(ev);
                };


                this.MSPointerDown = function (ev) {
                    ev.preventDefault();
                    ev.stopPropagation();
                    
                    var mousePos = getMousePos(canvas, ev);
                    var bWithin = MousePosdrwext(mousePos);
                    if(ev.button == 0){
                        if (!tool.started) {
                            tool.started = true;
                            if(DocObj){
                                DocObj.bMarkupchanged = true;
                            }

                            if (!bWithin && bLimMarkupExtent) {
                                tool.started = false;
                                if(DocObj){
                                    DocObj.bMarkupchanged = false;
                                }
                                
                                return;
                            }
                            if (havesnap) {
                                if(DocObj.getCurrentpage().getpageRotation() != 0){
                                    var rotatedpoint = snap_rotated(snapPoint);
                                }else{
                                    rotatedpoint = snapPoint;
                                }

                                tool.x0 = rotatedpoint.x;
                                tool.y0 = rotatedpoint.y;

                            } else {
                                tool.x0 = mousePos.x;
                                tool.y0 = mousePos.y;
                            }

                            dimensionmarkupobj = new MarkupObject(7, subtype, 0);
                            dimensionmarkupobj.pagenumber = DocObj.getcurPage();

                            dimensionmarkupobj.viewname = DocObj.getcur3DView();

                            if(dimensionmarkupobj.viewname != null){
                                dimensionmarkupobj.bfor3D = true;
                                dimensionmarkupobj.pageheight = canvasoheight;
                                dimensionmarkupobj.pagewidth = canvasowidth;
                            }

                            dimensionmarkupobj.pagerotation = DocObj.getCurrentpage().getpageRotation();

                            tool.setdimoffset(true);
                            tool.startx = tool.x0;
                            tool.starty = tool.y0;

                            dimensionmarkupobj.seteditaction(0);

                        } else {
                            if (bWithin || !bLimMarkupExtent) {
                                if (havesnap) {
                                    if(DocObj.getCurrentpage().getpageRotation() != 0){
                                        rotatedpoint = snap_rotated(snapPoint);
                                    }else{
                                        rotatedpoint = snapPoint;
                                    }

                                    dimensionmarkupobj.w = rotatedpoint.x;
                                    dimensionmarkupobj.h = rotatedpoint.y;

                                } else {
                                    dimensionmarkupobj.w = mousePos.x;
                                    dimensionmarkupobj.h = mousePos.y;

                                }
                                //tool.mousemove(ev);
                                context.clearRect(0, 0, canvas.width, canvas.height);
                                tool.setdimoffset(false);
                                dimensionmarkupobj.drawme(context);

                                if (!bMultimarkupadd) {

                                    bMarkupcrated = true;
                                    //ft 08.08.2018 changed from separate index to direct array length
                                    //nMarkupcreated = DocObj.nummarkups;
                                    
                                }
                                nMarkupcreated = DocObj.markuplist.length;

                                dimensionmarkupobj.savemetolistDraw();
                                dimensionmarkupobj = null;
                                tool.started = false;
                                img_update();

                                
                                if(bAlwaysMarkupCallback){
                                    if (RxCore_GUI_Markup != undefined) {
                                        
                                        RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[nMarkupcreated], {created : true, modified : false, deleted : false, multi : bMultimarkupadd});
                                    }
                        
                                }else{
                                    if (!bMultimarkupadd) {
                                        //need to move to connection object.
                                        //markupcreated();
                                        if (RxCore_GUI_Markup != undefined) {
                                            //RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[index],true);
                                            RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[nMarkupcreated], {created : true, modified : false, deleted : false});
                                        }
    
                                    }
    
                                }


                            }


                        }
                    }else{
                        prevx = mousePos.x;
                        prevy = mousePos.y;

                    }

                };
                
                this.startDimension = function(bWithin, mousePos){
                    if (bWithin || !bLimMarkupExtent) {
                        tool.started = true;
                        if(DocObj){
                            DocObj.bMarkupchanged = true;
                        }

                        if (havesnap) {
                            if(DocObj.getCurrentpage().getpageRotation() != 0){
                                var rotatedpoint = snap_rotated(snapPoint);
                            }else{
                                rotatedpoint = snapPoint;
                            }

                            tool.x0 = rotatedpoint.x;
                            tool.y0 = rotatedpoint.y;

                        } else {
                            tool.x0 = mousePos.x;
                            tool.y0 = mousePos.y;
                        }

                        dimensionmarkupobj = new MarkupObject(7, subtype, 0);
                        dimensionmarkupobj.pagenumber = DocObj.getcurPage();
                        dimensionmarkupobj.viewname = DocObj.getcur3DView();

                        if(dimensionmarkupobj.viewname != null){
                            dimensionmarkupobj.bfor3D = true;
                            dimensionmarkupobj.pageheight = canvasoheight;
                            dimensionmarkupobj.pagewidth = canvasowidth;
                        }

                        dimensionmarkupobj.pagerotation = DocObj.getCurrentpage().getpageRotation();
                        tool.setdimoffset(true);

                        tool.startx = tool.x0;
                        tool.starty = tool.y0;

                        //dimensionmarkupobj.x = tool.x0;
                        //dimensionmarkupobj.y = tool.y0;

                        dimensionmarkupobj.seteditaction(0);


                    }

                };

                this.completeDimension = function(bWithin, mousePos){


                    if(bOrthoOn){
                        bOrthoOn = false;
                        tempOrtho = true;
                    }

                    if (bWithin || !bLimMarkupExtent) {
                        tool.started = false;

                        if (havesnap) {
                            if(DocObj.getCurrentpage().getpageRotation() != 0){
                                var rotatedpoint = snap_rotated(snapPoint);
                            }else{
                                rotatedpoint = snapPoint;
                            }

                            dimensionmarkupobj.w = rotatedpoint.x;
                            dimensionmarkupobj.h = rotatedpoint.y;

                        } else {

                            /*console.log(tool.bUsemouseinput);
                            if (tool.bUsemouseinput){
                            }*/


                            if(DocObj.getCurrentpage().getpageRotation() != 0){

                                dimensionmarkupobj.w = mousePos.x;
                                dimensionmarkupobj.h = mousePos.y;
                            }else{

                                dimensionmarkupobj.w = mousePos.x;
                                dimensionmarkupobj.h = mousePos.y;

                            }


                        }

                        context.clearRect(0, 0, canvas.width, canvas.height);
                        tool.setdimoffset(false);
                        dimensionmarkupobj.drawme(context);

                        if (!bMultimarkupadd) {

                            bMarkupcrated = true;
                            //ft 08.08.2018 changed from separate index to direct array length
                            
                            //nMarkupcreated = DocObj.nummarkups;
                        }
                        nMarkupcreated = DocObj.markuplist.length;

                        dimensionmarkupobj.savemetolistDraw();

                        if (tempOrtho) {

                            var curpoint = dimensionmarkupobj.calculateOrthoDraw({x: dimensionmarkupobj.x, y: dimensionmarkupobj.y}, { x: dimensionmarkupobj.w, y: dimensionmarkupobj.h });

                            dimensionmarkupobj.w = curpoint.w;
                            dimensionmarkupobj.h = curpoint.h;

                        }

                        dimensionmarkupobj = null;
                        img_update();

                        RxCore_MarkupRedraw();

                        if(bAlwaysMarkupCallback){
                            if (RxCore_GUI_Markup != undefined) {
                                
                                RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[nMarkupcreated], {created : true, modified : false, deleted : false, multi : bMultimarkupadd});
                            }
                
                        }else{
                            if (!bMultimarkupadd) {
                                //need to move to connection object.
                                //markupcreated();
                                if (RxCore_GUI_Markup != undefined) {
                                    //RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[index],true);
                                    // RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[DocObj.nummarkups], true);
                                    RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[nMarkupcreated], {created : true, modified : false, deleted : false});
                                }
    
                            }
                        }


                        if(tempOrtho){
                            bOrthoOn = true;
                            tempOrtho = false;
                        }



                    }
                };

                this.drawDimension = function(mousePos){

                    if (havesnap) {
                        if(DocObj.getCurrentpage().getpageRotation() != 0){
                            var rotatedpoint = snap_rotated(snapPoint);
                        }else{
                            rotatedpoint = snapPoint;
                        }

                        dimensionmarkupobj.x = tool.x0;
                        dimensionmarkupobj.y = tool.y0;
                        dimensionmarkupobj.w = rotatedpoint.x;
                        dimensionmarkupobj.h = rotatedpoint.y;


                        if(type == 3){
                            dimensionmarkupobj.w = dimensionmarkupobj.w - dimensionmarkupobj.x;
                            dimensionmarkupobj.h = dimensionmarkupobj.h - dimensionmarkupobj.y;

                            //dimensionmarkupobj.w = Math.abs(dimensionmarkupobj.w - dimensionmarkupobj.x);
                            //dimensionmarkupobj.h = Math.abs(dimensionmarkupobj.h - dimensionmarkupobj.y);

    
                        }
                        tool.bfirstpointset = true;

                        limprevx = rotatedpoint.x;
                        limprevy = rotatedpoint.y;

                    } else {
                        dimensionmarkupobj.x = tool.x0;
                        dimensionmarkupobj.y = tool.y0;
                        dimensionmarkupobj.w = mousePos.x;
                        dimensionmarkupobj.h = mousePos.y;

                        if(type == 3){
                            dimensionmarkupobj.w = dimensionmarkupobj.w - dimensionmarkupobj.x;
                            dimensionmarkupobj.h = dimensionmarkupobj.h - dimensionmarkupobj.y;

                            //dimensionmarkupobj.w = Math.abs(dimensionmarkupobj.w - dimensionmarkupobj.x);
                            //dimensionmarkupobj.h = Math.abs(dimensionmarkupobj.h - dimensionmarkupobj.y);
    
                        }

                        tool.bfirstpointset = true;

                        limprevx = mousePos.x;
                        limprevy = mousePos.y;

                    }

                };

                this.startRect = function (bWithin, mousePos) {

                    if(bLimMarkupExtent && !bWithin){
                        return;
                    }

                    tool.started = true;
                        if(DocObj){
                            DocObj.bMarkupchanged = true;
                        }
        
                        if (havesnap) {
                            if(DocObj.getCurrentpage().getpageRotation() != 0){
                                var rotatedpoint = snap_rotated(snapPoint);
                            }else{
                                rotatedpoint = snapPoint;
                            }
    
                            tool.x0 = rotatedpoint.x;
                            tool.y0 = rotatedpoint.y;
                        } else {
                            tool.x0 = mousePos.x;
                            tool.y0 = mousePos.y;
                        }
                        
    
                        dimensionmarkupobj = new MarkupObject(type, subtype, fillstyle);
                        
        
                        /*if (subtype == 3){
                        
                            dimensionmarkupobj.transparency = 30;
                            dimensionmarkupobj.fillcolor = setTransp(rectmarkupobj.fillcolor,rectmarkupobj.transparency);
                        }*/
    
                        tool.setdimoffset(true);

                        //dimensionmarkupobj.scaling = getCurrentPageScale();
                        //var offset = getCurrentPageOffset();
                        //dimensionmarkupobj.xoffset = offset.x;
                        //dimensionmarkupobj.yoffset = offset.y;
    
                        dimensionmarkupobj.pagenumber = DocObj.getcurPage();
    
                        dimensionmarkupobj.viewname = DocObj.getcur3DView();
    
                        if(dimensionmarkupobj.viewname != null){
                            dimensionmarkupobj.bfor3D = true;
                            dimensionmarkupobj.pageheight = canvasoheight;
                            dimensionmarkupobj.pagewidth = canvasowidth;
                        }
    
    
                        dimensionmarkupobj.seteditaction(0);

                };

                this.drawRect = function (mousePos) {
                    /*if (!tool.bUsemouseinput){
                        return;
                    }*/

                    if (havesnap) {
                        if(DocObj.getCurrentpage().getpageRotation() != 0){
                            var rotatedpoint = snap_rotated(snapPoint);
                        }else{
                            rotatedpoint = snapPoint;
                        }

                        /*dimensionmarkupobj.x = tool.x0;
                        dimensionmarkupobj.y = tool.y0;
                        dimensionmarkupobj.w = rotatedpoint.x;
                        dimensionmarkupobj.h = rotatedpoint.y;
                        limprevx = rotatedpoint.x;
                        limprevy = rotatedpoint.y;*/


                        dimensionmarkupobj.x = Math.min(rotatedpoint.x, tool.x0);
                        dimensionmarkupobj.y = Math.min(rotatedpoint.y, tool.y0);

                        dimensionmarkupobj.w = Math.abs(rotatedpoint.x - dimensionmarkupobj.x);
                        dimensionmarkupobj.h = Math.abs(rotatedpoint.y - dimensionmarkupobj.y);

                        tool.bfirstpointset = true;

                        limprevx = rotatedpoint.x;
                        limprevy = rotatedpoint.y;

                        //prevx = rotatedpoint.x;
                        //prevy = rotatedpoint.y;


                    } else {
                        dimensionmarkupobj.x = Math.min(mousePos.x, tool.x0);
                        dimensionmarkupobj.y = Math.min(mousePos.y, tool.y0);
                        dimensionmarkupobj.w = Math.abs(mousePos.x - dimensionmarkupobj.x);
                        dimensionmarkupobj.h = Math.abs(mousePos.y - dimensionmarkupobj.y);

                        tool.bfirstpointset = true;

                        limprevx = mousePos.x;
                        limprevy = mousePos.y;


                    }
                };

                this.endRect = function (bWithin) {
                    /*if(rectmarkupobj.w==0 || rectmarkupobj.h==0){
                     return;
                     }*/
                    if(bLimMarkupExtent && !bWithin){
                        return;
                    }

                    tool.started = false;

                    if (!bMultimarkupadd) {

                        bMarkupcrated = true;
                        //ft 08.08.2018 changed from separate index to direct array length
                        //nMarkupcreated = DocObj.markuplist.length;
                        //nMarkupcreated = DocObj.nummarkups;
                    }

                    nMarkupcreated = DocObj.markuplist.length;

                    dimensionmarkupobj.savemetolistDraw();
                    dimensionmarkupobj = null;

                    tool.bfirstpointset = false;

                    //tool.started = false;
                    //markuplist.add(drawrectangle);
                    img_update();
                    drawmarkupAll(cntximg);

                    if(bAlwaysMarkupCallback){
                        if (RxCore_GUI_Markup != undefined) {
                            var operation = {created : true, modified : false, deleted : false, multi : bMultimarkupadd};
                            RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[nMarkupcreated], operation);
                        }
            
                    }else{
                        if (!bMultimarkupadd) {
                            //need to move to connection object.
                            //markupcreated();
                            if (RxCore_GUI_Markup != undefined) {
                                //RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[index],true);
                                var operation = {created : true, modified : false, deleted : false};
                                RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[nMarkupcreated], operation);
                            }
    
                        }
    
                    }



                };

                this.mousedown = function (ev) {
                    ev.preventDefault();
                    ev.stopPropagation();
                    
                    var mousePos = getMousePos(canvas, ev);
                    var bWithin = MousePosdrwext(mousePos);

                    if(ev.button == 0){
                        if (!tool.started) {

                            if(type == 3){
                                tool.startRect(bWithin, mousePos);
                            }else{
                                tool.startDimension(bWithin, mousePos);
                            }
                            


                        } else {

                            if(tool.bfirstpointset){
                                if(type == 3){
                                    tool.endRect(bWithin);
                                }else{
                                    tool.completeDimension(bWithin, mousePos);
                                }
                                
                            }
        
                            

                        }
                    }else{
                        prevx = mousePos.x;
                        prevy = mousePos.y;

                    }

                };

                this.touchstart = function (ev) {
                    //context.strokeStyle = "red";
                    tool.started = true;
                    if(DocObj){
                        DocObj.bMarkupchanged = true;
                    }
                    var touchPos = getTouchPos(canvas, ev, 0);
                    var bWithin = MousePosdrwext(touchPos);
                    if (!bWithin && bLimMarkupExtent) {
                        tool.started = false;
                        if(DocObj){
                            DocObj.bMarkupchanged = false;
                        }
                        
                        return;
                    }

                    tool.x0 = touchPos.x;
                    tool.y0 = touchPos.y;
                    dimensionmarkupobj = new MarkupObject(7, 0, 0);
                    dimensionmarkupobj.pagenumber = DocObj.getcurPage();
                    dimensionmarkupobj.viewname = DocObj.getcur3DView();

                    if(dimensionmarkupobj.viewname != null){
                        dimensionmarkupobj.bfor3D = true;
                        dimensionmarkupobj.pageheight = canvasoheight;
                        dimensionmarkupobj.pagewidth = canvasowidth;
                    }

                    dimensionmarkupobj.pagerotation = DocObj.getCurrentpage().getpageRotation();
                    tool.setdimoffset(true);

                    dimensionmarkupobj.seteditaction(0);
                };

                this.MSPointerMove = function (ev) {
                    ev.preventDefault();
                    ev.stopPropagation();
                    
                    var mousePos = getMousePos(canvas, ev);
                    tool.curmousepos = mousePos;

                    var xdiff = prevx - mousePos.x;
                    var ydiff = prevy - mousePos.y;

                    snapPoint = DocObj.getCurrentpage().getsnap(mousePos);

                    if (!snapPoint.found || snapPoint == undefined) {
                        havesnap = false;
                        context.clearRect(0, 0, canvas.width, canvas.height);
                    } else {
                        havesnap = true;
                        drawsnap(context,snapPoint);

                    }
                    if (ev.buttons == 2) {

                        DocObj.getCurrentpage().pan_update(xdiff, ydiff);
                        

                        tool.setdimoffset(false);
                        prevx = mousePos.x;
                        prevy = mousePos.y;

                    }


                    if (!tool.started) {
                        return;
                    }

                    var bWithin = MousePosdrwext(mousePos);
                    if (bWithin || !bLimMarkupExtent) {
                        if (havesnap) {
                            if(DocObj.getCurrentpage().getpageRotation() != 0){
                                var rotatedpoint = snap_rotated(snapPoint);
                            }else{
                                rotatedpoint = snapPoint;
                            }

                            dimensionmarkupobj.x = tool.x0;
                            dimensionmarkupobj.y = tool.y0;
                            dimensionmarkupobj.w = rotatedpoint.x;
                            dimensionmarkupobj.h = rotatedpoint.y;


                            limprevx = rotatedpoint.x;
                            limprevy = rotatedpoint.y;

                        } else {
                            dimensionmarkupobj.x = tool.x0;
                            dimensionmarkupobj.y = tool.y0;
                            dimensionmarkupobj.w = mousePos.x;
                            dimensionmarkupobj.h = mousePos.y;


                            limprevx = mousePos.x;
                            limprevy = mousePos.y;

                        }

                    } else {
                        dimensionmarkupobj.x = tool.x0;
                        dimensionmarkupobj.y = tool.y0;
                        dimensionmarkupobj.w = limprevx;
                        dimensionmarkupobj.h = limprevy;


                    }


                    context.clearRect(0, 0, canvas.width, canvas.height);


                    if (havesnap) {
                        drawsnap(context,snapPoint);
                    }

                    if (dimensionmarkupobj.w - dimensionmarkupobj.x != 0) {
                        tool.setdimoffset(false);
                        dimensionmarkupobj.drawme(context);
                    }

                };

                this.mousemove = function (ev) {
                    ev.preventDefault();
                    ev.stopPropagation();
                    
                    var mousePos = getMousePos(canvas, ev);
                    tool.curmousepos = mousePos;

                    var xdiff = prevx - mousePos.x;
                    var ydiff = prevy - mousePos.y;


                    snapPoint = DocObj.getCurrentpage().getsnap(mousePos);
                    
                    //test for foxit snap to check if old snaps are cleared.

                    

                    if (!snapPoint.found || snapPoint == undefined) {
                        context.clearRect(0, 0, canvas.width, canvas.height);
                        havesnap = false;
                        
                    } else {
                        havesnap = true;
                        context.clearRect(0, 0, canvas.width, canvas.height);
                        drawsnap(context,snapPoint);

                    }
                    if (ev.button == 2 || ev.buttons == 2) {

                        DocObj.getCurrentpage().pan_update(xdiff, ydiff);
                        
                        tool.setdimoffset(false);
                        prevx = mousePos.x;
                        prevy = mousePos.y;

                    }

                    if (!tool.started) {
                        return;
                    }
                    //var mousePos = getMousePos(canvas, ev);
                    var bWithin = MousePosdrwext(mousePos);
                    if (bWithin || !bLimMarkupExtent) {



                        /*if(type == 3){
                            tool.drawRect(mousePos);
                        }else{
                            //tool.startDimension(bWithin, mousePos);
                            
                        }*/

                        tool.drawDimension(mousePos);

                        /*if (havesnap) {
                            if(DocObj.getCurrentpage().getpageRotation() != 0){
                                var rotatedpoint = snap_rotated(snapPoint);
                            }else{
                                rotatedpoint = snapPoint;
                            }

                            dimensionmarkupobj.x = tool.x0;
                            dimensionmarkupobj.y = tool.y0;
                            dimensionmarkupobj.w = rotatedpoint.x;
                            dimensionmarkupobj.h = rotatedpoint.y;
                            limprevx = rotatedpoint.x;
                            limprevy = rotatedpoint.y;

                        } else {
                            dimensionmarkupobj.x = tool.x0;
                            dimensionmarkupobj.y = tool.y0;
                            dimensionmarkupobj.w = mousePos.x;
                            dimensionmarkupobj.h = mousePos.y;

                            limprevx = mousePos.x;
                            limprevy = mousePos.y;

                        }*/


                    } else {
                        dimensionmarkupobj.x = tool.x0;
                        dimensionmarkupobj.y = tool.y0;
                        dimensionmarkupobj.w = limprevx;
                        dimensionmarkupobj.h = limprevy;

                        

                    }


                    context.clearRect(0, 0, canvas.width, canvas.height);
   

                    if (havesnap) {
                        drawsnap(context,snapPoint);
                    }

                    if (dimensionmarkupobj.w - dimensionmarkupobj.x != 0) {
                        tool.setdimoffset(false);
                        dimensionmarkupobj.drawme(context);
                    }



                };

                this.touchmove = function (ev) {
                    ev.preventDefault();
                    if (!tool.started) {
                        return;
                    }
                    var touchPos = getTouchPos(canvas, ev, 0);
                    var bWithin = MousePosdrwext(touchPos);
                    if (bWithin || !bLimMarkupExtent) {

                        dimensionmarkupobj.x = tool.x0;
                        dimensionmarkupobj.y = tool.y0;
                        dimensionmarkupobj.w = touchPos.x;
                        dimensionmarkupobj.h = touchPos.y;
                        tool.bfirstpointset = true;

                        prevx = touchPos.x;
                        prevy = touchPos.y;

                    } else {
                        dimensionmarkupobj.x = tool.x0;
                        dimensionmarkupobj.y = tool.y0;
                        dimensionmarkupobj.w = prevx;
                        dimensionmarkupobj.h = prevy;

                        tool.bfirstpointset = true;

                    }


                    /*dimensionmarkupobj.x = tool.x0;
                     dimensionmarkupobj.y = tool.y0;
                     dimensionmarkupobj.w = touchPos.x;
                     dimensionmarkupobj.h = touchPos.y;*/

                    context.clearRect(0, 0, canvas.width, canvas.height);
                    if (dimensionmarkupobj.w - dimensionmarkupobj.x != 0) {
                        dimensionmarkupobj.drawme(context);
                    }


                };

                this.MSPointerUp = function (ev) {
                    /*if (tool.started) {
                     tool.mousemove(ev);
                     dimensionmarkupobj.savemetolist();
                     dimensionmarkupobj = null;
                     tool.started = false;
                     img_update();

                     

                     }*/

                };
                this.mouseup = function (ev) {
                    /*if (tool.started) {
                     tool.mousemove(ev);
                     dimensionmarkupobj.savemetolist();
                     dimensionmarkupobj = null;
                     tool.started = false;
                     img_update();
                     drawmarkupAll(cntximg);
                     

                     }*/
                };

                this.touchcancel = function(ev){
                    ev.preventDefault();

                    if (tool.started) {
                        tool.started = false;
                        tool.bfirstpointset = false;
                        if (!bMultimarkupadd) {

                            bMarkupcrated = true;
                            //ft 08.08.2018 changed from separate index to direct array length
                            
                            //nMarkupcreated = DocObj.nummarkups;
                        }
                        nMarkupcreated = DocObj.markuplist.length;

                        dimensionmarkupobj.savemetolistDraw();
                        img_update();
                        //tool.touchmove(ev);
                        dimensionmarkupobj = null;
                        //alert(DocObj.getCurrentpage().dscale);


                        if(bAlwaysMarkupCallback){
                            if (RxCore_GUI_Markup != undefined) {
                                
                                RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[nMarkupcreated], {created : true, modified : false, deleted : false, multi : bMultimarkupadd});
                            }
                
                        }else{
                            if (!bMultimarkupadd) {
                                //need to move to connection object.
                                //markupcreated();
                                if (RxCore_GUI_Markup != undefined) {
                                    //RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[index],true);
                                    RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[nMarkupcreated], {created : true, modified : false, deleted : false});
                                }
                            }
                        }


                    }
                };
                this.touchend = function (ev) {
                    ev.preventDefault();

                    if (tool.started) {
                        tool.started = false;
                        tool.bfirstpointset = false;
                        if (!bMultimarkupadd) {

                            bMarkupcrated = true;
                            //ft 08.08.2018 changed from separate index to direct array length
                            
                            //nMarkupcreated = DocObj.nummarkups;
                        }
                        nMarkupcreated = DocObj.markuplist.length;

                        dimensionmarkupobj.savemetolistDraw();
                        img_update();
                        //tool.touchmove(ev);
                        dimensionmarkupobj = null;
                        //alert(DocObj.getCurrentpage().dscale);


                        if(bAlwaysMarkupCallback){
                            if (RxCore_GUI_Markup != undefined) {
                                
                                RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[nMarkupcreated], {created : true, modified : false, deleted : false, multi : bMultimarkupadd});
                            }
                
                        }else{
                            if (!bMultimarkupadd) {
                                //need to move to connection object.
                                //markupcreated();
                                if (RxCore_GUI_Markup != undefined) {
                                    //RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[index],true);
                                    RxCore_GUI_Markup.setMarkupSelected(DocObj.markuplist[nMarkupcreated], {created : true, modified : false, deleted : false});
                                }
    
                            }
    
                        }


                    }
                };


            };

            tools.getoverlayscale = function(params){


                var tool = this;
                var page = params.p1;
                var index = params.p2;
                this.started = false;

                this.points = [];
                //this.compareref = Compareobject;
                this.pageobject = page;
                this.pageindex = index;


                this.name = 'getoverlayscale';
                this.anglelengthsupport = false;
                this.refreshSupport = false;

                this.startx = 0;
                this.starty = 0;
                this.x = 0;
                this.y = 0;

                this.w = 0;
                this.h = 0;
                this.distance = 0.0;
                this.orgscale = 1.0;
                this.orgdx = 0.0;
                this.orgdy = 0.0;

                //this.curmousepos = {x:0,y:0};
                this.curmousepos = {x:0,y:0,xus:0,yus:0};

                var curcanv = document.createElement('canvas');
                curcanv.width = 30;
                curcanv.height = 30;
                var curcntximg = curcanv.getContext('2d');

                curcntximg.strokeStyle = "gray";
                curcntximg.lineWidth = 1;

                curcntximg.strokeRect(0, 0,30,30);

                curcntximg.beginPath();
                curcntximg.moveTo(15, 0);
                curcntximg.lineTo(15, 14);
                curcntximg.moveTo(15, 30);
                curcntximg.lineTo(15, 16);
                curcntximg.moveTo(0, 15);
                curcntximg.lineTo(14, 15);
                curcntximg.moveTo(30, 15);
                curcntximg.lineTo(16, 15);
                curcntximg.closePath();
                curcntximg.stroke();

                var prevx = 0,
                    prevy = 0;

                var Unit = "mm";


                var havesnap = false;
                var snapPoint;


                this.dialogfieldtext = "";


                this.keydown = function (ev) {
                    
                    
                    if(docpagNotReady()){
                        return;
                    }
            
                    switch (ev.keyCode) {
                        case 27:
                            tool.resettool();
                            break;

                    }

                };


                this.resettool = function(){

                    tool.x = 0;
                    tool.y = 0;
    
                    tool.w = 0;
                    tool.h = 0;

                    tool.started = false;

                    context.clearRect(0, 0, canvas.width, canvas.height);
                } 


                this.getdiag = function (width, height) {


                    var dimwsq = Math.pow(width, 2);
                    var dimhsq = Math.pow(height, 2);
                    var dimdiag = Math.sqrt((dimwsq + dimhsq));

                    return dimdiag;

                };

                this.getangleofline = function (startx, starty, endx, endy, useabs) {

                    var width = 0;
                    var height = 0;
                    if (useabs) {
                        width = Math.abs(endx - startx);
                        height = Math.abs(endy - starty);
                    } else {
                        width = endx - startx;
                        height = endy - starty;

                    }
                    return Math.atan2(height, width);
                };


                this.dialogtext = function () {
                    var lcounter = 0;
                    var point1 = 0;
                    var point2 = 0;
                    var pointstext = "";
                    var angletext = "";
                    var length = 0;
                    var texttotable = "";
                    var trueangle = 0;

                    

                    //Unit = DocObj.getCurrentpage().Unitlabel;

                    Unit = DocObj.getCurrentpage().scaleObject.Unitlabel;

                    

                    var dimwidth = tool.w - tool.x;
                    var dimheight = tool.h - tool.y;

                    var centerx = (tool.x + tool.w) / 2;
                    var centery = (tool.y + tool.h) / 2;

                    centerx = centerx - tool.pageobject.getdx();
                    centery = centery - tool.pageobject.getdy();


                    var dimdiag = tool.getdiag(dimwidth, dimheight);

                    var lineangle = tool.getangleofline(tool.x, tool.y,tool.w,tool.h, false);
                    //console.log((lineangle / (Math.PI / 180)));

                    var scaledDist = 0;

                    var xoffset =  tool.x - tool.pageobject.getdx();
                    var yoffset =  tool.y - tool.pageobject.getdy();

                    var pagedim = tool.pageobject.getpagedim();
                    

                    //var pagescale = tool.pageobject.getdscale();
                    var pagescale = pagedim.scale;
                    tool.distance = dimdiag;

                    /*if (DocObj.getCurrentpage().usevectorxml) {
                        tool.distance = getUnitlength(dimdiag / DocObj.getCurrentpage().dscalevector);
                    } else if (DocObj.getCurrentpage().usepdfjs) {
                        tool.distance = getUnitlength(dimdiag / (DocObj.getCurrentpage().curpagescale * DocObj.getCurrentpage().dscalepdf));
                    } else {
                        tool.distance = getUnitlength(dimdiag / DocObj.getCurrentpage().dscale);
                    }*/


                    if (tool.pageobject.usevectorxml && tool.pageobject.VectorPageObj) {
                        var pagewidth = pagescale * tool.pageobject.VectorPageObj.width;
                        var opagewidth = tool.pageobject.VectorPageObj.width;
                        var opageheight = tool.pageobject.VectorPageObj.height;


                        //tool.distance = getUnitlength(dimdiag / tool.pageobject.dscalevector);

                        xoffset /= tool.pageobject.dscalevector;
                        yoffset /= tool.pageobject.dscalevector;

                        centerx /= tool.pageobject.dscalevector;
                        centery /= tool.pageobject.dscalevector;
    
                        scaledDist = tool.distance / tool.pageobject.dscalevector;



                    } else if (tool.pageobject.usepdfjs) {

                        switch(tool.pageindex){

                            case 0:
                                //pagewidth = tool.compareref.bgpagecanvas.width;

                                break;

                            case 1:
                                //pagewidth = tool.compareref.ovpagecanvas.width;


                                break;

                        }
                        //tool.distance = getUnitlength(dimdiag);
                        //tool.distance = getUnitlength(dimdiag / (tool.pageobject.curpagescale * tool.pageobject.dscalepdf));
                        //tool.distance = getUnitlength(dimdiag);
                        //tool.distance *= tool.pageobject.dscalepdf;

                        //pagewidth = tool.pageobject.pagecanvas.width; change to use offscreenwidth
                        pagewidth = tool.pageobject.offscreenwidth;

                        
                        //var scaledPageWidth = tool.pageobject.pagecanvas.width * tool.pageobject.dscalepdf; change to use offscreenwidth
                        var scaledPageWidth = tool.pageobject.offscreenwidth * tool.pageobject.dscalepdf;
                        

                        
                        pagewidth = scaledPageWidth;
                        

                        //console.log(tool.pageobject.dscalepdf);
                        //console.log(tool.distance);

                        var curpagescale  = scaledPageWidth / tool.pageobject.pdfpagewidth;

                        opagewidth = tool.pageobject.pdfpagewidth;
                        opageheight = tool.pageobject.pdfpageheight;


                        xoffset /= curpagescale;
                        yoffset /= curpagescale;

                        centerx /= curpagescale;
                        centery /= curpagescale;

                        scaledDist = tool.distance / curpagescale;



                    } else {
                        pagewidth = pagescale * tool.pageobject.MainImageWidth;
                        opagewidth = tool.pageobject.MainImageWidth;
                        opageheight = tool.pageobject.MainImageHeight;


                        xoffset /= tool.pageobject.dscale;
                        yoffset /= tool.pageobject.dscale;

                        centerx /= tool.pageobject.dscale;
                        centery /= tool.pageobject.dscale;
                        
                        scaledDist = tool.distance / tool.pageobject.dscale;

                    }

                    //pagewidth = getUnitlength(pagewidth);
                    var scaleinfo = {scaledDistance : scaledDist, pagedim : pagedim, linecenter : {x : centerx, y : centery}};

                    if(RxCore_GUI_CompareMeasure != undefined){
                        RxCore_GUI_CompareMeasure.AlignMeasure(tool.distance,lineangle,{x:xoffset,y:yoffset},pagewidth, scaleinfo);
                    }



                };

                this.drawCross = function(ctx,mousePos){
                    ctx.clearRect(0, 0, canvas.width, canvas.height);
                    ctx.strokeStyle = "gray";
                    ctx.lineWidth = 1;

                    ctx.beginPath();
                    ctx.moveTo(0, mousePos.y);
                    ctx.lineTo(mousePos.x - 1, mousePos.y);
                    ctx.moveTo(mousePos.x + 1, mousePos.y);
                    ctx.lineTo(canvas.width, mousePos.y);
                    ctx.moveTo(mousePos.x, 0);
                    ctx.lineTo(mousePos.x, mousePos.y - 1);
                    ctx.moveTo(mousePos.x, mousePos.y + 1);
                    ctx.lineTo(mousePos.x, canvas.height);
                    ctx.closePath();
                    ctx.stroke();

                };


                this.draw = function (ctx) {

                    var arrowangle = 22.5;
                    var arrowlength = nArrowSize;
                    var arrowanglerad = arrowangle / (180 / Math.PI);

                    var dimwidth = tool.w - tool.x;
                    var dimheight = tool.h - tool.y;

                    var dimanglerad = Math.atan2(dimheight, dimwidth);
                    var arrowh = Math.abs(arrowlength / Math.cos(arrowanglerad));




                    ctx.strokeStyle = "blue";
                    ctx.lineWidth = 2;


                    ctx.beginPath();
                    ctx.moveTo(this.x, this.y);
                    ctx.lineTo(this.w, this.h);
                    ctx.closePath();
                    ctx.stroke();

                    ctx.strokeRect(this.x-15, this.y-15,30,30);


                    ctx.beginPath();
                    ctx.moveTo(this.x, this.y-15);
                    ctx.lineTo(this.x, this.y-1);
                    ctx.moveTo(this.x, this.y+15);
                    ctx.lineTo(this.x, this.y+1);
                    ctx.moveTo(this.x-15, this.y);
                    ctx.lineTo(this.x-1, this.y);
                    ctx.moveTo(this.x+15, this.y);
                    ctx.lineTo(this.x+1, this.y);
                    ctx.closePath();
                    ctx.stroke();


                    ctx.strokeRect(this.w-15, this.h-15,30,30);

                    ctx.beginPath();
                    ctx.moveTo(this.w, this.h-15);
                    ctx.lineTo(this.w, this.h-1);
                    ctx.moveTo(this.w, this.h+15);
                    ctx.lineTo(this.w, this.h+1);
                    ctx.moveTo(this.w-15, this.h);
                    ctx.lineTo(this.w-1, this.h);
                    ctx.moveTo(this.w+15, this.h);
                    ctx.lineTo(this.w+1, this.h);
                    ctx.closePath();
                    ctx.stroke();



                };

                this.scrollhandling = function (movey) {
                    if (!documentopen || DocObj.getCurrentpage().usevector3Dxml) {
                        return;
                    }



                    if (movey < 0) {


                        if (DocObj.getCurrentpage().usepdfjs && DocObj.pages.length > 1 && !DocObj.pagelocked) {
                            //DocObj.getCurrentpage().pan_update(0,-movey);
                            if (DocObj.currentpage == DocObj.NumPages - 1) {
                                /*if (DocObj.getCurrentpage().endy <= canvas.height) {
                                    movey = 0;
                                }*/
                            }
                            DocObj.getCurrentpage().pan_update(0, -movey);
                            


                        } else {

                            //DocObj.getCurrentpage().ZoomOut(1.1, false, false);
                            DocObj.getCurrentpage().ZoomOut(1.1, false, false, tool.curmousepos);

                            var curscale = DocObj.getCurrentpage().getdscale();
                            var pagedx = DocObj.getCurrentpage().getdx();
                            var pagedy = DocObj.getCurrentpage().getdy();


                            if(tool.started){

                                var scalediff = (curscale / tool.orgscale);
                                var newx = (tool.startx - tool.orgdx) * scalediff;
                                var newy = (tool.starty - tool.orgdy) * scalediff;
                                newx += pagedx;
                                newy += pagedy;

                                tool.x = newx;
                                tool.y = newy;

                            }



                        }


                    } else {

                        if (DocObj.getCurrentpage().usepdfjs && DocObj.pages.length > 1 && !DocObj.pagelocked) {

                            if (DocObj.currentpage == 0) {
                                /*if (DocObj.getCurrentpage().starty >= 0) {
                                    movey = 0;
                                }*/

                            }
                            DocObj.getCurrentpage().pan_update(0, -movey);
                            


                        } else {


                            //DocObj.getCurrentpage().ZoomIn(1.1, false, false);
                            DocObj.getCurrentpage().ZoomIn(1.1, false, false, tool.curmousepos);

                            curscale = DocObj.getCurrentpage().getdscale();
                            pagedx = DocObj.getCurrentpage().getdx();
                            pagedy = DocObj.getCurrentpage().getdy();


                            if(tool.started){
                                scalediff = (curscale / tool.orgscale);
                                newx = (tool.startx - tool.orgdx) * scalediff;
                                newy = (tool.starty - tool.orgdy) * scalediff;
                                newx += pagedx;
                                newy += pagedy;

                                tool.x = newx;
                                tool.y = newy;

                            }

                        }

                    }


                };

                this.wheel = function (ev) {

                    var delta = 0;
                    if (ev.wheelDelta !== undefined) { // WebKit / Opera / Explorer 9

                        delta = ev.wheelDelta;

                    } else if (ev.deltaY !== undefined) { // Firefox

                        delta = -ev.deltaY*50;

                    }

                    tool.scrollhandling(delta);


                };

                this.pointermove = function (ev) {
                    tool.MSPointerMove(ev);
                };
                this.pointerdown = function (ev) {
                    tool.MSPointerDown(ev);
                };

                this.pointerup = function (ev) {
                    tool.MSPointerUp(ev);
                };


                this.MSPointerDown = function (ev) {
                    var mousePos = getMousePos(canvas, ev);

                    if(DocObj == null || DocObj == undefined){
                        return;
                    }
            

                    if(ev.button == 0){
                        if (!tool.started) {
                            ev.preventDefault();
                            ev.stopPropagation();

                            ev.target.style.cursor = 'crosshair';
                            //ev.target.style.cursor = 'url(' + curcanv.toDataURL() + ')15 15, auto';

                            tool.started = true;
                            if (havesnap) {
                                if(DocObj.getCurrentpage().getpageRotation() != 0){
                                    var rotatedpoint = snap_rotated(snapPoint);
                                }else{
                                    rotatedpoint = snapPoint;
                                }

                                tool.x = rotatedpoint.x;
                                tool.y = rotatedpoint.y;
                                tool.w = rotatedpoint.x;
                                tool.h = rotatedpoint.y;

                            } else {
                                tool.x = mousePos.x;
                                tool.y = mousePos.y;
                                tool.w = mousePos.x;
                                tool.h = mousePos.y;

                            }

                            tool.orgscale = DocObj.getCurrentpage().getdscale();
                            tool.orgdx = DocObj.getCurrentpage().getdx();
                            tool.orgdy = DocObj.getCurrentpage().getdy();
                            tool.startx = tool.x;
                            tool.starty = tool.y;


                        } else {

                            if (havesnap) {
                                if(DocObj.getCurrentpage().getpageRotation() != 0){
                                    rotatedpoint = snap_rotated(snapPoint);
                                }else{
                                    rotatedpoint = snapPoint;
                                }

                                tool.w = rotatedpoint.x;
                                tool.h = rotatedpoint.y;

                            } else {
                                tool.w = mousePos.x;
                                tool.h = mousePos.y;

                            }

                            tool.started = false;

                            tool.dialogtext();

                            context.clearRect(0, 0, canvas.width, canvas.height);
                            ev.target.style.cursor = 'default';


                        }
                    }else{
                        prevx = mousePos.x;
                        prevy = mousePos.y;

                    }


                };
                this.mousedown = function (ev) {
                    var mousePos = getMousePos(canvas, ev);

                    

                    if(ev.button == 0){
                        if (!tool.started ) {
                            ev.preventDefault();
                            ev.stopPropagation();

                            //ev.target.style.cursor = 'crosshair';
                            ev.target.style.cursor = 'url(' + curcanv.toDataURL() + ')15 15, auto';

                            tool.started = true;
                            if (havesnap) {
                                if(DocObj.getCurrentpage().getpageRotation() != 0){
                                    var rotatedpoint = snap_rotated(snapPoint);
                                }else{
                                    rotatedpoint = snapPoint;
                                }

                                tool.x = rotatedpoint.x;
                                tool.y = rotatedpoint.y;
                                tool.w = rotatedpoint.x;
                                tool.h = rotatedpoint.y;

                            } else {
                                tool.x = mousePos.x;
                                tool.y = mousePos.y;
                                tool.w = mousePos.x;
                                tool.h = mousePos.y;

                            }
                            tool.orgscale = DocObj.getCurrentpage().getdscale();
                            tool.orgdx = DocObj.getCurrentpage().getdx();
                            tool.orgdy = DocObj.getCurrentpage().getdy();
                            tool.startx = tool.x;
                            tool.starty = tool.y;


                        } else {
                            if (havesnap) {
                                if(DocObj.getCurrentpage().getpageRotation() != 0){
                                    rotatedpoint = snap_rotated(snapPoint);
                                }else{
                                    rotatedpoint = snapPoint;
                                }

                                tool.w = rotatedpoint.x;
                                tool.h = rotatedpoint.y;

                            } else {
                                tool.w = mousePos.x;
                                tool.h = mousePos.y;

                            }
                            tool.started = false;

                            tool.dialogtext();


                            context.clearRect(0, 0, canvas.width, canvas.height);
                            ev.target.style.cursor = 'default';

                        }
                    }else{

                        prevx = mousePos.x;
                        prevy = mousePos.y;


                    }



                };

                this.touchstart = function (ev) {

                    if (!tool.started) {
                        var touchPos = getTouchPos(canvas, ev, 0);
                        tool.started = true;
                        tool.x = touchPos.x;
                        tool.y = touchPos.y;
                        tool.w = touchPos.x;
                        tool.h = touchPos.y;

                        tool.orgscale = DocObj.getCurrentpage().getdscale();
                        tool.orgdx = DocObj.getCurrentpage().getdx();
                        tool.orgdy = DocObj.getCurrentpage().getdy();


                    } else {
                        //tool.started = true;
                    }
                };

                this.MSPointerMove = function (ev) {
                    var mousePos = getMousePos(canvas, ev);

                    tool.curmousepos = mousePos;

                    ev.preventDefault();
                    ev.stopPropagation();

                    ev.target.style.cursor = 'crosshair';

                    var xdiff = prevx - mousePos.x;
                    var ydiff = prevy - mousePos.y;


                    snapPoint = DocObj.getCurrentpage().getsnap(mousePos);

                    if (!snapPoint.found) {
                        havesnap = false;
                        context.clearRect(0, 0, canvas.width, canvas.height);
                    } else {
                        havesnap = true;
                        drawsnap(context,snapPoint);

                    }


                    if (ev.buttons == 2) {

                        DocObj.getCurrentpage().pan_update(xdiff, ydiff);
                        
                        prevx = mousePos.x;
                        prevy = mousePos.y;

                    }


                    if (!tool.started) {
                        return;
                    }

                    if (havesnap) {
                        if(DocObj.getCurrentpage().getpageRotation() != 0){
                            var rotatedpoint = snap_rotated(snapPoint);
                        }else{
                            rotatedpoint = snapPoint;
                        }

                        tool.w = rotatedpoint.x;
                        tool.h = rotatedpoint.y;

                    } else {
                        tool.w = mousePos.x;
                        tool.h = mousePos.y;

                    }


                    context.clearRect(0, 0, canvas.width, canvas.height);
                    if (ev.buttons == 2) {
                        tool.x -= xdiff;
                        tool.y -= ydiff;

                    }

                    if (tool.w - tool.x != 0) {
                        tool.draw(context);
                    }

                };
                this.mousemove = function (ev) {
                    ev.preventDefault();
                    ev.stopPropagation();

                    ev.target.style.cursor = 'url(' + curcanv.toDataURL() + ')15 15, auto';

                    var mousePos = getMousePos(canvas, ev);

                    tool.curmousepos = mousePos;

                    tool.drawCross(context,mousePos);

                    var xdiff = prevx - mousePos.x;
                    var ydiff = prevy - mousePos.y;


                    snapPoint = DocObj.getCurrentpage().getsnap(mousePos);

                    if (!snapPoint.found) {
                        havesnap = false;
                        //context.clearRect(0, 0, canvas.width, canvas.height);
                    } else {
                        havesnap = true;
                        drawsnap(context,snapPoint);

                    }
                    if (ev.button == 2 || ev.buttons == 2) {

                        DocObj.getCurrentpage().pan_update(xdiff, ydiff);
                        

                        prevx = mousePos.x;
                        prevy = mousePos.y;

                    }

                    if (!tool.started) {
                        return;
                    }


                    if (havesnap) {
                        if(DocObj.getCurrentpage().getpageRotation() != 0){
                            var rotatedpoint = snap_rotated(snapPoint);
                        }else{
                            rotatedpoint = snapPoint;
                        }

                        tool.w = rotatedpoint.x;
                        tool.h = rotatedpoint.y;

                    } else {
                        tool.w = mousePos.x;
                        tool.h = mousePos.y;

                    }

                    context.clearRect(0, 0, canvas.width, canvas.height);
                    if (ev.button == 2 || ev.buttons == 2) {
                        tool.x -= xdiff;
                        tool.y -= ydiff;

                    }

                    if (havesnap) {
                        drawsnap(context,snapPoint);
                    }

                    if (tool.w - tool.x != 0) {
                        tool.draw(context);
                    }

                };

                this.touchmove = function (ev) {
                    ev.preventDefault();
                    if (!tool.started) {
                        return;
                    }

                    var touchPos = getTouchPos(canvas, ev, 0);
                    tool.w = touchPos.x;
                    tool.h = touchPos.y;

                    context.clearRect(0, 0, canvas.width, canvas.height);
                    if (tool.w - tool.x != 0) {
                        tool.draw(context);
                    }


                };

                this.MSPointerUp = function () {
                    if (tool.started) {

                    }

                };
                this.mouseup = function () {
                    if (tool.started) {
                    }
                };

                this.touchcancel = function(ev){
                    ev.preventDefault();

                    if (tool.started) {
                        tool.started = false;
                        tool.dialogtext();

                        context.clearRect(0, 0, canvas.width, canvas.height);
                    }

                };                
                this.touchend = function (ev) {
                    ev.preventDefault();

                    if (tool.started) {
                        tool.started = false;
                        tool.dialogtext();

                        context.clearRect(0, 0, canvas.width, canvas.height);
                    }
                };

            };

            tools.calibrate = function () {

                var tool = this;
                var prevx = 0,
                    prevy = 0;
                var Unit = "mm";
                var havesnap = false;
                var snapPoint;

                this.started = false;
                this.name = 'calibrate';
                this.anglelengthsupport = false;
                this.refreshSupport = false;

                this.x = 0;
                this.y = 0;

                this.w = 0;
                this.h = 0;
                this.distance = 0.0;
                
                this.startx = 0;
                this.starty = 0;
                this.orgscale = 1.0;
                this.orgdx = 0.0;
                this.orgdy = 0.0;
                //this.curmousepos = {x:0,y:0};
                this.curmousepos = {x:0,y:0,xus:0,yus:0};

                this.dialogfieldtext = "";


                setCursor(getMarkupCursor('calibrate'));


                this.scrollhandling = function (movey) {
                    if (!documentopen || DocObj.getCurrentpage().usevector3Dxml) {
                        return;
                    }

                    if (movey < 0) {


                        if (DocObj.getCurrentpage().usepdfjs && DocObj.pages.length > 1) {
                            
                            /*if (DocObj.currentpage == DocObj.NumPages - 1) {
                                if (DocObj.getCurrentpage().endy <= canvas.height) {
                                    movey = 0;
                                }
                            }*/
                            DocObj.getCurrentpage().pan_update(0, -movey);
                            


                        } else {


                            if(belowlimitExtentEx(1.1, tool.curmousepos)){
                                DocObj.getCurrentpage().ZoomOut(1.1, false, false, tool.curmousepos);
                            }else{
                                RxCore_foxitForceRedraw();
                            }



                            if(tool.started){
                                tool.setdimoffset(false);
                                tool.draw(context);


                            }



                        }


                    } else {

                        if (DocObj.getCurrentpage().usepdfjs && DocObj.pages.length > 1) {

                            if (DocObj.currentpage == 0) {
                                /*if (DocObj.getCurrentpage().starty >= 0) {
                                    movey = 0;
                                }*/

                            }
                            DocObj.getCurrentpage().pan_update(0, -movey);
                            


                        } else {


                            DocObj.getCurrentpage().ZoomIn(1.1, false, false, tool.curmousepos);



                            if(tool.started){
                                tool.setdimoffset(false);
                                tool.draw(context);

                            }

                        }

                    }


                };

                this.belowlimitExtent = function(zoomscalefactor){
                    var belowlimit = true;
                    

                    var pagedim = DocObj.getCurrentpage().getpagedim();

                    var wlimit = canvasowidth - 60;
                    var hlimit = canvasoheight - 60;

                    if((pagedim.w / zoomscalefactor) < wlimit && (pagedim.h / zoomscalefactor) < hlimit){
                        belowlimit = false;
                        //scalechanged = false;
                        //DocObj.getCurrentpage().zoomall();
                    }

                    var pagerect = DocObj.getCurrentpage().ZoomOutTest(zoomscalefactor, false, tool.curmousepos);
                    
                    var heightlimit = canvasoheight - (nPanlimit*2);
                    var widthlimit = canvasowidth - (nPanlimit*2);
                    //var toplimit = 15;
                    var bottomlimit = ((pagerect.y + pagerect.h) <= heightlimit);
                    var toplimit = (pagerect.y >= nPanlimit);

                    // need to return these to reset the top and bottom positions.scroll|| bottomlimit || toplimit
                    if ((pagerect.w / pagerect.h) < (canvasowidth / canvasoheight)){
                        if(pagerect.h <= heightlimit){
                            belowlimit = false;
                        }
    
                    }else{
                        if(pagerect.w <= widthlimit){
                            belowlimit = false;
                        }

                    }


                    if(!bLimitZoomOut){
                        belowlimit = true;
                    }

                    return belowlimit;
                };            

                this.wheel = function (ev) {

                    var delta = 0;
                    if (ev.wheelDelta !== undefined) { // WebKit / Opera / Explorer 9

                        delta = ev.wheelDelta;

                    } else if (ev.deltaY !== undefined) { // Firefox

                        delta = -ev.deltaY*50;

                    }

                    tool.scrollhandling(delta);


                };

                this.setdimoffset = function(initial){

                    if(initial){

                        tool.orgscale = DocObj.getCurrentpage().getdscale();
                        tool.orgdx = DocObj.getCurrentpage().getdx();
                        tool.orgdy = DocObj.getCurrentpage().getdy();
                        tool.startx = tool.x;
                        tool.starty = tool.y;
                    

                    }
                    
                    var curscale = DocObj.getCurrentpage().getdscale();
                    var pagedx = DocObj.getCurrentpage().getdx();
                    var pagedy = DocObj.getCurrentpage().getdy();

                    var scalediff = (curscale / tool.orgscale);

                    if(tool.started){
                        var newx = (tool.startx - tool.orgdx) * scalediff;
                        var newy = (tool.starty - tool.orgdy) * scalediff;
                        newx += pagedx;
                        newy += pagedy;

                        tool.x = newx;
                        tool.y = newy;
    

                    }




                };  
                this.getdiag = function (width, height) {


                    var dimwsq = Math.pow(width, 2);
                    var dimhsq = Math.pow(height, 2);
                    var dimdiag = Math.sqrt((dimwsq + dimhsq));

                    return dimdiag;

                };


                this.dialogtext = function () {
                    //var lcounter = 0;
                    //var point1 = 0;
                    //var point2 = 0;
                    //var pointstext = "";
                    //var angletext = "";
                    //var length = 0;
                    //var texttotable = "";
                    //var trueangle = 0;

                    
                    //Unit = DocObj.getCurrentpage().Unitlabel;
                    Unit = DocObj.getCurrentpage().scaleObject.Unitlabel;

                    
                    

                    var dimwidth = tool.w - tool.x;
                    var dimheight = tool.h - tool.y;


                    var dimdiag = tool.getdiag(dimwidth, dimheight);
                    var tempscale = MeasureScale;
                    var tempscale = DocObj.getCurrentpage().scaleObject.getMeasureScale();

                    if(markupForScale) {
                        tempscale = markupForScale.scaleObject.getMeasureScale();
                    }
            

                    MeasureScale = 1.0;

                    tool.distance = getUnitlength(dimdiag / getCurrentPageScale());

                    //tool.distance = tool.distance.toFixed(2);
                    //nCalibrateMeasured = tool.distance;
                    
                    //DocObj.getCurrentpage().setMeasureScaledirect(1);
                    DocObj.getCurrentpage().scaleObject.setMeasureScaledirect(1);


                    nCalibrateMeasured = getUnitlength(dimdiag / getCurrentPageScale());


                    //DocObj.getCurrentpage().setMeasureScaledirect(tempscale);

                    DocObj.getCurrentpage().scaleObject.setMeasureScaledirect(tempscale);
                    
                    MeasureScale = tempscale;
                    /*this.dialogfieldtext = "<br>" +
                     "       <table>" +
                     "        <tr >" +
                     "	     <td nowrap>Measured line is: " + "</td>\n" +
                     "        </tr>" +
                     "        <tr >" +
                     "	     <td nowrap>" + "<input type='text' size='10' id='Cval' value='" + tool.distance + "' onChange='SetTempCal(this.value)'> " + Unit + " long</td>\n" +
                     "        </tr>" +
                     "        </table>" +
                     "        <br />\n" +
                     "        <br />\n";*/
                    //"        <button data-dojo-type='dijit/form/Button' data-dojo-props='onClick:hideCDialogOK'>OK</button>\n" +
                    //"        <button data-dojo-type='dijit/form/Button' data-dojo-props='onClick:hideCDialogCancel'>Cancel</button>";

                };


                this.draw = function (ctx) {

                    var curscale = DocObj.getCurrentpage().getdscale();
                    var fixedscalefactor = curscale / DocObj.getCurrentpage().fixedScale;

                    var arrowangle = 22.5;
                    var arrowlength = nArrowSize * fixedscalefactor;
                    var arrowanglerad = arrowangle / (180 / Math.PI);


                    var dimwidth = tool.w - tool.x;
                    var dimheight = tool.h - tool.y;


                    var dimanglerad = Math.atan2(dimheight, dimwidth);
                    var arrowh = Math.abs(arrowlength / Math.cos(arrowanglerad));


                    var dimangleradneg = Math.PI + dimanglerad;
                    var arrowupperangle = dimangleradneg + arrowanglerad;
                    var arrowlowerangle = dimangleradneg - arrowanglerad;

                    var topx = this.w + Math.cos(arrowupperangle) * arrowh;
                    var topy = this.h + Math.sin(arrowupperangle) * arrowh;
                    var botx = this.w + Math.cos(arrowlowerangle) * arrowh;
                    var boty = this.h + Math.sin(arrowlowerangle) * arrowh;

                    var sarrowupperangle = dimanglerad + arrowanglerad;
                    var sarrowlowerangle = dimanglerad - arrowanglerad;


                    var stopx = this.x + Math.cos(sarrowupperangle) * arrowh;
                    var stopy = this.y + Math.sin(sarrowupperangle) * arrowh;
                    var sbotx = this.x + Math.cos(sarrowlowerangle) * arrowh;
                    var sboty = this.y + Math.sin(sarrowlowerangle) * arrowh;

                    //this.dimtext = getUnitlength(dimdiag);
                    //this.dimtext = this.dimtext.toFixed(2);

                    //this.dimtext = this.dimtext + " " + Unitlabel;


                    //ctx.font = this.measuretextheight + "pt " + "Helvetica";
                    //var dimt = ctx.measureText(this.dimtext);
                    //var dimtextwidth = dimt.width;

                    //var dimtextx = this.x + (dimwidth / 2) - (dimtextwidth / 2);
                    //var dimtexty = this.y + (dimheight / 2)- (this.measuretextheight / 2);

                    //ctx.fillStyle = this.color;
                    //ctx.fillText(this.dimtext, dimtextx, dimtexty);

                    if (DocObj.backgroundColor == "#000000") {
                        ctx.strokeStyle = "white";
                    }else{
                        ctx.strokeStyle = "black";
                    }

                    ctx.lineWidth = 1;
                    ctx.beginPath();
                    ctx.moveTo(this.x, this.y);
                    ctx.lineTo(this.w, this.h);
                    ctx.closePath();
                    ctx.stroke();

                    //arrowlines

                    ctx.beginPath();
                    ctx.moveTo(this.x, this.y);
                    ctx.lineTo(stopx, stopy);
                    ctx.closePath();
                    ctx.stroke();

                    ctx.beginPath();
                    ctx.moveTo(this.x, this.y);
                    ctx.lineTo(sbotx, sboty);
                    ctx.closePath();
                    ctx.stroke();


                    ctx.beginPath();
                    ctx.moveTo(this.w, this.h);
                    ctx.lineTo(topx, topy);
                    ctx.closePath();
                    ctx.stroke();

                    ctx.beginPath();
                    ctx.moveTo(this.w, this.h);
                    ctx.lineTo(botx, boty);
                    ctx.closePath();
                    ctx.stroke();


                };


                this.pointermove = function (ev) {
                    tool.MSPointerMove(ev);
                };
                this.pointerdown = function (ev) {
                    tool.MSPointerDown(ev);
                };

                this.pointerup = function (ev) {
                    tool.MSPointerUp(ev);
                };


                this.MSPointerDown = function (ev) {
                    var mousePos = getMousePos(canvas, ev);
                    if(ev.button == 0){
                        if (!tool.started) {
                            ev.preventDefault();
                            ev.stopPropagation();
    
    
                            tool.started = true;
                            if (havesnap) {
                                if(DocObj.getCurrentpage().getpageRotation() != 0){
                                    var rotatedpoint = snap_rotated(snapPoint);
                                }else{
                                    rotatedpoint = snapPoint;
                                }
    
                                tool.x = rotatedpoint.x;
                                tool.y = rotatedpoint.y;
                                tool.w = rotatedpoint.x;
                                tool.h = rotatedpoint.y;
    
                            } else {
                                tool.x = mousePos.x;
                                tool.y = mousePos.y;
                                tool.w = mousePos.x;
                                tool.h = mousePos.y;
    
                            }
                            tool.setdimoffset(true);
                        } else {
    
                            if (havesnap) {
                                if(DocObj.getCurrentpage().getpageRotation() != 0){
                                    rotatedpoint = snap_rotated(snapPoint);
                                }else{
                                    rotatedpoint = snapPoint;
                                }
    
                                tool.w = rotatedpoint.x;
                                tool.h = rotatedpoint.y;
    
                            } else {
                                tool.w = mousePos.x;
                                tool.h = mousePos.y;
    
                            }
    
                            tool.started = false;
    
                            tool.dialogtext();
                            if (RxCore_GUI_Calibratediag != undefined) {
                                RxCore_GUI_Calibratediag.setCalibrate(tool.distance);
                            }
    
                            //setCContent(tool.dialogfieldtext);
                            context.clearRect(0, 0, canvas.width, canvas.height);
                            
                            
    
                        }
                    }else{
                        prevx = mousePos.x;
                        prevy = mousePos.y;

                    }

                    

                };
                this.mousedown = function (ev) {
                    var mousePos = getMousePos(canvas, ev);
                    if(ev.button == 0){
                        if (!tool.started) {
                            ev.preventDefault();
                            ev.stopPropagation();
    
    
                            tool.started = true;
                            if (havesnap) {
                                if(DocObj.getCurrentpage().getpageRotation() != 0){
                                    var rotatedpoint = snap_rotated(snapPoint);
                                }else{
                                    rotatedpoint = snapPoint;
                                }
    
                                tool.x = rotatedpoint.x;
                                tool.y = rotatedpoint.y;
                                tool.w = rotatedpoint.x;
                                tool.h = rotatedpoint.y;
    
                            } else {
                                tool.x = mousePos.x;
                                tool.y = mousePos.y;
                                tool.w = mousePos.x;
                                tool.h = mousePos.y;
    
                            }
                            tool.setdimoffset(true);
                        } else {
                            if (havesnap) {
                                if(DocObj.getCurrentpage().getpageRotation() != 0){
                                    rotatedpoint = snap_rotated(snapPoint);
                                }else{
                                    rotatedpoint = snapPoint;
                                }
    
                                tool.w = rotatedpoint.x;
                                tool.h = rotatedpoint.y;
    
                            } else {
                                tool.w = mousePos.x;
                                tool.h = mousePos.y;
    
                            }
                            tool.started = false;
    
                            tool.dialogtext();
                            if (RxCore_GUI_Calibratediag != undefined) {
                                RxCore_GUI_Calibratediag.setCalibrate(tool.distance);
                            }
    
                            //setCContent(tool.dialogfieldtext);
                            context.clearRect(0, 0, canvas.width, canvas.height);
                            
    
                        }
                    }else{
                        prevx = mousePos.x;
                        prevy = mousePos.y;


                    }

                    


                };

                this.touchstart = function (ev) {
                    //context.strokeStyle = "red";

                    if (!tool.started) {
                        var touchPos = getTouchPos(canvas, ev, 0);
                        tool.started = true;
                        tool.x = touchPos.x;
                        tool.y = touchPos.y;
                        tool.w = touchPos.x;
                        tool.h = touchPos.y;

                    } else {
                        //tool.started = true;
                        //tool.w = ev.targetTouches[0].pageX - canvas.offsetLeft;
                        //tool.h = ev.targetTouches[0].pageY - canvas.offsetTop - bannerheight;
                        //tool.started = false;
                    }
                };

                this.MSPointerMove = function (ev) {
                    var mousePos = getMousePos(canvas, ev);
                    ev.preventDefault();
                    ev.stopPropagation();
                    

                    tool.curmousepos = mousePos;
                    var xdiff = prevx - mousePos.x;
                    var ydiff = prevy - mousePos.y;


                    

                    snapPoint = DocObj.getCurrentpage().getsnap(mousePos);

                    if (!snapPoint.found || snapPoint == undefined) {
                        havesnap = false;
                        context.clearRect(0, 0, canvas.width, canvas.height);
                    } else {
                        havesnap = true;
                        drawsnap(context,snapPoint);

                    }

                    if (ev.buttons == 2) {

                        DocObj.getCurrentpage().pan_update(xdiff, ydiff);
                        prevx = mousePos.x;
                        prevy = mousePos.y;

                        
                    }

                    if (!tool.started) {
                        return;
                    }

                    if (havesnap) {
                        if(DocObj.getCurrentpage().getpageRotation() != 0){
                            var rotatedpoint = snap_rotated(snapPoint);
                        }else{
                            rotatedpoint = snapPoint;
                        }

                        tool.w = rotatedpoint.x;
                        tool.h = rotatedpoint.y;

                    } else {
                        tool.w = mousePos.x;
                        tool.h = mousePos.y;

                    }


                    context.clearRect(0, 0, canvas.width, canvas.height);
                    if (ev.buttons == 2) {
                        tool.x -= xdiff;
                        tool.y -= ydiff;

                    }

                    tool.setdimoffset(false);
                    drawsnap(context,snapPoint);
                    tool.draw(context);


                    /*if (tool.w - tool.x != 0) {
                        tool.setdimoffset(false);
                        tool.draw(context);
                    }*/

                };
                this.mousemove = function (ev) {
                    ev.preventDefault();
                    ev.stopPropagation();

                    var mousePos = getMousePos(canvas, ev);

                    tool.curmousepos = mousePos;
                    var xdiff = prevx - mousePos.x;
                    var ydiff = prevy - mousePos.y;

                    snapPoint = DocObj.getCurrentpage().getsnap(mousePos);

                    if (!snapPoint.found || snapPoint == undefined) {
                        havesnap = false;
                        context.clearRect(0, 0, canvas.width, canvas.height);
                    } else {
                        havesnap = true;
                        drawsnap(context,snapPoint);

                    }

                    if (ev.button == 2 || ev.buttons == 2) {

                        DocObj.getCurrentpage().pan_update(xdiff, ydiff);

                        

                        prevx = mousePos.x;
                        prevy = mousePos.y;

                    }
                    
                    if (!tool.started) {
                        return;
                    }


                    //this.x = tool.x0;
                    //dimensionmarkupobj.y = tool.y0;
                    if (havesnap) {
                        if(DocObj.getCurrentpage().getpageRotation() != 0){
                            var rotatedpoint = snap_rotated(snapPoint);
                        }else{
                            rotatedpoint = snapPoint;
                        }

                        tool.w = rotatedpoint.x;
                        tool.h = rotatedpoint.y;

                    } else {
                        tool.w = mousePos.x;
                        tool.h = mousePos.y;

                    }

                    context.clearRect(0, 0, canvas.width, canvas.height);

                    if (ev.button == 2 || ev.buttons == 2) {
                        tool.x -= xdiff;
                        tool.y -= ydiff;

                    }

                    tool.setdimoffset(false);
                    drawsnap(context,snapPoint);
                    tool.draw(context);
                    


                    /*if (tool.w - tool.x != 0) {
                        tool.setdimoffset(false);
                        tool.draw(context);
                        drawsnap(context,snapPoint);
                    }*/

                };

                this.touchmove = function (ev) {
                    ev.preventDefault();
                    if (!tool.started) {
                        return;
                    }
                    //dimensionmarkupobj.x = tool.x0;
                    //dimensionmarkupobj.y = tool.y0;
                    var touchPos = getTouchPos(canvas, ev, 0);
                    tool.w = touchPos.x;
                    tool.h = touchPos.y;

                    context.clearRect(0, 0, canvas.width, canvas.height);

                    tool.draw(context);

                    /*if (tool.w - tool.x != 0) {
                        tool.draw(context);
                    }*/


                };

                this.MSPointerUp = function () {
                    if (tool.started) {
                        /*tool.mousemove(ev);
                         //dimensionmarkupobj.savemetolist();
                         //dimensionmarkupobj = null;
                         //tool.started = false;
                         //img_update();*/

                    }

                };
                this.mouseup = function () {
                    if (tool.started) {
                        /*tool.mousemove(ev);
                         //dimensionmarkupobj.savemetolist();
                         //dimensionmarkupobj = null;
                         //tool.started = false;
                         //img_update();*/
                    }
                };

                this.touchcancel = function(ev){
                    ev.preventDefault();

                    if (tool.started) {
                        tool.started = false;
                        tool.dialogtext();
                        if (RxCore_GUI_Calibratediag != undefined) {
                            RxCore_GUI_Calibratediag.setCalibrate(tool.distance);
                        }

                        //setCContent(tool.dialogfieldtext);
                        context.clearRect(0, 0, canvas.width, canvas.height);
                    }

                };
                this.touchend = function (ev) {
                    ev.preventDefault();

                    if (tool.started) {
                        tool.started = false;
                        tool.dialogtext();
                        if (RxCore_GUI_Calibratediag != undefined) {
                            RxCore_GUI_Calibratediag.setCalibrate(tool.distance);
                        }

                        //setCContent(tool.dialogfieldtext);
                        context.clearRect(0, 0, canvas.width, canvas.height);
                    }
                };
            };

            tools.overlayscale = function (params) {

                var tool = this;
                var Compareobject = params.p1;
                this.started = false;

                this.points = [];
                this.compareref = Compareobject;


                this.name = 'overlayscale';
                this.anglelengthsupport = false;
                this.refreshSupport = false;

                this.startx = 0;
                this.starty = 0;
                this.x = 0;
                this.y = 0;

                this.w = 0;
                this.h = 0;
                this.distance = 0.0;
                this.orgscale = 1.0;
                this.orgdx = 0.0;
                this.orgdy = 0.0;
                //this.curmousepos = {x:0,y:0};
                this.curmousepos = {x:0,y:0,xus:0,yus:0};

                var curcanv = document.createElement('canvas');
                curcanv.width = 30;
                curcanv.height = 30;
                var curcntximg = curcanv.getContext('2d');

                curcntximg.strokeStyle = "gray";
                curcntximg.lineWidth = 1;

                curcntximg.strokeRect(0, 0,30,30);

                curcntximg.beginPath();
                curcntximg.moveTo(15, 0);
                curcntximg.lineTo(15, 14);
                curcntximg.moveTo(15, 30);
                curcntximg.lineTo(15, 16);
                curcntximg.moveTo(0, 15);
                curcntximg.lineTo(14, 15);
                curcntximg.moveTo(30, 15);
                curcntximg.lineTo(16, 15);
                curcntximg.closePath();
                curcntximg.stroke();

                var prevx = 0,
                    prevy = 0;

                var Unit = "mm";


                var havesnap = false;
                var snapPoint;


                this.dialogfieldtext = "";


                this.getdiag = function (width, height) {


                    var dimwsq = Math.pow(width, 2);
                    var dimhsq = Math.pow(height, 2);
                    var dimdiag = Math.sqrt((dimwsq + dimhsq));

                    return dimdiag;

                };


                this.dialogtext = function () {
                    var lcounter = 0;
                    var point1 = 0;
                    //var point2 = 0;
                    //var pointstext = "";
                    //var angletext = "";
                    //var length = 0;
                    //var texttotable = "";
                    //var trueangle = 0;

                    

                    //var Unitlabel = DocObj.getCurrentpage().Unitlabel;
                    var Unitlabel = DocObj.getCurrentpage().scaleObject.Unitlabel;

                    Unit = Unitlabel;

                    var dimwidth = tool.w - tool.x;
                    var dimheight = tool.h - tool.y;

                    //tool.distance = tool.getdiag(dimwidth, dimheight);
                    var dimdiag = tool.getdiag(dimwidth, dimheight);
                    //var tempscale = MeasureScale;
                    //MeasureScale = 1.0;

                    var xoffset =  tool.x - tool.compareref.pages[tool.compareref.scaleindex].getdx();
                    var yoffset =  tool.y - tool.compareref.pages[tool.compareref.scaleindex].getdy();

                    var pagescale = tool.compareref.pages[tool.compareref.scaleindex].getdscale();
                    tool.distance = dimdiag;

                    if (tool.compareref.pages[tool.compareref.scaleindex].usevectorxml && tool.compareref.pages[tool.compareref.scaleindex].VectorPageObj) {
                        //tool.distance = getUnitlength(dimdiag / tool.compareref.pages[tool.compareref.scaleindex].dscalevector);
                        var pagewidth = pagescale * tool.compareref.pages[tool.compareref.scaleindex].VectorPageObj.width;

                        //tool.distance = dimdiag / tool.compareref.pages[tool.compareref.scaleindex].dscalevector;
                        xoffset /= tool.compareref.pages[tool.compareref.scaleindex].dscalevector;
                        yoffset /= tool.compareref.pages[tool.compareref.scaleindex].dscalevector;

                    } else if (tool.compareref.pages[tool.compareref.scaleindex].usepdfjs) {
                        switch(tool.compareref.scaleindex){
                            case 0:
                                var curpagescale = tool.compareref.backgroundgpage.getPageScale();
                                pagewidth = tool.compareref.backgroundgpage.getwidth();
                                
                                break;

                            case 1:
                                
                                curpagescale = tool.compareref.overlaypage.getPageScale();
                                pagewidth = tool.compareref.overlaypage.getwidth();

                            //pagewidth = tool.compareref.ovpagecanvas.width;
        
                                break;

                        }
                        //pagewidth = tool.compareref.pages[tool.compareref.scaleindex].pagecanvas.width;
                        pagewidth = tool.compareref.pages[tool.compareref.scaleindex].offscreenwidth;
                        //var curpagescale  = tool.compareref.pages[tool.compareref.scaleindex].pagecanvas.width / tool.compareref.pages[tool.compareref.scaleindex].pdfpagewidth;
                        //use offscreenwidth instead of pagecanvas
                        var curpagescale  = tool.compareref.pages[tool.compareref.scaleindex].offscreenwidth / tool.compareref.pages[tool.compareref.scaleindex].pdfpagewidth;
                        
                        //tool.distance = getUnitlength(dimdiag / (tool.compareref.pages[tool.compareref.scaleindex].curpagescale * tool.compareref.pages[tool.compareref.scaleindex].dscalepdf));
                        //tool.distance = dimdiag / (tool.compareref.pages[tool.compareref.scaleindex].curpagescale * tool.compareref.pages[tool.compareref.scaleindex].dscalepdf);
                        
                        xoffset /= curpagescale;
                        yoffset /= curpagescale;


                    } else {
                        //tool.distance = getUnitlength(dimdiag / tool.compareref.pages[tool.compareref.scaleindex].dscale);
                        pagewidth = pagescale * tool.compareref.pages[tool.compareref.scaleindex].MainImageWidth;
                        xoffset /= tool.compareref.pages[tool.compareref.scaleindex].dscale;
                        yoffset /= tool.compareref.pages[tool.compareref.scaleindex].dscale;


                    }

                    //nCalibrateMeasured = tool.distance;
                    //MeasureScale = tempscale;

                    //var offsetdiag = tool.getdiag(xoffset, yoffset);



                    tool.compareref.setdistance(tool.distance,{x:xoffset,y:yoffset},pagewidth);

                    //var tempscale = MeasureScale;
                    //MeasureScale = 1.0;


                    //nCalibrateMeasured = tool.distance;
                    //MeasureScale = tempscale;

                };


                this.draw = function (ctx) {


                    //var arrowangle = 22.5;
                    //var arrowlength = nArrowSize;
                    //var arrowanglerad = arrowangle / (180 / Math.PI);


                    //var dimwidth = tool.w - tool.x;
                    //var dimheight = tool.h - tool.y;


                    //var dimanglerad = Math.atan2(dimheight, dimwidth);
                    //var arrowh = Math.abs(arrowlength / Math.cos(arrowanglerad));


                    //var dimangleradneg = Math.PI + dimanglerad;
                    //var arrowupperangle = dimangleradneg + arrowanglerad;
                    //var arrowlowerangle = dimangleradneg - arrowanglerad;

                    //var topx = this.w + Math.cos(arrowupperangle) * arrowh;
                    //var topy = this.h + Math.sin(arrowupperangle) * arrowh;
                    //var botx = this.w + Math.cos(arrowlowerangle) * arrowh;
                    //var boty = this.h + Math.sin(arrowlowerangle) * arrowh;

                    //var sarrowupperangle = dimanglerad + arrowanglerad;
                    //var sarrowlowerangle = dimanglerad - arrowanglerad;


                    //var stopx = this.x + Math.cos(sarrowupperangle) * arrowh;
                    //var stopy = this.y + Math.sin(sarrowupperangle) * arrowh;
                    //var sbotx = this.x + Math.cos(sarrowlowerangle) * arrowh;
                    //var sboty = this.y + Math.sin(sarrowlowerangle) * arrowh;


                    ctx.strokeStyle = "blue";
                    ctx.lineWidth = 2;


                    ctx.beginPath();
                    ctx.moveTo(this.x, this.y);
                    ctx.lineTo(this.w, this.h);
                    ctx.closePath();
                    ctx.stroke();

                    ctx.strokeRect(this.x-15, this.y-15,30,30);


                    ctx.beginPath();
                    ctx.moveTo(this.x, this.y-15);
                    ctx.lineTo(this.x, this.y-1);
                    ctx.moveTo(this.x, this.y+15);
                    ctx.lineTo(this.x, this.y+1);
                    ctx.moveTo(this.x-15, this.y);
                    ctx.lineTo(this.x-1, this.y);
                    ctx.moveTo(this.x+15, this.y);
                    ctx.lineTo(this.x+1, this.y);
                    ctx.closePath();
                    ctx.stroke();

                    //arrowlines

                    /*ctx.beginPath();
                    ctx.moveTo(this.x, this.y);
                    ctx.lineTo(stopx, stopy);
                    ctx.closePath();
                    ctx.stroke();*/

                    /*ctx.beginPath();
                    ctx.moveTo(this.x, this.y);
                    ctx.lineTo(sbotx, sboty);
                    ctx.closePath();
                    ctx.stroke();*/

                    ctx.strokeRect(this.w-15, this.h-15,30,30);

                    ctx.beginPath();
                    ctx.moveTo(this.w, this.h-15);
                    ctx.lineTo(this.w, this.h-1);
                    ctx.moveTo(this.w, this.h+15);
                    ctx.lineTo(this.w, this.h+1);
                    ctx.moveTo(this.w-15, this.h);
                    ctx.lineTo(this.w-1, this.h);
                    ctx.moveTo(this.w+15, this.h);
                    ctx.lineTo(this.w+1, this.h);
                    ctx.closePath();
                    ctx.stroke();

                    /*ctx.beginPath();
                    ctx.moveTo(this.w, this.h);
                    ctx.lineTo(topx, topy);
                    ctx.closePath();
                    ctx.stroke();*/

                    /*ctx.beginPath();
                    ctx.moveTo(this.w, this.h);
                    ctx.lineTo(botx, boty);
                    ctx.closePath();
                    ctx.stroke();*/


                };

                this.scrollhandling = function (movey) {
                    if (!documentopen || DocObj.getCurrentpage().usevector3Dxml) {
                        return;
                    }



                    if (movey < 0) {


                        if (DocObj.getCurrentpage().usepdfjs && DocObj.pages.length > 1) {
                            //DocObj.getCurrentpage().pan_update(0,-movey);
                            if (DocObj.currentpage == DocObj.NumPages - 1) {
                                /*if (DocObj.getCurrentpage().endy <= canvas.height) {
                                    movey = 0;
                                }*/
                            }
                            DocObj.getCurrentpage().pan_update(0, -movey);
                            


                        } else {

                            DocObj.getCurrentpage().ZoomOut(1.1, false, false, tool.curmousepos);

                            var curscale = DocObj.getCurrentpage().getdscale();
                            var pagedx = DocObj.getCurrentpage().getdx();
                            var pagedy = DocObj.getCurrentpage().getdy();


                            if(tool.started){

                                var scalediff = (curscale / tool.orgscale);
                                var newx = (tool.startx - tool.orgdx) * scalediff;
                                var newy = (tool.starty - tool.orgdy) * scalediff;
                                newx += pagedx;
                                newy += pagedy;

                                tool.x = newx;
                                tool.y = newy;

                            }



                        }


                    } else {

                        if (DocObj.getCurrentpage().usepdfjs && DocObj.pages.length > 1) {

                            if (DocObj.currentpage == 0) {
                                /*if (DocObj.getCurrentpage().starty >= 0) {
                                    movey = 0;
                                }*/

                            }
                            DocObj.getCurrentpage().pan_update(0, -movey);
                            

                        } else {


                            DocObj.getCurrentpage().ZoomIn(1.1, false, false, tool.curmousepos);
                            

                            curscale = DocObj.getCurrentpage().getdscale();
                            pagedx = DocObj.getCurrentpage().getdx();
                            pagedy = DocObj.getCurrentpage().getdy();


                            if(tool.started){
                                scalediff = (curscale / tool.orgscale);
                                newx = (tool.startx - tool.orgdx) * scalediff;
                                newy = (tool.starty - tool.orgdy) * scalediff;
                                newx += pagedx;
                                newy += pagedy;

                                tool.x = newx;
                                tool.y = newy;

                            }

                        }

                    }


                };

                this.wheel = function (ev) {

                    var delta = 0;
                    if (ev.wheelDelta !== undefined) { // WebKit / Opera / Explorer 9

                        delta = ev.wheelDelta;

                    } else if (ev.deltaY !== undefined) { // Firefox

                        delta = -ev.deltaY*50;

                    }

                    tool.scrollhandling(delta);


                };

                this.pointermove = function (ev) {
                    tool.MSPointerMove(ev);
                };
                this.pointerdown = function (ev) {
                    tool.MSPointerDown(ev);
                };

                this.pointerup = function (ev) {
                    tool.MSPointerUp(ev);
                };


                this.MSPointerDown = function (ev) {
                    var mousePos = getMousePos(canvas, ev);
                    if(ev.button == 0){
                        if (!tool.started) {
                            ev.preventDefault();
                            ev.stopPropagation();

                            ev.target.style.cursor = 'crosshair';
                            //ev.target.style.cursor = 'url(' + curcanv.toDataURL() + ')15 15, auto';

                            tool.started = true;
                            if (havesnap) {
                                if(DocObj.getCurrentpage().getpageRotation() != 0){
                                    var rotatedpoint = snap_rotated(snapPoint);
                                }else{
                                    rotatedpoint = snapPoint;
                                }

                                tool.x = rotatedpoint.x;
                                tool.y = rotatedpoint.y;
                                tool.w = rotatedpoint.x;
                                tool.h = rotatedpoint.y;

                            } else {
                                tool.x = mousePos.x;
                                tool.y = mousePos.y;
                                tool.w = mousePos.x;
                                tool.h = mousePos.y;

                            }

                            tool.orgscale = DocObj.getCurrentpage().getdscale();
                            tool.orgdx = DocObj.getCurrentpage().getdx();
                            tool.orgdy = DocObj.getCurrentpage().getdy();
                            tool.startx = tool.x;
                            tool.starty = tool.y;


                        } else {

                            if (havesnap) {
                                if(DocObj.getCurrentpage().getpageRotation() != 0){
                                    rotatedpoint = snap_rotated(snapPoint);
                                }else{
                                    rotatedpoint = snapPoint;
                                }

                                tool.w = rotatedpoint.x;
                                tool.h = rotatedpoint.y;

                            } else {
                                tool.w = mousePos.x;
                                tool.h = mousePos.y;

                            }

                            tool.started = false;

                            tool.dialogtext();
                            /*if (RxCore_GUI_Calibratediag != undefined) {
                             RxCore_GUI_Calibratediag.setCalibrate(tool.distance);
                             }*/

                            context.clearRect(0, 0, canvas.width, canvas.height);
                            ev.target.style.cursor = 'default';


                        }
                    }else{
                        prevx = mousePos.x;
                        prevy = mousePos.y;

                    }


                };
                this.mousedown = function (ev) {
                    var mousePos = getMousePos(canvas, ev);
                    if(ev.button == 0){
                        if (!tool.started ) {
                            ev.preventDefault();
                            ev.stopPropagation();

                            //ev.target.style.cursor = 'crosshair';
                            ev.target.style.cursor = 'url(' + curcanv.toDataURL() + ')15 15, auto';

                            tool.started = true;
                            if (havesnap) {
                                if(DocObj.getCurrentpage().getpageRotation() != 0){
                                    var rotatedpoint = snap_rotated(snapPoint);
                                }else{
                                    rotatedpoint = snapPoint;
                                }

                                tool.x = rotatedpoint.x;
                                tool.y = rotatedpoint.y;
                                tool.w = rotatedpoint.x;
                                tool.h = rotatedpoint.y;

                            } else {
                                tool.x = mousePos.x;
                                tool.y = mousePos.y;
                                tool.w = mousePos.x;
                                tool.h = mousePos.y;

                            }
                            tool.orgscale = DocObj.getCurrentpage().getdscale();
                            tool.orgdx = DocObj.getCurrentpage().getdx();
                            tool.orgdy = DocObj.getCurrentpage().getdy();
                            tool.startx = tool.x;
                            tool.starty = tool.y;


                        } else {
                            if (havesnap) {
                                if(DocObj.getCurrentpage().getpageRotation() != 0){
                                    rotatedpoint = snap_rotated(snapPoint);
                                }else{
                                    rotatedpoint = snapPoint;
                                }

                                tool.w = rotatedpoint.x;
                                tool.h = rotatedpoint.y;

                            } else {
                                tool.w = mousePos.x;
                                tool.h = mousePos.y;

                            }
                            tool.started = false;

                            tool.dialogtext();

                            /*if (RxCore_GUI_Calibratediag != undefined) {
                             RxCore_GUI_Calibratediag.setCalibrate(tool.distance);
                             }*/

                            context.clearRect(0, 0, canvas.width, canvas.height);
                            ev.target.style.cursor = 'default';

                        }
                    }else{

                        prevx = mousePos.x;
                        prevy = mousePos.y;


                    }



                };

                this.touchstart = function (ev) {

                    if (!tool.started) {
                        var touchPos = getTouchPos(canvas, ev, 0);
                        tool.started = true;
                        tool.x = touchPos.x;
                        tool.y = touchPos.y;
                        tool.w = touchPos.x;
                        tool.h = touchPos.y;

                        tool.orgscale = DocObj.getCurrentpage().getdscale();
                        tool.orgdx = DocObj.getCurrentpage().getdx();
                        tool.orgdy = DocObj.getCurrentpage().getdy();


                    } else {
                        //tool.started = true;
                        //tool.w = ev.targetTouches[0].pageX - canvas.offsetLeft;
                        //tool.h = ev.targetTouches[0].pageY - canvas.offsetTop - bannerheight;
                        //tool.started = false;
                    }
                };

                this.MSPointerMove = function (ev) {
                    var mousePos = getMousePos(canvas, ev);
                    ev.preventDefault();
                    ev.stopPropagation();

                    ev.target.style.cursor = 'crosshair';
                    //ev.target.style.cursor = 'url(' + curcanv.toDataURL() + ')15 15, auto';
                    var xdiff = prevx - mousePos.x;
                    var ydiff = prevy - mousePos.y;


                    snapPoint = DocObj.getCurrentpage().getsnap(mousePos);

                    if (!snapPoint.found) {
                        havesnap = false;
                        context.clearRect(0, 0, canvas.width, canvas.height);
                    } else {
                        havesnap = true;
                        drawsnap(context,snapPoint);

                    }

                    //console.log(ev.buttons);
                    if (ev.buttons == 2) {

                        DocObj.getCurrentpage().pan_update(xdiff, ydiff);
                        
                        prevx = mousePos.x;
                        prevy = mousePos.y;

                    }


                    if (!tool.started) {
                        return;
                    }

                    if (havesnap) {
                        if(DocObj.getCurrentpage().getpageRotation() != 0){
                            var rotatedpoint = snap_rotated(snapPoint);
                        }else{
                            rotatedpoint = snapPoint;
                        }

                        tool.w = rotatedpoint.x;
                        tool.h = rotatedpoint.y;

                    } else {
                        tool.w = mousePos.x;
                        tool.h = mousePos.y;

                    }


                    context.clearRect(0, 0, canvas.width, canvas.height);
                    if (ev.buttons == 2) {
                        tool.x -= xdiff;
                        tool.y -= ydiff;

                    }

                    if (tool.w - tool.x != 0) {
                        tool.draw(context);
                    }

                };
                this.mousemove = function (ev) {
                    ev.preventDefault();
                    ev.stopPropagation();
                    //ev.target.style.cursor = 'crosshair';
                    ev.target.style.cursor = 'url(' + curcanv.toDataURL() + ')15 15, auto';

                    var mousePos = getMousePos(canvas, ev);

                    var xdiff = prevx - mousePos.x;
                    var ydiff = prevy - mousePos.y;


                    snapPoint = DocObj.getCurrentpage().getsnap(mousePos);

                    if (!snapPoint.found) {
                        havesnap = false;
                        context.clearRect(0, 0, canvas.width, canvas.height);
                    } else {
                        havesnap = true;
                        drawsnap(context,snapPoint);

                    }
                    //console.log('button',ev.button);
                    //console.log('buttons',ev.buttons);
                    if (ev.button == 2 || ev.buttons == 2) {

                        DocObj.getCurrentpage().pan_update(xdiff, ydiff);
                        

                        prevx = mousePos.x;
                        prevy = mousePos.y;

                    }

                    if (!tool.started) {
                        return;
                    }


                    //this.x = tool.x0;
                    //dimensionmarkupobj.y = tool.y0;
                    if (havesnap) {
                        if(DocObj.getCurrentpage().getpageRotation() != 0){
                            var rotatedpoint = snap_rotated(snapPoint);
                        }else{
                            rotatedpoint = snapPoint;
                        }

                        tool.w = rotatedpoint.x;
                        tool.h = rotatedpoint.y;

                    } else {
                        tool.w = mousePos.x;
                        tool.h = mousePos.y;

                    }

                    context.clearRect(0, 0, canvas.width, canvas.height);
                    if (ev.button == 2 || ev.buttons == 2) {
                        tool.x -= xdiff;
                        tool.y -= ydiff;

                    }

                    if (tool.w - tool.x != 0) {
                        tool.draw(context);
                    }

                };

                this.touchmove = function (ev) {
                    ev.preventDefault();
                    if (!tool.started) {
                        return;
                    }

                    var touchPos = getTouchPos(canvas, ev, 0);
                    tool.w = touchPos.x;
                    tool.h = touchPos.y;

                    context.clearRect(0, 0, canvas.width, canvas.height);
                    if (tool.w - tool.x != 0) {
                        tool.draw(context);
                    }


                };

                this.MSPointerUp = function () {
                    if (tool.started) {
                        /*tool.mousemove(ev);
                         //dimensionmarkupobj.savemetolist();
                         //dimensionmarkupobj = null;
                         //tool.started = false;
                         //img_update();*/

                    }

                };
                this.mouseup = function () {
                    if (tool.started) {
                        /*tool.mousemove(ev);
                         //dimensionmarkupobj.savemetolist();
                         //dimensionmarkupobj = null;
                         //tool.started = false;
                         //img_update();*/
                    }
                };
                this.touchcancel = function(ev){
                    ev.preventDefault();

                    if (tool.started) {
                        tool.started = false;
                        tool.dialogtext();
                        /*if (RxCore_GUI_Calibratediag != undefined) {
                         RxCore_GUI_Calibratediag.setCalibrate(tool.distance);
                         }*/

                        context.clearRect(0, 0, canvas.width, canvas.height);
                    }

                };
                this.touchend = function (ev) {
                    ev.preventDefault();

                    if (tool.started) {
                        tool.started = false;
                        tool.dialogtext();
                        /*if (RxCore_GUI_Calibratediag != undefined) {
                         RxCore_GUI_Calibratediag.setCalibrate(tool.distance);
                         }*/

                        context.clearRect(0, 0, canvas.width, canvas.height);
                    }
                };
            };

            

            function animate() {


                requestAnimationFrame(animate);

                //if(bPDFtemprender && !documentcompare && !DocObj.bLargePDF){
                 if(bPDFtemprender && !documentcompare){
                    if(bAnimatePDFrender){
                        //DocObj.getCurrentpage().draw_canvas(true);
                        DocObj.draw_mpagepdf(false);
                    }

                    //console.log(DocObj.currentpage);
                }
                
                if(bAnimateLineStyle){
                    nlinestyleinterval ++;

                    if (nlinestyleinterval == 10){
                        RxCore_setLineStyleSpace(5, false);
                        
                    }
                    
                    if (nlinestyleinterval == 20){
                        RxCore_setLineStyleSpace(6, false);
                        nlinestyleinterval = 0;
                        
                    }
    
                    if (!bAnimateready) {
                        return;
                    }
                }

                if(docpagNotReady()){
                    return;
                }
        
                
                if (!documentopen || !DocObj.getCurrentpage().usevector3Dxml || !DocObj.getCurrentpage().Vector3DPageObj || suspendrender) {
                    return;
                }


                // Read more about requestAnimationFrame at http://www.paulirish.com/2011/requestanimationframe-for-smart-animating/


                //requestAnimationFrame( animate );
                // Render the scene.
                //var cx = camera.position.x;
                //var cy = camera.position.y;
                //var cz = camera.position.z;


                //var campos = camera.position.clone();


                //DocObj.getCurrentpage().Vector3DPageObj.scene.children[0].position.copy(campos);
                //var camoffset = campos.multiplyScalar(4);
                //camoffset.x *= DocObj.getCurrentpage().Vector3DPageObj.scale;
                //camoffset.y *= DocObj.getCurrentpage().Vector3DPageObj.scale;
                //camoffset.x *= DocObj.getCurrentpage().Vector3DPageObj.scale;


                //DocObj.getCurrentpage().Vector3DPageObj.scene.children[0].position.add(camoffset);
                //thispage.Vector3DPageObj.scene.children[1].rotation.set(90 * Math.PI / 180,0,0);


                //DocObj.getCurrentpage().Vector3DPageObj.scene.__lights[0].position.set(cx,cy,cz+50);
                //DocObj.getCurrentpage().Vector3DPageObj.scene.__lights[0].position.set(cx,cy,cz);


                //DocObj.getCurrentpage().Vector3DPageObj.scene.__lights[0].rotation.set(camera.rotation.x,camera.rotation.y,camera.rotation.z,'XYZ');
                //DocObj.getCurrentpage().Vector3DPageObj.scene.__lights[0].rotation.set(camera.rotation.x,camera.rotation.y,camera.rotation.z,'XYZ');


                renderer.render(DocObj.getCurrentpage().Vector3DPageObj.scene, DocObj.getCurrentpage().camera);

                if(bUsePostprocess){
                    DocObj.getCurrentpage().Vector3DPageObj.effectComposer.render();  
                }


                if(bCameramove){
                    DocObj.getCurrentpage().Vector3DPageObj.renderMarkupPoints(cntximg);
                }

                //DocObj.getCurrentpage().Vector3DPageObj.renderMarkupPoints();

                //DocObj.getCurrentpage().walkthroughcontrol.update(clock.getDelta());
                

                if(DocObj.getCurrentpage().walkthroughcontrol.update && DocObj.curcontrol3D == 'Walkthrough3D'){
                    //DocObj.getCurrentpage().walkthroughcontrol.update(DocObj.getCurrentpage().clock.getDelta());
                    DocObj.getCurrentpage().walkthroughcontrol.update();
                }


                //mesh.rotation.x += 0.01;
                //mesh.rotation.y += 0.02;

                //renderer.render( scene, camera );

            }

            init();
            animate();

            /*if (RxCore_GUI_Ready != undefined){
                RxCore_GUI_Ready.setupComplete(initialDoc);
            }

            if (RxCore_GUI_Permissions != undefined){
                var permissions = {
                    consolidate : bCanConsolidate
                };

                RxCore_GUI_Permissions.UserPermissions(permissions);
            }*/

        };
        
        /*$(document).ready(function () {
        });*/

        if (document.readyState === "complete" || (document.readyState !== "loading" && !document.documentElement.doScroll)){
          
            readycallback();

        } else {
          
            document.addEventListener("DOMContentLoaded", readycallback);

        }

        window.addEventListener('resize', function () {
            /*if (RxCore_GUI_Resize != undefined) {
                var canvasSize = {
                    w:canvasowidth,
                    h:canvasoheight
                };
                RxCore_GUI_Resize.onResize(canvasSize);
            }*/

            if(!defaultLayout.absolute){
                doResize(true);
            }


        }, false);
        //}
    };


    function RxCore_getLayout(){
        return defaultLayout;
    }


    function RxCore_setLayoutEx(offsetWidth, offsetHeight, babsolute) {
        
        if (DocObj){
            if (DocObj.foxitdoc != null){
                //when using iframes do not change layout.
                return;
            }   
        }


        defaultLayout.offsetWidth = defaultLayout.offsetWidth || offsetWidth;
        defaultLayout.offsetHeight = defaultLayout.offsetHeight || offsetHeight;
        defaultLayout.absolute = defaultLayout.absolute || babsolute;



    }

    function RxCore_setLayout(offsetWidth, offsetHeight, babsolute) {

        if (DocObj){
            /*if (DocObj.foxitdoc != null){
                //when using iframes do not change layout.
                return;
            }*/   
        }

        if(offsetWidth != undefined){
            defaultLayout.offsetWidth = offsetWidth;
        }
        if(offsetHeight != undefined){
            defaultLayout.offsetHeight = offsetHeight;
        }

        if(babsolute != undefined){
            defaultLayout.absolute = babsolute;
        }

    }

    function setSmoothingEnabledEx(enabled, ctx){
        if (ctx) {
            ctx.imageSmoothingEnabled = enabled;
            ctx.mozImageSmoothingEnabled = enabled;
            ctx.msImageSmoothingEnabled = enabled;
        }
    }

    function setSmoothingEnabled(enabled) {
        //var context = canvaso.getContext('2d');

        if (contexto) {
            contexto.imageSmoothingEnabled = enabled;
            contexto.mozImageSmoothingEnabled = enabled;
            contexto.msImageSmoothingEnabled = enabled;
        }
    }
    

    function resizeFoxitScroll(w,h){
            

            setCanvasSize(canvimg, w, h);
            setCanvasSize(canvas, w, h);


            //canvimg.width = w;
            //canvimg.height = h;
      
            //canvas.width = w;
            //canvas.height = h;

            //drawmarkupAll()
            drawmarkupAll(cntximg);

            if(tool.refreshSupport){
                tool.refreshDraw();

          }



    }

    function copyCanvasSizenocss(tcv, scv){

        tcv.width = scv.width;
        tcv.height = scv.height;

        //tcv.style.width = scv.style.width;
        //tcv.style.height = scv.style.height;

    }

    function copyCanvasSize(tcv, scv){

        tcv.width = scv.width;
        tcv.height = scv.height;

        tcv.style.width = scv.style.width;
        tcv.style.height = scv.style.height;

    }

    function setCanvasSizeunscaled(cv, width, height){

        const scale = getPixelRatio();

        cv.width = width;
        cv.height = height;
        //cv.style.width = width + 'px';
        //cv.style.height = height + 'px';


    }



    function setCanvasSize(cv, width, height){

        var aw = width;
        var ah = height;
        var sw = width;
        var sh = height;


        const scale = getPixelRatio();
        var style = cv.style;

        cv.width = Math.floor(width * scale);
        cv.height = Math.floor(height * scale);
        
        aw = Math.floor(width * scale);
        ah = Math.floor(height * scale);

        if(scale != 1){

            style.width  = '' + width + 'px';
            style.height = '' + height + 'px';

            sw = width;
            sh = height;

        }
        
        return {awidth : aw, aheight : ah, swidth : sw, sheight : sh};

        //cv.width = width;
        //cv.height = height;

        //cv.style.width = width + 'px';
        //cv.style.height = height + 'px';



    }

    function resizeCanvas(offsetWidth, offsetHeight) {
        //console.log(rxcontainer.clientWidth);
        //console.log(rxcontainer.clientHeight);

        const scale = getPixelRatio();
        
        offsetWidth = offsetWidth || defaultLayout.offsetWidth;
        offsetHeight = offsetHeight || defaultLayout.offsetHeight;

        /* offsetParent offsets are 0 when no position:fixed menus available */
        if (rxcontainer.offsetParent){

            if(!defaultLayout.absolute){
                offsetWidth += rxcontainer.offsetParent.offsetLeft;
                offsetHeight += rxcontainer.offsetParent.offsetTop;
                //console.log(rxcontainer.offsetParent.offsetWidth);
            }

        }
        
        //canvaso.width = rxcontainer.offsetWidth;
        //canvaso.height = rxcontainer.offsetHeight;


        if(defaultLayout.absolute){
            

            var retsize = setCanvasSize(canvaso, offsetWidth, offsetHeight);
            //setCanvasSize(canvaso, offsetWidth, offsetHeight);


            //canvaso.width = Math.floor(offsetWidth * scale);
            //canvaso.height = Math.floor(offsetHeight * scale);

            //canvaso.width = offsetWidth;
            //canvaso.height = offsetHeight;
            //canvasowidth = canvaso.width;
            //canvasoheight = canvaso.height;

            canvasowidth = retsize.awidth;// / scale;
            canvasoheight = retsize.aheight;// / scale;

            canvasowidthsize = retsize.swidth;
            canvasoheightsize = retsize.sheight;

            //canvas.style.width = offsetWidth + 'px';
            //canvas.style.height = offsetHeight + 'px';


        }else{
            
            //canvaso.width = window.innerWidth - offsetWidth;
            //canvaso.height = window.innerHeight - offsetHeight;
            //setCanvasSize(canvaso, window.innerWidth - offsetWidth, window.innerHeight - offsetHeight);

            retsize = setCanvasSize(canvaso, window.innerWidth - offsetWidth, window.innerHeight - offsetHeight);

            //canvaso.width = Math.floor((window.innerWidth - offsetWidth) * scale);
            //canvaso.height = Math.floor((window.innerHeight - offsetHeight) * scale);

            //canvas.style.width = window.innerWidth - offsetWidth + 'px';
            //canvas.style.height = window.innerHeight - offsetHeight + 'px';


            //canvasowidth = canvaso.width;
            //canvasoheight = canvaso.height;

            canvasowidth = retsize.awidth;// / scale;
            canvasoheight = retsize.aheight;// / scale;

            canvasowidthsize = retsize.swidth;
            canvasoheightsize = retsize.sheight;

                
        }
        
        if(docpagNotReady()){
            return;
        }

        if (documentopen && !DocObj.getCurrentpage().usevector3Dxml) {

            var curwidth = canvasowidth;
            //var curheight = canvasoheight;


            var scalefactor = (window.innerWidth - offsetWidth) / curwidth;

            /*if(DocObj.getCurrentpage().usevectorxml){
                //var tempdx = DocObj.getCurrentpage().dxvector;
                //var tempdy = DocObj.getCurrentpage().dyvector;
            }*/


            /*if(DocObj.getCurrentpage().usevectorxml){
                //DocObj.getCurrentpage().dxvector = tempdx;
                //DocObj.getCurrentpage().dyvector = tempdy;

            }*/

            if(documentopen && DocObj.getCurrentpage().usepdfjs){
                setSmoothingEnabled(false);
            }else{
                setSmoothingEnabled(false);
            }


        }



    }

    function doResize(binternal , offsetWidth, offsetHeight) {

       

        if (!canvaso) {
            //alert('Error: I cannot find the canvas element!');
            return;
        }

        resizeCanvas(offsetWidth, offsetHeight);

        setCanvasSize(canvimg, canvasowidthsize, canvasoheightsize);
        setCanvasSize(canvas, canvasowidthsize, canvasoheightsize);
        setCanvasSize(renderer.domElement, canvasowidthsize, canvasoheightsize);

        //canvimg.width = canvasowidth;
        //canvimg.height = canvasoheight;

        //canvas.width = canvasowidth;
        //canvas.height = canvasoheight;
        
        //renderer.domElement.width = canvasowidth;
        //renderer.domElement.height = canvasoheight;


        if(docpagNotReady()){
            if(!bwelcomedisabled){
                imagewidth = splashimage.naturalWidth;
                imageheight = splashimage.naturalHeight;
    
                var yscale = canvasoheight / imageheight;
                var xscale = canvasowidth / imagewidth;
                var dscale = Math.min(xscale, yscale);
    
                var dx = (canvasowidth - (imagewidth * dscale)) / 2;
                var dy = (canvasoheight - (imageheight * dscale)) / 2;
    
                context.clearRect(0, 0, canvas.width, canvas.height);
                cntximg.clearRect(0, 0, canvas.width, canvas.height);
                contexto.clearRect(0, 0, canvas.width, canvas.height);
    
                //numLayers = 0;
                draw_image(splashimage, dx, dy, dscale);

            }

        }else{
            if (documentopen && DocObj.getCurrentpage().usevector3Dxml) {
                //renderer.setSize(canvasowidth, canvasoheight);
                renderer.setSize(canvasowidthsize, canvasoheightsize);

    
                if (DocObj.getCurrentpage().camera) {
                    //DocObj.getCurrentpage().camera.aspect = canvasowidth / canvasoheight;
                    DocObj.getCurrentpage().camera.aspect = canvasowidthsize / canvasoheightsize;
                    DocObj.getCurrentpage().camera.updateProjectionMatrix();

                    if (bUsePostprocess){
                        //DocObj.getCurrentpage().Vector3DPageObj.effectComposer.setSize( canvaso.width, canvaso.height );
                        DocObj.getCurrentpage().Vector3DPageObj.effectComposer.setSize( canvasowidthsize, canvasoheightsize );
                    }
    
                }
    
            }

            DocObj.getCurrentpage().resize();
            
        }

        
        //RxCore_scrollBarCheck();

        if (binternal){
            if (RxCore_GUI_Resize != undefined) {
                var canvasSize = {
                    w:canvasowidth,
                    h:canvasoheight
                };
                RxCore_GUI_Resize.onResize(canvasSize);
            }
        }else{
            if (RxCore_GUI_Resize_ext != undefined) {
                var canvasSize = {
                    w:canvasowidth,
                    h:canvasoheight
                };
                RxCore_GUI_Resize_ext.onResize(canvasSize);
            }

        }
        
        /*window.setTimeout(function () {
            // run your actual function here
            RxCore.redrawCurrentPage();
        }, 100);*/

        

    }

    function resizeCanvasdirect(width, height) {


        if(defaultLayout.absolute){
            width = width || defaultLayout.offsetWidth;
            height = height || defaultLayout.offsetHeight;
        }else{
            return;
        }

        var retsize = setCanvasSize(canvaso, width, height);


        canvasowidth = retsize.swidth;// / scale;
        canvasoheight = retsize.sheight;// / scale;

        canvasowidthsize = retsize.awidth;
        canvasoheightsize = retsize.aheight;

        //canvaso.width = width;
        //canvaso.height = height;
        //canvasowidth = canvaso.width;
        //canvasoheight = canvaso.height;

        if(documentopen && DocObj.getCurrentpage().usepdfjs){
            setSmoothingEnabled(false);
        }else{
            setSmoothingEnabled(false);
        }


    }

    function doResizeEx(width, height){

        if (!canvaso) {
            //alert('Error: I cannot find the canvas element!');
            return;
        }

        resizeCanvasdirect(width, height);

        if(docpagNotReady()){
            if(!bwelcomedisabled){

                imagewidth = splashimage.naturalWidth;
                imageheight = splashimage.naturalHeight;
    
                var yscale = canvaso.height / imageheight;
                var xscale = canvaso.width / imagewidth;
                var dscale = Math.min(xscale, yscale);
    
                var dx = (canvaso.width - (imagewidth * dscale)) / 2;
                var dy = (canvaso.height - (imageheight * dscale)) / 2;
    
                context.clearRect(0, 0, canvas.width, canvas.height);
                cntximg.clearRect(0, 0, canvas.width, canvas.height);
                contexto.clearRect(0, 0, canvas.width, canvas.height);
    
                //numLayers = 0;
    
                draw_image(splashimage, dx, dy, dscale);
    
            }
        }else{
            if (documentopen && DocObj.getCurrentpage().usevector3Dxml) {
                renderer.setSize(canvaso.width, canvaso.height);
    
    
                if (DocObj.getCurrentpage().camera) {
                    DocObj.getCurrentpage().camera.aspect = canvaso.width / canvaso.height;
                    DocObj.getCurrentpage().camera.updateProjectionMatrix();
    
                    if (bUsePostprocess){
                        DocObj.getCurrentpage().Vector3DPageObj.effectComposer.setSize( canvaso.width, canvaso.height );
                    }
                }
    
            }
    
            DocObj.getCurrentpage().resize();
        }


        setCanvasSize(canvimg, canvasowidthsize, canvasoheightsize);
        setCanvasSize(canvas, canvasowidthsize, canvasoheightsize);
        setCanvasSize(renderer.domElement, canvasowidthsize, canvasoheightsize);


        //canvimg.width = canvaso.width;
        //canvimg.height = canvaso.height;

        //canvas.width = canvaso.width;
        //canvas.height = canvaso.height;

        //renderer.domElement.width = canvaso.width;
        //renderer.domElement.height = canvaso.height;


    }    

    function RxCore_hideTextInput() {
        if (RxCore_GUI_TextInput != undefined) {
            if(DocObj && DocObj.selectedmarkup && DocObj.selectedmarkup.edit){
                RxCore_GUI_TextInput.operation.edit = false;
                RxCore_GUI_TextInput.operation.start = false;
                RxCore_GUI_TextInput.operation.create = false;
                RxCore_GUI_TextInput.operation.save = true;
                RxCore_GUI_TextInput.operation.markup = DocObj.markuplist[DocObj.selectedmarkup.id];

                var rect = canvas.getBoundingClientRect();
                var xrect  = DocObj.markuplist[DocObj.selectedmarkup.id].rotatedrect.x + rect.left;
                var yrect = DocObj.markuplist[DocObj.selectedmarkup.id].rotatedrect.y + rect.top;

                //var xrect  = DocObj.markuplist[DocObj.selectedmarkup.id].xscaled + rect.left;
                //var yrect = DocObj.markuplist[DocObj.selectedmarkup.id].yscaled + rect.top;

                var wrect = DocObj.markuplist[DocObj.selectedmarkup.id].wscaled;
                var hrect = DocObj.markuplist[DocObj.selectedmarkup.id].hscaled;

                var txtrect = new Rectangle(xrect,yrect,wrect,hrect);
                txtrect.rotation = DocObj.getCurrentpage().getpageRotation();

                var pixelRatio = getPixelRatio();
                //txtrect.x = txtrect.x / pixelRatio;
                //txtrect.y = txtrect.y / pixelRatio;

                txtrect.x = ((txtrect.x - rect.left) / pixelRatio) + rect.left;
                txtrect.y = ((txtrect.y - rect.top) / pixelRatio) + rect.top;

                txtrect.w = txtrect.w / pixelRatio;
                txtrect.h = txtrect.h / pixelRatio;


                RxCore_GUI_TextInput.setTextInput(txtrect);


                DocObj.markuplist[DocObj.selectedmarkup.id].selectedit = false;
                DocObj.markuplist[DocObj.selectedmarkup.id].setselect(false);

                DocObj.selectedmarkup.edit = false;
                //DocObj.setMarkupMultiselected();

            }
        }
    }

    function RxCore_documentOpened() {
        return (typeof documentopen !== 'undefined' && documentopen);
    }

   
    function RxCore_getFileInfo(callback) {
        RxCore_GUI_FileInfo.callback = callback;
        RxCore_FileInfodialog();
        return RxCore_GUI_FileInfo.fileinfo;
    }

    function RxCore_markUpSelected(callback) {
        if (callback && typeof callback === "function") {
            RxCore_GUI_Markup.connect(callback);
        }
        //markUpSelected
        //RxCore_GUI_Markup.callback = callback;
        //RxCore_GUI_Markup.
    }

    function RxCore_onTextPlaced(callback) {
        RxCore_GUI_Textdiag.callback = callback;
    }

    function RxCore_setText(text) {
        RxCore_GUI_Textdiag.setText(text);
    }

    function RxCore_getTextInput(callback) {

        if (callback && typeof callback === "function") {
            connect(callback);
        }


        function connect(callback) {
            RxCore_GUI_TextInput.connect(callback);
        }

        function getText() {
            return RxCore_GUI_TextInput.getText().text
        }

        function setText(text) {
            RxCore_GUI_TextInput.setText(text);
        }

        return {
            getText: getText,
            setText: setText,
            connect: connect
        }
    }

    function RxCore_getNoteDiag(callback) {

        if (callback && typeof callback === "function") {
            connect(callback);
        }


        function connect(callback) {
            RxCore_GUI_Notediag.connect(callback);
        }

        function getText() {
            return RxCore_GUI_Notediag.getText().text;
        }

        function setText(text) {
            RxCore_GUI_Notediag.setText(text);
        }

        return {
            getText: getText,
            setText: setText,
            connect: connect
        }
    }

	function RxCore_getTextInputText() {
		return RxCore_GUI_TextInput.getText().text;
    }
    
    function RxCore_getTextMarkup(){
        return RxCore_GUI_TextInput.getText();
    }

	
	function RxCore_setTextInputText(text) {
		return RxCore_GUI_TextInput.setText(text);
	}

    function RxCore_onNotePlaced(callback) {
        RxCore_GUI_Notediag.callback = callback;
    }

    function RxCore_setNoteText(text) {
        RxCore_GUI_Notediag.setText(text);

        if(RxCore_GUI_MarkupChanged != undefined){
            //RxCore_GUI_MarkupChanged

            //var operation = markupobject.editaction;
            //RxCore_GUI_MarkupChanged.setMarkupSelected(markupobject, operation);


        }

    }

    function RxCore_ScaleonResize(toggle) {
        bDoReScaleOnSize = toggle;
    }
	
	function RxCore_changeTextColor(color) {
		if(bCanChangeLayer){
            RxCore_ChangeColor(color, 2);
        }

	}

    function RxCore_changeFillColorByIndex(indx,color) {
        RxCore_ChangeColorByIndex(indx,color, 0);
    }



	function RxCore_changeFillColor(color) {
		RxCore_ChangeColor(color, 0);
	}
	
	function RxCore_changeStrokeColor(color) {
        if(bCanChangeLayer){
            RxCore_ChangeColor(color, 1);
        }

	}

    function RxCore_printhelper() {
        return {
            areaUnitlabel:AreaUnitlabel,
            bMarkupNoLabel : bMarkupNoLabel,
            compareObj:CompareObj,
            docObj:DocObj,
            bdirectPDF : DocObj.directPDF,
            onPrintpage : RxCore_GUI_printpage,
            onPrintCompare : RxCore_GUI_printCompare,
            documentcompare:documentcompare,
            getDisplayName:GetDisplayName,
            getHatch : getHatch,
            getPageObject:GetPageObject,
            getPaperHeight:GetPaperHeight,
            getPaperWidth:GetPaperWidth,
            getPrintRect: GetPrintRect,
            markupprintlist:markupprintlist,
            measureScale:MeasureScale,
            printfocus:printfocus,
            stampList:Stamplist,
            Userlist : Userlist,
            unitLabel:Unitlabel,
            unitOfMeasure:Unitofmeasure,
            unitscale:unitscale,
            getUnitArea: getUnitArea,
            xmlurlrel:xmlurlrel,
            bUseCustomrelpath : bUseCustomrelpath,
            xmlurlrelcustom : xmlurlrelcustom,
            noteimage : noteimage,
            bUseFixedScale : bUseFixedScale,
            fixedScale : DocObj.getCurrentpage().fixedScale
        };

    }

    function RxCore_toggleShowHighlightMarkups(value) {
        if (docpagNotReady()) {
          return;
        }
        if (documentcompare) {
          return;
        }
        DocObj.foxitsearchobj.wholedocument = value;
      }


    function RxCore_getReadOnly(){
        return readonlymode;
    }

    function RxCore_helper() {
        return {
            getUnitLabel:getUnitLabel,
            getAreaUnitLabel:getAreaUnitLabel,
            getLength:getLength

        };

        function getUnitLabel() {

            
            //return DocObj.getCurrentpage().Unitlabel;
            return DocObj.getCurrentpage().scaleObject.AreaUnitlabel;
        }

        function getAreaUnitLabel() {
            return DocObj.getCurrentpage().AreaUnitlabel;
        }


        function getLength(length) {



            var res;

            res = getUnitlength(length / getCurrentPageScale());
            /*if (DocObj.getCurrentpage().usevectorxml) {
                res = getUnitlength(length / DocObj.getCurrentpage().dscalevector);
            } else if (DocObj.getCurrentpage().usepdfjs) {
                res = getUnitlength(length / (DocObj.getCurrentpage().curpagescale * DocObj.getCurrentpage().dscalepdf));
            } else {
                res = getUnitlength(length / DocObj.getCurrentpage().dscale);
            }*/
            return res;
        }
    }

    /* Andriy */
    /* Init GUI Connectors */
    RxCore_GUI_ActivateFile = new Rx_ActiveFileConn();

    RxCore_GUI_2DBlock = new Rx_2DBlockConn();
    RxCore_GUI_2DBlockHover = new Rx_2DBlockConnHover();

    

    RxCore_GUI_2DBlockID = new Rx_2DBlockConn();
    RxCore_GUI_2DBlockInfo = new Rx_2DBlockAttributeConn();
    RxCore_GUI_2DBlockInfoScreen = new Rx_2DBlockAttributeConnScreen();

    RxCore_GUI_2DEntityInfoScreen = new Rx_2DBlockAttributeConnScreen();
    RxCore_GUI_2DEntityInfo = new Rx_2DBlockAttributeConnScreen();
    

    Rxcore_GUI_3DCameraSave = new Rx_3DCameraSave();

    Rxcore_GUI_CanSaveTop3DView = new Rx_3DPartsReadComplete();


    Rxcore_GUI_3DPartInfo = new Rx_3DPartInfoConn();

    Rxcore_GUI_3DPartState = new Rx_3DPartInfoConn();
    
    Rxcore_GUI_3DMarkupInfo = new Rx_3DPartInfoConn();

    RxCore_GUI_RenderComplete = new Rx_RenderComplete();

    RxCore_GUI_CreationComplete = new Rx_GUIContentConn();

    RxCore_GUI_3DParts = new Rx_3DPartsConn();
    RxCore_GUI_Read3DComplete = new Rx_3DPartsReadComplete();
    RxCore_GUI_3DWalkthrough = new Rx_Walkthrough3DConn();
    RxCore_GUI_Calibratediag = new Rx_CalibrateConn();
    RxCore_GUI_CalibrateComplete = new Rx_CalibrateComplete();
    RxCore_GUI_CompareDiag = new Rx_CompareDialogConn();
    RxCore_GUI_CameraPos = new Rx_CameraPosEventConn();
    RxCore_GUI_UpdatebirdsEye = new Rx_BirdsEyeDrawEventConn();
    RxCore_GUI_Nav3DreplaceDiag = new Rx_CompareDialogConn();

    RxCore_GUI_CompareAlign = new Rx_CompareAlignConn();
    RxCore_GUI_CompareMeasure = new Rx_CompareMeasureConn();

    RxCore_GUI_Consolidate = new Rx_ConsolidateConn();

    RxCore_GUI_ConsolidateMarkup = new Rx_ConsolidateConnMarkup();
    
    RxCore_GUI_Permissions = new Rx_PermissionsConn();

    RxCore_GUI_TextCopied = new Rx_TextCopyConn();

    RxCore_GUI_PDFBookmarks = new Rx_GUIPDFBookmarsConn();
    RxCore_GUI_Download = new Rx_GUIDownload();
    
    Rxcore_GUI_exportComplete = new Rx_FileLoadComplete();

    RxCore_GUI_Servercompare = new Rx_FileLoadComplete();


    RxCore_GUI_FileInfo = new Rx_FileInfoConn();

    RxCore_GUI_ContextMenuEvt = new Rx_CanvasContext();

    RxCore_GUI_CloseDocInternal = new Rx_FileCloseComplete();

    RxCore_GUI_GuidRecieved = new Rx_GUIDreceived();


    Rxcore_GUI_fileLoadComplete = new Rx_FileLoadComplete();

    Rxcore_GUI_fileLoadError = new Rx_FileLoadComplete();

    Rxcore_GUI_search3Denabled = new Rx_FileLoadComplete();

    RxCore_GUI_FileActivated = new Rx_FileLoadComplete();

    RxCore_GUI_compareLoadComplete = new Rx_CompareCreated();

    Rxcore_GUI_floorLoadComplete = new Rx_FloorLoadComplete();
    RxCore_GUI_floorlevel = new Rx_FloorLevelConn();
    RxCore_GUI_3DNavPosition = new Rx_floorClickPosEvent();


    Rxcore_GUI_pagedimLoadComplete = new Rx_FileLoadComplete();
    Rxcore_GUI_markupLoadComplete = new Rx_MarkupLoadComplete();

    RxCore_GUI_SVGLoad = new Rx_MarkupSVGLoadConn();

    RxCore_GUI_MarkupHover = new Rx_GUIMarkupHoverConn();

    Rxcore_GUI_foxitmarkupLoadComplete = new Rx_MarkupLoadComplete();

    RxCore_GUI_Markup = new Rx_GUIMarkupConn();

    RxCore_GUI_MarkupIndex = new Rx_GUIMarkupConn();

    RxCore_GUI_3DMarkup = new Rx_GUIMarkupConn();

    RxCore_GUI_MarkupChanged = new Rx_GUIMarkupConn();
    

    RxCore_GUI_3DCameraSaves = new Rx_PageThumbnailsConn();
    RxCore_GUI_3DView = new Rx_GUI3DViewConn();

    Rxcore_GUI_SymbolAddFailed = new Rx_MarkupSave();
    //Rx_MarkupSave

    RxCore_GUI_MarkupLayers = new Rx_MarkupLayerConn();
    RxCore_GUI_Markuplist = new Rx_MarkupListConn();

    RxCore_GUI_MarkuplistJSON = new Rx_MarkupListConn();
    
    RxCore_GUI_FoxitAnnotlist = new Rx_MarkupListConn();
    
    RxCore_GUI_3DMarkupList = new Rx_Markup3DListConn();

    RxCore_GUI_MarkupLink = new Rx_GUIMarkupLinkConn();

    RxCore_GUI_MarkupDrawValue = new Rx_GUIMarkupdrawValConn();

    //Rx_GUIMarkupdrawValConn

    RxCore_GUI_MarkupUnselect = new Rx_GUIMarkupUnselectConn();
    RxCore_GUI_Measurediag = new Rx_MeasureConn();
    RxCore_GUI_Notediag = new Rx_NotediagConn();
    Rxcore_GUI_pageLoadComplete = new Rx_PageLoadComplete();

    Rxcore_GUI_scaleListLoadComplete = new Rx_ScaleListLoadComplete();

    Rxcore_GUI_PDFRenderComplete = new Rx_PDFRenderComplete();
    Rxcore_GUI_PDFPageRenderComplete = new Rx_PDFRenderComplete();

    Rxcore_GUI_PDFPassWord = new Rx_PDFPassWordHandler();

    RxCore_GUI_pagethumbs = new Rx_PageThumbnailsConn();
    
    RxCore_GUI_floorthumb = new Rx_PageThumbnailConn();
    RxCore_GUI_FloorplanCanvas = new Rx_FloorplanCanvasConn();


    RxCore_GUI_pagethumb = new Rx_PageThumbnailConn();
    RxCore_GUI_birdseye = new Rx_PageBirdseyeConn();
    RxCore_GUI_Resize = new Rx_CanvasResize();
    RxCore_GUI_Resize_ext = new Rx_CanvasResize();
    RxCore_GUI_Stamps = new Rx_MarkupStampsConn();
    RxCore_GUI_CustomStamps = new Rx_CustStampGUI();
    RxCore_GUI_Symbols = new Rx_SymbolGUI();
    RxCore_GUI_State = new Rx_GUIStateConn();
    RxCore_GUI_Page = new Rx_GUIPageConn();
    RxCore_GUI_PagePosition = new Rx_PanUpdate();
    RxCore_GUI_PanUpdate = new Rx_PanUpdate();
    RxCore_GUI_PanUpdated = new Rx_PanUpdate();
    RxCore_GUI_Textdiag = new Rx_TextdiagConn();
    RxCore_GUI_TextInput = new Rx_TextInputConn();
    RxCore_GUI_Upload = new Rx_GUIUpload();

    RxCore_GUI_UndoRedoEmpty = new Rx_GUIUndoListEmpty();

    RxCore_GUI_Users = new Rx_UserlistConn();

    RxCore_GUI_penTable = new Rx_PenTableListConn();

    RxCore_GUI_penTableJSON = new Rx_PenTableListConn();

    
    RxCore_GUI_getsignatureComplete = new Rx_getSignatureConn();
    RxCore_GUI_putsignatureComplete = new Rx_putSignatureConn();


    RxCore_GUI_VectorBlocks = new Rx_VectorBlocksConn();
    RxCore_GUI_VectorBlocksLoaded = new Rx_VectorBlocksConn();
    RxCore_GUI_VectorLayers = new Rx_VectorLayersConn();
    RxCore_GUI_ZoomUpdate = new Rx_ZoomUpdate();
    RxCore_GUI_ZoomUpdated = new Rx_ZoomUpdate();

    
    RxCore_GUI_PrintRect = new Rx_docPrintRect();

    RxCore_GUI_ClipRect = new Rx_docPrintRect();

    RxCore_GUI_MarkupSave = new Rx_MarkupSave();

    RxCore_GUI_MarkupPaths = new Rx_MarkupPath();


    RxCore_GUI_Ready = new Rx_Ready();
    RxCore_GUI_FoxitReady = new Rx_Ready();
    RxCore_GUI_RotatePage = new Rx_RotateEvent();
    RxCore_GUI_RotateDocument = new Rx_RotateEvent();
    RxCore_GUI_HasText = new Rx_GUIhasText();
    RxCore_GUI_NumMathces = new Rx_GUITextFound();
    RxCore_GUI_NumMathcesMarkup = new Rx_GUITextFound();
    RxCore_GUI_NumMathcesRect = new Rx_GUITextFound();


    RxCore_GUI_DocumentSearch = new Rx_GUIDocumentSearch();
    RxCore_GUI_RemovePage = new Rx_GUIRemovePage();
    RxCore_GUI_PageCountChanged = new RxCore_GUIPageCountChanged();


    RxCore_GUI_HatchChange = new RxCore_GUIHatchChange();
    RxCore_GUI_inputEvent = new RxCore_GUIinput();

    RxCore_GUI_MathcesRectsPage = new Rx_GUITextFound();

    RxCore_GUI_printpage = new Rx_PrintPageConn();
    RxCore_GUI_printCompare = new RxPrintCompareConn();
    RxCore_GUI_markupdrawParams = new Rx_MarkupDrawEvent();
    RxCore_GUI_MarkupAreaEdit = new Rx_MarkupDrawEvent();
    RxCore_GUI_markupParamsError = new Rx_MarkupDrawErrorEvt();
    RxCore_GUI_getResponsHeaders = new Rx_GUIResponseHeaders();

    RxCore_GUI_MarkupMeasureRealTimeData = new Rx_GUIMarkupMeasureRealTimeDataConn();


    return {
        abortPDFload : RxCore_AbortPDFload,
        add3Dsymbol : RxCore_add3Dsymbol,
        addAnnotationFoxit : RxCore_addAnnotationFoxit,
        addGltftoDoc : RxCore_addGltftoDoc,

        addSignature : Rxcore_AddSignature,

        updateScaleList : RxCore_updateScaleList,
        getDocScales : RxCore_getDocScales,
        allowPolygonMove : RxCore_AllowPolygonMove,
        autoHyperlink : RxCore_autoHyperlink,
        measure3D : RxCore_measure3D,
        create3DTexture : RxCore_create3DTexture,
        addMarkup : addMarkup,
        addHoverforBlock : RxCore_addHoverSpaceforBlock,
        alignCompare : RxCore_alignCompare,
        alignCompareEx : RxCore_alignCompareEx,
        appendCustomBlockAttribute : RxCore_AppendCustomBlockAttribute,
        animateLineStyle : RxCore_animateLineStyle,
        applyAngleLength : RxCore_applyAngleLength,
        applyAngleLengthSelected : RxCore_applyAngleLengthSelected,
        applyMarkup : RxCore_ApplyMarkup,
        applyRadiusSelected : RxCore_applyRadiusSelected,
        applyWidthHeight : RxCore_applyWidthHeight,
        applyWidthHeightSelected : RxCore_applyWidthHeightSelected,
        attributeMask : RxCore_AttributeMask,
        autoLoadThumbnails : RxCore_AutoLoadThumbnails,
        backgroundColor :Rxcore_BackgroundColor,
        backgroundCustomColor :RxCore_BackgroundCustomColor,

        blockhoverevent : RxCore_blockhoverevent,

        blockLoadMask : RxCore_BlockLoadMask,
        calibrate:RxCore_Calibrate,
        canvToImage : RxCore_canvToImage,
        change3DVectorBlock:RxCore_Change3DVectorBlock,
        set3DBlockState : RxCore_set3DBlockState,
        set3DBlockStateAll : RxCore_set3DBlockStateAll,
        changeFillColor:RxCore_changeFillColor,
        changeFillColorByIndex:RxCore_changeFillColorByIndex,
        changeMarkupLayer : RxCore_ChangeMarkupLayer,
        changeSnapState:RxCore_changeSnapState,
        changeStrokeColor:RxCore_changeStrokeColor,
        changeTextColor:RxCore_changeTextColor,						
        changeColor:RxCore_ChangeColor,
    
        clearMarkupComments : RxCore_ClearMarkupComments,
    

        changeColorAll : RxCore_ChangeColorAll,

        changeColorMarkup:RxCore_ChangeColorMarkup,
        changeLabelTransp : RxCore_changeLabelTransp,
        changeTransp:RxCore_ChangeTransp,
        changeTranspMarkup : RxCore_ChangeTranspMarkup,
        changeVectorBlock:RxCore_ChangeVectorBlock,
        changePDFLayer : RxCore_ChangePDFLayer,
        changeVectorLayer:RxCore_ChangeVectorLayer,
        checknewDoc : RxCore_checknewDoc,
        checkLargePDF : RxCore_checkLargePDF,
        clearMarkup : RxCore_ClearMarkup,

        clearHover : RxCore_clearHoverSpace,

        clipping3D : RxCore_3dClipping,
        closeAll :RxCore_CloseAll,

        closeDocument:RxCore_Closedocument,
        closeDocumentEx:RxCore_ClosedocumentEx,

        compare:RxCore_Compare,

        compareEx: RxCore_CompareEx,

        compareDialog : RxCore_Comparediag,
        compareFiles:RxCore_CompareFiles,

        compareOverlayServer : compareOverlayServer,

        compareOverlayServerJSON : compareOverlayServerJSON,

        compareOverlaySaveTo : compareOverlaySaveTo,

        compareMeasure : RxCore_CompareMesure,
        compareNudgeOffset : RxCore_CompareNudgeOffset,
        compareNudgeRotate : RxCore_CompareNudgeRotate,
        compareNudgeScale : RxCore_CompareNudgeScale,
        compareOffset : RxCore_CompareOffset,
        compareScale :RxCore_compareScale,
        consolidate : RxCore_consolidate,
        consolidatedOnly : RxCore_ConsolidatedOnly,
        //getConsolidateObj : RxCore_consolidateObj,
        copyMarkUp:RxCore_CopyMarkup,
        cutMarkUp : RxCore_CutMarkup,
        create3Dfloorplan : RxCore_Create3Dfloorplan,
        get3dFloorplanCanvas : RxCore_get3dFloorplanCanvas,
        create3DfloorplanfromPage : RxCore_Create3DfloorplanfromPage,
        create3DfloorplanfromCanvas : RxCore_Create3DfloorplanfromCanvas,
        replacefloorplan : RxCore_replacefloorplan,
        replacefloorplanfromPage : RxCore_replacefloorplanfromPage,

        createAreaAttributes : RxCore_createAreaAttributes,

        create3DMarkup : RxCore_create3DMarkup,

        createCustomStamps : createCustomStamps,

        createHatchStyles : RxCore_CreateHatchStyles,

        
        
        createServerContent : RxCore_CreateServerContent,

        gethatchimagebyType : RxCore_gethatchimagebyType,
        

        createTempcanvArray : RxCore_createTempcanvArray,
        createFoxitDoc : createFoxitDoc,
        createPageTileArray : RxCore_createPageTileArray,
        setfoxitPageSize : RxCore_setfoxitPageSize,
        setfoxitPageSizeEx : RxCore_setfoxitPageSizeEx,
        foxitcalibratepagePosY : RxCore_foxitcalibratepagePosY,
        foxitcalibratepagePosX : RxCore_foxitcalibratepagePosX,
        foxitcalibratepagemarkup : RxCore_foxitcalibratepagemarkup,
        foxitcalibratepagemarkupEx : RxCore_foxitcalibratepagemarkupEx,
        foxitonScrollupdate : RxCore_foxitonScrollupdate,
        createTextRect : RxCore_CreateTextRect,

        createImageMarkup : RxCore_createImageMarkup,

        createSpaceFromPolygon : RxCore_CreateSpaceFromPolygon,
        createSpacefromBlock : RxCore_createSpacefromBlock,
        deleteMarkUp:RxCore_DeleteMarkup,

        deletemarkupbyID : deletemarkupbyID,

        deleteVectorAttribute : RxCore_deleteVectorAttribute,

        change3DMarkup : RxCore_change3DMarkup,
        change3DMarkupSize : RxCore_change3DMarkupSize,
        
        deleteCamera : RxCore_deleteCamera,

        deleteMarkupbyGUID : deleteMarkupbyGUID,
        deletePoint : RxCore_DeletePoint,
        
        disablewelcome : RxCore_disablewelcome,
        
        forceUniqueMarkup : RxCore_forceUniqueMarkup,

        displayFloorplan : RxCore_displayFloorplan,
        documentOpened:RxCore_documentOpened,
        doResize:doResize,
        
        doResizeEx : doResizeEx,

        downloadSignature : downloadSignature,

        drawPoints : RxCore_DrawPoints,
        drawCanvas : RxCore_drawextcanvas,
        drawmarkupToCanvas : drawmarkupToCanvas,
        //drafoxitPages : RxCore_drafoxitPages,
        getPDFintialScale : RxCore_getinitPDFScale,
        getmarkupbyNumber : RxCore_GetmarkupbyNumber,

        endMarkupTextSearch : RxCore_endMarkupTextSearch,
        endTextSearch :RxCore_endTextSearch,
        endGetTextRects : RxCore_endGetTextRects,
        enableCamNavDraw : RxCore_enableCamNavDraw,

        enableZoomOnScrollMultipage : RxCore_EnableZoomOnScrollMultipage,

        forcetextsearchmarker : RxCore_forcetextsearchmarker,
        getTextRects : RxCore_getTextRects,
        
        exicompareMeasure : RxCore_exicompareMeasure,
        explode3D:RxCore_3DExplode,
        explode3DDistance:RxCore_3DExplodedistance,
        exportFile : RxCore_ExportFile,

        setDefultExportparams : RxCore_setDefultExportparams,

        exportFilePage : RxCore_ExportFilePage,
        exportFileRaster : RxCore_ExportFileRaster,
        exportPDF:RxCore_PDFExport,
        exportFDF : RxCore_exportFDF,
        exportFilePrintPDF : RxCore_ExportFilePrintPDF,
        foxitexportFDF : RxCore_foxitexportFDF,
        exportPDFBinary : RxCore_PDFExportBinary,
        exportSettings : RxCore_exportSettings,
        fileInfoDialog:RxCore_FileInfodialog,
        fileSelected:fileSelected,
        findBlockByAttr : RxCore_FindBlockByAttr,
        findMarkUp:RxCore_FindMarkup,
        forceMarkupCallback : RxCore_ForceMarkupCallback,
        
        foxitAnnotlist : RxCore_FoxitAnnotlist,


        convertPDFAnnots : RxCore_convertPDFAnnots,
        usePDFAnnotProxy : RxCore_usePDFAnnotProxy,

        
        foxitForceCanvas : RxCore_foxitForceCanvas,
        foxitForceRedraw : RxCore_foxitForceRedraw,
        foxitpagescalecheck : RxCore_foxitpagescalecheck,

        
        forcepagesState : RxCore_forcepagesState,
        freeLicense : FreeLicense,
        //get3DBlocks:RxCore_get3DBlocks,
        //get3DInfo:RxCore_get3DInfo,
        //getAnnotations:RxCore_getAnnotations,
        get2DVectorBlocks : RxCore_get2DVectorBlocks,
        get3DVectorBlocks : RxCore_get3DVectorBlocks,
        get2DVectorfilterAttributes : RxCore_get2DVectorfilterAttributes,
        get2DVectorPentable : RxCore_get2DVectorPentable,
        getActiveFileFloorlevel : RxCore_getActiveFileFloorlevel,
        getAllPageDimensions : RxCore_getAllPageDimensions,
        getBlockAttributes : RxCore_getBlockAttributes,
        getBookMarkPage : RxCore_getBookMarkPage,
        getCacheURL : RxCore_getCacheURL,

        getCameraimage : RxCore_getCameraimage,
        
        getCameraView : RxCore_getCameraView,
        get3DResetView : RxCore_get3DResetView,

        getCurrentPageScale : getCurrentPageScale,
        getCurrentPageOffset : getCurrentPageOffset,


        getCanvasContextMenuEvent : RxCore_getCanvasContextMenuEvent,
        getCanvasFont : RxCore_getCanvasFont,


        getCanvasSize : RxCore_GetCanvasSize,
        getColor : RxCore_GetColor,
        getCompareColors : RxCore_GetCompareColors,
        getcurPage : RxCore_GetcurPage,

    

        getfloorplanDocs : RxCore_getfloorplanDocs,
        getFloorplanList : RxCore_getFloorplanList,
        getPageScale : RxCore_getPageScale,

        getPage : RxCore_GetPage,
        getPageScaleObject : RxCore_getPageScaleObject,

        getDisplayName : GetDisplayName,
        getdivcontainer : RxCore_getdivcontainer,
        getDocFileName : RxCore_getDocFileName,
        getPDFDocobj : RxCore_getPDFDocobj,
        getdocInfo :RxCore_GetdocInfo,

        pasteClipboard : RxCore_imageFromClipboard,

        getClipRect : RxCore_getClipRect,
        getDocRect : RxCore_getDocRect,

        getCamNavPosition :RxCore_getCamNavPosition,
        getcentrepoint : getcentrepoint,
        getFoxitIframeID : RxCore_getFoxitIframeID,
        bringIframeToFront : RxCore_bringIframeToFront,
        getFillColor:RxCore_GetfillColor,
        getFillStyle :RxCore_GetFillstyle,
        getFixedScale : RxCore_GetFixedScale,
        getFont :RxCore_GetFont,
        getfoxitThumbnails : RxCore_getfoxitThumbnails,
        getfoxitThumbnail : RxCore_getfoxitThumbnail,
        getFileInfo :RxCore_getFileInfo,

        getLayout : RxCore_getLayout,

        getLineColor:RxCore_GetlineColor,
        getLineWidth:RxCore_GetLineWidth,

        getMarkupTypes : RxCore_getMarkupTypes,

        getMarkupTypebyname : RxCore_getMarkupTypebyname,
        
        getMarkupLayers : RxCore_GetMarkupLayers,

        getmarkupGUIDs : RxCore_getmarkupGUIDs,

        getMarkupProperties : RxCore_getMarkupProperties,

        getMarkupType : RxCore_getMarkupType,
        

        getMarkupControlProperties : getMarkupControlProperties,

         /* olek new functions  */
        toggleShowHighlightMarkups: RxCore_toggleShowHighlightMarkups,
        
        documentTextSearch: RxCore_documentTextSearch,
        clearDocumentTextSearch: RxCore_clearDocumentTextSearch,

        markVectorBlock : RxCore_markVectorBlock,

        markupDisplayOnload : RxCore_markupDisplayOnload,

        markupTextWithOrange: RxCore_markupTextWithOrange,
        removePage: RxCore_removePage,
        movePageTo: RxCore_movePageTo,

        downloadPDF : RxCore_downloadPDF,
        extractPage: RxCore_extractPage,
        insertBlankPages: RxCore_insertBlankPages,
        getDPI: RxCore_getDPI,
        importPages: RxCore_importPages,
        copyPage: RxCore_copyPage,
        pastePage: RxCore_pastePage,
        getAllThumbnailsFromFile: RxCore_getAllThumbnailsFromFile,
        getCurrentFileInfo: RxCore_getCurrentFileInfo,
        zoomWithMarkup: RxCore_zoomWithMarkup,
        /* olek new functions  */


        /* upload modified PDF  */

        uploadPDF : RxCore_uploadPDF,
        uploadPDFCustom : RxCore_uploadPDFCustom,
        uploadCustomPDF : RxCore_uploadCustomPDF,

        /* upload modified PDF  */

        getMarkupXMLData : RxCore_getMarkupXMLData,
        getmarkupobjByGUID : RxCore_getmarkupobjByGUID,
        getMarkupSearchPadding :RxCore_getMarkupSearchPadding,
        getMarkupXMLByGUID : RxCore_getMarkupXMLByGUID,
        getMarkupXMLSelected : RxCore_getMarkupXMLSelected,
        getNoteDiag:RxCore_getNoteDiag,
        getnumSymbols : RxCore_getnumSymbols,
        getOpenFiles : RxCore_GetOpenFiles,
        getOpenFilesList : RxCore_getOpenFilesList,
        getOriginalPath : RxCore_getOriginalPath,
        get3DmodelcurFloor : RxCore_get3DmodelcurFloor,
        getModelCenter : RxCore_getModelCenter,
        getModelBox : RxCore_getmodelBox,
        getmodelUnit : RxCore_getmodelUnit,
        getPageDimensions : RxCore_getPageDimensions,
        getPageRotation : RxCore_getPageRotation,

        getCurrentVectorPageObject : getCurrentVectorPageObject,

        getpagethumbnail : RxCore_getpagethumbnail,
        getCurrentPageScaleValue : RxCore_getCurrentPageScaleValue,

        getCurrentPageScaleLabel : RxCore_getCurrentPageScaleLabel,

        getReadOnly : RxCore_getReadOnly,
        getAllSelectedMarkup : RxCore_getAllSelectedMarkup,
        getSelectedMarkup : RxCore_getSelectedMarkup,
        getLastMarkup : RxCore_getLastMarkup,
        getSnapState :RxCore_getSnapState,
        
        getStoreyNames : RxCore_getStoreyNames,
        getStoreyAttributes : RxCore_getStoreyAttributes,
        getStoreyAttributesEx : RxCore_SearchStoreyAttributesEx,
        getStoreyIDs : RxCore_getStoreyIDs,
        getStoreyRealIDs : RxCore_getStoreyRealIDs,
        getSymbolLibNum : RxCore_getSymbolLibNum,
        getSymbolLibPNGData : RxCore_getSymbolLibPNGData,
        getSymbolName : RxCore_getSymbolName,
        getTextColor:RxCore_GetTextColor,

        getUnit : RxCore_getUnit,

        getUser : RxCore_getUser,
        getUsers : RxCore_GetUsers,
        
        gotoPage : RxCore_gotoPage,

        foxitPageEvt : RxCore_foxitPageEvt,

        testScroll : RxCore_testScroll,
        //getGUIState:RxCore_getGUIState,
        getHatchStyle:RxCore_GetHatchStyle,
        getHatchbyName : getHatchbyName,

        getHatches : getHatches,

        //getThumbnails:RxCore_getThumbnails,
        getTextInput:RxCore_getTextInput,
        helper:RxCore_helper,
        handlePDFPassword : RxCore_handlePDFPassword,
        hidedisplayCanvas : RxCore_hidedisplayCanvas,
        hideAllCanvas : RxCore_hideAllCanvas,
        hideMarkUp:RxCore_HideMarkup,
        hideMarkupByMarkupNumbers : RxCore_hideMarkupByMarkupNumbers,
        hideLabels : RxCore_hideLabels,
        hideTextInput: RxCore_hideTextInput,
        imperialUnit:RxCore_ImperialUnit,
        elementImperialUnit: RxCore_ElementImperialUnit,
        importFDF : RxCore_importFDF,
        listFoxitAnnots : RxCore_listFoxitAnnots,
        initialize:initialize,
        initFoxit : initFoxit,
        insertPoint : RxCore_InsertPoint,
        instanceReset : instanceReset,
        keepvectorColor :RxCore_KeepvectorColor,

        limitZoomOut : RxCore_limitZoomOut,

        loadIFCSpaces : RxCore_LoadIFCSpaces,
        loadThumbnails : RxCore_LoadThumbnails,
        loadThumbnail : RxCore_loadThumbnail,
        loadThumbnailPDF : RxCore_loadThumbnailPDF,
        lockMarkupbyGUID : RxCore_LockMarkupbyGUID,
        lockMarkup : RxCore_lockMarkup,

        loadMarkupJSON : RxCore_LoadMarkupJSON,

        magnifyGlass:RxCore_MagnifyGlass,

        movetoBack :RxCore_MoveToBack,
        movetoFront : RxCore_MoveToFront,

        toggleCycleSelect :RxCore_toggleCycleSelect,
    
        markupAddBlockText : RxCore_MarkupAddBlockText,
        markupAddMulti : RxCore_MarkupAddMulti,

        markupAngleSwitch : RxCore_markupAngleSwitch,

        markUpArea:RxCore_MarkupArea,

        markUpAreaHole : RxCore_MarkupAreaHole,

        markupAreaRect : RxCore_markupAreaRect,

        markupRectToAreaSwitch : RxCore_markupRectToAreaSwitch,

        markUpArrow:RxCore_MarkupArrow,

        measureArc : RxCore_MeasureArc,

        markupButton : RxCore_MarkupButton,
        markupButtonFromMatch : RxCore_markupButtonFromMatch,
        markupChanged : RxCore_MarkupChanged,
        markupCircle : RxCore_MarkupCircle,
        markupCount : RxCore_MarkupCounter,
        markUpDimension:RxCore_MarkupDimension,
        markupDisplay : RxCore_markupDisplay,
        markUpEdged:RxCore_MarkupEdged,
        markUpErase:RxCore_MarkupErase,
        markUpFilled:RxCore_MarkupFilled,
        markUpFreePen:RxCore_MarkupFreepen,
        markUpHatched:RxCore_MarkupHatched,
        markUpHighlight:RxCore_MarkupHighlight,

        markuphoverevent : RxCore_markuphoverevent,

        markUpLayerDialog:RxCore_MarkupLayerdialog,
        markupLine :RxCore_MarkupLine,
        markupLinkRect : RxCore_MarkupLinkRect,
        markupAngle : RxCore_MarkupAngle,
        markupMeasurePath : RxCore_MarkupMeasurePath,
        markupMeasureSegment : RxCore_MarkupMeasureSegment,
        markupMultiselect : RxCore_MarkupMultiselect,
        
        multiselect3Dpart : RxCore_Multiselect3Dpart,

        markUpNote:RxCore_MarkupNote,
        markUpOutlined:RxCore_MarkupOutlined,
        markUpPolycurve:RxCore_MarkupPolycurve,
        markUpPolyline:RxCore_MarkupPolyline,
        markuprevCloud : RxCore_MarkuprevCloud,

        markUpRedraw : RxCore_MarkupRedraw,
        redactRect : RxCore_RedactRect,
        markUpRotate:RxCore_MarkupRotate,
        markUpSave:RxCore_MarkupSave,

        markupGetJSON : RxCore_MarkupGetJSON,

        markupSaveJSON : RxCore_MarkupSaveJSON,

        setMarkupfromJSON : setMarkupfromJSON,

        setUniqueMarkupfromJSON : setUniqueMarkupfromJSON,

        addWatermarkToAllPages: addWatermark,
        removeWatermarkFromAllPages: removeWatermark,
        addWatermarkToPage : addWatermarkpage,
        removeWatermarkFromPage : removeWatermarkpage,

        //function (markupJSON, DocRef){

        setMarkup3DSavefile : RxCore_setMarkup3DSavefile,

        setMarkupCursor : RxCore_setMarkupCursor,

        setViews3DSavefile : RxCore_setViews3DSavefile,


        markupSaveCheck : RxCore_MarkupSaveCheck,
        markUpSelected:RxCore_markUpSelected,
        markUpShape:RxCore_MarkupShape,
        markupSpace : RxCore_MarkupSpace,
        markUpStamp:RxCore_StampMarkup,
        markupSymbol : Rxcore_MarkupSymbol,

        markupImageStamp : Rxcore_MarkupImageStamp,

        markupinteractiveStamp : Rxcore_MarkupmarkupinteractiveStamp,
        markupLink : Rxcore_MarkupLink,
        markUpSubType:RxCore_MarkupSubtype,
        markUpText:RxCore_MarkupText,
        markUpTextRect : RxCore_TextRect,
        markUpTextRectArrow : RxCore_TextRectArrow,
        markUpRedo : RxCore_MarkupRedo,        
        markUpUndo:RxCore_MarkupUndo,
        markUpUserDialog:RxCore_MarkupUserdialog,
        measure:RxCore_Measure,
        measuredistance : RxCore_Measuredistance,
        metricUnit:RxCore_MetricUnit,
        elementMetricUnit : RxCore_ElementMetricUnit,
        modifyMarkup : modifyMarkup,

        moveimageMarkup : RxCore_moveimageMarkup,

        moveLabelEnable : RxCore_moveLabelEnable,
        moveTo3DNavPos :RxCore_moveTo3DNavPos,
        moveTo3DPart :RxCore_MoveTo3DPart,
        mSIEeventCheck : RxCore_MSIEeventCheck,
        nav3Dreplacediag : RxCore_Nav3Dreplacediag,
        navigateBookmark : RxCore_navigateBookmark,
        nextMarkup:RxCore_NextMarkup,
        nextPage:RxCore_NextPage,
        noteFocus : RxCore_NoteFocus,

        noSelect3D : RxCore_NoSelect3D,
        //onCalibrateFinished:RxCore_onCalibrateFinished,
        //onMeasureFinished:RxCore_onMeasureFinished,

        onNotePlaced:RxCore_onNotePlaced,
        onTextPlaced:RxCore_onTextPlaced,
        openFile:RxCore_OpenFile,

        

        openFloorplanFile : RxCore_openFloorplanFile,
        openFileCustom : RxCore_OpenFileCustom,
        openFileLayouts : RxCore_OpenFileLayouts,
        openFileLayoutsEx : RxCore_OpenFileLayoutsEx,
        openFilePages : RxCore_OpenFilePages,
        openFilePagesPDFdoc : RxCore_OpenFilePagesPDFdoc,
        openFileSilent : RxCore_OpenFileSilent,
        open3DFloorplan : RxCore_Open3DFloorplan,
        open3DFloorplanPage : RxCore_open3DFloorplanPage,
        openFileGLTF : RxCore_openFileGLTF,
        openMarkup : RxCore_OpenMarkup,
        openRecent:RxCore_OpenRecent,
        orbit3D:RxCore_3DOrbit,
        overlayFiles : RxCore_OverlayFiles,
        overlay : RxCore_Overlay,
        overlaymode : RxCore_overlaymode,



        overrideLinewidth : RxCore_overrideLinewidth,

        penTabledialog : RxCore_PenTabledialog,

        setSnapSize : RxCore_setSnapSize,
        setPenTable : RxCore_setPenTable,
        setpentablePen : RxCore_setpentablePen,
        setPenTableScaled : RxCore_setPenTableScaled,

        setPenwidth : RxCore_setPenwidth,


        pageLock :RxCore_PageLock,
        pageOnlyMarkup:RxCore_PageonlyMarkup,
        pagePos : RxCore_PagePos,
        panPage : RxCore_PanPage,
        //panTomarkup : RxCore_panTomarkup,

        panWindow:RxCore_PanWindow,
        pasteMarkUp:RxCore_PasteMarkup,

        getBlockInsert : RxCore_GetBlockInsert,
        getVectorEntity : RxCore_GetVectorEntity,
        pickPolygon : RxCore_PickPolygon,
        pickRxSpace : RxCore_pickRxSpace,
        prevPage:RxCore_PrevPage,
        print:RxCore_Printdocument,
        printDoc:getDocObj,
        printEx:RXCore_PrintdocOptions,
        printSizeEx:RxCore_PrintOptionsSize,
        printhelper:RxCore_printhelper,
        //quickPage : RxCore_quickPage,
        refreshThumbnails : RxCore_refreshThumbnails,
        redrawPage : RxCore_redrawPage,

        redrawCurrentPage : RxCore_redrawCurrentPage,
        removeFloorplan : RxCore_removeFloorplan,
        removeLastPoint : RxCore_removeLastPoint,
        reset3DModel:RxCore_Reset3DModel,

        restoreStartCam : RxCore_restoreStartCam,

        saveresetCam : RxCore_saveresetCam,
        setresetCam : RxCore_setresetCam,

        


        resetBlocks:RxCore_resetBlocks,
        resetLayers:RxCore_resetLayers,
        restoreBlockStates : RxCore_RestoreBlockStates,
        restoreCamera : RxCore_restoreCamera,
        restoreCameraByName : RxCore_restoreCameraByName,
        restoreDefault:RxCore_default,
        reverseNavMouse : RxCore_reverseNavMouse,
        birdseyetool : RxCore_birdseye,
        restoreMouseInput : RxCore_restoreMouseInput,
        restrictPan : RxCore_RestrictPan,
        renderBirdseye : RxCore_renderBirdseye,
        rotate:RxCore_RotateDrawing,
        rotatePage : RxCore_RotatePage,

        rotateThumbnail : RxCore_rotateThumbnail,
        
        rotateClockwise : RxCore_rotateClockwise,

        saveTop3DCamera : RxCore_SaveTop3DCamera,
        save3DCamera : RxCore_Save3DCamera,

        find3DViewAttribute : RxCore_findViewAttribute,
        update3DViewAttribute : RxCore_updateViewAttribute,
        delete3DViewAttribute : RxCore_deleteViewAttribute,
        get3DViewAttributes : RxCore_getViewAttributes,
        add3DViewAttribute : RxCore_addViewAttribute,
        get3DViewAttribute : RxCore_getViewAttribute,


        savePDF : RxCore_savePDF,
        scale:RxCore_scale,
        elementScale : RxCore_elementScale,

        setScaleLabel : RxCore_setScaleLabel,
        setElementScaleLabel : RxCore_setElementScaleLabel,

        setElementDimPrecision : RxCore_setElementDimPrecision,

        resetToDefaultScaleValueForMarkup : RxCore_resetToDefaultScaleValueForMarkup,
        setElementDimPrecision : RxCore_setElementDimPrecision,

        scaleOnResize:RxCore_ScaleonResize,


        select3D : RxCore_3DSelect,
        select3DMarkup : RxCore_3DMarkupSelect,
        search3dAttribute : RxCore_Search3dAttribute,
        search3dAttributes : RxCore_Search3dAttributes,
        search3dAttributesEx : RxCore_Search3dAttributesEx,
        select3DVectorBlock : RxCore_Select3DVectorBlock,
        selectMarkUp : RxCore_MarkupSelect,
        SearchStoreyAttributes : RxCore_SearchStoreyAttributes,
        selectMarkupbyGUID : RxCore_SelectMarkupbyGUID,


        selectMarkupbyGUIDs : RxCore_SelectMarkupbyGUIDs,

        select3DMarkUpByID : RxCore_select3DMarkUpByID,
        selectMarkUpByIndex : RxCore_SelectMarkupbyIndex,
        

        selectSymbName : RxCore_selectSymbName,
        selectSymblib : RxCore_selectSymblib,
        selectVectorBlock : RxCore_SelectVectorBlock,
        unselectAllBlocks : RxCore_UnselectAllBlocks,
        selectRxSpace : RxCore_selectRxSpace,
        selectRxSpaceEdit : RxCore_selectRxSpaceEdit,
        set3DBlockDistance : RxCore_set3DBlockDistance,        
        set3DBlockPosition : RxCore_set3DBlockPosition,
        set3DmodelcurFloor : RxCore_set3DmodelcurFloor,
        setActiveFile : RxCore_setActiveFile,
        setActiveFileEx : setActiveFileEx,
        setAreaPerimeter : RxCore_setAreaPerimeter,
        setArrowSize : RxCore_setArrowSize,
        setTextheightFactor : RxCore_setTextheightFactor,
        setAuthorization : RxCore_setAuth,

        set3DViewSelectedMesh : RxCore_set3DViewSelectedMesh,
        setMarkupMarkedColor : RxCore_setMarkupMarkedColor,

        setBirdseyeFloor : RxCore_setBirdseyeFloor,
        setBlockColor : RxCore_setBlockColor,

        setBlockFill : RxCore_setBlockFill,

        setBlockHatch : RxCore_setBlockHatch,
        setCalibration : RxCore_setCalibration,
        setCompareFiles : RxCore_SetCompareFiles,
        setCompareColors : RxCore_setCompareColors,
        changeSpaceCustomLabel : RxCore_changeSpaceCustomLabel,
        setCompareScale : RxCore_setCompareScale,
        setConfiguration : RxCore_setConfiguration,
        setJSONConfiguration : RxCore_setJSONConfiguration,
        setCustomLabelText : RxCore_setCustomLabelText,
        setDimOffset : RxCore_SetDimOffset,
        
        setFoxitSnap : RxCore_setFoxitSnap,

        useFoxitRotateTo : RxCore_useFoxitRotateTo,

        setinitFile : RxCore_setinitFile,

        ScrollFoxitDiv : RxCore_ScrollFoxitDiv,
        setdisplayBackground : RxCore_setdisplayBackground,
        
        setdisplay : RxCore_setdisplay,

        setFont : RxCore_SetFont,
        setFontFull : RxCore_setFontFull,
        setFontBold : RxCore_SetFontBold,
        setFontHeight : RxCore_SetFontHeight,
        setFontItalic : RxCore_SetFontItalic,
        setGlobalStyle : RxCore_SetGlobalStyle,
        setGUIDMarkupSelected : RxCore_SetGUIDMarkupSelected,
        setHatch : setHatch,

        addFill : addFill,
        addHatch : setHatch,
        addColorFill : setColorFill,
        setHatchDescription : setHatchDescription,

        setCameraTumbWidth : RxCore_setCameraTumbWidth,

        setCountSize : RxCore_setCountSize,
        setCountSizeAll : RxCore_setCountSizeAll,

        setLabelSize : RxCore_setLabelSize,
        setLabelSizeAll : RxCore_setLabelSizeAll,

        setRotateOffsetbirdseye : RxCore_setRotateOffsetbirdseye,

        
        setLargePDFpageLimit : Rxcore_setLargePDFpageLimit,
        setLayout : RxCore_setLayout,
        setLineStyle : RxCore_SetLineStyle,
        setLineWidth : RxCore_SetLineWidth,

        setLineWidthAll : RxCore_SetLineWidthAll,

        setLineWidthMarkup : RxCore_SetLineWidthMarkup,
        setLineWidthUnits : RxCore_SetLineWidthUnits,
        setSegmentLineWidthUnits : RxCore_SetSegmentLineWidthUnits,
        setMarkupLayer : RxCore_SetMarkupLayer,
        setmarkupRxGUID : RxCore_setmarkupRxGUID,
        setMarkupSearchPadding : RxCore_setMarkupSearchPadding,
        setMarkupXMLData : RxCore_setMarkupXMLData,
        setMonoChrome : RxCore_setMonoChrome,
        setpageAs3DNavigator : RxCore_setpageAs3DNavigator,
        draw3DBirdseye : RxCore_draw3DBirdseye,
        drawFoxitAnnotation : RxCore_drawFoxitAnnotation,
        setnavigationScaleOffset : RxCore_setnavigationScaleOffset,
        setnavScaleOffsetPage : RxCore_setnavScaleOffsetPage,
        get3DmodelboxCenter : RxCore_get3DmodelboxCenter,
        set3dNavcameraPos : RxCore_set3dNavcameraPos,
        setPageInfoDim : RxCore_setPageInfoDim,
        setPageDimensions : RxCore_setPageDimensions,
        setPDFAnimateRender : RxCore_setPDFAnimateRender,
        setPDFinitalZoomAll : RxCore_setPDFinitalZoomAll,
        setPDFLayers : RxCore_setPDFLayers,
        setPDFBookmarks : RxCore_setBookmarks,
        getPDFBookmarks : RxCore_getBookmarks,
        setrxprintdiv : setrxprintdiv,
        setRxSpaceColor : RxCore_setRxSpaceColor,
        setscrollZoomKey : RxCore_SetScrollZoomKey,
        singlePageScrollPan : RxCore_SinglePageScrollPan,
        setSingleDocument : RxCore_SetSingleDocument,
        setSpaceID : RxCore_setSpaceID,
        setText : RxCore_setText,
        setUnit : RxCore_SetUnit,
        setElementUnit : RxCore_SetElementUnit,
        
        setVectorBlockArea : RxCore_setVectorBlockArea,

        showMarkupLabels : RxCore_showMarkupLabels,
        
        show3DViewMarkers : RxCore_Show3DViewMarkers,
        
        spaceEdit : RxCore_SpaceEdit,
        suspendDraw : RxCore_suspendDraw,
        switchPages : RxCore_switchpages,
        textSearch : RxCore_textSearch,

        //testmarktext : RxCore_testmarktext,

        markupTextSearch : RxCore_markupTextSearch,
        textSelect : RxCore_TextSelect,
        toggle3DVisible : RxCore_3DToggleVisible,
        toggleBackground : RxCore_togglebackground,
        transparency3D : RxCore_3DTransparency,
        transparency3DEx : RxCore_3DTransparencyEx,
        unSelectAllMarkup : RxCore_unSelectAllMarkup,
        unSelectMarkupbyGUID : RxCore_unSelectMarkupbyGUID,

        unSelectMarkupbyGUIDs : RxCore_unSelectMarkupbyGUIDs,

        unSelectMarkupbyIndex : RxCore_unSelectMarkupbyIndex,

        uploadSignature : uploadSignature,

        useCompanyFeature : RxCore_useCompanyFeature,

        useCredentials : RxCore_UseCredentials,
        useFixedScale : RxCore_UseFixedScale,

        useMarkupscaling : RxCore_useMarkupscaling,

        usesharepointmarkup : usesharepointmarkup,
        usedbmarkup : usedbmarkup,

        useNoScale : RxCore_useNoScale,
        useFraction : RxCore_UseFraction,
        useOrtho : RxCore_UseOrtho,

        userFromLocalStorage : userFromLocalStorage,
        userToLocalStorage : userToLocalStorage,
    
        vectorBlocksAll : RxCore_BlocksAll,
        vectorLayersAll : RxCore_LayersAll,
        viewMode : RxCore_SetViewMode,
        walkThrough3D : RxCore_3DWalkThrough,
        xmlurl : RxCore_xmlurl,
        xmlurlEx : RxCore_xmlurlEx,
        zoomToBlock : RxCore_ZoomToBlock,

        zoomToBlockInsert : RxCore_ZoomToBlockInsert,

        zoomFit : RxCore_ZoomFit,
        zoomHeight : RxCore_ZoomHeight,
        zoomWidth : RxCore_ZoomWidth,
        zoomIn : RxCore_ZoomIn,
        zoomOut : RxCore_ZoomOut,
        zoomPageUpdate : RxCore_ZoomPageUpdate,
        zoomWindow : RxCore_ZoomWindow,
        scrollBarCheck : RxCore_scrollBarCheck,
        setRxResize : RxCore_setRxResize,
        setEndPoints : RxCore_setEndPoints,
        setMarkUpHatch : RxCore_MarkupHatch,
        setBirdseyeCanvas : RxCore_SetBirdseyeCanvas,
        setdimPrecision : RxCore_setdimprecision,

        setDimPrecisionForPage: RxCore_setDimPrecisionForPage,

        usePanToMarkup :  RxCore_usePanToMarkup,
        usePentableUpload : usePentableUpload,

        useConvertUpload : useConvertUpload,

        //usetextscale : RxCore_usetextscale,

        setNoteText : RxCore_setNoteText,
        setTextInputText: RxCore_setTextInputText,
        
        getTextMarkup : RxCore_getTextMarkup,

		getTextInputText: RxCore_getTextInputText,
        setThumbnailctx : RxCore_SetThumbnailctx,
        
        setThumbnailSize : RxCore_setThumbnailSize,

        setBirdsEyeFoxit : RxCore_setBirdsEyeFoxit,
        setPageBitmap : RxCore_setPageBitmap,
        setThumbnailFoxit : RxCore_setThumbnailFoxit,
        getBirdseyeDim : RxCore_getBirdseyeDim,
        setUser : RxCore_setUser,
        transparent3DOn : RxCore_3DTransparentOn,
        set3DBlockTransparency : set3DBlockTransparency,
        set3DBlockNoSelect : set3DBlockNoSelect,
        zoomMarkup : RxCore_ZoomMarkup,

        /* GUI connectors */

        /* Olek new GUI connectors */
        GUI_DocumentSearch: RxCore_GUI_DocumentSearch,
        GUI_RemovePage: RxCore_GUI_RemovePage,
        GUI_PageCountChanged: RxCore_GUI_PageCountChanged,

        /* Olek new GUI connectors */

        GUI_ActivateFile : RxCore_GUI_ActivateFile,
        GUI_2DBlockID : RxCore_GUI_2DBlockID,

        GUI_2DBlock : RxCore_GUI_2DBlock,

        GUI_2DBlockHover : RxCore_GUI_2DBlockHover,

        GUI_2DBlockInfo : RxCore_GUI_2DBlockInfo,

        GUI_2DBlockInfoPos : RxCore_GUI_2DBlockInfoScreen,

        GUI_2DEntityInfoScreen : RxCore_GUI_2DEntityInfoScreen,

        GUI_2DEntityInfo : RxCore_GUI_2DEntityInfo,
        
        GUI_3DCameraSave : Rxcore_GUI_3DCameraSave,

        GUI_3DPartInfo : Rxcore_GUI_3DPartInfo,

        GUI_3DPartState : Rxcore_GUI_3DPartState,

        GUI_3DMarkupInfo : Rxcore_GUI_3DMarkupInfo,
        GUI_3DMarkup : RxCore_GUI_3DMarkup,

        GUI_3DCameraSaves : RxCore_GUI_3DCameraSaves,

        GUI_3DParts : RxCore_GUI_3DParts,
        GUI_3DWalkthrough : RxCore_GUI_3DWalkthrough,

        GUI_3DView : RxCore_GUI_3DView,

        GUI_Calibratediag : RxCore_GUI_Calibratediag,
        GUI_CalibrateComplete : RxCore_GUI_CalibrateComplete,
        GUI_CameraPos : RxCore_GUI_CameraPos,

        GUI_CanSaveTop3DView : Rxcore_GUI_CanSaveTop3DView,

        GUI_CreationComplete : RxCore_GUI_CreationComplete,


        GUI_ContextMenuEvt : RxCore_GUI_ContextMenuEvt,

        GUI_UpdatebirdsEye : RxCore_GUI_UpdatebirdsEye,
        GUI_CompareDiag : RxCore_GUI_CompareDiag,
        GUI_Nav3DreplaceDiag : RxCore_GUI_Nav3DreplaceDiag,
        
        GUI_compareLoadComplete : RxCore_GUI_compareLoadComplete,

        GUI_CompareMeasure : RxCore_GUI_CompareMeasure,
        GUI_CompareAlign : RxCore_GUI_CompareAlign,
        
        GUI_Consolidate : RxCore_GUI_Consolidate,
        GUI_ConsolidateMarkup : RxCore_GUI_ConsolidateMarkup,

        GUI_Download : RxCore_GUI_Download,
        GUI_exportComplete : Rxcore_GUI_exportComplete,
        
        GUI_Servercompare : RxCore_GUI_Servercompare,

        GUI_FileInfo : RxCore_GUI_FileInfo,
        GUI_FileLoadComplete : Rxcore_GUI_fileLoadComplete,
        GUI_fileLoadError : Rxcore_GUI_fileLoadError,
        GUI_search3Denabled : Rxcore_GUI_search3Denabled,
        GUI_FileActivated : RxCore_GUI_FileActivated,
        GUI_floorLoadComplete : Rxcore_GUI_floorLoadComplete,
        GUI_3DNavPosition : RxCore_GUI_3DNavPosition,
        GUI_floorlevel : RxCore_GUI_floorlevel,
        GUI_getResponsHeaders : RxCore_GUI_getResponsHeaders,
        GUI_HasText : RxCore_GUI_HasText,
        GUI_MarkupHover : RxCore_GUI_MarkupHover,
        GUI_Markup : RxCore_GUI_Markup,

        GUI_MarkupChanged : RxCore_GUI_MarkupChanged,

        GUI_MarkupDrawValue : RxCore_GUI_MarkupDrawValue, //returns markup dimtext

        GUI_MarkupIndex : RxCore_GUI_MarkupIndex,
        GUI_MarkupLayers:RxCore_GUI_MarkupLayers,
        GUI_MarkupLink : RxCore_GUI_MarkupLink,
        GUI_Markuplist:RxCore_GUI_Markuplist,

        GUI_MarkuplistJSON : RxCore_GUI_MarkuplistJSON,

        GUI_FoxitAnnotlist : RxCore_GUI_FoxitAnnotlist,

        GUI_3DMarkupList : RxCore_GUI_3DMarkupList,
        GUI_MarkupLoadComplete : Rxcore_GUI_markupLoadComplete,
        GUI_foxitmarkupLoadComplete : Rxcore_GUI_foxitmarkupLoadComplete,
        GUI_markupdrawParams : RxCore_GUI_markupdrawParams,
        GUI_markupParamsError : RxCore_GUI_markupParamsError,
        GUI_MarkupAreaEdit : RxCore_GUI_MarkupAreaEdit,

        GUI_inputEvent : RxCore_GUI_inputEvent,

        GUI_HatchChange : RxCore_GUI_HatchChange,


        GUI_MarkupSave : RxCore_GUI_MarkupSave,

        GUI_MarkupPaths : RxCore_GUI_MarkupPaths,

        GUI_MarkupUnselect : RxCore_GUI_MarkupUnselect,
        GUI_Measurediag : RxCore_GUI_Measurediag,
        GUI_Notediag : RxCore_GUI_Notediag,
        GUI_NumMathces : RxCore_GUI_NumMathces,

        GUI_NumMathcesMarkup : RxCore_GUI_NumMathcesMarkup,
        GUI_NumMathcesRect : RxCore_GUI_NumMathcesRect,

        GUI_MathcesRectsPage : RxCore_GUI_MathcesRectsPage,

        GUI_pageLoadComplete : Rxcore_GUI_pageLoadComplete,

        GUI_scaleListLoadComplete : Rxcore_GUI_scaleListLoadComplete,

        GUI_pagedimLoadComplete : Rxcore_GUI_pagedimLoadComplete,
        GUI_pagethumbs:RxCore_GUI_pagethumbs,
        GUI_pagethumb : RxCore_GUI_pagethumb,
        GUI_floorthumb : RxCore_GUI_floorthumb,
        GUI_FloorplanCanvas : RxCore_GUI_FloorplanCanvas,
        GUI_birdseye : RxCore_GUI_birdseye,
        GUI_PagePosition : RxCore_GUI_PagePosition,
        GUI_Page : RxCore_GUI_Page,
        GUI_PanUpdate : RxCore_GUI_PanUpdate,
        GUI_PanUpdated : RxCore_GUI_PanUpdated,
        GUI_PDFBookmarks : RxCore_GUI_PDFBookmarks,
        GUI_PDFRenderComplete : Rxcore_GUI_PDFRenderComplete,
        GUI_PDFPassWord : Rxcore_GUI_PDFPassWord,
        GUI_Permissions : RxCore_GUI_Permissions,

        GUI_TextCopied : RxCore_GUI_TextCopied,

        GUI_PrintRect : RxCore_GUI_PrintRect,

        GUI_ClipRect : RxCore_GUI_ClipRect,

        GUI_Read3DComplete : RxCore_GUI_Read3DComplete,
        GUI_Ready : RxCore_GUI_Ready,
        GUI_FoxitReady : RxCore_GUI_FoxitReady,
        GUI_RotatePage : RxCore_GUI_RotatePage,
        GUI_RotateDocument : RxCore_GUI_RotateDocument,
        GUI_Resize : RxCore_GUI_Resize,
        GUI_Resize_ext : RxCore_GUI_Resize_ext,
        GUI_Stamps:RxCore_GUI_Stamps,
        GUI_CustomStamps : RxCore_GUI_CustomStamps,
        GUI_State:RxCore_GUI_State,
        GUI_Symbols : RxCore_GUI_Symbols,
        GUI_Textdiag:RxCore_GUI_Textdiag,
        GUI_TextInput : RxCore_GUI_TextInput,
        GUI_Upload : RxCore_GUI_Upload,
        GUI_UndoRedoEmpty : RxCore_GUI_UndoRedoEmpty,
        GUI_Users : RxCore_GUI_Users,

        

        GUI_penTable : RxCore_GUI_penTable,
        GUI_penTableJSON : RxCore_GUI_penTableJSON,
    
        GUI_getsignatureComplete : RxCore_GUI_getsignatureComplete,
        GUI_putsignatureComplete : RxCore_GUI_putsignatureComplete,
    

        GUI_VectorBlocks:RxCore_GUI_VectorBlocks,
        GUI_VectorBlocksLoaded : RxCore_GUI_VectorBlocksLoaded,
        GUI_VectorLayers:RxCore_GUI_VectorLayers,
        GUI_ZoomUpdate : RxCore_GUI_ZoomUpdate,
        GUI_ZoomUpdated : RxCore_GUI_ZoomUpdated,
        GUI_MarkupMeasureRealTimeData : RxCore_GUI_MarkupMeasureRealTimeData
        
    };
   }
    return RxCore;
})();
var RxCore = RxCoreMulti();